# The Codex-host runbook closes the four gaps b run #4 measured, and the spec-filename convention becomes a stated rule at its authoring site

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1139

## Problem

An adopter ran this loop entirely from a Codex CLI host by following `docs/adopting.md`'s `## Using shell-team from Codex CLI` runbook, reached both gates green, and hit four separate places where the runbook was silent or wrong about something the run actually needed. The install step let a marketplace snapshot taken before the release serve a one-release-behind plugin, and the staleness test the runbook does carry is a presence test that cannot see that condition. The spec-filename convention `bin/check-durability.sh` depends on exists only as a precedent, so an adopting repository whose earlier spec lacked the task-id prefix propagated that precedent into a `not-durable` refusal mid-loop. Nobody is told who opens the pull request after `READY_FOR_MERGE`, so the Codex orchestrator stopped and left it to the human. And the standing grant that would have let a mechanics-only re-freeze proceed without a per-instance human turn is documented in `docs/tuning-oversight.md` and never mentioned where a Codex-host adopter is reading. Each gap cost the operator a turn on a run whose whole point was to need none.

## Summarized sources

- `docs/adopting.md` `## Using shell-team from Codex CLI` (`:363`–`:649`) — read first-hand, whole section. Distinctions carried over: step 1 (`:386`–`:402`) names only `codex plugin marketplace add` and `codex plugin add`, tests staleness by the `installed, enabled` STATUS value alone, and closes by deferring to step 2 ("That status alone does not mean the install is current — see step 2"); step 2 (`:418`–`:435`) already carries `codex plugin marketplace upgrade <marketplace>` as a remedy, grounded on `codex plugin marketplace upgrade --help`, and frames the staleness it detects as "installed before this Codex-host feature shipped, or before the fifth role was added to it" — a **presence** test over `bin/gen-codex-agents.sh` and its role list, which by construction cannot distinguish one release behind from current; step 10 (`:552`–`:561`) ends at "Its `APPROVE` reaches `READY_FOR_MERGE` — both gates green" and says nothing about who pushes the branch or opens the pull request; step 3 (`:447`–`:449`) fixes that the generator reads `agents/tech-lead.md`, `agents/pm-spec.md`, `agents/engineer.md`, `agents/qa-verifier.md` and `agents/codex-reviewer.md`. Measured token distribution, which is why every non-vacuity premise below is region-scoped and never file-wide: `marketplace upgrade` occurs in this file only at `:428` (step 2); `VERSION` occurs **zero** times in the whole file; `check-durability` occurs once, at `:763`, outside this section; `class-M` at `:1120` and `:1244`, `tuning-oversight` at `:68` and `:1279`, and every `pull request` / `push` occurrence (`:138`, `:167`, `:172`, `:703`, `:709`, `:721`, `:723`, `:759`, `:851`, `:853`) all lie outside this section too.
- `docs/adopting.ja.md` `## Codex CLI から shell-team を使う` (`:372`–`:655`) — read first-hand. Distinctions carried over: it is a full mirror, step for step — step 1 at `:394`–`:409` (closing with `確認方法と対処は手順 2 を見ること`), step 2's upgrade sentence at `:434`–`:443`, step 10 at `:559`–`:567` (ending at `READY_FOR_MERGE`). Load-bearing measurement: the subcommand `codex plugin marketplace upgrade` is **wrapped across a line break twice** in that sentence — once between `:434`, which ends after `codex`, and `:435`, which opens with `plugin marketplace upgrade <marketplace>`; and again between `:435`, which ends after `codex plugin`, and `:436`, which opens with `marketplace upgrade --help`. That is the measured ground for **D6**'s whitespace-squeeze rule rather than a hypothetical, and it is also why **AC5** asserts the shorter `marketplace upgrade`, whose literal bytes survive both break positions (`:435`, `:436`). Token distribution: `check-durability` once at `:770`, `class-M` at `:1074`/`:1201`, `tuning-oversight.ja.md` at `:72`/`:1237`, `プルリクエスト` at `:170`/`:707`/`:718`/`:729`/`:730` and `push` at `:174`/`:712`/`:766`/`:861`/`:863` — every one outside this section.
- `agents/pm-spec.md` — read first-hand, whole file. Distinctions carried over: `:16` is the **authoring** instruction ("Write a spec to `docs/specs/<slug>.md` and create/update an entry in `tasks/todo.md`") and `:58` is the board-entry template carrying the same legacy path; the token `slug` occurs at exactly those two lines and nowhere else in the file; `task-id` occurs exactly once, at `:98`, and is about the intent-block marker `<!-- BEGIN intent-block: T-NNN -->`, not about a filename; `check-durability` occurs **zero** times; `## Your job` runs `:12`–`:18` and its item 4 carries `READY_FOR_ARCH`; the file carries two generated regions, `<!-- BEGIN/END prompt-block: playbook-pm-spec -->` (`:145`–`:243`) and `<!-- BEGIN/END prompt-block: language -->` (`:245`–`:249`), which this task must leave byte-identical. So the task-id prefix genuinely is absent from this file, and the `## Your job` slice is the one place the authoring instruction lives.
- `bin/gen-codex-agents.sh` `:2`–`:27` — read first-hand at its header contract. Distinctions carried over: it derives one Codex CLI custom-agent TOML per role "from the unmodified `agents/<role>.md`", its default `ROLES` list is `tech-lead pm-spec engineer qa-verifier codex-reviewer` — so `agents/pm-spec.md` is squarely an input — it reads that file's frontmatter and the whole body after it, and **any** validation violation refuses the whole run non-zero with nothing written, never a partial generation. This is the measured ground for **D7**'s ruling that the file is generator *input* and for **AC8**'s run of `tests/codex-agents/run.sh`. Nothing in this task modifies this script.
- `templates/prompt-blocks/registry.txt` `:1`–`:29`, `:31`, `:35`, `:37`, `:45`, `:46`, `:51` — read first-hand. Distinctions carried over, and load-bearing for **AC6** and for `## Notes for engineer`: the registry declares two modes, `marker` (the consumer carries exactly one `BEGIN`/`END` pair for the block and the region between them matches the canonical file byte-for-byte after CRLF and trailing-whitespace normalization) and `contain` (**every non-empty line** of the canonical file must appear verbatim, as a fixed-string substring, somewhere in the consumer). `agents/pm-spec.md` is registered in **six** rows, not two: `marker language.md` (`:31`) and `marker playbook-pm-spec.md` (`:51`) — the two regions **AC6** locks with `cmp` — plus four `contain` rows, `flag-enum.md` (`:35`), `board-line-format.md` (`:37`), `adopter-docs-declaration.md` (`:45`) and `derived-populations.md` (`:46`), which constrain ordinary unmarked prose anywhere in the file. Measured consequence for this task: `templates/prompt-blocks/board-line-format.md` holds exactly two non-empty lines, `- [ ] **T-` and `` ` — spec: ``, both of which live on `agents/pm-spec.md:58` and neither of which names any spec path — so the board-entry template is pinned only through those two fragments, the legacy `docs/specs/<slug>.md` example beside them is pinned by nothing, and **AC6**'s decision to read only the `## Your job` slice leaves that line free either way.
- `bin/check-durability.sh` `:67`–`:74` — read first-hand. Distinctions carried over verbatim in substance: the `specs` registry row's pattern is `<task-id>-*.md`; "a spec file is named `<task-id>-<slug>.md`"; the hyphen is deliberate, keeping a shorter id from matching a longer one's spec; **zero** matches for the glob is `missing-working-file` and **two or more** is `structural` (ambiguous — never a silent pick of whichever the glob returned first, and a reachable state, since this repository's own T-1020 owns two spec files). The registry itself is `templates/durability-records.txt` (`:91`, `:204`). Nothing in this task modifies this script.
- `docs/tuning-oversight.md` `## Who may re-freeze a frozen intent block` (`:105`–`:138`) — read first-hand. Distinctions carried over: the shipped default is a per-instance human GO for **either** class (`:107`, `:109`); class-B — Goal sentence, Non-goals, a criterion's prose, or Input space — always needs that GO; class-M (mechanics repair, a delta confined to `- check:` lines that are broken as a command, vacuous, or measured-contradictory) "may instead cite a standing grant recorded in your own `CLAUDE.local.md`"; `bin/check-refreeze-class.sh` is the mechanical boundary (`:111`); and, load-bearing for **D5**, the grant is recorded "in your own checkout's `CLAUDE.local.md` (**never in a shipped file** — this project does not ship a transcription of your grant, and never invents one on your behalf)" (`:111`). `:99` fixes that the first stop "fires on whether the merge changes what runs", the ground **D4** reasons from. Three things stay human regardless of the grant (`:138`).
- `.shell-team/specs/T-1138-reviewer-host-self-classification.md` — read first-hand, whole file, as the current conformant spec shape: the declaration region's four keys, `## Pre-commitment`'s decided drop order and its factual-vs-contextual trigger wording (`:54`–`:62`, copied in substance here), the borrowed-vocabulary count-premise table with a `Measured` column the freeze run fills, `## Blast radius` as derivation probes rather than counts, the four-read change-set idiom and the region-sliced base-relative check idiom. Also the source of the one paragraph this task must not disturb: T-1138 added `**The self-detected-host observable (T-1138, issue #508).**` at `docs/adopting.md:627`–`:648` and its mirror at `docs/adopting.ja.md:631`–`:654`, each a contiguous block with no internal blank line — the measured ground for **AC7**'s slice shape.
- `.shell-team/todo.md` — read first-hand: the `## Active` heading at `:12`, and the `T-1138` entry at `:5341` with its sub-bullet precedent (`- entry-mode:`, `- dispatch:`, `- count:`, `- freeze-attestation`, `- intent-hash`), which this task's own board entry follows.
- GitHub issue **#509** and the b run #4 host observations (codex-cli 0.154.0, plugin v2.6.1, the marketplace clone's `git reflog` showing only the original clone, `installed, enabled 2.6.0` after a v2.6.1 release, the `not-durable` refusal at the Implement→Validate seam, the Codex orchestrator's "PR 作成・push・merge は実施していません", the class-M per-instance GO) — **relayed** verbatim in the task brief by the coordinating session, which holds the primary copy; this role has no tracker access. See `## Assumptions`.
- `codex plugin marketplace --help`, `codex plugin marketplace upgrade --help` and `codex plugin list` on codex-cli 0.154.0 — **relayed as measured by the coordinating session today**, not by this role. Distinction carried over: the `upgrade` subcommand exists, takes an optional `MARKETPLACE_NAME`, and is described as "Refresh configured Git marketplace snapshots"; `codex plugin list` prints the columns `PLUGIN  STATUS  VERSION  SOURCE`. Existence and argument shape only — what `upgrade` does to a following `plugin add` is **not** carried over from any help text (**D2**).

## Goal

<!-- BEGIN intent-block: T-1139 -->

A reader following the Codex-host runbook is told the four things b run #4 measured it did not tell them, in both languages, and the spec-filename convention stops being a precedent and becomes a stated rule where specs are actually named. Step 1 of the runbook names `codex plugin marketplace upgrade` for a marketplace that was already configured and tells the reader to compare `codex plugin list`'s `VERSION` column against the release they intend to run, cross-referencing step 2's existing remedy rather than duplicating it and saying why step 2's own presence test cannot see a one-release-behind install. The section states the convention `<specs dir>/<task-id>-<slug>.md` as a rule and names `check-durability` as the consumer that depends on it. Step 10 states what happens after `READY_FOR_MERGE` in terms of the loop contract's own `human_gate` list rather than as one fixed behaviour: under the shipped default contract both `merge` and `push` are human gates, so the session stops there and the human pushes the branch and opens the pull request against the base the spec names; a contract whose `human_gate` list omits `push` lets the session push and open the pull request itself; and the merge — and the merge GO — stays the human's under every contract. And one sentence points at `tuning-oversight.md` and names `class-M`, so an adopter who wants a mechanics-only re-freeze to proceed without a per-instance turn knows the knob exists and where their own grant is recorded. The same convention lands at its authoring site: `agents/pm-spec.md`'s `## Your job` instruction names `<task-id>-<slug>.md` instead of `docs/specs/<slug>.md` and the file names `check-durability` as the consumer, with both of that file's generated prompt-block regions byte-identical to their base blobs. Nothing else moves: no `bin/` script is added, changed or removed, no checker enforces the naming rule, the T-1138 paragraph added to both guides is byte-identical to its base blob, and this spec's own filename obeys the rule it writes.

- user-visible: no — derived rather than assumed. Every one of the four gaps names something an adopter could already do before this task and could already do after it: `codex plugin marketplace upgrade` is already documented in step 2 of the same section and already exists as a Codex CLI subcommand; opening a pull request is an ordinary git and forge operation nothing here adds; the class-M standing grant is already documented in `docs/tuning-oversight.md` and the mechanics/class-B boundary it rests on is already drawn by `bin/check-refreeze-class.sh`; and `bin/check-durability.sh` already requires the `<task-id>-*.md` filename shape, so stating it changes who knows it, not what is accepted. The deliverable is a correction to what an adopter **reads** in a runbook they already follow, plus one sentence at the role-prose site that names specs — no new configuration mechanism, no new command, no new adopter-facing behaviour. A `no` carries neither `- adopter-surface:` nor `- adopter-docs-waiver:`, and this declaration carries neither. Release-tier premise on record: **PATCH**.
- verification-class: no-mechanism — ruled rather than defaulted, because one edited file is generator input. The diff reaches `docs/adopting.md`, `docs/adopting.ja.md` and `agents/pm-spec.md` and no path under `bin/`, `tests/`, `templates/` or a CI workflow, and it changes no checker's semantics: `bin/gen-codex-agents.sh`'s own default role list and the prompt-block registry's six consumer rows both treat `agents/pm-spec.md` as **input**, and reading a file whose prose changed is not the same as changing what the reader does. That coupling is discharged by running both suites that consume it (**AC8**) rather than by re-pricing the whole task. So this spec runs **no** full-population two-arm sweep, **no** whole CI-equivalent re-run and **no** behaviour verification of a mechanism it does not touch (`## Non-goals`), its downstream-impact analysis is read-set-scoped with its indirection blind spot disclosed rather than assumed away (`## Blast radius`), CI-equivalence is scoped to the steps whose inputs this diff can reach with every step judged inapplicable named individually (`## Notes for engineer`), and every criterion's verification shape is one of the four this class can honestly carry — a string presence-or-absence grep, a byte-scoped diff, a checker's or suite's exit code, and a records-line existence check. One added obligation applies whatever the class, and is accepted here rather than waived: this task edits a gate surface's own briefing (`agents/pm-spec.md`), so the shipped norm text is read against this task's own spec — which is why this spec's own filename is itself a criterion (**AC10**) — and the read set includes every merged criterion asserting that file's bytes (`## Blast radius`).
- verification-ceiling: unit-and-static — every criterion is a fixed-string read of a committed file or of a named ref's committed blob, a byte-scoped `cmp` of two slices, a committed-blob set comparison, or the exit status of a suite this repository already ships. Nothing here needs a second host, a network call or a third-party CLI. No criterion sits above that ceiling, and none carries `- above-ceiling:`: the one question that would — whether following the corrected runbook on a real Codex host actually removes the operator turns b run #4 spent — is deliberately not written as a criterion at all, because a criterion no command in this repository can evaluate is worse than a disclosed owner, and its owner and occasion are named in `## Assumptions` instead. Non-vacuity: ten of ten criteria are verified at the declared ceiling.
- base-ref-discriminator: if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1 — this branch is stacked behind one still-open predecessor pull request (#514, branch `feature/reviewer-host-self-classification`), and criteria here do read base-side blobs, so the two-arm expression is the value rather than `not-applicable`. It resolves the **branch point** with `git merge-base` and never a `rev-parse` of the predecessor's tip, so a rework round that advances the predecessor after this branch was cut does not move what these criteria call base; it selects its arm by an explicit `git show-ref --verify --quiet` existence test in each namespace — both of which resolve in this checkout — rather than by a `2>/dev/null ||` chain that cannot tell a gone predecessor from a failed `merge-base`; and its third arm is the era-change fallback to this repository's own integration branch `develop`. It is spelled byte-identically in every criterion that reads a base-side blob, and no 40-hex literal is written into any of them. A predecessor deleted unmerged, rebased, force-pushed or squash-merged after this branch was cut is a route-back, not a case either arm is redesigned to survive.

## Non-goals

- **No checker, gate, status flag, loop phase or `bin/` change of any kind** (roadmap M0). In particular nothing enforces the spec-filename convention beyond what `bin/check-durability.sh` already does today: this task states the rule where it is read and where specs are named, and adds no second mechanism to police it. **AC9** locks the `bin/` tracked-name set identical to its base blob and the measured change set over `bin/` empty with no exceptions.
- **No edit to `bin/check-durability.sh`, to `templates/durability-records.txt`, or to the registry pattern.** The `<task-id>-*.md` pattern and its `missing-working-file` / `structural` outcomes are quoted, never changed. **AC9** is the promotion.
- **No edit to `docs/tuning-oversight.md`, and no transcription of the standing-grant text into a shipped file.** That document states the grant is recorded in the adopter's own `CLAUDE.local.md` and never in a shipped file; gap 4's fix is therefore a pointer and a class name, not a copy. **D5** rules it; **AC4** and **AC5** assert the pointer and the class name, and **AC9** locks `docs/tuning-oversight.md` unedited.
- **No claim about what `codex plugin marketplace upgrade` does to a subsequent `codex plugin add`.** The subcommand's existence, its optional `MARKETPLACE_NAME` argument and the `VERSION` column's existence are measured; the effect on a following install is relayed from one adopter run and is never asserted as this repository's own measurement. The operative instruction the runbook gives is the reader's own `VERSION`-column comparison against the release they intend to run. **D2** rules it; no criterion here asserts the effect.
- **No duplication of step 2's existing upgrade remedy into step 1.** Step 1 cross-references step 2 and states why step 2's presence test cannot see a one-release-behind install; it does not restate step 2's paragraph, because two copies of one remedy are two things to keep in sync. **D1** rules it; **AC1** asserts the cross-reference token is retained rather than introduced.
- **No change to the four gaps' subject matter beyond what issue #509 asks.** In particular the hand-off recording shape b run #4 used — `- engineer note` / `- qa note` sub-bullets rather than `### … hand-off` sections, which `bin/check-handoff.sh` accepted — was measured on that run and explicitly carries **no** change request; nothing in this task touches hand-off recording, `bin/check-handoff.sh`, or the board's hand-off grammar. **AC9** locks `bin/` and **AC8** asserts `check-handoff` still exits 0 over this repository's own board.
- **No edit to any `agents/*.md` other than `agents/pm-spec.md`, and no edit to either of that file's generated prompt-block regions.** The eight other role files and both generated regions stay byte-identical to their base blobs. **AC6** locks the two regions with `cmp`; **AC9** locks the other role files.
- **No edit to the T-1138 paragraph in either guide.** The `**The self-detected-host observable (T-1138, issue #508).**` paragraph and its Japanese mirror are byte-identical to their base blobs after this task, whichever neighbouring step or paragraph moves. **AC7** is the promotion.
- **No edit to any documentation file other than `docs/adopting.md` and `docs/adopting.ja.md`**, and no edit to `README.md`, `README.ja.md`, either CHANGELOG, `docs/loop-engineering/`, `.shell-team/lessons.md`, `.shell-team/retros/`, or this repository's `.shell-team/` records other than this task's own `T-1139` files. **AC9** is the promotion.
- **No CHANGELOG entry, no version bump and no release step in this task.**
- **No full-population two-arm sweep, no whole CI-equivalent re-run and no behaviour verification of a mechanism this task does not touch.** Under the operator-approved lightweight mode A2 this sprint runs one two-arm sweep at release rather than one per task, and under the `no-mechanism` verification class the downstream-impact analysis is read-set-scoped; `## Blast radius` carries the derivation probes, the predictions and the disclosed indirection gap until the release sweep settles them.
- **No scope-lock or diff-scope-closure criterion.** **AC9** is a protected-invariant negative over named directories with three named exceptions and a task-id pattern, not an allow-list over the diff.
- **The English and Japanese halves of every gap ship together or not at all.** A one-language edit is the mirror drift this repository's review rounds keep catching. **AC5** is the Japanese mirror of **AC1**–**AC4**'s four gaps.

## Pre-commitment

- **Never-dropped**: the three runbook gaps a Codex-host reader needs in order to run without an operator turn — gap 1 (**AC1**, and its half of **AC5**), gap 3 (**AC3**, and its half of **AC5**) and gap 4 (**AC4**, and its half of **AC5**) — **and** gap 2's runbook sentence (**AC2**, and its half of **AC5**), in both languages. They are the deliverable: issue #509 exists because a reader following this runbook hit each of them, and removing any one leaves the run that motivated the task still needing the turn it needed. Defeat of any of them across two consecutive review rounds **stops the task and returns it to planning** rather than buying a third patch round, per the operator-ratified ruling of 2026-09-06 (T-1121), cited here as an operator ruling and not as AI self-discipline.
- **Droppable, exactly one item, decided now rather than at the moment a trigger fires** — and when it is dropped, the findings accumulated against it travel with it as its requirement list rather than being re-derived at the destination:
  1. **AC6**, gap 2's authoring-site half — the rule sentence in `agents/pm-spec.md`. On a trigger it goes to a new issue the coordinating session files at the moment of dropping, carrying the intended sentence, the measured token distribution from `## Assumptions` and the rounds' findings as its requirement list; the runbook half (**AC2** and its mirror) ships without it. Ground for it being the droppable one, and the only one: it is the single edit whose blast radius leaves documentation — `agents/pm-spec.md` is named in `bin/gen-codex-agents.sh`'s default role list and in six of the prompt-block registry's consumer rows, and it carries two generated regions an edit can disturb — while the four runbook gaps touch two prose files neither of those two configurations names. Dropping it leaves the convention stated where a Codex-host reader meets it and unstated where specs are named, which is a smaller loss than any alternative and is exactly what the follow-up issue then closes.
- **Trigger (same-class-2)**: two consecutive review rounds producing independent new defects in the same component. Factual condition — the same component drew a new, independent finding in each of two consecutive rounds; contextual condition — before a third repair round on that component starts. When the two disagree, the factual condition rules. A trigger on the droppable component executes the drop above; a trigger on a never-dropped component stops the task and returns it to planning.
- **Named fragile component, priced now rather than mid-task**: the region slices every criterion depends on. Each one anchors on the shipped document structure — a `## ` heading, a numbered runbook step, a paragraph's opening bytes — and a slice that silently extracts nothing reads as a clean absence unless something forces it to fail. The expected failure mode is an edit that renumbers a step, introduces a line starting with `## ` or `N. ` inside a fenced block within a sliced region, or breaks an asserted machine token across a line in a way whitespace-squeezing cannot repair. The answer is every criterion's own `test -s` on each slice plus at least one fixed string measured present at the base ref in that same slice, not a looser pattern.

## Acceptance criteria

- [ ] **AC1** Gap 1, English: step 1 names the upgrade and the version comparison, and keeps its cross-reference to step 2 rather than duplicating it. The slice of `docs/adopting.md` running from the line `## Using shell-team from Codex CLI` to the next `## ` heading, further sliced from its first line beginning `1. ` up to (not including) its first subsequent line beginning `2. `, and then whitespace-squeezed to one line so a token wrapped across a line break still matches, contains the fixed strings `codex plugin marketplace upgrade` and `VERSION`. Base non-vacuity, region-scoped deliberately and not file-scoped, because `marketplace upgrade` already occurs at `docs/adopting.md:428` inside step 2: the same slice taken from that file's committed blob at the base ref contains each of those two strings **zero** times. Positive controls, each measured present at the base ref so a slice that extracted nothing cannot read as a pass: both slices are non-empty and both contain `codex plugin marketplace add` and `step 2` — the second of which is the cross-reference this criterion requires **retained**, so **D1**'s no-duplication ruling is visible as an invariant rather than as a new string. Whether the slice explains why step 2's own presence test cannot see a one-release-behind install is a semantic property no fixed-string check can measure and is judged by QA and the cross-provider review.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac1.XXXXXX") || exit 1; sec() { awk '/^## Using shell-team from Codex CLI$/{f=1;next} f&&/^## /{exit} f'; }; st1() { awk '/^1\. /{f=1} f&&/^2\. /{exit} f'; }; sq() { tr '\n' ' ' | tr -s ' '; }; git show "$B:docs/adopting.md" > "$T/bf" 2>/dev/null || rc=1; test -s "$T/bf" || rc=1; sec < docs/adopting.md | st1 | sq > "$T/h"; sec < "$T/bf" | st1 | sq > "$T/b"; test -s "$T/h" || rc=1; test -s "$T/b" || rc=1; for s in 'codex plugin marketplace add' 'step 2'; do grep -Fq -- "$s" "$T/h" || rc=1; grep -Fq -- "$s" "$T/b" || rc=1; done; for s in 'codex plugin marketplace upgrade' 'VERSION'; do grep -Fq -- "$s" "$T/h" || rc=1; if grep -Fq -- "$s" "$T/b"; then rc=1; fi; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC2** Gap 2, English: the runbook states the spec-filename convention as a rule and names its consumer. The whitespace-squeezed slice of `docs/adopting.md` running from `## Using shell-team from Codex CLI` to the next `## ` heading contains the fixed strings `<task-id>-<slug>.md` and `check-durability`. Base non-vacuity, section-scoped deliberately because `check-durability` already occurs at `docs/adopting.md:763` outside this section: the same slice of that file's base-ref blob contains each of those two strings **zero** times. Positive controls, both measured present at the base ref: the slice is non-empty and contains `codex plugin list` and `READY_FOR_MERGE`. Whether the sentence reads as a rule rather than as a precedent, and whether it names the consumer's dependence rather than merely the script, are semantic properties judged by QA and the cross-provider review.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac2.XXXXXX") || exit 1; sec() { awk '/^## Using shell-team from Codex CLI$/{f=1;next} f&&/^## /{exit} f'; }; sq() { tr '\n' ' ' | tr -s ' '; }; git show "$B:docs/adopting.md" > "$T/bf" 2>/dev/null || rc=1; test -s "$T/bf" || rc=1; sec < docs/adopting.md | sq > "$T/h"; sec < "$T/bf" | sq > "$T/b"; test -s "$T/h" || rc=1; test -s "$T/b" || rc=1; for s in 'codex plugin list' 'READY_FOR_MERGE'; do grep -Fq -- "$s" "$T/h" || rc=1; grep -Fq -- "$s" "$T/b" || rc=1; done; for s in '<task-id>-<slug>.md' 'check-durability'; do grep -Fq -- "$s" "$T/h" || rc=1; if grep -Fq -- "$s" "$T/b"; then rc=1; fi; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC3** Gap 3, English: step 10 says who opens the pull request and who merges. The slice of `docs/adopting.md` running from `## Using shell-team from Codex CLI` to the next `## ` heading, further sliced from its first line beginning `10. ` up to (not including) its first subsequent line beginning `11. `, and whitespace-squeezed, contains the fixed strings `push` and `pull request`. Base non-vacuity, region-scoped deliberately because both strings already occur elsewhere in the same file: the same slice of the base-ref blob contains each of them **zero** times. Positive controls, measured present at the base ref: the slice is non-empty and contains `READY_FOR_MERGE`, the flag the new sentence is anchored to. That the merge and the merge GO are stated as the human's, and that the base named is the one the spec names, are semantic properties judged by QA and the cross-provider review.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac3.XXXXXX") || exit 1; sec() { awk '/^## Using shell-team from Codex CLI$/{f=1;next} f&&/^## /{exit} f'; }; st10() { awk '/^10\. /{f=1} f&&/^11\. /{exit} f'; }; sq() { tr '\n' ' ' | tr -s ' '; }; git show "$B:docs/adopting.md" > "$T/bf" 2>/dev/null || rc=1; test -s "$T/bf" || rc=1; sec < docs/adopting.md | st10 | sq > "$T/h"; sec < "$T/bf" | st10 | sq > "$T/b"; test -s "$T/h" || rc=1; test -s "$T/b" || rc=1; grep -Fq -- 'READY_FOR_MERGE' "$T/h" || rc=1; grep -Fq -- 'READY_FOR_MERGE' "$T/b" || rc=1; for s in 'push' 'pull request'; do grep -Fq -- "$s" "$T/h" || rc=1; if grep -Fq -- "$s" "$T/b"; then rc=1; fi; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** Gap 4, English: the section points at the oversight document and names the class, without transcribing the grant. The whitespace-squeezed slice of `docs/adopting.md` running from `## Using shell-team from Codex CLI` to the next `## ` heading contains the fixed strings `tuning-oversight.md` and `class-M`. Base non-vacuity, section-scoped deliberately because both already occur elsewhere in the same file (`:68`, `:1279`, `:1120`, `:1244`): the same slice of the base-ref blob contains each of them **zero** times. Positive controls, measured present at the base ref: the slice is non-empty and contains `codex plugin list`. This criterion is deliberately **not** written as a prohibition on the grant's own wording: a pointer sentence may legitimately paraphrase what the grant covers, and the requirement that the grant text itself is not transcribed into a shipped file is carried by **D5** and by **AC9**'s lock on `docs/tuning-oversight.md`, both judged by QA and the cross-provider review.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac4.XXXXXX") || exit 1; sec() { awk '/^## Using shell-team from Codex CLI$/{f=1;next} f&&/^## /{exit} f'; }; sq() { tr '\n' ' ' | tr -s ' '; }; git show "$B:docs/adopting.md" > "$T/bf" 2>/dev/null || rc=1; test -s "$T/bf" || rc=1; sec < docs/adopting.md | sq > "$T/h"; sec < "$T/bf" | sq > "$T/b"; test -s "$T/h" || rc=1; test -s "$T/b" || rc=1; grep -Fq -- 'codex plugin list' "$T/h" || rc=1; grep -Fq -- 'codex plugin list' "$T/b" || rc=1; for s in 'tuning-oversight.md' 'class-M'; do grep -Fq -- "$s" "$T/h" || rc=1; if grep -Fq -- "$s" "$T/b"; then rc=1; fi; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** The Japanese mirror carries all four gaps, with the same machine tokens, in the same three regions. In `docs/adopting.ja.md`, sliced from `## Codex CLI から shell-team を使う` to the next `## ` heading and whitespace-squeezed: the step-1 sub-slice (first line beginning `1. ` up to the first subsequent `2. `) contains `marketplace upgrade` and `VERSION`; the step-10 sub-slice (first line beginning `10. ` up to the first subsequent `11. `) contains `push` and `プルリクエスト`; and the whole section contains `<task-id>-<slug>.md`, `check-durability`, `tuning-oversight.ja.md` and `class-M`. Base non-vacuity for all eight, each region-scoped for the same measured reason its English counterpart is: the corresponding slice of the base-ref blob contains each of them **zero** times. Positive controls, each measured present at the base ref: all three slices are non-empty; the step-1 slice contains `codex plugin marketplace add` and `手順 2`; the step-10 slice contains `READY_FOR_MERGE`. The gap-1 token is deliberately the shorter `marketplace upgrade` rather than the full subcommand, because the base-ref mirror already wraps that subcommand across a line break at `docs/adopting.ja.md:435`–`:436` and a shorter token survives more break positions; whitespace-squeezing repairs a break **between** tokens and not one **inside** a token, which is why the Japanese term is asserted rather than an English one that could wrap mid-word. Whether the Japanese sentences say the same thing as the English ones is a semantic property no fixed-string check can measure and is judged by QA and the cross-provider review.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac5.XXXXXX") || exit 1; sec() { awk '/^## Codex CLI から shell-team を使う$/{f=1;next} f&&/^## /{exit} f'; }; st1() { awk '/^1\. /{f=1} f&&/^2\. /{exit} f'; }; st10() { awk '/^10\. /{f=1} f&&/^11\. /{exit} f'; }; sq() { tr '\n' ' ' | tr -s ' '; }; git show "$B:docs/adopting.ja.md" > "$T/bf" 2>/dev/null || rc=1; test -s "$T/bf" || rc=1; sec < docs/adopting.ja.md > "$T/hs"; sec < "$T/bf" > "$T/bs"; test -s "$T/hs" || rc=1; test -s "$T/bs" || rc=1; sq < "$T/hs" > "$T/h"; sq < "$T/bs" > "$T/b"; st1 < "$T/hs" | sq > "$T/h1"; st1 < "$T/bs" | sq > "$T/b1"; st10 < "$T/hs" | sq > "$T/h10"; st10 < "$T/bs" | sq > "$T/b10"; for x in h1 b1 h10 b10; do test -s "$T/$x" || rc=1; done; for s in 'codex plugin marketplace add' '手順 2'; do grep -Fq -- "$s" "$T/h1" || rc=1; grep -Fq -- "$s" "$T/b1" || rc=1; done; for s in 'marketplace upgrade' 'VERSION'; do grep -Fq -- "$s" "$T/h1" || rc=1; if grep -Fq -- "$s" "$T/b1"; then rc=1; fi; done; grep -Fq -- 'READY_FOR_MERGE' "$T/h10" || rc=1; grep -Fq -- 'READY_FOR_MERGE' "$T/b10" || rc=1; for s in 'push' 'プルリクエスト'; do grep -Fq -- "$s" "$T/h10" || rc=1; if grep -Fq -- "$s" "$T/b10"; then rc=1; fi; done; for s in '<task-id>-<slug>.md' 'check-durability' 'tuning-oversight.ja.md' 'class-M'; do grep -Fq -- "$s" "$T/h" || rc=1; if grep -Fq -- "$s" "$T/b"; then rc=1; fi; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** Gap 2's authoring site: the instruction that tells this role where to write a spec names the convention, and the generated regions beside it do not move. In `agents/pm-spec.md` at HEAD, the slice running from `## Your job` to the next `## ` heading contains the fixed string `<task-id>-<slug>.md` and contains `docs/specs/<slug>.md` **zero** times; the file as a whole contains `check-durability`. Base non-vacuity and the precise statement of what remains, both measured rather than assumed: the same slice of the base-ref blob contains `docs/specs/<slug>.md` at least once and `<task-id>-<slug>.md` zero times, and the base-ref blob of the whole file contains `check-durability` zero times. The board-entry template under `## tasks/todo.md entry format` sits **outside** this slice and is deliberately left free to keep its legacy example: this criterion is about the authoring instruction only, and says nothing about that template either way. Protected invariant, asserted with `cmp` rather than inferred from `check-prompt-sync` being green: both generated regions — the `playbook-pm-spec` block and the `language` block, each taken inclusive of its own `BEGIN` and `END` marker lines — are byte-identical between the base-ref blob and HEAD. Positive controls: both slices are non-empty and both contain `READY_FOR_ARCH`, and all four extracted prompt-block regions are non-empty.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; F=agents/pm-spec.md; test -s "$F" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac6.XXXXXX") || exit 1; job() { awk '/^## Your job$/{f=1;next} f&&/^## /{exit} f'; }; pb() { awk -v m="$1" 'index($0,"BEGIN prompt-block: " m){f=1} f{print} f&&index($0,"END prompt-block: " m){exit}'; }; git show "$B:$F" > "$T/bf" 2>/dev/null || rc=1; test -s "$T/bf" || rc=1; job < "$F" > "$T/hj"; job < "$T/bf" > "$T/bj"; test -s "$T/hj" || rc=1; test -s "$T/bj" || rc=1; grep -Fq -- 'READY_FOR_ARCH' "$T/hj" || rc=1; grep -Fq -- 'READY_FOR_ARCH' "$T/bj" || rc=1; grep -Fq -- '<task-id>-<slug>.md' "$T/hj" || rc=1; if grep -Fq -- '<task-id>-<slug>.md' "$T/bj"; then rc=1; fi; if grep -Fq -- 'docs/specs/<slug>.md' "$T/hj"; then rc=1; fi; grep -Fq -- 'docs/specs/<slug>.md' "$T/bj" || rc=1; grep -Fq -- 'check-durability' "$F" || rc=1; if grep -Fq -- 'check-durability' "$T/bf"; then rc=1; fi; for m in playbook-pm-spec language; do pb "$m" < "$F" > "$T/h-$m"; pb "$m" < "$T/bf" > "$T/b-$m"; test -s "$T/h-$m" || rc=1; test -s "$T/b-$m" || rc=1; cmp -s "$T/h-$m" "$T/b-$m" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** The T-1138 paragraph this task's own edits sit beside is byte-identical to its base blob, in both languages. The paragraph of `docs/adopting.md` beginning at the line whose first bytes are `**The self-detected-host observable`, taken up to (not including) the first following empty line, is byte-identical between the base-ref blob and HEAD; the paragraph of `docs/adopting.ja.md` beginning at the line whose first bytes are `**self-detected-host という観測可能な事実`, taken the same way, likewise. The empty-line terminator rather than the next `## ` heading is deliberate: it means a new paragraph appended after this one — which gap 4 may well be — does not enter the compared region and cannot fail a criterion that has nothing to say about it, while an edit **inside** the T-1138 paragraph still does. Positive controls: all four extracted paragraphs are non-empty and each contains the fixed string `T-1138`, so a slice that extracted nothing cannot read as a byte-identical pass.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac7.XXXXXX") || exit 1; pen() { awk '/^\*\*The self-detected-host observable/{f=1} f&&/^$/{exit} f'; }; pja() { awk '/^\*\*self-detected-host という観測可能な事実/{f=1} f&&/^$/{exit} f'; }; git show "$B:docs/adopting.md" > "$T/ben" 2>/dev/null || rc=1; git show "$B:docs/adopting.ja.md" > "$T/bja" 2>/dev/null || rc=1; test -s "$T/ben" || rc=1; test -s "$T/bja" || rc=1; pen < docs/adopting.md > "$T/p-hen"; pen < "$T/ben" > "$T/p-ben"; pja < docs/adopting.ja.md > "$T/p-hja"; pja < "$T/bja" > "$T/p-bja"; for x in p-hen p-ben p-hja p-bja; do test -s "$T/$x" || rc=1; grep -Fq -- 'T-1138' "$T/$x" || rc=1; done; cmp -s "$T/p-hen" "$T/p-ben" || rc=1; cmp -s "$T/p-hja" "$T/p-bja" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** The suites whose inputs this diff can reach still pass, and the board still lints. `bash tests/codex-agents/run.sh` exits 0 — asserted because `bin/gen-codex-agents.sh`'s own header names `pm-spec` in its default role list and derives its TOML from the unmodified `agents/pm-spec.md`, which this task edits. `bash tests/check-prompt-sync/run.sh` exits 0 — asserted because `templates/prompt-blocks/registry.txt` registers `agents/pm-spec.md` as a consumer in six rows (`:31`, `:35`, `:37`, `:45`, `:46`, `:51`), two in `marker` mode and four in `contain` mode, so an edit anywhere in that file is within reach of that registry's rules and not only within reach of the two marked regions **AC6** locks. `bash bin/check-handoff.sh` over the board this repository resolves through `bin/team-paths.sh --get todo` exits 0 — the board named through the resolver rather than hardcoded, which is this spec's one criterion besides **AC10** reaching its target through indirection. Positive control: each suite path is non-empty as a file before it is run, so a missing suite fails rather than silently contributing nothing, and the resolved board path is non-empty before it is linted.
  - check: rc=0; export LC_ALL=C; for s in tests/codex-agents/run.sh tests/check-prompt-sync/run.sh; do test -s "$s" || { rc=1; continue; }; bash "$s" >/dev/null 2>&1 || rc=1; done; TD=$(bash bin/team-paths.sh --get todo) || rc=1; test -n "$TD" || rc=1; test -s "$TD" || rc=1; bash bin/check-handoff.sh "$TD" >/dev/null 2>&1 || rc=1; test "$rc" -eq 0
  - stale-at: a task adds or removes a suite under `tests/` that reads `agents/pm-spec.md`, at which point the two suites this criterion enumerates no longer describe the set covering this task's edit surface and the enumeration must be re-derived from `tests/` itself.

- [ ] **AC9** No mechanism ships, and everything the Non-goals exclude is untouched — the protected-invariant negative promoting eight Non-goals at once. The set of tracked file names under `bin/` at HEAD is identical to the set at the base ref, so no `bin/` script is added or removed. The measured change set is **empty** over the union `bin`, `templates`, `tests`, `skills`, `.github`, `agents`, `docs`, `README.md`, `README.ja.md`, `CHANGELOG.md`, `CHANGELOG.ja.md`, `.shell-team/lessons.md`, `.shell-team/retros`, `.shell-team/specs`, `.shell-team/reviews`, `.shell-team/provenance` and `.shell-team/interventions`, with exactly three paths excepted by name — `agents/pm-spec.md`, `docs/adopting.md` and `docs/adopting.ja.md` — and this task's own files excepted by the pattern `*T-1139*`. That union locks `bin/check-durability.sh`, `bin/check-handoff.sh`, `bin/check-refreeze-class.sh`, `templates/durability-records.txt`, `docs/tuning-oversight.md`, every other `agents/*.md` and every generated playbook block unedited. The measured change set is the union of an unbroken chain of four reads spanning base → HEAD → index → worktree, each of the three `git diff` reads spelled with `--no-renames` so a rename's source path is never invisible to all four at once, plus a read for untracked strays a range cannot see. This is **not** a scope lock: it carries no allow-list of permitted paths, it names directories the Non-goals already exclude, and it names its three exceptions by path and its fourth by pattern. Positive controls, floors and never equalities so ordinary growth cannot invalidate them: the base-ref `bin/` name set is non-empty and holds at least twenty entries, and the pathspec union holds at least twenty tracked files.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/feature/reviewer-host-self-classification; then B=$(git merge-base feature/reviewer-host-self-classification HEAD); elif git show-ref --verify --quiet refs/remotes/origin/feature/reviewer-host-self-classification; then B=$(git merge-base refs/remotes/origin/feature/reviewer-host-self-classification HEAD); else B=$(git merge-base develop HEAD); fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1139ac9.XXXXXX") || exit 1; git ls-tree -r --name-only "$B" -- bin/ | sort > "$T/bb"; git ls-files -- bin/ | sort > "$T/bh"; test -s "$T/bb" || rc=1; cmp -s "$T/bb" "$T/bh" || rc=1; test "$(grep -c . "$T/bb" || true)" -ge 20 || rc=1; test "$(git ls-files -- bin templates tests skills .github agents docs README.md README.ja.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md .shell-team/retros .shell-team/specs .shell-team/reviews .shell-team/provenance .shell-team/interventions | wc -l | tr -d ' ')" -ge 20 || rc=1; test -z "$(git diff --no-renames --name-only "$B"...HEAD -- bin templates tests skills .github agents docs README.md README.ja.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md .shell-team/retros .shell-team/specs .shell-team/reviews .shell-team/provenance .shell-team/interventions ':!agents/pm-spec.md' ':!docs/adopting.md' ':!docs/adopting.ja.md' ':!*T-1139*')" || rc=1; test -z "$(git diff --no-renames --cached --name-only -- bin templates tests skills .github agents docs README.md README.ja.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md .shell-team/retros .shell-team/specs .shell-team/reviews .shell-team/provenance .shell-team/interventions ':!agents/pm-spec.md' ':!docs/adopting.md' ':!docs/adopting.ja.md' ':!*T-1139*')" || rc=1; test -z "$(git diff --no-renames --name-only -- bin templates tests skills .github agents docs README.md README.ja.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md .shell-team/retros .shell-team/specs .shell-team/reviews .shell-team/provenance .shell-team/interventions ':!agents/pm-spec.md' ':!docs/adopting.md' ':!docs/adopting.ja.md' ':!*T-1139*')" || rc=1; test -z "$(git ls-files --others --exclude-standard -- bin templates tests skills .github agents docs README.md README.ja.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md .shell-team/retros .shell-team/specs .shell-team/reviews .shell-team/provenance .shell-team/interventions ':!agents/pm-spec.md' ':!docs/adopting.md' ':!docs/adopting.ja.md' ':!*T-1139*')" || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task adds or removes a tracked file under `bin/` or under any of the seventeen pathspecs the union names, at which point the twenty-file floors these positive controls count no longer describe those populations and both floors must be re-read against them.

- [ ] **AC10** This task's own spec obeys the rule this task writes — the dogfooding half of the added obligation a norm-editing task carries. In the specs directory this repository resolves through `bin/team-paths.sh --get specs`, never a hardcoded path, the glob `T-1139-*.md` matches **exactly one** file, and that file is `T-1139-codex-runbook-gaps.md` — the two conditions `bin/check-durability.sh`'s `specs` registry row needs in order to resolve this task unambiguously, since zero matches is `missing-working-file` and two or more is `structural`. The cited anchor is confirmed still present rather than assumed: `bin/check-durability.sh` is readable and contains the fixed string `<task-id>-*.md`. The exactly-one count is pinned to this task's own id and is not a growing population, so it needs no re-measurement trigger; the positive control beside it is a floor over the whole corpus — at least twenty `T-*.md` spec files are found — so an empty or misresolved directory cannot pass. This criterion deliberately does **not** invoke `bin/check-durability.sh` itself: that checker needs a phase, a task and a ref and observes provenance and review records this task has not yet produced, so running it here would fail for reasons this criterion has nothing to say about.
  - check: rc=0; export LC_ALL=C; SP=$(bash bin/team-paths.sh --get specs) || exit 1; test -n "$SP" || exit 1; test -d "$SP" || exit 1; n=0; for f in "$SP"/T-1139-*.md; do test -f "$f" || continue; n=$((n + 1)); done; test "$n" -eq 1 || rc=1; test -f "$SP/T-1139-codex-runbook-gaps.md" || rc=1; m=0; for f in "$SP"/T-*.md; do test -f "$f" || continue; m=$((m + 1)); done; test "$m" -ge 20 || rc=1; test -r bin/check-durability.sh || rc=1; grep -Fq -- '<task-id>-*.md' bin/check-durability.sh || rc=1; test "$rc" -eq 0
  - stale-at: a task adds or removes a spec file under the directory `bin/team-paths.sh --get specs` resolves, at which point the twenty-spec floor this criterion's positive control counts no longer describes that corpus and the floor must be re-read against it.

## Input space

**Reachable input classes** — what this repository's own committed corpus, this loop's own roles and an adopter's runs actually produce, all of which the implementation must handle correctly:

1. `docs/adopting.md` and `docs/adopting.ja.md` as committed UTF-8 markdown carrying a `## `-delimited section whose body is a numbered runbook of eleven steps, each step's continuation lines indented — the structure every slice in **AC1**–**AC5** anchors on.
2. A machine token wrapped across a line break inside a prose paragraph, with the break falling **between** two of the token's whitespace-delimited words. Measured, not hypothetical: `docs/adopting.ja.md:435`–`:436` wraps `codex plugin marketplace upgrade` exactly this way at the base ref. Handled by whitespace-squeezing each slice before matching (**D6**).
3. A token that occurs legitimately elsewhere in the same file, outside the region a criterion is about — `marketplace upgrade` in step 2, `check-durability` at `:763` / `:770`, `class-M`, `tuning-oversight`, `pull request`, `push`, `プルリクエスト`. Every non-vacuity premise is therefore region-scoped and never file-wide (**AC1**–**AC5**, `## Assumptions`).
4. `agents/pm-spec.md` carrying two generated prompt-block regions between `<!-- BEGIN/END prompt-block: … -->` markers, which an edit elsewhere in the file must leave byte-identical (**AC6**).
5. A paragraph delivered by the immediately preceding stacked task (T-1138) sitting at the end of the same section this task edits, with a new paragraph plausibly appended after it (**AC7**).
6. Either supported operating layout — the `.shell-team/` default and the legacy `tasks/` one — and a `TEAM_RUN_BASE` or equivalent override, wherever a criterion names the board or the specs directory: resolved through `bin/team-paths.sh` rather than hardcoded (**AC8**, **AC10**).
7. A working tree carrying this task's own uncommitted or untracked records at the moment the criteria run — the ordinary state at the QA seam, which is why **AC9** excepts `*T-1139*` by pattern across all four of its reads rather than demanding a clean tree.
8. A checkout in which the predecessor branch resolves as a local ref, as a remote-tracking ref only, or has been merged and deleted — the three arms the base-ref discriminator selects between by explicit existence test.
9. **Adopter-side, addressed by the prose rather than executed here**: a Codex CLI host whose marketplace clone was taken before the release the adopter intends to run, so `codex plugin list` reports `installed, enabled` at a version one release behind; and an adopting repository whose earlier specs are named `<slug>.md` with no task-id prefix, so the precedent propagates into the next spec written there.

**Out-of-scope synthetic extremes** — named concretely, and declined:

- **Any assertion about what `codex plugin marketplace upgrade` does to a subsequent `codex plugin add`.** Declined by construction: the effect is relayed from one adopter run and a foreign CLI's internal behaviour is never inferred from its `--help` output. The runbook's operative instruction is the reader's own `VERSION`-column comparison, and no criterion here asserts the effect (**D2**).
- **A Codex CLI at a version other than 0.154.0 whose `plugin` subcommands or `plugin list` columns differ.** Declined and disclosed: the runbook names the version its observations were measured on, exactly as the surrounding steps already do, and an adopter on a different version reads their own `--help`.
- **A restructured runbook** whose steps are no longer numbered, or in which step 1, step 2 or step 10 has been renumbered. Declined: the criteria's anchors are the shipped structure, and renumbering the runbook is a different task that would re-author these criteria rather than one these criteria should survive.
- **A machine token broken across a line break *inside* one of its own whitespace-delimited words** (`market-` / `place upgrade`). Declined: whitespace-squeezing repairs a break between tokens and not one inside a token, and every asserted string is required to sit unbroken on one line (`## Notes for engineer`).
- **A line beginning `## ` or `N. ` introduced inside a fenced code block within a sliced region**, which would truncate a slice early. Declined as an authoring constraint stated to the engineer rather than a parser to build: a markdown-aware slicer is a mechanism this task's own Non-goals exclude, and the `test -s` plus base-present-control pair in every criterion turns a truncated slice into a failure rather than a silent pass.
- **Adversarial or malformed document bytes**: non-UTF-8 sequences, CRLF line endings, a missing trailing newline, a zero-width character inside an asserted token. Not produced by this tree's authoring path; these files are this repository's own authored prose under its own security invariant, not untrusted input.
- **Records of unbounded size, or a specs directory holding thousands of files.** Real corpora here are in the hundreds; nothing in these criteria scales worse than a per-line scan.
- **Cost, latency or token bounds for anything this task touches.**

<!-- END intent-block: T-1139 -->

## Decision points ruled in this spec

| # | Decision | Ruling | Why the alternatives lost |
|---|---|---|---|
| **D1** | Where gap 1's upgrade instruction lives | **In step 1, as a new instruction plus a retained cross-reference to step 2 — never a second copy of step 2's remedy paragraph** | Putting it only in step 2 loses on measurement: step 2's staleness test is a **presence** test over `bin/gen-codex-agents.sh` and its role list (`docs/adopting.md:418`–`:427`), designed for "the install predates the feature", and a one-release-behind install passes it — the file is present and the role list is complete, and nothing in that test can see the version. Duplicating step 2's paragraph into step 1 loses on maintenance: two copies of one remedy drift, and this section already carries the remedy once with its `--help` grounding. Retaining the `step 2` cross-reference as an invariant rather than adding a new one is what makes the no-duplication ruling mechanically visible (**AC1**) |
| **D2** | What the reader is told to compare | **`codex plugin list`'s `VERSION` column against the release they intend to run** | The alternative — telling the reader that running `upgrade` makes the next `plugin add` install the current release — asserts a foreign CLI's internal behaviour, which this repository does not do from a help text (the T-1135 rule). What is measured here is only that the subcommand exists, takes an optional `MARKETPLACE_NAME`, is described as refreshing configured Git marketplace snapshots, and that `plugin list` prints a `VERSION` column. A reader who compares that column against the release they want has an observation they can make themselves and that stays true whatever `upgrade` does internally; a reader told an effect has a claim nobody in this repository measured |
| **D3** | Where gap 2's convention is stated | **Both in the runbook section and at the authoring site `agents/pm-spec.md`, naming `check-durability` as the consumer in each** | Runbook-only loses on causation: the precedent propagated through the **role that names specs**, not through the runbook, and b run #4's `pm-spec` followed the repository's existing `<slug>.md` precedent because its own instruction told it to write `docs/specs/<slug>.md`. Authoring-site-only loses because a Codex-host operator reading the runbook is the one who has to recognize a `not-durable` refusal for what it is. A third alternative — a checker enforcing the filename — loses outright on roadmap M0 and on redundancy: `bin/check-durability.sh` already refuses, and the gap was never that nothing refused, it was that nothing said so beforehand |
| **D4** | Who opens the pull request | **Stated in terms of the loop contract's `human_gate` list: under the shipped default (`templates/shell-team.contract.yaml`, `human_gate: [merge, push]`) the session stops at `READY_FOR_MERGE` and the human pushes and opens the pull request; a contract that omits `push` from `human_gate` lets the session push and open it; the merge and the merge GO are the human's under every contract** (v2 ruling — v1 stated the omit-`push` behaviour as if it were the default, contradicting the shipped template; found by cross-provider review round 1 Blocker) | Stating one fixed behaviour loses on measurement: the shipped contract template and this repository's own contract both list `push` beside `merge` under `human_gate` and both say "must not merge or push without a human gate", so "the session pushes" is true only of a contract an adopter has edited. Saying nothing — b run #4's state — loses because it produced the ambiguity that stopped the run. Making `push` non-gated in the shipped template is a mechanism/default change this docs task cannot make (`## Non-goals`, AC9). `docs/tuning-oversight.md:99`'s "the first stop fires on whether the merge changes what runs" explains why an adopter may drop `push`; it does not make that the default |
| **D5** | What gap 4's sentence may contain | **A pointer to `tuning-oversight.md` (`tuning-oversight.ja.md` in the mirror) and the class name `class-M` — never a transcription of the grant text** | `docs/tuning-oversight.md:111` states the grant is recorded in the adopter's own `CLAUDE.local.md` and **never in a shipped file**, and that this project "does not ship a transcription of your grant, and never invents one on your behalf". A runbook that pasted the grant block would ship exactly the transcription that document forbids, and would additionally go stale against it. A pointer plus a class name gives the reader the knob's name and its home in one sentence, and `docs/tuning-oversight.md` stays the single authority (**AC9** locks it unedited) |
| **D6** | How a criterion reads a sliced region | **Slice on the shipped structure — a `## ` heading, a numbered step's leading `N. `, a paragraph's opening bytes — then squeeze whitespace to one line before matching a fixed string; pair every absence assertion with a `test -s` and at least one string measured present at the base ref in that same slice** | An unsqueezed match loses on a measured case: `docs/adopting.ja.md:435`–`:436` already wraps `codex plugin marketplace upgrade` across a line break at the base ref, so a fixed-string grep over raw lines would miss a token that is genuinely there. A file-wide match loses on every one of the nine tokens measured to occur legitimately elsewhere in the same file. A looser pattern instead of the `test -s`-plus-control pair loses because a slice that extracted nothing reads as a clean absence, which is the failure mode this criteria set is most exposed to (`## Pre-commitment`, named fragile component) |
| **D7** | The verification class, given that one edited file is generator input | **`no-mechanism`** | The diff reaches no path under `bin/`, `tests/`, `templates/` or a CI workflow, and changes no checker's semantics. `bin/gen-codex-agents.sh` and `bin/check-prompt-sync.sh` read `agents/pm-spec.md` as input; a reader whose input's prose changed has not itself changed. Ruling `mechanism` on that basis loses on proportion — it would buy a full-population two-arm sweep and a whole CI-equivalent re-run to prove that a prose sentence in a role file did not move a checker — and the genuine coupling is discharged far more cheaply by running the two suites that consume the file (**AC8**) and by locking both generated regions byte-identical (**AC6**). What is **not** waived is the added obligation a norm-editing task carries whatever its class: this spec is read against the norm text it ships (**AC10**), and the read set includes every merged criterion asserting `agents/pm-spec.md`'s bytes (`## Blast radius`) |

## Body-to-AC correspondence

| # | Body directive | Promoted to |
|---|---|---|
| 1 | Step 1 names `codex plugin marketplace upgrade` and the `VERSION` comparison, cross-referencing step 2 rather than duplicating it | **AC1** (two new strings measured zero at base; `step 2` and `codex plugin marketplace add` retained as base-present controls) and **AC5** (the Japanese half) |
| 2 | Step 1 says why step 2's presence test cannot see a one-release-behind install | **AC1** promotes the measurable half (the region carries the upgrade and the version tokens). That the sentence explains the reason is a semantic property, stated in the criterion's own body as judged by QA and the cross-provider review; **D1** records the ruling |
| 3 | The section states `<specs dir>/<task-id>-<slug>.md` as a rule and names `check-durability` as the consumer | **AC2** and **AC5** |
| 4 | Step 10 states, in terms of the contract's `human_gate` list, who pushes and opens the pull request under the shipped default and under a contract that omits `push`, and that the merge and the merge GO are the human's under every contract | **AC3** (`push`, `pull request`, with `READY_FOR_MERGE` as the base-present control) and **AC5** (`push`, `プルリクエスト`). That the merge is stated as the human's is semantic, named in **AC3**'s body; **D4** records the ruling |
| 5 | One sentence points at `tuning-oversight.md` and names `class-M` | **AC4** and **AC5** |
| 6 | The grant's own text is never transcribed into a shipped file | **AC9** promotes the measurable half (`docs/tuning-oversight.md` is unedited, and this section's own pointer is the only new mention). That the runbook sentence points rather than transcribes is semantic, named in **AC4**'s body; **D5** records the ruling on `docs/tuning-oversight.md:111`'s own words |
| 7 | `agents/pm-spec.md`'s `## Your job` instruction names `<task-id>-<slug>.md` and not `docs/specs/<slug>.md`, and the file names `check-durability` | **AC6** |
| 8 | The board-entry template under `## tasks/todo.md entry format` may keep its legacy example | **AC6** promotes it precisely: the slice it reads stops at the next `## ` heading, so that template is outside it and is asserted neither present nor absent — stated in **AC6**'s own body so the permission is explicit rather than an accident of slicing |
| 9 | Both generated prompt-block regions in `agents/pm-spec.md` are byte-identical to their base blobs | **AC6** (two `cmp` comparisons), with **AC8** asserting `check-prompt-sync`'s suite green independently |
| 10 | The T-1138 paragraph in both guides is byte-identical to its base blob | **AC7** |
| 11 | The English and Japanese halves ship together or not at all | **AC5** is the mirror of **AC1**–**AC4** and fails as a whole if any Japanese half is missing; that both must ship is additionally the last Non-goal |
| 12 | No new `bin/` script, checker, gate, status flag or loop phase; `bin/check-durability.sh`, `templates/durability-records.txt` and `docs/tuning-oversight.md` unedited; no other `agents/*.md` or docs file edited; no CHANGELOG, retro, lessons or foreign record touched | **AC9** (the `bin/` name-set identity plus the four-read empty change set over a seventeen-pathspec union with three named exceptions and one pattern exception) |
| 13 | Nothing enforces the naming rule in any checker | **AC9** promotes the measurable half (no `bin/` file is added or changed). That no enforcement exists is info-only (not promoted to AC) — a negative about work nobody does, discharged by this spec's criteria set naming no new checker, gate or flag |
| 14 | No claim about what `upgrade` does to a following `plugin add` | info-only (not promoted to AC) — an absence of a claim cannot be grepped for without forbidding words the runbook may legitimately use; **D2** is the ruling a reviewer answers from, and the positive requirement it replaces (the `VERSION` comparison) **is** promoted, in **AC1** and **AC5** |
| 15 | The hand-off recording shape b run #4 used is explicitly not changed | **AC9** promotes the measurable half (`bin/` unchanged, so `bin/check-handoff.sh` is untouched) and **AC8** asserts that checker still exits 0 over this repository's own board |
| 16 | This spec's own filename obeys the rule it writes, and `bin/check-durability.sh`'s cited anchor is still present | **AC10** |
| 17 | No CHANGELOG entry, no version bump, no release step | **AC9** promotes the measurable half (both CHANGELOGs unchanged). The rest is info-only — release dispositions settled by the operator's planning approval and re-derived at the freeze-time version-derivation gate, not properties of this diff |
| 18 | No full-population sweep, no whole CI-equivalent re-run, no behaviour verification of an untouched mechanism, and no scope-lock criterion | info-only (not promoted to AC) — mode A2's sprint-level disposition and the `no-mechanism` class's own pricing, settled at the release sweep. **AC9** is deliberately not a scope lock, for the three reasons its own body states |
| 19 | The Pre-commitment's single droppable item, its trigger, and its named fragile component | info-only (not promoted to AC) — a process commitment addressed to the reviewing gates and the coordinating session, not a property of the deliverable any check line can read. It sits inside the frozen block deliberately, so that executing the trigger later confirms a pre-decided disposition instead of opening the design question at the worst moment |
| 20 | The `agents/pm-spec.md` generator/registry coupling is discharged by running both suites that consume that file, rather than by re-pricing the task as `mechanism` | **AC8** (both suite exit statuses, plus the board lint), with **AC6**'s two `cmp` comparisons covering the marker-mode half independently of the suite |
| 21 | Every criterion is verified at the `unit-and-static` ceiling and none sits above it; the one question that would — whether the corrected runbook removes the operator turns b run #4 spent — is deliberately not written as a criterion | info-only (not promoted to AC), and necessarily so: an unmeasurable question promoted to a criterion is the vacuous criterion the ceiling declaration exists to prevent. Its owner and occasion are named in `## Assumptions`, and the declaration's own non-vacuity clause (ten of ten at the ceiling) is what makes the absence of any `- above-ceiling:` sub-bullet honest rather than convenient |
| 22 | Every asserted machine token sits unbroken on one line; no `## ` or `N. ` line is introduced inside a fenced block in a sliced region | info-only (not promoted to AC) — an authoring constraint on the engineer, declared in `## Input space`'s out-of-scope list and repeated in `## Notes for engineer`. Promoting it would require a markdown-aware parser this task's Non-goals exclude; the `test -s`-plus-base-present-control pair in every criterion is what turns a violation into a failure rather than a silent pass |

## Blast radius

Predictions, named instances and derivation probes — not a measurement: `pm-spec` holds no shell. Under the `no-mechanism` verification class the downstream-impact analysis is **read-set-scoped** rather than a full-population diff: the set of merged criteria that read any path this task edits is derived mechanically, each is run at the base ref's committed blobs and again at HEAD, and the two verdict sets are differenced criterion by criterion. Under mode A2 the full-population two-arm sweep runs once at release rather than per task, so these predictions settle there. No population total or set delta is stated here by eye; the probes below are derivation commands the freeze run measures against the branch point's committed blobs, and the coordinating session transcribes each as a conformant `- count:` row onto the board once it carries an integer. Each probe resolves its base ref with the single expression the declaration region fixes, elided here for width and not optional.

- population probe — specs-naming-adopting-en — command: `git grep -lF 'docs/adopting.md' "$B" -- '.shell-team/specs/*.md' | wc -l | tr -d ' '`
- population probe — specs-naming-adopting-ja — command: `git grep -lF 'docs/adopting.ja.md' "$B" -- '.shell-team/specs/*.md' | wc -l | tr -d ' '`
- population probe — specs-naming-pm-spec-agent — command: `git grep -lF 'agents/pm-spec.md' "$B" -- '.shell-team/specs/*.md' | wc -l | tr -d ' '`

- **The read-set-scoped analysis's own scope, stated so it is not inferred.** The three probes above enumerate the candidate members by literal bytes; the analysis runs each candidate's criteria at both arms. The third probe's population is additionally the **added obligation** a norm-editing task carries: because this task edits a gate surface's own briefing (`agents/pm-spec.md`), every merged criterion asserting that file's bytes belongs in the read set whether or not it looks related.
- **Predicted green, with the instances that matter already read.** `.shell-team/specs/T-1138-reviewer-host-self-classification.md`'s **AC8** and **AC13** read the `## Using shell-team from Codex CLI` slice of `docs/adopting.md` and the `## Codex CLI から shell-team を使う` slice of `docs/adopting.ja.md` for the fixed strings `self-detected-host`, `claude -p`, `APPROVE`, `CODEX_THREAD_ID`, `spawn_agent`, `unmeasured` and `未計測`, plus base-ref zero counts on immutable blobs. This task adds strings to those same slices and removes none, so every one of those presence clauses stays satisfied and every base-side clause reads an immutable blob; **AC7** here additionally locks the paragraph those criteria describe byte-identical. `.shell-team/specs/T-1134-codex-host-slice1.md` and `T-1135-codex-host-slice2.md` read the same section for the Codex-host recipe; the same reasoning applies.
- **Predicted green, by class**: every criterion over `bin/`, `templates/`, `tests/`, `skills/`, `.github/` and `README*` (**AC9** asserts those unions empty, so a byte-identity lock on any of them stays green); every criterion over `CHANGELOG*.md`, `.shell-team/lessons.md`, `.shell-team/retros/` and the generated `templates/prompt-blocks/playbook-*.md` (same union); every criterion over `agents/*.md` other than `agents/pm-spec.md`.
- **Predicted at risk, named rather than hidden**: any merged criterion asserting a **byte-identity lock** over `docs/adopting.md`, `docs/adopting.ja.md` or `agents/pm-spec.md` as a whole file; any asserting a line-count or a line anchor inside those three; and any asserting that `docs/specs/<slug>.md` occurs in `agents/pm-spec.md` a particular number of times, or that the `## Your job` item 3 carries that exact path — **AC6** removes it from that slice by design. This spec does not chase them: the release sweep measures them, and a merged criterion found asserting any of those has its disposition recorded there rather than pre-empted here.
- **Not predictable from this role, and disclosed rather than assumed away**: a merged criterion that reaches any of the three edited paths through **indirection** — a path built at run time from `bin/team-paths.sh --get …`, a file named only through a variable, a loop over a role list — matches no search for those paths' literal bytes and is invisible to the probes above in principle. That class is named here and discharged at the release sweep's full-population two-arm diff, not by widening a pattern. This spec's own **AC8** and **AC10** are two live examples of the class: each names its target only through `bin/team-paths.sh --get todo` / `--get specs`.

## Assumptions

- **Relayed — GitHub issue #509 and every b run #4 host observation in it.** The four gaps, and each one's measured clause — the marketplace clone under `<home>/.codex/.tmp/marketplaces/<marketplace>` taken the previous afternoon; `installed, enabled 2.6.0` after v2.6.1 was published; `git reflog` in that clone showing only the original clone; the adopting repository's earlier `<slug>.md` spec and the resulting `not-durable` at the Implement→Validate seam; the Codex orchestrator's "PR 作成・push・merge は実施していません"; the class-M per-instance GO for a broken `- check:` line — reached this role in the task brief from the coordinating session, which holds the primary copy. This role has no tracker access and no access to that host. They ground this spec's Goal, its criteria and its Non-goals. The freeze run confirms issue #509 exists and its body matches the relayed text, and reports the value beside this line.
- **Relayed, measured by the coordinating session today on codex-cli 0.154.0, not by this role** — `codex plugin marketplace --help` lists `upgrade  Refresh configured Git marketplace snapshots`; `codex plugin marketplace upgrade --help` reads "Refresh configured Git marketplace snapshots. Omit MARKETPLACE_NAME to upgrade all configured Git marketplaces. Usage: codex plugin marketplace upgrade [OPTIONS] [MARKETPLACE_NAME]"; `codex plugin list` prints the columns `PLUGIN  STATUS  VERSION  SOURCE`. **Existence and argument shape only.** What `upgrade` does to a following `plugin add` is relayed from b run #4 and is not carried into any frozen sentence or any criterion (**D2**).
- **Relayed, to be measured at the freeze** — that pull request **#514** on branch `feature/reviewer-host-self-classification` is still **open**. Whether a predecessor pull request is open is a fact of the repository's state of record rather than of this checkout, so the freeze run confirms it before the `- base-ref-discriminator` declaration is frozen. What this role measured from this checkout instead, and what the declaration's arm selection actually turns on: both `refs/heads/feature/reviewer-host-self-classification` and `refs/remotes/origin/feature/reviewer-host-self-classification` resolve here, so the first arm fires and the second is a real fallback rather than a dead branch. Should #514 be found already merged and its branch deleted, the era-change fallback to `develop` is the correct arm and no criterion changes.
- **Measured by this role, 2026-09-13, by reading the working tree of this checkout** — the branch is clean and its tip equals the predecessor's tip, so these working-tree reads coincide with the branch point's committed blobs; the freeze run re-measures each at `$B` regardless, and the borrowed-vocabulary table below is where those re-measurements are recorded. The token distribution measured: in `docs/adopting.md`, `marketplace upgrade` occurs only at `:428`, `VERSION` occurs **zero** times in the whole file, `check-durability` once at `:763`, `class-M` at `:1120`/`:1244`, `tuning-oversight` at `:68`/`:1279`, and `pull request` / `push` at `:138`, `:167`, `:172`, `:703`, `:709`, `:721`, `:723`, `:759`, `:851`, `:853` — every one of them outside `:363`–`:649`. In `docs/adopting.ja.md`, `marketplace upgrade` only at `:435`–`:436` (wrapped), `check-durability` once at `:770`, `class-M` at `:1074`/`:1201`, `tuning-oversight.ja.md` at `:72`/`:1237`, `プルリクエスト` at `:170`/`:707`/`:718`/`:729`/`:730`, `push` at `:174`/`:712`/`:766`/`:861`/`:863` — every one outside `:372`–`:655`. In `agents/pm-spec.md`, `slug` at exactly `:16` and `:58`, `task-id` exactly once at `:98` (the intent-block marker, not a filename), `check-durability` **zero** times. **Consequence, and the reason this is recorded rather than assumed: every non-vacuity premise in this spec is region-scoped and never file-wide** — a file-wide zero would be false for seven of the nine asserted tokens.
- **Measured by this role — one substring hazard that changed a token choice.** `APPROVE` contains the substring `PR`, and `APPROVE` occurs in both step-10 slices at the base ref (`docs/adopting.md:559`, `docs/adopting.ja.md:566`). A fixed-string `PR` was therefore rejected as the Japanese gap-3 token — its base-ref count would have been non-zero for a reason having nothing to do with pull requests — and **AC5** asserts `プルリクエスト` instead. Recorded here because the near-miss is the kind a later rework would otherwise re-introduce.
- **Unmeasured, with a named owner and a named occasion, deliberately not written as a criterion** — whether a Codex-host operator following the corrected runbook actually spends zero turns on these four seams. That observation cannot be made from this repository's checkout at the declared `unit-and-static` ceiling; it is owed by the operator on the next Codex-host run (b run #5), read from that run's own turn count against b run #4's. It is recorded as a disclosed limit rather than as a criterion no command here could evaluate.
- **Relayed** — mode A2's dispositions (pm-authored spec, no spec review, one freeze with wording mutable, one two-arm sweep at release rather than per task, five-line hand-offs) and the release-tier premise **PATCH**, from the operator's host-approved lightweight operation. Not readable from this role; they ground the Non-goals about sweeps and version bumps and the `- user-visible: no` declaration's tier note. The version-derivation gate re-derives rather than restates the premise at the freeze.
- **Relayed, to be measured at the freeze** — the roadmap M0 rule (no new checker, no new gate, no new status flag, no new phase). Carried into the Non-goals and into **AC9**.
- **Relayed, measured by the coordinating session and recorded here as a finding about the hand-off rather than accepted silently** — the task brief states that `docs/specs/<slug>.md` appears at 17 sites outside `.shell-team/`, that `bin/check-handoff.sh:11` deliberately still accepts the legacy form, and that the sweep of the remaining reading sites is filed as a separate issue. This role measured the two sites inside `agents/pm-spec.md` (`:16`, `:58`) first-hand and confirmed them; the count of 17 and the disposition of the other sites are relayed and are not asserted by any criterion here. Nothing in this task edits a reading site.

**Borrowed-vocabulary count-premise sweep.** Every literal count premise this spec makes about a string token is classified and enumerated here at authoring time; the execution-capable side runs each command live against the branch point's committed blob and records the measured value beside its row **before the freeze**. A row whose measurement was never run, or whose measured value contradicts what the criterion asserts, is treated as a broken check line and routes back here for a meaning-preserving correction. Every command below resolves `$B` with the expression the declaration region fixes and squeezes each slice exactly as its criterion does; both are elided for width and neither is optional. **Every token below is borrowed** — not one of the nine is a coinage this task introduces — which is precisely the case a sweep scoped to "the new literals this task introduces" would say nothing about, and it is why every premise is region-scoped.

| # | Premise | Class | Measurement command | Measured |
|---|---|---|---|---|
| 1 | `codex plugin marketplace upgrade` and `VERSION` each occur **zero** times in the step-1 sub-slice of `docs/adopting.md`'s Codex section at the branch point (**AC1** non-vacuity) | **borrowed** — Codex CLI's own subcommand and its own `plugin list` column name; region-scoped deliberately, because `marketplace upgrade` occurs once in the same section at `:428` | `for s in 'codex plugin marketplace upgrade' 'VERSION'; do git show "$B:docs/adopting.md" \| <sec> \| <st1> \| <sq> \| grep -Fc -- "$s"; done` | 0 / 0 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 2 | `codex plugin marketplace add` and `step 2` each occur **at least once** in that same base-ref slice (**AC1** positive controls, one of which is **D1**'s retained cross-reference) | **borrowed** — both predate this task, which is why they work as controls rather than as content requirements | `for s in 'codex plugin marketplace add' 'step 2'; do git show "$B:docs/adopting.md" \| <sec> \| <st1> \| <sq> \| grep -Fc -- "$s"; done` | 1 / 1 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 3 | `<task-id>-<slug>.md` and `check-durability` each occur **zero** times in the whole Codex-section slice of `docs/adopting.md` at the branch point (**AC2** non-vacuity) | **borrowed** — `<task-id>-<slug>.md` is coined by `bin/check-durability.sh:68` and `check-durability` is that script's own name; section-scoped deliberately, because `check-durability` occurs at `:763` outside the section | `for s in '<task-id>-<slug>.md' 'check-durability'; do git show "$B:docs/adopting.md" \| <sec> \| <sq> \| grep -Fc -- "$s"; done` | 0 / 0 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 4 | `push` and `pull request` each occur **zero** times in the step-10 sub-slice of `docs/adopting.md`'s Codex section at the branch point, and `READY_FOR_MERGE` **at least once** (**AC3**) | **borrowed** — ordinary English plus a status flag this repository coined long before this task; region-scoped deliberately, because both English strings occur ten times elsewhere in the same file | `for s in 'push' 'pull request' 'READY_FOR_MERGE'; do git show "$B:docs/adopting.md" \| <sec> \| <st10> \| <sq> \| grep -Fc -- "$s"; done` | 0 / 0 / 1 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 5 | `tuning-oversight.md` and `class-M` each occur **zero** times in the whole Codex-section slice of `docs/adopting.md` at the branch point (**AC4** non-vacuity) | **borrowed** — both coined by `docs/tuning-oversight.md`; section-scoped deliberately, because both occur twice elsewhere in the same file | `for s in 'tuning-oversight.md' 'class-M'; do git show "$B:docs/adopting.md" \| <sec> \| <sq> \| grep -Fc -- "$s"; done` | 0 / 0 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 6 | In `docs/adopting.ja.md` at the branch point: `marketplace upgrade` and `VERSION` **zero** in the step-1 sub-slice; `push` and `プルリクエスト` **zero** in the step-10 sub-slice; `<task-id>-<slug>.md`, `check-durability`, `tuning-oversight.ja.md` and `class-M` **zero** in the whole section slice (**AC5** non-vacuity, eight premises) | **borrowed**, every one — same coinages as rows 1, 3, 4 and 5; region-scoped for the same measured reasons, since each occurs elsewhere in the same file | `for s in 'marketplace upgrade' 'VERSION'; do git show "$B:docs/adopting.ja.md" \| <sec> \| <st1> \| <sq> \| grep -Fc -- "$s"; done; for s in 'push' 'プルリクエスト'; do git show "$B:docs/adopting.ja.md" \| <sec> \| <st10> \| <sq> \| grep -Fc -- "$s"; done; for s in '<task-id>-<slug>.md' 'check-durability' 'tuning-oversight.ja.md' 'class-M'; do git show "$B:docs/adopting.ja.md" \| <sec> \| <sq> \| grep -Fc -- "$s"; done` | 0 0 · 0 0 · 0 0 0 0 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 7 | In `docs/adopting.ja.md` at the branch point: `codex plugin marketplace add` and `手順 2` **at least once** in the step-1 sub-slice, `READY_FOR_MERGE` **at least once** in the step-10 sub-slice (**AC5** positive controls) | **borrowed** — all three predate this task | `for s in 'codex plugin marketplace add' '手順 2'; do git show "$B:docs/adopting.ja.md" \| <sec> \| <st1> \| <sq> \| grep -Fc -- "$s"; done; git show "$B:docs/adopting.ja.md" \| <sec> \| <st10> \| <sq> \| grep -Fc -- 'READY_FOR_MERGE'` | 1 1 · 1 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 8 | In `agents/pm-spec.md` at the branch point: `<task-id>-<slug>.md` **zero** and `docs/specs/<slug>.md` **at least once** in the `## Your job` slice; `check-durability` **zero** in the whole file; `READY_FOR_ARCH` **at least once** in that slice (**AC6**) | **borrowed** — `<task-id>-<slug>.md` from `bin/check-durability.sh:68`, `docs/specs/<slug>.md` from this agent file's own long-standing text, `READY_FOR_ARCH` a status flag this repository coined | `for s in '<task-id>-<slug>.md' 'docs/specs/<slug>.md' 'READY_FOR_ARCH'; do git show "$B:agents/pm-spec.md" \| <job> \| grep -Fc -- "$s"; done; git show "$B:agents/pm-spec.md" \| grep -Fc -- 'check-durability'` | 0 / 1 / 1 · file 0 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |
| 9 | `T-1138` occurs **at least once** in each of the four extracted paragraphs (**AC7** positive control); `bin/` holds **at least twenty** tracked files at the branch point and the seventeen-pathspec union at least twenty (**AC9** floors); the resolved specs directory holds **at least twenty** `T-*.md` files and exactly one `T-1139-*.md` (**AC10**); `<task-id>-*.md` occurs **at least once** in `bin/check-durability.sh` (**AC10** cited-anchor check) | **borrowed** — every population and every token predates this task except the `T-1139-*.md` file this spec itself is, whose count is pinned to this task's own id and is not a growing population | `git show "$B:docs/adopting.md" \| <pen> \| grep -Fc -- 'T-1138'; git ls-tree -r --name-only "$B" -- bin/ \| wc -l \| tr -d ' '; git ls-files -- bin templates tests skills .github agents docs README.md README.ja.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md .shell-team/retros .shell-team/specs .shell-team/reviews .shell-team/provenance .shell-team/interventions \| wc -l \| tr -d ' '; SP=$(bash bin/team-paths.sh --get specs); ls -1 "$SP"/T-*.md \| wc -l \| tr -d ' '; ls -1 "$SP"/T-1139-*.md \| wc -l \| tr -d ' '; grep -Fc -- '<task-id>-*.md' bin/check-durability.sh` | 1 · 1 · 54 · 1062 · 137 · 1 · 1 (measured 2026-09-13 at `$B` = `ecab477`, coordinating session, bash) |

## Open questions

None blocking. The one unknown — whether the corrected runbook actually removes the operator turns b run #4 spent — is deliberately not an open question but a declared limit with a named owner and a named occasion (`## Assumptions`); the four gaps are worth closing whichever way that count lands, and nothing in this spec depends on the answer.

## Notes for engineer

**Consumer class and review depth.** `docs/adopting.md`, `docs/adopting.ja.md` and `agents/pm-spec.md` are **shipped adopter-surface artifacts** — an adopter reads the guides and an adopter's loop reads the role prose — so full adversarial review depth applies to all three. This spec's own inline `- check:` lines are the dev-scaffold class the operator's 2026-08-22 pricing ruling covers: their review scope is the correctness of what they measure, the direction of the conclusions they support, and any real instrument defect, not the adversarial completeness of each guard.

**Files expected to change** — and nothing else, per `## Non-goals` and **AC9**:

- `docs/adopting.md` — step 1 (gap 1), step 10 (gap 3), and the section body for gaps 2 and 4. Where gap 4's sentence goes is yours to choose within the section; the T-1138 paragraph at `:627`–`:648` must come out byte-identical (**AC7**), so append after it rather than editing into it if that is where it reads best.
- `docs/adopting.ja.md` — the same four edits in the mirror at `:372`–`:655`, with the same machine tokens. The T-1138 mirror paragraph at `:631`–`:654` is likewise byte-identical.
- `agents/pm-spec.md` — the `## Your job` item that currently reads "Write a spec to `docs/specs/<slug>.md`" (`:16`), plus wherever you choose to name `check-durability` as the consumer. Both generated prompt-block regions (`:145`–`:243`, `:245`–`:249`) must come out byte-identical (**AC6**); edit only outside them.

**Gotchas, each measured rather than guessed.**

1. **Keep every asserted machine token unbroken on one line.** The criteria squeeze whitespace before matching, which repairs a break *between* two of a token's words but not one *inside* a word. The tokens that matter: `codex plugin marketplace upgrade`, `VERSION`, `<task-id>-<slug>.md`, `check-durability`, `pull request`, `tuning-oversight.md`, `class-M`, and in the mirror `marketplace upgrade`, `プルリクエスト`, `tuning-oversight.ja.md`, `手順 2`.
2. **Do not introduce a line starting with `## ` or `N. ` inside a fenced code block** within the Codex section, step 1, step 10 or the `## Your job` slice. Every slice is a line-anchored `awk` range and such a line truncates it early. This is an authoring constraint, not a parser this task builds (`## Input space`).
3. **Step 1 cross-references step 2; it does not copy it.** `docs/adopting.md:418`–`:435` already carries the upgrade remedy with its `--help` grounding. **AC1** requires the existing `step 2` / `手順 2` reference to survive, so keep it.
4. **Never write what `upgrade` does to a following `plugin add`.** The reader's instruction is to compare `codex plugin list`'s `VERSION` column against the release they intend to run (**D2**).
5. **Point at `tuning-oversight.md`; do not transcribe the grant.** That document itself says the grant is recorded in the adopter's own `CLAUDE.local.md` and never in a shipped file (`docs/tuning-oversight.md:111`).
6. `bin/gen-codex-agents.sh:12`–`:15` names `pm-spec` in its default role list and derives that role's TOML from the unmodified `agents/pm-spec.md`, frontmatter plus whole body, so editing the prose changes the generated TOML; `:22`–`:25` refuses the **whole** run non-zero on any validation violation, never a partial generation. `.codex/agents/` is gitignored and nothing is committed, but **AC8** requires `tests/codex-agents/run.sh` green.
7. **`agents/pm-spec.md` is registered in six prompt-block rows, not two** — measured in `templates/prompt-blocks/registry.txt`: `marker language.md` (`:31`) and `marker playbook-pm-spec.md` (`:51`), plus four `contain` rows, `flag-enum.md` (`:35`), `board-line-format.md` (`:37`), `adopter-docs-declaration.md` (`:45`) and `derived-populations.md` (`:46`). A `contain` row requires **every non-empty line** of its canonical block to appear verbatim somewhere in the file, so a `## Your job` edit that deletes or reflows a sentence belonging to one of those four blocks breaks the checker even though it is nowhere near a marker. Concretely for this task: `templates/prompt-blocks/board-line-format.md` holds exactly two non-empty lines, `- [ ] **T-` and `` ` — spec: ``, both living on `agents/pm-spec.md:58` — keep both fragments on that line. The legacy `docs/specs/<slug>.md` example beside them is pinned by nothing and **AC6** reads only the `## Your job` slice, so updating it or leaving it is your call; `## Your job` is where the change is required.
8. `docs/` is not a free zone: **AC9** permits exactly `docs/adopting.md` and `docs/adopting.ja.md` and locks every other file under `docs/`, including `docs/tuning-oversight.md` and its Japanese mirror.

**CI-equivalence scope, with every inapplicable step named individually.** The diff reaches `docs/adopting.md`, `docs/adopting.ja.md` and `agents/pm-spec.md`. Steps whose inputs it reaches, and which therefore run: `tests/codex-agents/run.sh` (reads `agents/pm-spec.md` through the generator), `tests/check-prompt-sync/run.sh` (reads `agents/pm-spec.md`'s prompt-block regions), and `bin/check-handoff.sh` over the board (this task writes a board entry and a hand-off) — all three asserted by **AC8**. Steps judged inapplicable, each with its reason: `shellcheck` over `bin/` — inapplicable, this diff contains no shell and **AC9** asserts `bin/` unchanged; `tests/check-review-input/run.sh` and every other suite under `tests/` — inapplicable, none of them reads any of the three edited paths; `bin/check-codex-agents.sh` as a standalone invocation — inapplicable, `tests/codex-agents/run.sh` already exercises it and nothing here changes its inputs beyond the prose that suite regenerates from; `bin/check-pii-shapes.sh` — applicable as ordinary hygiene over this task's own new records, run by the engineer before the first commit per the standing discipline rather than asserted as a criterion, since no criterion here is about PII shapes.

**Verify-axis judgment (T-1100).** This task's verification duty is one kind, not two: there is no fixture suite this task authors and no mechanism-class full-population two-arm diff (that is mode A2's single release-time sweep). The parent `verify` axis is the row to record; no `verify-fixture` / `verify-mechanism` refinement has ground here.

**Measured-at-ref commands.** This task's deliverables print no `measured at <ref>` label, no `**Base ref**:` header and no per-row `- measurement:` field — **not applicable**. Where this spec's own criteria read a base-side blob they do so with `git show "$B:<path>"` or `git ls-tree "$B"`, reading that ref's committed blob and never the working tree; the declaration region fixes the one expression that resolves `$B`, and every such criterion spells it byte-identically.
