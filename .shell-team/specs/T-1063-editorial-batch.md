# The sprint's batched editorial pass: six recorded imprecisions corrected at their own sites, two of them through ratified class-B re-freezes

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1063

**Base ref / branch point — one discriminator for the whole spec.** Every base-side blob read in this spec reads **the branch point**, resolved at check time as `git merge-base feature/1062-release-notes-compare-link HEAD`, and never `git merge-base develop HEAD`. The choice is stated once and applied uniformly, because two refs in one document is the inconsistency a sibling-clause cross-check exists to catch. The reason it is the correct ref here is measured rather than conventional: four predecessors on this stack (T-1059, T-1060, T-1061, T-1062) are unmerged to `develop` under the sprint's batch-GO model, and two of the files this task edits — `agents/pm-spec.md` and `.shell-team/todo.md` — were last written by T-1061 and T-1062 **on this stack**, so a `develop`-side read would attribute their predecessors' bytes to this task and would make every survival and byte-identity clause below false on arrival through no act of this task. No 40-hex literal is written into any criterion: every criterion resolves the ref itself and fails closed when it does not resolve, including after the batch merge deletes the parent branch, which is the intended behaviour for the merge-point-scoped criterion that says so in its own body.

**Branch**: `feature/1063-editorial-batch` (measured: `.git/HEAD` reads `ref: refs/heads/feature/1063-editorial-batch`).
**Stack depth**: four predecessors on one line. Measured at authoring time by reading `.git/refs/heads/`: `feature/1063-editorial-batch` and `feature/1062-release-notes-compare-link` both resolve to `f63c46b30ea56d7be4c8aceb56fcc579a43e808c`, `feature/1061-adopter-docs-gate` resolves to `383e9af56f50634c189e872251e340a8a5c538c2`, and `develop` resolves to `d01ab30ea454b23292c12df6f80dceb151d5c42f`. Because `develop` has merged none of the predecessors, the merge base against `develop` is **not** this branch's divergence point, and the criteria below measure that premise rather than assuming it.

## Problem

Six defects sit in this repository's own records and shipped prompts, each already measured and recorded by a gate that then declined to fix it in place: four are filed issues (#226, #211, #210, #196) and two are review findings the sprint plan routed here (T-1061's Codex round-3 Minor, and a stale line-number citation in T-1060's spec). None is behavioural, none is adopter-facing, and none was worth a task of its own — which is exactly why they accumulated. Two of them sit inside merged frozen intent blocks and can only be repaired through a ratified class-B re-freeze; the rest are ordinary edits to mutable prose. A seventh item is a decision rather than a repair: a defect in the shipped `bin/check-acs.sh` that this task records and routes rather than fixes.

The unifying failure they exhibit is a citation that was true when it was written and is false now — an issue number imported from another tracker, a line number in a file that has since grown, a criterion described in terms of a mode that was later carved out, a measurement labelled with the wrong ref. Each was cheap to make and each is invisible to every machine gate this repository runs, because no `- check:` line reads an explanatory sentence.

## Summarized sources

One line per document whose facts this spec restates, naming the distinction carried over. Every entry below was opened first-hand by this role with a file-reading tool; this role has no shell, no network and no `gh`, so anything it could not open is in `## Assumptions` labelled **relayed**.

- **`.shell-team/specs/T-1055-adapter-envelope.md`** — read at the intent-block boundary (`<!-- BEGIN intent-block: T-1055 -->` at L36, `<!-- END intent-block: T-1055 -->` at L208), at **AC6** and **AC7** (L149–L152), **AC11** (L159), **AC17** (L173, one physical line carrying items (a)–(n)), **AC18** (L174–L175), the correspondence table's **row 52** (L267) and `## Notes for engineer` (L386–L396). Distinctions carried over: (a) AC17(a)'s last two mutation clauses name behaviour of the `--binding` cross-check mode, and **AC6 and AC7 no longer measure that behaviour** — post-carve-out AC6 is two byte-frozen `- normative: ` equality locks plus a zero-`--binding` clause on the contract document, and AC7 is a four-surface absence measurement, so the two named mutations cannot be performed on any scratch copy; (b) AC17(l)'s reproduction recipe still carries a `bind a proposes-authority role` step whose mode was removed, while the refusal it demonstrates (`retired-channel-declared`) fires at contract load; (c) row 52's exemption reason cites **AC6** as binding `codex-reviewer` to `codex-cli`, which post-v4 AC6 does not do — the properties that would still surface a role-to-adapter restriction are **AC11**'s six re-derived `bind` rows and **AC18**'s six-role scratch config, both read first-hand; (d) L396's engineer note says `doc-drift` will report a hand-edited canon region, and the `--doc` mode that emitted that token was carved out by DP16 v3, leaving **AC8**'s own canon-region byte comparison as what reports it; (e) L173 and L267 are one physical line each, so every repair here is a within-line fragment substitution rather than a line insertion.
- **`.shell-team/reviews/T-1055.md`** — read at `## Round 5` (L243 onward), specifically L280 and L293. Distinctions carried over, and one of them corrects the routing this spec was handed: the round-5 finding names **`.shell-team/specs/T-1055-adapter-envelope.md:173` (AC17(l))**, which is **inside** that spec's intent block, and the disposition line at L293 folds it into "the same batched editorial pass as the same class of AC17-narrative staleness as AC17(a)". The item is therefore **frozen, not mutable**, and this spec carries it inside the same class-B re-freeze as AC17(a) rather than as a third mutable edit.
- **`.shell-team/specs/T-1052-records-editorial.md`** — read at the intent-block boundary (BEGIN L38, END L187), at `## Problem` (L13), the `E171` frozen block (L90–L94), the `Q171` frozen block (L96–L100), DP6/DP7 (L86–L88), the criteria **AC1** (L133), **AC5** (L141), **AC6** (L143), **AC7**/**AC8** (L145–L148), **AC10** (L151), the `## Non-goals` no-checker line (L124) and `## Assumptions` (L298). Distinctions carried over: (a) **AC1's idiom** — a frozen `old`/`new` fragment pair extracted from the spec itself, asserted exactly-once/zero in one named target file, with the base blob supplying the mirrored positive control, and the negative sweep scoped to that one file **because the spec legitimately quotes the superseded string** — is the idiom this spec reuses throughout; (b) **AC8** re-derives `Q171` from the spec at check time and requires it exactly once as a whole line inside `agents/qa-verifier.md`'s hand-written `## Rules` region, so a coordinated re-freeze plus re-splice leaves AC8 green while an uncoordinated one reddens it; (c) **AC10** requires a `--numstat` deletion column of exactly `0` for `agents/engineer.md` and `agents/qa-verifier.md` against `a804703` and labels that half merge-point-scoped — an in-place line replacement in `agents/qa-verifier.md` reddens it by construction; (d) **DP7** declined `agents/pm-spec.md` as a third surface for the #171 norm and its quoted substring is pinned by AC8, so nothing in this task's pm-spec edits may touch that decision; (e) `## Problem` L13 frames the runtime criterion in the singular ("the one criterion"), the exact framing the round-1 Major had corrected inside the frozen blocks — the mutable half was left behind; (f) `## Assumptions` L298 records, in T-1052's own words, that this repository's shipped `agents/pm-spec.md` cites "issue #171" for a **different** rationale than the norm T-1052 shipped under that number, and names the discrepancy rather than resolving it — the disclosure #210 exists to close.
- **`agents/qa-verifier.md`** — read L55–L89. Distinctions carried over: the hand-written `## Rules` region runs from the `## Rules` heading at **L55** to the file's first generated-region marker `<!-- BEGIN prompt-block: careful-execution -->` at **L83**; `Q171` is the last rule in that region at **L68**; the mandatory-item idiom (mandatory / not subject to the out-of-input-space exemption / `environmentally-unverified` / prior behaviour unchanged) is stated at L63–L65 and `Q171` already reuses it. This is a hand-written region no generator owns, so the re-splice is an ordinary edit and **no regeneration is involved**.
- **`agents/engineer.md`** — read for the `E171` rule's no-enumeration fallback. Distinction carried over: the fragment `walk each distinct assertion its prose demands instead` occurs **exactly once** in that file and **zero times** in `agents/qa-verifier.md` (measured across the repository: one occurrence in `agents/engineer.md`, one in `.shell-team/specs/T-1052-records-editorial.md`'s own frozen block, three in `.shell-team/reviews/T-1052.md`). That single occurrence is what makes the cross-reference this task adds a reference rather than a copy, and it is the positive control the non-duplication criterion needs.
- **`agents/pm-spec.md`** — read L74–L90. Distinctions carried over: (a) `#171` occurs **exactly once** in the file, at **L79**, inside the Input-space-definition checklist item's rationale, beside the escalation example `full-width 30 chars → wide glyphs 4 → 40 → 100`; `full-width` likewise occurs exactly once, on the same line; (b) `state in the freeze attestation` occurs **exactly once**, at **L89**, inside the adopter-docs declaration checklist item, in a clause instructing this role to write the attestation before setting its own flag; (c) the canonical sentence registered from `templates/prompt-blocks/adopter-docs-declaration.md` sits **earlier in that same L89 bullet** than the defective clause, so repairing the clause leaves the canonical bytes untouched by construction rather than by care; (d) L88's adopter-facing-documentation blocker item is a separate bullet and is not touched.
- **`templates/prompt-blocks/adopter-docs-declaration.md` and `templates/prompt-blocks/registry.txt`** — read in full and at L45 respectively. Distinctions carried over: the block is three non-empty lines (the two `- user-visible:` forms and the `A yes declaration additionally requires…` sentence), and registry line 45 is `contain adopter-docs-declaration.md agents/pm-spec.md skills/run/SKILL.md`, so `bin/check-prompt-sync.sh` requires every one of those three lines to appear verbatim in **both** consumers. That is the mechanical form of this task's hard constraint.
- **`skills/run/SKILL.md`** — read at L12 and L47. Distinctions carried over, and the second corrects the routing this spec was handed: `#250` is at **L47** and is a correct citation, while the file's only occurrence of **`#171` is at L12**, in a different bullet, citing it for a visual-gate churn example ("10+ rework rounds, final score 70/100") — a third subject, distinct both from this repository's #171 (the runtime-criterion reporting norm) and from the input-space escalation `agents/pm-spec.md:79` cites it for. L47's own wording (`state in the freeze attestation which of the three outcomes you read`) is correct where it stands, because the coordinating session both performs that read and writes that record — it is the cross-file positive control for the attribution repair, and it is why the repair is one-sided.
- **`.shell-team/specs/T-1060-adopter-binding-docs.md`** — read at the intent-block boundary (BEGIN L31, END L105), at `## Summarized sources` L23, `## Non-goals` L44 and `## Decision points` **DP5 at L702**. Distinctions carried over: the literal `line 208` occurs **five times across three lines** — twice at L23, once at L44, twice at L702 — of which L44 is **inside** the marker region and L23 and L702 are outside it. The third site is at **L702, not L727**.
- **`README.md` and `README.ja.md`** — read at L196–L211 in each. Distinction carried over: the reviewer design-choices cross-reference paragraph now **ends at L207** in both files (L208 is blank in both), so every `line 208` citation of it is stale by one and would be stale again after the next edit to either file — which is why the repair is a content-named anchor rather than a corrected number.
- **`.shell-team/todo.md`, T-1047's entry (L1074–L1103)** — read end to end, per the append-only-record discipline. Distinctions carried over: (a) the board-hygiene sub-bullet at **L1087** claims `## Active` was measured empty "at this HEAD" and cites `.shell-team/todo.md:12–14`, while the same entry's own AC17(d)(5) report at **L1097** records the measurement as `git show 99d4038:.shell-team/todo.md` — the entry contradicts itself, and the base-ref reading is the correct one; (b) the meta-citations `.shell-team/todo.md:144` and `.shell-team/todo.md:22` occur at **L1096**, `.shell-team/todo.md:22` again at **L1100**, and the phrase `own heading is at line 149` at **L1100**; (c) the phrase `measured **empty** at this HEAD` occurs **six times in the board** (L861, L968, L1087, L1116, L1147, L1196) — five of them in **other tasks' entries**, which #196 does not scope and this task does not touch, so every absence assertion here is entry-scoped rather than board-scoped; (d) T-1046's entry now sits at **L1213**, T-1043's at **L1181** — neither at 144, 149 or 150 — which is the measured demonstration that renumbering these citations would make them wrong again rather than right.
- **`.shell-team/todo.md`, T-1052's entry (from its top-level `- [` line) and T-1055's entry** — read at their frozen-intent ledgers. Distinctions carried over, and both are load-bearing for **AC9**'s derived version arithmetic: T-1052's entry carries `- intent-hash (v3)` and three `- freeze-attestation (` lines with two `- intent-ratified (` lines above them, so this task's re-freeze is **v3→v4**; T-1055's entry carries `- intent-hash (v6)` with its own `- intent-ratified (2026-08-10): v5→v6 — class=B` line, so this task's is **v6→v7**. Neither version is written into a criterion: AC9 derives `base + 1` from the entry itself. T-1052's entry additionally records, at its codex round-1 disposition, that `agents/pm-spec.md:79`'s stale `#171` citation was recommended as a follow-up rather than fixed in that train because the file was locked by a merged criterion — the recommendation #210 was filed from.
- **`.shell-team/reviews/T-1047.md`** — read at the round-3 finding table (L28–L37) and the fast-follow disposition. Distinctions carried over: finding **5** is the "measured empty at this HEAD" claim, classified minor/fast-follow and explicitly "confined to mutable board prose, not a frozen block"; finding **6** is that the corrections about where T-1046's board entry sits are themselves both wrong ("144, then 149; actually 150, and 144 belongs to a different task entirely"), classified as a third layer of the same self-referential citation-imprecision class. Both were filed as #196; a third item (findings about AC12/AC13/AC14's region-presence assertions) was filed separately as **#195** and is not this task's.
- **`.shell-team/reviews/T-1061.md`** — read at L284–L305. Distinctions carried over: (a) the round-3 Minor at **L292** is the freeze-attestation misattribution, and the reviewer's own sweep records that the same wording pattern at `skills/run/SKILL.md:47` is **correct** there and that the mismatch is confined to `agents/pm-spec.md:89`; (b) the severity reasoning at L294 is that `## Spec completion self-check` is hand-written and mutable, so the repair is an ordinary edit and not a re-freeze; (c) L298 records `.shell-team/specs/T-1060-adopter-binding-docs.md` **AC11** measured **FAIL** at that task's head and `.shell-team/specs/T-1003-retro-reads-interventions.md` AC19 / `.shell-team/specs/T-1019-is-span-row-parity.md` AC10 measured **exit 0** — values measured at a different ref by a different role, carried here as predictions to re-measure rather than as facts; (d) L300 records the known phantom `AC1: FAIL (exit 127)` in T-1061's own spec as unchanged since round 1 and unrelated to that task.
- **`.shell-team/specs/T-1061-adopter-docs-gate.md`** — read at **AC1** (L56–L57) and **AC2** (L59–L60). Distinctions carried over, and they are load-bearing for this task's blast radius: AC1 requires `bin/check-prompt-sync.sh` to exit `0` and the registry row to name exactly the two consumers; **AC2 requires that every non-blank line the branch-point blob of `agents/pm-spec.md` carries is still present verbatim in the working file** — an additive-only lock that any in-place correction to that file reddens by construction, and which carries **no** merge-point-scoped label.
- **`.shell-team/specs/T-1062-release-notes-compare-link.md`** — read in full. Distinctions carried over: the one-discriminator base-ref header format this spec reuses; **AC5**'s scope-lock shape (tracked half from the commit range unioned with `git ls-files --others --exclude-standard`, plus explicit zero-count directories, plus the merge-point-scoped note); **AC6**'s board-survival and records shape; and its `## Notes from engineer` record of the pre-existing `bin/check-acs.sh` parser quirk against T-1061's spec, disclosed there and not fixed.
- **The checker contracts this spec's criteria invoke** — `bin/check-refreeze-class.sh` (exit 1 with the `check-refreeze-class: class-b:` token on an intent-differing pair, exit 2 on a byte-identical pair), `bin/check-intent.sh` (aligned at exit 0 against the resolved board), `bin/check-prompt-sync.sh` (exit 0 when every registered block line appears verbatim in every consumer), and the `check-handoff` / `check-board-headings` / `check-pii-shapes` / `check-provenance` / `check-interventions` exit contracts. **These are taken from the criteria of `.shell-team/specs/T-1052-records-editorial.md` (AC5, AC6, AC10) and `.shell-team/specs/T-1062-release-notes-compare-link.md` (AC6), both of which this role opened and read first-hand — not from the scripts themselves, which this role did not open.** The distinction carried over is the exit-code-plus-token pairing and, in the class-M/class-B case, the byte-identical control pair that makes the classifier's verdict non-vacuous; **AC11(e)** re-measures every one of them live, because a checker contract read out of another spec is a summary and not a measurement.
- **`bin/check-acs.sh`** — read at its header comment block (L1–L34). Distinction carried over: the header documents the criterion and `check:` grammar, the trust boundary and the backtick-wrapping prohibition, and describes **no** exclusion of fenced code blocks — consistent with the phantom criterion two independent records report against T-1061's spec. This spec makes no repair to that file and states the disposition as a decision (`## Decision points`, DP7).

## Goal

<!-- BEGIN intent-block: T-1063 -->

- user-visible: no — the deliverable is corrections to prose: two sentences in shipped agent prompts an agent reads, four sites in this repository's own specs, and one entry in this repository's own board. No command, configuration mechanism, capability or output an adopter invokes, configures or reads about changes; nothing under `bin/`, `tests/`, `templates/`, `.github/`, `README*` or `docs/` is touched at all; and the one clause added to `agents/qa-verifier.md` names a rule that already ships in `agents/engineer.md` rather than creating an obligation that did not exist. There is consequently no adopter-facing documentation surface for it to land on, so this declaration carries neither an `- adopter-surface:` line nor an `- adopter-docs-waiver:` line, per the freeze-time rule that a `no` declaration carries neither.

Six recorded imprecisions are corrected at their own sites and nowhere else. In `.shell-team/specs/T-1055-adapter-envelope.md`: the correspondence table's row-52 exemption reason names the criteria that actually carry it today, the `## Notes for engineer` canon-region warning names the comparison that actually reports a hand-edit today, and — through one ratified **class-B** re-freeze — **AC17(a)**'s two now-unperformable mutation clauses are replaced by mutations against what post-carve-out **AC6** and **AC7** measure, while **AC17(l)**'s reproduction recipe drops the binding step whose mode was removed; every `- check:` line in that spec is byte-identical to its branch-point blob, its criterion count and its `- stale-at:` count are unchanged, and `bin/check-refreeze-class.sh` measures the delta `class-b`. In `.shell-team/specs/T-1052-records-editorial.md`: through a second ratified **class-B** re-freeze, the frozen `Q171` block gains one clause naming — by reference, never by restatement — the no-enumeration fallback that `agents/engineer.md`'s matching rule states, and `## Problem`'s singular framing of the runtime criterion is pluralized to match the blocks that were already corrected; the same `Q171` text is re-spliced into `agents/qa-verifier.md`'s hand-written `## Rules` region so that spec's own symmetric-match criterion stays true, the superseded line is extinct there, and the fallback's own wording appears **zero** times in `agents/qa-verifier.md` against **exactly one** occurrence in `agents/engineer.md`. In `agents/pm-spec.md`: the `#171` citation is dropped from the input-space escalation example, which otherwise survives word for word, and the adopter-docs checklist item stops instructing this role to write a record the coordinating session writes after this role's flag is set — with the canonical declaration-grammar sentence registered in `templates/prompt-blocks/registry.txt` byte-identical and `bin/check-prompt-sync.sh` green. In `.shell-team/specs/T-1060-adopter-binding-docs.md`: both mutable `line 208` citations become content-named anchors, and the third occurrence, inside that spec's frozen `## Non-goals`, is left byte-identical to its branch-point blob. In `.shell-team/todo.md`: T-1047's entry stops claiming its `## Active`-empty measurement was taken at HEAD when its own report records it at `99d4038`, its three board line-number meta-citations are replaced by content-named anchors, and one correction sub-bullet says which lines were changed and why — while the same claim in five other tasks' entries, which no issue scopes, is left alone.

### Replacement texts

Every text this task substitutes into another file is frozen byte for byte here, so a criterion reads its expected bytes out of this file rather than restating them, and the ratification package quotes this section rather than a paraphrase of it. Each block holds exactly one line.

<!-- frozen-begin: R52old -->

**AC6** binds `codex-reviewer` to `codex-cli` and **AC18**'s config binds all six roles, neither of which would validate if a restriction had been added.

<!-- frozen-end: R52old -->

<!-- frozen-begin: R52new -->

**AC11** re-derives `templates/binding-template.conf`'s six `bind` rows, one per inner-loop role, and **AC18**'s scratch config binds all six roles, neither of which would validate if a restriction had been added.

<!-- frozen-end: R52new -->

<!-- frozen-begin: DDold -->

and `doc-drift` will say so — correctly, but after you have spent the round.

<!-- frozen-end: DDold -->

<!-- frozen-begin: DDnew -->

and **AC8**'s own canon-region byte comparison will say so — correctly, but after you have spent the round; the `--doc` mode that once reported `doc-drift` was carved out by DP16 v3.

<!-- frozen-end: DDnew -->

<!-- frozen-begin: A17old -->

accept a bound effort value absent from the adapter's declared set (AC6 red); make `--binding` proceed on a config `check-binding.sh` refused (AC7 red).

<!-- frozen-end: A17old -->

<!-- frozen-begin: A17new -->

alter one character inside one of the two byte-frozen `- normative: ` sentences in `docs/loop-engineering/task-envelope.md` (AC6 red on that equality lock); reintroduce a single `--binding` occurrence into `bin/check-adapter.sh` (AC7 red on the four-surface absence). These two replace the pre-carve-out pair — a bound effort value absent from the adapter's declared set, and `--binding` proceeding on a config `check-binding.sh` refused — which named behaviour of the cross-check mode DP18 removed, so no scratch copy can perform them any more; what AC6 and AC7 measure today is what these two attack instead.

<!-- frozen-end: A17new -->

<!-- frozen-begin: L17old -->

build a contract through `--contract` that declares `channel board`, remap a definition's `carries board-transition` onto it, bind a `proposes`-authority role to that adapter, and report the refusal token and exit code

<!-- frozen-end: L17old -->

<!-- frozen-begin: L17new -->

build a contract through `--contract` that declares `channel board`, remap a definition's `carries board-transition` onto it, and report the refusal token and exit code — the `proposes`-authority binding step this recipe carried until now is dropped, because the refusal it demonstrates fires when the contract is loaded and DP18 removed the mode that step ran through

<!-- frozen-end: L17new -->

<!-- frozen-begin: Q171x -->

Where such a criterion enumerates no items of its own, read the hand-off against the no-enumeration fallback stated in `agents/engineer.md`'s matching rule rather than against nothing; that rule is named here and never restated, so one norm cannot ship as two drifting sentences.

<!-- frozen-end: Q171x -->

<!-- frozen-begin: P52old -->

And the one criterion in every recent spec that no command can verify — the runtime criterion `check-acs.sh` reports as `SKIP` — is discharged in the hand-off by whatever shape the engineer chooses

<!-- frozen-end: P52old -->

<!-- frozen-begin: P52new -->

And the criteria in every recent spec that no command can verify — the runtime criteria `check-acs.sh` reports as `SKIP`, of which one spec may carry more than one — are discharged in the hand-off by whatever shape the engineer chooses

<!-- frozen-end: P52new -->

<!-- frozen-begin: EFALL -->

walk each distinct assertion its prose demands instead

<!-- frozen-end: EFALL -->

<!-- frozen-begin: PM79old -->

(issue #171: full-width 30 chars → wide glyphs 4 → 40 → 100, each finding individually valid)

<!-- frozen-end: PM79old -->

<!-- frozen-begin: PM79new -->

(the escalation shape this requirement exists to bound: full-width 30 chars → wide glyphs 4 → 40 → 100, each finding individually valid)

<!-- frozen-end: PM79new -->

<!-- frozen-begin: PM89old -->

read the declaration region yourself, require exactly one of the three outcomes above, and state in the freeze attestation which one you read, before setting `READY_FOR_ARCH`

<!-- frozen-end: PM89old -->

<!-- frozen-begin: PM89new -->

read the declaration region yourself and require exactly one of the three outcomes above before setting `READY_FOR_ARCH`, and say in your hand-off which of the three you read — the `- freeze-attestation` sub-bullet that carries it onto the board is written afterwards by the coordinating session, which performs the same read, and never by this role

<!-- frozen-end: PM89new -->

<!-- frozen-begin: T60anchor -->

named by its content rather than by a line number

<!-- frozen-end: T60anchor -->

## Non-goals

- **No edit *by this task* to anything under `bin/`, `tests/`, `.github/`, `templates/`, `docs/` or `skills/`, and none to `README.md`, `README.ja.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `CHANGELOG.ja.md` or `.shell-team/lessons.md`.** `skills/run/SKILL.md` is **read-and-judged, never edited**: its `#171` occurrence is measured, judged and recorded, and the judgment is that the repair is out of this task's declared scope and is filed instead. `agents/engineer.md` is likewise read and left byte-identical — the fallback it states is referenced, not moved.
- **No new checker, fixture suite, CI step, state machine or placement-assertion machinery, and no defence against inputs this spec's `## Input space` declines.** This is the operator's ruling of 2026-08-13, taken at T-1062's STOP resolution and named here as a constraint on this task rather than as background: acceptance criteria stay at four shapes — corrected-string presence and superseded-string absence through file-scoped greps with base-blob positive controls, byte-scoped diffs, checker exit codes, and records-line existence. A criterion that would need a new mechanism to state is not written; the property it would have asserted is delegated in the criterion's own body, or it is not asserted at all and says so.
- **No third class-B re-freeze.** `.shell-team/specs/T-1060-adopter-binding-docs.md`'s `## Non-goals` line 44 carries the same stale `line 208` citation as the two mutable sites and is **left untouched**, byte-identical to its branch-point blob. Spending a ratification cycle on a line-number nit inside merged frozen intent fails the proportionality this repository's own corpus states for frozen prose that carries no check line; the frozen twin's staleness is disclosed in `## Blast radius` and in `## Decision points` DP5 instead of being repaired.
- **No repair, widening or annotation *by this task* of any merged criterion, including the ones this task's diff reddens.** Every red is disclosed in `## Blast radius` and differenced against the branch point; none is fixed; no other spec gains a `- stale-at:` line, a wider base ref or an amended expected set. This applies with particular force to `.shell-team/specs/T-1061-adopter-docs-gate.md`'s **AC2**, whose every-base-line-survives clause any in-place correction to `agents/pm-spec.md` reddens by construction, and to `.shell-team/specs/T-1052-records-editorial.md`'s **AC10**, whose zero-deletion clause an in-place re-splice into `agents/qa-verifier.md` reddens the same way.
- **No fix for the fenced-example parser defect in `bin/check-acs.sh`.** The phantom criterion two independent records report against `.shell-team/specs/T-1061-adopter-docs-gate.md` is recorded as a disposition (`## Decision points`, DP7) and filed as its own issue at the ratification stop; it is not folded into #250 and it is not repaired here.
- **No correction of the same-class claim in any other task's board entry.** The phrase this task removes from T-1047's entry occurs in five other entries, which no issue scopes; they are measured, named in `## Blast radius`, and left alone. Renumbering any board line-number citation is likewise forbidden: the repair is a content-named anchor, because a corrected number is wrong again at the next append.
- **No item 3 of #196**, which rides with #195; **no closing of #226, #211, #210 or #196**, which happens at the sprint's batch merge; **no retro, no acting on any other fast-follow, no merge and no push.**

## Acceptance criteria

Every check below runs from the repository root, resolves the branch point itself before using it, names every file it reads explicitly, guards every `mktemp` with an immediate non-zero exit, writes nothing outside `$TMPDIR`, scopes every absence assertion to the one file it concerns — this spec quotes every superseded string legitimately, so a repository-wide sweep would refute itself — and pairs every absence with a base-blob positive control, so an unreadable or misnamed target reads as a failure rather than as a clean absence.

- [ ] **AC1** T-1055's two mutable corrections ship and their superseded strings are extinct in that one file. `R52old`, `R52new`, `DDold` and `DDnew` each extract to exactly one line from this spec. In `.shell-team/specs/T-1055-adapter-envelope.md`, `R52new` and `DDnew` each occur **exactly once** and `R52old` and `DDold` each occur **zero** times. The positive controls are taken from that file's own branch-point blob rather than from this spec: there, `R52old` and `DDold` each occur exactly once and `R52new` and `DDnew` each occur zero times — so a fragment matching nothing on either side cannot satisfy the extinction assertion. Positive controls: the branch point resolves, the base blob is non-empty, and every extracted fragment is asserted to be exactly one line before it is used as a pattern.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; SP=.shell-team/specs/T-1063-editorial-batch.md; S55=.shell-team/specs/T-1055-adapter-envelope.md; for f in "$SP" "$S55"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063a.XXXXXX") || exit 1; for n in R52old R52new DDold DDnew; do awk -v b="<!-- frozen-begin: $n -->" -v e="<!-- frozen-end: $n -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$n"; test "$(grep -c . "$T/$n" || true)" = "1" || rc=1; done; git show "$B:$S55" > "$T/base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/base" || rc=1; for n in R52old DDold; do test "$(grep -cFf "$T/$n" "$T/base" || true)" = "1" || rc=1; test "$(grep -cFf "$T/$n" "$S55" || true)" = "0" || rc=1; done; for n in R52new DDnew; do test "$(grep -cFf "$T/$n" "$T/base" || true)" = "0" || rc=1; test "$(grep -cFf "$T/$n" "$S55" || true)" = "1" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC2** T-1055's class-B delta ships inside its criteria and nowhere else, measured as fragment substitution rather than as placement. `A17old`, `A17new`, `L17old` and `L17new` each extract to exactly one line from this spec. In `.shell-team/specs/T-1055-adapter-envelope.md`, `A17new` and `L17new` each occur **exactly once** and `A17old` and `L17old` each occur **zero** times, with that file's branch-point blob supplying the mirrored control (`A17old`/`L17old` exactly once, `A17new`/`L17new` zero). The `- [ ] **AC17**` criterion bullet occurs exactly once and carries **both** new fragments on its own line, which is what confines the substitution to the criterion it belongs to without asserting where inside that line it sits. **What this criterion does not assert, stated so the bound is a delegation and not a silent drop**: that the replacement mutations are the right ones for post-carve-out AC6 and AC7 is a judgment carried by the ratification package and by review — no command can measure whether a mutation description matches what a criterion would measure. Positive controls as in AC1.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; SP=.shell-team/specs/T-1063-editorial-batch.md; S55=.shell-team/specs/T-1055-adapter-envelope.md; for f in "$SP" "$S55"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063b.XXXXXX") || exit 1; for n in A17old A17new L17old L17new; do awk -v b="<!-- frozen-begin: $n -->" -v e="<!-- frozen-end: $n -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$n"; test "$(grep -c . "$T/$n" || true)" = "1" || rc=1; done; git show "$B:$S55" > "$T/base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/base" || rc=1; for n in A17old L17old; do test "$(grep -cFf "$T/$n" "$T/base" || true)" = "1" || rc=1; test "$(grep -cFf "$T/$n" "$S55" || true)" = "0" || rc=1; done; for n in A17new L17new; do test "$(grep -cFf "$T/$n" "$T/base" || true)" = "0" || rc=1; test "$(grep -cFf "$T/$n" "$S55" || true)" = "1" || rc=1; done; test "$(grep -cE '^- \[[ xX]\] \*\*AC17\*\* ' "$S55" || true)" = "1" || rc=1; grep -E '^- \[[ xX]\] \*\*AC17\*\* ' "$S55" > "$T/ac17"; test -s "$T/ac17" || rc=1; for n in A17new L17new; do test "$(grep -cFf "$T/$n" "$T/ac17" || true)" = "1" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC3** T-1052's class-B delta and its mutable twin ship together, so the block and the sentence describing it stop disagreeing. `Q171x`, `P52old` and `P52new` each extract to exactly one line from this spec. In `.shell-team/specs/T-1052-records-editorial.md`: the `Q171` frozen block extracts to exactly one line and that line contains `Q171x`; the same block extracted from that file's **branch-point blob** contains `Q171x` **zero** times, which is the control proving the clause is new rather than inherited; `P52new` occurs exactly once and `P52old` zero times, with the base blob carrying `P52old` exactly once and `P52new` zero times. Positive controls: the branch point resolves, both `Q171` extractions are asserted to be exactly one line before either is searched, and the base blob is asserted non-empty.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; SP=.shell-team/specs/T-1063-editorial-batch.md; S52=.shell-team/specs/T-1052-records-editorial.md; for f in "$SP" "$S52"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063c.XXXXXX") || exit 1; for n in Q171x P52old P52new; do awk -v b="<!-- frozen-begin: $n -->" -v e="<!-- frozen-end: $n -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$n"; test "$(grep -c . "$T/$n" || true)" = "1" || rc=1; done; git show "$B:$S52" > "$T/base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/base" || rc=1; q(){ awk -v b='<!-- frozen-begin: Q171 -->' -v e='<!-- frozen-end: Q171 -->' '$0==b{f=1;next} $0==e{f=0} f' "$1" | sed '1d;$d'; }; q "$S52" > "$T/qw"; q "$T/base" > "$T/qb"; test "$(grep -c . "$T/qw" || true)" = "1" || rc=1; test "$(grep -c . "$T/qb" || true)" = "1" || rc=1; test "$(grep -cFf "$T/Q171x" "$T/qw" || true)" = "1" || rc=1; test "$(grep -cFf "$T/Q171x" "$T/qb" || true)" = "0" || rc=1; test "$(grep -cFf "$T/P52old" "$T/base" || true)" = "1" || rc=1; test "$(grep -cFf "$T/P52new" "$T/base" || true)" = "0" || rc=1; test "$(grep -cFf "$T/P52old" "$S52" || true)" = "0" || rc=1; test "$(grep -cFf "$T/P52new" "$S52" || true)" = "1" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** The re-spliced rule matches its frozen source symmetrically, the superseded line is extinct in the agent file, and the fallback is referenced rather than copied. `Q171` extracted from `.shell-team/specs/T-1052-records-editorial.md` occurs **exactly once as a whole line** in `agents/qa-verifier.md`, and exactly once inside that file's hand-written region — after the `## Rules` heading and before the file's first `<!-- BEGIN prompt-block:` marker — so a rule spliced into generated text, where a regeneration would silently drop it, fails here. `Q171` extracted from that spec's **branch-point blob** occurs **zero** times in the working `agents/qa-verifier.md` and **at least once** in that agent file's own branch-point blob, which is the control proving the superseded line existed and is gone. `EFALL` extracts to exactly one line from this spec, occurs **zero** times in `agents/qa-verifier.md` and **exactly once** in `agents/engineer.md` — the mechanical form of "named, never restated" — and `agents/engineer.md` itself is unchanged by this task, with neither a tracked diff against the branch point nor an untracked entry. Positive controls: both `Q171` extractions are asserted to be exactly one line, the `## Rules` heading is asserted to occur exactly once, the first generated marker is asserted present, and the extracted hand-written region is asserted non-empty before the line is sought inside it.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; SP=.shell-team/specs/T-1063-editorial-batch.md; S52=.shell-team/specs/T-1052-records-editorial.md; A=agents/qa-verifier.md; E=agents/engineer.md; for f in "$SP" "$S52" "$A" "$E"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063d.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: EFALL -->' -v e='<!-- frozen-end: EFALL -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/EFALL"; test "$(grep -c . "$T/EFALL" || true)" = "1" || rc=1; git show "$B:$S52" > "$T/spec.base" 2>/dev/null || { rm -rf "$T"; exit 1; }; git show "$B:$A" > "$T/qa.base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/spec.base" || rc=1; test -s "$T/qa.base" || rc=1; q(){ awk -v b='<!-- frozen-begin: Q171 -->' -v e='<!-- frozen-end: Q171 -->' '$0==b{f=1;next} $0==e{f=0} f' "$1" | sed '1d;$d'; }; q "$S52" > "$T/qw"; q "$T/spec.base" > "$T/qb"; test "$(grep -c . "$T/qw" || true)" = "1" || rc=1; test "$(grep -c . "$T/qb" || true)" = "1" || rc=1; test "$(grep -cxF -- '## Rules' "$A" || true)" = "1" || rc=1; grep -q '^<!-- BEGIN prompt-block:' "$A" || rc=1; awk '$0=="## Rules"{f=1;next} f && index($0,"<!-- BEGIN prompt-block:")==1 {exit} f' "$A" > "$T/reg"; test -s "$T/reg" || rc=1; test "$(grep -cxFf "$T/qw" "$A" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/qw" "$T/reg" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/qb" "$A" || true)" = "0" || rc=1; test "$(grep -cxFf "$T/qb" "$T/qa.base" || true)" -ge 1 || rc=1; test "$(grep -cFf "$T/EFALL" "$A" || true)" = "0" || rc=1; test "$(grep -cFf "$T/EFALL" "$E" || true)" = "1" || rc=1; test -z "$(git diff --name-only "$B"...HEAD -- "$E")" || rc=1; test -z "$(git ls-files --others --exclude-standard -- "$E")" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** Both `agents/pm-spec.md` corrections ship, the example's substance survives, and the registered canonical grammar does not move by one byte. `PM79old`, `PM79new`, `PM89old` and `PM89new` each extract to exactly one line from this spec. In `agents/pm-spec.md`: `PM79new` and `PM89new` each occur **exactly once**; `PM79old` and `PM89old` each occur **zero** times; the literal `#171` occurs **zero** times and the literal `full-width` still occurs **exactly once**, so the citation is dropped while the escalation example it annotated survives; and the literal `state in the freeze attestation` occurs **zero** times. The branch-point blob of that file supplies the mirrored control for all four fragments and carries `#171` exactly once. Two cross-file controls make the one-sidedness measurable rather than asserted: `state in the freeze attestation` occurs **exactly once** in `skills/run/SKILL.md`, where the reviewer's own sweep found it correct, and that file is unchanged by this task (no tracked diff against the branch point, no untracked entry). The canonical grammar is pinned from both directions: every non-empty line of `templates/prompt-blocks/adopter-docs-declaration.md` occurs in `agents/pm-spec.md`, that block file and `templates/prompt-blocks/registry.txt` are both unchanged by this task, and `bash bin/check-prompt-sync.sh` exits `0`. Positive controls: the block file is asserted non-empty and to yield at least three non-empty lines before they are searched for, and every extracted fragment is asserted to be exactly one line.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; SP=.shell-team/specs/T-1063-editorial-batch.md; P=agents/pm-spec.md; K=skills/run/SKILL.md; BL=templates/prompt-blocks/adopter-docs-declaration.md; RG=templates/prompt-blocks/registry.txt; for f in "$SP" "$P" "$K" "$BL" "$RG"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063e.XXXXXX") || exit 1; for n in PM79old PM79new PM89old PM89new; do awk -v b="<!-- frozen-begin: $n -->" -v e="<!-- frozen-end: $n -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$n"; test "$(grep -c . "$T/$n" || true)" = "1" || rc=1; done; git show "$B:$P" > "$T/base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/base" || rc=1; for n in PM79old PM89old; do test "$(grep -cFf "$T/$n" "$T/base" || true)" = "1" || rc=1; test "$(grep -cFf "$T/$n" "$P" || true)" = "0" || rc=1; done; for n in PM79new PM89new; do test "$(grep -cFf "$T/$n" "$T/base" || true)" = "0" || rc=1; test "$(grep -cFf "$T/$n" "$P" || true)" = "1" || rc=1; done; test "$(grep -cF -- '#171' "$T/base" || true)" = "1" || rc=1; test "$(grep -cF -- '#171' "$P" || true)" = "0" || rc=1; test "$(grep -cF -- 'full-width' "$P" || true)" = "1" || rc=1; test "$(grep -cF -- 'state in the freeze attestation' "$P" || true)" = "0" || rc=1; test "$(grep -cF -- 'state in the freeze attestation' "$K" || true)" = "1" || rc=1; for f in "$K" "$BL" "$RG"; do test -z "$(git diff --name-only "$B"...HEAD -- "$f")" || rc=1; test -z "$(git ls-files --others --exclude-standard -- "$f")" || rc=1; done; grep -v '^[[:space:]]*$' "$BL" > "$T/bl"; test "$(grep -c . "$T/bl" || true)" -ge 3 || rc=1; while IFS= read -r l; do test -n "$l" || continue; grep -qF -- "$l" "$P" || rc=1; done < "$T/bl"; bash bin/check-prompt-sync.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** Both re-freezes move intent and never mechanics, measured by the classifier rather than declared. For **each** of `.shell-team/specs/T-1055-adapter-envelope.md` and `.shell-team/specs/T-1052-records-editorial.md`, compared against its own branch-point blob: the full ordered list of `- check:` lines is byte-identical (`cmp`, not a count), the number of `- [ ] **ACn**` criterion bullets is equal on both sides, and the number of `- stale-at:` sub-bullets is equal on both sides. `bin/check-refreeze-class.sh` run on each pair (base blob, working file) exits **exactly 1** and prints the `check-refreeze-class: class-b:` token; the control that makes this non-vacuous is the same script run on a byte-identical pair built from that same base blob, which must exit **exactly 2** — proving the classifier is genuinely consulted and is discriminating between the two inputs rather than returning a constant. Positive controls: both extracted check-line lists are asserted non-empty and each base side is asserted to carry at least one criterion bullet, so two empty sets cannot compare equal. Nothing outside `$TMPDIR` is written.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; test -r bin/check-refreeze-class.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063f.XXXXXX") || exit 1; i=0; for S in .shell-team/specs/T-1055-adapter-envelope.md .shell-team/specs/T-1052-records-editorial.md; do i=$((i+1)); test -s "$S" || { rc=1; continue; }; git show "$B:$S" > "$T/old$i.md" 2>/dev/null || { rc=1; continue; }; test -s "$T/old$i.md" || rc=1; cp "$T/old$i.md" "$T/same$i.md" || rc=1; grep -E '^[[:space:]]+- check:' "$T/old$i.md" > "$T/cb$i" || true; grep -E '^[[:space:]]+- check:' "$S" > "$T/cw$i" || true; test -s "$T/cb$i" || rc=1; test -s "$T/cw$i" || rc=1; cmp -s "$T/cb$i" "$T/cw$i" || rc=1; ab=$(grep -cE '^- \[[ xX]\] \*\*AC[0-9]+\*\* ' "$T/old$i.md" || true); aw=$(grep -cE '^- \[[ xX]\] \*\*AC[0-9]+\*\* ' "$S" || true); test "$((10#$ab))" -ge 1 || rc=1; test "$((10#$aw))" = "$((10#$ab))" || rc=1; sb=$(grep -cE '^[[:space:]]+- stale-at:' "$T/old$i.md" || true); sw=$(grep -cE '^[[:space:]]+- stale-at:' "$S" || true); test "$((10#$sw))" = "$((10#$sb))" || rc=1; o=$(bash bin/check-refreeze-class.sh "$T/old$i.md" "$S" 2>&1); s=$?; test "$s" -eq 1 || rc=1; printf '%s\n' "$o" | grep -qF -- 'check-refreeze-class: class-b:' || rc=1; bash bin/check-refreeze-class.sh "$T/old$i.md" "$T/same$i.md" >/dev/null 2>&1; s2=$?; test "$s2" -eq 2 || rc=1; done; test "$i" = "2" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** T-1060's two mutable citations become content-named anchors and its frozen twin is proved untouched. In `.shell-team/specs/T-1060-adopter-binding-docs.md`, the region between `<!-- BEGIN intent-block: T-1060 -->` and `<!-- END intent-block: T-1060 -->` is **byte-identical** to the same extraction from that file's branch-point blob — the untouched-frozen-twin proof, and the mechanical form of this task's no-third-class-B decision. Outside that region the literal `line 208` occurs **zero** times; inside it, it occurs **exactly once**, which is the frozen twin still carrying the stale citation this task deliberately does not repair. `T60anchor` extracts to exactly one line from this spec, occurs **exactly twice** in the file and **zero** times inside the marker region — the `2` being the length of the two-site list this criterion itself enumerates (the `## Summarized sources` line and the `## Decision points` DP5 row), not a measurement of any growing population, so it neither is nor needs a base-relative delta or a re-measurement trigger. Positive controls: the branch point resolves, the base blob is non-empty, both marker-region extractions are asserted non-empty before they are compared, and the base blob is asserted to carry `line 208` at least twice, so a file that never had the citation cannot satisfy the extinction clause.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; SP=.shell-team/specs/T-1063-editorial-batch.md; S60=.shell-team/specs/T-1060-adopter-binding-docs.md; for f in "$SP" "$S60"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063g.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: T60anchor -->' -v e='<!-- frozen-end: T60anchor -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/anchor"; test "$(grep -c . "$T/anchor" || true)" = "1" || rc=1; git show "$B:$S60" > "$T/base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/base" || rc=1; test "$(grep -cF -- 'line 208' "$T/base" || true)" -ge 2 || rc=1; ib(){ awk '$0=="<!-- BEGIN intent-block: T-1060 -->"{f=1;next} $0=="<!-- END intent-block: T-1060 -->"{exit} f' "$1"; }; ob(){ awk '$0=="<!-- BEGIN intent-block: T-1060 -->"{f=1} $0=="<!-- END intent-block: T-1060 -->"{f=0;next} !f' "$1"; }; ib "$T/base" > "$T/ib.base"; ib "$S60" > "$T/ib.work"; ob "$S60" > "$T/ob.work"; test -s "$T/ib.base" || rc=1; test -s "$T/ib.work" || rc=1; test -s "$T/ob.work" || rc=1; cmp -s "$T/ib.base" "$T/ib.work" || rc=1; test "$(grep -cF -- 'line 208' "$T/ob.work" || true)" = "0" || rc=1; test "$(grep -cF -- 'line 208' "$T/ib.work" || true)" = "1" || rc=1; test "$(grep -cFf "$T/anchor" "$S60" || true)" = "2" || rc=1; test "$(grep -cFf "$T/anchor" "$T/ib.work" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** #196's two corrections land inside T-1047's own board entry and nowhere else, with the same claim in five other entries left measurably alone. The entry is extracted per entry — from its top-level `- [` line bearing `**T-1047**` to the next top-level `- [` line, never by a board-wide grep, since this phrasing collides across entries — on both the board's branch-point blob and the working board, and its heading line occurs exactly once on each side. In the **working** entry: the literals `measured **empty** at this HEAD`, `.shell-team/todo.md:144`, `.shell-team/todo.md:22` and `own heading is at line 149` each occur **zero** times; the literals `git show 99d4038:.shell-team/todo.md` and `at the base ref` each occur **at least once**; and exactly one line matches `^  - editorial correction \(T-1063`, carrying the literal `#196`. In the **base** entry each of the four superseded literals occurs at least once, which is the positive control. Outside that entry the count of `measured **empty** at this HEAD` is **equal** on both sides — a delta of zero, re-derived at check time from both blobs rather than compared against a typed number — so the five other tasks' entries are proved untouched rather than merely unmentioned. Positive controls: the board path resolves through `bin/team-paths.sh`, the base blob is non-empty, and both entry extractions are asserted non-empty before any literal is counted.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063h.XXXXXX") || exit 1; git show "$B:$BD" > "$T/base" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/base" || rc=1; for side in base work; do if [ "$side" = base ]; then src="$T/base"; else src="$BD"; fi; c=$(awk 'index($0,"- [")==1 && index($0,"**T-1047**")>0 {n++} END{print n+0}' "$src"); test "$((10#$c))" = "1" || rc=1; awk 'index($0,"- [")==1 && index($0,"**T-1047**")>0 {f=1} f && index($0,"- [")==1 && index($0,"**T-1047**")==0 {exit} f' "$src" > "$T/e.$side"; test -s "$T/e.$side" || rc=1; done; for s in 'measured **empty** at this HEAD' '.shell-team/todo.md:144' '.shell-team/todo.md:22' 'own heading is at line 149'; do test "$(grep -cF -- "$s" "$T/e.base" || true)" -ge 1 || rc=1; test "$(grep -cF -- "$s" "$T/e.work" || true)" = "0" || rc=1; done; for s in 'git show 99d4038:.shell-team/todo.md' 'at the base ref'; do test "$(grep -cF -- "$s" "$T/e.work" || true)" -ge 1 || rc=1; done; n=$(grep -cE '^  - editorial correction \(T-1063' "$T/e.work" || true); test "$((10#$n))" = "1" || rc=1; grep -E '^  - editorial correction \(T-1063' "$T/e.work" | grep -qF -- '#196' || rc=1; tb=$(grep -cF -- 'measured **empty** at this HEAD' "$T/base" || true); tw=$(grep -cF -- 'measured **empty** at this HEAD' "$BD" || true); eb=$(grep -cF -- 'measured **empty** at this HEAD' "$T/e.base" || true); ew=$(grep -cF -- 'measured **empty** at this HEAD' "$T/e.work" || true); test "$((10#$tb - 10#$eb))" = "$((10#$tw - 10#$ew))" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC9** Records, board and frozen-intent hygiene, with both re-freeze ledgers recorded in the shape the route requires. `.shell-team/provenance/T-1063.md` and `.shell-team/interventions/T-1063.md` both exist and are conformant under their own checkers; the resolved board passes `bin/check-handoff.sh` and `bin/check-board-headings.sh` against the branch point; the branch diff is clean under `bin/check-pii-shapes.sh --base <branch-point>`; every top-level `- [` entry line present in the board's branch-point blob is still present verbatim in the working board; the board carries exactly one top-level entry line for this task, matched in a form that tolerates the close-out transition from `- [ ]` to `- [x]`; and `bin/check-intent.sh` reports **aligned** for all three of this spec, T-1052's spec and T-1055's spec against the resolved board. For **each** of `T-1052` and `T-1055`, its entry extracted per entry on both sides: the single anchored `- intent-hash (v` record names the version **one greater** than the base entry's, derived rather than transcribed; every `- freeze-attestation (` line present in the base entry is still present verbatim and their count is exactly one greater; and exactly one new anchored `- intent-ratified (` line is gained, carrying the token `class=B`. Positive controls: the board path resolves and is readable, its base blob is non-empty and carries at least one top-level `- [` line before the survival loop runs, and each base entry is asserted to carry at least one attestation line so the survival comparison cannot pass on an empty expected set.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063i.XXXXXX") || exit 1; if [ -r .shell-team/provenance/T-1063.md ]; then bash bin/check-provenance.sh .shell-team/provenance/T-1063.md >/dev/null 2>&1 || rc=1; else rc=1; fi; if [ -r .shell-team/interventions/T-1063.md ]; then bash bin/check-interventions.sh --task T-1063 -- .shell-team/interventions/T-1063.md >/dev/null 2>&1 || rc=1; else rc=1; fi; bash bin/check-handoff.sh "$BD" >/dev/null 2>&1 || rc=1; bash bin/check-board-headings.sh "$BD" --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-pii-shapes.sh --base "$B" >/dev/null 2>&1 || rc=1; git show "$B:$BD" > "$T/bb" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/bb" || rc=1; grep '^- \[' "$T/bb" > "$T/bl" || true; test -s "$T/bl" || rc=1; while IFS= read -r l; do test -n "$l" || continue; grep -qxF -- "$l" "$BD" || rc=1; done < "$T/bl"; test "$(grep -c '^- \[[ xX]\] \*\*T-1063\*\* ' "$BD" || true)" = "1" || rc=1; for S in .shell-team/specs/T-1063-editorial-batch.md .shell-team/specs/T-1052-records-editorial.md .shell-team/specs/T-1055-adapter-envelope.md; do test -s "$S" || rc=1; bash bin/check-intent.sh "$S" "$BD" >/dev/null 2>&1 || rc=1; done; for tid in T-1052 T-1055; do for side in base work; do if [ "$side" = base ]; then src="$T/bb"; else src="$BD"; fi; c=$(awk -v k="**$tid**" 'index($0,"- [")==1 && index($0,k)>0 {n++} END{print n+0}' "$src"); test "$((10#$c))" = "1" || rc=1; awk -v k="**$tid**" 'index($0,"- [")==1 && index($0,k)>0 {f=1} f && index($0,"- [")==1 && index($0,k)==0 {exit} f' "$src" > "$T/$tid.$side"; test -s "$T/$tid.$side" || rc=1; done; vb=$(grep -oE '^  - intent-hash \(v[0-9]+\)' "$T/$tid.base" | sed -E 's/.*\(v([0-9]+)\)/\1/' | head -n 1); vw=$(grep -oE '^  - intent-hash \(v[0-9]+\)' "$T/$tid.work" | sed -E 's/.*\(v([0-9]+)\)/\1/' | head -n 1); test -n "$vb" || rc=1; test -n "$vw" || rc=1; if [ -n "$vb" ] && [ -n "$vw" ]; then test "$((10#$vw))" = "$((10#$vb + 1))" || rc=1; fi; test "$(grep -cE '^  - intent-hash \(v[0-9]+\)' "$T/$tid.work" || true)" = "1" || rc=1; grep -F -- '- freeze-attestation (' "$T/$tid.base" > "$T/ab" || true; test -s "$T/ab" || rc=1; while IFS= read -r al; do test -n "$al" || continue; grep -qxF -- "$al" "$T/$tid.work" || rc=1; done < "$T/ab"; cb=$(grep -cE '^  - freeze-attestation \(' "$T/$tid.base" || true); cw=$(grep -cE '^  - freeze-attestation \(' "$T/$tid.work" || true); test "$((10#$cw))" = "$((10#$cb + 1))" || rc=1; grep -E '^  - intent-ratified \(' "$T/$tid.base" > "$T/rb" || true; grep -E '^  - intent-ratified \(' "$T/$tid.work" > "$T/rw" || true; rbn=$(grep -c . "$T/rb" || true); rwn=$(grep -c . "$T/rw" || true); test "$((10#$rwn))" = "$((10#$rbn + 1))" || rc=1; if [ -s "$T/rb" ]; then grep -vxFf "$T/rb" "$T/rw" > "$T/rn" || true; else cp "$T/rw" "$T/rn" || rc=1; fi; test "$(grep -c . "$T/rn" || true)" = "1" || rc=1; grep -qF -- 'class=B' "$T/rn" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC10** Scope lock. The branch's changed-and-added file set — the tracked half from the commit range `<branch-point>...HEAD` unioned with the untracked half from `git ls-files --others --exclude-standard`, since this spec and this task's records are untracked at freeze time and a commit range cannot see them — contains this spec, the three edited specs, `agents/pm-spec.md`, `agents/qa-verifier.md` and the resolved board, and **nothing** outside the allow-list. The allow-list is those seven paths plus this task's three records (`.shell-team/provenance/T-1063.md`, `.shell-team/interventions/T-1063.md`, `.shell-team/reviews/T-1063.md`) and `.shell-team/test-recipe.md`. Asserted as absences rather than merely omitted from the allow-list: the set contains **zero** paths under `bin/`, `tests/`, `.github/`, `templates/`, `docs/` or `skills/`, and **zero** occurrences of `README.md`, `README.ja.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `CHANGELOG.ja.md`, `.shell-team/lessons.md` or `agents/engineer.md`; no `agents/*.md` other than the two named appears; and no spec under `.shell-team/specs/` other than the four named is changed or added. The range is taken from the branch point precisely so four predecessors' delivered paths are not attributed to this task; the criterion measures that premise by requiring the branch point to differ from `develop`'s tip. **This criterion is merge-point-scoped and expected to go stale after merge** — once a later task's files land on the same ref, or once the parent branch is deleted at the batch merge and the ref stops resolving, its expected set no longer describes reality; do not widen its ref resolution, do not re-derive it per rework round, and do not annotate it afterwards. Positive controls: the branch point resolves, the board path resolves through `bin/team-paths.sh`, the required-path list is asserted non-empty before it is walked, and the changed-and-added set is asserted non-empty so an empty measurement cannot satisfy the zero-counts.
  - check: rc=0; BPREF=feature/1062-release-notes-compare-link; git rev-parse --verify --quiet "$BPREF^{commit}" >/dev/null || exit 1; B=$(git merge-base "$BPREF" HEAD) || exit 1; test -n "$B" || exit 1; D=$(git rev-parse 'develop^{commit}') || exit 1; test "$B" != "$D" || rc=1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1063j.XXXXXX") || exit 1; SELF=.shell-team/specs/T-1063-editorial-batch.md; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$BD" || exit 1; printf '%s\n' "$SELF" "$BD" .shell-team/specs/T-1055-adapter-envelope.md .shell-team/specs/T-1052-records-editorial.md .shell-team/specs/T-1060-adopter-binding-docs.md agents/pm-spec.md agents/qa-verifier.md > "$T/own"; test -s "$T/own" || rc=1; printf '%s\n' .shell-team/provenance/T-1063.md .shell-team/interventions/T-1063.md .shell-team/reviews/T-1063.md .shell-team/test-recipe.md > "$T/opt"; { git diff --name-only "$B"...HEAD; git ls-files --others --exclude-standard; } | LC_ALL=C sort -u > "$T/set"; test -s "$T/set" || rc=1; while IFS= read -r f; do test -n "$f" || continue; grep -qxF -- "$f" "$T/set" || rc=1; done < "$T/own"; cat "$T/own" "$T/opt" | LC_ALL=C sort -u > "$T/allow"; extra=$(grep -vxFf "$T/allow" "$T/set" | grep -c . || true); test "$((10#$extra))" = "0" || rc=1; for d in bin tests .github templates docs skills; do test "$(grep -c "^$d/" "$T/set" || true)" = "0" || rc=1; done; for p in README.md README.ja.md CONTRIBUTING.md CHANGELOG.md CHANGELOG.ja.md .shell-team/lessons.md agents/engineer.md; do test "$(grep -cxF -- "$p" "$T/set" || true)" = "0" || rc=1; done; ag=$(grep '^agents/' "$T/set" | grep -vxF -e agents/pm-spec.md -e agents/qa-verifier.md | grep -c . || true); test "$((10#$ag))" = "0" || rc=1; sp=$(grep '^\.shell-team/specs/' "$T/set" | grep -vxF -e "$SELF" -e .shell-team/specs/T-1055-adapter-envelope.md -e .shell-team/specs/T-1052-records-editorial.md -e .shell-team/specs/T-1060-adopter-binding-docs.md | grep -c . || true); test "$((10#$sp))" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** Runtime, `SKIP` by design — no command can prove a command was run. **This criterion is reported item by item, in this order, one line per item, each naming what was measured and what the result was; a compressed summary of it does not discharge it, and a whole-set claim inside any item carries the enumeration it summarises.** **(a)** The **ratification-package byte-match audit**: for **every** frozen block in `### Replacement texts` — the set enumerated from that section at report time rather than from a number declared here — the bytes extracted from this spec compared against the bytes actually present in the target file after the edit, reported block by block as matched or not, with the enumeration carried beside the whole-set claim; and, separately, confirmation that the two class-B replacement texts the ratification package carried are byte-identical to the blocks frozen here, with any divergence reported as a finding rather than reconciled silently. **(b)** The **full-population blast-radius diff**: the literal enumeration command, the population it returned, the scratch tree the base-side verdicts were read from **at that ref's committed blobs**, and per differing spec one line stating which criterion changed verdict and why. Four predictions are called out as ones that must be **measured, not inferred**, and each is re-measured rather than trusted — `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC2** (predicted newly red; the every-base-line-survives lock on `agents/pm-spec.md`), `.shell-team/specs/T-1052-records-editorial.md` **AC10** (predicted newly red; the zero-deletion clause on `agents/qa-verifier.md`), `.shell-team/specs/T-1051-inspection-ux-polish.md` **AC12** (whose own text declares it merge-point-scoped and expected stale) and `.shell-team/specs/T-1060-adopter-binding-docs.md` **AC11** (relayed from another task's review record at a different ref). The known phantom `AC1: FAIL (exit 127)` inside T-1061's spec is reported as observed on both sides and excluded from the difference by pairing verdicts same-value-to-same-value rather than by row order. Where a measured verdict differs from this spec's prediction, `## Blast radius` — which sits outside the intent block — is corrected to the measurement and the correction is stated; the prediction is never defended. **(c)** The **`#171` class sweep**: the literal command, every shipped file it returned, and for each one whether it was repaired, and if not, why — including the measured judgment on `skills/run/SKILL.md`'s occurrence, which this spec measured at **line 12** rather than at line 47. **(d)** The **`skills/run/SKILL.md` judgment recorded as a decision**: what its `#171` citation claims, whether that claim holds for this repository's #171, the disposition taken, and the issue it was routed to — stated as a judgment with its reason, never as a silence. **(e)** The full CI-wired step list of `.github/workflows/check-handoff.yml` run locally in workflow order, every step's result stated, any step judged inapplicable named individually with its reason. **(f)** A producer-run mutation self-check on scratch copies outside the checkout (never the working tree), each mutation observed red on the intended criterion, restored, observed green again, reported separately: revert one `new` fragment to its `old` twin in each of `.shell-team/specs/T-1055-adapter-envelope.md` and `agents/pm-spec.md` (AC1 or AC2, and AC5, red); delete the `Q171x` clause from the re-spliced line in `agents/qa-verifier.md` only, leaving the spec's block intact (AC4 red on the whole-line symmetric match); paste the `EFALL` fragment into `agents/qa-verifier.md` (AC4 red on non-duplication); change one byte inside `.shell-team/specs/T-1060-adopter-binding-docs.md`'s marker region (AC7 red on the frozen twin); move one `- check:` line's bytes in either re-frozen spec (AC6 red); and add one file under `templates/` (AC10 red). **(g)** The measured 40-hex values of `git merge-base feature/1062-release-notes-compare-link HEAD`, `git rev-parse feature/1062-release-notes-compare-link`, `git rev-parse feature/1061-adopter-docs-gate` and `git rev-parse develop`, plus `git branch --show-current` and the exit status of `git merge-base --is-ancestor <branch-point> HEAD` — the stacked premise every base-side criterion here rests on, including the branch-point-≠-`develop`-tip inequality AC10 asserts. **(h)** Every relayed premise in `## Assumptions` re-measured with the measured value reported beside it, and any premise that could not be measured from the executing side named as still relayed rather than silently dropped — the four issue bodies and their open state specifically. **(i)** A per-source report for every `## Summarized sources` entry, stating whether the reporting role opened it and what it confirmed or contradicted about the cited distinction.

## Input space

**Reachable input classes** — what this task's deliverables really face:

- **This task has no runtime input surface at all, and that is a deliberate statement rather than an omitted section.** Every deliverable is prose: two sentences inside shipped agent prompts an agent reads, four sites inside this repository's own specs, and one entry in its own board. Nothing this task writes is parsed, executed, or given input by any program at run time.
- **The mechanical readers are checkers reading fixed, named paths**, and they are the whole of the input space: `bin/check-acs.sh` over this spec's own `- check:` lines and over the two re-frozen specs' unchanged ones; `bin/check-intent.sh`, `bin/check-refreeze-class.sh`, `bin/check-prompt-sync.sh`, `bin/check-handoff.sh`, `bin/check-board-headings.sh`, `bin/check-pii-shapes.sh`, `bin/check-provenance.sh` and `bin/check-interventions.sh`; and the `- check:` lines of merged criteria in other specs, which read these same files. Each is invoked explicitly, from the repository root, on committed, reviewed content.
- **The board this task appends to is a single markdown file of roughly 2.4 MB whose entries are extracted by the top-level `- [` idiom**, with per-task sub-bullets that legitimately repeat one another's phrasing across entries — which is why every board-scoped assertion here is entry-scoped. That collision is real, measured (six entries carry one of the phrases this task removes from one of them), and handled.
- **The two agent files are hand-written regions with generated marker regions elsewhere in the same file**, so an edit inside the hand-written region and an edit inside a generated region are different kinds of change; only the first is reachable here, and `bin/check-prompt-sync.sh` is what distinguishes them.
- **Ordinary UTF-8 markdown prose containing backticks, em dashes, arrows and asterisk emphasis**, which every fragment this task substitutes does, and which `grep -F` with a pattern file reads without interpretation.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect against:

- **An adversarially planted marker** — a nested, duplicated or forged `<!-- frozen-begin: … -->` pair, or an intent-block marker inside a fenced code block, designed to defeat the `awk` extractions. Every extraction here asserts an exact line count and fails closed on it; no criterion is widened to detect a deliberate forgery, because `CLAUDE.md`'s security invariant makes this repository's own committed content trusted.
- **A NUL byte, invalid UTF-8, mixed line endings, or a file with no trailing newline** in any file this task edits or any blob it reads.
- **A filename containing a newline, a tab, or a byte `core.quotePath` would C-style-quote**, in any path this task touches or any path a criterion enumerates.
- **A hostile `$TMPDIR`**, a symlink race against `mktemp -d`, or a permission flip between creation and first write.
- **A concurrent writer** mutating the board, either agent file or any spec mid-run — including a second agent appending to the board while a criterion extracts an entry from it.
- **A board carrying two top-level entry lines for one task id**, or an entry whose sub-bullets are indented differently from every existing entry. Both are refused by the exactly-one clauses rather than parsed; neither is produced by any shipped path.
- **Ever-longer synthetic prose**: a replacement fragment thousands of characters long, a spec with hundreds of frozen blocks, a board entry with hundreds of sub-bullets. Real content produces none of these, and a finding that only fires past such a size is `out-of-input-space`.
- **A shipped file this task does not name** being edited by someone else during the task, and any defect whose only trigger is such an edit. The scope lock measures the file set; it does not defend against a third party.

<!-- END intent-block: T-1063 -->

## Decision points

| DP | Decision |
|---|---|
| **DP1** | **The two class-B re-freezes are the smallest deltas that make the frozen text true again, and they are fragment substitutions inside existing criteria — never new criteria, never renumbering, never a `- check:` line.** AC6 locks that mechanically from both sides (byte-identical check lines, equal criterion counts, equal `- stale-at:` counts, plus the classifier's own `class-b` verdict against a byte-identical control). The alternative — leaving the frozen text stale and disclosing it — is rejected for these two specifically because both describe *how to perform a verification*: AC17(a) and AC17(l) instruct a future rework to run mutations that cannot be run, and a reader following them either fails or invents a substitute, which is a different defect class from an explanatory sentence going stale. |
| **DP2** | **The `Q171` clause names the engineer-side fallback and never restates it.** Duplicating the fallback's wording into `agents/qa-verifier.md` would put one norm in two files with no generator keeping them equal — the exact shape this repository's own rule about a second hand-written copy of a rule warns about. AC4 measures the reference rather than trusting it: the fallback's own fragment must occur **zero** times in `agents/qa-verifier.md` and **exactly once** in `agents/engineer.md`. The rejected alternative, promoting the fallback into a shared generated prompt block, is a mechanism this task's operator ruling forbids and would touch `templates/` and every consumer. |
| **DP3** | **The re-splice into `agents/qa-verifier.md` is a coordinated pair with the re-freeze, not an independent edit.** `.shell-team/specs/T-1052-records-editorial.md`'s own **AC8** re-derives `Q171` from that spec at check time, so the two must move together or that merged criterion reddens. AC4 reuses that criterion's idiom deliberately — extract from the spec, require exactly once as a whole line, require it inside the hand-written region — so the two criteria agree by construction rather than by care. Measured, and load-bearing: the file's first generated marker is at line 83 and `Q171` sits at line 68, so no regeneration is involved and no generated byte moves. |
| **DP4** | **`agents/pm-spec.md`'s `#171` citation is dropped rather than repointed, and the example survives verbatim.** The escalation example's true origin was searched for across this checkout and not found; #210 explicitly permits dropping the number. Repointing to a guessed issue would replace a measured-wrong citation with an unmeasured one — strictly worse, because the next reader has no way to tell. AC5 keeps the substance by requiring `full-width` still present exactly once while `#171` goes to zero. |
| **DP5** | **No third class-B re-freeze for T-1060's frozen twin.** The same stale `line 208` citation sits inside that spec's `## Non-goals`, and repairing it would cost a full ratification cycle for a line-number nit inside merged frozen intent — disproportionate on this repository's own terms, which treat frozen prose carrying no check line as a disclosed finding collected into a batched editorial pass rather than a per-instance re-freeze. This task **is** that batched pass for the mutable half; the frozen half stays stale, is disclosed in `## Blast radius`, and AC7 proves it byte-identical to its branch-point blob so the decision is measurable rather than merely stated. |
| **DP6** | **The board repairs use content-named anchors, never corrected line numbers, and the correction is announced in the entry rather than performed silently.** Measured: T-1046's entry now sits at line 1213 and T-1043's at 1181, so the numbers the record argues about (144, 149, 150) are all wrong today — renumbering would make them wrong again at the next append. The `- editorial correction (T-1063` sub-bullet AC8 requires is what keeps this from being a silent rewrite of an append-only record; the review record that found the defect stays untouched, so the history is preserved where history belongs. The five other entries carrying the same phrase are out of #196's scope and are proved untouched by AC8's outside-the-entry equality rather than left unmentioned. |
| **DP7** | **The `bin/check-acs.sh` fenced-example defect is filed as its own issue, not folded into #250 and not fixed here.** Two independent records report a phantom criterion matched inside a fenced illustrative block in `.shell-team/specs/T-1061-adopter-docs-gate.md`, always exit 127, on both sides of an unrelated diff. Folding it into #250 is rejected on a measured distinction: #250 builds a checker that does not exist yet, while this defect is in the checker **every task in this repository already runs**, so folding it would hide a live defect behind an unshipped one's schedule. Fixing it here is rejected because it is a `bin/` change with a fixture suite behind it — a different task with a different input space, and one this task's operator ruling explicitly excludes. The orchestrator files it at the ratification stop; **AC11(d)** requires the disposition reported. |
| **DP8** | **The declaration is `no`, and the discriminator is stated rather than assumed.** This task edits two shipped agent prompts, which an adopter's loop does read — so the question is real. The discriminator applied is whether an adopter gains, loses or must learn something they invoke, configure or read about: no command, flag, config file, layout or output changes; nothing under `README*` or `docs/` describes the two sentences being corrected, so a `yes` would produce a waiver for a citation fix rather than documentation. The one clause added to `agents/qa-verifier.md` names an obligation that already ships in `agents/engineer.md`. Per the freeze-time rule, a `no` carries neither an `- adopter-surface:` line nor a waiver, and this spec carries neither. |
| **DP9** | **Three anchors this task was handed were measured false and are recorded as hand-off findings rather than corrected in passing.** (1) #226's third item is `.shell-team/specs/T-1055-adapter-envelope.md` **AC17(l)**, which is **inside** that spec's intent block — it was routed as a mutable item and is carried here inside the class-B re-freeze instead. (2) T-1060's third `line 208` site is at **L702** (`## Decision points`, DP5), not L727. (3) `skills/run/SKILL.md`'s `#171` occurrence is at **L12**, not L47; L47 carries `#250` and is correct. Each is recorded in `.shell-team/interventions/T-1063.md` as an `assumption-contradicted` entry, per the rule that a relayed premise measured false is a finding about the hand-off rather than a silent correction. |

## Body-to-AC correspondence

| # | Body directive | Where stated | Promoted to |
|---|---|---|---|
| 1 | T-1055's row-52 exemption names the criteria that carry it today | Goal, DP1 | **AC1** (`R52old` zero, `R52new` once, base-blob controls) |
| 2 | T-1055's canon-region warning names the comparison that reports a hand-edit today | Goal | **AC1** (`DDold`/`DDnew`) |
| 3 | AC17(a)'s two unperformable mutations are replaced by mutations against what AC6/AC7 measure now | Goal, DP1 | **AC2** (`A17old`/`A17new`, both carried on the AC17 bullet line) |
| 4 | AC17(l)'s recipe drops the binding step whose mode was removed | Goal, DP1 | **AC2** (`L17old`/`L17new`) |
| 5 | Neither re-freeze moves a `- check:` line, a criterion count or a `- stale-at:` count | Goal, DP1 | **AC6** (byte-identical check lines, equal counts, classifier `class-b` with a control) |
| 6 | `Q171` gains one clause naming the fallback by reference, never restating it | Goal, DP2 | **AC3** (the clause is in the block, absent at base) + **AC4** (`EFALL` zero in qa-verifier, once in engineer) |
| 7 | T-1052's `## Problem` stops framing the runtime criterion in the singular | Goal | **AC3** (`P52old`/`P52new`) |
| 8 | The same `Q171` text is re-spliced so T-1052's own symmetric-match criterion stays true, and the superseded line is extinct | Goal, DP3 | **AC4** (whole-line match once, in the hand-written region; base-blob line at zero) |
| 9 | `agents/engineer.md` is read and left unchanged | Goal, Non-goals | **AC4** (no tracked diff, no untracked entry) + **AC10** (absent from the changed set) |
| 10 | The `#171` citation is dropped while the escalation example survives | Goal, DP4 | **AC5** (`#171` zero, `full-width` exactly once) |
| 11 | The adopter-docs checklist item stops attributing the freeze attestation to this role | Goal | **AC5** (`PM89old`/`PM89new`, plus the SKILL.md cross-file control) |
| 12 | The canonical declaration-grammar sentence does not move by one byte | Goal, hard constraint | **AC5** (every non-empty block line present, block and registry unchanged, `check-prompt-sync.sh` exit 0) |
| 13 | Both mutable `line 208` citations become content-named anchors | Goal | **AC7** (`line 208` zero outside the region, `T60anchor` twice) |
| 14 | T-1060's frozen twin stays stale and untouched; no third class-B | Non-goals, DP5 | **AC7** (marker region byte-identical to base; `line 208` exactly once inside it) |
| 15 | T-1047's entry stops claiming the measurement was taken at HEAD | Goal, DP6 | **AC8** (superseded literal zero in the entry, base-ref literals present) |
| 16 | The three board line-number meta-citations are replaced by content-named anchors | Goal, DP6 | **AC8** (three literals zero in the entry, base entry carrying each) |
| 17 | One correction sub-bullet says which lines changed and why | Goal, DP6 | **AC8** (exactly one `- editorial correction (T-1063` line, carrying `#196`) |
| 18 | The same claim in five other entries is left alone | Goal, Non-goals, DP6 | **AC8** (outside-the-entry count equal on both sides) |
| 19 | Records exist and are conformant; no board line is lost; both ledgers record the re-freeze | Goal, repo convention | **AC9** |
| 20 | No edit under `bin/`, `tests/`, `.github/`, `templates/`, `docs/`, `skills/`; no `README`/`CONTRIBUTING`/`CHANGELOG`/`lessons` edit | Non-goals | **AC10** |
| 21 | `skills/run/SKILL.md` is read and judged, never edited | Non-goals, DP7 | **AC10** (absent from the changed set) + **AC5** (unchanged, and the positive control) + **AC11(c)(d)** (the judgment reported) |
| 22 | No merged criterion is repaired, widened or annotated; every red is disclosed | Non-goals | **AC10** (no other spec changed or added) + **AC11(b)** (disclosure by full-population diff) |
| 23 | No new checker, suite, CI step or placement machinery; criteria stay at four shapes | Non-goals (operator ruling) | **AC10** (zero paths under `bin/`, `tests/`, `.github/`) — the shape constraint itself is *info-only (not promoted to AC)*: it governs how this spec is written, so a criterion asserting it would be a criterion asserting its own form, and review is the surface that judges it |
| 24 | The `bin/check-acs.sh` fenced-example defect is filed, not fixed and not folded into #250 | Non-goals, DP7 | **AC10** (`bin/` at zero) + **AC11(d)** (the disposition reported) — the filing itself is *info-only (not promoted to AC)*: no command run in this checkout can read GitHub state |
| 25 | Issue closure happens at the batch merge, not here | Non-goals | *info-only (not promoted to AC)* — a GitHub-side state no command in this checkout can assert, and the loop's close-out convention already defers it |
| 26 | No retro, no acting on another fast-follow, no merge, no push | Non-goals | *info-only (not promoted to AC)* — process constraints outside any criterion this branch can run on itself; a retro would appear under `.shell-team/retros/`, which **AC10**'s allow-list already excludes |
| 27 | The declaration is `no`, with neither a surface line nor a waiver beside it | Goal (declaration), DP8 | *info-only (not promoted to AC)* — the freeze-time gate that reads it is a duty the coordinating session performs, not an exit code; `bin/check-adopter-docs.sh` was carved out to issue #250 and does not exist, so a criterion invoking it would be broken as a command. **AC9**'s `check-intent.sh` clause confirms only that the line is inside the hashed region |
| 28 | One base-ref discriminator applies to the whole spec | Base ref header | **AC1**–**AC10** all resolve the same ref and none resolves `git merge-base develop HEAD`; **AC10** additionally measures the branch-point-≠-`develop`-tip premise |
| 29 | Three relayed anchors were measured false and are recorded as hand-off findings | DP9 | **AC9** (`check-interventions.sh` conformant) + **AC11(h)** (the re-measurement report) |

## Blast radius

Method: **full-population diff**. Enumerate every merged spec, run each one's `- check:` lines at the base ref and again at `HEAD`, and difference the two verdict sets criterion by criterion. A `grep` inventory of the specs that happen to mention `agents/pm-spec.md` or the board is **not** an acceptable substitute: a criterion that reaches its target through indirection — a board path built at run time from `bin/team-paths.sh --get todo`, a file named only through a shell variable — matches no search for a literal path, and that method has already missed live criteria in this repository.

- enumeration command: `git ls-tree -r --name-only <branch-point> -- .shell-team/specs | grep '\.md$'`, where `<branch-point>` is `$(git merge-base feature/1062-release-notes-compare-link HEAD)`
- population: re-derived at run time by that command rather than declared here, so this section carries no count that ordinary corpus growth can falsify; **AC11(b)** requires the returned population to be stated beside the measured table.
- base-side reading target: a detached `git worktree add --detach <scratch> <branch-point>`, removed after use, so every base-side verdict is produced from that ref's **committed blobs** and never from the working tree, whose bytes at read time are not that ref's. Individual blob comparisons inside the table use `git show <ref>:<path>`.

The rows below are this spec's **predictions**, reasoned from first-hand reads of each named criterion's own check line by a role that cannot run git. The authoritative set is the differencing run required by **AC11(b)**; where a measured verdict differs, this table is corrected to the measurement and the correction is stated in the hand-off.

| Merged criterion | base | head | Why |
|---|---|---|---|
| `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC2** | base: PASS | head: FAIL | **The costliest newly-reddened criterion, and it is unavoidable.** Its clause requires every non-blank line of `agents/pm-spec.md`'s branch-point blob to survive verbatim; an in-place correction replaces two lines, so any repair to that file reddens it by construction. It carries **no** merge-point-scoped label. The sprint plan accepted this cost when it scheduled #210 and T-1061's own round-3 Minor against that file. Disclosed, not repaired — repairing it means editing merged frozen intent. Its sibling clauses (the generated-region byte-identity and `check-prompt-sync.sh`) stay green, which **AC5** independently re-asserts. |
| `.shell-team/specs/T-1052-records-editorial.md` **AC10** | base: PASS | head: FAIL | Newly reddened, and labelled: its `--numstat` deletion-column-zero clause on `agents/qa-verifier.md` is broken by an in-place line replacement, and its own body declares the `--numstat` halves merge-point-scoped and expected to go stale. Must be measured rather than inferred — its base verdict depends on whether any task since `a804703` already deleted a line from either agent file. |
| `.shell-team/specs/T-1052-records-editorial.md` **AC8** | base: PASS | head: PASS | The criterion the re-splice is coordinated with, and the one **AC4** exists to keep green: it re-derives `Q171` from the spec at check time, so a matched pair of edits leaves it satisfied and an unmatched pair reddens it. |
| `.shell-team/specs/T-1052-records-editorial.md` **AC1**, **AC3**, **AC4**, **AC5**, **AC6**, **AC7**, **AC9** | base: — | head: unchanged | None reads a file this task edits, except AC9's `agents/pm-spec.md` clause, which asserts that file **empty** in T-1052's own diff against `a804703` — already false since T-1053, so pre-existing rather than caused here. Every verdict must be measured. |
| `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC1** | base: PASS | head: PASS | Requires `bin/check-prompt-sync.sh` exit `0` and the registry row unchanged — both re-asserted independently by **AC5**, which is why the canonical-line constraint is a criterion here and not a note. |
| `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC13** | base: FAIL | head: FAIL | Merge-point-scoped scope lock, already reddened by T-1062; this task's paths deepen it. Its own body declares it expected to go stale exactly this way. |
| `.shell-team/specs/T-1062-release-notes-compare-link.md` **AC5** | base: PASS | head: FAIL | Newly reddened merge-point-scoped scope lock: its allow-list is T-1062's own paths plus its records, and its own clause forbids any other spec in the changed set. Its body declares it expected to go stale after merge. |
| `.shell-team/specs/T-1062-release-notes-compare-link.md` **AC6** | base: PASS | head: PASS | Board survival plus `check-intent.sh` for T-1062: this task only appends to the board and touches neither T-1062's spec nor its hash. |
| `.shell-team/specs/T-1051-inspection-ux-polish.md` **AC12** | base: FAIL | head: FAIL | Its five byte-locked English surfaces include `agents/pm-spec.md` and `skills/run/SKILL.md`, both already edited by T-1061 on this stack. Pre-existing, deepened by this task's `agents/pm-spec.md` edit. Labelled merge-point-scoped. Must be measured, not inferred. |
| `.shell-team/specs/T-1050-check-layer-fast-follow.md` **AC15** | base: FAIL | head: FAIL | Names `agents/engineer.md` and `agents/qa-verifier.md` among files required byte-identical to `4a709af`; already red since T-1052 shipped the two rules. Labelled merge-point-scoped. Pre-existing, deepened. |
| `.shell-team/specs/T-1055-adapter-envelope.md` **AC16**, `.shell-team/specs/T-1059-docs-release-notes.md` **AC10**, `.shell-team/specs/T-1060-adopter-binding-docs.md` **AC11**, `.shell-team/specs/T-1054-binding-config.md` **AC15** | base: FAIL | head: FAIL | Population criteria comparing a declared spec count against the live corpus. Already several behind at this task's base; this task's own spec moves each by one more. Each carries its own `- stale-at:` naming this event, or is disclosed in its own spec. The T-1060 row is **relayed** from `.shell-team/reviews/T-1061.md:298` and must be re-measured. |
| `.shell-team/specs/T-1055-adapter-envelope.md` **AC14**, `.shell-team/specs/T-1054-binding-config.md` **AC13** | base: FAIL | head: FAIL | Scope locks against older base refs, already red from four intervening tasks. Pre-existing. |
| `.shell-team/specs/T-1055-adapter-envelope.md` **AC1**–**AC13**, **AC15**, **AC17**, **AC18** | base: — | head: unchanged | The re-freeze moves prose inside AC17 only and no `- check:` line at all, which **AC6** locks; AC15's `check-prompt-sync.sh` clause stays green by **AC5**. Every verdict must be measured. |
| `.shell-team/specs/T-1047-promote-retro-2026-08-08.md` **AC12**, **AC13**, **AC14** | base: — | head: unchanged | They assert region-presence of `M1`/`M2A`/`M2B`/`M3` in `agents/pm-spec.md`, `skills/run/SKILL.md` and `agents/codex-reviewer.md`. This task edits neither `M1`'s line (the relayed-premise checklist item) nor any other pinned text, so they should be unaffected — a prediction, and one **AC11(b)** must measure, since `M1`'s own criterion reads the same section this task edits two bullets of. |
| `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC1** (phantom duplicate) | base: FAIL (127) | head: FAIL (127) | Not a criterion: an illustrative `- [ ] **AC1**` inside a fenced block in that spec's `## Round-3 drop-execution package`, matched as a second `AC1` with a literal `check: ...` that is not a command. Identical on both sides, pre-existing, excluded from the difference by same-value pairing. **DP7** files it. |

## Assumptions

- **Measured first-hand by this role** (file reads, no shell): every line anchor, count and quoted fragment in `## Summarized sources`; the branch and ref SHAs read from `.git/HEAD` and `.git/refs/heads/`; that `agents/pm-spec.md` carries `#171` once, `full-width` once and `state in the freeze attestation` once; that `skills/run/SKILL.md` carries the last of those exactly once and `#171` at L12; that `walk each distinct assertion its prose demands instead` occurs once in `agents/engineer.md` and zero times in `agents/qa-verifier.md`; that `measured **empty** at this HEAD` occurs six times in the board across six entries; that T-1052's board entry is at **v3** and T-1055's at **v6**, so this task's re-freezes are v3→v4 and v6→v7; and that the board currently carries zero `T-1063` lines.
- **Relayed, not measured — the bodies and open state of issues #226, #211, #210 and #196.** This role has no GitHub read path. Each defect statement above is reconstructed from in-repository records this role did read first-hand (`.shell-team/reviews/T-1047.md`, `.shell-team/reviews/T-1055.md`, `.shell-team/reviews/T-1061.md`, `.shell-team/specs/T-1052-records-editorial.md`'s own `## Assumptions`). **AC11(h)** requires the executing side to measure each issue's state and body and report the values; if any issue's scope differs from this spec's, that is a finding about the hand-off, recorded in the interventions file rather than silently absorbed.
- **Relayed, not measured — the operator's ruling of 2026-08-13 at T-1062's STOP resolution.** It reached this role through the orchestrator's prompt and is written into `## Non-goals` as a named constraint. This role cannot read the planning exchange; the orchestrator holds the primary confirmation and the freeze run states it beside this line.
- **Relayed, not measured — `.shell-team/specs/T-1060-adopter-binding-docs.md` **AC11**'s FAIL verdict.** Read first-hand at `.shell-team/reviews/T-1061.md:298`, but measured there by another role at another ref. **AC11(b)** re-measures it at this task's own base and head; the prediction is never defended against the measurement.
- **Relayed, not measured — that T-1059, T-1060, T-1061 and T-1062 are unmerged to `develop`.** Consistent with the measured SHA inequality (`develop` at `d01ab30e…`, branch point at `f63c46b3…`) but the ancestry itself is unverified from this role. **AC10** asserts the inequality mechanically; **AC11(g)** reports the values and the ancestor check.
- **Assumed** that the working tree was clean and identical to `f63c46b3…` at authoring time, which is what makes the file reads above equivalent to branch-point blob reads. Stated because every base-side prediction rests on it; **AC11(g)** and the differencing run measure it properly.
- **Measured false, recorded as hand-off findings** (DP9): #226's third item is frozen rather than mutable; T-1060's third citation site is L702 rather than L727; `skills/run/SKILL.md`'s `#171` is at L12 rather than L47. None is silently corrected — each goes into `.shell-team/interventions/T-1063.md`.

## Open questions

None blocking.

## Notes for engineer

- **Every substitution is a within-line fragment replacement.** Five of the six target lines (T-1055's L173 and L267, T-1052's `Q171` block line, `agents/pm-spec.md`'s L79 and L89) are single physical lines carrying far more than the fragment being replaced. Replace the fragment, leave the rest of the line byte-identical, and do not reflow.
- **Order of work.** (1) The two re-freezes' target specs, since the ratification package is built from them; (2) the `agents/qa-verifier.md` re-splice, immediately after T-1052's block changes, so the pair never sits half-done in a commit; (3) `agents/pm-spec.md`; (4) T-1060 and T-1055's mutable sites; (5) the board's T-1047 corrections and this task's own entry; (6) blast radius and records. Nothing else.
- **Where the `Q171x` clause goes**: immediately before `Q171`'s closing backward-compatibility sentence ("When the spec carries no such criterion at all, keep the prior behavior unchanged — this rule is backward compatible."), so the rule still ends with its own scope statement. No criterion asserts that placement — it is guidance, per the operator ruling that this task builds no placement machinery — but a reviewer will read it, and putting it after the closing sentence reads as an afterthought.
- **Suggested wording for the sites this spec does not freeze**, offered as a starting point and not binding — the criteria admit any wording that satisfies them:
  - T-1060 `## Summarized sources` (L23): `- `README.md` and `README.ja.md`'s reviewer design-choices bullet delivered by T-1059, named by its content rather than by a line number, since README line numbering moves as those files grow — the distinction carried over: …` (keep the rest of the line unchanged).
  - T-1060 DP5 (L702): `**Relationship to `README.md` / `README.ja.md`'s reviewer design-choices bullet — named by its content rather than by a line number — and the no-duplication lock.** The new section is the **six-role mechanism**; that bullet is the **reviewer row's** default and its rationale. …` (keep the rest of the row unchanged).
  - Board L1087: `board hygiene: `## Active` was measured **empty** at the base ref `99d4038`, read as that ref's committed blob (`git show 99d4038:.shell-team/todo.md`) rather than at this HEAD, so this entry is a **pure insertion** …` (keep the rest unchanged).
  - Board L1096 / L1099 / L1100: replace each `.shell-team/todo.md:<n>` and `at line 149` with the entry it names — "T-1046's own board entry", "this entry's own self-application bullet" — and keep every surrounding clause.
  - Board correction sub-bullet: `  - editorial correction (T-1063, 2026-08-13): per issue #196, two claims in this entry were corrected in place — the board-hygiene bullet's "at this HEAD" now names the base ref `99d4038` its own AC17(d)(5) report measured against, and three board line-number meta-citations (in the AC17(c) report, the post-rework note and the QA PASS verdict) now name the entries they refer to instead of line numbers that have since moved. The findings themselves stay recorded in `.shell-team/reviews/T-1047.md`; nothing else in this entry changed.`
- **Do not renumber any board line-number citation.** Measured: T-1046's entry is at line 1213 today and T-1043's at 1181 — a corrected number is wrong again at the next append, which is the whole reason #196 exists.
- **Do not touch `.shell-team/specs/T-1060-adopter-binding-docs.md`'s marker region.** AC7 compares it byte for byte against the branch point. The third `line 208` in that file is inside it and stays.
- **Do not spell the fallback's own wording into `agents/qa-verifier.md`.** AC4 greps for it and requires zero. Refer to the engineer-side rule; do not quote it.
- **`agents/pm-spec.md` L89 carries one further oddity this task deliberately does not touch**: the phrase "carved out of this task's own droppable component" reads, in a shipped prompt, as if it were about the reader's current task rather than about T-1061. It is not in any issue's scope, no criterion here reads it, and repairing it would add a third edit to a file whose every-base-line lock this task already reddens. Observed, recorded, left alone; report it under **AC11(c)** so it is filed rather than rediscovered.
- **`- stale-at:` triggers**: none is attached to any criterion here, and that is a decision rather than an omission. No criterion counts a growing population: AC7's `2` is the length of a two-site list its own body enumerates, AC8's outside-the-entry comparison re-derives both sides at check time, and `## Blast radius` declares no population at all, re-deriving it in **AC11(b)** instead. AC10 carries the merge-point-scoped note its class requires.
- **Measured-at-ref labels**: this task's deliverables do print a result beside a ref label — the board correction whose whole point is that `## Active` was measured at `99d4038` and not at HEAD — so the check applies rather than being `not applicable`, and **AC8** carries it: the corrected text must name the ref *and* the committed-blob command (`git show 99d4038:.shell-team/todo.md`), which is what makes the label and the command agree. `## Blast radius`'s own base-side reads are covered by **AC11(b)**'s committed-blob requirement.
- **Run `bin/check-pii-shapes.sh` before the first commit**, per AC9 and the repository convention — the CI diff-time check is the backstop, not the primary defence.
- **`.shell-team/test-recipe.md` is allowed but not required** in AC10's allow-list. Append only if the blast-radius run or the entry-extraction idiom on a 2.4 MB board turns up a procedure worth passing on.
