# Cross-task dispatch reflection: a task's board entry carries a per-axis record of what the immediately preceding task elected, verified fail-closed against that predecessor's own entry before the freeze

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1109

## Problem

In a stacked train, each task's dispatch elections are decided at Plan and transcribed onto its own board entry, and nothing anywhere reads the previous task's elections while making them. Measured, not inferred: I grepped `skills/run/SKILL.md`'s whole Specify-phase gate stack (`:44`–`:55` — board transcription order, frozen-intent confirmation, adopter-docs gate, base-ref-discriminator gate, conformance-read gate, spec-review gate, verification-ceiling gate, oversight-profile gate) and **none of them reads a preceding task's `- dispatch:` rows**; `:51` names the predecessor only to resolve its *branch ref* for `- base-ref-discriminator:`, which answers a different question. So a mis-election repeated across three consecutive tasks reaches an operator's question rather than a loop gate, and by then it is three tasks old.

This is **Proposal 3 of issue #365, and it is the only part of that issue still open.** Proposals 1 and 2 shipped in T-1100, verified first-hand at their anchors rather than relayed:

- **Proposal 1 — the dispatch grammar split: complete.** `bin/close-out.sh`'s `DISPATCH_AXIS_TABLE` carries `verify-fixture:serial|tier1-fanout` and `verify-mechanism:serial|tier1-fanout` (the table's own assignment block); the parent-versus-refinement exclusivity refusal sits after the per-line loop (`case "$DISPATCH_SEEN_AXES" in *" verify "*)`); the duplicate-axis refusal sits inside it (`DISPATCH_SEEN_AXES`). Mirrored into `templates/prompt-blocks/dispatch-record.md`'s `**Refinements (T-1100).**` bullet and, through the registry, into `agents/tech-lead.md` and `skills/run/SKILL.md`.
- **Proposal 2 — the authoring rule: complete.** `agents/pm-spec.md:131`–`:133`, section `## Verify-axis refinement authoring duty (T-1100)`, carrying the literal `prices each half independently`; `docs/loop-engineering/phase-multiplexing.md:360`–`:361` (the two refinement `- dispatch-axis:` lines) and `:367`–`:370` (the four `- dispatch-rule:` lines, `:370`'s trigger naming the `full-population` mechanism-class diff on its own).

Re-specifying either of those is the single most likely failure mode of this task, and they are named as Non-goals below.

## Summarized sources

One line per document whose facts this spec restates, naming the distinction carried over. **Primary** means this role opened the file itself with a file-reading tool. **RELAYED** means the coordinating session pasted it into this role's prompt; this role holds no shell, no network and no `gh`.

- **Issue #365, Proposal 3** — **RELAYED** (scope pasted into this role's launch prompt; the issue body itself was not pasted verbatim this round). Distinction carried over: the remaining gap is that the freeze sweep for task N should read the immediately preceding task's `- dispatch:` rows for the same axis before task N's own are recorded, so a repeated mis-election surfaces inside the loop. Its measured **open** state is recorded in `## Assumptions` row **A-1**.
- `skills/run/SKILL.md` — **primary**, read at `:30`–`:99`. Distinctions: `:45` fixes the transcription window ("immediately after the entry exists and before the freeze sweep runs, in one fixed order") and is where `bin/check-entry-mode.sh` is invoked and branched on; `:46`–`:57` is the freeze sweep, which begins at `**Frozen-intent confirmation**` and therefore sits **after** `:45`; and no bullet in `:44`–`:55` reads a preceding task's dispatch rows.
- `bin/check-entry-mode.sh` — **primary**, read end to end (263 lines). Distinctions this spec rests on: it locates **exactly one** top-level entry for `--task` **in `## Active` only** (`sec ~ /^## Active/`), so it does not today look at `## Done`; its source-2 scan is anchored `'^[[:space:]]*- dispatch: specify — '` and its loose diagnostic net `'^[[:space:]]*-[[:space:]]+dispatch:[[:space:]]+specify[[:space:]]+—'` — both requiring the literal `dispatch:` with its colon; it strips a trailing CR per line (`:112`–`:117`); it distinguishes *absent* from *malformed* with a wider detection-only stem before choosing its message (`:131`–`:150`, `:165`–`:175`, `:206`–`:223`); it exits `1` for a content refusal and `2` for usage or an unresolvable environment; and its header (`:47`–`:53`) states its forward-only scoping — invoked only at the pre-freeze seam for the one task being frozen, **never from `bin/close-out.sh`**.
- `bin/close-out.sh` — **primary**, read at `:1`–`:60` and `:215`–`:363`. Distinctions: the dispatch gate is **validate-if-present**, saying nothing when the entry carries no `- dispatch:` sub-bullet, "adopter back-compat — presence is the shipped norm text's requirement … never this script's" (`:237`–`:248`) — the exact idiom this spec reuses for the reflection family; its scan anchor is `'^[[:space:]]*- dispatch:'`, requiring the literal colon; and `:46`–`:55` states that #341's board-sub-bullet reader is deliberately **not** invoked from this file.
- `templates/prompt-blocks/dispatch-record.md` — **primary**, read end to end (13 lines). Distinctions: it is the canonical statement of the record (`**The grammar.**` line 5, ` — ` as the field separator), of who decides and who records (line 4 — "the orchestrator transcribes each decision onto the task's own board entry … `tech-lead` never writes the board"), and of the validate-if-present validation contract (line 12).
- `templates/prompt-blocks/registry.txt` — **primary**, read end to end. Distinction: `contain dispatch-record.md agents/tech-lead.md skills/run/SKILL.md` — the block is `contain`-mode with exactly those two consumers, so every non-empty line added to it must appear verbatim in both, enforced by `bin/check-prompt-sync.sh`; `agents/pm-spec.md` is **not** a registered consumer of it.
- `docs/loop-engineering/record-tamper-resistance.md` — **primary**, read end to end. Distinctions: `tamper-arm-rule-v1` is two prescriptive questions asked in order (`:18`–`:49`), both conjuncts "prescriptive tests on the obligation's own consequence and content, never observations of what happens to exist today", with "no shipped script executes an arm-A obligation yet" named an **implementation gap** and "never evidence for arm B" (`:32`–`:40`); a single obligation may split across arms per duty (the T-1103 duty-A/B/C and T-1104 duty-A/B/C/D precedents, `:110`–`:214`); arm A's structural form may be satisfied by an **existing** arm-A script (`:94`–`:99`, T-1099's third promotion landing inside `bin/check-board-headings.sh`); and the re-evaluation trigger fired at the third promotion and is owed at a retro, explicitly not re-fired by the fourth or fifth (`:143`–`:147`, `:204`–`:214`).
- `.shell-team/todo.md` — **primary**, read at `:1`–`:40`, `:1962`–`:1975`, `:3630`–`:3659`, and by three targeted content searches over the whole file. Distinctions: `## Active` occurs at `:12` and `## Done` at `:3634`; **T-1108's entry, with its five `- dispatch:` rows (`:3642`–`:3646`), sits in `## Done`**, because `bin/close-out.sh` ran on its own branch before this branch was cut — so a scan restricted to `## Active` cannot see this task's own predecessor; the two entries currently in `## Active` are `T-1089` (`:1962`) and `T-1086` (`:2072`), both `BLOCKED`, which is the case where a predecessor *is* still in `## Active`; the predecessor relation is named in prose in two spellings and two keys — `- branch: … stacked behind …` (8 occurrences, `:3639` onward, the current era) and `- stacked: … branches from …` (16 occurrences, `:4577` onward, the earlier era) — and **both name a branch, never a task id**; `T-1060`'s `- stacked:` line (`:5229`) is the real corpus instance of the no-predecessor case ("**first task of sprint v2.0.1**, so it branches directly from `develop`"); and `no-predecessor` already occurs in this file at `:5168`, `:5254` and `:5263`.
- `CONTRIBUTING.md` — **primary**, read at `:55`–`:99`. Distinctions: `bin/close-out.sh` "moves the board entry to `## Done`", which is why a predecessor's rows are normally in `## Done` by the time its successor freezes; `## The board line format` (`:69`–`:88`) defines the top-level `- [ ]` shape and states that "the linter skips indented lines" and that "the `- [x]` entries under `## Done` are never inspected", so a new indented sub-bullet key is invisible to `bin/check-handoff.sh` by construction; and `:87` names the two board linters this task must keep green.
- `.shell-team/specs/T-1108-sweep-corpus-isolation.md` — **primary**, read at `:1`–`:78`, `:106`–`:153` and `:342`–`:373`. Distinctions carried over in **form**: the four-key declaration region and its `- base-ref-discriminator:` two-arm spelling (`:43`–`:46`), the `- reproduce:`-plus-verbatim-derivation-block pattern with whole-line anchoring (`:194`–`:203`), the enumerated-disclosed-exception carve-out idiom for a base-green-to-head-red flip (`:145`), the `- procedure-version:` row as **mandatory content** rather than presence-only (`:193`), and the pre-commitment's never-dropped/droppable-order shape (`:342`–`:353`). Carried over in **fact**: this task inherits the sweep procedure T-1108 delivered, in which the base arm's read is staged from one snapshot of the untracked telemetry corpus.
- `agents/pm-spec.md` — **primary**, read at `:78`–`:94` and `:108`–`:137`. Distinctions: `:86` is the canonical two-arm sweep procedure **as amended by T-1108**, whose staging clause discloses that only the base arm is staged and "the live/HEAD arm still reads the live corpus unstaged"; `:91` triggers `- verification-class: mechanism` on "any path under `bin/`, `tests/`, `templates/`, a CI workflow, or the semantics of a checker", and states the **Downstream-impact inventory method governs unchanged** for that class; `:131`–`:133` is Proposal 2's shipped authoring duty, which this task does not touch.
- `.github/workflows/check-handoff.yml` — **primary**, read by targeted content search at `:137`–`:147`. Distinction: `tests/check-entry-mode/run.sh` already has its own workflow step (`:140`–`:141`), which is why extending `bin/check-entry-mode.sh` satisfies arm A's structural form (named `bin/` script + own suite + own CI step) **without** adding a workflow step.
- `README.md` and `README.ja.md` — **primary**, read at `README.md:170`–`:181` and by targeted content search in `README.ja.md`. Distinction: both carry a parallel **Situational dispatch record** bullet at `:175` stating the four-field grammar and `bin/close-out.sh`'s validate-if-present contract; neither mentions `bin/check-entry-mode.sh`.
- `docs/adopting.md` — **primary**, read at `:541`–`:600`. Distinction: `:577`–`:592` is the adopter-facing statement of exactly what `bin/check-entry-mode.sh` refuses on ("refuses a task's freeze unless the board carries both … a missing source refuses rather than passing silently"), which is the paragraph this task's change makes incomplete unless it is updated. Measured: `docs/adopting.ja.md` carries **no** counterpart — `check-entry-mode` occurs zero times in it — so there is no Japanese paragraph to keep in sync.
- `bin/derive-populations.sh` — **primary**, read at `:1`–`:60`. Distinctions: the emitted block's byte-exact grammar, that an unaccepted set exit status refuses the whole derivation rather than reading as an empty set, and that between two and eight `--set` values are required.
- `agents/tech-lead.md` — **primary**, read by targeted content search for the dispatch block. Distinction: it carries the `dispatch-record.md` block's own lines, including `**The grammar.**` at `:93`, which is the measured ground for the frozen claim that a line added to the canonical block must appear verbatim in this installed file too.
- `tests/check-entry-mode/run.sh` — **primary**, located by content search for `check-entry-mode`. Distinction, and the only one the frozen text rests on: this suite **exists** as a file under `tests/`, which together with `.github/workflows/check-handoff.yml:140-141` is what makes `bin/check-entry-mode.sh` already satisfy arm A's structural form. No claim is made here about which cases it currently contains; **AC9** measures its base-side content at check time rather than asserting it.
- `bin/team-paths.sh` — **primary**, read at `:100`–`:199` (during this stack's predecessor task, and re-confirmed here for the one fact used). Distinction: `--get todo` resolves the board path for either supported layout, which is why every criterion and every documented command below reach the board through it rather than naming `.shell-team/todo.md` literally.
- `bin/check-readme-version.sh` — **primary**, located by content search and by its use in `.shell-team/specs/T-1060-adopter-binding-docs.md:73`'s own check line. Distinction, and the only one **AC11** rests on: it is invoked as `bin/check-readme-version.sh README.md README.ja.md` and its exit status is what a merged criterion already treats as the version-agreement verdict for that pair. **AC11** reuses that established invocation rather than asserting anything further about the checker's internals.
- `docs/adopting.ja.md` — **primary**, measured by content search over the whole file. Distinction, carried into the frozen `- adopter-surface:` line: `check-entry-mode` occurs **zero** times in it, so there is no Japanese counterpart of `docs/adopting.md:577-592`'s backstop paragraph to keep in sync. The file does discuss the dispatch axes generally (`:176`, `:177`), which is why it sits in **AC12**'s allow-list as an optional path rather than being excluded outright.
- `docs/loop-engineering/phase-multiplexing.md` — **primary**, read at `:350`–`:379`. Distinctions: `:355` defines `recommendation: tier1-verification-fanout`'s second precondition as an orchestration step that can "actually launch N *agent* instances", `:370`'s `verify-mechanism — tier1-fanout` trigger requires both preconditions to **exist**, and `:375` still stands as an open follow-up to verify empirically whether the harness runs N concurrent Agent-tool invocations. Those three together are the measured ground for `## Assumptions` row **A-10**'s contested-election finding; no criterion in this spec rests on them.
- `.shell-team/specs/T-1060-adopter-binding-docs.md` — **primary**, read at `:73`–`:86`. Distinctions: the scope-lock shape this spec's **AC12** reuses (tracked half from `<base>...HEAD` unioned with `git ls-files --others --exclude-standard`, a required set plus an optional set, and the merge-point-scoped staleness note); and the pre-existing use of the token `no-predecessor` at `:75` and `:85`, which is why that token is classified **borrowed** in `## Assumptions` row **A-5**.

## Goal

<!-- BEGIN intent-block: T-1109 -->

- user-visible: yes — the deliverable adds a board sub-bullet family an adopter's loop must write and a new refusal class in a checker an adopter's freeze already runs. `bin/check-entry-mode.sh` is invoked by `skills/run/SKILL.md:45` at every task's Specify seam in every repository that loads this plugin, and `templates/prompt-blocks/dispatch-record.md`'s grammar is mirrored into two installed files (`agents/tech-lead.md`, `skills/run/SKILL.md`), so what an adopter's loop is required to record and what their freeze can be refused for both change. The corresponding documentation surfaces were measured to exist rather than assumed: `README.md:175` and `README.ja.md:175` state the dispatch record's grammar and validation contract, and `docs/adopting.md:577-592` states precisely what `bin/check-entry-mode.sh` refuses on. Discharged by an acceptance criterion carrying an indented `- adopter-surface:` line (**AC11**); no `- adopter-docs-waiver:` line is carried, since the two are exclusive.
- verification-class: mechanism — ruled against `agents/pm-spec.md:91`'s own definition rather than by feel: this task's diff reaches `bin/check-entry-mode.sh` (a path under `bin/`, and the semantics of a checker), `tests/check-entry-mode/run.sh` (a path under `tests/`) and `templates/prompt-blocks/dispatch-record.md` (a path under `templates/`) — three independent triggers, any one of which is sufficient. Nothing is priced down. In particular the **Downstream-impact inventory method** governs unchanged: the full-population two-arm diff over every merged spec is owed (**AC14**), and the procedure it is taken by is the version `agents/pm-spec.md:86` carries at this branch point — T-1108's amended text, in which one snapshot of the untracked telemetry corpus is staged into the base arm's worktree before either arm runs, with the live/HEAD arm reading the live corpus unstaged. That version is named in the inventory's own `- procedure-version:` row rather than left implicit.
- base-ref-discriminator: `B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi)` — the merge-base of the still-open stacked predecessor `feature/1108-sweep-corpus-isolation` with `HEAD` while that branch resolves in `refs/heads/`, and the merge-base of the integration branch `develop` with `HEAD` once it does not. The arm is selected by an explicit `git show-ref --verify --quiet` existence test and never by a `2>/dev/null ||` chain, which cannot tell a genuinely deleted predecessor apart from a `git merge-base` that failed for another reason; the predecessor's **merge-base** rather than its tip is used deliberately, because a rework round advancing that branch after this one was cut moves its tip but not the common ancestor. Spelled byte-identically in every criterion below that reads a base-side blob — **AC1**, **AC2**, **AC7**, **AC9**, **AC10**, **AC11**, **AC12** — seven in total, and no other criterion below reads one; the eight remaining criteria were each read for a base-side read and each measured to have none (**AC3**, **AC4**, **AC5**, **AC6** and **AC8** run only against fixture boards written into a temporary directory; **AC13** reads only the live resolved board; **AC14** reads only this spec and the live spec corpus; **AC15** is a reporting criterion), so the enumeration is a measurement rather than a list of the criteria that happened to catch the eye. Cross-checked as one set before the freeze. No 40-hex literal appears in any of them. Three residual cases are disclosed rather than engineered around, on the same footing: a predecessor deleted without being merged invalidates the stack and is a route-back to planning; a predecessor rebased, force-pushed or squash-merged after this branch was cut is a route-back on the same footing, because the recorded common ancestor may no longer be an ancestor of it and a squash lands its changes under a SHA sharing nothing with the originals; and a checkout that fetched the predecessor in neither `refs/heads/` nor `refs/remotes/` is a fetch-depth gap and a route-back, never the fallback arm firing silently.
- verification-ceiling: unit-and-static — every criterion below is settled by a scoped `grep`/`cmp`/`awk`/`git show`/`git diff` over committed blobs, by a shipped checker's exit status run against fixture boards written into a temporary directory, or by a fixture suite's own exit status; **AC14** additionally reads merged specs' committed criteria inside a `git worktree add --detach` scratch tree on this machine. Nothing here needs an adopter machine, a network service, a live executor invocation or a CI-only surface. **AC1** through **AC14** are each verified at exactly this ceiling non-vacuously, so the declaration is not vacuous. **No criterion sits above the declared ceiling and no `- above-ceiling:` sub-bullet is carried** — including **AC15**, whose subject is the reporting of work performed at this same ceiling in this same checkout, which is why it is a `SKIP`-by-design reporting criterion rather than an above-ceiling one. The one premise class that genuinely needs a network — issue **#365**'s state in **A-1** and the predecessor PR's state in **A-2** — is measured by the network-capable coordinating session and recorded beside those rows; no acceptance verdict turns on it.

**Goal (one sentence).** A task's board entry gains a per-axis **dispatch reflection** record — `- dispatch-reflection: <axis> — <predecessor> — <verdict> — <ground>`, one indented sub-bullet per dispatch axis that entry itself records, written by the coordinating session in the same window `skills/run/SKILL.md:45` already fixes for the `- dispatch:` transcription and **before** it, so the skim informs the elections it is about; where the task has no predecessor at all the record is the single conformant line `- dispatch-reflection: all — no-predecessor — no-predecessor-row — <ground>` rather than silence; and `bin/check-entry-mode.sh` — the arm-A script that already owns this exact seam, already has its own `tests/check-entry-mode/run.sh` suite and its own step in `.github/workflows/check-handoff.yml` — gains the judgment that makes the record non-fabricable, refusing the freeze (exit 1) when the family is present but malformed, when it does not cover every axis the entry's own `- dispatch:` rows record, when it mixes the no-predecessor form with a per-axis row, when the named predecessor id does not resolve to exactly one top-level board entry in **either** `## Active` or `## Done`, or when a stated `<verdict>` disagrees with the predecessor entry's own recorded value for that axis; while an entry carrying **no** reflection line at all still passes, the same validate-if-present shape and for the same adopter-back-compat reason `bin/close-out.sh` already applies to the `- dispatch:` family itself. The substantive judgment the record exists to provoke — whether a repetition is a mis-election — stays a reading a human or an agent performs and is disclosed as such, never claimed closed by the checker.

## Non-goals

- **Issue #365's Proposals 1 and 2 are out of scope and are not re-specified.** Both shipped in T-1100 and were verified first-hand at the anchors named in `## Problem`. Nothing below adds, removes or renames a dispatch axis key, changes any axis's closed value set, or edits `agents/pm-spec.md`'s `## Verify-axis refinement authoring duty (T-1100)` section.
- **`agents/pm-spec.md` is not edited at all.** The reflection is written by the party that writes the `- dispatch:` rows — the coordinating session — and `agents/pm-spec.md` is not a registered consumer of `templates/prompt-blocks/dispatch-record.md`, so it has no mirroring obligation either. Keeping it out of the diff is also what keeps this task from re-reddening the criteria that byte-pin its lines. **AC12** pins this mechanically.
- **No new `bin/` script and no new CI step.** The arm-A structural form is satisfied by the existing arm-A script `bin/check-entry-mode.sh`, its existing suite and its existing workflow step, the shape T-1099's third promotion established. `.github/workflows/check-handoff.yml` is byte-identical to its base blob (**AC12**).
- **No change to the `implement`, `verify`, `verify-fixture`, `verify-mechanism`, `specify` or `spec-review` axis definitions, to `bin/close-out.sh`, or to any depth-axis (#274) work.** `bin/close-out.sh` is byte-identical to its base blob, and its own dispatch gate must remain blind to the new key rather than newly refusing it (**AC7**, **AC12**).
- **No fan-out mechanism is built or changed.** `bin/aggregate-verdicts.sh`, `bin/check-fanout-instances.sh` and the T-1069 pilot's pricing stay untouched; only the election *record* and the new reflection step are in play.
- **No machine-readable predecessor-relationship key is introduced on the board.** No `- stacked-on:` key is coined, the existing `- stacked:` and `- branch:` prose spellings are neither normalized nor rewritten, and the predecessor is never inferred positionally from board order. The predecessor's task id is written into the reflection record itself, and the checker's duty is to verify that id resolves to exactly one board entry — not to derive it.
- **The reflection is one hop, never a chain.** Only the immediately preceding task is read; no grandparent, no transitive stack walk, no per-sprint aggregate.
- **No historical record is rewritten.** No merged spec, board entry, review or provenance file is retro-annotated with a reflection row; the family is forward-only, and every already-merged entry carrying none of them stays conformant by construction.
- **No merged criterion is repaired.** Criteria this task's own files redden are **disclosed** in `## Blast radius` and left alone; repairing another task's frozen criterion is a class-B re-freeze of someone else's intent.
- **`docs/loop-engineering/record-tamper-resistance.md`'s own re-evaluation trigger is not discharged here.** It fired at the third promotion (T-1099) and is owed at a retro; this task records a promotion and states that the trigger is not re-fired, exactly as the fourth and fifth promotions did.
- **Merging any pull request, and the sprint batch GO, are out of scope.**

## Acceptance criteria

- [ ] **AC1** The canonical grammar lands in `templates/prompt-blocks/dispatch-record.md`, additively, and both registered consumers carry it verbatim. In the base-ref committed blob the literals `dispatch-reflection`, `no-predecessor-row` and `immediately preceding task` each occur **zero** times; at `HEAD` each occurs at least once. The edit is additive rather than a rewrite: three strings the block already carries — `- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>`, `Who decides, who records.` and `**Validation.**` — are present on **both** sides, and every non-blank line the base blob carried is still present verbatim at `HEAD`. `bash bin/check-prompt-sync.sh` exits `0`, which is what proves `agents/tech-lead.md` and `skills/run/SKILL.md` both carry every added line verbatim under the registry's `contain` mode. Positive controls: the base blob is asserted non-empty before it is searched, and the three survival strings must be found on the base side too, so a criterion that read the wrong file cannot satisfy the deltas. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; P=templates/prompt-blocks/dispatch-record.md; test -r "$P" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109a.XXXXXX") || exit 1; git show "$B:$P" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; for s in 'dispatch-reflection' 'no-predecessor-row' 'immediately preceding task'; do test "$(grep -cF -- "$s" "$T/base" || true)" = "0" || rc=1; grep -qF -- "$s" "$P" || rc=1; done; for s in '- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>' 'Who decides, who records.' '**Validation.**'; do grep -qF -- "$s" "$T/base" || rc=1; grep -qF -- "$s" "$P" || rc=1; done; grep -v '^[[:space:]]*$' "$T/base" > "$T/bnl" || true; test -s "$T/bnl" || rc=1; test "$(grep -vxFf "$P" "$T/bnl" | grep -c . || true)" = "0" || rc=1; bash bin/check-prompt-sync.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC2** The skim step lands in `skills/run/SKILL.md`, inside the Specify seam's transcription window and before the freeze sweep. In the base-ref committed blob the literal `dispatch-reflection` occurs **zero** times; at `HEAD` it occurs at least once, and the **first** such occurrence sits at or after the line carrying `**Board transcription order` and strictly before the line carrying `**Frozen-intent confirmation` — which is the mechanical form of "the skim is recorded in the window that closes before the freeze sweep opens". The file still names `check-entry-mode.sh` as the gate run at that window, and carries the literal `immediately preceding task`. Positive controls: the base blob is asserted non-empty; and all three anchor line numbers are asserted non-empty before any comparison, so a failed extraction cannot satisfy an ordering claim by comparing nothing. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; P=skills/run/SKILL.md; test -r "$P" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109b.XXXXXX") || exit 1; git show "$B:$P" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; test "$(grep -cF -- 'dispatch-reflection' "$T/base" || true)" = "0" || rc=1; test "$(grep -cF -- 'dispatch-reflection' "$P" || true)" -ge 1 || rc=1; f=$(grep -nF -- 'dispatch-reflection' "$P" | head -1 | cut -d: -f1); h=$(grep -nF -- '**Board transcription order' "$P" | head -1 | cut -d: -f1); g=$(grep -nF -- '**Frozen-intent confirmation' "$P" | head -1 | cut -d: -f1); test -n "$f" || rc=1; test -n "$h" || rc=1; test -n "$g" || rc=1; if [ -n "$f" ] && [ -n "$h" ] && [ -n "$g" ]; then test "$((10#$f))" -ge "$((10#$h))" || rc=1; test "$((10#$f))" -lt "$((10#$g))" || rc=1; fi; grep -qF -- 'check-entry-mode.sh' "$P" || rc=1; grep -qF -- 'immediately preceding task' "$P" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC3** Duty A — the record's own grammar and its per-axis coverage — is enforced by `bin/check-entry-mode.sh`, exercised behaviourally against fixture boards. Against a conformant fixture (an `## Active` entry recording two `- dispatch:` axes and two matching `- dispatch-reflection:` rows naming a predecessor entry that exists in `## Done`) the checker exits `0`. Each of four mutations of that same fixture exits **exactly `1`** — a content refusal, never `2`: (a) one reflection row deleted, so the family no longer covers every axis the entry's own `- dispatch:` rows record; (b) one row's axis field replaced by a token that is not a dispatch axis key; (c) two rows carrying the same axis; (d) a row whose four ` — `-separated fields are not all non-empty. Positive control: the conformant fixture's own exit `0` is asserted first, so a checker that refused everything could not satisfy this criterion. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; C=bin/check-entry-mode.sh; test -r "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109c.XXXXXX") || exit 1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — serial — unconditional — recommendation: r' '  - dispatch-reflection: specify — T-900 — repeat — ground' '  - dispatch-reflection: verify — T-900 — differs — ground' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — tier1-fanout — unconditional — recommendation: r' > "$T/ok.md"; bash "$C" --board "$T/ok.md" --task T-901 >/dev/null 2>&1 || rc=1; grep -vF -- '- dispatch-reflection: verify' "$T/ok.md" > "$T/gap.md"; sed 's/dispatch-reflection: verify —/dispatch-reflection: bogus —/' "$T/ok.md" > "$T/axis.md"; sed 's/dispatch-reflection: verify — T-900 — differs/dispatch-reflection: specify — T-900 — repeat/' "$T/ok.md" > "$T/dup.md"; sed 's/dispatch-reflection: verify — T-900 — differs — ground/dispatch-reflection: verify — T-900 — differs — /' "$T/ok.md" > "$T/empty.md"; for m in gap axis dup empty; do s=0; bash "$C" --board "$T/$m.md" --task T-901 >/dev/null 2>&1 || s=$?; test "$s" = "1" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** Duty B — the stated verdict must agree with the predecessor entry's own recorded value — is enforced, and the predecessor is found in `## Done` as readily as in `## Active`. Using the same conformant fixture: flipping the `specify` row's verdict from `repeat` to `differs` while both entries still record `pm-authored` exits **exactly `1`**; flipping the `verify` row's verdict from `differs` to `repeat` while the two entries record `serial` and `tier1-fanout` exits **exactly `1`**; a fixture in which the predecessor entry sits in `## Active` as `- [ ] **T-900** …` instead of in `## Done`, with the same rows, exits `0`; and a fixture in which the predecessor records **no** `- dispatch: verify` row at all exits `0` when that reflection row's verdict reads `no-predecessor-row` and exits **exactly `1`** when it reads `repeat`. Positive control: the two exit-`0` cases are asserted, so a checker that refused every cross-entry comparison could not satisfy this criterion. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; C=bin/check-entry-mode.sh; test -r "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109d.XXXXXX") || exit 1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — serial — unconditional — recommendation: r' '  - dispatch-reflection: specify — T-900 — repeat — ground' '  - dispatch-reflection: verify — T-900 — differs — ground' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — tier1-fanout — unconditional — recommendation: r' > "$T/ok.md"; sed 's/dispatch-reflection: specify — T-900 — repeat/dispatch-reflection: specify — T-900 — differs/' "$T/ok.md" > "$T/lie1.md"; sed 's/dispatch-reflection: verify — T-900 — differs/dispatch-reflection: verify — T-900 — repeat/' "$T/ok.md" > "$T/lie2.md"; grep -vF -- '- dispatch: verify — tier1-fanout' "$T/ok.md" | sed 's/dispatch-reflection: verify — T-900 — differs/dispatch-reflection: verify — T-900 — no-predecessor-row/' > "$T/norow.md"; grep -vF -- '- dispatch: verify — tier1-fanout' "$T/ok.md" | sed 's/dispatch-reflection: verify — T-900 — differs/dispatch-reflection: verify — T-900 — repeat/' > "$T/norowbad2.md"; for m in lie1 lie2 norowbad2; do s=0; bash "$C" --board "$T/$m.md" --task T-901 >/dev/null 2>&1 || s=$?; test "$s" = "1" || rc=1; done; bash "$C" --board "$T/norow.md" --task T-901 >/dev/null 2>&1 || rc=1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch-reflection: specify — T-900 — repeat — ground' '' '- [ ] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '' '## Done' '' > "$T/inactive.md"; bash "$C" --board "$T/inactive.md" --task T-901 >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** The empty-predecessor case is a conformant recorded outcome, and it is distinguished fail-closed from a failed read. An entry whose **only** reflection line is `- dispatch-reflection: all — no-predecessor — no-predecessor-row — <ground>` exits `0`, whatever `- dispatch:` axes it records. Three near-misses each exit **exactly `1`**: (a) that same line sitting alongside a per-axis reflection row, so the entry claims both that there is no predecessor and that there is one; (b) a per-axis row naming a task id that matches **no** top-level board entry in either section — an unresolvable reference, which is a failed read and never the no-predecessor case; (c) a per-axis row naming a task id that matches **two** top-level board entries, one in each section — an ambiguous reference. Positive control: the conformant no-predecessor board's own exit `0` is asserted first. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; C=bin/check-entry-mode.sh; test -r "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109e.XXXXXX") || exit 1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — serial — unconditional — recommendation: r' '  - dispatch-reflection: all — no-predecessor — no-predecessor-row — first task of the train' '' '## Done' '' > "$T/none.md"; bash "$C" --board "$T/none.md" --task T-901 >/dev/null 2>&1 || rc=1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch-reflection: all — no-predecessor — no-predecessor-row — g' '  - dispatch-reflection: specify — T-900 — repeat — g' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/mix.md"; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch-reflection: specify — T-899 — repeat — g' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/absent.md"; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch-reflection: specify — T-900 — repeat — g' '' '- [ ] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/ambig.md"; for m in mix absent ambig; do s=0; bash "$C" --board "$T/$m.md" --task T-901 >/dev/null 2>&1 || s=$?; test "$s" = "1" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** The family is validate-if-present, so an entry carrying no reflection line at all still passes — adopter back-compat, the same shape and the same reason `bin/close-out.sh` already applies to the `- dispatch:` family itself. A fixture entry recording two `- dispatch:` axes, a conformant `- entry-mode:` and **zero** `- dispatch-reflection:` lines exits `0`. Its non-vacuity control is a second run of the **same** checker against the **same** board with exactly one malformed reflection row added, which must exit **exactly `1`** — proving the exit `0` above came from the family genuinely being empty rather than from a checker that never looks. **Expected red at freeze** — the malformed-row half cannot refuse until the family is implemented.
  - check: rc=0; export LC_ALL=C; C=bin/check-entry-mode.sh; test -r "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109f.XXXXXX") || exit 1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — serial — unconditional — recommendation: r' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/bare.md"; bash "$C" --board "$T/bare.md" --task T-901 >/dev/null 2>&1 || rc=1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — serial — unconditional — recommendation: r' '  - dispatch-reflection: verify — T-900' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/one.md"; test "$(grep -cF -- '- dispatch-reflection: verify — T-900' "$T/one.md" || true)" = "1" || rc=1; grep -vF -- '- dispatch-reflection:' "$T/one.md" | cmp -s - "$T/bare.md" || rc=1; s=0; bash "$C" --board "$T/one.md" --task T-901 >/dev/null 2>&1 || s=$?; test "$s" = "1" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** The new key is invisible to every shipped anchored `- dispatch:` scan, and `bin/close-out.sh` is unchanged. Two halves, because either alone is insufficient. **Byte-pin half**: the three anchor expressions this claim depends on still occur verbatim in the scripts that own them — `'^[[:space:]]*- dispatch:'` in `bin/close-out.sh`, and `'^[[:space:]]*- dispatch: specify — '` together with the loose diagnostic `'^[[:space:]]*-[[:space:]]+dispatch:[[:space:]]+specify[[:space:]]+—'` in `bin/check-entry-mode.sh` — so a rewritten anchor turns this criterion red rather than silently invalidating it. **No-match half**: a canonical reflection line matches **none** of those three expressions. `bin/close-out.sh` is additionally byte-identical to its base-ref committed blob, and `bash tests/close-out/run.sh` exits `0`. Positive control: each expression is asserted to match a genuine `- dispatch:` line, so a pattern that matched nothing at all could not satisfy the no-match half. **Green at freeze and required to stay green** for the byte-pin, base-identity and suite clauses; the no-match half is green at freeze by construction and is a regression lock.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109g.XXXXXX") || exit 1; grep -qF -- "'^[[:space:]]*- dispatch:'" bin/close-out.sh || rc=1; grep -qF -- "'^[[:space:]]*- dispatch: specify — '" bin/check-entry-mode.sh || rc=1; grep -qF -- "'^[[:space:]]*-[[:space:]]+dispatch:[[:space:]]+specify[[:space:]]+—'" bin/check-entry-mode.sh || rc=1; printf '%s\n' '  - dispatch-reflection: verify — T-900 — repeat — ground' '  - dispatch-reflection: specify — T-900 — differs — ground' > "$T/refl"; printf '%s\n' '  - dispatch: verify — serial — unconditional — recommendation: r' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/real"; for e in '^[[:space:]]*- dispatch:' '^[[:space:]]*- dispatch: specify — ' '^[[:space:]]*-[[:space:]]+dispatch:[[:space:]]+specify[[:space:]]+—'; do test "$(grep -cE -- "$e" "$T/refl" || true)" = "0" || rc=1; test "$(grep -cE -- "$e" "$T/real" || true)" -ge 1 || rc=1; done; git show "$B:bin/close-out.sh" > "$T/cob" 2>/dev/null || rc=1; test -s "$T/cob" || rc=1; cmp -s "$T/cob" bin/close-out.sh || rc=1; bash tests/close-out/run.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** Reachable spacing and line-ending variants are seen, not swallowed. A reflection row whose bullet dash is followed by **two** spaces instead of one, standing as the **only** reflection line on an entry that records one `- dispatch:` axis, exits **exactly `1`** — which is the discriminating observation: an implementation whose stem net missed that variant would see an empty family, take the validate-if-present path and exit `0`, so exit `1` is the mechanical proof that the variant was collected and then refused as malformed rather than rendered invisible. This is the T-1096 rework Blocker-2 class, reopened at a third marker family and closed here. Separately, a CRLF-terminated copy of the conformant fixture used by **AC3** exits `0`, matching the CR tolerance `bin/check-entry-mode.sh:112-117` already applies. Positive control: the LF original of that same CRLF board is asserted to exit `0` too, so the CRLF clause cannot pass by both copies being refused. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; C=bin/check-entry-mode.sh; test -r "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109h.XXXXXX") || exit 1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  -  dispatch-reflection: specify — T-900 — repeat — ground' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' > "$T/wide.md"; s=0; bash "$C" --board "$T/wide.md" --task T-901 >/dev/null 2>&1 || s=$?; test "$s" = "1" || rc=1; printf '%s\n' '# Tasks' '' '## Active' '' '- [ ] **T-901** fixture' '  - entry-mode: pm-authored' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — serial — unconditional — recommendation: r' '  - dispatch-reflection: specify — T-900 — repeat — ground' '  - dispatch-reflection: verify — T-900 — differs — ground' '' '## Done' '' '- [x] **T-900** predecessor' '  - dispatch: specify — pm-authored — unconditional — recommendation: r' '  - dispatch: verify — tier1-fanout — unconditional — recommendation: r' > "$T/lf.md"; bash "$C" --board "$T/lf.md" --task T-901 >/dev/null 2>&1 || rc=1; awk '{printf "%s\r\n", $0}' "$T/lf.md" > "$T/crlf.md"; test -s "$T/crlf.md" || rc=1; bash "$C" --board "$T/crlf.md" --task T-901 >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC9** The fixture suite grows with the checker, and stays green. In the base-ref committed blob of `tests/check-entry-mode/run.sh` the literal `dispatch-reflection` occurs **zero** times; at `HEAD` it occurs strictly more times than at base, and the file additionally carries the literals `no-predecessor-row`, `repeat` and `differs`. Every non-blank line the base blob carried is still present verbatim at `HEAD`, so cases are added rather than traded away. `bash tests/check-entry-mode/run.sh` exits `0`, and `bash bin/check-entry-mode.sh --help` exits `0` so the script's own documented contract is still readable. Positive controls: the base blob is asserted non-empty; and the head-versus-base count comparison is asserted only after both counts have been read as digits. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; P=tests/check-entry-mode/run.sh; test -r "$P" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109i.XXXXXX") || exit 1; git show "$B:$P" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; nb=$(grep -cF -- 'dispatch-reflection' "$T/base" || true); nh=$(grep -cF -- 'dispatch-reflection' "$P" || true); test "$((10#$nb))" = "0" || rc=1; test "$((10#$nh))" -gt "$((10#$nb))" || rc=1; for s in 'no-predecessor-row' 'repeat' 'differs'; do grep -qF -- "$s" "$P" || rc=1; done; grep -v '^[[:space:]]*$' "$T/base" > "$T/bnl" || true; test -s "$T/bnl" || rc=1; test "$(grep -vxFf "$P" "$T/bnl" | grep -c . || true)" = "0" || rc=1; bash tests/check-entry-mode/run.sh >/dev/null 2>&1 || rc=1; bash bin/check-entry-mode.sh --help >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC10** The tamper note records this promotion, per duty, and does not re-fire its own trigger. `docs/loop-engineering/record-tamper-resistance.md` at `HEAD` carries the literal `dispatch-reflection` at least once (base: **zero**), names `bin/check-entry-mode.sh` as the arm-A host, carries both arm names verbatim (`arm-A-tested-primitive` and `arm-B-enumerated-instrument`) so the split across duties is stated rather than implied, and carries the literal `re-evaluation trigger` in the added region. Every non-blank line the base blob carried is still present verbatim at `HEAD` — the note is appended to, never rewritten, which is what keeps the five recorded promotions intact. Positive controls: the base blob is asserted non-empty; and the base blob is asserted to already carry `arm-A-tested-primitive`, so the arm-name clause cannot pass on a file the check failed to read. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; P=docs/loop-engineering/record-tamper-resistance.md; test -r "$P" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109j.XXXXXX") || exit 1; git show "$B:$P" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; test "$(grep -cF -- 'dispatch-reflection' "$T/base" || true)" = "0" || rc=1; grep -qF -- 'arm-A-tested-primitive' "$T/base" || rc=1; for s in 'dispatch-reflection' 'bin/check-entry-mode.sh' 'arm-A-tested-primitive' 'arm-B-enumerated-instrument' 're-evaluation trigger'; do grep -qF -- "$s" "$P" || rc=1; done; grep -v '^[[:space:]]*$' "$T/base" > "$T/bnl" || true; test -s "$T/bnl" || rc=1; test "$(grep -vxFf "$P" "$T/bnl" | grep -c . || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** The adopter-facing documentation lands in the same task. `README.md`, `README.ja.md` and `docs/adopting.md` each carry the literal `dispatch-reflection` at least once at `HEAD` and **zero** times in their base-ref committed blobs; every non-blank line each base blob carried is still present verbatim at `HEAD`, so each is an addition rather than a rewrite; and `bash bin/check-readme-version.sh README.md README.ja.md` exits `0`, so the two README files stay in the version agreement that checker enforces. Positive controls: all three base blobs are asserted non-empty before they are searched. **Expected red at freeze.**
  - adopter-surface: `README.md`'s and `README.ja.md`'s **Situational dispatch record** bullets (each gains the reflection record's grammar and the fact that a missing family still passes), and `docs/adopting.md`'s `bin/check-entry-mode.sh` backstop paragraph (which today enumerates exactly what that checker refuses on and is incomplete without the new refusal classes). Measured: `docs/adopting.ja.md` carries **no** counterpart paragraph — `check-entry-mode` occurs zero times in it — so there is no Japanese counterpart to mirror, and its absence is a measurement rather than an omission.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109k.XXXXXX") || exit 1; for P in README.md README.ja.md docs/adopting.md; do test -r "$P" || { rc=1; continue; }; git show "$B:$P" > "$T/base" 2>/dev/null || { rc=1; continue; }; test -s "$T/base" || rc=1; test "$(grep -cF -- 'dispatch-reflection' "$T/base" || true)" = "0" || rc=1; grep -qF -- 'dispatch-reflection' "$P" || rc=1; grep -v '^[[:space:]]*$' "$T/base" > "$T/bnl" || true; test -s "$T/bnl" || rc=1; test "$(grep -vxFf "$P" "$T/bnl" | grep -c . || true)" = "0" || rc=1; done; bash bin/check-readme-version.sh README.md README.ja.md >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** Scope lock, and the two structural negatives it carries. The branch's changed-and-added file set — the tracked half from the commit range `<base>...HEAD` unioned with the untracked half from `git ls-files --others --exclude-standard`, since this spec and this task's records are untracked at freeze time — **contains** each of: this spec, `templates/prompt-blocks/dispatch-record.md`, `agents/tech-lead.md`, `skills/run/SKILL.md`, `bin/check-entry-mode.sh`, `tests/check-entry-mode/run.sh`, `docs/loop-engineering/record-tamper-resistance.md`, `README.md`, `README.ja.md`, `docs/adopting.md`, `.shell-team/test-recipe.md` and the resolved board; and **nothing** outside the allow-list, which is those twelve paths plus this task's three records (`.shell-team/provenance/T-1109.md`, `.shell-team/interventions/T-1109.md`, `.shell-team/reviews/T-1109.md`) and `docs/adopting.ja.md`, the last four optional. Two structural negatives ride on the same measurement: `bin/check-entry-mode.sh` is the **only** path under `bin/` in the set, which is the mechanical form of "no new `bin/` script"; and `.github/workflows/check-handoff.yml` is **byte-identical** to its base-ref committed blob, which is the mechanical form of "no new CI step". `agents/pm-spec.md` is likewise byte-identical to its base blob, and no spec under `.shell-team/specs/` other than this one is changed or added. **This criterion is merge-point-scoped and expected to go stale after merge** — once a later task's files land on the same base ref its expected set no longer describes reality; do not widen its base-ref resolution and do not re-derive it per rework round. Positive controls: the base ref resolves and both base blobs are asserted non-empty; and the changed-and-added set is asserted non-empty. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1108-sweep-corpus-isolation; then git merge-base "feature/1108-sweep-corpus-isolation" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109l.XXXXXX") || exit 1; SELF=.shell-team/specs/T-1109-dispatch-reflection.md; BD=$(bash bin/team-paths.sh --get todo) || exit 1; printf '%s\n' "$SELF" templates/prompt-blocks/dispatch-record.md agents/tech-lead.md skills/run/SKILL.md bin/check-entry-mode.sh tests/check-entry-mode/run.sh docs/loop-engineering/record-tamper-resistance.md README.md README.ja.md docs/adopting.md .shell-team/test-recipe.md "$BD" > "$T/own"; printf '%s\n' .shell-team/provenance/T-1109.md .shell-team/interventions/T-1109.md .shell-team/reviews/T-1109.md docs/adopting.ja.md > "$T/opt"; { git diff --name-only "$B"...HEAD; git ls-files --others --exclude-standard; } | LC_ALL=C sort -u > "$T/set"; test -s "$T/set" || rc=1; while IFS= read -r f; do test -n "$f" || continue; grep -qxF -- "$f" "$T/set" || rc=1; done < "$T/own"; cat "$T/own" "$T/opt" | LC_ALL=C sort -u > "$T/allow"; test "$(grep -vxFf "$T/allow" "$T/set" | grep -c . || true)" = "0" || rc=1; nb=$(grep -c '^bin/' "$T/set" || true); test "$((10#$nb))" = "1" || rc=1; grep -qxF -- 'bin/check-entry-mode.sh' "$T/set" || rc=1; for p in .github/workflows/check-handoff.yml agents/pm-spec.md; do git show "$B:$p" > "$T/pb" 2>/dev/null || rc=1; test -s "$T/pb" || rc=1; cmp -s "$T/pb" "$p" || rc=1; done; sp=$(git diff --name-only "$B"...HEAD -- .shell-team/specs | grep -vxF -- "$SELF" | grep -c . || true); test "$((10#$sp))" = "0" || rc=1; su=$(git ls-files --others --exclude-standard -- .shell-team/specs | grep -vxF -- "$SELF" | grep -c . || true); test "$((10#$su))" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC13** This task's own board entry dogfoods the positive case, and the dogfood is non-vacuous. The resolved board's `T-1109` entry carries at least one `- dispatch-reflection:` sub-bullet; the set of axis keys those rows name is **equal**, in both directions, to the set of axis keys the entry's own `- dispatch:` rows record; every one of them names `T-1108` as the predecessor; and `bash bin/check-entry-mode.sh --board "$(bash bin/team-paths.sh --get todo)" --task T-1109` exits `0`. Because that exit `0` is also what an unimplemented checker returns, the criterion carries its own non-vacuity control: a scratch copy of the board (never the real board) with one reflection row's verdict flipped must make the same command exit **exactly `1`**, which only an implemented duty B can produce. Positive controls: the board path resolves and is readable; the entry's own dispatch-axis set is asserted non-empty before either comparison. **Expected red at freeze** — the non-vacuity control cannot pass until the checker is implemented.
  - check: rc=0; export LC_ALL=C; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109m.XXXXXX") || exit 1; awk '/^- \[ \] \*\*T-1109\*\* /{f=1;print;next} f&&/^[[:space:]]*$/{next} f&&/^[[:space:]]+[^[:space:]]/{print;next} f{exit}' "$BD" > "$T/e"; test -s "$T/e" || rc=1; grep -E -- '^[[:space:]]*- dispatch: ' "$T/e" | sed -nE 's/^[[:space:]]*- dispatch: ([a-z0-9-]+) — .*$/\1/p' | LC_ALL=C sort -u > "$T/da"; grep -E -- '^[[:space:]]*- dispatch-reflection: ' "$T/e" | sed -nE 's/^[[:space:]]*- dispatch-reflection: ([a-z0-9-]+) — .*$/\1/p' | LC_ALL=C sort -u > "$T/ra"; test -s "$T/da" || rc=1; test -s "$T/ra" || rc=1; cmp -s "$T/da" "$T/ra" || rc=1; np=$(grep -cE -- '^[[:space:]]*- dispatch-reflection: [a-z0-9-]+ — T-1108 — ' "$T/e" || true); nr=$(grep -cE -- '^[[:space:]]*- dispatch-reflection: ' "$T/e" || true); test "$((10#$nr))" -ge 1 || rc=1; test "$((10#$np))" = "$((10#$nr))" || rc=1; bash bin/check-entry-mode.sh --board "$BD" --task T-1109 >/dev/null 2>&1 || rc=1; sed -E 's/^([[:space:]]*- dispatch-reflection: [a-z0-9-]+ — T-1108 — )repeat( — )/\1@@T1109@@\2/; s/^([[:space:]]*- dispatch-reflection: [a-z0-9-]+ — T-1108 — )differs( — )/\1repeat\2/; s/@@T1109@@/differs/' "$BD" > "$T/mut.md"; cmp -s "$BD" "$T/mut.md" && rc=1; s=0; bash bin/check-entry-mode.sh --board "$T/mut.md" --task T-1109 >/dev/null 2>&1 || s=$?; test "$s" = "1" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC14** The downstream-impact disclosure is produced by the full-population-diff method the declared `mechanism` class owes, its population is re-derived rather than transcribed, and every base-green-to-head-red flip is enumerated. This spec carries a `## Blast radius` section outside the intent block; that heading occurs exactly once; the section is non-empty; it states the literal `full-population diff` as its method, names its base-side reading target as a committed-blob read (`git show` or `git worktree add --detach`) rather than the working tree, carries both a `base:` and a `head:` verdict token, and carries a `- procedure-version:` line whose text names `agents/pm-spec.md` and either `staged` or `snapshot` — the version fact, not merely the field. It declares its population on a line of the shape `- population: <n> spec files`, and `<n>` is **equal** to the count re-derived live at check time from `.shell-team/specs/*.md` — a value-independent invariant that re-derives both sides, never an absolute literal typed into the check. Every merged criterion that flips from a base `PASS` to a head `FAIL` is enumerated in that section on one line carrying its spec path, its `**ACn**` label and the literal `head: FAIL`; an unenumerated flip fails this criterion, and an empty or missing section fails closed before any exception is honoured. Positive controls: the live enumeration must yield at least one path; and the section must name at least one merged spec by full `.shell-team/specs/` path, so a row cannot resolve to a bare label. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1109-dispatch-reflection.md; test -f "$SP" || exit 1; test -d .shell-team/specs || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1109n.XXXXXX") || exit 1; test "$(grep -cxF -- '## Blast radius' "$SP" || true)" = "1" || rc=1; awk '/^## Blast radius$/{f=1;next} f&&/^## /{exit} f' "$SP" > "$T/sec"; test -s "$T/sec" || rc=1; for s in 'full-population diff' 'base:' 'head:'; do grep -qF -- "$s" "$T/sec" || rc=1; done; grep -qE 'git (show|worktree add --detach)' "$T/sec" || rc=1; grep -qE '^- procedure-version: .*agents/pm-spec\.md' "$T/sec" || rc=1; grep -E '^- procedure-version: ' "$T/sec" | grep -qE '(staged|snapshot)' || rc=1; grep -qE '\.shell-team/specs/T-1[0-9]{3}-[a-z0-9-]+\.md' "$T/sec" || rc=1; : > "$T/live"; for f in .shell-team/specs/*.md; do test -r "$f" || { rc=1; continue; }; printf '%s\n' "$f" >> "$T/live"; done; n=$(grep -c . "$T/live" || true); test "$((10#$n))" -ge 1 || rc=1; p=$(grep -m1 -E '^- population: [0-9]+ spec files' "$T/sec" | sed -E 's/^- population: ([0-9]+) spec files.*/\1/'); test -n "$p" || rc=1; if [ -n "$p" ]; then test "$((10#$p))" = "$((10#$n))" || rc=1; fi; q=$(grep -m1 -E '^- predicted-red: [0-9]+' "$T/sec" | sed -E 's/^- predicted-red: ([0-9]+).*/\1/'); test -n "$q" || rc=1; r=$(grep -cF -- 'head: FAIL' "$T/sec" || true); if [ -n "$q" ]; then test "$((10#$r))" -ge "$((10#$q))" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task adds or removes a `*.md` file under `.shell-team/specs/`, at which point the declared population no longer describes the corpus this criterion re-derives and the table must be re-measured rather than the criterion widened.

- [ ] **AC15** Runtime, `SKIP` by design — no command can prove a command was run. **This criterion is reported item by item, in this order, one line per item, each naming what was measured and what the result was; a compressed summary of it does not discharge it, and a whole-set claim inside any item carries the enumeration it summarises.** **(a)** A producer-run mutation self-check on scratch copies outside the checkout (never the working tree), each mutation observed red, restored, observed green again, each reported separately: remove the coverage rule from `bin/check-entry-mode.sh` (**AC3**(a) red); remove the axis-vocabulary test (**AC3**(b) red); remove the duplicate-axis test (**AC3**(c) red); remove the cross-entry value comparison (**AC4**'s two lie cases red, each observed); restrict the predecessor lookup to `## Active` only (**AC4**'s `## Done` case red); accept an unresolvable predecessor id silently (**AC5**(b) red); narrow the stem net to exactly one space after the bullet dash (**AC8**'s doubled-space case red — the T-1096 Blocker-2 probe); drop the CR strip (**AC8**'s CRLF case red); and make the family mandatory rather than validate-if-present (**AC6**'s bare-board case red). **(b)** The full CI-wired step list of `.github/workflows/check-handoff.yml` run locally in workflow order, every step's result stated, any step judged inapplicable named individually with its reason, the reached-steps scope derived mechanically by reverse-mapping each edited path to the suites that read it (`grep -rl -- '<edited-path>' tests/*/run.sh` plus the workflow's own dogfood steps naming it) rather than from memory, and the fixed-scratch-path suites run strictly sequentially. **(c)** The `## Blast radius` table's production narrated: the literal enumeration command, the population it returned, the scratch tree the base-side verdicts were read from **at that ref's committed blobs**, the staging step that populated that tree's runs corpus, and per differing spec one line stating which criterion changed verdict and why — with the whole-population claim carrying the enumeration behind it, not a blanket sentence, and any spec not run at both refs named individually as unmeasured rather than folded into a clean claim. Where a measured verdict differs from this spec's prediction, the table (which sits outside the intent block) is corrected to the measurement and the correction is stated; the prediction is never defended. **(d)** The measured 40-hex value of the `- base-ref-discriminator:` expression, which arm its existence test selected, and the measured value of `git branch --show-current`. **(e)** Every relayed premise in `## Assumptions` re-measured with the measured value reported beside it, and any premise that could not be measured from the executing side named as still relayed rather than silently dropped — issue **#365**'s open state and Proposal-3 scope, and the predecessor PR's state, each stated as confirmed or as still relayed. **(f)** A per-source report for every `## Summarized sources` entry, stating whether the reporting role opened it and what it confirmed or contradicted about the cited distinction. **(g)** The **execution-context matrix** for `bin/check-entry-mode.sh` as this task changes it, reported cell by cell: cell 1 — this repository's own checkout root **without** the plugin on `PATH`, invoked as `bash bin/check-entry-mode.sh`; cell 2 — an adopter-shaped repository scaffolded by `bin/team-init.sh` into a scratch directory **with** the plugin loaded, so the script is reached by bare name from a different working directory and the board path is resolved through `bin/team-paths.sh` rather than named literally. The conformant fixture and at least one refusal fixture are run in **both** cells and their exit statuses and first stderr lines reported; a cell that cannot run is named individually with the reason.

## Input space

**Reachable input classes** — what real usage of this repository and of an adopter's can produce, all of which the reflection record and its checker must handle correctly:

- A task whose predecessor's board entry sits in **`## Done`**, because `bin/close-out.sh` moved it there on the predecessor's own branch before this branch was cut. This is **this task's own live case**: `T-1108`'s entry, with its five `- dispatch:` rows, is at `.shell-team/todo.md:3642-3646`, inside `## Done`.
- A task whose predecessor's board entry is still in **`## Active`**, because the predecessor is open, `BLOCKED` or mid-loop — the shape `T-1089` and `T-1086` currently have.
- A task with **no predecessor at all**: the head of a train cut directly from the integration branch (the shape `.shell-team/todo.md:5229` records for `T-1060`), or a branch cut from a docs-only or chore branch that carries no board entry (the shape `chore/lesson-promotion-2026-08-13` has at `:4983`).
- A predecessor that records a **different axis key** than this task for the same phase — the parent `verify` against a `verify-fixture`/`verify-mechanism` refinement, or an axis this task elects and the predecessor did not — for which the conformant verdict is `no-predecessor-row`.
- An entry carrying **no** reflection line at all: every already-merged entry in this repository, and every task in an adopter's repository that has not adopted the record.
- Spacing variants after the bullet dash — one space (canonical), two or more, a tab — the class `bin/check-entry-mode.sh`'s own `## Input space` already names as reachable and whose invisibility was a rework Blocker at two earlier marker families.
- A **CRLF**-terminated board file, which `bin/check-entry-mode.sh:112-117` already tolerates for its existing sources.
- A `<ground>` field containing further ` — ` sequences, since it is free text — the same class `bin/close-out.sh`'s dispatch extraction already handles by anchoring on the first three separators only.
- A named predecessor id that resolves to **zero** top-level board entries, or to **two** (one in each section) — both failed reads rather than empty cases.
- An adopter repository on the default `.shell-team/` layout or on the legacy `tasks/` layout, which is why every criterion and every documented command resolve the board through `bin/team-paths.sh --get todo` rather than naming a path literally.

**Out-of-scope synthetic extremes** — named concretely, and declined:

- A board large enough that a second full-file scan is itself expensive (tens of thousands of entries, multi-megabyte boards). This repository's board is one file of a few thousand lines; no performance ceiling is specified or enforced.
- Task ids outside the `T-<digits>` shape the existing `--task` validator already refuses, and predecessor references naming a task in a different repository.
- Em-dash homoglyphs other than U+2014 in the field separator, and non-UTF-8 or mixed-encoding board bytes.
- A **transitive** stack walk — grandparent, great-grandparent, or a whole-sprint aggregate of elections. The record is one hop by construction and a deeper read is a different mechanism.
- A board mutated by a concurrent writer **while** the checker reads it, and multi-machine or network-filesystem boards. Single-machine, single-read semantics only.
- Deliberately hostile board content — a reflection row engineered to make the entry-extent scan run past its own entry, a task id containing regex metacharacters that survive the existing validator, an entry whose sub-bullets are crafted to collide with another entry's extent. The operator writing the board is trusted, consistent with this repository's standing threat model for its own checkers.
- Any attempt to make the checker judge whether a recorded repetition is *correct*. That judgment fails `tamper-arm-rule-v1`'s second conjunct outright and is disclosed as arm B rather than approximated.

<!-- END intent-block: T-1109 -->

## Assumptions

- **A-1 — issue #365's Proposal-3 scope is RELAYED, and its open state is measured by the coordinating session.** The scope reached this role through the launch prompt; this role holds no `gh` and no network. The coordinating session reports it measured **#365 open** on 2026-08-30 (`state: open`, `closed_by_pull_requests` total 0 — T-1100's `closes #365` never fired because that PR merged to `develop` rather than to the default branch). The freeze run confirms the value and records it beside this row. This discharges the Routing Map's reminder **(e)**, which was written before that measurement existed. **Measured at freeze (2026-08-30, coordinating session)**: issue #365 `state: open`, `closed_by_pull_requests` total_count 0, Proposal 3 present in the issue body as relayed — confirmed.
- **A-2 — the predecessor's PR state is RELAYED.** That `feature/1108-sweep-corpus-isolation`'s PR (**#392**, per the board's own close-out sub-bullet at `.shell-team/todo.md:3637`) is still open at authoring time reached this role through the coordinating session. The freeze run measures the PR state and records it beside this row. The branch facts the `- base-ref-discriminator:` rests on are **not** relayed and are measured by the freeze run with the discriminator's own existence test, whose selected arm is reported under **AC15**(d). **Measured at freeze (2026-08-30, coordinating session)**: PR #392 open, its `check-handoff lint` check-run `completed:success` at tip `d819531`; branch `feature/1108-sweep-corpus-isolation` resolves in `refs/heads/` at this checkout — confirmed.
- **A-3 — the map's line-number citations are stale and were re-anchored rather than inherited.** Measured first-hand at authoring: `bin/close-out.sh`'s `DISPATCH_AXIS_TABLE` is at `:270-:275`, not `:258-:263`; its exclusivity refusal is at `:345-:362`, not `:333-:349`; its duplicate-axis refusal is at `:314-:319`, not `:302-:307`. The map's *content* claims were confirmed correct at every one of those anchors — only the line numbers had moved, by roughly twelve lines, from intervening merges. Every citation in `## Problem` and `## Summarized sources` is therefore stated as a named anchor (a variable name, a heading, a bullet's own text) rather than as a line number wherever a durable anchor exists.
- **A-4 — the map's DP-2 measurement is a false negative, corrected here.** The map reports `grep -c '^  - \(stacked-on\|predecessor\|stack\):' .shell-team/todo.md` → **0 matches** and concludes that no machine-readable predecessor key exists. Measured first-hand with a corrected pattern, `^[[:space:]]*- (stacked-on|predecessor|stack|stacked):` returns **16** matches — the map's alternation tested `stack:` where the board actually uses `stacked:` (`.shell-team/todo.md:4577` onward). The *conclusion* nonetheless survives, for a different reason than the map gave: `- stacked:` names a **branch**, never a task id (`stacked: branches from feature/1076-log-run-locking's tip`), the current era uses `- branch: … stacked behind …` prose instead, and one recorded predecessor is a chore branch with no task id at all (`chore/lesson-promotion-2026-08-13`, `:4983`) — so neither key is usable as a machine-readable predecessor reference, and this spec's Non-goals decline to coin one. Recorded as a finding about the hand-off, not corrected silently.
- **A-5 — borrowed-vocabulary count premises, classified and enumerated for branch-point measurement.** Each is measured by the execution-capable side against the branch point's committed blob, with the measured value recorded beside this row before the freeze; a value contradicting what a criterion asserts routes back to `pm-spec` for a meaning-preserving correction. **Measured at freeze (2026-08-30) against the branch point `d819531`**: `dispatch-reflection` 0 in all seven scoped files; `no-predecessor-row` 0 in both scoped files; `immediately preceding task` 0 in both scoped files; `no-predecessor` 2 (T-1060 spec) + 3 (board) = 5 — every value matches the authoring-time claim.
  - `dispatch-reflection` — **own-coinage** (this task introduces it). Measured by this role over the whole working tree at authoring: **0** occurrences. **AC1**, **AC2**, **AC9**, **AC10** and **AC11** each assert `0` at base, scoped to one named file. Measure per file: `git show <base>:<path> | grep -cF -- 'dispatch-reflection'` → expected `0`.
  - `no-predecessor` — **borrowed** (already coined by `.shell-team/specs/T-1060-adopter-binding-docs.md:75,:85` as the name of a *premise*, and used in `.shell-team/todo.md:5168,:5254,:5263`). Measured by this role: **5** occurrences across those two files at authoring. **No criterion asserts a zero count for it anywhere**, deliberately — this row exists so that a later rework does not add one. The token's use here as a closed board value does not clash: the two senses live in different files and neither criterion reads the other's.
  - `no-predecessor-row` — **own-coinage** (this task introduces it; it is `no-predecessor`'s superstring, so a count of the shorter token includes it). Measured by this role over the whole working tree: **0** occurrences. **AC1** and **AC10** assert `0` at base, scoped to one named file each. Measure: `git show <base>:<path> | grep -cF -- 'no-predecessor-row'` → expected `0`.
  - `immediately preceding task` — **own-coinage** as a required literal in these two files. **AC1** and **AC2** assert `0` at base, scoped to `templates/prompt-blocks/dispatch-record.md` and `skills/run/SKILL.md` respectively. Measure: `git show <base>:<path> | grep -cF -- 'immediately preceding task'` → expected `0`.
  - `repeat` and `differs` — **borrowed** in the ordinary-English sense and certain to occur in prose throughout the corpus. **No criterion asserts any count for either token anywhere.** They appear only as required *presence* literals inside one named file (**AC9**, `tests/check-entry-mode/run.sh`), never as a count premise. This row exists so that a later rework does not turn either into one.
- **A-6 — the `## Blast radius` derivation block is produced by the tool, never hand-authored.** This role holds no shell. The section carries a `- reproduce:` line and a placeholder derivation block; the freeze run executes that line and embeds the emitted block **verbatim** between the markers, and writes the measured `- population:` value. The command to run: `bash bin/derive-populations.sh --label t1109specs --set tracked-specs="git ls-files -- '.shell-team/specs/*.md'" --set untracked-specs="git ls-files --others --exclude-standard -- '.shell-team/specs/*.md'"`.
- **A-7 — the two-arm sweep this task owes inherits T-1108's amended procedure, and that inheritance is a premise the freeze run confirms.** `agents/pm-spec.md:86` at this branch point carries the corpus-staging clause T-1108 delivered; **AC12** byte-pins that file unchanged, so the procedure cannot drift underneath the sweep during this task. The disclosed residual travels with it and is not re-litigated here: only the base arm is staged, the live/HEAD arm reads the live corpus unstaged, so a content- or count-sensitive criterion is not covered by that step. `## Blast radius`'s `- procedure-version:` row states which version produced the inventory and whether that residual was reached; **AC14** requires that row to carry the version fact rather than merely to exist.
- **A-8 — `bin/check-entry-mode.sh`'s current entry scan is `## Active`-only, and this is a measured fact rather than an inference.** Its `awk` guard is `sec ~ /^## Active/`, so the predecessor lookup **AC4** requires is genuinely new behaviour and not a re-parameterization of an existing scan. The engineer therefore adds a second scan rather than widening the first: the `--task` entry's own resolution must stay `## Active`-only and must keep refusing a task that is not exactly one such entry (exit `2`), because that is the forward-only scoping the script's own header `:47-:53` rests on and no criterion here relaxes it.
- **A-9 — `CHECK_ACS_TIMEOUT` must be raised above its 120 s default for this spec.** **AC7** runs `tests/close-out/run.sh`, **AC9** runs `tests/check-entry-mode/run.sh`, and **AC14** is a full-population two-arm sweep that creates a `git worktree add --detach`. Use `CHECK_ACS_TIMEOUT=900` for a full-spec run, consistent with the value `.shell-team/specs/T-1108-sweep-corpus-isolation.md`'s row **A-9** records for the same class of sweep.
- **A-10 — the `verify-mechanism` dispatch election is contested between two primary sources, and this row records the conflict rather than resolving it.** The Routing Map elects `verify-mechanism — tier1-fanout — conditional`, citing `docs/loop-engineering/default-path-firing.md:688` and `:698` as evidence that both of `recommendation: tier1-verification-fanout`'s preconditions hold. The immediately preceding task's own board entry (`.shell-team/todo.md:3644`) elects `verify-mechanism — serial — unconditional`, on the ground that "the N-instance orchestration step has not fired end to end". Measured first-hand: `phase-multiplexing.md:370`'s trigger requires both preconditions to **exist**, `:355` defines precondition 2 as an orchestration step "that can actually launch N *agent* instances", and `:375` still stands as an open follow-up to "verify empirically whether this harness genuinely runs N concurrent Agent-tool sub-agent invocations" — which reads against the map's claim rather than for it. The election is the coordinating session's to transcribe and no acceptance criterion below turns on it; this row exists so the transcription is made against both sources rather than one. **This is exactly the class of repeated-versus-diverging election this task exists to make visible**, and its appearance at authoring time is evidence for the task's premise rather than an obstacle to it.

## Open questions

None blocking. Two decisions are recorded here rather than left implicit.

**Why the checker is validate-if-present at the family level rather than mandatory.** A mandatory family would refuse every adopter's freeze until they adopted the sub-bullet, and would refuse this repository's own already-merged tasks on any re-freeze. The repository's own settled answer to that tension is `bin/close-out.sh`'s dispatch gate (`:237-248`): "presence is the shipped norm text's requirement … never this script's." The reflection follows it exactly — the norm text in `templates/prompt-blocks/dispatch-record.md` requires the record whenever the entry carries any `- dispatch:` row, and the checker validates it in full the moment one appears. The cost is disclosed rather than hidden: an entry that records nothing passes, so the mechanism catches a **wrong** reflection, never a **missing** one. That residual is arm B's and is stated as such in `docs/loop-engineering/record-tamper-resistance.md` (**AC10**).

**Why the arm-A judgment extends an existing script rather than adding a new one.** `bin/check-entry-mode.sh` already owns this exact seam — it is invoked at `skills/run/SKILL.md:45` in the same window the reflection is written, it already locates the task's entry and already parses its `- dispatch:` rows, and it already has a suite and a CI step. `docs/loop-engineering/record-tamper-resistance.md:94-99` explicitly blesses this: the third promotion satisfied arm A's structural form inside the existing `bin/check-board-headings.sh`. The naming residual is real and is disclosed rather than argued away — the script's name says `entry-mode` while it now carries three obligations at one seam — and the remedy chosen is documentary (its header and `--help` text state the widened scope, **AC9**) rather than a rename, which would break every caller and every citation in the corpus for no change in what is measured.

## Body-to-AC correspondence

Every normative directive stated in this spec's body is mapped to an acceptance criterion or explicitly exempted with a reason. A bare exempt mark without a reason does not count.

| # | Body directive | Where stated | Disposition |
|---|---|---|---|
| 1 | The canonical grammar lands in `templates/prompt-blocks/dispatch-record.md` and both consumers carry it | Goal | **AC1** |
| 2 | The skim step lands in the Specify seam's transcription window, before the freeze sweep | Goal, DP-4 | **AC2** (ordering assertion) |
| 3 | One reflection row per dispatch axis the entry itself records; no cherry-picking | Goal | **AC3**(a) |
| 4 | The four ` — `-separated fields, with a closed axis vocabulary and no duplicate axis | Goal | **AC3**(b), **AC3**(c), **AC3**(d) |
| 5 | The stated verdict must agree with the predecessor entry's own recorded value | Goal | **AC4** |
| 6 | The predecessor is found in `## Done` as readily as in `## Active` | Goal, Input space | **AC4** |
| 7 | An axis the predecessor did not record yields `no-predecessor-row` | Goal, Input space | **AC4** |
| 8 | The no-predecessor case is a conformant recorded outcome, never silence | Goal, DP-3 | **AC5** |
| 9 | A failed read (unresolvable or ambiguous predecessor id) refuses, distinct from the empty case | Goal, DP-3 | **AC5**(b), **AC5**(c) |
| 10 | An entry carrying no reflection line still passes (validate-if-present) | Goal, Open questions | **AC6** (with its non-vacuity control) |
| 11 | The new key is invisible to every shipped anchored `- dispatch:` scan | Goal (implied), Non-goals | **AC7** |
| 12 | `bin/close-out.sh` is not changed | Non-goals | **AC7** (byte-pin), **AC12** |
| 13 | Reachable spacing variants are collected and refused, never rendered invisible | Input space | **AC8** |
| 14 | CRLF board lines are tolerated | Input space | **AC8** |
| 15 | The suite grows with the checker and stays green | Goal | **AC9** |
| 16 | The tamper note records the promotion per duty and does not re-fire its trigger | Non-goals, DP-1 | **AC10** |
| 17 | Adopter-facing documentation lands in the same task | Declaration | **AC11** (with `- adopter-surface:`) |
| 18 | No new `bin/` script and no new CI step | Non-goals | **AC12** (both structural negatives) |
| 19 | `agents/pm-spec.md` is not edited at all | Non-goals | **AC12** (byte-pin) |
| 20 | No merged criterion is repaired, only disclosed | Non-goals | **AC12** (no other spec changed), **AC14** (disclosure exists) |
| 21 | This task's own entry dogfoods the positive case | Goal | **AC13** |
| 22 | The full-population two-arm diff is owed, by T-1108's amended procedure | Declaration, **A-7** | **AC14** (`- procedure-version:` content) |
| 23 | Counts are command-derived, never counted by eye | **A-5**, **A-6** | **AC14** (live re-derivation), **A-6**'s derivation command |
| 24 | Proposals 1 and 2 are not re-specified; no axis key or value set changes | Non-goals | **AC12** (`agents/pm-spec.md` byte-pin, `bin/close-out.sh` byte-pin — the two files carrying Proposal 1's table and Proposal 2's authoring duty) |
| 25 | No machine-readable predecessor-relationship key is coined; no positional inference | Non-goals, DP-2 | info-only (not promoted to AC) — a negative about a key this task never introduces; **AC12**'s scope lock is the mechanical floor, since coining one would require touching the board grammar's own linters, all of which it pins at zero, and **AC5**(b)/(c) prove the id is resolved by lookup rather than by position |
| 26 | The reflection is one hop, never a chain | Non-goals, Input space | info-only (not promoted to AC) — an absence of behaviour; no check line can observe a walk that never happens, and **AC3**'s conformant fixture contains exactly one predecessor entry so a transitive implementation has nothing to walk to |
| 27 | No historical record is retro-annotated | Non-goals | **AC12** (the allow-list names every touched path) |
| 28 | No fan-out mechanism is built or changed | Non-goals | **AC12** (allow-list contains no fan-out path) |
| 29 | The substantive "is this a mis-election" judgment stays arm B and is disclosed, never claimed closed | Goal, Input space | **AC10** (`arm-B-enumerated-instrument` literal required in the note) |
| 30 | The tamper note's own re-evaluation trigger is not discharged here | Non-goals | **AC10** (`re-evaluation trigger` literal required, in the added region) |
| 31 | No merged pull request; the sprint batch GO is out of scope | Non-goals | info-only (not promoted to AC) — both are outside this branch's diff entirely and **AC12**'s allow-list is what would catch any file touched on their account |

## Blast radius

Method: **full-population diff**, which is what `agents/pm-spec.md:91` requires unchanged for the declared `- verification-class: mechanism`. Every merged spec under `.shell-team/specs/` is enumerated, each one's criteria are run at the base ref's **committed blobs** — via a detached scratch checkout made with `git worktree add --detach`, or `git show` for a single-blob read, never the working tree — and again at `HEAD`, and the two verdict sets are differenced criterion by criterion. A `grep`-of-mentions inventory is **not** an acceptable substitute here, and the indirection class it cannot see — a criterion that reaches its target through a path built at run time from `bin/team-paths.sh --get todo` or named only through a variable — is discharged by running those criteria too rather than by disclosing it away, since the whole population is run.

- procedure-version: written by the freeze run (2026-08-30): the version of `agents/pm-spec.md:86` in the tree at this branch point is T-1108's amended text — one **snapshot** of the untracked telemetry corpus is **staged** into the base arm's worktree before either arm runs, resolved through `bin/team-paths.sh --get runs` inside that worktree and copied member by member as a real directory, skipping members git tracks at either ref — and `agents/pm-spec.md` is byte-pinned unchanged by **AC12**, so the version cannot drift mid-task. The freeze run's own pre-implementation measurements below were taken under that procedure (staged base arm). Whether the disclosed residual (only the base arm is staged; the live/HEAD arm reads the live corpus unstaged) is reached by any criterion in this population is owed by the implementation-time sweep and reported here with its result; the freeze run reached no content- or count-sensitive corpus read in the two criteria it measured. No disagreement between procedure versions was observed at freeze.
- reproduce: bash bin/derive-populations.sh --label t1109specs --set tracked-specs="git ls-files -- '.shell-team/specs/*.md'" --set untracked-specs="git ls-files --others --exclude-standard -- '.shell-team/specs/*.md'"
- population: 114 spec files — written by the freeze run from the `- reproduce:` line above (union item count of the two disjoint md-scoped sets: tracked 113 + untracked 1 = 114; derivation block embedded verbatim above). **AC14** compares this declared value against a live re-derivation. See `## Assumptions` row **A-6**.
- predicted-red: 5 — corrected twice, never defended: the authoring-time shape prediction was 2, the implementation-round partial measurement lowered it to 0, and the coordinating session's COMPLETE 113/113 both-arms fan-out sweep (below) measured the true value at 5 flipped criteria across 3 members, every one enumerated below with `head: FAIL`; the prediction is never defended. The shapes predicted: **(i)** any merged criterion that byte-pins `skills/run/SKILL.md`, `agents/tech-lead.md` or `templates/prompt-blocks/dispatch-record.md` as an unchanged whole line or whole file — T-1100's and T-1092's own scope locks are the candidates, and both are merge-point-scoped and already stale by their own terms; **(ii)** any merged criterion that counts `- dispatch:` sub-bullets on a live board entry, since this task adds a sibling key those counts must stay blind to — `T-1084` **AC9** and `T-1085` **AC8** both assert an exactly-two-rows count scoped to their own entries and both carry `- stale-at:` triggers naming a third axis. Explicitly **not** predicted red: `.shell-team/specs/T-1065-task-class-verification-pricing.md` **AC2**, which byte-pins a line in `agents/pm-spec.md` — that criterion was already reddened by T-1108's own edit and this branch's base ref is T-1108's tip, so it is base-red and head-red, no flip. Every measured base-green-to-head-red flip must be enumerated below on one line carrying its spec path, its `**ACn**` label and the literal `head: FAIL`; an unenumerated flip fails **AC14**.
- **Implementation-time measurement (2026-08-30, post-implementation, this task's own base ref `d8195312...`)**: shape (i)'s two candidates run in full at both this task's own base ref and `HEAD`. `.shell-team/specs/T-1100-verify-axis-split.md` **AC9** (the `skills/run/SKILL.md` byte-pin): base: FAIL, head: FAIL — no flip (pre-existing, unrelated to this task). `.shell-team/specs/T-1100-verify-axis-split.md` **AC7** (the `agents/pm-spec.md` differs-from-base pin, cited as a T-1100-scope-lock candidate): base: FAIL, head: FAIL — no flip. Every other criterion in `T-1100-verify-axis-split.md`'s own FAIL set is identical at both arms — `AC2 AC5 AC6 AC7 AC8 AC9 AC11 AC13 AC14 AC15` at base vs `AC2 AC5 AC6 AC7 AC9 AC11 AC13 AC14 AC15` at head (**AC8** alone moves base: FAIL to head: PASS, the disallowed-but-harmless direction, never a flip in **AC14**'s own sense) — no base-green-to-head-red flip anywhere in this spec. `.shell-team/specs/T-1092-specify-seam-review.md`'s FAIL set is byte-identical at both arms: `AC4 AC5 AC6 AC10 AC12 AC15 AC16 AC18 AC19 AC20` (each: base: FAIL, head: FAIL) — no flip. Shape (ii)'s two candidates likewise measured identical at both arms: `.shell-team/specs/T-1084-dispatch-routing-record.md` **AC9** — base: PASS, head: PASS — no flip (its own FAIL set otherwise: `AC3 AC5 AC10 AC11 AC14 AC15 AC16`, identical both arms); `.shell-team/specs/T-1085-default-path-firing.md` **AC8** — base: PASS, head: PASS — no flip (its own FAIL set otherwise: `AC1 AC16 AC18`, identical both arms) — reconfirms the freeze-run's own pre-implementation direction correction. `.shell-team/specs/T-1065-task-class-verification-pricing.md` **AC2** not independently re-run this round (unrelated to any file this task edits; its own base: FAIL/head: FAIL status is a T-1108-caused, disclosed, pre-existing fact, not this task's to re-verify).
- **QA-time reconciliation (2026-08-30, qa-verifier round 1).** Extending the engineer's disclosed partial rather than repeating it: QA re-ran the two authoring-predicted candidates and the two already-reconfirmed ones at both arms independently (matching the engineer's own measurement exactly, zero flips) and then drove its own strictly serial, one-spec-at-a-time, **both-arms** sweep (`HEAD` via the live checkout, `base` via a `git worktree add --detach` at this task's own base ref `d819531...` with the untracked runs corpus staged into it per `agents/pm-spec.md:86`'s procedure) over the population re-derived live at QA time (`bin/derive-populations.sh` → tracked 114 + untracked 0 = 114, since this spec itself is now a tracked commit on this branch rather than an untracked file — the union total is unchanged at 114, so no correction to the `- population:` line above is needed). Within the session time available to this QA round, **16 of the 113 non-self population members** were run to completion at **both** arms with their full per-AC FAIL sets differenced (`design-note-T-1012.md`, `T-1000` through `T-1014` in `git ls-files` sort order) — every one of them `base == head`, zero flips, `awk -F'\t' '{if ($2!=$3) print "FLIP"}' results3.tsv` over the reconciliation driver's own `results3.tsv` confirms 0 mismatched rows. This is genuinely new coverage, not a repeat of the engineer's own HEAD-only sweep: the engineer's 59-member pass measured `HEAD` alone for these same members, so QA's own pass is the first time their **base** arm was read at all for this task. The remaining ~97 members beyond that (including the rest of the engineer's own 59-member HEAD-only range, `T-1015` onward, and every member past `T-1057`) are named here as still unmeasured **at QA's own hand** rather than folded into a clean claim, on the same footing the engineer's own paragraph above already established — QA's environment carries the identical `bin/log-run.sh` lock-contention constraint and the identical absence of a `timeout`/`gtimeout` binary the engineer's interventions entry discloses, so the same strictly-serial discipline bounds QA's own wall-clock budget the same way. No `head: FAIL` enumeration is added below because none was found among any member either QA or the engineer actually ran, at either arm.
- **Full-population coverage, disclosed honestly rather than claimed complete.** A serial, one-spec-at-a-time driver (`bash bin/check-acs.sh <spec>` per population member, `HEAD` arm) was launched against the live population re-derived above, in a scratch working area outside the checkout that is not preserved past this engineering round (its own completed/remaining split is therefore a narrated fact about this round, not a re-derivable repository property, unlike the `- population:` count above). **Environment quirk newly discovered and recorded in `.shell-team/test-recipe.md`'s T-1109 entry**: an `xargs -P`-style parallel fan-out across distinct specs' `check-acs.sh` invocations (the pattern the T-1084/T-1083 recipe entries document) causes severe cross-invocation contention on `bin/log-run.sh`'s never-stealing directory lock whenever two or more concurrently-running specs' criteria each call `bin/log-run.sh`, stalling an 8-way batch for several minutes on this machine; the serial driver avoids the contention, at the cost of session wall-clock time (roughly 40-50s per member observed). Within the time available this round, the serial `HEAD`-arm driver reached **59 of 114** population members (`.shell-team/specs/T-1000-operating-conventions.md` through `.shell-team/specs/T-1057-loop-integration.md` in `git ls-files -- '.shell-team/specs/*.md'`'s own sort order, plus the four candidates named below, run out of order). Among every member reached, no FAIL set differed in composition from what an old, merge-point-scoped spec's own staleness already explains, and none newly mentioned any of the eleven files this task's `## Notes for engineer` names as touched. The two authoring-time-predicted candidates (`T-1100-verify-axis-split.md`, `T-1092-specify-seam-review.md`) plus the two already-reconfirmed ones (`T-1084-dispatch-routing-record.md`, `T-1085-default-path-firing.md`) were additionally run to completion at BOTH arms, ahead of the serial sweep's own order, specifically because they are the only members a textual/structural argument (their check commands are the only ones in the population that read one of this task's eleven touched paths, or a shared checker — `check-prompt-sync.sh`, `check-readme-version.sh` — whose own inputs include one) could possibly flip, per the measurement immediately above. The remaining 55 population members were not run to completion within this round's session time and are named here as unmeasured rather than folded into a clean claim; no `head: FAIL` enumeration is added below because none was found among every member actually run.
- **Complete-population reconciliation (2026-08-30/31, coordinating session — supersedes both partial disclosures above).** Under host direction the remaining coverage was executed as a tier1-fanout firing: 6 parallel background shell slices (executor deviation from agent instances disclosed on the board's election row and in `.shell-team/interventions/T-1109.md`), each slice owning its own PAIR of `git worktree add --detach` scratch trees — base arm detached at `d81953126a18cc76c94e4d02b15c260712be2652` (this task's measured base), head arm detached at `35a40e8…` — with the untracked runs corpus staged into BOTH arms from ONE pre-firing snapshot (42 files; the head worktree needs the staging too because, unlike the live checkout the canonical procedure names, a detached worktree carries no untracked corpus — a faithful reproduction of the live checkout's corpus state, noted as a procedure adaptation, not a deviation from its intent). Members: all 113 non-self tracked specs, round-robin into 6 slices; per member, `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh <spec>` run in each arm and the two per-AC FAILED sets differenced. **113/113 members measured at both arms — zero unmeasured; wall-clock 5506 s (~92 min) against the ~6 h serial projection.** Cross-checks: QA round 1's 46-member both-arms tally (`t1109-qa-partial-coverage.txt`) agrees with the fan-out on every overlapping member; the engineer's 4-candidate both-arms set agrees likewise. Result: **3 members carry base-green→head-red flips (5 criteria), all of the merge-point-scoped byte-pin/scope-lock staleness class, disclosed here and not repaired** (Non-goal: no merged criterion is repaired); 3 members flipped base-red→head-green (favourable direction, informational: `T-1100-verify-axis-split.md` **AC8**, `T-1103-oversight-profiles.md` **AC14**, `T-1104-review-input-fidelity.md` **AC17**); 109 of 113 members carry at least one base-red criterion (their own historical merge-point staleness, present at BOTH arms, no flip). **A finding about the earlier targeted argument, recorded rather than smoothed over**: the engineer's and QA's reverse-mapping claim ("only four members' check commands can reach a touched path") was measured INCOMPLETE — it missed `T-1104` **AC13**'s thirteen-path byte-lock (which names `bin/check-entry-mode.sh`, this task's principal edit) and `T-1053` **AC8**'s declined-surface diff-empty assertion over `agents/tech-lead.md` (this task's mirror edit). Only the full-population run surfaced them — the exact argument-versus-measurement gap the mechanism class's full-population norm exists to close.
- .shell-team/specs/T-1053-retro-mechanization.md **AC8** — base: PASS — head: FAIL — its "every declined surface stayed declined" clause asserts an empty `git diff <its base>...HEAD` for `agents/tech-lead.md`; this task's prompt-block mirror edit to that file lands inside that range at HEAD. Merge-point-scoped staleness of T-1053's own criterion; disclosed, not repaired.
- .shell-team/specs/T-1104-review-input-fidelity.md **AC13** — base: PASS — head: FAIL — its thirteen-path byte-lock pins `bin/check-entry-mode.sh` unchanged over `<its base>...HEAD`; this task's principal deliverable edits that script. Merge-point-scoped byte-pin staleness; disclosed, not repaired.
- .shell-team/specs/T-1108-sweep-corpus-isolation.md **AC10** — base: PASS — head: FAIL — the immediate predecessor's scope lock: its changed-and-added set now contains this task's files, outside its allow-list. Its own text declares it merge-point-scoped and expected to go stale; disclosed, not repaired.
- .shell-team/specs/T-1108-sweep-corpus-isolation.md **AC12** — base: PASS — head: FAIL — its `- population: 113 spec files` declaration no longer matches the live corpus once this spec's own file exists (114). Same merge-point-scoped class, its own `stale-at`-style note anticipated exactly this; disclosed, not repaired.
- .shell-team/specs/T-1108-sweep-corpus-isolation.md **AC13** — base: PASS — head: FAIL — its read-set sweep byte-compares criteria naming `.shell-team/test-recipe.md` and `docs/adopting.md` against ITS base blobs, and this task appends to both; the resulting flips are unlisted in T-1108's own Blast radius (frozen before this task existed). Merge-point-scoped; disclosed, not repaired.

<!-- BEGIN derivation: t1109specs -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: tracked-specs — status: 0 — lines: 113 — items: 113 — command: git ls-files -- '.shell-team/specs/*.md'
- set: untracked-specs — status: 0 — lines: 1 — items: 1 — command: git ls-files --others --exclude-standard -- '.shell-team/specs/*.md'
- union: items: 114
- bucket: tracked-specs — items: 113
  - .shell-team/specs/T-1000-operating-conventions.md
  - .shell-team/specs/T-1001-retro-input-acquisition.md
  - .shell-team/specs/T-1002-intervention-capture-channel.md
  - .shell-team/specs/T-1003-retro-reads-interventions.md
  - .shell-team/specs/T-1004-optin-hook-sample.md
  - .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md
  - .shell-team/specs/T-1006-lessons-resolver-key.md
  - .shell-team/specs/T-1007-scope-typed-ledger.md
  - .shell-team/specs/T-1008-lessons-corpus-import.md
  - .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md
  - .shell-team/specs/T-1010-operator-language-boundary.md
  - .shell-team/specs/T-1011-telemetry-event-rows.md
  - .shell-team/specs/T-1012-loop-replay-generator.md
  - .shell-team/specs/T-1013-loop-replay-docs-wiring.md
  - .shell-team/specs/T-1014-flag-rail-data-path.md
  - .shell-team/specs/T-1015-cutting-a-release.md
  - .shell-team/specs/T-1016-close-out-entry-boundary.md
  - .shell-team/specs/T-1017-close-out-interventions-gate.md
  - .shell-team/specs/T-1018-freeze-attestation-gate.md
  - .shell-team/specs/T-1019-is-span-row-parity.md
  - .shell-team/specs/T-1020-lessons-supersede-sweep.md
  - .shell-team/specs/T-1020-supersede-adjudication.md
  - .shell-team/specs/T-1021-arith-base10-audit.md
  - .shell-team/specs/T-1022-close-out-gate-symmetry.md
  - .shell-team/specs/T-1023-block-size-deferral-record.md
  - .shell-team/specs/T-1024-check-line-mktemp-guard.md
  - .shell-team/specs/T-1025-assert-parity-dead-comparison.md
  - .shell-team/specs/T-1026-skill-md-doc-completeness.md
  - .shell-team/specs/T-1027-promote-retro-2026-08-04.md
  - .shell-team/specs/T-1028-class-m-refreeze.md
  - .shell-team/specs/T-1029-claim-fidelity-qa-step.md
  - .shell-team/specs/T-1030-reviewer-board-write-boundary.md
  - .shell-team/specs/T-1031-check-handoff-flag-anchor.md
  - .shell-team/specs/T-1032-audit-prose-accuracy.md
  - .shell-team/specs/T-1033-promote-retro-2026-08-05.md
  - .shell-team/specs/T-1034-refreeze-hardening-execbit.md
  - .shell-team/specs/T-1035-spec-template-staleness-locks.md
  - .shell-team/specs/T-1036-wording-batch-141-143-144.md
  - .shell-team/specs/T-1037-checker-retro-precision.md
  - .shell-team/specs/T-1038-errexit-safe-pin-keying.md
  - .shell-team/specs/T-1039-promote-retro-2026-08-06.md
  - .shell-team/specs/T-1040-frozen-repair-batch.md
  - .shell-team/specs/T-1041-freeze-ux.md
  - .shell-team/specs/T-1042-ignored-base-and-retro-ledger.md
  - .shell-team/specs/T-1043-pm-spec-check-conventions.md
  - .shell-team/specs/T-1044-test-infra-bundle.md
  - .shell-team/specs/T-1045-codex-version-provenance.md
  - .shell-team/specs/T-1046-ignored-base-verdict.md
  - .shell-team/specs/T-1047-promote-retro-2026-08-08.md
  - .shell-team/specs/T-1048-handoff-durability-barrier.md
  - .shell-team/specs/T-1050-check-layer-fast-follow.md
  - .shell-team/specs/T-1051-inspection-ux-polish.md
  - .shell-team/specs/T-1052-records-editorial.md
  - .shell-team/specs/T-1053-retro-mechanization.md
  - .shell-team/specs/T-1054-binding-config.md
  - .shell-team/specs/T-1055-adapter-envelope.md
  - .shell-team/specs/T-1056-loop-liveness.md
  - .shell-team/specs/T-1057-loop-integration.md
  - .shell-team/specs/T-1058-telemetry-binding.md
  - .shell-team/specs/T-1059-docs-release-notes.md
  - .shell-team/specs/T-1060-adopter-binding-docs.md
  - .shell-team/specs/T-1061-adopter-docs-gate.md
  - .shell-team/specs/T-1062-release-notes-compare-link.md
  - .shell-team/specs/T-1063-editorial-batch.md
  - .shell-team/specs/T-1064-shipped-docs-accuracy.md
  - .shell-team/specs/T-1065-task-class-verification-pricing.md
  - .shell-team/specs/T-1066-effort-time-telemetry.md
  - .shell-team/specs/T-1067-context-lifecycle.md
  - .shell-team/specs/T-1068-agent-concurrency.md
  - .shell-team/specs/T-1069-phase-multiplexing.md
  - .shell-team/specs/T-1070-check-handoff-scaling.md
  - .shell-team/specs/T-1071-record-set-derivation.md
  - .shell-team/specs/T-1072-telemetry-span-discriminator.md
  - .shell-team/specs/T-1073-harness-agent-concurrency.md
  - .shell-team/specs/T-1074-fanout-orchestration.md
  - .shell-team/specs/T-1075-fanout-adoption-versioning.md
  - .shell-team/specs/T-1076-log-run-locking.md
  - .shell-team/specs/T-1077-worktree-reconcile.md
  - .shell-team/specs/T-1078-tier3-pilot.md
  - .shell-team/specs/T-1079-tier2-judge.md
  - .shell-team/specs/T-1080-depth-axis-contract.md
  - .shell-team/specs/T-1081-freeze-sweep-hardening.md
  - .shell-team/specs/T-1082-telemetry-discriminator.md
  - .shell-team/specs/T-1083-agent-launch-fanout.md
  - .shell-team/specs/T-1084-dispatch-routing-record.md
  - .shell-team/specs/T-1085-default-path-firing.md
  - .shell-team/specs/T-1086-subject-harness.md
  - .shell-team/specs/T-1087-judge-briefing.md
  - .shell-team/specs/T-1088-tier2-firing.md
  - .shell-team/specs/T-1089-harness-recut.md
  - .shell-team/specs/T-1090-sign-safety-close.md
  - .shell-team/specs/T-1091-operator-authored-entry.md
  - .shell-team/specs/T-1092-specify-seam-review.md
  - .shell-team/specs/T-1093-verification-ceiling.md
  - .shell-team/specs/T-1094-trial-adoption.md
  - .shell-team/specs/T-1095-means-ends-reflection.md
  - .shell-team/specs/T-1096-selection-trust-gates.md
  - .shell-team/specs/T-1097-trial-branch-flag.md
  - .shell-team/specs/T-1098-trial-recipe-execution.md
  - .shell-team/specs/T-1099-board-heading-integrity.md
  - .shell-team/specs/T-1100-verify-axis-split.md
  - .shell-team/specs/T-1101-pii-path-shapes.md
  - .shell-team/specs/T-1102-check-acs-fence.md
  - .shell-team/specs/T-1103-oversight-profiles.md
  - .shell-team/specs/T-1104-review-input-fidelity.md
  - .shell-team/specs/T-1105-review-executor-resolution.md
  - .shell-team/specs/T-1106-check-binding-consistency.md
  - .shell-team/specs/T-1107-close-out-gates.md
  - .shell-team/specs/T-1108-sweep-corpus-isolation.md
  - .shell-team/specs/T-111-pii-shape-checker.md
  - .shell-team/specs/T-112-commit-identity-and-ignore-lock.md
  - .shell-team/specs/T-113-lessons-deidentification.md
  - .shell-team/specs/design-note-T-1012.md
- bucket: untracked-specs — items: 1
  - .shell-team/specs/T-1109-dispatch-reflection.md
<!-- END derivation: t1109specs -->

## Pre-commitment

This task builds a verification mechanism, so the drop order and its triggers are fixed **before the first review round**, and executing a trigger requires only confirming its factual precondition — never inventing a disposition mid-task.

- **never-dropped — duty A and duty B together in `bin/check-entry-mode.sh` (**AC3**, **AC4**, **AC5**), with the fixture cases that exercise them (**AC9**) and the `skills/run/SKILL.md` step that runs the gate in the right window (**AC2**).** They are one unit and not four: duty A without duty B leaves a record anyone can transcribe falsely, which is the arm-B instrument this task exists to replace; duty B without the seam step is a judgment nothing invokes. This is the task's floor and the whole of its value. If it is defeated in two consecutive rounds, the task **stops and returns to planning** rather than being patched a third time: the same mechanism defeated across consecutive design generations is evidence about the design premise, not about the implementation's craft.
- **droppable-1 — the adopter-documentation sentences (**AC11**, and the `- adopter-surface:` line it carries).** Dropped first if the three documentation files draw the rounds — a mirrored sentence in a bilingual README pair is the component here most exposed to findings that are about wording rather than about the mechanism. On a drop, the `- user-visible: yes` declaration is **not** changed (that would be a class-B re-freeze of a ruling the measurement supports); instead the documentation travels to its own filed issue as a fast-follow carrying the rounds' findings as its requirement list, the drop is recorded as a **known departure** from `agents/pm-spec.md:89`'s freeze-time blocker with this pre-commitment cited as its authority, and the departure is surfaced at the next human gate rather than absorbed.
- **droppable-2 — the canonical-block half: the grammar's placement in `templates/prompt-blocks/dispatch-record.md` and its mirror into `agents/tech-lead.md` (**AC1**).** Dropped second, only if droppable-1 survives but the prompt-sync mirroring draws the rounds. On a drop, the grammar lives in `skills/run/SKILL.md`'s own step prose — the party that actually writes the record — the two files revert to their base bytes, `bin/check-prompt-sync.sh` stays green by construction, and `tech-lead`'s Plan-time awareness of the predecessor's rows travels to the same filed issue. The mechanism is unaffected: the checker reads the board, not the block.
- **trigger-factual:** the same component draws independent new defects in **two consecutive** review rounds.
- **trigger-contextual:** before a third round of rework on that component begins.
- **trigger-priority:** when the two conditions disagree, the **factual** condition governs — the drop executes at the second consecutive round even if a third has not been started, because the cost this pre-commitment bounds is rounds spent, not rounds planned.
- **drop-disposition:** at the moment a trigger fires, execute the numbered drop above — confirm the factual precondition, state which component is dropped and where it goes, and proceed. No design question is re-opened at that point.
- **Authority:** this pre-commitment is **AI self-discipline**, self-imposed by this role at authoring time. It is **not** an operator-ratified ruling, and any report citing it must say so.

## Notes for engineer

**Files this task touches, and nothing else**: `templates/prompt-blocks/dispatch-record.md`, `agents/tech-lead.md`, `skills/run/SKILL.md`, `bin/check-entry-mode.sh`, `tests/check-entry-mode/run.sh`, `docs/loop-engineering/record-tamper-resistance.md`, `README.md`, `README.ja.md`, `docs/adopting.md`, `.shell-team/test-recipe.md`, the resolved board, this spec, and this task's three records. Explicitly **not** touched: `agents/pm-spec.md`, `bin/close-out.sh`, `.github/workflows/check-handoff.yml`, `bin/team-paths.sh`, `bin/check-handoff.sh`, `bin/check-board-headings.sh`, anything else under `bin/`, anything else under `tests/`, and `docs/loop-engineering/phase-multiplexing.md`.

**Measured-at-ref labels: not applicable — this task's deliverables print no `measured at <ref>` label, no `**Base ref**:` header and no per-row `- measurement:` field.** The block and the run-skill step prescribe a procedure; the checker prints refusal messages, not measured results; the documentation sentences state a rule. The one place a ref-labelled result appears is `## Blast radius`, whose base-side reads **AC14** requires to name a committed-blob reading target.

**Your first action, before writing any code.** Read `bin/check-entry-mode.sh` end to end, then decide where the predecessor lookup goes. It must be a **second** scan, not a widening of the first: the `--task` entry's own resolution stays `## Active`-only and keeps exiting `2` when the task is not exactly one such entry, because the script's forward-only scoping (`:47-:53`) depends on it. The predecessor scan is a different question — "does this id name exactly one top-level entry anywhere on this board" — and answers over both `- [ ] **T-NNNN** ` in `## Active` and `- [x] **T-NNNN** ` in `## Done`, refusing on zero matches and on more than one. `## Assumptions` row **A-8** records why.

**Five traps, each measured rather than guessed.**

1. **The section trap, and it is this task's own live case.** `bin/close-out.sh` moves a closed entry to `## Done`, so by the time task N freezes its predecessor's rows are almost always in `## Done` — measured: `T-1108`'s five `- dispatch:` rows are at `.shell-team/todo.md:3642-3646`, inside `## Done`, while `bin/check-entry-mode.sh`'s existing scan is guarded by `sec ~ /^## Active/`. A predecessor lookup restricted to `## Active` would return "not found" for the ordinary case and would then, if that were treated as the empty case, silently pass. It must refuse. **AC4** and **AC5**(b) assert both halves.
2. **The prefix trap.** `- dispatch-reflection:` shares the stem `dispatch` with `- dispatch:`. Every shipped anchored scan requires the literal colon immediately after `dispatch`, so none of them matches the new key — verified against all three expressions in **AC7** — but your own new scans must not reintroduce the collision in the other direction: test the **longer** stem first, exactly as `bin/check-entry-mode.sh:30-37` already documents for `- flagged-gap` versus `- flagged-gap-resolution`, or a reflection line will be counted as a dispatch record. **AC7** byte-pins the three existing anchors so a rewrite of any of them turns red rather than silently invalidating this reasoning.
3. **The invisible-variant trap, which has now bitten this repository twice.** T-1096's rework round 1 carried a Blocker and a Major for exactly this: a stem net pinned to one space after the bullet dash made a doubled-space marker invisible to the whole gate, so a genuinely flagged, genuinely unresolved gap read as the conformant zero-gaps case. Collect with `-[[:space:]]+` and validate with the strict single-space grammar, so a variant is **refused as malformed** rather than dropped. **AC8**'s discriminating observation is exactly this: exit `1`, never exit `0`.
4. **The vacuous-pass trap in this task's own dogfood.** `bin/check-entry-mode.sh` today exits `0` on a board carrying reflection rows, because it ignores sub-bullets it does not know. So **AC13**'s exit-`0` clause is satisfiable before you write a line of code, and **AC6**'s bare-board clause is too. Both criteria carry a mutation control that only an implemented duty can satisfy — do not "fix" a criterion by removing its control.
5. **The suite's own scratch-path trap.** `tests/check-entry-mode/run.sh` and `tests/close-out/run.sh` both build fixture boards under a temp root. Run them sequentially, not concurrently, and confirm `git worktree list` shows no leftover after **AC14**'s sweep.

**Running this spec.** Use `CHECK_ACS_TIMEOUT=900` (see `## Assumptions` row **A-9**).

**Append to `.shell-team/test-recipe.md`** the procedure you establish: the predecessor-lookup shape you settled on, how you built the CRLF and doubled-space fixtures, the timeout you needed, and the measured behaviour of the three shipped `- dispatch:` anchors against a reflection line.
