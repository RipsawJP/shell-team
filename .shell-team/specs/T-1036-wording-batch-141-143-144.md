# Two cross-task class-B re-freezes correct a mis-categorised site label and a closed enumeration that forbids its own mandatory tool, and two orchestrator-facing skill lines restate the reviewer's board boundary as a rule

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1036
**Base**: `5a5c09b` — the tip of T-1035's branch and the v1.6.0 sprint train's **fourth** car, not `develop`'s tip. Every `git` anchor below resolves that literal and guards itself with `git rev-parse --verify` first, so an unresolvable ref fails closed rather than passing vacuously.
**Branch**: `feature/141-143-144-wording-batch` (PR base stays `develop`; the pull request is held open for the sprint batch merge, stacked on #152 / #154 / #156)

## Problem

Three findings filed out of the v1.5.0 train's cross-provider rounds all name shipped wording rather than shipped behaviour, and two of them sit inside frozen intent blocks that only pm-spec may amend. T-1029's frozen prose calls the `## Summarized sources` declaration line "one such site" among the restatement sites **in the frozen region**, while that spec's own D1 deliberately placed the section outside the intent block — the label contradicts the placement decision it is describing. T-1030's canonical write-boundary Rule closes its write surface with "Write these paths and no others", but the `codex exec` subprocess this role is required to run writes its own state directory on every invocation, so a strictly literal reading turns the mandatory tool into a rule violation. And two orchestrator-facing skill lines state the reviewer's board prohibition as a capability absence — "it is read-only on the board, with no `gh`/API access" — which is factually false (the role carries `Bash`) and therefore reads as no rule at all, which is precisely the defect T-1030 shipped to remove from the agent file while leaving these two copies behind.

## Summarized sources

One line per document whose facts this spec restates, naming the distinction being carried over. This section sits outside the intent block deliberately; naming a further source during a rework is an ordinary edit, not a re-freeze.

- `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` — **read directly, in full**. Distinctions carried over, all load-bearing: its **D1** places `## Summarized sources` *outside* the intent block deliberately, and gives the mutability reason for it; its **F3** frozen block is exactly one physical line and currently ends the site list with the apposition `the declaration line itself, which is one such site and never the whole set`, while its **Goal** carries the same apposition verbatim and its **D3(ii)** carries the variant `the declaration line, which is one site among them and never the whole set`; its **AC4** pins seven fixed strings of which one is the scope clause `every site in the frozen region that restates, quotes or summarizes those facts` — the imprecision this task repairs is in the *following* apposition, not in that clause, which is why AC4's pinned strings do not move; its **F2** (`agents/pm-spec.md`'s self-check bullet) already frames the declaration additively ("an index into the frozen region, not the only place the summary lives") and therefore carries no defect; its **Input space** bullet already frames the check surface additively ("the intent block … **plus the declaration line**"), which is the measured ground for calling #141 terminology-only; its intent block carries **10 `- check:` lines across 11 criteria**, which is the `lines=10/10` the v3 attestation must declare.
- `.shell-team/specs/T-1030-reviewer-board-write-boundary.md` — **read directly, in full**. Distinctions carried over: its **F1** frozen block is exactly one physical line and its closed enumeration has exactly four members (the review record, the canonical raw-capture basenames, the two `--alloc` per-invocation raws plus their `cleanup` removal, `$TMPDIR` scratch); its **D3** heading asserts `everything not on it is read-only`, which is the one claim the carve-out makes imprecise and therefore the one body site that must move with F1; its **Input space** reachable-classes bullet enumerates the runtime situations the Rule governs and omits the `codex exec` state-directory write; its **AC4** pins **thirteen** fixed strings — including `Write these paths and no others` — and three occurrence counts in `agents/codex-reviewer.md` (`Board and write boundary` = 1, `a rule you follow, not a capability you lack` = 4, `board-and-write-boundary Rule` = 3), all of which an additive carve-out leaves intact; its **AC5** is a whole-file delta lock (four added, three removed against `53955c5`) whose work-only set is compared against the concatenation of its own four frozen blocks, so a re-freeze of F1 moves both sides together; its **AC9** locks `skills/` byte-identical to `53955c5`, which #143 breaks by design; its intent block carries **11 `- check:` lines across 12 criteria**, which is the `lines=11/11` the v2 attestation must declare.
- `.shell-team/specs/T-1028-class-m-refreeze.md` — read directly at its **AC10**. Distinction carried over: AC10 is a **live** lock, not a merge-point-scoped one — it requires the F4 frozen line to sit *between* the `6. **Review**` and `7. **Done**` step lines of `skills/run/SKILL.md`, and it additionally requires `bash tests/codex-skeleton-hygiene/run.sh` and `bash bin/check-prompt-sync.sh` to be green, explicitly so that green cannot be bought by editing a lock. The `#143` replacement line lives inside that same window; replacing it in place preserves the window, moving it out of the window would not.
- `tests/codex-skeleton-hygiene/run.sh` — read directly at its live-file lock section. Distinctions carried over: two regexes run against **live** files rather than fixtures, and both must match **zero** lines. `NEW_AC3` (the broken-invocation shape `(^|[[:space:]])([^[:space:]]*/)?(bash|sh|source|\.)[[:space:]]+bin/check-(provenance|interventions)\.sh`) is run over `agents/qa-verifier.md`, `skills/run/SKILL.md` and `skills/goal/SKILL.md` — three of this task's four target files. `NEW_AC15` (the stateful-trace shape `(provenance|interventions)[-[:space:]]+gate:AC[0-9]|route[-[:space:]]+back[-[:space:]]+through[-[:space:]]+loop[-[:space:]]+guard`) is run over `skills/run/SKILL.md` alone. Both locks fail closed on a `grep` exit ≥ 2, so a missing file is a failure and not a silent pass.
- `.shell-team/specs/T-1035-spec-template-staleness-locks.md` — read directly. Distinction carried over: the shape of a cross-task class-B re-freeze executed from a later task's spec — a frozen ratification block pinned byte-exact with a board occurrence count of exactly one, a criterion running `bin/check-refreeze-class.sh` against a **materialised base blob** and requiring **exit status exactly 1**, a deletion-column lock that *identifies* the removed line rather than merely counting it, and the GO grounds recorded in the ratification line itself rather than left to conversation. This spec restates that pattern twice over; it does not restate T-1035's own subject matter.
- `.shell-team/todo.md` — the `## Done` entries for **T-1029**, **T-1030** and **T-1035**, read directly. Distinctions carried over: T-1029 is at **v2** (`- intent-hash (v2): aa47eaa0525af8a73cf89b9b3655b310e62bdadb`, with **both** a v1 and a v2 `freeze-attestation` line present, `lines=10/10`), so this task takes it to **v3**; T-1030 is at **v1** (`- intent-hash (v1): d0f73437565c9211c998e78a863948eb23e27972`, one attestation, `lines=11/11`), so this task takes it to **v2**; and the ledger shape a vK→vK+1 re-freeze executes — the ratification line appended, **every prior attestation retained**, a new attestation appended, and the single `intent-hash` record **replaced**, that replacement being the branch's only board deletion *per re-freeze*. Two re-freezes therefore mean exactly **two** board deletions, not one.
- `bin/check-refreeze-class.sh` — read directly (its header contract). Distinctions carried over: the usage form is `check-refreeze-class.sh [--old-hash <40-hex>] [--new-hash <40-hex>] [--] <old-spec.md> <new-spec.md>` with **both positional arguments required**; the exit contract is `0` = mechanics, `1` = class-b, `2` = usage or structural; the mechanics rule requires the two normalised blocks to have the **same line count**, so any insertion or deletion is class-b, and any prose change at a differing index is class-b; two blocks byte-identical after normalisation are `structural` (exit 2) and never a pass. Both re-freezes here change prose at equal line counts, so both are class-b by rule (iii), and `2` would mean the classifier could not evaluate the pair rather than that the delta was clean.
- `agents/qa-verifier.md` (line 65), `agents/codex-reviewer.md` (lines 156–157), `skills/run/SKILL.md` (line 81), `skills/goal/SKILL.md` (line 233) — the four shipped lines, **read directly at those positions**. Distinctions carried over: all four are hand-written and sit **outside** every generated marker region (`agents/qa-verifier.md`'s markers begin at line 81; `agents/codex-reviewer.md` carries zero marker regions and only `contain`-mode obligations), so a direct edit is the canonical route and no generator is implicated; `agents/codex-reviewer.md`'s R1 sits at 156 and F1 at 157, contiguous, which is the adjacency T-1030's AC1 pins; and the phrase `read-only on the board` occurs exactly once in each of the two skill files and nowhere else in `skills/`.
- **GitHub issues #141, #143 and #144 — RELAYED verbatim by the coordinating session, not opened by this role.** pm-spec has no network or `gh` access. Distinctions carried over as relayed: **#141** is *terminology-only* — no reading lets QA skip the declaration line (F3 enumerates it), behaviour is correct and only the label is imprecise, and the requested fix is to reword so the declaration line is an **additional** checked site rather than a member of the frozen region. **#143** names two orchestrator-facing lines and asks for **one-for-one line replacements** rephrasing the capability-absence claim into rule form pointing at the reviewer's canonical prohibition bullet; it prescribes no re-freeze because neither line is in anybody's frozen region. **#144** is the T-1030 round-1 Minor: the mandatory `codex exec` subprocess *incidentally* writes `$CODEX_HOME` / `~/.codex` on every invocation, and the requested fix at re-freeze is either to add that state directory to the enumeration **or** to add an explicit "tool-internal state directories written by the tools this role must run" clause. The verifying roles record all three as `environmentally-unverified` if they cannot reach the issues either.
- **The tech-lead's measured facts and the 2026-08-05 sprint v1.6.0 planning GO — RELAYED, not opened.** They reached this role only as text in the coordinating session's task prompt. Distinctions carried over, with what pm-spec re-measured marked: the two ledger versions and their hashes — **re-measured** against the board; the four shipped lines' positions and their marker-region status — **re-measured** against the files themselves; T-1028's AC10 window and the two skeleton-hygiene live regexes — **re-measured** against those files' bytes; the #141 same-class inventory (T-1029's L24 / L40 / L82 / L158 plus `agents/qa-verifier.md:65`) — **re-measured, and extended by two negative findings of pm-spec's own**: T-1029's F2 (L74) and its Input-space bullet (L122) both already carry the additive framing and therefore carry no defect, which is recorded here rather than left implicit so a later reader does not read their absence from the inventory as an oversight.

## Goal

<!-- BEGIN intent-block: T-1036 -->

Two cross-task class-B re-freezes and two plain line replacements land in one branch, each independently revertable. `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` goes **v2 → v3**: its Goal sentence, its D3(ii) property and its frozen **F3** block stop categorising the `## Summarized sources` declaration line as a member of the frozen region and instead state it as a site checked **in addition to** the frozen-region restatement sites, with the verification surface unchanged, the declaration line still mandatory, and AC4's pinned scope clause untouched. `.shell-team/specs/T-1030-reviewer-board-write-boundary.md` goes **v1 → v2**: its frozen **F1** block, its D3 decision and its Input-space reachable-classes bullet each gain the same bounded carve-out stating that a state or cache directory a tool this role is *required* to run maintains for itself sits outside the closed enumeration rather than inside it, with the enumeration's four members and AC4's thirteen pinned strings unchanged. The four shipped lines — `agents/qa-verifier.md`'s summarized-source item, `agents/codex-reviewer.md`'s board-and-write-boundary Rule, and the fast-follow-recording lines of `skills/run/SKILL.md` and `skills/goal/SKILL.md` — carry those bytes exactly, the two skill lines restating the reviewer's board boundary as a rule pointing at that canonical Rule rather than as a capability absence. Both re-freezes are recorded on the board with a ratification line, a retained prior attestation, a new attestation and a replaced hash record, so the board's deletion count for this branch is exactly two. No checker is written or changed; `bin/`, `tests/`, `templates/`, `docs/`, `.github/`, `.shell-team/lessons.md`, every other `agents/*.md` and every other file under `skills/` are byte-identical to the base ref; the generated playbook blocks still regenerate to a zero diff; and T-1028's AC10 window over `skills/run/SKILL.md` is intact.

### Settled decisions

Each decision below is resolved; nothing here is left to implementation judgment. Every text this task ships is frozen byte for byte in this section inside a `<!-- frozen-begin: X -->` / `<!-- frozen-end: X -->` pair, and every criterion that pins one reads its expected bytes **out of this file** rather than restating them inside a shell quote — each of these texts carries apostrophes, backticks and em-dashes that no single quoting style survives intact.

**GO authority for both re-freezes, transcribed rather than re-opened.** No per-instance host GO is taken for either. Three grounds apply to each independently: (1) the 2026-08-05 sprint v1.6.0 planning GO approved #141, #143 and #144 inside T-1036's scope; (2) each issue itself prescribes the pm-spec re-freeze route, which is also what the target spec's own pre-commitment requires (T-1029's R1; T-1030's R-1) for a defect that lives in frozen wording; (3) this checkout's operator ruling that a freeze or re-freeze proceeds on orchestrator attestation and that no new human gate is invented. **The cross-provider review receives both re-freezes as mandatory disclosure items and keeps the veto over each one independently** — a `REQUEST_CHANGES` against one re-freeze does not implicate the other, and P2 below fixes what a revert of either looks like. The shipped default for adopters (a per-instance human GO, `docs/tuning-oversight.md`) is unchanged and no document describing it moves.

- **DP-1 — #141's reword fixes the categorisation and nothing else; the check surface stays additive and the declaration line stays mandatory.** The defect is a label, not a behaviour: F3 already enumerates the declaration line, so no reading of the shipped text lets QA skip it. What the text gets wrong is calling it "one such site" *among* the sites in the frozen region, when T-1029's own D1 placed the section outside that region deliberately, and when that spec's Input-space bullet already frames the surface additively ("the intent block … plus the declaration line"). The repair therefore states the same surface with the right shape — **frozen-region restatement sites PLUS the declaration line** — and never with a shape a reader could narrow. **Any phrasing that admits a narrowing reading is Blocker-class**, because it would resurrect the exact Major the T-1029 v1 → v2 re-freeze closed. The machine half is two new positive pins asserted in AC5 (the declaration line named as an *additional* checked site, and the "never in their place" clause), which is a positive lock on a requirement rather than only a negative sweep for the old phrase.
- **DP-2 — T-1029's AC4 scope clause and T-1030's thirteen pinned strings do not move.** AC4 of T-1029 pins `every site in the frozen region that restates, quotes or summarizes those facts`; that clause is *correct* — the imprecision lives in the apposition that follows it — so the reword keeps it byte-identical and AC4's own line does not move at all. AC4 of T-1030 pins thirteen fixed strings including `Write these paths and no others`; the #144 carve-out is framed as a **scope statement about what the enumeration governs**, not as a fifth member, so every one of those strings survives verbatim and all three of that criterion's occurrence counts are unmoved. Keeping both criteria's own lines out of the delta is what keeps each re-freeze's `lines=N/N` unchanged and each classification unambiguous.
- **DP-3 — the #141 same-class inventory, and the two sites deliberately *not* moved, with reasons.** Live sites carrying the mis-categorisation: T-1029's **Goal** (the apposition `and the declaration line itself, which is one such site and never the whole set`), its **D3(ii)** (the variant `and the declaration line, which is one site among them and never the whole set`), its frozen **F3** block (the same apposition as the Goal), its **Body-to-AC correspondence** row (`the declaration line being one of them and never the whole set` — outside the intent block, so an ordinary consistency edit and not part of the class-B delta), and the shipped `agents/qa-verifier.md` line. Two further sites were inventoried and **carry no defect**: T-1029's **F2** already says the declaration "is an index into the frozen region, not the only place the summary lives", which is the additive framing stated correctly; and T-1029's **Input space** already says the scope is "the intent block … plus the declaration line". Neither moves, and neither is a half-fix — a correct sentence rewritten for symmetry would enlarge a frozen delta for no truth-preserving gain. Board entries and `.shell-team/reviews/*` records carrying the old phrasing are **history and are never rewritten**, which is why every negative sweep in this spec is scoped to the four target files and the two specs rather than to the repository.
- **DP-4 — #144's carve-out shape: one bounded clause naming both the general class and the concrete instance.** Two shapes were live. Enumerating `$CODEX_HOME` / `~/.codex` alone bakes an environment-variable name into frozen bytes that a CLI rename would falsify, and covers only one tool. A purely generic "tool-internal state directories" clause is durable but open-ended — and D3 of T-1030 rejected an open-ended prohibition list on exactly the ground that *its gaps read as permission*; the symmetric objection applies to an open-ended carve-out, which reads as permission for anything the role chooses to run. **Both halves ship in one sentence**: a general clause bounded to *a tool this role is required to run*, with the Codex CLI's own state directory named as the instance, plus an explicit statement that the carve-out covers those tools' own bookkeeping and is never a licence to write there deliberately. The bounding to *required* tools is what stops the generic half from becoming a loophole; the named instance is what makes the clause falsifiable by a reviewer who can check whether `codex exec` really writes it.
- **DP-5 — the #144 delta lands at three frozen sites and skips one, with reasons.** Machine inventory of T-1030's frozen region for restatements of the write surface returns four sites: **F1** (the shipped enumeration), **D3** (the decision that fixes it), the **Input-space** reachable-classes bullet (the runtime situations the Rule governs), and the **Goal** sentence. **F1 moves** — it is the shipped text the finding is against. **D3 moves** — its heading asserts `everything not on it is read-only`, and that is the single claim the carve-out makes imprecise; leaving it would be the half-fix the tech-lead's constraint names, a body directive contradicting the shipped bytes it describes. **The Input-space bullet moves** — the `codex exec` state-directory write *is* a reachable runtime situation, and an input space that omits an input class the shipped Rule now speaks to is the same omission #144 names one level up. **The Goal sentence does not move**, and the reason is recorded rather than left implicit: it says the Rule "closes the list of paths this role may write" and enumerates them parenthetically, making no "everything else is read-only" claim — the carve-out's own content is that a required tool's state directory *is not a path this role writes*, so the Goal stays literally true after the change and rewriting it would widen the frozen delta with no truth-preserving gain. One row is added to T-1030's Body-to-AC correspondence table (outside the intent block) so the new directive is promoted 1:1 rather than living only in prose.
- **DP-6 — #143's two replacements are role-contextual variants, not one identical sentence.** The two lines differ in who they address: `skills/run/SKILL.md`'s step-6 line speaks in the second person to the orchestrator ("filing and recording are **your** responsibility"), while `skills/goal/SKILL.md`'s step-4 line speaks in the third person ("filing and recording are **the orchestrator's** job"). An identical replacement sentence would flatten that difference and would rewrite each line's untouched tail, a wider delta than #143 asks for. Each replacement therefore preserves its own line's surviving clauses verbatim and replaces **only** the capability-absence parenthetical, exactly as T-1030's own R1 / R2 / R3 precedent did. Both carry the same fixed pointer phrase `board-and-write-boundary Rule` and the same rule-form phrase `not a capability it lacks`, which is the machine-checkable common half. Neither replacement is a re-freeze: the two lines sit in **nobody's** frozen region.
- **DP-7 — T-1030's AC9 breaks further and is disclosed, never repaired.** That criterion pins `skills/` byte-identical to `53955c5`; it is already stale (three later cars have landed) and #143 breaks it by design. **No retroactive `stale-at:` grant is added to it, and its base ref is not widened** — merge-point-scoped criteria are declared stale in their own text and disclosed at the gate, and merge-ranging one trades away the very thing it exists to confine. The same disposition applies to T-1029's AC7 / AC8 and T-1030's AC5 / AC8.
- **DP-8 — the engineer's surface is exactly four lines, and no ledger line.** The engineer splices the four frozen texts into the four shipped files and touches nothing else in them. The two specs' frozen regions are pm-spec's edit, already made in this file's own commit; the board's ratification, attestation and hash records are the coordinating session's step at the freeze gate. An engineer edit to hashed bytes is precisely what the class-B route exists to prevent.

The six frozen texts. **Q1** and **Q2** are the re-frozen shipped lines (each also replacing the corresponding frozen block inside its own spec, so the two copies can never drift); **Q3** and **Q4** are the two plain replacements; **R29** and **R30** are the board ratification lines. Every one is exactly one physical line.

**Q1** — the new `agents/qa-verifier.md` summarized-source item, identical to T-1029's re-frozen **F3** block:

<!-- frozen-begin: Q1 -->
```markdown
- **Verify every summarized source against the source itself, everywhere the spec restates it.** When the spec carries a `## Summarized sources` section whose content is not the `not applicable — <reason>` sentinel, this item is mandatory. For each named document: open it, locate **every site in the frozen region that restates, quotes or summarizes those facts** — the Goal sentence, a Non-goal, an Acceptance Criterion's prose, an Input-space clause, a settled-decision paragraph inside the marker pair (for a spec with no intent-block markers, read its `## Goal`, `## Non-goals` and `## Acceptance criteria` sections instead) — **and, in addition to those sites, the `## Summarized sources` declaration line itself**, which sits outside the frozen region by design and is always checked alongside them and never in their place; then match each of those sites and that line clause by clause against the source's own bytes. Confirming the declaration line is accurate while a criterion two sections away drops the same distinction is the exact miss this item exists to close: the incidents behind it flattened a count and a per-case requiredness contract inside frozen criteria, not inside a declaration. Re-reading the spec's paraphrase, or re-deriving the claim from the spec's own wording, verifies nothing and never satisfies this item. Your verdict carries one line per named source: its path or locator, the sites you checked, the distinction the spec claimed to carry over, and the result; one blanket "sources checked" sentence is itself a flattening and does not count. Report a flattened distinction as a finding (kick back to `READY_FOR_ENG`), not as a wording nit — and when it sits inside the frozen region, say where, since the repair is a class-B re-freeze routed through pm-spec rather than an engineer edit to hashed bytes. When a named source cannot be opened from this session for an environmental reason (a tracker item you have no network access to, a path outside this checkout), record it as `environmentally-unverified` naming that source, in the risk notes of your verdict — the same convention as the Adversarial fixture synthesis checklist below; that alone never forces a FAIL, but never silently omit it, never fake the check, and never invent a new token for it. This item is mandatory and is **not subject to the out-of-input-space exemption** above. When the spec carries no `## Summarized sources` section at all, keep the prior behavior unchanged — this rule is backward compatible.
```
<!-- frozen-end: Q1 -->

**Q2** — the new `agents/codex-reviewer.md` canonical Rule, identical to T-1030's re-frozen **F1** block:

<!-- frozen-begin: Q2 -->
```markdown
- **Board and write boundary — a rule you follow, not a capability you lack.** Your `tools:` line grants `Bash`, which this role genuinely needs (`codex exec`, `codex-capture.sh`, and read-only `git log` / `git diff` / `git rev-parse` calls) and which can redirect output into any writable path; no agent in this repository carries per-path write scoping in its frontmatter, so nothing structurally stops you from writing the board. The boundary is therefore a rule, and it binds exactly as much as if it were structural. **Write these paths and no others**: your review record `T-XXX.md` in the reviews dir resolved through `team-paths.sh --get reviews`; the canonical raw-capture basenames `T-XXX-codex-primary.{txt,jsonl}` and `T-XXX-codex-adversarial.{txt,jsonl}` in that same dir; the two per-invocation raw paths `codex-capture.sh --alloc` printed for this round, including removing them in the `cleanup` block; and scratch files under `$TMPDIR`. That enumeration bounds the paths **you** write; a state or cache directory that a tool this role is required to run maintains for itself sits outside it — in particular the Codex CLI's own state directory (`$CODEX_HOME`, or `~/.codex` when that is unset), which every `codex exec` invocation writes, and the equivalent internal state of the wrapper scripts named above. That carve-out covers those tools' own bookkeeping and is never a licence to write there deliberately, to keep anything of your own there, or to treat a path you chose to write as covered because some tool also touches it. Everything else in the checkout is read-only for you, and in particular you never edit the board (`team-paths.sh --get todo`): you do not flip a `- [ ]` checkbox to `- [x]`, change a status flag, or add, edit, reorder or delete any board line — not even when your own verdict is what would justify the change, and not even to record that verdict. You state it in your verdict block and in your review record, and the orchestrator transcribes it. You run no mutating git subcommand — no commit, add, checkout, restore, reset, rm, mv, branch, merge, rebase, stash, tag or push — with exactly one exception: when the coordinating session's task prompt for this round tells you to commit your own review record, you may stage and commit that one file and nothing else in the same commit. A commit you decided to make on your own initiative is outside the exception, and so is any commit carrying a second path. Provenance: in the v1.4.0 cycle this role flipped a board checkbox ahead of the merge gate, and the flip was undone **by hand** inside T-1025's close-out commit `9d82a49`, whose message records "also reverts the reviewer's premature checkbox flip — the close-out script owns the [x] transition". Read that backstop honestly: `bin/close-out.sh` contains no detect-and-revert logic — it finds a task's Active entry by an anchored `- [ ]` match, so an already-flipped line is not found at all and close-out fails with `not found as a top-level entry in ## Active`, loud but naming neither the flip nor its cause — and `bin/check-handoff.sh` validates only `- [ ]` top-level lines in `## Active`, so a flipped line passes the lint unseen. Nothing detects the flip when it is made and nothing reverts it; that repair was a human's. Re-evaluation trigger: if agent frontmatter ever gains per-path write scoping, this instruction-only boundary is re-evaluated against a structural one.
```
<!-- frozen-end: Q2 -->

**Q3** — the replacement for `skills/run/SKILL.md`'s step-6 fast-follow-recording line (a one-for-one whole-line replacement; the three-space indentation is part of the frozen bytes):

<!-- frozen-begin: Q3 -->
```markdown
   - **Fast-follow disposition recording (T-068)**: `codex-reviewer` declares, under `#### Fast-follow disposition` in its verdict, every minor / nit it deferred as a fast-follow with an intended disposition (file-an-issue or won't-fix) rather than requiring fixed before merge — it states intent only and never opens issues or edits the board. **You (the orchestrator) are the actor** who acts on each declared intent at this recording step, before advancing the board to `READY_FOR_MERGE`: for a file-an-issue intent, open the issue (via this repo's normal issue-creation flow, including a user-approval gate where that applies) and capture its number; for a won't-fix intent, capture the reason. Then transcribe the result onto the task's board entry as a sub-bullet anchored `- fast-follow disposition (YYYY-MM-DD): …`, where every finding's disposition is one of this closed set: `filed as issue #N` (with rationale), `waived: <reason>`, or — only when the issue genuinely cannot be filed right now (e.g. awaiting user approval) — a time-bound `pending: <reason> — <deadline>` placeholder (for example `pending: issue filing pending user approval — resolve by close-out`) that you must resolve to `filed as issue #N` or `waived: <reason>` by the task's close-out (a `pending` disposition must never survive close-out — `bin/close-out.sh` fails closed if one remains on the entry). If the reviewer declared `no fast-follow deferrals`, no line is required. The reviewer never edits the board itself — that is a rule it follows, carried in its own board-and-write-boundary Rule in `agents/codex-reviewer.md`, and not a capability it lacks: its `tools:` line grants `Bash`, so it could write the board and does not. Filing and recording are your responsibility, so a deferral the reviewer surfaced can never silently vanish (the T-066 record gap this rule closes).
```
<!-- frozen-end: Q3 -->

**Q4** — the replacement for `skills/goal/SKILL.md`'s step-4 fast-follow-recording line (a one-for-one whole-line replacement; the three-space indentation is part of the frozen bytes):

<!-- frozen-begin: Q4 -->
```markdown
   - **Fast-follow disposition recording (T-068)**: before reporting done at `READY_FOR_MERGE`, act on `codex-reviewer`'s `#### Fast-follow disposition` declaration — the reviewer states intent only (file-an-issue or won't-fix) and never opens issues or edits the board, so **you (the orchestrator) are the actor**: for a file-an-issue intent, open the issue (via this repo's normal issue-creation flow, user-approval gate where it applies) and capture its number; for a won't-fix intent, capture the reason. Transcribe the result onto the task's board entry as a sub-bullet anchored `- fast-follow disposition (YYYY-MM-DD): …`, using the same closed disposition set as `/shell-team:run`'s Review step — `filed as issue #N`, `waived: <reason>`, or a time-bound `pending: <reason> — <deadline>` that must resolve to `filed as issue #N` or `waived: <reason>` before close-out (`pending` must never survive close-out — `bin/close-out.sh` fails closed if one remains). If the reviewer declared `no fast-follow deferrals`, no line is required. The reviewer does not edit the board — a rule it follows, carried in its own board-and-write-boundary Rule in `agents/codex-reviewer.md`, and not a capability it lacks: its `tools:` line grants `Bash`, so it could write the board and does not. Filing and recording are the orchestrator's job, so a surfaced deferral never silently vanishes (the T-066 record gap).
```
<!-- frozen-end: Q4 -->

**R29** — T-1029's board ratification line, appended to that task's `## Done` entry (the two-space indentation is part of the frozen bytes; the line carries no backtick, deliberately, so it survives any quoting style intact):

<!-- frozen-begin: R29 -->
```markdown
  - intent-ratified (2026-08-05): v2→v3 — no per-instance host GO was taken; this class-B re-freeze is recorded on standing authority under three grounds: (i) the sprint v1.6.0 planning GO of 2026-08-05 approved issue #141 inside T-1036's scope, (ii) issue #141 itself prescribes the pm-spec re-freeze route, which is also what T-1029's own pre-commitment R1 requires for a defect that lives in frozen wording, and (iii) this checkout's operator ruling that a freeze or re-freeze proceeds on orchestrator attestation and that no new human gate is invented; the cross-provider review receives this re-freeze as a mandatory disclosure item and keeps the veto over it independently of the other re-freeze in the same task, and the shipped default of a per-instance GO (docs/tuning-oversight.md) is unchanged for adopters — class-B re-freeze confined to a terminology correction at three intent-block lines, the Goal sentence, D3(ii) and the frozen F3 block: each stops categorising the ## Summarized sources declaration line as a member of the frozen region, which D1 deliberately placed it outside of, and instead states it as a site checked in addition to the frozen-region restatement sites; the verification surface is unchanged and stays additive, the declaration line stays mandatory, AC4's pinned scope clause and its own line are untouched, no criterion prose and no check line moved, the intent block still carries ten check lines across eleven criteria, and bin/check-refreeze-class.sh classifies the v2 to v3 delta as class-b at exit status exactly 1 (T-1036 AC7)
```
<!-- frozen-end: R29 -->

**R30** — T-1030's board ratification line, appended to that task's `## Done` entry (same shape, same no-backtick discipline):

<!-- frozen-begin: R30 -->
```markdown
  - intent-ratified (2026-08-05): v1→v2 — no per-instance host GO was taken; this class-B re-freeze is recorded on standing authority under three grounds: (i) the sprint v1.6.0 planning GO of 2026-08-05 approved issue #144 inside T-1036's scope, (ii) issue #144 itself prescribes the pm-spec re-freeze route, which is also what T-1030's own pre-commitment R-1 requires for any change to the frozen F1 text, and (iii) this checkout's operator ruling that a freeze or re-freeze proceeds on orchestrator attestation and that no new human gate is invented; the cross-provider review receives this re-freeze as a mandatory disclosure item and keeps the veto over it independently of the other re-freeze in the same task, and the shipped default of a per-instance GO (docs/tuning-oversight.md) is unchanged for adopters — class-B re-freeze confined to three intent-block lines, the frozen F1 block, D3 and the Input-space reachable-classes bullet: each gains the same bounded carve-out stating that a state or cache directory a tool this role is required to run maintains for itself, the Codex CLI's own CODEX_HOME being the named instance, is not a path this role writes and therefore sits outside the closed enumeration rather than inside it; the enumeration's four members are unchanged, AC4's thirteen pinned strings including Write these paths and no others are untouched, the Goal sentence is deliberately not moved because it makes no everything-else-is-read-only claim and stays literally true, no criterion prose and no check line moved, the intent block still carries eleven check lines across twelve criteria, and bin/check-refreeze-class.sh classifies the v1 to v2 delta as class-b at exit status exactly 1 (T-1036 AC8)
```
<!-- frozen-end: R30 -->

## Non-goals

- **Any change under `bin/`, `tests/`, `templates/`, `docs/`, `.github/`, `README*`, `CHANGELOG*`, `CONTRIBUTING.md` or `CLAUDE.md`** — not one byte. No checker is written, extended or wired, and no test fixture is added or altered. AC12.
- **Editing `.shell-team/lessons.md` or any generated playbook block.** No corpus entry is promoted, superseded, re-scoped or reworded, and the four `playbook-*.md` blocks still regenerate to a zero diff. AC12.
- **Editing any `agents/*.md` other than `agents/qa-verifier.md` and `agents/codex-reviewer.md`**, and any file under `skills/` other than `skills/run/SKILL.md` and `skills/goal/SKILL.md`. AC12.
- **Editing any spec other than T-1029's, T-1030's and this one** — and, within those two, anything outside the lines DP-3 and DP-5 name. No third re-freeze, no vK→vK+2 jump, and no class-M path for either delta: both are class-B and AC7 / AC8 measure the classification rather than asserting it.
- **Rewriting history.** No past board entry, no `.shell-team/reviews/*` record and no `.shell-team/provenance/*` record of any earlier task is edited, including the ones that quote the superseded phrasings. Every negative sweep in this spec is scoped to the four target files and the two edited specs for exactly that reason.
- **Repairing, re-basing, widening or annotating any merge-point-scoped criterion** — T-1029's AC5 / AC7 / AC8 and T-1030's AC5 / AC8 / AC9. Their staleness is declared behaviour in their own text; it is disclosed at the freeze and in the hand-off and never chased, and **no retroactive `- stale-at:` line is added to any of them** (DP-7).
- **Any adjacent "while we're here" fix.** The `## Spec completion self-check` bullets, the spec template, the `- stale-at:` grammar and every other norm T-1035 shipped stay exactly as they are; issue #155's three items are out of contract (P4).
- **Any `.ja.md` counterpart.** All four edited files are agent-read or agent-executed; the repository's language rule gives them no translated copy.
- **Any change to a verdict format, severity vocabulary, status flag, machine token, output heading or exit contract**, in any of the four files.
- **Enforcing either repaired boundary mechanically.** Nothing shipped here detects or prevents a write to a tool state directory, or a QA pass that skipped a restatement site; see Input space.
- **No CHANGELOG entry and no version bump.** Release-cut time writes those.

## Acceptance criteria

Base ref for every `git` anchor is the literal `5a5c09b`, guarded with `git rev-parse --verify --quiet` before use. Every check runs from the repository root, names every file it reads, asserts readability (and, for base-ref reads, that the ref resolves) before any comparison or negative grep, writes only inside a `mktemp -d` scratch directory created with the guarded idiom, reflects producer failures into `rc`, and carries a positive control wherever it counts or greps for an absent condition. Every criterion that pins a frozen text reads its expected bytes out of this file's own `<!-- frozen-begin: X -->` / `<!-- frozen-end: X -->` fenced blocks, fence lines stripped with `sed '1d;$d'`.

- [ ] **AC1** Three-way byte identity for Q1: this spec's `Q1` block, T-1029's re-frozen `F3` block and the live line in `agents/qa-verifier.md` are one and the same physical line. The `Q1` block extracts to exactly one line containing `Verify every summarized source against the source itself`; T-1029's `F3` block extracts to exactly one line; the two `cmp` byte-identical; that line occurs in `agents/qa-verifier.md` **exactly once** as a whole line; and it is the physical line **immediately after** the intervention-record bullet, which is the adjacency T-1029's own AC3 pins and which this task must not disturb. Positive controls: both extractions must yield exactly one line and the intervention-record anchor must be located before any position arithmetic runs, so a mangled extraction or a missing anchor fails rather than matching vacuously. **#157 repair (T-1040):** the intervention-record anchor is now the closed literal `- Read the task's intervention record as a primary input (` matched by an `index($0, anchor)==1` lookup carrying an explicit occurrence-count-of-one assertion, rather than the open `index($0,"- Read the task")==1` prefix paired with a separate unanchored `>0` substring test — a prefix that any bullet beginning "- Read the task" could satisfy regardless of which task-related bullet it actually was.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; S29=.shell-team/specs/T-1029-claim-fidelity-qa-step.md; A=agents/qa-verifier.md; test -r "$SP" || exit 1; test -r "$S29" || exit 1; test -r "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036a.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: Q1 -->' -v e='<!-- frozen-end: Q1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q1"; awk -v b='<!-- frozen-begin: F3 -->' -v e='<!-- frozen-end: F3 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S29" | sed '1d;$d' > "$T/f3"; test "$(grep -c . "$T/q1" || true)" = "1" || rc=1; test "$(grep -c . "$T/f3" || true)" = "1" || rc=1; grep -qF -- 'Verify every summarized source against the source itself' "$T/q1" || rc=1; cmp -s "$T/q1" "$T/f3" || rc=1; test "$(grep -cxFf "$T/q1" "$A" || true)" = "1" || rc=1; anc="- Read the task's intervention record as a primary input ("; c=$(awk -v a="$anc" 'index($0,a)==1{c++} END{print c+0}' "$A"); test "$((10#$c))" = "1" || rc=1; p=$(awk -v a="$anc" 'index($0,a)==1{print NR; exit}' "$A"); test -n "$p" || rc=1; if [ -n "$p" ]; then sed -n "$((10#$p + 1))p" "$A" > "$T/nx"; cmp -s "$T/q1" "$T/nx" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC2** Three-way byte identity for Q2: this spec's `Q2` block, T-1030's re-frozen `F1` block and the live line in `agents/codex-reviewer.md` are one and the same physical line. Both blocks extract to exactly one line, the `Q2` line contains `Board and write boundary`, the two `cmp` byte-identical, that line occurs in `agents/codex-reviewer.md` **exactly once** as a whole line and inside its `## Rules` section, and it is the physical line **immediately after** T-1030's `R1` line — the adjacency that spec's AC1 pins, re-derived here from T-1030's own `R1` frozen block rather than from a transcription. Positive controls: all three extractions must yield exactly one line, the `## Rules` extraction must be non-empty, and `R1` must be located in the file before any position arithmetic runs.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; S30=.shell-team/specs/T-1030-reviewer-board-write-boundary.md; A=agents/codex-reviewer.md; test -r "$SP" || exit 1; test -r "$S30" || exit 1; test -r "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036b.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: Q2 -->' -v e='<!-- frozen-end: Q2 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q2"; awk -v b='<!-- frozen-begin: F1 -->' -v e='<!-- frozen-end: F1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S30" | sed '1d;$d' > "$T/f1"; awk -v b='<!-- frozen-begin: R1 -->' -v e='<!-- frozen-end: R1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S30" | sed '1d;$d' > "$T/r1"; for n in q2 f1 r1; do test "$(grep -c . "$T/$n" || true)" = "1" || rc=1; done; grep -qF -- 'Board and write boundary' "$T/q2" || rc=1; cmp -s "$T/q2" "$T/f1" || rc=1; test "$(grep -cxFf "$T/q2" "$A" || true)" = "1" || rc=1; awk '/^## Rules$/{f=1;next} /^## /{f=0} f' "$A" > "$T/sec"; test -s "$T/sec" || rc=1; grep -qxFf "$T/q2" "$T/sec" || rc=1; p=$(grep -nxFf "$T/r1" "$A" | head -n 1 | cut -d: -f1); test -n "$p" || rc=1; if [ -n "$p" ]; then sed -n "$((10#$p + 1))p" "$A" > "$T/nx"; cmp -s "$T/q2" "$T/nx" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC3** Q3 ships byte-exact in `skills/run/SKILL.md`, exactly once as a whole line, **inside T-1028's AC10 window** — strictly between that file's `6. **Review**` and `7. **Done**` step lines — and that window still contains T-1028's own `F4` line, extracted from T-1028's spec by its frozen block rather than transcribed. The window is the load-bearing clause: a replacement that landed outside it would leave T-1028's live lock red while Q3's own bytes were present. Positive controls: the `Q3` and `F4` extractions must each yield exactly one line, and all four line numbers (`6. **Review**`, `7. **Done**`, Q3, F4) must be found before any comparison runs, so a missing anchor fails rather than passing vacuously.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; S28=.shell-team/specs/T-1028-class-m-refreeze.md; K=skills/run/SKILL.md; test -r "$SP" || exit 1; test -r "$S28" || exit 1; test -r "$K" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036c.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: Q3 -->' -v e='<!-- frozen-end: Q3 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q3"; awk -v b='<!-- frozen-begin: F4 -->' -v e='<!-- frozen-end: F4 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S28" | sed '1d;$d' > "$T/f4"; test "$(grep -c . "$T/q3" || true)" = "1" || rc=1; test "$(grep -c . "$T/f4" || true)" = "1" || rc=1; grep -qF -- 'Fast-follow disposition recording (T-068)' "$T/q3" || rc=1; test "$(grep -cxFf "$T/q3" "$K" || true)" = "1" || rc=1; l3=$(grep -nxFf "$T/q3" "$K" | head -n 1 | cut -d: -f1); l4=$(grep -nxFf "$T/f4" "$K" | head -n 1 | cut -d: -f1); l6=$(awk 'index($0,"6. **Review**")==1{print NR; exit}' "$K"); l7=$(awk 'index($0,"7. **Done**")==1{print NR; exit}' "$K"); for v in "$l3" "$l4" "$l6" "$l7"; do test -n "$v" || rc=1; done; if [ -n "$l3" ] && [ -n "$l4" ] && [ -n "$l6" ] && [ -n "$l7" ]; then test "$((10#$l6))" -lt "$((10#$l3))" || rc=1; test "$((10#$l3))" -lt "$((10#$l7))" || rc=1; test "$((10#$l6))" -lt "$((10#$l4))" || rc=1; test "$((10#$l4))" -lt "$((10#$l7))" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC4** Q4 ships byte-exact in `skills/goal/SKILL.md`, exactly once as a whole line, positioned by its two **immediate physical neighbours** rather than by a line number or by a first-match anchor: the line directly above it is the `Gate **green**` decision item's closing continuation line `     gate).` and the line directly below it carries `loop-guard prints`, so a replacement that drifted out of the step-4 decision list fails here even though its own bytes are present. The neighbours are read by offset from Q4's own located line, never by an independent `grep | head -1`, because neither anchor is unique in that file and a first-match reading would compare against the wrong line. Positive controls: the `Q4` extraction must yield exactly one line, Q4 must be located before any offset arithmetic runs, and both extracted neighbour lines must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; G=skills/goal/SKILL.md; test -r "$SP" || exit 1; test -r "$G" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036d.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: Q4 -->' -v e='<!-- frozen-end: Q4 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q4"; test "$(grep -c . "$T/q4" || true)" = "1" || rc=1; grep -qF -- 'Fast-follow disposition recording (T-068)' "$T/q4" || rc=1; test "$(grep -cxFf "$T/q4" "$G" || true)" = "1" || rc=1; q=$(grep -nxFf "$T/q4" "$G" | head -n 1 | cut -d: -f1); test -n "$q" || rc=1; if [ -n "$q" ]; then sed -n "$((10#$q - 1))p" "$G" > "$T/prev"; sed -n "$((10#$q + 1))p" "$G" > "$T/next"; test -s "$T/prev" || rc=1; test -s "$T/next" || rc=1; grep -qxF -- '     gate).' "$T/prev" || rc=1; grep -qF -- 'loop-guard prints' "$T/next" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC5** Old-phrase extinction **and** the positive declaration-line pin, both scoped to the four target files and the two edited specs and never to the repository, because board entries and `.shell-team/reviews/*` records quote the superseded phrasings as history and this spec quotes them in its own body. Absent everywhere in `agents/qa-verifier.md` and `.shell-team/specs/T-1029-claim-fidelity-qa-step.md`: `the declaration line itself, which is one such site and never the whole set`, `the declaration line, which is one site among them and never the whole set`, and `the declaration line being one of them and never the whole set`. Absent from `skills/run/SKILL.md` and `skills/goal/SKILL.md`: `read-only on the board`. Present as fixed strings in **both** the `Q1` frozen block and — **scoped to Q1's own shipped line, located by whole-line match and extracted, never searched for file-wide** — `agents/qa-verifier.md` (DP-1's positive lock, so a re-narrowing is caught by a requirement and not only by the absence of a phrase): ``in addition to those sites, the `## Summarized sources` declaration line itself``, `always checked alongside them and never in their place`, and the unmoved scope clause `every site in the frozen region that restates, quotes or summarizes those facts`. Present as fixed strings in both the `Q3` and `Q4` blocks and, on the same line-scoped terms, in their respective shipped lines: `board-and-write-boundary Rule` and `not a capability it lacks`. **The line scoping is the point**: a file-wide pin is satisfied by the phrase surviving *anywhere* in the file, so a clause that narrowed the shipped line while leaving the phrase in a neighbouring bullet would pass a whole-file grep — the pin must account the line it names, not the file that contains it. Positive controls: every file must be readable and non-empty, and a phrase guaranteed to survive each edit must still be found in it, so a mistyped path cannot turn "grep found nothing" into a pass.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; A=agents/qa-verifier.md; S29=.shell-team/specs/T-1029-claim-fidelity-qa-step.md; K=skills/run/SKILL.md; G=skills/goal/SKILL.md; for f in "$SP" "$A" "$S29" "$K" "$G"; do test -s "$f" || exit 1; done; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036e.XXXXXX") || exit 1; grep -qF -- 'Adversarial fixture synthesis checklist' "$A" || rc=1; grep -qF -- '**Task ID**: T-1029' "$S29" || rc=1; grep -qF -- 'Fast-follow disposition recording (T-068)' "$K" || rc=1; grep -qF -- 'Fast-follow disposition recording (T-068)' "$G" || rc=1; for s in 'the declaration line itself, which is one such site and never the whole set' 'the declaration line, which is one site among them and never the whole set' 'the declaration line being one of them and never the whole set'; do test "$(grep -cF -- "$s" "$A" || true)" = "0" || rc=1; test "$(grep -cF -- "$s" "$S29" || true)" = "0" || rc=1; done; test "$(grep -cF -- 'read-only on the board' "$K" || true)" = "0" || rc=1; test "$(grep -cF -- 'read-only on the board' "$G" || true)" = "0" || rc=1; awk -v b='<!-- frozen-begin: Q1 -->' -v e='<!-- frozen-end: Q1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q1"; test "$(grep -c . "$T/q1" || true)" = "1" || rc=1; q1ln=$(grep -nxFf "$T/q1" "$A" | head -n 1 | cut -d: -f1); test -n "$q1ln" || rc=1; if [ -n "$q1ln" ]; then sed -n "$((10#$q1ln))p" "$A" > "$T/q1live"; test -s "$T/q1live" || rc=1; for s in 'in addition to those sites, the `## Summarized sources` declaration line itself' 'always checked alongside them and never in their place' 'every site in the frozen region that restates, quotes or summarizes those facts'; do grep -qF -- "$s" "$T/q1" || rc=1; grep -qF -- "$s" "$T/q1live" || rc=1; done; fi; for pair in "Q3 $K" "Q4 $G"; do set -- $pair; N=$1; F=$2; awk -v b="<!-- frozen-begin: $N -->" -v e="<!-- frozen-end: $N -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/blk"; test "$(grep -c . "$T/blk" || true)" = "1" || rc=1; bln=$(grep -nxFf "$T/blk" "$F" | head -n 1 | cut -d: -f1); test -n "$bln" || rc=1; if [ -n "$bln" ]; then sed -n "$((10#$bln))p" "$F" > "$T/blive"; test -s "$T/blive" || rc=1; for s in 'board-and-write-boundary Rule' 'not a capability it lacks'; do grep -qF -- "$s" "$T/blk" || rc=1; grep -qF -- "$s" "$T/blive" || rc=1; done; fi; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC6** T-1030's own AC4 contract survives the re-freeze untouched, re-derived here rather than trusted, **and** the new carve-out is machine-visible. All thirteen of that criterion's pinned fixed strings appear in both the `Q2` frozen block and **Q2's own shipped line**, the latter located by whole-line match and extracted rather than searched for file-wide, for the reason AC5 states; its three occurrence counts remain genuinely **file**-scoped, because file-wide occurrence is exactly what T-1030's AC4 defines them to be — a count is an accounting over the file, a presence pin is an accounting over one line, and each is scoped to its own subject. Those three counts in that file are unchanged (`Board and write boundary` exactly once, `a rule you follow, not a capability you lack` exactly four times, `board-and-write-boundary Rule` exactly three times), which is the machine half of "the carve-out is additive and moved no other line"; and three new carve-out strings appear in both the block and the file — `a state or cache directory that a tool this role is required to run maintains for itself`, `$CODEX_HOME`, and `never a licence to write there deliberately`. Machine-token hygiene is re-asserted: the `Q2` block carries zero occurrences of `APPROVE` and zero of `REQUEST_CHANGES`. This criterion is the revert-detection lock for the carve-out: an edit that drops the bounding to *required* tools, the named instance, or the no-licence clause turns it red while AC2's byte equality would go red only for the symmetric case. Positive control: the `Q2` extraction must yield exactly one line before any string is sought in it.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; A=agents/codex-reviewer.md; test -r "$SP" || exit 1; test -s "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036f.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: Q2 -->' -v e='<!-- frozen-end: Q2 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q2"; test "$(grep -c . "$T/q2" || true)" = "1" || rc=1; for s in 'Board and write boundary' 'a rule you follow, not a capability you lack' 'team-paths.sh --get reviews' 'team-paths.sh --get todo' 'Write these paths and no others' 'you never edit the board' 'You run no mutating git subcommand' 'with exactly one exception' 'commit that one file and nothing else in the same commit' '9d82a49' 'contains no detect-and-revert logic' 'passes the lint unseen' 'if agent frontmatter ever gains per-path write scoping' 'a state or cache directory that a tool this role is required to run maintains for itself' '$CODEX_HOME' 'never a licence to write there deliberately'; do grep -qF -- "$s" "$T/q2" || rc=1; done; q2ln=$(grep -nxFf "$T/q2" "$A" | head -n 1 | cut -d: -f1); test -n "$q2ln" || rc=1; if [ -n "$q2ln" ]; then sed -n "$((10#$q2ln))p" "$A" > "$T/q2live"; test -s "$T/q2live" || rc=1; for s in 'Board and write boundary' 'a rule you follow, not a capability you lack' 'team-paths.sh --get reviews' 'team-paths.sh --get todo' 'Write these paths and no others' 'you never edit the board' 'You run no mutating git subcommand' 'with exactly one exception' 'commit that one file and nothing else in the same commit' '9d82a49' 'contains no detect-and-revert logic' 'passes the lint unseen' 'if agent frontmatter ever gains per-path write scoping' 'a state or cache directory that a tool this role is required to run maintains for itself' '$CODEX_HOME' 'never a licence to write there deliberately'; do grep -qF -- "$s" "$T/q2live" || rc=1; done; fi; test "$(grep -cF -- 'Board and write boundary' "$A" || true)" = "1" || rc=1; test "$(grep -cF -- 'a rule you follow, not a capability you lack' "$A" || true)" = "4" || rc=1; test "$(grep -cF -- 'board-and-write-boundary Rule' "$A" || true)" = "3" || rc=1; test "$(grep -cF -- 'APPROVE' "$T/q2" || true)" = "0" || rc=1; test "$(grep -cF -- 'REQUEST_CHANGES' "$T/q2" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC7** T-1029's re-freeze of the lines DP-3 names is class-B, measured rather than asserted. **Invariance principle, binding on every accounting clause in this spec: a criterion here describes the shipped final state and never how many ratified repairs produced it.** A task's ledger history — how many versions it has reached, how many attestations it has accumulated, how many lines its spec file has gained since a base ref — is `bin/check-intent.sh`'s domain and is delegated to AC11. A clause that counts repairs falsifies itself the moment a legitimate repair lands, and this spec measured that coupling three times before the class was named (a population-blind global count, then an own-version-shaped count, then a repair-count delta). **No spec-file line-count literal is pinned here, and none may be re-added.** What is asserted instead is stable under any number of further repairs: the intent block still carries exactly **ten** `- check:` lines and the file still carries exactly **eleven** column-0 acceptance-criterion bullets, so no criterion was added, removed or silently de-recognised however often its prose is repaired; the re-frozen `F3` block is byte-identical to this spec's `Q1` block (AC1's other half, re-asserted here so the class-B claim and the shipped bytes cannot diverge); and `bin/check-refreeze-class.sh`, run over the materialised base blob and the current file with both positional arguments, exits with status **exactly 1** — not 0, which would mean the delta was confined to `- check:` lines, and not 2, which would mean the classifier could not evaluate the pair. That verdict is itself repair-agnostic: the comparison is always current-bytes-against-base, so it stays class-b whatever sequence of ratifications produced the current bytes. **Merge-point-scoped and expected to go stale after merge** — once later work lands on `5a5c09b`, a diff against it no longer describes this task; do not widen its base-ref resolution and do not re-derive it per rework round. Positive controls: the base ref must resolve and the base blob must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; S29=.shell-team/specs/T-1029-claim-fidelity-qa-step.md; test -r "$SP" || exit 1; test -r "$S29" || exit 1; test -r bin/check-refreeze-class.sh || exit 1; git rev-parse --verify --quiet '5a5c09b^{commit}' >/dev/null || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036g.XXXXXX") || exit 1; nchk=$(awk -v b='<!-- BEGIN intent-block: T-1029 -->' -v e='<!-- END intent-block: T-1029 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S29" | grep -cE '^[[:space:]]+- check:' || true); test "$nchk" = "10" || rc=1; nac=$(grep -cE '^- \[[ xX]\] \*\*AC' "$S29" || true); test "$nac" = "11" || rc=1; awk -v b='<!-- frozen-begin: Q1 -->' -v e='<!-- frozen-end: Q1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q1"; awk -v b='<!-- frozen-begin: F3 -->' -v e='<!-- frozen-end: F3 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S29" | sed '1d;$d' > "$T/f3"; test "$(grep -c . "$T/q1" || true)" = "1" || rc=1; cmp -s "$T/q1" "$T/f3" || rc=1; git show "5a5c09b:$S29" > "$T/old.md" || rc=1; test -s "$T/old.md" || rc=1; bash bin/check-refreeze-class.sh -- "$T/old.md" "$S29" >/dev/null 2>&1; ec=$?; test "$ec" = "1" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC8** T-1030's re-freeze of the lines DP-5 names is class-B, measured rather than asserted, on AC7's invariance terms exactly — **no spec-file line-count literal is pinned, and none may be re-added.** DP-5's 1:1 promotion of the new directive is therefore asserted **by the row's own presence rather than by a diff asymmetry**: T-1030's Body-to-AC correspondence table carries the carve-out row exactly once, matched **as a table row by anchored `| ` prefix at column 1** rather than as a bare substring anywhere in the file — so a prose mention of the same sentence cannot stand in for the row, which a substring count would have accepted — and a re-freeze that changed `F1` while leaving the directive unpromoted fails here — and keeps failing however many later repairs that spec receives, which the superseded "four added, three deleted" form would not have. Its intent block still carries exactly **eleven** `- check:` lines and the file still carries exactly **twelve** column-0 acceptance-criterion bullets; the re-frozen `F1` block is byte-identical to this spec's `Q2` block; and `bin/check-refreeze-class.sh` over the materialised base blob and the current file exits with status **exactly 1**. **Merge-point-scoped and expected to go stale after merge**, as AC7. Positive controls: the base ref must resolve, the base blob must be non-empty, and the `Q2` extraction must yield exactly one line before any comparison runs. **#157 repair (T-1040):** the carve-out row is now matched by its **whole first cell, including its closing delimiter**, rather than by a prefix that stopped mid-cell — a row whose cell text was truncated or reworded partway through could otherwise still satisfy the old, shorter anchor.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; S30=.shell-team/specs/T-1030-reviewer-board-write-boundary.md; test -r "$SP" || exit 1; test -r "$S30" || exit 1; test -r bin/check-refreeze-class.sh || exit 1; git rev-parse --verify --quiet '5a5c09b^{commit}' >/dev/null || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036h.XXXXXX") || exit 1; anc='| One bounded carve-out: a state or cache directory a *required* tool maintains for itself sits outside the enumeration rather than inside it (T-1036, issue #144 — a strictly literal reading of the closed list made the mandatory `codex exec` a violation) |'; nrow=$(awk -v a="$anc" 'index($0,a)==1{n++} END{print n+0}' "$S30"); test "$((10#$nrow))" = "1" || rc=1; nchk=$(awk -v b='<!-- BEGIN intent-block: T-1030 -->' -v e='<!-- END intent-block: T-1030 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S30" | grep -cE '^[[:space:]]+- check:' || true); test "$nchk" = "11" || rc=1; nac=$(grep -cE '^- \[[ xX]\] \*\*AC' "$S30" || true); test "$nac" = "12" || rc=1; awk -v b='<!-- frozen-begin: Q2 -->' -v e='<!-- frozen-end: Q2 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/q2"; awk -v b='<!-- frozen-begin: F1 -->' -v e='<!-- frozen-end: F1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$S30" | sed '1d;$d' > "$T/f1"; test "$(grep -c . "$T/q2" || true)" = "1" || rc=1; cmp -s "$T/q2" "$T/f1" || rc=1; git show "5a5c09b:$S30" > "$T/old.md" || rc=1; test -s "$T/old.md" || rc=1; bash bin/check-refreeze-class.sh -- "$T/old.md" "$S30" >/dev/null 2>&1; ec=$?; test "$ec" = "1" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC9** The board carries both ledger updates in DP-8's shape and **exactly two deletions, individually identified**. The deletion column of `git diff --numstat 5a5c09b` over the resolved board is exactly `2`; the removed lines, re-derived independently with `git diff -U0` (never a `^-[^-]` count), are exactly the two superseded hash records — `  - intent-hash (v2): aa47eaa0525af8a73cf89b9b3655b310e62bdadb` and `  - intent-hash (v1): d0f73437565c9211c998e78a863948eb23e27972` — each cross-checked as present in the base blob and absent from the working board. The `R29` and `R30` frozen lines each occur on the board exactly once. Hash records are counted **base-relative, by anchored record shape, and deliberately with no per-version clause at all**. Only a *delta* against the base blob expresses replace-not-append on a board whose `## Done` history already carries dozens of ledger records, and only the anchored form `^  - intent-hash \(vN\): <40-hex>$` counts records rather than mentions, since several freeze notes quote the token in prose. The whole accounting is therefore one clause: **the anchored record-shape total across every version grows by exactly one** against the base blob. That clause is **invariant under this task's own intent-block version** — each of the two re-freezes replaces rather than appends and so contributes nothing to the total, and T-1036 contributes exactly one record whatever version it is at. A clause counting `(v2)`- or `(v3)`-shaped records cannot have that property, and the reason is measured rather than predicted: repairing *this very criterion* bumps T-1036's own version and re-shapes T-1036's own record, so the v1→v2 repair turned its own "(v2)-shaped count unchanged" clause red the instant its ratification landed. A criterion that falsifies itself every time it is fixed is not a strict criterion, it is a self-reference loop, and it is removed here rather than re-tuned. **Per-task hash uniqueness and the v1..vN chain — for T-1029's v3, T-1030's v2 and T-1036's own record alike — are `bin/check-intent.sh`'s authority**, re-run live by AC11 over all three; they are deliberately not re-proved here, so this criterion carries the population delta and nothing more. Anti-vacuity floor, **not a measurement**: the base blob's anchored record total must be at least 20, so a regex that went dead against the real ledger cannot satisfy the delta arithmetic by counting nothing on both sides. Attestations accumulate rather than replace, asserted **per owning task entry, by subset survival, and never by a count or a board-wide grep**: for each of T-1029 and T-1030, that task's own `## Done` entry region is extracted — from its top-level `- [` line bearing its id to the next top-level `- [` line — from **both** the base blob and the working board, and **every** `- freeze-attestation (` line in the base entry must still be present verbatim in the work entry. Deriving the expected lines from the base blob rather than transcribing them is what removes the last literal; scoping to the entry is what makes the clause account anything at all. A board-wide grep for a transcribed attestation literal is **measurably** vacuous here: the string `freeze-attestation (v1, 2026-08-04): lines=10/10` occurs under T-1029's entry *and* under an unrelated already-merged task's, so deleting T-1029's real record would still satisfy a whole-board search — reproduced by a cross-provider round against the live board, not hypothesised. AC11's `bin/check-intent.sh` remains the task-scoped backstop, but a clause that presents itself as affirmative proof must be sound on its own rather than lean on another criterion. *How many* attestations each task ends with is `bin/check-intent.sh`'s business, exercised live by AC11 over both re-frozen tasks and this one, and that checker requires one conformant attestation for **every** version v1..vN for whatever N each task reaches — a `+1` count-delta clause here would instead pin the number of ratified repairs and go red on the next legitimate one, which is the coupling class AC7's invariance principle forbids. T-1036's own entry is a pure insertion. **Merge-point-scoped and expected to go stale after merge**, as AC7. Positive controls: the base ref must resolve, the base blob must be non-empty, and the removed-line extraction must be non-empty. **#157 repair (T-1040):** each per-task entry selector (T-1029, T-1030) now carries an explicit occurrence-count-of-one assertion against its own `- [` heading line, applied on both the base blob and the working board **before** either entry region is extracted, so a duplicated top-level heading for the same task id cannot silently select the wrong occurrence.
  - check: rc=0; SP=.shell-team/specs/T-1036-wording-batch-141-143-144.md; test -r "$SP" || exit 1; git rev-parse --verify --quiet '5a5c09b^{commit}' >/dev/null || exit 1; B=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$B" || exit 1; test -r "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036i.XXXXXX") || exit 1; git show "5a5c09b:$B" > "$T/base" || rc=1; test -s "$T/base" || rc=1; del=$(git diff --numstat 5a5c09b -- "$B" | awk '{print $2}'); test "$del" = "2" || rc=1; git diff -U0 5a5c09b -- "$B" | grep '^-' | grep -v '^---' | sed 's/^-//' > "$T/removed"; test "$(grep -c . "$T/removed" || true)" = "2" || rc=1; for h in '  - intent-hash (v2): aa47eaa0525af8a73cf89b9b3655b310e62bdadb' '  - intent-hash (v1): d0f73437565c9211c998e78a863948eb23e27972'; do grep -qxF -- "$h" "$T/removed" || rc=1; grep -qxF -- "$h" "$T/base" || rc=1; test "$(grep -cxF -- "$h" "$B" || true)" = "0" || rc=1; done; for n in R29 R30; do awk -v b="<!-- frozen-begin: $n -->" -v e="<!-- frozen-end: $n -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$n"; test "$(grep -c . "$T/$n" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/$n" "$B" || true)" = "1" || rc=1; done; nb=$(grep -cE '^  - intent-hash \(v[0-9]+\): [0-9a-f]{40}$' "$T/base" || true); nw=$(grep -cE '^  - intent-hash \(v[0-9]+\): [0-9a-f]{40}$' "$B" || true); test "$((10#$nb))" -ge 20 || rc=1; test "$((10#$nw))" = "$((10#$nb + 1))" || rc=1; for tid in T-1029 T-1030; do cbase=$(awk -v t="**$tid**" 'index($0,"- [")==1 && index($0,t)>0 {c++} END{print c+0}' "$T/base"); test "$((10#$cbase))" = "1" || rc=1; cwork=$(awk -v t="**$tid**" 'index($0,"- [")==1 && index($0,t)>0 {c++} END{print c+0}' "$B"); test "$((10#$cwork))" = "1" || rc=1; awk -v t="**$tid**" 'index($0,"- [")==1 && index($0,t)>0 {f=1;next} f && index($0,"- [")==1 {exit} f' "$T/base" | grep -F -- '- freeze-attestation (' > "$T/ab" || true; awk -v t="**$tid**" 'index($0,"- [")==1 && index($0,t)>0 {f=1;next} f && index($0,"- [")==1 {exit} f' "$B" | grep -F -- '- freeze-attestation (' > "$T/aw" || true; test -s "$T/ab" || rc=1; test -s "$T/aw" || rc=1; while IFS= read -r al; do [ -n "$al" ] || continue; grep -qxF -- "$al" "$T/aw" || rc=1; done < "$T/ab"; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC10** Both re-frozen specs are measured against their own criteria rather than assumed, with the honest partition declared here so a surprise is itself a finding. `bin/check-acs.sh` over T-1029's spec produces exactly **eleven** verdict lines, exits 0 or 1 (an exit of 2 means the checker could not evaluate the spec and is never a pass), reports `SKIP` for its AC11, reports **PASS for its AC1, AC2, AC3, AC4, AC6, AC9 and AC10**, and every criterion it reports as FAIL is one of **its AC5, AC7, AC8** — the three that resolve the superseded ref `2570d68` and that its own text declares merge-point-scoped. `bin/check-acs.sh` over T-1030's spec produces exactly **twelve** verdict lines, exits 0 or 1, reports `SKIP` for its AC12, reports **PASS for its AC1, AC2, AC3, AC4, AC6, AC7, AC10 and AC11**, and every FAIL is one of **its AC5, AC8, AC9** — the three that resolve `53955c5`, of which AC9 is red **by design** because #143 edits the `skills/` tree it locks. A red anywhere outside those two permitted sets is a defect in this task's repair; a red inside them is disclosed and never chased, re-based or annotated.
  - check: rc=0; S29=.shell-team/specs/T-1029-claim-fidelity-qa-step.md; S30=.shell-team/specs/T-1030-reviewer-board-write-boundary.md; test -r "$S29" || exit 1; test -r "$S30" || exit 1; test -r bin/check-acs.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036j.XXXXXX") || exit 1; bash bin/check-acs.sh "$S29" > "$T/o29.txt" 2>&1; e29=$?; test "$e29" -le 1 || rc=1; v29=$(grep -cE '^AC[0-9]+: (PASS|FAIL|SKIP)' "$T/o29.txt" || true); test "$v29" = "11" || rc=1; grep -qE '^AC11: SKIP' "$T/o29.txt" || rc=1; for n in 1 2 3 4 6 9 10; do grep -qE "^AC$n: PASS" "$T/o29.txt" || rc=1; done; grep -E '^AC[0-9]+: FAIL' "$T/o29.txt" | sed -E 's/^AC([0-9]+): .*/\1/' > "$T/f29.txt"; while IFS= read -r n; do case "$n" in ''|5|7|8) : ;; *) rc=1 ;; esac; done < "$T/f29.txt"; bash bin/check-acs.sh "$S30" > "$T/o30.txt" 2>&1; e30=$?; test "$e30" -le 1 || rc=1; v30=$(grep -cE '^AC[0-9]+: (PASS|FAIL|SKIP)' "$T/o30.txt" || true); test "$v30" = "12" || rc=1; grep -qE '^AC12: SKIP' "$T/o30.txt" || rc=1; for n in 1 2 3 4 6 7 10 11; do grep -qE "^AC$n: PASS" "$T/o30.txt" || rc=1; done; grep -E '^AC[0-9]+: FAIL' "$T/o30.txt" | sed -E 's/^AC([0-9]+): .*/\1/' > "$T/f30.txt"; while IFS= read -r n; do case "$n" in ''|5|8|9) : ;; *) rc=1 ;; esac; done < "$T/f30.txt"; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC11** Every frozen ledger on this stacked train still validates against the edited board: `bin/check-intent.sh` reports aligned (exit 0) for both re-frozen specs at their new versions and for all five stacked neighbours — `.shell-team/specs/T-1029-claim-fidelity-qa-step.md`, `.shell-team/specs/T-1030-reviewer-board-write-boundary.md`, `.shell-team/specs/T-1024-check-line-mktemp-guard.md`, `.shell-team/specs/T-1027-promote-retro-2026-08-04.md`, `.shell-team/specs/T-1033-promote-retro-2026-08-05.md`, `.shell-team/specs/T-1034-refreeze-hardening-execbit.md`, `.shell-team/specs/T-1035-spec-template-staleness-locks.md` — and for this spec itself, against the board resolved through `bin/team-paths.sh --get todo`. A board edit that damaged a neighbour's `- intent-hash` sub-bullet, or a re-freeze whose new hash was recorded wrong, is caught here rather than at the next task's gate. Positive controls: the resolved board path must be non-empty and readable, and each spec must be readable.
  - check: rc=0; B=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$B" || exit 1; test -r "$B" || exit 1; test -r bin/check-intent.sh || exit 1; for s in .shell-team/specs/T-1029-claim-fidelity-qa-step.md .shell-team/specs/T-1030-reviewer-board-write-boundary.md .shell-team/specs/T-1024-check-line-mktemp-guard.md .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/specs/T-1033-promote-retro-2026-08-05.md .shell-team/specs/T-1034-refreeze-hardening-execbit.md .shell-team/specs/T-1035-spec-template-staleness-locks.md .shell-team/specs/T-1036-wording-batch-141-143-144.md; do test -r "$s" || rc=1; bash bin/check-intent.sh "$s" "$B" >/dev/null || rc=1; done; test "$rc" -eq 0
- [ ] **AC12** Preservation locks, asserted by name rather than only by AC13's allow-list. Byte-identical to the base ref: every `agents/*.md` other than `agents/qa-verifier.md` and `agents/codex-reviewer.md`; every file under `skills/` other than `skills/run/SKILL.md` and `skills/goal/SKILL.md`, re-derived by enumerating the base ref's own `skills/` tree rather than trusting a directory diff; `.shell-team/lessons.md`; and every path under `bin/`, `tests/`, `templates/`, `docs/` and `.github/`, with neither a tracked diff nor an untracked addition. Live and independent of any base-ref comparison: `bash bin/check-prompt-sync.sh` is green over the whole registry; the four generated `playbook-*.md` blocks regenerate to a zero diff in a scratch tree (the CI dogfood's own procedure, re-run here, which is what proves the `agents/qa-verifier.md` edit stayed outside its marker regions); and `bash tests/codex-skeleton-hygiene/run.sh` is green, which is the suite carrying the two live-file regex locks over three of this task's four target files and which T-1028's AC10 also requires. **The base-ref comparison half is merge-point-scoped and expected to go stale after merge**, as AC7; the three live checks are not. Positive controls: the base ref must resolve, a base-ref blob must resolve, the base `skills/` enumeration must yield at least five files, and the registry must yield exactly eight generated-block files.
  - check: rc=0; git rev-parse --verify --quiet '5a5c09b^{commit}' >/dev/null || exit 1; git cat-file -e '5a5c09b:agents/engineer.md' || exit 1; test -r templates/prompt-blocks/registry.txt || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1036k.XXXXXX") || exit 1; for f in agents/engineer.md agents/pm-spec.md agents/tech-lead.md agents/scrum-master.md agents/ui-designer.md agents/triage-orchestrator.md agents/drift-evaluator.md .shell-team/lessons.md; do test -r "$f" || rc=1; d=$(git diff --name-only 5a5c09b -- "$f"); test -z "$d" || rc=1; done; git ls-tree -r --name-only 5a5c09b skills/ > "$T/skills.txt" || rc=1; test "$(grep -c . "$T/skills.txt" || true)" -ge 5 || rc=1; while IFS= read -r p; do case "$p" in ''|skills/run/SKILL.md|skills/goal/SKILL.md) continue;; esac; test -r "$p" || rc=1; git show "5a5c09b:$p" > "$T/blob" || rc=1; cmp -s "$T/blob" "$p" || rc=1; done < "$T/skills.txt"; u=$(git ls-files --others --exclude-standard -- skills); test -z "$u" || rc=1; for d in bin tests templates docs .github; do test -d "$d" || rc=1; x=$(git diff --name-only 5a5c09b -- "$d"); test -z "$x" || rc=1; y=$(git ls-files --others --exclude-standard -- "$d"); test -z "$y" || rc=1; done; L=$(bash bin/team-paths.sh --get lessons) || exit 1; test -r "$L" || exit 1; S=$(mktemp -d "${TMPDIR:-/tmp}/t1036kk.XXXXXX") || exit 1; files=$(awk '$1 == "marker" && $2 ~ /^playbook-/ { for (i = 3; i <= NF; i++) print $i; print "templates/prompt-blocks/" $2 }' templates/prompt-blocks/registry.txt | sort -u); test "$(printf '%s\n' "$files" | grep -c . || true)" = "8" || rc=1; for f in $files; do mkdir -p "$S/$(dirname "$f")" || rc=1; cp "$f" "$S/$f" || rc=1; done; mkdir -p "$S/templates/prompt-blocks" || rc=1; cp templates/prompt-blocks/registry.txt "$S/templates/prompt-blocks/registry.txt" || rc=1; bash bin/gen-playbook-blocks.sh --root "$S" --lessons "$L" >/dev/null 2>&1 || rc=1; for f in $files; do cmp -s "$f" "$S/$f" || rc=1; done; rm -rf "$S"; bash bin/check-prompt-sync.sh >/dev/null || rc=1; bash tests/codex-skeleton-hygiene/run.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC13** Scope, records and board hygiene lock. The branch's changed-and-added file set (tracked changes ∪ untracked additions, since this spec and the records are untracked at freeze time) contains the four shipped files, the two re-frozen specs, this spec and the board, and **nothing** outside the eleven-path allow-list in Notes for engineer; `.shell-team/provenance/T-1036.md` and `.shell-team/interventions/T-1036.md` both exist and are conformant under their own checkers; the board passes the hand-off lint and the structural heading check against the base ref; and the branch diff is clean under `bin/check-pii-shapes.sh`. Positive control: the base ref must resolve and the changed set must be non-empty. **Merge-point-scoped and expected to go stale after merge**, as AC7.
  - check: rc=0; git rev-parse --verify --quiet '5a5c09b^{commit}' >/dev/null || exit 1; changed=$( { git diff --name-only 5a5c09b; git ls-files --others --exclude-standard; } | sort -u ); test -n "$changed" || rc=1; for f in agents/qa-verifier.md agents/codex-reviewer.md skills/run/SKILL.md skills/goal/SKILL.md .shell-team/specs/T-1029-claim-fidelity-qa-step.md .shell-team/specs/T-1030-reviewer-board-write-boundary.md .shell-team/specs/T-1036-wording-batch-141-143-144.md .shell-team/todo.md; do printf '%s\n' "$changed" | grep -qxF -- "$f" || rc=1; done; extra=$(printf '%s\n' "$changed" | grep -vxF -e 'agents/qa-verifier.md' -e 'agents/codex-reviewer.md' -e 'skills/run/SKILL.md' -e 'skills/goal/SKILL.md' -e '.shell-team/specs/T-1029-claim-fidelity-qa-step.md' -e '.shell-team/specs/T-1030-reviewer-board-write-boundary.md' -e '.shell-team/specs/T-1036-wording-batch-141-143-144.md' -e '.shell-team/todo.md' -e '.shell-team/provenance/T-1036.md' -e '.shell-team/reviews/T-1036.md' -e '.shell-team/interventions/T-1036.md' | grep -c . || true); test "$extra" = "0" || rc=1; if [ -r .shell-team/provenance/T-1036.md ]; then bash bin/check-provenance.sh .shell-team/provenance/T-1036.md >/dev/null || rc=1; else rc=1; fi; if [ -r .shell-team/interventions/T-1036.md ]; then bash bin/check-interventions.sh --task T-1036 -- .shell-team/interventions/T-1036.md >/dev/null || rc=1; else rc=1; fi; B=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$B" || exit 1; bash bin/check-handoff.sh "$B" >/dev/null || rc=1; bash bin/check-board-headings.sh "$B" --base 5a5c09b >/dev/null || rc=1; bash bin/check-pii-shapes.sh --base 5a5c09b >/dev/null || rc=1; test "$rc" -eq 0
- [ ] **AC14** Runtime, `SKIP` by design — no command can prove that a command was run, that a document was opened, or that a mutation was observed red and then restored. Five things are done and reported in the board hand-off. **(a)** Every applicable step in `.github/workflows/check-handoff.yml` was run locally, in workflow order, each step declared inapplicable named individually with its reason — and any step that enumerates a directory or a glob is **run rather than reasoned about**, because this task adds files under `.shell-team/provenance/`, `.shell-team/reviews/` and `.shell-team/interventions/`. **(b)** A producer-run mutation self-check on a scratch `git worktree` outside this checkout, each mutation observed red then restored and observed green: change one word inside Q1 in `agents/qa-verifier.md` alone (AC1 red — the equality lock); re-narrow Q1's declaration clause back to the v2 apposition symmetrically in both this spec's `Q1` block and T-1029's `F3` block and the agent file (AC5 red on the positive pins and on the extinction sweep, AC1 green in the symmetric case — the revert-detection lock DP-1 requires, and the reason the pins are positive rather than only negative); delete the carve-out's bounding phrase `required to run` from Q2 in the agent file (AC6 red, AC2 red); move Q3 out of the `6. **Review**`/`7. **Done**` window in `skills/run/SKILL.md` (AC3 red on the window clause alone, Q3's own bytes still present, and T-1028's own AC10 red — the cross-task lock this task must not break); revert T-1029's `F3` block to its base-ref bytes while leaving the rest of that spec's re-frozen delta in place (AC7 red on its `Q1`↔`F3` byte-identity clause and AC1 red on the three-way identity, while AC7's class-b clause stays **green** — the remaining delta still classifies as class-b, which is exactly why the identity clause and not the classifier verdict is what pins the shipped bytes); add one unrelated line to `agents/codex-reviewer.md` (T-1030's own AC5 whole-file lock red while this spec's AC2 and AC6 stay green); delete one of the two board hash records without replacing it (AC9 red on the deletion count and on the per-record identification); **and the two anchoring regression locks, each replaying a false-pass a cross-provider round reproduced live against the superseded unanchored forms** — delete the `Body-to-AC 1:1 promotion check` bullet from a scratch copy of `agents/pm-spec.md` while leaving another bullet's cross-reference to that phrase intact (T-1029's own AC2 must go **red** on the anchored bullet-head lookup; the superseded bare-substring form reported PASS), and delete T-1029's own `freeze-attestation (v1, …)` line from a scratch copy of the board while an unrelated merged task's byte-identical attestation line survives elsewhere on it (AC9 must go **red** on the entry-scoped subset survival; the superseded board-wide grep could not see the collision). **(c)** The relayed claims listed in Assumptions were measured on the host before the freeze and the hand-off states the measured value beside each — in particular whether `5a5c09b` is the real branch point, and whether the two superseded hash literals AC9 transcribes match the base blob byte for byte. **(d)** A per-source report against this spec's `## Summarized sources`, one line per named document, naming the sites checked — including the two negative findings DP-3 records (T-1029's F2 and its Input-space bullet carry no defect), which are claims about a source's bytes and must be re-derived rather than inherited. The three GitHub issues are expected to end as `environmentally-unverified` for any role without network access, and that record — not silence — is the correct outcome. **(e)** Both re-freezes are surfaced to the cross-provider round as **two separate mandatory disclosure items**, each naming its target spec, its version transition, its GO grounds and its measured `check-refreeze-class.sh` exit status, so the reviewer can veto one without implicating the other (P2).

## Input space

**Reachable input classes** — what this change's readers and its criteria really encounter, and what the edit must handle correctly:

- The four shipped files as committed at `5a5c09b`: UTF-8 markdown, LF endings, `## ` sections, very long single-physical-line bullets, three-space-indented sub-bullets inside numbered step lists (`skills/*/SKILL.md`), fenced blocks whose bullets look like rules but are template text (`agents/codex-reviewer.md`'s `## Output`), two Japanese-language prose bullets in that same file, `marker`-mode prompt-block regions in `agents/qa-verifier.md` beginning at line 81, and registry `contain` obligations interleaved through all four.
- The two spec files as committed at `5a5c09b`: intent blocks delimited by `<!-- BEGIN/END intent-block: T-NNN -->`, frozen texts delimited by `<!-- frozen-begin: X -->` / `<!-- frozen-end: X -->` fences, criterion bullets anchored at column 0, `- check:` sub-bullets at one indentation level, and mutable sections (Body-to-AC correspondence, Assumptions, Notes) outside the markers.
- The board as committed at `5a5c09b`: `## Active` empty, `## Done` carrying entries whose every line begins with `- ` or an indented `- `, ledger sub-bullets in three shapes (`intent-hash`, `freeze-attestation`, `intent-ratified`), and a file large enough that a whole-file read is not a sensible verification step — which is why AC9 reads `git diff --numstat`'s columns and a `-U0` extraction rather than scanning the file.
- The real runtime situations the two repaired texts govern: a QA round reading a spec whose `## Summarized sources` section is populated, whose restatement sites number zero to a handful, and whose declaration line sits above the intent-block marker; and a review round in which `codex exec` writes the Codex CLI's own state directory as an unrequested side effect while the role writes its review record, its canonical raw captures, its `--alloc` per-invocation raws and `$TMPDIR` scratch.
- Both re-freeze deltas as `bin/check-refreeze-class.sh` sees them: two normalised intent blocks of **equal line count** with prose differing at three indices in each case and no `- check:` line moving — the input shape that classifies as `class-b` by that script's rule (iii) rather than by its line-count rule (i).
- `git` plumbing output for the literal `5a5c09b`: `show <ref>:<path>`, `ls-tree -r --name-only`, `diff --name-only`, `diff --numstat`, `diff -U0`, `cat-file -e`, `rev-parse --verify`, `ls-files --others --exclude-standard`.
- The three resolver-based paths the criteria use (`team-paths.sh --get todo` / `--get lessons`) across the layouts `team-paths.sh` supports: the `.shell-team/` default, the legacy `tasks/` layout, and a `$TEAM_RUN_BASE` override.

**Out-of-scope synthetic extremes** — declined deliberately; a finding grounded only in one of these is out of contract:

- **A role that disregards either repaired text.** Both deliverables are instruction-only: nothing shipped here detects a QA pass that skipped a restatement site, or a write to a path outside the enumeration. A finding that neither boundary is machine-enforced is a request to build a detector, which is out of contract (P4), not a defect in this task.
- **A tool other than the Codex CLI and the wrapper scripts the agent file names.** The #144 carve-out is bounded to *tools this role is required to run*, deliberately (DP-4). A finding that some other tool a reviewer might choose to invoke also writes state, and that the carve-out does not name it, is a request to widen the carve-out into the open-ended form D3 rejects — it is the boundary working, not a gap.
- **A future rename of `$CODEX_HOME`, a change to the Codex CLI's state layout, or an environment where `$CODEX_HOME` is set inside the checkout.** The carve-out names the general class first and the instance second precisely so a rename falsifies only the example; re-deriving the example is a later task's ordinary edit, not this one's defect.
- **Restatement sites outside the two frozen regions.** T-1029's and T-1030's board entries, their `.shell-team/reviews/*` records and their `.shell-team/provenance/*` records quote the superseded phrasings as history and are **never rewritten** (Non-goals). A finding that the old wording survives in a past board entry, in a review record, or in this spec's own body is out of contract; every negative sweep here is scoped for exactly that reason.
- **Machine parsing of the repaired texts.** No checker parses, validates or enumerates a restatement site, a declaration line's entries, or a write-surface member. A finding that a malformed declaration or an unlisted write path is not mechanically rejected is a request to reopen T-1029's D5 and T-1030's D5, not a defect here.
- **Merge-point staleness in any criterion of either re-frozen spec, or of this one.** T-1029's AC5 / AC7 / AC8 and T-1030's AC5 / AC8 / AC9 are declared merge-point-scoped in their own text, are red or going red by ordinary train movement, and — for T-1030's AC9 — red by this task's own design. AC10 admits exactly those and no others. A finding that one of them is red, or a proposal to widen its base ref, add a retroactive `- stale-at:` line, or re-derive it per rework round, is out of contract (DP-7, P4).
- **Adversarial paths and designators**: a spec path containing a newline, NUL byte, control character or shell metacharacter; a task id crafted to collide with a marker; a hostile `$TMPDIR`; a board line crafted to collide with a ledger anchor. No shell command in this task consumes a value read out of a shipped text — every criterion reads fixed strings this spec froze or line numbers it located by anchor.
- **Escalating scale**: a board of many megabytes, hundreds of ledger records on one entry, dozens of frozen blocks in one spec, a `skills/` tree of thousands of files. The criteria read a handful of one-line blocks, one board, two specs and four shipped files.
- **Non-UTF-8 bytes, CRLF endings or NUL bytes** inside any of the six edited files. `bin/check-intent.sh` owns the normalisation contract for the intent regions, `bin/check-prompt-sync.sh` owns it for the marker regions, and this task adds no parser.
- **Non-GNU `grep` / `awk` / `sed` / `cmp` variants beyond the POSIX behaviour these lines rely on**, and non-bash shells. `bin/check-acs.sh` invokes each check through `bash -c`; the executing side confirms the host's own tools at the freeze gate (Assumptions).
- **Whether either target spec's underlying decision was right.** T-1029's D1 (the section sits outside the intent block) and T-1030's D3 (the write surface is a closed enumeration) are settled and are the *premises* of these repairs, not their subject. An argument to move the declaration line inside the frozen region, or to replace the enumeration with a prohibition list, is a new task.

<!-- END intent-block: T-1036 -->

## Same-class inventory tables

### #141 — every site carrying the declaration-line categorisation, and its disposition

**Accounting for the completeness claim**: the population was produced by reading `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` in full and `agents/qa-verifier.md`'s `## Rules` in full, then searching both for every sentence that names the declaration line in relation to the frozen region. The tech-lead's independent machine inventory returned five live sites; pm-spec re-derived those five and found **two further sites that carry the framing correctly**, listed here so their absence from the fix is a decision rather than an omission. Historical occurrences in `.shell-team/todo.md` and `.shell-team/reviews/T-1029.md` are excluded as records of what was said at the time, which this task never rewrites.

| Site | Shape at the base ref | Disposition |
|---|---|---|
| T-1029 spec, Goal sentence | `…an Input-space clause, and the declaration line itself, which is one such site and never the whole set` | **rewritten now**, inside the intent block — class-B |
| T-1029 spec, D3(ii) | `…inside the marker pair, and the declaration line, which is one site among them and never the whole set` | **rewritten now**, inside the intent block — class-B |
| T-1029 spec, frozen `F3` block | the Goal's apposition verbatim | **rewritten now**, inside the intent block — class-B; identical to this spec's `Q1` |
| `agents/qa-verifier.md` (the shipped line) | the same apposition | **re-spliced by the engineer** from `Q1`; not itself a re-freeze surface |
| T-1029 spec, Body-to-AC correspondence row | `the declaration line being one of them and never the whole set` | **rewritten now**, **outside** the intent block — an ordinary consistency edit, not part of the class-B delta |
| T-1029 spec, `F2` (the `agents/pm-spec.md` bullet) | `The declaration is an index into the frozen region, not the only place the summary lives` | **not moved, measured** — already the additive framing, stated correctly. Rewriting a correct sentence for symmetry would enlarge a frozen delta for no truth-preserving gain |
| T-1029 spec, Input space | `The QA item's scope is the intent block … plus the declaration line` | **not moved, measured** — already additive, and it is the measured ground for calling #141 terminology-only |

### #144 — every site in T-1030's frozen region restating the write surface, and its disposition

**Accounting**: the population was produced by reading `.shell-team/specs/T-1030-reviewer-board-write-boundary.md`'s intent block (its `<!-- BEGIN/END intent-block: T-1030 -->` markers) in full and identifying every passage that enumerates or characterises what this role may write. Four sites. The Body-to-AC row outside the block is listed separately because it is mutable.

| Site | Claim at the base ref | Disposition |
|---|---|---|
| `F1` (the shipped Rule) | `**Write these paths and no others**: …` four members | **moved** — the carve-out sentence lands after the enumeration; this is the text the finding is against |
| `D3` | heading asserts `everything not on it is read-only`; body says `The list is exactly:` | **moved** — the heading is the single claim the carve-out makes imprecise; leaving it would be a body directive contradicting the shipped bytes it describes |
| `Input space`, reachable-classes bullet | enumerates the runtime situations the Rule governs; omits the `codex exec` state write | **moved** — that write is a reachable runtime situation, and an input space omitting an input class the Rule now speaks to is #144 one level up |
| `Goal` sentence | `closes the list of paths this role may write (…)` | **not moved, measured** — it makes no everything-else-is-read-only claim, and the carve-out's own content is that a required tool's state directory *is not a path this role writes*, so the sentence stays literally true. Moving it would widen the frozen delta with no truth-preserving gain |
| Body-to-AC correspondence table (outside the block) | one row for the closed enumeration | **one row added** — the carve-out promoted 1:1 rather than left in prose; its presence is asserted directly by AC8's own row anchor, never by a diff asymmetry |

## Body-to-AC correspondence

Every normative directive stated in the body above, mapped to the criterion that enforces it or to an explicit exemption with a reason.

| Body directive | Where | AC / disposition |
|---|---|---|
| Q1's bytes are one line, identical in this spec, in T-1029's `F3` and in `agents/qa-verifier.md` | Goal / DP-1 | **AC1** (three-way byte identity + occurrence count + adjacency) · **AC7** (the identity re-asserted as part of the class-B claim) |
| Q2's bytes are one line, identical in this spec, in T-1030's `F1` and in `agents/codex-reviewer.md` | Goal / DP-4 | **AC2** (three-way byte identity + occurrence count + adjacency to R1) · **AC8** |
| Q3 ships byte-exact and stays inside T-1028's AC10 window | Goal / DP-6 | **AC3** (window clause asserted by line-number ordering against T-1028's own `F4` block) |
| Q4 ships byte-exact and stays in the step-4 decision list | Goal / DP-6 | **AC4** (immediate-neighbour offsets, never a first-match anchor) |
| The check surface stays additive; the declaration line stays mandatory; no narrowing reading is admitted | DP-1 | **AC5** (two positive pins asserted in both the frozen block and the shipped file, plus the unmoved scope clause) · **AC14**(b) (the re-narrowing mutation, which is the revert-detection lock) |
| The three superseded appositions are gone from the target files | DP-1 / DP-3 | **AC5** (extinction sweep, scoped to two files) |
| The capability-absence phrasing is gone from both skill files | Problem / DP-6 | **AC5** (`read-only on the board` count zero in each) |
| Both replacements carry the rule-form pointer at the canonical bullet | DP-6 | **AC5** (`board-and-write-boundary Rule` and `not a capability it lacks` in both the blocks and the files) |
| T-1029's AC4 scope clause and its own line do not move | DP-2 | **AC5** (the clause asserted present as a fixed string) · **AC7** (10 check lines and 11 criteria — a removed, split or de-recognised AC4 line breaks one of those counts, and both are repair-count-agnostic) |
| T-1030's AC4 thirteen pinned strings and three occurrence counts do not move | DP-2 | **AC6** (all thirteen re-derived in both block and file; all three counts asserted) |
| The carve-out is bounded to *required* tools, names the Codex CLI instance, and is never a licence | DP-4 | **AC6** (three new pins, in both the block and the file) · **AC14**(b) (the `required to run` deletion mutation) |
| The carve-out is a scope statement, not a fifth enumeration member | DP-4 / DP-5 | **AC6** (`Write these paths and no others` still pinned, so the enumeration's own membership is measured unchanged) |
| The #144 delta lands at F1, D3 and Input space, and not at the Goal | DP-5 | **AC2** (F1's carve-out bytes, three-way identity) · **AC6** (the carve-out's three pins plus `Write these paths and no others`, so the enumeration's membership is measured unchanged) · **AC8** (class-b verdict) — and `info-only (not promoted to AC)` for the **Goal's deliberate non-movement** specifically: the only machine shape for "this sentence did not change" is a diff literal against the base ref, which is exactly the repair-count coupling AC7's invariance principle forbids. The #144 inventory table above is where that exclusion is binding, with its reason |
| The new #144 directive is promoted 1:1 into T-1030's own correspondence table | DP-5 | **AC8** (the row asserted present exactly once by its own text anchor — a final-state property, unlike the superseded diff-asymmetry form it replaces) |
| Both re-freezes are class-B, measured not asserted | GO authority / DP-2 | **AC7**, **AC8** (`check-refreeze-class.sh` exit status exactly 1, over a materialised base blob) |
| Each re-freeze's `lines=N/N` is unchanged (10/10 and 11/11) | GO authority / R29 / R30 | **AC7** (10 check lines) · **AC8** (11 check lines) · **AC11** (`check-intent.sh` validates one conformant attestation per version for whatever N each task reaches; AC9 deliberately carries no attestation count, per AC7's invariance principle) |
| The board carries exactly two deletions, each identified | DP-8 | **AC9** (numstat column + `-U0` extraction + per-record cross-check against the base blob) |
| Ratification lines land once each; attestations accumulate; hash records are replaced | DP-8 | **AC9** (occurrence counts; per-shape attestation delta; and the base-relative anchored hash-record **total** delta of +1 — version-agnostic by construction, since each re-freeze replaces rather than appends and T-1036 contributes exactly one record whatever version it is at) · **AC11** (`check-intent.sh` is the authority for per-task hash uniqueness and the v1..vN chain; AC9 deliberately carries no per-version count, because such a clause is re-shaped by any repair of AC9 itself) |
| T-1036's own entry is a pure insertion | DP-8 / board hygiene | **AC9** (the two deletions are individually identified as the two hash records, so a third deletion anywhere fails) · **AC13** (hand-off lint + structural heading check) |
| Both re-frozen specs still satisfy their own criteria, with an honest declared partition | GO authority / DP-7 | **AC10** (recursive `check-acs.sh`, required PASS set + permitted FAIL set per spec) |
| Merge-point-scoped criteria are disclosed, never repaired, re-based, widened or annotated | DP-7 / Non-goals | **AC10** (the permitted FAIL sets are named and closed) · `info-only` for the *prohibition* itself — asserting the absence of a repair has no non-vacuous check shape; it is binding as the Input-space boundary that stops an escalating "make it green" finding |
| Every ledger on the stacked train still validates | DP-8 | **AC11** (`check-intent.sh` over both re-frozen specs, five neighbours and this spec) |
| No `bin/`, `tests/`, `templates/`, `docs/`, `.github/` or lessons-corpus change | Non-goals | **AC12** (tracked diff and untracked addition, both) |
| No other `agents/*.md` and no other `skills/` file changes | Non-goals | **AC12** (each agent named; the `skills/` tree enumerated from the base ref itself rather than diffed) |
| The generated playbook blocks still regenerate to a zero diff | Goal / Non-goals | **AC12** (scratch-tree regeneration of all eight registry files) |
| T-1028's AC10 live lock stays green, including its two suite requirements | Summarized sources / DP-6 | **AC3** (the window) · **AC12** (`tests/codex-skeleton-hygiene/run.sh` and `check-prompt-sync.sh` both run) |
| No replacement line carries a `bash bin/check-*.sh`-shaped or `gate:ACn`-shaped string | R5 (tech-lead constraint) | **AC12** (`tests/codex-skeleton-hygiene/run.sh` green is the authoritative measurement — its two live-file regexes are run over three of the four target files by the suite itself, so a duplicate regex here would be a second, drifting definition of the same lock) |
| History is never rewritten; every sweep is scoped | DP-3 / Non-goals | **AC5** (every grep names its files) · **AC13** (allow-list contains no review or past-task record) |
| The engineer's surface is exactly four lines | DP-8 | **AC13** (allow-list) · **AC7**, **AC8** (the two specs' deltas are pm-spec's, fixed in size) |
| No adjacent "while we're here" fix; #155's items are out of contract | Non-goals / P4 | **AC12**, **AC13** (nothing outside the allow-list may appear) |
| Neither repaired boundary is enforced mechanically | Non-goals / Input space | `info-only (not promoted to AC)` — an explicitly declined guarantee; no artifact property asserts a non-guarantee and asserting its absence would be vacuous. It is binding as the Input-space boundary |
| Both re-freezes reach the cross-provider round as two separate disclosure items, each vetoable alone | GO authority / P2 | **AC14**(e) — a reporting duty on the hand-off, not an artifact property |
| The full applicable CI-wired list runs locally, directory-scanning steps run rather than reasoned about | Notes / corpus entry 2026-08-04 | **AC14**(a) |
| A producer-run mutation self-check precedes the first review round | Notes / playbook | **AC14**(b) |
| Relayed claims are measured before the freeze and reported | Assumptions | **AC14**(c) |
| Every named source is opened and reported per source, including the two negative findings | `## Summarized sources` / DP-3 | **AC14**(d) |
| Checkers fail closed; no check passes on an unreadable input or an unresolvable ref | repo contract | **AC1–AC6** (extraction line-count and content controls before any comparison) · **AC7–AC9**, **AC12**, **AC13** (`rev-parse --verify` / `cat-file -e` before any base-ref read) · **AC10** (exit 2 is never a pass) · **AC11** (resolved board must be readable) |

## Assumptions

- **pm-spec has no shell in this role, so not one `check:` line above has been executed.** Every file named here was read with a file-reading tool. The executing side runs all 13 live `check:` lines against the pre-implementation tree, decomposes each compound line into the clauses it asserts, corrects anything mechanically broken or vacuously passing with a meaning-preserving fix, records the measured shape, and only then records the freeze attestations and the three hash records (T-1036 v1, T-1029 v3, T-1030 v2). **Verify, then correct, then freeze** — and note that a correction to `Q1` or `Q2` must land identically in this spec's block **and** in the target spec's block, because AC1 / AC2 / AC7 / AC8 compare them.
- **Relayed claims this spec leans on, disclosed rather than absorbed.** Each is a claim pm-spec could not measure; the freeze run measures it and reports the value.
  1. **The bodies of issues #141, #143 and #144**, relayed verbatim by the coordinating session. pm-spec has no network or `gh` access and opened none of them. If an issue's actual text asks for something this spec declines — a checker for either boundary, an enumeration-only carve-out shape, an identical sentence for both skill lines — that is a scope question to raise **before** the freeze, not a rework item.
  2. **`5a5c09b` is T-1035's branch tip and this branch's real branch point.** The literal appears inside this spec's frozen region in AC7, AC8, AC9, AC12 and AC13; if it is wrong, five criteria measure the wrong comparison and the literal must be corrected before the freeze rather than worked around.
  3. **The two superseded hash literals AC9 transcribes** — `aa47eaa0525af8a73cf89b9b3655b310e62bdadb` (T-1029 v2) and `d0f73437565c9211c998e78a863948eb23e27972` (T-1030 v1). Read by pm-spec from the **working-tree** board, not from the base blob. AC9 cross-checks each against `git show 5a5c09b:<board>`; confirm both are present there before trusting the identification.
  4. **`## Active` is empty at the base ref.** Observed by pm-spec in the working tree, not in the base blob. Re-confirm with `git show 5a5c09b:.shell-team/todo.md | sed -n '10,18p'`; if a line is already there, AC9's deletion count still holds but T-1036's insertion must not disturb it.
  5. **The 2026-08-05 sprint v1.6.0 planning GO covers #141, #143 and #144 inside T-1036's scope**, and this checkout's operator ruling stands. Ground (i) and (iii) of both ratification lines. A conversational premise cannot be re-run; the strongest available re-measurement is the interventions record for this task.
- **Both re-freezes' class-B classification is a prediction until the classifier runs.** `bin/check-refreeze-class.sh`'s rule (iii) makes a prose change at equal line count class-b, and neither delta touches a `- check:` line or changes a line count — but that is pm-spec's reading of the script's header contract, not an observation. If either measures `0` (mechanics) or `2` (structural), **stop and route back to pm-spec**: `0` would mean the delta somehow landed only on check lines, and `2` would mean the two regions normalise identically, which for a prose repair means the edit did not take.
- **`bin/check-acs.sh`'s 120 s default may not cover AC10.** That criterion runs the checker recursively over two specs, one of which (T-1029) itself runs a `gen-playbook-blocks.sh` scratch regeneration inside its own AC6. T-1035's equivalent criterion needed `CHECK_ACS_TIMEOUT=600`. If the default truncates AC10, **raise the timeout for the run and record that the elevation was needed** — never narrow the criterion to fit the clock. No other criterion here is close: the heaviest remaining are AC12 (one scratch regeneration plus one fixture suite) and AC11 (eight `check-intent.sh` runs over a large board), both seconds.
- **This spec is self-hosting and quotes the very phrases it requires to be absent.** AC5's negative greps are scoped to `agents/qa-verifier.md`, `.shell-team/specs/T-1029-claim-fidelity-qa-step.md`, `skills/run/SKILL.md` and `skills/goal/SKILL.md` — never the repository — so this spec's own quotations of the three superseded appositions and of `read-only on the board` (in Problem, DP-1, DP-3, DP-6 and the inventory tables) cannot trip them. Nor can the board's or the review records' historical copies.
- **`agents/codex-reviewer.md` is expected to be byte-identical to its T-1030 state at the base ref**, which is why that spec's AC5 whole-file delta lock may still be green through this re-freeze: AC5 compares the file's work-only line set against the concatenation of T-1030's own four frozen blocks, and this task moves both sides together. AC10 nonetheless lists T-1030's AC5 among the **permitted** FAILs rather than the required PASSes, because pm-spec cannot measure whether some later car touched that file. If it measures green, that is the better outcome and not a mismatch; if it measures red, it is ordinary merge-point staleness and is disclosed, never chased.
- **`bin/gen-playbook-blocks.sh` prints a non-fatal block-size warning to stderr** for the roles whose blocks exceed its threshold. It does so already at the base ref and the script's contract says the warning never fails the run. AC12 redirects stderr and judges by exit status and `cmp`; do not read the warning as a failure and do not respond to it by shortening a block.
- **No `check:` value begins or ends with a backtick** (`bin/check-acs.sh` rejects that shape fail-closed), no AC label carries a suffix glued to its digits, every `grep` whose pattern could begin with `-` passes `--` first, and no frozen block contains a line matching `^[[:space:]]+- check:` — so none of the six blocks can be parsed as this spec's fifteenth criterion or counted into its attestation total. Confirm at the freeze that this spec measures **14 criteria and 13 check lines**.
- **`R29` and `R30` carry no backtick character at all**, deliberately, following T-1035's `R` precedent: they are board lines that AC9 pins by whole-line equality, and a backtick inside a value that passes through `bash -c` is a needless quoting risk for no readability gain. Their `→` characters are literal UTF-8 and must round-trip unchanged.
- **The frozen-block extraction patterns contain a `!`**, always inside single quotes. `bin/check-acs.sh` executes each line through a non-interactive `bash -c`, where history expansion is off; pasting one of these lines into an *interactive* shell to debug it can trip history expansion and produce a failure unrelated to the criterion.
- **Every `mktemp` invocation above uses the guarded idiom** (`T=$(mktemp -d "${TMPDIR:-/tmp}/<slug>.XXXXXX") || exit 1`, failure exiting before the first composed path is used), each with its own scratch slug, and every `|| true` sits on a `grep -c` whose zero is a meaningful measurement, never on a producer whose failure would otherwise be invisible.
- **Predicted `check-acs.sh` shape for this spec's own 13 lines at the freeze moment** — measured *after* the two spec edits and the board's ledger lines exist, but *before* the engineer splices the four shipped files. Recorded so a mismatch is itself a finding: **PASS AC7, AC8, AC9, AC11, AC12** (5); **FAIL AC1, AC2, AC3, AC4, AC5, AC6, AC10, AC13** (8); **SKIP AC14**. Per-line reasons, since state-assertions true at the base must measure green pre-implementation (T-1034's freeze lesson): **AC7 and AC8** pass from the moment pm-spec's own edits land — a red there means `check-refreeze-class.sh` did not report class-b, that one of the two criterion-population counts moved (10 check lines / 11 bullets for T-1029; 11 / 12 for T-1030), that a frozen block and its target diverged, or — for AC8 — that T-1030's carve-out row was not promoted into its Body-to-AC table. **Neither criterion pins a spec-file line-count literal, by construction**: both are repair-count-agnostic, so a later ratified repair of either spec leaves them green (this is the v3→v4 invariance principle, and the reason the earlier `4/4` / `4/3` forms were removed rather than re-tuned). **AC9 and AC11 are red before the ledger lines are written and green immediately after** — measure both shapes and record both; AC11 in particular cannot pass until the two new hashes exist. **AC12 is a preservation lock that must be green before, during and after** — a red there at any point is a finding about this spec's premises, not about the work. **AC1 and AC2 fail only on their shipped-file halves** — the block-to-block identity is already satisfiable, since both blocks live in files pm-spec edits. **AC5 fails only on its `agents/qa-verifier.md` and two-skill-file halves**; its two-spec extinction clauses must be **green** at the freeze. **AC10 is red at the freeze because T-1029's own AC3 is red** — phase lag, not staleness: that criterion compares the re-frozen `F3` against the shipped agent line, which the engineer has not spliced yet. It is the red most likely to be misread as a defect. **AC13 is red only on the two missing record files.** If any criterion behaves the other way round, route it back to pm-spec rather than working around it.

## Open questions

None blocking. Two items are deliberately settled rather than asked, and are recorded here so they are not re-opened in a round: whether the #144 carve-out should have been an enumeration entry instead of a scope statement (DP-4 — it is a scope statement, which is what keeps T-1030's AC4 pins and its four-member enumeration intact), and whether T-1030's Goal sentence should have moved with the rest (DP-5 — it should not, because it makes no everything-else-is-read-only claim and stays literally true). Both are re-freeze proposals routed to pm-spec if a reviewer disagrees, never rework fixes.

## Notes for engineer

**Your surface is exactly four lines in four files.** You splice `Q1` into `agents/qa-verifier.md`, `Q2` into `agents/codex-reviewer.md`, `Q3` into `skills/run/SKILL.md` and `Q4` into `skills/goal/SKILL.md`. Every one is a **one-for-one whole-line replacement** — nothing is inserted, nothing is deleted, and no neighbouring line moves. **You do not edit either re-frozen spec's intent block, and you do not write any board ledger line**: the two spec deltas are pm-spec's and already landed in this file's own commit, and the ratification, attestation and hash records are the coordinating session's step at the freeze gate. An engineer edit to hashed bytes is exactly what the class-B route exists to prevent.

Read before you touch anything: `agents/qa-verifier.md`'s `## Rules` end to end (so you see the four contiguous mandatory items `Q1` belongs to), `agents/codex-reviewer.md`'s `## Rules` end to end (so you see R1 sitting immediately above `Q2` and the two Japanese-language bullets you must not disturb), and `skills/run/SKILL.md`'s steps 6 and 7 (so you see the T-1028 AC10 window `Q3` sits inside).

Order of work — the P1 split order, one commit each, so whichever car is clean can ship alone:

1. **C1 — #143 alone**, the safest and the only one with no ledger consequence: replace `skills/run/SKILL.md`'s line with `Q3` and `skills/goal/SKILL.md`'s line with `Q4`. One commit.
2. **C2 — #141**: replace `agents/qa-verifier.md`'s summarized-source item with `Q1`. One commit.
3. **C3 — #144**: replace `agents/codex-reviewer.md`'s board-and-write-boundary Rule with `Q2`. One commit. C2 and C3 are mutually independent and may be done in either order.
4. Records: `.shell-team/provenance/T-1036.md` and `.shell-team/interventions/T-1036.md`. Run `bin/check-pii-shapes.sh` over each newly written record **before its first commit**, not only at the CI gate.

Scope-lock allow-list (the files that may change on this branch) — the four shipped files are yours; the rest are listed because they are already in the diff or are unconditionally required, and AC13 counts them all:

```
agents/qa-verifier.md                                          (Q1 — yours)
agents/codex-reviewer.md                                       (Q2 — yours)
skills/run/SKILL.md                                            (Q3 — yours)
skills/goal/SKILL.md                                           (Q4 — yours)
.shell-team/specs/T-1029-claim-fidelity-qa-step.md             (pm-spec's re-freeze — do not touch)
.shell-team/specs/T-1030-reviewer-board-write-boundary.md      (pm-spec's re-freeze — do not touch)
.shell-team/specs/T-1036-wording-batch-141-143-144.md          (this file — you do not edit its intent block)
.shell-team/todo.md                                            (hand-off line only; ledger lines are the orchestrator's)
.shell-team/provenance/T-1036.md
.shell-team/reviews/T-1036.md
.shell-team/interventions/T-1036.md
```

Anything else in the diff fails AC13 — including a stray untracked scratch file, since AC13's union counts untracked additions. Run every mutation experiment on a scratch `git worktree` outside this checkout, never in the working tree.

Traps in the criteria and in the files themselves, so you do not find them the expensive way:

- **Extract the frozen blocks; do not retype them, and do not "fix" them.** Every one of `Q1`–`Q4` is a single very long physical line carrying apostrophes, em-dashes, backticks, a `{txt,jsonl}` brace pair and a `…` ellipsis. `awk` the block out of this spec, `sed '1d;$d'` the fence lines, and splice the exact bytes. A single smart quote, a collapsed double space, or a hyphen where an em-dash belongs fails the criterion — **and the failure will look like a placement problem rather than a transcription problem**, which is how the expensive hour gets spent.
- **`Q3` and `Q4` carry three leading spaces that are part of the frozen bytes.** They are sub-bullets inside numbered step lists. An editor that trims leading whitespace, or a splice that re-indents to two or four spaces, fails AC3 / AC4 on a difference you cannot see.
- **`Q1`'s and `Q2`'s bytes must equal the target spec's re-frozen block, not merely resemble it.** AC1 and AC2 compare all three copies; AC7 and AC8 compare two of them again as part of the class-B proof. If you find a defect in the frozen bytes, that is a **re-freeze proposal routed to pm-spec** (P3), never an in-place correction — correcting one copy and not the other four is the single most likely way to turn one finding into four red criteria.
- **`Q3` must land strictly between `6. **Review**` and `7. **Done**` in `skills/run/SKILL.md`.** That window is T-1028's AC10, a **live** lock and not a merge-point-scoped one, and it additionally requires `tests/codex-skeleton-hygiene/run.sh` and `bin/check-prompt-sync.sh` to be green — explicitly so that green cannot be bought by editing a lock. Replacing the line in place preserves the window; moving it does not.
- **Three of your four target files are under a live regex lock.** `tests/codex-skeleton-hygiene/run.sh` runs a broken-invocation regex over `agents/qa-verifier.md`, `skills/run/SKILL.md` and `skills/goal/SKILL.md` requiring **zero** matches, and a stateful-trace regex over `skills/run/SKILL.md` requiring zero. The four frozen texts contain no `bash bin/check-…\.sh`-shaped invocation and no `gate:AC<n>`-shaped sentinel, deliberately. If you paraphrase anything — and you should not — do not introduce one. Run that suite after each commit, not only at the end.
- **`agents/qa-verifier.md`'s marker regions begin at line 81, below your edit; `agents/codex-reviewer.md` has none at all.** Your two agent-file edits are outside every generated region and outside every registry-owned `contain` line. AC12 re-derives both facts (scratch-tree regeneration plus `check-prompt-sync.sh`); an edit that strayed inside one turns AC12 red, which is the signal you touched a line the registry owns.
- **Do not touch `agents/codex-reviewer.md`'s two Japanese-language `## Rules` bullets, its `## Output` fenced block, or T-1028's class-M item at the end of the file.** T-1030's own AC5 is a whole-file delta lock and AC6 pins that class-M line; both go red on any other edit, and both are outside your allow-list to repair.
- **`grep -c` under `set -euo pipefail`** aborts a script when the count is genuinely `0`; append `|| true` to the pipeline, never to the assignment, and keep the explicit assertion right after. Relevant if you write scratch verification around the mutation self-check.
- **Do not add an intent-hash, ratification or freeze-attestation sub-bullet to the board**, for T-1036 or for either re-frozen task. pm-spec has no shell and computed none; the orchestrator runs the freeze on its standing authority after running every `check:` line live.

Pre-commitments, set before the first round and binding on every role on this branch:

- **P1 — the split order is decided now**, and commit separation is required for exactly that reason: **C1 = #143 alone** (safest, zero ledger consequence, no frozen bytes involved), **C2 = #141** (T-1029's re-freeze), **C3 = #144** (T-1030's re-freeze). C2 and C3 are mutually independent — neither's revert implicates the other, and neither implicates C1.
- **P2 — two consecutive rework rounds of independent new `Blocker`/`Major` findings against the same re-freeze surface** ends the patching for that surface. Factual trigger: round *n* and round *n+1* each produce new, independent findings against that re-freeze's frozen wording or its ledger. Contextual trigger: a third round on that surface is about to start. **The factual trigger governs when the two disagree.** Two consecutive rounds is this repository's standing threshold and nothing here loosens it. The two surfaces are counted **separately**, since the cross-provider round holds an independent veto over each. On trigger, **revert that re-freeze to its prior version** — restore the intent block's bytes, roll the hash record back to the superseded value, drop that version's ratification and attestation lines, and **re-disclose the drift** the re-freeze was closing rather than half-repairing it — and ship the remaining cars. If both re-freezes trigger, **C1 ships alone**: removing a false capability claim from two orchestrator-facing lines is a strict improvement with no frozen bytes involved.
- **P3 — a defect found in frozen prose routes to pm-spec as a re-freeze proposal**, never as engineer rework. That covers `Q1`, `Q2`, anything inside T-1029's or T-1030's intent blocks, and the `R29` / `R30` ratification texts. `Q3` and `Q4` are **not** frozen bytes of anyone's intent block, so a defect in them is an ordinary rework fix — but it must land in this spec's frozen block and in the shipped file together, because AC3 and AC4 compare them.
- **P4 — out of contract, refused in the round it appears**: issue #155's three items; any new or changed `bin/` checker; any `tests/`, `templates/`, `docs/`, `.github/` or lessons-corpus edit; any retroactive `- stale-at:` line on a merge-point-scoped criterion; any widening of a merge-point base ref; and any adjacent "while we're here" fix in the four target files. Record each in the review record and, if one recurs, as an issue candidate for the next planning input. AC12 and AC13 are the machine half of the same refusal.

**Task classification**, stated so review rigour is calibrated rather than guessed: **not verification-mechanism work** — no checker, parser or state tracker is written or extended, and `bin/` / `tests/` are byte-identical (AC12 locks it) — **but not a plain pure-addition either**: it edits the frozen regions of **two** already-merged tasks in one branch, it makes two board deletions rather than the usual one, and three of its criteria depend on other checkers' exit-status semantics (`check-refreeze-class.sh` exit exactly 1, twice; `check-acs.sh` run recursively over two specs). Review the two re-freezes, their ledger lines and the frozen wording at the **higher** standard, and the two skill-line replacements at the ordinary one. The one place a prose gate's whole effect lives is its wording, and QA's adversarial fixture-synthesis checklist is structurally inapplicable to a prose-only deliverable by its own applicability paragraph — which leaves the criterion walkthrough and the cross-provider read as the only two surfaces. 14 criteria with 13 `check:` lines; the count is driven by the surface count (four shipped lines, two re-freezes, one board ledger, one recursive measurement, three preservation locks), not by the size of the edit.
