# The sprint rider: `check-count-claims`'s zero-count refusal message teaches only a recipe that survives `pipefail`, and four stale precondition/phase claims are repaired in place across `phase-multiplexing.md` and both README mirrors

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1120

**Branch**: `feature/1120-rider-count-claims-message-and-fanout-docs`, cut from `feature/1119-effort-application`'s close-out tip. Measured first-hand at authoring time by reading the ref files directly: `.git/HEAD` reads `ref: refs/heads/feature/1120-rider-count-claims-message-and-fanout-docs`; `.git/refs/heads/feature/1120-rider-count-claims-message-and-fanout-docs` reads `492eff047886347aa2e6fd05a251ae347b017cce`; `.git/refs/heads/feature/1119-effort-application` reads `4f969892e9ddb5ef3615dec24c0c627513414ad8`, and `.git/refs/remotes/origin/feature/1119-effort-application` reads the same commit. So the predecessor resolves as a **local branch** — **arm 1** of the `- base-ref-discriminator:` is what this checkout selects — arm 2 names the remote-tracking ref by its full path because it resolves there too, and this branch **already carries a commit of its own** (the coordinating session's `.shell-team/interventions/T-1120.md` entry), which is why no criterion below treats `HEAD` as the branch point. PR base stays `develop`; the PR is held open and merges at the sprint batch GO. No 40-hex literal is written into any acceptance criterion.

## Problem

Two shipped surfaces say something that is no longer true, and both were filed as fast-follows rather than fixed in the task that noticed them.

`bin/check-count-claims.sh`'s refusal message for a `- count:` command that exited non-zero teaches the legitimate-zero-count recipe in two steps: it quotes `grep zzz file | wc -l` framed with "instead" and a second unguarded negative example, and only then the guarded form that actually works. Since the checker began running every recorded command under `set -o pipefail` in a fresh child shell, the intermediate unguarded form **still refuses** on a genuine zero match, so an operator who follows the message's first suggestion hits the identical error once more before reaching the working recipe (#409). The script's own header idiom is already correct — re-measured at `:64`–`:73` this run — so nothing above the one `fail` call is owed an edit.

`docs/loop-engineering/phase-multiplexing.md` still frames the `per-instance-telemetry-discriminator` precondition as unbuilt and still says the fan-out recommendation is gated on **two** unbuilt preconditions, while `:369` already makes `verify-mechanism — tier1-fanout` **unconditional**, `:375` records the second precondition `DISCHARGED`, and the discriminator itself ships as `bin/check-fanout-instances.sh` with its fixture suite CI-wired. Both README mirrors' `- **Within-phase fan-out**` bullet still says the election is "made inside the Validate phase", when it is a Plan-time `verify`-axis dispatch decision transcribed at the Specify-to-Implement seam — `skills/run/SKILL.md:281` already says so, which is why the README is the leftover and not the skill (#407).

## Summarized sources

- Issue **#409** (GitHub, **relayed** as a scratch file — see `## Assumptions` **A-1**) — the distinctions carried over: the message teaches the recipe **in two steps** and it is the *intermediate* form that still refuses under `pipefail` (`grep` exits 1 and `wc -l`'s success does not clear it); the header comment's idiom is **already correct**; the fix is "drop or guard the intermediate example so the message teaches only the working form — one string edit, no behavior change"; and the fixture suite's positive control **already exercises the guarded form**, so no new fixture is owed for the recipe itself. The issue's own scope word is *prose-only*, which this spec does not inherit as a class claim — the file is under `bin/`, and `agents/pm-spec.md:91` keys the class on the path.
- Issue **#407** (GitHub, **relayed** as a scratch file — see **A-1**) — the distinctions carried over: **two** items, both from T-1112's delivered-change review round 1, both dispositioned file-an-issue and **neither behavioral**. Item 1 names the `:355`-area prose *and* the unedited `verify`/`verify-fixture` rules at `:366`/`:367` still saying "both … preconditions exist" against a recommendation that now carries a single live precondition, and prescribes "repair in place, line-count-preserving, minding the byte-locks the T-1112 sweep measured (T-1079 AC8 class)" — the shape this spec's edit-form locks encode. Item 2 states the READMEs' clause was inherited **byte-identical from before T-1112** (confirmed against the base blob there), that the election is a Plan-time dispatch decision transcribed at the Specify seam "per `skills/run/SKILL.md`'s own dispatch-record mechanics", and that machine tokens stay **English verbatim in JA**. The issue's quoted phrase for item 2 is a **paraphrase** of the shipped sentence, not its bytes — recorded as the committed intervention entry in `.shell-team/interventions/T-1120.md` and the reason every criterion below keys on the file's own words.
- `tech-lead`'s 2026-09-04 Routing Map (**relayed** as a scratch file — see **A-2**) — the distinctions carried over and formalized rather than re-opened: the four-site table with each site's own constraint set; that the `| wc -l` substring has **three** separate readers (T-1113 AC6 at `:90`–`:91`, T-1113 AC9's echo-before-run ordering anchor at `:99`–`:100`, and the fixture suite at `:140` and `:228`) and that AC9's anchor works *because the echoed command text cannot contain it*; that the `phase-multiplexing.md` half **must** rewrite lines T-1112 AC1/AC2 lock, which is the task's real design call and is settled rather than delegated; DP-A's recommendation to include `:373`; the edit-shape constraints (line-count-preserving, exact `- dispatch-rule:` prefixes including `trigger: `, `:355`'s five literals kept and its agent-shaped literal still absent, no `^## ` heading moved, `pilot3-evidence` untouched); the reddening **disclosed and never repaired** (T-1112 AC1's sibling clause and AC2's non-`verify-mechanism` set clause, by design; T-1019 AC10 on Site 1; the **relayed** claim that T-1019 AC10 and T-1003 AC19 are *already* red at the branch point, which this spec re-reads at its primary source rather than inheriting); that T-1075 AC5/AC6 and T-1100 AC6 may already be red for a base-resolution reason wholly independent of this diff; the per-item PATCH table and the sprint tier as its column maximum; the pre-commitment's drop order verbatim in substance; and the five dispatch rows with `spec-review — none` as the one axis differing from T-1119.
- `bin/check-count-claims.sh` (`:55`–`:73`, `:102`–`:105`, `:284`–`:299`) — read first-hand: the header already carries the guarded idiom `(grep zzz file || true) | wc -l` with its explicit "NO `-c` on grep, since `grep -c`'s own \"0\" text output would then itself be counted as one line by `wc -l`, double-counting a zero match as one" note, so the teaching this task must preserve is stated correctly two hundred lines above the defect; `fail()` prints one line to **stderr** and exits **1**, `die()` exits 2, so a rendered refusal is one physical line on the combined output; the per-row echo (`printf '%s: count %s — declared %s — command: %s\n'`) is emitted **before** the command runs; each command runs as `bash -c "set -o pipefail; $cmd"` in a fresh child shell so a failing upstream stage is never masked; and the defect is confined to the single `fail` call on the non-zero-exit branch, whose message quotes `grep zzz file | wc -l` and `grep -c zzz file | wc -l` before the guarded form. The script resolves no sibling and reads no operating path, which is what makes running its **base-ref blob** as a script a legitimate two-arm probe.
- `tests/check-count-claims/run.sh` (`:120`, `:136`–`:142`, `:222`–`:232`, `:286`–`:308`) — read first-hand: **nine** lines carry the substring `| wc -l`, of which two are the readers the map names — `:140` asserts the rendered stderr carries the fragment `'| wc -l'` for the `grep -c zzz /dev/null` fixture, and `:228` extracts the first line carrying it to prove the echo strictly precedes it; `:292` deliberately records an **unguarded** pipeline (`git show <badref>:path | wc -l`) as a fixture that *must* refuse, and `:305` records the guarded `(grep zzz /dev/null || true) | wc -l` as the legitimate-zero positive control. The distinction that matters for this task's negative criterion: an unguarded pipeline inside the **test corpus** is a correct fixture, so the absence assertion is scoped to the **rendered message's own quoted recipes** and never to a file-wide or repository-wide search.
- `.shell-team/specs/T-1113-quantity-relay-checker.md` (`:90`–`:91`, `:99`–`:100`) — the distinctions carried over: **AC6** requires the fifth fixture's stderr to carry the remedy literal `| wc -l` (`grep -qF -- '| wc -l' "$T/e5"`), and **AC9** uses that same literal as an **ordering** anchor, stating in its own prose that the literal is chosen "precisely because it cannot appear in the echo: the echoed row's own command text does not contain it, and the remedy is only reachable after the command has actually run". So a repair that keeps the fragment `| wc -l` in the remedy — and keeps it out of the echoed command text — satisfies both readers, and dropping the unguarded intermediate examples is invisible to both.
- `README.md` (`:173`–`:175`) and `README.ja.md` (`:173`–`:175`) — read first-hand: each `- **Within-phase fan-out**` / `- **フェーズ内 fan-out**` bullet is **one physical line** at `:174`; `:173` already ships the `--instance` span discriminator on the adopter surface; the English stale clause reads "using fan-out for other verification work is still an **explicit per-task choice made inside the Validate phase**, where the run skill's own fan-out step documents the degree rule, the liveness requirement and the telemetry convention", and the JA mirror reads "他の検証作業で fan-out を使うかどうかは、**依然として Validate フェーズの中でタスクごとに明示的に選ぶ**。degree の決定則・liveness の要件・テレメトリ規約は run skill 自身の fan-out ステップを参照。"; each phrase occurs **exactly once** in its own file (measured, and the English one also occurs once in `.shell-team/reviews/T-1112.md`, which is why every criterion names its file rather than searching the tree); and the surrounding line already carries English machine tokens verbatim inside Japanese prose (`verify-mechanism`, `tier1-fanout`, `- execution-host:`, `sub-agent-hosted`), which is the convention the repair follows rather than invents.
- `.shell-team/specs/T-1075-fanout-adoption-versioning.md` (`:61`–`:66`) — the distinctions carried over: **AC5** forbids four **case-insensitive** phrases (`always fan out`, `fans out by default`, `automatically fan`, `fanned out by default`) in `skills/run/SKILL.md`, `README.md` and `README.ja.md`, and requires three opt-in literals to stay present — measured first-hand this run, `Opt-in only — no phase is rewired to fan out by default` and `あくまで opt-in` both live on line **180** of their files, not on 174, so they are outside the edited bullet and stay green by construction rather than by care. **AC6** greps the literal `Validate` **inside the extracted bullet** in each mirror, so `Validate` must remain on that same physical line; AC6's own `- adopter-surface:` line names "line 174" in both files, which is why the position lock below is a criterion rather than a preference. Both criteria resolve their base through `BPREF=feature/1074-fanout-orchestration` and **exit 1** when neither the local nor the origin ref exists — a base-resolution failure mode independent of this diff.
- `docs/loop-engineering/phase-multiplexing.md` (`:80`, `:345`–`:351`, `:355`–`:377`) — read first-hand: `:80` is the `- saving: tier1-verification-fanout — measured — …43.84% wall-clock reduction` line the dispatch grounds price against; `:351` is the seventh `- invariant-lock:` line, which records `per-instance-telemetry-discriminator` as "named as a follow-up only (DP-F)" — a statement about **T-1078's own pilot scope**, not about today's repository, and therefore *not* part of this task's repair; `:355` carries the disposition token `staged-adoption` together with the stale status clause "gated explicitly on one named precondition this task's own pilot could not build" and all five literals T-1112 AC3 requires; `:365` and `:367` each read "(staged-adoption, gated on **two unbuilt preconditions**)" and `:366`/`:368` each assert that **both** of the recommendation's named preconditions exist, against `:369`'s already-unconditional `verify-mechanism` rule and `:375`'s recorded discharge; `:373` is a `- follow-up:` line still asking that the discriminator be *designed*; and `:375` begins `- follow-up: DISCHARGED`. Measured counts at authoring time: the literal `two unbuilt preconditions` occurs **2** times and `preconditions exist` **1** time in this file (3 occurrences together), and `^- follow-up: DISCHARGED` matches **exactly one** line.
- `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` (`:59`–`:66`) — the distinctions carried over, each read first-hand because this task's edit-shape is derived from them: **AC1**'s last clause concatenates every `- dispatch-rule: verify — ` and `- dispatch-rule: verify-fixture — ` line and requires **byte-identity** base↔HEAD — the clause this task's Site 4 repair reddens by design — while its `verify-mechanism` count and trigger-literal clauses are untouched by this diff; **AC2** locks total line count, the three key counts, the `- dispatch-axis:` concatenation, the sorted set of **every non-`verify-mechanism`** `- dispatch-rule:` line (the second clause this task reddens), the `^## ` heading list and the `pilot3-evidence` region; **AC3** pins the single `- recommendation: tier1-verification-fanout — ` line's five required literals and its absent agent-shaped literal, and compares the `^- follow-up: DISCHARGED` extraction with `cmp` — so a repaired `:373` that *began* `- follow-up: DISCHARGED` would make that extraction two lines and redden AC3, which is a criterion this task's repair is required to keep green (see **DP-G**).
- `.shell-team/specs/T-1100-verify-axis-split.md` (`:71`–`:72`) — the distinctions carried over: **AC6** requires exactly one `- dispatch-axis: <refinement> — serial|tier1-fanout — ` line per refinement, each stating that it **refines** the `verify` axis; exactly one `- dispatch-rule: <refinement> — serial — unconditional — ` and one `- dispatch-rule: <refinement> — tier1-fanout — conditional — trigger: ` line per refinement; base-relative `+2` / `+4` / unchanged deltas on the three key counts and a `-1` delta on the literal `exactly two, note-wide`; the `full-population` literal on the `verify-mechanism — tier1-fanout — conditional` rule — a clause **T-1112 already superseded** by flipping that rule to unconditional; and the `pilot3-evidence` byte-identity. Its deltas are keyed to `develop`, into which T-1100 is already merged, so its verdict at this branch point is measured rather than assumed.
- `skills/run/SKILL.md` (`:276`–`:281`) — the distinction carried over, and the truth the README repair states: `:281` reads "`tech-lead` decides in step 1; you (the orchestrator) transcribe the printed decision onto the task's board entry at the Specify-to-Implement seam … the `verify`-axis record — or, when the verification duty is split (T-1100), its `verify-fixture` and `verify-mechanism` refinements — is what step 5's fan-out decision point (above) now points at instead of deciding independently." So the election is made at Plan, recorded at the Specify-to-Implement seam, and *read* by the fan-out step inside Validate — three distinct phases the README's single clause currently collapses into one. This file is byte-identity-locked here.
- `bin/check-fanout-instances.sh` and `.github/workflows/check-handoff.yml` (`:346`–`:347`) — the distinction carried over, measured first-hand: the discriminator **exists as a shipped script** and its fixture suite runs as its own CI step (`- name: Run check-fanout-instances fixture suite` / `run: bash tests/check-fanout-instances/run.sh`). That pair is the evidence the repaired prose cites, and both are byte-identity-locked here.
- `.shell-team/specs/T-1019-is-span-row-parity.md` (`:88`–`:89`) — the distinction carried over: **AC10** is a preservation lock asserting `test -z "$(git diff --name-only develop -- bin tests/rollup-runs tests/cluster-failures)"`. Two properties follow rather than one: it reddens on any `bin/` edit, and its **one-argument** `git diff develop` form compares that ref against the **working tree**, so its verdict is a property of the checkout at read time. It is a merged frozen criterion; this task discloses it and repairs nothing.
- `.shell-team/specs/T-1003-retro-reads-interventions.md` (`:297`–`:307`) — the distinction carried over: **AC19** asserts six files byte-unchanged against `develop`, one of them `.github/workflows/check-handoff.yml`, and its own prose already declares itself **merge-point-scoped** and "expected to go stale once this task lands there". This diff touches none of its six files; whether it is already red at the branch point is measured, not assumed.
- `agents/pm-spec.md` (`:91`) — the distinction carried over: `mechanism` is the class "whenever the task's diff can reach an executing surface — **any path under `bin/`**, `tests/`, `templates/`, a CI workflow, or the semantics of a checker", and for that class the full-population downstream inventory, the whole CI-equivalent list and the mutation-probe matrix are all owed, with the `no-mechanism` read-set-scoped carve-out **unavailable**. `bin/check-count-claims.sh` is such a path, so the class is settled by the trigger and not by the diff's size.
- `CONTRIBUTING.md` `## What a version number encodes` (`:120`–`:131`) — the distinctions carried over: MINOR is gated **jointly** on the headline test and the default-reachability test, and passing one is not sufficient; PATCH covers bug fixes and internal accuracy work "regardless of diff size"; and T-1115's tiebreaker grants MINOR for a falsified behavioural claim only where the inversion is **of the default shipped path**.
- `.shell-team/interventions/T-1120.md` — the distinction carried over: this file **already exists and is committed on this branch** with one `assumption-contradicted` entry recording that the Plan briefing asserted #407 item 2's phrase was absent from the train head, and that the file's own words contradicted it minutes later. This role neither creates nor edits it; **AC15** reads it for conformance.

## Goal

<!-- BEGIN intent-block: T-1120 -->

- user-visible: yes — two of the four repaired sites are on the shipped adopter surface. `README.md`'s and `README.ja.md`'s `- **Within-phase fan-out**` bullet is documentation an adopter reads before deciding how their own loop elects fan-out, and this task changes what that bullet says about which phase makes the election; and `bin/check-count-claims.sh`'s refusal message is text an adopter's own close-out prints at them, whose current first suggestion costs them a second identical failure. This declaration is discharged by exactly one indented `- adopter-surface:` line, under **AC5**, naming both README bullets as where the adopter-facing documentation lands; this spec therefore carries **no** `- adopter-docs-waiver:` line. The documentation is not deferred to a fast-follow: **AC5** and **AC6** are freeze-time blockers and the repair *is* the deliverable.
- verification-class: mechanism — the diff reaches `bin/check-count-claims.sh`, a path under `bin/`, which `agents/pm-spec.md:91` makes the `mechanism` trigger outright; the trigger is **path-based, not judgment-based**, so the `no-mechanism` pricing-down of the downstream inventory is unavailable to this task however small the string edit is. Nothing is priced down: the downstream-impact inventory is a **full-population** two-arm diff over every merged spec's own criteria (a reverse-mapped `candidates` set is a lower bound on that population and never a substitute for it), the CI-equivalent step list is run whole with every step judged inapplicable named individually, and mutation probes are a matrix over this spec's own criteria rather than only the `- check:` lines it adds.
- verification-ceiling: unit-and-static — **AC1** through **AC16** are each reached by a static read of committed bytes or by a local run of pure-bash scripts against a scratch tree under `${TMPDIR:-/tmp}`; none needs a network call, a CI runner, an installed `shellcheck`, a `timeout` binary or an adopter machine, which is what makes this declaration non-vacuous rather than a floor. **AC17** is the one criterion above the ceiling and carries its own `- above-ceiling:` line: its subject is qa-verifier's own report, and what a criterion can read is the record's conformance rather than whether the reading it records actually happened.
- base-ref-discriminator: `B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi)` — the multi-arm form is the required value here rather than `not-applicable`, because criteria below read base-side blobs and the predecessor has an open pull request at authoring time (**A-3**, relayed). Measured first-hand by reading the ref files: the predecessor resolves in `refs/heads/`, so **arm 1** is what the existence test selects; it also resolves in `refs/remotes/origin/`, which is why arm 2 names that full ref path rather than a bare name; `develop` is this repository's integration branch and the fallback's parameter. `git merge-base` rather than a `rev-parse` of the predecessor tip is deliberate: this branch already carries a commit of its own, so branch point and predecessor tip are not interchangeable even today. The expression is spelled **byte-identically** in each of the **fourteen** criteria that resolve it — **AC1**, **AC2**, **AC4**, **AC5**, **AC6**, **AC7**, **AC8**, **AC9**, **AC10**, **AC11**, **AC12**, **AC13**, **AC14**, **AC15** — established by matching the exact string against each check line rather than by recollection. Those fourteen split three ways by **read kind**, and the three sets overlap rather than partition — three criteria appear in two of them, which is stated as a property rather than smoothed into a partition. **Twelve** read a base-side blob's **bytes** with `git show "$B:<path>"`: **AC1**, **AC2**, **AC4**, **AC5**, **AC6**, **AC7**, **AC8**, **AC9**, **AC10**, **AC11**, **AC12** and **AC13** — the last one reading `skills/run/SKILL.md`'s branch-point blob as its own positive control, which is why it is counted here and not only as a range read. **Five** read the **committed range** `"$B"...HEAD`: **AC5**, **AC6** and **AC7** for a per-file `--numstat` / `--name-only` delta, and **AC13** and **AC14** for the four-read union's committed arm. **One**, **AC15**, hands the resolved commit to `check-board-headings.sh --base` and `check-pii-shapes.sh --base`, which perform their own base-side reads. The union of the three sets is exactly the fourteen labelled above. Every criterion **not** in that list of fourteen was read for such a use and measured to have none — **AC3**, whose object is the working tree's own checker plus its fixture suite, **AC16**, whose object is this spec's own `## Blast radius` record and a live re-derivation, and **AC17**, whose object is the board's QA block.

**Goal (one sentence).** `bin/check-count-claims.sh`'s zero-count refusal message quotes **no unguarded pipeline anywhere in its teaching text** — every recipe it quotes exits `0` under `set -o pipefail` on a genuine zero match, while the `| wc -l` fragment its three merged readers depend on and the drop-`-c` double-counting teaching both survive — and the four stale claims at `docs/loop-engineering/phase-multiplexing.md:355`, `:365`–`:368` and `:373` plus both README mirrors' `- **Within-phase fan-out**` bullet are repaired **in place**, line-count-preserving, position-preserving and one-physical-line, so that every merged criterion pinning those bytes stays green except the two T-1112 clauses whose reddening is supersession by design and is disclosed rather than repaired.

**Why the `#409` repair is written against a class rather than against the reported case.** The issue reproduced one unguarded example. The message quotes **two**, and a repair that dropped only the reported one would leave the identical defect standing one clause later — the omission class this repository keeps catching. So the property the criteria measure is "**no unguarded pipeline is quoted anywhere in the teaching text**", derived from the rendered message rather than from the source line: every backtick-delimited span of the live refusal that names both `grep` and `wc -l` must carry the `|| true` guard, and every such span, executed live under `set -o pipefail` against a file with no match, must exit `0` and print `0`. The base-ref arm of the same probe is the positive control and the mechanical form of #409's own report: at the branch point at least one such span exits **non-zero**.

**Why `| wc -l` survives and what that costs the repair.** Three merged readers key on that fragment — T-1113 **AC6** greps the rendered stderr for it, T-1113 **AC9** uses it as the anchor proving the echo precedes execution, and the fixture suite reads it at `:140` and `:228`. AC9's own prose states *why* the anchor works: the echoed command text cannot contain it. So the repair keeps the guarded example `(grep zzz file || true) | wc -l` — which contains the fragment — and keeps the bare `| wc -l` mention, and drops only the unguarded intermediates, which no reader reads. The drop-`-c` teaching stays too, expressed without quoting a pipeline: the trap is named (`grep -c`) and its consequence stated (`double-count`), exactly as the script's own header already does at `:70`–`:72`.

**Why Site 4's repair is a status correction and never a disposition flip.** `:355`'s disposition token stays `staged-adoption`, because whether fan-out is adopted per axis or blanket is a design decision this task has no mandate to make; what moves is the **status of the named precondition**, which has shipped (`bin/check-fanout-instances.sh`, its fixture suite CI-wired at `.github/workflows/check-handoff.yml:346`–`:347`) and whose sibling was already recorded `DISCHARGED` at `:375`. The precondition keeps its **name** on the line — T-1112 AC3 requires `per-instance-telemetry-discriminator` to stay there, so precondition (1) is not quietly deleted along with the repair of its status — and the file's seventh `- invariant-lock:` line at `:351`, which records the same token as "a follow-up only" for **T-1078's own pilot scope**, is left standing untouched: it is a true statement about that pilot, not a claim about today's repository.

**Why `:373` is in scope (DP-A) and how it must not be repaired.** The same now-false open status is asserted twice, two lines apart: at `:355` and again at `:373`'s `- follow-up: design the …` line. Repairing one and leaving the other is the omission class above, so both move. But the repair of `:373` may **not** begin `- follow-up: DISCHARGED`: T-1112 **AC3** extracts every line matching `^- follow-up: DISCHARGED` and compares it with `cmp` against a base extraction holding exactly one line, so a second such line would redden a criterion this task is required to keep green. The repaired line keeps a `- follow-up: ` prefix, states the discharge in its body, and names the shipped script.

**Why two merged clauses go red, and why that is supersession rather than breakage.** T-1112 **AC1**'s sibling clause (the `verify` and `verify-fixture` rule lines byte-identical base↔HEAD) and **AC2**'s non-`verify-mechanism` sorted-set clause both redden the moment `:365`–`:368` are reworded. Those clauses were authored to prove that T-1112 flipped **only** the `verify-mechanism` refinement; a later deliberate prose repair of the sibling lines is exactly what they were not written to forbid. Both are disclosed in `## Blast radius` and neither is repaired, re-based or annotated — repairing another task's frozen check line is a class-B re-freeze of someone else's intent. What this task **does** owe, and **AC12** measures, is that the supersession is *scoped*: the `verify-mechanism` rule lines stay byte-identical, no `- dispatch-axis:` line moves, and T-1100 AC6's four per-refinement prefix counts are each still exactly one.

## Non-goals

- **No behavioural change to `bin/check-count-claims.sh` beyond the message text.** No new flag, no new exit code, no change to the grammar, the trim rule, the timeout wrapper, the echo contract or the `set -o pipefail` child shell. The edit is confined to the one `fail` call on the non-zero-exit branch, and **AC4** is the mechanical form of that: everything above and below that line is byte-identical to the branch point.
- **No edit to the script's header comment.** Its idiom at `:64`–`:73` is already correct — re-measured this run — so no repair is owed there, and **AC4**'s head comparison would redden if one landed.
- **No new fixture, no new `tests/` directory and no edit to `tests/check-count-claims/run.sh`.** The suite's positive control at `:305` already exercises the guarded form, as #409 itself notes; the behavioural half of this task's own claim lives in **AC2**'s check line rather than in the shipped suite. **AC13** enforces this.
- **No edit to `skills/run/SKILL.md`.** It is measured correct at `:281` and is the source the README repair states; it is byte-identity-locked in **AC13**.
- **No edit to `bin/check-fanout-instances.sh`, `bin/aggregate-verdicts.sh`, any file under `templates/`, any file under `agents/`, or `.github/workflows/check-handoff.yml`.** The doc repair *cites* the shipped discriminator and its CI step; citing is not touching. **AC13** enforces this as a four-read union over those paths.
- **No flip of any disposition, recommendation or dispatch rule in `docs/loop-engineering/phase-multiplexing.md`.** `:355` keeps `staged-adoption`; `:369`'s already-unconditional `verify-mechanism` rule and `:370`'s conditional flip-back are untouched; no `- dispatch-axis:` line, no `- tier:` line, no `- saving:` line and no `- invariant-lock:` line moves. Only the **status** of a named precondition is corrected, on four rule/recommendation lines and one follow-up line.
- **No edit to `:375`.** T-1112 AC3 pins the `- follow-up: DISCHARGED` line byte-identical, and this task cites that discharge rather than restating, re-deriving or re-wording it.
- **No edit to the `pilot3-evidence` region and no `^## ` heading added, moved or removed.** Both are T-1112 AC2 clauses that can and must stay green (**AC11**).
- **No line added to or removed from `docs/loop-engineering/phase-multiplexing.md`, `README.md` or `README.ja.md`.** Every repair is an in-place rewrite of one physical line, because the board's own dispatch grounds and T-1075 AC6's `- adopter-surface:` line cite these files **by line number**. **AC5**, **AC6** and **AC11** measure this as total-line-count equality plus, for the READMEs, a `1`-added/`1`-removed `--numstat` per file and an equal bullet line number at both refs.
- **No repair, re-basing or annotation of any merged criterion this diff reddens** — T-1112 AC1 and AC2 by design, T-1019 AC10 on the `bin/` edit, and whatever the two-arm sweep finds. Every red is disclosed in `## Blast radius` and left alone.
- **No new `- invariant-lock:` line, no new dispatch axis or value, no new status flag, no new phase and no new human gate.** The three standing human gates are untouched.
- **No CHANGELOG edit, no version bump, no plugin-manifest edit, no lessons-corpus edit, no board archiving and no retro.** The version derivation is recorded on the board by the coordinating session at freeze; writing release notes is not this task's act.
- **No closing of #409 or #407, and no merge.** Both issues close at the sprint r3-body batch GO; this task's own scope ends at `READY_FOR_MERGE`.
- **No spec review at the Specify seam.** `spec-review — none` is the elected value: both halves of this task turn on facts this repository measures itself — bash `pipefail` semantics under its own fixture suite, and the truth of four claims against `skills/run/SKILL.md:281`, `phase-multiplexing.md:369`/`:375` and a shipped script with a CI-wired suite. There is no domain premise this repository cannot measure.
- **Nothing about the verification protocol is priced down.** This task's class is `mechanism`, so the full-population sweep, the whole CI-equivalent list and the mutation-probe matrix are all owed; declaring that here is the counterpart of the `no-mechanism` carve-out this task does **not** take.

## Acceptance criteria

Every check runs from the repository root under `bash`, so `grep` resolves to the platform's own `/usr/bin/grep` (BSD on this host, whose interval-matching cap is 255 — no criterion below uses a bounded-repetition interval); resolves the branch point with the byte-identical multi-arm expression declared above and fails closed when it does not resolve; resolves operating paths through `bin/team-paths.sh` wherever a board or record path is read; names the files it reads explicitly rather than searching the tree; guards every `mktemp` with an immediate non-zero exit; writes nothing outside `$TMPDIR`; **reads every Japanese line whole** rather than cutting a byte window, because a window whose cut lands mid-UTF-8-sequence makes BSD `grep` treat the whole extract as binary and report false absence for every literal in it, including ASCII ones (issue #434); and pairs every absence assertion with a positive control, so an unreadable, empty or misnamed target reads as a failure rather than as a clean absence. Where a criterion's subject is a count, it is stated as a base-relative delta or as an invariant that re-derives both sides at run time. **AC1**–**AC10** and **AC15**–**AC17** are expected red at freeze and satisfiable once the deliverables exist; **AC11**, **AC12**, **AC13** and **AC14** are green now and required to stay green. There are 17 criteria.

- [ ] **AC1** The refusal message quotes **no unguarded pipeline anywhere in its teaching text**, measured on the rendered message rather than on the source line, with the branch point's own message as the positive control that the probe fires. A scratch single-entry board carrying one row whose command is `grep -c zzz /dev/null` (the named trap: it prints `0` and exits `1`) is run through the working-tree checker and through the **branch-point committed blob** of that same script, executed as a script from `$TMPDIR`; each arm exits `1`; the refusal line is located on each arm's combined output as the first line carrying `| wc -l`; and each arm's backtick-delimited spans are extracted from that line, of which the **pipeline spans** are those naming both `grep` and `wc -l`. Then: each arm carries **at least one** pipeline span (the extraction is proven to work on both sides); at `HEAD` **zero** pipeline spans match `grep[^|]*\| wc -l` and **zero** lack the guard literal `|| true`; at the branch point **at least one** pipeline span matches that same pattern — the mechanical form of #409's report, and the control proving the pattern is capable of matching. The surviving teaching is asserted directly: the `HEAD` message carries the fragment `| wc -l` (what T-1113 **AC6** and the fixture suite read), the guarded recipe `(grep zzz file || true) | wc -l`, the trap's name `grep -c`, and the consequence literal `double-count`. The pattern is applied **per span** rather than to the whole line deliberately: applied line-wide it matches across two separate backtick spans wherever no `|` character sits between them, which the current message's own text does. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; P=bin/check-count-claims.sh; test -r "$P" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; BT=$(printf '\140'); T=$(mktemp -d "${TMPDIR:-/tmp}/t1120a.XXXXXX") || exit 1; git show "$B:$P" > "$T/base.sh" 2>/dev/null || rc=1; test -s "$T/base.sh" || rc=1; { printf '%s\n' '# Fixture' '' '## Active' '' '- [ ] **T-901** Fixture entry' '  - count: sites — 0 — command: grep -c zzz /dev/null'; } > "$T/b.md"; test -s "$T/b.md" || rc=1; for arm in head base; do if [ "$arm" = head ]; then S="$P"; else S="$T/base.sh"; fi; bash "$S" --board "$T/b.md" --task T-901 > "$T/out.$arm" 2>&1; s=$?; test "$s" = "1" || rc=1; grep -F -- '| wc -l' "$T/out.$arm" | head -1 > "$T/msg.$arm"; test -s "$T/msg.$arm" || rc=1; awk -F"$BT" '{for(i=2;i<=NF;i+=2) print $i}' "$T/msg.$arm" | grep -F -- 'grep' | grep -F -- 'wc -l' > "$T/pipe.$arm" || true; test "$(grep -c . "$T/pipe.$arm" || true)" -ge 1 || rc=1; done; test "$(grep -cE -- 'grep[^|]*\| wc -l' "$T/pipe.base" || true)" -ge 1 || rc=1; test "$(grep -cE -- 'grep[^|]*\| wc -l' "$T/pipe.head" || true)" = "0" || rc=1; test "$(grep -vF -- '|| true' "$T/pipe.head" | grep -c . || true)" = "0" || rc=1; grep -qF -- '| wc -l' "$T/msg.head" || rc=1; grep -qF -- '(grep zzz file || true) | wc -l' "$T/msg.head" || rc=1; grep -qF -- 'grep -c' "$T/msg.head" || rc=1; grep -qF -- 'double-count' "$T/msg.head" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC2** The message's own recommended recipe **runs**: every pipeline span it quotes exits `0` and reads `0` under `set -o pipefail` on a genuine zero match, and the branch point's own message fails that same test. The extraction of **AC1** is repeated on both arms; each arm's pipeline spans are executed one at a time as `bash -c "set -o pipefail; <span>"` with the working directory set to a scratch directory containing a file named `file` whose single line does not match `zzz`. At `HEAD`: at least one pipeline span exists, **every** one exits `0`, and **every** one's trimmed stdout is exactly `0` — so "exits 0 on a genuine zero match" is measured as a reading and not merely as a status. At the branch point: at least one pipeline span exists and **at least one** exits non-zero, which is the defect #409 reported, reproduced rather than quoted. Positive controls: both arms' extractions are asserted non-empty before either is executed; the scratch `file` is created before either arm runs and its content is asserted to be a real non-matching line by the `HEAD` arm's own `0` reading. This criterion is the behavioural half of the class in **AC1**, and neither substitutes for the other — a span that is guarded but wrong, and a span that runs by accident while quoting an unguarded form, are different defects. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; P=bin/check-count-claims.sh; test -r "$P" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; BT=$(printf '\140'); T=$(mktemp -d "${TMPDIR:-/tmp}/t1120b.XXXXXX") || exit 1; mkdir -p "$T/run" || exit 1; printf '%s\n' 'hello' > "$T/run/file" || exit 1; test -s "$T/run/file" || rc=1; git show "$B:$P" > "$T/base.sh" 2>/dev/null || rc=1; test -s "$T/base.sh" || rc=1; { printf '%s\n' '# Fixture' '' '## Active' '' '- [ ] **T-901** Fixture entry' '  - count: sites — 0 — command: grep -c zzz /dev/null'; } > "$T/b.md"; test -s "$T/b.md" || rc=1; for arm in head base; do if [ "$arm" = head ]; then S="$P"; else S="$T/base.sh"; fi; bash "$S" --board "$T/b.md" --task T-901 > "$T/out.$arm" 2>&1; s=$?; test "$s" = "1" || rc=1; grep -F -- '| wc -l' "$T/out.$arm" | head -1 > "$T/msg.$arm"; test -s "$T/msg.$arm" || rc=1; awk -F"$BT" '{for(i=2;i<=NF;i+=2) print $i}' "$T/msg.$arm" | grep -F -- 'grep' | grep -F -- 'wc -l' > "$T/pipe.$arm" || true; test "$(grep -c . "$T/pipe.$arm" || true)" -ge 1 || rc=1; n=0; bad=0; zero=0; while IFS= read -r span; do test -n "$span" || continue; n=$((n+1)); o=$( cd "$T/run" && bash -c "set -o pipefail; $span" 2>/dev/null ); st=$?; if [ "$st" -ne 0 ]; then bad=$((bad+1)); fi; if [ "$(printf '%s' "$o" | tr -d ' \n')" = "0" ]; then zero=$((zero+1)); fi; done < "$T/pipe.$arm"; printf '%s %s %s\n' "$n" "$bad" "$zero" > "$T/res.$arm"; done; read -r hn hbad hzero < "$T/res.head"; read -r bn bbad bzero < "$T/res.base"; test "$((10#$hn))" -ge 1 || rc=1; test "$((10#$hbad))" = "0" || rc=1; test "$((10#$hzero))" = "$((10#$hn))" || rc=1; test "$((10#$bn))" -ge 1 || rc=1; test "$((10#$bbad))" -ge 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC3** The three merged readers of the `| wc -l` fragment still read what they read, re-derived from their own mechanisms rather than cited by line number. On the rendered refusal of the same `grep -c zzz /dev/null` fixture: the combined output carries the fragment `| wc -l` (T-1113 **AC6**'s assertion, re-derived), and the line beginning `check-count-claims: count sites` appears on a **strictly earlier** line than the first line carrying that fragment (T-1113 **AC9**'s ordering anchor, re-derived, with both extracted line numbers asserted to be non-empty digit runs before they are compared so a missing line cannot satisfy the ordering by defaulting). In `tests/check-count-claims/run.sh`: the fragment occurs on **at least two** lines and the quoted form `'| wc -l'` — the shape both of that suite's readers pass as an argument — occurs at least once. And the suite itself exits `0`. Positive control: the fixture board is asserted non-empty and the invocation is asserted to exit `1`, so the ordering is read from a run that genuinely refused. **Expected red at freeze** — the suite is green now, but its verdict is part of this criterion and the criterion as a whole is only satisfiable once the repaired message exists.
  - check: rc=0; export LC_ALL=C; P=bin/check-count-claims.sh; S=tests/check-count-claims/run.sh; test -r "$P" || exit 1; test -r "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120c.XXXXXX") || exit 1; { printf '%s\n' '# Fixture' '' '## Active' '' '- [ ] **T-901** Fixture entry' '  - count: sites — 0 — command: grep -c zzz /dev/null'; } > "$T/b.md"; test -s "$T/b.md" || rc=1; bash "$P" --board "$T/b.md" --task T-901 > "$T/o" 2>&1; s=$?; test "$s" = "1" || rc=1; grep -qF -- '| wc -l' "$T/o" || rc=1; e=$(grep -n -- '^check-count-claims: count sites' "$T/o" | head -1 | cut -d: -f1); w=$(grep -nF -- '| wc -l' "$T/o" | head -1 | cut -d: -f1); case "${e:-}" in ''|*[!0-9]*) rc=1 ;; esac; case "${w:-}" in ''|*[!0-9]*) rc=1 ;; esac; test "$((10#${e:-0}))" -lt "$((10#${w:-0}))" || rc=1; test "$(grep -cF -- '| wc -l' "$S" || true)" -ge 2 || rc=1; grep -qF -- "'| wc -l'" "$S" || rc=1; bash "$S" >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** The `bin/` edit is confined to the one `fail` call, so nothing above it shifts and nothing below it moves. In `bin/check-count-claims.sh`, comparing the working tree against the branch-point committed blob: the total line count is equal at both refs and non-zero; the line carrying the fixed string `command exited $rc` is found at the **same line number** at both refs and that number is a non-empty digit run; everything **above** that line is byte-identical; everything **below** it is byte-identical; and that line itself **differs**. The header's own correct idiom is asserted inside the compared head region rather than file-wide: the guarded literal `(grep zzz file || true) | wc -l` is present in the branch-point blob and present in the `HEAD` head region, so "no header edit is owed and none landed" is measured rather than promised. Positive controls: the base blob is asserted non-empty; both head extractions are asserted non-empty before they are compared; and both single-line extractions are asserted non-empty before the difference is judged, so a comparison of two empty extractions cannot read as a difference. **Expected red at freeze** — at the branch point that line is byte-identical to itself, which is exactly what the final clause forbids.
  - check: rc=0; export LC_ALL=C; P=bin/check-count-claims.sh; test -r "$P" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120d.XXXXXX") || exit 1; git show "$B:$P" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; lb=$(wc -l < "$T/base" | tr -d ' '); lh=$(wc -l < "$P" | tr -d ' '); test -n "$lb" || rc=1; test "$((10#${lb:-0}))" -gt 0 || rc=1; test "$lb" = "$lh" || rc=1; nb=$(grep -nF -- 'command exited $rc' "$T/base" | head -1 | cut -d: -f1); nh=$(grep -nF -- 'command exited $rc' "$P" | head -1 | cut -d: -f1); case "${nb:-}" in ''|*[!0-9]*) rc=1 ;; esac; case "${nh:-}" in ''|*[!0-9]*) rc=1 ;; esac; test "$nb" = "$nh" || rc=1; if [ "${nh:-0}" -gt 1 ] 2>/dev/null; then sed -n "1,$((nh-1))p" "$T/base" > "$T/hb"; sed -n "1,$((nh-1))p" "$P" > "$T/hh"; else rc=1; : > "$T/hb"; : > "$T/hh"; fi; test -s "$T/hb" || rc=1; test -s "$T/hh" || rc=1; cmp -s "$T/hb" "$T/hh" || rc=1; sed -n "$((${nh:-0}+1)),\$p" "$T/base" > "$T/tb"; sed -n "$((${nh:-0}+1)),\$p" "$P" > "$T/th"; test -s "$T/tb" || rc=1; cmp -s "$T/tb" "$T/th" || rc=1; sed -n "${nh:-0}p" "$T/base" > "$T/lb"; sed -n "${nh:-0}p" "$P" > "$T/lh"; test -s "$T/lb" || rc=1; test -s "$T/lh" || rc=1; cmp -s "$T/lb" "$T/lh" && rc=1; grep -qF -- '(grep zzz file || true) | wc -l' "$T/base" || rc=1; grep -qF -- '(grep zzz file || true) | wc -l' "$T/hh" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** `README.md`'s `- **Within-phase fan-out**` bullet says who elects and who reads, on the same physical line and at the same line number. In `README.md`: exactly one line carries the anchor `- **Within-phase fan-out**` at both refs; that line's own **line number** is equal at both refs; the file's total line count is equal at both refs and non-zero; the committed range's `--numstat` for this file is exactly one added and one removed line, so the bullet was rewritten in place rather than reflowed, split or moved; the stale fragment `explicit per-task choice made inside the Validate phase` occurs exactly once in the branch-point blob's own extracted bullet and **zero** times anywhere in the file at `HEAD`; and the `HEAD` bullet carries all four of `Plan`, `Specify`, `Validate` and `verify` — `Validate` because T-1075 **AC6** greps that literal inside this very bullet, and the other three because the repaired claim is that the election is a Plan-time `verify`-axis decision transcribed at the Specify-to-Implement seam. T-1075 **AC5**'s constraints are re-derived rather than assumed safe: each of the four case-insensitive phrases `always fan out`, `fans out by default`, `automatically fan` and `fanned out by default` occurs **zero** times in this file, and the literal `Opt-in only — no phase is rewired to fan out by default` is still present. Positive controls: the base blob is asserted non-empty; both extracted bullets are asserted non-empty before any search; and the base-side extraction is asserted to carry the stale fragment, so a mis-anchored extraction cannot satisfy the absence half. **Expected red at freeze.**
  - adopter-surface: the `- **Within-phase fan-out**` bullet in `README.md`'s `## Operating loop` list and its `- **フェーズ内 fan-out**` mirror in `README.ja.md` (line 174 in both files at authoring time), each having its stale "chosen inside the Validate phase" clause replaced by the truth `skills/run/SKILL.md:281` already states — extending the bullets T-1074/T-1075 shipped rather than adding a new adopter surface. This is the single `- adopter-surface:` line the `- user-visible: yes` declaration is discharged by; **AC6** carries the JA half's own criteria.
  - check: rc=0; export LC_ALL=C; F=README.md; A='- **Within-phase fan-out**'; test -s "$F" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120e.XXXXXX") || exit 1; git show "$B:$F" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; test "$(grep -acF -- "$A" "$T/base" || true)" = "1" || rc=1; test "$(grep -acF -- "$A" "$F" || true)" = "1" || rc=1; grep -am1 -F -- "$A" "$T/base" > "$T/bb" || rc=1; grep -am1 -F -- "$A" "$F" > "$T/hb" || rc=1; test -s "$T/bb" || rc=1; test -s "$T/hb" || rc=1; pb=$(grep -anF -- "$A" "$T/base" | head -1 | cut -d: -f1); ph=$(grep -anF -- "$A" "$F" | head -1 | cut -d: -f1); case "${pb:-}" in ''|*[!0-9]*) rc=1 ;; esac; case "${ph:-}" in ''|*[!0-9]*) rc=1 ;; esac; test "$pb" = "$ph" || rc=1; lb=$(wc -l < "$T/base" | tr -d ' '); lh=$(wc -l < "$F" | tr -d ' '); test "$((10#${lb:-0}))" -gt 0 || rc=1; test "$lb" = "$lh" || rc=1; test "$(git diff --no-renames --numstat "$B"...HEAD -- "$F" | head -1)" = "$(printf '1\t1\t%s' "$F")" || rc=1; test "$(grep -acF -- 'explicit per-task choice made inside the Validate phase' "$T/bb" || true)" = "1" || rc=1; test "$(grep -acF -- 'explicit per-task choice made inside the Validate phase' "$F" || true)" = "0" || rc=1; for lit in 'Plan' 'Specify' 'Validate' 'verify'; do grep -aqF -- "$lit" "$T/hb" || rc=1; done; for p in 'always fan out' 'fans out by default' 'automatically fan' 'fanned out by default'; do test "$(grep -aciF -- "$p" "$F" || true)" = "0" || rc=1; done; grep -aqF -- 'Opt-in only — no phase is rewired to fan out by default' "$F" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** `README.ja.md`'s mirror says the same thing, with its machine tokens in English, read whole-line. In `README.ja.md`: exactly one line carries the anchor `- **フェーズ内 fan-out**` at both refs; that line's own line number is equal at both refs; the file's total line count is equal at both refs and non-zero; the committed range's `--numstat` for this file is exactly one added and one removed line; the stale fragment `依然として Validate フェーズの中でタスクごとに明示的に選ぶ` occurs exactly once in the branch-point blob's own extracted bullet and **zero** times anywhere in the file at `HEAD`; the `HEAD` bullet carries all four English machine tokens `Plan`, `Specify`, `Validate` and `verify` verbatim — `Validate` because T-1075 **AC6** greps it inside this bullet too, and the tokens in English because that is this file's own shipped convention; and T-1075 **AC5**'s constraints hold here as well (the four forbidden phrases zero, the literal `あくまで opt-in` still present). Every read is of a **whole line**: no byte window is cut anywhere in this criterion, because a cut landing mid-UTF-8-sequence makes BSD `grep` report false absence for every literal in the extract (issue #434), and every `grep` reading a Japanese extract passes `-a` so a stray non-text byte cannot silently convert a real match into "binary file matches". Positive controls: the base blob is asserted non-empty; both extracted bullets are asserted non-empty; and the base-side extraction is asserted to carry the stale fragment. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; F=README.ja.md; A='- **フェーズ内 fan-out**'; test -s "$F" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120f.XXXXXX") || exit 1; git show "$B:$F" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; test "$(grep -acF -- "$A" "$T/base" || true)" = "1" || rc=1; test "$(grep -acF -- "$A" "$F" || true)" = "1" || rc=1; grep -am1 -F -- "$A" "$T/base" > "$T/bb" || rc=1; grep -am1 -F -- "$A" "$F" > "$T/hb" || rc=1; test -s "$T/bb" || rc=1; test -s "$T/hb" || rc=1; pb=$(grep -anF -- "$A" "$T/base" | head -1 | cut -d: -f1); ph=$(grep -anF -- "$A" "$F" | head -1 | cut -d: -f1); case "${pb:-}" in ''|*[!0-9]*) rc=1 ;; esac; case "${ph:-}" in ''|*[!0-9]*) rc=1 ;; esac; test "$pb" = "$ph" || rc=1; lb=$(wc -l < "$T/base" | tr -d ' '); lh=$(wc -l < "$F" | tr -d ' '); test "$((10#${lb:-0}))" -gt 0 || rc=1; test "$lb" = "$lh" || rc=1; test "$(git diff --no-renames --numstat "$B"...HEAD -- "$F" | head -1)" = "$(printf '1\t1\t%s' "$F")" || rc=1; test "$(grep -acF -- '依然として Validate フェーズの中でタスクごとに明示的に選ぶ' "$T/bb" || true)" = "1" || rc=1; test "$(grep -acF -- '依然として Validate フェーズの中でタスクごとに明示的に選ぶ' "$F" || true)" = "0" || rc=1; for lit in 'Plan' 'Specify' 'Validate' 'verify'; do grep -aqF -- "$lit" "$T/hb" || rc=1; done; for p in 'always fan out' 'fans out by default' 'automatically fan' 'fanned out by default'; do test "$(grep -aciF -- "$p" "$F" || true)" = "0" || rc=1; done; grep -aqF -- 'あくまで opt-in' "$F" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** The two mirrors moved together and attribute the election and the reading to the **same** phases in both languages. The set of changed `README*` paths in the committed range is exactly `README.ja.md` and `README.md`, derived and compared as a sorted list rather than asserted. In **each** of the two extracted `HEAD` bullets, all four of `Plan`, `Specify`, `Validate` and `verify` are present, and the first occurrence of `Plan` sits at a **strictly smaller** byte offset than the first occurrence of `Validate` — the ordering that encodes "elected at Plan, read at Validate" and the one a mirror that swapped them would fail. Offsets are computed with `awk`'s `index` over the **whole** line, never by cutting a window, so the Japanese line is measured without a UTF-8 hazard. Positive controls: both extracted bullets are asserted non-empty and both computed offsets are asserted to be non-zero digit runs before they are compared, so a literal that is absent cannot satisfy the ordering by defaulting to `0`; and the changed-path list is asserted non-empty before the comparison. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120g.XXXXXX") || exit 1; git diff --no-renames --name-only "$B"...HEAD -- 'README*' | LC_ALL=C sort > "$T/ch"; test -s "$T/ch" || rc=1; printf '%s\n' README.ja.md README.md > "$T/exp"; cmp -s "$T/ch" "$T/exp" || rc=1; i=0; for pair in 'README.md=- **Within-phase fan-out**' 'README.ja.md=- **フェーズ内 fan-out**'; do i=$((i+1)); f=${pair%%=*}; a=${pair#*=}; test -s "$f" || rc=1; grep -am1 -F -- "$a" "$f" > "$T/h$i" || rc=1; test -s "$T/h$i" || rc=1; for lit in 'Plan' 'Specify' 'Validate' 'verify'; do grep -aqF -- "$lit" "$T/h$i" || rc=1; done; op=$(awk -v s='Plan' '{print index($0,s); exit}' "$T/h$i"); ov=$(awk -v s='Validate' '{print index($0,s); exit}' "$T/h$i"); case "${op:-0}" in ''|0|*[!0-9]*) rc=1 ;; esac; case "${ov:-0}" in ''|0|*[!0-9]*) rc=1 ;; esac; test "$((10#${op:-0}))" -lt "$((10#${ov:-0}))" || rc=1; done; test "$i" = "2" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** `phase-multiplexing.md`'s `- recommendation: tier1-verification-fanout` line states the precondition's real status while keeping its name, its disposition and every literal a merged criterion reads. In `docs/loop-engineering/phase-multiplexing.md`: exactly one line begins `- recommendation: tier1-verification-fanout — ` at each ref; the branch-point blob's own extracted line carries the stale fragment `gated explicitly on one named precondition`, and that fragment occurs **zero** times anywhere in the file at `HEAD`; the `HEAD` line carries all five literals T-1112 **AC3** requires — `mechanism-class`, `execution host`, `judgment`, `staged-adoption` and `per-instance-telemetry-discriminator` — so neither the disposition token nor the precondition's name is dropped along with the repair of its status; the agent-shaped literal `can actually launch N *agent* instances` occurs **zero** times at `HEAD`, the state T-1112 left and this task preserves; and the `HEAD` line names the shipped mechanism `bin/check-fanout-instances.sh` and carries the fixed status fragment `no longer gated`. Positive controls: the base blob is asserted non-empty; both extracted lines are asserted non-empty before any search; and the base-side extraction is asserted to carry the stale fragment, so a failed extraction cannot satisfy the repair clause by matching nothing. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120h.XXXXXX") || exit 1; git show "$B:$N" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; test "$(grep -c -- '^- recommendation: tier1-verification-fanout — ' "$T/base" || true)" = "1" || rc=1; test "$(grep -c -- '^- recommendation: tier1-verification-fanout — ' "$N" || true)" = "1" || rc=1; grep -- '^- recommendation: tier1-verification-fanout — ' "$T/base" > "$T/rb" || rc=1; grep -- '^- recommendation: tier1-verification-fanout — ' "$N" > "$T/rh" || rc=1; test -s "$T/rb" || rc=1; test -s "$T/rh" || rc=1; grep -qF -- 'gated explicitly on one named precondition' "$T/rb" || rc=1; test "$(grep -cF -- 'gated explicitly on one named precondition' "$N" || true)" = "0" || rc=1; for s in 'mechanism-class' 'execution host' 'judgment' 'staged-adoption' 'per-instance-telemetry-discriminator' 'bin/check-fanout-instances.sh' 'no longer gated'; do grep -qF -- "$s" "$T/rh" || rc=1; done; test "$(grep -cF -- 'can actually launch N *agent* instances' "$N" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC9** The four sibling rule lines stop asserting two unbuilt preconditions, and every one of them keeps the exact grammar prefix three merged criteria count by. In `docs/loop-engineering/phase-multiplexing.md`, at **both** refs, each of these four prefixes matches exactly one line: `^- dispatch-rule: verify — serial — unconditional — `, `^- dispatch-rule: verify — tier1-fanout — conditional — trigger: `, `^- dispatch-rule: verify-fixture — serial — unconditional — ` and `^- dispatch-rule: verify-fixture — tier1-fanout — conditional — trigger: ` — including the `trigger: ` token on both conditionals, because T-1100 **AC6** and T-1112 **AC1** both count by these prefixes. The stale status claims are gone file-wide: `two unbuilt preconditions` occurs exactly **2** times in the branch-point blob and **zero** times at `HEAD`; `preconditions exist` occurs exactly **1** time in the branch-point blob and **zero** times at `HEAD`. The repair is real rather than nominal: the concatenation of those four lines **differs** between the two refs, and each of the four `HEAD` lines still carries `priced from ` and still names `recommendation: tier1-verification-fanout`, so no rule line loses the priced ground its own grammar requires. Positive controls: the base blob is asserted non-empty; both four-line extractions are asserted to hold exactly four lines before they are compared; and each base-side count is asserted greater than zero before its `HEAD` counterpart is required to be zero. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120i.XXXXXX") || exit 1; git show "$B:$N" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; : > "$T/sb"; : > "$T/sh"; for pre in '^- dispatch-rule: verify — serial — unconditional — ' '^- dispatch-rule: verify — tier1-fanout — conditional — trigger: ' '^- dispatch-rule: verify-fixture — serial — unconditional — ' '^- dispatch-rule: verify-fixture — tier1-fanout — conditional — trigger: '; do test "$(grep -c -- "$pre" "$T/base" || true)" = "1" || rc=1; test "$(grep -c -- "$pre" "$N" || true)" = "1" || rc=1; grep -- "$pre" "$T/base" >> "$T/sb" || rc=1; grep -- "$pre" "$N" >> "$T/sh" || rc=1; done; test "$(grep -c . "$T/sb" || true)" = "4" || rc=1; test "$(grep -c . "$T/sh" || true)" = "4" || rc=1; cmp -s "$T/sb" "$T/sh" && rc=1; test "$(grep -cF -- 'two unbuilt preconditions' "$T/base" || true)" = "2" || rc=1; test "$(grep -cF -- 'two unbuilt preconditions' "$N" || true)" = "0" || rc=1; test "$(grep -cF -- 'preconditions exist' "$T/base" || true)" = "1" || rc=1; test "$(grep -cF -- 'preconditions exist' "$N" || true)" = "0" || rc=1; test "$(grep -cF -- 'priced from ' "$T/sh" || true)" = "4" || rc=1; test "$(grep -cF -- 'recommendation: tier1-verification-fanout' "$T/sh" || true)" = "4" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC10** DP-A: the follow-up line two lines below the repaired recommendation stops asserting the same now-false open status, **without** minting a second `DISCHARGED` follow-up. In `docs/loop-engineering/phase-multiplexing.md`: exactly one line matches `^- follow-up: design the ` in the branch-point blob and it carries `per-instance-telemetry-discriminator`; **zero** lines match that prefix at `HEAD`; at `HEAD` exactly one `^- follow-up: ` line carries `per-instance-telemetry-discriminator`, and that line carries both `bin/check-fanout-instances.sh` and the lowercase status word `discharged`; and the total count of `^- follow-up: ` lines is equal at both refs, so the repair rewrote a line rather than adding or removing one. The collision this criterion exists to prevent is measured directly: the extraction `^- follow-up: DISCHARGED` holds exactly **one** line at each ref and is **byte-identical** between them — the same extraction T-1112 **AC3** compares with `cmp`, so a repaired line beginning with that prefix would break a merged criterion this task is required to keep green. Positive controls: the base blob is asserted non-empty; the base-side `design the ` extraction is asserted non-empty before its `HEAD` absence is judged; and both `DISCHARGED` extractions are asserted non-empty before they are compared. **Expected red at freeze.**
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120j.XXXXXX") || exit 1; git show "$B:$N" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; test "$(grep -c -- '^- follow-up: design the ' "$T/base" || true)" = "1" || rc=1; grep -- '^- follow-up: design the ' "$T/base" > "$T/db" || rc=1; test -s "$T/db" || rc=1; grep -qF -- 'per-instance-telemetry-discriminator' "$T/db" || rc=1; test "$(grep -c -- '^- follow-up: design the ' "$N" || true)" = "0" || rc=1; grep -- '^- follow-up: ' "$N" | grep -F -- 'per-instance-telemetry-discriminator' > "$T/dh" || true; test "$(grep -c . "$T/dh" || true)" = "1" || rc=1; grep -qF -- 'bin/check-fanout-instances.sh' "$T/dh" || rc=1; grep -qF -- 'discharged' "$T/dh" || rc=1; fb=$(grep -c -- '^- follow-up: ' "$T/base" || true); fh=$(grep -c -- '^- follow-up: ' "$N" || true); test "$((10#${fb:-0}))" -gt 0 || rc=1; test "$fb" = "$fh" || rc=1; grep -- '^- follow-up: DISCHARGED' "$T/base" > "$T/xb" || rc=1; grep -- '^- follow-up: DISCHARGED' "$N" > "$T/xh" || rc=1; test "$(grep -c . "$T/xb" || true)" = "1" || rc=1; test "$(grep -c . "$T/xh" || true)" = "1" || rc=1; cmp -s "$T/xb" "$T/xh" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** The note's citable structure is preserved, asserted as base-versus-`HEAD` invariants rather than as literals — the clauses of T-1112 **AC2** that can and must stay green, re-derived here independently of that criterion's own base resolution. In `docs/loop-engineering/phase-multiplexing.md`: the total line count is equal at both refs and non-zero, so every line number the board's dispatch grounds cite still resolves; the occurrence counts of `^- dispatch-axis:`, `^- dispatch-rule:` and `^- dispatch-note:` are each equal at both refs and each at least two; the concatenation of every `- dispatch-axis:` line is byte-identical, so no axis key or closed value set moved; the sorted set of `- dispatch-rule: implement — ` lines is byte-identical, so the implement axis's three rules are untouched; the list of `^## ` headings is byte-identical; the `pilot3-evidence` region, from its `BEGIN` marker line through its `END` marker line inclusive, is byte-identical and non-empty; and the sets of `^- tier: `, `^- saving: ` and `^- invariant-lock: ` lines are each byte-identical, so the priced tier inventory at `:80` and the seven invariant locks — including the one at `:351` recording the discriminator as a follow-up **of T-1078's own pilot scope** — are left exactly as they stand. Positive controls: the base blob is asserted non-empty; every extraction is asserted non-empty on the base side before its comparison; and the implement-rule extraction is asserted to hold at least one line, so an extraction that captured nothing cannot satisfy the identity. **Green at freeze and required to stay green** — this is a preservation lock, and its base-side non-emptiness assertions are what stop it passing vacuously.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120k.XXXXXX") || exit 1; git show "$B:$N" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; lb=$(wc -l < "$T/base" | tr -d ' '); lh=$(wc -l < "$N" | tr -d ' '); test "$((10#${lb:-0}))" -gt 0 || rc=1; test "$lb" = "$lh" || rc=1; for k in 'dispatch-axis' 'dispatch-rule' 'dispatch-note'; do cb=$(grep -c -- "^- $k:" "$T/base" || true); ch=$(grep -c -- "^- $k:" "$N" || true); test "$((10#${cb:-0}))" -ge 2 || rc=1; test "$cb" = "$ch" || rc=1; done; i=0; for pat in '^- dispatch-axis:' '^- dispatch-rule: implement — ' '^## ' '^- tier: ' '^- saving: ' '^- invariant-lock: '; do i=$((i+1)); grep -- "$pat" "$T/base" > "$T/pb$i" || true; grep -- "$pat" "$N" > "$T/ph$i" || true; test -s "$T/pb$i" || rc=1; cmp -s "$T/pb$i" "$T/ph$i" || rc=1; done; test "$(grep -c . "$T/pb2" || true)" -ge 1 || rc=1; awk '/BEGIN pilot3-evidence/{f=1} f{print} /END pilot3-evidence/{if(f)exit}' "$T/base" > "$T/eb"; awk '/BEGIN pilot3-evidence/{f=1} f{print} /END pilot3-evidence/{if(f)exit}' "$N" > "$T/eh"; test -s "$T/eb" || rc=1; cmp -s "$T/eb" "$T/eh" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** The supersession is **scoped**: only the `verify` and `verify-fixture` rule lines move, and every clause of T-1112 and T-1100 that is not about them stays true. In `docs/loop-engineering/phase-multiplexing.md`: the set of `- dispatch-rule: verify-mechanism — ` lines is byte-identical at both refs and holds at least two lines, so T-1112 **AC1**'s own subject — the refinement whose modality that task flipped — is untouched here; each of T-1100 **AC6**'s four per-refinement prefixes still matches exactly one line at `HEAD` (`^- dispatch-axis: verify-fixture — serial|tier1-fanout — `, `^- dispatch-axis: verify-mechanism — serial|tier1-fanout — `, `^- dispatch-rule: verify-fixture — serial — unconditional — `, `^- dispatch-rule: verify-fixture — tier1-fanout — conditional — trigger: `); both refinement `- dispatch-axis:` lines still carry the literal `refines`; and the whole-file count of the literal `exactly two, note-wide` is equal at both refs, so the grammar-declaration anchor T-1100 AC6 measures a delta against is unmoved. Positive controls: the base blob is asserted non-empty; the `verify-mechanism` extraction is asserted non-empty on the base side before the comparison; and the `exactly two, note-wide` base count is asserted greater than zero before equality is judged. **Green at freeze and required to stay green.**
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120l.XXXXXX") || exit 1; git show "$B:$N" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; grep -- '^- dispatch-rule: verify-mechanism — ' "$T/base" > "$T/vb" || true; grep -- '^- dispatch-rule: verify-mechanism — ' "$N" > "$T/vh" || true; test "$(grep -c . "$T/vb" || true)" -ge 2 || rc=1; cmp -s "$T/vb" "$T/vh" || rc=1; for pre in '^- dispatch-axis: verify-fixture — serial|tier1-fanout — ' '^- dispatch-axis: verify-mechanism — serial|tier1-fanout — ' '^- dispatch-rule: verify-fixture — serial — unconditional — ' '^- dispatch-rule: verify-fixture — tier1-fanout — conditional — trigger: '; do test "$(grep -c -- "$pre" "$N" || true)" = "1" || rc=1; done; for k in verify-fixture verify-mechanism; do grep -- "^- dispatch-axis: $k — " "$N" | grep -qF -- 'refines' || rc=1; done; xb=$(grep -cF -- 'exactly two, note-wide' "$T/base" || true); xh=$(grep -cF -- 'exactly two, note-wide' "$N" || true); test "$((10#${xb:-0}))" -gt 0 || rc=1; test "$xb" = "$xh" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC13** Every surface the repaired prose *cites* is untouched, measured as a four-read union rather than as a committed-range diff alone. The union of four reads over the pathspecs `skills/run/SKILL.md`, `templates`, `agents`, `.github`, `tests` and `bin` — the committed range `git diff --no-renames --name-only "$B"...HEAD`, the staged delta `git diff --no-renames --cached --name-only`, the unstaged delta `git diff --no-renames --name-only`, and the untracked strays `git ls-files --others --exclude-standard`, each `git diff` read spelled with `--no-renames` so a rename's source path is never invisible to all four reads at once — contains, after removing `bin/check-count-claims.sh`, **zero** paths. So `skills/run/SKILL.md` (measured correct at `:281`), `bin/check-fanout-instances.sh` and its fixture suite, `bin/aggregate-verdicts.sh`, every prompt block under `templates/`, every agent definition and the CI workflow are all unmoved. Two independent confirmations rather than one: `skills/run/SKILL.md` is additionally read as its branch-point **blob** and compared byte-for-byte with the working tree, so an empty diff produced by a broken read cannot satisfy the identity; and `bash bin/check-prompt-sync.sh` and `bash tests/check-fanout-instances/run.sh` each exit `0`, the mechanical evidence that the `contain` contract and the shipped discriminator's own suite are both intact. Positive controls: the base blob of the skill file is asserted non-empty before the comparison, and `bin/check-fanout-instances.sh` is asserted readable, so the criterion cannot pass by reading nothing. **Green at freeze and required to stay green.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120m.XXXXXX") || exit 1; set -- skills/run/SKILL.md templates agents .github tests bin; { git diff --no-renames --name-only "$B"...HEAD -- "$@"; git diff --no-renames --cached --name-only -- "$@"; git diff --no-renames --name-only -- "$@"; git ls-files --others --exclude-standard -- "$@"; } | LC_ALL=C sort -u | grep -v '^bin/check-count-claims\.sh$' > "$T/u" || true; test "$(grep -c . "$T/u" || true)" = "0" || rc=1; test -r bin/check-fanout-instances.sh || rc=1; git show "$B:skills/run/SKILL.md" > "$T/skill" 2>/dev/null || rc=1; test -s "$T/skill" || rc=1; cmp -s "$T/skill" skills/run/SKILL.md || rc=1; bash bin/check-prompt-sync.sh >/dev/null 2>&1 || rc=1; bash tests/check-fanout-instances/run.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC14** The change set is confined to the declared allow-list, measured as the same unbroken four-read union spanning base→HEAD→index→worktree. The union of `git diff --no-renames --name-only "$B"...HEAD`, `git diff --no-renames --cached --name-only`, `git diff --no-renames --name-only` and `git ls-files --others --exclude-standard`, sorted and de-duplicated, contains no path outside this list: `bin/check-count-claims.sh`, `README.ja.md`, `README.md`, `docs/loop-engineering/phase-multiplexing.md`, `.shell-team/interventions/T-1120.md`, `.shell-team/provenance/T-1120.md`, `.shell-team/reviews/T-1120.md`, `.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md`, `.shell-team/test-recipe.md`, `.shell-team/todo.md`. The list includes this task's unconditionally-required records from the start (spec, board, provenance, interventions, review) so a deliverable that must appear cannot read as a scope violation; the fan-out sweep's own launch and aggregation records are **not** listed because `.shell-team/.gitignore` covers `runs/`, so `--exclude-standard` never sees them. **This criterion is merge-point-scoped and is expected to go stale after merge**: once later work lands on this branch's base, the committed range no longer describes this task. Its base-ref resolution is not widened and it is not re-derived per rework round. Positive controls: the union is asserted non-empty and asserted to contain this spec's own path, so a criterion reading nothing cannot pass. **Green at freeze and required to stay green.**
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120n.XXXXXX") || exit 1; { git diff --no-renames --name-only "$B"...HEAD; git diff --no-renames --cached --name-only; git diff --no-renames --name-only; git ls-files --others --exclude-standard; } | LC_ALL=C sort -u > "$T/u" || true; test -s "$T/u" || rc=1; grep -qxF -- '.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md' "$T/u" || rc=1; printf '%s\n' 'bin/check-count-claims.sh' 'README.ja.md' 'README.md' 'docs/loop-engineering/phase-multiplexing.md' '.shell-team/interventions/T-1120.md' '.shell-team/provenance/T-1120.md' '.shell-team/reviews/T-1120.md' '.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md' '.shell-team/test-recipe.md' '.shell-team/todo.md' | LC_ALL=C sort > "$T/allow"; test -s "$T/allow" || rc=1; grep -vxFf "$T/allow" "$T/u" > "$T/extra" || true; test "$(grep -c . "$T/extra" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC15** Every record gate this task's own artifacts pass through is green, with each path resolved through `bin/team-paths.sh` rather than hardcoded. The board, this spec, `.shell-team/provenance/T-1120.md` and `.shell-team/interventions/T-1120.md` are each asserted non-empty at their resolved paths, and each of these exits `0`: `check-handoff.sh <board>`; `check-board-headings.sh <board> --base "$B"`; `check-entry-mode.sh --board <board> --task T-1120` (the durable `- entry-mode:` sub-bullet against the board's own `- dispatch: specify — …` row, plus the `- dispatch-reflection:` family — the dispatch rows are the coordinating session's to transcribe, so this gate is red until they land); `check-count-claims.sh --board <board> --task T-1120`, live, which re-derives every `- count:` row on the entry rather than reading its number; `check-intent.sh <spec> <board>`; `check-provenance.sh <provenance>`; `check-interventions.sh --task T-1120 -- <interventions>`; `check-pii-shapes.sh --base "$B"`; and `check-review-input.sh --task T-1120`. Positive controls: all four artifacts are asserted non-empty before any gate runs, so a gate passing on an absent file cannot masquerade as conformance. **Expected red at freeze** — the intent hash, the provenance file and the dispatch rows do not exist yet.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1119-effort-application; then git merge-base "feature/1119-effort-application" HEAD; elif git show-ref --verify --quiet refs/remotes/origin/feature/1119-effort-application; then git merge-base "refs/remotes/origin/feature/1119-effort-application" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; SD=$(bash bin/team-paths.sh --get specs) || exit 1; PD=$(bash bin/team-paths.sh --get provenance) || exit 1; ID=$(bash bin/team-paths.sh --get interventions) || exit 1; SP="$SD/T-1120-rider-count-claims-message-and-fanout-docs.md"; for f in "$BD" "$SP" "$PD/T-1120.md" "$ID/T-1120.md"; do test -s "$f" || rc=1; done; bash bin/check-handoff.sh "$BD" >/dev/null 2>&1 || rc=1; bash bin/check-board-headings.sh "$BD" --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-entry-mode.sh --board "$BD" --task T-1120 >/dev/null 2>&1 || rc=1; bash bin/check-count-claims.sh --board "$BD" --task T-1120 >/dev/null 2>&1 || rc=1; bash bin/check-intent.sh "$SP" "$BD" >/dev/null 2>&1 || rc=1; bash bin/check-provenance.sh "$PD/T-1120.md" >/dev/null 2>&1 || rc=1; bash bin/check-interventions.sh --task T-1120 -- "$ID/T-1120.md" >/dev/null 2>&1 || rc=1; bash bin/check-pii-shapes.sh --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-review-input.sh --task T-1120 >/dev/null 2>&1 || rc=1; test "$rc" -eq 0

- [ ] **AC16** The `## Blast radius` record is complete in its own grammar, and its population is a tool's output re-derivable at read time rather than a number anyone typed. In this spec's `## Blast radius` section, extracted from its heading up to the next `## ` heading: exactly one `- base-ref: ` line, one `- method: ` line, one `- reproduce: ` line and one `- population: ` line, at least one `- row: ` line, and one embedded derivation block delimited by `<!-- BEGIN derivation: t1120-read-set -->` and `<!-- END derivation: t1120-read-set -->` whose second line is exactly `- derived-by: bin/derive-populations.sh`. The accounting is then a **value-independent invariant** with both sides derived at run time: the `items` field of the block's own `- set: all — ` line equals the live count of `.shell-team/specs/*.md`, the `- population: ` line's own leading integer equals that same number, and re-running the exact command on the `- reproduce: ` line produces stdout **byte-identical** to the embedded block — so a record whose numbers drifted from its own command fails here rather than being believed. Positive controls: the extracted section and the extracted block are each asserted non-empty; the parsed `items` value and the parsed population are each asserted to be non-empty digit runs before either comparison; and the re-run output is asserted non-empty before the byte comparison. **Expected red at freeze** — the scaffold's placeholders are filled by the freeze run.
  - check: rc=0; export LC_ALL=C; SD=$(bash bin/team-paths.sh --get specs) || exit 1; SP="$SD/T-1120-rider-count-claims-message-and-fanout-docs.md"; test -s "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120p.XXXXXX") || exit 1; awk '/^## Blast radius$/{f=1;next} f&&/^## /{exit} f' "$SP" > "$T/sec"; test -s "$T/sec" || rc=1; for k in 'base-ref' 'method' 'reproduce' 'population'; do test "$(grep -c -- "^- $k: " "$T/sec" || true)" = "1" || rc=1; done; test "$(grep -c -- '^- row: ' "$T/sec" || true)" -ge 1 || rc=1; awk '/^<!-- BEGIN derivation: t1120-read-set -->$/{f=1} f{print} /^<!-- END derivation: t1120-read-set -->$/{if(f)exit}' "$T/sec" > "$T/blk"; test -s "$T/blk" || rc=1; test "$(sed -n '2p' "$T/blk")" = '- derived-by: bin/derive-populations.sh' || rc=1; a=$(awk -F' — ' '/^- set: all — /{for(i=1;i<=NF;i++) if($i ~ /^items: /){sub(/^items: /,"",$i); print $i; exit}}' "$T/blk"); case "${a:-}" in ''|*[!0-9]*) rc=1 ;; esac; p=$(sed -n 's/^- population: \([0-9][0-9]*\).*$/\1/p' "$T/sec" | head -1); case "${p:-}" in ''|*[!0-9]*) rc=1 ;; esac; live=$(ls .shell-team/specs/*.md | LC_ALL=C sort | grep -c . || true); test "$((10#${live:-0}))" -gt 0 || rc=1; test "$((10#${a:-0}))" = "$((10#$live))" || rc=1; test "$((10#${p:-0}))" = "$((10#$live))" || rc=1; r=$(sed -n 's/^- reproduce: //p' "$T/sec" | head -1); test -n "$r" || rc=1; bash -c "$r" > "$T/re" 2>/dev/null || rc=1; test -s "$T/re" || rc=1; cmp -s "$T/re" "$T/blk" || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task adds or removes a spec file under `.shell-team/specs/`, at which point the `items`, `- population:` and embedded-block figures this record froze no longer describe the corpus the criterion re-derives, and the record is re-derived with the `- reproduce:` command rather than the assertion loosened.

- [ ] **AC17** qa-verifier's own report records what a `mechanism`-class task owes, in a form a later reader can check for presence. The board carries exactly one line beginning `### QA verification — T-1120` — a prefix match rather than a whole-line equality, because this board's own convention appends a role suffix to that heading — and the block from that line up to the next `^### ` or `^## ` heading carries each of these literals: `Verdict: PASS`; `mutation` (the per-criterion probe matrix, one probe per criterion minimum, each driven red then restored and reconfirmed green); `full-population` and `two-arm` (the owed downstream-impact inventory, base arm against HEAD arm, differenced criterion by criterion); `CI-equivalen` (the whole step list, with every step judged inapplicable named individually together with its reason); `Summarized sources` (the per-source report confirming each named source was opened and matched clause by clause, which no checker can observe); `unit-and-static` (the declared ceiling carried verbatim into the PASS block); and `unguarded` (the report's own reading of **AC1**/**AC2**'s class claim). Positive control: the extracted block is asserted non-empty before any literal is looked for, so an absent heading cannot satisfy the criterion by matching an empty extract. **Expected red at freeze.**
  - above-ceiling: `qa-verifier` owns this criterion after the gate. Its subject is that role's own report, and what a check can read is the record's conformance to the shape above — never whether the reading, the mutation probes or the two-arm sweep it records actually happened, which is the same self-attestation boundary every gate report in this repository stands on.
  - check: rc=0; export LC_ALL=C; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -s "$BD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1120q.XXXXXX") || exit 1; test "$(grep -c -- '^### QA verification — T-1120' "$BD" || true)" = "1" || rc=1; awk '/^### QA verification — T-1120/{f=1;next} f&&/^(###|##) /{exit} f' "$BD" > "$T/qa"; test -s "$T/qa" || rc=1; for lit in 'Verdict: PASS' 'mutation' 'full-population' 'two-arm' 'CI-equivalen' 'Summarized sources' 'unit-and-static' 'unguarded'; do grep -aqF -- "$lit" "$T/qa" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

## Input space

**Reachable input classes** — what this task's deliverables really meet:

1. **A board `- count:` row whose recorded command exits non-zero** — the refusal branch the repaired message lives on, reached in this repository every time someone writes a `grep -c`-shaped count command. The named trap prints `0` and exits `1`, which is why the checker refuses rather than reading zero.
2. **An operator who follows the message's own advice under `set -o pipefail`, on a genuine zero match.** This is the class #409 reports and the class **AC2** executes: the quoted recipe is run against a file with no match, and the honest outcome is exit `0` with the reading `0`.
3. **A legitimately-failing upstream stage that is not a zero-match `grep`** — a bad ref, an unreadable path. The message names one recipe for one named trap and the fixture suite already covers the bad-ref shape at `:292`; the repaired text does not become a general `pipefail` tutorial.
4. **The three merged readers of the `| wc -l` fragment** — T-1113 AC6's stderr grep, T-1113 AC9's ordering anchor, and the fixture suite's two argument-passing readers. Each reads the fragment and none reads the unguarded intermediates, which is what makes the repair invisible to them.
5. **A test corpus that deliberately contains unguarded pipelines as fixtures** (`tests/check-count-claims/run.sh:292`). This is why the absence assertion is scoped to the rendered message's own spans and never to a file-wide or repository-wide search.
6. **Both README bullets as single physical lines inside a hard-wrapped bilingual document**, cited by line number by T-1075 AC6's own `- adopter-surface:` line, and read by adopters deciding how their loop elects fan-out.
7. **A Japanese line whose bytes are multi-byte UTF-8**, read whole rather than windowed, because a byte-window cut landing mid-sequence makes BSD `grep` treat the extract as binary and report false absence for every literal in it, ASCII included (#434).
8. **A line-bound citation surface**: `docs/loop-engineering/phase-multiplexing.md` is cited by line number from the board's dispatch grounds (`:80`, `:355`, `:362`, `:365`, `:367`, `:369`, `:370`) and from three merged specs, which is why every repair is an in-place, line-count-preserving rewrite.
9. **Merged criteria that read exactly these bytes** — T-1112 AC1/AC2/AC3, T-1100 AC6, T-1075 AC5/AC6, T-1113 AC6/AC9, T-1019 AC10, T-1003 AC19 — some of which resolve their base through a ref that may no longer exist, or through a one-argument `git diff <ref>` that reads the working tree. Both are real base-resolution behaviours this task measures rather than assumes.
10. **A stacked branch whose predecessor resolves as a local branch, as a remote-tracking ref, or as neither.** The first two are the discriminator's arms 1 and 2, measured here as both present; the third is the era-change fallback to `develop`.
11. **A board that grows while the task runs**, appended to by this very loop, which is why every board-reading criterion anchors on a task-scoped line rather than on a count.
12. **A checkout with no `shellcheck`, no `timeout`/`gtimeout` and no Codex CLI** — including this repository's own CI runner for some of those. No criterion below shells out to any of them.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect against:

1. **An adversarially constructed `- count:` command designed to defeat the message's advice** — a pipeline with several failing stages, a command whose failure is not an upstream `grep`, a `PIPESTATUS` or `shopt -s lastpipe` construction. The message teaches one recipe for the one trap the checker's own header names; a finding that it does not cover every pipeline shape is a correct description of scope.
2. **Every possible spelling of a guarded pipeline.** `(cmd || true) | wc -l` is the recipe this repository ships, in the header and now in the message alone; enumerating equivalent guards is not owed.
3. **Any behavioural change to the checker.** A finding that the message could be shorter, differently ordered, or emitted on a different stream is out of scope: **AC4** confines the diff to one line by construction.
4. **A repair, re-basing or annotation of any merged criterion this diff reddens.** Disclosed in `## Blast radius`; touching another task's frozen check line is a class-B re-freeze of someone else's intent.
5. **A disposition flip in `phase-multiplexing.md`.** Whether fan-out is adopted per axis or blanket, and whether `:370`'s conditional flip-back should stay, are design questions with their own tasks. A finding that `staged-adoption` is now too conservative is out of scope here.
6. **Ever-longer README bullets, an ever-larger set of machine tokens, a third README language, or a reflow of the surrounding list.** The repair rewrites one physical line per mirror and the criteria measure exactly that.
7. **Non-UTF-8 bytes, CRLF line endings or a missing trailing newline** in any file a criterion reads. Every shipped file here is LF-terminated UTF-8; a byte comparison would report a difference and that is correct behaviour, but no criterion is designed to diagnose it.
8. **Concurrent mutation** — two processes editing the script, a README or the note while a check reads them. These are sequential local and CI invocations.
9. **A hostile operator, and an adversarially authored message, README bullet or note line** constructed to defeat a line-anchored or span-anchored read with no reachable authoring path that would produce it. The threat model is this repository's own: the operator and the working directory are trusted, and what these mechanisms defend against is accidents — a repaired clause leaving its twin two lines down standing, a mirror updated in one language only, a follow-up line minting a second `DISCHARGED` prefix. A verdict a check reaches that its own inputs do not support is squarely **in** scope.
10. **Whether GitHub actually ran a CI step.** **AC13** runs the suites' own committed bytes locally; a runner's own record is outside any artifact read.

<!-- END intent-block: T-1120 -->

## Decision points

Every design point below was settled by `tech-lead`'s 2026-09-04 Routing Map and by the coordinating session's decisions of the same date, or — where marked — by this role's own first-hand reading at authoring time. This section records where each is encoded so a later reader does not reopen a closed question.

- **DP-A — `:373` is in scope.** The coordinating session's decision, on the Routing Map's own recommendation: the identical stale claim is repaired at `:355`, and leaving it standing two lines down at `:373` is the omission class this repository keeps catching. `:375` stays byte-identical (T-1112 AC3). Encoded in **AC10**.
- **DP-B — the class is `mechanism`.** Path-based trigger (`agents/pm-spec.md:91`: "any path under `bin/`"), not judgment-based, so the full-population two-arm sweep is **owed** and the `no-mechanism` read-set-scoped carve-out is unavailable. The sweep runs as `tier1-fanout`, executed by the coordinating session. Encoded in the declaration region and in `## Blast radius`'s method line.
- **DP-C — the `#409` repair is written against the class**, "an unguarded pipeline quoted anywhere in the teaching text", so both quoted unguarded examples go rather than only the one the issue reproduced; the guarded form stays, which keeps the `| wc -l` fragment its three readers depend on; and the drop-`-c` teaching stays without quoting a pipeline. The behavioural half — the message's own recommended recipe executed live under `set -o pipefail` exiting `0` on a genuine zero match — is a criterion rather than a claim. Encoded in **AC1**, **AC2** and **AC3**.
- **DP-D — line-count and position locks.** `phase-multiplexing.md`'s total line count is unchanged; both README bullets stay one physical line at their existing line number; the `bin/` edit is confined to the one `fail` call so nothing above it shifts. Encoded in **AC4**, **AC5**, **AC6** and **AC11**.
- **DP-E — version tier: PATCH per item**, the sprint tier being that column's maximum. The coordinating session writes the T-1110 version-derivation row on the board at freeze; the per-item grounds are the Routing Map's, carried in **A-6**.
- **DP-F — `spec-review: none`.** No domain premise: both halves turn on facts this repository measures itself. This is the one dispatch axis differing from T-1119, whose three provider-behaviour premises this repository cannot measure.
- **DP-G — measured first-hand by this role, and not named by the Routing Map: the `- follow-up: DISCHARGED` collision.** T-1112 **AC3** extracts every line matching `^- follow-up: DISCHARGED` and compares it with `cmp` against a base extraction holding exactly one line. A repaired `:373` beginning with that prefix would make the extraction two lines and redden a criterion this task is otherwise required to keep green — a break caused by the *repair's spelling* rather than by its meaning, invisible to any criterion that only read `:373` itself. The repaired line therefore keeps a `- follow-up: ` prefix and states the discharge in its body. Encoded in **AC10**'s last clause, and stated in `## Notes for engineer` as a hard constraint.
- **DP-H — `:355`'s disposition token is not flipped.** `staged-adoption` stays; only the **status** of the named precondition moves. Flipping the disposition would be a design change with no mandate here, and it would additionally break T-1112 AC3's own required literal. Encoded in **AC8**.
- **DP-I — measured first-hand: the note's seventh `- invariant-lock:` line at `:351` is not part of the repair.** It records `per-instance-telemetry-discriminator` as "named as a follow-up only (DP-F)" — a true statement about **T-1078's own pilot scope**, not a claim about today's repository — so repairing it would rewrite a historical scope note rather than a stale claim. **AC11** locks the whole `- invariant-lock:` set byte-identical, which is the mechanical form of that decision.

## Pre-commitment

**Authority: AI self-discipline** — self-imposed by this loop, never operator-ratified; any escalation citing it says so. Written before round 1, so the trigger executes a decision rather than opening one. **Trigger**: two consecutive review rounds landing an independently-new `Blocker` or `Major` against the same component class — the standing two-round convention, with no loosening claimed. The **factual** trigger governs on disagreement with the contextual one ("before a third round of rework on that component starts"). The drop order is the Routing Map's, carried verbatim in substance:

1. **Dropped first — #407 item 1** (Site 4: `docs/loop-engineering/phase-multiplexing.md`, including DP-A's `:373`). It is the component whose repair deliberately reddens two merged locks, so it carries the highest round-count risk, and it is the only component with no adopter reader. It returns to issue **#407** as a still-open item, with that round's findings travelling with it as its requirement list rather than being re-derived there; #407 item 2 having shipped, the issue's remaining scope becomes item 1 only, stated in the drop note.
2. **Dropped second — #407 item 2** (both README mirrors), likewise back to **#407**.
3. **Never dropped — #409** (Site 1). It is the rider's headline and the only executing-surface item. If *it* is defeated across two consecutive rounds, the task **stops and returns to planning**, because the same repair defeated twice is evidence about the premise — "a one-string edit fenced by three merged criteria is safe" — rather than about the engineer's craft.

**Every repair is written against the class of mutation, not the reported case**: name the property of the input that makes the defect fire, enumerate the sibling inputs sharing it (the second quoted example, the second mirror, the twin claim two lines down, the base-side arm), confirm the repair closes those too, and report which variants were tried.

**Round tally against the trigger, kept on the page so the next round reads a count rather than re-deriving one.** No review round has run. Nothing is dropped.

## Verdict scope

**Consumer class, split explicitly, per the Routing Map's step 4 — and stated in the briefing at *every* round, because a briefing that omits the consumer class invites shipped-code depth by default.**

**(a) Full adversarial depth** — `bin/check-count-claims.sh` is a shared loop-trust checker an adopter executes. In scope at that depth: whether the repaired message can still teach a recipe that refuses under `pipefail`; the message's interaction with all three merged criteria that read `| wc -l`, including the ordering property T-1113 AC9 relies on; whether the edit reaches any behaviour beyond the message; and whether **AC1**'s per-span pattern can be defeated by a quoted form it does not match.

**(b) Prose-accuracy depth** — `README.md` and `README.ja.md` are shipped adopter docs and `docs/loop-engineering/phase-multiplexing.md` is an internal loop-engineering note. In scope at that depth: is the corrected claim **true** against `skills/run/SKILL.md:281`, `phase-multiplexing.md:369` and `:375`, and against `bin/check-fanout-instances.sh`'s shipped CI step; are the two mirrors semantically equivalent; are machine tokens English verbatim in the JA line; and does the Site 4 repair reach every occurrence of the stale claim rather than the first one.

**The dev-scaffold inline-`- check:` carve-out does not apply to this task.** The operator's 2026-08-22 ruling prices down adversarial guard-mutation completeness for a spec's own inline check lines *on dev-scaffold tasks with no adopter surface*. This task declares `- user-visible: yes` and ships an adopter surface, so this spec's check lines are reviewed at full strength — and `bin/` shared checkers were never inside that carve-out in the first place.

## Blast radius

**Method: full-population** — this task's declared `- verification-class: mechanism` prices the downstream-impact inventory as a diff over **every** merged spec's criteria, base arm against HEAD arm, differenced criterion by criterion. A reverse-mapped candidate set — the specs whose `- check:` lines name a path this task edits — is derived below as the `candidates` set and is used only to **prioritize** which slices run first; it is a lower bound on the affected population and is never presented as the population. The `indirect` set names the class a literal-byte derivation cannot see (a criterion reaching the board through `bin/team-paths.sh --get todo` rather than a literal path) and those members are **run** rather than disclosed as unmeasured. The edited paths are closed by **AC14**'s allow-list.

**Predicted movers, by class — predictions, confirmed by the arms and never asserted.**

- **T-1112 AC1 and AC2 — `PASS`→`FAIL` by design.** AC1's sibling clause concatenates every `- dispatch-rule: verify — ` and `- dispatch-rule: verify-fixture — ` line and requires byte-identity base↔HEAD; AC2's wider clause requires the sorted set of **every non-`verify-mechanism`** rule line to be byte-identical. Both redden the moment `:365`–`:368` are reworded. This is **supersession, not breakage**: those clauses were authored to prove T-1112 flipped only the `verify-mechanism` refinement, and a later deliberate prose repair of the sibling lines is exactly what they were not written to forbid. Disclosed, not repaired. **AC12** measures that the supersession is scoped — the `verify-mechanism` rule lines and every `- dispatch-axis:` line stay byte-identical.
- **T-1112 AC3 — must not redden.** Its five required literals stay on the repaired `:355` line, its agent-shaped literal stays absent, and its `^- follow-up: DISCHARGED` extraction stays one byte-identical line (**AC8**, **AC10**). A red row here is a defect to repair before hand-off, not a disclosure — and DP-G names the exact spelling that would cause it.
- **T-1100 AC6 — base-resolution-dependent, measured rather than predicted.** Its deltas are keyed to `develop`, into which T-1100 is already merged, and one of its clauses asserts `full-population` on a `verify-mechanism — tier1-fanout — conditional` rule that **T-1112 already flipped to unconditional**. So it may be red at both refs for reasons wholly independent of this diff. Its four per-refinement prefix counts, which this diff must not disturb, are re-derived independently in **AC12**.
- **T-1075 AC5 and AC6 — base-resolution-dependent, and must not redden on content.** Both resolve their base through `BPREF=feature/1074-fanout-orchestration` and exit `1` if neither the local nor the origin ref exists. On content they stay green by construction: `Validate` remains inside both bullets, the four forbidden phrases stay absent, and the opt-in literals live on line 180 rather than 174 (**AC5**, **AC6**).
- **T-1113 AC6 and AC9 — must not redden.** The `| wc -l` fragment survives in the remedy and stays absent from the echoed command text, so AC6's stderr grep and AC9's ordering anchor both hold (**AC3**).
- **T-1019 AC10 — reddens on the `bin/` edit, and is likely already red.** Its clause is "not one byte changes under `bin/`", read with a **one-argument** `git diff --name-only develop` that compares that ref against the **working tree**. **Relayed** (T-1118/T-1119 records, per the briefing): it is already red at the branch point because the branch point already carries later `bin/` work — which if true means this task adds **zero** new delta on the two-arm diff. The freeze run measures the base-side verdict and records it rather than inheriting the relay (**A-4**).
- **T-1003 AC19 — `develop`-keyed, predicted unchanged red.** Its six locked files include the CI workflow, which this diff does not touch; its own prose already declares it merge-point-scoped. Same relay, same treatment.
- **Predecessor scope-lock / population-equality class.** T-1119's own scope lock (**AC22** there) and its `all`-set population equality (**AC23** there, recorded at 124) redden as the predecessor lock, exactly as T-1118's did for T-1119. Disclosed.
- **Board-commit-count and parity class — merge-point artifacts.** T-1099 **AC4** and the T-1103 AC14 / T-1104 AC17 family, which flipped in both preceding sweeps.
- **Prompt-block `contain`-sync class — must not redden.** No prompt block moves and `bash bin/check-prompt-sync.sh` is asserted green (**AC13**).

**Record (owed by the freeze run; this scaffold is what it fills).** The freeze run replaces every `<…>` below with a measured value, adds one `- row:` line per moved criterion, and embeds the derivation block verbatim in place of the placeholder region.

- base-ref: `<40-hex, resolved by the declared discriminator's arm 1 — measured and recorded by the freeze run, together with the ref file it was read from and the predecessor PR's live state>`; every base-side verdict below is produced in detached worktrees checked out at that commit (`git worktree add --detach`), never from this working tree.
- method: each member's own `- check:` lines are run as themselves with `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh <spec>`, once in a detached worktree at the branch point (base arm, N harness-tracked background shell slices over the `all` set) and once at the implementation commit (HEAD arm), and the two FAILED sets are differenced per criterion label. Before either arm is read, one snapshot of the runs corpus resolved through `bash bin/team-paths.sh --get runs` is copied into each base-arm worktree as a real directory, member by member, never a symlink, copying only a member git tracks at neither ref, so a corpus-reading criterion does not produce a base-arm false FAIL for a reason this diff never caused. The `indirect` set's members are run rather than left to the literal-byte derivation. A member with no runnable `- check:` line at the base ref is recorded as a `- sentinel:` unit, not as a verdict.
- reproduce: bash bin/derive-populations.sh --label t1120-read-set --set "all=ls .shell-team/specs/*.md | LC_ALL=C sort" --set "candidates=grep -lE -- '^[[:space:]]+- check:.*(check-count-claims\.sh|README\.md|README\.ja\.md|phase-multiplexing\.md|team-paths\.sh --get todo|\.shell-team/todo\.md)' .shell-team/specs/*.md | grep -v 'T-1120-rider-count-claims-message-and-fanout-docs\.md' | LC_ALL=C sort" --set "indirect=grep -lF -- 'team-paths.sh --get' .shell-team/specs/*.md | grep -v 'T-1120-rider-count-claims-message-and-fanout-docs\.md' | LC_ALL=C sort" --accept-status candidates=1 --accept-status indirect=1
- population: `<n>` — the `all` set of the derivation block below, every spec in the corpus; **AC16** re-derives the live count and requires equality with both this line and the block's own `- set: all — ` line. The `candidates` and `indirect` sets are prioritization inputs, not the scope. Measured at authoring time: 124 specs before this one, 125 with it (`ls .shell-team/specs/*.md | wc -l`).
- head-ref: `<short sha of the arm run at the commit carrying both green gates and the READY_FOR_MERGE flag, with the launch and aggregation record names and their unit/verdict/sentinel counts>`
- row: `<one line per moved criterion — spec path, criterion labels, base verdict → head verdict, and a class word: expected, predicted-confirmed, prediction-corrected, or real-defect-repaired — added by the freeze run and the HEAD arm>`

<!-- BEGIN derivation: t1120-read-set -->
`<the freeze run replaces this placeholder region with bin/derive-populations.sh's verbatim output for the - reproduce: command above, markers included>`
<!-- END derivation: t1120-read-set -->

## Body-to-AC correspondence

One row per normative directive stated in the body prose, mapped to the criterion carrying it or marked exempt with a one-line reason.

| # | Body directive | Source | Carried by |
|---|---|---|---|
| 1 | No unguarded pipeline is quoted anywhere in the message's teaching text | Goal, DP-C | **AC1** (per-span pattern zero at HEAD, ≥1 at base; guard literal required on every pipeline span) |
| 2 | The repair is written against the class, so both quoted examples go | Goal, DP-C, Pre-commitment | **AC1** (the span population is derived, so a second unguarded example fails it) |
| 3 | The message's own recommended recipe runs clean under `pipefail` on a genuine zero match | Goal, DP-C | **AC2** (every HEAD span exits 0 and reads 0; the base arm reproduces #409) |
| 4 | The `\| wc -l` fragment survives for its three merged readers | Goal, DP-C | **AC1** (fragment present), **AC3** (AC6's grep and AC9's ordering re-derived; the suite's two argument readers and its own exit 0) |
| 5 | The drop-`-c` double-counting teaching survives without quoting a pipeline | Goal, DP-C | **AC1** (`grep -c` and `double-count` required; the pipeline-span clauses forbid the quoted form) |
| 6 | No behavioural change to the checker; the edit is confined to one `fail` call | Non-goals, DP-D | **AC4** (head and tail byte-identical, that line differs, line count and line number equal) |
| 7 | No header edit is owed and none lands | Problem, Non-goals | **AC4** (the guarded idiom asserted inside the compared head region at both refs) |
| 8 | No new fixture and no edit to the fixture suite | Non-goals | **AC13** (four-read union over `tests/` empty), **AC3** (the suite still exits 0) |
| 9 | Both README bullets state a Plan-time election transcribed at the Specify seam and read at Validate | Goal, Problem | **AC5**, **AC6** (four tokens each), **AC7** (the `Plan`-before-`Validate` ordering in both mirrors) |
| 10 | `Validate` stays inside each bullet (T-1075 AC6) | Goal, Blast radius | **AC5**, **AC6** |
| 11 | T-1075 AC5's opt-in literal stays and its four phrases stay absent | Blast radius | **AC5**, **AC6** |
| 12 | Each README bullet stays one physical line at the same line number | DP-D, Non-goals | **AC5**, **AC6** (equal line number, equal total line count, `--numstat` 1/1) |
| 13 | The two mirrors move together and nothing else in either README moves | Goal, Non-goals | **AC7** (changed `README*` set exactly the two files), **AC5**/**AC6** (`--numstat` 1/1 per file) |
| 14 | Machine tokens stay English verbatim in the JA mirror | Summarized sources (#407 item 2) | **AC6** (all four tokens asserted as English literals in the JA bullet) |
| 15 | Japanese content is read whole-line, never through a byte window | Acceptance-criteria preamble, Input space 7 | **AC6**, **AC7** (whole-line reads and `awk index` over the whole line; `-a` on every JA read) |
| 16 | `:355` states the precondition's real status while keeping its name and its five locked literals | Goal, DP-H | **AC8** |
| 17 | `:355`'s disposition token `staged-adoption` is not flipped | Non-goals, DP-H | **AC8** (the literal required at HEAD) |
| 18 | The four sibling rule lines stop asserting two unbuilt preconditions | Goal, Problem | **AC9** (`two unbuilt preconditions` 2→0, `preconditions exist` 1→0) |
| 19 | Every edited rule line keeps its exact `- dispatch-rule:` prefix including `trigger: ` | Goal, Summarized sources | **AC9** (four prefixes, one line each, at both refs) |
| 20 | Every edited rule line keeps its priced ground | Goal | **AC9** (`priced from ` and `recommendation: tier1-verification-fanout` on all four HEAD lines) |
| 21 | `:373` is repaired too (DP-A) | DP-A | **AC10** |
| 22 | The `:373` repair must not begin `- follow-up: DISCHARGED` | DP-G | **AC10** (the `DISCHARGED` extraction is one byte-identical line at both refs) |
| 23 | `:375` is not edited | Non-goals | **AC10** (same clause), **AC11** (line count and heading list) |
| 24 | The note's total line count, key counts, axis lines, implement rules, headings, `pilot3-evidence`, tier/saving/invariant-lock sets are preserved | DP-D, DP-I, Non-goals | **AC11** |
| 25 | The seventh `- invariant-lock:` line at `:351` is left standing | DP-I | **AC11** (the `- invariant-lock:` set byte-identical) |
| 26 | The supersession is scoped: `verify-mechanism` rules and every axis line stay identical, and T-1100 AC6's prefix counts are undisturbed | Goal, Blast radius | **AC12** |
| 27 | `skills/run/SKILL.md`, `bin/check-fanout-instances.sh`, `templates/`, `agents/`, `.github/` and `tests/` are untouched | Non-goals | **AC13** (four-read union plus a base-blob byte comparison and two suite/checker exits) |
| 28 | The change set is confined to the declared allow-list | Non-goals | **AC14** (merge-point-scoped, four-read union) |
| 29 | Records pass every gate they belong to, including the durable `- entry-mode:` sub-bullet | Goal, board entry | **AC15** |
| 30 | The downstream inventory is full-population and its population is `bin/derive-populations.sh`'s output | Declaration region, Blast radius | **AC16** (grammar, invariant equality, re-run byte-identity), **AC17** (`full-population`, `two-arm` in the report) |
| 31 | Nothing about the verification protocol is priced down (`mechanism` class) | Declaration region, Non-goals, DP-B | **AC17** (the report's own CI-equivalence, mutation-matrix and sweep items) |
| 32 | No merged criterion this diff reddens is repaired | Non-goals, Blast radius | `info-only (not promoted to AC)` — an absence of action; **AC14** additionally keeps every other spec file outside the allow-list, so a repair could not land silently |
| 33 | The reddening is disclosed by class in `## Blast radius` | Goal, Blast radius | `info-only (not promoted to AC)` — the disclosure is mutable prose outside the intent block, filled by the freeze run and the HEAD arm; **AC16** checks the record's grammar rather than its judgments |
| 34 | Neither #409 nor #407 is closed here, and nothing merges | Non-goals | `info-only (not promoted to AC)` — actions outside this checkout's tracked files, taken at the sprint batch GO |
| 35 | The version tier is PATCH per item, sprint tier the column maximum | DP-E, Assumptions **A-6** | `info-only (not promoted to AC)` — the T-1110 version-derivation row is the coordinating session's board line, written at freeze; no criterion of this spec asserts a tier |
| 36 | `spec-review: none` | DP-F, Non-goals | `info-only (not promoted to AC)` — a dispatch election recorded on the board and cross-checked against the Routing Map by the run skill's own gate, not a property of any artifact here |
| 37 | `- user-visible: yes` with exactly one `- adopter-surface:` line and no waiver | Declaration region | `info-only (not promoted to AC)` — the declaration gate is a read `pm-spec` performs at authoring and the freeze run repeats; no checker ships for it, and issue #250 carries the mechanical gate |
| 38 | `- verification-class: mechanism` and `- verification-ceiling: unit-and-static`, both non-vacuous | Declaration region | **AC17** (the ceiling value carried verbatim into the PASS block); the declaration reads themselves are `info-only (not promoted to AC)` for the same #250 reason |
| 39 | The `- base-ref-discriminator:` expression is spelled byte-identically in every criterion that resolves it, split by read kind | Declaration region | **AC1**, **AC2**, **AC4**–**AC15** — **fourteen**, matching the declaration region's own list exactly; enforcement is the freeze sweep's own read, established by matching the exact string against every check line rather than by recollection |
| 40 | Every count premise on the board travels with its regenerating command | Goal, board entry | `info-only (not promoted to AC)` — `bin/check-count-claims.sh` re-derives each `- count:` row live and **AC15** runs it, so the property is enforced by that gate rather than by a criterion of its own |
| 41 | Review depth is priced by consumer class, and the dev-scaffold carve-out does not apply | `## Verdict scope` | `info-only (not promoted to AC)` — a pricing statement about how a gate reads this deliverable, not a property of any artifact |
| 42 | The pre-commitment's trigger, drop order, never-dropped item and self-imposed authority | `## Pre-commitment` | `info-only (not promoted to AC)` — rules governing how this loop reacts to review rounds, which no artifact-reading check can observe; any report citing them declares them AI self-discipline |
| 43 | The interventions file already exists and this role neither creates nor edits it | Summarized sources, Notes | **AC15** (`check-interventions.sh` reads it for conformance) |

## Assumptions

Rows **A-1** through **A-6** are `**relayed**`: the primary confirmation is held by the coordinating session. This role has no shell and no network path, so each row names the holder, states the premise as the holder states it, and names what the freeze run must re-read and report beside it. Rows **A-7** onward were measured first-hand at authoring time with this role's own file reads.

- **A-1 — relayed — the bodies of issues #409 and #407.** Holder: the coordinating session, which fetched both through the GitHub API on 2026-09-04 and materialized them as scratch files this role read first-hand. Their distinctions are carried in `## Summarized sources`. The freeze run re-measures both issues' live state (open, unchanged scope) and records it beside this row.
- **A-2 — relayed — `tech-lead`'s 2026-09-04 Routing Map.** Holder: the coordinating session, which saved the verbatim final report as a scratch file. This role read it first-hand there but cannot re-derive it. Its four-site table, constraint sets, reddening classes, PATCH table, pre-commitment and dispatch rows are formalized in `## Decision points`, `## Pre-commitment`, `## Verdict scope` and `## Blast radius`, and are not re-opened.
- **A-3 — relayed, and measured as far as this checkout allows — the predecessor's pull request is open.** Holder: the coordinating session (PR #436, head `4f96989`). It is the premise that makes the multi-arm `- base-ref-discriminator:` mandatory rather than `not-applicable`. Measured first-hand here: `.git/refs/heads/feature/1119-effort-application` and `.git/refs/remotes/origin/feature/1119-effort-application` both read `4f969892e9ddb5ef3615dec24c0c627513414ad8`, and this branch's own ref reads a **different** commit, so arm 1 is selected and `HEAD` is not the branch point. T-1119's board entry records `- closed: 2026-09-04, PR #436 → develop`, which under this repository's stacked-batch-GO arrangement does **not** mean the pull request merged; the PR's live state is a fact of the repository of record this role cannot query. The freeze run measures PR #436's state and records the value beside this row.
- **A-4 — relayed — T-1019 AC10 and T-1003 AC19 are already red at the branch point.** Holder: the coordinating session, from the T-1118 and T-1119 sweep records. If true, this task's `bin/` edit adds **zero** new delta on the two-arm diff for AC10 and none at all for AC19. Measured as far as reading allows: AC10's own check is `test -z "$(git diff --name-only develop -- bin tests/rollup-runs tests/cluster-failures)"`, a **one-argument** form comparing `develop` against the **working tree**, so its verdict is a property of the checkout rather than of any commit range; AC19 compares six files against `develop` and none of them is in this task's allow-list. The freeze run executes both criteria at the base ref and records the measured base-side verdict rather than inheriting this relay.
- **A-5 — relayed — the stacked-train arrangement and the sprint batch GO.** Holder: the coordinating session. Feature PRs are held open, each task branches from the previous task's tip, and merge waits for one batch GO. This is why **AC14** is keyed to the branch point rather than to `develop`, and why it is expected to go stale at the batch merge.
- **A-6 — relayed — the sprint's approved version premise is PATCH.** Holder: the coordinating session (r3-body planning, approved 2026-09-02) and the Routing Map's per-item table, whose three rows read `not-met` on the headline test with no new default-reachable benefit: a refusal-path message correction, an internal loop-engineering note's accuracy, and a stale sentence about where an existing election is made. This role's own authoring-time reading against `CONTRIBUTING.md:120`–`:131`, applied **jointly**, is also **PATCH**: MINOR's gate cannot be reached because no new adopter-perceivable benefit ships, and T-1115's tiebreaker grants MINOR only for an inversion **of the default shipped path**, which does not occur — the README repair corrects a claim about *where* an existing election is made, and the default remains that no phase fans out except the one `verify-mechanism` refinement T-1112 already flipped. Recorded as a derivation the operator **ratified**, and therefore no independent evidence for the freeze-time re-derivation, whose verdict governs. **Premise-break watch handed to the T-1110 freeze gate**: the tier holds only while the README repair stays a *where*-correction rather than becoming a claim that fan-out is now on by default — which **AC5**/**AC6**'s four-forbidden-phrase clauses are the mechanical form of.
- **A-7 — measured first-hand — the branch point and the ref namespaces.** Read directly from the ref files: `.git/HEAD` names `refs/heads/feature/1120-rider-count-claims-message-and-fanout-docs`; that branch's ref file reads `492eff047886347aa2e6fd05a251ae347b017cce`; the predecessor's reads `4f969892e9ddb5ef3615dec24c0c627513414ad8` in **both** the local and the `origin` namespace. So arm 1 is selected, and this branch already carries a commit of its own, which is why every criterion resolves the branch point with `git merge-base` rather than treating `HEAD` as the base. No 40-hex literal enters any criterion.
- **A-8 — measured first-hand — the uniqueness of every anchor this spec's criteria key on.** Each of the following occurs **exactly once** in the file named, measured at authoring time: `explicit per-task choice made inside the Validate phase` in `README.md` (and once in `.shell-team/reviews/T-1112.md`, which is why every criterion names its file rather than searching the tree); `依然として Validate フェーズの中でタスクごとに明示的に選ぶ` in `README.ja.md`, and nowhere else in the repository; `gated explicitly on one named precondition` in `docs/loop-engineering/phase-multiplexing.md`, and nowhere else; `command exited $rc` in `bin/check-count-claims.sh`. Each `^- follow-up: design the ` and `^- recommendation: tier1-verification-fanout — ` pattern matches exactly one line in the note. Each Japanese anchor was additionally checked to sit **within one physical line**, and every criterion reading one reads the whole line.
- **A-9 — borrowed-vocabulary count premises, classified here and measured at the branch point by the execution-capable side before the freeze.** Every literal count premise this spec asserts is **borrowed** — this task coins no new token at all, so there is no own-coinage class to sweep, and a sweep scoped to "the new literals this task introduces" would have said nothing about any of them. Each is enumerated with the command that confirms it against the branch point's committed blob, expected value stated: `explicit per-task choice made inside the Validate phase` in `README.md` — `git show "$B:README.md" | grep -c -F 'explicit per-task choice made inside the Validate phase' || true`, expected **1**; `依然として Validate フェーズの中でタスクごとに明示的に選ぶ` in `README.ja.md` — same form, expected **1**; `gated explicitly on one named precondition` in `docs/loop-engineering/phase-multiplexing.md` — expected **1**; `two unbuilt preconditions` in that file — expected **2**; `preconditions exist` in that file — expected **1**; `can actually launch N *agent* instances` in that file — expected **0** (removed by T-1112, and this task preserves that absence, so the premise is about a token another task already retired); `^- follow-up: design the ` in that file — expected **1** line; `^- follow-up: DISCHARGED` in that file — expected **1** line; `Validate` inside each README's own extracted bullet — expected **1** or more; and `Opt-in only — no phase is rewired to fan out by default` / `あくまで opt-in` — expected present, on line 180 of their files rather than 174. Every authoring-time measurement above was taken at `HEAD`, which for these four files equals the branch point because this branch's only commit touched `.shell-team/interventions/T-1120.md`. The freeze run re-runs each command against the branch point's committed blob and records the measured value beside this row; a contradiction routes back to `pm-spec` as a broken check line rather than being corrected in passing.
- **A-10 — measured first-hand — the shape premises the criteria rest on, each with its regenerating command.** `grep -c '^effort-value ' …`-style row counts are not used here; the shapes that matter are: `bin/check-count-claims.sh`'s `fail()` writes one line to **stderr** and exits **1** (`:105`), so the rendered refusal is one physical line on the combined output; the per-row echo at `:284` precedes execution and its text cannot contain `| wc -l`, which is what makes T-1113 AC9's ordering anchor sound; the script resolves **no** sibling script and reads **no** operating path, which is what makes running its base-ref blob from `$TMPDIR` a legitimate arm; `tests/check-count-claims/run.sh` carries the fragment `| wc -l` on **9** lines (`grep -cF -- '| wc -l' tests/check-count-claims/run.sh`), of which two are the argument-passing readers, which is why **AC3** asserts `≥2` rather than a total; `.shell-team/.gitignore` covers `runs/`, so the fan-out sweep's own records never enter **AC14**'s untracked read; and `bin/check-fanout-instances.sh` exists with its fixture suite wired as its own CI step at `.github/workflows/check-handoff.yml:346`–`:347`. Every count that reaches the board travels as a `- count: <label> — <value> — command: <cmd>` row that `bin/check-count-claims.sh` re-derives live, each command written so a zero-match stage is `|| true`-guarded, since that checker runs every command under `set -o pipefail` in a fresh child shell.
- **A-11 — host premises, stated because criteria depend on them.** `bash` resolves a `grep` whose interval cap is 255 (BSD on this host), which is why no criterion uses a bounded-repetition interval; BSD `grep` additionally reports a false absence for **every** literal in an extract containing a broken UTF-8 sequence, which is why no criterion cuts a byte window and every read of Japanese content passes `-a` (#434). No criterion requires `shellcheck`, a `timeout` binary, a SHA-256 tool or a Codex CLI: nothing under `bin/` other than the one message line moves, the two suites this spec runs are pure bash, and `CHECK_ACS_TIMEOUT` is inert on this host — raise it anyway and record the value, but do not rely on it as a bound. **AC1** and **AC2** execute the branch-point blob of a shipped script as a script from `$TMPDIR`; that is safe here because the script is standalone and read-only over its board argument, and it is the only place in this spec where committed bytes are executed rather than read.

## Open questions

**None blocking.** Every design call is settled: DP-A includes `:373`, DP-B fixes the class, DP-C fixes the `#409` repair's target property, DP-D fixes the edit form, DP-E and DP-F fix the tier and the review election, and DP-G and DP-H close the two collisions this role found at authoring time. Two non-blocking items are recorded for the coordinating session rather than left as questions. **(i)** T-1100 **AC6** carries a clause asserting `full-population` on a `verify-mechanism — tier1-fanout — conditional` rule that T-1112 already flipped to unconditional; that is a pre-existing supersession in a merged frozen spec, out of this task's scope, and worth a fast-follow note rather than a repair here. **(ii)** T-1019 **AC10**'s one-argument `git diff <ref>` form makes its verdict a property of the working tree rather than of a commit range — the shape the `## Rules` in `agents/pm-spec.md` now forbids for new criteria; it is frozen and merged, so it is disclosed in **A-4** and left alone.

## Notes for engineer

- **The `#409` repair's target property is a class, not a case.** The message must quote **no** unguarded pipeline anywhere in its teaching text: both `grep zzz file | wc -l` and `grep -c zzz file | wc -l` go. Keep the guarded example **exactly** as `(grep zzz file || true) | wc -l` — **AC1** greps that fixed string and **AC2** executes it — and keep the bare `| wc -l` mention, which is what T-1113 AC6 and the fixture suite read. Keep the drop-`-c` teaching by naming the trap (`grep -c`) and its consequence (`double-count`) in prose, exactly as the script's own header already does at `:70`–`:72`, without quoting a pipeline. **AC1** measures this per backtick-delimited span, so a new span naming both `grep` and `wc -l` without `|| true` fails even if it looks harmless.
- **The edit is one physical line: the `fail` call carrying `command exited $rc`.** **AC4** compares everything above and everything below it byte-for-byte against the branch point and requires that line's own line number to be unchanged, so do not reflow, re-indent, split the call across lines, or touch the header. The header idiom is already correct and **AC4** asserts it is still there inside the compared region.
- **The three `| wc -l` readers, and why the fragment must stay out of the echoed command text.** T-1113 **AC9**'s ordering anchor works only because the echoed row's own command text cannot contain `| wc -l`; the remedy is reachable only after the command ran. Nothing you write should put that fragment into the echo path.
- **Both README bullets are one physical line and are cited by line number.** T-1075 **AC6**'s own `- adopter-surface:` line names line 174 in both files. **AC5**/**AC6** require the bullet's line number to be equal at both refs, the file's total line count to be equal, and the committed range's `--numstat` for each README to be exactly `1` added and `1` removed. So rewrite the clause **inside** the existing line; do not wrap, split, or add a sentence on a new line.
- **`Validate` must stay on that same line**, because T-1075 AC6 greps it inside the extracted bullet. The repaired claim keeps it naturally: the election is a Plan-time `verify`-axis dispatch decision transcribed at the **Specify-to-Implement seam**, and the **Validate** phase reads that recorded election. All four of `Plan`, `Specify`, `Validate` and `verify` must appear on each bullet, and **AC7** additionally requires the first `Plan` to sit at a smaller byte offset than the first `Validate` in **both** mirrors — so name the election before the phase that reads it, in both languages, rather than opening with "the Validate phase reads…".
- **The JA mirror keeps its machine tokens in English** (`Plan`, `Specify`, `Validate`, `verify`), matching the convention the same line already uses for `verify-mechanism`, `tier1-fanout` and `- execution-host:`. And keep `あくまで opt-in` where it is — it lives on line 180, not in your bullet.
- **Japanese text: never cut a byte window.** Read whole lines. A window whose cut lands mid-UTF-8-sequence makes BSD `grep` treat the extract as binary and report false absence for every literal in it, ASCII included (#434) — the trap T-1119's engineer phase spent real time on. Every criterion here reads whole lines for exactly that reason; do the same when you verify.
- **`phase-multiplexing.md` is line-count-preserving and in place.** Four rule/recommendation lines plus one follow-up line are rewritten; nothing is added or removed. The board's own dispatch grounds cite `:80`, `:355`, `:362`, `:365`, `:367`, `:369` and `:370` by number, and **AC11** compares the total line count against the branch point.
- **Every edited rule line keeps its exact grammar prefix**, `- dispatch-rule: <axis> — <value> — <modality> — `, including the `trigger: ` token on the two conditionals, because T-1100 **AC6** and T-1112 **AC1** both count by these prefixes (**AC9** re-derives all four). Keep `priced from ` and the `recommendation: tier1-verification-fanout` citation on each one.
- **`:355` keeps `staged-adoption` and keeps the precondition's name.** T-1112 **AC3** requires all five of `mechanism-class`, `execution host`, `judgment`, `staged-adoption` and `per-instance-telemetry-discriminator` on that line, and requires `can actually launch N *agent* instances` to stay absent. Correct the **status** — the precondition has shipped as `bin/check-fanout-instances.sh` with a CI-wired suite — and say `no longer gated`, which is the fixed fragment **AC8** greps.
- **The sharpest trap in this task: do not begin the repaired `:373` with `- follow-up: DISCHARGED`.** T-1112 **AC3** extracts every line matching `^- follow-up: DISCHARGED` and `cmp`s it against a base extraction holding exactly one line — `:375`. A second such line reddens a criterion this task is required to keep green, and it would be caused by the repair's *spelling* rather than by its meaning. Keep a `- follow-up: ` prefix, state the discharge in the body (the lowercase word `discharged` is what **AC10** greps), and name `bin/check-fanout-instances.sh`. `:375` itself is not yours to touch.
- **Leave the seventh `- invariant-lock:` line at `:351` alone.** It records the discriminator as "a follow-up only" for **T-1078's own pilot scope**, which is a true statement about that pilot; **AC11** locks the whole `- invariant-lock:` set byte-identical.
- **Two merged clauses go red by design and you repair neither** — T-1112 **AC1**'s sibling byte-identity clause and **AC2**'s non-`verify-mechanism` sorted-set clause. Record them in `## Blast radius`; do not edit another task's frozen check line. **AC12** is what proves the reddening is scoped rather than sprawling.
- **Files you touch**: `bin/check-count-claims.sh` (one `fail` call), `README.md`, `README.ja.md`, `docs/loop-engineering/phase-multiplexing.md`, `.shell-team/test-recipe.md`, `.shell-team/provenance/T-1120.md` (new), this spec (status flag only) and the board. `.shell-team/interventions/T-1120.md` **already exists and is committed** — do not write it. `.shell-team/reviews/T-1120.md` is the reviewer's. Nothing under `templates/`, `agents/`, `skills/`, `tests/`, `.github/` or anywhere else in `bin/`, which **AC13** and **AC14** both enforce.
- **Run `bash bin/check-pii-shapes.sh --base <branch point>` before the first commit of every new record**, not as a formality — the CI diff-time check is the last-resort backstop, and a value that reaches pushed history costs a history rewrite.
- **Measured-at-ref command check — applicable, and discharged in the criteria.** The `## Blast radius` record's `- base-ref:` line is the one label naming a ref its results were measured at, and its `- method:` line states that every base-side verdict comes from a detached worktree at that commit. Every base-side read in **AC1**, **AC2**, **AC4**–**AC13** uses `git show "$B:<path>"` or `git merge-base`, never the working tree; **AC5**/**AC6**/**AC7**/**AC13**/**AC14** read a committed **range** with the three-dot form; and the `- reproduce:` command is a live re-derivation carrying no ref label, so nothing here prints a ref-labelled result that a working-tree read produced. The one deliberate exception is named rather than hidden: **A-4** records that T-1019 AC10's *own* frozen check line uses the one-argument working-tree form, which is a property of that merged criterion and not of anything this task writes.
- **Marker discipline**: this spec carries exactly **one** marker pair, the intent block. `bin/check-intent.sh` matches its markers by exact whole-line equality against a task-id-interpolated literal. The `## Blast radius` derivation block's `<!-- BEGIN derivation: t1120-read-set -->` / `<!-- END derivation: t1120-read-set -->` pair sits **outside** the intent block, in mutable prose, and is the freeze run's to fill.
