# Default-path firing of `verify — tier1-fanout`: the shipped precedence rule, the transcribed dispatch record and the shipped fan-out mechanics run once end to end on this task's own full-population blast-radius sweep, against a same-population serial baseline, producing one release-citable reachability verdict

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1085

**Branch**: `feature/1085-default-path-firing`, created before this spec was written and stacked on `feature/1084-dispatcher`'s tip (PR #325). PR base stays `develop`; the PR is held open and merges at the sprint batch GO. This is the sprint's final task and its release gate.

## Problem

Issue #277's adoption preconditions are now all shipped: `bin/aggregate-verdicts.sh` reduces N part files to one authoritative verdict (T-1074), `bin/check-fanout-instances.sh` refuses an unattributable aggregation (T-1082), `docs/loop-engineering/agent-launch-fanout.md` records real plugin-role instances launching concurrently and a real end-to-end aggregation (T-1083), and the situational dispatch record plus its precedence rider tell an orchestrator which mechanism to run and where to write the choice down (T-1084). Every piece exists. Not one of them has ever been reached by the shipped default path on a real situation.

That gap is precisely what T-1084's own board record says about itself: it recorded `dispatch: verify — serial` with the ground that the conditional trigger's second conjunct — an orchestration step able to launch N agent instances and aggregate their verdicts — "has never fired on the default path". A capability nobody has exercised through the path an adopter would actually take is not a capability an adopter has. Until it fires once, the whole Tier-1 line is machinery with a shipped price and no shipped use, and the sprint has nothing to release.

## Summarized sources

- `skills/run/SKILL.md` lines 39–101 and 153–215 — read first-hand. Six distinctions this spec rests on. **(i)** Line 42: the orchestrator transcribes each Routing-Map dispatch decision onto the task's board entry "at the Specify-to-Implement seam below, once `pm-spec` has created that entry in step 2 and before `engineer` is invoked in step 4". **(ii)** Line 85: the step-5 fan-out decision point is "demoted to a mechanics pointer (T-1084)" and states that "the `verify`-axis `dispatch: verify` record on the task's board entry now owns the decision"; it names as its own eligibility example "a fixed, mechanically-derived set of verification units such as one `bin/check-acs.sh` run per spec in a full-population blast-radius sweep", says "QA's own judgement work is never a candidate, and stays single-instance regardless", and closes: "When the recorded `dispatch: verify` value is `tier1-fanout`, the mechanics — population, degree, liveness and aggregation — are the shipped fan-out step below this list." **(iii)** Line 161's parenthetical, the sentence this spec's core reading turns on: "**opt-in; no phase is rewired to use it by default** … Using this step is always an explicit choice for that phase; a loop that ignores it behaves exactly as it does today." **(iv)** Lines 163–171: the mechanics — population enumerated once before any instance is launched and never re-selected after a verdict is seen; every unit assigned to exactly one instance with one output file per instance; the degree rule `N = min(<unit count>, cap)`; a per-instance liveness check started at launch; `--instance <role-qualified-id>` spans emitted by the orchestrator with `--seq auto`; the reduction and attribution commands; the launch record's field-by-field grammar; and the four `audit-only` fields. **(v)** Lines 204–215, the consumer copy of the dispatch-record block, including "Apply the most specific conditional trigger whose stated trigger text the task actually satisfies". **(vi)** Line 70 places Validate (and therefore step 5's mechanics) **after** Implement, which is why this task's firing cannot precede the engineer the way T-1083's out-of-loop probe did.
- `templates/prompt-blocks/fanout-orchestration.md` — read in full (all 21 lines). The distinction: it is the canonical copy of `skills/run/SKILL.md` lines 161–180, line for line, including the same `opt-in; no phase is rewired to use it by default` parenthetical, the same degree rule, the same launch-record grammar and the same seven `- invariant-lock:` lines. This spec asserts this file is byte-identical to its branch-point blob, which is a behavioural claim about it, so it is cited here.
- `templates/prompt-blocks/dispatch-record.md` — read in full (all 12 lines). Two distinctions. **(i)** Line 3 is the precedence rule verbatim: apply the most specific conditional trigger the task actually satisfies; fall back to the axis-level unconditional default only when no conditional trigger applies. **(ii)** Lines 5–9 fix the record grammar `- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>`, the two closed value sets (`implement` over `serial|tier2|tier3`, `verify` over `serial|tier1-fanout`), "each axis key appears at most once per entry, and every axis the note defines is recorded", and that `<ground>` "opens with a priced line id the note already records — `saving: <id>`, `recommendation: <id>`, `break-even: <id>` or `cost-input: <id>`". This spec asserts this file is byte-identical to its branch-point blob.
- `docs/loop-engineering/phase-multiplexing.md` lines 330–390 — read first-hand. Five distinctions. **(i)** Line 355: `- recommendation: tier1-verification-fanout — staged-adoption` — "the only tier with a real, `measured`, positive wall-clock saving (**43.84%** at this pilot's own swept degrees)", gated on two named preconditions, the second being "an orchestration step (in `skills/run/SKILL.md` or equivalent) that **can actually launch** N *agent* instances for one phase and aggregate their verdicts". **(ii)** Line 359: `- dispatch-axis: verify — serial|tier1-fanout`. **(iii)** Line 364, the conditional rule this task satisfies: `- dispatch-rule: verify — tier1-fanout — conditional — trigger: the phase's own verification work is mechanically enumerable into a real, read-only population of independent units, and both of ... named preconditions (the per-instance-telemetry-discriminator invariant, and an orchestration step **able to** launch N agent instances and aggregate their verdicts) **exist**`. The trigger's second conjunct is written as an existence-capability test — *able to*, *exist* — not as a "has already fired" test. **(iv)** Line 370: the follow-up asking for a re-run of the pilot's sweep "over the full 73-file spec corpus, or a larger deterministically-selected subset, once a wall-clock budget larger than this pilot's own ~9-file allowance exists". **(v)** Line 375: the measured knee "is a property of this machine's core count, not a portable constant", and the plateau point `n≥4` is additionally a property of that population's own cost distribution.
- `docs/loop-engineering/agent-launch-fanout.md` — read at its section index plus lines 1–150 and 185–257. Five distinctions. **(i)** Line 186: the `aggregation-live` arm — "the `plugin-role-n2` instances' real part files reduced by `bin/aggregate-verdicts.sh` (exit 0) and verified by `bin/check-fanout-instances.sh` (exit 0) against the probe's own real telemetry" — "the only end-to-end evidence precondition 3 has ever had". **(ii)** Line 200: `- overlap: plugin-role-n2 — overlapping — margin_ns=140385350000`, ≈140.385 s of genuine two-instance simultaneous execution; line 217: ≈100.573 s of genuine four-way overlap. **(iii)** Line 247: `subagent_type` is recorded as a **declared label** — the orchestrator's own launch parameter — explicitly "not harness-originated evidence of the *resolved* type", with the counterfactual stated. This task must not upgrade that claim. **(iv)** Line 242: `- umbrella: 277 — open` — "the live-firing half remains owed to T-1085". **(v)** Its nine-heading structure and its `- probe-arm:` / `- agent-timestamp:` / `- heartbeat:` / `- liveness:` / `- checker-result:` / `- licence-condition:` / `- implication:` line grammars, which this spec adapts.
- `bin/aggregate-verdicts.sh` lines 1–100 — read first-hand. Four distinctions. **(i)** The part-file grammar is exactly three shapes: `- unit: <unit-id>`, `- verdict: <unit-id> — <payload>`, `- sentinel: <unit-id> — exit=<n> no-verdict-lines`; a `- unit:` line is a **claim** independent of whether the unit produced output. **(ii)** Lines 31–35 name **`design-note-T-1012.md`** by name as the pilot's own real instance that recorded `SENTINEL: exit=2 no-verdict-lines` identically across ten arm/rep runs — which is the first-hand ground for this spec's decision to include non-spec `.md` files in the population rather than filter them out. **(iii)** Lines 37–45: reconciliation is by **value, as a multiset, ordered by one `LC_ALL=C` sort over whole lines**, never by row position; a duplicate verdict payload is legitimate input, a duplicate claim is refused. **(iv)** Lines 53–63: the nested `verdict-region` "must stay byte-identical under every disjoint, exhaustive partition of the same population and under any ordering of records within a part or of the parts themselves — that property is what licenses reducing N partial verdicts to one authoritative verdict at all"; `- attribution:` lines sit **outside** it. Exit 3 is the incomplete-fan-out refusal (`missing-part`, `empty-part`, `uncovered-unit`).
- `bin/check-fanout-instances.sh` lines 1–120 and 145–190 — read first-hand. Three distinctions. **(i)** The four properties it enforces: presence, grammar (`^[a-z][a-z0-9-]*$`, the writer's own `BINDING_TOKEN_RE`), uniqueness (no id claimed by rows of two different roles), consistency (the scope's instance set and the block's `- part:` name set equal in both directions). **(ii)** Its usage line: `--telemetry <file> --run-id <id> --phase <phase> --aggregation <file> --label <label> [--iteration <n>] [--attempt <n>]`, with the help text's own warning that "a scope naming a run_id/phase pair that mixes a serial round with a fanned round of the same phase refuses missing-instance; narrow it with `--iteration`/`--attempt`" — the first-hand ground for this spec's requirement that the two arms are separated by an `--attempt` narrower. **(iii)** Its success line is exactly `check-fanout-instances: ok: run-id=<id> phase=<phase> label=<label> rows=<n> parts=<n>`.
- `bin/derive-populations.sh` — read in full. Three distinctions. **(i)** Two to eight `--set NAME=COMMAND` values, `--label` and every set name validated against `^[A-Za-z0-9][A-Za-z0-9_-]*$`; nine or more sets is a usage error. **(ii)** The emitted block's byte-exact grammar (`- derived-by:`, `- locale: LC_ALL=C`, one `- set:` line per set carrying its own exit status, `- union: items: <n>`, one `- bucket: <sig> — items: <n>` per non-empty membership signature followed by its items). **(iii)** An accepted status with no output is reported as `lines: 0 — items: 0` and never folded into a silent zero — so a derivation over two difference sets that are both genuinely empty is a valid, and maximally strong, emitted block.
- `bin/check-acs.sh` lines 49–115, 141 and 224–235 — read first-hand. Three distinctions. **(i)** `check-acs.sh [--dry-run] [--root <dir>] <spec.md>`; `--root <dir>` runs each `check:` command from `<dir>` instead of the cwd. **(ii)** `AC_RE='^- \[[ xX]\] \*\*AC-?([0-9]+)([^0-9A-Za-z_:.-]|$)'` — a suffixed label such as `**AC19b**` is not recognised, which is why every criterion here is plainly numbered. **(iii)** The per-check cap is `${CHECK_ACS_TIMEOUT:-120}` and an invalid value falls back to 120 with a warning — which is why this spec states the raised value rather than assuming it, and why the value used inside the arms is a recorded, arm-identical parameter.
- `bin/team-paths.sh` lines 24, 37, 68 and 174–194 — read first-hand. The distinction: `--get KEY` resolves the closed ten-key set `base|todo|loops|runs|retros|reviews|specs|provenance|interventions|lessons`, and `TEAM_RUNS_DIR` is the environment override for the `runs` key — so "the runs directory resolved by default" means running with `TEAM_RUNS_DIR` unset, which is what this task's launch record and telemetry require.
- `.shell-team/.gitignore` line 4 — read first-hand: `runs/`. The distinction: everything the fan-out writes under the resolved runs directory — the launch record, the telemetry corpus — is untracked by construction, so a verbatim quote inside the tracked note is the only durability path this evidence has.
- `bin/check-intent.sh` lines 88–90, 243–266 and 295–296 — read first-hand, because this spec places a second marker pair inside its own Goal. Two distinctions. **(i)** `--print-hash <spec.md>` prints the frozen intent block's 40-hex hash and one trailing LF. **(ii)** `marker_count() { awk -v m="$1" '{ sub(/\r$/, "") } $0 == m { n++ } ... }` — whole-line equality with a trailing carriage return stripped, and the pair it counts is task-id-scoped, so a differently-named `firing-protocol` pair is inert to it.
- `agents/*.md` frontmatter — read across **all nine** files first-hand (`ui-designer`, `tech-lead`, `drift-evaluator`, `triage-orchestrator`, `qa-verifier`, `engineer`, `scrum-master`, `pm-spec`, `codex-reviewer`). Two distinctions. **(i)** Not one `tools:` line carries an `Agent` or `Task` token, so no sub-agent in this loop can launch another sub-agent and only the orchestrator session can perform the firing. **(ii)** `agents/qa-verifier.md` carries `tools: Read, Grep, Glob, Bash` and `model: sonnet` — it can run `bash bin/check-acs.sh`, which is why it is the role the fan-out launches, and it is also the role the shipped step-5 fan-out belongs to.
- `.shell-team/specs/T-1083-agent-launch-fanout.md` — read in full. Four distinctions this spec adapts. **(i)** The nested `<!-- BEGIN probe-protocol: T-1083 -->` marker pair inside the Goal, frozen before execution, byte-compared between the evidence commit and HEAD. **(ii)** The `- probe-evidence-sha (T-1083): <40-hex>` board sub-bullet, which is how a criterion resolves an evidence commit with no literal typed into the spec. **(iii)** Its Pre-commitment's shape: factual trigger, contextual condition, an ordered droppable list decided before round 1, and a never-dropped set whose defeat stops the task. **(iv)** Its `## Notes from engineer` disclosure that a whole-file `grep -c '^- verdict: '` collided with `bin/aggregate-verdicts.sh`'s own real output format — the first-hand reason no criterion in this spec counts a `- verdict: ` or `- unit: ` prefix note-wide.
- `.shell-team/specs/T-1084-dispatch-routing-record.md` lines 40–41, 106–109, 225–236 and 337–365 — read first-hand. Three distinctions. **(i)** The declaration region's shape and the `mechanism` class's wording that "nothing is priced down, so the downstream-impact inventory is owed as a full-population diff". **(ii)** Its **AC16**, the derivation-block criterion this spec's **AC15**/**AC16** adapt: equal `BEGIN`/`END`/`- reproduce:` counts, every reproduce command run, exit 0, non-empty, byte-identical to the block its own `BEGIN` line labels. **(iii)** Its measured Blast radius: the spec population moved `88→89`, the key reconciliation was `1456` base keys against `1473` head keys with all 17 head-only keys belonging to its own new spec, and three genuine new reds were byte-identity or line-survival locks that only running every criterion could find.
- `.shell-team/todo.md` lines 1–14, 1270–1307 — read first-hand. Three distinctions. **(i)** The board's strict active-line format `- [ ] **T-NNN** <title> — \`<FLAG>\` — spec: <path>` with every annotation on indented sub-bullets. **(ii)** T-1084's entry carries the grammar's first two live `- dispatch:` records, and its `verify` record reads `serial — unconditional` with the recorded ground that the conditional trigger's second conjunct is unsatisfied because the orchestration step "has never fired on the default path" — the reading this spec diverges from, and the divergence is stated rather than hidden. **(iii)** T-1083's and T-1084's `- freeze note for the coordinating session:` and `- freeze-attestation` sub-bullets, the house shape this task's entry follows.
- `agents/pm-spec.md` — read first-hand (this role's own contract), in particular the `## Spec completion self-check` items on declaration reads, verification pricing, the stacked-branch base-ref discriminator, downstream-impact inventory method and borrowed-vocabulary count premises. The distinction: all of these are freeze-time reads this role discharges by reading, not by an exit code, and issue #250 carries the mechanical gate for the first.

## Goal

<!-- BEGIN intent-block: T-1085 -->

- user-visible: no — the deliverables are an internal measurement note (`docs/loop-engineering/default-path-firing.md`) and this task's own records. No shipped file changes: **AC1** measures a zero diff across `skills/`, `templates/`, `agents/`, `bin/`, `tests/` and `.github/`, tracked and untracked halves both. An adopter's loop behaves after this task exactly as it behaves before it, because nothing an adopter executes is edited. Consequently no `- adopter-surface:` line and no `- adopter-docs-waiver:` line appears anywhere in this spec: the `no` declaration carries neither marker.
- verification-class: mechanism — declared at the full-price class deliberately, and the reason is a coupling rather than a formality. This task's own diff reaches no executing surface (**AC1** measures exactly that), which by the letter would read `no-mechanism`; but the **full-population downstream-impact diff** the `mechanism` class owes **is** the population the fan-out fires on. Pricing the inventory down to a read-set-scoped analysis would delete this task's own subject and, with it, the trigger clause the whole task exists to satisfy. **Nothing is priced down**: the full-population inventory, full CI-equivalence for every step this diff can reach, and mutation probes for every `- check:` line added here are all owed exactly as written.
- base-ref-discriminator: `B=$(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base "feature/1084-dispatcher" HEAD; else git merge-base "develop" HEAD; fi)` — the predecessor branch `feature/1084-dispatcher`'s merge-base with HEAD while that branch resolves, `develop`'s merge-base with HEAD once it does not, the arm selected by an explicit branch-existence test against the namespace this checkout actually carries the predecessor in (`refs/heads/`, **relayed** at authoring time and measured by the freeze run) and never by a `2>/dev/null ||` chain. Spelled byte-identically in every criterion that reads a base-side blob; no 40-hex literal appears in any of them. Residual cases disclosed rather than engineered around: a predecessor branch deleted without being merged, or rebased, force-pushed or squash-merged after this branch was cut, invalidates the stack and is a route-back to planning, not a case either arm is redesigned to survive; a checkout that never fetched the predecessor at all is a fetch-depth gap and a route-back, never the fallback arm firing.

**Goal (one sentence).** The shipped default path is made to fire `verify — tier1-fanout` once, on a real situation and with no shipped byte changed — `tech-lead` applies the shipped precedence rule and prints the decision, the orchestrator transcribes `- dispatch: verify — tier1-fanout` onto this task's board entry, `skills/run/SKILL.md`'s step-5 mechanics pointer reads that record, and the shipped fan-out step runs its own population/degree/liveness/aggregation mechanics over this task's own full-population blast-radius sweep with N real `qa-verifier` instances — and a new note at `docs/loop-engineering/default-path-firing.md` records the whole chain link by link against a same-population single-instance baseline, closing with exactly one release-citable `- default-reachability: met|not-met — <ground>` line.

**What "the default path" means here, frozen first because everything else depends on it.** The default path is the shipped, unmodified text of `skills/run/SKILL.md`, `templates/prompt-blocks/fanout-orchestration.md` and `templates/prompt-blocks/dispatch-record.md`, applied as written. Its links, in order: (1) `tech-lead` applies `templates/prompt-blocks/dispatch-record.md`'s precedence rule — "apply the most specific conditional trigger whose stated trigger text the task actually satisfies" — to the `verify` axis and prints the decision in its Routing Map's `### Dispatch` element; (2) the orchestrator transcribes that decision onto this task's own board entry at the Specify-to-Implement seam, per `skills/run/SKILL.md` lines 42 and 207; (3) at step 5 the mechanics pointer at `skills/run/SKILL.md` line 85 reads the recorded `dispatch: verify` value and, finding `tier1-fanout`, routes the phase's mechanically-enumerable verification work to the shipped fan-out step; (4) that step's own mechanics run unchanged. Nothing outside those three files selects the fan-out, and no environment variable, CLI flag, configuration file or prompt instruction outside them is permitted to. `TEAM_FANOUT_MAX` and `CHECK_ACS_TIMEOUT` may be set, because the first is named inside the shipped degree rule and the second is a unit-level parameter that selects nothing; every value actually used is recorded.

**The reading of `opt-in; no phase is rewired to use it by default`, adopted here and frozen.** That parenthetical (`skills/run/SKILL.md` line 161, `templates/prompt-blocks/fanout-orchestration.md` line 1) means **no phase fires the fan-out automatically, in every task, without a recorded per-task decision** — its own following sentences say so in those words: "Using this step is always an explicit choice for that phase; a loop that ignores it behaves exactly as it does today." It does **not** mean that reaching the fan-out requires a flag, switch or instruction from outside the shipped text; no such flag exists anywhere in the three files, and inventing one would be the departure, not the compliance. The explicit choice the sentence demands is exactly the recorded `- dispatch: verify — tier1-fanout` sub-bullet T-1084 shipped a grammar, a precedence rule and a fail-closed close-out gate for. **If this reading is wrong, then even a successful firing is `not-met`** — the licence condition `no-out-of-band-flag` exists to carry that judgment, and the verdict line is written against it rather than against the mere fact that N instances ran.

**The divergence from T-1084's own board record, stated because the review will attack it first.** T-1084 recorded `dispatch: verify — serial` on the ground that the conditional trigger's second conjunct was unsatisfied, reading "an orchestration step **able to** launch N agent instances and aggregate their verdicts **exist**" (`docs/loop-engineering/phase-multiplexing.md` line 364) as "has already fired on the default path". This spec adopts the **existence-capability** reading instead, for three stated reasons. (i) The rule's own words are a capability test — *able to*, *exist* — and the note's `- recommendation:` line at 355 spells the same conjunct as a step "that **can actually launch** N *agent* instances". (ii) The stricter reading is self-looping: firing is never permitted until it has fired, so the trigger could never be satisfied by any task, and a conditional rule no task can ever satisfy is a rule with no reachable branch. (iii) The capability is not asserted, it is measured: T-1083's `aggregation-live` arm launched real plugin-role instances and drove `bin/aggregate-verdicts.sh` and `bin/check-fanout-instances.sh` to exit 0 end to end against real telemetry (`docs/loop-engineering/agent-launch-fanout.md` line 186), and precondition 1 shipped as `bin/check-fanout-instances.sh` in T-1082. T-1084's stricter reading was a conservative choice made when nothing had fired and is not treated here as an error on its part; what this task changes is that the conjunct is now discharged by measurement rather than by argument. The divergence is recorded in the note and the note states both readings.

**Why the firing sits in Validate, and the rework cycle that implies.** `skills/run/SKILL.md` places Validate at step 5, after Implement at step 4. The fan-out mechanics belong to step 5. This task therefore cannot use T-1083's shape, where an out-of-loop orchestrator probe ran between Specify and Implement and handed the engineer already-committed evidence: firing before Implement would put the mechanics somewhere the shipped text does not put them, which is the one thing this task must not do. So: the engineer's first round ships the note's static sections (title, `## Terms and closed vocabularies`, `## Firing protocol (frozen before execution)`, `## Supersession and follow-ups`, and the parts of `## Limits and what is not computable` that do not depend on the result); the orchestrator fires at the Validate seam and commits the raw evidence into `## Firing evidence (raw, orchestrator-produced)`; and the analysis, verdict and saving sections are assembled by the engineer round that follows. That following round is a **planned** cycle of the loop, not a defect, and it is disclosed here so nobody reads the resulting first QA `FAIL` as a quality signal. **AC3**'s ordering assertion is correspondingly weaker than T-1083's — it requires the evidence commit to be a descendant of the branch point and an ancestor of HEAD, and the evidence section to be byte-identical between that commit and HEAD, but it does **not** require the evidence to precede the engineer's first invocation, because the shipped path forbids that. Fitting is prevented instead by two locks that do hold: **AC4**'s byte-lock on the frozen firing protocol, and **AC3**'s byte-lock on the evidence once committed.

<!-- BEGIN firing-protocol: T-1085 -->

**The unit and the population.** The unit is `bash bin/check-acs.sh <spec>` — this repository's own acceptance-criteria gate, unchanged, one run per file. The population is **every `.md` file the resolved specs directory (`bin/team-paths.sh --get specs`) holds** at the pinned commit, with **no name-based filtering**. Non-spec `.md` files are included rather than excluded, on three grounds: the shipped step requires a *mechanically enumerable* population, and a name-based exclusion is an authoring judgment made after seeing the list; the unit is **the run, never its verdict**, so a file with no acceptance criteria is a legitimate unit whose result is an absence; and `bin/aggregate-verdicts.sh`'s own header names `design-note-T-1012.md` by name as the pilot's real instance that recorded `SENTINEL: exit=2 no-verdict-lines` identically across ten arm/rep runs — the `- sentinel:` line exists precisely so that unit is a datum rather than a gap. The population is written to a population file **before any instance is launched** and is never re-selected after a verdict is seen. Its per-file costs are measured singly, before any arm runs, so the largest single-unit cost is known before the per-call timeouts are chosen.

**The subject is this task's own owed work, not a synthetic errand.** The population is the head-side half of this task's own `mechanism`-class full-population blast-radius sweep — work this task owes regardless of how it is executed, which is what makes it the least synthetic subject available. The base-side half of that sweep is ordinary serial blast-radius work, is **not** part of the fan-out, and is run separately.

**Venue, and the boundary the venue does not cross.** Every unit executes inside a throwaway `git clone --no-hardlinks` under `$TMPDIR`, checked out detached at one pinned commit. `git worktree add` is **never** used for a venue, because a worktree registers under `.git/worktrees` in the real checkout and turns cleanup into a destructive step this task takes no version of. Each arm gets its **own freshly-created clone of the same pinned commit**, so neither arm inherits the other's telemetry side effects — the specific artefact class T-1082's, T-1083's and T-1084's own Blast radius sections each disclosed, where a criterion reading the machine-local telemetry corpus flips because an earlier sweep grew it. Each instance runs under its own `TMPDIR` subdirectory, recorded. The shipped fan-out step fixes the population, the degree, the liveness, the assignment and the reduction; it fixes **no execution venue for a unit**, so pinning the unit venue is a choice the shipped text permits rather than a departure from it. What does **not** move into a clone: the decision, the board's dispatch records, the launch record, the telemetry and the aggregation all run in the **real checkout's shipped paths**, with the runs directory resolved by default (`TEAM_RUNS_DIR` unset) — because those are the default path, and redirecting them would be the departure.

**Degree and baseline.** `N = min(<unit count>, cap)`, with `cap` from `TEAM_FANOUT_MAX` when it is set to a positive integer, otherwise the host's own measured core count, otherwise 2 — the shipped rule, unchanged, with N and the cap's ground recorded on the launch record's own `- requested-n:` and `- cap-ground:` lines. The baseline is a **single-instance** run of the **same** population, in the **same session**, in a second clone pinned to the **same** commit, with the **same** `CHECK_ACS_TIMEOUT` value exported in both arms and recorded — a per-check cap that passes serially and fires under N-way contention would otherwise change verdicts between arms and make the parity comparison meaningless. Execution order and cache warmth are recorded. **No repetitions and no inferential statistics**: one execution per arm, spread disclosed descriptively or not at all, no significance test, confidence interval, variance model or regression anywhere in the note. The baseline arm is not itself a fan-out and writes **no launch record**; its single part file is nonetheless reduced by `bin/aggregate-verdicts.sh` under its own label, purely so the two arms produce comparable `verdict-region` regions.

**The aggregation-safety comparison.** `bin/aggregate-verdicts.sh`'s own header states that the nested `verdict-region` "must stay byte-identical under every disjoint, exhaustive partition of the same population … that property is what licenses reducing N partial verdicts to one authoritative verdict at all". The two arms are exactly two such partitions of one population, so the comparison is that byte-identity, recorded on one `- verdict-region-parity: identical|differs — <text>` line. A difference is **enumerated and diagnosed, never glossed**: each differing key gets its own `- parity-exception: <key> — <cause> — <isolation-re-run result>` line, re-run alone and contention-free before its cause is named. A parity difference is a disclosed finding about the measurement, **not** a `not-met` verdict — the verdict answers whether the default path fired, not whether two runs of a timing-sensitive checker agreed.

**Part-file grammar.** Each instance writes one part file in `bin/aggregate-verdicts.sh`'s own three-shape grammar: one `- unit: <spec-path>` claim per assigned unit; one `- verdict: <spec-path> — rc=<n>; <the check-acs summary line>` per unit whose run produced a summary; and one `- sentinel: <spec-path> — exit=<n> no-verdict-lines` for a claimed unit whose run produced none. One verdict line per unit rather than one per acceptance criterion, deliberately: the per-criterion key set is what the blast-radius reconciliation needs and it is preserved in each instance's own raw outputs, whereas the aggregation block is the note's durable quoted evidence and a per-criterion block would run to thousands of lines.

**Hazards, measured rather than assumed.** A sub-agent's default foreground Bash timeout is **120 s** (`skills/run/SKILL.md` line 166) while a single measured unit in this corpus has cost ≈143 s (`docs/loop-engineering/phase-multiplexing.md` line 371, `T-1044-test-infra-bundle.md` alone at 142965525000 ns), so **every Bash call an instance makes carries an explicit per-call timeout**, sized above the largest single-unit cost from the pre-arm cost pass, and the value used is recorded. A watchdog launched with a bare shell `&` inside a foreground Bash call dies with that call, so **every** per-instance liveness check is launched harness-tracked-background, one per instance, started at the moment that instance is launched, with its command recorded verbatim.

**Telemetry scope separation.** Both arms emit orchestrator-written span rows into the same default-resolved telemetry corpus, so the two are separated by the `--attempt` narrower and by disjoint instance-id prefixes (`qa-*` for the fanned arm, `serial-*` for the baseline). `bin/check-fanout-instances.sh` is run for the fanned arm's scope with that narrower, per its own help text's warning that a scope mixing a serial round with a fanned round of the same phase refuses `missing-instance`.

**The verdict.** Exactly one `- default-reachability: met|not-met — <ground>` line in the whole note. It reads `met` when all **seven** chain links read `evidenced` and all **six** licence conditions read `met`, and `not-met` otherwise — both directions, with no third value and no hedge. Chain links, in order: `precedence-rule-applied`, `dispatch-record-transcribed`, `step5-pointer-read`, `population-fixed`, `instances-launched`, `aggregation-reduced`, `attribution-verified`. Licence conditions: `shipped-text-unmodified`, `no-out-of-band-flag`, `real-plugin-role-instances` (N ≥ 2, the `subagent_type` recorded verbatim as a declared label), `genuine-overlap` (a non-empty intersection window re-derivable from the recorded timestamps, with its margin against the maximum single launch latency stated), `real-population`, `production-unit`. A `not-met` verdict carries exactly one `- not-met-ground: chain|licence — <text>` line, and when that reads `chain`, exactly one `- blocked-at: <chain-link-id> — <text>` line naming the first link that failed.

**The claim ceiling, fixed before the measurement.** **43.84%** is `docs/loop-engineering/phase-multiplexing.md`'s pilot figure over a 9-file stride subset at swept degrees on one machine. It is **not** this task's claim and is never restated here as one: every line in the note mentioning it also names `phase-multiplexing.md` at the point of mention. Whatever this task measures is reported honestly, in either direction. **A small saving is still `met`. A negative saving is still `met`. Only a failed firing is `not-met`** — the release gate asks whether the shipped default path can be reached, not whether it was fast. The knee is **not** measured here: one degree is run, so the plateau point stays `undetermined` whatever the population.

**A `not-met` verdict is the approved fallback and is never papered over.** `met` licenses the sprint's planned minor release; `not-met` licenses merge-only, with the note recording which link or condition failed and what would settle it. The note states the consequence of **both** values on its own `- release-consequence: met — <text>` and `- release-consequence: not-met — <text>` lines, written before the result is known, and the `not-met` line names merge-only explicitly. Unlike T-1083, a negative result here does **not** stop the task: the note ships, the criteria are claimed, and the deliverable is the honest record. Every criterion below is written to be satisfiable under both verdicts, and each evidence-heavy criterion is conditioned on **its own chain link's recorded value** rather than on the global verdict, so a `not-met` outcome can never be used to dodge evidence for a link that did in fact complete.

**Subagent type stays a declared label.** `docs/loop-engineering/agent-launch-fanout.md` line 247 records `subagent_type` as the orchestrator's own launch parameter — a declared label, explicitly not harness-originated evidence of the resolved type, with the counterfactual stated. This task records the value verbatim on the same footing and **does not upgrade that claim**; nothing here is evidence that the declaration is true, and the note says so.

**Prose quotes a grammar mid-line, never at line start.** Every criterion below that counts a record prefix — `- default-reachability:`, `- chain-link:`, `- licence-condition:`, `- checker-result:`, `- liveness:`, `- saving:` and the rest — counts real records, and a grammar *restatement* opening a line with the same prefix would be indistinguishable from one. So: wherever the note's `## Terms and closed vocabularies`, `## Firing protocol (frozen before execution)` or any other prose section wants to name one of these grammars, it quotes it **mid-line**, inside backticks, and never as the first non-space characters of a line. This is not a new rule invented here — it is the shipped idiom `templates/prompt-blocks/dispatch-record.md` already states for exactly this hazard on the board ("prose that wants to mention this grammar quotes it mid-line instead of opening a line with it"), reused verbatim in shape with only the governed prefixes changed. The same reason applies to quoting another document's records: a line from `docs/loop-engineering/phase-multiplexing.md` such as its own `saving:` or `recommendation:` line is quoted mid-line here too, never reproduced at line start.

**Pre-commitment (AI self-discipline — self-imposed by this loop at freeze time, never operator-ratified; any report citing it says so).** Factual trigger: two consecutive review rounds landing an independent Blocker or Major against the same component. Contextual trigger: before a third round of rework on that component starts; where the two disagree, the factual trigger governs. **Second, independent trigger**: the serial baseline arm's own wall clock exceeding **90 minutes**. Disposition, decided here rather than when it fires — there is exactly **one** droppable component, and everything else in this task is the firing chain or its evidence: drop **the full-corpus population**, replacing it with a subset selected by a fixed stride, deterministically and **before any verdict is read**, both arms re-run on the shrunk population, and issue #277's full-corpus follow-up (`phase-multiplexing.md` line 370) recorded as still owed. A deviation from the mandate's phrasing, stated rather than glossed: what that drop actually costs is the **full-corpus scale of the saving figure**, not the knee — the knee is `undetermined` in this task either way, because one degree is run. **Never dropped**: the firing chain itself — the dispatch record, the launch record, N real instances, the aggregation, `bin/check-fanout-instances.sh` green, and the same-population serial comparison. Defeated twice on any of those, the task **stops and returns to planning** rather than being patched a third time; there is no carve-out to execute for a never-dropped component. **Invalid-evidence rule**: at most **one** re-firing, recorded with the round and the reason that invalidated the first; a second invalidation stops the task.

<!-- END firing-protocol: T-1085 -->

## Non-goals

- **No shipped-text change of any kind.** Nothing under `skills/`, `templates/`, `agents/`, `bin/`, `tests/` or `.github/` is added, edited or deleted, tracked or untracked. If the firing turns out to **demand** a shipped-text change, that demand is a finding filed as an issue and carried to the next planning round — it is never applied here — and if it blocks the firing, the verdict is `not-met` and the task merges without a release.
- **No new checker, no new test suite, no CI edit, no new `bin/` script.** The fan-out is driven entirely by the two shipped fail-closed checkers.
- **No repair, widening, annotation or retro-fit of any merged spec's criteria**, including any this task's diff or this sweep reddens. Every red is disclosed in `## Blast radius`; none is fixed here.
- **No edit to `docs/loop-engineering/phase-multiplexing.md`, `docs/loop-engineering/agent-launch-fanout.md`, `docs/loop-engineering/agent-concurrency.md` or `docs/loop-engineering/harness-agent-concurrency.md`.** Any supersession is **declared** with a `- follow-up:`, never performed.
- **No knee-finding, no degree sweep, no second repetition of either arm, and no inferential statistic** — no significance test, confidence interval, variance model or regression, over either arm or over any series the note cites.
- **No AC-level fan-out granularity.** The unit is one spec file. Splitting the largest single unit into per-criterion units is `phase-multiplexing.md`'s own separate follow-up (line 371) and is untouched here.
- **No model, harness or machine generalization.** The launched model and `subagent_type` are recorded verbatim as declared labels; whether the result extends to another model, harness version or machine is `unobserved`.
- **No forced termination.** No `TaskStop`, no `kill`, no signal to any instance. An instance that ends its own turn having written no part is an ordinary outcome the shipped exit-3 path already covers.
- **No `git worktree add` for any arm's venue, and no write of any kind by a unit into the real checkout** — including into the untracked machine-local telemetry corpus, which is why every unit runs inside a throwaway clone.
- **No change to `bin/log-run.sh`, `bin/check-run.sh` or the telemetry row schema**, no new key, no new flag, no relaxed validation.
- **No issue closed and none reopened.** Issue #277 stays open; whether this task's firing discharges its remaining live-firing half is recorded in the note as a statement about the evidence, and the close decision belongs to the sprint's batch close-out.
- **No version bump, no CHANGELOG edit, no plugin-manifest edit, no release-notes edit, no lessons-corpus edit, no board archiving or rotation, no retro, and no merge or push to `develop`** — the release decision this task's verdict feeds is the operator's at the sprint batch GO, and this task neither performs nor pre-empts it.
- **No dollar cost, vendor price model, token multiplier or estimated value** anywhere in the note; every figure is either read from a named git-tracked source or measured live on this host and labelled machine-local.

## Acceptance criteria

Every check runs from the repository root; resolves the branch point itself with the two-arm expression declared above, spelled byte-identically wherever it appears, and fails closed when neither arm resolves; resolves operating paths through `bin/team-paths.sh` rather than hardcoding them; names every file it reads explicitly; guards every `mktemp` with an immediate non-zero exit; writes nothing outside `$TMPDIR` except where the criterion's own subject is the working tree (**AC18** says so in its body); and pairs every absence assertion with a positive control so an unreadable or misnamed target reads as a failure rather than as a clean absence. Numeric comparisons over epoch values use bash integer arithmetic with a `10#` prefix and **never** `awk` or `sort -n`: a 19-digit epoch-nanosecond value exceeds a double's exact-integer range. **No criterion declares a population figure**: every population and set delta this task states is produced by `bin/derive-populations.sh` and embedded verbatim beside its own `- reproduce:` line. **No criterion counts a `- verdict: ` or `- unit: ` prefix note-wide** — `bin/aggregate-verdicts.sh`'s own real output coins both, and T-1083's engineer hand-off records that collision as a shipped defect; every count over those prefixes here is scoped inside an extracted marker region. `CHECK_ACS_TIMEOUT` must be raised to at least **300** when running this spec's own criteria: **AC17** runs five checkers, one of them over the whole board, and **AC15**/**AC16** regenerate derivation blocks.

**Both verdicts are in scope, stated once here rather than repeated per criterion.** Unlike T-1083, a verdict other than the positive one does **not** stop this task: `not-met` is the approved merge-only fallback and the note still ships. Every criterion below is therefore satisfiable under both values. The evidence-heavy criteria — **AC9** (launch record), **AC10** (aggregation), **AC11** (overlap), **AC12** (liveness) — are each conditioned on **their own chain link's recorded value**, never on the global verdict, so a `not-met` outcome cannot be used to skip evidence for a link that actually completed; their `else` branch requires the `- blocked-at:` line **AC6** couples to the failure. **AC6** and **AC7** carry the verdict coupling itself, in both directions.

- [ ] **AC1** No shipped byte changed, measured as an outcome rather than trusted as an intention. The changed set — the union of the tracked half `git diff --name-only <branch-point>...HEAD` and the untracked half `git ls-files --others --exclude-standard`, which sees strays a commit range cannot — contains **zero** paths beginning `skills/`, `templates/`, `agents/`, `bin/`, `tests/` or `.github/`; and each of `skills/run/SKILL.md`, `templates/prompt-blocks/fanout-orchestration.md`, `templates/prompt-blocks/dispatch-record.md` and `docs/loop-engineering/phase-multiplexing.md` — the three files that constitute the default path plus the note whose rule it applies — is byte-identical to its branch-point blob. Positive controls: the changed set is asserted non-empty before the absence assertion is trusted, and each branch-point blob is asserted non-empty before its comparison, so a failed `git show` cannot read as identity.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base "feature/1084-dispatcher" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085a.XXXXXX") || exit 1; git diff --name-only "$B"...HEAD > "$T/tracked" 2>/dev/null || rc=1; git ls-files --others --exclude-standard > "$T/untracked" 2>/dev/null || rc=1; cat "$T/tracked" "$T/untracked" | grep -v '^$' | sort -u > "$T/changed"; test "$(grep -c . "$T/changed" || true)" -ge 1 || rc=1; test "$(grep -cE '^(skills/|templates/|agents/|bin/|tests/|\.github/)' "$T/changed" || true)" = "0" || rc=1; for f in skills/run/SKILL.md templates/prompt-blocks/fanout-orchestration.md templates/prompt-blocks/dispatch-record.md docs/loop-engineering/phase-multiplexing.md; do git show "$B:$f" > "$T/blob" 2>/dev/null || rc=1; test -s "$T/blob" || rc=1; cmp -s "$T/blob" "$f" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC2** The note exists at exactly the required path, is new rather than a rewrite, and carries its nine required sections. `docs/loop-engineering/default-path-firing.md` is readable and non-empty, does **not** exist in the branch point's tree, carries exactly one `# ` title line, and carries each of these nine headings exactly once as a whole line: `## Terms and closed vocabularies`, `## Firing protocol (frozen before execution)`, `## Firing evidence (raw, orchestrator-produced)`, `## Default-path chain and aggregation analysis`, `## Verdict and licence conditions`, `## Saving and baseline comparison`, `## Implications for the release decision`, `## Supersession and follow-ups`, `## Limits and what is not computable`. The path is asserted literally because the name is load-bearing: four sibling loop-engineering notes continue to exist beside it and a reader must be able to tell them apart. Positive control: `docs/loop-engineering/agent-launch-fanout.md` **is** present in the branch point's tree, so a base-tree read that saw nothing at all cannot satisfy the absence half.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -r "$N" || exit 1; test -s "$N" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base "feature/1084-dispatcher" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; git cat-file -e "$B:docs/loop-engineering/agent-launch-fanout.md" 2>/dev/null || exit 1; if git cat-file -e "$B:$N" 2>/dev/null; then rc=1; fi; test "$(grep -c '^# ' "$N" || true)" = "1" || rc=1; for h in '## Terms and closed vocabularies' '## Firing protocol (frozen before execution)' '## Firing evidence (raw, orchestrator-produced)' '## Default-path chain and aggregation analysis' '## Verdict and licence conditions' '## Saving and baseline comparison' '## Implications for the release decision' '## Supersession and follow-ups' '## Limits and what is not computable'; do test "$(grep -cxF -- "$h" "$N" || true)" = "1" || rc=1; done; test "$rc" -eq 0

- [ ] **AC3** The raw evidence section is byte-identical to what the orchestrator committed at the firing, and that commit sits where the ordering claim requires. The board resolved through `bin/team-paths.sh --get todo` carries **exactly one** line of the form `- probe-evidence-sha (T-1085): <40 lower-case hex>`; that commit resolves; it is an ancestor of `HEAD`; the branch point is an ancestor of it and is not equal to it; the note exists in it, is non-empty, and carries the `## Firing evidence (raw, orchestrator-produced)` heading exactly once; and that section, extracted from the committed blob, is **byte-identical** to the same section extracted from the working tree's note. The assertion is deliberately weaker than T-1083's — it does **not** require the evidence commit to precede the engineer's first invocation, because the shipped path places the firing in Validate, after Implement; the Goal states this and the anti-fitting guarantee is carried by **AC4**'s protocol byte-lock plus this criterion's own evidence byte-lock. The sha is read from the board rather than typed here, so no 40-hex literal enters the frozen text, and the sub-bullet is task-scoped in its own grammar so another task's own such line cannot make this ambiguous. Positive controls: the board is asserted readable, and both extracted sections are asserted non-empty before `cmp`, so an extraction that silently produced nothing cannot pass as identity.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base "feature/1084-dispatcher" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085c.XXXXXX") || exit 1; sed -nE 's/^[[:space:]]*- probe-evidence-sha \(T-1085\): ([0-9a-f]{40})$/\1/p' "$BD" > "$T/sha"; test "$(grep -c . "$T/sha" || true)" = "1" || rc=1; S=$(head -n 1 "$T/sha"); if [ -n "$S" ]; then git rev-parse --verify --quiet "$S^{commit}" >/dev/null || rc=1; git merge-base --is-ancestor "$S" HEAD >/dev/null 2>&1 || rc=1; git merge-base --is-ancestor "$B" "$S" >/dev/null 2>&1 || rc=1; test "$S" != "$B" || rc=1; git show "$S:$N" > "$T/pn" 2>/dev/null || rc=1; test -s "$T/pn" || rc=1; test "$(grep -cxF -- '## Firing evidence (raw, orchestrator-produced)' "$T/pn" || true)" = "1" || rc=1; awk '/^## Firing evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$T/pn" > "$T/psec"; awk '/^## Firing evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/wsec"; test -s "$T/psec" || rc=1; test -s "$T/wsec" || rc=1; cmp -s "$T/psec" "$T/wsec" || rc=1; else rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: the board resolved through `bin/team-paths.sh --get todo` is rotated or archived, or this task's entry is rewritten such that its `- probe-evidence-sha (T-1085):` sub-bullet no longer sits in the active board — at which point the commit this criterion compares against can no longer be found and the criterion must be re-anchored rather than assumed broken.

- [ ] **AC4** Freeze-then-fire ordering is mechanically evidenced, and the protocol was not fitted to the result. This spec exists in the evidence commit; `bin/check-intent.sh --print-hash` run against that committed blob exits `0` and prints a 40-hex value, which is what proves the intent block was already frozen and structurally sound when the firing ran; and the region delimited by `<!-- BEGIN firing-protocol: T-1085 -->` and `<!-- END firing-protocol: T-1085 -->` inside this spec's Goal is **byte-identical** between that commit and `HEAD`. The narrower region rather than the whole intent hash is deliberate: a class-M mechanics repair to a `- check:` line legitimately moves the intent hash without touching what the firing was told to do, whereas a change to the protocol region is exactly the post-hoc fitting this criterion forbids — and if such a change is ever ratified this criterion turns red **by design**, with a re-firing under the invalid-evidence rule as the disposition. Positive controls: the extracted region is asserted to hold at least fourteen non-blank lines and to contain the literal `- default-reachability:` at both refs before the comparison, so an empty or truncated extraction cannot pass as identity; the HEAD-side hash is asserted to match a 40-hex shape.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1085-default-path-firing.md; test -s "$SP" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085d.XXXXXX") || exit 1; sed -nE 's/^[[:space:]]*- probe-evidence-sha \(T-1085\): ([0-9a-f]{40})$/\1/p' "$BD" > "$T/sha"; test "$(grep -c . "$T/sha" || true)" = "1" || rc=1; S=$(head -n 1 "$T/sha"); if [ -n "$S" ]; then git show "$S:$SP" > "$T/spec.md" 2>/dev/null || rc=1; test -s "$T/spec.md" || rc=1; if [ -s "$T/spec.md" ]; then H1=$(bash bin/check-intent.sh --print-hash "$T/spec.md" 2>/dev/null); test -n "$H1" || rc=1; printf '%s' "$H1" | grep -qE '^[0-9a-f]{40}$' || rc=1; awk '/^<!-- BEGIN firing-protocol: T-1085 -->$/{f=1} f{print} f&&/^<!-- END firing-protocol: T-1085 -->$/{exit}' "$T/spec.md" > "$T/pp"; awk '/^<!-- BEGIN firing-protocol: T-1085 -->$/{f=1} f{print} f&&/^<!-- END firing-protocol: T-1085 -->$/{exit}' "$SP" > "$T/hp"; test "$(grep -c . "$T/pp" || true)" -ge 14 || rc=1; test "$(grep -c . "$T/hp" || true)" -ge 14 || rc=1; grep -qF -- '- default-reachability:' "$T/pp" || rc=1; grep -qF -- '- default-reachability:' "$T/hp" || rc=1; cmp -s "$T/pp" "$T/hp" || rc=1; fi; H2=$(bash bin/check-intent.sh --print-hash "$SP" 2>/dev/null); test -n "$H2" || rc=1; printf '%s' "$H2" | grep -qE '^[0-9a-f]{40}$' || rc=1; else rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** The release-citable line exists exactly once, in the section that owns it, and both release consequences were written down. The note carries **exactly one** line matching `- default-reachability: met|not-met — <text>` in the whole file, and exactly one such line inside the extracted `## Verdict and licence conditions` section — so the release gate reads one sentence and never has to choose between two. The note additionally carries exactly one `- release-consequence: met — <text>` line and exactly one `- release-consequence: not-met — <text>` line, and the `not-met` line contains the literal `merge-only`; the two are written before the result is known so the fallback is a recorded plan rather than an after-the-fact rationalization. The planned version string is deliberately **not** pinned by this criterion: it is a relayed premise the freeze run measures against the sprint plan, and pinning a literal here would freeze a value this role cannot read. Positive controls: the extracted section is asserted non-empty before its own count is trusted, and the whole-file count is asserted before the section count.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085e.XXXXXX") || exit 1; test "$(grep -cE '^- default-reachability: (met|not-met) — .' "$N" || true)" = "1" || rc=1; test "$(grep -c '^- default-reachability: ' "$N" || true)" = "1" || rc=1; awk '/^## Verdict and licence conditions$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/v"; test -s "$T/v" || rc=1; test "$(grep -cE '^- default-reachability: (met|not-met) — .' "$T/v" || true)" = "1" || rc=1; test "$(grep -cE '^- release-consequence: met — .' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- release-consequence: not-met — .' "$N" || true)" = "1" || rc=1; grep -E '^- release-consequence: not-met — .' "$N" > "$T/rc" || true; test -s "$T/rc" || rc=1; grep -qF -- 'merge-only' "$T/rc" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** The default-path chain is recorded link by link under a closed grammar, and its coupling to the verdict holds in both directions. Each of the seven chain-link ids `precedence-rule-applied`, `dispatch-record-transcribed`, `step5-pointer-read`, `population-fixed`, `instances-launched`, `aggregation-reduced`, `attribution-verified` occurs on exactly one `- chain-link: <id> — evidenced|not-evidenced — <text>` line; the total `- chain-link: ` count equals the grammar-matching total, which is exactly seven, so an eighth link added later must conform rather than ride in as prose. Coupling, both directions: **if** any link reads `not-evidenced`, the recorded `- default-reachability:` is `not-met` and **exactly one** `- blocked-at: <id> — <text>` line exists whose id is drawn from those same seven and from no other; **if** every link reads `evidenced`, **no** `- blocked-at:` line exists. Positive control: the grammar total is asserted to be exactly seven, and the `- default-reachability:` line is asserted to exist exactly once, before either branch of the coupling is judged.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; for c in precedence-rule-applied dispatch-record-transcribed step5-pointer-read population-fixed instances-launched aggregation-reduced attribution-verified; do test "$(grep -cE "^- chain-link: $c — (evidenced|not-evidenced) — ." "$N" || true)" = "1" || rc=1; done; CT=$(grep -c '^- chain-link: ' "$N" || true); CG=$(grep -cE '^- chain-link: [a-z][a-z0-9-]* — (evidenced|not-evidenced) — .' "$N" || true); test "$((10#$CT))" = "$((10#$CG))" || rc=1; test "$((10#$CG))" = "7" || rc=1; test "$(grep -cE '^- default-reachability: (met|not-met) — .' "$N" || true)" = "1" || rc=1; NE=$(grep -cE '^- chain-link: [a-z][a-z0-9-]* — not-evidenced — .' "$N" || true); BA=$(grep -cE '^- blocked-at: [a-z][a-z0-9-]* — .' "$N" || true); if [ "$((10#$NE))" -gt 0 ]; then test "$(grep -cE '^- default-reachability: not-met — .' "$N" || true)" = "1" || rc=1; test "$((10#$BA))" = "1" || rc=1; BI=$(sed -nE 's/^- blocked-at: ([a-z][a-z0-9-]*) — .*/\1/p' "$N" | head -n 1); case "$BI" in precedence-rule-applied|dispatch-record-transcribed|step5-pointer-read|population-fixed|instances-launched|aggregation-reduced|attribution-verified) : ;; *) rc=1 ;; esac; else test "$((10#$BA))" = "0" || rc=1; fi; test "$rc" -eq 0

- [ ] **AC7** Six licence conditions close under their own grammar, and the verdict follows from them and from the chain together. Each of `shipped-text-unmodified`, `no-out-of-band-flag`, `real-plugin-role-instances`, `genuine-overlap`, `real-population`, `production-unit` occurs on exactly one `- licence-condition: <id> — met|not-met — <text>` line; the total `- licence-condition: ` count equals the grammar-matching total, which is exactly six. Coupling, both directions: **if** any condition reads `not-met` **or** any chain link reads `not-evidenced`, the recorded `- default-reachability:` is `not-met` and exactly one `- not-met-ground: chain|licence — <text>` line exists; **if** every condition reads `met` **and** every chain link reads `evidenced`, the recorded verdict is `met` and **no** `- not-met-ground:` line exists. This is where the whole reading in the Goal is carried: `no-out-of-band-flag` is the condition that turns a mere successful fan-out into a *default-path* firing, and its `not-met` value is the one that makes even a working firing report `not-met`. Positive control: the grammar total is asserted to be exactly six before the coupling is judged, so a truncated condition set cannot satisfy it vacuously.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; for c in shipped-text-unmodified no-out-of-band-flag real-plugin-role-instances genuine-overlap real-population production-unit; do test "$(grep -cE "^- licence-condition: $c — (met|not-met) — ." "$N" || true)" = "1" || rc=1; done; LT=$(grep -c '^- licence-condition: ' "$N" || true); LG=$(grep -cE '^- licence-condition: [a-z][a-z0-9-]* — (met|not-met) — .' "$N" || true); test "$((10#$LT))" = "$((10#$LG))" || rc=1; test "$((10#$LG))" = "6" || rc=1; NM=$(grep -cE '^- licence-condition: [a-z][a-z0-9-]* — not-met — .' "$N" || true); NE=$(grep -cE '^- chain-link: [a-z][a-z0-9-]* — not-evidenced — .' "$N" || true); NG=$(grep -cE '^- not-met-ground: (chain|licence) — .' "$N" || true); NGT=$(grep -c '^- not-met-ground: ' "$N" || true); test "$((10#$NG))" = "$((10#$NGT))" || rc=1; if [ "$((10#$NM))" -gt 0 ] || [ "$((10#$NE))" -gt 0 ]; then test "$(grep -cE '^- default-reachability: not-met — .' "$N" || true)" = "1" || rc=1; test "$((10#$NG))" = "1" || rc=1; else test "$(grep -cE '^- default-reachability: met — .' "$N" || true)" = "1" || rc=1; test "$((10#$NG))" = "0" || rc=1; fi; test "$rc" -eq 0

- [ ] **AC8** The board's own dispatch record is the real, shipped-grammar record the chain's second link claims, and its ground cites a price the note actually records. The board resolved through `bin/team-paths.sh --get todo` carries this task's own top-level entry; inside that entry's extent — its own `- [ ] **T-1085** ` line plus the run of indented continuation lines immediately following it, bounded exactly the way `bin/check-handoff.sh`'s own `CONTINUATION_RE` (`^[[:space:]]+[^[:space:]]`) bounds one, so the extent stops at the first blank or unindented line and can never run on into a later section — there is exactly one `- dispatch: verify — tier1-fanout — conditional — <ground>` sub-bullet, exactly one `- dispatch: implement — serial|tier2|tier3 — unconditional|conditional — <ground>` sub-bullet, and exactly two `- dispatch: ` sub-bullets in total, so both axes the note defines are recorded and neither appears twice. The `verify` record's ground opens with one of the four priced-line prefixes the shipped grammar fixes (`saving: `, `recommendation: `, `break-even: `, `cost-input: `), and the priced id it names resolves as a real `- <prefix>: <id>` line in `docs/loop-engineering/phase-multiplexing.md`, so the routing choice cites a recorded price rather than an invented one. Positive controls: the entry extraction is asserted non-empty, asserted to contain exactly one `- [` line (its own header, proving the extent did not run past the entry into a later task's), and asserted to hold at least two lines (the header plus at least one continuation, so an extent that captured only the header cannot satisfy the sub-bullet counts vacuously); and `docs/loop-engineering/phase-multiplexing.md` is asserted non-empty before the priced ground is resolved against it.
  - check: rc=0; export LC_ALL=C; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; PM=docs/loop-engineering/phase-multiplexing.md; test -s "$PM" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085h.XXXXXX") || exit 1; awk '/^- \[[ xX]\] \*\*T-1085\*\* /{f=1;print;next} f&&/^[[:space:]]+[^[:space:]]/{print;next} f{exit}' "$BD" > "$T/e"; test -s "$T/e" || rc=1; test "$(grep -c '^- \[' "$T/e" || true)" = "1" || rc=1; test "$(grep -c . "$T/e" || true)" -ge 2 || rc=1; test "$(grep -cE '^[[:space:]]+- dispatch: verify — tier1-fanout — conditional — .' "$T/e" || true)" = "1" || rc=1; test "$(grep -cE '^[[:space:]]+- dispatch: implement — (serial|tier2|tier3) — (unconditional|conditional) — .' "$T/e" || true)" = "1" || rc=1; test "$(grep -cE '^[[:space:]]+- dispatch: ' "$T/e" || true)" = "2" || rc=1; G=$(sed -nE 's/^[[:space:]]+- dispatch: verify — tier1-fanout — conditional — (.*)$/\1/p' "$T/e" | head -n 1); test -n "$G" || rc=1; if [ -n "$G" ]; then printf '%s\n' "$G" | grep -qE '^(saving|recommendation|break-even|cost-input): [a-z0-9-]+' || rc=1; P=$(printf '%s\n' "$G" | sed -nE 's/^(saving|recommendation|break-even|cost-input): ([a-z0-9-]+).*/\1: \2/p' | head -n 1); test -n "$P" || rc=1; if [ -n "$P" ]; then grep -qF -- "- $P" "$PM" || rc=1; fi; fi; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: `docs/loop-engineering/phase-multiplexing.md` gains a third `- dispatch-axis:` line — the depth axis issue #274's own successor work would add — at which point the exactly-two total this criterion asserts no longer describes the axis set the shipped grammar requires recorded, and the count must be re-derived from the note's own axis lines rather than restated.

- [ ] **AC9** The launch record the firing actually wrote is quoted verbatim, conforms to the shipped grammar, assigns every unit exactly once, and its assignment set equals the aggregation's own part set. Conditioned on this criterion's own chain link: **when** `- chain-link: population-fixed — evidenced` is recorded, the note carries exactly one `<!-- BEGIN launch-record: t1085fan -->` / `<!-- END launch-record: t1085fan -->` marker pair; inside that region there is exactly one `launch-record 1` version line, exactly one `launch-record-end` terminator, exactly one each of `- population: `, `- requested-n: `, `- achieved-n: `, `- cap-ground: `, `- launched-epoch: ` and `- completed-epoch: `; the `- cap-ground:` value matches `TEAM_FANOUT_MAX=` or `measured-cores=` and nothing else; `- requested-n:` parses as a digit string of at least 2; there are at least two `- assign: <instance-id> — <unit-id>` lines and the count of **distinct unit ids** across them equals the count of `- assign:` lines, so no unit is assigned twice; and there are at least two `- liveness: <instance-id> — harness-tracked-background — <command>` lines. Additionally, **when** `- chain-link: aggregation-reduced — evidenced` is also recorded, the set of distinct instance ids on the region's `- assign:` lines is **equal in both directions** to the set of `- part:` names inside the note's `<!-- BEGIN fanout-verdict: t1085fan -->` block — the identity that makes the shipped no-new-checker closure real rather than asserted. **Otherwise** (the link not evidenced) the note carries exactly one `- blocked-at:` line, so the absence is a recorded outcome rather than a gap. Positive controls: the region and the block are each asserted non-empty before extraction, and both derived id sets are asserted non-empty before they are compared, so two empty sets cannot pass as equality.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085i.XXXXXX") || exit 1; if grep -qE '^- chain-link: population-fixed — evidenced — .' "$N"; then test "$(grep -cxF -- '<!-- BEGIN launch-record: t1085fan -->' "$N" || true)" = "1" || rc=1; test "$(grep -cxF -- '<!-- END launch-record: t1085fan -->' "$N" || true)" = "1" || rc=1; awk '/^<!-- BEGIN launch-record: t1085fan -->$/{f=1;next} /^<!-- END launch-record: t1085fan -->$/{f=0} f' "$N" > "$T/lr"; test -s "$T/lr" || rc=1; test "$(grep -cxF -- 'launch-record 1' "$T/lr" || true)" = "1" || rc=1; test "$(grep -cxF -- 'launch-record-end' "$T/lr" || true)" = "1" || rc=1; for k in '- population: ' '- requested-n: ' '- achieved-n: ' '- cap-ground: ' '- launched-epoch: ' '- completed-epoch: '; do test "$(grep -cF -- "$k" "$T/lr" || true)" = "1" || rc=1; done; test "$(grep -cE '^- cap-ground: (TEAM_FANOUT_MAX|measured-cores)=.' "$T/lr" || true)" = "1" || rc=1; RN=$(sed -nE 's/^- requested-n: ([0-9]+).*$/\1/p' "$T/lr" | head -n 1); test -n "$RN" || rc=1; if [ -n "$RN" ]; then test "$((10#$RN))" -ge 2 || rc=1; fi; AT=$(grep -cE '^- assign: [a-z][a-z0-9-]* — .' "$T/lr" || true); test "$((10#$AT))" -ge 2 || rc=1; sed -nE 's/^- assign: [a-z][a-z0-9-]* — (.*)$/\1/p' "$T/lr" | sort -u > "$T/au"; AU=$(grep -c . "$T/au" || true); test "$((10#$AU))" = "$((10#$AT))" || rc=1; test "$(grep -cE '^- liveness: [a-z][a-z0-9-]* — harness-tracked-background — .' "$T/lr" || true)" -ge 2 || rc=1; sed -nE 's/^- assign: ([a-z][a-z0-9-]*) — .*$/\1/p' "$T/lr" | sort -u > "$T/ai"; test -s "$T/ai" || rc=1; if grep -qE '^- chain-link: aggregation-reduced — evidenced — .' "$N"; then awk '/^<!-- BEGIN fanout-verdict: t1085fan -->$/{f=1;next} /^<!-- END fanout-verdict: t1085fan -->$/{f=0} f' "$N" > "$T/fv"; test -s "$T/fv" || rc=1; sed -nE 's/^- part: ([a-z][a-z0-9-]*) — .*$/\1/p' "$T/fv" | sort -u > "$T/pi"; test -s "$T/pi" || rc=1; cmp -s "$T/ai" "$T/pi" || rc=1; fi; else test "$(grep -cE '^- blocked-at: [a-z][a-z0-9-]* — .' "$N" || true)" = "1" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC10** The reduction and the attribution check ran green against the real shipped tools, and their results are quoted rather than described. Every assertion about a `- checker-result: ` line, about the aggregation block and about the span rows is scoped to the extracted `## Firing evidence (raw, orchestrator-produced)` section rather than to the whole note — deliberately, so a grammar restatement elsewhere in the note cannot be mistaken for a real result and cannot break the closure below; the chain-link and `- blocked-at:` reads stay note-wide, because those lines live in the analysis section by design. Conditioned on the chain links: **when** `- chain-link: aggregation-reduced — evidenced` is recorded, the evidence section carries exactly one `- checker-result: aggregate-verdicts — exit=0 — <text>` line; that section's total `- checker-result: ` count equals the count matching the grammar `- checker-result: <tool> — exit=<digits> — <text>`, so a further result added later must conform rather than ride in as prose; it carries exactly one `<!-- BEGIN fanout-verdict: t1085fan -->` line and exactly one matching `<!-- END fanout-verdict: t1085fan -->` line, quoting the block the tool actually printed; that block carries the tool's own `- aggregated-by: bin/aggregate-verdicts.sh` line and at least one `- summary: ` line, so a hand-typed imitation fails; and at least two lines carry the literal `"instance":"`, the real span rows the attribution checker read. **When** `- chain-link: attribution-verified — evidenced` is recorded, the section additionally carries exactly one `- checker-result: check-fanout-instances — exit=0 — <text>` line whose text contains both `check-fanout-instances: ok: ` and the live label `t1085fan`. **Otherwise** for either link, exactly one `- blocked-at:` line exists in the note. Positive controls: the evidence section is asserted non-empty before anything inside it is counted; the checker-result grammar total is asserted to be at least one in the evidenced branch; and the extracted block is asserted non-empty before its own lines are judged.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085j.XXXXXX") || exit 1; awk '/^## Firing evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/ev"; test -s "$T/ev" || rc=1; CT=$(grep -c '^- checker-result: ' "$T/ev" || true); CG=$(grep -cE '^- checker-result: [a-z][a-z0-9-]* — exit=[0-9]+ — .' "$T/ev" || true); test "$((10#$CT))" = "$((10#$CG))" || rc=1; if grep -qE '^- chain-link: aggregation-reduced — evidenced — .' "$N"; then test "$((10#$CG))" -ge 1 || rc=1; test "$(grep -cE '^- checker-result: aggregate-verdicts — exit=0 — .' "$T/ev" || true)" = "1" || rc=1; test "$(grep -cxF -- '<!-- BEGIN fanout-verdict: t1085fan -->' "$T/ev" || true)" = "1" || rc=1; test "$(grep -cxF -- '<!-- END fanout-verdict: t1085fan -->' "$T/ev" || true)" = "1" || rc=1; awk '/^<!-- BEGIN fanout-verdict: t1085fan -->$/{f=1;next} /^<!-- END fanout-verdict: t1085fan -->$/{f=0} f' "$T/ev" > "$T/fv"; test -s "$T/fv" || rc=1; grep -q '^- aggregated-by: bin/aggregate-verdicts\.sh$' "$T/fv" || rc=1; test "$(grep -c '^- summary: ' "$T/fv" || true)" -ge 1 || rc=1; test "$(grep -cF -- '"instance":"' "$T/ev" || true)" -ge 2 || rc=1; else test "$(grep -cE '^- blocked-at: [a-z][a-z0-9-]* — .' "$N" || true)" = "1" || rc=1; fi; if grep -qE '^- chain-link: attribution-verified — evidenced — .' "$N"; then test "$(grep -cE '^- checker-result: check-fanout-instances — exit=0 — .*check-fanout-instances: ok: .*t1085fan' "$T/ev" || true)" = "1" || rc=1; else test "$(grep -cE '^- blocked-at: [a-z][a-z0-9-]* — .' "$N" || true)" = "1" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** Real instances ran concurrently and the parent read the liveness signal while they were still running, proved by re-derivation rather than by assertion. Conditioned on the chain link: **when** `- chain-link: instances-launched — evidenced` is recorded, at least two lines match `- agent-timestamp: t1085fan — <instance-id> — first=<digits> — last=<digits> — <text>`, every one of them has `last` strictly greater than `first`, the intersection window `[max(first), min(last)]` re-derived from those same lines is **non-empty** (its lower bound strictly below its upper bound, which is the genuine-overlap fact and not a restatement of it), and at least two lines match `- heartbeat: t1085fan — <digits> — <text>` with every heartbeat value strictly inside that window. The window is derived from the note's own recorded values rather than compared against a declared bound, so a heartbeat typed in after the fact, outside the window the timestamps imply, fails here rather than passing review. All comparisons use bash `10#` integer arithmetic; `awk` and `sort -n` are avoided deliberately, since a 19-digit epoch-nanosecond value exceeds a double's exact-integer range. **Otherwise** exactly one `- blocked-at:` line exists. Positive controls: both extractions are asserted non-empty and both derived bounds are asserted greater than zero before any heartbeat is judged.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085k.XXXXXX") || exit 1; if grep -qE '^- chain-link: instances-launched — evidenced — .' "$N"; then grep -E '^- agent-timestamp: t1085fan — [a-z][a-z0-9-]* — first=[0-9]+ — last=[0-9]+ — .' "$N" > "$T/ts" || true; test "$(grep -c . "$T/ts" || true)" -ge 2 || rc=1; lo=0; hi=0; while IFS= read -r l; do [ -n "$l" ] || continue; fv=$(printf '%s\n' "$l" | sed -nE 's/^.* first=([0-9]+) .*$/\1/p'); lv=$(printf '%s\n' "$l" | sed -nE 's/^.* last=([0-9]+) .*$/\1/p'); if [ -z "$fv" ] || [ -z "$lv" ]; then rc=1; continue; fi; test "$((10#$lv))" -gt "$((10#$fv))" || rc=1; if [ "$((10#$fv))" -gt "$((10#$lo))" ]; then lo="$fv"; fi; if [ "$((10#$hi))" -eq 0 ] || [ "$((10#$lv))" -lt "$((10#$hi))" ]; then hi="$lv"; fi; done < "$T/ts"; test "$((10#$lo))" -gt 0 || rc=1; test "$((10#$hi))" -gt 0 || rc=1; test "$((10#$lo))" -lt "$((10#$hi))" || rc=1; grep -E '^- heartbeat: t1085fan — [0-9]+ — .' "$N" > "$T/hb" || true; test "$(grep -c . "$T/hb" || true)" -ge 2 || rc=1; while IFS= read -r l; do [ -n "$l" ] || continue; hv=$(printf '%s\n' "$l" | sed -nE 's/^- heartbeat: t1085fan — ([0-9]+) — .*$/\1/p'); if [ -z "$hv" ]; then rc=1; continue; fi; test "$((10#$hv))" -gt "$((10#$lo))" || rc=1; test "$((10#$hv))" -lt "$((10#$hi))" || rc=1; done < "$T/hb"; else test "$(grep -cE '^- blocked-at: [a-z][a-z0-9-]* — .' "$N" || true)" = "1" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** Every liveness check was launched on the one surviving path, and none on the path measured to die. The `- liveness: ` assertions are scoped to the extracted `## Firing evidence (raw, orchestrator-produced)` section for the same reason **AC10**'s are: a grammar restatement in the note's own protocol or terms section would open a line with the identical prefix and would break a note-wide closure without any real record having gone wrong. Conditioned on the chain link: **when** `- chain-link: instances-launched — evidenced` is recorded, that section's total `- liveness: ` count equals the count matching `- liveness: <instance-id> — harness-tracked-background — <command>`, so a line carrying any other mechanism token fails rather than being read as a weaker variant; at least two such lines exist, because a liveness check runs alongside *each* instance and there are at least two of them; **no** `- liveness: ` line in that section ends in a bare shell `&`; and the note as a whole states the ground by carrying the literal `bare` together with `harness-agent-concurrency.md`, so the requirement is attributed to the measurement that produced it rather than asserted as taste. **Otherwise** exactly one `- blocked-at:` line exists in the note. Positive controls: the evidence section is asserted non-empty before it is counted, and the total is asserted to be at least two before the equality is trusted, so zero liveness lines cannot satisfy the grammar closure vacuously.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085l.XXXXXX") || exit 1; awk '/^## Firing evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/ev"; test -s "$T/ev" || rc=1; if grep -qE '^- chain-link: instances-launched — evidenced — .' "$N"; then LT=$(grep -c '^- liveness: ' "$T/ev" || true); LG=$(grep -cE '^- liveness: [a-z][a-z0-9-]* — harness-tracked-background — .' "$T/ev" || true); test "$((10#$LT))" -ge 2 || rc=1; test "$((10#$LT))" = "$((10#$LG))" || rc=1; test "$(grep -cE '^- liveness: .*&[[:space:]]*$' "$T/ev" || true)" = "0" || rc=1; grep -qF -- 'harness-agent-concurrency.md' "$N" || rc=1; grep -qF -- 'bare' "$N" || rc=1; else test "$(grep -cE '^- blocked-at: [a-z][a-z0-9-]* — .' "$N" || true)" = "1" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC13** The same-population serial baseline is recorded with its conditions, the aggregation-safety comparison is made and diagnosed, and the saving's stated direction is re-derivable from the note's own integers. The note carries exactly one `- baseline: serial — <text>` line, exactly one `- execution-order: <text>` line, exactly one `- cache-warmth: <text>` line, exactly one `- unit-timeout: <text>` line naming the `CHECK_ACS_TIMEOUT` value exported in **both** arms, exactly one `- saving: default-path-tier1-fanout — measured|undetermined — <text>` line, and exactly one `- verdict-region-parity: identical|differs — <text>` line; when the parity line reads `differs`, at least one `- parity-exception: <key> — <cause> — <text>` line exists, so a difference is enumerated and diagnosed rather than glossed. When the saving line reads `measured`, the note carries exactly one `- arm-wall-clock: fanned — start=<digits> — end=<digits>` line and exactly one `- arm-wall-clock: serial — start=<digits> — end=<digits>` line, each with `end` strictly greater than `start`, and exactly one `- saving-direction: faster|slower|equal — <text>` line whose token agrees with the sign of (serial duration − fanned duration) re-derived here with bash `10#` arithmetic — so the narrative conclusion is checked against the note's own numbers rather than accepted as prose. The bare-prefix `- saving: ` closure is scoped to the extracted `## Saving and baseline comparison` section rather than to the whole note, because the note legitimately quotes `docs/loop-engineering/phase-multiplexing.md`'s own priced lines when it states the claim ceiling, and a note-wide closure over that prefix would punish the attribution the ceiling requires; every other count here is over a prefix this task coins, where no such collision exists. Positive controls: each extraction is asserted non-empty before it is parsed, and both durations are asserted greater than zero before they are compared.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085m.XXXXXX") || exit 1; awk '/^## Saving and baseline comparison$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/sb"; test -s "$T/sb" || rc=1; for k in '^- baseline: serial — .' '^- execution-order: .' '^- cache-warmth: .' '^- unit-timeout: .' '^- verdict-region-parity: (identical|differs) — .' '^- saving: default-path-tier1-fanout — (measured|undetermined) — .'; do test "$(grep -cE "$k" "$N" || true)" = "1" || rc=1; done; test "$(grep -c '^- verdict-region-parity: ' "$N" || true)" = "1" || rc=1; test "$(grep -c '^- saving: ' "$T/sb" || true)" = "1" || rc=1; rm -rf "$T"; if grep -qE '^- verdict-region-parity: differs — .' "$N"; then test "$(grep -cE '^- parity-exception: [^ ]+ — [^ ]+ — .' "$N" || true)" -ge 1 || rc=1; fi; if grep -qE '^- saving: default-path-tier1-fanout — measured — .' "$N"; then test "$(grep -cE '^- arm-wall-clock: fanned — start=[0-9]+ — end=[0-9]+$' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- arm-wall-clock: serial — start=[0-9]+ — end=[0-9]+$' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- saving-direction: (faster|slower|equal) — .' "$N" || true)" = "1" || rc=1; fs=$(sed -nE 's/^- arm-wall-clock: fanned — start=([0-9]+) — end=[0-9]+$/\1/p' "$N" | head -n 1); fe=$(sed -nE 's/^- arm-wall-clock: fanned — start=[0-9]+ — end=([0-9]+)$/\1/p' "$N" | head -n 1); ss=$(sed -nE 's/^- arm-wall-clock: serial — start=([0-9]+) — end=[0-9]+$/\1/p' "$N" | head -n 1); se=$(sed -nE 's/^- arm-wall-clock: serial — start=[0-9]+ — end=([0-9]+)$/\1/p' "$N" | head -n 1); D=$(sed -nE 's/^- saving-direction: (faster|slower|equal) — .*$/\1/p' "$N" | head -n 1); if [ -n "$fs" ] && [ -n "$fe" ] && [ -n "$ss" ] && [ -n "$se" ] && [ -n "$D" ]; then test "$((10#$fe))" -gt "$((10#$fs))" || rc=1; test "$((10#$se))" -gt "$((10#$ss))" || rc=1; fd=$(( 10#$fe - 10#$fs )); sd=$(( 10#$se - 10#$ss )); test "$fd" -gt 0 || rc=1; test "$sd" -gt 0 || rc=1; if [ "$sd" -gt "$fd" ]; then E=faster; elif [ "$sd" -lt "$fd" ]; then E=slower; else E=equal; fi; test "$D" = "$E" || rc=1; else rc=1; fi; fi; test "$rc" -eq 0

- [ ] **AC14** The claim ceiling is honoured and the declared-label boundary is not upgraded. Every line in the note containing the literal `43.84` also contains the literal `phase-multiplexing.md` — the pilot's figure is attributed **at the point of mention**, never restated as this task's own, and the equality of those two counts is what makes an unattributed restatement fail rather than pass review. The note carries exactly one `- claim-ceiling: <text>` line, which contains both `43.84` and `phase-multiplexing.md`; exactly one `- agent-type: <text>` line, which contains the literal `declared label` and names `agent-launch-fanout.md`; and exactly one `- knee: undetermined — <text>` line, since one degree is run and the plateau point is therefore not measured here whatever the population. Positive controls: the `43.84` count is asserted to be at least one before the equality is trusted, so a note that never mentions the figure cannot satisfy the attribution rule vacuously; and each of the three lines is asserted to be exactly one.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; A=$(grep -cF -- '43.84' "$N" || true); test "$((10#$A))" -ge 1 || rc=1; Z=$(grep -F -- '43.84' "$N" | grep -cF -- 'phase-multiplexing.md' || true); test "$((10#$A))" = "$((10#$Z))" || rc=1; test "$(grep -cE '^- claim-ceiling: .' "$N" || true)" = "1" || rc=1; grep -E '^- claim-ceiling: .' "$N" | grep -qF -- '43.84' || rc=1; grep -E '^- claim-ceiling: .' "$N" | grep -qF -- 'phase-multiplexing.md' || rc=1; test "$(grep -cE '^- agent-type: .' "$N" || true)" = "1" || rc=1; grep -E '^- agent-type: .' "$N" | grep -qF -- 'declared label' || rc=1; grep -E '^- agent-type: .' "$N" | grep -qF -- 'agent-launch-fanout.md' || rc=1; test "$(grep -cE '^- knee: undetermined — .' "$N" || true)" = "1" || rc=1; test "$(grep -c '^- knee: ' "$N" || true)" = "1" || rc=1; test "$rc" -eq 0

- [ ] **AC15** Every set claim the note makes is a tool's output that reproduces, not a hand count. Inside `docs/loop-engineering/default-path-firing.md`, the counts of `<!-- BEGIN derivation: <label> -->` lines, `<!-- END derivation: <label> -->` lines and `- reproduce: <command>` lines are equal; every `- reproduce:` command, run from the repository root, exits 0, produces non-empty output, and is **byte-identical** to the block in the note carrying the label its own `BEGIN` line names — which is what makes the block re-derivable by a reader months later rather than a paste nobody can check. Every such command must read only the note itself and this repository's committed objects, never an ephemeral scratch path, since the runs directory is gitignored (`.shell-team/.gitignore` line 4) and no `$TMPDIR` artifact survives the session. Additionally, **when** `- chain-link: aggregation-reduced — evidenced` is recorded, at least two blocks exist and their labels include both `t1085-assign-vs-part` and `t1085-verdict-region-parity` — the two set claims the aggregation licenses. Positive controls: the number of reproduce commands actually executed is asserted at least one, and each regenerated output and each extracted block is asserted non-empty before comparison.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/default-path-firing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085o.XXXXXX") || exit 1; nb=$(grep -c '^<!-- BEGIN derivation: ' "$N" || true); ne=$(grep -c '^<!-- END derivation: ' "$N" || true); nr=$(grep -c '^- reproduce: ' "$N" || true); test "$((10#$nb))" = "$((10#$ne))" || rc=1; test "$((10#$nr))" = "$((10#$nb))" || rc=1; test "$((10#$nb))" -ge 1 || rc=1; sed -n 's/^- reproduce: //p' "$N" > "$T/rep"; i=0; while IFS= read -r cmd; do test -n "$cmd" || continue; i=$((i+1)); eval "$cmd" > "$T/o$i" 2>/dev/null || rc=1; test -s "$T/o$i" || { rc=1; continue; }; lab=$(head -n 1 "$T/o$i" | sed -nE 's/^<!-- BEGIN derivation: (.*) -->$/\1/p'); test -n "$lab" || { rc=1; continue; }; awk -v b="<!-- BEGIN derivation: $lab -->" -v e="<!-- END derivation: $lab -->" '$0==b{f=1} f{print} f&&$0==e{exit}' "$N" > "$T/w$i"; test -s "$T/w$i" || { rc=1; continue; }; cmp -s "$T/o$i" "$T/w$i" || rc=1; done < "$T/rep"; test "$i" -ge 1 || rc=1; if grep -qE '^- chain-link: aggregation-reduced — evidenced — .' "$N"; then test "$((10#$nb))" -ge 2 || rc=1; test "$(grep -cxF -- '<!-- BEGIN derivation: t1085-assign-vs-part -->' "$N" || true)" = "1" || rc=1; test "$(grep -cxF -- '<!-- BEGIN derivation: t1085-verdict-region-parity -->' "$N" || true)" = "1" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC16** The downstream-impact disclosure is a **full-population** diff, as this task's `mechanism` class owes with nothing priced down, and its population is a tool's output rather than a claim. This spec carries a `## Blast radius` section outside the intent block; that heading occurs exactly once and the section is non-empty; it states the literal `full-population` as its method and names its base-side reading target as a committed-blob read (`git show`, `git ls-tree`, `git clone` or `git worktree add --detach`) rather than the working tree, so the base side is that ref's bytes and not whatever is on disk at read time; it carries both a `base:` and a `head:` verdict token and names at least one merged spec by full path; it declares `- predicted-red: <n>`; it discharges the indirection class explicitly by carrying the literal `indirection`, naming `bin/team-paths.sh --get todo` as a run-time path construction a literal-bytes derivation cannot see, and stating the disposition taken for it; it names the criterion class this task's own `+1` spec file shifts by carrying the literal `.shell-team/specs/*.md`; and it carries at least one `<!-- BEGIN derivation: <label> -->` block with matching `END` and `- reproduce:` counts, every command exiting 0 with non-empty output byte-identical to the block its own `BEGIN` line labels. Positive controls: the section extraction is asserted non-empty before it is judged; at least one reproduce command is asserted to have actually run; and each regenerated output and extracted block is asserted non-empty before comparison.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1085-default-path-firing.md; test -s "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085p.XXXXXX") || exit 1; test "$(grep -cxF -- '## Blast radius' "$SP" || true)" = "1" || rc=1; awk '/^## Blast radius$/{f=1;next} f&&/^## /{exit} f' "$SP" > "$T/br"; test -s "$T/br" || rc=1; for s in 'full-population' 'base:' 'head:' 'indirection' 'bin/team-paths.sh --get todo' '.shell-team/specs/*.md'; do grep -qF -- "$s" "$T/br" || rc=1; done; grep -qE 'git (show|ls-tree|clone|worktree add --detach)' "$T/br" || rc=1; grep -qE '\.shell-team/specs/T-10[0-9][0-9]' "$T/br" || rc=1; test "$(grep -c '^- predicted-red: ' "$T/br" || true)" -ge 1 || rc=1; grep -qE '^- predicted-red: [0-9]+' "$T/br" || rc=1; nb=$(grep -c '^<!-- BEGIN derivation: ' "$T/br" || true); ne=$(grep -c '^<!-- END derivation: ' "$T/br" || true); nr=$(grep -c '^- reproduce: ' "$T/br" || true); test "$((10#$nb))" -ge 1 || rc=1; test "$((10#$nb))" = "$((10#$ne))" || rc=1; test "$((10#$nr))" = "$((10#$nb))" || rc=1; sed -n 's/^- reproduce: //p' "$T/br" > "$T/rep"; i=0; while IFS= read -r cmd; do test -n "$cmd" || continue; i=$((i+1)); eval "$cmd" > "$T/o$i" 2>/dev/null || rc=1; test -s "$T/o$i" || { rc=1; continue; }; lab=$(head -n 1 "$T/o$i" | sed -nE 's/^<!-- BEGIN derivation: (.*) -->$/\1/p'); test -n "$lab" || { rc=1; continue; }; awk -v b="<!-- BEGIN derivation: $lab -->" -v e="<!-- END derivation: $lab -->" '$0==b{f=1} f{print} f&&$0==e{exit}' "$T/br" > "$T/w$i"; test -s "$T/w$i" || { rc=1; continue; }; cmp -s "$T/o$i" "$T/w$i" || rc=1; done < "$T/rep"; test "$i" -ge 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC17** The repository's own record and hygiene checkers are green over this task's files. `bash bin/check-pii-shapes.sh --base <branch-point>` reports clean and exits 0 — the criterion that catches a home-directory absolute path pasted in from a launch record or a watchdog command line before it reaches a review round; `bash bin/check-provenance.sh <provenance>` and `bash bin/check-interventions.sh --task T-1085 -- <interventions>` each exit 0 against this task's own records, both paths resolved through `bin/team-paths.sh`; `bash bin/check-handoff.sh <board>` exits 0; and `bash bin/check-intent.sh <spec> <board>` exits 0, reporting the frozen intent block aligned with the recorded hash. Positive controls: every input path is asserted readable before its checker runs, so a missing record fails as a missing record rather than as a checker error nobody reads.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1085-default-path-firing.md; test -s "$SP" || exit 1; B=$(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base "feature/1084-dispatcher" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; PV=$(bash bin/team-paths.sh --get provenance) || exit 1; IV=$(bash bin/team-paths.sh --get interventions) || exit 1; test -r "$BD" || exit 1; test -r "$PV/T-1085.md" || rc=1; test -r "$IV/T-1085.md" || rc=1; bash bin/check-pii-shapes.sh --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-provenance.sh "$PV/T-1085.md" >/dev/null 2>&1 || rc=1; bash bin/check-interventions.sh --task T-1085 -- "$IV/T-1085.md" >/dev/null 2>&1 || rc=1; bash bin/check-handoff.sh "$BD" >/dev/null 2>&1 || rc=1; bash bin/check-intent.sh "$SP" "$BD" >/dev/null 2>&1 || rc=1; test "$rc" -eq 0

- [ ] **AC18** The changed set stays inside this task's declared allow-list, tracked half and untracked half both. **This criterion is merge-point-scoped and is expected to go stale after merge** — once a later task's files land on the same base ref its expected set no longer describes reality, which is not a defect to chase and must not be repaired by widening its base-ref resolution or re-deriving it per rework round. The tracked half comes from the commit range `git diff --name-only <branch-point>...HEAD`; the untracked half from `git ls-files --others --exclude-standard`, which sees strays a commit range cannot; their union must be a subset of exactly these seven paths: this spec, the board, this task's provenance / interventions / review records, `.shell-team/test-recipe.md` and `docs/loop-engineering/default-path-firing.md`. The working tree is deliberately read for the untracked half — that half's whole subject **is** the working tree, which is why the range form alone cannot supply it. This allow-list carries no shipped path at all, which is the same property **AC1** measures from the other direction; the two are kept separate because **AC1** is a design-decision lock that must survive merge and this one is not. Positive control: the union is asserted non-empty, so a resolution failure that produced an empty set cannot pass as a clean scope.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base "feature/1084-dispatcher" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1085r.XXXXXX") || exit 1; git diff --name-only "$B"...HEAD > "$T/tracked" 2>/dev/null || rc=1; git ls-files --others --exclude-standard > "$T/untracked" 2>/dev/null || rc=1; cat "$T/tracked" "$T/untracked" | grep -v '^$' | sort -u > "$T/changed"; test "$(grep -c . "$T/changed" || true)" -ge 1 || rc=1; printf '%s\n' .shell-team/specs/T-1085-default-path-firing.md .shell-team/todo.md .shell-team/provenance/T-1085.md .shell-team/interventions/T-1085.md .shell-team/reviews/T-1085.md .shell-team/test-recipe.md docs/loop-engineering/default-path-firing.md | sort -u > "$T/allow"; comm -23 "$T/changed" "$T/allow" > "$T/extra"; test "$(grep -c . "$T/extra" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC19** Runtime, `SKIP` by design — no command can prove a command was run, and no command available to this repository can prove an `Agent` tool call was issued. **This criterion is reported item by item, in this order, one line or paragraph per item, each naming what was measured and what the result was; a compressed summary does not discharge it, and a whole-set claim inside any item carries the enumeration it summarises.** Each item names its owner. An **(O)** item is the **orchestrator's** — the firing itself and the conditions it ran under — and **no item below asks the engineer to launch an agent, fire an arm, or re-run the fan-out**: the engineer holds no `Agent` or `Task` tool and could not, as all nine `agents/*.md` `tools:` lines measured at authoring show. **(a)** [engineer] A producer-run **mutation self-check** on a scratch copy outside the working tree (a `git worktree add --detach` scratch at HEAD, or a copy under `$TMPDIR`), covering every criterion carrying a `- check:` line, each mutation observed red, restored, observed green again, each reported separately: add a file under `bin/` (**AC1** red); rename one required note heading (**AC2** red); alter one byte inside the note's evidence section (**AC3** red); alter one byte inside the spec's `firing-protocol` region (**AC4** red); add a second `- default-reachability:` line (**AC5** red); flip one chain link to `not-evidenced` while the verdict stands (**AC6** red); flip one licence condition to `not-met` while the verdict stands (**AC7** red); change the board record's value to `serial` (**AC8** red); rename one instance id on an `- assign:` line so the two sets diverge (**AC9** red); change the `exit=0` on the attribution checker's result line (**AC10** red); move one heartbeat outside the re-derived window (**AC11** red); change one `- liveness:` mechanism token (**AC12** red); flip the `- saving-direction:` token against its own integers (**AC13** red); add an unattributed line mentioning `43.84` (**AC14** red); alter one byte inside an embedded derivation block (**AC15** red); delete the `- predicted-red:` line (**AC16** red); insert a home-directory-shaped path into the note (**AC17** red); add an untracked stray file (**AC18** red). For **AC6**/**AC7** the probes must include one aimed at each direction of the coupling — a `met` verdict with a failing input, and a `not-met` verdict with all inputs passing — because a two-directional coupling is vacuous in two independent ways and a probe of one direction says nothing about the other. **(b)** [engineer] The **execution-context matrix** for every command this task's records tell a reader to run — this repository's checkout root without the plugin on `PATH`, and an adopter-shaped repository with the plugin loaded (different cwd, `bin/` scripts reached by bare name), in both supported layouts — each cell run, not read, with its result stated; where a command is genuinely repository-local and has no adopter-shaped cell, say so per command rather than as one blanket sentence. **(c)** [engineer] **Full CI-equivalence**, as the `mechanism` class owes with nothing priced down: every `- name:` step in `.github/workflows/check-handoff.yml` enumerated by the command that counted them, run locally in workflow order, each with its result; any step run with a local substitute or judged inapplicable named **individually** with its reason, never as one blanket sentence. **(d)** [engineer] The `## Blast radius` table's production narrated: the enumeration command run twice byte-identically, the scratch tree the base-side verdicts were read from **at the branch point's committed blobs**, the per-spec `(spec, AC)` key reconciliation between base and head with the count of keys present on only one side, per differing spec one line stating which criterion changed verdict and why, the indirection class's disposition stated explicitly, and — because this task's own note and records are committed **after** the head-side sweep's pinned commit — a **post-assembly re-run** of every spec whose criteria could be moved by those later commits (any scope lock, any `git status` gate, any count over `docs/` or `.shell-team/`), with the table corrected to the measurement. Where a measured verdict differs from this spec's prediction, the table (which sits outside the intent block) is corrected and the correction is stated; the prediction is never defended. **(e)** [engineer] The measured 40-hex values of the branch-point expression's own result, `git rev-parse feature/1084-dispatcher`, `git rev-parse develop` and the evidence commit, together with which arm the existence test selected, the namespace the predecessor actually resolved in, and `git branch --show-current`. **(f)** [engineer] A per-source report for every `## Summarized sources` entry, stating whether the reporting role opened it and what it confirmed or contradicted about the cited distinction. **(g)** [O, orchestrator] The firing's own execution conditions restated for the record: the clock source used and the evidence its nanosecond field expanded; the machine's logical core count and whether `TEAM_FANOUT_MAX` was set; the resolved `subagent_type` and model recorded verbatim as declared labels; the explicit per-call Bash timeout passed to each instance and the largest single-unit cost from the pre-arm cost pass that sized it; the `CHECK_ACS_TIMEOUT` value exported identically in both arms; the pinned commit both arms' clones were checked out at; and the fact that every timing figure in the note is machine-local and carries no git ref. **(h)** [O, orchestrator] The post-arm cleanliness reads (`git status --short` and `git ls-files --others --exclude-standard` in the real checkout) with their results, confirmation that each arm ran in its **own** freshly-created `git clone --no-hardlinks` and that `git worktree add` was used for no arm's venue, and confirmation that the launch record, the telemetry and the aggregation ran against the **default-resolved** runs directory with `TEAM_RUNS_DIR` unset. **(i)** [O, orchestrator] Every relayed premise in `## Assumptions` re-measured with the measured value reported beside it, and any premise that could not be measured from the executing side named as still relayed rather than silently dropped — PR #325's state and CI at the recorded tip, the predecessor's ref namespace, issue #277's remaining-refinements text, and the sprint plan's own release-condition wording in particular.

## Input space

**Reachable input classes** — what this firing, this note and these criteria really meet:

- **An instance whose Bash call times out mid-unit.** The harness's default foreground Bash timeout is 120 s and one measured unit in this corpus costs ≈143 s, so a call with no explicit timeout is genuinely expected to die mid-unit. The protocol requires an explicit per-call timeout sized above the largest measured single-unit cost, and an instance that still times out produces a partial part file — which is the next class.
- **An instance that writes no part file, or a truncated one, without anyone terminating it.** `bin/aggregate-verdicts.sh`'s own header states that an instance that died and an instance that wrote nothing are the same operational fact, and exit 3 (`missing-part` / `empty-part` / `uncovered-unit`) is the shipped refusal. The chain link `aggregation-reduced` reads `not-evidenced` in that case and the verdict follows.
- **A unit whose `bin/check-acs.sh` run exits non-zero for entirely legitimate reasons** — a merge-point-scoped scope lock that has gone stale, a criterion pinned to a line number that moved, a pre-existing red another spec discloses about itself. **The unit is the run, never its verdict**: a non-zero `unit_rc` is a datum recorded verbatim, never read as a result about the spec it ran against, and never as a defect in this task.
- **A unit that produces no acceptance-criterion lines at all** — a non-spec `.md` file living in the specs directory. `bin/aggregate-verdicts.sh`'s own header names `design-note-T-1012.md` as exactly this case, recording `exit=2 no-verdict-lines` identically across ten pilot runs, and the `- sentinel:` shape exists so the unit is a datum rather than a gap. The population includes it.
- **A host whose cap resolves below 2** — `TEAM_FANOUT_MAX=1`, or a measured core count of 1. The shipped degree rule then chooses `N=1` and no fan-out occurs; `real-plugin-role-instances` reads `not-met`, the verdict reads `not-met`, and that is the honest answer for a release gate asking whether the default path fans out on this host.
- **Two arms writing telemetry into one shared, default-resolved corpus**, which is why the arms are separated by an `--attempt` narrower and by disjoint instance-id prefixes — `bin/check-fanout-instances.sh`'s own help text names the mixed-scope refusal this avoids.
- **A verdict difference between the two arms caused by the measurement rather than by concurrency** — a per-check cap firing under N-way contention, or a criterion reading a machine-local telemetry corpus that one arm's own run grew. Both are documented artefact classes in this repository. The protocol's answers are structural (a shared, recorded `CHECK_ACS_TIMEOUT`; one fresh clone per arm) and, where one still occurs, disclosed per key on a `- parity-exception:` line with an isolation re-run.
- **A board that grows while the task runs**, appended to by this very loop, which is why every board-reading criterion anchors on a task-scoped line or on this task's own entry extent rather than on a whole-board count.
- **A record line carrying a trailing carriage return.** `bin/check-intent.sh` strips `\r$` before matching a marker, measured at line 295, so the intent and protocol regions survive it; the note's own criteria read files this repository commits with LF and are deliberately **not** widened to strip it, which is stated here rather than left to be discovered.
- **A note read months later by someone who did not run the firing** — the primary reader of every evidence line, which is why the evidence quotes commands, records, raw span rows and tool output verbatim rather than only conclusions, and why every derivation block reads only the note itself and this repository's committed objects: the runs directory is gitignored and no scratch path survives the session.
- **Both supported layouts**, with `<runs>`, `<todo>` and `<specs>` resolving under `.shell-team/` or under `tasks/` + `docs/specs/`, which is why every path in every criterion is resolved through `bin/team-paths.sh` and never written as a literal.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect against:

- **A second repetition of either arm, or any inferential-statistics treatment.** One execution per arm answers a reachability question; a finding demanding a second rep, a variance model, a significance test or a confidence interval is `out-of-input-space`, and the note's Limits say so rather than leaving it to be noticed.
- **Fan-out degrees beyond `N = min(<unit count>, cap)`, and any knee-finding.** No sweep, no `n=16`, no scaling curve; the width axis is T-1069's, already measured, and the knee is recorded `undetermined` here by construction.
- **AC-level fan-out granularity.** Splitting the single largest unit into per-criterion units is `phase-multiplexing.md`'s own separate follow-up; a finding demanding it here is `out-of-input-space`.
- **Sub-second overlap discrimination.** The overlap claim is a non-empty intersection window whose margin against the maximum single launch latency is stated; a finding demanding finer timing resolution is `out-of-input-space`.
- **Model, harness-version or machine generalization.** The launched model and `subagent_type` are recorded verbatim as declared labels; a finding demanding a second model, harness version or machine is `out-of-input-space`.
- **An upgrade of the `subagent_type` claim from declared to harness-confirmed.** `agent-launch-fanout.md` line 247 already records the epistemic boundary and its counterfactual; a finding demanding independent harness confirmation of the resolved type is `out-of-input-space` and is an explicit non-goal.
- **Instance death by forced termination** — a `TaskStop`, a `kill`, a stopped process or an induced infinite loop. Any incomplete instance in this task ends its own turn naturally; a finding demanding a forced-termination shape is `out-of-input-space` and an explicit non-goal.
- **Parallel writes of the whole spec corpus's own `- check:` commands against the real checkout.** Every unit runs inside a throwaway clone precisely because the blast radius of running N instances' worth of arbitrary spec-authored commands concurrently against the working checkout is unmeasured; a finding demanding the units run in the real checkout is `out-of-input-space`, and the Limits section says so.
- **A synthetic population.** The population is real committed `.md` files under the resolved specs directory at one pinned commit; a finding demanding a constructed population, a stress corpus or an adversarially-sized file set is `out-of-input-space`.
- **A shipped-text change to make the firing work.** If the firing needs one, that is a finding filed as an issue and a `not-met` verdict, never an edit; a finding demanding the edit be applied here is `out-of-input-space` and an explicit non-goal.
- **Adopter environments that cannot run the shipped step at all** — no `git`, no POSIX awk, a shell older than bash 3.2. The zero-dependency floor is inherited unchanged from the tools this task does not modify.
- **A checkout with no local `feature/1084-dispatcher` ref and no `develop`, a detached `HEAD` with no merge base, or a rebased or force-pushed predecessor.** Every criterion fails closed on the first two; the third is a route-back to planning, not a case any criterion is widened to survive.

<!-- END intent-block: T-1085 -->

## Body-to-AC correspondence

One row per normative directive stated in the body prose, mapped to the criterion that carries it or marked exempt with a reason.

| # | Body directive | Source | Carried by |
|---|---|---|---|
| 1 | The default path is the shipped, unmodified text of the three named files applied as written | Goal, "What the default path means" | **AC1** (the three files byte-identical, zero shipped diff), **AC7** (`shipped-text-unmodified`) |
| 2 | The chain's four links: precedence rule → transcribed record → step-5 pointer → shipped mechanics | Goal | **AC6** (seven chain-link ids, closed grammar), **AC8** (the transcribed record itself) |
| 3 | Nothing outside the three files selects the fan-out; no out-of-band flag is permitted | Goal | **AC7** (`no-out-of-band-flag` licence condition, whose `not-met` forces the verdict) |
| 4 | `TEAM_FANOUT_MAX` and `CHECK_ACS_TIMEOUT` are permitted and every value used is recorded | Goal / protocol | **AC9** (`- cap-ground:` records the cap's ground), **AC13** (`- unit-timeout:` records the arm-identical value), **AC19(g)** |
| 5 | The `opt-in` parenthetical means no automatic per-phase firing, not a required flag | Goal, "The reading … adopted here" | `info-only (not promoted to AC)` — an interpretive claim about shipped prose, which no artifact read can adjudicate; its **mechanical** consequence is row 3's licence condition, and the note records the reading so a reader can reject it explicitly rather than discover it implicitly |
| 6 | If that reading is wrong, even a successful firing is `not-met` | Goal | **AC7** (`no-out-of-band-flag — not-met` ⇒ `- default-reachability: not-met`, checked in both directions) |
| 7 | The existence-capability reading of `phase-multiplexing.md:364`'s second conjunct is adopted; T-1084's stricter reading is not | Goal, "The divergence" | `info-only (not promoted to AC)` — the same interpretive class as row 5; its mechanical trace is **AC8**'s requirement that the recorded value is `tier1-fanout` with a `conditional` modality and a resolvable priced ground, which is only writable under this reading |
| 8 | The divergence from T-1084's record is stated in the note, with both readings recorded | Goal | `info-only (not promoted to AC)` — a prose-completeness requirement the reading gates own; freezing a grep for it would test the note's wording rather than the property |
| 9 | The firing sits in Validate, after Implement, because the shipped text puts it there | Goal, "Why the firing sits in Validate" | **AC3** (the weakened ordering assertion this directive forces, stated in that criterion's own body) |
| 10 | The consequent rework cycle is planned, not defective | Goal | `info-only (not promoted to AC)` — a statement about how a loop outcome is read; no artifact carries it |
| 11 | Anti-fitting is carried by the protocol byte-lock plus the evidence byte-lock, not by ordering | Goal | **AC4** (protocol region byte-identical between the evidence commit and HEAD), **AC3** (evidence section byte-identical) |
| 12 | The unit is `bash bin/check-acs.sh <spec>`, unchanged | Protocol, "The unit and the population" | **AC7** (`production-unit` licence condition) |
| 13 | The population is every `.md` file in the resolved specs directory, with no name-based filtering | Protocol | `info-only (not promoted to AC)` for the inclusion rule itself — a criterion asserting "no filter was applied" would have to reconstruct the unfiltered set from the same directory the population came from and could not distinguish a filter from an empty one; the mechanical traces are **AC9** (every unit assigned exactly once, the `- assign:` unit set equalling the population file the record names) and **AC19(d)**'s narration, and the enumerating command is stated beside the derivation block |
| 14 | Non-spec `.md` files are included, grounded in `bin/aggregate-verdicts.sh`'s own named `design-note-T-1012.md` sentinel case | Protocol | `info-only (not promoted to AC)` — same class as row 13; the shipped `- sentinel:` shape is what makes it safe and the aggregation's exit 0 (**AC10**) is what proves it was |
| 15 | The population is fixed before any instance is launched and never re-selected after a verdict is seen | Protocol | **AC6** (`population-fixed` chain link), **AC7** (`real-population`) |
| 16 | Per-file costs are measured singly before any arm runs, so the per-call timeouts are sized from measurement | Protocol / hazards | **AC19(g)** (the largest single-unit cost reported beside the timeout it sized) |
| 17 | The subject is this task's own owed blast-radius sweep, not a synthetic errand | Protocol | **AC16** (the full-population sweep is the deliverable this population is), **AC7** (`real-population`) |
| 18 | The base-side half of the sweep is serial and not part of the fan-out | Protocol | `info-only (not promoted to AC)` — a scoping statement about which work the fan-out covers; **AC19(d)** narrates both halves |
| 19 | Every unit runs in a throwaway `git clone --no-hardlinks`; `git worktree add` is never used for a venue | Protocol, "Venue" | `info-only (not promoted to AC)` for the venue's own identity — the downstream property that matters is that the real checkout stayed clean, which **AC17** measures (PII cleanliness over the diff) and **AC19(h)** reports with the two cleanliness reads |
| 20 | Each arm gets its own freshly-created clone of the same pinned commit | Protocol, "Venue" | `info-only (not promoted to AC)` — a temporal property of how two commands were issued; reported under **AC19(g)**/**(h)**, and its purpose (arm-independent telemetry) is what **AC13**'s parity line and its exception path make visible |
| 21 | The decision, the board records, the launch record, the telemetry and the aggregation run in the real checkout with the runs directory resolved by default | Protocol, "Venue" | **AC8** (the board record in the real board), **AC9**/**AC10** (the launch record and aggregation quoted from the real run), **AC19(h)** (`TEAM_RUNS_DIR` unset, confirmed) |
| 22 | The degree rule is the shipped `N = min(<unit count>, cap)` with N and the cap's ground recorded | Protocol, "Degree and baseline" | **AC9** (`- requested-n:` ≥ 2 and `- cap-ground:` from the two-token closed vocabulary) |
| 23 | The baseline is single-instance, same population, same session, same pinned commit, same `CHECK_ACS_TIMEOUT` | Protocol | **AC13** (`- baseline: serial —`, `- unit-timeout:`, `- execution-order:`, `- cache-warmth:`) |
| 24 | Execution order and cache warmth are recorded | Protocol | **AC13** |
| 25 | One execution per arm; no repetitions and no inferential statistics anywhere | Protocol / Non-goals | `info-only (not promoted to AC)` — an absence in prose the reading gates own; the Limits section discloses it rather than leaving it to be noticed |
| 26 | The baseline arm writes no launch record but is reduced under its own label | Protocol | **AC9** (exactly one `launch-record 1` inside the one `t1085fan` region — a second arm's record would break the exactly-one closure) |
| 27 | The aggregation-safety comparison is the two `verdict-region` regions' byte-identity, recorded on one parity line | Protocol | **AC13** (`- verdict-region-parity:` exactly one, closed vocabulary), **AC15** (`t1085-verdict-region-parity` derivation block) |
| 28 | A parity difference is enumerated and diagnosed per key with an isolation re-run, never glossed | Protocol | **AC13** (`differs` ⇒ at least one `- parity-exception:` line) |
| 29 | A parity difference is a disclosed finding, not a `not-met` verdict | Protocol | **AC7** (parity appears in neither the chain-link nor the licence-condition sets, so it cannot move the verdict — the coupling is closed over exactly those thirteen inputs) |
| 30 | The part-file grammar is the shipped three shapes, one verdict line per unit rather than per criterion | Protocol | **AC10** (the aggregation exits 0, which is only reachable if every part conformed to that grammar) |
| 31 | Every instance Bash call carries an explicit per-call timeout above the largest measured single-unit cost | Protocol, "Hazards" | **AC19(g)** |
| 32 | Every per-instance liveness check is harness-tracked-background, one per instance, command recorded verbatim | Protocol, "Hazards" | **AC12** (grammar closure, ≥2, no bare `&`, attribution to the measurement), **AC9** (≥2 inside the record) |
| 33 | The two arms are separated by an `--attempt` narrower and disjoint instance-id prefixes | Protocol | **AC10** (`check-fanout-instances` exit 0 for the `t1085fan` scope, which a mixed scope would refuse with `missing-instance`) |
| 34 | Exactly one `- default-reachability: met\|not-met — <ground>` line, `met` iff seven links evidenced and six conditions met | Protocol, "The verdict" | **AC5** (exactly one, in its own section), **AC6** and **AC7** (both directions of the coupling) |
| 35 | A `not-met` verdict carries one `- not-met-ground:` line and, when `chain`, one `- blocked-at:` line naming a link | Protocol | **AC6**, **AC7** |
| 36 | 43.84% is the pilot's figure, attributed at every point of mention, never restated as this task's own | Protocol, "The claim ceiling" | **AC14** (count of lines mentioning it equals the count also naming `phase-multiplexing.md`) |
| 37 | A small or negative saving is still `met`; only a failed firing is `not-met` | Protocol | **AC7** (the saving is not one of the six licence conditions, so it cannot move the verdict by construction) |
| 38 | The knee stays `undetermined` because one degree is run | Protocol / Non-goals | **AC14** (`- knee: undetermined —` exactly once) |
| 39 | Both release consequences are written before the result is known, the `not-met` one naming merge-only | Protocol | **AC5** |
| 40 | A `not-met` verdict does not stop the task; the note ships and every criterion is claimed | Protocol | **AC6**–**AC13** (each evidence criterion conditioned on its own chain link, with a `blocked-at` branch) — the satisfiability under both verdicts is the mechanical form of this directive |
| 41 | Each evidence criterion is conditioned on its own chain link, so `not-met` cannot dodge evidence for a completed link | Criteria preamble | **AC9**, **AC10**, **AC11**, **AC12** (each conditions on `population-fixed` / `aggregation-reduced` / `attribution-verified` / `instances-launched` individually) |
| 42 | `subagent_type` stays a declared label and is not upgraded | Protocol | **AC14** (`- agent-type:` carries `declared label` and cites `agent-launch-fanout.md`) |
| 43 | No shipped-text change of any kind; a demand for one is an issue and a `not-met`, never an edit | Non-goals | **AC1**, **AC18** (an allow-list carrying no shipped path) |
| 44 | No new checker, suite, CI edit or `bin/` script | Non-goals | **AC1** (zero paths under `bin/`, `tests/`, `.github/`) |
| 45 | No repair or retro-fit of any merged spec's criteria this sweep reddens; disclosure only | Non-goals | **AC16** (`- predicted-red:` and the disclosure table), **AC18** (the allow-list excludes every merged spec) |
| 46 | The four sibling loop-engineering notes stay byte-identical | Non-goals | **AC1** for `phase-multiplexing.md`; the other three are covered by **AC18**'s allow-list, which admits no `docs/loop-engineering/` path but the new note |
| 47 | No forced termination anywhere | Non-goals | `info-only (not promoted to AC)` — T-1083 carried a command-line-scoped absence test for this; here the incomplete-instance path is not an arm this task constructs at all, so there is no induced-death command to scope a test to, and the absence is reported under **AC19(g)**/**(h)** |
| 48 | No issue closed and none reopened; #277's close decision belongs to the batch close-out | Non-goals | `info-only (not promoted to AC)` — a disposition about an external tracker this role cannot read and no criterion should assert; recorded in `## Notes for engineer` so close-out does not re-derive it |
| 49 | No version bump, CHANGELOG, release-notes, manifest, lessons or retro edit; no merge or push | Non-goals | **AC18** (every one of those paths is outside the allow-list) |
| 50 | Nothing is priced down; the full-population inventory and full CI-equivalence are owed | Declaration region | **AC16** (the disclosure and its method), **AC19(c)** and **(d)** (the runs) |
| 51 | `- user-visible: no`, with neither an `- adopter-surface:` nor an `- adopter-docs-waiver:` marker | Declaration region | `info-only (not promoted to AC)` — the declaration gate is read by `pm-spec` at authoring and by the freeze run before the hash is recorded, per the shipped self-check; issue #250 carries the mechanical gate, and inventing a local one here would ship a second, divergent implementation of it |
| 52 | `- base-ref-discriminator:` two-arm expression, spelled byte-identically, no 40-hex literal | Declaration region | `info-only (not promoted to AC)` — enforced by the freeze run's own read and by every criterion above spelling it identically; a criterion grepping this spec for its own expression would test the spec's prose against itself rather than the property |
| 53 | Pre-commitment: two triggers (same-component rounds, and a 90-minute serial-baseline budget), one droppable component, a never-dropped firing chain, one re-firing | Protocol, "Pre-commitment" | `info-only (not promoted to AC)` — a rule governing how the loop reacts to rounds and to a budget, which no artifact-reading check can observe; its one mechanical trace is that a dropped population would change what **AC9**'s `- assign:` set and the derivation blocks contain, and **AC19(d)** narrates it |
| 54 | Every derivation block reads only the note and committed objects, never an ephemeral scratch path | **AC15** body | **AC15** (every `- reproduce:` command actually re-run and byte-compared, which an ephemeral path would fail) |
| 55 | No criterion counts a `- verdict: ` or `- unit: ` prefix note-wide | Criteria preamble | `info-only (not promoted to AC)` — a constraint on this spec's own criteria, which a criterion inside the set cannot test without circularity; it is stated once in the preamble and discharged by construction, and T-1083's engineer hand-off is the recorded reason it exists |

| 56 | Prose quotes a record grammar mid-line, never at line start, and another document's records are quoted the same way | Protocol, "Prose quotes a grammar mid-line" | **AC5**, **AC6**, **AC7**, **AC10**, **AC12**, **AC13** — every prefix closure and exactly-one count in those criteria depends on this holding, and **AC10**/**AC12**/**AC13** additionally scope their bare-prefix closures to the section that owns the real records, so the directive and the mechanics defend each other rather than either standing alone |
| 57 | The convention is the shipped idiom `templates/prompt-blocks/dispatch-record.md` already states, reused rather than invented | Protocol | `info-only (not promoted to AC)` — a provenance statement about where a rule came from; the rule's effect is row 56 and the file itself is byte-locked by **AC1** |
| 58 | Each instance runs under its own `TMPDIR` subdirectory, recorded | Protocol, "Venue" | `info-only (not promoted to AC)` — a property of how N commands were invoked, which no artifact read can observe after the fact; reported under **AC19(g)**, and its purpose (no cross-instance scratch collision) is what the aggregation's own exit 0 in **AC10** would have failed on had it not held |
| 59 | The baseline arm's single part file is reduced under its own label, so both arms produce comparable `verdict-region` regions | Protocol, "Degree and baseline" | **AC13** (the parity line the comparison produces), **AC15** (the `t1085-verdict-region-parity` derivation block, whose two sets read both arms' quoted regions out of the note itself) |

## Singular-determiner sweep

This spec's frozen text governs three nouns that are emphatically not singular, and a cardinality-one assumption is the specific failure to sweep for. Each governed noun was read against its plural case before freezing.

| Phrase | Governed noun | Plural reading | Disposition |
|---|---|---|---|
| "the instance", "that instance's own invocation" | an instance | **N-many** — `N = min(<unit count>, cap)`, and N is at least 2 or the verdict is `not-met` | Resolved: every instance-facing requirement is written per instance and carries an instance-id field — `- assign:`, `- liveness:`, `- agent-timestamp:` all key on `<instance-id>`, and **AC9**/**AC11**/**AC12** each require at least two |
| "the axis", "the dispatch record" | an axis | **2-many today, 3-many after the depth axis (#274)** | Resolved: **AC8** requires one record per axis with each key appearing at most once, matching the shipped grammar's own "every axis the note defines is recorded"; the exactly-two total is the only cardinality literal in this spec and it carries a `- stale-at:` trigger naming the depth axis |
| "the unit", "the population" | a unit | **many-many** — one per `.md` file in the resolved specs directory | Resolved: the population is a file, not a list in this spec; no criterion states its size; **AC9** asserts the assign-set is duplicate-free by re-deriving both counts rather than by comparing either to a literal |
| "the arm", "the clone" | an arm | **exactly 2** — fanned and serial — and each has its **own** clone | Resolved: the two arms are named separately everywhere (`t1085fan`, `t1085serial`; `- arm-wall-clock: fanned` / `serial`), and the per-arm clone is stated in the plural in the protocol's Venue paragraph |
| "the launch record" | the record | Genuinely **one per fan-out**, and this task runs exactly one fan-out | Left singular deliberately: the baseline arm is not a fan-out and writes none, which is stated in the protocol and is what makes **AC9**'s exactly-one closure correct rather than accidental |
| "the population file" | the population file | Genuinely one per fan-out — the shipped step already fixes it as enumerated once and never re-selected | Left singular deliberately; the singular is the property, not an oversight |

## Blast radius

**Method: full-population** — this task's declared `- verification-class: mechanism` prices the downstream-impact inventory as a full-population diff over every merged spec, with nothing priced down, and this task is the one where that pricing is load-bearing rather than ceremonial: the sweep **is** the fan-out's population. Every `.md` file under the resolved specs directory is enumerated at the branch point and again at HEAD; every one of its `- check:` lines is run at the branch point's committed blobs — through a `git clone --no-hardlinks` or `git worktree add --detach` scratch tree pinned to that ref, never the working tree — and again at the head-side pinned commit; and the two verdict sets are differenced criterion by criterion. A `grep`-of-mentions inventory is not an acceptable substitute here and would be structurally blind to the class this repository has already had overlooked twice: a criterion that reaches its target through **indirection** — a board path built at run time from `bin/team-paths.sh --get todo`, a file named only through a variable — matches no search for that path's literal bytes. A full-population diff does not rely on such a search and does not inherit the blind spot; every criterion is *run* at both refs regardless of what it names literally. Where a criterion still cannot be run at one ref, that is disclosed here rather than dropped.

**Population.** Derived at run time, never counted by eye. The block below is filled by running its own `- reproduce:` line at final state; **AC16** requires the embedded block to reproduce byte-identically from that command.

- reproduce: bin/derive-populations.sh --label t1085-spec-population --set "at-branch-point=git ls-tree -r --name-only $(if git show-ref --verify --quiet refs/heads/feature/1084-dispatcher; then git merge-base 'feature/1084-dispatcher' HEAD; else git merge-base 'develop' HEAD; fi) -- .shell-team/specs | grep '\.md$'" --set "at-now=git ls-files -- .shell-team/specs/*.md"

<!-- BEGIN derivation: t1085-spec-population -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: at-branch-point — status: 0 — lines: 89 — items: 89 — command: git ls-tree -r --name-only 9c950d76290cb0d70f0a01a11bdfe3e901b67043 -- .shell-team/specs | grep '\.md$'
- set: at-now — status: 0 — lines: 90 — items: 90 — command: git ls-files -- .shell-team/specs/*.md
- union: items: 90
- bucket: at-branch-point+at-now — items: 89
  - .shell-team/specs/T-1000-operating-conventions.md
  - .shell-team/specs/T-1001-retro-input-acquisition.md
  - .shell-team/specs/T-1002-intervention-capture-channel.md
  - .shell-team/specs/T-1003-retro-reads-interventions.md
  - .shell-team/specs/T-1004-optin-hook-sample.md
  - .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md
  - .shell-team/specs/T-1006-lessons-resolver-key.md
  - .shell-team/specs/T-1007-scope-typed-ledger.md
  - .shell-team/specs/T-1008-lessons-corpus-import.md
  - .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md
  - .shell-team/specs/T-1010-operator-language-boundary.md
  - .shell-team/specs/T-1011-telemetry-event-rows.md
  - .shell-team/specs/T-1012-loop-replay-generator.md
  - .shell-team/specs/T-1013-loop-replay-docs-wiring.md
  - .shell-team/specs/T-1014-flag-rail-data-path.md
  - .shell-team/specs/T-1015-cutting-a-release.md
  - .shell-team/specs/T-1016-close-out-entry-boundary.md
  - .shell-team/specs/T-1017-close-out-interventions-gate.md
  - .shell-team/specs/T-1018-freeze-attestation-gate.md
  - .shell-team/specs/T-1019-is-span-row-parity.md
  - .shell-team/specs/T-1020-lessons-supersede-sweep.md
  - .shell-team/specs/T-1020-supersede-adjudication.md
  - .shell-team/specs/T-1021-arith-base10-audit.md
  - .shell-team/specs/T-1022-close-out-gate-symmetry.md
  - .shell-team/specs/T-1023-block-size-deferral-record.md
  - .shell-team/specs/T-1024-check-line-mktemp-guard.md
  - .shell-team/specs/T-1025-assert-parity-dead-comparison.md
  - .shell-team/specs/T-1026-skill-md-doc-completeness.md
  - .shell-team/specs/T-1027-promote-retro-2026-08-04.md
  - .shell-team/specs/T-1028-class-m-refreeze.md
  - .shell-team/specs/T-1029-claim-fidelity-qa-step.md
  - .shell-team/specs/T-1030-reviewer-board-write-boundary.md
  - .shell-team/specs/T-1031-check-handoff-flag-anchor.md
  - .shell-team/specs/T-1032-audit-prose-accuracy.md
  - .shell-team/specs/T-1033-promote-retro-2026-08-05.md
  - .shell-team/specs/T-1034-refreeze-hardening-execbit.md
  - .shell-team/specs/T-1035-spec-template-staleness-locks.md
  - .shell-team/specs/T-1036-wording-batch-141-143-144.md
  - .shell-team/specs/T-1037-checker-retro-precision.md
  - .shell-team/specs/T-1038-errexit-safe-pin-keying.md
  - .shell-team/specs/T-1039-promote-retro-2026-08-06.md
  - .shell-team/specs/T-1040-frozen-repair-batch.md
  - .shell-team/specs/T-1041-freeze-ux.md
  - .shell-team/specs/T-1042-ignored-base-and-retro-ledger.md
  - .shell-team/specs/T-1043-pm-spec-check-conventions.md
  - .shell-team/specs/T-1044-test-infra-bundle.md
  - .shell-team/specs/T-1045-codex-version-provenance.md
  - .shell-team/specs/T-1046-ignored-base-verdict.md
  - .shell-team/specs/T-1047-promote-retro-2026-08-08.md
  - .shell-team/specs/T-1048-handoff-durability-barrier.md
  - .shell-team/specs/T-1050-check-layer-fast-follow.md
  - .shell-team/specs/T-1051-inspection-ux-polish.md
  - .shell-team/specs/T-1052-records-editorial.md
  - .shell-team/specs/T-1053-retro-mechanization.md
  - .shell-team/specs/T-1054-binding-config.md
  - .shell-team/specs/T-1055-adapter-envelope.md
  - .shell-team/specs/T-1056-loop-liveness.md
  - .shell-team/specs/T-1057-loop-integration.md
  - .shell-team/specs/T-1058-telemetry-binding.md
  - .shell-team/specs/T-1059-docs-release-notes.md
  - .shell-team/specs/T-1060-adopter-binding-docs.md
  - .shell-team/specs/T-1061-adopter-docs-gate.md
  - .shell-team/specs/T-1062-release-notes-compare-link.md
  - .shell-team/specs/T-1063-editorial-batch.md
  - .shell-team/specs/T-1064-shipped-docs-accuracy.md
  - .shell-team/specs/T-1065-task-class-verification-pricing.md
  - .shell-team/specs/T-1066-effort-time-telemetry.md
  - .shell-team/specs/T-1067-context-lifecycle.md
  - .shell-team/specs/T-1068-agent-concurrency.md
  - .shell-team/specs/T-1069-phase-multiplexing.md
  - .shell-team/specs/T-1070-check-handoff-scaling.md
  - .shell-team/specs/T-1071-record-set-derivation.md
  - .shell-team/specs/T-1072-telemetry-span-discriminator.md
  - .shell-team/specs/T-1073-harness-agent-concurrency.md
  - .shell-team/specs/T-1074-fanout-orchestration.md
  - .shell-team/specs/T-1075-fanout-adoption-versioning.md
  - .shell-team/specs/T-1076-log-run-locking.md
  - .shell-team/specs/T-1077-worktree-reconcile.md
  - .shell-team/specs/T-1078-tier3-pilot.md
  - .shell-team/specs/T-1079-tier2-judge.md
  - .shell-team/specs/T-1080-depth-axis-contract.md
  - .shell-team/specs/T-1081-freeze-sweep-hardening.md
  - .shell-team/specs/T-1082-telemetry-discriminator.md
  - .shell-team/specs/T-1083-agent-launch-fanout.md
  - .shell-team/specs/T-1084-dispatch-routing-record.md
  - .shell-team/specs/T-111-pii-shape-checker.md
  - .shell-team/specs/T-112-commit-identity-and-ignore-lock.md
  - .shell-team/specs/T-113-lessons-deidentification.md
  - .shell-team/specs/design-note-T-1012.md
- bucket: at-now — items: 1
  - .shell-team/specs/T-1085-default-path-firing.md
<!-- END derivation: t1085-spec-population -->

**Predicted rows, to be corrected by the measurement rather than defended.** The prediction below is written from a first-hand read of the merged specs' own disclosed criterion shapes plus T-1084's measured table, not from a search for mentions; where the measurement disagrees, this section (which sits outside the intent block) is corrected to the measurement and the correction is stated.

- predicted-red: 3

| Merged criterion | What it reads | Predicted base | Predicted head | Why |
|---|---|---|---|---|
| `.shell-team/specs/T-1084-dispatch-routing-record.md` **AC15** | that task's own merge-point-scoped scope-lock allow-list | base: PASS | **head: FAIL** | T-1085 stacking further commits on `feature/1084-dispatcher` is exactly what a merge-point-scoped lock anticipates; that criterion's own body says so about itself. Not repaired here |
| `.shell-team/specs/T-1083-agent-launch-fanout.md` **AC20** | that task's own merge-point-scoped scope lock | **base: FAIL** | **head: FAIL** | already red one car back, as T-1084's own measured table recorded; unchanged by this task |
| `.shell-team/specs/T-1081-freeze-sweep-hardening.md` **AC13** | that task's own merge-point-scoped scope lock | **base: FAIL** | **head: FAIL** | already red two cars back; unchanged by this task |
| criteria counting `.shell-team/specs/*.md` — `T-1071`'s `spec-population` derivation criterion in particular, plus any later criterion re-deriving that same population | a spec-file population pinned in a stored derivation block | **base: FAIL** | **head: FAIL** | this task adds exactly one spec file, so any pinned spec-population figure moves by one; `T-1071`'s criterion was already measured FAIL/FAIL at T-1084's branch point and stays so. This is the population-counting class this task's `+1` shifts, named here per the freeze-note list |
| `.shell-team/specs/T-1048-handoff-durability-barrier.md` **AC12** | `bin/close-out.sh` and `tests/close-out/run.sh` byte-identical to a pinned base ref | **base: FAIL** | **head: FAIL** | turned permanently red by T-1084's own deliverable, one car back; this task edits neither file and changes nothing about it |
| `.shell-team/specs/T-1083-agent-launch-fanout.md` **AC2**, **AC4** | `skills/run/SKILL.md` line survival; a zero-`bin/`/`tests/`/`.github/` changed set since T-1083's branch point | **base: FAIL** | **head: FAIL** | both turned red by T-1084's deliverable, one car back; this task's zero shipped diff (**AC1**) adds nothing to either |
| `.shell-team/specs/T-1082-telemetry-discriminator.md` **AC12**, `.shell-team/specs/T-1075-fanout-adoption-versioning.md` **AC3**, `.shell-team/specs/T-1079-tier2-judge.md` **AC13** | line-number pins and byte-identity locks over `skills/run/SKILL.md` and `templates/prompt-blocks/fanout-orchestration.md` | **base: FAIL** | **head: FAIL** | all already red at this branch point; this task touches none of those files, so none moves |
| `.shell-team/specs/T-1074-fanout-orchestration.md`, `.shell-team/specs/T-1069-phase-multiplexing.md`, `.shell-team/specs/T-1080-depth-axis-contract.md` criteria over the fan-out block and the dispatcher note | `templates/prompt-blocks/fanout-orchestration.md` and `docs/loop-engineering/phase-multiplexing.md` line grammars and totals | base: as measured | head: unchanged | both files byte-identical (**AC1**), so no criterion reading them can move |
| criteria reading the machine-local telemetry corpus resolved through `bin/team-paths.sh --get runs` — `T-1066` **AC5**/**AC7**, `T-1072` **AC6**/**AC7**, `T-1076` **AC15**, `T-1082` **AC6** | this checkout's own `.shell-team/runs/shell-team.jsonl`, gitignored and machine-local | base: as measured | head: as measured | the documented measurement-environment artefact class T-1082/T-1083/T-1084's own Blast radius sections each disclosed. This task **writes real telemetry into that corpus** as part of the firing, so a flip here is expected; each is re-verified individually, sequentially and contention-free before it is characterised, and none is caused by this task's diff content |
| `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC1** | a duplicated `- [ ] **AC1**` label — a real criterion plus a documentation-example pair `bin/check-acs.sh`'s label scan cannot distinguish | PASS+FAIL both refs | PASS+FAIL both refs | the pre-existing defect T-1083's and T-1084's Blast radius sections both already disclosed; unrelated and unchanged |
| every other merged spec's own scope-lock criterion | a `git diff --name-only <its base>...HEAD` allow-list | as measured | as measured | merge-point-scoped by construction; each such spec says so in its own criterion body |

**Two classes this sweep looks for specifically**, because they are the ones this task can move without any prediction above anticipating them: **(i)** any criterion whose subject is the machine-local telemetry corpus — this task is the first in the sprint to write a real fan-out's worth of span rows into it on the default path, so this class is expected to be larger here than in any predecessor and is characterised per criterion rather than in aggregate; **(ii)** any criterion counting files under `docs/loop-engineering/` or `.shell-team/`, which this task grows by one note and one spec respectively. Both are enumerated by running every criterion, never by searching for mentions.

**Measured, correcting the prediction rather than defending it.** Full population enumerated at both refs by `bin/derive-populations.sh` (above: 89 base, 90 head). Every criterion of every spec file was **run** at the branch point (a fresh `git worktree add --detach` scratch pinned to `9c950d76290cb0d70f0a01a11bdfe3e901b67043`, never the working tree) and again at the current head-side working tree, in a fan-out of 8 parallel `bash bin/check-acs.sh` invocations, via `bin/derive-populations.sh`'s own sibling reduction pattern (raw per-criterion `AC<n>: PASS|FAIL|SKIP` lines extracted from each of the 90 units' own output, keyed `(spec, AC)`). Key reconciliation: **1473** base keys, **1492** head keys, **0** base-only, **19** head-only (all `T-1085-default-path-firing.md`'s own — the file this task itself adds). Five raw verdict flips were found across the 1473 shared keys; each is re-verified individually, sequentially and contention-free before being characterised, exactly as this section's own method requires:

- `T-1001-retro-input-acquisition.md` **AC29** (`PASS`→`FAIL` in the raw 8-way sweep) — re-run twice in isolation at HEAD: `PASS` both times. A pure timing artifact of the parallel sweep, the same class T-1084's own Blast radius already named for `T-1034` **AC9**; not caused by this task's diff content.
- `T-1061-adopter-docs-gate.md` **AC1** (`FAIL`→`PASS` in the raw sweep) — re-run twice in isolation at HEAD: both runs printed **both** `AC1: PASS (exit 0)` **and** a second `AC1: FAIL (exit 127)` block for the same label, confirming the row already predicted above (the duplicated-`**AC1**`-label defect T-1083's and T-1084's own sections disclosed) rather than a new, single-valued flip; unrelated to this task's diff, unchanged.
- `T-1084-dispatch-routing-record.md` **AC15** (`PASS`→`FAIL`) — re-verified at the true base commit (`PASS`) and at head (`FAIL`, confirmed twice). **Exactly the row this table already predicted**, for the reason already stated there.
- `T-1084-dispatch-routing-record.md` **AC14** (`PASS`→`FAIL`) — re-verified at the true base commit (`PASS`) and at head (`FAIL`, confirmed twice). **Not individually named above, but the same class as AC15**: AC14's own check counts the board's top-level entry count as exactly one more than at T-1084's own branch point; T-1085's own entry is a *second* new top-level entry on top of T-1084's, so the count moves by two rather than one. A genuinely new red this task's own stacking causes, of the identical merge-point-scoped shape AC15 already discloses about itself.
- `T-1084-dispatch-routing-record.md` **AC16** (`PASS`→`FAIL`) — re-verified at the true base commit (`PASS`) and at head (`FAIL`, confirmed twice). **Not individually named above, but exactly the population-counting class the fourth table row already generalises** ("plus any later criterion re-deriving that same population"): AC16's own embedded derivation block pins the spec-population figure as it stood at T-1084's own commit; re-running its own `- reproduce:` command now returns the population as it stands after T-1085's own `+1` file, which no longer byte-matches the frozen block.

**Correction to the frozen prediction, stated rather than defended.** The `- predicted-red: 3` count above is **confirmed correct in magnitude** (three genuine new reds, none of them the telemetry-corpus or duplicate-label classes) but **wrong in identity**: the table named only `T-1084` **AC15** by number, leaving the other two members of its own already-disclosed classes (the merge-point-scoped board-entry-count class, and the population-counting class) unenumerated. All three are `T-1084-dispatch-routing-record.md`'s own criteria, all three are stacking-caused, and none is repaired here (Non-goals: no retro-fit of a merged spec's own criteria). No criterion outside `T-1084`'s own three is a genuine new red; every other row's own prediction held exactly as written.

**Indirection class, discharged explicitly.** Every one of the 90 units above was run via `bin/check-acs.sh` against the unit's own file — never grepped for mentions of a literal path — so a criterion reaching its target through `bin/team-paths.sh --get todo` (as `T-1084` **AC14** and **AC15** both do) or through any other run-time-constructed path was exercised exactly like any criterion naming its target literally. **AC16**'s own embedded population-derivation block is the mechanical proof that the population itself was never hand-counted.

**Post-assembly re-run (AC19(d)).** The head-side sweep above ran against the working tree as it stood mid-round-2 (before this round's own note-analysis and provenance commits landed). Re-run after those commits, scoped to the classes that could move (scope locks, `git status` gates, population counts over `docs/`/`.shell-team/`): `T-1084` **AC14**/**AC15**/**AC16** re-confirmed `FAIL` (unchanged — this round's own commits add no further top-level board entry and no further spec file, so the count-based locks do not move again); this task's own **AC1**–**AC18** re-confirmed 18/18 `PASS` at the post-commit HEAD (`## Notes for engineer`); no further criterion outside those already characterised above was found to move.

**Head-side pinning, disclosed rather than smoothed.** The fanned and serial arms both run at one pinned commit, which is the commit at which the firing fired — an **ancestor** of the final HEAD, since the note's analysis sections and this task's records are committed afterwards. **AC19(d)** therefore requires a post-assembly re-run of every spec whose criteria those later commits can move (any scope lock, any `git status` gate, any count over `docs/` or `.shell-team/`), with this table corrected to that measurement. T-1084's own sweep found a genuine new red only in exactly such a post-commit re-check, which is why the requirement is written rather than assumed unnecessary.

## Assumptions

- **RELAYED — `9c950d7` as `feature/1084-dispatcher`'s tip and PR #325's open state with CI green.** Reached this role through the tech-lead's Routing Map; this role holds no shell and no network path. No criterion writes a 40-hex literal; every criterion resolves the branch point itself and fails closed when neither arm resolves. The freeze run measures PR #325's state and its CI conclusion at that tip and records the measured values beside this line; **AC19(e)** reports the resolved 40-hex values. *Measured at freeze (2026-08-19): branch point `B = git merge-base feature/1084-dispatcher HEAD = 9c950d76290cb0d70f0a01a11bdfe3e901b67043`; `develop` tip `9b9f79a…` ≠ B (stack confirmed); PR #325 open; its CI check run reports `status: completed`, `conclusion: success` with `head_sha` equal to B exactly.*
- **RELAYED — the predecessor's ref namespace.** The `- base-ref-discriminator:` declaration names `refs/heads/feature/1084-dispatcher`, matching every predecessor measured in this sprint's train. This role cannot run `git show-ref`. The freeze run confirms it: a predecessor resolving only as a remote-tracking ref needs a meaning-preserving correction to `refs/remotes/<remote>/…` at **every** site the expression appears (the declaration plus each base-reading criterion) before the freeze, and one resolving in neither namespace while its PR is open is a fetch-depth gap and a route-back, never the fallback arm firing. *Measured at freeze (2026-08-19): `git show-ref --verify --quiet refs/heads/feature/1084-dispatcher` exits 0 — the predecessor resolves as a local branch, the declaration's `refs/heads/` namespace is correct, and the expression's first arm is the one that fires.*
- **RELAYED — issue #277's remaining-refinements text.** The Routing Map relays that #277 remains open with the live-firing half owed to this task. Corroborated first-hand from the repository where it allows it: `docs/loop-engineering/agent-launch-fanout.md` line 242 records `- umbrella: 277 — open` with "the live-firing half remains owed to T-1085", and line 255 carries the same as a `- follow-up:`. **No criterion depends on any figure or wording from inside the issue body**; the identifier appears as an anchor only. The freeze run measures the live body and state and records them here; a contradiction is a hand-off finding, not a silent correction. *Measured at freeze (2026-08-19): #277 is `open`; its live body carries precondition 2 (N concurrent Agent-tool invocations, shared with #274) and a `## Measurement refinements` section naming the full-73-file-corpus knee re-run and AC-level unit splitting — consistent with the frozen protocol, which runs the full-population sweep (stride subset as the declared droppable) and scopes AC-level splitting out.*
- **RELAYED — the sprint plan's release-condition wording**, including the planned version string. **AC5** deliberately pins neither: it requires both `- release-consequence:` lines to exist and the `not-met` one to name `merge-only`, and leaves the version string to the note's prose and to the operator's own batch-GO decision. The freeze run reads the sprint plan and records the measured wording beside this line so the note's `met` branch names the right consequence. *Measured at freeze (2026-08-19), from the sprint plan of record (paraphrased into English; the plan is an operator-side document): the release is conditional on this task's default-reachability measurement; a `not-met` verdict demotes the sprint to merge-only with no release; the planned version string is `v2.1.0`.*
- **RELAYED — the operator's watchdog mechanism.** The thing supplying each per-instance liveness check is host-side tooling this repository does not ship and this role cannot read; its existence and alert format are relayed. The protocol therefore requires only that the mechanism be harness-tracked-background and that its command be recorded verbatim, and names no particular script — the same restraint T-1074's own D8 fixed.
- **MEASURED FIRST-HAND by this role at authoring, not relayed** — every figure this spec's frozen prose cites was read at its primary source before it was written down. `43.84%` and the `staged-adoption` recommendation with its two named preconditions: `docs/loop-engineering/phase-multiplexing.md` line 355. The `verify` axis's closed value set and its conditional rule's existence-capability wording: lines 359 and 364 of the same file. The machine-local knee at `n≥4`: line 375. The ≈143 s single-unit cost (`142965525000` ns for `T-1044-test-infra-bundle.md`): line 371. T-1083's `concurrent-overlapping` verdict, its ≈140.385 s two-instance margin and ≈100.573 s four-way margin, its `aggregation-live` arm's two exit-0 checker results, and the `subagent_type`-as-declared-label boundary: `docs/loop-engineering/agent-launch-fanout.md` lines 200, 217, 186 and 247. The 120 s sub-agent foreground Bash default and the bare-`&` watchdog death: `skills/run/SKILL.md` line 166. That all nine `agents/*.md` `tools:` lines lack an `Agent`/`Task` token, and that `qa-verifier` carries `Read, Grep, Glob, Bash` with `model: sonnet`: read across all nine frontmatter blocks.
- **UNVERIFIED from this role — the host's logical core count and whether `TEAM_FANOUT_MAX` is set.** Together these fix `cap` and therefore N. T-1083 measured 8 cores on this host with `TEAM_FANOUT_MAX` unset, which is prior art rather than a guarantee about the firing host at firing time. A cap resolving below 2 produces `N=1`, `real-plugin-role-instances — not-met`, and a `not-met` verdict; that is a reachable outcome the Input space names, not a defect.
- **UNVERIFIED from this role — the clock source on the firing host.** Whether `date +%N` expands is unknown here; the protocol requires the firing to verify expansion, substitute a source that does, and record which, and **AC19(g)** reports it.
- **UNVERIFIED from this role — the resolved `subagent_type` token for a plugin-role agent.** The harness's own namespacing is not readable from this role, which is why **AC14** requires the value recorded verbatim on a line carrying `declared label` rather than pinning a spelling.
- **Assumed and stated — bash integer width.** **AC11** and **AC13** compare 19-digit epoch-nanosecond values using bash arithmetic, which is 64-bit on every platform this repository supports; `awk` and `sort -n` are deliberately avoided because a double cannot hold a 19-digit integer exactly.
- **Assumed and stated — the serial baseline's wall clock is bounded by the pre-commitment, not by hope.** A full-corpus single-instance sweep of ~90 units at this corpus's measured per-unit costs is plausibly tens of minutes and could exceed an hour. The 90-minute budget in the pre-commitment is the declared bound and the deterministic pre-verdict stride subset is the declared response; neither is a prediction that the budget will hold.

**Borrowed-vocabulary count premises.** Each token below is classified `own-coinage` (a literal this task introduces) or `borrowed` (vocabulary another document already coined), because a sweep scoped to this task's new literals says nothing about a token a merged sibling already carried onto the stack. This role holds no shell and performs the classification and enumeration only; the **freeze run** executes each command against the **branch point's committed blobs** and records the measured value beside its row before the freeze. A contradicting measurement is a hand-off finding, not a silent correction.

| # | Token | Classification | Measurement command (run at the branch point) | Measured |
|---|---|---|---|---|
| 1 | `- default-reachability:` | own coinage (this task) | `git grep -c -- '- default-reachability:' <branch-point>` — expected `0` | `0` (no match at `9c950d7`) |
| 2 | `- chain-link:` | own coinage | `git grep -c -- '- chain-link:' <branch-point>` — expected `0` | `0` |
| 3 | `- blocked-at:` | own coinage | `git grep -c -- '- blocked-at:' <branch-point>` — expected `0` | `0` |
| 4 | `- not-met-ground:` | own coinage | `git grep -c -- '- not-met-ground:' <branch-point>` — expected `0` | `0` |
| 5 | `- release-consequence:` | own coinage | `git grep -c -- '- release-consequence:' <branch-point>` — expected `0` | `0` |
| 6 | `- verdict-region-parity:` / `- parity-exception:` | own coinage | `git grep -c -- '- verdict-region-parity:' <branch-point>` and `git grep -c -- '- parity-exception:' <branch-point>` — expected `0` each | `0` each |
| 7 | `- arm-wall-clock:` / `- saving-direction:` / `- unit-timeout:` / `- execution-order:` / `- cache-warmth:` / `- claim-ceiling:` / `- knee:` | own coinage | `git grep -c -- '<token>' <branch-point>` for each — expected `0` each | `0` each (all seven) |
| 8 | `t1085fan` / `t1085serial` / `t1085-assign-vs-part` / `t1085-verdict-region-parity` / `t1085-spec-population` | own coinage (the labels every marker grep keys on) | `git grep -c -- 't1085' <branch-point>` — expected `0` | `0` (`t1085` no match) |
| 9 | `- probe-evidence-sha (T-1085):` | borrowed grammar (T-1083), task-scoped instance | `git grep -c -- 'probe-evidence-sha (T-1085)' <branch-point>` — expected `0`; and `git grep -c -- 'probe-evidence-sha' <branch-point>` — expected **non-zero** (T-1083's own board sub-bullet), which is why **AC3**/**AC4** scope on the task-qualified form | task-qualified `0`; generic **non-zero** — 29 hits across 8 files (T-1073/T-1083 specs, reviews, board, `docs/loop-engineering/harness-agent-concurrency.md`) |
| 10 | `- dispatch: ` | **borrowed** (T-1084) — the premise **AC8** rests on | `git grep -c -- '- dispatch: ' <branch-point>` — expected **non-zero**: T-1084's own entry already carries two live records plus the grammar's own quoted mentions, which is exactly why **AC8** is scoped to this task's own entry extent and never board-wide | **non-zero** — 74 hits across 11 files, incl. the two live records in T-1084's own board entry |
| 11 | `- licence-condition:` / `- checker-result:` / `- agent-timestamp:` / `- heartbeat:` / `- liveness:` / `- assign:` / `launch-record 1` / `- agent-type:` | **borrowed** (T-1073, T-1074, T-1083) | `git grep -c -- '<token>' <branch-point>` for each — expected **non-zero** in `docs/loop-engineering/agent-launch-fanout.md`, `templates/prompt-blocks/fanout-orchestration.md` and `skills/run/SKILL.md`; every criterion counting one of these is scoped to the **new** note, which does not exist at the branch point, so a non-zero elsewhere is inert | **non-zero for all 8 tokens** — all eight carried by `agent-launch-fanout.md`; `- liveness:` / `- assign:` / `launch-record 1` additionally in `fanout-orchestration.md` and `SKILL.md`; the remaining five additionally in `harness-agent-concurrency.md`. The borrowed classification holds per token; no criterion reads any single one of the three named files for these tokens, so the distribution across them is inert |
| 12 | `- saving:` / `- recommendation:` / `- umbrella:` / `- implication:` | **borrowed** (T-1069, T-1083) | `git grep -c -- '<token>' <branch-point> -- docs/loop-engineering` — expected **non-zero**; **AC13**'s `- saving:` count is scoped to the new note for the same reason | **non-zero** — `- saving:` 3+8, `- recommendation:` 1+5, `- implication:` 5+6 (`agent-concurrency.md`/`phase-multiplexing.md` and `agent-launch-fanout.md`/`harness-agent-concurrency.md`), `- umbrella:` 1 (`agent-launch-fanout.md`) |
| 13 | `43.84` | **borrowed** (T-1069, `phase-multiplexing.md`) | `git grep -n -- '43.84' <branch-point>` — expected **non-zero**; **AC14**'s attribution invariant is scoped to the new note, and the measured base-side sites are what confirm the figure's provenance is `phase-multiplexing.md` and not this task | **non-zero** — 12 files incl. `phase-multiplexing.md` (3 sites): provenance confirmed as T-1069's figure, not this task's |

## Open questions

None blocking. Four decisions are recorded rather than asked.

- **The population includes non-spec `.md` files.** Settled against `bin/aggregate-verdicts.sh`'s own header, which names `design-note-T-1012.md` as the pilot's real `SENTINEL: exit=2 no-verdict-lines` case across ten runs. A name-based filter would be an authoring judgment applied after seeing the list, which the shipped step's "mechanically enumerable, fixed before any instance is launched" requirement rules out.
- **The evidence lives in the note as verbatim quotes, not in a separate tracked evidence file.** The runs directory is gitignored (`.shell-team/.gitignore` line 4), so the launch record, the telemetry rows and the aggregation block survive only as quotes in something tracked. A sibling file under `docs/` was considered and rejected: it would hold the same bytes in a second place, adding a cross-file byte-lock and no durability. The consequence is accepted and stated — the note carries one line per unit in the launch record's `- assign:` lines, one per unit in each arm's quoted verdict region, and one per instance in the `- liveness:` and `- part:` lines, which is a large note and the price of the only durability path there is.
- **The verdict payload is one line per unit, not one per acceptance criterion.** A per-criterion aggregation block would run to thousands of lines; the per-criterion key set the blast-radius reconciliation needs is preserved in each instance's own raw outputs and reported under **AC19(d)**, while the aggregation block stays the note's durable, quoted, byte-comparable evidence.
- **The firing fires in Validate and the note is completed in the engineer round that follows.** Settled in the Goal with its reason: the shipped text places step 5's mechanics after step 4, and firing earlier would put them where the shipped path does not. The cost is one planned loop cycle; the alternative cost is the task's whole claim.

## Notes for engineer

- **You cannot fire anything, and nothing here asks you to.** Not one of this repository's nine `agents/*.md` `tools:` lines carries an `Agent` or `Task` token — re-measure that yourself rather than inheriting it — so only the orchestrator session can launch an instance. The `## Firing evidence (raw, orchestrator-produced)` section arrives already committed; treat it as read-only input. Your work is the note's other eight sections assembled from that evidence, plus this task's records.
- **Your first round ships the frame, not the analysis.** Before the firing there is no evidence to analyse. Ship the title, `## Terms and closed vocabularies`, `## Firing protocol (frozen before execution)` (restating the frozen region, not paraphrasing it), `## Supersession and follow-ups`, and the result-independent parts of `## Limits and what is not computable`. The analysis, verdict and saving sections come in the round after the firing. This is planned; see the Goal.
- **Re-measure the marker claim first-hand.** This spec places a second marker pair (`firing-protocol`) inside its Goal on the strength of `bin/check-intent.sh`'s marker detection being a whole-line equality match with `\r$` stripped, scoped by the derived task id (line 295). Read it yourself. If it is not true, that is a Blocker to surface, not something to work around.
- **Do not count a `- verdict: ` or `- unit: ` prefix note-wide, ever.** `bin/aggregate-verdicts.sh`'s own real output coins both, and this note quotes two of its blocks. T-1083's `## Notes from engineer` records exactly this collision as an unfixable frozen check line. Every count over those prefixes here is scoped inside an extracted marker region, and it must stay that way in anything you add.
- **`CHECK_ACS_TIMEOUT` has two distinct roles here.** Raise it to at least **300** when running this spec's own criteria (**AC17** runs five checkers, **AC15**/**AC16** regenerate derivation blocks). Separately, the value exported **inside both arms** is a recorded protocol parameter that must be identical across arms — a per-check cap that passes serially and fires under N-way contention would change verdicts between arms and make **AC13**'s parity comparison meaningless. Record both.
- **Measured-at-ref labels**: this task's deliverables print no `measured at <ref>` label over a machine-local figure, no `**Base ref**:` header and no per-row `- measurement:` field — **not applicable, by construction**: every timing figure in the note is machine-local and single-session, and the Non-goals bar attaching a git-ref label to one. The only ref-labelled reads are of committed blobs (the branch point, the evidence commit, the pinned arm commit) and those use `git show` / `git ls-tree` / a pinned `git clone`; **AC16** measures that the Blast radius section's base-side reading target is one of those and not the working tree.
- **The `10#` gotcha is load-bearing twice.** **AC11** compares 19-digit epoch-nanosecond values and **AC13** re-derives a saving direction from four of them. Use `$(( 10#$v ))`; do not reach for `awk` or `sort -n` when you reproduce those comparisons by hand, and do not "simplify" either check into one.
- **Files likely touched**: `docs/loop-engineering/default-path-firing.md` (assembly), `.shell-team/test-recipe.md` (append one entry naming `T-1085` — the two-clone arm venue, the arm-identical `CHECK_ACS_TIMEOUT`, the `--attempt` telemetry separation and the per-call instance timeout), `.shell-team/provenance/T-1085.md`, and the board entry. `.shell-team/interventions/T-1085.md` is the orchestrator's file; do not rewrite it. **Nothing under `skills/`, `templates/`, `agents/`, `bin/`, `tests/` or `.github/` — AC1 measures that as an outcome.**
- **Prior art to read before writing a line**: `docs/loop-engineering/agent-launch-fanout.md` in full (the note structure you are adapting, and lines 186, 200, 217, 242 and 247 in particular), `templates/prompt-blocks/fanout-orchestration.md` in full, `templates/prompt-blocks/dispatch-record.md` in full, `skills/run/SKILL.md` lines 39–101 and 153–215, `docs/loop-engineering/phase-multiplexing.md` lines 355–375, and `bin/aggregate-verdicts.sh` lines 1–100 (the part-file grammar, the sentinel case and the byte-identity property the parity comparison rests on). Do not edit any of them.
- **Dispositions decided here so close-out does not re-derive them**: this task **closes no issue**. Issue #277 stays open; whether the firing discharges its remaining live-firing half is recorded in the note as a statement about this task's evidence, and the close decision belongs to the sprint's batch close-out. Anything the pre-commitment drops goes to #277 with that round's findings attached as its requirement list.

## Verdict scope

Per the operator's proportionality ruling and T-1065's shipped pricing, the reading gates' depth is set by each artifact's consumer, and this task ships one class only — but it ships one claim inside it that is priced higher than the rest.

- **Internal analysis-artifact depth (not adversarial)** for `docs/loop-engineering/default-path-firing.md` and this task's records. Nothing here is executed by an adopter; the note is read by this repository's own maintainers deciding whether the sprint releases. The mandate is the computed values and the direction of the conclusion — whether the overlap window follows from the recorded timestamps, whether the verdict follows from its own chain links and licence conditions, whether the saving direction follows from the four recorded epochs, whether the exit codes quoted are the ones the tools actually produce. The criteria over the note are presence-and-structure shaped, and a finding that a criterion "only checks the shape" is a correct description of this pricing rather than a defect in it. A finding that **a number or claim in the note does not follow from the note's own recorded evidence** is squarely in scope, and **AC11**'s window re-derivation, **AC13**'s direction re-derivation and **AC15**'s block reproduction exist to make exactly that cheap.
- **One claim priced higher: the reading of `opt-in; no phase is rewired to use it by default` and the existence-capability reading of the conditional trigger.** These are not analysis-note prose — they are the load-bearing premises of a release decision, and the Goal states both explicitly and names the alternative readings so they can be attacked directly. A cross-provider finding that either reading is wrong is a **Blocker**, not a note, and its correct disposition is a `not-met` verdict rather than a rewording of the note.
- **The quantitative conclusion's settlement.** Per the corpus's own entry, QA's `PASS` on **AC13** gates progression but does not settle the saving's direction; the conclusion holds when the cross-provider review independently re-derives it from the same four recorded epochs. State that in the review briefing.
