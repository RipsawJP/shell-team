# The 2026-08-08 retro's four adjudicated corpus promotions land, and three prompt surfaces gain the mechanisations the adjudication scoped to them

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1047
**Base**: `99d4038` — relayed as `develop`'s tip and this branch's real branch point. Every `git` anchor below resolves that literal.
**Branch**: `feature/1047-lessons-promotion-prompt-contracts`

## Problem

The 2026-08-08 retro raised six lesson candidates. Four of them become corpus entries; three of the six additionally have a mechanisation half that belongs on a prompt surface, and one is deferred to a task that is already scheduled. Nothing in this loop executes an adjudication on its own — `bin/playbook-promote.sh` and `bin/gen-playbook-blocks.sh` are both run by hand — so a disposition that no task carries out evaporates while the defect classes it named keep firing. All four promoted classes cost real work in the cycle just closed: a pre-commitment fired for the first time and its value turned out to lie in a part of it that no existing entry prescribes; a repair that closed only the reproduced case cost a further round twice, in two unrelated tasks; an obligation added to a shared prompt region acquired a mode that had no carrier for it, three lines away from the file's own precedent for exactly that split; and a task whose only deliverables were records drew four consecutive rounds of contradictions between its own appends.

The mechanisation halves are what make three of those classes stop firing rather than merely be named. Each lands on a different surface and each is deliberately small: a checklist item in this role's own completion self-check, one mirrored sub-bullet in each of the two phases of the orchestrator's loop where a rework instruction is composed, and one review-contract item in the cross-provider reviewer's `## Rules` — that last one scoped to its mode by reusing the idiom the same file already carries, which is the promoted lesson applied in place rather than described.

## Summarized sources

One line per document whose facts this spec restates, naming the distinction being carried over. This section sits outside the intent block deliberately; naming a further source during a rework is an ordinary edit, not a re-freeze.

- `.shell-team/retros/2026-08-08.md`, `## Lesson candidates` (lines 60–65) — **read directly, in full**, along with `## Problem` and `## Try`. Distinctions carried over: **which of the six candidates is promoted here and which is not** (2, 3, 4 and 5 in; 1 mechanised without promotion; 6 deferred without promotion), and that the retro's own `[common]` / `[target-specific]` labels are an **input** to the `Scope` decision rather than the decision (DP2). Two distinctions the retro draws that this spec preserves rather than flattening: candidate 3's two instances are **independent, in two different tasks** (a `mktemp` exemption repair in one, an orientation-sentence repair in another) rather than two rounds of one task, and candidate 5's subclass claim is explicitly **distinct from** the corpus's `2026-08-06` frozen-prose entry — the retro says that entry covers frozen prose *describing a mechanism* while candidate 5 covers an *append-only log contradicting itself*, and DP3's audit keeps that distinction rather than collapsing the two.
- The records the retro's candidate lines cite — `.shell-team/interventions/T-1044.md`, `.shell-team/reviews/T-1044.md`, `.shell-team/reviews/T-1045.md`, `.shell-team/reviews/T-1046.md`, and the `T-1044` / `T-1046` entries of `.shell-team/todo.md` — **NOT opened by pm-spec**. Their content reaches this spec only through the retro's own candidate prose, which cites them. The four frozen entries' `Why` fields therefore restate **the retro**, not those records. AC17 (c) requires the engineer to open each named record and confirm every clause of every `Why` against it before the first review round, and to report which clauses were confirmed and which were generalised deliberately.
- `.shell-team/interventions/T-1047.md` — **read directly, in full**. Distinction carried over: its single `assumption-contradicted` entry records that the retro's premise "candidate 1 is unmechanised" is **partially false** — a relay re-measurement rule already exists in `agents/tech-lead.md`'s hand-written `## Rules` — and that Part B‑1 is therefore re-scoped from "add the rule" to "extend it into a named checklist item on the prompt-construction side". DP5 and the Non-goals are written to that corrected premise, not to the retro's.
- `.shell-team/specs/T-1039-promote-retro-2026-08-06.md` — **read directly, in full**. Distinctions carried over: the frozen-block-plus-extraction idiom every criterion below reuses (`<!-- frozen-begin: X -->`, a ```` ```markdown ```` fence, `sed '1d;$d'`), the nine-line `loop`-scoped entry shape, the derived-accounting criterion form its own DP7 established, and the standing precedent that a pm-spec hand-off entry carries **no** `intent-hash` or `freeze-attestation` sub-bullet. This spec does **not** edit that file and no criterion below reads it.
- `.shell-team/lessons.md` — **read directly**: its `## Format` section (the field grammar, the `all` = four-IN-roles gloss, the `Scope` shipping rule, and the two field bullets deliberately kept outside the fenced example), its complete `## ` heading index with each entry's `Applies-to` and `Scope`, and in full every entry named in the adjacency audit below. Distinctions carried over: `Scope` has exactly two values and `maintainer` ships to no generated block while requiring `Bound-in`; `Extended by` is recognised by field name only and its value is never schema-checked, while `Superseded-by` is resolved by equality against every other entry's key — which is why DP3's no-supersede verdict has to be a judgment recorded in prose and DP4's no-pointer default is a free choice rather than a constraint.
- `bin/playbook-promote.sh` — **read directly, in full**. Distinctions carried over: the emitted field order `Category, Applies-to, Scope, [Bound-in], Status, Source, Rule, Why, How to apply`; one blank line before each appended entry; that a `loop`-scoped entry emits **no** `Bound-in` bullet, giving the nine-line shape; and that `--title` takes the heading text **after** the em-dash only, the script writing `## <date> — <title>` itself.
- `bin/gen-playbook-blocks.sh` — **read directly** at its `ROLES` / `LINE_WARN_THRESHOLD` declarations, its `role_in_applies()` routing gate and its emit statement. Distinctions carried over: `Scope: loop` decides **whether** an entry ships and `Applies-to` decides **where**; only the `Rule` field reaches a generated block; the emitted line's exact shape is `- <rule> (<pointer-path>, <date> — <remainder>)`; and `LINE_WARN_THRESHOLD=40` produces a **non-fatal stderr warning**, never a failure.
- `bin/check-playbook.sh` — **read directly** at its enum and field-name declarations. Distinction carried over: the closed sets `KNOWN_CATEGORIES`, `KNOWN_ROLES` (`engineer qa-verifier tech-lead pm-spec all`), `KNOWN_SCOPES` (`loop maintainer`) and `KNOWN_FIELD_NAMES`. The four-role loop in AC9/AC10/AC11 is that closed set, not a population that grows.
- `templates/prompt-blocks/registry.txt` — **read directly, in full**. Distinctions carried over: `agents/pm-spec.md` is a **marker**-mode consumer of exactly two blocks (`language.md`, `playbook-pm-spec.md`); `agents/codex-reviewer.md` and `skills/run/SKILL.md` appear **only** under `contain` rules and therefore carry **no marker pair at all**, so every hand edit to them is by construction outside a marker region; and no rule of any mode names `agents/pm-spec.md`'s `## Spec completion self-check` section.
- `agents/pm-spec.md` — **read directly**, its `## Rules`, `## Spec completion self-check`, `## Frozen intent freeze` sections and its heading/marker index. Distinctions carried over: the self-check section is a list of bolded named items each on one physical line, several tagged `(forward-only)`; it is followed by `## Frozen intent freeze (v0.3.0 Phase A)`; and the file's first `<!-- BEGIN prompt-block:` marker comes after both. That ordering is what AC12 measures against.
- `agents/codex-reviewer.md` — **read directly**, its `## Preconditions`, `## Output`, `## Finding-evaluation mode (review-response)` and `## Rules` sections and its heading index. Distinctions carried over: the file carries **no** prompt-block marker pair; the mode heading sits **above** `## Rules`, which runs to end of file; and the self-scoping idiom exists in two shapes already — the Preconditions sentence naming the mode a duty does *not* apply in, and the `## Rules` item tagged `(default / fresh-review mode only)` with the same clause inline. M3 reuses the second shape, in the section the second shape lives in.
- `agents/tech-lead.md` — **read directly**, its `## Rules` section and marker index. Distinction carried over: the relay re-measurement rule is the **last** plain bullet of that hand-written section, above all three of that file's marker regions, and it is stated for the Routing Map. **This file is not edited by this task** (DP5, Non-goals); it is read only to establish that the rule exists and what it already covers.
- `skills/run/SKILL.md` — **read directly**, its phase list and steps 4–7. Distinctions carried over: the numbered steps are `1.`–`7.` at column zero with three-space-indented sub-bullets; the same-class-2 rule and the T-100 early-escalation branch are **duplicated across step 5 (Validate) and step 6 (Review)**, which is why a one-sided addition would be asymmetric; and the file's own idiom for mirroring a norm into step 6 is a reference line reading "the same instruction as step 5 applies here", used twice already.
- `.shell-team/todo.md` — **read directly** (its `## Status flags` section, its **empty** `## Active` section, and the most recent `## Done` entries). Distinctions carried over: the strict `- [ ] **T-NNN** <title> — `<FLAG>` — spec: <path>.md` line format, that annotations live on indented sub-bullets outside the linter's reach, and the recent precedent for disclosing a relayed source on a `- source:` sub-bullet.
- **The 2026-08-08 adjudication — RELAYED, not observed.** The coordinating session adjudicated the six candidates while recommending the v1.8.0 sprint plan, and the host GO'd that plan; the retro file itself records only the candidates and their tags, never a disposition. pm-spec was not present and has no way to read the planning exchange. Everything in this class is relayed: that candidates 2, 3, 4 and 5 are promoted; that candidate 1 is mechanise-only with its rule already partly present; that candidate 6 is promotion-**dropped** with its mechanisation internalised into issue **#188** as task **T-1049** and a re-evaluation trigger recorded; and that `99d4038` is `develop`'s tip. Assumptions lists each one for re-measurement at the freeze. If the adjudication differs from this relay in any particular, that is a scope question to raise **before** the freeze rather than a wording fix afterwards.

## Goal

<!-- BEGIN intent-block: T-1047 -->

`.shell-team/lessons.md` carries four new active entries — the 2026-08-08 retro's candidates 2, 3, 4 and 5, authored in English at promotion — appended at end of file and changing nothing that was already there: every byte of the corpus up to the base ref's last line is identical to the base blob, and no existing entry is superseded, retired, re-scoped or reworded. The four generated playbook blocks and their four consumer agent files are regenerated from the corpus and gain exactly the new `Rule` lines that each role's `Applies-to` routing entitles it to, and nothing else. Three prompt surfaces additionally gain exactly the hand-written text frozen below, each outside every marker region: `agents/pm-spec.md` gains one item at the end of its `## Spec completion self-check`; `skills/run/SKILL.md` gains one sub-bullet in step 5 and one in step 6, so the norm reaches both parallel surfaces rather than one; and `agents/codex-reviewer.md` gains one item in its `## Rules`, carrying a mode-scoping clause that names `## Finding-evaluation mode (review-response)`. The 2026-08-08 retro's candidates 1 and 6 do not enter the corpus. `bin/`, `tests/`, `.github/`, `docs/`, `agents/tech-lead.md`, `agents/` beyond the four block consumers and `agents/codex-reviewer.md`, `templates/` beyond the four generated blocks, `skills/` beyond `skills/run/SKILL.md`, every adopter-facing document, every other task's spec, and `.shell-team/retros/2026-08-08.md` itself are byte-identical to the base ref.

### Settled decisions

Each decision below is resolved; nothing here is left to implementation judgment. Every text this task ships is frozen byte for byte in this section, and every acceptance criterion reads its expected bytes **out of this file** rather than restating them — so a criterion cannot drift from the text it guards.

- **DP1 — the four new entries and the four hand-written mechanisation texts are frozen here, in full.** `bin/playbook-promote.sh` emits exactly the field order `Category`, `Applies-to`, `Scope`, `Status`, `Source`, `Rule`, `Why`, `How to apply` for a `loop`-scoped entry, with one blank line before each appended entry; the criteria assert the resulting **bytes**, not which tool produced them. The wording is frozen rather than left to engineer discretion because a `Rule` line is spliced verbatim into up to four shipped prompt blocks, and because a task whose subject is claim fidelity cannot leave its own claims to a later paraphrase. `Source` is the retro file for all four.

  Entry **E1** (retro candidate 2):

<!-- frozen-begin: E1 -->
```markdown
## 2026-08-08 — A verification-mechanism task's pre-commitment names the drop order before the first round, so the trigger executes a decision instead of opening one
- **Category**: process
- **Applies-to**: pm-spec, tech-lead
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-08.md
- **Rule**: A pre-commitment written for a task that builds or extends a verification mechanism states, before the first review round, not only the trigger but the disposition the trigger executes — which component is dropped first and why it is the droppable one, where the dropped part goes, and that the rounds' findings travel with it as its requirement list rather than being re-derived there. At the moment a trigger fires, one more patch is always the cheapest move available, so a trigger with no pre-decided disposition reopens the design question at exactly the point where nobody is placed to settle it.
- **Why**: A pre-commitment of this shape fired in practice for the first time: a newly built lock suite drew independent new cross-provider findings in two consecutive rounds, and because the splitting order had been fixed in advance — the droppable component named first — the split was carried out on the spot instead of a fourth round of point repairs, with both rounds' findings carried into the follow-up task as its requirement list. Several existing rules already prescribe the counting and the threshold; none of them prescribes the disposition, and the disposition is what the firing actually consumed. It was decided while the design was still cold, which is the only time it can be decided cheaply.
- **How to apply**: When a task builds or extends a checker, parser, lock or state tracker, write its pre-commitment in three parts: the factual trigger, the component dropped first with the reason it is the droppable one, and the destination that inherits the findings already collected. Whoever plans the task confirms all three exist before the first round rather than at the moment the trigger fires. When it fires, execute the recorded disposition, record the firing where the task's own history is kept, and treat a proposal to patch once more as the move the pre-commitment exists to refuse.
```
<!-- frozen-end: E1 -->

  Entry **E2** (retro candidate 3):

<!-- frozen-begin: E2 -->
```markdown
## 2026-08-08 — A repair closes the class of mutation the finding is an instance of, verified before the round closes
- **Category**: verification-discipline
- **Applies-to**: engineer, qa-verifier, tech-lead
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-08.md
- **Rule**: A fix for a review finding is written against the class of mutation the reported case is an instance of, not against the reported case: before the round closes, name the property of the input that makes the defect fire, enumerate the other inputs, orderings or spellings that share that property, confirm the repair closes those too, and report in the hand-off which variants were tried and what each did. A repair that plugs only the literally reproduced case leaves the class open, and the next round finds the same defect wearing a different input.
- **Why**: Two independent instances appeared in one cycle, in two unrelated tasks. A repair to an exemption rule blocked the exact literal the reviewer had demonstrated while four other spellings of the same exemption still bypassed it, which the following round found. In the other, an orientation sentence was corrected where it had been flagged while the identical defect stayed in neighbouring sentences, and three further rounds each corrected one more site. Both cost a full round to learn what one enumeration before closing would have shown, and neither had yet reached the second-occurrence threshold that switches the instruction to a full inventory — the first occurrence was already enough to see the class.
- **How to apply**: Before calling a review finding closed, restate it as a class and list the variants that share its triggering property, then run the repair against them and report the variants and their results in the hand-off, so the next gate audits the closure rather than the single case. Whoever relays a rework instruction asks for the class rather than the spot from the first occurrence, which is earlier than the same-class-2 rule fires and does not replace it. The verifying role treats a repair demonstrated only against the reported reproduction as an open finding rather than a closed one.
```
<!-- frozen-end: E2 -->

  Entry **E3** (retro candidate 4):

<!-- frozen-begin: E3 -->
```markdown
## 2026-08-08 — An obligation added to a shared region reuses the file's existing scoping idiom rather than inventing one
- **Category**: process
- **Applies-to**: engineer, pm-spec
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-08.md
- **Rule**: Before adding an obligation to a region that several modes, paths or callers share, read the file for the scoping idiom it already uses for exactly that situation — a sentence naming which mode a duty applies in and which one it does not — and reuse that shape, adjusting only the mode names and the direction words the new position requires. A duty written into a shared region with no such clause silently acquires every mode the region serves, including the ones that have no carrier for it, and the repair is almost never a new pattern to invent.
- **Why**: A new obligation was added to a shared preconditions block of an agent prompt, and in the mode where that role produces a different output contract there was nothing for the obligation to attach to. The same file already carried a self-scoping sentence written for precisely that split, a few lines from the edit, and a second one in another section. The cross-provider review raised it on the first round. The failure was not a missing convention but an unread local precedent, which is both the more basic defect and the more repeatable one, because a file's own precedents are invisible to anyone who read only the region being edited.
- **How to apply**: When a change adds a rule to a file with modal or shared regions, search that file for its existing scoping sentences before writing, and copy their shape rather than composing a new one. The spec states in which region and under which mode scope the new wording must sit, so its criterion asserts the placement and the scope rather than the wording's mere presence, and the verifying role reads for the scoping clause rather than for the obligation alone.
```
<!-- frozen-end: E3 -->

  Entry **E4** (retro candidate 5):

<!-- frozen-begin: E4 -->
```markdown
## 2026-08-08 — An append to a task's append-only record is written after re-reading the whole record, not after reading the last append
- **Category**: process
- **Applies-to**: engineer, pm-spec, qa-verifier
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-08.md
- **Rule**: A task whose deliverable is a record — a board entry, a spec's mutable prose, a review or provenance file that each round appends to — is re-read end to end before each new append, and an append that changes what an earlier line said says so explicitly instead of merely sitting after it. The defect this class produces is not a wrong new sentence but a new sentence contradicting an older one still standing above it: the record accumulates while attention stays on the tail, and no gate re-reads the head.
- **Why**: A task that ended with no product change at all, and five records as its entire output, drew four consecutive cross-provider rounds of findings — every one of them an internal contradiction between a later append and an earlier line of the same entry, and three of them against the immediately preceding repair. Each append was accurate about what had just happened and wrong about what the entry already said. A mechanism has tests that re-read it on every run; a record has only whoever opens it next, and in a records-only task nobody is scheduled to.
- **How to apply**: Before appending to a board entry, a spec's mutable sections or a review record, read the whole entry rather than the last block, and state in the append which earlier line it supersedes when it changes one. A task whose deliverables are mostly records says so in its own hand-off, so the reading is budgeted rather than skipped. The verifying role reads such a record end to end for the same reason and reports a contradiction between two of its own lines as a finding rather than as a wording nit.
```
<!-- frozen-end: E4 -->

  The hand-written item **M1**, appended as the **last** item of `agents/pm-spec.md`'s `## Spec completion self-check` list (DP5). It is one physical line, written in that list's own bolded-name style:

<!-- frozen-begin: M1 -->
```markdown
- **Relayed-premise re-measurement check (forward-only)**: before setting `READY_FOR_ARCH`, split every premise this spec rests on into what you measured yourself and what reached you from a coordinating layer — a routing map's counts and line anchors, another task's predicted check result, a quoted frozen constant, an adjudication's disposition — and re-read each measurable one at its primary source with your own file-reading tools, writing the measured value into the spec in place of the relayed wording. Where the source is not readable from this role at all (a planning conversation, an external tracker), record the premise in `## Assumptions` explicitly labelled **relayed**, name the side holding the primary confirmation, and require the freeze run to measure it and report the value beside it. A premise that reaches `READY_FOR_ARCH` neither measured nor labelled is what this item exists to catch, and a relayed premise you measure and find false is recorded as a finding about the hand-off — an interventions entry — rather than silently corrected in passing. This is the pm-spec-side checklist form of the discipline the tech-lead's hand-written `## Rules` section already states for the Routing Map; the corpus entry both halves derive from ships through the Lessons playbook below and is deliberately not restated here.
```
<!-- frozen-end: M1 -->

  The hand-written sub-bullet **M2A**, inserted in `skills/run/SKILL.md`'s **step 5 (Validate)**, indented three spaces to match that step's existing sub-bullets, immediately after the `- **Same-class-2 rule**:` sub-bullet and before the `- **Early escalation branch (T-100…)**:` one (DP6):

<!-- frozen-begin: M2A -->
```markdown
   - **Class-closure check before a finding is closed (fires on the first occurrence, distinct from the same-class-2 rule above)**: the same-class-2 rule above fires on the *second* occurrence of a class; this one fires on the *first*. When you relay a rework instruction for a `qa-verifier` finding, ask for the class rather than the spot — name the property of the input that makes the defect fire, require `engineer` to enumerate the other inputs, orderings or spellings sharing that property, and require the rework hand-off to report which variants were tried and what each did. A repair demonstrated only against the reported reproduction is not a closed finding: route it back rather than advancing, and count the occurrence for the same-class-2 rule exactly as before.
```
<!-- frozen-end: M2A -->

  The hand-written sub-bullet **M2B**, inserted in `skills/run/SKILL.md`'s **step 6 (Review)** at the matching position — immediately after that step's `- **Same-class-2 rule**:` sub-bullet and before its `- **Early escalation branch (T-100…)**:` one — in the file's own established mirroring idiom (DP6):

<!-- frozen-begin: M2B -->
```markdown
   - **Class-closure check before a finding is closed (fires on the first occurrence)**: the same instruction as step 5 applies here — relay a `codex-reviewer` finding as a class rather than as a spot from its first occurrence, require the enumeration of same-property variants and the per-variant result in the rework hand-off, and treat a repair demonstrated only against the reported reproduction as still open. This is orthogonal to the same-class-2 rule above, which counts occurrences cumulatively across both phases and switches to a full inventory at the second.
```
<!-- frozen-end: M2B -->

  The hand-written item **M3**, appended as the **last** bullet of `agents/codex-reviewer.md`'s `## Rules` section (DP7). It is one physical line, carrying the mode-scoping clause in the shape that section already uses:

<!-- frozen-begin: M3 -->
```markdown
- **Read a records-only deliverable end to end, every round (default / fresh-review mode only).** This item applies only to the default fresh-review pass; it does not apply in `## Finding-evaluation mode (review-response)` above, whose per-finding output contract judges findings someone else produced against a supplied PR diff and has no task record of its own to read. When the round's diff is mostly or entirely record prose — a board entry, a spec's mutable sections, a provenance or interventions file, a review record — read each such record from its first line rather than only the lines this round added, and check the new text against what the record already says. The defect this class produces is a later append contradicting an earlier line that no gate re-reads, so a diff-shaped reading cannot see it by construction. Report such a contradiction as a finding on the record, quoting the earlier line it contradicts, rather than as a wording nit.
```
<!-- frozen-end: M3 -->

- **DP2 — `Scope` and `Applies-to`, decided with reasons.** The retro's `[common]` / `[target-specific]` labels are input, not the decision, and `Scope` has only the two values `loop` and `maintainer`. All four are `loop`: none depends on a convention private to this repository, and a `maintainer` entry ships to no prompt, which would leave a promoted rule with no prevention surface at all. Every included role is justified individually, because each one widens a shipped prompt block by a line.
  - **E1 — `loop`, `pm-spec, tech-lead`.** This matches the three neighbouring pre-commitment entries (`2026-07-19` twice, `2026-07-20`), and for the same division of labour: `pm-spec` authors the pre-commitment inside the spec, `tech-lead` classifies the task's rigour at planning and is where the "verification-mechanism tasks run long" rule already lives. `engineer` is excluded with a reason rather than by omission: the engineer's action when a trigger fires is to stop patching and route, which the corpus already ships to it as `2026-07-12 — Two consecutive rounds of new Blocker/Major findings against a verification subsystem should trigger a redesign, not another patch`; E1 constrains what the pre-commitment must *contain* when it is written, which the engineer does not write. `qa-verifier` neither authors nor executes a pre-commitment.
  - **E2 — `loop`, `engineer, qa-verifier, tech-lead`.** `engineer` performs the repair and is the role the rule's first clause binds. `qa-verifier` is the gate positioned to re-run a repair against a variant fixture, and without it the rule has no enforcement surface at the moment it matters. `tech-lead` is included for a measured reason rather than by analogy: the corpus already ships `2026-07-12 — Rework instructions should require a batch verification grounded in the input's canonical contract, not a point-fix transcription` into the tech-lead's own block, whose trigger is a reviewer flagging a *verification gap*; shipping that without E2 leaves the narrower trigger looking like the whole rule. `pm-spec` is excluded: it neither writes nor verifies a rework repair, and its authored half is E1's subject.
  - **E3 — `loop`, `engineer, pm-spec`.** `engineer` makes the edit that acquires the wrong mode. `pm-spec` is the role that must say **in which region and under which mode scope** the new wording sits, which is what turns the rule into a checkable criterion — this task's own AC14 is that shape. `qa-verifier` is excluded with a stated reason: a criterion written per the rule already tells QA what to check, so adding the role to the block adds a line without adding an action. `tech-lead` authors no such edit. The retro tagged this `[target-specific]` because the incident was in one of this repository's own agent files; the rule's subject is a file's own scoping precedent, which any repository with modal or shared regions has, so the tag is input rather than verdict.
  - **E4 — `loop`, `engineer, pm-spec, qa-verifier`.** `engineer` appends to a board entry and a hand-off every round and is the highest-frequency appender. `pm-spec` appends to a spec's mutable sections across rework rounds and owns the record whose head goes unread longest. `qa-verifier` reads the board entry and the spec it verifies against, and is the in-loop gate that can report a record's self-contradiction; the cross-provider reviewer, which found the originating instances, is not an `Applies-to` token, which is exactly why M3 exists. `tech-lead` is excluded: it writes one routing map per task and no accumulating per-round record.
- **DP3 — the adjacency audit is in the body below, and its verdict is: no supersede, for any of the four.** Each new entry was compared against the corpus neighbours the retro names and against the entries in the same family that the corpus's own heading index turns up. Two rows exist specifically to resolve an apparent duplicate rather than a resemblance: E1 against the three existing pre-commitment entries, and E4 against `2026-08-06 — Frozen prose carrying no check line goes stale…`. No existing entry's `Status`, `Scope`, `Applies-to` or text changes anywhere in this task.
- **DP4 — no `Extended by` pointer is attached to any existing entry, and that is the default taken deliberately.** E4's nearest neighbour, `2026-08-06 — Frozen prose carrying no check line goes stale when what it describes moves…`, is the entry that would most naturally carry one; its text is frozen as the `N3` block of `.shell-team/specs/T-1039-promote-retro-2026-08-06.md`, so adding a bullet to the corpus copy would desynchronise that frozen block, and re-synchronising it is a class-B re-freeze of another task's merged spec plus a board-ledger update — a scope this task does not have, for a pointer `bin/check-playbook.sh` never schema-checks. The same applies to E1's neighbours, whose texts are frozen in earlier promotion specs. The adjacencies are recorded in the audit table instead.
- **DP5 — Part B‑1 is exactly one item at the end of `agents/pm-spec.md`'s `## Spec completion self-check`, and `agents/tech-lead.md` is not touched.** The retro's Try line asks for a checklist item in the "tech-lead / pm-spec prompt-construction procedure". The tech-lead half already exists — its hand-written `## Rules` carries the relay re-measurement rule as its last plain bullet — which the interventions record for this task states as a measured contradiction of the retro's premise. What is missing is the pm-spec side, and the established shape there is a bolded named item in the self-check list, several of them tagged `(forward-only)`; `M1` is written in that shape and placed last, after the population-count item. That section is hand-written: `templates/prompt-blocks/registry.txt` registers `agents/pm-spec.md` as a marker-mode consumer of `language.md` and `playbook-pm-spec.md` only, and names no section. AC12 measures that `M1` sits between the `## Spec completion self-check` heading and the `## Frozen intent freeze` heading, and that the file's first marker comes after both — so the placement is proved outside **every** marker region rather than outside a counted number of them. Re-stating the tech-lead's rule in `M1`'s own words is deliberately avoided: `M1` names the tech-lead section as the other half and points at the generated playbook line for the rule itself, because a hand-written second copy of a rule in the same file is the copy that goes stale.
- **DP6 — Part B‑2 lands on both parallel surfaces of the rework contract, and step 6 uses the file's own mirroring idiom rather than a second full statement.** The rework contract is not a prompt block: `skills/run/SKILL.md` appears in the registry only under `contain` rules, so it carries no marker pair, and the same-class-2 rule plus the T-100 early-escalation branch are duplicated by hand across step 5 (Validate) and step 6 (Review). Adding the class-closure norm to one step only would make the two asymmetric, so `M2A` goes in step 5 and `M2B` in step 6, each immediately after that step's same-class-2 sub-bullet. `M2B` is a reference line in the shape the file already uses twice ("the same instruction as step 5 applies here") rather than a second full statement: the file's own convention, and the form that cannot drift from `M2A` through an independent later edit. The parallel-surface symmetry table in the body records both cells as mirrored-now, and AC13 is its machine half — each step's region must contain its own frozen line exactly once and the other step's zero times, so a one-sided landing fails.
- **DP7 — Part B‑3 is one item at the end of `agents/codex-reviewer.md`'s `## Rules`, mode-scoped by reusing that file's existing idiom, and the direction word is `above`.** The file carries no marker pair at all (registry: `contain` rules only), so every section of it is hand-written. The obligation belongs in `## Rules`, where the file's other mandatory per-round review items live, and where the second shape of the self-scoping idiom already appears inline on a bolded item tagged `(default / fresh-review mode only)`. `M3` copies that shape. One detail is deliberate and is the difference between reusing an idiom and transcribing a sentence: the Preconditions instance of the idiom says the mode heading is **below** it, because Preconditions precedes that heading; `## Rules` follows it, so `M3` says **above**. AC14 asserts the placement, the ordering that makes `above` true, and that the frozen bytes actually contain the mode heading's name — so a scoping clause dropped in transcription fails rather than passing as prose.
- **DP8 — every accounting criterion below is base-relative or value-independent.** No criterion states a corpus size, a heading count, a block bullet count or a per-file line delta as a literal measured while drafting. The corpus's expected addition is computed from the frozen blocks' own line counts at run time (AC5); the untouched region is proved against the base blob's own line count (AC7); each role's expected delta is derived by reading each frozen entry's `Applies-to` and evaluating the routing rule (AC9, AC10, AC11). **Exactly one absolute literal survives, with its reason stated: AC8's assertion that the registry yields eight files** — four `playbook-*` blocks and their four consumers — a positive control against a registry read that silently produced nothing; it carries a `- stale-at:` trigger. Three further literals are structural rather than population counts and are exempt with reasons: the `9` line count each frozen entry must extract to, which is the field grammar `bin/playbook-promote.sh` emits for a `loop` entry; the four-role loop, which is `bin/check-playbook.sh`'s closed `KNOWN_ROLES` minus `all`; and the hand-written line counts `+1` (pm-spec, codex-reviewer) and `+2` (SKILL.md), which are consequences DP5–DP7 fix by decision rather than measurements of anything that grows.
- **DP9 — retro candidates 1 and 6 do not enter the corpus, and their absence is machine-checked rather than merely stated.** Candidate 1 is mechanise-only: its rule is already in the corpus as `2026-08-06 — A relayed premise is re-measured at its primary source before it is written into a downstream prompt`, so promoting it again would duplicate a shipped rule; what this task adds for it is `M1`. Candidate 6 (a vacuous frozen positive control) is **promotion-dropped with a recorded trigger, not silently deferred**: its mechanisation — a mini self-check that a newly frozen positive-control fixture actually reaches the logic its label names — is internalised as a requirement of issue #188, scheduled in this sprint as task T-1049, and the re-evaluation trigger is that it is promoted as a standalone lesson if a vacuous control recurs on a task other than #188. AC6 requires the set of `## ` headings the corpus gains against the base ref to be **exactly four** and exactly the keys of E1–E4, so no fifth entry, candidate-derived or otherwise, can hide in the diff.
- **DP10 — appends go through `bin/playbook-promote.sh`, one run per entry, in the order E1, E2, E3, E4; `bin/gen-playbook-blocks.sh` is run once afterwards; `M1`, `M2A`, `M2B` and `M3` are the only hand edits to shipped files.** That is the documented promotion path and its fail-closed re-validation runs `bin/check-playbook.sh` over a temp copy before the real file is written. No criterion can prove which tool produced a byte, so this is a procedural directive rather than an assertion: the criteria assert the resulting bytes and the file order. No generated block and no marker region is ever hand-edited.
- **DP11 — no surface beyond the three named ones is widened, and the omissions are stated rather than silent.** `agents/tech-lead.md` keeps its existing relay rule unedited (DP5). `agents/scrum-master.md`, `agents/drift-evaluator.md`, `agents/ui-designer.md` and `agents/triage-orchestrator.md` gain nothing: none is a playbook consumer and none composes a rework instruction or a task record of the kind E4 and M3 govern. `bin/` gains no checker for any of the four new rules — each is a judgment about what a human-or-agent-authored text must contain, and a text-shaped checker for it would be a verification mechanism, which is a task of its own rather than a rider on a promotion. The parallel-surface symmetry table records each of these as not-applicable with its reason.

## Non-goals

- **Promoting the 2026-08-08 retro's candidate 1 or candidate 6 into the corpus, in any form.** Their dispositions were adjudicated; an argument that either belongs here is a relitigation (pre-commitment R1). For candidate 1 it is additionally a duplicate of a shipped entry.
- **Candidate 6's mechanisation.** No mini self-check for a frozen positive control is designed, written or specified here; that requirement belongs to issue #188 / task T-1049. In particular **no fixture, lock or control anywhere in `tests/` is inspected, repaired or extended by this task**.
- **Any change to `agents/tech-lead.md`** — including its `## Rules`, its frontmatter, its `tools:` line and the interior of any of its marker regions — **except** the one generated `Rule` pointer per entry that `Applies-to` entitles its playbook block and marker region to. The relay rule already there is not reworded, re-scoped, moved or cross-referenced.
- **Any change to `agents/scrum-master.md`, `agents/drift-evaluator.md`, `agents/ui-designer.md`, `agents/triage-orchestrator.md`, `skills/goal/SKILL.md`, or any skill other than `skills/run/SKILL.md`.** Not one byte.
- **Any change under `bin/`, `tests/`, `.github/`, `docs/`, `README*`, `CHANGELOG*`, `CONTRIBUTING.md`, `CLAUDE.md`, or `templates/` other than the four generated playbook blocks.** No checker is written or extended, no CI step is added, and `templates/prompt-blocks/registry.txt` is unchanged — every hand edit here lands in a file the registry either does not govern by marker or does not govern at all.
- **Editing `.shell-team/retros/2026-08-08.md`.** Per-cycle artifacts are immutable.
- **Any edit to, or re-freeze of, any other task's spec** — `T-1039`, `T-1044`, `T-1045`, `T-1046` and every other merged spec included. No `intent-hash`, `intent-ratified` or `freeze-attestation` line belonging to another task is added, removed or altered, and no criterion of another task is re-derived, re-based or widened so that it reads green again.
- **Superseding, retiring, re-scoping or rewording any existing corpus entry, or attaching an `Extended by` pointer to one** (DP3, DP4). The `## Format` section is byte-unchanged and the corpus is a pure append.
- **Work on #167, #188 or any other open issue.** No design for the ignored-base question, the scratch-root-hygiene lock redesign or any other backlog item is produced, proposed or partially landed here.
- **Reducing the size of any generated playbook block.** Two of the four are already past the generator's non-fatal 40-line warning threshold and both grow here; the standing deferral forbids launching a shortening or supersede sweep on that signal alone.
- **Any `.ja.md` counterpart.** Every file this task writes is agent-read; the repository's language rule gives them no translated copy.
- **Re-opening any candidate's disposition, or the wording of any frozen text here.** Adjudicated at planning; see R1.

## Acceptance criteria

Base ref for every `git` anchor is the literal `99d4038`. Every check runs from the repository root, names every file it reads, asserts readability before any negative grep, and writes nothing outside a `mktemp -d` scratch directory created with the guarded idiom. **Every criterion that pins frozen text reads its expected bytes out of this spec's own `<!-- frozen-begin: X -->` / `<!-- frozen-end: X -->` fenced blocks** — the fence lines are stripped with `sed '1d;$d'` — rather than restating them inside a shell quote; that is deliberate, since every one of these texts contains apostrophes, backticks and em-dashes that no single shell quoting style survives intact. Per DP8, no criterion states a count that was measured while drafting.

- [ ] **AC1** Entry **E1** is present in `.shell-team/lessons.md` exactly as frozen: the region from its heading to the line before the next `## ` heading, with trailing blank lines dropped, is byte-identical to the `E1` frozen block (heading plus eight field bullets — the shape `bin/playbook-promote.sh` emits for a `loop` entry), and its `Applies-to` is `pm-spec, tech-lead` and its `Scope` is `loop`, asserted as explicit clauses so DP2's routing decision is machine-visible. Positive control: the extracted block is non-empty and carries a `- **Rule**: ` bullet, so an extraction failure cannot read as a clean pass.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047a.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: E1 -->' -v e='<!-- frozen-end: E1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: pm-spec, tech-lead' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC2** Entry **E2** is present in `.shell-team/lessons.md` exactly as frozen, same extraction and same nine-line shape as AC1, and its `Applies-to` is `engineer, qa-verifier, tech-lead` — the three-role decision DP2 justified role by role, asserted here so a silent narrowing to two roles fails rather than passing quietly.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047b.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: E2 -->' -v e='<!-- frozen-end: E2 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: engineer, qa-verifier, tech-lead' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC3** Entry **E3** is present in `.shell-team/lessons.md` exactly as frozen, same extraction and same nine-line shape, with `Applies-to: engineer, pm-spec` and `Scope: loop` asserted as explicit clauses (DP2) — so a widening to qa-verifier or tech-lead, which DP2 rejected with reasons, fails here rather than shipping unnoticed into two more prompt blocks, and a downgrade to `maintainer`, which the retro's `[target-specific]` tag would suggest and DP2 rejected, fails with it.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047c.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: E3 -->' -v e='<!-- frozen-end: E3 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: engineer, pm-spec' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC4** Entry **E4** is present in `.shell-team/lessons.md` exactly as frozen, same extraction and same nine-line shape, with `Applies-to: engineer, pm-spec, qa-verifier` and `Scope: loop` asserted as explicit clauses (DP2).
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047d.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: E4 -->' -v e='<!-- frozen-end: E4 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: engineer, pm-spec, qa-verifier' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC5** The whole corpus is schema-valid — `bin/check-playbook.sh` green over the resolved lessons path, which is also what proves no pointer graph was disturbed — and its diff against the base ref adds exactly the number of lines the four frozen blocks account for and **deletes exactly zero**. Per DP8 the expected addition is **not** a literal: it is computed at run time as the sum of each frozen block's own line count plus one blank separator each, so the criterion cannot go stale against its own frozen text. Positive controls: each block must extract to nine lines, and the computed expectation must be greater than zero.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; test -r "$SP" || exit 1; L=$(bash bin/team-paths.sh --get lessons) || exit 1; test -n "$L" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047e.XXXXXX") || exit 1; bash bin/check-playbook.sh "$L" >/dev/null || rc=1; want=0; for k in E1 E2 E3 E4; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; n=$(grep -c . "$T/$k" || true); test "$n" = "9" || rc=1; want=$((want + n + 1)); done; test "$want" -gt 0 || rc=1; ns=$(git diff --numstat 99d4038...HEAD -- "$L"); test -n "$ns" || rc=1; add=$(printf '%s\n' "$ns" | awk '{print $1}'); del=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$add" = "$want" || rc=1; test "$del" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC6** Retro candidates 1 and 6 are genuinely absent (DP9): the set of `## ` headings the corpus gained since the base ref is **exactly four**, and is exactly the keys of E1, E2, E3 and E4, compared as sets with `comm -13` rather than by counting alone. This criterion is **merge-point-scoped and expected to go stale once later work lands on `99d4038`**; do not widen its base-ref resolution and do not re-derive it per rework round. Positive control: the base blob's heading list must be non-empty, so an unreadable base blob cannot read as "nothing was added".
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047f.XXXXXX") || exit 1; git show 99d4038:.shell-team/lessons.md > "$T/base" || rc=1; test -s "$T/base" || rc=1; grep '^## ' "$T/base" | sort > "$T/hb"; grep '^## ' "$L" | sort > "$T/hn"; test -s "$T/hb" || rc=1; test -s "$T/hn" || rc=1; comm -13 "$T/hb" "$T/hn" > "$T/hadd"; test "$(grep -c . "$T/hadd" || true)" = "4" || rc=1; for k in E1 E2 E3 E4; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' | head -n 1 > "$T/k$k"; test -s "$T/k$k" || rc=1; grep -qxFf "$T/k$k" "$T/hadd" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC7** The corpus change is a **pure append and nothing else**, proved at the byte level rather than by counting: the first *N* lines of the current file, *N* being the base blob's own line count read at run time, are byte-identical to the base blob; and everything after line *N* is exactly a blank line followed by E1, a blank line followed by E2, a blank line followed by E3, and a blank line followed by E4, in that order. This is the criterion that forecloses a supersede, a reword, a re-scope, an `Extended by` pointer or a `## Format` edit anywhere in the file (DP3, DP4, Non-goals), and it holds no absolute literal at all. Merge-point-scoped, as AC6. Positive controls: the base blob must be non-empty, *N* must be greater than zero, and the constructed expectation must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047g.XXXXXX") || exit 1; git show 99d4038:.shell-team/lessons.md > "$T/base" || rc=1; test -s "$T/base" || rc=1; bl=$(grep -c '' "$T/base" || true); test "$bl" -gt 0 || rc=1; head -n "$bl" "$L" > "$T/pre"; cmp -s "$T/base" "$T/pre" || rc=1; tail -n +"$((bl + 1))" "$L" > "$T/tail"; : > "$T/exp"; for k in E1 E2 E3 E4; do printf '\n' >> "$T/exp"; awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' >> "$T/exp"; done; test -s "$T/exp" || rc=1; cmp -s "$T/exp" "$T/tail" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC8** The committed generated blocks and their consumers are exactly what the corpus regenerates: the eight files the registry names for the four `playbook-*` blocks are copied into a scratch tree, `bin/gen-playbook-blocks.sh` is run against it, and every one of the eight compares byte-identical (the CI dogfood step's own procedure, re-run here); `bin/check-prompt-sync.sh` is green over the whole registry, which is also what asserts every marker region in every consumer still matches its canonical block **and** that the `contain`-mode obligations on `agents/codex-reviewer.md` and `skills/run/SKILL.md` survived their hand edits. Positive control, and the one absolute literal DP8 permits: the registry must yield exactly eight files, so a registry read that produced nothing cannot pass. Expect a non-fatal block-size warning on stderr for two roles — it is not a failure (see Assumptions).
  - check: rc=0; test -r templates/prompt-blocks/registry.txt || exit 1; L=$(bash bin/team-paths.sh --get lessons) || exit 1; test -n "$L" || exit 1; test -r "$L" || exit 1; S=$(mktemp -d "${TMPDIR:-/tmp}/t1047h.XXXXXX") || exit 1; files=$(awk '$1 == "marker" && $2 ~ /^playbook-/ { for (i = 3; i <= NF; i++) print $i; print "templates/prompt-blocks/" $2 }' templates/prompt-blocks/registry.txt | sort -u); test "$(printf '%s\n' "$files" | grep -c . || true)" = "8" || rc=1; for f in $files; do mkdir -p "$S/$(dirname "$f")" || rc=1; cp "$f" "$S/$f" || rc=1; done; mkdir -p "$S/templates/prompt-blocks" || rc=1; cp templates/prompt-blocks/registry.txt "$S/templates/prompt-blocks/registry.txt" || rc=1; bash bin/gen-playbook-blocks.sh --root "$S" --lessons "$L" >/dev/null 2>&1 || rc=1; for f in $files; do cmp -s "$f" "$S/$f" || rc=1; done; rm -rf "$S"; bash bin/check-prompt-sync.sh >/dev/null || rc=1; test "$rc" -eq 0
  - stale-at: a task registers or removes a `playbook-<role>` marker rule in `templates/prompt-blocks/registry.txt`, at which point the declared eight-file population no longer describes the registry this criterion reads and the literal must be re-measured against it.
- [ ] **AC9** The propagation matrix is exactly what DP2 decided, in both the canonical block and its consumer agent file, and the expectation is **derived rather than transcribed**: for each of the four IN roles and each of the four frozen entries, the criterion reads that entry's own `Applies-to` field out of this spec, evaluates the routing rule (`all`, or the role named in the comma list), and requires the entry's `Rule` line to appear exactly once in `templates/prompt-blocks/playbook-<role>.md` and exactly once in `agents/<role>.md` when it qualifies, and exactly zero times in each when it does not. A block regenerated without being spliced into its consumer, or a consumer spliced without its block being regenerated, both fail. The four-role loop is `bin/check-playbook.sh`'s closed `KNOWN_ROLES` set minus `all`, not a population that grows, so it carries no re-measurement trigger. Positive controls: each frozen block must be non-empty, and each entry's `Applies-to` and `Rule` values must extract non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; test -r "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047i.XXXXXX") || exit 1; for k in E1 E2 E3 E4; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test -s "$T/$k" || rc=1; done; for r in engineer qa-verifier tech-lead pm-spec; do BF="templates/prompt-blocks/playbook-$r.md"; AF="agents/$r.md"; test -r "$BF" || rc=1; test -r "$AF" || rc=1; for k in E1 E2 E3 E4; do ap=$(sed -n 's/^- \*\*Applies-to\*\*: //p' "$T/$k"); test -n "$ap" || rc=1; case "$ap" in all) x=1 ;; *) case ",${ap// /}," in *",$r,"*) x=1 ;; *) x=0 ;; esac ;; esac; ru=$(sed -n 's/^- \*\*Rule\*\*: //p' "$T/$k"); test -n "$ru" || rc=1; n=$(grep -cF -- "$ru" "$BF" || true); m=$(grep -cF -- "$ru" "$AF" || true); test "$n" = "$x" || rc=1; test "$m" = "$x" || rc=1; done; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC10** The four generated blocks moved by exactly the derived amount and nothing else: for each role, the expected delta is computed at run time from the four frozen blocks' `Applies-to` fields, and then `git diff --numstat 99d4038` for that block must show exactly that many lines added and **exactly zero deleted**, while the block's `^- ` bullet count must equal the base blob's own bullet count plus the same derived delta. A deletion count of zero is what proves no existing bullet was modified, since a modification renders as one addition plus one deletion. Merge-point-scoped, as AC6. Positive controls: each numstat row must be non-empty, each base blob must be non-empty, and each derived delta must be at least one (every one of the four roles gains at least one entry under DP2's routing).
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; test -r "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047j.XXXXXX") || exit 1; for k in E1 E2 E3 E4; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test -s "$T/$k" || rc=1; done; for r in engineer qa-verifier tech-lead pm-spec; do BF="templates/prompt-blocks/playbook-$r.md"; test -r "$BF" || rc=1; d=0; for k in E1 E2 E3 E4; do ap=$(sed -n 's/^- \*\*Applies-to\*\*: //p' "$T/$k"); test -n "$ap" || rc=1; case "$ap" in all) d=$((d + 1)) ;; *) case ",${ap// /}," in *",$r,"*) d=$((d + 1)) ;; esac ;; esac; done; test "$d" -ge 1 || rc=1; ns=$(git diff --numstat 99d4038...HEAD -- "$BF"); test -n "$ns" || rc=1; a=$(printf '%s\n' "$ns" | awk '{print $1}'); dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$a" = "$d" || rc=1; test "$dl" = "0" || rc=1; git show "99d4038:$BF" > "$T/b" || rc=1; test -s "$T/b" || rc=1; bc=$(grep -c '^- ' "$T/b" || true); cc=$(grep -c '^- ' "$BF" || true); test "$cc" = "$((bc + d))" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC11** The four consumer agent files moved by exactly the derived amount and nothing else: same run-time derivation as AC10, and `git diff --numstat 99d4038` for each `agents/<role>.md` must show exactly the derived delta lines added — **plus exactly one more for `agents/pm-spec.md` alone**, that one being `M1` (DP5) — and **exactly zero deleted** in every case. The pm-spec `+1` is stated as a routing consequence rather than a measurement: it is the count of hand-written items this task adds to that file, which DP5 fixes at one. This is also the mechanical half of the Non-goal forbidding any other change to `agents/tech-lead.md`: its added-line count must equal its derived delta with nothing extra. Merge-point-scoped, as AC6. Positive control: each numstat row must be non-empty, so an untouched file fails rather than passing silently.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; test -r "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047k.XXXXXX") || exit 1; for k in E1 E2 E3 E4; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test -s "$T/$k" || rc=1; done; for r in engineer qa-verifier tech-lead pm-spec; do AF="agents/$r.md"; test -r "$AF" || rc=1; d=0; for k in E1 E2 E3 E4; do ap=$(sed -n 's/^- \*\*Applies-to\*\*: //p' "$T/$k"); test -n "$ap" || rc=1; case "$ap" in all) d=$((d + 1)) ;; *) case ",${ap// /}," in *",$r,"*) d=$((d + 1)) ;; esac ;; esac; done; if [ "$r" = "pm-spec" ]; then d=$((d + 1)); fi; test "$d" -ge 1 || rc=1; ns=$(git diff --numstat 99d4038...HEAD -- "$AF"); test -n "$ns" || rc=1; a=$(printf '%s\n' "$ns" | awk '{print $1}'); dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$a" = "$d" || rc=1; test "$dl" = "0" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC12** Part B‑1 is where DP5 put it: `M1` extracts to exactly one line, occurs **exactly once** in `agents/pm-spec.md`, and occurs exactly once inside the region running from that file's `## Spec completion self-check` heading to the line before its `## Frozen intent freeze` heading — and that whole region lies before the file's **first** `<!-- BEGIN prompt-block:` marker, which is what proves `M1` sits outside **every** marker region rather than outside a counted number of them. The set of prompt-block marker names in the file is byte-identical to the set in the base blob, so a region cannot have been added, removed or renamed. Positive controls: each of the two headings must occur exactly once, at least one marker must be found, the first marker must follow the second heading, and the extracted region must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; A=agents/pm-spec.md; test -r "$SP" || exit 1; test -r "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047l.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: M1 -->' -v e='<!-- frozen-end: M1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/m"; test "$(grep -c . "$T/m" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/m" "$A" || true)" = "1" || rc=1; grep -n '^## Spec completion self-check$' "$A" > "$T/h1" || rc=1; test "$(grep -c . "$T/h1" || true)" = "1" || rc=1; grep -n '^## Frozen intent freeze' "$A" > "$T/h2" || rc=1; test "$(grep -c . "$T/h2" || true)" = "1" || rc=1; s=$(cut -d: -f1 < "$T/h1"); f=$(cut -d: -f1 < "$T/h2"); test -n "$s" || rc=1; test -n "$f" || rc=1; test "${s:-0}" -lt "${f:-0}" || rc=1; grep -n '^<!-- BEGIN prompt-block: ' "$A" > "$T/mk" || rc=1; test "$(grep -c . "$T/mk" || true)" -ge 1 || rc=1; fm=$(head -n 1 "$T/mk" | cut -d: -f1); test -n "$fm" || rc=1; test "${f:-0}" -lt "${fm:-0}" || rc=1; sed -n "$((s + 1)),$((f - 1))p" "$A" > "$T/sec"; test -s "$T/sec" || rc=1; test "$(grep -cxFf "$T/m" "$T/sec" || true)" = "1" || rc=1; git show "99d4038:$A" > "$T/b" || rc=1; test -s "$T/b" || rc=1; sed -n 's/^<!-- BEGIN prompt-block: \(.*\) -->$/\1/p' "$T/b" | sort > "$T/n0"; sed -n 's/^<!-- BEGIN prompt-block: \(.*\) -->$/\1/p' "$A" | sort > "$T/n1"; test -s "$T/n0" || rc=1; cmp -s "$T/n0" "$T/n1" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC13** Part B‑2 reached **both** parallel surfaces and neither one only — the machine half of DP6's symmetry table. In `skills/run/SKILL.md`: `M2A` and `M2B` each extract to exactly one line and each occurs exactly once in the whole file; the step-5 region (from the `5. **Validate**` line to the line before the `6. **Review**` line) contains `M2A` exactly once and `M2B` exactly zero times; the step-6 region (from the `6. **Review**` line to the line before the `7. **Done**` line) contains `M2B` exactly once and `M2A` exactly zero times; the file's diff against the base ref adds **exactly two lines and deletes exactly zero**, that count being DP6's decision rather than a measurement; and the file carries the same number of `<!-- BEGIN prompt-block:` markers as the base blob, which is what proves the edits stayed in hand-written prose in a file the registry governs only by `contain` rules. Positive controls: each of the three step-anchor lines must occur exactly once, their line numbers must be strictly increasing, both extracted regions must be non-empty, and the numstat row must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; K=skills/run/SKILL.md; test -r "$SP" || exit 1; test -r "$K" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047m.XXXXXX") || exit 1; for k in M2A M2B; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test "$(grep -c . "$T/$k" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/$k" "$K" || true)" = "1" || rc=1; done; l5=$(grep -n '^5\. \*\*Validate\*\*' "$K" | head -n 1 | cut -d: -f1); l6=$(grep -n '^6\. \*\*Review\*\*' "$K" | head -n 1 | cut -d: -f1); l7=$(grep -n '^7\. \*\*Done\*\*' "$K" | head -n 1 | cut -d: -f1); test "$(grep -c '^5\. \*\*Validate\*\*' "$K" || true)" = "1" || rc=1; test "$(grep -c '^6\. \*\*Review\*\*' "$K" || true)" = "1" || rc=1; test "$(grep -c '^7\. \*\*Done\*\*' "$K" || true)" = "1" || rc=1; test -n "$l5" || rc=1; test -n "$l6" || rc=1; test -n "$l7" || rc=1; test "${l5:-0}" -lt "${l6:-0}" || rc=1; test "${l6:-0}" -lt "${l7:-0}" || rc=1; sed -n "$((l5 + 1)),$((l6 - 1))p" "$K" > "$T/s5"; sed -n "$((l6 + 1)),$((l7 - 1))p" "$K" > "$T/s6"; test -s "$T/s5" || rc=1; test -s "$T/s6" || rc=1; test "$(grep -cxFf "$T/M2A" "$T/s5" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/M2B" "$T/s5" || true)" = "0" || rc=1; test "$(grep -cxFf "$T/M2B" "$T/s6" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/M2A" "$T/s6" || true)" = "0" || rc=1; ns=$(git diff --numstat 99d4038...HEAD -- "$K"); test -n "$ns" || rc=1; a=$(printf '%s\n' "$ns" | awk '{print $1}'); dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$a" = "2" || rc=1; test "$dl" = "0" || rc=1; git show "99d4038:$K" > "$T/b" || rc=1; test -s "$T/b" || rc=1; c0=$(grep -c '^<!-- BEGIN prompt-block: ' "$T/b" || true); c1=$(grep -c '^<!-- BEGIN prompt-block: ' "$K" || true); test "$c0" = "$c1" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC14** Part B‑3 is where DP7 put it, **with its mode scope intact** — the criterion asserts the region and the scope, not merely that the wording exists. In `agents/codex-reviewer.md`: `M3` extracts to exactly one line, occurs exactly once in the file, and occurs exactly once inside the region running from the `## Rules` heading to end of file; the `## Finding-evaluation mode (review-response)` heading occurs exactly once and its line number is **less** than the `## Rules` heading's, which is what makes the frozen text's direction word `above` true; `M3`'s own frozen bytes contain that heading's name, so a scoping clause dropped in transcription fails here rather than passing as prose; the file carries the same number of `<!-- BEGIN prompt-block:` markers as the base blob; and the file's diff against the base ref adds **exactly one line and deletes exactly zero**, that count being DP7's decision rather than a measurement. Positive controls: both headings must occur exactly once, the `## Rules` region must be non-empty, and the numstat row must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; A=agents/codex-reviewer.md; test -r "$SP" || exit 1; test -r "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1047n.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: M3 -->' -v e='<!-- frozen-end: M3 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/m"; test "$(grep -c . "$T/m" || true)" = "1" || rc=1; grep -qF -- '## Finding-evaluation mode (review-response)' "$T/m" || rc=1; test "$(grep -cxFf "$T/m" "$A" || true)" = "1" || rc=1; test "$(grep -c '^## Finding-evaluation mode (review-response)$' "$A" || true)" = "1" || rc=1; test "$(grep -c '^## Rules$' "$A" || true)" = "1" || rc=1; fm=$(grep -n '^## Finding-evaluation mode (review-response)$' "$A" | head -n 1 | cut -d: -f1); rl=$(grep -n '^## Rules$' "$A" | head -n 1 | cut -d: -f1); test -n "$fm" || rc=1; test -n "$rl" || rc=1; test "${fm:-0}" -lt "${rl:-0}" || rc=1; tail -n +"$((rl + 1))" "$A" > "$T/sec"; test -s "$T/sec" || rc=1; test "$(grep -cxFf "$T/m" "$T/sec" || true)" = "1" || rc=1; git show "99d4038:$A" > "$T/b" || rc=1; test -s "$T/b" || rc=1; c0=$(grep -c '^<!-- BEGIN prompt-block: ' "$T/b" || true); c1=$(grep -c '^<!-- BEGIN prompt-block: ' "$A" || true); test "$c0" = "$c1" || rc=1; ns=$(git diff --numstat 99d4038...HEAD -- "$A"); test -n "$ns" || rc=1; a=$(printf '%s\n' "$ns" | awk '{print $1}'); dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$a" = "1" || rc=1; test "$dl" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC15** The board carries this task's entry in the strict line format and nothing on it was disturbed: `bin/check-handoff.sh` green over the resolved board, `bin/check-board-headings.sh <board> --base 99d4038` green (a heading replacement or deletion is caught here rather than at review), `bin/check-intent.sh` reporting aligned for this spec against that board, the board's `git diff --numstat` deletion column exactly **zero** — so no other task's ledger line, flag or prose was replaced — and exactly one board line matching `- [<mark>] **T-1047** <title> — `<FLAG>` — spec: .shell-team/specs/T-1047-promote-retro-2026-08-08.md`. Merge-point-scoped, as AC6. Positive control: the numstat row must be non-empty, so an untouched board fails.
  - check: rc=0; SP=.shell-team/specs/T-1047-promote-retro-2026-08-08.md; test -r "$SP" || exit 1; B=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$B" || exit 1; test -r "$B" || exit 1; bash bin/check-handoff.sh "$B" >/dev/null || rc=1; bash bin/check-board-headings.sh "$B" --base 99d4038 >/dev/null || rc=1; bash bin/check-intent.sh "$SP" "$B" >/dev/null || rc=1; ns=$(git diff --numstat 99d4038...HEAD -- "$B"); test -n "$ns" || rc=1; del=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$del" = "0" || rc=1; n=$(grep -cE '^- \[[ x]\] \*\*T-1047\*\* .+ — .[A-Z_]+. — spec: \.shell-team/specs/T-1047-promote-retro-2026-08-08\.md$' "$B" || true); test "$n" = "1" || rc=1; test "$rc" -eq 0
- [ ] **AC16** Scope, records and hygiene lock. The branch's changed-and-added file set (tracked changes ∪ untracked additions, since some records are untracked until they are written) contains the thirteen required files and **nothing** outside the sixteen-path allow-list in Notes for engineer — so `bin/`, `tests/`, `.github/`, `docs/`, `templates/prompt-blocks/registry.txt`, `skills/goal/SKILL.md`, `CONTRIBUTING.md`, `CLAUDE.md`, every other agent file, every other task's spec and the retro file itself are all byte-identical by construction; `.shell-team/provenance/T-1047.md` and `.shell-team/interventions/T-1047.md` both exist and are conformant under their own checkers; and the branch diff is clean under `bin/check-pii-shapes.sh`. Merge-point-scoped, as AC6 — once later work lands, `99d4038` no longer describes this task's merge point. Positive control: an empty changed-set fails.
  - check: rc=0; changed=$( { git diff --name-only 99d4038...HEAD; git ls-files --others --exclude-standard; } | sort -u ); test -n "$changed" || rc=1; for f in .shell-team/lessons.md templates/prompt-blocks/playbook-engineer.md templates/prompt-blocks/playbook-qa-verifier.md templates/prompt-blocks/playbook-tech-lead.md templates/prompt-blocks/playbook-pm-spec.md agents/engineer.md agents/qa-verifier.md agents/tech-lead.md agents/pm-spec.md agents/codex-reviewer.md skills/run/SKILL.md .shell-team/specs/T-1047-promote-retro-2026-08-08.md .shell-team/todo.md; do printf '%s\n' "$changed" | grep -qxF -- "$f" || rc=1; done; extra=$(printf '%s\n' "$changed" | grep -vxF -e '.shell-team/lessons.md' -e 'templates/prompt-blocks/playbook-engineer.md' -e 'templates/prompt-blocks/playbook-qa-verifier.md' -e 'templates/prompt-blocks/playbook-tech-lead.md' -e 'templates/prompt-blocks/playbook-pm-spec.md' -e 'agents/engineer.md' -e 'agents/qa-verifier.md' -e 'agents/tech-lead.md' -e 'agents/pm-spec.md' -e 'agents/codex-reviewer.md' -e 'skills/run/SKILL.md' -e '.shell-team/specs/T-1047-promote-retro-2026-08-08.md' -e '.shell-team/todo.md' -e '.shell-team/provenance/T-1047.md' -e '.shell-team/reviews/T-1047.md' -e '.shell-team/interventions/T-1047.md' | grep -c . || true); test "$extra" = "0" || rc=1; if [ -r .shell-team/provenance/T-1047.md ]; then bash bin/check-provenance.sh .shell-team/provenance/T-1047.md >/dev/null || rc=1; else rc=1; fi; if [ -r .shell-team/interventions/T-1047.md ]; then bash bin/check-interventions.sh --task T-1047 -- .shell-team/interventions/T-1047.md >/dev/null || rc=1; else rc=1; fi; bash bin/check-pii-shapes.sh --base 99d4038 >/dev/null || rc=1; test "$rc" -eq 0
- [ ] **AC17** Runtime, `SKIP` by design — no command can prove that a command was run, or that a claim was checked against its source. Five things are done and reported in the board hand-off. **(a)** Every applicable step in `.github/workflows/check-handoff.yml` was run locally, in workflow order, with each step declared inapplicable named individually together with its reason — and, per the corpus's `2026-08-04` byte-identity entry, the "everything else is byte-identical" rationale is **split**: any step that enumerates a directory or a glob is **run** rather than reasoned about, because this task adds new files under `.shell-team/provenance/` and `.shell-team/reviews/`. **(b)** A producer-run mutation self-check on scratch copies outside the checkout, each mutation observed red then restored and observed green: change one word inside E1's `Rule` in the corpus (AC1 red, AC7 red, AC9 red on its two roles, AC10 red); change E2's `Applies-to` to drop `tech-lead` and regenerate (AC2 red, AC9 red on the tech-lead cells, AC10/AC11 red on the tech-lead delta); insert one of the four entries in the **middle** of the corpus instead of at the end (AC7 red while AC1–AC6 may stay green — this is the probe aimed at AC7's own blind spot); append a fifth entry (AC5 red, AC6 red, AC7 red); hand-edit one bullet of a generated block (AC8 red, AC10 red on that file's deletion column); move `M1` below the first prompt-block marker in `agents/pm-spec.md` (AC8 red, AC12 red); add `M2A` to step 5 but not `M2B` to step 6 (AC13 red — the one-sided-landing probe, which is the symmetry table's whole point); place `M3` in `## Preconditions` instead of `## Rules` (AC14 red); delete the mode-scoping clause from `M3` in the file while leaving it in this spec (AC14 red). **(c)** Per the corpus's `2026-08-04` summary-fidelity entry, every clause of the four new entries' `Why` fields was read back against the records the retro names for that candidate — `.shell-team/interventions/T-1044.md`, `.shell-team/reviews/T-1044.md`, `.shell-team/reviews/T-1045.md`, `.shell-team/reviews/T-1046.md` and the `T-1044` / `T-1046` board entries, none of which pm-spec opened (see `## Summarized sources`) — and the hand-off states, per entry, which clauses were confirmed against a record and which were generalised deliberately, including whether the retro's "two independent instances in two unrelated tasks" (candidate 3) and its explicit "distinct subclass from the `2026-08-06` frozen-prose entry" (candidate 5) survive the generalisation. **(d)** Per the corpus's `2026-08-04` relayed-claim entry, every relayed claim listed in Assumptions was re-measured before the freeze and the hand-off states the measured value beside each. **(e)** The `## Summarized sources` per-source report: for each named source, whether the reporting role opened it, and what the source said about the distinction it is cited for — with the adjudication's relayed-not-observed status restated rather than quietly dropped.

## Input space

**Reachable input classes** — what the criteria above really read, and what the edit must handle correctly:

- `.shell-team/lessons.md` as committed at the base ref: a UTF-8 markdown file whose canonical `## YYYY-MM-DD — <title>` entry set is **re-derived by every criterion from the base blob rather than declared here** (DP8 applies to this section too). It contains at least one `superseded` entry with a resolving `Superseded-by`, several `maintainer`-scoped entries carrying a `Bound-in` bullet, and at least one `active` entry carrying an `Extended by` bullet; field values routinely contain apostrophes, backticks, em-dashes, double quotes, parentheses and angle-bracket placeholders; one fenced ```` ```markdown ```` example under `## Format` carries a synthetic `## YYYY-MM-DD — <short title>` heading that must stay invisible to both the schema checker and the block generator, and the `Superseded-by` / `Extended by` bullets are documented outside that fenced example on purpose.
- The four appended entries, appended at end of file with one blank line before each, and no `Bound-in` bullet on any of them because all four are `loop`-scoped. No line anywhere else in the file changes. All four frozen blocks carry the fields in `bin/playbook-promote.sh`'s emission order, so a tool-produced append and the frozen bytes agree by construction.
- `templates/prompt-blocks/registry.txt` as committed, unchanged by this task: four `marker`-mode `playbook-*` rules naming one consumer each, `marker` rules for `language.md` and `careful-execution.md`, and `contain` rules whose consumers include `agents/codex-reviewer.md` and `skills/run/SKILL.md` — neither of which appears under any `marker` rule, so neither carries a marker pair.
- `agents/pm-spec.md` as committed: YAML frontmatter, hand-written prose including a `## Rules` section and a `## Spec completion self-check` list of bolded named items each on one physical line, then `## Frozen intent freeze (v0.3.0 Phase A)`, then two prompt-block marker pairs. Exactly two lines are added — one inside the `playbook-pm-spec` region by the generator, one at the end of the self-check list by hand — and none is removed.
- `agents/codex-reviewer.md` as committed: no prompt-block marker pair at all; `## Preconditions` carrying one shape of the self-scoping idiom; `## Output` carrying a fenced verdict template; `## Finding-evaluation mode (review-response)`; and `## Rules` running to end of file, whose bullets include two that already carry an inline mode-scope clause and two written in Japanese. Exactly one line is added at the end of `## Rules`; none is removed.
- `skills/run/SKILL.md` as committed: a numbered phase list `1.`–`7.` at column zero, sub-bullets indented three spaces, fenced bash blocks between steps, and steps 5 and 6 each carrying a `- **Same-class-2 rule**:` sub-bullet and a `- **Early escalation branch (T-100…)**:` sub-bullet. Exactly two lines are added, one in each step's region; none is removed.
- The other three consumer agent files, each carrying exactly one `playbook-<role>` marker pair whose interior is machine-written and whose bullets are appended to at end of block.
- The board as committed: `## Active` empty, `## Done` carrying more than thirty entries, every entry line beginning with `- `, sub-bullets indented by two spaces, and existing `intent-hash` / `intent-ratified` / `freeze-attestation` records belonging to other tasks that this task must not touch. A file large enough that a whole-file read is not a sensible verification step, which is why AC15 reads `git diff --numstat`'s columns.
- `git` plumbing output for the literal `99d4038`: `diff --name-only 99d4038...HEAD`, `diff --numstat 99d4038...HEAD`, `show 99d4038:<path>`, and `ls-files --others --exclude-standard`. Every diff is taken as a **commit range** rather than as the one-argument working-tree form, so an unrelated uncommitted edit cannot fail a criterion that has nothing to say about it; the one thing a range cannot see — an untracked stray — comes from `ls-files --others` in AC16's union instead. Every base-ref read goes through `git show` against the committed blob, never through a working-tree read standing in for the ref. A consequence worth stating: at the freeze run, before anything is committed, every range-based criterion sees an empty diff and fails its own non-empty positive control, which is the predicted red rather than a defect.
- `.shell-team/retros/2026-08-08.md` and `.shell-team/interventions/T-1047.md` as **read-only** inputs: the retro's `## Lesson candidates` section in the operator's language, and the interventions file's single conformant entry, both of which the engineer reads and neither of which it edits — except that the interventions file gains further entries if this task's own rounds produce interventions, which is its normal use.

**Out-of-scope synthetic extremes** — declined deliberately; a finding grounded only in one of these is out of contract:

- **NUL bytes, CRLF line endings, non-UTF-8 bytes, control characters or reserved prompt-block marker strings inside a corpus field value, an entry title, or any of `M1` / `M2A` / `M2B` / `M3`.** `bin/check-playbook.sh` owns that class fail-closed already and this task adds no parser; AC5 runs it and AC8 runs the generator.
- **Supersede and `Extended by` pointer graphs.** This task creates no pointer at all: no chain, no cycle, no dangling reference, no duplicate key, no `loop`-superseded-by-`maintainer` direction. An argument about pointer mechanics is an argument about DP3's or DP4's verdict, both of which are settled with reasons.
- **Ever-larger generated blocks, and the 40-line block-size threshold warning.** It already fires at the base ref for two of the four blocks and both grow here; `bin/gen-playbook-blocks.sh` prints it to stderr and exits zero regardless, and `docs/loop-engineering/playbook-block-size-deferral.md` forbids launching a shortening or supersede sweep on that signal alone. A finding that the blocks are too long, or that reading the warning as a failure makes a criterion red, is a request to re-open that deferral.
- **A fifth playbook block, a fifth IN role, or an `Applies-to` token outside `bin/check-playbook.sh`'s closed `KNOWN_ROLES` set.** The four-role loops read that closed set; AC8 carries the one re-measurement trigger that a registry change would invalidate.
- **A prompt-block marker pair appearing in `agents/codex-reviewer.md` or `skills/run/SKILL.md`, or a reordering of `agents/pm-spec.md` that puts a marker region above its `## Spec completion self-check`.** AC12 measures against the *first* marker and the base blob's marker-name set; AC13 and AC14 measure each file's marker count against its own base blob. A hypothetical file layout other than the one committed is not protected here.
- **The runtime behaviour of `## Finding-evaluation mode (review-response)`, of the `review-response` skill, or of `bin/review-gate.sh`.** `M3` adds prose that scopes itself out of that mode; nothing in this task exercises, changes or verifies the mode's own contract, and no fixture for it is written.
- **Any re-freeze in this task, of this spec or any other.** Exactly zero re-freezes happen: no `intent-ratified` line is written, no other task's `intent-hash` is replaced, and no vK→vK+1 transition occurs anywhere.
- **Any board entry other than T-1047's.** Every other task's ledger, flag and prose is untouched; a finding about a neighbour's stale record belongs to that neighbour's task, and AC15's zero-deletion clause is the mechanical guarantee.
- **Additional lesson candidates, from this retro or any other**, and any re-adjudication of the six. That set is closed here (DP9, R1).
- **Adversarial `mktemp` or `$TMPDIR` behaviour** (a hostile temp dir, a symlink race, a path containing whitespace), and non-bash shells. `bin/check-acs.sh` invokes each check through `bash -c`, and the guarded idiom's contract is "exit non-zero when `mktemp` fails", nothing stronger.
- **Non-GNU `grep` / `awk` / `comm` / `sed` / `diff` variants beyond the POSIX behaviour these lines rely on.** The executing side confirms the host's own tools at freeze time (Assumptions); an argument about a toolchain not present on this host is not a finding.
- **Any input reaching `bin/` itself.** No tool's behaviour, default or validation changes in this task; `bin/` is byte-identical (AC16).

<!-- END intent-block: T-1047 -->

## Adjacent-entry audit (DP3)

Every corpus neighbour the retro names for the four promoted candidates, plus the entries in the same families that the corpus's own heading index turns up. Default verdict is **no supersede**; each row states why the neighbour is adjacent rather than covering. No row changed the default.

| New entry | Neighbour | Verdict | Reason |
|---|---|---|---|
| E1 | `2026-07-19 — Default a pre-commitment's trigger threshold to the existing "two consecutive rounds"; state the reason if loosening it` | **no supersede** | The load-bearing row for E1, and the one that answers "is this just a confirming instance?". That entry fixes the **threshold** of the trigger and requires a reason when it is loosened. E1 says nothing about the threshold and constrains what the pre-commitment must contain **besides** a trigger: the disposition, the drop order and the destination that inherits the findings. A pre-commitment with a perfectly defaulted threshold and no named drop order is exactly the case that entry cannot see, and it is the case the live firing showed to be the expensive one. |
| E1 | `2026-07-20 — A verification-mechanism pre-commitment should separate its factual trigger condition from its contextual one` | **no supersede** | Same family, same artefact, different half. That entry decomposes the **trigger** into a factual and a contextual condition and sets a priority when they disagree; E1 decomposes what happens **after** the trigger evaluates true. Both fields are needed and neither implies the other: a precisely decomposed trigger with no pre-decided disposition still reopens the design question at firing time. |
| E1 | `2026-07-19 — When a new subsystem grafted onto stable judgment logic hits two consecutive rounds of independent defects, re-propose splitting it out or deferring it` | **no supersede** | Closest in outcome and still distinct in timing. That entry prescribes an action **at** the two-round mark — re-present the split option to the user — which is a proposal made under pressure with the design already hot. E1 requires the same split to have been **decided in advance**, so what happens at the two-round mark is execution rather than proposal. The retro's own account of the first live firing is precisely that the decision had already been made, which is the distinction this row preserves. |
| E1 | `2026-07-12 — Two consecutive rounds of new Blocker/Major findings against a verification subsystem should trigger a redesign, not another patch` | **no supersede** | The engineer/tech-lead-facing statement of the counting rule; E1 is the pm-spec/tech-lead-facing statement of the pre-commitment's required contents. This row is also DP2's ground for excluding `engineer` from E1's `Applies-to`: the engineer's action on a fired trigger already ships to it here. |
| E2 | `2026-07-12 — Rework instructions should require a batch verification grounded in the input's canonical contract, not a point-fix transcription` | **no supersede**, and the tension is resolved by trigger and actor | The closest neighbour to E2 and the reason `tech-lead` is in its `Applies-to`. That entry fires when **a reviewer flags a verification gap** and directs the batch to be grounded in the **producer's canonical contract**; it is about how a rework instruction is composed in that specific situation. E2 fires on **any** finding at its **first** occurrence and binds the party performing the repair as well as the party relaying it, and its test is the mutation class rather than a contract document. Shipping the older entry without E2 leaves its narrower trigger looking like the whole rule, which is exactly the reading that produced both incidents. |
| E2 | `2026-07-25 — A new lock or guard should get a producer-run mutation self-check before its first review round` | **no supersede** | Same technique, different moment and different subject. That entry governs a **newly written lock**, before any review has happened, and asks its author to break it deliberately. E2 governs a **repair to an existing defect**, after a finding, and asks whether the repair generalises. A lock that passed its own producer-run mutation check can still be repaired later with a fix that closes only the reported case. |
| E2 | `2026-07-26 — A task that creates a verification mechanism needs two self-checks: is the method sound, and where is the detector itself blind` | **no supersede** | Adjacent in spirit — both ask what the current artefact cannot see — and different in scope. That entry is a task-level design self-check for a verification mechanism; E2 is a per-finding closure test that applies to any repair in any task, including tasks that build no mechanism at all. |
| E3 | `2026-08-03 — Code feeding a regex-captured digit string into bash arithmetic follows the file's existing base-10 normalization convention` | **no supersede** | The nearest structural cousin — "follow the file's existing convention" — and instructive for the `Scope` decision rather than for coverage. That entry is `maintainer`-scoped and `Bound-in` to one script, and its subject is one specific numeric convention. E3's subject is the general act of adding an obligation to a shared region, and the property it names — a file carrying its own scoping idiom — belongs to any file with modal regions, which is why DP2 makes it `loop` despite the retro's `[target-specific]` tag. |
| E3 | `2026-07-15 — Audit a shared norm across parallel gate surfaces with a symmetry table, not just a diff` | **no supersede**, and the two stack in this very task | That entry governs a norm landing on **several files or surfaces** and requires a cell-by-cell symmetry table; E3 governs a norm landing in **one shared region serving several modes**. This task exercises both independently: DP6 builds the symmetry table for the two SKILL.md surfaces, and DP7 applies E3's rule inside the single codex-reviewer file. Neither substitutes for the other — a perfectly symmetric landing across two files can leave a mode uncarried in each. |
| E4 | `2026-08-06 — Frozen prose carrying no check line goes stale when what it describes moves, and the proportionate repair is one batched editorial pass` | **no supersede** — and this is the distinction the retro itself draws | The retro states the two are neighbours and different subclasses, and this row keeps that. That entry's subject is **frozen prose describing a mechanism**, whose failure mode is the *world moving underneath a sentence that nobody may edit*, and whose remedy is a batched editorial re-freeze. E4's subject is an **append-only record contradicting itself**, whose failure mode is *the record's own later line disagreeing with its earlier one* with nothing external having moved, and whose remedy is a read before each append. The first is about staleness against an external referent; the second is about internal consistency. A record can be perfectly current about the world and still contradict its own third paragraph. |
| E4 | `2026-08-04 — A factual claim is re-measured against the primary artifact before it is written down, inherited or relayed` | **no supersede** | Adjacent and orthogonal. That entry governs a claim's truth against the **primary artefact it describes**; E4 governs a record's consistency with **its own earlier lines**. Every append in the originating incident was accurate about the world at the moment it was written, which is exactly why that entry could not catch any of them. |
| E4 | `2026-08-02 — A review-record appendix is committed by the round that writes it` | **no supersede** | Same artefact class, different property. That entry is about **durability** — a record's content reaching git in the round that produced it; E4 is about **internal consistency** of the content itself. A record committed punctually every round is the ideal substrate for E4's defect to accumulate in. |
| — | `2026-08-06 — A relayed premise is re-measured at its primary source before it is written into a downstream prompt` | **not applicable — this is why candidate 1 is not promoted** | Candidate 1 asks for the relay class again after another cycle of firings. Its rule already shipped last cycle as this entry, whose text states the writer-side obligation, the labelling fallback and the receiver's second line of defence. A recurrence is evidence *for* an entry, not a second entry; what candidate 1 actually lacked was a checklist surface on the pm-spec side, which is `M1` (DP5, DP9). |
| — | `2026-07-26 — A verification command needs array/literal file args, an unswallowed exit code, and a positive control` | **not applicable — this is the nearest neighbour of the candidate that was dropped** | Candidate 6's subject is a **frozen positive control that never reaches the logic its label names**, which this entry's third clause requires the existence of but not the reachability of. It is a genuine gap, and it is deliberately **not** filled here: its mechanisation is internalised into issue #188 / task T-1049, with the re-evaluation trigger that it is promoted standalone if a vacuous control recurs on a task other than #188 (DP9). This row exists so the gap is on the record rather than implied by an absence. |

## Parallel-surface symmetry table (DP6, DP11)

Each surface where one of the four new norms could land, marked present, mirrored-now, or not-applicable with a reason, per the corpus's `2026-07-15` symmetry-table rule.

| Norm | Surface | Disposition |
|---|---|---|
| E2 / class-closure (the rework contract) | `skills/run/SKILL.md` step 5 (Validate) | **mirrored-now** — `M2A`. This is the phase where a `qa-verifier` finding is relayed. |
| E2 / class-closure (the rework contract) | `skills/run/SKILL.md` step 6 (Review) | **mirrored-now** — `M2B`, in the file's own reference idiom. The same-class-2 rule and the T-100 branch are already duplicated across these two steps, so a one-sided landing would be the asymmetry this table exists to prevent; AC13 fails it mechanically. |
| E2 / class-closure | `templates/prompt-blocks/playbook-{engineer,qa-verifier,tech-lead}.md` and their consumers | **present** — E2's `Rule` ships to those three blocks via `Applies-to` (DP2). No surface-specific wording is added; the corpus line is the mechanism. |
| E1 / pre-commitment contents | `templates/prompt-blocks/playbook-{pm-spec,tech-lead}.md` and their consumers | **present** — via `Applies-to` (DP2). No hand-written mechanisation: the rule constrains what a spec's own pre-commitment section contains, and the two roles that write and calibrate it receive it in their blocks. |
| E3 / reuse the local scoping idiom | `templates/prompt-blocks/playbook-{engineer,pm-spec}.md` and their consumers | **present** — via `Applies-to` (DP2), plus applied in place by DP7 rather than merely described. |
| E4 / records-only whole-record read | `templates/prompt-blocks/playbook-{engineer,pm-spec,qa-verifier}.md` and their consumers | **present** — via `Applies-to` (DP2). |
| E4 / records-only whole-record read | `agents/codex-reviewer.md` | **mirrored-now** — `M3`. The cross-provider reviewer is not an `Applies-to` token and receives no generated block by design (evaluator independence), so a corpus entry alone never reaches it; the gate that found the originating instances is exactly this one, which is why the hand-written item exists. |
| relay re-measurement (candidate 1) | `agents/pm-spec.md` `## Spec completion self-check` | **mirrored-now** — `M1` (DP5). |
| relay re-measurement (candidate 1) | `agents/tech-lead.md` `## Rules` | **present, unedited** — the rule is already the last plain bullet of that hand-written section, which the interventions record for this task establishes as a measured correction to the retro's premise. Editing it again here would produce a second wording of one rule in two files. |
| all four norms | `agents/scrum-master.md`, `agents/drift-evaluator.md`, `agents/ui-designer.md`, `agents/triage-orchestrator.md` | **not applicable — no carrier** (DP11). None is a playbook consumer; none composes a rework instruction, authors a pre-commitment, or keeps an accumulating per-round task record. The retro agent does append to a retro, but a retro is written once per cycle by one pass rather than appended to across rounds, which is the shape E4 names. A finding that one of them should carry a line is a next-planning input, not a gap in this task. |
| all four norms | `bin/` | **not applicable — no mechanical form** (DP11). Each of the four is a judgment about what an authored text must contain; a checker for it would itself be a verification mechanism, which is a task of its own rather than a rider on a promotion. |

## Body-to-AC correspondence

Every normative directive stated in the body above, mapped to the criterion that enforces it or to an explicit exemption with a reason.

| Body directive | Where | AC / disposition |
|---|---|---|
| Candidate 2 becomes a new entry, byte-exact as frozen | DP1 | **AC1** · **AC7** (the append is byte-exact in position and content) |
| Candidate 3 becomes a new entry, byte-exact as frozen | DP1 | **AC2** · **AC7** |
| Candidate 4 becomes a new entry, byte-exact as frozen | DP1 | **AC3** · **AC7** |
| Candidate 5 becomes a new entry, byte-exact as frozen | DP1 | **AC4** · **AC7** |
| `M1`, `M2A`, `M2B`, `M3` are frozen text, not engineer discretion | DP1 / DP5 / DP6 / DP7 | **AC12** · **AC13** · **AC14** (each byte-exact and counted) |
| E1 is `loop` / `pm-spec, tech-lead` | DP2 | **AC1** (both fields asserted explicitly) · **AC9** (all four propagation cells) |
| E2 is `loop` / `engineer, qa-verifier, tech-lead` | DP2 | **AC2** · **AC9** (three cells present, pm-spec cell zero) |
| E3 is `loop` / `engineer, pm-spec` — and not `maintainer`, despite the `[target-specific]` tag | DP2 | **AC3** (both fields asserted explicitly; a `maintainer` downgrade fails the `Scope` clause and would additionally require a `Bound-in` bullet that breaks AC3's nine-line shape) · **AC9** |
| E4 is `loop` / `engineer, pm-spec, qa-verifier` | DP2 | **AC4** · **AC9** (three cells present, tech-lead cell zero) |
| No new entry supersedes, retires, re-scopes or rewords any existing entry | DP3 / Non-goals | **AC7** (the base blob is a byte-identical prefix of the current file) · **AC5** (`del=0`) · **AC6** (heading set gained exactly four) |
| The adjacency verdicts themselves, and the reason candidate 1 is not re-promoted | DP3 / DP9 / audit table | `info-only (not promoted to AC)` — a recorded judgment about entries this task does not touch; its assertable half is the byte-immutability of every other entry, which **AC7** carries, and the absence of a fifth entry, which **AC6** carries |
| No `Extended by` pointer is attached to any entry | DP4 / Non-goals | **AC7** (a pointer bullet added to an existing entry breaks the prefix comparison) |
| No other task's spec is edited or re-frozen | DP4 / Non-goals | **AC16** (the allow-list contains no other spec path) |
| `M1` is the last item of pm-spec's self-check list, outside **every** marker region | DP5 | **AC12** (`M1` inside the self-check→frozen-intent region, and that whole region before the *first* `BEGIN prompt-block` marker; marker-name set matches the base blob) |
| `agents/tech-lead.md`'s `## Rules` is not edited | DP5 / DP11 / Non-goals | **AC11** (its added-line count equals its derived generated delta with nothing extra, deletions zero) |
| `M1` does not restate the tech-lead rule or any playbook line in its own words | DP5 | `info-only (not promoted to AC)` — the frozen bytes settle what `M1` says, and **AC12** pins them; whether a sentence duplicates a rule elsewhere is a judgment no command can make |
| The class-closure norm lands on **both** step 5 and step 6, neither alone | DP6 | **AC13** (each region carries its own frozen line exactly once and the other's zero times) |
| `M2B` uses the file's reference idiom rather than a second full statement | DP6 | **AC13** (the frozen bytes are what land; the idiom is inside them) |
| `skills/run/SKILL.md` gains exactly two lines and loses none | DP6 | **AC13** (numstat `2` / `0`) |
| `M3` sits in `## Rules`, carries its mode-scoping clause, and the direction word `above` is true | DP7 | **AC14** (region assertion, heading-order assertion, and the clause's presence inside the frozen bytes) |
| `agents/codex-reviewer.md` gains exactly one line and loses none | DP7 | **AC14** (numstat `1` / `0`) |
| Neither `agents/codex-reviewer.md` nor `skills/run/SKILL.md` gains a marker pair | DP6 / DP7 | **AC13** · **AC14** (marker count equals the base blob's in each) · **AC8** (`bin/check-prompt-sync.sh` over the whole registry, which is what re-asserts their `contain` obligations) |
| Every accounting criterion is base-relative or value-independent | DP8 | **AC5** (expected addition computed from the frozen blocks) · **AC7** (base blob's own line count) · **AC9**/**AC10**/**AC11** (deltas derived from `Applies-to`) · **AC10** (block sizes as base + delta) |
| The one surviving absolute literal is AC8's eight-file registry control, with a reason and a staleness trigger | DP8 | **AC8** (the literal, its positive-control purpose, and its `- stale-at:` sub-bullet) |
| The nine-line entry shape, the four-role loop, and the hand-written `+1`/`+2` counts are structural, not population counts | DP8 | **AC1**–**AC5** (the `9` assertions) · **AC9**/**AC10**/**AC11** (the closed role set) · **AC11**/**AC13**/**AC14** (the decided line counts) — exempted in DP8 with the reason that none grows |
| Retro candidates 1 and 6 never enter the corpus | DP9 | **AC6** (the gained heading set is exactly the four frozen keys) |
| Candidate 6's mechanisation belongs to #188 / T-1049, with a stated re-evaluation trigger | DP9 / Non-goals | `info-only (not promoted to AC)` — a recorded deferral with a trigger; no artefact property can assert that a deferral was decided rather than forgotten, which is why it is stated in DP9, in the audit table's last row and on the board entry. Its assertable half is that nothing under `tests/` moves, which **AC16** carries |
| Appends go through `bin/playbook-promote.sh` in the order E1–E4; blocks are never hand-edited | DP10 | `info-only (not promoted to AC)` for the tool attribution — no command can prove which tool wrote a byte. The bytes and the file order are asserted by **AC1**–**AC4** and **AC7**'s ordered tail comparison; the never-hand-edited half is asserted by **AC8** (a hand-edited block cannot survive regeneration into a scratch tree) |
| No surface beyond the three named ones is widened | DP11 / Non-goals | **AC16** (allow-list) · **AC11** (tech-lead's line count) |
| No checker is written for any of the four new rules | DP11 / Non-goals | **AC16** (`bin/` and `tests/` are outside the allow-list) |
| No work on #167, #188 or any other issue | Non-goals | **AC16** (allow-list — every path such work would touch is outside it) |
| No generated block is shortened and the size warning is not treated as a failure | Non-goals / Input space | **AC8** (judged by exit status and `cmp`, stderr redirected) · **AC10** (a shortened block shows deletions) |
| The retro file is immutable | Non-goals | **AC16** (outside the allow-list) |
| The `## Format` section is byte-unchanged | Non-goals | **AC7** (it lies inside the prefix compared byte for byte) |
| No `.ja.md` counterpart | Non-goals | **AC16** (the allow-list contains no `.ja.md` path) |
| The new entries are authored in English at promotion | Task / repo language rule | **AC1**–**AC4** — the frozen bytes settle it; there is nothing left for a language check to decide |
| The board entry follows the strict line format and disturbs no other entry | Board hygiene | **AC15** (hand-off lint, structural heading check, zero deletions, the entry line's own format) |
| Checkers fail closed; no check may pass on an unreadable or unextractable input | repo contract | **AC1**–**AC4** (extraction line-count and non-empty assertions before any comparison) · **AC5** (the computed expectation must exceed zero) · **AC6** (the base blob's heading list must be non-empty) · **AC7** (base non-empty, *N* > 0, expectation non-empty) · **AC8** (the eight-file registry control) · **AC9**–**AC11** (empty numstat rows and empty field extractions fail) · **AC12** (six positive controls) · **AC13** (anchor uniqueness, ordering, non-empty regions) · **AC14** (heading uniqueness, ordering, the clause's presence) · **AC15** (an untouched board fails) · **AC16** (an empty changed-set and a missing record file both fail) |
| The full applicable CI-wired list runs locally, with directory-scanning steps run rather than reasoned about | AC17 (a) | **AC17** (a) |
| A producer-run mutation self-check precedes the first review round, including probes at AC7's blind spot and at the one-sided landing | AC17 (b) / playbook | **AC17** (b) |
| Every `Why` clause is checked back against the records the retro names | AC17 (c) | **AC17** (c) |
| Every relayed claim is re-measured before the freeze | AC17 (d) | **AC17** (d) |
| Each named source gets a per-source report, with the adjudication's relayed status restated | AC17 (e) / Summarized sources | **AC17** (e) |

## Assumptions

- **pm-spec has no shell in this role, so not one `check:` line above has been executed.** Every file named in `## Summarized sources` as read directly was read with a file-reading tool. The executing side runs all 16 live `check:` lines against the pre-implementation tree (base + this spec + the board entry), decomposes each compound line into the clauses it asserts, corrects anything mechanically broken or vacuously passing with a meaning-preserving fix, records the measured shape, and only then records the freeze attestation and `- intent-hash (v1)` for T-1047. Verify, then correct, then freeze.
- **Predicted pre-implementation shape for this spec's own 16 lines, recorded so a mismatch is itself a finding: PASS AC8 (1); FAIL AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC9, AC10, AC11, AC12, AC13, AC14, AC15, AC16 (15); SKIP AC17.** **AC8 is a preservation lock that must be green before, during and after the change** — the blocks are in sync with the corpus at the base ref — and a red there at the base ref is a finding about this spec's premises, not about the work. **AC15 is the ledger-flip line**: it is red at freeze for one reason only, that `bin/check-intent.sh` needs the `intent-hash` and `freeze-attestation` sub-bullets this role does not write, and it goes green the moment the coordinating session appends them. Measure it once before writing the ledger lines and once after, and record both. Misreading AC15's pre-freeze red as a board-format defect is the likeliest false finding on this task.
- **Relayed claims this spec leans on, disclosed rather than absorbed.** Each is a claim pm-spec could not measure; the freeze run measures it and reports the value beside it.
  1. **`99d4038` is `develop`'s tip and this branch's real branch point.** Relayed. Every `git` anchor in the criteria resolves that literal (AC5, AC6, AC7, AC10, AC11, AC12, AC13, AC14, AC15, AC16); if it is false they all measure the wrong comparison. Confirm with `git rev-parse --verify` and an ancestry check before the freeze. pm-spec observed the same short SHA in its own session context, which corroborates but does not measure it.
  2. **The 2026-08-08 candidates were adjudicated as: 2, 3, 4, 5 promoted; 1 mechanise-only; 6 promotion-dropped.** Relayed — the coordinating session recommended these dispositions in the v1.8.0 sprint planning and the host GO'd the plan; the retro file records only the candidates and their tags. The whole shape of DP1, DP2 and DP9 rests on it. If the adjudication differs, that is a pre-freeze scope question, not a wording fix.
  3. **Candidate 6's mechanisation is internalised into issue #188 as task T-1049, in this same sprint, with the stated re-evaluation trigger.** Relayed. A wrong issue or task id makes the deferral untraceable rather than wrong, but correct it before the freeze; the trigger's wording is DP9's and does not depend on the id.
  4. **Candidate 1's mechanisation is scoped to exactly one item in `agents/pm-spec.md`'s self-check, and `agents/tech-lead.md` is not re-edited.** Relayed as the adjudication; pm-spec independently **measured the tech-lead side** and confirms the relay re-measurement rule is present as the last plain bullet of that file's hand-written `## Rules`, above all three of its marker regions. The disposition is relayed; the existing rule's presence is measured. This is the correction the task's interventions record already carries.
  5. **`## Active` is empty at the base ref.** Relayed, and separately observed by pm-spec in the **working tree** rather than in the base blob. This is what makes the board edit a pure insertion, which AC15's zero-deletion clause depends on.
  6. **`.shell-team/interventions/T-1047.md` already exists and is conformant.** pm-spec read the file and it carries exactly one well-formed `assumption-contradicted` entry inside a matching marker pair; whether `bin/check-interventions.sh` agrees is a measurement pm-spec cannot run, and AC16 asserts it.
- **The derived per-role deltas the criteria compute at run time, stated here for diagnosis only and never as a criterion's expected value.** Evaluating DP2's routing against the four frozen `Applies-to` fields gives: engineer 3 (E2, E3, E4), qa-verifier 2 (E2, E4), tech-lead 2 (E1, E2), pm-spec 3 (E1, E3, E4); `agents/pm-spec.md` additionally gains `M1`, for four lines total. If the freeze run measures anything else, the disagreement is between the measurement and DP2, and the reconciliation is to re-read DP2 rather than to adjust a number — no criterion above contains these figures.
- **`bin/gen-playbook-blocks.sh` prints a non-fatal block-size warning to stderr for blocks past its `LINE_WARN_THRESHOLD=40`.** It already does so at the base ref for at least the engineer and pm-spec blocks, and all four grow here. The script's contract says the warning never fails the run; AC8 redirects stderr and judges by exit status and `cmp`. Do not read the warning as a failure, do not respond to it by shortening an entry or dropping a role from an `Applies-to` list — that would silently revert DP2 — and do not open a supersede sweep on it, which `docs/loop-engineering/playbook-block-size-deferral.md` forbids.
- **No `CHECK_ACS_TIMEOUT` elevation is required, stated explicitly so the runner does not add one by analogy.** The heaviest criterion is AC8 — one scratch-tree copy of eight small files, one regeneration (which internally runs `bin/check-playbook.sh` once over the corpus), eight `cmp`s, and one `bin/check-prompt-sync.sh` — seconds, well inside the 120s default. AC16's `bin/check-pii-shapes.sh --base` over a small diff and AC15's `bin/check-intent.sh` over a large board are the next heaviest and are also seconds.
- **This spec is self-hosting and quotes strings that also live in the artifacts it checks.** AC6 compares heading sets **inside the corpus only**, so the frozen headings written here are invisible to it; AC9's greps name specific block and agent targets and never the repository, so this file's own quotation of a `Rule` line cannot trip them; AC12's, AC13's and AC14's greps name one file each, so this file's own quotation of `M1`–`M3` and of the marker syntax cannot trip them; AC15's grep names the resolved board path only. Run `bin/check-pii-shapes.sh` over this spec before the first commit regardless.
- **No `check:` value begins or ends with a backtick** (`bin/check-acs.sh` rejects that shape fail-closed), no AC label carries a suffix glued to its digits, and every `grep` whose pattern could begin with `-` passes `--` first.
- **The frozen-block extraction patterns contain a `!`** (`<!-- frozen-begin: … -->`), always inside single quotes or an `awk -v` assignment. `bin/check-acs.sh` executes each line through a non-interactive `bash -c`, where history expansion is off, so this is safe in the harness — but pasting one of these lines into an *interactive* shell to debug it can trip history expansion and produce a failure that has nothing to do with the criterion. Run them through `bash -c`, or through `bin/check-acs.sh` itself.
- **Every `mktemp` invocation above uses the guarded idiom** (`T=$(mktemp -d "${TMPDIR:-/tmp}/<slug>.XXXXXX") || exit 1`, failure exiting before the first composed path is used), each with its own scratch slug, and every `|| true` sits on a `grep -c` whose zero is a meaningful measurement, never on a producer whose failure would otherwise be invisible.
- **AC13's and AC14's step and heading anchors are matched with anchored patterns whose only metacharacters are escaped literals.** `^5\. \*\*Validate\*\*`, `^6\. \*\*Review\*\*`, `^7\. \*\*Done\*\*`, `^## Rules$` and `^## Finding-evaluation mode (review-response)$` are matched with basic `grep` (the parentheses are literal there, which is why the mode heading needs no escaping and would need it under `grep -E`). Confirm each anchor's uniqueness at the freeze; each criterion already asserts a count of one, so a duplicated anchor fails rather than silently selecting the first.
- **Host tooling assumptions to confirm at the freeze**: `comm`, `cmp`, `sed -n`, `awk`, `tail -n +N`, `grep -c ''` and `grep -cxFf` behave POSIX-ly on this host, and `grep -c ''` counts lines including a final line with no trailing newline. If the corpus's last line lacks a trailing newline at the base ref, AC7's `head`/`tail` split needs re-measurement before the freeze rather than after.

## Open questions

None blocking. Two items are recorded rather than asked:

- The retro's Try line for candidate 1 names "tech-lead / pm-spec のプロンプト作成手順" — two surfaces, of which one already carries the rule. DP5 lands the pm-spec half and leaves the tech-lead file untouched, on the strength of pm-spec's own read of that file. If the coordinating session's reading of the adjudication is that the tech-lead bullet should also be reworded, say so before the freeze; it changes AC11's tech-lead clause and the scope lock.
- The retro's Try 2 asks that #188 inherit T-1044's round-1 and round-2 findings as a requirement list rather than re-deriving them. That is task T-1049's input, and E1's `Rule` states the general form of it. Nothing here files, drafts or scopes that task.

## Notes for engineer

**Read `.shell-team/retros/2026-08-08.md`'s `## Lesson candidates` section before you touch anything**, then `.shell-team/interventions/T-1047.md`, then `.shell-team/lessons.md`'s `## Format` section and the fourteen entries named in the adjacency audit. The candidates are in the operator's language; the entries you ship are English, authored at promotion, and already written for you in DP1 — you are not translating anything.

**Measured-at-ref labels: not applicable — none of this task's deliverables prints a command beside a `measured at <ref>` label.** The four corpus entries, the four regenerated blocks, the four consumer agent files and the four hand-written texts contain no measurement label and no printed command at all. Every criterion that resolves `99d4038` reads that ref's committed blob through `git show 99d4038:<path>` or the commit range `git diff --numstat 99d4038...HEAD`, never a working-tree read standing in for the ref; if you add a measured value to the board hand-off, print the ref-reading command beside it.

**Self-application, and it is not decorative: this task's own board entry, this spec's mutable sections and its review record are exactly the append-only record prose E4 governs.** Before every append to any of them — each rework round, each disposition line, each freeze note — re-read the whole entry rather than the block you last wrote, and when a new line changes what an earlier one said, say so in the new line instead of leaving both standing. The four consecutive rounds that produced candidate 5 were all of that shape, and this task is the same shape: mostly records, no product mechanism, nothing that re-reads the head.

Order of work, which is also the order that keeps every intermediate state schema-valid:

1. Four `bin/playbook-promote.sh` runs, `--date 2026-08-08`, in the order E1, E2, E3, E4. Each one re-validates the whole file before it writes.
2. `bin/gen-playbook-blocks.sh`, once, from the repository root, with no `--lessons` flag so the pointer path resolves the way every existing block's pointer did. Commit what it writes; never hand-edit a block or a marker region.
3. Append `M1` by hand as the last item of `agents/pm-spec.md`'s `## Spec completion self-check`, after the population-count item and before the `## Frozen intent freeze` heading.
4. Insert `M2A` and `M2B` by hand into `skills/run/SKILL.md`, each immediately after its own step's `- **Same-class-2 rule**:` sub-bullet, at three-space indentation.
5. Append `M3` by hand as the last bullet of `agents/codex-reviewer.md`'s `## Rules`. Re-run `bin/check-prompt-sync.sh` after steps 3–5.
6. Board and records.

Commit separation is **required** by R2, in exactly this shape, so that whichever half is clean can ship alone: **(1)** the four corpus appends, **(2)** the block regeneration, **(3)** the three hand-edited prompt surfaces, **(4)** the board and the records.

Scope-lock allow-list (the files you may change). Required:

```
.shell-team/lessons.md                                       (E1, E2, E3, E4)
templates/prompt-blocks/playbook-engineer.md                 (generated)
templates/prompt-blocks/playbook-qa-verifier.md              (generated)
templates/prompt-blocks/playbook-tech-lead.md                (generated)
templates/prompt-blocks/playbook-pm-spec.md                  (generated)
agents/engineer.md                                           (marker region only, generated)
agents/qa-verifier.md                                        (marker region only, generated)
agents/tech-lead.md                                          (marker region only, generated — no hand edit)
agents/pm-spec.md                                            (marker region, generated + M1 by hand)
agents/codex-reviewer.md                                     (M3 by hand — this file has no marker region)
skills/run/SKILL.md                                          (M2A + M2B by hand — this file has no marker region)
.shell-team/specs/T-1047-promote-retro-2026-08-08.md         (this file — you do not edit its intent block)
.shell-team/todo.md
```

Permitted in addition, and required by AC16 in the provenance and interventions cases: `.shell-team/provenance/T-1047.md`, `.shell-team/reviews/T-1047.md`, `.shell-team/interventions/T-1047.md`. Anything else in the diff fails AC16 — including a stray untracked scratch file in the checkout, since AC16's union counts untracked additions. Run every mutation experiment on a `$TMPDIR` copy or a scratch `git worktree`, never in the working tree.

Traps in the criteria themselves, so you do not find them the expensive way:

- **All four frozen blocks are written in `bin/playbook-promote.sh`'s emission order** — `Category, Applies-to, Scope, Status, Source, Rule, Why, How to apply` — so a tool-produced append matches the frozen bytes by construction. Confirm that at the freeze run rather than trusting this note: if any block's order disagrees with the tool's, the repair is a re-freeze of that block routed to pm-spec (a class-B delta, since it changes a criterion's expected bytes), **never** a hand-written entry or a hand-reordered one, either of which would put a hand-edited entry in the corpus and contradict the promotion path DP10 fixes.
- **Every frozen text is read out of this spec by AC1–AC7 and AC9–AC14.** If you retype one instead of copying it, a single smart quote or a collapsed double space fails the criterion, and the failure will look like a corpus or prompt problem rather than a transcription problem. Extract the fenced blocks with `awk`/`sed` into scratch files and splice those bytes programmatically.
- **Every frozen field value carries characters that shell quoting mangles**: backticked fragments, angle-bracket placeholders, straight double quotes, apostrophes and em-dashes. Extract each field's bytes into a scratch file and pass them as `--rule "$(cat "$T/rule")"` and so on; a variable's *contents* are never re-expanded, whereas anything you retype into the command line is at the mercy of whatever quoting you chose.
- **`bin/playbook-promote.sh --title` takes the heading text only.** The script writes `## <date> — <title>` itself, so the value is everything *after* the em-dash separator in the frozen heading — do not include the date and do not include the em-dash, or the entry key will not match the frozen block and AC1–AC7 all fail together on what looks like a byte mismatch.
- **AC7 is the strictest criterion here and it has one blind spot worth knowing.** It proves the corpus is a pure append by comparing the base blob against the current file's first *N* lines and then comparing the remainder against the four frozen blocks in order. It therefore catches a reworded entry, a supersede, an added pointer bullet, a `## Format` edit and a wrong append order — but if you append the four entries in the wrong order it fails on the *tail* comparison with no indication which entry is misplaced. Check the order before you blame the extraction.
- **`M2A` and `M2B` begin with three spaces.** The frozen blocks carry that indentation inside them, so extract-and-splice preserves it; a re-indented line fails AC13's `grep -cxFf` with a message that says nothing about whitespace.
- **`M3` says `above`, not `below`.** The Preconditions instance of the same idiom says "below" because it precedes the mode heading; `## Rules` follows it. Copying that sentence verbatim instead of adapting its direction word is precisely the defect E3 names, in the task that promotes E3.
- **AC14 also checks that `M3`'s own bytes name the mode heading.** If a rework ever proposes trimming the scoping clause for brevity, that is a class-B re-freeze, not a wording tidy.
- **Every diff-based criterion reads the commit range `99d4038...HEAD`, not the working tree.** Uncommitted work is invisible to AC5, AC10, AC11, AC13, AC14, AC15 and AC16's tracked half, so run `bin/check-acs.sh` **after** committing each stage rather than before — an uncommitted edit reads as "nothing changed" and fails the non-empty positive control, which looks like a missing edit rather than a missing commit. AC16's untracked half (`git ls-files --others --exclude-standard`) is the deliberate exception: it sees strays the range cannot, which is why a scratch file left in the checkout fails the scope lock even though no commit contains it.
- **AC10's and AC11's teeth are in the deletion column, not the addition count.** Any reflow, any "improvement" to an existing bullet, any regeneration that reorders a block turns a 0 into a positive number and fails. The generator appends at end of block; if a bullet moves, something else changed.
- **AC11 is what forbids a hand edit to `agents/tech-lead.md`.** Its added-line count must equal the derived generated delta exactly — so a "while I was in there" clarification to the existing relay bullet fails a criterion whose message names neither the bullet nor the file's role in this task.
- **AC15 is red until the freeze records exist.** `bin/check-intent.sh` needs the `intent-hash` and `freeze-attestation` sub-bullets, which the coordinating session writes. That red is expected at hand-off and is not a board-format defect.
- **`grep -c` under `set -euo pipefail`** aborts a script when the count is genuinely `0`; append `|| true` to the pipeline, never to the assignment, and keep the explicit assertion right after. Relevant if you write scratch verification around the mutation self-check.
- **The 40-line block-size warning will fire.** It is not a failure, it is not a reason to shorten an entry or narrow an `Applies-to` list, and it is not a reason to open a supersede sweep.

Pre-commitments, set before the first round and binding on every role on this branch:

- **R1 — the six dispositions and every frozen text are settled.** An argument that retro candidate 1 or 6 belongs in the corpus, that E2 should drop a role, that E3 should be `maintainer`-scoped, that E4 should add the tech-lead, that a new entry should supersede one of its neighbours, that an `Extended by` pointer should be attached, or that a `Rule` sentence should read differently is a **re-freeze proposal (vK→vK+1) routed to pm-spec**, not a rework fix the engineer may apply. Arguments to add candidates 1 or 6 to the corpus are **out of contract** outright, not merely re-routed.
- **R2 — two consecutive rounds of independent new `Blocker`/`Major` findings against the hand-written prompt-surface half** (`M1`, `M2A`, `M2B`, `M3` and their AC12/AC13/AC14 criteria) ends the patching. Factual trigger: round *n* and round *n+1* each produce new, independent findings in that class. Contextual trigger: a third round is about to start. **The factual trigger governs when the two disagree**, because rounds can be relabelled and independent defect classes cannot. On trigger, execute this disposition rather than opening the question: **ship the four corpus appends and the block regeneration alone — commits 1, 2 and 4 above — and spin the three prompt surfaces plus AC12/AC13/AC14 into a follow-up task, which inherits both rounds' findings as its requirement list rather than re-deriving them.** The droppable component is the hand-written half, because the corpus appends are self-contained, are the adjudication's primary deliverable, and depend on nothing the hand edits touch. Two consecutive rounds is this repository's standing threshold and nothing here loosens it. This pre-commitment is written in the three-part shape E1 requires, in the task that promotes E1.
- **R3 — any proposal to touch `bin/`, `tests/`, `.github/`, `docs/`, `templates/prompt-blocks/registry.txt`, `agents/tech-lead.md`'s hand-written prose, another agent file, another skill, the retro files, another task's spec, or the mechanisation of candidate 6 is unconditionally out of contract.** Record it in the review record (and, if it recurs, as an issue candidate for the next planning input) and refuse it in the round it appears. AC6, AC7, AC11 and AC16 are the machine half of the same refusal.

Task classification, stated so review rigour is calibrated rather than guessed: **this is not verification-mechanism work — no checker, parser or state tracker is written or extended, and `bin/` and `tests/` are byte-identical** — but it is not a plain pure-addition either. It exercises three existing checkers' contracts at once (the corpus schema, the block generator's routing, and the prompt-sync registry's two modes), it hand-edits prose in three shipped prompt surfaces, two of which carry no marker region at all and are governed only by `contain` obligations, and one of those edits must land symmetrically across two parallel surfaces in the same file. Review the three hand-edited surfaces and their placement criteria at the higher of the two standards, and the four corpus appends at the ordinary standard for a frozen-text promotion.
