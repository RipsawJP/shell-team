# The 2026-08-06 retro's three adjudicated corpus promotions land, and the tech-lead's hand-written Rules section gains the pre-write re-measurement item

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1039
**Base**: `253f4b7` — `develop`'s tip and the first car of the v1.7.0 sprint train. Every `git` anchor below resolves that literal.
**Branch**: `chore/promote-retro-2026-08-06`

## Problem

The 2026-08-06 retro raised seven lesson candidates and the host adjudicated each one: promote three as new corpus entries, mechanise three elsewhere in this sprint without promoting them, and drop one because a general form of it already shipped last cycle. Nothing in this loop executes an adjudication on its own — `bin/playbook-promote.sh` and `bin/gen-playbook-blocks.sh` are both run by hand — so an adjudicated disposition that no task carries out simply evaporates while the defect classes it named keep firing. All three promoted classes cost real work in the cycle just closed: one produced four `assumption-contradicted` records across three tasks, one produced three consecutive coupling defects inside a single task before the same-class rule fired, and one produced three stale frozen sentences that no machine check can reach.

The first of the three also has a mechanisation half that belongs here rather than in a separate task. The defect is a coordinating layer writing a premise it never measured into a downstream role's instructions; the surface where those instructions are composed is the tech-lead's Routing Map, and the tech-lead's own prompt carries a hand-written `## Rules` section with no item about it. One line there converts a discipline the corpus states into a checklist item at the point the prompt is written.

## Summarized sources

One line per document whose facts this spec restates, naming the distinction being carried over. This section sits outside the intent block deliberately; naming a further source during a rework is an ordinary edit, not a re-freeze.

- `.shell-team/retros/2026-08-06.md`, `## Lesson candidates` — **read directly, in full**. Distinctions carried over: **which of the seven candidates is promoted here and which is not** (candidates 1, 2 and 4 in; 3, 5 and 6 mechanised elsewhere without promotion; 7 dropped as already covered), and that the retro's own `[common]` / `[target-specific]` labels are an **input** to the `Scope` decision rather than the decision (DP2). The three candidates' own prose is the substance the three frozen entries restate. Two further distinctions the retro draws and this spec preserves rather than flattening: candidate 1's four contradicted premises are recorded as **at least** four (the retro says `少なくとも 4 件` and then enumerates exactly four, two of them in one task), and candidate 2's three instances were **consecutive within a single task**, which is what triggered the switch from point repairs to a full inventory — a claim about ordering, not merely about count.
- The records the retro's candidate lines cite — `.shell-team/interventions/T-1036.md`, `.shell-team/interventions/T-1033.md`, `.shell-team/interventions/T-1034.md`, `.shell-team/interventions/T-1035.md`, `.shell-team/reviews/T-1035.md`, `.shell-team/reviews/T-1036.md`, `.shell-team/reviews/T-1033.md` — **NOT opened by pm-spec**. Their content reaches this spec only through the retro's own candidate prose, which cites them. The three frozen entries' `Why` fields therefore restate **the retro**, not those records. AC15 (c) requires the engineer to open each named record and confirm every clause of every `Why` against it before the first review round, and to report which clauses were confirmed and which were generalised deliberately.
- `.shell-team/specs/T-1033-promote-retro-2026-08-05.md` — **read directly, in full**. Distinctions carried over: the frozen-block-plus-extraction idiom every criterion below reuses (`<!-- frozen-begin: X -->`, a ```` ```markdown ```` fence, `sed '1d;$d'`), the nine-line `loop`-scoped entry shape, and — load-bearing for DP7 — that **its own AC10 pinned the four generated blocks with the absolute literals `44 / 32 / 23 / 53`**, which is exactly the shape candidate 2 forbids. This task self-applies the new rule rather than reproducing that shape. This spec does **not** edit T-1033's file, and no criterion below reads it.
- `.shell-team/specs/T-1027-promote-retro-2026-08-04.md` — **read directly**, its frozen `E3` block specifically. Distinction carried over: `E3` is the frozen text of the corpus entry `2026-08-04 — A factual claim is re-measured against the primary artifact before it is written down, inherited or relayed`, which is N1's nearest neighbour. That entry's text living inside **another merged task's frozen intent block** is the whole reason DP4 declines to attach an `Extended by` pointer to it here.
- `.shell-team/lessons.md` — **read directly**: its `## Format` section (the field grammar, the `all` = four-IN-roles gloss, the `Scope` shipping rule, and the two field bullets deliberately kept outside the fenced example), its complete `## ` heading index, and in full every entry named in the adjacency audit below. Distinction carried over: `Extended by` is recognised by field name only and its value is never schema-checked, while `Superseded-by` is resolved by equality against every other entry's key — which is why DP3's no-supersede verdict has to be a judgment recorded in prose and DP4's no-pointer default is a free choice rather than a constraint.
- `bin/playbook-promote.sh` — **read directly, in full**. Distinctions carried over: the emitted field order `Category, Applies-to, Scope, [Bound-in], Status, Source, Rule, Why, How to apply`; one blank line before each appended entry; that a `loop`-scoped entry emits **no** `Bound-in` bullet, giving the nine-line shape; and that `--title` takes the heading text **after** the em-dash only, the script writing `## <date> — <title>` itself.
- `bin/gen-playbook-blocks.sh` — **read directly** at its `Scope`/`Applies-to` gate and its emit statement. Distinctions carried over: `Scope: loop` decides **whether** an entry ships and `Applies-to` decides **where**; only the `Rule` field reaches a generated block; and the emitted line's exact shape is `- <rule> (<pointer-path>, <date> — <title>)`, with the pointer path being the file actually read rather than a hardcoded literal. AC12 reconstructs that line from N1's frozen bytes, so the shape is load-bearing.
- `bin/check-playbook.sh` — **read directly** at its enum and field-name declarations. Distinction carried over: the closed sets `KNOWN_CATEGORIES`, `KNOWN_ROLES` (`engineer qa-verifier tech-lead pm-spec all`), `KNOWN_SCOPES` (`loop maintainer`) and `KNOWN_FIELD_NAMES`. The four-role loop in AC8/AC9/AC10 is that closed set, not a population that grows — which is why those criteria carry no re-measurement trigger.
- `templates/prompt-blocks/registry.txt` — **read directly, in full**, and it **corrects a premise relayed into this role's prompt**. The relay said `agents/tech-lead.md` carries **two** marker pairs (`careful-execution`, `playbook-tech-lead`). Measured: the registry registers `agents/tech-lead.md` as a **marker**-mode consumer of **three** blocks — `language.md`, `careful-execution.md` and `playbook-tech-lead.md` — and the file carries three marker pairs accordingly. Distinction carried over: **no registry rule of any mode names the hand-written `## Rules` section**, so the new item needs no canonical block and must sit outside **every** marker region, not merely outside two of them (DP5, AC11).
- `agents/tech-lead.md` — **read directly, in full**. Distinctions carried over: the hand-written `## Rules` section is five plain `- ` bullets of prose placed **before** the first `<!-- BEGIN prompt-block:` marker in the file, and the three marker regions all follow it. That ordering is what AC11 measures against, rather than enumerating region boundaries one at a time.
- `.shell-team/todo.md` — **read directly** (its `## Status flags` section, its empty `## Active` section, and the most recent `## Done` entries). Distinctions carried over: the strict `- [ ] **T-NNN** <title> — `<FLAG>` — spec: <path>.md` line format, that annotations live on indented sub-bullets outside the linter's reach, and the standing precedent that a pm-spec hand-off entry carries **no** `intent-hash` or `freeze-attestation` sub-bullet — the executing side appends both after running every `check:` line live.
- `bin/check-board-headings.sh` — **read directly** at its usage block. Distinction carried over: the invocation form is `check-board-headings.sh <board-file> [--base <ref>]`, the board file being a positional argument, and `--base` resolving through `git merge-base <ref> HEAD` when that succeeds.
- **The host's adjudication of 2026-08-06 — RELAYED, not observed.** The coordinating session holds the primary confirmation; pm-spec was not present for the planning conversation and has no way to read it. Everything in this class is relayed: that candidates 1, 2 and 4 are promoted and 3, 5, 6 and 7 are not; that candidate 1's mechanisation is scoped to one item in the tech-lead's `## Rules` and no further surface; that candidate 2's pm-spec-convention mechanisation ships as **T-1043** under issue **#168** and candidate 4's batched editorial pass ships as **T-1040**; that candidate 7 is dropped because T-1033's general form already covers it; and that `253f4b7` is `develop`'s tip and this branch's real branch point. Assumptions lists each one for re-measurement at the freeze. If the adjudication differs from this relay in any particular, that is a scope question to raise **before** the freeze rather than a wording fix afterwards.

## Goal

<!-- BEGIN intent-block: T-1039 -->

`.shell-team/lessons.md` carries three new active entries — the 2026-08-06 retro's candidates 1, 2 and 4, authored in English at promotion — appended at end of file and changing nothing that was already there: every byte of the corpus up to the base ref's last line is identical to the base blob, and no existing entry is superseded, retired, re-scoped or reworded. The four generated playbook blocks and their four consumer agent files are regenerated from the corpus and gain exactly the new `Rule` lines that each role's `Applies-to` routing entitles it to, and nothing else. `agents/tech-lead.md` additionally gains exactly one hand-written bullet in its `## Rules` section, byte-identical to the text frozen below, sitting outside every one of its three prompt-block marker regions, with those regions still byte-identical to their canonical blocks. The 2026-08-06 retro's candidates 3, 5, 6 and 7 do not enter the corpus. `bin/`, `tests/`, `.github/`, `docs/`, `skills/`, `templates/` beyond the four generated blocks, `agents/` beyond the four consumers, every adopter-facing document, every other task's spec, and `.shell-team/retros/2026-08-06.md` itself are byte-identical to the base ref.

### Settled decisions

Each decision below is resolved; nothing here is left to implementation judgment. Every text this task ships is frozen byte for byte in this section, and every acceptance criterion reads its expected bytes **out of this file** rather than restating them — so a criterion cannot drift from the text it guards.

- **DP1 — the three new entries and the one hand-written Rules item are frozen here, in full.** `bin/playbook-promote.sh` emits exactly the field order `Category`, `Applies-to`, `Scope`, `Status`, `Source`, `Rule`, `Why`, `How to apply` for a `loop`-scoped entry, with one blank line before each appended entry; the criteria assert the resulting **bytes**, not which tool produced them. The wording is frozen rather than left to engineer discretion because a `Rule` line is spliced verbatim into up to four shipped prompt blocks, and because a task whose subject is claim fidelity and accounting discipline cannot leave its own claims to a later paraphrase. `Source` is the retro file for all three.

  Entry **N1** (retro candidate 1):

<!-- frozen-begin: N1 -->
```markdown
## 2026-08-06 — A relayed premise is re-measured at its primary source before it is written into a downstream prompt
- **Category**: verification-discipline
- **Applies-to**: all
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-06.md
- **Rule**: A premise that reached you from a coordinating layer rather than from the artifact itself — another task's check result, a predicted verdict, a count, where a frozen constant lives, which marker regions a file carries — is re-read at its primary source before it is written into the instructions of the role that will act on it, and it is written there as the measured value; where the writing side cannot run the measurement, it labels the premise as relayed at the point it appears, so the receiving role opens the source instead of trusting the sentence. Re-measurement by the receiving role is the second line of defence, not the first.
- **Why**: In one cycle at least four hand-offs carried a premise from a coordinating layer into a downstream role's instructions where it disagreed with the primary artifact: two predictions about the shape of another task's check lines, a conflation of two different populations of the same kind of location, and a claim about which criterion a pair of counts had been frozen into. Every one was caught, but only after the receiving role had already acted on it, and one of them originated in the previous cycle's own retro summary, so the class reached a ratified planning input. The relay is what makes it durable: a premise arrives pre-formed, reads as settled, and is cheaper to repeat than to check, so it travels one hand-off further each time nobody opens the source.
- **How to apply**: Before writing a premise into another role's task prompt, a routing map, or a spec, separate what you measured from what you were handed, and re-read the handed part at its primary source — the file, the blob at the named ref, the tool's own output — replacing the relayed wording with the measured value. Where you have no way to measure it, write the premise as relayed, name the side that holds the primary confirmation, and require the receiving role to re-measure and report the value it found. The receiving role treats an unlabelled premise it cannot trace to a source as relayed anyway, and reports a disagreement it finds as a finding about the hand-off rather than silently correcting it.
```
<!-- frozen-end: N1 -->

  Entry **N2** (retro candidate 2):

<!-- frozen-begin: N2 -->
```markdown
## 2026-08-06 — An accounting criterion is written as a base-relative delta or a value-independent invariant, never as an absolute literal
- **Category**: verification-discipline
- **Applies-to**: engineer, pm-spec, qa-verifier
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-06.md
- **Rule**: A criterion or assertion whose subject is a count — how many elements a set has, how many times a pattern occurs, how many lines a file gained — states that count as a delta measured against a named base ref, or as an invariant that re-derives both sides at run time and compares them, never as an absolute literal typed into the check; the machine-checkable anchor stays required and only its form changes. An absolute literal is correct only for the population that existed when it was typed, it passes on that day and passes review, and once it sits inside frozen text nobody is permitted to repair it.
- **Why**: One task produced three instances of a single coupling defect in a row before the pattern was recognised: a count blind to the population it measured, then a count coupled to the version number of the very document carrying it, then a count coupled to how many repairs the task had already made. Each was repaired individually and each repair created the next, because the underlying shape — an absolute number standing in for a measurement — survived every fix; only after the third consecutive instance did the work switch from point repairs to a full inventory of the class. Nothing distinguishes an absolute-literal accounting check from a sound one until the population moves, which is always after the freeze.
- **How to apply**: When writing a counting criterion, derive both sides at run time: read the named ref's committed blob and compare the current artifact against it, or compute the expected count from something the criterion already pins rather than restating it as a number. Where an absolute literal is genuinely unavoidable, state the reason beside it and attach the observable event that invalidates it. The verifying role re-derives the population independently and treats an absolute-literal accounting check as a finding about the criterion rather than as a red to work around; a role that cannot repair such a literal because it is frozen discloses it and routes a re-freeze instead of bending its own work to match the number.
```
<!-- frozen-end: N2 -->

  Entry **N3** (retro candidate 4):

<!-- frozen-begin: N3 -->
```markdown
## 2026-08-06 — Frozen prose carrying no check line goes stale when what it describes moves, and the proportionate repair is one batched editorial pass
- **Category**: process
- **Applies-to**: pm-spec, qa-verifier
- **Scope**: loop
- **Status**: active
- **Source**: .shell-team/retros/2026-08-06.md
- **Rule**: An explanatory sentence inside a frozen intent block — a design-decision paragraph, a goal sentence, a note naming where a fact came from — carries no check line, so no machine gate re-reads it when the criterion, version or document it describes moves; such a sentence goes stale silently and only a human reader notices. Treat each occurrence as a disclosed finding rather than an immediate re-freeze, and collect the accumulated ones into a single editorial pass, because a per-instance re-freeze spends a full ratification cycle on a sentence whose meaning nobody disputes.
- **Why**: Three frozen sentences went stale in one cycle for the same structural reason: a design-decision sentence came to contradict the criterion it introduced once that criterion was itself re-frozen, a goal sentence named a version number that a neighbouring task's later re-freeze moved past, and a sentence citing a fact's origin named the wrong record. None was reachable by any machine check, all three were found by a reviewer reading prose, and each was routed individually as a fast-follow — three ratification cycles for three sentences, none of which changed what any task did. The staleness is a property of the freeze itself: the criteria a frozen block pins are re-run every round, and the prose beside them is read once.
- **How to apply**: When freezing prose that refers to a criterion, a version number or another document's contents, prefer wording that stays true as those move, and where it cannot, say beside the sentence that it is expected to need an editorial correction later. The verifying role reports a stale frozen sentence as a finding with a correction proposed, not as a wording nit and not as something to fix in place. Whoever owns the spec collects the accumulated corrections and re-freezes them together in one low-risk editorial pass rather than one at a time, and states in that pass which sentences it covers so the backlog is visibly emptied rather than merely reduced.
```
<!-- frozen-end: N3 -->

  The hand-written checklist item **M1**, appended as the last bullet of `agents/tech-lead.md`'s `## Rules` section (DP5). It is one physical line, written in that section's own plain-bullet style rather than as a bolded lead-in:

<!-- frozen-begin: M1 -->
```markdown
- Re-measure a relayed premise before you write it into a downstream prompt. Anything you carry into another role's instructions that reached you from a coordinating layer rather than from the artifact itself — another task's check result, a predicted verdict, a count, where a frozen constant lives, which marker regions a file carries — is re-read at its primary source first and written into the map with the measured value; where you cannot measure it, label it **relayed** in the map and name the side holding the primary confirmation, so the receiving role opens the source instead of trusting the sentence.
```
<!-- frozen-end: M1 -->

- **DP2 — `Scope` and `Applies-to`, decided with reasons.** The retro's `[common]` / `[target-specific]` labels are input, not the decision. All three are `loop`: none depends on a convention private to this repository, and a `maintainer` entry ships to no prompt, which would defeat the point of promoting a rule about how work is handed off. Every included role is justified individually, because each one widens a shipped prompt block by a line.
  - **N1 — `loop`, `all`.** `all` is the four IN roles at once. The actor in the incidents — the coordinating layer that composes a downstream role's prompt — is **not an `Applies-to` token at all**, which is precisely why the rule has to be stated at the level where every role meets it: `tech-lead` writes the Routing Map that becomes another role's drafting instruction, `pm-spec` writes premises into a spec that the engineer acts on, the engineer writes measurements into a hand-off that QA reads, and `qa-verifier` writes verdicts that the review round consumes. Every one of the four is on both sides of a relay. The retro labelled candidate 1 `[common]` and it is: nothing in it depends on this repository's layout.
  - **N2 — `loop`, `engineer, pm-spec, qa-verifier`.** `pm-spec` authors the criterion and is where the originating defect lived. `qa-verifier` re-runs it and must classify an absolute-literal accounting criterion as a finding about the criterion rather than as a red to route around — without that, the rule has no enforcement surface at all, since the author has already frozen the text by the time it is first run. `engineer` is included for a measured reason rather than by analogy: the corpus already ships `2026-07-14 — A same-class completeness AC should be a machine-checkable anchor (e.g. a grep count), not a prose claim` into the engineer's own block, so an engineer's playbook currently carries the recommendation to express a completeness claim as a count with no qualifier on the count's form; N2 is that qualifier, and shipping one without the other is the asymmetry that reproduces the defect from the engineer's side. The engineer also writes counting assertions into test suites and count evidence into hand-offs, which is the same shape outside a spec. `tech-lead` is excluded: it neither authors a criterion nor runs one, and its own accounting appears in a routing map that no gate re-runs.
  - **N3 — `loop`, `pm-spec, qa-verifier`.** `pm-spec` writes the frozen prose and owns the batched editorial pass, so both halves of the rule are its own actions. `qa-verifier` is the in-loop role that reads frozen prose back against the criteria it references and is the only gate positioned to report a stale sentence as a finding rather than as a nit. `engineer` is excluded with a stated reason rather than by omission: an engineer who spots a stale frozen sentence has exactly one permitted action — disclose it and route a re-freeze to pm-spec — and that routing is already covered by the standing re-freeze rules, so adding the role to the block adds a line without adding an action. `tech-lead` is excluded: it authors no frozen prose and verifies none.
- **DP3 — the adjacency audit is in the body below, and its verdict is: no supersede, for any of the three.** Each new entry was compared against the corpus neighbours the retro names and against the entries in the same family that the corpus's own heading index turns up. Every row states why the neighbour is adjacent rather than covering, and one row exists specifically to resolve an apparent contradiction rather than a resemblance: `2026-07-14 — A same-class completeness AC should be a machine-checkable anchor (e.g. a grep count), not a prose claim` **recommends** a count where N2 **constrains the count's form**, and the two are compatible by construction because N2's own `Rule` states that the anchor stays required and only its form changes. No existing entry's `Status`, `Scope`, `Applies-to` or text changes anywhere in this task.
- **DP4 — no `Extended by` pointer is attached to any existing entry, and that is the default taken deliberately.** N1's nearest neighbour, `2026-08-04 — A factual claim is re-measured against the primary artifact before it is written down, inherited or relayed`, is the entry that would most naturally carry one. Its text is frozen as the `E3` block of `.shell-team/specs/T-1027-promote-retro-2026-08-04.md`, so adding a bullet to the corpus copy would desynchronise that frozen block, and re-synchronising it is a **class-B re-freeze of another task's merged spec plus a board-ledger update** — a scope this task does not have and a cost out of proportion to a pointer whose value `bin/check-playbook.sh` never schema-checks anyway. The adjacency is recorded in the audit table below instead, which is where a reader looks for it. This is the default-no branch and not a judgment that the entries are unrelated; the judgment that they are **distinct** is DP3's, and its ground is stated there.
- **DP5 — the mechanisation is exactly one hand-written line in `agents/tech-lead.md`'s `## Rules` section, outside every marker region, and the region count is three rather than the two that were relayed.** `templates/prompt-blocks/registry.txt` registers `agents/tech-lead.md` as a **marker**-mode consumer of three blocks — `language.md`, `careful-execution.md` and `playbook-tech-lead.md` — and registers nothing at all against the hand-written `## Rules` section, so no canonical block is created and none is extended. `M1` is appended as the last bullet of that section, which sits before the first `<!-- BEGIN prompt-block:` marker in the file; AC11 measures that ordering directly rather than enumerating three region boundaries, so the criterion cannot be defeated by a fourth pair being registered later. The three marker regions stay byte-identical to their canonical blocks, which AC7's `bin/check-prompt-sync.sh` run asserts, and the set of marker names in the file is unchanged against the base blob. `agents/tech-lead.md` therefore gains exactly two lines in total: `M1`, and the one generated `Rule` pointer that N1's `Applies-to: all` entitles the tech-lead block to.
- **DP6 — `skills/run/SKILL.md` is out of scope, stated rather than silently skipped.** The retro's Try line names "Routing Map / tech-lead プロンプト構成手順" as the owning surface, and the coordinating session's prompt-composition procedure in `skills/run/SKILL.md` is the parallel surface where the same discipline would apply to the orchestrator's own hand-offs. The adjudicated scope covers the tech-lead item only, so that surface is neither widened here nor ignored: the parallel-surface symmetry table in the body records it as deferred with a reason, and it is named as an input to the next planning round. Widening it in this task would edit a file the scope lock forbids and would put an unadjudicated norm on a shipped surface.
- **DP7 — every accounting criterion below is base-relative or value-independent, and this task self-applies N2 rather than waiting for its convention mechanisation.** No criterion states a corpus size, a heading count, a block bullet count or a per-file line delta as an absolute literal derived from a measurement taken while drafting. Instead: the corpus's expected addition is computed from the frozen blocks' own line counts at run time (AC4); the corpus's untouched region is proved by comparing against the base blob's own line count (AC6); each role's expected delta is derived by reading each frozen block's `Applies-to` field and evaluating the routing rule (AC8, AC9, AC10); each block's expected bullet count is the base blob's count plus that derived delta (AC9). **Exactly one absolute literal survives, with its reason stated: AC7's assertion that the registry yields eight files** — four `playbook-*` blocks and their four consumers — which is a positive control against a registry read that silently produced nothing, and it carries a `- stale-at:` trigger naming the event that invalidates it. Two further literals are structural rather than population counts and are exempt with reasons: the `9` line count each frozen entry block must extract to, which is the field grammar `bin/playbook-promote.sh` emits for a `loop` entry and not a measurement of anything that grows, and the four-role loop in AC8/AC9/AC10, which is `bin/check-playbook.sh`'s closed `KNOWN_ROLES` set minus `all`. T-1033's AC10 pinned the same four blocks with the absolute literals `44 / 32 / 23 / 53`; reproducing that shape in the task that promotes the rule against it would ship the anti-pattern under the rule's own name.
- **DP8 — appends go through `bin/playbook-promote.sh`, one run per entry, in the order N1, N2, N3; `bin/gen-playbook-blocks.sh` is run once afterwards; `M1` is the only hand edit to a shipped file.** That is the documented promotion path and its fail-closed re-validation runs `bin/check-playbook.sh` over a temp copy before the real file is written. No criterion can prove which tool produced a byte, so this is a procedural directive rather than an assertion: the criteria assert the resulting bytes and the file order. No generated block and no marker region is ever hand-edited.
- **DP9 — retro candidates 3, 5, 6 and 7 do not enter the corpus, and their absence is machine-checked rather than merely stated.** Candidates 3, 5 and 6 are mechanised elsewhere in this sprint without being promoted; candidate 7 is dropped because the general form of it already shipped as `2026-08-05 — A background agent is monitored by an active liveness check started at launch, because a completion notification cannot report a stall`, so promoting it again would duplicate a rule rather than add one. AC5 requires the set of `## ` headings the corpus gains against the base ref to be **exactly three** and exactly the keys of N1, N2 and N3, so no fourth entry, candidate-derived or otherwise, can hide in the diff.

## Non-goals

- **Promoting the 2026-08-06 retro's candidate 3, 5, 6 or 7 into the corpus, in any form.** Their dispositions were adjudicated; an argument that any of them belongs here is a relitigation (pre-commitment R1), and for candidate 7 it is additionally a duplicate of a shipped entry.
- **Candidate 2's pm-spec-convention mechanisation and candidate 4's batched editorial pass.** Those are separate tasks in this sprint. This task promotes the two rules into the corpus and does nothing to the conventions or the frozen sentences they will later govern. In particular, **no accumulated stale frozen sentence anywhere in this repository is corrected here.**
- **Any change to `skills/run/SKILL.md`** (DP6), or to any other orchestrator-facing procedure. The parallel-surface symmetry is recorded as deferred, not applied.
- **Any change under `bin/`, `tests/`, `.github/`, `docs/`, `README*`, `CHANGELOG*`, `CONTRIBUTING.md`, `CLAUDE.md`, or `templates/` other than the four generated playbook blocks.** Not one byte. No checker is written or extended, no CI step is added, and `docs/loop-engineering/lessons-import-disposition.md` is untouched — it is the record of one import round and cannot be keyed for a later retro.
- **Editing `agents/scrum-master.md`, `agents/codex-reviewer.md`, `agents/ui-designer.md` or `agents/triage-orchestrator.md`.** None is a consumer of a generated playbook block, and nothing in this adjudication touches those roles' prompts.
- **Any change to `agents/tech-lead.md` beyond `M1` and the one generated `Rule` pointer** — including its frontmatter, its `tools:` list, its `model:` line, its existing `## Rules` bullets, its output-format block, and the interior of any marker region.
- **Editing `.shell-team/retros/2026-08-06.md`.** Per-cycle artifacts are immutable.
- **Any edit to, or re-freeze of, any other task's spec** — `T-1027`, `T-1033`, `T-1036` and every other merged spec included. No `intent-hash`, `intent-ratified` or `freeze-attestation` line belonging to another task is added, removed or altered, and no criterion of another task is re-derived, re-based or widened so that it reads green again.
- **Superseding, retiring, re-scoping or rewording any existing corpus entry, or attaching an `Extended by` pointer to one** (DP3, DP4). The `## Format` section is byte-unchanged and the corpus is a pure append.
- **Converting any fixed scratch path to `mktemp`**, in a CI-wired suite or anywhere else. That is candidate 6's mechanisation and belongs to whichever task owns it.
- **Any `.ja.md` counterpart.** Every file this task writes is agent-read; the repository's language rule gives them no translated copy.
- **Re-opening any candidate's disposition, or the wording of any frozen text here.** Adjudicated at planning; see R1.

## Acceptance criteria

Base ref for every `git` anchor is the literal `253f4b7`. Every check runs from the repository root, names every file it reads, asserts readability before any negative grep, and writes nothing outside a `mktemp -d` scratch directory created with the guarded idiom. **Every criterion that pins frozen text reads its expected bytes out of this spec's own `<!-- frozen-begin: X -->` / `<!-- frozen-end: X -->` fenced blocks** — the fence lines are stripped with `sed '1d;$d'` — rather than restating them inside a shell quote; that is deliberate, since every one of these texts contains apostrophes, backticks and em-dashes that no single shell quoting style survives intact. Per DP7, no criterion states a count that was measured while drafting.

- [ ] **AC1** Entry **N1** is present in `.shell-team/lessons.md` exactly as frozen: the region from its heading to the line before the next `## ` heading, with trailing blank lines dropped, is byte-identical to the `N1` frozen block (heading plus eight field bullets — the shape `bin/playbook-promote.sh` emits for a `loop` entry), and its `Applies-to` is `all` and its `Scope` is `loop`, asserted as explicit clauses so DP2's routing decision is machine-visible. Positive control: the extracted block is non-empty and carries a `- **Rule**: ` bullet, so an extraction failure cannot read as a clean pass.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039a.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: N1 -->' -v e='<!-- frozen-end: N1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: all' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC2** Entry **N2** is present in `.shell-team/lessons.md` exactly as frozen, same extraction and same nine-line shape as AC1, and its `Applies-to` is `engineer, pm-spec, qa-verifier` — the three-role decision DP2 justified role by role, asserted here so a silent narrowing to two roles fails rather than passing quietly.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039b.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: N2 -->' -v e='<!-- frozen-end: N2 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: engineer, pm-spec, qa-verifier' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC3** Entry **N3** is present in `.shell-team/lessons.md` exactly as frozen, same extraction and same nine-line shape, with `Applies-to: pm-spec, qa-verifier` and `Scope: loop` asserted as explicit clauses (DP2) — so a widening to the engineer or the tech-lead, which DP2 rejected with reasons, fails here rather than shipping unnoticed into two more prompt blocks.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039c.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: N3 -->' -v e='<!-- frozen-end: N3 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/want"; test "$(grep -c . "$T/want" || true)" = "9" || rc=1; grep -qF -- '- **Rule**: ' "$T/want" || rc=1; h=$(head -n 1 "$T/want"); test -n "$h" || rc=1; awk -v h="$h" '$0==h{f=1} f&&/^## /&&$0!=h{exit} f{print}' "$L" | awk 'NF{for(i=0;i<b;i++)print ""; b=0; print; next}{b++}' > "$T/got"; test -s "$T/got" || rc=1; cmp -s "$T/want" "$T/got" || rc=1; grep -qxF -- '- **Applies-to**: pm-spec, qa-verifier' "$T/got" || rc=1; grep -qxF -- '- **Scope**: loop' "$T/got" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC4** The whole corpus is schema-valid — `bin/check-playbook.sh` green over the resolved lessons path, which is also what proves no pointer graph was disturbed — and its diff against the base ref adds exactly the number of lines the three frozen blocks account for and **deletes exactly zero**. Per DP7 the expected addition is **not** a literal: it is computed at run time as the sum of each frozen block's own line count plus one blank separator each, so the criterion cannot go stale against its own frozen text. Positive controls: each block must extract to nine lines, and the computed expectation must be greater than zero.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; test -r "$SP" || exit 1; L=$(bash bin/team-paths.sh --get lessons) || exit 1; test -n "$L" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039d.XXXXXX") || exit 1; bash bin/check-playbook.sh "$L" >/dev/null || rc=1; want=0; for k in N1 N2 N3; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; n=$(grep -c . "$T/$k" || true); test "$n" = "9" || rc=1; want=$((want + n + 1)); done; test "$want" -gt 0 || rc=1; ns=$(git diff --numstat 253f4b7 -- "$L"); test -n "$ns" || rc=1; add=$(printf '%s\n' "$ns" | awk '{print $1}'); del=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$add" = "$want" || rc=1; test "$del" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC5** Retro candidates 3, 5, 6 and 7 are genuinely absent (DP9): the set of `## ` headings the corpus gained since the base ref is **exactly three**, and is exactly the keys of N1, N2 and N3, compared as sets with `comm -13` rather than by counting. This criterion is **merge-point-scoped and expected to go stale once later work lands on `253f4b7`**; do not widen its base-ref resolution and do not re-derive it per rework round. Positive control: the base blob's heading list must be non-empty, so an unreadable base blob cannot read as "nothing was added".
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039e.XXXXXX") || exit 1; git show 253f4b7:.shell-team/lessons.md > "$T/base" || rc=1; test -s "$T/base" || rc=1; grep '^## ' "$T/base" | sort > "$T/hb"; grep '^## ' "$L" | sort > "$T/hn"; test -s "$T/hb" || rc=1; test -s "$T/hn" || rc=1; comm -13 "$T/hb" "$T/hn" > "$T/hadd"; test "$(grep -c . "$T/hadd" || true)" = "3" || rc=1; for k in N1 N2 N3; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' | head -n 1 > "$T/k$k"; test -s "$T/k$k" || rc=1; grep -qxFf "$T/k$k" "$T/hadd" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC6** The corpus change is a **pure append and nothing else**, proved at the byte level rather than by counting: the first *N* lines of the current file, *N* being the base blob's own line count read at run time, are byte-identical to the base blob; and everything after line *N* is exactly a blank line followed by N1, a blank line followed by N2, and a blank line followed by N3, in that order. This is the criterion that forecloses a supersede, a reword, a re-scope, an `Extended by` pointer or a `## Format` edit anywhere in the file (DP3, DP4, Non-goals), and it holds no absolute literal at all. Merge-point-scoped, as AC5. Positive controls: the base blob must be non-empty, *N* must be greater than zero, and the constructed expectation must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; L=.shell-team/lessons.md; test -r "$SP" || exit 1; test -r "$L" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039f.XXXXXX") || exit 1; git show 253f4b7:.shell-team/lessons.md > "$T/base" || rc=1; test -s "$T/base" || rc=1; bl=$(grep -c '' "$T/base" || true); test "$bl" -gt 0 || rc=1; head -n "$bl" "$L" > "$T/pre"; cmp -s "$T/base" "$T/pre" || rc=1; tail -n +"$((bl + 1))" "$L" > "$T/tail"; : > "$T/exp"; for k in N1 N2 N3; do printf '\n' >> "$T/exp"; awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' >> "$T/exp"; done; test -s "$T/exp" || rc=1; cmp -s "$T/exp" "$T/tail" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC7** The committed generated blocks and their consumers are exactly what the corpus regenerates: the eight files the registry names for the four `playbook-*` blocks are copied into a scratch tree, `bin/gen-playbook-blocks.sh` is run against it, and every one of the eight compares byte-identical (the CI dogfood step's own procedure, re-run here); `bin/check-prompt-sync.sh` is green over the whole registry, which is also what asserts every marker region in every consumer — including `agents/tech-lead.md`'s three — still matches its canonical block. Positive control, and the one absolute literal DP7 permits: the registry must yield exactly eight files, so a registry read that produced nothing cannot pass. Expect a non-fatal block-size warning on stderr for two roles — it is not a failure (see Assumptions).
  - check: rc=0; test -r templates/prompt-blocks/registry.txt || exit 1; L=$(bash bin/team-paths.sh --get lessons) || exit 1; test -n "$L" || exit 1; test -r "$L" || exit 1; S=$(mktemp -d "${TMPDIR:-/tmp}/t1039g.XXXXXX") || exit 1; files=$(awk '$1 == "marker" && $2 ~ /^playbook-/ { for (i = 3; i <= NF; i++) print $i; print "templates/prompt-blocks/" $2 }' templates/prompt-blocks/registry.txt | sort -u); test "$(printf '%s\n' "$files" | grep -c . || true)" = "8" || rc=1; for f in $files; do mkdir -p "$S/$(dirname "$f")" || rc=1; cp "$f" "$S/$f" || rc=1; done; mkdir -p "$S/templates/prompt-blocks" || rc=1; cp templates/prompt-blocks/registry.txt "$S/templates/prompt-blocks/registry.txt" || rc=1; bash bin/gen-playbook-blocks.sh --root "$S" --lessons "$L" >/dev/null 2>&1 || rc=1; for f in $files; do cmp -s "$f" "$S/$f" || rc=1; done; rm -rf "$S"; bash bin/check-prompt-sync.sh >/dev/null || rc=1; test "$rc" -eq 0
  - stale-at: a task registers or removes a `playbook-<role>` marker rule in `templates/prompt-blocks/registry.txt`, at which point the declared eight-file population no longer describes the registry this criterion reads and the literal must be re-measured against it.
- [ ] **AC8** The propagation matrix is exactly what DP2 decided, in both the canonical block and its consumer agent file, and the expectation is **derived rather than transcribed**: for each of the four IN roles and each of the three frozen entries, the criterion reads that entry's own `Applies-to` field out of this spec, evaluates the routing rule (`all`, or the role named in the comma list), and requires the entry's `Rule` line to appear exactly once in `templates/prompt-blocks/playbook-<role>.md` and exactly once in `agents/<role>.md` when it qualifies, and exactly zero times in each when it does not. A block regenerated without being spliced into its consumer, or a consumer spliced without its block being regenerated, both fail. The four-role loop is `bin/check-playbook.sh`'s closed `KNOWN_ROLES` set minus `all`, not a population that grows, so it carries no re-measurement trigger. Positive controls: each frozen block must be non-empty, and each entry's `Applies-to` and `Rule` values must extract non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; test -r "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039h.XXXXXX") || exit 1; for k in N1 N2 N3; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test -s "$T/$k" || rc=1; done; for r in engineer qa-verifier tech-lead pm-spec; do BF="templates/prompt-blocks/playbook-$r.md"; AF="agents/$r.md"; test -r "$BF" || rc=1; test -r "$AF" || rc=1; for k in N1 N2 N3; do ap=$(sed -n 's/^- \*\*Applies-to\*\*: //p' "$T/$k"); test -n "$ap" || rc=1; case "$ap" in all) x=1 ;; *) case ",${ap// /}," in *",$r,"*) x=1 ;; *) x=0 ;; esac ;; esac; ru=$(sed -n 's/^- \*\*Rule\*\*: //p' "$T/$k"); test -n "$ru" || rc=1; n=$(grep -cF -- "$ru" "$BF" || true); m=$(grep -cF -- "$ru" "$AF" || true); test "$n" = "$x" || rc=1; test "$m" = "$x" || rc=1; done; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC9** The four generated blocks moved by exactly the derived amount and nothing else: for each role, the expected delta is computed at run time from the three frozen blocks' `Applies-to` fields, and then `git diff --numstat 253f4b7` for that block must show exactly that many lines added and **exactly zero deleted**, while the block's `^- ` bullet count must equal the base blob's own bullet count plus the same derived delta. A deletion count of zero is what proves no existing bullet was modified, since a modification renders as one addition plus one deletion; the base-plus-delta form is what replaces the absolute size literals T-1033's AC10 used (DP7). Merge-point-scoped, as AC5. Positive controls: each numstat row must be non-empty, each base blob must be non-empty, and each derived delta must be at least one (every one of the four roles gains at least N1's line).
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; test -r "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039i.XXXXXX") || exit 1; for k in N1 N2 N3; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test -s "$T/$k" || rc=1; done; for r in engineer qa-verifier tech-lead pm-spec; do BF="templates/prompt-blocks/playbook-$r.md"; test -r "$BF" || rc=1; d=0; for k in N1 N2 N3; do ap=$(sed -n 's/^- \*\*Applies-to\*\*: //p' "$T/$k"); test -n "$ap" || rc=1; case "$ap" in all) d=$((d + 1)) ;; *) case ",${ap// /}," in *",$r,"*) d=$((d + 1)) ;; esac ;; esac; done; test "$d" -ge 1 || rc=1; ns=$(git diff --numstat 253f4b7 -- "$BF"); test -n "$ns" || rc=1; a=$(printf '%s\n' "$ns" | awk '{print $1}'); dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$a" = "$d" || rc=1; test "$dl" = "0" || rc=1; git show "253f4b7:$BF" > "$T/b" || rc=1; test -s "$T/b" || rc=1; bc=$(grep -c '^- ' "$T/b" || true); cc=$(grep -c '^- ' "$BF" || true); test "$cc" = "$((bc + d))" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC10** The four consumer agent files moved by exactly the derived amount and nothing else: same run-time derivation as AC9, and `git diff --numstat 253f4b7` for each `agents/<role>.md` must show exactly the derived delta lines added — **plus exactly one more for `agents/tech-lead.md` alone**, that one being `M1` (DP5) — and **exactly zero deleted** in every case. The tech-lead's `+1` is stated as a routing consequence rather than a measurement: it is the count of hand-written items this task adds, which DP5 fixes at one. Merge-point-scoped, as AC5. Positive control: each numstat row must be non-empty, so an untouched file fails rather than passing silently.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; test -r "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039j.XXXXXX") || exit 1; for k in N1 N2 N3; do awk -v b="<!-- frozen-begin: $k -->" -v e="<!-- frozen-end: $k -->" '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/$k"; test -s "$T/$k" || rc=1; done; for r in engineer qa-verifier tech-lead pm-spec; do AF="agents/$r.md"; test -r "$AF" || rc=1; d=0; for k in N1 N2 N3; do ap=$(sed -n 's/^- \*\*Applies-to\*\*: //p' "$T/$k"); test -n "$ap" || rc=1; case "$ap" in all) d=$((d + 1)) ;; *) case ",${ap// /}," in *",$r,"*) d=$((d + 1)) ;; esac ;; esac; done; if [ "$r" = "tech-lead" ]; then d=$((d + 1)); fi; test "$d" -ge 1 || rc=1; ns=$(git diff --numstat 253f4b7 -- "$AF"); test -n "$ns" || rc=1; a=$(printf '%s\n' "$ns" | awk '{print $1}'); dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$a" = "$d" || rc=1; test "$dl" = "0" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC11** The mechanisation item is where DP5 put it: `M1` extracts to exactly one line, occurs **exactly once** in `agents/tech-lead.md`, and occurs exactly once inside the region running from that file's `## Rules` heading to the line before its **first** `<!-- BEGIN prompt-block:` marker — which is what proves it sits outside **every** marker region rather than outside a hard-coded number of them, correcting the relayed count of two. The set of prompt-block marker names in the file is byte-identical to the set in the base blob, so a region cannot have been added, removed or renamed. Positive controls: `## Rules` must occur exactly once, at least one marker must be found, the marker heading must follow the `## Rules` heading, the extracted region must be non-empty, and the base blob's marker-name list must be non-empty.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; A=agents/tech-lead.md; test -r "$SP" || exit 1; test -r "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039k.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: M1 -->' -v e='<!-- frozen-end: M1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/m"; test "$(grep -c . "$T/m" || true)" = "1" || rc=1; test "$(grep -cxFf "$T/m" "$A" || true)" = "1" || rc=1; grep -n '^## Rules$' "$A" > "$T/rh" || rc=1; test "$(grep -c . "$T/rh" || true)" = "1" || rc=1; rl=$(cut -d: -f1 < "$T/rh"); test -n "$rl" || rc=1; grep -n '^<!-- BEGIN prompt-block: ' "$A" > "$T/mk" || rc=1; test "$(grep -c . "$T/mk" || true)" -ge 1 || rc=1; fm=$(head -n 1 "$T/mk" | cut -d: -f1); test -n "$fm" || rc=1; test "${rl:-0}" -lt "${fm:-0}" || rc=1; sed -n "$((rl + 1)),$((fm - 1))p" "$A" > "$T/sec"; test -s "$T/sec" || rc=1; test "$(grep -cxFf "$T/m" "$T/sec" || true)" = "1" || rc=1; git show "253f4b7:$A" > "$T/b" || rc=1; test -s "$T/b" || rc=1; sed -n 's/^<!-- BEGIN prompt-block: \(.*\) -->$/\1/p' "$T/b" | sort > "$T/n0"; sed -n 's/^<!-- BEGIN prompt-block: \(.*\) -->$/\1/p' "$A" | sort > "$T/n1"; test -s "$T/n0" || rc=1; cmp -s "$T/n0" "$T/n1" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC12** `agents/tech-lead.md`'s diff against the base ref consists of **exactly two added lines and zero deleted**, and both are identified individually rather than counted: one is byte-identical to `M1`, and the other is byte-identical to the generated pointer line that `bin/gen-playbook-blocks.sh` emits for N1, reconstructed at run time from N1's own frozen bytes in the shape `- <rule> (<pointer-path>, <entry-key>)` — the entry key being N1's heading with the leading `## ` removed, which is exactly the `<date> — <title>` text the generator re-emits, so **no em-dash is ever matched by a pattern** (see Assumptions) — with the pointer path resolved through `bin/team-paths.sh`. This is the mechanical half of the Non-goal forbidding any other edit to that file: its frontmatter, its `tools:` list, its `model:` line, its existing `## Rules` bullets and its output-format block cannot have moved. Merge-point-scoped, as AC5. Positive controls: the reconstruction's rule and entry-key parts must each be non-empty and the key must differ from the raw heading (proving the `## ` prefix was actually stripped), and the added-line set must be exactly two lines before comparison.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; A=agents/tech-lead.md; test -r "$SP" || exit 1; test -r "$A" || exit 1; LP=$(bash bin/team-paths.sh --get lessons) || exit 1; test -n "$LP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039l.XXXXXX") || exit 1; awk -v b='<!-- frozen-begin: M1 -->' -v e='<!-- frozen-end: M1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/m"; test "$(grep -c . "$T/m" || true)" = "1" || rc=1; awk -v b='<!-- frozen-begin: N1 -->' -v e='<!-- frozen-end: N1 -->' '$0==b{f=1;next} $0==e{f=0} f' "$SP" | sed '1d;$d' > "$T/n1"; test -s "$T/n1" || rc=1; ru=$(sed -n 's/^- \*\*Rule\*\*: //p' "$T/n1"); test -n "$ru" || rc=1; hd=$(head -n 1 "$T/n1"); test -n "$hd" || rc=1; ky=${hd#'## '}; test -n "$ky" || rc=1; test "$ky" != "$hd" || rc=1; printf -- '- %s (%s, %s)\n' "$ru" "$LP" "$ky" > "$T/gl"; ns=$(git diff --numstat 253f4b7 -- "$A"); test -n "$ns" || rc=1; dl=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$dl" = "0" || rc=1; git diff -U0 253f4b7 -- "$A" | grep '^+' | grep -v '^+++' > "$T/plus"; test "$(grep -c . "$T/plus" || true)" = "2" || rc=1; sed 's/^+//' "$T/plus" | sort > "$T/pl"; cat "$T/m" "$T/gl" | sort > "$T/wl"; cmp -s "$T/wl" "$T/pl" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC13** The board carries this task's entry in the strict line format and nothing on it was disturbed: `bin/check-handoff.sh` green over the resolved board, `bin/check-board-headings.sh <board> --base 253f4b7` green (a heading replacement or deletion is caught here rather than at review), `bin/check-intent.sh` reporting aligned for this spec against that board, the board's `git diff --numstat` deletion column exactly **zero** — so no other task's ledger line, flag or prose was replaced — and exactly one board line matching `- [<mark>] **T-1039** <title> — `<FLAG>` — spec: .shell-team/specs/T-1039-promote-retro-2026-08-06.md`. Merge-point-scoped, as AC5. Positive control: the numstat row must be non-empty, so an untouched board fails.
  - check: rc=0; SP=.shell-team/specs/T-1039-promote-retro-2026-08-06.md; test -r "$SP" || exit 1; B=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$B" || exit 1; test -r "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1039m.XXXXXX") || exit 1; bash bin/check-handoff.sh "$B" >/dev/null || rc=1; bash bin/check-board-headings.sh "$B" --base 253f4b7 >/dev/null || rc=1; bash bin/check-intent.sh "$SP" "$B" >/dev/null || rc=1; ns=$(git diff --numstat 253f4b7 -- "$B"); test -n "$ns" || rc=1; del=$(printf '%s\n' "$ns" | awk '{print $2}'); test "$del" = "0" || rc=1; n=$(grep -cE '^- \[[ x]\] \*\*T-1039\*\* .+ — .[A-Z_]+. — spec: \.shell-team/specs/T-1039-promote-retro-2026-08-06\.md$' "$B" || true); test "$n" = "1" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC14** Scope, records and hygiene lock. The branch's changed-and-added file set (tracked changes ∪ untracked additions, since the records are untracked until they are written) contains the eleven required files and **nothing** outside the fourteen-path allow-list in Notes for engineer — so `bin/`, `tests/`, `.github/`, `docs/`, `skills/run/SKILL.md`, `CONTRIBUTING.md`, `CLAUDE.md`, every other agent file, every other task's spec and the retro file itself are all byte-identical by construction; `.shell-team/provenance/T-1039.md` and `.shell-team/interventions/T-1039.md` both exist and are conformant under their own checkers; and the branch diff is clean under `bin/check-pii-shapes.sh`. Merge-point-scoped, as AC5 — once later work lands, `253f4b7` no longer describes this task's merge point. Positive control: an empty changed-set fails.
  - check: rc=0; changed=$( { git diff --name-only 253f4b7; git ls-files --others --exclude-standard; } | sort -u ); test -n "$changed" || rc=1; for f in .shell-team/lessons.md templates/prompt-blocks/playbook-engineer.md templates/prompt-blocks/playbook-qa-verifier.md templates/prompt-blocks/playbook-tech-lead.md templates/prompt-blocks/playbook-pm-spec.md agents/engineer.md agents/qa-verifier.md agents/tech-lead.md agents/pm-spec.md .shell-team/specs/T-1039-promote-retro-2026-08-06.md .shell-team/todo.md; do printf '%s\n' "$changed" | grep -qxF -- "$f" || rc=1; done; extra=$(printf '%s\n' "$changed" | grep -vxF -e '.shell-team/lessons.md' -e 'templates/prompt-blocks/playbook-engineer.md' -e 'templates/prompt-blocks/playbook-qa-verifier.md' -e 'templates/prompt-blocks/playbook-tech-lead.md' -e 'templates/prompt-blocks/playbook-pm-spec.md' -e 'agents/engineer.md' -e 'agents/qa-verifier.md' -e 'agents/tech-lead.md' -e 'agents/pm-spec.md' -e '.shell-team/specs/T-1039-promote-retro-2026-08-06.md' -e '.shell-team/todo.md' -e '.shell-team/provenance/T-1039.md' -e '.shell-team/reviews/T-1039.md' -e '.shell-team/interventions/T-1039.md' | grep -c . || true); test "$extra" = "0" || rc=1; if [ -r .shell-team/provenance/T-1039.md ]; then bash bin/check-provenance.sh .shell-team/provenance/T-1039.md >/dev/null || rc=1; else rc=1; fi; if [ -r .shell-team/interventions/T-1039.md ]; then bash bin/check-interventions.sh --task T-1039 -- .shell-team/interventions/T-1039.md >/dev/null || rc=1; else rc=1; fi; bash bin/check-pii-shapes.sh --base 253f4b7 >/dev/null || rc=1; test "$rc" -eq 0
- [ ] **AC15** Runtime, `SKIP` by design — no command can prove that a command was run, or that a claim was checked against its source. Five things are done and reported in the board hand-off. **(a)** Every applicable step in `.github/workflows/check-handoff.yml` was run locally, in workflow order, with each step declared inapplicable named individually together with its reason — and, per the corpus's `2026-08-04` byte-identity entry, the "everything else is byte-identical" rationale is **split**: any step that enumerates a directory or a glob is **run** rather than reasoned about, because this task adds new files under `.shell-team/provenance/`, `.shell-team/reviews/` and `.shell-team/interventions/`. **(b)** A producer-run mutation self-check on scratch copies outside the checkout, each mutation observed red then restored and observed green: change one word inside N1's `Rule` in the corpus (AC1 red, AC6 red, AC8 red on all four roles, AC12 red); change N2's `Applies-to` to drop `engineer` and regenerate (AC2 red, AC8 red on the engineer cells, AC9/AC10 red on the engineer delta); insert one of the three entries in the **middle** of the corpus instead of at the end (AC6 red while AC1–AC5 may stay green — this is the probe aimed at AC6's own blind spot); append a fourth entry (AC4 red, AC5 red, AC6 red); hand-edit one bullet of a generated block (AC7 red, AC9 red on that file's deletion column); move `M1` inside the `playbook-tech-lead` marker region (AC7 red, AC11 red); reword `M1` in the file but not in this spec (AC11 red, AC12 red). **(c)** Per the corpus's `2026-08-04` summary-fidelity entry, every clause of the three new entries' `Why` fields was read back against the records the retro names for that candidate — `.shell-team/interventions/T-1033.md`, `T-1034.md`, `T-1035.md`, `T-1036.md` and `.shell-team/reviews/T-1033.md`, `T-1035.md`, `T-1036.md`, none of which pm-spec opened (see `## Summarized sources`) — and the hand-off states, per entry, which clauses were confirmed against a record and which were generalised deliberately, including whether the retro's own "at least four" and "three consecutive within one task" distinctions survive the generalisation. **(d)** Per the corpus's `2026-08-04` relayed-claim entry, every relayed claim listed in Assumptions was re-measured before the freeze and the hand-off states the measured value beside each. **(e)** The `## Summarized sources` per-source report: for each named source, whether the reporting role opened it, and what the source said about the distinction it is cited for — with the host adjudication's relayed-not-observed status restated rather than quietly dropped.

## Input space

**Reachable input classes** — what the criteria above really read, and what the edit must handle correctly:

- `.shell-team/lessons.md` as committed at the base ref: a UTF-8 markdown file whose canonical `## YYYY-MM-DD — <title>` entry set is **re-derived by every criterion from the base blob rather than declared here** (DP7 applies to this section too). It contains at least one `superseded` entry with a resolving `Superseded-by`, at least one `maintainer`-scoped entry carrying a `Bound-in` bullet, and at least one `active` entry carrying an `Extended by` bullet; field values routinely contain apostrophes, backticks, em-dashes, double quotes, parentheses and angle-bracket placeholders; one fenced ```` ```markdown ```` example under `## Format` carries a synthetic `## YYYY-MM-DD — <short title>` heading that must stay invisible to both the schema checker and the block generator, and the `Superseded-by` / `Extended by` bullets are documented outside that fenced example on purpose.
- The three appended entries in `bin/playbook-promote.sh`'s emitted field order, appended at end of file with one blank line before each, and no `Bound-in` bullet on any of them because all three are `loop`-scoped. No line anywhere else in the file changes.
- `templates/prompt-blocks/registry.txt`: four `marker`-mode `playbook-*` rules naming one consumer each, plus `marker` and `contain` rules for other blocks; `agents/tech-lead.md` appears as a consumer under three `marker` rules and at least one `contain` rule.
- `agents/tech-lead.md` as committed: YAML frontmatter, hand-written prose including a `## Rules` section of plain `- ` bullets, and three prompt-block marker pairs all located after that section. Exactly two lines are added — one inside the `playbook-tech-lead` region by the generator, one at the end of `## Rules` by hand — and none is removed.
- The other three consumer agent files, each carrying exactly one `playbook-<role>` marker pair whose interior is machine-written and whose bullets are appended to at end of block.
- The board as committed: `## Active` empty, `## Done` carrying more than thirty entries, every entry line beginning with `- `, sub-bullets indented by two spaces, and existing `intent-hash` / `intent-ratified` / `freeze-attestation` records belonging to other tasks that this task must not touch. A file large enough that a whole-file read is not a sensible verification step, which is why AC13 reads `git diff --numstat`'s columns.
- `git` plumbing output for the literal `253f4b7`: `diff --name-only`, `diff --numstat`, `diff -U0`, `show <ref>:<path>`, and `ls-files --others --exclude-standard`. Every base-ref read goes through `git show` against the committed blob, never through a working-tree read standing in for the ref.
- `.shell-team/retros/2026-08-06.md` as a **read-only** input: its `## Lesson candidates` section in the operator's language, which the engineer reads and never edits.

**Out-of-scope synthetic extremes** — declined deliberately; a finding grounded only in one of these is out of contract:

- **NUL bytes, CRLF line endings, non-UTF-8 bytes, control characters or reserved prompt-block marker strings inside a corpus field value, an entry title, or `M1`.** `bin/check-playbook.sh` owns that class fail-closed already and this task adds no parser; AC4 runs it and AC7 runs the generator.
- **Supersede and `Extended by` pointer graphs.** This task creates no pointer at all: no chain, no cycle, no dangling reference, no duplicate key, no `loop`-superseded-by-`maintainer` direction. An argument about pointer mechanics is an argument about DP3's or DP4's verdict, both of which are settled with reasons.
- **Ever-larger generated blocks, and the 40-line block-size threshold warning.** It already fires at the base ref for the engineer and pm-spec blocks and both grow here; it is documented as deliberately non-fatal, and the standing deferral says not to launch a shortening sweep on that signal alone. A finding that the blocks are too long is a request to re-open that deferral.
- **A fifth playbook block, a fifth IN role, or an `Applies-to` token outside `bin/check-playbook.sh`'s closed `KNOWN_ROLES` set.** The four-role loops read that closed set; AC7 carries the one re-measurement trigger that a registry change would invalidate.
- **A fourth prompt-block marker pair appearing in `agents/tech-lead.md`, or a marker pair being placed before its `## Rules` section.** AC11 measures against the *first* marker in the file and against the base blob's marker-name set, which covers a pair being added, removed or renamed; a hypothetical reordering that puts a marker region above `## Rules` is a different file layout than the one committed and is not protected here.
- **Any re-freeze in this task, of this spec or any other.** Exactly zero re-freezes happen: no `intent-ratified` line is written, no other task's `intent-hash` is replaced, and no vK→vK+1 transition occurs anywhere.
- **Any board entry other than T-1039's.** Every other task's ledger, flag and prose is untouched; a finding about a neighbour's stale record belongs to that neighbour's task, and AC13's zero-deletion clause is the mechanical guarantee.
- **Additional lesson candidates, from this retro or any other**, and any re-adjudication of the seven. That set is closed here (DP9, R1).
- **Adversarial `mktemp` or `$TMPDIR` behaviour** (a hostile temp dir, a symlink race, a path containing whitespace), and non-bash shells. `bin/check-acs.sh` invokes each check through `bash -c`, and the guarded idiom's contract is "exit non-zero when `mktemp` fails", nothing stronger.
- **Non-GNU `grep` / `awk` / `comm` / `sed` / `diff` variants beyond the POSIX behaviour these lines rely on.** The executing side confirms the host's own tools at freeze time (Assumptions); an argument about a toolchain not present on this host is not a finding.
- **Any input reaching `bin/` itself.** No tool's behaviour, default or validation changes in this task; `bin/` is byte-identical (AC14).

<!-- END intent-block: T-1039 -->

## Adjacent-entry audit (DP3)

Every corpus neighbour the retro names for the three promoted candidates, plus the entries in the same families that the corpus's own heading index turns up. Default verdict is **no supersede**; each row states why the neighbour is adjacent rather than covering. No row changed the default.

| New entry | Neighbour | Verdict | Reason |
|---|---|---|---|
| N1 | `2026-08-04 — A factual claim is re-measured against the primary artifact before it is written down, inherited or relayed` | **no supersede** | The load-bearing row. That entry's relay clause reads "a summary of facts relayed by a coordinating layer is **hearsay until the receiving role opens** the artifact itself" — an obligation on the **receiver**, discharged after the relay has already happened. N1's obligation is the **writer's**, discharged **before** the premise is written into the downstream prompt at all, and it covers a surface that entry does not reach: an agent's task prompt is not a durable artifact, so a premise can travel through one without ever triggering that entry's "reaches a durable artifact only after" test. Both are needed and they fire at different moments; N1 says so explicitly in its own `Rule` ("the second line of defence, not the first") so a reader of either block cannot mistake one for the other. |
| N1 | `2026-08-04 — A summary of another document preserves the source's own distinctions, checked against the source and not the summary` | **no supersede** | Same family — claim fidelity — different defect. That entry governs a **summary of a document's facts** and its remedy is to open the source and match clause by clause; the failure it names is a *flattened distinction* in text that is otherwise accurate. N1 governs a **premise carried into an instruction** and the failure it names is a value that is simply *wrong*. A premise that flattens nothing and cites nothing can still be false, which is exactly the case that entry cannot see. |
| N1 | `2026-07-12 — Rework instructions should require a batch verification grounded in the input's canonical contract, not a point-fix transcription` | **no supersede** | Closest of the routing-map rows and still orthogonal. That entry governs **what a rework instruction asks for** (a batch grounded in the producer's contract rather than a transcribed point fix); N1 governs **whether the facts in any instruction were measured**. A perfectly batch-shaped rework instruction can rest on an unmeasured relayed premise, and a point-fix instruction can rest on a measured one. |
| N2 | `2026-07-14 — A same-class completeness AC should be a machine-checkable anchor (e.g. a grep count), not a prose claim` | **no supersede**, and the tension is resolved in N2's own text | This is the one row that answers an apparent contradiction rather than a resemblance. That entry says a completeness criterion must be a **machine-checkable count instead of prose**; N2 says a counting criterion must express that count as a **base-relative delta or a re-derived invariant instead of an absolute literal**. They stack: the anchor stays required, and N2 constrains its form. N2's `Rule` states that stacking in the shipped sentence ("the machine-checkable anchor stays required and only its form changes") so neither block can be read as revoking the other, and DP2 includes the engineer in N2's `Applies-to` precisely because that entry already ships to the engineer's block and would otherwise arrive unqualified. |
| N2 | `2026-08-03 — A completeness claim attaches its accounting: population total, selection method, and exclusion reasons (supersedes the bulk-fix inventory entry)` | **no supersede** | That entry fires for a **completeness claim** and requires an accounting to be **attached** at the claim site — population total, selection method, exclusion reasons. It says nothing about how the numbers in that accounting are expressed, and a fully accounted claim whose total is an absolute literal is exactly the defect N2 names. N2 fires for any counting criterion, complete-set claim or not, and constrains the count's form rather than its documentation. |
| N2 | `2026-07-12 — Regression fixtures must cross the boundary and assertions must check final state, not that the mechanism fired` | **no supersede** | Adjacent in spirit — both prefer an assertion over a proxy — and different in subject. That entry is about **what an assertion observes** (the final state rather than the mechanism firing); N2 is about **how a count is written down** once you have decided to observe it. A final-state assertion with a hardcoded absolute count satisfies that entry and violates N2. |
| N3 | `2026-08-02 — A spec that ships runnable commands verifies them across an execution-context matrix` | **no supersede** | That entry governs a deliverable's **runnable commands** and requires each to be executed in every context cell; its whole premise is that the artifact in question *can* be run. N3 governs the frozen prose that carries **no runnable form at all**, which is why no context matrix reaches it and why the remedy is editorial rather than executional. |
| N3 | `2026-07-13 — "Settled, won't revisit" configuration decisions are conditional on a fixed environment — pair them with an explicit re-evaluation trigger` | **no supersede** | The nearest structural cousin: both are about a written decision going stale as its environment moves, and both prescribe making that staleness visible. The difference is what is missing. That entry's gap is a **stated trigger** for re-evaluating a decision whose content is still what the author meant; N3's gap is that a sentence **no longer says what its author meant** and no machine gate can tell, so its remedy is a batched correction pass rather than a trigger. A frozen sentence with a perfect re-evaluation trigger still goes stale silently if nobody reads it. |
| N3 | `2026-07-26 — Run a spec's `check:` lines live and reconcile them before recording an intent hash` | **no supersede** | Clean complement, and worth the row because the two together cover the frozen block completely. That entry governs the frozen block's **`check:` lines** and requires them run live before the hash is recorded; N3 governs the frozen block's **prose, which has no `check:` line** and therefore cannot be reconciled that way at any moment, before the freeze or after. |
| — | `2026-08-05 — A background agent is monitored by an active liveness check started at launch, because a completion notification cannot report a stall` | **not applicable — this is why candidate 7 is dropped** | The retro's candidate 7 records that the watchdog mechanism fired in practice for the first time and that the documented procedure worked. Its general form shipped last cycle as this entry, whose `Rule` already states the invariant (an active liveness check exists, starts with the agent, and a stall verdict is measured rather than inferred). A confirming instance is evidence *for* an entry, not a second entry, so promoting it would duplicate a shipped rule; the confirmation belongs in the retro, where it is. |

## Parallel-surface symmetry table (DP6)

The norm N1 states is written onto surfaces that compose downstream instructions. Each cell is marked present, mirrored-now, or not-applicable with a reason, per the corpus's `2026-07-15` symmetry-table rule.

| Surface | Who composes instructions there | Disposition |
|---|---|---|
| `agents/tech-lead.md`, hand-written `## Rules` | the tech-lead, writing a Routing Map that becomes another role's drafting instruction | **mirrored-now** — `M1` (DP5). This is the surface the retro's Try line names and the only one the adjudication covers. |
| `templates/prompt-blocks/playbook-*.md` and the four consumer agents | every IN role, in the hand-off it writes for the next one | **present** — N1's `Rule` ships to all four blocks via `Applies-to: all` (DP2). No surface-specific wording is added; the corpus line is the mechanism. |
| `skills/run/SKILL.md`, the coordinating session's prompt-composition procedure | the orchestrator, writing each role's task prompt | **not applicable now, deferred with a reason** — the orchestrator is not an `Applies-to` token and this file is outside the adjudicated scope (DP6, Non-goals). It is the genuine parallel surface, so it is named as an input to the next planning round rather than left unmentioned; widening it here would put an unadjudicated norm on a shipped file. |
| `agents/scrum-master.md` | the retro agent, writing a cycle summary that becomes a planning input | **not applicable — no rule needed here** — the retro that produced these candidates already discloses relayed claims explicitly in its own Loop-trap check section, so the discipline is being applied on this surface without a prompt line. A finding that it should carry one is a next-planning input, not a gap in this task. |

## Body-to-AC correspondence

Every normative directive stated in the body above, mapped to the criterion that enforces it or to an explicit exemption with a reason.

| Body directive | Where | AC / disposition |
|---|---|---|
| Candidate 1 becomes a new entry, byte-exact as frozen | DP1 | **AC1** · **AC6** (the append is byte-exact in position and content) |
| Candidate 2 becomes a new entry, byte-exact as frozen | DP1 | **AC2** · **AC6** |
| Candidate 4 becomes a new entry, byte-exact as frozen | DP1 | **AC3** · **AC6** |
| `M1` is frozen text, not engineer discretion | DP1 / DP5 | **AC11** (byte-exact, exactly once) · **AC12** (it is one of exactly two added lines) |
| N1 is `loop` / `all` | DP2 | **AC1** (both fields asserted explicitly) · **AC8** (all four propagation cells) |
| N2 is `loop` / `engineer, pm-spec, qa-verifier` | DP2 | **AC2** (both fields asserted explicitly) · **AC8** (three cells present, tech-lead cell zero) |
| N3 is `loop` / `pm-spec, qa-verifier` | DP2 | **AC3** (both fields asserted explicitly) · **AC8** (two cells present, engineer and tech-lead cells zero) |
| No new entry supersedes, retires, re-scopes or rewords any existing entry | DP3 / Non-goals | **AC6** (the base blob is a byte-identical prefix of the current file) · **AC4** (`del=0`) · **AC5** (heading set gained exactly three) |
| The adjacency verdicts themselves, and the reason candidate 7 is dropped | DP3 / DP9 / audit table | `info-only (not promoted to AC)` — a recorded judgment about entries this task does not touch; its assertable half is the byte-immutability of every other entry, which **AC6** carries, and the absence of a fourth entry, which **AC5** carries |
| No `Extended by` pointer is attached to any entry | DP4 / Non-goals | **AC6** (a pointer bullet added to an existing entry breaks the prefix comparison) |
| No other task's spec is edited or re-frozen | DP4 / Non-goals | **AC14** (the allow-list contains no other spec path) |
| `M1` sits outside **every** marker region, the count being three rather than the relayed two | DP5 | **AC11** (`M1` precedes the *first* `BEGIN prompt-block` marker, and the marker-name set matches the base blob) |
| The three marker regions stay byte-identical to their canonical blocks | DP5 | **AC7** (`bin/check-prompt-sync.sh` over the whole registry) · **AC12** (only two lines were added at all) |
| Nothing else in `agents/tech-lead.md` moves — frontmatter, `tools:`, `model:`, existing Rules bullets, output format | DP5 / Non-goals | **AC12** (exactly two added lines, both identified individually; zero deleted) |
| `skills/run/SKILL.md` is untouched and the parallel surface is deferred rather than ignored | DP6 / Non-goals | **AC14** (outside the allow-list) for the untouched half; the deferral itself is `info-only (not promoted to AC)` — a recorded planning input, and no artifact property can assert that a deferral was considered rather than forgotten, which is why the symmetry table states it in the body |
| Every accounting criterion is base-relative or value-independent | DP7 | **AC4** (expected addition computed from the frozen blocks) · **AC6** (base blob's own line count) · **AC8**/**AC9**/**AC10** (deltas derived from `Applies-to`) · **AC9** (block sizes as base + delta) |
| The one surviving absolute literal is AC7's eight-file registry control, with a reason and a staleness trigger | DP7 | **AC7** (the literal, its positive-control purpose, and its `- stale-at:` sub-bullet) |
| The nine-line entry shape and the four-role loop are structural, not population counts | DP7 | **AC1**–**AC4** (the `9` assertions) · **AC8**/**AC9**/**AC10** (the closed role set) — exempted in DP7 with the reason that neither grows |
| Appends go through `bin/playbook-promote.sh` in the order N1, N2, N3; blocks are never hand-edited | DP8 | `info-only (not promoted to AC)` for the tool attribution — no command can prove which tool wrote a byte. The bytes and the file order are asserted by **AC1**, **AC2**, **AC3** and **AC6**'s ordered tail comparison; the never-hand-edited half is asserted by **AC7** (a hand-edited block cannot survive regeneration into a scratch tree) |
| Retro candidates 3, 5, 6 and 7 never enter the corpus | DP9 | **AC5** (the gained heading set is exactly the three frozen keys) |
| No fixed scratch path is converted to `mktemp` anywhere | Non-goals | **AC14** (`bin/` and `tests/` are outside the allow-list) |
| No `bin/`, `tests/`, `.github/`, `docs/`, `skills/`, adopter-facing or other-`agents/` change | Non-goals | **AC14** (allow-list) |
| The retro file is immutable | Non-goals | **AC14** (outside the allow-list) |
| The `## Format` section is byte-unchanged | Non-goals | **AC6** (it lies inside the prefix compared byte for byte) |
| No `.ja.md` counterpart | Non-goals | **AC14** (the allow-list contains no `.ja.md` path) |
| The new entries are authored in English at promotion | Task / repo language rule | **AC1**, **AC2**, **AC3** — the frozen bytes settle it; there is nothing left for a language check to decide |
| The board entry follows the strict line format and disturbs no other entry | Board hygiene | **AC13** (hand-off lint, structural heading check, zero deletions, the entry line's own format) |
| Checkers fail closed; no check may pass on an unreadable or unextractable input | repo contract | **AC1**–**AC3** (extraction line-count and non-empty assertions before any comparison) · **AC4** (the computed expectation must exceed zero) · **AC5** (the base blob's heading list must be non-empty) · **AC6** (base non-empty, *N* > 0, expectation non-empty) · **AC7** (the eight-file registry control) · **AC8**–**AC10** (empty numstat rows and empty field extractions fail) · **AC11** (five positive controls) · **AC12** (the reconstruction's three parts must extract non-empty) · **AC13** (an untouched board fails) · **AC14** (an empty changed-set and a missing record file both fail) |
| The full applicable CI-wired list runs locally, with directory-scanning steps run rather than reasoned about | AC15 (a) | **AC15** (a) |
| A producer-run mutation self-check precedes the first review round, including a probe at AC6's own blind spot | AC15 (b) / playbook | **AC15** (b) |
| Every `Why` clause is checked back against the records the retro names | AC15 (c) | **AC15** (c) |
| Every relayed claim is re-measured before the freeze | AC15 (d) | **AC15** (d) |
| Each named source gets a per-source report, with the adjudication's relayed status restated | AC15 (e) / Summarized sources | **AC15** (e) |

## Assumptions

- **pm-spec has no shell in this role, so not one `check:` line above has been executed.** Every file named in `## Summarized sources` as read directly was read with a file-reading tool. The executing side runs all 14 live `check:` lines against the pre-implementation tree (base + this spec + the board entry), decomposes each compound line into the clauses it asserts, corrects anything mechanically broken or vacuously passing with a meaning-preserving fix, records the measured shape, and only then records the freeze attestation and `- intent-hash (v1)` for T-1039. Verify, then correct, then freeze.
- **Predicted pre-implementation shape for this spec's own 14 lines, recorded so a mismatch is itself a finding: PASS AC7 (1); FAIL AC1, AC2, AC3, AC4, AC5, AC6, AC8, AC9, AC10, AC11, AC12, AC13, AC14 (13); SKIP AC15.** **AC7 is a preservation lock that must be green before, during and after the change** — the blocks are in sync with the corpus at the base ref — and a red there at the base ref is a finding about this spec's premises, not about the work. **AC13 is the ledger-flip line**: it is red at freeze for one reason only, that `bin/check-intent.sh` needs the `intent-hash` and `freeze-attestation` sub-bullets this role does not write, and it goes green the moment the coordinating session appends them. Measure it once before writing the ledger lines and once after, and record both. Misreading AC13's pre-freeze red as a board-format defect is the likeliest false finding on this task.
- **Relayed claims this spec leans on, disclosed rather than absorbed.** Each is a claim pm-spec could not measure; the freeze run measures it and reports the value beside it.
  1. **`253f4b7` is `develop`'s tip and this branch's real branch point.** Relayed. Every `git` anchor in the criteria resolves that literal (AC4, AC5, AC6, AC9, AC10, AC11, AC12, AC13, AC14); if it is false they all measure the wrong comparison. Confirm with `git rev-parse --verify` and an ancestry check before the freeze. pm-spec observed the same short SHA in its own session context, which corroborates but does not measure it.
  2. **The host adjudicated candidates 1, 2 and 4 as promotions and 3, 5, 6, 7 as non-promotions on 2026-08-06.** Relayed; the coordinating session holds the primary confirmation. The whole shape of DP1, DP2 and DP9 rests on it. If the adjudication differs, that is a pre-freeze scope question, not a wording fix.
  3. **Candidate 1's mechanisation is scoped to exactly one item in `agents/tech-lead.md`'s hand-written `## Rules`, and `skills/run/SKILL.md` is excluded.** Relayed; it is the ground of DP5 and DP6. If the adjudication in fact covered both surfaces, DP6's deferral is wrong and the symmetry table's third row has to move to mirrored-now — which changes the scope lock, so raise it before the freeze.
  4. **Candidate 2's convention mechanisation is T-1043 under issue #168, and candidate 4's editorial pass is T-1040.** Relayed; those ids appear in the Non-goals and the board entry. A wrong id makes the exclusion untraceable rather than wrong, but correct it before the freeze.
  5. **Candidate 7 is dropped because T-1033's general form covers it.** Relayed as the adjudication; pm-spec independently **measured the covering entry** and confirms it is present and `active` in the corpus with a `Rule` that states the invariant in general form (the audit table's last row). The disposition is relayed; the coverage is measured.
  6. **`## Active` is empty at the base ref.** Relayed, and separately observed by pm-spec in the **working tree** rather than in the base blob. This is what makes the board edit a pure insertion, which AC13's zero-deletion clause depends on.
- **One relayed premise was measured and found wrong, and the spec carries the corrected value rather than the relayed one.** The task prompt stated that `agents/tech-lead.md` carries **two** marker pairs (`careful-execution`, `playbook-tech-lead`) and that `M1` must sit outside those two. Measured against `templates/prompt-blocks/registry.txt` and the file itself: there are **three** — `language.md` is also a `marker`-mode block registered for that consumer. DP5 and AC11 are written to the measured value, and AC11 deliberately asserts "before the *first* marker" plus base-blob marker-set equality rather than naming a count, so the criterion does not encode any number at all. Recorded here because this task's own N1 is the rule that says a relayed premise gets re-measured before it is written down, and a task promoting that rule while transcribing an unmeasured premise would falsify itself.
- **The derived per-role deltas the criteria compute at run time, stated here for diagnosis only and never as a criterion's expected value.** Evaluating DP2's routing against the three frozen `Applies-to` fields gives: engineer 2 (N1, N2), qa-verifier 3 (N1, N2, N3), tech-lead 1 (N1), pm-spec 3 (N1, N2, N3); `agents/tech-lead.md` additionally gains `M1`, for two lines total. If the freeze run measures anything else, the disagreement is between the measurement and DP2, and the reconciliation is to re-read DP2 rather than to adjust a number — no criterion above contains these figures.
- **`bin/gen-playbook-blocks.sh` prints a non-fatal block-size warning to stderr for blocks past its 40-line threshold.** It already does so at the base ref for the engineer and pm-spec blocks, and both grow here. The script's contract says the warning never fails the run; AC7 redirects stderr and judges by exit status and `cmp`. Do not read the warning as a failure, and do not respond to it by shortening an entry or dropping a role from an `Applies-to` list — that would silently revert DP2.
- **No `CHECK_ACS_TIMEOUT` elevation is required, stated explicitly so the runner does not add one by analogy.** The heaviest criterion is AC7 — one scratch-tree copy of eight small files, one regeneration (which internally runs `bin/check-playbook.sh` once over the corpus), eight `cmp`s, and one `bin/check-prompt-sync.sh` — seconds, well inside the 120s default. AC14's `bin/check-pii-shapes.sh --base` over a small diff and AC13's `bin/check-intent.sh` run over a large board are the next heaviest and are also seconds.
- **This spec is self-hosting and quotes strings that also live in the artifacts it checks.** AC5 compares heading sets **inside the corpus only**, so the frozen headings written here are invisible to it; AC8's and AC12's greps name specific block, agent and diff targets and never the repository, so this file's own quotation of a `Rule` line cannot trip them; AC11's greps name `agents/tech-lead.md` alone, so this file's own quotation of `M1` and of the marker syntax cannot trip them; AC13's grep names the resolved board path only. Run `bin/check-pii-shapes.sh` over this spec before the first commit regardless.
- **No `check:` value begins or ends with a backtick** (`bin/check-acs.sh` rejects that shape fail-closed), no AC label carries a suffix glued to its digits, and every `grep` whose pattern could begin with `-` passes `--` first.
- **The frozen-block extraction patterns contain a `!`** (`<!-- frozen-begin: … -->`), always inside single quotes or an `awk -v` assignment. `bin/check-acs.sh` executes each line through a non-interactive `bash -c`, where history expansion is off, so this is safe in the harness — but pasting one of these lines into an *interactive* shell to debug it can trip history expansion and produce a failure that has nothing to do with the criterion. Run them through `bash -c`, or through `bin/check-acs.sh` itself.
- **Every `mktemp` invocation above uses the guarded idiom** (`T=$(mktemp -d "${TMPDIR:-/tmp}/<slug>.XXXXXX") || exit 1`, failure exiting before the first composed path is used), each with its own scratch slug, and every `|| true` sits on a `grep -c` whose zero is a meaningful measurement, never on a producer whose failure would otherwise be invisible.
- **AC12 reconstructs the generated pointer line rather than quoting it, and it never matches an em-dash with a pattern.** The generator emits `- <rule> (<pointer-path>, <date> — <title>)` and re-inserts the separator itself so that the pointer text is exactly the entry key; AC12 therefore takes the key as a whole, with `ky=${hd#'## '}` — a fixed-string prefix removal on a three-byte-safe ASCII prefix — rather than splitting date from title. A `.` standing in for the em-dash was **deliberately rejected**: `sed`'s `.` matches one *character* under a UTF-8 locale and one *byte* under `LC_ALL=C`, so the same line would strip a three-byte em-dash in one locale and one third of it in the other, and `bin/check-acs.sh` does not pin the locale. The only literal em-dash left in the criterion is inside the frozen block it reads, never inside a pattern. Confirm at the freeze that the reconstructed line matches the real generated line byte for byte before trusting AC12's green; a reconstruction that silently drops the title would fail in a way that looks like a generator bug.
- **Host tooling assumptions to confirm at the freeze**: `comm`, `cmp`, `sed -n`, `awk`, `tail -n +N`, `grep -c ''` and `grep -cxFf` behave POSIX-ly on this host, and `grep -c ''` counts lines including a final line with no trailing newline. If the corpus's last line lacks a trailing newline at the base ref, AC6's `head`/`tail` split needs re-measurement before the freeze rather than after.

## Open questions

None blocking. Two items are recorded rather than asked:

- The retro's Try line for candidate 1 names "Routing Map / tech-lead プロンプト構成手順" — two surfaces, of which the adjudication covers one. DP6 defers the other with a reason and the symmetry table records it as a next-planning input. If the coordinating session's own reading of the adjudication is that both were covered, say so before the freeze; it changes the scope lock.
- The retro's Try list also proposes a cross-join step in the same-class-2 judgment logic (candidate 3's neighbour) owned by `skills/run/SKILL.md`. That is candidate 3's mechanisation, owned elsewhere in this sprint, and is out of contract here.

## Notes for engineer

**Read `.shell-team/retros/2026-08-06.md`'s `## Lesson candidates` section before you touch anything**, then read `.shell-team/lessons.md`'s `## Format` section and the ten entries named in the adjacency audit. The candidates are in the operator's language; the entries you ship are English, authored at promotion, and already written for you in DP1 — you are not translating anything.

**Measured-at-ref labels: not applicable — none of this task's deliverables prints a command beside a `measured at <ref>` label.** The three corpus entries, the four regenerated blocks, the four consumer agent files and `M1` contain no measurement label and no printed command at all. Every criterion that resolves `253f4b7` reads that ref's committed blob through `git show 253f4b7:<path>` or `git diff --numstat 253f4b7`, never a working-tree read standing in for the ref; if you add a measured value to the board hand-off, print the ref-reading command beside it.

Order of work, which is also the order that keeps every intermediate state schema-valid:

1. Three `bin/playbook-promote.sh` runs, `--date 2026-08-06`, in the order N1, N2, N3. Each one re-validates the whole file before it writes.
2. `bin/gen-playbook-blocks.sh`, once, from the repository root, with no `--lessons` flag so the pointer path resolves the way every existing block's pointer did. Commit what it writes; never hand-edit a block or a marker region.
3. Append `M1` by hand as the last bullet of `agents/tech-lead.md`'s `## Rules` section — after the existing "Never write files…" bullet and before the blank line preceding `<!-- BEGIN prompt-block: careful-execution -->`. Re-run `bin/check-prompt-sync.sh`.
4. Board and records.

Commit separation is **required** by R2, in exactly this shape, so that whichever half is clean can ship alone: **(1)** the three corpus appends, **(2)** the block regeneration, **(3)** the `M1` hand edit, **(4)** the board and the records.

Scope-lock allow-list (the files you may change). Required:

```
.shell-team/lessons.md                                       (N1, N2, N3)
templates/prompt-blocks/playbook-engineer.md                 (generated)
templates/prompt-blocks/playbook-qa-verifier.md              (generated)
templates/prompt-blocks/playbook-tech-lead.md                (generated)
templates/prompt-blocks/playbook-pm-spec.md                  (generated)
agents/engineer.md                                           (marker region only, generated)
agents/qa-verifier.md                                        (marker region only, generated)
agents/tech-lead.md                                          (marker region, generated + M1 by hand)
agents/pm-spec.md                                            (marker region only, generated)
.shell-team/specs/T-1039-promote-retro-2026-08-06.md         (this file — you do not edit its intent block)
.shell-team/todo.md
```

Permitted in addition, and required by AC14 in the provenance and interventions cases: `.shell-team/provenance/T-1039.md`, `.shell-team/reviews/T-1039.md`, `.shell-team/interventions/T-1039.md`. Anything else in the diff fails AC14 — including a stray untracked scratch file in the checkout, since AC14's union counts untracked additions. Run every mutation experiment on a `$TMPDIR` copy or a scratch `git worktree`, never in the working tree.

Traps in the criteria themselves, so you do not find them the expensive way:

- **Every frozen text is read out of this spec by AC1–AC6, AC8–AC12.** If you retype one instead of copying it, a single smart quote or a collapsed double space fails the criterion, and the failure will look like a corpus problem rather than a transcription problem. Extract the fenced blocks with `awk`/`sed` into scratch files and splice those bytes programmatically.
- **Every frozen field value carries characters that shell quoting mangles**: backticked fragments, angle-bracket placeholders, straight double quotes, apostrophes and em-dashes. Extract each field's bytes into a scratch file and pass them as `--rule "$(cat "$T/rule")"` and so on; a variable's *contents* are never re-expanded, whereas anything you retype into the command line is at the mercy of whatever quoting you chose.
- **`bin/playbook-promote.sh --title` takes the heading text only.** The script writes `## <date> — <title>` itself, so the value is everything *after* the em-dash separator in the frozen heading — do not include the date and do not include the em-dash, or the entry key will not match the frozen block and AC1–AC6 all fail together on what looks like a byte mismatch.
- **AC6 is the strictest criterion here and it has one blind spot worth knowing.** It proves the corpus is a pure append by comparing the base blob against the current file's first *N* lines and then comparing the remainder against the three frozen blocks in order. It therefore catches a reworded entry, a supersede, an added pointer bullet, a `## Format` edit and a wrong append order — but if you append the three entries in the wrong order it fails on the *tail* comparison with no indication which entry is misplaced. Check the order before you blame the extraction.
- **`agents/tech-lead.md` carries three marker pairs, not two.** `language`, `careful-execution` and `playbook-tech-lead` are all `marker`-mode blocks registered for it. `M1` goes in the hand-written `## Rules` section, which precedes all three. AC11 measures "before the first marker", so putting `M1` anywhere below `## Rules` but above the first marker satisfies it — put it at the end of the section, where DP5 says.
- **AC12's teeth are that it names both added lines individually.** Any additional edit to `agents/tech-lead.md` — a reflow, a typo fix, a note explaining `M1` — turns two into three and fails, and the failure names neither line. If you want to explain the item, the board entry is where that goes.
- **AC9's and AC10's teeth are in the deletion column, not the addition count.** Any reflow, any "improvement" to an existing bullet, any regeneration that reorders a block turns a 0 into a positive number and fails. The generator appends at end of block; if a bullet moves, something else changed.
- **AC13 is red until the freeze records exist.** `bin/check-intent.sh` needs the `intent-hash` and `freeze-attestation` sub-bullets, which the coordinating session writes. That red is expected at hand-off and is not a board-format defect.
- **`grep -c` under `set -euo pipefail`** aborts a script when the count is genuinely `0`; append `|| true` to the pipeline, never to the assignment, and keep the explicit assertion right after. Relevant if you write scratch verification around the mutation self-check.
- **The 40-line block-size warning will fire for the engineer and pm-spec blocks.** It is not a failure and it is not a reason to shorten an entry or narrow an `Applies-to` list.

Pre-commitments, set before the first round and binding on every role on this branch:

- **R1 — the seven dispositions and every frozen text are settled.** An argument that retro candidate 3, 5, 6 or 7 belongs in the corpus, that N2 should drop the engineer, that N3 should add the engineer, that a new entry should supersede one of its neighbours, that an `Extended by` pointer should be attached, or that a `Rule` sentence should read differently is a **re-freeze proposal (vK→vK+1) routed to pm-spec**, not a rework fix the engineer may apply. Arguments to add candidates 3, 5, 6 or 7 to the corpus are **out of contract** outright, not merely re-routed.
- **R2 — two consecutive rounds of independent new `Blocker`/`Major` findings against the derived-accounting surface** (AC4's computed expectation, AC6's prefix-and-tail comparison, AC8/AC9/AC10's `Applies-to`-derived deltas, AC12's reconstructed pointer line) ends the patching. Factual trigger: round *n* and round *n+1* each produce new, independent findings in that class. Contextual trigger: a third round is about to start. **The factual trigger governs when the two disagree**, because rounds can be relabelled and independent defect classes cannot. On trigger, stop and propose the split: ship the three corpus appends and the block regeneration alone — commits 1, 2 and 4 above — and spin `M1` plus its AC11/AC12 pair into its own task. Two consecutive rounds is this repository's standing threshold and nothing here loosens it. **Note the deliberate self-exposure**: this task promotes the rule against absolute-literal accounting and then writes six criteria in the derived form, which is a newer and less-exercised shape than the literals it replaces. R2 exists because that is where a defect is most likely, and reverting a derived criterion to an absolute literal is **default-rejected** — it would ship the anti-pattern under the rule's own name. A finding that a derivation is wrong is a finding to fix in the derivation.
- **R3 — any proposal to touch `bin/`, `tests/`, `.github/`, `docs/`, `skills/run/SKILL.md`, the retro files, another task's spec, or the mechanisation targets of the candidates this task does not promote is unconditionally out of contract.** Record it in the review record (and, if it recurs, as an issue candidate for the next planning input) and refuse it in the round it appears. AC5, AC6 and AC14 are the machine half of the same refusal.

Task classification, stated so review rigour is calibrated rather than guessed: **this is not verification-mechanism work — no checker, parser or state tracker is written or extended, and `bin/` and `tests/` are byte-identical** — but it is not a plain pure-addition either. It exercises three existing checkers' contracts at once (the corpus schema, the block generator's `Scope`/`Applies-to` routing, and the prompt-sync registry), it hand-edits a prose region of a shipped agent prompt immediately adjacent to three machine-written marker regions, and six of its criteria use a run-time-derived expectation instead of a literal, which is the newest idiom in this spec. Review the `M1` placement and the derived-accounting criteria at the higher of the two standards, and the three corpus appends at the ordinary standard for a frozen-text promotion.
