# The README states the install and update path for both hosts, side by side, with the Codex CLI runbook one link away

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1148

**Branch**: `feature/564-readme-two-hosts`, cut from `develop` at `970edc1c`. **No predecessor pull request is open**, so this spec's base-side reads resolve the branch point against `develop` directly, through the single `show-ref`-gated expression declared below rather than through a two-arm predecessor discriminator.

## Problem

`README.md` and `README.ja.md` still open by calling shell-team a Claude Code plugin, and their `## Install` section shows Claude Code's `/plugin` commands only. The Codex CLI host path — installing through `codex plugin marketplace add` / `codex plugin add`, generating the Codex custom agents, and driving the whole loop from a Codex session to `READY_FOR_MERGE` with both gates green — shipped in v2.6.0 and has since been exercised on real adopter runs, yet no sentence, badge, install line or link in either README says so. The complete runbook lives only in `docs/adopting.md`'s `## Using shell-team from Codex CLI` (and its Japanese counterpart), and nothing in the README points at it. There is no `## Update` section at all in either file: the update path for either host is documented only in `docs/distribution.md`. A shipped capability nobody can find from the front page is, for an adopter, a capability that did not ship.

## Summarized sources

- `README.md` — read first-hand in full (319 lines). Distinctions this spec rests on: the badge row occupies the region before the first `## ` heading and carries seven badge lines in two groups — the two language badges at `:3`–`:4`, then CI, version, Claude Code plugin, reviewer and `bin` at `:6`–`:10` — with the Claude Code plugin badge at `:8` linking to `docs/distribution.md`; the concept image line at `:22`; `## What shell-team is` at `:24` opening with the retired sentence `A Claude Code **plugin** that drops a dev team into any repo`; `## Prerequisites` at `:36` with three bullets written as one list assuming Claude Code; `## Install` at `:42` carrying exactly two fenced `/plugin` command lines at `:47`–`:48` and the `/shell-team:team-init` line at `:54`; `## When the full loop fits (task aptitude)` at `:100`; `## Develop / dogfood` at `:312` documenting `claude --plugin-dir ./` only; and the `bash "<plugin root>/bin/<script>"` invocation convention this file already states at `:240` and `:265`. Measured: **16** `^## ` headings, and no `^## Update` heading anywhere.
- `README.ja.md` — read first-hand in full (323 lines). Distinctions: the same badge row shape with the Claude Code plugin badge at `:8`; the concept image line at `:22`; `## shell-team とは` at `:24` opening with the retired clause `Claude Code **プラグイン**です`; `## 前提` at `:36`; `## インストール` at `:42` with the identical two `/plugin` lines at `:47`–`:48` and `/shell-team:team-init` at `:54`; `## フルループが向くタスク（適性）` at `:100`; `## 開発 / dogfood` at `:317`; and the precedent at `:177`–`:179` for linking into `docs/adopting.ja.md` with a GitHub-generated Japanese anchor. Measured: **16** `^## ` headings, equal to the English file's, and no `^## 更新` heading.
- `docs/adopting.md` — read first-hand at `:363`–`:647` (the whole `## Using shell-team from Codex CLI` runbook) and `:766`–`:783`. Distinctions carried into the frozen region: the heading itself is `## Using shell-team from Codex CLI` at `:363`; the two install commands are `codex plugin marketplace add RipsawJP/shell-team` and `codex plugin add shell-team@ripsawjp` at `:393`–`:394`, with the subcommand names read from `codex plugin --help` on codex-cli 0.154.0 (`:397`); the upgrade command is `codex plugin marketplace upgrade <marketplace>` at `:435`, a placeholder rather than a literal marketplace name; the generator is `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents` at `:447`; the drift reporter is `bash "<plugin root>/bin/check-codex-agents.sh"` at `:592`, which reads and writes nothing; step 7 (`:510`–`:528`) states that a Codex CLI host needs Claude Code CLI **installed and authenticated** because the review pass runs a real `claude -p` child process; step 10 (`:573`–`:583`) states that both `merge` and `push` are human gates under the shipped default loop contract and that the human opens the pull request; `:410`–`:414` states that `bin/` on `PATH` is measured **false** for an installed plugin on either host, which is why every shell command this spec names is spelled `bash "<plugin root>/bin/<script>"`; the runbook's own eleven steps contain **no** `team-init` step; and the host-neutral shell form of the scaffolder appears at `:774` as `team-init.sh .`. The honest-limits paragraph at `:599`–`:647` and the T-1147 rewrite at `:675`–`:685` are the shape this spec copies for a disclosed, unmeasured residual: this project ships no Codex instruction surface, so what a Codex CLI orchestrator loads automatically is stated as unmeasured from this repository and never as a claim about the host.
- `docs/adopting.ja.md` — read first-hand at `:372` for the heading `## Codex CLI から shell-team を使う`, the Japanese counterpart of the runbook. The distinction: a Japanese README link resolves into this file, not into the English one.
- `docs/distribution.md` — read first-hand at `:81`–`:93`. Distinctions: the update command for a Claude Code host is `/plugin marketplace update ripsawjp` at `:86`, under a `## Update` heading; `## Version line` at `:91` states that a default marketplace add resolves the manifest from `main` HEAD. This file is **read and linked, never edited** by this task.
- `.claude-plugin/marketplace.json` — read first-hand in full (12 lines). The distinction: the marketplace's own `name` is `ripsawjp` (`:2`), which is the value both update commands name; the file's `description` (`:4`) still reads "RipsawJP's Claude Code plugin marketplace", observed and deliberately left alone by this task.
- `bin/team-init.sh` — read first-hand at `:25`–`:43`. The distinction: the usage line is `bin/team-init.sh [--force] [--trial-branch <name>] <target_path>`, so the target is a positional argument and `.` names the adopted repository's own root — the two measured inputs from which this spec derives the Codex-host `team-init` form rather than inventing one.
- `bin/check-readme-version.sh` — read first-hand at `:1`–`:34`. The distinction: its scope note states it looks **only** at the static version badge, comparing it against `.claude-plugin/plugin.json`'s `version`; it asserts nothing about heading counts, badge rows, install commands or any other README content, which is why every other README property this task ships has to be a `- check:` line here.
- `.github/workflows/check-handoff.yml` — read first-hand at `:26`–`:31` and `:106`–`:116`. The distinction: the only workflow step whose input this task's diff can reach is `Dogfood check-readme-version on the repo's READMEs` at `:113`–`:114`, running `bash bin/check-readme-version.sh README.md README.ja.md`; the `shellcheck` step at `:28`–`:29` and every fixture-suite step read paths this task does not touch.
- `.shell-team/todo.md` — read first-hand: `## Active` at `:12`, `## Done` at `:6360`, and T-1147's entry at `:6362` with its full sub-bullet grammar. Used for the board line format and the sub-bullet precedent.
- `.shell-team/specs/T-1147-class-m-grant-host-neutral-record.md` — read first-hand in full. Read as the immediately preceding task's conformant shape (declaration region, raw-command `- check:` idiom, merge-point-scoped scope-lock note, `## Blast radius` deferral wording, borrowed-vocabulary classification) and, more importantly, for its frozen Non-goal "No assertion about what a Codex CLI orchestrator loads as instruction" — the rule this task inherits and makes mechanical in **AC10**, together with the two review rounds that rule cost when it was violated (recorded on the board at `:6371` and `:6368`).
- **GitHub issue #564 and the operator's 2026-09-18 direction** — relayed verbatim into this role by the coordinating session, which holds the primary copy (this role has no GitHub-reading tool). Distinctions restated in the frozen region: that both READMEs open by naming one host; that the Codex host path shipped in v2.6.0 and has been exercised on adopter runs; that install and update must stand side by side per host; that the runbook must be one link away; that the concept image and the full-loop-aptitude section stay unchanged; and the brand principles the public wording follows — shipped fact separated from future hope, no unproven self-declaration, the author's experience before jargon, limits stated rather than hidden. Labelled **relayed** in `## Assumptions`.

## Goal

<!-- BEGIN intent-block: T-1148 -->

- user-visible: yes — the deliverable is the adopter-facing front page itself. An adopter on a Codex CLI host gains, at the surface they read first, the install commands, the update commands and the runbook link for their own host; an adopter on either host gains an `## Update` section that did not exist. Nothing an adopter runs changes behaviour, which is why the tier below is still PATCH — adopter-visibility and release tier are separate questions.
- verification-class: no-mechanism — the diff reaches `README.md` and `README.ja.md` and nothing else: no path under `bin/`, `tests/` or `templates/`, no CI workflow, no checker's semantics. Under that class this spec prices three things down and says so in `## Non-goals`: the downstream-impact inventory is read-set-scoped rather than full-population, CI-equivalence is run only for the one workflow step whose input this diff reaches, and mutation probes are owed for the `- check:` lines this spec adds rather than as a matrix over every criterion. Every criterion's verification shape is one of the four this class allows — a scoped string presence-or-absence grep, a byte-scoped diff, a checker's exit code, or a records-line existence check.
- verification-ceiling: unit-and-static — **AC1** through **AC13** are each settled by a scoped fixed-string `grep`, an `awk` region extraction, a `cmp` against a committed blob read with `git show`, a `git diff`/`git ls-files` read, or a shipped checker's exit code, all runnable in a plain checkout with no adopter machine, no host other than the one running the check, and no network. The declaration is non-vacuous because thirteen criteria are verified at exactly this ceiling. **AC14** sits above it and carries its own `- above-ceiling:` owner.
- base-ref-discriminator: not-applicable — this branch was cut from `develop` and no predecessor pull request is open at authoring time, so no two-arm predecessor expression applies. The criteria that read a base-side blob (**AC11**, **AC12**, **AC13**) resolve the branch point through one expression spelled byte-identically in all three: an explicit `git show-ref --verify --quiet` existence test against `refs/heads/develop`, then against `refs/remotes/origin/develop`, taking `git merge-base` against whichever namespace the test found and **failing closed** when neither resolves — never a `2>/dev/null ||` chain, and no 40-hex literal anywhere.

**Goal (one sentence).** `README.md` and `README.ja.md` each open by saying that the same team runs from either Claude Code or Codex CLI, carry a Codex CLI plugin badge beside the existing Claude Code plugin badge, state each host's prerequisites separately, print each host's install commands and each host's own `team-init` form side by side, carry a new `## Update` section printing both hosts' update commands with the one follow-up the Codex path needs, link directly to the Codex runbook in `docs/adopting.md` (English) and `docs/adopting.ja.md` (Japanese), and state the path's honest limits — while the concept image, the full-loop-aptitude section and the dogfood section stay byte-identical to their base blobs, no sentence anywhere claims what a Codex CLI host does or does not load, and the diff touches no file but those two READMEs and this task's own records.

**Design decisions, numbered, with the ground each was decided on.**

**D-1 — the opening sentence names both hosts, and the surrounding paragraph stops attributing the review to one provider.** The retired English sentence calls shell-team "A Claude Code **plugin** that drops a dev team into any repo"; its Japanese counterpart ends "Claude Code **プラグイン**です". Both are now false as a description of what ships. The replacement names both hosts and says what is the same across them — the roles, the status-flag gates, and a review that comes from the other provider. Two sentences in the same section carry the same defect and move with it: the "Codex-powered cross-provider Reviewer" apposition and the bullet stating the final review runs through Codex CLI. On a Codex CLI host the reviewer is Claude (`docs/adopting.md:375`–`:384`), so both are rewritten host-aware rather than left contradicting the new opening. Nothing else in that section changes.

**D-2 — one new badge, in the shipped badge row, styled from the two badges already there.** The new badge reuses the existing `img.shields.io` static-badge form and the `style=flat-square` the whole row uses, takes its label from the Claude badge's own `<host>-plugin` shape, and takes its colour from the row's existing Codex badge (`10a37f`). Its link target is the runbook for that file's language. Fixed strings, per file, pinned by **AC3**:

- `README.md`: `[![Codex CLI plugin](https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square)](docs/adopting.md#using-shell-team-from-codex-cli)`
- `README.ja.md`: `[![Codex CLI plugin](https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square)](docs/adopting.ja.md#codex-cli-から-shell-team-を使う)`

The badge's alt text stays English in both files, as every badge in the Japanese file's row already is. Anchor **resolution** is GitHub's renderer behaviour and is not claimed here: **AC8** verifies that each link's target file carries the named heading as a whole line, which is the part this repository can measure.

**D-3 — prerequisites are stated per host, because the two hosts need different things and one list cannot say so.** A Claude Code host needs Claude Code plus Codex CLI installed and authenticated, for the review pass — today's list, unchanged in substance. A Codex CLI host needs Codex CLI plus **Claude Code CLI installed and authenticated**, for the same review pass on the other side: `docs/adopting.md:510`–`:528` measures that the review pass runs a real `claude -p` child process and that its absence and its unauthenticated state are two different failure modes. The existing sandbox bullet stays and gains the Codex-host counterpart by pointing at the runbook's own sandbox steps rather than restating them.

**D-4 — the install section prints both hosts' commands and each host's own `team-init` form; the Codex block is three lines and the Claude block is two, and that asymmetry is a measurement, not an oversight.** The Claude Code host has exactly two install commands; no third exists, and inventing one would be a claim rather than a measurement. The Codex host's block carries the two `codex plugin` commands plus the generator step, because on that host the generated custom agents are what makes the roles dispatchable at all. Fixed strings, each required as a whole line inside the install section of **both** files (**AC5**), with the Codex lines additionally required to occur verbatim in `docs/adopting.md` (**AC6**):

- Claude Code host: `/plugin marketplace add RipsawJP/shell-team`, `/plugin install shell-team@ripsawjp`, then the per-repo step `/shell-team:team-init`.
- Codex CLI host: `codex plugin marketplace add RipsawJP/shell-team`, `codex plugin add shell-team@ripsawjp`, `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents`, then the per-repo step `bash "<plugin root>/bin/team-init.sh" .`.

The Codex-host `team-init` form is **derived, not invented**: the runbook's eleven steps contain no `team-init` step, `bin/team-init.sh:37` gives the usage as `bin/team-init.sh [--force] [--trial-branch <name>] <target_path>` with the target positional, `docs/adopting.md:774` shows `team-init.sh .` as the host-neutral shell form, and `docs/adopting.md:410`–`:414` records that `bin/` on `PATH` is measured false for an installed plugin — so the form this README prints is the `bash "<plugin root>/bin/<script>"` convention the file already uses for `gen-loop-replay.sh` and `derive-populations.sh`, applied to that same command. The install section also says, in prose, that the Codex path's remaining setup — repository trust, the sandbox's writable roots, the Claude Code CLI prerequisite, network access and the `PATH` export — lives in the runbook, so three printed lines are never mistaken for the whole path.

**D-5 — a new `## Update` section, one per file, printing both hosts' update commands and the one follow-up the Codex path needs.** `/plugin marketplace update ripsawjp` for a Claude Code host (`docs/distribution.md:86`) and `codex plugin marketplace upgrade ripsawjp` for a Codex CLI host. The second substitutes the measured marketplace name for the placeholder `<marketplace>` that `docs/adopting.md:435` prints; the substituted value is `.claude-plugin/marketplace.json`'s own `name` field, and **AC7** asserts that equality by deriving the name at check time rather than by trusting the literal. The Codex-only follow-up is stated because the generated agents go stale on upgrade: re-run the generator, and `bash "<plugin root>/bin/check-codex-agents.sh"` reports the drift without writing anything (`docs/adopting.md:584`–`:597`). This is the **only** new `## ` heading in either file, which is what makes **AC12**'s parity assertion a base-relative delta of exactly one.

**D-6 — the runbook is one link away, per language.** `README.md` links to `docs/adopting.md#using-shell-team-from-codex-cli`; `README.ja.md` links to `docs/adopting.ja.md#codex-cli-から-shell-team-を使う`. The Japanese file links into the Japanese runbook rather than the English one, following the precedent already in that file at `:177`–`:179`.

**D-7 — the honest limits are stated where the adopter chooses, not buried.** Inside the install section of each file, three facts: whichever instruction surface a Codex CLI orchestrator loads automatically is **unmeasured from this repository**, and the runbook's steps are what this project relies on instead; merge and push stay the human's on both hosts (`docs/adopting.md:573`–`:583`); and when the independent reviewer cannot be reached the review returns `BLOCKED` rather than quietly substituting a same-family one. **AC9** pins the three facts by short fixed fragments rather than by whole sentences, so the wording stays editable under this task's one-freeze mode while the meaning stays locked.

**D-8 — no sentence in either README asserts what a Codex CLI host loads, positively or negatively.** This is the class that cost T-1147 two review rounds, and it is made mechanical here: **AC10** requires zero occurrences, in either file, of `never loads`, `does not load`, `doesn't load`, `always loads`, `読み込まない`, `読み込みません`, `必ず読み込` and `読み込んでいます`, and requires that wherever the English phrase `loads automatically` appears it is immediately continued by `is unmeasured`, measured as an equality of two counts over a whitespace-flattened copy so a hard wrap cannot decide the verdict. The permitted form is the disclosure, never the claim.

**D-9 — three regions stay byte-identical to their base blobs, and one of them is an honest outcome rather than a scope cut.** The concept image line and the full-loop-aptitude section are unchanged because the issue says so. `## Develop / dogfood` and `## 開発 / dogfood` are unchanged because a Codex CLI counterpart to `claude --plugin-dir ./` is **unmeasured** from this repository: the runbook's own path to a checkout-rooted plugin is step 2's "a checkout of this repository works too", which is not a dogfood loader invocation, and printing a guess there would be exactly the class **D-8** forbids one section over. **AC11** locks all three regions, with a positive control proving the instrument can see a region that did change.

**D-10 — this task edits two files.** `docs/distribution.md` and `docs/workflow.md` (both languages) are the next task's (#565); `docs/adopting.md` and `docs/adopting.ja.md` are read and linked, never edited; `.claude-plugin/marketplace.json`'s description, which still says "Claude Code plugin marketplace", is observed and deliberately left alone. **AC13**'s allow-list is what makes that a measured property rather than an intention.

**Pre-commitment (AI self-discipline — self-imposed by this loop at freeze time, never operator-ratified; any report citing it says so).** Factual trigger: two consecutive review or QA rounds landing an independent Blocker or Major against the **same component**. Contextual condition: before a third round on that component starts; the factual trigger governs when the two disagree. At the trigger the drop is the **default**, executed on the loop's own authority. **Droppable 1st**: the Japanese badge's and the Japanese runbook link's generated anchors — the fallback is a plain `docs/adopting.ja.md` file link with the section named in the surrounding prose, which needs no anchor-derivation judgment at all, and **AC3**'s and **AC8**'s Japanese clauses then assert the plain link plus the same target-heading existence. **Droppable 2nd**: the `## Update` section's Codex follow-up sentence reduced to a bare pointer at the runbook. Both dropped parts travel to a fresh issue carrying both rounds' findings as their requirement list, never re-derived there. **Never dropped**: the absence of any categorical host-loading claim (**AC10**) and the byte-identity of both hosts' printed commands with the measured sources they came from (**AC5**, **AC6**). Defeat of either across two consecutive rounds is evidence about the design premise rather than implementation craft, and the task **stops and returns to planning**.

## Non-goals

- **No edit to `docs/adopting.md` or `docs/adopting.ja.md`.** Both are read and linked only; **AC13**'s allow-list admits neither.
- **No edit to `docs/distribution.md`, `docs/workflow.md` or `docs/workflow.ja.md`.** Those belong to the next task (#565) and **AC13**'s allow-list admits none of them.
- **No edit to `.claude-plugin/marketplace.json`.** Its description's wording is observed here and left for whoever decides it is worth a task.
- **No new heading other than `## Update` and its Japanese counterpart.** **AC12** pins the delta at exactly one per file.
- **No change to the concept image line, to `## When the full loop fits (task aptitude)` / `## フルループが向くタスク（適性）`, or to `## Develop / dogfood` / `## 開発 / dogfood`.** All three stay byte-identical to their base blobs (**AC11**).
- **No claim, anywhere, about what a Codex CLI host loads as instruction** — positive or negative (**AC10**). The question is unmeasurable from this repository and is disclosed, not answered.
- **No new capability claim.** Nothing here says the loop does anything it did not already do in v2.6.0; what changes is where an adopter can read about it.
- **No unproven self-declaration.** No "standard", "revolutionary", "fully autonomous" or equivalent; no claim that zero humans is the goal, and no removal of a limit the project already discloses.
- **No word-for-word translation lock between the two files.** The Japanese edits carry the same meaning in natural Japanese; parity is asserted structurally (`^## ` counts, the same fixed command strings, the same required fragments per language), never as a sentence-level correspondence.
- **No version bump, no CHANGELOG entry and no release.** The manifest and both version badges stay where they are; **AC12** requires `bin/check-readme-version.sh` still to exit 0, and **AC13**'s allow-list admits neither the manifest nor either changelog.
- **No full-population downstream-impact sweep, no whole CI-equivalent re-run, and no behaviour verification of any mechanism this task does not touch.** This is the `no-mechanism` class's declared pricing: the inventory is read-set-scoped (`## Blast radius`), CI-equivalence runs the one workflow step whose input this diff reaches, and mutation probes cover the `- check:` lines this spec adds.
- **No per-task two-arm blast-radius sweep.** Deferred to the release sweep under the operator's standing mode-A2 ruling; the authority and the deferred scope are named in `## Blast radius`.
- **No `.shell-team/lessons.md` entry and no playbook-block regeneration.**
- **No re-freezing, widening or repairing of any merged task's frozen criterion.**
- **No merging.** Both gates must be green and the pull request joins the stacked train awaiting the batch GO.

## Acceptance criteria

Every `check:` runs from the repository root under `bash`. Every literal carrying a regex metacharacter — every badge string, every bracketed link, every command containing `"` or `.` — is matched as a **fixed string** (`grep -F`, `grep -qxF`, `grep -cF`). Every criterion names the files it reads explicitly, guards every `mktemp` with an immediate non-zero exit, writes nothing outside `$TMPDIR`, and pairs every absence assertion with a positive control. No criterion contains a literal backtick. The three criteria reading a base-side blob — **AC11**, **AC12**, **AC13** — spell the branch-point expression byte-identically.

**AC13** is inherently tied to the merge point it was authored at and says so in its own body: it goes stale by design once this branch merges and the resolved base ref advances past it, and it is not to be widened, re-derived per rework round or otherwise merge-ranged.

- [ ] **AC1** This spec is a conformant carrier of the declaration keys. Inside the region from its `<!-- BEGIN intent-block: T-1148 -->` marker to its `## Non-goals` heading there is exactly one each of the four declaration lines, each with a non-empty value, and each of the four keys occurs exactly once as a line-initial top-level bullet in the **whole** spec so no second copy outside the region can disagree with the frozen one. Because the declaration is `yes` with a named adopter-docs surface, at least one indented `adopter-surface` line exists and zero line-initial `adopter-docs-waiver` bullets exist, the two being exclusive. Because one criterion sits above the declared ceiling, the indented `above-ceiling` marker occurs at least once, and the extracted ceiling declaration line carries the floor-case token `no criterion verified at this ceiling` zero times — that assertion scoped to the one extracted line rather than to the document. Positive controls: the extracted region is asserted to carry at least four non-empty lines before any literal is judged, and the extracted declaration line is asserted non-empty before its token count is trusted.
  - check: rc=0; export LC_ALL=C; S=.shell-team/specs/T-1148-readme-two-hosts.md; test -s "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148a.XXXXXX") || exit 1; awk '/^<!-- BEGIN intent-block: T-1148 -->$/{f=1;next} f&&/^## Non-goals$/{exit} f{print}' "$S" > "$T/reg"; test "$(grep -c . "$T/reg" || true)" -ge 4 || rc=1; for k in '^- user-visible: yes — .' '^- verification-class: no-mechanism — .' '^- verification-ceiling: unit-and-static — .' '^- base-ref-discriminator: not-applicable — .'; do test "$(grep -cE "$k" "$T/reg" || true)" = "1" || rc=1; done; for k in '^- user-visible: ' '^- verification-class: ' '^- verification-ceiling: ' '^- base-ref-discriminator: '; do test "$(grep -c "$k" "$S" || true)" = "1" || rc=1; done; test "$(grep -c '^- adopter-docs-waiver: ' "$S" || true)" = "0" || rc=1; test "$(grep -cE '^[[:space:]]+- adopter-surface: ' "$S" || true)" -ge 1 || rc=1; test "$(grep -cE '^[[:space:]]+- above-ceiling: ' "$S" || true)" -ge 1 || rc=1; grep -m1 '^- verification-ceiling: ' "$S" > "$T/dl" || rc=1; test -s "$T/dl" || rc=1; test "$(grep -cF -- "no criterion verified at this ceiling" "$T/dl" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC2** Neither README opens with a sentence naming only one host, and both name both. The region of `README.md` between its `## What shell-team is` heading and the next `## ` heading, and the region of `README.ja.md` between `## shell-team とは` and the next `## ` heading, each hold at least three non-empty lines, and each region's **first** non-empty line carries the fixed strings `Claude Code` and `Codex CLI`. The retired opening string `A Claude Code **plugin** that drops a dev team into any repo` occurs zero times in `README.md`, and its Japanese counterpart `Claude Code **プラグイン**です` occurs zero times in `README.ja.md`. Positive controls: each extracted region and each extracted first line is asserted non-empty before any token is looked for, and the same fixed-string instrument is required to find `Codex CLI` at least once in each whole file — so a search that could match nothing at all reds here rather than reporting the retired strings absent for the wrong reason.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; test -s "$EN" || exit 1; test -s "$JA" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148b.XXXXXX") || exit 1; awk '/^## What shell-team is$/{f=1;next} f&&/^## /{exit} f{print}' "$EN" > "$T/en"; awk '/^## shell-team とは$/{f=1;next} f&&/^## /{exit} f{print}' "$JA" > "$T/ja"; test "$(grep -c . "$T/en" || true)" -ge 3 || rc=1; test "$(grep -c . "$T/ja" || true)" -ge 3 || rc=1; grep -m1 . "$T/en" > "$T/en1"; grep -m1 . "$T/ja" > "$T/ja1"; test -s "$T/en1" || rc=1; test -s "$T/ja1" || rc=1; for f in "$T/en1" "$T/ja1"; do grep -qF -- 'Claude Code' "$f" || rc=1; grep -qF -- 'Codex CLI' "$f" || rc=1; done; for f in "$EN" "$JA"; do test "$(grep -cF -- 'Codex CLI' "$f" || true)" -ge 1 || rc=1; done; test "$(grep -cF -- 'A Claude Code **plugin** that drops a dev team into any repo' "$EN" || true)" = "0" || rc=1; test "$(grep -cF -- 'Claude Code **プラグイン**です' "$JA" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC3** Both badge rows carry both host badges, and the existing one is not displaced by the new one. For each README, the region before its first `## ` heading is extracted, asserted to hold at least five non-empty lines, and required to carry the existing Claude Code plugin badge line `[![Claude Code plugin](https://img.shields.io/badge/Claude_Code-plugin-d97757?style=flat-square)](docs/distribution.md)` as a fixed string. `README.md`'s row carries, as a whole line, `[![Codex CLI plugin](https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square)](docs/adopting.md#using-shell-team-from-codex-cli)`; `README.ja.md`'s row carries, as a whole line, `[![Codex CLI plugin](https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square)](docs/adopting.ja.md#codex-cli-から-shell-team-を使う)`. Both rows are additionally required to carry the badge's own image URL fragment `https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square`, so a badge added outside the row, or with a mangled image URL, reds here. Positive control: each extracted row is asserted non-empty and at least five lines deep before any badge literal is looked for.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; test -s "$EN" || exit 1; test -s "$JA" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148c.XXXXXX") || exit 1; awk '/^## /{exit} {print}' "$EN" > "$T/hen"; awk '/^## /{exit} {print}' "$JA" > "$T/hja"; for h in "$T/hen" "$T/hja"; do test "$(grep -c . "$h" || true)" -ge 5 || rc=1; grep -qF -- '[![Claude Code plugin](https://img.shields.io/badge/Claude_Code-plugin-d97757?style=flat-square)](docs/distribution.md)' "$h" || rc=1; grep -qF -- 'https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square' "$h" || rc=1; done; grep -qxF -- '[![Codex CLI plugin](https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square)](docs/adopting.md#using-shell-team-from-codex-cli)' "$T/hen" || rc=1; grep -qxF -- '[![Codex CLI plugin](https://img.shields.io/badge/Codex_CLI-plugin-10a37f?style=flat-square)](docs/adopting.ja.md#codex-cli-から-shell-team-を使う)' "$T/hja" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** Prerequisites are stated per host in both files, and the Codex host's own review-side prerequisite is named. The region of `README.md` between `## Prerequisites` and the next `## ` heading, and the region of `README.ja.md` between `## 前提` and the next `## ` heading, each hold at least four non-empty lines. The English region carries the fixed strings `Claude Code`, `Codex CLI`, `Claude Code CLI` and `authenticated`; the Japanese region carries `Claude Code`, `Codex CLI`, `Claude Code CLI` and `認証`. Both regions keep the existing sandbox pointer, required as the fixed string `docs/distribution.md#sandbox-enabled-permission-settings`. `Claude Code CLI` is the load-bearing token: it is what distinguishes a per-host list from today's single list, since a Codex CLI host needs the Claude Code CLI installed and authenticated for the review pass. Positive control: each region is asserted non-empty and at least four lines deep before any token is looked for.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; test -s "$EN" || exit 1; test -s "$JA" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148d.XXXXXX") || exit 1; awk '/^## Prerequisites$/{f=1;next} f&&/^## /{exit} f{print}' "$EN" > "$T/en"; awk '/^## 前提$/{f=1;next} f&&/^## /{exit} f{print}' "$JA" > "$T/ja"; test "$(grep -c . "$T/en" || true)" -ge 4 || rc=1; test "$(grep -c . "$T/ja" || true)" -ge 4 || rc=1; for s in 'Claude Code' 'Codex CLI' 'Claude Code CLI' 'authenticated' 'docs/distribution.md#sandbox-enabled-permission-settings'; do grep -qF -- "$s" "$T/en" || rc=1; done; for s in 'Claude Code' 'Codex CLI' 'Claude Code CLI' '認証' 'docs/distribution.md#sandbox-enabled-permission-settings'; do grep -qF -- "$s" "$T/ja" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** Both hosts' install commands and both hosts' own `team-init` forms stand in the install section of both files, each as an un-indented whole line inside that section. The region of `README.md` between `## Install` and the next `## ` heading, and the region of `README.ja.md` between `## インストール` and the next `## ` heading, each carry all seven of these whole lines: `/plugin marketplace add RipsawJP/shell-team`; `/plugin install shell-team@ripsawjp`; `/shell-team:team-init`; `codex plugin marketplace add RipsawJP/shell-team`; `codex plugin add shell-team@ripsawjp`; `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents`; and `bash "<plugin root>/bin/team-init.sh" .`. Each region is additionally required to name the runbook, as the fixed string `adopting` — so three printed Codex lines are never left standing as the whole path. Positive control: each extracted region is asserted to hold at least ten non-empty lines before any command literal is looked for, so an extraction that silently produced nothing reds here rather than passing seven absence-free assertions over an empty file.
  - adopter-surface: `README.md` and `README.ja.md` — the install and update sections an adopter reads before running anything, on whichever host they already use.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; test -s "$EN" || exit 1; test -s "$JA" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148e.XXXXXX") || exit 1; awk '/^## Install$/{f=1;next} f&&/^## /{exit} f{print}' "$EN" > "$T/en"; awk '/^## インストール$/{f=1;next} f&&/^## /{exit} f{print}' "$JA" > "$T/ja"; for r in "$T/en" "$T/ja"; do test "$(grep -c . "$r" || true)" -ge 10 || rc=1; grep -qF -- 'adopting' "$r" || rc=1; grep -qxF -- '/plugin marketplace add RipsawJP/shell-team' "$r" || rc=1; grep -qxF -- '/plugin install shell-team@ripsawjp' "$r" || rc=1; grep -qxF -- '/shell-team:team-init' "$r" || rc=1; grep -qxF -- 'codex plugin marketplace add RipsawJP/shell-team' "$r" || rc=1; grep -qxF -- 'codex plugin add shell-team@ripsawjp' "$r" || rc=1; grep -qxF -- 'bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents' "$r" || rc=1; grep -qxF -- 'bash "<plugin root>/bin/team-init.sh" .' "$r" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** Every Codex-host command the READMEs print occurs verbatim in the runbook it was read from, so the two surfaces cannot drift apart silently. Each of the four fixed strings `codex plugin marketplace add RipsawJP/shell-team`, `codex plugin add shell-team@ripsawjp`, `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents` and `bash "<plugin root>/bin/check-codex-agents.sh"` occurs at least once in `docs/adopting.md` and at least once in each README — matched as a substring on the runbook side, because the runbook indents these lines inside its own numbered list, and the same literal is what **AC5** and **AC7** require as whole lines in the READMEs. Positive controls: `docs/adopting.md` is asserted non-empty and asserted to carry the whole line `## Using shell-team from Codex CLI`, so a read of the wrong file, or of a file that no longer contains the runbook, reds here rather than satisfying four substring searches by accident.
  - check: rc=0; export LC_ALL=C; A=docs/adopting.md; EN=README.md; JA=README.ja.md; test -s "$A" || exit 1; test -s "$EN" || exit 1; test -s "$JA" || exit 1; grep -qxF -- '## Using shell-team from Codex CLI' "$A" || rc=1; for s in 'codex plugin marketplace add RipsawJP/shell-team' 'codex plugin add shell-team@ripsawjp' 'bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents' 'bash "<plugin root>/bin/check-codex-agents.sh"'; do grep -qF -- "$s" "$A" || rc=1; grep -qF -- "$s" "$EN" || rc=1; grep -qF -- "$s" "$JA" || rc=1; done; test "$rc" -eq 0

- [ ] **AC7** Both files carry a new update section printing both hosts' update commands and the Codex path's own follow-up, with the marketplace name derived rather than asserted. `README.md` carries the whole line `## Update` and `README.ja.md` carries the whole line `## 更新`, each extracted to the next `## ` heading and asserted to hold at least four non-empty lines. The marketplace name is read at check time from `.claude-plugin/marketplace.json`'s first `name` field, asserted non-empty and asserted **not** to be `shell-team` (which is the second `name` in that file, and reading it would mean the extraction picked the plugin instead of the marketplace). Each region then carries, as whole lines, `/plugin marketplace update <name>` and `codex plugin marketplace upgrade <name>` with `<name>` that derived value — a value-independent invariant rather than a literal typed into the check. Each region additionally carries the fixed strings `bash "<plugin root>/bin/gen-codex-agents.sh"` and `check-codex-agents.sh`, the Codex-only follow-up. Positive control: `docs/distribution.md` is asserted to carry the fixed string `/plugin marketplace update ` , so the Claude-side command is shown to be the one this repository already documents rather than one invented here.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; M=.claude-plugin/marketplace.json; D=docs/distribution.md; test -s "$EN" || exit 1; test -s "$JA" || exit 1; test -s "$M" || exit 1; test -s "$D" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148g.XXXXXX") || exit 1; grep -qF -- '/plugin marketplace update ' "$D" || rc=1; N=$(sed -n 's/.*"name"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' "$M" | head -1); test -n "$N" || rc=1; test "$N" != "shell-team" || rc=1; grep -qxF -- '## Update' "$EN" || rc=1; grep -qxF -- '## 更新' "$JA" || rc=1; awk '/^## Update$/{f=1;next} f&&/^## /{exit} f{print}' "$EN" > "$T/en"; awk '/^## 更新$/{f=1;next} f&&/^## /{exit} f{print}' "$JA" > "$T/ja"; for r in "$T/en" "$T/ja"; do test "$(grep -c . "$r" || true)" -ge 4 || rc=1; grep -qxF -- "/plugin marketplace update $N" "$r" || rc=1; grep -qxF -- "codex plugin marketplace upgrade $N" "$r" || rc=1; grep -qF -- 'bash "<plugin root>/bin/gen-codex-agents.sh"' "$r" || rc=1; grep -qF -- 'check-codex-agents.sh' "$r" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** Each README links directly to the Codex runbook in its own language, and each link's target heading exists as a whole line in the target file. `README.md` carries the fixed string `docs/adopting.md#using-shell-team-from-codex-cli` at least once, and `docs/adopting.md` carries the whole line `## Using shell-team from Codex CLI`. `README.ja.md` carries the fixed string `docs/adopting.ja.md#codex-cli-から-shell-team-を使う` at least once, and `docs/adopting.ja.md` carries the whole line `## Codex CLI から shell-team を使う`. What is deliberately **not** claimed: that a renderer resolves either generated anchor — that is GitHub's own algorithm, outside this repository's reach; the target heading's existence is the part that can be measured, and it is what this criterion measures. Positive controls: both runbook files are asserted non-empty first, and each is asserted to carry at least five `^## ` headings, so a truncated or wrong file reds here rather than failing the heading assertion for an unrelated reason.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; A=docs/adopting.md; AJ=docs/adopting.ja.md; for f in "$EN" "$JA" "$A" "$AJ"; do test -s "$f" || exit 1; done; test "$(grep -c '^## ' "$A" || true)" -ge 5 || rc=1; test "$(grep -c '^## ' "$AJ" || true)" -ge 5 || rc=1; grep -qF -- 'docs/adopting.md#using-shell-team-from-codex-cli' "$EN" || rc=1; grep -qxF -- '## Using shell-team from Codex CLI' "$A" || rc=1; grep -qF -- 'docs/adopting.ja.md#codex-cli-から-shell-team-を使う' "$JA" || rc=1; grep -qxF -- '## Codex CLI から shell-team を使う' "$AJ" || rc=1; test "$rc" -eq 0

- [ ] **AC9** The honest limits are stated where the adopter chooses, in each file's own language. Matched against a **whitespace-flattened** copy of each install section — the region between `## Install` and the next `## ` heading in `README.md`, and between `## インストール` and the next `## ` heading in `README.ja.md` — so a hard wrap cannot decide the verdict. The English region carries the fixed strings `unmeasured from this repository`, `merge and push` and `BLOCKED`. The Japanese region carries `このリポジトリからは測定`, `マージ`, `push` and `BLOCKED` — the Japanese fragment written in the Japanese file's own register, measured: `README.ja.md` carries `リポジトリ` on four lines and the English word `repository` on none, so a fragment demanding the English word would make natural Japanese impossible here. Those are the three facts the section owes an adopter: what this project has not measured about a Codex CLI host, that the loop's two irreversible steps stay the human's on both hosts, and that an unreachable independent reviewer yields a refusal rather than a silent same-family substitution. The fragments are short by design: this task freezes the meaning, not the sentences, so wording stays editable under its one-freeze mode. Positive controls: each flattened region is asserted non-empty and each region is asserted to hold at least ten non-empty lines before flattening.
  - check: rc=0; export LC_ALL=C; EN=README.md; JA=README.ja.md; test -s "$EN" || exit 1; test -s "$JA" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148i.XXXXXX") || exit 1; awk '/^## Install$/{f=1;next} f&&/^## /{exit} f{print}' "$EN" > "$T/en"; awk '/^## インストール$/{f=1;next} f&&/^## /{exit} f{print}' "$JA" > "$T/ja"; test "$(grep -c . "$T/en" || true)" -ge 10 || rc=1; test "$(grep -c . "$T/ja" || true)" -ge 10 || rc=1; tr '\n' ' ' < "$T/en" | tr -s ' ' > "$T/fen"; tr '\n' ' ' < "$T/ja" | tr -s ' ' > "$T/fja"; test -s "$T/fen" || rc=1; test -s "$T/fja" || rc=1; for s in 'unmeasured from this repository' 'merge and push' 'BLOCKED'; do grep -qF -- "$s" "$T/fen" || rc=1; done; for s in 'このリポジトリからは測定' 'マージ' 'push' 'BLOCKED'; do grep -qF -- "$s" "$T/fja" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC10** Neither README claims what a Codex CLI host loads — positively or negatively. This is a **never-dropped** component. Across `README.md` and `README.ja.md`, each of the fixed strings `never loads`, `does not load`, `doesn't load`, `always loads`, `読み込まない`, `読み込みません`, `必ず読み込` and `読み込んでいます` occurs **zero** times. The permitted hedged form is bound rather than banned: over a whitespace-flattened copy of each whole file, the count of `loads automatically` equals the count of `loads automatically is unmeasured`, so the phrase may appear only as part of the disclosure and never as a standing claim — an equality that is satisfied both by the disclosed form and by the phrase not appearing at all. The population is two named files and cannot grow, so this criterion carries no re-measurement trigger. Positive control: each flattened copy is asserted non-empty and asserted to carry `Codex CLI` at least once, so a search over an empty intermediate cannot report eight absences as a pass.
  - check: rc=0; export LC_ALL=C; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148j.XXXXXX") || exit 1; for f in README.md README.ja.md; do test -s "$f" || exit 1; tr '\n' ' ' < "$f" | tr -s ' ' > "$T/flat"; test -s "$T/flat" || rc=1; test "$(grep -cF -- 'Codex CLI' "$T/flat" || true)" -ge 1 || rc=1; for s in 'never loads' 'does not load' "doesn't load" 'always loads' '読み込まない' '読み込みません' '必ず読み込' '読み込んでいます'; do test "$(grep -cF -- "$s" "$f" || true)" = "0" || rc=1; done; a=$(grep -oF -- 'loads automatically' "$T/flat" | grep -c . || true); b=$(grep -oF -- 'loads automatically is unmeasured' "$T/flat" | grep -c . || true); test "$a" = "$b" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** The three regions this task declares untouched are byte-identical to their base-ref blobs. The branch point is resolved by the frozen expression: an explicit `git show-ref --verify --quiet` test against `refs/heads/develop`, then against `refs/remotes/origin/develop`, taking `git merge-base` against whichever namespace resolved and exiting non-zero when neither does. Both READMEs' base blobs are read with `git show` and asserted non-empty. For each file, three extractions are compared with `cmp -s` against the same extraction from the working tree: the full-loop-aptitude section (`## When the full loop fits (task aptitude)` / `## フルループが向くタスク（適性）`, each extracted by whole-line equality rather than by a regex, since both headings carry parentheses); the dogfood section (`## Develop / dogfood` / `## 開発 / dogfood`); and the concept image line (the line carrying `![shell-team concept` / `![shell-team の概念図`). Each base-side extraction is asserted to hold at least three non-empty lines, or to be non-empty for the image line, before any comparison is trusted — three rather than five because the dogfood section is measured to carry exactly four non-empty lines in each file, and a depth control must not red on a region it was written to protect. Positive control on the instrument: the `## What shell-team is` region's base blob is extracted in the same way and asserted to **differ** from the working tree's, so a `git show` that silently produced the working tree, or an extraction that always returns nothing, reds here rather than passing five comparisons over.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/develop; then B=$(git merge-base develop HEAD); elif git show-ref --verify --quiet refs/remotes/origin/develop; then B=$(git merge-base refs/remotes/origin/develop HEAD); else exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148k.XXXXXX") || exit 1; git show "$B:README.md" > "$T/ben" || rc=1; git show "$B:README.ja.md" > "$T/bja" || rc=1; test -s "$T/ben" || rc=1; test -s "$T/bja" || rc=1; x() { awk -v h="$2" '$0==h{f=1;next} f&&/^## /{exit} f{print}' "$1"; }; sec() { x "$1" "$3" > "$T/s.base"; x "$2" "$3" > "$T/s.head"; test "$(grep -c . "$T/s.base" || true)" -ge 3 || rc=1; cmp -s "$T/s.base" "$T/s.head" || rc=1; }; sec "$T/ben" README.md '## When the full loop fits (task aptitude)'; sec "$T/ben" README.md '## Develop / dogfood'; sec "$T/bja" README.ja.md '## フルループが向くタスク（適性）'; sec "$T/bja" README.ja.md '## 開発 / dogfood'; img() { grep -F -- "$3" "$1" > "$T/i.base"; grep -F -- "$3" "$2" > "$T/i.head"; test -s "$T/i.base" || rc=1; cmp -s "$T/i.base" "$T/i.head" || rc=1; }; img "$T/ben" README.md '![shell-team concept'; img "$T/bja" README.ja.md '![shell-team の概念図'; x "$T/ben" '## What shell-team is' > "$T/c.base"; x README.md '## What shell-team is' > "$T/c.head"; test -s "$T/c.base" || rc=1; cmp -s "$T/c.base" "$T/c.head" && rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** The two files stay at heading parity, the new heading is the only one added, and the version-badge lock still passes. The branch point is resolved by the same frozen expression **AC11** uses, spelled byte-identically. The `^## ` heading counts of both base blobs and both working-tree files are derived at check time — no absolute literal is written into the check. The base counts are asserted equal to each other and at least one; the working-tree counts are asserted equal to each other; and each working-tree count is asserted to equal its own base count **plus exactly one**, which is the base-relative delta form of "`## Update` and `## 更新` are the only new headings". `bash bin/check-readme-version.sh README.md README.ja.md` exits 0, confirming the shipped badge lock is untouched by the row this task adds to. Positive control: both base blobs are asserted non-empty before any count is taken.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/develop; then B=$(git merge-base develop HEAD); elif git show-ref --verify --quiet refs/remotes/origin/develop; then B=$(git merge-base refs/remotes/origin/develop HEAD); else exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148l.XXXXXX") || exit 1; git show "$B:README.md" > "$T/ben" || rc=1; git show "$B:README.ja.md" > "$T/bja" || rc=1; test -s "$T/ben" || rc=1; test -s "$T/bja" || rc=1; be=$(grep -c '^## ' "$T/ben" || true); bj=$(grep -c '^## ' "$T/bja" || true); he=$(grep -c '^## ' README.md || true); hj=$(grep -c '^## ' README.ja.md || true); test "$be" -ge 1 || rc=1; test "$be" = "$bj" || rc=1; test "$he" = "$hj" || rc=1; test "$((10#$he))" = "$((10#$be+1))" || rc=1; test "$((10#$hj))" = "$((10#$bj+1))" || rc=1; bash bin/check-readme-version.sh README.md README.ja.md || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a later task adds or removes a `## ` heading in either README, at which point the base-relative delta of exactly one no longer describes this branch's own change and the criterion has to be re-derived rather than adjusted by guess.

- [ ] **AC13** The diff stays inside the allow-list, across an unbroken four-read union spanning base to HEAD to index to worktree. **This criterion is merge-point-scoped and is expected to go stale after this branch merges** — once a later task's files land on the same base ref its expected set no longer describes reality; it is not to be widened, re-derived per rework round or otherwise merge-ranged. The branch point is resolved by the same frozen expression **AC11** uses, spelled byte-identically. The measured set is the union of `git diff --no-renames --name-only "$B"...HEAD`, `git diff --no-renames --cached --name-only`, `git diff --no-renames --name-only` and `git ls-files --others --exclude-standard`, each of the three `git diff` reads spelled with `--no-renames` so a rename's source path is never invisible to all four at once; every path in that union is a member of the allow-list — this spec, `.shell-team/todo.md`, `.shell-team/provenance/T-1148.md`, `.shell-team/interventions/T-1148.md`, `.shell-team/reviews/T-1148.md`, `README.md`, `README.ja.md` — the task's required deliverables included from the start. Two controls on the instrument itself: the committed-range read is asserted non-empty, so a failed or empty diff cannot satisfy the subset assertion vacuously; and the assembled allow-list is asserted **not** to contain `docs/adopting.md`, a path this task's Non-goals forbid, so an allow-list that had silently grown to admit everything reds here rather than passing every path handed to it.
  - check: rc=0; export LC_ALL=C; if git show-ref --verify --quiet refs/heads/develop; then B=$(git merge-base develop HEAD); elif git show-ref --verify --quiet refs/remotes/origin/develop; then B=$(git merge-base refs/remotes/origin/develop HEAD); else exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1148m.XXXXXX") || exit 1; printf '%s\n' .shell-team/specs/T-1148-readme-two-hosts.md .shell-team/todo.md .shell-team/provenance/T-1148.md .shell-team/interventions/T-1148.md .shell-team/reviews/T-1148.md README.md README.ja.md > "$T/allow"; test -s "$T/allow" || rc=1; grep -qxF -- 'docs/adopting.md' "$T/allow" && rc=1; git diff --no-renames --name-only "$B"...HEAD > "$T/r1" || rc=1; test -s "$T/r1" || rc=1; git diff --no-renames --cached --name-only > "$T/r2" || rc=1; git diff --no-renames --name-only > "$T/r3" || rc=1; git ls-files --others --exclude-standard > "$T/r4" || rc=1; cat "$T/r1" "$T/r2" "$T/r3" "$T/r4" | sort -u > "$T/union"; while IFS= read -r p; do test -n "$p" || continue; grep -qxF -- "$p" "$T/allow" || rc=1; done < "$T/union"; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC14** An adopter on a Codex CLI host, starting from the README alone, reaches a working install: the three Codex install lines and the `team-init` form print what that host actually accepts, the update commands upgrade an existing install, and the runbook link lands on the steps the README defers to. This cannot be produced from this repository's own checkout, which has no Codex CLI host to run them on — every criterion above measures what the two files **say**, and none of them measures a host accepting a command. Nothing below the ceiling substitutes for it: **AC5** and **AC7** pin the printed strings, **AC6** pins their agreement with the runbook they were read from, and neither observes a real host.
  - above-ceiling: the operator's next Codex-host adopter run — the measurement belongs to that run and is recorded there, not in this task's own gates.

## Input space

**Reachable input classes** — the situations these two files really meet, which the wording must handle correctly:

1. An adopter reading `README.md` on GitHub, on a Claude Code host, deciding how to install for the first time.
2. An adopter reading either file on a **Codex CLI host**, deciding the same thing — the class the current text serves worst, and the reason this task exists.
3. An adopter who already installed on one host and now wants to upgrade, reaching for an `## Update` section that does not yet exist.
4. An adopter following the runbook link out of the README into `docs/adopting.md` or `docs/adopting.ja.md` and back.
5. A Japanese reader for whom `README.ja.md` is the only file read, so every fact the English file gains has to be present there in natural Japanese.
6. `bin/check-readme-version.sh`, reading both files in CI through the workflow's own dogfood step — the only shipped checker that reads them.
7. A merged spec's `- check:` line reading either README's bytes, its `^## ` heading count, a named region, or its version badge.
8. GitHub's own markdown renderer resolving the badge images, the intra-repo file links and the generated heading anchors.
9. A reader treating the README's honest-limits sentence as the project's statement of what it has and has not measured about a Codex CLI host.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect:

1. A Codex CLI version whose `codex plugin` subcommands differ from the ones the runbook read on codex-cli 0.154.0. The README prints what was measured there and points at the runbook, which carries the version it was measured on.
2. Anchor-resolution behaviour of any renderer other than GitHub's, and any future change to GitHub's own anchor-generation algorithm. **AC8** verifies the target heading exists; it claims nothing about the fragment resolving.
3. What a Codex CLI orchestrator loads as instruction, at any path and in any form. Unmeasurable from this repository, disclosed rather than asserted, and forbidden as a claim by **AC10**.
4. Ever-larger or adversarial README content — thousands of badges, tables of unbounded width, non-UTF-8 bytes, control characters. These files are hand-authored prose in this repository.
5. A fork or adopter copy whose README was already edited away from this one.
6. Sentence-level or word-for-word equivalence between the English and Japanese files. Parity is structural, and a translation checker is neither shipped nor implied.
7. The air-gapped / vendored install path (`bin/install`) and every other distribution route. Those live in `docs/distribution.md`, which this task does not edit.
8. A host other than Claude Code or Codex CLI. Nothing here generalizes to a third one.

<!-- END intent-block: T-1148 -->

## Body-to-AC correspondence

| Body directive | Promoted to |
|---|---|
| The opening sentence names both hosts and the retired one-host sentence is gone (**D-1**) | **AC2** |
| The section's reviewer apposition and the review bullet stop attributing the review to one provider (**D-1**) | **AC2** (the first line names both hosts) plus **AC11**'s positive control, which requires that region to have changed; the sentence-level rewrite itself is info-only (not promoted to AC) — it is prose accuracy the reading gates judge, and freezing its bytes would contradict this task's wording-mutable mode |
| A Codex CLI plugin badge stands beside the Claude Code plugin badge, in the shipped badge row, with the exact string per file (**D-2**) | **AC3** |
| Anchor resolution is not claimed; the target heading's existence is (**D-2**, **D-6**) | **AC8** |
| Prerequisites are stated per host, and a Codex CLI host needs Claude Code CLI installed and authenticated (**D-3**) | **AC4** |
| Both hosts' install commands stand side by side, each followed by that host's own `team-init` form (**D-4**) | **AC5** |
| The Codex-host `team-init` form is derived from `bin/team-init.sh`'s usage and the README's own invocation convention (**D-4**) | **AC5** (the exact derived line) |
| The Codex block's three lines are never presented as the whole path; the runbook is named in the same section (**D-4**) | **AC5** (the region names `adopting`) |
| Every Codex-host command the READMEs print matches the runbook it came from (**D-4**, **D-5**) | **AC6** |
| A new `## Update` section prints both hosts' update commands (**D-5**) | **AC7** |
| The Codex update command's marketplace name equals `marketplace.json`'s own `name` (**D-5**) | **AC7** (derived at check time, not typed as a literal) |
| The Codex path's upgrade follow-up — regenerate the agents, `check-codex-agents.sh` reports drift (**D-5**) | **AC7** |
| `## Update` / `## 更新` is the only new heading (**D-5**) | **AC12** (base-relative delta of exactly one, per file) |
| The runbook is one link away, per language (**D-6**) | **AC8** |
| The honest limits — unmeasured host loading, merge and push stay the human's, an unreachable reviewer yields `BLOCKED` (**D-7**) | **AC9** |
| No claim about what a Codex CLI host loads, positive or negative (**D-8**, never-dropped) | **AC10** |
| The concept image line, the full-loop-aptitude section and the dogfood section are byte-identical to base (**D-9**) | **AC11** |
| `## Develop / dogfood` stays Claude-only because a Codex counterpart is unmeasured (**D-9**) | **AC11** (byte-identity) and **AC10** (no claim may be substituted for the missing measurement) |
| This task edits two files and no others; `docs/adopting.md`, `docs/distribution.md`, `docs/workflow.md` and `marketplace.json` are untouched (**D-10**) | **AC13** |
| No version bump, no CHANGELOG, no release | **AC12** (the version lock still passes) and **AC13** (the allow-list admits none of those paths) |
| No word-for-word translation lock; parity is structural | **AC12** (heading-count parity) plus **AC4**, **AC5**, **AC7**, **AC9**, each of which states the Japanese file's own required fragments in Japanese — info-only beyond that, deliberately, because a sentence-level lock is the thing this directive forbids |
| No unproven self-declaration; shipped fact separated from future hope | info-only (not promoted to AC) — a brand-conformance judgment the reading gates make, named in `## Notes for engineer` as a mandatory item for the verifying roles; a grep for a banned adjective list would be a weaker gate than a reader and would freeze a vocabulary this task has no authority to close |
| Declaration keys are conformant | **AC1** |
| The Codex-host adopter actually reaches a working install from what is printed | **AC14** (above the ceiling, owner named) |

## Blast radius

The downstream-impact inventory this task owes is **read-set-scoped**, which is what the `no-mechanism` class prices it at: this diff reaches `README.md` and `README.ja.md` and nothing else, so the affected population is the merged criteria that read either path. The set is derived at run time rather than restated as a count:

- reproduce: git grep -l -E '^[[:space:]]*- check:.*README\.(ja\.)?md' -- .shell-team/specs

Each member is run against the base ref's committed blobs and again at HEAD, and the two verdict sets are differenced criterion by criterion. Two classes of flip are predictable and named in advance: a merged criterion asserting either README byte-identical to a base ref, counting its lines, or counting its `^## ` headings flips red because this task adds a heading and a badge line to both; and a merged criterion extracting a named README region that this task edits (the opening section, the install section, the badge row) flips for the same reason. Both are merge-point-scoped staleness in already-merged specs, not behavioural regressions, and no repair is owed to them here.

The derivation above is a **lower bound**, not the population: a merged criterion reaching either README through indirection — a path built at run time, a file named only through a variable, a loop over a directory listing — matches no search for those literal bytes and is invisible to it in principle. That gap is discharged by disclosure rather than by widening the pattern, and the release sweep is what closes it.

The **full-population** two-arm inventory is **deferred to this release's single sweep**, not taken per task. The authority is the operator's standing mode-A2 ruling for this repository's own loop (the two-arm sweeps are not run per task; once, immediately before the release), relayed into this task's brief by the coordinating session and recorded on this task's board entry. This is a disclosed deviation, not a silent one.

CI-equivalence, reverse-mapped: the only workflow step whose input this diff can reach is `Dogfood check-readme-version on the repo's READMEs` (`.github/workflows/check-handoff.yml:113`–`:114`), which **AC12** runs. Every other step is named individually as inapplicable in `## Notes for engineer` rather than dismissed in one sentence.

## Version derivation note

Derived tier: **PATCH**, matching the premise on record (v2.7.2). CONTRIBUTING's two tests, applied to this task's own item rather than to the backlog:

- **Headline test — not met.** An adopter's one-line summary of the release is not "the README mentions Codex CLI". The capability being described — driving the whole loop from a Codex CLI session to `READY_FOR_MERGE` — shipped in v2.6.0 and earned its headline there; what changes here is that an adopter can find it from the front page. Discoverability of a shipped capability is not a new capability.
- **Default-reachability test — not met.** Nothing an adopter runs behaves differently after this change. No shipped default moves, no code path is added, and the same commands produce the same results as before the edit.

Verdict: both tests not met, so PATCH, equal to the recorded premise; no deviation notice is owed. The `- user-visible: yes` declaration answers adopter-visibility — whether this task owes adopter-facing documentation — which it discharges by *being* that documentation, and is not a tier verdict.

## Review depth

The dev-scaffold review carve-out recorded in this checkout's own operator overrides — which prices a spec's inline `- check:` lines below adversarial completeness — **does not apply to this task**, and is named here so it is not claimed by default: that carve-out is scoped to `user-visible: no` tasks with no adopter surface, and this task declares `user-visible: yes` with both READMEs as the surface. The shipped prose is priced at full strength: a finding that a printed command is wrong, that a stated prerequisite is not what the runbook measures, that a limit is overstated or hidden, or that a sentence claims what a Codex CLI host loads, is a Major. The spec's own inline `- check:` lines remain instruments rather than shipped product, and a finding about one of them is priced on whether it produces a wrong verdict, not on whether an adversary could construct an input that defeats it.

## Assumptions

- **Relayed — the body of GitHub issue #564 and the operator's 2026-09-18 direction.** This role has no GitHub-reading tool; both reached it verbatim in the task brief from the coordinating session, which holds the primary copy. Every claim either one makes about this repository's own files has been re-measured first-hand and is recorded in `## Summarized sources` at the measured line numbers — including the two retired opening strings, the absence of a `## Update` heading in both READMEs, the Codex install and upgrade commands at `docs/adopting.md:393`–`:394` and `:435`, the generator at `:447`, the drift reporter at `:592`, and the runbook's own lack of a `team-init` step. Nothing in the frozen region rests on the relayed wording alone. The freeze run confirms the issue number and its open state.
- **Relayed — the release-tier premise PATCH (v2.7.2).** Recorded in the task brief as this sprint's premise. The derivation above is this role's own, from CONTRIBUTING's two tests; it agrees with the premise.
- **Relayed — mode A2 and its deferrals** (pm-authored spec, no spec review, one freeze, wording mutable, 5-line hand-offs, no per-task two-arm sweep). The authority is the operator's standing ruling for this checkout, named in `## Blast radius`.
- **Relayed — the brand principles the public wording follows** (separate shipped fact from future hope; no unproven self-declaration; the author's experience before jargon; convey why the design is what it is; do not hide limits). The maintainer's own brand document is not readable from this role; the principles reached it in translation through the task brief. They are carried as authoring constraints and as a mandatory reading item for the verifying roles, never as a `- check:` line — a grep over an adjective list would be a weaker gate than a reader and would freeze a vocabulary this task has no authority to close.
- **Measured — the branch's own base.** This branch was cut from `develop` at `970edc1c` and no predecessor pull request is open, which is why `- base-ref-discriminator:` is `not-applicable` and all three base-side criteria resolve `develop` through one `show-ref`-gated expression. The freeze run confirms that `develop` resolves in `refs/heads/` or `refs/remotes/origin/` in this checkout; where it resolves in neither, the expression fails closed and the criterion reds rather than silently reading something else.
- **Borrowed-vocabulary count-premise classification.** The literal count premises this spec makes, each with the measurement command the freeze run executes at the branch point:
  - `A Claude Code **plugin** that drops a dev team into any repo` at **zero** occurrences in `README.md` at HEAD (**AC2**) — **borrowed**: pre-existing README prose, not a literal this task coins. Command at the branch point: `git grep -c -F -e 'A Claude Code **plugin** that drops a dev team into any repo' <branch point> -- README.md`, expected to report **one** occurrence, which is what makes the HEAD-side zero a removal rather than a string that was never there.
  - `Claude Code **プラグイン**です` at **zero** occurrences in `README.ja.md` at HEAD (**AC2**) — **borrowed**, same class. Command at the branch point: `git grep -c -F -e 'Claude Code **プラグイン**です' <branch point> -- README.ja.md`, expected to report **one**.
  - `never loads`, `does not load`, `doesn't load`, `always loads`, `読み込まない`, `読み込みません`, `必ず読み込`, `読み込んでいます` each at **zero** occurrences in both READMEs (**AC10**) — **borrowed**: generic vocabulary, and the defect class was coined by T-1147 rather than by this task, so a sweep scoped to this task's own new literals would say nothing about them. Command at the branch point, per token: `git grep -c -F -e '<token>' <branch point> -- README.md README.ja.md`, each expected to report **zero**; a non-zero reading is a pre-existing occurrence this task must remove rather than a criterion to weaken.
  - `## Update` and `## 更新` each at **zero** occurrences as whole lines at the branch point (**AC7**, **AC12**) — **borrowed**: `## Update` is a heading `docs/distribution.md` already uses, so the premise is scoped to the two README paths rather than to the corpus. Command: `git grep -c -F -e '## Update' <branch point> -- README.md` and the Japanese counterpart against `README.ja.md`, both expected **zero**.
  - `Codex_CLI-plugin` at **zero** occurrences at the branch point (**AC3**) — **own-coinage**: the badge slug is introduced by this task. Command: `git grep -l -F -e 'Codex_CLI-plugin' <branch point>`, expected to exit 1 with no output.
  - The freeze run measures each at the branch point and records the value beside this line.
  - measured at the branch point (`970edc1c`, 2026-09-18, freeze run): `A Claude Code **plugin** that drops a dev team into any repo` = **1** line in `README.md`; `Claude Code **プラグイン**です` = **1** line in `README.ja.md`; each of the eight forbidden host-loading phrasings = **0** in both READMEs (and `loads automatically` = 0 in both); `## Update` = **0** whole lines in `README.md`, `## 更新` = **0** in `README.ja.md`; `Codex_CLI-plugin` matched **0** tracked files. No enumerated token's recorded value contradicts the criterion asserting it. Pre-freeze correction recorded: AC9's Japanese fragment was re-authored from a foreign-register form to `このリポジトリからは測定` (the Japanese README's own vocabulary, `リポジトリ` ×4 lines / `repository` ×0), before any hash was recorded.

## Open questions

None blocking.

## Notes for engineer

**Files this task edits**, and nothing else (**AC13** locks it): `README.md`, `README.ja.md`, plus this task's own records (`.shell-team/todo.md`, `.shell-team/provenance/T-1148.md`, `.shell-team/interventions/T-1148.md`, `.shell-team/reviews/T-1148.md`). `docs/adopting.md` and `docs/adopting.ja.md` are read and linked; `docs/distribution.md`, `docs/workflow.md` and `docs/workflow.ja.md` belong to the next task (#565).

**Drafts to refine, not text to paste blindly.** The criteria pin fixed strings for commands, badges and short meaning-bearing fragments; the sentences around them are yours to write well, in each file's own language. These are the coordinating session's drafts, carried here so the intent is legible:

- English opening (`## What shell-team is`): *A spec-driven dev team you can run from **either Claude Code or Codex CLI** — the same roles, the same status-flag gates, the same cross-provider review — installed once as a plugin on whichever host you already use.* The Japanese counterpart says the same thing in natural Japanese, not word for word. The apposition that currently reads "a Codex-powered cross-provider Reviewer", and the bullet that currently says the final review runs through Codex CLI, both need to become host-aware: on a Claude Code host the review runs through Codex CLI, and on a Codex CLI host it runs through a `claude -p` pass (`docs/adopting.md:375`–`:384`).
- English honest limits, in the install section: *Which instruction surface a Codex CLI orchestrator **loads automatically is unmeasured** from this repository — the runbook's steps are what this project relies on instead. The loop's own stop points are the same on both hosts: **merge and push** stay yours, and when the independent reviewer cannot be reached the review returns `BLOCKED` rather than quietly falling back to a same-family one.* Note the bolded fragments: **AC9** requires `unmeasured from this repository`, `merge and push` and `BLOCKED`, and **AC10** requires that wherever `loads automatically` appears it is continued by `is unmeasured`.
- Japanese honest limits: the same three facts, carrying `このリポジトリからは測定`, `マージ`, `push` and `BLOCKED` — e.g. *Codex CLI の orchestrator がどの instruction 面を自動で読み込むかは、**このリポジトリからは測定**できていません。拠り所にするのは runbook の手順です。ループ自身の停止点はどちらの host でも同じで、**マージ**と **push** はあなたの判断のまま、独立レビュアーに到達できない場合は同一ファミリーへ黙って切り替えず `BLOCKED` を返します。* Do not write `読み込まない` or any other categorical form — **AC10** forbids eight of them by name, and this is the class that cost T-1147 two review rounds.

**Layout of the install section.** Two labelled blocks, Claude Code first (it is the older path and the larger audience), Codex CLI second, each as its own fenced block so every command line sits un-indented at the start of its line — **AC5** matches them with whole-line equality and an indented line will not match. Each block is followed by that host's own per-repo step, also un-indented. The existing `.shell-team/`-in-git paragraph and the `docs/distribution.md` pointer stay; the pointer's "updates" wording may be adjusted now that `## Update` exists, and that adjustment is yours to make or skip.

**Invocation discipline.** Every shell command either README prints for an adopter is spelled `bash "<plugin root>/bin/<script>"`. `docs/adopting.md:410`–`:414` records that `bin/` on `PATH` is measured **false** for an installed plugin on either host, so a bare-name invocation in shipped prose is a defect even though it resolves in this checkout — the convention the README already uses at `:240` and `:265`.

**Measured-at-ref labels.** Not applicable — no deliverable of this task prints a command beside a label naming the git ref its result was measured at, so the measured-at-ref criterion the spec-completion self-check calls for has no subject here. The three criteria that read a base-side blob do so through `git show` and `git diff <ref>...HEAD`, never through the working tree.

**CI-equivalence, step by step.** The workflow's only step whose input this diff reaches is `Dogfood check-readme-version on the repo's READMEs` (`:113`–`:114`), which **AC12** runs. Named individually as inapplicable, with the reason: `shellcheck` (`:28`–`:29`) reads only `bin/` and `tests/` scripts, none of which this task touches; the board-template lint and every fixture-suite step read `templates/`, `tests/` and `.shell-team/` paths outside this diff; the `check-readme-version` **fixture** suite (`:110`–`:111`) reads its own fixtures, not these READMEs. Run them anyway if cheap, but the derived reached-set is the one step above.

**Mandatory items for the verifying roles**, stated here because no criterion claims them:

- **Reading judgments, not check lines.** (a) Every Codex-path sentence must be entailed by `docs/adopting.md`'s runbook — in particular that a Codex CLI host still needs Claude Code CLI installed and authenticated for the review pass (step 7), that merge and push stay the human's (step 10), and that an unavailable independent reviewer yields `BLOCKED` rather than a silent substitution. (b) No sentence, in either file, claims what a Codex CLI host loads — **AC10** catches eight phrasings, and a ninth phrasing is exactly what a reader is for. (c) Brand conformance: shipped fact separated from future hope, no unproven self-declaration, limits stated rather than hidden. (d) English/Japanese semantic equivalence: every fact one file gained is present in the other, in natural Japanese rather than a word-for-word rendering.
- **Mutation probes**, one per `- check:` line this spec adds: restore the retired English opening sentence; delete the Codex badge line from one row; delete one of the seven install lines from one region; change one Codex command by a byte so it no longer matches `docs/adopting.md`; delete the `## 更新` heading; replace the derived marketplace name with a literal that differs; delete the runbook link from one file; delete one honest-limits fragment; insert the phrase `never loads` into one file; edit one line inside the dogfood section; add a second new heading; add a path outside the allow-list. Confirm each reds the criterion it should, restore byte-identically, confirm green again.
- **Anchor sanity.** **AC8** deliberately does not claim a renderer resolves either anchor. Before hand-off, open both READMEs' rendered form (or the runbook files' heading text) and confirm by eye that the two link targets are the headings intended — the part no command here measures.
