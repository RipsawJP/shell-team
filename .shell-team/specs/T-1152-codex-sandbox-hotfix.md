# Shipped `codex exec` blocks become single bare commands so Claude Code 2.1.278 and later run them outside the sandbox, and a review run inside the sandbox surfaces as `BLOCKED`

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1152

**Branch**: `feature/T-1152-codex-sandbox-hotfix`, cut from `develop` at `30d71fcb`. **No predecessor pull request is open**, so the branch point is `develop` itself. Every criterion below that reads a base-side blob names that commit directly, resolved by `git rev-parse --verify 30d71fcb^{commit}` and spelled byte-identically in each, so a checkout that does not carry the object fails closed instead of resolving something else. The literal is written on purpose rather than resolved through `git merge-base develop HEAD`: `develop` moves when later work merges, and a criterion resolving it at read time would silently re-point at a commit that is not this branch's base.

## Problem

Claude Code 2.1.278 changed `sandbox.excludedCommands` so that a Bash command is exempted only when **every part** of it matches, not just its leading command. Every shipped `codex exec` block in this plugin ends in a shell redirection (`> "<RAW_JSONL>" 2>&1`, some also `< /dev/null`), and the alternate-executor recipe also passes its prompt as a `"$(cat …)"` command substitution. Measured on this host (Claude Code 2.1.281), each of those counts as a separate, non-matching part, so the whole call runs **inside** the outer sandbox. Codex then cannot reach its backend or run its own tools. The cross-provider review either fails loudly (`workspace routing discovery failed` → `turn.failed`, exit 1, no `-o` written) or, as measured on 2.1.278, "succeeds" with exit 0 and a final message of `Unable to determine.` that `codex-capture.sh --publish` accepts as a verdict. The same bare call without any redirection runs outside the sandbox and completes normally. `docs/distribution.md` still says the exclusion matches on a command line's first token, which is no longer true.

## Summarized sources

- the issue #586 body, its 2026-09-24 adopter-run comment and the orchestrator's initial design ruling (the relayed scratch copy `scratchpad/t1152/issue-586.md`, read first-hand; the primary source is GitHub issue #586, held by the coordinating session). Distinctions carried over: the break sits between 2.1.275 and 2.1.278 and matches that version's CHANGELOG entry "every part must now match"; **two failure shapes with one root cause** — silent (exit 0, `Unable to determine.`, `sandbox_apply` errors, 2.1.278) and loud (`turn.failed` / `workspace routing discovery failed`, 2.1.281); adopters running with the sandbox off, or with `allowUnsandboxedCommands: true`, are unaffected; the sub-agent misread `<sandbox_violations>` as a network-allowlist problem and kept adding hosts; `codex exec` has no flag that writes the JSONL event stream to a file (`-o` captures only the final message).
- the tech-lead Routing Map (`scratchpad/t1152/routing-map.md`, read first-hand). Distinctions carried over: the AC skeleton AC1–AC6, the file list, the Out-of-scope list (`host-dispatch.md` `claude -p` line and its SKILL copy, verdict grammar, new checker, `resolve-executor.sh` bindings, the Codex CLI host `spawn_agent` path, the `permissions.allow` form), the rule that the five-marker structure and per-site counts (code-reviewer 3, drift 1) stay, and Concerns 1–7.
- the orchestrator's rulings 1–13 (`scratchpad/t1152/orchestrator-rulings.md`, read first-hand). They override the Routing Map where they differ. Distinctions carried over: stream-token detection moves to agent prose, recorded as a deviation from issue #586's draft AC 2 (ruling 1); **no token scan is added to the script on either path** (rulings 1, 9); `--alloc --out-only` prints one path while plain `--alloc` stays byte-for-byte unchanged (ruling 2); the `.txt` is never modified and a missing `<stem>.jsonl` *is* the record, plus exactly one named stderr line (ruling 3); the poll target becomes "`<RAW_OUT>` exists and is non-empty", and the Bash tool's over-timeout behaviour is not measured (ruling 4); `host-dispatch.md` is a Non-goal on two grounds (ruling 5); the L42 audit source changes to the harness output file, or a disclosure sentence if that file does not keep the stream (ruling 6); `< /dev/null` is dropped everywhere (ruling 8); a literal-path prompt replaces `"$(cat "<BRIEFING_FILE>")"` (ruling 12); backslash-continued multi-line prompts may stay (ruling 13); and the pre-commitment (ruling 10).
- the probe record (`scratchpad/t1152/probe-evidence.md`, read first-hand; measured by the coordinating session on this host). Distinctions carried over: Probe A (bare) exit 0, Codex ran `wc -l`, `-o` = `365`, no sandbox symptom; Probe B (shipped tail) exit 1, no command execution, `-o` never written, 12× `workspace routing discovery failed` → `turn.failed`, **no** `sandbox_apply` string; probe (a) bare `review --base` works; (b) a backslash-newline inside the quoted prompt is harmless; (c) a `"$(cat file)"` argument fails exactly like Probe B; (d) a literal sentence naming the briefing file's path works.
- `agents/code-reviewer.md` — read first-hand at `:10`, `:33`–`:94`, `:148`, `:175`–`:198`, `:212`–`:220`. Distinctions: three `codex` marker blocks (primary `:45`, adversarial `:73`–`:78`, spec-review `:182`–`:186`), each with `alloc`/`diagnose`/`cleanup`/`publish` blocks that reference `"<RAW_JSONL>"`; the poll-fallback prose on `turn.completed` in `<RAW_JSONL>` is at `:37`, `:65`, `:175`; `:148` tells the role to save the JSONL stream as `T-XXX-codex-primary.jsonl`; `:212`/`:218` name `{txt,jsonl}` canonical stems **that the Codex-CLI-host `claude -p` path still publishes**, so those two lines are *not* stale; `:216`'s troubleshooting note points at a missing exclusion.
- `agents/drift-evaluator.md` — read first-hand at `:55`–`:62`, `:80`–`:114`, `:140`. Distinctions: one `codex` marker block (`:89`–`:99`) with the same tail; `:82` says "redirecting into them"; `:140` names `T-XXX-drift-codex.jsonl`; this role's failure disposition is "tooling precondition failure, never fabricate one of the four verdicts", not `BLOCKED`.
- `templates/prompt-blocks/alternate-executor-invocation.md` — read first-hand in full. Distinctions: the single `codex exec` line at `:23` carries `"$(cat "<BRIEFING_FILE>")" < /dev/null > "<RAW_JSONL>" 2>&1`; `:29` explains the file-plus-substitution delivery by the shell-quoting hazard; `:42` names `--publish-jsonl`, the stdin rule "which is exactly what the line's own `< /dev/null` already guards against", and the read-scope audit taken from "the published `.jsonl` stream".
- `templates/prompt-blocks/registry.txt:56`–`:57` — read first-hand: `contain alternate-executor-invocation.md skills/run/SKILL.md` and `contain host-dispatch.md skills/run/SKILL.md`. Distinction: `contain` mode requires every non-empty canonical line verbatim in the consumer, so `skills/run/SKILL.md:223` follows the template and `:184` follows `host-dispatch.md`.
- `skills/run/SKILL.md` — read first-hand at `:184`, `:188`, `:200`–`:229`. Distinction: `:223` is the only line starting `codex exec `; `:184` is the `claude -p` line from `host-dispatch.md`.
- `templates/prompt-blocks/host-dispatch.md` — read first-hand in full. Distinctions: `:13`'s `claude -p` line keeps a `< /dev/null > "<RAW_JSONL>" 2>&1` tail and runs on the Codex CLI host; `:15` publishes through plain `--alloc` (two raws) and `--publish-jsonl`, which is why plain `--alloc` and the jsonl-present publish path must not change; `:15` also calls the alternate-executor recipe's `"$(cat "<BRIEFING_FILE>")"` "the identical pattern", a cross-reference this task makes stale and does not edit.
- `bin/codex-capture.sh` — read first-hand in full. Distinctions: the header's first-token claim (`:14`–`:20`); the usage lines (`:76`–`:77`); `--publish-jsonl` is mandatory at `:167`–`:168`; the placement precondition (`:219`–`:251`), the `-o` non-empty check (`:258`–`:260`), the JSONL structural check (`:263`–`:268`), the non-regular-canonical refusal (`:272`–`:279`), the atomic `mv` and post-verify (`:280`–`:288`); exit 2/3/4 contract (`:93`–`:106`); no sweep of stale raws (DP-d, `:57`–`:62`).
- `tests/codex-skeleton-hygiene/run.sh` and its three `fixtures/agentmd-block-*.txt` — read first-hand at `:1`–`:260`, `:650`–`:1345`. Distinctions: `agentmd-bare-codex-present` pins 3 / 1 bare `codex exec ` lines and the `--publish … --publish-jsonl "<RAW_JSONL>"` full-sentence forms (`:660`–`:701`); `check_fence_structure` expects first tokens `codex-capture.sh`/`codex`/`cat`/`rm` and forbids `$(` (`:831`–`:931`); the `connector` mutation anchors on `-o "<RAW_OUT>"` (`:1008`–`:1019`); `tailconnector` appends after the block's last line and its comment names the redirection (`:1020`–`:1037`); the three frozen fixtures are compared trimmed and byte-for-byte (`:1216`–`:1294`). The suite does **not** reference `alternate-executor-invocation.md` today.
- `tests/check-invocation-path/run.sh:166`–`:194` and `bin/check-invocation-path.sh:402` — read first-hand. Distinction: `code-reviewer` is admitted `wrapper-hosted` through the anchor `^[[:space:]]*codex exec ` in its agent file, so that line must keep starting with `codex exec `.
- `bin/retro-inputs.sh` — read first-hand at `:1`–`:63`, `:400`–`:468`. Distinction: `review-artifacts` counts **any regular file** (empty suffix at `:463`; rationale at `:47`–`:56`), so a reviews dir holding only `.txt` captures reports `read`, not `empty`.
- `docs/distribution.md` `:74`–`:102` and `docs/distribution.ja.md` `:74`–`:100` — read first-hand. Distinctions: both sections carry the first-token claim (EN `:81`, `:100`; JA `:98`); the EN `What is and is not verifiable here` paragraph marks the runtime effect `environmentally-unverified` (`:102`); the JSON snippet (`"codex *"`, `"Bash(codex *)"`) is the recommended settings form; each file has 8 `^## ` headings.
- `.shell-team/specs/T-1148-readme-two-hosts.md` — read first-hand at **AC4** (`:106`–`:107`). Distinction: both READMEs must carry the fixed string `docs/distribution.md#sandbox-enabled-permission-settings`, so the English heading must not be renamed.
- `README.md:49`, `README.ja.md:49`, `docs/adopting.md` (searched for `excludedCommands`, `codex exec`, `.jsonl`, first-token wording; read at `:480`–`:494`, `:655`–`:678`, `:1227`–`:1291`), `docs/workflow.md:177`, `docs/workflow.ja.md:174`, `docs/loop-engineering/task-envelope.md:80`–`:89`, `templates/adapters/codex-cli.txt:1`–`:20` — read first-hand for the shipped-docs inventory. Dispositions are in `## Shipped-docs inventory`.
- `.shell-team/test-recipe.md` — read first-hand at the `## Appended by tasks` heading (`:85`). Distinction: append-only, one entry per task id.
- `.shell-team/specs/T-1151-adopter-docs-checker.md` and `.shell-team/specs/T-1150-codex-wait-and-docs-parity.md` — read first-hand as format models (declaration region, `- check:` idiom, scope lock, pre-commitment, pinned-base-commit form).

## Goal

<!-- BEGIN intent-block: T-1152 -->

- user-visible: yes — adopters run these `codex exec` blocks and `bin/codex-capture.sh` on their own hosts; after this task the cross-provider review runs outside a sandbox-enabled Claude Code 2.1.278+ session under the shipped `"codex *"` exclusion instead of failing or publishing an empty verdict, and the adopter-facing sandbox settings page states the new matching rule.
- verification-class: mechanism — the diff reaches `bin/codex-capture.sh`, `tests/codex-skeleton-hygiene/`, `templates/prompt-blocks/alternate-executor-invocation.md`, `skills/run/SKILL.md` and the executed skeleton blocks in `agents/code-reviewer.md` and `agents/drift-evaluator.md`, and changes a shipped helper's argument contract.
- verification-ceiling: unit-and-static — AC1–AC17, AC19 and AC20 are each settled by a fixed-string or anchored grep, an awk block or section extraction, a synthetic fixture under a temp root run through the shipped helper, a comparison against the branch point's committed blob read with `git show`, a shipped suite's or checker's exit code, or a `git diff` / `git ls-files` read — all in a plain checkout with no sandbox, no network and no Codex CLI. AC18 — the call actually running outside the sandbox — sits above the ceiling and names its owner.
- base-ref-discriminator: not-applicable — this branch was cut from `develop` at `30d71fcb` and no predecessor pull request is open, so there is no stack to discriminate. The criteria that read a base-side blob (AC6, AC14, AC15, AC16, AC17, AC19) name that commit directly through `git rev-parse --verify 30d71fcb^{commit}`, spelled byte-identically in each.
- shipped-docs: docs/distribution.md — this-task
- shipped-docs: docs/distribution.ja.md — this-task
- shipped-docs: agents/code-reviewer.md — this-task
- shipped-docs: agents/drift-evaluator.md — this-task
- shipped-docs: templates/prompt-blocks/alternate-executor-invocation.md — this-task
- shipped-docs: skills/run/SKILL.md — this-task
- shipped-docs: .shell-team/test-recipe.md — this-task
- shipped-docs: templates/prompt-blocks/host-dispatch.md — issue #590
- shipped-docs: docs/workflow.md — issue #590
- shipped-docs: docs/workflow.ja.md — issue #590
- shipped-docs: docs/loop-engineering/task-envelope.md — issue #590
- shipped-docs: templates/adapters/codex-cli.txt — issue #590

**Goal (one sentence).** Every shipped `codex exec` block that runs on a Claude Code host — the three in `agents/code-reviewer.md`, the one in `agents/drift-evaluator.md`, and the alternate-executor recipe line in `templates/prompt-blocks/alternate-executor-invocation.md` with its prompt-block copy in `skills/run/SKILL.md` — becomes one bare command whose first token is `codex` and which carries no redirection, stdin redirect, command substitution, connector or variable assignment, and whose result is read from `-o "<RAW_OUT>"` alone, so that Claude Code 2.1.278 and later (which exempt a command only when every part matches) run it outside the sandbox under the shipped `"codex *"` exclusion; `bin/codex-capture.sh` gains `--alloc --out-only` and accepts `--publish` without `--publish-jsonl`, while plain `--alloc` and every `--publish` call that does pass `--publish-jsonl` behave byte-for-byte as they did at `30d71fcb`; the two agents' prose states the default diagnosis — a `<sandbox_violations>` block, `sandbox_apply`, `workspace routing discovery failed`, `turn.failed`, an empty `-o`, an inability sentence such as `Unable to determine.`, or a non-zero `codex` exit means the call ran inside the sandbox or failed, the invocation shape is wrong, no host is ever added to `allowed_domains`, and nothing is published as a verdict; the hygiene suite follows the new block shape; and `docs/distribution.md` and `docs/distribution.ja.md` restate the matching rule with the 2.1.278 boundary and this task's dated, versioned measurements.

**Recorded deviation from issue #586's draft acceptance.** The issue's draft AC 2 asks `codex-capture.sh --publish` to refuse a capture whose event stream shows `sandbox_apply`. Once no shell captures the stream to a file, the script has no stream to read. Detection therefore moves to agent prose, which reads the Bash tool result that carries the whole `--json` stream (orchestrator ruling 1). Ground: the stream is no longer a file.

## Non-goals

- **No token scan in `bin/codex-capture.sh`, on either path.** With `--publish-jsonl` omitted, `--publish` still checks only the `-o` raw's placement, existence and non-emptiness. With `--publish-jsonl` supplied, it stays byte-equivalent to `30d71fcb` — a JSONL carrying `sandbox_apply` or `turn.failed` publishes exactly as it did. Inability-sentence detection is agent prose, not script logic. (**AC5**, **AC6**)
- **No change to plain `--alloc`**, and no automatic sweep of stale raws. (**AC4**, **AC5**)
- **No change to `templates/prompt-blocks/host-dispatch.md` or its `claude -p` copy in `skills/run/SKILL.md`.** Two grounds: the hygiene suite does not pin that line, and it runs on the Codex CLI host, outside Claude Code's `excludedCommands`, publishing a `.out` derived from its `.jsonl`. Nothing on the Codex CLI host's `spawn_agent` path changes. (**AC15**)
- **No change to the review verdict grammar or the review-input record grammar** (`bin/check-review-input.sh`), **and no change to `bin/resolve-executor.sh` or `templates/binding-default.conf`.** (**AC15**)
- **No new checker, `bin/` script, status flag, board field or telemetry key.** (**AC19**)
- **No change to the recommended settings snippet** — `"codex *"` in `sandbox.excludedCommands` and `"Bash(codex *)"` in `permissions.allow` stay as they are. (**AC16**)
- **No rename of `## Sandbox-enabled permission settings`** in `docs/distribution.md` or of its Japanese counterpart heading. Merged `T-1148` **AC4** pins that anchor from both READMEs. (**AC16**)
- **No edit to `README.md`, `README.ja.md`, `docs/adopting.md`, `docs/adopting.ja.md`, the lessons corpus, any generated playbook block, or any spec other than this one**, and no repair, widening or annotation of any merged criterion this diff reddens. (**AC19**)
- **No version bump, CHANGELOG entry or release step in this task.** (**AC19**)
- **No measurement of what the Bash tool does with a call that outlives its 600000 ms ceiling.** The prose treats a call that returns without a written `-o` as a failed pass. (info-only)
- **No per-task full-population two-arm sweep.** It is deferred to the v2.7.4 release sweep under the operator's standing mode-A2 ruling and disclosed in `## Blast radius`. (info-only)

## Acceptance criteria

Every `check:` runs from the repository root under `bash`, reads the post-implementation tree, guards every `mktemp` with an immediate exit, writes only under `$TMPDIR`, and carries no literal backtick (a fence or code-span delimiter is built at run time with `printf '\140'`).

- [ ] **AC1** The four `codex` marker blocks in the two agent files are single bare commands. This is a **never-dropped** component. `agents/code-reviewer.md` still carries exactly 3 `# T-107-step: codex` markers and 3 lines matching `^[[:space:]]*codex exec `; `agents/drift-evaluator.md` carries 1 and 1. Each block, extracted trimmed from its marker to its closing fence, starts with `codex exec --sandbox read-only --cd <repo> `, carries `-o "<RAW_OUT>"`, ends its last line with a closing double quote, and contains none of `RAW_JSONL`, `2>&1`, `/dev/null`, `&&`, `||`. The primary block is exactly the one line `codex exec --sandbox read-only --cd <repo> review --base <base> --json -o "<RAW_OUT>"`. Multi-line prompt layout with backslash continuation may stay. Positive control: each extraction is asserted to succeed and be non-empty before it is judged, so a missing marker cannot pass as "no forbidden token".
  - check: rc=0; export LC_ALL=C; F3=$(printf '\140\140\140'); CR=agents/code-reviewer.md; DE=agents/drift-evaluator.md; test -s "$CR" || exit 1; test -s "$DE" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac1.XXXXXX") || exit 1; test "$(grep -c '^[[:space:]]*# T-107-step: codex$' "$CR" || true)" = 3 || rc=1; test "$(grep -c '^[[:space:]]*# T-107-step: codex$' "$DE" || true)" = 1 || rc=1; test "$(grep -cE '^[[:space:]]*codex exec ' "$CR" || true)" = 3 || rc=1; test "$(grep -cE '^[[:space:]]*codex exec ' "$DE" || true)" = 1 || rc=1; ext() { awk -v want="$2" -v f3="$F3" '{t=$0; sub(/^[ \t]+/,"",t); sub(/[ \t]+$/,"",t)} s==0 && t=="# T-107-step: codex" {n++; if (n==want) s=1; next} s==1 && substr(t,1,3)==f3 {s=2; exit} s==1 && t!="" {print t} END {exit (s==2) ? 0 : 1}' "$1"; }; for pair in "$CR 1" "$CR 2" "$CR 3" "$DE 1"; do set -- $pair; ext "$1" "$2" > "$T/b" || rc=1; test -s "$T/b" || rc=1; head -n 1 "$T/b" | grep -q '^codex exec --sandbox read-only --cd <repo> ' || rc=1; grep -qF -- '-o "<RAW_OUT>"' "$T/b" || rc=1; tail -n 1 "$T/b" | grep -q '"$' || rc=1; for bad in 'RAW_JSONL' '2>&1' '/dev/null' '&&' '||'; do grep -qF -- "$bad" "$T/b" && rc=1; done; done; ext "$CR" 1 > "$T/p" || rc=1; test "$(cat "$T/p")" = 'codex exec --sandbox read-only --cd <repo> review --base <base> --json -o "<RAW_OUT>"' || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task adds or removes a `codex` caller site in either agent file, at which point the declared 3 / 1 no longer describes the population this criterion counts.

- [ ] **AC2** The agents' `alloc`, `diagnose`, `cleanup` and `publish` blocks follow the single-raw shape. This is a **never-dropped** component. Neither agent file carries `RAW_JSONL`, `--publish-jsonl` or the two-raw form `--alloc --stem` anywhere. After trimming, `agents/code-reviewer.md` carries, for each of `T-XXX-codex-primary`, `T-XXX-codex-adversarial` and `T-XXX-codex-specreview`, the whole lines `codex-capture.sh --alloc --out-only --stem <stem>` and `codex-capture.sh --publish --stem <stem> --publish-out "<RAW_OUT>"`; `agents/drift-evaluator.md` carries the same two lines for `T-XXX-drift-codex`. The cleanup line `rm -f "<RAW_OUT>"` occurs 3 / 1 times, and 3 / 1 trimmed lines begin `cat "<RAW_OUT>"` — the `diagnose` block now reads the `-o` capture. Positive control: the per-stem lines are whole-line matches, so a line that merely contains the stem cannot satisfy them.
  - check: rc=0; export LC_ALL=C; CR=agents/code-reviewer.md; DE=agents/drift-evaluator.md; test -s "$CR" || exit 1; test -s "$DE" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac2.XXXXXX") || exit 1; for f in "$CR" "$DE"; do for bad in 'RAW_JSONL' '--publish-jsonl' '--alloc --stem'; do test "$(grep -cF -- "$bad" "$f" || true)" = 0 || rc=1; done; done; sed 's/^[[:space:]]*//; s/[[:space:]]*$//' "$CR" > "$T/cr"; sed 's/^[[:space:]]*//; s/[[:space:]]*$//' "$DE" > "$T/de"; for s in T-XXX-codex-primary T-XXX-codex-adversarial T-XXX-codex-specreview; do grep -qxF -- "codex-capture.sh --alloc --out-only --stem $s" "$T/cr" || rc=1; grep -qxF -- "codex-capture.sh --publish --stem $s --publish-out \"<RAW_OUT>\"" "$T/cr" || rc=1; done; grep -qxF -- 'codex-capture.sh --alloc --out-only --stem T-XXX-drift-codex' "$T/de" || rc=1; grep -qxF -- 'codex-capture.sh --publish --stem T-XXX-drift-codex --publish-out "<RAW_OUT>"' "$T/de" || rc=1; test "$(grep -cxF -- 'rm -f "<RAW_OUT>"' "$T/cr" || true)" = 3 || rc=1; test "$(grep -cxF -- 'rm -f "<RAW_OUT>"' "$T/de" || true)" = 1 || rc=1; test "$(grep -c '^cat "<RAW_OUT>"' "$T/cr" || true)" = 3 || rc=1; test "$(grep -c '^cat "<RAW_OUT>"' "$T/de" || true)" = 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task adds or removes a `codex` caller site in either agent file, at which point the declared 3 / 1 no longer describes the population this criterion counts.

- [ ] **AC3** The alternate-executor recipe line is one bare command with a literal-path prompt, and its `skills/run/SKILL.md` copy matches it. This is a **never-dropped** component. `templates/prompt-blocks/alternate-executor-invocation.md` and `skills/run/SKILL.md` each carry exactly one line starting `codex exec `, and the two lines are byte-identical. That line starts `codex exec --sandbox read-only --cd <REPO> --ignore-user-config --ignore-rules --ephemeral --json -o "<RAW_OUT>" "`, names `<BRIEFING_FILE>` inside its prompt string, ends with a closing double quote, carries exactly four double-quote characters (the `-o` value and one prompt argument — no inner quote, nothing after), and contains none of `$(`, a backtick, `/dev/null`, `2>&1`, `RAW_JSONL`, `&&`, `||`. The template file as a whole carries no `RAW_JSONL`, no `/dev/null`, no `--publish-jsonl`, and no `"$(cat "<BRIEFING_FILE>")"`; it carries `--alloc --out-only --stem T-<task-id>-tech-lead-run`, `2.1.278` and `command substitution` (the prose names the measured pair: a substitution fails, a literal-path prompt works). `bash bin/check-prompt-sync.sh` exits `0`. Positive control: the extracted line is asserted non-empty before it is judged.
  - check: rc=0; export LC_ALL=C; F=templates/prompt-blocks/alternate-executor-invocation.md; S=skills/run/SKILL.md; test -s "$F" || exit 1; test -s "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac3.XXXXXX") || exit 1; test "$(grep -c '^codex exec ' "$F" || true)" = 1 || rc=1; test "$(grep -c '^codex exec ' "$S" || true)" = 1 || rc=1; grep '^codex exec ' "$F" > "$T/l"; grep '^codex exec ' "$S" > "$T/s"; test -s "$T/l" || rc=1; cmp -s "$T/l" "$T/s" || rc=1; grep -q '^codex exec --sandbox read-only --cd <REPO> --ignore-user-config --ignore-rules --ephemeral --json -o "<RAW_OUT>" "' "$T/l" || rc=1; grep -qF -- '<BRIEFING_FILE>' "$T/l" || rc=1; grep -q '"$' "$T/l" || rc=1; test "$(tr -cd '"' < "$T/l" | wc -c | tr -d ' ')" = 4 || rc=1; BT=$(printf '\140'); for bad in '$(' "$BT" '/dev/null' '2>&1' 'RAW_JSONL' '&&' '||'; do grep -qF -- "$bad" "$T/l" && rc=1; done; for bad in 'RAW_JSONL' '/dev/null' '--publish-jsonl' '"$(cat "<BRIEFING_FILE>")"'; do test "$(grep -cF -- "$bad" "$F" || true)" = 0 || rc=1; done; for s in '--alloc --out-only --stem T-<task-id>-tech-lead-run' '2.1.278' 'command substitution'; do grep -qF -- "$s" "$F" || rc=1; done; bash bin/check-prompt-sync.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** `bin/codex-capture.sh --alloc --out-only` allocates exactly one raw, and plain `--alloc` still allocates two. This is a **never-dropped** component. Against a fresh reviews dir, `--alloc --out-only --stem t1152a` exits `0`, prints exactly one stdout line and nothing on stderr; that path is a regular file directly under the reviews dir whose basename starts `.codex-capture.t1152a.out.`; no `.codex-capture.t1152a.jsonl.*` file exists; the dir holds exactly one file. Plain `--alloc --stem t1152b` still exits `0` and prints two lines, the second naming a `.codex-capture.t1152b.jsonl.` raw. `--out-only` given with `--publish` is refused with exit `2`. Positive control: the reviews dir is canonicalized with `pwd -P` first, so the parent-directory equality cannot fail on a symlinked `$TMPDIR`.
  - check: rc=0; export LC_ALL=C; C=bin/codex-capture.sh; test -s "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac4.XXXXXX") || exit 1; mkdir -p "$T/rv" || exit 1; R=$(cd "$T/rv" && pwd -P) || exit 1; bash "$C" --alloc --out-only --stem t1152a --reviews-dir "$R" > "$T/o" 2> "$T/e"; r=$?; test "$r" -eq 0 || rc=1; test "$(grep -c . "$T/o" || true)" = 1 || rc=1; test ! -s "$T/e" || rc=1; p=$(head -n 1 "$T/o"); test -f "$p" || rc=1; test "$(dirname -- "$p")" = "$R" || rc=1; case "$(basename -- "$p")" in .codex-capture.t1152a.out.*) : ;; *) rc=1 ;; esac; test "$(find "$R" -name '.codex-capture.t1152a.jsonl.*' | grep -c . || true)" = 0 || rc=1; test "$(find "$R" -type f | grep -c . || true)" = 1 || rc=1; bash "$C" --alloc --stem t1152b --reviews-dir "$R" > "$T/o2" 2> "$T/e2"; r=$?; test "$r" -eq 0 || rc=1; test "$(grep -c . "$T/o2" || true)" = 2 || rc=1; sed -n 2p "$T/o2" | grep -qF -- '.codex-capture.t1152b.jsonl.' || rc=1; bash "$C" --publish --out-only --stem t1152a --publish-out "$p" --reviews-dir "$R" > /dev/null 2>&1; r=$?; test "$r" -eq 2 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** `--publish` without `--publish-jsonl` publishes the `-o` capture alone, records the absence on exactly one stderr line, and keeps every refusal. This is a **never-dropped** component. On an `--alloc --out-only` raw holding `verdict body`, `--publish --stem t1152p --publish-out <raw>` exits `0`, writes nothing on stdout, writes exactly one stderr line equal to `codex-capture: publish: no event stream captured for t1152p (--publish-jsonl omitted)`, publishes `t1152p.txt` with that content, publishes no `t1152p.jsonl`, and removes the raw. A raw holding only `Unable to determine.` also publishes with exit `0` — the script does not judge content. An empty raw is exit `3`, publishes nothing, removes the raw, and prints no absence notice (the notice sits after the last refusal). A raw outside the reviews dir, and a raw inside it with another stem's prefix, are exit `2` with nothing published. A canonical `.txt` that already exists as a directory is exit `4` and stays a directory. An unrelated `.codex-capture.*` raw left in the dir by the wrong-prefix case is still there at the end (no sweep). Positive control: every raw is allocated through the shipped `--alloc --out-only` and asserted to exist before it is published.
  - check: rc=0; export LC_ALL=C; C=bin/codex-capture.sh; test -s "$C" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac5.XXXXXX") || exit 1; mkdir -p "$T/rv" || exit 1; R=$(cd "$T/rv" && pwd -P) || exit 1; al() { bash "$C" --alloc --out-only --stem "$1" --reviews-dir "$R" 2>/dev/null | head -n 1; }; p=$(al t1152p); test -f "$p" || rc=1; printf 'verdict body\n' > "$p"; bash "$C" --publish --stem t1152p --publish-out "$p" --reviews-dir "$R" > "$T/o" 2> "$T/e"; r=$?; test "$r" -eq 0 || rc=1; test ! -s "$T/o" || rc=1; test "$(grep -c . "$T/e" || true)" = 1 || rc=1; grep -qxF -- 'codex-capture: publish: no event stream captured for t1152p (--publish-jsonl omitted)' "$T/e" || rc=1; test "$(cat "$R/t1152p.txt" 2>/dev/null)" = 'verdict body' || rc=1; test ! -e "$R/t1152p.jsonl" || rc=1; test ! -e "$p" || rc=1; u=$(al t1152u); test -f "$u" || rc=1; printf 'Unable to determine.\n' > "$u"; bash "$C" --publish --stem t1152u --publish-out "$u" --reviews-dir "$R" > /dev/null 2>&1; r=$?; test "$r" -eq 0 || rc=1; test -s "$R/t1152u.txt" || rc=1; q=$(al t1152q); test -f "$q" || rc=1; bash "$C" --publish --stem t1152q --publish-out "$q" --reviews-dir "$R" > "$T/o" 2> "$T/e"; r=$?; test "$r" -eq 3 || rc=1; test ! -e "$R/t1152q.txt" || rc=1; test ! -e "$q" || rc=1; grep -qF -- 'no event stream captured' "$T/e" && rc=1; printf 'x\n' > "$T/foreign.out"; bash "$C" --publish --stem t1152f --publish-out "$T/foreign.out" --reviews-dir "$R" > /dev/null 2>&1; r=$?; test "$r" -eq 2 || rc=1; test ! -e "$R/t1152f.txt" || rc=1; printf 'x\n' > "$R/.codex-capture.other.out.zz"; bash "$C" --publish --stem t1152w --publish-out "$R/.codex-capture.other.out.zz" --reviews-dir "$R" > /dev/null 2>&1; r=$?; test "$r" -eq 2 || rc=1; test ! -e "$R/t1152w.txt" || rc=1; d=$(al t1152d); test -f "$d" || rc=1; printf 'v\n' > "$d"; mkdir "$R/t1152d.txt" || rc=1; bash "$C" --publish --stem t1152d --publish-out "$d" --reviews-dir "$R" > /dev/null 2>&1; r=$?; test "$r" -eq 4 || rc=1; test -d "$R/t1152d.txt" || rc=1; test -f "$R/.codex-capture.other.out.zz" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** Every `--publish` call that passes `--publish-jsonl` behaves byte-for-byte as `bin/codex-capture.sh` did at `30d71fcb`, and still scans no token. This is a **never-dropped** component. The branch point's committed script and the working script are each run over the same five fixtures in the same reviews-dir path: a valid pair (`ok`); an `-o` of `Unable to determine.` with a JSONL carrying a `sandbox_apply` error event and `turn.failed` (`sbx`); an empty `-o` (`emptyout`); a JSONL with no event line (`badjsonl`); and a canonical `.txt` pre-existing as a directory (`canondir`). For every fixture the two runs produce identical exit code, stdout, stderr and resulting reviews-dir tree (path plus `cksum` of every file). Positive controls: the base script is read from the committed blob and asserted non-empty and still carrying `--publish-jsonl`; the base exit codes are asserted to be `0`, `0`, `3`, `3`, `4` in fixture order, so identical-but-broken runs cannot pass — and `sbx` exiting `0` on both is the proof that no token scan was added.
  - check: rc=0; export LC_ALL=C; C=bin/codex-capture.sh; test -s "$C" || exit 1; B=$(git rev-parse --verify 30d71fcb^{commit}) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac6.XXXXXX") || exit 1; git show "$B:bin/codex-capture.sh" > "$T/old.sh" 2>/dev/null || rc=1; test -s "$T/old.sh" || rc=1; grep -qF -- '--publish-jsonl' "$T/old.sh" || rc=1; mkdir -p "$T/rv" || exit 1; R=$(cd "$T/rv" && pwd -P) || exit 1; run() { find "$R" -mindepth 1 -delete; O="$R/.codex-capture.eq.out.fixed"; J="$R/.codex-capture.eq.jsonl.fixed"; case "$2" in ok) printf 'verdict\n' > "$O"; printf '{"type":"turn.completed"}\n' > "$J" ;; sbx) printf 'Unable to determine.\n' > "$O"; printf '{"type":"error","message":"sandbox_apply: Operation not permitted"}\n{"type":"turn.failed"}\n' > "$J" ;; emptyout) : > "$O"; printf '{"type":"x"}\n' > "$J" ;; badjsonl) printf 'v\n' > "$O"; printf 'not json\n' > "$J" ;; canondir) printf 'v\n' > "$O"; printf '{"type":"x"}\n' > "$J"; mkdir "$R/eq.txt" ;; esac; bash "$1" --publish --stem eq --publish-out "$O" --publish-jsonl "$J" --reviews-dir "$R" > "$T/out.$3" 2> "$T/err.$3"; echo "$?" > "$T/rc.$3"; ( cd "$R" && find . -type f | sort | while IFS= read -r f; do printf '%s ' "$f"; cksum < "$f"; done ) > "$T/tree.$3"; }; for pair in 'ok 0' 'sbx 0' 'emptyout 3' 'badjsonl 3' 'canondir 4'; do set -- $pair; c=$1; want=$2; run "$T/old.sh" "$c" old; run "$C" "$c" new; test "$(cat "$T/rc.old")" = "$want" || rc=1; for k in out err rc tree; do cmp -s "$T/$k.old" "$T/$k.new" || rc=1; done; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** `bin/codex-capture.sh`'s own header states the new contract. It carries no occurrence of `FIRST token`, and it carries `2.1.278`, `sandbox.excludedCommands`, `--out-only` and the optional-flag usage form `[--publish-jsonl`. Positive control: the file is asserted non-empty first.
  - check: rc=0; C=bin/codex-capture.sh; test -s "$C" || exit 1; test "$(grep -cF -- 'FIRST token' "$C" || true)" = 0 || rc=1; for s in '2.1.278' 'sandbox.excludedCommands' '--out-only' '[--publish-jsonl'; do grep -qF -- "$s" "$C" || rc=1; done; test "$rc" -eq 0

- [ ] **AC8** Both agents state the default diagnosis, and the poll fallback no longer waits on a stream file. Each of `agents/code-reviewer.md` and `agents/drift-evaluator.md` carries `<sandbox_violations>`, `sandbox_apply`, `workspace routing discovery failed`, `turn.failed`, `allowed_domains`, `Unable to determine.` and `invocation shape` — the prose reads the Bash tool result, treats any of those, an empty `-o` or a non-zero `codex` exit as a call that ran inside the sandbox or failed, says the invocation shape is wrong and no host is added to `allowed_domains`, and never publishes a verdict from it (`BLOCKED` for `code-reviewer`; the existing tooling-precondition-failure disposition for `drift-evaluator`). `agents/code-reviewer.md` still carries `600000` and `<RAW_OUT>`, and no longer carries the two old poll phrases `capture stream's own` and `foreground poll on` followed by a code-spanned `turn.completed`. Positive control: both files are asserted non-empty first, and the old phrases are built with the same code-span delimiter the file uses.
  - check: rc=0; export LC_ALL=C; CR=agents/code-reviewer.md; DE=agents/drift-evaluator.md; test -s "$CR" || exit 1; test -s "$DE" || exit 1; BT=$(printf '\140'); for f in "$CR" "$DE"; do for s in '<sandbox_violations>' 'sandbox_apply' 'workspace routing discovery failed' 'turn.failed' 'allowed_domains' 'Unable to determine.' 'invocation shape'; do grep -qF -- "$s" "$f" || rc=1; done; done; grep -qF -- 'BLOCKED' "$CR" || rc=1; grep -qF -- '600000' "$CR" || rc=1; grep -qF -- '<RAW_OUT>' "$CR" || rc=1; test "$(grep -cF -- "capture stream's own" "$CR" || true)" = 0 || rc=1; test "$(grep -cF -- "foreground poll on ${BT}turn.completed${BT}" "$CR" || true)" = 0 || rc=1; test "$rc" -eq 0

- [ ] **AC9** The sentences that would now be false on the Claude Code host are gone. `agents/code-reviewer.md` no longer tells the role to save `T-XXX-codex-primary.jsonl`, `agents/drift-evaluator.md` no longer tells it to save `T-XXX-drift-codex.jsonl`, and neither file says `first-token invocation redirecting`. The `{txt,jsonl}` canonical-stem wording in `agents/code-reviewer.md`'s write boundary and file-naming rule is **not** in scope — the Codex CLI host's `claude -p` path still publishes a `.jsonl` under those stems. Positive control: `T-XXX-codex-primary.txt` and `T-XXX-drift-codex.txt` are still named, so a deleted paragraph cannot satisfy the absence.
  - check: rc=0; CR=agents/code-reviewer.md; DE=agents/drift-evaluator.md; test -s "$CR" || exit 1; test -s "$DE" || exit 1; test "$(grep -cF -- 'T-XXX-codex-primary.jsonl' "$CR" || true)" = 0 || rc=1; test "$(grep -cF -- 'T-XXX-drift-codex.jsonl' "$DE" || true)" = 0 || rc=1; for f in "$CR" "$DE"; do test "$(grep -cF -- 'first-token invocation redirecting' "$f" || true)" = 0 || rc=1; done; grep -qF -- 'T-XXX-codex-primary.txt' "$CR" || rc=1; grep -qF -- 'T-XXX-drift-codex.txt' "$DE" || rc=1; test "$rc" -eq 0

- [ ] **AC10** `tests/codex-skeleton-hygiene/run.sh` passes against the new block shape, counterfactuals included. This is a **never-dropped** component. The suite exits `0`, prints no `FAIL` line on either stream, and prints the `PASS:` lines of `agentmd-bare-codex-present`, `agentmd-fence-structure`, `agentmd-fence-mutation`, `agentmd-block-verbatim` and `agentmd-block-verbatim-mutation`. Each of the three frozen fixtures `fixtures/agentmd-block-code-reviewer-adversarial.txt`, `fixtures/agentmd-block-code-reviewer-specreview.txt` and `fixtures/agentmd-block-drift-evaluator.txt` is non-empty, carries no `RAW_JSONL`, and ends its last line with a closing double quote. Positive control: the suite's stdout is asserted non-empty before its content is judged.
  - check: rc=0; S=tests/codex-skeleton-hygiene/run.sh; test -s "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac10.XXXXXX") || exit 1; bash "$S" > "$T/out" 2> "$T/err"; r=$?; test "$r" -eq 0 || rc=1; test -s "$T/out" || rc=1; test "$(cat "$T/out" "$T/err" | grep -c '^FAIL' || true)" = 0 || rc=1; for s in 'PASS: agentmd-bare-codex-present' 'PASS: agentmd-fence-structure' 'PASS: agentmd-fence-mutation' 'PASS: agentmd-block-verbatim ' 'PASS: agentmd-block-verbatim-mutation'; do grep -qF -- "$s" "$T/out" || rc=1; done; for f in tests/codex-skeleton-hygiene/fixtures/agentmd-block-code-reviewer-adversarial.txt tests/codex-skeleton-hygiene/fixtures/agentmd-block-code-reviewer-specreview.txt tests/codex-skeleton-hygiene/fixtures/agentmd-block-drift-evaluator.txt; do test -s "$f" || rc=1; test "$(grep -c -- 'RAW_JSONL' "$f" || true)" = 0 || rc=1; tail -n 1 "$f" | grep -q '"$' || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** The hygiene suite carries a new case asserting that no shipped `codex exec` block carries a shell operator after its arguments, with its own counterfactual. **Droppable 1st** (see `## Pre-commitment`). The live case, labelled `agentmd-no-trailing-operator`, covers the four agent `codex` blocks and the alternate-executor recipe line in `templates/prompt-blocks/alternate-executor-invocation.md`; its mutation case, labelled `agentmd-no-trailing-operator-mutation`, appends a redirection tail to each covered site in a scratch copy and shows each one caught. Each label prints as `PASS: <label> — …`. Positive control: the suite source is asserted to name `alternate-executor-invocation.md` and to carry the label at least twice (definition plus use), so an output line printed without a real case cannot satisfy it alone.
  - check: rc=0; S=tests/codex-skeleton-hygiene/run.sh; test -s "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac11.XXXXXX") || exit 1; grep -qF -- 'alternate-executor-invocation.md' "$S" || rc=1; test "$(grep -cF -- 'agentmd-no-trailing-operator' "$S" || true)" -ge 2 || rc=1; bash "$S" > "$T/out" 2> "$T/err"; r=$?; test "$r" -eq 0 || rc=1; grep -qF -- 'PASS: agentmd-no-trailing-operator ' "$T/out" || rc=1; grep -qF -- 'PASS: agentmd-no-trailing-operator-mutation ' "$T/out" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** The alternate-executor recipe's read-scope audit names its new source, or discloses that none exists. **Droppable 2nd** (see `## Pre-commitment`). `templates/prompt-blocks/alternate-executor-invocation.md` still carries `command_execution`, no longer says the items come from the published code-spanned `.jsonl` stream, and carries **exactly one** of the two fixed phrases `harness output file` (the audit is transcribed from the background Bash slice's harness output file) or `after-the-fact audit is unavailable` (the disclosure used if the engineer measures that the harness output file does not keep the stream). Positive control: `command_execution` is asserted present, so a deleted paragraph cannot pass.
  - check: rc=0; F=templates/prompt-blocks/alternate-executor-invocation.md; test -s "$F" || exit 1; BT=$(printf '\140'); grep -qF -- 'command_execution' "$F" || rc=1; test "$(grep -cF -- "the published ${BT}.jsonl${BT} stream" "$F" || true)" = 0 || rc=1; n=0; grep -qF -- 'harness output file' "$F" && n=$((n + 1)); grep -qF -- 'after-the-fact audit is unavailable' "$F" && n=$((n + 1)); test "$n" -eq 1 || rc=1; test "$rc" -eq 0

- [ ] **AC13** The other suites and gates that read the changed files stay green, and `code-reviewer` stays admitted `wrapper-hosted`. `tests/check-invocation-path/run.sh`, `tests/codex-agents/run.sh`, `tests/gitignore-raw-dumps/run.sh`, `tests/retro-inputs/run.sh` and `tests/check-prompt-sync/run.sh` each exit `0`; `bash bin/check-prompt-sync.sh` exits `0`; `shellcheck bin/codex-capture.sh tests/codex-skeleton-hygiene/run.sh` exits `0`; and `agents/code-reviewer.md` still carries 3 lines matching `^[[:space:]]*codex exec ` — the anchor `bin/check-invocation-path.sh` reads. Positive control: each suite file is asserted non-empty and `shellcheck` asserted resolvable, so a missing file or tool fails rather than skips.
  - check: rc=0; for s in tests/check-invocation-path/run.sh tests/codex-agents/run.sh tests/gitignore-raw-dumps/run.sh tests/retro-inputs/run.sh tests/check-prompt-sync/run.sh; do test -s "$s" || rc=1; bash "$s" > /dev/null 2>&1 || rc=1; done; bash bin/check-prompt-sync.sh > /dev/null 2>&1 || rc=1; command -v shellcheck > /dev/null 2>&1 || rc=1; shellcheck bin/codex-capture.sh tests/codex-skeleton-hygiene/run.sh > /dev/null 2>&1 || rc=1; test "$(grep -cE '^[[:space:]]*codex exec ' agents/code-reviewer.md || true)" = 3 || rc=1; test "$rc" -eq 0
  - stale-at: a task adds or removes a `codex` caller site in `agents/code-reviewer.md`, at which point the declared 3 no longer describes the population this criterion counts.

- [ ] **AC14** A reviews dir holding only `.txt` captures still counts as material for the retro. `bin/retro-inputs.sh` is byte-identical to its blob at `30d71fcb`, and it still registers `review-artifacts` with an empty suffix and still carries the rule that that input counts any regular file. Positive control: the base blob is asserted non-empty before it is compared.
  - check: rc=0; F=bin/retro-inputs.sh; test -s "$F" || exit 1; B=$(git rev-parse --verify 30d71fcb^{commit}) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac14.XXXXXX") || exit 1; git show "$B:$F" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; cmp -s "$T/base" "$F" || rc=1; grep -qF -- 'report_dir_input review-artifacts "${TEAM_REVIEWS_DIR:-}"    ""' "$F" || rc=1; grep -qF -- 'counts any regular file' "$F" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC15** What this task must not touch is byte-identical to `30d71fcb`. `templates/prompt-blocks/host-dispatch.md`, `bin/resolve-executor.sh`, `templates/binding-default.conf` and `bin/check-review-input.sh` each equal their blob at that commit. `host-dispatch.md`'s single `claude -p ` line still carries its `> "<RAW_JSONL>" 2>&1` tail and still appears verbatim as a whole line in `skills/run/SKILL.md`. Positive control: every base blob is asserted non-empty before comparison, and the extracted `claude -p` line is asserted to be exactly one line.
  - check: rc=0; B=$(git rev-parse --verify 30d71fcb^{commit}) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac15.XXXXXX") || exit 1; for f in templates/prompt-blocks/host-dispatch.md bin/resolve-executor.sh templates/binding-default.conf bin/check-review-input.sh; do test -s "$f" || rc=1; git show "$B:$f" > "$T/b" 2>/dev/null || rc=1; test -s "$T/b" || rc=1; cmp -s "$T/b" "$f" || rc=1; done; grep '^claude -p ' templates/prompt-blocks/host-dispatch.md > "$T/cl"; test "$(grep -c . "$T/cl" || true)" = 1 || rc=1; grep -qF -- '> "<RAW_JSONL>" 2>&1' "$T/cl" || rc=1; grep -qxF -f "$T/cl" skills/run/SKILL.md || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC16** The adopter-facing sandbox settings page states the new matching rule in both languages. This is a **never-dropped** component. `docs/distribution.md` carries the heading `## Sandbox-enabled permission settings` exactly once and `docs/distribution.ja.md` carries `## サンドボックス有効時の permission 設定` exactly once, and each file's `^## ` count equals its count at `30d71fcb`. The English section carries `2.1.278`, `every part`, `2.1.281`, `0.156.1`, `2026-09-24`, `"codex *"`, `"Bash(codex *)"` and `sandbox.excludedCommands`, and no longer carries `environmentally-unverified`. The Japanese section carries `2.1.278`, `2.1.281`, `0.156.1`, `2026-09-24`, `"codex *"`, `"Bash(codex *)"` and `sandbox.excludedCommands`. The whole English file no longer carries `matches on a command line's first token`, and the whole Japanese file no longer carries `コマンドラインの先頭トークンに一致する`. Positive controls: both base blobs are asserted to carry those two old phrases (so the absence is attributable to this edit and the grep reads), both section extractions are asserted non-empty, and the base heading counts are asserted non-zero.
  - adopter-surface: `docs/distribution.md` `## Sandbox-enabled permission settings` and `docs/distribution.ja.md` `## サンドボックス有効時の permission 設定`, rewritten inside the existing sections with no new heading.
  - check: rc=0; export LC_ALL=C; E=docs/distribution.md; J=docs/distribution.ja.md; test -s "$E" || exit 1; test -s "$J" || exit 1; B=$(git rev-parse --verify 30d71fcb^{commit}) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac16.XXXXXX") || exit 1; HE='## Sandbox-enabled permission settings'; HJ='## サンドボックス有効時の permission 設定'; test "$(grep -cxF -- "$HE" "$E" || true)" = 1 || rc=1; test "$(grep -cxF -- "$HJ" "$J" || true)" = 1 || rc=1; git show "$B:$E" > "$T/be" 2>/dev/null || rc=1; git show "$B:$J" > "$T/bj" 2>/dev/null || rc=1; for pair in "$T/be $E" "$T/bj $J"; do set -- $pair; nb=$(grep -c '^## ' "$1" || true); nn=$(grep -c '^## ' "$2" || true); test "${nb:-0}" -gt 0 || rc=1; test "$nb" = "$nn" || rc=1; done; grep -qF -- "matches on a command line's first token" "$T/be" || rc=1; grep -qF -- 'コマンドラインの先頭トークンに一致する' "$T/bj" || rc=1; sec() { awk -v h="$2" '$0==h{f=1;next} f && /^## /{exit} f{print}' "$1"; }; sec "$E" "$HE" > "$T/es"; sec "$J" "$HJ" > "$T/js"; test -s "$T/es" || rc=1; test -s "$T/js" || rc=1; for s in '2.1.278' 'every part' '2.1.281' '0.156.1' '2026-09-24' '"codex *"' '"Bash(codex *)"' 'sandbox.excludedCommands'; do grep -qF -- "$s" "$T/es" || rc=1; done; for s in '2.1.278' '2.1.281' '0.156.1' '2026-09-24' '"codex *"' '"Bash(codex *)"' 'sandbox.excludedCommands'; do grep -qF -- "$s" "$T/js" || rc=1; done; test "$(grep -cF -- 'environmentally-unverified' "$T/es" || true)" = 0 || rc=1; test "$(grep -cF -- "matches on a command line's first token" "$E" || true)" = 0 || rc=1; test "$(grep -cF -- 'コマンドラインの先頭トークンに一致する' "$J" || true)" = 0 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC17** `.shell-team/test-recipe.md` gains a `T-1152` entry under `## Appended by tasks` that names `--out-only` (the new capture shape the suites exercise), and every non-blank line of its blob at `30d71fcb` is still present verbatim — appended to, never rewritten. Positive control: the extracted tail section and the base blob are asserted non-empty first.
  - check: rc=0; F=.shell-team/test-recipe.md; test -s "$F" || exit 1; B=$(git rev-parse --verify 30d71fcb^{commit}) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac17.XXXXXX") || exit 1; awk '/^## Appended by tasks$/{f=1} f' "$F" > "$T/tailsec"; test -s "$T/tailsec" || rc=1; grep -qF -- 'T-1152' "$T/tailsec" || rc=1; grep -qF -- '--out-only' "$T/tailsec" || rc=1; git show "$B:$F" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; while IFS= read -r l; do test -n "$l" || continue; grep -qxF -- "$l" "$F" || rc=1; done < "$T/base"; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC18** On Claude Code 2.1.278 or later, in a sandbox-enabled session with `"codex *"` in `sandbox.excludedCommands` and `allowUnsandboxedCommands: false`, the new block shape runs outside the sandbox: Codex executes its own commands, `-o` is written, and no `<sandbox_violations>`, `sandbox_apply` or `workspace routing discovery failed` appears. The mechanical half checked here is that `.shell-team/provenance/T-1152.md` records that live run with its versions and its contrasting failure: it carries `2.1.281`, `0.156.1`, `codex exec --sandbox read-only` and `workspace routing discovery failed`.
  - above-ceiling: operator (in-session sandbox-enabled live probe, recorded in the spec's Assumptions and in .shell-team/provenance/T-1152.md)
  - check: rc=0; P=.shell-team/provenance/T-1152.md; test -s "$P" || exit 1; for s in '2.1.281' '0.156.1' 'codex exec --sandbox read-only' 'workspace routing discovery failed'; do grep -qF -- "$s" "$P" || rc=1; done; test "$rc" -eq 0

- [ ] **AC19** This task's diff is confined to a fixed allow-list. The union of four reads — the committed range `git diff --no-renames --name-only <base>...HEAD` from `30d71fcb`, the staged delta, the unstaged delta, and the untracked strays from `git ls-files --others --exclude-standard` — contains no path outside `agents/code-reviewer.md`, `agents/drift-evaluator.md`, `bin/codex-capture.sh`, `docs/distribution.md`, `docs/distribution.ja.md`, `skills/run/SKILL.md`, `templates/prompt-blocks/alternate-executor-invocation.md`, anything under `tests/codex-skeleton-hygiene/`, `.shell-team/test-recipe.md`, `.shell-team/todo.md`, and this task's own spec, provenance, review and interventions records. **This criterion is merge-point-scoped and expected to go stale after merge**, once later tasks' files land on the same base; that is expected and is never repaired by widening the base resolution or re-deriving it per rework round. Positive control: the measured union is asserted non-empty.
  - check: rc=0; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac19.XXXXXX") || exit 1; B=$(git rev-parse --verify 30d71fcb^{commit}) || exit 1; test -n "$B" || exit 1; { git diff --no-renames --name-only "$B"...HEAD; git diff --no-renames --cached --name-only; git diff --no-renames --name-only; git ls-files --others --exclude-standard; } > "$T/raw" || rc=1; LC_ALL=C sort -u "$T/raw" > "$T/got"; test -s "$T/got" || rc=1; printf '%s\n' .shell-team/interventions/T-1152.md .shell-team/provenance/T-1152.md .shell-team/reviews/T-1152.md .shell-team/specs/T-1152-codex-sandbox-hotfix.md .shell-team/test-recipe.md .shell-team/todo.md agents/code-reviewer.md agents/drift-evaluator.md bin/codex-capture.sh docs/distribution.ja.md docs/distribution.md skills/run/SKILL.md templates/prompt-blocks/alternate-executor-invocation.md | LC_ALL=C sort -u > "$T/allow"; grep -v '^tests/codex-skeleton-hygiene/' "$T/got" > "$T/rest" || true; comm -23 "$T/rest" "$T/allow" > "$T/extra"; test ! -s "$T/extra" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC20** This spec is a conformant carrier of its own declarations. `bash bin/check-adopter-docs.sh` on this spec exits `0` with zero bytes on both streams; the spec carries exactly one line-initial `- user-visible:`, `- verification-class: mechanism — `, `- verification-ceiling: unit-and-static — ` and `- base-ref-discriminator: not-applicable — `, at least seven line-initial `- shipped-docs:` lines, and no `- shipped-docs:` line still ending in the placeholder `issue #590`. Positive control: the spec and the checker are asserted non-empty first.
  - check: rc=0; C=bin/check-adopter-docs.sh; S=.shell-team/specs/T-1152-codex-sandbox-hotfix.md; test -s "$C" || exit 1; test -s "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1152-ac20.XXXXXX") || exit 1; bash "$C" "$S" > "$T/o" 2> "$T/e"; r=$?; test "$r" -eq 0 || rc=1; test ! -s "$T/o" || rc=1; test ! -s "$T/e" || rc=1; for k in '^- user-visible: ' '^- verification-class: mechanism — ' '^- verification-ceiling: unit-and-static — ' '^- base-ref-discriminator: not-applicable — '; do test "$(grep -c "$k" "$S" || true)" = 1 || rc=1; done; test "$(grep -c '^- shipped-docs: ' "$S" || true)" -ge 7 || rc=1; test "$(grep -c '^- shipped-docs: .*issue #590$' "$S" || true)" = 0 || rc=1; rm -rf "$T"; test "$rc" -eq 0

## Input space

**Reachable input classes** — what real adopter sessions and real Codex runs produce:

1. A Claude Code session with the sandbox enabled, `allowUnsandboxedCommands: false`, `"codex *"` in `sandbox.excludedCommands`, on Claude Code 2.1.278 or later (the broken case this task fixes) or on 2.1.275 or earlier (where the bare form also matches, because its first token is `codex`).
2. A session with the sandbox disabled, or with `allowUnsandboxedCommands: true` (unaffected before and after).
3. A session with the sandbox enabled but no `"codex *"` exclusion: the bare call still runs inside the sandbox and produces one of the failure shapes below, which the agents' prose must turn into `BLOCKED` rather than a verdict.
4. The Codex outcomes a real call returns to the Bash tool: success with `-o` written; exit 1 with no `-o` and `workspace routing discovery failed` → `turn.failed` in the stream (measured on 2.1.281); exit 0 with `-o` = `Unable to determine.` and `sandbox_apply` errors in the stream (measured on 2.1.278); a `<sandbox_violations>` block on the tool result; an unrelated `error` item in an otherwise successful stream (the measured Codex plugin hook-timeout notice).
5. `bin/codex-capture.sh` inputs callers produce: plain `--alloc`; `--alloc --out-only`; `--publish` with and without `--publish-jsonl`; an empty `-o` raw; a raw outside the reviews dir; a raw with another stem's prefix; a canonical `.txt` already present as a directory; a stale `.codex-capture.*` raw left by an interrupted run.
6. The Codex CLI host's `claude -p` review path, which still uses plain `--alloc` and `--publish-jsonl` (protected by AC6 and AC15, not changed).
7. A multi-line prompt kept inside one quoted argument with backslash-newline continuation (measured harmless).

**Out-of-scope synthetic extremes** — declined explicitly:

1. A future Claude Code release that changes `excludedCommands` matching again (for example, treating a backslash-newline or a quoted argument as a separate part). This task records what 2.1.278–2.1.281 measurably do.
2. A Bash tool call that outlives its 600000 ms ceiling: whether the tool backgrounds or kills it is unmeasured. The prose treats a return with no written `-o` as a failed pass and goes no further.
3. A Codex run that exits 0 with a plausible, non-refusal `-o` verdict written without reading any file, and whose stream carries none of the named tokens. Nothing in a single tool result distinguishes it from a real review; the bare shape makes it unreachable by removing the cause.
4. Concurrent `--publish` calls on the same stem, and a raw replaced between `--alloc` and `--publish` by another process.
5. Adversarial content in the `-o` capture (prompt-injection text, control bytes, a verdict block forged by the reviewed diff).
6. Adopter settings files carrying exclusion patterns other than `"codex *"` (for example `codex exec *` or per-flag patterns).

<!-- END intent-block: T-1152 -->

## Body-to-AC correspondence

| # | Directive (where stated) | AC or exemption |
|---|---|---|
| 1 | The four agent `codex` blocks are single bare commands (Goal) | **AC1** |
| 2 | The primary block's exact shape (Goal; ruling 7) | **AC1** |
| 3 | Multi-line prompt layout may stay (Goal; ruling 13) | **AC1** (checks last line only, allows continuation lines) |
| 4 | `alloc --out-only`, single-raw `cleanup`, `publish` without `--publish-jsonl`, `diagnose` reads `-o` (Goal; rulings 2, 3) | **AC2** |
| 5 | Alternate-executor line bare, literal-path prompt, no substitution, no `< /dev/null` (Goal; rulings 8, 12) | **AC3** |
| 6 | SKILL copy follows through the prompt-block sync (Goal; ruling 12) | **AC3** (`check-prompt-sync.sh` exit 0 plus line equality) |
| 7 | `--alloc --out-only` prints one path; plain `--alloc` unchanged (Goal; ruling 2) | **AC4**, **AC6** |
| 8 | `--publish` without `--publish-jsonl` publishes `.txt` alone plus exactly one stderr line (Goal; ruling 3) | **AC5** |
| 9 | jsonl-present path byte-equivalent to `30d71fcb` (Goal; ruling 1) | **AC6** |
| 10 | No token scan in the script on either path (Non-goals; rulings 1, 9) | **AC6** (`sbx` exits 0 on both), **AC5** (`Unable to determine.` publishes) |
| 11 | No automatic sweep of stale raws (Non-goals; ruling 2) | **AC5** (the unrelated raw survives) |
| 12 | Script header states the contract (Goal) | **AC7** |
| 13 | Default diagnosis in agent prose, never add hosts, `BLOCKED` (Goal; rulings 1, 3, issue comment) | **AC8** |
| 14 | Poll target becomes `<RAW_OUT>` (ruling 4) | **AC8** (old poll phrases absent, `<RAW_OUT>` present) |
| 15 | Sentences false on the Claude Code host removed (Goal) | **AC9** |
| 16 | Hygiene suite follows the new shape, fixtures updated (Goal; ruling 10) | **AC10** |
| 17 | New no-trailing-operator mutation (ruling 10, droppable 1st) | **AC11** |
| 18 | L42 audit source changes, or disclosure (ruling 6, droppable 2nd) | **AC12** |
| 19 | `code-reviewer` stays `wrapper-hosted`; related suites green (Routing Map AC6) | **AC13** |
| 20 | `.txt`-only reviews dir counts as material (ruling 3) | **AC14** |
| 21 | `host-dispatch.md` and its SKILL copy unchanged; Codex CLI host path unchanged (Non-goals; ruling 5) | **AC15** |
| 22 | No verdict/review-input grammar change; no binding change (Non-goals) | **AC15** |
| 23 | Settings snippet unchanged (Non-goals) | **AC16** (`"codex *"` and `"Bash(codex *)"` still present) |
| 24 | Heading not renamed (Non-goals) | **AC16** |
| 25 | Docs restate every-part matching, the 2.1.278 boundary, dated and versioned measurements (Goal; ruling 11) | **AC16** |
| 26 | Test recipe entry, append-only (shipped-docs) | **AC17** |
| 27 | The call actually runs outside the sandbox (Goal) | **AC18** (above ceiling) |
| 28 | No new checker/script/flag/field/key; no README/adopting/lessons/other-spec edit; no version bump (Non-goals) | **AC19** |
| 29 | Declarations conformant, placeholder replaced (declaration region) | **AC20** |
| 30 | Recorded deviation from issue #586 draft AC 2 (Goal) | info-only (not promoted to AC) — a statement of provenance, not an artifact state; its substance is AC5/AC6 (no script scan) plus AC8 (prose detection). |
| 31 | No Bash-tool timeout measurement (Non-goals) | info-only (not promoted to AC) — a criterion that a measurement did *not* happen is unfalsifiable; the prose rule it bounds is AC8. |
| 32 | No per-task two-arm sweep (Non-goals) | info-only (not promoted to AC) — disclosed process deviation under mode A2; see `## Blast radius`. |

## Shipped-docs inventory

The population is derived by command, not by eye. This role has no shell; the freeze run executes the command and records its output here.

- reproduce: git grep -l -E 'RAW_JSONL|publish-jsonl|excludedCommands|codex-\*\.\{txt,jsonl\}|JSONL event stream|first-token' -- README.md README.ja.md docs agents skills templates bin/codex-capture.sh

The rows below come from this role's first-hand searches with the same terms. The freeze run adds a row for any path the command prints that is missing here.

| Shipped document | Disposition | Ground |
|---|---|---|
| `docs/distribution.md` | this-task | States the first-token rule and `environmentally-unverified`; rewritten (**AC16**). |
| `docs/distribution.ja.md` | this-task | Same rule in Japanese (`:98`); rewritten (**AC16**). |
| `agents/code-reviewer.md` | this-task | Carries three of the blocks and the stale poll/save prose (**AC1**, **AC2**, **AC8**, **AC9**). |
| `agents/drift-evaluator.md` | this-task | Carries one block and the stale save/redirect prose (**AC1**, **AC2**, **AC8**, **AC9**). |
| `templates/prompt-blocks/alternate-executor-invocation.md` | this-task | Carries the recipe line and the L29/L42 prose (**AC3**, **AC12**). |
| `skills/run/SKILL.md` | this-task | The `contain` copy of the recipe line, updated through sync (**AC3**). |
| `.shell-team/test-recipe.md` | this-task | The new capture shape's run notes (**AC17**). Outside the command's pathspec; listed because this task edits it. |
| `bin/codex-capture.sh` | this-task (script, not a document) | Header comment restates the contract (**AC7**); no `- shipped-docs:` line because it is not a document. |
| `templates/prompt-blocks/host-dispatch.md` | issue #590 | `:15` calls the alternate-executor recipe's `"$(cat "<BRIEFING_FILE>")"` "the identical pattern"; after this task the recipe no longer uses it. The file is a Non-goal (ruling 5) and must stay byte-identical (**AC15**), so the stale cross-reference is deferred. |
| `docs/workflow.md` | issue #590 | `:177` lists raw traces as `T-XXX-codex-*.{txt,jsonl}`; on the Claude Code host only `.txt` is now published (the Codex CLI host still publishes both). Not wrong for every host, imprecise for one. |
| `docs/workflow.ja.md` | issue #590 | `:174`, same as above. |
| `docs/loop-engineering/task-envelope.md` | issue #590 | `:84`–`:85` describes the codex pattern as returning "the JSONL event stream" as a file. |
| `templates/adapters/codex-cli.txt` | issue #590 | `:11`–`:12` comment says results return as files including "a JSONL event stream". A comment in an adapter definition, no declared value changes. |
| `README.md` | unchanged | `:49` points to the sandbox section and names `sandbox.excludedCommands`; both stay true. The anchor is pinned by `T-1148` **AC4**. |
| `README.ja.md` | unchanged | `:49`, same ground. |
| `docs/adopting.md` / `docs/adopting.ja.md` | unchanged | No first-token claim, no `excludedCommands`, no JSONL-raw claim found; the Codex CLI host runbook is not touched by this task. |

One follow-up issue can carry all five `issue #590` rows; the orchestrator files it and replaces the placeholder in the five `- shipped-docs:` lines before the intent hash is recorded (**AC20** refuses the placeholder).

## Pre-commitment

Scope and threshold are the orchestrator's ruling 10. The "stop and return to planning" clause cites the 2026-09-06 T-1121 operator-ratified ruling; the rest is AI self-discipline.

**Never-dropped:** the bare single-command blocks at every in-scope site (**AC1**, **AC2**, **AC3**); `--publish-jsonl` optional plus `--alloc --out-only`, with the jsonl-present path byte-equivalent (**AC4**, **AC5**, **AC6**); the hygiene suite green with updated fixtures (**AC10**); `docs/distribution.md` and `docs/distribution.ja.md` restated with the 2.1.278 boundary (**AC16**). If a never-dropped component is defeated in two consecutive rounds, the task stops and returns to planning instead of taking a third patch round.

**Droppable, in order:**

1. **The new no-trailing-operator hygiene case (AC11).** Disposition: drop the case; the three frozen fixtures and `agentmd-fence-structure` still pin the block shape; file an issue carrying that round's findings as its requirement list.
2. **The L42 audit-source rewrite (AC12).** Disposition: ship the disclosure branch (`after-the-fact audit is unavailable`) instead of the harness-output-file branch. AC12 accepts either branch, so this drop needs no change to the frozen criterion.

**Trigger:** two consecutive review rounds each finding a new defect in the same droppable component executes that component's disposition.

## Blast radius

`- verification-class: mechanism`. The read-set is derived at run time:

- reproduce: git grep -l -E '^[[:space:]]*- check:.*(code-reviewer\.md|drift-evaluator\.md|alternate-executor-invocation|skills/run/SKILL|codex-capture|codex-skeleton-hygiene|distribution(\.ja)?\.md|test-recipe)' -- .shell-team/specs

**Predicted flips, by class, not repaired here:**

- Merged criteria that pin the old skeleton bytes: the T-107 anchors on the single-line blocks (named in the hygiene suite's own comment at `:1181`–`:1183`), the T-097/T-111/T-1092 publish-form and block locks, and the T-1118 criteria that pin the recipe line's `"$(cat "<BRIEFING_FILE>")"` and `< /dev/null` shape.
- Merged criteria that byte-pin the English sandbox region of `docs/distribution.md` (T-1149 **AC13**, T-1150 **AC15**, per T-1150's own Summarized sources and Non-goals).
- Any merged criterion that asserts `--publish-jsonl` is required.

**Indirection class:** a merged criterion that reaches one of the edited paths through a path built at run time (`bin/team-paths.sh --get …`, a variable) matches none of the literal bytes above. It is disclosed, not measured here; the release sweep's full-population arm covers it.

**Disclosed deviation:** the full-population two-arm inventory is deferred to the v2.7.4 release sweep under the operator's standing mode-A2 ruling. The docs-only carve-out does not apply: the diff reaches `bin/`, `tests/`, `templates/`, `skills/` and `agents/`.

## Review depth

Shipped code an adopter executes — `bin/codex-capture.sh` and the agent skeleton blocks — at full strength. This is a `user-visible: yes` task, so the dev-scaffold carve-out for a spec's own inline `- check:` lines does not apply. Review focus: whether `--publish-jsonl` becoming optional lets any input bypass the placement precondition or the exit 2/3/4 contract; whether the jsonl-present path is truly byte-equivalent; whether AC11's mutation is non-vacuous.

## Version derivation note

| Item | headline test | default-reachability test | derived tier | ground |
|---|---|---|---|---|
| issue #586 hotfix | not met | met | PATCH | The broken path is the shipped default review path, so default-reachability is met. Nothing new becomes possible: the fix restores a review that worked before 2.1.278. |

Tier = PATCH (v2.7.4), matching the premise on record.

## Assumptions

**Measured on this host by the coordinating session, 2026-09-24** (Claude Code **2.1.281**, codex-cli **0.156.1**, sandbox enabled, `allowUnsandboxedCommands: false`, `"codex *"` in `excludedCommands`; primary record `scratchpad/t1152/probe-evidence.md`, to be transcribed into `.shell-team/provenance/T-1152.md` — AC18):

- Probe A, bare `codex exec --sandbox read-only --cd <repo-root> --json -o <out> "<prompt>"` in one Bash call: exit 0; Codex ran `wc -l README.md` itself (exit 0); `-o` = `365`, equal to a direct `wc -l`; no `sandbox_apply`, `Operation not permitted`, `routing discovery` or `turn.failed`; no `<sandbox_violations>` block. One unrelated `error` item (a Codex plugin hook's timeout notice) appeared in the successful stream.
- Probe B, same command with the shipped `< /dev/null > <jsonl> 2>&1` tail: exit 1; no command execution; `-o` never written; 12× `Reconnecting... N/5 (workspace routing discovery failed)` then `turn.failed`; no `sandbox_apply` string.
- Probe (a) bare `review --base develop` form: exit 0, Codex ran four git commands, `-o` written. Probe (b) a backslash-newline inside the quoted prompt: exit 0, `-o` = `365`. Probe (c) a `"$(cat <briefing-file>)"` argument: exit 1, no command execution, no `-o`, the same routing-failure sequence as Probe B. Probe (d) a literal sentence naming the briefing file's absolute path: exit 0, Codex read the file and ran the command, `-o` = `365`.
- Without `< /dev/null`, the bare call did not hang and the stream carried no stdin prompt.

**Relayed, from issue #586** (the coordinating session holds the primary copy): on Claude Code 2.1.278 / codex-cli 0.154.0 (2026-09-20) the shipped form ran inside the sandbox and exited 0 with `-o` = `Unable to determine.` and `sandbox_apply: Operation not permitted` in the stream; the bare form worked; transcripts from 2.1.263–2.1.275 show the shipped form producing real verdicts; the 2.1.278 CHANGELOG line reads "every part must now match". An adopter run on 2.1.281 / 0.156.1 reported four `turn.failed` runs of the shipped form, one `<sandbox_violations>` block, and a sub-agent that kept adding hosts to `allowed_domains`.

**Unmeasured:** what the Bash tool does when a call exceeds its 600000 ms timeout (auto-background or kill) — not measured in this task (ruling 4). Whether the harness output file of a background Bash slice keeps the whole `--json` stream — the engineer measures this before choosing AC12's branch (ruling 6).

**Interpretation, flagged:** ruling 3 says `--publish` without `--publish-jsonl` "prints exactly one stderr line". This spec prints it on the successful publish only, after the last refusal, so a refused publish (exit 2/3/4) prints the refusal and not the notice (AC5). Ground: a diagnostic added to a script with several exit paths belongs after the last refusal (`.shell-team/lessons.md`, 2026-09-16).

**Measured by this role (file reads):**

- `develop` = `30d71fcbcde5f98a1eaa7b0bf5c860e5a8cc535c` (`.git/refs/heads/develop`); this branch's HEAD `90d8b766` (`.git/refs/heads/feature/T-1152-codex-sandbox-hotfix`) is the only commit after it (session git log). The freeze run confirms `git merge-base develop HEAD` = `30d71fcb…`.
- `.shell-team/provenance/T-1152.md` does not exist yet; `.shell-team/interventions/T-1152.md` does.

**Borrowed count premises** — each measured by this role by reading the file at HEAD (= `30d71fcb` for these paths); the freeze run re-measures each at the branch point with the command shown and records the value here:

| Token / count | Class | Value read now | Command at the branch point |
|---|---|---|---|
| `# T-107-step: codex` markers | borrowed (T-107) | code-reviewer **3**, drift **1** | `git show 30d71fcb:agents/code-reviewer.md \| grep -c '^[[:space:]]*# T-107-step: codex$'` (and the same for `agents/drift-evaluator.md`) |
| lines matching `^[[:space:]]*codex exec ` | borrowed (T-107, T-1118 anchor) | code-reviewer **3**, drift **1** | `git show 30d71fcb:agents/code-reviewer.md \| grep -cE '^[[:space:]]*codex exec '` |
| lines starting `codex exec ` | borrowed (T-1118) | template **1** (`:23`), SKILL **1** (`:223`) | `git show 30d71fcb:templates/prompt-blocks/alternate-executor-invocation.md \| grep -c '^codex exec '` |
| shipped lines ending `> "<RAW_JSONL>" 2>&1` | borrowed (issue #586's "eight") | **8** — code-reviewer 3, drift 1, template 1, SKILL 2 (`:184`, `:223`), host-dispatch 1; **6** in scope, **2** out (`host-dispatch.md:13`, `SKILL.md:184`) | `git grep -c -F -e '> "<RAW_JSONL>" 2>&1' 30d71fcb -- agents skills templates` |
| `^## ` headings | borrowed | distribution.md **8**, distribution.ja.md **8** | `git show 30d71fcb:docs/distribution.md \| grep -c '^## '` |
| `matches on a command line's first token` | borrowed | EN **2** (`:81`, `:100`) | `git show 30d71fcb:docs/distribution.md \| grep -cF "matches on a command line's first token"` |
| `コマンドラインの先頭トークンに一致する` | borrowed | JA **1** (`:98`) | `git show 30d71fcb:docs/distribution.ja.md \| grep -cF 'コマンドラインの先頭トークンに一致する'` |
| `--out-only`, `agentmd-no-trailing-operator`, `no event stream captured` | own-coinage | **0** each | `git grep -c -F -e '<token>' 30d71fcb -- .` |
| `alternate-executor-invocation.md` in the hygiene suite | borrowed | **0** | `git show 30d71fcb:tests/codex-skeleton-hygiene/run.sh \| grep -cF 'alternate-executor-invocation.md'` |

## Open questions

1. **`issue #590` placeholder.** Five shipped documents are deferred to one follow-up issue (see `## Shipped-docs inventory`). Until the orchestrator files it and replaces `#TBD` with the number in the five `- shipped-docs:` lines, `bin/check-adopter-docs.sh` refuses this spec (`shipped-docs-malformed`) and **AC20** is red. The replacement is inside the intent block, so it must land before the intent hash is recorded.
2. **Loud-failure prose for the alternate-executor recipe.** Rulings 1 and 3 put the default diagnosis in the invoking agent's prose. The alternate-executor recipe is run by the orchestrator through `skills/run/SKILL.md`, and the rulings do not say whether its prose also gets the diagnosis. This spec does not require it. Adding it would mean one more AC over the template.
3. **Release packaging.** The brief says PATCH v2.7.4 in an individual PR. **AC19** excludes `.claude-plugin/plugin.json`, the CHANGELOGs and the README version badges, matching v2.7.3's separate release PR. If the release is meant to land in this task's PR, AC19's allow-list must widen before the freeze.
4. **Base-ref form.** The brief allows either `not-applicable` or a two-arm expression with `develop`. The pm-spec checklist reserves the two-arm form for a branch with an open predecessor, so this spec uses `not-applicable` and pins `30d71fcb` directly, as T-1150 did with `daf43bcb`.

## Notes for engineer

- **Files likely touched:** exactly the AC19 allow-list. `skills/run/SKILL.md` changes only at its `:223` copy line and any recipe prose lines copied from the template. Registry row `:56` is `contain`, so edit the template and mirror the same lines in SKILL; `bash bin/check-prompt-sync.sh` is what proves they agree.
- **Five-marker structure stays**: `alloc`/`codex`/`diagnose`/`cleanup`/`publish` per site, with counts 3 / 1. `check_fence_structure` requires first tokens `codex-capture.sh`/`codex`/`cat`/`rm`, so `diagnose` stays a `cat` of `"<RAW_OUT>"`.
- **Fixtures:** update the three `agentmd-block-*.txt` in the same commit as the agent edits; they are compared trimmed. The `connector` mutation's anchor `-o "<RAW_OUT>"` stays valid. `tailconnector` still appends after the last line, now after the prompt's closing quote; fix its comment, which names the old redirection. Update `agentmd-bare-codex-present`'s publish full-sentence greps.
- **Prose to revisit beyond the ACs:** `agents/code-reviewer.md:10` and `agents/drift-evaluator.md:10` ("exactly what lets … `codex *` exclusion pattern match it" — true only if the line has no other part); `:33` / `:82` ("redirecting into them"); `:35` / `:65` / `:175` ("`alloc`'s two printed paths"); `:94` / `:113` (reading the last event "in the JSONL stream" — now the Bash tool result); `:216` (the troubleshooting note that suspects a missing exclusion — reconcile it with the default diagnosis: check the invocation shape first, then the exclusion; never add hosts). Do **not** remove `{txt,jsonl}` at `:212` / `:218`: the Codex CLI host path still publishes `.jsonl` under those stems.
- **`bin/codex-capture.sh`:** `--out-only` is valid only with `--alloc` (exit 2 otherwise). With `--out-only`, allocate one `mktemp` and keep the EXIT-trap discipline; plain `--alloc` stays byte-for-byte as it is. In `--publish`, when `--publish-jsonl` is omitted, skip only the jsonl-specific placement, validation, canonical check and `mv`; keep every `-o` check; print the one notice line only after a successful publish. When it is supplied, the code path must stay equivalent to today — AC6 diffs stdout, stderr, exit code and resulting tree against the committed base script. Stay shellcheck-clean and pure bash.
- **Alternate-executor recipe:** the prompt argument becomes a literal sentence naming `<BRIEFING_FILE>`'s absolute path and telling Codex to read and follow it (ruling 12's example has no inner double quote; AC3 counts exactly four `"` on the line). The briefing template itself (`:31`–`:40`) is unchanged. `:29`'s shell-quoting explanation and `:42`'s stdin sentence both change, because the substitution and `< /dev/null` are gone.
- **AC12:** measure whether a background Bash slice's harness output file keeps the whole stream, then write exactly one of the two phrases. Record the measurement in provenance.
- **AC13 runtime:** `tests/codex-agents/run.sh` and the hygiene suite are not fast; run `check-acs` with a raised `CHECK_ACS_TIMEOUT` (see `.shell-team/test-recipe.md`).
- **Measured-at-ref command check:** not applicable — the shipped docs cite a date and Claude Code / codex-cli versions, not a git ref, and print no command labelled as measured at a ref.
- **This task's own review** runs through the orchestrator with the new block shape (ruling 7); the loaded `agents/code-reviewer.md` in this session is the pre-fix version.
