# A human-adjudicated supersede sweep retires the lessons corpus entries whose ground a newer entry already covers

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1020
**Source**: GitHub issue #97 — the whole of it. Backlog item 5/5 of the v1.3.0 sprint goal.
**Base**: `develop` @ `392b934`. Every `git` anchor below resolves the base ref by the name `develop`; the executing side may substitute the resolved 7-hex literal `392b934` before freezing if it prefers a pinned ref — a meaning-preserving correction (T-1019 precedent).
**Branch**: `feature/97-lessons-supersede-sweep`

> **Two-stage spec, now closed.** This file was written in two passes. Pass 1 settled the retirement predicate, the machine constraints, the recording venue and the acceptance-criteria shape, and left the retirement set open; the set was proposed in the companion `.shell-team/specs/T-1020-supersede-adjudication.md` (24 adjudicated candidates, each with a per-condition verdict) and **ratified by the human on 2026-08-03**. Pass 2 baked the ratified set into the acceptance criteria, recorded it in "Ratified retirement set" below, placed the intent-block markers and set the status flag. Every pass-1 substitution site is now a real frozen value, and AC16 is the machine proof that none survived — it assembles the pass-1 placeholder token at run time rather than spelling it, so that this file's own prose can never satisfy its own negative check (the self-hosted-negative-check trap, corpus entry 2026-06-18).

## Problem

`bin/gen-playbook-blocks.sh` warns (non-fatally) when a generated role block exceeds `LINE_WARN_THRESHOLD=40` lines. Two blocks are over it: `templates/prompt-blocks/playbook-pm-spec.md` at 51 lines (49 bullets) and `templates/prompt-blocks/playbook-engineer.md` at 41 lines (39 bullets). The other two — `playbook-qa-verifier.md` (28 lines / 26 bullets) and `playbook-tech-lead.md` (23 lines / 21 bullets) — sit under it. *(All four counts: `pm-spec-measured-by-reading`; the pm-spec and engineer bullet counts were independently corroborated against the committed block files by enumerating the corpus's own `Scope: loop` × `Applies-to` fields and arriving at 49 and 39.)*

The threshold exists because these blocks are spliced verbatim into `agents/*.md` and are therefore paid for on every invocation of the role. The warning's own remedy text says "consider superseding stale entries" — but the corpus has no mechanism for deciding *which* entries are stale, and `bin/check-playbook.sh` deliberately refuses to guess: an entry leaves a shipped block only by being marked `Status: superseded` with a `Superseded-by` pointer at an `active` entry that now covers its ground. That judgment is a content judgment, and this task is the first time it is being made at scale.

## Goal

<!-- BEGIN intent-block: T-1020 -->

A human-ratified set of retirements — each one a `(retiree, successor)` pair that satisfies a stated three-condition predicate and every machine constraint `bin/check-playbook.sh` enforces — is applied to `.shell-team/lessons.md` append-preservingly (one `Status` line rewritten and one `Superseded-by` line added per retiree, nothing else touched), the four generated blocks and their four `agents/*.md` consumer regions are regenerated as genuine script output, and the resulting per-block line counts are frozen. Where the ratified set leaves a block above the 40-line threshold, that remaining overage is **explicitly accepted on the board with a stated reason** rather than closed by manufacturing pairs.

### Settled decisions

Each decision below is resolved. Nothing here is left to implementation judgment.

- **D1 — the retirement predicate.** "The older entry's ground is now covered by a newer entry" means all three of the following hold. They are separate tests and each is answered separately, with quoted evidence from both entries' actual text, in the adjudication companion:
  - **(i) Rule subsumption**, in two parts, both required:
    - **(i-a) occasion subsumption** — whenever the older entry's rule fires, the newer entry's rule also fires. The newer entry's occasions may be broader; they may not be a strict special case of the older's.
    - **(i-b) protection subsumption** — at those occasions, the *protection the older entry secures* is still secured by the newer entry's required action. This is an outcome test, not a wording test: the newer entry need not restate every step the older listed, but the failure the older prevents must still be prevented. A step of the older that is merely one *means* to a protection the newer secures by another means does not block retirement; a protection the newer secures by no means at all does.
  - **(ii) Applies-to role coverage does not shrink** — the successor's `Applies-to` role set (with `all` expanded to the four IN roles) must be a superset of the retiree's. **If it would shrink, the verdict is keep, not retire.** A shrinking retirement silently removes a rule from a named role's block with nothing replacing it there, which is the exact harm `bin/check-playbook.sh`'s DP-b directional rule exists to prevent one level up.
  - **(iii) the `Why` incident class is the same** — both entries record the same *kind* of incident, not merely two incidents in the same category. Two entries whose `Why` sections describe structurally different failures do not supersede each other however similar their rules read.
- **D2 — machine constraints are pre-filters, applied before the predicate.** These are properties of `bin/check-playbook.sh` (read, not assumed — see its pass-2 reference-resolution loop and T-1007 DP-b), and a candidate that violates one is out of candidacy regardless of how well it scores on D1:
  - **C1 — an entry that is already the target of another entry's `Superseded-by` cannot be retired** (the checker rejects a chain into a non-`active` target). Two entries are excluded from candidacy on this ground today: `2026-06-12 — Review gate caught a regression in practice (cross-provider review vindicated)` and `2026-07-26 — A change to a completion gate's condition count needs a contamination check and a sentinel-distinctness AC (supersedes two earlier entries)`.
  - **C2 — a `Scope: loop` entry may not point at a `Scope: maintainer` entry** (T-1007 DP-b; the other three direction combinations are legal).
  - **C3 — the `Superseded-by` value must equal the target's `date — title` key exactly after trim** — equality, never containment, em-dash separator included.
  - **C4 — the target's `Status` must be `active`.**
  - **C5 — a `Scope: maintainer` entry never appears in a generated block**, so retiring one is corpus hygiene with **zero** effect on any block's line count and never counts toward the threshold goal.
  - **C6 — retiring an `Applies-to: all` entry removes one bullet from all four blocks at once**, including the two that are already under the threshold.
- **D3 — anti-count-gaming pre-commitment, set before the proposal was written, not after it fell short.** The target arithmetic (−11 bullets for `playbook-pm-spec.md`, −1 for `playbook-engineer.md`) is a *consequence* of the retirement set, never an input to it. If the set of pairs that satisfy D1 and D2 falls short of that arithmetic, **no pair is manufactured, no predicate condition is relaxed, and no borderline pair is upgraded to reach a number.** The task instead lands on issue #97 task 4's branch: the remaining overage is explicitly accepted, on the board, naming each block still over the threshold, its measured line count, and the reason. AC9 is the machine form of that commitment.
- **D4 — the recording venue for retirements is the board plus this task's adjudication companion; `docs/loop-engineering/lessons-import-disposition.md` is not touched.** That ledger is import-scoped by its own opening sentence ("the disposition of every entry considered for **import** into ...") and declares "There are two independent tables". A retirement is not an import disposition, and adding a third table would require rewriting the ledger's own scope statement to hold a record the board and `.shell-team/specs/T-1020-supersede-adjudication.md` already hold durably and citably. **Decided explicitly, not silently mixed:** board + companion, ledger unchanged. AC14 locks the ledger byte-identical.
- **D5 — the corpus edit is exactly two line-level operations per retiree, and nothing else.** For each ratified retiree: the single line `- **Status**: active` becomes `- **Status**: superseded`, and one new line `- **Superseded-by**: <successor key>` is added inside that entry. No prose is rewritten, no entry is deleted, no entry is added, no field is reordered, no `Applies-to` or `Scope` value is edited. In particular, a candidate blocked by D1 (ii) is **not** rescued by widening the successor's `Applies-to` — that is a prose edit, and it is forbidden here.
- **D6 — the generated artifacts must be proven to be genuine script output, not hand-edited.** Regeneration is `bin/gen-playbook-blocks.sh`, and the proof is the scratch-copy regeneration diff CI already runs as its "Dogfood gen-playbook-blocks" step, reproduced locally over all eight registered files (four `templates/prompt-blocks/playbook-*.md` plus the four `agents/*.md` marker regions listed in `templates/prompt-blocks/registry.txt`). AC7.
- **D7 — the quoting rule for an entry key inside a `check:` line.** Entry keys in this corpus contain apostrophes **and** backticks. In every `check:` line, an entry key is written inside **single** quotes, with an embedded apostrophe rendered as the four-character sequence `'\''`. A key is never placed inside double quotes, because a key containing a backtick would then be a command substitution inside our own verification command — the precise class of self-inflicted vacuous pass the corpus's verification-command rule exists to prevent.

### Ratified retirement set

Ratified by the human on 2026-08-03 against `.shell-team/specs/T-1020-supersede-adjudication.md`. **N (the ratified pair count) = 1.** The ratified set is candidate C-01 and nothing else; all 23 remaining candidates keep their `KEEP` verdict, and both priced overrides were declined.

| # | candidate | retiree key | successor key | blocks shrunk |
|---|---|---|---|---|
| 1 | C-01 | `2026-07-25 — Editing a file-line-pinned` `` `bin/` `` `script needs an explicit check that cross-suite registries still match` (Scope: maintainer) | `2026-08-01 — A bin/ edit's completion checklist runs the full CI-wired suite list, not a self-selected subset` (Scope: loop) | none — the retiree is `Scope: maintainer`, so it appears in no generated block (C5) |

Consequences of that set, frozen here because the acceptance criteria measure them:

- **No block's line count changes.** `playbook-pm-spec.md` stays at 51 lines, `playbook-engineer.md` at 41, `playbook-qa-verifier.md` at 28, `playbook-tech-lead.md` at 23 (AC8). The regeneration is still required and still byte-compared (AC7): a `maintainer` retirement must leave every block untouched, and proving that is the point.
- **Both blocks remain over the 40-line threshold**, so D3's branch fires and AC9 requires the board's overage-acceptance line naming both files and both counts.
- **The corpus diff is exactly one deleted line and two added lines** (AC5), inside the single retiree entry.

Declined overrides, recorded so the decision is traceable rather than reconstructable:

- **Declined — closing the engineer block's −1 by retiring C-06** (`2026-07-12 — A "whole artifact exits 0" assertion can false-pass against a stale remote-tracking ref — narrow the assertion instead`). Would have taken engineer 41 → 40 and qa-verifier 28 → 27, at the cost of the rule that requires checking a regression assertion for a remote-ref, network or clock dependency before writing it.
- **Declined — closing the engineer block's −1 by retiring C-05** (`2026-08-02 — A ratification request pairs the exact bytes with a summary in the approver's language`). Would have taken all four blocks −1, at the cost of the approver-language and presenter-attestation requirements at every human gate — including the gate that ratified this very set.
- **Declined — closing the pm-spec block's −11.** It would have required overriding eleven of the seventeen keep verdicts that shrink that block, at least eight of them drawn from the thirteen that do not additionally violate predicate condition (ii).

## Non-goals

- **Any new lesson.** No entry is added to the corpus by this task. AC6 locks the canonical-entry-heading count against the base ref.
- **Any threshold change.** `LINE_WARN_THRESHOLD=40` in `bin/gen-playbook-blocks.sh` stays exactly as it is. Moving the threshold to meet the corpus is the inverse of the task. AC14.
- **Any `bin/` change, any new test suite, any `.github/**` change.** No checker is written or modified; no CI step is added, removed or reordered. AC14.
- **Targeting the two under-threshold blocks.** No retiree is selected because it would shrink `playbook-qa-verifier.md` or `playbook-tech-lead.md`. Those two blocks may shrink only as a *consequence* of retiring an entry whose `Applies-to` also covers pm-spec or engineer (or is `all`, C6). AC15.
- **Rewriting, re-scoping or re-roling any entry's prose** (D5). Including the tempting case: a candidate blocked by D1 (ii) is kept, not rescued by editing the successor's `Applies-to`.
- **Editing `docs/loop-engineering/lessons-import-disposition.md`** (D4).
- **A CHANGELOG entry, a version bump, or any adopter-facing document change** (`README*`, `docs/`, `CONTRIBUTING.md`, `templates/todo-template.md`). The only `templates/` files that change are the four generated `playbook-*.md` blocks.
- **Retiring an entry because a tool now enforces it.** Two entries in the corpus describe disciplines a shipped checker now also enforces (a board heading-identity check; a board line-format check). The schema has no "obsolete by mechanization" retirement path — `Status: superseded` structurally requires a successor *entry* — and inventing one would be a `bin/` change. Recorded as an observation in the companion, not acted on.

## Acceptance criteria

Base ref for every `git` anchor is `develop`. Every check runs from the repository root, names every file it reads explicitly, writes only under `$TMPDIR`, and carries a positive control where it greps for an absent or expected-empty condition. Entry keys inside a check follow D7's quoting rule.

Every value that depended on the ratification is baked in below at N = 1 (the ratified set of "Ratified retirement set"). Where a criterion names an entry key, the key is written inside single quotes with an embedded apostrophe rendered as `'\''` — both keys in the ratified pair contain a shell-significant character (the retiree's title carries backticks, the successor's carries an apostrophe), so D7 is load-bearing here rather than precautionary.

- [ ] **AC1** The set of entry keys carrying `Status: superseded` in the lessons corpus equals exactly the three pre-existing retirements plus the one ratified retiree — no more, no fewer. Four keys, and the fourth is C-01's retiree.
  - check: rc=0; L=.shell-team/lessons.md; test -r "$L" || exit 1; d=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac1.XXXXXX") || exit 1; awk '/^## [0-9][0-9][0-9][0-9]-/{k=substr($0,4)} /^- \*\*Status\*\*: superseded[[:space:]]*$/{if (k != "") print k}' "$L" | sort -u > "$d/actual.txt"; printf '%s\n' '2026-06-13 — Don'\''t take a Codex inline-paste review'\''s false positive at face value' '2026-07-19 — A change to a completion gate'\''s condition count needs an AC covering downstream consumers of the gate'\''s result' '2026-07-19 — A downstream-impact inventory needs a check for other-gate raw output bleeding into a combined value' '2026-07-25 — Editing a file-line-pinned `bin/` script needs an explicit check that cross-suite registries still match' | sort -u > "$d/expected.txt"; cmp -s "$d/actual.txt" "$d/expected.txt" || rc=1; n=$(grep -c . "$d/actual.txt" || true); test "$n" = "4" || rc=1; rm -rf "$d"; test "$rc" -eq 0
- [ ] **AC2** Every `Superseded-by` pointer in the corpus — four in total, the three pre-existing ones plus C-01's — names its successor by the exact `date — title` key (C3: equality after trim, never containment), and no other entry gained a pointer.
  - check: rc=0; L=.shell-team/lessons.md; test -r "$L" || exit 1; d=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac2.XXXXXX") || exit 1; awk '/^## [0-9][0-9][0-9][0-9]-/{k=substr($0,4)} /^- \*\*Superseded-by\*\*: /{v=$0; sub(/^- \*\*Superseded-by\*\*: /,"",v); gsub(/[[:space:]]+$/,"",v); if (k != "") print k " => " v}' "$L" | sort -u > "$d/actual.txt"; printf '%s\n' '2026-06-13 — Don'\''t take a Codex inline-paste review'\''s false positive at face value => 2026-06-12 — Review gate caught a regression in practice (cross-provider review vindicated)' '2026-07-19 — A change to a completion gate'\''s condition count needs an AC covering downstream consumers of the gate'\''s result => 2026-07-26 — A change to a completion gate'\''s condition count needs a contamination check and a sentinel-distinctness AC (supersedes two earlier entries)' '2026-07-19 — A downstream-impact inventory needs a check for other-gate raw output bleeding into a combined value => 2026-07-26 — A change to a completion gate'\''s condition count needs a contamination check and a sentinel-distinctness AC (supersedes two earlier entries)' '2026-07-25 — Editing a file-line-pinned `bin/` script needs an explicit check that cross-suite registries still match => 2026-08-01 — A bin/ edit'\''s completion checklist runs the full CI-wired suite list, not a self-selected subset' | sort -u > "$d/expected.txt"; cmp -s "$d/actual.txt" "$d/expected.txt" || rc=1; n=$(grep -c . "$d/actual.txt" || true); test "$n" = "4" || rc=1; rm -rf "$d"; test "$rc" -eq 0
- [ ] **AC3** The corpus is schema-valid: the shipped validator accepts it, which is also what `bin/gen-playbook-blocks.sh` runs as its own fail-closed preflight.
  - check: L="$(bash bin/team-paths.sh --get lessons)"; test -n "$L" && test -r "$L" && bash bin/check-playbook.sh "$L"
- [ ] **AC4** D2's constraints re-measured independently of `bin/check-playbook.sh`, so a validator regression cannot hide a violated pre-filter: C-01's successor exists and is `Scope: loop` / `Status: active` (C4), it carries no `Superseded-by` of its own (C1 — it is not itself retired), and C2 is satisfied for free because the retiree is `maintainer` (a `maintainer` → `loop` pointer is one of the three legal directions). The 2026-07-26 completion-gate entry — the corpus's other supersede target — is asserted `loop|active` as a second control.
  - check: rc=0; L=.shell-team/lessons.md; test -r "$L" || exit 1; d=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac4.XXXXXX") || exit 1; awk '/^## [0-9][0-9][0-9][0-9]-/{k=substr($0,4); sc=""} /^- \*\*Scope\*\*: /{sc=$0; sub(/^- \*\*Scope\*\*: /,"",sc); gsub(/[[:space:]]+$/,"",sc)} /^- \*\*Status\*\*: /{st=$0; sub(/^- \*\*Status\*\*: /,"",st); gsub(/[[:space:]]+$/,"",st); if (k != "") print k "|" sc "|" st}' "$L" | sort -u > "$d/t.txt"; n=$(grep -c . "$d/t.txt" || true); test "$n" -ge 80 || rc=1; grep -qxF -- '2026-07-26 — A change to a completion gate'\''s condition count needs a contamination check and a sentinel-distinctness AC (supersedes two earlier entries)|loop|active' "$d/t.txt" || rc=1; grep -qxF -- '2026-08-01 — A bin/ edit'\''s completion checklist runs the full CI-wired suite list, not a self-selected subset|loop|active' "$d/t.txt" || rc=1; grep -qxF -- '2026-07-25 — Editing a file-line-pinned `bin/` script needs an explicit check that cross-suite registries still match|maintainer|superseded' "$d/t.txt" || rc=1; awk '/^## [0-9][0-9][0-9][0-9]-/{k=substr($0,4)} /^- \*\*Superseded-by\*\*: /{if (k != "") print k}' "$L" | sort -u > "$d/p.txt"; np=$(grep -c . "$d/p.txt" || true); test "$np" = "4" || rc=1; if grep -qxF -- '2026-08-01 — A bin/ edit'\''s completion checklist runs the full CI-wired suite list, not a self-selected subset' "$d/p.txt"; then rc=1; fi; rm -rf "$d"; test "$rc" -eq 0
- [ ] **AC5** Append-preservation, measured in the shape this file actually has. `.shell-team/lessons.md` is an all-bullet file, so a deleted line appears in a unified diff as a two-hyphen run and a check requiring a non-hyphen second character would be vacuous; the check reads the diff stat's own deletion column and then asserts the *breakdown*. At N = 1: exactly **1** deletion, and it is the literal line `- **Status**: active`; exactly **2** additions, each of them either `- **Status**: superseded` or a `- **Superseded-by**: ` line. No prose line is deleted or rewritten.
  - check: rc=0; L=.shell-team/lessons.md; test -r "$L" || exit 1; d=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac5.XXXXXX") || exit 1; git diff -U0 develop -- "$L" > "$d/diff.txt" || rc=1; add=$(git diff --numstat develop -- "$L" | awk '{print $1}'); del=$(git diff --numstat develop -- "$L" | awk '{print $2}'); test -n "$add" || add=0; test -n "$del" || del=0; test "$del" = "1" || rc=1; test "$add" = "2" || rc=1; grep '^-' "$d/diff.txt" | grep -v '^--- ' > "$d/del.txt" || true; grep '^+' "$d/diff.txt" | grep -v '^+++ ' > "$d/add.txt" || true; nd=$(grep -c . "$d/del.txt" || true); na=$(grep -c . "$d/add.txt" || true); test "$nd" = "1" || rc=1; test "$na" = "2" || rc=1; bd=$(grep -vxF -- '-- **Status**: active' "$d/del.txt" | grep -c . || true); test "$bd" = "0" || rc=1; ba=$(grep -v -e '^+- \*\*Status\*\*: superseded$' -e '^+- \*\*Superseded-by\*\*: .' "$d/add.txt" | grep -c . || true); test "$ba" = "0" || rc=1; rm -rf "$d"; test "$rc" -eq 0
- [ ] **AC6** No new lesson and no removed lesson: the count of canonical entry headings is identical to the base ref's, with a positive control proving the pattern actually matches this corpus.
  - check: rc=0; L=.shell-team/lessons.md; test -r "$L" || exit 1; now=$(grep -c '^## [0-9][0-9][0-9][0-9]-' "$L" || true); base=$(git show develop:.shell-team/lessons.md | grep -c '^## [0-9][0-9][0-9][0-9]-' || true); test "$now" = "$base" || rc=1; test "$now" -ge 80 || rc=1; test "$rc" -eq 0
- [ ] **AC7** D6: every shipped block and every consumer marker region is genuine `bin/gen-playbook-blocks.sh` output. Regenerating into a scratch copy reproduces all eight registered files byte-for-byte — the same procedure as CI's "Dogfood gen-playbook-blocks" step, with the file count asserted at 8 so a registry misread cannot make it pass vacuously.
  - check: rc=0; L="$(bash bin/team-paths.sh --get lessons)"; test -n "$L" && test -r "$L" || exit 1; files="$(awk '$1 == "marker" && $2 ~ /^playbook-/ { for (i = 3; i <= NF; i++) print $i; print "templates/prompt-blocks/" $2 }' templates/prompt-blocks/registry.txt | sort -u)"; nf=$(printf '%s\n' "$files" | grep -c . || true); test "$nf" = "8" || rc=1; s=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac7.XXXXXX") || exit 1; for f in $files; do mkdir -p "$s/$(dirname "$f")"; cp "$f" "$s/$f" || rc=1; done; mkdir -p "$s/templates/prompt-blocks"; cp templates/prompt-blocks/registry.txt "$s/templates/prompt-blocks/registry.txt" || rc=1; bash bin/gen-playbook-blocks.sh --root "$s" --lessons "$L" >/dev/null 2>&1 || rc=1; for f in $files; do cmp -s "$f" "$s/$f" || rc=1; done; rm -rf "$s"; test "$rc" -eq 0
- [ ] **AC8** The four blocks' line counts are exactly the frozen post-retirement values: engineer **41**, qa-verifier **28**, tech-lead **23**, pm-spec **51** — all four unchanged from the base ref, because C-01's retiree is `Scope: maintainer` and C5 says such an entry appears in no block. This criterion passes at the base ref by construction, and that is deliberate: paired with AC7 it asserts that regenerating after a maintainer-only retirement leaves every shipped block byte-identical, which is the property a maintainer retirement is claimed to have.
  - check: rc=0; for r in engineer qa-verifier tech-lead pm-spec; do f="templates/prompt-blocks/playbook-$r.md"; test -r "$f" || rc=1; done; test "$rc" -eq 0 || exit 1; e=$(wc -l < templates/prompt-blocks/playbook-engineer.md | tr -d ' '); q=$(wc -l < templates/prompt-blocks/playbook-qa-verifier.md | tr -d ' '); t=$(wc -l < templates/prompt-blocks/playbook-tech-lead.md | tr -d ' '); p=$(wc -l < templates/prompt-blocks/playbook-pm-spec.md | tr -d ' '); test "$e" = "41" || rc=1; test "$q" = "28" || rc=1; test "$t" = "23" || rc=1; test "$p" = "51" || rc=1; test -z "$(git diff --name-only develop -- templates/prompt-blocks/playbook-engineer.md templates/prompt-blocks/playbook-qa-verifier.md templates/prompt-blocks/playbook-tech-lead.md templates/prompt-blocks/playbook-pm-spec.md)" || rc=1; test "$rc" -eq 0
- [ ] **AC9** D3's anti-count-gaming commitment in machine form: **either** every block is at or under the 40-line threshold, **or** the board carries a `T-1020 overage accepted` line that names every block still over it. The check derives the over-threshold set from the real files rather than from a frozen list, so it cannot go stale against the ratified set.
  - check: rc=0; B="$(bash bin/team-paths.sh --get todo)"; test -n "$B" && test -r "$B" || exit 1; nb=$(ls templates/prompt-blocks/playbook-*.md | grep -c . || true); test "$nb" = "4" || rc=1; over=0; for f in templates/prompt-blocks/playbook-*.md; do n=$(wc -l < "$f" | tr -d ' '); if [ "$n" -gt 40 ]; then over=$((over + 1)); fi; done; if [ "$over" -gt 0 ]; then line=$(grep -F -- 'T-1020 overage accepted' "$B" | head -1); test -n "$line" || rc=1; for f in templates/prompt-blocks/playbook-*.md; do n=$(wc -l < "$f" | tr -d ' '); if [ "$n" -gt 40 ]; then printf '%s\n' "$line" | grep -qF -- "$(basename "$f")" || rc=1; printf '%s\n' "$line" | grep -qF -- "$n" || rc=1; fi; done; fi; test "$rc" -eq 0
- [ ] **AC10** The adjudication companion exists and adjudicates rather than pre-trims: every candidate block carries all three predicate verdicts and one recommendation, and the counts agree — so a candidate cannot be listed without a verdict, and a verdict cannot be recorded without its evidence lines.
  - check: rc=0; A=.shell-team/specs/T-1020-supersede-adjudication.md; test -r "$A" || exit 1; nb=$(grep -c '^### C-[0-9][0-9] ' "$A" || true); nv=$(grep -c '^\*\*Verdict\*\*: ' "$A" || true); nr=$(grep -c '^\*\*Retiree\*\*: ' "$A" || true); ns=$(grep -c '^\*\*Successor\*\*: ' "$A" || true); ni=$(grep -c '^- (i) ' "$A" || true); nii=$(grep -c '^- (ii) ' "$A" || true); niii=$(grep -c '^- (iii) ' "$A" || true); nl=$(grep -c '^\*\*Line impact\*\*: ' "$A" || true); test "$nb" -ge 24 || rc=1; for c in "$nv" "$nr" "$ns" "$ni" "$nii" "$niii" "$nl"; do test "$c" = "$nb" || rc=1; done; test "$rc" -eq 0
- [ ] **AC11** Scope lock: the branch's changed-and-added file set is exactly the four required files, plus at most this task's three record files. **The eight generated files are deliberately absent from the required list** — at N = 1 the ratified retiree is `Scope: maintainer`, so regeneration reproduces every block and every marker region byte-identically and none of them enters the diff at all. AC8 asserts that byte-identity positively; this criterion is its negative half, and a generated file appearing here is a scope violation rather than an expected artifact. **Merge-point-scoped and expected to go stale after merge** — once later work lands on `develop`, `git diff develop` no longer describes this task. Do not widen its base-ref resolution and do not re-derive it per rework round.
  - check: d=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac11.XXXXXX") || exit 1; git diff --name-only develop > "$d/raw.txt"; git ls-files --others --exclude-standard >> "$d/raw.txt"; sort -u "$d/raw.txt" > "$d/actual.txt"; printf '%s\n' .shell-team/lessons.md .shell-team/specs/T-1020-lessons-supersede-sweep.md .shell-team/specs/T-1020-supersede-adjudication.md .shell-team/todo.md | sort > "$d/required.txt"; miss=$(comm -13 "$d/actual.txt" "$d/required.txt" | grep -c . || true); extra=$(comm -23 "$d/actual.txt" "$d/required.txt" | grep -vE '^\.shell-team/(provenance|reviews|interventions)/T-1020\.md$' | grep -c . || true); rm -rf "$d"; test "$miss" = "0" && test "$extra" = "0"
- [ ] **AC12** The task's records exist and every board-side lint is clean, including the structural heading diff against the base and the PII-shape check over the branch diff.
  - check: test -r .shell-team/provenance/T-1020.md && test -r .shell-team/reviews/T-1020.md && bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)" && bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base develop && bash bin/check-pii-shapes.sh --base develop
- [ ] **AC13** Every suite and dogfood step in `.github/workflows/check-handoff.yml` was run locally, in the order the workflow lists them, and the result reported in the hand-off. No `check:` — no command can prove that a command was run. `SKIP` is its expected `check-acs.sh` result.
- [ ] **AC14** The prohibited surfaces are byte-identical to the base ref — no `bin/` change, no `.github/**` change, no new or edited suite, no skill or doc change, and the import-disposition ledger untouched (D4) — with the threshold constant asserted present and unchanged as its own positive control.
  - check: rc=0; test -z "$(git diff --name-only develop -- bin .github tests skills docs CONTRIBUTING.md README.md templates/todo-template.md templates/prompt-blocks/registry.txt)" || rc=1; grep -qxF 'LINE_WARN_THRESHOLD=40' bin/gen-playbook-blocks.sh || rc=1; test -r docs/loop-engineering/lessons-import-disposition.md || rc=1; test "$rc" -eq 0
- [ ] **AC15** No retiree was selected to shrink an already-under-threshold block: every ratified retiree is either `Scope: maintainer` (C5 — zero block impact) or has an `Applies-to` that includes `pm-spec`, `engineer` or `all`. A retiree whose roles are only `qa-verifier` and/or `tech-lead` fails this criterion.
  - check: rc=0; L=.shell-team/lessons.md; test -r "$L" || exit 1; d=$(mktemp -d "${TMPDIR:-/tmp}/t1020ac15.XXXXXX") || exit 1; awk '/^## [0-9][0-9][0-9][0-9]-/{k=substr($0,4); ap=""; sc=""} /^- \*\*Applies-to\*\*: /{ap=$0; sub(/^- \*\*Applies-to\*\*: /,"",ap); gsub(/[[:space:]]+$/,"",ap)} /^- \*\*Scope\*\*: /{sc=$0; sub(/^- \*\*Scope\*\*: /,"",sc); gsub(/[[:space:]]+$/,"",sc)} /^- \*\*Status\*\*: superseded[[:space:]]*$/{if (k != "") print k "|" sc "|" ap}' "$L" > "$d/ret.txt"; n=$(grep -c . "$d/ret.txt" || true); test "$n" = "4" || rc=1; bad=$(grep -v '|maintainer|' "$d/ret.txt" | grep -vE '\|(all|.*(pm-spec|engineer))' | grep -c . || true); test "$bad" = "0" || rc=1; rm -rf "$d"; test "$rc" -eq 0
- [ ] **AC16** No pass-1 substitution site survived the freeze. The placeholder token this spec used before ratification is **assembled at run time by the check** rather than written into the file, because a negative check for a token that its own spec spells can never go green — the self-hosted-negative-check trap (corpus entry 2026-06-18), which this criterion would otherwise have walked straight into. Positive control: the frozen heading "Ratified retirement set" must be found by the same grep, proving the file was really read.
  - check: rc=0; S=.shell-team/specs/T-1020-lessons-supersede-sweep.md; test -r "$S" || exit 1; tok="$(printf 'PENDING_%s' RATIFICATION)"; if grep -qF -- "$tok" "$S"; then rc=1; fi; grep -qF -- 'Ratified retirement set' "$S" || rc=1; test "$rc" -eq 0

## Input space

**Reachable input classes** — what the corpus and its consumers really contain, and what the change must handle correctly:

- **The 83 committed entries of `.shell-team/lessons.md`** as they stand at the base ref: 80 `active` (71 `Scope: loop`, 9 `Scope: maintainer`) and 3 already `superseded`. *(`pm-spec-measured-by-reading`; the derived per-role bullet counts reproduce the four committed blocks exactly, which is the corroboration.)*
- **Entry keys containing shell-significant characters.** Real keys carry apostrophes (`gate's`), backticks (`` `bin/` ``), parentheses, colons, semicolons and the em-dash separator. A `Superseded-by` value and every `check:` line that names a key must reproduce those bytes exactly (C3, D7).
- **Every `Applies-to` shape the schema permits**: a single role, a comma-and-space list, a comma-without-space list (`engineer,qa-verifier` occurs), and the `all` token — all of which `bin/gen-playbook-blocks.sh` trims before matching, so the block membership arithmetic must be derived from the trimmed tokens, not from the raw string.
- **Both `Scope` values**, with the C5 consequence that a maintainer retirement is invisible to every block, and the C2 consequence that a `loop` entry's successor must also be `loop`.
- **The four generated blocks and the four `agents/*.md` marker regions** registered in `templates/prompt-blocks/registry.txt`, each of which must come out of the generator byte-identically (D6).
- **The board as it is**, 500+ KB with one `## Active` section and a long `## Done` section, where this task's entry is inserted and where the overage acceptance (if any) is recorded.

**Out-of-scope synthetic extremes** — declined deliberately; a finding grounded only in one of these is out of contract:

- **Malformed corpus inputs**: a NUL byte, an unterminated or ambiguous fence, a forged or non-canonical `## ` heading, a multi-line field value, a control character in a field, an unknown field bullet, a duplicate entry key. `bin/check-playbook.sh` already rejects each of these fail-closed and this task changes no checker; AC3 confirms the corpus passes, it does not re-derive the validator's own coverage.
- **Adversarial keys constructed to defeat equality matching** — a key that is a prefix or substring of another, a key differing only by trailing whitespace or by a hyphen where the em-dash belongs. Equality-not-containment is T-108's decision, tested in `tests/gen-playbook-blocks/` and `bin/check-playbook.sh`'s own suite; escalating exotic near-miss keys here is not an open finding class.
- **Hypothetical or future corpus entries.** The adjudication is over the entries committed at the base ref. "But what if a later entry made this pair valid" is not a finding.
- **Corpus files other than the one `bin/team-paths.sh --get lessons` resolves to**, and multi-file `check-playbook.sh` invocations.
- **CRLF line endings, a missing final newline, and non-UTF-8 bytes** anywhere in the corpus or the consumers. The generator's `head`/`tail` splice already preserves them outside the marker region (T-047), and no such shape exists in the committed files.
- **Scale**: a corpus of thousands of entries, a single entry of megabyte length, or a block whose regeneration time matters.
- **The retirement predicate applied to anything other than the committed corpus** — for example to a candidate raised in a retro but never promoted. Those are the import ledger's subject (D4), not this task's.

<!-- END intent-block: T-1020 -->

## Assumptions

- The 40-line threshold's purpose is prompt economy, and its warning is advisory by design (`bin/gen-playbook-blocks.sh` prints it to stderr and never fails the run). Nothing in CI fails on an over-threshold block today. *Unverified as an intent claim; verified as behavior by reading the generator's own comment ("This never fails the run") and its warning branch.*
- The corpus's promotion discipline — nearly every entry carries an explicit "this is adjacent to, but distinct from" or "this extends/generalizes" sentence positioning it against its neighbours — means measurable redundancy is low by construction. The adjudication's outcome should be read against that, not against an assumption that a 49-bullet block must contain 11 stale entries.
- `develop` @ `392b934` resolves locally as this branch's merge point. The executing side confirms this before freezing and may substitute the pinned literal.

## Open questions

- **None. The one open question — the retirement set — was closed by human ratification on 2026-08-03**: C-01 only, all 23 remaining keep verdicts standing, both priced overrides declined, remaining overage accepted. Recorded in "Ratified retirement set" and baked into AC1/AC2/AC4/AC5/AC8/AC11.
- Related but deliberately outside this task, recorded so it is not mistaken for an unresolved question here: the pre-publication repository ran a 74-entry pairwise domination sweep on 2026-07-26 (its T-108) and found zero dominations, and that deferral record never migrated into this repository. Porting it is **issue #112**. It is corroborating context for this task's own result, not an input to it — this adjudication was performed against the committed corpus independently, and its finding stands on the quoted evidence in the companion.

## Notes for engineer

**Scope-lock allow-list** (AC11), at the ratified N = 1. Four files must appear in the diff, three may:

- `.shell-team/lessons.md` — exactly one Status rewrite and one Superseded-by addition, inside the single retiree entry (D5)
- `.shell-team/specs/T-1020-lessons-supersede-sweep.md`, `.shell-team/specs/T-1020-supersede-adjudication.md`
- `.shell-team/todo.md`
- `.shell-team/provenance/T-1020.md`, `.shell-team/reviews/T-1020.md`, `.shell-team/interventions/T-1020.md` — permitted, not required by AC11's list; the provenance and interventions records are required deliverables regardless (AC12)

**The eight generated files must NOT appear in the diff**, and that is the ratified set's defining consequence rather than an omission: `templates/prompt-blocks/playbook-{engineer,qa-verifier,tech-lead,pm-spec}.md` and the marker regions of `agents/{engineer,qa-verifier,tech-lead,pm-spec}.md` all stay byte-identical to the base ref, because C-01's retiree is `Scope: maintainer` and never appeared in any block. AC8 asserts that positively (frozen counts 41 / 28 / 23 / 51 **and** an empty `git diff` over the four blocks); AC11 asserts it negatively (any of the eight showing up counts as `extra`). If one of them does change, something other than a maintainer entry was retired — stop and escalate rather than committing it.

**Order of operations.** Edit the corpus first, then run `bin/gen-playbook-blocks.sh` from the repository root — never hand-edit a block or a marker region. The generator must still be run even though its output is expected to be unchanged: AC7 regenerates into a scratch copy and `cmp`s all eight files, which is what turns "the blocks did not change" from an assumption into a measurement. Note that the generator refuses to write anything at all if `bin/check-playbook.sh` rejects the corpus (its fail-closed preflight), so a malformed Superseded-by value surfaces as "nothing was regenerated", not as a bad block.

**Gotchas.**

- The generator's own threshold warning goes to **stderr** and does not fail the run. Do not read a clean exit as "the block is under the threshold" — AC8 and AC9 are the measurements.
- `wc -l` prints leading whitespace on this platform; every check pipes it through `tr -d ' '`.
- A retirement produces **one** deletion and **two** additions per entry in the corpus diff (the Status line is a rewrite). At N = 1, AC5's frozen numbers are 1 and 2, not 0 and 1 — and the deletion column is the right instrument precisely because the corpus is an all-bullet file where a deleted line begins with two hyphens.
- `agents/pm-spec.md`'s playbook region is one of the eight regenerated files, so a differently-ratified set would have edited the prompt of the role that wrote this spec. At N = 1 it does not, and AC7's byte comparison — not a visual read — is what establishes that.
- **The retiree's key contains backticks and the successor's contains an apostrophe.** Both appear in `check:` lines (AC1, AC2, AC4) and one of them goes into the corpus as a `Superseded-by` value. Inside the corpus the value is plain text and needs no escaping; inside a `check:` line it is single-quoted per D7, with the apostrophe written as `'\''`. Do not "tidy" a key by dropping a backtick or normalising the em-dash — C3 is equality, and `bin/check-playbook.sh` will reject a near-miss as a dangling pointer.
- **AC9's anchor phrase is a self-reference hazard on the board.** AC9 greps the board for the frozen phrase `T-1020 overage accepted`. This task's own board entry discusses that mechanism in prose, and if the prose ever quotes the anchor verbatim, AC9 passes on a *description* of an acceptance rather than an acceptance. The entry as written at task-open deliberately paraphrases it ("accept the remaining overage under D3") for exactly this reason. Write the anchor exactly once, on the real acceptance line, and only when the acceptance is real — and re-read the entry after editing to confirm there is still exactly one occurrence.

**Prior art.** `bin/check-playbook.sh`'s pass-2 reference-resolution loop (AC1–AC7 of T-108, plus T-1007 DP-b) is the authority on what a legal retirement looks like; the three retirements already in the corpus — one from 2026-06-13 and the two folded into the 2026-07-26 completion-gate entry — are the worked examples, and the 2026-07-26 entry's title (`... (supersedes two earlier entries)`) is the precedent for how a successor announces itself.

## Body-to-AC correspondence

Every normative directive in the body above, mapped 1:1 to the acceptance criterion that carries it or to an explicit exemption with a reason.

| # | body directive | AC |
|---|---|---|
| 1 | D1 — the three-condition retirement predicate governs every retirement, each condition answered separately with quoted evidence | **AC10** (every candidate block carries all three verdicts plus its evidence lines) + **AC1** (only ratified retirees are superseded) |
| 2 | D2/C1 — an entry already targeted by another `Superseded-by` cannot be retired | **AC3** + **AC4** |
| 3 | D2/C2 — a `loop` entry may not point at a `maintainer` entry | **AC3** + **AC4** |
| 4 | D2/C3 — the pointer value equals the target key exactly after trim | **AC2** |
| 5 | D2/C4 — the target's Status must be `active` | **AC4** |
| 6 | D2/C5 — a maintainer retirement never counts toward the threshold goal | **AC8** (frozen counts encode the arithmetic) + **AC15** |
| 7 | D2/C6 — an `all` retirement shrinks all four blocks | **AC8** |
| 8 | D3 — do not manufacture pairs; accept the remaining overage with a stated reason | **AC9** |
| 9 | D4 — record on the board and in the companion; leave the import ledger untouched | **AC14** (ledger byte-identical) + **AC9** (board record) |
| 10 | D5 — only a Status rewrite plus a Superseded-by addition; no prose edit | **AC5** |
| 11 | D5 — a (ii)-blocked candidate is kept, never rescued by widening the successor's `Applies-to` | **AC5** (an `Applies-to` edit is an unpermitted added/deleted line) |
| 12 | D6 — the blocks and marker regions must be genuine generator output | **AC7** |
| 13 | D7 — an entry key inside a `check:` line is single-quoted, never double-quoted | info-only (not promoted to AC) — reason: it is a rule about how *this spec's own* check lines are written, and AC1/AC2/AC4 are the artifacts it governs; a check line that violated it would fail those criteria outright rather than needing a separate detector |
| 14 | Non-goal — no new lesson | **AC6** |
| 15 | Non-goal — no threshold change | **AC14** |
| 16 | Non-goal — no `bin/`, no new suite, no `.github/**` | **AC14** |
| 17 | Non-goal — do not target the two under-threshold blocks | **AC15** |
| 18 | Non-goal — no adopter-facing document change | **AC14** + **AC11** |
| 19 | Non-goal — an entry is not retired merely because a tool now enforces it | info-only (not promoted to AC) — reason: the schema makes it unrepresentable (a `superseded` entry structurally requires a successor entry), so no artifact state exists for a criterion to measure; the observation is recorded in the companion instead |
| 20 | Frozen per-block line counts | **AC8** |
| 21 | The companion adjudicates every candidate, including the ones recommended for keeping | **AC10** |
| 22 | Scope lock, merge-point-scoped and expected to go stale after merge | **AC11** |
| 23 | Records exist and board lints are clean | **AC12** |
| 24 | The full CI-wired suite list runs locally, in workflow order | **AC13** (runtime, `SKIP` by design) |
| 25 | No pass-1 substitution site survives the freeze (the placeholder token is assembled at run time by the check, never spelled in this file) | **AC16** |
| 25a | AC9's board anchor is written exactly once, on the real acceptance line, never quoted in describing prose | **AC9** — the criterion greps the board for the anchor and then requires that same line to carry each over-threshold block's basename and its measured line count, so a descriptive mention that carries neither cannot satisfy it |
| 26 | Every count in this spec and the companion is `pm-spec-measured-by-reading` and is re-measured by the executing side before the freeze | info-only (not promoted to AC) — reason: it is an instruction to the coordinating session about pre-freeze conduct, not a property of the delivered artifact; the artifact-side guarantee is AC8, whose frozen numbers are measured mechanically |
| 27 | Task classification: corpus-judgment work, not verification-mechanism work | info-only (not promoted to AC) — reason: a review-rigour calibration statement recorded on the board, with no artifact state to assert |
| 28 | The base ref may be substituted for its pinned 7-hex literal before freezing | info-only (not promoted to AC) — reason: a meaning-preserving freeze-time correction allowance (T-1019 precedent), not a deliverable property |
| 29 | Ratified set (pass 2) — C-01 and nothing else; N = 1; both priced overrides declined | **AC1** + **AC2** + **AC4** (the exact retiree and pointer, no others) + **AC5** (the 1/2 diff shape only that set produces) |
| 30 | Ratified set consequence — no block's line count changes and none of the eight generated files enters the diff | **AC8** (positive: frozen counts plus an empty diff over the four blocks) + **AC11** (negative: a generated file in the diff is `extra`) + **AC7** (the blocks are still proven to be regenerated output, not merely untouched) |
| 31 | Issue #112 (porting the pre-publication 74-entry domination-sweep deferral record) is outside this task and is not an input to its finding | info-only (not promoted to AC) — reason: it names work in another issue; this task's artifacts carry no state that could assert or deny it, and the adjudication's evidence is quoted from the committed corpus independently of it |
