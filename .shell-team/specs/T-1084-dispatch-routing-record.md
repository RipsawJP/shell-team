# Situational dispatch routing record: the two-axis choice decided in the Routing Map, recorded on the board, and audited against closed vocabularies

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1084

## Problem

`docs/loop-engineering/phase-multiplexing.md` already carries a complete, priced dispatcher on paper: two `- dispatch-axis:` lines fixing the closed value sets, five `- dispatch-rule:` lines binding each axis value to a modality and a priced ground, and two `- dispatch-note:` lines. Nothing in the shipped loop reads any of it. No role is told to apply those rules, no artefact records which value was chosen for a given task, and no checker can tell a real routing choice from a sentence someone wrote in prose. The consequence is that the loop's own width decisions are unauditable: a task that ran serially and a task that considered and rejected `tier1-fanout` leave byte-identical evidence, and the note's `unconditional` defaults are indistinguishable from nobody having looked at the rules at all.

## Summarized sources

- `docs/loop-engineering/phase-multiplexing.md:358–366` — the two `- dispatch-axis:` lines' closed value sets (`implement` over `serial|tier2|tier3`; `verify` over `serial|tier1-fanout`), the five `- dispatch-rule:` lines' four-field shape (axis-value, modality from `unconditional|conditional`, situation, priced ground), the fact that both implement-axis tiers are `conditional` with a `trigger: `-opening situation while both `serial` values are `unconditional`, and the two `- dispatch-note:` lines — `inter-axis` (a tier3 implement choice makes tier1-fanout *more* attractive, not competing) and `depth-axis` (issue #274's depth axis joins as a **third axis**, multiplying the existing six routing targets rather than requiring fresh rows).
- `docs/loop-engineering/phase-multiplexing.md:355–357` — the three `- recommendation:` lines whose ids this record's ground field cites: `tier1-verification-fanout — staged-adoption` gated on two named preconditions (the per-instance-telemetry-discriminator invariant, and an orchestration step able to launch N agent instances and aggregate their verdicts); `tier2-parallel-implementations-judge — not-yet` (no judge exists to run); `tier3-work-splitting — not-yet` (measured negative saving).
- `.shell-team/specs/T-1079-tier2-judge.md:123–124` (**AC11**) — the note's `- dispatch-axis:`/`- dispatch-rule:`/`- dispatch-note:` totals and their closed grammars, including the **priced-ground discipline** this record mirrors (every rule line must contain one of `saving:`, `recommendation:`, `break-even:`, `cost-input:`) and the fixed `- dispatch-note: ` total of **2**. The distinction carried over: that criterion re-derives the note's own counts at run time and is satisfied only while the note stays exactly as it is — so any edit to the note's dispatch region is a merged criterion's problem, not a free choice.
- `.shell-team/specs/T-1083-agent-launch-fanout.md:174–175` (**AC18**) — `docs/loop-engineering/phase-multiplexing.md` is byte-locked to that task's own branch-point blob. The distinction carried over: this file is untouchable twice over, by a count criterion and by a byte criterion, so the precedence rider cannot land in it.
- `bin/close-out.sh:190–207` — the T-068 fast-follow `pending:` gate: an **anchored** first `grep -E` on the sub-bullet's line-start shape (so hand-off prose that merely quotes the anchor mid-line is not mistaken for a record), a literal second `grep -F`, both scoped to the closing task's own Active entry range `A_START..A_END`, refusing with `fail` (exit 1). The distinction carried over: this is a **validate-if-present content gate** — it says nothing when no such sub-bullet exists.
- `bin/close-out.sh:62–69` — the exit contract: exit 1 for a board-content refusal, exit 2 for usage/validation/resolver errors, and on any non-zero exit the board file is byte-untouched.
- `bin/check-handoff.sh:95–131` — the board-entry continuation canon: every indented non-blank line, **whatever its first character**, continues the entry and is never itself validated against `LINE_RE`; only non-indented non-blank lines are boundaries. The distinction carried over: a new indented sub-bullet grammar needs no change to the hand-off linter and cannot collide with its line format.
- `templates/prompt-blocks/registry.txt:1–29,52` — `contain` mode's exact meaning (every non-empty line of the canonical file must appear verbatim as a fixed-string substring somewhere in each consumer, and a consumer's legitimate role-specific tail lives **outside** the core), and the scope limit that the checker governs only consumers listed in the registry.
- `skills/run/SKILL.md:41` — step 1 **Plan**, where `tech-lead` produces the Routing Map.
- `skills/run/SKILL.md:84` — the T-1075 within-phase fan-out **decision point**, which today tells the orchestrator to decide, inside Validate and immediately before invoking `qa-verifier`, whether that phase's verification work is a Tier-1 fan-out candidate. The distinction carried over: this bullet currently owns the same decision the `verify` axis owns, and it explicitly disclaims the mechanics ("this decision point only names when the choice exists and what qualifies, not how the mechanics work").
- `skills/run/SKILL.md:160–179` and `:181–201` — the T-1074 fan-out step and the T-1077 `reconcile-step` region, both placed *below* the numbered phase list, with `<!-- END reconcile-step: T-1077 -->` as the last structural line before `User request:`. The distinction carried over: shipped mechanics live below the numbered list and are reached from it by a pointer, which is the placement precedent this task follows.
- `agents/tech-lead.md:4` — `tools: Read, Grep, Glob`, the line this task must leave byte-identical.
- `agents/tech-lead.md:47–71` — the Routing Map's exact output shape (a fenced block with `## Routing Map:`, `**Goal**`, `**Risk**`, `### Steps`, `### Hand-off artifacts`, `### Out of scope`), the surface a `### Dispatch` element joins.
- `agents/tech-lead.md:73–81` — the Rules section, including "Never write files" — the rule that fixes `tech-lead` as decider and never as recorder.
- `docs/loop-engineering/telemetry-instance-verification.md:70–83` — T-1082's stored derivation, which pins `skills/run/SKILL.md:177`, `skills/run/SKILL.md:197` and `templates/prompt-blocks/fanout-orchestration.md:18` as absolute line numbers. The distinction carried over: those pins are **already stale** at this task's branch point (T-1083 inserted lines above them, and its own sweep recorded T-1082 **AC12** as a genuine new red there), so this task's insertion points are chosen to avoid *creating* a new pin failure, not to repair an existing one.
- `templates/prompt-blocks/fanout-orchestration.md:10–11` — the launch-record grammar and the four `audit-only` fields. The distinction carried over: this is exactly the surface issue #319's items would edit, and this task must leave it byte-identical.
- `.shell-team/reviews/T-1083.md:64–72` — the fast-follow dispositions from that task's cross-provider review, including the round-2 Major on the launch record's missing `- agent-type:` field, dispositioned `file-an-issue` with the note "T-1084 着手前に検討" (consider before T-1084 starts). This is the primary source for #319's intent.
- `.shell-team/todo.md:1114–1137` (T-1083's Done entry) — the composition of issue #319 (agent-type field + `cap-ground` fallback token + `achieved-n`/epoch partial-launch semantics + the "already gated" wording, one editing task over the same mirror pair), the stacked-PR sprint convention, and the declaration / freeze-note / attestation sub-bullet shapes this spec's board entry follows.
- `templates/prompt-blocks/adopter-docs-declaration.md` — the `- user-visible:` declaration grammar and the exclusive `- adopter-surface:` / `- adopter-docs-waiver:` pair.
- `README.md:167–179` — the `## Operating loop` bullet list, including the T-1074/T-1083 within-phase fan-out bullet, the adopter-facing surface this task's own bullet joins.

## Goal

<!-- BEGIN intent-block: T-1084 -->

The five shipped `- dispatch-rule:` lines become a deterministic routing step the loop actually executes and leaves an auditable trace of: `tech-lead` decides each axis in its Routing Map and prints the decision there; the orchestrator transcribes each decision onto the task's own board entry as one axis-keyed sub-bullet under the grammar `- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>`, with `<value>` bound to that axis's own closed set and `<ground>` opening with a priced line id the note already records; `bin/close-out.sh` validates every such sub-bullet that is present and refuses a malformed one; and the precedence rule — apply the most specific satisfied `conditional` trigger, fall back to the axis-level `unconditional` default only when none applies — ships as a new canonical prompt block that both `agents/tech-lead.md` and `skills/run/SKILL.md` carry. This task's own board entry carries the first two real records, so the grammar has a live instance rather than only a definition.

- user-visible: yes — the change lands in two adopter-executed prompt surfaces (`agents/tech-lead.md`, `skills/run/SKILL.md`), adds a new board sub-bullet an adopter's orchestrator writes, and adds a fail-closed content gate to `bin/close-out.sh`, a script adopters run.
- verification-class: mechanism — the diff reaches `bin/`, `tests/`, `templates/` and `skills/`, and moves `bin/check-prompt-sync.sh`'s input set; **nothing is priced down**, so the downstream-impact inventory is owed as a full-population diff and CI-equivalence is owed for every step this diff can reach.
- base-ref-discriminator: git merge-base "feature/1083-agent-launch-fanout" HEAD while git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout resolves, git merge-base "develop" HEAD once it does not — the arm selected by that explicit existence test and never by a `2>/dev/null ||` chain, spelled byte-identically in every criterion that reads a base-side blob (**AC3**, **AC5**, **AC6**, **AC10**, **AC11**, **AC13**, **AC14**, **AC15**), with no 40-hex literal written into any criterion.

## Non-goals

- **No edit to `docs/loop-engineering/phase-multiplexing.md`.** It stays byte-identical to its branch-point blob. The note is locked twice over — by T-1079 **AC11**'s re-derived label totals and by T-1083 **AC18**'s byte-lock — so the precedence rider lands in a new shipped prompt block instead. Editing the two `- dispatch-note:` lines is a frozen non-goal even though the rider is conceptually a note about the rules.
- **No default-path firing of any width mechanism.** This task routes; it fires nothing. Making the shipped fan-out step actually run on the default path is T-1085's scope and this task must not pre-empt it.
- **No tier2 or tier3 enablement.** No judge role is built (issue #307), and tier3 stays measured-negative. The dispatcher routes both to the honest `serial` default rather than quietly making them reachable.
- **No peer-to-peer scheduler, negotiation protocol or agent-to-agent dispatch.** A single orchestrator deterministically applying recorded rules is the loop's structural defence; nothing here lets roles negotiate their own routing.
- **No widening of `agents/tech-lead.md`'s `tools:` line**, and no new agent definition file. `tech-lead` decides and prints; it never writes the board.
- **No depth-axis design.** Issue #274's third axis gets no value, no condition, no price and no closed set here; the grammar is axis-keyed so it can join later, and that is the whole of this task's provision for it.
- **No launch-record edits.** Every item of issue #319 — the `- agent-type:` field, the `cap-ground` fallback token, the `achieved-n`/epoch partial-launch semantics, the "already gated" wording — is deferred. `templates/prompt-blocks/fanout-orchestration.md`, `bin/aggregate-verdicts.sh`, `bin/check-fanout-instances.sh` and `docs/loop-engineering/agent-launch-fanout.md` all stay byte-identical to their branch-point blobs, because #319 is one editing task over that mirror pair and splitting it across two tasks would leave the grammar half-edited.
- **No action on issue #308's items 1, 3 or 4.** Item 1 rides the judge task, and items 3 and 4 are errata against T-1079's own file, which this task does not open. Only item 2 — the precedence rider — is absorbed here.
- **No new checker script.** The gate is an extension of `bin/close-out.sh` in the exact shape of its existing `pending:` gate; a third tool reading the board is not built.
- **No requirement that the record be present.** The shipped gate validates what is there and passes on absence, so an adopter board written before this record existed still closes out.

## Acceptance criteria

- [ ] **AC1** The record grammar ships as a new canonical prompt block that states every field, both axis-bound value sets and the closed ground vocabulary, and states the completeness requirement as norm text rather than leaving it to a script. `templates/prompt-blocks/dispatch-record.md` exists, is non-empty, carries at least eight non-empty lines, and contains verbatim: the grammar template `- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>`; the two axis keys `implement` and `verify`; both closed value sets written out as `serial|tier2|tier3` and `serial|tier1-fanout`; all four priced-ground prefixes `saving: `, `recommendation: `, `break-even: ` and `cost-input: `; the literal `at most once per entry`; the literal `dispatch-axis:` (so the axis key set is sourced from the note rather than restated as a fixed pair); and both role words `tech-lead` and `orchestrator`. Positive control: the file is asserted non-empty and its non-empty line count asserted at least eight before any literal is judged, so an empty or truncated block cannot satisfy twelve containment assertions vacuously.
  - check: rc=0; export LC_ALL=C; K=templates/prompt-blocks/dispatch-record.md; test -s "$K" || exit 1; test "$(grep -c . "$K" || true)" -ge 8 || rc=1; grep -qF -- '- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>' "$K" || rc=1; for t in 'implement' 'verify' 'serial|tier2|tier3' 'serial|tier1-fanout' 'saving: ' 'recommendation: ' 'break-even: ' 'cost-input: ' 'at most once per entry' 'dispatch-axis:' 'tech-lead' 'orchestrator'; do grep -qF -- "$t" "$K" || rc=1; done; test "$rc" -eq 0

- [ ] **AC2** The precedence rider ships verbatim in the canonical block and in **both** registered consumers, so no surface carries the routing duty without the rule that resolves a conflict between a conditional trigger and a default. The sentence `Apply the most specific conditional trigger whose stated trigger text the task actually satisfies; fall back to the axis-level unconditional default only when no conditional trigger applies.` occurs in each of `templates/prompt-blocks/dispatch-record.md`, `agents/tech-lead.md` and `skills/run/SKILL.md`. The canonical block additionally carries the literal `is not a reason to skip the axis`, so an unsatisfied conditional trigger yields a **recorded default with its ground**, never a missing record. Positive control: each of the three files is asserted non-empty before its containment is judged.
  - check: rc=0; export LC_ALL=C; S='Apply the most specific conditional trigger whose stated trigger text the task actually satisfies; fall back to the axis-level unconditional default only when no conditional trigger applies.'; for f in templates/prompt-blocks/dispatch-record.md agents/tech-lead.md skills/run/SKILL.md; do test -s "$f" || rc=1; grep -qF -- "$S" "$f" || rc=1; done; grep -qF -- 'is not a reason to skip the axis' templates/prompt-blocks/dispatch-record.md || rc=1; test "$rc" -eq 0

- [ ] **AC3** The block is registered exactly once, in `contain` mode, against exactly the two consumers, and the registry gained exactly one rule rather than replacing one. `templates/prompt-blocks/registry.txt` carries exactly one line opening `contain dispatch-record.md ` and that line matches the whole-line shape `contain dispatch-record.md <whitespace> agents/tech-lead.md <whitespace> skills/run/SKILL.md`; the HEAD count of `^contain ` lines equals the branch-point blob's own count **plus one** — a base-relative delta, no literal typed in — and the string `dispatch-record` occurs **zero** times in that branch-point blob, so this is a new registration and not a rename of an existing one. `bash bin/check-prompt-sync.sh` exits 0. Positive control: the branch-point blob is asserted non-empty with its own `^contain ` count greater than zero, so a failed read cannot satisfy the delta by comparing against nothing.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; R=templates/prompt-blocks/registry.txt; test -s "$R" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084c.XXXXXX") || exit 1; git show "$B:$R" > "$T/br" 2>/dev/null || rc=1; test -s "$T/br" || rc=1; BC=$(grep -c '^contain ' "$T/br" || true); test "$((10#$BC))" -gt 0 || rc=1; HC=$(grep -c '^contain ' "$R" || true); test "$((10#$HC))" = "$((10#$BC + 1))" || rc=1; test "$(grep -c 'dispatch-record' "$T/br" || true)" = "0" || rc=1; test "$(grep -c '^contain dispatch-record\.md ' "$R" || true)" = "1" || rc=1; grep -qE '^contain dispatch-record\.md[[:space:]]+agents/tech-lead\.md[[:space:]]+skills/run/SKILL\.md[[:space:]]*$' "$R" || rc=1; bash bin/check-prompt-sync.sh >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** The `contain`-mode mirror actually holds, re-derived independently of `bin/check-prompt-sync.sh`'s own verdict: **every** non-empty line of `templates/prompt-blocks/dispatch-record.md` occurs as a fixed-string substring in `agents/tech-lead.md` and in `skills/run/SKILL.md`. Positive control and the anti-vacuity half: the number of non-empty canonical lines actually iterated is asserted to be at least eight, so a canonical file that was emptied, or a loop that read nothing, fails here rather than satisfying a for-all over an empty set.
  - check: rc=0; export LC_ALL=C; K=templates/prompt-blocks/dispatch-record.md; test -s "$K" || exit 1; for f in agents/tech-lead.md skills/run/SKILL.md; do test -s "$f" || exit 1; done; n=0; while IFS= read -r ln; do case "$ln" in "") continue;; esac; n=$((n+1)); for f in agents/tech-lead.md skills/run/SKILL.md; do grep -qF -- "$ln" "$f" || rc=1; done; done < "$K"; test "$n" -ge 8 || rc=1; test "$rc" -eq 0

- [ ] **AC5** `tech-lead` gains the deciding duty and nothing else: its Routing Map output shape carries a `### Dispatch` element with one templated `- dispatch:` line per axis, its tool grant is unchanged, and no other agent definition moved. `agents/tech-lead.md` carries the whole line `### Dispatch` exactly once, exactly one line opening `- dispatch: implement — ` and exactly one opening `- dispatch: verify — `; its `tools:` line is **byte-identical** to the branch point's and reads exactly `tools: Read, Grep, Glob`; the file as a whole **differs** from its branch-point blob; and `git diff --name-only <branch point>...HEAD -- agents/` names exactly one path, `agents/tech-lead.md`. Positive controls: the branch-point blob and both extracted `tools:` lines are asserted non-empty before comparison, and the file is asserted to differ, so a harness that silently compared a file with itself fails here.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; A=agents/tech-lead.md; test -s "$A" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084e.XXXXXX") || exit 1; git show "$B:$A" > "$T/ba" 2>/dev/null || rc=1; test -s "$T/ba" || rc=1; cmp -s "$T/ba" "$A" && rc=1; grep -m1 '^tools: ' "$T/ba" > "$T/bt" || true; grep -m1 '^tools: ' "$A" > "$T/ht" || true; test -s "$T/bt" || rc=1; test -s "$T/ht" || rc=1; cmp -s "$T/bt" "$T/ht" || rc=1; grep -qxF -- 'tools: Read, Grep, Glob' "$A" || rc=1; test "$(grep -cxF -- '### Dispatch' "$A" || true)" = "1" || rc=1; test "$(grep -c '^- dispatch: implement — ' "$A" || true)" = "1" || rc=1; test "$(grep -c '^- dispatch: verify — ' "$A" || true)" = "1" || rc=1; git diff --name-only "$B"...HEAD -- agents/ > "$T/ag" || rc=1; test "$(grep -c . "$T/ag" || true)" = "1" || rc=1; grep -vxF -- 'agents/tech-lead.md' "$T/ag" > "$T/stray" || true; test "$(grep -c . "$T/stray" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC6** The run skill reaches the new step from the numbered list and the pre-existing decision point is reconciled rather than left duplicating it. `skills/run/SKILL.md` carries the anchor `- **Dispatch record (T-1084)**:` exactly once, and its line number lies strictly between the `1. **Plan**` line's and the `2. **Specify**` line's — both derived at check time, no literal line number typed in. The canonical block's own opening literal `Situational dispatch record (T-1084)` occurs at a line number **greater** than `<!-- END reconcile-step: T-1077 -->`'s, so the mechanics sit below the existing fan-out and reconcile regions and no line those regions' own criteria pin is shifted by this block. The bullet carrying `Within-phase fan-out decision point (T-1075)` still occurs exactly once, **differs** from its branch-point form, and now contains the literal `dispatch: verify` — so the Validate-phase bullet points at the recorded axis instead of independently deciding it. The string `dispatch-record` occurs **zero** times in the branch-point blob. Positive controls: the branch-point blob is asserted non-empty; all four derived line numbers are asserted non-empty before they are compared; and both extracted T-1075 bullet lines are asserted non-empty before the difference is judged.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; S=skills/run/SKILL.md; test -s "$S" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084f.XXXXXX") || exit 1; git show "$B:$S" > "$T/bs" 2>/dev/null || rc=1; test -s "$T/bs" || rc=1; test "$(grep -c 'dispatch-record' "$T/bs" || true)" = "0" || rc=1; test "$(grep -cF -- '- **Dispatch record (T-1084)**:' "$S" || true)" = "1" || rc=1; P=$(grep -n '^1\. \*\*Plan\*\*' "$S" | head -n1 | cut -d: -f1); Q=$(grep -n '^2\. \*\*Specify\*\*' "$S" | head -n1 | cut -d: -f1); D=$(grep -nF -- '- **Dispatch record (T-1084)**:' "$S" | head -n1 | cut -d: -f1); E=$(grep -n '^<!-- END reconcile-step: T-1077 -->$' "$S" | head -n1 | cut -d: -f1); F=$(grep -nF -- 'Situational dispatch record (T-1084)' "$S" | head -n1 | cut -d: -f1); for v in "$P" "$Q" "$D" "$E" "$F"; do test -n "$v" || rc=1; done; test -n "$P" && test -n "$D" && { test "$((10#$P))" -lt "$((10#$D))" || rc=1; }; test -n "$D" && test -n "$Q" && { test "$((10#$D))" -lt "$((10#$Q))" || rc=1; }; test -n "$E" && test -n "$F" && { test "$((10#$E))" -lt "$((10#$F))" || rc=1; }; test "$(grep -cF -- 'Within-phase fan-out decision point (T-1075)' "$S" || true)" = "1" || rc=1; grep -m1 -F -- 'Within-phase fan-out decision point (T-1075)' "$T/bs" > "$T/b1" || true; grep -m1 -F -- 'Within-phase fan-out decision point (T-1075)' "$S" > "$T/h1" || true; test -s "$T/b1" || rc=1; test -s "$T/h1" || rc=1; cmp -s "$T/b1" "$T/h1" && rc=1; grep -qF -- 'dispatch: verify' "$T/h1" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** `bin/close-out.sh` carries a fail-closed **validate-if-present** content gate over the closing task's own Active entry, in the `pending:` gate's shape: it refuses a malformed record with exit 1 and a stderr message naming it, and says nothing at all when the record is absent, partial or merely quoted in prose. Measured by running the real script against nine synthesized boards in a scratch directory, with `$TEAM_TODO`, `$TEAM_INTERVENTIONS_DIR` and `$TEAM_RUNS_DIR` redirected so the repository's own board is never read or written. The five **refusal** cases each exit **1** (not 2 — a usage or resolver error would prove the gate was never reached) and each write the literal `malformed dispatch record` to stderr: a value from another axis's set (`verify — tier2`); a duplicated axis; an axis key outside the note's own set (`depth`); a modality outside `unconditional|conditional`; and a ground opening with none of the four priced-line prefixes. The four **silent** cases write that literal nowhere: a conformant two-axis record; an entry with no `- dispatch:` sub-bullet at all (adopter back-compat); an entry carrying only the `implement` axis (a partial record is not the gate's business — completeness is norm text, and a future third axis must never retroactively refuse an in-flight task's close-out); and an entry whose prose sub-bullet quotes a malformed record **mid-line**, which the anchored line-start match must not mistake for one. Positive controls: exactly nine cases are asserted to have run, and the first refusal case's stderr is asserted non-empty, so a script that produced no output at all cannot satisfy four absence assertions vacuously.
  - check: rc=0; export LC_ALL=C; C=bin/close-out.sh; test -s "$C" || exit 1; EM=$(printf '\342\200\224'); BT=$(printf '\140'); T=$(mktemp -d "${TMPDIR:-/tmp}/t1084g.XXXXXX") || exit 1; mkdir -p "$T/iv" "$T/runs" || exit 1; printf '<!-- BEGIN interventions: T-000 -->\nno interventions occurred\n<!-- END interventions: T-000 -->\n' > "$T/iv/T-000.md" || exit 1; i=0; run() { i=$((i+1)); printf '# Tasks\n\n## Active\n\n- [ ] **T-000** Fixture %s %sREADY_FOR_MERGE%s %s spec: docs/specs/fixture.md\n%b\n\n## Done\n' "$EM" "$BT" "$BT" "$EM" "$1" > "$T/b$i.md"; TEAM_TODO="$T/b$i.md" TEAM_INTERVENTIONS_DIR="$T/iv" TEAM_RUNS_DIR="$T/runs" bash "$C" --task T-000 --date 2026-08-19 > "$T/o$i" 2> "$T/e$i"; printf '%s' "$?" > "$T/rc$i"; }; OKI="  - dispatch: implement $EM serial $EM unconditional $EM recommendation: tier2-parallel-implementations-judge"; OKV="  - dispatch: verify $EM serial $EM unconditional $EM recommendation: tier1-verification-fanout"; run "$OKI\n$OKV"; run "  - source: fixture"; run "$OKI"; run "  - note: quoting ${BT}- dispatch: verify $EM tier2 $EM unconditional $EM x${BT} inline"; run "$OKI\n  - dispatch: verify $EM tier2 $EM unconditional $EM recommendation: tier1-verification-fanout"; run "$OKI\n$OKI"; run "$OKI\n  - dispatch: depth $EM serial $EM unconditional $EM recommendation: tier1-verification-fanout"; run "$OKI\n  - dispatch: verify $EM serial $EM maybe $EM recommendation: tier1-verification-fanout"; run "$OKI\n  - dispatch: verify $EM serial $EM unconditional $EM because I said so"; test "$i" = "9" || rc=1; TOK='malformed dispatch record'; for k in 1 2 3 4; do test -f "$T/e$k" || rc=1; if grep -qF -- "$TOK" "$T/e$k"; then rc=1; fi; done; for k in 5 6 7 8 9; do test -f "$T/e$k" || rc=1; grep -qF -- "$TOK" "$T/e$k" || rc=1; test "$(cat "$T/rc$k" 2>/dev/null)" = "1" || rc=1; done; test -s "$T/e5" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** The gate is covered by the shipped suite rather than only by this spec's own criteria. `bash tests/close-out/run.sh` exits 0, its output carries at least one `PASS:` line and at least **six** lines matching `^PASS: T-1084`, and zero lines matching `^FAIL`. Positive control: the log is asserted non-empty and its total `PASS` count asserted at least one before the two class counts are judged, so a suite that produced no output cannot satisfy a zero-failure assertion vacuously.
  - check: rc=0; export LC_ALL=C; test -s tests/close-out/run.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084h.XXXXXX") || exit 1; bash tests/close-out/run.sh > "$T/log" 2>&1 || rc=1; test -s "$T/log" || rc=1; test "$(grep -c '^PASS' "$T/log" || true)" -ge 1 || rc=1; test "$(grep -c '^PASS: T-1084' "$T/log" || true)" -ge 6 || rc=1; test "$(grep -c '^FAIL' "$T/log" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC9** The grammar has a live instance: this task's own board entry carries a real record for each axis, and each record's ground resolves to a line the note actually contains. Within the extent of the `**T-1084**` top-level entry on the resolved board — bounded the way `bin/check-handoff.sh`'s continuation canon defines it, at the next non-indented non-blank line — there are exactly **two** lines matching `^[[:space:]]*- dispatch: `; exactly one matches the `implement` axis against `serial|tier2|tier3` and exactly one matches the `verify` axis against `serial|tier1-fanout`, each with a modality from `unconditional|conditional` and a non-empty ground. For **each** of the two, the ground field's leading priced-line id is extracted and the note is required to carry at least one line opening `- <label>: <id> — `, so a routing choice cites a price that exists rather than an invented one. Positive controls: the board path resolves readable, the extracted entry is asserted non-empty, the note is asserted non-empty, and the number of grounds actually resolved is asserted to equal two, so a failed extraction cannot satisfy the grounding requirement over an empty set.
  - check: rc=0; export LC_ALL=C; TODO=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$TODO" || exit 1; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084i.XXXXXX") || exit 1; awk '/^- \[[x ]\] \*\*T-1084\*\* /{f=1;next} f&&/^[^[:space:]]/{exit} f' "$TODO" > "$T/entry"; test -s "$T/entry" || rc=1; grep -E '^[[:space:]]*- dispatch: ' "$T/entry" > "$T/d" || true; test "$(grep -c . "$T/d" || true)" = "2" || rc=1; test "$(grep -cE '^[[:space:]]*- dispatch: implement — (serial|tier2|tier3) — (unconditional|conditional) — .' "$T/d" || true)" = "1" || rc=1; test "$(grep -cE '^[[:space:]]*- dispatch: verify — (serial|tier1-fanout) — (unconditional|conditional) — .' "$T/d" || true)" = "1" || rc=1; g=0; while IFS= read -r ln; do test -n "$ln" || continue; pair=$(printf '%s\n' "$ln" | sed -nE 's/^[[:space:]]*- dispatch: [a-z0-9-]+ — [a-z0-9-]+ — [a-z]+ — (saving|recommendation|break-even|cost-input): ([a-z0-9-]+).*$/\1 \2/p'); test -n "$pair" || { rc=1; continue; }; lab=${pair%% *}; val=${pair##* }; test -n "$lab" || { rc=1; continue; }; test -n "$val" || { rc=1; continue; }; test "$(grep -c "^- $lab: $val — " "$N" || true)" -ge 1 || rc=1; g=$((g+1)); done < "$T/d"; test "$g" = "2" || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: issue #274's depth axis is added as a third `- dispatch-axis:` line in the note, at which point a conformant entry carries three records and this criterion's exactly-two count no longer describes a complete record.

- [ ] **AC10** The priced note is untouched, proven byte for byte and by its own label cardinalities. `docs/loop-engineering/phase-multiplexing.md` is **byte-identical** to its branch-point blob, and each of `- dispatch-axis: `, `- dispatch-rule: ` and `- dispatch-note: ` occurs at HEAD exactly as many times as in that blob — both sides derived at check time, no literal count typed in. Positive control and the anti-vacuity half: the branch-point blob is asserted non-empty and each of the three base-side counts is asserted greater than zero before the equalities are compared, so a failed read cannot satisfy three equalities by comparing zero against zero.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; N=docs/loop-engineering/phase-multiplexing.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084j.XXXXXX") || exit 1; git show "$B:$N" > "$T/bn" 2>/dev/null || rc=1; test -s "$T/bn" || rc=1; cmp -s "$T/bn" "$N" || rc=1; for L in '^- dispatch-axis: ' '^- dispatch-rule: ' '^- dispatch-note: '; do x=$(grep -c "$L" "$T/bn" || true); test "$((10#$x))" -gt 0 || rc=1; y=$(grep -c "$L" "$N" || true); test "$((10#$x))" = "$((10#$y))" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC11** Issue #319's items are deferred rather than half-landed, and the executing surface this task touches is exactly one script. Each of `templates/prompt-blocks/fanout-orchestration.md`, `bin/aggregate-verdicts.sh`, `bin/check-fanout-instances.sh` and `docs/loop-engineering/agent-launch-fanout.md` is **byte-identical** to its branch-point blob; the string `- agent-type:` occurs **zero** times in `templates/prompt-blocks/fanout-orchestration.md` (the round-2 Major's field, not smuggled in ahead of #319); and `git diff --name-only <branch point>...HEAD -- bin/` names nothing except `bin/close-out.sh`. Positive controls: each branch-point blob and each working-tree file is asserted non-empty before its comparison, the iteration is asserted to have reached all four paths, and the `bin/` diff is asserted to name `bin/close-out.sh` itself — so a diff that named nothing at all fails here rather than satisfying the allow-list by being empty.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084k.XXXXXX") || exit 1; i=0; for f in templates/prompt-blocks/fanout-orchestration.md bin/aggregate-verdicts.sh bin/check-fanout-instances.sh docs/loop-engineering/agent-launch-fanout.md; do i=$((i+1)); test -s "$f" || rc=1; git show "$B:$f" > "$T/b$i" 2>/dev/null || rc=1; test -s "$T/b$i" || rc=1; cmp -s "$T/b$i" "$f" || rc=1; done; test "$i" = "4" || rc=1; test "$(grep -c -- '- agent-type:' templates/prompt-blocks/fanout-orchestration.md || true)" = "0" || rc=1; git diff --name-only "$B"...HEAD -- bin/ > "$T/bd" || rc=1; grep -qxF -- 'bin/close-out.sh' "$T/bd" || rc=1; grep -vxF -- 'bin/close-out.sh' "$T/bd" > "$T/stray" || true; test "$(grep -c . "$T/stray" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** This spec carries the three declarations the shipped freeze self-check requires, in the conformant shape for a `yes` user-visibility ruling. Inside the region from this spec's `<!-- BEGIN intent-block: T-1084 -->` marker to its `## Non-goals` heading there is exactly one `- user-visible: yes — ` line, exactly one `- verification-class: mechanism — ` line and exactly one `- base-ref-discriminator: ` line, each with a non-empty value; each of those three keys occurs exactly once in the **whole** spec, so a second copy outside the region cannot disagree with the frozen one; exactly one indented `- adopter-surface: ` line exists anywhere in the spec; and no line-initial `- adopter-docs-waiver:` marker exists, because the two are exclusive. Positive control: the extracted declaration region is asserted non-empty before it is parsed, so a missing or renamed marker fails here rather than satisfying an absence assertion vacuously.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1084-dispatch-routing-record.md; test -s "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084l.XXXXXX") || exit 1; awk '/^<!-- BEGIN intent-block: T-1084 -->$/{f=1;next} f&&/^## Non-goals$/{exit} f' "$SP" > "$T/decl"; test -s "$T/decl" || rc=1; test "$(grep -c '^- user-visible: yes — .' "$T/decl" || true)" = "1" || rc=1; test "$(grep -c '^- verification-class: mechanism — .' "$T/decl" || true)" = "1" || rc=1; test "$(grep -c '^- base-ref-discriminator: .' "$T/decl" || true)" = "1" || rc=1; test "$(grep -c '^- user-visible: ' "$SP" || true)" = "1" || rc=1; test "$(grep -c '^- verification-class: ' "$SP" || true)" = "1" || rc=1; test "$(grep -c '^- base-ref-discriminator: ' "$SP" || true)" = "1" || rc=1; test "$(grep -c '^[[:space:]][[:space:]]*- adopter-surface: .' "$SP" || true)" = "1" || rc=1; test "$(grep -c '^- adopter-docs-waiver:' "$SP" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC13** The adopter-facing documentation lands in the same task as the capability, in both README languages. `README.md` and `README.ja.md` each contain the grammar template literal `- dispatch: <axis>` at HEAD, each contains it **zero** times in its own branch-point blob, and each **differs** from that blob — so the bullet was added here rather than inherited. Positive controls: both files and both branch-point blobs are asserted non-empty before any containment or difference is judged, and the iteration is asserted to have reached both files.
  - adopter-surface: a new bullet in `README.md`'s `## Operating loop` list, immediately after the within-phase fan-out bullet, plus its `README.ja.md` mirror — the same pair the fan-out capability's own adopter surface used.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084m.XXXXXX") || exit 1; i=0; for f in README.md README.ja.md; do i=$((i+1)); test -s "$f" || rc=1; git show "$B:$f" > "$T/b$i" 2>/dev/null || rc=1; test -s "$T/b$i" || rc=1; grep -qF -- '- dispatch: <axis>' "$f" || rc=1; test "$(grep -cF -- '- dispatch: <axis>' "$T/b$i" || true)" = "0" || rc=1; cmp -s "$T/b$i" "$f" && rc=1; done; test "$i" = "2" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC14** The task's records, board line and hygiene gates are conformant, and the board gained exactly this task's one entry without losing an existing one. The resolved board carries exactly one more top-level `- [` entry line at HEAD than in the branch point's committed blob — a base-relative delta, not a declared total — and one of them is this task's `**T-1084**` entry in either checkbox state. `bash bin/check-handoff.sh <the resolved board>` exits 0; `bash bin/check-board-headings.sh <the resolved board> --base <branch point>` exits 0; `bash bin/check-pii-shapes.sh --base <branch point>` exits 0; `bash bin/check-provenance.sh .shell-team/provenance/T-1084.md` exits 0; `bash bin/check-interventions.sh --task T-1084 -- .shell-team/interventions/T-1084.md` exits 0; and `bash bin/check-intent.sh .shell-team/specs/T-1084-dispatch-routing-record.md <the resolved board>` exits 0, which is what ties the frozen block above to the hash recorded on the board. Positive controls: the board path resolves readable, the base board blob is asserted non-empty with its own entry count greater than zero, and every named record file is asserted non-empty before its checker runs, so a missing record fails here rather than being skipped.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; TODO=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$TODO" || exit 1; SP=.shell-team/specs/T-1084-dispatch-routing-record.md; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084n.XXXXXX") || exit 1; git show "$B:$TODO" > "$T/base" 2>/dev/null || rc=1; test -s "$T/base" || rc=1; BC=$(grep -c '^- \[' "$T/base" || true); test "$((10#$BC))" -gt 0 || rc=1; HC=$(grep -c '^- \[' "$TODO" || true); test "$((10#$HC))" = "$((10#$BC + 1))" || rc=1; grep -qE '^- \[[x ]\] \*\*T-1084\*\* ' "$TODO" || rc=1; for f in "$SP" .shell-team/provenance/T-1084.md .shell-team/interventions/T-1084.md; do test -s "$f" || rc=1; done; bash bin/check-handoff.sh "$TODO" >/dev/null 2>&1 || rc=1; bash bin/check-board-headings.sh "$TODO" --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-pii-shapes.sh --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-provenance.sh .shell-team/provenance/T-1084.md >/dev/null 2>&1 || rc=1; bash bin/check-interventions.sh --task T-1084 -- .shell-team/interventions/T-1084.md >/dev/null 2>&1 || rc=1; bash bin/check-intent.sh "$SP" "$TODO" >/dev/null 2>&1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC15** The changed set is confined to this task's allow-list, on both its tracked and its untracked half. The union of `git diff --name-only <branch point>...HEAD` and `git ls-files --others --exclude-standard` contains nothing outside: `templates/prompt-blocks/dispatch-record.md`, `templates/prompt-blocks/registry.txt`, `agents/tech-lead.md`, `skills/run/SKILL.md`, `bin/close-out.sh`, `tests/close-out/run.sh`, `README.md`, `README.ja.md`, `.shell-team/specs/T-1084-dispatch-routing-record.md`, the resolved board, `.shell-team/test-recipe.md`, `.shell-team/provenance/T-1084.md`, `.shell-team/interventions/T-1084.md` and `.shell-team/reviews/T-1084*.md`. The untracked half is taken from `git ls-files --others --exclude-standard` rather than from a working-tree diff, because a commit range cannot see a stray file that was never added — and it is also what would catch a scratch fixture board written beside the checkout instead of in `$TMPDIR`. **This criterion is merge-point-scoped and is expected to go stale after merge**: once a later task's files land on the same base ref, the expected set no longer describes reality — that is the criterion doing its job at the moment it was authored, not a defect to chase, and it must not be merge-ranged, re-based per rework round or otherwise kept evergreen. Positive control: the derived union is asserted non-empty, so an empty diff cannot satisfy the allow-list by containing nothing.
  - check: rc=0; export LC_ALL=C; B=$(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base "feature/1083-agent-launch-fanout" HEAD; else git merge-base "develop" HEAD; fi) || exit 1; test -n "$B" || exit 1; TODO=$(bash bin/team-paths.sh --get todo) || exit 1; test -n "$TODO" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084o.XXXXXX") || exit 1; { git diff --name-only "$B"...HEAD; git ls-files --others --exclude-standard; } | LC_ALL=C sort -u > "$T/all" || rc=1; test -s "$T/all" || rc=1; grep -vE "^(templates/prompt-blocks/(dispatch-record\.md|registry\.txt)|agents/tech-lead\.md|skills/run/SKILL\.md|bin/close-out\.sh|tests/close-out/run\.sh|README\.md|README\.ja\.md|\.shell-team/specs/T-1084-dispatch-routing-record\.md|$TODO|\.shell-team/test-recipe\.md|\.shell-team/(provenance|interventions)/T-1084\.md|\.shell-team/reviews/T-1084[^/]*\.md)$" "$T/all" > "$T/stray" || true; test "$(grep -c . "$T/stray" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC16** The downstream impact is disclosed as a produced table rather than a claim, and the population it was taken over is a tool's output rather than a hand count. This spec carries a `## Blast radius` section containing at least one `- predicted-red: ` line, at least one `<!-- BEGIN derivation: <label> -->` block, an equal number of `<!-- END derivation: <label> -->` lines and an equal number of `- reproduce: <command>` lines; every `- reproduce:` command, run from the repository root, exits 0, produces non-empty output, and is **byte-identical** to the block in that section carrying the label its own `BEGIN` line names. Positive controls: the extracted section is asserted non-empty; the number of reproduce commands actually executed is asserted at least one; and each regenerated output and each extracted block is asserted non-empty before comparison.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1084-dispatch-routing-record.md; test -s "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1084p.XXXXXX") || exit 1; awk '/^## Blast radius$/{f=1;next} f&&/^## /{exit} f' "$SP" > "$T/br"; test -s "$T/br" || rc=1; test "$(grep -c '^- predicted-red: ' "$T/br" || true)" -ge 1 || rc=1; nb=$(grep -c '^<!-- BEGIN derivation: ' "$T/br" || true); ne=$(grep -c '^<!-- END derivation: ' "$T/br" || true); nr=$(grep -c '^- reproduce: ' "$T/br" || true); test "$((10#$nb))" -ge 1 || rc=1; test "$((10#$nb))" = "$((10#$ne))" || rc=1; test "$((10#$nr))" = "$((10#$nb))" || rc=1; sed -n 's/^- reproduce: //p' "$T/br" > "$T/rep"; i=0; while IFS= read -r cmd; do test -n "$cmd" || continue; i=$((i+1)); eval "$cmd" > "$T/o$i" 2>/dev/null || rc=1; test -s "$T/o$i" || { rc=1; continue; }; lab=$(head -n 1 "$T/o$i" | sed -nE 's/^<!-- BEGIN derivation: (.*) -->$/\1/p'); test -n "$lab" || { rc=1; continue; }; awk -v b="<!-- BEGIN derivation: $lab -->" -v e="<!-- END derivation: $lab -->" '$0==b{f=1} f{print} f&&$0==e{exit}' "$T/br" > "$T/w$i"; test -s "$T/w$i" || { rc=1; continue; }; cmp -s "$T/o$i" "$T/w$i" || rc=1; done < "$T/rep"; test "$i" -ge 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC17** The runtime verification this task owes is reported item by item, each item naming its owner, rather than summarized. This criterion carries **no** `- check:` line and is `SKIP` by design; it is discharged in the hand-off, one paragraph per item, never as a compressed sentence. (a) **Mutation self-check**, engineer — every criterion carrying a `- check:` line is probed on a scratch worktree **outside** the real checkout, each observed red, restored, and observed green again, and each probe reported separately; for **AC7** the probes must include one aimed at the gate's *anchoring* (make the anchored first match unanchored and confirm case 4, the mid-line prose quote, flips red) and one aimed at its *wiring* (make the refusal print without changing the exit status and confirm the exit assertion flips red), because a population-exception control is vacuous in two independent ways. (b) **Execution-context matrix**, engineer — every command this task's shipped text tells a reader to run, executed in three cells: this repository's checkout root without the plugin on `PATH`; an adopter-shaped repository on the default `.shell-team/` layout with the plugin loaded and the scripts reached by bare name; and an adopter-shaped repository on the legacy `tasks/` + `docs/specs/` layout. (c) **CI-equivalence**, engineer — the full `- name:` step list of `.github/workflows/check-handoff.yml` run locally in workflow order, each step's result reported, and any step judged inapplicable named individually with its own reason; nothing is priced down here, per the `mechanism` declaration. (d) **Full-population downstream-impact diff**, engineer — every merged spec's criteria run at the branch point's **committed blobs** (through a scratch worktree, never the working tree) and again at HEAD, differenced criterion by criterion, with the enumerating command stated beside the table and the population re-derived at run time; a `grep`-of-mentions inventory is not an acceptable substitute, and criteria that reach their target through indirection (a path built from `bin/team-paths.sh --get todo`, a file named only through a variable) are run rather than searched for. (e) **`shellcheck`**, engineer — `bin/close-out.sh` and `tests/close-out/run.sh` clean at the version CI pins. (f) **Relayed-premise re-measurement**, coordinating session — every row of `## Assumptions` marked *relayed* measured at its primary source and the measured value recorded beside it. (g) **Borrowed-vocabulary measurement**, coordinating session — every row of the `## Assumptions` count-premise table run against the **branch point's committed blob** with the command written beside it, and the measured value recorded in that table's `Measured` column.

## Input space

**Reachable input classes** — what this grammar, this gate and these criteria really encounter, and which the implementation must handle correctly:

- A Routing Map that prints no dispatch decision at all (a task `tech-lead` judged trivial, or a map written before this task shipped). The board entry then carries no `- dispatch:` sub-bullet, the shipped gate says nothing, and close-out succeeds — the record's presence is the norm text's requirement, not the script's.
- A record naming a value outside its own axis's closed set — most reachably `verify — tier2`, because the two axes share the value `serial` and an author transposing rows is the obvious slip. Refused.
- A record naming an axis key the note does not define (`depth`, ahead of issue #274). Refused today; it becomes conformant on the day the note defines that axis, with no change to the grammar.
- A record repeating an axis, typically from an edit that appended a corrected line instead of replacing the wrong one. Refused, because two records for one axis leave no fact about what was chosen.
- A **partial** record — one axis present, another missing — from an author who decided one axis and forgot the other. Grammar-conformant and not refused: completeness is norm text, and a future third axis must never retroactively refuse the close-out of a task recorded under the two-axis dispatcher.
- A choice whose conditional trigger text is examined and found **not** satisfied. The precedence rider makes this a recorded `unconditional` default carrying the ground that made it the default — never a skipped axis, and never a `conditional` modality on a rule that did not fire.
- A ground field that carries explanatory prose after the priced-line id, and a ground field carrying an em dash inside that prose. The last field is free text, so the grammar's separator appearing inside it must not truncate it.
- A hand-off sub-bullet or narrative line that **quotes** the anchor `- dispatch:` mid-line, inside backticks, while discussing the grammar — including this very task's own board entry and review record. The gate's first match is anchored to the sub-bullet's line-start shape, exactly as the `pending:` gate's is, so a quotation is not a record; correspondingly, an indented line whose first non-space characters are `- dispatch: ` **is** a record wherever it sits in the entry, and prose that wants to mention the grammar quotes it mid-line.
- Both supported layouts: the default `.shell-team/` layout and the legacy `tasks/` + `docs/specs/` layout, with every operating path resolved through `bin/team-paths.sh` and none hardcoded.
- An adopter board on which **no** entry, past or present, carries a dispatch record — the ordinary state of every repository that adopts this plugin after the fact.
- An entry whose sub-bullets are indented with a tab rather than spaces, which `bin/check-handoff.sh`'s continuation canon already accepts.
- A board carrying CRLF line endings, which the existing board tooling already tolerates.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect, because real data cannot produce them:

- A board entry carrying tens of synthetic axis keys, or thousands of `- dispatch:` lines, to probe the gate's scan cost. The population is bounded by the number of axes the note defines, which is two today and three after #274.
- Unicode look-alike separators substituted for the em dash (en dash, hyphen-minus runs, non-breaking hyphen). The separator is the em dash the whole board already uses; a look-alike is a malformed record and the gate is free to refuse it or to report it as an unrecognised axis, without a criterion fixing which.
- A ground field megabytes long, or one carrying NUL bytes, control characters or terminal escape sequences.
- A board whose encoding is neither UTF-8 nor the CR-tolerant variant the existing tooling handles.
- A `- dispatch:` line placed inside a fenced code block **within a board entry**, to exploit the fact that the gate reads a line range rather than parsing markdown. Fenced illustrative examples belong in specs and docs, not inside a board entry's extent; the gate deliberately does not learn markdown fences.
- Adversarial nesting of one task's entry inside another's, or a board with no `## Active`/`## Done` structure at all — refusals `bin/close-out.sh` already owns, unchanged by this task.

<!-- END intent-block: T-1084 -->

## Assumptions

Every count premise below is classified `own-coinage` (a literal this task introduces) or `borrowed` (vocabulary another document already coined). The `Measured` column is filled by the freeze run, which has a shell this role does not: each command is run against the **branch point's committed blob**, and a measured value contradicting the premise is a hand-off finding that routes back here, never a silent correction.

| # | Token | Class | Premise at the branch point | Measurement command | Measured |
|---|---|---|---|---|---|
| 1 | `- dispatch: ` (the sub-bullet grammar) | own-coinage | 0 occurrences anywhere | `git grep -c -- '^[[:space:]]*- dispatch: ' <B> -- . ; echo rc=$?` | Measured 2026-08-19 at `012bd9c`: no match (grep exit 1) — 0, premise holds |
| 2 | `- dispatch-rule: ` | borrowed (T-1079) | exactly 5, all in `docs/loop-engineering/phase-multiplexing.md` | `git grep -c -- '^- dispatch-rule: ' <B> -- .` | Measured: 5, all in that one file — premise holds |
| 3 | `- dispatch-note: ` | borrowed (T-1079) | exactly 2, same file | `git grep -c -- '^- dispatch-note: ' <B> -- .` | Measured: 2, same file — premise holds |
| 4 | `- dispatch-axis: ` | borrowed (T-1079) | exactly 2, same file | `git grep -c -- '^- dispatch-axis: ' <B> -- .` | Measured: 2, same file — premise holds |
| 5 | `serial`, `tier2`, `tier3`, `tier1-fanout` (the five axis values) | borrowed (T-1079) | each occurs on the note's own `- dispatch-axis:`/`- dispatch-rule:` lines; **no criterion here depends on their global frequency**, which is why this row records the axis-line occurrence rather than a repo-wide count | `for v in serial tier2 tier3 tier1-fanout; do printf '%s ' "$v"; git grep -c -- "^- dispatch-rule: .*$v" <B> -- docs/loop-engineering/phase-multiplexing.md; done` | Measured (rule-line occurrences): serial=2, tier2=2, tier3=2, tier1-fanout=1 — every value present on at least one rule line, premise holds |
| 6 | `dispatch-record` (the block name) | own-coinage | 0 occurrences anywhere | `git grep -c -- 'dispatch-record' <B> -- .` | Measured: no match (exit 1) — 0, premise holds |
| 7 | `malformed dispatch record` (the refusal token) | own-coinage | 0 occurrences anywhere | `git grep -c -- 'malformed dispatch record' <B> -- .` | Measured: no match (exit 1) — 0, premise holds |
| 8 | `### Dispatch` | own-coinage | 0 occurrences in `agents/` | `git grep -c -- '^### Dispatch$' <B> -- agents` | Measured: no match (exit 1) — 0, premise holds |
| 9 | `- **Dispatch record (T-1084)**:` | own-coinage | 0 occurrences anywhere | `git grep -c -F -- '- **Dispatch record (T-1084)**:' <B> -- .` | Measured: no match (exit 1) — 0, premise holds |
| 10 | `^contain ` in `templates/prompt-blocks/registry.txt` | borrowed (T-039) | greater than zero; **AC3** asserts a `+1` delta rather than a total | `git show <B>:templates/prompt-blocks/registry.txt \| grep -c '^contain '` | Measured: 16 — premise holds |
| 11 | `- fast-follow disposition (` anchored gate shape in `bin/close-out.sh` | borrowed (T-068) | exactly one anchored `grep -E` / literal `grep -F` pair, the shape this task's gate mirrors | `git show <B>:bin/close-out.sh \| grep -c -- '- fast-follow disposition ('` | Measured: 1 — premise holds |
| 12 | `- agent-type:` in `templates/prompt-blocks/fanout-orchestration.md` | borrowed (#319 / T-1083 review round 2) | 0 — the field is issue #319's, not this task's | `git show <B>:templates/prompt-blocks/fanout-orchestration.md \| grep -c -- '- agent-type:'` | Measured: 0 (grep exit 1) — premise holds |
| 13 | `- adopter-surface: ` | borrowed (T-1061) | this spec carries exactly one, under **AC13** | `grep -c '^[[:space:]][[:space:]]*- adopter-surface: ' .shell-team/specs/T-1084-dispatch-routing-record.md` | Measured: 1 — premise holds |
| 14 | `- [` top-level entries on the resolved board | borrowed (board canon) | greater than zero; **AC14** asserts a `+1` delta rather than a total | `git show <B>:$(bash bin/team-paths.sh --get todo) \| grep -c '^- \['` | Measured: 87 at the branch point — premise holds |
| 15 | `- invariant-lock: ` in `skills/run/SKILL.md` and `templates/prompt-blocks/` | borrowed (T-1069/T-1074/T-1077) | this task adds none and removes none; the count is unchanged at HEAD | `git grep -c -- '^- invariant-lock: ' <B> -- skills templates` | Measured: SKILL.md=14, fanout-orchestration.md=7 — the baseline the unchanged-at-HEAD premise is judged against |

`<B>` above is the branch point this spec's `- base-ref-discriminator:` declaration resolves.

**Relayed premises** — reached this role from the coordinating layer rather than from an artefact it can open. `pm-spec` holds no shell and no GitHub-reading tool, so each is labelled relayed here and the freeze run measures it and records the value beside this line:

- **relayed** — PR **#323** (T-1083) is open with CI green at tip `012bd9c`. Primary confirmation: the coordinating session's own live GitHub read plus `git rev-parse`. Nothing in this spec's criteria depends on it; it grounds only the stacking decision. **Measured at the freeze (2026-08-19): PR #323 created and pushed by the coordinating session itself; its CI check run `check-handoff lint` = success (completed 2026-08-19T04:34Z) on head `012bd9c`; `git rev-parse feature/1083-agent-launch-fanout` = `012bd9c…` = the branch point.**
- **relayed** — the predecessor branch `feature/1083-agent-launch-fanout` resolves in this checkout as a **local** branch (`refs/heads/`). Every predecessor in this sprint's train has measured that way — T-1083's own board entry records its predecessor measured in `refs/heads/` — and the declared existence test names that namespace accordingly. If the freeze run measures it resolving only as a remote-tracking ref, the declaration and all eight base-reading criteria need a meaning-preserving correction to `refs/remotes/<remote>/feature/1083-agent-launch-fanout` before the freeze; if it resolves in **neither** namespace while its PR is still open, that is a fetch-depth gap and a route-back, not the fallback arm firing. **Measured at the freeze: `git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout` succeeds — local branch, exactly as declared.**
- **relayed** — the branch point differs from `develop`'s tip (the stacked-PR train is not yet merged). **Measured at the freeze: branch point `012bd9c…` ≠ `develop` tip `9b9f79a…`.**
- **relayed** — issue **#308**'s body and its four items, and specifically that item 2 is the precedence rider absorbed here while items 1, 3 and 4 are deferred for the reasons the Non-goals state. This number appears nowhere in this repository at the branch point, so it could not be re-measured from any file; the substance absorbed is a single sentence, reproduced verbatim in **AC2**, and no criterion here reads the issue. **Measured at the freeze (2026-08-19, GitHub API): state=open; four items exactly as summarized; item 2's own wording — "apply the most specific `conditional` trigger; fall back to the `unconditional` default only when none applies" — is the sentence AC2's literal preserves in substance.**
- **relayed** — issue **#319**'s state (open) and that it is one editing task covering all four launch-record refinements. Its *composition* was re-measured first-hand from `.shell-team/todo.md:1132` and `.shell-team/reviews/T-1083.md:64–72`; only its live state is relayed. **Measured at the freeze: open (filed 2026-08-19 by the coordinating session itself; no state change since).**
- **relayed** — issue **#307**'s state (the judge is absent). Nothing here depends on it beyond the Non-goal's stated reason. **Measured at the freeze (2026-08-19, GitHub API): state=open; its body confirms the judge briefing and committed subject harness do not exist yet — the judge is absent, exactly as the Non-goal's reason states.**

## Body-to-AC correspondence

| # | Body directive | Where stated | Promoted to |
|---|---|---|---|
| 1 | The record grammar is `- dispatch: <axis> — <value> — <unconditional\|conditional> — <ground>` | Goal | **AC1**, **AC9** |
| 2 | `<value>` is bound to its own axis's closed set | Goal, Input space | **AC1**, **AC7**, **AC9** |
| 3 | `<ground>` opens with a priced line id the note records | Goal | **AC1**, **AC7**, **AC9** |
| 4 | One record per axis, axis-keyed so a third axis joins without grammar breakage | Goal, Non-goals | **AC1** (norm text), **AC9** (live instance), **AC7** (a partial record is not refused) |
| 5 | `tech-lead` decides and prints; the orchestrator records | Goal | **AC1**, **AC5**, **AC6** |
| 6 | The precedence rider ships verbatim-in-substance | Goal | **AC2** |
| 7 | An unsatisfied conditional trigger yields a recorded default, not a skipped axis | Goal, Input space | **AC2** |
| 8 | The rider lands in a new prompt block, `contain` mode, two consumers | Goal, Non-goals | **AC1**, **AC3**, **AC4** |
| 9 | `bin/close-out.sh` validates if present and refuses a malformed record | Goal | **AC7**, **AC8** |
| 10 | Absence passes, for adopter back-compat | Non-goals, Input space | **AC7** (cases 2 and 3) |
| 11 | `docs/loop-engineering/phase-multiplexing.md` is untouched | Non-goals | **AC10** |
| 12 | No default-path firing (T-1085's scope) | Non-goals | **AC11** (the fan-out mechanics byte-identical; nothing in `bin/` moves but `close-out.sh`) |
| 13 | No tier2/tier3 enablement; both route to `serial` honestly | Non-goals | **AC9** (this task's own records are `serial`), **AC10** |
| 14 | No peer-to-peer scheduler or negotiation | Non-goals | `info-only (not promoted to AC)` — an absence of a design, not an observable artefact; the nearest observable half is **AC11**'s and **AC15**'s confinement of the diff, which leaves no surface a scheduler could live in |
| 15 | No `tools:` widening, no new agent file | Non-goals | **AC5** |
| 16 | No depth-axis value, condition or price | Non-goals | **AC10** (the note's axis count unchanged), **AC9**'s `- stale-at:` trigger |
| 17 | Issue #319's items all deferred; the launch-record surfaces byte-identical | Non-goals | **AC11** |
| 18 | Issue #308's items 1, 3, 4 deferred | Non-goals | `info-only (not promoted to AC)` — those items name work in files this task's scope lock already forbids touching (**AC15**); a criterion asserting an unfiled edit's absence would restate the scope lock under a second name |
| 19 | No new checker script | Non-goals | **AC11** (the `bin/` diff names only `bin/close-out.sh`) |
| 20 | This task's own entry carries the first live records | Goal | **AC9** |
| 21 | Adopter-facing documentation lands in the same task | Goal (`- user-visible: yes`) | **AC13** |
| 22 | The three freeze declarations are present in the conformant `yes` shape | Goal declaration region | **AC12** |
| 23 | Nothing is priced down; the full-population inventory and full CI-equivalence are owed | Goal (`- verification-class: mechanism`) | **AC16** (the disclosure), **AC17** (c) and (d) (the runs) |
| 24 | The gate is anchored so a quoted mention is not a record | Input space | **AC7** (case 4) |
| 25 | Both layouts are supported and paths resolve through `bin/team-paths.sh` | Input space | **AC9**, **AC14**, **AC15** (all resolve the board through `bin/team-paths.sh --get todo`), **AC17** (b) |
| 26 | The new SKILL.md text sits below the reconcile terminator; the step-1 pointer sits inside step 1 | Goal (placement), `## Placement decisions` | **AC6** |

## Singular-determiner sweep

Norm text bound for two gate surfaces (`agents/tech-lead.md` and `skills/run/SKILL.md`) is read here for wording that assumes exactly one of the thing it governs. The governed noun is **axis**, which is two today and three once issue #274's depth axis lands — so a cardinality-two assumption is as much a defect here as a cardinality-one one.

| Determiner / phrasing | Governed noun | Verdict |
|---|---|---|
| "the axis" | axis | Rejected in the shipped block; every clause is written per-axis (`one sub-bullet per axis`, `at most once per entry`, `the axis-level unconditional default`, which reads correctly for any number of axes). |
| "both axes" / "the two axes" | axis | Rejected everywhere in the shipped block and in the frozen criteria. **AC1** requires the block to cite `dispatch-axis:` so the key set is sourced from the note rather than restated as a fixed pair. |
| "the value set" | value set | Kept, deliberately: each axis has exactly one value set, which is a property of an axis, not a count of axes. |
| "the record" | record | Kept where it means *the record for one axis*; the grammar's own filename-free, per-axis phrasing resolves it. |
| "the ground" | ground field | Kept: each record has exactly one ground field, by the grammar. |
| exactly-two counts | dispatch lines on one entry | Present **only** in **AC9**, which is about *this task's own entry under today's two-axis dispatcher* and carries a `- stale-at:` trigger naming the depth axis. Deliberately absent from **AC1**, **AC7** and the shipped gate, so no adopter-facing surface and no shipped script assumes two. |
| "the most specific conditional trigger" | trigger | Kept: the rider is about selecting one trigger among candidates, so the singular is the meaning, not an assumption. |

Read against a plural corpus instance: `docs/loop-engineering/phase-multiplexing.md` itself carries **two** `- dispatch-axis:` lines and **five** `- dispatch-rule:` lines, three of them on one axis — so the "one rule per axis-value pair, several pairs per axis" plurality is exercised by the very document the norm cites.

## Placement decisions

Line-number pins in merged specs make *where* text is inserted a real decision, not a formatting preference. T-1083's own blast-radius sweep recorded T-1082 **AC12** turning red for exactly this reason: its stored derivation pins `skills/run/SKILL.md:177` and `:197`, and T-1083 inserted lines above them.

- **The canonical block goes into a new file** (`templates/prompt-blocks/dispatch-record.md`) rather than into `templates/prompt-blocks/fanout-orchestration.md`, because that file is pinned at line 18 by the same derivation and is the surface issue #319 will edit.
- **In `skills/run/SKILL.md` the block is appended below `<!-- END reconcile-step: T-1077 -->`**, so no line of the fan-out or reconcile regions moves. **AC6** locks this by comparing the two line numbers derived at check time.
- **The step-1 pointer is one sub-bullet.** It has to sit inside the numbered list to be found, and any insertion there shifts every line below it — including the two already-stale pins. This is accepted with its consequence stated: T-1082 **AC12** is **already** red at this task's branch point and stays red; this task creates no new pin failure and repairs none (repairing a merged spec's criterion is not this task's to do).
- **In `agents/tech-lead.md` the block goes after `## Rules` and before the generated `careful-execution` prompt block**, where role duties live. The generated blocks below it are located by marker, never by line number, so shifting them is inert for `bin/check-prompt-sync.sh` and `bin/check-intent.sh`.
- **The `### Dispatch` element joins the Routing Map's fenced output shape** rather than being described in prose, so `tech-lead` emits it by pattern-matching its own template.
- **The step-5 T-1075 decision point is demoted to a mechanics pointer, in place** — rewritten without changing the line count, so it shifts nothing. It is demoted rather than superseded because the *mechanics* it introduces (what qualifies, that the choice is opt-in) remain true and are still needed at that point in the phase; what moves out of it is the **decision**, which the `verify` axis now owns and the board now records. Silently leaving both would give one question two owners.

## Blast radius

**Method.** The inventory owed here is a **full-population diff**, not a search for specs mentioning the touched paths: enumerate every merged spec, run each one's `- check:` lines at the branch point's committed blobs (through a scratch worktree pinned to that ref, never the working tree) and again at HEAD, and difference the two verdict sets criterion by criterion. A `grep`-of-mentions inventory cannot see a criterion that reaches its target through indirection — a board path built at run time from `bin/team-paths.sh --get todo`, a file named only through a variable — and this repository has already had two such criteria overlooked inside one task by that method. The `- verification-class: mechanism` declaration prices nothing down, so this is owed in full.

**Population.** Derived at run time, never counted by eye. The engineer fills the derivation block below by running its own `- reproduce:` line at final state; **AC16** requires the embedded block to reproduce byte-identically from that command.

- reproduce: bin/derive-populations.sh --label t1084-spec-population --set "at-branch-point=git ls-tree -r --name-only $(if git show-ref --verify --quiet refs/heads/feature/1083-agent-launch-fanout; then git merge-base 'feature/1083-agent-launch-fanout' HEAD; else git merge-base 'develop' HEAD; fi) -- .shell-team/specs | grep '\.md$'" --set "at-now=git ls-files -- .shell-team/specs/*.md"

<!-- BEGIN derivation: t1084-spec-population -->
PLACEHOLDER — the engineer replaces this whole block with the verbatim output of the `- reproduce:` line above, run at final state. AC16 fails while this placeholder stands.
<!-- END derivation: t1084-spec-population -->

**Predictions, to be confirmed or corrected by the measured table.** Each is a prediction this spec freezes so that a measurement contradicting it is a recorded finding rather than a quiet edit.

- predicted-red: 0 genuinely new reds caused by this task's diff. Every criterion named below is predicted either unchanged-green or already-red-for-another-task's-reason.

| Merged criterion | What it reads | Base | Head | Why |
|---|---|---|---|---|
| `T-1079` **AC11** | the note's `- dispatch-axis:`/`- dispatch-rule:`/`- dispatch-note:` totals and grammars, re-derived at run time | PASS | PASS | the note is byte-untouched (**AC10**) |
| `T-1069` **AC6**, **AC7** | the note's cost/recommendation lines | PASS | PASS | same reason; both relayed from the tech-lead map and re-measured by the sweep |
| `T-1083` **AC18** | `phase-multiplexing.md`, `agent-concurrency.md`, `harness-agent-concurrency.md` byte-identical to *its* branch point | PASS | PASS | none of the three is touched |
| `T-1082` **AC12** | a stored derivation pinning `skills/run/SKILL.md:177`/`:197` and `fanout-orchestration.md:18` | **FAIL** | **FAIL** | already red at this branch point (T-1083's own sweep recorded it as that task's one new red); this task's step-1 insertion shifts the same pins further without changing the verdict |
| `T-1075` **AC3** | byte-identity lock on `templates/prompt-blocks/fanout-orchestration.md` | **FAIL** | **FAIL** | pre-existing, broken by T-1082 one car back; this task leaves that file byte-identical (**AC11**) |
| `T-1079` **AC13** | `skills/run/SKILL.md`, `agents/*.md`, `templates/prompt-blocks/fanout-orchestration.md` byte-identical to *its* branch point, plus an empty `bin/ tests/ templates/ agents/ skills/` diff | **FAIL** | **FAIL** | pre-existing (T-1082/T-1083 already edited those paths); this task edits them again, verdict unchanged |
| `T-1071`'s `spec-population` derivation criterion | `git ls-files -- .shell-team/specs/*.md` against a pinned tree | **FAIL** | **FAIL** | pre-existing since the corpus grew past its recorded `at-now: 76`; this task adds one more spec, verdict unchanged |
| `T-1081` **AC13** | that task's own merge-point-scoped scope lock | **FAIL** | **FAIL** | merge-point-scoped, disclosed by that spec itself |
| `T-1074`'s criteria over the canonical fan-out block's line grammars | `templates/prompt-blocks/fanout-orchestration.md` | PASS | PASS | block byte-identical (**AC11**) |
| every merged spec's own scope-lock criterion | a `git diff --name-only <its base>...HEAD` allow-list | as measured | as measured | merge-point-scoped by construction; each such spec says so in its own criterion body |
| criteria counting `.shell-team/specs/*.md` or `^contain ` registry rules | populations this task grows by one each | as measured | as measured | the two population classes this task's own files join; the sweep must name every one it finds |

Two classes the sweep must look for specifically, because they are the ones this diff can move without any prediction above anticipating them: **(i)** any criterion pinning an absolute line number in `skills/run/SKILL.md`, `agents/tech-lead.md` or `templates/prompt-blocks/registry.txt`; **(ii)** any criterion asserting a total over `agents/*.md`, `templates/prompt-blocks/*`, `bin/*` or the registry's rules, which this task grows by one file, one rule and zero scripts respectively.

## Pre-commitment (AI self-discipline — self-imposed at freeze, not operator-ratified)

- **Factual trigger**: two consecutive cross-provider review rounds of independent Blocker/Major findings against the **same component**. **Contextual condition**: before a third round on that component starts. When the two disagree, the factual trigger governs.
- **Droppable, in order**: (1) the `bin/close-out.sh` validate-if-present gate — option **(B)** in the three options priced below. Dropping it executes option **(C)**: the record grammar and the precedence rider ship as norm text plus this spec's own `- check:` lines only, `bin/close-out.sh` and `tests/close-out/run.sh` leave the diff, **AC7** and **AC8** are withdrawn with the rounds' findings travelling to a new issue as that gate's requirement list, and the `- verification-class:` declaration is **not** re-priced (the diff still reaches `templates/` and `skills/`).
- **Never-dropped**: the record grammar itself, the two closed value sets, and the precedence rider. Their defeat across consecutive rounds is evidence about the design premise rather than about implementation craft: it **stops the task and returns it to planning**, and there is no carve-out to execute.
- **The three checker options, priced before the first round** — (A) a new `bin/check-dispatch.sh`: zero regression risk to any existing script, but pays the full new-checker-class round tax this repository has measured repeatedly. **(B, chosen default)** extend `bin/close-out.sh` in the exact shape of its `pending:` gate: reuses a shipped, reviewed, fail-closed precedent, adds no new tool for an adopter to learn, and confines the regression surface to one script already covered by `tests/close-out/run.sh`. (C) no checker at all: norm text plus this spec's own check lines, leaving the grammar unenforced on an adopter's board.

## Open questions

None blocking. One question was settled here rather than left open, and is recorded so it is not reopened: the mandate's "the run skill gains the transcription step near step 1" is implemented as a **pointer** at step 1 plus transcription **at the Specify-to-Implement seam**, because the task's board entry does not exist during step 1 — `pm-spec` creates it at step 2. The deviation is from the placement, never from the duty, and the reason is a measured property of the loop's own ordering.

## Notes for engineer

**Files likely touched** — exactly the allow-list **AC15** fixes: `templates/prompt-blocks/dispatch-record.md` (new), `templates/prompt-blocks/registry.txt`, `agents/tech-lead.md`, `skills/run/SKILL.md`, `bin/close-out.sh`, `tests/close-out/run.sh`, `README.md`, `README.ja.md`, this spec, the resolved board, `.shell-team/test-recipe.md`, and the three record files.

**`CHECK_ACS_TIMEOUT` guidance.** **AC7** runs `bin/close-out.sh` nine times, **AC8** runs the whole close-out suite and **AC16** regenerates a derivation over the spec corpus. Run the sweep with `CHECK_ACS_TIMEOUT=300` at minimum; a heavily loaded host may need more, and a timeout is reported as such rather than smoothed over.

**Draft of the canonical block** (mutable — the frozen criteria fix the load-bearing literals, not these bytes). Remember `contain` mode: every non-empty line below must appear verbatim in **both** `agents/tech-lead.md` and `skills/run/SKILL.md`, and each consumer's role-specific tail lives outside this core.

> **Situational dispatch record (T-1084).** `docs/loop-engineering/phase-multiplexing.md`'s own `- dispatch-axis:` and `- dispatch-rule:` lines fix which mechanism runs a task's implement phase and which runs its verify phase. That choice is made per axis, recorded per axis, and auditable against the axis's own closed value set — never left implicit in prose.
>
> - **Precedence.** Apply the most specific conditional trigger whose stated trigger text the task actually satisfies; fall back to the axis-level unconditional default only when no conditional trigger applies. A conditional trigger the task does not satisfy is not a reason to skip the axis — it is the reason the default is recorded, with the ground that made it the default.
> - **Who decides, who records.** `tech-lead` decides each axis and prints the decision in its Routing Map; the orchestrator transcribes each decision onto the task's own board entry once `pm-spec` has created that entry, at the Specify-to-Implement seam and before `engineer` is invoked. `tech-lead` never writes the board.
> - **The grammar.** One indented sub-bullet per axis under the task's own top-level board entry, ` — ` (space, em dash, space) as the field separator: `- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>`.
> - `<axis>` is an axis key one of the note's `- dispatch-axis:` lines defines. Each axis key appears at most once per entry, and every axis the note defines is recorded — so a later axis joins by adding its own key, never by reshaping this grammar.
> - `<value>` is bound to that axis's own closed set: `implement` over `serial|tier2|tier3`, `verify` over `serial|tier1-fanout`. A value belonging to another axis is a malformed record, not a routing choice.
> - `<unconditional|conditional>` is the modality of the rule that was actually applied, taken from the note's own rule line.
> - `<ground>` opens with a priced line id the note already records — `saving: <id>`, `recommendation: <id>`, `break-even: <id>` or `cost-input: <id>` — optionally followed by explanatory text, so a routing choice cites a recorded price and never an invented one.
> - An indented line whose first non-space characters are `- dispatch: ` is a record wherever it sits in the entry; prose that wants to mention this grammar quotes it mid-line instead of opening a line with it.
> - **Validation.** `bin/close-out.sh` validates this record when it is present: every `- dispatch:` sub-bullet on the entry being closed must match the grammar above, and no axis key may appear twice, or the close-out refuses. An entry carrying none of them passes — presence is this norm's requirement, not the script's, so a board written before this record existed still closes out.

**Gate implementation.** Mirror `bin/close-out.sh:190–207` exactly: scan only `A_START..A_END`, anchor the first match to `^[[:space:]]*- dispatch: `, refuse through `fail` (exit 1, board byte-untouched), and place the gate **beside** the `pending:` gate — before the interventions gate — so a malformed record refuses on its own terms rather than behind an unrelated one. The refusal message must carry the literal `malformed dispatch record` (**AC7** greps it) plus the offending line, and must name the task. Keep the axis→value map as a small explicit table in one place, so adding the depth axis later is a data edit; do **not** hard-code a count of axes anywhere.

**Portability boundary, decided rather than left implicit.** The shipped gate validates the ground field's **shape** (it opens with one of the four priced-line prefixes), never whether the named id exists in `docs/loop-engineering/phase-multiplexing.md`. Adopters do not have that note, and a shipped script that reads it would refuse on every adopter board. The existence half is this repository's own concern and lives in **AC9**.

**Test cases** to add to `tests/close-out/run.sh`, each labelled `T-1084 <name>` so `grep -c '^PASS: T-1084'` counts them (**AC8** requires at least six): conformant two-axis record passes the gate; absent record passes; single-axis record passes; mid-line prose quote passes; cross-axis value refuses with exit 1; duplicate axis refuses; unknown axis refuses; bad modality refuses; groundless ground refuses. Assert the exit code **and** the message, never only the exit code.

**Prior art to follow, not re-derive**: T-1083's board entry (`.shell-team/todo.md:1114–1137`) for the hand-off, declaration and attestation sub-bullet shapes; T-1082 **AC12** for the reproduce-and-compare derivation check idiom; T-1079 **AC11** for the priced-ground discipline this record mirrors.

**Measured-at-ref labels**: not applicable — this task's deliverables print no "measured at &lt;ref&gt;" label, no "Base ref" header and no per-row `- measurement:` field. Every ref-reading command in this spec's criteria is a `git show`, `git ls-tree` or `git diff` range against the resolved branch point, which reads that ref's committed blobs by construction; the only working-tree read in the whole spec is **AC15**'s untracked half, which says so in its own body because the working tree genuinely is that clause's subject.
