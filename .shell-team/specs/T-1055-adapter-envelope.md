# The adapter contract layer: the task-envelope contract, the two shipped adapter definitions, and fail-closed effort capability

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1055
**Base ref**: resolved at check time as `git merge-base develop HEAD` — this branch's divergence point from `develop`. No 40-hex literal is written into any criterion: this role has no shell and cannot measure one, so every criterion resolves the value itself and fails closed when `develop` does not resolve. This branch is **stacked** on T-1054's tip, which is itself stacked on T-1053's tip, so the merge base is `develop`'s tip and **both earlier tasks' delivered files are already changed against it** — every scope criterion below allows them explicitly as pre-existing rather than treating them as this task's diff.
**Branch**: `feature/1055-adapter-envelope`

## Problem

T-1054 shipped a host-authored binding config: a host can now declare that `codex-reviewer` runs on the `codex` provider through the `codex-cli` adapter, and a fail-closed validator refuses anything it cannot fully evaluate. Nothing consumes that binding. `adapter` is a token from an allowlist and no document anywhere says what an adapter *is* — what it is handed, what it must give back, or what happens when a host assigns an `effort` value the executor has no way to honour.

Two gaps follow from that, and both are the kind that get expensive after something is built on top of them. First, the **contract** between the loop and an executor is currently implicit in one agent prompt (`agents/codex-reviewer.md`), where it exists as a specific recipe for one CLI rather than as a general statement; T-1056 (loop integration) and T-1057 (telemetry) would each re-derive it, differently. Second, `effort` is validated for *shape* and nothing else, so a host who writes `effort: high` against an executor with no effort knob gets silence — the exact "unknowingly running at a different tier" failure epic issue #203 names. This task closes both without invoking anything: it documents the contract and ships the two initial adapter definitions with their capabilities **measured rather than guessed**. It also once made an unsupported effort value a refusal a host saw before a run; that enforcement was carved out to a successor issue when this spec's pre-commitment trigger fired a second time (DP18), so what ships here is the rule as a byte-frozen normative statement in the contract document, with its enforcement inherited rather than delivered.

## Summarized sources

One line per document whose facts this spec restates, naming the distinction carried over. **Primary** means this role opened the file itself with a file-reading tool. **RELAYED** means the coordinating session pasted it into this role's prompt; this role has no shell, no network and no `gh`.

- **GitHub issue #203 (the epic) — RELAYED, quoted verbatim in this role's prompt, not opened.** Distinctions carried over: (a) design point 5 — the adapter contract is a **task envelope** (role, request, artifact paths, phase, working dir → status/verdict, produced artifacts, board-transition *request*, usage, error class), with roles that write the board directly distinguished from roles that only propose a transition, and the boundary that it is "a documented contract for what adapters receive and return, **not a new serialization protocol** — the substrate (files + board) remains the actual interchange"; (b) design point 1 — `effort` is optional, each adapter declares its support, unset means the provider/model default, a supported value is translated to the provider-specific setting, and **a value the adapter does not support fails closed**, never silently ignored, with "verifying each provider's actual effort mechanism" named as a planning-time task; (c) design point 4 — telemetry records resolved role/provider/model/effort/adapter+version, usage and error class (T-1057's work, but the envelope must already carry them); (d) principles 1–3 — host-owned static binding, fail-closed with no substitution and no fallback chains, and a correctness boundary of POSIX files + git + bash with harness-specific features admitted only as optional operator-layer accelerators carrying a degradation path; (e) custom adapters are out of scope for the initial cut. Every one of these is a **relayed** premise; `## Assumptions` records the confirmation owner.
- **GitHub issue #221 — RELAYED via `.shell-team/todo.md`'s T-1054 fast-follow disposition line, which this role read directly; the issue body itself was not opened.** Distinction carried over: `bin/check-binding.sh`'s registry-override flag is a **testing affordance** with **zero consumers today**, and the concern the issue records is a *future* production path forwarding it; the disposition line names T-1056 as the design input's owner. What this task does with that is DP9.
- **`.shell-team/specs/T-1054-binding-config.md` — primary, read in full.** Distinctions carried over: DP3 (`effort` is positionally required, "no value" is the literal `-`, and *capability* semantics are explicitly deferred to this task); DP6 (the adapter allowlist is plugin-shipped, resolved from the script's own installed directory, never the adopter's tree, and pairs each adapter with exactly one provider); DP7 (the canonical resolved binding — `bound <role> <provider> <model> <effort|-> <adapter>` rows, `LC_ALL=C`-sorted — is the stable interchange, deliberately keyword-distinct from the config's own `bind`); DP8 (stdout is the only output channel, no write side anywhere); DP10 (the threat model is accidents, with the operator and working directory trusted); the closed refusal-token/fixed-exit-code convention and its 20-row matrix; the scope-lock shape (tracked half from the commit range unioned with `git ls-files --others --exclude-standard`); and — load-bearing for this spec's own AC14 — the corrected AC13 prose showing how to state a stacked-branch pre-existing set **without a false whole-set claim**.
- **`templates/binding-adapters.txt` — primary, read in full.** Distinctions carried over: the shipped registry is exactly the two rows `claude-cli claude` and `codex-cli codex` — the adapter token is spelled **claude-cli**, a CLI, not "claude-subagent"; an adapter is "a token here, never a command string, a path or an argument list"; the registry resolves from the checker's own installed directory and a checked-out repository cannot register an adapter by editing anything in its own tree; and its own closing sentence, that adding a row "is a task for whoever builds the adapter layer (T-1055), not something an adopter is expected to do" — which this task declines to act on (DP8).
- **`templates/binding-template.conf` — primary, read in full.** Distinction carried over: the shipped specimen binds `tech-lead` with the effort value `high` on the `claude-cli` adapter, and every other role with the literal `-`. That one row is what makes this task's capability measurement able to invalidate a shipped artifact, which is why DP7 pre-decides both branches rather than discovering the conflict at implementation time.
- **`bin/check-binding.sh` — primary, header block, symlink bootstrap and argument parser read.** Distinctions carried over: `--print-binding` validates first and then emits the canonical form on stdout, which is the interchange this task consumed instead of re-implementing a parser **through v3, and no longer consumes at all since v4 carved that cross-check out (DP18)** — the distinction is retained here because the successor issue's requirement list rests on it; the registry-override flag is documented in the script's own usage text as a testing affordance rather than an adopter knob; `print_help` prints the script's own header comment up to the first non-comment line, so header prose is shipped documentation; every `cd DIR && pwd` site is `pwd -P` (physical) with the header recording *why* — a T-1054 review round reproduced an **ancestor** directory symlink (an adopter's `bin/` symlinked into the plugin's real `bin/`, ordinary vendoring) silently resolving the script's own directory inside the adopter's tree and defeating the registry-identity guarantee.
- **`agents/codex-reviewer.md` — primary, read in full.** The pattern being generalized. Distinctions carried over: the executor is reached as a bare-first-token `codex exec --sandbox read-only --cd <repo> …` invocation whose *request* is a prompt and whose *working dir* is an argv value; results come back as **files** (the `-o` capture, the JSONL event stream, the review record under the resolved reviews dir) and as **board tokens** in a verdict block; availability is probed with `codex --version`, whose verbatim output is transcribed as a recorded provenance value; a missing CLI or failed auth is `BLOCKED` with the exact error and **never** a substituted Claude-only review; and the role "never edits the board … you state it in your verdict block and in your review record, and the orchestrator transcribes it" — the measured instance of #203's propose-versus-write distinction.
- **`agents/engineer.md`, `agents/qa-verifier.md`, `agents/ui-designer.md`, `agents/tech-lead.md` — primary, board-authority lines read.** Distinctions carried over, and this is the measured ground for DP4's authority table: `engineer` writes the board itself ("Update `tasks/todo.md`: append a one-line summary of what changed, set status to `READY_FOR_QA`"); `qa-verifier`'s loop sets the flag to `READY_FOR_REVIEW` or back to `READY_FOR_ENG` itself; `ui-designer` transitions nothing ("Don't introduce a new status flag. The board flag stays where pm-spec set it"); `tech-lead` writes no files at all (frontmatter `tools: Read, Grep, Glob`, description "Does NOT execute code or write files"). Also carried over as a **disclosed nuance, not resolved here**: `qa-verifier`'s frontmatter `tools:` line grants neither `Edit` nor `Write` while its own loop instructs a board update — a pre-existing inconsistency this task records and does not touch.
- **`.shell-team/todo.md` (header, `## Active`, and T-1054's `## Done` entry with its sub-bullets) — primary.** Distinctions carried over: `## Active` is **empty** at this point, so this task's entry is a pure insertion; T-1054 is `READY_FOR_MERGE` against PR #222 and merges at the sprint batch GO, which is why its commits are on this branch and not on `develop`; the fast-follow disposition sub-bullet naming #218/#219/#220/#221 and what each is; and T-1054's own freeze-note conventions this entry follows.
- **`.github/workflows/check-handoff.yml` — primary, step names and the two T-1054 steps read.** Distinctions carried over: one `shellcheck` step naming every script and suite path explicitly (a new script is invisible to CI until listed), one `bash tests/<suite>/run.sh` step per suite, and the "Dogfood <checker> — …" idiom running a real checker against a shipped artifact (`bash bin/check-binding.sh --config templates/binding-template.conf` is the most recent instance). The wiring shape this task follows is that idiom.
- **`.shell-team/test-recipe.md` — primary, read in full.** Distinctions carried over: the T-1044 entry's rules for a **new** `tests/<suite>/run.sh` (index mode `100755`; `mktemp -d … XXXXXX`; the two-arm `TMPDIR`-then-`$HERE` idiom at `tests/check-refreeze-class/run.sh:82-87` **unless** the suite builds `git init` repos, which this one does not); the T-1024 entry's `- check:` mktemp-guard idiom and its reason (`check-acs.sh` runs each check under `bash -c` with no `set -e`); the T-1016 `grep -c`-under-`pipefail` hazard; and T-1054's own entry, whose two authoring traps this suite inherits — a per-mode refusal ordering that makes a differently-named fixture file trip an earlier refusal than the one under test, and a payload with embedded whitespace being refused as a wrong-field-count row rather than as a malformed token.
- **`docs/loop-engineering/` directory listing — primary (enumerated).** Distinction carried over: four of its files (`lessons-import-disposition.md`, `playbook-block-size-deferral.md`, `arith-base10-audit.md`, `check-line-mktemp-guard-audit.md`) ship with no `*.ja.md` counterpart while `goal-loop.md` has one — the measured precedent for DP3's English-only decision.
- **`.shell-team/reviews/T-1055.md` (round 1) — primary, read in full by this role before v2 was written.** Distinctions carried over, each an *empirically reproduced* result rather than an opinion, which is why v2 treats them as measurements: a three-token row appended to the real shipped `templates/binding-adapters.txt` left `bin/check-adapter.sh` at exit `0` while `bin/check-binding.sh`, parsing the identical file, refused `registry-malformed` at exit `2`; stripping every `error-class` row from a copy of the contract registry still validated; a contract with `schema 2` plus matching definitions validated; a definitions directory holding only `claude-cli.txt` validated; `claude-cli.txt` with `task-id` remapped to `not-carried` validated; flipping the document's `task-id` bullet from `in` to `out` left AC8 passing; the `status` vocabulary had no directive anywhere; and a binding assigning `codex-reviewer` to `claude`/`claude-cli` was accepted by both checkers while `claude-cli.txt` unconditionally declared `carries board-transition board`. Also carried over: **all 16 frozen check lines, the 65-case suite, `shellcheck` and the records were independently re-confirmed green** — the finding is about what the criteria do not test, never about the implementation failing them; the two live effort measurements (`codex-cli 0.145.0`; Claude Code `2.1.226`) were independently confirmed; and the ledger's two re-gradings (the `status` gap **down** to major because the document disclosed the limit honestly and no envelope instance exists to misbehave against; the allowlist gap **up** to blocker because it was reproduced against the real shipped file with a working side-by-side contrast). The reviewer's own recommendation to re-read the pre-commitment before starting rework is carried over verbatim into `## Notes for engineer`.
- **The task prompt's framing — RELAYED.** Sprint membership and ordering, the branch name, T-1054's tip, the T-1055/T-1056/T-1057 split, and the planning decision that no third adapter ships. Recorded in `## Assumptions`.

## Goal

<!-- BEGIN intent-block: T-1055 -->

Four artifacts ship together and nothing else does. **(1) The task-envelope contract, documented** in `docs/loop-engineering/task-envelope.md`: a normative statement of what every adapter receives and returns, naming each envelope field, its direction, its requiredness (with the condition stated for every conditional field), the closed status and error-class vocabularies, the per-role board-transition authority split between roles that write the board and roles that only propose a transition, how the `codex-reviewer` pattern generalizes to an adapter, and the boundary that this is a contract and **not a new serialization protocol** — nothing in this task writes, reads or parses an envelope instance, and files plus the board remain the actual interchange. **(2) A plugin-shipped, line-oriented envelope contract registry**, `templates/task-envelope.txt`: whitespace-delimited rows under a required `schema` line, declaring the envelope's fields with their direction and requiredness, the closed channel vocabulary, the closed **status** vocabulary with exactly one member marked as the success value, the closed error-class vocabulary, the closed effort-mechanism vocabulary, and one `role-board-authority` row for each of the six inner-loop roles. **(3) The two initial adapter definitions**, `templates/adapters/claude-cli.txt` and `templates/adapters/codex-cli.txt`, one per row of T-1054's shipped allowlist and no others — each declaring its identity (adapter token, paired provider, its own version, the envelope schema it implements), a `carries` row mapping **every** contract-declared envelope field onto a channel from the contract's closed vocabulary, and its **capability declarations**, of which `effort` is the one this cut settles: whether the adapter supports it, by which mechanism, and which values it accepts — **measured against the real provider at implementation time and recorded, never guessed**. **(4) A fail-closed validator**, `bin/check-adapter.sh`, pure bash, zero new runtime dependency, shellcheck-clean, whose every refusal is one token from a closed set with a fixed exit code, which never returns success on input it could not fully evaluate, which resolves the contract and the definitions from its own installed directory rather than the working directory, which validates the plugin-shipped adapter allowlist it depends on **exactly as strictly as `bin/check-binding.sh` validates that same file, refusing with the same token at the same exit code**, which enforces every declared minimum of the contract registry rather than relying on the shipped file happening to be correct, **which refuses a retired channel token wherever a channel is declared, from a set compiled into the checker itself rather than read from the file being validated**, and which reads no binding config at all — the cross-check that once did was carved out when this spec's pre-commitment trigger fired a second time (DP18), so the fail-closed effort rule and the board-transition authority rule ship here as **normative statements in the contract document**, byte-frozen and pinned by AC6, with their enforcement inherited by a successor issue and by T-1056. A fixture suite (`tests/check-adapter/run.sh`) exercises every refusal and CI runs the suite plus a dogfood step. Nothing invokes an executor: no adapter composes an argv, no loop phase reads the contract, no run-time unavailability handling ships, `templates/binding-adapters.txt` gains no third row, and `bin/check-binding.sh` and its suite are left exactly as T-1054 shipped them.

### The task envelope (DP1)

The frozen field set, its directions and its requiredness. Direction is stated from the adapter's point of view: `in` is what an adapter receives, `out` is what it returns. A `conditional` field is always *declared*; its condition governs when a value is required, and the contract document states that condition for each one.

| Field | Direction | Requiredness | What it carries |
|---|---|---|---|
| `task-id` | in | required | the `T-NNN` this invocation serves |
| `role` | in | required | which of the six inner-loop roles is being run |
| `phase` | in | required | which loop phase this invocation serves |
| `request` | in | required | the work statement handed to the executor |
| `artifact-paths` | in | required | the operating paths the role reads and writes, as already resolved — never re-derived by the adapter |
| `working-dir` | in | required | the repository root the invocation runs in |
| `resolved-binding` | in | required | the bound row for this role: provider, model, effort, adapter |
| `status` | out | required | whether the invocation completed, from the contract's closed status vocabulary |
| `verdict` | out | conditional | the role's own outcome token, required when the role's phase produces one |
| `produced-artifacts` | out | required | the paths the invocation wrote — the envelope names them and never carries their content |
| `board-transition` | out | conditional | required when the role's board authority is `writes` or `proposes`; for a `proposes` role it is a **request** the orchestrator transcribes, never an act |
| `usage` | out | required | the resource accounting the executor reported; where the executor reports none, the adapter says so explicitly rather than omitting the field |
| `error-class` | out | conditional | required when `status` is not the success value, from the contract's closed error-class vocabulary |
| `adapter-version` | out | required | the version of the adapter definition that actually ran |

### Settled decisions

Every decision below is resolved. Where a decision departs from the most obvious reading of the epic, its reason is stated at the decision, not deferred to review.

**DP1 — the envelope is a contract, and this task ships no instance of one.** The field table above is normative; no file format for an envelope instance is defined, no code serializes or parses one, and `bin/check-adapter.sh` accepts no flag whose name begins `--envelope`. #203's own boundary sentence is what this implements: the substrate is files plus the board, and an envelope is the vocabulary in which a later component describes what it passed and what came back. The enforceable half is negative (AC9) because a contract with no instances has no positive behaviour to observe.

**DP2 — the contract is shipped twice, in two forms, for two readers, and the two are checked against each other.** `docs/loop-engineering/task-envelope.md` is the prose — meanings, conditions, the generalized invocation pattern, the measured effort mechanisms. `templates/task-envelope.txt` is the machine-readable half — the same field tokens, plus the closed vocabularies, in the line-oriented whitespace-delimited idiom `templates/binding-adapters.txt` and `templates/durability-records.txt` already use. Two documents of one contract is exactly the shape that drifts, so AC8 requires their field sets to be **equal in both directions**, each side re-derived at check time rather than compared against a literal.

**DP3 — the prose half lives in `docs/loop-engineering/` and ships English-only.** Its readers are maintainers, adapter authors and whoever implements T-1056 and T-1057 — not adopters, who are shipped nothing wired by this task. `docs/loop-engineering/` already holds exactly this class of internal-mechanism document and four of its five files carry no `*.ja.md` counterpart, which is the measured precedent. `README.md` and `docs/adopting.md` are declined for the same reason T-1054 declined all adopter-facing documentation: adopter documentation lands with the feature that is actually wired, in T-1056. The full candidate set and each decline's reason are in `## Surface decision table`.

**DP4 — the board-transition authority split is declared per role, in the registry, not only in prose.** One `role-board-authority <role> <writes|proposes|none>` row per inner-loop role, from a closed three-token vocabulary. `writes` means the role edits the board itself and the envelope's `board-transition` records what it wrote; `proposes` means the role returns the requested flag and the orchestrator transcribes it; `none` means the role requests no transition. This is the one part of #203's design point 5 that would otherwise exist only as body prose, which is precisely the shape that escapes a criterion-driven gate. The values are measured from each role's own shipped prompt (`## Summarized sources`), not assigned by this task.

**DP5 — an adapter definition is declarative and cannot spell a command.** Every value in a definition file is a bare token; no field is a command string, an argv list, an environment assignment or a path to an executable, and the checker refuses anything outside the declared token classes. #203 forbids the *config* from storing a raw shell command; extending the same property one level up to the adapter registry costs nothing and removes the obvious place the property would have leaked. How the envelope reaches the executor is declared as a `carries <field> <channel>` mapping onto the contract's closed channel vocabulary — a claim about which part of the existing substrate carries each field, not an instruction to execute. The concrete invocation shape stays prose in the contract document and code in T-1056, where it is reviewed as code.

**DP6 — one definition file per adapter, named for its own token, looked up rather than scanned.** `templates/adapters/<adapter-token>.txt`; a definition whose declared `adapter` token differs from its own basename is a refusal. This makes the lookup for a bound adapter deterministic (compose the path, do not search) and makes "the definition set" enumerable for the default whole-directory validation, without adding a row of any kind to `templates/binding-adapters.txt` — whose two-token grammar T-1054's validator enforces and which a third column would break.

**DP7 — the `effort` capability is measured against the real provider, and the shipped specimen is reconciled to whatever the measurement says.** For `codex-cli` the measurement is what the locally installed Codex CLI actually accepts; for `claude-cli` it is the harness's own effort semantics. Both branches are pre-decided so the conflict is not discovered mid-implementation: if the measured capability admits the value `high` on the `claude-cli` adapter, `templates/binding-template.conf` is untouched; if it does not, that file's `tech-lead` row has its **`effort` field and nothing else** changed to a value the measurement supports (`-` if none does), and the reason is recorded in provenance. Any other edit to that file is a scope violation. Either way the shipped specimen and the shipped definitions must agree. **Superseded in v4 as to enforcement**: the cross-check that asserted this agreement is carved out (DP18), so no criterion in this task measures it any more; the agreement holds on the measured values but is inherited by the successor issue, and that loss is recorded as part of the drop's stated price rather than left implicit.

**DP8 — no third adapter, and no edit to T-1054's validator.** The initial cut is the two allowlisted rows; #203 puts custom adapters out of scope, and `templates/binding-adapters.txt`'s own header defers a new row to this task, which declines it. T-1054's `templates/binding-adapters.txt`, `bin/check-binding.sh` and `tests/check-binding/run.sh` are pre-existing on this branch and not editable here (AC11, AC14). T-1054's AC2 carries a `- stale-at:` note anticipating that this task might add a registry row; it does not, so that criterion's declared sets still describe the shipped artifacts.

**DP9 — issue #221's split: this task ships the static invariant over today's tree, T-1056 owns the runtime lock.** The concern is a production path forwarding a testing affordance. This task creates no production caller of anything, so what it can honestly assert is that **no file under `bin/`, `skills/*/SKILL.md` or `agents/*.md` forwards a registry-override or definitions-override flag except the checker that defines it** — measured today (AC12) and true today. `bin/check-adapter.sh` therefore never spells the sibling validator's registry-override flag at all, in code or in comments, and delegates to it without one. The half this task cannot hold is the one that only exists once a production caller exists: T-1056 introduces the first, and re-asserting this invariant over its own new call sites is T-1056's, recorded here so it is inherited rather than rediscovered.

**DP10 (superseded in v4 — the mode it governs is carved out per DP18; retained because the successor issue's requirement list rests on it) — the cross-check delegates parsing and refuses rather than proceeding.** `--binding` runs `bin/check-binding.sh --config <path> --print-binding` and reads the canonical `bound` rows; it never parses the config's own grammar. A config that sibling refuses is `binding-invalid` at exit 2 with nothing written to stdout — the adapter layer does not get a second opinion about a config the format's own validator rejected. One parser, two consumers, for the reason T-1041 settled for `check-intent.sh --print-hash` and T-1054 reused: two implementations of one normalization drift.

**DP11 — resolution is physical from the first commit.** The contract registry and the definitions directory resolve from this script's own installed location using `cd DIR && pwd -P`, never a logical `pwd`, so an adapter's own `templates/` in the working directory — or an **ancestor** directory symlink from ordinary vendoring — cannot substitute either. This is not a hardening step discovered later: it is the defect a T-1054 review round reproduced against the sibling checker, and it ships correct here rather than being repaired after a round. `bin/check-durability.sh` carries the same defect and is issue #218's, deliberately untouched.

**DP12 — the threat model is accidents, identical to T-1054's and `bin/check-durability.sh`'s, for the identical reason.** A stray edit, a vendored install resolving to the wrong tree, a host binding an effort value the executor cannot honour. A hostile operator replacing a shipped definition inside the plugin's own installation is out of scope, because `CLAUDE.md`'s security invariant makes the operator and the working directory trusted.

### Settled decisions added in v2, after round 1 of cross-provider review

The four decisions below resolve contract-design gaps a cross-provider review round reproduced empirically against the v1 implementation. Each is recorded here rather than repaired ad hoc, because each changes what the contract *means*, not only what the checker happens to catch.

**DP13 — a `carries` row declares a value's return path, never an authority to act; the `board` channel is retired.** Round 1 reproduced the category error this fixes: `role-board-authority` is **role**-scoped while `carries` is **adapter**-scoped, nothing in T-1054's grammar restricts which role binds to which adapter, and `templates/adapters/claude-cli.txt` declared `carries board-transition board` unconditionally — so a `proposes` role (`codex-reviewer`) legally bound to `claude-cli` inherited a declaration that the adapter writes the board itself, which is exactly what that role must never do. The resolution is definitional, not a new check: **a `carries <field> <channel>` row states which part of the existing substrate the field's own *value* travels on between the orchestrator and the executor, and states nothing about who may act on it.** Whether the board is actually written during an invocation is decided **solely** by the bound role's `role-board-authority` row. The `board` channel token named an action rather than a path and is therefore **removed from the contract's channel vocabulary**; every `board-transition` return path is an ordinary one (`stdout`, `file`, `exit-status`), and a `writes` role's board mutation is a side effect of that role acting as itself, declared by its authority row and never by its adapter's channel. Making the wrong state **unrepresentable** is preferred here over detecting it, which is why no per-`(role, adapter)` declaration is introduced and no general join check is invented: with `board` retired there is nothing left for one to catch. One genuine join does remain and **is** enforced, because `--binding` is the one mode that knows the role and the adapter together: `board-transition` is a *conditional* field, so `carries board-transition not-carried` stays legal in general (an adapter through which no transition ever returns), but binding a role whose authority is `writes` or `proposes` to such an adapter is a contradiction the contract can see — the role must produce a transition and the adapter declares no path for it — and is refused `authority-channel-conflict`. **Superseded in v4 as to enforcement**: that join lived in `--binding`, which DP18 carves out, so the rule survives as a byte-frozen normative statement in the contract document (pinned by AC6) and its enforcement travels to the successor issue — the stated price of the drop, recorded rather than quietly lost. That rule and DP14's required-field rule are complementary: the required-field rule forbids `not-carried` unconditionally for fields that are always required, and this one forbids it conditionally, for the one field whose requiredness is decided by a role.

**DP14 — the `status` vocabulary gets a machine form, `status-value <token> <success|failure>`, rather than a re-scoped promise.** The v1 contract document promised `status` came "from the contract's closed status vocabulary" while the registry had no directive for one — so T-1056 would have had to add a directive type to both the registry and the checker to obtain concrete status tokens, which is precisely the reopening this task exists to prevent. Re-scoping the promise was considered and rejected on that ground: the task's stated purpose is that the next component consumes this contract without amending it, and a vocabulary that exists only as prose is not consumable. The row carries a second column because the one thing every consumer needs from this vocabulary is *which member means success* — `error-class` is conditional on `status` not being it, and a convention ("the token happens to be spelled `ok`") is not a contract. Exactly one row is marked `success` and at least one `failure`; the membership itself is not frozen here, for the same reason DP-level vocabularies elsewhere in this spec are shape-frozen and membership-free. Fields that are always `required` must additionally name a real carrier: a `carries` row placing a `required` field on `not-carried` is refused `field-coverage-gap`, closing the reproduced case where every required `in` field could be declared uncarried and still validate.

**DP15 — the plugin-shipped adapter allowlist is validated at full parity with `bin/check-binding.sh`, reusing that checker's own refusal tokens.** Round 1's Blocker: a malformed row in `templates/binding-adapters.txt` was silently skipped, so appending a three-token row to the real shipped file left `bin/check-adapter.sh` reporting valid at exit `0` while `bin/check-binding.sh`, parsing the identical file, correctly refused. Two checkers reading one file must not disagree about whether it is well-formed. The decision is therefore not merely "add a check" but **which vocabulary the check speaks**: this checker adopts T-1054's own tokens, `registry-unreadable` (2) and `registry-malformed` (2), byte-identical for the identical conditions on the identical file — two-token rows, character class, length cap, and no adapter token declared twice. This **supersedes** the v1 implementation decision that folded an unreadable allowlist into `contract-unreadable`; that token now names the envelope contract registry alone. The property this buys is testable rather than asserted, and AC18 tests it: the same corruption, presented to both checkers, produces the same token at the same exit code. Deliberately **not** adopted: a path-override flag for the allowlist. `bin/check-adapter.sh` must never spell that flag's name (DP9), so the parity test operates on a scratch copy of the installed tree instead — which has the further merit of exercising the real `$SCRIPT_DIR`-relative resolution rather than a test-only code path.

### Settled decisions revised in v4, after round 3 of cross-provider review

**DP18 — the `--binding` cross-check mode is carved out; the two rules it enforced ship as byte-frozen normative statements instead.** Round 3 produced one new Major against `bin/check-adapter.sh`: the delegated `schema` line was validated for **presence but not content**, so a stubbed sibling emitting `schema` with no version, `schema 999`, or `schema 1 extra` was accepted — the same "trust the shape without validating it" pattern that produced round 2's finding in this same reassertion block, recurring in a narrower form. That is a third consecutive round of new independent findings against this component and the second firing of the factual trigger, so drop item (1) as v3 renumbered it — the `--binding` mode — executes. Carved out: the mode itself, AC6's static half, AC7 as it stood, and the `effort-unsupported`, `authority-channel-conflict` and `binding-invalid` tokens. **The reviewer's own framing — "a much smaller rework surface … one finding, one mechanism, no design decision to re-open" — is exactly the cheap-fix reasoning this pre-commitment exists to overrule**, and it is recorded here as seen and refused rather than missed; a trigger that yields to "but this one is small" is not a trigger. **What survives, and how**: the fail-closed effort rule (unset means the provider or model default; a value the adapter does not declare fails closed with `error-class capability-unsupported` and no invocation, never a silent ignore) and the board-transition authority rule (authority is decided solely by the bound role's `role-board-authority` value, never by the adapter, and binding a `writes`/`proposes` role to an adapter declaring no return path for `board-transition` is forbidden) are both stated in the contract document as **byte-frozen `- normative: ` lines**, equality-locked by AC6 exactly as the `- boundary: ` line is by AC9. **What is genuinely lost, stated rather than argued away**: nothing in this task now checks a binding config at all — not the effort agreement, not the authority join, and not the shipped specimen's agreement with the shipped definitions (DP7). Those are the drop's price and they travel to the successor issue as its requirement list, together with round 2's V4 findings and round 3's schema-content finding. **T-1056's spec depends on that issue** and must treat it as a planning input rather than assuming this task delivered a pre-flight.

**The never-dropped check, run before executing and recorded because the answer decides which branch fires.** The nine never-dropped items were re-read against this drop. Item 4 is "the fail-closed treatment of an unsupported effort value being **stated normatively somewhere**, never silently ignored" — the wording chosen at v2 says *stated*, not *enforced*, and the contract document states it, so the item holds. Items 8 and 9 — the `board` channel staying retired, and that retirement being **structural** — live in the contract loader's compiled-in retired set and its `retired-channel-declared` refusal, which no part of this drop touches; round 3 independently confirmed that refusal fires in the default mode, not only through `--binding`. Items 1, 2, 3, 5, 6 and 7 are untouched by construction. **DP13's `authority-channel-conflict` join is not on the never-dropped list** — it was enumerated inside drop item (1) at v2 and again at v3, so its loss is the pre-agreed price of executing the drop rather than the weakening of a protected item. **Therefore the stop-and-return-to-planning branch does not fire**, and this is a judgment recorded with its reasoning so a later reader can disagree with it on the evidence rather than guess at it.

### Settled decisions revised in v3, after round 2 of cross-provider review

Round 2 produced one new Blocker and six Major findings against `bin/check-adapter.sh`, the same component round 1 hit. That is the second consecutive round, so **this spec's own pre-commitment factual trigger fired and its recorded disposition was executed rather than reopened**: droppable item (1), the `--doc` mode, is carved out to a successor issue. Two decisions below record what changed; every other v2 decision stands.

**DP17 — a retired channel token is refused from a set compiled into the checker, and declared in the contract so its reader can see it.** Round 2's Blocker: v2 achieved DP13's retirement by *removing* `board` from the shipped registry, which is a fact about one file rather than a property of the contract — a scratch contract supplied through `--contract` could re-declare `channel board`, remap `carries board-transition` onto it, and validate cleanly with a `proposes`-authority role bound to it, reproducing the exact category error DP13 exists to eliminate. DP13's own stated preference ("making the wrong state unrepresentable is preferred over detecting it") was therefore not met by v2's implementation, and a regression test on the shipped file could not meet it either, because the attack does not go through the shipped file. The fix is structural and has two halves that lock each other. **The authority is compiled in**: `bin/check-adapter.sh` carries its own retired-channel set, and a `channel` row declaring any member is refused `retired-channel-declared` (2) **whichever contract file is loaded**, so no override can reintroduce the token; a `carries` row naming a retired token then simply names a vocabulary member that cannot exist, and is refused `unknown-channel` (1) by the machinery that already exists. **The declaration is visible**: the contract registry additionally carries a `retired-channel <token>` row per retired token, emitted into the canonical contract and therefore into the document's canon region, so a reader implementing T-1056 sees the retirement in the contract rather than only in prose; the registry's declared set must equal the checker's compiled-in set exactly, and a divergence in either direction is refused `contract-incomplete` (2). This is the first defeat of a never-dropped item, not a consecutive-generations defeat, and closing it **strengthens** that item rather than weakening it — so the never-dropped stop-and-return-to-planning branch does not fire.

**DP16 (v3) — the doc-sync mode is carved out; AC8 retreats to the independent comparison the drop order named.** The `--doc` mode drew two of round 2's Majors — a zero-match `grep` under `set -euo pipefail` aborting with a bare, unclassified exit 1 (a direct violation of the one-token-from-a-closed-set property), and a `head -n1` per-field comparison that never reads a second, contradicting bullet for the same field. Rather than patch a third time, the pre-commitment's recorded disposition executes: the mode, its `doc-drift` and `doc-unreadable` tokens, its fixture-suite cases and its CI dogfood step are **deleted from this branch** — the v2 implementation exists in the tree, so this is a removal rather than a never-add — and carried to a successor issue **named in `## Notes for engineer` rather than here, so the issue number can be recorded without a re-freeze**; the successor's requirement list is DP16's design as v2 stated it plus those two findings. What remains is what the drop order promised — "strictly better than v1 rather than nothing": **AC8 performs the comparison itself**, at canon-region byte level and per-field tuple level, and additionally requires **exactly one bullet per field**, which closes the duplicate-contradicting-bullet class in the retained scope. What is genuinely lost is the standing CI gate: two-form agreement is checked when this spec's criteria are checked, not on every run. That loss is the price the pre-commitment named in advance and is recorded here rather than argued away.

## Non-goals

- **No invocation of anything.** Nothing composes an executable, an argv or an environment; no provider is called; no `codex exec` or harness invocation is added anywhere. That is T-1056.
- **No loop integration.** No phase transition, skill or agent reads the contract, the registry or a definition; no `run-id` is minted; no envelope instance is constructed. That is T-1056.
- **No run-time unavailability handling.** What the loop does when an executor is missing is T-1056's; this task only declares the error class the situation belongs to.
- **No migration, `team-init` scaffolding or v1-default behaviour**, and no `.shell-team/binding.conf` for this repository.
- **No third adapter, no custom-adapter mechanism, no adapter discovery, no user-extensible registry**, and no new row in `templates/binding-adapters.txt` (DP8).
- **No `--binding` cross-check mode ships, and on this branch that is a REMOVAL rather than a never-add.** The v3 implementation is already in the tree, so a "gains no `--binding` mode" reading of this line would be false on arrival: what this task delivers is the deletion of the `--binding` flag and its dispatch arm, the `effort-unsupported`, `authority-channel-conflict` and `binding-invalid` tokens, the mode's fixture-suite cases, and its CI dogfood command. Nothing in this task reads a binding config. The mode drew findings in rounds 2 and 3 and was carved out to a successor issue when the pre-commitment trigger fired a second time (DP18).
- **No doc-sync mode ships, and on this branch that is a REMOVAL rather than a never-add.** The v2 implementation of the mode is already in this branch's tree, so a "gains no `--doc` flag" reading of this line would be false on arrival: what this task delivers is the deletion of the `--doc` flag, the `doc-drift` and `doc-unreadable` tokens, the mode's fixture-suite cases, and its CI dogfood step. The mode drew two Majors in round 2 and was carved out to a successor issue when this spec's pre-commitment trigger fired (DP16 v3). Two-form agreement is still required and still checked — by AC8, at spec-check time — but it is no longer a standing CI gate, and that is a stated loss rather than an oversight.
- **No restriction on which role may bind to which adapter.** Round 1 observed that `codex-reviewer` may legally bind to `claude`/`claude-cli`; that is correct and stays correct. T-1054's DP11 deliberately left cross-provider review a shipped default rather than a validation rule, because #203 explicitly reopened that question, and adding a role-to-adapter restriction here would decide it in the one layer no host can override. DP13 resolves the reproduced defect by fixing what a `carries` row *means*, never by narrowing what a host may bind.
- **No edit to `bin/check-binding.sh` or `tests/check-binding/run.sh`**, and no change to any refusal token, exit code or mode T-1054 shipped.
- **No edit to `templates/binding-template.conf` other than the single conditional `effort`-field change DP7 pre-decides**, and no change to that file's row count, role set, provider fields, model fields or adapter fields.
- **No `*.ja.md` counterpart** for the contract document (DP3), and no edit to `README.md`, `README.ja.md`, `CONTRIBUTING.md`, `CHANGELOG*.md` or any pre-existing file under `docs/`.
- **No envelope serialization** of any kind: no instance format, no parser, no writer, no flag beginning `--envelope` (DP1).
- **No write side, anywhere.** No mode of `bin/check-adapter.sh` creates, truncates or overwrites a file; stdout is its only output channel.
- **No edit *by this task* to any `agents/*.md` or any `skills/*/SKILL.md`.** Stated as a claim about this task's own edits rather than about the tree's state against the base ref, because `agents/pm-spec.md` and `skills/run/SKILL.md` already carry T-1053's committed changes on this stacked branch and a tree-state reading of this line would be false on arrival.
- **No edit to any file T-1053 or T-1054 delivered on this branch**, beyond the three files this task legitimately appends to (`.shell-team/todo.md`, `.shell-team/test-recipe.md`, `.github/workflows/check-handoff.yml`) and the conditional specimen edit DP7 pre-decides.
- **No repair, re-basing, widening or annotation of any merged criterion this task's diff turns red**, and no merged spec gains a `- stale-at:` sub-bullet from this pass.
- **No retro, no acting on any fast-follow** surfaced during this task, **no merge and no push to `develop`.**

## Acceptance criteria

Every check runs from the repository root, guards every `mktemp` with an immediate non-zero exit, writes nothing outside `$TMPDIR`, names the files it reads explicitly, and pairs every absence or refusal assertion with a positive control, so an unreadable target or a checker that never ran reads as a failure rather than as a clean result. Criteria that resolve a base ref do so as `git merge-base develop HEAD` and fail closed when `develop` does not resolve.

- [ ] **AC1** The six deliverable files exist and follow this repository's shipping conventions. `bin/check-adapter.sh`, `tests/check-adapter/run.sh`, `templates/task-envelope.txt`, `templates/adapters/claude-cli.txt`, `templates/adapters/codex-cli.txt` and `docs/loop-engineering/task-envelope.md` are all present and readable; the two scripts each begin with `#!` and are tracked at git index mode `100755` (judged from `git ls-files -s`, never a working-tree `test -x`); and `bin/check-adapter.sh` invokes no interpreter or processor outside bash + git + standard POSIX tools — no `jq`, `python`, `python3`, `perl`, `node` or `yq` appears in command position anywhere in it. Positive control: the same command-position search form applied to `printf` returns at least one hit, so a search form that matched nothing cannot satisfy the absence clause.
  - check: rc=0; for f in bin/check-adapter.sh tests/check-adapter/run.sh templates/task-envelope.txt templates/adapters/claude-cli.txt templates/adapters/codex-cli.txt docs/loop-engineering/task-envelope.md; do test -r "$f" || rc=1; done; test "$rc" -eq 0 || exit 1; for f in bin/check-adapter.sh tests/check-adapter/run.sh; do head -n1 "$f" | grep -q '^#!' || rc=1; done; test "$(git ls-files -- bin/check-adapter.sh tests/check-adapter/run.sh | grep -c . || true)" = "2" || rc=1; m=$(git ls-files -s -- bin/check-adapter.sh tests/check-adapter/run.sh | awk '{print $1}' | sort -u | tr '\n' ' '); test "$m" = "100755 " || rc=1; test "$(grep -cE '(^|[;&|(]|\$\()[[:space:]]*(jq|python3|python|perl|node|yq)([[:space:]]|$)' bin/check-adapter.sh || true)" = "0" || rc=1; test "$(grep -cE '(^|[;&|(]|\$\()[[:space:]]*printf([[:space:]]|$)' bin/check-adapter.sh || true)" -ge 1 || rc=1; test "$rc" -eq 0
- [ ] **AC2** The shipped contract registry validates under the shipped checker and declares the envelope vocabulary this contract is made of. `bash bin/check-adapter.sh` with no arguments exits `0` — the default mode validates the shipped contract registry and every definition in the shipped definitions directory. `bash bin/check-adapter.sh --print-contract` exits `0` and emits a canonical contract in which: each of `task-id`, `role`, `phase`, `request`, `artifact-paths`, `working-dir` and `resolved-binding` appears as a `field` row with direction `in`; each of `status`, `verdict`, `produced-artifacts`, `board-transition`, `usage`, `error-class` and `adapter-version` appears as a `field` row with direction `out`; every `field` row's requiredness is `required` or `conditional`; at least two `channel` rows exist and **no** row declares the retired `board` channel (DP13); exactly one `status-value` row is marked `success` and at least one is marked `failure` (DP14); each of `executor-unavailable`, `capability-unsupported`, `invocation-failed` and `contract-violation` appears as an `error-class` row; at least two `effort-mechanism` rows exist, one of them exactly `none` and at least one of them not `none`; and `role-board-authority` rows number six, one for each of `tech-lead`, `pm-spec`, `engineer`, `qa-verifier`, `codex-reviewer` and `ui-designer`, each carrying a value from the closed set `writes|proposes|none`, with none of `scrum-master`, `triage-orchestrator` or `drift-evaluator` present. The canonical contract additionally carries a `retired-channel` row for every retired channel token, `board` among them (DP17), so the retirement is visible to a reader of the contract and not only of its prose. **Every one of those minimums is enforced by the checker rather than merely observed on a shipped file that happens to be correct**, proved on nine mutated copies of the shipped registry supplied through `--contract`: stripping every `error-class` row, stripping any **one** of the four required error-class tokens while leaving an arbitrary row in its place, stripping the `effort-mechanism none` row, stripping every `status-value` row, adding a second `success`-marked `status-value` row, and stripping the `retired-channel` row that the checker's own compiled-in set requires are each refused `2` `contract-incomplete`; changing the `schema` version to an unsupported value and declaring one `status-value` token as **both** `success` and `failure` are each refused `2` `contract-malformed`; and declaring `channel board` — the retired token — is refused `2` `retired-channel-declared` **even though the contract supplying it is the one under test**, which is the property v2 lacked and round 2 reproduced: the retired set is compiled into the checker, so no `--contract` override can reintroduce the token. Positive controls: the canonical output is asserted non-empty and to carry exactly one `schema` line before any row assertion is made, and every mutated copy is asserted to differ from the shipped registry, so an edit that failed to apply cannot masquerade as a refusal.
  - check: rc=0; test -r bin/check-adapter.sh || exit 1; CT=templates/task-envelope.txt; test -r "$CT" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055b.XXXXXX") || exit 1; bash bin/check-adapter.sh >/dev/null 2>&1 || rc=1; bash bin/check-adapter.sh --print-contract > "$T/c" 2>/dev/null || rc=1; test -s "$T/c" || rc=1; test "$(grep -c '^schema ' "$T/c" || true)" = "1" || rc=1; for f in task-id role phase request artifact-paths working-dir resolved-binding; do test "$(grep -cE "^field $f in (required|conditional)$" "$T/c" || true)" = "1" || rc=1; done; for f in status verdict produced-artifacts board-transition usage error-class adapter-version; do test "$(grep -cE "^field $f out (required|conditional)$" "$T/c" || true)" = "1" || rc=1; done; test "$(grep -c '^channel ' "$T/c" || true)" -ge 2 || rc=1; test "$(grep -c '^channel board$' "$T/c" || true)" = "0" || rc=1; test "$(grep -cE '^status-value [a-z][a-z0-9-]* success$' "$T/c" || true)" = "1" || rc=1; test "$(grep -cE '^status-value [a-z][a-z0-9-]* failure$' "$T/c" || true)" -ge 1 || rc=1; for e in executor-unavailable capability-unsupported invocation-failed contract-violation; do grep -qxF -- "error-class $e" "$T/c" || rc=1; done; test "$(grep -c '^effort-mechanism ' "$T/c" || true)" -ge 2 || rc=1; grep -qxF -- 'effort-mechanism none' "$T/c" || rc=1; test "$(grep '^effort-mechanism ' "$T/c" | grep -vxF -e 'effort-mechanism none' | grep -c . || true)" -ge 1 || rc=1; for r in tech-lead pm-spec engineer qa-verifier codex-reviewer ui-designer; do test "$(grep -cE "^role-board-authority $r (writes|proposes|none)$" "$T/c" || true)" = "1" || rc=1; done; for r in scrum-master triage-orchestrator drift-evaluator; do test "$(grep -cE "^role-board-authority $r " "$T/c" || true)" = "0" || rc=1; done; test "$(grep -c '^role-board-authority ' "$T/c" || true)" = "6" || rc=1; mut() { cmp -s "$CT" "$T/m" && rc=1; bash bin/check-adapter.sh --contract "$T/m" --print-contract >/dev/null 2>"$T/e"; test "$?" = "$1" || rc=1; grep -q -- "$2" "$T/e" || rc=1; }; test "$(grep -cxF -- 'retired-channel board' "$T/c" || true)" = "1" || rc=1; grep -v '^error-class ' "$CT" > "$T/m"; mut 2 'contract-incomplete'; { grep -v '^error-class capability-unsupported' "$CT"; printf '%s\n' 'error-class zzzarbitrary'; } > "$T/m"; mut 2 'contract-incomplete'; grep -v '^effort-mechanism none$' "$CT" > "$T/m"; mut 2 'contract-incomplete'; grep -v '^status-value ' "$CT" > "$T/m"; mut 2 'contract-incomplete'; { cat "$CT"; printf '%s\n' 'status-value zzzsecond success'; } > "$T/m"; mut 2 'contract-incomplete'; grep -v '^retired-channel ' "$CT" > "$T/m"; mut 2 'contract-incomplete'; sed -E 's/^schema[[:space:]]+[0-9]+/schema 99/' "$CT" > "$T/m"; mut 2 'contract-malformed'; sv=$(grep -m1 -E '^status-value [a-z][a-z0-9-]* success$' "$T/c" | awk '{print $2}'); test -n "$sv" || rc=1; { cat "$CT"; printf 'status-value %s failure\n' "$sv"; } > "$T/m"; mut 2 'contract-malformed'; { cat "$CT"; printf '%s\n' 'channel board'; } > "$T/m"; mut 2 'retired-channel-declared'; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task makes a seventh role bindable, at which point the `6` here and the enumerated role set describe the v2.0.0 initial cut rather than the shipped role set, and this criterion must be re-measured rather than widened.
- [ ] **AC3** The envelope already carries what telemetry will need, so T-1057 does not have to reopen the contract. `docs/loop-engineering/task-envelope.md` carries exactly one `## Telemetry readiness` heading; the section is non-empty; and it names, as literal tokens, the envelope fields that supply #203 design point 4's recorded values — `role`, `resolved-binding`, `adapter-version`, `usage` and `error-class` — together with the words `provider`, `model` and `effort`, which `resolved-binding` is the carrier for. Positive control: the extracted section is asserted non-empty before any token is searched for, so a mis-typed heading cannot read as a clean pass.
  - check: rc=0; D=docs/loop-engineering/task-envelope.md; test -r "$D" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055c.XXXXXX") || exit 1; test "$(grep -cxF -- '## Telemetry readiness' "$D" || true)" = "1" || rc=1; awk '/^## Telemetry readiness$/{f=1;next} f&&/^## /{exit} f' "$D" > "$T/sec"; test -s "$T/sec" || rc=1; for t in role resolved-binding adapter-version usage error-class provider model effort; do grep -qF -- "$t" "$T/sec" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC4** Both shipped adapter definitions validate, declare an identity consistent with T-1054's shipped allowlist, and cover every contract-declared field. For each of `claude-cli` and `codex-cli`: `templates/adapters/<token>.txt` is readable, `bash bin/check-adapter.sh --adapter <token>` exits `0`, the file declares `adapter <token>` matching its own basename, and it declares the provider T-1054's allowlist pairs with that adapter (`claude` and `codex` respectively). Every `field` token in the canonical contract has a `carries <field> <channel>` row in each definition, with the channel a bare lower-case token. Field coverage is enforced, not merely present: with the two shipped definitions copied to a scratch directory and one `carries` row deleted from the copy, `--definitions <scratch> --adapter claude-cli` is refused `1` `field-coverage-gap`. Three further properties are enforced, each closing a case round 1 reproduced as passing. **Set completeness in the whole-directory mode**: the default mode validates the file *set* against T-1054's allowlist, not merely whatever files are present, so a definitions directory holding only `claude-cli.txt` (the shape an accidental deletion leaves) is refused `2` `definition-missing` rather than validating — v1's behaviour here falsified this spec's own `## Input space` claim that an absent definition fails closed, which held for the per-adapter mode and for the binding cross-check **as those modes then stood** but not for the mode AC2 and the CI dogfood step actually exercise. **No stray definition**: a definitions directory additionally holding a file whose basename is not an allowlisted adapter token is refused `1` `unknown-adapter`. **No vacuous coverage**: a `carries` row placing a `required` field on the `not-carried` channel is refused `1` `field-coverage-gap`, so an adapter cannot declare every required `in` field uncarried and still validate; `not-carried` stays legal for `conditional` fields — the case DP13's `authority-channel-conflict` join **once** covered, whose enforcement was carved out with the binding cross-check (DP18) and which now survives only as the byte-frozen normative statement **AC6** pins. Positive controls: the unmutated scratch copy exits `0` before the mutation is judged, the two-file scratch directory validates `0` before the stray file is added to it, and every mutated file is asserted to differ from the shipped one.
  - check: rc=0; test -r bin/check-adapter.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055d.XXXXXX") || exit 1; for a in claude-cli codex-cli; do test -r "templates/adapters/$a.txt" || exit 1; done; bash bin/check-adapter.sh --print-contract > "$T/c" 2>/dev/null || rc=1; grep '^field ' "$T/c" | awk '{print $2}' > "$T/f"; test -s "$T/f" || rc=1; for a in claude-cli codex-cli; do bash bin/check-adapter.sh --adapter "$a" >/dev/null 2>&1 || rc=1; grep -qxF -- "adapter $a" "templates/adapters/$a.txt" || rc=1; while IFS= read -r fl; do [ -n "$fl" ] || continue; test "$(grep -cE "^carries $fl [a-z][a-z0-9-]*$" "templates/adapters/$a.txt" || true)" = "1" || rc=1; done < "$T/f"; done; grep -qxF -- 'provider claude' templates/adapters/claude-cli.txt || rc=1; grep -qxF -- 'provider codex' templates/adapters/codex-cli.txt || rc=1; mkdir -p "$T/defs" || rc=1; cp templates/adapters/claude-cli.txt templates/adapters/codex-cli.txt "$T/defs/" || rc=1; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>&1 || rc=1; one=$(head -n1 "$T/f"); test -n "$one" || rc=1; grep -v "^carries $one " "$T/defs/claude-cli.txt" > "$T/m" && mv "$T/m" "$T/defs/claude-cli.txt"; cmp -s templates/adapters/claude-cli.txt "$T/defs/claude-cli.txt" && rc=1; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'field-coverage-gap' "$T/e" || rc=1; mkdir -p "$T/d2" || rc=1; cp templates/adapters/claude-cli.txt "$T/d2/" || rc=1; bash bin/check-adapter.sh --definitions "$T/d2" >/dev/null 2>"$T/e"; test "$?" = "2" || rc=1; grep -q -- 'definition-missing' "$T/e" || rc=1; mkdir -p "$T/d3" || rc=1; cp templates/adapters/claude-cli.txt templates/adapters/codex-cli.txt "$T/d3/" || rc=1; bash bin/check-adapter.sh --definitions "$T/d3" >/dev/null 2>&1 || rc=1; sed 's/^adapter claude-cli$/adapter zzz-cli/' templates/adapters/claude-cli.txt > "$T/d3/zzz-cli.txt" || rc=1; bash bin/check-adapter.sh --definitions "$T/d3" >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'unknown-adapter' "$T/e" || rc=1; mkdir -p "$T/d4" || rc=1; cp templates/adapters/claude-cli.txt templates/adapters/codex-cli.txt "$T/d4/" || rc=1; req=$(grep '^field ' "$T/c" | awk '$4=="required"{print $2; exit}'); test -n "$req" || rc=1; sed -E "s/^carries $req .*/carries $req not-carried/" "$T/d4/claude-cli.txt" > "$T/mm" && mv "$T/mm" "$T/d4/claude-cli.txt"; cmp -s templates/adapters/claude-cli.txt "$T/d4/claude-cli.txt" && rc=1; bash bin/check-adapter.sh --definitions "$T/d4" --adapter claude-cli >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'field-coverage-gap' "$T/e" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC5** The `effort` capability declaration has a fixed shape and its internal consistency is enforced. Each shipped definition carries exactly one `capability effort <supported|unsupported>` row and exactly one `effort-mechanism <token>` row whose token appears as an `effort-mechanism` row in the canonical contract. The consistency rule is measured on four scratch mutations rather than asserted in prose: a definition declaring `supported` with zero `effort-value` rows is refused `1` `capability-inconsistent`; one declaring `supported` with `effort-mechanism none` is refused `1` `capability-inconsistent`; one declaring `unsupported` with an `effort-value` row is refused `1` `capability-inconsistent`; and one whose `effort-mechanism` token is absent from the contract's vocabulary is refused `1` `unknown-effort-mechanism`. Positive controls: every scratch definition is built from the same generator and the unmutated one exits `0` first, so a generator that produced an always-invalid file could not satisfy this criterion.
  - check: rc=0; test -r bin/check-adapter.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055e.XXXXXX") || exit 1; bash bin/check-adapter.sh --print-contract > "$T/c" 2>/dev/null || rc=1; test -s "$T/c" || rc=1; for a in claude-cli codex-cli; do test "$(grep -cE '^capability effort (supported|unsupported)$' "templates/adapters/$a.txt" || true)" = "1" || rc=1; test "$(grep -c '^effort-mechanism ' "templates/adapters/$a.txt" || true)" = "1" || rc=1; em=$(grep -m1 '^effort-mechanism ' "templates/adapters/$a.txt" | awk '{print $2}'); test -n "$em" || rc=1; grep -qxF -- "effort-mechanism $em" "$T/c" || rc=1; done; sv=$(grep -m1 '^schema ' "$T/c" | awk '{print $2}'); ch=$(grep '^channel ' "$T/c" | awk '{print $2}' | grep -vx 'not-carried' | head -n1); gm=$(grep '^effort-mechanism ' "$T/c" | awk '{print $2}' | grep -v '^none$' | head -n1); test -n "$sv" || rc=1; test -n "$ch" || rc=1; test -n "$gm" || rc=1; mkdir -p "$T/defs" || rc=1; gen() { { printf '%s\n' "envelope-schema $sv" 'adapter claude-cli' 'provider claude' 'adapter-version 1' "capability effort $1" "effort-mechanism $2"; if [ -n "$3" ]; then printf 'effort-value %s\n' "$3"; fi; grep '^field ' "$T/c" | awk -v c="$ch" '{print "carries " $2 " " c}'; } > "$T/defs/claude-cli.txt"; cp templates/adapters/codex-cli.txt "$T/defs/codex-cli.txt"; }; gen supported "$gm" high; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>&1 || rc=1; gen supported "$gm" ""; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'capability-inconsistent' "$T/e" || rc=1; gen supported none high; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'capability-inconsistent' "$T/e" || rc=1; gen unsupported none high; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'capability-inconsistent' "$T/e" || rc=1; gen supported zzz-not-a-mechanism high; bash bin/check-adapter.sh --definitions "$T/defs" --adapter claude-cli >/dev/null 2>"$T/e"; test "$?" = "1" || rc=1; grep -q -- 'unknown-effort-mechanism' "$T/e" || rc=1; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: the Codex CLI or the Claude harness changes its effort/reasoning mechanism or the set of values it accepts, at which point the shipped definitions' `effort-mechanism` and `effort-value` rows no longer describe the provider and must be re-measured rather than reasoned about. Moved here from AC6 in v4, when that criterion stopped measuring effort values and this one became the nearest criterion whose subject the event actually invalidates.
- [ ] **AC6** The two rules the carved-out cross-check enforced survive the drop as byte-frozen normative statements, so carving out the enforcement does not quietly carry off the rules with it (DP18). `docs/loop-engineering/task-envelope.md` carries exactly **two** lines beginning `- normative: `, and each one's remainder, after that prefix is stripped, is **exactly** the sentence given below — equality locks, not containment searches, so a negated or historically-framed restatement cannot satisfy them. The fail-closed effort rule: `An adapter presented an effort value it does not declare fails closed: it returns a non-success status with error-class capability-unsupported and invokes nothing, never silently ignoring the value and never substituting a default. An unset effort means the provider or model default.` The board-transition authority rule: `Whether the board is written during an invocation is decided solely by the bound role, from its role-board-authority value, and never by the adapter; binding a role whose authority is writes or proposes to an adapter that declares no return path for board-transition is a contradiction this contract forbids.` The document additionally carries exactly one `## Board-transition authority` heading whose section names all six inner-loop roles, and the token `capability-unsupported` appears in the document, so the error class the runtime rule names is the one the contract declares. Finally — the clause that keeps this criterion honest about the carve-out — the document contains **zero** occurrences of `--binding`: a document still describing a mode this task removed would be exactly the stale-prose class round 2 caught, and the rules must read as inherited obligations rather than as something already enforced here. Positive controls: the document is asserted readable and non-empty and the authority section non-empty before any clause is judged, and the two `- normative: ` lines are counted before either is compared, so a document carrying none of them cannot pass by matching nothing.
  - check: rc=0; D=docs/loop-engineering/task-envelope.md; test -r "$D" || exit 1; test -s "$D" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055f.XXXXXX") || exit 1; test "$(grep -c '^- normative: ' "$D" || true)" = "2" || rc=1; n1=$(grep -m1 -- '^- normative: An adapter presented' "$D" | sed -e 's/^- normative: //'); test "$n1" = "An adapter presented an effort value it does not declare fails closed: it returns a non-success status with error-class capability-unsupported and invokes nothing, never silently ignoring the value and never substituting a default. An unset effort means the provider or model default." || rc=1; n2=$(grep -m1 -- '^- normative: Whether the board' "$D" | sed -e 's/^- normative: //'); test "$n2" = "Whether the board is written during an invocation is decided solely by the bound role, from its role-board-authority value, and never by the adapter; binding a role whose authority is writes or proposes to an adapter that declares no return path for board-transition is a contradiction this contract forbids." || rc=1; test "$(grep -cxF -- '## Board-transition authority' "$D" || true)" = "1" || rc=1; awk '/^## Board-transition authority$/{f=1;next} f&&/^## /{exit} f' "$D" > "$T/ba"; test -s "$T/ba" || rc=1; for r in tech-lead pm-spec engineer qa-verifier codex-reviewer ui-designer; do grep -qF -- "$r" "$T/ba" || rc=1; done; grep -qF -- 'capability-unsupported' "$D" || rc=1; test "$(grep -c -- '--binding' "$D" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC7** The `--binding` cross-check mode is gone from every surface it reached, measured as an absence rather than asserted as an intention (DP18). **This is a removal, not a never-add**: the v3 implementation of the mode, its three tokens, its fixture cases and its CI dogfood command are all in the tree as this criterion is written, so this criterion is red until they are deleted and green only then. The literal `--binding` returns **zero** files from `bin/*.sh`, `skills/*/SKILL.md`, `agents/*.md` and `tests/*/run.sh`, and appears **zero** times in `.github/workflows/check-handoff.yml` — the workflow being named explicitly because the mode had a dogfood command there and a CI step is a production path by any reading. Each of the three carved-out tokens — `effort-unsupported`, `authority-channel-conflict` and `binding-invalid` — likewise returns zero files from `bin/*.sh` and `tests/*/run.sh`, so the refusal vocabulary shrinks with the mode instead of leaving orphaned branches no criterion reaches. Note the scoping, which is deliberate and matches AC12's: only executable and CI surfaces are searched, so this spec, the board, the review record and the successor issue remain free to discuss the mode and its tokens by name — a carve-out that could not be described in prose would be unauditable. Positive controls: the same search form applied to `--config` over the identical four-surface pathspec returns at least one file, and the workflow is asserted readable, non-empty and to name `check-adapter` before its absence clause is judged, so a search that read nothing and a mistyped workflow path both read as failures rather than as clean absences.
  - check: rc=0; W=.github/workflows/check-handoff.yml; test -r bin/check-adapter.sh || exit 1; test -r "$W" || exit 1; test -s "$W" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055g.XXXXXX") || exit 1; grep -q -- 'check-adapter' "$W" || rc=1; git grep -l -e '--config' -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' 'tests/*/run.sh' > "$T/pc"; test "$(grep -c . "$T/pc" || true)" -ge 1 || rc=1; git grep -l -e '--binding' -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' 'tests/*/run.sh' > "$T/bb"; test "$(grep -c . "$T/bb" || true)" = "0" || rc=1; test "$(grep -c -- '--binding' "$W" || true)" = "0" || rc=1; for t in effort-unsupported authority-channel-conflict binding-invalid; do git grep -l -e "$t" -- 'bin/*.sh' 'tests/*/run.sh' > "$T/tk"; test "$(grep -c . "$T/tk" || true)" = "0" || rc=1; done; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC8** The contract's two shipped forms agree, at table level and tuple level, measured by this criterion itself. The `--doc` mode that v2 put in the checker was carved out to a successor issue when the pre-commitment trigger fired (DP16 v3), so this criterion performs the comparison rather than delegating it — strictly stronger than v1, which compared field **names** only, and weaker than v2 in exactly one stated way: it is checked when this spec's criteria are checked, not on every CI run. **Table level, by construction**: the document carries the canonical contract verbatim between the marker lines `<!-- BEGIN contract-canon -->` and `<!-- END contract-canon -->`, each occurring exactly once and placed *inside* a fenced block so no fence line falls between them, and that region is **byte-identical** to `--print-contract`'s output — covering `field`, `channel`, `retired-channel`, `status-value`, `error-class`, `effort-mechanism` and `role-board-authority` alike, and covering any directive a later task adds, with nothing to extend. **Tuple level, per field**: each field is documented in the `## Envelope fields` section on its own physical line of the fixed shape `- **<field>** — <in|out>, <required|conditional> — <prose>`; the documented field set equals the registry's; **exactly one bullet exists per field**, so a second contradicting bullet for the same field cannot hide behind the first (the leak round 2 found in the carved-out mode, closed here by construction rather than inherited); each bullet's direction and requiredness match that field's registry row; and every `conditional` field's line carries the phrase `required when` on that same physical line. **Prose level, scoped**: the `## The generalized invocation pattern` section — whose subject is which channels carry which fields, and which is the section round 2 found still calling "the board itself" a valid return channel while this same document's DP13 section declared that channel retired — contains **no standalone occurrence of a retired channel token**. The scoping is deliberate and is what keeps this from being an open-ended phrase hunt: one named section, whose job is naming channels, may not name a retired one; `board-transition` and every other hyphenated compound is unaffected, since only the standalone token is matched. Positive controls: both derived field sets and the extracted canon region are asserted non-empty before any comparison, the invocation-pattern section is asserted non-empty before its absence clause is judged, and the registry is asserted to declare at least one retired channel, so an empty retired set cannot satisfy that clause vacuously.
  - check: rc=0; D=docs/loop-engineering/task-envelope.md; test -r "$D" || exit 1; test -r bin/check-adapter.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055h.XXXXXX") || exit 1; bash bin/check-adapter.sh --print-contract > "$T/c" 2>/dev/null || rc=1; test -s "$T/c" || rc=1; test "$(grep -c 'BEGIN contract-canon' "$D" || true)" = "1" || rc=1; test "$(grep -c 'END contract-canon' "$D" || true)" = "1" || rc=1; awk '/BEGIN contract-canon/{f=1;next} /END contract-canon/{f=0} f' "$D" > "$T/canon"; test -s "$T/canon" || rc=1; cmp -s "$T/canon" "$T/c" || rc=1; test "$(grep -cxF -- '## Envelope fields' "$D" || true)" = "1" || rc=1; awk '/^## Envelope fields$/{f=1;next} f&&/^## /{exit} f' "$D" > "$T/sec"; test -s "$T/sec" || rc=1; grep -oE '^- \*\*[a-z][a-z0-9-]*\*\*' "$T/sec" | sed -E 's/^- \*\*(.*)\*\*$/\1/' > "$T/dall"; LC_ALL=C sort -u "$T/dall" > "$T/doc"; grep '^field ' "$T/c" | awk '{print $2}' | LC_ALL=C sort -u > "$T/reg"; test -s "$T/doc" || rc=1; test -s "$T/reg" || rc=1; cmp -s "$T/doc" "$T/reg" || rc=1; test "$(grep -c . "$T/dall" || true)" = "$(grep -c . "$T/doc" || true)" || rc=1; grep '^field ' "$T/c" | awk '{print $2, $3, $4}' > "$T/ftuple"; test -s "$T/ftuple" || rc=1; while IFS=' ' read -r fn fd fq; do [ -n "$fn" ] || continue; test "$(grep -cE "^- \*\*$fn\*\* — $fd, $fq — " "$T/sec" || true)" = "1" || rc=1; if [ "$fq" = "conditional" ]; then grep -E "^- \*\*$fn\*\*" "$T/sec" | grep -q -- 'required when' || rc=1; fi; done < "$T/ftuple"; grep '^retired-channel ' "$T/c" | awk '{print $2}' > "$T/rt"; test -s "$T/rt" || rc=1; test "$(grep -cxF -- '## The generalized invocation pattern' "$D" || true)" = "1" || rc=1; awk '/^## The generalized invocation pattern$/{f=1;next} f&&/^## /{exit} f' "$D" > "$T/ip"; test -s "$T/ip" || rc=1; while IFS= read -r rt; do [ -n "$rt" ] || continue; test "$(grep -cE "(^|[^a-z-])$rt([^a-z-]|\$)" "$T/ip" || true)" = "0" || rc=1; done < "$T/rt"; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC9** The envelope is a contract and not a serialization protocol, proved as a negative. `bash bin/check-adapter.sh --help` exits `0`, names `--adapter`, and contains no occurrence of the string `--envelope`; the script's own source contains no occurrence of that string either; and an invocation passing a flag beginning `--envelope` is refused `2` `usage` rather than accepted — there is no envelope-instance mode to reach. The document states the boundary in a fixed, byte-frozen form: a line beginning `- boundary: ` whose remainder, after that prefix is stripped, is **exactly** the sentence `The task envelope is a documented contract for what adapters receive and return, not a new serialization protocol: the substrate (files and the board) remains the actual interchange.` — an equality lock rather than a containment search, so a negated or historically-framed sentence embedding the phrase cannot satisfy it. Positive control: the help output is asserted non-empty and to name a mode this script really has before any absence clause is judged. **The mode named here is `--adapter` as of v5, corrected from `--binding`**: that clause was byte-identical from v3 and had only ever passed as a side effect of the carved-out mode's own usage paragraph, so after v4's removal it made this criterion and **AC7** mutually unsatisfiable — no implementation could both delete every `--binding` occurrence from `bin/*.sh` and keep it in a `--help` output that prints the script's own header. `--adapter` is chosen because it survives the removal, is a real mode, and carries no substring ambiguity (the script's own filename spells `-adapter` with one hyphen, so a two-hyphen match can only be the flag); it also matches the suite's own `ca-help-sane` case. The clause's substance is unchanged — the help text is real and names a mode that exists, which is what keeps the `--envelope` absence clauses from passing against empty output.
  - check: rc=0; D=docs/loop-engineering/task-envelope.md; test -r "$D" || exit 1; test -r bin/check-adapter.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055i.XXXXXX") || exit 1; bash bin/check-adapter.sh --help > "$T/h" 2>&1 || rc=1; test -s "$T/h" || rc=1; grep -q -- '--adapter' "$T/h" || rc=1; test "$(grep -c -- '--envelope' "$T/h" || true)" = "0" || rc=1; test "$(grep -c -- '--envelope' bin/check-adapter.sh || true)" = "0" || rc=1; bash bin/check-adapter.sh --envelope "$T/nothing" >/dev/null 2>"$T/e"; test "$?" = "2" || rc=1; grep -q -- 'usage' "$T/e" || rc=1; test "$(grep -c '^- boundary: ' "$D" || true)" = "1" || rc=1; b=$(grep -m1 '^- boundary: ' "$D" | sed -e 's/^- boundary: //'); test "$b" = "The task envelope is a documented contract for what adapters receive and return, not a new serialization protocol: the substrate (files and the board) remains the actual interchange." || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC10** The contract and the definitions are the plugin's, never the adopter's, under both substitution topologies. A decoy `templates/task-envelope.txt` and a decoy `templates/adapters/claude-cli.txt` planted in a scratch working directory do not change what the real script reads: run from that directory by absolute path, `--print-contract` exits `0`, its output is byte-identical to the output produced from the repository root, and the decoy's distinguishing token appears nowhere in it. The same holds under the **ancestor-directory symlink** topology a T-1054 review round reproduced against the sibling checker: a scratch tree whose `bin` is a symlink to this repository's real `bin/`, carrying its own decoy `templates/`, invoked as a relative `bin/check-adapter.sh` from inside that tree, still emits the byte-identical plugin contract. Positive controls: the decoy files are asserted readable and to contain the decoy token, and the reference output from the repository root is asserted non-empty, before either comparison is judged.
  - check: rc=0; test -r bin/check-adapter.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055j.XXXXXX") || exit 1; R=$(pwd); bash bin/check-adapter.sh --print-contract > "$T/c1" 2>/dev/null || rc=1; test -s "$T/c1" || rc=1; mkdir -p "$T/plain/templates/adapters" || rc=1; printf '%s\n' 'schema 1' 'field zzzdecoy in required' > "$T/plain/templates/task-envelope.txt" || rc=1; printf '%s\n' 'adapter zzzdecoy-cli' > "$T/plain/templates/adapters/claude-cli.txt" || rc=1; test -r "$T/plain/templates/task-envelope.txt" || rc=1; grep -q 'zzzdecoy' "$T/plain/templates/task-envelope.txt" || rc=1; ( cd "$T/plain" && bash "$R/bin/check-adapter.sh" --print-contract > "$T/c2" 2>/dev/null ); test "$?" = "0" || rc=1; test -s "$T/c2" || rc=1; cmp -s "$T/c1" "$T/c2" || rc=1; test "$(grep -c 'zzzdecoy' "$T/c2" || true)" = "0" || rc=1; mkdir -p "$T/ad/templates/adapters" || rc=1; ln -s "$R/bin" "$T/ad/bin" || rc=1; test -L "$T/ad/bin" || rc=1; cp "$T/plain/templates/task-envelope.txt" "$T/ad/templates/task-envelope.txt" || rc=1; cp "$T/plain/templates/adapters/claude-cli.txt" "$T/ad/templates/adapters/claude-cli.txt" || rc=1; ( cd "$T/ad" && bash bin/check-adapter.sh --print-contract > "$T/c3" 2>/dev/null ); test "$?" = "0" || rc=1; test -s "$T/c3" || rc=1; cmp -s "$T/c1" "$T/c3" || rc=1; test "$(grep -c 'zzzdecoy' "$T/c3" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC11** T-1054's shipped surface is intact: no third adapter, and its own gates still green. `templates/binding-adapters.txt` carries exactly `2` rows among its non-comment, non-blank lines, and they are `claude-cli claude` and `codex-cli codex` — the initial cut's allowlist, unextended (DP8). `templates/binding-template.conf` still carries exactly `6` lines beginning `bind` followed by whitespace, with each of the six inner-loop roles as the role field of exactly one and none of the three advisory roles present as a role field at all — the properties T-1054's own AC2 asserts, re-derived here rather than quoted, so the conditional `effort`-field edit DP7 permits cannot silently take anything else with it. `bash bin/check-binding.sh --config templates/binding-template.conf` exits `0` and `bash tests/check-binding/run.sh` exits `0`. Positive control: the specimen is asserted non-empty and its `schema` line present exactly once before any `bind`-row assertion is made.
  - check: rc=0; A=templates/binding-adapters.txt; C=templates/binding-template.conf; for f in "$A" "$C" bin/check-binding.sh tests/check-binding/run.sh; do test -r "$f" || exit 1; done; test -s "$C" || rc=1; test "$(grep -cE '^schema[[:space:]]' "$C" || true)" = "1" || rc=1; nr=$(grep -vE '^[[:space:]]*(#|$)' "$A" | grep -c . || true); test "$((10#$nr))" = "2" || rc=1; grep -qE '^claude-cli[[:space:]]+claude[[:space:]]*$' "$A" || rc=1; grep -qE '^codex-cli[[:space:]]+codex[[:space:]]*$' "$A" || rc=1; test "$(grep -cE '^bind[[:space:]]' "$C" || true)" = "6" || rc=1; for r in tech-lead pm-spec engineer qa-verifier codex-reviewer ui-designer; do test "$(grep -cE "^bind[[:space:]]+$r[[:space:]]" "$C" || true)" = "1" || rc=1; done; for r in scrum-master triage-orchestrator drift-evaluator; do test "$(grep -cE "^bind[[:space:]]+$r[[:space:]]" "$C" || true)" = "0" || rc=1; done; bash bin/check-binding.sh --config "$C" >/dev/null 2>&1 || rc=1; bash tests/check-binding/run.sh >/dev/null 2>&1 || rc=1; test "$rc" -eq 0
  - stale-at: a task adds a row to `templates/binding-adapters.txt` or makes a seventh role bindable, at which point the `2` and the `6` here describe the v2.0.0 initial cut rather than the shipped sets, and this criterion must be re-measured rather than widened.
- [ ] **AC12** No production path forwards a testing affordance, measured over the surfaces production paths live on (issue #221, DP9). Searching only `bin/*.sh`, `skills/*/SKILL.md` and `agents/*.md` — the three surfaces that carry executable or agent-directed instructions, deliberately not the whole tree, so a spec, a board entry or a review discussing the flag by name is free to do so: the registry-override flag T-1054's validator documents as a testing affordance appears in **exactly one** file, `bin/check-binding.sh`, the checker that defines it; and each of `--definitions` and `--contract` appears in **exactly one** file, `bin/check-adapter.sh`, the checker that defines them. `--doc` is not on that list, and its absence is **measured rather than asserted**: the carve-out (DP16 v3) is a **removal on this branch, not a never-add** — the v2 implementation of the mode is in the tree as this criterion is written — so a fourth search must return **zero** files, over those three surfaces **plus every `tests/*/run.sh`**, since v2 put `--doc` cases in this task's own suite. The widened pathspec is correct for an absence claim and would be wrong for the exactly-one claims above, which must not see the suite's legitimate use of `--definitions` and `--contract`; that is why the two searches are scoped differently rather than sharing one pathspec. Positive controls: the same search form applied to `--config` returns at least one file over `bin/*.sh`, guarding the exactly-one clauses' pathspec, **and** at least one file over the widened pathspec, guarding the absence clause's own — so a search that read nothing cannot satisfy either.
  - check: rc=0; test -r bin/check-adapter.sh || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055k.XXXXXX") || exit 1; git grep -l -e '--config' -- 'bin/*.sh' > "$T/pc"; test "$(grep -c . "$T/pc" || true)" -ge 1 || rc=1; git grep -l -e '--adapters' -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' > "$T/a"; test "$(grep -c . "$T/a" || true)" = "1" || rc=1; grep -qxF -- 'bin/check-binding.sh' "$T/a" || rc=1; for fl in --definitions --contract; do git grep -l -e "$fl" -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' > "$T/d"; test "$(grep -c . "$T/d" || true)" = "1" || rc=1; grep -qxF -- 'bin/check-adapter.sh' "$T/d" || rc=1; done; git grep -l -e '--config' -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' 'tests/*/run.sh' > "$T/pc2"; test "$(grep -c . "$T/pc2" || true)" -ge 1 || rc=1; git grep -l -e '--doc' -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' 'tests/*/run.sh' > "$T/dd"; test "$(grep -c . "$T/dd" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC13** The fixture suite passes, is shellcheck-clean, and its CI wiring exists and is asserted by the suite itself. `bash tests/check-adapter/run.sh` exits `0`. Where `shellcheck` is installed locally, `shellcheck bin/check-adapter.sh tests/check-adapter/run.sh` exits `0`; where it is not, the criterion still requires the CI wiring, which is what enforces it. `.github/workflows/check-handoff.yml` names both `bin/check-adapter.sh` and `tests/check-adapter/run.sh` in its shellcheck argument list, carries a step running `bash tests/check-adapter/run.sh`, and carries a dogfood step running `bash bin/check-adapter.sh` against the shipped contract and definitions. No `--doc` step is required or permitted: that mode is carved out (DP16 v3), and two-form agreement is AC8's to measure. `tests/check-adapter/run.sh` itself asserts that wiring (it names the workflow file), so a future edit that drops the suite from CI fails the suite rather than passing silently. Positive control: the workflow file is asserted readable and non-empty before any of its content is searched.
  - check: rc=0; W=.github/workflows/check-handoff.yml; for f in tests/check-adapter/run.sh bin/check-adapter.sh "$W"; do test -r "$f" || exit 1; done; test -s "$W" || rc=1; bash tests/check-adapter/run.sh >/dev/null 2>&1 || rc=1; if command -v shellcheck >/dev/null 2>&1; then shellcheck bin/check-adapter.sh tests/check-adapter/run.sh >/dev/null 2>&1 || rc=1; fi; grep -q -- 'bin/check-adapter.sh' "$W" || rc=1; grep -q -- 'tests/check-adapter/run.sh' "$W" || rc=1; grep -q -- 'bash tests/check-adapter/run.sh' "$W" || rc=1; grep -q -- 'bash bin/check-adapter.sh' "$W" || rc=1; test "$(grep -c -- '--doc' "$W" || true)" = "0" || rc=1; grep -q -- 'check-handoff.yml' tests/check-adapter/run.sh || rc=1; test "$rc" -eq 0
- [ ] **AC14** Scope lock. The branch's changed-and-added file set — the tracked half from the commit range `<base>...HEAD` unioned with the untracked half from `git ls-files --others --exclude-standard`, since this spec and this task's records are untracked at freeze time — contains this spec, the six deliverables, `.github/workflows/check-handoff.yml` and the resolved board, and **nothing** outside the allow-list in `## Notes for engineer`. That allow-list includes the paths T-1053 and T-1054 delivered on this stacked branch, enumerated there, which are already changed against this base through no act of this task; every other path is excluded. Byte-identical to the base ref with neither a tracked diff nor an untracked addition: every path under `bin/` that exists in that ref's tree, every path under `templates/` that exists in it, every path under `docs/` that exists in it, and every `skills/*/SKILL.md` other than `skills/run/SKILL.md`. The only untracked addition under `docs/` is `docs/loop-engineering/task-envelope.md`; the only spec differing under `.shell-team/specs/` is this task's own plus T-1053's and T-1054's one file each. **No `agents/*.md` is byte-asserted by this criterion at all, deliberately**: `agents/pm-spec.md` already differs from the base ref on this stacked branch because T-1053's generated-block splice is committed in it, so a whole-set byte-identity claim over `agents/` would be false at freeze time. What bounds the agent prompts instead is the allow-list — any `agents/*.md` other than `agents/pm-spec.md` appearing in the changed-and-added set fails this criterion — and **AC15**'s `bin/check-prompt-sync.sh` clause is what mechanically evidences that `agents/pm-spec.md`'s own generated regions did not move. The three enumerated byte-identity sets are true whole-set claims and were chosen because they are: every file T-1053 and T-1054 added under `bin/` and `templates/` is a **new** file, absent from the base ref's tree by construction, so it is not a member of any of those sets. **This criterion is merge-point-scoped and expected to go stale after merge** — once a later task's files land on the same base ref its expected set no longer describes reality; do not widen its base-ref resolution and do not re-derive it per rework round. Positive controls: the base ref resolves, the changed-and-added set is non-empty, and each of the base ref's `bin/`, `templates/`, `docs/` and `skills/` enumerations yields at least two paths.
  - check: rc=0; git rev-parse --verify --quiet 'develop^{commit}' >/dev/null || exit 1; B=$(git merge-base develop HEAD) || exit 1; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055m.XXXXXX") || exit 1; SELF=.shell-team/specs/T-1055-adapter-envelope.md; { git diff --name-only "$B"...HEAD; git ls-files --others --exclude-standard; } | sort -u > "$T/set"; test -s "$T/set" || rc=1; for f in "$SELF" bin/check-adapter.sh tests/check-adapter/run.sh templates/task-envelope.txt templates/adapters/claude-cli.txt templates/adapters/codex-cli.txt docs/loop-engineering/task-envelope.md .github/workflows/check-handoff.yml .shell-team/todo.md; do grep -qxF -- "$f" "$T/set" || rc=1; done; extra=$(grep -vxF -e "$SELF" -e 'bin/check-adapter.sh' -e 'tests/check-adapter/run.sh' -e 'templates/task-envelope.txt' -e 'templates/adapters/claude-cli.txt' -e 'templates/adapters/codex-cli.txt' -e 'templates/binding-template.conf' -e 'docs/loop-engineering/task-envelope.md' -e '.github/workflows/check-handoff.yml' -e '.shell-team/todo.md' -e '.shell-team/test-recipe.md' -e '.shell-team/provenance/T-1055.md' -e '.shell-team/interventions/T-1055.md' -e 'skills/run/SKILL.md' -e 'agents/pm-spec.md' -e '.shell-team/specs/T-1053-retro-mechanization.md' -e '.shell-team/provenance/T-1053.md' -e '.shell-team/interventions/T-1053.md' -e 'bin/check-binding.sh' -e 'tests/check-binding/run.sh' -e 'templates/binding-adapters.txt' -e '.shell-team/specs/T-1054-binding-config.md' -e '.shell-team/provenance/T-1054.md' -e '.shell-team/interventions/T-1054.md' "$T/set" | grep -v '^\.shell-team/reviews/T-105[345]' | grep -c . || true); test "$((10#$extra))" = "0" || rc=1; for d in bin templates docs skills; do git ls-tree -r --name-only "$B" "$d/" > "$T/l"; test "$(grep -c . "$T/l" || true)" -ge 2 || rc=1; while IFS= read -r p; do case "$p" in ''|skills/run/SKILL.md) continue;; esac; test -z "$(git diff --name-only "$B"...HEAD -- "$p")" || rc=1; test -z "$(git ls-files --others --exclude-standard -- "$p")" || rc=1; done < "$T/l"; done; du=$(git ls-files --others --exclude-standard -- docs | grep -vxF -e 'docs/loop-engineering/task-envelope.md' | grep -c . || true); test "$((10#$du))" = "0" || rc=1; sp=$(git diff --name-only "$B"...HEAD -- .shell-team/specs | grep -vxF -e "$SELF" -e '.shell-team/specs/T-1053-retro-mechanization.md' -e '.shell-team/specs/T-1054-binding-config.md' | grep -c . || true); test "$((10#$sp))" = "0" || rc=1; su=$(git ls-files --others --exclude-standard -- .shell-team/specs | grep -vxF -e "$SELF" | grep -c . || true); test "$((10#$su))" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC15** Records, board and recipe hygiene, plus the one content-level assertion this task makes about a pre-existing surface: `agents/pm-spec.md`'s generated regions are evidenced unmoved by `bin/check-prompt-sync.sh`. `skills/run/SKILL.md` is **not** content-asserted here or anywhere in this spec — it is bounded by AC14's allow-list alone, which is stated rather than left to be inferred. `.shell-team/provenance/T-1055.md` and `.shell-team/interventions/T-1055.md` both exist and are conformant under their own checkers; the resolved board passes `bin/check-handoff.sh` and `bin/check-board-headings.sh` against the base ref; the branch diff is clean under `bin/check-pii-shapes.sh`; every top-level `- [` entry line present in the board's base blob is still present **verbatim** in the working board; and `.shell-team/test-recipe.md` gains at least one line naming `T-1055`, appended below its `## Appended by tasks` heading, with every **non-blank** line the base blob carried still present verbatim (blank lines are excluded from both sides of that comparison deliberately: a blank line in a `grep -vxFf` pattern file matches every input line and would make the survival assertion vacuous). Positive controls: the resolved board path is readable and its base blob non-empty and carrying at least one top-level `- [` line, and the recipe's base blob is non-empty, before either survival comparison is made.
  - check: rc=0; git rev-parse --verify --quiet 'develop^{commit}' >/dev/null || exit 1; B=$(git merge-base develop HEAD) || exit 1; test -n "$B" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; RP=.shell-team/test-recipe.md; test -r "$RP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055n.XXXXXX") || exit 1; if [ -r .shell-team/provenance/T-1055.md ]; then bash bin/check-provenance.sh .shell-team/provenance/T-1055.md >/dev/null || rc=1; else rc=1; fi; if [ -r .shell-team/interventions/T-1055.md ]; then bash bin/check-interventions.sh --task T-1055 -- .shell-team/interventions/T-1055.md >/dev/null || rc=1; else rc=1; fi; bash bin/check-handoff.sh "$BD" >/dev/null || rc=1; bash bin/check-board-headings.sh "$BD" --base "$B" >/dev/null || rc=1; bash bin/check-pii-shapes.sh --base "$B" >/dev/null || rc=1; bash bin/check-prompt-sync.sh >/dev/null || rc=1; git show "$B:$BD" > "$T/bb" || exit 1; test -s "$T/bb" || rc=1; grep '^- \[' "$T/bb" > "$T/bl" || true; test -s "$T/bl" || rc=1; while IFS= read -r l; do [ -n "$l" ] || continue; grep -qxF -- "$l" "$BD" || rc=1; done < "$T/bl"; git show "$B:$RP" > "$T/rb" || exit 1; test -s "$T/rb" || rc=1; grep -v '^[[:space:]]*$' "$RP" > "$T/cur" || true; grep -v '^[[:space:]]*$' "$T/rb" > "$T/bnl" || true; test -s "$T/cur" || rc=1; test -s "$T/bnl" || rc=1; miss=$(grep -vxFf "$T/cur" "$T/bnl" | grep -c . || true); test "$((10#$miss))" = "0" || rc=1; awk '/^## Appended by tasks$/{f=1;next} f' "$RP" > "$T/tail"; test -s "$T/tail" || rc=1; test "$(grep -c 'T-1055' "$T/tail" || true)" -ge 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0
- [ ] **AC16** This task's downstream-impact disclosure is produced by the full-population-diff method `agents/pm-spec.md`'s downstream-impact-inventory item requires, and its population is re-derived rather than transcribed. This spec carries a `## Blast radius` section outside the intent block; that heading occurs exactly once; the section is non-empty; it states the literal `full-population diff` as its method, states the literal command that enumerated the population, names its base-side reading target as a committed-blob read (`git show` or `git worktree add --detach`) rather than the working tree, and carries both a `base:` and a `head:` verdict token. It declares its population on a line of the shape `- population: <n> spec files`, and `<n>` is **equal** to the count re-derived live at check time from `.shell-team/specs/*.md` — a value-independent invariant that re-derives both sides, never an absolute literal typed into the check. Positive control: the live enumeration must yield at least one path, so an empty population cannot satisfy the equality.
  - check: rc=0; SP=.shell-team/specs/T-1055-adapter-envelope.md; test -r "$SP" || exit 1; test -d .shell-team/specs || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055o.XXXXXX") || exit 1; test "$(grep -cxF -- '## Blast radius' "$SP" || true)" = "1" || rc=1; awk '/^## Blast radius$/{f=1;next} f&&/^## /{exit} f' "$SP" > "$T/sec"; test -s "$T/sec" || rc=1; for s in 'full-population diff' 'base:' 'head:'; do grep -qF -- "$s" "$T/sec" || rc=1; done; grep -qE 'git (show|worktree add --detach)' "$T/sec" || rc=1; : > "$T/live"; for f in .shell-team/specs/*.md; do test -r "$f" || { rc=1; continue; }; printf '%s\n' "$f" >> "$T/live"; done; n=$(grep -c . "$T/live" || true); test "$((10#$n))" -ge 1 || rc=1; p=$(grep -m1 -E '^- population: [0-9]+ spec files' "$T/sec" | sed -E 's/^- population: ([0-9]+) spec files.*/\1/'); test -n "$p" || rc=1; if [ -n "$p" ]; then test "$((10#$p))" = "$((10#$n))" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: a task adds or removes a `*.md` file under `.shell-team/specs/`, at which point the declared population no longer describes the corpus this criterion re-derives and the table must be re-measured rather than the criterion widened.
- [ ] **AC17** Runtime, `SKIP` by design — no command can prove a command was run. **This criterion is reported item by item, in this order, one line per item, each naming what was measured and what the result was; a compressed summary of it does not discharge it, and a whole-set claim inside any item carries the enumeration it summarises.** **(a)** A producer-run mutation self-check on scratch copies outside the checkout (never the working tree), each mutation observed red, restored, observed green again: delete one refusal branch from `bin/check-adapter.sh` so its token can never be printed (its owning criterion red); make one refusal exit `0` instead of its stated code (same); accept a definition whose `adapter` token differs from its basename (AC4 red); accept a `carries` row naming a field the contract does not declare (AC4 red); resolve the contract registry from the working directory instead of the script's own directory (AC10 red); revert one `pwd -P` to a logical `pwd` (AC10's ancestor-symlink half red specifically, and the plain-decoy half **not** red — report both, since a mutation that reddens both would mean the two halves are not measuring different topologies); alter one character inside one of the two byte-frozen `- normative: ` sentences in `docs/loop-engineering/task-envelope.md` (AC6 red on that equality lock); reintroduce a single `--binding` occurrence into `bin/check-adapter.sh` (AC7 red on the four-surface absence). These two replace the pre-carve-out pair — a bound effort value absent from the adapter's declared set, and `--binding` proceeding on a config `check-binding.sh` refused — which named behaviour of the cross-check mode DP18 removed, so no scratch copy can perform them any more; what AC6 and AC7 measure today is what these two attack instead. **(b)** The **effort mechanism measurement**, per shipped adapter, reported as a measurement and never as an expectation: for `codex-cli`, the literal command run against the locally installed Codex CLI, that CLI's version verbatim, the mechanism found, and the exact set of values it accepts; for `claude-cli`, what was consulted for the harness's own effort semantics, the mechanism found, and the accepted value set — or, for either, an explicit statement that the provider exposes no per-invocation effort mechanism, which is a valid outcome and is what `capability effort unsupported` records. State separately whether `templates/binding-template.conf` was edited under DP7's second branch, and if so exactly which field of which row changed and to what. **(c)** The full CI-wired step list of `.github/workflows/check-handoff.yml` run locally in workflow order, every step's result stated, any step judged inapplicable named individually with its reason. **(d)** The `## Blast radius` table's production narrated: the literal enumeration command, the population it returned, the scratch tree the base-side verdicts were read from at the base ref's committed blobs, and per differing spec one line stating which criterion changed verdict and why — with the whole-population claim carrying the enumeration behind it, not a blanket sentence. **(e)** The measured 40-hex value of `git merge-base develop HEAD` reported once, with `git merge-base --is-ancestor develop HEAD`'s exit status, and separately whether T-1053's tip **and** T-1054's tip are each ancestors of `HEAD`, so the two-deep stacked-branch premise this spec's scope lock rests on is measured rather than assumed. **(f)** Every relayed premise in `## Assumptions` re-measured with the measured value reported beside it, and any premise that could not be measured from the executing side named as still relayed rather than silently dropped — issue #203's quoted design points and principles, and issue #221's disposition, each stated as confirmed against the issue or as still relayed. **(g)** A per-source report for every `## Summarized sources` entry, stating whether the reporting role opened it and what it confirmed or contradicted about the cited distinction. **(h)** Whether `bin/check-adapter.sh` was confirmed to contain no occurrence of the sibling validator's registry-override flag name **in comments as well as code** (AC12 measures the file as a whole, so a comment mentioning the flag by name fails it) — reported as a measurement, with the substitute wording used where the header needed to refer to that flag. **(i)** Round 1's Blocker reproduction re-run verbatim against the fixed checker on a scratch copy of the installed tree: append a three-token row to that copy's `templates/binding-adapters.txt` and report both checkers' token and exit code side by side, then revert the fix's registry validation on a second scratch copy and report that the same corruption goes back to being accepted — the revert-observation that proves the fix is load-bearing rather than incidental. **(j)** Round 1's AC8 drift reproduction and round 2's duplicate-bullet leak both re-run **on scratch copies, never the working tree**: flip the document's `task-id` bullet from `in` to `out`, and separately insert a second contradicting `task-id` bullet beside the correct one, and report that AC8's own comparison reddens on each — the property that survived the `--doc` carve-out, measured rather than assumed. State `git status --short` as clean before and after. **(k)** Whether any provenance decision recorded in v1 was superseded by a v2 decision — specifically the one that folded an unreadable adapter allowlist into `contract-unreadable`, which DP15 supersedes — named individually with the superseding decision, so the provenance record does not carry two contradicting entries with the later one merely sitting after the earlier. **(l)** Round 2's Blocker reproduction re-run verbatim against the v3 checker, with its revert-observation: build a contract through `--contract` that declares `channel board`, remap a definition's `carries board-transition` onto it, and report the refusal token and exit code — the `proposes`-authority binding step this recipe carried until now is dropped, because the refusal it demonstrates fires when the contract is loaded and DP18 removed the mode that step ran through; then revert the compiled-in retired set on a scratch copy and report that the same construction goes back to validating — which is what distinguishes a structural refusal from a regression assertion on the shipped file, and is the specific claim DP17 makes. **(m)** The **descope** itself, reported as an executed decision rather than described: which pre-commitment trigger fired and on what evidence (the two rounds and the component), which droppable item was dropped, what travelled with it to the successor issue, and that `.shell-team/interventions/T-1055.md` carries the work-abandoned entry its own checker's contract requires. State the successor issue number as recorded on the board, and confirm no `--doc` flag, token or CI step remains anywhere in the tree. **(n)** The **second** descope, reported on the same terms as (m): that round 3's finding fired the trigger a second time, that drop (1) as v3 renumbered it — the `--binding` mode — was executed rather than reopened, what travelled to its successor issue (round 2's V4 findings, round 3's schema-content finding, DP13's join, and DP7's specimen-versus-definitions agreement), and that `.shell-team/interventions/T-1055.md` carries a **second** work-abandoned entry for it. Report the never-dropped re-read explicitly: which of the nine items the drop was checked against, and why the stop-and-return-to-planning branch did not fire. Confirm no `--binding` flag, token, fixture case, CI command or document mention remains anywhere in the tree.
- [ ] **AC18** Two checkers reading one file agree about whether it is well-formed. This criterion is placed last, after the runtime criterion, deliberately: it is new in v2 and appending it keeps every other criterion's number byte-stable across the re-freeze, which is what lets the v1→v2 delta be read as the set of things that actually changed. On a scratch copy of the installed tree (`bin/` and `templates/` copied together, so both checkers resolve the same `$SCRIPT_DIR`-relative allowlist and the real working tree is never written to), each of three corruptions of `templates/binding-adapters.txt` — a three-token row, a row whose adapter token carries an upper-case character, and a row declaring an already-declared adapter token a second time — is refused by **both** `bin/check-adapter.sh` and `bin/check-binding.sh` with the **same** token, `registry-malformed`, at the **same** exit code, `2`; and with the allowlist removed entirely both refuse `registry-unreadable` at `2` (DP15). Each corruption is reverted before the next is applied, so the four cases are independent rather than cumulative. Positive controls: the pristine scratch tree is asserted to pass **both** checkers at exit `0` before any corruption is applied — so a copy that was broken on arrival cannot produce a passing parity result — and every corrupted allowlist is asserted to differ from the pristine one, so an edit that failed to apply cannot masquerade as a refusal.
  - check: rc=0; test -r bin/check-adapter.sh || exit 1; test -r bin/check-binding.sh || exit 1; test -r templates/binding-adapters.txt || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1055p.XXXXXX") || exit 1; mkdir -p "$T/tree" || rc=1; cp -R bin "$T/tree/bin" || rc=1; cp -R templates "$T/tree/templates" || rc=1; printf '%s\n' 'schema 1' 'bind tech-lead claude m1 - claude-cli' 'bind pm-spec claude m1 - claude-cli' 'bind engineer claude m1 - claude-cli' 'bind qa-verifier claude m1 - claude-cli' 'bind ui-designer claude m1 - claude-cli' 'bind codex-reviewer codex m2 - codex-cli' > "$T/cfg" || exit 1; RA="$T/tree/templates/binding-adapters.txt"; test -r "$RA" || rc=1; cp "$RA" "$T/reg.bak" || rc=1; bash "$T/tree/bin/check-adapter.sh" >/dev/null 2>&1 || rc=1; bash "$T/tree/bin/check-binding.sh" --config "$T/cfg" >/dev/null 2>&1 || rc=1; par() { cmp -s "$T/reg.bak" "$RA" && rc=1; bash "$T/tree/bin/check-adapter.sh" >/dev/null 2>"$T/ea"; a=$?; bash "$T/tree/bin/check-binding.sh" --config "$T/cfg" >/dev/null 2>"$T/eb"; b=$?; test "$a" = "$1" || rc=1; test "$b" = "$1" || rc=1; grep -q -- "$2" "$T/ea" || rc=1; grep -q -- "$2" "$T/eb" || rc=1; cp "$T/reg.bak" "$RA" || rc=1; }; { cat "$T/reg.bak"; printf '%s\n' 'claude-cli claude extra-junk'; } > "$RA"; par 2 'registry-malformed'; { cat "$T/reg.bak"; printf '%s\n' 'ZZZCLI claude'; } > "$RA"; par 2 'registry-malformed'; { cat "$T/reg.bak"; printf '%s\n' 'claude-cli codex'; } > "$RA"; par 2 'registry-malformed'; rm -f "$RA"; bash "$T/tree/bin/check-adapter.sh" >/dev/null 2>"$T/ea"; a=$?; bash "$T/tree/bin/check-binding.sh" --config "$T/cfg" >/dev/null 2>"$T/eb"; b=$?; test "$a" = "2" || rc=1; test "$b" = "2" || rc=1; grep -q -- 'registry-unreadable' "$T/ea" || rc=1; grep -q -- 'registry-unreadable' "$T/eb" || rc=1; rm -rf "$T"; test "$rc" -eq 0

## Input space

**Reachable input classes** — what this task's deliverables really read and must handle correctly:

- **The plugin-shipped contract registry and adapter definitions as installed**, reached from any working directory, through a direct invocation, through a `PATH` symlink to the script itself, and through an **ancestor** directory symlink (an adopter's `bin/` symlinked into the plugin's real `bin/` — ordinary vendoring, reproduced against the sibling checker in a T-1054 review round).
- **A decoy `templates/task-envelope.txt` or `templates/adapters/*.txt` in the working directory**, reachable whenever the loop runs in a repository that carries a `templates/` directory of its own — which every adopter of this plugin's conventions might.
- **Hand-authored adapter definitions during development**, and definitions wrong in exactly one way: a directive misspelled, a `carries` row omitted, a channel token guessed, a capability line contradicting its `effort-value` rows, an `adapter` token not matching the file's own name, an `envelope-schema` left at an older version.
- **Binding configs are no longer an input class at all** (DP18). Nothing in this task reads one: the cross-check that did is carved out, so a finding whose only trigger is a binding config's content — an effort disagreement, an authority/channel contradiction, a malformed delegated output — is `out-of-input-space` for this cut and belongs to the successor issue. The shipped specimen still ships and is still validated by its **own** validator, `bin/check-binding.sh` (AC11), which is a different artifact's input space.
- **All three layouts the resolver supports**: the default `.shell-team` base, the legacy `tasks` base, and an explicit `TEAM_RUN_BASE` override.
- **Absent inputs**: no definition file for a bound adapter, an unreadable contract registry, an empty definitions directory. Each is reachable and each fails closed — **in every mode**, including the no-flag whole-directory mode, where v1 accepted a directory missing an allowlisted adapter's file and thereby falsified this very line (round 1). A definitions directory carrying a *stray* file whose basename is not an allowlisted adapter token is reachable the same way and fails closed too.
- **A stray edit to a plugin-shipped file this checker depends on** — a malformed row appended to `templates/binding-adapters.txt`, a contract registry missing a vocabulary the contract requires, a `schema` version bumped without the checker being taught it. These are not hypothetical: DP12's threat model names a stray edit as the thing being defended against, and round 1 reproduced the first of them against the real shipped file. Each fails closed, and the allowlist case fails closed *identically to how `bin/check-binding.sh` fails on the same file* (DP15).
- **A contract document that has drifted from the contract registry** — a field's documented direction or requiredness edited without the registry being edited, a second contradicting bullet added for a field that already has one, a conditional field's condition dropped, a canon-region line stale after a registry change, or prose elsewhere in the document still naming a channel the contract has retired. Reachable from any ordinary edit to either half; AC8 measures all five.
- **A contract registry that re-declares a retired channel token**, whether by an edit to the shipped file or by a registry supplied through the `--contract` testing affordance. Round 2 reproduced the second: it is how a retirement enforced as a property of one file rather than of the checker gets defeated, and it is why DP17 compiles the retired set into the checker.
- **The current working directory being a git repository**, which is the loop's own precondition, with `git` available.
- **The two real provider CLIs as installed on the executing machine**, read once, for the effort-capability measurement `AC17(b)` reports — including the outcome that a provider exposes no per-invocation effort mechanism at all.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect against:

- **An actual executor invocation.** Nothing in this task calls a provider, so a finding whose only trigger is a live invocation, a network failure, a partial response, a timeout, a retry or a concurrent run is `out-of-input-space` for this cut and belongs to T-1056.
- **An envelope instance.** No instance format exists (DP1), so a finding about parsing, escaping, encoding or round-tripping an envelope payload has no artifact to attach to here.
- **A hostile operator.** Replacing a shipped definition inside the plugin's own installation, or overriding a shell builtin to defeat the parser. `CLAUDE.md`'s security invariant makes the operator and the working directory trusted, and `bin/check-durability.sh` and `bin/check-binding.sh` scope their own threat models identically.
- **Environment-variable neutralization, `GIT_*` enumeration, `GIT_DIR` redirection, replace refs, clean filters or LFS.** Named explicitly because two sibling checkers name them, and for the same reason: they belong to a separate hardening issue.
- **Ever-longer or ever-larger synthetic input**: field values beyond the 64-character cap, definitions with hundreds of rows, contract registries with thousands of fields, definition directories with thousands of files. Real shipped and hand-authored data produces none of these; a finding that only fires past the cap is `out-of-input-space`.
- **Non-ASCII tokens** in any declared field, and Unicode homoglyph confusables. Every vocabulary here is closed and ASCII by construction; a homoglyph simply is not the token and is refused as unknown.
- **A NUL byte or invalid UTF-8** inside the contract registry, a definition or a config.
- **Filenames containing a newline, a tab, or a byte `core.quotePath` would C-style-quote**, in any path this task reads or any path a criterion enumerates.
- **A hostile `$TMPDIR`**: an attacker-controlled temp directory, a symlink race against `mktemp -d`, or a permission flip between creation and first write.
- **A concurrent writer** mutating the contract registry, a definition or the working tree mid-run.
- **Adapters beyond the two shipped rows**, including any host-supplied or discovered adapter. Custom adapters are out of scope for the initial cut by the epic's own decision, and a finding whose premise is a third adapter is `out-of-input-space`.
- **A provider CLI version other than the one measured.** The declared capability describes what was measured, with `AC6`'s `- stale-at:` naming the event that invalidates it; a finding that a future CLI version might behave differently is not a defect in this cut.

<!-- END intent-block: T-1055 -->

## Body-to-AC correspondence table

One row per normative directive stated in the body prose, mapped to the criterion that carries it or marked exempt with a reason.

| # | Body directive | Source | Carried by |
|---|---|---|---|
| 1 | The envelope's fourteen fields, their directions and their requiredness are the contract | DP1 table, Goal | **AC2** (registry declares each with its direction), **AC8** (document and registry agree) |
| 2 | Every conditional field's condition is stated, never left implicit | DP1 table, Goal | **AC8** (`required when` on each conditional field's documented line) |
| 3 | The envelope is a contract, not a serialization protocol; nothing here writes, reads or parses an instance | DP1, Goal, Non-goals, epic quote | **AC9** (no `--envelope` flag, refused `usage`; the byte-frozen boundary sentence), **AC14** (no instance file can appear outside the allow-list) |
| 4 | The contract ships in two forms for two readers and the two must not drift | DP2 | **AC8** (set equality, both sides re-derived) |
| 5 | The prose half lives in `docs/loop-engineering/` and ships English-only | DP3, Non-goals | **AC1** (the path is the deliverable named), **AC14** (no other `docs/` addition, so no `*.ja.md` counterpart can appear) |
| 6 | The board-transition authority split is declared per role from a closed vocabulary, measured from each role's own prompt | DP4, epic quote | **AC2** (six rows, closed vocabulary, advisory roles absent) |
| 7 | The measured authority values themselves (`engineer`/`qa-verifier` write, `codex-reviewer` proposes, `ui-designer`/`tech-lead` none) | DP4, `## Summarized sources` | *info-only (not promoted to AC)* — AC2 fixes the vocabulary and the role set; which value each role carries is a reading of that role's shipped prompt, and a criterion pinning the values would freeze a fact about `agents/*.md` that this task is forbidden to edit and a later task may legitimately change. **AC17(g)** is where the reading is reported. |
| 8 | `qa-verifier`'s `tools:` line grants no `Edit`/`Write` while its loop instructs a board update — disclosed, not resolved | `## Summarized sources` | *info-only (not promoted to AC)* — a pre-existing inconsistency in a file this task must not touch; recorded so a later reader finds it already known rather than rediscovering it as a defect of this task. |
| 9 | An adapter definition is declarative: every value a bare token, no command string, argv list, environment assignment or executable path | DP5, epic quote | **AC4** (`carries` channels constrained to bare tokens), **AC5** (capability tokens from the contract's own vocabulary) |
| 10 | How the envelope reaches the executor is declared as a field-to-channel mapping onto a closed vocabulary | DP5 | **AC2** (channel vocabulary declared), **AC4** (a `carries` row per contract field, coverage enforced by refusal) |
| 11 | The concrete invocation shape stays prose and code, not a definition-file value | DP5, Non-goals | *info-only (not promoted to AC)* — an absence of work; AC4's token-class constraint is what makes a command unrepresentable, and AC14's allow-list bounds every file this task may touch. |
| 12 | One definition file per adapter, named for its own token, looked up rather than scanned | DP6 | **AC4** (`adapter <token>` matches the basename; the shipped pair validates by token) |
| 13 | `effort` capability is measured against the real provider, never guessed | DP7, epic quote | **AC17(b)** (the measurement reported as a measurement), **AC5** (the declaration's shape), **AC6**'s `- stale-at:` |
| 14 | The shipped specimen is reconciled to the measurement; only its `effort` field may change, and only under DP7's second branch | DP7, Non-goals | **AC11** (row count, role set and advisory absence unchanged), **AC17(b)** (the edit reported field by field). *Enforcement of the specimen-versus-definitions agreement itself is carved out in v4 (DP18) and inherited by the successor issue — recorded as the drop's stated price rather than silently re-homed.* |
| 15 | An unset effort (`-`) is always accepted, including by an adapter declaring no effort support | DP7, epic quote | **AC6** (byte-frozen `- normative: ` line; the sentence states it) — *enforcement carved out (DP18)* |
| 16 | An effort value the adapter does not declare fails closed, never silently ignored | DP7, Goal, epic quote | **AC6** (byte-frozen `- normative: ` line naming `capability-unsupported` and "never silently ignoring") — *enforcement carved out (DP18)* |
| 17 | The runtime half of the fail-closed effort rule (an adapter returning `capability-unsupported` and invoking nothing) is T-1056's | Goal, Non-goals | **AC2** (the error class exists in the vocabulary so T-1056 need not invent it); the behaviour itself is *info-only* — nothing invokes anything here, so there is no runtime to observe. |
| 18 | No third adapter row; the initial cut is the two allowlisted adapters | DP8, Non-goals, epic quote | **AC11** (exactly two rows, both named) |
| 19 | No edit to `bin/check-binding.sh` or `tests/check-binding/run.sh` and no change to any token or exit code T-1054 shipped | DP8, Non-goals | **AC11** (its suite and dogfood still green), **AC14** (both allow-listed as pre-existing, never as this task's own deliverables) |
| 20 | Issue #221: no production path forwards a testing affordance; `bin/check-adapter.sh` never spells that flag name at all | DP9 | **AC12**, **AC17(h)** |
| 21 | The half of #221 that needs a production caller is T-1056's, recorded here so it is inherited | DP9 | *info-only (not promoted to AC)* — a scope hand-off to a task whose files do not exist; `## Notes for engineer` carries it forward and AC12 is the invariant T-1056 re-asserts. |
| 22 | The cross-check delegates parsing to `check-binding.sh --print-binding` and never re-implements the config grammar | DP10 *(superseded v4)* | **AC7** — retargeted: the mode is carved out (DP18), so AC7 now measures its **absence** and this directive travels to the successor issue as a requirement rather than being carried here |
| 23 | A config the sibling validator refuses is `binding-invalid` at exit 2 with zero bytes on stdout | DP10 *(superseded v4)* | **AC7** (same: absence measured; directive carried by the successor issue) |
| 24 | Resolution is physical (`pwd -P`) from the first commit; neither a cwd decoy nor an ancestor symlink can substitute the registry | DP11 | **AC10** (both topologies), **AC17(a)** (the revert-observation distinguishing the two halves) |
| 25 | `bin/check-durability.sh`'s identical defect is issue #218's and stays untouched | DP11, Non-goals | **AC14** (it is absent from the allow-list, so any edit fails the scope lock) |
| 26 | The threat model is accidents; the operator and working directory are trusted | DP12 | *info-only (not promoted to AC)* — a scoping statement; `## Input space` declares it as the falsifiable boundary, which is where a gate reads it. |
| 27 | Refusals are single tokens from a closed set with fixed exit codes; nothing unevaluable returns success | Goal, `## Refusal matrix` | **AC4**, **AC5**, **AC6**, **AC7**, **AC9** |
| 28 | Pure bash, zero new dependency, shellcheck-clean, index mode `100755`, CI-wired with a dogfood step | Goal | **AC1**, **AC13** |
| 29 | No invocation, no loop integration, no run-time unavailability handling, no migration or scaffolding | Non-goals | **AC14** (nothing under `skills/`, `agents/` or `bin/` other than the new script changes) |
| 30 | No write side anywhere; stdout is the only output channel | Non-goals, Goal | **AC7** (zero bytes on a refusal), **AC14** (no file appears outside the allow-list) |
| 31 | No edit by this task to any `agents/*.md` or `skills/*/SKILL.md` | Non-goals | **AC14**, **AC15** (`check-prompt-sync` green) |
| 32 | No edit to any file T-1053 or T-1054 delivered beyond the three appended-to files and DP7's conditional specimen edit | Non-goals | **AC14** (allow-list), **AC11**, **AC17(e)** |
| 33 | Every red this task produces is disclosed and differenced, never repaired; no merged spec gains a `- stale-at:` | Non-goals | **AC14** (no other spec changes), **AC17(d)** |
| 34 | Records exist and are conformant; no board entry line is lost; the test recipe gains this task's procedure | Non-goals, recipe convention | **AC15** |
| 35 | The blast radius is produced by a full-population diff with base-side verdicts read from committed blobs | pm-spec self-check item | **AC16**, **AC17(d)** |
| 36 | The envelope carries what telemetry will need so T-1057 need not reopen the contract | Goal, epic quote | **AC3**, **AC2** (the five supplying fields declared) |
| 37 | No retro, no acting on fast-follows, no merge, no push | Non-goals | *info-only (not promoted to AC)* — a retro would appear under `.shell-team/retros/`, which AC14's allow-list already excludes; the rest are process constraints outside any criterion this branch can run on itself. |
| 38 | A `carries` row declares a value's return path, never an authority to act | DP13 | **AC2** (no `board` row survives), **AC4** (channels drawn from the contract's vocabulary), **AC8** (the canon lock reddens a re-added `board` row) |
| 39 | Whether the board is written is decided solely by the bound role's `role-board-authority` | DP13 | *info-only (not promoted to AC)* — a definitional statement about what two existing directives mean; what makes it enforceable is the retirement of the `board` channel, which AC2 asserts and AC8's canon lock keeps asserted. |
| 40 | The `board` channel token is removed from the contract vocabulary | DP13 | **AC2** |
| 41 | A `writes`/`proposes` role bound to an adapter declaring `carries board-transition not-carried` is refused | DP13 *(enforcement superseded v4)* | **AC6** (the byte-frozen `- normative: ` line states the prohibition) — the `authority-channel-conflict` join that enforced it is carved out (DP18); the rule survives, its enforcement is inherited |
| 42 | The `status` vocabulary gets a machine form, `status-value <token> <success\|failure>`, with exactly one success member | DP14 | **AC2** (shipped shape plus two mutation refusals) |
| 43 | A `required` field may not be declared `not-carried` | DP14 | **AC4** (`field-coverage-gap`) |
| 44 | The adapter allowlist is validated at full parity with `bin/check-binding.sh`, reusing that checker's own tokens | DP15 | **AC18** |
| 45 | `contract-unreadable` names the envelope contract registry alone; the v1 decision folding the allowlist into it is superseded | DP15 | **AC18** (the allowlist's own tokens are what both checkers emit), **AC17(k)** (the superseded provenance decision named) |
| 46 | No path-override flag for the allowlist; parity is tested on a scratch copy of the installed tree | DP15, DP9 | **AC12** (the sibling's flag name still appears in exactly one file), **AC18** (the scratch-tree method itself) |
| 47 | The two contract forms agree, at table level and tuple level | DP16 v3 | **AC8** (measured by the criterion itself; the checker-held form was carved out) |
| 48 | The `--doc` mode, its two tokens and its CI step are carved out to a successor issue | DP16 v3, Non-goals | **AC12** (`--doc` no longer in the exactly-one list), **AC13** (the workflow carries no `--doc` step), **AC9** (no flag beginning `--envelope`; the same absence discipline) |
| 49 | The delegated canonical binding is re-asserted for shape before it is consumed | Goal *(superseded v4)*, DP10 | **AC7** (absence measured; the requirement travels to the successor issue, carrying round 3's schema-content finding with it) |
| 50 | The whole-directory mode validates the definition file *set*, not merely the files present | Goal | **AC4** (`definition-missing`, `unknown-adapter`) |
| 51 | Every declared contract minimum is enforced by the checker, not merely observed on a correct shipped file | Goal | **AC2** (five mutation refusals) |
| 52 | No role-to-adapter restriction is introduced | Non-goals, DP13 | *info-only (not promoted to AC)* — an absence of work, and one that would surface immediately if violated: **AC11** re-derives `templates/binding-template.conf`'s six `bind` rows, one per inner-loop role, and **AC18**'s scratch config binds all six roles, neither of which would validate if a restriction had been added. |
| 53 | A retired channel token is refused wherever a channel is declared, from a set compiled into the checker rather than read from the file under test | DP17, Goal | **AC2** (`retired-channel-declared` refused on a `--contract`-supplied registry) |
| 54 | The retired set is also declared in the contract, emitted into the canon, and must equal the compiled-in set exactly | DP17 | **AC2** (a `retired-channel board` row present; stripping it refused `contract-incomplete`), **AC8** (canon-region byte equality carries it into the document) |
| 55 | A `carries` row naming a retired token is refused by the existing vocabulary machinery, not a new special case | DP17 | *info-only (not promoted to AC)* — a consequence, not an independent rule: once `channel board` cannot be declared, `carries x board` names a non-member and **AC4**'s channel-membership requirement refuses it as `unknown-channel`. |
| 56 | A `status-value` token may not be declared twice, including as both `success` and `failure` | DP14, round 2 | **AC2** (`contract-malformed`) |
| 57 | Error-class completeness is the four required tokens, not non-emptiness | DP14, round 2 | **AC2** (`contract-incomplete` on an arbitrary single row) |
| 58 | The delegated output's `schema` line is required and every bound row's provider and model tokens are actually read | DP10, round 2 *(superseded v4)* | **AC7** (absence measured) — round 3 found this half-done (presence checked, content not), which is what fired the trigger; the completed requirement is the successor issue's |
| 62 | The `--binding` mode, its three tokens, its fixture cases and its CI command are **removed** from this branch | DP18, Non-goals | **AC7** (four-surface plus workflow absence, token-by-token) |
| 63 | The fail-closed effort rule and the board-transition authority rule survive as byte-frozen normative statements | DP18, Goal | **AC6** (two `- normative: ` equality locks) |
| 64 | The contract document no longer describes a mode this task removed | DP18 | **AC6** (`--binding` appears zero times in the document) |
| 65 | Nothing in this task reads a binding config at all | DP18, Goal, Non-goals | **AC7** (absence), **AC6** (the rules read as inherited obligations rather than as enforced here) |
| 66 | T-1056's spec depends on the successor issue and must treat it as a planning input | DP18, `## Notes for engineer` | *info-only (not promoted to AC)* — a statement about another task's planning inputs, which no criterion on this branch can observe; carried in the successor issue's own body and in the notes' inheritance list. |
| 59 | The `## The generalized invocation pattern` section names no retired channel token | DP13, DP17, round 2 | **AC8** (scoped standalone-token absence) |
| 60 | Exactly one documented bullet exists per envelope field | DP16 v3, round 2 | **AC8** (bullet count equals distinct field count) |
| 61 | The pre-commitment's factual trigger fired and its recorded disposition was executed rather than reopened | DP16 v3, `## Notes for engineer` | *info-only (not promoted to AC)* — a process fact about how this spec reached v3, not a property of the delivered artifact; the enforceable half is that `--doc` is absent, which rows 48 and 60 carry. |

## Surface decision table

Every candidate surface considered for each of this task's three shipped halves, with the mark and the reason. Marks are `chosen`, `declined` (considered and rejected, with the reason), or `not-applicable` (the surface has no carrier for this half).

| Candidate surface | (a) the normative envelope contract prose | (b) the machine-readable contract vocabulary | (c) the two adapter definitions |
|---|---|---|---|
| `docs/loop-engineering/task-envelope.md` | **chosen** — the internal-mechanism documentation area, read by maintainers and by T-1056/T-1057's implementers; four of its five existing files already ship English-only, which is the measured precedent for DP3 | **declined** — a checker parsing markdown prose for a contract vocabulary is the fragile half of a two-form contract; the registry carries it and AC8 pins the two together | **not-applicable** — a definition is per-adapter data, not prose |
| `templates/task-envelope.txt` | **declined** — a header comment can document a file's own format, as `templates/binding-adapters.txt` does, but not a fourteen-field contract with per-field conditions | **chosen** — the shipped line-oriented contract-file idiom (`durability-records.txt`, `binding-adapters.txt`, `prompt-blocks/registry.txt`), resolved from the checker's own installed directory | **declined** — mixing per-adapter rows into the contract file would make the contract's own vocabulary editable by adding an adapter |
| `templates/adapters/<token>.txt` | **not-applicable** | **not-applicable** | **chosen** — one file per adapter, named for its token, so a bound adapter's definition is composed rather than searched (DP6) |
| `templates/binding-adapters.txt` (extend it) | **not-applicable** | **declined** — its grammar is exactly two tokens per row and `bin/check-binding.sh` refuses anything else as `registry-malformed`; a third column would break a merged, tested validator this task must not edit | **declined** — same; and DP8 adds no row to it at all |
| `README.md` / `README.ja.md` | **declined** — adopter-facing, and nothing is wired for an adopter to use; adopter documentation lands with T-1056, exactly as T-1054 decided for the same reason | **not-applicable** | **not-applicable** |
| `docs/adopting.md` / `docs/workflow.md` | **declined** — same adopter-facing ground; both describe a loop that does not yet read a binding | **not-applicable** | **not-applicable** |
| `agents/*.md` | **declined** — no role reads this contract in this cut, and editing an agent prompt to describe a mechanism nothing invokes adds a reader-less instruction; the role-side wiring is T-1056's | **not-applicable** | **not-applicable** |
| `skills/run/SKILL.md` | **declined** — same, and it is a T-1053 deliverable this branch carries as pre-existing | **not-applicable** | **not-applicable** |
| `templates/prompt-blocks/` | **not-applicable** — generated canonical blocks are never hand-edited | **not-applicable** | **not-applicable** |
| A `*.ja.md` counterpart | **declined** — the readers are agents and maintainers; the repository's Language rule tests who reads the file, and a second copy of an agent-facing document doubles the maintenance surface without adding a reader | **not-applicable** | **not-applicable** |

## Refusal matrix

The closed set every refusal comes from. Each row is one token, one exit code, one tested case. A condition not in this table cannot produce a non-zero exit without being a defect, and no condition in it may produce exit `0`. Exit `1` is a content refusal; exit `2` is input the checker could not evaluate at all — the same split `bin/check-binding.sh` ships.

| Token | Exit | Condition | Criterion |
|---|---|---|---|
| `contract-unreadable` | 2 | the **envelope contract registry** is absent, not a regular file, or unreadable — this token names that file alone; the adapter allowlist has its own two tokens below (DP15, superseding v1) | suite |
| `contract-malformed` | 2 | a registry row whose first field is not a known directive, a wrong field count, a bad token, a duplicated single-valued declaration, a `status-value` row whose second column is neither `success` nor `failure`, **a `status-value` token declared more than once — including one claimed as both `success` and `failure`**, or a missing or unsupported `schema` line — the version allowlist being the parity with `check-binding.sh`'s own that v1 lacked | AC2, suite |
| `contract-incomplete` | 2 | a declared minimum is unmet: no `field` rows, no `channel` rows, fewer than one `role-board-authority` row per inner-loop role, **any of the four required error-class tokens absent** (non-emptiness is not the floor — v2's was, and round 2 reproduced an arbitrary single row passing), no `effort-mechanism none` row, no non-`none` `effort-mechanism` row, no `status-value` rows, a `status-value` set that does not mark **exactly one** member `success` and at least one `failure`, or a `retired-channel` declared set that does not equal the checker's own compiled-in retired set | AC2, suite |
| `retired-channel-declared` | 2 | a `channel` row declares a token in the checker's compiled-in retired set — refused whichever contract file is loaded, including one supplied through `--contract`, which is what makes the retirement structural rather than a property of the shipped file (DP17) | AC2, suite |
| `registry-unreadable` | 2 | the plugin-shipped adapter allowlist (`templates/binding-adapters.txt`) is absent or unreadable — token and exit code deliberately identical to `bin/check-binding.sh`'s for the identical condition on the identical file (DP15) | AC18, suite |
| `registry-malformed` | 2 | an allowlist row that is not exactly two tokens, a token outside the character class or over the length cap, or an adapter token declared twice — same parity (DP15) | AC18, suite |
| `definition-missing` | 2 | the definition file for an adapter named through `--adapter` is absent or unreadable, **or** the whole-directory mode finds an allowlisted adapter with no definition file | AC4, suite |
| `definitions-unreadable` | 2 | the definitions directory is absent or unreadable | suite |
| `unparseable-line` | 1 | a non-blank, non-comment definition line whose first field is not a known directive, or a row whose field count is wrong for its directive | suite |
| `bad-token` | 1 | a definition field violating its character class or the 64-character cap | suite |
| `missing-field` | 1 | a required definition directive is absent | suite |
| `duplicate-field` | 1 | a single-valued definition directive appears more than once | suite |
| `adapter-name-mismatch` | 1 | the declared `adapter` token differs from the definition file's own basename | AC4, suite |
| `unknown-adapter` | 1 | the declared adapter token is absent from `templates/binding-adapters.txt`, **or** the definitions directory carries a file whose basename is not an allowlisted adapter token | AC4, suite |
| `provider-adapter-mismatch` | 1 | the declared provider is not the one the allowlist pairs with that adapter | AC4, suite |
| `unsupported-envelope-schema` | 1 | the definition's `envelope-schema` is not the contract's schema version | suite |
| `unknown-envelope-field` | 1 | a `carries` row names a field the contract does not declare | suite |
| `unknown-channel` | 1 | a `carries` row's channel is absent from the contract's channel vocabulary | suite |
| `field-coverage-gap` | 1 | a contract-declared field has no `carries` row in the definition, **or** a field the contract marks `required` has a `carries` row naming the `not-carried` channel (DP14) | AC4 |
| `capability-inconsistent` | 1 | `supported` with no `effort-value` row or with `effort-mechanism none`, or `unsupported` with an `effort-value` row or a mechanism other than `none` | AC5 |
| `unknown-effort-mechanism` | 1 | the declared `effort-mechanism` token is absent from the contract's vocabulary | AC5 |
| `usage` | 2 | an unknown flag, a flag missing its value, or mutually exclusive modes | AC9, suite |

## Blast radius

Produced by the method `agents/pm-spec.md`'s downstream-impact-inventory item requires — a full-population diff, never a grep inventory. The executing side fills every cell with a measured value and reports the production under **AC17(d)**; this role can state the method and the population but can run neither.

- population: 60 spec files under `.shell-team/specs/`, enumerated live by `for f in .shell-team/specs/*.md; do printf '%s\n' "$f"; done` — 59 measured by this role at authoring time plus this task's own new spec, written here as the value that command is expected to return at implementation time and **compared, not trusted**, by AC16's own live re-derivation. Re-measure and correct this number rather than adjusting the criterion if the two disagree.
- method: every spec in that population has its criteria run with `bash bin/check-acs.sh` at the base ref **and** at HEAD, and the two per-criterion verdict sets are differenced. Base-side verdicts are read from that ref's committed blobs — a disposable `git worktree add --detach` at the measured base ref, or `git show <base>:<path>` — never from the working tree, whose bytes at read time are not that ref's. A spec that does not exist at the base ref (this task's own, T-1054's and T-1053's) counts toward the population and is excluded from the base-vs-head diff by construction.
- a grep of the corpus for `bin/`, `docs/`, `templates/` or `.github/workflows/check-handoff.yml` is **not** the method and is not reported as one; where it is run at all it is a cross-check whose disagreement with the diff is itself a finding.
- this task shares T-1054's base ref exactly (`git merge-base develop HEAD` is still `develop`'s tip), so T-1054's own measured table is the strongest available predictor: every row it measured is expected to reappear here, because this branch carries T-1054's commits. The predictions below distinguish rows **inherited** from the stack from rows this task **additionally** causes.

| Owning spec | Criterion | Labelled? | base: | head: | Reason |
|---|---|---|---|---|---|
| `T-1004-optin-hook-sample.md` | AC2 | inherited from the stack | FAIL | PASS | Positive control requires a non-empty `git diff --name-only develop`; empty at the base ref (nothing yet diverges from `develop`), non-empty at HEAD (three stacked tasks' commits). Unrelated to this task's own diff; identical root cause T-1054 already disclosed. |
| `T-1019-is-span-row-parity.md` | AC10 | inherited and additionally caused by this task | PASS | FAIL | Preservation lock asserts an empty `git diff --name-only develop -- bin tests/rollup-runs tests/cluster-failures`; T-1054 already added `bin/check-binding.sh` (a new tracked file under `bin/`), which alone already breaks this lock, and this task's own `bin/check-adapter.sh` addition keeps it broken. Neither `tests/rollup-runs/` nor `tests/cluster-failures/` themselves are touched. |
| `T-1020-lessons-supersede-sweep.md` | AC14 | inherited and additionally caused by this task | PASS | FAIL | Same shape as the row above: an empty-diff lock over `bin/`, plus (for this criterion specifically) `docs/`; T-1054's `bin/` addition already broke it, and this task's own `bin/check-adapter.sh` plus `docs/loop-engineering/task-envelope.md` — the first file this stack has added under `docs/` — keep it broken. |
| `T-1041-freeze-ux.md` | AC15 | inherited from the stack | PASS | FAIL | Pins `skills/run/SKILL.md` against a historical ref; broken by T-1053's own commit `fa7fbd3` (a splice into that file), present at HEAD through the stack and absent at this base ref. Unrelated to this task's own diff; identical root cause T-1054 already disclosed and independently re-derived. |
| `T-1050-check-layer-fast-follow.md` | AC5 | inherited from the stack | PASS | FAIL | Same root cause as the row above — a `git diff --numstat` assertion against `skills/run/SKILL.md` that T-1053's commit `fa7fbd3` already moved. Unrelated to this task's own diff. |
| `T-1054-binding-config.md` | AC13 | caused by this task alone (no base-side counterpart — this spec is absent at the base ref by construction, excluded from the diff above) | *(n/a)* | FAIL | Merge-point-scoped scope lock whose allow-list does not contain this task's six new paths — exactly the staleness its own prose declares expected, and exactly as its own board entry predicted. |
| `T-1054-binding-config.md` | AC15 | caused by this task alone (no base-side counterpart, same reason) | *(n/a)* | FAIL | Compares its declared population of 59 spec files against a live re-derivation; this task's own new spec makes the live count 60, moving the equality — exactly the event its own `- stale-at:` sub-bullet names. |

No sixth row was found in the 55-spec diffable population beyond the five listed first (measured via a full re-run of every criterion in that population at both faces; every other criterion in it is identical at both faces). `T-1051` AC12 and `T-1052` AC9 (both pin `agents/pm-spec.md`) were confirmed, as T-1054 already measured, to report **no row** — that file is broken at both faces by a pre-existing, unrelated commit, so neither criterion's verdict moves.

Predicted before measurement by this role, recorded so a mismatch is itself a finding rather than a silent correction — **these are predictions, not results, and the measured table above replaces them**:

- **Inherited from the stack, expected to reappear unchanged**: `T-1004` AC2 (FAIL→PASS: its positive control requires a non-empty diff against `develop`, empty at the base ref and non-empty at HEAD); `T-1041` AC15 and `T-1050` AC5 (PASS→FAIL: both pin `skills/run/SKILL.md` against a historical ref, broken by T-1053's splice which is present at HEAD through the stack and absent at this base ref); `T-1051` AC12 and `T-1052` AC9 (pin `agents/pm-spec.md`, measured by T-1054 as already broken at both faces by a pre-existing unrelated commit, so **no row**).
- **Inherited and additionally caused by this task**: `T-1019` AC10 and `T-1020` AC14 (PASS→FAIL: both assert an empty `git diff --name-only develop` over broad path lists including `bin`, `tests`, `.github` and — for `T-1020` AC14 — `docs`; T-1054 already added files under `bin`, `tests` and `.github`, and this task adds further files under all four, `docs/` for the first time in this stack).
- **Caused by this task alone**: `T-1054` AC15 (PASS→FAIL: it compares its declared population of 59 against a live re-derivation, and this task's own new spec makes the live count 60 — exactly the event its own `- stale-at:` sub-bullet names); `T-1054` AC13 (PASS→FAIL: a merge-point-scoped scope lock whose allow-list does not contain this task's six new paths — exactly the staleness its own prose declares expected).
- **Expected NOT to move, and a defect rather than a disclosure if they do**: `T-1054` AC2 (the specimen's row count, role set and registry rows — DP7 permits only an `effort`-field edit, which that criterion does not read, and DP8 adds no registry row); `T-1054` AC3 (`bin/team-paths.sh` and its suite byte-identical — this task adds no resolver key); `T-1054` AC1 and AC4–AC12 (the validator's own behaviour, which this task must not touch); `T-1054` AC14 (records, board-line survival and `check-prompt-sync`, all of which this task's own AC15 also requires).

## Assumptions

- **The base ref is `git merge-base develop HEAD` — UNMEASURED by this role.** Every criterion resolves it live; no 40-hex literal appears anywhere in this spec. **AC17(e)** requires the executing side to report the measured value.
- **This branch is stacked two deep, on T-1054's tip which is stacked on T-1053's tip — RELAYED.** Supplied by the coordinating session; deliberately not transcribed as a SHA into any criterion, since this role cannot verify one and a wrong literal frozen into a check line is unrepairable without a re-freeze. Corroborated by this role by reading rather than by running git: `.shell-team/todo.md` records both T-1053 and T-1054 as closed against PRs that "merge at the sprint batch GO", which means their commits are on a branch and not on `develop`, which is why `git merge-base develop HEAD` is `develop`'s tip and why both tasks' delivered paths appear in this branch's diff against that base through no act of this task. **AC14's allow-list is written on that premise**; AC17(e) requires the executing side to measure whether each tip is an ancestor of `HEAD` and report both. If either is not, this spec's scope lock is measuring something other than what it claims and the freeze must stop rather than proceed.
- **Issue #203's quoted design points and principles — RELAYED.** This role has no network and no `gh`; the quotations in `## Summarized sources` are as the coordinating session supplied them. The coordinating session holds the primary confirmation. **AC17(f)** requires each to be confirmed against the issue itself by a side that can read it, or named as still relayed.
- **Issue #221's content — RELAYED at one remove.** This role read the board's fast-follow disposition line naming it, not the issue body. What DP9 rests on is that line's own words: a testing affordance, zero consumers today, T-1056 as the design input's owner. **AC17(f)** covers it.
- **The planning decisions — RELAYED**: that no third adapter ships, that T-1056 owns loop integration and run-time unavailability handling and T-1057 owns telemetry, and that this is the sprint's second feature task. These came from a planning conversation this role cannot read.
- **The two shipped allowlist rows and the specimen's `tech-lead high claude-cli` row — MEASURED by this role** in `templates/binding-adapters.txt` and `templates/binding-template.conf`. DP7's two branches rest on that measurement: the adapter token really is `claude-cli`, and the specimen really does carry one non-`-` effort value.
- **The board-authority values — MEASURED by this role** from `agents/engineer.md`, `agents/qa-verifier.md`, `agents/ui-designer.md`, `agents/tech-lead.md` and `agents/codex-reviewer.md`'s own prompt text, not relayed. The `qa-verifier` `tools:`-line nuance is measured from its frontmatter and disclosed rather than resolved.
- **`docs/loop-engineering/`'s English-only precedent — MEASURED by this role** by enumerating that directory: four files with no `*.ja.md` counterpart, one with.
- **Nothing under `bin/`, `skills/*/SKILL.md` or `agents/*.md` currently spells `--adapters`, `--definitions` or `--contract` except `bin/check-binding.sh` (which spells only the first) — MEASURED by this role.** AC12 is written on that measurement, and it is what makes the "exactly one file" form of that criterion satisfiable rather than aspirational.
- **The effort mechanism of either provider — DELIBERATELY UNMEASURED BY THIS ROLE, and since measured by two others.** This role cannot run `codex --help` or inspect the harness's settings, and #203 names verifying each provider's actual effort mechanism as work rather than as a known fact. No criterion, no frozen sentence and no `## Notes for engineer` vocabulary in this spec asserts what either mechanism is — that remains true at v2, and it is why `AC6`'s `- stale-at:` and `AC17(b)` are written the way they are. **Status at v2:** the measurement has since been performed by the engineer and independently confirmed by the round-1 reviewer against the live CLIs (`codex-cli 0.145.0`; Claude Code `2.1.226`), and its results are recorded where they belong — in the shipped adapter definitions and the contract document, not here. Round 1 raised no finding against either measurement, so v2 changes nothing about it; **AC17(b)** stands unchanged as the reporting duty, and a re-measurement is owed only on the event `AC6`'s `- stale-at:` names.
- **Round 1's nine findings — MEASURED, not relayed, and read at the primary artifact by this role.** `.shell-team/reviews/T-1055.md` was opened in full before v2 was drafted, and the four contract-design gaps were re-grounded against the shipped implementation this role also read directly (`bin/check-adapter.sh`'s registry loader, contract loader, coverage check, whole-directory validator and `--binding` dispatch; `templates/task-envelope.txt`; both adapter definitions). Two premises v2's design rested on were confirmed that way rather than taken from the review's prose: the sibling validator is invoked as `bash "$SCRIPT_DIR/check-binding.sh"`, which is what made **v2/v3's** AC7 stubbed-sibling case reachable — that case went with the carved-out mode at v4 (DP18) and AC7 now measures the mode's absence instead, so this premise is retained as the ground v2's design stood on rather than as a live one; and the adapter allowlist has **no** path-override flag, which is what forces AC18's parity test onto a scratch copy of the installed tree rather than a flag DP9 forbids — that one is still live.
- **`shellcheck` 0.11.0 is what CI installs — MEASURED** at `.github/workflows/check-handoff.yml`'s `SHELLCHECK_VERSION` by T-1054 and unchanged since; whether the local copy matches is the executing side's to confirm before trusting a shellcheck-clean claim, per the test recipe's own instruction.
- **No class-M standing grant exists in this checkout — RELAYED.** `CLAUDE.local.md` is outside this role's reach. Consequential only if a re-freeze becomes necessary.

## Open questions

None blocking.

## Notes for engineer

- **THE PRE-COMMITMENT TRIGGER FIRED AGAIN AFTER ROUND 3. Read this before anything else; it supersedes the round-2 block below.** Round 3 produced one new Major against `bin/check-adapter.sh` — the delegated `schema` line validated for presence but not content — a third consecutive round of new independent findings against that component. **The recorded disposition executed**: droppable item (1) as v3 renumbered it, the **`--binding` cross-check mode**, is carved out to a successor issue whose number is on the board entry. The reviewer's own "much smaller rework surface … no design decision to re-open" is precisely the cheap-fix reasoning the trigger exists to overrule, and it was seen and refused rather than missed. **Requirement W0 below is the whole of this round's implementation work** unless your own sweep finds something: the retained scope had no other round-3 findings.
- **X0 (v6, and the only requirement of this round) — correct the contract document's introduction, which still claims a pre-run effort validation exists.** Measured at `docs/loop-engineering/task-envelope.md` lines 16–19 (the reviewer reported line 17; the claim actually spans 16–19 — verify against the file, not against this number, since an edit above it shifts it). The sentence currently ends "…what it is handed, what it must give back, and how an unsupported `effort` value fails closed before a run rather than surprising a host inside one." That was true through v3 and is false since v4, and it is the **first substantive claim** a reader of this document meets — including whoever plans T-1056, for whom the precise question it gets wrong (does effort get pre-validated?) is the one DP18 exists to settle. Replace from "Nothing consumed" to the end of that sentence with **exactly** the following two sentences, byte for byte, preserving the markdown emphasis and code spans shown and re-wrapping only at line boundaries:

  > Nothing consumes that binding — this document is what an adapter *is*: what it is handed, what it must give back, and what a host is owed when it assigns an `effort` value the executor cannot honour. That last rule is **normative here, not enforced here**: the pre-run cross-check that once enforced it was carved out to a successor issue (DP18), so a component acting on this contract must implement it rather than assume it already runs.

  Do **not** introduce the two-hyphen flag spelling anywhere in the document while doing this — AC6 asserts it appears zero times, and the surrounding prose already uses "binding-config cross-check mode" for exactly that reason. **The rest of the document is already correct**: I swept every other occurrence of the binding-era vocabulary in it and every one is properly marked historical ("was … until it was carved out", "it now ships as the byte-frozen statement above"), including the whole `## Fail-closed effort` section, the DP13 join paragraph and the inheritance list. This is one sentence, not a pass.
- **Why X0 carries no AC pin, and what that leaves open.** Applying the coordinating session's own test — an AC-level pin only where an existing criterion already reads that region — no criterion reads the document's introduction: AC8's canon region covers the fenced contract block, AC6 reads the whole file only for the `--binding` count and `capability-unsupported`'s presence, and AC9 reads it only for the `- boundary: ` line. So X0 ships as a plain requirement and v6 keeps every `- check:` line byte-identical, as instructed. **The residual risk is real and is disclosed rather than absorbed**: this prose-fidelity class has now recurred three times in one task (v5's three invocation-pattern sites, round 4's five, and the sixth site I found in `## Assumptions` this round), and an unpinned sentence can go stale a fourth time. Two things reduce it without a pin: the replacement text is written so that it states the carve-out as a fact rather than describing enforcement, so it only goes stale if the successor issue actually delivers the enforcement — at which point that task owns updating this document — and the sentence names DP18, which a reader following the reference lands on. If a future round finds this class again, the proportionate answer is a pin on the introduction region or a checker clause, not a fourth prose fix; that judgment is recorded here rather than left to be re-derived.
- **W0 (v5's requirement, completed and verified — retained for traceability) — DELETE the v3 `--binding` implementation from this branch.** It is a removal, not a never-add. From `bin/check-adapter.sh`: the `--binding` flag and its argument parsing, the mode's dispatch arm and every function reached only from it, the sibling-invocation and delegated-output reassertion block, and the `effort-unsupported`, `authority-channel-conflict` and `binding-invalid` tokens **including their rows in the script's own header refusal-matrix comment and any prose in that header describing the mode**. From `tests/check-adapter/run.sh`: every fixture case exercising the mode (re-derive the case count from the suite itself; do not hand-adjust a declared number). From `.github/workflows/check-handoff.yml`: the `bash bin/check-adapter.sh --binding templates/binding-template.conf` dogfood command — measured present today at that step, and note the step also carries the flagless default-mode command, which **stays**. From `docs/loop-engineering/task-envelope.md`: every occurrence of `--binding`, and rewrite the sections that describe the static enforcement so the fail-closed effort rule and the authority rule read as **inherited obligations** on the successor issue and T-1056 rather than as something enforced here — then add the two byte-frozen `- normative: ` lines AC6 pins, verbatim. Leave `templates/binding-template.conf`, `bin/check-binding.sh` and everything under `templates/adapters/` untouched: the specimen and the definitions still ship, only the cross-check between them goes. Round 1 and round 2 each produced new, independent `Blocker`/`Major` findings against one component (`bin/check-adapter.sh`'s fail-closed guarantees) — two consecutive rounds, which is exactly this spec's own factual trigger. **The recorded disposition was executed, not reopened**: droppable item (1), the `--doc` mode, is **carved out of this task** to a successor issue whose number is recorded on the board entry. **The v2 implementation of `--doc` is already in this branch's tree, so your job is to DELETE it, not to refrain from writing it.** That is requirement **V0** below, and it is the first thing to do this round — AC12 and AC13 are red until it lands, by design. If round 3 produces new independent `Blocker`/`Major` findings against `bin/check-adapter.sh` again, the next recorded drop (`--binding`) executes on the same terms; that is a decision already made, not one to re-litigate mid-round.
- **Round-3 rework requirements for the retained scope, numbered 1:1 against round 2's findings** (`.shell-team/reviews/T-1055.md`, `## Round 2`). Read that section in full first; every item was empirically reproduced there. **V0 (do this first) — DELETE the v2 `--doc` implementation from this branch.** It is a removal, not a never-add: the mode exists in the tree right now. Remove, from `bin/check-adapter.sh`, the `--doc` flag and its argument parsing, the doc mode's dispatch arm and every function reached only from it, and the `doc-drift` and `doc-unreadable` tokens including their rows in the script's own header refusal-matrix comment; from `tests/check-adapter/run.sh`, every fixture case exercising that mode (the suite's case count drops accordingly — re-derive it, do not adjust a declared number by hand); and from `.github/workflows/check-handoff.yml`, the `--doc` dogfood step. Leave the document's canon region, its markers and its per-field bullet shape **in place** — AC8 now measures them directly, so they are load-bearing for the retained scope even though nothing in the checker reads them any more. **V1 (the Blocker) — structural channel retirement (DP17):** give `bin/check-adapter.sh` its own compiled-in retired-channel set containing `board`; refuse `retired-channel-declared` (2) on any `channel` row declaring a member, **whichever contract file is loaded**, so the `--contract`-override attack round 2 reproduced cannot reintroduce the token; add a `retired-channel <token>` directive to the registry, emit it in `print_contract` (so it lands in the canon region), and refuse `contract-incomplete` (2) when the declared set and the compiled-in set differ in either direction. The existing `ca-board-channel-retired` suite case stays, but it is no longer the protection — it is a regression assertion beside a structural refusal. **V2 — `status-value` token uniqueness:** a token declared more than once, including one claimed as both `success` and `failure`, is refused `contract-malformed` (2). **V3 — error-class completeness floor:** the four AC2-required tokens must each be present; non-emptiness is not the floor. Refuse `contract-incomplete` (2). **V4 — `--binding` delegated-output reassertion, completed:** require exactly one leading `schema` line (v2 set a flag and never read it), and actually read each bound row's provider and model tokens — validate their shape and cross-check the provider against the one T-1054's allowlist pairs with that row's adapter. Refuse `binding-invalid` (2). **V5 — the stale document prose, and it is three sites rather than the one round 2 named.** Measured on the current tree: `## The generalized invocation pattern` (the section between that heading and `## Envelope fields`) carries **three** standalone occurrences of `board` — the stale channel example round 2 reported ("some other existing channel (a file, stdout, the exit code, the board …)"), plus two in a sentence describing how results come back "as **board tokens** … the role itself never edits the board." AC8 forbids all three, deliberately: that section's subject is which channels carry which fields, so a retired channel token may not appear there **and** an authority statement does not belong there either — DP13 puts authority entirely in `role-board-authority`, and the document has its own DP13 section for it. Rewrite the channel example to name only live vocabulary members, and move or rephrase the authority sentence so the `proposes` behaviour is stated where authority is documented. `board-transition` and every other hyphenated compound is untouched, since AC8 matches the standalone token only; prose elsewhere in the document (including the `board-transition` field bullet, which legitimately explains what a `writes` role already wrote to the board) is out of this clause's scope. **V6 — do NOT fix the three Minors** (silent dedup of duplicate vocabulary rows, now also reachable via `status-value`; `--binding` collapsing any sibling nonzero exit; the canon-marker substring/trailing-newline edges): the reviewer disposed all three as file-an-issue and the coordinating session files them at close-out. The third is moot for this task anyway, since the canon comparison it describes moved out of the checker with `--doc`.
- **Interventions record — the descope needs its own entry.** `bin/check-interventions.sh`'s contract requires a work-abandoned/descope entry when scope is dropped mid-task. `.shell-team/interventions/T-1055.md` must gain one for the `--doc` carve-out, naming the trigger that fired, the two round-2 findings that travelled with it, and the successor issue. pm-spec cannot write that record from here (it is the engineer's file for this task); it is called out so it is not discovered at the close-out gate.
- **Round-2 rework requirements, retained for traceability, numbered 1:1 against round 1's findings** (`.shell-team/reviews/T-1055.md`, round 1). R1–R5 and R7–R9 are **closed** per round 2's verification table; R2's error-class half and R6's delegated-shape half were only partially closed and are re-issued above as V3 and V4; R7's `--doc` half is carved out. Read that record in full first; every item below was empirically reproduced there, so none of them is a judgment call about whether the gap is real. **R1 (the Blocker) — `load_adapters_registry`:** validate `templates/binding-adapters.txt` strictly — exactly two whitespace-delimited tokens per substantive row, each within the character class and length cap, and no adapter token declared twice — refusing `registry-malformed` (2); and change the unreadable branch from `contract-unreadable` to `registry-unreadable` (2). Both tokens are T-1054's, deliberately (DP15). Record a **superseding** provenance decision for the v1 decision that folded the allowlist into `contract-unreadable`; do not leave two contradicting entries with the later one merely sitting after the earlier. **R2 — `load_contract` completeness minimums:** at least one `error-class` row, exactly one `effort-mechanism none` row, at least one non-`none` mechanism, at least one `status-value` row, exactly one marked `success` and at least one `failure` — each unmet minimum refusing `contract-incomplete` (2). **R3 — contract schema-version allowlist:** the analogue of `check-binding.sh`'s `schema_version_supported()`; an unsupported version refuses `contract-malformed` (2). **R4 — `validate_all_definitions` set completeness:** every allowlisted adapter must have a definition file (`definition-missing`, 2), and a file whose basename is not an allowlisted adapter token refuses `unknown-adapter` (1). **R5 — `field-coverage-gap` widened:** a `required` field whose `carries` row names `not-carried` is refused (1). **R6 — `--binding` delegated-output re-assertion:** exactly six `bound` rows, one per inner-loop role, no other non-blank line, before any of them is consumed; a violation refuses `binding-invalid` (2). **R7 — the `--doc [PATH]` mode:** canon-region byte equality, per-field direction/requiredness tuples, `required when` on every conditional field, and field-set equality in both directions; `doc-drift` (1) and `doc-unreadable` (2), plus its own CI dogfood step. **R8 — the `status-value` directive:** registry rows, the directive table, `print_contract`'s canonical emission (grouped with the other vocabularies, `LC_ALL=C`-sorted within the group), and both halves of the document. **R9 — `board-transition`:** remove `channel board` from `templates/task-envelope.txt`, change `templates/adapters/claude-cli.txt`'s `carries board-transition board` to a real return path, add the `authority-channel-conflict` (1) join to `--binding`, and state DP13's semantics normatively in the document. **R10 — do NOT fix the two Minors** (duplicate vocabulary rows silently deduplicated; `--binding` collapsing any sibling nonzero exit into `binding-invalid`): the reviewer disposed both as file-an-issue and the coordinating session files them at close-out. Fixing them here would blur which round closed which finding and would put changes in the diff that no criterion asked for.
- **Order of work.** (1) `templates/task-envelope.txt` — the contract registry, since everything else is validated against it. (2) `docs/loop-engineering/task-envelope.md` — the prose half, written beside it so the two cannot diverge from the start. (3) **The effort measurement**, before either adapter definition is written: run the real probes, record what you found, and only then (4) write `templates/adapters/claude-cli.txt` and `templates/adapters/codex-cli.txt`. (5) `bin/check-adapter.sh` — contract validation, then definition validation, then `--print-contract`, then `--binding` on top of the same loaded state. (6) `tests/check-adapter/run.sh`. (7) CI wiring plus the test-recipe append. (8) DP7's specimen reconciliation, if the measurement requires it. (9) Blast-radius table and the records. Nothing else.
- **Allow-list for AC14**, stated once here so the criterion can point at it. **Editable by this task**: `.shell-team/specs/T-1055-adapter-envelope.md`, `bin/check-adapter.sh`, `tests/check-adapter/run.sh`, `templates/task-envelope.txt`, `templates/adapters/claude-cli.txt`, `templates/adapters/codex-cli.txt`, `docs/loop-engineering/task-envelope.md`, `.github/workflows/check-handoff.yml`, `.shell-team/todo.md`, `.shell-team/test-recipe.md`, `.shell-team/provenance/T-1055.md`, `.shell-team/interventions/T-1055.md`, any path beginning `.shell-team/reviews/T-1055`, and `templates/binding-template.conf` **only under DP7's second branch and only in the `effort` field of one row**. **Pre-existing on this stacked branch, permitted in the diff but NOT editable** — T-1053's `skills/run/SKILL.md`, `agents/pm-spec.md`, `.shell-team/specs/T-1053-retro-mechanization.md`, `.shell-team/provenance/T-1053.md`, `.shell-team/interventions/T-1053.md`, any path beginning `.shell-team/reviews/T-1053`; and T-1054's `bin/check-binding.sh`, `tests/check-binding/run.sh`, `templates/binding-adapters.txt`, `.shell-team/specs/T-1054-binding-config.md`, `.shell-team/provenance/T-1054.md`, `.shell-team/interventions/T-1054.md`, any path beginning `.shell-team/reviews/T-1054`. Nothing else, tracked or untracked. Touching anything in the second group is a scope violation this task's own criteria will not catch; AC17(e)'s ancestry measurement and the blast-radius rows for `T-1041` AC15 / `T-1050` AC5 are where an accidental edit would surface.
- **Do not spell the sibling validator's registry-override flag name anywhere in `bin/check-adapter.sh` — not in code, not in a comment.** AC12 greps the whole file, so a header comment explaining issue #221 by quoting the flag fails it. Refer to it as "the sibling validator's registry-override testing affordance" instead. This is the same class of hazard as the corpus's T-1041 entry about an id that cannot be renamed once frozen: cheap to avoid up front, a re-freeze to fix afterwards.
- **Suggested vocabularies, deliberately outside the frozen block so the measurement can change them.** Channels: `argv`, `prompt`, `stdin`, `stdout`, `stderr`, `file`, `exit-status`, `not-carried` — **`board` is retired and must not be reintroduced** (DP13, DP17). In v3 the retirement is structural rather than a fact about the shipped file: the checker carries its own compiled-in retired set, refuses `retired-channel-declared` on any contract that declares a member as a `channel`, and requires the registry's `retired-channel` rows to equal that set exactly. Adding a retired token to the compiled-in set is therefore a two-file change by design — the checker and the registry — and the canon region then carries it into the document automatically. Effort mechanisms: `cli-flag`, `cli-config-override`, `harness-setting`, `api-parameter`, `none`. Status values (new in v2, `status-value <token> <success|failure>`): one success token plus a refusal token plus an error token is the shape the contract document already describes; exactly one row is marked `success`. Error classes beyond the four AC2 requires: whatever the contract genuinely needs. Apart from `board`'s retirement none of these is frozen; AC2 constrains the vocabularies' *shape* (closed, declared in the registry, `none` present among the mechanisms, exactly one `success` among the status values) and otherwise never their membership, precisely so a measurement that finds something unexpected does not cost a re-freeze.
- **The effort measurement is the one irreducibly manual step, and it is where this task's value is.** For `codex-cli`: run the real Codex CLI's help, record its version verbatim (the same discipline `agents/codex-reviewer.md` already imposes on its own `- Codex CLI:` line), and find whether a per-invocation reasoning/effort setting exists and what values it takes. For `claude-cli`: consult the harness's own effort semantics. **A finding of "no per-invocation mechanism exists" is a valid, complete answer** and is recorded as `capability effort unsupported` with `effort-mechanism none` — do not manufacture a mechanism to avoid that outcome, and do not infer one from documentation you did not run. Whatever you find, DP7's second branch may then require the one-field specimen edit; make it, and report it under AC17(b).
- **The five line shapes the document must carry**, because three criteria parse it (AC3, AC8, AC9) and a parser that cannot find its anchor reads as a failure rather than as a missing heading. **(v)** — new in v3 — a `## The generalized invocation pattern` heading occurring exactly once, whose section names **no standalone retired channel token**; round 2 found it still calling "the board itself" a valid return channel while this same document's DP13 section declared that channel retired, so a reader implementing T-1056 from the document alone met the contract contradicting itself on the exact point DP13 settles. Rewrite that section's channel examples to name only live vocabulary members; `board-transition` and other hyphenated compounds are unaffected, since AC8 matches the standalone token only. (i) A `## Envelope fields` heading occurring exactly once, under which every field is documented on its own physical line of the shape `- **<field>** — <in\|out>, <required\|conditional> — <prose>`; AC8 extracts the token set and the direction/requiredness tuple from exactly that shape, and every field the contract marks `conditional` must carry the phrase `required when` **on that same physical line** (a wrapped continuation line will not satisfy it). (ii) A single line beginning `- boundary: ` whose remainder is byte-identical to the sentence AC9 pins; it is an equality lock, so a trailing space or a substituted punctuation mark fails it. (iii) A `## Telemetry readiness` heading occurring exactly once, whose section names the five supplying envelope fields plus `provider`, `model` and `effort` as literal tokens (AC3). (iv) **New in v2** — a canon region delimited by the marker lines `<!-- BEGIN contract-canon -->` and `<!-- END contract-canon -->`, each occurring exactly once, containing `--print-contract`'s output byte for byte. Put both markers **inside** the fenced block rather than around it, so that no fence line falls between them and the extraction needs no fence stripping; the markers render as literal text inside the code block, which is harmless. Everything else in the document is free prose.
- **Regenerate the canon region, never hand-edit it.** It is `bash bin/check-adapter.sh --print-contract` redirected between the markers. Hand-typing it is how it goes stale by one character, and **AC8**'s own canon-region byte comparison will say so — correctly, but after you have spent the round; the `--doc` mode that once reported `doc-drift` was carved out by DP16 v3.
- **Canonical `--print-contract` form, spelled out** so the implementation and the criteria cannot drift: line 1 is `schema <version>`; then every `field`, `channel`, `error-class`, `effort-mechanism` and `role-board-authority` row, single-space delimited, grouped by directive in that order and `LC_ALL=C`-sorted within each group. Comments, blank lines, row order and intra-row whitespace in the source registry must not change a byte of it — AC10 compares two invocations' output for byte equality, and the decoy cases would pass vacuously if the output were unstable for unrelated reasons.
- **Prior art to read before starting**: `bin/check-binding.sh` in full (the physical `pwd -P` bootstrap and the header explaining why, the closed refusal helper, the two-pass validation order with per-line refusals winning over set completeness, `print_help` reading its own header), `agents/codex-reviewer.md` in full (the invocation pattern being generalized, and the board-write boundary that DP4's `proposes` value describes), and `.shell-team/specs/T-1054-binding-config.md` for the criterion, scope-lock and disclosure conventions this spec follows.
- **Suite conventions to copy, not re-derive** (test recipe, T-1044 and T-1054 entries): commit `tests/check-adapter/run.sh` at index mode `100755`; build the scratch root with the two-arm `TMPDIR`-then-`$HERE` `mktemp -d … XXXXXX` idiom at `tests/check-refreeze-class/run.sh:82-87` — this suite builds **no** `git init` repositories, so that idiom is the right one; route every case through a single `assert_case`-shaped helper asserting exit code **and** token together; beware the T-1016 hazard (`x="$(producer | grep -c PATTERN)"` aborts the script under `pipefail` when the count is genuinely zero); and heed T-1054's own two traps — per-mode refusal ordering means a differently-named fixture can trip an earlier refusal than the one under test, and a payload with embedded whitespace is refused as a wrong-field-count row rather than as a malformed token.
- **Coverage the suite must carry beyond the criteria**, because a `- check:` line cannot spell them: a definition file with CRLF line endings, one with no trailing newline, one that is entirely comments, one with its directives in a scrambled order (order must not matter), a definitions directory containing a file whose name is not `<known-adapter>.txt`, an empty definitions directory, a contract registry with a duplicated `field` row, a `carries` row appearing twice for one field, and a definition declaring an `envelope-schema` one version ahead of the contract's.
- **Measured-at-ref command check**: this task's deliverables do print results beside a ref label — the `## Blast radius` table's `base:` verdicts — so the check applies rather than being `not applicable`, and the `## Blast radius` method statement plus AC16 carry it: the base-side reading target must be that ref's committed blobs, never the working tree. Separately, the effort-capability values are labelled against a **provider CLI version** rather than a git ref; AC6's `- stale-at:` names the invalidating event and AC17(b) requires the version to be transcribed verbatim beside the values.
- **What T-1056 inherits from here, recorded so it is not rediscovered**: the runtime half of the fail-closed effort rule (an adapter presented an unsupported effort returns `status` non-success with `error-class capability-unsupported` and invokes nothing); the runtime half of issue #221 (re-asserting AC12's invariant over whatever production call sites T-1056 adds, since this task creates none); adopter-facing documentation of the whole feature; and the decision about what a repository with no binding config does, which T-1054 already left as its own DP5.
- **Task classification, so review rigour is calibrated rather than guessed**: this **is** verification-mechanism work — a new checker, a new grammar, and a new closed-vocabulary contract validated in two directions, all categories this repository measures as running long. **18 criteria, 17 `check:` lines** — unchanged across v2, v3 and v4: AC18 was appended rather than inserted, and both carve-outs removed *modes* rather than criteria, retargeting AC6, AC7, AC8, AC12 and AC13 in place instead. Every criterion number has been byte-stable since v2, so each re-freeze delta reads as the set of things that actually changed. Round 1: 1 Blocker + 8 Major. Round 2: 1 new Blocker + 6 Major. Round 3: 1 new Major. That is what this classification predicted — and the trigger has now fired twice, each time executed rather than reopened. **The right read of a declining finding count is not "we are nearly there"**: round 3's single finding was the same "presence checked, content ignored" pattern as round 2's in the same block, which is what a recurring-defect trigger is for.
- **Pre-commitment, re-stated for v4 — the trigger has FIRED TWICE and two drops are spent.** Factual trigger: two consecutive rework rounds of new independent `Blocker`/`Major` findings against one component. Contextual trigger: a third round about to start. **The factual trigger governs when they disagree.** **Where this stands:** rounds 1–2 fired it once (the `--doc` mode dropped); rounds 2–3 fired it again (the `--binding` mode dropped). Both drops were executed rather than reopened, and in each case the reviewer's own "this one is small" framing was recorded as refused. **The remaining drop order, renumbered:** **(1) the `role-board-authority` directive** now drops first, moving the write-versus-propose distinction into the contract document's prose table alone; the distinction survives — and note it is *already* the document's, since AC6 pins it there — so what would be lost is only the registry's machine-checkable role completeness. **(2) the `channel` vocabulary and the `carries` rows** drop second, leaving each definition to declare identity and capabilities only; **this now also carries DP17's `retired-channel` machinery**, because a contract with no channel vocabulary has nothing to retire — which means executing drop (2) *would* touch never-dropped items 8 and 9, and is therefore the point at which the stop-and-return-to-planning branch fires instead. Say that plainly: **there is one drop left before the planning-return branch, not two.** **Never dropped**, still nine and re-read against this drop: the envelope's field set with directions and requiredness; contract-not-a-serialization-protocol; the effort capability being measured rather than guessed; the fail-closed unsupported-effort treatment being **stated normatively somewhere** (the v2 wording said *stated*, not *enforced*, which is exactly why this drop does not touch it — AC6 now pins the statement); no production path forwarding a testing affordance; no third adapter row; the allowlist validated at parity with `bin/check-binding.sh`; the `board` channel staying retired; and that retirement being structural. **The `authority-channel-conflict` join was not on that list** — it was enumerated inside drop item (1) at v2 and v3 — so its loss is the pre-agreed price, not a weakening, and the stop branch does not fire this time. **If round 4 produces new independent `Blocker`/`Major` findings against `bin/check-adapter.sh`**, do not execute drop (1) reflexively: check first whether the finding lands in machinery whose removal would touch items 8 or 9, and if it does, **stop and return to sprint planning** rather than dropping.
- **Pre-commitment as recorded at v3, retained so the second executed disposition can be audited against its own pre-agreed text — the trigger had FIRED once and drop (1) was spent.** Factual trigger: two consecutive rework rounds of new independent `Blocker`/`Major` findings against one component. Contextual trigger: a third round about to start. **The factual trigger governs when they disagree.** **Where this stands:** rounds 1 and 2 each produced new independent findings against `bin/check-adapter.sh`; the trigger fired; **the `--doc` mode was carved out to a successor issue** and AC8 retreated to measuring two-form agreement itself, which is what the v2 drop order promised ("strictly better than v1 rather than nothing"). **The remaining drop order, renumbered:** **(1) the `--binding` cross-check mode** (AC6's static half, AC7, the `effort-unsupported`, `authority-channel-conflict` and `binding-invalid` tokens) now drops first — it has no consumer until T-1056, the normative fail-closed effort rule and the normative authority rule both survive in the contract document, and dropping it leaves a complete definition validator; V4 and DP13's join travel with it as the receiving issue's requirement list. **(2) the `role-board-authority` directive** drops second. **(3) the `channel` vocabulary and the `carries` rows** drop third — note this now also carries DP17's `retired-channel` machinery, since a contract with no channel vocabulary has nothing to retire. **Nothing else moved**: the drop order's remaining items are in the same relative order v2 recorded, with the spent item removed rather than the list re-derived. **Never dropped**, now nine: the envelope's field set with its directions and requiredness; the "contract, not a serialization protocol" property; the effort capability being **measured rather than guessed**; the fail-closed treatment of an unsupported effort value being stated normatively somewhere; no production path forwarding a testing affordance; no third adapter row; the adapter allowlist being validated at parity with `bin/check-binding.sh`; the `board` channel staying retired; and — added in v3 — **that retirement being structural rather than a regression assertion on one file**, because round 2 showed the file-level form does not hold. **On the Blocker that attacked a never-dropped item:** the `board`-stays-retired item was defeated for the **first** time, at v2, and closing it **strengthens** the item rather than weakening it, so the "stops and returns to sprint planning" branch does **not** fire. That branch fires only if a round's findings would require *weakening* one of the nine — and if `board`-stays-retired is defeated again in its v3 structural form, that is the branch to take rather than a fourth patch. **If round 3 produces new independent `Blocker`/`Major` findings against `bin/check-adapter.sh`**, drop (1) — `--binding` — executes immediately, on the same terms and without reopening the question.
- **Pre-commitment as recorded at v2, retained so the executed disposition can be audited against its own pre-agreed text — and round 2 was the trigger round.** Factual trigger: two consecutive rework rounds of new independent `Blocker`/`Major` findings against one component. Contextual trigger: a third round about to start. **The factual trigger governs when they disagree.** **Where this stands now, stated so nobody has to reconstruct it mid-round:** round 1 produced 1 Blocker and 5 Major against **one** component — `bin/check-adapter.sh`'s fail-closed guarantees (R1–R6) — which is **one** round of the two the factual trigger names. The reviewer said so explicitly in its own recommendation. If round 2 produces new independent `Blocker`/`Major` findings against that same component, **the trigger has fired and drop (1) below executes immediately** — do not open the question, do not spend the round on one more patch, and do not treat "the fix is cheap" as a reason to continue; that reasoning is exactly what the trigger exists to overrule. The remaining three round-1 Majors (R7–R9) were contract-design gaps, resolved in v2 by DP13/DP14/DP16 rather than by patching, and do not count toward the trigger against the checker. Droppable, in this order, each carrying the round-1 findings that travel with it as the receiving issue's requirement list — **(1) the `--doc` mode** (R7) drops first: it is the newest mechanism, it has no consumer at all until someone edits the contract, and dropping it **retreats to something strictly better than v1 rather than to nothing** — AC8's own independent canon-region and tuple-level comparison, which this spec already requires that criterion to perform, so the property survives at spec-check time and only the "red in CI on every run" strengthening is lost. **(2) the `--binding` cross-check mode** (AC6's static half, AC7, the `effort-unsupported`, `authority-channel-conflict` and `binding-invalid` tokens; carrying R6 and DP13's join) drops second: it is a convenience that surfaces a host's mistake earlier than T-1056 would, the normative fail-closed effort rule and the normative authority rule both survive in the contract document, and dropping it leaves a complete definition validator; T-1056's spec then depends on the receiving issue. **(3) the `role-board-authority` directive** drops third, moving the write-versus-propose distinction back into the document's prose table alone; the distinction survives, only its machine-checkable completeness goes. **(4) the `channel` vocabulary and the `carries` rows** drop fourth, leaving each definition to declare identity and capabilities only. **Never dropped**: the envelope's field set with its directions and requiredness; the "contract, not a serialization protocol" property; the two adapter definitions' effort capability being **measured rather than guessed**; the fail-closed treatment of an unsupported effort value being stated normatively somewhere; no production path forwarding a testing affordance; no third adapter row; **the adapter allowlist being validated at parity with `bin/check-binding.sh`** (R1 — this was the Blocker, and a fail-closed checker that accepts a corrupt allowlist is precisely the thing this task exists not to be); and **the `board` channel staying retired** (R9/DP13 — reintroducing it re-opens a contradiction round 1 reproduced). If a round's findings would require weakening any of those eight, the task **stops and returns to sprint planning** rather than shipping a weakened contract — T-1056 and T-1057 are both specified against it, and a contract weakened after they are planned is worse than one that arrives a sprint later.
