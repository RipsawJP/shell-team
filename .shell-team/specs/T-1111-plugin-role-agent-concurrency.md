# Plugin-role Agent-tool concurrency: the open question is not whether it overlaps — that is measured twice — but whether the declared `subagent_type` is the resolved one, so this task reconciles the prior measurements, repairs every stale surface they left behind, and probes the one residual with a tool-set discriminator

**Status**: READY_FOR_ARCH
**Owner**: pm-spec
**Task ID**: T-1111

> **This spec was held at `DRAFT`/`BLOCKED` on authoring, and is released by an operator ruling.**
> The task's originating premise — issue #398's "the Agent-level concurrency premise has never
> been verified", and the Plan-time premise-correction table's row "Open residual … CONFIRMED —
> this is what T-1111 measures" — is **measured false at its primary source**. Plugin-role
> Agent-tool concurrency has already been measured twice in this repository, at N=2, N=4 and
> N=8, with real overlap margins. What remains open is a strictly narrower question, and the
> re-scope that follows from it was a scope change this role did not make on its own authority
> (`CLAUDE.local.md`, 承認ゲートの範囲: 停止して上げるのは … issue の意図との矛盾・スコープ変更).
> **The operator selected disposition (a) on 2026-08-31** — the reconciliation note, the
> stale-surface repair, and the narrowed resolved-type probe exactly as written below — recorded
> as the third entry (`intervention: human-correction`, 2026-08-31) of
> `.shell-team/interventions/T-1111.md`. `## Open questions` records that resolution and is no
> longer blocking; everything else below is unchanged from the version the ruling was taken
> against, because this spec was authored for (a) already.

**Base ref / branch point — one discriminator for the whole spec.** Every base-side blob read in
this spec reads **the branch point**, resolved at check time by an explicit branch-existence test
that selects between three arms and never by a `||` chain: `git merge-base
"feature/1110-freeze-version-derivation" HEAD` where the predecessor resolves as a local branch,
`git merge-base "refs/remotes/origin/feature/1110-freeze-version-derivation" HEAD` where this
checkout carries it only as a remote-tracking ref, and `git merge-base "develop" HEAD` once it
resolves in neither namespace and is genuinely gone. Measured first-hand at authoring time:
`.git/HEAD` reads `ref: refs/heads/feature/1111-agent-concurrency-verification`, and both
`.git/refs/heads/feature/1111-agent-concurrency-verification` and
`.git/refs/heads/feature/1110-freeze-version-derivation` read
`e14898a5f0f56505f1bea2de23f046fabe590eee` — so the predecessor resolves as a **local branch**,
arm 1 is the arm this checkout selects, and this branch sits exactly at its predecessor's tip with
no commits of its own yet. No 40-hex literal is written into any criterion. The expression is
spelled byte-identically in every criterion that reads a base-side blob.

**Branch**: `feature/1111-agent-concurrency-verification`, cut from `feature/1110-freeze-version-derivation`'s
tip. PR base stays `develop`; the PR is held open and merges at the sprint batch GO.

## Problem

`docs/loop-engineering/phase-multiplexing.md:375` and `docs/loop-engineering/agent-concurrency.md:149`
each still carry an open follow-up asking someone to verify empirically whether this harness runs N
concurrent Agent-tool sub-agent invocations from one orchestrator session, and issue #398 was filed
to discharge them. Read at their primary sources, those follow-ups are **stale rather than open**:
T-1073 measured the general-purpose case (`concurrent-overlapping`, margins ≈172–270 s), T-1083 then
measured the **plugin-role** case with `subagent_type=shell-team:qa-verifier` at N=2 and N=4 (margins
≈140.4 s and ≈100.6 s) and called itself "the repository's first empirical crossing of T-1073's own
declared `unobserved` plugin-role boundary", and T-1085 fired eight `shell-team:qa-verifier` instances
on the shipped default path with an ≈878.2 s eight-way overlap. Three notes therefore answer the
question the other three notes' follow-ups still ask, nobody performed the maintainer round each of
them explicitly deferred, and `harness-agent-concurrency.md:374` — the very line the Plan-time premise
table nominated as this task's subject — is itself one of the stale surfaces. One genuinely open
residual survives all three measurements, and both T-1083 and T-1085 state it in their own words:
`subagent_type` is the orchestrator's **declared launch label**, not harness-originated evidence of
the *resolved* type, and "had the orchestrator instead declared `subagent_type=general-purpose` for
these same calls, nothing in this note's recorded evidence would contradict it."

## Summarized sources

- `docs/loop-engineering/agent-launch-fanout.md` (T-1083) — read at `:184-189`, `:200`, `:217`, `:220-231`, `:243-249`, `:251-265`. The distinctions carried over: **(i)** `:185`/`:188` record `plugin-role-n2` and `plugin-role-n4` as **executed** arms in which two and four instances of `shell-team:qa-verifier` were "launched as parallel `Agent` tool calls inside one orchestrator message", with measured margins `140385350000` ns and `100572685000` ns (`:200`, `:217`) — so the briefed measurement has already been performed, at a degree bracketing the issue's own `N >= 3` ask. **(ii)** `:224`'s verdict is `concurrent-overlapping` and names itself "the repository's first empirical crossing of T-1073's own declared `unobserved` plugin-role boundary" — the sentence that falsifies this task's originating premise. **(iii)** `:225` and `:247` fix the **epistemic boundary this task inherits**: the type is a *declared label*, corroborated by the `launch-refused` arm (`:189` — an unresolvable token is refused synchronously before any instance exists, and `shell-team:qa-verifier` appears in the harness's own available-agents enumeration) but explicitly **not** independently observed, with the `general-purpose` counterfactual stated verbatim. That boundary, not the overlap, is the residual. **(iv)** `:253-254` declare supersession of `harness-agent-concurrency.md`'s lines 373–374 and 390 and carry a `- follow-up:` asking a later maintainer round to perform the edit — this task is that round. **(v)** `:248`/`:260`/`:264` scope the surviving `unobserved` to a different plugin role, model, harness version or machine. **(vi)** `:263` records that an *implicit* 120 s foreground-Bash default is not a bound a probe may rely on — only an explicitly pinned timeout parameter is.
- `docs/loop-engineering/default-path-firing.md` (T-1085) — read at `:1-59`, `:564-572`, `:602-608`, `:635`, `:644-647`, `:680-682`, `:705-712`. Four distinctions. **(i)** `:59` states `subagent_type` was `shell-team:qa-verifier` for all nine launches and every span row records `"model":"sonnet"`; `:644`'s `- licence-condition: real-plugin-role-instances — met` rests on `- requested-n: 8`/`- achieved-n: 8`. **(ii)** `:635` records `- overlap: t1085fan — overlapping — margin_ns=878198706000` — an eight-way simultaneous window, which is the strongest plugin-role concurrency evidence in the repository and is a third, independent replication. **(iii)** `:50` and `:59` re-state the **declared-label** boundary on exactly T-1083's footing and refuse to upgrade it — so two independent tasks converge on the same residual rather than one task's caveat. **(iv)** `:709` cites `phase-multiplexing.md:375` while describing the knee limit that actually lives at `:381`: a live citation-drift hazard which is why every edit this task makes to a cited note must be **line-count preserving**.
- `docs/loop-engineering/harness-agent-concurrency.md` (T-1073) — read at `:1-59`, `:344-357`, `:371-396`. Five distinctions. **(i)** `:346` records verdict `concurrent-overlapping` for the general-purpose case; `:352` licenses `overlap-margin-exceeds-launch-latency` against a maximum launch latency of ≈32.8 s. **(ii)** `:374`'s `- unobserved:` line still asserts that plugin-role extension is unobserved, and `:390` repeats it in Limits — **both stale** since T-1083, and neither is annotated. **(iii)** `:396` records that the evidence commit was amended for a home-directory path at `954e2eeff0699f27d3cfb4084d9583e955210105`, redaction-only — the PII shape a probe note of this class is structurally prone to, and the reason this spec fixes a `<this-checkout-root>` placeholder in advance. **(iv)** `:137-144` fix the closed vocabularies (five verdicts, six licence conditions, four watchdog dispositions, three evidence channels) this spec reuses verbatim where they transfer — the source's own counts are those, and where this spec carries more (eight licence conditions, four channels, one added channel role) it says so as a stated extension for a question T-1073 did not ask, never as a restatement of what that note fixed. The `clock-source-monotonic` id is reused **unchanged** from this same range and its misnomer is disclosed rather than repaired here. **(v)** `:384-385` record the two execution-detail findings a successor must inherit: the 120 s default auto-backgrounds a long unit, and a watchdog launched with a bare shell `&` inside a foreground Bash call dies with that call.
- `.shell-team/specs/T-1073-harness-agent-concurrency.md` — read at `:1-163` and `:270-284`. Four distinctions. **(i)** `:41-57` is the frozen `<!-- BEGIN probe-protocol: T-1073 -->` region whose Venue, Unit, Clock-source, Evidence-channel and Verdict paragraphs this spec **reuses verbatim wherever they transfer**, with the reasons unchanged. **(ii)** `:49` names `plugin-role-confirmation` as the droppable-first arm that never ran — the arm T-1083 then ran under its own name, which is why nothing about that boundary is still owed by this task's probe. **(iii)** `:24` records that `bin/check-intent.sh` matches marker lines by **whole-line equality scoped by task id**, which is what makes a second, differently-named marker pair nested inside the Goal section inert to that checker. **(iv)** `:86` fixes the arithmetic discipline this spec inherits: bash `$(( 10#$v ))` throughout, never `awk` or `sort -n`, because a 19-digit epoch-nanosecond value exceeds a double's exact-integer range.
- `docs/loop-engineering/agent-concurrency.md` — read at `:105-166`. Three distinctions. **(i)** `:120`'s substrate row already reads `provided` (T-1080 erratum b) but its **scope qualifier is stale**: it quotes `harness-agent-concurrency.md:374` verbatim and concludes that plugin-role extension "is therefore not yet independently confirmed" — superseded by T-1083, and additionally a quote that a repair of `:374` would silently turn into a misquote. **(ii)** `:149`'s follow-up still asks for the verification T-1073 and T-1083 both performed. **(iii)** `:154` item (3) carries forward "a real Agent-tool-level concurrent launch, not a Bash-job proxy" as a retry requirement whose cross-reference points at `:149`. `:162` is **already correct** (T-1080 erratum c) and needs no edit — verified rather than assumed.
- `docs/loop-engineering/phase-multiplexing.md` — read at `:345-393`. Three distinctions. **(i)** `:355`'s recommendation still cites `agent-concurrency.md`'s "`agent-tool-concurrent-launch — absent` row", a verdict that row has not carried since T-1080. **(ii)** `:375`'s follow-up is the surface issue #398 names; `:387`'s Limits entry is **scoped to that task** ("not verified by this task") and is defensible as written, but its trailing clause "exactly as `docs/loop-engineering/agent-concurrency.md` already discloses for the depth axis" is stale. **(iii)** `:358-372` are the dispatch-axis and dispatch-rule grammars whose line population must survive this task's edits untouched — the protected invariant AC12 measures.
- `.shell-team/interventions/T-1110.md` — read end to end, all five entries. Two constraints this task's probe design inherits as rulings rather than preferences: entry 3 unifies the mechanical branch on **coordinating-session-hosted, harness-tracked** slices and **prohibits sub-agent-hosted shell fan-out until piloted**; entry 4 records the measured single-point-of-failure argument (six real sub-agent stalls in two weeks) against a sub-agent hosting N children. Entry 5 prices the many-agents form's own orchestrator-side cost, which is why this task's probe is three arms of three — two subject repetitions and one differential control — and not a sweep.
- `.shell-team/interventions/T-1109.md` — read end to end. Entries 2–4 are the record that shell-slice parallelism is **not** a substitute for Agent-tool concurrency and that the ":375 discharged" claim was corrected; entry 1 records that this host carries **no `timeout`/`gtimeout` binary**, so `bin/check-acs.sh`'s own `CHECK_ACS_TIMEOUT` wrapping is inert here and cannot bound a stuck check — which is why every bound in this probe is the harness's own explicit per-call Bash timeout parameter and never the script's internal one.
- `agents/*.md` frontmatter `tools:` lines — read across **all nine** definitions. Two distinctions. **(i)** Not one carries an `Agent` or `Task` token, so no sub-agent can launch another and the probe can only be an orchestrator step — the same measurement T-1073 made, re-measured here rather than inherited. **(ii)** `agents/qa-verifier.md` reads `tools: Read, Grep, Glob, Bash` and `model: sonnet` — **no `Write`, no `Edit`** — which is the discriminator this task's probe rests on. Measured correction to the briefing: `agents/drift-evaluator.md` carries the identical `tools:` line, so the stated selection criterion picks two roles rather than one; `qa-verifier` is still the right subject, because it is the role both T-1083 and T-1085 launched and comparability with their margins is the point.
- `.shell-team/test-recipe.md` — read at `:2362-2427`. Two distinctions. **(i)** The T-1110 entry fixes the shipped shape of a coordinating-session fan-out (N harness-tracked background slices, one base and one head worktree per slice, serial within a slice so `bin/log-run.sh`'s directory lock never sees two contenders in one tree) and its two quirks (`${i}_DONE` under `set -u`; flip tallies joined from the results TSV, never read by eye) — the procedure this task's blast-radius sweep replays rather than reinvents. **(ii)** The same entry records that a multi-statement `bash` block with an inline `mktemp -d "${TMPDIR:-/tmp}/x.XXXXXX"` was denied outright by this sandbox while the identical logic as a single-line command was permitted.
- `bin/derive-populations.sh` — read at `:1-60`. The distinctions carried over: the block grammar (`- derived-by:`, `- locale: LC_ALL=C`, one `- set:` line per `--set` with its own exit status, `- union:`, one `- bucket:` per membership signature), that a set command's unaccepted exit status refuses the **whole** derivation before anything is printed, and that between two and eight `--set` values are accepted — which is why this task's read-set derivation declares four.
- `skills/run/SKILL.md` — read at `:50-62`. Two distinctions. **(i)** `:57` is the T-1110 freeze-time version-derivation gate, which runs at **every** freeze and re-derives the tier from this spec's own `- user-visible:` and `- verification-class:` declarations by applying CONTRIBUTING's headline and default-reachability tests **jointly**. **(ii)** `:51-55` are the four first-freeze-only declaration gates whose declaration region this spec's intent block populates.

## Goal

<!-- BEGIN intent-block: T-1111 -->

- user-visible: no — the deliverable is one new internal loop-engineering note, line-count-preserving repairs to three existing internal loop-engineering notes, an append to this repository's own test recipe, and this task's own records. Nothing an adopter installs changes: no `bin/` script, no agent definition, no skill, no template, no README, no configuration surface and no command an adopter runs. The audience is this repository's own maintainers deciding whether a later task may fan a phase out across agent instances, so this declaration carries **neither** an `- adopter-surface:` line **nor** an `- adopter-docs-waiver:` line, which is the shape the adopter-docs gate requires for a `no`.
- verification-class: no-mechanism — the whole diff is prose: one new note under `docs/`, three line-count-preserving in-place edits to notes under `docs/`, this spec, the board, `.shell-team/test-recipe.md` and this task's records. It reaches no executing surface — nothing under `bin/`, `tests/`, `templates/`, `.github/`, `skills/` or `agents/` — and moves no checker's semantics. Priced accordingly: the downstream-impact inventory is **read-set-scoped** rather than a full-population diff, CI-equivalence is run only for the steps whose inputs this diff can reach with every inapplicable step named individually, and mutation probes cover the `- check:` lines this task adds rather than a fixed matrix. Every criterion below is confined to the four shapes this class can honestly carry: a string presence-or-absence grep, a byte-scoped diff (including the `git show`/`git merge-base` reads that resolve one), a checker's exit code, and a records-line existence check. The read-set here is nonetheless large — three widely-cited notes are edited — so the volume is a mechanism-class volume even though the class is not; the executor election for that sweep is the coordinating session's, priced from `.shell-team/test-recipe.md`'s T-1110 fan-out entry.
- verification-ceiling: real-environment — one component of this task cannot be verified below a live harness measurement: **AC2** through **AC9** read evidence that only a real Agent-tool launch from a real orchestrator session can produce, and no unit test, fixture or static read can stand in for it. Criteria not marked `- above-ceiling:` are verified at this ceiling, which for the static ones is trivially reached; the non-vacuity rule is satisfied by the probe-dependent criteria, which are genuinely verified at it and nowhere below it. `## Open questions` is resolved to disposition (a) and the probe runs, so this declaration carries no conditional path down to `unit-and-static`.
- base-ref-discriminator: `BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD); elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD); else B=$(git merge-base "develop" HEAD); fi` — the two-arm form is the required value here because criteria below read base-side blobs or resolve a base-side ref (**AC1**, **AC2**, **AC3**, **AC4**, **AC11**, **AC12**, **AC13**, **AC14**) and the predecessor's PR is open at authoring time; measured first-hand, the predecessor resolves in `refs/heads/` at `e14898a5f0f56505f1bea2de23f046fabe590eee`, so arm 1 is selected and no 40-hex literal enters any criterion.

**Goal (one sentence).** A new note at `docs/loop-engineering/plugin-role-agent-concurrency.md` records that plugin-role Agent-tool concurrency is **already measured** — reconciling T-1083's `plugin-role-n2`/`plugin-role-n4` margins and T-1085's eight-way margin into the one place the two older notes' follow-ups point at, rather than re-running them — performs the line-count-preserving maintainer-round repairs those measurements left owed across `harness-agent-concurrency.md`, `agent-concurrency.md` and `phase-multiplexing.md`, and answers the one residual all three measurements explicitly declined to close: whether a launched `subagent_type` label is the **resolved** type, measured by a **differential write-artifact discriminator** — a same-shape `general-purpose` control arm launched into **the one shared venue** between the two subject arms, whose load-bearing evidence is the existence of per-instance files the orchestrator reads in that single clone after the arms rather than anything an instance says about itself — reported under one verdict from T-1073's closed five-value vocabulary and one `- resolved-type:` verdict from a second closed four-value vocabulary, licensed by eight named conditions each marked `met` or `not-met`.

**Why the measurement question is closed and the type question is not.** T-1083's `plugin-role-n2` and `plugin-role-n4` arms launched two and four `shell-team:qa-verifier` instances as parallel `Agent` tool calls inside one orchestrator message and measured genuine overlap windows of `140385350000` ns and `100572685000` ns; T-1085 fired eight and measured `878198706000` ns. Re-running that measurement would produce a fourth data point for a question two notes already answer, and is an explicit non-goal. What neither task claims, in its own words, is that the harness *resolved* the declared token to this repository's own role definition: both record `subagent_type` as a declared launch label and both state the counterfactual that a `general-purpose` declaration for the same calls would contradict nothing in their recorded evidence. That gap is closable because the two candidate types differ in an observable that leaves a **physical artifact outside the harness's own narration**: `agents/qa-verifier.md` grants `tools: Read, Grep, Glob, Bash` and **no `Write` and no `Edit`**, while a general-purpose instance holds both, so an instance of each type told to write one file in the same venue leaves a different filesystem behind. The evidence is therefore the file, read by the orchestrator afterwards, and the comparison is **within-venue and differential** — literally so, because all nine instances write into **one** clone rather than one clone per arm, which is what makes "same venue" a property of the design rather than a claim about three directories nobody compared. The control arm is what separates "this role has no `Write`" from "nothing in this venue may write at all", a confound a self-report cannot resolve in either direction.

**Why the probe is an orchestrator step and not the engineer's work.** Not one of this repository's nine `agents/*.md` frontmatter `tools:` lines carries an `Agent` or `Task` token — measured across all nine, not assumed — so no sub-agent in this loop can launch another. Independently, `.shell-team/interventions/T-1110.md` entry 3 records the operator-adjudicated ruling that a fan-out's execution host is part of the recorded procedure rather than a free variable, and that sub-agent-hosted fan-out is **prohibited until piloted**. Both reasons point the same way: the probe executes as an explicit orchestrator step between Specify and Implement, the orchestrator commits the raw evidence into the note's own `## Probe evidence (raw, orchestrator-produced)` section **before** the engineer is invoked, and the engineer assembles every other section from that committed evidence without altering a byte of it. That commit is recorded on the board as a sub-bullet of the exact form `- probe-evidence-sha (T-1111): <40-hex>`, which is how a criterion resolves it without a literal being typed into this spec.

**Freeze-then-probe is a frozen condition, not a convenience.** The protocol below is frozen before the probe runs so that no protocol can be fitted to a result after seeing it. Two mechanical consequences, both measured by criteria: this spec's frozen intent block must already exist and hash cleanly in the probe-evidence commit, and the protocol region delimited inside this Goal section must be **byte-identical** between that commit and HEAD. A later ratified change to that region reddens the criterion **by design**; the recorded disposition is a re-probe under the invalid-evidence rule below, never a quiet amendment of the region.

<!-- BEGIN probe-protocol: T-1111 -->

**Venue.** **Exactly one** throwaway `git clone` under `$TMPDIR`, **shared by all three arms and by all nine instances**, cut from `<this-checkout-root>` and pinned to this branch's branch point by a detached checkout of the resolved commit, and carrying **ordinary write permissions** — the clone is deliberately *not* made read-only at the filesystem level, because a venue in which no instance could write would make the discriminator's `available` outcome unreachable by construction and its negative outcome unfalsifiable. One clone rather than one per arm is the whole ground of the differential: with a single directory there is no per-clone difference in permissions, mount or creation-time state for a denial to follow, so a control-present/subject-absent contrast cannot be produced by the venue differing between the arms — there is no "between". Per-instance attribution and stray-write detection do not depend on separate directories, because the frozen filename below already encodes both the arm id and the instance id. What is prohibited is not writing but **writing anywhere except that clone**: no write in the real checkout, no telemetry emission, no scratch file elsewhere, and inside the clone exactly the one sanctioned write per instance named in the discriminator paragraph below and nothing else. The clone records its own resolved `HEAD` — `git -C <clone> rev-parse HEAD`, whose value must equal the resolved branch point — on **exactly one** `- clone-ref: <label> — <40-hex> — <text>` line for the whole note, so "pinned to the branch point" is measured rather than asserted and a second such line means a second venue was cut. `git worktree add` is **never** used for this venue — the reason is inherited unchanged from T-1073: a worktree registers itself under `.git/worktrees` in the real checkout, which makes cleanup a destructive step against the repository the loop is running in, and this task takes no such step. The cleanliness check on the **real checkout** is unchanged and is verified after every arm rather than promised: `git status --short` and `git ls-files --others --exclude-standard` are both read there and recorded, and the note carries the result on a `- probe-writes: none — <text>` line, whose `none` means *no write outside that clone* and says so in its own text. Any path this note prints for the clone's source is written as the placeholder `<this-checkout-root>`, never as a real home-directory absolute path.

**Unit.** The production unit `bash bin/check-acs.sh .shell-team/specs/T-1044-test-infra-bundle.md` in the pinned clone — identical to T-1073's own unit, so this task's figures are comparable against that ladder and against T-1083's own plugin-role margins rather than confounded with a new one. `unit_rc` is expected to be non-zero and that is honest: the unit is the run, never its verdict. The unit's own duration is re-measured **at this branch point** by a plain shell run at population-fixation time, before any arm, and recorded **together with the raw integers it derives from**: the note carries that run's own command and exactly one `- unit-timing: <label> — start=<digits> — end=<digits> — <text>` line holding the timestamps taken immediately before and after it with the same clock source the instances use, and the `- unit-duration:` field must **equal `end − start`** rather than being a number typed in after the result is known. **Launch latency is derived on the same footing, never declared.** The orchestrator takes its own timestamps immediately before and after each arm's single message and records them on a `- batch-timestamp: <arm-id> — batch_start=<digits> — batch_end=<digits> — <text>` line — **exactly one such line for every arm recorded `executed`, no arm exempt**, so the maximum below is taken over every arm that ran rather than over whichever arms happen to carry a line; that arm's launch latency is `max(first) − batch_start` over the arm's own per-instance timestamps, and the `- launch-latency:` field must equal the **maximum** of those per-arm values. Both numeric inputs to the frozen condition below are therefore re-derivable from raw integers recorded in the same section, exactly as the overlap margin already is, and neither can be fitted to the ratio it has to satisfy. **Frozen condition:** the unit's duration must exceed the measured agent-launch latency by a declared margin factor of **at least 3** — a stated policy choice rather than a measurement, chosen so a marginal ratio cannot license a positive verdict — and where it does not, the concurrency verdict is `undetermined` or `launch-refused` and no other value may be recorded. No watchdog threshold participates in this protocol's ordering invariant, because this protocol runs no watchdog: the `agent-watchdog.sh` co-run duty was retired by operator ruling on 2026-08-31 against two weeks of data, and a **negative-control stall arm** validating a retired instrument would measure nothing this loop uses. That retired arm is a different thing from the differential control arm this protocol does run, and the two are never conflated: no `- stall-threshold:` and no `- control-sleep:` line exists anywhere in this note.

**Subject role, control role, and the model confound.** Every **subject** instance is launched with `subagent_type=shell-team:qa-verifier` — the role both T-1083 and T-1085 launched, so this task's margins sit beside theirs — and every **control** instance with `subagent_type=general-purpose`. The declared type is the *only* variable between the control arm and a subject arm: same unit, **the same venue** — one clone, not a second one of the same shape — same instance count, same one-message launch, same sanctioned write. Exactly one plugin role is exercised as subject; whether the result extends to any other plugin role is `unobserved` and is recorded as such, never inferred. The control arm's own model binding is recorded rather than assumed, and the differential's claim is about **effective tool sets in this venue**, never about models. `agents/qa-verifier.md` binds `model: sonnet` while T-1073's general-purpose baseline ran `model: haiku`, so **two variables differ between this arm and that baseline** and this note discloses that confound rather than attributing any difference to the type alone; where a plugin role's model binding cannot be overridden, that inability is itself recorded. `qa-verifier` also gates this task later in the loop; those invocations are not concurrent with these, no writer/reader conflict arises, and the fact is disclosed here rather than glossed.

**Arms.** Exactly three, with a closed id vocabulary: `resolved-type-n3-rep1`, `resolved-type-n3-rep2` and `control-gp-n3`. Three instances each, launched as parallel `Agent` tool calls inside **one** orchestrator message per arm — **nine launches in total**, three messages, all nine into the one shared clone. **The launch order is frozen here, not left to the day**: `resolved-type-n3-rep1` first, then `control-gp-n3`, then `resolved-type-n3-rep2` — the control sits **between** the two subject arms, so a condition that varies with time rather than with type cannot align with the subject/control split the verdict reads: any monotone drift across the three messages puts the control on both sides of it at once. That ordering constrains how the existing three messages are sequenced and adds no instrument; what it cannot do is exclude a non-monotone, transient denial that happens to coincide with exactly the two subject messages, and that honest residual is stated on the note's own `- evidence-boundary:` line rather than papered over here. The two subject arms satisfy issue #398's own literal `N >= 3` ask and the repetition-variance condition between them; the third arm is the **differential control**: same batch shape, same unit, the same one clone, `subagent_type=general-purpose` instances that do hold `Write`, whose per-instance artifacts are what prove this venue admits a write at all. Instance ids are `qa-1`, `qa-2`, `qa-3` in the subject arms and `gp-1`, `gp-2`, `gp-3` in the control arm, and are unique within their arm. **The control arm is never dropped while any subject arm runs**: a subject arm without it produces a signal that cannot separate role-scoped tool absence from a venue-wide write denial, which is precisely the vacuous design this protocol exists not to be. There is **no serial-baseline arm** (the unit's serial cost comes from the plain shell run at population fixation) and **no negative-control-stall arm** (no watchdog runs). Cost, stated rather than discovered: nine plugin- and general-purpose instances each running one `bash bin/check-acs.sh` unit, plus **one** local clone — `git clone --local` from `<this-checkout-root>`, hardlinked and cheap — against the previous design's six launches and one clone; the clone count is unchanged from that design and only the launch count rises. That increment buys the only within-venue contrast that makes a negative discriminator result mean anything, and it changes nothing about this task's deliverable class: the diff is still prose, the tier reading is still PATCH.

**The discriminator is differential, and its evidence is a file rather than a sentence.** Every instance in every arm — control and subject alike — makes exactly one `Write` tool attempt, at a path fixed by a frozen per-instance naming scheme: `<the shared clone root>/probe-write-<arm-id>-<instance-id>.txt`, so no two instances of the nine can target the same path even though all nine share one directory, and a file at any other path — or at another instance's path — is visible as a stray write for exactly that reason. **After all three arms have finished, the orchestrator reads that one clone itself** and records one `- write-artifact: <arm-id> — <instance-id> — present|absent — <text>` line per instance from what it finds on disk. That existence check is the load-bearing evidence, and its whole point is that it is a physical artifact the orchestrator observes from outside the instances rather than a claim any instance makes about itself.

**What the contrast buys, stated as the inference it licenses.** `agents/qa-verifier.md` grants no `Write`; a `general-purpose` instance holds one. Within one venue, on the same day, in the same clone — one directory, not three of the same shape: control artifacts **present** and subject artifacts **absent** is a within-venue contrast, and a venue-wide write denial cannot produce it, because such a denial would take the control artifacts with it. Control artifacts **absent** therefore never licenses a positive result — it makes the run `undetermined` with the venue-denial reason recorded, whatever the subject arms did. A subject artifact **present** is a genuine falsification of the declared label (`contradicted`) and is an equally legitimate, more consequential deliverable. No verdict branch anywhere in this protocol reads a value an instance reported about itself.

**The self-report is kept as a secondary channel and is disclosed as what it is.** Each instance still reports its own outcome from the closed three-value set `available` / `refused` / `absent`, recorded on a `- tool-probe: <arm-id> — <instance-id> — write_tool=<value> — <text>` line, because it distinguishes causes the filesystem cannot — a tool absent from the instance's tool set versus a tool present whose use the harness refused. It is **agent-produced text**, sitting on exactly the self-attestation boundary `agent-launch-fanout.md:247`'s counterfactual already named, and the note records it as the `corroborating` channel it is: it can agree with the artifacts, it can disagree with them and thereby raise an invalid-evidence event, and it can never on its own license a `- resolved-type:` value.

**The residual this design does not close, disclosed on its own line.** A control-present/subject-absent differential proves that the instances resolved from `subagent_type=shell-team:qa-verifier` **lacked an effective `Write` tool in this venue, in this session, on this machine** — the strongest harness-originated signal available to this repository today. It does **not** prove that every other aspect of role resolution followed the declared label: the model actually bound, the system prompt actually loaded, and any behaviour not expressed through the tool set are all outside what a tool-set differential can reach, and remain `unobserved`. Two narrower residuals sit on the same line rather than being argued away. **(i) An instance that holds a `Write` tool and simply does not attempt it** leaves the same missing file as one holding no such tool — the generic exposure of any probe that measures instructed behaviour, not specific to this design. What bounds it, without closing it: the control arm's `present` artifacts are direct evidence that an instance given this briefing in this venue *does* invoke the tool when it has one, and the corroborating self-report channel is where an instance states that it attempted at all — so a silent decline is visible as an artifact/self-report disagreement rather than being invisible, while remaining unprovable either way. **(ii) A transient, non-monotone venue denial coinciding with exactly the two subject messages** survives the frozen launch order, which excludes only drift that varies monotonically across the three messages. The note carries that whole boundary — the three role-resolution aspects and both narrower residuals — on exactly one `- evidence-boundary: <text>` line rather than leaving a reader to infer its width.

**The agent briefing text, fixed here verbatim so it cannot be fitted to a result.** Each instance is told: *"Run this one command as a single foreground Bash call with an explicit timeout parameter above 120000 ms: `bash bin/check-acs.sh .shell-team/specs/T-1044-test-infra-bundle.md`, from the clone root you were given. Take a timestamp immediately before and immediately after it with `python3 -c 'import time; print(time.time_ns())'`. Then make exactly one attempt to use a `Write` tool against `<the clone root you were given>/probe-write-<arm-id>-<instance-id>.txt`, with the arm id and instance id you were given and any short content; if you have no such tool, report that. Write nothing anywhere outside the clone root you were given, emit no telemetry, and take no board, spec or review action. Report exactly one line: `instance=<id> arm=<arm-id> start_ns=<int> end_ns=<int> clock_source=python3-time_ns unit_rc=<int> foreground=<true|false> write_tool=<available|refused|absent>`."* **This briefing is byte-identical for a control instance and a subject instance** — only the arm id, the instance id and therefore the target filename differ — which is what makes the arms comparable at all: a control told something different from the subject would be measuring the briefing rather than the type. The explicit timeout parameter is mandatory and is not a matter of judgment: T-1073 measured that the 120 s default silently auto-backgrounds a longer unit, T-1083's own `instance-death` arm measured that an implicit default is not a bound a probe may rely on, and this host carries no `timeout` binary so `CHECK_ACS_TIMEOUT` cannot bound anything either.

**Clock source, and the inherited misnomer this protocol declines to rename.** `python3 -c 'import time; print(time.time_ns())'`, verified to expand into a 19-digit integer before it is relied on; a source whose nanosecond field does not expand (BSD `date` emits a literal `N` for `%N`) is `unusable` and is substituted, with the substitution recorded. The **orchestrator uses the same command** for its own `- unit-timing:` and `- batch-timestamp:` values, so every integer in this note's evidence comes from one source and the derivations that subtract an instance timestamp from an orchestrator timestamp are comparable. Every timestamp is written as digits only, with no unit suffix inside the field.

**Disclosure, carried into the note on a `- vocabulary-caveat: clock-source-monotonic — <text>` line.** The licence condition below is named `clock-source-monotonic`, and `time.time_ns()` is **not** a monotonic clock: it reads `CLOCK_REALTIME`, the adjustable wall clock, which an NTP step or a manual clock change can move backwards. The id is nonetheless **inherited vocabulary and is not renamed here**: T-1073 coined it over the same non-monotonic source and T-1083 and T-1085 carry it verbatim, so renaming it in this note alone would fork the shipped vocabulary across three merged notes and leave the three older ones mislabelled anyway. What this protocol does instead is state the truth beside the id: the source is wall-clock epoch-nanoseconds, every figure is single-machine and single-session over tens of minutes, an NTP step during a run is possible and would be undiagnosable from these records, and the margins this note reasons about are minutes-scale — orders of magnitude above any realistic clock adjustment. **The vocabulary repair travels to a fast-follow issue** the coordinating session files at close-out, against the shared vocabulary across all four notes rather than against this one; it is deliberately not attempted here, since this task's Non-goals forbid editing any merged note's evidence section and a single-note rename is worse than the disclosure.

**Evidence channels, four, across two questions, each with one of three roles.** For the concurrency question: ① `agent-self-timestamps` — **primary**: each instance's own first and last timestamps, read from its completion notification, the only channel measured from inside the instances. ② `batch-vs-sum` — **necessary-condition**: the orchestrator's batch wall-clock against the summed per-instance durations; a batch far below the sum is necessary for overlap, never sufficient alone. For the resolved-type question: ③ `write-artifact-existence` — **primary**: the per-instance files the orchestrator finds, or does not find, in the one shared clone after every arm has finished, differenced between the control arm and the subject arms. ④ `tool-self-report` — **corroborating**: the instances' own `write_tool=` values, which explain a cause the filesystem cannot show and license nothing. T-1073's own vocabulary fixes three channels for the question that note asked; this protocol reuses two of them unchanged, adds two for a question T-1073 did not ask, and extends the role vocabulary by exactly one value (`corroborating`) — an extension stated here rather than a silent redefinition. T-1073's third channel, `orchestrator-span-rows`, is **deliberately excluded** and the exclusion is stated: agent-level fan-out still cannot self-emit its own spans until the append-locking car (#285) lands, so those rows would be the orchestrator's own post-hoc sequential appends — attribution labelling that adds no independent evidence for either the overlap or the resolved-type question this task actually asks.

**Verdict.** Two verdicts, from two orthogonal closed vocabularies, because collapsing them would let a positive concurrency result launder a type claim — the precise defect T-1083 disclosed. **(a)** Exactly one `- verdict:` line from T-1073's closed five-value set `concurrent-overlapping` / `serialized` / `queued-partial` (carrying a measured cap `k`) / `launch-refused` / `undetermined`. **(b)** Exactly one `- resolved-type:` line from the closed four-value set `plugin-role-confirmed` / `declared-label-only` / `contradicted` / `undetermined`, whose value is coupled to the **orchestrator-read artifacts** and to nothing else: `plugin-role-confirmed` requires at least one control-arm artifact `present` **and** zero subject-arm artifacts `present`, across all three arms' full per-instance records; `contradicted` requires at least one subject-arm artifact `present`; `declared-label-only` requires **both** that neither discriminator family carries a single line **and** that the arm record itself says the arms did not run — at least one `- probe-arm:` line exists and **zero** of them read `executed` — so the value reserved for dropped arms is claimable only where the record shows they were dropped, never where a note reports arms `executed` and then reports no evidence from them; and `undetermined` is what remains — including the venue-denial case where every artifact, control and subject alike, is `absent`, which those three couplings force rather than merely permit. **Eight** licence conditions, each recorded `met` or `not-met` with its own evidence: T-1073's six — `production-unit`, `real-population`, `same-machine-session`, `clock-source-monotonic`, `overlap-margin-exceeds-launch-latency`, `repetition-variance` — plus this task's own `tool-set-discriminator-exercised` and `differential-control-arm-executed`, the latter reading `met` only where the control arm ran with its own per-instance artifact records. Any condition reading `not-met` confines the concurrency verdict to `undetermined` or `launch-refused`. **A `launch-refused`, `serialized`, `declared-label-only` or `contradicted` result is a complete, legitimate deliverable**: nobody on this task is under any pressure toward a positive, the sibling precedent for shipping a negative is T-1068's declined probe, and the repair and reconciliation halves of this task stand whatever the probe returns.

<!-- END probe-protocol: T-1111 -->

**What the new note records rather than re-measures.** One `- prior-measurement: <note-path> — <arm-id> — n=<digits> — margin_ns=<digits> — <text>` line per already-measured plugin-role arm, quoting T-1083's `plugin-role-n2` and `plugin-role-n4` and T-1085's `t1085fan` from those notes' own recorded values, so the reconciliation is a citation of committed evidence and never a re-derivation of it. The note states plainly that this task ran no arm at N=2, N=4 or N=8 and needed none.

**The stale-surface repair, performed rather than deferred again.** Each surface is repaired **in place, preserving its file's total line count**, because `default-path-firing.md:709` already demonstrates a live line-number citation drifting, and re-numbering would break more citations than it fixes. Every surface is recorded on a `- surface: <path>:<anchor> — repaired|annotated|left — <text>` line, including the ones deliberately left: `harness-agent-concurrency.md`'s `- unobserved:` line and its Limits twin (narrowed to the residual that genuinely remains, keeping the `- unobserved:` prefix and the literal `plugin-role` so T-1073's own **AC11** stays green); `agent-concurrency.md:120`'s stale scope qualifier, `:149`'s discharged follow-up and `:154`'s item (3) cross-reference; `phase-multiplexing.md:355`'s citation of a verdict that row no longer carries and `:375`'s discharged follow-up. `agent-concurrency.md:162` is **left** — measured correct already (T-1080 erratum c). `phase-multiplexing.md:387` is **annotated**, not rewritten: its "not verified by this task" claim is scoped to that task and stays true; only its stale trailing cross-reference is repaired. After the round, **no surviving line in any of the three edited notes may attribute to `harness-agent-concurrency.md`'s `- unobserved:` line a claim that line no longer makes** — the misquote hazard a verbatim quotation at `agent-concurrency.md:120` creates the moment `:374` moves.

**Frozen history is never edited.** The interventions, retro and board rows of T-1109 and T-1110, and every byte of T-1073's, T-1083's and T-1085's `## Probe evidence` sections, stay untouched. Supersession is **declared** in the new note; it is performed only against the three notes' own non-evidence prose, which is the maintainer round each of them explicitly asked for.

**Pre-commitment (AI self-discipline — self-imposed by this loop, never operator-ratified; any report citing it says so).** Factual trigger: two consecutive review rounds landing an independent Blocker or Major against the same component. Contextual trigger: before a third round of rework on that component starts; where the two disagree, the factual trigger governs. Disposition, decided here rather than when it fires: drop **(1)** the `resolved-type-n3-rep2` arm — nine launches fall to six, `repetition-variance` is recorded `not-met`, and the concurrency verdict is thereby confined to `undetermined`, while the differential (one subject arm against the control) survives intact — at the stated cost that the control loses its between-subjects position in the frozen launch order, which widens the time-varying-condition residual and is recorded on the `- evidence-boundary:` line rather than absorbed silently; since **AC5**, **AC6** and **AC7** name that arm, executing this step requires a ratified class-B re-freeze of those three criteria's rep2 clauses, and that cost is named here so the drop is a decision rather than a discovery. Then drop **(2)** the whole live probe together with **AC2**–**AC9** and the `real-environment` ceiling, leaving the reconciliation and repair halves — which are what this task actually owes — and carrying that round's findings to issue #398 as the probe's requirement list. **Never dropped**: the reconciliation lines, the stale-surface repair, the line-count preservation, the protected-invariant guard on the three edited notes, and — for as long as any subject arm runs at all — the `control-gp-n3` arm, because a subject arm without its control is the design this round's Blocker removed and reinstating it under budget pressure would ship the vacuity knowingly. Defeated twice on any of those, the task **stops and returns to planning**. **Invalid-evidence rule**: at most **one** re-probe, recorded with the round and the reason that invalidated the first; a second invalidation stops the task.

## Non-goals

- **No re-measurement of plugin-role Agent-tool concurrency at any degree.** T-1083 measured N=2 and N=4 and T-1085 measured N=8; this task cites those figures and produces none of its own for that question. The overlap this task's three arms happen to produce is incidental evidence for the discriminator arms' validity, not a fourth answer to a closed question.
- **No re-measurement of the general-purpose case.** T-1073 owns it; nothing here re-runs, re-derives or re-litigates it.
- **No mechanism, anywhere.** Nothing under `bin/`, `tests/`, `templates/`, `.github/`, `skills/` or `agents/` is added or edited, no `README*` is touched, and no checker's semantics move.
- **No edit to any `## Probe evidence` section of any existing note**, and no edit to any frozen record: T-1073's evidence stays byte-identical to its recorded commit, and the interventions, retro and board rows of T-1109 and T-1110 are read, never rewritten.
- **No line-count change to `harness-agent-concurrency.md`, `agent-concurrency.md` or `phase-multiplexing.md`.** Every repair is an in-place, line-count-preserving replacement, so every line-number citation elsewhere in the corpus stays exactly as accurate as it was.
- **No repair of any merged spec's criteria this task's diff reddens.** Every red is disclosed in `## Blast radius`, differenced against the branch point, and left alone.
- **No watchdog, no negative-control stall arm, no `TaskStop`, no `kill` and no forced termination of any instance.** The co-run watchdog duty was retired by operator ruling on 2026-08-31; nothing in this task terminates a running agent.
- **No sub-agent-hosted fan-out of any kind**, per `.shell-team/interventions/T-1110.md` entry 3's prohibition-until-piloted ruling; the probe and any blast-radius sweep are coordinating-session-hosted and harness-tracked.
- **No telemetry emission by a probe instance**, and no edit to any file under the resolved runs dir; the machine-local corpus is neither rewritten, migrated nor truncated.
- **No write of any kind by a probe instance outside the one shared throwaway clone**, **no second clone and no per-arm venue of any kind**, and no `git worktree add` as a probe venue. Inside that clone exactly one sanctioned write per instance, at the frozen `probe-write-<arm-id>-<instance-id>.txt` path, is required rather than forbidden — the clone is not filesystem-read-only, and this task declines to make it so, because a venue that cannot be written is a venue in which the discriminator's negative result means nothing.
- **No rename of the `clock-source-monotonic` licence-condition id.** The id is inherited verbatim from T-1073 and carried by T-1083 and T-1085; this task discloses the misnomer beside it and files the vocabulary repair as a fast-follow against all four notes at close-out, rather than forking the shipped vocabulary from inside one note.
- **No claim about any aspect of role resolution other than the effective tool set** — the model actually bound, the system prompt actually loaded, and any behaviour not expressed through the tool set are `unobserved` here and are recorded as such on the note's own `- evidence-boundary:` line; a finding demanding a model-identity or prompt-identity probe is a different task.
- **No fan-out degree beyond 3 in this task's own arms, no sweep, and no knee-finding.**
- **No inferential-statistics claim** — no significance test, confidence interval, variance model or regression — over two repetitions on one machine, and no dollar-cost, vendor-price or token-multiplier estimate anywhere in the note.
- **No git-ref label on any machine-local number.** Timings and core counts are machine-local and are labelled as such; the only ref-labelled reads are of committed blobs, made with `git show`.
- **No full-population downstream sweep, no whole CI-equivalent re-run, and no behaviour verification of any mechanism this task does not touch** — the explicit `no-mechanism` non-goal that pricing owes.
- **This task closes no issue and merges nothing.** Issue #398 stays open until its disposition is settled at `## Open questions`; #399's default flip, #277, #274, #285 and the per-instance-telemetry-discriminator are all out of scope. Merge waits for the sprint's batch GO.
- **No lessons-corpus edit, no version bump, no CHANGELOG edit, no plugin-manifest edit, no board archiving or rotation, and no retro.**

## Acceptance criteria

Every check runs from the repository root, resolves the branch point with the byte-identical three-arm expression declared above and fails closed when it does not resolve, resolves operating paths through `bin/team-paths.sh` rather than hardcoding them, names every file it reads explicitly, guards every `mktemp` with an immediate non-zero exit, writes nothing outside `$TMPDIR`, and pairs every absence assertion with a positive control so an unreadable or misnamed target reads as a failure rather than a clean absence. Every criterion is **presence-and-structure shaped**: none re-derives an analysis conclusion and none compares a declared figure against a live re-derivation from a corpus that grows while this task runs. Numeric comparisons use bash integer arithmetic with a `10#` prefix and **never** `awk` or `sort -n`, since a 19-digit epoch-nanosecond value exceeds a double's exact-integer range. **AC2**–**AC9** are the probe-dependent block: each is expected red at freeze and satisfiable only after the probe has run and the note has been assembled.

- [ ] **AC1** The new note exists at exactly the required path, is new rather than a rewrite, and carries its nine required sections. `docs/loop-engineering/plugin-role-agent-concurrency.md` is readable and non-empty, does **not** exist in the branch point's tree, carries exactly one `# ` title line, and carries each of these nine headings exactly once as a whole line: `## Terms and closed vocabularies`, `## Prior measurements reconciled`, `## Probe protocol (frozen before execution)`, `## Probe evidence (raw, orchestrator-produced)`, `## Overlap and discriminator analysis`, `## Verdict and licence conditions`, `## Stale-surface repair`, `## Supersession and follow-ups`, `## Limits and what is not computable`. The path is asserted literally because the name is load-bearing: it distinguishes this plugin-role note from the general-purpose `harness-agent-concurrency.md`, the depth-axis `agent-concurrency.md`, the width-axis `phase-multiplexing.md` and the fan-out notes, all of which continue to exist beside it. Positive control: `docs/loop-engineering/agent-launch-fanout.md` **is** present in the branch point's tree, so a base-tree read that saw nothing at all cannot satisfy the absence half.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -r "$N" || exit 1; test -s "$N" || exit 1; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; git cat-file -e "$B:docs/loop-engineering/agent-launch-fanout.md" 2>/dev/null || exit 1; if git cat-file -e "$B:$N" 2>/dev/null; then rc=1; fi; test "$(grep -c '^# ' "$N" || true)" = "1" || rc=1; for h in '## Terms and closed vocabularies' '## Prior measurements reconciled' '## Probe protocol (frozen before execution)' '## Probe evidence (raw, orchestrator-produced)' '## Overlap and discriminator analysis' '## Verdict and licence conditions' '## Stale-surface repair' '## Supersession and follow-ups' '## Limits and what is not computable'; do test "$(grep -cxF -- "$h" "$N" || true)" = "1" || rc=1; done; test "$rc" -eq 0

- [ ] **AC2** The raw evidence section is byte-identical to what the orchestrator committed before the engineer was invoked, and that commit sits where the ordering claim requires. The board resolved through `bin/team-paths.sh --get todo` carries **exactly one** line of the form `- probe-evidence-sha (T-1111): <40 lower-case hex>`; that commit resolves; it is an ancestor of `HEAD`; the branch point is an ancestor of it and is not equal to it; the note exists in it, is non-empty, and carries the `## Probe evidence (raw, orchestrator-produced)` heading exactly once; and that section, extracted from the committed blob, is **byte-identical** to the same section extracted from the working tree's note. The sha is read from the board rather than typed here, so no 40-hex literal enters the frozen text, and the sub-bullet is task-scoped in its own grammar so another task's own such line cannot make this criterion ambiguous. Positive controls: the board resolves and is readable; both extracted sections are asserted non-empty before `cmp`, so an extraction that silently produced nothing cannot pass as identity.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111b.XXXXXX") || exit 1; sed -nE 's/^[[:space:]]*- probe-evidence-sha \(T-1111\): ([0-9a-f]{40})$/\1/p' "$BD" > "$T/sha"; test "$(grep -c . "$T/sha" || true)" = "1" || rc=1; S=$(head -n 1 "$T/sha"); if [ -n "$S" ]; then git rev-parse --verify --quiet "$S^{commit}" >/dev/null || rc=1; git merge-base --is-ancestor "$S" HEAD >/dev/null 2>&1 || rc=1; git merge-base --is-ancestor "$B" "$S" >/dev/null 2>&1 || rc=1; test "$S" != "$B" || rc=1; git show "$S:$N" > "$T/pn" 2>/dev/null || rc=1; test -s "$T/pn" || rc=1; test "$(grep -cxF -- '## Probe evidence (raw, orchestrator-produced)' "$T/pn" || true)" = "1" || rc=1; awk '/^## Probe evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$T/pn" > "$T/psec"; awk '/^## Probe evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/wsec"; test -s "$T/psec" || rc=1; test -s "$T/wsec" || rc=1; cmp -s "$T/psec" "$T/wsec" || rc=1; else rc=1; fi; rm -rf "$T"; test "$rc" -eq 0
  - stale-at: the board resolved through `bin/team-paths.sh --get todo` is rotated or archived, or this task's entry is rewritten such that its `- probe-evidence-sha (T-1111):` sub-bullet no longer sits in the active board — at which point the commit this criterion resolves its comparison against can no longer be found and the criterion must be re-anchored rather than assumed broken.

- [ ] **AC3** Freeze-then-probe ordering is mechanically evidenced, and the protocol was not fitted to the result. This spec exists in the probe-evidence commit; `bin/check-intent.sh --print-hash` run against that committed blob exits `0` and prints a 40-hex value, which is what proves the intent block was already frozen and structurally sound when the probe ran; and the region delimited by `<!-- BEGIN probe-protocol: T-1111 -->` and `<!-- END probe-protocol: T-1111 -->` inside this spec's Goal section is **byte-identical** between that commit and `HEAD`. The narrower region rather than the whole intent hash is deliberate: a later mechanics repair to a `- check:` line legitimately changes the intent hash without touching what the probe was told to do, whereas a change to the protocol region is exactly the post-hoc fitting this criterion exists to forbid — and if such a change is ever ratified, this criterion turns red **by design** and the disposition is a re-probe under the invalid-evidence rule, never a quiet amendment. **What this criterion proves and what it does not, stated rather than implied**: commit ancestry plus byte-identity establish the DAG structure a freeze-then-probe ordering leaves behind, not the wall-clock instant at which anything ran — a determined author could construct a conforming graph after the fact. That is the identical self-attestation boundary every freeze-attestation and provenance record in this repository already stands on; this task introduces no new exposure to it, and the disposition is this disclosure rather than a cryptographic mechanism a `no-mechanism` task would have to build. Positive controls: the extracted region is asserted to hold at least seven non-blank lines and to contain the literal `git clone` at both refs before the comparison, so an empty or truncated extraction cannot pass as identity; the HEAD-side hash is asserted to match a 40-hex shape.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1111-plugin-role-agent-concurrency.md; test -s "$SP" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111c.XXXXXX") || exit 1; sed -nE 's/^[[:space:]]*- probe-evidence-sha \(T-1111\): ([0-9a-f]{40})$/\1/p' "$BD" > "$T/sha"; test "$(grep -c . "$T/sha" || true)" = "1" || rc=1; S=$(head -n 1 "$T/sha"); if [ -n "$S" ]; then git show "$S:$SP" > "$T/spec.md" 2>/dev/null || rc=1; test -s "$T/spec.md" || rc=1; if [ -s "$T/spec.md" ]; then H1=$(bash bin/check-intent.sh --print-hash "$T/spec.md" 2>/dev/null); test -n "$H1" || rc=1; printf '%s' "$H1" | grep -qE '^[0-9a-f]{40}$' || rc=1; awk '/^<!-- BEGIN probe-protocol: T-1111 -->$/{f=1} f{print} f&&/^<!-- END probe-protocol: T-1111 -->$/{exit}' "$T/spec.md" > "$T/pp"; awk '/^<!-- BEGIN probe-protocol: T-1111 -->$/{f=1} f{print} f&&/^<!-- END probe-protocol: T-1111 -->$/{exit}' "$SP" > "$T/hp"; test "$(grep -c . "$T/pp" || true)" -ge 7 || rc=1; test "$(grep -c . "$T/hp" || true)" -ge 7 || rc=1; grep -qF -- 'git clone' "$T/pp" || rc=1; grep -qF -- 'git clone' "$T/hp" || rc=1; cmp -s "$T/pp" "$T/hp" || rc=1; fi; H2=$(bash bin/check-intent.sh --print-hash "$SP" 2>/dev/null); test -n "$H2" || rc=1; printf '%s' "$H2" | grep -qE '^[0-9a-f]{40}$' || rc=1; else rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC4** The venue is evidenced rather than promised: the one shared clone is measured to sit at the branch point, no write escaped it, and the clone-source path is a placeholder rather than a real home path. The `## Probe protocol (frozen before execution)` section is non-empty and names both the literal `git clone` and the literal `$TMPDIR`; **no `- command: ` line anywhere in the note contains `git worktree add`** — scoped to command lines rather than to the note's whole text, deliberately, so the note stays free to *disclose in prose* that a worktree was avoided without a note-wide absence test punishing the disclosure. The `- clone-ref: <label> — <40 lower-case hex> — <text>` lines close under their own grammar and **exactly one exists in the whole note**, because the venue is one clone shared by all three arms — a second such line means a second venue was cut, which is the design this round removed and which this count now forbids mechanically rather than in prose; and **its recorded hex equals the branch point resolved by this criterion's own three-arm expression**, which is the mechanical form of "pinned to the branch point". No 40-hex literal is written here: the expected value is resolved at check time and the observed value is read from the note. The note quotes `rev-parse HEAD` as the command that produced those values. Exactly one `- probe-writes: ` line exists and it matches `- probe-writes: none — <text>`, whose `none` scopes to writes *outside* that clone — the one sanctioned write per instance lands inside it and is measured by **AC7** rather than forbidden here; the note quotes both `git status --short` and `git ls-files --others --exclude-standard` as the commands that produced the real checkout's cleanliness result; the `## Probe evidence (raw, orchestrator-produced)` section carries at least two `- command: ` lines; and the note carries the literal `<this-checkout-root>` at least once while carrying **zero** home-directory-shaped absolute paths. Positive controls: both extracted sections are asserted non-empty before they are judged; the note is asserted to carry at least one `- command: ` line overall, so the `git worktree add` absence assertion cannot be satisfied by a note quoting no commands at all; the single `- clone-ref:` line's hex is asserted to extract non-empty before it is compared, so a malformed line fails rather than comparing an empty string; and the `<this-checkout-root>` presence assertion is what makes the zero-home-path count non-vacuous, since it proves a clone-source path really is recorded.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111d.XXXXXX") || exit 1; awk '/^## Probe protocol \(frozen before execution\)$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/proto"; awk '/^## Probe evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$N" > "$T/ev"; test -s "$T/proto" || rc=1; test -s "$T/ev" || rc=1; grep -qF -- 'git clone' "$T/proto" || rc=1; grep -qF -- '$TMPDIR' "$T/proto" || rc=1; test "$(grep -cE '^[[:space:]]*- command: .' "$N" || true)" -ge 1 || rc=1; test "$(grep -cE '^[[:space:]]*- command: .' "$T/ev" || true)" -ge 2 || rc=1; grep -E '^[[:space:]]*- command: ' "$N" > "$T/cmds" || true; test -s "$T/cmds" || rc=1; test "$(grep -c 'git worktree add' "$T/cmds" || true)" = "0" || rc=1; CRT=$(grep -c '^- clone-ref: ' "$N" || true); CRG=$(grep -cE '^- clone-ref: [a-z0-9-]+ — [0-9a-f]{40} — .' "$N" || true); test "$CRT" = "$CRG" || rc=1; test "$((10#$CRG))" = "1" || rc=1; h=$(sed -nE 's/^- clone-ref: [a-z0-9-]+ — ([0-9a-f]{40}) — .*$/\1/p' "$N" | head -n 1); test -n "$h" || rc=1; test "$h" = "$B" || rc=1; test "$(grep -c '^- probe-writes: ' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- probe-writes: none — .' "$N" || true)" = "1" || rc=1; grep -qF -- 'git status --short' "$N" || rc=1; grep -qF -- 'git ls-files --others --exclude-standard' "$N" || rc=1; grep -qF -- 'rev-parse HEAD' "$N" || rc=1; grep -qF -- '<this-checkout-root>' "$N" || rc=1; test "$(grep -cE '/(Users|home)/[A-Za-z0-9._-]+/' "$N" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC5** The arms are closed, all three ran — the two subject arms and the differential control — and any dropped arm says where its requirements went. Each of `resolved-type-n3-rep1`, `resolved-type-n3-rep2` and `control-gp-n3` occurs on exactly one line opening `- probe-arm: <id> — ` and each reads `executed`; the total `- probe-arm: ` count equals the count matching the full grammar `- probe-arm: <id> — executed|declined — <text>`, so a fourth arm added later must conform rather than ride in as free prose; and the number of `- declined-to: ` lines equals the number of `declined` arms, so a dropped arm always names where its findings travelled — the disposition the pre-commitment fixes in advance rather than deciding when the trigger fires. `control-gp-n3` is required here rather than optional, and that is the whole point of this round's redesign: it is the arm whose `general-purpose` instances make a subject arm's silence mean something, and a note reporting subject arms with no control is reporting a measurement this spec does not accept. The note additionally states, on a `- not-run: <arm-class> — <reason>` line, that no serial-baseline arm and no negative-control-stall arm ran, so their absence is a recorded design decision rather than an omission a reader has to infer — and the retired stall control named there is a different thing from the differential control that did run. Positive control: the three exact-one assertions cannot pass on an empty or renamed set of lines, and the grammar total is asserted to be at least three.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; for a in resolved-type-n3-rep1 resolved-type-n3-rep2 control-gp-n3; do test "$(grep -c "^- probe-arm: $a — " "$N" || true)" = "1" || rc=1; test "$(grep -cE "^- probe-arm: $a — executed — ." "$N" || true)" = "1" || rc=1; done; PT=$(grep -c '^- probe-arm: ' "$N" || true); PG=$(grep -cE '^- probe-arm: [a-z0-9-]+ — (executed|declined) — .' "$N" || true); test "$PT" = "$PG" || rc=1; test "$((10#$PG))" -ge 3 || rc=1; DC=$(grep -cE '^- probe-arm: [a-z0-9-]+ — declined — .' "$N" || true); DT=$(grep -cE '^[[:space:]]*- declined-to: .' "$N" || true); test "$((10#$DC))" = "$((10#$DT))" || rc=1; NT=$(grep -c '^- not-run: ' "$N" || true); NG=$(grep -cE '^- not-run: [a-z0-9-]+ — .' "$N" || true); test "$NT" = "$NG" || rc=1; test "$((10#$NG))" -ge 2 || rc=1; test "$rc" -eq 0

- [ ] **AC6** The four evidence channels are present and closed with their roles, and **every arm** — both subject repetitions and the control — carries its own per-instance timestamps and an overlap margin equal to the one re-derived from them. Each of `agent-self-timestamps`, `batch-vs-sum`, `write-artifact-existence` and `tool-self-report` occurs on exactly one `- channel: <id> — primary|necessary-condition|corroborating — <text>` line and the total equals the grammar-matching total, so the deliberate exclusion of `orchestrator-span-rows` cannot be quietly reversed by adding a free-prose line. Two of those lines are additionally pinned to their roles: `write-artifact-existence` must read `primary` and `tool-self-report` must read `corroborating`, which is the mechanical form of "the artifacts license the verdict and the self-report never does" — demoting the artifact channel or promoting the self-report channel fails here rather than in prose review. The `- agent-timestamp: <arm-id> — (qa|gp)-[1-3] — first=<digits> — last=<digits> — <text>` lines close under their own grammar; and **for each of `resolved-type-n3-rep1`, `resolved-type-n3-rep2` and `control-gp-n3`** at least three lines exist, their instance ids are **three distinct values** so three records of one instance cannot stand in for three instances, every line has `last` strictly greater than `first`, exactly one `- overlap: <arm-id> — ` line exists whose `margin_ns=` value **equals** `min(last) − max(first)` re-derived from that arm's own timestamp lines with its disposition agreeing with the sign (`overlapping` iff positive, `disjoint` iff not, `undetermined` unconstrained), and exactly one `- batch-vs-sum: <arm-id> — batch_ns=<digits> — sum_ns=<digits> — <text>` line exists. Extending every one of those requirements from one arm to all three is this round's repair of the rep2 evidence gap: `repetition-variance` cannot be claimed `met` while rep2 carries no records, and the control arm cannot be claimed to have run while carrying none either. The `- overlap: ` and `- batch-vs-sum: ` line sets each close under their own grammar. The note names `#285` as the car after which agent-level fan-out could emit its own spans, which is the stated ground for excluding `orchestrator-span-rows`. Positive controls: each arm's `first` and `last` extractions are asserted non-empty before any arithmetic, and the derived window bounds are asserted non-empty before comparison.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111f.XXXXXX") || exit 1; for c in agent-self-timestamps batch-vs-sum write-artifact-existence tool-self-report; do test "$(grep -c "^- channel: $c — " "$N" || true)" = "1" || rc=1; done; CT=$(grep -c '^- channel: ' "$N" || true); CG=$(grep -cE '^- channel: (agent-self-timestamps|batch-vs-sum|write-artifact-existence|tool-self-report) — (primary|necessary-condition|corroborating) — .' "$N" || true); test "$CT" = "$CG" || rc=1; test "$(grep -cE '^- channel: write-artifact-existence — primary — .' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- channel: tool-self-report — corroborating — .' "$N" || true)" = "1" || rc=1; AT=$(grep -c '^- agent-timestamp: ' "$N" || true); AG=$(grep -cE '^- agent-timestamp: [a-z0-9-]+ — (qa|gp)-[1-3] — first=[0-9]+ — last=[0-9]+ — .' "$N" || true); test "$AT" = "$AG" || rc=1; for a in resolved-type-n3-rep1 resolved-type-n3-rep2 control-gp-n3; do grep "^- agent-timestamp: $a — " "$N" > "$T/a" || true; test "$(grep -c . "$T/a" || true)" -ge 3 || rc=1; test "$(sed -nE 's/^- agent-timestamp: [a-z0-9-]+ — ((qa|gp)-[1-3]) — .*$/\1/p' "$T/a" | LC_ALL=C sort -u | grep -c . || true)" -ge 3 || rc=1; sed -nE 's/^.*first=([0-9]+).*$/\1/p' "$T/a" > "$T/f"; sed -nE 's/^.*last=([0-9]+).*$/\1/p' "$T/a" > "$T/l"; test -s "$T/f" || rc=1; test -s "$T/l" || rc=1; mf=""; while IFS= read -r v; do test -n "$v" || continue; if [ -z "$mf" ] || [ "$((10#$v))" -gt "$((10#$mf))" ]; then mf="$v"; fi; done < "$T/f"; ml=""; while IFS= read -r v; do test -n "$v" || continue; if [ -z "$ml" ] || [ "$((10#$v))" -lt "$((10#$ml))" ]; then ml="$v"; fi; done < "$T/l"; test -n "$mf" || rc=1; test -n "$ml" || rc=1; while IFS= read -r ln; do test -n "$ln" || continue; x=$(printf '%s' "$ln" | sed -nE 's/^.*first=([0-9]+).*$/\1/p'); y=$(printf '%s' "$ln" | sed -nE 's/^.*last=([0-9]+).*$/\1/p'); if [ -z "$x" ] || [ -z "$y" ]; then rc=1; else test "$((10#$y))" -gt "$((10#$x))" || rc=1; fi; done < "$T/a"; test "$(grep -c "^- overlap: $a — " "$N" || true)" = "1" || rc=1; OL=$(grep -m1 "^- overlap: $a — " "$N" || true); if [ -n "$OL" ] && [ -n "$mf" ] && [ -n "$ml" ]; then d=$(printf '%s' "$OL" | sed -nE 's/^.*margin_ns=(-?[0-9]+).*$/\1/p'); exp=$(( 10#$ml - 10#$mf )); if [ -z "$d" ]; then rc=1; else test "$d" = "$exp" || rc=1; fi; disp=$(printf '%s' "$OL" | sed -nE 's/^- overlap: [a-z0-9-]+ — ([a-z-]+) — .*$/\1/p'); case "$disp" in overlapping) test "$exp" -gt 0 || rc=1 ;; disjoint) test "$exp" -le 0 || rc=1 ;; undetermined) : ;; *) rc=1 ;; esac; else rc=1; fi; test "$(grep -cE "^- batch-vs-sum: $a — batch_ns=[0-9]+ — sum_ns=[0-9]+ — ." "$N" || true)" = "1" || rc=1; done; OT=$(grep -c '^- overlap: ' "$N" || true); OG=$(grep -cE '^- overlap: [a-z0-9-]+ — (overlapping|disjoint|undetermined) — margin_ns=-?[0-9]+ — .' "$N" || true); test "$OT" = "$OG" || rc=1; BT=$(grep -c '^- batch-vs-sum: ' "$N" || true); BG=$(grep -cE '^- batch-vs-sum: [a-z0-9-]+ — batch_ns=[0-9]+ — sum_ns=[0-9]+ — .' "$N" || true); test "$BT" = "$BG" || rc=1; grep -qF -- '#285' "$N" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC7** The discriminator is differential and its verdict is coupled to the **orchestrator-read artifacts**, never to what an instance said about itself. The `- write-artifact: <arm-id> — (qa|gp)-[1-3] — present|absent — <text>` lines — the orchestrator's own reading of the one shared clone after the arms — close under their own grammar, and so, separately, do the `- tool-probe: <arm-id> — (qa|gp)-[1-3] — write_tool=available|refused|absent — <text>` lines that carry the corroborating self-report. Exactly one `- resolved-type: ` line exists and it matches the closed four-value alternation `plugin-role-confirmed|declared-label-only|contradicted|undetermined` with a non-empty ground, and exactly one `- evidence-boundary: <text>` line states what a tool-set differential does **not** reach. Four value-independent couplings, every one of them reading artifact lines only: `plugin-role-confirmed` requires **at least one** `control-gp-n3` artifact `present` — the within-venue proof that this venue admits a write at all — **and zero** subject-arm artifacts `present`, **and** at least three artifact records with three distinct instance ids in each of the three arms plus at least three self-report records per arm; `contradicted` requires **at least one** subject-arm artifact `present`; `declared-label-only` requires that neither family carries a single line **and** that **AC5**'s own `- probe-arm:` record says the arms did not run — at least one such line exists and **zero** of them read `executed` — so a note reporting three arms `executed` and then reporting no discriminator evidence at all can no longer take this branch, which was the one branch reachable with zero evidence; and `undetermined` is unconstrained. While **AC5** requires all three arms `executed`, that coupling makes `declared-label-only` unreachable in any note this spec accepts, and that is the intended reading rather than an accident: the world in which `declared-label-only` is the honest value is the one the pre-commitment's second drop creates, and in that world **AC5**–**AC9** are dropped together with the probe. Those four together **force** the venue-denial case: with artifacts recorded, no control artifact present and no subject artifact present, the first three branches are all unreachable and `undetermined` is the only value left — which is exactly the confound the previous round's uniform-self-report design could not see, closed here by the arithmetic rather than by a promise. **No verdict branch reads a `write_tool=` value**, which is the mechanical form of "the self-report corroborates and never licenses". Outside the branches, one **consistency coupling** compares the two channels pair by pair: for each `(arm, instance)` pair a `- write-artifact:` line records — the nine of them where all three arms ran — exactly one matching `- tool-probe:` line must exist, and `present` requires that self-report read `available` while `absent` requires it read `refused` or `absent`. Any other combination is exactly the artifact/self-report disagreement this protocol calls an **invalid-evidence event**, which until now lived only in prose; this coupling makes it mechanical. It can only fail the criterion and can never license a `- resolved-type:` value — the artifacts still carry the verdict alone, and an instance that silently declines to attempt a `Write` it holds is thereby visible as a disagreement rather than passing as a missing tool. The note quotes `agents/qa-verifier.md`'s own tool set by carrying the literal `tools: Read, Grep, Glob, Bash`, carries the literal `general-purpose` so the control's type is named rather than implied, and carries the literal `probe-write-` so the frozen per-instance filename scheme is on the page a reader can check. Positive controls: both line families are compared raw-total against grammar-total, so a malformed record fails rather than being skipped; the `- resolved-type:` line is asserted to parse before its branch is taken; each per-arm artifact extraction is asserted to hold three distinct ids rather than three copies of one; and in the consistency coupling a `- write-artifact:` line whose matching `- tool-probe:` line is missing, duplicated or unparsable fails rather than being skipped.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111g.XXXXXX") || exit 1; WT=$(grep -c '^- write-artifact: ' "$N" || true); WG=$(grep -cE '^- write-artifact: [a-z0-9-]+ — (qa|gp)-[1-3] — (present|absent) — .' "$N" || true); test "$WT" = "$WG" || rc=1; TT=$(grep -c '^- tool-probe: ' "$N" || true); TG=$(grep -cE '^- tool-probe: [a-z0-9-]+ — (qa|gp)-[1-3] — write_tool=(available|refused|absent) — .' "$N" || true); test "$TT" = "$TG" || rc=1; test "$(grep -c '^- evidence-boundary: ' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- evidence-boundary: .' "$N" || true)" = "1" || rc=1; test "$(grep -c '^- resolved-type: ' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- resolved-type: (plugin-role-confirmed|declared-label-only|contradicted|undetermined) — .' "$N" || true)" = "1" || rc=1; R=$(grep -m1 '^- resolved-type: ' "$N" | sed -nE 's/^- resolved-type: ([a-z-]+) — .*$/\1/p'); test -n "$R" || rc=1; CP=$(grep -cE '^- write-artifact: control-gp-n3 — gp-[1-3] — present — .' "$N" || true); SP=$(grep -cE '^- write-artifact: resolved-type-n3-rep[12] — qa-[1-3] — present — .' "$N" || true); case "$R" in plugin-role-confirmed) test "$((10#$CP))" -ge 1 || rc=1; test "$((10#$SP))" = "0" || rc=1; for a in resolved-type-n3-rep1 resolved-type-n3-rep2 control-gp-n3; do grep "^- write-artifact: $a — " "$N" > "$T/w" || true; test "$(grep -c . "$T/w" || true)" -ge 3 || rc=1; test "$(sed -nE 's/^- write-artifact: [a-z0-9-]+ — ((qa|gp)-[1-3]) — .*$/\1/p' "$T/w" | LC_ALL=C sort -u | grep -c . || true)" -ge 3 || rc=1; test "$(grep -c "^- tool-probe: $a — " "$N" || true)" -ge 3 || rc=1; done ;; contradicted) test "$((10#$SP))" -ge 1 || rc=1 ;; declared-label-only) test "$((10#$WG))" = "0" || rc=1; test "$((10#$TG))" = "0" || rc=1; PA=$(grep -cE '^- probe-arm: [a-z0-9-]+ — (executed|declined) — .' "$N" || true); PE=$(grep -cE '^- probe-arm: [a-z0-9-]+ — executed — .' "$N" || true); test "$((10#$PA))" -ge 1 || rc=1; test "$((10#$PE))" = "0" || rc=1 ;; undetermined) : ;; *) rc=1 ;; esac; grep -E '^- write-artifact: [a-z0-9-]+ — (qa|gp)-[1-3] — (present|absent) — .' "$N" > "$T/wa" || true; while IFS= read -r ln; do test -n "$ln" || continue; wa=$(printf '%s' "$ln" | sed -nE 's/^- write-artifact: ([a-z0-9-]+) — .*$/\1/p'); wi=$(printf '%s' "$ln" | sed -nE 's/^- write-artifact: [a-z0-9-]+ — ((qa|gp)-[1-3]) — .*$/\1/p'); ws=$(printf '%s' "$ln" | sed -nE 's/^- write-artifact: [a-z0-9-]+ — (qa|gp)-[1-3] — (present|absent) — .*$/\2/p'); if [ -z "$wa" ] || [ -z "$wi" ] || [ -z "$ws" ]; then rc=1; continue; fi; test "$(grep -c "^- tool-probe: $wa — $wi — " "$N" || true)" = "1" || rc=1; wv=$(sed -nE "s/^- tool-probe: $wa — $wi — write_tool=(available|refused|absent) — .*\$/\1/p" "$N" | head -n 1); if [ -z "$wv" ]; then rc=1; else case "$ws:$wv" in present:available|absent:refused|absent:absent) : ;; *) rc=1 ;; esac; fi; done < "$T/wa"; grep -qF -- 'tools: Read, Grep, Glob, Bash' "$N" || rc=1; grep -qF -- 'general-purpose' "$N" || rc=1; grep -qF -- 'probe-write-' "$N" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC8** The probe's own timing preconditions are declared, **re-derived from raw recorded integers**, and self-consistent, so a result cannot rest on a unit shorter than the noise it is measured against and neither input to that test can be fitted after the outcome is known. Exactly one `- clock-source: <text> — verified-expanding|unusable — <text>` line exists; exactly one each of `- unit-duration: <digits> — <text>`, `- launch-latency: <digits> — <text>` and `- margin-factor: <digits> — <text>`. **`unit-duration` is re-derived**: exactly one `- unit-timing: <label> — start=<digits> — end=<digits> — <text>` line records the population-fixation shell run's own before-and-after timestamps, `end` is strictly greater than `start`, and the declared `unit-duration` must **equal `end − start`**. **`launch-latency` is re-derived**: the `- batch-timestamp: <arm-id> — batch_start=<digits> — batch_end=<digits> — <text>` lines close under their own grammar with `batch_end` strictly greater than `batch_start`, and **exactly one exists for every arm that AC5's own `- probe-arm:` family records `executed`, with the count of those lines equal to the count of executed arms** — the completeness half, without which the arm carrying the largest true latency could simply be omitted and the maximum taken over whichever arms happened to carry a line; for each such arm the criterion computes `max(first) − batch_start` over that arm's own `- agent-timestamp:` lines, each of which must be **non-negative** — an instance stamping itself before the orchestrator stamped the batch it was launched from is an invalid-evidence event rather than a small negative latency — and the declared `launch-latency` must **equal the maximum** of those per-arm values. **The frozen launch order is verified from those same recorded integers rather than reported**: `resolved-type-n3-rep1`'s `batch_start` must be strictly less than `control-gp-n3`'s, which must be strictly less than `resolved-type-n3-rep2`'s, so the between-subjects position of the control — the whole ground of the "a condition varying with time cannot align with the subject/control split" argument — is arithmetic over data the note already carries rather than a claim **AC16(h)** merely restates. Where an arm carries both a `- batch-timestamp:` and a `- batch-vs-sum:` line, that arm's `batch_ns` must equal `batch_end − batch_start`, so **AC6**'s necessary-condition channel is anchored to the same raw integers rather than floating free. Only then is the frozen condition applied: the margin factor is at least **3**, and where `unit-duration` is not at least `margin-factor × launch-latency`, the recorded concurrency verdict must be `undetermined` or `launch-refused`. The note carries **zero** `- stall-threshold: ` and **zero** `- control-sleep: ` lines, because no watchdog and no **negative-control stall** arm exist in this protocol — the differential control arm that *did* run is a different instrument and carries neither field — and a note carrying them would be describing an arm this task did not run. The three declared fields are durations rather than epoch timestamps, so the product and comparisons stay well inside integer range, while the raw fields they derive from are 19-digit epoch values compared only with bash `10#` arithmetic. Positive controls: every value is asserted to parse as digits before it is compared; the executed-arm extraction is asserted non-empty before the per-arm batch-line count is compared against it, so a note recording no executed arm fails rather than satisfying the completeness test vacuously; each arm's `first` extraction is asserted non-empty before its latency is computed; the derived maximum is asserted non-empty before the declared field is compared against it; all three named arms' `batch_start` values are asserted to extract non-empty before the ordering comparison, so a missing, misnamed or malformed arm line fails rather than comparing empty strings; and the verdict line is asserted to parse.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111h.XXXXXX") || exit 1; test "$(grep -cE '^- clock-source: .+ — (verified-expanding|unusable) — .' "$N" || true)" = "1" || rc=1; test "$(grep -c '^- clock-source: ' "$N" || true)" = "1" || rc=1; for k in unit-duration launch-latency margin-factor; do test "$(grep -c "^- $k: " "$N" || true)" = "1" || rc=1; test "$(grep -cE "^- $k: [0-9]+ — ." "$N" || true)" = "1" || rc=1; done; test "$(grep -c '^- stall-threshold: ' "$N" || true)" = "0" || rc=1; test "$(grep -c '^- control-sleep: ' "$N" || true)" = "0" || rc=1; UT=$(grep -c '^- unit-timing: ' "$N" || true); UG=$(grep -cE '^- unit-timing: [a-z0-9-]+ — start=[0-9]+ — end=[0-9]+ — .' "$N" || true); test "$UT" = "$UG" || rc=1; test "$((10#$UG))" = "1" || rc=1; us=$(sed -nE 's/^- unit-timing: [a-z0-9-]+ — start=([0-9]+) — end=[0-9]+ — .*$/\1/p' "$N" | head -n 1); ue=$(sed -nE 's/^- unit-timing: [a-z0-9-]+ — start=[0-9]+ — end=([0-9]+) — .*$/\1/p' "$N" | head -n 1); ud=$(sed -nE 's/^- unit-duration: ([0-9]+) — .*$/\1/p' "$N" | head -n 1); if [ -z "$us" ] || [ -z "$ue" ] || [ -z "$ud" ]; then rc=1; else test "$((10#$ue))" -gt "$((10#$us))" || rc=1; test "$((10#$ud))" = "$(( 10#$ue - 10#$us ))" || rc=1; fi; BT=$(grep -c '^- batch-timestamp: ' "$N" || true); BG=$(grep -cE '^- batch-timestamp: [a-z0-9-]+ — batch_start=[0-9]+ — batch_end=[0-9]+ — .' "$N" || true); test "$BT" = "$BG" || rc=1; test "$((10#$BG))" -ge 1 || rc=1; sed -nE 's/^- probe-arm: ([a-z0-9-]+) — executed — .*$/\1/p' "$N" | LC_ALL=C sort -u > "$T/ea"; test -s "$T/ea" || rc=1; test "$(grep -c . "$T/ea" || true)" = "$((10#$BG))" || rc=1; while IFS= read -r a; do test -n "$a" || continue; test "$(grep -cE "^- batch-timestamp: $a — batch_start=[0-9]+ — batch_end=[0-9]+ — ." "$N" || true)" = "1" || rc=1; done < "$T/ea"; grep -E '^- batch-timestamp: ' "$N" > "$T/bt" || true; best=""; while IFS= read -r ln; do test -n "$ln" || continue; a=$(printf '%s' "$ln" | sed -nE 's/^- batch-timestamp: ([a-z0-9-]+) — .*$/\1/p'); bs=$(printf '%s' "$ln" | sed -nE 's/^.*batch_start=([0-9]+).*$/\1/p'); be=$(printf '%s' "$ln" | sed -nE 's/^.*batch_end=([0-9]+).*$/\1/p'); if [ -z "$a" ] || [ -z "$bs" ] || [ -z "$be" ]; then rc=1; continue; fi; test "$((10#$be))" -gt "$((10#$bs))" || rc=1; BV=$(grep -m1 "^- batch-vs-sum: $a — " "$N" || true); if [ -n "$BV" ]; then bn=$(printf '%s' "$BV" | sed -nE 's/^.*batch_ns=([0-9]+).*$/\1/p'); if [ -z "$bn" ]; then rc=1; else test "$((10#$bn))" = "$(( 10#$be - 10#$bs ))" || rc=1; fi; fi; grep "^- agent-timestamp: $a — " "$N" > "$T/ts" || true; mx=""; while IFS= read -r l2; do v=$(printf '%s' "$l2" | sed -nE 's/^.*first=([0-9]+).*$/\1/p'); test -n "$v" || continue; if [ -z "$mx" ] || [ "$((10#$v))" -gt "$((10#$mx))" ]; then mx="$v"; fi; done < "$T/ts"; if [ -z "$mx" ]; then rc=1; else lat=$(( 10#$mx - 10#$bs )); test "$lat" -ge 0 || rc=1; if [ -z "$best" ] || [ "$lat" -gt "$best" ]; then best="$lat"; fi; fi; done < "$T/bt"; ll=$(sed -nE 's/^- launch-latency: ([0-9]+) — .*$/\1/p' "$N" | head -n 1); if [ -z "$best" ] || [ -z "$ll" ]; then rc=1; else test "$((10#$ll))" = "$best" || rc=1; fi; mfac=$(sed -nE 's/^- margin-factor: ([0-9]+) — .*$/\1/p' "$N" | head -n 1); V=$(grep -m1 '^- verdict: ' "$N" | sed -nE 's/^- verdict: ([a-z-]+) — .*$/\1/p'); test -n "$V" || rc=1; if [ -z "$ud" ] || [ -z "$ll" ] || [ -z "$mfac" ]; then rc=1; else test "$((10#$mfac))" -ge 3 || rc=1; if [ "$((10#$ud))" -lt "$(( 10#$mfac * 10#$ll ))" ]; then case "$V" in undetermined|launch-refused) : ;; *) rc=1 ;; esac; fi; fi; o1=$(sed -nE 's/^- batch-timestamp: resolved-type-n3-rep1 — batch_start=([0-9]+) — .*$/\1/p' "$N" | head -n 1); o2=$(sed -nE 's/^- batch-timestamp: control-gp-n3 — batch_start=([0-9]+) — .*$/\1/p' "$N" | head -n 1); o3=$(sed -nE 's/^- batch-timestamp: resolved-type-n3-rep2 — batch_start=([0-9]+) — .*$/\1/p' "$N" | head -n 1); if [ -z "$o1" ] || [ -z "$o2" ] || [ -z "$o3" ]; then rc=1; else test "$((10#$o1))" -lt "$((10#$o2))" || rc=1; test "$((10#$o2))" -lt "$((10#$o3))" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC9** The concurrency verdict is one of five, the word it carries is licensed mechanically rather than rhetorically, and the one inherited misnomer among the licence-condition ids is disclosed on the page rather than silently carried. Exactly one `- verdict: ` line exists and it matches the closed five-value alternation `concurrent-overlapping|serialized|queued-partial|launch-refused|undetermined` with a non-empty reason. Each of the **eight** licence conditions — `production-unit`, `real-population`, `same-machine-session`, `clock-source-monotonic`, `overlap-margin-exceeds-launch-latency`, `repetition-variance`, `tool-set-discriminator-exercised`, `differential-control-arm-executed` — occurs on exactly one `- licence-condition: <id> — met|not-met — <evidence>` line, and the total equals the grammar-matching total so the set is closed in both directions without a literal count being written here. Exactly one `- vocabulary-caveat: clock-source-monotonic — <text>` line exists: the id is inherited verbatim from T-1073 and carried by T-1083 and T-1085 over the same wall-clock source, this task deliberately does **not** rename it (a single-note rename would fork the shipped vocabulary across four notes and leave three of them mislabelled), and the disclosure line is what keeps the mislabel from being load-bearing. The vocabulary repair itself travels to a fast-follow issue filed at close-out, which is a coordinating-session action rather than anything this criterion can observe. Two value-independent couplings: if the verdict is `concurrent-overlapping`, `serialized` or `queued-partial`, **no** condition may read `not-met`; and if the verdict is `queued-partial`, exactly one `- measured-k: <digits> — <text>` line exists, while for every other verdict there are none. `launch-refused` and `undetermined` are deliberately exempt from the all-conditions-met requirement: a refusal is evidenced by the refusal itself, and `undetermined` is the outcome a `not-met` condition is supposed to force. Positive controls: the eight exact-one assertions cannot pass on an empty file, and both couplings are evaluated from counts taken on that same file.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; test "$(grep -c '^- verdict: ' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- verdict: (concurrent-overlapping|serialized|queued-partial|launch-refused|undetermined) — .' "$N" || true)" = "1" || rc=1; test "$(grep -c '^- vocabulary-caveat: ' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- vocabulary-caveat: clock-source-monotonic — .' "$N" || true)" = "1" || rc=1; for m in production-unit real-population same-machine-session clock-source-monotonic overlap-margin-exceeds-launch-latency repetition-variance tool-set-discriminator-exercised differential-control-arm-executed; do test "$(grep -c "^- licence-condition: $m — " "$N" || true)" = "1" || rc=1; done; LT=$(grep -c '^- licence-condition: ' "$N" || true); LG=$(grep -cE '^- licence-condition: (production-unit|real-population|same-machine-session|clock-source-monotonic|overlap-margin-exceeds-launch-latency|repetition-variance|tool-set-discriminator-exercised|differential-control-arm-executed) — (met|not-met) — .' "$N" || true); test "$LT" = "$LG" || rc=1; NM=$(grep -cE '^- licence-condition: [a-z-]+ — not-met — .' "$N" || true); V=$(grep -m1 '^- verdict: ' "$N" | sed -nE 's/^- verdict: ([a-z-]+) — .*$/\1/p'); test -n "$V" || rc=1; case "$V" in concurrent-overlapping|serialized|queued-partial) test "$((10#$NM))" = "0" || rc=1 ;; esac; MK=$(grep -cE '^- measured-k: [0-9]+ — .' "$N" || true); MKT=$(grep -c '^- measured-k: ' "$N" || true); test "$MK" = "$MKT" || rc=1; if [ "$V" = "queued-partial" ]; then test "$((10#$MK))" = "1" || rc=1; else test "$((10#$MKT))" = "0" || rc=1; fi; test "$rc" -eq 0

- [ ] **AC10** The prior measurements are reconciled by citation rather than re-run, and the supersession names all three sources. The `- prior-measurement: <note-path> — <arm-id> — n=<digits> — margin_ns=<digits> — <text>` lines close under their own grammar and at least three exist; among them, at least one names `docs/loop-engineering/agent-launch-fanout.md` and at least one names `docs/loop-engineering/default-path-firing.md`, so the two notes that already answered this task's briefed question are cited rather than paraphrased. The note carries at least three `- supersedes: <text>` lines and at least one names the literal `harness-agent-concurrency.md`; it carries at least one `- follow-up: <text>` line; and it states, on exactly one `- not-re-measured: <text>` line, that this task ran no arm at N=2, N=4 or N=8 and needed none — the claim that keeps a reader from mistaking the reconciliation for fresh measurement. Positive controls: each line family is asserted present with a non-empty payload before its content is judged, and the grammar-matching total is compared against the raw total so a malformed line cannot ride in.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; PT=$(grep -c '^- prior-measurement: ' "$N" || true); PG=$(grep -cE '^- prior-measurement: docs/loop-engineering/[a-z-]+\.md — [a-z0-9-]+ — n=[0-9]+ — margin_ns=[0-9]+ — .' "$N" || true); test "$PT" = "$PG" || rc=1; test "$((10#$PG))" -ge 3 || rc=1; test "$(grep -c '^- prior-measurement: docs/loop-engineering/agent-launch-fanout\.md — ' "$N" || true)" -ge 1 || rc=1; test "$(grep -c '^- prior-measurement: docs/loop-engineering/default-path-firing\.md — ' "$N" || true)" -ge 1 || rc=1; test "$(grep -cE '^- supersedes: .' "$N" || true)" -ge 3 || rc=1; test "$(grep -c '^- supersedes: .*harness-agent-concurrency\.md' "$N" || true)" -ge 1 || rc=1; test "$(grep -cE '^- follow-up: .' "$N" || true)" -ge 1 || rc=1; test "$(grep -c '^- not-re-measured: ' "$N" || true)" = "1" || rc=1; test "$(grep -cE '^- not-re-measured: .' "$N" || true)" = "1" || rc=1; test "$rc" -eq 0

- [ ] **AC11** Every enumerated stale surface is dispositioned on a record line, the repairs are line-count preserving, and no surviving line misquotes the line this task moves. The `- surface: <path>:<anchor> — repaired|annotated|left — <text>` lines close under their own grammar and at least six exist; among them, at least one names each of `harness-agent-concurrency.md`, `agent-concurrency.md` and `phase-multiplexing.md`, so no edited file is silently absent from the disposition record. Each of `docs/loop-engineering/harness-agent-concurrency.md`, `docs/loop-engineering/agent-concurrency.md` and `docs/loop-engineering/phase-multiplexing.md` has **exactly the same total line count** at `HEAD` as in the branch point's committed blob — the mechanical form of "every repair was an in-place replacement", which is what keeps every line-number citation elsewhere in the corpus as accurate as it was. Each of those three files also **differs** from its branch-point blob, so a criterion that would pass by editing nothing fails instead. And no line anywhere in those three files contains the literal string `not yet independently confirmed`, the stale conclusion the repair removes — a scoped absence assertion whose positive control is that all three files are asserted non-empty and readable first. **The removal carries its own context guard**: `docs/loop-engineering/agent-concurrency.md` must carry at least one occurrence of the literal `plugin-role-agent-concurrency.md` at `HEAD`, the one-line cross-reference to the new note that keeps a later reader meeting the repaired `:120` in isolation from concluding that the *type-resolution* residual closed along with it. The sentence being removed is scoped to the **overlap** question, which T-1083 and T-1085 genuinely settled; the narrower residual stays alive on `harness-agent-concurrency.md`'s own `- unobserved:` line, which **AC12** protects rather than deletes, and the cross-reference is what makes those two facts reachable from the same page. Positive controls: each base blob is asserted non-empty before its line count is taken; the difference assertion is what makes the equality assertion non-vacuous; and the cross-reference assertion is taken on a file already asserted readable and non-empty.
  - check: rc=0; export LC_ALL=C; N=docs/loop-engineering/plugin-role-agent-concurrency.md; test -s "$N" || exit 1; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111k.XXXXXX") || exit 1; ST=$(grep -c '^- surface: ' "$N" || true); SG=$(grep -cE '^- surface: docs/loop-engineering/[a-z-]+\.md:[^ ]+ — (repaired|annotated|left) — .' "$N" || true); test "$ST" = "$SG" || rc=1; test "$((10#$SG))" -ge 6 || rc=1; for f in harness-agent-concurrency agent-concurrency phase-multiplexing; do test "$(grep -c "^- surface: docs/loop-engineering/$f\.md:" "$N" || true)" -ge 1 || rc=1; done; i=0; for p in docs/loop-engineering/harness-agent-concurrency.md docs/loop-engineering/agent-concurrency.md docs/loop-engineering/phase-multiplexing.md; do i=$((i+1)); test -r "$p" || rc=1; test -s "$p" || rc=1; git show "$B:$p" > "$T/b$i" 2>/dev/null || rc=1; if [ -s "$T/b$i" ]; then a=$(wc -l < "$T/b$i" | tr -d ' '); c=$(wc -l < "$p" | tr -d ' '); test "$((10#$a))" = "$((10#$c))" || rc=1; cmp -s "$T/b$i" "$p" && rc=1; else rc=1; fi; test "$(grep -c 'not yet independently confirmed' "$p" || true)" = "0" || rc=1; done; test "$i" = "3" || rc=1; test "$(grep -c 'plugin-role-agent-concurrency\.md' docs/loop-engineering/agent-concurrency.md || true)" -ge 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC12** The protected invariants of the three edited notes survive the repair — the negative criterion that makes the edits safe rather than merely small. `docs/loop-engineering/harness-agent-concurrency.md`'s `## Probe evidence (raw, orchestrator-produced)` section is **byte-identical** to the same section extracted from its branch-point blob (this task edits prose elsewhere in that file and no byte of the evidence), it still carries exactly one `# ` title line, it still carries at least one `- unobserved: ` line of which at least one carries the literal `plugin-role`, and it still carries exactly one `- claim-under-test: <text> — under-test-not-evidence` line and exactly one `- agent-type: ` line — the four grammars T-1073's own **AC1** and **AC11** depend on, so a repair that narrowed the residual honestly cannot break the merged criteria guarding it. `docs/loop-engineering/agent-concurrency.md` still carries exactly one `- substrate: agent-tool-concurrent-launch — provided — ` line, so T-1080's erratum b survives. `docs/loop-engineering/phase-multiplexing.md` carries the **same number** of `- dispatch-axis: ` lines and the same number of `- dispatch-rule: ` lines as its branch-point blob, so the dispatcher grammar T-1100 shipped is untouched by this task's prose repair. Positive controls: both extracted evidence sections are asserted non-empty before `cmp`; every base-side count is taken from a blob asserted non-empty.
  - check: rc=0; export LC_ALL=C; H=docs/loop-engineering/harness-agent-concurrency.md; A=docs/loop-engineering/agent-concurrency.md; P=docs/loop-engineering/phase-multiplexing.md; for p in "$H" "$A" "$P"; do test -s "$p" || exit 1; done; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111l.XXXXXX") || exit 1; git show "$B:$H" > "$T/hb" 2>/dev/null || rc=1; test -s "$T/hb" || rc=1; awk '/^## Probe evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$T/hb" > "$T/be"; awk '/^## Probe evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' "$H" > "$T/he"; test -s "$T/be" || rc=1; test -s "$T/he" || rc=1; cmp -s "$T/be" "$T/he" || rc=1; test "$(grep -c '^# ' "$H" || true)" = "1" || rc=1; test "$(grep -c '^- unobserved: ' "$H" || true)" -ge 1 || rc=1; test "$(grep -c '^- unobserved: .*plugin-role' "$H" || true)" -ge 1 || rc=1; test "$(grep -cE '^- claim-under-test: .+ — under-test-not-evidence$' "$H" || true)" = "1" || rc=1; test "$(grep -c '^- agent-type: ' "$H" || true)" = "1" || rc=1; test "$(grep -c '^- substrate: agent-tool-concurrent-launch — provided — ' "$A" || true)" = "1" || rc=1; git show "$B:$P" > "$T/pb" 2>/dev/null || rc=1; test -s "$T/pb" || rc=1; for g in 'dispatch-axis' 'dispatch-rule'; do x=$(grep -c "^- $g: " "$T/pb" || true); y=$(grep -c "^- $g: " "$P" || true); test "$((10#$x))" = "$((10#$y))" || rc=1; test "$((10#$x))" -ge 1 || rc=1; done; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC13** Records, board, recipe and hygiene, including the one PII shape a probe note of this class is structurally prone to. `.shell-team/provenance/T-1111.md` and `.shell-team/interventions/T-1111.md` both exist and are conformant under their own checkers; the board resolved through `bin/team-paths.sh --get todo` passes `bin/check-handoff.sh` and passes `bin/check-board-headings.sh` against the branch point; the branch diff is clean under `bin/check-pii-shapes.sh --base <branch-point>`; every top-level `- [` entry line present in the board's branch-point blob is still present **verbatim** in the working board; and `.shell-team/test-recipe.md` gains at least one line naming `T-1111` below its `## Appended by tasks` heading, with every non-blank line its branch-point blob carried still present verbatim (blank lines excluded from both survival comparisons deliberately, since a blank pattern line matches every input line and would make the assertion vacuous). Positive control against a vacuous absence: the branch-point board blob's entry-line extraction is asserted non-empty before it is walked, and the recipe's own base-side non-blank extraction likewise.
  - check: rc=0; export LC_ALL=C; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; BD=$(bash bin/team-paths.sh --get todo) || exit 1; test -r "$BD" || exit 1; RP=.shell-team/test-recipe.md; test -r "$RP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111m.XXXXXX") || exit 1; if [ -r .shell-team/provenance/T-1111.md ]; then bash bin/check-provenance.sh .shell-team/provenance/T-1111.md >/dev/null 2>&1 || rc=1; else rc=1; fi; if [ -r .shell-team/interventions/T-1111.md ]; then bash bin/check-interventions.sh --task T-1111 -- .shell-team/interventions/T-1111.md >/dev/null 2>&1 || rc=1; else rc=1; fi; bash bin/check-handoff.sh "$BD" >/dev/null 2>&1 || rc=1; bash bin/check-board-headings.sh "$BD" --base "$B" >/dev/null 2>&1 || rc=1; bash bin/check-pii-shapes.sh --base "$B" >/dev/null 2>&1 || rc=1; git show "$B:$BD" > "$T/bb" 2>/dev/null || { rm -rf "$T"; exit 1; }; test -s "$T/bb" || rc=1; grep '^- \[' "$T/bb" > "$T/bl" || true; test -s "$T/bl" || rc=1; while IFS= read -r l; do test -n "$l" || continue; grep -qxF -- "$l" "$BD" || rc=1; done < "$T/bl"; git show "$B:$RP" > "$T/rb" 2>/dev/null || { rm -rf "$T"; exit 1; }; grep -v '^[[:space:]]*$' "$T/rb" > "$T/rn" || true; grep -v '^[[:space:]]*$' "$RP" > "$T/cn" || true; test -s "$T/rn" || rc=1; test -s "$T/cn" || rc=1; test "$(grep -vxFf "$T/cn" "$T/rn" | grep -c . || true)" = "0" || rc=1; awk '/^## Appended by tasks$/{f=1;next} f' "$RP" > "$T/tail"; test -s "$T/tail" || rc=1; test "$(grep -c 'T-1111' "$T/tail" || true)" -ge 1 || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC14** Scope lock. The branch's changed-and-added file set — the tracked half from the commit range `<branch-point>...HEAD` unioned with the untracked half from `git ls-files --others --exclude-standard`, since this spec and this task's records are untracked at freeze time — contains this spec, the board resolved through `bin/team-paths.sh --get todo`, `docs/loop-engineering/plugin-role-agent-concurrency.md`, the three edited notes and `.shell-team/test-recipe.md`, and **nothing** outside the allow-list, which is those paths plus this task's three records (`.shell-team/provenance/T-1111.md`, `.shell-team/interventions/T-1111.md`, `.shell-team/reviews/T-1111.md`). It contains zero paths under `bin/`, `tests/`, `skills/`, `agents/`, `templates/` and `.github/`, no `README` file anywhere, no `docs/` path other than the four named notes, no spec under `.shell-team/specs/` other than this one, zero paths under the resolved runs dir, and **not** the path `bin/team-paths.sh --get lessons` resolves. The range is taken from the **branch point** rather than from `git merge-base develop HEAD`, precisely so the stacked predecessors' delivered paths are not attributed to this task; the criterion measures that premise rather than asserting it, by requiring the branch point to differ from `develop`'s tip. **This criterion is merge-point-scoped and expected to go stale after merge** — once a later task's files land on the same ref, or once the parent branch is deleted at the batch merge, its expected set no longer describes reality; do not widen its ref resolution and do not re-derive it per rework round. Positive controls: the branch point resolves and is asserted not equal to `develop`'s tip; the changed-and-added set is asserted non-empty; the required-path list is asserted non-empty before it is walked.
  - check: rc=0; export LC_ALL=C; BPREF=feature/1110-freeze-version-derivation; if git show-ref --verify --quiet "refs/heads/$BPREF"; then B=$(git merge-base "$BPREF" HEAD) || exit 1; elif git show-ref --verify --quiet "refs/remotes/origin/$BPREF"; then B=$(git merge-base "refs/remotes/origin/$BPREF" HEAD) || exit 1; else B=$(git merge-base "develop" HEAD) || exit 1; fi; test -n "$B" || exit 1; D=$(git rev-parse 'develop^{commit}') || exit 1; test "$B" != "$D" || rc=1; SELF=.shell-team/specs/T-1111-plugin-role-agent-concurrency.md; BD=$(bash bin/team-paths.sh --get todo) || exit 1; LS=$(bash bin/team-paths.sh --get lessons) || exit 1; RD=$(bash bin/team-paths.sh --get runs) || exit 1; test -n "$LS" || exit 1; test -n "$RD" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111n.XXXXXX") || exit 1; printf '%s\n' "$SELF" "$BD" docs/loop-engineering/plugin-role-agent-concurrency.md docs/loop-engineering/harness-agent-concurrency.md docs/loop-engineering/agent-concurrency.md docs/loop-engineering/phase-multiplexing.md .shell-team/test-recipe.md > "$T/own"; test -s "$T/own" || rc=1; printf '%s\n' .shell-team/provenance/T-1111.md .shell-team/interventions/T-1111.md .shell-team/reviews/T-1111.md > "$T/opt"; { git diff --name-only "$B"...HEAD; git ls-files --others --exclude-standard; } | LC_ALL=C sort -u > "$T/set"; test -s "$T/set" || rc=1; while IFS= read -r f; do test -n "$f" || continue; grep -qxF -- "$f" "$T/set" || rc=1; done < "$T/own"; cat "$T/own" "$T/opt" | LC_ALL=C sort -u > "$T/allow"; test -s "$T/allow" || rc=1; test "$(grep -vxFf "$T/allow" "$T/set" | grep -c . || true)" = "0" || rc=1; for d in '^bin/' '^tests/' '^skills/' '^agents/' '^templates/' '^\.github/'; do test "$(grep -c "$d" "$T/set" || true)" = "0" || rc=1; done; test "$(grep -c 'README' "$T/set" || true)" = "0" || rc=1; test "$(grep '^docs/' "$T/set" | grep -vxF -- 'docs/loop-engineering/plugin-role-agent-concurrency.md' | grep -vxF -- 'docs/loop-engineering/harness-agent-concurrency.md' | grep -vxF -- 'docs/loop-engineering/agent-concurrency.md' | grep -vxF -- 'docs/loop-engineering/phase-multiplexing.md' | grep -c . || true)" = "0" || rc=1; test "$(grep '^\.shell-team/specs/' "$T/set" | grep -vxF -- "$SELF" | grep -c . || true)" = "0" || rc=1; test "$(grep -c "^$RD/" "$T/set" || true)" = "0" || rc=1; test "$(grep -cxF -- "$LS" "$T/set" || true)" = "0" || rc=1; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC15** The downstream-impact disclosure is produced by the **read-set-scoped** method this task's `no-mechanism` class prices, its population is derived by a tool rather than counted by eye, and it names what that method cannot see. This spec carries a `## Blast radius` section outside the intent block; that heading occurs exactly once; the section is non-empty; it states the literal `read-set-scoped` as its method; it embeds at least one `bin/derive-populations.sh` derivation block — a `<!-- BEGIN derivation: ` / `<!-- END derivation: ` marker pair carrying a `- derived-by: bin/derive-populations.sh` line, a `- locale: LC_ALL=C` line and a `- union: items: <digits>` line — immediately preceded by its own `- reproduce: ` line, so no population total in this spec is hand-counted; it names its base-side reading target as a committed-blob read (`git show`, `git ls-tree` or `git worktree add --detach`) rather than the working tree; it carries both a `base:` and a `head:` verdict token and names at least one merged spec by full path; it discharges the **indirection** class explicitly by carrying that literal together with `bin/team-paths.sh --get todo` and the disposition taken; and it declares on a line of the shape `- predicted-red: <n>` how many merged criteria this diff newly reddens, with at least that many rows carrying the literal `head: FAIL`. Positive controls: the section extraction is asserted non-empty before it is judged; the derivation block's own extraction is asserted non-empty before its grammar lines are counted; and the `- predicted-red:` line is asserted to parse before the row count is compared against it.
  - check: rc=0; export LC_ALL=C; SP=.shell-team/specs/T-1111-plugin-role-agent-concurrency.md; test -s "$SP" || exit 1; T=$(mktemp -d "${TMPDIR:-/tmp}/t1111o.XXXXXX") || exit 1; test "$(grep -cxF -- '## Blast radius' "$SP" || true)" = "1" || rc=1; awk '/^## Blast radius$/{f=1;next} f&&/^## /{exit} f' "$SP" > "$T/sec"; test -s "$T/sec" || rc=1; for s in 'read-set-scoped' 'base:' 'head:' 'indirection' 'bin/team-paths.sh --get todo'; do grep -qF -- "$s" "$T/sec" || rc=1; done; grep -qE 'git (show|ls-tree|worktree add --detach)' "$T/sec" || rc=1; grep -qE '\.shell-team/specs/T-10[0-9][0-9]' "$T/sec" || rc=1; test "$(grep -c '^<!-- BEGIN derivation: ' "$T/sec" || true)" -ge 1 || rc=1; test "$(grep -c '^<!-- END derivation: ' "$T/sec" || true)" -ge 1 || rc=1; test "$(grep -c '^- reproduce: ' "$T/sec" || true)" -ge 1 || rc=1; awk '/^<!-- BEGIN derivation: /{f=1} f{print} f&&/^<!-- END derivation: /{exit}' "$T/sec" > "$T/der"; test -s "$T/der" || rc=1; grep -qxF -- '- derived-by: bin/derive-populations.sh' "$T/der" || rc=1; grep -qxF -- '- locale: LC_ALL=C' "$T/der" || rc=1; grep -qE '^- union: items: [0-9]+$' "$T/der" || rc=1; q=$(sed -nE 's/^- predicted-red: ([0-9]+).*/\1/p' "$T/sec" | head -n 1); test -n "$q" || rc=1; r=$(grep -cF -- 'head: FAIL' "$T/sec" || true); if [ -n "$q" ]; then test "$((10#$r))" -ge "$((10#$q))" || rc=1; fi; rm -rf "$T"; test "$rc" -eq 0

- [ ] **AC16** Runtime, `SKIP` by design — no command can prove a command was run. **This criterion is reported item by item, in this order, one line per item, each naming what was measured and what the result was; a compressed summary does not discharge it, and a whole-set claim inside any item carries the enumeration it summarises.** **(a)** A producer-run **mutation self-check** on scratch copies outside the working tree, each mutation observed red, restored, observed green again, each reported separately — scoped, per the `no-mechanism` pricing, to the `- check:` lines this task adds: rename one required heading in the new note (**AC1** red); alter one byte inside the new note's evidence section (**AC2** red); alter one byte inside the spec's `probe-protocol` region (**AC3** red); insert `git worktree add` into one of the note's `- command: ` lines (**AC4** red); corrupt one `- clone-ref:` hex so it no longer equals the resolved branch point (**AC4** red), and separately add a second `- clone-ref:` line (**AC4** red — this round's one-shared-clone lock, which a per-arm venue would trip immediately); flip one arm to `declined` (**AC5** red); delete the `control-gp-n3` arm line outright (**AC5** red — the control arm's own presence lock); change the declared `margin_ns` on any arm so it no longer equals the re-derived value (**AC6** red); delete `resolved-type-n3-rep2`'s `- agent-timestamp:` lines (**AC6** red — this round's rep2 gap repair); duplicate one instance id so an arm carries three records naming only two instances (**AC6** red); demote `- channel: write-artifact-existence` from `primary` (**AC6** red); flip all three `control-gp-n3` artifacts to `absent` while `- resolved-type:` reads `plugin-role-confirmed` (**AC7** red — the venue-denial confound this round closes); flip one subject-arm artifact to `present` while `- resolved-type:` reads `plugin-role-confirmed` (**AC7** red); delete the `- evidence-boundary:` line (**AC7** red); flip one instance's `- tool-probe:` self-report to `available` while its own `- write-artifact:` line still reads `absent` (**AC7** red — this round's artifact/self-report consistency coupling, which makes the invalid-evidence rule mechanical); set `- resolved-type:` to `declared-label-only` while the three arms stand recorded `executed` and both discriminator families are emptied (**AC7** red — this round's closure of the one branch that was reachable with zero evidence); set `- margin-factor:` to `2` (**AC8** red); alter `- unit-duration:` so it no longer equals `end − start` (**AC8** red); alter one `- batch-timestamp:`'s `batch_start` so `- launch-latency:` no longer equals the derived per-arm maximum (**AC8** red), and separately delete one executed arm's `- batch-timestamp:` line outright (**AC8** red — the previous round's one-line-per-executed-arm completeness repair, which the `>= 1` form before it let pass), and separately swap the `batch_start` values of the `control-gp-n3` and `resolved-type-n3-rep2` lines so the recorded order no longer reads rep1 → control → rep2 (**AC8** red — this round's launch-order arithmetic); flip one `- licence-condition:` to `not-met` while the verdict stands (**AC9** red, unless the verdict is already `undetermined`/`launch-refused`, in which case state that and probe the other coupling instead); delete the `- vocabulary-caveat:` line (**AC9** red); delete one `- prior-measurement:` line (**AC10** red); add one line to `docs/loop-engineering/phase-multiplexing.md` so its line count no longer matches its base blob (**AC11** red); delete the `plugin-role-agent-concurrency.md` cross-reference from `agent-concurrency.md` (**AC11** red); alter one byte inside `harness-agent-concurrency.md`'s `## Probe evidence` section (**AC12** red); remove the `T-1111` line from the recipe tail (**AC13** red); add an untracked stray file (**AC14** red); delete the `- predicted-red:` line (**AC15** red). **(b)** The **execution-context matrix** for every command this task's note and records tell a reader to run: this repository's checkout root without the plugin on `PATH`, and an adopter-shaped repository with the plugin loaded — each cell run, not read, with its result stated; where a command is genuinely repository-local and has no adopter-shaped cell, say so per command rather than as one blanket sentence. **(c)** CI-equivalence **scoped to the steps whose inputs this diff can reach**, derived mechanically by reverse-mapping each edited path to the suites that read it (`grep -rl -- '<edited-path>' tests/*/run.sh` plus the workflow's own dogfood steps naming it): every step run named with its result, and every step judged inapplicable named **individually** with its reason. **(d)** The `## Blast radius` table's production narrated: the derivation command's own output, the scratch trees the base-side verdicts were read from **at that ref's committed blobs**, per differing spec one line stating which criterion changed verdict and why, the executor form used for the sweep (coordinating-session-hosted harness-tracked slices, per `.shell-team/test-recipe.md`'s T-1110 entry), and the indirection class's disposition stated explicitly. Where a measured verdict differs from this spec's prediction, the table is corrected to the measurement and the correction is stated; the prediction is never defended. **(e)** The measured 40-hex values of the resolved branch point, `git rev-parse feature/1110-freeze-version-derivation`, `git rev-parse develop` and the probe-evidence commit, with the branch-point-versus-`develop` inequality this stacked premise rests on, together with `git branch --show-current` and which arm of the base-ref discriminator the existence test selected. **(f)** Every relayed premise in `## Assumptions` re-measured with the measured value reported beside it, and any premise that could not be measured from the executing side named as still relayed rather than silently dropped. **(g)** A per-source report for every `## Summarized sources` entry, stating whether the reporting role opened it and what it confirmed or contradicted about the cited distinction. **(h)** The probe's own execution conditions restated: the clock source actually used and the evidence its nanosecond field expanded, the machine's logical core count, the explicit per-call Bash timeout value passed to every instance, the arm-and-instance mapping of all nine launches with the **one** shared clone root they all wrote into and that clone's resolved `HEAD`, the order in which the three arms' messages were actually issued against the frozen order (`resolved-type-n3-rep1`, `control-gp-n3`, `resolved-type-n3-rep2`), the control arm's own model binding as observed, and the fact that every timing figure in the note is machine-local and carries no git ref.

## Input space

**Reachable input classes** — what this probe, this repair and this note really meet:

- **Harness outcomes genuinely reachable on a real plugin-role launch**: three instances whose execution windows overlap; three executed one after another; a partial queue admitting `k < 3` at a time; a refusal to launch at all (a tool error, a permission refusal, an unavailable agent type — a shape `agent-launch-fanout.md:189` has already produced once, deliberately); and a run whose evidence licenses none of those four, which is what `undetermined` names.
- **Discriminator outcomes genuinely reachable**: an instance with no `Write` tool at all (`absent`), an instance holding one whose use the harness refuses (`refused`), and an instance holding a working one (`available`) — the last being a genuine falsification of the declared label rather than a defect in the probe.
- **A venue that denies every write regardless of the declared type**, including a denial that comes and goes while the arms run rather than holding for the whole session. A sandbox-wide or `$TMPDIR`-wide write denial, or a permission failure inside the one clone, produces `absent` artifacts in the control arm *and* the subject arms alike; a denial that varies monotonically over the three messages cannot align with the subject/control split, because the frozen launch order puts the control between the two subject arms, and a transient denial coinciding with exactly the two subject messages is the residual the `- evidence-boundary:` line states. This is the confound the control arm exists to expose, it is genuinely reachable rather than hypothetical, and the couplings force it to `undetermined` with the venue-denial reason recorded — never to a positive.
- **A `general-purpose` control instance that turns out not to hold an effective `Write` here.** The control's own capability is measured rather than assumed; if it is absent the differential simply has no contrast to offer and the run is `undetermined`, which is an honest and complete deliverable.
- **A self-report that disagrees with the artifact** — an instance reporting `available` where no file exists, or `absent` where one does. The artifact governs, the disagreement is recorded, and a disagreement is an invalid-evidence event under the frozen rule rather than something to reconcile in prose.
- **A plugin role's own bound model.** `agents/qa-verifier.md` binds `model: sonnet`; whether that binding can be overridden at launch is unknown here and is recorded either way, so a confound is disclosed rather than assumed away.
- **The unit's real cost distribution**: `bash bin/check-acs.sh` over real committed specs, whose per-spec cost varies by roughly an order of magnitude across this corpus — the reason the unit's duration is re-measured at this branch point before any arm rather than inherited from T-1073's figure.
- **A host with no `timeout` binary**, measured on this machine (`.shell-team/interventions/T-1109.md` entry 1), so `CHECK_ACS_TIMEOUT` is inert and only an explicit harness-level per-call timeout bounds anything.
- **Clock sources actually available on the probe host**, including one whose nanosecond field does not expand (BSD `date` emits a literal `N`).
- **Three merged notes carrying live line-number citations into each other**, one of which (`default-path-firing.md:709`) is already drifted — the reachable input that makes line-count preservation a requirement rather than a nicety.
- **A merged corpus whose criteria read the edited notes both literally and through `bin/team-paths.sh --get`**, which is why the read set is derived by a tool and the indirection class is named rather than assumed empty.
- **A board that grows while the task runs**, appended to by this very loop — which is why every board-reading criterion is anchored on a task-scoped line or a base-relative survival comparison rather than on a count.
- **A note read months later by someone who ran none of this** — the primary reader of every evidence line, which is why the evidence carries commands and raw values rather than only conclusions.

**Out-of-scope synthetic extremes** — inputs this spec explicitly declines to protect against:

- **Any degree beyond 3 in this task's own arms**, any sweep and any knee-finding; a finding demanding N=4, N=8 or a scaling curve is `out-of-input-space` — those degrees are already measured by T-1083 and T-1085 and are cited, not re-run.
- **More than two repetitions, or any inferential-statistics treatment of them.** A finding demanding a significance test, confidence interval, variance model or third repetition is `out-of-input-space`.
- **Sub-millisecond overlap discrimination.** The margin-factor floor exists precisely because this probe claims no resolution below the launch-latency noise floor.
- **Discriminators other than the tool set** — a prompt-echo test, a model-identity self-report, a harness-internal API — are `out-of-input-space`: the tool set is the one difference between the two candidate types that leaves a physical artifact a differential can read, which is the property that makes it decisive; a richer discriminator is a different task.
- **Any claim about role resolution beyond the effective tool set in this venue.** The model actually bound, the system prompt actually loaded, and any behaviour not expressed through the tool set are outside what this design reaches; the note's `- evidence-boundary:` line says so, and a finding that the differential does not prove them is a correct description of this task's declared scope rather than a defect.
- **A cryptographic or externally-timestamped proof that the freeze preceded the probe.** Commit ancestry and byte-identity establish DAG structure, not wall-clock order; this is the self-attestation boundary every freeze-attestation and provenance record in this repository already stands on, this task introduces no new exposure to it, and building an attestation mechanism is out of a `no-mechanism` task's scope.
- **A genuinely monotonic clock, or a rename of the `clock-source-monotonic` id.** The id is inherited vocabulary shared with three merged notes and is disclosed rather than renamed here; a finding demanding `time.monotonic_ns()` or a new id is answered by the fast-follow issue filed at close-out against all four notes.
- **Plugin roles other than `qa-verifier`, models other than the one bound, harness versions other than this one, and machines other than this one.** Every such generalization is `unobserved` by construction and a finding demanding it is `out-of-input-space`.
- **Adversarial stall shapes, forced termination, watchdog behaviour.** No watchdog runs and nothing here terminates an agent; a finding demanding a stall arm, a `TaskStop` or a liveness instrument is `out-of-input-space` and is answered by the operator's 2026-08-31 retirement ruling.
- **Write contention between concurrent instances.** Probe instances write nothing outside the one shared clone by construction, and inside it no two of the nine share a path, since the frozen filename carries both the arm id and the instance id; interleaved appends, duplicate `seq` values and lock design are the append-locking car's subject (#285).
- **Self-emitted telemetry from a probe instance.** Channel ③ is excluded with its reason stated; a finding that the instances did not emit their own spans is a correct description of this task's declared scope.
- **A renumbering repair of any cited line.** A finding that a repaired line would read better as two lines, or that a note would be clearer re-flowed, is `out-of-input-space`: line-count preservation is the requirement, and prose quality inside one line is subordinate to it.
- **Any claim about what a future fan-out would cost in tokens or dollars**, and any portability claim across machines or harness versions.
- **A checkout with no local and no remote-tracking `feature/1110-freeze-version-derivation` ref, a detached `HEAD` with no merge base, a rebased or force-pushed predecessor, and a board from which the probe-evidence sub-bullet has been removed.** Every criterion fails closed on the first two, the third is a route-back rather than a case any arm is widened for, and the fourth is exactly what **AC2**'s `- stale-at:` trigger names.

<!-- END intent-block: T-1111 -->

## Body-to-AC correspondence

One row per normative directive stated in the body prose, mapped to the criterion carrying it or marked exempt with a reason.

| # | Body directive | Source | Carried by |
|---|---|---|---|
| 1 | The note lands at exactly `docs/loop-engineering/plugin-role-agent-concurrency.md`, distinct from the four sibling notes | Goal | **AC1** |
| 2 | The note is new, not a rewrite of merged prose | Goal / Non-goals | **AC1** (absence at the branch point) |
| 3 | The probe is an orchestrator step; no sub-agent can launch one, and sub-agent hosting is prohibited | Goal, "Why the probe is an orchestrator step" | **AC2** (evidence committed before the engineer), **AC3** (ordering); the hosting prohibition itself is `info-only (not promoted to AC)` — no artifact-reading check can observe which session issued a launch, and it is carried by the ruling in `.shell-team/interventions/T-1110.md` plus **AC16(d)** |
| 4 | Raw evidence is committed before the engineer is invoked and is byte-immutable thereafter | Goal | **AC2** |
| 5 | The probe-evidence commit is recorded on the board as a task-scoped sub-bullet | Goal | **AC2** |
| 6 | Freeze-then-probe: no post-hoc protocol fitting | Goal | **AC3** |
| 7 | Venue is **exactly one** throwaway `git clone` under `$TMPDIR`, shared by all three arms and pinned to the branch point; no second clone and no per-arm venue | Protocol, "Venue" / Non-goals | **AC4** (the `git clone`/`$TMPDIR` literals plus **exactly one** `- clone-ref:` line in the whole note, whose hex must equal the resolved branch point — the count is the mechanical form of "one venue") |
| 8 | `git worktree add` is never used as the venue | Protocol, "Venue" | **AC4** (zero occurrences in any `- command: ` line, so the note stays free to *disclose* the avoidance) |
| 9 | Instances write nothing outside the shared clone, and the real checkout's cleanliness is verified after each arm rather than promised | Protocol, "Venue" | **AC4** (`- probe-writes: none` scoped to writes outside that clone, both commands quoted) |
| 10 | Any clone-source path is written as `<this-checkout-root>`, never a real home path | Protocol, "Venue" | **AC4** (placeholder present, zero home-shaped paths, the former making the latter non-vacuous) |
| 11 | The unit is `bash bin/check-acs.sh .shell-team/specs/T-1044-test-infra-bundle.md`, re-timed at this branch point before any arm | Protocol, "Unit" | **AC8** (`- unit-duration:`), **AC9** (`production-unit`), **AC16(h)** |
| 12 | Unit duration must exceed launch latency by a declared factor of at least 3, else `undetermined`/`launch-refused` | Protocol, "Unit" | **AC8** |
| 13 | No watchdog and no **negative-control stall** arm, so no stall-threshold/control-sleep ordering invariant exists — a different instrument from the differential control arm that does run | Protocol, "Unit" / Non-goals | **AC8** (zero `- stall-threshold:` and zero `- control-sleep:` lines), **AC5** (`- not-run:`) |
| 14 | Exactly one role type (`shell-team:qa-verifier`) is exercised; extension to others is `unobserved` | Protocol, "Subject role" | **AC7** (the note quotes that role's tool set), **AC12** (the `- unobserved:` grammar survives in the edited sibling note); the note's own `unobserved` line is `info-only (not promoted to AC)` — a Limits statement whose wording no grammar can validate without prescribing the prose, reported under **AC16(h)** |
| 15 | The model confound (sonnet here, haiku in T-1073's baseline) is disclosed, not attributed away | Protocol, "Subject role" | `info-only (not promoted to AC)` — a disclosure obligation over free prose; the reading gates own it and **AC16(h)** reports it |
| 16 | Exactly three arms — two subject repetitions plus the `general-purpose` differential control — three instances each, one message per arm, nine launches in total, **launched in the frozen order rep1 → control → rep2** so a time-varying condition cannot align with the subject/control split | Protocol, "Arms" | **AC5** (all three `executed`), **AC6** (≥3 timestamp lines with three distinct instance ids in **each** arm); **AC8** (the recorded `batch_start` integers must read `resolved-type-n3-rep1 < control-gp-n3 < resolved-type-n3-rep2`, so the frozen order is verified arithmetically from data the note already carries); the one-message-per-arm half stays `info-only (not promoted to AC)` — no artifact-reading check can observe how many messages a launch was split across, the frozen protocol region fixes it (**AC3**), and **AC16(h)** still reports the issued order beside the frozen one |
| 17 | No serial-baseline arm and no negative-control-stall arm | Protocol, "Arms" | **AC5** (`- not-run:` lines, at least two) |
| 18 | Each instance — control and subject — makes exactly one `Write` attempt at the frozen `probe-write-<arm-id>-<instance-id>.txt` path inside the shared clone, and reports a closed three-value outcome | Protocol, "The discriminator is differential" | **AC7** (the artifact and self-report grammars, plus the `probe-write-` literal on the page) |
| 19 | A subject-arm artifact `present` falsifies the declared label and is a legitimate deliverable | Protocol, "What the contrast buys" / "Verdict" | **AC7** (`contradicted` is a conformant value with its own artifact-read coupling) |
| 20 | The agent briefing text is fixed verbatim inside the frozen region | Protocol, "The agent briefing text" | **AC3** (the region is byte-locked between the evidence commit and HEAD) |
| 21 | Every instance passes an explicit per-call timeout above 120000 ms | Protocol, "The agent briefing text" | **AC16(h)**; `info-only (not promoted to AC)` for the note's own text — the parameter is a launch-time argument no artifact-reading check can observe, which is why the frozen briefing fixes it and the runtime item reports it |
| 22 | The clock source is verified to expand before it is relied on | Protocol, "Clock source" | **AC8** (`- clock-source:` closed vocabulary), **AC9** (`clock-source-monotonic`) |
| 23 | Four evidence channels with fixed roles across two questions; `orchestrator-span-rows` is excluded with its reason stated | Protocol, "Evidence channels" | **AC6** (closed four-id, three-role channel grammar, with `write-artifact-existence` pinned `primary` and `tool-self-report` pinned `corroborating`, plus the `#285` literal) |
| 24 | Exactly one concurrency verdict from the closed five-value vocabulary | Protocol, "Verdict" | **AC9** |
| 25 | Exactly one `- resolved-type:` verdict from a second, orthogonal closed four-value vocabulary, with `declared-label-only` claimable only where the arm record itself says no arm ran | Protocol, "Verdict" | **AC7** (the branch reads **AC5**'s `- probe-arm:` family: at least one line, zero `executed`) |
| 26 | Eight licence conditions, each `met`/`not-met`; any `not-met` confines the concurrency verdict | Protocol, "Verdict" | **AC9** |
| 27 | `queued-partial` states a measured `k` | Protocol, "Verdict" | **AC9** |
| 28 | A negative result of either kind is a complete deliverable and nobody is pressured toward a positive | Protocol, "Verdict" | `info-only (not promoted to AC)` — a statement about how the reading gates must judge the outcome; its mechanical consequence is that **AC7**/**AC9** pass on every value of both vocabularies, which is the testable half |
| 29 | The prior measurements are recorded by citation, one line per already-measured arm, never re-run | Goal, "What the new note records" / Non-goals | **AC10** |
| 30 | Every stale surface is dispositioned on a record line, including the ones left alone | Goal, "The stale-surface repair" | **AC11** |
| 31 | Every repair is line-count preserving | Goal, "The stale-surface repair" / Non-goals | **AC11** (equal line counts plus a non-identity assertion) |
| 32 | No surviving line misquotes `harness-agent-concurrency.md`'s `- unobserved:` line after it moves | Goal, "The stale-surface repair" | **AC11** (the `not yet independently confirmed` absence, scoped to the three files) |
| 33 | `agent-concurrency.md:162` is left; `phase-multiplexing.md:387` is annotated, not rewritten | Goal, "The stale-surface repair" | **AC11** (`- surface: … — left` / `— annotated` are conformant values of the same grammar) |
| 34 | T-1073's own grammars survive the repair, and no `## Probe evidence` byte moves | Goal / Non-goals | **AC12** |
| 35 | T-1080's erratum b and T-1100's dispatcher grammar survive | Goal, "The stale-surface repair" | **AC12** |
| 36 | Frozen history — interventions, retro, board rows of T-1109/T-1110 — is never edited | Goal / Non-goals | **AC14** (those paths are outside the allow-list, so an edit to a board row other than this task's own additions is caught as a survival failure by **AC13**) |
| 37 | `- user-visible: no`, therefore no adopter-surface and no waiver line | Declaration region | `info-only (not promoted to AC)` — the declaration gate is read by pm-spec at authoring and by the freeze run before the hash is recorded; issue #250 carries the mechanical gate and inventing a local one would ship a second, divergent implementation |
| 38 | `- verification-class: no-mechanism`, so the inventory is read-set-scoped, CI-equivalence is reachability-scoped and mutation probes cover added check lines only | Declaration region | **AC15** (read-set-scoped inventory), **AC16(a)(c)** |
| 39 | `- verification-ceiling: real-environment`, non-vacuous because **AC2**–**AC9** are verified only at it | Declaration region | `info-only (not promoted to AC)` — the ceiling gate is a read the freeze run performs; no checker ships for it (issue #250) |
| 40 | No mechanism anywhere: nothing under `bin/`, `tests/`, `templates/`, `.github/`, `skills/`, `agents/`, no `README*` | Non-goals | **AC14** |
| 41 | Populations are derived by `bin/derive-populations.sh`, never counted by eye | Goal (verification-class) / Blast radius | **AC15** (embedded derivation block plus its `- reproduce:` line) |
| 42 | No git-ref label on any machine-local number | Non-goals | `info-only (not promoted to AC)` — a labelling discipline over free prose; a grammar strict enough to test it would prescribe the note's sentences, and **AC16(h)** reports it |
| 43 | No inferential-statistics claim, no cost model | Non-goals | `info-only (not promoted to AC)` — an absence of a claim class in free prose; the reading gates own it and **AC16** reports it |
| 44 | Records, board, recipe hygiene and PII cleanliness | Goal / Non-goals | **AC13**, **AC4** (the note's own home-path count) |
| 45 | Scope lock, merge-point-scoped and expected to go stale | Non-goals | **AC14** |
| 46 | No repair of any merged criterion this diff reddens; disclosure only | Non-goals | **AC15** |
| 47 | Pre-commitment: trigger, drop order, never-dropped set, invalid-evidence rule, AI self-discipline | Goal, "Pre-commitment" | `info-only (not promoted to AC)` — a rule governing how the loop reacts to review rounds, which no artifact-reading check can observe; **AC5**'s `- declined-to:` parity is the one mechanical trace it leaves |
| 48 | This task closes no issue and merges nothing | Non-goals | `info-only (not promoted to AC)` — a close-out disposition, enforced by `bin/close-out.sh`'s own gates rather than by a criterion here |
| 49 | The clone carries ordinary write permissions and is deliberately not made filesystem-read-only, so the `available` outcome stays reachable | Protocol, "Venue" / Non-goals | `info-only (not promoted to AC)` — a property of a temporary directory no committed artifact records; its observable consequence is that a control artifact can be `present` at all, which **AC7**'s `plugin-role-confirmed` coupling requires and **AC16(h)** reports |
| 50 | The shared clone records its resolved `HEAD` on one line, which must equal the branch point | Protocol, "Venue" | **AC4** |
| 51 | The load-bearing discriminator evidence is orchestrator-read file existence, no verdict branch reads an instance's self-report, and **an artifact/self-report disagreement is an invalid-evidence event** | Protocol, "The discriminator is differential" / "The self-report is kept as a secondary channel" / Input space | **AC7** (every verdict coupling reads `- write-artifact:` lines only, **plus** a consistency coupling requiring, per `(arm, instance)` pair, `present` ↔ `available` and `absent` ↔ `refused`/`absent` — the invalid-evidence rule made mechanical, able to fail the criterion but never to license a value), **AC6** (`write-artifact-existence` pinned `primary`, `tool-self-report` pinned `corroborating`) |
| 52 | The control arm is never dropped while any subject arm runs | Protocol, "Arms" / Pre-commitment | **AC5** (`control-gp-n3` required `executed`), **AC9** (`differential-control-arm-executed`) |
| 53 | Control artifacts all `absent` is a venue-wide denial and forces `undetermined`, never a positive | Protocol, "What the contrast buys" | **AC7** (the three positive branches are jointly unreachable in that state, leaving `undetermined` as the only conformant value) |
| 54 | The residual a tool-set differential does not reach — model binding, system prompt, anything not expressed through the tool set, **an instance holding `Write` that silently declines to attempt it**, and **a transient non-monotone venue denial coinciding with exactly the two subject messages** — is disclosed on its own line | Protocol, "The residual this design does not close" / "Arms" / Non-goals | **AC7** (exactly one `- evidence-boundary:` line); the line's *width* is `info-only (not promoted to AC)` — a grammar strict enough to test which residuals it names would prescribe the note's sentences, and **AC16(h)** reports it |
| 55 | `unit-duration` and `launch-latency` are re-derived from raw recorded integers rather than declared, `batch_ns` is anchored to the same raw values, and **every executed arm carries exactly one `- batch-timestamp:` line** so none can be omitted from the maximum | Protocol, "Unit" | **AC8** (the per-arm count equals the executed-arm count taken from **AC5**'s own record) |
| 56 | `clock-source-monotonic` is inherited vocabulary, is not renamed here, and its misnomer is disclosed beside it | Protocol, "Clock source" / Non-goals | **AC9** (exactly one `- vocabulary-caveat: clock-source-monotonic — <text>` line); the fast-follow **filing** is `info-only (not promoted to AC)` — a coordinating-session close-out action no criterion in this spec can observe |
| 57 | The repaired `agent-concurrency.md` carries a cross-reference to the new note, so the removed sentence cannot read as closing the type-resolution residual too | Goal, "The stale-surface repair" | **AC11** (the `plugin-role-agent-concurrency.md` literal at `HEAD`) |
| 58 | Freeze-then-probe ordering rests on commit ancestry, which proves DAG structure rather than wall-clock order — the repository-wide self-attestation boundary, disclosed rather than mechanised | **AC3**'s own prose / Input space | `info-only (not promoted to AC)` — a disclosure about the limit of an existing mechanism; building an attestation mechanism is out of a `no-mechanism` task's scope and is named `out-of-input-space` |
| 59 | The briefing text is byte-identical for control and subject instances apart from arm id, instance id and target filename | Protocol, "The agent briefing text" | `info-only (not promoted to AC)` — launch-time input no artifact-reading check can observe, which is why the frozen region fixes it verbatim (**AC3**) and **AC16(h)** reports the launches |

## Blast radius

**Method: read-set-scoped** — this task's declared `- verification-class: no-mechanism` prices the downstream-impact inventory as an analysis over the merged criteria that read any path this task edits, rather than as a full-population diff over every merged spec. The edited paths are closed by **AC14**'s allow-list: this spec (new), the board, `docs/loop-engineering/plugin-role-agent-concurrency.md` (new), the **three edited notes**, `.shell-team/test-recipe.md`, and this task's three records. A new file can be read by no merged criterion, so the real read set is the board, the recipe and the three edited notes — and that last group is what makes this read set large, since all three are cited by many merged specs' `- check:` lines.

- reproduce: bash bin/derive-populations.sh --label t1111-read-set --set phasemux="grep -lE -- '- check:.*loop-engineering/phase-multiplexing\.md' .shell-team/specs/*.md | LC_ALL=C sort" --set agentconc="grep -lE -- '- check:.*loop-engineering/agent-concurrency\.md' .shell-team/specs/*.md | LC_ALL=C sort" --set harnessconc="grep -lE -- '- check:.*loop-engineering/harness-agent-concurrency\.md' .shell-team/specs/*.md | LC_ALL=C sort" --set indirect="grep -lF -- 'team-paths.sh --get' .shell-team/specs/*.md | LC_ALL=C sort" --accept-status phasemux=1 --accept-status agentconc=1 --accept-status harnessconc=1 --accept-status indirect=1

*(The emitted `<!-- BEGIN derivation: t1111-read-set -->` … `<!-- END derivation: t1111-read-set -->` block is embedded verbatim here by the execution-capable side — the freeze run or the engineer — because this role holds no shell and may not hand-derive a population. **AC15** requires the block's presence and grammar; no population figure is written into this section by hand, and none can therefore go stale.)*

- base-side reading target: the branch point's **committed blobs**, read through `git show <branch-point>:<spec>` into a scratch tree under `$TMPDIR`, or through per-slice `git worktree add --detach` trees staged per `.shell-team/test-recipe.md`'s T-1108 corpus-staging entry; never the working tree, whose bytes at read time are not that ref's.
- executor form: coordinating-session-hosted harness-tracked background slices, serial within a slice, per `.shell-team/test-recipe.md`'s T-1110 entry — never a sub-agent hosting detached children (`.shell-team/interventions/T-1110.md` entry 3).
- **indirection** — the class this method cannot see by construction: a criterion that reaches the board through `bin/team-paths.sh --get todo` at run time, or names an edited note only through a variable, matches no search for those literal bytes. Disposition, to be stated as measurement rather than prediction: every merged spec carrying a `bin/team-paths.sh --get` invocation in any `- check:` line is run at both refs regardless of whether it names an edited path literally — which is exactly why the `indirect` set above is declared as a fourth `--set` rather than left implicit — and whatever remains unrun is named here explicitly rather than left implied.
- predicted-red: 1

| Merged criterion | What it reads | Predicted base | Predicted head | Why |
|---|---|---|---|---|
| `.shell-team/specs/T-1110-freeze-version-derivation.md` **AC11** | its own scope-lock allow-list, resolved against its own predecessor's branch point | base: PASS | head: FAIL | genuinely caused by this task: this task's own spec, board additions, four notes and records enter the changed set taken from that predecessor's branch point, exceeding its allow-list — the ordinary stacked-car staleness both specs' Non-goals already name |
| `.shell-team/specs/T-1073-harness-agent-concurrency.md` **AC12** | `agent-concurrency.md` and `phase-multiplexing.md` byte-identical to *T-1073's own* branch point | base: FAIL (already) | head: FAIL | not caused by this task: those two notes have been edited by merged tasks since T-1073's branch point (T-1080's errata among them), so that criterion is already red before this diff exists; this task's repair adds to an already-broken comparison rather than introducing a new defect |
| `.shell-team/specs/T-1073-harness-agent-concurrency.md` **AC1**/**AC11** | `harness-agent-concurrency.md`'s nine headings, its `- unobserved:`/`plugin-role` and `- claim-under-test:` grammars | base: PASS | head: PASS | **AC12** of this spec exists precisely to keep this row green: the repair narrows the `- unobserved:` line's content while preserving its prefix and the literal `plugin-role` |

*(These rows are predictions authored without a shell. Every one is replaced by the measured verdict under **AC16(d)**, and where a measurement differs the table is corrected to it rather than the prediction defended. The full row set comes from the derivation block above, not from this table.)*

## Assumptions

- **A-1 — MEASURED FALSE, recorded as a finding about the hand-off rather than corrected in passing.** The briefing's premise-correction table states: *"Open residual — `harness-agent-concurrency.md:374` … CONFIRMED — this is what T-1111 measures."* Measured first-hand at the primary sources: `docs/loop-engineering/agent-launch-fanout.md:185`/`:188`/`:200`/`:217`/`:224` record two and four `shell-team:qa-verifier` instances launched as parallel `Agent` tool calls inside one orchestrator message with margins `140385350000` ns and `100572685000` ns and a verdict naming itself "the repository's first empirical crossing of T-1073's own declared `unobserved` plugin-role boundary"; `docs/loop-engineering/default-path-firing.md:635`/`:644`/`:59` record eight such instances with a `878198706000` ns eight-way window and `- licence-condition: real-plugin-role-instances — met`. The residual is therefore **not** whether plugin-role Agent-tool concurrency happens, but whether the declared `subagent_type` is the resolved one (`agent-launch-fanout.md:225`, `:247`). Exact text for the orchestrator's interventions entry, since this role does not own that file: *"Relayed premise measured false: the Plan-time premise table nominates `harness-agent-concurrency.md:374`'s unobserved plugin-role boundary as T-1111's subject; that boundary was crossed by T-1083 (`plugin-role-n2`, `plugin-role-n4`) and replicated by T-1085 (N=8), and `:374` is itself a stale surface T-1083's own follow-up asked a later maintainer round to repair. The genuinely open residual is the declared-label-versus-resolved-type gap both notes disclose with an explicit counterfactual. The spec is re-scoped accordingly and held at DRAFT/`BLOCKED` pending the disposition in `## Open questions`."*
- **A-2 — RELAYED — issue #398's body and open state.** The full body reached this role as text pasted into its launch prompt; this role has no network path and could not open the issue. Its own Scope sentence ("launch N (>= 3) real Agent-tool sub-agent invocations in one message … record whether execution actually overlaps or serializes") is satisfied by this spec's three three-instance arms — two subject repetitions and the differential control — even under the re-scope, which is why the re-scope does not abandon the issue's literal ask. **No criterion depends on any figure or wording from the issue.** The freeze run must re-measure the issue's live state and report it beside this line, and must additionally decide whether the falsification above warrants an issue comment before this task proceeds.
- **A-3 — RELAYED — the sprint premise "v2.4.1 PATCH, every item internal-mechanism".** Held by the coordinating session as conversation, with no tracked file this role can read. This spec's own declarations (`- user-visible: no`, `- verification-class: no-mechanism`) are consistent with it, and the authoring-time tier reading is **PATCH** (headline test not met — no adopter recognizes an internal loop-engineering note; default-reachability not met; CONTRIBUTING requires both jointly for MINOR). The T-1110 freeze-time version-derivation gate re-derives this independently at the freeze and records its own verdict, which governs over this reading.
- **A-4 — RELAYED, and measured as far as this checkout allows — PR #404's open state for `feature/1110-freeze-version-derivation`.** The PR's state is a repository-of-record fact this role cannot query. What is measured first-hand: `.git/refs/heads/feature/1110-freeze-version-derivation` exists and reads `e14898a5f0f56505f1bea2de23f046fabe590eee`, identical to this branch's own ref, so the predecessor resolves as a local branch and arm 1 of the `- base-ref-discriminator:` is the arm this checkout selects. The freeze run reports the PR's measured state beside this line.
- **A-5 — Measured correction to the briefing's subject-role uniqueness claim.** The briefing describes `qa-verifier` as "the only Bash-holding plugin role without Write/Edit whose normal operation does not invoke the external Codex CLI". Measured across all nine `agents/*.md` frontmatter blocks: `agents/drift-evaluator.md` carries the identical `tools: Read, Grep, Glob, Bash` line and the same absence of Write/Edit and of any Codex invocation, so the criterion selects **two** roles rather than one. The choice of `qa-verifier` stands on a different and stronger ground — it is the role both T-1083 and T-1085 launched, so this task's discriminator applies directly to the instances whose type claim it is auditing. Related, measured and **not repaired here** because it is merged prose outside this task's scope: `agent-launch-fanout.md:247` calls `qa-verifier` "the one role among this repository's nine `agents/*.md` definitions that can run `bash bin/check-acs.sh`", while five of the nine carry a `Bash` token.
- **A-6 — Borrowed-vocabulary count premises, enumerated for the freeze run's measurement half.** This spec asserts a literal `= 0` count for two **borrowed** tokens, neither coined by this task: (i) `not yet independently confirmed` (**AC11**), coined by `agent-concurrency.md:120` under T-1080 — measurement command: `for p in docs/loop-engineering/harness-agent-concurrency.md docs/loop-engineering/agent-concurrency.md docs/loop-engineering/phase-multiplexing.md; do git show <branch-point>:$p | grep -c 'not yet independently confirmed'; done`, expected non-zero at the branch point and zero at HEAD, which is what makes **AC11**'s absence assertion a real repair rather than a vacuous truth; (ii) `- stall-threshold: ` and `- control-sleep: ` (**AC8**), coined by T-1073's own note grammar — measurement command: `grep -c '^- stall-threshold: ' docs/loop-engineering/plugin-role-agent-concurrency.md`, over this task's **own new note only**, where the expected count is zero because the file does not exist at the branch point. **Own-coinage** literals, needing no branch-point measurement — the list absorbs every literal this round's differential redesign introduced: `resolved-type-n3-rep1`, `resolved-type-n3-rep2`, `control-gp-n3`, `- resolved-type: `, `- tool-probe: `, `write_tool=`, `- write-artifact: `, `probe-write-`, `- clone-ref: `, `- unit-timing: `, `- batch-timestamp: `, `- evidence-boundary: `, `- vocabulary-caveat: `, `write-artifact-existence`, `tool-self-report`, `corroborating` (as a channel-role value), `tool-set-discriminator-exercised`, `differential-control-arm-executed`, `gp-1`/`gp-2`/`gp-3`, `- prior-measurement: `, `- not-re-measured: `, `- surface: `, `- not-run: `, `probe-protocol: T-1111`, `- probe-evidence-sha (T-1111): `. Retired at the round-2 redesign and no longer asserted anywhere: `mixed-type-n3`, which that redesign replaced with the never-dropped `control-gp-n3`. **The round-3 narrowing introduced no literal and retired none**: it changed counts and couplings over the grammars already listed here (`- clone-ref:` from one-per-arm to exactly one, `- batch-timestamp:` from at-least-one to one-per-executed-arm, and `declared-label-only` now reading the existing `- probe-arm:` family), so this enumeration is unchanged by it. Two further **borrowed** tokens are reused with no `= 0` premise attached and therefore owe no branch-point measurement, named here so the classification is complete rather than selective: `clock-source-monotonic` (T-1073's licence-condition id, asserted present exactly once in **this task's own new note** and deliberately not renamed) and `general-purpose` (the harness's own built-in agent type, asserted present at least once in the same new note). The freeze run measures every borrowed token in the `= 0` list above at the branch point's committed blob and records the value beside this line.
- **A-7 — UNVERIFIED from this role — the clock source and the `Write`-tool refusal shape.** Whether the probe host's `python3` is present and whether a plugin-role instance reports a missing `Write` tool as `absent` rather than attempting and failing in some third way is unknown here. The protocol therefore fixes a closed three-value report and requires the probe to record which it observed; an outcome outside that set is an invalid-evidence event under the frozen rule, not a value to be invented. **This round's redesign moves that uncertainty off the load-bearing path**: the report is now the corroborating channel, while the verdict reads file existence, which has exactly two states and no third way to fail into.
- **A-9 — MEASURED BY THE PROBE ITSELF, deliberately not assumed — that a `general-purpose` instance holds an effective `Write` in this venue, and that the venue admits a write at all.** This role cannot launch an agent and holds no shell, so neither premise is measurable here; and neither is *assumed* by the design, which is the point of making the control arm's artifacts a precondition of the only positive value. If `control-gp-n3`'s artifacts come back `absent`, the recorded outcome is `undetermined` with the venue-denial reason, not a positive on the subject arms' silence. The freeze run and the probe report the observed values beside this line rather than the expected ones.
- **A-8 — Assumed and stated — bash integer width.** The criteria compare 19-digit epoch-nanosecond values using bash arithmetic, which is 64-bit on every platform this repository supports; `awk` and `sort -n` are deliberately avoided because a double cannot hold a 19-digit integer exactly.

## Open questions

**None outstanding. The one question that was blocking is resolved — kept below as the record of what was decided and against what alternatives.**

**Resolution: disposition (a), operator-selected on 2026-08-31**, recorded as the third entry (`intervention: human-correction`, date 2026-08-31) of `.shell-team/interventions/T-1111.md`, which states the ruling and its ground: the probe "is the first verification that a plugin role's shipped tool restriction is actually enforced on concurrently launched instances", and the sprint's v2.4.1 PATCH premise is unaffected. This spec was authored for (a), so nothing in the Goal, the frozen protocol region, the Non-goals, the acceptance criteria or the Input space changed when the ruling landed; the spec's Status moved `DRAFT` → `READY_FOR_ARCH` and the board entry moved `BLOCKED` → `READY_FOR_ENG`.

The question as it stood, and the three options it was decided among — retained verbatim so a later reader sees what was weighed rather than only what was chosen. Given that the measurement issue #398 asked for has already been performed twice (**A-1**), which disposition does T-1111 take?

- **(a) Re-scoped as written above — CHOSEN** — the reconciliation note, the stale-surface repair, and the narrowed live probe closing the declared-label-versus-resolved-type residual. Cost: three three-instance arms — nine launches, the third being the differential control added in spec-review round 2 — plus the repair round. Declarations stay as written (`real-environment` ceiling). This is **this role's recommendation**, on the ground that the repair is owed regardless (three merged notes carry follow-ups explicitly deferring it to "a later maintainer round", and this is that round), the residual is real and both T-1083 and T-1085 name it in their own words, and the probe as specified also satisfies issue #398's own literal `N >= 3` Scope sentence, so the issue closes honestly rather than being closed on a technicality.
- **(b) Repair-only** — drop the probe and **AC2**–**AC9**, drop the `real-environment` ceiling to `unit-and-static`, ship the reconciliation note and the stale-surface repair, and carry the resolved-type residual to issue #398 (or a successor) as its remaining requirement. Cost: no arms at all. This is the cheapest honest outcome and is exactly the pre-commitment's droppable-second disposition, executed up front rather than after two adverse rounds.
- **(c) Cancel and re-plan** — close #398 as already-discharged, file the repair round and the resolved-type residual as separate issues, and return T-1111 to planning. Cost: the sprint loses a slot; the three stale surfaces stay stale until someone picks the repair issue up.

The choice **is** recorded in `.shell-team/interventions/T-1111.md` together with **A-1**'s exact text (that file's first entry carries it verbatim), because the premise that broke is the task's own justification and `CLAUDE.local.md`'s 承認ゲートの範囲 names「issue の意図との矛盾」and「スコープ変更」as the two classes that stop and rise rather than proceed on this role's own authority. That same file's first entry additionally records the layered-relay accounting the coordinating session owns — three successive readers stopping at three different depths of one supersession-declared-but-not-performed chain — which is carried into this task's own repair scope and to the retro.

## Notes for engineer

- **You cannot run the probe, and nothing here asks you to.** The `## Probe evidence (raw, orchestrator-produced)` section arrives already committed; treat it as read-only input. Your job is the other eight sections plus the three in-place repairs. If the evidence does not support a conclusion, the honest write-up is `undetermined` with the reason.
- **The three repairs are the delicate part, and the delicacy is mechanical rather than editorial.** Each is an in-place replacement that must not change its file's total line count (**AC11**) and must not disturb the grammars **AC12** guards. In particular: `harness-agent-concurrency.md`'s `- unobserved:` line must **stay** a `- unobserved:` line and must **keep** the literal `plugin-role`, because T-1073's own merged **AC11** counts exactly that — narrow the line's content to the residual that genuinely remains (the resolved-type gap, and generalization beyond `qa-verifier`/sonnet) rather than deleting or re-prefixing it. `agent-concurrency.md:120` quotes that line **verbatim**; repair both in the same edit or the quote becomes a misquote.
- **Measured-at-ref labels**: this task's deliverable prints no `measured at <ref>` label, no `**Base ref**:` header over a machine-local figure and no per-row `- measurement:` field tying a timing to a git ref — **not applicable, by construction**: every timing figure is machine-local and single-session, and the Non-goals bar attaching a git-ref label to one. The only ref-labelled reads are of committed blobs, made with `git show`.
- **Files likely touched**: `docs/loop-engineering/plugin-role-agent-concurrency.md` (assembly), `docs/loop-engineering/harness-agent-concurrency.md`, `docs/loop-engineering/agent-concurrency.md`, `docs/loop-engineering/phase-multiplexing.md` (line-count-preserving repairs), `.shell-team/test-recipe.md` (append one line naming `T-1111` under `## Appended by tasks`), `.shell-team/provenance/T-1111.md`, the board entry, and nothing else. `.shell-team/interventions/T-1111.md` is the orchestrator's file; do not write it — it already exists and carries the three entries this task owed at authoring (**A-1**'s exact text, the Routing-Map relay omission, and the operator's disposition ruling), so **AC13**'s conformance clause has a real file to read; anything further owed there is the orchestrator's to append.
- **The `10#` and `awk` gotchas are load-bearing.** Several criteria compare 19-digit integers. Use `$(( 10#$v ))`; do not reach for `awk` or `sort -n` when you reproduce these comparisons by hand, and do not "simplify" a check into one.
- **`CHECK_ACS_TIMEOUT` is inert on this host** — `.shell-team/interventions/T-1109.md` entry 1 measured that neither `timeout` nor `gtimeout` resolves here, so the wrapper falls back to a plain run and cannot bound a stuck check. Raise the value anyway (at least 900) and record it, since **AC13** runs five checkers over a large board, but do not rely on it as a bound.
- **Sandbox quirk worth inheriting** — `.shell-team/test-recipe.md`'s T-1110 entry: a multi-statement `bash` block with an inline `mktemp -d "${TMPDIR:-/tmp}/x.XXXXXX"` was denied outright by this sandbox, while the identical logic as a single-line command was permitted. Every `- check:` line above is already a single line for that reason.
- **Marker discipline**: this spec carries two marker pairs — the intent block and, nested inside its Goal section, `probe-protocol`. `bin/check-intent.sh` matches whole lines scoped by task id, so the second pair is inert to it. Do not move, reformat or re-indent either pair; **AC3** compares the inner region byte for byte.
- **Prior art to read before writing a line**: `docs/loop-engineering/agent-launch-fanout.md` `:184-189`, `:200`, `:217`, `:220-231`, `:243-254`, `:257-265`; `docs/loop-engineering/default-path-firing.md` `:44-59`, `:635`, `:644`, `:705-712`; `docs/loop-engineering/harness-agent-concurrency.md` `:344-357`, `:371-396`. Those are the sources the reconciliation cites; do not paraphrase them from this spec's summary.
- **The probe evidence you are handed carries more line families than the previous round's did.** Beyond `- agent-timestamp:` and `- tool-probe:`, expect `- write-artifact:` (the orchestrator's own reading of the one shared clone, one line per instance), `- clone-ref:` (exactly one, for that one clone), `- unit-timing:` and `- batch-timestamp:` (exactly one per executed arm). **AC8** re-derives `- unit-duration:` and `- launch-latency:` from those raw integers, so do not "tidy" a raw line, round a value, or reconcile a declared field to a nicer number: if a declared field does not equal its derivation, the honest write-up is that the evidence is invalid, and the frozen invalid-evidence rule says what happens next.
- **The self-report never licenses a conclusion, and your prose must not let it.** Where a `write_tool=` value disagrees with the artifact the orchestrator found, the artifact governs and the disagreement is recorded as one. Where every artifact including the control's is `absent`, the note says `undetermined` with the venue-denial reason — not "the plugin role's tool restriction held".
- **Dispositions decided here so close-out does not re-derive them**: this task **closes no issue**. Issue #398's disposition is `## Open questions`'s decision, not close-out's. #277, #274, #285 and #399 all stay open and untouched. One issue is **filed** rather than closed: the `clock-source-monotonic` vocabulary repair across all four notes, which the **coordinating session** files at close-out — not you, and not from inside any note this task edits.

## Verdict scope

Per the operator's 2026-08-14 proportionality ruling and T-1065's shipped pricing, the reading gates' mandate on this deliverable is **the computed values and the direction of the conclusion** — whether the overlap margin follows from the recorded timestamps, whether the two verdicts follow from the evidence, whether the licence conditions are honestly marked, and whether the reconciliation's cited figures match the notes they are cited from. The criteria above are presence-and-structure shaped and do not re-derive an analysis conclusion; a finding that a criterion "only checks the shape" is a correct description of this task's declared pricing rather than a defect in it. Conversely, a finding that a **number in the note does not follow from the note's own recorded evidence, or does not match the merged note it is quoted from**, is squarely in scope and is exactly what **AC6**'s margin re-derivation and **AC10**'s citation grammar exist to make cheap. Per `CLAUDE.local.md`'s 2026-08-22 dev-scaffold ruling, adversarial completeness of this spec's own inline `- check:` guards is not a Major-bar subject; the shared checkers under `bin/` keep full strength.
