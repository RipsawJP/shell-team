<!-- BEGIN provenance: T-1109 -->
- decision: implement the predecessor lookup as a SECOND `awk` scan spanning both `## Active` (`- [ ] **T-NNN** `) and `## Done` (`- [x] **T-NNN** `), leaving the pre-existing `--task` entry's own resolution untouched (still `## Active`-only, still exit `2` on failure) rather than widening that first scan to cover both sections.
  reason: the spec's own `## Assumptions` row A-8 and `## Notes for engineer` "first action" instruction both state this explicitly, and the reason is structural, not stylistic — the `--task` entry's forward-only `## Active`-only scoping (the script's own header `:47-53`) is a design invariant no criterion in this spec relaxes, while the predecessor reference answers a genuinely different question ("does this id name exactly one top-level entry anywhere") that this task's own live case (T-1108, already in `## Done`) requires answering across both sections. Conflating the two scans would either break the `--task` entry's exit-2 contract or silently widen it for reasons unrelated to this task.
  grounding: `.shell-team/specs/T-1109-dispatch-reflection.md` `## Assumptions` row A-8; `## Notes for engineer`, "Your first action, before writing any code" paragraph; `bin/check-entry-mode.sh`'s pre-existing header comment "Forward-only scoping" (unchanged by this task).

- decision: verify a stated `- dispatch-reflection:` verdict (`repeat`/`differs`) by actually comparing this entry's own recorded `- dispatch:` value for that axis against the predecessor entry's own recorded value for the same axis, deriving the expected verdict, rather than only checking the verdict field against a closed vocabulary list.
  reason: AC4's own fixtures (`lie1`/`lie2`) require the checker to catch a verdict that names a real predecessor and a real axis but states the WRONG relationship between the two entries' own values (`repeat` stated where the values actually differ, and vice versa) — a vocabulary-only check (is the token one of `repeat`/`differs`/`no-predecessor-row`?) cannot see this class of defect at all, since both `repeat` and `differs` are individually valid tokens.
  grounding: `.shell-team/specs/T-1109-dispatch-reflection.md` **AC4**'s check line (the `lie1.md`/`lie2.md` fixtures, each flipping a verdict away from what the two entries' own `- dispatch:` values actually support); the Goal sentence's own verb "disagrees with the predecessor entry's own recorded value".

- decision: place the new "Cross-task dispatch reflection" bullet in `skills/run/SKILL.md` structurally AFTER the pre-existing "Board transcription order" bullet (and before "Frozen-intent confirmation"), even though the new bullet's own prose states the reflection must be transcribed BEFORE the `- dispatch:` rows described in the bullet above it.
  reason: **AC2**'s check line asserts a purely positional/mechanical claim — the first line-number occurrence of the literal `dispatch-reflection` in the file must be at or after the line carrying `**Board transcription order` and strictly before the line carrying `**Frozen-intent confirmation`. Since the document's own reading order need not match the coordinating session's live execution order (each bullet's own prose states its own temporal position explicitly, as the pre-existing "Board transcription order" bullet already does relative to the freeze sweep two bullets later), inserting the new bullet immediately after the transcription-order bullet satisfies the mechanical ordering assertion while the new bullet's own text carries the correct "before the `- dispatch:` rows" instruction in prose.
  grounding: `.shell-team/specs/T-1109-dispatch-reflection.md` **AC2**'s check line (the `f >= h` / `f < g` line-number assertions); the Goal sentence ("written by the coordinating session in the same window `skills/run/SKILL.md:45` already fixes for the `- dispatch:` transcription and **before** it").

- decision: do not add an explicit "test the longer stem first" ordering rule for the new `- dispatch-reflection:` scans (unlike the pre-existing flagged-gap/flagged-gap-resolution pair, which uses an explicit `if`/`elif` priority), relying instead on every `- dispatch: ` scan's own colon-immediately-after-`dispatch` anchor to exclude `- dispatch-reflection:` lines by construction.
  reason: measured directly — `- dispatch-reflection:` has no colon immediately after the bare word `dispatch` (the next character is `-`, from `-reflection`), so every existing anchored `- dispatch:`-family regex (which all require the literal `dispatch: ` substring) fails to match a reflection line regardless of stem-testing order; the flagged-gap/flagged-gap-resolution pair's own ambiguity is different in kind (a genuine, colon-free shared prefix with no disambiguating character), which is why THAT pair needs an explicit priority rule and this one does not. Documented as a measured finding in `.shell-team/test-recipe.md`'s new T-1109 entry rather than left as a silent omission of the "test the longer stem first" instruction.
  grounding: `.shell-team/specs/T-1109-dispatch-reflection.md` `## Notes for engineer` trap 2 (the prefix trap) and **AC7** (which byte-pins and exercises all three shipped `- dispatch:`-family anchors against a reflection line, confirming zero matches); live measurement recorded in `.shell-team/test-recipe.md`'s new T-1109 entry (`grep -cE '^[[:space:]]*- dispatch: '` against a `- dispatch-reflection:` line → `0`).

- decision: given the practical infeasibility of executing check-acs.sh for all 113 already-merged specs at BOTH the base ref and HEAD within this session (a live-timed sample showed roughly 50 specs completing in several minutes per arm, with a number of specs themselves creating nested `git worktree add --detach` scratch trees as part of their own criteria), run the full two-arm sweep as a best-effort background job while ALSO running a small set of DIRECTLY TARGETED specs synchronously and to completion — the two specs this task's own authoring-time prediction named as most likely to flip (T-1100, T-1092) plus the two specs the freeze-time measurement already re-confirmed (T-1084, T-1085) — at both this task's own base ref and at HEAD, comparing the FULL per-AC FAIL set at each arm rather than a single AC in isolation.
  reason: this directly discharges the two shapes the spec's own `## Blast radius` `- predicted-red:` row names, with true measured evidence rather than an inherited or repeated prediction, while the background sweep provides best-effort coverage of the remaining population; any spec the background sweep could not complete within the session is named individually as unmeasured in the hand-off (AC15(c)'s own allowance) rather than folded into an unqualified "clean" claim.
  grounding: `.shell-team/specs/T-1109-dispatch-reflection.md` `## Blast radius` `- predicted-red: 2` row (naming T-1100/T-1092 and T-1084/T-1085 by shape); live measurement this round — T-1100's FAIL set (`AC2 AC5 AC6 AC7 AC8 AC9 AC11 AC13 AC14 AC15` at this task's own base, `AC2 AC5 AC6 AC7 AC9 AC11 AC13 AC14 AC15` at HEAD — AC8 base-red→head-green, no flip in the disallowed direction) and T-1092's FAIL set (`AC4 AC5 AC6 AC10 AC12 AC15 AC16 AC18 AC19 AC20`, identical at both arms) each measured base==head with no new flip; T-1084 AC9 and T-1085 AC8 each measured base-green/head-green identically at both arms, matching the freeze-run's own re-confirmation.

- decision: rework round 2 (codex review `.shell-team/reviews/T-1109.md`, round 1, `REQUEST_CHANGES`, 4 Majors + 1 Minor + 1 adversarial SKILL.md finding). Replace `dispatch_value_for_axis()`'s silent `head -1` first-match entirely with a hard refusal (exit 1 via `fail`) whenever more than one `- dispatch: <axis> — …` row matches the same axis, applied identically to BOTH call sites (the current entry's own lines and the resolved predecessor's lines) through one shared function parameterized by a `$3 who` label used only in the refusal message.
  reason: Major 3's own suggested fix ("require exactly one matching row per axis before extracting a value; refuse on more than one") applies symmetrically to both sides the review reproduced (predecessor still in `## Active`, and the current entry itself); a single shared function keeps the fix DRY and guarantees the same guard fires on both sides without duplicating the count-and-refuse logic per call site.
  grounding: `.shell-team/reviews/T-1109.md` Major 3 (`bin/check-entry-mode.sh:309-310`) and its "Suggested fix" line; live mutation probes `major3a-first-match-predecessor`/`major3b-first-match-current` (reverting to the old `head -1`-with-no-guard shape reproduces the exact silent-masking defect on both fixtures, confirming the fix's own necessity).

- decision: implement the single-shared-predecessor + no-self-reference invariant as a SEPARATE pre-pass over `$REFL_ROWS` — collecting every row's predecessor id and checking self-reference/consistency BEFORE any `resolve_predecessor_entry()` call runs — rather than folding the checks into the existing duty-B per-row loop.
  reason: Major 2's own suggested fix states the shape directly ("collect every row's `r_pred` first; require exactly one distinct value across the whole family... reject `r_pred == TASK`"); a pre-pass refuses a self-referencing or multi-predecessor family before wasting (or being misled by) a resolution attempt against a predecessor id already known to be invalid by construction, and keeps the invariant's own two refusal messages independent of duty B's per-row verdict-comparison messages.
  grounding: `.shell-team/reviews/T-1109.md` Major 2 (`bin/check-entry-mode.sh:324-347, 405-429`) and its "Suggested fix" line; live mutation probes `major2a-self-reference`/`major2b-single-predecessor`.

- decision: widen `refl_stem_re`'s colon tolerance to BOTH sides of the colon (space before AND after), not only the side the review's own reproduction demonstrated (a space before the colon).
  reason: Major 4's own suggested fix says explicitly "widen `refl_stem_re` to tolerate whitespace on both sides of the colon too" — naming both sides — and symmetry with the existing dash-to-fieldname widening (which already tolerates any count of spaces, not only the minimum reproduced case) argues against a narrower, single-sided patch that would leave a mirror-image variant (space after the colon) equally invisible to the next reviewer or the next rework round.
  grounding: `.shell-team/reviews/T-1109.md` Major 4 (`bin/check-entry-mode.sh:297-298`) and its "Suggested fix" line ("tolerate whitespace on both sides of the colon too"); live mutation probe `major4-colon-spacing` plus the added suite case for whitespace on both sides simultaneously.

- decision: implement the SKILL.md ordering-ambiguity finding via the review's FIRST suggested option (reword the pre-existing "Board transcription order" bullet's own invocation-trigger sentence to name the dispatch-reflection sub-bullets too) rather than its second option (restructure the "Cross-task dispatch reflection" bullet to be fully self-contained about timing).
  reason: the first option is a smaller, purely-additive wording change confined to one sentence, carrying no risk to **AC2**'s own byte-position assertions (the first `dispatch-reflection` occurrence still lands on the same line, still after `**Board transcription order` and before `**Frozen-intent confirmation`); the second option would require restructuring the newer bullet's own prose for no additional correctness benefit once the earlier bullet's own trigger already names both sub-bullet families.
  grounding: `.shell-team/reviews/T-1109.md`'s adversarial finding (`skills/run/SKILL.md:45-46`) and its "Suggested fix" line (both options offered, joined by "or"); `.shell-team/specs/T-1109-dispatch-reflection.md` **AC2**'s check line, re-verified green after the wording change.

- decision: (Minor finding scope) apply the collect-wide/count-mismatch-refuse discipline to `ENTRY_DISPATCH_LINES` using a narrower wide net that tolerates only extra space after the bullet dash (mirroring the reproduced shape exactly), and explicitly decline to touch `bin/close-out.sh`'s own identical, pre-existing blind spot to the same variant.
  reason: the Minor's own severity rationale scopes the fix to this script's own coverage-input reliance ("now that a fail-closed judgment depends on reading it correctly"), not to repairing `bin/close-out.sh`'s independent, pre-existing weakness to the same malformed shape — which is out of this task's Non-goals ("No change to... `bin/close-out.sh`") and would be a second, unrequested mechanism change mid-rework.
  grounding: `.shell-team/reviews/T-1109.md` Minor finding (`bin/check-entry-mode.sh:306`) — "not a novel class T-1109 introduces on its own... `close-out.sh`'s own gate would already miss the same malformed row" — and its own scoped "Suggested fix"; `.shell-team/specs/T-1109-dispatch-reflection.md` Non-goals; live mutation probe `minor6-malformed-dispatch-line`.

- decision: rework round 3 (codex review `.shell-team/reviews/T-1109.md` round 2, one new Major — a regression the round-2 Minor fix introduced). Move the ENTRY_DISPATCH_WIDE/ENTRY_DISPATCH_LINES malformed-dispatch-line count-mismatch-refuse block, and its dependent ENTRY_DISPATCH_AXES derivation, from their unconditional top-level position into the existing `if [ "$REFL_ROW_COUNT" -gt 0 ] && [ "$REFL_HAS_ALL" -eq 0 ]` gate — the same scope the coverage/duty-B logic that actually consumes them already runs under — rather than guarding only the `fail` call and leaving the two variable assignments unconditional.
  reason: `ENTRY_DISPATCH_AXES` (consumed at the coverage loop) and `ENTRY_DISPATCH_LINES` (consumed via `dispatch_value_for_axis` in duty B) are both read exclusively inside that same gate already, so moving the whole read-and-guard block there in one piece keeps the fix minimal (a pure relocation, zero new mechanism) while avoiding a `set -u` unbound-variable hazard that guarding only the `fail` line and leaving `ENTRY_DISPATCH_LINES`'s assignment at top level would not create here, but that a narrower partial move risks introducing if a future edit moves the assignment without also moving `ENTRY_DISPATCH_AXES`'s own derivation from it.
  grounding: `.shell-team/reviews/T-1109.md` round 2, "Major (new this round)" (`bin/check-entry-mode.sh:327-331`) and its "Suggested fix" line ("move the … check-mismatch check inside the existing … gate"); live reproduction of the regression fixture (zero reflection rows + a malformed `- dispatch:` line, exit 0 before the round-2 fix shipped, wrongly exit 1 under the round-2 shape, exit 0 again after this round's relocation) and the mutation probe reverting both gate conditions to `true`.
<!-- END provenance: T-1109 -->
