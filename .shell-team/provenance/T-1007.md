<!-- BEGIN provenance: T-1007 -->

- decision: `tests/playbook-promote/fixtures/lessons-base.md`'s own `## Format` fenced example documents `Bound-in` as `- **Bound-in** (required when Scope is maintainer, forbidden when Scope is loop): <repository-relative path>` — no colon immediately after `**Bound-in**` — instead of the `- **Bound-in**: <placeholder>` shape used in the other two shipped corpora (`tests/check-playbook/fixtures/valid-base.md`, `tests/gen-playbook-blocks/fixtures/root/tasks/lessons.md`).
  reason: AC10's `check:` line promotes a `--scope loop` entry (no `--bound-in`) and then asserts `test "$(grep -cF -- '- **Bound-in**: ' "$F/l.md")" -eq 0` BEFORE the second, maintainer promotion runs — a whole-file, `grep -F` substring count, not scoped to real entries. A `- **Bound-in**: <placeholder>` line inside the `## Format` fence (which the spec's Fixture contract requires all three corpora to carry) satisfies that same substring and makes the count 1, not 0, failing AC10 for a reason unrelated to the property it is testing (whether a real Bound-in bullet was emitted). The spec's Fixture contract text mandates the angle-bracket placeholder SHAPE only for `Scope`'s fenced line (so `sed` targeting `- **Scope**: loop`/`maintainer` cannot reach it) and only says the fence must "document both new fields" for `Bound-in` — it does not mandate a literal `- **Bound-in**: ` bullet shape — so rewording only this one corpus's fenced documentation, where the AC10 collision actually occurs, keeps the fence's meaning (the field is documented, its rule stated) while removing the exact colon-space substring the count would otherwise catch. The other two corpora keep the direct `- **Bound-in**: <placeholder>` shape because `tests/check-playbook/fixtures/valid-base.md`'s AC2 explicitly relies on the fence's own bullet counting toward its `-ge 2` floor ("Bound-in at least twice (the ## Format example plus real entries)"), and no AC for the `gen-playbook-blocks` fixture does a whole-file substring-absence count on `Bound-in` at all.
  grounding: .shell-team/specs/T-1007-scope-typed-ledger.md AC10 check line `test "$(grep -cF -- '- **Bound-in**: ' "$F/l.md")" -eq 0` (the pre-second-promotion assertion); AC2 check line `test "$(grep -cF -- '- **Bound-in**: ' "$B")" -ge 2`; Fixture contract intro "its Scope line uses an angle-bracket placeholder ... so a sed aimed at ... cannot reach the fenced example" (states the mandate for Scope's shape only, not Bound-in's).

- decision: `bin/check-playbook.sh`'s pre-existing header comment "Reads only. Pure bash + coreutils — no jq/yq/python." is reworded to "Reads only. Pure bash + coreutils; no external interpreter is invoked." — the substance (no runtime dependency) is unchanged; only the banned-token names are removed from the sentence.
  reason: AC17's `check:` line greps `bin/check-playbook.sh`, `bin/gen-playbook-blocks.sh` and `bin/playbook-promote.sh` for `(^|[^a-z])(jq|yq|python3?|perl|node|ruby)([^a-z]|$)` and requires zero hits, to prove no new runtime dependency was introduced by this task's edit. The pre-existing comment (present verbatim at base ref `72b6e8b`, before this task touched the file) already contained the literal substring "jq/yq/python" as a declaration of the ABSENCE of those dependencies, which is exactly the kind of prose the D plan and this repo's own `check:` conventions warn against (a `check:` that cannot distinguish a mention from a use — the same class the lessons corpus records for self-referential `grep` checks). Before this task, AC17 as a whole failed earlier at its file-count assertion (0 modified `bin/` files pre-implementation, not 3), so the comment's collision with the dependency-scan clause was never reached during the orchestrator's pre-implementation freeze-verification run — it only surfaces once the count assertion starts passing. Since the comment lives in a `bin/` script (not the frozen spec), rewording it to state the same invariant without naming the banned interpreters is a legitimate, minimal fix within the engineer's normal editing authority, and is recorded in the spec's `## Notes from engineer` section as a discovered-but-unspec'd interaction.
  grounding: `git show 72b6e8b:bin/check-playbook.sh` line 135 (pre-existing comment, unrelated to this task's diff); .shell-team/specs/T-1007-scope-typed-ledger.md AC17 check line `grep -rnE -- '(^|[^a-z])(jq|yq|python3?|perl|node|ruby)([^a-z]|$)' bin/check-playbook.sh bin/gen-playbook-blocks.sh bin/playbook-promote.sh`; this file's own `## Notes from engineer` section.

- decision: `bin/check-playbook.sh`'s `Bound-in` shape check (non-empty after trim, not absolute, not `~`-prefixed) runs whenever the `- **Bound-in**:` bullet is present at all, independent of whether the same entry's `Scope` value is itself valid (`loop`/`maintainer`) or missing/unknown.
  reason: Two reasonable designs existed — (a) only run the shape check when `Scope` is one of the two known tokens (skip it if `Scope` itself already failed its own enum check), or (b) run it unconditionally whenever the bullet is present. Design (a) risks masking a genuine shape violation (an absolute or `~`-prefixed `Bound-in` value) behind an unrelated `Scope` enum violation on the same entry, leaving a reader to fix one problem, re-run, and only then discover the second — the opposite of what a fail-closed validator that already reports every violation it finds (not just the first) should do. Design (b), which was chosen, reports both problems in the same run, consistent with how every other structural check in this file (`check_structural`, the required-field checks) already runs independent of a sibling field's own validity.
  grounding: .shell-team/specs/T-1007-scope-typed-ledger.md DP-d "the checker validates: non-empty after trimming, not beginning with `/`, not beginning with `~`, plus the structural checks (`check_structural`) every other field value gets" (states the rule, not its conditioning on Scope's own validity — an implementation gap the engineer resolved); `bin/check-playbook.sh`'s existing pattern of independent per-field checks in `finalize_entry()` (e.g. `Source`/`Rule`/`Why`/`How to apply` non-empty checks all run regardless of any other field's state).

<!-- END provenance: T-1007 -->
