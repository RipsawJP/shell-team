<!-- BEGIN provenance: T-1114 -->
- decision: Defer AC13's read-set-scoped two-arm blast-radius sweep to the coordinating-session-hosted fan-out procedure rather than running it (or a partial slice of it) inside this engineer round; the spec's `- flips:` line is left `unmeasured (sweep not yet run)` and the board's status flag stays `READY_FOR_ENG` rather than flipping to `READY_FOR_QA`.
  reason: The derived population (`bin/derive-populations.sh --label t1114-read-set`) resolved corpus-scale — 119 of this corpus's own 119 spec files, driven mainly by the `indirect` set matching 108/119 — which this task's own engineer briefing instructs reporting rather than running once the union exceeds roughly 40 members; running any slice of a corpus-scale population from inside a single engineer round would misrepresent partial coverage as the read-set-scoped inventory the spec's own `- verification-class: no-mechanism` declaration actually owes.
  grounding: .shell-team/test-recipe.md T-1111/T-1112/T-1113 entries (the same corpus-scale-derivation hazard and its coordinating-session-hosted, never sub-agent-hosted, disposition); .shell-team/specs/T-1114-scope-lock-worktree-deltas.md `## Blast radius` (embedded `t1114-read-set` derivation block, `- union: items: 119`).
<!-- END provenance: T-1114 -->
