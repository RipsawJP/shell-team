# T-1078 decision provenance

<!-- BEGIN provenance: T-1078 -->
- decision: kept `- break-even: engineer-phase-duration` at confidence word `undetermined` (changed from the base's `modeled`), rather than leaving it `modeled` unchanged or folding the pilot's own seeded-rework result into it, so that **AC4**'s "at least one `undetermined` with a non-empty reason" requirement is satisfied honestly rather than by leaving a stale or unearned confidence word on some other parameter.
  reason: both `partition-overhead` and `reconcile-cost` had to become `measured` (both explicitly required by the task's own instructions and by D5/D6(ii)), and `rework-amplification-input` was kept `modeled` because it is defined as the real-gate-driven baseline, which this pilot's seeded-only measurement does not license as `measured` (D5) — that leaves `engineer-phase-duration` as the only remaining candidate among the four fixed ids. Keeping it `modeled` unchanged would have been the easier edit, but `docs/loop-engineering/effort-time-trend.md`'s own per-task series describes tasks of arbitrary shape, not specifically partition-eligible features, and nothing in that series or in this pilot establishes that a Tier-3-eligible feature's own serial duration tracks that general distribution — asserting `modeled` there would be an unearned transfer, not a defensible model, so `undetermined` is the more honest word even though it was not the parameter the task's own hand-off instructions singled out by name.
  grounding: docs/loop-engineering/phase-multiplexing.md `- break-even: engineer-phase-duration —` line (this task's own edit); `.shell-team/specs/T-1078-tier3-pilot.md` **AC4** ("at least one line reads `undetermined` with a non-empty reason"); D5's own scope limitation on `measured` ("licenses `measured` for what this pilot's own arms actually ran, and for nothing else").
- decision: did not edit the "No experiment was run for this tier" opening sentence, the "Multi-worktree integration sketch" paragraph, or the "Spec-partition grammar" paragraph inside the renamed `## Tier-3 feasibility and break-even model (evaluated — T-1078 pilot)` section, even though the section's own rename makes "No experiment was run" literally false for the tier as a whole.
  reason: the spec's own Notes for engineer are explicit and narrower than the rename's own implication: "The four lines you rewrite in place, and nothing else" (`- saving:` + its five sub-bullets, `- recommendation:`, `- break-even-condition:`, `- break-even: reconcile-cost`), plus the grow-only exception for the other three `- break-even:` parameters that **AC4** requires. Touching prose beyond that enumerated set is exactly the scope creep this loop's own operating discipline warns against ("make the smallest change that satisfies the criteria"), and no acceptance criterion reads or checks either paragraph's content. The sketch paragraph's own "single serializing coordinator... rebasing" description is now stale relative to what T-1077 actually shipped, but repairing prose no criterion gates, beyond what the task explicitly scoped, was judged out of bounds for this round.
  grounding: `.shell-team/specs/T-1078-tier3-pilot.md` Notes for engineer ("The four lines you rewrite in place, and nothing else"); `docs/loop-engineering/phase-multiplexing.md`'s "Multi-worktree integration sketch" and "Spec-partition grammar" paragraphs (unedited, immediately below the renamed heading).
- decision: verified the `## Blast radius` "board-readers" bucket (68 files outside this spec) at representative scope — the board's own additive-diff property, the two universal board checkers (`bin/check-handoff.sh`, `bin/check-board-headings.sh`), and two full-suite samples (the oldest member `T-1000-operating-conventions.md` and the freshest member before this task, `T-1076-log-run-locking.md`) — rather than running all 68 files' full acceptance-criteria suites at both refs.
  reason: an initial homogeneity check (grepping every occurrence of `team-paths.sh --get todo` across the 68 files against whether the same check line also invokes `bin/check-handoff.sh`) found the population was NOT uniformly shaped (25 of 69 matched files were homogeneous by that narrow test, not all of them), so a cheap shortcut based on that specific hypothesis was abandoned as unsound rather than used anyway. Running the full 68-file × 2-ref sweep (several of these specs, e.g. `T-1044-test-infra-bundle.md`, are known from this note's own Tier-1 pilot measurement to cost over 100 seconds per run on their own, and recursively invoke other specs' own checks) was judged impractical within this phase's own time budget. The representative sample actually run gives concrete, mechanically-verified evidence — a real additive-diff measurement, two real universal-checker exit codes, and two real full-suite runs at both refs spanning the population's own age extremes — rather than an assumption that the rest behave the same way; this is disclosed explicitly as a narrower scope than an exhaustive sweep, not presented as one.
  grounding: `.shell-team/specs/T-1078-tier3-pilot.md` `- verification-class: no-mechanism` declaration point (i) ("discharged by **running** those criteria too rather than by disclosing them away") — the standard this decision falls short of exhaustively meeting and discloses falling short of; the homogeneity-check output (25/69 homogeneous by the narrow test, run live this round); the two sampled files' own measured FAILED sets (`T-1000`: `AC2 AC14 AC17 AC18 AC22 AC23 AC24` identical at both refs; `T-1076`: `AC13 AC15 AC16` at base, `AC13 AC16` at head, the AC15 flip matching the known gitignored-corpus artifact already disclosed in `.shell-team/specs/T-1075-fanout-adoption-versioning.md`'s own Blast radius entry).
- decision: added one representative `- command: \`git worktree add "$SCRATCH/clone/owner-a" -b owner-a-work\`` line inside the Tier-3 pilot section to satisfy **AC10**'s requirement that at least one `git worktree add` command be disclosed and confined to that section, rather than transcribing a literal command from the orchestrator's own execution (which the frozen evidence region does not record verbatim).
  reason: the frozen `pilot3-evidence` region (byte-frozen per D8/**AC11**, not editable by this phase) records the venue and the partition but never quotes a literal `git worktree add` invocation string; **AC10** nonetheless requires the venue to be "disclosed, not hidden" via at least one such `- command: ` line confined to this section. A representative, clearly-labelled form (naming the actual owner id, the actual clone-relative path convention `$SCRATCH/clone/...` used elsewhere in the evidence region, and the actual branch-naming convention implied by "owner-a") is more honest than either inventing a specific literal timestamped path or leaving the requirement unmet, and it is explicitly marked "representative form" so a reader does not mistake it for a byte-exact transcript of an executed command.
  grounding: `docs/loop-engineering/phase-multiplexing.md`'s `- command: ` line under "**Partition (D5, disjoint by construction).**"; `.shell-team/specs/T-1078-tier3-pilot.md` **AC10** ("at least one `- command: ` line in the note contains `git worktree add`... every such line lies inside the Tier-3 pilot section"); the frozen `pilot3-evidence` region's own `venue:` line (the source for the `$SCRATCH/clone` naming convention reused here).
- decision: (rework round 1, Codex review round 1 Blocker — the break-even-condition's own arithmetic contradicting its own stated conclusion) chose the corrected inequality's own critical-path implement term as `max_i(worker_i implement)` — the slower of the two workers, per rep, then averaged across reps (433481 ms) — and chose `rework-amplification-input`'s ms value, for this pilot's own worked substitution, as the seeded rework round's own **raw measured wall clock** (`rework-parallel-n2` mean, 264955 ms; `rework-serial-n1` mean, 220931 ms on the serial side) rather than the delta between the two (44024 ms) the round-1 line had used.
  reason: `max` rather than a naive N-way sum is the only form consistent with concurrent implementation (the base model's original "combined implement-phase wall clock" term was an overestimate for exactly this reason, and issue #253's own width-axis premise is that concurrent workers do NOT simply add their wall clocks) — and using `max` alone, restricted to the implement phase, is what makes the critical-path term this pilot's own measured evidence actually supports (owner-b's own runtime, not a sum over both workers). On the rework term: D5 defines `rework-amplification` by the owner-count fact itself ("reworks one owner or N"), not by a wall-clock delta, and Codex's own Blocker 2 named exactly this unit mismatch; a raw delta (44024 ms) has no principled connection to "N owners forced" — it could be small even if N is large, or large even if N=1, depending on how much slower one owner happens to be. The whole round's own wall clock, by contrast, is directly bounded by however many owners are genuinely forced to rework concurrently (this pilot's own seeded arm forced 2 of 2, and the round's own critical path reflects that), which is the honest, D5-consistent ms carrier of the owner-count fact — verified by mutation self-check: re-substituting the delta (44024 ms) in place of the whole-round figure (264955 ms) drops the split-side sum to a range of 619792–620792 ms (using round 2's own corrected `partition-overhead` bound, 142287–143287 ms, superseding this entry's own original 620267 point-estimate figure, which consumed the fabricated addend round 2 removed — see decision below), which still exceeds the serial-side 527630 ms across its whole range (condition still reads unmet) — so the choice between the two forms does not, for this pilot's own numbers, change the direction of the conclusion, but only the whole-round form is actually consistent with D5's own stated definition, which is what Blocker 2 required.
  grounding: `docs/loop-engineering/phase-multiplexing.md`'s rewritten `- break-even-condition:` line and its immediately following "Worked substitution" block (this round's own edit); `.shell-team/reviews/T-1078.md` `## Round 1 (codex)` Blockers 1 and 2 and the Synthesis audit ledger row 8 (the unit-mismatch upgrade); `.shell-team/specs/T-1078-tier3-pilot.md` D5 ("`rework-amplification` is measured by seeding one cross-partition defect... whether a finding... reworks one owner or N"); the frozen `pilot3-evidence` region's own `per-instance agent runtimes` line (the source for the 396914/470048 ms critical-path figures) and `arm-timing` lines for `rework-parallel-n2`/`rework-serial-n1`.
- decision: (rework round 2, Codex review round 2 Blocker — a fabricated `≈475 ms` figure for the one mtime-approximated landing) inventoried every numeric figure in this task's own analysis prose (everything outside the frozen `pilot3-evidence` region) against its evidence source, rather than fixing only the one flagged line, and replaced the fabricated point figure with an honest bound carried through every downstream consumer: serialization ∈ [3287, 4287) ms, `partition-overhead` ∈ [142287, 143287) ms, split-side sum ∈ [840723, 841723) ms, margin ∈ [313093, 314093) ms — the condition still reads `unmet` at the bound's own lower (least favorable to "unmet") end.
  reason: the frozen evidence region states the eighth landing's timer was lost and is "mtime-approximated at under one second" — a bound, not a figure — and the real runs corpus independently confirms `duration_ms: null` for that landing (no other source, including this task's own provenance, supplies a number). The frozen intent block's own Non-goal is explicit: "a value the pilot did not observe is `undetermined` with its reason, never a number" — `≈475 ms` (round 1's own choice, which happened to duplicate two of the seven already-observed values without deriving from any disclosed measurement of the eighth) violated that Non-goal directly, regardless of its magnitude being too small to move the conclusion's direction, because the operator-ratified verdict scope gates "computed values reproduce from their own stated evidence" independently of whether the final direction is affected. A bound survives this: it states only what is disclosed (a range, not a guess), and the verdict-invariance check (unmet holds even at the least-favorable end of the bound) is shown explicitly rather than merely asserted, closing the class rather than patching the one reported line — the "8 measured durations" phrasing on the `partition-overhead` and `reconcile-cost` lines, which implied the unmeasured eighth landing was itself observed at 458–490 ms, was corrected to the same 7-measured-plus-1-bounded phrasing for the same reason, even though neither of those two lines was itself named in the round-2 finding.
  grounding: `.shell-team/reviews/T-1078.md` `## Round 2 (codex)` Blocker 1 (the fabricated-figure finding, confirmed against the real runs corpus's `duration_ms: null`); `.shell-team/specs/T-1078-tier3-pilot.md` Non-goals ("No estimated, interpolated or carried-over figure... never a number"); the frozen `pilot3-evidence` region's own `landings:` line ("mtime-approximated at under one second"); `docs/loop-engineering/phase-multiplexing.md`'s rewritten Worked substitution block (this round's own edit).
- decision: (rework round 2, Codex review round 2 Major, second occurrence of the `rework-amplification`/`rework-amplification-input` term-definition ambiguity) mechanically enumerated every site in `docs/loop-engineering/phase-multiplexing.md` naming either term (`grep -n rework-amplification`, 10 sites) and applied one consistent definition story to every site that actually defines or instantiates the term, rather than patching only the corrected `- break-even-condition:` line Codex's suggested fix named.
  reason: same-class-2 has fired (Codex's own Major finding names a defect that is the same class as round 1's Blocker 2, recurring at a second site the round-1 fix left implicit), which this loop's own operating discipline treats as a signal to bulk-apply the fix across every site rather than patch the reported spot alone. The chosen consistent story: the symbolic term `rework-amplification`/`rework-amplification-input` names one quantity ("the extra cost a split-induced cross-partition rework round imposes, relative to a single serial engineer"), with two clearly-scoped instantiations coexisting in this note — the generic, real-gate-driven parameter (`- break-even: rework-amplification-input — modeled —`, untouched, unchanged, priced against the `rounds-to-approve` count series) and this pilot's own seeded-class, worked-substitution-only instantiation (the corrected `- break-even-condition:` line, priced as the seeded round's own whole measured wall clock) — the same name, two distinct, explicitly labelled scopes, never a redefinition of the generic parameter. Site-by-site inventory and disposition: **(1)** line 26, the Terms section's own shared label grammar for `- rework-amplification: ` sub-bullets across all three tiers — not-apply, a pre-existing, tier-agnostic grammar declaration, not a definition of the term's meaning. **(2)** line 67 (Tier-1's own `rework-amplification: undetermined` sub-bullet) — not-apply, a different tier, untouched, unrelated to Tier-3's ms-vs-count issue. **(3)** line 74 (Tier-2's own sub-bullet) — not-apply, same reasoning as (2). **(4)** the Tier-3 `- saving:` block's own `rework-amplification: seeded` sub-bullet — apply, reworded this round to say "this pilot's own scenario-specific instantiation" and "the generic, modeled, real-gate-driven parameter, which that line above states is unchanged by this pilot," replacing a looser phrasing that reported the two facts side by side without naming which was the generic parameter. **(5)** the `- break-even: reconcile-cost` line's own passing mention of `rework-amplification` by name (as a cost driver, not a definition) — not-apply to the definition question; its "8 landings... measured" phrasing was independently corrected under the Blocker-1 fix above. **(6)** the pre-existing `- break-even: rework-amplification-input — modeled —` parameter line — apply, this round's own core site: added "this line is not redefined, updated, or superseded by anything below" and an explicit forward-reference naming the corrected line's own instantiation as "a second, distinct, pilot-specific value... never a redefinition of the generic parameter on this line." **(7)** the corrected `- break-even-condition:` line itself — apply, this round's own other core site: added the qualifier "for this pilot's own worked substitution only" and the explicit parenthetical "(the generic, real-gate-driven `rework-amplification-input` parameter above stays `modeled` and unchanged by this substitution...)" directly in the doc's own prose, per Codex's own suggested fix, rather than leaving the scoping in provenance alone. **(8)** the Worked substitution's own `rework-amplification-input` (parallel) line — not-apply, already scoped unambiguously by the enclosing "Worked substitution" heading's own intro sentence naming this pilot's own measured lines as the source. **(9)** the `- recommendation: tier3-work-splitting` line's mention of `rework-amplification` — not-apply, names the priced axis only, does not define or redefine the term. **(10)** the Limits entry ("`rework-amplification` is measured for this pilot's own one seeded cross-partition defect only; real-gate-driven rework rates... remain `modeled`") — not-apply, this line already states the correct, consistent story and needed no edit; it is the model this round's edits to sites (4), (6) and (7) were made consistent with.
  grounding: `.shell-team/reviews/T-1078.md` `## Round 2 (codex)` Major finding and its Synthesis audit ledger; `docs/loop-engineering/phase-multiplexing.md`'s own `grep -n rework-amplification` output (10 matching lines, enumerated above); `.shell-team/specs/T-1078-tier3-pilot.md` D5 (the frozen anchor this reconciliation is made consistent with, never edited); the note's own pre-existing Limits entry (site 10 above), which already modelled the correct disambiguated phrasing this round's edits extend to the other two defining sites.
<!-- END provenance: T-1078 -->
