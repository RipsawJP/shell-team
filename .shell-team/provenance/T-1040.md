<!-- BEGIN provenance: T-1040 -->

- decision: spliced every `X`/`O`/`A`/`S`/`Q`/`B17` frozen block from this spec into its target file programmatically (Python `str.replace`/`str.count` on the extracted, fence-stripped block text; `awk`-based marker extraction feeding the replacement), and verified the occurrence count matched the spec's own stated expectation before writing — never retyping or hand-copying any frozen prose, and never treating extraction and splice as two independently-transcribed steps.
  reason: several blocks (`X1`/`X2`/`X3`, `O24b`, `B17`) carry apostrophes, em-dashes, backticks and (`B17`/`O24b`) two significant leading spaces that no manual retyping reliably survives, and this task's own Notes for engineer name exactly this failure class ("`B17` and `O24b` carry two leading spaces that are part of the frozen bytes"). Routing bytes straight from the frozen fence to the target line closes that class structurally; AC1–AC9 and AC12 (byte/occurrence/class-b measurements) all measured PASS on the spliced text.
  grounding: `.shell-team/specs/T-1040-frozen-repair-batch.md`, `## Notes for engineer` ("Five things to watch on the freeze pass" item 2 and 3); `bin/check-acs.sh` live runs on this spec, AC1–AC9/AC12 `PASS`.

- decision: for the three #157 targets (`T-1029`, `T-1030`, `T-1036`), appended one sentence to each repaired criterion's own prose stating what its anchor now guarantees, in addition to the check-line edit DP4 required — rather than editing only the `- check:` line.
  reason: `bin/check-refreeze-class.sh` classified all three deltas as `mechanics` (exit 0) when only the check lines were touched — a class-M repair, not the class-B repair DP5 requires for this batch (DP5 ground (i): an under-anchored anchor that works on today's input honestly fits none of the class-M trigger vocabulary, and routing part of this batch through a stretched token is exactly the label-versus-measurement divergence this task exists to remove). Adding one prose sentence per repaired criterion, describing the new anchor's own guarantee, moved every one of the three targets' deltas to class-b (exit 1), confirmed by re-running `bin/check-refreeze-class.sh` before writing any ledger record, per DP5's own instruction to run the classifier and let the record follow the machine verdict.
  grounding: DP5 ("(i) The class-M trigger vocabulary is closed... routing part of a batch through a stretched token is exactly the label-versus-measurement divergence this task exists to remove"); `bin/check-refreeze-class.sh` live runs on `T-1029`/`T-1030`/`T-1036` base-vs-current pairs, exit `0` (mechanics) before the prose addition, exit `1` (class-b) after.

- decision: the freeze-attestation `verdict=<P>P/<F>F` recorded for each of the ten targets' new (vK+1) ledger line is the number measured from a live `bin/check-acs.sh` run taken AFTER all ten board updates were written (not the number measured immediately after the spec edit, before any board update) — several targets' own criteria check a *neighbouring* target's `bin/check-intent.sh` alignment (e.g. `T-1029`'s AC9 checks `T-1027`/`T-1028`; `T-1036`'s AC10 recurses into `T-1029`/`T-1030`), and those criteria measured FAIL before the neighbour's own board entry was updated and PASS after — a transient, not a defect.
  reason: recording the pre-board-update number would have written a freeze-attestation whose verdict undercounts what the shipped state actually supports, purely as an artifact of this batch's sequencing (ten simultaneous re-freezes, versus the historical one-at-a-time convention the ledger's own past entries were written under). Re-measuring after all ten board writes landed reports the true post-repair shape and avoids a self-inflicted, momentary red being permanently recorded as if it were the settled state.
  grounding: `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` AC9 (`bin/check-intent.sh` reports aligned for `T-1027`/`T-1028`); `.shell-team/specs/T-1036-wording-batch-141-143-144.md` AC10 (recurses `bin/check-acs.sh` into `T-1029`/`T-1030`); live `bin/check-acs.sh` runs on each of the ten targets taken twice — once immediately post-edit, once after all ten board writes — with the second run's PASS/FAIL counts written into the ledger.

- decision: did not attempt to resolve, or route around, the discovery that `X1`'s frozen text (spliced into `T-1037`'s `S1` block per AC4) contains the literal substring `` `git log --merges --first-parent` ``, which collides with `T-1037`'s own pre-existing AC3 anti-optionality/no-fetch lock (its forbidden-token sweep includes the bare string `git log`) — implementing `X1` verbatim turns `T-1037`'s AC3 from PASS (confirmed at base `6fc3910`) to FAIL. Disclosed in `## Notes from engineer` and in the hand-off instead of editing either the frozen `X1` text or `T-1037`'s AC3 check line.
  reason: `X1` is frozen byte-for-byte in this spec's `### Settled decisions` (DP13) and this task's own R1 states that an argument a hedged sentence should read differently is a re-freeze proposal routed to pm-spec, not an engineer fix; R3 separately forbids adding or removing a criterion or `- check:` line in any target. No AC in this spec (AC1–AC19) actually asserts `T-1037`'s own AC3 outcome — AC18's triple check-acs verification is scoped to `T-1029`/`T-1030`/`T-1036` only (DP11) — so this finding does not block any of this spec's own criteria, but it is a genuine, newly-caused regression in an already-shipped criterion that the spec's own blast-radius design (DP11/AC17/AC20(d)) and disclosure norms (R1) exist to surface rather than silently absorb or unilaterally patch.
  grounding: `.shell-team/specs/T-1040-frozen-repair-batch.md`, `### Settled decisions` `X1` block (contains `` `git log --merges --first-parent` ``) and `## Notes for engineer` R1/R3; `.shell-team/specs/T-1037-checker-retro-precision.md` AC3's `- check:` line (forbidden-token loop including `'git log'`); live `bin/check-acs.sh` run on `T-1037-checker-retro-precision.md` showing AC3 FAIL post-repair against the blast matrix's own at-base PASS record for the same criterion.

- decision: wrote T-1017's final-state AC15 replacement to assert exactly the three invariants named in DP8 (the interventions gate lives in exactly one `bin/` file and it is `bin/close-out.sh`; `.github/workflows/` holds exactly one workflow file; that file lints `bin/close-out.sh`) and nothing more — in particular, did not carry forward the superseded criterion's separate "no new file appears under `bin/` or as a new `tests/*/run.sh`" clause as a permanent invariant, since that clause has no permanent equivalent (it is a claim about what THIS branch's diff added, which is exactly the shape DP8 says AC17 — left untouched — already owns).
  reason: DP8 states the boundary explicitly: a base red is repaired only where its ref-relative history claim stood in for a permanent invariant, and where the claim's substance is the branch's own diff, it is left exactly as it is (`T-1017`'s AC17, the named boundary case). Folding the "no new file" clause into AC15's replacement would have duplicated AC17's own, deliberately-untouched substance inside a criterion DP8 says should hold permanent facts only.
  grounding: DP8 ("`T-1017`'s AC17 is the boundary case and it does not move: its substance is the exact file set that branch touched, it has no permanent equivalent"); `.shell-team/specs/T-1017-close-out-interventions-gate.md` AC17 (unedited, confirmed byte-identical to its base blob by AC11's live run).

<!-- END provenance: T-1040 -->
