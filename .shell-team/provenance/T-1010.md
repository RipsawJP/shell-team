<!-- BEGIN provenance: T-1010 -->

- decision: Rule 4's ("## Retro inputs" ledger) region-closing condition was extended to close on a T-1010 section marker line (`^<!-- retro-section: `), not only on the next `## ` heading — rather than instead changing where the first marker is placed in every retro (e.g. leaving one full blank "spacer" heading between the ledger and the first marker+heading pair, so the existing "next `## ` closes the region" rule alone would suffice).
  reason: The frozen migration recipe (D3) places a marker immediately before the section's own H2, with nothing else inserted, and the shipped skeleton has no spacer heading between `## Retro inputs` and the first section. Verified empirically before choosing: with rule 4 unchanged, a canonical fixture with a marker placed directly after the ledger failed with a spurious "unrecognised line inside ## Retro inputs" violation on the marker line itself, because a marker is not a `## ` line and so never closed the open region. Patching rule 4 to also treat a marker line as a region boundary (mirrored exactly on the existing `## ` boundary check, same `in_s = 0` action) fixed this without touching the marker/heading placement contract or rule 4's ledger-content validation, and left `has_exact_line`'s independent heading detection and rule 2/3's own (differently-named) CR-strip untouched, so the CRLF mutation self-check in `tests/check-retro/run.sh` still isolates rule 4 exactly as documented.
  grounding: bin/check-retro.sh comment "A T-1010 section marker (\`<!-- retro-section: <id> -->\`) closes an open region"; .shell-team/specs/T-1010-operator-language-boundary.md heading "D3 — the one existing retro is migrated by pure insertion" (marker inserted immediately before the existing heading, nothing else changed); AC16 check: (`tests/check-retro/run.sh` green, which exercises the mutation self-check that depends on this boundary fix).

- decision: `templates/prompt-blocks/retro-sections.md` (the new canonical block) contains only the five bare marker lines plus the `- (none)` placeholder — no explanatory prose line — rather than including a descriptive paragraph the way an early draft did.
  reason: The registry's `contain` mode requires every non-empty line of a canonical block to appear verbatim in all of its registered consumers (`bin/check-retro.sh`, `agents/scrum-master.md`, `docs/templates/retro-template.md`). A prose paragraph explaining the marker mechanism would force byte-identical wrapped-line duplication across a bash comment, an agent prompt, and adopter-facing template prose — three genuinely different registers — for no behavior the checker needs; the marker lines and the placeholder token are the only content any consumer actually enforces or must reproduce exactly. Keeping the block to just those six lines mirrors the existing `templates/prompt-blocks/verdict-labels.md`/`flag-enum.md` shape (bare tokens, no prose) rather than `retro-inputs.md`'s single explanatory sentence, because unlike `retro-inputs.md`'s sentence (already independently a fixed one-line contract token used verbatim across four consumers before this task), no comparable single fixed sentence about markers is enforced anywhere in this task's ACs.
  grounding: templates/prompt-blocks/registry.txt comment block "contain — every non-empty line of the canonical file must appear verbatim"; .shell-team/specs/T-1010-operator-language-boundary.md AC3/AC4 check: (registry `contain` rows plus the six bare lines, no prose assertion); bin/check-prompt-sync.sh function check_contain (per-non-empty-line substring requirement).

- decision: The canonical order check (rule 2) evaluates each id's FIRST marker occurrence only (sorted by line number) when determining "out of order," rather than checking every occurrence of every id pairwise.
  reason: A duplicated marker (two occurrences of the same id) is already reported as its own, independent "duplicated section marker" violation; folding duplicate occurrences into the order check as well would either double-report the same underlying defect under two different reasons for the same root cause, or require inventing a rule for which of several same-id occurrences counts toward ordering — a distinction the spec's Input space and AC10 never draw (AC10's own dup.md fixture asserts only the exit code, not which of the two mechanisms fires). First-occurrence-only order checking is the smallest rule that satisfies AC10's two required cases (a genuine swap between two distinct ids, and a same-id duplication) without a canonical concept of a duplicate's own file-order.
  grounding: .shell-team/specs/T-1010-operator-language-boundary.md AC10 check: (`dup.md` asserted on exit code alone, `ord.md` asserted on exit code plus the literal 'out of order' substring); bin/check-retro.sh comment "canonical order, checked over each ids first occurrence, sorted by line number".

- decision: `.shell-team/reviews/T-1010.md` was NOT seeded by this hand-off (AC28's `test -r .shell-team/reviews/T-1010.md` clause is left red at this hand-off point), rather than writing a placeholder review file so AC28 reports fully green before QA/Codex ever run.
  reason: Checked this repository's own precedent before deciding: `git log --diff-filter=A -- .shell-team/reviews/T-1009.md` (and the equivalent for T-1007/T-1008) shows the reviews file is first created by the QA verification commit, never by the preceding engineer commit, in every one of the three most recent tasks. Seeding a placeholder here would be inventing a new convention unilaterally rather than following the measured one, and a placeholder review file the engineer wrote about their own work would misrepresent provenance (the file's whole purpose is to record an independent verdict). The spec's own instruction to the engineer explicitly names this exact fork ("follow the repo's own precedent and say what you found"); this decision is that finding, reported rather than resolved silently.
  grounding: git log --oneline --all --diff-filter=A -- .shell-team/reviews/T-1009.md (first introduced by commit f1c4ad0, "T-1009: QA verification — PASS, board to READY_FOR_REVIEW"); the equivalent commits 3b94202 (T-1007) and 22bc2fc (T-1008); .shell-team/specs/T-1010-operator-language-boundary.md AC28 check: (`test -r .shell-team/reviews/T-1010.md`).

<!-- END provenance: T-1010 -->
