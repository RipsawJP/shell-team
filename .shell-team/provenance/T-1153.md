<!-- BEGIN provenance: T-1153 -->

- decision: The Language rule's sentence describing how the evidence pass receives a language directive is left byte-identical to its shipped form ("add it to the `codex review` / `codex exec` prompt — Codex is a separate provider, so the prompt is the only channel"), rather than reconciling it to name the perspectives file, as `## Notes for engineer` suggests.
  reason: That sentence is the canonical `templates/prompt-blocks/language-code-reviewer.md` block, registered `contain` mode against `agents/code-reviewer.md` in `templates/prompt-blocks/registry.txt`. A first attempt to reword it broke `bash bin/check-prompt-sync.sh` (`missing required phrase from 'language-code-reviewer.md'`) because `contain` mode requires the canonical file's text to appear verbatim as a substring. The canonical block itself is outside AC11's allow-list (not `agents/code-reviewer.md`, `templates/prompt-blocks/host-dispatch.md`, `skills/run/SKILL.md`, `docs/workflow*.md`, or a `tests/codex-skeleton-hygiene/` path), so editing it would also redden AC11. No check line in this spec requires the reconciliation; it was Notes-for-engineer prose, not a frozen acceptance criterion.
  grounding: templates/prompt-blocks/registry.txt:34 (`contain language-code-reviewer.md agents/code-reviewer.md`); live run of `bash bin/check-prompt-sync.sh` against the attempted edit, which failed with the exact message quoted above; .shell-team/specs/T-1153-review-evidence-contract.md — `## Acceptance criteria` AC11's allow-list.

- decision: The evidence pass's `codex exec` block prompt is split across the flag line plus exactly two continuation lines (three non-fence content lines total under the marker), rather than two.
  reason: `tests/codex-skeleton-hygiene/run.sh`'s `midbacktick` mutation targets the content line at `marker index + 3` and refuses (`SystemExit`) when that index lands on the block's closing fence — which happens if the block has fewer than three non-fence content lines. Keeping the flag line plus a two-line prompt (`"Open the file at the absolute path <PERSPECTIVES_FILE> without changing a character of the path, \` / `and follow the instructions it contains."`) keeps `marker + 3` landing on real prompt content, matching the spec's own `## Notes for engineer` instruction to "keep at least three non-fence lines in the block."
  grounding: tests/codex-skeleton-hygiene/run.sh — the `midbacktick` branch of `mutate_fence_site()` (`target = idx + 3`, raises when `target >= end`); .shell-team/specs/T-1153-review-evidence-contract.md — `## Notes for engineer`, "The Hygiene suite" bullet.

- decision: On the Codex CLI host, the evidence pass's `<PROMPT_FILE>` embeds the perspectives file's full text directly, rather than naming `<PERSPECTIVES_FILE>` by path and asking the `claude -p` pass's `Read` tool to open it.
  reason: Whether that pass's `Read` tool, invoked under `--permission-mode dontAsk`, may open a path under `$TMPDIR` is explicitly unmeasured by this spec. Embedding the perspectives text into the already-constructed `<PROMPT_FILE>` (the same mechanism the primary/adversarial prompt templates already use for the diff scope and spec path) avoids depending on that unmeasured permission behavior, at the cost of no new invocation shape.
  grounding: .shell-team/specs/T-1153-review-evidence-contract.md — `## Assumptions`, "pm-spec addition from the symmetry audit, flagged for the orchestrator" paragraph (states this is "the engineer's call" and names the unmeasured Read-tool dependency as the reason to prefer embedding).

- decision: The new `#### Evidence ledger` section is placed in `## Output` immediately after the header fields (`- Task:` … `- Diff scope:`) and before `#### Blockers`, rather than elsewhere in the block.
  reason: AC4 and AC7 only require the section to open with the literal heading and close at the next `#### ` line, with no position pinned; placing it first among the finding sections matches its role as the gating artifact a reader checks before any severity-bucketed finding, and keeps `#### Synthesis audit ledger` and `#### Evidence ledger` from reading as duplicates of each other when adjacent.
  grounding: .shell-team/specs/T-1153-review-evidence-contract.md — AC4 ("the section it opens (up to the next `#### ` line)"), AC7 ("Inside the `## Output` block's `#### Evidence ledger` section (no separate heading)"), neither of which names a position.

- decision: The Notes-for-engineer scratch probe of `bin/check-review-input.sh` against a heading-less `BLOCKED — review not established` round was re-run live from a fresh scratch copy of `.shell-team/reviews/T-1152.md` with an appended Round 3 (fields suffixed `-r3`, no verdict heading), rather than treating the spec's own freeze-time measurement (`## Assumptions`, "Measured at the freeze") as sufficient.
  reason: `## Notes for engineer` directs the engineer to run this probe and record the result before hand-off, distinct from the freeze-time measurement, which was made by the coordinating session before implementation existed. Re-running it against the actual post-implementation shape (the pass id `evidence-r3`, matching this task's own `evidence-r<N>` convention rather than the freeze probe's generic suffix) confirms the checker's acceptance holds for the id family this task actually emits.
  grounding: .shell-team/specs/T-1153-review-evidence-contract.md — `## Notes for engineer`, "Heading-less round and `check-review-input`"; live run: `bash bin/check-review-input.sh --record <scratch copy of .shell-team/reviews/T-1152.md with an appended Round 3> --task T-1152` → exit 0.

<!-- END provenance: T-1153 -->
