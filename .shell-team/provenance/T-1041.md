<!-- BEGIN provenance: T-1041 -->

- decision: kept AC8's frozen tenth named assertion id (the one covering the malformed-Task-ID-line print-mode case) byte-for-byte in `tests/check-intent/run.sh` (assigning it to one shell variable reused at four call sites, rather than respelling the literal each time), instead of rewording the id or adding any suppression for the `bin/check-pii-shapes.sh` finding it triggers. This note deliberately does not re-transcribe the id's own text, since doing so would itself be a second occurrence of the exact shape under discussion (the affected files are the reference for the real value).
  reason: the id's exact spelling is dictated by AC8's frozen `check:` line, which this role cannot edit without a pm-spec re-freeze; the false positive (an unboundaried `sk-` + 16-or-more-token-char substring that the id's own hyphenation happens to spell out) is a known checker gap tracked as issue #178 and named out of scope by this spec's own Non-goals bullet 1, and `bin/check-pii-shapes.sh`'s own header states it carries no path allowlist or inline-allow mechanism by design (fail-closed). Renaming would not remove the collision anyway, since AC8 requires this exact literal; the only real lever available was reducing the finding's occurrence count, which the variable does (measured: 5 lines to 1 in `tests/check-intent/run.sh`).
  grounding: `.shell-team/specs/T-1041-freeze-ux.md` AC8's `check:` line (the `for id in ...` enumeration, tenth entry) and `## Non-goals` bullet 1 ("Issues #9, #21, #153 and #178 are out of scope"); `bin/check-pii-shapes.sh` header comment ("no path allowlist beyond DP-8's narrow, test-locked, other-guard-only list, and no inline allow"); live `bin/check-pii-shapes.sh --base 8049120` runs measuring 6 findings before the variable refactor and 2 after (the remaining one in this file's own diff is AC8's identical literal inside the already-frozen spec, unrelated to this refactor).

- decision: phrased the two refusal-readability additions (D4a's pace-rule sentence, D4b's malformed-record clause) as new prose appended to the existing multi-line `fail_attestation` message strings, rather than as a second `printf`/second stderr write, and chose their exact wording freely within the anchors AC9/AC11 require.
  reason: D3 requires no new stderr write site be added (every refusal path reuses the existing `die`/`fail_attestation` call), so the new sentences had to land inside the SAME string argument already being built; the spec names the required anchor substrings (`the lines= counts above are measured, not an example`; `the intent block being checked carries`) but not full sentences, leaving the surrounding wording to this role, chosen to read as a plain instruction to a human freeze-runner rather than as another grammar fragment.
  grounding: `.shell-team/specs/T-1041-freeze-ux.md` D3 ("no new stderr write site is added: every failure path reuses `fail_usage` / `fail_structural`") and D4/AC9/AC11 (the required anchor substrings, quoted verbatim); live `bin/check-acs.sh` run on the spec, AC9–AC12 `PASS`.

- decision: worded the new freeze-gate cross-join sub-bullet in `skills/run/SKILL.md` as a single self-contained sentence naming both operands and the lesson's own negation, rather than restating the fuller mutual-satisfiability procedure already documented on the preceding (unedited) `attestation` branch line.
  reason: AC18 requires the SAME line to contain `cross-join`, `disclosed`, `requirement set` and `not per-criterion decomposition alone`, and D7 forbids editing the preceding line (the trailing-space frozen literal) or introducing any of the three DP-7/DP-8-forbidden shapes (an interpreter-prefixed `bin/check-(provenance|interventions).sh` invocation, a `gate:ACn` sentinel, or the phrase "route back through loop guard"); a shorter, self-contained sentence satisfies all of AC18's substring requirements and the negative locks in one physical line without touching the byte-invariant line beside it.
  grounding: `.shell-team/specs/T-1041-freeze-ux.md` AC18's `check:` line (the four required substrings) and D7 ("the new step's own line names both operands... and must contain no interpreter-prefixed `bin/check-*.sh` invocation, no `gate:ACn` sentinel and no 'route back through loop guard' phrase"); `.shell-team/interventions/T-1036.md`'s fourth entry (the lesson sentence quoted verbatim in D7); live `tests/codex-skeleton-hygiene/run.sh` run, DP-7/DP-8 live-file locks `PASS`.

- decision: implemented mode exclusivity and the board-argument-optional-in-print-mode behaviour as two added `if`/`case` branches inside the existing single argument-parsing block (a `PRINT_HASH` integer flag checked before each existing positional assignment), rather than writing a second, parallel argument parser for print mode or refactoring the parser into a shared function.
  reason: the two-argument mode's observable contract must stay byte-identical (Non-goals), so the smallest change that adds mode exclusivity (D1, following `bin/team-paths.sh`:81's `set_mode` precedent) without touching any existing branch's behaviour was to gate the BOARD assignment and its `-f`/`-r` checks behind `[ "$PRINT_HASH" -eq 0 ]`, leaving every existing line for the two-argument mode untouched and reachable exactly as before.
  grounding: `.shell-team/specs/T-1041-freeze-ux.md` D1 ("mode flags are mutually exclusive... following `bin/team-paths.sh`:81's `set_mode` precedent") and `## Non-goals` ("The two-argument mode's observable contract does not change"); live `bash tests/errexit-safe/run.sh` and `bash tests/check-intent/run.sh` runs, both `PASS`, confirming the zero-argument and existing two-argument fixtures are unaffected.

<!-- END provenance: T-1041 -->
