# T-1093 decision provenance

<!-- BEGIN provenance: T-1093 -->
- decision: no `CHANGELOG.md`/`CHANGELOG.ja.md` entry is added for this task, even though both files are on the AC9 allow-list and the routing map's step 2 names them among the files to touch.
  reason: this repository's own history shows changelog entries are added only at release-prep time, in a dedicated `release: vX.Y.Z …` commit, never in the individual feature commit that ships the capability — confirmed by walking `git log --oneline --all -- CHANGELOG.md`, whose every hit is a `release: …` commit, and by confirming neither T-1091's nor T-1092's own commits touch `CHANGELOG.md`. The spec's own `## Notes for engineer` states the routing map merely names the files as touchable and that "whether an entry is warranted with no release expected is the ordinary repository convention and no criterion requires one either way" — and this task's board entry records "No release expected from this sprint." Adding a per-task entry now would depart from the observed convention for no stated benefit, and AC9's allow-list permits but never requires touching either file.
  grounding: `git log --oneline --all -- CHANGELOG.md` (every hit is a `release: …`/`chore: release …` commit); `git log --oneline feature/1092-specify-seam-review -- CHANGELOG.md` and `git log --oneline feature/1091-operator-authored-entry -- CHANGELOG.md` (both show only the same shared release commits, none of T-1091's or T-1092's own commits); `.shell-team/specs/T-1093-verification-ceiling.md` `## Notes for engineer`, first bullet ("whether an entry is warranted with no release expected is the ordinary repository convention and no criterion requires one either way"); `.shell-team/todo.md` T-1093 entry's `branch:` line ("No release expected from this sprint").
- decision: README.md's and README.ja.md's new bullet describes the verification-ceiling declaration in its own terms ("every spec declares…") rather than framing it as a fourth member of the `dispatch-axis` bullet family it sits beside.
  reason: the routing map and the spec's `## Notes for engineer` place this bullet "beside the dispatch-axis bullets" for proximity only — the declaration is not itself a `tech-lead` Routing Map dispatch decision (it carries no `- dispatch:` axis, no `unconditional|conditional` modality, and is never transcribed onto the board's dispatch record the way `specify`/`spec-review`/`implement`/`verify` are). The three sibling bullets it sits beside each open with a variant of "as a dispatch axis" precisely because that is what they are; writing the new bullet the same way would misstate the mechanism to a reader of the adopter-facing README, so it is worded to state what the capability does without claiming dispatch-axis status.
  grounding: `.shell-team/runs/t1093-routing-map.md` (introduces no `dispatch:` axis for this task — the four axes recorded are `implement`, `verify`, `specify`, `spec-review`, none of them `verification-ceiling`); `.shell-team/specs/T-1093-verification-ceiling.md` Non-goals ("No new status flag, no new seam and no new phase") and Goal (the declaration lives in the spec's own declaration region, not in the board's `- dispatch:` grammar `bin/close-out.sh` validates); `README.md` lines carrying `- **Spec authorship as a dispatch axis**` / `- **Spec review at the Specify seam as a dispatch axis**` (the sibling bullets' own self-description, confirmed by direct read before drafting the new bullet).
- decision: the new `- Verification ceiling: …` field in `agents/qa-verifier.md`'s PASS-block template is placed between `- Acceptance criteria:` and `- Edge cases tried:`, rather than appended as the last field before the closing fence.
  reason: the spec's own criteria (AC5) fix only that the field sits somewhere in the block between `### QA verdict: PASS` and `**Fail:**`, and fix no ordinal position among the five fields — so the exact placement among siblings was a free choice. Placed after `- Acceptance criteria:` because the ceiling declaration is a property *of* how the acceptance criteria were verified (what level they were verified at) and reads most naturally immediately following them, before the unrelated `- Edge cases tried:` / `- Risk notes for reviewer:` fields that describe QA's own exploratory work rather than the criteria's verification level.
  grounding: `.shell-team/specs/T-1093-verification-ceiling.md` **AC5**'s own `- check:` line (extracts the whole PASS block via `awk` between the `### QA verdict: PASS` and `**Fail:**` lines and asserts only that exactly one `^- Verification ceiling: .` line exists somewhere inside it — no ordinal-position assertion); `agents/qa-verifier.md`'s PASS-block template as edited (the five fields' final order, verified live: `awk` extraction of the block shows `- Verification ceiling:` as the fourth of six non-blank lines, immediately after `- Acceptance criteria:`).
<!-- END provenance: T-1093 -->
