<!-- BEGIN provenance: T-1032 -->

- decision: each of the four corrected `- site:` rows in `docs/loop-engineering/arith-base10-audit.md` **points at** the new `## Check-line ceiling measurement (T-1032)` section (`see ## Check-line ceiling measurement (T-1032)`) for the measuring command, rather than repeating the full command inline in every row.
  reason: the spec's own `## Open questions` leaves this choice to the engineer explicitly, naming both outcomes as satisfying AC3 and AC4: carry the full measuring command inline in each `- site:` row (self-contained, but each row is already roughly 400 characters before this correction and the command itself is another ~140 characters), or reference the new section once. I chose the reference form because the four rows are read together as a block (they already share identical wording per D2/AC3's one-pass requirement) and a reader who wants the command has one canonical place to find it — `## Check-line ceiling measurement (T-1032)` — rather than four copies that could drift from each other on a future edit. AC3 and AC4 both pass under this choice (verified live).
  grounding: `.shell-team/specs/T-1032-audit-prose-accuracy.md`, `## Open questions` ("whether the arithmetic audit's four `- site:` rows should carry the full measuring command inline... or reference the new `## Check-line ceiling measurement (T-1032)` section for it... Both outcomes satisfy AC3 and AC4"); `docs/loop-engineering/arith-base10-audit.md`'s four corrected `- site:` rows and its `## Check-line ceiling measurement (T-1032)` section (the shipped choice).

- decision: D3's `- measurement:` line in `docs/loop-engineering/check-line-mktemp-guard-audit.md` carries a literal, independently re-run command (`for n in 270 283 291 298; do sed -n "${n}p" ... | grep -o 'mktemp' | wc -l; done`) rather than citing the spec's own relayed "2/4/n/4" figure.
  reason: the spec's `## Summarized sources` states T-1005's four invocation counts as a mix of pm-spec's own direct reads (`:270`, `:283`, `:298`) and one relayed count (`:291`, "read as part of the same run"). Two options existed: transcribe that mixed-provenance figure into the audit's new measurement line, or re-derive all four counts myself against the audit's own base ref (`1e31600`) with one literal command and use the result. I chose the latter — this repository's own D1 discipline (measure at the ref a document accounts for, never trust prose) applies just as much to a number this task is *adding* as to the number it corrects, and a self-re-derived measurement is strictly stronger evidence than repeating a partially-relayed one. The re-run confirmed 2/4/4/4, matching the relayed figure exactly (no discrepancy to disclose).
  grounding: `.shell-team/specs/T-1032-audit-prose-accuracy.md` D1 ("measured with `git show <ref>:<path>`, never the working tree alone, and never from this spec's prose") and `## Summarized sources` (the T-1005 invocation counts, "`:291` read as part of the same run" marking it as the one relayed figure); live command run against `git show 1e31600:.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`, output `2 4 4 4` (this session).

<!-- END provenance: T-1032 -->

## AC15 disclosures

### (a) CI wiring

`.github/workflows/check-handoff.yml` was read in full. No step names or reads
anything under `docs/loop-engineering/` — a repository-wide grep for the
literal string `loop-engineering` inside the workflow file returns zero
matches. **Confirmed, not merely inherited**: the two audit documents this
task corrects (`docs/loop-engineering/arith-base10-audit.md`,
`docs/loop-engineering/check-line-mktemp-guard-audit.md`) have **no CI
backstop at all** — their correctness rests entirely on this spec's own
`check:` lines (AC1–AC14) and on human/cross-provider review, not on any CI
gate. `bin/check-intent.sh`, the one live-code surface this task also
touches, **is** CI-wired, at two steps: the pinned `shellcheck` list
(`.github/workflows/check-handoff.yml:29`, which names
`bin/check-intent.sh tests/check-intent/run.sh`) and the dedicated
`Run check-intent fixture suite` step (`:188-189`, `bash tests/check-intent/run.sh`).

### (b) Mutation self-check

Five mutations, each applied on the working tree, measured, then restored and
re-confirmed green before the next (no mutation left uncommitted; `git status`
clean after each restore and at the end of this pass):

1. **Blank line inserted into `bin/check-intent.sh` at line 315.** AC5 went
   red on both clauses at once: the base/live line-count comparison
   (`726` vs `727`) and the `git diff --numstat` added/removed-column
   equality (`8` added vs `7` removed). Restored; `wc -l bin/check-intent.sh`
   back to `726`, AC5 green again.
2. **One of the four corrected `- site:` rows in the arithmetic audit
   reverted to the old wording** (`(this task's own spec, the largest
   measured, carries 24)` in place of the corrected parenthetical). AC3 went
   red on the exact-4 count alone: A1 (`35 (T-1011)`) dropped from 4 to 3 in
   the audit file, while `bin/check-intent.sh` (1) and the provenance file
   (2) stayed at their corrected counts — confirming the exact-count clause
   really does catch a partial bulk-apply. Restored; A1 count back to 4.
3. **The provenance `grounding:` line's correction reverted**
   (`.shell-team/provenance/T-1021.md`, restoring the original "the largest
   measured intent block" wording). AC3's provenance A1 count went from 2 to
   1 (red). Restored; count back to 2.
4. **Detector blind-spot probe: reworded the excluded-occurrence row that
   carries 7 of AC5's 47 cited addresses**
   (`bin/check-intent.sh:285,286,313,620,621,623,634`). Deleting all 7 citations
   from that row drops AC5's extracted count to 40 — below the ≥45 floor, so
   AC5 (correctly) goes red in that case. But dropping only **2** of the 7
   (`623`, `634`) leaves the extracted count at exactly **45** — still ≥45 —
   so AC5 stays green even though two cited addresses silently stopped being
   checked for drift. **This is the blind spot to disclose**: AC5's
   anti-vacuity floor (`≥45`, chosen because 47 were present at base) has a
   2-address margin. A future edit that silently drops up to 2 citations from
   the audit document (rather than changing the underlying code at those
   addresses) evades AC5's byte-identity check entirely, because an address
   never extracted is never compared. AC5 cannot, by construction, detect the
   removal of a citation it was never told to look at — only the ≥45 floor
   stands between that and full silence, and the floor's margin here is 2.
   Restored; `diff` against the pre-mutation file confirmed byte-identical.

### (c) Both measurement refs, side by side

- measuring command: `for s in .shell-team/specs/*.md; do n=$(awk '/BEGIN intent-block/{inb=1;next} /END intent-block/{inb=0} inb' "$s" | grep -c '^[[:space:]]*- check:'); printf '%s %s\n' "$n" "$s"; done | sort -rn | head -1`
- at `a865ec0` (26 spec files in the population): `35 .shell-team/specs/T-1011-telemetry-event-rows.md`
- at `7deb02a` (this task's merge point, 33 spec files in the population): `35 .shell-team/specs/T-1011-telemetry-event-rows.md`
- **the two refs agree**: the true maximum is 35, held by the same file, at
  both refs. No premise contradiction — D2's anchors (`35 (T-1011)`, `285x`)
  are confirmed correct rather than reopened.

### (d) The pre-existing red (T-1024's spec AC2, before and after)

**Before this task's edits**, measured against `7deb02a` (via a per-file
`git show 7deb02a:<spec> | grep ...` re-derivation, since AC2's own `check:`
line assumes a checked-out working tree and this task's own edits already
occupy this one): declared pair in
`docs/loop-engineering/check-line-mktemp-guard-audit.md` = **214 / 26**; live
pair re-derived from every spec file at `7deb02a` = **254 / 33**. Declared ≠
live, so AC2 was already red before this task touched anything — exactly the
Non-goals section's own disclosed premise.

**After this task's edits** (current working tree, all three commits
applied): declared pair (unchanged, per D5/Non-goals — this task does not
re-measure T-1024's population) = **214 / 26**, still; live pair re-derived
now = **260 / 34**. Declared ≠ live, still red — **the same kind of failure**
(a static declared pair against a population that keeps growing as later
specs land), not a new failure this task introduced. The +6 population / +1
file delta from 254/33 → 260/34 is `.shell-team/specs/T-1032-audit-prose-accuracy.md`
itself joining the population (its own `mktemp`-bearing `check:` lines, AC5/
AC9/AC13/AC14) — disclosed in the spec's Non-goals as an expected consequence,
not fixed here.

### (e) Wall-clock

- **AC12** (`shellcheck bin/check-intent.sh && bash tests/check-intent/run.sh
  && bash bin/check-handoff.sh .shell-team/todo.md && bash bin/check-handoff.sh
  templates/todo-template.md && bash bin/check-board-headings.sh
  .shell-team/todo.md --base 7deb02a`): measured via `check-acs.sh`'s own run,
  well under the 120s default; the `tests/check-intent/run.sh` component
  alone measured **~6.07s wall-clock** (`2.68s user 2.31s system 82% cpu
  6.069 total`) on this host.
- **AC11** (seven `bin/check-intent.sh` runs, one per stacked-neighbour spec,
  against the live board — `.shell-team/todo.md`, 882280 bytes / ~862KB at
  measurement time): **~4.37s wall-clock** for all seven runs together
  (`4.10s user 0.25s system 99% cpu 4.373 total`), all seven reporting
  `aligned`.

Neither approached the 120s default; no `CHECK_ACS_TIMEOUT` elevation was
needed for this spec's run. Recorded so a later task can lower the default
from data, per the spec's own instruction.
