# T-1072 decision provenance

Prose context for the triples below (outside the marker region, not part of
the checker's schema): this task added the `instance` span-only field to
`bin/log-run.sh`/`bin/check-run.sh`, settled the span-only key count as a
multi-site invariant, extended both suites, documented the gate-span and
rehydration conventions in `skills/run/SKILL.md`, wrote the dogfooded note at
`docs/loop-engineering/telemetry-span-discriminator.md`, and mirrored the
adopter-facing README clause. This file records only the non-trivial
decisions — where at least two reasonable options existed and the choice
affects the contract, an interface, observable behavior, or the frozen
intent block.

<!-- BEGIN provenance: T-1072 -->
- decision: refused the bare `-` value for `--instance` implicitly, by reusing `BINDING_TOKEN_RE` (`^[a-z][a-z0-9-]*$`) verbatim with no special-case branch — rather than adding an explicit `if [[ "$INSTANCE" == "-" ]]` guard mirroring `--effort`'s own explicit `-`-to-null branch.
  reason: `BINDING_TOKEN_RE` already refuses `-` on its own (the regex's own leading-character anchor requires `[a-z]` first, which `-` fails), so an explicit branch would be dead code duplicating a refusal the shared regex already delivers; the frozen intent block itself states the asymmetry is deliberate ("the bare `-` is refused... it fails the same leading-letter requirement") rather than prescribing a specific implementation shape, so reusing the regex as-is — one identifier grammar, not two, and no second special-case branch — is the smaller diff that still satisfies AC3's refusal requirement.
  grounding: bin/log-run.sh (the "T-1072: the per-instance discriminator (nullable)" validation block, immediately after the `EFFORT_OUT` handling); .shell-team/specs/T-1072-telemetry-span-discriminator.md's Goal section ("The bare `-` is ALSO refused here, unlike --effort... admitting a second spelling of 'no instance' beside plain omission is exactly the ambiguity that exemption exists to prevent") and AC3's `- inst`/`-` refusal check.
- decision: wrote each README's new telemetry clause with the flag name `--instance` and the literal `per-instance` appearing exactly once, rather than repeating `--instance` in a second illustrative example within the same bullet.
  reason: AC13 requires the two READMEs' `--instance` occurrence counts to be equal without either count being written down anywhere as a number; keeping each file's mention to exactly one occurrence is the simplest way to make that equality hold by construction (one clause, translated once) rather than by coincidence, and avoids a future EN-only elaboration silently breaking the mirror invariant the next time either file is touched.
  grounding: README.md and README.ja.md (the `- **Telemetry**` / `- **テレメトリ**` outer-loop bullet, each carrying one new sentence); .shell-team/specs/T-1072-telemetry-span-discriminator.md AC13 ("the two files' occurrence counts of `--instance` are **equal** — a mirror invariant, so a clause added to one language and forgotten in the other is caught without either count being written down").
- decision: placed the two new convention paragraphs (gate-span cost, rehydration checkpoint) in `skills/run/SKILL.md` immediately after the existing event-row bullet list and before the "Between phases..." sentence, rather than at the end of the Telemetry section after the existing "Telemetry binding (T-1058)" paragraph.
  reason: both new conventions extend the event-row emission guidance immediately above them (gate cost is emitted "alongside" the `--event gate`/`--event human` row the bullet list just described, and the rehydration checkpoint is a variant of the same `--span` call pattern), so placing them adjacent to that guidance keeps the reading order topic-contiguous; the T-1058 binding paragraph is about resolved-executor binding, a different subject, and interleaving the new material before it would separate the event-row guidance from its own immediate extension.
  grounding: skills/run/SKILL.md (the "Gate and orchestration cost as a span, not a relaxed event row (T-1072)" and "Substrate-reading checkpoint (T-1072)" paragraphs, between the `--event release` bullet and "Between phases, give the user a one-line status update..."); .shell-team/specs/T-1072-telemetry-span-discriminator.md AC10/AC11 (naming `skills/run/SKILL.md`'s telemetry section as the required location without pinning an exact line position).
<!-- END provenance: T-1072 -->
