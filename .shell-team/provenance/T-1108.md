<!-- BEGIN provenance: T-1108 -->
- decision: measure the symlink-vs-untracked question with `core.excludesfile=/dev/null` pinned in the scratch repo, rather than reporting the raw result under this contributor's own personal global excludes.
  reason: this contributor's own `~/.gitignore_global` carries a blanket `.shell-team/` rule, unrelated to the repository's own `.shell-team/.gitignore:4` `runs/` (trailing-slash) rule under test. An unpinned measurement would have reported the whole `.shell-team` directory as ignored for a reason that has nothing to do with the mechanism this spec's Goal and `## Notes for engineer` first-action instruction actually asks to be measured, would not reproduce on another contributor's machine or in CI (which carries no such global excludes), and would misattribute the observed behaviour to the wrong rule in the shipped `.shell-team/test-recipe.md` record. Pinning `/dev/null` isolates the measurement to exactly the mechanism the repository itself ships.
  grounding: `.shell-team/test-recipe.md`'s pre-existing "Environment quirks" bullet ("Assertions about ignore behavior must pin the excludes file. `git check-ignore` reads the operator's global `core.excludesFile` ... Pin it with `git -c core.excludesFile=…`"); this task's own scratch measurement, first unpinned (`.shell-team` reported ignored by this contributor's own personal global excludes file) and then pinned (`.shell-team/runs` reported `?? .shell-team/runs` by `git status --porcelain` and exit `1`/no-match by `git check-ignore -v`).

- decision: on discovering that acceptance criterion **AC4**'s frozen `- check:` line contains an off-by-one column constant (`index($0,p)==6` where the universal checkbox line format `- [ ] **ACn** …` places the `**ACn**` marker's first character at column 7, never 6) that makes the criterion's extraction step find zero check lines for all five population members unconditionally — independent of whether the staging step exists — escalate this as a stop-and-report finding in the hand-off rather than attempting a workaround or silently proceeding to `READY_FOR_QA` unqualified.
  reason: this task's own hand-off contract states "Broken/contradictory frozen check line → stop and report," and the intent-block markers (this spec's own `<!-- BEGIN intent-block: T-1108 -->` … `<!-- END intent-block: T-1108 -->` region, which contains **AC4**) forbid the engineer from editing frozen content by even one byte. No compliant workaround exists within this task's own Non-goals: reformatting the five referenced specs' checkbox lines would violate "No merged criterion is repaired," and adding new `bin/` logic to bridge the gap would violate "No `bin/` script is added or edited." Rather than leave the finding as an unverified guess, this role independently re-ran **AC4**'s own staging-and-comparison logic in a disposable scratch copy with the single constant corrected (`6` → `7`) and confirmed it passes cleanly (`n=5 diffw=5 rc=0`, `m=5 same=5`) — evidence that the staging design and this task's prose changes are sound, and that the blocker is exactly the one off-by-one constant, not a deeper design problem.
  grounding: `.shell-team/specs/T-1108-sweep-corpus-isolation.md` **AC4**'s check line (`index($0,p)==6 && substr($0,1,3)=="- ["`); the universal checkbox format measured directly at `.shell-team/specs/T-1066-effort-time-telemetry.md:91` (`**AC5**` begins at column 7 via `awk 'NR==91{print index($0,"**AC5**")}'`) and at this spec's own `.shell-team/specs/T-1108-sweep-corpus-isolation.md:62` (`**AC1**` likewise at column 7); this spec's Non-goals ("No merged criterion is repaired", "No `bin/` script is added or edited, and no `tests/` suite is added or edited"); the engineer role's own hand-off contract line "Broken/contradictory frozen check line → stop and report."

- decision: on discovering, via a live run of **AC13**'s own read-set sweep, that `.shell-team/specs/T-1065-task-class-verification-pricing.md` **AC2** turns `base: PASS` → `head: FAIL` because this task's additive edit to `agents/pm-spec.md`'s bullet (**AC1**) breaks T-1053's own frozen whole-physical-line quote (`BLAST`) of that exact bullet, disclose the finding in the spec's own `## Blast radius` table (correcting the unpredicted row) rather than reformat the bullet to preserve T-1065's byte-pin, and separately confirm T-1053's own **AC3**/**AC5** are pre-existing-red (unrelated to this task) rather than a second new regression.
  reason: no compliant implementation of frozen **AC1** could have avoided this — **AC1** requires the corpus-staging step to land inside that exact one physical line, and **AC3** freezes the file's total line count, so the bullet's bytes necessarily stop matching T-1053's byte-locked excerpt the moment **AC1** is implemented at all. This task's own Non-goals state "No merged criterion is repaired... repairing another task's frozen criterion is a class-B re-freeze of someone else's intent," so disclosure is the only compliant response, and **AC14**(c) specifically requires "any measured verdict differing from this spec's prediction corrected in the table... with the correction stated" — this task's own authoring-time Blast-radius table did not predict this row, so the correction belongs in the table itself, not only in prose. Distinguishing T-1053 AC3/AC5 (already broken independent of this task, since their own check resolves its base directly against `develop`, which today already sits well past T-1053's own merge) from the genuinely new T-1065 AC2 regression matters because conflating the two would misstate which failures this task caused.
  grounding: `.shell-team/specs/T-1065-task-class-verification-pricing.md` **AC2**'s check line (asserts `BLAST` occurs exactly once as a whole line in `agents/pm-spec.md` at both the base blob and `HEAD`); `.shell-team/specs/T-1053-retro-mechanization.md`'s `<!-- frozen-begin: BLAST -->`/`<!-- frozen-end: BLAST -->` block (lines 63-67) and its own **AC3**/**AC5** check lines; this task's own live measurement (`base=0 live=1` for T-1065 AC2 when run at $B=merge-base(chore/lesson-promotion-2026-08-13, HEAD); T-1053 AC3 measured failing identically with or without this task's changes present, using $B=merge-base(develop, HEAD) alone); `git grep -rl 'frozen-begin: BLAST\|frozen-begin: TIER' .shell-team/specs/*.md` → exactly these two files, confirming no third instance of this whole-line-pin mechanism against the same bullet; this spec's own Non-goals ("No merged criterion is repaired") and **AC14**(c)'s correction requirement.
<!-- END provenance: T-1108 -->
