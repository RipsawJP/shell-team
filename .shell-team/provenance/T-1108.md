<!-- BEGIN provenance: T-1108 -->
- decision: measure the symlink-vs-untracked question with `core.excludesfile=/dev/null` pinned in the scratch repo, rather than reporting the raw result under this contributor's own personal global excludes.
  reason: this contributor's own `~/.gitignore_global` carries a blanket `.shell-team/` rule, unrelated to the repository's own `.shell-team/.gitignore:4` `runs/` (trailing-slash) rule under test. An unpinned measurement would have reported the whole `.shell-team` directory as ignored for a reason that has nothing to do with the mechanism this spec's Goal and `## Notes for engineer` first-action instruction actually asks to be measured, would not reproduce on another contributor's machine or in CI (which carries no such global excludes), and would misattribute the observed behaviour to the wrong rule in the shipped `.shell-team/test-recipe.md` record. Pinning `/dev/null` isolates the measurement to exactly the mechanism the repository itself ships.
  grounding: `.shell-team/test-recipe.md`'s pre-existing "Environment quirks" bullet ("Assertions about ignore behavior must pin the excludes file. `git check-ignore` reads the operator's global `core.excludesFile` ... Pin it with `git -c core.excludesFile=…`"); this task's own scratch measurement, first unpinned (`.shell-team` reported ignored by this contributor's own personal global excludes file) and then pinned (`.shell-team/runs` reported `?? .shell-team/runs` by `git status --porcelain` and exit `1`/no-match by `git check-ignore -v`).

- decision: on discovering that acceptance criterion **AC4**'s frozen `- check:` line contains an off-by-one column constant (`index($0,p)==6` where the universal checkbox line format `- [ ] **ACn** …` places the `**ACn**` marker's first character at column 7, never 6) that makes the criterion's extraction step find zero check lines for all five population members unconditionally — independent of whether the staging step exists — escalate this as a stop-and-report finding in the hand-off rather than attempting a workaround or silently proceeding to `READY_FOR_QA` unqualified.
  reason: this task's own hand-off contract states "Broken/contradictory frozen check line → stop and report," and the intent-block markers (this spec's own `<!-- BEGIN intent-block: T-1108 -->` … `<!-- END intent-block: T-1108 -->` region, which contains **AC4**) forbid the engineer from editing frozen content by even one byte. No compliant workaround exists within this task's own Non-goals: reformatting the five referenced specs' checkbox lines would violate "No merged criterion is repaired," and adding new `bin/` logic to bridge the gap would violate "No `bin/` script is added or edited." Rather than leave the finding as an unverified guess, this role independently re-ran **AC4**'s own staging-and-comparison logic in a disposable scratch copy with the single constant corrected (`6` → `7`) and confirmed it passes cleanly (`n=5 diffw=5 rc=0`, `m=5 same=5`) — evidence that the staging design and this task's prose changes are sound, and that the blocker is exactly the one off-by-one constant, not a deeper design problem.
  grounding: `.shell-team/specs/T-1108-sweep-corpus-isolation.md` **AC4**'s check line (`index($0,p)==6 && substr($0,1,3)=="- ["`); the universal checkbox format measured directly at `.shell-team/specs/T-1066-effort-time-telemetry.md:91` (`**AC5**` begins at column 7 via `awk 'NR==91{print index($0,"**AC5**")}'`) and at this spec's own `.shell-team/specs/T-1108-sweep-corpus-isolation.md:62` (`**AC1**` likewise at column 7); this spec's Non-goals ("No merged criterion is repaired", "No `bin/` script is added or edited, and no `tests/` suite is added or edited"); the engineer role's own hand-off contract line "Broken/contradictory frozen check line → stop and report."

- decision: on discovering, via a live run of **AC13**'s own read-set sweep, that `.shell-team/specs/T-1065-task-class-verification-pricing.md` **AC2** turns `base: PASS` → `head: FAIL` because this task's additive edit to `agents/pm-spec.md`'s bullet (**AC1**) breaks T-1053's own frozen whole-physical-line quote (`BLAST`) of that exact bullet, disclose the finding in the spec's own `## Blast radius` table (correcting the unpredicted row) rather than reformat the bullet to preserve T-1065's byte-pin, and separately confirm T-1053's own **AC3**/**AC5** are pre-existing-red (unrelated to this task) rather than a second new regression.
  reason: no compliant implementation of frozen **AC1** could have avoided this — **AC1** requires the corpus-staging step to land inside that exact one physical line, and **AC3** freezes the file's total line count, so the bullet's bytes necessarily stop matching T-1053's byte-locked excerpt the moment **AC1** is implemented at all. This task's own Non-goals state "No merged criterion is repaired... repairing another task's frozen criterion is a class-B re-freeze of someone else's intent," so disclosure is the only compliant response, and **AC14**(c) specifically requires "any measured verdict differing from this spec's prediction corrected in the table... with the correction stated" — this task's own authoring-time Blast-radius table did not predict this row, so the correction belongs in the table itself, not only in prose. Distinguishing T-1053 AC3/AC5 (already broken independent of this task, since their own check resolves its base directly against `develop`, which today already sits well past T-1053's own merge) from the genuinely new T-1065 AC2 regression matters because conflating the two would misstate which failures this task caused.
  grounding: `.shell-team/specs/T-1065-task-class-verification-pricing.md` **AC2**'s check line (asserts `BLAST` occurs exactly once as a whole line in `agents/pm-spec.md` at both the base blob and `HEAD`); `.shell-team/specs/T-1053-retro-mechanization.md`'s `<!-- frozen-begin: BLAST -->`/`<!-- frozen-end: BLAST -->` block (lines 63-67) and its own **AC3**/**AC5** check lines; this task's own live measurement (`base=0 live=1` for T-1065 AC2 when run at $B=merge-base(chore/lesson-promotion-2026-08-13, HEAD); T-1053 AC3 measured failing identically with or without this task's changes present, using $B=merge-base(develop, HEAD) alone); `git grep -rl 'frozen-begin: BLAST\|frozen-begin: TIER' .shell-team/specs/*.md` → exactly these two files, confirming no third instance of this whole-line-pin mechanism against the same bullet; this spec's own Non-goals ("No merged criterion is repaired") and **AC14**(c)'s correction requirement.

- decision: rework round 2 (codex review `.shell-team/reviews/T-1108.md`, Major rows 2-3 — the prose half; the check-line half of Majors 1-2 was already repaired by pm-spec's v2→v3 re-freeze). Reword `agents/pm-spec.md:86`'s staging clause so its skip condition names "neither the base ref nor HEAD" instead of "the base worktree already tracks", and add a narrowing clause stating the staging stabilizes only the base arm's read, rather than reusing or restating a phrase like "both arms read identical frozen bytes" anywhere in the norm text — both edits confined to the same single physical bullet line **AC3** freezes the length of.
  reason: Major 2 (mixed tracked/untracked-at-HEAD-only injection) and Major 3 (HEAD arm not frozen to the same snapshot) were raised against the shipped adopter-facing norm's own wording, not only against the check-line instrument; the check-line repair (v3) already fixed the *behaviour* (AC4/AC5's either-ref filter), but the norm text engineers read as the prescription still described the old, narrower base-worktree-only skip and made no explicit disclosure that only the base arm is staged. Leaving the prose as-is would have shipped an adopter-facing description that undersold the mixed-tracked-member fix already delivered and oversold (by omission) an isolation guarantee that is not delivered for a future content-sensitive criterion. AC1's seven required literals and AC3's single-physical-line, unsplit-bullet constraint bounded the edit to a wording change inside the existing sentence rather than a restructure; verified both by running AC1/AC2/AC3 standalone after the edit (all three `rc=0`).
  grounding: `.shell-team/reviews/T-1108.md` `#### Major` rows 2 and 3, and their `Suggested fix` sub-bullets (row 2: "filter the copy set by 'tracked at HEAD'... in addition to preserving members already tracked at the base ref"; row 3: "narrow the Goal's wording to 'the base arm's read no longer depends on a moving target'... which is what is actually delivered" — the exact phrase reused verbatim in the new clause); `.shell-team/specs/T-1108-sweep-corpus-isolation.md` **AC1**'s seven required literals and **AC3**'s single-physical-line/byte-identical-elsewhere assertion; the repaired **AC4**/**AC5** check lines' own either-ref skip logic (`git -C "$W" ls-files --error-unmatch -- "$D/$g"` OR `git ls-files --error-unmatch -- "$RD/$g"` OR `git cat-file -e "HEAD:$RD/$g"`), read directly from the frozen spec to confirm the prose now describes what the check lines already do.

- decision: rewrite `.shell-team/test-recipe.md`'s T-1108 "Staging command shape settled on" illustration to match the repaired **AC4**/**AC5** check lines — resolver-based destination (`D=$( cd "$W" && bash bin/team-paths.sh --get runs )`, fail-closed on empty/absolute), the either-ref skip filter, and a NUL-delimited temp-file-list copy loop (read via `< "$list"` redirect) instead of `find ... | while ...` — and append a short "Rework note" sub-bullet naming the two things the first draft got wrong, rather than silently overwriting the illustration with no trace of the earlier defective shape.
  reason: the reviewer's Major 1 and Major 2 findings were traced specifically against this recipe entry's own illustration (`.shell-team/test-recipe.md:2222-2228` in the pre-rework text), not only against the check lines — an engineer reading only the recipe (the artifact `test-recipe.md`'s own house rule says to read first) would still copy the hardcoded-path, base-worktree-only-skip shape into a fresh implementation even after the check lines were repaired. The reviewer's disposition additionally asked that Minor row 6 (silent per-member copy failure not propagated to `rc`, a pipe-subshell scoping defect) be "fixed alongside whichever round addresses the two Major findings on the same lines" — the redirect-based file-list form is the same mechanism the repaired check lines themselves already use to get this property, so the recipe's illustration is brought into agreement with the check lines' own shape rather than inventing a second fix. The "Rework note" sub-bullet is added because this recipe's own stated purpose is to prevent re-deriving a wrong idiom from scratch — a silent rewrite with no memory of the wrong shape does not protect the next reader from reaching for it again from an old habit or a stale local copy.
  grounding: `.shell-team/reviews/T-1108.md` `#### Major` row 1's `Suggested fix` ("resolve the destination the same way the source is resolved... and use `$D` for `mkdir -p`, the copy loop, and every assertion"), row 2's `Suggested fix` ("filter the copy set by 'tracked at HEAD'... in addition to preserving members already tracked at the base ref"), and `#### Fast-follow disposition` row 1 ("worth fixing alongside whichever round addresses the two Major findings on the same lines"); the repaired **AC4** check line's own destination-resolution, either-ref-filter and NUL-delimited-file-list-via-redirect shape, read directly from `.shell-team/specs/T-1108-sweep-corpus-isolation.md` (post v2→v3 re-freeze) rather than reproduced from memory; `## Assumptions` row **A-11**'s three numbered repairs, which this recipe rewrite mirrors in prose form.

- decision: leave `docs/adopting.md` and `docs/adopting.ja.md` unedited in this round — neither restates the per-member skip condition or an isolation/both-arms-frozen claim that Majors 2/3 concern.
  reason: the task's own instruction required checking whether either adopter-facing doc restated the skip condition or the both-arms claim before deciding to touch them, rather than assuming either outcome. Both files' existing sentence ("A two-arm sweep additionally stages a single snapshot of the gitignored `.shell-team/runs` corpus into its base arm before either arm runs (once per sweep) — without that staging step, a criterion reading the corpus reports a base-arm FAIL that the diff never caused.") and its Japanese equivalent describe only that a snapshot is staged into the base arm, with no mechanics of the skip filter and no claim that both arms read frozen/identical bytes — so there is nothing in either file for the two Majors' repairs to contradict or need narrowing. Editing them anyway would have been an unrequested, ungrounded addition.
  grounding: `grep -n "runs corpus\|snapshot\|.shell-team/runs\|full-population\|once per sweep" docs/adopting.md docs/adopting.ja.md`, and the surrounding sentence read directly at `docs/adopting.md:113-116` and `docs/adopting.ja.md:120-124`; **AC7**'s check line (re-run standalone after this round, `rc=0`, confirming the round's non-edit did not regress the criterion).
<!-- END provenance: T-1108 -->
