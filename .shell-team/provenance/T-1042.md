<!-- BEGIN provenance: T-1042 -->

- decision: implemented `review-artifacts`'s "any regular file" rule (D3) by passing an EMPTY string as `count_dir_entries`'s existing `suffix` parameter (`case "$f" in *"$suffix")` degenerates to the glob `*`, matching every name) at the `report_dir_input review-artifacts ...` call site, instead of adding a new boolean/mode parameter to `count_dir_entries` or `report_dir_input`, or hand-rolling a second enumeration function.
  reason: the spec's own `## Notes for engineer` names this exact shape ("`count_dir_entries` already takes a suffix parameter and matches with `case "$f" in *"$suffix")`, so the open-extension rule has a natural minimal shape") as the intended minimal change; an empty suffix needs zero new branches in the shared helper, so the five other directory inputs' `.md`/`.jsonl` suffix rules (D3's "the other five keep their own suffix rules") stay provably untouched by construction rather than by a second code path that could drift from them, and `-f "$f"` (already in the shared helper) continues to exclude subdirectories and the `-L`-but-not-`-e` broken-symlink case with no new logic.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D3 and `## Notes for engineer`; `bin/retro-inputs.sh`'s `count_dir_entries()`/`report_dir_input()` (the shared helper's existing suffix-matching `case` arm); live `bash -c 'suffix=""; case "foo.txt" in *"$suffix") echo match;; esac'` confirming the empty-suffix pattern degenerates to match-all; `tests/retro-inputs/run.sh` `LB1`/`LB3`/`LB4` fixtures `PASS`.

- decision: left `bin/retro-inputs.sh`'s resolver invocation (`team-paths.sh --export`, no `--root`) and `bin/team-paths.sh`'s legacy-detection `elif`/`BASE`/`SPECS` branch behaviourally untouched.
  reason: D1 already discriminated the relayed `specs`-symptom report's mechanism against the code at `6439eb6` and concluded the resolver's cwd-relative convention (D1(i)) and the legacy split-root (D1(iii)) are correct as-is — only the `.md`-only suffix (D3, a measured first-party defect) and the legacy-marker documentation drift (D7) needed fixing; re-deriving that discrimination myself while implementing, rather than trusting it, would have risked a second, possibly conflicting judgment call on the same evidence pm-spec already weighed.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D1 (the three-candidate discrimination and its "change nothing about (i) or (iii)" conclusion); `## Non-goals` bullets 3 and 4 (no resolver-convention change; no split-root change); AC4 (`git show 6439eb6:bin/retro-inputs.sh`'s `--export` line and `--root` absence, byte-identical) `PASS`.

- decision: repaired the legacy-marker documentation drift (D7) at all four sites — `bin/team-paths.sh`'s header precedence entry, its header "the marker is …" sentence, its `print_help` precedence line, and `tests/team-paths/run.sh`'s own header comment (`OD1`-`OD4`) — doc-only, with the `elif` branch's detection behaviour (the contract-file test, `BASE="tasks"`, `SPECS="docs/specs"`) left byte-identical to the base blob.
  reason: D7 states the contradiction is real and measured (the `elif` anchors on `tasks/loops/shell-team.contract.yaml` while the surrounding prose in all four sites described a bare `tasks/loops/` directory) but explicitly scopes the fix to documentation only — the marker itself, and what it detects, does not move. Rewriting the four prose sites to name the contract file explicitly closes the drift without touching the one `elif`/two-assignment block AC5's byte-identity clauses lock.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D7 and the frozen `OD1`-`OD4` blocks; AC5's `check:` line (zero occurrences of each `OD` line, byte-identical `elif`/`BASE`/`SPECS` lines against the base blob) `PASS`.

- decision: reverted the entire Half A surface on this branch — `bin/team-init.sh` restored byte-identical to `6439eb6` (including the `log_warn()` `|| true` errexit guard from commit `9d28bae`, per pm-spec's explicit "do not re-apply it here" ruling); `bin/team-paths.sh`'s `warn_ignored_base()` function and its `print)`-arm call site removed entirely, leaving only the D7 documentation-line fixes; `tests/team-init/run.sh`, `README.md`, `README.ja.md`, `docs/adopting.md` and `docs/adopting.ja.md` restored byte-identical to `6439eb6`; `tests/team-paths/run.sh` restored byte-identical to `6439eb6` (see the next entry for why that specific file's revert is the one non-obvious call in this list) — rather than a partial revert that kept any Half A code, fixture, or documentation delta.
  reason: the spec's own `## Same-class-2 pre-commitment` fired (two consecutive rounds of new Majors against the ignored-base mechanism) and disposition (i) — split Half A into successor task T-1046, ship Half B alone — was taken and recorded in the v2→v3 re-freeze. AC16's allow-list is v3's mechanical enforcer of exactly this: `bin/team-init.sh`, `tests/team-paths/run.sh`, `tests/team-init/run.sh` and the four adopter documents are deliberately absent from it, so any surviving Half A residue (code, fixture, or prose) turns AC16 red. `## Notes for engineer` states this explicitly for the `log_warn` guard by name ("a correct one-token fix to a real pre-existing defect... but its T-1042 trigger was the notice, which has left... Do not re-apply it here").
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` AC16 (the 10-path allow-list and its absence list), `## Notes for engineer` (the `log_warn` ruling, quoted above, and "Your first step is a revert, not an edit... Restore ... to their `6439eb6` blobs"), and `## Same-class-2 pre-commitment — FIRED 2026-08-07` (disposition (i) taken); live `cmp` against each `git show 6439eb6:<file>` blob for `bin/team-init.sh`, `tests/team-init/run.sh`, `README.md`, `README.ja.md`, `docs/adopting.md`, `docs/adopting.ja.md` and `tests/team-paths/run.sh`, all byte-identical; `git diff --name-only 6439eb6` reduced to exactly the 9 files this task's surviving surface touches, all within AC16's 10-path allow-list; AC16 `PASS`.

- decision: reverted `tests/team-paths/run.sh` fully to its `6439eb6` blob — including the `OD4` legacy-marker documentation fix that AC5's own `check:` line still requires absent from that exact file — rather than keeping the `OD4` fix alone (stripped of the Half A `LA1`-`LA8` fixtures and `GTMP` machinery), even though a relayed instruction named that narrower revert shape.
  reason: v3's `AC5` and `AC16` are mutually unsatisfiable for this one file, measured directly rather than assumed: `AC5`'s `check:` line hardcodes `S=tests/team-paths/run.sh` and requires the frozen `OD4` line to occur zero times in it (occurring once at base, the positive control) — satisfiable only by editing the file — while `AC16`'s `check:` line's allow-list `case` pattern does not include `tests/team-paths/run.sh` at all, so ANY diff against base in that file (including an `OD4`-only edit) fails `AC16`'s completeness sweep. Both cannot be green at once for the same file state. Three converging signals decided which criterion to honor: (1) `AC16`'s own prose calls itself out by name as "what mechanically enforces the Half A revert" and lists `tests/team-paths/run.sh` among the files "deliberately absent from the allow-list, so ... any residue of the descoped work ... turns it red" — an explicit, load-bearing statement about this exact file, not an omission; (2) `## Notes for engineer`'s "Files touched, after the revert" list enumerates `bin/retro-inputs.sh`, `bin/team-paths.sh` (documentation region only) and `tests/retro-inputs/run.sh` and states "Nothing else — AC16 enumerates the closed set", again naming the closed set without `tests/team-paths/run.sh`; (3) `AC5`'s `check:` line is, by contrast, unchanged text carried forward from the pre-descope (v1/v2) spec versions, unlike `AC16` and `## Notes for engineer`, both of which were visibly rewritten for the v3 descope — consistent with a residual check-line bug that survived the re-freeze rather than a considered v3 requirement. Given the two cannot both be satisfied, `AC16` — explicitly named as the mechanical enforcer of the very outcome this rework's own directive requires — is the criterion honored; `AC5`'s `S=tests/team-paths/run.sh` clause is left red as a disclosed, structural spec residue, not a silently-resolved implementation shortcut, and is reported to the coordinating session rather than resolved by editing the frozen spec.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` AC5's `check:` line (`S=tests/team-paths/run.sh`; `grep -cxFf "$T/OD4" "$S"` required `= "0"`) and AC16's `check:` line (`case "$f" in bin/team-paths.sh|bin/retro-inputs.sh|tests/retro-inputs/run.sh|...` — no `tests/team-paths/run.sh` arm) and prose ("`tests/team-paths/run.sh`... deliberately absent from the allow-list") and `## Notes for engineer`'s file list; live `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh` run with `tests/team-paths/run.sh` fully reverted to base: `AC16` `PASS`, `AC5` `FAIL` (the `S`-clause only) — confirming the conflict is real rather than a misreading, and that this disposition trades a disclosed, named-file `AC5` sub-clause failure for a clean `AC16`, not the reverse.

<!-- END provenance: T-1042 -->
