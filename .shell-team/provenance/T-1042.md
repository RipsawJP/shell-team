<!-- BEGIN provenance: T-1042 -->

- decision: implemented `review-artifacts`'s "any regular file" rule (D3) by passing an EMPTY string as `count_dir_entries`'s existing `suffix` parameter (`case "$f" in *"$suffix")` degenerates to the glob `*`, matching every name) at the `report_dir_input review-artifacts ...` call site, instead of adding a new boolean/mode parameter to `count_dir_entries` or `report_dir_input`, or hand-rolling a second enumeration function.
  reason: the spec's own `## Notes for engineer` names this exact shape ("`count_dir_entries` already takes a suffix parameter and matches with `case "$f" in *"$suffix")`, so the open-extension rule has a natural minimal shape") as the intended minimal change; an empty suffix needs zero new branches in the shared helper, so the five other directory inputs' `.md`/`.jsonl` suffix rules (D3's "the other five keep their own suffix rules") stay provably untouched by construction rather than by a second code path that could drift from them, and `-f "$f"` (already in the shared helper) continues to exclude subdirectories and the `-L`-but-not-`-e` broken-symlink case with no new logic.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D3 and `## Notes for engineer`; `bin/retro-inputs.sh`'s `count_dir_entries()`/`report_dir_input()` (the shared helper's existing suffix-matching `case` arm); live `bash -c 'suffix=""; case "foo.txt" in *"$suffix") echo match;; esac'` confirming the empty-suffix pattern degenerates to match-all; `tests/retro-inputs/run.sh` `LB1`/`LB3`/`LB4` fixtures `PASS`.

- decision: in `warn_ignored_base()` (both `bin/team-paths.sh` and `bin/team-init.sh`), routed the "git absent" case through the SAME `case "$rc" in ... *) ...` branch that handles "check-ignore returned an unexpected status" (via a sentinel `rc=99` set when `command -v git` fails), rather than giving the git-absent branch its own early `printf`/return with a second copy of the N3 body text.
  reason: AC9 requires each of the three frozen message bodies (N1/N2/N3) to occur EXACTLY ONCE, verbatim, in each script's own source; an early-return branch with its own `printf -v msg '...whether the resolved base dir %s is matched by a git ignore rule could not be determined...'` would have put the N3 literal in the source twice (once for "git absent", once for "check-ignore answered oddly"), failing AC9's exactly-once count the moment both paths were reachable in the same function. Funnelling both into one shared `case` arm keeps the frozen body as a single source-text occurrence while still reaching it from either precondition failure.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` AC9's `check:` line (`grep -cFf "$T/$n" "$P"` = "1") and D6 ("the last covering git being unavailable and any `git check-ignore` exit status other than the two that mean 'ignored' and 'not ignored'" — both grouped as one undeterminable class); live `grep -cFf` counts against `bin/team-paths.sh` and `bin/team-init.sh` for each of N1/N2/N3 = 1 in both files; `check-acs.sh` AC9 `PASS`.

- decision: for every ignore-behaviour test fixture, pinned `core.excludesFile` by writing it into the FIXTURE REPO'S OWN persisted `.git/config` (`git -C "$dir" config core.excludesFile <path>`) rather than passing a transient `git -c core.excludesFile=<path>` flag on the invocation that calls `team-paths.sh`/`team-init.sh`.
  reason: D12 requires "every such assertion pins the excludes file explicitly", but the git invocation that needs pinning runs INSIDE the production script (`warn_ignored_base()`'s own `git -C "$root" check-ignore ...`), which the test never calls directly and which AC12 forbids from carrying any `-c core.excludesFile=` override itself (the production probe must honour whatever the operator's real global excludes are). A repo-local `core.excludesFile` config value is read by every subsequent git invocation against that repo — including the production script's — without the production code needing to know a test is even running, whereas a `-c` flag on the test's own outer command would never reach the script's internal git calls at all.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D12 and AC12 ("`core.excludesFile` occurs zero times anywhere under `bin/`"); live `git config core.excludesFile /dev/null` (persisted) followed by a plain `git check-ignore` in a fresh fixture repo, confirmed to override this machine's real `~/.gitignore_global` entry that otherwise ignores `.shell-team/`; `tests/team-paths/run.sh` `LA1`-`LA5`, `LA8` and `tests/team-init/run.sh` `LA9`/`LA10` fixtures `PASS`.

- decision: relocated every ignore-assertion fixture that needs a real `git init`-ed work tree (`LA1`-`LA5`, `LA8`'s reuse of `LA1`, and `tests/team-init/run.sh`'s `LA9`/`LA10`) out of the suites' existing `$HERE/tmp-roots` / `$HERE/tmp-targets` directories (which are nested inside this checked-out repository's own work tree) and into a new `$TMPDIR`-based root (falling back to `$HERE/git-tmp`), mirroring `tests/retro-inputs/run.sh`'s pre-existing pattern for the identical constraint, instead of building them under the pre-existing `tmp-roots`/`tmp-targets` directories as D12's prose literally describes for the suite's OTHER (non-git) fixtures.
  reason: this sandboxed execution environment measurably denies `git init`/`git config` writes under any path inside this repository's own checkout (`fatal: cannot copy '.../hooks/commit-msg.sample' ... Operation not permitted`, and separately `could not write config file .../.git/config: Operation not permitted`, reproduced at multiple locations under both `tests/team-paths/` and `tests/team-init/`), while the identical operation succeeds immediately under `$TMPDIR`. `tests/retro-inputs/run.sh`'s own header comment already documents and works around this exact constraint ("sandboxed runs deny writes to a nested .git/ inside this repo's own tree"). D12's actual normative requirement is "an independently `git init`-ed work tree" plus "pins the excludes file explicitly" — neither depends on the fixture's literal parent directory, and D12's prose about `tests/team-paths/run.sh`'s existing convention describes fixtures that were ALREADY plain (non-git) directories before this task, not a constraint this task's new git-needing fixtures must additionally satisfy in the same location.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D12 (normative: "independently `git init`-ed work tree" + "pins the excludes file explicitly"; descriptive: "`tests/team-paths/run.sh` deliberately avoids mktemp and builds its roots under its own directory"); `tests/retro-inputs/run.sh`'s header comment (the pre-existing, same-repo precedent for this exact sandbox constraint and its `$TMPDIR` workaround); live `git -C <path-under-this-repo> init -q` reproducing the `Operation not permitted` failure versus `git -C <path-under-$TMPDIR> init -q` succeeding, both measured directly in this task's own execution; `tests/team-paths/run.sh` and `tests/team-init/run.sh` full suite runs `PASS` (all assertions, including every `LA*` case) after the relocation.

- decision: left `bin/retro-inputs.sh`'s resolver invocation (`team-paths.sh --export`, no `--root`) and `bin/team-paths.sh`'s legacy-detection `elif`/`BASE`/`SPECS` branch behaviourally untouched, and added no acknowledgement flag (`--allow-ignored-base` or equivalent) to either `bin/team-paths.sh` or `bin/team-init.sh`, even though both would have been available levers while touching these exact files/lines.
  reason: D1 already discriminated the relayed `specs`-symptom report's mechanism against the code at `6439eb6` and concluded the resolver's cwd-relative convention (D1(i)) and the legacy split-root (D1(iii)) are correct as-is — only the `.md`-only suffix (D3, a measured first-party defect) and the legacy-marker documentation drift (D7) needed fixing; re-deriving that discrimination myself while implementing, rather than trusting it, would have risked a second, possibly conflicting judgment call on the same evidence pm-spec already weighed. D5 separately settled that no acknowledgement flag ships, for reasons (no durable home, single-invocation-only suppression value) that remain true regardless of how the notice itself gets implemented, so adding one — even as a convenience for this task's own test fixtures — would have silently reopened a closed decision.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` D1 (the three-candidate discrimination and its "change nothing about (i) or (iii)" conclusion) and D5 ("no acknowledgement flag ships, on either script... Recorded as an issue candidate for the next planning input, not filed from this role"); `## Non-goals` bullets 2 and 3 (no flag; no resolver-convention change); AC4 (`git show 6439eb6:bin/retro-inputs.sh`'s `--export` line and `--root` absence, byte-identical) and AC11 (`allow-ignored-base` token count = 0 across `bin/`, `tests/`, `skills/`, `agents/`, `templates/`, `docs/`, `README.md`, `README.ja.md`) both `PASS`.

- decision: for M1's pathspec-metacharacter fix, used TWO DIFFERENT literal-forcing mechanisms across the two git calls within the same function — `:(literal)$base` pathspec magic for `ls-files`, and `-c core.literalPathspecs=true` for `check-ignore` — rather than one uniform mechanism for both, even though the review's own suggested-fix text named both mechanisms as if either could cover both call sites ("`:(literal)$base` or `-c core.literalPathspecs=true` ... in both scripts' `ls-files` and `check-ignore` calls").
  reason: neither mechanism actually covers both commands in this environment's git (2.53.0), measured directly rather than assumed from documentation. `check-ignore` REJECTS `:(literal)` outright ("fatal: ... pathspec magic not supported by this command: 'literal'"), so that option is not available to it at all. Conversely, `-c core.literalPathspecs=true` (and the persisted `git config core.literalPathspecs true` form, and the `GIT_LITERAL_PATHSPECS=1` env-var form) measurably do NOT suppress `ls-files`'s own glob-directory-prefix matching in this git version — `git -c core.literalPathspecs=true ls-files -- "a*"` still returned an unrelated tracked file living under a directory named `abc` (glob-matching `a*` as a directory prefix) in every repeated trial, while `:(literal)a*` correctly returned nothing. Given neither mechanism is universally supported, the only way to make BOTH calls genuinely literal was one mechanism per command, chosen for what that specific command actually honors.
  grounding: `.shell-team/reviews/T-1042.md` M1's suggested-fix text (quoted above); live `git --version` = 2.53.0; live repeated trials (`git -c core.literalPathspecs=true ls-files -- "a*"` returning a false match three separate times across three fresh fixture repos; `git check-ignore -q -- ':(literal)a*'` failing with the quoted fatal error; `git ls-files -- ':(literal)a*'` correctly returning empty) all captured during this task's own execution, not asserted from git documentation alone; the new `tests/team-paths/run.sh` regression fixture (a literal two-character directory named `a*`, built with `mkdir --`, plus an unrelated tracked file under a directory named `abc` that glob-matches the same pattern) reproduces the reviewer's exact live repro and passes after the fix, and a mutation self-check (reverting `ls-files`'s pathspec to plain `"$base"`) reproduces the FAIL, confirming the fixture is load-bearing rather than vacuous.

- decision: for M2, distinguished "genuinely no git repository anywhere" (must stay N2, since `LA6`/AC7 already lock this class and the frozen `N2` body's own wording — "not inside a git work tree" — is definitionally true of it) from other `rev-parse --is-inside-work-tree` failures (must be N3) by matching git's own fatal-message text ("not a git repository") against the merged stdout+stderr capture, rather than following the review's literally-relayed suggested mapping verbatim ("rc=0 && out=true -> inside; rc=0 && out=false -> N2; anything else -> N3" — which would reclassify the no-repo-anywhere case as N3 too, since it is also a nonzero `rc`).
  reason: the review's own suggested fix, applied byte-for-byte, would have broken the ALREADY-CORRECT, ALREADY-FROZEN-REQUIRED `LA6` fixture (a target dir with no `.git` anywhere in its ancestry, which this spec's `AC7` already locks to `N2`) — a live check confirmed `rev-parse --is-inside-work-tree` against such a target exits 128 with "fatal: not a git repository (or any of the parent directories): .git", which is a NONZERO `rc`, and the literal "anything else -> N3" rule would have sent it to `N3` instead. Since M2's own text frames the goal as distinguishing "outside a work tree" from "undeterminable for an unrelated reason" (its own words: "dubious ownership / permission-denied / corrupted-repo failures... make rev-parse exit non-zero for a reason that is NOT outside a work tree"), and "not a git repository" IS exactly "outside a work tree" by that same standard, preserving it as N2 is what the review's own stated INTENT requires even though its literal suggested-fix text under-specified this one sub-case.
  grounding: `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md`'s frozen `N2` body ("the resolved base dir %s is not inside a git work tree...") and `LA6`'s frozen label + AC7's `check:` line (both require `N2` for a no-git-repo-at-all target); `.shell-team/reviews/T-1042.md` M2 (both the suggested-fix mapping AND its own stated rationale about ownership/permission/corruption failures being distinct from "outside a work tree"); live `git -C <no-git-ancestor-dir> rev-parse --is-inside-work-tree` exit 128 / "fatal: not a git repository (or any of the parent directories): .git", captured directly; a fake-`git` stub simulating a "detected dubious ownership" fatal message, confirmed to classify as `N3` (undeterminable) under the shipped code; `tests/team-paths/run.sh`'s existing `LA6` fixture `PASS` after the fix, and a mutation self-check (removing the "not a git repository" special case) reproduces `LA6`'s `FAIL`, confirming it is load-bearing.

- decision: normalized the `check-ignore` query to `"${base%/}/"` (strip any existing trailing slash, then append exactly one) for M3, applied unconditionally to every call in both scripts, rather than conditioning the trailing slash on whether the base dir happens to exist on disk at probe time.
  reason: the whole point of M3 is that on-disk existence is NOT a reliable signal `--print` can depend on (no scaffolding step precedes it, unlike `team-init.sh`'s D9 ordering) — branching the query shape on existence would just move the same "existence-dependent behaviour" bug into the new code instead of removing it. A directory-form query is safe unconditionally because a bare-form `.gitignore` rule still matches a directory-form query (confirmed live), so the only behaviour this changes is exactly the gap M3 names.
  grounding: `.shell-team/reviews/T-1042.md` M3 (repro and suggested fix); `docs/adopting.md`'s recommendation of the directory form; live trials: a bare-form rule (`.shell-team`, no trailing slash in `.gitignore`) matched by a directory-form query (`.shell-team/`) both when the target exists and when it is absent; a directory-form rule (`.shell-team/`) matched by a directory-form query when the target is absent (the exact M3 gap, confirmed closed); the new `tests/team-paths/run.sh` `LA4` sub-case (base dir deliberately never `mkdir -p`-ed, unlike every other fixture in the suite) `PASS`, and a mutation self-check (reverting the query back to plain `"$base"`) reproduces its `FAIL`, confirming it is load-bearing; `LA4`'s pre-existing bare-form sub-case unaffected (still `PASS`).

- decision: closed review Minor #1 (`ls-files`'s own exit status discarded via `2>/dev/null || true`, which could turn a corrupt/unreadable index into a false silent read) as part of the M2 restructuring, reflecting a nonzero `ls-files` exit into the `undeterminable` class, rather than filing it as a separate fast-follow issue per the review's own disposition.
  reason: the coordinating session's relay explicitly invited this trade: "if your M2 restructuring makes reflecting `ls-files`'s own failure into N3 nearly free, doing it now shrinks the follow-up issue to nothing." The M2 rewrite already needed a single classify-then-emit shape (capturing rc from each git call rather than swallowing it), so adding the same three-line shape to the `ls-files` call was a marginal, not a structural, cost — cheaper than tracking and later re-implementing the identical pattern in a standalone fast-follow.
  grounding: the coordinating session's relayed message ("Minor #1 ... if your M2 restructuring makes reflecting ls-files' own failure into N3 nearly free, doing it now shrinks the follow-up issue to nothing"); `.shell-team/reviews/T-1042.md` Minor #1 and its Fast-follow disposition ("file-an-issue"); the shipped `warn_ignored_base()` (both scripts) now captures `tracked_rc` from the same `if var="$(cmd)"; then rc=0; else rc=$?; fi` idiom already used for `check-ignore` and `rev-parse`, with a nonzero `tracked_rc` routed to `class="undeterminable"` before the emptiness check is ever consulted.

<!-- END provenance: T-1042 -->
