<!-- BEGIN provenance: T-1120 -->
- decision: Rewrote `bin/check-count-claims.sh`'s refusal message as "guard the upstream stage with `\|\| true` before piping to `wc -l`, e.g. `(grep zzz file \|\| true) \| wc -l` (drop `-c` from `grep` ...)" — leading with the one guarded recipe and naming the `grep -c` trap and its `double-count` consequence in prose — rather than keeping the original two-recipe "pipe through `\| wc -l` instead (drop `-c`: ...)" shape with only the unguarded quotes deleted.
  reason: Two shapes both satisfy AC1's literal requirements (guarded recipe present, `grep -c` and `double-count` present, `\| wc -l` fragment present, zero unguarded pipeline spans). (a) Minimally delete the two unguarded quoted examples in place, leaving the surrounding sentence structure ("pipe through ... instead (drop -c: ..., never ..., which double-counts ...)") intact around the hole. (b) Restructure the sentence so the guarded recipe is the only pipeline ever quoted, with the drop-`-c` trap named without quoting a pipeline at all. (a) was rejected because deleting only the two bad quotes without restructuring leaves a grammatically broken or misleading sentence ("pipe through `\| wc -l` instead (drop `-c`: , never , which double-counts...)" with empty slots) — the class-not-case principle in Pre-commitment implies the fix should read as a coherent single recipe, not a patched hole. (b) also matches Notes for engineer's explicit template: "keep the guarded example exactly as `(grep zzz file \|\| true) \| wc -l` ... keep the drop-`-c` teaching by naming the trap and its consequence in prose ... without quoting a pipeline."
  grounding: `.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md` `## Notes for engineer`, first bullet ("The `#409` repair's target property is a class, not a case ... Keep the drop-`-c` teaching by naming the trap ... and its consequence ... in prose ... without quoting a pipeline"); AC1's per-span pipeline extraction and literal-presence clauses.

- decision: Edited all four sibling `- dispatch-rule:` lines in `docs/loop-engineering/phase-multiplexing.md` (the `verify`/`verify-fixture` serial and tier1-fanout-conditional rows) to drop their stale precondition framing, including the two rows (`verify — tier1-fanout` and `verify-fixture — tier1-fanout`) whose exact stale substrings ("two unbuilt preconditions", "preconditions exist") AC9's own grep counts do not literally match — rather than editing only the two rows AC9's literal-count assertions require (the `serial — unconditional` rows).
  reason: AC9's own prose states "the four sibling rule lines stop asserting two unbuilt preconditions" (plural, naming all four), and the Problem section explicitly names `:366`/`:368` as each asserting "both of the recommendation's named preconditions exist" as a stale claim, even though that literal three-word substring "preconditions exist" happens to be broken up by an intervening parenthetical in `:366` and therefore does not contribute to AC9's file-wide count. Editing only the two rows the grep count technically requires would leave `:366`/`:368` narratively asserting a still-unbuilt-precondition gate the Goal and Problem sections identify as false, while the disposition/modality of those two rows (conditional, trigger-based) stays untouched per Non-goals and DP-H — the edit corrects only the precondition's status prose inside the existing trigger clause, never the `conditional`/`unconditional` modality itself.
  grounding: `.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md` `## Problem` ("`:366`/`:368` each assert that both of the recommendation's named preconditions exist"); AC9's own prose ("The four sibling rule lines stop asserting two unbuilt preconditions"); `## Non-goals` ("No flip of any disposition, recommendation or dispatch rule ... Only the status of a named precondition is corrected, on four rule/recommendation lines and one follow-up line.").

- decision: Reworded the repaired `:373` follow-up line as "the `per-instance-telemetry-discriminator` this row once asked to design is discharged — it has shipped as `bin/check-fanout-instances.sh` ... — so a real N-instance fan-out's own merged hand-off records already stay attributable ..." (a `- follow-up: the ...` opening, discharge stated mid-sentence) rather than any phrasing beginning with the word `discharged` or a variant of `- follow-up: DISCHARGED ...`.
  reason: DP-G names an exact hazard: T-1112 AC3 `cmp`s the whole-file extraction of lines matching `^- follow-up: DISCHARGED` against a one-line base extraction, so any repaired wording beginning with that literal prefix — even a natural-sounding one like "- follow-up: DISCHARGED (again) ..." — would turn that extraction into two lines and redden a merged criterion this task must keep green. Multiple safe openings existed ("- follow-up: the ...", "- follow-up: this precondition ...", "- follow-up: per-instance-telemetry-discriminator ..."); the one adopted keeps a `- follow-up: ` prefix, states the discharge and names the shipped script in the body, and preserves the `per-instance-telemetry-discriminator` term AC10 greps for.
  grounding: `.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md` `## Decision points` DP-G ("A repaired `:373` beginning with that prefix would make the extraction two lines and redden a criterion this task is otherwise required to keep green ... The repaired line therefore keeps a `- follow-up: ` prefix and states the discharge in its body."); AC10's check line (extracts `^- follow-up: DISCHARGED` at both refs and requires the extraction to stay one byte-identical line).

- decision: Reworded both README `- **Within-phase fan-out**` bullets' stale clause as "a Plan-time `verify`-axis dispatch decision `tech-lead` elects and the orchestrator transcribes onto the task's board entry at the Specify-to-Implement seam, which the Validate phase's own fan-out step then reads" (English mirror) and its literal JA translation, naming `tech-lead` and the orchestrator explicitly as the two actors — rather than a shorter clause that names only the phases (Plan/Specify/Validate) without naming who acts at each one.
  reason: `skills/run/SKILL.md:281` (cited by the spec as the source of truth this repair states) frames the decision as `tech-lead` deciding and the orchestrator transcribing, not as an unattributed phase-level event; naming both actors keeps the README bullet accurate to that source rather than merely satisfying AC7's phase-token-ordering check (`Plan` before `Validate`) with vaguer prose. The added actor-naming text also does not disturb AC5/AC6's forbidden-phrase or opt-in-literal clauses, confirmed by re-running the check lines.
  grounding: `.shell-team/specs/T-1120-rider-count-claims-message-and-fanout-docs.md` `## Summarized sources` (`skills/run/SKILL.md:276`–`:281` entry: "`tech-lead` decides in step 1; you (the orchestrator) transcribe the printed decision onto the task's board entry at the Specify-to-Implement seam"); AC5/AC6/AC7 check lines (four-token presence, `Plan`-before-`Validate` ordering, forbidden-phrase absence — all re-verified green against the adopted wording).
<!-- END provenance: T-1120 -->
