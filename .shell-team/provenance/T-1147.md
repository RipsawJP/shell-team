<!-- BEGIN provenance: T-1147 -->

- decision: named the eight content-refusal tokens in `bin/check-refreeze-grant.sh`'s `parse_config()` (`unparseable-line`, `missing-schema`, `duplicate-schema`, `schema-not-first`, `unsupported-schema`, `missing-grant`, `duplicate-grant`, `unknown-grant`) as a direct rename of `bin/check-oversight.sh`'s own `schema`/`profile` token set to a `schema`/`grant` vocabulary, rather than inventing a differently-shaped token set for the new field.
  reason: neither AC4 nor D-4/D-5 mandate the literal spelling of any exit-1 token — AC4 only asserts exit code, empty stdout and non-empty stderr — so the naming was a free choice. Reusing `bin/check-oversight.sh`'s own naming pattern member for member keeps the two sibling readers' refusal vocabularies recognizably parallel for anyone who has to read both, and Notes for engineer explicitly directs copying that file's parser "member for member".
  grounding: `bin/check-refreeze-grant.sh` (`parse_config()`, the `case "$first"`/`case "$grant_value"` refusal tokens); `bin/check-oversight.sh:292`-`:328` (the sibling `missing-schema`/`duplicate-schema`/`schema-not-first`/`unsupported-schema`/`missing-profile`/`duplicate-profile`/`unknown-profile` tokens); spec `## Notes for engineer` ("`bin/check-oversight.sh` is the shape to copy member for member: ... its two-pass parser").

- decision: in `docs/tuning-oversight.md` and `docs/tuning-oversight.ja.md`, placed the new "Where the grant is recorded, per host" material as a `### ` (level-3) subsection nested under the existing `## Who may re-freeze a frozen intent block` heading, and placed the sample `schema 1` / `grant class-m` block as an un-indented fenced code block sitting after the two-item bullet list rather than nested inside the second bullet's own continuation.
  reason: AC8 requires the `^## ` heading count to stay equal between each language pair (measured 10/10 for the tuning-oversight pair, 21/21 for the adopting pair) — a new `## ` heading in one file with no counterpart in the other would break that invariant, so a `### ` subsection was the only way to add a titled section without moving the count. Un-indenting the sample fence was required separately: AC9 asserts `grep -qxF -- 'schema 1'` and `grep -qxF -- 'grant class-m'` against the whole file, which is a whole-line-equality match — a fence nested inside a list item's continuation would have carried a leading two-space indent on every content line, which `-x` does not tolerate, so the sample had to sit outside the list's own indentation to read back as an exact bare line.
  grounding: `docs/tuning-oversight.md` (`### Where the grant is recorded, per host (T-1147, issue #515)` and the un-indented ` ```markdown / schema 1 / grant class-m / ``` ` block immediately after it); `docs/tuning-oversight.ja.md` (the parallel `### grant はホストごとにどこへ記録するか` subsection); **AC8** (`test "$a" = "$b"`, `test "$c" = "$d"` heading-count equality); **AC9** (`grep -qxF -- 'schema 1' "$G"`, `grep -qxF -- 'grant class-m' "$G"`).

- decision: edited `skills/run/SKILL.md`'s class-M branch bullet (line 61) as a mid-sentence insertion immediately after the branch's opening parenthetical, replacing only the words "with no grant" with a new clause ending "... surfaced in that escalation. With no grant, ..." — never splitting the bullet into two bullets or moving any of its pre-existing sentences.
  reason: the Notes for engineer paragraph states this is "an **addition** to one existing line, never a rewrite" because every `contain`-mode registry row naming this file requires its canonical block lines to survive verbatim (`bin/check-prompt-sync.sh` is the enforcing check), and AC5 additionally requires the whole family of required fixed strings to sit on the *single* line matched by `grep -F -- 'class-M (mechanics repair) branch'` — confirmed by grep before and after the edit to still be exactly one line. Splitting into a second bullet, or moving the insertion to the bullet's tail, would have risked either creating a second matching line (failing AC5's single-line assertion) or landing outside whichever `contain` block's verbatim-lines boundary `check-prompt-sync.sh` actually walks.
  grounding: `skills/run/SKILL.md` (the class-M branch bullet at what was line 61 before this edit); **AC5** (`test "$(grep -c . "$T/line" || true)" = "1"` and the seven required fixed strings); **AC11** (`bash bin/check-prompt-sync.sh --root "$PWD"` exits 0, unchanged after the edit — confirmed by running it); spec `## Notes for engineer` ("This is an **addition** to one existing line, never a rewrite").

- decision: in `tests/check-refreeze-grant/run.sh`, added a bare `pass "crg-malformed-not-granted"` line immediately after the first (descriptively-named) malformed fixture, and a bare `pass "crg-occupancy-not-granted"` line immediately after the first (descriptively-named) occupancy fixture, rather than renaming either fixture's own descriptive `pass` call to the bare id.
  reason: AC10 requires the suite's stdout to carry the **exact** lines `PASS: crg-malformed-not-granted` and `PASS: crg-occupancy-not-granted` (`grep -qxF`, whole-line match), while the suite's existing convention (mirroring `tests/check-oversight/run.sh`) names each `pass` call with a human-readable description of what was just asserted. Live-running the AC10 check against the first draft of the suite (before this fix) reproduced exactly this gap — the descriptive lines `PASS: crg-malformed-not-granted: unknown first field` and `PASS: crg-occupancy-not-granted: a directory at the record path refuses declaration-occupancy` did not satisfy `-x` equality against the bare id — so a second, bare pass line was added alongside the descriptive one rather than sacrificing either the exact-match requirement or the suite's own descriptive-pass convention.
  grounding: `tests/check-refreeze-grant/run.sh` (the two added bare `pass "crg-malformed-not-granted"` / `pass "crg-occupancy-not-granted"` lines); **AC10** (`grep -qxF -- "PASS: $id" "$T/out"` for each of the four pinned ids); live run of AC10's own `- check:` line against the pre-fix suite, observed to fail at exactly this assertion before the fix and pass after (both re-run live during this task).

- decision: in `bin/check-refreeze-grant.sh`, resolved the record filename as a bare `.conf` suffix (`refreeze-grant.conf`) rather than reusing any portion of `oversight.conf`'s own literal name, and resolved the shipped default at `templates/refreeze-grant-default.conf` (a distinct basename, not `oversight-default.conf` with a suffix appended).
  reason: AC7 pins, as a base-relative delta, that the literal string `refreeze-grant.conf` occurs zero times in every tracked file at the branch point and is introduced by this task alone — the spec's own `## Assumptions` classifies this as "own-coinage" specifically over the full literal, "stated over the full literal rather than the bare token `refreeze-grant`, which shares a prefix with the pre-existing `check-refreeze-class.sh` family," precisely so the zero-occurrence premise cannot collide with anything that family's own files already mention. A shorter or overlapping filename would have risked exactly the collision the spec's own assumption calls out.
  grounding: `bin/check-refreeze-grant.sh` (`CONFIG_PATH="$BASE_DIR/refreeze-grant.conf"`); `templates/refreeze-grant-default.conf` (the file itself); spec `## Assumptions`, "Borrowed-vocabulary count-premise classification" ("`refreeze-grant.conf` at **zero** occurrences ... own-coinage: the whole filename is introduced by this task, and the premise is deliberately stated over the full literal rather than the bare token `refreeze-grant`"); **AC7** (`git grep -l -F -e 'refreeze-grant.conf' <branch point>` exits 1 with no output).

<!-- END provenance: T-1147 -->
