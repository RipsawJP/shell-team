<!-- BEGIN provenance: T-1118 -->
- decision: implement `admits-authority`'s "duplicated conflicting row" declaration-integrity check as a cross-family grounding rule — for every adapter appearing in `admits-authority` rows, each declared authority value must equal the `role-board-authority` of at least one role a well-formed `wires-role` row actually wires to that same adapter — rather than a simple "this adapter's admits-authority rows must not exceed one" or "no two rows may share the same (adapter, value) pair" rule.
  reason: AC2 requires exactly three `admits-authority claude-cli` rows (writes/proposes/none) and exactly one `admits-authority codex-cli` row (none) as shipped, correct content; AC13(iv)/AC14 mutate a *second*, differently-valued `admits-authority codex-cli` row (adding `writes` beside the existing `none`) and require this to be refused as a declaration-integrity defect (exit 2, family named on stderr) rather than silently accepted as a widened admitted set. A naive "count(rows) > 1 for this adapter is bad" rule would also flag claude-cli's own three legitimate rows, breaking AC12's requirement that the shipped, unmutated default admits every role claude-cli wires (a wired role's authority spans all three values there). A naive "the exact (adapter, value) pair must not repeat" rule would not flag the AC14 mutation at all, since `(codex-cli, writes)` is a genuinely new pair distinct from the existing `(codex-cli, none)`. The cross-check against `wires-role` is the one rule I found that is simultaneously: (a) satisfied by claude-cli's shipped three rows (its wired roles' distinct authorities are exactly {none, writes, proposes}, matching its three admits-authority values one-for-one); (b) satisfied by codex-cli's shipped one row (its one wired role, tech-lead, carries authority none, matching); (c) violated by AC14's mutation (codex-cli's wired-role set still only carries `none`, so the added `writes` value is ungrounded); and (d) does not fire on AC13(ii)'s scenario (engineer, authority writes, newly wired to codex-cli via an appended `wires-role` row, with `admits-authority codex-cli` left at its single `none` row) — there the *wires-role* set gained an authority the *admits-authority* set does not cover, which is the ordinary, evaluated `authority-incompatible` outcome (AC13(ii)) and must not be conflated with a declaration-integrity defect. I verified all four scenarios by hand against the frozen check lines (AC2, AC12, AC13, AC14) before committing to this rule, and confirmed the shipped recipe passes cleanly (`bash tests/check-invocation-path/run.sh`, cases cip-default-all-six-admit for the five claude-cli-bound roles, cip-authority-incompatible, cip-authority-duplicated, cip-authority-token-not-leaked all pass under this rule).
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC2 (claude-cli's three admits-authority rows, codex-cli's one), AC12 ("every role is admissible" under the shipped default, first part), AC13(ii) (engineer/writes wired to codex-cli, admits-authority unchanged, must refuse `authority-incompatible` not a declaration-integrity exit 2), AC13(iv) and AC14 (the `admits-authority codex-cli writes` duplicate-value mutation must refuse exit 2 with `admits-authority` named, and must never print `authority-incompatible`); `bin/check-adapter.sh --print-contract`'s `role-board-authority` table (tech-lead/ui-designer=none, pm-spec/engineer/qa-verifier=writes, codex-reviewer=proposes), read live by `role_authority_for()` in `bin/check-invocation-path.sh`.

- decision: define "duplicated" for `invocation-recipe` and `admits-authority` as "the same adapter token (field 2) appears in more than one row of that family", and for `wires-role` as "the same (adapter, role) pair (fields 2 and 3 together) appears in more than one row" — rather than requiring the *values* to differ before calling it a duplicate.
  reason: AC14's `invocation-recipe` mutation duplicates the adapter token `codex-cli` with a *different* invocation-kind (`in-process` appended beside the existing `sandbox-read-only`); AC14's `wires-role` mutation duplicates the exact same `(codex-cli, tech-lead)` pair with the *same* value (a literal repeated line). A rule that only fires when values differ would miss the wires-role case; treating any repeat of the family's own key as a defect handles both uniformly and matches DP-j's framing that the recipe is validated "as a whole" rather than tolerating redundant-but-harmless-looking repetition, which is itself a signal of a corrupted or hand-edited file this gate should not try to interpret charitably.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC14's `invocation-recipe`/`wires-role` duplicate-row mutations and DP-j ("The recipe is validated as a whole, before any per-adapter lookup: a duplicated or malformed row in any of the three families is exit 2 whichever adapter the role under test resolves to").

- decision: resolve the six-role population and the `--role` argument's validity by reading `bin/resolve-executor.sh --print-resolved`'s own live output (does this checkout's effective binding name this role at all?) rather than hardcoding a `SIX_ROLES` array literal inside `bin/check-invocation-path.sh` the way `bin/resolve-executor.sh` itself does.
  reason: `bin/resolve-executor.sh` already needs to be called for the adapter-token lookup regardless, so its own six-row output is a population this gate can read rather than duplicate; a second hardcoded six-name array would be a second place for the same population to drift if a future task changes which roles are bindable (AC12's own stale-at note names exactly this risk for the *test's* population — the same reasoning applies to the implementation reading it live rather than re-typing it).
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC12's stale-at note ("a task changes which roles are bindable... the count must be re-measured from templates/binding-default.conf rather than the loop widened") and DP-g (the gate delegates rather than re-reading the adapter/binding grammar).

- decision: on success (an admitted role/adapter/sandbox-mode triple), print `admitted <role> <adapter> <invocation-kind>` on stdout — a fourth field beyond the bare confirmation AC12 requires ("a non-empty line") — rather than a bare role name or a single fixed word.
  reason: No criterion pins this line's exact grammar (only that it is non-empty), so the choice was mine; I chose a shape that mirrors `bin/resolve-executor.sh --role`'s own `resolved <role> <provider> <model> <effort> <adapter> <probe-kind>` line closely enough to read as "the same family of tool" while carrying the two facts a caller reading this gate's output would actually want (which adapter and which invocation-kind licensed the admission), without inventing a new field vocabulary this repo does not already use elsewhere.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC12 ("printing a non-empty line on stdout for each"); `bin/resolve-executor.sh`'s own `resolved ...` stdout line shape (no AC pins a specific grammar for this gate's admit line beyond non-emptiness).

- decision: word the `templates/prompt-blocks/executor-resolution.md` amendment as "Bindings to an alternate-executor adapter are the disclosed exception..." (plural "bindings", "such invocations") rather than "the role on the alternate path" or any other cardinality-one phrasing naming `tech-lead` specifically.
  reason: `## Notes for engineer` explicitly flags this as the criterion the reviewer reads for exactly this determiner discipline (AC5's own text forbids a singular determiner over roles or bindings, because the corpus already discusses several roles and several bindings at once). I chose plural, adapter-scoped language throughout so the sentence remains true if a second alternate-executor path is ever wired, without naming `tech-lead` in the canonical line itself (the recipe's own declarations are where the one-role narrowing lives, per AC2).
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` `## Notes for engineer` ("AC5 pins two literals, not your wording — so watch the determiners... write it without a cardinality-one determiner over roles or bindings").

- decision: continue completing the full implementation (recipe, gate, fixture suite, CI wiring, docs) faithfully to every other frozen criterion, rather than pausing implementation, on discovering that AC12/AC15 (via `cip-default-all-six-admit`, "every one of the six roles is admissible under the shipped default") cannot be satisfied simultaneously with AC2's frozen `wires-role codex-cli`/`admits-authority codex-cli` cardinality (exactly one row each, naming only `tech-lead`/`none`), given `codex-reviewer`'s own shipped default binding already resolves to `codex-cli` with authority `proposes` — and to report this as a named, evidenced finding in the hand-off (`STATUS: BLOCKED` on this one criterion pair) rather than silently widening `wires-role codex-cli`/`admits-authority codex-cli` to admit `codex-reviewer`/`proposes` myself.
  reason: `wires-role codex-cli`/`admits-authority codex-cli`'s exact-line grammar is machine-checked by AC2's `- check:` line, which sits inside the frozen intent block — I may not edit it, and widening the recipe to satisfy AC12 would silently contradict AC2, the Non-Goals text ("Roles with writes or proposes authority are not wired under a read-only-sandbox adapter — a Non-goal and, after this task, a fail-closed refusal"), and Input-space class 3 (which explicitly lists `codex-reviewer (proposes)` among the roles this gate "must be refused"). Given three independent, frozen sources (AC2, Non-Goals, Input-space class 3) all point the same direction (codex-cli admits `none` only) while AC12/AC15 require the opposite outcome for `codex-reviewer` specifically, this is a genuine contradiction in the frozen spec rather than an implementation gap I can close by picking a side. I verified this is not a bug in my own reading by exhaustively testing four alternative admissibility theories against every fixture AC12/AC13/AC14 specify (documented in the hand-off) before concluding no implementation of the gate, as specified, can satisfy both criteria at once.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC2 (`admits-authority codex-cli none` exactly one row), AC12 ("Under the shipped default every role is admissible... exits 0 for each of the six inner-loop roles read live out of templates/binding-default.conf's own bind rows"), Non-Goals ("Roles with writes or proposes authority are not wired under a read-only-sandbox adapter"), Input space class 3 ("codex-reviewer (proposes) all resolve cleanly today; each must be refused by the gate"); `templates/binding-default.conf:43` (`bind codex-reviewer codex provider-configured - codex-cli`, unedited per this task's own Non-Goals); `bin/check-adapter.sh --print-contract` (`role-board-authority codex-reviewer proposes`).

- decision: leave AC6's `- check:` line's `grep -oE '.{0,400}--sandbox read-only.{0,400}'` construct untouched and instead verify its intended semantics (co-location of `tech-lead`/`codex-cli` within 400 characters of the first `--sandbox read-only` occurrence) with an independent Python re-derivation, after this host's `/usr/bin/grep` (BSD grep 2.6.0-FreeBSD) refused the exact frozen construct with `grep: maximum repetition exceeds 255` — rather than narrowing the interval myself.
  reason: the check line sits inside the frozen intent block; I may not edit it. This is a real, reproducible tool-portability defect (confirmed with a minimal `echo x | grep -oE '.{0,256}x'` vs `.{0,255}x'` probe on this host) rather than a content defect — the same construct is expected to work under GNU grep (CI runners), so the criterion's *content requirement* is satisfiable and I verified my documentation edits meet it independently; only this host's local verification of the exact frozen command is blocked.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC6's `- check:` line; `.shell-team/test-recipe.md`'s new T-1118 entry (the BSD-grep 255-repetition-cap finding, appended by this task); local probe `echo x | grep -oE '.{0,256}x'` → `grep: maximum repetition exceeds 255` on this host's `/usr/bin/grep`.

- decision: leave AC17's `- check:` line's bare `grep -c 'codex exec '` (no line-start anchor) untouched and instead verify the underlying invariant it is meant to protect (no `agents/*.md` file gained or lost a *bare, first-token* `codex exec ` caller line against the branch point) via `tests/codex-skeleton-hygiene/run.sh`'s own `agentmd-bare-codex-present` case, which uses the anchored pattern `^[[:space:]]*codex exec ` and is itself one of the six suites AC17 already requires to pass — rather than editing the frozen check line or trying to make `agents/codex-reviewer.md`/`agents/drift-evaluator.md` match an unanchored count of 3/1.
  reason: `agents/codex-reviewer.md` and `agents/drift-evaluator.md` are untouched by this task (confirmed identical to the branch point) and already carry the exact 3 and 1 bare, line-starting `codex exec ` invocations `tests/codex-skeleton-hygiene/run.sh:660`'s `BARE_CODEX_RE='^[[:space:]]*codex exec '` counts — but they also carry several *prose* mentions of the string "codex exec " (in explanatory sentences, e.g. "you run `codex exec …` yourself"), which the frozen check's unanchored `grep -c 'codex exec '` counts too, yielding 8 and 4 respectively at both the branch point and HEAD (unchanged by this task, so not a regression I introduced). This is a missing-anchor authoring defect in AC17's own `- check:` line, independent of content; the invariant it exists to protect (no agent file's caller-site count moved) is intact and independently verified by the anchored suite AC17 already requires.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC17's `- check:` line (`grep -c 'codex exec ' "$f"`, no `^` anchor); `tests/codex-skeleton-hygiene/run.sh:660`–`:678` (`BARE_CODEX_RE='^[[:space:]]*codex exec '`, asserting exactly 3 and exactly 1); measured counts (`grep -c 'codex exec ' agents/codex-reviewer.md` = 8, `agents/drift-evaluator.md` = 4, identical at the branch point commit `dfac5cd` and at HEAD).

- decision: (v2 rework — the `wrapper-hosted` derivation, DP-m) place the derivation strictly between the `invocation-recipe` lookup and the `wires-role` lookup, gated on `$INVOCATION_KIND = "sandbox-read-only"`, so it short-circuits (returns exit 0 immediately) rather than running as a parallel OR-condition alongside `wires-role`.
  reason: DP-m's own wording ("for a role whose resolved adapter's recipe is sandbox-read-only, test whether...") presupposes the recipe's existence and its invocation-kind are already known, which only the prior `invocation-recipe` lookup establishes — so the derivation cannot run before it. Placing it before `wires-role` (rather than after, or as an alternative path evaluated only on a `role-not-wired` refusal) is what AC13(vi) requires: with the pristine tree, `codex-reviewer` is admitted `wrapper-hosted` even though it is *not* in `wires-role codex-cli`'s set at all — if the derivation ran only as a fallback after a `wires-role` failure, that would still work for this one test, but the design is cleaner and matches the prose ("This runs BEFORE the wires-role question, so a role need not be named in wires-role at all to be admitted this way") as a genuine short-circuit: once matched, execution never reaches the `wires-role`/`admits-authority` code at all, so no widening of either declaration's *evaluation* (not just their text) can occur for a wrapper-hosted role. Gating on `sandbox-read-only` specifically (rather than running the check unconditionally for every role) is required because an `in-process` adapter (`claude-cli`) has no such conflation to resolve — DP-m states the derivation exists only "because the codex-cli token conflates two shapes", a claim that does not apply to `claude-cli`.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` DP-m ("This runs BEFORE the wires-role question..."); AC13(vi) (codex-reviewer admitted `wrapper-hosted` from the pristine tree despite not being in `wires-role codex-cli`'s set); `## Notes for engineer` bullet (i) ("before the wires-role question, for a role whose resolved adapter's recipe is sandbox-read-only...").

- decision: (v2 rework) the wrapper-hosted admit line's stdout format is `admitted <role> <adapter> wrapper-hosted <absolute-path-to-agents/role.md>` — the same three-field `admitted <role> <adapter> ...` prefix as the ordinary admit line (v1 decision, ratified), with `wrapper-hosted` and the agent file's path as the variable tail, rather than a differently-shaped line.
  reason: AC12 requires the line to carry the literal `wrapper-hosted` and to name `agents/codex-reviewer.md`, both checked with `grep -qF` (substring match) rather than a fixed-field grammar, so any format carrying both substrings is conformant; I kept the existing three-field prefix so a caller parsing this gate's stdout sees one consistent shape (`admitted <role> <adapter> ...`) across both admission routes, with the fourth-plus fields distinguishing which one fired. The path is printed absolute (as `$TEMPLATES_ROOT/agents/$ROLE_ARG.md` resolves it) rather than relativized to `agents/<role>.md`, because an absolute path ending in `.../agents/codex-reviewer.md` already contains that literal as a substring and AC12's check only tests for the substring — relativizing it would need a second resolution step this gate does not otherwise perform, for no grammar benefit.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC12's `- check:` line (`grep -qF -- 'wrapper-hosted' "$T/o.codex-reviewer"`, `grep -qF -- 'agents/codex-reviewer.md' "$T/o.codex-reviewer"` — both `grep -F`, substring, not anchored to a field position).

- decision: (v2 rework) resolve the wrapper-hosted derivation's agent file as `"$TEMPLATES_ROOT/agents/$ROLE_ARG.md"`, reusing the already-resolved `TEMPLATES_ROOT` variable (`cd "$SCRIPT_DIR/.." && pwd -P`, physical, resolved once near the top of the script for the recipe path) rather than adding a second, separate physical-resolution call for `agents/`.
  reason: `## Notes for engineer` names the exact shape to use ("$SCRIPT_DIR/../agents", physically, the shape `bin/check-model-pins.sh` already uses) and `TEMPLATES_ROOT` already IS `$SCRIPT_DIR/..` resolved physically — `$TEMPLATES_ROOT/agents` is byte-for-byte the same directory `$SCRIPT_DIR/../agents` names, so introducing a second `cd && pwd -P` call would duplicate work the script has already done, with no additional safety (both express the identical physical path).
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` `## Notes for engineer` bullet (i) ("Resolve agents/ from the gate's own installed directory ($SCRIPT_DIR/../agents, physically)... which is why AC12, AC13 and AC14 now copy agents/ into their scratch installed trees"); `bin/check-invocation-path.sh`'s own `TEMPLATES_ROOT="$(cd "$SCRIPT_DIR/.." && pwd -P)"` line (already present from the v1 implementation).

- decision: (review round 1 rework — Minor 2, the `admits-authority` duplicate-key gap) implement the fix as an INDEPENDENT `i≠j` duplicate loop keyed on the full `(adapter, value)` pair — the same shape the `invocation-recipe` (adapter-keyed) and `wires-role` ((adapter, role)-keyed) loops already carry — rather than folding the check into the existing "is this value grounded by a wired role's own authority" cross-check.
  reason: the cross-check and the duplicate-key check ask genuinely different questions and can each fire independently: the cross-check catches an *ungrounded* value (a value naming an authority no wired role for that adapter actually carries — the `cip-authority-duplicated` case's first arm, `admits-authority codex-cli writes` beside the existing `none`), while the duplicate-key check catches an *exact, byte-identical repeat* of the same row (the review's own live reproduction: two `admits-authority codex-cli none` lines) — a case the cross-check structurally cannot catch, since a value that IS grounded (it matches an existing row's own value) trivially passes the "is it grounded" test no matter how many times it is repeated. Writing one combined check would have required threading a repeat-count into the cross-check's own grounding loop, which is more entangled than adding the same three-line `i≠j` pattern the other two families already use, so mirroring the sibling shape is both the simplest and the most consistent fix. Confirmed no legitimate shipped content trips it: claude-cli's three rows are three distinct `(adapter, value)` pairs (`(claude-cli, none)`, `(claude-cli, writes)`, `(claude-cli, proposes)`) and codex-cli's one row is its own singleton pair — `bash tests/check-invocation-path/run.sh` stays 26 `PASS:` / 0 `FAIL:` after adding it.
  grounding: `.shell-team/reviews/T-1118.md` Minor 2 / Independent verification ("Reproduced the `admits-authority` duplicate-key gap live... unlike `invocation-recipe` and `wires-role`, both of which have an explicit `i≠j` duplicate-key loop the `admits-authority` family lacks"); the coordinating session's rework instruction ("Minor, fix now: `admits-authority` lacks the duplicate-(adapter,role/value)-key detection its two sibling families have — add it (exit 2, family named)"); `bin/check-invocation-path.sh`'s own pre-existing `invocation-recipe`/`wires-role` duplicate-key loops (the shape mirrored).

- decision: (review round 1 rework) extend the existing `cip-authority-duplicated` fixture case with a second arm (the byte-identical-row scenario) rather than adding a new case id.
  reason: AC15 pins a closed, frozen list of exactly 25 case ids with no placeholder for a 26th, and the coordinating session's own instruction named this exact constraint ("AC15 pins named case ids, so extend an existing duplicated-arm case rather than adding a 26th id unless AC15 permits more"). `cip-authority-duplicated` is already the case testing the `admits-authority` family's duplicate-row behavior, so it is the natural home for the second (byte-identical) arm rather than, say, `cip-authority-malformed` or a case renamed to a plural form — AC15's `grep -qF` presence check for each literal id is unaffected by a case emitting more than one `pass` line under the same id-bearing block.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` AC15 ("it carries each of these **twenty-five** case ids as a literal... The id list is fixed here rather than left to the engineer"); the coordinating session's rework instruction (quoted above).

- decision: (review round 1 rework — Blocker 1) scope the imperative gate-then-dispatch sequence to the single seam this recipe's own declarations wire an alternate path onto — step 1's `tech-lead` invocation in `skills/run/SKILL.md` — rather than inserting it generically at all six of T-1057's per-phase invocation points.
  reason: the coordinating session's own rework instruction states the branch literally as `check-invocation-path.sh --role tech-lead`, naming the role rather than a `<role>` placeholder; DP-c already records that "`tech-lead` is invoked from exactly one skill file, which is what keeps the new block's consumer set to one," and AC2's own `wires-role codex-cli` cardinality wires only `tech-lead` onto any adapter this gate could ever return something other than `in-process` for — so for the other five roles, the gate would deterministically return `in-process` under every reachable host config this task's own Non-goals and AC9's byte-locks permit, making a generic six-point insertion a no-op everywhere except this one seam. Widening the insertion to all six points would also be scope creep beyond "Rework scope, nothing else." The canonical recipe's own prose stays role-agnostic (it says "any bound role," matching the Blocker's own fix text and AC3's role-generic `check-invocation-path.sh --role` literal); the seam-specific wiring — which literal role, and exactly where in the phase list — lives in a tail sentence appended in `skills/run/SKILL.md` after the canonical `contain`-governed text, mirroring the shape the adjacent Executor resolution (T-1057) paragraph's own consumer-specific "Apply this at..." tail already uses (that tail is NOT part of `templates/prompt-blocks/executor-resolution.md`'s own canonical file — confirmed by reading it — so this is the established precedent, not an invented one).
  grounding: the coordinating session's rework instruction ("(i) run check-invocation-path.sh --role tech-lead"; "Rework scope, nothing else"); `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` DP-c ("tech-lead is invoked from exactly one skill file, which is what keeps the new block's consumer set to one") and `## Notes for engineer` ("The insertion seam is the existing Executor resolution (T-1057) paragraph at :168; the new block's lines go with it as a branch on the resolved adapter token at step 1's tech-lead invocation"); `templates/prompt-blocks/executor-resolution.md` (its own canonical text carries no per-step "Apply this at..." tail; each of its four consumers appends its own).

- decision: (review round 1 rework — Blocker 2) generalize the frozen `payload-briefing` region into a new, separate parametrized template inside `templates/prompt-blocks/alternate-executor-invocation.md` — with `<TASK-REQUEST>` replacing the frozen region's hardcoded T-1117 request string, `<PAYLOAD>`/`<REPO>` kept as the same two placeholders — rather than editing the frozen region itself or writing the template from a blank page.
  reason: the frozen `payload-briefing` region between its own `BEGIN`/`END` markers is this task's own evidence (AC21 byte-locks it against the evidence commit) and must stay byte-identical; the coordinating session's own instruction states this explicitly ("the spec's frozen `payload-briefing` region itself stays byte-identical, it is this task's evidence"). Copying its exact wording (the two-tier scope rule, the write-nothing rule, the three report lines, the `injection-observed` non-licensing disclaimer) into a NEW, un-frozen location and substituting only the one hardcoded sentence that was T-1117-specific (the `TASK:` line's quoted request string) is what "generalized from" means here: every other sentence is unchanged prose, so the two texts read as the same design at two different genericity levels rather than as two independently-authored briefings that could drift apart. Also carried over: the frozen region's own "why a file, never inline" rationale (an inner double quote in the substituted text would terminate the shell's own quoting) — restated rather than re-derived, since the underlying shell hazard is identical for any `<TASK-REQUEST>` value, not particular to T-1117's own request text.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` `<!-- BEGIN/END payload-briefing: T-1118 -->` (frozen, unedited) and its own "The briefing is delivered as a file, and that is the sole delivery path" paragraph (the shell-quoting rationale, restated); AC21 (byte-identity lock on the frozen region against the evidence commit); the coordinating session's rework instruction ("a parametrized briefing template generalized from the spec's frozen `payload-briefing` region... the spec's frozen region itself stays byte-identical, it is this task's evidence").

- decision: (review round 1 rework — Blocker 2) state the telemetry-span recording (`--provider codex --adapter codex-cli --model provider-configured`) as a pointer to the existing Telemetry binding (T-1058) mechanism's own instance for this path, rather than describing or inventing a second, path-specific telemetry procedure.
  reason: T-1058's paragraph already reads "that role's own resolved row" for `--provider`/`--effort`/`--adapter`, and for a `tech-lead` binding resolved to `codex`/`codex-cli`, that resolved row's own fields already ARE `codex`/`-`/`codex-cli`, with the model field read as `provider-configured` per the ratified `templates/prompt-blocks/executor-resolution.md` amendment (v1 decision, ratified) — nothing about this path needs a second telemetry code path or a new flag combination; it needs only an explicit statement that this is the SAME mechanism's own reachable instance, so a reader does not wonder whether a separate procedure was silently omitted.
  grounding: `.shell-team/specs/T-1118-alternate-executor-invocation-path.md` `## Assumptions` (`templates/binding-default.conf`'s own comment on why `codex-reviewer`'s model column reads `provider-configured`); `skills/run/SKILL.md`'s Telemetry binding (T-1058) paragraph (unedited by this task, read live); the coordinating session's rework instruction ("the telemetry span records `--provider codex --adapter codex-cli --model provider-configured`").
<!-- END provenance: T-1118 -->
