<!-- BEGIN provenance: T-1130 -->

- decision: documented `budget.extension_ratifier` as a new standalone comment paragraph inserted after the existing "Enforcement model:" list (rather than folding a fifth bullet into that numbered list, or leaving the field undocumented in the header at all).
  reason: the field's validation-only, split-of-duties shape (guard validates vocabulary; skills apply the board condition) does not fit the "Numeric budget caps enforced whenever > 0" framing the existing list uses for its four bullets — a fifth bullet phrased the same way would misstate what this guard actually decides. A separate paragraph lets the header state plainly that the guard reaches no new decision and that the board condition belongs elsewhere, without bending the existing list's own voice.
  grounding: `bin/loop-guard.sh` header, "Enforcement model:" list (the four existing bullets share one shape: `budget.<key> -> STOP:<reason> (condition)`); spec `## Notes for engineer` ("Update the header comment block's enforcement-model section too, so the file's own documentation matches") — the instruction names the section to update, not the exact insertion shape.

- decision: the accepted-arm suite cases (`T-1130 ratifier-accepted`) each run their contract at both an under-cap iteration (1) and an at-cap iteration (3), rather than only one iteration point per contract.
  reason: AC2's equal-decision-vector requirement covers a seven-invocation matrix including both `CONTINUE|0` and `STOP:max_iterations_reached|3` outcomes; testing only one iteration point per accepted-arm contract in the suite would leave the suite's own `T-1130 ratifier-accepted` PASS tokens silent on whether the STOP outcome is also unchanged, even though AC2's scratch-contract check already covers it — the suite arm is additional, human-legible regression coverage, not the sole evidence, so it is worth exercising both reachable outcomes rather than one.
  grounding: `.shell-team/specs/T-1130-stop-extension-ratifier.md` AC2 (four reachable decision paths asserted present in the reference vector, including `STOP:max_iterations_reached|3`); AC4 (`T-1130 ratifier-accepted` PASS token required, no further shape specified); `## Notes for engineer` ("Name the cases exactly `T-1130 ratifier-accepted` and `T-1130 ratifier-rejected` in the `assert` description" — naming constraint only, not a fixed number or shape of assertions per case).

- decision: placed both new oversight-document sections (`## Who may grant a loop extension` / `## ループの延長を誰が裁定してよいか`) immediately before `## Limits`, i.e. after the *entire* existing "Who may re-freeze a frozen intent block" section including its worked example and sub-headings, rather than immediately after that section's own heading and before its body.
  reason: the spec says "adjacent to the existing 'who may re-freeze' section" without specifying before or after that section's own content; inserting mid-section (after the heading, before the worked example) would visually nest the new "who may grant a loop extension" topic inside the re-freeze section's own body, which is a different concern. Placing it after the whole section keeps both "who may rule on X" sections as siblings at the same heading depth, consistent with how `## Who may re-freeze a frozen intent block` itself sits as a sibling of the sections before it.
  grounding: `.shell-team/specs/T-1130-stop-extension-ratifier.md` `## Notes for engineer` ("Place each new section adjacent to the existing 'who may re-freeze' section in its file" — no before/after specified); AC7's check line (extracts the new section from its own heading to the next `^## `, asserting only content and the pre-existing heading's continued presence — no placement-order requirement beyond "adjacent" is checked).

<!-- END provenance: T-1130 -->
