# T-1053 decision provenance

<!-- BEGIN provenance: T-1053 -->
- decision: exclude this task's own new spec (`.shell-team/specs/T-1053-retro-mechanization.md`) from the base-vs-head criterion diff the full-population-diff method (DP7) prescribes, while still counting it toward the `## Blast radius` population total.
  reason: DP7 requires "every spec in that population has its criteria run … at the base ref and at HEAD, and the two verdict sets are differenced," but this task's own spec does not exist at the base ref (`git show <base>:.shell-team/specs/T-1053-retro-mechanization.md` cannot resolve it — it is untracked-then-newly-committed by this very task), so there is nothing to diff it against. AC9's own `check:` line re-derives the population as a live, unfiltered count of `.shell-team/specs/*.md` with no self-exclusion clause, so any construction that omitted this task's own spec from the *population count* would fail AC9's literal equality assertion the moment the spec is committed (the same class of construction gap T-1052's engineer disclosed for its own AC14). Including it in the population count while excluding it from the base-vs-head diff (since a diff against a non-existent base-side file is not a comparison at all) is the only construction consistent with both AC9's literal check and DP7's own instruction, and is disclosed explicitly in the `## Blast radius` method paragraph rather than left implicit.
  grounding: `.shell-team/specs/T-1053-retro-mechanization.md` DP7 ("every spec in that population…") and AC9's `- check:` line (`for f in .shell-team/specs/*.md; …` with no exclusion); live measurement (`git show 298129c4b67262cb06b843ed9bd0a4e5bd1308f2:.shell-team/specs/T-1053-retro-mechanization.md` fails to resolve; `ls .shell-team/specs/*.md | wc -l` = 58 at HEAD, 57 at the base-ref worktree, the sole difference being this task's own spec).
- decision: disclose the AC10(a) mutation-self-check finding that a same-file duplicate of `BLAST` in `agents/pm-spec.md` reddens AC3 but leaves AC5 green — contradicting AC10(a)'s own frozen prediction ("AC3 and AC5 red, one mutation, two observers") — as a `## Notes for engineer` addendum plus this interventions record, rather than editing the frozen AC10(a) text (forbidden to this role) or stopping the task to escalate.
  reason: `git grep -lF` lists matching *pathnames*, one per file, regardless of how many times the pattern occurs inside that file — so a second, same-file occurrence of `BLAST` is invisible to AC5's file-count check, which only ever asks "does exactly one file match," never "how many times." AC3 independently catches the same mutation because its check separately asserts the pattern's *occurrence count* (`grep -cxFf`) is exactly 1, both whole-file and within the extracted section — so the safety property AC10(a) exists to protect (no second copy of BLAST ships anywhere) is still caught, just by AC3 alone rather than by both named observers. This is a genuine, measured mismatch inside the spec's own frozen intent block, which this role may not edit; the engineer's escalation triggers (per this task's own orchestrator framing) name a fragment's exactly-once contract, a classifier's exit code, a hash moving, or a need to touch a third surface — none of which this finding is, so silent disclosure via the mutable `## Notes for engineer` section plus this record is the correct-weight response, not a stop.
  grounding: this task's own `.shell-team/specs/T-1053-retro-mechanization.md` AC10(a) (the frozen mutation-list bullet naming mutation 5); live measurement (mutation 5 run on a disposable worktree outside the checkout: AC3 FAIL, AC5 PASS after duplicating the `BLAST` line inside `agents/pm-spec.md`); `git grep --help`'s documented `-l`/`--files-with-matches` semantics (list matching pathnames, not occurrence counts).
- decision: root-cause the two predicted-but-unmoved `## Blast radius` rows (T-1051 AC12, T-1052 AC9) down to the specific prior commit responsible, rather than reporting only "already FAIL at base, unrelated to this task."
  reason: DP8 forbids attributing a criterion already red at the base ref to this task's pass, but a bare "already red, not us" disclosure without a named cause would be exactly the kind of unenumerated claim this repository's own corpus (2026-07-14, same-class completeness) and this task's own SING discipline exist to close. `git diff --stat` between each of those two specs' own historical base refs (`e57c287`, `a804703`) and this branch's base ref (298129c) showed a small, real diff (3 insertions/1 deletion) in `agents/pm-spec.md` at both, and `git log --oneline <old-base>..298129c -- agents/pm-spec.md` named exactly one intervening commit, `23908e1` ("docs: promote 2026-08-09 retro lesson candidates into the lessons corpus", PR #215) — the corpus-promotion regeneration of `agents/pm-spec.md`'s generated prompt-block region, entirely unrelated to and predating this task. Naming that commit turns a plausible-sounding disclosure into a falsifiable, re-checkable one.
  grounding: `git diff --stat e57c287 298129c4b67262cb06b843ed9bd0a4e5bd1308f2 -- agents/pm-spec.md` and `git diff --stat a804703 298129c4b67262cb06b843ed9bd0a4e5bd1308f2 -- agents/pm-spec.md` (each: 3 insertions(+), 1 deletion(-)); `git log --oneline a804703..298129c4b67262cb06b843ed9bd0a4e5bd1308f2 -- agents/pm-spec.md` (single result: `23908e1`); `.shell-team/specs/T-1053-retro-mechanization.md`'s own `## Summarized sources` entry for `templates/prompt-blocks/playbook-pm-spec.md` (naming the same regeneration as the reason `agents/pm-spec.md`'s generated region already carries the 2026-08-09 digest).
<!-- END provenance: T-1053 -->
