# T-1081 decision provenance

<!-- BEGIN provenance: T-1081 -->
- decision: adopted D1 as frozen — "checked" means a new checklist item in `agents/pm-spec.md`'s hand-written prose, not a new machine checker; no mechanical gate was built for either the `- base-ref-discriminator:` declaration or the borrowed-vocabulary sweep.
  reason: the spec froze this at D1 with the exact precedent (`agents/pm-spec.md` line 90 and `skills/run/SKILL.md` line 47, the T-1061 declaration gate's own "no checker ships for it yet" sentence) already in the corpus; building a parser here would duplicate the reader issue #250 already scopes and flip this task's own declared `verification-class` from `no-mechanism` to `mechanism`, which is a class-B decision the spec, not the engineer, owns.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` D1 ("Consequence recorded so it is not mistaken for an oversight: neither new item is enforced by an exit code, and both say so in their own text (AC4, and the mirror in AC8).").
- decision: placed both new checklist items in `agents/pm-spec.md` between the existing line-91 item and the `## Frozen intent freeze` heading, and both mirror bullets in `skills/run/SKILL.md` immediately after the T-1061 Adopter-facing-documentation gate bullet (before the Cross-join step bullet), rather than at the end of each shared region.
  reason: the Notes for engineer name line 91 and line 47 as the exact insertion anchors ("Files to touch... after the existing line-91 item and before the Frozen intent freeze heading"; "beside the T-1061 gate bullet at line 47"), and AC1/AC5/AC8's own check lines assert only that the anchor sits strictly between the section's bounding headings — any position satisfying that ordering is conformant, so the position named in the Notes was taken literally rather than re-derived.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` `## Notes for engineer` ("Files to touch, in the order that keeps each verifiable on its own" and "Reuse the existing scoping idiom, do not invent one").
- decision: in `skills/run/SKILL.md`, scoped the declaration-read mirror bullet to the first-freeze branch only, and the borrowed-vocabulary measurement mirror bullet to every verify-then-correct sweep (bootstrap freeze, per-version attestation backfill, class-M path), by writing an explicit "Scope, stated because..." sentence in each bullet, reusing the shape of the existing Cross-join step bullet's own scope sentence rather than inventing a new one.
  reason: the spec's own Notes for engineer state this split explicitly ("the declaration read is a first-freeze duty..., while the borrowed-token measurement belongs to every verify-then-correct sweep the gate runs...") and warn that "a duty written into the shared region without that clause silently acquires branches that have no carrier for it" — the Cross-join step bullet immediately below the insertion point already carries exactly this idiom, so it was reused rather than reworded.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` `## Notes for engineer` ("Reuse the existing scoping idiom, do not invent one."); `skills/run/SKILL.md`'s pre-existing Cross-join step bullet ("Scope, stated because this step's position beside the `attestation` branch is not its boundary...").
- decision: for AC18's `## Blast radius` section, measured the population's criterion-level verdicts by actually running all 177 mechanically-extracted `- check:` lines (from the 49-file population) live, once with this task's own diff reverted via `git stash push -u` and once restored via `git stash pop`, rather than reasoning from the purely-additive diff shape alone or sampling a subset.
  reason: an initial attempt to bound the analysis by structural argument alone (purely-additive diff ⇒ only absence-style and byte-identity-style checks are at risk) was abandoned once a live run surfaced a real instrumentation bug (a `timeout` wrapper not installed on this host, producing a false uniform-failure signal) — the corpus's own recurring lesson ("A verification command needs... a positive control" / "a completeness claim is written only as the extraction command plus its pasted output") applied directly: only a live, differenced run of the actual population, with the instrumentation bug caught and fixed before trusting its output, is evidence: the corrected run shows the diff changed exactly this task's own 9 new criteria and nothing in any sibling spec.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` `## Blast radius` ("**63/177 passed at the branch point, 72/177 passed at HEAD**... the only 9 lines whose verdict changed are this spec's own... AC1–AC9"); `.shell-team/lessons.md`, 2026-08-15 — "A completeness claim is written only as the extraction command plus its pasted output".
- decision: scoped the AC18 indirection-class discharge for the `agents/*.md` glob category to the two live check lines found in `T-1055-adapter-envelope.md` (lines 152, 163) and declared the `bin/team-paths.sh --get`-built-path category **not applicable** on measured grounds, rather than running the 185-line `team-paths.sh --get` population.
  reason: `team-paths.sh --get` resolves per-repo base-dir artifacts (`todo`, `specs`, `reviews`, `interventions`, …) — none of its resolvable keys names a plugin-shipped agent or skill file — so no criterion built from it can reach `agents/pm-spec.md` or `skills/run/SKILL.md`; this was measured by reading `bin/team-paths.sh --get`'s own key set rather than assumed, and disclosed in the Blast radius section as the third named-and-discharged indirection sub-class.
  grounding: `bin/team-paths.sh` (its `--get` key enumeration: `todo|specs|runs|retros|reviews|loops|interventions|base`, none of which resolves to an `agents/` or `skills/` path); `.shell-team/specs/T-1081-freeze-sweep-hardening.md` `## Blast radius` ("(iii) A path built at run time from `bin/team-paths.sh --get`: measured **not to apply**...").
- decision: documented the `- base-ref-discriminator:` declaration and the borrowed-vocabulary sweep as a new `## Declaring...` section placed immediately before `## Operating rules` in both `docs/adopting.md` and `docs/adopting.ja.md`, reusing the existing `## Declaring adopter-facing documentation` section's shape (duty-not-checker sentence, allowed-values enumeration, parameterized rather than hard-coded convention) rather than folding the new content into that existing section.
  reason: AC14's adopter-surface note names "the `## Declaring adopter-facing documentation` neighbourhood" as the shape to match, and the two declaration keys are independent freeze-time duties (one about a base-ref expression, one about a premise-classification sweep) rather than a sub-case of the adopter-facing-documentation gate, so a sibling section beside it — rather than a sub-bullet inside it — keeps each duty separately anchorable for a future edit.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` **AC14** ("adopter-surface: `docs/adopting.md` (the `## Declaring adopter-facing documentation` neighbourhood... )"); `docs/adopting.md`'s pre-existing `## Declaring adopter-facing documentation` section (the section shape reused).
- decision: in the two new `agents/pm-spec.md` checklist items, spelled `not-applicable — <reason>` and every other AC2/AC3/AC6/AC7 literal exactly byte-for-byte as quoted in the spec's own check lines (copied via the spec's own grep output rather than retyped from memory), including the em dash character.
  reason: AC2/AC3/AC4/AC6/AC7 are `grep -qF` fixed-string checks against exact substrings including a non-ASCII em dash (`—`, U+2014); a hand-retyped hyphen or en dash would silently fail the check with no diagnostic distinguishing "wrong text" from "wrong character", so each literal was extracted with `grep -n` from the frozen spec before being pasted into the target file.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` **AC4**'s check body (`grep -qF -- "$s" agents/pm-spec.md` over the literal `'not-applicable — <reason>'`); this task's own extraction command (`grep -n "not-applicable" .shell-team/specs/T-1081-freeze-sweep-hardening.md`) run before editing `agents/pm-spec.md`.
- decision: (rework round 1, Codex review round 1 Major 1) widened the branch-existence-test guidance in all four norm files (`agents/pm-spec.md`, `skills/run/SKILL.md`, `docs/adopting.md`, `docs/adopting.ja.md`) to name a second ref namespace (`refs/remotes/<remote>/<predecessor-branch>`) alongside the existing `refs/heads/<predecessor-branch>`, rather than switching to a different existence-test tool or command.
  reason: the review's own suggested fix said "broaden the existence test to also recognize a remote-tracking ref... rather than switching tools," and AC2's frozen literals (`git merge-base "<predecessor-branch>" HEAD`, `explicit branch-existence test`) and AC9's one-notation rule (`git merge-base` + `git show-ref --verify --quiet` present in all four files, no second notation) both survive a widened *guidance sentence* around the same `git show-ref --verify --quiet` command but would not survive introducing a second command (e.g. `git rev-parse --verify`) as an alternate notation.
  grounding: `.shell-team/reviews/T-1081.md` Major 1 ("Suggested fix: broaden the existence test to also recognize a remote-tracking ref... rather than switching tools"); `.shell-team/specs/T-1081-freeze-sweep-hardening.md` **AC2**/**AC9**'s own check bodies (unchanged literals, re-verified green after this edit).
- decision: (rework round 1, Major 1's second sub-finding) disclosed the rebased/force-pushed predecessor as a **second, co-equal route-back**, worded identically in shape to the existing "deleted without being merged" disclosure at every site that already carried the first case, rather than writing a single combined sentence covering both.
  reason: the re-frozen `## Input space`'s own Out-of-scope element (v1→v2 delta) disclosed the two cases as parallel, separately-named bullets ("on the same footing as an unmerged deletion"), and the coordinator's rework brief named this "now two cases, not one" — mirroring that shape (two named cases, same disposition, same "route-back" vocabulary) into the norm text keeps the shipped duty's wording congruent with the frozen spec's own resolution rather than inventing a new phrasing for the norm alone.
  grounding: `.shell-team/specs/T-1081-freeze-sweep-hardening.md` `## Input space` Out-of-scope element, v2 ("A predecessor branch **rebased or force-pushed after this branch was cut**... Disclosed as a route-back on the same footing as an unmerged deletion"); coordinator rework brief, Class 2.
- decision: (rework round 1, Class 3 / Major 2's SKILL.md-mirror fix) added one sentence to `skills/run/SKILL.md`'s declaration-read mirror bullet stating the accepted declaration form is decidable from the spec's own measured stack state (reads a base-side blob AND has an open predecessor ⇒ the two-arm expression; otherwise ⇒ `not-applicable`), and added an equivalent alignment sentence to `agents/pm-spec.md`'s authoring-side item and to both `docs/adopting*.md` files, rather than fixing only the SKILL.md gate the review named.
  reason: the review's suggested fix named only "add one sentence to the SKILL.md mirror gate," but the coordinator's Class 3 instruction widened this to "the two shipped items and both mirrors" and warned QA would verify "cell-by-cell (D4 semantic equivalence)" — leaving the authoring-side item and the adopter docs silent on the same ordered rule the reading gate now states would create exactly the same "no declared rule tying form to state" gap the review found, one level removed (an author reading only `agents/pm-spec.md` would still have no textual rule to follow when choosing a declaration form).
  grounding: `.shell-team/reviews/T-1081.md` Major 2 ("add one sentence to the SKILL.md mirror gate tying the accepted declaration form to the spec's measured stack state"); coordinator rework brief, Class 3 ("Read the two shipped items and both mirrors against it... QA will verify this cell-by-cell (D4 semantic equivalence)").
- decision: (rework round 1, Class 4) fixed only `docs/adopting.ja.md:355-356`'s "engineer して回避する" and left every other English-verb-plus-suru-conjugation instance in the same new section (`stack され`, `spell される`, `resolve しなくなった`, `coin した`, `classify する`, `enumerate され`, `instantiate した`, `refuse する`) unchanged, after sweeping the whole section and cross-checking those forms against the document's own pre-existing (lines 1-318) usage of the identical pattern.
  reason: `grep`-swept lines 1-318 of the same file show `pin してください`, `refuse されます`, `probe する`, `fallback すること`, `enforce される`, `discharge される`, `pass すること` already established as this document's own convention for English technical-action verbs; "engineer" is the one word in the flagged sentence that functions as a noun (a person's role) rather than an action verb in ordinary usage, so attaching a direct suru-conjugation to it breaks in a way the other, already-conventional forms do not — treating the whole class as broken would mean rewriting an established, already-shipped house style rather than fixing the one genuine defect.
  grounding: `.shell-team/reviews/T-1081.md` Minor ("`docs/adopting.ja.md:355-356` — garbled bilingual text... leaves the English verb 'engineer' untranslated with Japanese conjugation attached"); this task's own sweep command (`grep -nE '[a-zA-Z]+ ?(して|した|する|される|します|しない|でき|され|しなく)' docs/adopting.ja.md` run over both the pre-existing lines 1-318 and the new lines 319-398, compared for pattern precedent).
<!-- END provenance: T-1081 -->
