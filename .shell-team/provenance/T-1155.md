<!-- BEGIN provenance: T-1155 -->

- decision: Place the `reviews/` mention on the same physical line as `<base>/.gitignore` in `bin/team-init.sh`'s `--help` text (wrapped as a continuation of that one line's sentence), rather than as a separate following line.
  reason: AC4's check extracts only the single line matching `<base>/.gitignore` (`grep -F -- '<base>/.gitignore' "$T/h"`) and then greps that extracted line alone for `reviews/`; a mention on a separate line would never be seen by the second grep and the criterion would fail even though the text is present in the help output as a whole.
  grounding: `.shell-team/specs/T-1155-gitignore-published-raw-captures.md` AC4's `- check:` line (`grep -F -- '<base>/.gitignore' "$T/h" | grep -qF -- 'reviews/'`).

- decision: Replace `bin/team-init.sh`'s closing-summary line (formerly ending "commit the rest if you want it tracked") with one new sentence naming both ignored artifact classes (telemetry and published raw review captures) and the still-trackable curated record, rather than appending a second, separate printf line.
  reason: AC4 requires both that `commit the rest` no longer appears anywhere in the script and that a single printed line names `.gitignore` and `reviews/` together; rewriting the existing line satisfies both without adding a redundant second announcement of what the base `.gitignore` covers.
  grounding: `bin/team-init.sh` pre-task line (`printf 'Telemetry (%s/) is ignored via %s/.gitignore — commit the rest if you want it tracked.\n' ...`, read at the branch point via `git show c168eb63:bin/team-init.sh`); spec AC4's `- check:` line.

- decision: Author the three new fixture cases (`raw-captures-ignored-default`, `-legacy`, `-override`) in `tests/team-init/run.sh` as git-initialized targets under the suite's pre-existing `$GIT_TMP` root, sharing one `assert_raw_captures_ignored()` helper that derives the reviews/runs paths per case through `bin/team-paths.sh --root <target> --get reviews|runs`, rather than reusing the suite's existing plain (non-git) `$TMP`-rooted `T_OV`/`T_LEG` targets.
  reason: `git check-ignore` needs a real git repository, and `.shell-team/test-recipe.md`'s T-1042 entry documents that a `git init`-needing fixture built under the plain `$TMP` root fails with "Operation not permitted" in this sandbox (copying `.git/`'s hook templates); the suite's own pre-existing `$GIT_TMP` root and its T-1097 comment already exist for exactly this class of fixture, so reusing that root — with fresh, dedicated targets per layout — is the documented pattern rather than a new mechanism.
  grounding: `.shell-team/test-recipe.md` T-1042 entry ("a fixture suite that needs a real throwaway `git init` repository ... must NOT create it under `$HERE/tmp`"); `tests/team-init/run.sh`'s own `$GIT_TMP` construction and its adjacent T-1097 comment.

- decision: Add the migration guidance to `docs/adopting.md` / `docs/adopting.ja.md` as its own new blank-line-delimited paragraph placed immediately after the existing "Your host root is left untouched" paragraph, rather than folding the guidance into that existing paragraph's own text.
  reason: AC4's check only requires that some one paragraph (an `awk -v RS=` paragraph unit) in the file carry the migration tokens together (`reviews/*.txt`, `reviews/*.jsonl`, `--force`, `<base>/.gitignore`, and `history`/`履歴`); keeping it as a separate paragraph avoids disturbing the existing paragraph's other wording and matches the Notes-for-engineer placement guidance ("near 'Your host root is left untouched'") without requiring a re-derivation of what that paragraph already asserts.
  grounding: `.shell-team/specs/T-1155-gitignore-published-raw-captures.md` AC4's `- check:` line (the `awk -v RS= ... index($0, ...)` paragraph test) and its `## Notes for engineer` placement note.

<!-- END provenance: T-1155 -->
