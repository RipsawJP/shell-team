<!-- BEGIN provenance: T-1030 -->

- decision: `.shell-team/interventions/T-1030.md` was **not created** by this engineer pass, even though `bin/check-acs.sh`'s AC8 requires it to exist and this leaves the spec's overall check-acs run at 10 passed / 1 failed (AC8) / 1 skipped rather than the 11/0/1 shape the spec allows for.
  reason: The task prompt that dispatched this engineer pass states explicitly that this file is orchestrator-owned and instructs not to create it, and the spec itself (Notes for engineer, step 4) lists only `.shell-team/provenance/T-1030.md` as the engineer's record to write. Two options existed: create a sentinel file myself to reach the numeric target, or honor the explicit boundary and report the resulting gap. I chose the latter — the interventions file records interventions across the task's whole lifecycle, a scope this single engineer pass cannot see in full, and writing it myself would assert a "no interventions occurred" claim on visibility I do not have.
  grounding: this task's own dispatching prompt ("Do NOT create `.shell-team/interventions/T-1030.md` (orchestrator-owned; AC8 will go fully green after the orchestrator adds it...)"); spec `.shell-team/specs/T-1030-reviewer-board-write-boundary.md`, `## Notes for engineer`, Order of work item 4 (names only the provenance record for the engineer) and AC8's own `check:` line (the `else rc=1` branch on `.shell-team/interventions/T-1030.md`'s absence).

- decision: AC12(c)'s "does commit `9d82a49`'s message match the fragment quoted in F1 byte for byte" duty was resolved as **confirmed match, once the commit message's own hard line-wrap is unwrapped to a single line** — not as a literal whole-message substring match and not as a mismatch.
  reason: `git show -s --format=%B 9d82a49` returns the relevant paragraph as `(also reverts the reviewer's premature checkbox flip — the close-out\nscript owns the [x] transition; recorded for the retro)` — a hard-wrapped commit body with a leading paren and a trailing `; recorded for the retro)` that F1 does not quote. Joining the two physical lines with a single space and comparing only the substring F1 actually quotes (`also reverts the reviewer's premature checkbox flip — the close-out script owns the [x] transition`) gives an exact character-for-character match, including the em-dash and the apostrophe. Treating "byte for byte" as requiring the raw, still-wrapped bytes to match would make this AC unsatisfiable by construction for any wrapped commit message quoted as a fragment, which is not what the spec's own Summarized-sources entry for this commit already discloses (it explicitly calls out that F1 quotes "the fragment of its message that names the revert," not the whole body).
  grounding: `git show -s --format=%B 9d82a49` (run live this session); `.shell-team/specs/T-1030-reviewer-board-write-boundary.md` `## Summarized sources`, second bullet ("Commit `9d82a49`'s message — quoted as provenance in F1... quoting a fragment it was given"); F1's own frozen text, provenance clause ("whose message records \"also reverts the reviewer's premature checkbox flip — the close-out script owns the [x] transition\"").

- decision: AC12(c)'s "does issue #134's body actually carry the detect-and-revert premise this spec corrects" duty was resolved as **measured/confirmed** (not `environmentally-unverified`), by fetching the issue directly with `curl -s -H "Accept: application/vnd.github+json" https://api.github.com/repos/RipsawJP/shell-team/issues/134`.
  reason: The spec's Assumptions anticipate this claim ending `environmentally-unverified` for "any role without network access," grounded in pm-spec's own no-network constraint. This engineer pass is not under that constraint in this sandbox, and `api.github.com` is an allow-listed host, so `curl` succeeded where the dispatching prompt itself predicted `gh` would fail on TLS. The issue body reads "The close-out pass detected and reverted it (git commit `9d82a49`...)" — which is exactly the detect-and-revert premise the spec's `## Summarized sources` measures as false against `bin/close-out.sh`'s actual behaviour. Reporting this as `environmentally-unverified` would understate the verification actually performed and would misclassify a reachable, already-checked fact as unreachable.
  grounding: live `curl` response captured this session (issue #134 title: "codex-reviewer flipped a board checkbox that close-out.sh owns — make the write boundary structural, not detect-and-revert"; body opens "The close-out pass detected and reverted it (git commit `9d82a49` ... also reverts the reviewer's premature checkbox flip)"); `.shell-team/specs/T-1030-reviewer-board-write-boundary.md` `## Summarized sources`, first bullet (the corrected-premise disclosure) and `## Assumptions`, relayed-claim 1.

<!-- END provenance: T-1030 -->
