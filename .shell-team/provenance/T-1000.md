# T-1000 decision provenance

<!-- BEGIN provenance: T-1000 -->

- decision: round 6 (v6 re-freeze, supersedes the original wording of this entry) — `CLAUDE.md`'s single pointer bullet names exactly the three content areas `CONTRIBUTING.md` currently documents (the pull-request flow, confirming the CI check is green, and the board line format) as backticked `` `## …` `` heading tokens, rather than a bare "see CONTRIBUTING.md" pointer with no topic list or the four-area wording that survived the round-5 section removal.
  reason: a bare pointer would satisfy AC17's shape assertions (one bullet, names `CONTRIBUTING.md`, no second top-level line) just as it would have before, so naming topics at all is still not checker-forced on that half — but AC17 gained a **correspondence** half this round: each backticked token is read out of the bullet at check time and looked up as a real heading in `CONTRIBUTING.md`, which is only checkable because the bullet names its destinations as those exact tokens. The four-area wording this entry originally described is the reason that half exists: the release procedure was deferred out of `CONTRIBUTING.md` at v5, and the pointer bullet went on naming it anyway, because nothing had verified that a topic it named still had a section. Naming exactly the three headings that exist right now, in the token form the checker reads, is what keeps the pointer honest to the file as it stands rather than to its history.
  grounding: AC17 check ("`hs=\"$(printf '%s\n' \"$s\" | grep -oE '`## [^`]+`' | tr -d '`')\"` ... `grep -qxF \"$h\" CONTRIBUTING.md`" — the correspondence lookup); AC17's body ("The bullet also has to point at something that exists... the topics are read out of the bullet at run time... every one of them must match a real heading in `CONTRIBUTING.md`"); the Notes-for-engineer worked example for the replacement bullet.

- decision: the five new sections are inserted at the exact point the spec's Notes for engineer name — after `## About CI on your pull request` and before `## Where the behavior is documented` — rather than at the end of the file (after `## On the absence of a code of conduct`), which would equally satisfy AC1's presence-only heading checks and AC2's zero-deletion insert-only requirement.
  reason: appending at the file's end would satisfy every AC that checks presence, but would separate the new operational content from `## About CI on your pull request`, the section it most directly extends (the CI section says a check can fail for repository-convention reasons; the new "Confirming the CI check is green" section is the mechanics of that same check). Placing the new sections immediately after keeps a reader moving from "a check can fail" straight into "here is how to read that check," instead of past two unrelated sections (`## Where the behavior is documented`, `## On the absence of a code of conduct`) first.
  grounding: spec's "Notes for engineer" ("Where the new sections go in `CONTRIBUTING.md`: after `## About CI on your pull request` and before `## Where the behavior is documented`"); AC1 (heading presence, no order assertion) and AC2 (zero deleted lines) — both are equally satisfied by either insertion point, so the choice of point is a readability decision rather than a checker-forced one.

- decision: the worked board-line example (the fenced `markdown` block containing `T-1042`) sits between the "notes go in indented sub-bullets" bullet and the "lint the board before pushing" bullet inside `## The board line format`, rather than after all five canonical bullets.
  reason: the three bullets before the example describe the shape and its two failure-mode rules (no parenthetical, notes in sub-bullets); the concrete example demonstrates exactly that shape holding a note in a sub-bullet, so placing it immediately after those three keeps the abstract rule and its concrete instance adjacent, before the two bullets that are about *acting on* the format (linting it, and where the flag vocabulary lives) rather than describing it.
  grounding: AC12 (five `grep -qxF` whole-line matches, none order-dependent); AC13 (`grep -m1 -F -- '- [ ] **T-1042**'` extracts the line regardless of its position in the file, since it is the sole occurrence).

- decision: AC8's decision-disclosure bullet ("the promotion and the tag are a maintainer decision, not an observed practice") is placed immediately after the tag bullet and before the install/changelog pointer bullet inside `## Cutting a release`, rather than at the very end of the section.
  reason: the disclosure modifies exactly the two claims immediately preceding it (promote-by-pull-request, tag-after-merge) and not the pointer bullet that follows (install side, changelog, README — all independently grounded per R6). Placing the caveat directly after the claims it qualifies, rather than after an unrelated pointer bullet, keeps the modifies-what relationship legible without needing to name the claims a second time.
  grounding: spec's claim-to-evidence table rows R4/R5/R7 (R7: "The promotion and tag are a maintainer decision, not an observed practice" grounded against R4/R5, the promote and tag bullets); DP-5 ("The release procedure is not measured at all... the disclosure bullet AC8 pins").

- decision: round 2 (v2 re-freeze) — the changelog bullet AC28 requires is placed immediately after the badge-bump bullet and before the "Nothing else is machine-checked" bullet inside `## Cutting a release`, rather than immediately before the promotion bullet (i.e. after "Nothing else is machine-checked" instead of before it).
  reason: the spec's Notes for engineer pin the bullet's position only loosely ("after the badge-bump bullet and before the promotion bullet"), and two bullets already sit between those two anchors, so either side of "Nothing else is machine-checked" satisfies that instruction. Placing the changelog step directly after the badge bump groups the two release-mechanics steps a maintainer actually performs in order (bump the badge, then add the changelog entry) before the disclosure bullet ("Nothing else is machine-checked") that comments on what is *not* enforced about either of them — reading as steps-then-caveat rather than interleaving a caveat between two steps.
  grounding: spec's "Notes for engineer" ("The changelog bullet (AC28) goes inside `## Cutting a release`, after the badge-bump bullet and before the promotion bullet. Its position is not pinned mechanically"); AC28 check (whole-line `grep -qxF` on the bullet text only, no position assertion); Body-to-AC correspondence row "The changelog bullet sits after the badge bump and before the promotion bullet | info-only (not promoted to AC) ... AC28 pins the text, not its position."

- decision: round 2 (v2 re-freeze) — the two AC27 replacement lines are written as two separate paragraphs (a blank line between them), rather than as two physical lines with no blank line between, inside `## About CI on your pull request`.
  reason: AC27's check matches each line independently via whole-line `grep -qxF`, so a blank line between them is not required by any AC; the choice is purely about how the section reads. A blank line marks them as two distinct claims (what CI runs, and what it deliberately does not run against this tree) rather than one continuous sentence group, matching the prose-paragraph convention the rest of this document already uses (compare the two-paragraph split immediately below, in the retained "If a check fails..." text).
  grounding: AC27 check (two independent `grep -qxF --` whole-line matches, no adjacency or blank-line assertion); AC2 check (deletion is scoped to the base blob's four-line paragraph by content anchor; it does not constrain what replaces it beyond the two canonical lines being present verbatim).

<!-- END provenance: T-1000 -->
