<!-- BEGIN provenance: T-1015 -->

- decision: the unpinned intro line of the new `## Cutting a release` section reads "The steps of a release, written from the release this repository has actually run." rather than any of the several other true, equally short phrasings that came to mind while drafting (e.g. one naming the operator, one naming a wrapper, one naming where the procedure runs).
  reason: the spec leaves this one sentence's wording to the engineer by design (it is explicitly called out as unpinned), but it still sits inside AC11's slice of the section, which forbids `host`/`terminal`/`laptop` (case-insensitive) and the standalone word `gh`, and separately requires the slice to contain the phrase `semantic versioning` somewhere (satisfied by the "Measure the release before naming it" bullet, not the intro). A phrasing naming who runs a step or where it runs would have tripped the vocabulary lock this task exists to keep clean (Non-goals: "Environment-specific prose"), so the chosen sentence states only that the section exists and where it came from — nothing about who executes it or on what.
  grounding: .shell-team/specs/T-1015-cutting-a-release.md, Notes for engineer ("Give it one unpinned intro line in the style of the other sections … The intro line is not pinned, but it is inside AC11's slice, so keep the three nouns and the one tool out of it too."); AC11's check: pattern (the `for t in host terminal laptop` loop and the `grep -qwF -- 'gh'` guard).

- decision: `.shell-team/reviews/T-1015.md` is created by the engineer, before hand-off, as a placeholder record rather than left absent for QA to create (the precedent T-1013 followed, where the equivalent criterion was reported as an expected, disclosed FAIL because the reviews file is QA's own artifact).
  reason: this spec's own Notes for engineer explicitly instruct writing all three record files, "including the interventions record … This task dogfoods the line it adds," and AC9's `check:` line asserts `test -r .shell-team/reviews/T-1015.md` as one of eleven criteria this task's own orchestrator instructions require to report PASS at hand-off (not the disclosed-FAIL shape T-1013 used). Since this task's whole deliverable is the convention that the interventions/provenance/review records are complete before a board entry moves, leaving this file un-created would make T-1015 fail the very bullet it is adding on its own hand-off. The placeholder states plainly that it is a stand-in for the qa-verifier's real verification record, so it cannot be mistaken for a completed QA pass.
  grounding: .shell-team/specs/T-1015-cutting-a-release.md, Notes for engineer ("write all three, including the interventions record as a zero-entry sentinel if nothing interrupted the task. This task dogfoods the line it adds."); AC9's check: line (`test -r .shell-team/reviews/T-1015.md`); contrast case `.shell-team/todo.md`'s T-1013 Done entry, "engineer (2026-08-01): … `.shell-team/reviews/T-1013.md` intentionally not created — QA's artifact."

<!-- END provenance: T-1015 -->
