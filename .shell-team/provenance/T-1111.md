<!-- BEGIN provenance: T-1111 -->
- decision: disclose the frozen `## Probe evidence (raw, orchestrator-produced)` section's own missing `- command: ` grammar (zero lines matching `^- command: ` inside that section, against AC4's requirement of at least two) as an honest, unrepaired AC4 partial-FAIL in this note's own `## Limits and what is not computable`, rather than attempting to satisfy AC4's evidence-scoped assertion by any other means.
  reason: two paths existed once the gap was found — (a) treat it as an engineer-owed repair and add `- command: ` lines somewhere the evidence-scoped `awk` extraction would still pick up, or (b) leave the frozen section untouched and report the gap. (a) is foreclosed on its own terms: AC2 requires the extracted `## Probe evidence` section to stay byte-identical to the committed evidence commit, the Non-goals bar any edit to that section, and "Notes for engineer" states this role holds no authority over it ("treat it as read-only input"). No wording choice inside sections this role does own (Probe protocol, Limits) can satisfy an assertion scoped to a different, frozen section by the check's own `awk` boundary. The honest disclosure is therefore the only conformant action, and restating the same commands (already present in the evidence's own prose, just untagged) as `- command:` lines under `## Probe protocol` satisfies the note-wide (not evidence-scoped) half of AC4 without touching the frozen bytes.
  grounding: `.shell-team/specs/T-1111-plugin-role-agent-concurrency.md` AC4's check line (`test "$(grep -cE '^[[:space:]]*- command: .' "$T/ev" || true)" -ge 2 || rc=1` where `$T/ev` is the `awk`-extracted `## Probe evidence` section only); the same spec's `## Non-goals` ("No edit to any `## Probe evidence` section of any existing note"); `## Notes for engineer` ("treat it as read-only input"); live reproduction — `awk '/^## Probe evidence \(raw, orchestrator-produced\)$/{f=1;next} f&&/^## /{exit} f' docs/loop-engineering/plugin-role-agent-concurrency.md | grep -cE '^[[:space:]]*- command: .'` → `0`.

- decision: STOP before running the full 105-item `t1111-read-set` union as a base/head sweep, and instead measure only the 7-item `phasemux+agentconc+harnessconc+indirect` bucket (the specs whose criteria read all three edited notes together) directly in this engineer round, naming the remaining 98 items as unrun rather than silently omitted.
  reason: the derived population resolves to 105 of 116 specs in the corpus — corpus-scale, dominated entirely by the generic `indirect` set (any spec whose check line invokes `bin/team-paths.sh --get`) rather than the tighter notes-referencing subset this task's Blast radius method was priced for. This role's own task briefing states explicitly that a corpus-scale resolution is a STOP-and-report condition, and `.shell-team/interventions/T-1110.md` entry 3 rules that a fan-out's execution host is part of the recorded procedure (coordinating-session-hosted, harness-tracked background slices) rather than a free variable available to a single engineer round. Measuring the small, directly-relevant 7-item bucket instead of doing nothing keeps the table's three named rows verified rather than merely predicted, without incurring the corpus-scale sweep this role's own briefing forecloses.
  grounding: this task's engineer briefing ("If the derived population resolves to the full merged-spec corpus rather than the notes-referencing subset, STOP and report back before running it — the coordinating session owns that fan-out per the executor-hosting ruling (interventions/T-1110)"); `.shell-team/interventions/T-1110.md` entry 3 (coordinating-session-hosted, harness-tracked execution; sub-agent-hosted fan-out prohibited until piloted); live reproduction — `bash bin/derive-populations.sh --label t1111-read-set ...` (per the spec's own `- reproduce:` line) → `- union: items: 105`; `ls .shell-team/specs/*.md | wc -l` → `116`.

- decision: write the `- unit-duration:` and `- launch-latency:` fields in the new note as raw nanosecond integers (equal to `end − start` / the per-arm-maximum derivation, respectively), not converted to milliseconds as T-1073's own sibling note does for the same fields.
  reason: T-1073's own note (`harness-agent-concurrency.md:311-313`) records these fields in milliseconds, but this task's own AC8 check line compares `- unit-duration:` directly against `10#$ue - 10#$us` and `- launch-latency:` against a per-arm maximum, both computed from the nanosecond `- unit-timing:`/`- batch-timestamp:`/`- agent-timestamp:` integers with no unit conversion anywhere in the check. Reusing T-1073's millisecond convention would make the declared field fail the equality assertion the check actually performs; the two sibling notes are allowed to differ here because each note's own spec's `- check:` line is the authority for that note's own grammar, not the other note's precedent.
  grounding: `.shell-team/specs/T-1111-plugin-role-agent-concurrency.md` AC8's check line (`test "$((10#$ud))" = "$(( 10#$ue - 10#$us ))"` and the analogous per-arm-maximum equality for `- launch-latency:`, both against nanosecond inputs with no ms conversion); live reproduction — the derivation script (`bin/derive.sh`, this round's scratch script) computed `unit_duration=200463930000` and `launch-latency=32887049000` directly from the evidence's own nanosecond integers, and `CHECK_ACS_TIMEOUT=900 check-acs.sh .shell-team/specs/T-1111-plugin-role-agent-concurrency.md` reports AC8 `PASS` with these values written verbatim into the note.

- decision: set the declared `- margin-factor:` field to `3` (the frozen protocol's stated floor) rather than the actual computed ratio (≈6.10×), reporting the ratio separately in prose.
  reason: the frozen protocol states the requirement as "a declared margin factor of **at least 3** — a stated policy choice rather than a measurement"; T-1073's own sibling note follows the identical convention (`harness-agent-concurrency.md:313`, "margin-factor: 3 — the frozen floor stated in the protocol (a policy choice, not a measurement)"), and AC8's own check only requires `margin-factor >= 3` and the unit-duration/launch-latency comparison against it — it does not require the field to equal the computed ratio. Writing the actual ratio into the field itself would conflate the policy threshold with the measured outcome the threshold gates.
  grounding: `.shell-team/specs/T-1111-plugin-role-agent-concurrency.md` frozen `<!-- BEGIN probe-protocol: T-1111 -->` region, "Unit" paragraph ("a declared margin factor of at least 3 — a stated policy choice rather than a measurement"); `docs/loop-engineering/harness-agent-concurrency.md:313` (`- margin-factor: 3 — the frozen floor stated in the protocol (a policy choice, not a measurement)`); AC8's check line (`test "$((10#$mfac))" -ge 3`).
<!-- END provenance: T-1111 -->
