<!-- BEGIN provenance: T-1101 -->
- decision: `temp-session` is implemented as two top-level variables — `RE_TEMP_SESSION_ROOT` (the root alternation plus ordinary path characters, unanchored) and `RE_TEMP_SESSION` (composing `RE_TEMP_SESSION_ROOT` with the dashed-UUID requirement) — rather than as a single flat pattern.
  reason: AC5 requires both negative-control fixtures to be *proven*, not assumed, to "match the temp-root half of the temp-session rule ... while the whole rule does not match them," read out of the checker's own source in the shape `assert_reaches_email_candidates` established. That idiom needs a named, extractable sub-regex representing "looks like a temp/session path" independent of the UUID requirement — exactly the role `RE_EMAIL_BASE` plays for the email exclusions (the base shape is extractable and testable on its own, separately from the domain exclusions that filter it). A single, non-decomposed `RE_TEMP_SESSION` pattern would give the test suite nothing to extract for that half. The composition also mirrors the repository's own existing precedent (`RE_HOME_PATH = RE_HOME_PATH_BOUNDARY + RE_HOME_PATH_RAW`), which the spec's own "Prior art to imitate closely" note names.
  grounding: `.shell-team/specs/T-1101-pii-path-shapes.md` AC5 ("read out of the checker's own source in the shape `assert_reaches_email_candidates` established") and `## Notes for engineer` ("Prior art to imitate closely: ... `assert_reaches_email_candidates` ... for AC5's precondition"); `bin/check-pii-shapes.sh`'s own `RE_HOME_PATH="${RE_HOME_PATH_BOUNDARY}${RE_HOME_PATH_RAW}"` composition (pre-existing) and `RE_EMAIL_BASE`'s role in `assert_reaches_email_candidates` (`tests/check-pii-shapes/run.sh`, pre-existing lines ~401-419).

- decision: the AC5 negative-control fixture representing "a committed `.shell-team/runs/<file>` relative-path citation" is composed as ONE line that names both a `.shell-team/runs/` file citation AND a genuine machine-local temp-root citation (`/private/tmp/claude-502/<worktree>-scratch-worktree`), rather than as a bare relative path with no temp-root text at all.
  reason: AC5's own text requires this fixture to be *asserted to match the temp-root half* of the rule. A bare `.shell-team/runs/<file>` relative path contains no `/private/tmp/`, `/tmp/` or `/var/folders/` literal by construction, so it structurally cannot match any sub-regex requiring one of those roots — a fixture built that way could never satisfy AC5's own stated precondition. Composing a realistic sentence that cites both conventions together is also the shape this corpus's own records actually take (measured: `.shell-team/reviews/T-1098.md` and `.shell-team/todo.md` routinely combine a `.shell-team/runs/` citation with nearby session-scratch prose in the same paragraph), so the fixture is not an artificial contrivance.
  grounding: `.shell-team/specs/T-1101-pii-path-shapes.md` AC5 ("Two negative-control fixtures ... are each asserted to match the temp-root half of the temp-session rule ... while the whole rule does not match them"); measured directly in this session — `git grep -n -- '.shell-team/runs/' .shell-team/reviews/T-1098.md .shell-team/todo.md` shows `.shell-team/runs/` citations co-occurring with `/private/tmp/claude-502/` session-scratch prose in the same records this task's own `## Summarized sources` already cites.

- decision: the AC3 mutation lock for `temp-session` neutralises `RE_TEMP_SESSION_ROOT` (the base/root variable), not the composed `RE_TEMP_SESSION` variable that `scan_content_file()` actually passes to `report_pattern_lines`.
  reason: mirrors the repository's own existing precedent for `home-path`, whose AC3-equivalent mutation lock (pre-existing, `tests/check-pii-shapes/run.sh`) neutralises `RE_HOME_PATH_RAW` — the base shape — rather than the composed `RE_HOME_PATH` variable that is actually passed to `report_pattern_lines`. Since bash evaluates top-to-bottom, rewriting `RE_TEMP_SESSION_ROOT`'s own assignment line in a throwaway copy still neutralises the composed `RE_TEMP_SESSION` at the point it is assigned, so the effect on the reported finding is identical either way; targeting the base/root variable keeps the new mutation consistent with the one precedent already in this file for a composed pattern.
  grounding: `tests/check-pii-shapes/run.sh`'s pre-existing `assert_neutralised_pattern_unreported RE_HOME_PATH_RAW ...` call (not `RE_HOME_PATH`); `bin/check-pii-shapes.sh`'s `RE_TEMP_SESSION="${RE_TEMP_SESSION_ROOT}[0-9a-fA-F]{8}-..."` composition (this task); confirmed live — `bash tests/check-pii-shapes/run.sh` reports `PASS: mutation: pattern is load-bearing (temp-session neutralised -> its own positive fixture reports nothing)`.

- decision: AC9's placeholder-forms regression fixture is extended with all THREE new example spellings (`-Users-<name>-`, `_home_<name>_`, and a `/private/tmp/.../<session-uuid>-scratch` temp path) in one file, while the assertion's own label text is written as "all six documented forms" per the spec's literal check requirement, rather than "all seven".
  reason: the spec's Input space class 4 enumerates "the four T-111 already ships, plus the two this task introduces (`-Users-<name>-`, `_home_<name>_`) and a temp path whose session segment reads `<session-uuid>`" — grammatically ambiguous between "three new forms" (7 total) and "two new pattern ids, each contributing one placeholder-form entry to the six-form tally, with the home-encoded entry illustrated by both root/separator spellings". AC9's own `- check:` line is a literal `grep -qF` for the string `all six documented forms`, which settles the count that must appear in the label regardless of how many example lines the fixture itself carries. The fixture includes every example spelling named in Input space class 4 (maximum coverage of the permanent regression case), while the label text matches the AC's own literal requirement.
  grounding: `.shell-team/specs/T-1101-pii-path-shapes.md` AC9's `- check:` line (`grep -qF -- 'placeholder forms are not findings (all six documented forms, one change, clean)'`) and Input space class 4; `tests/check-pii-shapes/run.sh`'s extended `placeholders.md` fixture (7 lines: 4 pre-existing + 3 new) and its `assert_clean "placeholder forms are not findings (all six documented forms, one change, clean)"` call.

- decision: in the header's rule-count arithmetic, `RE_TEMP_SESSION_ROOT` is counted as one of the "seven patterns" (not as a fifth "exclusion"), yielding the sentence "eleven independently load-bearing rules: seven patterns, plus four exclusions."
  reason: AC10(iii)'s check is a pure arithmetic floor (`$((a - c)) -ge 11`, where `a` is the count of top-level `RE_*=` lines and `c` is the count of composition lines) and does not independently parse or verify the "seven"/"four" breakdown text — so the classification choice does not affect the check's own pass/fail, but the header prose should still describe the shipped rules accurately for an adopter reading `--help`. `RE_TEMP_SESSION_ROOT` is not an exclusion (it excludes nothing; it is a shared sub-shape used only for a fixture-side reachability precondition), so classifying it as an exclusion would misdescribe its function. Classifying it as a "pattern" component keeps the pattern count (7) matching the 7 reported pattern ids (`home-path`, `home-path-win`, `home-encoded`, `temp-session`, `email-nonnoreply`, `private-key`, `token`), which is the simplest accurate story for a reader, even though `RE_TEMP_SESSION_ROOT` is not itself one of those 7 ids.
  grounding: `bin/check-pii-shapes.sh`'s rule-count sentence and the `## Anchoring/boundary inventory` region (this task); AC10(iii)'s `- check:` line (`test "$((a - c))" -ge 11`, no parsing of the sentence's own "seven"/"four" text); measured directly — `grep -cE '^RE_[A-Z0-9_]+=' bin/check-pii-shapes.sh` = 13, `grep -cE '^RE_[A-Z0-9_]+="\$\{RE_' bin/check-pii-shapes.sh` = 2 (RE_HOME_PATH, RE_TEMP_SESSION), 13-2=11.
<!-- END provenance: T-1101 -->
