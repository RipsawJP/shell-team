<!-- BEGIN provenance: T-1101 -->
- decision: `temp-session` is implemented as two top-level variables — `RE_TEMP_SESSION_ROOT` (the root alternation plus ordinary path characters, unanchored) and `RE_TEMP_SESSION` (composing `RE_TEMP_SESSION_ROOT` with the dashed-UUID requirement) — rather than as a single flat pattern.
  reason: AC5 requires both negative-control fixtures to be *proven*, not assumed, to "match the temp-root half of the temp-session rule ... while the whole rule does not match them," read out of the checker's own source in the shape `assert_reaches_email_candidates` established. That idiom needs a named, extractable sub-regex representing "looks like a temp/session path" independent of the UUID requirement — exactly the role `RE_EMAIL_BASE` plays for the email exclusions (the base shape is extractable and testable on its own, separately from the domain exclusions that filter it). A single, non-decomposed `RE_TEMP_SESSION` pattern would give the test suite nothing to extract for that half. The composition also mirrors the repository's own existing precedent (`RE_HOME_PATH = RE_HOME_PATH_BOUNDARY + RE_HOME_PATH_RAW`), which the spec's own "Prior art to imitate closely" note names.
  grounding: `.shell-team/specs/T-1101-pii-path-shapes.md` AC5 ("read out of the checker's own source in the shape `assert_reaches_email_candidates` established") and `## Notes for engineer` ("Prior art to imitate closely: ... `assert_reaches_email_candidates` ... for AC5's precondition"); `bin/check-pii-shapes.sh`'s own `RE_HOME_PATH="${RE_HOME_PATH_BOUNDARY}${RE_HOME_PATH_RAW}"` composition (pre-existing) and `RE_EMAIL_BASE`'s role in `assert_reaches_email_candidates` (`tests/check-pii-shapes/run.sh`, pre-existing lines ~401-419).

- decision: the AC5 negative-control fixture representing "a committed `.shell-team/runs/<file>` relative-path citation" is composed as ONE line that names both a `.shell-team/runs/` file citation AND a genuine machine-local temp-root citation (`/private/tmp/claude-502/<worktree>-scratch-worktree`), rather than as a bare relative path with no temp-root text at all.
  reason: AC5's own text requires this fixture to be *asserted to match the temp-root half* of the rule. A bare `.shell-team/runs/<file>` relative path contains no `/private/tmp/`, `/tmp/` or `/var/folders/` literal by construction, so it structurally cannot match any sub-regex requiring one of those roots — a fixture built that way could never satisfy AC5's own stated precondition. Composing a realistic sentence that cites both conventions together is also the shape this corpus's own records actually take (measured: `.shell-team/reviews/T-1098.md` and `.shell-team/todo.md` routinely combine a `.shell-team/runs/` citation with nearby session-scratch prose in the same paragraph), so the fixture is not an artificial contrivance.
  grounding: `.shell-team/specs/T-1101-pii-path-shapes.md` AC5 ("Two negative-control fixtures ... are each asserted to match the temp-root half of the temp-session rule ... while the whole rule does not match them"); measured directly in this session — `git grep -n -- '.shell-team/runs/' .shell-team/reviews/T-1098.md .shell-team/todo.md` shows `.shell-team/runs/` citations co-occurring with `/private/tmp/claude-502/` session-scratch prose in the same records this task's own `## Summarized sources` already cites.

- decision: the AC3 mutation lock for `temp-session` neutralises `RE_TEMP_SESSION_ROOT` (the base/root variable), not the composed `RE_TEMP_SESSION` variable that `scan_content_file()` actually passes to `report_pattern_lines`.
  reason: mirrors the repository's own existing precedent for `home-path`, whose AC3-equivalent mutation lock (pre-existing, `tests/check-pii-shapes/run.sh`) neutralises `RE_HOME_PATH_RAW` — the base shape — rather than the composed `RE_HOME_PATH` variable that is actually passed to `report_pattern_lines`. Since bash evaluates top-to-bottom, rewriting `RE_TEMP_SESSION_ROOT`'s own assignment line in a throwaway copy still neutralises the composed `RE_TEMP_SESSION` at the point it is assigned, so the effect on the reported finding is identical either way; targeting the base/root variable keeps the new mutation consistent with the one precedent already in this file for a composed pattern.
  grounding: `tests/check-pii-shapes/run.sh`'s pre-existing `assert_neutralised_pattern_unreported RE_HOME_PATH_RAW ...` call (not `RE_HOME_PATH`); `bin/check-pii-shapes.sh`'s `RE_TEMP_SESSION="${RE_TEMP_SESSION_ROOT}[0-9a-fA-F]{8}-..."` composition (this task); confirmed live — `bash tests/check-pii-shapes/run.sh` reports `PASS: mutation: pattern is load-bearing (temp-session neutralised -> its own positive fixture reports nothing)`.

- decision: AC9's placeholder-forms regression fixture is extended with all THREE new example spellings (`-Users-<name>-`, `_home_<name>_`, and a `/private/tmp/.../<session-uuid>-scratch` temp path) in one file, while the assertion's own label text is written as "all six documented forms" per the spec's literal check requirement, rather than "all seven".
  reason: the spec's Input space class 4 enumerates "the four T-111 already ships, plus the two this task introduces (`-Users-<name>-`, `_home_<name>_`) and a temp path whose session segment reads `<session-uuid>`" — grammatically ambiguous between "three new forms" (7 total) and "two new pattern ids, each contributing one placeholder-form entry to the six-form tally, with the home-encoded entry illustrated by both root/separator spellings". AC9's own `- check:` line is a literal `grep -qF` for the string `all six documented forms`, which settles the count that must appear in the label regardless of how many example lines the fixture itself carries. The fixture includes every example spelling named in Input space class 4 (maximum coverage of the permanent regression case), while the label text matches the AC's own literal requirement.
  grounding: `.shell-team/specs/T-1101-pii-path-shapes.md` AC9's `- check:` line (`grep -qF -- 'placeholder forms are not findings (all six documented forms, one change, clean)'`) and Input space class 4; `tests/check-pii-shapes/run.sh`'s extended `placeholders.md` fixture (7 lines: 4 pre-existing + 3 new) and its `assert_clean "placeholder forms are not findings (all six documented forms, one change, clean)"` call.

- decision: in the header's rule-count arithmetic, `RE_TEMP_SESSION_ROOT` is counted as one of the "seven patterns" (not as a fifth "exclusion"), yielding the sentence "eleven independently load-bearing rules: seven patterns, plus four exclusions."
  reason: AC10(iii)'s check is a pure arithmetic floor (`$((a - c)) -ge 11`, where `a` is the count of top-level `RE_*=` lines and `c` is the count of composition lines) and does not independently parse or verify the "seven"/"four" breakdown text — so the classification choice does not affect the check's own pass/fail, but the header prose should still describe the shipped rules accurately for an adopter reading `--help`. `RE_TEMP_SESSION_ROOT` is not an exclusion (it excludes nothing; it is a shared sub-shape used only for a fixture-side reachability precondition), so classifying it as an exclusion would misdescribe its function. Classifying it as a "pattern" component keeps the pattern count (7) matching the 7 reported pattern ids (`home-path`, `home-path-win`, `home-encoded`, `temp-session`, `email-nonnoreply`, `private-key`, `token`), which is the simplest accurate story for a reader, even though `RE_TEMP_SESSION_ROOT` is not itself one of those 7 ids.
  grounding: `bin/check-pii-shapes.sh`'s rule-count sentence and the `## Anchoring/boundary inventory` region (this task); AC10(iii)'s `- check:` line (`test "$((a - c))" -ge 11`, no parsing of the sentence's own "seven"/"four" text); measured directly — `grep -cE '^RE_[A-Z0-9_]+=' bin/check-pii-shapes.sh` = 13, `grep -cE '^RE_[A-Z0-9_]+="\$\{RE_' bin/check-pii-shapes.sh` = 2 (RE_HOME_PATH, RE_TEMP_SESSION), 13-2=11.

- decision: `RE_HOME_ENCODED`'s name-continuation class is repaired to `[A-Za-z0-9_.]([A-Za-z0-9_.-]*[A-Za-z0-9_.])?` — a mandatory alphanumeric-or-dot anchor character at both the start and the end of the captured name, with the full permissive class (including a mid-name hyphen) allowed only in between — rather than simply substituting the sibling `RE_HOME_PATH_RAW`'s own unrestricted `[A-Za-z0-9_.-]+` class verbatim.
  reason: round-1 review's Major names four live-reproduced misses (a 2-character name, a hyphen-split name, a digit-led name, an underscore-led name) and explicitly asks for parity with `RE_HOME_PATH_RAW`'s coverage philosophy, but also explicitly warns of an interaction risk: "a wider class may make separator-run negatives (`-Users---`) fire differently." A verbatim substitution of `[A-Za-z0-9_.-]+` (which includes the separator characters `-`/`_` themselves inside the class with no other constraint) does exactly what the review warns against: for `-Users---`, the class would greedily consume one of the two trailing hyphens as "the name" and the other as the closing separator, producing a false positive on a string that carries no real name at all — confirmed by manual regex trace before implementation, since a bare-separator-run string is composed ENTIRELY of characters the naive substitution would also accept as valid "name" content. Anchoring both ends of the captured name to a non-separator character (alnum-or-dot) closes all four review-named gaps (a single alnum-or-dot character is a sufficient whole name; a digit or an underscore may lead; a hyphen may appear strictly between two anchor characters) while making a pure separator run structurally impossible to satisfy the mandatory leading anchor — verified live, not just reasoned: `-Users---` stays clean after this exact class (new regression-lock fixture, `tests/check-pii-shapes/run.sh`), and all four review-named spellings now fire (new fixtures added to the existing family array). **Correction (round-2 review Minor, T-1101 rework 2)**: the paragraph above's own claim — "the class requires a non-separator anchor character at both the start and the end" — was itself overbroad, because `_` is a member of BOTH the separator alphabet `[-_]` and the anchor alphabet `[A-Za-z0-9_.]` this round-1 design used; a BARE `_` (or a run of only `-`/`_` characters) therefore satisfied the anchor requirement while carrying no real name at all, reproduced live in three shapes (underscore separators around a lone `_` in both roots, and hyphen separators around a lone `_`). The design is repaired in rework 2 (see the dedicated decision entry below) to require at least one alphanumeric-or-dot character SOMEWHERE in the captured name, which is the claim that is actually true of the shipped regex — this paragraph is left as written (it accurately describes what rework 1 shipped and why) rather than silently edited, and the correction is recorded here and in a new entry rather than erasing the history of what was believed at rework 1.
  grounding: `.shell-team/reviews/T-1101.md`'s round-1 Major finding and its own suggested fix text ("adjusted to still exclude the encoding separator itself from the continuation class"); manual regex trace performed before editing the real file (documented in the engineer's session, not re-derivable from a committed artifact); `bin/check-pii-shapes.sh`'s `RE_HOME_ENCODED` assignment line and its updated `## Anchoring/boundary inventory` entry (this rework round); `tests/check-pii-shapes/run.sh`'s extended `HE_FAM_LINES` array (four new spellings) and the new `regression: a bare separator run (no real name) stays clean after the name-class widening` fixture, both confirmed live (`bash tests/check-pii-shapes/run.sh`, PASS).

- decision: `RE_TEMP_SESSION_ROOT`'s prefix class is widened to admit a literal `=` but NOT bare whitespace, and the whitespace gap is disclosed as a declared limitation in `docs/pii-controls.md`/`.ja.md` rather than closed by a further widening.
  reason: the round-1 review's own Minor combined two example characters (`=` and "an embedded space") under one illustrative reproduction, but only `=` was actually LIVE-reproduced (`/var/folders/xy=z/<uuid>` confirmed not reported); the space was offered as a second, hypothetical instance of the same class, not independently reproduced. Treating the two as separable rather than one monolithic go/no-go decision: widening for `=` is cheap (one character added to an already-permissive `*`-quantified class) and self-reference-safe (verified: the defining source line for the composed `RE_TEMP_SESSION`, which is the pattern actually fed to `report_pattern_lines`, still does not self-match after the widening — confirmed by scratch-file trace before editing the real file). Widening for bare whitespace is a materially different risk: an unbounded `*`-quantified class admitting a literal space would let the prefix span across unrelated words anywhere later on the same line before it ever reaches a UUID-shaped segment — a false-positive surface increase disproportionate to the single confirmed gap the review measured, and the review's own severity ledger already accepted "documented limitation... not a merge blocker" as a sufficient disposition for this class of gap (no live carrier in this repository's own reach; the measured negative-control population of thirteen `/private/tmp/` citations carries neither `=` nor whitespace).
  grounding: `.shell-team/reviews/T-1101.md`'s round-1 Minor finding, its own text distinguishing "confirmed live" (`=`) from the hypothetical companion example (whitespace), and its `#### Fast-follow disposition` paragraph accepting a documented-limitation resolution as sufficient; `bin/check-pii-shapes.sh`'s `RE_TEMP_SESSION_ROOT` assignment line and its updated `## Anchoring/boundary inventory` entry; `docs/pii-controls.md`/`.ja.md`'s new declared-limitation bullets (this rework round); `tests/check-pii-shapes/run.sh`'s extended `TS_FAM_LINES` array (the `=`-carrying fixture), confirmed live (PASS).

- decision: the cross-provider review's own round-1 record (`.shell-team/reviews/T-1101.md`) is edited to rewrite its four literal shape-reproduction examples — the review's own 2-character, hyphen-split, digit-led and underscore-led illustrations of the pre-widening gap — into the documented angle-bracket placeholder form, with no other byte on the affected line touched.
  reason: the widened `RE_HOME_ENCODED` correctly starts reporting the review's own round-1 record, because the reviewer quoted the gap-reproduction examples as literal (non-placeholder) text — a genuine instance of the self-reference trap landing on a file this task had not previously touched, discovered only after committing to the widening and re-running `bash bin/check-pii-shapes.sh --all` against the whole tree (not just the file being widened for). T-111's own frozen AC15 explicitly names "a spec, review record, provenance file or board note" as the class of tracked file this exact placeholder-rewriting mechanism governs — a review record is not a special case exempted from it. Leaving the finding un-redacted would break AC6/AC7 (both require the whole corpus clean), which are not this task's to leave broken; leaving the widening un-committed to avoid the trip would leave the round-1 Major itself unrepaired. The edit is scoped as narrowly as the T-1098 precedent this spec's own `## Summarized sources` cites (the reviewer's own self-correction of their round-1 record in a later round): only the four shape literals change, verified by a full `diff` of the file before/after (one line touched, nothing else) and by the verdict/severity/finding-label token counts (`APPROVE`/`REQUEST_CHANGES`/`Blockers`/`Major`/`Minor`) over that one file being identical before and after.
  grounding: `.shell-team/reviews/T-1101.md:23` (before and after, diffed); `bash bin/check-pii-shapes.sh --all` (red before the redaction, clean after — reproduced live); `.shell-team/specs/T-111-pii-shape-checker.md` AC15 ("a real-looking name segment in a spec, review record, provenance file or board note is fixed by rewriting it to the placeholder form, never by adding the path to the known-shapes list"); this spec's own `## Summarized sources` citing `.shell-team/reviews/T-1098.md:69`'s precedent (the reviewer's own self-correction of a round-1 record in a later round); token-count comparison performed directly in this session (`APPROVE`/`REQUEST_CHANGES`/`Blockers`/`Major`/`Minor`, all five identical before and after).

- decision: `RE_HOME_ENCODED`'s name-continuation shape is repaired (round 2) from "a non-separator anchor character at both the start and the end" to `[A-Za-z0-9_.-]*[A-Za-z0-9.][A-Za-z0-9_.-]*` — a permissive run of the full class (hyphen and underscore both allowed) on EITHER side of exactly one MANDATORY alphanumeric-or-dot character, with the mandatory character allowed to sit anywhere within the captured span rather than specifically at an edge.
  reason: round-2 review's Minor is precise about the mechanism: `_` sits in both the separator alphabet `[-_]` and rework 1's own anchor alphabet `[A-Za-z0-9_.]`, so rework 1's "anchor both ends" design let a captured span consisting ENTIRELY of `_` characters (one or more) satisfy both anchor positions using nothing but that one overloaded character — the anchors being non-separator was never suffient, since `_` both IS and ISN'T a separator depending on which alphabet is consulted. The fix does not try to patch the "both ends" shape (which structurally cannot distinguish "an anchor happens to be `_`" from "the whole name is `_`" without an explicit "at least one truly-non-underscore character exists" requirement); it replaces the invariant directly: require exactly one alphanumeric-or-dot (never `_`, never `-`) character to exist SOMEWHERE in the captured name, with the fully permissive class (both `-` and `_` freely) allowed to surround it on both sides. This closes all three round-2-reproduced shapes (underscore separators around a lone `_` in both roots; hyphen separators around a lone `_`) because none of them contains ANY alphanumeric-or-dot character at all, while preserving every spelling that DOES contain one — including the round-1-critical `_www` (underscore-LED, not underscore-ONLY: the leading `_` is absorbed by the permissive prefix, `www` supplies the mandatory anchor) and the hyphen-only regression lock (`-Users---`, still no alphanumeric-or-dot character anywhere, still correctly clean). Verified live before finalizing, not by trace alone: all three round-2-reported shapes plus the pre-existing hyphen-only regression lock stay clean, and all nine previously-verified family spellings (the four round-0 shapes, the four round-1 additions, `_www` restated as its own standalone positive control) still fire — `bash tests/check-pii-shapes/run.sh`, 93/93 PASS, 0 FAIL.
  grounding: `.shell-team/reviews/T-1101.md` round-2 record (the Minor's own three reproduced shapes and its own diagnosis, "`_` belongs to both the separator alphabet ... and the name-anchor alphabet"); `bin/check-pii-shapes.sh`'s `RE_HOME_ENCODED` assignment line and its rewritten `## Anchoring/boundary inventory` entry (this rework round); `tests/check-pii-shapes/run.sh`'s three new regression-lock fixtures plus the standalone `_www` positive control (this rework round), confirmed live (PASS); a scratch-file self-reference trace performed before editing the real file (the new regex's own defining source line does not self-match).

- decision: the header's adopter-facing claim about what the separator-run regression lock proves is reworded from an unconditional "a run composed only of `-`/`_` characters can never satisfy it" (which was true only for the hyphen flavour at rework 1) to "a captured span containing NO alphanumeric-or-dot character at all — a run composed only of `-`/`_` characters, whatever their mix — can never satisfy the name requirement," with the four concrete non-matching shapes it now covers named explicitly (`-Users---`, `_Users___`, `_home___`, `-Users-_-`).
  reason: round-2 review's own second point: "the header's adopter-facing claim ... is overbroad — true for hyphen, false for underscore" and asks for it to be "reword[ed] to what the repaired regex actually guarantees" and "verif[ied] ... against the shipped regex rather than the intent." The reworded sentence is verified, not merely asserted: each of the four named shapes was run against the actual shipped `RE_HOME_ENCODED` line (not reasoned about from the class definition) and confirmed to stay clean, matching what the sentence now claims exactly — no broader and no narrower.
  grounding: `.shell-team/reviews/T-1101.md` round-2 Minor's own suggested-resolution text ("narrow the header/provenance claim to its actual ... scope"); `bin/check-pii-shapes.sh`'s rewritten `## Anchoring/boundary inventory` entry for `RE_HOME_ENCODED` (this rework round); live verification performed in this session against the shipped (post-fix) regex, all four named shapes confirmed clean.
<!-- END provenance: T-1101 -->
