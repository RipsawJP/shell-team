<!-- BEGIN provenance: T-1037 -->

- decision: spliced `S1`/`S2` into `agents/scrum-master.md` and `G1`/`G2` into `tests/check-handoff/run.sh` by extracting each frozen block from the spec with `awk`/`sed` into a scratch file, then replacing the exact target line(s) by line number with an `awk` script that reads the scratch file's bytes via `getline` — never retyping or hand-copying any of the frozen prose at any point, matching the method the spec's Notes for engineer require.
  reason: `S1`, `S2`, `G1` and `G2` all carry apostrophes, em-dashes, backticks and (the guards) single-quoted ERE metacharacters that no manual retype survives intact, and the spec warns a transcription defect "will look like a checker problem rather than a transcription problem." Routing bytes straight from the frozen fence to the target line removes that failure class structurally. AC1–AC8 all measured PASS on the first attempt after each splice, consistent with this method.
  grounding: `.shell-team/specs/T-1037-checker-retro-precision.md`, `## Notes for engineer` ("Extraction, not retyping... Splice by extracting the ... bytes with `awk`/`sed` into a scratch file and inserting *those bytes* programmatically — never by retyping"); `bin/check-acs.sh` live run on that spec, AC1/AC2/AC3/AC4/AC5/AC6/AC7/AC8 all `PASS` after the two splices.

- decision: after splicing `G1`/`G2` into `tests/check-handoff/run.sh` via an `awk`-into-file redirect followed by `mv`, noticed the file's mode had silently changed from `100755` to `100644` (a side effect of the redirect+`mv`, not of the line content), and restored `100755` with `chmod` in a dedicated follow-up commit rather than leaving it or folding the fix into the guard-splice commit.
  reason: the spec's own frame for this file is that nothing changes except the two guard blocks — "the full replacement of `tests/check-handoff/run.sh` lines 267–268" and "289–290" — and an unreviewed mode change is a side effect outside that frame; the suite is designed to be runnable directly (matching `tests/close-out/run.sh`'s own `+x` convention) as well as via `bash tests/check-handoff/run.sh`, so silently dropping the executable bit would change observable behavior for a caller using the direct-invocation form without anyone having decided that on purpose. A dedicated follow-up commit (rather than amending the guard-splice commit) keeps the guard-content commit's diff exactly the ten spliced lines the spec measures, with the unrelated mode correction visible as its own line in history.
  grounding: `.shell-team/specs/T-1037-checker-retro-precision.md`, `## Notes for engineer`, "Order of work" step 3 ("`tests/check-handoff/run.sh`: replace lines 267–268 with `G1`... and lines 289–290 with `G2`... Then run the suite" — no other change to that file is described); `git diff 8d47eed -- tests/check-handoff/run.sh` showing `old mode 100755` / `new mode 100644` before the follow-up commit and no mode line after it.

<!-- END provenance: T-1037 -->
