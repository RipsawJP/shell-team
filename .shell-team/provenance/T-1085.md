# T-1085 decision provenance

<!-- BEGIN provenance: T-1085 -->
- decision: the preserved WIP frame (commit `7b3826c`, `docs/loop-engineering/default-path-firing.md`) was left byte-for-byte unmodified in its `## Firing protocol (frozen before execution)` section, `## Terms and closed vocabularies` section, `## Supersession and follow-ups` section and the result-independent bullets of `## Limits and what is not computable`, rather than rewritten, expanded, or re-derived from the spec afresh, even though the task hand-off described the protocol section as "in progress."
  reason: a line-by-line diff of the doc's `## Firing protocol` section against the spec's frozen `<!-- BEGIN firing-protocol: T-1085 -->` … `<!-- END firing-protocol: T-1085 -->` region showed every one of the region's fourteen paragraphs already faithfully restated in the same order, plus the required "Constants recorded now" bullets AC14 needs; all nine required AC2 headings were present exactly once each; every forbidden note-wide prefix (`- chain-link:`, `- licence-condition:`, `- default-reachability:`, `- arm-wall-clock:`, `- saving-direction:`, `- release-consequence:`, `- blocked-at:`, bare-`- saving: `, `- verdict: `, `- unit: `) was measured absent; and the three chain-independent AC14 lines (`- claim-ceiling:`, `- agent-type:`, `- knee: undetermined —`) were each present exactly once. Rewriting a section already verified faithful to a frozen region risks introducing exactly the paraphrase-drift or accidental forbidden-prefix collision the spec's own grammar-collision discipline exists to prevent, for no benefit the verification did not already confirm.
  grounding: `.shell-team/specs/T-1085-default-path-firing.md` lines 53–83 (the frozen `firing-protocol` region) diffed against `docs/loop-engineering/default-path-firing.md` lines 24–59 (this round's own diff, run and inspected before this decision was recorded); `.shell-team/specs/T-1085-default-path-firing.md` **AC2** (the nine required-heading closure), **AC14** (the three chain-independent lines), and the criteria preamble ("No criterion counts a `- verdict: ` or `- unit: ` prefix note-wide").
- decision: the `.shell-team/test-recipe.md` entry naming this task's two-arm firing venue procedure (per-arm fresh `git clone --no-hardlinks`, the arm-identical `CHECK_ACS_TIMEOUT` export, the `--attempt`/instance-id-prefix telemetry separation, and the explicit per-call instance timeout) was appended now, in round 1, rather than deferred to round 2 alongside the evidence-dependent sections.
  reason: the spec's own `## Notes for engineer` states the recipe entry may be appended now "if their content is result-independent" — every element of this procedure (the venue, the timeout roles, the telemetry-separation requirement, the liveness-launch mechanism) is fixed inside the spec's frozen `firing-protocol` region and does not depend on what the firing measures, so deferring it to round 2 would only cost a second read-through of the same frozen text with no informational gain, and appending it now lets a reader who inherits this task mid-cycle (before the firing has run) find the environment procedure without waiting on round 2's evidence-dependent commit.
  grounding: `.shell-team/specs/T-1085-default-path-firing.md` `## Notes for engineer` ("Optionally append the `.shell-team/test-recipe.md` entry ... now if their content is result-independent; otherwise defer to round 2 and say so."); the same spec's frozen `firing-protocol` region paragraphs "Venue, and the boundary the venue does not cross," "Degree and baseline," "Telemetry scope separation" and "Hazards, measured rather than assumed," which the appended entry restates.
- decision: (round 2) `genuine-overlap`'s margin is stated against the spread between the earliest and latest per-instance `first` timestamp (min-first to max-first, ≈34.495 s), rather than against `first − launched-epoch` (the sibling T-1083 note's own idiom for "maximum single launch latency").
  reason: this launch record's own `- launched-epoch:` field is disclosed in `## Firing evidence` as an audit-only stamp taken at the orchestrator's first opportunity after the launch call returned, and it precedes several instances' own `first` stamps by up to ~49 s — so `first − launched-epoch` is negative for those instances and cannot supply a meaningful latency bound. T-1083's own idiom assumed `launched-epoch` precedes every instance's own first stamp, an assumption this task's own evidence does not share; porting the idiom unmodified would have silently produced a meaningless or misleading margin.
  grounding: `docs/loop-engineering/default-path-firing.md` `## Firing evidence (raw, orchestrator-produced)`, disclosure (1) ("the instances' own first per-unit timestamps precede it by up to ~49 s, so the per-instance `agent-timestamp` lines below, not that audit-only field, are the overlap derivation's inputs"); the same note's `## Default-path chain and aggregation analysis` section, licence-condition `genuine-overlap`.
- decision: (round 2) the `t1085-verdict-region-parity` derivation block compares a `cksum` digest of each arm's sorted verdict-region content (one item per set) rather than the full ~91-line sorted content itself.
  reason: the two arms' full verdict-region blocks are already quoted verbatim, twice, inside the byte-locked `## Firing evidence` section; embedding the same ~91 lines a third and fourth time inside a derivation block adds no new information and meaningfully inflates an already-large note. A `cksum`-based single-item set comparison is exactly as rigorous for `bin/derive-populations.sh`'s own bucket-equality proof (either the two digests land in one bucket or they do not) and remains byte-reproducible per **AC15**'s own requirement.
  grounding: `bin/derive-populations.sh --help` ("Runs two to eight named population-extraction commands ... any command any deterministic shell pipeline"); `.shell-team/specs/T-1085-default-path-firing.md` **AC15**'s own check body (reproduces the `- reproduce:` command and byte-compares its output to the embedded block — content-agnostic about what the set items are).
- decision: (round 2) the Blast radius's base-side (commit `9c950d76290cb0d70f0a01a11bdfe3e901b67043`) per-criterion verdicts for the 89 spec files shared with T-1084 are read from T-1084's own git-tracked board hand-off and QA round-1 record (both already independently verified at that exact commit) rather than from a fresh from-scratch full 89-file re-run; six criteria most likely to depend on external, time-varying state (`T-1074` AC9, `T-1076` AC10, `T-1077` AC3, `T-1083` AC2/AC4, and `git status --porcelain -- bin/ tests/` itself) were independently re-verified live against a fresh `git worktree add --detach` at that commit before this reuse was relied on.
  reason: `git diff --stat 9c950d76...HEAD -- .shell-team/specs` shows exactly one file differs between T-1085's own branch point and its current working tree (`T-1085-default-path-firing.md` itself, newly added) — every one of the 89 shared spec files is byte-identical, so any of their own criteria can only move between base and head via external state (the board, the telemetry corpus, git status/diff ranges, the spec-file population count), never via that file's own literal content. T-1084's own hand-off and QA round both already measured and disclosed the corrected state of exactly these external-state-dependent criteria at this exact commit; re-running the full 89-file sweep a second time would reproduce content-only checks whose result cannot have moved, at a cost of roughly another 56-minute serial pass (the measured serial-arm wall clock for this same population). The live spot-check of the six criteria most exposed to drift (three `git status` gates and the two `T-1083` scope-lock criteria the hand-off flagged as pre-/post-commit sensitive) confirmed the git-tracked record's corrected values exactly, rather than trusting it unverified.
  grounding: `.shell-team/todo.md` T-1084 entry, "Local test result — T-1084" (the `⚠ Disclosed correction` paragraph naming the three permanent new reds and the three post-commit `git status` flips) and "QA round 1 — T-1084" (the independent re-confirmation of the same three findings); live re-verification this round: `git worktree add --detach <scratch> 9c950d76290cb0d70f0a01a11bdfe3e901b67043` then `git status --porcelain -- bin/ tests/` (empty) and `CHECK_ACS_TIMEOUT=150 bash bin/check-acs.sh <spec>` for `T-1074-fanout-orchestration.md` AC9 (PASS), `T-1076-log-run-locking.md` AC10 (PASS), `T-1077-worktree-reconcile.md` AC3 (PASS), `T-1083-agent-launch-fanout.md` AC2 (FAIL) and AC4 (FAIL) — all five matching the hand-off's own corrected, post-commit values.
- decision: (round 2) two additional `T-1084` criteria (AC5, AC11) initially reused from the same borrowed pre-commit snapshot were re-corrected from `FAIL` to `PASS` after the full 90-file base-vs-head reconciliation surfaced them as apparent flips; and two further apparent flips found in that same reconciliation (`T-1001-retro-input-acquisition.md` AC29; `T-1061-adopter-docs-gate.md` AC1) were characterised as artefacts (a parallel-sweep timing artifact; the already-disclosed duplicate-`**AC1**`-label defect) rather than genuine new reds, each confirmed by an isolated, contention-free re-run before being so characterised.
  reason: the borrowed base-side snapshot (see the prior decision entry) was T-1084's own **pre-commit** raw sweep; its own hand-off explicitly corrected only the three criteria its own narrative discussed (`T-1074` AC9, `T-1076` AC10, `T-1077` AC3, `T-1083` AC4) because those were the ones ITS OWN diff made relevant. `T-1084`'s own self-referential criteria (AC5, AC11, checking `agents/tech-lead.md` and the fan-out block/`bin/` diff) were never discussed in that hand-off because they were not stale from T-1084's own point of view — but they were still captured pre-commit in the reused snapshot, and T-1085's own full reconciliation (which this task's own Method paragraph requires: run every criterion, never trust a borrowed table uncorroborated) caught the residual staleness a partial reuse would have missed. This is exactly why the spec's own Method section requires running every criterion rather than searching for or reusing mentions: a reused base-side dataset's own known corrections do not bound its own unknown ones.
  grounding: live re-verification this round: `git -C <the same t1085-base-wt scratch worktree, pinned to 9c950d76290cb0d70f0a01a11bdfe3e901b67043> ` `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1084-dispatch-routing-record.md` → AC5 PASS, AC11 PASS, AC14 PASS, AC15 PASS, AC16 PASS (all five at the true base commit); the same command at current HEAD (twice) → AC5 PASS, AC11 PASS, AC14 FAIL, AC15 FAIL, AC16 FAIL; `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` at HEAD (twice) → AC29 PASS both times; `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1061-adopter-docs-gate.md` at HEAD (twice) → both runs print both `AC1: PASS (exit 0)` and a second `AC1: FAIL (exit 127)` block for the same label, matching the pre-existing duplicate-label defect `.shell-team/specs/T-1085-default-path-firing.md`'s own Blast radius table already names.
- decision: (round 3, rework) decision entry 3 above (the round-2 choice to state `genuine-overlap`'s margin against instance launch dispersion instead of `first − launched-epoch`) is **superseded, not deleted**: round 3 computes the frozen wording's own metric literally — `first − launched-epoch` maximized over the eight instances — on the note's own recorded integers, finds it negative (`qa-8`, `-13881421000` ns), discloses why (the same audit-field-precedence limitation entry 10 already named), and rests the licence condition's `met` value on the non-empty window plus the disclosed computation of the frozen metric itself, retaining the dispersion figure only as a secondary, non-load-bearing cross-check.
  reason: the cross-provider review's round-1 Major (`.shell-team/reviews/T-1085.md`, "Major" §1) correctly identified that substituting a different indicator (launch dispersion) for the frozen wording's own named metric (spec line 71, "with its margin against **the maximum single launch latency** stated") leaves the frozen text's own literal requirement undischarged, even though the underlying overlap fact (a genuine 878.199 s simultaneous-execution window) was never in question. Round 2's own reasoning (entry 10) was sound as a disclosure of *why* the literal formula is unhelpful here, but stopped short of actually computing and stating that formula's own value before reaching for a substitute — computing it (even though negative) and disclosing its negativity's cause satisfies the frozen wording without inventing a metric the frozen text does not name.
  grounding: `.shell-team/specs/T-1085-default-path-firing.md` line 71 (frozen `firing-protocol` region, "genuine-overlap (a non-empty intersection window re-derivable from the recorded timestamps, with its margin against the maximum single launch latency stated)"); `.shell-team/reviews/T-1085.md` "Cross-provider review — round 1", Major finding 1 (`docs/loop-engineering/default-path-firing.md:645`); `docs/loop-engineering/default-path-firing.md` `## Firing evidence`, disclosure (1) (the `launched-epoch` audit-stamp-precedence limitation, unchanged from round 2); the same note's `## Default-path chain and aggregation analysis` "Genuine overlap" paragraph and `## Verdict and licence conditions` `genuine-overlap` line, both rewritten this round.
- decision: (round 3, rework, class sweep for the genuine-overlap finding) the other five licence-condition lines (`shipped-text-unmodified`, `no-out-of-band-flag`, `real-plugin-role-instances`, `real-population`, `production-unit`) and all seven chain-link lines were each individually read against the frozen protocol's own wording (spec line 71 and the Goal's chain-link enumeration) for the same substitute-metric defect class; none apply.
  reason: `shipped-text-unmodified`, `no-out-of-band-flag`, `real-population` and `production-unit` carry no frozen numeric formula in the protocol's own text to substitute away from — their `met` grounds (AC1's zero-diff measurement, the absence of an out-of-band flag, the 90-unit population count, the verbatim `check-acs` summary lines) are exactly what the frozen wording asks for, not a stand-in for something else. `real-plugin-role-instances`'s frozen wording is explicit and numeric (spec line 71, "N ≥ 2") and the note's own ground (`- requested-n: 8` / `- achieved-n: 8`) is that literal metric, not a substitute. None of the seven chain-link ids carries a frozen numeric-comparison formula in the Goal's own text (each is a evidenced/not-evidenced existence claim over a named mechanism step, not a threshold); `instances-launched`'s own text references the non-empty overlap window, but that window value itself (878198706000 ns) is unchanged by this round's rework and was never the disputed metric — only the value it was compared against was.
  grounding: `.shell-team/specs/T-1085-default-path-firing.md` line 71 (all six licence-condition ids' frozen definitions, in full) and the Goal's "Chain links, in order:" sentence (line 43, all seven chain-link ids named with no per-link numeric formula); `docs/loop-engineering/default-path-firing.md` `## Verdict and licence conditions` (all six lines re-read this round) and `## Default-path chain and aggregation analysis` (all seven chain-link lines re-read this round).
- decision: (round 3, rework) the `- saving:`, both `- arm-wall-clock:` lines and the `- saving-direction:` line are recomputed on a symmetric basis — both arms' durations taken from the instances' own inner `- agent-timestamp:` first/last stamps (fanned: `min(first)` across all eight `qa-*` lines to `max(last)` across all eight; serial: the single `serial-1` line's own first/last, unchanged) — replacing the round-2 figure's asymmetric basis (fanned: orchestrator `launched-epoch`→`completed-epoch`; serial: instance inner timestamp), and the superseded round-2 figure (≈45.66%) is disclosed inline alongside the new one (≈46.89%) rather than silently overwritten.
  reason: the cross-provider review's round-1 Major (§2) correctly identified that comparing an orchestrator-side epoch pair against an instance-side timestamp pair is not an apples-to-apples wall-clock comparison, even though the direction (`faster`) and the underlying overlap facts were never in dispute. The instances' own `- agent-timestamp:` grammar is recorded symmetrically for both arms (the same field names, the same clock source, the same per-unit runner mechanism per the Terms section's closed-vocabulary "Arm ids" entry), so re-deriving both arms' durations from that one shared grammar removes the basis mismatch without inventing a new measurement.
  grounding: `.shell-team/reviews/T-1085.md` "Cross-provider review — round 1", Major finding 2 (`docs/loop-engineering/default-path-firing.md:679-681`); `docs/loop-engineering/default-path-firing.md` lines 564–572 (the eight `t1085fan` and one `t1085serial` `- agent-timestamp:` lines, byte-locked evidence, read but not edited); **AC13**'s own check (re-derives `- saving-direction:`'s token from the `- arm-wall-clock:` lines' own integers with bash `10#` arithmetic) re-run live this round and PASS both before and after this edit's own bash-arithmetic self-check (`fanned_dur_inner=1779893739000`, `serial_dur=3351333776000`, `(3351333776000-1779893739000)/3351333776000≈46.89%`, sign `faster` unchanged).
- decision: (round 3, rework, class sweep for the arm-clock-basis finding) every other cross-arm comparison in the note (the shared 90/88/2 `- summary:` population counts, the `t1085-verdict-region-parity` cksum-of-sorted-content comparison, the `t1085-assign-vs-part` derivation, the pre-commitment's 90-minute trigger check, the execution-order/cache-warmth qualitative disclosure) was read for the same basis-symmetry defect class; none apply.
  reason: the two `- summary:` lines and the `verdict-region-parity` derivation each compare a value independently produced by the **same** tool (`bin/aggregate-verdicts.sh`) run identically against each arm's own part file(s) — no orchestrator-side value is compared against an instance-side one anywhere in either. `t1085-assign-vs-part` is not a cross-arm comparison at all — both of its sets (`ASSIGN`, `PART`) are drawn from the fanned arm's own launch record and aggregation block; the serial arm never appears in it. The pre-commitment's 90-minute trigger reads only the serial arm's own single duration, with no fanned-arm value entering the comparison at all, so there is no second basis to mismatch against. Execution-order and cache-warmth are qualitative, narrative disclosures (which arm ran first, which was cache-warm), not a numeric comparison with a basis to state.
  grounding: `docs/loop-engineering/default-path-firing.md` `## Default-path chain and aggregation analysis` (`population-fixed` chain-link line, the `t1085-assign-vs-part` derivation block) and `## Saving and baseline comparison` (`- verdict-region-parity:` line and its derivation block, `- execution-order:`, `- cache-warmth:`, and the pre-commitment paragraph following the `- saving-direction:` line) — all re-read this round for the basis-symmetry class, none edited.
<!-- END provenance: T-1085 -->
