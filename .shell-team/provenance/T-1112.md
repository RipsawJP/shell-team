<!-- BEGIN provenance: T-1112 -->
- decision: use the spec-review round-1 disposition's own offered wording, near-verbatim, for the AC4 host-rule sentence in `templates/prompt-blocks/fanout-orchestration.md` (mirrored into `skills/run/SKILL.md`), rather than composing an independent sentence that also satisfies the same seven-literal binding clause.
  reason: at least two valid options existed — author a fresh sentence carrying all seven required literals (`verify-mechanism`, `coordinating session`, `harness-tracked background shell slice`, `isolated staged worktree`, `never sub-agent-hosted`, `until a pilot`, `single point of failure`) on one line, or reuse the wording the spec's own `## Notes for engineer` already offered "as proof the clause is writable rather than as a mandate." The spec's own text records that this exact wording was already checked against Major 3's finding and found satisfying; reusing it minimizes the risk of a fresh sentence silently dropping one of the seven bound literals or splitting them across two lines, the exact defect class Major 3 was raised against. This is non-trivial because it fixes the literal shipped prose of an adopter-facing operational rule (contain-mode mirrored into `skills/run/SKILL.md`), not merely an internal implementation detail.
  grounding: `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` `## Notes for engineer`, "Major 3 (AC4 a bag of disconnected substrings)" bullet, which quotes the sentence verbatim; live reproduction — `grep -F -- 'execution host' templates/prompt-blocks/fanout-orchestration.md` returns exactly one line matching that quoted text, and AC4's check line (`- check:` under **AC4**) verified `PASS` against it.

- decision: leave **AC12**'s `- flips: <count>` line unwritten in this engineer round — the population and its size are derived and disclosed in the spec's `## Blast radius`, but the full base/head two-arm sweep across the corpus is not executed here, and the criterion is handed off knowingly red.
  reason: at least two options existed once `bin/derive-populations.sh --label t1112-read-set ...` (the spec's own `- reproduce:` line) resolved to corpus-scale (117/117 specs reachable, 99 of them in one dominant `all+literal+indirect` bucket) — (a) run the full two-arm sweep across the corpus inside this engineer round anyway, or (b) derive and disclose the population, defer the sweep, and report the size. Option (a) contradicts this task's own explicit instruction ("do NOT run the full sweep... report the derived population size and STOP there") and `.shell-team/test-recipe.md`'s T-1111 entry, which names exactly this corpus-scale resolution as "a STOP-and-report condition, not an execution procedure to push through," reserving the actual two-arm sweep for the coordinating-session-hosted, harness-tracked background-shell-slice fan-out procedure the T-1110 entry documents. This is non-trivial because it leaves one of the spec's own frozen acceptance criteria (AC12) unsatisfied at hand-off, an observable, contract-level outcome rather than an internal implementation choice.
  grounding: this task's engineer briefing ("do NOT run the full sweep — the verify-mechanism duty is the coordinating session's fan-out per the dispatch row (report the derived population size and STOP there)"); `.shell-team/test-recipe.md` T-1111 entry ("a STOP-and-report condition, not an execution procedure to push through") and T-1110 entry (coordinating-session-hosted, harness-tracked shell-slice execution); live reproduction — `bash bin/derive-populations.sh --label t1112-read-set --set all="ls .shell-team/specs/*.md | LC_ALL=C sort" --set literal="grep -lE -- '- check:.*(phase-multiplexing\.md|fanout-orchestration\.md|dispatch-record\.md|skills/run/SKILL\.md|agents/tech-lead\.md|README(\.ja)?\.md|test-recipe\.md)' .shell-team/specs/*.md | LC_ALL=C sort" --set indirect="grep -lF -- 'team-paths.sh --get' .shell-team/specs/*.md | LC_ALL=C sort" --accept-status literal=1 --accept-status indirect=1` → `- union: items: 117`, `all+literal+indirect` bucket 99 items; `CHECK_ACS_TIMEOUT=900 check-acs.sh .shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` reports AC12 `FAIL` as a result, disclosed rather than hidden.

- decision: word both READMEs' affirmative replacement bullets as a full explanatory sentence naming the hosting form and the audit-only field, rather than the minimum literal fragment AC8's check would accept on its own.
  reason: AC8's check only requires the bullet line to carry `fan-out by default` / `既定で fan-out` together with `execution-host` and `verify-mechanism`; a minimal replacement (for example, appending those three tokens to a short clause) would satisfy the check but leave an adopter reading the sentence with no idea what "the execution host" actually is. The spec's own `- adopter-surface:` sub-bullet under AC8 states the correction must "name the execution host an adopter's orchestrator is expected to use," which the mechanical check does not itself enforce byte-for-byte — so the fuller wording was chosen deliberately to satisfy the adopter-surface intent, not merely the grep. This is non-trivial because it is the literal shipped, adopter-facing correction for a claim the spec states is now false.
  grounding: `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` **AC8**'s `- adopter-surface:` sub-bullet ("names the execution host an adopter's orchestrator is expected to use... and names the `- execution-host:` launch-record field"); live reproduction — `git diff README.md README.ja.md` shows the replacement sentences naming "harness-tracked background shell slices launched by the coordinating session" / "coordinating session が起動する harness-tracked のバックグラウンド shell slice" and the `- execution-host:` field explicitly; `CHECK_ACS_TIMEOUT=900 check-acs.sh .shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` reports **AC8** `PASS`.
<!-- END provenance: T-1112 -->
