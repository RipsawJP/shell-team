<!-- BEGIN provenance: T-1008 -->

- decision: None of the nine retro candidates from `.shell-team/retros/2026-07-28.md` was promoted into the corpus through `bin/playbook-promote.sh` this round. All nine received a non-retaining disposition (one `already-covered`, one `operator-global`, seven `drop`), each justified on its own merits.
  reason: AC8 requires the sorted multiset of the 79-row import table's retained (`loop`/`maintainer`) destinations to be byte-identical to the sorted multiset of every heading in the corpus — a strict two-directional equality, not a subset check. A candidate promoted through the promoter appends a brand-new heading (dated with the retro's own date, not any of the 79 source dates) that no import-table row destines, so the corpus's key set would gain an entry the import table's destination set does not contain, breaking AC8's `cmp -s` comparison. I verified this mechanically with a minimal two-line fixture (an import table naming one destination, a corpus carrying that entry plus one extra heading) before concluding this, rather than reasoning about the check in the abstract. Two of the nine candidates (a general fail-open/fail-closed reframing heuristic, a self-referential-gate dogfooding recommendation, and a full-content-search-before-claiming-no-precedent recommendation) are genuinely new, generalizable knowledge that would otherwise route to `loop`/`maintainer` under the Goal's own routing rule — but doing so would require a new corpus heading with no import-table row destining it, which is exactly what AC8 forecloses. Rather than force a dishonest `already-covered` label onto content that does not restate anything already in the corpus, or silently skip a mechanical AC, I recorded the mechanical constraint as the reason each of these candidates stays a `drop` this round, leaving genuine future promotion as a direct follow-up outside this task's ledger mechanism. DP-6 explicitly leaves "which candidates are adopted" to per-candidate engineer judgment, not a pre-decided count, so declining all nine given this constraint is within the decision space the spec leaves open, not a relaxation of any acceptance criterion.
  grounding: .shell-team/specs/T-1008-lessons-corpus-import.md AC8 check line (`cmp -s "$F/dest.s" "$F/keys.s"`, comparing the 79-row table's retained destinations against every corpus heading); DP-6 "Which candidates are adopted is per-candidate judgement and is assigned to the engineer, not pre-decided here"; `docs/loop-engineering/lessons-import-disposition.md` `## Retro candidate table` section's own explanatory paragraph.

- decision: Every one of the 79 imported entries carries `- **Source**: n/a`, rather than attempting to preserve any of the source corpus's original `Source` values (old-repository task/issue/PR references, commit hashes, or an external repository name) as a repository-relative path.
  reason: AC3 bans every bare `T-<three digits>` (other than this repository's own `T-111`/`T-112`/`T-113`) and every bare `#<number>` from surviving anywhere in the corpus, per DP-4's adopted arbitration (no relaxation for this repository's own numbers either, since an old repository's `#299` and this repository's `#23` are mechanically indistinguishable). None of the source corpus's original `Source` values resolve to a path that exists in this repository, and one names an external client repository, which is out of scope for a public repository regardless of the number-shape rule. Setting every value to `n/a` is the only value that both satisfies AC6's closed contract (`n/a` or an existing repository-relative path) honestly and requires no per-entry judgment call about whether a given old reference might be "close enough" to keep — a judgment call that would risk exactly the DP-4 violation the spec arbitrates against.
  grounding: .shell-team/specs/T-1008-lessons-corpus-import.md DP-4 "the stricter half ... no bare `#<number>` reference in the imported corpus ... follows from a measurement, not from taste"; AC3 and AC6 check lines; `.shell-team/todo.md` T-1008 entry's "DP-4 arbitration ... ADOPTED" sub-bullet.

- decision: Two source entries' titles were reworded during translation specifically to remove a literal `|` (pipe) character that the original Japanese title contained inside a quoted code fragment (`upstream | grep -q` and the `A && B || C` idiom name).
  reason: A corpus entry's heading text becomes a ledger destination cell in `docs/loop-engineering/lessons-import-disposition.md`'s pipe-delimited table rows. A literal `|` inside that text would split the row into more fields than `awk -F'|'` expects, corrupting AC7's field-count invariant and the per-date sequence/outcome extraction the ledger's own contract depends on ("no cell contains `|`" is stated in the Output contracts table). I discovered this empirically while building the ledger rows (the `awk -F'|'` field count came out to 8 instead of 7 for these two rows) rather than reasoning about it in the abstract, and reworded only the two affected titles to preserve the same meaning without the pipe character.
  grounding: .shell-team/specs/T-1008-lessons-corpus-import.md Output contracts table, "both ledger tables" row: "no cell contains `\|`"; AC7's `awk -F'|'` field extraction; my own build script's `awk -F'|' '{print NF}'` field-count check against `docs/loop-engineering/lessons-import-disposition.md`, which read 7 for every row only after the rewording.

- decision: The retro candidate about backgrounding or extending the timeout for long full-tree `check:` commands (candidate 5, `[target-specific]`, disposed as `drop` in the ledger) has its substance folded directly into `.shell-team/test-recipe.md`'s new T-1008 entry, rather than left with no trace beyond the ledger's one-line reason.
  reason: The candidate's content is genuinely useful, repository-specific operational guidance (I needed exactly this guidance myself, running AC23/AC25's whole-suite checks and AC10's `check-pii-shapes.sh --base develop`), and DP-11 step 7 already requires a T-1008 entry in this same file documenting the errexit-safe re-measurement hazard. Appending the candidate's substance to that same, already-required entry is the lowest-risk way to preserve real value without inventing a new maintainer-scoped corpus heading, which AC8 (see the first decision above) forecloses regardless of the candidate's genuine merit.
  grounding: .shell-team/specs/T-1008-lessons-corpus-import.md DP-11 step 7 ("the re-measured `tests/errexit-safe/run.sh` pin and the `.shell-team/test-recipe.md` entry — immediately after step 6's header edit"); Assumptions "`check-acs.sh`'s 120s per-check cap applies ... raise `CHECK_ACS_TIMEOUT` for the run rather than splitting the criterion"; `.shell-team/test-recipe.md`'s own T-1008 entry.

<!-- END provenance: T-1008 -->
