# T-1062 decision provenance

<!-- BEGIN provenance: T-1062 -->
- decision: the preamble bullet is repaired with the spec's `## Notes for engineer` suggested wording, adopted essentially verbatim (the em dash, the ordering of `vPREV` before `vX.Y.Z`, and the "same fill-in notation the title rule above uses" closing clause), rather than composing an independent phrasing that also satisfies AC1's literal requirements.
  reason: more than one wording satisfies AC1's literal set (`## Install / update`, `## Links`, `vPREV`, `vX.Y.Z`, case-insensitive `compare`, case-insensitive `previous`, all inside the same bullet) and AC2's seven-literal preamble survival, so a real choice existed. The reviewer's own T-1059 round-1 finding characterized the correct fix as "a one-line clarification" (`.shell-team/reviews/T-1059.md`), and the spec's Notes frame its suggested text as a starting point specifically to minimize wording risk on a criterion set this dense — composing new prose adds a source of AC1/AC2 mismatch (a missed literal, an unintended second bullet) for no benefit over text already checked against the criteria at spec-authoring time. This changes observable, user-facing prose (what a release preparer reads), which is why the choice is recorded rather than treated as a formatting triviality.
  grounding: `.shell-team/specs/T-1062-release-notes-compare-link.md` `## Notes for engineer` ("Suggested shape, offered as a starting point and not frozen"), and its quoted fenced block; `.shell-team/reviews/T-1059.md` round-1 Minor bullet naming the fix "a one-line clarification".
- decision: no repair or annotation is made to the pre-existing, orphaned top-level `### Local test result` heading found sitting in `## Active` (board line 27, carrying T-1060's stale local-test text, left behind when T-1060's entry moved to `## Done` without it). This task's own `### Local test result` section is appended as a new heading immediately after this task's entry, ahead of the orphaned one, rather than merging into or overwriting it.
  reason: two valid placements existed — repair/relocate the stale T-1060 heading while adding this task's own section (cleans up a pre-existing defect, but is scope creep: nothing in this spec's Goal, Non-goals or acceptance criteria names the board's `### Local test result` hygiene as this task's job, and AC5's allow-list only permits this task's own board edit), or leave it untouched and append this task's own section in the conventional position (stays inside this task's actual scope; the orphan is disclosed rather than silently fixed or silently left unexplained). The second was chosen because the operating discipline governing this role is "make the smallest change that satisfies the criteria" and "no speculative refactors" — fixing an unrelated, pre-existing board-hygiene defect discovered mid-task is exactly the kind of unrequested additional work that discipline reserves for a one-line flag rather than an in-task fix.
  grounding: `.shell-team/todo.md` line 27 (`### Local test result`, orphaned — content names `T-1060-adopter-binding-docs.md`) versus line 32 (`- [x] **T-1060** ...` now under `## Done`), both read first-hand; `.shell-team/specs/T-1062-release-notes-compare-link.md` `## Non-goals` (no clause authorizes board-hygiene work beyond this task's own entry) and **AC5** (allow-list scoped to this task's own paths only).
<!-- END provenance: T-1062 -->
