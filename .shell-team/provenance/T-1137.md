<!-- BEGIN provenance: T-1137 -->

- decision: appended the new sentence — `This is a misconfiguration the loop does not support — see docs/adopting.md, "Where the operating files live," for the one-line re-include.` — directly after the existing `this hand-off is not durable.` clause, inside the same `printf` format string in `fail_not_durable()`, rather than adding a second `printf` call, a per-reason-token branch, or replacing any existing text.
  reason: `tests/check-durability/run.sh:112` and `.shell-team/specs/T-1048-handoff-durability-barrier.md` **AC3** both match `this hand-off is not durable.` as a substring, so appending after it (never replacing, never splitting across a second `printf`) keeps both green — verified live rather than assumed (`bash tests/check-durability/run.sh` passed after the edit). The sentence is a general statement true of all five reason tokens (`no-recorded-commit`, `missing-working-file`, `not-in-recorded-commit`, `uncommitted-change`, `untracked-opt-out`) — it names the condition as a misconfiguration and points at the documented remedy without asserting which token fired or that this repository's own base dir is ignored, satisfying the spec's second Non-goal ("no prediction, and no verdict, from the new message"). It carries the fixed strings `misconfiguration` and `docs/adopting.md` (**AC2**'s anchors, **D5**), and deliberately avoids `check-ignore`, `.shell-team` and `tasks/` (the three forbidden literals **D4** names, each of which would turn a merged criterion or a shipped fixture red) by pointing at the remedy through the document's own section title ("Where the operating files live") rather than quoting the re-include line inline.
  grounding: `.shell-team/specs/T-1137-excludes-detection.md` **AC2**, **AC3**(c), **D3**, **D4**, **D5**, and `## Notes for engineer` ("Append, never replace." / "One sentence for five reason tokens."); `bin/check-durability.sh:110-113` (`fail_not_durable()`); `tests/check-durability/run.sh:112`; `.shell-team/specs/T-1048-handoff-durability-barrier.md` **AC3**.

- decision: in both `README.md` and `README.ja.md`, inserted the durability consequence clause into the middle of the existing "Keep it out of git" bullet's second sentence (between the existing "board tracked" clause and the existing `!.shell-team/` re-include clause), rather than appending a new trailing sentence after the bullet's existing content or splitting the bullet onto a second physical line.
  reason: **AC1** measures "the consequence is stated in the same bullet as the choice" as "on that line" — both files carry the whole two-choice bullet as one physical line at the branch point, so any additional clause had to be woven into the same physical line rather than appended as a new paragraph, which would either break the one-line measurement or read as a disconnected afterthought several sentences later (the same defect #475 reports: the durability consequence documented somewhere the reader who makes the choice never reaches). Placing the new clause immediately after "board tracked" and before the existing `!.shell-team/` re-include sentence keeps the causal order in each language (the global route's scope problem, then its durability problem, then the remedy) rather than requiring the reader to jump back to an earlier clause after reading the remedy. Each README's new clause states the same two facts (`not-durable` becomes permanent for the global route; hand-off stays local-only to that machine) with the same remedy pointer (prefer the repo-level `.gitignore`), in that file's own register (README.md's direct English prose; README.ja.md's already-established です/ます-free direct register matching the rest of that bullet).
  grounding: `.shell-team/specs/T-1137-excludes-detection.md` **AC1** (the base non-vacuity measurement and the "one-line scoping is a measurement rather than a convenience" clause), **D6**, and `## Pre-commitment`'s droppable-component note ("The Japanese and English halves drop together or not at all"); `README.md:60` and `README.ja.md:60` as measured at the branch point (`git show 9985f1cdda6a26ee408a0bafb618add91c4005fb:README.md` / `:README.ja.md`, each one physical line carrying `core.excludesFile` and `!.shell-team/`).

- decision: for **AC8**'s derivation block (label `t1137-refusal-text-readers`), chose three `--set` populations — files under `.shell-team/specs/*.md` asserting the `this hand-off is not durable.` substring, files under `bin`/`tests` asserting the same substring, and files under `docs` mentioning `not-durable` — over an alternative built from the three `## Blast radius` population probes (`specs-naming-check-durability`, `specs-asserting-the-not-durable-tail`, `specs-naming-either-readme`).
  reason: the label names "refusal-text-readers" — the population of files whose own correctness depends on the exact shape of the refusal text this task edits, i.e. who would need to change if the message's substring contract changed — which is the `this hand-off is not durable.` assertion itself, not merely any file that mentions the checker's name or either README. The `## Blast radius` probes measure a different, already-recorded population (specs naming `check-durability` by name, or naming either README) and were left as this spec's own probes rather than duplicated here under a different label. The chosen sets partition cleanly into a spec-side bucket (`.shell-team/specs/T-1048-handoff-durability-barrier.md`, this task's own `T-1137` spec) and a code-side bucket (`bin/check-durability.sh`, `tests/check-durability/run.sh`); the third set (`docs` mentioning `not-durable`) returned zero matches, requiring `--accept-status docs-mentioning-not-durable=1` per `bin/derive-populations.sh --help`'s documented `git grep` no-match case — run live and confirmed empty rather than assumed, since `docs/adopting.md` documents the re-include mechanism and the ignore-behavior consequence but never quotes the checker's own `not-durable` reason-token vocabulary.
  grounding: `.shell-team/specs/T-1137-excludes-detection.md` **AC8** and `## Notes for engineer` ("The derivation block (**AC8**)."); `bin/derive-populations.sh --help` (identifier grammar, `--accept-status` semantics); this task's own live run of the `- reproduce:` command below, diffed byte-for-byte against the embedded block.

- decision: rework round 1 (QA verdict FAIL, `docs-overclaim`) — reworded the shared `fail_not_durable` sentence from an unconditional "This is a misconfiguration the loop does not support" into a disjunctive form: "A required record is not durably observable yet — never committed, edited since, or, where a hidden base dir is the cause, a misconfiguration the loop does not support; see docs/adopting.md, "Where the operating files live," for the one-line re-include in that case." — rather than a per-token variant, a second `printf`, or dropping the `misconfiguration`/`docs/adopting.md` fixed strings **AC2** anchors.
  reason: QA reproduced live, against real scratch repositories built the same way **AC2**'s own fixture is built, that the PRIOR sentence's unconditional "This is a misconfiguration" claim and its unconditional gitignore-remedy pointer were false for `no-recorded-commit` (zero commits — nothing configured yet), `missing-working-file` (a record simply never authored), and `uncommitted-change` (an ordinary edit not yet re-committed), and that the remedy pointer was also wrong for `untracked-opt-out`. The new sentence names three disjuncts — never committed / edited since / (conditionally) hidden by an ignore rule — each independently true of at least one of the five reason tokens, and the ignore-rule clause is scoped with "where … is the cause" rather than asserted as fact of every firing, so no single reading of the sentence asserts a condition the checker did not observe. Re-verified live against all five tokens (`no-recorded-commit`, `missing-working-file`, `not-in-recorded-commit`, `uncommitted-change`, `untracked-opt-out`), each built the same way as QA's own round-1 reproduction (`core.excludesFile /dev/null`, no gitignore file except where the token itself concerns one), and each held true under the new wording. `misconfiguration` and `docs/adopting.md` remain present exactly once each (**AC2**'s anchors), `not-durable: %s: %s` remains on exactly one line (**AC3**'s static companion), and none of `check-ignore`, `.shell-team`, `tasks/` appear in the comments-stripped body (**AC3**(c), **D4**).
  grounding: `.shell-team/todo.md` `### QA verdict — T-1137 (round 1)` (the `docs-overclaim` finding, its five-item runtime-criterion audit, and its "Suggested next step"); `.shell-team/specs/T-1137-excludes-detection.md` **AC2**, **AC3**, `## Pre-commitment`'s "Named fragile component" paragraph, and `## Notes for engineer`'s "One sentence for five reason tokens."; `bin/check-durability.sh` (the single `fail_not_durable` `printf`, and its five call sites for the closed reason-token set).

<!-- END provenance: T-1137 -->

- reproduce: bash bin/derive-populations.sh --label t1137-refusal-text-readers --set 'specs-asserting-tail=git grep -lF -- "this hand-off is not durable." -- .shell-team/specs/*.md' --set 'code-asserting-tail=git grep -lF -- "this hand-off is not durable." -- bin tests' --set 'docs-mentioning-not-durable=git grep -lF -- "not-durable" -- docs' --accept-status 'docs-mentioning-not-durable=1'
<!-- BEGIN derivation: t1137-refusal-text-readers -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: specs-asserting-tail — status: 0 — lines: 2 — items: 2 — command: git grep -lF -- "this hand-off is not durable." -- .shell-team/specs/*.md
- set: code-asserting-tail — status: 0 — lines: 2 — items: 2 — command: git grep -lF -- "this hand-off is not durable." -- bin tests
- set: docs-mentioning-not-durable — status: 1 — lines: 0 — items: 0 — command: git grep -lF -- "not-durable" -- docs
- union: items: 4
- bucket: code-asserting-tail — items: 2
  - bin/check-durability.sh
  - tests/check-durability/run.sh
- bucket: specs-asserting-tail — items: 2
  - .shell-team/specs/T-1048-handoff-durability-barrier.md
  - .shell-team/specs/T-1137-excludes-detection.md
<!-- END derivation: t1137-refusal-text-readers -->
