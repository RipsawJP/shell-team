# T-1063 decision provenance

<!-- BEGIN provenance: T-1063 -->
- decision: the T-1047 board entry's stale "post-rework `check-acs.sh` shape" sub-bullet (the one quoting the citation-precision note as `` `todo.md:22` vs `todo.md:144` ``, without the `.shell-team/` path prefix) was also corrected to a content-named anchor, even though AC8's own `grep -cF` check only tests the fully-prefixed literal `.shell-team/todo.md:22` and would not have caught this bare form.
  reason: the correction sub-bullet this task appends claims "three board line-number meta-citations (in the AC17(c) report, the post-rework note and the QA PASS verdict) now name the entries they refer to" — leaving the post-rework note's bare-form citation stale would make that claim false for one of the three named sites, which is exactly the self-referential citation-imprecision defect class `.shell-team/reviews/T-1047.md`'s finding 6 already flagged once in this same entry. Fixing only what the checker happens to test and leaving a sibling instance of the identical defect stale would repeat that class rather than close it.
  grounding: `.shell-team/todo.md`, T-1047 entry's "post-rework `check-acs.sh` shape" sub-bullet (pre-edit text); `.shell-team/specs/T-1063-editorial-batch.md` `## Notes for engineer`, "Board correction sub-bullet" (naming three sites: "the AC17(c) report, the post-rework note and the QA PASS verdict"); `.shell-team/reviews/T-1047.md` finding 6 (the same citation-imprecision class recurring).
- decision: the three board rewordings (the board-hygiene bullet, the AC17(c)-report sentence, the QA-verdict sentence) were composed as free paraphrase rather than a mechanical find-and-replace of the bare line number, because the spec's `## Notes for engineer` gives a full suggested sentence only for L1087 (the board-hygiene bullet) and describes the other two sites only generically ("replace each `.shell-team/todo.md:<n>` and `at line 149` with the entry it names").
  reason: this is mutable board prose (not a frozen block), so no `- check:` line pins its exact bytes beyond the literal absence/presence assertions AC8 tests; a mechanical substitution of the bare number for a content-named phrase inside the original sentence structure would in two of the three cases produce an ungrammatical or redundant sentence (e.g. "not T-1046's own board entry (which sits at [content-named-phrase])" reads as nonsense once the number is gone), so each sentence's surrounding clauses were re-worded to keep the meaning intact while removing every literal AC8's negative assertions require to be zero.
  grounding: `.shell-team/specs/T-1063-editorial-batch.md` `## Notes for engineer`, "Board L1096 / L1099 / L1100"; the same spec's **AC8** (the four literals required at zero in the working entry, with no clause pinning replacement wording).
- decision: the new `- editorial correction (T-1063, ...)` sub-bullet is appended as the last sub-bullet of T-1047's entry (after the existing "fast-follow disposition" bullet), rather than inserted immediately after the corrected board-hygiene bullet near the top of the entry.
  reason: this repository's own append-only-record discipline (T-1052's `E171` frozen block, which this entry's own text already reuses) states that an append explaining a change to an earlier line says so in a new line placed after the change, rather than rewriting history in place — appending at the very end preserves the entry's own chronological order (each existing sub-bullet already reads as a step in that order: engineer, QA, Codex review, fast-follow disposition) instead of inserting the correction note out of sequence in the middle of that history.
  grounding: `.shell-team/specs/T-1052-records-editorial.md`'s `E171` frozen block ("An item you could not exercise is named together with its criterion..." — more directly, the entry's own already-shipped self-application bullet stating "Every append to this entry re-reads the whole entry first, and an append that changes what an earlier line said says so in the new line instead of leaving both standing"); `.shell-team/specs/T-1063-editorial-batch.md` **DP6** ("the correction is announced in the entry rather than performed silently").
<!-- END provenance: T-1063 -->
