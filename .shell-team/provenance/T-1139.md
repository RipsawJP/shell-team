<!-- BEGIN provenance: T-1139 -->

- decision: placed gap 2's runbook-half sentence (the `<specs dir>/<task-id>-<slug>.md` rule and `check-durability` as its consumer) inside step 10, immediately after the existing `READY_FOR_MERGE` sentence and before the new gap-3 sentence — rather than as a separate paragraph elsewhere in the section or attached to step 3 (the generator step).
  reason: the spec's Goal and `## Decision points ruled in this spec` (**D3**) name the causal reason the precedent propagated — "the precedent propagated through the role that names specs," i.e. `pm-spec`, which step 10 is the one place in the runbook that names by dispatching `shell-team-pm-spec`. `## Notes for engineer` leaves gap 2's placement "yours to choose within the section," so step 10 — where the reader is told which generated agent writes the spec in the first place — is the point a Codex-host reader is actually thinking about which role names specs, closer to the causal seam than an unrelated new paragraph would be. Placing it beside the gap-3 sentence (same step) also let one edit carry both `push`/`pull request` (AC3/AC5) and `<task-id>-<slug>.md`/`check-durability` (AC2/AC5) without duplicating the "once APPROVE reaches READY_FOR_MERGE" anchor sentence twice.
  grounding: `.shell-team/specs/T-1139-codex-runbook-gaps.md` Goal paragraph and **D3** ("Runbook-only loses on causation..."), `## Notes for engineer` ("Where gap 4's sentence goes is yours to choose within the section" — the same latitude **D3**'s ruling extends to gap 2); `docs/adopting.md` step 10 (the line naming `shell-team-pm-spec` in the `spawn_agent` dispatch list), read first-hand before editing.

- decision: placed gap 4's sentence (the `tuning-oversight.md` / `class-M` pointer) as a new standalone paragraph after the existing T-1138 `**The self-detected-host observable ...**` paragraph, separated by a blank line, and before the `## Conversational usage` heading — rather than folding it into the T-1138 paragraph, into the "Honest limits" paragraph, or into any numbered step.
  reason: **AC7** locks the T-1138 paragraph byte-identical up to its first following empty line, so any edit landing inside that paragraph (rather than after its terminating blank line) would break AC7's `cmp`; appending after the existing blank-line terminator, with a fresh blank line before the new paragraph, keeps the old paragraph's own slice untouched while still reading naturally as "one more disclosure at the end of the section," which is exactly the shape `## Notes for engineer` names ("append after it rather than editing into it if that is where it reads best"). Gap 4's own subject (a re-freeze standing grant) has no natural home inside any of the eleven numbered steps — none of them discuss intent-block freezing — so a standalone paragraph at the section's tail, alongside the other standalone disclosure paragraphs the section already carries ("Honest limits," the T-1138 paragraph), matches the section's own existing structure rather than inventing a new one.
  grounding: `.shell-team/specs/T-1139-codex-runbook-gaps.md` **AC7** (the empty-line terminator ruling and its stated reason: "a new paragraph appended after this one — which gap 4 may well be — does not enter the compared region"), `## Notes for engineer` ("If you place it after the T-1138 paragraph ..., leave a blank line between them"); `docs/adopting.md`'s `## Using shell-team from Codex CLI` section structure (the "Honest limits" and T-1138 paragraphs as the section's own precedent for a standalone trailing disclosure paragraph), read first-hand before editing.

- decision: left the board-entry template line under `## tasks/todo.md entry format` (`agents/pm-spec.md:58`, the legacy `docs/specs/<slug>.md` example) unedited, touching only the `## Your job` item-3 sentence that **AC6** actually reads.
  reason: **AC6**'s own body states the template line "sits outside this slice and is deliberately left free to keep its legacy example: this criterion is about the authoring instruction only, and says nothing about that template either way." The Body-to-AC correspondence table (row 8) independently confirms the same latitude. Editing it anyway would have risked the two `board-line-format.md` `contain`-mode fragments (`- [ ] **T-` and `` ` — spec: ``) that live on that exact line — `## Notes for engineer` gotcha 7 names this as a real hazard for any edit near that line, even though a change limited to the legacy path string alone would not touch either fragment. Leaving the line untouched is the smaller, sufficient change: the convention is now stated as a rule at the one place this task's Goal requires it (`## Your job`), and the template example is a secondary illustration the spec explicitly does not require moving.
  grounding: `.shell-team/specs/T-1139-codex-runbook-gaps.md` **AC6** ("The board-entry template ... sits outside this slice and is deliberately left free"), Body-to-AC correspondence row 8, `## Notes for engineer` gotcha 7 (the `board-line-format.md` two-fragment hazard on `agents/pm-spec.md:58`); `templates/prompt-blocks/registry.txt` (the `board-line-format.md` `contain` row) and `templates/prompt-blocks/board-line-format.md` itself, read first-hand before deciding not to touch line 58.

- decision: (round 2, rework) reordered step 1's already-present upgrade/`VERSION` instruction to precede the "skip this step" trigger sentence, in both `docs/adopting.md` and `docs/adopting.ja.md` — rather than leaving it trailing the trigger as round 1 shipped it.
  reason: cross-provider review round 1's Major finding (class `instruction-ordering`) traced that a reader who matches "the plugin already shows `installed, enabled`" reads the skip trigger first and stops before reaching the version-check instruction several sentences later — exactly the skim-and-move-on failure mode this task exists to close, and the same failure class b run #4's own operator hit. Reordering costs nothing against **AC1**/**AC5**: both criteria only assert that `codex plugin marketplace upgrade` (`marketplace upgrade` in the mirror) and `VERSION` occur somewhere in the whitespace-squeezed step-1 sub-slice, with `codex plugin marketplace add` and `step 2` (`手順 2`) retained as base-present positive controls — none of the four checked strings is order-sensitive, so the fix is pure prose reordering with no criterion redesign.
  grounding: `.shell-team/reviews/T-1139.md` round 1 Major finding 1 ("placing the fix immediately after a 'skip this step if X' trigger ... creates a real risk of exactly the skim-and-move-on failure"; Synthesis audit ledger row 1); `.shell-team/specs/T-1139-codex-runbook-gaps.md` **AC1** check line (asserts presence only, no ordering clause) and **D1** (retains the `step 2` cross-reference as the only invariant); `docs/adopting.md` step 1 and `docs/adopting.ja.md` step 1, read first-hand before and after the edit.

- decision: (round 2, rework) rewrote step 10's "who opens the pull request" sentence, in both languages, to state the behaviour in terms of the loop contract's own `human_gate` list — the shipped default (`human_gate: [merge, push]`) stops the session at `READY_FOR_MERGE` and the human pushes and opens the pull request; a contract whose `human_gate` list omits `push` lets the session do it instead; the merge and the merge GO stay the human's under every contract — replacing round 1's single fixed claim that "the session pushes the branch and opens the pull request."
  reason: cross-provider review round 1's Blocker traced that round 1's sentence contradicted this project's own shipped default loop-contract template, `templates/shell-team.contract.yaml:24` ("must not merge or push without a human gate") and `:43-45` (`human_gate: [merge, push]`), mirrored in `templates/goal.contract.yaml` and in this repository's own `.shell-team/loops/shell-team.contract.yaml:42` — so "the session pushes" was true only of a contract an adopter had edited to drop `push`, never of the shipped default. The spec was re-frozen to v2 with a new Goal sentence and a revised **D4** ruling stating exactly this conditional framing before this rework began; the rewrite here implements that v2 ruling rather than re-deciding it. Naming the contract file (`<base>/loops/shell-team.contract.yaml`, scaffolded by `team-init` from `templates/shell-team.contract.yaml`) and the `human_gate` key lets the reader check their own contract rather than trust one fixed claim.
  grounding: `.shell-team/reviews/T-1139.md` round 1 Blocker finding (citing `templates/shell-team.contract.yaml:24,43-45` and `templates/goal.contract.yaml:27,46`) and Synthesis audit ledger row 2; `.shell-team/specs/T-1139-codex-runbook-gaps.md` v2 Goal paragraph and **D4** ("v2 ruling — v1 stated the omit-`push` behaviour as if it were the default, contradicting the shipped template"); `templates/shell-team.contract.yaml:24,43-45` and `.shell-team/loops/shell-team.contract.yaml:42`, read first-hand before writing the new sentence.

- decision: (round 2, rework) superseded round 1's decision (entry 3 above, "left the board-entry template line ... unedited") — updated `agents/pm-spec.md:58`'s legacy `spec: docs/specs/<slug>.md` example to `spec: <specs dir>/<task-id>-<slug>.md`, keeping both `templates/prompt-blocks/board-line-format.md` `contain`-mode fragments (`- [ ] **T-` and `` ` — spec: ``) intact on that same line.
  reason: cross-provider review round 1's Major finding traced a concrete, if narrow, downstream consequence round 1 did not check: `bin/check-oversight.sh:644-658` extracts the board's own literal `spec:` pointer via regex and runs `git log -1 --format=%H -- "$SPEC_PATH"`, which exits 0 with empty output for a nonexistent path, so `[ -n "$LAST_TOUCH" ]` is false and the pre-merge freshness check silently no-ops on a host running the opt-in `governance-controlled` oversight profile if `pm-spec` ever copies the stale literal template rather than the path it actually used. **AC6** itself only reads the `## Your job` slice and says nothing about the template line either way (round 1's own ground for leaving it), so updating it costs nothing against any frozen criterion; re-verified after the edit that both `board-line-format.md` fragments still appear verbatim on the line and that `bash tests/check-prompt-sync/run.sh` stays green.
  grounding: `.shell-team/reviews/T-1139.md` round 1 Major finding 2 (`bin/check-oversight.sh:644-658`'s `git log -1` empty-output trace) and Synthesis audit ledger row 3; `.shell-team/specs/T-1139-codex-runbook-gaps.md` **AC6** body ("The board-entry template ... says nothing about that template either way" — the permission this edit uses, not a prohibition); `templates/prompt-blocks/board-line-format.md` and `templates/prompt-blocks/registry.txt`'s `board-line-format.md` `contain` row, re-read before and after the edit; `agents/pm-spec.md:58` itself, diffed before/after.

- decision: (round 3, rework) rewrote step 1's mismatch clause, in both languages, to condition the skip explicitly on the `VERSION` comparison's **result** rather than merely on having performed it — an older-release `VERSION` now reads "the install is stale: take step 2's remedy" (JA: "そのインストールは古い——手順 2 の対処を行うこと"), a matching `VERSION` reads "skip the install commands above," and the not-yet-installed branch reads "run them instead" (JA: "まだインストールされていなければ、上記のコマンドを実行すること") — replacing round 2's "Only once that comparison is done, skip the install commands above; if the plugin was not yet installed at all, run them, then see step 2 for the one further check and the remedy if the version is still stale," whose "see step 2" clause was grammatically the tail of the not-yet-installed branch only.
  reason: review round 3's Blocker (class `branch-outcome-unstated`) traced that round 2's clause conditioned the skip on having performed the comparison, never on its result, so a reader who ran `upgrade`, compared `VERSION`, and found a genuine mismatch was told nothing different from a reader who found a match — both were licensed to skip the install commands — reproducing the exact silent-stale-plugin failure mode gap 1 exists to close. The v3 Goal's gap-1 sentence requires "telling the reader whose comparison shows an older release than the one they intend to run what to do — take step 2's remedy — so a reader who finds a mismatch is told something different from one who finds a match." The rework cross-references step 2's already-shipped remedy rather than restating its paragraph (**D1**, no-duplication) and asserts no effect of `upgrade` on a following `plugin add` (**D2**, Non-goals). Cost against **AC1**/**AC5**: none — both criteria assert only that `codex plugin marketplace upgrade` (`marketplace upgrade` in the mirror) and `VERSION` occur somewhere in the whitespace-squeezed step-1 sub-slice, with `codex plugin marketplace add` and `step 2` (`手順 2`) retained as base-present positive controls; none of the four checked strings is order- or branch-sensitive, so the fix is pure prose restructuring with no criterion redesign. Re-verified after the edit: `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1139-codex-runbook-gaps.md` reports `AC1: PASS` and `AC5: PASS`.
  grounding: `.shell-team/reviews/T-1139.md` review-round-3-pending Blocker finding "gap-1 step-1 mismatch-remedy ambiguity" (second `### Codex Review verdict: REQUEST_CHANGES` section, Synthesis audit ledger row 1) and the resulting class-B re-freeze v2→v3 (board `intent-hash (v3): 40cc1fec5a9b6a0421dd926bea3f2cb8e6c0454a`); `.shell-team/specs/T-1139-codex-runbook-gaps.md` v3 Goal sentence and **D1**/**D2**; `docs/adopting.md` step 1 and `docs/adopting.ja.md` step 1, read first-hand before and after the edit.

- decision: (round 3, rework) removed the clause "a contract whose `human_gate` list omits `push` lets the session push the branch and open the pull request itself" (and its Japanese twin) from step 10 in both guides, and restated the tail as the shipped loop's one behaviour: after `READY_FOR_MERGE` the session stops there because both `merge` and `push` are human gates under the shipped default loop contract (`human_gate: [merge, push]` in `<base>/loops/shell-team.contract.yaml`, scaffolded from `templates/shell-team.contract.yaml`), and the run skill's own `push-go` liveness gate hands the push to the human **independently of that list** — describing no contract variant.
  reason: review round 3's Major finding (class `shipped-behaviour-claim-unread-mechanism`) traced that no shipped mechanism reads `human_gate` at all (`grep -rn human_gate bin/` empty; `templates/loop-contract-template.yaml:64` — "ignored by the linter") and that `skills/run/SKILL.md` gates push unconditionally under the `push-go` liveness-reason token regardless of what any contract's `human_gate` list says, so the omit-`push` clause described a knob this project has never built. The v3 Goal and v3 **D4** row state the ruling: "the session stops at `READY_FOR_MERGE`; `push` and `merge` are both human gates in the shipped contract ... and the run skill's `push-go` liveness gate hands the push to the human; the human pushes the branch and opens the pull request, and the merge and the merge GO are the human's. No contract variant is described." The rewrite keeps naming the contract file and `human_gate` as *where the gates are declared* (permitted) but states explicitly that the run skill's gate acts "independently of that list," so it does not say editing `human_gate` changes the behaviour. Cost against **AC3**/**AC5**: none — both criteria assert only that `push` and `pull request` (`プルリクエスト`) occur in the whitespace-squeezed step-10 sub-slice with `READY_FOR_MERGE` retained as a base-present control; neither the omitted clause nor its replacement is itself an asserted string, so removing it and restating the tail costs nothing against either criterion. Re-verified: `AC3: PASS` and `AC5: PASS`, and `grep -n 'omits\|外せ\|外した\|session push\|セッションは.*push' docs/adopting.md docs/adopting.ja.md` returns no matches.
  grounding: `.shell-team/reviews/T-1139.md` review-round-3-pending Major finding "gap-3 step-10 omit-push clause has no shipped implementation" (second `### Codex Review verdict: REQUEST_CHANGES` section, Synthesis audit ledger row 2, citing `grep -rn "human_gate" bin/` empty and `skills/run/SKILL.md`'s "Done" step never mentioning push); `.shell-team/specs/T-1139-codex-runbook-gaps.md` v3 Goal sentence and v3 **D4** row; `skills/run/SKILL.md:167` (the `push-go` liveness-reason token, "the human GO before a push," read first-hand before writing the new sentence); `docs/adopting.md` step 10 and `docs/adopting.ja.md` step 10, read first-hand before and after the edit.

<!-- END provenance: T-1139 -->
