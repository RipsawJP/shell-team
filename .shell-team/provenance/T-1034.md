<!-- BEGIN provenance: T-1034 -->

- decision: Both scripts' new header paragraph documenting the DP11 out-of-contract signal exits ("Signal exits (T-1034 DP11): 129, 130 and 143 mean SIGHUP, SIGINT and SIGTERM terminated the run; they carry no classification and never collide with the 0/1/2 classification contract above.") was worded identically in `bin/check-refreeze-class.sh` and `bin/check-intent.sh`, and placed as the new final header-comment line, immediately before `set -euo pipefail`, in both files.
  reason: DP11 requires "each header gains a line stating that 129/130/143 mean SIGHUP/SIGINT/SIGTERM terminated the run and carry no classification" but leaves the exact wording and placement open. AC10 requires the header's own LAST comment line to be reproduced verbatim by `--help` and non-empty (the Notes for engineer trap: "the header's last line must not be a bare `#`"), and AC11 requires all six tokens (SIGHUP/SIGINT/SIGTERM/129/130/143) to appear in `--help`. Making this new paragraph the shared, byte-identical final line in both headers satisfies both criteria at once and keeps the two headers maximally parallel, consistent with the "one definition, two consumers" spirit DP10 states for the signal fragment even though DP10 itself only requires the `SIG` code fragment, not the surrounding header prose, to be byte-identical.
  grounding: `.shell-team/specs/T-1034-refreeze-hardening-execbit.md`, DP11 ("each header gains a line stating that `129`/`130`/`143` mean..."); AC10's check clause (`$h` must reproduce the header's own last comment line via `grep -qxF`, and must not contain `set -euo pipefail`); AC11's check clause (`for t in 'SIGHUP' 'SIGINT' 'SIGTERM' '129' '130' '143'`, all required in `--help`); Notes for engineer, "The header's last line must not be a bare `#`."

- decision: `tests/bin-exec-bit/run.sh`'s rule-application helper (`count_non_100755`) returns the literal string `EMPTY` — rather than a numeric sentinel such as `-1`, or a separate boolean flag — to distinguish "the population had no lines at all" from "the population had lines and zero were non-conformant," and the two DP5 frozen control labels are embedded as substrings inside longer, self-describing PASS/FAIL sentences rather than printed as bare standalone tokens.
  reason: AC1 and AC3 assert the two frozen labels via `grep -qF` substring match against the suite's whole stdout, so the surrounding prose is free; embedding each label inside a fuller PASS/FAIL sentence, following this suite's own `pass()`/`fail()` idiom (itself carried over from `tests/gitignore-raw-dumps/run.sh`'s structure, the spec's named structural precedent), keeps the suite self-documenting and consistent with this repository's other `tests/*/run.sh` style. The `EMPTY` sentinel (rather than overloading a numeric `0`, which is a legitimate "population present, zero violations" result) is what lets the suite tell "no subjects at all" apart from "every subject passed" inside one return channel — DP2's fail-closed requirement (empty/unavailable population is never a clean pass) depends on that distinction being unambiguous at the point the helper's result is consumed.
  grounding: `.shell-team/specs/T-1034-refreeze-hardening-execbit.md`, DP5 ("prints, verbatim, `control: a 100644 member is rejected` and `control: an empty population is rejected`") and DP2 ("Empty or unavailable `git` output is a failure, never a clean pass"); AC1's check clause (`grep -qF -- "$t"` over the suite's own captured stdout) and AC3's check clause (the same substring-match shape over both control labels); `tests/gitignore-raw-dumps/run.sh`'s `pass()`/`fail()` helper idiom.

- decision: `bin/check-refreeze-class.sh`'s and `bin/check-intent.sh`'s new `on_signal()` function's `printf ... >&2 || true` write was NOT added as a new `NOT_APPLY` entry, and no new part-(ii) behavioral row was added, to `tests/errexit-safe/run.sh` — that file was left completely unedited.
  reason: `derive_candidates()`'s three static patterns (P1 `>&2; *exit [0-9]+` on one line; P2 a bare-line-ending `>&2[[:space:]]*$`; P3 an unguarded heredoc opener) were checked by hand against the new line: the `printf` line ends in `|| true`, not a bare `>&2` and not a same-line numeric-literal `exit N` (the following `exit "$2"` is a variable reference on its own separate line), so it matches none of P1/P2/P3 and never appears in `derive_candidates()`'s output at all — confirmed by re-running `tests/errexit-safe/run.sh` after both edits and observing its completeness self-audit and reverse-staleness check both stay green, with no new candidate reported and no stale `NOT_APPLY` entry. Since the line never appears as a candidate, no `NOT_APPLY` reason-letter classification applies to it, and adding one defensively would be recording an entry the audit itself never flags. A part-(ii) behavioral row would separately be vacuous here: the trigger is a real OS signal, and that suite's own harness only closes stderr (`2>&-`) on synchronous invocations — it never delivers a signal, so no such row could exercise this code path.
  grounding: `tests/errexit-safe/run.sh`, part (iii)'s `derive_candidates()` (the P1/P2/P3 grep definitions) and its `NOT_APPLY_FILE` heredoc (contains no `check-refreeze-class.sh`/`check-intent.sh` entries, before or after this task); `.shell-team/specs/T-1034-refreeze-hardening-execbit.md`, Assumptions ("No `NOT_APPLY` entry in `tests/errexit-safe/run.sh` names either script this task edits... so no registry edit and no new behavioral row is required"); measured: `bash tests/errexit-safe/run.sh` passes unmodified after both scripts' edits (every part, including the completeness self-audit and its own mutation self-check, green).

- decision: An ad hoc SIGINT probe beyond AC9's own frozen, SIGTERM-only runtime clause (launching each script as a background job with `bash script &`, then `kill -INT`) was diagnosed as a test-harness artifact — bash forces SIGINT/SIGQUIT to `SIG_IGN` for an asynchronous `&` command started from a non-interactive, job-control-disabled shell, and that ignore cannot be overridden by a `trap` set later inside the same process — and was NOT reported as a defect in `on_signal`, nor as evidence that AC9 is broken-as-written; AC9's own check clause was left completely unmodified.
  reason: The finding was confirmed independently with a bare control experiment (`bash -c 'trap ... INT; sleep 3' &` then `kill -INT`, carrying no `check-refreeze-class.sh`/`check-intent.sh` code at all) that reproduced the identical un-trappable-ignore behavior, isolating the cause to the shell/job-control environment rather than either script. This repository's own routing rule for a broken-as-written probe ("route back as a finding for pm-spec... never an engineer rewrite of frozen text") applies when the probe AS WRITTEN is what fails; AC9's actual frozen check clause tests only `SIGTERM` at runtime (HUP/INT are verified only via the `SIG` block's static byte-identity across both scripts), and that clause passed on both scripts (confirmed via `bash bin/check-acs.sh`). Reporting the ad hoc SIGINT experiment as if it broke AC9 would misattribute an artifact of my own EXTRA verification (beyond the frozen check) to the frozen text itself. In real interactive use (an operator pressing Ctrl-C on a foreground invocation), the terminal driver delivers SIGINT directly to the foreground process group under job control, bypassing the async-ignore rule entirely — so this finding does not describe the reachable input class named in Input space ("reachable by an operator pressing Ctrl-C or a CI job cancelling").
  grounding: `.shell-team/specs/T-1034-refreeze-hardening-execbit.md`, AC9's check clause (its `kill -TERM`-only runtime probe; no `kill -INT`/`kill -HUP` line in the frozen text); Notes for engineer / Assumptions ("If the executing side finds the probe flaky or unsynthesizable on this host, the correct response is to say so at the freeze gate... never to ship it as apparent coverage" — stated of AC9 as written, which contains no INT clause to be broken); measured: `bash -c 'trap "echo TRAPPED" INT; sleep 3' &` followed by `kill -INT <pid>` after a 1s delay reproduces the identical un-trappable-ignore behavior with no target script involved, isolating the cause to the shell.

<!-- END provenance: T-1034 -->
