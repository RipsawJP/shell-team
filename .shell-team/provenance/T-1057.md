# T-1057 decision provenance

<!-- BEGIN provenance: T-1057 -->
- decision: DP6's branch selector (whether this harness accepts a per-invocation model for a sub-agent) was measured as "unconfirmed from this role's own execution context" rather than assumed, and Branch B (the frontmatter pins stay; a two-directional pin-to-default equality lock ships instead; pin retirement travels to an issue) was taken.
  reason: this role's own tool surface (Read, Edit, Write, Bash — no Task/Agent tool) has no way to invoke a sub-agent or inspect the harness's Agent-tool parameter schema; a repo-wide search for prior evidence (`grep -rn "Task tool\|subagent_type\|Agent tool"`) turned up only `skills/review-response/SKILL.md`'s own invocation shape, which names `subagent_type` but no per-call `model` override parameter. Given the capability cannot be confirmed true from the executing side, and DP6 states both branches are pre-decided so neither the freeze nor the implementation has to reopen the design, Branch B is the conservative choice: it never-drops item 2 (no-config v1-equivalent behaviour) either way, and it requires editing no `agents/*.md` file at all, since AC5's own OR-clause is already satisfied when a role's frontmatter `model:` line equals the shipped default's model token for that role — true today for all five `claude-cli` roles without any edit.
  grounding: `.shell-team/specs/T-1057-loop-integration.md` DP6 (both branches pre-decided; "the confirmation owner is the executing side") and `## Assumptions`'s third relayed-and-unmeasurable bullet (this role cannot read the harness's Agent-tool parameter surface); this engineer turn's own declared tool list (Read, Edit, Write, Bash); `skills/review-response/SKILL.md`'s `codex-reviewer` invocation line (names `subagent_type`, no model-override parameter).
- decision: the resolver's own refusal token for "the effective binding, the delegated contract, or a bound adapter's definition did not resolve to a well-formed, trustworthy form" is a single token, `binding-unresolved`, fixed at exit code 2 in every case — including the AC8-shape re-assertion failures of the delegated canonical form, which are structurally a different condition (a malformed sibling response) from an ordinary content refusal (an invalid host config, already exit 1 inside `check-binding.sh` itself).
  reason: DP4 mints `binding-unresolved` for "the effective binding would not resolve" but does not state its exit code, and AC15's own token inventory (`for t in capability-unsupported contract-violation executor-unavailable binding-unresolved usage`) fixes the vocabulary at five tokens without fixing exit codes either. AC8's own check requires exit 2 for every one of its eight malformed-canonical-form mutations, and the spec's own preamble reserves exit 1 for genuine content-level refusals (deliberately distinguishable from bash's own errexit-1 fallback) and treats "the input could not be evaluated at all" as the exit-2 shape throughout `check-binding.sh`/`check-adapter.sh`'s own conventions. Since every `binding-unresolved` case in this resolver is exactly that shape — a sibling refused, a sibling's canonical output failed re-assertion, or a bound adapter's own definition file is unreadable or malformed in the two fields this resolver reads — exit 2 uniformly is both consistent with AC8's fixed requirement and with the repo's own usage/content exit-code split, and a single fixed code per token keeps the refusal table itself simple (no branch inside `binding-unresolved` needs a second exit code).
  grounding: `.shell-team/specs/T-1057-loop-integration.md` DP4 (mints `binding-unresolved`, no exit code stated), AC8 (`test "$g" = "2"` on every one of its eight mutation cases), AC15 (the five-token closed vocabulary, no exit-code table), and the AC-preamble sentence ("content refusals here exit `1`, which is also bash's own `errexit` fallback"); `bin/check-binding.sh`'s and `bin/check-adapter.sh`'s own exit-code convention (1 = content refusal, 2 = could-not-evaluate); `bin/resolve-executor.sh`'s `refuse binding-unresolved 2 ...` call sites (every one of them).
- decision: the resolver reads each bound adapter's own definition file directly (a bare two-field grep/awk extraction of its `capability effort` and `carries board-transition` rows), rather than delegating that read to `bin/check-adapter.sh --adapter <token>`.
  reason: DP2's Goal-level phrasing ("delegates every act of parsing to `bin/check-binding.sh --print-binding` and to `bin/check-adapter.sh`") could be read as requiring delegation here too, but `check-adapter.sh --adapter TOKEN` performs a STRICTER internal-consistency validation (`validate_definition`'s capability/effort-mechanism/effort-value agreement check) than this resolver's two rules need, and AC6's own fixture — `sed`-mutating only the `capability effort supported` line to `unsupported` while leaving `effort-mechanism cli-flag` and five `effort-value` rows untouched — is deliberately internally INCONSISTENT under that stricter check: running the mutated definition through `check-adapter.sh --adapter claude-cli` would refuse `capability-inconsistent`, not the `capability-unsupported` token AC6 requires on stderr. DP2's own text scopes the "same discipline" (re-assertion before trust) explicitly to `--print-contract`'s role-board-authority rows, not to adapter definitions, so a direct, narrow, two-field read — reading only what this resolver's two normative rules actually consume, never re-implementing `check-adapter.sh`'s full grammar — is both what AC6/AC7's fixture technique presupposes and the reading DP2's own scoped sentence supports.
  grounding: `.shell-team/specs/T-1057-loop-integration.md` Goal (the delegation sentence) and DP2 ("The same discipline applies to `bin/check-adapter.sh --print-contract`, whose `role-board-authority` rows DP4 consumes" — scoped to `--print-contract` only) and AC6 (the `sed 's/^capability effort supported$/capability effort unsupported/'` mutation, leaving `effort-mechanism`/`effort-value` rows untouched, and the required `capability-unsupported` token); `bin/check-adapter.sh`'s `validate_definition` function (the `capability`/`effort-mechanism`/`effort-value` consistency check that would refuse this exact mutated fixture with a different token); `bin/resolve-executor.sh`'s `capability_effort_for`/`carries_board_transition_channel` functions (direct `awk` field reads, no call to `check-adapter.sh --adapter`).
- decision: `templates/binding-default.conf`'s `effort` column is the literal `-` (unset) on every one of the six rows, including `tech-lead` — never a declared value such as `high`.
  reason: DP5 states the model column is v1-equivalent "by measurement" but is silent on the effort column's value; a repo-wide check of every loop invocation site (`skills/run/SKILL.md`, `skills/goal/SKILL.md`) found no `--effort` flag or per-role effort level passed anywhere in the current (pre-T-1057) loop, so no role's v1 invocation ever applied a per-invocation effort level. `templates/binding-template.conf`'s own specimen row for `tech-lead` (`high`) is illustrative documentation of the grammar (showing a non-`-` value is legal), not a claim about v1's actual behaviour, and DP10 forbids editing that specimen or treating its rows as the source of the v1-equivalent facts (which now live in this file instead). A default that introduced a real effort level where v1 applied none would not be v1-equivalent — it would be a new behaviour the migration is supposed to avoid (Non-goals: "No new stop the previous version did not have").
  grounding: `.shell-team/specs/T-1057-loop-integration.md` DP5 (silent on the effort column), DP10 (`templates/binding-template.conf` is a specimen, left byte-untouched, and its rows are not the source of the v1-equivalent facts), and the pre-commitment's never-dropped item 2 ("no new stop the previous version did not have"); `skills/run/SKILL.md` and `skills/goal/SKILL.md` (neither file's telemetry or invocation instructions name a `--effort` flag anywhere); `templates/binding-default.conf`'s own six `bind` rows (every `effort` field is `-`).
- decision: `tests/team-init/run.sh`'s pre-existing AC5 generic-content sweep (`ripsawjp|loop-engineering|T-[0-9]{3,}`) is amended to exclude the newly scaffolded `binding.conf.example` from its target set BY NAME (a `find ... ! -name 'binding.conf.example' ... | xargs grep`), with a new positive-control assertion proving the excluded file does still match the pattern — rather than widening the regex, or editing `templates/binding-template.conf` to remove its own `T-1054` citation.
  reason: `templates/binding-template.conf` (DP10, byte-untouched by this task) carries a legitimate authorship citation, `T-1054`, in its own header — the same class of provenance-not-bake-in citation `templates/liveness-reasons.txt` carries for `T-1056` (named in this spec's own `## Summarized sources` entry for `docs/loop-engineering/task-envelope.md` as correctly out of scope). Scaffolding a byte-copy of that specimen for the first time (AC14/DP10) is this task's own explicit requirement, and it is the FIRST scaffolded template to carry a concrete task-id citation, so the existing (pre-T-1057) AC5 sweep — written before any scaffolded file carried one — trips on legitimate content rather than a real bake-in. Excluding one named file, rather than loosening the pattern generally, keeps the sweep's actual protection (no `ripsawjp`/`loop-engineering`/concrete-task-id leaking into any OTHER generated file) exactly as strict as it was.
  grounding: `.shell-team/specs/T-1057-loop-integration.md` DP10 ("`templates/binding-template.conf` is left byte-untouched") and the Goal sentence assigning `bin/team-init.sh` the scaffold duty; `templates/binding-template.conf`'s own header line ("T-1054; GitHub issue #203"); `tests/team-init/run.sh`'s pre-existing AC5 block (before this task's edit) and its own comment ("the literal placeholder `T-XXX`... is intentionally allowed").
- decision: round-1 review rework, Blocker 1 — the occupancy-lattice fix at `<base>/binding.conf` deliberately keeps a LIVE symlink resolving to a regular file behaviorally identical to a plain regular file (both take the "host config wins" branch), stated explicitly rather than left as an accidental side effect of `[ -f ]`'s own symlink-following semantics; only a dangling symlink, a directory, a FIFO, or any other non-regular occupant is newly refused `binding-unresolved`. The "is anything present at all" test added for the refusal branch is `[ -e "$HOST_CONFIG" ] || [ -L "$HOST_CONFIG" ]` — `-e` alone would miss a dangling symlink (its target does not exist, so `-e` is false exactly like true absence), which is precisely the reviewer's first reproduced case.
  reason: the review explicitly asked this decision be made and stated rather than left implicit ("decide and state the semantics... keeping that is defensible, say so explicitly"). Treating a live symlink-to-regular the same as a regular file is defensible because a host authoring the config at a symlinked path (a config-management tool's own convention, a shared-config pattern) is not an accident DP13's threat model needs to guard against — there is nothing broken about that occupant, unlike a dangling link or a directory. The `[ -e ] || [ -L ]` combination is the same "any occupant, including a dangling symlink" idiom `bin/team-init.sh`'s `copy_template()`/`copy_template_protected()`/`ensure_gitkeep()` already use for the identical reason (a dangling symlink is invisible to `-e` alone).
  grounding: the round-1 review record (`.shell-team/reviews/T-1057.md`, Blocker 1, "decide and state the semantics... say so explicitly"); `bin/team-init.sh`'s `copy_template()` (`{ [ -e "$dest" ] || [ -L "$dest" ]; }` — the same any-occupant-including-dangling-symlink test, ported here); `bin/resolve-executor.sh`'s occupancy-lattice comment block immediately above the `if [ -f ] ... elif [ -e ] || [ -L ] ... else` chain.
- decision: round-1 review rework, Blocker 2 — an unrecognized or malformed board-transition channel value refuses `binding-unresolved` for EVERY role bound to that adapter, not only for a `writes`/`proposes`-authority role — the membership check runs unconditionally inside `carries_board_transition_channel`, before the authority rule's own `case` statement ever sees the value.
  reason: a channel value outside the contract's own closed `channel` set is evidence the ADAPTER DEFINITION itself is malformed (a typo, a stale value, a value from a retired or nonexistent vocabulary), a property of the definition independent of which role happens to be bound to it — DP13's own threat model (an accident, not a targeted attack on one role) supports treating this as a structural defect rather than a per-role conditional one. This also keeps the fix simple and matches the other three narrow readers' shape (each already refuses unconditionally on a malformed value, never conditioned on the caller's own authority), and it does not conflict with AC7's own frozen fixture: `not-carried` is itself a legitimate closed-set member, so AC7's `tech-lead`/`none`-authority case (mutated to `not-carried`, a VALID value) still resolves exactly as that criterion requires — only a value outside the closed set at all triggers this new, unconditional refusal.
  grounding: the round-1 review record (`.shell-team/reviews/T-1057.md`, Blocker 2, "the comparison must be exact-match against the CLOSED channel vocabulary... refuse an unknown token, never default it to the safe branch"); `.shell-team/specs/T-1057-loop-integration.md` DP13 (accidents, not a hostile operator) and AC7 (the `not-carried`-mutation fixture, which remains green since `not-carried` is a real `channel` row); `bin/resolve-executor.sh`'s `carries_board_transition_channel()` (the `channel_known` call happens before `lookup_and_validate_role`'s authority `case` statement is ever reached) and its own `channel-membership-unrecognized-value`/`channel-membership-positive-control` suite cases (which exercise the mutation against `engineer`, a `writes`-authority role, but the refusal itself is not scoped to that authority value in the implementation).
- decision: round-1 review rework, Major 3 — `effort_value_declared`'s new field-count check (`NF==2`) is scoped PER REQUESTED VALUE — only the `effort-value` row(s) that would match the value actually being asked about must be well-formed — rather than auditing every `effort-value` row in the adapter definition regardless of relevance.
  reason: unlike `capability_effort_for`/`carries_board_transition_channel`/`role_board_authority_for`, each of which validates exactly ONE expected row (so any malformed instance of that one row is inherently the row this function cares about), `effort-value` is a MULTI-ROW membership check by design — an adapter legitimately declares several. A malformed row naming a DIFFERENT value than the one requested is not a defect in the answer this function is asked to give ("is $val specifically declared, by a well-formed row"), and auditing the whole population would conflate two distinct concerns (this value's own membership vs. the file's overall well-formedness) that the other three readers keep separate by construction (each is already scoped to its own single expected row). Keeping the same per-key scoping across all four readers is also what the round-1 review's own suggested fix names generically ("add an explicit NF check inside each predicate") without prescribing whole-file auditing for the one reader whose vocabulary is inherently multi-row.
  grounding: the round-1 review record (`.shell-team/reviews/T-1057.md`, Major 3, "add an explicit `NF==3` (or `NF==2` for `effort-value`) check inside each predicate, refusing `binding-unresolved` on mismatch" — no whole-population-audit language); `bin/resolve-executor.sh`'s `capability_effort_for`/`carries_board_transition_channel`/`role_board_authority_for` (each scoped to exactly one expected row by its own `$1`/`$2` key) versus `effort_value_declared` (scoped to the one row matching the caller-supplied `$2`); the suite's `field-count-effort-value` case (the malformed row disqualifies only ITS OWN value from membership, observed as `capability-unsupported` rather than `binding-unresolved`, which is the behavioral signature of this scoping choice).
<!-- END provenance: T-1057 -->
