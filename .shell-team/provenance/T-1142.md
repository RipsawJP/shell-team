<!-- BEGIN provenance: T-1142 -->

- decision: rewrote the closing sentence of `## How changes get merged`'s new paragraph so that the fixed string `the diff` occurs in lower case, mid-sentence after a colon ("...however many records the change touches: the diff is the record of what changed."), rather than shipping the spec's `## Notes for engineer` proposed draft verbatim, whose corresponding sentence begins "The diff is the record of what changed." with a sentence-initial capital `T`.
  reason: AC1's `check:` line matches `the diff` as a case-sensitive `grep -qF` fixed string. A sentence-initial "The diff" does not contain the lower-case substring `the diff` and would fail AC1 as written. Restructuring the two closing sentences into one, joined by a colon, preserves every word of the proposed draft's meaning while placing the required phrase in the case the criterion matches.
  grounding: `.shell-team/specs/T-1142-records-maintenance-rule.md` **AC1**'s `check:` line (`for s in 'records maintenance' 'nothing more' 'commit subject' 'pull-request title' 'changelog entry' 'the diff'; do grep -qF -- "$s" "$D/sec.txt" || rc=1; done`) and `## Notes for engineer`'s proposed wording for `## How changes get merged` ("The diff is the record of what changed."); live run of `awk '/^## How changes get merged$/{f=1;next} /^## /{f=0} f' CONTRIBUTING.md | grep -qF -- 'the diff'` before the rewrite (exit 1, against the draft's sentence-initial capitalized form) and after (exit 0).

<!-- END provenance: T-1142 -->
