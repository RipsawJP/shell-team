<!-- BEGIN provenance: T-1157 -->

- decision: Insert the `--help`/`-h` arm immediately after each of the 14 scripts' own `set -euo pipefail` line — before locale-only statements too (`export LC_ALL=C` in `cluster-failures.sh` / `rollup-runs.sh`) and before any helper-function *definition* (`die()` in `codex-capture.sh` / `log-run.sh`, `emit_stop()` in `loop-guard.sh`, `usage()` in `goal-state.sh` / `rework-digest.sh`), rather than after those definitions or immediately before the first argument read.
  reason: `## Notes for engineer` states the arm goes "immediately after each script's `set -euo pipefail` line, before any other statement", and calls out `log-run` / `check-readme-version` / `codex-capture` by name as needing the arm before their `SCRIPT_DIR`/`ROOT`/manifest/`team-paths` work. A function *definition* has no side effect and would not itself violate **AC1**'s no-side-effect clauses, but placing the arm strictly first, before every other statement without exception, is a single uniform rule across all 14 rather than a per-file judgment call about which statements are "side-effecting" and which are not.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` `## Notes for engineer` ("Placement: put the arm immediately after each script's `set -euo pipefail` line, before any other statement" and "log-run / check-readme-version / codex-capture: the arm must come before the SCRIPT_DIR/ROOT/manifest/team-paths work, not only before the loop_id check").

- decision: Print the header with a single `awk` pass whose comment-stripping regex is byte-identical to **AC1**'s own check-side regex, `sub(/^#[ \t]*/, "", line)` (strip a leading `#` plus zero-or-more spaces/tabs), rather than the single-optional-space form `bin/check-pii-shapes.sh`'s `print_help` uses (`sed 's/^# \{0,1\}//'`).
  reason: **AC1**'s check builds its membership set `H` from the current file's own header region using exactly `sub(/^#[ \t]*/, "", h)` before trimming trailing whitespace, and separately fully trims leading/trailing whitespace off every printed stdout line before testing membership — so the printed line's exact leading-whitespace count does not have to match the check's own stripping as long as internal and trailing content are preserved unchanged. Matching the check's own regex removes the one remaining risk (a header line with more than one space after `#`, printed with residual leading spaces) without adding a second `sed` stage — one `awk` pass stays the whole mechanism, avoiding `grep | head` under `pipefail` per the header-surfacing precedent this spec cites.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` **AC1**'s check line (`awk 'NR==FNR { h=$0; if (h !~ /^#/) next; sub(/^#[ \t]*/, "", h); ...`) and `## Notes for engineer` ("one `awk` pass over the script's own file, finding the header's end at the first `set -euo pipefail` line rather than a hardcoded line range (see `bin/check-pii-shapes.sh:278`–`:294`) ... Avoid `grep | head` under `pipefail`").

- decision: Amend `bin/loop-guard.sh`'s header sentence by appending a new clause after the existing "stdout always carries the decision." text, rather than rewriting or replacing that sentence.
  reason: **AC1**'s check reads the branch-point header (`$T/hb`) for loop-guard and asserts it still contains the literal `stdout always carries the decision` (a read of the OLD blob, unaffected by the current file), and separately asserts the current header (`$T/hh`) contains the literal `--help`. Appending, rather than rewriting, keeps every other reader of this file's header intact and makes the change a single additive clause, the smallest edit satisfying both the "--help is the exception" requirement and the Non-goals' "no new usage prose" spirit for the other 13 scripts.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` **AC1** ("whose branch-point header carried `stdout always carries the decision`") and its check's `if [ "$s" = bin/loop-guard.sh ]` branch; `## Non-goals` ("`bin/loop-guard.sh --help` no longer prints `STOP:guard_error`, and its header contract is amended to say so").

- decision: Build `tests/bin-help/run.sh`'s population, per-case scratch directory, and fail-closed git-status handling directly on `tests/bin-exec-bit/run.sh`'s pattern (`git -C "$REPO_ROOT" ls-files -- bin/`, explicit exit-status capture rather than `|| true`, `pass`/`fail` counters, a trailing summary line), and add a `# shellcheck disable=SC2329` comment above the EXIT-trap `cleanup()` function, copying the exact directive and wording already used for the same shellcheck-0.11.0 false positive in `bin/aggregate-verdicts.sh`.
  reason: `## Notes for engineer` names `tests/bin-exec-bit/run.sh` as the model to follow, and its population-source and fail-closed idioms are exactly what **AC2**'s check re-derives and greps for. Bisecting a plain `shellcheck` failure (SC2329 "this function is never invoked") down to a minimal reproduction showed it fires on an EXIT-trap-only-invoked function specifically when the script also contains an earlier `if ...; then ...; exit 1; fi` branch before the trap is registered — the same pattern already present, and already worked around with this exact directive, in `bin/aggregate-verdicts.sh`; reusing the repo's own precedent keeps one convention for the same tool quirk rather than inventing a different suppression.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` `## Notes for engineer` ("Suite: model it on `tests/bin-exec-bit/run.sh`") and **AC2**'s check (`grep -Eq 'ls-files.* -- bin/'`, `<[[:space:]]*/dev/null`, `-mindepth 1`); `bin/aggregate-verdicts.sh` line `# shellcheck disable=SC2329 # invoked indirectly, via the EXIT trap below` (read first-hand, the precedent for the identical directive added to `tests/bin-help/run.sh`).

- decision: Do not modify any of the 41 already-armed scripts or `bin/install`. Record the zero-fix outcome as a `bin/derive-populations.sh` block (label `t1157-already-armed-fixes`, appended after this file's own provenance marker pair, the same placement `.shell-team/provenance/T-1136.md` already uses for its own derivation blocks) rather than a prose sentence, because a zero-decision sentinel and a decision-entry region are mutually exclusive under `bin/check-provenance.sh`'s schema and an embedded block would itself be an unrecognized non-blank line inside a decision entry.
  reason: A fresh run of `tests/bin-help/run.sh` against the post-implementation tree produced zero `FAIL` lines, so the already-armed population (the 41 scripts `git grep` finds plus `bin/install`, 42 total) needed no fix. `## Assumptions` requires the engineer to record the failing set "in provenance as a `bin/derive-populations.sh` block with its `- reproduce:` line", and Body-to-AC row 9 states the count is info-only "because a count of zero is equally correct" — a derived, reproducible block makes that zero itself falsifiable (re-running the same two `--set` commands against a later tree would show a non-empty `suite-failed` bucket if a regression appeared), rather than an unverifiable claim.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` `## Assumptions` ("Any failure is fixed in this task, and the engineer records the failing set in provenance as a `bin/derive-populations.sh` block with its `- reproduce:` line") and `## Body-to-AC correspondence` row 9 ("The count of such fixes is info-only ... because a count of zero is equally correct"); `bin/check-provenance.sh --help`'s schema description (a decision entry region admits no non-blank line beyond `decision:`/`reason:`/`grounding:`) and `.shell-team/provenance/T-1136.md` (the precedent placing its own `- reproduce:` / derivation blocks after the `<!-- END provenance: T-1136 -->` marker rather than inside a decision entry).

- decision: Add the shipped-docs paragraphs as a new trailing bullet appended to each existing contiguous bullet list (`## Operating rules` in both adopting pages, the `## About CI on your pull request` prose block in `CONTRIBUTING.md`) rather than as a new heading or a standalone paragraph elsewhere in the file.
  reason: **AC4**'s check reads the target text in `awk -v RS=` paragraph mode, where a paragraph is a blank-line-delimited block; a bullet list with no blank line between items is one paragraph in that mode. Appending to the end of an existing bullet list keeps the new sentence inside the same paragraph the check inspects, satisfies the positive control (no other paragraph in either file names both `` `--help` `` and `` `-h` `` — confirmed by grepping both files for each token before the edit), and avoids adding a new heading that would perturb any heading-count-based check elsewhere in these large files.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` **AC4**'s check (`awk -v RS= 'index($0, "\`--help\`") && index($0, "\`-h\`") && index($0, "bin/") {n++} END {exit n ? 0 : 1}'`) and `## Notes for engineer` ("one paragraph in each adopting page, near the `bash \"<plugin root>/bin/<script>\"` invocation form ... one paragraph in `CONTRIBUTING.md`").

- decision: (rework, release-sweep regression) Do NOT edit `CONTRIBUTING.md`'s `--help` paragraph to remove its literal `tests/bin-help/run.sh` path, even though T-1000 AC19 forbids that literal path in `CONTRIBUTING.md` and this task's own diff introduced it. Leave T-1000 AC19 red and report the conflict instead.
  reason: T-1157's own frozen **AC4** check requires the literal substring `tests/bin-help/run.sh` to co-occur with `--help` in the same paragraph of `CONTRIBUTING.md` (`awk -v RS= 'index($0, "--help") && index($0, "tests/bin-help/run.sh") {n?} ...'`), live-confirmed PASS before this round's edits. T-1000 AC19 requires zero occurrences of the regex `tests/[a-z][a-z0-9-]*/run\.sh` anywhere in the same file, live-confirmed FAIL. `tests/bin-help/run.sh` matches that regex, so no wording of the paragraph can satisfy both frozen, intent-block-owned criteria at once — reword one and the other flips. Both ACs sit inside `<!-- BEGIN/END intent-block -->` regions this role does not edit, so this is exactly the "fix would break a frozen T-1157 AC" case the rework briefing named as a stop-and-report condition, not a silent pick between two frozen contracts.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` **AC4**'s check line (line containing `index($0, "tests/bin-help/run.sh")`); `.shell-team/specs/T-1000-operating-conventions.md` **AC19**'s check line (`grep -oE 'tests/[a-z][a-z0-9-]*/run\.sh' CONTRIBUTING.md | wc -l`) and its `## Notes for engineer`-equivalent guidance at "Do not add a `tests/<name>/run.sh` path to `CONTRIBUTING.md`"; live run `bash bin/check-acs.sh .shell-team/specs/T-1157-bin-help-flag.md 2>&1 | grep '^AC4:'` → `PASS`, `bash bin/check-acs.sh .shell-team/specs/T-1000-operating-conventions.md 2>&1 | grep '^AC19:'` → `FAIL`, both this round.

- decision: (rework, release-sweep regression) Fix `.shell-team/test-recipe.md`'s `## CI parity` regression by restoring the existing shellcheck line to its byte-exact `30d71fcb` form and adding `shellcheck tests/bin-help/run.sh` as a wholly new, separate line inside the same fenced code block, rather than re-appending the new target to the end of the restored line.
  reason: T-1152 **AC18** requires every non-blank line of the file's `30d71fcb` blob to still appear verbatim (whole-line match) somewhere in the current file — appending text to the existing line changes its bytes and makes it fail that whole-line match, which is exactly how this round found the regression. The workflow file's own precedent for the same class of addition (T-1151's `check-adopter-docs.sh`/`tests/check-adopter-docs/run.sh`) was a **separate** `shellcheck (T-1151 additions)` step, never appended to the main `shellcheck` step's line, and this task's own diff already followed that same separate-step shape in `.github/workflows/check-handoff.yml` (`shellcheck (T-1157 additions)`). A new line inside the recipe's fenced block mirrors that precedent and still satisfies T-1157's own frozen **AC2**, whose check only requires some `^[[:space:]]*shellcheck ` line inside `## CI parity` to contain the substring `tests/bin-help/run.sh` — it does not require that substring on the pre-existing line.
  grounding: `.shell-team/specs/T-1152-codex-sandbox-hotfix.md` **AC18**'s check line (`grep -qxF -- "$l" "$F"` per non-blank base line); `.github/workflows/check-handoff.yml` commit `055c1a5e` (T-1151, `git show 055c1a5e -- .github/workflows/check-handoff.yml`) adding a separate `shellcheck (T-1151 additions)` step rather than appending to the main line; `.shell-team/specs/T-1157-bin-help-flag.md` **AC2**'s check line (`awk '/^## CI parity[[:space:]]*$/ {f=1; next} /^## / {f=0} f && /^[[:space:]]*shellcheck / && index($0, "tests/bin-help/run.sh") {n++} END {exit n ? 0 : 1}'`); live run `bash bin/check-acs.sh .shell-team/specs/T-1152-codex-sandbox-hotfix.md 2>&1 | grep '^AC18:'` → `PASS` and `bash bin/check-acs.sh .shell-team/specs/T-1157-bin-help-flag.md 2>&1 | grep '^AC2:'` → `PASS`, both after this fix.

- decision: (v2 AC4 re-freeze fix) Reword `CONTRIBUTING.md`'s sentence to "The `` `bin-help` `` suite enforces this in CI for every tracked `bin/` entry (T-1157, issue #592)." — naming the suite by name — rather than dropping the CI-enforcement sentence entirely or wording it to avoid naming any suite.
  reason: The operator-ratified v2 AC4 requires one paragraph of `CONTRIBUTING.md` to name `--help` together with the backtick-quoted token `` `bin-help` `` (its check: `index($0, "--help") && index($0, "\`bin-help\`")`), and forbids any literal `tests/[a-z][a-z0-9-]*/run\.sh` path anywhere in the file — the same regex T-1000 AC19 polices. Naming the suite by its short name rather than its path satisfies both simultaneously: contributors still learn a CI suite exists and enforces the rule (the operative claim), and no literal test-path string remains for AC19's regex to match. Dropping the sentence instead would satisfy AC19 but not AC4's own co-occurrence requirement.
  grounding: `.shell-team/specs/T-1157-bin-help-flag.md` **AC4**'s check line (`awk -v RS= 'index($0, "--help") && index($0, "\`bin-help\`") {n++} END {exit n ? 0 : 1}'`) and its prose ("One paragraph of `CONTRIBUTING.md` names `--help` and the `` `bin-help` `` suite by name"); `.shell-team/specs/T-1000-operating-conventions.md` **AC19**'s check line (`grep -oE 'tests/[a-z][a-z0-9-]*/run\.sh' CONTRIBUTING.md | wc -l`); live run this round: `bash bin/check-acs.sh .shell-team/specs/T-1157-bin-help-flag.md 2>&1 | grep '^AC4:'` → `PASS`, `bash bin/check-acs.sh .shell-team/specs/T-1000-operating-conventions.md 2>&1 | grep '^AC19:'` → `PASS`.

<!-- END provenance: T-1157 -->

- reproduce: bash bin/derive-populations.sh --label t1157-already-armed-fixes --set already-armed="{ git grep -l -E -e '--help[|]-h[)]' -- 'bin/*.sh'; printf '%s\n' bin/install; } | sort" --set suite-failed="bash tests/bin-help/run.sh 2>&1 >/dev/null | grep '^FAIL: ' | awk '{print \$2}' | sort -u" --accept-status suite-failed=1

<!-- BEGIN derivation: t1157-already-armed-fixes -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: already-armed — status: 0 — lines: 42 — items: 42 — command: { git grep -l -E -e '--help[|]-h[)]' -- 'bin/*.sh'; printf '%s\n' bin/install; } | sort
- set: suite-failed — status: 1 — lines: 0 — items: 0 — command: bash tests/bin-help/run.sh 2>&1 >/dev/null | grep '^FAIL: ' | awk '{print $2}' | sort -u
- union: items: 42
- bucket: already-armed — items: 42
  - bin/aggregate-verdicts.sh
  - bin/check-acs.sh
  - bin/check-adapter.sh
  - bin/check-adopter-docs.sh
  - bin/check-binding.sh
  - bin/check-board-headings.sh
  - bin/check-codex-agents.sh
  - bin/check-commit-identity.sh
  - bin/check-count-claims.sh
  - bin/check-durability.sh
  - bin/check-entry-mode.sh
  - bin/check-fanout-instances.sh
  - bin/check-intent.sh
  - bin/check-interventions.sh
  - bin/check-invocation-path.sh
  - bin/check-liveness.sh
  - bin/check-model-pins.sh
  - bin/check-oversight.sh
  - bin/check-pii-shapes.sh
  - bin/check-prompt-sync.sh
  - bin/check-provenance.sh
  - bin/check-refreeze-class.sh
  - bin/check-refreeze-grant.sh
  - bin/check-review-input.sh
  - bin/check-spec-review.sh
  - bin/close-out.sh
  - bin/consolidate-proposals.sh
  - bin/derive-populations.sh
  - bin/discover-work.sh
  - bin/gen-codex-agents.sh
  - bin/gen-loop-replay.sh
  - bin/gen-playbook-blocks.sh
  - bin/gen-project-status.sh
  - bin/install
  - bin/land-worktree.sh
  - bin/playbook-promote.sh
  - bin/resolve-executor.sh
  - bin/retro-inputs.sh
  - bin/review-gate.sh
  - bin/rollup-track.sh
  - bin/team-init.sh
  - bin/team-paths.sh
<!-- END derivation: t1157-already-armed-fixes -->
