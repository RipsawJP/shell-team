<!-- BEGIN provenance: T-1031 -->

- decision: the two `check-handoff.sh` `NOT_APPLY` triples in `tests/errexit-safe/run.sh` keep reason letter **(a)**, with the surrounding comment's rationale corrected rather than the letter itself changed to a different classification.
  reason: two structurally valid options existed. (a) Reclassify one or both triples to a different letter — e.g. (d), the "accumulator, decoupled from this write's own success/failure" class the same comment already uses for every other site in the file. (b) Keep (a), but correct what it stands for. (b) was chosen because (d) genuinely does not fit either candidate line: the `cannot read file` site (`check-handoff.sh:27`) is a `printf ... >&2` immediately followed by `exit 2` on the very next line — the exact errexit-UNSAFE `DP-3` shape this whole suite exists to find, not an accumulator write "decided/exited elsewhere" — and the `emit()` write, while genuinely an accumulator (matching (d)'s own definition), was grouped with the other line under one shared reason in the original comment, so splitting it out to (d) alone while leaving (a) referring to nothing would read as an unexplained asymmetry. The narrower, still-true ground for (a) is that both lines sit inside `check-handoff.sh`'s own frozen, byte-locked observable contract — its exit codes, classification strings and message format — which T-1031 (D7/AC11) restates and re-freezes rather than hardening; that framing survives even though the FILE itself is no longer byte-unchanged as a whole (T-1031 edits a different region on purpose). The spec's own Open question explicitly leaves this choice — correct the letter, or record why it stands — to the engineer's reading of the file, and this is that reading.
  grounding: `.shell-team/specs/T-1031-check-handoff-flag-anchor.md`, D2 ("if the engineer concludes the classification survives on other grounds, the reason is recorded in the same place") and `## Open questions` (explicitly delegates this judgment); `tests/errexit-safe/run.sh`'s corrected `NOT_APPLY` comment block (the paragraph beginning "check-handoff.sh's two candidate lines... are (a)"), which states the narrowed rationale; `bin/check-handoff.sh:26-29` (the `cannot read file` / `exit 2` shape that rules out reclassifying that line to (d)).

- decision: `bin/close-out.sh`'s D3 comment correction (lines 316-318) was kept at the **same 3-line length** as the text it replaced, rather than allowed to expand to a longer explanation with `tests/errexit-safe/run.sh`'s `close-out.sh:444` ledger pin updated to match the new line number.
  reason: two structurally valid options existed. (a) Write a longer, more thorough correction (the first draft did this, expanding to 8 lines) and update the `close-out.sh:444` pin in `tests/errexit-safe/run.sh` to `close-out.sh:449` to match the resulting shift. (b) Keep the correction to exactly 3 lines, preserving every line number below it — including the unrelated `close-out.sh:444` pin, which has nothing to do with this task's edit region. (b) was chosen, and only after (a) was tried first and measured to break `tests/errexit-safe/run.sh`'s completeness self-audit (the pin at `:444` no longer matched real content at that line, and the real content now at `:449` was reported as an unaccounted candidate) — a live, observed failure, not a hypothetical one. D3 itself warns that "the file carries its own ledger pin at `close-out.sh:444` that a logic edit could disturb," which reads naturally as a warning against LOGIC edits; a same-length comment edit was the option that left that unrelated pin genuinely undisturbed rather than requiring a second, out-of-scope ledger update to compensate for a self-inflicted line-count shift.
  grounding: `.shell-team/specs/T-1031-check-handoff-flag-anchor.md`, D3 ("the file carries its own ledger pin at `close-out.sh:444` that a logic edit could disturb"); this session's own measured `bash tests/errexit-safe/run.sh` failure against the first (8-line) draft of the comment ("FAIL: completeness self-audit: found errexit-unsafe candidate site(s) NOT in NOT_APPLY... close-out.sh:449" / "FAIL: ...NOT_APPLY entry no longer matches any real candidate line byte-for-byte... close-out.sh:444"), and the same suite passing clean once the comment was trimmed back to 3 lines.

<!-- END provenance: T-1031 -->

## Runtime disclosure (AC17)

No `check:` line covers this section — it is evidence for a human or a later
gate to read, per the spec's AC17. Everything below was measured live this
session; commands are quoted verbatim so the measurement is reproducible.

### (a) CI wiring

Directly CI-wired (`.github/workflows/check-handoff.yml`) for every file this
task edits:

- `bin/check-handoff.sh` — shellcheck (workflow line 29); "Lint the shipped
  board template" (32); "Dogfood check-handoff — this repository's own
  board" (34-39); "Run check-handoff fixture suite" (42).
- `tests/check-handoff/run.sh` (now wiring the two new fixtures) —
  shellcheck (29); "Run check-handoff fixture suite" (42).
- `bin/close-out.sh` — shellcheck (29); "Run close-out fixture suite" (132)
  exercises it as the fixture suite's subject-under-test.
- `tests/errexit-safe/run.sh` — shellcheck (29); "Run errexit-safe
  regression suite" (213-214).
- `tests/close-out/run.sh` (read, not edited — AC12/AC13's oracle) —
  shellcheck (29); "Run close-out fixture suite" (131-132).
- `tests/machine-tokens/run.sh` (read, not edited — AC10) — shellcheck (29);
  "Run machine-tokens fixture suite" (101-102).
- `bin/check-prompt-sync.sh` (exercised, not edited) — shellcheck (29); "Run
  check-prompt-sync fixture suite" (134-135); "Dogfood check-prompt-sync"
  (137-138).

**Not CI-wired — local execution on this one OS/coreutils is the only
evidence for these**, because the workflow has no step that dogfoods any of
the following against a real per-task artifact (only each checker's OWN
fixture suite, testing the checker's mechanism, runs in CI):

- `bin/check-acs.sh` against `.shell-team/specs/T-1031-check-handoff-flag-anchor.md`
  — `check-acs.sh` is CI-wired only via shellcheck and its own fixture suite
  (`tests/check-acs/run.sh`, workflow 71-72); there is no "run check-acs.sh
  against every merged spec" CI step. Every AC1-AC18 pass/fail claim in this
  hand-off is therefore local-only evidence, from this host (see (d)).
- `bin/check-provenance.sh` against this very file — same shape: shellcheck
  + `tests/check-provenance/run.sh` (191-192) only; no dogfood-on-real-corpus
  step exists for `check-provenance.sh` (unlike `check-retro.sh`,
  `check-playbook.sh` and `check-interventions.sh`, which each have an
  explicit "Dogfood ... this repository's own X" step at 95-99/149-154/
  200-208).
- `bin/check-board-headings.sh .shell-team/todo.md --base 1686495` (AC16's
  clause) — CI-wired only via its own fixture suite (210-211); no CI step
  invokes it against the real board with `--base`.
- Every `git show`/`git diff 1686495 ...` base-ref comparison in AC3, AC8,
  AC11, AC15 — a one-time local git read against this host's checkout of
  `1686495`; CI would re-derive the same comparisons from its own
  `fetch-depth: 0` checkout, but this run's specific measurement is local.

### (b) Mutation self-check

All four required mutations, plus one aimed at a detector's own blind spot,
were performed on scratch copies under `$TMPDIR` (never on the tracked
checkout), each confirmed RED, then restored and confirmed GREEN — except
the fifth, which is the disclosed finding:

1. **Revert `LINE_RE`'s capture group** (`` `[^`]+` `` instead of `` `([^`]+)` ``).
   RED: `bin/check-handoff.sh: line 171: BASH_REMATCH[1]: unbound variable`
   (rc 1) against `decoy-real-flag-invalid.md` — the mutation crashes under
   `set -u` rather than merely misreporting, because nothing populates the
   capture group any more. Restored: AC1's fixture reports
   `unknown status flag 'T1031_DECOY_BAD'` again (rc 1), as before.
2. **Re-introduce a leftmost-anchored second extraction** (the original bug:
   an unanchored `FLAG_RE` read via a second `=~`, replacing the bound
   extraction). RED: the false-PASS fixture now exits **0** (should be 1 —
   the false-PASS direction reopens) and the false-FAIL fixture now exits
   **1** reporting `unknown status flag 'T1031_DECOY_BAD'` (should be 0 —
   the false-FAIL direction reopens) — both directions of the original
   defect reproduced exactly. Restored: both fixtures return to their
   correct exit codes.
3. **Blank one new fixture's decoy token** (`decoy-real-flag-invalid.md`'s
   real-slot `T1031_DECOY_BAD` replaced with a valid flag, `READY_FOR_QA`).
   RED: the fixture now lints clean (rc 0, zero violations), so AC1's own
   `check:` (which requires rc 1 and the specific message) fails against it.
   Restored: AC1's check passes again.
4. **Move the `emit()` write without updating the ledger** (two comment
   lines inserted directly above `emit()`'s `printf`, shifting it from line
   79 to line 81, on a full scratch copy of the repository tree so the real
   `tests/errexit-safe/run.sh` — with its ledger still pinned at `:79:` —
   could run against it unmodified). RED:
   `bash tests/errexit-safe/run.sh` failed two assertions — "found
   errexit-unsafe candidate site(s) NOT in NOT_APPLY... check-handoff.sh:81"
   and "NOT_APPLY entry no longer matches any real candidate line
   byte-for-byte... check-handoff.sh:79" — exactly AC6's live re-derivation
   catching a moved-but-unledgered write. Restored (scratch copy discarded):
   the real, unmutated pair (`:27:`, `:79:`) passes clean.
5. **Detector's-own-blind-spot mutation**: in a scratch copy of
   `decoy-real-flag-valid.md`, the decoy token `T1031_DECOY_BAD` was moved
   out of the title's own `` — `<token>` — spec: <path>.md `` shape
   entirely — the title became a plain, undecorated well-formed entry — and
   the literal string `T1031_DECOY_BAD` was instead placed in an indented
   continuation sub-bullet below it (a harmless, ignored line). **This did
   NOT go red.** AC2's own guard clause (`grep -qF -- 'T1031_DECOY_BAD'
   tests/check-handoff/fixtures/decoy-real-flag-valid.md`) still finds the
   substring — it is textually present, just no longer inside the
   decoy-separator shape — so the guard's "cannot pass vacuously against a
   fixture that lost its decoy" claim is narrower than it reads: it protects
   against the token being deleted outright, not against the token being
   *relocated out of the shape that makes it a decoy* while a copy of the
   bare word survives elsewhere in the file. The checker itself behaved
   correctly throughout (the mutated fixture is, correctly, a well-formed
   line with an unrelated comment beneath it, and 0 violations is the right
   verdict for it) — the gap is in AC2's *own* non-vacuity guard, not in
   `bin/check-handoff.sh`. This is disclosed rather than silently patched:
   AC2 is spec-frozen (its `check:` line is part of the intent block this
   engineer pass does not edit), and widening it is out of this task's scope
   (Non-goals: "Widening or narrowing what the checker accepts" is declined,
   and this finding is about a *test guard*, not the checker under test, but
   the same non-escalation principle applies — a rework/rescope of AC2's own
   guard shape is a question for pm-spec, not a silent engineer fix).

### (c) R2 before/after (measured, not estimated)

Both runs used `bash tests/close-out/run.sh`'s own differential harness
(`closeout-lineshape-differential`), unmodified, against the file's 29-line
line-shape corpus — once from a `git archive 1686495` extraction (a full
scratch tree, since the harness invokes `close-out.sh`, which itself shells
out to `check-handoff.sh`), and once from this branch's current tree:

| | corpus | refused | accepted | notlocated | mismatches |
|---|---|---|---|---|---|
| **before** (`1686495`) | 29 | 14 | 11 | 4 | 0 |
| **after** (this branch) | 29 | 13 | 12 | 4 | 0 |

Exactly one line flipped refused→accepted (`- [ ] **T-901** decoy separator
— ...`, `tests/close-out/run.sh:967`) — matching pm-spec's own static
estimate in the spec's Assumptions (`refused≈13`, `accepted≈12`) almost
exactly, and confirming AC13's `mismatches=0` floor held across the flip,
not merely before or after it in isolation.

### (d) R3 portability

- Host: `GNU bash, version 3.2.57(1)-release (arm64-apple-darwin25)` on
  `Darwin 25.5.0` (macOS, arm64, BSD regex — not glibc).
- AC1, AC2 and AC12 all pass on this host (see the acceptance-criteria
  report in the hand-off). That is the direct behavioural evidence that
  POSIX greedy-subexpression assignment resolves the **rightmost** decoy
  slot here, on a BSD-regex host — the harder of the two libc families to
  have gotten this right by accident, since `bin/close-out.sh:319`'s
  pre-existing rewrite regex (which this fix aligns with) already runs
  successfully in CI on `ubuntu-latest` (glibc), and this session adds the
  BSD-side confirmation locally. Shape A (D1's primary) is not falsified on
  either libc family measured; Shape B (the fallback) was not needed.

### `CHECK_ACS_TIMEOUT` note

AC13 (`bash tests/close-out/run.sh`) measured **14.7s wall-clock** on this
host — well under the 120s default `check-acs.sh` timeout. Per the spec's
Notes for engineer, this is disclosed so a later task can lower the default
from data: this run did not need the `CHECK_ACS_TIMEOUT=900` elevation the
spec pre-authorized, though it was still passed for this run's `check-acs.sh`
invocation out of caution, per the spec's instruction.
