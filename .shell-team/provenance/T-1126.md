<!-- BEGIN provenance: T-1126 -->
- decision: replace the sprint-specific "this stop is not a fourth human gate: it re-enters the existing planning-approval gate, one of exactly three standing human gates this same skill already declares in its own `invariant-lock: human-gate-set` lines" framing (`skills/run/SKILL.md:57` and its mirror in `docs/adopting.md`/`docs/adopting.ja.md`) with "this stop adds no new gate: it is the same authority the loop already defers to before it merges (`invariant-lock: human-gate-set`) reasserting itself once the operator's approved release-tier premise no longer holds" — restating the "not a new gate" claim through the authority framing (merging is a human action) rather than through a gate count.
  reason: the spec's `## Assumptions` row flags exactly this site as the place to "keep this distinction in the replacement prose" — the fixed layer is authority (the loop never merges on its own), not an interruption count — and AC4 forbids the literal phrases "three standing human gate" / "three human gate" / "planning-approval" repository-wide (minus `docs/loop-engineering/`), so any replacement had to drop the gate-counting frame entirely rather than merely rewording it. More than one restatement could have satisfied AC4's negative grep (e.g. citing the completion gate instead of the merge-authority claim); I chose the merge-authority framing because it is the same claim `docs/tuning-oversight.md:12` already states as fixed, keeping this site consistent with the Goal's own wording rather than introducing a third phrasing of "what's fixed" alongside the two already-frozen ones.
  grounding: `.shell-team/specs/T-1126-method-neutral-human-gates.md` `## Assumptions` ("Flagged for the engineer: keep this distinction in the replacement prose"); `.shell-team/specs/T-1126-method-neutral-human-gates.md` **AC4**'s check line; `docs/tuning-oversight.md:12` ("The loop never merges on its own — merging is a human action").

- decision: add an explicit pointer to `docs/tuning-oversight.md` (or its `.ja.md` mirror) inside `templates/prompt-blocks/means-ends-reflection.md`, `templates/prompt-blocks/fanout-orchestration.md`, and the `docs/adopting.md`/`docs/adopting.ja.md` version-derivation passage, beyond the two files (`docs/workflow.md`, `docs/workflow.ja.md`) **AC8** names as the check target.
  reason: AC8 only requires the pointer at `docs/workflow.*`, but the Goal's own prose ("names any further stop as whatever the operator's own oversight configuration adds, pointing at `docs/tuning-oversight.md`") is stated as a property of every reworded occurrence, not of two named files; adding the same pointer at the other reworded sites keeps the claim's own grounding reachable from each site it appears at, rather than leaving four of the eight in-scope files asserting the clause with no cited source. This is additive prose only — no AC required it and none is weakened by its absence, so it was a genuine open choice rather than a forced one.
  grounding: `.shell-team/specs/T-1126-method-neutral-human-gates.md` `## Goal` ("and names any further stop as whatever the operator's own oversight configuration adds, pointing at docs/tuning-oversight.md"); `.shell-team/specs/T-1126-method-neutral-human-gates.md` **AC8**'s check line (scoped to `docs/workflow.md` and `docs/workflow.ja.md` only, confirming the wider placement was not itself required).

- decision: round-2 rework — remove "it stops before a destructive or irreversible operation" from the "what the loop actually guarantees" clause at all 8 sites and restate the fixed layer as only the completion gate ("a task is done only when both gates are green") plus the never-merges authority claim; move the destructive/irreversible stop into "any further stop — including one before a destructive or irreversible operation — is whatever the operator's own oversight configuration adds", pointing at `docs/tuning-oversight.md`. The T-1095 means-ends rule's own third escalation condition ("the pre-priced disposition itself is destructive or irreversible") is left untouched at all sites, since it is a narrow, real escalation trigger distinct from the removed general guarantee.
  reason: Codex review round 2 (`.shell-team/reviews/T-1126.md`, Blocker) measured that round 1's wording reintroduced the exact over-claim T-1126 exists to remove — the shipped generic contract `templates/shell-team.contract.yaml:41-44` declares `human_gate: [merge, push]` only, and `docs/tuning-oversight.md:101` classifies the destructive-operation stop as "a discipline the operator imposes on themselves, not a rule the loop enforces," not a loop guarantee. Two ways existed to close this (frame it under the T-1095 rule's own narrower escalation condition, or move it wholesale into the operator's oversight-configuration clause); I chose the latter at every site because the review's own suggested fix names it as option (b) and it keeps a single consistent restatement across all 8 files rather than mixing framings per site.
  grounding: `.shell-team/reviews/T-1126.md` round 2 Blocker (`templates/prompt-blocks/means-ends-reflection.md:4` finding, "Suggested fix" bullet); `.shell-team/interventions/T-1126.md` entry 2 (`assumption-contradicted`); `templates/shell-team.contract.yaml:41-44` (`human_gate: [merge, push]`); `docs/tuning-oversight.md:10-16` (`## What is fixed, and what is yours`) and `:101` ("Naming it is a discipline the operator imposes on themselves, not a rule the loop enforces").
<!-- END provenance: T-1126 -->
