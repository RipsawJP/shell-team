# T-1066 decision provenance

## No-mechanism pricing evidence (AC12)

- read-set-specs: 33 specs
  - detail: live re-derivation `git grep -lF -e docs/loop-engineering -e .shell-team/runs -- '.shell-team/specs/*.md' | wc -l` = 33 at report time (includes this task's own spec, which names both literals).
- indirection-class: `team-paths.sh --get` (specifically `--get runs` and `--get todo`) — a criterion resolving its target through that resolver rather than a literal path is invisible to the derivation above in principle. Discharge: **ran them too** — `git grep -lF -e 'team-paths.sh --get' -- '.shell-team/specs/*.md' | wc -l` = 60. Inside the union of both derivations, searched for any other criterion whose subject is a population count over `.shell-team/specs/*.md` (`git grep -n "'.shell-team/specs/\*\.md'" -- '.shell-team/specs/*.md'`) or a directory-listing count over `docs/loop-engineering/` (`git grep -n "docs/loop-engineering" -- '.shell-team/specs/*.md' | grep -n '^\s*- check:'` cross-referenced with `wc -l`/`ls`/`find` on the same line): found exactly one, `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC8, disclosed and measured in `.shell-team/specs/T-1066-effort-time-telemetry.md`'s own `## Blast radius` (base: PASS → head: FAIL, 45→46). No directory-listing-over-`docs/loop-engineering/` criterion was found (`T-1008-lessons-corpus-import.md`'s several `docs/loop-engineering` checks all count fixed content inside one named file, not a directory listing). Whatever a criterion reaches through an indirection neither of these two literal-string derivations can see (for example, a hardcoded path fragment built from string concatenation rather than a full literal) is disclosed here as **unmeasured**, not claimed clean.
- mutation-probe: AC1 — deleted the `## Population and exclusions` heading line from a scratch copy of the note in a disposable `git worktree add --detach` (never the working tree) → `AC1: FAIL (exit 1)`; restored from the pre-mutation baseline → `AC1: PASS (exit 0)`.
- mutation-probe: AC2 — appended trailing text after `- metric: overhead-share — declined` (violating the "ends at the disposition word" grammar) → `AC2: FAIL (exit 1)`; restored → `AC2: PASS (exit 0)`.
- mutation-probe: AC3 — deleted the `- result:` sub-bullet from the `engineer-wall-clock-total` computed block → `AC3: FAIL (exit 1)`; restored → `AC3: PASS (exit 0)`.
- mutation-probe: AC4 — changed the declared `- population: total-rows = 1298` line to `9999` (breaking the live-equality re-derivation) → `AC4: FAIL (exit 1)`; restored → `AC4: PASS (exit 0)`.
- mutation-probe: AC5 — replaced every occurrence of `20260804T-t1024` in the note with a non-matching string → `AC5: FAIL (exit 1)`; restored → `AC5: PASS (exit 0)`.
- mutation-probe: AC6 — shortened the exact qualification `not an ask-independent raw-latency measure` to `not an ask-independent measure` → `AC6: FAIL (exit 1)`; restored → `AC6: PASS (exit 0)`.
- mutation-probe: AC7 — replaced every occurrence of the backtick-quoted `` `adapter` `` inside `## Recommended standing instrument` with `` `X` `` → `AC7: FAIL (exit 1)`; restored → `AC7: PASS (exit 0)`.
- mutation-probe: AC8 — appended a line pasting a fake span row (`{"run_id":"fake","duration_ms":123}`) to the end of the note → `AC8: FAIL (exit 1)`; restored → `AC8: PASS (exit 0)`.
- mutation-probe: AC9 — added an untracked stray file (`stray-file.txt`) at the worktree root, outside the allow-list → `AC9: FAIL (exit 1)`; removed it → `AC9: PASS (exit 0)`.
- mutation-probe: AC10 — deleted the top-level `- [ ] **T-1066**` board line from the scratch worktree's resolved board (leaving the delta at `+0` instead of `+1`) → `AC10: FAIL (exit 1)`; restored → `AC10: PASS (exit 0)`.
- mutation-probe: AC11 — duplicated the `- verification-class: no-mechanism — ` line inside the intent-block declaration region of the scratch spec copy (violating the exactly-once count) → `AC11: FAIL (exit 1)`; restored from the pre-mutation baseline → `AC11: PASS (exit 0)`.
- mutation-probe: AC12 — deleted the `- indirection-class: ` line from the scratch provenance copy → `AC12: FAIL (exit 1)`; restored → `AC12: PASS (exit 0)`.
- mutation-probe: AC13 — replaced one allowed `git show <ref>:<path>` command form with a disallowed two-dot `git diff <ref> HEAD -- <path>` form → `AC13: FAIL (exit 1)`; restored → `AC13: PASS (exit 0)`.
- ci-step: Checkout — not-applicable — infrastructure step (repository checkout), not a repository-content check; its behaviour does not depend on this task's diff.
- ci-step: Install shellcheck (pinned — must match local dev version) — not-applicable — tooling-install step, not a repository-content check.
- ci-step: shellcheck — not-applicable — this task's diff adds or edits no file in the step's fixed shellcheck argument list (no path under `bin/` or `tests/` changes).
- ci-step: Lint the shipped board template (hand-off linter) — not-applicable — targets `templates/todo-template.md`, untouched by this task.
- ci-step: Dogfood check-handoff — this repository's own board — applicable
  - detail: this task edits the resolved board (`.shell-team/todo.md`). Run live: `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` exits 0 (see hand-off for the fresh run against the final board state).
- ci-step: Run check-handoff fixture suite — not-applicable — fixture-only suite under `tests/check-handoff`, untouched by this task.
- ci-step: Lint the shipped shell-team loop contract — not-applicable — `templates/shell-team.contract.yaml`, untouched.
- ci-step: Lint the shipped generic loop-contract template — not-applicable — `templates/loop-contract-template.yaml`, untouched.
- ci-step: Lint the shipped goal loop contract — not-applicable — `templates/goal.contract.yaml`, untouched.
- ci-step: Run check-contract fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run loop-guard fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-run fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run log-run resolution suite — not-applicable — fixture-only, untouched.
- ci-step: Run team-init fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run discover-work fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-acs fixture suite — not-applicable — fixture-only suite under `tests/check-acs`; this task's own spec is exercised directly via `bash bin/check-acs.sh`, not through this CI-wired fixture suite.
- ci-step: Run check-design-note fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run goal-state fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rework-digest fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-retro fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs bounded invariants lock — not-applicable — fixture-only, untouched.
- ci-step: Dogfood retro-inputs — the cycle window resolves against this repository — not-applicable — invoked with `--base HEAD`, which resolves an empty cycle window regardless of this task's diff; a resolver mechanism smoke test, not a content-sensitive read of any file this task changes.
- ci-step: Dogfood check-retro — this repository's own retros pass the ledger contract — not-applicable — scans `.shell-team/retros/*.md`; this task adds no retro file.
- ci-step: Dogfood check-retro — the shipped retro template passes its own contract — not-applicable — `docs/templates/retro-template.md`, untouched.
- ci-step: Run machine-tokens fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-loop-replay fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-readme-version fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-readme-version on the repo's READMEs — not-applicable — `README.md`/`README.ja.md`, untouched.
- ci-step: Run rollup-runs fixture suite — not-applicable — fixture-only, untouched (this task reads `bin/rollup-runs.sh`'s documented behaviour but edits nothing under it).
- ci-step: Run rollup-track fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run consolidate-proposals fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run cluster-failures fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run is-span-row-parity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run review-gate fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run close-out fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-prompt-sync fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-prompt-sync — this repo's prompt blocks must be in sync — not-applicable — scans `templates/prompt-blocks/registry.txt` and its registered `agents/`/`skills/` consumers; this task edits none of them.
- ci-step: Run check-playbook fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-playbook-blocks fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run playbook-promote fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-playbook — the real repository corpus at the resolved lessons path is schema-valid — not-applicable — `.shell-team/lessons.md`, untouched by this task.
- ci-step: Dogfood gen-playbook-blocks — regenerating into a scratch copy reproduces every shipped block and consumer — not-applicable — reads `templates/prompt-blocks/registry.txt` and `.shell-team/lessons.md`, both untouched.
- ci-step: Run team-paths fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood team-paths — a repo with no legacy markers resolves to the default layout — not-applicable — asserts fixed resolver output values against this repo's own layout markers, which this task does not touch.
- ci-step: Run install fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-intent fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-provenance fixture suite — not-applicable — fixture-only suite under `tests/check-provenance`; this task's own provenance file is checked directly via `bash bin/check-provenance.sh .shell-team/provenance/T-1066.md`, outside this CI-wired fixture suite.
- ci-step: Run check-interventions fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run interventions-reminder fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-interventions — every committed interventions file in this repository is conformant — applicable
  - detail: loops `.shell-team/interventions/*.md`, and this task's own `.shell-team/interventions/T-1066.md` (orchestrator-authored, untouched by this role) is a new file in that glob. Run live: `for f in .shell-team/interventions/*.md; do bash bin/check-interventions.sh "$f" || echo "FAIL $f"; done` — no `FAIL` line printed (see hand-off).
- ci-step: Run check-board-headings fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run errexit-safe regression suite — not-applicable — fixture-only, untouched.
- ci-step: Run codex-skeleton-hygiene suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-pii-shapes fixture suite — not-applicable — fixture-only, untouched.
- ci-step: check-pii-shapes on the PR diff — applicable
  - detail: scans this task's own PR diff, exactly the file set this task adds/edits. Run live: `bash bin/check-pii-shapes.sh --base "$(git merge-base feature/1065-task-class-verification-pricing HEAD)"` → `clean (no PII-shaped bytes found)`.
- ci-step: Run check-commit-identity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gitignore-raw-dumps lock suite — not-applicable — fixture-only, untouched.
- ci-step: check-commit-identity on the PR commits — applicable
  - detail: scans this task's own PR commits. Run live: `bash bin/check-commit-identity.sh --base "$(git merge-base feature/1065-task-class-verification-pricing HEAD)"` (see hand-off for the fresh result against the final commit set).
- ci-step: Run check-refreeze-class fixture suite — not-applicable — fixture-only, untouched (this task performs no re-freeze of any other spec's intent block).
- ci-step: Run bin-exec-bit lock suite — not-applicable — fixture-only, untouched (no path under `bin/` added or edited).
- ci-step: Run check-durability fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-durability — this repository's own T-1048 implement-phase records — not-applicable — reads T-1048's own durability records, unrelated to T-1066.
- ci-step: Run check-binding fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-binding — the shipped binding-config specimen validates — not-applicable — `templates/binding-template.conf`, untouched.
- ci-step: Run check-adapter fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-adapter — the shipped contract and definitions validate — not-applicable — fixed adapter definitions, untouched.
- ci-step: Run check-liveness fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-liveness — the real classifier's --help — not-applicable — `bin/check-liveness.sh --help`, untouched by this task.
- ci-step: Run resolve-executor fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood resolve-executor — probe-free mode against this repository (the runner carries neither executor CLI) — not-applicable — reads binding config, untouched by this task.

<!-- BEGIN provenance: T-1066 -->
- decision: chose the composite de-duplication key **(`run_id`, `seq`, `span`)** over the spec Notes' other suggested option, `(run_id, ts, span)`.
  reason: measuring both known duplicate-`seq` pairs directly showed `ts` is exactly the field that differs between the two rows in each pair (rows 169/170 differ in `ts` **and** `model`; rows 297/298 differ in `ts` alone) — keying on `ts` would therefore fail to de-duplicate either pair, since the two rows would then hash to two different keys instead of colliding. `seq` is the field both rows in each pair share, so keying on it is the only one of the two named options that actually removes the duplicates.
  grounding: `.shell-team/specs/T-1066-effort-time-telemetry.md` `## Notes for engineer` ("Decide the de-duplication key (`run_id` + `seq` + `span`, or `run_id` + `ts` + `span`)") and the Assumptions section's direct measurement of both pairs; confirmed live by re-running the de-duplication command over the 570-row span population, which removed exactly 2 rows (the two named pairs).
- decision: operationalized `runtime-duty-items` as the count of lettered sub-items (`**(a)**`, `**(b)**`, …) inside each task's one runtime/`SKIP` criterion, rather than adopting the spec Notes' other named candidate, `grep -c '^  - check: '`.
  reason: measuring the candidate directly showed it counts something already covered by a different metric — `criteria-count` minus the one runtime criterion each spec carries — not a duty an engineer discharges with no machine check. The spec's own Notes explicitly instruct declining a metric "if either proves to measure something other than what the metric name says... rather than silently redefining it," but here a second, genuinely-different operationalization existed (the runtime criterion's own enumerated items) that does measure the named concept, so the metric was computed under that operationalization rather than declined.
  grounding: `.shell-team/specs/T-1066-effort-time-telemetry.md` `## Notes for engineer` ("A count of a spec's runtime duties: `grep -c '^  - check: '` counts criteria commands, and the runtime criterion's own item list is where the runtime-duty count comes from... if either proves to measure something other than what the metric name says, decline the metric with that as the measured reason rather than silently redefining it") and a live comparison of both candidate counts across the window's 13 specs (`grep -c '^  - check: '` values track `criteria-count - 1` in every case, never the lettered-item counts, which range 7–14).
- decision: bounded the wall-clock/round metrics' computed table to the window `T-1053`–`T-1065` (issue #248's own stated start, through the task immediately preceding this one under the self-measurement exclusion), rather than extending the table further back into the reviews-only era or including partial `T-1066` data.
  reason: issue #248's own body names the window as "T-1053 → current," and the self-measurement exclusion (DP4) requires this task's own rows to be excluded from the analyzed corpus, which fixes "current" at T-1065 rather than at T-1066 itself. Extending further back (to tasks with no telemetry at all) would have nothing to compute for the wall-clock metrics; the Notes for engineer instead direct carrying those earlier tasks' rigor metrics via the reviews-only series rather than folding them into the same wall-clock table.
  grounding: `.shell-team/specs/T-1066-effort-time-telemetry.md`'s `## Summarized sources` (issue #248 distinction (c), "six per-task metrics over the window T-1053 → current"), DP4 (the self-measurement exclusion) and `## Notes for engineer` ("The reviews-only series is the honest answer for the window's early tasks... a task with no telemetry can still carry its rigor metrics while its wall-clock metrics are declined").
- decision: operationalized `rounds-to-approve` as the count of `"span":"codex-reviewer"` rows for a task's `run_id`, cross-checked against the last such row's `verdict`, rather than the `iteration` field's maximum value (+1).
  reason: measuring `T-1065`'s own telemetry directly showed the `iteration` field is a whole-run monotonic counter spanning every phase (values 4, 5, 6, 8 across its four `codex-reviewer` rows — non-contiguous, because other phases' spans also increment it), not a review-round-specific counter; using it as `rounds-to-approve` would have produced 9 rounds for a task whose actual review history is 4 rounds. The `codex-reviewer` row count is directly interpretable as "how many times review actually ran," which is what the metric name asks for.
  grounding: direct measurement of `.shell-team/runs/shell-team.jsonl`'s `run_id":"20260813T150636Z-t1065` rows (`iteration` values 4, 5, 6, 8 across 4 `codex-reviewer` spans) versus the `codex-reviewer` row count itself (4), reported in the note's own `## Metrics` ledger under `rounds-to-approve`.
- decision: reported `T-1058`'s and `T-1059`'s `rounds-to-approve` as a measured floor rather than silently treating the raw `codex-reviewer` row count as a confirmed final figure.
  reason: `T-1058`'s last recorded `codex-reviewer` row carries verdict `REQUEST_CHANGES`, not `APPROVE`, and `T-1059` carries zero `codex-reviewer` rows at all — in both cases the git-tracked board and reviews corpus show the task reached `READY_FOR_MERGE`, so telemetry demonstrably did not capture the approving round. Reporting the raw count as-is would have silently presented an unconfirmed number as the answer; the spec's own DP5 requires honest infeasibility to be a first-class, disclosed outcome rather than a shortfall papered over.
  grounding: direct measurement of both `run_id`s' `codex-reviewer` rows and their last verdict field, cross-referenced against `.shell-team/todo.md`'s `READY_FOR_MERGE` status for both tasks; `.shell-team/specs/T-1066-effort-time-telemetry.md` DP5 ("Each metric therefore reaches `computed` or `declined`, and a declined metric with its measured reason is a correct result, not a shortfall").
- decision: placed the AC12-required `- read-set-specs:`, `- indirection-class:`, `- mutation-probe:` and `- ci-step:` lines in this file **before** the `<!-- BEGIN provenance: T-1066 -->` marker, outside the region `bin/check-provenance.sh` schema-validates, rather than inside the marked region alongside the decision/reason/grounding triples.
  reason: `bin/check-provenance.sh`'s own schema (read directly, its header comment) accepts, strictly between its markers, only the zero-decision sentinel line or well-formed `- decision:`/`reason:`/`grounding:` triples — "any... unrecognized non-blank line" inside that region is a schema violation (exit 1). AC12's own check line greps this file's four required line-prefixes without scoping to the marker region, so placing them outside the markers satisfies AC12 while keeping the marker-delimited region schema-conformant for AC10's separate `bin/check-provenance.sh` invocation; placing them inside the markers would have made every AC10 run fail on an unrecognized-line schema violation regardless of the triples' own correctness.
  grounding: `bin/check-provenance.sh`'s header comment ("the region strictly BETWEEN the marker lines... must satisfy EXACTLY ONE of: (a) the sentinel... or (b) one or more well-formed decision entries... Any violation... => exit 1"); `.shell-team/specs/T-1066-effort-time-telemetry.md` **AC12**'s check line (greps `$P` directly, no marker-scoped extraction) and **AC10**'s check line (`bash bin/check-provenance.sh .shell-team/provenance/T-1066.md`); confirmed live by running both against this file in its final form.
- decision: **[rework, round-1 correction — supersedes the "reported as a measured floor" decision above]** replaced `rounds-to-approve` and `wall-clock-per-round`'s denominator with the git-tracked reviews corpus's own `### Codex Review verdict` heading count as the canonical source, rather than continuing to report the telemetry `codex-reviewer` span count (labelled a "floor") as the metric's own value.
  reason: Codex round-1 review measured a case (`T-1058`) where the telemetry-derived count was not merely incomplete but wrong in direction — its own review record shows exactly 1 round ending in `APPROVE`, while telemetry carried 2 `codex-reviewer` rows, the second an unrelated, later, higher-`tool_uses` row with verdict `REQUEST_CHANGES` and no matching review heading. Labelling that 2 a "floor" was itself incorrect (a floor implies undercounting; this was overcounting), and the git-tracked reviews corpus already carries the true per-round history for all 13 window tasks, including the three (`T-1053`, `T-1054`, `T-1055`) telemetry has no rows for at all — so it is strictly more complete as well as more correct.
  grounding: `.shell-team/reviews/T-1058.md` (exactly one `### Codex Review verdict:` heading, verdict `APPROVE`) versus the telemetry `run_id":"20260810T222051Z-t1058` rows (2 `codex-reviewer` spans, the second timestamped after the first and carrying `REQUEST_CHANGES`); the round-1 Codex review record (`.shell-team/reviews/T-1066.md`, Blocker finding) that first measured and reported this disagreement.
- decision: **[rework]** wrote the canonical-source heading pattern as `^### Codex Review verdict` (prefix match, no trailing colon required) rather than the narrower `^### Codex Review verdict:` literal.
  reason: a multi-round review file (e.g. `.shell-team/reviews/T-1061.md`) writes its first round's heading as `### Codex Review verdict: <verdict>` but later rounds as `### Codex Review verdict (round N): <verdict>` — the colon comes after `(round N)`, not immediately after `verdict`. The narrower pattern undercounts any such file (measured: it returns 1 for `T-1061` instead of the correct 3). The prefix-only pattern matches both forms and was verified against every window task's own review file, none of which needed a third heading shape.
  grounding: `.shell-team/reviews/T-1061.md` lines 1, 138, 268 (the three heading lines, two of which carry the `(round N)` infix); direct comparison of `grep -cE '^### Codex Review verdict:'` (returns 1) versus `grep -cE '^### Codex Review verdict'` (returns 3) against that file.
- decision: **[rework]** anchored the `freeze-versions` extraction's `grep -oE` at line start (`^  - freeze-attestation \(v[0-9]+`) rather than the unanchored form used when this note was first authored.
  reason: the unanchored form matches a version number anywhere on any line inside a task's own board block, including inside that task's own hand-off prose when it narrates re-freezing a *different* task's spec. Measured directly: `T-1064`'s board block narrates "the two new v3 `- freeze-attestation` lines" for `T-1043` and `T-1050` (two specs `T-1064` itself re-froze), so the unanchored form returned `3` for `T-1064` even though `T-1064`'s own ledger line reads `- freeze-attestation (v1, ...)`. This is exactly the "canonical source, unsafe extraction" class Codex round-1 flagged for `rounds-to-approve`, found here during this rework's own sweep of the other seven metrics for the same class rather than reported by the reviewer.
  grounding: `.shell-team/todo.md`'s `T-1064` board block (the "freeze-attestation (v3)" prose mention, board line ~107 in this task's own hand-off record); live comparison of the anchored versus unanchored `grep -oE` over all 13 window tasks' board blocks (only `T-1064` differs, `3` vs `1`); the anchor-indentation control (`grep -nE '^ +- freeze-attestation \(v[0-9]+' .shell-team/todo.md | grep -vE '^[0-9]+:  - freeze-attestation'` → no output), confirming no genuine ledger line sits at a different indentation the anchor would have to also match.
- decision: **[rework]** replaced the single five-capture `sed -E` regex extraction (used to build the `duration-per-1k-tokens` and de-duplication tables) with five independent single-field `grep -o` passes joined by column position via `paste`, and additionally split the resulting commands across separate physical bullets wherever one combined bullet would otherwise still place the `run_id` and `duration_ms` field-name-plus-quote fragments on the same markdown line.
  reason: AC8 rejects any line containing both `"run_id":"..."` and `"duration_ms":` as a pasted-span-row shape, and the round-1-authored version hid this by describing the five-capture regex in prose instead of quoting it — satisfying AC8 mechanically but not Major 2's demand that every quoted command be the literal, fully-substituted command actually run. Splitting the extraction into independent single-field passes preserves full literalness (every command is exactly what was run, re-run live to confirm the same output) while never producing a single command or a single markdown line that juxtaposes the two field fragments — closing both the AC8 mechanical requirement and the Major-2 reproducibility requirement at once, rather than trading one for the other.
  grounding: Codex round-1 review, Major #2 ("`- command:` 行が...再実行して数値を検算できる...を字義通りには満たさない"); `.shell-team/specs/T-1066-effort-time-telemetry.md` DP2 ("the quoted command is what a later reader re-runs to check the number"); live re-run of every split command against the resolved telemetry file, reproducing the same `570`/`568` row counts and the same by-role/by-date figures as the single-regex version it replaces.
- decision: **[rework, round 2]** defined "flat" as ≤10% relative change and "rising"/"falling" as >25% relative change (with 10–25% left as an explicit `ambiguous` third class, claimed as neither), and applied this single numeric pair uniformly to every transition in both the corpus-wide and window-restricted by-date series, rather than continuing to use the word "flat"/"roughly flat" informally at whatever threshold made a given sentence's point.
  reason: Codex round-2 review measured that round 1's informal usage was internally inconsistent — an 8.6% `duration_ms` change was called "roughly flat" in one place while the blanket claim "neither shape holds... at any point" implicitly treated much larger co-occurring changes (up to 9.9%–10% token spreads, and in one case a ratio move of +34.8%) as not qualifying anywhere else — and demonstrated two concrete counter-examples under any reasonably consistent reading. A real choice existed between several candidate threshold values; 10%/25% was chosen because it is consistent with the note's own pre-existing usage (the 8.6% duration change was already called flat) rather than introducing a new number that would have silently redefined an existing claim, and because it is wide enough to leave a genuine `ambiguous` band (10–25%) rather than collapsing nearly every transition into "flat" or "rising" by having the two bounds coincide.
  grounding: `.shell-team/reviews/T-1066.md` round-2 Major #1 (the `07-31→08-01` and `08-03→08-05` counter-examples, and the inconsistent 8.6%-vs-9% treatment); live re-derivation of all 18 corpus-wide and 4 window-restricted transitions under the stated threshold (`## Rigor vs raw latency`'s own quoted `awk` sweep), confirming exactly 2 corpus-wide Shape-B hits, 1 corpus-wide multi-day Shape-A hit and 0 window-restricted hits of either shape.
- decision: **[rework, round 2]** added a second, window-restricted by-date series (the 10 telemetry-bearing window tasks' own `run_id`s only, 5 dates) alongside the existing corpus-wide series (all `run_id`s, 19 dates), rather than either recomputing the corpus-wide series in place or leaving it as the sole population and only adding a caveat sentence.
  reason: Codex round-2 review measured that the corpus-wide series (which includes many non-window tasks such as `t1016`/`t1022`/`t1024`/`t1032`) was being narratively compared against the 13-task window's own rigor metrics (`criteria-count`, `freeze-versions`) as if the two shared one population. Recomputing the corpus-wide series in place would have discarded a legitimate, separately-useful corpus-wide baseline reading; a caveat sentence alone would not have let a reader verify, by the numbers actually printed, whether the two populations in fact diverge anywhere. Computing both and stating each one's own population explicitly lets the note show, rather than merely assert, that the two coincide exactly for the 5 dates they overlap (verified: `diff` between the corpus-wide series' last 5 rows and the full window-restricted series is empty) and diverge only over the 14 earlier dates that predate the window.
  grounding: `.shell-team/reviews/T-1066.md` round-2 Major #3 (the population-mismatch finding); `.shell-team/specs/T-1066-effort-time-telemetry.md`'s own Notes for engineer on population labelling; live re-derivation of the window-restricted series from the 10 window `run_id`s and a byte-for-byte `diff` against the corpus-wide series' tail, confirming the stated overlap.
- decision: **[rework, round 3]** defined three named, closed population labels (`P13-window`, `P10-telemetry`, `Pcorpus`) once in `## Population and exclusions`, each with its own derivation command or explicit declaration, and re-labelled every metric result line and every comparison sentence in the note to use them — rather than continuing round 2's approach of describing each series' population in its own local prose each time it was mentioned.
  reason: Codex round-3 review measured that round 2's per-site prose descriptions, despite being individually well-intentioned, produced a genuine mislabel at the one site that mattered most (`duration-per-1k-tokens`'s window-restricted series described as "the same population `criteria-count`/`freeze-versions`/`runtime-duty-items` cover," which is false — those three cover `P13-window`'s 13 tasks, three more than the 10-task series actually covers), and that mislabel propagated into the section's own closing conclusion. Local, re-worded-each-time population descriptions have no mechanism forcing internal consistency across a dozen sites; a closed, named label set defined once and then just *cited* (not re-described) at each site removes the surface area for a new site-specific mislabel to recur, which is what a "generator-closing" repair (rather than a two-spot patch) requires.
  grounding: `.shell-team/reviews/T-1066.md` round-3 Major #1 (the population mislabel and its propagation into the conclusion); the orchestrator's round-3 disposition instructing "mechanize the population vocabulary... define a closed label set ONCE"; live re-derivation of `P10-telemetry` (`10`, `T-1056`–`T-1065`) from `P13-window`'s own declared list, and a full re-read of every metric result line and every `## Rigor vs raw latency` sentence confirming each now carries the correct one of the three labels.
- decision: **[rework, round 3]** replaced the single-day-transition sweep (round 2) with one mechanized sweep over **every contiguous span** of every length (`C(N,2)` spans for an `N`-date series — 171 for the 19-date `Pcorpus` series, 10 for the 5-date `P10-telemetry` series), using a span's internal max/min spread (over every date inside it, not only its endpoints) for the "flat" classification and its endpoint-to-endpoint change for the "rising"/"falling" classification — rather than keeping the single-day sweep and hand-adding further multi-day examples as they are found.
  reason: Codex round-3 review measured that round 2's own exhaustiveness claim ("mechanically swept... rather than read by eye") was itself false in the same way round 2 was created to fix — the cited `awk` command's actual domain was adjacent single-day transitions only, with the one 3-day span (`08-03→08-05`) added to the prose by hand rather than by the same mechanism, and a second genuine multi-day instance (`07-31→08-02`, tokens +378.9%, ratio spread 5.3%) existed in the un-swept remainder of the domain. Any bound on span length chosen *after* seeing which spans happen to produce a hit would reintroduce the identical defect in a new shape (a domain picked to match a known answer, not stated in advance); the only closure that survives a next round is enumerating literally everything a "span" can mean in this series (every `(i,j)` with `i<j`) and reporting the total count as the positive control that the domain is what it claims to be (`171 = 19×18/2`, `10 = 5×4/2`, both confirmed).
  grounding: `.shell-team/reviews/T-1066.md` round-3 Major #2 (the false-exhaustiveness finding and the `07-31→08-02` counter-example, arithmetic independently reproduced); live re-run of the all-spans `awk` sweep over both populations, reproducing `TOTAL spans=171 shapeA_hits=1 shapeB_hits=3` for `Pcorpus` and `TOTAL spans=10 shapeA_hits=0 shapeB_hits=0` for `P10-telemetry`; a targeted re-run of the same sweep restricted to 2-date spans only, confirming it reproduces round 2's own single-day table exactly (2 `Shape B`, 0 `Shape A`) before extending the domain, so the new method is a strict generalization rather than a second, silently-inconsistent measurement definition.
<!-- END provenance: T-1066 -->
