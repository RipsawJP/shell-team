<!-- BEGIN provenance: T-1013 -->

- decision: `docs/workflow.md` / `.ja.md` gain the replay-page material as a continuation of the existing `**Per-cycle artifacts**` paragraph — new sentences appended directly after "...marker contract existed." (English) / "...そのファイルの移行注記を参照してください。" (Japanese), on new lines with no blank line separating them from the paragraph's existing lines — rather than as a new, separate paragraph (blank-line-delimited) placed after it, and rather than as a new `### ` subsection under the `## Language — mirror the conversation` heading.
  reason: the spec's D5 states the requirement as "place the generated page **in** the existing per-cycle-artifact paragraph" and rejects a new `## ` section on the grounds that "the language boundary already has exactly one home in that file. A second home is where the two copies start to disagree" — the same reasoning extends to a second, adjacent *paragraph*: a blank-line-separated paragraph reads as a second, distinct claim about language handling rather than as the existing claim extended to a second artifact class, which is what D5 asks for. Appending without a blank line keeps it one markdown paragraph block (same rendered unit) while satisfying D6's "extended … in place, not reflowed" — the pre-existing lines are untouched, so `git diff --numstat`'s deletion count against `5cc9049` stayed at 1 for each file, inside AC10's cap of 5 for `docs/workflow.md`/`.ja.md` (a whole-paragraph rewrite, by contrast, would have touched most of the paragraph's original 8 lines and risked exceeding that cap for no functional reason).
  grounding: .shell-team/specs/T-1013-loop-replay-docs-wiring.md, D5 ("`workflow.md` / `.ja.md` place the generated page in the existing per-cycle-artifact paragraph, and draw the two-sided line explicitly." + the D2-parallel rejection "Rejected — a new `## ` section in `workflow.md`."); docs/workflow.md, the `**Per-cycle artifacts**` paragraph (the sentence beginning "The run-replay page `bin/gen-loop-replay.sh` renders on demand joins the same class"); `git diff --numstat 5cc9049 -- docs/workflow.md docs/workflow.ja.md` showing 1 deletion in each file.

- decision: (rework round 1) both README sections' two runnable command examples (the generic usage line and the `flag-rail` demo line) are rewritten from the bare `gen-loop-replay.sh …` form to the `bash bin/gen-loop-replay.sh …` form in BOTH `README.md` and `README.ja.md`, and the parenthetical claim that "the bare form above and the `bin/`-prefixed form both work" is replaced with a claim that the `bash bin/…` form works regardless of `PATH` or the executable bit — rather than waiting for the in-flight mode-bit fix (`fix/86-bin-exec-bits`, issue #86) to merge first and leaving the README wording as-is.
  reason: `bin/gen-loop-replay.sh` is tracked `100644` (non-executable) at `develop`, at this task's own base ref `5cc9049`, and at this round's HEAD — a pre-existing defect from T-1012/T-1014, not a T-1013 regression, and out of this task's scope to fix directly (AC11's byte-identity lock on `bin/` forbids it). QA, Codex's primary pass, and Codex's own devil's-advocate pass all independently reproduced the same `exit 126` on a clean `git archive` extraction and converged on the same disposition: making the README's own prose unconditionally correct, regardless of whether or in what order issue #86 merges, is cheaper and more robust than depending on merge-order timing this task cannot control (a read-only reviewer/engineer role has no way to guarantee #86 lands first). The `bash bin/…` form is also the genuinely better documented form on its own merits — it is mode-bit- and `PATH`-independent, and it is the exact form every step of `.github/workflows/check-handoff.yml` and AC8's own `check:` line already use, so the README now teaches the same invocation shape CI relies on rather than a shape CI never actually exercises (which is *why* no mechanical check caught the original defect in round 0). Verified the frozen anchors survive the rewording: `gen-loop-replay.sh <run-id>` is a literal substring of `bash bin/gen-loop-replay.sh <run-id>`, so AC4/AC5's anchor and AC6's projection are both unaffected — confirmed directly, `check-acs.sh` reports 13/13 after the edit (up from 12/13 before this round, since AC12 also went green once QA's reviews file existed).
  grounding: .shell-team/reviews/T-1013.md, §4(3) "The demo command, run verbatim" (QA's exec-bit finding, root-cause and wider-pattern measurement) and the `### Codex Review verdict` section's Blockers entry (the ratified resolution: rewrite both commands to `bash bin/gen-loop-replay.sh …`, remove/soften the "both work" assertion, confirmed anchor-safe) plus its Adversarial findings item (1) (the devil's-advocate conclusion adopted here); README.md / README.ja.md, the `## Replaying a run` / `## run のリプレイ` sections' two ` ```bash ` fences (both now `bash bin/gen-loop-replay.sh …`); `bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md` showing 13 passed / 0 failed / 2 skipped after the fix.

<!-- END provenance: T-1013 -->
