<!-- BEGIN provenance: T-1129 -->

- decision: worded the new closing-summary line as `If %s/ does not appear in git status afterwards, a repo-level or global ignore rule may be hiding it — see "Where the operating files live" in docs/adopting.md for the one-line re-include.` (conditional "may be hiding it", double-quoted section title, no backticks), rather than the spec's own example sentence verbatim or a backtick-quoted section title requiring a new `# shellcheck disable=SC2016` line.
  reason: multiple wordings satisfy AC1's four chained greps (the anchor phrase, `docs/adopting.md`, `Where the operating files live`, `$TEAM_RUN_BASE`) and the Non-goals' "no ignore-state claim" bar; "may be hiding it" keeps the sentence conditional rather than asserting the base dir *is* ignored, and dropping backticks around the section title avoids adding a second `SC2016` disable line for a sentence that has no literal backtick command in it (the Notes for engineer precedent at `bin/team-init.sh`:390 is for genuine backtick-quoted shell syntax, not markdown emphasis).
  grounding: `.shell-team/specs/T-1129-team-init-ignored-base-warning.md` `## Notes for engineer` ("Say nothing about ignore state. A conditional ... is fine; an assertion ... is not") and ("Prefer no backticks in the new `printf`."); AC1's check line (four `grep -qF` assertions against the single anchor line).

- decision: placed the new fixture arm (`T-1129 pointer-line`) at the very end of `tests/team-init/run.sh`, immediately before the closing `printf '\nAll team-init assertions passed.\n'` line, rather than adjacent to the T-1097 git-free arm it is thematically closest to.
  reason: the Non-goals forbid touching `tests/team-init/run.sh`:581-601 verbatim, and AC2 locks those three assertion fragments as still present; inserting immediately after that block (at the old :602) was a viable alternative, but appending at the file's tail is more visibly additive-only in the diff and carries no risk of an off-by-one line-range collision with the locked block during rework. Either placement satisfies every AC2 clause (new `PASS` token present, seven pre-existing tokens present, closing line present, `T-1129 pointer-line` on a line with no `GIT_TMP`).
  grounding: `.shell-team/specs/T-1129-team-init-ignored-base-warning.md` `## Non-goals` ("No change to the T-1097 git-free arm's assertions... The new arm needs no repository and belongs under the plain $TMP root"); `## Notes for engineer` ("Do not touch tests/team-init/run.sh:581–601... Adding an arm elsewhere in the file is what is wanted."); AC2's check line.

- decision: added the English and Japanese doc sentences as an appended clause/sentence onto the existing paragraph that already ends "...even for an operator who ignores `.shell-team/` globally" (English) and its mirror, rather than as a new standalone paragraph inside `## Where the operating files live`.
  reason: both placements satisfy AC4 (same section, ≥10 non-empty lines, anchor phrase, literal `team-init`); appending to the existing re-include paragraph keeps the new sentence next to the exact instructions it refers back to ("the one-line re-include"), rather than separating the reminder from the mechanism it points at by a paragraph break.
  grounding: `.shell-team/specs/T-1129-team-init-ignored-base-warning.md` **AC4**'s check line (section extraction plus literal presence, no placement-within-section requirement beyond the section boundary); `docs/adopting.md`'s `## Where the operating files live` section, the re-include paragraph.

<!-- END provenance: T-1129 -->
