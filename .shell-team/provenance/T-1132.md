<!-- BEGIN provenance: T-1132 -->

- decision: appended all new prose purely as a suffix at the very end of each skill file's T-068 bullet (after its existing final sentence), rather than weaving the new `deferred: <reason>` value inline into the existing "one of this closed set: ..." enumeration sentence.
  reason: AC1/AC2's check requires every non-empty line of the base blob to survive at HEAD as a fixed-string substring, verified by `grep -Fq -- "$ln" "$R"` over each base line. Inserting new text in the middle of the existing single physical line would split the original line's bytes around the insertion, so the original full line would no longer occur anywhere as one contiguous substring — only a pure append (new text strictly after the last existing byte) guarantees the old line is a literal prefix of the new one and therefore still matches as a substring.
  grounding: `.shell-team/specs/T-1132-deferred-disposition.md` AC1's check line (`grep . "$T/rb" > "$T/rbl"; ... grep -Fq -- "$ln" "$R"`, run against every non-empty base-blob line, not only the bounded region); AC1 prose ("The edit is additive at the sentence level: every non-empty line of the base blob still occurs at HEAD as a fixed-string substring, so an existing line may be extended but none may be reworded, weakened or removed").

- decision: placed the two new `bin/close-out.sh` comment blocks as (a) a new paragraph appended after the existing gate-order header list (~line 49-57) and (b) a new paragraph inserted inside the T-068 gate's own comment block, immediately before the `if sed -n ...` executable line (~line 244-253), rather than editing any of the existing comment sentences in place.
  reason: AC4 requires every non-comment line of `bin/close-out.sh` byte-identical to its base blob via `cmp -s` over the two `#`-filtered streams, and separately requires every line containing the literal `deferred:` to be a whole-line comment. Adding wholly new `#`-prefixed lines rather than editing existing comment text keeps the diff unambiguously additive and keeps every `deferred:` occurrence on its own comment line, with zero risk of the literal landing on or beside an executable line.
  grounding: `.shell-team/specs/T-1132-deferred-disposition.md` AC4's check line (`cmp -s "$T/cbc" "$T/chc"`, plus `grep -F -- 'deferred:' "$C" | grep -cvE '^[[:space:]]*#'` asserted zero); `## Non-goals` ("No new gate class ... The rejection message's bytes do not change").

- decision: reused the existing `inject_pending_task` helper unchanged (which hardcodes task id T-150 and the fixed disposition-sub-bullet prefix) for both new fixtures, injecting each into its own fresh `make_legacy_root` scratch directory, rather than introducing a parameterized task id or new fixture-range ids such as T-151/T-152.
  reason: `inject_pending_task` already accepts an arbitrary disposition body as its only variable input and is explicitly documented in the spec's own Notes for engineer as "a general disposition injector" whose name is historical; each of the two new fixtures runs in its own freshly created, independent scratch root (`PD`, `PDR`), so reusing task id T-150 across roots creates no collision — each root's board has exactly one entry with that id. This matches the frozen spec's own stated fixture shape more closely than inventing new ids, and needs no change to the existing helper or its callers.
  grounding: `.shell-team/specs/T-1132-deferred-disposition.md` `## Notes for engineer` ("The fixture helper already exists. `inject_pending_task` ... appends any disposition body verbatim after the fixed prefix ... so both new fixtures are a `make_legacy_root` + one call + the assertions, in the shape of the two fixtures immediately below it. Its name says `pending` for historical reasons; it is a general disposition injector."); `tests/close-out/run.sh` `inject_pending_task()` definition (parameterizes only `$1 root` and `$2 disposition`, task id T-150 fixed in the awk body).

- decision: worded the surfacing-duty sentences on both skill files using plural/indefinite phrasing ("every `deferred:` finding", "one such finding or several", "any other undispositioned fast-follow") and explicitly avoided the two forbidden literal phrases named by the spec, rather than writing a sentence that only reads correctly for a single finding.
  reason: AC1/AC2 assert the two literal strings `the deferred finding` and `the one deferred` occur zero times anywhere in each file, and the Goal states the surfacing wording must read correctly for N ≥ 2 findings; using plural/indefinite determiners throughout satisfies both the negative literal check and the readability requirement without needing a separate N=1-vs-N≥2 branch in the prose.
  grounding: `.shell-team/specs/T-1132-deferred-disposition.md` AC1's check (`for bad in 'the deferred finding' 'the one deferred'; do test "$(grep -Fc -- "$bad" "$R" || true)" -eq 0 || rc=1; done`); Goal ("This is why the surfacing duty's wording must read correctly for N ≥ 2").

<!-- END provenance: T-1132 -->
