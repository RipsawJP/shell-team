<!-- BEGIN provenance: T-1036 -->

- decision: spliced Q1–Q4 by extracting each frozen block from the spec with `awk`/`sed` into a scratch file, then replacing the exact target line in the shipped file with that scratch file's bytes via a small Python script that only reads and rewrites whole lines by index — never retyping or hand-copying any of the frozen prose, and never running the extraction and the splice as two independently-transcribed steps.
  reason: every one of Q1–Q4 carries apostrophes, em-dashes, backticks, a `{txt,jsonl}` brace pair and (Q1) an ellipsis that no single quoting style survives intact, and the spec's own Notes for engineer warn that a transcription defect "will look like a placement problem rather than a transcription problem". Routing the bytes straight from the frozen fence to the target line, with no manual retyping at any point, removes that failure class structurally rather than relying on careful proofreading. AC1/AC2/AC3/AC4 (byte/occurrence/adjacency) and AC7/AC8 (cross-check against T-1029's/T-1030's re-frozen blocks) all measured PASS on the first attempt, consistent with this method.
  grounding: `.shell-team/specs/T-1036-wording-batch-141-143-144.md`, Notes for engineer ("Extract the frozen blocks; do not retype them, and do not \"fix\" them... A single smart quote, a collapsed double space, or a hyphen where an em-dash belongs fails the criterion"); `bin/check-acs.sh` live run on that spec, AC1/AC2/AC3/AC4/AC7/AC8 all `PASS`.

- decision: committed the four splices as three separate commits (C1 = `skills/run/SKILL.md` + `skills/goal/SKILL.md` for #143; C2 = `agents/qa-verifier.md` alone for #141; C3 = `agents/codex-reviewer.md` alone for #144), each independently revertable, rather than the two-file-group commit shape (one `agents/` commit, one `skills/` commit) sketched in the coordinating session's task prompt.
  reason: the spec's own Notes for engineer pins a numbered "Order of work" and a pre-commitment P1 with exactly this three-way split, stated as binding on every role on this branch, and gives the reason directly: "whichever car is clean can ship alone" — C2 and C3 are mutually independent (a `REQUEST_CHANGES` on one re-freeze's shipped line does not implicate the other), and C1 has zero ledger consequence and is safest to land regardless of either re-freeze's outcome. The spec is the frozen contract; a looser two-group description elsewhere in the coordinating session's own summary of the task does not supersede it, and grouping qa-verifier.md and codex-reviewer.md into one commit would make an independent revert of one re-freeze (P2) touch a commit carrying the other.
  grounding: `.shell-team/specs/T-1036-wording-batch-141-143-144.md`, Notes for engineer ("Order of work — the P1 split order, one commit each, so whichever car is clean can ship alone") and pre-commitment P1 ("C1 = #143 alone... C2 = #141... C3 = #144... C2 and C3 are mutually independent").

- decision: did not attempt to make T-1036's own AC10 measure green, and did not touch `agents/pm-spec.md` or any spec's frozen bytes to try to close the gap; instead measured and disclosed the actual failure cause for escalation.
  reason: `bin/check-acs.sh` on `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` measures AC2 as FAIL (its `F2` block is pinned as the LAST non-empty line of `agents/pm-spec.md`'s `## Spec completion self-check` section, but T-1035's two forward-only bullets — the measured-at-ref and population-count-staleness norms — now sit after it), which is outside AC10's permitted FAIL set of `{AC5, AC7, AC8}` and therefore fails AC10's own `PASS`-set requirement (which pins AC2 as required-PASS). This condition predates this engineer round: it involves no file this task's allow-list lets an engineer touch (`agents/pm-spec.md`, T-1029's F2 block), and the board's own freeze-run reconciliation note (recorded before this engineer round started) already names it as "a newly discovered member of the frozen-positional-pin staleness class... disclosed per R3 (minimal delta — not folded into this v3), routed to the #155 repair class" — i.e. a known condition the freezing round deliberately chose not to fold into T-1036's own AC10. Per this task's hard boundary ("A frozen text defect = stop and report (re-freeze route via pm-spec, P3)") and the engineer's standing rule to route an unclear or unsatisfiable frozen criterion back to pm-spec rather than force it or guess, I report the measured shape rather than attempt an in-place fix that would be out of the engineer's allow-list in either direction (repairing T-1029's `F2`/`agents/pm-spec.md`, or widening T-1036's own frozen AC10).
  grounding: `bin/check-acs.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md` live run (AC2: FAIL); `agents/pm-spec.md`, `## Spec completion self-check` section (items 9–10, the measured-at-ref and population-count-staleness norms, now trailing the Summarized-source declaration check item AC2 pins as last); `.shell-team/todo.md` T-1036's freeze-run reconciliation sub-bullet ("AC2 = a newly discovered member of the frozen-positional-pin staleness class... routed to the #155 repair class").

<!-- END provenance: T-1036 -->
