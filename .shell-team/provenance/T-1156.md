<!-- BEGIN provenance: T-1156 -->

- decision: Add the new decision-rule text as a separate new paragraph in `templates/prompt-blocks/host-dispatch.md`, placed immediately after the existing `:15` paragraph (the one ending "the network-refused `Not logged in` symptom … is the one this path is measured to actually produce"), rather than appending the new sentences onto the end of that existing paragraph.
  reason: The spec's own `## Notes for engineer` permits either shape ("Add the rule either by appending sentences to the end of the `:15` paragraph or as a new paragraph right after it"); a new paragraph keeps the pre-existing "diagnose, cleanup, skip publish, report `BLOCKED`" sentence intact as its own distinct statement and gives the new rule a clean paragraph boundary, which makes the `awk -v RS=` paragraph-scoped AC1 check easier to reason about than growing an already very long existing paragraph further.
  grounding: `.shell-team/specs/T-1156-codex-host-not-logged-in-diagnosis.md` `## Notes for engineer` ("`host-dispatch.md`: … Add the rule either by appending sentences to the end of the `:15` paragraph or as a new paragraph right after it; do not rewrite or reflow any existing line").

- decision: Word the new paragraph so the isolation call's own failure (not the `Not logged in` symptom by itself) is stated as what means a genuine authentication failure, and so the "never advise `/login` on this symptom alone" instruction is a standalone clause rather than an aside folded into the isolation-call sentence.
  reason: AC1's own prose requires that "a genuine authentication failure is never misrouted" and the Body-to-AC correspondence table flags this direction as review-judged rather than machine-anchored (no string literal can assert a conditional's direction); stating the causal chain affirmatively and separately reduces the chance a reviewing role reads a merely-present `/login` token as satisfying the requirement when the surrounding sentence's direction is what actually matters.
  grounding: `.shell-team/specs/T-1156-codex-host-not-logged-in-diagnosis.md` `## Acceptance criteria` AC1 ("a genuine authentication failure is never misrouted") and `## Body-to-AC correspondence` row 4 ("direction is review-judged … no string can assert a conditional's direction").

- decision: (Rework round 1, review finding on `templates/prompt-blocks/host-dispatch.md:17`) Rewrite clause (iii)/(iv) of the `host-dispatch.md` paragraph — and its byte-identical `skills/run/SKILL.md` mirror — so the isolation call's own reported outcome, not merely whether it fails, decides the diagnosis: printing `ok` and exiting `0` confirms the in-sandbox failure was the network refusal; a login prompt or an authentication error from that call means a genuine authentication failure and `/login` is then the fix; any other outcome from that call (a missing `claude` binary, a rate limit, a provider outage, a network problem outside the sandbox) means neither cause and is reported as-is, matching `docs/adopting.md` step 7's own three-way reading of that same call rather than treating "the isolation call failed" as a single undifferentiated signal for authentication.
  reason: The reported finding showed the previous wording ("a failure there … is what means a genuine authentication failure") would misclassify any non-auth failure of the isolation call (a provider outage, a rate limit, a missing binary, a network problem outside the sandbox) as authentication and wrongly advise `/login`. The class of the defect is "the outcome of the isolation call is interpreted without regard to what it reported," not the single reported instance, so the same three-way reading was applied to every sentence this task added that interprets that outcome, not only the one the finding quoted.
  grounding: `docs/adopting.md:536`–`539` (step 7: "It should print `ok` and exit `0`. If it instead reports a login prompt or an authentication error, resolve that first (`claude /login` …)") is the pre-existing auth-specific reading this rework brings the new prose into agreement with; the finding itself is relayed by the coordinating session (`templates/prompt-blocks/host-dispatch.md:17`, Codex primary pass, P2 → Major).

- decision: (Rework round 1) Also add a clarifying clause to the step-8 sentence in `docs/adopting.md` and `docs/adopting.ja.md` — naming the same three-way reading of step 7's isolation call inside the sentence that points to the `code-reviewer` `BLOCKED` report — rather than leaving those two sentences untouched.
  reason: The finding named all four sites (`host-dispatch.md`, its `SKILL.md` mirror, and the step-8 sentence in both adopting pages) as sentences this task added that interpret the isolation call's outcome. Read on their own, the two step-8 sentences only point to the `BLOCKED` report and do not restate the flawed logic themselves, so no correctness defect exists there; the added clause is a belt-and-suspenders closure of the named class across every site the finding enumerated, rather than a claim that those two sentences were independently wrong. AC4's checks (`BLOCKED`/`code-reviewer` presence, heading counts, whole-line survival of every pre-existing line) all still pass with the addition.
  grounding: `docs/adopting.md` step 8's own diff against the branch-point blob (`git diff ac65e6d2 -- docs/adopting.md`), which shows the T-1156 sentence is a single appended clause with no pre-existing content restating the isolation-call decision; the finding's own enumeration of four sites (relayed by the coordinating session).

<!-- END provenance: T-1156 -->
