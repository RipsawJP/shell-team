<!-- BEGIN provenance: T-1044 -->

- decision: derived the two synthetic blob shas for the DP4 non-vacuity controls in `tests/bin-exec-bit/run.sh` live at run time (`git -C "$REPO_ROOT" rev-parse HEAD:tests/bin-exec-bit/run.sh` for a shebang-carrying blob, `git -C "$REPO_ROOT" rev-parse HEAD:.shell-team/test-recipe.md` for a non-shebang blob) rather than hardcoding two literal sha1 hex strings.
  reason: the control needs `git cat-file blob <sha>` to resolve to a real object with a known first-two-bytes shape; a hardcoded sha becomes silently vacuous (or starts failing for the wrong reason) the moment either reference file's content changes, since a literal sha pins a specific blob's *content*, not a *property* of whichever content currently lives at that path. Deriving both shas from `HEAD:<path>` ties the control to the property (this path's first line is/isn't a shebang) rather than to a byte-frozen snapshot nothing else in this task freezes.
  grounding: `tests/bin-exec-bit/run.sh`'s `SHEBANG_SHA`/`NO_SHEBANG_SHA` assignments, immediately above the `SYN_TESTS_SHEBANG_100644`/`SYN_TESTS_NOSHEBANG_100755` synthetic listings they feed (DP4's "control: a shebang-carrying 100644 member is rejected" / "control: an executable member with no shebang is rejected" labels).

- decision: for the 20-file scratch conversion (DP6), adopted the full two-arm `TMPDIR`-then-`$HERE`-fallback `mktemp -d` idiom as the default shape even for sites that had only a single fixed-path arm before conversion (`tests/check-acs/run.sh`, `tests/check-run/run.sh`, `tests/discover-work/run.sh`, `tests/goal-state/run.sh`, `tests/log-run/run.sh`, `tests/loop-guard/run.sh`, `tests/team-init/run.sh`, `tests/team-paths/run.sh`, and all three `tests/check-retro/run.sh` variables), instead of only wrapping each site's pre-existing single arm in `mktemp -d`.
  reason: DP6 requires "every conversion uses the in-repo idiom at `tests/check-refreeze-class/run.sh:82-87` verbatim in shape", and that idiom is a genuine two-branch `if [ -n "${TMPDIR:-}" ]; then … else …; fi`, not a single-arm wrap; applying the same two-branch shape uniformly, rather than shape-matching each site's own prior arm count, keeps every converted suite honoring `$TMPDIR` when a sandboxed environment sets it, consistent with the corpus's existing two-arm sites (`tests/check-playbook/run.sh`, `tests/close-out/run.sh`, etc.) that already did this before T-1044 touched them.
  grounding: `.shell-team/specs/T-1044-test-infra-bundle.md` DP6 ("Every conversion uses the in-repo idiom at `tests/check-refreeze-class/run.sh:82-87` verbatim in shape") and `## Input space` ("`$TMPDIR` set to a writable directory, and `$TMPDIR` unset — the fallback arm must survive both"); `tests/check-refreeze-class/run.sh`'s own two-arm block, the idiom cited by name.

- decision: kept `tests/check-board-headings/run.sh` and `tests/codex-skeleton-hygiene/run.sh` on a single `${TMPDIR:-/tmp}` arm (wrapped in `mktemp -d … .XXXXXX`, dropping their prior `.$$`/`-$$` suffix) with no `$HERE` fallback arm, as the one deliberate exception to the two-arm default above.
  reason: both suites build throwaway `git init` repositories under their own scratch root, and both already carried a pre-existing comment stating those repos must never live inside this checkout's own tree ("no nested `.git` under the repo tree" / "sandboxed runs deny writes to a nested `.git/` inside the repo tree"); adding a `$HERE`-fallback arm — the standard idiom's second branch — would, on a host with `$TMPDIR` unset, relocate that scratch root into the checkout and reproduce exactly the hazard the pre-existing comment documents avoiding.
  grounding: `tests/check-board-headings/run.sh`'s header comment ("Temp git roots live under $TMPDIR … deny writes to a nested .git/ inside the repo tree") and `tests/codex-skeleton-hygiene/run.sh`'s header comment ("no nested .git under the repo tree … the throwaway git repos below live under $TMP, never inside this checkout"), both unchanged by this task; the spec's DP6 idiom description, which this decision departs from for these two files only.

- decision: kept `tests/rework-digest/run.sh` on a single `$HERE` arm (no `$TMPDIR` arm), and moved its `TMP` assignment from its original, unconditional, early position (before the parent/child golden-diff-comparison guard) down into the child-only branch where `$TMP` is actually first used.
  reason: this suite's relative-symlink launch-path case (`$linkdir/rework-digest-rel.sh` -> `../../../../bin/rework-digest.sh`) depends on `$TMP` sitting at a fixed depth under the repo root (`tests/rework-digest/<TMP>/linkdir`, four hops to `REPO_ROOT`); a `$TMPDIR` arm would relocate the root out of tree on a host with `$TMPDIR` set and break that fixed hop count, discovered by actually running the converted suite and watching this exact case fail with "No such file or directory". Separately, the original `TMP="$HERE/tmp"` assignment ran unconditionally in BOTH the parent and the child process (the parent never used it, exiting earlier at line 54), so converting it to `mktemp -d` in place — which creates a directory as a side effect of assignment, not lazily — would have made every parent-process invocation also create a scratch directory it never uses and never cleans up (its `trap` for `$TMP` is registered later, in the child-only branch). Moving the assignment to where it is first used removes both problems at once.
  grounding: `tests/rework-digest/run.sh` lines matching `linkdir is 4 levels below the repo root` and the `REWORK_DIGEST_RUN_CHILD` guard block preceding it; this role's own run of `bash tests/rework-digest/run.sh` after the first conversion attempt, which failed with `.../linkdir/rework-digest-rel.sh: No such file or directory` under this session's `$TMPDIR`, and passed once the fix above was applied.

- decision: left every site's pre-existing header/inline rationale comment byte-unchanged during the scratch conversion, including comments that now describe a shape the file no longer has (for example `tests/check-acs/run.sh`'s "Writes under $HERE/tmp (no mktemp) so it runs in restricted sandboxes." and `tests/loop-guard/run.sh`'s "Avoids mktemp so it runs in restricted sandboxes"), rather than editing any of them to describe the post-conversion mktemp shape.
  reason: DP6 explicitly instructs "each site's own header rationale comment is preserved", and DP8 states a converted suite "may still describe the shape it used to have" because the scratch-root-hygiene lock's own rule deliberately excludes full-line comments from its sweep — the spec treats a stale descriptive comment as an accepted, foreseen byproduct of the conversion rather than a defect to correct in the same task.
  grounding: `.shell-team/specs/T-1044-test-infra-bundle.md` `## Notes for engineer` ("each site's own header rationale comment is preserved") and DP8 ("Full-line comments are excluded, so a converted suite may still describe the shape it used to have").

- decision: built the two "rejected" non-vacuity control lines inside `tests/scratch-root-hygiene/run.sh` (`SYN_HERE_LINE`, `SYN_TMPDIR_LINE`) at run time from separated pieces — a literal `$` supplied as its own `printf` argument, never concatenated with `HERE`/`{TMPDIR` in the same quoted literal — instead of writing each as one contiguous single-quoted string the way `tests/bin-exec-bit/run.sh`'s own synthetic-listing controls are written.
  reason: `tests/scratch-root-hygiene/run.sh` is itself a tracked file under `tests/`, so its own source is part of the population its main rule sweeps; a literal, contiguous occurrence of `"$HERE/tmp` or `="${TMPDIR…}/<literal>` written directly into this file's source (not inside a full-line comment, not containing `mktemp`) would make the suite's own population sweep flag itself as a violation of the very rule it is proving, on every run including the one this task's own AC8/AC9 checks exercise. Verified empirically: the frozen `FSR_HERE`/`FSR_TMPDIR` variable *definitions* happen not to self-match (their own literal text contains an escaping backslash the regex's `\$` doesn't expect against a raw source line), but a hand-written positive-example control line has no such accidental protection and does self-match unless deliberately split.
  grounding: `tests/scratch-root-hygiene/run.sh`'s comment directly above the controls section ("The two 'rejected' synthetic lines are assembled at run time from separated pieces … so this file's OWN source never trips the rule it is proving — self-hosting"); this role's own interactive test confirming a directly-written `SYN_HERE='TMP="$HERE/tmp-fixed-example"'` line self-matches the frozen `FSR_HERE` pattern while the split `printf`-built construction does not, run before this file was committed.

- decision: simplified `tests/install/run.sh`'s `EXIT` trap to `rm -rf "$WORK" 2>/dev/null || true` (dropping the `"$SCRATCH_ROOT"/install-tmp-*` glob entirely) rather than narrowing the glob to something like `"$WORK"/install-tmp-*`.
  reason: once `mktmp_file()` creates every temp file under `$WORK` (itself an `mktemp -d`-created, already-unique directory removed wholesale by the trap), a second glob targeting the same tree is redundant — `rm -rf "$WORK"` alone already removes everything `mktmp_file()` ever created, and keeping a narrower but still-present glob clause would be dead code inviting a future edit to widen it back toward the shared parent by analogy.
  grounding: `.shell-team/specs/T-1044-test-infra-bundle.md` DP7 ("The fix is confined to that: temp files move inside the already-`mktemp`'d `$WORK`, and the glob leaves the trap"); `tests/install/run.sh`'s `mktmp_file()` definition and the `trap` line immediately below it.

- decision: landed the eleven mode normalizations (DP3) via an on-disk `chmod +x` on each file followed by `git add`, not via `git update-index --chmod=+x` alone.
  reason: this checkout has `core.fileMode=true`; running `git update-index --chmod=+x <file>` first and then a plain `git add <file>` (needed anyway once the suite's source content also changed) caused `git add` to re-read the working-tree file's real (still non-executable) permission bit and silently reset the index mode back to `100644`, discovered by re-running `git ls-files -s` after the first attempt and finding the eleven files still at `100644`. `chmod +x` on disk first, then `git add`, makes the working tree and the index agree from the start so no later `git add` call can regress it.
  grounding: this role's own `git ls-files -s -- tests/check-commit-identity/run.sh` re-check after the first `git update-index --chmod=+x` + `git add` attempt, which showed the file still reported at mode `100644`; `.shell-team/specs/T-1044-test-infra-bundle.md` `## Notes for engineer` ("use `git update-index --chmod=+x` if in doubt, and confirm with `git ls-files -s` rather than `ls -l`").

- decision: left `tests/check-retro/run.sh`'s pre-existing pattern of three separate `trap '… ' EXIT` registrations (one each for `MUT_TMP`, `COPY_TMP`, `TMP`) exactly as it already behaved — each new registration still overwrites the previous one, so only the last-registered directory (`TMP`) is actually removed by the `EXIT` trap that fires — rather than fixing it to accumulate all three cleanups while converting the three scratch-root assignments to `mktemp -d`.
  reason: this is a pre-existing property of the file, unrelated to the exec-bit or scratch-root-hygiene defect classes this task's Goal names, and outside DP6's scope (which converts the fixed-path *assignment* shape, not `trap` composition); fixing it would be undocumented scope creep the spec's Non-goals ("Goal-external discoveries … filed as an issue … and not acted on") route elsewhere. Neither of this task's two locks depends on this suite's own cleanup completeness — `tests/scratch-root-hygiene/run.sh`'s rule is a source-shape lint, not a runtime leak check.
  grounding: `tests/check-retro/run.sh`'s three `trap 'rm -rf "$…_TMP"' EXIT` / `trap 'rm -rf "$TMP"' EXIT` lines (unchanged in shape by this task, each still only wrapping its own scratch-root variable in `mktemp -d`); `.shell-team/specs/T-1044-test-infra-bundle.md` Non-goals ("Goal-external discoveries. Anything surfaced here that is not #160, #153 or the retro's lesson 6 is filed as an issue for the next planning input and not acted on").

<!-- END provenance: T-1044 -->
