<!-- BEGIN provenance: T-1113 -->
- decision: leave the spec's own **AC16** `- flips: <n>` line unwritten and the full base/HEAD two-arm sweep unrun in this engineer round — only the read-set population (via `bin/derive-populations.sh --label t1113-read-set`) is derived and embedded in `## Blast radius`.
  reason: at least two options existed once the derivation resolved to corpus-scale (107 of this corpus's 118 specs, via a `--set` built from mentions of `.github/workflows/check-handoff.yml` and `templates/prompt-blocks/derived-populations.md` / `bin/check-prompt-sync.sh` — both referenced by nearly every task's own criteria): (a) run the full two-arm sweep across all 107 specs inside this engineer round anyway, or (b) derive and disclose the population, defer the sweep, and stop. Option (a) directly contradicts this task's own explicit launch instruction ("do NOT run the full sweep: report the derived population and STOP, the coordinating session hosts it") and reproduces the exact hazard `.shell-team/test-recipe.md`'s T-1111/T-1112 entries already name and resolve the same way for the same reason (a generic-reachability `--set` built from a near-universally-referenced file). This is non-trivial because it leaves one of the spec's own frozen, scriptable acceptance criteria (AC16) knowingly red at hand-off — an observable, contract-level outcome disclosed in the board hand-off and in `.shell-team/test-recipe.md` rather than hidden or fabricated with an unverified `- flips: 0`.
  grounding: this task's own launch instruction (verbatim quoted above); `.shell-team/test-recipe.md`'s T-1111 entry ("a STOP-and-report condition, not an execution procedure to push through") and T-1112 entry (the same disposition applied to its own corpus-scale read-set); live reproduction — `bash bin/derive-populations.sh --label t1113-read-set --set "close-out=grep -lF -- 'bin/close-out.sh' .shell-team/specs/*.md" --set "derived-pop=grep -lE -- 'derived-populations\.md|check-prompt-sync\.sh' .shell-team/specs/*.md" --set "workflow=grep -lF -- 'check-handoff.yml' .shell-team/specs/*.md" --accept-status close-out=1 --accept-status derived-pop=1 --accept-status workflow=1` prints `- union: items: 107`; `CHECK_ACS_TIMEOUT=900 check-acs.sh .shell-team/specs/T-1113-quantity-relay-checker.md` reports **AC16** `FAIL` as a direct, disclosed result.

- decision: wire the new `- count:` claim grammar gate into `bin/close-out.sh` between the oversight-profile gate and the review-input-fidelity gate, rather than at the head of the gate chain or immediately before the source-line/hand-off-lint gate.
  reason: at least three placements were available in the existing gate chain (fast-follow → dispatch record → interventions → spec-review → oversight → **[new gate]** → review-input → source-line → pre-flip). The spec's own DP-3 resolves *which structural shape* to copy (the oversight gate's unconditional wiring, not the spec-review gate's election-scoped wiring) but does not fix an exact position in the chain. Placing it directly after the oversight gate keeps every validate-if-present, unconditionally-wired gate (oversight, count-claims, review-input) grouped together and after the two gates whose refusal is more foundational to the record's own identity (interventions, spec-review), so a task failing an earlier, more fundamental gate sees that refusal first rather than a `- count:` grammar complaint about a task that also has no interventions record. This is non-trivial because gate ORDER is observable: which refusal message an operator sees first when a board entry carries more than one simultaneous defect depends on it, and no criterion in the frozen spec pins the exact position.
  grounding: `.shell-team/specs/T-1113-quantity-relay-checker.md` **DP-3** ("wiring mirrors the oversight gate ... for the reason the oversight gate states") and `## Notes for engineer` Trap 12 (the sibling gates' own shared structure); live reproduction — `bin/close-out.sh`'s own gate order, readable directly in the file between the `T-1103` oversight-profile gate comment and the `T-1104` review-input-fidelity gate comment; `bash tests/close-out/run.sh` (130 `PASS:` lines, `grep -c '^PASS:'`) exercises every pre-existing gate's fixtures unmodified against this ordering and all still pass.

- decision: implement `canon_num()`'s decimal canonicalization (sign-strip, then strip leading zeros one character at a time via `${v:0:1}`/`${v:1}` substring expansion) rather than the arithmetic `$((10#$v))` idiom `## Notes for engineer` Trap 3 names as one option.
  reason: at least two implementations satisfy the spec's own behavioural gate (AC2's `octal-trap`/`negative-zero` rows, AC6's numeric-comparison requirement): (a) `$((10#$v))` bash arithmetic with a base-10 prefix to defeat octal interpretation, or (b) pure string manipulation with no arithmetic evaluation at all. Option (a) is the idiom the spec's own Trap 3 mentions as the trap's remedy, but it silently caps correctness at bash's signed 64-bit integer range and would misbehave (or error) on a value string longer than that, which the grammar's `^[+-]?[0-9]+$` admits without a length bound. Option (b) has no such ceiling: it costs nothing extra to implement, so it was chosen even though the spec did not require handling an arbitrarily long digit run — this is non-trivial because it is an unrequested robustness margin, not a status-quo default, and it changes what the comparison does for an input class no acceptance criterion exercises.
  grounding: `.shell-team/specs/T-1113-quantity-relay-checker.md` `## Notes for engineer` Trap 3 ("use a sign-strip plus `10#`, or `awk` arithmetic"); `bin/check-count-claims.sh`'s own `canon_num()` function, readable directly in the file; live reproduction — `bash tests/check-count-claims/run.sh` (11 `PASS:` lines, `grep -c '^PASS:'`) exercises the `octal-trap`/`negative-zero`/`negative-case` rows this function's own behaviour is gated on, all passing.

- decision: emit the per-row live-execution echo (`check-count-claims: count <label> — declared <value> — command: <cmd>`) on stdout rather than stderr.
  reason: at least two destinations were available for a message this checker's own header contract does not pin to a stream (the spec's AC9 fixtures capture combined stdout+stderr throughout, so either choice satisfies every scriptable criterion). `bin/check-acs.sh`'s own equivalent per-check echo (`AC%s: running: %s`) is the closest sibling in shape and function within this repository and prints to stdout; stderr in this checker is reserved for refusal messages (`die`/`fail`) and the advisory uncommitted-board warning, so keeping the echo on stdout preserves a clean separation an operator or a future automation layer could rely on (stderr = something is wrong or worth a heads-up; stdout = normal progress) without any acceptance criterion forcing that separation. This is non-trivial because it is an observable interface choice (which stream a downstream consumer redirects to capture just the echoes) made ahead of any criterion requiring it.
  grounding: `bin/check-acs.sh`'s own `printf 'AC%s: running: %s\n' "$acnum" "$cmd"` line (no redirection — stdout by construction), the sibling this checker's own header cites for its trust-boundary and echo-before-run precedent; `bin/check-count-claims.sh`'s own per-row `printf` call, readable directly in the file, carrying no `>&2` redirection; live reproduction — `bash bin/check-count-claims.sh --board <fixture> --task T-901 2>/dev/null` (stderr discarded) still shows the `count <label>` echo lines on the retained stdout.

- decision: delivered-change review round 1 fix — reword the `| wc -l` remedy message and header comment to drop `-c` from the suggested `grep` idiom and add an explicit `|| true` guard, rather than leaving the pre-existing remedy text unchanged once `set -o pipefail` was added to the child shell.
  reason: at least two candidate remedy texts existed once pipefail was enabled inside the command's own child shell: (a) leave the existing `pipe through | wc -l instead` wording unchanged, or (b) correct it to name the specific working idiom. Option (a) is what the reviewer's own suggested fix literally wrote (`(grep -c zzz file || true) | wc -l`), but live reproduction showed that pattern still double-counts a legitimate zero match as `1` (`bash -c 'set -o pipefail; (grep -c zzz /dev/null || true) | wc -l'` → prints `1`, not `0`), because `grep -c`'s own `"0"` text output is itself one line for `wc -l` to count — the `-c` flag and `wc -l` compose incorrectly regardless of the `|| true` guard. Only dropping `-c` (`(grep zzz file || true) | wc -l`) measures correctly (verified live: prints `0`, exits `0`). This is non-trivial because it corrects the reviewer's own suggested fix text rather than applying it verbatim, and because a wrong remedy message would send an adopter toward a command that silently double-counts.
  grounding: `.shell-team/reviews/T-1113.md`'s delivered-change review round 1 finding 1, suggested fix (`out="$( $TIMEOUT_PREFIX bash -c "set -o pipefail; $cmd" )"`) and its own coordinator-relayed caveat ("the refusal-with-remedy message... now needs `|| true`-style guidance updated"); live reproduction of both the reviewer's literal suggestion (`(grep -c zzz /dev/null || true) | wc -l` → `1`, wrong) and the corrected form (`(grep zzz /dev/null || true) | wc -l` → `0`, right) run directly in this shell; `bin/check-count-claims.sh`'s own remedy message and header comment, readable directly in the file.

- decision: delivered-change review round 1 fix — reject a whitespace-only `command:` field as an additional post-parse content check on the already-captured `BASH_REMATCH[3]` group, rather than tightening the byte-pinned `STRICT_RE` regex itself (e.g. `command: ([^[:space:]].*)$`, the reviewer's own alternative suggestion).
  reason: at least two implementations satisfy the review's own two-line fix instruction ("if the fix can live as an additional post-parse validation without touching the pinned regex, prefer that"): (a) change the `STRICT_RE` pattern's `command:` capture group to require a non-whitespace first character, or (b) keep `STRICT_RE` byte-for-byte unchanged and add a trim-and-refuse check after a successful match. First checked whether any acceptance criterion byte-pins the `(.+)$` capture the way **AC1**/**AC3** byte-pin the collect-wide stem literal (`grep -qF` on the exact stem string) — none does; only the stem and the label/value sub-grammars (`^[A-Za-z0-9][A-Za-z0-9_-]*$`, `^[+-]?[0-9]+$`) are byte-pinned, so changing `STRICT_RE` would not have been a frozen-byte violation either. Option (b) was chosen anyway, per the review's own stated preference and because it is the more conservative diff against a regex three spec-review rounds already scrutinized line by line, leaving the parse boundary itself untouched and adding a narrowly-scoped, separately-named refusal reason instead. This is non-trivial because it decides where a fail-closed content check lives relative to a grammar three rounds of review already approved byte-for-byte.
  grounding: `.shell-team/reviews/T-1113.md`'s delivered-change review round 1 finding 2 and its coordinator-relayed instruction ("check whether the frozen grammar regex... byte-pins `(.+)$`... if the fix can live as an additional post-parse validation... prefer that"); `.shell-team/specs/T-1113-quantity-relay-checker.md` **AC1**'s check line (`grep -qF` over the collect-wide stem and the label/value sub-grammar literals only — no `(.+)$` literal present); `bin/check-count-claims.sh`'s own post-match `row_cmd_trimmed` block, readable directly in the file; live reproduction — the three-trailing-space fixture now refuses under `--no-exec` (`exit 1`, "the `command:` field is empty or whitespace-only"), and `bash tests/check-count-claims/run.sh 2>&1 | grep -c '^PASS:'` → `14`.
<!-- END provenance: T-1113 -->
