<!-- BEGIN provenance: T-1016 -->

- decision: `bin/close-out.sh`'s single required occurrence of the frozen canon marker `board-entry continuation canon (T-1016)` is placed inside the file's own `--help` header block (within the `sed -n '2,40p'` range), adjacent to the "moves the task's top-level line plus its entry extent" prose, rather than as a comment directly beside the pass-1 awk predicate (~line 145) where the other three sites carry theirs.
  reason: AC1 only requires the literal to appear exactly once anywhere in the file, so either placement satisfies it mechanically; the spec's own Body-to-AC correspondence table states the alternative directly and singles this file out — "the header comment's wording is re-grounded... the `--help` range is a byte-count property no grep can assert meaningfully" — i.e. the marker "must sit in that same header" specifically for this site, unlike the other three where D1's general rationale ("a comment adjacent to its predicate, so the next reader finds the canon instead of re-deriving one") governs. Following the site-specific override rather than the general rule avoids a second marker occurrence in this file (AC1 requires exactly one), which an adjacent-to-predicate placement would have produced on top of the header placement.
  grounding: .shell-team/specs/T-1016-close-out-entry-boundary.md, "## Body-to-AC correspondence" table row "`close-out`'s header wording is re-grounded and stays inside the `--help` range | D5 | `info-only (not promoted to AC)`..."; bin/close-out.sh, header lines 5-16 (the entry-extent description carrying the marker) versus the pass-1 awk comment at the "board-entry continuation canon (T-1016)" absence there.

- decision: `bin/check-handoff.sh`'s strand check recognizes an entry-opening "task line" via a broader `TASK_LINE_RE='^- \[[x ]\] '` (checkbox shape only), separate from and more permissive than the full hand-off-grammar `LINE_RE` a line must satisfy to lint clean — so a malformed `- [ ]` line that fails `LINE_RE` still opens an entry (its own sub-bullets are never re-reported as strands), while the format-mismatch violation still fires exactly once for the malformed line itself.
  reason: D4 requires "one defect, one message" — a malformed top-level line must not additionally cascade a strand violation for each of its own sub-bullets. The two mechanically available alternatives were (a) use the strict `LINE_RE` itself to decide whether a line opens an entry, which would make every malformed top-level line simultaneously a format-mismatch AND fail to open an entry, cascading a strand violation onto every one of its sub-bullets (violates D4 directly, and the existing `tests/check-handoff/fixtures/bad-format.md` / `whitespace-title.md` regression fixtures would still pass only because neither happens to carry a sub-bullet — a coincidence, not a proof); or (b) the checkbox-shape-only `TASK_LINE_RE` this change actually uses, which matches the Terms table's own "task line" definition (`^- \[[x ]\] `, the exact shape D4 names as "task line", distinct from the shape `close-out.sh`'s pass-1 and `check-board-headings.sh`'s extractor already use for the same purpose) and opens the entry regardless of whether the rest of the line satisfies the hand-off grammar.
  grounding: .shell-team/specs/T-1016-close-out-entry-boundary.md, "### Terms" table row "task line | a boundary line matching `^- \[[x ]\] `"; D4, "A malformed top-level line still opens an entry. A line beginning `- [ ]` that fails `LINE_RE` is reported once, as a format mismatch, and does **not** additionally produce a strand violation for each of its sub-bullets. One defect, one message."; bin/check-handoff.sh, the `TASK_LINE_RE` definition and its use ahead of the `LINE_RE`/`FLAG_RE` validation block.

- decision: in `tests/close-out/run.sh`'s four new `move-*` assertions, every `awk ... | grep -c '[^[:space:]]'` count used to prove "`## Active` is left with zero non-blank lines" is written as `... | grep -c '[^[:space:]]' || true`, rather than the bare form the frozen spec's own AC7/AC9 `check:` lines use (`a=$(awk '...' | grep -c '[^[:space:]]'); test "$a" = "0" ...`).
  reason: this test file runs under `set -euo pipefail` (unlike a `check:` line, which `check-acs.sh` runs as a standalone command, not inside this file's own errexit context). When the Active section legitimately becomes empty after a successful move, `grep -c` prints `0` but still exits 1 (no matching line) under GNU/BSD grep semantics, and `pipefail` propagates that 1 as the pipeline's exit status; a plain top-level `x="$(pipeline)"` assignment under `set -e` then aborts the whole script before the `[ "$x" -eq 0 ]` assertion is ever reached — the exact `test-recipe.md` T-112 class of hazard ("a plain top-level assignment already aborts the script via errexit on failure"), here triggered by a *correct* zero-count result rather than a failure. Observed directly: the suite silently died with no `FAIL:` line and a bare exit 1 until this guard was added (confirmed by reproducing the abort under `bash -x` and tracing it to the exact `m1_a_count=` assignment). Appending `|| true` neutralizes only the count computation's own exit status, not the count value itself, and every count is still asserted with an explicit `[ "$x" -eq 0 ] || fail ...` immediately after — the `test-recipe.md` T-1001 discipline of asserting rather than silently accepting an empty read stays intact.
  grounding: .shell-team/test-recipe.md, T-112 entries ("a plain top-level `x=\"$(mktemp ...)\"` assignment... already aborts the script via errexit on failure" and "Under `pipefail`, piping into `grep -q` can false-fail on SIGPIPE — verify through a temp file instead", the sibling class this is); tests/close-out/run.sh, the four `m{1,2,3,4}_a_count="$(... | grep -c '[^[:space:]]' || true)"` assignments and their immediately-following `[ "$m*_a_count" -eq 0 ] || fail ...` assertions; self-run reproduction (removing `|| true` from any one of the four reproduces the silent abort; restoring it and re-running the suite reaches every `PASS:`/`FAIL:` line as expected).

- decision: AC10's frozen `check:` line is left byte-unchanged, is reported as mechanically broken in this hand-off, and is NOT silently corrected — instead, this task's own `tests/check-intent/run.sh` gained two independent, equivalent assertions (`blank-line-inside-entry-keeps-scope`, `blank-line-verdict-invariance`) that verify the exact D2 property AC10 was written to check, using this suite's own `write_spec()` helper (which does emit a well-formed `**Task ID**: T-900` line).
  reason: AC10's inline `spec.md` fixture (`printf '# S\n\n## Goal\n\n<!-- BEGIN intent-block: T-901 -->\n\nfrozen text\n\n...` — no `**Task ID**: T-901` line anywhere) never satisfies `bin/check-intent.sh`'s own hard requirement (`TASK_ID_RE`) for a spec argument, so ALL THREE of AC10's boards (`blank.md`, `noblank.md`, `single.md`) return the SAME `structural: no '**Task ID**: T-NNN' line found in spec` error (exit 2) — for a reason that has nothing to do with D2's blank-line neutrality. This makes `test "$b" = "$n"` and `test "$b" = "2"` pass for the wrong reason (both hit the identical unrelated defect, not the invariance the assertion is meant to prove), while `test "$g" = "1"` fails outright (single.md also returns 2, not the expected 1) — confirmed measured directly (`blank rc=2`, `noblank rc=2`, `single rc=2`, all with the identical `no '**Task ID**: T-NNN' line found` stderr). This is precisely the failure class the spec's own "## Assumptions" item 2 anticipated ("If the control returns 2, replace it with a control that distinguishes the classes some other way — do **not** delete the control") — but AC10's `check:` line sits inside the intent block frozen at v1, and the task's own operating instructions are explicit that a frozen `check:` line found mechanically broken during implementation must be reported BLOCKED with evidence rather than silently rewritten, since only a human-ratified re-freeze may correct frozen text. The minimal, meaning-preserving correction (adding a `**Status**: READY_FOR_ARCH\n**Owner**: pm-spec\n**Task ID**: T-901\n\n` prologue to AC10's inline spec.md, matching every other case's spec shape in the same file) is recommended in the hand-off for that re-freeze, not applied here.
  grounding: .shell-team/specs/T-1016-close-out-entry-boundary.md, AC10's `check:` line (the `printf` building `$d/spec.md` with no `**Task ID**:` line) and "## Assumptions" item 2 ("If the control returns 2, replace it with a control that distinguishes the classes some other way — do **not** delete the control..."); bin/check-intent.sh, `TASK_ID_RE` and `[ -n "$TASK_ID" ] || fail_structural "no '**Task ID**: T-NNN' line found in spec: $SPEC"`; self-run reproduction of AC10's exact three fixtures against the real `bin/check-intent.sh` (all three return `structural: no '**Task ID**: T-NNN' line found`, exit 2); tests/check-intent/run.sh, the new "(xiv) T-1016 D2" case using `write_spec()` (which does emit `**Task ID**: %s`), independently verifying the same property AC10 targets.

<!-- END provenance: T-1016 -->
