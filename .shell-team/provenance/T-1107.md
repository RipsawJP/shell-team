<!-- BEGIN provenance: T-1107 -->
- decision: the two AC3(v) divergent decoy rows are built by mirroring the pre-existing `tests/close-out/run.sh:997`-shaped "decoy separator" construction — a title ending in a fake ` — \`FLAG\` — spec: fake.md but continues` fragment before the real rightmost ` — \`FLAG\` — spec: real.md` slot — rather than a simpler shape such as two `- [ ]` lines concatenated onto one string, or a line carrying only a single decoy backtick pair with no second ` — spec:` fragment.
  reason: several shapes could plausibly produce "two backticked tokens on one line, only one of them the true rightmost flag", but not all of them exercise the same failure mode a first-match extraction bug actually has: a shape whose first backtick pair is not itself followed by a `— spec: *.md` sequence never gets mistaken for a legitimate rightmost match by any regex resembling `close-out.sh:463`'s own, so it would not discriminate the mutation AC15(a) mutation 3 targets. Reusing the proven `:997` construction keeps the discriminating property (a full second ` — spec:` fragment that could pass as the "real" one under a naive first-match implementation) while changing only the flag values, which is what makes Row A/B genuinely diverge on accept/refuse where `:997` itself (per the spec's own Notes for engineer) does not.
  grounding: `tests/close-out/run.sh`'s `LS_LINES` array, the "divergent decoy row A"/"divergent decoy row B" entries; `.shell-team/specs/T-1107-close-out-gates.md` AC3(v) ("Row A ... a row whose first backticked token is READY_FOR_MERGE and whose rightmost ... slot carries a non-READY_FOR_MERGE allowed flag") and its own citation of `tests/close-out/run.sh:997` as a shape that does *not* discriminate.

- decision: every inline fixture in `tests/close-out/run.sh` whose Active flag was incidental to the property under test (move mechanics, interventions handling, dispatch/oversight/review-input wiring, malformed-title/path/separator shapes) is flipped to `READY_FOR_MERGE`, including cases such as `closeout-unrelated-active-line-interlock-unchanged` (`UAL_ROOT`) and `closeout-done-section-loose-entry-unaffected` (`DSL_ROOT`) where flipping T-901's own flag changes which gate produces the refusal (from the pre-write interlock reached only once T-901's own flag clears the new pre-flip gate) while leaving the asserted exit code and stderr literal unchanged — rather than leaving those two fixtures' flag at `READY_FOR_QA` and updating their assertions to expect the new gate's own message instead.
  reason: two options existed for every site the new gate could now reach — flip the incidental flag to `READY_FOR_MERGE` so the fixture reaches the SAME later gate/interlock it always tested, or leave the flag and rewrite the assertion to expect the new gate's own refusal. The spec's own Notes for engineer state the default-flip rule and explicitly reject "refuses at an earlier gate anyway" as an exemption, but `UAL_ROOT`/`DSL_ROOT` are not "refuses earlier" cases — the pre-write interlock they test fires AFTER the new gate in the file's control flow, so flipping was required (not merely permitted) to keep testing the interlock rather than accidentally re-testing the new gate under a different fixture name. This is observable in the suite's own stderr assertions (`'rewritten board would fail check-handoff.sh'` for UAL_ROOT) staying attributable to the interlock rather than being silently replaced by the new gate's distinct message.
  grounding: `tests/close-out/run.sh`'s `UAL_ROOT`/`DSL_ROOT` printf lines and their `grep -qF -- 'rewritten board would fail check-handoff.sh'` / Done-entry assertions; `bin/close-out.sh`'s gate order (the new pre-flip gate sits immediately after the source-line gate's `esac`, strictly before the entry-file build and the pre-write interlock further down); `.shell-team/specs/T-1107-close-out-gates.md` `## Notes for engineer` trap 3 (the default-to-MERGE rule and its "why 'refuses at an earlier gate anyway' is not an exemption" paragraph) and the round-3 spec-review finding naming `:1100`'s masked-interlock-reason risk.

- decision: the new pre-flip gate's refusal message is worded `"${BOARD}:${A_START}: $TASK's Active flag is \`$FOUND_FLAG\`, not \`READY_FOR_MERGE\` — a close-out only promotes an entry the Codex reviewer has already approved (found \`$FOUND_FLAG\`, expected \`READY_FOR_MERGE\`)"`, and the `#266` note is worded `"close-out: note: no --issue given — if this task closes a GitHub issue, develop merges do NOT auto-close it; run close-out.sh again with --issue N (or close it by hand) once you have the number."`, rather than any of several other phrasings that would equally satisfy the spec's own literal-substring `- check:` assertions.
  reason: the spec pins only substrings (the found flag, the literal `READY_FOR_MERGE`, a `todo.md:<n>:` shape for the gate message; the literal `close-out: note: no --issue given` and the phrase `develop merges do NOT auto-close` for the note) and explicitly leaves the rest of each sentence to the engineer (Body-to-AC correspondence: "pinning a full sentence would make an improvement in wording a criterion failure"). Both messages are adopter-visible stderr/stdout text (AC7's adopter-surface declaration), so the exact wording is an observable-behaviour choice worth recording even though no check line pins it exactly.
  grounding: `bin/close-out.sh`'s new pre-flip gate (the `fail "${BOARD}:${A_START}: ..."` line, immediately after the source-line gate's `esac`) and the `else` arm of `if [ -n "$ISSUE" ]`; `.shell-team/specs/T-1107-close-out-gates.md` AC1 (message-substring assertions), AC4 (note-substring assertions), and the Body-to-AC correspondence row for "The note's exact wording follows issue #266's own proposal" (`info-only`, not promoted to an AC).
<!-- END provenance: T-1107 -->
