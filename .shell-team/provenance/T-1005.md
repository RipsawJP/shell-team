<!-- BEGIN provenance: T-1005 -->

- decision: The AC7 clause ("not on which branch you are merging into") and the AC5/AC6 reason paragraphs are placed together in the "What is worth keeping in either direction" section, immediately after the two bullets — not inside the Fixed section's DP-1 paragraph, and not inside the "fewer interruptions" example section.
  reason: The spec's Measured inventory table assigns only one new-prose site to that location ("worth keeping" section, after the bullets → "the reason paragraph (AC5, AC6) and the trap paragraph (AC8)") and assigns no site at all to AC7's clause specifically, so where the branch-agnostic clause lands was mine to decide. The Fixed section's job (DP-1) is authority vs. interruption, a different claim than "which branch" — folding AC7 in there would conflate two distinct points the spec keeps separate. The "worth keeping" section is the general-principle section every reader passes through regardless of which example they read first (Input space class 2), and DP-3 already grounds the trap paragraph there for the identical reason ("belongs next to the criterion, not next to one example") — the branch-agnostic clause is part of the same criterion statement, so co-locating it with the reason/trap paragraphs keeps one criterion in one place instead of splitting it across two sections.
  grounding: .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md Measured inventory row "worth keeping section, after the bullets | new / new | the reason paragraph (AC5, AC6) and the trap paragraph (AC8)"; Body-to-AC correspondence row "Not gated on the base branch, and the page says so | AC7"; DP-3's placement reasoning ("belongs next to the criterion, not next to one example"); docs/tuning-oversight.md paragraph beginning "The first stop fires on whether the merge changes what runs — not on which branch you are merging into".

- decision: All newly authored prose paragraphs (the Fixed section's DP-1 addition, the AC16 substitution notes, and the four new paragraphs/bullet text in "What is worth keeping in either direction") are written as single unwrapped physical lines in both files, rather than hand-wrapped at this file's existing ~79-column English convention (or left unwrapped-but-multi-sentence-per-line for the Japanese, which was already the file's own convention).
  reason: Several `check:` anchors in this spec are multi-word literal substrings (`grep -qF`, which matches only within one physical line) — e.g. `a discipline the operator imposes on themselves` (49 chars) and `not on which branch you are merging into` (42 chars). Hand-wrapping at ~79 columns risks splitting an anchor phrase across a line boundary, which would make the anchor's characters non-contiguous in either resulting line and silently fail the criterion despite the sentence reading correctly to a human. The spec explicitly declines to assert line-width/wrapping mechanically (Non-goals-adjacent Out-of-scope-synthetic-extremes item 5, and the correspondence table's matching info-only row), so choosing correctness-of-anchor-placement over the file's pre-existing wrap style costs nothing against any criterion and removes a self-inflicted failure mode entirely rather than hand-verifying each wrap point.
  grounding: .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md Out-of-scope synthetic extremes item 5 ("Line-width, wrapping and other typographic uniformity of the edited prose ... declined"); Body-to-AC correspondence row "Line width ~79 columns for English prose, unwrapped paragraphs for Japanese | info-only (not promoted to AC)"; AC6's check line requiring the literal 49-character substring `a discipline the operator imposes on themselves` to be grep -qF-matched.

<!-- END provenance: T-1005 -->
