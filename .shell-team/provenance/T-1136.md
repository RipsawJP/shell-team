<!-- BEGIN provenance: T-1136 -->

- decision: rewrote both canonical `contain`-mode prompt blocks (`templates/prompt-blocks/operating-paths-core.md` and `templates/prompt-blocks/executor-resolution.md`) to carry the fixed string `<plugin root>/bin/` inside their own shared line — `operating-paths-core.md` with a generic `<script>` placeholder (`` invoked as `bash "<plugin root>/bin/<script>"` ``), `executor-resolution.md` with its one always-named script (`resolve-executor.sh`) — rather than stripping the literal out of the blocks entirely and requiring every registered consumer to independently carry its own script-specific `<plugin root>/bin/<real-script>` phrase in a tail outside the spliced text.
  reason: AC2's own check only requires the fixed string `<plugin root>/bin/` to appear somewhere in each carried file's flattened text; a block that already carries it propagates the requirement to every one of its registered consumers automatically via `bin/check-prompt-sync.sh`'s `contain`-mode verbatim splice, so no per-consumer follow-up edit is needed purely to satisfy AC2 — and the generic placeholder keeps `operating-paths-core.md` genuinely script-name-less (AC2's own stated exemption ground for that file), since `<script>` names no real script and the actual script is still stated in the surrounding consumer prose that names it (e.g. `` `team-paths.sh --get …` (invoked as `bash "<plugin root>/bin/<script>"` …) ``).
  grounding: `.shell-team/specs/T-1136-invocation-policy.md` AC2 (the fixed-string check `grep -Fq -- '<plugin root>/bin/'`) and its own rationale paragraph ("whose canonical lines are script-name-less mid-sentence fragments"); `templates/prompt-blocks/registry.txt:38,43` (the two `contain` rows and their consumer lists); `bin/check-prompt-sync.sh`'s `check_contain()` (verbatim-line-in-consumer requirement).

- decision: for the two registered consumers whose every occurrence of the retired claim named a specific script (`skills/run/SKILL.md`'s "Step 0" team-paths.sh sentence, `skills/goal/SKILL.md`'s "Step 0" team-paths.sh sentence), rewrote exactly one occurrence in each file to carry the canonical block's own literal `<script>`-placeholder text verbatim (rather than the specific-script phrasing used everywhere else the claim recurred in those files), and left every other occurrence in those same two files using the specific real script name.
  reason: `bin/check-prompt-sync.sh`'s `check_contain()` reads the canonical block file line by line and requires each line to appear as a literal, unmodified substring somewhere in the consumer — a consumer that only ever names a specific script (e.g. substitutes `team-paths.sh` in place of the literal `<script>` token at every site) never carries the block's own exact bytes and fails sync, discovered live when `bash bin/check-prompt-sync.sh` first flagged both files after an initial pass that used only the specific-script phrasing everywhere; reusing the pre-existing "Step 0" sentence for the literal form avoided adding a wholly new standalone note purely to host the exact canonical text.
  grounding: `bin/check-prompt-sync.sh`'s `check_contain()` function body (verbatim fixed-string-per-line match against the consumer file); this task's own first `bash bin/check-prompt-sync.sh` run, which reported `skills/run/SKILL.md` and `skills/goal/SKILL.md` (and, before a further fix, `agents/ui-designer.md`) as missing the required phrase from `operating-paths-core.md`.

- decision: rewrote `agents/ui-designer.md`'s "Operating paths" blockquote paragraph as a single physical line (previously six wrapped `> `-prefixed lines) rather than keeping the hard-wrap and only replacing the parenthetical's inner text in place.
  reason: `check_contain()` matches a canonical block's line against the consumer file with a per-physical-line `grep -F`, so a canonical line spliced across a hard line-wrap in the consumer is invisible to it even though the same bytes are present once whitespace is collapsed — the same class of gap `## Input space` item 2 and `## Assumptions`' D3 identify for AC1's own reading rule, encountered here on the write side instead of the read side; collapsing the paragraph to one physical line is the same fix already in use in the sibling agent files this block is registered against (`agents/engineer.md`, `agents/qa-verifier.md`, `agents/scrum-master.md`, `agents/triage-orchestrator.md`, `agents/codex-reviewer.md`), none of which hard-wrap this paragraph.
  grounding: `bin/check-prompt-sync.sh`'s `check_contain()` (per-line `grep -qF` against the whole consumer file, not a whitespace-normalized read); this task's own `bash bin/check-prompt-sync.sh` run, which reported the same missing-phrase failure for `agents/ui-designer.md` before this rewrite and reported `all registered prompt blocks in sync` after it.

- decision: rewrote `CLAUDE.md`'s `## Dogfood` section as an "observed in this checkout's own sessions, not guaranteed" statement that keeps the bare-name instruction as the primary path and adds an explicit `bash bin/<script>` fallback sentence for when it does not resolve, rather than leaving the bare-name instruction as an unconditional imperative and merely appending a short disclaimer sentence after it.
  reason: **AC1** requires this file to carry none of the nine claim spellings — including the unconditional "`bin/` is on `PATH`" — because `CLAUDE.md` is in AC1's population even though **AC2** exempts it from the `<plugin root>/bin/` literal requirement (its own subject is this checkout, not an installed plugin); a bare disclaimer bolted onto an unchanged imperative sentence would still literally read as the same false claim it was appended to, so the sentence itself had to change, and stating both the maintainer's own measured contradiction (2026-09-11, plugin load and `PATH` injection are separate mechanisms) and the concrete fallback keeps the section genuinely useful to a contributor in either observed state rather than merely hedged.
  grounding: `.shell-team/specs/T-1136-invocation-policy.md` **AC4** and **D7** ("Keeps `--plugin-dir ./`, drops the guarantee, states the observed-not-guaranteed shape"); the maintainer's 2026-09-11 comment on issue #485 relayed in the spec's `## Assumptions` (a resumed `claude --continue --plugin-dir ./` session loaded the checkout plugin while the Bash tool's `PATH` did not carry `<checkout>/bin`).

- decision: implemented #494 item 2's cleanup-trap fix by tracking the current pass-2 staging file in a single scalar variable `CUR_STAGE_FILE` (set immediately after `mktemp`, cleared immediately after `mv` succeeds, read by `cleanup()` under the same `${VAR:-}` set -u-safe expansion the existing `CONTENT_FILES` guard already uses), rather than appending each staged path to a second array cleaned up the same way as `CONTENT_FILES`.
  reason: pass 2's loop only ever has at most one staged-but-not-yet-renamed file in flight at a time (the loop stages, then immediately renames, one role per iteration), so a scalar variable holding "the one path currently at risk, or empty" is the minimal state that makes the trap's cleanup exact — an array would carry every prior iteration's already-renamed (and therefore already-nonexistent) path forever, doing nothing wrong on `rm -f` but growing without bound and obscuring, on inspection, which single path the trap is actually protecting at the moment of failure.
  grounding: `.shell-team/specs/T-1136-invocation-policy.md` `## Notes for engineer` ("Track the current stage path in a variable the `cleanup()` trap also removes, cleared after the `mv` succeeds, and keep the bash-3.2 `"${CONTENT_FILES[@]+…}"` alternate-value guard... exactly as it is"); `bin/gen-codex-agents.sh`'s pass-2 loop (`STAGE_FILE="$(mktemp …)"` / `mv "$STAGE_FILE" …`), which stages and renames exactly one file per iteration.

<!-- END provenance: T-1136 -->

- reproduce: bash bin/derive-populations.sh --label t1136-claim-sites-en --set 'onpath-loaded=git grep -lF -- "on PATH when the plugin is loaded" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates' --set 'onpath-btick-loaded=git grep -lF -- "on `PATH` when the plugin is loaded" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates' --set 'onpath-enabled=git grep -lF -- "on PATH while the plugin is enabled" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates' --set 'added-btick-enabled=git grep -lF -- "added to `PATH` while the plugin is enabled" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates' --set 'loaded-bin-onpath=git grep -lF -- "the plugin loaded, `bin/` is on `PATH`" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates'
<!-- BEGIN derivation: t1136-claim-sites-en -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: onpath-loaded — status: 0 — lines: 13 — items: 13 — command: git grep -lF -- "on PATH when the plugin is loaded" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates
- set: onpath-btick-loaded — status: 0 — lines: 8 — items: 8 — command: git grep -lF -- "on \`PATH\` when the plugin is loaded" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates
- set: onpath-enabled — status: 0 — lines: 1 — items: 1 — command: git grep -lF -- "on PATH while the plugin is enabled" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates
- set: added-btick-enabled — status: 0 — lines: 1 — items: 1 — command: git grep -lF -- "added to \`PATH\` while the plugin is enabled" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates
- set: loaded-bin-onpath — status: 0 — lines: 2 — items: 2 — command: git grep -lF -- "the plugin loaded, \`bin/\` is on \`PATH\`" $(git merge-base develop HEAD) -- README.md README.ja.md CLAUDE.md CONTRIBUTING.md docs skills agents templates
- union: items: 19
- bucket: added-btick-enabled — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:docs/distribution.md
- bucket: loaded-bin-onpath — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:CLAUDE.md
- bucket: onpath-btick-loaded — items: 3
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:docs/workflow.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:skills/review/SKILL.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:templates/prompt-blocks/executor-resolution.md
- bucket: onpath-enabled+loaded-bin-onpath — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:README.md
- bucket: onpath-loaded — items: 8
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/codex-reviewer.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/drift-evaluator.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/engineer.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/triage-orchestrator.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/ui-designer.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:skills/loop-triage/SKILL.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:skills/team-init/SKILL.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:templates/prompt-blocks/operating-paths-core.md
- bucket: onpath-loaded+onpath-btick-loaded — items: 5
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/qa-verifier.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:agents/scrum-master.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:skills/goal/SKILL.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:skills/review-response/SKILL.md
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:skills/run/SKILL.md
<!-- END derivation: t1136-claim-sites-en -->

- reproduce: bash bin/derive-populations.sh --label t1136-claim-sites-ja --set 'ja-enabled-allpath=git grep -lF -- "プラグイン有効時すべて PATH に載る" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates' --set 'ja-enabled-addedpath=git grep -lF -- "プラグイン有効時に `PATH` に追加されます" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates' --set 'ja-loaded-binpath=git grep -lF -- "プラグインをロードしていれば `bin/` は `PATH` に載る" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates' --set 'ja-loaded-onpath=git grep -lF -- "プラグインが読み込まれていれば `PATH` 上にある" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates'
<!-- BEGIN derivation: t1136-claim-sites-ja -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: ja-enabled-allpath — status: 0 — lines: 1 — items: 1 — command: git grep -lF -- "プラグイン有効時すべて PATH に載る" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates
- set: ja-enabled-addedpath — status: 0 — lines: 1 — items: 1 — command: git grep -lF -- "プラグイン有効時に \`PATH\` に追加されます" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates
- set: ja-loaded-binpath — status: 0 — lines: 2 — items: 2 — command: git grep -lF -- "プラグインをロードしていれば \`bin/\` は \`PATH\` に載る" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates
- set: ja-loaded-onpath — status: 0 — lines: 1 — items: 1 — command: git grep -lF -- "プラグインが読み込まれていれば \`PATH\` 上にある" $(git merge-base develop HEAD) -- README.ja.md docs skills agents templates
- union: items: 4
- bucket: ja-enabled-addedpath — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:docs/distribution.ja.md
- bucket: ja-enabled-allpath+ja-loaded-binpath — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:README.ja.md
- bucket: ja-loaded-binpath — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:docs/adopting.ja.md
- bucket: ja-loaded-onpath — items: 1
  - 7be4d9b6c773cd251956b8c7a6a3e52e50d0fd36:docs/workflow.ja.md
<!-- END derivation: t1136-claim-sites-ja -->
