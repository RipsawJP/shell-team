<!-- BEGIN provenance: T-1150 -->

- decision: The duration class stated in the "Waiting on a spawned role, on the Codex CLI host (T-1150)." paragraph (`templates/prompt-blocks/host-dispatch.md`, spliced into `skills/run/SKILL.md`) is taken from a measurement made in this checkout, never recalled or estimated.
  reason: An orchestrator that aborted spawned roles at eight to thirteen minutes had no way to know that duration is ordinary for this loop's long steps; a wall-clock figure with no reproducible origin would be exactly as unfounded as the impatience it replaces.
  grounding: measured — command 1: `for d in tests/*/; do bash "$d/run.sh"; done` (full sweep of every suite under `tests/`, run in the foreground, no parallelism), reproduce from the repository root with `SECONDS=0; for d in tests/*/; do bash "$d/run.sh" || echo "FAILED: $d"; done; echo "ELAPSED=${SECONDS}s"`, measured **523 seconds** (~8.7 minutes, rounded to "about 9 minutes" in the shipped sentence), all 60 suites passing, on this checkout at commit `7a6d421f`. command 2: `bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md` (T-1149's own spec, whose criteria only read committed files — no nested `tests/<suite>/run.sh` invocation among them), reproduce from the repository root with `SECONDS=0; bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md; echo "ELAPSED=${SECONDS}s"`, measured **4 seconds** on the same checkout.

- decision: The "4 seconds" figure above is a poor stand-in for this loop's actual long class, and the shipped sentence is corrected to a range rather than a single number (round 2, per the coordinating session's own diff-read finding). `check-acs` over a spec is not uniformly fast: a spec whose own `- check:` lines re-run other `tests/<suite>/run.sh` fixture suites inside the criterion (the class the measured adopter failure in issue #566 actually hit — `qa-verifier` was aborted mid-collection of exactly this kind of long run) takes far longer than one whose criteria only grep committed files, and T-1149 (round 1's example) happens to be the fast kind, not a fair stand-in for "long."
  reason: A Codex-host orchestrator reading only the round-1 sentence would learn that `check-acs` always finishes in about 4 seconds, which is the opposite lesson of the one the measured failure needed — the adopter run this task exists to fix aborted a role specifically because a long-running check was mistaken for a stall.
  grounding: measured — the long class was enumerated mechanically with `git grep -l -E '^[[:space:]]*- check:.*bash tests/[^ ]+/run\.sh' -- .shell-team/specs` (81 matching specs); `.shell-team/specs/T-1144-code-reviewer-rename.md` was picked as a member that completes and whose own prose already states "AC13 runs six fixture suites and AC3 regenerates every Codex agent, so several criteria are minutes rather than seconds" — reproduce with, from the repository root, `SECONDS=0; CHECK_ACS_TIMEOUT=3600 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1144-code-reviewer-rename.md; echo "ELAPSED=${SECONDS}s"`, measured **85 seconds** (rounded to "about 85 seconds" in the shipped sentence) on a disposable `git clone --shared` of this checkout at commit `177ca55f`, checked out to `feature/566-codex-wait-on-spawned-role`, run to completion in the foreground with the command's own output and elapsed time written to a file under `$TMPDIR` and awaited with a bounded foreground polling loop rather than left as a detached background job (the T-1001 attempt in round 1's own targeted-regression note did not finish within 25+ minutes and was abandoned as a poor pick for this same reason — a nested-suite-heavy spec is not automatically a *fast-nested-suite-heavy* spec, and T-1144 was chosen and measured, not assumed, because its own prose already named the mechanism and a live run confirmed it finishes).

<!-- END provenance: T-1150 -->
