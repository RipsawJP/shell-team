<!-- BEGIN provenance: T-1150 -->

- decision: The duration class stated in the "Waiting on a spawned role, on the Codex CLI host (T-1150)." paragraph (`templates/prompt-blocks/host-dispatch.md`, spliced into `skills/run/SKILL.md`) is taken from a measurement made in this checkout, never recalled or estimated.
  reason: An orchestrator that aborted spawned roles at eight to thirteen minutes had no way to know that duration is ordinary for this loop's long steps; a wall-clock figure with no reproducible origin would be exactly as unfounded as the impatience it replaces.
  grounding: measured — command 1: `for d in tests/*/; do bash "$d/run.sh"; done` (full sweep of every suite under `tests/`, run in the foreground, no parallelism), reproduce from the repository root with `SECONDS=0; for d in tests/*/; do bash "$d/run.sh" || echo "FAILED: $d"; done; echo "ELAPSED=${SECONDS}s"`, measured **523 seconds** (~8.7 minutes, rounded to "about 9 minutes" in the shipped sentence), all 60 suites passing, on this checkout at commit `7a6d421f`. command 2: `bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md` (T-1149's own spec, carrying long criteria), reproduce from the repository root with `SECONDS=0; bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md; echo "ELAPSED=${SECONDS}s"`, measured **4 seconds** on the same checkout.

<!-- END provenance: T-1150 -->
