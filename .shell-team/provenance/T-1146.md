<!-- BEGIN provenance: T-1146 -->

- decision: rewrote the whole `bin/gen-codex-agents.sh` legacy-removal comment paragraph (the sentence claiming the step is "bounded to a regular file only") into a new two-paragraph explanation — one paragraph restating the unchanged pre-existing behaviour, a second, new paragraph naming the symlink widening and explicitly restating that a directory/FIFO/other non-symlink special occupant is still left alone — rather than patching the single false clause in place with a minimal inline correction.
  reason: AC7 requires the fixed string `bounded to a regular file only` absent everywhere in the file and requires the comment region to carry `symlink`, but the region also has to keep reading as true prose for a maintainer, not just pass a grep. A minimal inline patch (e.g. deleting the clause and leaving a dangling "and" or silently dropping the FIFO/directory disposition) would have left the paragraph internally inconsistent with the widened behaviour it now describes, and would have separated the removal rule from its own rationale. Structuring it as "what stays true" then "what widens, and why the widening still stops at a symlink" mirrors the frozen Goal's own two-part explanation ("Which occupant types are removed, which are only reported, and why the line sits there"), so the shipped comment and the frozen spec prose make the same argument in the same order.
  grounding: `bin/gen-codex-agents.sh` (the rewritten `# --- one named legacy-basename removal ...` comment block, lines preceding `legacy_role_requested=0`); `.shell-team/specs/T-1146-codex-agents-migration-followups.md` Goal section, "Which occupant types are removed, which are only reported, and why the line sits there"; **AC7** (`bounded to a regular file only` at zero occurrences; `symlink` present in the extracted region).

- decision: in `bin/check-codex-agents.sh`'s header Method bullet list, folded the dangling-symlink audit-loop widening into the existing "an extra shell-team-*.toml --out-dir does not own -> violation" bullet as an appended clause, rather than adding a new, separate top-level Method bullet describing symlink handling on its own.
  reason: the audit loop's widened guard (`[ -e "$f" ] || [ -L "$f" ] || continue`) does not add a new violation class — a symlink occupant of an unowned `shell-team-*.toml` name was always meant to reach the same "extra" bullet's logic; the pre-fix gap was only that a *dangling* one failed `-e` and was silently skipped before reaching that logic at all. Describing it as an extension of the existing bullet, rather than a new one, keeps the header's own taxonomy accurate: there are still exactly the same violation classes (drift, missing, extra), and "extra" now explicitly names that a symlink occupant is not exempt from it.
  grounding: `bin/check-codex-agents.sh` (the "extra" Method bullet, and the audit loop's `[ -e "$f" ] || [ -L "$f" ] || continue` guard at what was originally `:141`); `.shell-team/specs/T-1146-codex-agents-migration-followups.md` **AC3**, **AC7** (the header region must carry `symlink`); Goal section, "The checker's audit loop widens from `[ -e "$f" ] || continue` to `[ -e "$f" ] || [ -L "$f" ] || continue`, so a dangling symlink reaches the same legacy-specific hint every other occupant of that name reaches".

- decision: embedded the #545 set delta as two separate `bin/derive-populations.sh` blocks — one labelled `t1146-issue545-header-tokens` (base vs. HEAD of the header-matrix extraction) and one labelled `t1146-issue545-callsite-tokens` (base vs. HEAD of the `refuse`-call-site extraction) — rather than one combined four-set block mixing both extractions together.
  reason: `## Notes for engineer` names "the two set commands" as "the `calls()` and `hdr()` extractions **AC9** spells out, run against the base blob and against HEAD", which is naturally two independent base-vs-HEAD comparisons (one per extraction kind), not one four-way comparison across extraction kinds that would never be meaningfully compared against each other (a `calls()` item and an `hdr()` item are never expected to coincide as the same population). Two blocks also let each one directly witness a different part of AC9/AC10's own claim: the header-tokens block shows the exact one-token widening (`collision` added to the header set, nothing else), and the callsite-tokens block shows the call-site set is unchanged between base and HEAD (this task adds no new `refuse` call site) — a fact AC10's byte-scope assertion also depends on but does not itself enumerate as a set.
  grounding: `.shell-team/specs/T-1146-codex-agents-migration-followups.md` `## Notes for engineer` ("Run `bin/derive-populations.sh` for the #545 set delta ... the two set commands are the `calls()` and `hdr()` extractions **AC9** spells out, run against the base blob and against HEAD"); **AC9**, **AC10**; `bin/derive-populations.sh --help` (2–8 `--set` values per block, no upper bound reason to force a single block).

- decision: appended the new `tests/check-binding/run.sh` case (`cb-refusal-matrix-complete`) at the very end of the file — after the T-1144 alias diagnostic-ordering regression block, immediately before the final `printf 'check-binding suite: all cases passed\n'` — rather than immediately after `cb-adapters-not-forwarded-population`, the case chronologically nearest the point where the header case-id list ends.
  reason: every other case in this suite already appends strictly in file order (each new task's fixtures land after the previous task's, never interleaved into an earlier task's block), and the T-1144 alias block is the suite's own most-recently-appended section; keeping `cb-refusal-matrix-complete` after it, not before it, preserves that same append-only convention rather than inserting a new case ahead of an already-shipped one. The header case-id list documents cases in the same append order (the new entry was added at the end of that list, immediately before `set -euo pipefail`), so the file's physical order and its documented order agree.
  grounding: `tests/check-binding/run.sh` (case-id list ending immediately before `set -euo pipefail`; the new case body immediately before the final `printf`); `.shell-team/specs/T-1146-codex-agents-migration-followups.md` `## Notes for engineer` ("append one case with the id `cb-refusal-matrix-complete` and add that id to the header case-id list").

<!-- END provenance: T-1146 -->

- reproduce: bash bin/derive-populations.sh --label t1146-issue545-header-tokens --set 'base=git show "$(git merge-base develop HEAD):bin/check-binding.sh" | LC_ALL=C sed -n '"'"'/^# Refusal matrix/,/^# Validation order/p'"'"' | grep -oE '"'"'[a-z][a-z-]+ \([12]\)'"'"' | sed '"'"'s/ .*//'"'"' | sort -u' --set 'head=LC_ALL=C sed -n '"'"'/^# Refusal matrix/,/^# Validation order/p'"'"' bin/check-binding.sh | grep -oE '"'"'[a-z][a-z-]+ \([12]\)'"'"' | sed '"'"'s/ .*//'"'"' | sort -u'
<!-- BEGIN derivation: t1146-issue545-header-tokens -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: base — status: 0 — lines: 20 — items: 20 — command: git show "$(git merge-base develop HEAD):bin/check-binding.sh" | LC_ALL=C sed -n '/^# Refusal matrix/,/^# Validation order/p' | grep -oE '[a-z][a-z-]+ \([12]\)' | sed 's/ .*//' | sort -u
- set: head — status: 0 — lines: 21 — items: 21 — command: LC_ALL=C sed -n '/^# Refusal matrix/,/^# Validation order/p' bin/check-binding.sh | grep -oE '[a-z][a-z-]+ \([12]\)' | sed 's/ .*//' | sort -u
- union: items: 21
- bucket: base+head — items: 20
  - bad-token
  - binding-changed
  - duplicate-role
  - duplicate-schema
  - lock-missing
  - lock-structural
  - missing-config
  - missing-role
  - missing-schema
  - path-mismatch
  - provider-adapter-mismatch
  - registry-malformed
  - registry-unreadable
  - schema-not-first
  - unknown-adapter
  - unknown-provider
  - unknown-role
  - unparseable-line
  - unsupported-schema
  - usage
- bucket: head — items: 1
  - collision
<!-- END derivation: t1146-issue545-header-tokens -->

- reproduce: bash bin/derive-populations.sh --label t1146-issue545-callsite-tokens --set 'base=git show "$(git merge-base develop HEAD):bin/check-binding.sh" | LC_ALL=C grep -oE '"'"'refuse [a-z][a-z-]+ [12]'"'"' | awk '"'"'{print $2}'"'"' | sort -u' --set 'head=LC_ALL=C grep -oE '"'"'refuse [a-z][a-z-]+ [12]'"'"' bin/check-binding.sh | awk '"'"'{print $2}'"'"' | sort -u'
<!-- BEGIN derivation: t1146-issue545-callsite-tokens -->
- derived-by: bin/derive-populations.sh
- locale: LC_ALL=C
- set: base — status: 0 — lines: 21 — items: 21 — command: git show "$(git merge-base develop HEAD):bin/check-binding.sh" | LC_ALL=C grep -oE 'refuse [a-z][a-z-]+ [12]' | awk '{print $2}' | sort -u
- set: head — status: 0 — lines: 21 — items: 21 — command: LC_ALL=C grep -oE 'refuse [a-z][a-z-]+ [12]' bin/check-binding.sh | awk '{print $2}' | sort -u
- union: items: 21
- bucket: base+head — items: 21
  - bad-token
  - binding-changed
  - collision
  - duplicate-role
  - duplicate-schema
  - lock-missing
  - lock-structural
  - missing-config
  - missing-role
  - missing-schema
  - path-mismatch
  - provider-adapter-mismatch
  - registry-malformed
  - registry-unreadable
  - schema-not-first
  - unknown-adapter
  - unknown-provider
  - unknown-role
  - unparseable-line
  - unsupported-schema
  - usage
<!-- END derivation: t1146-issue545-callsite-tokens -->

## Out-of-scope observation (recorded per `## Notes for engineer`, not fixed here)

`bin/gen-codex-agents.sh`'s `--help` prints `sed -n '2,107p'` while its header comment runs to `:119` (measured against the file as it stands after this task's own edits, which do not touch this range), so the `Exit:` paragraph is already truncated out of the printed help. This predates this task, is not made worse by it (this task's edits are all below line `:119`, inside the legacy-removal comment and the removal gate, neither of which is inside the `2,107` range printed by `--help`), and the Non-goals explicitly forbid fixing it here. Filing the corresponding issue is the coordinating session's action, not this role's (this role has no GitHub-issue-filing tool).
