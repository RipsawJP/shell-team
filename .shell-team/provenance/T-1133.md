<!-- BEGIN provenance: T-1133 -->

- decision: wrote the qa-verifier Rule as a single conditional sentence whose true branch (`.github/workflows` present) states what to run, what a non-zero exit means, and what to do when a step cannot run locally, and whose false branch is the file's own `keep the prior behavior unchanged` idiom, rather than splitting the norm across two bullets or a bulleted sub-list.
  reason: AC1 requires all five literals (`.github/workflows`, `a non-zero exit is a FAIL of the round`, `never a silent skip`, `the version the workflow pins`, `keep the prior behavior unchanged`) on **one physical line** beginning at column 0 with `- **`, matching the file's own Rule shape; a second bullet would put at least one literal outside that line and fail the check's single-hit assertion.
  grounding: `.shell-team/specs/T-1133-ci-parity-verification.md` AC1's check line (`grep -nF ... | grep -F ... | ... > "$T/hit"; test "$(awk 'END{print NR}' "$T/hit")" -eq 1`); `## Notes for engineer` ("do not split the norm across two bullets").

- decision: gave the docs section its own `## ` heading in both `docs/adopting.md` and `docs/adopting.ja.md` (`## Both gates green and your own CI` / `## 両ゲート green と自分のリポジトリの CI`), rather than appending a paragraph to the preceding `## Declaring adopter-facing documentation` section.
  reason: the preceding section is an unrelated subject (the `- user-visible:`/`- adopter-surface:` declaration duty), so a trailing paragraph would silently attach the new content to it; a dedicated heading also keeps the EN/JA `## ` heading count symmetric and machine-checkable (both files go from 18 to 19), which AC5 asserts as a base-relative delta.
  grounding: `.shell-team/specs/T-1133-ci-parity-verification.md` `## Notes for engineer` ("Placement reason for the new documentation heading... the paragraph gets its own `## ` heading rather than being appended to the preceding section... A heading also keeps the EN/JA mirror measurable"); AC5's check (`HE -eq HJ` and each `-eq` its own base count `+1`).

- decision: implemented the team-init summary line's trigger as a directory-existence test against `$TARGET/.github/workflows` only (no enumeration of files inside it, no distinction between `.yml`/`.yaml`, no check for an empty workflows directory).
  reason: the spec's `## Input space` classes 3 and 4 both resolve to "print the line once" regardless of file count or mix, and class 4 (an empty `workflows/` directory) explicitly still prints — the directory's existence is the trigger, not its contents; a file-walk would also cross into the Non-goals' declined "workflow paths engineered against a file walk" surface for no behavioral gain.
  grounding: `.shell-team/specs/T-1133-ci-parity-verification.md` `## Input space` items 3-4; `## Non-goals` ("No formatter execution... the team-init fixture stops at workflow-directory detection").

- decision: chose the fixed prefix `This repository has .github/workflows` for the new team-init line (rather than, e.g., leading with the base-dir interpolation) specifically so the negative fixture arm can assert its absence with a single `grep -qF` over the whole prefix without also matching the unrelated, always-printed `.github/workflows`-adjacent doc-pointer lines in the same summary block.
  reason: AC4 requires the negative arm to assert absence of "the new line's fixed prefix" in captured stdout; none of the block's other lines (recipe pointer, binding specimen, re-include hint) contain this literal string, so the prefix is unambiguous and collision-free against every other line team-init already prints.
  grounding: `bin/team-init.sh`'s summary block (lines ~374-397, no other line contains the substring `This repository has .github/workflows`); `.shell-team/specs/T-1133-ci-parity-verification.md` AC4 ("it asserts the absence of the new line's fixed prefix in that run's captured stdout").

<!-- END provenance: T-1133 -->
