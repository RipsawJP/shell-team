<!-- BEGIN provenance: T-1133 -->

- decision: wrote the qa-verifier Rule as a single conditional sentence whose true branch (`.github/workflows` present) states what to run, what a non-zero exit means, and what to do when a step cannot run locally, and whose false branch is the file's own `keep the prior behavior unchanged` idiom, rather than splitting the norm across two bullets or a bulleted sub-list.
  reason: AC1 requires all five literals (`.github/workflows`, `a non-zero exit is a FAIL of the round`, `never a silent skip`, `the version the workflow pins`, `keep the prior behavior unchanged`) on **one physical line** beginning at column 0 with `- **`, matching the file's own Rule shape; a second bullet would put at least one literal outside that line and fail the check's single-hit assertion.
  grounding: `.shell-team/specs/T-1133-ci-parity-verification.md` AC1's check line (`grep -nF ... | grep -F ... | ... > "$T/hit"; test "$(awk 'END{print NR}' "$T/hit")" -eq 1`); `## Notes for engineer` ("do not split the norm across two bullets").

- decision: gave the docs section its own `## ` heading in both `docs/adopting.md` and `docs/adopting.ja.md` (`## Both gates green and your own CI` / `## 両ゲート green と自分のリポジトリの CI`), rather than appending a paragraph to the preceding `## Declaring adopter-facing documentation` section.
  reason: the preceding section is an unrelated subject (the `- user-visible:`/`- adopter-surface:` declaration duty), so a trailing paragraph would silently attach the new content to it; a dedicated heading also keeps the EN/JA `## ` heading count symmetric and machine-checkable (both files go from 18 to 19), which AC5 asserts as a base-relative delta.
  grounding: `.shell-team/specs/T-1133-ci-parity-verification.md` `## Notes for engineer` ("Placement reason for the new documentation heading... the paragraph gets its own `## ` heading rather than being appended to the preceding section... A heading also keeps the EN/JA mirror measurable"); AC5's check (`HE -eq HJ` and each `-eq` its own base count `+1`).

- decision: implemented the team-init summary line's trigger as a directory-existence test against `$TARGET/.github/workflows` only (no enumeration of files inside it, no distinction between `.yml`/`.yaml`, no check for an empty workflows directory).
  reason: the spec's `## Input space` classes 3 and 4 both resolve to "print the line once" regardless of file count or mix, and class 4 (an empty `workflows/` directory) explicitly still prints — the directory's existence is the trigger, not its contents; a file-walk would also cross into the Non-goals' declined "workflow paths engineered against a file walk" surface for no behavioral gain.
  grounding: `.shell-team/specs/T-1133-ci-parity-verification.md` `## Input space` items 3-4; `## Non-goals` ("No formatter execution... the team-init fixture stops at workflow-directory detection").

- decision: chose the fixed prefix `This repository has .github/workflows` for the new team-init line (rather than, e.g., leading with the base-dir interpolation) specifically so the negative fixture arm can assert its absence with a single `grep -qF` over the whole prefix without also matching the unrelated, always-printed `.github/workflows`-adjacent doc-pointer lines in the same summary block.
  reason: AC4 requires the negative arm to assert absence of "the new line's fixed prefix" in captured stdout; none of the block's other lines (recipe pointer, binding specimen, re-include hint) contain this literal string, so the prefix is unambiguous and collision-free against every other line team-init already prints.
  grounding: `bin/team-init.sh`'s summary block (lines ~374-397, no other line contains the substring `This repository has .github/workflows`); `.shell-team/specs/T-1133-ci-parity-verification.md` AC4 ("it asserts the absence of the new line's fixed prefix in that run's captured stdout").

- decision: rewrote both `docs/adopting.md`'s and `docs/adopting.ja.md`'s new section bodies (heading, placement and the two locked tokens unchanged) to state that QA's CI-parity coverage is unconditional on the recipe — QA reads the workflow files directly, and the `## CI parity` section is a record filled in by the engineer or QA on first use, not a precondition QA waits on — after the coordinating session flagged the first draft's "unless the recipe names those commands" / "until it is filled in, QA's gate does not reach your CI at all" wording as contradicting the qa-verifier Rule's own trigger (`.github/workflows` existing) and the spec's Input space class 9.
  reason: the qa-verifier Rule (agents/qa-verifier.md) fires on `.github/workflows` existing and has QA fill the recipe section in itself when empty — the recipe is downstream of the Rule's own execution, not a gate the Rule waits on; the spec's Input space class 9 states this in terms: "the QA Rule still applies, because it reads workflows and not the recipe." The corrected wording also checked `templates/test-recipe.md`'s own `## CI parity` comment for the same defect and found none — it already reads as a record ("QA runs these commands locally... so this list stays authoritative") with no precondition framing, so no change was needed there.
  grounding: `agents/qa-verifier.md`'s CI-parity Rule line ("run them locally... reading the exact commands from the repo's own test recipe's `## CI parity` section (fill that section in yourself first if it is still empty...)"); `.shell-team/specs/T-1133-ci-parity-verification.md` `## Input space` item 9 ("the QA Rule still applies, because it reads workflows and not the recipe; the section arrives through the existing append-back duty on the next task").

- decision: rework round 1 (review record `.shell-team/reviews/T-1133.md` @ `8dd9959`, same-class-2 on `norm-text-coverage-gap`) — reworded the qa-verifier Rule to say "if that section is missing or still empty ... derive the commands from the workflow files yourself and write the section in" (covering both a wholly absent `## CI parity` heading and an existing-but-unfilled one), trimmed the engineer.md sentence back to "name it and fill it in on first use" with no run-before-`READY_FOR_QA` duty, added a fourth `Static-analysis commands:` field to `templates/test-recipe.md` and moved `## CI parity` before `## Appended by tasks`, tightened the team-init positive fixture arm to an exact-count assertion, and aligned both docs sections' recipe-fields sentence from `lint/format commands` to `lint/format/static commands` to match the broadened field set — rather than fixing only the three literally-reported Major spots.
  reason: the trigger was same-class-2 (two independent findings — the Rule's condition and the template's field set — each a scope stated but not fully covered by the adjacent text), which routes to a full inventory of the seven shipped files rather than a spot fix; grepping all seven for the scope vocabulary (`lint/format/static`, `.github/workflows`, the recipe section as a record, "cannot run locally") found: qa-verifier.md (fixed — missing-section case now explicit), engineer.md (fixed — trimmed to the frozen Goal's "names the section" scope and the field list widened to match the template), templates/test-recipe.md (fixed — static field added, section reordered so the append-only log stays at end-of-file), bin/team-init.sh (not-apply — a one-line signpost, not a scope-bearing norm statement, no carve-out expected), tests/team-init/run.sh (fixed for Minor 5's exact-count gap; otherwise fixture code, not norm text), docs/adopting.md and docs/adopting.ja.md (fixed — the recipe-fields sentence under-named the scope after the template change, aligned to match).
  grounding: `.shell-team/reviews/T-1133.md` `#### Major` items 1-3 and `#### Minor / nits` items 1-2 (the two fast-follow-eligible ones folded into this round per the orchestrator's instruction); `.shell-team/specs/T-1133-ci-parity-verification.md` `## Input space` item 9 ("an adopter repository scaffolded before this task, whose recipe has no `## CI parity` section: the QA Rule still applies"); `## Body-to-AC correspondence` row 6 ("The engineer's append-back duty names the new recipe section → AC2") as the scope AC2 actually authorizes.

<!-- END provenance: T-1133 -->
