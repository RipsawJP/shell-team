<!-- BEGIN provenance: T-1110 -->
- decision: place the new "Freeze-time version-derivation gate" bullet in `skills/run/SKILL.md`'s freeze-sweep branch immediately after the T-1103 Oversight-profile approval gate (structurally the last of the six pre-existing gate bullets in that branch), rather than among the five first-freeze-only gates earlier in the same branch.
  reason: the new gate shares T-1103's every-freeze scope (first freeze and every re-freeze, including class-M) rather than the five first-freeze-only gates' scope, and the spec's own Goal sentence directs it to borrow T-1103's every-freeze sentence verbatim and cite T-1103 by name; placing it as T-1103's structural neighbor keeps every-freeze-scoped gates grouped together in the file and makes the borrowed-sentence citation read naturally in context, while AC1/AC2's own checks are purely positional (between `Frozen-intent confirmation` and `Skip this step entirely...`) and impose no other ordering constraint.
  grounding: `.shell-team/specs/T-1110-freeze-version-derivation.md` Goal sentence ("scope, and it is opposite to the five gates above... in **scope** to the T-1103 gate whose every-freeze sentence it borrows verbatim"); AC2's check line (asserts `T-1103` citation and the borrowed scope sentence, with no position constraint beyond AC1's between-anchors requirement).

- decision: record the adopter-degradation outcome in `skills/run/SKILL.md`'s new gate bullet as `verdict=no-premise-on-record` (a value of the `verdict=` field), not as a value written into the `premise=` field.
  reason: the spec's Goal sentence and `## This task's own version derivation` both state the record's closed `verdict=` field carries the enum `match|mismatch|no-premise-on-record`, and AC13's regex on this task's own dogfooded board record confirms `verdict=` (not `premise=`) is where that literal value lands; writing the degradation case into `premise=` instead would contradict the record grammar the same bullet defines two sentences earlier and would be internally inconsistent with the task's own freeze-time record already committed to the board.
  grounding: `.shell-team/specs/T-1110-freeze-version-derivation.md` AC7's check line (`verdict=<match|mismatch|no-premise-on-record>` as one of the four closed-vocabulary literals, distinct from `premise=<expected-tier + the ground it was approved on>`); AC13's regex (`verdict=(match|mismatch|no-premise-on-record)`); `.shell-team/todo.md`'s existing T-1110 `- version-derivation (v1, 2026-08-31):` line (`verdict=match — … — premise=sprint recurrence-mechanization …`), written by the freeze run before implementation began.

- decision: do not name issue #402 (the deferred `bin/check-version-derivation.sh` follow-up issue, per the task instructions) inside the `skills/run/SKILL.md` gate bullet or the adopter-facing docs sections; describe it only as "filed as a follow-up issue... in the same #250 pattern the gates above already use", matching the spec's own Non-goals wording.
  reason: this engineer role holds no network/`gh` access and cannot independently verify issue #402's existence, number or state first-hand; the spec's own Non-goals section and AC9/AC12's negative checks require only that no checker or relay-checker path was built and that the arm is deferred — none require the issue number to appear in shipped prose, and the five sibling gates already establish the convention of citing "issue #250" as the pattern rather than re-deriving a fresh number per gate. The specific issue number is reported instead in this task's hand-off, where it is explicitly labelled as relayed from the coordinating session (AC15(g)).
  grounding: `.shell-team/specs/T-1110-freeze-version-derivation.md` `## Non-goals` first bullet ("The checker arm is filed as a follow-up issue by the coordinating session, in the same #250 pattern the five sibling declaration gates already use"); AC15(g) ("confirmation that the follow-up issue... was filed, with its number, or an explicit statement that it was not and why" — a reporting-only, `SKIP`-by-design item, not a `- check:`-gated requirement on shipped prose).

- decision: when a required literal phrase (e.g. `no mechanical checker ships for it yet`, `not a fourth human gate`) would otherwise fall across a hard-wrap line break in `docs/adopting.md` / `docs/adopting.ja.md`'s prose, join that specific sentence onto one physical line rather than reflowing the whole paragraph or shortening the phrase.
  reason: AC10's check line extracts the section with `awk` and greps each required token with `grep -qF` per line — a token split across two physical lines by an ordinary hard-wrap is invisible to a single-line literal `grep`, even though the words are correct and read naturally to a human; the minimal fix that preserves both the exact literal and the surrounding prose's existing wrap style is to keep only the load-bearing sentence unwrapped, discovered by re-running the actual extraction-plus-grep rather than eyeballing the rendered file (recorded as a general trap in `.shell-team/test-recipe.md`'s new T-1110 entry for future docs edits to these two files).
  grounding: `.shell-team/specs/T-1110-freeze-version-derivation.md` AC10's check line (`grep -qF -- "$L" "$f"` per required token against the `awk`-extracted section, one physical line at a time); live reproduction this round — `grep -qF -- 'no mechanical checker ships for it yet' docs/adopting.ja.md` failed against the first hard-wrapped draft and passed after joining the sentence onto one line, confirmed by re-running the identical extraction command.
<!-- END provenance: T-1110 -->
