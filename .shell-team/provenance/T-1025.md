<!-- BEGIN provenance: T-1025 -->

- decision: T-1019's provenance entry 1 (its "THREE separate assertions" description) is corrected by this record — T-1025's own decision/reason/grounding triple below — rather than by editing that entry in place, and no supersede note is appended to it either.
  reason: two structurally available ways to record the correction existed: (a) edit T-1019's provenance entry 1 directly, appending a supersede note pointing at this task; or (b) leave that file byte-identical and record the correction as a new triple in this task's own provenance file, grounded on the entry being corrected. (a) is not merely undesirable, it is schema-impossible: `bin/check-provenance.sh`'s region grammar admits only well-formed `- decision:`/`reason:`/`grounding:` triples (or the zero-decision sentinel) inside the marker pair — a free-form annotation line is an unrecognized non-blank line and fails the schema check. (b) was chosen because it is the only option the schema permits, and because T-1019's own spec Goal already said each class is "asserted twice" — the "three checks" language was implementation-side drift (the suite's header/doc comments and this one provenance entry), not a spec defect, so correcting the spec or the entry itself would misplace the fix.
  grounding: `.shell-team/specs/T-1025-assert-parity-dead-comparison.md` D2 ("T-1019's provenance entry 1 is NOT edited, and no supersede note is added to it... a free annotation line there is a structural error... The correction is recorded instead as T-1025's own decision/reason/grounding triple in .shell-team/provenance/T-1025.md, whose grounding: cites .shell-team/provenance/T-1019.md"); `.shell-team/provenance/T-1019.md` (decision entry 1, "structures each of the six classes as THREE separate assertions" — the description that entry's implementation-side language now describes a structure `tests/is-span-row-parity/run.sh` no longer has, since this task deletes the third, cross-consumer comparison that entry described as dead code under fail-fast: two assertions against the same frozen value already imply the third would hold, by transitivity).

<!-- END provenance: T-1025 -->
