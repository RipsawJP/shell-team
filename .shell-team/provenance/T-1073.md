# T-1073 decision provenance

Prose context for the triples below (outside the marker region, not part of the
checker's schema): this task assembles a note from probe evidence the orchestrator
already committed and could not be re-run by this role (no Agent tool). The
non-trivial decisions below are about how to fill genuine gaps between the frozen
check grammar and the evidence's own real format/values, honestly, without
fabricating anything the probe did not actually produce.

<!-- BEGIN provenance: T-1073 -->
- decision: declared `unit-duration` as the frozen, pre-arm candidate measurement (147968 ms, `T-1044-test-infra-bundle`'s single-run timing) rather than any post-hoc observed per-agent duration (which ranged 161352–270065 ms across the real arms).
  reason: the frozen protocol's own margin-factor/stall-threshold preconditions are about the population-fixation decision made *before* any arm ran ("Population decision, frozen before the arms" in the committed evidence) — using a post-hoc observed value would silently substitute a different, unfrozen quantity for the one the protocol's own frozen condition actually governs, and the pre-arm value is the one explicitly and unambiguously named in the evidence's own prose.
  grounding: docs/loop-engineering/harness-agent-concurrency.md, `## Probe evidence (raw, orchestrator-produced)` → "Venue and population fixation (before any arm ran)"; `.shell-team/specs/T-1073-harness-agent-concurrency.md`'s frozen `probe-protocol` region, "Unit" paragraph.
- decision: declared `- stall-threshold: 300000` (the healthy-arm watchdog's 5-minute static threshold) as the note's single required field, rather than the negative control's own separately-sized 2-minute (120000 ms) threshold, and disclosed the resulting AC8 ordering-invariant failure explicitly rather than picking whichever value made the check pass or inventing a third value neither watchdog actually used.
  reason: no single real, actually-configured threshold value satisfies AC8's own `control-sleep(200000) > stall-threshold > unit-duration(147968)` ordering given the real values this probe used (the healthy value 300000 sits above that window, the negative-control value 120000 sits below it) — this is a genuine execution-detail deviation (two thresholds by design, not one), not a data-entry error, and reporting the healthy-arm value (which governs the four never-dropped-or-width arms whose evidence the verdict rests on) while disclosing the tension in full is more honest than fabricating a value that was never run, which would make the check pass while misrepresenting the probe.
  grounding: docs/loop-engineering/harness-agent-concurrency.md, `## Overlap analysis` → "Honest finding — the frozen ordering invariant ... does not hold"; `## Probe evidence (raw, orchestrator-produced)` → "Watchdog machinery" (the two distinct invocation shapes, `5 20 5` vs `2 10 2`).
- decision: reported AC4's own `- command:`-line-count sub-clause as a genuine, unfixable FAIL (the frozen, byte-locked evidence section carries zero lines matching `^- command: `) rather than adding `- command:`-labelled restatements of the evidence's own content anywhere inside that frozen section, or arguing the requirement is satisfied by `- command:` lines placed in this engineer's own later sections.
  reason: AC2 locks the evidence section byte-identical to the probe-evidence commit, and AC4's own check extracts only the text between the evidence heading and the next `## ` heading — a byte the engineer cannot touch cannot be made to satisfy a criterion that reads only that byte range; disclosing this precisely (in the note's own Limits section, in this file, and in the hand-off) lets QA and the reviewer see the exact, narrow, load-bearing gap rather than a silent workaround that would misrepresent conformance.
  grounding: docs/loop-engineering/harness-agent-concurrency.md, `## Limits and what is not computable` → "Two check-grammar gaps this note's evidence cannot close"; `.shell-team/specs/T-1073-harness-agent-concurrency.md` **AC4**'s `- check:` line (the `awk` extraction bounded to the evidence heading).
- decision: chose the maximum observed single-agent launch latency (32510 ms, `width-n4 probe-d`) as the single `- launch-latency:` field, rather than the mean (≈22.2 s) or the minimum (≈16.1 s).
  reason: the frozen margin-factor floor exists to bound the verdict against the worst-case noise floor, not the average case — using the maximum is the conservative choice that keeps the margin-factor check honest against every launch this probe actually observed, and the note discloses the full range and the mean alongside it so a reader is not misled into thinking 32510 ms is typical.
  grounding: docs/loop-engineering/harness-agent-concurrency.md, `## Overlap analysis` → "Launch latency" and the timing-preconditions paragraph immediately after it.
- decision: performed the mutation self-check (AC16(a)) on a plain `cp -R` directory copy under `$TMPDIR` rather than a `git worktree add --detach` scratch, departing from the T-1070/T-1071 precedent's own stated method.
  reason: this session's own `.git/worktrees/` already carried two stale, un-prunable entries left over from other tasks (`t1061-blast-worktree`, `t1070-base`), confirmed live (`git worktree prune` could not remove them this session); adding a third such registration risked leaving another one behind with no reliable removal path, whereas a plain directory copy needs no worktree registration at all and is trivially removed with `rm -rf` once the mutation probes finish.
  grounding: docs/loop-engineering/harness-agent-concurrency.md, `## AC16 — runtime, reported item by item` → "(a) Mutation self-check" (the worktree-avoidance paragraph, naming both stale entries by name).
<!-- END provenance: T-1073 -->
