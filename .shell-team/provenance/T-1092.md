# T-1092 decision provenance

<!-- BEGIN provenance: T-1092 -->
- decision: in `docs/loop-engineering/specify-seam-review.md`'s `cross-provider` dispatch-rule line, the ground field is written `trigger: domain-premise — <explanation> — cost-input: t1092-domain-premise-count` (situation text first, the priced-line id embedded at the end of the same field) rather than opening the field with the priced-line id first.
  reason: T-1091's own provenance record (`.shell-team/provenance/T-1091.md`, decision 1) documents that its spec's AC1 and AC2 froze two requirements on the analogous `operator-authored` rule line that are jointly unsatisfiable under a price-first ordering — AC1 requires the conditional rule's ground field to open with the literal `trigger: `, while AC2's per-rule ground-resolution loop extracts the ground's trailing priced-line id via a greedy pattern that tolerates free text before it. This task's own AC1 (`grep -cE '^- dispatch-rule: spec-review — cross-provider — conditional — trigger: .'`) and AC2 (the `sed -nE 's/^- dispatch-rule: spec-review — [a-z0-9-]+ — [a-z]+ — .*(saving|recommendation|break-even|cost-input): ([a-z0-9-]+).*$/\1 \2/p'` extraction) carry the identical shape, so the trigger-first ordering was chosen from the start rather than discovered by trial. Both checks were run live against the finished note (`AC1 rc=0`, `AC2 rc=0`) to confirm the ordering is jointly satisfiable here, closing the class T-1091 hit rather than re-litigating it.
  grounding: `.shell-team/provenance/T-1091.md` decision 1 (the AC1/AC2 conflict it hit and its resolution reasoning); `.shell-team/specs/T-1092-specify-seam-review.md` **AC1**'s and **AC2**'s own `- check:` lines; live reproduction under `$TMPDIR` (`ac1check.sh` / `ac2check.sh`) against the finished `docs/loop-engineering/specify-seam-review.md`, both exiting 0.
- decision: the teeth gate's fixtures in `tests/close-out/run.sh` are exercised through a dedicated `spec_review_case` helper rather than a seventh optional parameter on the existing `dispatch_case` helper; `dispatch_case` itself gains only an unconditional `TEAM_REVIEWS_DIR` export (harmless for every pre-existing call site, none of which elects `spec-review — cross-provider`).
  reason: the spec's own `## Notes for engineer` explicitly offers both shapes as equally acceptable ("a seventh optional parameter defaulting to a no-record directory, or a thin sibling helper"), the same choice-with-either-answer T-1091 faced for `dispatch_case`'s label parameter (recorded in `.shell-team/provenance/T-1091.md` decision 3). Unlike that case, the teeth cases need a materially different assertion (a different refusal sentinel, `elected spec review has no APPROVE verdict`, versus `dispatch_case`'s `malformed dispatch record`, plus a review-record fixture body `dispatch_case` has no parameter for) — overloading `dispatch_case` with a seventh parameter plus a sentinel-selection branch would have made one helper serve two materially different assertion contracts, so a dedicated helper reads more honestly than a parameterization here, in contrast to the pure label pass-through T-1091 chose a shared parameter for.
  grounding: `.shell-team/specs/T-1092-specify-seam-review.md` `## Notes for engineer` ("Add the reviews redirection in whichever way keeps every pre-existing call site untouched... the same choice T-1091 faced with the label parameter"); `.shell-team/provenance/T-1091.md` decision 3 (the precedent choice and its reasoning); `tests/close-out/run.sh`'s `spec_review_case` function definition and its five `T-99501`–`T-99505` call sites, all independently PASS-ing against the real `bin/close-out.sh` (`bash tests/close-out/run.sh`, `grep -c '^PASS: T-1092' <log>` = 12).
- decision: the fixture task ids added to `tests/close-out/run.sh` for this task's cases (`T-99501`–`T-99505`, `T-99510`–`T-99516`) use a five-digit range rather than continuing the existing `T-995`–`T-999`-style three/four-digit sequence T-1084's and T-1091's own fixtures occupy.
  reason: this repository's `CLAUDE.md` `## Task IDs` reserves `T-000`–`T-999` as the fixture range and `T-1000`–onward as live task ids; the existing `dispatch_case` fixtures already occupy `T-970`–`T-994`, leaving little headroom before colliding with either the fixture ceiling or a live task id in the `T-1000`s (where this very task, T-1092, and its neighbors live). A five-digit range is unambiguously outside both bands, so no fixture id can ever be mistaken for — or later collide with — a real task id as the board's live id space grows past `T-1099`.
  grounding: `CLAUDE.md` `## Task IDs` ("`T-000`–`T-999` is also the fixture range... New task IDs start at `T-1000`"); `tests/close-out/run.sh`'s pre-existing `dispatch_case T-970`..`T-994` call sites (unmodified by this diff); the live board's own task ids in the `T-1000`s range (`.shell-team/todo.md`).
- decision: `agents/codex-reviewer.md`'s board-and-write-boundary Rule and canonical-naming Rule are edited to add the third capture stem `T-XXX-codex-specreview.{txt,jsonl}` to their existing closed lists, rather than leaving those two Rules unedited and relying only on the new mode section's own prose to state the closed write surface.
  reason: the routing map's D5 deliverable explicitly names both rules as landing sites ("a third capture stem `T-XXX-codex-specreview` added to the closed write-path list in the board-and-write-boundary Rule (`:161`) and to the canonical-naming Rule (`:167`)"), and the write-boundary Rule's own stated purpose is to be the closed enumeration of what this role may write — leaving a real new write path (this mode's own raw/canonical files) out of that enumeration while only describing it in a different section would make the Rule's own "write these paths and no others" claim inaccurate the moment the new mode is used, independent of whether AC9's own machine check happened to be satisfied by fewer edits.
  grounding: `.shell-team/runs/t1092-routing-map.md` D5 ("a third capture stem ... added to the closed write-path list in the board-and-write-boundary Rule (`:161`) and to the canonical-naming Rule (`:167`)"); `agents/codex-reviewer.md`'s board-and-write-boundary Rule and canonical-naming Rule as edited; **AC9**'s own check line (verified live, `AC9 rc=0`).
- decision: D1's note prices its `cross-provider` rule's ground with a `- cost-input:` line measuring the count of `**relayed**` premise lines in this task's own frozen spec commit (`c23743d`), mirroring T-1091's `t1091-relay-count` proxy shape, rather than pricing it with the `rounds-to-approve-series` figure `docs/loop-engineering/spec-authorship-entry.md` already carries.
  reason: the spec's own `## Notes for engineer` names both as available honest proxies ("a count derived from a git-tracked artefact... and the recorded rounds-to-approve series this repository already carries"), but reusing the existing `rounds-to-approve-series` `- cost-input:` line verbatim across two different notes would let one axis's rule cite a price line that is not "this note's own" priced line in the sense **AC2**'s per-rule ground-resolution loop requires (it asserts the note itself carries a line opening `- <label>: <id> — ` for the id the rule's ground names) — a fresh, note-local proxy avoids relying on a second note's line to close this note's own ground-citation requirement, and a relay-count measured against this task's own spec is the more directly relevant proxy for "how many decision inputs a spec relays rather than measures first-hand" in any case.
  grounding: `.shell-team/specs/T-1092-specify-seam-review.md` `## Notes for engineer` ("Two honest cost inputs are available without running anything: a count derived from a git-tracked artefact... and the recorded rounds-to-approve series this repository already carries"); **AC2**'s own per-rule ground-resolution loop (requires the ground's id to resolve to a `- <label>: <id> — ` line "this same note" carries); `docs/loop-engineering/spec-authorship-entry.md`'s own `t1091-relay-count` priced line (the shape mirrored); live measurement `git show c23743d:.shell-team/specs/T-1092-specify-seam-review.md | grep -c '^- \*\*relayed'` = 8.

- decision: rework round 1 (review Blocker) — `agents/codex-reviewer.md`'s spec-review-mode Route-back paragraph is rewritten to resolve the answering author from `pm-spec`'s own `- entry-mode:` hand-off field, cross-checked against the `specify` decision `tech-lead`'s Routing Map printed at Plan (a mismatch in either direction refuses and, where the two texts can't say which is wrong, escalates to the human) — rather than either (a) leaving the board's `- dispatch: specify — …` sub-bullet as the resolution source with an added cross-check bolted on, or (b) inventing a new, third condition-source design distinct from the one `skills/run/SKILL.md`'s Conformance-read confirmation gate (T-1091) and Specify-seam spec-review gate (T-1092, same file) already use for the identical timing problem.
  reason: the review's own Blocker traced the defect to a timing fact this repository's own board data demonstrates unconditionally — every sampled entry's `- dispatch:` sub-bullets are transcribed strictly *after* the `- intent-hash` line is recorded, while this mode fires strictly *before* the intent-hash is recorded — so option (a) (patch the board-keyed design in place) cannot be made correct by adding a cross-check alone: the named condition source is provably absent at the one point the mode ever runs, cross-checked or not. `skills/run/SKILL.md` already carries a proven, twice-reviewed condition-source design for this exact seam (`- entry-mode:` primary source, Routing Map cross-check, fail-closed on absence/mismatch, escalate-to-human on indistinguishability) that survived T-1091's own five review rounds, three of which specifically attacked this class (order-dependence, then no-cross-check). Reusing that design rather than inventing a fourth (option b) means the Route-back paragraph inherits an already-adversarially-tested shape instead of adding a fresh, unreviewed condition-source design for the reviewer to attack in a future round.
  grounding: `.shell-team/reviews/T-1092.md` Blocker text and Suggested fix ("resolve route-back the same way the Specify-seam gate itself resolves `spec-review`'s own election — from `pm-spec`'s hand-off... cross-checked against the Routing Map's printed decision — never from the board"); `skills/run/SKILL.md`'s Conformance-read confirmation gate (T-1091) and Specify-seam spec-review gate (T-1092) bullets, both already stating "never the board" for the identical reason; `.shell-team/todo.md`'s T-1091 entry, Round 2–5 review history (the order-dependence and no-cross-check defeats this reused design already survived); direct repository sweep this round (`git diff b49d29e...HEAD -- <every touched doc/skill/agent file> | grep -n '^+' | grep -iE "recorded on the task.s board entry"` → 0 hits at final state, confirming no other site in this task's own diff still names the board as a pre-intent-hash condition source).
- decision: rework round 1 (review Majors 2+3) — `bin/close-out.sh`'s teeth-gate verdict match is rewritten to (1) anchor on end-of-line immediately after the literal `APPROVE` token (`APPROVE[[:space:]]*$`) and (2) scope the scan to the text between the LAST `^## Spec review$` heading in the review record and the next `^## ` heading (or EOF) — falling back to a whole-file scan only when the file carries no `## Spec review` heading at all (the shape AC6's own frozen fixtures already exercise, a bare verdict line with no wrapping section) — rather than either leaving the match unanchored with only a length/character-class guard bolted on, or requiring every synthesized review record to carry a `## Spec review` heading (which would break AC6's own frozen check line, itself inside the intent-block markers and out of this round's edit authority).
  reason: the review's two Majors are independent defects on the same line — an unanchored prefix match (accepts `APPROVE_WITH_CAVEATS` or the mode's own unfilled output template `APPROVE | REQUEST_CHANGES`) and an unscoped whole-file scan (a stale earlier-round APPROVE survives alongside a later REQUEST_CHANGES) — and both needed independent fixes rather than one fix papering over the other (an anchor alone does not stop a stale earlier APPROVE from matching; a section scope alone does not stop a near-miss verdict value inside the correct section from matching). The whole-file fallback for the no-heading case is required rather than optional: AC6's own five fixtures are frozen inside this spec's intent block and one of them (`T-99501`, the "elected + APPROVE record passes" positive control) synthesizes a bare `### Codex Spec-Review verdict: APPROVE` line with no `## Spec review` wrapper at all — a design that unconditionally required the heading would turn that frozen, must-stay-green fixture red, which is out of this round's authority to change.
  grounding: `.shell-team/reviews/T-1092.md` Major findings 3 and 4 (`bin/close-out.sh:337`) and their Suggested fixes; `.shell-team/specs/T-1092-specify-seam-review.md` **AC6**'s own frozen `- check:` line (the five fixture bodies, including `T-99501`'s heading-less `### Codex Spec-Review verdict: APPROVE` body); `tests/close-out/run.sh`'s new `T-99506`–`T-99509` fixtures. Isolated on two scratch worktrees outside this checkout (`git worktree add --detach` at `/tmp/claude-502/t1092-mutwt-parent/mut` and `.../mut2`, both removed after): an anchoring-only mutation (revert requirement 1 alone, keep requirement 2) flips **T-99506** and, run separately, **T-99507** each from PASS to FAIL with the exact expected-exit-1-got-0 message, restored to PASS after reverting the mutation; a scoping-only mutation (revert requirement 2 alone, keep requirement 1) flips **T-99508** from PASS to FAIL the same way, restored to PASS after reverting; **T-99509** (the positive control) was independently confirmed to stay PASS under both the fixed code and the scoping-reverted mutation, matching what a scope-independent control should do (its own final-round verdict is APPROVE either way, so scoping never gates it) — the mutation that must flip it is a defect in requirement 1, not requirement 2, and no such flip was asserted or found here.

- decision: rework round 3 (review round 2, Blocker 1) — the section-boundary fix normalizes the heading SEARCH by trimming trailing whitespace before the equality test against the literal `## Spec review` (so `## Spec review ` / `## Spec review<TAB>` still count as a genuine round heading), while leaving the boundary check's own loose `/^## /` match unchanged — rather than the alternative considered and rejected: making BOTH the search and the boundary use the same loose `/^## /` test (i.e. "the last top-level heading of any kind, extract to EOF"), which would be simpler but is unsafe against this record's own realistic shape.
  reason: this task's own design states the spec-review mode always fires BEFORE any implementation exists, so by the time close-out runs (after the whole loop, including the default review mode's own later rounds, has completed) a real review record can legitimately carry a `## Spec review` section FOLLOWED by unrelated later `## `-prefixed sections (the default review mode's own round headings) that never carry a `### Codex Spec-Review verdict:` line at all. Verified by direct construction: a record shaped `## Spec review` (APPROVE) followed by `## Round 1` (an unrelated section) — under the "last loose match of any kind, extract to EOF" alternative, the scan would anchor to `## Round 1` and find no verdict line there, incorrectly refusing a task whose spec review genuinely, currently stands APPROVED. The trim-normalize-then-exact-compare design avoids this: a heading that is genuinely a DIFFERENT section (not a whitespace-only variant of the canonical text) does not trim-equal `## Spec review` and is correctly excluded from the search, so only trailing-whitespace deviations of the canonical heading — the reviewer's own reproduced and named defect class — gain new coverage, and no other section type is newly and wrongly treated as a spec-review round boundary.
  grounding: `.shell-team/reviews/T-1092.md` `## Round 2` Blocker 1 text and Suggested fix ("make the heading search and the boundary check use the *same* match strictness in a single pass"); `.shell-team/specs/T-1092-specify-seam-review.md`'s Goal paragraph ("the mode fires... after `pm-spec`'s freeze sweep and before the `- intent-hash (v1)` is recorded" — establishing the spec-review section is always chronologically first, with later default-mode rounds appended afterward in the same file, by construction); `bin/close-out.sh`'s new single-awk-pass implementation; `tests/close-out/run.sh`'s `T-99517` fixture (the reviewer's own trailing-space repro) and the pre-existing `T-99501`/`T-99508`/`T-99509` fixtures (all still green, confirming the design does not regress the single-round and well-formed-multi-round cases). Mutation-probed on a scratch worktree outside this checkout (`git worktree add --detach` at `/tmp/claude-502/t1092-mutwt-parent/mut3`, removed after): reverting the trim-normalization alone (search back to a byte-exact match, boundary unchanged) flips `T-99517` from PASS to FAIL, restored to PASS.
- decision: rework round 3 (review round 2, Blocker 2) — the racy `printf '%s\n' "$LATEST_SECTION" | grep -qE '...'` pipe is replaced with a here-string read (`grep -qE '...' <<< "$LATEST_SECTION"`), rather than the reviewer's other suggested option (folding the verdict match into the same `awk` pass that already does the section extraction).
  reason: both options genuinely eliminate the SIGPIPE exposure (neither leaves a separate writer process racing against `grep`'s early exit), but the here-string keeps the extraction and the verdict-match steps as two separately-readable, separately-testable statements — matching this gate's own existing style (every other refusal class in this file is a small, independently-commented `if` block) — rather than merging two distinct concerns (which lines belong to the latest round; does that content contain an exact APPROVE) into one larger `awk` script, which would also have required re-deriving the `[[:space:]]*$` anchor inside `awk`'s own regex dialect rather than reusing the already-reviewed `grep -E` pattern verbatim.
  grounding: `.shell-team/reviews/T-1092.md` `## Round 2` Blocker 2 text and Suggested fix ("replace the `printf | grep -q` pipe with a here-string... or perform the match inside the same `awk` pass... eliminating the inter-process pipe... entirely" — both named as acceptable); `bin/close-out.sh`'s new here-string line; `tests/close-out/run.sh`'s `T-99518` fixture (a ~200KB section with a genuine APPROVE). Mutation-probed on the same scratch worktree (`.../mut3`): reverting the here-string alone (back to the piped form) flips `T-99518` from PASS to FAIL with the exact false-refusal message the review reproduced, restored to PASS.
- decision: one-item scope extension (QA round 3's own probe, operator-approved 2026-08-23) — the heading-search normalization is widened from "trim trailing whitespace only" to "collapse every whitespace run (leading, internal, or trailing) to a single space, then trim the ends" before the equality test against `## Spec review`, rather than the narrower alternative of adding a second, position-specific normalization step just for internal runs (e.g. a separate `gsub` targeting only the space between `##` and the first word) alongside the existing trailing-only trim.
  reason: QA's own probe found that round 3's trailing-only trim left an INTERNAL whitespace variant (`##  Spec review`, a double space between `##` and `Spec`) unrecognized by the search while the boundary's loose `/^## /` match still treated it as a heading — the identical defect shape as the trailing-space case, one whitespace position over. Patching only the reported position (internal-double-space) would leave the class open to a third position (e.g. a tab instead of a space in that same gap, or a run of three spaces) and invite a fourth round on the same component; a single whitespace-normalization pass covering every position closes the whole class in one edit rather than the reported instance, which is the discipline this task's own review history (three consecutive rounds against the same component) makes non-optional here.
  grounding: `.shell-team/interventions/T-1092.md` third entry (QA round 3's probe and the operator's one-item extension approval); `bin/close-out.sh`'s widened `gsub(/[ \t]+/, " ", norm)` + leading/trailing single-space strip, replacing the round-3 `sub(/[ \t]+$/, "", trimmed)`; `tests/close-out/run.sh`'s `T-99519` fixture (QA's own internal-double-space repro) and the pre-existing `T-99517` fixture (still green, confirming the trailing-whitespace case the widened normalization must still cover). Mutation-probed on a scratch worktree outside this checkout (`git worktree add --detach` at `/tmp/claude-502/t1092-mutwt-parent/mut4`, removed after): reverting the widened normalization alone (back to the round-3 trailing-only trim) flips `T-99519` from PASS to FAIL (`expected exit 1, got 0`), restored to PASS.
<!-- END provenance: T-1092 -->
