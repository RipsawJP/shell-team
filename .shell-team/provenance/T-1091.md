# T-1091 decision provenance

<!-- BEGIN provenance: T-1091 -->
- decision: in `docs/loop-engineering/spec-authorship-entry.md`'s `operator-authored` dispatch-rule line, the ground field opens with the literal `trigger: judgment-density — <explanation> — cost-input: t1091-relay-count` (situation text first, priced-line id embedded later in the same field) rather than opening the field with the priced-line id first (`cost-input: t1091-relay-count — trigger: judgment-density — <explanation>`).
  reason: AC1's own frozen check line requires the operator-authored rule line to match the literal prefix `^- dispatch-rule: specify — operator-authored — conditional — trigger: .` (the field must open with `trigger: `), while AC2's own frozen check line extracts the ground's leading priced-line id with a pattern that requires the SAME field to open with one of `saving:`/`recommendation:`/`break-even:`/`cost-input:` immediately after the modality separator. These two frozen, byte-locked requirements are jointly unsatisfiable for a single field on a single line — empirically confirmed by constructing both field orderings and running each check line's literal body against each (see grounding): trigger-first satisfies AC1 and fails AC2's per-rule ground-resolution loop; price-first satisfies that AC2 clause and fails AC1's trigger-prefix clause. Given the frozen Goal prose itself states "the `operator-authored` rule is `conditional` with its **situation field opening with the literal `trigger: `**" — the same requirement AC1's check line encodes — trigger-first is the reading that matches the frozen intent's own prose, not only one of the two check lines; it also lets AC1's much larger surface (rule-per-value closure, modality correctness, the axis's own declared value set, the priced-total equality) pass in full, whereas price-first would only rescue one clause inside AC2's own multi-clause body while still failing AC2 as a whole (a single fixture-line disagreement anywhere in the script drives its accumulated `rc` to 1 regardless of which ordering is chosen). Given the intent block is frozen and this role has no authority to edit a `- check:` line inside it, this is reported as an unresolved AC1/AC2 conflict in the engineer hand-off rather than silently "fixed" by picking one interpretation and hiding the other's failure.
  grounding: `.shell-team/specs/T-1091-operator-authored-entry.md` **AC1**'s own `- check:` line (the `grep -cE '^- dispatch-rule: specify — operator-authored — conditional — trigger: .'` clause) and its prose ("the `operator-authored` rule is `conditional` with its situation field opening with the literal `trigger: `"); **AC2**'s own `- check:` line (the `sed -nE 's/^- dispatch-rule: specify — [a-z0-9-]+ — [a-z]+ — (saving|recommendation|break-even|cost-input): ([a-z0-9-]+).*$/\1 \2/p'` extraction inside its per-rule ground-resolution loop); live reproduction run against both field orderings during this task's implementation (both AC1 and AC2's exact check-line bodies executed against a trigger-first candidate line and a price-first candidate line in a scratch script under `$TMPDIR`, confirming the two checks disagree on which ordering passes — recorded in `.shell-team/test-recipe.md`'s T-1091 entry).
- decision: `docs/loop-engineering/spec-authorship-entry.md`'s two `- cost-input:` priced lines measure (a) the count of `**relayed**` premise lines in this task's own frozen `## Assumptions` section, read from the task's own frozen spec+board-entry commit `6cbd845`, and (b) the total count of `### Codex Review verdict` headings across every `.shell-team/reviews/*.md` file at this task's own branch point `1fca08c313b634d03c4e88e1bf06b6ca4d7a19c6`, rather than inventing a synthetic figure or leaving both fields at `undetermined`.
  reason: the spec's own `## Notes for engineer` names two concrete, honestly-measurable proxies ("the count of decision inputs a hub-and-spoke task's spec would have to relay... and the recorded rounds-to-approve series this repository already carries") and requires every `- cost-input:` line's paired `- command:` to read a named ref's committed blob rather than the working tree (AC2's own body). Both chosen metrics are real, git-tracked, reproducible counts rather than estimates, and both commands were run live to confirm non-zero, sane values (6 and 213 respectively) before being written into the note.
  grounding: `.shell-team/specs/T-1091-operator-authored-entry.md` `## Notes for engineer` ("Two honest cost inputs are available without running anything..."); **AC2**'s own `- check:` line (the `- command:`/`- measured-at:` sub-bullet requirement and the "reads that ref's committed blob" constraint); live commands run this round: `git show 6cbd845:.shell-team/specs/T-1091-operator-authored-entry.md | grep -c '^- \*\*relayed'` (= 6) and `git ls-tree -r --name-only 1fca08c313b634d03c4e88e1bf06b6ca4d7a19c6 -- .shell-team/reviews | grep '\.md$' | while read -r f; do git show 1fca08c313b634d03c4e88e1bf06b6ca4d7a19c6:"$f"; done | grep -c '^### Codex Review verdict'` (= 213).
- decision: `tests/close-out/run.sh`'s `dispatch_case` helper gains a sixth, optional `label` parameter (`local ... label="${6:-T-1084}"`) rather than a separate sibling helper duplicating its body.
  reason: the spec's own `## Notes for engineer` explicitly offers both options as equally acceptable ("Add a label parameter that defaults to the existing value, or a thin sibling helper — either satisfies both"). A single added parameter with a back-compat default changes zero existing call sites' behavior (all eighteen keep emitting `PASS: T-1084 ...`) while a sibling helper would duplicate ~25 lines of board-synthesis, redirection and assertion logic that must stay in lockstep with the original — a duplication this repository's own lessons corpus disfavors when a parameterization achieves the same effect.
  grounding: `.shell-team/specs/T-1091-operator-authored-entry.md` `## Notes for engineer` ("The fixtures need their own label... Add a label parameter... or a thin sibling helper — either satisfies both, and silently reusing the T-1084 label satisfies neither."); `tests/close-out/run.sh`'s `dispatch_case` function definition and its eighteen pre-existing T-1084 call sites (all unmodified in this diff) plus the seven new `label=T-1091` call sites.
- decision: the T-1091 fixtures in `tests/close-out/run.sh` are named `T-988` through `T-994` (pure `T-<digits>` task ids), not `T-1091-01`..`T-1091-07` as first drafted.
  reason: `bin/close-out.sh --task` validates its argument against a strict `T-<digits>` shape and refuses (`usage`, exit 2) any id carrying a trailing non-digit suffix; a hyphen-suffixed fixture id like `T-1091-01` is refused before the dispatch gate under test is ever reached, which was confirmed live (`close-out: invalid --task 'T-1091-01' (expected T-<digits>)`) during this task's own test run. `T-988`..`T-994` sit inside the fixture range this repository's own `CLAUDE.md` reserves (`T-000`–`T-999`) and are unused by any pre-existing `dispatch_case` call (`T-970`..`T-987` were already taken).
  grounding: `bin/close-out.sh`'s `--task` argument validation (the `T-<digits>` usage-error branch); live test run output during this task (`FAIL: T-1091 conformant three-axis record...: expected exit 0, got 2 (stderr: close-out: invalid --task 'T-1091-01' (expected T-<digits>))`); `CLAUDE.md` `## Task IDs` ("`T-000`–`T-999` is also the fixture range used throughout `tests/`").
<!-- END provenance: T-1091 -->
