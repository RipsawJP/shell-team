# T-1067 decision provenance

## Blast radius — read-set derivation (live re-run by this role)

- read-set-specs: 34 specs
  - detail: live re-derivation `git grep -lF -e docs/loop-engineering -e .shell-team/runs -- '.shell-team/specs/*.md' | wc -l` = 34 at report time (includes this task's own spec, which names both literals in its own text).
- indirection-class: `team-paths.sh --get` — a criterion reaching its target through that resolver (`--get todo`, `--get runs`, `--get reviews`, `--get interventions`) rather than a literal path is invisible to the derivation above in principle. Discharge: additionally ran `git grep -lF -e 'team-paths.sh --get' -- '.shell-team/specs/*.md' | wc -l` = 61. Inside the union of both derivations (65 unique files), searched for any other criterion whose subject is a population count over `.shell-team/specs/*.md` (`git grep -n "'.shell-team/specs/\*\.md'"`) or a directory-listing count over `docs/loop-engineering/` (grepping every `- check:` line in the union for `wc -l`/`ls `/`find ` applied to that directory): found no directory-listing criterion (every `docs/loop-engineering` occurrence in the union names one specific file's own content, never a directory listing), and found exactly two population-count-over-specs criteria: `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC8 and `.shell-team/specs/T-1066-effort-time-telemetry.md` AC12, both disclosed below under "Disclosed reds." Whatever a criterion reaches through an indirection neither literal-string derivation can see (a hardcoded path fragment built from string concatenation, for instance) is disclosed here as unmeasured, not claimed clean.

## Disclosed reds (DP11 — measured, differenced, never repaired)

Three classes were predicted in this spec's own `## Blast radius`; all three are confirmed exactly as predicted, and none is repaired (no other spec is in this task's own AC9 allow-list, so none could be edited even if it were tempting to).

- class (i) — a merged criterion whose subject is a population count over `.shell-team/specs/*.md` moves by one when this spec lands:
  - `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC8: declared `read-set-specs: 45 specs`. Live re-derivation of its own stated command (`git grep -lF -e agents/pm-spec.md -e skills/run/SKILL.md -e skills/goal/SKILL.md -- '.shell-team/specs/*.md' | wc -l`) measured in a disposable `git worktree add --detach` at this task's own base ref (`3d72e5b`): `46`. At `HEAD` (this task's diff landed): `47`. `base: FAIL (45 declared vs 46 live) → head: FAIL (45 declared vs 47 live)` — already red at base from T-1066's own merge, and this task's own spec moves it by exactly the predicted +1.
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC12: declared `read-set-specs: 33 specs` (its own provenance record). Live re-derivation of the same read-set-scoped command this task's own Blast radius uses: `34` at `HEAD` (was `33` before this task's spec was added, since that spec is now itself a member of the population). `base: PASS (33 declared = 33 live) → head: FAIL (33 declared vs 34 live)`.
- class (ii) — T-1066's own pre-declared, self-acknowledged staleness:
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC9 (scope lock, merge-point-scoped by its own frozen text): `base: PASS → head: FAIL` — a stray path (this task's own diff) now appears in the changed-and-added set its allow-list does not admit, exactly as its own criterion text anticipates.
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC10 (board-delta, `+1` expected): `base: PASS → head: FAIL` — the board now carries `+2` entries relative to that spec's own base ref (T-1066's own entry plus this task's), not `+1`.
  - `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC10 (its own scope lock, self-declared "merge-point-scoped and expected to go stale after merge"): `base: FAIL → head: FAIL` — already stale before this task (T-1066's own files already violated its allow-list), unaffected in kind by this task's own diff.
- class (iii) — a criterion already red at this branch point for reasons predating this task's own diff, unrelated to the class-(i)/(ii) mechanism:
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC4: draws a live equality between a declared `- population: total-rows = 1298` figure and a fresh re-derivation of the resolved `shell-team.jsonl`, filtered only for rows carrying `t1066`. The corpus is append-only and grows with every task's own telemetry writes on this machine — including this task's own `tech-lead`/`pm-spec` phases, which had already appended rows before this role (`engineer`) started — so this criterion's own self-measurement-exclusion design (excluding only `t1066`-tagged rows, per its own frozen text) cannot stay satisfied once any later task runs the loop at all, with or without a diff landing anywhere. `base: FAIL → head: FAIL` (live total exceeds 1298 at both points; measured live at report time: current unfiltered `shell-team.jsonl` row count is 1351, well past the `1298` this criterion still declares). Attributed to nothing this task's own diff changed — this is the structural fragility DP9 of T-1066's own spec exists to bound, not a defect this task introduces.

## Mutation-probes (the 10 check lines this task adds)

Run in a disposable `git worktree add --detach "$WT" HEAD` (never the working tree), mutating a copy of `docs/loop-engineering/context-lifecycle.md`, running `bash bin/check-acs.sh .shell-team/specs/T-1067-context-lifecycle.md` from inside the worktree, then restoring the pre-mutation baseline before the next probe.

- mutation-probe: AC1 — deleted the `## Cross-PR consistency test` heading line from the scratch copy → `AC1: FAIL (exit 1)`; restored → `AC1: PASS (exit 0)`.
- mutation-probe: AC2 — renamed one `- site: S2-resumed-subagent — ` line so it no longer matches the closed three-label set → `AC2: FAIL (exit 1)`; restored → `AC2: PASS (exit 0)`.
- mutation-probe: AC3 — shortened the exact-lock phrase `no longitudinal growth curve is constructed` to `no growth curve` → `AC3: FAIL (exit 1)`; restored → `AC3: PASS (exit 0)`.
- mutation-probe: AC4 — deleted the `- source-disposition: close-out.jsonl — ...` line → `AC4: FAIL (exit 1)`; restored → `AC4: PASS (exit 0)`.
- mutation-probe: AC5 — appended trailing text (` (final)`) after the `status-quo-carry-everything — recommended` disposition word, violating the ends-at-the-disposition-word grammar → `AC5: FAIL (exit 1)`; restored → `AC5: PASS (exit 0)`.
- mutation-probe: AC6 — duplicated the `- test-verdict: ` line (violating the exactly-once count) → `AC6: FAIL (exit 1)`; restored → `AC6: PASS (exit 0)`.
- mutation-probe: AC7 — shortened the exact-lock phrase `no vendor price model is used` to `no price model` → `AC7: FAIL (exit 1)`; restored → `AC7: PASS (exit 0)`.
- mutation-probe: AC8 — replaced an allowed `git show <ref>:<path>` command form with a disallowed `git diff <ref> -- <path>` (two-argument, non-three-dot) form → `AC8: FAIL (exit 1)`; restored → `AC8: PASS (exit 0)`.
- mutation-probe: AC9 — added an untracked stray file (`stray-file.txt`) at the worktree root, outside the allow-list → `AC9: FAIL (exit 1)`; removed it (and a leftover `.bak` scratch file from the AC1–AC8 probes above, itself confirmed as the actual cause of one initially-surprising restore-and-still-red result — recorded here rather than silently discarded) → `AC9: PASS (exit 0)`.
- mutation-probe: AC10 — deleted the top-level `- [ ] **T-1067**` board line from the scratch worktree's resolved board (leaving the delta at `+0` instead of `+1`) → `AC10: FAIL (exit 1)`; restored → `AC10: PASS (exit 0)`.

## Changed-input CI steps (individually, `.github/workflows/check-handoff.yml`)

This task's diff touches exactly: one new file under `docs/loop-engineering/`, this task's own spec, the resolved board, and this task's own provenance/interventions/reviews records. No path under `bin/`, `tests/`, `templates/`, `.github/`, `agents/`, `skills/`, or any other `docs/` file changes.

- ci-step: Checkout — not-applicable — infrastructure step, not a repository-content check.
- ci-step: Install shellcheck (pinned — must match local dev version) — not-applicable — tooling-install step.
- ci-step: shellcheck — not-applicable — no path under `bin/` or `tests/` changes.
- ci-step: Lint the shipped board template (hand-off linter) — not-applicable — targets `templates/todo-template.md`, untouched.
- ci-step: Dogfood check-handoff — this repository's own board — applicable
  - detail: this task edits the resolved board. Run live: `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` exits 0 (see hand-off for the run against the final board state).
- ci-step: Run check-handoff fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Lint the shipped shell-team loop contract — not-applicable — untouched.
- ci-step: Lint the shipped generic loop-contract template — not-applicable — untouched.
- ci-step: Lint the shipped goal loop contract — not-applicable — untouched.
- ci-step: Run check-contract fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run loop-guard fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-run fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run log-run resolution suite — not-applicable — fixture-only, untouched.
- ci-step: Run team-init fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run discover-work fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-acs fixture suite — not-applicable — fixture-only suite under `tests/check-acs`; this task's own spec is exercised directly via `bash bin/check-acs.sh`, not through this suite.
- ci-step: Run check-design-note fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run goal-state fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rework-digest fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-retro fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs bounded invariants lock — not-applicable — fixture-only, untouched.
- ci-step: Dogfood retro-inputs — the cycle window resolves against this repository — not-applicable — this task adds no retro file and does not touch `.shell-team/retros/`.
- ci-step: Dogfood check-retro — this repository's own retros pass the ledger contract — not-applicable — scans `.shell-team/retros/*.md`; untouched.
- ci-step: Dogfood check-retro — the shipped retro template passes its own contract — not-applicable — `docs/templates/retro-template.md`, untouched.
- ci-step: Run machine-tokens fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-loop-replay fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-readme-version fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-readme-version on the repo's READMEs — not-applicable — `README.md`/`README.ja.md`, untouched.
- ci-step: Run rollup-runs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rollup-track fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run consolidate-proposals fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run cluster-failures fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run is-span-row-parity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run review-gate fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run close-out fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-prompt-sync fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-prompt-sync — this repo's prompt blocks must be in sync — not-applicable — scans `templates/prompt-blocks/registry.txt` and its registered `agents/`/`skills/` consumers; none touched.
- ci-step: Run check-playbook fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-playbook-blocks fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run playbook-promote fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-playbook — the real repository corpus at the resolved lessons path is schema-valid — not-applicable — `.shell-team/lessons.md`, untouched.
- ci-step: Dogfood gen-playbook-blocks — regenerating into a scratch copy reproduces every shipped block and consumer — not-applicable — reads `templates/prompt-blocks/registry.txt` and `.shell-team/lessons.md`, both untouched.
- ci-step: Run team-paths fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood team-paths — a repo with no legacy markers resolves to the default layout — not-applicable — this task's diff does not touch any layout marker.
- ci-step: Run install fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-intent fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-provenance fixture suite — not-applicable — fixture-only suite under `tests/check-provenance`; this task's own provenance file is checked directly via `bash bin/check-provenance.sh .shell-team/provenance/T-1067.md`, outside this suite.
- ci-step: Run check-interventions fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run interventions-reminder fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-interventions — every committed interventions file in this repository is conformant — applicable
  - detail: loops `.shell-team/interventions/*.md`; this task's own `.shell-team/interventions/T-1067.md` (orchestrator-authored, untouched by this role) is a member. Run live: `for f in .shell-team/interventions/*.md; do bash bin/check-interventions.sh "$f" || echo "FAIL $f"; done` — no `FAIL` line printed (see hand-off).
- ci-step: Run check-board-headings fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run errexit-safe regression suite — not-applicable — fixture-only, untouched.
- ci-step: Run codex-skeleton-hygiene suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-pii-shapes fixture suite — not-applicable — fixture-only, untouched.
- ci-step: check-pii-shapes on the PR diff — applicable
  - detail: scans this task's own PR diff. Run live: `bash bin/check-pii-shapes.sh --base "$(git merge-base feature/1066-effort-time-telemetry HEAD)"` → `clean (no PII-shaped bytes found)`.
- ci-step: Run check-commit-identity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gitignore-raw-dumps lock suite — not-applicable — fixture-only, untouched.
- ci-step: check-commit-identity on the PR commits — applicable
  - detail: scans this task's own PR commits. Run live: `bash bin/check-commit-identity.sh --base "$(git merge-base feature/1066-effort-time-telemetry HEAD)"` → `clean (no non-conformant commit identity found in range)`.
- ci-step: Run check-refreeze-class fixture suite — not-applicable — fixture-only, untouched (this task performs no re-freeze of any other spec's intent block).
- ci-step: Run bin-exec-bit lock suite — not-applicable — fixture-only, untouched (no path under `bin/` added or edited).
- ci-step: Run check-durability fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-durability — this repository's own T-1048 implement-phase records — not-applicable — unrelated to T-1067.
- ci-step: Run check-binding fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-binding — the shipped binding-config specimen validates — not-applicable — `templates/binding-template.conf`, untouched.
- ci-step: Run check-adapter fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-adapter — the shipped contract and definitions validate — not-applicable — fixed adapter definitions, untouched.
- ci-step: Run check-liveness fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-liveness — the real classifier's --help — not-applicable — `bin/check-liveness.sh --help`, untouched.
- ci-step: Run resolve-executor fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood resolve-executor — probe-free mode against this repository (the runner carries neither executor CLI) — not-applicable — reads binding config, untouched.

<!-- BEGIN provenance: T-1067 -->
- decision: priced `S2-resumed-subagent`'s cross-sectional measurement through a telemetry `run_id`+`span` reading (a per-round `tokens` figure attributed by task) rather than through a transcript-file mtime-correlated reading, as the note's primary S2 evidence.
  reason: a transcript file's own role (engineer vs. qa-verifier, etc.) is not recoverable from its path, size or mtime alone (measured directly — 15 candidate sub-agent files sit in the one session matched to T-1060's own telemetry window, with no way to single one out without opening content, which is a Non-goal); the telemetry route, by contrast, resolves unambiguously to one task and one role via the `run_id`+`span` convention `docs/loop-engineering/effort-time-trend.md` already established and this spec's own DP3 explicitly permits declining per-file role attribution in favour of it.
  grounding: `.shell-team/specs/T-1067-context-lifecycle.md` DP3 ("the rule is stated together with every case it cannot resolve... a per-round attribution explicitly declined") and `## Open questions` ("a distribution-only reading with per-task attribution explicitly declined... is acceptable"); measured directly in `docs/loop-engineering/context-lifecycle.md`'s own `## What was measured` S2 blocks.
- decision: excluded this task's own transcripts from every S1/S2 corpus count using the `$CLAUDE_CODE_SESSION_ID` environment variable (naming the variable in the note's own quoted commands, never its expanded value) rather than a static string or a task-id-shaped filter.
  reason: the transcript corpus carries no task id anywhere in its own paths (measured, `## Attribution and measurement rules`), so a `t1067`-style string filter — the pattern that works for `shell-team.jsonl`'s `run_id` — has nothing to match against in a transcript path; the running session's own env var was the only reachable, environment-independent (never a literal path or UUID) identifier available to this role for excluding its own in-progress session.
  grounding: measured directly — `find "$T" -type f -name '*.jsonl' | wc -l` = 480 before exclusion, 464 after (`-not -path "*$CLAUDE_CODE_SESSION_ID*"`), confirming the filter matched exactly this task's own running session (1 top-level file + 15 sub-agent files); positive control recorded in `docs/loop-engineering/context-lifecycle.md`'s own `- self-measurement-exclusion:` line.
- decision: fixed the cross-PR consistency test's verdict rule as a strict-majority vote (>50% "supported", <50% "contradicted", exact tie "undetermined") before running the count, rather than a specific percentage threshold.
  reason: a percentage threshold (for example, ≥80%) chosen after seeing a result close to a round number risks looking reverse-fitted to the data; a majority rule is population-size-invariant, cannot be tuned to a specific observed fraction, and is the same rule this decision was actually committed to before the 16-of-20 count was run.
  grounding: DP7 ("the test's population, its command and its three-value verdict vocabulary... are fixed before it is run, so the result cannot be reverse-fitted to the conclusion the note would prefer"); measured live in `docs/loop-engineering/context-lifecycle.md`'s `## Cross-PR consistency test` — `grep -cE 'AC[0-9]+|check-[a-z-]+\.sh|checker|criterion|criteria'` over the 20 extracted `effect:` lines = 16 (80%, a strict majority under the rule fixed above).
- decision: measured S3 (`launch-brief`) through the board's own byte size at two points (a per-task board-entry comparison and a whole-board-size comparison across named refs) rather than a per-call telemetry `tokens` proxy.
  reason: the recorded telemetry schema does not decompose a call's `tokens` total into "launch-brief content" versus "the agent's own subsequent work" (DP10, `no interpolated value`), so any figure built from that field for this purpose would be an estimate this task's own Non-goals forbid; the board's own byte size is git-tracked, directly measurable at any two refs with no decomposition assumption, and is a documented input every role's own prompt (including this task's own engineer prompt) names as something to read first.
  grounding: `.shell-team/specs/T-1067-context-lifecycle.md` DP10 and Non-goals ("No estimated, interpolated or illustrative value"); measured live — `git show v2.0.1:"$TODO" | wc -c` = 2602159, `git show 3d72e5b:"$TODO" | wc -c` = 2757501, and the per-task block extraction for T-1053 (30474 bytes) vs T-1066 (57943 bytes).
- decision: disposed `status-quo-carry-everything` as `recommended`, `per-round-fresh-subagents` as `not-recommended`, and `per-issue-orchestrator-reset` as `undetermined`.
  reason: rehydration cost is the one axis this note can price cleanly (zero under status quo, a real measured floor cost under the fresh-subagents option, and entirely unpriced under the orchestrator-reset option because no orchestration span exists in the telemetry schema at all); combined with the cross-PR consistency test's own `supported` verdict (removing the correctness argument for any reset), the three options' own measured axis readings — not a prior preference for the status quo — determined the three dispositions.
  grounding: `docs/loop-engineering/context-lifecycle.md`'s own `## Reset options and their cost` disposition-reason lines, each citing a specific measured figure from `## What was measured` or `## Cross-PR consistency test`; DP6 ("Rehydration is the axis a reset argument usually omits and the one that decides it").
<!-- END provenance: T-1067 -->
