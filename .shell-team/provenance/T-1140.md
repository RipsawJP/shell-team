<!-- BEGIN provenance: T-1140 -->

- decision: gave `tracker-key` two separate building-block variables — `RE_TRACKER_KEY_OUTLINE` (broad: `[A-Z][A-Z0-9]{0,9}-[0-9]+`, reachable by a single-character namespace and a digit-bearing namespace, used only by the fixture suite's precondition idiom) and `RE_TRACKER_KEY_NAMESPACE` (narrow: `[A-Z]{2,10}`, actually composed into the reported rule as `RE_TRACKER_KEY="${RE_TRACKER_KEY_NAMESPACE}-[0-9]+"`) — rather than composing the reported rule directly out of the outline variable the way `RE_HOST_LOCAL` and the shipped `RE_HOME_PATH` do.
  reason: `home-path`'s pair (`RE_HOME_PATH_BOUNDARY` + `RE_HOME_PATH_RAW` → `RE_HOME_PATH`) and `host-local`'s pair narrow their outline by ADDING a boundary requirement around an unchanged base shape — concatenation alone expresses that. `tracker-key`'s narrowing changes the CHARACTER CLASS inside the same position (any alnum vs. letters-only) and the LENGTH range (1-10 vs. 2-10), which concatenation cannot express: there is no boundary text that can be appended before or after a broad class to turn it into a narrower class at the same position. Two independently-named assignment lines — one that is genuinely reachable by both excluded populations (for AC5's precondition) and one that is the real, composed narrowing requirement (for the mutation lock) — satisfies AC5's decomposition duty (DP-4) without inventing a lookaround the shipped `grep -E` invocations cannot express, and keeps both new pattern ids symmetric with each other in spirit even though the actual mechanics differ.
  grounding: `.shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md` **AC5** ("a broader outline half exists as its own named, independently neutralisable assignment line ... a negative-control fixture is asserted to match that outline half ... while the whole rule does not match it") and **DP-4** ("the established pair shape ... is what lets a negative control be shown to reach the outline and be excluded by the named class"); `bin/check-pii-shapes.sh`'s own shipped `RE_HOME_PATH_BOUNDARY`/`RE_HOME_PATH_RAW`/`RE_HOME_PATH` and `RE_TEMP_SESSION_ROOT`/`RE_TEMP_SESSION` pairs, read first-hand before designing the new pair; empirical check that neither BSD grep (`/usr/bin/grep`, this machine) nor the shipped script's own `grep -nE` invocations support a lookaround construct.

- decision: excluded the configuration-file-name case from `RE_HOST_LOCAL` (the composed, reported rule) with a right-side requirement of "end-of-line, or a character that is not a letter, digit, or dot" — `"${RE_HOST_LOCAL_OUTLINE}(\$|[^A-Za-z0-9.])"` — rather than a lookahead, an enumerated extension list, or a boundary expressed only as "not a dot".
  reason: the shipped script has no PCRE support in its `grep -nE`/`grep -oE` invocations (verified empirically against both this machine's `/usr/bin/grep`, a BSD grep, and the shimmed default `grep`), so a negative lookahead (`(?!\.)`) is unavailable; an alternation group ending in `$` inside `grep -E` is supported by both BSD and GNU grep (verified empirically) and reads as "the suffix is the end of the matched text, or is followed by something that cannot continue a file name". Excluding only a literal dot (rather than the fuller `[^A-Za-z0-9.]` class) would have left a letter-continuation case (`.localhost`, `.localStorage`) matching the composed rule, which is not the shape this rule exists to report (an unbroken machine-local suffix) and which measurably exists in this tree already (`templates/loop-replay.html`'s `localStorage` calls) — see the `## Notes from engineer` disclosure on AC11's own verification-only filter, which does not make this same distinction.
  grounding: `.shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md` **AC1** ("no assignment line of any regex uses `\b`") and **AC5** ("the placeholder form `<host>.local` is a non-match by construction"); live probes run during implementation against a bare host name, the same host name with a configuration-file extension appended, and the same host name with an ordinary word appended with no intervening dot, using `/usr/bin/grep -nE '[A-Za-z0-9][A-Za-z0-9-]*\.local($|[^A-Za-z0-9.])'` against each in turn, confirming the exact match set the spec's Input space and `## Blast radius` derivation blocks require); `bin/check-pii-shapes.sh`'s own header note (this file, DP-11/RE_HOST_LOCAL inventory entry) written to record the same reasoning for a future reader.

- decision: chose an environment variable, `PII_CHECK_TRACKER_KEY` (non-empty to enable), as the opt-in mechanism for `tracker-key`, read inside `scan_content_file()` as `if [ -n "${PII_CHECK_TRACKER_KEY:-}" ]; then report_pattern_lines tracker-key ...; fi` — rather than a trailing optional CLI flag/argument to the script itself.
  reason: DP-5 fixes the requirement (reachable by the suite's existing assertion helpers without a signature change) but leaves the spelling to the engineer. A CLI flag would need `run_checker`'s "`[extra args...]`" trailing-argument slot threaded through every call site that wants the opt-in on, and would also need `scan_content_file()` (which has no argument-parsing context of its own — it runs once per changed path, deep inside the existing control flow) to receive that flag's value somehow, which the current architecture does not plumb. An environment variable needs no signature change anywhere: `run_checker`'s `bash "$BIN" --base "$base" "$@"` and every mutation helper's `bash "$copy" --base "$base"` already fork a child process that inherits whatever the calling test exported beforehand, so the suite enables it with a plain `export`/`unset` bracket around the relevant assertions. Named `PII_CHECK_TRACKER_KEY` for symmetry with the already-shipped `PII_CHECK_BASE` (the base-ref override env var `bin/check-pii-shapes.sh` already reads), rather than a bespoke name.
  grounding: `.shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md` **DP-5** ("An environment-carried mechanism satisfies it because the helpers' child processes inherit the environment ... The spec fixes the requirement, not the spelling"); `tests/check-pii-shapes/run.sh`'s existing `run_checker()` (`OUT="$(cd "$repo" && bash "$BIN" --base "$base" "$@" 2>&1)"`) and `assert_neutralised_pattern_unreported()` (`out="$(cd "$repo" && bash "$copy" --base "$base" 2>&1)"`), read first-hand before choosing the mechanism; `bin/check-pii-shapes.sh`'s pre-existing `PII_CHECK_BASE` read in the default-base-ref candidate chain, the naming precedent followed here.

- decision: mutate `RE_HOST_LOCAL_OUTLINE` (not `RE_HOST_LOCAL`) for the host-local pattern-load-bearing lock, and `RE_TRACKER_KEY_NAMESPACE` (not `RE_TRACKER_KEY` and not `RE_TRACKER_KEY_OUTLINE`) for the tracker-key one, mirroring the shipped convention of mutating the innermost building block a composed rule's `${...}` expansion actually reads at run time.
  reason: `assert_neutralised_pattern_unreported` rewrites one variable's own assignment line to a fixed placeholder string in a throwaway copy of the script; because bash evaluates a `"${VAR}"` reference at the point the REFERENCING assignment executes (not at parse time), neutralising the referenced variable earlier in the file still breaks the composed rule that reads it later, exactly as the shipped suite already does for `RE_HOME_PATH_RAW` (read by `RE_HOME_PATH`) and `RE_TEMP_SESSION_ROOT` (read by `RE_TEMP_SESSION`). For `host-local`, `RE_HOST_LOCAL_OUTLINE` is the only variable `RE_HOST_LOCAL` reads, so it is the correct (and only) mutation target. For `tracker-key`, `RE_TRACKER_KEY_OUTLINE` is deliberately NOT read by `RE_TRACKER_KEY` at all (see the first decision above), so mutating it would prove nothing about the reported rule; `RE_TRACKER_KEY_NAMESPACE` is the variable `RE_TRACKER_KEY` actually composes from, so it is the correct target.
  grounding: `bin/check-pii-shapes.sh`'s shipped `assert_neutralised_pattern_unreported RE_HOME_PATH_RAW ...` and `assert_neutralised_pattern_unreported RE_TEMP_SESSION_ROOT ...` call sites (`tests/check-pii-shapes/run.sh`, pre-existing), read first-hand before choosing the new call sites' target variables; manual verification during implementation that mutating `RE_TRACKER_KEY_NAMESPACE` (not `RE_TRACKER_KEY_OUTLINE`) actually silences the composed rule's own positive fixture with `PII_CHECK_TRACKER_KEY=1` exported (see the engineer hand-off's mutation self-check evidence).

- decision: disclosed, rather than repaired, a pre-existing false-positive population in AC11's own residual-grep filter (`templates/loop-replay.html:542`/`:1669`'s `window.localStorage` calls and `tests/check-pii-shapes/run.sh:581`'s pre-existing `mail.localhost` reserved-domain fixture) instead of editing either file to make AC11's check line pass.
  reason: both files are measured to already carry this same shape at the branch point (`B=$(git merge-base develop HEAD)`), before this task's diff — so editing them would not be a fix this task's own shape work requires, and Non-goals states plainly that "Only the sites the extended checker actually reports in its shipped default mode are in scope" for corpus work; `bin/check-pii-shapes.sh --all` (the actual shipped checker, not AC11's own verification-only filter) does not report either site as `host-local`, confirming there is no real leak to close. AC11's check line lives inside this spec's frozen intent block, so widening its exemption filter to also recognise a letter-continuation (not just a dot-continuation) as "not a host name" would be a re-freeze this task's own Non-goals forbids performing unilaterally.
  grounding: `.shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md` Non-goals ("A general hygiene sweep of the corpus. Only the sites the extended checker actually reports in its shipped default mode are in scope." and "Re-freezing, widening or repairing any merged task's frozen criterion."); branch-point measurement performed during implementation (`git grep -nE -e '[A-Za-z0-9-]\.local' "$B" -- 'bin' 'tests' 'docs' 'agents' 'skills' 'templates' '.shell-team' '*.md' | grep -vE '\.local\.'`, 4 lines, 2 of which predate this task); `.shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md`'s new `## Notes from engineer` section, where this finding is recorded in full for QA and the reviewer.

<!-- END provenance: T-1140 -->
