<!-- BEGIN provenance: T-1138 -->

- decision: implemented the `self-detected-host` value's token/ground split (D8's `codex-cli — <ground>` grammar) with plain bash parameter expansion over a `case` pattern containing the literal em-dash separator (`sdh_token="${sdh_val%% — *}"`, `sdh_ground="${sdh_val#* — }"`), rather than a `read`/here-string split (`read -r tok rest <<< "$sdh_val"`) or an ERE capture group fed through `[[ =~ ]]`.
  reason: the file's own `briefing-fidelity` check (`bf_first="${bf_val%% *}"` / `bf_rest="${bf_val#"$bf_first"}"`) already establishes this exact idiom for a single-space separator, so extending it to a multi-byte literal separator keeps one splitting convention in the file rather than introducing a second (a here-string) for one field alone. It also sidesteps a locale question a regex route would raise (an ERE bracket expression over the em-dash's 3 UTF-8 bytes under `LC_ALL=C` is exactly Gotcha 5's forbidden shape; a `case`/parameter-expansion literal match never decomposes the bytes, since glob matching is byte-literal, not class-based). Verified live: `%%` removes the longest matching suffix, which is anchored at the FIRST occurrence of the separator (a longer suffix starts earlier), and `#` removes the shortest matching prefix, anchored at that same first occurrence — so a ground that itself later contains the separator's bytes does not shift where token and ground split (traced by hand against the AC2/AC3 fixtures before committing, and confirmed by the live `check-acs.sh` run).
  grounding: `.shell-team/specs/T-1138-reviewer-host-self-classification.md` **D8**, **D4**'s Gotcha-5 cross-reference, `## Notes for engineer` item 5; `bin/check-review-input.sh:338`–`:346` (the pre-existing `briefing-fidelity` `bf_first`/`bf_rest` idiom this decision extends); `bin/check-handoff.sh:87` and `bin/check-count-claims.sh:7` (the repository's own em-dash-separator precedent).

- decision: normalized `executor-invocation`'s leading whitespace for the cross-field contradiction check (D4) with an explicit `case`-driven character-strip `while` loop, rather than `read -r tok1 tok2 rest <<< "$ei_val"` (a bash here-string, which strips leading `$IFS` whitespace and splits into fields as a single builtin call).
  reason: `read` would have been shorter, but its field-splitting semantics are governed by `$IFS` (which by default also treats newline as a separator) rather than literally "leading whitespace, then the first two whitespace-delimited tokens" the spec's own wording names; the explicit strip-then-`%%`/`#` approach (mirroring the ground-splitting decision above) makes exactly the two operations the spec names — leading-whitespace normalization, then first-two-token extraction — visible as two separate, auditable steps rather than one opaque builtin call, and needs no third "leftover" variable to keep shellcheck's unused-variable check clean. Verified live: both AC3 fixtures (no leading whitespace, and three leading spaces before `codex`) refuse `self-detected-host-contradiction` as required, and AC4's three non-refusal arms (`claude`-first, `claude-code` declared, `codexify`-first) all exit 0.
  grounding: `.shell-team/specs/T-1138-reviewer-host-self-classification.md` **D4**, `## Notes for engineer` item 1; `bin/check-review-input.sh`'s own self-detected-host block (the `ei_norm`/`ei_tok1`/`ei_tok2` lines); AC3 and AC4's own check-acs.sh fixtures, run live.

- decision: in `agents/codex-reviewer.md`, added the ladder as a new, standalone paragraph titled "Self-detected-host ladder (T-1138, issue #508)" immediately after the existing "Host-conditional note (T-1135)" paragraph, and the field's grammar/ground-convention/refusals as a new standalone Rule immediately after the existing `executor-invocation` Rule — rather than folding either into the paragraph or Rule it sits beside.
  reason: the spec's own citations (`## Summarized sources`, `## Assumptions`) measure that the existing Host-conditional note and `executor-invocation` Rule are the two correct anchor points, but folding new content into either would have made the pre-existing paragraph read as though the ladder were part of the *T-1135* decision it is not — the ladder is this task's own decision, dated and issue-numbered separately, and readers who already know the T-1135 note should see the boundary between what T-1135 decided and what T-1138 adds. A standalone paragraph/Rule also keeps `AC7`'s base-relative "host-conditional" line-count floor trivially satisfied (the pre-existing paragraph is untouched, so its own "host-conditional" occurrences cannot be lost by an edit that touches unrelated text inside it).
  grounding: `.shell-team/specs/T-1138-reviewer-host-self-classification.md` **AC7** (the base-relative `host-conditional` floor), `## Summarized sources`' citation of `agents/codex-reviewer.md:61`/`:217`, `## Notes for engineer`'s "Files expected to change" bullet for `agents/codex-reviewer.md`; `agents/codex-reviewer.md`'s own "Host-conditional note (T-1135)" paragraph and `executor-invocation` Rule, read first-hand before editing.

- decision: in both `docs/adopting.md` and `docs/adopting.ja.md`, added the Codex-host observable (AC8/AC13) as a new standalone paragraph placed after the existing "Honest limits" paragraph and before the next `## ` heading, and the fifth-field documentation (AC9) as a new standalone paragraph inside `## Recording review-input fidelity` / `## review-input fidelity を記録する`, immediately after the section's opening paragraph whose cardinality word ("four things") the spec requires to move — rather than rewriting either existing paragraph in place.
  reason: rewriting the "Honest limits" paragraph in place risked disturbing its own already-precise, measured claims (each one a distinct "measured (codex-cli …)" statement); a new paragraph keeps this task's own disclosure independently addressable and independently revertable from the pre-existing text. For `## Recording review-input fidelity`, the spec's own directive ("the cardinality word has to move with the field") required editing the section's own opening sentence from "four things" to "four required things, plus one optional fifth" (both languages) — that edit could not be avoided — but the field's own grammar/vocabulary/refusal-token documentation was still added as a new, separate paragraph rather than woven into the existing "What the verbatim field must never carry" / "What this mechanism does not close" paragraphs, which document a different field (`executor-invocation`) and a different set of limits.
  grounding: `.shell-team/specs/T-1138-reviewer-host-self-classification.md` **AC8**, **AC9**, **AC13**, `## Blast radius`'s "Predicted at risk" note naming the cardinality word; `docs/adopting.md`'s `## Using shell-team from Codex CLI` (Honest limits paragraph) and `## Recording review-input fidelity`, read first-hand before editing; `docs/adopting.ja.md`'s mirrored sections at the same anchors.

- decision: in `tests/check-review-input/run.sh`, built the new self-detected-host fixture arms by reusing the pre-existing `$f1`/`$f2`/`$f3`/`$f4` variables and `hdr()` function (already defined for the "Per-pass field completeness" section) for the legacy/mixed/vocabulary/duplicate arms, and wrote fresh, fully inline four-field blocks for the accept/contradiction arms whose `executor-invocation` text is itself the load-bearing part of the fixture — rather than extending the suite's own top-of-file `blk()` emitter (whose `executor-invocation` value is fixed to one literal string) or writing a new dedicated `sdh_blk()` helper function.
  reason: `blk()`'s `executor-invocation` value is a single fixed literal (`codex exec --sandbox read-only --cd . review --base develop`), which is exactly wrong for the accept/contradiction arms, each of which needs a *different* `executor-invocation` value as the very thing under test (a `claude -p` invocation mentioning `codex exec` in prose, a `codexify`-first-token invocation, a leading-whitespace-before-`codex` invocation); parameterizing `blk()` to accept the invocation text as an argument would have required touching a function every other pre-existing case in the suite already calls, risking a change with much larger blast radius than this task's `## Non-goals` and **AC12** permit. Reusing `$f1`–`$f4`/`hdr()` for the arms that do NOT need a varying invocation (legacy, mixed, vocabulary, duplicate) kept those five/six new cases minimal instead of duplicating the same four-field boilerplate a third way.
  grounding: `.shell-team/specs/T-1138-reviewer-host-self-classification.md` **AC11**, `## Notes for engineer`'s "Files expected to change" bullet for the suite; `tests/check-review-input/run.sh`'s own pre-existing `blk()` (top-of-file) and `hdr()`/`$f1`–`$f4` (Per-pass field completeness section), read first-hand before adding new cases.

<!-- END provenance: T-1138 -->
