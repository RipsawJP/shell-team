<!-- BEGIN provenance: T-1098 -->
- decision: the extractor (`tests/trial-recipe/extract-recipe.sh`) validates a whole-document line-count FLOOR (the doc's currently-shipped `wc -l`, one constant per recognized language) as a precondition, in addition to the within-section fence-count/floor checks the design started with.
  reason: AC2's own four adversarial mutants are built from `docs/adopting.md` by a single shared awk one-liner, and two of them (`m2`, which deletes one fence-delimiter line; `m3`, which truncates a fence body to fewer lines) do NOT touch the trial-adoption section's own text at all — both mutate the FIRST, unrelated fenced block in the document (the ASCII directory tree under "## Where the operating files live"), confirmed by literally running the check's own mutant-construction commands against the real file and diffing the result. A heading-text-match-plus-within-section-fence-count design (the first, narrower implementation tried) extracts byte-identical, correct setup/teardown text from both of these mutants and therefore cannot fail on them — the defect these two mutants represent is invisible from inside the target section alone; only a signal sensitive to the WHOLE document (its total line count, since both mutants shrink it by 1–2 lines while leaving the section untouched) can catch them. Growth is never penalized (a floor, not an exact match), so an unrelated future addition elsewhere in either doc does not break this lock — this is the class-level generalization, not a fix aimed at the two specific reproduced mutants alone.
  grounding: `.shell-team/specs/T-1098-trial-recipe-execution.md` AC2's `- check:` line (the `m2`/`m3` construction: `awk -v f="$F" 'index($0,f)==1{n++; if(n==2) next} {print}'` and the `n==1` truncation variant); measured directly in this session by running both constructions against `docs/adopting.md` and diffing against the original — both mutations land at lines 19–33 (the "Where the operating files live" ASCII tree), never inside the "## Trying the team on one ticket" section; `tests/trial-recipe/extract-recipe.sh`'s `FLOOR_LINES_EN`/`FLOOR_LINES_JA` constants and the `doc_lines -ge "$floor"` check.

- decision: AC11's "the resolved base dir is gone from the working tree afterwards" is implemented as two git-level assertions — `git ls-files -- "$D1_BASE"` is empty once back on the integration branch, and any filesystem survivor is asserted BY PATH to be confined to `<base>/runs/*` — rather than a raw `[ ! -e "$D1/$D1_BASE" ]` filesystem-existence check, and rather than a `git check-ignore` check on the survivor.
  reason: measured directly in this session: `templates/shell-team.gitignore` ships `runs/`, so a plain `git add <base>` (the shipped recipe's own line, unmodified) never tracks `<base>/runs/.gitkeep` in the first place; a branch switch only removes files that WERE tracked on the branch being left, so this file (and hence the `<base>/runs/` and `<base>/` directories) survives on disk after teardown regardless of how correctly teardown itself behaves — a raw `[ ! -e ]` check would fail on every single run, not because teardown is broken but because the shipped `.gitignore` behaves exactly as documented elsewhere in this repo. A `git check-ignore` assertion on the survivor was tried next and also failed: `<base>/.gitignore` itself WAS tracked and is correctly removed by the same branch switch, so the rule that would have explained the survivor is gone by the time it's checked. The working design asserts what is actually, verifiably true: nothing tracked remains (the git-level claim the doc's own teardown paragraph actually makes — "destroys every commit reachable only from it"), and the one class of physical survivor this repo's own shipped files predict is named by path rather than re-derived from a rule that no longer exists to re-derive it from.
  grounding: measured directly in this session — `git -C <fixture> ls-tree -r --name-only HEAD` after the scaffold commit never lists `<base>/runs/.gitkeep`; `templates/shell-team.gitignore`'s own `runs/` line; a live repro (`tests/trial-recipe/.scratch-probe7.sh`, not committed) showing an ignored-but-tracked-elsewhere file surviving a branch switch as `?? base/` while `git check-ignore` reports it NOT ignored once the governing `.gitignore` is itself removed by the same switch; `.shell-team/specs/T-1098-trial-recipe-execution.md` AC11 body text ("the resolved base dir is gone from the working tree afterwards") and the doc's own teardown paragraph in `docs/adopting.md` ("destroys every commit reachable only from it").

- decision: the suite establishes and mutates its scratch global-config scope's `core.excludesFile` by writing `[core]\n\texcludesFile = <path>\n` directly into the resolved scratch config file with `printf`, never by invoking `git config --global` at any point in `tests/trial-recipe/run.sh` — including in comments.
  reason: AC14's own hygiene sweep is a literal `grep -cF -- 'git config --global'` over the WHOLE file (code and comments alike, not code-only), so the exact substring appearing anywhere fails it. `GIT_CONFIG_GLOBAL` (or a redirected `HOME`) just names which file `git config --global` would target; writing that same file's content directly with `printf` achieves the identical, verifiable effect (confirmed via the suite's own `probe_repo_sees()` control both before and after the change) without the invocation ever needing to exist.
  grounding: `.shell-team/specs/T-1098-trial-recipe-execution.md` AC14's `- check:` line (`test "$(grep -cF -- 'git config --global' "$f" || true)" = "0"`); `tests/trial-recipe/run.sh`'s `write_global_excludes()` helper and `probe_repo_sees()` control; measured directly in this session — the suite's `hermetic-global` and `adverse-excludes` cases both PASS with this design, and `grep -cF -- 'git config --global' tests/trial-recipe/run.sh` reports 0.

- decision: `tests/trial-recipe/run.sh`'s scratch git-fixture root uses the `${TMPDIR:-/tmp}`-only `mktemp -d` arm, with no `$HERE` fallback arm — a deliberate departure from `tests/team-init/run.sh`'s own two-arm (`$TMPDIR`-then-`$HERE`) idiom for its `GIT_TMP` root.
  reason: this is a NEW suite (post-dating T-1044) whose every fixture is a `git init` repository; T-1044's own test-recipe entry states that a suite in this shape should keep its scratch root under `${TMPDIR:-/tmp}` only, because a `$HERE` fallback would nest a `.git` inside this checkout's own work tree, which a sandboxed run denies. `tests/team-init/run.sh`'s own two-arm form predates that guidance and is left as-is per this task's Non-goals (no widening of that suite's existing scope).
  grounding: `.shell-team/test-recipe.md`'s T-1044 entry ("if the new suite either (a) builds throwaway `git init` repos under its scratch root, in which case keep the root under `${TMPDIR:-/tmp}` only and never add a `$HERE` fallback"); `tests/trial-recipe/run.sh`'s `GIT_TMP="$(mktemp -d "${TMPDIR:-/tmp}/trial-recipe-test-git.XXXXXX")"` line.

- decision: every fixture that will run the extracted `git commit` line gets a PERSISTED, repo-local `user.email`/`user.name` (via `git -C <dir> config user.email/user.name`, never a `git -c` prefix) applied once right after `git init`, rather than rewriting the extracted commit line to add `-c` flags.
  reason: the shipped setup fence's `git commit -m "..."` line carries no identity flags of its own, and this task's substitution list is closed to exactly three members (`<integration-branch>`, `team-init.sh`, `team-paths.sh`) — adding a fourth, ad hoc `-c` substitution to inject identity would violate that closure and diverge the executed text from what an adopter actually runs. A persisted, repo-local config value is read by any git invocation against that repository regardless of who makes it, so the UNMODIFIED extracted line succeeds without needing to know about the identity mechanism at all — the same reasoning `.shell-team/test-recipe.md`'s T-1042 entry already gives for pinning `core.excludesFile` the same way when the git call being pinned is made inside the code under test.
  grounding: `docs/adopting.md`'s shipped setup fence (`git commit -m "chore: scaffold shell-team for a one-ticket trial"`, no `-c` flags); `.shell-team/specs/T-1098-trial-recipe-execution.md`'s closed three-member substitution list (`## Goal`, "The substitution list applied to extracted text is closed..."); `.shell-team/test-recipe.md`'s T-1042 entry ("pinning `git check-ignore`'s `core.excludesFile` input... needs the fixture repo's OWN persisted config... not a transient `git -c ... <cmd>` on the outer invocation"); `tests/trial-recipe/run.sh`'s `set_identity()` helper.
<!-- END provenance: T-1098 -->
