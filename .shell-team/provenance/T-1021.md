<!-- BEGIN provenance: T-1021 -->

- decision: `bin/check-intent.sh`'s already-`10#`-normalized freeze-attestation fields (`hash_version`, `av`/`ar`/`at`/`ap`/`af`, and the `$((ap + af))` / `expected_ratified=$((version_int - 1))` derivations) are classified `NORMALIZED` with `width: none`, and the file is left untouched — no `NEEDS-WIDTH-BOUND` fix was applied there.
  reason: two structurally valid designs existed. (a) Treat D2's ladder mechanically — every already-normalized site whose grammar is unbounded `[0-9]+` (true for all of these) crosses into `NEEDS-WIDTH-BOUND` and gets a new width regex on `ATTEST_FULL_RE`/`HASH_FULL_RE`. (b) Read D4's declination clause ("an overflow cannot change any comparison's answer" / "already bounded upstream") as licensing a stated decline, on the grounds that these values are board/spec content this repository's own security model already treats as fully-trusted operator-authored input (the same trust boundary `CLAUDE.md` states for the file class), and that the scope-lock's own allow-list lists `bin/check-intent.sh` as "Permitted... only if the inventory's width decision lands on `NEEDS-WIDTH-BOUND` there" — i.e. explicitly anticipating that it might legitimately not. (b) was chosen: widening the diff into a fourth file for a class of value with no demonstrated real-world path to a 19-digit board sub-bullet (unlike `max_iterations: 08`, which the Problem statement measures as a real hand-edit shape) would be scope creep against D3/D12's "every fix is local... no adopter doc/agent/skill/template/workflow" discipline and the Notes for engineer's explicit "only if" framing, not a second required fix.
  grounding: .shell-team/specs/T-1021-arith-base10-audit.md D4 ("A site may legitimately decline a bound... What is forbidden is silence") and Notes for engineer's scope-lock table ("Permitted in addition, not required: `bin/check-intent.sh`... only if the inventory's width decision lands on NEEDS-WIDTH-BOUND there"); docs/loop-engineering/arith-base10-audit.md's `- overflow: bin/check-intent.sh:539` / `:616` rows (decision: NO-BOUND).

- decision: `bin/rollup-runs.sh`'s `tok_sum`/`dur_sum` accumulation sites (`:162`/`:164`) are fixed with `10#` (class `NEEDS-10#`) but declared `width: none` — no width regex was added to `field_num()`'s `([0-9]+)` capture or to the accumulators.
  reason: two structurally valid designs existed. (a) Bound `tokens`/`duration_ms` to a fixed digit width (mirroring `bin/loop-guard.sh`'s `{1,9}` convention), rejecting an out-of-range value with a `rollup-runs`-namespaced diagnostic. (b) Leave the grammar unbounded and rely on D4's declination clause, since `tok_sum`/`dur_sum` are reporting-only aggregates with no downstream gating decision (no `STOP`/cap/comparison reads them) — an overflow wraparound could only mis-report a displayed total, never silently pass a check that should have failed. (b) was chosen because the open question the spec itself records ("Whether `field_num()`'s grammar should reject a JSON-invalid leading zero outright... AC9 admits either answer deliberately, because both are fail-closed and the choice is a legibility trade-off the engineer is better placed to make") extends naturally to the width question: adding a width bound here would reject a legitimately large (if unusual) token count with no safety benefit, which is the opposite of AC9's own preference for the display staying informative over refusing on non-load-bearing arithmetic.
  grounding: .shell-team/specs/T-1021-arith-base10-audit.md D4 ("Where... an overflow cannot change any comparison's answer, `width: none (reason: …)` is the correct row") and Open questions ("both are fail-closed and the choice is a legibility trade-off the engineer is better placed to make"); docs/loop-engineering/arith-base10-audit.md's `- overflow: bin/rollup-runs.sh:162` / `:164` rows (decision: NO-BOUND).

- decision: `bin/goal-state.sh` and `bin/loop-guard.sh` normalize each raw value with `10#` on its own dedicated line, re-assigning the SAME variable name (`start=$((10#$start))`, `ITERATION=$((10#$ITERATION))`), immediately after the existing width-bound regex check — rather than folding the `10#` prefix directly into each downstream comparison/increment site.
  reason: two structurally valid designs existed. (a) Apply `10#` at every downstream arithmetic use (`it=$((10#$it + 1))`, `(( 10#$MAX_ITER > 0 ))`, etc.), leaving the raw variable untouched until each individual use. (b) Normalize once into a fresh assignment immediately after validation, then let every downstream site consume the already-decimal value with no `10#` of its own. (b) was chosen because the spec's own "Notes for engineer" names this exact shape as the model to follow ("Apply `10#` once, immediately after the value is proven to be digits, not at each downstream comparison... normalize into a local, then compare the locals") and names the two operands at `bin/loop-guard.sh:144`/`:149` (pre-fix line numbers) as "the easy one to miss because it reads like a triviality" if normalization is scattered per-site — a single normalization point makes it structurally impossible to forget one of the two operands the way T-1018's own bug forgot one `10#` site.
  grounding: .shell-team/specs/T-1021-arith-base10-audit.md Notes for engineer ("Two implementation gotchas that will otherwise cost a round" — the `bin/check-intent.sh:614-623` shape, and the loop-guard.sh two-operand warning); bin/check-intent.sh:614-623 (the pre-existing T-1018 model this task mirrors); bin/goal-state.sh:83-88,100-106; bin/loop-guard.sh:88-94,134-147 (the implemented fix).

- decision: a `- site:` row's `class:` field describes which occurrence is the load-bearing fix point (the address whose `10#` token, if removed, reproduces the historical bug), not a literal re-evaluation of D2's ladder against the row's own line of code in isolation — stated explicitly in the audit document's "How `NEEDS-10#` is read in this document" paragraph.
  reason: two structurally valid readings of D2's ladder existed. (a) Read step 2 ("does the operand enter arithmetic without base-10 normalization?") as a literal, present-tense text test on each occurrence: since the fixed normalization lines (`start=$((10#$start))`) now literally contain `10#`, they would answer "no" and fall through to `NORMALIZED`/`NEEDS-WIDTH-BOUND`, leaving zero `NEEDS-10#` rows anywhere in the final, fixed document — which would make AC13's "at least one `NEEDS-10#`/`NEEDS-WIDTH-BOUND` row with a lock" unsatisfiable by construction, and would erase the audit trail of which addresses this task actually fixed. (b) Read the class as describing the finding the fix is anchored to: the occurrence where an untrusted, previously-unnormalized digit string first enters arithmetic is `NEEDS-10#` regardless of the token now present there, and a downstream occurrence consuming the resulting clean local is `NORMALIZED`. (b) was chosen — it is the only reading under which the document's own lock-case mutation self-checks (revert the `10#` at a named address, confirm the corresponding AC probe goes red, restore, confirm green) make sense, since those mutations target exactly the `NEEDS-10#` rows' own addresses.
  grounding: .shell-team/specs/T-1021-arith-base10-audit.md D2 (the four-leaf ladder) and D7/AC13 ("every fix class gets a lock case in the owning suite"; "for each `- site:` row classified NEEDS-10# or NEEDS-WIDTH-BOUND, its `lock:` field..."); Notes for engineer's mutation self-check list ("Revert the `10#` at `bin/goal-state.sh:89` only — AC7's first probe goes red..."); docs/loop-engineering/arith-base10-audit.md's own "How `NEEDS-10#` is read in this document" paragraph.

<!-- END provenance: T-1021 -->
