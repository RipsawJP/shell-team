<!-- BEGIN provenance: T-1027 -->

- decision: Every frozen text this task ships (E1, E2, E3, O's post-edit shape, F1, F2, F3, F4, F5) was transcribed into the working tree by extracting each `<!-- frozen-begin: X --> … <!-- frozen-end: X -->` block directly out of the spec file with `awk`/`sed` into scratch files under `$TMPDIR`, then splicing those exact bytes into the target files programmatically — as CLI arguments to `bin/playbook-promote.sh` read from the scratch files by command substitution for E1/E2/E3, and via a small Python script driven by line-number/line-content anchors for O's `Status`/`Superseded-by` edit, the `## Format` paragraph (F5), and the F1–F4 edits to `agents/scrum-master.md` — rather than reading the spec on screen and retyping any of the text by hand.
  reason: Two implementation paths existed for getting frozen prose into the shipped files: (a) read the frozen block on screen and retype it into the target file, or (b) extract the block's bytes programmatically from the spec file and let a script place them, never passing through a manual retype. The spec's own Notes for engineer name the exact failure mode of (a) — "a single smart quote or a collapsed double space fails the criterion, and the failure will look like a corpus problem rather than a transcription problem" — and every acceptance criterion that pins frozen text (AC1–AC4, AC8, AC11) compares against the spec's own fenced block, not a restated copy. (b) was chosen because it structurally rules out that failure mode rather than merely relying on care while retyping, and because it let every byte-identity claim below be verified with `cmp` against the same extraction the acceptance criteria themselves perform, before any commit was made.
  grounding: `.shell-team/specs/T-1027-promote-retro-2026-08-04.md`, "Notes for engineer" § "Traps in the criteria themselves" ("Every frozen text is read out of this spec by AC1–AC4, AC8 and AC11. If you retype one instead of copying it, a single smart quote or a collapsed double space fails the criterion... Copy the fenced blocks."); the same spec's AC1/AC2/AC3/AC4/AC8/AC11 `check:` lines, which extract each frozen block from the spec via the identical `awk -v b='<!-- frozen-begin: … -->' ... | sed '1d;$d'` idiom used here.

<!-- END provenance: T-1027 -->
