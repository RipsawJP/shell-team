<!-- BEGIN provenance: T-1105 -->
- decision: both new consumers of `templates/prompt-blocks/executor-resolution.md` (`skills/review/SKILL.md`, `skills/review-response/SKILL.md`) and the new consumer of `templates/prompt-blocks/telemetry-binding.md` (`skills/review-response/SKILL.md`) carry the canonical paragraph introduced by a bolded label — `**Executor resolution (T-1057).**` / `**Telemetry binding (T-1058).**` — immediately before the canonical text, rather than inserting the bare canonical paragraph with no such label.
  reason: two options were genuinely available (label the paragraph, or paste it unlabelled) and the choice affects the committed bytes of an adopter-facing skill file. `skills/run/SKILL.md` and `skills/goal/SKILL.md` — the two existing consumers of both blocks — already open each canonical paragraph with exactly this bolded, task-id-suffixed label (`**Executor resolution (T-1057).** For each of the six bound roles...`, `**Telemetry binding (T-1058).** After each bound role's...`). Reusing that label keeps the two new consumers visually and structurally identical to the shipped precedent rather than introducing a second, unlabelled convention for the same canonical content, and the spec's own Notes for engineer name this file pair as prior art to read before writing the tails.
  grounding: `skills/run/SKILL.md:166` and `:168` (the existing labelled paragraphs); `skills/goal/SKILL.md:287` and `:289` (same labels); `.shell-team/specs/T-1105-review-executor-resolution.md` `## Notes for engineer` — "Prior art: ... the two tasks that registered these blocks onto the run and goal skills; read how their tails are written before writing yours."

- decision: in `skills/review-response/SKILL.md`'s `## Telemetry (best-effort)` section, the new `telemetry-binding.md` paragraph (with Tail 3) is appended AFTER the section's pre-existing best-effort sentence ("If you drive sub-agents, you may emit a span... it must never stop the flow."), leaving that sentence byte-for-byte untouched, rather than editing, reordering, or merging the two into one paragraph.
  reason: two orderings were available — new-paragraph-first or new-paragraph-last — and the choice affects the observable content and reading order of a shipped skill file's telemetry section. Appending last keeps the pre-existing sentence exactly as `.shell-team/specs/T-1105-review-executor-resolution.md`'s AC3 requires it to survive ("the section's own pre-existing best-effort sentence `it must never stop the flow` is still present"), and matches the section's own idiom the spec names: that sentence decides *whether* a span is emitted at all, and the newly-appended paragraph decides *what values* an emitted span carries — stating the "whether" framing first and the "what" framing second follows the natural reading order of the distinction the spec itself draws.
  grounding: `.shell-team/specs/T-1105-review-executor-resolution.md` AC3 ("the section's own pre-existing best-effort sentence `it must never stop the flow` is still present — so the block was composed with the section's idiom rather than replacing it") and its Goal sentence ("that section keeps deciding whether a span is emitted at all, while the block fixes what values any emitted span carries").

- decision: the four re-widened paragraphs on `README.md`, `README.ja.md`, `docs/adopting.md` and `docs/adopting.ja.md` are hard-wrapped at roughly the same column width as the surrounding paragraph in each file (~72-74 columns), rather than written as a single unwrapped long line, and every frozen anchor or AC-checked literal inside the new text is verified, line by line, to sit entirely on one physical line rather than spanning a wrap point.
  reason: two shapes were available (rewrap to match the file's existing hard-wrap style, or leave the inserted text as one long line) and the choice is observable both to a human reader of the shipped docs and to every AC's `grep`/`awk` line-based extraction. This task's own launch instructions and the recorded intervention for this task's freeze both name exactly this defect class — a frozen anchor (`呼び出し経路`) spanning a line break on `docs/ja-brushup` zeroed AC4's contiguous in-section occurrence and had to be repaired upstream before this branch was re-cut — so the wrapping choice was made deliberately rather than left to whatever a naive paste produced, and every literal AC9–AC12 assert (both the four command names and the ten retired/four retained anchors) was grepped against the freshly-wrapped section text before commit to confirm none straddles a line break.
  grounding: `.shell-team/interventions/T-1105.md` ("the predecessor's own re-wrapping of `docs/adopting.ja.md` so the frozen anchor 呼び出し経路 spanned a line break, zeroing its contiguous in-section occurrence"); `.shell-team/specs/T-1105-review-executor-resolution.md` AC10/AC11/AC12's own stated reason for choosing contiguous single-line literals ("this page is hard-wrapped and a literal spanning a line break could never be found by grep, which would turn a retirement lock into a vacuous one"); live verification for each of the four files (`grep -cF` on every asserted literal against the extracted section, all non-zero, run before commit).
<!-- END provenance: T-1105 -->
