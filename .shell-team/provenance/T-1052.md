# T-1052 decision provenance

<!-- BEGIN provenance: T-1052 -->
- decision: place `Q171` as the physical line immediately after `agents/qa-verifier.md`'s "Never destroy the working tree (T-073)" bullet (the last hand-written `- ` bullet before the `## Adversarial fixture synthesis checklist` heading), rather than searching for a blank line immediately preceding the file's first `<!-- BEGIN prompt-block: -->` marker and inserting there.
  reason: the file's structure is `## Rules` heading -> hand-written `- ` bullets -> `## Adversarial fixture synthesis checklist` heading (with its own prose and a numbered list) -> the first `<!-- BEGIN prompt-block: careful-execution -->` marker. AC8's own `check:` line extracts the "hand-written region" as everything between the `## Rules` heading and the first prompt-block marker via `awk '$0=="## Rules"{f=1;next} f && index($0,"<!-- BEGIN prompt-block:")==1 {exit} f'`, which spans both the bullet list and the Adversarial-checklist section — so both candidate insertion points satisfy the check's literal region test. Placing `Q171` as the last item of the bullet list (rather than immediately before the far-later prompt-block marker, which would land it inside or after the Adversarial-checklist prose) keeps it grouped with the file's other mandatory-item bullets (the intent-block rule, the provenance rule, the intervention-record rule, the summarized-sources rule, the working-tree rule) it shares its "mandatory, not subject to the out-of-input-space exemption" idiom with, matching the spec's Notes for engineer instruction to insert "after the final existing rule bullet."
  grounding: `.shell-team/specs/T-1052-records-editorial.md`'s `## Notes for engineer` ("Splicing the frozen texts" paragraph: "inserted as the last bullet of their file's hand-written `## Rules` region — after the final existing rule bullet and before the blank line preceding the first `<!-- BEGIN prompt-block:` marker") and AC8's `check:` line (the region-extraction `awk` clause, confirmed live to include both the bullet list and the Adversarial-checklist section); `agents/qa-verifier.md` at HEAD (the shipped placement, verified via `bash bin/check-acs.sh` -> AC8 PASS).
- decision: measure every `## Pinner inventory` row's `base:` verdict via a disposable `git worktree add --detach "$TMPDIR/..." a804703` full checkout, running each target spec's own `bash bin/check-acs.sh` inside that worktree, rather than materialising only `agents/engineer.md` and `agents/qa-verifier.md` via two `git show a804703:<path>` writes into an otherwise-empty scratch directory.
  reason: several of the thirteen pre-existing pinning specs' own criteria read files beyond the two agent files at the same historical ref (their own spec's base blob for a line-count delta, the board's base blob for a version-arithmetic check, other `agents/*.md` files for a preservation lock) — a two-file materialisation could not run those criteria's real `- check:` commands at all, only a hand-inspection of the two agent files' bytes, which would not satisfy AC11/AC16(d)'s "run the pinning criteria against that state" instruction as literally as a full worktree does. A `git worktree add --detach <path> a804703` reads exactly `a804703`'s committed tree for every file, including the two agent files, so it is a strict superset of "materialise `git show a804703:<path>`" for those two files and additionally lets every other file the pinning criteria read resolve correctly at the same ref — satisfying DP11's "never read the working tree for a base value" constraint no less strictly than the narrower two-file approach, while producing a genuinely re-run (not merely re-inspected) verdict for all thirteen specs.
  grounding: `.shell-team/specs/T-1052-records-editorial.md` DP11 and the `## Pinner inventory` template's "Base-side measurement procedure" paragraph ("materialise `git show a804703:<path>` ... run the pinning criteria against that state ... never read the working tree for a `base:` value"); live measurement — all thirteen pre-existing specs' `bash bin/check-acs.sh` shapes at the `a804703` worktree, `git worktree remove --force` afterward, `git status --short` confirmed empty on the real checkout before, during and after.
- decision: treat AC14's own `check:` line as carrying a construction gap — its `sp=` sub-clause (`git diff --name-only a804703...HEAD -- .shell-team/specs | grep -vxF -e '<T-1044 path>' -e '<T-1034 path>' | grep -c .`) excludes only the two named target specs, never this task's own `.shell-team/specs/T-1052-records-editorial.md`, even though AC14's own prose requires "every spec under `.shell-team/specs/` other than the two named and this task's own" to be unchanged — and disclose it as a benign, evidenced FAIL in the hand-off rather than editing the frozen intent block or escalating (stopping the task).
  reason: measured directly: `git diff --name-only a804703...HEAD -- .shell-team/specs` returns exactly three paths (T-1044, T-1034, and this task's own spec, all three tracked and committed on this branch, all three differing from `a804703` where the third did not exist), and the `sp=` sub-clause's `grep -vxF` exclusion list names only the first two — so `sp=1` unconditionally, for every possible implementation of this task, regardless of correctness, the moment this task's own spec is committed (which freezing itself requires). This is a genuine, unavoidable-without-a-frozen-text-edit defect, not a missing deliverable: the *actual* safety property AC14 exists to protect (no spec outside the three named is touched) is independently and correctly proven by the same criterion's earlier, broader `extra=` sub-clause, which explicitly allows `.shell-team/specs/T-1052-records-editorial.md` in the allow-list and measures `extra=0` (confirmed, real PASS on that half). Editing AC14's frozen `- check:` line is forbidden to this role (pm-spec's ownership; any fix needs a class-B correction through that channel plus human ratification) and is outside the four explicit escalation triggers this task's own orchestrator instructions name (a fragment's exactly-once contract, the classifier's exit code, T-1044's hash moving, or a need to touch `agents/scrum-master.md`/`agents/pm-spec.md`/a third spec) — none of which this finding is. Silently declaring PASS, or quietly working around it (e.g. by not committing this spec, which is impossible; or by editing the frozen check line, which is forbidden), would both be worse than disclosing a narrow, well-evidenced, redundant-check defect and letting QA/the cross-provider reviewer/the orchestrator decide whether a fast-follow class-B correction to this spec's own AC14 is warranted.
  grounding: `.shell-team/specs/T-1052-records-editorial.md` AC14's `- check:` line (the `sp=` sub-clause's literal `grep -vxF` exclusion list, live-read); AC14's own prose ("every spec under `.shell-team/specs/` other than the two named and this task's own"); live measurement (`git diff --name-only a804703...HEAD -- .shell-team/specs` returning three paths; the `sp=` sub-clause in isolation returning `1`; the same criterion's `extra=` sub-clause in isolation returning `0`); this task's orchestrator instructions' explicit, four-item "Escalate (STOP, no flag) if" list (none of which names this scenario); the general engineer rule "the intent block ... engineer は 1 byte も編集しない" (`agents/engineer.md` Rules).
<!-- END provenance: T-1052 -->
