# T-1094 decision provenance

<!-- BEGIN provenance: T-1094 -->
- decision: the new `docs/adopting.md` / `docs/adopting.ja.md` section's heading is `## Trying the team on one ticket` (English) / `## 1 チケットでチームを試す` (Japanese), rather than one of several other AC-satisfying wordings (e.g. "Running a one-ticket trial", "One ticket, no commitment").
  reason: multiple headings would have satisfied the mechanical constraints — contain `one ticket`/`One ticket` (AC2's anchor), avoid all six forbidden substrings and sit before all three merged anchor headings (AC6) — so the exact wording was a free choice among them. This wording was chosen because it echoes the requester's own phrasing verbatim (issue #334's outcome sketch, relayed into this spec's `## Assumptions`), so an adopter scanning the table of contents recognizes the capability by the same words the issue used to ask for it.
  grounding: `.shell-team/specs/T-1094-trial-adoption.md` AC2's `- check:` line (anchors on `index($0,"one ticket")||index($0,"One ticket")`) and AC6's `- check:` line (the six forbidden substrings and the three-anchor ordering); `.shell-team/specs/T-1094-trial-adoption.md` `## Assumptions`, the "Verbatim re-presentation owed at the batch GO" bullet, which quotes issue #334's outcome sketch verbatim as `"docs/adopting.md" gains a "trying the team on one ticket" section`.
- decision: the runnable example branch name used consistently across every command in both fenced blocks, in both languages, is `trial/one-ticket` — a slash-namespaced name, distinct from this repository's own `feature/<n>-<slug>` branch convention.
  reason: AC10 requires the setup and teardown blocks to carry specific command-shape literals (`git switch -c`, `git add`, `git commit`, `git switch`, `git branch -D`) but asserts no specific branch-name literal, so the exact name was a free choice, locked once for internal consistency across both blocks and both language files. A `trial/` prefix was chosen over reusing this repo's own `feature/<n>-<slug>` shape specifically so a reader does not mistake the throwaway example for a permanent-feature-branch naming instruction lifted from this project's own contributor workflow.
  grounding: `.shell-team/specs/T-1094-trial-adoption.md` AC10's `- check:` line (requires the named command-shape literals inside the setup/teardown blocks, asserts no specific branch-name token); `CONTRIBUTING.md`'s branch-naming convention `feature/<n>-<slug>` (the shape deliberately not reused here); `docs/adopting.md`'s new section (the same literal `trial/one-ticket` appears in every command across both fenced blocks) and `docs/adopting.ja.md`'s mirror (identical literal, since it is one of the four language-neutral tokens the mirror must reproduce).
- decision: the reconciliation paragraph added under `## Where the operating files live`, and the new section's own global-excludes remedy paragraph, cross-reference each other with markdown anchor links (`[Trying the team on one ticket](#trying-the-team-on-one-ticket)` and `[Where the operating files live](#where-the-operating-files-live)`) — a cross-reference style not previously used anywhere else in `docs/adopting.md`.
  reason: AC7 requires the reconciliation paragraph to carry the literal tokens `one ticket` and `gates` inside the existing region as an addition, never a rewrite, of the existing "If you keep the base dir out of git" sentence. A bare mention of those words would satisfy the grep but read as dead prose; an actual clickable anchor link makes the pointer genuinely useful to the adopter it is written for, and the reverse link (new section back to the base-dir paragraph, for the global-excludes remedy) reuses the identical idiom rather than introducing a second cross-reference style in the same file.
  grounding: `.shell-team/specs/T-1094-trial-adoption.md` AC7's `- check:` line (requires `one ticket` and `gates` inside the "Where the operating files live" window, and requires the pre-existing sentence `If you keep the base dir out of git` to survive unchanged); `docs/adopting.md`'s reconciliation paragraph and the new section's global-excludes paragraph (the two anchor links, the only `](#` occurrences in the file per `grep -n '\](#' docs/adopting.md`).
- decision: the shipped `docs/adopting.md` / `docs/adopting.ja.md` new section carries no mention of the orphan-branch route or its cost/benefit comparison at all.
  reason: the frozen spec's own Non-goals state plainly that "the written comparison stays in this spec's own body and this task's provenance; no orphan-ref mechanics ship and no new `docs/loop-engineering/` note is authored," and no acceptance criterion (AC1–AC12) requires any orphan-related literal inside the shipped section — confirmed by grepping every `- check:` line in the spec for the substring `orphan` (zero hits). The task hand-off's own mention of an "orphan-route bounding" as expected deliverable content is read as referring to the comparison paragraph already frozen inside the spec's own intent block (which this role has no authority to edit) rather than as an instruction to add new orphan-related prose to the shipped, adopter-facing documentation.
  grounding: `.shell-team/specs/T-1094-trial-adoption.md` Non-goals, third bullet ("No orphan-branch mechanism ... The written comparison stays in this spec's own body and this task's provenance; no orphan-ref mechanics ship and no new `docs/loop-engineering/` note is authored (**AC8**, **AC9**)"); `grep -n '^\s*- check:' .shell-team/specs/T-1094-trial-adoption.md | grep -c orphan` → `0`; `.shell-team/specs/T-1094-trial-adoption.md`'s frozen Goal body, "The orphan-branch comparison, priced and declined" paragraph, which is itself the comparison the Non-goals say stays in the spec rather than shipping in the doc.
<!-- END provenance: T-1094 -->
