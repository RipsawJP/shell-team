<!-- BEGIN provenance: T-1149 -->

- decision: Use bold inline labels (`**Claude Code:**` / `**Codex CLI:**`, and their Japanese counterparts) to separate each host's commands inside `## Install` and `## Update` / `## インストール` and `## アップデート`, rather than adding a subheading for each host.
  reason: A new `## ` heading inside these sections would be counted by AC12's `^## ` count, and D-2 requires adding no heading at all to either distribution file — a base-relative delta AC12 locks per language. Bold inline labels give the same host separation without moving that count.
  grounding: `.shell-team/specs/T-1149-distribution-workflow-two-hosts.md` — **AC12**'s `- check:` line (`docs/distribution.md`/`.ja.md` `^## ` count must equal its own base count) and design decision **D-2** ("Adding no heading is deliberate").

- decision: In `## Update` / `## アップデート`, print the full `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents` command (with the explicit `--out-dir` flag), the same literal already used in `## Install`, rather than the bare no-flag `gen-codex-agents.sh` form the runbook also documents as equivalent.
  reason: **AC7** requires the exact whole string `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents` to occur in both distribution files (it is one of the seven runbook-derived literals every AC3/AC4/AC5 command is drawn from); reusing the already-pinned Install-section command keeps one copy-pasteable command form across both sections rather than introducing a second, shorter variant no criterion separately requires.
  grounding: `.shell-team/specs/T-1149-distribution-workflow-two-hosts.md` — **AC3**, **AC4** and **AC7** `- check:` lines (the shared literal `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents`).

- decision: Split the pre-existing single-paragraph `## Version line` / `## バージョン系統` and `## Air-gapped / locked CI fallback (vendoring)` / `## エアギャップ …` sections into multiple paragraphs when adding the Codex-host sentence, rather than appending the new sentence onto the existing paragraph as one continuous block.
  reason: **AC5** and **AC6** each require the extracted section to hold at least three non-empty lines under a `grep -c .` instrument that counts physical source lines; a single continuous paragraph counts as exactly one non-empty line regardless of how much text it holds, so appending the Codex sentence in-line would leave both criteria red. Splitting into paragraphs is the only way to satisfy the line-count instrument without adding non-substantive filler.
  grounding: `.shell-team/specs/T-1149-distribution-workflow-two-hosts.md` — **AC5** and **AC6** `- check:` lines (`test "$(grep -c . "$r" || true)" -ge 3`); confirmed live during implementation (initial `check-acs.sh` run: AC5 and AC6 FAIL on the single-paragraph draft, PASS after the split, verified again with the file's own `grep -c .` count directly).

- decision: Wrote `## Two hosts, one loop` / `## 2 つのホスト、1 つのループ` as four (EN) / five (JA) separate paragraphs rather than the single continuous block the spec's own "Draft to refine" text shows, while keeping every literal token the draft and the ACs require.
  reason: Same line-count mechanism as above — **AC8** requires the extracted region to hold at least three non-empty lines, and the spec's own draft is one continuous paragraph. Splitting preserves every pinned token (`Claude Code`, `Codex CLI`, `spawn_agent`, `claude -p`, `READY_FOR_MERGE`, the five generated role names, `ui-designer`, `scrum-master`, the runbook link) while satisfying the depth check; the spec explicitly names this draft as "not text to paste blindly."
  grounding: `.shell-team/specs/T-1149-distribution-workflow-two-hosts.md` — **AC8**'s `- check:` line (`test "$(grep -c . "$r" || true)" -ge 3`) and `## Notes for engineer`'s "Draft to refine, not text to paste blindly" paragraph.

- decision: Round 2 (QA `host-parity-overclaim` fix) — narrow the lead paragraph's "either host" reachability claim to sub-agents and `bin/` helpers only, and state separately that the `/shell-team:<skill>` slash-command surface is a Claude Code mechanism, rather than measuring and stating what (if anything) a Codex CLI host can reach of skills.
  reason: QA's suggested remedy offered two paths — name only what ships per host, or measure and state the Codex-side skill reachability once known. Measuring the latter would require a new runbook-level fact this spec's read-set does not establish (the runbook documents no Codex-host mechanism for `skills/` at all), so stating it would risk a fresh unmeasured claim of the same class **AC11**/D-11 forbids for host-loading. Naming what already ships (sub-agents, `bin/` helpers) and naming the slash-command surface's actual host (Claude Code) keeps every sentence grounded in a mechanism this task's read-set already measured.
  grounding: `.shell-team/todo.md` — QA verdict `host-parity-overclaim` Major 1 (its "Suggested next step"); `.shell-team/specs/T-1149-distribution-workflow-two-hosts.md` — `## Summarized sources` (`bin/gen-codex-agents.sh` reads only `agents/<role>.md`, never `skills/`) and **AC11**/D-11 (no claim, positive or negative, about what a Codex CLI host loads).

- decision: Round 2 (QA `unqualified-invariant-overclaim` fix) — qualify the workflow section's review-provider sentence with "under the shipped default binding" / 出荷時の既定, using the register `README.md:353` / `README.ja.md:358` and `CLAUDE.md`'s "Working rules" already use for this exact caveat, rather than coining new wording.
  reason: QA's finding named the exact register to match ("matching the register CONTRIBUTING/CLAUDE.md already use for this exact caveat"). Reusing the shipped, already-reviewed phrasing avoids introducing a second, unreviewed formulation of the same disclaimer this repository has already settled on.
  grounding: `.shell-team/todo.md` — QA verdict `unqualified-invariant-overclaim` Major 2; `README.md:353` and `README.ja.md:358` (the shipped-default/rebind register); `CLAUDE.md` "Working rules" (the cross-provider binding is the shipped default, not an unconditional invariant).

- decision: Round 3 (QA `unmeasured-manifest-parity-claim` fix) — drop the round-2 explanatory clause about the shared manifest entirely and put the "Omitting `version`" sentence back exactly where the base blob placed it (immediately after the Claude Code block, before the `**Codex CLI:**` label), rather than rewording the clause to hedge it as unmeasured.
  reason: QA's finding showed `docs/adopting.md` carries zero occurrences of `plugin.json`, `.claude-plugin` or `manifest`, so no shipped runbook measures what a Codex CLI host's plugin manager reads — any wording asserting or hedging that fact would still be an unmeasured claim of the exact class **AC11**/D-11 forbids. The base blob's own placement (sentence directly after the Claude Code block) already conveys the "omitting version" caveat without naming Codex at all, so restoring that placement satisfies round 2's Minor 2 (the sentence's original position) and closes round 3's Major without introducing a new sentence.
  grounding: `.shell-team/todo.md` — QA verdict `unmeasured-manifest-parity-claim` Major (round 3, the sole finding); base blob `git show a28b01e9:docs/distribution.md` lines 81–90 and `git show a28b01e9:docs/distribution.ja.md` lines 51–60 (the sentence's original position, before any host split existed).

<!-- END provenance: T-1149 -->
