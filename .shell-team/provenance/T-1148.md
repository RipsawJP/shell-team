<!-- BEGIN provenance: T-1148 -->

- decision: in both README files' `## Install` honest-limits sentence, wrapped the bold markers (`**...**`) around the whole run `loads automatically is unmeasured from this repository` (English) and `このリポジトリからは測定` (Japanese) rather than closing the bold immediately after `unmeasured` / `測定` as the spec's own `## Notes for engineer` draft literally shows.
  reason: AC9's `- check:` line matches the fixed string `unmeasured from this repository` (English) and `このリポジトリからは測定` (Japanese) as a contiguous byte run against a whitespace-flattened copy of the section; `tr`/whitespace-flattening does not remove Markdown `**` characters, so any `**` landing inside a required fragment breaks `grep -qF`'s exact-substring match. The Notes draft's own bold placement (`**loads automatically is unmeasured**` followed by a space then `from this repository`) would have inserted `**` between `unmeasured` and ` from this repository`, splitting the very fragment AC9 requires contiguous — this was caught by re-reading the draft against AC9's literal string before writing either file, not by a failed check-acs run. Extending the bold to cover the whole fragment keeps the required bytes contiguous while preserving the same visual emphasis the draft intended.
  grounding: spec `.shell-team/specs/T-1148-readme-two-hosts.md` `## Notes for engineer` ("English honest limits, in the install section" draft: `**loads automatically is unmeasured**`) and `## Acceptance criteria` **AC9**'s `- check:` line (`grep -qF -- 'unmeasured from this repository'`, `grep -qF -- 'このリポジトリからは測定'`, both against `"$T/fen"`/`"$T/fja"`); `README.md` `## Install` (the sentence beginning "Which instruction surface a Codex CLI orchestrator **loads automatically is unmeasured from this repository**"); `README.ja.md` `## インストール` (the parallel sentence carrying `**このリポジトリからは測定**`); live re-run of AC9 via `check-acs.sh` after writing, observed PASS.

- decision: rewrote the "Codex-powered cross-provider Reviewer" apposition in `README.md`'s `## What shell-team is` to "an independent cross-provider Reviewer", and the bullet "Runs the final review through Codex CLI (OpenAI)..." to name the reviewing provider as host-relative ("the *other* provider from whichever host is driving the loop — Codex CLI on a Claude Code host, a `claude -p` pass on a Codex CLI host"), instead of naming a second, always-Codex reviewer bullet with a parenthetical carve-out for the Codex-host case.
  reason: D-1 requires both sentences to "stop attributing the review to one provider" without specifying replacement wording (the rewrite itself is recorded in the Body-to-AC correspondence table as info-only, judged by the reading gates rather than a `- check:` line), so the exact phrasing was a free choice among several that would satisfy D-1's intent. Naming the relationship structurally ("the *other* provider from whichever host is driving") rather than enumerating "Codex CLI, or Claude Code CLI on a Codex host" keeps the sentence readable as one clause and mirrors `docs/adopting.md`'s own step 10 framing (Claude reviews on a Codex CLI host precisely because Codex is "the provider the Codex CLI host is not"), which this task's `## Summarized sources` already cites for D-1's ground.
  grounding: spec `## Goal`, **D-1** ("the surrounding paragraph stops attributing the review to one provider... both are rewritten host-aware rather than left contradicting the new opening"); `## Body-to-AC correspondence` table row "The section's reviewer apposition and the review bullet stop attributing the review to one provider (D-1)" → "AC2 ... plus AC11's positive control ... the sentence-level rewrite itself is info-only"; `docs/adopting.md` `## Using shell-team from Codex CLI` intro paragraph ("closes the remaining gap by running its review on Claude instead of Codex — the provider the Codex CLI host is not"); `README.md` `## What shell-team is` (the rewritten apposition and second bullet); `README.ja.md` `## shell-team とは` (the parallel Japanese rewrite, not a word-for-word rendering per `## Non-goals`' "No word-for-word translation lock").

- decision: placed the new `## Update` heading immediately after `## Install` and before `## Usage` in both files, rather than at the end of the document or adjacent to `## Versioning`.
  reason: AC12 only pins a base-relative delta of exactly one new heading per file and asserts nothing about heading order, so placement was unconstrained by any `- check:` line. Update logically follows Install (the same two hosts, the same two command pairs, read together) and precedes the unrelated `## Usage`/`## When the full loop fits` material; `docs/distribution.md`'s own `## Update` section likewise sits directly after its install/adopting material rather than at the file's end, and this task's `## Summarized sources` already reads that file's `## Update` heading as the source of the Claude-side update command, so following the same relative placement keeps the two documents' section order comparable for a reader moving between them.
  grounding: spec **AC12**'s `- check:` line (asserts `^## ` count parity and the base-relative `+1` delta only; no order assertion); `docs/distribution.md` `## Update` (sits after the adopting-docs material, before `## Version line`); `README.md` (new `## Update` heading placed between `## Install` and `## Usage`); `README.ja.md` (new `## 更新` heading placed between `## インストール` and `## 使い方`).

- decision: in `README.md`'s `## Install` and `README.ja.md`'s `## インストール`, ordered the two host blocks Claude Code first, then Codex CLI, and gave the Codex CLI block three printed commands (two `codex plugin` lines plus the generator) against the Claude Code block's two commands, rather than a symmetric two-and-two split.
  reason: the spec's own `## Notes for engineer` states this exact ordering and asymmetry as the intended layout ("Claude Code first (it is the older path and the larger audience), Codex CLI second") and D-4 states the three-versus-two line count is "a measurement, not an oversight" — the Codex host's third line (the agent generator) is what makes the roles dispatchable at all on that host, so trimming it to match the Claude block's count would have been a claim rather than a measurement. This was a decision only in the sense of following the spec's stated preference rather than inventing a different, symmetric layout; recorded because it fixes the exact line count AC5 and AC6 verify.
  grounding: spec `## Notes for engineer` ("Layout of the install section... Claude Code first (it is the older path and the larger audience), Codex CLI second"); spec `## Goal` **D-4** ("the Codex block is three lines and the Claude block is two, and that asymmetry is a measurement, not an oversight"); `README.md` `## Install` (Claude Code block, two lines; Codex CLI block, three lines); **AC5** (`- check:` requiring all seven whole lines, unconditional on ordering) and **AC6** (requiring the three Codex-host command lines to match `docs/adopting.md` verbatim).

- decision: round 2 (answering QA round-1 FAIL) — in `README.md`'s `## Prerequisites` and `README.ja.md`'s `## 前提`, added a second pointer to the Codex CLI host's own sandbox grant (`docs/adopting.md#using-shell-team-from-codex-cli` steps 6 and 8, `docs/adopting.ja.md#codex-cli-から-shell-team-を使う` steps 6 and 8) beside the existing Claude-Code-host pointer (`docs/distribution.md#sandbox-enabled-permission-settings`), rather than narrowing the "on both hosts" claim to name only the one host the existing link covers; before writing the fix, inventoried every other sentence in both files that (a) claims coverage of both hosts / either host / whichever host, or names one host, and (b) carries or relies on a link, to confirm no sibling instance of the same defect class remained unaddressed.
  reason: QA round 1 FAIL, class `prerequisite-pointer-does-not-cover-the-host-it-claims` — the sentence read "Sandbox-enabled sessions need extra settings on both hosts" but linked only `docs/distribution.md#sandbox-enabled-permission-settings`, whose entire content (`.claude/settings.local.json`, `sandbox.excludedCommands`, `permissions.allow`) is Claude-Code-host material with no mention of a Codex CLI host's own `sandbox_workspace_write.writable_roots`/`.network_access` grant; the spec's own D-3 had asked for the sandbox bullet to "gain the Codex-host counterpart by pointing at the runbook's own sandbox steps", which round 1 never carried out. Adding the second pointer (rather than narrowing the claim) was chosen because both hosts genuinely do need extra settings — narrowing would have made the sentence true but less useful, where a second pointer makes it true and complete, matching D-3's own stated design intent and reusing the exact `[Using shell-team from Codex CLI](docs/adopting.md#using-shell-team-from-codex-cli)` / `[Codex CLI から shell-team を使う](docs/adopting.ja.md#codex-cli-から-shell-team-を使う)` link text this file already uses at line 47 and line 82 (line 47/82 in the Japanese file respectively), so no new link vocabulary was introduced. The class-closure inventory covered every other link-carrying, host-naming sentence in both files and found no second instance: the two Claude Code plugin / Codex CLI plugin badges each name one host and link to that same host's own material (`docs/distribution.md`; `docs/adopting.md#using-shell-team-from-codex-cli` / `.ja.md` equivalent); the reviewer badge ("reviewer: Codex") links to the in-file `#design-choices` anchor, which exists and is on-topic, and the badge names a provider rather than one of the two adopter hosts, so it carries no host-coverage claim against a link target to check; the two Prerequisites host bullets ("On a Claude Code host:" / "On a Codex CLI host:") each name one host and link only into that host's own material (the OpenAI Codex CLI docs; `docs/adopting.md#using-shell-team-from-codex-cli` step 7); the Install section's "These three printed lines are not the whole path" sentence names Codex CLI only and links to the matching runbook anchor; the honest-limits paragraph's "The loop's own stop points are the same on both hosts" clause carries no link of its own (it is grounded in the `## Design choices` reviewer bullet and in the runbook's own step 10, both already read and entailed in QA round 1's own entailment read) and so falls outside the link-carrying half of the class; the `.shell-team/`-in-git paragraph's closing `docs/adopting.md` pointer and the Install section's closing `docs/distribution.md` pointer ("Full details and the air-gapped fallback") and the Update section's closing `docs/distribution.md` pointer ("the full update path, version-line policy, and the air-gapped fallback") make no explicit both-hosts claim, and in both cases the actual per-host commands are already printed inline above the pointer, so the pointer supplies supplementary depth rather than being the sole source of a host-specific fact (`docs/distribution.md`'s own Codex-path coverage is task #565's, per this spec's D-10 and Non-goals, and is flagged here for that task rather than fixed in this one); the `## Design choices` "Single base dir, host root untouched" bullet's claim is confirmed host-neutral at `docs/adopting.md:35` ("Your host root is left untouched... does not edit your CLAUDE.md and does not append to your root .gitignore"), with no host qualifier in either the README bullet or the linked text; the `## Design choices` reviewer-binding bullet ("code-reviewer ships bound to Codex CLI... returns BLOCKED rather than falling back to Claude") carries no link at all, so it is outside the class by the same (b) filter, and the host-relative reviewer nuance it does not state is already carried, host-aware, by the linkless `## What shell-team is` bullet from round 1 (D-1); `## Versioning`'s CHANGELOG links and the `## Binding roles to executors` / telemetry / dispatch-axis bullets' `docs/adopting.md` anchors describe role/config mechanics orthogonal to which of the two adopter hosts is driving the loop and phrase no both-hosts coverage claim; `## Develop / dogfood` is the AC11-locked byte-identical region and untouched.
  grounding: board `.shell-team/todo.md` T-1148's `qa-verifier round 1` sub-bullet (FAIL, class `prerequisite-pointer-does-not-cover-the-host-it-claims`) and its `### QA verdict: FAIL — T-1148` block's `Failure`/`Reproduction`/`Suggested next step` lines; spec `.shell-team/specs/T-1148-readme-two-hosts.md` **D-3** ("the existing sandbox bullet stays and gains the Codex-host counterpart by pointing at the runbook's own sandbox steps rather than restating them") and **AC4**'s `- check:` line (requires the fixed string `docs/distribution.md#sandbox-enabled-permission-settings` present in the Prerequisites region, asserts nothing about a second pointer, so adding one cannot regress it); `docs/distribution.md:51`-`:59` (`## Sandbox-enabled permission settings`, entirely `.claude/settings.local.json` / `sandbox.excludedCommands` / `permissions.allow`, no mention of `sandbox_workspace_write`, `writable_roots`, `network_access` or a Codex CLI host); `docs/adopting.md` step 6 (`:485`-`:499`, "Grant the sandbox write access to `.git`... and, only if Codex itself runs step 3's generator inside the session, to `.codex` too") and step 8 (`:522`-`:535`, "Grant the sandbox network access the Claude-side review pass needs"); `docs/adopting.ja.md` steps 6 (`:492`) and 8 (`:536`), the same two step numbers in the Japanese runbook; `README.md:49` and `README.ja.md:49` (the rewritten sentence, both pointers present); a reading-probe mutation reverting the sentence to round 1's single-pointer shape in a `$TMPDIR` scratch copy (never the working tree) reproduced the exact defect QA reported — re-read against `docs/distribution.md:51`-`:59`'s own content, the reverted sentence again claimed "on both hosts" while linking only Claude-Code-only material — then discarded, `git status --short` on the real checkout confirmed to carry only the two intended README edits throughout.

<!-- END provenance: T-1148 -->
