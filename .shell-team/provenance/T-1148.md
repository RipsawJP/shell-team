<!-- BEGIN provenance: T-1148 -->

- decision: in both README files' `## Install` honest-limits sentence, wrapped the bold markers (`**...**`) around the whole run `loads automatically is unmeasured from this repository` (English) and `このリポジトリからは測定` (Japanese) rather than closing the bold immediately after `unmeasured` / `測定` as the spec's own `## Notes for engineer` draft literally shows.
  reason: AC9's `- check:` line matches the fixed string `unmeasured from this repository` (English) and `このリポジトリからは測定` (Japanese) as a contiguous byte run against a whitespace-flattened copy of the section; `tr`/whitespace-flattening does not remove Markdown `**` characters, so any `**` landing inside a required fragment breaks `grep -qF`'s exact-substring match. The Notes draft's own bold placement (`**loads automatically is unmeasured**` followed by a space then `from this repository`) would have inserted `**` between `unmeasured` and ` from this repository`, splitting the very fragment AC9 requires contiguous — this was caught by re-reading the draft against AC9's literal string before writing either file, not by a failed check-acs run. Extending the bold to cover the whole fragment keeps the required bytes contiguous while preserving the same visual emphasis the draft intended.
  grounding: spec `.shell-team/specs/T-1148-readme-two-hosts.md` `## Notes for engineer` ("English honest limits, in the install section" draft: `**loads automatically is unmeasured**`) and `## Acceptance criteria` **AC9**'s `- check:` line (`grep -qF -- 'unmeasured from this repository'`, `grep -qF -- 'このリポジトリからは測定'`, both against `"$T/fen"`/`"$T/fja"`); `README.md` `## Install` (the sentence beginning "Which instruction surface a Codex CLI orchestrator **loads automatically is unmeasured from this repository**"); `README.ja.md` `## インストール` (the parallel sentence carrying `**このリポジトリからは測定**`); live re-run of AC9 via `check-acs.sh` after writing, observed PASS.

- decision: rewrote the "Codex-powered cross-provider Reviewer" apposition in `README.md`'s `## What shell-team is` to "an independent cross-provider Reviewer", and the bullet "Runs the final review through Codex CLI (OpenAI)..." to name the reviewing provider as host-relative ("the *other* provider from whichever host is driving the loop — Codex CLI on a Claude Code host, a `claude -p` pass on a Codex CLI host"), instead of naming a second, always-Codex reviewer bullet with a parenthetical carve-out for the Codex-host case.
  reason: D-1 requires both sentences to "stop attributing the review to one provider" without specifying replacement wording (the rewrite itself is recorded in the Body-to-AC correspondence table as info-only, judged by the reading gates rather than a `- check:` line), so the exact phrasing was a free choice among several that would satisfy D-1's intent. Naming the relationship structurally ("the *other* provider from whichever host is driving") rather than enumerating "Codex CLI, or Claude Code CLI on a Codex host" keeps the sentence readable as one clause and mirrors `docs/adopting.md`'s own step 10 framing (Claude reviews on a Codex CLI host precisely because Codex is "the provider the Codex CLI host is not"), which this task's `## Summarized sources` already cites for D-1's ground.
  grounding: spec `## Goal`, **D-1** ("the surrounding paragraph stops attributing the review to one provider... both are rewritten host-aware rather than left contradicting the new opening"); `## Body-to-AC correspondence` table row "The section's reviewer apposition and the review bullet stop attributing the review to one provider (D-1)" → "AC2 ... plus AC11's positive control ... the sentence-level rewrite itself is info-only"; `docs/adopting.md` `## Using shell-team from Codex CLI` intro paragraph ("closes the remaining gap by running its review on Claude instead of Codex — the provider the Codex CLI host is not"); `README.md` `## What shell-team is` (the rewritten apposition and second bullet); `README.ja.md` `## shell-team とは` (the parallel Japanese rewrite, not a word-for-word rendering per `## Non-goals`' "No word-for-word translation lock").

- decision: placed the new `## Update` heading immediately after `## Install` and before `## Usage` in both files, rather than at the end of the document or adjacent to `## Versioning`.
  reason: AC12 only pins a base-relative delta of exactly one new heading per file and asserts nothing about heading order, so placement was unconstrained by any `- check:` line. Update logically follows Install (the same two hosts, the same two command pairs, read together) and precedes the unrelated `## Usage`/`## When the full loop fits` material; `docs/distribution.md`'s own `## Update` section likewise sits directly after its install/adopting material rather than at the file's end, and this task's `## Summarized sources` already reads that file's `## Update` heading as the source of the Claude-side update command, so following the same relative placement keeps the two documents' section order comparable for a reader moving between them.
  grounding: spec **AC12**'s `- check:` line (asserts `^## ` count parity and the base-relative `+1` delta only; no order assertion); `docs/distribution.md` `## Update` (sits after the adopting-docs material, before `## Version line`); `README.md` (new `## Update` heading placed between `## Install` and `## Usage`); `README.ja.md` (new `## 更新` heading placed between `## インストール` and `## 使い方`).

- decision: in `README.md`'s `## Install` and `README.ja.md`'s `## インストール`, ordered the two host blocks Claude Code first, then Codex CLI, and gave the Codex CLI block three printed commands (two `codex plugin` lines plus the generator) against the Claude Code block's two commands, rather than a symmetric two-and-two split.
  reason: the spec's own `## Notes for engineer` states this exact ordering and asymmetry as the intended layout ("Claude Code first (it is the older path and the larger audience), Codex CLI second") and D-4 states the three-versus-two line count is "a measurement, not an oversight" — the Codex host's third line (the agent generator) is what makes the roles dispatchable at all on that host, so trimming it to match the Claude block's count would have been a claim rather than a measurement. This was a decision only in the sense of following the spec's stated preference rather than inventing a different, symmetric layout; recorded because it fixes the exact line count AC5 and AC6 verify.
  grounding: spec `## Notes for engineer` ("Layout of the install section... Claude Code first (it is the older path and the larger audience), Codex CLI second"); spec `## Goal` **D-4** ("the Codex block is three lines and the Claude block is two, and that asymmetry is a measurement, not an oversight"); `README.md` `## Install` (Claude Code block, two lines; Codex CLI block, three lines); **AC5** (`- check:` requiring all seven whole lines, unconditional on ordering) and **AC6** (requiring the three Codex-host command lines to match `docs/adopting.md` verbatim).

- decision: round 2 (answering QA round-1 FAIL) — in `README.md`'s `## Prerequisites` and `README.ja.md`'s `## 前提`, added a second pointer to the Codex CLI host's own sandbox grant (`docs/adopting.md#using-shell-team-from-codex-cli` steps 6 and 8, `docs/adopting.ja.md#codex-cli-から-shell-team-を使う` steps 6 and 8) beside the existing Claude-Code-host pointer (`docs/distribution.md#sandbox-enabled-permission-settings`), rather than narrowing the "on both hosts" claim to name only the one host the existing link covers; before writing the fix, inventoried every other sentence in both files that (a) claims coverage of both hosts / either host / whichever host, or names one host, and (b) carries or relies on a link, to confirm no sibling instance of the same defect class remained unaddressed.
  reason: QA round 1 FAIL, class `prerequisite-pointer-does-not-cover-the-host-it-claims` — the sentence read "Sandbox-enabled sessions need extra settings on both hosts" but linked only `docs/distribution.md#sandbox-enabled-permission-settings`, whose entire content (`.claude/settings.local.json`, `sandbox.excludedCommands`, `permissions.allow`) is Claude-Code-host material with no mention of a Codex CLI host's own `sandbox_workspace_write.writable_roots`/`.network_access` grant; the spec's own D-3 had asked for the sandbox bullet to "gain the Codex-host counterpart by pointing at the runbook's own sandbox steps", which round 1 never carried out. Adding the second pointer (rather than narrowing the claim) was chosen because both hosts genuinely do need extra settings — narrowing would have made the sentence true but less useful, where a second pointer makes it true and complete, matching D-3's own stated design intent and reusing the exact `[Using shell-team from Codex CLI](docs/adopting.md#using-shell-team-from-codex-cli)` / `[Codex CLI から shell-team を使う](docs/adopting.ja.md#codex-cli-から-shell-team-を使う)` link text this file already uses at line 47 and line 82 (line 47/82 in the Japanese file respectively), so no new link vocabulary was introduced. The class-closure inventory covered every other link-carrying, host-naming sentence in both files and found no second instance: the two Claude Code plugin / Codex CLI plugin badges each name one host and link to that same host's own material (`docs/distribution.md`; `docs/adopting.md#using-shell-team-from-codex-cli` / `.ja.md` equivalent); the reviewer badge ("reviewer: Codex") links to the in-file `#design-choices` anchor, which exists and is on-topic, and the badge names a provider rather than one of the two adopter hosts, so it carries no host-coverage claim against a link target to check; the two Prerequisites host bullets ("On a Claude Code host:" / "On a Codex CLI host:") each name one host and link only into that host's own material (the OpenAI Codex CLI docs; `docs/adopting.md#using-shell-team-from-codex-cli` step 7); the Install section's "These three printed lines are not the whole path" sentence names Codex CLI only and links to the matching runbook anchor; the honest-limits paragraph's "The loop's own stop points are the same on both hosts" clause carries no link of its own (it is grounded in the `## Design choices` reviewer bullet and in the runbook's own step 10, both already read and entailed in QA round 1's own entailment read) and so falls outside the link-carrying half of the class; the `.shell-team/`-in-git paragraph's closing `docs/adopting.md` pointer and the Install section's closing `docs/distribution.md` pointer ("Full details and the air-gapped fallback") and the Update section's closing `docs/distribution.md` pointer ("the full update path, version-line policy, and the air-gapped fallback") make no explicit both-hosts claim, and in both cases the actual per-host commands are already printed inline above the pointer, so the pointer supplies supplementary depth rather than being the sole source of a host-specific fact (`docs/distribution.md`'s own Codex-path coverage is task #565's, per this spec's D-10 and Non-goals, and is flagged here for that task rather than fixed in this one); the `## Design choices` "Single base dir, host root untouched" bullet's claim is confirmed host-neutral at `docs/adopting.md:35` ("Your host root is left untouched... does not edit your CLAUDE.md and does not append to your root .gitignore"), with no host qualifier in either the README bullet or the linked text; the `## Design choices` reviewer-binding bullet ("code-reviewer ships bound to Codex CLI... returns BLOCKED rather than falling back to Claude") carries no link at all, so it is outside the class by the same (b) filter, and the host-relative reviewer nuance it does not state is already carried, host-aware, by the linkless `## What shell-team is` bullet from round 1 (D-1); `## Versioning`'s CHANGELOG links and the `## Binding roles to executors` / telemetry / dispatch-axis bullets' `docs/adopting.md` anchors describe role/config mechanics orthogonal to which of the two adopter hosts is driving the loop and phrase no both-hosts coverage claim; `## Develop / dogfood` is the AC11-locked byte-identical region and untouched.
  grounding: board `.shell-team/todo.md` T-1148's `qa-verifier round 1` sub-bullet (FAIL, class `prerequisite-pointer-does-not-cover-the-host-it-claims`) and its `### QA verdict: FAIL — T-1148` block's `Failure`/`Reproduction`/`Suggested next step` lines; spec `.shell-team/specs/T-1148-readme-two-hosts.md` **D-3** ("the existing sandbox bullet stays and gains the Codex-host counterpart by pointing at the runbook's own sandbox steps rather than restating them") and **AC4**'s `- check:` line (requires the fixed string `docs/distribution.md#sandbox-enabled-permission-settings` present in the Prerequisites region, asserts nothing about a second pointer, so adding one cannot regress it); `docs/distribution.md:51`-`:59` (`## Sandbox-enabled permission settings`, entirely `.claude/settings.local.json` / `sandbox.excludedCommands` / `permissions.allow`, no mention of `sandbox_workspace_write`, `writable_roots`, `network_access` or a Codex CLI host); `docs/adopting.md` step 6 (`:485`-`:499`, "Grant the sandbox write access to `.git`... and, only if Codex itself runs step 3's generator inside the session, to `.codex` too") and step 8 (`:522`-`:535`, "Grant the sandbox network access the Claude-side review pass needs"); `docs/adopting.ja.md` steps 6 (`:492`) and 8 (`:536`), the same two step numbers in the Japanese runbook; `README.md:49` and `README.ja.md:49` (the rewritten sentence, both pointers present); a reading-probe mutation reverting the sentence to round 1's single-pointer shape in a `$TMPDIR` scratch copy (never the working tree) reproduced the exact defect QA reported — re-read against `docs/distribution.md:51`-`:59`'s own content, the reverted sentence again claimed "on both hosts" while linking only Claude-Code-only material — then discarded, `git status --short` on the real checkout confirmed to carry only the two intended README edits throughout.

- decision: round 3 (answering codex-reviewer round-1 REQUEST_CHANGES, class `host-parity-overclaim`) — rewrote `README.md`'s `## What shell-team is` opening sentence (and `README.ja.md`'s parallel `## shell-team とは` sentence) to scope the "same roles" claim to the five roles that carry the status-flag chain to `READY_FOR_MERGE` (PM, Tech Lead, Engineer, QA, an independent cross-provider Reviewer), and to state explicitly that the UI Designer and Scrum-Master ship as Claude Code roles today while a Codex CLI host drives the five roles above through the runbook instead — rather than narrowing "either Claude Code or Codex CLI" to one host, or dropping the UI Designer/Scrum-Master mention from the paragraph entirely. Separately rewrote the lead-in sentence to the `## Usage` slash-command block in both files (`README.md`/`README.ja.md`) to name it a Claude Code surface and to state that a Codex CLI host drives the same five roles through the runbook's own dispatch instead, with no slash-command surface of its own — rather than leaving the block unscoped as round 2 shipped it.
  reason: the Major finding read the opening sentence's parity claim ("the same roles, the same status-flag gates, the same cross-provider review") together with the very next sentence's seven-role roster (UI Designer, Scrum-Master included) as a claim that all seven roles ship identically on both hosts; `bin/gen-codex-agents.sh:144`'s default `ROLES=` list generates exactly five Codex custom agents, and `docs/adopting.md`'s Codex CLI runbook never names `ui-designer` or `scrum-master` in its eleven steps, so a Codex-host reader (the spec's own Input-space class 2) would expect to spawn a role that does not exist there. Scoping the sentence to the five roles that actually carry the gate chain, and naming the other two roles' host today, closes the over-claim without deleting true information (the two roles do exist and do ship, just not as Codex-host-dispatchable roles) and without inventing a Codex-host capability that is not the fix this Major asked for. The reviewer's own suggested fix offered two options — narrow "the same roles" to the five, or explicitly mark the other two Claude-Code-only/opt-in — this decision does both at once because dropping the two roles from the sentence entirely would have silently removed true, useful information (the repository does ship seven agent definitions; `## Layout` and `## Phase flow` already document all seven) rather than merely fixing the false cross-host claim. Before writing the fix, inventoried every sentence in both READMEs carrying a parity-or-availability-across-hosts token (`same`, `either`, `both`, `whichever host`, `on both hosts`, `同じ`, `どちらの host`, `両host`) to confirm no sibling instance of the `host-parity-overclaim` class remained unaddressed, checked against what ships per host as measured first-hand rather than assumed: (1) `README.md:27`/`README.ja.md:27`, the opening sentence — the Major above, fixed as described. (2) `README.md:30`/`README.ja.md:30`, "so it never comes from the same model family as the implementation team" — dispositioned a Minor by the review (finding 2, pre-existing base-text pattern this task did not introduce); left as the review's own recorded disposition instructs, not addressed this round. (3) `README.md:49`/`README.ja.md:49`, "Sandbox-enabled sessions need extra settings on both hosts" — true and already fixed correctly in round 2 (both a Claude-Code-host pointer and a Codex-CLI-host pointer, each linking that host's own sandbox material); re-verified unchanged this round. (4) `README.md:53`/`README.ja.md:53`, "Install once per machine, on whichever host you use — both are shown below" — true: both host blocks are shown immediately below, verified present. (5) `README.md:84`/`README.ja.md:84`, "The loop's own stop points are the same on both hosts: merge and push stay yours, and when the independent reviewer cannot be reached the review returns BLOCKED" — genuinely identical on both hosts per `docs/adopting.md` step 10 (merge/push are the human's on both hosts) and the `BLOCKED`-on-unreachable-executor behaviour, which is host-invariant in `agents/code-reviewer.md`/`templates/prompt-blocks/host-dispatch.md`; the review's own Minor 3 already covers the narrower residual risk (self-detected-host misclassification) as a separate, already-disclosed-elsewhere gap outside this task's Non-goals, dispositioned file-an-issue — not addressed this round. (6) `README.md:76`/`README.ja.md:76` region ("the same host-neutral scaffolder the Claude Code block above drives through a slash command") — true: `bin/team-init.sh` is the same script both host paths invoke, one through the slash command's own dispatch and one through a direct `bash` invocation; no over-claim. (7) `## Usage`'s slash-command lead-in (`README.md`/`README.ja.md`, previously "It also works standalone..." / "明示的に使いたいときは...") — unscoped to either host in round 2's text, and the slash-command/`@`-mention syntax it lists is Claude-Code-specific: the Codex CLI runbook drives roles through `spawn_agent` calls and documents no slash-command surface anywhere in its eleven steps. Fixed this round as described above. (8) The immediately preceding "conversational usage" paragraph (`README.md:119`/`README.ja.md:119`, "The main Claude session recognizes...") already names "the main Claude session" explicitly — not a both-hosts claim, left unchanged. (9) `## Binding roles to executors` (`README.md:233`-`:275`) and `## Layout`/`## Phase flow` (`README.md:158`-`:212`) describe the plugin's own shipped file/config structure (six inner-loop roles' binding-config eligibility, all seven agent definition files, the phase diagram) rather than what a given host's generated agent set contains; these are accurate as descriptions of the repository's own shipped files regardless of host and carry no per-host availability claim, so they are outside the class. No further instance of the class was found beyond the two fixed above.
  grounding: `.shell-team/reviews/T-1148.md` round 1 (`REQUEST_CHANGES`, Major finding on `README.md:27`/`README.ja.md:27`, "Suggested fix: scope the parity claim to the five roles... and either drop ui-designer/scrum-master from the same sentence's immediate vicinity or explicitly mark them Claude-Code-only/opt-in-and-undocumented-for-Codex"; Minor 2 disposition "won't-fix for this task"; Minor 3 disposition "file-an-issue"); `bin/gen-codex-agents.sh:144` (`ROLES=(tech-lead pm-spec engineer qa-verifier code-reviewer)`, five entries, no `ui-designer`/`scrum-master`); `docs/adopting.md` `## Using shell-team from Codex CLI` (eleven numbered steps, `grep -c` for `ui-designer`/`scrum-master` across that section returns zero); `templates/binding-template.conf`/`bin/check-binding.sh` (six-role vocabulary: `tech-lead, pm-spec, engineer, qa-verifier, code-reviewer, ui-designer` — no `scrum-master`); `README.md` `## Layout` (`agents/scrum-master.md` and `agents/ui-designer.md` both listed as shipped agent definition files, independent of host) and `## Phase flow` (`[Design] ui-designer` conditional step, no host qualifier); `README.md`/`README.ja.md` post-edit, the rewritten `## What shell-team is`/`## shell-team とは` opening and the rewritten `## Usage`/`## 使い方` slash-command lead-in; `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1148-readme-two-hosts.md` (13 passed, 0 failed, 1 skipped, re-run after the edits, confirming AC2/AC10/AC11/AC12/AC13 all still PASS with the region-diff and heading-count checks unaffected by a change confined to prose inside two already-edited headings).

- decision: round 4 (answering codex-reviewer round-2 REQUEST_CHANGES, Blocker class `update-procedure-omits-runbook-check`) — added the runbook's own step-1 `VERSION`-column comparison and its conditional `codex plugin add shell-team@ripsawjp` re-run to both README files' `## Update` / `## 更新` Codex-host block, grounded verbatim in `docs/adopting.md:399`-`:410`'s "do not skip yet... compare `codex plugin list`'s `VERSION` column... take step 2's remedy" language; and separately added "gitignoring the generated agents" (the runbook's step 4) to the Install section's "not the whole path" sentence's deferred-prerequisite list in both files, rather than patching only the reported Update-section Blocker in isolation.
  reason: three consecutive gating rounds each found a different gap between the READMEs' Codex-path text and the runbook (QA r1: a host-coverage link gap; review r1: a role-parity overclaim; review r2: the Update section's missing check/conditional) — three instances of one meta-class, "a procedure the README prints or defers omits a step the runbook itself treats as load-bearing." Per this round's own instruction to close the meta-class rather than the instance, a full runbook-entailment pass was run before writing any fix: every one of the runbook's eleven numbered steps (`docs/adopting.md:386`-`:597`) was read against what each README sentence covers, defers by step number, or omits — the eleven-row result is recorded in this entry's grounding, and the same read was repeated against `docs/adopting.ja.md:394`-`:598` (matching step numbers and content) to confirm parity. The pass confirmed the reported Blocker (step 1, now fixed) and surfaced exactly one further live gap: the Install section's "not the whole path" sentence names five of the six steps 4-9 the task's own instructions enumerate (repository trust/5, sandbox writable roots/6, Claude Code CLI/7, network access/8, PATH export/9) but omitted the sixth (gitignoring the generated agents/4) — an omission the task's own brief named directly ("check that sentence's list against the steps and complete it"). Both are fixed in this round because both are the same class of defect (a runbook-mandated item silently absent from a printed-or-deferred README procedure) and deferring the second to a hypothetical round 5 would repeat the exact failure pattern the last two rounds already established. Three further candidate gaps were read and deliberately left unfixed, each for a stated reason rather than by omission: step 2's own presence-based staleness fallback (`bin/gen-codex-agents.sh` existence / role-list currency) is a narrower, one-time backward-compatibility check distinct from step 1's routine VERSION check this Blocker named, not raised by any of the three review rounds, and not one of the six steps 4-9 the task's own gap list enumerates — fixing it would be a new claim this round's own converging-correction discipline (no new mechanisms past the third correction round) argues against; step 2's plugin-root-location guidance is carried by the file's own pre-existing `<plugin root>` placeholder convention (unedited by any round of this task) rather than by a step-number deferral, and inventing one now would be new prose outside the reported class; step 10's dispatch mechanism (`spawn_agent`) is a loop-usage concern, not an Install/Update prerequisite, and its own human-gate half is already covered by AC9's honest-limits disclosure ("merge and push stay yours"), unaffected by this round.
  grounding: eleven-row runbook-entailment table (step # — what it requires — README.md/README.ja.md coverage — verdict), `docs/adopting.md:386`-`:597` cross-read against `docs/adopting.ja.md:394`-`:598` for parity: [1] install the plugin, with an already-installed `VERSION`-column check + conditional reinstall (`:386`-`:409`) — Install section prints the two install commands verbatim (AC5/AC6); the already-installed check+conditional reinstall is now printed in the Update section (this round's fix, `README.md`'s `## Update` / `README.ja.md`'s `## 更新`) — covered (split across Install for fresh-install, Update for the already-enabled/upgrade case, matching D-5's own placement of update-time procedure under `## Update`). [2] locate the installed plugin root (`SOURCE` column, no fixed layout) + a presence-based staleness fallback test (`:410`-`:442`) — the `<plugin root>` placeholder is used throughout per this file's own pre-existing Invocation-discipline convention (unedited by any round); the presence-based fallback is neither printed nor deferred by step number — deferred-by-convention (location) / no fix owed (fallback test: narrower one-time check, not named by any review round, not in the task's steps-4-9 list). [3] run the generator, `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents` (`:443`-`:465`) — printed verbatim in the Install section's Codex block — covered. [4] add `.codex/agents` to `.gitignore` (or `.git/info/exclude`) and commit (`:466`-`:473`) — deferred by name in the Install section's "not the whole path" sentence as "gitignoring the generated agents" — covered (this round's fix). [5] grant the repository Codex trust (`:474`-`:483`) — deferred by name in the same sentence as "repository trust" — covered. [6] grant the sandbox write access to `.git` (and `.codex` if the generator runs in-session) (`:484`-`:509`) — deferred by name as "the sandbox's writable roots"; the Prerequisites section (round 2) additionally names it by step number ("steps 6 and 8" / JA "手順 6・8") — covered. [7] confirm Claude Code CLI installed and authenticated (`:510`-`:528`) — the Prerequisites section states this and names it by step number ("step 7"); the "not the whole path" sentence also names it as "the Claude Code CLI prerequisite" — covered. [8] grant the sandbox network access the `claude -p` review pass needs (`:529`-`:547`) — deferred by name/number the same way as step 6 ("network access"; "steps 6 and 8") — covered. [9] export `<plugin root>/bin` onto `PATH` before starting codex (`:548`-`:558`) — deferred by name in the "not the whole path" sentence as "the `PATH` export" — covered. [10] start a Codex CLI session, dispatch a role via `spawn_agent`; `APPROVE` reaches `READY_FOR_MERGE`; merge and push stay human gates (`:559`-`:583`) — the dispatch mechanism itself is a loop-usage concern, not an Install/Update prerequisite, and is not printed or step-numbered in either section; its human-gate half is stated directly in the Install section's honest-limits paragraph ("merge and push stay yours"), matching AC9 — covered (human-gates half, via AC9) / out of scope for Install/Update (dispatch half, not named by any review round). [11] re-run step 3's command after a role-file edit, a plugin upgrade, or a `binding.conf`/`TEAM_RUN_BASE` change; `check-codex-agents.sh` reports drift (`:584`-`:597`) — the Update section's Codex follow-up covers the upgrade trigger by name (re-run the generator, run `check-codex-agents.sh`); the other two triggers are development-time concerns documented in the runbook itself, not Install/Update prerequisites — covered (upgrade trigger) / out of scope (the other two triggers). Sources read directly for this table: `docs/adopting.md:386`-`:597` (English runbook, all eleven numbered steps); `docs/adopting.ja.md:394`-`:598` (Japanese runbook, same eleven steps, same step numbers, `:473`-`:481` for step 4's gitignore text); `README.md`'s `## Install`/`## Update` and `README.ja.md`'s `## インストール`/`## 更新`, both pre- and post-edit; `.shell-team/reviews/T-1148.md`'s Round 2 Blocker (`docs/adopting.md:399`-`:410` quoted verbatim); live `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1148-readme-two-hosts.md` re-run after both edits, 13 passed / 0 failed / 1 skipped.

- decision: round 5 (answering codex-reviewer round-3 REQUEST_CHANGES, two Majors, class `per-machine-vs-per-repo-scope-unstated`) — in `README.md`'s `## Install` and `README.ja.md`'s `## インストール`, kept the Codex CLI block's three printed lines and the section's lead sentence's own bytes unchanged, and instead added one sentence between the fenced Codex block and the existing "Then initialize per-repo data once" sentence stating that the block's first two commands are the once-per-machine plugin install and the third (the generator) runs in each adopted repository, from that repository's own root, alongside `team-init.sh`; made the parallel addition to the Claude Code block ("Both commands above are the once-per-machine plugin install.") for symmetry with the lead sentence's own new promise ("stated separately, block by block"); and reduced both files' `## Update` / `## 更新` Codex-host follow-up to the two update commands (unchanged, AC7-pinned) plus one sentence pointing at the runbook's own step 1 version check and naming the per-repository regenerate-and-confirm follow-up — executing the spec's pre-commitment's Droppable 2nd disposition, which the loop's own record (`.shell-team/todo.md`, "pre-commitment fired" sub-bullet under this task) already ruled the factual trigger for and pre-authorized as the default with no new design decision owed.
  reason: round 3's two Majors, both independently confirmed by that round's adversarial pass, read the Install section's lead sentence ("Install once per machine, on whichever host you use") as covering the Codex block's third line (the generator) even though `docs/adopting.md:443`-`:465` (step 3) states the generator runs "from the adopted repository's own root" — a per-repository action — and found the same ambiguity unaddressed in the Update section's regen-and-check follow-up. The task's own constraint forbade touching any of AC5's seven pinned whole lines or the Codex block's three-line shape, so the fix had to live in the surrounding prose rather than in the printed commands themselves; adding one scoping sentence per block, naming which lines are per-machine and which are per-repository, closes the ambiguity without altering a single pinned literal or removing the generator from the block it must stay in (D-4: "the generated custom agents are what makes the roles dispatchable at all" on that host). The Claude Code side got a symmetric one-line addition because the revised lead sentence now promises the scope is "stated separately, block by block" — leaving the Claude Code block silent on the point that promise raises would itself be a fresh instance of the same unstated-scope class one paragraph over. The Update-section reduction executes the spec's own pre-commitment rather than adding more procedural detail, because the board's `pre-commitment fired` entry already ran the four-question means-ends reflection and found all three execution conditions holding (never-dropped AC10/AC5/AC6 green and untargeted; the firing hits the Droppable-2nd-priced component and not the Install-section Major, which is ordinary rework on a different, first-hit component; the pre-authorized drop order names the Update follow-up as the 2nd item; the aggregate cost of a further detail round is at least one more review round on prose the runbook already owns) — a fourth engineer round adding yet more procedure to the Update section, rather than executing the pre-priced reduction, would have repeated the exact pattern (round 2's Blocker, round 3's Major) the pre-commitment exists to stop. The removed VERSION-column comparison, conditional `codex plugin add` block and "step 1's own remedy" attribution travel to issue #567 rather than being re-derived in the README, per the spec's own pre-commitment text ("both dropped parts travel to a fresh issue carrying both rounds' findings as their requirement list, never re-derived there") — the issue number itself is not written into either README, consistent with the project's public-artifact hygiene rule that a record identifier is not adopter guidance.
  grounding: `.shell-team/reviews/T-1148.md` Round 3 (`REQUEST_CHANGES`, two Majors: `README.md:53`-`74`/`README.ja.md`'s parallel block, "Install once per machine" framing includes the per-repository generator; `README.md:107`-`115`/`README.ja.md`'s parallel block, the Update follow-up never states it applies per adopted repository; both confirmed by the round's own adversarial pass, per the Synthesis audit ledger rows 1-2); `docs/adopting.md:443`-`465` (step 3, "Run the generator from your own shell, from the adopted repository's own root"); board `.shell-team/todo.md` T-1148's `pre-commitment fired` sub-bullet (four-question means-ends reflection, all three execution conditions holding, disposition: "the Update Codex block is reduced to the two commands plus a bare pointer at the runbook, and the dropped detail... travels to issue #567"); spec `## Pre-commitment` ("Droppable 2nd: the `## Update` section's Codex follow-up sentence reduced to a bare pointer at the runbook. Both dropped parts travel to a fresh issue carrying both rounds' findings as their requirement list, never re-derived there"); spec **D-4** ("the generated custom agents are what makes the roles dispatchable at all" on the Codex host — why the generator stays in the three-line block rather than being split out); **AC5**'s `- check:` line (unchanged, all seven whole lines re-verified present post-edit); **AC7**'s `- check:` line (the two update commands as whole lines, plus the substrings `bash "<plugin root>/bin/gen-codex-agents.sh"` and `check-codex-agents.sh`, both carried by the new pointer sentence — `bash "<plugin root>/bin/check-codex-agents.sh"` written in full in that sentence, which is also the exact literal **AC6** requires to appear verbatim somewhere in each README); `README.md`/`README.ja.md` post-edit `## Install` (the two new scoping sentences) and `## Update`/`## 更新` (the reduced follow-up sentence); a reading-probe mutation in a scratch copy outside the repository (removing only the new Codex-block scoping sentence) reproduced the exact round-3 reading — with the sentence gone, the block again carries nothing distinguishing the generator's per-repository scope from the two per-machine commands above it, so the lead sentence's generic "once per machine" again reads as covering all three lines — then discarded, `git status --short` on the real checkout confirmed to carry only this round's two intended README edits and this provenance entry; live `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1148-readme-two-hosts.md` re-run after both edits, 13 passed / 0 failed / 1 skipped.

- decision: round 6 (answering codex-reviewer round-4 REQUEST_CHANGES, Major class `per-machine-vs-per-repo-scope-unstated`, operator GO for round 6) — in `README.md`'s `## Update` section and `README.ja.md`'s `## 更新` section, inserted the qualifier "from that repository's own root" (English) / "そのリポジトリそのものの root から" (Japanese) into the existing Codex-host regen-and-confirm pointer sentence, reusing the exact English wording and the equivalent Japanese phrasing the Install section's own round-5 fix already carries for the sibling generator-scoping sentence, rather than rewriting the sentence's structure, re-adding round 5's dropped VERSION-check/conditional-reinstall detail, or touching any other line.
  reason: round 4's Major found the Update section's reduced follow-up sentence ("in each repository where you use shell-team, re-run the generator... and confirm with...") states the per-repository scope but omits the second half of round 3's own suggested fix — the "own root" qualifier — that the sibling Install-section fix, written in the same round 5 commit, does carry verbatim; both `bin/gen-codex-agents.sh` and `bin/check-codex-agents.sh` default `--out-dir` to `$PWD/.codex/agents`, so running the Update section's two commands from a subdirectory of the adopted repository silently regenerates and validates a nested, wrong `.codex/agents` path while the repository root's real one stays stale, with `check-codex-agents.sh` reporting "in sync" against the wrong path. The operator's round-6 GO scoped this round to exactly this one addition ("one round adding the 'from that repository's own root' qualifier to the Update pointer sentence in both languages"), and the review's own suggested fix named reusing the Install section's own wording rather than inventing new prose. Copying the Install section's already-reviewed phrase (rather than paraphrasing) keeps both sentences' scoping claims byte-consistent with each other and with `docs/adopting.md:443`-`:444`'s own runbook language, and avoids introducing a fourth wording for the same fact across the two sections. No other sentence, heading, or AC-pinned literal (AC7's four fixed strings, AC6's four Codex command strings, AC10's forbidden-phrase list) is touched by this one-clause insertion; re-run `check-acs.sh` after the edit confirms all 13 non-runtime ACs still PASS.
  grounding: `.shell-team/reviews/T-1148.md` Round 4 (`REQUEST_CHANGES`, Major on `README.md:107`/`README.ja.md`'s parallel line, "add 'from that repository's own root' (or equivalent) to the Update section's regen-and-check sentence in both files, mirroring the Install section's own round-5 wording"; Synthesis audit ledger row 1, `check-codex-agents.sh`'s own success message reproduced live as printing the exact `$PWD`-relative path it checked); board `.shell-team/todo.md` T-1148's `operator answer` sub-bullet ("`T-1148 round 6 GO` — one round adding the 'from that repository's own root' qualifier to the Update pointer sentence in both languages; another gating finding after this returns the task to planning"); `docs/adopting.md:443`-`:444` (step 3, "Run the generator from your own shell, from the adopted repository's own root"); `README.md`'s `## Install` section's own round-5 sentence ("the generator — runs in each repository you adopt shell-team into, from that repository's own root, alongside the `team-init.sh` step below"), copied verbatim into the Update section's sentence; `README.ja.md`'s `## インストール` section's own round-5 sentence ("3 行目の generator は、適用先リポジトリそのものの root から、そのリポジトリごとに実行する"), whose "そのものの root から" phrase is reused in the Update section's sentence; `README.md`/`README.ja.md` post-edit `## Update`/`## 更新` (the two-line-diff insertion); `git diff --stat -- README.md README.ja.md` (2 files changed, 2 insertions(+), 2 deletions(-), confirming exactly one line changed per file); live `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1148-readme-two-hosts.md` re-run after the edit, 13 passed / 0 failed / 1 skipped.

<!-- END provenance: T-1148 -->
