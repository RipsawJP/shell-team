<!-- BEGIN provenance: T-1134 -->

- decision: gated the `model` / `model_reasoning_effort` emitting branch in `bin/gen-codex-agents.sh` on the resolved row's ADAPTER field equalling the literal `codex-cli`, rather than on the resolved row's PROVIDER field equalling `codex`.
  reason: D3-model's own wording names the trigger as "a role whose resolved adapter is a Codex adapter" — the adapter field, not the provider field — and today the two are 1:1 (the sole Codex adapter is bound to the sole Codex provider), but the spec's own vocabulary singles out the adapter token as the gate, and gating on it is the more literal reading that also survives unchanged if a future adapter definition reuses the `codex` provider token for a materially different invocation shape.
  grounding: `.shell-team/specs/T-1134-codex-host-slice1.md` `## Decision points ruled in this spec`, row D3-model ("emitted **only** for a role whose resolved adapter is a Codex adapter"); AC4's own check line reads `model`/`model_reasoning_effort` absence against the shipped default's `claude-cli` rows, consistent with either gate under today's 1:1 mapping.

- decision: scoped the control-character refusal (Input space class 7, D5) to the WHOLE source file's bytes — including the frontmatter region between the two `---` markers — rather than to the extracted body alone.
  reason: the spec's own Input space item 7 states the reachable class as "a source file containing a control character other than tab and newline", file-scoped wording rather than body-scoped; a control character sitting in the frontmatter (an unlikely but reachable corruption) would not reach the emitted `developer_instructions` string either way, but refusing on it too is the more conservative reading of "a source file containing" and costs nothing against any real shipped role file, all of which carry ordinary ASCII frontmatter.
  grounding: `.shell-team/specs/T-1134-codex-host-slice1.md` `## Input space`, reachable class 7 ("A source file containing a control character other than tab and newline, a lone carriage return included."); AC3's own check line only exercises the body-appended case, so both scopings satisfy it identically and this decision is not itself discriminated by any `- check:` line.

- decision: placed the new `templates/prompt-blocks/host-dispatch.md` block's spliced text in `skills/run/SKILL.md` between the existing "Executor resolution (T-1057)" paragraph and the "Alternate-executor invocation (T-1118)" paragraph, rather than at the top of the file or immediately before the numbered phase list.
  reason: the block explains which host dispatches a role through which mechanism and names the Alternate-executor recipe as a Claude-Code-host-only mechanism with no Codex counterpart — a reader needs the Executor resolution paragraph's role-binding context first, and needs to read the host-dispatch distinction immediately before the Alternate-executor section it directly qualifies, rather than encountering that qualification only after already reading the recipe it modifies.
  grounding: `skills/run/SKILL.md`'s own paragraph order (the "Executor resolution (T-1057)" and "Alternate-executor invocation (T-1118)" paragraphs sit adjacent, in that order, both before this change and after it); `.shell-team/specs/T-1134-codex-host-slice1.md` `## Notes for engineer` ("the alternate-executor recipe as a whole ... is a Claude-Code-host mechanism ... and the new block should say so rather than leaving a Codex reader to work it out").

- decision: (rework round 1, QA AC9 finding) changed `bin/gen-codex-agents.sh`'s `--root` default from the caller's current working directory (`"."`) to the script's own plugin root (the parent of its own `bin/` directory, resolved the same symlink-safe way `SCRIPT_DIR` already is), rather than leaving the cwd default in place and only fixing the documentation's invocation text.
  reason: QA's live run showed the documented `bash bin/gen-codex-agents.sh` fails with no working fallback from a genuine adopter's repository (no `bin/`, no `agents/` of its own); a docs-only fix would still leave the flag defaulting to "read whatever's in the caller's cwd", which is wrong for the tool's own stated purpose (deriving agents from *this plugin's* `agents/*.md`) and would keep failing for any adopter who runs the command with `--out-dir` only, matching the documented form literally. Changing the default itself closes the class rather than one reported invocation string.
  grounding: `.shell-team/todo.md` `### QA verification — T-1134 (qa-verifier)`, the AC9 finding paragraphs ("Both `docs/adopting.md` step 1 and `docs/adopting.ja.md` step 1 ... instruct ... `bash bin/gen-codex-agents.sh` ... exit 127"); this rework round's own briefing, Finding A item 1 ("`--root` defaults to the generator's own plugin root ... never the current directory").

- decision: (rework round 1) changed `--out-dir`'s default from `"$ROOT/.codex/agents"` to `"$PWD/.codex/agents"` — deliberately independent of `--root` rather than relative to it — and mirrored both default changes onto `bin/check-codex-agents.sh`, whose own defaults were `--root="."` / `--out-dir="$ROOT/.codex/agents"` before this round even though the briefing named only the generator.
  reason: once `--root` defaults to the plugin's own tree (a location an adopter did not choose and should not need to know), an out-dir still keyed off `--root` would write the generated TOMLs inside the plugin install rather than into the adopted repository the adopter is standing in — the opposite of the documented and intended location. Mirroring the same two defaults onto the checker was the only option that keeps the shipped pair internally consistent: an adopter running both commands with no flags in the same repository would otherwise have the generator write into `$PWD/.codex/agents` while the checker read from `./.codex/agents` relative to a `--root` default that no longer means "this repository" — two different directories under the one no-flags invocation shape the documentation now advertises for both.
  grounding: `bin/gen-codex-agents.sh` header comment, `--out-dir` bullet ("NOT --root's directory ... an adopter running the default form expects the output in the repository they are standing in"); `docs/adopting.md` `## Using shell-team from Codex CLI`, step 1 ("Same shape for `check-codex-agents.sh`", this round's briefing, Finding A item 3).

- decision: (rework round 1, QA AC10 finding) documented the Codex `.git` sandbox-write requirement as a new numbered step (step 3, between the existing "grant Codex trust" and "start the session" steps) in `docs/adopting.md` / `.ja.md`, plus one added sentence in `templates/prompt-blocks/host-dispatch.md`'s Codex CLI paragraph (mirrored verbatim into `skills/run/SKILL.md`), rather than folding it into the existing trust-granting step or leaving it only in the "Honest limits" paragraph.
  reason: the requirement is a distinct precondition an adopter must act on *before* starting a session in which a role will commit, not a passive limitation to be aware of after the fact (which is what the "Honest limits" paragraph already covers for `sandbox_mode`) and not a variant of the trust-granting step (a different Codex config surface — `sandbox_workspace_write.writable_roots` / `config.toml`, not `trust_level`). A dedicated numbered step is the shape the surrounding list already uses for exactly this kind of "do this before you start" precondition.
  grounding: this rework round's own briefing, Finding B ("state the requirement, with the measured reason, in `docs/adopting.md` / `.ja.md` (a numbered step before starting the session, plus one sentence in the honest-limits paragraph)"); the spec's `## Assumptions`, item 4 ("Adding the repository's `.git` directory to the sandbox's writable roots ... lets `git commit` succeed").

- decision: (rework round 2, QA round-2 finding `docs-assume-dogfood-layout` reopened on the `.codex` write) closed the newly-measured `codex-sandbox-protected-dir` class (`.git`, `.codex`, `.agents` all refused under `codex exec --sandbox workspace-write`) by **extending the existing `.git` step 3 and honest-limits paragraph to also name `.codex`**, plus one script-header note in `bin/gen-codex-agents.sh`, rather than by adding a new numbered step, a new script flag, or any change to the scripts' actual mkdir/write logic.
  reason: this round's own briefing frames the class as a documentation gap, not a code defect — the generator's and checker's default-resolution logic (round 1's fix) already behaves correctly; what was missing is telling the adopter that having Codex itself execute step 1 or step 5 in-session hits the same sandbox policy `.git` already hits, for a second, Codex-owned directory. Extending step 3 (rather than inserting a new step) keeps the "grant sandbox write access before starting a session" guidance in one place instead of splitting one concept — the sandbox's writable-roots requirement — across two list items with the reader left to notice they are the same mechanism. No script logic changes because the measured remedy (run from your own shell, or add `.codex` to writable-roots) requires no new code path; a retry-with-different-flags or a `--skip-mkdir-check` option would invent a mechanism this task's Non-goals and Pre-commitment do not call for and the spec's own `## Assumptions` does not ask this task to build around the CLI's policy.
  grounding: this rework round's briefing ("Same-class-2 ... State the class once, as a set, at each applying site ... Keep the block sentence a pointer to the docs for the exact invocation, as it is now"); `.shell-team/todo.md` `### QA verification — T-1134 (qa-verifier, round 2)`, Finding A closure sub-item (b) ("codex exec --sandbox workspace-write ... protects a repo-local .codex/ directory from every write, structurally analogous to its existing .git/ protection ... and this is not documented"); `.shell-team/specs/T-1134-codex-host-slice1.md` `## Assumptions`, item 4 as extended at QA round 2 ("the protected set is `.git`, `.codex` and `.agents` ... the documented remedy is to run the generator from the adopter's own shell, or to add `.codex` to the writable roots alongside `.git`").

- decision: (rework round 2) added the `.codex` sandbox note to `bin/gen-codex-agents.sh`'s own `--out-dir` header bullet (not only to `docs/adopting.md`), and updated the script's `--help` extraction range (`sed -n '2,89p'` → `'2,98p'`) to keep printing the full header after the note lengthened it.
  reason: the header comment is this script's own first-line documentation, read directly by `--help` and by anyone opening the file — leaving the sandbox note only in `docs/adopting.md` would mean the one artifact most likely to be read at the moment the failure occurs (the script itself, via `--help` or its source) says nothing about why `mkdir -p "$OUT_DIR"` failed. Updating the `--help` range was mechanically required by the addition: the hardcoded end-line number is exactly the citation this repository's own lesson calls out as going stale silently, so it had to be re-derived (the new last comment line, "--out-dir.") rather than left at its old value, which would have truncated `--help`'s own Exit section.
  grounding: `bin/gen-codex-agents.sh`'s own header comment, `--out-dir` bullet (now carrying "NOTE: under Codex CLI's own `codex exec --sandbox workspace-write` ..."); the script's own `--help|-h)` case line, whose `sed -n` range must cover the full header for `bash bin/gen-codex-agents.sh --help` (this round's own re-run, tail -20, confirmed the full updated header including the Exit section prints).

- decision: (rework round 4, QA round-3 finding `docs-overclaim-sandbox-requirement`) narrowed the `.codex` writable-root remedy at all four sites (`docs/adopting.md` step 3 + honest-limits, `docs/adopting.ja.md` mirror, `templates/prompt-blocks/host-dispatch.md`'s Codex-host paragraph, its `skills/run/SKILL.md` consumer copy) to `bin/gen-codex-agents.sh` only, and added an explicit sentence stating `bin/check-codex-agents.sh` needs no such grant — rather than silently dropping every mention of the checker from those sites, or leaving the grouped "generator or checker" wording in place with a caveat appended elsewhere.
  reason: QA's own reproduction (existing out-dir → exit 0; missing out-dir → exit 1 with the checker's own "missing" violations; neither case producing a permission error under plain `--sandbox workspace-write`, no `.codex` writable-root) shows the checker's `--out-dir` touches are read-only (`cmp`, `[ -f ]`, `[ -d ]`, a glob against a path it never `mkdir`s), so the prior grouped claim was not merely imprecisely worded but factually false for the checker. Stating the exemption plainly (rather than dropping the checker from the sentence with no explanation) matches this task's own established practice for `sandbox_mode`'s "not an enforcement boundary" disclosure — telling the reader what does *not* apply is treated as part of the honest-limits obligation, not as scope creep — and it is the reasoning QA cited as already correct in `bin/check-codex-agents.sh`'s own header comment, now carried into the four adopter/instruction-facing sites that had not matched it.
  grounding: `.shell-team/todo.md` `### QA verification — T-1134 (qa-verifier, round 3)`, Finding (c) ("Root cause, read from `bin/check-codex-agents.sh` itself: it never creates `--out-dir` ... every touch of `$OUT_DIR` is a read ... this is exactly the round's own 'not-apply' reasoning for the script's *header comment*") and its "Suggested next step" ("narrow the `.codex`-writable-root remedy to `gen-codex-agents.sh` (step 1) only, and state plainly that `check-codex-agents.sh` (step 5) needs no such grant"); `bin/check-codex-agents.sh`'s own header comment, "check-only: this script NEVER writes into --out-dir".

- decision: (rework round 5, Codex review round 1 Major 1 — `refusal-class-incomplete`) detected a NUL byte (0x00) in a role file's source by byte-count comparison (`wc -c` on the source vs. `wc -c` after `LC_ALL=C tr -d '\000' < "$SRC"`), as a check separate from and preceding the existing bracket-expression `grep` over `\x01`–`\x1f`/`\x7f`, rather than prepending `\x00` to that same bracket expression, switching to `grep -P '\x00'`, or scanning with `od`/`awk`.
  reason: prepending `\x00` to the existing `grep -q $'[...]'` pattern is the fix that looks obvious and does nothing: bash's own ANSI-C quoting (`$'...'`) truncates a string at its first NUL byte when building an argv entry, so the pattern argument grep would receive is silently truncated before grep ever runs — this is the actual root cause the review's reproduction exposed, not a wrong byte range. `grep -P` was excluded by this round's own briefing (not portable). `od`/hex-scanning was rejected because concatenating hex-digit pairs from an `od` dump and grepping for the literal substring `00` risks a false match straddling two adjacent bytes (e.g. `a0` next to `0a`) that never formed an actual NUL. A byte-count comparison via `tr -d '\000'` never needs to pass a NUL through argv at all — `'\000'` in the `tr` command string is four ordinary ASCII characters that `tr` itself interprets as an octal escape, not an embedded NUL — and is portable (GNU and BSD `tr` both support octal escapes in a delete set).
  grounding: `bin/gen-codex-agents.sh`'s new comment block directly above the byte-count check ("bash's own $'\\x00' ANSI-C quoting truncates ... a single `grep -q $'[...\\x00...]'` invocation can never actually search for a NUL"); `.shell-team/reviews/T-1134.md` Major 1 ("The character class in the script is `[\\x01\\x02...\\x1f\\x7f]` — it starts at `\\x01` and never includes `\\x00`... Suggested fix: extend the byte-range check to include `\\x00`") — the suggested one-line fix is exactly the form this decision declines, for the argv-truncation reason recorded here; own mutation self-check (disabled the new check, reproduced the review's exact repro shape — `rc=0`, 4 files written — restored byte-identically, reproduced the refusal).

- decision: (rework round 5, Codex review round 1 Major 2 — `docs-missing-adopter-procedure`, trust-granting instance) stated the Codex trust-granting command in `docs/adopting.md`/`.ja.md` step 2 as `-c 'projects."<repo>".trust_level="trusted"'` on the `codex` invocation, with the equivalent `config.toml` form as an alternative in the same sentence, reusing the `<repo>` placeholder step 3's writable-roots override already establishes, rather than describing Codex's interactive first-launch trust prompt or coining a new placeholder token.
  reason: the spec's own `## Assumptions` item 3/4 and every QA round's own live run used the `-c 'projects."<abs path>".trust_level="trusted"'` form measured against codex-cli 0.154.0 — this is the one this task's own AC10 recipe actually exercised, and the round's converging constraint (every environment statement traces to a measured `## Assumptions` item) rules out describing the interactive prompt, which no `## Assumptions` item measures. Reusing `<repo>` rather than introducing a second placeholder (e.g. `<repo path>`) keeps the two adjacent config-override instructions (trust, then writable-roots) visibly the same shape and the same referent.
  grounding: `.shell-team/specs/T-1134-codex-host-slice1.md` `## Assumptions`, item 3 ("trust for a fixture repository is granted per-invocation with `-c 'projects."<abs path>".trust_level="trusted"'`"); `.shell-team/reviews/T-1134.md` Major 2 ("state the measured `-c 'projects."<repo path>".trust_level="trusted"'` config-override form... in step 2, in both languages"); `docs/adopting.md` step 3's own pre-existing `<repo>` placeholder in `-c 'sandbox_workspace_write.writable_roots=["<repo>/.git"]'`.

- decision: (rework round 5, Codex review round 1 Major 3 — `docs-missing-adopter-procedure`, adopter's-own-`.gitignore` instance) added the adopter's-own-`.gitignore` instruction as one sentence at the end of `docs/adopting.md`/`.ja.md` step 1, rather than at step 5 (the re-run/refresh step) or as a new standalone step.
  reason: step 1 is where the out-dir (`.codex/agents/`) is first created in an adopter's repository, so the instruction to ignore it belongs at the point the untracked directory first comes into existence rather than deferred to a later step the adopter might run days afterward, in the interim leaving `git status --short` non-empty and the T-073 gate tripped on every commit in between. Step 5 already carries a different, narrower `.gitignore` fact (that the generated TOMLs are not committed, addressed to *this* repository's own entry) — appending the adopter's-own-repository instruction there too would either duplicate or blur the two claims; step 1 is the one place this task's own diff had not yet said anything about the adopter's own ignore rules.
  grounding: `.shell-team/reviews/T-1134.md` Major 3 ("add one sentence to step 1 or step 5... mirroring this repository's own comment"); `.gitignore`'s own comment above its `.codex/agents` entry ("this checkout's own out-dir stays untracked exactly like every other adopter's, and never trips the T-073 clean-tree guard") — the sentence added to step 1 mirrors this wording; QA round 2's own risk note (quoted in the review) naming the T-073 seam as the concrete consequence.

- decision: (rework round 5) added the NUL fixture as a fifth case inside the existing `for bad in noeol ctrl nofm` refusal loop in `tests/codex-agents/run.sh` (renamed to `for bad in noeol ctrl nul nofm`), rather than as a new, separately-named fixture block matching AC7's five-fixed-string shape.
  reason: the four cases in that loop already share one assertion shape (refused, non-zero, zero `*.toml` files written) and one Input-space grouping (the spec's own Input space items 6–8, refusal classes beyond AC7's own two fixed strings) — NUL is the same refusal class (Input space item 7, control character other than tab/newline) as the existing `ctrl` case (form feed), so it belongs in the same loop rather than as a new standalone block that would duplicate the loop's own fixture-copying and assertion code for one more case. This keeps the diff to one line changed (the `for` list) plus one `case` arm, and still emits its own distinct `PASS: T-1134 extra: 'nul' source is refused with no file written` line the round's instructions require.
  grounding: this round's own briefing ("Add a fixture in `tests/codex-agents/run.sh` for the NUL case, alongside the form-feed / carriage-return probes... new `PASS:` line; AC7's five lines must remain, round-4's 20 a pure subset"); `tests/codex-agents/run.sh`'s own comment above the loop ("adversarial coverage: the three [now four] other refusal classes... the same Input space classes 6-8 the generator itself must refuse").

- decision: (rework round 6, answering cross-provider review round 2 Major "no documented way to install the plugin into Codex CLI") added the two-command install sequence (`codex plugin marketplace add RipsawJP/shell-team` then `codex plugin add shell-team@ripsawjp`) as a new first numbered step directly inside `docs/adopting.md`'s and `docs/adopting.ja.md`'s existing Codex section, rather than adding a mirrored Codex install block to `README.md`/`README.ja.md`'s `## Install` section as the review's own suggested fix proposed first.
  reason: the frozen spec's own Non-goals freezes "No change to `README.md` or `README.ja.md`" verbatim, explaining that the README's host claim is a release-facing statement that moves with the `MINOR` slice 2 cuts, not with this unversioned slice — editing either README would violate a frozen Non-goal that this round has no authority to reopen. The review's own suggested fix already offered "and/or a forward reference from `docs/adopting.md` step 1's opening sentence" as an alternative; since there is nowhere else in this repository's shipped documentation that states the Codex install commands (the READMEs are Claude-Code-only today, by design, per the same frozen Non-goal), the self-contained form — stating the two commands directly in the section that already carries every other Codex-host adopter action — is the only option that both closes the gap and leaves the frozen Non-goal untouched.
  grounding: `.shell-team/specs/T-1134-codex-host-slice1.md` `## Non-goals` ("No change to `README.md` or `README.ja.md`. The README's host claim is a release-facing statement; it moves with the `MINOR` that slice 2 cuts, not with an unversioned slice that ships the mechanism."); `.shell-team/reviews/T-1134.md` round 2 Major 1 ("Suggested fix: add a mirrored Codex install block to `README.md`/`.ja.md`'s `## Install` section ..., and/or a forward reference from `docs/adopting.md` step 1's opening sentence"); this round's own briefing ("do not touch the READMEs ... A forward reference from the docs section is the whole answer here").

- decision: (rework round 6, answering cross-provider review round 2 Major "the claimed Codex plugin cache path is factually wrong") replaced the fixed `<home>/.codex/plugins/cache/<marketplace>/shell-team/<version>/` parenthetical with an instruction to read the exact `SOURCE` column value `codex plugin list` prints, stated as "whatever shape it has on your host" with the previously-measured value kept only as a disclosed example (not a guaranteed pattern) — and reworded the parallel Claude Code clause the same way ("read the cache path that exists on your own machine"), rather than leaving the Claude Code half as an unqualified fixed pattern.
  reason: the spec's own `## Assumptions` item 9 already carries the corrected measurement ("the exact `SOURCE` column value `codex plugin list` prints for the plugin, with no assumed shape") from this same review's round-2 finding, so the docs text is brought into agreement with an already-corrected spec assumption rather than independently re-deriving it. The review's own finding asked, as one of two acceptable fixes, that "both [hosts]" be phrased as "read, do not assume" — qualifying only the Codex clause and leaving the Claude Code clause as a bare fixed pattern would repeat the exact defect class (a specific path presented as guaranteed rather than measured-on-one-host) the review just found on the Codex side.
  grounding: `.shell-team/specs/T-1134-codex-host-slice1.md` `## Assumptions`, item 9 ("Codex: the exact `SOURCE` column value `codex plugin list` prints for the plugin, with no assumed shape (corrected at review round 2, 2026-09-11: on this host that value is the marketplace clone ... whose root is the plugin because the marketplace entry's source is `./`"); `.shell-team/reviews/T-1134.md` round 2 Major 2 ("replace the fixed-pattern parenthetical with 'the exact `SOURCE` column value `codex plugin list` prints for this plugin' (no assumed shape), matching what the doc already does correctly for Claude Code only if that pattern is re-verified too, or state both as 'read `SOURCE` directly, do not assume a fixed layout' for both hosts").

- decision: (rework round 6, answering cross-provider review round 2 Major "the adopter's own `.gitignore` sentence creates the exact class of gap it was added to close") appended "commit that change before starting the loop... or add the entry to `.git/info/exclude` instead, which needs no commit" to the same sentence in both languages, rather than moving the instruction to a later step or introducing a new step dedicated to committing it.
  reason: the review's own suggested fix states this exact sentence verbatim as sufficient ("append '...' to the same sentence, in both languages") and explicitly frames the round-1 gap as its own prior suggestion's incompleteness rather than a new defect requiring a different placement — keeping the instruction at step 1 (now step 4 after the renumbering the install-step insertion required), where the untracked directory and the `.gitignore` edit are both first created, avoids splitting one atomic adopter action (add the entry, then commit it) across two list items.
  grounding: `.shell-team/reviews/T-1134.md` round 2 Major 3 ("Suggested fix: append 'commit this change before starting the loop (or add the entry to `.git/info/exclude` instead, which needs no commit)' to the same sentence, in both languages"); the same finding's own framing ("This is my own round-1 suggested fix's own incompleteness surfacing on its literal implementation, not a new defect the engineer introduced independently — owned here rather than pinned on the rework").

- decision: (rework round 6) restructured the Codex section from 5 numbered steps into 8, inserting the new install step as step 1 and renumbering every subsequent step and every in-text cross-reference to a step number (the trust-granting step, the writable-roots step, the dispatch step, the re-run step, and every "see step N" / "step N's checker" reference in both the numbered list and the "Honest limits" paragraph), rather than keeping the install command as an unnumbered lead-in sentence before a still-5-item list.
  reason: this round's briefing specifies the runbook as "a numbered runbook an adopter runs top to bottom, each step one copyable command or one file edit, in this order" with 8 named items; leaving the install command as prose outside the numbered sequence would make it easy for an adopter to skip when scanning the numbered list, and would leave the "which step am I on" cross-references (several of which already existed pre-round, e.g. "see step 3") silently wrong the moment a step is inserted before them — the same stale-cross-reference risk this repository's own lesson corpus already names for line-number citations, applied here to step-number citations instead. Renumbering every cross-reference in the same edit, rather than leaving any at their pre-insertion values, is what keeps the runbook internally consistent.
  grounding: this round's own briefing ("Restructure the Codex section as a numbered runbook an adopter runs top to bottom, each step one copyable command or one file edit, in this order: (0) install ... (1) locate ... (2) generate ... (3) gitignore ... (4) trust ... (5) writable roots ... (6) start session ... (7) re-run"); `docs/adopting.md`'s own pre-round-6 text, which already cross-referenced step numbers by position ("see step 3", "step 5's checker") — re-derived via `grep -n 'step [0-9]' docs/adopting.md` before and after the edit to confirm every reference was updated to its new target.

- decision: (rework round 7, Codex review round 3 Major — `docs-missing-adopter-procedure`, stale-install instance) added the stale-install fallback (`codex plugin marketplace upgrade <marketplace>`, then re-run `codex plugin add`; or use a checkout of this repository as `<plugin root>`) as a new paragraph inside step 2 (where the located plugin root is first inspected), and represented "a version at or above the one these docs ship with" via the file's own presence (`bin/gen-codex-agents.sh` existing at the located root) rather than naming a literal version number.
  reason: step 1 only inspects `codex plugin list`'s STATUS column; step 2 is where the located root's actual contents are inspected, so testing for the presence of `bin/gen-codex-agents.sh` — the exact file step 3 needs and the exact file this round's Major reproduced as absent on the maintainer's own installed marketplace clone — belongs at the point the root is already in hand, matching the review's own instruction to state the fix "once where the located root is first inspected (step 1's skip branch or step 2)". A literal version number cannot be named honestly: the frozen spec's own `## Summarized sources` records "this slice merges **unversioned**; `MINOR` is cut with slice 2", so at the time these docs ship there is no version number yet that contains this feature for a skip gate to compare against — the file's own presence is the only measured, non-invented stand-in available, and connecting it to the already-named checkout fallback (rather than inventing a third remedy) keeps the round's converging constraint of no new mechanism.
  grounding: `.shell-team/reviews/T-1134.md` round 3 Major ("add one sentence to step 1 (or step 2, where the located root is first inspected) stating that if the located plugin root's `bin/gen-codex-agents.sh` is missing, either run `codex plugin marketplace upgrade <marketplace>`... or use a checkout of this repository as `<plugin root>` instead"); `.shell-team/specs/T-1134-codex-host-slice1.md` `## Summarized sources` ("this slice merges **unversioned**; `MINOR` is cut with slice 2"); live measurement this round — `codex plugin marketplace upgrade --help` on `codex-cli 0.154.0` reads "Refresh configured Git marketplace snapshots... Omit MARKETPLACE_NAME to upgrade all configured Git marketplaces"; `ls ~/.codex/.tmp/marketplaces/ripsawjp/bin/gen-codex-agents.sh` → "No such file or directory", reproducing the review's own maintainer-host finding live on this same host, `codex plugin list` reporting that install at `2.5.6`.

- decision: (rework round 7, Codex review round 3 Minor — skip-gate `enabled` gap) tightened step 1's skip condition from "the plugin already shows as installed" to requiring the exact STATUS value `installed, enabled`, in both languages, rather than leaving the looser "shows as installed" wording with a separate enablement caveat appended elsewhere.
  reason: the review's Minor finding names the literal gap — a disabled plugin also shows up in `codex plugin list`'s output, so a "shows as installed" reading would incorrectly skip step 1 for a disabled install, with no generated agents ever appearing later and no traceback to this cause. Live measurement this round on `codex plugin list` (non-shell-team rows on this same host) confirms the STATUS column genuinely carries both `installed, enabled` and `not installed` as distinct values side by side, so requiring the two-word value verbatim is a real, checkable condition rather than a hypothetical one, and stating it once in the skip clause itself (rather than as a trailing caveat) is the more literal reading of "require installed, enabled specifically" the review's own suggested fix names.
  grounding: `.shell-team/reviews/T-1134.md` round 3 Minor ("step 1's skip condition is satisfied by `codex plugin list` reporting the plugin in **any** installed state, but Codex's own STATUS column distinguishes `installed, enabled` from a disabled state... Fast-follow: require 'installed, enabled' specifically"); live `codex plugin list` output this round showing `installed, enabled` and `not installed` as distinct STATUS values on the same host (e.g. `chrome@openai-bundled  not installed` beside `browser@openai-bundled  installed, enabled`).

- decision: (rework round 7, Codex review round 4 Major — undelivered "Honest limits" spawned-role `bin/*.sh` disclosure sentence) added the one owed sentence to the end of `docs/adopting.md`'s and `docs/adopting.ja.md`'s "Honest limits" paragraph, naming the spawned-role `PATH`/`cwd` gap, the `team-paths.sh`/`check-acs.sh`/`bin/*.sh` bare-name calls, step 3's non-coverage, the session-side `PATH`-prepend workaround every measured live run actually used, and GitHub issue #486 as the substantive-fix tracker — rather than opening a new paragraph, a new step, or editing any other section.
  reason: review round 4's own Major is scoped narrowly to one specific, previously-promised sentence ("owed within this task's remaining rework, not deferred" — this review's own round 2 fast-follow disposition), not to a new mechanism or a second disclosure; the third-round-plus converging constraint in force this round (no new mechanism, no new claim, primary-sources-first) rules out anything beyond that one sentence. Appending it to the existing "Honest limits" paragraph — rather than a new paragraph or step — matches that paragraph's own established practice of stating every other limitation plainly in the same place (`sandbox_mode` non-enforcement, the `.git`/`.codex` refusals, the `READY_FOR_REVIEW`-only stop point), which round 2's own suggested fix explicitly pointed at ("matching the paragraph's existing practice of stating other limitations plainly"). Verified GitHub issue #486 exists, open, and is the exact routing issue this gap names (title: "Codex host: a spawned role's own bin/*.sh calls assume bin/ is reachable from its own execution context — the generated agents need a PATH/cwd resolution the one-time generator bootstrap does not give them") before citing it, rather than trusting the orchestrator's relayed issue number at face value.
  grounding: `.shell-team/reviews/T-1134.md` round 4 Major ("add one sentence to the 'Honest limits' paragraph disclosing that a spawned role's own command execution assumes `bin/` is reachable from its own execution context (`PATH` or a checkout-rooted `cwd`), and that this is not addressed by the one-time `gen-codex-agents.sh` bootstrap in step 3 — pointing at the file-an-issue this review's own round 2 fast-follow disposition names for the substantive fix, in both languages"); same file, round 2 Minor #7's own suggested fix ("add one 'Honest limits' sentence in `docs/adopting.md` disclosing that a dispatched role's own commands assume `bin/` is reachable from its own execution context, pointing at that issue — matching the paragraph's existing practice of stating other limitations plainly"); live `curl -s https://api.github.com/repos/RipsawJP/shell-team/issues/486` this round, confirming the issue's existence, open state, and exact title match before the sentence was written.

<!-- END provenance: T-1134 -->
