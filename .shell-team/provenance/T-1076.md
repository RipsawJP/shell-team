# T-1076 decision provenance

<!-- BEGIN provenance: T-1076 -->
- decision: split the contention suite's two arms so only the AC10 negative control carries the 16384+-byte `--error` payload, while the AC9 positive/main 8x20 arm carries no `--error` value at all (both arms share the same N=8/M=20 writer/row floors, and the single printed `contention-parameters` line reports `payload_bytes=16384` — the value the negative control actually uses — rather than a value the positive arm also carries).
  reason: measured live (this session, this repo's own stock bash 3.2.57 dev host) that `bin/check-run.sh`'s own unbalanced-quote detection (`unq="${line//\\\\/}"; unq="${unq//\\\"/}"; quotes="${unq//[!\"]/}"`) exhibits a severe super-quadratic slowdown once a JSON string field passes a few kilobytes — direct isolated benchmarks of that exact substitution chain: 500B→0.03s, 1000B→0.13s, 2000B→0.82s, 4000B→5.7s (third stage) / 8000B→20s (second stage, tested separately), 16384B→did not complete within 120s. `bin/check-run.sh` is Non-goal'd (must stay byte-identical to its branch-point blob, AC13/AC15) so this cannot be fixed here. D6's own text scopes the multi-kilobyte payload requirement to the negative control's own sentence ("run at the same or higher concurrency with multi-kilobyte --error payloads"), not to the main case, so keeping the positive arm's rows small is a reading the frozen text itself supports, and it is what keeps `bin/check-run.sh bash tests/log-run/run.sh`'s `contention-check-run-clean` assertion computationally tractable (the positive arm's file is the one actually linted for that assertion). The negative control's own detection logic never invokes `bin/check-run.sh` at all (line count + derived seq-set exactness only), for the identical reason.
  grounding: bin/check-run.sh:205-207 (the three-stage unbalanced-quote substitution); `.shell-team/specs/T-1076-log-run-locking.md` D6 ("run at the same or higher concurrency with multi-kilobyte --error payloads (at least 16384 bytes)"); `.shell-team/test-recipe.md`'s T-1076 entry (the measured benchmark numbers, recorded there for the next task that touches this writer or its readers).
- decision: gave INT/TERM their own `on_lock_signal` handler that calls `exit "$2"` explicitly (mirroring `bin/check-intent.sh`'s and `bin/check-refreeze-class.sh`'s existing `on_signal` shape), rather than a single combined `trap release_lock EXIT INT TERM`.
  reason: measured live that the combined-trap form is a real, reproducible bug, not a style preference — bash's own documented trap semantics mean a caught INT/TERM signal that does not call `exit` inside its handler only RUNS the handler and then RESUMES the script from where it was interrupted; a `sleep 5` injected right after lock acquisition (this task's own `lock-released-on-signal` fixture) proved this directly: with the combined-trap form, `kill -TERM` on the holder released the lock immediately (correct) but the script then continued past the interrupted `sleep`, computed `--seq auto` and appended a row anyway — the fixture's own "the killed holder should not have appended a row" assertion caught it on the first run, before the fix.
  grounding: bin/log-run.sh's `on_lock_signal` function and its `trap 'on_lock_signal INT 130' INT` / `trap 'on_lock_signal TERM 143' TERM` lines; `bin/check-intent.sh:198-204` and `bin/check-refreeze-class.sh:133-139` (the pre-existing repo convention this reuses); `tests/log-run/run.sh`'s `lock-released-on-signal` case (the fixture whose failure, before this fix, is what surfaced the bug).
- decision: `TEAM_LOG_LOCK_TIMEOUT` follows `bin/check-acs.sh`'s own `CHECK_ACS_TIMEOUT` shape exactly — an UNSET value is substituted with the default via `${VAR:-10}` before validation ever runs (silent, no warning), while a PRESENT value that fails validation (non-integer, zero, or negative) falls back to the same default but additionally warns on stderr naming the variable — rather than warning on every invocation that simply never sets the variable at all (which D2's own prose, read most literally, could be taken to require, since it lists "absent" alongside "non-integer, zero or negative" as values that "fall back... and emit a warning").
  reason: D2 itself names `bin/check-acs.sh`'s own shape as the model to follow ("following the same shape bin/check-acs.sh already uses for CHECK_ACS_TIMEOUT"), and that script's own `acs_timeout="${CHECK_ACS_TIMEOUT:-120}"` line conflates "absent" with "already the default" before its validation `case` ever runs, so an absent value never reaches the warning branch there either; matching that precedent literally means an ordinary invocation that never sets `TEAM_LOG_LOCK_TIMEOUT` (which is every invocation in this repository today — no caller sets it) does not print a stderr line on every single append. No `- check:` line in the frozen spec asserts silence on the absent case one way or the other, so this is a genuine implementation choice between two readings of one prose sentence, resolved in favor of the shape the sentence itself names as the model.
  grounding: `.shell-team/specs/T-1076-log-run-locking.md` D2 ("following the same shape bin/check-acs.sh already uses for CHECK_ACS_TIMEOUT"); `bin/check-acs.sh`'s `acs_timeout="${CHECK_ACS_TIMEOUT:-120}"` line and its adjacent invalid-value `case` block.
- decision: chose whole-second `sleep 1` retry granularity for the lock's bounded-wait polling loop, rather than measuring and adopting a fractional-second granularity.
  reason: D2 explicitly leaves this as the engineer's choice ("the engineer either measures that it works and records the measurement, or uses whole-second steps"); whole seconds are simpler, need no portability measurement of this repo's own `/bin/sh` or of an adopter's, and are sufficient for the acceptance criteria's own bounds (AC6 requires only that a `TEAM_LOG_LOCK_TIMEOUT=1` refusal return within 10 wall-clock seconds, which whole-second polling satisfies with room to spare). Measured separately, for the record only (not load-bearing for this choice): fractional `sleep 0.2` does work on this session's own sandboxed bash 3.2.57, but the choice was made for portability toward an unknown adopter host, not because fractional sleep was unavailable here.
  grounding: `.shell-team/specs/T-1076-log-run-locking.md` D2 ("Retry granularity is measured on the host, not assumed... the engineer either measures that it works and records the measurement, or uses whole-second steps"); `.shell-team/test-recipe.md`'s T-1076 entry (records the fractional-sleep measurement and the whole-second choice together).
- decision: (round 2, Codex review round 1 Blocker) closed both `LOCK_ACQUIRED` signal-timing windows by bracketing the mkdir-then-flag-set transition and the flag-reset-then-rmdir transition inside `release_lock` with `trap '' INT TERM` .. re-arm, and additionally reordered `release_lock` to flip `LOCK_ACQUIRED=0` BEFORE calling `rmdir` (not after), rather than adopting Codex's own alternative suggestion of an explicit acquiring/held/releasing state machine.
  reason: a signal delivered while a signal's disposition is SIG_IGN (`trap ''`) is discarded by the kernel outright, never queued for later delivery — this is a stronger and simpler guarantee than "defer and re-deliver," and it closes the exact windows the reviewer's trap-timing probe demonstrated (acquire-side: a signal between `mkdir` success and `LOCK_ACQUIRED=1`; release-side: a signal between `rmdir` success and `LOCK_ACQUIRED=0` re-entering `release_lock` while the flag still read "held", which could `rmdir` a SUCCESSOR's freshly-created lock at the same path) without introducing a new state variable or new state-transition logic to itself get wrong. The flag-flip-before-rmdir reordering is defense in depth, not load-bearing by itself under the trap mask (the mask alone already makes the pair atomic with respect to signal delivery): with it, a hypothetical stray re-entry from OUTSIDE the masked bracket fails SAFE (skips the rmdir, matching D3's own never-steal instinct) rather than fails open (attempts a second rmdir). Verified with a mutation self-check: two A/B pairs of scratch mutants (an OLD-SHAPE mutant structurally reproducing round-1's reviewed code with an artificially widened window via an injected `sleep 3`, against a FIXED-SHAPE mutant carrying the identical widened window but with the mask/reorder intact) were built and driven through the exact interleavings the reviewer's probe exercised — see `tests/log-run/run.sh`'s `signal-race-acquire-side` and `signal-race-release-side` cases, both newly added this round. The OLD-SHAPE acquire mutant reproduces the orphan (exit 143, lock left behind); the OLD-SHAPE release mutant reproduces the theft (a live successor's lock directory is deleted out from under it, confirmed via a marker-file-timed choreography with a slow successor writer); the FIXED-SHAPE mutants of both show the opposite outcome under the identical injected window.
  grounding: `bin/log-run.sh`'s `release_lock` function and the acquire `while :; do ... done` loop (the `trap '' INT TERM` / re-arm brackets around each transition); `.shell-team/reviews/T-1076.md` (round 1 Blocker, both windows and Codex's own suggested-fix text); `tests/log-run/run.sh`'s `signal-race-acquire-side` and `signal-race-release-side` cases (the regression pins, run live this round).
- decision: (round 2, Codex review round 1 Major #1) fixed `compute_auto_seq`'s comparison by escaping the caller's raw `run_id` argument with `jesc` before comparing (matching the file's own stored, escaped form) AND widened the extraction regex from `[^"]*` to `(\\.|[^"\\])*` (treating an escaped pair as one atomic unit so an embedded `\"` no longer truncates the capture early) — but deliberately did NOT add a new character-class validation to `--run-id` (leaving it, as before, validated only for non-emptiness), even though the finding named that as a coherent alternative/companion fix.
  reason: the escaping-plus-extraction fix alone fully closes the actual defect (a silent counter restart) for ANY `run_id` value, including ones containing a backslash or a double quote — jesc's own two-step transform (backslash-double, then quote-escape) is now applied identically on both the write side and the compare side, so the comparison is correct regardless of the value's shape. Adding a NEW character-class restriction on `--run-id` is not required to close this defect and carries its own risk: the frozen spec's intent block imposes no grammar on `--run-id` beyond non-emptiness, no frozen `- check:` line exercises one, and the finding itself demanded that any new class "must not reject any run_id shape this repo's own shipped callers and existing runs files already use" — verified here by enumerating the actual shapes: `bin/close-out.sh`'s one executing call site (a generated id, `--seq 1` unconditional — grounded below), `skills/run/SKILL.md`'s five prompt-text invocation sites (recommending timestamp-style ids), and the machine-local corpus's own `run_id` values (`20260802T052149Z-t1016`-shaped, colon-free timestamp+task-id pairs) — none of which motivate or need a narrower class, so inventing one now would be pure unrequested scope against no concrete failing input. Left as a follow-up if a real narrower-class need is ever measured.
  grounding: `bin/log-run.sh`'s `compute_auto_seq` function (`run_id_field_re` and the `want_run_id_esc` comparison) and its `jesc` function; `.shell-team/reviews/T-1076.md` (round 1 Major #1 finding and its own "may" framing of the validation alternative); `bin/close-out.sh` (the one executing caller, grounded in the frozen spec's own Summarized sources); `skills/run/SKILL.md` (the five invocation sites, same source); `tests/log-run/run.sh`'s `seq-auto-escaping` case (the new regression, run live this round, covering `a\b`, `a"b`, `a\"b`, `a\\b`).
- decision: (round 2, Codex review round 1 Major #2) tracked each background contention writer's own PID and its exit status individually (each of the N per-writer subshells now latches non-zero across its own M sequential invocations and `exit`s that latch), and — in the negative-control arm only — reported a non-zero writer-process count as its OWN named clause in the `neg_findings` text, distinct from the line-count/seq-set clauses, rather than inventing a third AC10 outcome token.
  reason: AC10's own frozen check line requires the negative-control PASS line and the provenance `- negative-control:` line to match one of exactly two tokens (`detected`/`not-detected`) — a third "writer-infrastructure-failure" token would violate that frozen grammar, so the distinction the finding asked for ("distinguish writer infrastructure failed from mutant produced corrupt output explicitly") has to live in the descriptive TEXT beside the token, not in a new token. Folding a writer-process failure into `detected` (rather than silently discarding it or forcing `not-detected`) is the conservative reading: a background writer that crashed IS a genuine deviation from the positive arm's fully-clean baseline (which asserts, as a new check, that ALL its own writers exit 0), so it is honestly `detected`-worthy evidence even when it is not itself proof of interleaved-append corruption — and the text now names which kind of deviation actually occurred, so a human or a later provenance reader is not misled into reading a writer crash as raw byte-interleaving.
  grounding: `tests/log-run/run.sh`'s `run_contention_writer` (per-PID `wait`, `WRITER_FAILED_COUNT`/`WRITER_FAILED_PIDS`) and the positive-arm `contention-writer-exit-status` assertion (new this round) plus the negative-arm `writer-process-failure=` clause; `.shell-team/reviews/T-1076.md` (round 1 Major #2 finding and its own caution that "exit-3 refusals or similar may be legitimate outcomes of lock-disabled chaos"); **AC10**'s check line (the frozen two-token grammar this decision works within).
<!-- END provenance: T-1076 -->

## AC10 — negative control outcome

- negative-control: detected — the lock-disabled mutant's contention run produced a duplicate or a gap in the derived `seq` set for the shared `run_id` (two independently observed runs of `tests/log-run/run.sh`, both reporting `seq-set-mismatch` — the classic --seq-auto TOCTOU race: two concurrent processes reading the same file state before either writes back, independent of raw write()-level byte interleaving)

