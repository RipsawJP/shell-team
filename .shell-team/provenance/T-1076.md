# T-1076 decision provenance

<!-- BEGIN provenance: T-1076 -->
- decision: split the contention suite's two arms so only the AC10 negative control carries the 16384+-byte `--error` payload, while the AC9 positive/main 8x20 arm carries no `--error` value at all (both arms share the same N=8/M=20 writer/row floors, and the single printed `contention-parameters` line reports `payload_bytes=16384` — the value the negative control actually uses — rather than a value the positive arm also carries).
  reason: measured live (this session, this repo's own stock bash 3.2.57 dev host) that `bin/check-run.sh`'s own unbalanced-quote detection (`unq="${line//\\\\/}"; unq="${unq//\\\"/}"; quotes="${unq//[!\"]/}"`) exhibits a severe super-quadratic slowdown once a JSON string field passes a few kilobytes — direct isolated benchmarks of that exact substitution chain: 500B→0.03s, 1000B→0.13s, 2000B→0.82s, 4000B→5.7s (third stage) / 8000B→20s (second stage, tested separately), 16384B→did not complete within 120s. `bin/check-run.sh` is Non-goal'd (must stay byte-identical to its branch-point blob, AC13/AC15) so this cannot be fixed here. D6's own text scopes the multi-kilobyte payload requirement to the negative control's own sentence ("run at the same or higher concurrency with multi-kilobyte --error payloads"), not to the main case, so keeping the positive arm's rows small is a reading the frozen text itself supports, and it is what keeps `bin/check-run.sh bash tests/log-run/run.sh`'s `contention-check-run-clean` assertion computationally tractable (the positive arm's file is the one actually linted for that assertion). The negative control's own detection logic never invokes `bin/check-run.sh` at all (line count + derived seq-set exactness only), for the identical reason.
  grounding: bin/check-run.sh:205-207 (the three-stage unbalanced-quote substitution); `.shell-team/specs/T-1076-log-run-locking.md` D6 ("run at the same or higher concurrency with multi-kilobyte --error payloads (at least 16384 bytes)"); `.shell-team/test-recipe.md`'s T-1076 entry (the measured benchmark numbers, recorded there for the next task that touches this writer or its readers).
- decision: gave INT/TERM their own `on_lock_signal` handler that calls `exit "$2"` explicitly (mirroring `bin/check-intent.sh`'s and `bin/check-refreeze-class.sh`'s existing `on_signal` shape), rather than a single combined `trap release_lock EXIT INT TERM`.
  reason: measured live that the combined-trap form is a real, reproducible bug, not a style preference — bash's own documented trap semantics mean a caught INT/TERM signal that does not call `exit` inside its handler only RUNS the handler and then RESUMES the script from where it was interrupted; a `sleep 5` injected right after lock acquisition (this task's own `lock-released-on-signal` fixture) proved this directly: with the combined-trap form, `kill -TERM` on the holder released the lock immediately (correct) but the script then continued past the interrupted `sleep`, computed `--seq auto` and appended a row anyway — the fixture's own "the killed holder should not have appended a row" assertion caught it on the first run, before the fix.
  grounding: bin/log-run.sh's `on_lock_signal` function and its `trap 'on_lock_signal INT 130' INT` / `trap 'on_lock_signal TERM 143' TERM` lines; `bin/check-intent.sh:198-204` and `bin/check-refreeze-class.sh:133-139` (the pre-existing repo convention this reuses); `tests/log-run/run.sh`'s `lock-released-on-signal` case (the fixture whose failure, before this fix, is what surfaced the bug).
- decision: `TEAM_LOG_LOCK_TIMEOUT` follows `bin/check-acs.sh`'s own `CHECK_ACS_TIMEOUT` shape exactly — an UNSET value is substituted with the default via `${VAR:-10}` before validation ever runs (silent, no warning), while a PRESENT value that fails validation (non-integer, zero, or negative) falls back to the same default but additionally warns on stderr naming the variable — rather than warning on every invocation that simply never sets the variable at all (which D2's own prose, read most literally, could be taken to require, since it lists "absent" alongside "non-integer, zero or negative" as values that "fall back... and emit a warning").
  reason: D2 itself names `bin/check-acs.sh`'s own shape as the model to follow ("following the same shape bin/check-acs.sh already uses for CHECK_ACS_TIMEOUT"), and that script's own `acs_timeout="${CHECK_ACS_TIMEOUT:-120}"` line conflates "absent" with "already the default" before its validation `case` ever runs, so an absent value never reaches the warning branch there either; matching that precedent literally means an ordinary invocation that never sets `TEAM_LOG_LOCK_TIMEOUT` (which is every invocation in this repository today — no caller sets it) does not print a stderr line on every single append. No `- check:` line in the frozen spec asserts silence on the absent case one way or the other, so this is a genuine implementation choice between two readings of one prose sentence, resolved in favor of the shape the sentence itself names as the model.
  grounding: `.shell-team/specs/T-1076-log-run-locking.md` D2 ("following the same shape bin/check-acs.sh already uses for CHECK_ACS_TIMEOUT"); `bin/check-acs.sh`'s `acs_timeout="${CHECK_ACS_TIMEOUT:-120}"` line and its adjacent invalid-value `case` block.
- decision: chose whole-second `sleep 1` retry granularity for the lock's bounded-wait polling loop, rather than measuring and adopting a fractional-second granularity.
  reason: D2 explicitly leaves this as the engineer's choice ("the engineer either measures that it works and records the measurement, or uses whole-second steps"); whole seconds are simpler, need no portability measurement of this repo's own `/bin/sh` or of an adopter's, and are sufficient for the acceptance criteria's own bounds (AC6 requires only that a `TEAM_LOG_LOCK_TIMEOUT=1` refusal return within 10 wall-clock seconds, which whole-second polling satisfies with room to spare). Measured separately, for the record only (not load-bearing for this choice): fractional `sleep 0.2` does work on this session's own sandboxed bash 3.2.57, but the choice was made for portability toward an unknown adopter host, not because fractional sleep was unavailable here.
  grounding: `.shell-team/specs/T-1076-log-run-locking.md` D2 ("Retry granularity is measured on the host, not assumed... the engineer either measures that it works and records the measurement, or uses whole-second steps"); `.shell-team/test-recipe.md`'s T-1076 entry (records the fractional-sleep measurement and the whole-second choice together).
- decision: (round 2, Codex review round 1 Blocker) closed both `LOCK_ACQUIRED` signal-timing windows by bracketing the mkdir-then-flag-set transition and the flag-reset-then-rmdir transition inside `release_lock` with `trap '' INT TERM` .. re-arm, and additionally reordered `release_lock` to flip `LOCK_ACQUIRED=0` BEFORE calling `rmdir` (not after), rather than adopting Codex's own alternative suggestion of an explicit acquiring/held/releasing state machine.
  reason: a signal delivered while a signal's disposition is SIG_IGN (`trap ''`) is discarded by the kernel outright, never queued for later delivery — this is a stronger and simpler guarantee than "defer and re-deliver," and it closes the exact windows the reviewer's trap-timing probe demonstrated (acquire-side: a signal between `mkdir` success and `LOCK_ACQUIRED=1`; release-side: a signal between `rmdir` success and `LOCK_ACQUIRED=0` re-entering `release_lock` while the flag still read "held", which could `rmdir` a SUCCESSOR's freshly-created lock at the same path) without introducing a new state variable or new state-transition logic to itself get wrong. The flag-flip-before-rmdir reordering is defense in depth, not load-bearing by itself under the trap mask (the mask alone already makes the pair atomic with respect to signal delivery): with it, a hypothetical stray re-entry from OUTSIDE the masked bracket fails SAFE (skips the rmdir, matching D3's own never-steal instinct) rather than fails open (attempts a second rmdir). Verified with a mutation self-check: two A/B pairs of scratch mutants (an OLD-SHAPE mutant structurally reproducing round-1's reviewed code with an artificially widened window via an injected `sleep 3`, against a FIXED-SHAPE mutant carrying the identical widened window but with the mask/reorder intact) were built and driven through the exact interleavings the reviewer's probe exercised — see `tests/log-run/run.sh`'s `signal-race-acquire-side` and `signal-race-release-side` cases, both newly added this round. The OLD-SHAPE acquire mutant reproduces the orphan (exit 143, lock left behind); the OLD-SHAPE release mutant reproduces the theft (a live successor's lock directory is deleted out from under it, confirmed via a marker-file-timed choreography with a slow successor writer); the FIXED-SHAPE mutants of both show the opposite outcome under the identical injected window.
  grounding: `bin/log-run.sh`'s `release_lock` function and the acquire `while :; do ... done` loop (the `trap '' INT TERM` / re-arm brackets around each transition); `.shell-team/reviews/T-1076.md` (round 1 Blocker, both windows and Codex's own suggested-fix text); `tests/log-run/run.sh`'s `signal-race-acquire-side` and `signal-race-release-side` cases (the regression pins, run live this round).
- decision: (round 2, Codex review round 1 Major #1) fixed `compute_auto_seq`'s comparison by escaping the caller's raw `run_id` argument with `jesc` before comparing (matching the file's own stored, escaped form) AND widened the extraction regex from `[^"]*` to `(\\.|[^"\\])*` (treating an escaped pair as one atomic unit so an embedded `\"` no longer truncates the capture early) — but deliberately did NOT add a new character-class validation to `--run-id` (leaving it, as before, validated only for non-emptiness), even though the finding named that as a coherent alternative/companion fix.
  reason: the escaping-plus-extraction fix alone fully closes the actual defect (a silent counter restart) for ANY `run_id` value, including ones containing a backslash or a double quote — jesc's own two-step transform (backslash-double, then quote-escape) is now applied identically on both the write side and the compare side, so the comparison is correct regardless of the value's shape. Adding a NEW character-class restriction on `--run-id` is not required to close this defect and carries its own risk: the frozen spec's intent block imposes no grammar on `--run-id` beyond non-emptiness, no frozen `- check:` line exercises one, and the finding itself demanded that any new class "must not reject any run_id shape this repo's own shipped callers and existing runs files already use" — verified here by enumerating the actual shapes: `bin/close-out.sh`'s one executing call site (a generated id, `--seq 1` unconditional — grounded below), `skills/run/SKILL.md`'s five prompt-text invocation sites (recommending timestamp-style ids), and the machine-local corpus's own `run_id` values (`20260802T052149Z-t1016`-shaped, colon-free timestamp+task-id pairs) — none of which motivate or need a narrower class, so inventing one now would be pure unrequested scope against no concrete failing input. Left as a follow-up if a real narrower-class need is ever measured.
  grounding: `bin/log-run.sh`'s `compute_auto_seq` function (`run_id_field_re` and the `want_run_id_esc` comparison) and its `jesc` function; `.shell-team/reviews/T-1076.md` (round 1 Major #1 finding and its own "may" framing of the validation alternative); `bin/close-out.sh` (the one executing caller, grounded in the frozen spec's own Summarized sources); `skills/run/SKILL.md` (the five invocation sites, same source); `tests/log-run/run.sh`'s `seq-auto-escaping` case (the new regression, run live this round, covering `a\b`, `a"b`, `a\"b`, `a\\b`).
- decision: (round 2, Codex review round 1 Major #2) tracked each background contention writer's own PID and its exit status individually (each of the N per-writer subshells now latches non-zero across its own M sequential invocations and `exit`s that latch), and — in the negative-control arm only — reported a non-zero writer-process count as its OWN named clause in the `neg_findings` text, distinct from the line-count/seq-set clauses, rather than inventing a third AC10 outcome token.
  reason: AC10's own frozen check line requires the negative-control PASS line and the provenance `- negative-control:` line to match one of exactly two tokens (`detected`/`not-detected`) — a third "writer-infrastructure-failure" token would violate that frozen grammar, so the distinction the finding asked for ("distinguish writer infrastructure failed from mutant produced corrupt output explicitly") has to live in the descriptive TEXT beside the token, not in a new token. Folding a writer-process failure into `detected` (rather than silently discarding it or forcing `not-detected`) is the conservative reading: a background writer that crashed IS a genuine deviation from the positive arm's fully-clean baseline (which asserts, as a new check, that ALL its own writers exit 0), so it is honestly `detected`-worthy evidence even when it is not itself proof of interleaved-append corruption — and the text now names which kind of deviation actually occurred, so a human or a later provenance reader is not misled into reading a writer crash as raw byte-interleaving.
  grounding: `tests/log-run/run.sh`'s `run_contention_writer` (per-PID `wait`, `WRITER_FAILED_COUNT`/`WRITER_FAILED_PIDS`) and the positive-arm `contention-writer-exit-status` assertion (new this round) plus the negative-arm `writer-process-failure=` clause; `.shell-team/reviews/T-1076.md` (round 1 Major #2 finding and its own caution that "exit-3 refusals or similar may be legitimate outcomes of lock-disabled chaos"); **AC10**'s check line (the frozen two-token grammar this decision works within).
- decision: (round 3, Codex review round 2 Blocker) fixed `signal-race-release-side`'s scheduling-dependent kill by building a DEDICATED successor mutant (P2, a fresh copy of the real writer with its acquire-loop's masked `mkdir` branch carrying one added line — `: > "${LOCK_DIR}.p2-acquired-marker"` — written the instant P2's own `mkdir` succeeds) and making the test wait on THAT marker's presence before sending `kill -TERM` to P1, rather than sending the kill the instant P1's own post-rmdir marker appeared (round 2's shape) or attempting a full acquiring/held/releasing state machine in the mutant itself.
  reason: round 2's ordering — kill P1 immediately on P1's own marker, THEN poll (up to 5x1s) for the successor to have reacquired — could not retroactively fix an ordering problem: whether the OLD-shape mutant's reentrant `rmdir` found a live successor lock to steal depended on whether P2's own ~1s mkdir-retry cadence had already won the freed path in the window between P1's rmdir and the kill, an unforced coincidence on the same ~1-second timescale as the polling itself (Codex's own analysis: "close to a coin flip"). Moving the wait to BEFORE the kill and keying it on a fact P2 itself asserts (not a fact the test infers from watching `$lockdir`, which could theoretically also reflect some other transient state) makes the ordering a built structural guarantee rather than a probability; it also required widening the injected vulnerability window (`sleep 3` → `sleep 8` in both P1 mutants) so the two added ~1s-granularity poll waits (P1-marker, then P2-marker) fit comfortably inside it before the flag settles, and widening P2's own post-acquire hold (`sleep 5` → `sleep 12`, in the DEDICATED P2 mutant only — SIG_BIN_DIR, used by the separate `lock-released-on-signal` fixture, is untouched) so the FIXED-shape arm's "successor's lock must still be there" assertion is never read against a P2 that finished its own lifecycle and released its own lock for an unrelated, benign reason before the FIXED-shape P1 (which must run its full masked window to completion) even returns. Verified with a mutation self-check beyond the original single run: 5 consecutive full iterations of this exact regression (both the OLD-shape theft arm and the FIXED-shape absorb arm, each run) all passed — `PASS: T-1076 signal-race-release-side` on every one of 5 consecutive runs, no flake observed (command: a standalone driver replaying this file's own `replace_range`/`ACQ_START`/`ACQ_END`/release-side block verbatim in a loop, `bash <driver> relrace 5`).
  grounding: `tests/log-run/run.sh`'s `relrace_choreograph` function (the `p2marker` wait, moved before `kill -TERM "$p1_pid"`) and the `REL_P2_RAW`/`REL_P2_BIN` construction (reusing the already-validated `ACQ_START`/`ACQ_END` anchors); `.shell-team/reviews/T-1076.md` (round 2 Blocker finding, its own suggested fix — "have P2 write its own explicit marker... before kill -TERM is sent to P1" — adopted directly); the 5-consecutive-run tally recorded in `.shell-team/test-recipe.md`'s T-1076 entry.
- decision: (round 3, Codex review round 2 Major) did NOT add an explicit synchronization barrier forcing two AC10 negative-control writers through the scan-then-append window at the same instant; instead disclosed the arm's actual (empirical, not structurally forced) guarantee in a test comment and left the frozen two-token grammar and its `- limit:` escalation path as the honest answer to the residual.
  reason: the finding's own text offers this as an explicit alternative ("or, short of that, note the residual nondeterminism explicitly"), and a true barrier would require pausing the lock-DISABLED mutant's own compute-then-append critical section on a shared rendezvous controllable from the test — restructuring the very TOCTOU mechanism this arm exists to observe, a materially heavier change than this bounded rework's own authorized scope (`loop-guard STOP record`, `.shell-team/todo.md`) covers. The bound actually guaranteed is stated honestly rather than claimed as certain: CONT_N (>=8) writers each performing CONT_M (>=20) fully-unserialized read-scan-then-append cycles against one shared file (>=160 total appends, zero serialization in the mutant — `if true; then`, no lock attempt at all) makes at least one collision empirically near-certain on a real multi-core host, not structurally forced. Verified with a mutation self-check: 5 consecutive full runs of this exact arm (command: the same standalone driver replaying this file's own `run_contention_writer` definition plus the negative-control block verbatim in a loop, `bash <driver> negctrl 5`) all reported `detected — seq-set-mismatch`, consistent with the two prior independently-observed runs already recorded below — a `not-detected` outcome remains structurally possible in principle, which is exactly why AC10's own frozen grammar accepts both tokens and requires a `- limit:` line on the other one, rather than this task inventing a stronger guarantee it cannot actually build within scope.
  grounding: `tests/log-run/run.sh`'s new comment block immediately above `NEG_BIN_DIR="$TMP/negctrl-bin"` (the disclosed-bound rationale); `.shell-team/reviews/T-1076.md` (round 2 Major finding and its own "or... note the residual nondeterminism explicitly" alternative); `.shell-team/specs/T-1076-log-run-locking.md` **AC10** (the frozen two-token grammar and the `- limit:` escalation clause this decision relies on rather than replaces); the 5-consecutive-run tally recorded in `.shell-team/test-recipe.md`'s T-1076 entry.
- decision: (round 3, Codex review round 2 Minor) fixed `lock_mtime`'s stat-dialect fallthrough by explicitly probing which dialect is present (`stat --version` succeeds on GNU coreutils, fails on BSD/macOS `stat`) and running ONLY that dialect's own format string, rather than trying the BSD form first and falling through to the GNU form on a non-zero exit (the shape that let GNU `stat -f '%Sm'` silently "succeed" with a garbled value instead of failing over).
  reason: the defect was that exit status alone cannot discriminate the two dialects — GNU `stat` accepts `-f '%Sm'` as a syntactically valid (if semantically wrong) format string and returns exit 0 with a nonsense value, so the old fallthrough never triggered on GNU/Linux. Probing the dialect itself via `stat --version` (a flag GNU coreutils recognizes and BSD/macOS `stat` does not) and branching BEFORE attempting either format string means the wrong-dialect call is never attempted at all, closing the defect at its root rather than trying to make the exit-status check smarter. Verified live, this session, this host: `stat --version` exits 1 with `illegal option -- -` on this repo's own macOS dev host, confirming the BSD branch is the one actually selected here; a direct manual invocation against a real lock directory produced a legible timestamp (`Aug 16 02:08:43 2026`), not a garbled value or `unknown`. Honest bound: only the BSD branch is exercised by this session — the GNU branch is written from GNU `stat`'s documented `-c`/`--version` behaviour and is NOT independently verified against a live GNU/Linux host from here (this repo's CI runner is the first environment that will exercise it). Mutation self-check on the NEW `lock-mtime-legible` regression itself, run before this round closed: an initial draft asserted only `grep -qE '[0-9]{4}'` on the extracted mtime field, and a deliberate scratch mutant reproducing this same review finding's own named garbled value (`4096m` — GNU stat's actual filesystem-block-size output plus the stray literal `m`) was fed to that draft assertion and, wrongly, PASSED it (`4096` alone satisfies a bare four-digit check) — a real blind spot in the guard, caught before submission rather than after. Strengthened to require the recognizable SHAPE of this host's own BSD default format (a three-letter month abbreviation, an `HH:MM:SS` time, and a trailing plausible year), which the `4096m` mutant and a plain `unknown` mutant both now fail, confirmed by re-running both mutants against the strengthened assertion.
  grounding: `bin/log-run.sh`'s `lock_mtime` function (the `stat --version` dialect probe and its own header comment stating the verified-vs-unverified bound); `.shell-team/reviews/T-1076.md` (round 2 Minor finding, GNU `stat -f`'s own format-string grammar traced there); `tests/log-run/run.sh`'s new `lock-mtime-legible` case (extracts and asserts the refusal message's mtime field on this host).
- decision: (round 4, Codex review round 3 Blocker) rewrote `lock-mtime-legible`'s three round-3 assertions (a month-name check, an HH:MM:SS check, a year anchored to end-of-string — all three BSD-shape-only) into a platform-agnostic PAIR (an HH:MM:SS-shaped time anywhere in the string, AND a plausible `(19|20)[0-9]{2}` year anywhere in the string, neither anchored to either end), i.e. option (ii) from this round's own authorized choice between a dialect-aware branch-based assertion and a platform-agnostic pair — rather than option (i), mirroring `lock_mtime`'s own `stat --version` dialect probe inside the test to select which per-branch assertion set to run.
  reason: option (i) would duplicate the exact dialect-detection logic `lock_mtime` itself already runs, inside the TEST, as a second, independent copy — if that duplicated probe or `lock_mtime`'s own probe ever drifted apart (a future edit to one and not the other), the test could silently start asserting the wrong branch's shape without either copy ever being wrong on its own. The platform-agnostic pair needs no branch detection at all and is verified to hold for BOTH real shapes without any host-conditional logic in the test itself: BSD's own default format (`Jan 15 10:30:45 2024`) and GNU's documented format (`2024-01-15 10:30:45.123456789 +0000`) both satisfy the pair, and all four garbled values this test exists to catch (`4096m`, `unknown`, and QA's own `4096manual`/`Aug 4096` variants) fail the time check alone regardless of the year check, confirmed by a dedicated negative-literal loop run before the real-host assertion (see the new `lock-mtime-legible negative literals` case, immediately above it in the same test). The month-name requirement is dropped entirely rather than kept as an additional, non-required check: GNU's shape carries no month name at all, so keeping it as a MUST would still be BSD-only; keeping it as a MAY (accept either month-name-present or absent) adds no discriminating power the time+year pair does not already provide, at the cost of a third clause to maintain.
  grounding: `tests/log-run/run.sh`'s `lock-mtime-legible` case (the rewritten HH:MM:SS + year assertion pair, and the negative-literal loop immediately above it); `.shell-team/reviews/T-1076.md` (round 3 Blocker finding, its own two suggested shapes — dialect-branch-aware or platform-agnostic — and its own quoted GNU `stat -c '%y'` output shape, `YYYY-MM-DD HH:MM:SS.nnnnnnnnn +ZZZZ`, that the platform-agnostic pair is verified against).
- decision: (round 4, same Blocker) in addition to the rewritten assertion pair, built a PATH-prepended fake `stat` (answering `--version` so `lock_mtime`'s own dialect probe selects the GNU branch, `-c '%y'` with a realistic GNU-shaped byte string, and refusing `-f` outright) and ran the REAL `lock_mtime` function — extracted from `bin/log-run.sh` with the same unique-anchor `sed -n '/^lock_mtime() {$/,/^}$/p'` technique already used elsewhere in this file, not reimplemented — against that stub, live, on this session's own macOS/BSD host, rather than leaving the GNU branch's correctness as a documented-but-unexecuted claim (round 3's own "disclosed as unverified" bound is exactly what let this Blocker through in the first place; a repeated disclosure without execution would not close the same gap twice).
  reason: round 3's own review found the Blocker specifically because its assertions were "written and verified only against this session's own macOS/BSD host output" — a static, first-principles trace of GNU's documented format, never actually run. Closing that with ANOTHER static trace (even a more careful one) would leave the identical class of gap open for the next round to find again. A PATH-prepended fake `stat` lets the ACTUAL `lock_mtime` function (not a reimplementation, not a copy — the literal extracted source) take its GNU branch and produce real output on a host that has no real GNU `stat` to test against, which is the only way to get genuine execution evidence here rather than another disclosure. Verified live, this session: the stub run produced `2024-03-07 10:15:42.123456789 +0000` (captured via `PASS: T-1076 lock-mtime-legible (GNU stub) — ... returns '2024-03-07 10:15:42.123456789 +0000', which the platform-agnostic check pair accepts`), which the new assertion pair accepts — closing the Blocker with an executed positive control, not a second round of reasoning about a format string.
  grounding: `tests/log-run/run.sh`'s `LT_STUB_DIR/stat` fake and the `LT_DRIVER`/`LT_GNU_OUT` execution (the stub, the extraction, and the live run); `.shell-team/reviews/T-1076.md` (round 3 Blocker's own "I could not execute GNU `stat` directly from this macOS sandbox... this is a static, first-principles trace... not a live-executed reproduction" line — the exact gap this stub closes).
- decision: (round 4, Codex review round 3 Major) removed `signal-race-release-side`'s fixed `sleep 8` (both P1 mutants) and `sleep 12` (the dedicated P2 mutant) entirely, replacing each with a bounded event-driven wait on an explicit marker file the test driver itself writes only once it has already gathered the fact that marker exists to certify (`${LOCK_DIR}.rel-proceed-marker` for P1, `${LOCK_DIR}.p2-proceed-marker` for P2, each with a `-lt 60` anti-hang backstop that is NOT sized to budget any correctness-relevant transition) — rather than widening the two fixed sleeps again with a larger explicit margin.
  reason: the finding's own text names the fixed-width-margin PATTERN itself, across three consecutive rounds, as the actual problem ("this task's own three-round history is now three-for-three on scheduling-dependent test defects in this exact fixture family... a signal about the fixed-sleep-margin design pattern itself"), and explicitly offers the marker-driven redesign as its preferred fix over "size it with an explicit justified margin." A larger fixed margin only raises the contention threshold at which the SAME class of defect recurs (a fourth round finding the identical mechanism at a new width) rather than removing it: no finite sleep width can be proven to always outlast an unbounded, contention-dependent choreography, whereas a driver-controlled marker makes every transition wait for the FACT it depends on, not a duration chosen to usually be long enough. Verified with 5 consecutive full iterations of this exact regression via a standalone extraction driver (the same technique round 3 recorded, reused rather than reinvented): `grep -c '^=== iteration' <log>` = **5**, `grep -c '^PASS: T-1076 signal-race-release-side' <log>` = **5**, `grep -c '^FAIL:' <log>` = **0** — no flake, both the OLD-shape theft arm and the FIXED-shape absorb arm passing on every run.
  grounding: `tests/log-run/run.sh`'s `REL_OLD_REPL`/`REL_FIXED_REPL`/`REL_P2_BIN` bodies (the `rp_w`/`p2p_w` marker-wait loops replacing `sleep 8`/`sleep 12`) and the `relrace_choreograph` function (the `p1proceed`/`p2proceed` marker writes, gated so `p1proceed` is only ever written for the `expect_rc=0` FIXED-shape call); `.shell-team/reviews/T-1076.md` (round 3 Major finding — "have P2 write its own explicit... marker... before kill -TERM is sent to P1"-style rendezvous gates, adopted and extended to both P1's own hold and P2's hold); the 5-consecutive-run tally recorded in `.shell-team/test-recipe.md`'s T-1076 round 4 entry.
- decision: (round 4, Codex review round 3 Minor) reworded the AC10 negative-control disclosure comment to remove "guarantees" and "near-certain... on any real multi-core host" (an unqualified universal claim from a sample of runs on one host) and to describe the omitted synchronization barrier as a scope choice for this bounded rework rather than a technical necessity ("restructuring the very TOCTOU mechanism"), stating only the measured evidence (`detected` observed in every recorded run of this arm on this host, across this task's full run history) — a pure wording change, with no change to the test's actual behavior, assertions, or pass/fail outcome.
  reason: the finding is explicitly a wording-only nit against this task's own existing honest-disclosure norm, not a defect in what the test does; the fix the finding itself suggests ("soften to state only the measured evidence... describe the omitted barrier as a scope choice... rather than a technical necessity") is adopted verbatim in substance.
  grounding: `tests/log-run/run.sh`'s AC10 disclosure comment (immediately above `NEG_BIN_DIR="$TMP/negctrl-bin"`); `.shell-team/reviews/T-1076.md` (round 3 Minor finding and its own suggested rewording).
<!-- END provenance: T-1076 -->

## AC10 — negative control outcome

- negative-control: detected — the lock-disabled mutant's contention run produced a duplicate or a gap in the derived `seq` set for the shared `run_id` (two independently observed runs of `tests/log-run/run.sh`, both reporting `seq-set-mismatch` — the classic --seq-auto TOCTOU race: two concurrent processes reading the same file state before either writes back, independent of raw write()-level byte interleaving)

