<!-- BEGIN provenance: T-1135 -->

- decision: added `#487` hardening (ii)'s `--roles` token validation as two sequential `case` glob tests (a leading-lowercase-letter test, then a no-illegal-character test) rather than a single `grep -E '^[a-z][a-z0-9-]*$'` call.
  reason: `case` glob matching needs no external process per token and keeps the validation in the same portable-`case`-statement style the rest of this script's own arg-parsing already uses (the `while … case "$1" in` loop just above), rather than introducing the first `grep -E` dependency into a hot per-token loop; splitting the anchor-start check from the allowed-character check also gives a token like `../evil` a clear, single failure path (it fails the first case on its leading `.`) without needing look-ahead in one combined pattern.
  grounding: `.shell-team/specs/T-1135-codex-host-slice2.md` `## Notes for engineer` ("validate each token against the role-identifier shape the frontmatter `name:` values already use (`^[a-z][a-z0-9-]*$`), refusing before anything is built"); `bin/gen-codex-agents.sh`'s own arg-parsing `while`/`case` loop immediately above the new validation block.

- decision: implemented issue #487 hardening (iv)'s bash-3.2 `set -u` guard with the `"${CONTENT_FILES[@]+"${CONTENT_FILES[@]}"}"` alternate-value expansion, rather than an explicit `[ "${#CONTENT_FILES[@]}" -gt 0 ]` count guard wrapping the `rm -f` call.
  reason: the spec's own Notes for engineer names both as acceptable ("`"${CONTENT_FILES[@]+"${CONTENT_FILES[@]}"}"` or an explicit count guard both close it"); the alternate-value form keeps the trap's body a single `rm -f` line reused unchanged from before this task, whereas a count guard would need an `if`/`fi` around it inside a one-line `cleanup()` function definition, and the parameter-expansion form is the same idiom `bin/resolve-executor.sh` and sibling `bin/` scripts already reach for when guarding an array that may be empty under `set -u`.
  grounding: `.shell-team/specs/T-1135-codex-host-slice2.md` `## Notes for engineer` (the bash 3.2 trap paragraph); `bin/gen-codex-agents.sh`'s `cleanup()` definition and its preceding comment.

- decision: fixed issue #487 hardening (i)'s `--root /` collapse by special-casing `ROOT="/"` before the trailing-slash strip (`[ "$ROOT" = "/" ] || ROOT="${ROOT%/}"`), rather than rewriting the two-line `ROOT="${ROOT%/}"` / `[ -n "$ROOT" ] || ROOT="."` pair into a single conditional or reordering the two fallback lines.
  reason: this is the exact one-liner the spec's own Notes for engineer names as the fix ("`[ "$ROOT" = "/" ] || ROOT="${ROOT%/}"` leaves `ROOT=/`... that is the refusal AC10(i) measures, not a new error branch"); it changes one line, leaves the second fallback line (`[ -n "$ROOT" ] || ROOT="."`) doing the same job it always did for a genuinely empty `--root` value, and does not touch the `[ -d "$ROOT" ]` directory check immediately above it.
  grounding: `.shell-team/specs/T-1135-codex-host-slice2.md` `## Notes for engineer` (the `--root /` paragraph, quoting the one-liner verbatim); `bin/gen-codex-agents.sh`'s `[ -d "$ROOT" ] || die …` / `ROOT="${ROOT%/}"` / `[ -n "$ROOT" ] || ROOT="."` block.

- decision: placed the new host-conditional reference sentence in `agents/codex-reviewer.md` immediately after the primary read path's existing closing sentence ("...means stop and return BLOCKED with the exact error.") and before the "Run each of the five blocks..." paragraph, as its own new paragraph — rather than folding it into the Operating-paths note at the top of the file, or appending it after the adversarial read path's own block further down.
  reason: D2 requires the reference added "beside the existing `codex exec` line, never replacing it" and the primary read path (the first `codex exec` occurrence AC3's grep counts) is the block a Codex-host reader reaches first; stating the host-conditional branch immediately after that block, before the reader is walked through the five alloc/codex/diagnose/cleanup/publish steps that follow it, avoids letting a Codex-hosted reader work through Codex-only mechanics (worktree pinning inside a `concurrent-review-window`, the five `T-107-step` blocks) before learning that block does not apply to their host at all. Folding it into the Operating-paths note would separate the "which mechanism runs" statement from the block it conditions, and the Operating-paths note's own scope (paths and helper resolution) is a different subject.
  grounding: `.shell-team/specs/T-1135-codex-host-slice2.md` `## Decision points ruled in this spec`, row D2 ("One reference added beside the existing `codex exec` block, never replacing it"); `agents/codex-reviewer.md`'s own step-3 paragraph structure (the primary read path's closing BLOCKED sentence, immediately followed by the five-block "Run each of the five blocks..." paragraph).

- decision: composed the Claude-side `--tools` list as `Read Grep Glob "Bash(git diff:*)" "Bash(git log:*)" "Bash(git rev-parse:*)"`, adding `Bash(git rev-parse:*)` beyond the two Bash allowances (`git diff:*`, `git log:*`) the spec's Working rules bullet names explicitly.
  reason: `agents/codex-reviewer.md`'s own `## Your loop` step 1 (unchanged by this task, and read by the spawned Claude pass on either host) runs `git rev-parse --abbrev-ref HEAD` as part of determining diff scope, alongside `git log --oneline <base>..HEAD` and `git diff --stat <base>...HEAD`; omitting it from `--tools` would make the Claude-hosted pass unable to complete the same step-1 procedure it is asked to follow, for a read-only, non-mutating git subcommand of the same class already allowed.
  grounding: `agents/codex-reviewer.md` `## Your loop` step 1 (`git rev-parse --abbrev-ref HEAD`, `git log --oneline <base>..HEAD`, `git diff --stat <base>...HEAD`, all three read-only); the task brief's Working rules bullet naming `Bash(git diff:*)` / `Bash(git log:*)` as "the read-only ... allowances" without asserting the list is exhaustive.

- decision: in the Claude-JSON-result fixture (`tests/codex-agents/run.sh`, T-1135 fixture 8 / AC7's fourth PASS line), modeled the `usage` field as a single flat `"usage":{"total_tokens":890}` object rather than reproducing Claude Code CLI's actual `usage` schema (separate `input_tokens`/`output_tokens`/cache-token fields).
  reason: AC12's own check line asserts only that `templates/prompt-blocks/host-dispatch.md` names which flag reads which field name (`--tokens` from `usage`, and so on) — it does not pin the field's internal shape — and the fixture's job is to prove `log-run.sh` accepts a `--tokens` value sourced from a Claude result's `usage` field, not to be a byte-accurate stand-in for the CLI's real JSON. A flat single-number field keeps the fixture's own `sed` extraction (`sed -n 's/.*"total_tokens":\([0-9]*\).*/\1/p'`) a one-line, dependency-free read consistent with this repository's no-`jq` convention, rather than requiring an ad hoc two-field sum to stand in for a schema this task does not otherwise assert.
  grounding: `.shell-team/specs/T-1135-codex-host-slice2.md` **AC12**'s own check line (asserts only the fixed-string field names inside `host-dispatch.md`, not a JSON schema); `## Assumptions` item 4 ("The result carries `usage`, `total_cost_usd`, `duration_ms`, ... enough to fill a `log-run.sh` span row") — naming the fields present, not their internal shape; this repository's existing `bin/` scripts' avoidance of `jq` (`bin/codex-capture.sh`'s own header: "dependency-free single-pass awk (no jq...)").

- decision: (rework round 1, orchestrator's pre-Validate probe) replaced the shipped isolation combination (`--tools` naming a built-in set plus `--disallowedTools Write,Edit,NotebookEdit` alone) with `--permission-mode dontAsk` plus a git-only `--allowedTools` allowlist plus `--strict-mcp-config` plus `--setting-sources ""`, and rewrote each flag's stated effect in the block's prose to the measured one rather than the one inferred from `--help` text.
  reason: three of the orchestrator's own live probes (Claude Code CLI 2.1.268, this host, 2026-09-12) measured the shipped shape wrong on two independent points before this round shipped anything: `--tools` does not accept permission-rule syntax like `Bash(git diff:*)` (a bare tool name is required, so the reviewer had no working Bash at all under the original line) and does not exclude MCP servers (every one of the operator's claude.ai connector tools, write-capable ones included, stayed exposed, since MCP tools sit outside `--tools`'s built-in vocabulary entirely); a further probe showed `--allowedTools` alone does not deny a non-allowlisted `Bash` call — the adopter's own permission-mode setting decides that, and only `--permission-mode dontAsk` turns a non-allowlisted call into an actual harness denial. I re-confirmed the corrected combination with an independent, this-round live probe (recorded in the next entry) before shipping it, rather than shipping on the strength of the relayed probes alone.
  grounding: `.shell-team/interventions/T-1135.md`'s `assumption-contradicted` entry (the orchestrator's three probes, quoted there); this round's own class-closure probe (next entry).

- decision: (rework round 1) placed the `<PROMPT>` placeholder immediately after `-p`, before every flag on the line, rather than appending it at the end of the invocation the way `codex exec`'s own free-form-prompt recipe elsewhere in this repository appends its prompt as an ordinary trailing positional argument.
  reason: measured live during this round's own mandatory class-closure probe (below): `--tools`, `--allowedTools` and `--disallowedTools` on this CLI are each variadic (`<tools...>` per `claude --help`), consuming every following token as an additional value until the next `--`-prefixed flag or end of argv. A trailing prompt with nothing after it on the line is silently absorbed into the last such flag's own value list instead of being read as the prompt, and the invocation then refuses with `Input must be provided either through stdin or as a prompt argument when using --print` even though a prompt was in fact supplied — reproduced with a minimal repro (`claude -p --tools Read Grep Glob Bash --allowedTools "Bash(git diff:*)" "hello world"` refuses; `claude -p "hello world" --tools Read Grep Glob Bash --allowedTools "Bash(git diff:*)"` succeeds) before fixing the shipped line. This is a property of Claude Code CLI's own variadic-flag argument grammar that `codex exec`'s single-positional-argument grammar does not share, so nothing about the Codex recipe already in this file could have surfaced it by analogy.
  grounding: this round's own live repro transcript (`claude -p --tools Read Grep Glob Bash --allowedTools "Bash(git diff:*)" "hello world" < /dev/null` → `Error: Input must be provided either through stdin or as a prompt argument when using --print`; the identical invocation with the prompt moved first → succeeds); `claude --help`'s own flag descriptions (`--tools <tools...>`, `--allowedTools, --allowed-tools <tools...>`, `--disallowedTools, --disallowed-tools <tools...>`).

- decision: (rework round 1, class-closure mandate) ran the corrected, final recipe line exactly as shipped, once, from this checkout, with a probe prompt asking it to run `git rev-parse --short HEAD`, attempt `touch t1135-recipe-probe.txt` via Bash, and list its available tools — rather than treating the orchestrator's relayed probe results as sufficient grounds to ship this round's own further-corrected line (the `<PROMPT>`-placement fix above was found only by running it).
  reason: the rework's own mandate states plainly that "a recipe whose flags were not run is the defect class this round exists to close; help-text reasoning does not close it" — the relayed probes graded the flag combination, not this round's own additional edit (prompt placement), so shipping without an independent run of the actually-final line would have repeated the same defect class one edit later.
  grounding: this round's own live probe (2026-09-12, this host, cwd outside this checkout, not a git repository — `git rev-parse` therefore correctly failed with `fatal: not a git repository`, an expected artifact of the scratch cwd and not a defect): `claude -p "<probe prompt>" --output-format json --verbose --no-session-persistence --permission-mode dontAsk --setting-sources "" --strict-mcp-config --tools Read Grep Glob Bash --allowedTools "Bash(git diff:*)" "Bash(git log:*)" "Bash(git rev-parse:*)" --disallowedTools Write,Edit,NotebookEdit < /dev/null` → exit 0, `is_error:false`, reported tool list `["Bash","Glob","Grep","Read"]` (no Agent, no Write/Edit/NotebookEdit), `mcp_servers:[]`, `permission_denials` non-empty (one entry, `tool_name:"Bash"`, `tool_input.command:"touch t1135-recipe-probe.txt"`), `total_cost_usd:0.075372`, `duration_ms:16007`; `ls` of the scratch cwd before and after confirmed no `t1135-recipe-probe.txt` (or any other new file) was ever created.

- decision: (rework round 2, codex-reviewer round 1 REQUEST_CHANGES, Blocker B1) replaced the `<PROMPT>` bare-double-quote substitution with the `"$(cat "<PROMPT_FILE>")"` command-substitution shape, reusing `skills/run/SKILL.md`'s own T-1118 alternate-executor recipe (`"$(cat "<BRIEFING_FILE>")"`) verbatim as the precedent, rather than inventing a new quoting mechanism or switching to a `<file` stdin-redirect form.
  reason: two independent Codex review passes on this task (primary and adversarial) named the exact same defect — a backtick, `$(...)`, `$VAR` or embedded double quote inside a free-text review instruction substituted into a bare `"<PROMPT>"` could be expanded, executed, or could terminate the argument early — and this repository already ships a proven, reviewed fix for exactly this class one paragraph above in the same file (T-1118's own briefing-delivery rule, whose own stated reason is "an inner double quote in the substituted text would terminate the shell's own quoting"). Reusing that exact idiom, rather than a stdin-redirect (`< "<PROMPT_FILE>"`), was chosen because `claude -p`'s own positional prompt argument is what carries the instruction on this CLI (unlike a subcommand reading from stdin), and command substitution is the mechanism this repository has already reviewed and shipped for passing untrusted-shaped free text into a positional argument without re-parsing it.
  grounding: `.shell-team/reviews/T-1135.md` Blocker 1 (both primary "P1" and adversarial "blocker" — "バッククォート、`$(...)`/`$VAR`、埋め込みダブルクォートは…展開・解釈されうる"); `skills/run/SKILL.md`'s "Constructing `<BRIEFING_FILE>`" paragraph (T-1118, unedited by this task) and its own invocation line `codex exec … "$(cat "<BRIEFING_FILE>")" < /dev/null …`; this round's own live class-closure probe (below).

- decision: (rework round 2, Blocker B1 class closure) defined `<PROMPT_FILE>`'s own content as a self-contained review-procedure template (scope rule, the three read-only git subcommands, the exact `## Output` heading/verdict-label shape read from `agents/codex-reviewer.md`) rather than a briefing pointing at a separate role-definition file the way T-1118's own `<BRIEFING_FILE>` does.
  reason: T-1118's briefing dispatches an ENTIRE role by pointing the executor at that role's own `agents/<role>.md` file to read and act as; here, the spawned `claude -p` process is not being asked to become the `codex-reviewer` role itself (which already ran, as the Codex agent that reached this dispatch point) — it is asked to perform the one narrower task the primary/adversarial `codex exec` passes already perform (read the diff, judge it, return findings in the shape `## Output` fixes), which is data the review procedure itself needs to state inline rather than point at a file for. A payload-sha256/payload-bytes verification pair (T-1118's own trust-boundary check on the pointed-at role file) has no analogue here because there is no pointed-at file to verify — the template's own text is the entire instruction.
  grounding: `agents/codex-reviewer.md` `## Output` section (the exact headings/labels the template asks the inner pass to reproduce); `skills/run/SKILL.md`'s own `<BRIEFING_FILE>` paragraph, contrasted (it dispatches a role BY POINTING at `agents/<role>.md`, not by inlining a procedure).

- decision: (rework round 2, Blocker B2) added a one-sentence host-conditional cross-reference to the adversarial/fallback read-path paragraph and to `## Your loop` step 5, rather than duplicating the primary path's own longer "Host-conditional note" paragraph a second and third time.
  reason: D2's own ruling ("one reference added beside the existing `codex exec` block, never replacing it") governs every dispatch site this role's own file carries, not only the first one the reviewer named; a short cross-reference ("same as step 3's own note") keeps AC4's zero-duplicated-block-line invariant trivially satisfied at every site, rather than growing three separate near-identical paragraphs that would drift out of sync with each other over time.
  grounding: `.shell-team/reviews/T-1135.md` Blocker 2 (both primary "P1" and adversarial "major" — "adversarial/fallback read path...および `## Your loop` step 5 は無条件に `codex exec` を実行し続ける"); `.shell-team/specs/T-1135-codex-host-slice2.md` **D2**.

- decision: (rework round 2, Blocker B2 inventory) left `## Spec-review mode (specify seam, T-1092)`'s own `codex exec` block (agents/codex-reviewer.md's third bare invocation line) and five other generic `codex exec` mentions (the Operating-paths note, the primary-pass description sentence itself, the findings-reading sentence, the board-and-write-boundary Rule, the executor-invocation recording convention, and the Language rule) without any host-conditional addition — recorded as `not-apply`, not overlooked.
  reason: spec-review mode is a wholly different seam (Specify, before any implementation or intent-hash exists) gated by its own, unrelated conditional entry (a Routing Map election at Plan) that this task's own Goal, Non-goals, D-points and ACs never mention; extending host-conditional treatment there would design a THIRD Claude-side recipe variant and a third capture stem this frozen spec's AC5/AC12 do not ask for, and the review's own Blocker 2 finding named only "primary read path... adversarial/fallback... and step 5" as broken — not spec-review mode. The five other sites are prose describing the mechanism generically (which tool the role has, how findings get read after either pass, a write-boundary enumeration, a recording convention, a language rule) rather than a dispatch instruction telling the role to run `codex exec` at that point, so there is no site-specific instruction there for a host-conditional sentence to qualify.
  grounding: `agents/codex-reviewer.md` `## Spec-review mode (specify seam, T-1092)`'s own conditional-entry paragraph ("this mode activates only when the coordinating session invokes you against a `READY_FOR_ARCH` task whose Routing Map elected `spec-review — cross-provider`" — a precondition orthogonal to this task's Codex-host dispatch mechanism entirely); `.shell-team/specs/T-1135-codex-host-slice2.md`'s Goal/Non-goals/D-points/ACs (none names spec-review mode); `.shell-team/reviews/T-1135.md` Blocker 2's own suggested fix (names only the adversarial/fallback paragraph and step 5).

- decision: (rework round 2, Minor m1) refused a role's own final `shell-team-<role>.toml` path in `bin/gen-codex-agents.sh` when it already exists as something other than a regular file (checked for every requested role, before pass 2 stages or writes any of them), rather than checking only immediately before each individual `mv`.
  reason: checking every requested role's target up front, before pass 2 begins writing ANY of them, keeps the refusal whole-run and nothing-written for this specific hazard — consistent with pass 1's own no-partial-output discipline — rather than a per-role check that could still leave earlier roles' files written by the time a later role's directory-collision is discovered.
  grounding: `.shell-team/reviews/T-1135.md` Minor 1 (`mv` onto an existing directory "silently移動...exit 0 を返し、期待される TOML ファイルが実際には作られない"); this round's own reproduction (`mkdir -p "$OUT_DIR/shell-team-pm-spec.toml"` then a full default run → refused, exit 2, zero regular-file `*.toml` written).

- decision: (rework round 2, Minor m2) wrapped `ROLES_ARR=($ROLES)` in `set -f`/`set +f` (suppressing pathname expansion only, keeping word-splitting) rather than switching to `read -ra` or an explicit no-glob splitting loop.
  reason: `set -f` is the minimal, one-line change that removes exactly the hazard the finding names (glob expansion) while leaving the existing word-splitting behavior, the existing shellcheck-disabled `SC2206` comment's own rationale, and every other line of this pass 1 block completely unchanged — a `read -ra` rewrite would touch more lines for the same effect and would need its own IFS-handling verification.
  grounding: `.shell-team/reviews/T-1135.md` Minor 2 ("クォートなし配列代入はパス名展開（glob）も行う...`set -f` を敷くか...置き換えるのが望ましい" — naming `set -f` first, as this task's own suggested fix); this round's own reproduction (`--roles '*'` from the repository root → refused, message names the literal token `*`, not an expanded filename, confirmed via `grep` on the refusal message).

- decision: (rework round 2, Minor m3) derived `--tokens` as the sum of `usage.input_tokens` + `usage.output_tokens` + `usage.cache_creation_input_tokens` + `usage.cache_read_input_tokens`, both in `templates/prompt-blocks/host-dispatch.md`'s prose and in `tests/codex-agents/run.sh` fixture 8's realistic `usage` object, rather than picking one of the four fields alone or leaving the derivation unstated.
  reason: the review's own finding states plainly that Claude's real `usage` object carries no single `total_tokens` field and that the fixture's own previous flattened shape hid this from anyone trying to fill `--tokens` from a real Claude result; the sum of all four fields is the quantity this repository's own `<usage>`-block convention (`skills/run/SKILL.md`'s Telemetry (Observe) paragraph, `subagent_tokens`) already means by "tokens used", so stating the sum (rather than any single field) keeps this span's own `--tokens` meaning the same quantity the Claude Code Agent-tool host's own span rows already record under that name.
  grounding: `.shell-team/reviews/T-1135.md` Minor 3 ("実際の Claude JSON result の `usage` は単一の `total_tokens` を持たず、`input_tokens`/`output_tokens`/`cache_creation_input_tokens`/`cache_read_input_tokens` に分かれている"); `skills/run/SKILL.md`'s Telemetry (Observe) paragraph (`--tokens <subagent_tokens from the result's <usage> block>`); this round's own live probe transcripts (both round-1's and round-2's own class-closure probes), whose raw `usage` objects show exactly these four field names and no `total_tokens`.

- decision: (rework round 2, Blocker B1 class closure, mandatory) ran the corrected, final `claude -p "$(cat "<PROMPT_FILE>")" ...` recipe once, exactly as shipped, from a scratch cwd outside this checkout, with a `<PROMPT_FILE>` whose text contained all four hazard sequences the review named (a backtick pair, a `$(echo INJECTED)` command substitution, a `$HOME` variable reference, and an embedded double quote) and an explicit instruction to echo them back verbatim and state whether any had been expanded — rather than treating the repo-local deterministic fixture (`tests/codex-agents/run.sh`'s own new arm, which proves the SAME quoting mechanism in isolation via a mock command) as sufficient on its own.
  reason: the rework's own class-closure mandate for B1 requires running the shipped line "once as shipped" and recording that the model received the hazard characters verbatim — a deterministic, no-API-call fixture proves the shell-level quoting mechanism is sound, but only a live run through the actual `claude` binary confirms nothing between the shell and the model's own transcript introduced a second point of interpretation (the CLI's own argument handling, its own prompt-ingestion path).
  grounding: this round's own live probe (2026-09-12, this host, scratch cwd, not a git repository — the prompt itself instructed no git commands): `claude -p "$(cat "<PROMPT_FILE>")" --output-format json --verbose --no-session-persistence --permission-mode dontAsk --setting-sources "" --strict-mcp-config --tools Read Grep Glob Bash --allowedTools "Bash(git diff:*)" "Bash(git log:*)" "Bash(git rev-parse:*)" --disallowedTools Write,Edit,NotebookEdit < /dev/null` → exit 0, `is_error:false`, `total_cost_usd:0.040887`, `duration_ms:5609`; the model's own result text reproduced all four sequences verbatim (`` `LIKE THIS` ``, `$(echo INJECTED)`, `$HOME`, `" right here`) and stated explicitly that none were expanded, executed or substituted — "`$HOME` is still a dollar sign followed by `HOME` rather than a path like `<home>/...`"; `ls` of the scratch cwd before and after confirmed no file was created.

<!-- END provenance: T-1135 -->
