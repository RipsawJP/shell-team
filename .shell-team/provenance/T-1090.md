# T-1090 decision provenance

<!-- BEGIN provenance: T-1090 -->
- decision: the corrected `- subject-ac: 8` block states the sign-separate-then-`10#`-normalize-then-reapply mechanism verbatim (separate a leading `-`, `10#`-normalize the remaining digits as the magnitude, reapply the sign afterward), rather than a different equivalent decimal-parsing rule (e.g. `printf '%d'` coercion, or stripping leading zeros with parameter expansion before a bare `$(( ))` read).
  reason: the spec's own AC1 body accepts either "separating the sign before `10#`-normalizing the magnitude and reapplying it afterwards, or any equivalent decimal-parsing rule stated in the brief itself," so more than one substantively correct mechanism was open. The chosen one is the exact suggested fix the governing round-3 review recorded, is expressible in the pure-bash-plus-POSIX-text-utilities idiom `README.md`'s own `candidate-rule: zero-dependency-bash` already requires of a candidate (a leading-character test plus arithmetic, no `awk`/`bc`/interpreter), and keeps the shipped rule traceable to the finding it closes rather than introducing a fresh mechanism a future reader would have to independently re-verify against the same two failure modes.
  grounding: `.shell-team/reviews/T-1089.md`, `### Codex Review verdict: REQUEST_CHANGES (round 3)` → `#### Major`, the `docs/loop-engineering/subjects/subject-01/README.md:99` finding's own "Suggested fix" line ("state explicitly that the sign must be stripped before `10#` normalization of the magnitude and reapplied afterward (or an equivalent decimal-parsing rule)"); `docs/loop-engineering/subjects/subject-01/README.md`, `candidate-rule: zero-dependency-bash`; `.shell-team/specs/T-1090-sign-safety-close.md` **AC1**'s own body.
- decision: within the `- subject-ac: 8` block, the required literals are split so the first physical line carries only the rule statement (`decimal`, `` `10# ` ``-normalize, sign-reapply) and the trap illustration (`10#-010`, the `reapplied` alternation word) is pushed onto the indented continuation lines, rather than cramming the trap example onto the first physical line alongside the rule statement.
  reason: **AC1** requires exactly `10#` and `decimal` on the block's first physical line, and separately requires `10#-010` plus one of `reapplied`/`re-applied`/`negated`/`restored` anywhere across the whole block (first line through continuations) — two different scopes, so more than one line-split was compliant. Keeping the first line to the rule statement alone (rather than also appending the trap example there) keeps the sentence a candidate reads first short and states the positive rule before the negative example, mirroring the existing block's own structure (rule, then elaboration, then reason) that the predecessor task's block already used and this task's Notes for engineer instruct not to disturb beyond the mechanism fix itself.
  grounding: `docs/loop-engineering/subjects/subject-01/README.md`, `- subject-ac: 8` block (first physical line vs. continuation lines, current bytes); `.shell-team/specs/T-1090-sign-safety-close.md` **AC1**'s check body (`grep -qF -- '10#' "$T/l8"` / `'decimal'` scoped to the first-line extraction `l8`, vs. `grep -qF -- '10#-010'` / the alternation regex scoped to the whole-block extraction `b8`); self-check: `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1090-sign-safety-close.md` **AC1** PASS against these bytes (see hand-off).
<!-- END provenance: T-1090 -->
