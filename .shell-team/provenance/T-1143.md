<!-- BEGIN provenance: T-1143 -->

- decision: AC6(c)'s fixture assertion ("the same record passes once the shape is described in words instead") checks two things together — no `FINDING` line naming the record's path, and the invocation's exit status equal to `0` — rather than checking only for the absence of the specific `FINDING` line the label names.
  reason: The label's own word "passes" is read as the whole scan completing clean for this fixture (a fresh, single-file throwaway repository), not merely as "this one path stopped appearing in the findings list" while some other, unrelated finding could in principle still leave the run at a nonzero exit. Since this repository is created fresh by `new_repo` and carries only the one record file under test, the stronger condition and the weaker one are extensionally identical for this fixture as written, but the stronger form is the one that would actually catch a future edit to the fixture that added a second finding-bearing line by mistake.
  grounding: `tests/check-pii-shapes/run.sh` (the added `hand-off shape scan` section, assertion (c)); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` **AC6** (label (c) text: "the same record passes once the shape is described in words instead").

- decision: Round 2 (Codex REQUEST_CHANGES) rewrote the `hand-off shape scan` fixture's negative control (assertion (b)) to use a tracked file committed with innocuous content strictly after the repository's base commit, then modified on disk and left uncommitted, instead of the round-1 design's wholly untracked file whose repo base and HEAD were identical.
  reason: The round-1 negative control passed for a cause unrelated to its own label. With the record entirely untracked, `git diff --raw <base> HEAD` is empty regardless of the file's content because no commit ever occurred between `HOS_BASE` and `HEAD` — so the assertion never actually exercised change-scoped mode's committed-blob read (`git cat-file -p "HEAD:$path"`), the mechanism DP2 and the assertion's own label claim. The corrected fixture puts the record's path genuinely inside the base..HEAD diff (via an intervening commit of innocuous content) while its HEAD blob stays innocuous, so change-scoped mode's clean result is caused by reading the stale committed blob rather than by the diff being empty. Directly verified empirically: committing the shape-bearing content into the same repository (a defeat scenario constructed on a disposable scratch repo, never committed to this tree) flips the change-scoped scan to report the finding, confirming the corrected control is not vacuous.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES`, Major 3); `tests/check-pii-shapes/run.sh` (the rewritten `hand-off shape scan` section, assertions (a)–(c) and the added (d)); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` `## Input space` reachable class 2 ("untracked, or tracked and modified but uncommitted"); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` `## Notes for engineer` (the round-2 amendment recorded in place, superseding the round-1 recipe that instructed leaving the record untracked).

- decision: Kept a fourth, separately labeled assertion (d) in the `hand-off shape scan` fixture round, covering a wholly untracked record, alongside the three AC6-named labels.
  reason: AC6 requires only the three verbatim labels to be present and does not forbid a fourth; the spec's `## Notes for engineer` explicitly permits keeping an untracked-record variant "if you judge it adds evidence." A wholly untracked record is a real, distinct input class this spec's own Input space names, and the round-1 design's only fixture for it collapsed into the (now-corrected) load-bearing control. Keeping it as its own honestly-labeled assertion (worded as demonstrating the different, weaker never-entered-any-diff mechanism) preserves coverage of that input class without letting its label overstate what it proves, unlike the round-1 control it replaces.
  grounding: `tests/check-pii-shapes/run.sh` (assertion (d), "a wholly untracked record is also reported by --all, and is invisible to the change-scoped default because it never entered any diff"); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` **AC6** (three-label requirement) and the round-2 `## Notes for engineer` amendment.

- decision: Fixed the canonical block's code sample (`templates/prompt-blocks/record-shape-scan.md`) to define `out` via `out="$(mktemp)"` before the invocation and to capture `rc` with an `if …; then rc=0; else rc=$?; fi` form, rather than the round-1 form (`$out` undefined, `; rc=$?` on one statement).
  reason: Verified empirically in this checkout: the round-1 snippet, run literally in `bash -e -u`, fails immediately with "out: unbound variable" before the checker ever runs (the `$out` gap), and separately, run with `out` defined but with the one-line `; rc=$?` form, aborts under `set -e` before `rc=$?` executes whenever the checker legitimately exits 1 (a real finding) — in both cases a scan that either never ran or a legitimate finding is left indistinguishable from a clean run under the block's own exit-contract prose. The corrected form was re-run literally and captures `rc=1` correctly for a real finding without aborting.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES`, Major 1); `templates/prompt-blocks/record-shape-scan.md` (the corrected code fence); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` **AC8**'s own `- check:` line (the pre-existing repository idiom the fix now matches: `if PII_CHECK_TRACKER_KEY=1 bash "$S" --all > "$D/scan.txt" 2>&1; then g=0; else g=$?; fi`).

- decision: Added a new sentence to both adopter documents (`docs/pii-controls.md` and `docs/pii-controls.ja.md`) stating the canonical block's accepted-noise carve-out, inserted after the existing paragraph rather than rewording any existing sentence.
  reason: Both adopter-facing sections stated only that a hit on the writer's own file is fixed in place, omitting the block's own carve-out that a hit in the checker's declared accepted-noise class is classified rather than edited away — a reader of the adopter doc alone could reasonably conclude every hit must be rewritten, including a legitimate design-point label or standards identifier the accepted-noise class exists to protect. The fix is a pure insertion (a new sentence after the existing paragraph, before the next `## ` heading) so AC5's zero-deletion invariant and the existing sentences both stay intact; AC7's region-scoped anchor extraction still covers the new sentence since it sits before the next heading.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES`, Major 2); `docs/pii-controls.md` and `docs/pii-controls.ja.md` (the added sentence, immediately before `## What this gate does not cover` / `## このゲートが扱わないもの`); `templates/prompt-blocks/record-shape-scan.md` (the block's own "accepted noise" bullet, the source of the carve-out being restated).

- decision: Removed the fenced `bash` code sample from `templates/prompt-blocks/record-shape-scan.md` entirely and restated the invocation inline in the first bullet's prose, as inline code (`` `PII_CHECK_TRACKER_KEY=1 bash "<plugin root>/bin/check-pii-shapes.sh" --all` ``), with the capture and exit contract described in words (a file the role creates under its own scratch directory, read back after the run; a redirect or file-creation failure, or an exit status of `2`, is a scan that did not complete) rather than as a second, executable code fragment.
  reason: The class "a shipped command fragment is not runnable as written" fired three times against this one fenced exemplar across two review rounds — round 1 Major 1 (`$out` undefined, `; rc=$?` fragile under `set -e`), round 2 Blocker 1 (the round-1 fix's `out="$(mktemp)"` is refused by this platform's own sandboxed shell — measured live: bare `mktemp` fails `Operation not permitted`, a templated form succeeds), round 2 Major 2 (the fenced snippet emits neither `$out` nor `$rc`, so its own next instruction, read across a separate shell invocation, cannot act on the result). Patching the exemplar's bytes a fourth time repeats the same class against the same design premise: a fenced code block presented as paste-and-run text carries an implicit contract (portable across shells and platforms, self-contained across separate invocations) that this specific two-line form has now failed three ways. Removing the fenced form and stating the invocation and its contract in prose makes the class unreachable by construction — there is no executable fragment left to be non-portable, `set -e`-fragile, or non-self-contained, and the capture requirement is named generically (an own-owned, readable file) rather than as an incantation that can itself fail on a specific platform.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES (round 2)`, Blocker 1 and Major 2); `.shell-team/todo.md` (the T-1143 board entry's `same-class-2` and `means-ends-reflection` sub-bullets, round 2, recording the design-premise-change disposition); `templates/prompt-blocks/record-shape-scan.md` (the rewritten first bullet, no fenced code block remaining).

- decision: Added "from a run made after any fix" to the canonical block's summary-line bullet (`Quote the run's summary in your hand-off as one line, from a run made after any fix`), rather than leaving the ordering implicit in the bullets' sequence alone.
  reason: Codex round 1's Minor 4 and round 2's carried-forward Minor both named the same gap — the block's bullet ordering (fix, then quote) plausibly implies a rerun after the fix but never states it as an explicit step, so a role could in principle quote a pre-fix run's stale counts. This was folded into the same design-premise change as the fenced-exemplar removal (the block's prose is being rewritten regardless) rather than deferred to a fast-follow issue a third time, since the cost of stating the ordering explicitly here is one clause.
  grounding: `.shell-team/reviews/T-1143.md` (round 1 `#### Minor / nits`, first bullet; round 2 `#### Minor / nits`, first bullet, "Carried from round 1, still unaddressed"); `templates/prompt-blocks/record-shape-scan.md` (the summary-line bullet's added clause).

- decision: Replaced the spec's `## Notes for engineer` "proposed wording for the canonical block" blockquote (the pre-round-1-fix snippet, still quoting the undefined-`$out` form after two rounds of fixes elsewhere) with two sentences pointing at the shipped canonical file and this provenance record, rather than updating the blockquote's own snippet to match the current shipped form a third time.
  reason: Codex round 2 Major 3 found the blockquote unchanged since before round 1 while the shipped block was corrected twice — the exact "a later append contradicting an earlier line no gate re-reads" defect class this task's own originating retro exists to catch, present in the spec that ships the mechanism. A second copy of the block's text inside `## Notes for engineer` is a second site every future fix to the canonical block would also have to reach, which is the identical drift DP6 already states as the reason the summary-line grammar lives only in the block and not in four role templates; removing the draft's second copy applies DP6's own reasoning to this note rather than reproducing the drift DP6 warns against.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES (round 2)`, Major 3); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` `## Decision points, resolved`, DP6 ("Putting `- shape-scan:` into four fenced hand-off templates would create four more copies of one grammar… the exact drift the block form exists to prevent"); `.shell-team/specs/T-1143-handoff-shape-scan-step.md` `## Notes for engineer` (the replaced paragraph, now "The canonical block's shipped text").

- decision: Added one paragraph to both `docs/pii-controls.md` and `docs/pii-controls.ja.md`, inside the `## When the loop runs it` / `## ループがこれを走らせる時点` region, stating the did-not-complete contract (a redirect/file-creation failure or exit status `2` is a scan that did not complete and is never read as an absence of findings) and the run-after-fix ordering (the quoted `- shape-scan:` line is from a run made after any fix), as a pure insertion after the existing accepted-noise paragraph.
  reason: The canonical block's round-3 rewrite states both of these explicitly for the first time (the did-not-complete contract, previously only in the block's second bullet's exit-status classification, and the run-after-fix ordering, newly added this round); re-reading both adopter documents against the rewritten block found both clauses absent from the adopter-facing summary, which an adopter reading only the doc (not the block a role's prompt carries) would not learn. AC7's region-scoped anchor set (`--all`, `PII_CHECK_TRACKER_KEY`, `shape-scan`, `hand-off`/`ハンドオフ`) does not name either clause, so this insertion is not required by any `- check:` line — it closes a documentation-summary gap the same class of finding (Major 2, round 2) already flagged for the accepted-noise carve-out, applied here to the two clauses the round-3 block wording newly makes explicit.
  grounding: `templates/prompt-blocks/record-shape-scan.md` (the round-3 first and fourth bullets, the source of both restated clauses); `docs/pii-controls.md` and `docs/pii-controls.ja.md` (the added paragraph, immediately before `## What this gate does not cover` / `## このゲートが扱わないもの`); `.shell-team/reviews/T-1143.md` (round 1 `#### Minor / nits`, first bullet — the run-after-fix ambiguity Codex already named once).

- decision: Pinned `core.excludesFile` to `/dev/null` as a per-repo local git config (`git -C "$REPO" config core.excludesFile /dev/null`, set once right after `new_repo`) on both `HOS_UT_REPO` (assertion (d)) and `HOS_REPO` (assertions (a)-(c)), rather than passing a `-c core.excludesFile=…` flag on the outer `bash "$BIN" --all` invocation, the idiom `tests/rollup-track/run.sh` and `tests/gitignore-raw-dumps/run.sh` use.
  reason: Those two precedents call `git check-ignore` directly, so a `-c` flag given to that same `git` invocation reaches the exact process consulting the excludes file. This fixture instead calls `bash "$BIN" --all`, a wrapping script whose own internal `git ls-files -z --others --exclude-standard` (`bin/check-pii-shapes.sh:744`) runs as a separate process that never receives a `-c` flag given to the outer `bash`. A per-repo `git config` write, by contrast, persists in that repo's own `.git/config`, which git's precedence rules read ahead of (and override) any ambient global `core.excludesFile` for every subsequent git invocation with that repo as its working directory — including `check-pii-shapes.sh`'s internal calls. Verified live: with `GIT_CONFIG_GLOBAL` pointed at a hostile `.gitconfig` whose `excludesFile` matches this record's own literal filename, the suite still exits 0 and every `hand-off shape scan` assertion still reports `PASS`.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES (round 3)`, Major 1); `tests/check-pii-shapes/run.sh` (the `git -C "$HOS_UT_REPO" config core.excludesFile /dev/null` and `git -C "$HOS_REPO" config core.excludesFile /dev/null` lines, `hand-off shape scan` section); `.shell-team/test-recipe.md` ("Environment quirks" section, the pin-the-excludes-file convention).

- decision: Extended the pin to `HOS_REPO` (assertions (a)-(c)) as well as `HOS_UT_REPO` (assertion (d)), beyond the Major finding's own reported scope of assertion (d) alone.
  reason: The Major finding's own text says assertions (a)-(c) are unaffected because their READ mechanism (`ls-files -s --cached`, or `git cat-file -p "HEAD:$path"`) never consults excludes once a path is tracked — true, but live-tested this round: `add_fixture_line "$HOS_REPO" "$HOS_RECORD_RELPATH" "$HOS_WORDS_LINE"` runs `git add`, itself an ignore-consulting call, and under the same hostile ambient config (`core.excludesFile` matching this record's own literal filename) `git add` refuses the path outright and aborts the whole script under `set -e` — before assertion (d) is ever reached, since it comes later in the section. This is the identical underlying property the Major names (an ignore-consulting git call, unpinned, inheriting the ambient environment) reached through a second call site the finding's illustrative text did not enumerate. Reproduced live both ways: unpinned, the suite aborts partway through the `hand-off shape scan` section under this hostile config; pinned, the full suite (all sections) exits 0 under the same config.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES (round 3)`, Major 1 — the general property, "any ignore-consulting git call"); `tests/check-pii-shapes/run.sh` (`add_fixture_line` function definition, and its call site on `$HOS_REPO`); this task's own round-4 engineer briefing ("any other place in `tests/check-pii-shapes/run.sh` your branch added that touches untracked or ignore-consulting enumeration").

- decision: Built the round-3-requested control fixture (proving the neutral pin "has teeth") as a hostile `core.excludesFile` matching the record's own literal filename (`uncommitted-record.md`), rather than a broader `*.md` glob, and as a separate scratch repo (`HOS_UT_HOSTILE_REPO`) rather than reusing `HOS_UT_REPO`.
  reason: The Major finding names both forms as equally plausible contributor configurations ("match `*.md` … or the literal filename `uncommitted-record.md`"); the literal-filename form was chosen because it is scoped exactly to this fixture's own record path and, live-tested, does not collide with or break any other fixture's own differently-named `.md` files elsewhere in this large suite (confirmed: the full suite still exits 0 under a global `core.excludesFile` matching only this literal filename), whereas live-testing the broader `*.md` form against the same ambient mechanism showed it aborts an unrelated, pre-existing, out-of-scope fixture (`placeholder forms are not findings`, from an earlier task) via the same `git add`-refuses-an-ignored-path mechanism — a real but out-of-scope defect this task does not touch or fix. A separate repo was used for the control (rather than re-running `HOS_UT_REPO` a second time) so the neutral-pin assertion and the hostile-pin control each exercise one unambiguous git-config state, never a repo whose config is mutated a second time after its first assertion already ran.
  grounding: `.shell-team/reviews/T-1143.md` (`### Codex Review verdict: REQUEST_CHANGES (round 3)`, Major 1, "a plausible, unremarkable choice for scratch notes"); `tests/check-pii-shapes/run.sh` (`HOS_UT_HOSTILE_REPO` and its control assertion, `hand-off shape scan` section); `tests/rollup-track/run.sh` (lines ~159-182, the precedent two-arm hostile-fixture-plus-teeth-control shape this control follows).

<!-- END provenance: T-1143 -->
