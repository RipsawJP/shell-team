<!-- BEGIN provenance: T-1014 -->

- decision: the hoisting hazard the spec's Assumptions section flags (`FLAG_STOPS` sitting below the `PAYLOAD.rows.map(adaptRow)` call, so a derivation reading it would see `undefined`) is resolved by moving BOTH the `FLAG_STOPS` array literal and the new `flagFromLabel(label)` function to immediately above `adaptRow`'s own definition, rather than leaving them at their original position and instead deferring `FLAG_STOPS`'s read (e.g. wrapping the array in a getter function, or reading it lazily inside `flagFromLabel` via a closure over a not-yet-assigned outer variable).
  reason: `var` hoists the declaration but not the assignment, so anything that reads `FLAG_STOPS` before its `= [...]` line executes sees `undefined` regardless of how the read is wrapped — a getter/closure indirection does not change WHEN the assignment runs, only postpones the crash to first-call time instead of load time. Moving the assignment itself earlier in top-level execution order is the only fix that actually changes the hazard; and AC1 forbids the alternative most likely to look tempting (a second `FLAG_STOPS`-shaped literal placed conveniently near `adaptRow`), since it counts `READY_FOR_ARCH` occurrences and requires exactly one. Placing both declarations directly above `adaptRow` (rather than, say, at the very top of the IIFE) keeps the single-source array and its one match-rule function textually adjacent to the one place that calls it, which is also where a future reader would look first.
  grounding: templates/loop-replay.html, the comment block directly above `function adaptRow(row){` ("declared here, ABOVE adaptRow's own call site below, so the derivation never reads it before it is assigned"); .shell-team/specs/T-1014-flag-rail-data-path.md, Assumptions bullet beginning "`FLAG_STOPS` is declared after `adaptRow` today" ("how the engineer resolves it is their call, and AC1's exactly-one-occurrence count is what forbids the tempting resolution of a second copy").

- decision: the node harness's `rail-step-back` assertion (`tests/gen-loop-replay/run.sh`) compares the FULL SET of reached flag names (via `.indexOf` membership over an array built from `railStopsB`) against the expected four-token list, rather than only asserting a reached COUNT of 4.
  reason: a count-only assertion (`reachedFlags().length === 4`) would pass identically whether the four reached dots are the correct four stops or some other wrong-but-same-sized subset — exactly the detector blind spot the spec's own mutation-self-check section calls out by name ("Does `rail-step-back` assert a *set*, or only a count that a wrong-but-same-sized set would satisfy?"). Asserting set membership (every expected flag present, and the reached count equal to the expected list's length, so no extra flag can sneak in either) makes the assertion non-vacuous against a subtler defect than a plain off-by-one in `updateRailB`'s reached-derivation loop.
  grounding: .shell-team/specs/T-1014-flag-rail-data-path.md, Notes for engineer, "attack the detector's own blind spots" paragraph ("Does `rail-step-back` assert a *set*, or only a count that a wrong-but-same-sized set would satisfy?"); tests/gen-loop-replay/run.sh, the `rail-step-back` block inside `boardFlagRail()` (`reachedSetOk = reachedAtStepBack.length === expectedReached.length && expectedReached.every(...)`).

- decision: inside the node harness's `boardFlagRail()` IIFE, a `rail-precondition` failure on the `flag-rail` fixture does NOT `return` early — every other rail-* assertion still runs against the same `scene` — and the two assertions with a positional dependency on the fixture's exact shape (`rail-step-back`'s `flagEvents[4]`, `rail-determinism`'s `flagEvents[2]`) each carry their own explicit `if (!x) { fail(...); }` existence guard, rather than a single early `return` protecting all of them at once (an earlier draft of this file did return early, and was changed after mutation self-check #2 showed it swallowing a more specific diagnostic — see below).
  reason: an early return after a failed precondition avoids an uncaught `TypeError` on the two positionally-dependent assertions, but it does so at a real cost the mutation self-check surfaced directly: mutation #2 (trim the label before comparing) changes the trailing-space near-miss row into a genuine flag, which changes `flagEvents.length` from 7 to 8 — under the early-return design, this trips `rail-precondition` and the run stops there, so `rail-near-miss` never gets to report the specific, more informative violation ("flagFromLabel() derived a flag for the near-miss label 'READY_FOR_QA '") that Notes for engineer's own mutation table names for this exact mutation. Every assertion other than `rail-step-back`/`rail-determinism` has no positional dependency on the exact count (they filter/some() over whatever `flagEvents`/`railStopsB` actually are), so letting them run costs nothing and gains the more specific attribution; the two that do need a defined element get their own narrow guard instead of a blanket one.
  grounding: .shell-team/specs/T-1014-flag-rail-data-path.md, Notes for engineer, "Producer-run mutation self-check" item 2 ("Make the match rule trim the label before comparing → `rail-near-miss` must go red on the trailing-space row specifically"); tests/gen-loop-replay/run.sh, `boardFlagRail()`'s precondition `if (...) { fail(...); } else { ok(...); }` (no `return`) and the separate `if (!stepBack || ...)` / `if (!midEvt)` guards on `rail-step-back` / `rail-determinism`; self-run mutation evidence (mutation #2 against a scratch-copied template: with the early-return design `rail-near-miss` never ran and only the generic `rail-precondition` message appeared; without it, both `rail-precondition` and the specific `rail-near-miss` message on the trailing-space row appear).

- decision: the `rail-determinism` assertion runs FIRST among the rail-* assertions (immediately after `rail-precondition`, before `rail-forward-reached`/`rail-step-back`/`rail-rewind`), rather than in the position implied by its listing order in the spec's assertion table (after `rail-rewind`).
  reason: mutation self-check #5 (restore the module-level `flagStopsReached` accumulator) was run against the assertions in their original, table-listed order and produced a false PASS on `rail-determinism` — not the RED the spec's own mutation table predicts ("`rail-rewind` and `rail-determinism` red, `rail-forward-reached` still green"). The cause: `rail-forward-reached` and `rail-rewind` each call `scene.updateRailB(scene.TOTAL_MS)` before `rail-determinism` gets to run its own forward-vs-rewind comparison, and a broken monotonic accumulator never clears once anything has called `updateRailB` with a large `simMs` — so by the time `rail-determinism` ran, the accumulator was already saturated to "everything reached" for BOTH of its own forward and rewind-first snapshots, which then compared as identical (a false pass) purely because of assertion execution order, not because the accumulator bug was actually absent. Running `rail-determinism` first, before any other assertion has a chance to saturate a broken accumulator, restores the real distinguishing signal (verified: the same mutation #5 artifact now produces the exact red/red/green pattern the spec's mutation table names).
  grounding: .shell-team/specs/T-1014-flag-rail-data-path.md, Notes for engineer, mutation self-check item 5 ("Restore the module-level accumulator → `rail-rewind` and `rail-determinism` red, `rail-forward-reached` still green (that asymmetry is the whole point)"); tests/gen-loop-replay/run.sh, the `rail-determinism` block's own comment ("Runs BEFORE every other rail-* assertion below — deliberately... mutation self-check #5 caught this ordering blind spot"); self-run mutation evidence (mutation #5 against a scratch-copied template restoring a module-level accumulator: in file order, `rail-determinism` was green — a false pass; reordered to run first, it is red, matching the spec's predicted pattern exactly, with `rail-forward-reached` still green in both orderings).

<!-- END provenance: T-1014 -->
