# T-1089 decision provenance

<!-- BEGIN provenance: T-1089 -->
- decision: the six new note lines (`- oracle-claim:`, the three `- claim-limit:` lines, and the two `- supersedes:` lines) are placed together as one new paragraph at the end of `## Limits and what is not computable`, rather than split between that section and `## Supersession and follow-ups`, and rather than inserted mid-section among the existing "Fixed regardless" / "Resolved this round" / "New this rework round" paragraphs.
  reason: the spec's own Notes for engineer state no criterion pins the section ("your call"), but requires the two `- supersedes:` lines to sit "beside" the claim/limit lines. Keeping all six together in one place, rather than splitting the declaration (oracle-claim + limits) from the supersession (which claims those limits displace), avoids a reader having to cross-reference two sections to see that the two `- supersedes:` lines are the direct consequence of the `- oracle-claim:`/`- claim-limit:` lines immediately above them. Appending as a new paragraph, rather than interleaving with the three existing "rework round" paragraphs, keeps each of those paragraphs' own byte-for-byte history (each documents a specific prior rework round) undisturbed and makes this task's own addition a clearly separate, dated unit in the file's own append-only narrative style.
  grounding: `.shell-team/specs/T-1089-harness-recut.md`, `## Notes for engineer`, "Where each new claim goes" bullet ("your call — no criterion pins the section... the two `- supersedes:` lines belong beside them"); `docs/loop-engineering/tier2-subject-harness.md`, `## Limits and what is not computable`, the new final paragraph beginning "**T-1089 — the oracle's claim, re-scoped and bounded explicitly...**".
- decision: the corrected `- baseline-basis:` line uses the word "agree" (never "coincide") to describe `phase-multiplexing.md` line 226's own true claim about `rework-round-cost`/`judge-pass-proxy`, and reframes the sentence around "a claim true in that line's own scope... and not transferable to this arm without checking first" rather than simply deleting the false half.
  reason: **AC2** bans the literal `coincide` from the corrected line itself, but the corrected line still has to state, honestly, that `phase-multiplexing.md`'s own claim about those two other quantities is true — deleting all mention of agreement would understate what that file actually says and make the correction read as a blanket denial rather than a scope correction. "Agree" carries the same meaning without tripping the literal-string ban, and it lets the sentence distinguish "true there" from "not transferable here" — the exact shape `docs/loop-engineering/phase-multiplexing.md` line 226 itself calls for once the false transfer is removed (spec `## Problem`: "The corrected text must therefore distinguish itself from a true claim rather than contradict it").
  grounding: `.shell-team/specs/T-1089-harness-recut.md`, `## Summarized sources` sixth bullet (`phase-multiplexing.md` lines 205/226/257/262-263) and `## Problem`'s "line 226's basis paragraph is true in its own scope" sentence; **AC2**'s own check body (`if grep -qF -- 'coincide' "$T/bb"; then rc=1; fi`).
- decision: the new `README.md` obligation is a single new `- subject-ac: 8` line rather than a rewording of `- subject-ac: 5`'s own pattern clause, and its first physical line carries both the literal `10#` and the literal `decimal` rather than spreading the decimal-reading statement and the `10#`-normalization detail across the item's wrapped continuation lines.
  reason: `- subject-ac: 5`'s own frozen text (unedited by this task) already states the pattern `^-?[0-9]+$`; adding the decimal-reading obligation as a *new*, additive line rather than editing that one avoids ambiguity about whether subject-ac 5 itself changed (it did not — **AC3** only requires the new obligation to exist, at least eight lines total). Keeping both required literals on the first physical line of the new item was a mechanical necessity discovered while verifying **AC3** live: the check's own extraction regex (`^- subject-ac: [0-9]+ — .+$`) only captures a bullet's *first* physical markdown line, so a wrapped continuation line carrying `10#` or `decimal` would never reach the extracted set the check searches — this was caught by running the check against an earlier draft that wrapped the two literals onto a later line, which failed, and fixed by moving both literals onto the first line.
  grounding: `docs/loop-engineering/subjects/subject-01/README.md`, `- subject-ac: 8 —` line (first physical line carries both `10#` and `decimal`); `.shell-team/specs/T-1089-harness-recut.md` **AC3**'s check body (`grep -E '^- subject-ac: [0-9]+ — .+$' "$RM" > "$T/acs"` — a single-line regex extraction); self-check: `bash bin/check-acs.sh .shell-team/specs/T-1089-harness-recut.md` **AC3** PASS after the fix (see hand-off), FAIL on the earlier wrapped draft (observed directly, not merely inferred).
- decision: the new "enumerated-case instrument" claim text added to `acceptance.sh`'s own header comment does not name or path-reference `docs/loop-engineering/tier2-subject-harness.md` (the note), even though `README.md`'s own parallel addition does name it by path.
  reason: **AC9** in T-1086's own frozen spec (unedited, out of this task's allow-list) sweeps exactly `acceptance.sh` and `regenerate.sh` for the literal `docs/loop-engineering`, among other repository-path literals, to prove the two committed scripts are self-contained and carry no dependency back into this repository's own layout. An earlier draft of the new header comment wrote "See docs/loop-engineering/tier2-subject-harness.md's own `- oracle-claim:`..." and this was caught live: re-running T-1086's own **AC9** check against the edited tree turned it from PASS to FAIL, a real regression against a byte-locked invariant this task must not touch, not a disclosed merge-point-scoped class. The fix rewords the sentence to reference "this harness's own measurement note" without spelling out its path, preserving the cross-reference for a human reader while removing the literal token the sweep matches on. `README.md` carries no equivalent self-containment sweep (T-1086's **AC9** names only the two scripts), so naming the note's path there is unaffected and left as originally drafted.
  grounding: `.shell-team/specs/T-1086-subject-harness.md` **AC9** check body (`grep -cF -- 'docs/loop-engineering' "$P"` over exactly `acceptance.sh` and `regenerate.sh`); `docs/loop-engineering/subjects/subject-01/acceptance.sh` header comment (final wording, no `docs/loop-engineering` literal); self-check: `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1086-subject-harness.md` — **AC9** observed FAIL against the draft that named the path, PASS after the reword (see hand-off for both runs' summary lines).
- decision: the full-population Blast radius diff for this task reuses T-1086's own round-1 method verbatim (a `git worktree add --detach` at the branch-point commit for base-side verdicts, an `xargs -P 8` parallel fan-out of `bash bin/check-acs.sh <spec>` over every merged spec, per-spec logs, then a key-level join on `(spec, ACn)`) rather than inventing a lighter-weight sampling approach (e.g. only re-running specs that mention the touched paths by name).
  reason: the spec's own `## Blast radius` "Method" paragraph requires "enumerate every merged spec at the branch point, run every criterion... and difference the verdict sets criterion by criterion" at the `mechanism` verification class's full price, explicitly not "by widening a search" — a name-based pre-filter would be exactly the pattern-search shortcut the spec's own Method paragraph rules out, and T-1086's own hand-off already found a criterion class invisible to that kind of filter (a `bin/team-paths.sh --get runs` resolution reachable only by actually running the check). Reusing the precedent method rather than a new one also means this task inherits a proven procedure (including its own documented auto-background hazard) instead of re-discovering the same pitfalls fresh.
  grounding: `.shell-team/specs/T-1089-harness-recut.md`, `## Blast radius`, "Method, at the `mechanism` class's full price" paragraph; `.shell-team/todo.md` T-1086's own hand-off, "Local test result — T-1086 (engineer, round 1...)" section, item (d) ("Base-side per-criterion verdicts read from a scratch `git worktree add --detach`... an 8-way `xargs -P 8` fan-out"); this round's own fan-out logs under `/tmp/claude-502/t1089-radius/` (see hand-off for the reconciliation numbers).
<!-- END provenance: T-1089 -->
