# T-1080 decision provenance

<!-- BEGIN provenance: T-1080 -->
- decision: for surface S1, added the conditional `READY_FOR_QA` entry as a **new, separate bullet** immediately after `agents/codex-reviewer.md`'s existing `- Task is at \`READY_FOR_REVIEW\` in \`tasks/todo.md\`.` precondition line, rather than appending the conditional clause onto that same line.
  reason: **AC3**'s check line anchors the preserved-invariant assertion with a full-line regex (`^- Task is at .READY_FOR_REVIEW. in .tasks/todo\.md.\.$`) against both the base blob and the HEAD file's own `## Preconditions` section — appending text after the line's own terminal period would have broken that anchor on the HEAD side even though the substance of the precondition was unchanged. A new sibling bullet keeps the original line byte-identical while still placing the conditional clause in the same section AC3 extracts.
  grounding: `.shell-team/specs/T-1080-depth-axis-contract.md` **AC3**'s own check body (the `grep -qE '^- Task is at .READY_FOR_REVIEW. in .tasks/todo\.md.\.$'` line, run against both `"$T/hp"` and `"$T/bp"`).
- decision: for erratum (b)/(c), split the two required literal citations (`harness-agent-concurrency`, `concurrent-overlapping`) across two separate physical lines — one in the rewritten `agent-tool-concurrent-launch` substrate row, one in the rewritten Limits entry — rather than repeating both literals twice within one of the two rewritten lines.
  reason: **AC12**'s check uses `grep -cF` for both literals, which counts matching **lines**, not occurrences within a line; a `-ge 2` floor on `harness-agent-concurrency` can only be satisfied by two distinct lines each containing the string once (or one line containing it twice, which reads as artificially repetitive prose). Placing one citation per corrected site both satisfies the count mechanically and matches the errata's own D5 framing (two separately-grounded corrections, not one corrected site restating itself).
  grounding: `.shell-team/specs/T-1080-depth-axis-contract.md` **AC12**'s own check body (`test "$(grep -cF -- 'harness-agent-concurrency' "$N" || true)" -ge 2`); `docs/loop-engineering/harness-agent-concurrency.md`'s own `## Supersession and follow-ups` section (the two `- supersedes:` lines this task's erratum (b)/(c) transcribe).
- decision: for the QA-FAIL discard rule (S4) and the counting carve-out (S5), placed both the `discarded-review: qa-fail` rule and the byte-exact shared clause (`a discarded concurrent-review-window verdict is not a cycle and adds nothing to this count`) inside **one single bullet** in `skills/run/SKILL.md`'s Validate region, immediately after the existing `--iteration` definition line, rather than as two separate bullets.
  reason: **AC6** requires the set of lines carrying `discarded-review: qa-fail` to also carry both `--iteration` and `same-class-2` literals, and the Notes for engineer explicitly names the T-1002 interventions-gate bullet's own "one bullet, one rule, both counters named in it" shape as the precedent to follow — splitting the S4 rule and the S5 clause across two bullets would still satisfy AC6 and AC7 independently, but consolidating them into one keeps the discard rule and its two counting consequences (loop-guard's `--iteration`, SKILL.md's own `same-class-2`) legible as a single norm rather than two half-rules a reader has to join.
  grounding: `.shell-team/specs/T-1080-depth-axis-contract.md` Notes for engineer ("The existing T-1002 interventions-gate bullet is the precedent to follow in shape: one bullet, one rule, both counters named in it.") and **AC6**'s own check body (the `$T/dr` extraction requiring `--iteration` and `same-class-2` on the same token-carrying line set).
- decision: for `bin/loop-guard.sh`'s header carve-out (S5), wrote the shared byte-exact clause on its own single comment line (`#   a discarded concurrent-review-window verdict is not a cycle and adds nothing to this count`) rather than word-wrapping it across the surrounding 80-column comment prose the rest of the header uses.
  reason: a first pass wrapped the clause across two comment lines to match the header's existing wrap width, and `grep -cF` (line-based) then read zero matches for the clause in the script even though the words were all present — confirmed by a local mutation probe (wrapped: 0 matches; unwrapped: 1 match). **AC7**'s cross-file consistency lock greps for the literal as one line-contained fixed string in both files, so the clause has to survive on one physical line even at the cost of a locally longer line than the header's own prose style.
  grounding: `.shell-team/specs/T-1080-depth-axis-contract.md` **AC7**'s own check body (`test "$(grep -cF -- "$CL" "$G" || true)" -ge 1`); this task's own local mutation probe (`grep -cF -- "$CL" bin/loop-guard.sh` returning `0` on the wrapped draft and `1` after unwrapping to one line).
- decision: for S6/S7's orchestrator-side duties (naming `test-edit-deferred`, `pinned-read`, `record-commit-deferred`, and `qa-handoff: unavailable-concurrent-window` in `skills/run/SKILL.md`), consolidated all four into one new bullet inside step 6's block (naming the launch-brief duties for both the `codex-reviewer` and `qa-verifier` invocations together) rather than four separate bullets scattered across steps 4/5/6.
  reason: **AC2**, **AC8** and **AC9** each require `skills/run/SKILL.md` to carry specific tokens, but none constrains where in the file they appear; grouping them in one place keeps the orchestrator's own launch-brief obligations for a declared window readable as one coherent duty (declare window + pinned SHA + no-edit instruction + no-commit instruction) rather than four independent facts a reader has to reassemble from different steps.
  grounding: `.shell-team/specs/T-1080-depth-axis-contract.md` D4's own parallel-surface symmetry table (the `SKILL.md` (orchestrator) column for S6/S7, both `mirrored-now`) and **AC2**/**AC8**/**AC9**'s own check bodies (token-presence assertions with no line-position constraint).
- decision: (rework round 1, Codex review round 1 Major #1) fixed S6's board-hand-off/"no working-tree edit at all" conflict by adding the exception **inline, inside the same sentence** that states the no-edit rule in `agents/qa-verifier.md`, quoting T-073's own phrasing ("apart from your own hand-off append to the board (`tasks/todo.md`)") verbatim, rather than adding a separate cross-referencing sentence or bullet.
  reason: the review's own fix instruction asked for the scope to be "recoverable from one sentence" rather than requiring a reader to reconcile three separate bullets (S6, T-073, S7) — an inline exception using T-073's own already-established wording satisfies that literally, and reusing the exact phrase (rather than paraphrasing "the board edit") keeps the two rules' vocabulary identical so a future reader greps one string, not two near-synonyms.
  grounding: `.shell-team/reviews/T-1080.md` Major #1 ("Fix: make the board hand-off append an explicit, named exception inside S6's own sentence, mirroring T-073's own phrasing"); `agents/qa-verifier.md`'s own pre-existing T-073 Rule ("apart from your own hand-off append to the board (`tasks/todo.md`)"), unedited by this round, now quoted verbatim inside S6 rather than paraphrased.
- decision: (rework round 1, Codex review round 1 Major #2) closed S7's write-isolation gap by instructing the reviewer to **buffer its review-record and capture-file writes under `$TMPDIR` and return them to the coordinating session** (the review's first offered option) rather than widening `qa-verifier`'s own T-073 clean-tree assertion to tolerate the reviewer's declared write paths (the review's second offered option).
  reason: the coordinator's own rework instruction named the first option as the one to take ("Fix per the reviewer's first option (it matches S7's existing framing)"), and it is also the option consistent with this task's own Non-goals ("No new agent, skill, template, prompt block or checker" and "No behavioral change to `bin/loop-guard.sh`") — widening the clean-tree assertion would have meant teaching `qa-verifier`'s own T-073 check to recognize a *new*, window-conditional exception class (untracked reviewer-owned paths), a heavier and more checker-adjacent change than instructing the reviewer to keep its own writes out of the shared tree until the window closes. No script (`codex-capture.sh`, `check-*.sh`) was edited; the buffering is stated as a role-prompt instruction only, consistent with the `no-mechanism` verification class.
  grounding: `.shell-team/reviews/T-1080.md` Major #2 ("Fix: either instruct the reviewer to buffer these writes under `$TMPDIR`... or explicitly widen QA's clean-tree assertion... inside the window"); the coordinator's rework message ("Fix per the reviewer's first option (it matches S7's existing framing)"); `.shell-team/specs/T-1080-depth-axis-contract.md` Non-goals ("No new agent, skill, template, prompt block or checker").
- decision: (rework round 1, Codex review round 1 Major #3) named the pinned-read mechanism (`git worktree add --detach <scratch> <SHA>`, substituted for `<repo>`) as an **inline comment inside each of the two `codex exec ... --cd <repo> ...` bash code blocks themselves**, rather than as a preceding prose paragraph outside the code fence or as a new, separate numbered sub-step.
  reason: the review's own finding named the exact defect as "the concrete invocation recipe in step 3 is not amended" and contrasted it with "this document's own convention elsewhere (T-097/T-107) ... to keep the executable bash blocks in lockstep with the prose describing them" — putting the mechanism as a comment directly above the command line inside the same fenced block is the literal reading of "in the actual invocation recipe itself," and keeps the T-107-step marker-name convention intact (the marker comment `# T-107-step: codex` still opens the block; the new comment lines sit between it and the command, which the existing "read each block as its own standalone invocation" instruction already tolerates without a marker-parsing change).
  grounding: `.shell-team/reviews/T-1080.md` Major #3 ("Fix: name the concrete mechanism ... in the actual invocation recipe, not only in the surrounding prose"); `agents/codex-reviewer.md`'s own T-107-step marker-name convention ("these five blocks are referred to by their marker name ... never by an ordinal 'step N'"), unedited by this round; `.shell-team/specs/T-1080-depth-axis-contract.md`'s own `## Blast radius` section (the `git worktree add --detach` pattern this fix reuses verbatim, per the review's own citation of it).
- decision: (rework round 1, Codex review round 1 Minor) fixed erratum (b)'s missing plugin-role scope qualifier by **quoting `docs/loop-engineering/harness-agent-concurrency.md`'s own Limits sentence verbatim** ("unobserved: whether general-purpose Agent-tool concurrency extends to a plugin-role subagent type...") inside the corrected substrate row, rather than writing a new, shorter paraphrase of the same caveat.
  reason: the review's own instruction was to "read that source's verdict section and mirror its qualifier" — quoting the cited note's own sentence verbatim is the literal way to mirror it (rather than risk a paraphrase drifting from what that note actually discloses), and it keeps this errata row's own established pattern of citing the superseding note's language rather than restating it in this task's own words.
  grounding: `.shell-team/reviews/T-1080.md` Minor ("Fix... read that source's verdict section and mirror its qualifier" / Fast-follow disposition, "file-an-issue"); `docs/loop-engineering/harness-agent-concurrency.md`'s own `## Limits and what is not computable` section, the `unobserved: whether general-purpose Agent-tool concurrency extends to a plugin-role subagent type` line (unedited by this round, quoted here).
<!-- END provenance: T-1080 -->
