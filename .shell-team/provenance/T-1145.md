<!-- BEGIN provenance: T-1145 -->

- decision: Where to insert the convergence-block validation and derivation code in `bin/rework-digest.sh`, and how to gate it (a single `CONV_REQUESTED` flag computed from presence of any of the three new flags, rather than three independent opt-in checks).
  reason: The spec's Notes for engineer name the insertion points (post-`flush_record` cross-field validation, derivation reusing the existing `uniq_classes`/`uniq_counts` repetition-judgment arrays) but leave the exact gating mechanism to the engineer. A single flag computed once, checked first for the `--trigger` refusal and then for the mandatory-together requirement, keeps every downstream branch (series/trend/never-dropped/instances derivation, output assembly) behind one condition, so the no-flag path is provably unreached rather than reached-and-happening-to-agree — this is what AC3's byte-identity assertion depends on structurally, not just empirically.
  grounding: `bin/rework-digest.sh` (`CONV_REQUESTED` variable, first set after the early-mode contradiction check, gating the validation block, the derivation block and the `if [[ "$CONV_REQUESTED" -eq 1 ]]` guard inside the digest-assembly `else` branch); `.shell-team/specs/T-1145-stop-digest-convergence.md` `## Notes for engineer` ("validation of the cross-field rules... belongs with the existing post-loop checks... after flush_record").

- decision: How to detect and report the `--rounds-total < highest --round` case, and in what order the four mandatory-together / cross-field refusals are checked (TRIGGER-paired first, then `--rounds-total` missing, then `--never-dropped` missing, then the round-count bound, then the per-repeated-class `--instance` completeness, then the per-supplied-`--instance` validity).
  reason: The spec's AC4 requires each of eighteen malformed shapes to refuse with exit 2 / empty stdout / usage on stderr, but does not require a specific message or a specific check to be the one that fires for a given malformed shape — only that *some* fail-closed path fires. Ordering the checks from "which mode" to "which mandatory field" to "which numeric bound" to "which per-class completeness" follows the natural dependency order (you cannot validate round-count bounds without a `--rounds-total` value, and cannot validate `--instance` completeness without knowing the repeated-class set, which is already computed earlier in the script). A live mutation probe (removing the explicit `--rounds-total is required` fail call) showed the max-round-bound check still refuses correctly whenever at least one record exists (since `10#""` evaluates to `0`, which is always less than any recorded round `>= 1`) — this is disclosed as a defense-in-depth property discovered during verification, not a design the ordering was chosen to rely on; the explicit check is kept because its message is clearer and its correctness does not depend on the arithmetic-empty-string coincidence.
  grounding: `bin/rework-digest.sh` (the `# --- convergence block validation and derivation` section); `.shell-team/specs/T-1145-stop-digest-convergence.md` AC4 (eighteen-case enumeration, no per-case message pinned) and `## Notes for engineer` gotcha ("A `--never-dropped` value with no `=` and one with an empty name... are distinct malformed shapes").

- decision: The `never-dropped:`/`instances:` per-entry tail ordering — never-dropped entries print in first-seen `--never-dropped`-flag order; instance entries print in the first-seen order of the *repeated class in the records* (i.e. `uniq_classes` order filtered to count>=2), not in `--instance`-flag supply order.
  reason: The spec's Notes for engineer state explicitly that "the list on the `instances:` line follows the same first-seen order the existing `repeated-classes:` line already uses, so the two lines line up for a reader" — this is a frozen requirement, not a free choice, and the implementation reuses the already-computed `uniq_classes`/`uniq_counts` arrays (in their existing first-seen order) to build `repeated_classes_ordered` rather than iterating `INST_CLASSES` in supply order.
  grounding: `bin/rework-digest.sh` (`repeated_classes_ordered` built from `uniq_classes`/`uniq_counts`, then the `inst_tail` loop iterates `repeated_classes_ordered` and looks up each class's supplied `--instance` value); `.shell-team/specs/T-1145-stop-digest-convergence.md` `## Notes for engineer` ("The per-entry list on the `never-dropped:` line follows the first-seen order of the `--never-dropped` flags; the list on the `instances:` line follows the same first-seen order the existing `repeated-classes:` line already uses").

- decision: Where in `skills/run/SKILL.md` and `skills/goal/SKILL.md` to splice the canonical `convergence-escalation.md` bullet — immediately after the existing "Rework-history digest (T-058)" bullet and before "Re-routing branch (T-063)" — rather than after the extension-ratifier or means-ends-reflection bullets.
  reason: Topical adjacency: the canonical block's content is entirely about how to invoke `rework-digest.sh` and how to open the escalation message with its output, which is exactly the "Rework-history digest" bullet's own subject; placing it there keeps the digest-related bullets contiguous and does not require re-reading the whole numbered list to find the convergence instruction. AC10/AC11 do not require a specific position inside the region (only that the required fixed strings and the three pre-existing T-1095 anchors are all present somewhere in the region, and that the two inversion strings occur zero times), so position was free to choose on readability grounds.
  grounding: `skills/run/SKILL.md` (new bullet directly after "Rework-history digest (T-058)" and before "Re-routing branch (T-063)"); `skills/goal/SKILL.md` (same relative position); `.shell-team/specs/T-1145-stop-digest-convergence.md` AC10/AC11 (region-scoped presence checks with no position requirement beyond the region boundaries).

- decision: The exact wording of the widened signature-refusal message — appended `" or never-dropped component name"` to the existing message rather than restructuring it into two separate sentences or a field-labeled form.
  reason: The spec's Notes for engineer propose exactly this wording ("digest would leak goal-state signature token(s) '<sig>' — rename the offending class slug or never-dropped component name") as a suggestion, explicitly noting the wording may be adjusted as long as the two fixed strings `signature token` and `component name` survive. The suggested wording was adopted verbatim because it satisfies AC6's two fixed-string requirements, keeps the pre-existing fixed string `signature token` byte-identical (protecting the existing fixture at `tests/rework-digest/run.sh` that greps for it), and required no further engineering judgment to improve on.
  grounding: `bin/rework-digest.sh` (`fail "digest would leak goal-state signature token(s) '${sig}' — rename the offending class slug or never-dropped component name"`); `.shell-team/specs/T-1145-stop-digest-convergence.md` `## Notes for engineer` ("A wording that satisfies both: `digest would leak goal-state signature token(s) '<sig>' — rename the offending class slug or never-dropped component name`").

<!-- END provenance: T-1145 -->
