<!-- BEGIN provenance: T-1029 -->

- decision: AC11(b)'s mutation "delete the phrase `not subject to the out-of-input-space exemption` from F3 in both the spec and the agent file (AC4 red, AC3 red)" was executed as **an asymmetric single-file deletion** (removed only from the spec's frozen F3 block, `agents/qa-verifier.md` left untouched) rather than an identical deletion applied to both files.
  reason: I first tried the literal symmetric reading — deleting the same substring from both the spec's extracted F3 block and the agent file's F3 line — and measured that AC3 stayed GREEN under that mutation, because AC3 is a byte-equality lock between the two copies and an identical edit to both sides preserves equality. AC4 alone went red. Since the spec's own AC11(b) text states this mutation should turn **both** AC3 and AC4 red, I tried the asymmetric form (spec only) and measured both ACs going red, which matches the stated expectation and is the mutation actually recorded as run in the hand-off.
  grounding: `.shell-team/specs/T-1029-claim-fidelity-qa-step.md`, AC11(b) ("delete the phrase ... from F3 in both the spec and the agent file (AC4 red, AC3 red)") and AC3's own `check:` line (the `cmp -s "$T/f3" "$T/nx"` byte-equality assertion between the frozen extract and the agent file's line) — the check line is what makes the symmetric-deletion reading vacuous for AC3 and the asymmetric reading the one that actually reproduces the stated outcome.

- decision: Assumption relayed-claim 1 (issue #133's body and two-part proposal) was resolved as **measured/confirmed**, not `environmentally-unverified`, by fetching the issue directly with `curl -s -H "Accept: application/vnd.github+json" https://api.github.com/repos/RipsawJP/shell-team/issues/133` after the `gh` CLI failed in this sandbox with a TLS trust error.
  reason: The spec's Assumptions predicted this claim would likely end `environmentally-unverified` for "any role without network access," grounded in pm-spec's own no-network constraint. The engineer role in this session is not under that same constraint, and `curl` to `api.github.com` (an allow-listed host) succeeded where `gh` did not — so the honest, evidence-based classification is that the claim was actually checked against the issue's own bytes, not that it was environmentally out of reach. Reporting it as `environmentally-unverified` here would understate the verification actually performed.
  grounding: live `curl` response captured to a scratch file during this task (issue #133 title: "QA verifies the paraphrase, not the source: claim-fidelity defects passed QA twice and were caught only by the cross-provider round"; its `## Proposal` section's two numbered items match the spec's `## Summarized sources` characterization of #133 clause by clause — the spec-template slot item and the qa-verifier open-and-verify item, with no checker requested).

- decision: `.shell-team/interventions/T-1029.md` was **not created** by this engineer pass, even though `bin/check-acs.sh`'s AC7 requires it to exist and this leaves AC7 measured FAIL (not the target 10/0/1) at hand-off time.
  reason: The task prompt that dispatched this engineer pass states explicitly: "you do not edit the spec's intent block or `.shell-team/interventions/T-1029.md` (orchestrator-owned)." Two options existed — create the sentinel file myself to reach the numeric target, or honor the explicit prohibition and report the resulting gap. I chose the latter: the file records interventions across the task's whole lifecycle (a scope this scoped engineer invocation cannot see in full), and creating it myself would be asserting a "no interventions occurred" claim on visibility I do not have, which is exactly the kind of unverified-state assertion this repository's playbook warns against.
  grounding: this task's own dispatching prompt ("you do not edit the spec's intent block or `.shell-team/interventions/T-1029.md` (orchestrator-owned)"); `bin/check-interventions.sh`'s own header comment ("a durable record of those moments, written by the orchestrator — the one participant that can see them").

<!-- END provenance: T-1029 -->
