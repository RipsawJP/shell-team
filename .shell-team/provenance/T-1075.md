# T-1075 decision provenance

<!-- BEGIN provenance: T-1075 -->
- decision: wrote the Validate-phase decision point's mechanics reference as generic nouns ("population, degree, liveness and aggregation") rather than paraphrasing or partially quoting the fan-out step's own text, and pointed at it as "the shipped fan-out step below this list" rather than restating any of its three obligation sentences.
  reason: **AC3** requires each of `fixed before any instance is launched`, `per-instance liveness check` and `N = min(` to occur **exactly once** in the whole file — they already occur once each in the shipped fan-out step (T-1074). A second occurrence anywhere, including a near-paraphrase that happened to reuse one of those exact substrings, would break AC3's exactly-once count. Generic nouns naming the four mechanics categories (population/degree/liveness/aggregation) let a reader find the right place without risking a literal collision, and this is the same "point rather than copy" instruction the spec's body directive #3 and Notes for engineer both state explicitly.
  grounding: `.shell-team/specs/T-1075-fanout-adoption-versioning.md` **AC3** (the exactly-once check) and body directive #3 ("The decision point points at the shipped step rather than restating its obligations"); `skills/run/SKILL.md`'s own fan-out step (lines ~156-159, the three obligation sentences in their one committed location).
- decision: in `CONTRIBUTING.md`, placed the new pointer bullet ("Version numbers follow a value rule, not this list.") as the LAST bullet of the `## Cutting a release` list — after the existing "Draft the release body..." bullet — rather than immediately adjacent to the "Measure the release before naming it" bullet it cross-references.
  reason: **AC11** requires the "Measure the release before naming it" bullet to stay byte-identical to its branch-point blob; inserting the new pointer bullet directly beside it (before or after, without a blank-line boundary) risked an accidental adjacent-line edit under the same `Edit` call. Appending at the list's end is a pure insertion after the last existing bullet, leaving every earlier bullet's own bytes and its neighbours untouched, and it reads naturally as a closing pointer to the reader who just finished the mechanical steps.
  grounding: `.shell-team/specs/T-1075-fanout-adoption-versioning.md` **AC11** (byte-identity requirement on the "Measure the release before naming it" bullet); `CONTRIBUTING.md`'s own `## Cutting a release` list (the pointer bullet is the thirteenth and last line of that list).
- decision: worded the `**Release notes trace to the request.**` addendum as an inference tying the headline test's `inventory` disqualifier to what a release note should name, rather than transcribing a sentence that already exists verbatim in the operator's ruling.
  reason: `.shell-team/interventions/T-1074.md`'s second entry states the MINOR gate (adopter-perceivable benefit, both tests) and the wasted-cost principle in full, but does not carry a standalone sentence about release-note authorship; the nearest grounded material is entry 1's "origin-request trace" framing (a different context: planning-approval re-anchoring) and this task's own body directive #12/AC9's headline-test disqualifier (`inventory`). The spec's Notes for engineer instruct routing back to pm-spec rather than rewording if a ruling sentence "seems to need rewording to fit" — this addendum is not that case, since no ruling sentence exists to preserve here; it is a genuinely new anchor **AC8** requires, built from the same substance (the headline test) restated for the release-notes surface, not a paraphrase of an existing ratified sentence.
  grounding: `.shell-team/interventions/T-1074.md` entry 2 (the MINOR/headline-test substance) and entry 1 ("origin-request trace" framing); `.shell-team/specs/T-1075-fanout-adoption-versioning.md` **AC8** (the anchor is required) and **AC9** (the headline test's `inventory` disqualifier, which this addendum restates for a different surface); Notes for engineer ("If a sentence of the ruling seems to need rewording to fit, that is a signal to route back rather than to reword").
- decision: worded the new section's `**MAJOR.**` bullet by reusing the existing "Measure the release before naming it" bullet's own definition of the stable surface ("the plugin namespace and its command names") verbatim, rather than defining MAJOR independently.
  reason: `## Cutting a release`'s own line 107 already fixes what "the surface this project declared stable at v1.0.0" means, in those exact words; **AC11**'s role-division principle (this new section states its relationship to the existing measurement bullet, not a competing definition) is best served by MAJOR citing the same words rather than inventing a second, potentially-diverging description of the same surface.
  grounding: `CONTRIBUTING.md`'s existing "Measure the release before naming it" bullet ("the plugin namespace and its command names"); `.shell-team/specs/T-1075-fanout-adoption-versioning.md` **AC11** (role division with the existing measurement bullet).
<!-- END provenance: T-1075 -->
