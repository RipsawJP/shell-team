# T-1083 decision provenance

<!-- BEGIN provenance: T-1083 -->
- decision: the AC16 `- check:` line's total-count assertion (`test "$(grep -c '^- verdict: ' "$N")" = "1"`) is treated as a genuine, unfixable-by-engineer defect and left unresolved rather than worked around, because the only two levers that could satisfy it are both out of engineer authority or would break a sibling AC: rewriting the frozen `<!-- BEGIN intent-block: T-1083 -->` region (pm-spec's territory, never the engineer's) or deleting the aggregator's own verbatim-quoted `- verdict: <unit> — <text>` lines from `## Probe evidence` and the `<!-- BEGIN fanout-verdict: t1083live -->` block (byte-locked by **AC7** and required verbatim by **AC13**/**AC14**).
  reason: `bin/aggregate-verdicts.sh`'s own real part-file and `fanout-verdict` output format uses the identical literal line prefix `- verdict: ` that AC16's vocabulary-scoped grammar assumes is unique to the note's own verdict statement; the probe evidence (produced by the orchestrator after freeze, quoting the real tool's real output) unavoidably contains four such lines (two in the quoted part files, two inside the quoted `fanout-verdict` block's `verdict-region`), so the whole-file count is structurally 5, never 1, regardless of what text this engineer writes. This is the same defect class the repo's own lessons name ("Verifying a grep/regex invariant needs fixtures that deliberately collide with its own vocabulary") — except it surfaced through real tool output at freeze-then-probe time rather than through a synthetic test fixture, which is exactly why it could not have been caught before the probe ran.
  grounding: `.shell-team/specs/T-1083-agent-launch-fanout.md` AC16's own `- check:` line (`grep -c '^- verdict: '` asserted `= "1"`); `docs/loop-engineering/agent-launch-fanout.md` `## Probe evidence (raw, orchestrator-produced)` (the quoted part files and the `<!-- BEGIN fanout-verdict: t1083live -->` block, both carrying `- verdict: <unit> — rc=...` lines); measured live: `grep -c '^- verdict: ' docs/loop-engineering/agent-launch-fanout.md` → `5`.
- decision: the recommended repair for AC16 (stated in the hand-off and in the spec's `## Notes from engineer`, never applied by this engineer) is to replace the bare-prefix total-count assertion with the same closed-vocabulary regex already used for the first assertion, or to scope the count to lines at or after the `## Verdict and licence conditions` heading — not to rename this task's own vocabulary or to touch the frozen evidence.
  reason: the closed-vocabulary regex (`^- verdict: (concurrent-overlapping|serialized|queued-partial|launch-refused|undetermined) — .`) already excludes every part-file/`verdict-region` line by construction, since none of those lines' text is one of the five verdict tokens; reusing it (or heading-scoping the count) repairs the checker's own mechanics without moving what the frozen protocol told the probe to do, which is exactly the class-M mechanics-repair shape `.shell-team/specs/T-1073-harness-agent-concurrency.md`'s own history already used for an analogous checker-only defect.
  grounding: `docs/loop-engineering/harness-agent-concurrency.md` `## Limits and what is not computable` ("Round 1's three check-grammar gaps are resolved this round, per the frozen invalid-evidence rule and a class-M mechanics re-freeze") — the precedent this recommendation follows; `.shell-team/specs/T-1083-agent-launch-fanout.md` Summarized sources, `.shell-team/specs/T-1073-harness-agent-concurrency.md` row ("a class-M mechanics repair to a `- check:` line legitimately moves the intent hash without touching what the probe was told to do").
- decision: the launch-record grammar definition is inserted into `templates/prompt-blocks/fanout-orchestration.md` as two new flat top-level bullets placed immediately after the existing "Verify the aggregation is attributable" bullet and before the "Seven invariants stay locked" bullet, rather than as nested/indented sub-bullets or inserted immediately after the degree-rule bullet (line 5) where "launch record" is first mentioned.
  reason: `contain`-mode mirroring (`templates/prompt-blocks/registry.txt:52`) requires every non-blank canonical line to appear as a byte-identical whole line in `skills/run/SKILL.md` (**AC2**); keeping every new line at the same flat indentation as the file's existing bullets (none of which nest) avoids introducing an indentation mismatch risk between the two mirrored copies, and placing the definition after the two checker-invocation bullets (rather than immediately after the degree-rule bullet) lets the grammar's own text refer to `bin/aggregate-verdicts.sh --population`/`--part` and `bin/check-fanout-instances.sh` by name without forward-referencing bullets the reader has not reached yet.
  grounding: `templates/prompt-blocks/registry.txt:52` (`contain fanout-orchestration.md skills/run/SKILL.md`); `.shell-team/specs/T-1083-agent-launch-fanout.md` **AC2**'s own check body (`grep -qxF` whole-line comparison, which requires identical leading whitespace between the two files).
- decision: the README clause (**AC5**'s adopter-surface) is appended as an additional sentence onto the existing "Within-phase fan-out" bullet in both `README.md` and `README.ja.md`, rather than added as a new standalone bullet.
  reason: the launch record is a sub-mechanism of the same within-phase fan-out feature the existing bullet already documents end to end (population, degree, liveness, telemetry, aggregation, attribution); a new standalone bullet would duplicate that framing sentence and read as a second, competing feature description, whereas one added sentence on the existing bullet keeps the fan-out feature documented in one place, consistent with the file's own existing per-feature-one-bullet structure.
  grounding: `README.md`/`README.ja.md`'s own existing "Within-phase fan-out" bullet (the sole existing bullet documenting `bin/aggregate-verdicts.sh`/`bin/check-fanout-instances.sh`); **AC5**'s own check body (asserts a `.launch`-carrying line exists and also carries `fanout-`, without requiring a new bullet).
- decision: `## Blast radius`'s full-population sweep is run with `CHECK_ACS_TIMEOUT=90` per spec (rather than the spec's own required minimum of 300 for this task's *own* AC19 checker run), against a `git worktree add --detach` scratch checked out to the branch point for the base side.
  reason: the sweep runs 87 specs' full acceptance-criteria suites at two refs (174 total `check-acs.sh` invocations); the spec's own required-minimum-300 applies to AC19's five-checker composition over this task's own records, a fixed, small, one-time cost, not to a 174-invocation population sweep, where a lower per-check bound keeps the total sweep tractable while still comfortably exceeding every candidate spec's own measured single-unit cost from the probe evidence (max observed 156440 ms) short of the small number of specs whose own suites intentionally exercise long-running fixtures; those are named individually in the Blast radius table rather than silently timed out and folded into a clean row.
  grounding: `.shell-team/specs/T-1083-agent-launch-fanout.md` Non-goals / AC19 body (`CHECK_ACS_TIMEOUT` must be raised to at least 300 for AC19's own five-checker composition, not stated as a floor for every invocation this task performs); `docs/loop-engineering/agent-launch-fanout.md` `## Probe evidence`, "Execution conditions" (per-spec unit costs measured at the branch point, max 156440 ms for `T-1044-test-infra-bundle`).
<!-- END provenance: T-1083 -->
