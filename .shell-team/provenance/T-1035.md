<!-- BEGIN provenance: T-1035 -->

- decision: re-derived the multi-invocation tie-break finding by re-scanning the whole 280-line re-derived population for lines carrying more than one real `mktemp` invocation, rather than only inspecting the ten newly-added files' hit counts superficially and assuming the existing four-line finding (T-1005's 270/283/291/298) still described the whole corpus.
  reason: the spec's Notes for engineer is explicit that this finding "must be re-derived, not carried over" and that a newly-added file carrying a multi-invocation line moves both the finding's count and its per-line list. A narrower check (only the new files, only counting substring occurrences without checking invocation position) would have missed that `.shell-team/specs/T-1031-check-handoff-flag-anchor.md:347` and `.shell-team/specs/T-1032-audit-prose-accuracy.md:388` each carry two real invocations, both guarded, so both still resolve to class (a) — but the finding's own prose and its `- measurement:` line would then misstate how many multi-invocation lines exist in this population.
  grounding: `.shell-team/specs/T-1035-spec-template-staleness-locks.md`, Notes for engineer ("The multi-invocation tie-break finding must be re-derived, not carried over... Re-run the same shape at `03e6250` over the *whole* new population; if a newly-added file carries a multi-invocation line, the finding's count and its per-line list both move."); `docs/loop-engineering/check-line-mktemp-guard-audit.md`'s updated Live finding paragraph and `- measurement:` line, listing all six lines (T-1005's four plus T-1031:347 and T-1032:388).

- decision: for `.shell-team/specs/T-1032-audit-prose-accuracy.md` line 388, disclosed both the raw `grep -o 'mktemp'` substring count (3) and the actual invocation-position count (2) in the measurement line's trailing clause, rather than silently reporting one number that matched the pattern of the other five re-derived lines.
  reason: line 388 also names this audit document's own `mktemp`-bearing filename inside an allow-list enumeration (`docs/loop-engineering/check-line-mktemp-guard-audit.md`), so a plain `grep -o 'mktemp' | wc -l` returns 3 there while only 2 of those 3 occurrences are in invocation position (immediately preceded by `(` after `$`, per the document's own **invocation** definition). Silently printing "2" to match the other five lines' pattern would misrepresent what a reader reproducing the measurement command verbatim actually gets; silently printing the raw "3" without the qualifying clause would misstate the site's real invocation count and could be misread as a third invocation needing its own guard check. Disclosing both numbers with the reason for the discrepancy keeps every printed figure independently reproducible while still landing on the correct class-(a) classification.
  grounding: `docs/loop-engineering/check-line-mktemp-guard-audit.md`, Classification section's `- measurement:` line and its trailing explanatory clause; live re-run this session (`git show 03e6250:.shell-team/specs/T-1032-audit-prose-accuracy.md | sed -n '388p' | grep -o mktemp` → 3 occurrences; invocation-position count under the document's own **invocation** definition → 2, both `|| exit 2`-guarded).

- decision: kept `.shell-team/specs/T-1019-is-span-row-parity.md`'s strongest-not-apply grounding line pointing at `git diff --quiet 1e31600` (the authoring ref) and only relabeled the ambiguous phrase "the base ref" to "the authoring ref `1e31600`" for clarity, rather than re-deriving that byte-identity check against `03e6250` or leaving the ambiguous wording as-is.
  reason: DP-E3 draws a firm line between measured fields (population, file total, the four class totals, each per-file `sites:`/`classes:` field, the `reconcile:` identity — re-derived at `03e6250`) and decision fields (`decision:`/`task-state:`/`regime:` and their grounding prose, which keep the frame they were decided in and are not re-derived here). T-1019's strongest-not-apply line is a decision-provenance sentence, not one of the accounting-identity fields AC2/AC6 lock, so re-deriving its check against a different ref would be scope creep beyond what this task's Non-goals and AC5 authorize (no edit to any existing row's decision beyond the header relabeling DP-E2 requires). I independently re-confirmed the underlying fact is still true at the new ref before leaving the line's own command unchanged.
  grounding: `.shell-team/specs/T-1035-spec-template-staleness-locks.md` DP-E3 ("Every `decision:` records the disposition reached when that file first entered this audit... it is not re-decided here") and DP-E2 ("no single unqualified 'base ref' remains to be misread"); `docs/loop-engineering/check-line-mktemp-guard-audit.md`'s strongest-not-apply line; live re-check this session (`git diff --quiet 1e31600 -- .shell-team/specs/T-1019-is-span-row-parity.md`, exit 0).

- decision: wrote each of the ten new per-file rows' `reason:` field as a sentence describing that specific file's own check-line content (which guard idiom is used, what a mention-only occurrence actually references, and — for the two multi-invocation lines — the line number and the tie-break outcome), rather than a single templated sentence reused verbatim across the ten rows.
  reason: T-1024's own DP8 requires a reason that is "a sentence about that file's sites" and mechanically rejects a bare category name; T-1032 was itself created to fix an earlier instance of under-specified per-site provenance in a sibling audit. A reused generic sentence across all ten rows would satisfy AC8's 60-character floor mechanically while repeating the exact defect class T-1032 closed, so each reason instead names the specific guarded-idiom shape, the specific mention target (a filename reference or a required-file enumeration), and, where applicable, the specific line number and its tie-break resolution.
  grounding: `.shell-team/specs/T-1024-check-line-mktemp-guard.md` DP8 ("A `reason:` is a sentence **about that file's sites**; a category name (`frozen`, `closed task`, `out of scope`) is not a reason and AC3 rejects it mechanically."); `docs/loop-engineering/check-line-mktemp-guard-audit.md`'s ten new `- file:` rows for T-1025 through T-1034.

<!-- END provenance: T-1035 -->
