<!-- BEGIN provenance: T-1046 -->

- decision: transcribed the nine-row decision table (D1) as a single `report_ignored_base_verdict()` function whose two `if`/`elif`/`else` chains follow the table's row order literally — DT1-DT4 over `rev-parse`'s (rc, stdout) pair, then DT5-DT8 over `check-ignore`'s exit status — with DT3 and DT8 written as explicit `elif`/`else` arms that `return 0` / call `emit_ignored_base_v2` rather than a bare fall-through, instead of a lookup table, a case-driven dispatcher, or folding DT3/DT8 into a shared default arm with DT2/DT4/DT6.
  reason: D1 states the deliverable is the table and a finding against this mechanism is "a row is missing, cites a wrong guarantee, or the implementation disagrees with a row" — the closest way to make that judgment cheap for a reviewer is to keep one line of code (or one clearly-commented arm) per row, in the table's own id order, so the diff between the frozen table and the function reads almost line-for-line. Folding DT3/DT8 into a shared default would have satisfied the same outputs but hidden exactly the property D1 calls out as load-bearing: that an out-of-contract value is caught by name, not absorbed silently into another row's arm.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md`, "### The frozen decision table" (DT0-DT8) and decision D1; `bin/team-init.sh`'s `report_ignored_base_verdict()` function body, each arm commented with its row id; AC19's `check:` line (every `DT0`-`DT8` token present in `tests/team-init/run.sh`) `PASS`.

- decision: dropped the `git ls-files` / tracked-file channel entirely — the implementation reads exactly two channels (`rev-parse --is-inside-work-tree`, `check-ignore -q`) and no code path in `bin/team-init.sh` calls `ls-files` in any form.
  reason: D4 gives three independent grounds (the documented remedy already resolves to `check-ignore` rc=1 through `DT6`; `V1`'s reworded claim — "git reports the resolved base dir as ignored" — stays true even under a force-added tracked file, so the second channel's original false-positive guard is moot; and the verdict-acquisition layer's same-class-2 budget is one round, so an extra channel is an extra admissibility surface this task cannot afford). All three hold as measured, not merely asserted: `LX08`'s re-include twin fixture confirms the `DT6` route alone silences the documented-remedy case with no `ls-files` call anywhere.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D4 and Non-goals ("No use of `git ls-files` ..."); AC4's `check:` line (zero occurrences of `ls-files` in `bin/team-init.sh`) `PASS`; `tests/team-init/run.sh`'s `LX08` fixture (hostile excludes honoured, re-include silences it) `PASS`.

- decision: implemented the notice in exactly one place — `bin/team-init.sh`'s `report_ignored_base_verdict()`, called once, after every `copy_template`/`ensure_gitkeep` call and before the closing summary block — and left `bin/team-paths.sh` completely unedited (not even a comment).
  reason: D6 names `team-init` as the sole emitter (fires once per adoption, has a natural after-scaffolding site, and deletes the one-body/two-implementations symmetry problem T-1042 could not close) and states `bin/team-paths.sh` "gains nothing and is byte-identical to the base ref" as the strongest available form of the `--export`/`--get` immunity constraint. Placing the call after the last `copy_template` line (rather than, say, interleaving it between scaffolding steps) is what AC9 asserts: the notice is additive to a completed scaffold, never a substitute for any part of it.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D6; AC7's `check:` line (`cmp` of `bin/team-paths.sh` against its `d825293` blob) `PASS`; AC9's `check:` line (notice plus every scaffold file plus the closing `team-init: ` summary line, all in one run) `PASS`; AC13's allow-list (which excludes `bin/team-paths.sh` and every other `bin/` script by name) `PASS`.

- decision: left `bin/team-paths.sh`'s `validate_base()` completely untouched — no new rejection for a leading `:`, a glob metacharacter, `!`, or `-` — and instead relied entirely on the frozen `./`-prefixed, trailing-slash-normalized `check-ignore` query (`"./${TEAM_RUN_BASE%/}/"`) to answer correctly for every one of those shapes.
  reason: D7 declines the tightening on three grounds (the gap is already closed for this mechanism by the query normalization; tightening the shared choke point would be a breaking change to a published contract bundled into a notice task; a pathspec hazard in some other consumer is that consumer's own finding). This holds only if the normalized query really does answer correctly for the hostile shapes `validate_base` still permits — verified directly rather than assumed: `LX01` (a `a*` glob-shaped base beside a tracked glob-colliding path) and `LX06` (`:foo`, a colon-leading base) both still resolve to `V1` firing.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D7 and Non-goals ("No change to `validate_base`'s accepted value set"); AC6's `check:` line (a `:foo` base is both accepted by `team-paths.sh --get base` AND reported ignored by `team-init.sh`) `PASS`; AC7's `cmp`-against-base-ref check on `bin/team-paths.sh` `PASS`; `tests/team-init/run.sh`'s `LX01`/`LX06` fixtures `PASS`.

- decision: scoped every git call through a single `git_probe()` helper using `git -C "$TARGET" "$@"` (never `cd`-ing into `$TARGET`), rather than changing the script's working directory or threading `-C "$TARGET"` into each of the two call sites individually.
  reason: `bin/team-init.sh` never `cd`s anywhere else in the file (it resolves `$TARGET` to an absolute path once and passes it explicitly to every helper), so a one-off `cd` for this mechanism alone would be a new, inconsistent pattern; centralizing the `-C` flag and the `env -u ... LC_ALL=C` neutralization prefix in one function also means the frozen invocation constraints (D8: normalized query, separated streams, neutralized environment) are satisfied for every future caller of `git_probe()` by construction, not by convention repeated at each call site.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D8 ("query normalization and invocation hygiene are frozen ... what is NOT frozen is how the emitter is factored into functions or variables"); `bin/team-init.sh`'s `git_probe()` definition and its two call sites (`rev-parse --is-inside-work-tree`, `check-ignore -q`); AC4's positive controls (`env -u GIT_DIR`, `LC_ALL=C` each present) `PASS`.

- decision: added the `team-init-log-warn-guard` protected-content-lock row to `tests/errexit-safe/run.sh` part (iv) (an exact-substring `protect` call), rather than adding a behavioral (`2>&-` exit-code) row in part (ii), or leaving the guard covered only by AC8's own `check:` line in this task's spec.
  reason: D10(i) and the spec's Notes for engineer state the guarded form ends `>&2 || true; }`, which matches none of part (iii)'s P1/P2/P3 static-sweep patterns (P2's bare-line-ending pattern requires the line to END at `>&2`, and this line has `; }` after it) — exactly the shape `tests/errexit-safe/run.sh`'s own header already documents as needing a protected-content lock instead. A behavioral row would also be vacuous by construction here for the wrong reason than usual (not because the contract exit code is 1, but because `log_warn()` never itself does an `exit N` — it is a void-returning helper called from many sites with different downstream contracts), so the static lock is the only sound mechanism.
  grounding: `tests/errexit-safe/run.sh`'s own header comment, part (iv) ("Three known sites have a `|| true`-guarded stderr write where MORE CODE follows on the SAME line ..."); the existing `team-init-log-err-guard` row (added by T-096) as the precedent for the identical `; }` shape; AC8's `check:` line (`grep -qF -- 'team-init-log-warn-guard' tests/errexit-safe/run.sh`) `PASS`; `bash tests/errexit-safe/run.sh` exit 0 with the new row printing `ok:`.

- decision: ran a producer-side mutation self-check on each of the eleven frozen fixture labels (`LX01`-`LX11`) against private scratch copies of `bin/team-init.sh` before hand-off, rather than treating "the fixture currently passes" as sufficient evidence that it discriminates.
  reason: "a lock nobody has seen fail is not yet a lock" — a fixture whose assertion happens to be satisfied by the correct implementation could equally be satisfied by a vacuous assertion that never really exercises the guarded element. Each check below deliberately broke the ONE element the fixture's label names, ran that fixture's own scenario against the mutated scratch copy (never the real, committed `bin/team-init.sh`), confirmed the expected assertion now fails (red), and confirmed the real, unmutated `bin/team-init.sh` still produces the correct (green) outcome for the same scenario — the latter independently reconfirmed by `bash tests/team-init/run.sh`'s full, unmutated run printing `PASS:` for all eleven labels.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` AC11 and `## Notes for engineer` ("Mutation self-check every one of the eleven labels before the hand-off ... break the specific element the fixture guards ... confirm red, restore, confirm green"); `bash tests/team-init/run.sh` (real, unmutated) exit 0, 31 `PASS:` lines including all eleven `case: ...` labels.
- decision: **LX01** mutation self-check — broke the query's `./`-prefix + trailing-slash normalization (changed the `check-ignore` pathspec argument from `"./${TEAM_RUN_BASE%/}/"` to the bare `"$TEAM_RUN_BASE"`) in a scratch copy.
  reason: this is the one element LX01's label names (M1: a glob-shaped base beside a tracked glob-colliding path).
  grounding: red — against the scratch copy, `TEAM_RUN_BASE='a*'` with a tracked `abc/file.txt` and a `.gitignore` rule for `a*` produced ZERO `WARN:` lines (the historical M1 false negative reproduced exactly). green — the real `bin/team-init.sh` against the identical fixture emits `V1`'s anchor line naming `a*` exactly once (confirmed live, and by `tests/team-init/run.sh`'s `LX01` `PASS`).
- decision: **LX02** mutation self-check — removed the `return 0` from `DT4`'s `else` arm (rev-parse rc≠0) in a scratch copy, so a nonzero `rev-parse` no longer short-circuits before the `check-ignore` probe.
  reason: this is the branch LX02's stubbed-git-fatal sub-case exercises (the "some other fatal condition" half of DT4).
  grounding: red — a `git` shim forcing `rev-parse` to a non-English `fatal:` 128 exit, run against an otherwise-ignored fixture, now incorrectly emitted `V1` (the DT4 fall-through). green — the real `bin/team-init.sh` against the identical stub and fixture stays silent (confirmed live, and by `tests/team-init/run.sh`'s `LX02` `PASS`).
- decision: **LX03** mutation self-check — this label is doubly protected (either the query normalization alone, or the after-scaffolding call ordering alone, independently defeats the historical M3 pre-existence gap, each confirmed measured), so a single-element break does not flip it red; breaking BOTH together (bare query AND `report_ignored_base_verdict` moved to before the `copy_template` calls) in a scratch copy does.
  reason: LX03's label names the M3 shape (a directory-form rule written before the base dir exists); this task's `D9`-ordering closes that gap for `team-init` independently of the query fix, per `.shell-team/reviews/T-1042.md`'s own note that the init call site is "immune by construction".
  grounding: red — with both elements broken together, a fresh repo whose `.gitignore` holds `.shell-team/` (bare directory-form rule, written before any scaffolding) produced ZERO `WARN:` lines. green — the real `bin/team-init.sh` (both protections intact) emits `V1` for the identical fixture (confirmed live, and by `tests/team-init/run.sh`'s `LX03` `PASS`); the fixture's own standalone `git check-ignore -q -- '.shell-team'` positive control independently reproduced the bare-form hazard (rc=1) before team-init ever ran, proving the hazard is real and not merely hypothesized.
- decision: **LX04** mutation self-check — hardcoded the `check-ignore` query to the literal `"./.shell-team/"` in a scratch copy, breaking the resolved-`$TEAM_RUN_BASE` substitution the legacy split-root layout depends on.
  reason: this is the resolver-integration element LX04's label protects (a legacy layout resolves `base=tasks`, not `.shell-team`).
  grounding: red — against a legacy-marker fixture (`tasks/loops/shell-team.contract.yaml` present, `.gitignore` holding `tasks/`), the mutated copy produced no `V1` notice naming `tasks` (querying the wrong, non-ignored path instead). green — the real `bin/team-init.sh` emits `V1` naming `tasks`, with no occurrence of `specs` anywhere in the notice (confirmed live, and by `tests/team-init/run.sh`'s `LX04` `PASS`).
- decision: **LX05** mutation self-check — reintroduced the literal diagnostic-text substring `not a git repository` into a comment in a scratch copy of `bin/team-init.sh`.
  reason: LX05's label (NM1) is protected by a static invariant — no git diagnostic text is ever read or compared — and the fixture's static half is exactly the `grep -qF 'not a git repository' "$INIT"` assertion this reintroduction is designed to trip.
  grounding: red — `grep -qF 'not a git repository'` against the mutated scratch file matches, which is exactly the condition `tests/team-init/run.sh`'s LX05 assertion treats as a failure. green — the same grep against the real, committed `bin/team-init.sh` matches zero times (confirmed live via AC4's own zero-occurrence check, and by `tests/team-init/run.sh`'s `LX05` `PASS`).
- decision: **LX06** mutation self-check — removed only the `./` prefix from the normalized query in a scratch copy, keeping the trailing slash (`"${TEAM_RUN_BASE%/}/"` instead of `"./${TEAM_RUN_BASE%/}/"`).
  reason: this isolates the ONE half of the normalization that defeats a colon-leading base per the measured channel-admissibility record (trailing-slash alone does not fix `:foo`; only the `./`-prefix does) — exactly what LX06's label (NM2) names.
  grounding: red — `TEAM_RUN_BASE=':foo'` with `.gitignore` holding `:foo/` produced ZERO `WARN:` lines against the mutated scratch copy (the colon-leading false negative reproduced). green — the real `bin/team-init.sh` emits `V1` naming `:foo` for the identical fixture (confirmed live, and by `tests/team-init/run.sh`'s `LX06`/AC6 `PASS`).
- decision: **LX07** mutation self-check — this label is doubly protected too (either stream separation alone, or the `env -u GIT_TRACE ...` neutralization alone, independently defeats ambient `GIT_TRACE` pollution, each confirmed measured), so breaking only the stream separation (changed the `rev-parse` capture's `2>/dev/null` to `2>&1`) left it green; breaking BOTH together (merged streams AND removing `-u GIT_TRACE` from `git_probe()`'s neutralization chain) in a scratch copy flips it red.
  reason: LX07's label (NM3) names ambient `GIT_TRACE`/`GIT_*` immunity; this task's D8 invocation constraints provide two independent defenses against exactly this class, and the honest mutation result is that both must fail together for the fixture to fail.
  grounding: red — with both elements broken, `GIT_TRACE=1 GIT_DIR=/nonexistent-gitdir GIT_LITERAL_PATHSPECS=1 GIT_GLOB_PATHSPECS=1` against an otherwise-ignored fixture produced ZERO `WARN:` lines (the historical NM3 false-negative reproduced). green — the real `bin/team-init.sh` under the identical hostile environment still emits `V1` (confirmed live, and by `tests/team-init/run.sh`'s `LX07`/AC5 `PASS`).
- decision: **LX08** mutation self-check — swapped the `DT5`/`DT6` branch conditions in a scratch copy (`check-ignore` rc=0 now silences, rc=1 now emits `V1`).
  reason: this is the classification branch LX08's twin-fixture pair (a hostile global excludes file honoured; a repo-level re-include silencing it) depends on to tell the two states apart.
  grounding: red — against the mutated copy, the hostile-excludes fixture (should warn) went silent AND the re-include twin (should be silent) incorrectly emitted `V1` — both sub-assertions flipped wrong simultaneously. green — the real `bin/team-init.sh` gets both directions right (confirmed live, and by `tests/team-init/run.sh`'s `LX08` `PASS`).
- decision: **LX09** mutation self-check — swapped the `"true"`/`"false"` string comparisons in `DT1`'s and `DT2`'s conditions in a scratch copy.
  reason: this is the exact contract LX09's label names (`rev-parse`'s documented `true`/`false` print contract, per `DT1`/`DT2`).
  grounding: red — against the mutated copy, a normal (non-bare) ignored work tree went silent (DT1 no longer matched "true"), AND a bare repository incorrectly proceeded to `check-ignore` (DT2 no longer matched "false"), which then hit `check-ignore`'s own fatal-outside-a-work-tree 128 and wrongly emitted the undeterminable notice. green — the real `bin/team-init.sh` gets both cases right (confirmed live, and by `tests/team-init/run.sh`'s `LX09` `PASS`).
- decision: **LX10** mutation self-check — removed the `emit_ignored_base_v2` call from `DT7`'s branch in a scratch copy (replaced with a no-op), leaving `DT8`'s arm as the only place `V2` could still fire.
  reason: this is the exact emission LX10's label (the reachable `DT7` row) asserts.
  grounding: red — against a `check-ignore`-stubbing shim (forcing exit 128) and the mutated copy, ZERO `WARN:` lines were produced (no `V2`, contrary to the frozen table). green — the real `bin/team-init.sh` against the identical stub emits `V2` and exits 0 (confirmed live, and by `tests/team-init/run.sh`'s `LX10` `PASS`).
- decision: **LX11** mutation self-check — removed the `|| true` guard from `log_warn()`'s definition in a scratch copy (reverting it to `bin/team-init.sh`'s pre-T-1046 form), then ran the IGNORED-class and UNDETERMINABLE-class (via the `DT7`-stubbing shim) fixtures with stderr closed (`2>&-`).
  reason: this is the exact guard LX11's label (D2: exit 0 in every outcome class, including with stderr closed) depends on for the two outcome classes that write to stderr at all.
  grounding: red — both the IGNORED-class and UNDETERMINABLE-class runs exited 1 (not 0) against the mutated copy, though scaffolding had already completed by the time the write failed. green — the real `bin/team-init.sh` exits 0 for all three outcome classes (IGNORED, SILENCE, UNDETERMINABLE) under `2>&-` (confirmed live, and by `tests/team-init/run.sh`'s `LX11` `PASS`); the SILENCE class is unaffected by this guard either way, since it never writes to stderr.

<!-- END provenance: T-1046 -->
