<!-- BEGIN provenance: T-1046 -->

- decision: rewrote `report_ignored_base_verdict()` as a five-row transcription (D1, amended) — `DT0` (a static `command -v git` guard) followed by a single `git_probe check-ignore -q -- "./${TEAM_RUN_BASE%/}/"` call whose exit status is compared against exactly one condition, `[ "$ci_rc" -eq 0 ]`, rather than an enumerated `elif` chain covering `DT6`/`DT7`/`DT8` separately.
  reason: D1 states totality is now structural rather than enumerated — "only `rc=0` ever emits ... an unexpected exit status therefore cannot produce a false verdict even if no row named it" — and explicitly permits collapsing `DT6`/`DT7`/`DT8` into one non-emitting arm since they share the identical SILENCE outcome. Writing a single positive condition (rather than three negative arms) makes that structural guarantee visible in the code itself: there is no `elif`/`else` chain whose fall-through behavior could silently diverge from the table.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D1 ("Totality is structural, not enumerated ... that collapse cannot create a fall-through, and it is permitted explicitly here") and "### The frozen decision table"; `bin/team-init.sh`'s `report_ignored_base_verdict()` function body; AC19's `check:` line (exactly five `DT` table rows in the spec, `DT0`/`DT5`/`DT6`/`DT7`/`DT8` present in the test suite, `DT1`-`DT4` absent from `bin/team-init.sh`) PASS.

- decision: deleted the `rev-parse --is-inside-work-tree` channel, the `DT1`-`DT4` rows, the `emit_ignored_base_v2` function and the whole `env -u`-based neutralization chain, rather than keeping either as dead code behind an unreachable branch.
  reason: D3 (amended) grounds the deletion in the retired-rows list — every state `DT1`-`DT4` classified now reaches `DT5`/`DT6`/`DT7` through the single channel with an identical outcome, so the second channel bought exactly one distinction (a genuine work tree where `check-ignore` still fails) at the cost of a second admissibility surface. Cross-provider round 1's finding 3 landed on the invocation constraints shared by both channels, which is the concrete cost D3 says this reduction avoids paying twice. `## Notes for engineer`'s "this is a deletion task first" instruction and the AC4 retirement locks (`rev-parse`, `is-inside-work-tree`, `env -u`, `GIT_` each required to occur zero times) make this the load-bearing shape of the diff, not an optional cleanup.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D3, the retired-rows list under "### The frozen decision table", and `## Notes for engineer` ("If the diff is not overwhelmingly red, something that should have left is still there"); AC4's `check:` line (zero occurrences of `rev-parse`, `is-inside-work-tree`, `env -u`, `GIT_`, `ls-files`, `--porcelain`, `2>&1`, every explicit pathspec-magic form) PASS; AC19's `check:` line (`DT1`-`DT4` occur zero times in `bin/team-init.sh`) PASS.

- decision: kept the `git ls-files` / tracked-file channel dropped (D4, carried) and rewrote `V1`'s body to state the tracked-file exception in the shipped text itself, rather than relying on a claim that the message "stays true because a reading of it does" (v1's superseded ground (ii)).
  reason: cross-provider round 1's finding 1 proved v1's `V1` false for an already-tracked, force-added file — the message predicted a commit-time consequence beyond what `check-ignore`'s exit status and `gitignore(5)`'s own documented scope (untracked paths only) actually support. D4's replacement ground is that the assertion rule closes finding 1 "by construction rather than by a narrowing: the sentence a reviewer would have to falsify is no longer in the message" — so the fix belongs in the frozen `V1` text (a documentation/assertion-rule change), not in a reintroduced `ls-files` channel that D4's grounds (i) and (iii) still hold against.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D4 and "### The assertion rule"; frozen `V1` body ("A file already tracked under it is unaffected."); AC2's `check:` line (byte-exact `V1` extraction, substituted, matched line-for-line against `bin/team-init.sh`'s stderr output) PASS; AC4's `ls-files` zero-occurrence check PASS.

- decision: kept the notice as a single emitter in `bin/team-init.sh`, called once after every `copy_template`/`ensure_gitkeep` call and before the closing summary block (D6, carried unchanged), and left `bin/team-paths.sh` completely unedited.
  reason: this is unchanged from v1 and none of the cross-provider round-1 findings touched the emitter-placement decision; D6's grounds (single emission site, natural after-scaffolding ordering, `bin/team-paths.sh` byte-identity as the strongest available `--export`/`--get` immunity) still hold verbatim for v2's rewritten mechanism, so re-deriving this placement independently would have risked a second, possibly conflicting judgment call on evidence pm-spec had already settled.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D6; AC7's `check:` line (`cmp` of `bin/team-paths.sh` against its `d825293` blob) PASS; AC9's `check:` line (notice plus every scaffold file plus the closing `team-init: ` summary line, all in one run) PASS; AC13's allow-list (excludes `bin/team-paths.sh` and every other `bin/` script by name) PASS.

- decision: left `bin/team-paths.sh`'s `validate_base()` untouched — no new rejection for a leading `:`, a glob metacharacter, `!`, or `-` — relying entirely on the frozen `./`-prefixed, trailing-slash-normalized `check-ignore` query to answer correctly for every one of those shapes (D7, carried unchanged).
  reason: D7's three grounds are unchanged by the v2 rewrite (the gap is already closed by query normalization; tightening the shared choke point would be a breaking change to a published contract bundled into a notice task; a pathspec hazard in some other consumer is that consumer's own finding), and this holds only if the normalized query still answers correctly for the hostile shapes `validate_base` permits — reverified directly for v2 rather than assumed: `LX01` (a glob-shaped base beside a tracked glob-colliding path) and `LX06` (a colon-leading base) both still resolve to `V1` firing under the single-channel rewrite.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D7; AC6's `check:` line (a `:foo` base is both accepted by `team-paths.sh --get base` AND reported ignored by `team-init.sh`) PASS; AC7's `cmp`-against-base-ref check PASS; `tests/team-init/run.sh`'s `LX01`/`LX06` fixtures PASS.

- decision: replaced the `env -u`-enumerated blacklist with an `env -i` allow-list carrying exactly four names (`PATH`, `HOME`, `TMPDIR` forwarded; `LC_ALL` pinned to `C`), written on the single `git_probe()` function-body line, rather than extending the v1 blacklist with the newly-named `GIT_CONFIG_COUNT`/`KEY_n`/`VALUE_n` family plus the six other candidates the cross-provider record named (D8, amended — the load-bearing change of v2).
  reason: this is the coordinating session's ruled disposition, not an engineer's latitude — the pre-commitment fired on cross-provider round 1's finding 3 (the acquisition layer's one-round budget), and the ruling reasoned that "an omission class is not closed by extending the enumeration": two verified injection instances plus six unverified candidates in one round, plus a thirteenth channel (`GIT_CONFIG_PARAMETERS`) found by the very next measurement pass, is a diverging enumeration rather than a converging one. An allow-list closes the class by construction — no `GIT_*` variable, named or unnamed, survives `env -i` — which is exactly what the six unverified candidates' Non-goals disposition ("closed by construction, not investigated") depends on being true.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` decision D8, "### The neutralization rule", and the re-freeze ledger's trigger entry; AC20's `check:` line (the `env -i` line's `NAME=` set, sorted, matches the frozen `NEUTSET` exactly; `env -u` occurs zero times; `GIT_` occurs zero times on that line) PASS; AC5's `check:` line (both measured injection channels, `GIT_CONFIG_COUNT`/`KEY_0`/`VALUE_0` and `GIT_CONFIG_PARAMETERS`, verified live to flip a bare `check-ignore` in this environment, then shown not to flip `team-init.sh`'s verdict) PASS.

- decision: added `HOME="${HOME:-}"` to the allow-list forwarding, rather than dropping it in favor of the narrowest possible surface (only `PATH`, `TMPDIR`, `LC_ALL`).
  reason: the neutralization rule states `HOME` is forwarded "on purpose" because a legitimate global `core.excludesFile` reached through `~/.gitconfig` is a real ignore source an adopter is entitled to be told about — dropping it silently loses that source rather than merely tightening a surface. This was reverified for v2's rewritten `git_probe()`, not assumed from the v1-era measurement record alone: `LX14`'s own fixture pair shows a legitimate global excludes file reached through a scratch `HOME` is honoured, and its negative control (an empty scratch `HOME`) confirms the positive result comes from that source and nothing ambient.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` "### The neutralization rule" consequence (i) and decision D9's extension ("`LX14` redirects `HOME` to a scratch home ... its negative control is the same fixture under an empty scratch home"); AC21's `check:` line (both directions: a legitimate global excludes source fires `V1`, an empty scratch `HOME` stays silent) PASS.

- decision: performed a producer-side mutation self-check on each of the thirteen frozen fixture labels (`LX01`-`LX09`, `LX11`-`LX14`) against the real, committed `bin/team-init.sh` — mutating it in place with the Edit tool, confirming the label's own scenario now fails (red), restoring the exact original bytes with a second Edit call, and confirming the restore was byte-identical to a pre-mutation backup before moving to the next label — rather than relying on "the fixture currently passes" as sufficient evidence that it discriminates.
  reason: "a lock nobody has seen fail is not yet a lock." Because all thirteen fixtures already passed against the correct, unmutated implementation (confirmed by a full `bash tests/team-init/run.sh` run before any mutation began), that alone cannot distinguish a fixture that truly exercises the guarded element from one whose assertion happens to be vacuously satisfiable. Mutating the real file in place (rather than a copy under a different path) was necessary because `bin/team-init.sh`'s own bootstrap resolves `TEMPLATES_DIR` relative to its own location (`$SCRIPT_DIR/..`), so a copy placed elsewhere would fail before ever reaching the probed mechanism; each mutation was therefore followed immediately by an exact-byte restore, verified with `cmp` against a backup taken before any mutation, before the next mutation began.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` AC11 and `## Notes for engineer` ("Mutation self-check every one of the thirteen labels before the hand-off ... break the specific element the fixture guards ... confirm red, restore, confirm green"); a full `bash tests/team-init/run.sh` run immediately before mutation testing began (all thirteen `case: ...` labels PASS) and again immediately after all thirteen mutations completed (identical PASS set); `cmp` against the pre-mutation backup confirmed byte-identical after every single restore, not only at the end.

- decision: **LX01** mutation self-check — changed the `check-ignore` pathspec argument from `"./${TEAM_RUN_BASE%/}/"` to the bare `"$TEAM_RUN_BASE"` (dropping both the `./` prefix and the trailing slash together).
  reason: this is the query-normalization element LX01's label (M1: a glob-shaped base beside a tracked glob-colliding path) protects.
  grounding: mutation red — `TEAM_RUN_BASE='a*'` with a tracked `abc/file.txt` and a `.gitignore` rule `a\*/` produced zero `WARN:` lines (the historical M1 false negative reproduced) against the live-edited `bin/team-init.sh`. restore green — the real, restored `bin/team-init.sh` against the identical fixture (a fresh re-run of the same scaffolded target) fired `V1` naming `a*` (confirmed live; `cmp` against the pre-mutation backup byte-identical); `tests/team-init/run.sh`'s `LX01` PASS.

- decision: **LX02** mutation self-check — changed the emission gate from `[ "$ci_rc" -eq 0 ]` to `[ "$ci_rc" -ne 1 ]`, so any non-"not-ignored" status (including `DT7`'s fatal 128) fires `V1`.
  reason: this is the exact discrimination LX02's label (DT7 M2: a bare repository and a stubbed git fatal both stay silent) depends on — both sub-cases reach `check-ignore` rc=128, and only a gate that treats rc=128 as SILENCE (not as "not rc=1, so emit") keeps them silent.
  grounding: mutation red — a bare repository (`git init --bare`) produced 8 `WARN:` lines (`V1` fired) against the mutated copy, and a `check-ignore`-stubbing shim forcing a 128 exit likewise produced 8 `WARN:` lines against an otherwise-ignored fixture — both should have been silent. restore green — the real, restored `bin/team-init.sh` stays silent for both (0 occurrences of `V1`'s anchor line; the only `WARN:` lines present are unrelated "skipped existing file" notices from re-running against an already-scaffolded target); `tests/team-init/run.sh`'s `LX02` PASS.

- decision: **LX03** mutation self-check — this label is doubly protected in v2, exactly as it was in v1: breaking only the query normalization (the LX01 mutation above) left it green, because `report_ignored_base_verdict()` still runs after `copy_template` has already created the base dir on disk. Breaking BOTH the query normalization AND the after-scaffolding call ordering (moving the `report_ignored_base_verdict` call to before the first `copy_template` call) together flips it red.
  reason: LX03's label names the M3 shape (a directory-form `.gitignore` rule written before the base dir exists on disk). The base-dir-not-yet-created hazard this label protects against is closed independently by D6's after-scaffolding ordering (the directory always exists by the time the probe runs) — so a single-element break of the query alone is insufficient, matching this label's v1-era characterization as doubly protected.
  grounding: single-mutation test (query only) — 8 `WARN:` lines still produced (green, not red) — disclosed below as an intervention rather than silently narrowed to "single-element break". double-mutation red — with both the query normalization AND the call ordering broken, a fresh repo whose `.gitignore` holds `.shell-team/` (written before any scaffolding ran) produced zero `WARN:` lines. restore green — with both elements restored, the real `bin/team-init.sh` fires `V1` for the identical fixture (confirmed live via two separate Edit-tool reverts, `cmp` byte-identical after each); `tests/team-init/run.sh`'s `LX03` PASS.

- decision: **LX04** mutation self-check — hardcoded the `check-ignore` pathspec to the literal `"./.shell-team/"` in place of the resolved `"./${TEAM_RUN_BASE%/}/"`.
  reason: this is the resolver-integration element LX04's label protects (a legacy split-root layout resolves `base=tasks`, not `.shell-team`).
  grounding: mutation red — against a legacy-marker fixture (`tasks/loops/shell-team.contract.yaml` present, `.gitignore` holding `tasks/`), the mutated copy produced no `V1` notice naming `tasks` (only an unrelated skip warning). restore green — the real, restored `bin/team-init.sh` emits `V1` naming `tasks` (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX04` PASS.

- decision: **LX05** mutation self-check — reintroduced the literal diagnostic-text substring `not a git repository` into a comment in `bin/team-init.sh`.
  reason: LX05's label (NM1) is protected by a static invariant — no git diagnostic text is ever read or compared — and the fixture's static half is exactly the `grep -qF 'not a git repository' "$INIT"` assertion this reintroduction is designed to trip.
  grounding: mutation red — `grep -qF 'not a git repository'` against the mutated file matched, which is exactly the condition `tests/team-init/run.sh`'s LX05 assertion treats as a failure. restore green — the same grep against the real, restored `bin/team-init.sh` matches zero times (confirmed live via AC4's own zero-occurrence check, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX05` PASS.

- decision: **LX06** mutation self-check — removed only the `./` prefix from the normalized query, keeping the trailing slash (`"${TEAM_RUN_BASE%/}/"` instead of `"./${TEAM_RUN_BASE%/}/"`).
  reason: this isolates the one half of the normalization that defeats a colon-leading base per the measured channel-admissibility record (trailing-slash alone does not fix `:foo`; only the `./`-prefix does) — exactly what LX06's label (NM2) names, and deliberately the complementary half of LX01's mutation (which broke both at once).
  grounding: mutation red — `TEAM_RUN_BASE=':foo'` with `.gitignore` holding `:foo/` produced zero `WARN:` lines against the mutated copy (the colon-leading false negative reproduced). restore green — the real, restored `bin/team-init.sh` emits `V1` naming `:foo` for the identical fixture (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX06`/AC6 PASS.

- decision: **LX07** mutation self-check — reverted `git_probe()` from the `env -i` allow-list wrapper to a bare `git -C "$TARGET" "$@"` call, then ran the fixture under ambient `GIT_TRACE=1 GIT_DIR=/nonexistent-gitdir GIT_LITERAL_PATHSPECS=1 GIT_GLOB_PATHSPECS=1`.
  reason: LX07's label (NM3) names ambient `GIT_TRACE`/`GIT_*` immunity; in v2 this immunity comes entirely from the allow-list (D8), so removing the wrapper is the single element that reintroduces exposure to ambient variables — unlike v1, where this same scenario needed two elements broken together (stream separation and the `env -u` chain) because v1's protection was itself two-layered.
  grounding: mutation red — with the bare `git_probe()` and the hostile `GIT_DIR=/nonexistent-gitdir` override in the ambient environment, an otherwise-ignored fixture produced zero `WARN:` lines (the injected `GIT_DIR` redirected git away from the target repository entirely). restore green — the real, restored `bin/team-init.sh` under the identical hostile environment still emits `V1` (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX07` PASS.

- decision: **LX08**/**LX09** shared mutation self-check — changed the emission gate from `[ "$ci_rc" -eq 0 ]` to `[ "$ci_rc" -eq 1 ]` (inverting DT5 and DT6: "not ignored" now fires, "ignored" now stays silent).
  reason: this is the exact `DT5`/`DT6` discrimination both labels depend on — LX08's twin fixtures (a hostile global excludes file honoured; a repo-level re-include silencing it) and LX09's ignored/not-ignored pair both test whether rc=0 versus rc=1 is mapped to the correct outcome.
  grounding: mutation red, all four sub-cases — the hostile-excludes fixture (should warn) went silent (0 `WARN:` lines) AND its re-include twin (should be silent) incorrectly emitted `V1` (8 `WARN:` lines); independently, LX09's ignored fixture went silent (0) and its not-ignored fixture incorrectly fired (8) — all four flipped exactly as the inverted gate predicts. restore green — the real, restored `bin/team-init.sh` gets all four directions right (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX08` and `LX09` PASS. (LX09's third sub-case, DT7's underlying 128 contract, is measured directly against the real `git` binary rather than through `team-init.sh`, so it is unaffected by any mutation of this script — this is why the spec frames it as "checked directly against git itself".)

- decision: **LX11** mutation self-check — removed the `|| true` guard from `log_warn()`'s definition, reverting it to `printf 'WARN: %s\n' "$*" >&2;` with no guard.
  reason: this is the exact guard LX11's label (D2: exit 0 in both outcome classes, including with stderr closed) depends on for the one outcome class (IGNORED) that writes to stderr.
  grounding: mutation red — the IGNORED-class fixture run with `2>&-` (closed stderr) exited 1 against the mutated copy (the unguarded `printf` failed under `set -euo pipefail` when its output destination was closed, aborting the script via `errexit`). restore green — the real, restored `bin/team-init.sh` exits 0 for the identical fixture under `2>&-` (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX11` PASS. (v2 has only two outcome classes rather than v1's three, so this label's frozen text was correspondingly narrowed from "every outcome class" to "both outcome classes" per the re-freeze ledger — the SILENCE class is unaffected by this guard either way, since it never writes to stderr.)

- decision: **LX12**/**LX13** shared mutation self-check — reverted `git_probe()` to a bare `git -C "$TARGET" "$@"` call (the same mutation as LX07's, applied here under a hostile injected environment instead of a merely noisy one).
  reason: this is the allow-list wrapper itself, the single element both labels' assertions exist to lock: LX12 targets `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_0`/`GIT_CONFIG_VALUE_0`, LX13 targets `GIT_CONFIG_PARAMETERS`, and both channels are neutralized by the identical `env -i` prefix rather than by anything channel-specific.
  grounding: mutation red — with the bare `git_probe()`, an unrelated (`build/`) `.gitignore` fixture under `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.excludesFile GIT_CONFIG_VALUE_0=<hostile-file>` produced 8 `WARN:` lines (the injected excludes file flipped the verdict) — and the identical fixture under `GIT_CONFIG_PARAMETERS="'core.excludesFile=<hostile-file>'"` likewise produced 8 `WARN:` lines. Both injections were separately confirmed to flip a bare `git check-ignore` in this environment before the `team-init.sh` mutation test ran (this is AC5's own anti-vacuity control, re-derived here). restore green — the real, restored `bin/team-init.sh` under the identical `GIT_CONFIG_COUNT` injection stays silent (0 `WARN:` lines) (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX12` and `LX13` PASS.

- decision: **LX14** mutation self-check — dropped `HOME="${HOME:-}"` from `git_probe()`'s allow-list, leaving only `PATH`, `TMPDIR`, `LC_ALL`.
  reason: this is the exact forwarding LX14's label (D9: a legitimate global `core.excludesFile` reached through `HOME` is still honoured) depends on.
  grounding: mutation red — a fresh repository with no repo-local `core.excludesFile` and a scratch `HOME` whose `.gitconfig` names a global excludes file matching the base dir produced zero `WARN:` lines against the mutated copy (the real, adopter-configured ignore source was silently lost). restore green — the real, restored `bin/team-init.sh` against the identical scratch-`HOME` fixture fires `V1` (confirmed live, `cmp` byte-identical); `tests/team-init/run.sh`'s `LX14`/AC21 PASS.

<!-- END provenance: T-1046 -->
