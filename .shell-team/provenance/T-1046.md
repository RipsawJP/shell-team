<!-- BEGIN provenance: T-1046 -->

- decision: rewrote this file end to end rather than patching the standing v2/v3-era record in place, and organized it as one entry per v5-surviving-or-new decision id (`D6`, `D7`, `D10`, `D13`, `D14`, `D15`) instead of leaving the old entries (which narrate transcribing `DOC2`/`DOC2JA` into `docs/adopting.md`/`.ja.md` and report AC4/AC5 PASS for criteria that no longer exist) standing beside new ones.
  reason: the standing record described a deliverable — two frozen documentation blocks and a printed command — that is no longer in the tree after the **`same-class-2` pre-commitment fired on the documentation surface's one remaining round** (cross-provider round 3, finding 1) and the terminal fallback executed; a record that narrates a shipped block that is not shipped is a false statement about the repository's own state, which is the exact defect class cross-provider review found on the documentation surface itself (round 1 finding 2, round 3 finding 1). `bin/check-provenance.sh`'s own grammar also makes an edit-around-the-edges impossible here: `D14` and `D15` did not exist in the standing file, and the checker requires each `- decision:` to carry exactly one `reason:` and one `grounding:`, so satisfying AC5 required real new triples, not a splice.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` `## Notes for engineer` ("Rewrite `.shell-team/provenance/T-1046.md` end to end... the two new ids cannot be satisfied by editing around the edges") and D14 ("the provenance record is rewritten, because it still narrates transcribing two blocks that no longer exist"); `bin/check-provenance.sh` `DECISION_RE`/`REASON_RE`/`GROUNDING_RE` and its one-reason/one-grounding-per-entry schema (lines 30-38 of that script's header).

- decision: recorded `D6` (amended to zero shipped change) as the ruling that the whole-tree byte-identity claim is the thing this task ships, and grounded it in a live re-measurement performed by this role — not a relay of the spec's or the board's own claim that the reverts already happened — before writing the claim down.
  reason: this task's own history (four freezes, three cross-provider rounds) is a history of relayed and inherited claims about the tree's state turning out to need correction at the next checkpoint (the spec's `## Summarized sources` names several: the `log_warn` call-site count, the `docs/adopting.md` `check-ignore` base-content mention). Writing `D6`'s byte-identity claim into this record without independently checking it would repeat that pattern one level deeper — in the provenance file itself.
  grounding: this role ran `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1046-ignored-base-verdict.md` live before writing this entry: AC1 (whole-tree diff plus untracked-file sweep against `d825293` across all ten shipped pathspecs) and AC2 (the seven files `cmp`-verified one by one against their `d825293` blobs, including `bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md`, `docs/adopting.ja.md`) both PASS; `git diff --name-only d825293` independently confirmed at exactly five paths (AC3, also PASS) before this file was touched.

- decision: recorded `D7` (`validate_base` not tightened against a leading `:`) as carried rather than re-argued, and did not add any behavioral justification beyond citing the byte-identity lock.
  reason: `D7` was settled in v2 and is unchanged in v5; `bin/team-paths.sh` is one of AC2's seven `cmp`-locked files, so the file cannot have changed regardless of what a fresh argument might conclude, and re-arguing a decision the byte-identity assertion already forecloses would be motion without effect. The issue candidate this decision leaves behind (a symlink/gitlink/regular-file base) is `D10`(iii)'s territory, not repeated here.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D7 ("CARRIED unchanged... Locked by whole-tree byte-identity: `bin/team-paths.sh` does not change") and `## Open questions` item 4; AC2's live PASS above, specifically the `bin/team-paths.sh` `cmp` comparison.

- decision: recorded `D10`'s three inherited disposals by name (the `log_warn` errexit guard as a corrected-reason fast-follow, the `ls-files` minor as moot, the symlink/gitlink base as an issue candidate) as carried, rather than re-measuring `bin/team-init.sh`'s `log_warn` call sites a second time from this role.
  reason: the spec's own `## Summarized sources` already re-measured `bin/team-init.sh` at this branch's HEAD with a content search for `log_warn` and reports one definition plus exactly three pre-existing scaffolding call sites and zero emitter lines; AC2's live `cmp` above confirms that file is byte-identical to its base blob, so a second independent re-measurement by this role would necessarily reproduce the same count and add no new information — the byte-identity lock is the stronger guarantee.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D10 ("CARRIED... re-measured at this HEAD: `bin/team-init.sh` carries one `log_warn` definition and three pre-existing scaffolding call sites") and `## Summarized sources`' `bin/team-init.sh` row; the board's `.shell-team/todo.md` fast-follow bullets carrying both candidates forward, present and matched by AC11 (PASS above).

- decision: recorded `D13` (`.shell-team/test-recipe.md` stays at its base blob, absorbed into `D6`) without re-adding a `- T-1046:` entry to that file, and confirmed the two surviving occurrences of the token `T-1046` in it belong to the base-content `T-1042` entry rather than to this task.
  reason: this task's own Goal forbids any change to that file, and AC2's `cmp` lock (above) already asserts it byte-identical to base; adding an entry, even a well-intentioned one, would fail AC1/AC2 outright and would also misdescribe a file this task ships nothing into.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D13 and `## Summarized sources`' `.shell-team/test-recipe.md` row ("the two occurrences of the token `T-1046`... are inside the base-content T-1042 entry... not drift"); AC2's live PASS above, specifically the `.shell-team/test-recipe.md` `cmp` comparison.

- decision: left `.shell-team/interventions/T-1046.md` exactly as it stands (its single v2-cycle `assumption-contradicted` entry), rather than adding a new entry for this rewrite step, after checking whether anything found during the rewrite contradicted a spec assumption.
  reason: `D14` requires the interventions record to be left alone because it records history, not what ships, and rewriting or appending to it would falsify or dilute that history; the check performed while writing this file — the live AC1-AC4 re-run above — found no file that was not byte-identical to base and no `check:` line that measured nothing, so there was nothing that met this role's own bar (`## Notes for engineer`: "If the rewrite turns up anything that contradicts this spec's assumptions... *then* add an entry") for adding a new one.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D14 ("The interventions record is left exactly as it is... rewriting it would falsify the history it exists to hold") and `## Notes for engineer` ("Leave `.shell-team/interventions/T-1046.md` alone... If the rewrite turns up anything that contradicts this spec's assumptions... add an entry"); AC6's live PASS above (`bash bin/check-interventions.sh --task T-1046` conformant, one entry, zero sentinel, unchanged).

- decision: recorded `D15`'s three tracker dispositions in the contract's own words — `#167 is left open as unanswered`, the sprint v1.7.0 goal item for `#167` not met, and `#67` untouched — in this record as well as relying on the board's own copies, rather than paraphrasing them here or omitting them on the assumption the board alone suffices.
  reason: AC5 anchors the literal phrase `left open as unanswered` in this file independently of AC10's anchor on the board, so the two records must agree in the words used rather than merely in substance; `D15` itself states that a record which softens or omits the unmet goal item fails the criterion that reads it, and this role has no network access to re-file `#167` itself, so writing the disposition accurately in every record that carries it is the whole of what this role can do about it.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D15 ("A board that describes this task as a success, or that omits the unmet goal item, fails AC10") and `## What this achieved and what it cost` ("**#167 is left open as unanswered**... **Sprint v1.7.0's goal item for #167 is not met.** #67 is untouched"); AC5's own `check:` line requiring the phrase `left open as unanswered` in this file, and the board's matching lines (already present, `bash bin/check-acs.sh` AC10 PASS, independently re-run by this role before this entry was written).

- decision: changed only the status flag on the board's `T-1046` task line (`READY_FOR_ENG` to `READY_FOR_QA`) and added no new sub-bullet narrating this rewrite, rather than appending an engineer hand-off bullet in the board's usual style.
  reason: the board's T-1046 entry already carries the six defeat vectors, the four dispositions and both fast-follow candidates, each asserted by AC10 and AC11 with exact-occurrence and exact-phrase checks (`grep -c` equals 1, chained single-line filters); an added bullet risks nothing structurally forbidden, but the smallest change that satisfies the contract is the one least likely to accidentally duplicate or drift from an anchor `bin/check-acs.sh` already re-ran and found PASS, given this task's own history of small edits producing unintended regressions on this exact surface.
  grounding: `bash bin/check-acs.sh` live re-run by this role (above): AC9, AC10 and AC11 all PASS against the board as it stood before this file was touched; the coordinating session's task instructions ("So your entire job is: rewrite `.shell-team/provenance/T-1046.md`... then set the board status. Do not touch any other file.").

<!-- END provenance: T-1046 -->
