<!-- BEGIN provenance: T-1046 -->

- decision: executed v2's fired `## Same-class-2 pre-commitment` verbatim rather than proposing any repair, softening or third disposition for the verdict-acquisition layer — reverted `bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh` and `tests/errexit-safe/run.sh` to their `d825293` blobs via `git checkout d825293 -- <path>` for each of the four, after confirming with `git status --short` that all four were fully committed on this branch (so the revert is recoverable, not destructive), rather than hand-unwinding the mechanism's edits line by line.
  reason: the pre-commitment recorded the acquisition layer's budget as zero rounds remaining and named the fallback in advance — this is v2's fired `same-class-2` pre-commitment, not a fresh decision; round 2's addendum produced two new acquisition-layer findings (Finding A, Blocker; Finding B, Major), both `Acquisition layer: YES`. `## Same-class-2 pre-commitment`'s own text forbids re-presenting the disposition menu a third time, so no alternative was evaluated. Restoring from the base blob (rather than reverse-editing) is the meaning-preserving way to reach exact byte-identity, which is what AC1 asserts and what a hand-unwind could miss by a near-invisible whitespace or ordering difference.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` `## Same-class-2 pre-commitment` (v2, quoted in this file's own `## Re-freeze ledger`) and D6 (amended: zero emitters); AC1's `check:` line (four-file `cmp` against `d825293` blobs, plus a zero-occurrence control for `check-ignore` in the restored `bin/team-init.sh`) PASS.

- decision: extended the fallback's named revert target (v2's text names only `bin/team-init.sh`) to include `tests/team-init/run.sh`, `tests/errexit-safe/run.sh` and `.shell-team/test-recipe.md` as well, rather than reverting only the single file the fallback's own prose names.
  reason: v2's fallback text is silent about the other three because it was written before the mechanism's fixture and recipe surfaces existed to name; the thirteen frozen fixture labels live in `tests/team-init/run.sh`, the `log_warn` protected-content row lives in `tests/errexit-safe/run.sh`, and `.shell-team/test-recipe.md`'s `- T-1046:` entry cites `make_git_shim()`, a helper that leaves the repository the moment the first suite reverts — leaving any of the three un-reverted would ship dead fixtures or a recipe entry whose only citation no longer exists, which is the same documentation-accuracy defect class round 1's finding 2 established, arriving through a different door (D13).
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D13 and `## Summarized sources`' `tests/team-init/run.sh` and `.shell-team/test-recipe.md` rows; AC1's `check:` line (all four shell/test files byte-identical to base) PASS; AC2's `check:` line (`.shell-team/test-recipe.md` byte-identical to base, `T-1042` token present as a positive control) PASS.

- decision: replaced the paragraph this task previously added to `docs/adopting.md` (lines 58-68 pre-revert: "`team-init` itself checks this for you …") with the frozen `DOC2` block transcribed byte-for-byte from this spec's own `<!-- frozen-begin: DOC2 -->` / `<!-- frozen-end: DOC2 -->` fence, and the corresponding `docs/adopting.ja.md` paragraph with `DOC2JA`, rather than paraphrasing the frozen text or reflowing its wrap points.
  reason: D11 requires the shipped bytes to be the reviewed bytes ("a user-facing document making a false claim about the tool's own behaviour is precisely the defect cross-provider round 1 found... that class closes only when the shipped bytes are the reviewed bytes"), and AC4 asserts whole-sequence contiguity via a newline-to-`\002` substitution match, which a reflow or paraphrase would fail. Confirmed directly rather than assumed: `git diff -U0 d825293 -- docs/adopting.md docs/adopting.ja.md` shows zero removed lines in both files after the replacement, which still holds because the paragraph being replaced was added by this task and never merged (`## Summarized sources`).
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D11, the frozen `DOC2`/`DOC2JA` fenced blocks, and `## Notes for engineer`'s paragraph-location note ("in the pre-revert working tree it is `docs/adopting.md` lines 58-68 and `docs/adopting.ja.md` lines 59-68"); AC4's `check:` line (contiguity match, zero removed lines, `check-ignore -v` present in the added lines, both retired-claim fragments absent) PASS.

- decision: verified the command `DOC2`/`DOC2JA` print by actually executing it under `bin/check-acs.sh`'s AC5, across both of D12's execution-context cells and all three documented answers, rather than reading the command and reasoning about its correctness from the frozen text alone.
  reason: D12 states the shipped command is a deliverable and names it as "verified rather than read" — a rewritten frozen block is exactly the kind of change a stale wording review could pass while the actual command silently drifted (a `team-paths.sh --get base` resolution mistake, or a substitution-free form that is not a byte-identical equivalent). Running it live is the only way to confirm cell A and cell B produce byte-identical stdout for the same repository state, which is what makes the substitution-free fallback a real equivalent rather than an approximation.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D12 and `## Notes for engineer` ("Do not substitute reading the command for running it in either cell"); AC5's `check:` line (three `git init`-ed fixtures — ignored, clean, no-repository — both cells run and compared byte-identical, all three documented answers matched) PASS, run live under `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh`.

- decision: recorded the `log_warn()` errexit guard and the git-shim subcommand-matching lesson as fast-follow candidates on the board (`.shell-team/todo.md`) rather than filing GitHub issues for either, and corrected the fallback's own stated reason for the `log_warn` guard rather than repeating it verbatim.
  reason: this role has no network access, so the board is the only durable record available to it; re-measuring `bin/team-init.sh`'s (pre-revert) call sites directly (`grep -n 'log_warn'`) showed three pre-existing scaffolding call sites (`skipped existing file: …`) survive the emitter's removal, so the guard's exposure narrows from "unreachable" (the fallback's original claim) to "reachable only on a re-run over existing files" — a materially different and narrower claim that D10(i) requires be corrected rather than repeated, because leaving the imprecise reason in place would itself be a small instance of the same documentation-accuracy defect class this task exists to close.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D10(i) and `## Summarized sources`' `bin/team-init.sh` row (the re-measured "three pre-existing scaffolding sites" fact); AC12's `check:` line (both fast-follow strings present on the board, the `log_warn` entry additionally carrying the token `skipped existing file`) PASS.

- decision: did not tighten `bin/team-paths.sh`'s `validate_base()` against a leading `:`, and made no other change to that file, relying on AC1's byte-identity assertion to lock D7 rather than re-deriving a behavioral argument for it.
  reason: D7 is carried unchanged from v2, and this task's own Goal states the file must be byte-identical to base — any edit, tightening or otherwise, would fail AC1 outright. The issue candidate (`## Open questions` item 3) stands unfiled, again for lack of network access.
  grounding: `.shell-team/specs/T-1046-ignored-base-verdict.md` D7 (carried) and `## Open questions` item 3; AC1's `check:` line (`bin/team-paths.sh` byte-identical to its `d825293` blob) PASS.

<!-- END provenance: T-1046 -->
