<!-- BEGIN provenance: T-1022 -->

- decision: the source-line gate captures the hand-off lint's stderr on the synthesized board via a command-substitution variable (`SL_ERR="$(bash "$HANDOFF_LINT" "$SYN_BOARD" 2>&1 >/dev/null)"`), the same pattern the existing T-1017 interventions gate already uses for `CK_ERR`, rather than adding a second `GATE_ERR`-shaped temp file dedicated to this invocation.
  reason: two structurally valid options existed. (a) Add a second stderr-capture temp file (mirroring the pre-write interlock's `GATE_ERR`), which the gate's temp files could still join per D11 ("the gate's temp files need are created in the EXISTING mktemp block"). (b) Capture into a shell variable via command substitution, adding zero new temp files. (b) was chosen because D11's own stated purpose — keeping the single `trap`'s removal list minimal and never adding a second `trap … EXIT` — is served more directly by not creating a temp file at all where one is avoidable, and the interventions gate two sections above already establishes exactly this pattern for an analogous "capture a sibling checker's stderr before deciding how to react" need, so (b) is also the more consistent choice against the file's own existing style. AC8's static single-trap count and zero-leftover measurement pass either way, but (b) keeps the diff smaller and the trap-list shorter.
  grounding: `.shell-team/specs/T-1022-close-out-gate-symmetry.md`, D11 ("the temp files the source-line gate needs are created in the EXISTING mktemp block... trap is not additive"); `bin/close-out.sh`'s pre-existing interventions gate (`CK_ERR="$(bash "$CHECKER" --task "$TASK" -- "$RECORD" 2>&1 >/dev/null)"`), the pattern this decision mirrors.

- decision: the differential harness (`closeout-lineshape-differential`, D9/D10) classifies each corpus line as `notlocated` by grepping the REAL `close-out.sh` invocation's own stderr for its literal `not found as a top-level entry` message, never by independently re-implementing or mimicking pass 1's `awk` locator regex to predict which lines pass 1 would fail to find.
  reason: two structurally valid ways to build the "deliberately-not-located" classification existed. (a) Write a second, harness-local regex that mirrors pass 1's locator pattern (`^- \[ \] \*\*T-901\*\* `) and use it to pre-classify each corpus line as located/not-located before running anything. (b) Run the real `close-out.sh` and observe its actual refusal message live, treating the "not found" message as the ground truth for the notlocated bucket. (b) was chosen because (a) would be exactly the "second copy of the line grammar" D1 exists to prevent (this time a copy of the LOCATOR regex rather than LINE_RE), and D9 states the escape surface must be "enumerated mechanically... never transcribed from a reading of the two regexes" — a harness that hardcodes its own copy of the locator pattern to predict outcomes is the same class of defect this task fixes elsewhere. (b) also means a future change to pass 1's locator regex would be caught by the harness (a drift), whereas (a) would silently stay in sync with nothing.
  grounding: `.shell-team/specs/T-1022-close-out-gate-symmetry.md`, D9 ("the escaping-input set is enumerated mechanically, by differential testing, never transcribed from a reading of the two regexes") and D1 ("no local copy of LINE_RE... a hand-rolled regex has a structural ceiling"); `tests/close-out/run.sh`'s `lineshape_case()` function, the `not found as a top-level entry` grep this decision produced.

- decision: reason F's stderr line (`<BOARD>:<A_START>: would be rejected by the hand-off lint — refusing to move a malformed line into ## Done`) is printed with a bare `printf` carrying no `close-out: ` prefix, unlike every other refusal message in this script (which all go through `die()`/`fail()`, both of which prepend `close-out: `).
  reason: two structurally valid formats existed, both satisfying AC4's and AC6's literal substring assertions (`grep -qF -- "$d/bad/todo.md:5:"` matches a substring anywhere in the line, not only at line-start). (a) Route reason F through `fail()` with the board:line baked into the message argument, giving `close-out: <BOARD>:<A_START>: would be rejected...`. (b) Print the bare `<BOARD>:<A_START>: ...` line with no `close-out: ` prefix, mirroring the exact `<file>:<lineno>: <reason>` diagnostic shape `bin/check-handoff.sh`'s own `emit()` uses (and the shape D4's own note line ("the file:line below refers to...") explicitly primes the reader for). (b) was chosen so the line an operator sees immediately reads as the same kind of `<file>:<lineno>:` diagnostic as the checker's own preceding line, which is what D4's note line exists to disambiguate ("refers to a synthesized single-entry board, not the real board") — prefixing it with `close-out: ` first would visually separate it from that diagnostic shape and weaken the note's own framing. `fail()`'s usual "close-out: " framing is still used for every OTHER exit-1 reason string in this script (D3's other rows), so this is a narrow, deliberate departure for this one line only, not a stylistic rewrite of the file's convention.
  grounding: `.shell-team/specs/T-1022-close-out-gate-symmetry.md`, D4 ("the file:line below refers to a synthesized single-entry board, not the real board... reason F's line additionally carries the real `<BOARD>:<A_START>:`") and AC4/AC6 (both assert the substring, not an anchored line-start match); `bin/check-handoff.sh`'s `emit()` (`printf '%s:%s: %s: %s\n' "$FILE" "$1" "$2" "$3"`), the diagnostic shape this decision mirrors.

<!-- END provenance: T-1022 -->
