<!-- BEGIN provenance: T-1103 -->
- decision: the approval record's five ` — `-separated fields (`approver=`, `producer=`, `approves=`, `date=`, `record=`) are extracted with sequential anchored `sed -E` substitutions using a greedy `.*` for every field including `approver=`/`producer=`/`approves=`/`date=` (not only the trailing free-form `record=` field), each re-anchored from the line start with the literal marker text for every field already known.
  reason: a greedy `.*` backtracks to the correct split point even when a field's own value carries trailing whitespace right up against the separator (AC12's padding-evasion case, `producer=alice   `), because the regex engine finds the rightmost position at which every subsequent literal marker can still match; a stricter `[^ ]*`/`[^—]*` character-exclusion class cannot recover the padding once excluded, and a plain string-split on the first occurrence of ` — ` would mis-split whenever an earlier field's value happened to be empty. The same anchored-literal-with-trailing-`.*` shape is already this repository's own idiom for a similar multi-field ` — `-separated record (bin/close-out.sh's `d_axis`/`d_value`/`d_modality`/`d_ground` dispatch-record extraction), so this decision also avoids introducing a second parsing idiom for one grammar family.
  grounding: `bin/close-out.sh`'s dispatch-record field extraction (the four sequential `sed -nE` lines assigning `d_axis`, `d_value`, `d_modality`, `d_ground`); `tests/check-oversight/run.sh`'s "trailing-padded producer still refuses after whitespace stripping" case, verified passing against this extraction.

- decision: `--config PATH` bypasses the occupancy lattice's absent-arm fallback entirely — a missing or non-regular file named that way refuses `declaration-unreadable` (exit 2) rather than resolving to the shipped `autonomous` default, reusing the existing `declaration-unreadable` token rather than coining a new one for this case.
  reason: the spec requires "an absent file named that way is exit 2 rather than a fallback" but does not fix which token; `declaration-unreadable`'s own closed-vocabulary gloss in the spec's `## Notes for engineer` ("an unreadable regular file, or a missing shipped default") already names exactly this shape — a caller pointing `--config` at a path it expected to exist and finding it gone — so reusing it keeps the refusal-token vocabulary from growing for a case the spec's own prose already covers under an existing member.
  grounding: `.shell-team/specs/T-1103-oversight-profiles.md` `## Notes for engineer`, the "Exit codes and the closed refusal set" paragraph's `declaration-unreadable` gloss.

- decision: `parse_config()`'s post-scan validation order is schema checks (missing/duplicate/not-first/unsupported), then profile checks (missing/duplicate/unknown), then the per-seam vocabulary/duplicate loop, then the profile-seam cross-checks (`no-seam-declared`/`seam-under-autonomous`) — each stage refusing immediately rather than collecting all violations.
  reason: this order is what makes every one of **AC5**'s ten grammar/vocabulary cases resolve to its own specifically-required token when a synthesized config could, in principle, be read as violating more than one rule at once (e.g. `schema 2\nprofile autonomous` violates only `unsupported-schema` under this order, never coincidentally also flagged for a profile-level defect it does not actually have); the order was derived by tracing each of the ten AC5 fixtures through the implementation by hand before freezing the sequence.
  grounding: `.shell-team/specs/T-1103-oversight-profiles.md` **AC5**'s ten enumerated cases and their required tokens; `tests/check-oversight/run.sh`'s ten `grammar_case` assertions, each verified passing with its expected token.

- decision: the sticky-enrollment whole-board scan runs before the task's own `## Active` entry is located (rather than after), when the profile resolves to `autonomous` from the absent arm.
  reason: the spec's `## Notes for engineer` discloses this ordering as unconstrained ("it may run before or after the `--task` lookup, and no criterion here constrains that order"), so an explicit choice was still needed to produce one deterministic implementation; scanning first was chosen because the sticky check needs no entry resolution at all (it is, in the spec's own words, "the crudest read in the checker"), so running it first avoids paying for a task lookup on a board a governance-vanished repository is about to refuse anyway.
  grounding: `.shell-team/specs/T-1103-oversight-profiles.md` `## Notes for engineer`, "The sticky-enrollment condition, in the order it must be evaluated" paragraph.

- decision: within the approval-record validation for a declared seam, refusals are checked in this order: record count (missing/duplicate) -> whole-line grammar (`approval-malformed`) -> per-handle charset (`bad-handle`) -> handle equality (`approver-equals-producer`) -> anchor shape (`approval-anchor-malformed`) -> the seam-specific anchor comparison (`approval-stale`/`approval-anchor-ahead`, or the three `pre-merge` git-based clauses).
  reason: no acceptance criterion exercises a record violating two of these classes at once, so the order is a design choice rather than one derived from a specific test case; the chosen order proceeds from the coarsest structural judgment (does a record for this seam exist at all) to the finest content judgment (is the specific anchor value current), which is the same coarse-to-fine shape `bin/check-binding.sh`'s own two-pass validation already uses for its config grammar.
  grounding: `bin/check-binding.sh`'s two-pass validation order (schema-level facts judged before per-row content, per that script's own header comment); `.shell-team/specs/T-1103-oversight-profiles.md` **AC3**/**AC12**/**AC22**, none of which constructs a record violating two of these classes simultaneously.

- decision: `tests/close-out/run.sh`'s new `oversight_case` fixture helper does not `cd` into its scratch root before invoking `bin/close-out.sh` (every path is instead redirected through absolute `TEAM_TODO`/`TEAM_INTERVENTIONS_DIR`/`TEAM_OVERSIGHT_BASE` env overrides), unlike the pre-existing sibling `spec_review_case` helper immediately above it, which does `cd` into its own scratch root.
  reason: the `pre-merge` seam's anchor comparison runs real `git` plumbing (`git rev-parse --is-inside-work-tree`, `git cat-file -e`, `git merge-base --is-ancestor`) against `HEAD`, so the invocation's working directory must be inside the real git checkout; a scratch root under `$TMP` is not a git repository, and `cd`-ing into it (as first attempted, mirroring the sibling helper) reproduced `check-oversight: not-a-git-repo` live before this decision was made.
  grounding: `bin/check-oversight.sh`'s `pre-merge` branch, the `git rev-parse --is-inside-work-tree` refusal; live reproduction of the `not-a-git-repo` failure when the sibling `spec_review_case` shape (`cd "$root" && ...`) was first tried for `oversight_case` (`stderr: check-oversight: not-a-git-repo: the pre-merge seam requires a git repository to compare the approval anchor`), before the fix.

- decision: the pre-existing `tests/close-out/run.sh` "interventions-resolver-failure-exit2 positive control" invocation (a scratch `bin/` copy with `team-paths.sh` deleted) was widened to additionally set `TEAM_OVERSIGHT_BASE="$RF_ROOT/root/.shell-team"`, rather than left unchanged.
  reason: this task's new pre-merge gate is unconditional, so every `bin/close-out.sh` invocation now also resolves an oversight base directory; this specific pre-existing test's crippled `bin/` copy has no `team-paths.sh` for that resolution to fall back to, and its own positive-control invocation (which asserts a bare `bash "$CLOSEOUT" ... || fail ...`, i.e. exit 0 is required) supplied no override for the new dependency, reproducing `check-oversight: usage: cannot resolve operating paths` live before this fix. Pointing the new override at the same already-existing `.shell-team` directory the interventions override already uses keeps the fixture minimal (no new scratch directory) and resolves to the shipped `autonomous` default there (no `oversight.conf` exists in that directory), so the gate stays silent exactly as the test's own name and comment require.
  grounding: `tests/close-out/run.sh`'s pre-existing "interventions-resolver-failure-exit2" block (the scratch `bin/` copy with `team-paths.sh` removed); live reproduction of the failure (`stderr: check-oversight: usage: cannot resolve operating paths (team-paths.sh missing or unreadable next to check-oversight.sh)`) before this fix, and the full suite passing (120 `PASS` lines, 0 `FAIL`) after it.

- decision: round-2 rework, supersedes this file's FIRST decision above — the five ` — `-separated fields are re-extracted with a single left-to-right pass over the raw record string using bash parameter-expansion pattern matching (`${rest%% "$EM" *}` / `${rest#*"$EM" }`), consuming exactly one field per loop iteration and leaving `record=` — the only genuinely free-form field — to receive whatever text remains, rather than the five independent whole-line greedy `sed -E` passes the FIRST decision above chose and this round's cross-provider review found defeatable.
  reason: the round-1 implementation review reproduced, live, that a decoy `— approves=v999 —` sequence embedded inside `record=`'s own free-form value shifted the real `approves=` field's greedy `.*` capture to the decoy's value instead — falsifying this task's own frozen body-text claim that "record= is last and free-form, so an em dash inside a locator cannot forge a field." Two designs were weighed to close it: (a) an awk-based tokenizer, and (b) the bash parameter-expansion left-to-right consumption implemented here. (b) was chosen because it needs no second language/process per record (awk would add a subprocess per field), and because consuming the separator itself (rather than restricting each field to a non-` — `-containing character class) preserves the FIRST decision's own reasoning for why a plain character-exclusion class was rejected — this shape still tolerates an in-field value abutting the separator (the padding-evasion case) while making it structurally impossible for a later field's bytes to influence where an earlier field is read from, since each field is consumed and discarded before the next one is inspected.
  grounding: `.shell-team/reviews/T-1103.md`, "Codex review — round 1 (implementation)", Blocker 2 (the live `sed` reproduction showing `approves_raw` bound to a decoy's `v999` instead of the record's real `v1`); `tests/check-oversight/run.sh`'s "record forgery" fixture pair (an em-dash decoy tail that must still pass, and a genuinely malformed record that must still refuse despite carrying a decoy `approves=` tail), both verified passing against the new parser.

- decision: round-2 rework — the `specify-seam` anchor's accepted form is narrowed from `^v[0-9]+$` (unbounded digit count) to `^v(0|[1-9][0-9]{0,14})$` (at most fifteen digits, no leading zero other than the literal `0`), refusing anything outside that form with the EXISTING `approval-anchor-malformed` token rather than a new one.
  reason: the review reproduced, live, that `$((10#$N))` on an unbounded digit string wraps bash's 64-bit signed arithmetic (`10#18446744073709551617` evaluates to `1`), letting an absurd `approves=` value pass the equality check as if it read `v1`. The fix has to bound the digit run BEFORE any arithmetic runs on it (the review's own suggested fix shape); fifteen digits was chosen as the ceiling because it is comfortably below bash's ~19-20-digit 64-bit ceiling with wide margin for any board this loop will ever produce, while a tighter bound (e.g. 3-4 digits) risked rejecting a legitimate, if unusually long-lived, re-freeze count with no compensating security benefit. Leading zeros are refused rather than tolerated because the anchor is compared for EQUALITY against a canonically-formatted `v<N>` string with no leading zeros (DP-12), so a leading-zero form is never the literal the equality is checking for. `approval-anchor-malformed` (not a new token) was reused because its existing message text ("is not of the required form v<N>") already covers "the digit run is out of the accepted range" as one more way the form can fail, and this task's frozen Goal section does not enumerate an exhaustive refusal-token list that a new token would need to be reconciled against.
  grounding: `.shell-team/reviews/T-1103.md`, "Codex review — round 1 (implementation)", Blocker 1 (the live `bash -c` reproduction of the overflow); `tests/check-oversight/run.sh`'s three "anchor bound" fixtures (the overflow value itself, a 20-digit value, and a leading-zero form), all verified refusing `approval-anchor-malformed`.

- decision: round-2 rework — the `approval-malformed` refusal for a grammar-mismatching record now names the record's absolute board line number (computed via `grep -n` against the already-extracted entry, offset by the entry's own start line) instead of interpolating the raw matched line's bytes.
  reason: the review found this the one refusal path in the file that echoed a handle-bearing line verbatim, contradicting the script's own header invariant (lines 93-96) and every other refusal's discipline. Two ways to keep the message useful without the leak were weighed: naming nothing about the record's location at all, or naming a locator that carries no field content. A line number was chosen over "name nothing" because it still lets a human or CI log reader find the offending record without republishing its bytes, and a line number cannot itself carry PII the way even a truncated snippet might.
  grounding: `.shell-team/reviews/T-1103.md`, "Codex review — round 1 (implementation)", Major (finding 2, `approval-malformed` echo) and its cited invariant (`bin/check-oversight.sh` header, lines 93-96); `tests/check-oversight/run.sh`'s "no-echo" fixture (a grammar-mismatching record carrying real-looking handle bytes, asserted absent from stderr), verified passing.

- decision: round-2 rework — a genuine `git log` failure while resolving the last commit that touched the task's spec path (distinguished from the legitimate "no commit has touched this path yet" empty-output case by capturing the command's own exit status via the `cmd || rc=$?` errexit-safe idiom) refuses with the EXISTING `not-a-git-repo` token rather than a new one.
  reason: the review found the prior `|| true` swallowed this distinction entirely, collapsing a real git error into a silent skip of the spec-precedence freshness check. The refusal needed SOME exit-2 token; reusing `not-a-git-repo` rather than coining a new one was chosen because both conditions are the same class of fact from this checker's own point of view — "the git operation this seam's anchor comparison depends on did not succeed" — and the message text itself (which does get echoed, since it carries no party-supplied handle bytes, only the git exit status and the operator-supplied spec path already visible on the board) is what actually distinguishes the two cases for a reader, so a second top-level token would add vocabulary without adding information a reader could not already get from the message.
  grounding: `.shell-team/reviews/T-1103.md`, "Codex review — round 1 (implementation)", Minor (finding 5); live reproduction that `git log -1 --format=%H -- ':(bogus)notreal.md'` exits 128 with "fatal: Invalid pathspec magic" (used as the fixture's forcing mechanism); `tests/check-oversight/run.sh`'s "git-log failure" fixture, verified refusing exit 2 with the failure named in stderr.

- decision: round-2 rework — `date=` is now validated against `^[0-9]{4}-[0-9]{2}-[0-9]{2}$` and a shape violation refuses with the EXISTING `approval-malformed` token rather than a new one.
  reason: the review found `date=` accepted any byte sequence despite the record grammar's own documented `YYYY-MM-DD` shape. `approval-malformed` (rather than a new token) was reused because a date shape violation is, structurally, the same class of defect as any other field not matching the record's documented grammar — the same token this file already uses for a missing separator or a missing key= prefix — and `date=` carries no role in the anchor/staleness security logic (that is `approves=`'s job alone, per this task's own disclosed design), so there is no security-relevant distinction for a caller to make between this and any other grammar defect.
  grounding: `.shell-team/reviews/T-1103.md`, "Codex review — round 1 (implementation)", Minor (finding 6); `.shell-team/specs/T-1103-oversight-profiles.md`, `## Notes for engineer`, the record-grammar block (`<date>` is `YYYY-MM-DD`); `tests/check-oversight/run.sh`'s "date grammar" fixture, verified refusing `approval-malformed`.

- decision: round-2 rework — four PRE-EXISTING `tests/close-out/run.sh` fixtures that build a scratch `bin/` copy (`STUB_ROOT`, `RF_ROOT`, `HS_ROOT`, `STUB_SL_ROOT`) were each widened with an additional `cp -R "$REPO_ROOT/templates" "<root>/templates"`, mirroring the real installed layout, rather than redirecting each fixture's oversight resolution through an explicit `profile autonomous` config file.
  reason: fixing the absent arm (this round's Major 1) to genuinely read `templates/oversight-default.conf` from the invoked checker's OWN installed directory broke every one of these four fixtures' own "positive control" case (an exit-0 expectation that previously passed only because the pre-fix absent arm hardcoded `autonomous` and never touched the filesystem) — reproduced live for each site before this fix, each with `check-oversight: declaration-unreadable: the shipped default oversight declaration is missing or unreadable: <root>/templates/oversight-default.conf`. Copying the real `templates/` directory alongside the scratch `bin/` copy (the same shape `tests/check-adapter/run.sh:300`'s `cp -R "$REPO_ROOT/templates" "$1/templates"` already uses) was chosen over authoring a synthetic `profile autonomous` config at each site's `$TEAM_OVERSIGHT_BASE`, because it needs no per-site config content decision and it is what actually proves the absent arm resolves the SHIPPED file from the checker's own directory (an adopter's real failure mode), rather than merely proving some `autonomous` file works when explicitly pointed at.
  grounding: `tests/check-adapter/run.sh:300` (`cp -R "$REPO_ROOT/templates" "$1/templates"`, the precedent this decision copies); live reproduction of `declaration-unreadable` at each of the four sites before this fix; `tests/close-out/run.sh`'s full suite passing (120 `PASS` lines, 0 `FAIL`) after it.

- decision: round-3 rework (class sweep, host-authorized 2026-08-29) — the `specify-seam` equality's `MAXV` operand (derived from the board's own `- intent-hash (v<M>):` sub-bullets) is now bounded to `{1,4}` digits before `$((10#$v))` runs, PORTING `bin/check-intent.sh`'s own `HASH_FULL_RE` bound for the identical field, rather than reusing round-2's own `{1,15}` bound already chosen for the `APPROVES` operand of this same equality.
  reason: enumerated every `10#`/`$((...))` site in `bin/check-oversight.sh` (grep for `10#` and `$((`) as the mandate required: internal loop counters (`schema_count`, `profile_count`) and line-count arithmetic (`RLINE_NUM`) are safe by construction (never derived from an untrusted digit string, bounded by the file's/board's own practical line count); `N="$((10#$N))"` (the `APPROVES` operand) is already safe because the upstream `^v(0|[1-9][0-9]{0,14})$` bound sits immediately above it, unconditionally, on the only path that reaches it; `v="$((10#$v))"` inside the `MAXV` loop was the one exposed site, matching the fresh Blocker exactly. Between porting `check-intent.sh`'s own `{1,4}` bound for the identical `- intent-hash (v<M>):` field and reusing `APPROVES`'s own `{1,15}` bound, `{1,4}` was chosen because the reviewer's own recommendation named `check-intent.sh`'s bound by file and line as "the exact reasoning to reuse," `- intent-hash` versions and `approves=` versions are the same underlying counter (a spec's re-freeze count) already bounded to `{1,4}` at the sibling site with a comment reasoning through this repository's own real re-freeze history, and bounding both operands of the same equality to the SAME digit-width ceiling (rather than to two different ceilings for what is, DP-13's own design, a class-blind version count) removes rather than introduces an asymmetry a future reader would have to explain.
  grounding: `bin/check-intent.sh:428-440` (`HASH_FULL_RE='^[[:space:]]+- intent-hash \(v([0-9]{1,4})\): ([0-9a-f]{40})$'`, its T-1021 (D4) comment); `.shell-team/reviews/T-1103.md`, "Codex review — round 2 (implementation)", the fresh Blocker's live repro and its recommendation naming `check-intent.sh`'s bound by file and line; `tests/check-oversight/run.sh`'s "MAXV bound" fixture group, verified refusing/passing as designed.

- decision: round-3 rework — a board line matching the loose `- intent-hash (` anchor but failing the bounded strict grammar refuses fail-closed with a NEW token, `intent-hash-malformed`, at exit 1 (the existing "content refusal" class every other malformed-grammar refusal in this file already uses), rather than being silently skipped-and-excluded from the `MAXV` count, and rather than a structural exit-2 refusal.
  reason: skip-and-continue was rejected because it would let a fabricated oversized (or otherwise malformed) intent-hash line hide the board's real maximum from the equality instead of being counted against it — exactly the failure mode the mandate named. Exit 1 (content refusal) rather than exit 2 (structural/cannot-evaluate) was chosen because this checker's own header already classifies every OTHER malformed-grammar-on-committed-bytes refusal (`approval-malformed`, `approval-anchor-malformed`, `unparseable-line`, etc.) as exit-1 content, and a malformed intent-hash sub-bullet is the same class of fact (committed board content that fails this checker's own grammar), not a "cannot even attempt evaluation" class like a missing file or a git failure. A NEW token (rather than reusing `approval-anchor-malformed`) was chosen because the offending sub-bullet is not the approval record itself — reusing that token would misname WHICH board line failed for a reader or a script keying off the token. The refusal message names the task and the sub-bullet SHAPE but never the offending digit string itself (per the mandate's explicit instruction), both because an oversized digit run serves no diagnostic purpose once already known to be out of bounds, and to keep the message length independent of how large an adversarially-fabricated digit string might be.
  grounding: `bin/check-intent.sh:680-699` (`hash_bad_count`/`fail_hash_structural`'s own disposition for a line matching `HASH_LINE_RE` but not `HASH_FULL_RE` — refuses rather than dropping the line from the count, the same disposition ported here); `bin/check-oversight.sh`'s own header exit-code enumeration (the exit-1 "content refusal" list, now naming `intent-hash-malformed`); `tests/check-oversight/run.sh`'s "MAXV bound" fixtures, verified refusing exit 1 with `intent-hash-malformed` and never echoing the oversized digit string.
<!-- END provenance: T-1103 -->
