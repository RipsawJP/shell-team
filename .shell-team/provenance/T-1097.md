<!-- BEGIN provenance: T-1097 -->
- decision: the invalid-branch-name refusal (input space class 8) is implemented by shelling out to `git check-ref-format --branch "$TRIAL_BRANCH"` rather than a hand-rolled regex validator.
  reason: git's own ref-name grammar (consecutive dots, control characters, trailing `.lock`, a leading/trailing slash, and the rest of `git-check-ref-format(1)`'s rules) is exactly the boundary the spec's own out-of-scope declaration names ("`git check-ref-format`'s own verdict is the boundary; nothing beyond it is protected") — re-deriving that grammar in bash would drift from git's actual acceptance rules the moment either one changes, and this script already shells out to git for every other trial-branch check once `--trial-branch` is given.
  grounding: .shell-team/specs/T-1097-trial-branch-flag.md, `## Input space` → "Out-of-scope synthetic extremes" item 4 ("`git check-ref-format`'s own verdict is the boundary; nothing beyond it is protected"); bin/team-init.sh's `git check-ref-format --branch "$TRIAL_BRANCH"` line.
- decision: refusal 1 (no git work tree, including a bare repository) is detected by capturing `git -C "$TARGET" rev-parse --is-inside-work-tree` and refusing unless the captured value is exactly the string `true` — rather than, say, testing for a `.git` entry directly or parsing `git rev-parse --is-bare-repository`.
  reason: `--is-inside-work-tree` collapses both reachable no-work-tree shapes the spec names (no repository at all, where the command fails and the capture is empty; a bare repository, where the command succeeds and prints `false`) into a single non-`true` outcome, so one comparison refuses both without a second git call or a bare-repository special case.
  grounding: .shell-team/specs/T-1097-trial-branch-flag.md AC4 and `## Input space` item 5 ("Both refused by the same condition"); measured directly in this session (`git -C <bare> rev-parse --is-inside-work-tree` prints `false` exit 0; `git -C <no-repo> rev-parse --is-inside-work-tree` fails with a non-zero exit and empty stdout).
- decision: the argument-parsing loop treats an empty string (`''`) as a value for `--trial-branch` identically to a missing value or a dash-leading value — all three die with `--trial-branch requires a branch name` — even though AC7's six named forms do not include an explicit empty-string case.
  reason: the spec's own `## Input space` reachable-classes list names "a branch name git itself rejects (`bad..name`, a leading `-`, an empty value)" as a class this implementation must handle correctly; without this extra `case` arm, `--trial-branch ''` would silently set `TRIAL_BRANCH=""`, and the later `[ "$TRIAL_BRANCH_GIVEN" -eq 1 ]` gate (which does not re-test emptiness) would attempt `git check-ref-format --branch ''`, exiting 2 for the right reason but through a code path this task did not otherwise design for and with no guarantee of a `--trial-branch`-naming message.
  grounding: .shell-team/specs/T-1097-trial-branch-flag.md `## Input space` item 8 ("A branch name `git` itself rejects (`bad..name`, a leading `-`, an empty value)"); bin/team-init.sh's `--trial-branch)` case arm, `-*|'') die "--trial-branch requires a branch name" ;;`.
- decision: the suite's no-work-tree fixture (`tests/team-init/run.sh`, case `T-1097 no-work-tree`) exports `GIT_CEILING_DIRECTORIES` set to the new git-needing scratch root (`$GIT_TMP`) for the remainder of the suite's process, rather than scoping it to a single invocation via an inline env-var prefix or a subshell.
  reason: every other git-touching fixture in this suite invokes git with `-C <a-directory-that-already-contains-its-own-.git>`, so git finds that repository immediately and never searches upward past the ceiling — a persistent, script-wide export is behaviorally equivalent to a narrowly-scoped one for every existing and new fixture, and it is what the spec's own equivalent `- check:` lines do (`export GIT_CEILING_DIRECTORIES="$T"`) for the identical non-vacuity reason: the note-for-engineer's warning that `$HERE/tmp-targets.*` sits inside this repository's own work tree applies regardless of which arm of the `$TMPDIR`-or-`$HERE` fallback produced the scratch root.
  grounding: .shell-team/specs/T-1097-trial-branch-flag.md, `## Notes for engineer` ("set `GIT_CEILING_DIRECTORIES` and assert the fixture's repo-freeness before asserting the refusal"); the spec's own AC3/AC4 `- check:` lines (`export GIT_CEILING_DIRECTORIES="$T"`); tests/team-init/run.sh's pre-existing `git -C "$REPO_ROOT" hash-object ...` call (T-1057), confirmed unaffected because it names a directory that is already this repository's own root.
- decision: (rework round 1, Major 2) any `--trial-branch` value beginning with the literal character `@` is refused outright by a plain `case "$TRIAL_BRANCH" in @*) die ...` — BEFORE `check-ref-format` or any other git call runs — rather than the alternative the round's own fix instructions also offered: run `check-ref-format --branch` with `-C "$TARGET"`, capture its (expanded) stdout, and require that value to equal the literal input, refusing on any difference.
  reason: every git revision-shorthand form the round demonstrated or named (`@`, `@{-1}`, `@{upstream}`, `@{u}`) begins with `@` (gitrevisions(7)'s whole "at-mark" grammar), so a single leading-character rule closes the entire class in one place, independent of git version-specific expansion semantics, and never invokes `--branch`'s DWIM expansion mode on an untrusted value at all — the captured-and-compared alternative would still call that expansion mode (just now scoped to `-C "$TARGET"`), leaving its behavior dependent on git's own expansion semantics rather than eliminating the exposure. Measured live: with a real, non-colliding previous branch in the target repository, `git -C "$TARGET" check-ref-format --branch "@{-1}"` returns rc=0 and prints the expanded name (not `@{-1}`) — confirming the round's own report that `--branch` mode expands against whatever repository `-C` points at, which is exactly the surface the leading-`@` refusal removes rather than narrows. `HEAD` is not itself `@`-prefixed and needs no new rule: `git check-ref-format --branch "HEAD"` already refuses it unconditionally (verified live, independent of `-C` or repository state), so it was already refused before this task even without a fix.
  grounding: .shell-team/reviews/T-1097.md, Major 2 ("`--trial-branch '@{-1}'` bypasses the already-exists refusal entirely... validate the LITERAL name without at-expansion (avoid `--branch`'s expansion mode ...)"); bin/team-init.sh's `case "$TRIAL_BRANCH" in @*) die ... ;; esac` line, placed before the `check-ref-format` call; tests/team-init/run.sh's `T-1097 at-shorthand` case, whose mutation probe (removing the `@*` guard) reproduces the round's own bypass live — `--trial-branch '@'` against a fresh target exits 0 instead of 2.
- decision: (rework round 1, Major 1) `--no-track` is placed AFTER the branch name in the `switch` invocation — `git -C "$TARGET" switch -c "$TRIAL_BRANCH" --no-track` — never `switch -c --no-track "$TRIAL_BRANCH"`.
  reason: measured live, `git switch -c --no-track <name>` fails with `fatal: invalid reference: <name>` — git's own option parser reads the token immediately following `-c` as the new branch name, so `--no-track` in that position is consumed as the (invalid) branch name itself, not as a flag. `switch -c <name> --no-track` and `switch --no-track -c <name>` were both measured to work correctly (no upstream tracking configured even under `branch.autoSetupMerge=always`); the name-then-flag order was chosen as the more conventional reading of the two working forms.
  grounding: .shell-team/reviews/T-1097.md, Major 1 ("Fix is a one-token addition: `git switch -c --no-track \"$TRIAL_BRANCH\"`" — the reviewer's own suggested token order, measured in this session to mis-parse); measured directly in this session (three orderings tried against a real repository; `-c --no-track <name>` fails, `-c <name> --no-track` and `--no-track -c <name>` both succeed with no `branch.<name>.remote`/`.merge` set); bin/team-init.sh's `switch -c "$TRIAL_BRANCH" --no-track` line; `tests/team-init/run.sh`'s `T-1097 no-track` case.
- decision: (rework round 1, Major 3) `GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` are neutralized with `env -u GIT_DIR -u GIT_WORK_TREE -u GIT_INDEX_FILE` prefixed on EACH of the five individual git invocations, rather than factored into a single wrapper function (e.g. `tgit() { env -u ... git -C "$TARGET" "$@"; }`) called at each site.
  reason: **AC9**'s own `- check:` line statically greps the file's non-comment bytes for the literal substrings `git (-C [^ ]+ )?(add|commit|...)( |$)` (must be zero), `git checkout -b` (must be zero), and `git (-C [^ ]+ )?switch ` (must be at least one, the closed-set's own positive control). A generic wrapper function would move the literal token `git` out of every call SITE and into the wrapper's own single definition, so the wrapper's one line (`git -C "$TARGET" "$@"`) — which has no verb after `-C "$TARGET"`, only `"$@"` — would satisfy neither the zero-verb-count checks (trivially, nothing to match) nor, critically, the `switch` positive control (no literal `git ... switch ` text would exist anywhere in the file once every call site reads `tgit switch -c ...` instead), silently breaking the AC9 criterion this task must keep green. Repeating the five-word `env -u ...` prefix at each of the five call sites keeps every site's literal git-invocation text unchanged (still `git -C "$TARGET" switch -c ...`, etc.), so AC9's own extraction and both its counts and its positive control keep measuring exactly what they were written to measure.
  grounding: .shell-team/specs/T-1097-trial-branch-flag.md AC9 ("read from `bin/team-init.sh`'s non-comment lines only... the same non-comment extraction is asserted to carry `git [-C <arg>] switch ` at least once"); `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1097-trial-branch-flag.md` — AC9 PASS after the rework, confirmed by re-running the criterion's own extraction (`grep -cE 'git (-C [^ ]+ )?switch '` = 1) against the post-rework file; .shell-team/reviews/T-1097.md's own instruction to keep the invoked-subcommand set's extraction intact ("update AC9's enumeration expectations only if the spec's own text permits — it names the extraction, not the exact set").
- decision: (rework round 1, Minor taken) the previously-unguarded `toplevel="$(cd "$toplevel_raw" && pwd -P)"` is changed to `... || die "--trial-branch: could not resolve the repository root (target moved or vanished mid-run: $TARGET)"`.
  reason: measured directly in this session (a standalone reproduction: `cd` on a path that no longer exists, under `set -euo pipefail`, crashes the whole script with bash's own `No such file or directory` message and exit 1) that the pre-rework line broke this flag's own promised contract — every refusal is exit 2 with a `--trial-branch`-naming remedy — the one place in the block that did not already route through `die()`. The coordinator's own rework message named this specific fix as cheap enough to take now rather than deferring it as the review's own "file-an-issue" disposition suggested.
  grounding: .shell-team/reviews/T-1097.md, Minor/nit (`bin/team-init.sh:226-228`, "has no fallback if `cd` fails... `set -euo pipefail` kills the script... exit 1, not the promised... exit 2") and its "Fast-follow disposition" entry recommending file-an-issue; the coordinating session's rework instructions ("apply cheap Minor fixes at your judgment (the unguarded `cd` exiting 1 instead of the promised 2 is worth taking)"); measured directly in this session (`bash -c 'set -euo pipefail; ...; toplevel="$(cd "/does/not/exist" && pwd -P)" || die ...'` → prints the `die()` message and exits 2, versus the unguarded form crashing with exit 1).
<!-- END provenance: T-1097 -->
