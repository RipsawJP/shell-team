<!-- BEGIN provenance: T-1141 -->

- decision: wrote the canonical block's substance-carrying bullet with the literal, lowercase, case-sensitive phrase "measured values" appended in an explanatory parenthetical-like clause ("Durations, counts, exit statuses, verdict labels and executor names — measured values and verdicts — are what a later reader acts on"), and added a plural-corpus clarifying clause to the naming bullet ("even where the governed thing occurs more than once in the same record ... the same four generic nouns cover every occurrence, and no singular phrasing here should be read as capping the count at one"), rather than shipping the spec's own proposed draft wording in `## Notes for engineer` verbatim.
  reason: AC1's `check:` line greps the fixed string `measured values` (lower-case `m`) as a case-sensitive `grep -qF`; the proposed draft's own sentence began "Measured values and verdicts carry the substance" (capital `M`, sentence-initial), which does not contain the lower-case string and would have failed AC1 as written. Appending the lower-case phrase once, inline, closes the gap without discarding the proposed sentence's own meaning. Separately, AC14 item (e) requires "the block's own text read against a consumer where the governed thing occurs more than once ... so no singular determiner smuggles in a cardinality-one assumption" — the proposed draft's "a project, a tracker, a person and a machine" reads naturally as one of each, so an explicit plural-corpus clause closes that self-check rather than leaving it to be discovered later as a finding.
  grounding: `.shell-team/specs/T-1141-record-portability-block.md` **AC1**'s `check:` line (`for s in 'never by name' 'measured values' ...; do grep -qF -- "$s" "$P" || rc=1; done`) and **AC14** item (e) ("Plural-corpus read"); the proposed draft text itself, quoted in the spec's `## Notes for engineer` section under "A proposed wording for the canonical block"; live run of AC1's exact check command against the drafted file before and after the edit (before: `grep -qF -- 'measured values' "$P"` exit 1 against the unmodified proposed draft; after: exit 0).

- decision: added the retro template's external-run form as two brand-new, explicitly conditional lines — `**Cycle scope, external run**` and `**Pull requests, external run**` — placed immediately after the existing `**Generated by**` line, rather than editing the existing `**Cycle scope**` / `**Pull requests**` lines in place or making the new lines mandatory on every retro.
  reason: AC6 requires the region between `**Cycle scope**` and `## Retro inputs` to gain the two fixed strings while asserting the five section markers and the nine `- input:` ledger lines stay untouched, and the spec's own frozen intent block (via AC3's sibling zero-deletion requirement for the six role files and the Notes-for-engineer guidance "an addition to the template, never a rewrite of the structure the linter validates") makes a pure, additive line the only compatible shape for the six role-file edits — extending the same discipline to the template kept one authoring convention rather than two. Making the two new lines explicitly conditional ("only when a bullet in this cycle relays a run that happened outside this repository") avoids obligating every future retro-writing agent to fill in two lines that have nothing to say on an ordinary, non-relaying cycle, which the spec's Goal text does not require and Non-goals' authoring-time-discipline framing would not support turning into a mandatory field.
  grounding: `.shell-team/specs/T-1141-record-portability-block.md` **AC6** ("the region ... contains the fixed strings `the adopter repository` and `orchestrator record`; each of the five section markers still occurs exactly once; and the template's `- input:` ledger lines are byte-identical") and its `## Notes for engineer` ("Adding two lines beside the existing cycle-scope and pull-request lines is the least invasive shape"); `docs/templates/retro-template.md`'s own pre-existing migration note above the `**Cycle scope**` line, describing markers as "a pure, content-free addition" — the same additive framing applied here to prose lines; live run of AC6's exact `check:` command against the edited file, exit 0.

- decision: placed the relay-boundary paragraph in `agents/scrum-master.md`'s `## Inputs you read` as a new, unnumbered paragraph immediately after numbered item 6 (Interventions) and before the closing "If `retro-inputs.sh` itself is unavailable" fallback sentence, rather than renumbering the list to insert it as item 7, appending it after the whole section, or folding it into an existing numbered item.
  reason: AC3 asserts the six role files' committed delta against the branch point deletes no line; renumbering item 6's neighbours to insert a new item 7 would rewrite every subsequent list-item line even though their text is unchanged, and folding the boundary into an existing item (e.g. item 6's own Interventions text) would conflate two unrelated inputs' contracts in one paragraph. An unnumbered paragraph is a pure addition that reads as a continuation of "the numbered usage notes" the spec's `## Notes for engineer` names, and sits before the section's own closing fallback sentence rather than after the section ends, keeping it inside the region AC5's `check:` line extracts (`awk` from the `## Inputs you read` heading to the next `## ` heading).
  grounding: `.shell-team/specs/T-1141-record-portability-block.md` **AC3** ("each of the six role files' committed delta against the branch point deletes no line") and **AC5** ("the text between that heading and the next `## ` heading contains ..."); `## Notes for engineer`'s placement guidance ("as an added paragraph or bullet after the numbered usage notes ... not as a rewrite of an existing numbered item, which would delete a line and red AC3"); live run of AC3's and AC5's exact `check:` commands against the edited file, both exit 0.

- decision: chose the tracker-key positive control's restored named reference to be the literal text `reference AB-123 filed` (a two-letter uppercase namespace, a hyphen, and digits, in a short sentence), mirroring `tests/check-pii-shapes/run.sh`'s pre-existing `TK_KEY_POS_LINE="reference ${TK_KEY_NS}-${TK_KEY_NUM} filed"` convention, rather than inventing a differently-shaped positive fixture.
  reason: the file already carries an established, reviewed convention for a minimal tracker-key-shaped positive fixture (the same shape T-1140's own POS/NEG pair uses); reusing it rather than inventing a new shape keeps the new retro-fixture positive control obviously equivalent in kind to the suite's other tracker-key positives, lowering the review surface for "is this really the shape the rule targets" to a single, already-settled precedent instead of a second one to independently re-justify.
  grounding: `tests/check-pii-shapes/run.sh`'s pre-existing `TK_KEY_POS_LINE="reference ${TK_KEY_NS}-${TK_KEY_NUM} filed"` assignment and its paired `assert_finding "POS/NEG pair: tracker-key ..." "tracker-key" ...` call, read first-hand before choosing the new fixture's text; mutation self-check performed during implementation (removing the restored line from the positive-control fixture makes its own `assert_finding` call fail, confirming the fixture path genuinely depends on the restored shape).

<!-- END provenance: T-1141 -->
