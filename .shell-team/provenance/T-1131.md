<!-- BEGIN provenance: T-1131 -->

- decision: placed the new `- refreeze-ratifier:` family arm in `bin/check-entry-mode.sh` immediately after the two-source (`- entry-mode:` vs `- dispatch: specify`) mismatch check and before the `- flagged-gap:` / `- dispatch-reflection:` sections, rather than at the very end of the file after the dispatch-reflection block.
  reason: `EM_VALUE` is already parsed and validated at that point, which the new arm's `pm-authored`-with-line refusal needs to compare against; placing it immediately after keeps that dependency local and readable, and keeps it before the gap/reflection sections so a reader encounters the four families in the same order the header comment now documents them.
  grounding: `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md` `## Notes for engineer` ("Put the whole arm after the two-source mismatch check at `:215`–`:217` and before the flagged-gap block, so its refusals read in a natural order; the arm must not disturb `ENTRY`, `EM_VALUE` or `D_VALUE`").

- decision: used a two-regex collect-wide/parse-strict pair (`rr_stem_re` for detection, `rr_full_re` for the strict grammar) rather than a single regex with optional-whitespace groups, and made the full grammar require the literal two-space board indent (`^  - refreeze-ratifier: (operator|authoring-session)$`) instead of `[[:space:]]*` for the indent.
  reason: this is the exact shape the file's own two prior repairs (the gap/resolution stem widening and the dispatch-reflection colon-spacing widening) already establish as the pattern for this recurring defect class, so reusing it rather than inventing a new shape keeps the file internally consistent. The literal two-space indent matches every existing family's fixture and production shape (`bd`/`mk` helpers in `tests/check-entry-mode/run.sh` always prefix sub-bullets with exactly two spaces, and real board entries — e.g. `.shell-team/todo.md`'s own `- entry-mode:` lines — carry the same two-space indent), so pinning it in the full grammar catches a doubled-space-after-dash variant without also having to special-case indentation depth.
  grounding: `bin/check-entry-mode.sh` comments at the gap/resolution stem (`T-1096 rework round 1, Blocker 2`) and the dispatch-reflection stem (`Codex review round 1, Major 4`); `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md` `## Notes for engineer` ("Write the new stem net the same way... and keep the full grammar strict"); `tests/check-entry-mode/run.sh`'s `bd()` helper (`printf '  %s\n' "$l"`).

- decision: wrote the run-skill's six-token sentence as an addition appended to the same physical line as the existing `drift-detected` bullet (line ~60 in `skills/run/SKILL.md`), rather than as a new separate bullet or a new paragraph on its own line.
  reason: AC4's check requires all six tokens (`refreeze-ratifier`, `drift-detected`, `authoring-session`, `operator-authored`, the anchor phrase, `authoring-session-declared`) to co-occur on one grep-matched line; the file's own style already carries very long single-line bullets, so extending the existing line matches the local shape and satisfies the mechanical check without inventing a new formatting convention. It also means both reachable paths (the `:50` attestation branch's routing into this same bullet, and this bullet itself) read the identical sentence with no duplication.
  grounding: `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md` Goal ("Both its `drift-detected` sites... are one rule"); AC4's check line (`grep -F ... | grep -F ...` piped chain, which only matches within one line); `## Notes for engineer` ("Place it in step 2's `drift-detected` bullet at `:60`... the file's existing bullets there are already long single lines, so this matches the local shape").

- decision: extended the existing T-1096 board-transcription paragraph in `agents/pm-spec.md` (appending a new sentence to the same paragraph) rather than adding a new top-level subsection for the producer duty.
  reason: the spec's droppable-second pre-commitment names this exact paragraph as the extension point, and the duty is a one-sentence addition to an existing rule about what `pm-spec` transcribes onto the board in the same fixed window — a new subsection would duplicate context (the fixed-window timing, the `check-entry-mode.sh` relationship) already stated there.
  grounding: `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md` `## Notes for engineer` ("Extend the T-1096 board-transcription paragraph at `:123`... reusing the file's own scoping idiom"); Pre-commitment ("Droppable, second — the `agents/pm-spec.md` producer-duty wording (AC5's first half)").

- decision: placed the English oversight-document addition as a new `###` subsection ("Which human the class-B GO is addressed to") directly after the existing class-M `### The class-M boundary is mechanical; the trigger is not` subsection and before the next `## ` heading, and mirrored the same relative placement in the Japanese file.
  reason: AC6 extracts the whole section from its own `## ` heading to the next `## ` heading and only requires the four tokens and both fixed phrases to appear somewhere inside that extracted text — no sub-heading position is asserted — so placing the new material as the section's last subsection keeps the shipped default sentence and the class-M worked example untouched above it, satisfying the Notes-for-engineer instruction to keep :107 "exactly as it is" and add prose after it.
  grounding: `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md` AC6's check line (awk extraction between the two named headings, no sub-heading position asserted); `## Notes for engineer` ("`docs/tuning-oversight.md`:107 stays exactly as it is; add prose after it").

<!-- END provenance: T-1131 -->
