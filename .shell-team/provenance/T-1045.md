<!-- BEGIN provenance: T-1045 -->

- decision: rework round 2 (mode-scoping Major) — scoped the version-transcription duty by appending a clause to the same Preconditions bullet, immediately after the frozen sentence, rather than adding a separate sentence inside the `## Finding-evaluation mode (review-response)` section.
  reason: keeping the scoping clause on the same physical line as the sentence it qualifies preserves the file's own existing precedent for exactly this kind of cross-mode scoping (the severity-grounding rule under `## Rules` scopes itself within its own paragraph rather than in a remote section), leaves the review-response mode's closed six-field-per-finding output contract completely untouched, and keeps the frozen sentence and its co-location partner on one line so the acceptance criteria's one-line, exactly-one-hit assertions continue to hold by construction rather than by coincidence; the alternative shape (a standalone sentence inside the review-response section stating the duty does not apply there) was also viable and was the round-1 reviewer's other suggested fix, but it would have split the scoping statement away from the obligation it qualifies, which is a farther reach for a reader auditing the Preconditions bullet in isolation.
  grounding: agents/codex-reviewer.md, the "Ground finding severity in the spec's input space (default / fresh-review mode only)" bullet under `## Rules` (the self-scoping precedent already present in this file); .shell-team/reviews/T-1045.md, round-1 Codex verdict, Major finding at `agents/codex-reviewer.md:18` (both suggested fix shapes).

<!-- END provenance: T-1045 -->
