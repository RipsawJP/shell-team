<!-- BEGIN provenance: T-1151 -->

- decision: The frozen refusal precedence places `declaration-malformed` before `declaration-misplaced`, and the `shipped-docs-*` family last — a candidate-set-then-precedence decision rule (build every applicable token into a set from the recorded facts, then refuse the first token the fixed precedence order finds present) rather than the carved-out generation's sequential early-exit checks (which checked misplaced before grammar).
  reason: The spec's `## Notes for engineer` freezes this exact order and states the `shipped-docs-*` family sits last "deliberately: it preserves every one of the salvage's 42 refusal verdicts unchanged." None of the 42 carried-over fixtures exercise a declaration that is simultaneously malformed and misplaced, so re-ordering those two relative to the salvage's own sequential-check order changes no existing verdict; implementing the decision as an explicit ordered-candidate-set walk (rather than reproducing the salvage's sequential `if`/`elif` shape) is what makes the frozen order auditable directly against the precedence list rather than reconstructable only by reading control flow.
  grounding: .shell-team/specs/T-1151-adopter-docs-checker.md — `## Notes for engineer`, the "Refusal precedence, frozen" paragraph, naming the exact 14-item order and stating the shipped-docs family's position is deliberate.

- decision: The declaration region `R` closes at the FIRST unfenced `^## ` heading after `BEGIN` — any heading, not specifically `## Non-goals` — and this window is used only for the declaration (`- user-visible:`) and `- shipped-docs:` families; `- adopter-docs-waiver:` and `- adopter-surface:` instead use the wider "strictly inside the intent block" window (between `BEGIN` and `END`), computed from the same marker positions the intent-block-missing gate already resolves, not from a fourth state variable.
  reason: AC4 requires a spec with no `## Non-goals` heading at all to still close the declaration region at whatever heading comes first, closing the carved-out generation's unbounded-fallback defect. The Goal text separately states the waiver/surface window is "the intent block" (T-1061's original wording), a strictly wider window than R — implementing both with a single R state variable would either wrongly narrow waiver/surface's discharge window to R's own closing heading, or wrongly widen the declaration's own misplaced check past the first heading. Keeping the three state variables at exactly F, R, S (as the Goal insists) while still giving waiver/surface their own, wider window is achieved by reusing the begin_ln/end_ln line positions the intent-block-missing pre-pass already computes, rather than inventing a fourth tracked variable.
  grounding: .shell-team/specs/T-1151-adopter-docs-checker.md — Goal, "R tracks the declaration region ... closing at the first `^## ` heading after it" and the following sentence "the waiver must be unindented and inside the intent block ... the surface must be indented, inside the intent block, and under a real AC bullet"; AC4's check line and prose.

- decision: The `- shipped-docs:` separator uses the same em-dash-split-on-first-occurrence-then-trim rule as `- user-visible:`, rather than a stricter literal-` — `-with-exactly-one-space rule.
  reason: The spec's `## Notes for engineer` states this explicitly as issue #250 requirement 6, grounded in avoiding two different separator rules inside one canonical grammar file.
  grounding: .shell-team/specs/T-1151-adopter-docs-checker.md — `## Notes for engineer`, the "Separator rule, decided deliberately (issue #250 requirement 6)" paragraph.

- decision: `- adopter-docs-waiver:`'s valid window is the whole intent block (between `BEGIN` and `END`), not narrowed to the declaration region `R`, per the spec's own frozen `## Open questions` resolution.
  reason: The brief's own token map and its fixed seven-positional-requirements inventory disagreed on this point; narrowing to `R` would narrow a positional requirement T-1061 already froze in its Goal ("the spec carries a top-level ... line"), which would be a behaviour change to already-merged frozen intent rather than a design choice open to this task. AC5's p3/p4 probes (waiver indented, and waiver after `END`) are written to this reading.
  grounding: .shell-team/specs/T-1151-adopter-docs-checker.md — `## Open questions`, item 1, and AC5's check-line probes p3/p4.

- decision: `tests/check-adopter-docs/run.sh`'s case inventory is derived one-way from the checker's own transition table (documented as a comment block at the top of `bin/check-adopter-docs.sh`): every row of that table is exercised by at least one case (the `cad-region-*` pair for R's first-heading-not-Non-goals closure, the `cad-control-pair-*` pair and the distinct `cad-fence-scope-survives` case for S's identity transition under a fenced line, `cad-shipped-*` for each of the four new tokens, `cad-nofs-*` for the no-filesystem-access property), and no case was invented outside what a table row licenses.
  reason: The spec's `## Reading-judgment sweep round` declines to make this inventory itself mechanically checkable (a fixed vocabulary it has no authority to close) and instead directs it to the verifying roles' reading judgment, naming round 1 as the round the full sweep runs in per this task's own briefing.
  grounding: .shell-team/specs/T-1151-adopter-docs-checker.md — `## Reading-judgment sweep round`; `bin/check-adopter-docs.sh`'s own header comment, the "THE THREE-VARIABLE STATE MACHINE" transition tables for F, R and S; `tests/check-adopter-docs/run.sh`'s header comment listing each case class against the property it probes.

- decision: The round-3 review fix (class `positional-boundary-unbounded-at-END`) adds `i < end_ln` as a SECOND positional clause, checked at decision time alongside `R == 1`, for the declaration and shipped-docs misplaced determinations — rather than gating the RECORDING of `DECL_POS`/`SHIP_POS` itself by the same bound (which would mirror `WAIVER_RE`/`SURFACE_RE`'s own shape more literally, since those gate at record time).
  reason: Gating recording would make an out-of-block occurrence (before BEGIN, after the first heading, or now at/after END) invisible rather than counted, which would silently break declaration-duplicate detection for the exact "one legit declaration inside plus one sneaky one outside" scenario the reviewer's own analysis names as already-working protection ("declaration-duplicate suppression... every R-recorded declaration is still counted"). Recording every unindented, unfenced occurrence anywhere in the file — regardless of position — and only gating the WELL-PLACED verdict by position preserves that existing missing/duplicate/malformed/misplaced precedence exactly as documented ("a well-formed occurrence anywhere else in the file is declaration-misplaced... because 'you put it in the wrong place' is a different repair from 'you forgot it'"), while still closing the reported gap: a sole occurrence at R==1 but at-or-after `end_ln` now fails the second clause and is correctly `declaration-misplaced` (or `shipped-docs-misplaced`) rather than accepted as well-placed. Verified live: a single sneaky declaration/shipped-docs line after END is misplaced; a legit-inside-plus-sneaky-outside pair is `declaration-duplicate`, not silently collapsed to the legit one.
  grounding: .shell-team/reviews/T-1151.md — Round 1's Major finding and its "Suggested fix" paragraph (naming the `begin_ln`/`end_ln` bound WAIVER_RE/SURFACE_RE already use as the pattern to extend) and its own live-reproduced note that duplicate-suppression already counts every R-recorded occurrence; `bin/check-adopter-docs.sh`'s Exit-codes doc comment, the `declaration-misplaced (1)` entry's own "a different repair from 'you forgot it'" wording (pre-existing, unchanged by this fix).

<!-- END provenance: T-1151 -->
