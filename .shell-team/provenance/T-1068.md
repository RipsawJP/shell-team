# T-1068 decision provenance

## Blast radius — read-set derivation (live re-run by this role)

- read-set-docs-skill-log: 54 specs
  - detail: live re-derivation `git grep -lF -e docs/loop-engineering -e skills/run/SKILL.md -e bin/log-run.sh -- '.shell-team/specs/*.md' | wc -l` = 54 at report time (includes this task's own spec, which names all three literals in its own text).
- indirection-class: `team-paths.sh --get` — a criterion reaching its target through that resolver (`--get todo`, `--get runs`, `--get reviews`, `--get interventions`, `--get provenance`) rather than a literal path is invisible to the derivation above in principle. Discharge: additionally ran `git grep -lF -e 'team-paths.sh --get' -- '.shell-team/specs/*.md' | wc -l` = 62. Union of both derivations: 66 unique files (`LC_ALL=C sort -u` over both lists).
  - Inside that 66-file union, searched for every criterion whose subject is a population count over `.shell-team/specs/*.md` (compared to a fixed literal, not a base-relative delta or a pinned-ref re-derivation), since this task's own diff adds exactly one new spec file and one new board entry — the only two populations this task's diff can move. Found `T-1024-check-line-mktemp-guard.md` AC2 (pinned to ref `03e6250`'s own committed blobs, never the working tree — unaffected by this task, which lands on a later ref entirely) and `T-1032-audit-prose-accuracy.md` AC5 (a live re-derivation of *which spec file holds the maximum* intent-block `- check:` count, currently `T-1011` at 35; this task's own spec carries 10 acceptance criteria, far under 35, so adding it cannot change the maximum-holder) — both confirmed non-vulnerable by direct measurement below, not merely reasoned about.
    - `T-1024` AC2's own DP1: `git show 03e6250:<audit-doc>` and a fixed historical ref, quoted verbatim in that spec's own frozen text — confirmed unaffected because this task's spec is not part of that ref's tree.
    - `T-1032` AC5 re-run live at HEAD (after this task's spec and board landed): `for s in .shell-team/specs/*.md; do n=$(awk '/BEGIN intent-block/{inb=1;next} /END intent-block/{inb=0} inb' "$s" | grep -c '^[[:space:]]*- check:'); printf '%s %s\n' "$n" "$s"; done | sort -rn | head -1` → `35 .shell-team/specs/T-1011-telemetry-event-rows.md` (unchanged; this task's own spec carries 10 `- check:` lines inside its own intent block, confirmed with the same command filtered to `T-1068`: `10 .shell-team/specs/T-1068-agent-concurrency.md`).
  - Three population-count-over-specs criteria genuinely vulnerable to this task's own diff were already pre-declared in this spec's own `## Blast radius` (T-1065 AC8, T-1066 AC12) and board-delta criteria (T-1066 AC10, T-1067 AC10) — measured below under "Disclosed reds," not re-discovered here.
  - Whatever a criterion reaches through an indirection neither literal-string derivation can see (a hardcoded path fragment built from string concatenation, for instance) is disclosed here as unmeasured, not claimed clean.

## Disclosed reds (DP5 — measured, differenced, never repaired)

Three classes were pre-declared in this spec's own `## Blast radius`; all three confirmed exactly as predicted at this task's own base ref (`29c35d97ed1c9468cc82c38598f75f4be1d842ab`, `git merge-base feature/1067-context-lifecycle HEAD`) and at HEAD (this task's diff landed), using a disposable `git worktree add --detach` at the base ref (never the working tree) so the base-side reading is stable. None is repaired — no other spec is in this task's own AC9 allow-list.

- class (i) — T-1067's own pre-declared, self-acknowledged merge-point staleness:
  - `.shell-team/specs/T-1067-context-lifecycle.md` AC9 (scope lock): `base: PASS (exit 0) → head: FAIL (exit 1)` — this task's own diff (a new note, spec, board entry, records) is exactly the "later task's files land on the same base ref" event that spec's own frozen text anticipates.
  - `.shell-team/specs/T-1067-context-lifecycle.md` AC10 (board-delta, `+1` expected relative to T-1067's own base ref `3d72e5b`): `base: PASS (exit 0) → head: FAIL (exit 1)` — the board now carries `+2` relative to that ref (T-1067's own entry plus this task's), not `+1`.
- class (ii) — T-1066's own pre-declared scope lock and board delta, plus a population count over `.shell-team/specs/*.md`:
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC9 (scope lock): `base: FAIL (exit 1) → head: FAIL (exit 1)` — already stale before this task (T-1067's own files already violated its allow-list at this task's own base ref); unaffected in kind by this task's own diff.
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC10 (board-delta): `base: FAIL (exit 1) → head: FAIL (exit 1)` — already off before this task (the board already carries more than one entry past T-1066's own base ref); this task's own new board line adds one more entry to an already-broken delta, not the first break.
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC12 (population count over `.shell-team/specs/*.md` inside its own provenance record): `base: FAIL (exit 1) → head: FAIL (exit 1)` — already stale before this task (T-1067's own spec file already moved the population past T-1066's own declared figure); this task's own spec adds one further unit to an already-stale count.
- class (iii) — T-1066's own criterion already red at this branch point for a reason predating both T-1067's and this task's own diff:
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC4 (a live equality between a declared `- population: total-rows = 1298` figure and a fresh re-derivation of the resolved, ever-growing `shell-team.jsonl`): `base: FAIL (exit 1) → head: FAIL (exit 1)` — that spec's own self-measurement-exclusion design (excluding only `t1066`-tagged rows) cannot stay satisfied once any later task runs the loop at all, with or without a diff landing anywhere; attributed to nothing this task's own diff changed. Same structural fragility T-1067's own provenance record already disclosed for this same criterion.
- class (iv), pre-declared for T-1065 (already red at T-1067's own base ref, per that task's own provenance record, and confirmed unchanged here):
  - `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC8 (population count over `.shell-team/specs/*.md`): `base: FAIL (exit 1) → head: FAIL (exit 1)`.
  - `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC10 (its own pre-declared merge-point-scoped scope lock): `base: FAIL (exit 1) → head: FAIL (exit 1)`.

## Mutation-probes (the 10 check lines this task adds)

Run in a disposable `git worktree add --detach "$WT" HEAD` (never the working tree), mutating a scratch copy of `docs/loop-engineering/agent-concurrency.md` (and, for AC10, of the resolved board) inside that worktree, running `bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` from inside the worktree, then restoring the pre-mutation baseline before the next probe. AC1–AC9 were run in a worktree created after this task's note and spec-fix commits landed but before the provenance/board commits; AC10 (which needs `check-provenance.sh`/`check-interventions.sh` to find a real provenance file) was re-run in a second worktree created after every commit below landed.

- mutation-probe: AC1 — deleted the `## Terms and closed vocabularies` heading line from the scratch copy → `AC1: FAIL (exit 1)`; restored → `AC1: PASS (exit 0)`.
- mutation-probe: AC2 — renamed one `- candidate: intertask-pipelining —` line to `intertask-pipeliningX`, breaking the candidate/saving id-set equality → `AC2: FAIL (exit 1)`; restored → `AC2: PASS (exit 0)`.
- mutation-probe: AC3 — deleted the `- contention: telemetry-seq — ` line, breaking the closed six-id inventory → `AC3: FAIL (exit 1)`; restored → `AC3: PASS (exit 0)`.
- mutation-probe: AC4 — corrupted one letter of the exact-lock phrase (`whole working tree` → `whole workin tree`) → `AC4: FAIL (exit 1)`; restored → `AC4: PASS (exit 0)`.
- mutation-probe: AC5 — injected `git worktree add /tmp/x HEAD;` into the probe block's own first `- command:` line, the venue DP1 forbids → `AC5: FAIL (exit 1)`; restored → `AC5: PASS (exit 0)`.
- mutation-probe: AC6 — duplicated the `- recommendation: ` line (violating the exactly-once count) → `AC6: FAIL (exit 1)`; restored → `AC6: PASS (exit 0)`.
- mutation-probe: AC7 — appended a bare `- note: leaning on external-cited practice here with no id` line inside `## Recommendation and follow-ups` (a non-excluded region), violating the traceability requirement that every `external-cited` occurrence outside the two excluded sections names a declared prior-art id → `AC7: FAIL (exit 1)`; restored → `AC7: PASS (exit 0)`.
- mutation-probe: AC8 — appended a home-directory-shaped path (the placeholder shape `/Users/<name>/scratch`, a real name segment substituted in at run time, never transcribed literally here per this repository's own PII-shape checker convention) inside `## Limits and what is not computable` → `AC8: FAIL (exit 1)`; restored → `AC8: PASS (exit 0)`.
- mutation-probe: AC9 — added an untracked stray file (`stray-t1068.txt`) at the worktree root, outside the allow-list → `AC9: FAIL (exit 1)`; removed it — restoring still read `AC9: FAIL (exit 1)` on the first re-check, traced to a leftover `docs/loop-engineering/agent-concurrency.md.bak` scratch file from the AC1–AC8 probes above (the same class of self-inflicted stray T-1067's own provenance record names); removed that too → `AC9: PASS (exit 0)`.
- mutation-probe: AC10 — deleted the top-level `- [ ] **T-1068**` board line from the scratch worktree's resolved board (leaving the delta at `+0` instead of `+1`) → `AC10: FAIL (exit 1)`; restored → `AC10: PASS (exit 0)`.

## Changed-input CI steps (individually, `.github/workflows/check-handoff.yml`)

This task's diff touches exactly: one new file under `docs/loop-engineering/`, this task's own spec, the resolved board, and this task's own provenance/interventions/reviews records. No path under `bin/`, `tests/`, `templates/`, `.github/`, `agents/`, `skills/`, or any other `docs/` file changes.

- ci-step: Checkout — not-applicable — infrastructure step, not a repository-content check.
- ci-step: Install shellcheck (pinned — must match local dev version) — not-applicable — tooling-install step.
- ci-step: shellcheck — not-applicable — no path under `bin/` or `tests/` changes.
- ci-step: Lint the shipped board template (hand-off linter) — not-applicable — targets `templates/todo-template.md`, untouched.
- ci-step: Dogfood check-handoff — this repository's own board — applicable
  - detail: this task edits the resolved board. Run live: `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` exits 0 (see hand-off for the run against the final board state).
- ci-step: Run check-handoff fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Lint the shipped shell-team loop contract — not-applicable — untouched.
- ci-step: Lint the shipped generic loop-contract template — not-applicable — untouched.
- ci-step: Lint the shipped goal loop contract — not-applicable — untouched.
- ci-step: Run check-contract fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run loop-guard fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-run fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run log-run resolution suite — not-applicable — fixture-only, untouched; `bin/log-run.sh` itself is read (never edited) by this task's own note.
- ci-step: Run team-init fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run discover-work fixture suite — not-applicable — fixture-only, untouched; `bin/discover-work.sh` itself is read (never edited) by this task's own note.
- ci-step: Run check-acs fixture suite — not-applicable — fixture-only suite under `tests/check-acs`; this task's own spec is exercised directly via `bash bin/check-acs.sh`, not through this suite.
- ci-step: Run check-design-note fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run goal-state fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rework-digest fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-retro fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs bounded invariants lock — not-applicable — fixture-only, untouched.
- ci-step: Dogfood retro-inputs — the cycle window resolves against this repository — not-applicable — this task adds no retro file and does not touch `.shell-team/retros/`.
- ci-step: Dogfood check-retro — this repository's own retros pass the ledger contract — not-applicable — scans `.shell-team/retros/*.md`; untouched.
- ci-step: Dogfood check-retro — the shipped retro template passes its own contract — not-applicable — `docs/templates/retro-template.md`, untouched.
- ci-step: Run machine-tokens fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-loop-replay fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-readme-version fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-readme-version on the repo's READMEs — not-applicable — `README.md`/`README.ja.md`, untouched.
- ci-step: Run rollup-runs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rollup-track fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run consolidate-proposals fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run cluster-failures fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run is-span-row-parity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run review-gate fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run close-out fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-prompt-sync fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-prompt-sync — this repo's prompt blocks must be in sync — not-applicable — scans `templates/prompt-blocks/registry.txt` and its registered `agents/`/`skills/` consumers; none touched.
- ci-step: Run check-playbook fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-playbook-blocks fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run playbook-promote fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-playbook — the real repository corpus at the resolved lessons path is schema-valid — not-applicable — `.shell-team/lessons.md`, untouched.
- ci-step: Dogfood gen-playbook-blocks — regenerating into a scratch copy reproduces every shipped block and consumer — not-applicable — reads `templates/prompt-blocks/registry.txt` and `.shell-team/lessons.md`, both untouched.
- ci-step: Run team-paths fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood team-paths — a repo with no legacy markers resolves to the default layout — not-applicable — this task's diff does not touch any layout marker.
- ci-step: Run install fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-intent fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-provenance fixture suite — not-applicable — fixture-only suite under `tests/check-provenance`; this task's own provenance file is checked directly via `bash bin/check-provenance.sh .shell-team/provenance/T-1068.md`, outside this suite.
- ci-step: Run check-interventions fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run interventions-reminder fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-interventions — every committed interventions file in this repository is conformant — applicable
  - detail: loops `.shell-team/interventions/*.md`; this task's own `.shell-team/interventions/T-1068.md` (orchestrator-authored, untouched by this role, per this task's own briefing) is a member. Run live: `for f in .shell-team/interventions/*.md; do bash bin/check-interventions.sh "$f" || echo "FAIL $f"; done` — no `FAIL` line printed (see hand-off).
- ci-step: Run check-board-headings fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run errexit-safe regression suite — not-applicable — fixture-only, untouched.
- ci-step: Run codex-skeleton-hygiene suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-pii-shapes fixture suite — not-applicable — fixture-only, untouched.
- ci-step: check-pii-shapes on the PR diff — applicable
  - detail: scans this task's own PR diff. Run live: `bash bin/check-pii-shapes.sh --base "$(git merge-base feature/1067-context-lifecycle HEAD)"` → `clean (no PII-shaped bytes found)` (run again after every commit below; see hand-off for the final run).
- ci-step: Run check-commit-identity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gitignore-raw-dumps lock suite — not-applicable — fixture-only, untouched.
- ci-step: check-commit-identity on the PR commits — applicable
  - detail: scans this task's own PR commits. Run live: `bash bin/check-commit-identity.sh --base "$(git merge-base feature/1067-context-lifecycle HEAD)"` (see hand-off for the result against the final commit set).
- ci-step: Run check-refreeze-class fixture suite — not-applicable — fixture-only, untouched (this task performs no re-freeze of any other spec's intent block).
- ci-step: Run bin-exec-bit lock suite — not-applicable — fixture-only, untouched (no path under `bin/` added or edited).
- ci-step: Run check-durability fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-durability — this repository's own T-1048 implement-phase records — not-applicable — unrelated to T-1068.
- ci-step: Run check-binding fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-binding — the shipped binding-config specimen validates — not-applicable — `templates/binding-template.conf`, untouched.
- ci-step: Run check-adapter fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-adapter — the shipped contract and definitions validate — not-applicable — fixed adapter definitions, untouched.
- ci-step: Run check-liveness fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-liveness — the real classifier's --help — not-applicable — `bin/check-liveness.sh --help`, untouched.
- ci-step: Run resolve-executor fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood resolve-executor — probe-free mode against this repository (the runner carries neither executor CLI) — not-applicable — reads binding config, untouched.

<!-- BEGIN provenance: T-1068 -->
- decision: re-derived per-task `qa-verifier` and `codex-reviewer` wall-clock totals from the machine-local telemetry over a 12-task population (`T-1056`–`T-1067`), extending `docs/loop-engineering/effort-time-trend.md`'s own `P10-telemetry` (`T-1056`–`T-1065`) by the two tasks that landed since that note was written, rather than reusing its window unchanged or restricting to a smaller population.
  reason: `effort-time-trend.md` publishes no per-task `qa-verifier`/`codex-reviewer` wall-clock at all, which the Validate ∥ Review saving model needs; the most recent 12 tasks with a resolvable `run_id` is the largest population available at report time under the same inherited attribution rule, and including `T-1066`/`T-1067` (this task's own immediate predecessors, already closed) gives the saving model two additional, real data points rather than an artificially frozen window.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` Goal ("the per-phase durations the Validate ∥ Review saving model needs are re-derived from the machine-local telemetry using that note's own attribution and de-duplication rules") and Notes for engineer ("inherit `docs/loop-engineering/effort-time-trend.md`'s attribution rule ... rather than writing new ones"); measured live in `docs/loop-engineering/agent-concurrency.md`'s own `## Overlap candidates and expected saving` telemetry re-derivation block.
- decision: excluded `T-1059` from the 11-task aggregate sum (`min`/`max`/serial totals) feeding `validate-parallel-review`'s own saving figure, rather than folding its zero-valued row into the sum as a genuine zero-duration round.
  reason: `T-1059` carries no `engineer`, `qa-verifier` or `codex-reviewer` span at all in the analyzed telemetry corpus, despite a merged spec and a review record existing for that task — `effort-time-trend.md`'s own inherited bound states a missing row is evidence of nothing, so folding a real zero into a `min()`/sum aggregate would silently understate the reconcile-at-join and saving-cap figures for a task that in fact ran normally.
  grounding: `docs/loop-engineering/effort-time-trend.md` `## Limits and what is not computable` ("Telemetry logging is explicitly best-effort ... a missing row ... is not evidence that no work happened"); measured live — `T-1059`'s own three span counts are all `0` in this task's own telemetry re-derivation, matching that note's own `T-1059 NO-TELEMETRY`/`engineer_ms=0` finding for the same task.
- decision: labelled `validate-parallel-review`'s own saving `modeled` (from telemetry), not `measured`, even though this task's own pilot probe `ran` and produced a real wall-clock comparison (serial ≈1.805 s vs parallel ≈1.774 s).
  reason: the pilot's own two-reader shell-job proxy substitutes a few milliseconds of `grep` for what the real `qa-verifier`/`codex-reviewer` roles actually cost (several minutes to tens of minutes per task, per this task's own telemetry re-derivation) — labelling the candidate's saving `measured` from a proxy two to three orders of magnitude cheaper than the thing it stands in for would misrepresent DP2's own promise that `measured` means "observed by this task's own pilot" of the actual saving, not of a mechanical-safety demonstration.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP2 ("`measured` is a promise, not an adjective") and Notes for engineer ("If the probe is declined, every saving is `modeled` or `undetermined`... If the probe is declined" — generalized here to "if the probe's own result is not representative of the candidate's real magnitude"); measured live in `docs/loop-engineering/agent-concurrency.md`'s own `## Pilot probe` section (the disclosed representativeness gap) and `## Overlap candidates and expected saving` (`validate-parallel-review`'s own `- saving:` line, labelled `modeled`).
- decision: used the tip of `feature/1067-context-lifecycle` (`29c35d97ed1c9468cc82c38598f75f4be1d842ab` — this task's own base ref and T-1067's own closed, both-gates-green board entry) as the pilot probe's "already-merged task" target, rather than a task literally merged into `develop`.
  reason: no task in this sprint's own stacked train has yet merged into `develop` (the sprint runs on a one-time batch GO after every task's gates are green, per this checkout's own operating convention); T-1067's own branch tip is the closest available analogue to "a fixed committed tree" DP1 requires — a task whose spec is closed on the board, whose gates are both green, and whose branch will not move again before the batch GO — and using it keeps the probe's own venue a read-only target exactly as DP1 requires.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP1 ("The target is an already-merged task: a fixed committed tree"); measured live — `.shell-team/todo.md`'s own `## Done` section carries T-1067's entry with `READY_FOR_MERGE` and `closed: 2026-08-14, closes #249`; `git rev-parse feature/1067-context-lifecycle` = `29c35d97ed1c9468cc82c38598f75f4be1d842ab`, identical to this task's own resolved base ref.
- decision: reported the pilot probe's own two hazards (a sandbox `nice(5) failed` warning on every backgrounded job; four unrelated `check-acs.sh` failures in the detached clone that do not reproduce in the real checkout) rather than treating either as a probe defect to fix before reporting.
  reason: DP1 asks for hazards observed, not a probe engineered to avoid every friction point it encounters; both hazards are properties of the *venue* (a sandboxed shell, a detached clone lacking full branch context) rather than of the candidate design itself, and disclosing them lets a future reader tell "the harness restricts backgrounded process priority" and "a detached clone is not branch-context-complete" apart from "the overlap design itself is unsafe."
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` `## Input space` ("Reachable failures include: the clone not being creatable, the harness declining to run two agents concurrently, a watchdog firing, one agent stalling ... Each is a hazard to report"); measured live in this task's own pilot run (`nice(5) failed: operation not permitted` printed on both backgrounded jobs; `AC1 AC8 AC9 AC10` reported `FAILED` by `check-acs.sh` inside the detached clone against T-1067's own spec).
- decision: classified `shared-git-index` as `serialize` (a single verdict covering both the read-only and the write-concurrent case), rather than splitting it into two separate contention-inventory lines for reads versus writes.
  reason: the closed six-id inventory (DP3) fixes exactly one line per id; the prose `<reason>` field is where the read/write distinction belongs, and the verdict itself has to describe the harder case — write-concurrency, which has no safe mechanism today — since a reader relying on the softer `partition-safe` half of the story for `validate-parallel-review` specifically would otherwise miss that `intertask-pipelining`'s own writers are unsafe under the same id.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP3 ("the closed inventory `board` / `test-recipe` / `lessons-corpus` / `telemetry-seq` / `shared-git-index` / `per-task-records`" — one line each); measured live — `skills/run/SKILL.md`'s own quoted "integrating 2+ concurrent worktrees into one branch is NOT covered" is the write-concurrency evidence this decision's verdict rests on, present in `docs/loop-engineering/agent-concurrency.md`'s own `shared-git-index` line and its `## Isolation and reconcile` section.
- decision: reached `staged-adoption` (not `not-yet`) as this task's single recommendation, gated explicitly on two named preconditions (a `bin/log-run.sh` lock; empirical confirmation of real Agent-tool concurrency) rather than treating either open item as a reason to recommend `not-yet` outright.
  reason: `validate-parallel-review`'s own structural safety is real and independently established (a fixed, already-committed commit; two disjoint write targets; a positive, non-trivial modeled saving of 33.4%) even though two preconditions remain unmet — `staged-adoption` names the gate explicitly rather than either overstating readiness (a bare `staged-adoption` with no precondition) or understating it (`not-yet`, which would discard a real, already-safe-by-construction candidate over an open question this task's own pilot could not answer, not one it found unsafe).
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP3 (`- recommendation: staged-adoption|not-yet — <…>`, a two-value grammar) and issue #252's own "suggested acceptance" ("a staged adoption recommendation or a reasoned 'not worth it yet' with the blocker named"); the recommendation line and its named preconditions are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Recommendation and follow-ups`.
<!-- END provenance: T-1068 -->
