# T-1068 decision provenance

## Blast radius — read-set derivation (live re-run by this role)

- read-set-docs-skill-log: 54 specs
  - detail: live re-derivation `git grep -lF -e docs/loop-engineering -e skills/run/SKILL.md -e bin/log-run.sh -- '.shell-team/specs/*.md' | wc -l` = 54 at report time (includes this task's own spec, which names all three literals in its own text).
- indirection-class: `team-paths.sh --get` — a criterion reaching its target through that resolver (`--get todo`, `--get runs`, `--get reviews`, `--get interventions`, `--get provenance`) rather than a literal path is invisible to the derivation above in principle. Discharge: additionally ran `git grep -lF -e 'team-paths.sh --get' -- '.shell-team/specs/*.md' | wc -l` = 62. Union of both derivations: 66 unique files (`LC_ALL=C sort -u` over both lists).
  - Inside that 66-file union, searched for every criterion whose subject is a population count over `.shell-team/specs/*.md` (compared to a fixed literal, not a base-relative delta or a pinned-ref re-derivation), since this task's own diff adds exactly one new spec file and one new board entry — the only two populations this task's diff can move. Found `T-1024-check-line-mktemp-guard.md` AC2 (pinned to ref `03e6250`'s own committed blobs, never the working tree — unaffected by this task, which lands on a later ref entirely) and `T-1032-audit-prose-accuracy.md` AC5 (a live re-derivation of *which spec file holds the maximum* intent-block `- check:` count, currently `T-1011` at 35; this task's own spec carries 10 acceptance criteria, far under 35, so adding it cannot change the maximum-holder) — both confirmed non-vulnerable by direct measurement below, not merely reasoned about.
    - `T-1024` AC2's own DP1: `git show 03e6250:<audit-doc>` and a fixed historical ref, quoted verbatim in that spec's own frozen text — confirmed unaffected because this task's spec is not part of that ref's tree.
    - `T-1032` AC5 re-run live at HEAD (after this task's spec and board landed): `for s in .shell-team/specs/*.md; do n=$(awk '/BEGIN intent-block/{inb=1;next} /END intent-block/{inb=0} inb' "$s" | grep -c '^[[:space:]]*- check:'); printf '%s %s\n' "$n" "$s"; done | sort -rn | head -1` → `35 .shell-team/specs/T-1011-telemetry-event-rows.md` (unchanged; this task's own spec carries 10 `- check:` lines inside its own intent block, confirmed with the same command filtered to `T-1068`: `10 .shell-team/specs/T-1068-agent-concurrency.md`).
  - Three population-count-over-specs criteria genuinely vulnerable to this task's own diff were already pre-declared in this spec's own `## Blast radius` (T-1065 AC8, T-1066 AC12) and board-delta criteria (T-1066 AC10, T-1067 AC10) — measured below under "Disclosed reds," not re-discovered here.
  - Whatever a criterion reaches through an indirection neither literal-string derivation can see (a hardcoded path fragment built from string concatenation, for instance) is disclosed here as unmeasured, not claimed clean.

## Disclosed reds (DP5 — measured, differenced, never repaired)

Three classes were pre-declared in this spec's own `## Blast radius`; all three confirmed exactly as predicted at this task's own base ref (`29c35d97ed1c9468cc82c38598f75f4be1d842ab`, `git merge-base feature/1067-context-lifecycle HEAD`) and at HEAD (this task's diff landed), using a disposable `git worktree add --detach` at the base ref (never the working tree) so the base-side reading is stable. None is repaired — no other spec is in this task's own AC9 allow-list.

- class (i) — T-1067's own pre-declared, self-acknowledged merge-point staleness:
  - `.shell-team/specs/T-1067-context-lifecycle.md` AC9 (scope lock): `base: PASS (exit 0) → head: FAIL (exit 1)` — this task's own diff (a new note, spec, board entry, records) is exactly the "later task's files land on the same base ref" event that spec's own frozen text anticipates.
  - `.shell-team/specs/T-1067-context-lifecycle.md` AC10 (board-delta, `+1` expected relative to T-1067's own base ref `3d72e5b`): `base: PASS (exit 0) → head: FAIL (exit 1)` — the board now carries `+2` relative to that ref (T-1067's own entry plus this task's), not `+1`.
- class (ii) — T-1066's own pre-declared scope lock and board delta, plus a population count over `.shell-team/specs/*.md`:
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC9 (scope lock): `base: FAIL (exit 1) → head: FAIL (exit 1)` — already stale before this task (T-1067's own files already violated its allow-list at this task's own base ref); unaffected in kind by this task's own diff.
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC10 (board-delta): `base: FAIL (exit 1) → head: FAIL (exit 1)` — already off before this task (the board already carries more than one entry past T-1066's own base ref); this task's own new board line adds one more entry to an already-broken delta, not the first break.
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC12 (population count over `.shell-team/specs/*.md` inside its own provenance record): `base: FAIL (exit 1) → head: FAIL (exit 1)` — already stale before this task (T-1067's own spec file already moved the population past T-1066's own declared figure); this task's own spec adds one further unit to an already-stale count.
- class (iii) — T-1066's own criterion already red at this branch point for a reason predating both T-1067's and this task's own diff:
  - `.shell-team/specs/T-1066-effort-time-telemetry.md` AC4 (a live equality between a declared `- population: total-rows = 1298` figure and a fresh re-derivation of the resolved, ever-growing `shell-team.jsonl`): `base: FAIL (exit 1) → head: FAIL (exit 1)` — that spec's own self-measurement-exclusion design (excluding only `t1066`-tagged rows) cannot stay satisfied once any later task runs the loop at all, with or without a diff landing anywhere; attributed to nothing this task's own diff changed. Same structural fragility T-1067's own provenance record already disclosed for this same criterion.
- class (iv), pre-declared for T-1065 (already red at T-1067's own base ref, per that task's own provenance record, and confirmed unchanged here):
  - `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC8 (population count over `.shell-team/specs/*.md`): `base: FAIL (exit 1) → head: FAIL (exit 1)`.
  - `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC10 (its own pre-declared merge-point-scoped scope lock): `base: FAIL (exit 1) → head: FAIL (exit 1)`.

## Mutation-probes (the 10 check lines this task adds)

Run in a disposable `git worktree add --detach "$WT" HEAD` (never the working tree), mutating a scratch copy of `docs/loop-engineering/agent-concurrency.md` (and, for AC10, of the resolved board) inside that worktree, running `bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` from inside the worktree, then restoring the pre-mutation baseline before the next probe. AC1–AC9 were run in a worktree created after this task's note and spec-fix commits landed but before the provenance/board commits; AC10 (which needs `check-provenance.sh`/`check-interventions.sh` to find a real provenance file) was re-run in a second worktree created after every commit below landed.

- mutation-probe: AC1 — deleted the `## Terms and closed vocabularies` heading line from the scratch copy → `AC1: FAIL (exit 1)`; restored → `AC1: PASS (exit 0)`.
- mutation-probe: AC2 — renamed one `- candidate: intertask-pipelining —` line to `intertask-pipeliningX`, breaking the candidate/saving id-set equality → `AC2: FAIL (exit 1)`; restored → `AC2: PASS (exit 0)`.
- mutation-probe: AC3 — deleted the `- contention: telemetry-seq — ` line, breaking the closed six-id inventory → `AC3: FAIL (exit 1)`; restored → `AC3: PASS (exit 0)`.
- mutation-probe: AC4 — corrupted one letter of the exact-lock phrase (`whole working tree` → `whole workin tree`) → `AC4: FAIL (exit 1)`; restored → `AC4: PASS (exit 0)`.
- mutation-probe: AC5 — injected `git worktree add /tmp/x HEAD;` into the probe block's own first `- command:` line, the venue DP1 forbids → `AC5: FAIL (exit 1)`; restored → `AC5: PASS (exit 0)`.
- mutation-probe: AC6 — duplicated the `- recommendation: ` line (violating the exactly-once count) → `AC6: FAIL (exit 1)`; restored → `AC6: PASS (exit 0)`.
- mutation-probe: AC7 — appended a bare `- note: leaning on external-cited practice here with no id` line inside `## Recommendation and follow-ups` (a non-excluded region), violating the traceability requirement that every `external-cited` occurrence outside the two excluded sections names a declared prior-art id → `AC7: FAIL (exit 1)`; restored → `AC7: PASS (exit 0)`.
- mutation-probe: AC8 — appended a home-directory-shaped path (the placeholder shape `/Users/<name>/scratch`, a real name segment substituted in at run time, never transcribed literally here per this repository's own PII-shape checker convention) inside `## Limits and what is not computable` → `AC8: FAIL (exit 1)`; restored → `AC8: PASS (exit 0)`.
- mutation-probe: AC9 — added an untracked stray file (`stray-t1068.txt`) at the worktree root, outside the allow-list → `AC9: FAIL (exit 1)`; removed it — restoring still read `AC9: FAIL (exit 1)` on the first re-check, traced to a leftover `docs/loop-engineering/agent-concurrency.md.bak` scratch file from the AC1–AC8 probes above (the same class of self-inflicted stray T-1067's own provenance record names); removed that too → `AC9: PASS (exit 0)`.
- mutation-probe: AC10 — deleted the top-level `- [ ] **T-1068**` board line from the scratch worktree's resolved board (leaving the delta at `+0` instead of `+1`) → `AC10: FAIL (exit 1)`; restored → `AC10: PASS (exit 0)`.

## Changed-input CI steps (individually, `.github/workflows/check-handoff.yml`)

This task's diff touches exactly: one new file under `docs/loop-engineering/`, this task's own spec, the resolved board, and this task's own provenance/interventions/reviews records. No path under `bin/`, `tests/`, `templates/`, `.github/`, `agents/`, `skills/`, or any other `docs/` file changes.

- ci-step: Checkout — not-applicable — infrastructure step, not a repository-content check.
- ci-step: Install shellcheck (pinned — must match local dev version) — not-applicable — tooling-install step.
- ci-step: shellcheck — not-applicable — no path under `bin/` or `tests/` changes.
- ci-step: Lint the shipped board template (hand-off linter) — not-applicable — targets `templates/todo-template.md`, untouched.
- ci-step: Dogfood check-handoff — this repository's own board — applicable
  - detail: this task edits the resolved board. Run live: `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` exits 0 (see hand-off for the run against the final board state).
- ci-step: Run check-handoff fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Lint the shipped shell-team loop contract — not-applicable — untouched.
- ci-step: Lint the shipped generic loop-contract template — not-applicable — untouched.
- ci-step: Lint the shipped goal loop contract — not-applicable — untouched.
- ci-step: Run check-contract fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run loop-guard fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-run fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run log-run resolution suite — not-applicable — fixture-only, untouched; `bin/log-run.sh` itself is read (never edited) by this task's own note.
- ci-step: Run team-init fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run discover-work fixture suite — not-applicable — fixture-only, untouched; `bin/discover-work.sh` itself is read (never edited) by this task's own note.
- ci-step: Run check-acs fixture suite — not-applicable — fixture-only suite under `tests/check-acs`; this task's own spec is exercised directly via `bash bin/check-acs.sh`, not through this suite.
- ci-step: Run check-design-note fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run goal-state fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rework-digest fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-retro fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run retro-inputs bounded invariants lock — not-applicable — fixture-only, untouched.
- ci-step: Dogfood retro-inputs — the cycle window resolves against this repository — not-applicable — this task adds no retro file and does not touch `.shell-team/retros/`.
- ci-step: Dogfood check-retro — this repository's own retros pass the ledger contract — not-applicable — scans `.shell-team/retros/*.md`; untouched.
- ci-step: Dogfood check-retro — the shipped retro template passes its own contract — not-applicable — `docs/templates/retro-template.md`, untouched.
- ci-step: Run machine-tokens fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-loop-replay fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-readme-version fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-readme-version on the repo's READMEs — not-applicable — `README.md`/`README.ja.md`, untouched.
- ci-step: Run rollup-runs fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run rollup-track fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run consolidate-proposals fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run cluster-failures fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run is-span-row-parity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run review-gate fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run close-out fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-prompt-sync fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-prompt-sync — this repo's prompt blocks must be in sync — not-applicable — scans `templates/prompt-blocks/registry.txt` and its registered `agents/`/`skills/` consumers; none touched.
- ci-step: Run check-playbook fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gen-playbook-blocks fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run playbook-promote fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-playbook — the real repository corpus at the resolved lessons path is schema-valid — not-applicable — `.shell-team/lessons.md`, untouched.
- ci-step: Dogfood gen-playbook-blocks — regenerating into a scratch copy reproduces every shipped block and consumer — not-applicable — reads `templates/prompt-blocks/registry.txt` and `.shell-team/lessons.md`, both untouched.
- ci-step: Run team-paths fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood team-paths — a repo with no legacy markers resolves to the default layout — not-applicable — this task's diff does not touch any layout marker.
- ci-step: Run install fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-intent fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-provenance fixture suite — not-applicable — fixture-only suite under `tests/check-provenance`; this task's own provenance file is checked directly via `bash bin/check-provenance.sh .shell-team/provenance/T-1068.md`, outside this suite.
- ci-step: Run check-interventions fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run interventions-reminder fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-interventions — every committed interventions file in this repository is conformant — applicable
  - detail: loops `.shell-team/interventions/*.md`; this task's own `.shell-team/interventions/T-1068.md` (orchestrator-authored, untouched by this role, per this task's own briefing) is a member. Run live: `for f in .shell-team/interventions/*.md; do bash bin/check-interventions.sh "$f" || echo "FAIL $f"; done` — no `FAIL` line printed (see hand-off).
- ci-step: Run check-board-headings fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run errexit-safe regression suite — not-applicable — fixture-only, untouched.
- ci-step: Run codex-skeleton-hygiene suite — not-applicable — fixture-only, untouched.
- ci-step: Run check-pii-shapes fixture suite — not-applicable — fixture-only, untouched.
- ci-step: check-pii-shapes on the PR diff — applicable
  - detail: scans this task's own PR diff. Run live: `bash bin/check-pii-shapes.sh --base "$(git merge-base feature/1067-context-lifecycle HEAD)"` → `clean (no PII-shaped bytes found)` (run again after every commit below; see hand-off for the final run).
- ci-step: Run check-commit-identity fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Run gitignore-raw-dumps lock suite — not-applicable — fixture-only, untouched.
- ci-step: check-commit-identity on the PR commits — applicable
  - detail: scans this task's own PR commits. Run live: `bash bin/check-commit-identity.sh --base "$(git merge-base feature/1067-context-lifecycle HEAD)"` (see hand-off for the result against the final commit set).
- ci-step: Run check-refreeze-class fixture suite — not-applicable — fixture-only, untouched (this task performs no re-freeze of any other spec's intent block).
- ci-step: Run bin-exec-bit lock suite — not-applicable — fixture-only, untouched (no path under `bin/` added or edited).
- ci-step: Run check-durability fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-durability — this repository's own T-1048 implement-phase records — not-applicable — unrelated to T-1068.
- ci-step: Run check-binding fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-binding — the shipped binding-config specimen validates — not-applicable — `templates/binding-template.conf`, untouched.
- ci-step: Run check-adapter fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-adapter — the shipped contract and definitions validate — not-applicable — fixed adapter definitions, untouched.
- ci-step: Run check-liveness fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood check-liveness — the real classifier's --help — not-applicable — `bin/check-liveness.sh --help`, untouched.
- ci-step: Run resolve-executor fixture suite — not-applicable — fixture-only, untouched.
- ci-step: Dogfood resolve-executor — probe-free mode against this repository (the runner carries neither executor CLI) — not-applicable — reads binding config, untouched.

<!-- BEGIN provenance: T-1068 -->
- decision: re-derived per-task `qa-verifier` and `codex-reviewer` wall-clock totals from the machine-local telemetry over a 12-task population (`T-1056`–`T-1067`), extending `docs/loop-engineering/effort-time-trend.md`'s own `P10-telemetry` (`T-1056`–`T-1065`) by the two tasks that landed since that note was written, rather than reusing its window unchanged or restricting to a smaller population.
  reason: `effort-time-trend.md` publishes no per-task `qa-verifier`/`codex-reviewer` wall-clock at all, which the Validate ∥ Review saving model needs; the most recent 12 tasks with a resolvable `run_id` is the largest population available at report time under the same inherited attribution rule, and including `T-1066`/`T-1067` (this task's own immediate predecessors, already closed) gives the saving model two additional, real data points rather than an artificially frozen window.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` Goal ("the per-phase durations the Validate ∥ Review saving model needs are re-derived from the machine-local telemetry using that note's own attribution and de-duplication rules") and Notes for engineer ("inherit `docs/loop-engineering/effort-time-trend.md`'s attribution rule ... rather than writing new ones"); measured live in `docs/loop-engineering/agent-concurrency.md`'s own `## Overlap candidates and expected saving` telemetry re-derivation block.
- decision: excluded `T-1059` from the 11-task aggregate sum (`min`/`max`/serial totals) feeding `validate-parallel-review`'s own saving figure, rather than folding its zero-valued row into the sum as a genuine zero-duration round.
  reason: `T-1059` carries no `engineer`, `qa-verifier` or `codex-reviewer` span at all in the analyzed telemetry corpus, despite a merged spec and a review record existing for that task — `effort-time-trend.md`'s own inherited bound states a missing row is evidence of nothing, so folding a real zero into a `min()`/sum aggregate would silently understate the reconcile-at-join and saving-cap figures for a task that in fact ran normally.
  grounding: `docs/loop-engineering/effort-time-trend.md` `## Limits and what is not computable` ("Telemetry logging is explicitly best-effort ... a missing row ... is not evidence that no work happened"); measured live — `T-1059`'s own three span counts are all `0` in this task's own telemetry re-derivation, matching that note's own `T-1059 NO-TELEMETRY`/`engineer_ms=0` finding for the same task.
- decision: labelled `validate-parallel-review`'s own saving `modeled` (from telemetry), not `measured`, even though this task's own pilot probe `ran` and produced a real wall-clock comparison (serial ≈1.805 s vs parallel ≈1.774 s).
  reason: the pilot's own two-reader shell-job proxy substitutes a few milliseconds of `grep` for what the real `qa-verifier`/`codex-reviewer` roles actually cost (several minutes to tens of minutes per task, per this task's own telemetry re-derivation) — labelling the candidate's saving `measured` from a proxy two to three orders of magnitude cheaper than the thing it stands in for would misrepresent DP2's own promise that `measured` means "observed by this task's own pilot" of the actual saving, not of a mechanical-safety demonstration.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP2 ("`measured` is a promise, not an adjective") and Notes for engineer ("If the probe is declined, every saving is `modeled` or `undetermined`... If the probe is declined" — generalized here to "if the probe's own result is not representative of the candidate's real magnitude"); measured live in `docs/loop-engineering/agent-concurrency.md`'s own `## Pilot probe` section (the disclosed representativeness gap) and `## Overlap candidates and expected saving` (`validate-parallel-review`'s own `- saving:` line, labelled `modeled`).
- decision: used the tip of `feature/1067-context-lifecycle` (`29c35d97ed1c9468cc82c38598f75f4be1d842ab` — this task's own base ref and T-1067's own closed, both-gates-green board entry) as the pilot probe's "already-merged task" target, rather than a task literally merged into `develop`.
  reason: no task in this sprint's own stacked train has yet merged into `develop` (the sprint runs on a one-time batch GO after every task's gates are green, per this checkout's own operating convention); T-1067's own branch tip is the closest available analogue to "a fixed committed tree" DP1 requires — a task whose spec is closed on the board, whose gates are both green, and whose branch will not move again before the batch GO — and using it keeps the probe's own venue a read-only target exactly as DP1 requires.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP1 ("The target is an already-merged task: a fixed committed tree"); measured live — `.shell-team/todo.md`'s own `## Done` section carries T-1067's entry with `READY_FOR_MERGE` and `closed: 2026-08-14, closes #249`; `git rev-parse feature/1067-context-lifecycle` = `29c35d97ed1c9468cc82c38598f75f4be1d842ab`, identical to this task's own resolved base ref.
- decision: reported the pilot probe's own two hazards (a sandbox `nice(5) failed` warning on every backgrounded job; four unrelated `check-acs.sh` failures in the detached clone that do not reproduce in the real checkout) rather than treating either as a probe defect to fix before reporting.
  reason: DP1 asks for hazards observed, not a probe engineered to avoid every friction point it encounters; both hazards are properties of the *venue* (a sandboxed shell, a detached clone lacking full branch context) rather than of the candidate design itself, and disclosing them lets a future reader tell "the harness restricts backgrounded process priority" and "a detached clone is not branch-context-complete" apart from "the overlap design itself is unsafe."
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` `## Input space` ("Reachable failures include: the clone not being creatable, the harness declining to run two agents concurrently, a watchdog firing, one agent stalling ... Each is a hazard to report"); measured live in this task's own pilot run (`nice(5) failed: operation not permitted` printed on both backgrounded jobs; `AC1 AC8 AC9 AC10` reported `FAILED` by `check-acs.sh` inside the detached clone against T-1067's own spec).
- decision: classified `shared-git-index` as `serialize` (a single verdict covering both the read-only and the write-concurrent case), rather than splitting it into two separate contention-inventory lines for reads versus writes.
  reason: the closed six-id inventory (DP3) fixes exactly one line per id; the prose `<reason>` field is where the read/write distinction belongs, and the verdict itself has to describe the harder case — write-concurrency, which has no safe mechanism today — since a reader relying on the softer `partition-safe` half of the story for `validate-parallel-review` specifically would otherwise miss that `intertask-pipelining`'s own writers are unsafe under the same id.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP3 ("the closed inventory `board` / `test-recipe` / `lessons-corpus` / `telemetry-seq` / `shared-git-index` / `per-task-records`" — one line each); measured live — `skills/run/SKILL.md`'s own quoted "integrating 2+ concurrent worktrees into one branch is NOT covered" is the write-concurrency evidence this decision's verdict rests on, present in `docs/loop-engineering/agent-concurrency.md`'s own `shared-git-index` line and its `## Isolation and reconcile` section.
- decision: reached `staged-adoption` (not `not-yet`) as this task's single recommendation, gated explicitly on two named preconditions (a `bin/log-run.sh` lock; empirical confirmation of real Agent-tool concurrency) rather than treating either open item as a reason to recommend `not-yet` outright.
  reason: `validate-parallel-review`'s own structural safety is real and independently established (a fixed, already-committed commit; two disjoint write targets; a positive, non-trivial modeled saving of 33.4%) even though two preconditions remain unmet — `staged-adoption` names the gate explicitly rather than either overstating readiness (a bare `staged-adoption` with no precondition) or understating it (`not-yet`, which would discard a real, already-safe-by-construction candidate over an open question this task's own pilot could not answer, not one it found unsafe).
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP3 (`- recommendation: staged-adoption|not-yet — <…>`, a two-value grammar) and issue #252's own "suggested acceptance" ("a staged adoption recommendation or a reasoned 'not worth it yet' with the blocker named"); the recommendation line and its named preconditions are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Recommendation and follow-ups`.
- decision: rework round 1 (QA FAIL — the `board` contention row's "measured FALSE by two orders of magnitude" claim against issue #269's ~2min `check-handoff.sh` figure does not reproduce) — retracted the falsification claim and rewrote the row with both locale-conditioned figures, a root cause isolated by splicing, and a cross-reference to T-1063's own board record, rather than simply widening the claim's hedge word or lowering its confidence.
  reason: QA's own reproduction (14.95s/14.92s/14.99s at `LANG=en_US.UTF-8` vs this note's original 2.49s) and root-cause (`bin/check-handoff.sh`'s blank-check disproportionately slower under a non-`C` locale) demanded more than a softer adjective — my own independent re-derivation went further than QA's own finding and located the *specific* driver precisely: splicing this task's own `- engineer hand-off:` bullet (5032 bytes) alone into the round-1 board's own committed content (`git show 8c1158e:.shell-team/todo.md`) reproduces round-1's fast figure exactly when that bullet is absent, and reproduces a slow figure (≈14.87s) closely matching QA's own per-line isolation (≈12.3s) once it is added back — meaning round-1's own reading was not simply "locale-favorable" as QA's own message first hypothesized, but was measured against a *board state that did not yet contain this task's own longest hand-off line*. Recording this more precise mechanism (content-and-locale jointly responsible, not locale alone) is more useful to a future reader than either retracting to a bare "undetermined" or accepting QA's own hypothesis unmodified where a sharper, independently-verified explanation was available.
  grounding: this rework's own splicing experiment (`awk` inserting the exact committed bullet text into `git show 8c1158e:.shell-team/todo.md`'s output, timed at ≈14.87s vs the unmodified file's ≈2.49s, both under the same ambient `LANG=en_US.UTF-8`/`LC_ALL`-unset shell); QA's FAIL verdict on the board (`.shell-team/todo.md`, flag `READY_FOR_ENG`, commit `d30daef`) citing its own 14.95s/14.92s/14.99s and 2.08s (`LC_ALL=C`) reproduction and root cause; `.shell-team/todo.md`'s own T-1063 board entry (round-1 QA verdict, "re-run twice, ~33s each — not the ~2.5min of #256 this round") as the corroborating third data point; the corrected text is recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `board` contention row.
- decision: rework round 2 (Codex round-1 REQUEST_CHANGES, Blocker 1 — the `validate-parallel-review` reconcile-cost sub-bullet claimed the QA-fail rate "cannot be measured," which is false; `bin/log-run.sh` records `--verdict` per span) — derived the QA-fail rate myself from telemetry (task-level 27.3%, round-level 14.0%, matching Codex's own reproduction exactly) and rebuilt the saving figure using a **per-task average-per-round debit** (each task's own `qa_fail_count × (that task's own codex-reviewer total / that task's own round count)`), rather than either Codex's own single-global-average method or its round-level `min`-recomputation method.
  reason: a single global average-per-round figure (used identically for every task regardless of that task's own actual review cost) understates the debit for tasks whose reviews cost more than average and overstates it for tasks whose reviews cost less; a per-task average keeps the debit proportional to each task's own recorded review cost while still not requiring a full per-round (iteration/attempt) join between qa-verifier and codex-reviewer spans, which this corpus's own iteration-numbering does not align cleanly (verified directly: T-1065's qa-verifier rows carry `iteration` 1–7 while its codex-reviewer rows carry `iteration` 4,5,6,8 — the two roles' own iteration counters do not correspond 1:1, so a literal per-round join would itself need an undocumented matching rule). The resulting **24.47%** sits inside Codex's own independently-reported 22.9%–28.6% range, close to its midpoint, without duplicating either of Codex's own two derivations — satisfying "re-derive, don't copy" while landing in a mutually corroborating place.
  grounding: `.shell-team/reviews/T-1068.md` Blocker 1 (the false "cannot be measured" claim, the 27.3%/6-of-43 reproduction, the 22.9%/28.6% adversarial sensitivity range) and issue #252's own text, fetched and quoted by the reviewer directly from the GitHub API ("a review round spent on code QA then fails is wasted; ... Model the trade"); measured live in `docs/loop-engineering/agent-concurrency.md`'s own `## Overlap candidates and expected saving` telemetry re-derivation block (the QA-fail-rate command/result and the debit command/result).
- decision: rework round 2 (Codex round-1 Major 2 — the T-1058 `codex-reviewer` telemetry figure double-counts two rows sharing one `(iteration, attempt)` pair) — corrected T-1058's `codex-reviewer` figure to `1079568` (the `seq=12` row only, matching the canonical single-round review record) and re-ran every downstream aggregate (`sum_rev`, `sum_serial`, `sum_parallel_max`, `saving_min`, `saving_pct`, `avg_per_round_ms`) from the corrected input, rather than patching only the headline percentage.
  reason: the same corrected figure feeds five different downstream numbers in this note (the raw saving sum, the serial total, the per-task max, the reconcile-cost's per-round average, and the corrected saving percentage itself); patching only the most visible one (the 33.4% headline) while leaving the others computed from the stale, double-counted figure would have left the note internally inconsistent — the same class of defect DP4's own read-set-scoped pricing discipline exists to prevent (a corrected input has to propagate through every aggregate that reads it, not just the aggregate a reviewer happened to name).
  grounding: `.shell-team/reviews/T-1068.md` Major 2 (the two `seq=12`/`seq=100` rows, both `iteration=1, attempt=1`, and `.shell-team/reviews/T-1058.md`'s own single-round canonical record) and `docs/loop-engineering/effort-time-trend.md`'s own prior disclosure of the identical anomaly for the identical task ("the second telemetry row is a later, higher-`tool_uses` `codex-reviewer` span ... with no corresponding review-record heading — a best-effort over-count, not a second real round"); measured live — the two raw rows re-read directly from `.shell-team/runs/shell-team.jsonl`, matching the reviewer's own citation exactly.
- decision: rework round 2 (Codex round-1 Blocker 2 — the `validate-parallel-review` "S" sizing and "two readers, no writer" description contradict the shipped `agents/codex-reviewer.md`/`agents/qa-verifier.md`/`skills/run/SKILL.md` contracts) — independently enumerated five contract surfaces true concurrency would require (not copied from the reviewer's own list, which this role cannot read in full) and re-sized the candidate from `S` to `M`, rather than either leaving it `S` with a caveat or jumping to `L`.
  reason: the enumeration (two `agents/codex-reviewer.md` preconditions, `skills/run/SKILL.md`'s own sequential phase order, an undefined QA-FAIL discard rule, and `bin/loop-guard.sh`'s iteration-counting contract) is a bounded, scoped set of precondition relaxations and one new reconciliation rule — materially more than a trivial `S` fix, but categorically smaller than `intertask-pipelining`'s own `L` (which needs an entirely new multi-worktree scheduler and reconcile mechanism, not merely relaxed preconditions on two already-existing roles) — so `M` is the sizing this task's own S/M/L vocabulary was built to express for exactly this shape of change.
  grounding: `agents/codex-reviewer.md` (`## Preconditions`, "Task is at READY_FOR_REVIEW in tasks/todo.md"; step 2, "Read the spec ... and the engineer + QA hand-offs"), `agents/qa-verifier.md` (step 1, "find tasks at READY_FOR_QA"), `skills/run/SKILL.md` (step 5 "Validate", step 6 "Review", the same-class-2 cumulative rule at step 6), `bin/loop-guard.sh` (`--iteration`-counting usage header) — all read directly by this role; the five-surface enumeration is recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own new "Contract surfaces true concurrency would require" paragraph.
- decision: rework round 2 (Codex round-1 Blocker 3 — the pilot probe's background jobs carried no watchdog, violating DP1's own "no exceptions" requirement, yet the probe reported `ran`) — chose to re-run the probe with each background job watchdog-wrapped (a companion process polling `kill -0 "$PID"` every 0.2s up to a 30s stall threshold, started at the same moment as the job it watches), rather than flipping the probe to `declined`.
  reason: DP1 offers `declined` as "a legal and honest outcome," but only when a condition genuinely cannot be satisfied; here the missing watchdog was an omission, not a structural impossibility — a lightweight companion polling process is trivial to add to a Bash-job probe and does not touch this task's own no-mechanism scope (nothing is shipped or committed; the watchdog exists only for the duration of this task's own throwaway pilot run). Declining a probe that could instead be re-run compliant would have thrown away the one empirical data point this investigation is allowed to collect (DP1), for no safety benefit the watchdog itself doesn't already provide.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` DP1 ("Every background agent the probe launches runs with a watchdog started at launch, with no exceptions. ... A probe that cannot satisfy all of this is `declined`, which is a legal and honest outcome.") and the coordinator's own round-2 briefing, which named both honest exits and left the choice between them to this role; `.shell-team/reviews/T-1068.md` Blocker 3 (the missing-watchdog finding); the watchdog-wrapped re-run is recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Pilot probe` section, including both watchdogs' own heartbeat logs confirming no stall.
- decision: rework round 2 (Codex round-1 Major 1 — `full-population-diff-cpu`'s `hard` classification carried no measurement) — measured two concurrent `check-acs.sh` sweeps directly on this task's own operator machine (8 logical CPUs) rather than reasoning about CPU/wall-clock trade-offs in the abstract, and reclassified to `redesignable` on the strength of that measurement.
  reason: the round-1 reasoning ("does not reduce total CPU-seconds ... can regress wall-clock") was an unmeasured general claim contradicting the same concurrency-shrinks-wall-clock premise every other candidate in this matrix relies on; a direct measurement (serial vs. concurrent) settles the question for *this* machine rather than arguing from priors, and the ≈50.3% real reduction observed is the more defensible basis for a verdict than either the round-1 assertion or an unmeasured reclassification to the opposite word.
  grounding: `.shell-team/reviews/T-1068.md` Major 1 (the unmeasured `hard` finding); measured live — `getconf _NPROCESSORS_ONLN` = 8, two `check-acs.sh` sweeps (T-1067's and T-1066's own specs) timed serially at `6491577000` ns and concurrently at `3224044000` ns; the measurement and its own capacity-dependence caveat are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `full-population-diff-cpu` line.
- decision: rework round 3 (Codex round-2 Blocker — the round-2 watchdog is structurally inert: the parent `wait`s on the reader before ever checking the watchdog's own verdict, so the watchdog can only ever report `exited-normally`; this is the same class of independent Blocker against the live pilot probe's own safety in two consecutive rounds) — executed the frozen `## Pre-commitment`'s own disposition (probe → `declined`, hazards → `unobserved`, savings confirmed still `modeled`/`undetermined`, requirements moved to a `- follow-up:` line) rather than attempting a third watchdog design.
  reason: the spec's own Pre-commitment decided this disposition at freeze time, precisely so that a third patch attempt is never the response to a second independent safety Blocker — the coordinator's own round-3 briefing named this explicitly ("do not attempt a third watchdog design"), and DP1 itself states `declined` is "a legal and honest outcome," never a lesser one; keeping the historical run's own timing figures as clearly-labelled, non-load-bearing narrative (rather than deleting them) preserves the record of what each safety-non-compliant attempt actually measured, which the frozen Pre-commitment's own wording ("with that round's findings recorded verbatim") requires.
  grounding: `.shell-team/specs/T-1068-agent-concurrency.md` `## Pre-commitment` (the factual trigger, the disposition, the "never-dropped" list); `.shell-team/reviews/T-1068.md` round-2 Blocker (the watchdog's own control-flow defect, quoted and independently re-derived by this role by reading the same script); the declined probe line, its `unobserved` hazard marker, and the new requirements follow-up are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Pilot probe` and `## Recommendation and follow-ups` sections.
- decision: rework round 3 (Codex round-2 Major 1 — the reconcile-at-join debit priced a FAIL round's *entire* average review cost rather than only the excess beyond that round's own QA duration) — recomputed the debit as `max(0, that task's own average per-round codex-reviewer cost − that FAIL round's own qa-verifier duration)`, summed per FAIL round rather than per task, landing at 3387586.00 ms and an adjusted saving of 29.58% (rounded to 29.6% in prose).
  reason: a concurrently-run review is already running *while* QA runs on the same round, so its wall-clock cost up to that round's own QA duration is not additional time a discard wastes — only the review's own time in excess of the QA duration is; debiting the full average (as the round-1-rework reading did) double-counts the portion of review time that would have elapsed regardless of whether QA passed or failed. The corrected figure (29.58%) matches Codex's own independently-derived same-methodology figure (≈29.58%) exactly, and both sit above its own stricter round-level re-pairing (≈25.05%) — this task adopts the task-aggregate-with-corrected-debit structure it already built, rather than switching methodologies again, since DP2 does not mandate a specific sub-formula and the note discloses its own choice.
  grounding: `.shell-team/reviews/T-1068.md` round-2 Major 1 (the `max(0, …)` correction, the four FAIL rounds' own real `qa-verifier` `duration_ms` values: `855438`/`1606704`/`691335,261676,489083,729609`, cited and independently re-read by this role directly from `.shell-team/runs/shell-team.jsonl`); the corrected debit derivation, its command and its result are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Overlap candidates and expected saving` section.
- decision: rework round 3 (Codex round-2 Major 2 — `background-agent-launch — provided` was evidenced only at Bash level while the note itself discloses Agent-tool-level concurrency as unverified, a direct contradiction) — split the row: kept `background-agent-launch` as `provided` with its evidence explicitly scoped to Bash-level backgrounding only, and added a new substrate line, `agent-tool-concurrent-launch`, verdict `absent` (the closed vocabulary offers only `provided`/`absent`, not `undetermined`), with a reason explicit that `absent` here means "absent from what this task can itself confirm," not "does not exist in the harness."
  reason: DP3's own closed substrate-verdict vocabulary is exactly two words (`provided`, `absent`); inventing a third word to express "unconfirmed" would violate AC4's own closed-vocabulary check, so the honest way to express "not confirmed as provided" within that vocabulary is `absent`, paired with a reason that names the distinction explicitly rather than letting the bare word imply a stronger claim (that the capability provably does not exist) than this task can support.
  grounding: `.shell-team/reviews/T-1068.md` round-2 Major 2 (the internal-contradiction finding); `.shell-team/specs/T-1068-agent-concurrency.md` DP3 (the closed substrate-verdict vocabulary, `provided`/`absent` only); the split row and its own reasoning are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own substrate inventory, and `bash bin/check-acs.sh`'s own AC4 confirms both the four-named-id count and the closed-vocabulary count still balance after the split.
- decision: rework round 3 (Codex round-2 Minor — `.shell-team/interventions/T-1068.md:8` records "41 of 47 suite scripts... via mktemp -d," but a comment-excluded, `-d`-specific count is 39, not 41) — re-derived the count myself (naive `grep -l mktemp` = 41; comment-excluded, any `mktemp` = 40; comment-excluded, `mktemp -d` specifically = 39, all three reproducing the reviewer's own cited figures exactly) and reported the finding here rather than editing the interventions file.
  reason: `.shell-team/interventions/T-1068.md` is explicitly orchestrator-owned and out of this role's edit scope, stated in this task's own briefing at every round and confirmed by the reviewer's own note that this file "is not edited by the engineer" and that the deliverable note's own equivalent measurement (40, comment-excluded, not restricted to `-d` specifically) does not inherit the error and is itself correct; Codex's own Fast-follow disposition for this finding is "won't-fix... appropriately an orchestrator-side fix," so verifying and reporting, not editing, is the correct action within this role's own boundary.
  grounding: `.shell-team/reviews/T-1068.md` round-2 Minor and its own Fast-follow disposition ("`.shell-team/interventions/T-1068.md:8`... orchestrator側での修正が妥当"); measured live — the three counts above, reproduced with `find tests -type f -name "*.sh" -exec grep -l mktemp {} \; | wc -l` (41), the comment-excluded any-`mktemp` sweep (40, matching `docs/loop-engineering/agent-concurrency.md`'s own `test-suite-scratch-paths` figure exactly), and the comment-excluded `mktemp -d`-specific sweep (39).
- decision: rework round 4 (Codex round-3 Major 1 — the 29.6% headline aggregates `min(ΣQA, ΣReview)` at task level, which can pair one cycle's own QA time against a different cycle's own review time in a multi-round task) — led with a newly-derived round-level, same-cycle pairing (25.05%) as the headline, and kept the task-aggregate figure (29.58%/29.6%) as a disclosed upper bound, rather than keeping the task-aggregate as the headline with only a caveat sentence.
  reason: the candidate's own claimed overlap is same-cycle by construction (both roles read the *same* frozen commit, inside one round) — a round-level pairing is a **stricter, more literal** model of exactly that claim, while task-aggregate `min`-of-sums is a looser structure that happens to produce a defensible bound but is not itself what the candidate describes; verified the pairing is well-founded before using it (every task's own PASS-`qa-verifier` count equals its own canonical `rounds-to-approve` exactly, confirming a genuine 1:1 chronological correspondence rather than an assumed one), and the resulting figure (25.05%) reproduces the reviewer's own independently-cited round-level figure exactly, which is strong corroboration the methodology is sound. Leading with the stricter, better-justified figure and disclosing the looser one as a bound (rather than the reverse) is the more honest ordering for a reader deciding whether to stage this candidate.
  grounding: `.shell-team/reviews/T-1068.md` round-3 Major 1 (the cross-cycle-pairing critique, the disclosed choice between leading with round-level or annotating task-aggregate); measured live — the round-level pairing's own derivation (37 real PASS→Review pairs, `sum_min=25260594`, verified 1:1 PASS-count-to-canonical-rounds correspondence for all 11 tasks first), the same FAIL-round debit applied to both numerators, and both formulas now recorded inline on the `- saving:` line itself in `docs/loop-engineering/agent-concurrency.md`, not only in `## Limits`.
- decision: rework round 4 (Codex round-3 Major 2 — `implement-tail-parallel-qa`'s own safety precondition describes a "tail verification" overlappable with QA, but `agents/engineer.md`'s step order and the T-073 guard mean that verification already completes before QA can start) — rewrote the candidate's own safety precondition to state plainly that no such window exists under the shipped sequence, re-sized `S`→`M`, and added a second, independent reason to its own saving's `undetermined` disposition (no real phase remains to isolate, in addition to the pre-existing telemetry-decomposition gap), rather than only patching the wording while leaving the `S` sizing and single-reason disposition unchanged.
  reason: the precondition's own factual claim was wrong (the verification tail is not "still being written" post-commit — only prose is), so a wording-only fix would have left a candidate whose own cost sizing (`S`) still implicitly assumed a real, no-redesign-needed overlap existed; once the true shape of the gap is named (restructuring the engineer's own phase to let qa-verifier read a not-yet-fully-verified state, which is exactly what T-073 exists to forbid), the sizing has to reflect a safety-guard change, the same `agents`/`skills`-edit weight `validate-parallel-review`'s own five-surface enumeration already prices at `M` — using the same word for the same shape of change keeps the note's own S/M/L vocabulary meaning one thing consistently across candidates.
  grounding: `.shell-team/reviews/T-1068.md` round-3 Major 2 (the step-order critique, citing `agents/engineer.md` step 4 and `skills/run/SKILL.md`'s T-073 guard); measured live — `agents/engineer.md`'s own step 4 ("Run the test suite locally. If it fails, fix it.") read directly, confirmed to precede step 5's commit-and-flag step; the corrected candidate line, its re-sized `M`, and its saving block's two-reason `undetermined` disposition are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`.
- decision: rework round 4 (Codex round-3 Minor — a sentence claimed all four debit-methodology figures (24.47%, 25.05%, 29.58%, 29.6%) sit *above* the independently-reported 22.9%–28.6% range, which is arithmetically false for two of the four) — corrected the sentence to state the true relation (24.47% and 25.05% sit inside that range; only 29.58%/29.6% sits above it) rather than simply deleting the comparison.
  reason: the comparison itself is informative (it corroborates that every methodology this task tried lands in a similar, bounded neighborhood) and deleting it would lose that corroboration; the fix is to state the actual arithmetic relation rather than to drop the sentence that got it wrong, since a correct comparison still serves the reader better than none.
  grounding: `.shell-team/reviews/T-1068.md` round-3 Minor 1 (the arithmetic check: `22.9 < 24.47 < 28.6` and `22.9 < 25.05 < 28.6`, both true; `29.58 > 28.6`, also true); the corrected sentence is recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Overlap candidates and expected saving` and `## Limits` sections.
- decision: rework round 4 (Codex round-3 Minor — the `implement-tail-parallel-qa` saving line's own sentence ended without an object, "...none finer-grained than `engineer` exists to isolate a tail-verification sub-phase from") — completed the sentence ("...from the rest of that span's own total") rather than restructuring the surrounding sentence.
  reason: the sentence's own meaning was already clear from context and the fix needed is a completion, not a rewrite — the smallest change that satisfies the finding, consistent with this task's own no-mechanism, minimal-diff discipline.
  grounding: `.shell-team/reviews/T-1068.md` round-3 Minor 2; the completed sentence is recorded verbatim in `docs/loop-engineering/agent-concurrency.md`.
- decision: rework round 4 (Codex round-3 Minor — `## Limits`'s own opening sentence conflated two different populations inside one bracketed clause: the reconcile-at-join proxy — `avg_per_round_ms`, n=11, machine-local telemetry — and T-1060's own single-task n=1 series from a different note, summarizing both as "a single-task n=1 observation") — split the one sentence into two, each scoped to its own population, rather than removing the n=1 claim entirely.
  reason: both underlying claims are true and belong in the note (T-1060's own series really is n=1 and board-sourced; the reconcile-at-join proxy really is n=11 and telemetry-sourced) — the defect was merging them under one shared label, not either claim being wrong on its own, so the fix is separation, not deletion.
  grounding: `.shell-team/reviews/T-1068.md` round-3 Minor 3 (`grep -n "reconcile-at-join proxy"` locating the two sites, confirming the n=1 mislabel); the two separated sentences are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Limits and what is not computable`.
- decision: rework round 5 (Codex round-4 Blocker, operator-ratified option B — incorporate the sixth contract surface as a named blocker on `validate-parallel-review`'s own safety precondition, do not re-price the matrix) — added the sixth surface as two bounded sentences to the candidate line and labelled the existing 25.05% saving conditional on all six surfaces, rather than re-deriving a new debit for the QA-PASS-with-edit case the finding also names.
  reason: the operator's own ruling is explicit that this is a minimal close, not a repricing pass — issue #252's own acceptance form permits a recommendation with the specific blocker named rather than a fully-priced one, and the planning consumer this note serves needs the blocker named, not a polished number; re-deriving a QA-PASS-edit-collision debit would require a new methodology (there is no historical data for "a review read mid-edit," unlike the FAIL-round debit which prices something that actually happened) and the operator's own instruction — "keep it minimal — new prose is finding surface" — rules that out for this pass.
  grounding: the coordinator's own round-5 briefing (quoting the operator's ruling verbatim: "two bounded sentences, no new derivation... Keep the modeled 25.05% saving as-is, labeled conditional on the (now six) preconditions"); `.shell-team/reviews/T-1068.md` round-4 Blocker (the sixth-surface finding: `agents/qa-verifier.md`'s test-edit licence during Validate vs. `agents/codex-reviewer.md`'s unpinned working-tree read, affecting QA-PASS as well as QA-FAIL); the two sentences and the conditional label are recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own candidate and saving lines.
- decision: rework round 5 (the recommendation's own Stage 0 framing) — restructured the recommendation to name the six-surface contract redesign as an explicit "Stage 0" that nothing in `validate-parallel-review` runs before, rather than leaving it as one item inside a flat three-item precondition list.
  reason: the operator's own instruction asks the recommendation to read as "not before the contracts change" — issue #252's own blocker-named acceptance form — and a flat list does not communicate an ordering constraint (that the contract redesign must land *before* anything else, not merely alongside it) the way a named Stage 0 does; the two remaining preconditions (the `bin/log-run.sh` lock, empirical Agent-tool concurrency confirmation) are demoted to "further preconditions beyond Stage 0" to keep that ordering explicit.
  grounding: the coordinator's own round-5 briefing ("state stage 0 = the contract-surface changes explicitly (the six surfaces), so staged-adoption reads as 'not before the contracts change' — issue #252's blocker-named acceptance form"); the restructured recommendation is recorded verbatim in `docs/loop-engineering/agent-concurrency.md`'s own `## Recommendation and follow-ups`.
- decision: rework round 5 (Codex round-4 Minor — `.shell-team/specs/T-1068-agent-concurrency.md`'s own Summarized-sources bullet names a non-existent `tests/retro-inventory` directory and conflates a naive 41-script mktemp count with a `mktemp -d`-specific claim) — fixed both in the spec's own mutable region (before the intent block, confirmed unchanged by `check-intent.sh` before and after), rather than leaving the wording fix to a later task.
  reason: this bullet sits entirely outside the frozen intent block (line 25, before the `BEGIN intent-block` marker at line 32), so fixing it is an ordinary mutable-region edit, not a class-B/M re-freeze — the same shape as this task's own earlier "nine-section"→"ten-section" fix (commit `8c1158e`); the operator's own ruling explicitly names this fix as part of the minimal close, and it is cheap and already measured (39 comment-excluded `mktemp -d`-specific, matching this task's own prior derivation exactly).
  grounding: `.shell-team/reviews/T-1068.md` round-4 Minor (`ls tests/ | grep -i retro` confirming `tests/retro-inputs`, not `tests/retro-inventory`; the 41/40/39 count distinction); `bash bin/check-intent.sh` re-run immediately after the edit, confirming hash `a2547b53...` unchanged; the corrected bullet is recorded verbatim in the spec's own `## Summarized sources` section.
<!-- END provenance: T-1068 -->
