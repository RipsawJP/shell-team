<!-- BEGIN provenance: T-1002 -->

- decision: The seven-member class enum is hardcoded as a bash `case` statement inside `bin/check-interventions.sh` (`is_recognized_class()`) rather than parsed at runtime from `templates/prompt-blocks/interventions-classes.md`.
  reason: Mirrors `bin/check-provenance.sh`'s self-contained design (no runtime dependency on another file's presence/layout) and follows the spec's own instruction to duplicate the canonical lines verbatim in the checker's header comment, with `bin/check-prompt-sync.sh` verifying the two copies never drift — the checker stays zero-dependency and pure bash rather than gaining a markdown-parsing responsibility.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md "## Notes for engineer" ("put it in the script's header comment verbatim, em dash included"), DP-7.

- decision: `check-interventions.sh --task <id>` does not format-validate its own value (unlike `bin/check-design-note.sh`'s `--task`, which enforces `^T-[0-9]+$`) — it only compares the given string against the BEGIN marker's captured id.
  reason: A rigid `^T-[0-9]+$` check would wrongly reject the legitimate invocation `--task no-task`, since the reserved literal `no-task` is a valid id distinct from `T-NNN` (DP-2). No acceptance criterion requires format-validating `--task`'s own value, so adding one would be an unrequested rule that conflicts with the reserved literal.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md DP-2 (reserved `no-task` id), AC6/AC7 (marker id acceptance and `--task` disagreement).

- decision: The entry-anchor regex (`ENTRY_RE`) and the class-validity check (`is_recognized_class`) are kept as two separate steps — an unrecognized class token still opens an "in_entry" scope and has its date/summary/effect fields parsed — rather than folding the enum alternation directly into the anchor regex so an unrecognized class line is never treated as an entry at all.
  reason: Keeping the anchor and the class check separate gives every entry the same field-parsing treatment regardless of class validity, so an entry with a bad class token but otherwise well-formed fields produces one clear violation path (invalid class) instead of a second failure mode where the line is silently reclassified as "not an entry" and its subsequent date/summary/effect lines are then reported as unrecognized non-blank lines with a confusing origin.
  grounding: bin/check-interventions.sh (`ENTRY_RE` / `is_recognized_class()` separation, and AC2's fixture for an unrecognized class `human-nudge`).

- decision: `skills/goal/SKILL.md`'s interventions translation combines `usage` and `structural` into a single reserved sentinel (`AC900006`), matching `check-provenance`'s own existing combining convention (`AC900004`) rather than giving interventions a third, separate sentinel for `structural`.
  reason: The provenance translation block already sits beside this one and already made this exact choice (DP-9); using the same combining shape for the sibling gate keeps the two translation blocks visibly parallel and avoids inventing a new shape that would only differ for the newer gate with no stated benefit.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md DP-9 and AC23; skills/goal/SKILL.md's pre-existing check-provenance translation bullet (AC900003/AC900004).

- decision: `bin/team-paths.sh --print`'s columns are re-padded uniformly with a single `printf '  %-13s %s\n'` format applied to every key, rather than hand-adjusting each row's trailing spaces individually.
  reason: DP-6 explicitly authorizes re-padding (no suite pins exact spacing, only the `[[:space:]]+` separator rule), and a single format string keyed to the widest label (`interventions`, 13 chars) is less error-prone than manually recounting spaces per row whenever the widest key changes again in the future.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md DP-6; bin/team-paths.sh (`--print` block).

- decision: The new "Interventions producer discipline (T-1002)" standing instruction is placed in `skills/run/SKILL.md` immediately before "Execute these phases in order" (alongside the Language-mirroring paragraph), rather than at the end of the file near the Telemetry section.
  reason: The discipline applies across every phase (not just Implement, where the gate paragraph lives), so it reads most naturally grouped with the other standing, phase-independent instructions (path resolution, language mirroring) that already precede the phase list, rather than being read only after all seven phases have been described.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md AC19 (states content requirements only, not placement); skills/run/SKILL.md (placement before "Execute these phases **in order**").

- decision: (rework1) The END-marker check is redesigned to mirror BEGIN's own matching shape exactly — a generic any-id regex requiring exactly one occurrence, with the captured id compared against BEGIN's captured id only afterward — rather than patching the old literal-derived-from-begin_id compare with a second, separate scan for "any other END line."
  reason: Two shapes could have closed the reviewer's reproduction (a trailing, well-formed END marker for a different id). Bolting on a second scan keeps the asymmetry in spirit (BEGIN and END would still be matched two different ways); making END's own matching shape identical to BEGIN's collapses the class to one invariant ("each marker kind is matched the same generic-regex-then-compare way") that is simpler to state, to test, and to keep in sync if a third marker property is ever added.
  grounding: bin/check-interventions.sh (`BEGIN_ANY_RE`/`END_ANY_RE` symmetry); .shell-team/reviews/T-1002.md round1 Major 2 (`bin/check-interventions.sh:218-233`).

- decision: (rework1) `--task`'s duplicate-detection rejects a REPEATED flag outright (usage exit 2), even when both occurrences carry the identical value — it does not special-case "repeated but agreeing" as tolerated.
  reason: The alternative (compare the two values and only reject on disagreement) adds a second comparison path for no stated benefit — a caller that passes `--task` twice, agreeing or not, has a malformed invocation the caller should fix, and "given more than once" is a strictly simpler invariant to state and to keep symmetric with the empty-value rejection added in the same fix.
  grounding: bin/check-interventions.sh (`TASK_GIVEN` guard); .shell-team/reviews/T-1002.md round1 Major 1 (suggested fix: "reject ... a repeated `--task` flag as `usage`").

- decision: (rework1) The Blocker-1 entry template (the marker-pair + field-quad illustration) and the sentinel-replace-first-entry branch are added to the canonical `templates/prompt-blocks/interventions-classes.md` as two prose sentences, with the illustrative `- intervention: <class>` fragment folded mid-sentence rather than started at column 0 as its own line.
  reason: A column-0 example line matching `^- intervention: ` would increment AC2's `grep -c -- '^- intervention: '` pin from 7 to 8 and fail a frozen-adjacent acceptance criterion outright — the review's own Blocker-1 text flagged this exact trap. Folding the illustration into prose keeps AC2's count exact while still satisfying `check-prompt-sync.sh`'s contain-mode requirement (each canonical line reproduced verbatim in both registered consumers).
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md AC2 check line (`grep -c -- '^- intervention: ' "$B"` pinned `-eq 7`); .shell-team/reviews/T-1002.md round1 Blocker 1 ("⚠️ TRAP").

- decision: (rework1) `skills/goal/SKILL.md`'s new producer-discipline paragraph is written as a REFERENCE to `skills/run/SKILL.md`'s own paragraph and to the canonical template (not a second verbatim copy), and is locked by a plain anchor-grep case in `tests/codex-skeleton-hygiene/run.sh` rather than a new `check-prompt-sync.sh` registry row naming `skills/goal/SKILL.md` as a third consumer.
  reason: AC17 pins the registry row's consumer list at exactly two (`bin/check-interventions.sh`, `skills/run/SKILL.md`); adding a third consumer there would itself fail AC17's own check. The round1 review's suggested fix explicitly asked for reference form, "NOT a verbatim canonical-block copy" — a plain grep-anchor lock in an existing CI-run suite is the only mechanism consistent with both constraints at once.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md AC17 check line (registry row regex pinned to exactly two consumers); .shell-team/reviews/T-1002.md round1 Blocker 2 ("NOT a verbatim canonical-block copy, because AC17's frozen check pins the registry row at exactly two consumers").

- decision: (rework1) The goal-loop same-tick interventions-repair path (Major 4) is expressed as one added sentence inside the existing Completion-gate bullet (step 2) of `skills/goal/SKILL.md`, rather than as a new numbered step or a restructuring of the tick loop / `goal-state.sh` plumbing.
  reason: The spec's own Non-goals state `bin/goal-state.sh` is reuse-only and AC23 locks it byte-unchanged from `develop`, so a mechanism change to the tick's iteration/SIG bookkeeping was out of scope for this rework; the reviewer's own suggested fix offered exactly this shape ("describe a same-tick repair-and-recheck path... or exempt it from the next tick's implement-pass requirement").
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md AC23 check line (`git diff --quiet develop -- bin/goal-state.sh`); .shell-team/reviews/T-1002.md round1 Major 4 (`skills/goal/SKILL.md:78`, adversarial pass).

- decision: (rework1) The T-073 commit-timing disposal sentence (Major 3) is appended to the existing "Preserve the implementation across the seam (T-073)" bullet in both `skills/run/SKILL.md` and `skills/goal/SKILL.md`, rather than folded into the "Interventions gate" bullet's own text.
  reason: AC18 extracts the "Interventions gate" paragraph as a single physical line and forbids specific literals on it; adding unrelated commit-ordering prose to that pinned line risks entangling with a frozen check for no benefit, whereas the T-073 bullet carries no such single-line/substring contract and is the bullet the interaction is actually about.
  grounding: .shell-team/specs/T-1002-intervention-capture-channel.md AC18 check line (`sed -n "${n}p" "$F"` single-line extraction); .shell-team/reviews/T-1002.md round1 Major 3 (`skills/run/SKILL.md:58,60`, adversarial pass).

<!-- END provenance: T-1002 -->
