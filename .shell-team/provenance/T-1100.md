<!-- BEGIN provenance: T-1100 -->
- decision: the parent-vs-refinement exclusivity refusal is placed as a single `case`/`case` guard immediately after the existing `while … done <<< "$DISPATCH_LINES"` loop, reading the loop's own `DISPATCH_SEEN_AXES` accumulator, rather than being folded into the per-line body of the loop itself.
  reason: whether the parent `verify` axis and a refinement (`verify-fixture`/`verify-mechanism`) both appear on the same entry is only knowable once the WHOLE entry has been scanned — a line-by-line check inside the loop could only ever see one axis at a time and would need its own separate accumulator duplicating `DISPATCH_SEEN_AXES`, or would refuse on the wrong line depending on which of the two conflicting rows happened to be read first (order-dependent, non-reproducible stderr). Placing it after the loop keeps the existing per-line refusals (unknown axis, duplicate axis, cross-axis value, bad modality, groundless ground) exactly as they were — first-malformed-record-wins — and adds exactly one new, order-independent check on the accumulated state.
  grounding: `.shell-team/specs/T-1100-verify-axis-split.md` `## Notes for engineer` ("The exclusivity test's placement matters... it belongs after the loop, reading `DISPATCH_SEEN_AXES`") and DP-1 ("the exclusivity refusal is *added* rather than left to prose... after the existing loop"); `bin/close-out.sh`'s own `DISPATCH_SEEN_AXES` accumulator, read directly before this task at the per-key duplicate-axis check inside the loop.

- decision: the new authoring duty (`prices each half independently`) is written into `agents/pm-spec.md` as its own new `## Verify-axis refinement authoring duty (T-1100)` section, placed between the existing `## Entry-mode hand-off field` material and `## Spec-review hand-off field (T-1092)`, rather than appended as another paragraph inside the existing `- entry-mode:` transcription section where the now-corrected "the four `- dispatch:` sub-bullets" phrase used to live.
  reason: the `- entry-mode:` section's own subject (which mode `pm-spec` ran in, and when to transcribe it onto the board) is unrelated to the verify-axis split's authoring judgment (whether a task's verification duty spans both a fixture-suite half and a mechanism-class full-population half); folding the new duty into that section would conflate two independent hand-off obligations under one heading, whereas the sibling `## Spec-review hand-off field (T-1092)` section already establishes the convention of one dedicated section per axis-shaped authoring duty this file owes.
  grounding: `agents/pm-spec.md`'s own existing structure (the `## Spec-review hand-off field (T-1092)` section immediately following, added by a prior axis task under the identical pattern); `.shell-team/specs/T-1100-verify-axis-split.md` DP-2 ("the obligation takes arm B, an authoring duty in prose (`agents/pm-spec.md`, and the dispatcher note's own trigger text)") and AC7 (requires the literal `prices each half independently` and the two key names to exist in the file, with no constraint on placement).

- decision: `docs/loop-engineering/phase-multiplexing.md:61`'s grammar-declaration parenthetical is rewritten to `(one line per axis this note declares, note-wide, each declaring that axis's own closed value set — a refinement axis is its own line too, its own text stating which axis it refines)`, replacing `(exactly two, note-wide: one per axis, each declaring that axis's own closed value set)` — a value-independent description rather than a literal count of any kind (not even a new number).
  reason: DP-1 and DP-6 both require the axis-count assertion to stop being pinned to a specific number at all ("rewritten value-independently rather than re-pinned to a new number"), since a later axis (issue #274's depth axis) would otherwise force editing this same line a second time. "Exactly two" literally became false the moment this task adds a third and fourth axis-shaped key; the replacement states the invariant that is actually true regardless of how many axes exist (one declaration line per axis), which is what stays true after the next axis lands too.
  grounding: `.shell-team/specs/T-1100-verify-axis-split.md` DP-1 ("`phase-multiplexing.md:61`'s "exactly two, note-wide" is rewritten value-independently rather than re-pinned to a new number") and AC6 (asserts the literal `exactly two, note-wide` is absent from the `- dispatch-axis:` grammar line specifically, while the whole-file count drops by exactly one, leaving the `- dispatch-note:` grammar line's own occurrence at `:63` untouched).

- decision: AC5's frozen check line is reported as a measured, reproducible FAIL rather than being forced green by any code change, and the frozen intent block is not edited to accommodate it. The root cause — a structural conflict between AC5 (byte-identical two-arm verdict across every distinct `- dispatch:` line currently on the resolved board) and AC12 (this task's own board entry must carry `verify-fixture`/`verify-mechanism` rows by the time this criterion is checked) — is recorded as an interventions entry and reported prominently in the hand-off instead.
  reason: this task's own board entry was already substituted with the split form at commit `82deff3`, before the engineer phase began (DP-5, performed at the Specify-to-Implement seam). Because AC5's population is computed from the CURRENT resolved board at check time, and that board now permanently contains rows using the two new axis keys this task introduces, the base-ref close-out.sh (which predates those keys and must refuse them) and HEAD's close-out.sh (which must accept them, by design) can never agree on those two rows once implemented — this is not a bug reachable by any implementation choice inside `bin/close-out.sh`; it is a consequence of when the board substitution was performed relative to when AC5 is measured. Silently editing AC5 would touch the frozen intent block without human ratification (a class-B change); silently reverting the committed board substitution would violate AC12, exceed engineer's scope (DP-5 names the substitution as the orchestrator's action, not engineer's or pm-spec's), and would not be recoverable without also breaking the already-recorded freeze-sweep evidence on the board entry. The pre-commitment's own "never-dropped-defeat" clause disposes of a defeated never-dropped item (AC5 is explicitly one) by routing back to planning, not by a patch round — reporting rather than patching follows that disposition.
  grounding: reproduced live on this branch (`bash <extracted AC5 check line>` → `rc=1`; a scratch debug run isolating the diff showed exactly the two `verify-fixture`/`verify-mechanism` rows on the resolved board flipping from `1 refused` at the base arm to `0 silent` at the head arm, with zero other rows differing); `git log --oneline -3` showing the board's DP-5 substitution already committed at `82deff3` before this session's edits; `.shell-team/specs/T-1100-verify-axis-split.md` AC5 (the byte-identity check), AC12 (the split-form requirement on this task's own entry) and `## Pre-commitment`'s `never-dropped: … the live-board back-compat diff (AC5)` plus `never-dropped-defeat: defeat of any never-dropped item stops the task and returns it to planning`.
<!-- END provenance: T-1100 -->
