<!-- BEGIN provenance: T-1009 -->

- decision: The `2026-06-17` board-format corpus entry was corrected **in place** — its heading left byte-unchanged, `Category`/`Applies-to`/`Scope`/`Status`/`Source` untouched, and `docs/loop-engineering/lessons-import-disposition.md` not edited in either direction — rather than superseded with a new dated entry.
  reason: Supersession would break no machine check (nothing on the merged tree enforces ledger⇄corpus key equality), which is exactly why it would be the wrong choice: it would silently invalidate the ledger's only human-readable mapping with nothing to catch it, and the entry's *ground* — "don't wedge a note between the flag and `— spec:`" — is unchanged; only a supporting factual claim about where the check runs was wrong. I implemented this by editing the three prose bullets (`Rule`, `Why`, `How to apply`) as clause-level substitutions inside the existing entry, then verified with `git diff --numstat 0906862 -- .shell-team/lessons.md` that the change was exactly 3 lines added and 3 deleted (no new heading, no removed entry).
  grounding: .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md heading "### DP-1 — in-place edit, not supersession; the key stays byte-identical; the ledger is not touched"; AC6 check: (byte-lock on the `2026-06-17 .*The board` heading against `git show 0906862:`); AC9 check: (`git diff --numstat` pinned at `3/3`).

- decision: The corrected `Rule`/`Why`/`How to apply` text drops every present-tense CI claim and names the linter by its bare distributed command `check-handoff.sh`, never by this repository's real workflow step name (`Lint the shipped board template`); the historical incident is kept in `Why` but attributed to "the repository where the incident happened" rather than to any named repository, organisation or person.
  reason: The `Rule` field is spliced verbatim into `templates/prompt-blocks/playbook-pm-spec.md` and `playbook-engineer.md` and from there into `agents/pm-spec.md` and `agents/engineer.md`, so it ships to every adopter. The plugin ships no workflow file, so a present-tense CI claim about an adopter's repository can never be true by construction, and naming this repository's own real step inside a shipped block would be the identical defect one level up. Bare-command reference is already the shipped precedent (`templates/prompt-blocks/language.md`'s "grepped by `check-handoff.sh` / `goal-state.sh` / `check-acs.sh`"), and attribution rather than naming keeps the shape-level PII/commit-identity gates green (no personal-name, hostname or employer-name shape introduced). I verified the three replacement clauses were applied as frozen (old text → new text, every other byte of each field's line unchanged) and that after the edit none of the three field lines contains the two-character sequence `CI` and none contains `board-lint`.
  grounding: .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md heading "### DP-2 — the corrected claim names the linter by its distributed command and makes no CI claim at all"; the frozen replacement table under "### The three fields' replacement text, frozen"; AC8 check: (zero `CI`, zero `board-lint`, `check-handoff.sh` present, `the repository where the incident happened` present in the entry block); AC10 check: (same four assertions scoped to each of the four shipped surfaces' anchored line); AC18 check: (`check-pii-shapes.sh --base develop` and `check-commit-identity.sh --base develop`).

- decision: (rework round 1) Reworded `docs/workflow.md`'s "Specs"/"Review artifacts" bullets and `docs/workflow.ja.md`'s "仕様"/"レビュー成果物" bullets from the literal `bin/team-paths.sh --get specs|reviews` form to the bare-command form `team-paths.sh --get specs|reviews` with the parenthetical "(on `PATH` when the plugin is loaded; else `bin/team-paths.sh`)" — mirrored in natural Japanese prose in the `.ja.md` twin — rather than any other rewording (e.g. dropping the `bin/` fallback entirely, or only fixing the English file).
  reason: Codex round 1's Major established that a literal `/`-containing path fails verbatim in an adopter repository, where the plugin's scripts are exposed as bare `PATH` commands rather than copied into the adopter's tree — the same maintainer-environment assumption this task exists to remove, reintroduced into the exact dual-audience surface (`README.md`/`CONTRIBUTING.md` both point readers here) the spec's Goal targets. Every `agents/*.md` operating-paths note already establishes the bare-command-plus-fallback phrasing as this project's precedent for the identical resolver reference, so matching it (rather than inventing new wording) keeps the fix minimal and consistent with the shipped pattern. I confirmed before editing that this is in-contract, not a re-freeze: AC2's `check:` requires only the containment substrings `team-paths.sh --get specs`/`--get reviews` present and zero `docs/specs/`/`tasks/reviews/` — both properties survive the reworded form (confirmed by a restore-mutation probe: reverting one file to the `bin/`-prefixed form left all 19 checks green, i.e. AC2 cannot distinguish the two forms — a confirmed detector gap, not a defect introduced by this fix).
  grounding: .shell-team/reviews/T-1009.md "#### Major" (Codex round 1 finding on `docs/workflow.md:101-102` / `docs/workflow.ja.md:99-100`) and its "Suggested fix" clause; agents/engineer.md:10 ("on `PATH` when the plugin is loaded; else `bin/team-paths.sh`") as the precedent phrasing; .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md AC2 `check:` line (containment-only grep for `team-paths.sh --get specs`/`--get reviews`).

<!-- END provenance: T-1009 -->
