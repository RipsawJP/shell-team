# T-1064 decision provenance

<!-- BEGIN provenance: T-1064 -->
- decision: the two `DCARVE` sentences are appended at the very end of each durability bullet's own physical line, after a single space — following the spec's `## Notes for engineer` suggestion — rather than inserting the sentence before the bullet's own escalation instruction or elsewhere on the same line.
  reason: no criterion asserts the exact position beyond "on the anchored line" (AC4 only requires `DCARVE` to occur on the same physical line as each anchor, exactly twice total), so more than one placement satisfies the acceptance criteria and a real choice existed. The spec's Notes explicitly flag this as guidance rather than a frozen requirement and warn that placing it before the escalation instruction "reads as an interruption" — adopting the suggested placement avoids introducing an ordering defect a reviewer would otherwise have to flag, for no cost against any criterion.
  grounding: `.shell-team/specs/T-1064-shipped-docs-accuracy.md` `## Notes for engineer` ("Where the `DCARVE` sentence goes: at the very end of each durability bullet's own physical line, after a single space... but a reviewer will read it, and putting it before the escalation instruction reads as an interruption") and **AC4**'s own check line, which anchors `DCARVE` to the line carrying each `check-durability.sh --phase <implement|pre-merge> ...` literal without asserting an intra-line position.
- decision: the `.shell-team/test-recipe.md` T-1064 entry uses the spec's suggested wording verbatim rather than composing an independent phrasing that also carries the four frozen fragments (`TRa`/`TRb`/`TRc`/`TRd`).
  reason: **AC5** admits any wording carrying the four fragments, so more than one entry text satisfies the criterion and a real choice existed. The spec's Notes frame the suggested text as "offered as a starting point and not binding," but it was already checked against the criterion's literal requirements at spec-authoring time (it embeds all four fragments verbatim and correctly frames the entry as a delta on the T-1056/T-1057 entries above it) — composing new prose adds a source of AC5 mismatch (a missed fragment, an inadvertent restatement of a referent entry) for no benefit over text already known to satisfy the acceptance criterion.
  grounding: `.shell-team/specs/T-1064-shipped-docs-accuracy.md` `## Notes for engineer`'s "Suggested wording for the test-recipe entry, offered as a starting point and not binding" bullet, and **AC5**'s own check line, which searches the joined entry for the four fragments rather than for a fixed string.
- decision: the two new `- freeze-attestation (v3, 2026-08-13): ...` board ledger lines (T-1043, T-1050) are written to conform exactly to `bin/check-intent.sh`'s `ATTEST_FULL_RE` grammar (`lines=<ran>/<total> sweep=mutual-satisfiability verdict=<P>P/<F>F owner=<value>`), with the measured live PASS/FAIL counts as the verdict, rather than a descriptive summary of what this pass actually did (the class-B substitution, the classifier exit codes).
  reason: the first draft of both lines used a descriptive `sweep=byte-identity-plus-classifier verdict=class-B applied` shape instead of the checker's exact grammar; running `bin/check-intent.sh` against the board (as part of re-verifying T-1050's own **AC11**, one of the `## Blast radius` rows this task must re-measure) surfaced `check-intent: attestation: 1 malformed freeze-attestation record(s)` for both T-1043 and T-1050, which is a hard requirement (AC10 of this task's own spec requires `check-intent.sh` to report `aligned` for both re-frozen specs) rather than a stylistic preference — there was a genuine two-option choice (keep the descriptive prose and fail the mechanical grammar, or conform to the grammar and move the descriptive content into free prose after `owner=`) and the grammar-conformant option was the only one that satisfies AC10.
  grounding: `bin/check-intent.sh`'s `ATTEST_FULL_RE` (`^[[:space:]]+- freeze-attestation \(v([1-9][0-9]{0,3}), ([0-9]{4}-[0-9]{2}-[0-9]{2})\): lines=([0-9]{1,4})/([0-9]{1,4}) sweep=mutual-satisfiability verdict=([0-9]{1,4})P/([0-9]{1,4})F owner=([^[:space:]].*)$`) and its `fail_attestation` message reproduced live by re-running `bin/check-intent.sh` against `.shell-team/todo.md` for both T-1043 and T-1050 before and after the fix; `.shell-team/specs/T-1064-shipped-docs-accuracy.md` **AC10**'s own requirement that `bin/check-intent.sh` report `aligned` for both re-frozen specs.
<!-- END provenance: T-1064 -->
