<!-- BEGIN provenance: T-1006 -->

- decision: The four corrected statements in `bin/retro-inputs.sh` (header framing, `--help` text, the `lessons` probe's design comment, and the runtime `note()` detail string) are worded as "a resolver key exists (`bin/team-paths.sh --get lessons`), but this script asks for the path explicitly, by choice" — naming the key, naming the choice, and pointing at the resolver invocation that proves the key is real — rather than a shorter phrasing that only drops the banned words without stating the reason.
  reason: DP-1 explicitly leaves the exact wording to the engineer ("The exact wording is the engineer's; the forbidden outcome is a shipped statement asserting the key does not exist (AC12), and the forbidden side effect is a behaviour change (AC13)"), so more than one non-forbidden phrasing was available. A phrasing that merely deleted "no resolver key" without saying what replaced it would satisfy AC12's negative greps but would not satisfy the Goal's own sentence: "its statements are corrected to say that this is a choice rather than an absence." Naming the actual resolver invocation also gives AC12's `team-paths.sh --get lessons` positive-count assertion a real anchor rather than an incidental one.
  grounding: .shell-team/specs/T-1006-lessons-resolver-key.md DP-1 "the exact wording is the engineer's"; Goal paragraph "its statements are corrected to say that this is a choice rather than an absence"; AC12's check line `grep -cF -- 'team-paths.sh --get lessons' bin/retro-inputs.sh) -ge 1`.

- decision: `bin/gen-playbook-blocks.sh`'s `--help` printed window (`sed -n 'N,Mp'`) is widened from `2,45p` to `2,48p` so the corrected `--lessons` description (now four lines instead of two, since it names the resolver invocation) stays fully inside the printed slice, including the `--blocks-dir` line that follows it.
  reason: The Non-goals section names this exact pre-existing wart and explicitly permits touching it for this reason and no other: "Widening the gen window is permitted where the corrected `--lessons` text needs the room; neither wart is this task's to fix." AC8 requires the `--lessons` option line to stay inside the printed window (the positive control that the fix was not truncated past) and requires the literal `team-paths.sh --get lessons` string to appear in the `--help` output, which only widening (not further compressing the prose) could satisfy without losing the explanatory sentence DP-1-equivalent clarity requires for this consumer too.
  grounding: .shell-team/specs/T-1006-lessons-resolver-key.md Non-goals "Widening the gen window is permitted where the corrected `--lessons` text needs the room; neither wart is this task's to fix"; AC8's check line requiring `grep -qF 'team-paths.sh --get lessons'` and `grep -qF -- '--lessons'` against `bin/gen-playbook-blocks.sh --help` output.

- decision: `bin/playbook-promote.sh`'s `--help` printed window (`sed -n '2,45p'`, which the spec's Non-goals states already truncates before the whole Usage block) is left untouched, even though the corrected `--lessons` description text is not visible in its `--help` output.
  reason: Unlike the gen script, no acceptance criterion pins `playbook-promote.sh --help`'s content, and the Non-goals section names this specific wart ("truncates ... before the whole Usage block") as one of "two pre-existing `--help` window warts" that are "not this task's to fix," permitting widening only where a criterion needs the room (gen's AC8) — no such criterion exists for promote. Widening it anyway would be scope creep against an explicit Non-goal with no criterion requiring it, and touching the same class of pre-existing wart differently in two sibling scripts for no criterion-driven reason would itself be an inconsistency worth avoiding.
  grounding: .shell-team/specs/T-1006-lessons-resolver-key.md Non-goals "No repair of two pre-existing `--help` window warts ... truncates before `--blocks-dir` (gen) and before the whole Usage block (promote) ... neither wart is this task's to fix"; Measured inventory row `bin/playbook-promote.sh:84 | header --lessons default description | correct` (names only the header text, not the `--help` window).

- decision: In `bin/gen-playbook-blocks.sh`, the generic prose mentions of `tasks/lessons.md` at the script's opening description, the CI-freshness-step comment, the `--lessons` threat-model NOTE, and the historical Round2 misattribution comment are left unedited; only the two statements the spec's own Body-to-AC correspondence and Measured inventory tie to a concrete criterion (the `--help` default description, and the schema-rejection message) are corrected.
  reason: The Measured inventory row for these exact line numbers states the disposition as "engineer's judgment; only false statements must go" — none of the four left-alone mentions asserts that the default IS `tasks/lessons.md` (they describe the source file generically, a threat-model narrowing, or a historical incident), so none is false the way the two corrected sites were. AC12 additionally only bans the specific hardcoded-default and stale-option-description phrasings, not every generic mention of the legacy filename, so leaving these matches both the measured disposition and the acceptance criteria's own scope.
  grounding: .shell-team/specs/T-1006-lessons-resolver-key.md Measured inventory row `bin/gen-playbook-blocks.sh:3,13,25,51,86,89,103,257 | prose naming tasks/lessons.md generically | engineer's judgment; only false statements must go`; AC12's check line (bans `LESSONS="$ROOT/tasks/lessons.md"` and `default: <root>/tasks/lessons.md` specifically, not every `tasks/lessons.md` substring).

<!-- END provenance: T-1006 -->
