<!-- BEGIN provenance: T-1039 -->

- decision: when running AC15(b)'s mutation probes 1 and 2 (a one-word edit to N1's `Rule`; dropping `engineer` from N2's `Applies-to`), regenerated the four playbook blocks via `bin/gen-playbook-blocks.sh` after each corpus mutation and before checking AC8/AC9/AC10/AC12 — rather than checking those criteria against the blocks that were already committed from the pre-mutation corpus.
  reason: the probe's own bracketed prediction (mutation 1: "AC8 red on all four roles... AC12 red"; mutation 2: "AC8 red on the engineer cells, AC9/AC10 red on the engineer delta") only manifests once the mutated corpus text is actually propagated into the generated blocks and consumer agents — a corpus-only edit checked against the last-good (pre-mutation) blocks left AC8/AC9/AC10/AC12 all green in a first attempt, which would have silently under-reported the checkers' real coverage in the self-check. Two readings of the probe's plain text existed (corpus-only edit, vs. edit-then-regenerate); regenerating is the reading that actually exercises what AC8/AC9/AC10/AC12 verify (`Applies-to`-derived propagation into the shipped blocks), and is the only one that reproduces the spec's own predicted red/green shape.
  grounding: `.shell-team/specs/T-1039-promote-retro-2026-08-06.md`, `## Acceptance criteria`, AC15(b) ("change one word inside N1's Rule in the corpus (AC1 red, AC6 red, AC8 red on all four roles, AC12 red); change N2's Applies-to to drop engineer and regenerate (AC2 red, AC8 red on the engineer cells, AC9/AC10 red on the engineer delta)"); live run in a scratch `git worktree` (`git worktree add`, HEAD `b4de0f8`): corpus-only edit left AC8/AC12 PASS; re-running `bin/gen-playbook-blocks.sh` before re-checking flipped both to FAIL exactly as predicted.

- decision: for AC15(b)'s AC6-blind-spot probe (inserting one of the three entries in the middle of the corpus instead of at the end), abandoned the first construction — splicing N1's frozen block in immediately after line 1 of the base blob — after it produced an AC1 FAIL the spec's probe description does not predict, and rebuilt the probe by inserting N1's block between two existing corpus entries deeper in the file (immediately after the `## 2026-04-29 — Bootstrap` entry) instead, with N2 and N3 still appended at the file's end.
  reason: the first construction interleaved N1's heading with the base file's own header prose (lines that do not start with `## `), so AC1's single-entry extraction — which only stops at the next literal `## ` line — swallowed that unrelated prose into "N1's entry," producing an AC1 FAIL that is an artifact of the insertion point rather than of the blind spot the probe exists to demonstrate. The spec's own hedge ("AC6 red while AC1–AC5 may stay green") signals the intended construction is a genuine append-position error sitting between two well-formed entries, not a corruption of the corpus header; rebuilding it there is what actually isolates and demonstrates the blind spot named, instead of reporting a different, unintended defect as if it were the one under test.
  grounding: `.shell-team/specs/T-1039-promote-retro-2026-08-06.md`, `## Acceptance criteria`, AC15(b) ("insert one of the three entries in the middle of the corpus instead of at the end (AC6 red while AC1–AC5 may stay green — this is the probe aimed at AC6's own blind spot)"); live run in the same scratch worktree: first construction → AC1 FAIL and AC6 FAIL together; second construction (mid-file, between two well-formed entries) → AC1–AC5 all PASS, AC6 alone FAIL, matching the spec's stated shape.

<!-- END provenance: T-1039 -->
