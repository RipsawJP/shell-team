# T-1060 decision provenance

<!-- BEGIN provenance: T-1060 -->
- decision: the grammar example shown in all four new sections binds `tech-lead`/`pm-spec` to `claude opus high claude-cli`, `engineer`/`qa-verifier`/`ui-designer` to `claude sonnet - claude-cli`, and `codex-reviewer` to `codex gpt-5 - codex-cli` — a host-override example that visibly differs from `templates/binding-default.conf`'s all-`-`-effort row set, rather than transcribing the shipped default verbatim or inventing a minimal one-role example.
  reason: AC1/AC3's own positive control requires the reconstructed example to pass `bash bin/check-binding.sh --config` and to carry exactly six `bind` rows (one per role), but the spec does not pin which valid six-row config to show. Two materially different options existed: reprint `templates/binding-default.conf` verbatim (correct, but would read as "here is the file that already exists" rather than demonstrating what an *authored* override looks like), or show a config that visibly exercises the effort field's two legal shapes (`-` and a real value) and a non-default model per role, which is what a reader actually needs to see to author their own. The chosen config is observable content shipped to every adopter reading these four files.
  grounding: `.shell-team/specs/T-1060-adopter-binding-docs.md` AC1 (the `check-binding.sh --config` positive-control clause and the six-row-count equality clause); `templates/binding-template.conf`'s own header ("edit the provider/model/effort/adapter columns to assign which executor runs each ... role").
- decision: the same grammar-example fenced block (byte-identical) is reused across all four surfaces — not just within each language pair (which AC2/AC4 require), but also between the README family and the adopting-docs family, which no acceptance criterion requires.
  reason: DP8 only pins byte-identity within a language pair (README.md vs README.ja.md; docs/adopting.md vs docs/adopting.ja.md); nothing in the spec forbids two different valid examples across the two surface families, so a genuine choice existed between one shared artefact and two independently-authored ones. A single shared example means a reader who has already validated the README's example does not need to re-verify a second, differently-shaped one on the adopting page, and it halves the number of "does this specific config still validate" facts this task's own maintenance burden has to track after this merges.
  grounding: `.shell-team/specs/T-1060-adopter-binding-docs.md` DP8 ("Code blocks are byte-identical across the two languages (AC2, AC4)" — scoped to the language-pair axis only, not the surface-family axis).
- decision: the new `binding.conf.example` line in both adopting docs' `<base>/` layout trees is placed immediately before `.gitignore` (after the `test-recipe.md` continuation line), rather than near the top of the tree beside `todo.md` or `AGENTS.md`.
  reason: AC5 pins only a `+1` line-count delta and verbatim survival of every pre-existing line; it does not pin the new line's position within the tree. `binding.conf.example` is, like `AGENTS.md` and `test-recipe.md`, an inert per-repo file `team-init` scaffolds directly under `<base>/` rather than inside a subdirectory, so grouping it with that same class of entries (immediately before the final `.gitignore` line) keeps the directory-then-loose-files ordering the existing tree already uses, rather than interposing it between `todo.md` and the `loops/` subdirectory where it would break that grouping.
  grounding: `docs/adopting.md` `## Where the operating files live` (the existing tree's own ordering: directories `loops/`, `specs/`, `runs/`, `retros/`, `reviews/` first, then loose per-repo files `AGENTS.md`, `test-recipe.md`, `.gitignore`); `.shell-team/specs/T-1060-adopter-binding-docs.md` DP6 (scopes the edit to "exactly one line," silent on position).
- decision: the documented `check-binding.sh --config ...` and `resolve-executor.sh --print-resolved` commands are printed WITHOUT a `bin/` prefix (`bash check-binding.sh ...`), with a one-time parenthetical noting the `bin/`-on-`PATH` resolution and naming `bash bin/check-binding.sh ...` as the fallback for a checkout with no plugin loaded — rather than always printing the `bin/`-prefixed form.
  reason: AC12(g)'s execution-context matrix caught a real defect in the first draft: `bash bin/check-binding.sh ...`, run from an adopter-shaped repository scaffolded by `team-init` with the plugin loaded (bin/ on PATH but no local `./bin/` subdirectory), fails with "No such file or directory" (exit 127) — measured live in a scratch adopter repo. `README.md`'s own prior-art section (`## Replaying a run`) already resolves the identical two-context problem by printing the bare form as primary and reserving the `bin/`-prefixed form for commands that also reference this repository's own repo-relative fixture paths. No acceptance criterion pins either form (AC1/AC3 require only the substring `check-binding.sh`/`resolve-executor.sh`, satisfied by both forms), so which form to print was a real, uncoerced choice, and the wrong one ships a copy-pasteable command that fails in the single most common adopter context.
  grounding: `README.md` `## Replaying a run` (`bash gen-loop-replay.sh <run-id>` as the plugin-loaded primary form, `bash bin/gen-loop-replay.sh ...` reserved for the checkout-fixture case); live measurement in a `bin/team-init.sh`-scaffolded scratch repository (AC12(g) cell 2): `bash bin/check-binding.sh --config .shell-team/binding.conf` → `bash: bin/check-binding.sh: No such file or directory` (exit 127); `bash check-binding.sh --config .shell-team/binding.conf` → `check-binding: valid: ...` (exit 0).
- decision: (rework, Codex round 1 Major) the four-refusal list in both adopting docs is reordered — `contract-violation` moved from third to last — and reworded from "a role ... is bound to an adapter that does not carry a board-transition channel" (implying reachability) to "enforced for any role ... bound to an adapter that does not carry a board-transition channel. Both shipped adapters ... declare `carries board-transition`, so binding a role to either one cannot reach this refusal today; it stays part of the closed set because a future or custom adapter could declare otherwise" — rather than dropping the refusal from the list, or leaving its position and wording unchanged while only adding a caveat sentence after it.
  reason: Codex round 1's Major finding is that `contract-violation` is unreachable under the shipped two-adapter allowlist (both `claude-cli` and `codex-cli` declare `carries board-transition`), so the original wording overclaimed reachability. Three ways to close this existed: (1) drop the refusal from the documented list entirely, (2) keep it in place and merely append a caveat, or (3) reword its own claim to state contract-enforcement rather than adopter-reachability and move it to the position that reads naturally last among "things you can trigger, and the one thing you structurally cannot yet." AC7 requires the token and its exit code to still appear (not that it appear at any particular list position or with any particular framing verb), so dropping it (option 1) would fail AC7 for no benefit, and (2) leaves the overclaiming sentence itself uncorrected, which is exactly the defect reported. Reordering to last groups it with the three reachable ones by contrast rather than by false peerage.
  grounding: `.shell-team/reviews/T-1060.md` `#### Major` (`docs/adopting.md:135-141` — the reachability overclaim, confirmed independently by Codex's adversarial pass per the review's Synthesis audit ledger row 2); `templates/adapters/claude-cli.txt` / `templates/adapters/codex-cli.txt` (`carries board-transition stdout`/`carries board-transition file` — both declare it carried); `templates/binding-adapters.txt` (the two-row allowlist, closed).
- decision: (rework, Codex round 1 Minor sweep) the `agents/<role>.md` reference in both adopting docs' no-config-default paragraph is additionally qualified ("that role's own `agents/<role>.md` pin, from the plugin's own agent definitions") even though neither Minor finding named this specific site.
  reason: the sweep instruction required inventorying every other site of the same class (a path whose plugin-install-dir vs adopter-repo location is unstated) and reporting an apply/not-apply decision for each. `agents/<role>.md` is a bare relative path with no scope qualifier in the same paragraph as the already-fixed `templates/binding-default.conf` mention, and `bin/team-init.sh`'s own scaffold list (measured) never creates an `agents/` directory in an adopter's repo, so a reader could plausibly look for it in their own tree and not find it — the same shape of gap as the two named Minors, just not named. Fixing it is a small, uncoerced addition; leaving it unfixed after having just fixed its sentence-mate for the identical reason would be inconsistent.
  grounding: `bin/team-init.sh` `Usage`/scaffold list (measured: creates `todo.md`, `loops/`, `runs/`, `retros/`, `reviews/`, `specs/`, `provenance/`, `interventions/`, `AGENTS.md`, `test-recipe.md`, `.gitignore`, `binding.conf.example` — no `agents/`); the coordinator's rework instruction #2 ("Sweep both languages for any OTHER same-class site ... and report each site with an apply/not-apply decision").
- decision: (rework, Codex round 1 Minor sweep, not-apply) the parenthetical `(<base> resolves via bin/team-paths.sh --get base)` — present in all four surfaces — is left unchanged, even though the literal command would fail (exit 127) if run verbatim from an adopter-shaped repo with no local `./bin/` (measured live, same failure mode as the pre-round-1 `bin/check-binding.sh` defect).
  reason: this parenthetical is descriptive prose naming a resolution mechanism, not a runnable instruction — the established, pre-existing convention across this repository's own docs (`README.md`'s `## Replaying a run`: "`<runs>` is whatever `bin/team-paths.sh --get runs` resolves"; `README.md`'s `## Design choices`: "resolved by `bin/team-paths.sh`"; `docs/adopting.md`'s own pre-existing line 17: "the resolver `bin/team-paths.sh` decides which layout is in effect") already cites this exact script with the `bin/` prefix in identical descriptive, non-runnable contexts, deliberately distinct from the actual runnable commands in the same sections (which do get the bare-name-plus-fallback treatment, per AC12(g)). Rewriting only this instance to a bare form would contradict the repository's own established convention for no reader benefit, since no instruction anywhere tells the reader to type this parenthetical.
  grounding: `README.md` `## Replaying a run` and `## Design choices` (pre-existing, unedited by this task); `docs/adopting.md` line 17 (pre-existing, unedited by this task); live measurement: `bin/team-paths.sh --get base`, run from a `team-init`-scaffolded scratch adopter repo with no local `./bin/`, fails `no such file or directory: bin/team-paths.sh` (exit 127), while the bare `team-paths.sh --get base` (relying on `PATH`) succeeds — confirming the mechanism this convention already accounts for.
- decision: (rework, Codex round 2 Major) the `binding-unresolved` bullet in both adopting docs is broadened to state two ordinary-edit causes (the non-regular-file occupant, already documented, and a malformed `bind` row — wrong field count or an unrecognized provider/adapter/role token) and to name that `resolve-executor.sh` folds both into the one token while `check-binding.sh` itself reports the more specific reason — rather than additionally documenting the header's third sub-case (a malformed delegated canonical form, or a channel/authority/capability value outside its own adapter-definition-internal closed set).
  reason: Codex round 2's Major finding is that the bullet covered only the non-regular-file case, while `bin/resolve-executor.sh`'s own header states `binding-unresolved` also covers "a malformed delegated canonical form, a row whose field count is wrong, a value outside its governing closed set ... AND a non-regular-file occupant." Of the header's causes, only "a row whose field count is wrong" (an ordinary `bind`-row typo) is something an adopter's own `<base>/binding.conf` edit can produce — the "malformed delegated canonical form" and "channel token not declared by the contract" sub-cases describe internal consistency between the delegated `check-binding.sh --print-binding` output and the bound adapter's own definition file, neither of which an adopter edits per this task's own scope (adapter definitions are plugin-shipped, never adopter-facing). Documenting the full technical sub-case would describe a state no `<base>/binding.conf` edit reaches, the same class of over-specification the round-1 Major fix on `contract-violation` was correcting in the opposite direction; naming the one adopter-reachable sub-case the header actually documents closes the gap without introducing a new one.
  grounding: `.shell-team/reviews/T-1060.md` `## Round 2` `#### Major` (`docs/adopting.md:146-149` — the description-scope gap); `bin/resolve-executor.sh` header (`binding-unresolved (2)` — "this covers a malformed delegated canonical form, a row whose field count is wrong, a value outside its governing closed set ... AND a non-regular-file occupant"); `bin/resolve-executor.sh`'s delegation call (`if ! BINDING_OUT="$(bash "$SCRIPT_DIR/check-binding.sh" --config "$CONFIG_PATH" --print-binding ...)"; then refuse binding-unresolved 2 ...` — confirming any `check-binding.sh` failure, regardless of its own specific token, folds into this one token when reached via `resolve-executor.sh`).
- decision: (rework, Codex round 2 Minor) the `contract-violation` bullet's closing clause is corrected from "a future or custom adapter could declare otherwise" to "a future shipped adapter could declare otherwise" with no added parenthetical explaining why custom adapters are out of scope, rather than additionally citing `templates/binding-adapters.txt`'s own out-of-scope statement or `check-binding.sh --adapters`' testing-affordance status inline.
  reason: Codex round 2's Minor finding is that "custom adapter" implies an adopter extension path that does not exist (`templates/binding-adapters.txt` states custom adapters are out of scope, and `--adapters` is a testing affordance `resolve-executor.sh` never forwards — confirmed live, `grep -c -- '--adapters' bin/resolve-executor.sh` returns `0`). The reviewer's own recommended correction is the minimal one-word-class swap ("a future shipped adapter"); a first draft of this fix additionally parenthesised the out-of-scope rationale inline, which was reverted before commit because it re-introduces the words "custom adapter" into the same sentence it was removing them from — precisely the phrase the finding says should not appear here — and adds a second claim (about `--adapters`) that itself would need the same reachability-derivation discipline this round's mandatory inventory applies to every refusal claim, for a sentence the reviewer's finding did not ask to expand.
  grounding: `.shell-team/reviews/T-1060.md` `## Round 2` `#### Minor / nits` (`docs/adopting.md:161-162` — the "custom adapter" finding, with the reviewer's own suggested correction named); `templates/binding-adapters.txt` ("Custom adapters are out of scope for this initial cut"); live measurement: `grep -c -- '--adapters' bin/resolve-executor.sh` → `0`.
- decision: (round 4, transcription rework) the claim→source table's row 15 (`available`, the `resolved <role> ...` / `resolved` one-line-per-role output shapes for `--role` vs `--print-resolved`) is not rendered as a dedicated example block on any of the four surfaces, even though the row explicitly permits it ("the cheapest way to let a reader see row 10 for themselves").
  reason: row 15 is `available`, not `add` — the design note's own status legend defines `available` as "grounded and true, not currently claimed, may be used but is not required," so omitting it is a conforming choice, not a gap the transcription rule forces closed. Two options existed: add a fifth (or sixth) fenced example showing `resolved codex-reviewer codex gpt-5 - codex-cli out-of-process` alongside the existing six-role config example, or rely on the prose the round already added (rows 9-12's in-process/out-of-process contrast, itself now fully stated) to carry the same fact. Adding it would require a NEW fenced block byte-identical across both languages (the row's own Bound clause), a second artifact to keep in sync on top of the existing six-row config example — for a fact the prose already states in full without one. The prose-only rendering was chosen to avoid a second maintained code block for no additional claim coverage.
  grounding: `.shell-team/specs/T-1060-adopter-binding-docs.md` `## Round-4 design note` row 15 (`Status: available`, Bound: "may be used but is not required... any code block added for it must be added byte-identically to both languages"); rows 9-12 (the in-process/out-of-process contrast those rows require, already rendered in prose on all four surfaces, making row 15's example a restatement rather than new information).
<!-- END provenance: T-1060 -->
