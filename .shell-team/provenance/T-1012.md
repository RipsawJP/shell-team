<!-- BEGIN provenance: T-1012 -->

- decision: an event row with `"event":"human"` is adapted, in the template's data layer, onto the SAME `{type:"gate", agent:"human", label:...}` shape a plain `gate` event produces, rather than introducing a new `"human"` branch into `buildStepsB`/`computeSceneB`.
  reason: the ratified ring model (`buildStepsB`) already special-cases exactly this shape — `if(e.type === "gate"){ if(e.agent === "human"){ ...settle("human", e, "accent"); } else { signal(e.agent, ...); } }` — so reusing it makes the human-layer settle work with zero changes to the ratified, out-of-scope interaction code (Non-goals: "redesigning the prototype's aesthetics or interactions" is forbidden). The alternative — adding a genuine fourth event-adapter branch and a matching `e.type === "human"` case inside `buildStepsB` — would touch code this task's Non-goals explicitly protect, for a rendering outcome that is otherwise identical.
  grounding: templates/loop-replay.html, `buildStepsB`'s `else if(e.type === "gate")` branch (unedited by this task); spec heading "D3 — the bash side injects, the template's JavaScript adapts" affordance table, row "event human | a human-layer settle, label as the action".

- decision: a `release` event's ball-settle destination (`agent`, in the adapted-event shape) is computed as `row.from != null ? row.from : "orchestrator"`, defaulting to `"orchestrator"` when the row carries no `from`.
  reason: T-1011's frozen 9-key event schema has no `agent` key on any event row at all (only `loop_id/run_id/seq/ts/kind/event/from/to/label`), and `release` is the one event id with NO required fields (D3's per-id table: handoff needs from+to, rework needs from+to+label, gate needs from+label, human needs label, release needs nothing) — so a release row may legitimately carry a `from` or may carry none. Two options existed: require `from` on every release row consumed here (rejecting one that lacks it), or fall back to a sensible default. Requiring `from` would make an already-schema-valid row (per T-1011, which this task must consume and never re-open) fail to render correctly, which contradicts "never drop a row" (D3). `orchestrator` is the correct fallback because it is the loop's own structural node through which every release conceptually flows (the ratified ring model already routes every non-ring move through `ORCH_POS`).
  grounding: bin/log-run.sh event-mode field table (release: no required fields) and bin/check-run.sh's `case "$event_val" in ... release) : ;;` (no requiredness enforced); spec Non-goals "Re-opening T-1011's schema" (forbidding an assumption that `agent` exists on event rows); templates/loop-replay.html `buildStepsB`'s `else if(e.type === "release"){ if(ballAt !== e.agent){ move(...) } ... }` branch, which needs a resolvable `e.agent`.

- decision: the payload's `source` array lists the basenames of EVERY `*.jsonl` file scanned in the resolved runs directory, not only the files that contributed at least one row of the requested run.
  reason: D2's own text ("source is the list of scanned filenames") is read two ways — "every file the scan visited" vs. "every file that turned out to contain this run" — and D1 elsewhere frames scanning as an operation over the whole directory ("Every `*.jsonl` file in the resolved directory is scanned"), which is the reading `source` most naturally continues. The all-scanned reading also matches D5's validate-before-select ordering (every scanned file is validated regardless of whether it contains the requested run), so `source` documenting exactly what was validated, not merely what was kept, is the more informative and less surprising choice for a reader diffing the payload against the runs directory.
  grounding: .shell-team/specs/T-1012-loop-replay-generator.md heading "D1 — input resolution" ("Every `*.jsonl` file in the resolved directory is scanned...") and heading "D2 — the injection is one line..." ("`source` is the list of scanned filenames (basenames only...)"); bin/gen-loop-replay.sh, the `SOURCE_ITEMS` loop (built from `SORTED_FILES`, the full scan list, before row selection narrows anything).

- decision: `TOTAL_MS` (the playback timeline's total duration) is computed as `cum > 0 ? cum : 1` rather than left as the raw accumulated sum.
  reason: an event-only run (D3's degraded shape ii, reachable when a run is interrupted before any span is recorded) contributes zero duration to `cum` (only `"work"`/span steps add to it), so the raw value would be `0`; every downstream consumer of `TOTAL_MS` (playback progress `simMs/TOTAL_MS`, the camera's distance/look-at easing) divides by it, and dividing by zero produces `NaN` that would propagate silently into the canvas transform and the transport's seek position — a "real page that renders the rows it has" (D3) failing silently is exactly the degradation-must-be-visible contract this task exists to uphold. The alternative of special-casing the whole render loop for a zero-duration timeline was rejected as a much larger, riskier change to the ratified playback code for the same outcome (a run with no spans still "plays", just instantaneously).
  grounding: .shell-team/specs/T-1012-loop-replay-generator.md heading "D3 — the bash side injects..." ("Three degraded shapes are reachable and each must produce a real page: ... (ii) event-only..."); templates/loop-replay.html, the `TOTAL_MS` assignment and its comment; `getCurrentStepB`'s `simMs / TOTAL_MS` division (unedited, pre-existing code this guard protects).

- decision: the board-status flag rail (`#bRailTrack`) is left permanently in its "empty" treatment for every real run this task can produce, rather than deriving a synthetic `flag` value from any row field (for example, inferring a `READY_FOR_*` stop from a `gate`/`human` event's `label` text).
  reason: T-1011's frozen event schema has no `flag` key anywhere in its 9-key shape, and no combination of the five event ids' required fields (from/to/label) encodes "which board status this row corresponds to" — the sample data's `flag` values were a narrative device authored for one specific retrospective, not a property telemetry actually records. Inventing a label-text heuristic (e.g., matching a `label` string against the five `READY_FOR_*` tokens) would be fragile pattern-matching over free-form operator prose with no basis in the frozen contract, and D3 already anticipates exactly this state as the ordinary case, not a bug, by freezing the `flagRailEmpty`/rail-empty visual treatment for it in the design note. Leaving `flagEvents` derived exactly as the prototype already computed it (`events.filter(e => !!e.flag)`) and adding the rail-empty note when it is empty is therefore the correct behavior for the real data this task's own frozen schema can produce, not merely the path of least resistance.
  grounding: .shell-team/specs/T-1011-telemetry-event-rows.md event row schema (9 keys: loop_id/run_id/seq/ts/kind/event/from/to/label — no `flag`); .shell-team/specs/design-note-T-1012.md heading "2. Empty/inactive flag rail" (treats a rail with zero flag-bearing rows as the state to design for, not an anomaly); templates/loop-replay.html, `flagEvents` (`events.filter(function(e){ return !!e.flag; })`, unedited) and the `rail-empty` classification in `buildRailB`.

- decision: the roster union (`RING_AGENTS`) that grants a ring position and a canvas node is expanded only from `type:"work"` (span) rows' `agent` values — never from a `handoff`/`rework` event's `from`/`to` values, which instead fall back to a text-only label via `agentLabel()` with no ring node of their own.
  reason: spec D3 freezes the union explicitly as "the six known ids ∪ every distinct `span` value in the payload" — `from`/`to` are not named in that union. `from`/`to` on a handoff/rework event are free-form per T-1011 D3 ("an adopter's phases are not this loop's phases") and are not required to also appear as a `span` value anywhere in the same run (a purely organizational id like an intake queue could appear only as a handoff source, never as a work agent) — attempting to also grant it a ring position would need a 3-D layout decision (where on the ring, at what angle) the spec never authorizes, and would silently violate the "every real work span, grouped by agent" comment already documenting `agentSpans`'s contract. The alternative (union everything from-shaped too) was rejected because it exceeds the frozen union and has no ring-layout rule to fall back on.
  grounding: .shell-team/specs/T-1012-loop-replay-generator.md heading "D3 — the bash side injects..." ("The roster is derived from the data, unioned with this loop's known roles. Ring membership = the six known ids ... ∪ every distinct `span` value in the payload"); templates/loop-replay.html, the `RING_AGENTS`-building loop (`if (e.type === "work" && e.agent && !ringSeen[e.agent])`) and the `agentLabel()` fallback used for `from`/`to` display text.

- decision: AC17's frozen `check:` line was left byte-for-byte unedited despite being independently reproduced as mechanically unsatisfiable for any non-empty `seq` list (a `sort`-adds-trailing-newline artifact makes its two compared strings structurally unequal regardless of generator behavior); the finding was recorded in a new "## Notes from engineer" section instead of altering the frozen Acceptance criteria.
  reason: per this task's own operating instruction, a believed-wrong frozen region is a BLOCKED/reported finding, never a judgment call to silently rewrite. I verified, independent of any fixture content, that `sort -n` unconditionally appends a trailing newline to non-empty output while the unsorted comparison branch never acquires one, so `got` (unsorted, `tr`-only) can never equal `want` (sorted, `sort`-then-`tr`) whenever the row count is `>= 1` — which AC17 itself also requires (`cnt -ge 4`). I additionally verified every substantive property the AC exists to test (no cross-run leakage, the cross-file row present, ascending order of the underlying multiset, row count) passes by hand and via a Node harness running the template's own adaptation logic against the real generated page, so the deliverable itself is not in question — only the check's own string-comparison construction is.
  grounding: .shell-team/specs/T-1012-loop-replay-generator.md, AC17's `check:` line (`got=$(printf '%s' "$s" | tr '\n' ' ')` vs `want=$(printf '%s' "$s" | sort -n | tr '\n' ' ')`); heading "## Notes from engineer" (this task's own addition, recording the reproduction and the recommended remediation); reproduction command (`printf '1\n2\n3\n4' | sort -n | xxd` ends in a `0a` byte; `printf '' | sort -n | wc -c` is `0`) — re-runnable on any POSIX `sort`.

- decision: this spec's frozen H8 bullet — which illustrates the personal-home-directory PII shape by quoting a literal matching example in backticks — was left byte-for-byte unedited despite independently reproducing that it trips `bin/check-pii-shapes.sh`'s own home-path pattern on the PR diff; the finding was recorded in `## Notes from engineer` instead, phrased to avoid quoting the same triggering substring a second time.
  reason: same operating rule as the AC17 finding above — a believed-wrong frozen region is reported, not silently rewritten, and H8 sits outside the intent block but is still pm-spec-authored prose I am not authorized to edit (the engineer's edit rights over an existing spec are limited to adding a `## Notes from engineer` section). I confirmed the collision is real and reproducible (`bash bin/check-pii-shapes.sh --base origin/develop` reports exactly one `FINDING` at H8's own line, against this branch), that it is currently masked inside AC27's `&&` chain by the still-missing `.shell-team/reviews/T-1012.md` (QA's file), and that it is NOT masked in the workflow's independent "check-pii-shapes on the PR diff" step, which will fail against a real PR regardless of AC27's status. I additionally re-ran the checker against my own new paragraph describing the collision, to confirm it does not itself reintroduce a second matching literal and trip the same finding.
  grounding: .shell-team/specs/T-1012-loop-replay-generator.md, the H8 bullet under `## Notes for engineer` (pm-spec-authored, unedited); `## Notes from engineer` (this task's own addition, recording the reproduction); bin/check-pii-shapes.sh, `RE_HOME_PATH_RAW` and the `KNOWN_SHAPE_PATHS` exemption list (scoped to test fixtures, not doc prose); reproduction command (`bash bin/check-pii-shapes.sh --base origin/develop` → exit 1, one `FINDING pattern=home-path` at the H8 line) — re-runnable against this branch at any time.

- decision: (rework round 1, Codex Major #2) the earlier decision to adapt `"event":"human"` onto the SAME `{type:"gate", agent:"human"}` shape a plain `gate` event produces (see the first decision in this file) is REVERSED: `human` now gets its own adapted `type:"human"`, and `buildStepsB`/the degradation classifier branch on that type directly instead of reconstructing "was this really a human event" from `agent==="human"`.
  reason: Codex round 1 found that `gate`'s `from` field is free-form (D3 — "an adopter's phases are not this loop's phases") and is a reachable input equal to the literal string `"human"` — a real gate signal, not a human-layer settle. The original shared-shape design could not tell the two apart once both collapsed onto `{type:"gate", agent:"human"}`: a `gate` event with `from:"human"` would silently render as if it were a genuine `human` settle, and the degradation classifier would double-count or miscount the event vocabulary. The first decision's own justification (reuse the ratified `if(e.agent==="human")` branch untouched, avoid a new branch) turned out to trade away exactly the information (the row's own event id) needed to make that distinction — the smaller diff was the wrong one once the conflation was reachable, not merely theoretical. The fix keeps the SAME rendering behavior for both real cases (a real `human` event still settles on the human layer; a real `gate` event, whatever its `from` value, is always a plain signal) while making the two cases structurally distinguishable again.
  grounding: templates/loop-replay.html, `adaptRow`'s `case "human":` (now `type:"human"`, no `agent` field) and `buildStepsB`'s now-separate `else if(e.type === "gate")` / `else if(e.type === "human")` branches; the degradation classifier's `distinctEventIds[e.type] = true` (no more `agent==="human"` recovery); `tests/gen-loop-replay/fixtures/hostile/shell-team.jsonl` seq 7 (`"event":"gate","from":"human"`), the regression fixture added this round; the embedded node harness's `gateHumanConflation()` check (`tests/gen-loop-replay/run.sh`), independently verified red on the pre-fix shape and green after (mutation self-check, this round).

- decision: (rework round 1, Codex Blocker #3) `worldPos(id)` gets a blanket fallback to `ORCH_POS` for any id it does not recognise (including `null`), and separately, `buildStepsB`'s `rework` branch compares against `ballAt` (the ball's actual tracked position, always valid) instead of `ringHolder` (only set once a `work` span has run, `null` before that) — rather than special-casing only the one reported repro (a `rework` arriving before any span).
  reason: Codex's primary repro was the committed `event-only` fixture — a `rework` event arriving while `ringHolder` is still `null`, which fed `null` into `move()`/`worldPos()`/`lerp3()` and threw a `TypeError` reading `.x` off `undefined`. Codex's own adversarial pass additionally found the SAME class is reachable through `handoff`/`release` events whose `from`/`to` reference an id that never appears as a `span` value (D3: "an adopter's phases are not this loop's phases" — free-form, not required to be a roster member). A rework-only fix (e.g. converting a null-`ringHolder` rework into a signal) would have closed only the reported instance and left the wider class open; the general `worldPos()` fallback closes every instance at one point, for any current or future caller, and directly implements one of the two remedies the rework request itself named ("start-from-orchestrator moves"). The `ballAt`-vs-`ringHolder` change is a second, independent improvement for the SAME branch: it is not merely a safety net (the general `worldPos()` fix alone already prevents the crash) but a correctness fix, since `ballAt` is the ball's actual last-known tracked position (initialised to `"human"`, kept valid by every branch), whereas `ringHolder` conflates "has ring work started" with "where is the ball" — using it as a move's origin could show the ball jumping from a stale or incorrect implied position even once the crash itself is fixed.
  grounding: templates/loop-replay.html, `worldPos(id)` (the `RING_POS[id] || ORCH_POS` fallback) and the `rework` branch's `if(ballAt !== e.to){ move(ballAt, e.to, ...) }`; `tests/gen-loop-replay/fixtures/event-only/shell-team.jsonl` (the committed repro: a `rework` at seq 2, before any span); the embedded node harness's crash sweep (`tests/gen-loop-replay/run.sh`), independently reproduced red (`Cannot read properties of undefined (reading 'x')` at simMs≈0.165 on a mutated copy with both fixes reverted) and green after restoring, this round.

- decision: (rework round 1, Codex Major #1) the ring's per-agent angle step is computed as `(Math.PI * 2) / RING_AGENTS.length` at layout time, rather than any fixed constant.
  reason: the prior fixed `Math.PI/3` step (2π ÷ 6) is exactly right for the six known agents and silently wrong for any larger roster — the 7th agent lands on the identical angle as the 1st, occluding it and making the 7th unfocusable via hit-testing (which returns whichever node the pointer-distance check finds first). D3's roster union (six known ids ∪ every distinct `span` value) makes a 7th+ agent reachable — the committed `adopter` fixture already does this with two adopter-shaped agents — so this is not a hypothetical scaling concern. Deriving the step from the actual roster size is the direct fix and requires no other layout change (the six-agent case is unaffected: `(2π)/6 == Math.PI/3` exactly).
  grounding: templates/loop-replay.html, `RING_ANGLE_STEP` and its use in the `RING_POS` build loop; `tests/gen-loop-replay/fixtures/adopter/my-loop.jsonl` (2, now 3 with this round's XSS-shaped addition, agents beyond the six known ids); the embedded node harness's `ringCollision()` check (`tests/gen-loop-replay/run.sh`), independently reproduced red (two exact coordinate collisions restored) on a mutated copy reverting to the fixed step, and green after restoring, this round.

- decision: (rework round 1, Codex Blocker #2) every free-form telemetry value reaching an `.innerHTML` assignment in `templates/loop-replay.html` is passed through one new `escapeHtml()` helper (or its wrapper `agentLabelHtml()`) before being concatenated into the HTML string, including values whose reachable content is already constrained by a closed enum (`verdict`, `status`) — rather than auditing each sink individually and leaving enum-constrained ones unescaped on the reasoning that they cannot carry an HTML metacharacter today.
  reason: the escaping applied at the JSON-envelope injection point (D2, `bin/gen-loop-replay.sh`'s `escape_payload`) protects only that ONE trust boundary (the inline `<script>` block cannot be broken out of); it says nothing about how this file's OWN JavaScript renders the `JSON.parse`-decoded values back into the DOM, a second, independent trust boundary Codex correctly identified as unaudited. `phase`, `model`, `from`, `to`, and any unrecognised `span` value (an adopter's own agent name — D3 requires it be rendered, never dropped) are genuinely free-form and reachable, and were the real, exploitable sinks (the `adopter` fixture's roster-legend row was Codex's own primary example). `verdict`/`status` are enum-constrained by `bin/check-run.sh` today and could not actually carry `<`/`>`/`&`, but escaping them anyway costs nothing (escaping a string with no metacharacters is a no-op) and removes the need for any future reader — or a future task re-opening T-1011's schema — to re-derive and trust that enum argument at each call site independently. Canvas `fillText()` call sites (the ring nodes, the LOD mini-card) are explicitly left un-escaped: canvas draws literal glyphs and never interprets markup, so escaping there would incorrectly show `&lt;` instead of `<` on the rendered page.
  grounding: templates/loop-replay.html, `escapeHtml()`/`agentLabelHtml()` and their use sites: `renderAgentLegend`'s unrecognised-agent row, `agentChip()`, the detail panel's from→to chip/`phase`/`flag` chip, `detailStats`'s `model` value, `showTip()`'s `label`/`model`/`verdict`/status line; `tests/gen-loop-replay/fixtures/adopter/my-loop.jsonl` seq 5 (`"span":"<img src=x onerror=alert(1)>"`) and `tests/gen-loop-replay/fixtures/hostile/shell-team.jsonl` seq 6 (`"to":"<img src=x onerror=alert(document.domain)>"`), the two proof-input fixtures added this round; the embedded node harness's `injectionProof()` check (`tests/gen-loop-replay/run.sh`), independently reproduced red (a raw `<img` present in `agentChip()`'s output) on a mutated copy with `agentLabelHtml()`'s escaping removed, and green after restoring, this round.

- decision: (rework round 1, Minor #1 disposition) `drawMiniCard()`'s single-span `verdict` line is changed to the design note's literal `field != null ? field : "—"` pattern (always shown, em-dash placeholder when null); `showTip()`'s `model`/`verdict` lines are left as conditionally-omitted-if-absent, NOT switched to the same pattern.
  reason: both reviewers (QA's own disclosed nuance, and Codex's Minor) correctly found `drawMiniCard()`'s verdict line and `showTip()`'s model/verdict lines all deviate from the design note §5's literal pattern (an omit-if-falsy guard instead of a null→"—" placeholder), though neither ever prints the literal word "null" (the core safety requirement already holds either way). `drawMiniCard()` is live code (reachable via LOD zoom / a focused node) and the fix is a one-line, side-effect-free change, so it is fixed outright rather than filed as a fast-follow. `showTip()`'s only live call site is `nodeTipData()`, which supplies exclusively `{label, outcome, agentId}` — never `model`/`tokens`/`tool_uses`/`duration_ms`/`verdict` — so applying the literal always-show pattern there would not be a null-safety fix at all: it would add five new, permanently-"—" lines to an unrelated, currently-working feature (the agent-identity hover tooltip), a cosmetic regression to that tooltip's shape rather than a fix to a reachable defect. The rework request's own escape hatch ("fix both if cheap... OR document why not") is exercised here for exactly this reason.
  grounding: templates/loop-replay.html, `drawMiniCard()`'s `T().miniCardVerdict+(e.verdict != null ? e.verdict : DASH)` line (now unconditional); `showTip()`'s `if(e.model)`/`if(e.verdict)` lines (unchanged in shape, only escaped per the Blocker #2 decision above); `nodeTipData()` (the only call site feeding `showTip()`, returning `{label, outcome, agentId}`); `.shell-team/reviews/T-1012.md` §5 (QA's original disclosed nuance) and the "Minor / nits" + "Fast-follow disposition" sections of the Codex round-1 verdict appended there.

- decision: (rework round 1, Minor #2) `bin/gen-loop-replay.sh`'s `source` array now escapes each scanned file's basename through a new `json_escape_str()` helper (mirroring `bin/log-run.sh`'s `jesc()` — backslash first, then double-quote) before wrapping it in quotes, rather than leaving the basename unescaped as before.
  reason: a `.jsonl` filename containing a literal `"` or `\` — filesystem-permitted, though never produced by this repository's own writers — could otherwise break the `source` array's JSON structure. Codex correctly characterised this as fail-safe rather than exploitable (a broken `source` array trips the page's own `JSON.parse` `catch` branch, showing the explicit error state D2/D3 already designed for that exact case) — filenames are operator/system-controlled, not telemetry content, so this is not a second injection-escaping trust boundary the way the innerHTML sinks are. Fixed anyway because it is genuinely cheap (one small helper, reused verbatim escaping logic already proven correct in `bin/log-run.sh`) and removes a corner case a future reader would otherwise have to reason through by hand.
  grounding: bin/gen-loop-replay.sh, `json_escape_str()` and its use in the `SOURCE_ITEMS` build loop; bin/log-run.sh's `jesc()` (the escaping-order precedent this copies: backslash-pairs stripped/escaped before quotes, matching `bin/check-run.sh`'s own quote-balance parity comment on the same ordering hazard).

- decision: the node-execution harness required by this round (Codex: "Add a node-harness regression that EXECUTES the adaptation over every fixture payload") is embedded as a heredoc inside `tests/gen-loop-replay/run.sh`, written to a temp file at runtime, rather than shipped as its own `tests/gen-loop-replay/node-harness.js` file.
  reason: AC28's scope-lock allow-list is frozen (intent v2) and explicitly self-describes as merge-point-scoped, with an explicit instruction not to re-derive it per rework round. A new standalone file under `tests/gen-loop-replay/` is neither on the required list nor covered by the `tests/gen-loop-replay/fixtures/**` exemption, so it would trip AC28 without a frozen-region edit — and the harness is not itself a fixture, so broadening that exemption's wording would also be a mis-fit. Embedding the (unedited, verbatim) harness source inside `tests/gen-loop-replay/run.sh` — already on the required list — satisfies the rework request's substance (a real, executed, mechanically-run regression) with zero new tracked files and zero change to AC28's frozen text, which is the smaller, non-frozen-touching move between the two options.
  grounding: tests/gen-loop-replay/run.sh, the `cat > "$HARNESS" <<'HARNESS_EOF'` block; .shell-team/specs/T-1012-loop-replay-generator.md AC28's own check: text ("This criterion is merge-point-scoped and is expected to go stale after merge... Do not... re-derive it per rework round, or otherwise try to keep it evergreen") and its scope-lock allow-list under `## Notes for engineer`.

<!-- END provenance: T-1012 -->
