# T-1065 decision provenance

<!-- BEGIN provenance: T-1065 -->
- decision: `BRIEF` is spliced into `skills/run/SKILL.md`'s step 6 immediately after the `- **Detection lens**` bullet (and `BRIEFG` into `skills/goal/SKILL.md`'s step 2 completion gate immediately after its own `- **Detection lens**` bullet), rather than at another position inside the same region — the end of the step, immediately before `- **Same-class-2 rule**`, or elsewhere.
  reason: AC3/AC4 assert only that the bullet occurs exactly once inside the named step's region (`6. **Review**` → `7. **Done**`, and `2. **Completion gate` → `3. **Bound gate`), not a specific ordinal position among the step's other bullets, so more than one insertion point satisfies both criteria and a real choice existed. The spec's `## Notes for engineer` names this exact position as "the recommended position," stating the reason (it "keeps the scope statement beside the statement of what the reviewer detects") — adopting the recommended position avoids introducing an ordering a reviewer would otherwise have to independently judge, for no cost against AC3 or AC4.
  grounding: `.shell-team/specs/T-1065-task-class-verification-pricing.md` `## Notes for engineer` ("the recommended position is immediately after the `- **Detection lens**` bullet... as a three-space-indented bullet after that region's own `- **Detection lens**` bullet") and **AC3**/**AC4**'s own `- check:` lines, which anchor the bullet to the named step's region without asserting an intra-region ordinal.
- decision: the two adopter-facing sections (`## Pricing a spec's verification by task class` / `## タスククラスによる検証の価格付け`) are spliced immediately before `## How to run` / `## 実行方法` in `docs/adopting.md` / `docs/adopting.ja.md`, rather than at another seam that also leaves every AC6-pinned heading adjacency and byte-identity intact.
  reason: AC6 asserts heading-total equality, ordinal equality and four specific adjacency pairs plus the byte-identity of `## Operating rules` / `## 運用ルール` against their base blobs — it does not pin a specific position, and the spec's own DP7 states that "any placement satisfying them is acceptable." The Notes for engineer name the pre-`How to run` seam as the one free seam between `## How to run` and `## Operating rules` (every other seam in that range is pinned by T-1060 AC3 or T-1061 AC11), so it was chosen as the only seam in that range verified in advance not to require re-deriving which adjacency pairs would still hold.
  grounding: `.shell-team/specs/T-1065-task-class-verification-pricing.md` `## Notes for engineer` ("Recommended placement for the adopter sections: immediately before `## How to run`... That is the seam the merged pins leave free") and DP7 ("The placement is stated as the adjacency pairs, heading totals and section byte-identities that merged criteria pin, so any placement satisfying them is acceptable"); confirmed by running **AC6** live post-edit (PASS, `bash bin/check-acs.sh` this spec).
- decision: the prose composing both new adopter-facing sections (beyond the four required machine tokens `- verification-class:`, `no-mechanism`, `full-population`, `read-set` and the two honesty-anchor sentences) is original wording written for this task rather than a verbatim transplant of any sentence from `TIER`, `BRIEF` or the spec body.
  reason: AC6 requires the four tokens and the two honesty-anchor sentences to appear, and requires the section's structural invariants (heading, ordinal, adjacency, neighbour byte-identity) to hold — it does not require any other sentence to match a specific source. A real choice existed between paraphrasing the frozen `TIER` block's dense single-paragraph form for adopter readability (chosen) and reproducing `TIER`'s own sentences verbatim (rejected, because `TIER` is written for the authoring role classifying a task, not for an adopter reading the page for the first time, and verbatim reuse would import `TIER`'s internal cross-references to "the item above" that have no referent on this page).
  grounding: `.shell-team/specs/T-1065-task-class-verification-pricing.md` `## Notes for engineer`'s "Section content" bullet, which lists the required tokens, required facts and the honesty anchor without prescribing sentence-level wording, and **AC6**'s own `- check:` line, which asserts token presence and structural invariants only.
<!-- END provenance: T-1065 -->
