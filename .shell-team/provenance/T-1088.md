# T-1088 decision provenance

<!-- BEGIN provenance: T-1088 -->
- decision: the `## Blast radius` mechanical derivation is expressed as one `git grep -F -l` call carrying nine `-e <path>` flags (one per path this task edits or names) wrapped in a single named `ALL` population-extraction set, plus a second `SELF` set naming this task's own spec for self-reference exclusion — rather than one `--set` per individual path (the shape T-1087's own `t1087-read-set` block used) or dropping a path to fit the tool's set-count ceiling.
  reason: `bin/derive-populations.sh --help` caps `--set` at eight per invocation, and this task edits or names nine paths (the contract source, the harness note, the briefing, five subject-01 files, and the note's own path) — one more than T-1087's own eight-path read-set derivation needed. Folding all nine into one `-e`-per-path `git grep -F -l` call inside a single named set preserves the exact same union semantics the spec's own Method paragraph prescribes ("one `git grep -l` per path... then run every criterion... union") without dropping a path (which would silently under-count the read set, the exact defect class T-1087's own interventions record disclosed) or requiring two separate derivation invocations manually unioned by hand (which would reintroduce the hand-computed-set-operation defect `.shell-team/lessons.md` disallows).
  grounding: `bin/derive-populations.sh --help`, the `--set` flag description ("two to eight required"); `.shell-team/specs/T-1088-tier2-firing.md` `## Blast radius`, "Method" paragraph ("one `git grep -l` per path this task edits or names... then run every criterion in each resulting spec at the branch point's own committed blobs and again at HEAD"); `docs/loop-engineering/default-path-firing.md`'s `t1085-assign-vs-part` derivation block, the shape precedent for a two-set union-and-bucket pattern.
- decision: **AC3**'s requirement that at least two `- reproduce:` lines citing `docs/loop-engineering/phase-multiplexing.md` or `docs/loop-engineering/tier2-subject-harness.md` each contain `git show ` is discharged by adding one standalone reproduce line citing `tier2-subject-harness.md`'s own `discard-cost-term` commensurability line (line 123, via `sed -n '123p'` over a `git show` of the evidence commit), placed immediately after the `measured-threshold` derived-figure's own `phase-multiplexing.md`-citing reproduce line — rather than folding both file citations into one compound reproduce command under a single `- derived-figure:` line, or adding a fifth `- derived-figure:` id to carry the second citation.
  reason: **AC3**'s own check line asserts the family total of `- derived-figure:` lines equals exactly the count matching the closed four-id alternation, and **AC7** item (g)'s own mutation-probe list names "add a fifth `- derived-figure:` id" as an **AC3** red case — so a fifth id was foreclosed by the spec's own guard shape before this decision was made, leaving "fold into an existing derived-figure's reproduce command" and "add a standalone, non-derived-figure reproduce line" as the two live options. The standalone line was chosen because it lets the note's own prose (the "same-basis discard-term pricing" paragraph) cite the tier2-subject-harness.md source directly at the point it explains *why* the discard term is priced against a per-instance duration rather than the baseline's headline wall-clock — the same substantive point `.shell-team/specs/T-1088-tier2-firing.md`'s own frozen protocol makes in "The cost substitution" — rather than burying a second file citation inside an arithmetic command already dedicated to a different figure.
  grounding: `.shell-team/specs/T-1088-tier2-firing.md` **AC3**'s check line (`grep -E 'docs/loop-engineering/(phase-multiplexing|tier2-subject-harness)\.md' "$T/rep" > "$T/src"... test "$(grep -c . "$T/src" || true)" -ge 2`) and **AC7** item (g)'s fifth mutation ("add a fifth `- derived-figure:` id (**AC3** red)"); `docs/loop-engineering/tier2-firing.md` `## Firing protocol (frozen before execution)`, "The cost substitution" paragraph.
- decision: CI-equivalence (**AC7** item (f)) classifies a named workflow step as **applicable** only when the step's own command reads a real, committed artifact this task's diff actually changed (the board, a committed `.shell-team/interventions/*.md` file, the PR diff's PII shape, the PR's commit identity) — every "Run `<checker>` fixture suite" step is classified **inapplicable**, on the ground that this task edits no path under `bin/` or `tests/` and a fixture suite exercises the checker script against its own bundled fixtures, never against this repository's own live content.
  reason: the workflow carries 77 named steps; a per-step judgment call was needed for steps whose relationship to this diff is not self-evident from the step name alone (for example, whether "Run check-board-headings fixture suite" is inapplicable because it is fixture-only, versus applicable because this task's own board gains new headings under its own task entry). The rule adopted treats "does this step's own command read a path this diff's changed-set actually touches" as the single discriminator, applied uniformly to all 77 steps rather than judged case-by-case with no stated rule — which is what let four "Dogfood" steps (board hand-off, interventions conformance, PII diff, commit-identity diff) surface as applicable and every fixture-suite step and every unrelated-Dogfood step (check-durability's `--task T-1048`, check-playbook's lessons corpus, etc.) surface as inapplicable, reported individually rather than as one blanket sentence per **AC7** item (f)'s own requirement.
  grounding: `.github/workflows/check-handoff.yml`, the `jobs.lint.steps` list (77 named steps, enumerated via `grep -n 'name:'`); `.shell-team/specs/T-1088-tier2-firing.md` **AC7** item (f) ("every step judged inapplicable named individually together with its reason — never as one blanket sentence").
<!-- END provenance: T-1088 -->
