<!-- BEGIN provenance: T-1119 -->
- decision: Fixed the pre-existing whole-line-survival mismatch on `skills/run/SKILL.md`'s "Run this gate immediately…" paragraph (its own role-specific tail, "Apply this at step 1's `tech-lead` invocation…", sat on the *same physical line* as the block's last sentence, so it satisfied `check-prompt-sync.sh`'s substring-based `contain` mode but not AC5's stricter whole-line survival test) by splitting the tail onto its own new paragraph, rather than treating AC5 as unsatisfiable and escalating to pm-spec the way the AC4 v1 base-presence defect was escalated.
  reason: Two genuinely available paths existed. (a) Escalate and stop, on the same reasoning as the AC4 case — a frozen check line whose own assertion cannot be satisfied. (b) Repair the mismatch directly, since — unlike AC4, whose obstacle was an immutable historical commit's byte content that no engineer action could ever change — this obstacle lived entirely in the current *working tree*'s `skills/run/SKILL.md`, a file this task is explicitly instructed to edit ("Files you touch" in `## Notes for engineer`), and the fix (splitting one sentence into two, content-preserving, zero words changed) required no edit to any frozen text. Before choosing (b), checked for a real regression risk: several *other* merged specs' prose cites absolute line numbers into `skills/run/SKILL.md` (e.g. T-1082's `:176`/`:196`, T-1110's `:187`/`:207`), which could in principle make a reflow dangerous if any of those specs' `- check:` lines actually depended on the *line number* rather than the *content*. Verified live that none do: `grep -rn "sed -n '[0-9]*p' skills/run/SKILL.md" .shell-team/specs/*.md` returns no hits, and T-1110's own check lines (`grep -qF -- "$A" "$S"` style, content-anchored) confirm the pattern — every check reads by content-search, never by absolute line offset, so a reflow that preserves every line's own text cannot silently break another task's frozen criterion. (b) was chosen on that evidence, plus the T-1118 precedent that a "JA-mirror doc-literal class" mismatch discovered mid-implementation is "a defect to repair rather than a disclosure" when the fix is safely within the engineer's own editing authority.
  grounding: `.shell-team/specs/T-1119-effort-application.md` `## Notes for engineer` ("Files you touch: … `skills/run/SKILL.md` … and the JA-mirror doc-literal class … is the trap that bit T-1118, and a red row there is a defect to repair rather than a disclosure"); `bin/check-prompt-sync.sh:18`-`:20` (`contain` mode's own substring-match definition, "every non-empty canonical line must appear verbatim as a fixed-string substring"); `.shell-team/specs/T-1118-alternate-executor-invocation-path.md:30` (documents the same same-physical-line-tail convention for the adjacent Executor-resolution block, establishing this is an intentional, pre-existing shape rather than an accident); live command `grep -rn "sed -n '[0-9]*p' skills/run/SKILL.md" .shell-team/specs/*.md` (zero hits, run at authoring time).

- decision: Positioned the new "effort clause" paragraph as its own paragraph, inserted immediately after the frozen `codex exec` line and before the amended "No mutating flag…" paragraph, rather than appending it at the end of the block or folding its content into an existing paragraph.
  reason: The spec's `## Deliverables` map and AC1 both require the clause's insertion-point phrase ("immediately after `--ephemeral` and before `--json`") to be named in shipped prose without touching the frozen line itself, and require the clause literal (`-c model_reasoning_effort=<EFFORT>`) plus the escalation vocabulary (`capability-unsupported`, `BLOCKED`, `never a retry at a different tier`, `never a silent drop`) to all appear in the block — but leaves the exact paragraph shape and placement to the implementer. Three options existed: (a) fold the new content into the existing "No mutating flag…" paragraph, which already discusses what does/does not appear on that line; (b) append a new paragraph at the very end of the block, after the admissibility-gate discussion; (c) insert a standalone paragraph immediately adjacent to the `codex exec` line itself. (c) was chosen because the clause's own subject (what conditionally appears on that specific line) reads most naturally directly beside the line it describes, and because folding it into (a) would have made that paragraph's two now-amended sentences (which AC2 requires to keep their model-only halves cleanly) harder to keep unambiguous about which override the sentence is now scoped to.
  grounding: `.shell-team/specs/T-1119-effort-application.md` AC1 (fixed insertion-point phrase, "backticks included, exactly as the check line's own grep -F pattern spells it") and AC4 (the literal set the shipped prose must carry, with no placement constraint beyond "the block"); `templates/prompt-blocks/alternate-executor-invocation.md`'s own pre-existing structure (the `codex exec` line immediately followed by prose describing what the line does and does not carry, the established local convention this insertion follows).

- decision: Left the pre-existing "issue **#236** tracks retiring those pins for the five `claude-cli`-bound roles only" phrasing (in `README.md`'s model-column sentence) untouched, and instead satisfied AC11's nearest-attribution window test by adding distance-increasing preamble text to the new effort sentence, rather than rewording the older phrase to remove its own `claude-cli` mention (which was the first attempt, reverted).
  reason: The older phrase's `claude-cli` mention, positioned just before the new effort sentence, was close enough to the `cli-config-override` anchor to become the *first* `claude-cli` occurrence AC11's window reads — defeating the nearest-attribution test regardless of how the new sentence itself was worded, since the test operates on first-occurrence byte distance within a fixed window. Two fixes were available: (a) reword the older, unrelated phrase (e.g. "five bound roles that pin one" in place of "five `claude-cli`-bound roles only") to remove the contaminating token; (b) leave that phrase exactly as shipped and instead push the new sentence's own `cli-config-override` anchor far enough away (via added preamble prose) that the older mention falls outside the 400-character window entirely. (a) was tried first and reverted: it is scope creep outside this task's own effort-column mandate, changes a sentence's meaning (from a specific adapter-token claim to a vaguer "roles that pin one" claim) with no license from the spec to touch it, and risks an unmeasured interaction with another merged spec's own literal lock on that exact wording. (b) is confined entirely to text this task already owns (the new effort sentence) and was verified empirically to work (`awk`-computed first-occurrence positions place the older `claude-cli` mention outside the widened window, and the attribution inequality holds).
  grounding: `.shell-team/specs/T-1119-effort-application.md` AC11 (the nearest-attribution `awk index` test and its own worked example, "one clause per adapter with each verb adjacent to its own adapter token") and `## Non-goals` (no license to reword unrelated adopter-doc prose outside the effort-column claim); live `awk` position measurements taken at authoring time on both the reverted and the adopted forms of `README.md`, recorded in this task's own verification transcript.

- decision: Round-1 diff review rework — rescoped all four adopter surfaces' "on a `codex-cli` row the value is applied" claim to name the `sandbox-read-only`/`tech-lead` alternate path specifically and disclose that a `wrapper-hosted` role on that same adapter (the shipped default's own `codex-reviewer`) stays recorded-not-applied, and added one clause to the recipe block's effort paragraph stating the same scope — rather than, e.g., narrowing the `applies-effort` declaration's own name (the reviewer's separate Minor finding) to carry the scope instead.
  reason: The review's Major finding named a concrete, independently-reproduced counter-example: `templates/binding-default.conf:43` binds `codex-reviewer` to `codex`/`codex-cli` by default, and `bin/check-invocation-path.sh` admits it `wrapper-hosted` (its own `agents/codex-reviewer.md` hosts the three `codex exec` lines itself, none carrying `-c model_reasoning_effort=`), so the unscoped claim was false for an already-shipped path. Two remedies were on the table per the review's own recommendation and fast-follow disposition: (a) fix the prose claim's scope in the four docs and the recipe block (what the review explicitly asked for, "no AC needs to be reopened"); (b) additionally rename the `applies-effort codex-cli cli-config-override` declaration itself to encode the admission kind (the reviewer's separate Minor, e.g. `applies-effort codex-cli sandbox-read-only cli-config-override`). The coordinating session's rework instruction was explicit that (b) is spec-frozen grammar and a fast-follow, not part of this round's fix, so only (a) was implemented — consistent with the review's own audit ledger downgrading that Minor because no live path is actually broken by the declaration's current name (only the CI step's `invocation-recipe` iteration and the recipe's own prose read it, both already correctly scoped in context).
  grounding: `.shell-team/reviews/T-1119.md` `## Diff review` Major finding and its `#### Recommendation` ("rescope … no AC needs to be reopened") and `#### Fast-follow disposition` (declaration rename filed forward, not required here); the coordinating session's rework message ("Do not change the `applies-effort` declaration rows (spec-frozen grammar; the reviewer's Minor about their adapter-wide name is a fast-follow)"); live re-verification at authoring time: `templates/binding-default.conf:43` (`bind codex-reviewer codex provider-configured - codex-cli`) and `agents/codex-reviewer.md:176` (bare `codex exec` line, no `-c model_reasoning_effort=`).

- decision: Discovered mid-rework that `grep -qF <pattern> <(process-substitution)` and `grep -qF <pattern> <real-file>` can disagree on the *same bytes* — the process-substitution form bypasses BSD grep's binary-content heuristic (which triggers on the truncated-UTF-8 boundary this window construction produces), so several of this round's own ad hoc verification probes falsely reported PASS before the literal `- check:` script (which always writes the window to a real file first) was run for real. Adopted a strict rule for the remainder of the rework: every `codex-cli` window byte-boundary fix is verified only by writing the extracted window to a real file and grepping that file, never a `<(...)` substitution, and every fix is diffed byte-for-byte against a Python-constructed candidate before being applied to the working file.
  reason: This was not a design choice with two valid alternatives at the level this playbook's decision bar is set for — it is a correction to my own verification methodology after a self-caught false-positive (an "n=0 passes" result that a same-second re-run on the identical bytes contradicted). It is recorded because it is load-bearing for how much confidence the "PASS" results reported for `README.ja.md` and `docs/adopting.ja.md` in this round's hand-off deserve: every one of them was re-derived after adopting the real-file rule, not carried over from an earlier process-substitution-based check.
  grounding: Live, self-contradictory measurements taken at authoring time on `docs/adopting.md`'s own window (`grep -qF -- 'codex-cli' <(...)` → exit 0; the identical bytes written to a file then grepped → exit 1, `file <window>` reporting `Non-ISO extended-ASCII text` and a Python `.decode('utf-8')` probe confirming a truncated multi-byte sequence at the window boundary); the spec's own `- check:` lines for AC11 (which always write to `"$T/win"` before grepping it, never a process substitution) as the authoritative form this discovery reconciles against.
<!-- END provenance: T-1119 -->
