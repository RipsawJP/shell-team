<!-- BEGIN provenance: T-1033 -->

- decision: The issue #136 reword (the single `- **Why**:` line replaced in the corpus entry) was applied with an `awk` line-substitution that reads the replacement text via `getline` from a scratch file built from the spec's own `<!-- frozen-begin: W -->` block, rather than an inline `sed 's/OLD/NEW/'` (or any other) substitution that would place the frozen text's literal bytes inside a shell command line.
  reason: The frozen `W` text contains backticked fragments, an em-dash and apostrophes — exactly the character classes the spec's own Notes for engineer warn mangle under ordinary shell quoting. An `awk`+`getline`-from-file substitution never re-interpolates the replacement text through shell or `sed` metacharacter rules at all, so it structurally rules out the "single smart quote or collapsed space" failure mode the spec names, the same way T-1027's engineer pass avoided it for its own frozen splices. This affects AC4's three-way byte-identity contract directly: a mangled splice here fails AC4 in a way that looks like a corpus defect rather than a transcription defect.
  grounding: `.shell-team/specs/T-1033-promote-retro-2026-08-05.md`, "Notes for engineer" § "Traps in the criteria themselves" ("Every frozen field value carries characters that shell quoting mangles... Extract each field's bytes into a scratch file and pass them as `--rule "$(cat "$T/rule")"`..."); the same spec's AC4 `check:` line, which extracts the `W` block, the corpus line and T-1027's spec `E2` line via the identical `awk -v b='<!-- frozen-begin: … -->' ... | sed '1d;$d'` idiom and requires all three `cmp`-identical.

- decision: AC15(c)'s clause-by-clause confirmation of entry N1's `Why` field was checked against `.shell-team/reviews/T-1028.md` (Major 1, the `extract_region()` subshell/EXIT-trap tmpfile-leak finding), not against `.shell-team/reviews/T-1032.md`, even though the spec's own AC15 text names "`.shell-team/reviews/T-1032.md` and `.shell-team/interventions/T-1028.md` for the new entries" as the record set to check against.
  reason: `.shell-team/retros/2026-08-05.md`'s own `## Lesson candidates` section cites candidate 2's (N1's) source as `.shell-team/reviews/T-1028.md`（Major 1）explicitly, and a direct read of that file's Major-1 bullet (the `TMP_FILES` command-substitution subshell, "1330件超のリーク") matches N1's `Why` field clause for clause, whereas `.shell-team/reviews/T-1032.md` contains no tmpfile/EXIT-trap incident at all — it documents the "measured at `<ref>`" defect that N2's `Why` restates instead. Following the spec's literal named-source list for N1 would have left the clause unconfirmable; following the retro's own citation (which the spec elsewhere treats as the record of the candidates) let every clause be confirmed against its real origin. This affects the AC15(c) disclosure's own honesty — reporting "confirmed against T-1032.md" for N1 would have been a false claim.
  grounding: `.shell-team/retros/2026-08-05.md`, `## Lesson candidates`, the candidate-2 bullet ("Source: `.shell-team/reviews/T-1028.md`（Major 1）"); `.shell-team/reviews/T-1028.md`, the `#### Major` section's `extract_region()`/`TMP_FILES`/EXIT-trap bullet (the "1330件超" tmpfile-leak finding); `.shell-team/specs/T-1033-promote-retro-2026-08-05.md`, AC15(c)'s named-source sentence, for the mismatch itself.

- decision: Two predicted measurements this task's own spec and board entry carried forward from the freeze — the block-size warning "firing for a third role" (Notes for engineer / Assumptions) and T-1027's own post-W `check-acs.sh` shape ("10P/2F, reds AC10/AC12 only") — were re-measured rather than restated, and both came back different from the prediction (only engineer/pm-spec warn, not a third role; T-1027 measures 8P/4F with AC5/AC7 also red), and both mismatches are disclosed in the hand-off rather than corrected quietly or omitted.
  reason: The spec's own DP8 requires a red among T-1027's merge-point-scoped criteria to be "disclosed rather than repaired," and the corpus's own 2026-08-04 "factual claim is re-measured... before it is written down, inherited or relayed" entry (which this very task's N-series promotes siblings of) binds a relayed prediction to re-measurement before it is trusted. The two alternatives were: (a) report the predicted shapes as if confirmed, since they came from the coordinating session and "should" hold, or (b) measure both directly and report whatever is actually true, flagging any mismatch as a finding. (b) is what the task's own governing lesson requires, and a silent mismatch here is exactly the failure mode that lesson exists to close.
  grounding: `.shell-team/specs/T-1033-promote-retro-2026-08-05.md`, DP8 ("a red among them is disclosed rather than repaired... a mismatch between prediction and live result is a finding to reconcile before the hash is recorded, not a number to overwrite") and the Notes for engineer's block-size-warning trap; `.shell-team/lessons.md`, the `2026-08-04 — A factual claim is re-measured against the primary artifact before it is written down, inherited or relayed` entry; measured values: `bin/gen-playbook-blocks.sh`'s stderr (two warnings, `playbook-engineer.md`/`playbook-pm-spec.md`, not three) and `bash bin/check-acs.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md` (8 passed, 4 failed: AC5, AC7, AC10, AC12).

<!-- END provenance: T-1033 -->
