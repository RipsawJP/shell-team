<!-- BEGIN review: T-1038 -->

# QA verification — T-1038

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

## check-acs.sh

`CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1038-errexit-safe-pin-keying.md`
→ **14 passed, 0 failed, 1 skipped (AC15, runtime)** — matches the required shape exactly. `--dry-run` confirmed first: every check line is read-only (mktemp/scratch writes only, no writes to `bin/` or the working tree).

## Suite / exec-bit

`bash tests/errexit-safe/run.sh` → green, all four parts (counterfactual, per-checker behavioural, completeness self-audit incl. both mutation self-checks and probe hygiene, protected-content locks). `git ls-files -s tests/errexit-safe/run.sh` → `100644` at both base `2dc423b` and HEAD (unchanged). Judged against `tests/bin-exec-bit/run.sh`: that lock scans `git ls-files -s -- bin/` only, so `tests/` is structurally outside its coverage — `tests/errexit-safe/run.sh` is invoked exclusively as `bash tests/errexit-safe/run.sh` (CI's own `run:` step, `.shell-team/test-recipe.md`'s documented recipe, and every consumer spec's `- check:` line), never by bare name or `./…` execution, so 100644 is the correct, consistent mode — not a gap in T-1034's scope.

## Frozen-intent alignment

`bin/check-intent.sh` run independently against the board for T-1038 and the five other still-open v1.6.0 train specs — all `aligned`: T-1038 **v1 (`cd7970c0f3b1bd2b41f171d97bf968af48c10797`)**, T-1033 v1, T-1034 v1, T-1035 v1, T-1036 v5, T-1037 v2. Hashes match the board's records.

## Mutation replays — reproduced myself, scratch `git worktree`s only, real `bin/` never touched

Two disposable worktrees (`git worktree add --detach`, later `git worktree remove --force`); `git status --short` on the real checkout confirmed clean before, during and after both.

- **(i) Unsafe rewrite of a registered entry.** In worktree 1, rewrote `bin/check-contract.sh`'s registered `emit()` line to append `; exit 9` (a P1-shaped unsafe form), then ran the real `bash tests/errexit-safe/run.sh` against the mutated `bin/` (not just the suite's own internal probe copy). Result: both the forward (`comm -23`, M2) and reverse (`comm -13`, M1) completeness/staleness messages fired on that exact record, plus the suite's own content-rewrite/duplicate-site self-checks (expected collateral once the real `bin/` baseline itself is broken). Restored the file byte-for-byte; suite green again.
- **(ii) The decoy — a byte-identical duplicate appended to the derivation input.** In the same worktree, appended a second copy of the registered `close-out.sh` line to the end of `bin/close-out.sh` and re-ran the real suite. Measured: `2 close-out.sh:<content>` (measured-only) vs `1 close-out.sh:<content>` (declared-only) — both directions fired, and the suite's own `duplicate-site self-check` line stayed `ok` (it is testing its own internal copy, unaffected by this external mutation) while the top-level completeness/staleness audit correctly caught the real-`bin/` duplicate. This is the design's whole point: a deduplicated set difference would have absorbed this silently; the counted mechanism did not. Restored (`git checkout -- bin/close-out.sh` inside the disposable worktree only); suite green again.
- **(iii) The fragile case, byte-exact.** Compared `sed -n '468p' bin/gen-playbook-blocks.sh` against the registry's `1 gen-playbook-blocks.sh:` record via a Python byte-for-byte comparison (not a visual diff): exact match, eight leading spaces included.

## AC15(b) divergence — reproduced myself, judged

In a second scratch worktree, swapped `run.sh`'s `counted_keys()` for a deduplicating variant (`sort -u | awk '{print "1 " $0}'`) — the exact mutation AC15(b)'s frozen prose names. Measured:
- `bash tests/errexit-safe/run.sh` → **red**, specifically at its own `duplicate-site self-check` (`10 of 10 registered record(s) failed to change the measured occurrence count when duplicated`).
- `bash bin/check-acs.sh` on this spec (same worktree) → **AC4 PASS**, **AC2/AC5/AC7 PASS** (matching the spec's literal prediction for those three), but **AC6 and AC11 also went FAIL** — an expected, undisclosed-but-benign consequence, not a new gap: both of those check lines independently invoke `bash "$R"` as part of their own assertion, so a genuinely-red suite fails them too; this is orthogonal to the AC15(b) claim, which only names AC2/AC4/AC5/AC7.

**Judgment**: the disclosed divergence is accurate and is a real, structural property, not a defect. AC4's `- check:` line embeds its own self-contained `d()` counting function and never invokes `run.sh`'s `counted_keys()`/`derive_candidates()` — it is a fully independent oracle by construction, so no mutation confined to `run.sh`'s internals can move it. The spec's AC15(b) prose ("confirm AC4 goes red") is therefore imprecise about *which* artifact catches this mutation (the suite's own internal self-check, not AC4), not about *whether* the property holds — the defended behaviour (the dedup-decoy is caught) is intact, just one layer removed from where the frozen prose says it is. Routing this as a future re-freeze of the AC15(b) wording rather than an engineer patch is correct: Non-goals forbids editing merged/frozen text for any reason including "to make a red green," and this is a spec-prose-accuracy question, not an implementation defect — the reviewer keeps the veto over that disposition per the spec's own framing.

## Blast radius — measured at the final tree against the spec's D9 table

- **Red by design, confirmed directly**: T-1031's `AC6` (`grep -c '^check-handoff.sh:27:'` now 0) and `AC7` stays **green** (preservation lock, D6). T-1022's `AC11` (`close-out\.sh:[0-9]+` now 0 occurrences, below its own `-ge 6` floor) and `AC14`'s suite-half stays **green**. T-1007's and T-1008's identical `gen-playbook-blocks.sh:${N}:${L}` clause: re-derived `N=468`, `grep -qF "gen-playbook-blocks.sh:468:<content>"` → no match, confirmed red by design; the suite itself stays green.
- **Measure, do not assert — confirmed genuinely pre-existing at base, not introduced**: T-1009's byte-identity clause (vs `0906862`) and T-1021's `AC17` (vs `a865ec0`) both already diverged at `2dc423b` (`git diff --name-only <ref> 2dc423b -- tests/errexit-safe/run.sh` non-empty for both, before this task touched the file at all — caused by T-1022's and T-1031's own earlier edits to the same file).
- **Two newly-surfaced pre-existing base reds, independently confirmed**: T-1018's `AC18` carries an additional byte-identity clause against ref `87daf74` (untabled in T-1038's own D9 accounting) — `git diff --name-only 87daf74 2dc423b -- tests/errexit-safe/run.sh` is already non-empty at base, caused by the intervening T-1022/T-1031 commits between `87daf74` and `2dc423b`; genuinely pre-existing, not introduced here. T-1017's allow-listed-changed-files clause (base `e18e50d`) — the set of top-level `tests/*/run.sh` files changed since `e18e50d` is **identical** at base `2dc423b` and at HEAD (both list the same 11 files including several unrelated later tasks' suites), so T-1038's own edit to `tests/errexit-safe/run.sh` — already on that clause's allow-list — adds nothing to, and does not worsen, this pre-existing break. Both are correctly recorded as issue candidates rather than chased, consistent with D9/Non-goals.
- **Green, confirmed**: T-1031's `AC7`; the suite-exits-0 population (all consumers share the one dependency `bash tests/errexit-safe/run.sh`, confirmed green above).

## Records

`bin/check-provenance.sh .shell-team/provenance/T-1038.md` → conformant, 4 decision entries. `bin/check-interventions.sh --task T-1038 -- .shell-team/interventions/T-1038.md` → conformant, 1 entry. The one intervention entry is a producer-side (engineer) self-disclosure of the AC15(b) measured-vs-predicted divergence rather than an operator-facing course correction — a boundary deviation worth naming for the reviewer, but the record itself is structurally conformant and its content is truthful (independently reproduced above), so it is not treated as a gate failure.

## Suites / CI

- Full CI-wired list (`.github/workflows/check-handoff.yml`, all `run:` steps run locally in workflow order, adapting the PR-diff steps' `origin/${GITHUB_BASE_REF:-develop}` to the local `develop` branch): all green. Note on method, disclosed rather than hidden: my first bulk pass accidentally ran two full copies of the 60-step list concurrently, and three suites sharing fixed (non-`mktemp`) scratch paths (`check-playbook`, `gen-playbook-blocks`, `playbook-promote`) plus `retro-inputs/invariants.sh` showed transient, non-reproducible failures from that self-inflicted race — each was individually re-run serially afterwards and passed clean, confirming the CI list is genuinely green and the earlier failures were my own tooling artifact, not a defect in this task's diff (none of those four suites/files are anywhere in T-1038's changed-file set).
- Both directory/glob-enumerating steps run rather than reasoned about: the `gen-playbook-blocks` scratch-regeneration dogfood (clean diff) and the `check-interventions` glob dogfood (`found=36`, matching the board's claim — up from 35 pre-task, the +1 being this task's own new `.shell-team/interventions/T-1038.md`).
- `shellcheck --version` confirmed `0.11.0` (matches the CI pin); the full CI multi-file argument list including `tests/errexit-safe/run.sh` → clean.
- `bin/check-prompt-sync.sh`, `bin/check-pii-shapes.sh --base 2dc423b`, `bash bin/check-board-headings.sh .shell-team/todo.md --base 2dc423b`, and `bin/check-handoff.sh .shell-team/todo.md` all exit 0.

## Claim-fidelity — `## Summarized sources` vs primary bytes

- `tests/errexit-safe/run.sh` — read in full. Confirmed: part markers `# (i)`/`# (ii)`/`# (iii)`/`# (iv)` each occur once at top level, and every `NOT_APPLY`/heredoc/`comm` occurrence is physically inside part (iii)'s span (including its nested mutation-probe sub-sections) — parts (i), (ii) and (iv) carry zero registry-shaped lines, confirmed by direct `awk` state-tracking over the real file, not by re-reading the spec's own claim.
- `bin/` as committed — re-derived directly with the suite's own three shapes: P1=0, P2=10, P3(unguarded)=0, matching exactly. The ten hit sites and their exact line numbers (`check-acs.sh` 234/260/294, `check-board-headings.sh` 319/329, `check-contract.sh` 37, `close-out.sh` 444, `gen-playbook-blocks.sh` 468, plus `check-handoff.sh` 27/79) all confirmed by direct `grep -n` against the real files. Zero within-file content collisions confirmed via AC1's own `uniq -d` clause (already exercised live above).
- `.shell-team/test-recipe.md` — `git show 2dc423b:.shell-team/test-recipe.md | grep -cin 'file:line'` → exactly **2**, both inside the T-1008/T-1016 region — matches the spec's claim exactly.
- `.shell-team/specs/T-1031-check-handoff-flag-anchor.md` — AC6/AC7 check lines re-run directly (see Blast radius above); asymmetry (AC6 red, AC7 green) confirmed exactly as summarized.
- `.shell-team/specs/T-1022-close-out-gate-symmetry.md` — AC11/AC14 check lines re-run directly (see Blast radius above); confirmed exactly as summarized.
- `.shell-team/specs/T-1007-scope-typed-ledger.md` / `T-1008-lessons-corpus-import.md` — the identical `gen-playbook-blocks.sh:${N}:${L}` clause re-run directly by hand (see Blast radius above); confirmed red by design in both.
- `.shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md` / `T-1021-arith-base10-audit.md` — the byte-identity clauses re-derived directly against their own named base refs (see Blast radius above); confirmed pre-existing at base, matching "may already be red" exactly.
- `.github/workflows/check-handoff.yml` — confirmed `tests/errexit-safe/run.sh` occurs exactly twice: once in the single-line shellcheck argument list, once as its own `run:` step — matches exactly.
- `.shell-team/specs/T-1035-spec-template-staleness-locks.md` — the `- stale-at:` grammar ("an indented sub-bullet placed immediately after that criterion's own `- check:` line") read directly at its D2/DP-A prose; matches, and T-1038's own AC1 uses exactly this shape.
- **GitHub issue #123 — fetched directly via `curl https://api.github.com/repos/RipsawJP/shell-team/issues/123` this session (network reachable for plain HTTPS even though the `gh` CLI's own TLS path fails in this sandbox) — not `environmentally-unverified` for this role.** Body read in full: the Codex-review/tech-lead-map/Non-goals triple-independent-recommendation claim, the seven-task re-measurement list, the six-coupled-site `close-out.sh` pin description, and refs #98/#101 all match the spec's relay exactly. The issue itself offers two candidate keying schemes (`file:content` or `file:anchor-regex`); the spec's Summarized-sources bullet names only the first, but this is not a flattening — D1's Settled-decisions prose (the frozen, load-bearing location) explicitly considers and rejects the anchor-regex alternative on its own merits ("an anchor+content key needs an anchor that is itself a position pin under another name"), so the distinction is preserved where it is actually load-bearing.

## Edge cases / adversarial fixtures tried

- **Class 1 (beyond happy-path)**: mutation (i)/(ii) above are payload-shape mutations crossing the exact boundary the registry defends (unsafe rewrite; multiplicity collision), asserting the final defended state (both `comm` directions) rather than that a mechanism merely fired. Filesystem/environment-state and resource/budget sub-axes are structurally inapplicable here (no config-path or budget surface in this artifact); time/liveness (a hanging command) is inapplicable — the suite has no external/network call to hang on.
- **Class 2 (regex/charset anchoring, collision with the invariant's own vocabulary)**: confirmed a legal-input collision does not false-positive — `1 foo.sh:123 starts with digits` (a count field legitimately followed by content that begins with a digit run) does not match AC1's negative shape `^[0-9]+ [A-Za-z0-9._-]+:[0-9]+:` (no second colon), so the grammar is anchored correctly and would not wrongly flag a legitimate record. Confirmed AC12's extinction sweep is genuinely non-vacuous (10+ hits at base, 0 at HEAD, not a dead pattern silently passing on both sides).
- **Class 3 (CommonMark/markdown structure)**: not applicable — the touched artifacts are a bash script and a flat markdown bullet-list recipe file with no fenced-block or heading-variant parsing in play.
- **Class 4 (test-harness short-circuit)**: ran the full CI-wired step list verbatim (see Suites/CI above, including the self-disclosed race-condition false-failure and its resolution) rather than trusting the hand-off's self-report.

## Risk notes for reviewer

1. The AC15(b) frozen-prose imprecision (predicts "AC4 goes red"; measured is "the suite's own duplicate-site self-check goes red, AC4 stays green") is a spec-wording question for a future re-freeze, not an implementation defect — see judgment above. The reviewer keeps the veto over that disposition per AC15(e).
2. Two newly-surfaced pre-existing base reds (T-1018 vs `87daf74`, T-1017's allow-list vs `e18e50d`) are correctly disclosed as issue candidates for next-planning-input rather than chased in this diff — both independently confirmed genuinely pre-existing at `2dc423b`, not worsened by this task.
3. D9's mandatory disclosure item (T-1031 AC6, T-1022 AC11, T-1007/T-1008's identical clause, all red by design and untouched) is surfaced to this round per the spec's own framing; the reviewer keeps an independent veto over that disposition too.

## Acceptance criteria: 14/14 checked (all 14 check-line criteria PASS; AC15 runtime disclosure independently reproduced/judged above)
## Tests: 14 passed / 0 failed / 1 skipped (check-acs.sh); full CI-wired step list green (see Suites/CI note on the self-corrected concurrency artifact); `tests/errexit-safe/run.sh` green standalone and inside the full CI run

<!-- END review: T-1038 -->

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1038
- Codex model: gpt-5-codex (via `codex exec`, codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 7 files changed, +758/-128 lines (`tests/errexit-safe/run.sh` +254/-73; `.shell-team/test-recipe.md` +67/-42; plus new spec/provenance/interventions/board records) — base `2dc423b`

#### Blockers
- None.

#### Major
- **`tests/errexit-safe/run.sh:475-476`** — the D3(i) content-rewrite self-check hard-codes `before_n != "1"` as its precondition for every registered record, rather than comparing against that record's own **declared** count parsed from `rec`. The spec's own "Loss 2" section and its `## Input space` "Reachable input classes" bullet explicitly frame a future registry record with a **declared count of 2** (two byte-identical, individually-reviewed exempt sites collapsed to one key) as a legitimate, anticipated, in-scope state — not a synthetic extreme. The moment such a record is added, this probe's precondition check fails for that record on every run, permanently red-lighting the suite's own `content-rewrite self-check` (and therefore the whole suite) until a human patches this loop, even though the actual detection mechanism (`comm -23`/`comm -13` over counted keys) would work correctly for that state. This directly contradicts the Goal sentence's claim that "the suite proves that mechanism non-vacuous in three directions" for exactly the state D1/Loss-2 designed the counting scheme to support.
  - Independently reproduced, isolated from the whole-suite run: a scratch `bin/` file with two byte-identical exempt lines (content occurring twice, as a future `2 <file>:<content>` record would legitimately declare) measures `before_n=2` against the probe's own extraction logic — which the hard-coded `!= "1"` check treats as a probe failure before any mutation is attempted.
  - Also independently found by Codex's own primary pass (`[P2] Honor declared counts in the rewrite probe`) and, at higher confidence, by the targeted adversarial pass against this exact block.
  - Today's registry has zero within-file collisions (all ten declared counts are `1`), so the suite is currently green and no shipped behaviour is broken — this is a latent defect, not a live one. Suggested fix (Codex's, independently endorsed): parse the declared count `N` from `rec`, assert `before_n == N` (not literally `1`), and adjust the rewrite/re-derivation assertions to the general `N`-count case.

#### Minor / nits
- **`tests/errexit-safe/run.sh:488`** — `derive_candidates()`'s `sed -E "s|^${dir}/||"` interpolates its `$1` argument unescaped into a sed pattern; a `$TMPDIR`/`$TMP` value containing an ERE or `|`-delimiter metacharacter would break the prefix-strip for the new probe-directory call sites this task adds. **Downgraded from the codex-native `[P2]`/major** — `out-of-input-space`: the spec's own `## Input space` "Out-of-scope synthetic extremes" explicitly declines "Adversarial `mktemp` or `$TMPDIR` behaviour (a hostile temp dir, a symlink race, a path containing whitespace, a path containing `|`)," which is exactly this trigger; the pre-existing `derive_candidates()` function already carried this fragility against `$BIN` before this task (this task only adds two new callers under the same `$TMP` root, not a new fragility class).
- **`.shell-team/test-recipe.md:391-394`** (the new T-1038 entry) — "run `tests/errexit-safe/run.sh` … and paste that output into the registry as-is" is imprecise: the suite's actual `bad()` output for a rewrite/count-mismatch mixes an explanatory sentence with the raw measured/declared rows, and for a **deletion** prints only the now-stale declared row (which should be removed, not re-pasted). A literal reading could induce exactly the kind of hand-transcription error this task's whole design (D5, "generate it, never type it") exists to prevent. Docs-only; any resulting corruption would be caught immediately by AC1/AC2/AC7 on the next `check-acs.sh` run, so this is not a silent-failure risk — just an avoidable footgun in guidance this task itself authored. Independently surfaced by Codex's primary pass on both invocations (run twice; this was the one finding that reproduced both times).
- **`tests/errexit-safe/run.sh:490-491,530-531`** — the probe loops' `fwd_n`/`rev_n` checks assert only "at least one line differs," not that the specific expected forward/reverse record for *this* iteration's key appears. Given AC2 already guarantees the pre-mutation baseline is byte-identical between the registry and a live re-derivation (verified independently: `check-acs.sh` AC2 PASS), a coincidental unrelated mismatch masking a real miss is not currently reachable, so this is a defense-in-depth hardening suggestion, not a live gap. **Downgraded from the adversarial pass's `major`.**
- **`tests/errexit-safe/run.sh:469,521` / `375`** — the adversarial pass raised a path-traversal/symlink concern (`target="$probe_dir/$rw_file"` built from a registry-derived file field with no containment check) and a consequent `NOT_APPLY_ITER_FILE`/`NOT_APPLY_FILE` divergence scenario via a malformed `../not-apply.txt`-shaped file field. **Downgraded from `major` to minor, `out-of-input-space`**: the registry is static, committed, human-reviewed source text, not runtime/attacker input, and AC1 itself pins the record grammar to `^[0-9]+ [A-Za-z0-9._-]+:` — a character class that excludes `/` (and space, and colon) outright, per the spec's own `## Input space` "File names match `[A-Za-z0-9._-]+` and contain neither a space nor a colon … asserted, not assumed" and the adjacent Out-of-scope bullet excluding a file name containing a space or colon. A record shaped to traverse `../` would already fail AC1 before this code path is ever reached with attacker-meaningful content.

#### Adversarial findings
- Targeted `codex exec` devil's-advocate pass against the D3 mutation-self-check block (lines ~440-560) and the registry parsing/counting functions independently reconfirmed the Major above (content-rewrite probe's hard-coded `before_n != "1"`) as its top, highest-confidence finding, and raised three further "major"-labeled findings (loose `comm`-based probe assertions; unvalidated registry-derived file field permitting path traversal; `NOT_APPLY_ITER_FILE`/`NOT_APPLY_FILE` mid-run divergence) — all three downgraded to Minor above after evaluating reachability against AC1's own grammar lock and AC2's own baseline-equality guarantee (see Synthesis audit ledger).
- No adversarial finding survived against D2's redefinition of reverse staleness, D4's one-canonical-definition lock (independently re-verified live: all ten registry keys occur exactly once in the file), or the AC8/AC12 `file:line` extinction claims (independently re-verified live: zero occurrences of `file:line` case-insensitively and zero `<name>.sh:<digits>` tokens in the working file, both against the actual checked-out file, not the spec's account of it).
- Multiplicity-guard off-by-one edges (declared 2/measured 1 and declared 1/measured 2) reasoned through and hand-verified via `comm` set semantics: both directions of a moved count correctly fire both `comm -23` and `comm -13` as claimed — the bug found above is confined to the *self-check probe's own precondition*, not to the underlying `comm`-based judgment mechanism itself.
- A content decoy embedding a `<digits><SP>` shape inside the *content* field (e.g. `foo.sh:printf '2 evil-payload' >&2`) does not collide with the record grammar's count/file/content parse boundary — reproduced live in a scratch `bin/` copy; the count is always the actual leading token stripped by the suite's own `awk` counter, never re-derived from content bytes.
- Unreadable-file/unreadable-`bin/` mid-derivation: reasoned through the `set -euo pipefail` + `|| true` bracketing — an unreadable file silently drops its candidates from the measured set, which then surfaces as a **stale** (declared-only) entry via `comm -13` rather than a silent pass; an unreadable `bin/` entirely empties the measured set, which surfaces **all ten** entries as stale. Both fail loud, not open — this is fail-closed by construction, contrary to the initial concern in the focus areas.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `run.sh:488` — `sed -E` prefix-strip breaks on a `$TMPDIR`/`$TMP` path containing ERE/`|` metacharacters | primary: codex-native `[P2]` → major | minor, `out-of-input-space` | Spec's own Out-of-scope bullet explicitly declines "a path containing whitespace, a path containing `|`" under adversarial `mktemp`/`$TMPDIR` behaviour; the fragility also pre-dates this task in `derive_candidates()`'s existing `$BIN` call. |
| 2 | `test-recipe.md:391-394` — "paste that output into the registry as-is" is imprecise for rewrite/deletion cases | primary: codex-native `[P2]` → major (reproduced on both primary invocations) | minor | Docs-only guidance; any resulting corruption is caught immediately by AC1/AC2/AC7 on the next check-acs run, so the risk is wasted effort, not silent failure. |
| 3 | `run.sh:490-491,530-531` — probe loops assert "any nonzero diff," not the specific expected record | adversarial: major | minor | AC2 already guarantees a clean pre-mutation baseline (verified independently), so a masking coincidental mismatch is not currently reachable; hardening suggestion only. |
| 4 | `run.sh:469,521` — unvalidated registry-derived file field permits path traversal/symlink escape | adversarial: major | minor, `out-of-input-space` | AC1's own grammar (`[A-Za-z0-9._-]+`) excludes `/`, space and colon from the file field, and the registry is static committed text, not runtime input — spec's Input-space section asserts this asymmetry as fact, not assumption. |
| 5 | `run.sh:375` — `NOT_APPLY_ITER_FILE`/`NOT_APPLY_FILE` mid-run divergence via a malformed file field | adversarial: major | minor, `out-of-input-space` | Derivative of #4's same impossible premise (a registry record AC1 would already reject). |
| 6 | `run.sh:475-476` — content-rewrite probe's hard-coded `before_n != "1"` breaks a legitimate future declared-count-2 record | primary: codex-native `[P2]` → major; adversarial: major (independently, higher confidence) | **major** (unchanged) | Confirmed independently by live reproduction in an isolated scratch fixture; grounded in the spec's own Loss-2/Input-space text as a reachable, intentionally-supported state, not a synthetic extreme — no downgrade applies. |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1038 entry back to `READY_FOR_ENG` with this finding (item 6 above: `tests/errexit-safe/run.sh:475-476`, the content-rewrite probe's hard-coded single-occurrence precondition) linked. Everything else independently re-verified in this round — the position-free re-keying itself, the one-mechanism `comm -23`/`comm -13` judgment, D4's one-canonical-definition lock, the `file:line` extinction sweep, shellcheck cleanliness, the suite's four-part integrity, and the already-disclosed AC15(b) prose imprecision / blast-radius items — holds up and needs no further rework.

#### Fast-follow disposition
- Item 1 (`run.sh:488`, `$TMPDIR`-as-regex fragility) — won't-fix: explicitly out-of-scope per the spec's own Input-space declaration, and pre-existing in the unmodified `derive_candidates()` function.
- Item 2 (`test-recipe.md:391-394`, "paste as-is" wording) — file-an-issue: cheap, low-risk documentation precision fix; worth tracking so a future editor of this recipe entry tightens the wording, but not worth blocking this round's required fix over.
- Item 3 (`run.sh:490-491,530-531`, non-exact probe assertions) — file-an-issue: reasonable hardening to bundle with whatever engineer touch resolves item 6 above (both are in the same two loops), but not independently merge-blocking given AC2's existing baseline guarantee.
- Item 4/5 (`run.sh:469,521,375`, path-traversal/divergence via malformed registry field) — won't-fix: unreachable given AC1's own grammar lock on the file field; the registry is static, human-reviewed source text, not attacker-controlled runtime input.


<!-- BEGIN review: T-1038 round 2 -->

## QA verification — round 2 (post-Codex-rework re-verification, `1ffba24`)

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

Scope: this round is a focused re-verification of the fix commit
`1ffba24` (`before_n != "1"` → `before_n != "$n_decl"`, derived from
`n_decl=${rec%% *}`) that resolves Codex round-1's sole Major. Round-1's
other verified surfaces (registry records, `derive_candidates`/
`counted_keys`, the one-canonical-definition lock, the test-recipe entry)
are confirmed untouched below rather than re-audited from scratch — the
spec itself is unchanged (`check-intent.sh` reports the same v1 hash as
round 1: `cd7970c0f3b1bd2b41f171d97bf968af48c10797`).

### Three-sided count-2 replay (scratch `git worktree`s only, real checkout never touched)

Two disposable worktrees, `git worktree add --detach` at `1ffba24~1`
(pre-fix) and at `HEAD` (current), both under `$TMPDIR`. In each, the
`close-out.sh` registry record's declared count was bumped `1` → `2` in
`tests/errexit-safe/run.sh`'s NOT_APPLY heredoc, and the corresponding
source line in that worktree's own `bin/close-out.sh` was duplicated so
the file legitimately contains it twice — reproducing the exact "two
byte-identical, individually-reviewed exempt sites collapsed to one key"
state the spec's Loss-2/Input-space text names as reachable, not synthetic.

- **(a) PRE-fix code, count-2 state → suite red at the D3(i) precondition,
  confirmed:** `bash tests/errexit-safe/run.sh` in the pre-fix worktree
  exits 1 with exactly `FAIL: content-rewrite self-check: 1 of 10
  registered record(s) failed to be caught in BOTH directions when
  rewritten unsafe` — the hard-coded `before_n != "1"` rejects `before_n=2`
  before any mutation is attempted, exactly as the Major predicted.
- **(b) CURRENT (fixed) code, same count-2 state → suite green, confirmed:**
  the identical scenario in the HEAD worktree: `bash tests/errexit-safe/run.sh`
  exits 0, `errexit-safe: all assertions passed`, including `ok:
  content-rewrite self-check: … caught in BOTH directions`.
- **(c) The D3(i) probe still correctly fires on this exact count-2 record
  specifically (not just in aggregate), confirmed via targeted debug
  instrumentation** (temporary `printf … >&2` added to the loop body,
  conditioned on `rw_file = "close-out.sh"`, removed before any commit —
  never left in the tree): measured `n_decl=2 before_n=2` (precondition
  now passes for this record), and after the rewrite mutation,
  `fwd_n=1 rev_n=1` (both `comm -23`/`comm -13` directions fired for this
  specific record). Both worktrees removed (`git worktree remove --force`)
  after use; `git status --short` on the real checkout was clean before,
  during and after.

### Sibling sweep — confirmed by reading the code

- `grep -nE '"1"' tests/errexit-safe/run.sh` → **zero hits** in the current
  file (the pre-fix `!= "1"` literal is gone). A broader sweep for any
  count-shaped literal/comparison in the probe block (lines 440-570) turns
  up only: `after_n != "0"` (correct for any declared N — the awk rewrite
  program replaces *every* line equal to the old text, so after rewriting,
  zero occurrences of the old text should remain regardless of how many
  there were before; confirmed live above with `n_decl=2`), and the two
  aggregate `_checked -ge 1` / `_fails -eq 0` guards (population-level, not
  per-record). The one remaining `-eq 1"`/`"rc=1"` literal in the file
  (`unsafe_rc -eq 1` at line 133) is the errexit-clobber counterfactual —
  bash's own errexit fallback code, unrelated to registry declared counts,
  as the hand-off states.
- **D3(ii) (duplicate-site) confirmed by construction to carry no count
  precondition**: read the loop body directly (lines 519-541) — it parses
  `key`/`dup_file`/`dup_content` from `rec` but never reads or compares the
  leading count token at all; it appends the duplicate content
  unconditionally and lets the real derivation/count machinery judge the
  result. There is no `before_n`-style gate to have hard-coded in the first
  place, confirming the "D3(ii) has no count precondition by construction"
  claim.

### Full re-verification

- `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1038-errexit-safe-pin-keying.md`
  → **14 passed, 0 failed, 1 skipped (AC15)** — unchanged shape.
- `bash tests/errexit-safe/run.sh` on the real checkout → green, all four
  parts (including the content-rewrite and duplicate-site self-checks).
- `check-intent.sh` aligned for all six still-open v1.6.0 train specs: T-1038
  v1 (`cd7970c0f3b1bd2b41f171d97bf968af48c10797`, unchanged from round 1),
  T-1033 v1, T-1034 v1, T-1035 v1, T-1036 v5, T-1037 v2.
- `shellcheck --version` → `0.11.0` (CI pin); the full CI-wired multi-file
  argument list from `.github/workflows/check-handoff.yml` (60+ files,
  including `tests/errexit-safe/run.sh`) run verbatim → clean, rc=0.
- `check-pii-shapes.sh --base 2dc423b` → clean, rc=0.
- `check-board-headings.sh .shell-team/todo.md --base 2dc423b` → rc=0.
- `check-handoff.sh .shell-team/todo.md` (board lint) → rc=0.
- `check-provenance.sh .shell-team/provenance/T-1038.md` → conformant, 4
  decision entries, 0 sentinel (unchanged from round 1 — no new
  non-trivial design decision this round, matching the engineer's
  disclosure that the fix directly implements Codex's own prescribed
  repair).
- `check-interventions.sh --task T-1038 -- .shell-team/interventions/T-1038.md`
  → conformant, 1 entry, 0 sentinel (unchanged from round 1).
- `check-prompt-sync.sh` → rc=0.
- `git diff 1ffba24~1..HEAD --stat` → confined to exactly
  `tests/errexit-safe/run.sh` (+19/-6 net across two hunks) and
  `.shell-team/todo.md` — nothing else.
- `git diff --quiet -- bin/` and `git diff --quiet HEAD -- bin/` → both
  rc=0 (no `bin/` change).

### Round-1 surfaces confirmed untouched

`git diff a232b3a..HEAD --stat` (round-1 QA PASS commit to current HEAD)
touches exactly three files: `.shell-team/reviews/T-1038.md` (this
addition), `.shell-team/todo.md` (board flag), and
`tests/errexit-safe/run.sh` — and the `run.sh` diff itself is exactly the
D3(i) precondition fix quoted in "What changed" above, nothing more. The
registry records (all ten, still declared count 1 in the shipped file),
`derive_candidates()`/`counted_keys()`, the one-canonical-definition lock,
D3(ii)'s duplicate-site probe, part (iv)'s protected-content locks, and
`.shell-team/test-recipe.md` are all byte-identical to what round-1 already
verified — confirmed by the diff's absence of any hunk touching them, not
re-derived from memory.

### Edge cases / adversarial fixtures tried

Beyond the required three-sided replay (which is itself the class-1
boundary-crossing fixture this round's Major concerns — a declared count
that "just barely" passes at 1 is not what was tested; a genuinely
multi-occurrence record was): confirmed the fix generalizes rather than
merely special-casing 2 — `$n_decl` is read directly off the record's own
leading token with no hard-coded alternative value anywhere in the
replacement code (grep-confirmed above), so a hypothetical declared-count-3
record would be handled identically; not re-run live as a fourth worktree
since the mechanism has no per-N branch to distinguish 2 from 3 in the
first place.

### Acceptance criteria: 14/14 checked (unchanged from round 1; this round's diff touches no `- check:` line)
### Tests: 14 passed / 0 failed / 1 skipped (check-acs.sh); `tests/errexit-safe/run.sh` green standalone and inside the full CI-wired step list; count-2 replay run three ways (pre-fix red, current green, targeted-record instrumentation) in disposable scratch worktrees

<!-- END review: T-1038 round 2 -->
