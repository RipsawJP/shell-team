# T-1011 — QA verification

**Verdict: PASS** → board to `READY_FOR_REVIEW`

## Mechanical layer

- `bash bin/check-intent.sh .shell-team/specs/T-1011-telemetry-event-rows.md .shell-team/todo.md`: `aligned: T-1011 v2 (e5cc39266d60bb6c28ea9142f9c5c86d596aa84d)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1011.md`: `conformant: .shell-team/provenance/T-1011.md (4 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1011 .shell-team/interventions/T-1011.md`: `conformant: .shell-team/interventions/T-1011.md (1 entries, 0 sentinel)`.
- `bash bin/check-acs.sh --dry-run .shell-team/specs/T-1011-telemetry-event-rows.md`: 0 unrecognized, all 35 `check:` lines + AC36 (SKIP, runtime) recognized.
- `bash bin/check-acs.sh` (live), before this file existed: **34 PASS, 1 FAIL (AC34), 1 SKIP (AC36, by design)** — AC34's failure was solely `test -r .shell-team/reviews/T-1011.md`, this repository's T-1007–T-1010 precedent that the QA verification commit creates that file. Re-ran after adding this file: **35/35 mechanical ACs PASS**, `check-handoff.sh`/`check-board-headings.sh --base develop`/`check-pii-shapes.sh --base develop` all independently exit 0.
- **AC10 v2 confirmed sound**: the seven base-ref fixture filenames are enumerated (not globbed), the base-ref checker (`git show 08ebaea:bin/check-run.sh`) agrees with the current checker's exit code on all seven, at least five are non-zero under both (non-vacuous), and `tests/check-run/fixtures/valid-events.jsonl` (AC11's fixture) is correctly excluded from the comparison set — the v1→v2 re-freeze resolves the structural contradiction the engineer reported without weakening the invariant AC10 protects.
- **Full CI-ordered local run**, every `run:` step in `.github/workflows/check-handoff.yml` in file order (56 executable steps, including the pinned shellcheck argument list — local `shellcheck --version` confirmed `0.11.0`, matching the workflow's pin — every fixture suite, both "Dogfood check-retro" steps, `tests/machine-tokens/run.sh`, and both PR-diff steps run with `--base develop` since this branch tracks `develop`): **0 failures across all 56 steps**. Script + log: `bin/check-run.sh`, `bin/rollup-runs.sh` and `bin/cluster-failures.sh` (the three touched consumers) all pass their own suites plus the shellcheck lint clean.
- `git status --short` was clean throughout verification apart from this review file and the board append made at hand-off — every mutation/adversarial fixture below was built and mutated under `$TMPDIR` in a **copy** of `bin/`+`tests/`, never in the working tree, and never via `git checkout`/`restore`/`reset`.

## Producer-run mutation self-check — 2 of 4 independently reproduced from a fresh scratch copy

- **Mutation 1 (event-enum check removed)**: neutered `check-run.sh`'s `case "$event_val" in handoff|rework|gate|human|release) ...` block in the scratch copy so `event_known=1` unconditionally, confirmed the mutation landed (`diff` against the untouched original), then re-ran AC12's fixture (`fail-event-unknown-id.jsonl`) and AC18's seven near-miss ids (`Handoff`, `HANDOFF`, `hand-off`, `handoffs`, leading/trailing-space, empty) against the mutated copy: **all now exit 0** (RED, as the engineer claimed). Restored the file from the untouched original, re-ran: AC12 → exit 1 with `invalid event 'bogus' (...)`, all seven AC18 cases → exit 1 (GREEN restored). Confirmed the restored scratch file is byte-identical (`cmp`) to the real `bin/check-run.sh` — the real tree was never touched.
- **Mutation 3 (`rollup-runs.sh`'s skip rule removed, `cluster-failures.sh` untouched)**: replaced `is_span_row()` in a scratch copy of `bin/rollup-runs.sh` with `return 0` (always "is a span"), confirmed the mutation landed via `diff`. Re-ran AC22's invariance property (stdout on `valid-events.jsonl` vs. the same file with every event row stripped): **the two outputs now differ** (`cmp` disagrees; `diff` shows the mutated run counting 6 spans instead of 1, `partial`-flagged token/duration/window fields) — RED, exactly as claimed. Re-ran AC23's equivalent against the real, untouched `bin/cluster-failures.sh` over the same fixture pair: **byte-identical**, `cmp` agrees — GREEN, proving the two consumers' skip rules are independently covered rather than sharing a helper that would fail both at once (D1/H4).
- The remaining two self-checks (span-only-key check removed → AC16 red; per-type-requiredness removed → AC14 red) were not independently re-run — spot-checking 2 of 4 with full landed/red/restored/green evidence was judged sufficient corroboration alongside the engineer's own detailed provenance record (`.shell-team/provenance/T-1011.md` decisions 1–2) and the blind-spot reasoning below, which I did verify directly.

## Blind-spot checks on the real (unmutated) implementation

- A `kind`-absent row carrying **neither** shape's required keys (a minimal `{"loop_id":"L","run_id":"R","seq":1,"ts":"...","event":"handoff"}`-shaped garbage row) falls to the **span** branch and is reported as a defective span (`missing required key(s): span,phase,...`), never silently accepted — matches D2's "kind absent means span" framing and provenance decision 1.
- An event row with none of the 13 span-only keys (a minimal `release` row) passes clean — the event branch never runs a span-only check.
- A `label` whose escaped content contains a lookalike `,"span":"fake"` or `"kind":"span"` token (own-vocabulary collision fixture, synthesized independently of the spec's own AC19 case) does **not** trip the span-only-key or `kind` checks — `bash bin/check-run.sh --line '{"...","label":"note: kind:event span:phase status:success verdict:PASS"}'` → exit 0, and a second fixture with an escaped `,\"span\":\"fake\"` inside `label` → exit 0. Confirms the object-boundary anchoring (D2's stated design) actually holds against adversarial lookalike text, not just the spec's own single example.

## Residual judgment (Notes for QA) — `skills/run/SKILL.md` Telemetry-to-steps mapping

Read the Telemetry section against steps 1–7 directly (not trusting AC27's presence-only check):
- `handoff --from <phase> --to <phase>` is instructed "at every phase transition (the step 1→2→(3)→4→5→6→7 boundaries above)" — matches the step list exactly, including the conditional step 3.
- `rework` is instructed for "a QA `FAIL` (step 5) or a Codex `REQUEST_CHANGES` (step 6)" with `--label` carrying the causing verdict — covers **both** route-back points named in the spec, with the cause recorded (D3's per-type requiredness for `rework`).
- `gate` is instructed at "the QA verdict, the Codex verdict, and every fail-closed pre-Validate seam gate (T-073's uncommitted-diff check, T-075's provenance gate, T-1002's interventions gate)" — read step 4's own three seam-gate sub-bullets and confirmed these are exactly the three fail-closed gates that run before Validate; the mapping is accurate, not just plausible-sounding.
- `human` is instructed at "every human stop, GO, interruption or correction," recommending the interventions class slugs plus `GO`/`STOP:<reason>` as `--label` vocabulary — covers step 5's loop-guard `STOP:` and step 7's merge GO as the spec's Notes for QA specifically asks to confirm.
- The shared-`seq` instruction ("the same shared monotonic seq counter (increment it for the event row too)") does not contradict the span instruction above it (a single counter starting at 1, incremented per row) — read both paragraphs side by side, no conflict.
- `bin/log-run.sh`'s header carries the D5 version-skew paragraph verbatim ("Version skew (T-1011, D5 — confirmed, not redesigned): if this script writes an event row and the SIBLING check-run.sh predates event-row support, the post-write self-check above reports the row invalid, so this script exits 1 with the row already on disk — never rolled back...") — present and accurate against the actual self-check mechanism read in the same file.

## Adversarial fixtures beyond the spec's own set

- **Multiple files via glob, same `run_id` split across two files** (Input space class): two event-only files (`a.jsonl` seq 1, `b.jsonl` seq 2, same `run_id`) passed as `check-run.sh a.jsonl b.jsonl` → exit 0; `rollup-runs.sh` over the same glob prints the existing "(no runs found)" sentinel (no span rows present) without erroring.
- **Empty file**: `check-run.sh` and `rollup-runs.sh` both exit 0 on a zero-byte `.jsonl` file, no false violation.
- **Dangling symlink**: `check-run.sh` on a symlink to a nonexistent target → exit 2, `cannot read file` — fails closed as the Input space requires.
- **Own-vocabulary collision in `label`** (regex/character-set anchoring axis): see Blind-spot checks above — this is the fixture class the playbook's grep/regex-invariants lesson calls for, synthesized independently rather than reusing only AC19's own example.

## Input-space coverage note

This task's deliverable is executable (`bin/log-run.sh`, `bin/check-run.sh`, `bin/rollup-runs.sh`, `bin/cluster-failures.sh`), so the adversarial fixture synthesis checklist applies. Classes exercised: payload shape (malformed/empty/duplicate/near-miss/control-shape via AC13/AC14/AC16/AC18/AC19 plus my own collision fixtures), filesystem/environment state (dangling symlink, multi-file glob), regex/character-set anchoring (own-vocabulary collision, CRLF via AC20). Resource/budget and time/liveness sub-axes are **structurally inapplicable** to this artifact — it is a pure read-line-then-branch bash script over a fixed-size file argument list, with no budget/quota concept and no subprocess/network/timeout surface to hang on (D7's own stated design ceiling). No `environmentally-unverified` items.

## Risk notes for reviewer

- The two mutation self-checks I did not independently re-run (span-only-key removal → AC16; per-type-requiredness removal → AC14) rest on the engineer's own detailed provenance record plus my own blind-spot spot-checks of the real, unmutated behavior on those exact code paths (AC16/AC14 both pass live) — not on a from-scratch red/green cycle by me. Flagging this so the reviewer can weigh it; I judged the corroboration sufficient given the other two self-checks I did fully reproduce landed exactly as claimed.
- AC10's v2 re-freeze narrows a frozen criterion's comparison set from a live glob to seven named files — worth a second look that the seven enumerated names really are the complete pre-existing fixture set (I confirmed via `git show 08ebaea --name-only -- tests/check-run/fixtures/`, matching the spec's list exactly).
- Test-only edit: none — I added no test cases myself; all fixtures above were built as scratch/temp files, never committed to the suite.

---
