# T-1011 — QA verification

**Verdict: PASS** → board to `READY_FOR_REVIEW`

## Mechanical layer

- `bash bin/check-intent.sh .shell-team/specs/T-1011-telemetry-event-rows.md .shell-team/todo.md`: `aligned: T-1011 v2 (e5cc39266d60bb6c28ea9142f9c5c86d596aa84d)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1011.md`: `conformant: .shell-team/provenance/T-1011.md (4 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1011 .shell-team/interventions/T-1011.md`: `conformant: .shell-team/interventions/T-1011.md (1 entries, 0 sentinel)`.
- `bash bin/check-acs.sh --dry-run .shell-team/specs/T-1011-telemetry-event-rows.md`: 0 unrecognized, all 35 `check:` lines + AC36 (SKIP, runtime) recognized.
- `bash bin/check-acs.sh` (live), before this file existed: **34 PASS, 1 FAIL (AC34), 1 SKIP (AC36, by design)** — AC34's failure was solely `test -r .shell-team/reviews/T-1011.md`, this repository's T-1007–T-1010 precedent that the QA verification commit creates that file. Re-ran after adding this file: **35/35 mechanical ACs PASS**, `check-handoff.sh`/`check-board-headings.sh --base develop`/`check-pii-shapes.sh --base develop` all independently exit 0.
- **AC10 v2 confirmed sound**: the seven base-ref fixture filenames are enumerated (not globbed), the base-ref checker (`git show 08ebaea:bin/check-run.sh`) agrees with the current checker's exit code on all seven, at least five are non-zero under both (non-vacuous), and `tests/check-run/fixtures/valid-events.jsonl` (AC11's fixture) is correctly excluded from the comparison set — the v1→v2 re-freeze resolves the structural contradiction the engineer reported without weakening the invariant AC10 protects.
- **Full CI-ordered local run**, every `run:` step in `.github/workflows/check-handoff.yml` in file order (56 executable steps, including the pinned shellcheck argument list — local `shellcheck --version` confirmed `0.11.0`, matching the workflow's pin — every fixture suite, both "Dogfood check-retro" steps, `tests/machine-tokens/run.sh`, and both PR-diff steps run with `--base develop` since this branch tracks `develop`): **0 failures across all 56 steps**. Script + log: `bin/check-run.sh`, `bin/rollup-runs.sh` and `bin/cluster-failures.sh` (the three touched consumers) all pass their own suites plus the shellcheck lint clean.
- `git status --short` was clean throughout verification apart from this review file and the board append made at hand-off — every mutation/adversarial fixture below was built and mutated under `$TMPDIR` in a **copy** of `bin/`+`tests/`, never in the working tree, and never via `git checkout`/`restore`/`reset`.

## Producer-run mutation self-check — 2 of 4 independently reproduced from a fresh scratch copy

- **Mutation 1 (event-enum check removed)**: neutered `check-run.sh`'s `case "$event_val" in handoff|rework|gate|human|release) ...` block in the scratch copy so `event_known=1` unconditionally, confirmed the mutation landed (`diff` against the untouched original), then re-ran AC12's fixture (`fail-event-unknown-id.jsonl`) and AC18's seven near-miss ids (`Handoff`, `HANDOFF`, `hand-off`, `handoffs`, leading/trailing-space, empty) against the mutated copy: **all now exit 0** (RED, as the engineer claimed). Restored the file from the untouched original, re-ran: AC12 → exit 1 with `invalid event 'bogus' (...)`, all seven AC18 cases → exit 1 (GREEN restored). Confirmed the restored scratch file is byte-identical (`cmp`) to the real `bin/check-run.sh` — the real tree was never touched.
- **Mutation 3 (`rollup-runs.sh`'s skip rule removed, `cluster-failures.sh` untouched)**: replaced `is_span_row()` in a scratch copy of `bin/rollup-runs.sh` with `return 0` (always "is a span"), confirmed the mutation landed via `diff`. Re-ran AC22's invariance property (stdout on `valid-events.jsonl` vs. the same file with every event row stripped): **the two outputs now differ** (`cmp` disagrees; `diff` shows the mutated run counting 6 spans instead of 1, `partial`-flagged token/duration/window fields) — RED, exactly as claimed. Re-ran AC23's equivalent against the real, untouched `bin/cluster-failures.sh` over the same fixture pair: **byte-identical**, `cmp` agrees — GREEN, proving the two consumers' skip rules are independently covered rather than sharing a helper that would fail both at once (D1/H4).
- The remaining two self-checks (span-only-key check removed → AC16 red; per-type-requiredness removed → AC14 red) were not independently re-run — spot-checking 2 of 4 with full landed/red/restored/green evidence was judged sufficient corroboration alongside the engineer's own detailed provenance record (`.shell-team/provenance/T-1011.md` decisions 1–2) and the blind-spot reasoning below, which I did verify directly.

## Blind-spot checks on the real (unmutated) implementation

- A `kind`-absent row carrying **neither** shape's required keys (a minimal `{"loop_id":"L","run_id":"R","seq":1,"ts":"...","event":"handoff"}`-shaped garbage row) falls to the **span** branch and is reported as a defective span (`missing required key(s): span,phase,...`), never silently accepted — matches D2's "kind absent means span" framing and provenance decision 1.
- An event row with none of the 13 span-only keys (a minimal `release` row) passes clean — the event branch never runs a span-only check.
- A `label` whose escaped content contains a lookalike `,"span":"fake"` or `"kind":"span"` token (own-vocabulary collision fixture, synthesized independently of the spec's own AC19 case) does **not** trip the span-only-key or `kind` checks — `bash bin/check-run.sh --line '{"...","label":"note: kind:event span:phase status:success verdict:PASS"}'` → exit 0, and a second fixture with an escaped `,\"span\":\"fake\"` inside `label` → exit 0. Confirms the object-boundary anchoring (D2's stated design) actually holds against adversarial lookalike text, not just the spec's own single example.

## Residual judgment (Notes for QA) — `skills/run/SKILL.md` Telemetry-to-steps mapping

Read the Telemetry section against steps 1–7 directly (not trusting AC27's presence-only check):
- `handoff --from <phase> --to <phase>` is instructed "at every phase transition (the step 1→2→(3)→4→5→6→7 boundaries above)" — matches the step list exactly, including the conditional step 3.
- `rework` is instructed for "a QA `FAIL` (step 5) or a Codex `REQUEST_CHANGES` (step 6)" with `--label` carrying the causing verdict — covers **both** route-back points named in the spec, with the cause recorded (D3's per-type requiredness for `rework`).
- `gate` is instructed at "the QA verdict, the Codex verdict, and every fail-closed pre-Validate seam gate (T-073's uncommitted-diff check, T-075's provenance gate, T-1002's interventions gate)" — read step 4's own three seam-gate sub-bullets and confirmed these are exactly the three fail-closed gates that run before Validate; the mapping is accurate, not just plausible-sounding.
- `human` is instructed at "every human stop, GO, interruption or correction," recommending the interventions class slugs plus `GO`/`STOP:<reason>` as `--label` vocabulary — covers step 5's loop-guard `STOP:` and step 7's merge GO as the spec's Notes for QA specifically asks to confirm.
- The shared-`seq` instruction ("the same shared monotonic seq counter (increment it for the event row too)") does not contradict the span instruction above it (a single counter starting at 1, incremented per row) — read both paragraphs side by side, no conflict.
- `bin/log-run.sh`'s header carries the D5 version-skew paragraph verbatim ("Version skew (T-1011, D5 — confirmed, not redesigned): if this script writes an event row and the SIBLING check-run.sh predates event-row support, the post-write self-check above reports the row invalid, so this script exits 1 with the row already on disk — never rolled back...") — present and accurate against the actual self-check mechanism read in the same file.

## Adversarial fixtures beyond the spec's own set

- **Multiple files via glob, same `run_id` split across two files** (Input space class): two event-only files (`a.jsonl` seq 1, `b.jsonl` seq 2, same `run_id`) passed as `check-run.sh a.jsonl b.jsonl` → exit 0; `rollup-runs.sh` over the same glob prints the existing "(no runs found)" sentinel (no span rows present) without erroring.
- **Empty file**: `check-run.sh` and `rollup-runs.sh` both exit 0 on a zero-byte `.jsonl` file, no false violation.
- **Dangling symlink**: `check-run.sh` on a symlink to a nonexistent target → exit 2, `cannot read file` — fails closed as the Input space requires.
- **Own-vocabulary collision in `label`** (regex/character-set anchoring axis): see Blind-spot checks above — this is the fixture class the playbook's grep/regex-invariants lesson calls for, synthesized independently rather than reusing only AC19's own example.

## Input-space coverage note

This task's deliverable is executable (`bin/log-run.sh`, `bin/check-run.sh`, `bin/rollup-runs.sh`, `bin/cluster-failures.sh`), so the adversarial fixture synthesis checklist applies. Classes exercised: payload shape (malformed/empty/duplicate/near-miss/control-shape via AC13/AC14/AC16/AC18/AC19 plus my own collision fixtures), filesystem/environment state (dangling symlink, multi-file glob), regex/character-set anchoring (own-vocabulary collision, CRLF via AC20). Resource/budget and time/liveness sub-axes are **structurally inapplicable** to this artifact — it is a pure read-line-then-branch bash script over a fixed-size file argument list, with no budget/quota concept and no subprocess/network/timeout surface to hang on (D7's own stated design ceiling). No `environmentally-unverified` items.

## Risk notes for reviewer

- The two mutation self-checks I did not independently re-run (span-only-key removal → AC16; per-type-requiredness removal → AC14) rest on the engineer's own detailed provenance record plus my own blind-spot spot-checks of the real, unmutated behavior on those exact code paths (AC16/AC14 both pass live) — not on a from-scratch red/green cycle by me. Flagging this so the reviewer can weigh it; I judged the corroboration sufficient given the other two self-checks I did fully reproduce landed exactly as claimed.
- AC10's v2 re-freeze narrows a frozen criterion's comparison set from a live glob to seven named files — worth a second look that the seven enumerated names really are the complete pre-existing fixture set (I confirmed via `git show 08ebaea --name-only -- tests/check-run/fixtures/`, matching the spec's list exactly).
- Test-only edit: none — I added no test cases myself; all fixtures above were built as scratch/temp files, never committed to the suite.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1011
- Codex model: gpt-5-codex
- Verification mode: static-only
- Diff scope: 18 files, +954/-86 lines (`develop@08ebaea..156e775`)

Codex に対して primary パスを本タスクの diff スコープで2回実行した（1回目は取り込み前のキャプチャ処理中に取り違えて破棄しかけたが、生ファイルが偶然ディスク上に残っていたため回収し、内容を自分で再現・確認した — 詳細は下記）。2回はそれぞれ異なる、独立に検証可能な実バグを1件ずつ報告しており、両方とも私自身がこのホスト上で直接再現して確認済みなので、両方を本レビューの findings として扱う。

#### Blockers
- なし。

#### Major
- **#1 — `bin/log-run.sh:117`**（本タスクの新規コード）: 新設の `seen()` ヘルパーが `for f in "${SEEN[@]}"; do ...; done` で `SEEN=()` を無条件に展開している。macOS 既定の `/bin/bash`（このホストでも実測 `bash --version` = 3.2.57）では `set -euo pipefail` 下で空配列を `"${arr[@]}"` 展開すると `unbound variable` エラーになり、スクリプトは意図した usage エラー（exit 2・何も書き込まない）ではなく **exit 1** で異常終了する。トリガは「`<loop_id>` だけを渡し、フラグを一切付けない呼び出し」（例: `bash bin/log-run.sh shell-team`）— `--run-id` 欠落という素直な usage エラーのはずが、`seen --event` 呼び出し時点で `SEEN` が空のまま `[@]` 展開されクラッシュする。
  - **このホストで直接再現・確認済み**: `bash bin/log-run.sh shell-team` → `bin/log-run.sh: line 115: SEEN[@]: unbound variable` / exit 1（`bash bin/log-run.sh`（位置引数なし）は「missing <loop_id>」で先に die するため exit 2 になり、この分岐は隠れる）。
  - 同リポジトリの既存パターンとの不整合が根拠を補強する: `bin/rollup-runs.sh`/`bin/cluster-failures.sh` の `LINES=()` は常に `if [[ "${#LINES[@]}" -gt 0 ]]; then ... for line in "${LINES[@]}"; do` という「空配列を先にガードしてから展開する」既存の安全イディオムを踏んでいる。本タスクが新規追加した `SEEN` 配列だけがこのイディオムを外している。
  - Input space のどの out-of-scope 項目にも該当しない（bash バージョン差は宣言済み対象外クラスに含まれない）。CI（Ubuntu の新しい GNU bash は空配列展開で nounset エラーにならない）はこの回帰を検出できず、35 件の `check:` もこの「フラグ皆無」形状を突いていない — 機械的 AC・CI 双方の盲点。
  - **提案**: `seen()` の先頭で `[[ "${#SEEN[@]}" -eq 0 ]] && return 1` を追加する。あわせて `bash bin/log-run.sh <loop_id>`（フラグなし）を回帰テストに追加する。
- **#2 — `bin/check-run.sh:189-196`**（既存コード・本タスク以前から存在する欠陥、`08ebaea` の時点で既に再現することを確認済み）: shape-independent の unbalanced-quote チェックが `unq="${line//\\\"/}"` で 2 文字シーケンス `\"`（バックスラッシュ+クォート）を単純な部分文字列除去で剥がしている。値がバックスラッシュ1文字で終わる場合（`jesc` はこれを正しく `\\` にエスケープする — 妥当な JSON）、実際の行末は `...\\"`（エスケープされたバックスラッシュ＋本物の閉じクォート）になるが、この単純除去はこの中の `\"` 部分（2文字目のバックスラッシュ＋閉じクォート）も「エスケープされたクォート」と誤認して剥がしてしまい、残るクォート個数の偶奇判定が狂って **正当な行が `unbalanced double-quotes (truncated string?)` として誤検出**される。
  - **このホストで直接再現・確認済み**（span 側・event 側の両方で）: `TEAM_RUNS_DIR= RUNS_DIR="$d" bash bin/log-run.sh L --run-id R --seq 1 --event human --label $'trailing backslash\\'` → 書き込まれた行は `..."label":"trailing backslash\\"}`（妥当な JSON）だが、`log-run.sh` 自身の post-write self-check が失敗して **exit 1**、以後 `bash bin/check-run.sh <file>` を実行するたびにこの行が恒久的に「unbalanced double-quotes」で違反報告され続ける。**base ref `08ebaea` の未修正 `bin/check-run.sh`/`bin/log-run.sh`（span 側の `--error` フィールドで同じ末尾バックスラッシュ）でも同一の誤検出が再現する**ことを別途確認済み — 本タスクの diff が原因ではなく、既存の欠陥である。
  - ただし Input space は `label`/`from`/`to`/`error` に「backslashes」を明示的にリーチャブルな内容として挙げており（"embedded double quotes, backslashes, and tabs/CR/LF"）、Non-goals/D7 が除外する「フルJSON文法」領域の話ではなく、この既存チェック自身が意図している「エスケープを認識した引用符バランス判定」機能そのもののバグである。本タスクはこの検証機構をまさに拡張・強化する task であり、event 行は `from`/`to`/`label` という自由記述フィールドを3つ持つため（span 行は `error` 1つのみ）、同じ既存バグの露出面が実質的に増える。
  - **提案**: 剥がす対象を「エスケープされたクォート」ではなく「クォート直前の連続バックスラッシュの個数の偶奇」で判定するよう書き換える（例: 各 `"` の直前の連続 `\` 数を数え、偶数なら real delimiter、奇数ならエスケープ済みとする）。既存の pre-existing バグではあるが、この検証機構の拡張タスクという性質上、Major #1 と同一ラウンドで併せて直すことを推奨する。

#### Minor / nits
- `bin/check-run.sh` の `kind`/`event` 判別（discriminator の正規表現）は、キーとコロンの間に空白を挟む形（`"kind" : "bogus"`）や同一オブジェクト内の重複 `kind` キーをそのまま通せば誤判定しうる、と adversarial パスが指摘した。ただしこれは spec の Input space の Out-of-scope 一覧が明示的に除外する「whitespace between a key and its colon」「duplicate keys within one object」に厳密に該当し、D7 の残差宣言（正規のライターは常にコンパクトな出力しかしない）とも一致する。攻撃者が任意送信できるペイロードでもトラストバウンダリを跨ぐ入力でもないため、`out-of-input-space` として Minor に格下げする（監査ledger参照）。
- `bin/rollup-runs.sh`/`bin/cluster-failures.sh` の `is_span_row` は H4 により意図的に独立コピーであり、現状は完全に同一実装で挙動も一致している（adversarial パスでも "currently identical... do not disagree today" と確認済み）。将来どちらか一方だけが修正されて乖離するリスクへの forward-looking な指摘であり、現時点のバグではない。

#### Adversarial findings
- Devil's-advocate パスを `bin/log-run.sh` の `SEEN` 配列、`bin/check-run.sh` の discriminator、`bin/rollup-runs.sh`/`bin/cluster-failures.sh` の `is_span_row` 独立コピーの3点に絞って実行。Major #1（bash 3.2 unbound-variable クラッシュ）を独立に再確認（severity "medium" と自己申告、P2 相当）。加えて上記2件の Minor（discriminator の空白/重複キー・二重実装の並行乖離リスク）を新規に指摘。Major #2（バックスラッシュ末尾の引用符バランス誤検出）は別の primary パス実行で単独報告され（[P2]、"largely sound, but..."）、adversarial パスの対象には含めていないが、私自身が直接再現して確認済みのため本レビューに独立して計上する。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `bin/check-run.sh` discriminator: whitespace-around-colon / duplicate `kind` key | adversarial: medium（P2 相当 → major） | minor | out-of-input-space — spec Input space の Out-of-scope 一覧「Full JSON-grammar conformance beyond the structural lint: … duplicate keys within one object, whitespace between a key and its colon.」に直接該当し、D7 の残差宣言とも整合するため minor に格下げ。攻撃者到達可能なセキュリティ/トラストバウンダリ系ではないため格下げ可。 |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_ENG` に戻し、上記 Major #1・#2 をリンクする。#1 は `seen()` への空配列ガード追加、#2 は既存の unbalanced-quote チェックのバックスラッシュ偶奇判定への書き換え — いずれも D1–D8 のどの凍結決定にも触れない狭いバグ修正であり、spec の再凍結は不要と見込む（#2 は本タスク以前からの欠陥だが、この検証機構自体を拡張するタスクという性質上、同一ラウンドでの修正を推奨する）。

#### Fast-follow disposition
- `bin/check-run.sh` discriminator の空白/重複キー耐性（Minor、上記ledger参照） — won't-fix: spec の Input space が明示的に out-of-scope としている合成極端系であり、D7・Non-goals（"Turning `check-run.sh` into a JSON parser"）とも一致するため、追跡 issue は起票しない。
- `bin/rollup-runs.sh`/`bin/cluster-failures.sh` の `is_span_row` 独立コピー間の将来的な乖離リスク — file-an-issue: H4 により共有ヘルパー化はできない設計だが、両者を同一の discriminator フィクスチャ集合（正常/absent/unknown/malformed/whitespace/duplicate 等）に対して回帰させる軽量テストを追加すれば、将来どちらかだけ修正されて挙動が割れる事故を安く防げる。現時点でバグではないため merge のブロッカーにはしない。

#### Other notes
- 一次キャプチャの取り扱いについて透明性のため記録する: 1回目の `codex exec ... review --base develop --json -o <raw>` 実行が私の待機タイムアウトの都合で `--publish` 呼び出し時点ではまだ出力ファイルを書き終えておらず、`codex-capture.sh --publish` が validation failure（exit 3）でその回の生ファイルを削除した。しかし codex プロセス自体はその後も実行を続けて完了しており、削除後に同じパスへ最終出力（822 バイト、Major #2 の内容）を書き込んでいたため、そのファイルは "orphan"（未 publish のまま残存）として発見できた。内容を読み、そこに書かれたバグを自分で直接再現して確認した上で本レビューに正式に組み込んでいる — 推測や伝聞ではなく実機再現によるものである。2回目の primary パス（正しく `--alloc`/`--publish` サイクルを完了）は Major #1 を報告し、こちらも同様に再現確認済み。両方の生ペアは正規名 `T-1011-codex-primary.{txt,jsonl}` / `T-1011-codex-adversarial.{txt,jsonl}` として reviews dir に publish 済み。

補足（issue-canon reliance）: 本レビューの判定は spec が引用する GitHub issue #77 の記述や、板エントリが記録する Part 1/2/3 の切り分けそのものには依存していない（すべて spec 自身と実装コードの内的整合性のみで検証可能）。念のため明記する: **internal consistency verified / issue-canon match environmentally-unverified（orchestrator の一次確認に依拠）**。

---

# T-1011 — QA delta verification (rework round 2)

**Verdict: PASS** → board to `READY_FOR_REVIEW`

Scope: Codex round-1 `REQUEST_CHANGES` (two Majors — #1 `bin/log-run.sh`'s `seen()` helper crashed exit 1 under bash 3.2's nounset empty-array expansion; #2 `bin/check-run.sh`'s pre-existing unbalanced-quote parity check false-flagged a valid trailing-backslash value) → engineer fix `8f1f610` → this is a delta re-verification against the round-1 record above, not a full re-run of every class it already covers.

## 1 — both original failure shapes confirmed gone, on this host's actual bash

- Host bash confirmed `3.2.57` on both `PATH` and `/bin/bash` (same version the Codex reproduction and the engineer's fix both cite).
- `RUNS_DIR=<scratch> bash bin/log-run.sh shell-team` (zero flags) → `log-run: missing required --run-id`, **exit 2**. `RUNS_DIR=<scratch> /bin/bash bin/log-run.sh shell-team` → same: `missing required --run-id`, **exit 2**. Neither invocation form leaks `unbound variable` or exits 1.
- Span-shape round-trip, `--error` ending in a single literal backslash (`--error $'ends in a backslash \\'`): writer exit 0; `check-run.sh` on the written row → exit 0. Row on disk: `"error":"ends in a backslash \\"` (correct `jesc` escaping).
- Event-shape round-trip, `--label` ending in a single literal backslash: writer exit 0; `check-run.sh` on the written row → exit 0. Row on disk: `"label":"ends in a backslash \\"`.
- Genuinely truncated line (`"label":"truncated}` with no closing quote at all) → `check-run.sh --line` still **exit 1**, `unbalanced double-quotes (truncated string?)` — the fix narrows the false-positive, it does not weaken real truncation detection.

## 2 — both fixes confirmed load-bearing, mutated in a fresh scratch copy of `bin/` (never the working tree)

- **Fix #1** (`log-run.sh`'s `[[ "${#SEEN[@]}" -gt 0 ]] || return 1` guard): deleted the guard line in the scratch copy, confirmed via `diff` the mutation landed. Re-ran the zero-flag invocation under both `bash` and `/bin/bash` against the mutated copy → **both now reproduce the exact original crash**: `.../log-run.sh: line 115: SEEN[@]: unbound variable`, exit 1. Restored the file from an untouched pre-mutation copy, re-ran both forms → back to `missing required --run-id`, exit 2 (GREEN). `cmp` confirms the restored scratch file is byte-identical to the real `bin/log-run.sh`.
- **Fix #2** (`check-run.sh`'s `unq="${line//\\\\/}"` pass added before the pre-existing `unq="${unq//\\\"/}"` strip): reverted the scratch copy to the pre-fix single-pass order (strip `\"` directly off `$line`, no `\\` pass), confirmed via `diff`/inspection. Re-ran `--line` against the exact valid rows captured in step 1 (both span and event shape, each ending in a literal trailing backslash) on the mutated copy → **both now false-positive**: `unbalanced double-quotes (truncated string?)`, exit 1 — the exact reported bug. Restored from the untouched pre-mutation copy, re-ran both → exit 0 (GREEN). `cmp` confirms the restored scratch file is byte-identical to the real `bin/check-run.sh`.
- `git status --short` on the real working tree was clean before, during and after this mutation pass — every mutated file lived under a `$TMPDIR` scratch copy, never `git checkout`/`restore`/`reset`.

## 3 — `check-acs.sh`, with attention to the four named criteria

- `bash bin/check-acs.sh .shell-team/specs/T-1011-telemetry-event-rows.md` (live) → **35 passed, 0 failed, 1 skipped** (AC36, by design).
- **AC10 v2**, specifically re-checked beyond the aggregate pass: independently ran the base-ref checker (`git show 08ebaea:bin/check-run.sh`) and the current checker against `tests/check-run/fixtures/unbalanced.jsonl` directly — **both exit 1**, unchanged — the Major #2 strip-order fix did not flip this fixture's classification, which is exactly the risk this criterion exists to catch on a change to the shared unbalanced-quote check.
- **AC21** (errexit-safe pins): passed as part of the aggregate run; independently re-ran `bash tests/errexit-safe/run.sh` directly → `errexit-safe: all assertions passed`, exit 0, including its own mutation self-checks on the NOT_APPLY registry and the protected-content locks.
- **AC31** (suites + shellcheck): passed as part of the aggregate run; independently re-ran `bash tests/check-run/run.sh` directly → all cases pass including the four new round-1 regression cases (zero-flag under both bash forms, trailing-backslash round-trip on both shapes, genuinely-truncated negative case).
- **AC35** (scope lock, now with the reviews-file relocation): passed — the relocation of the Codex round-1 verdict into `.shell-team/reviews/T-1011.md` (and removal of the stray `-codex.md` file) falls inside AC35's permitted-not-required exclusion for `.shell-team/(provenance|reviews|interventions)/T-1011\.md`, so it does not appear as an unexplained extra file.

## 4 — suites spot-run directly (not solely via `check-acs.sh`)

- `bash tests/check-run/run.sh` → all assertions pass (including both new regression blocks named above).
- `bash tests/errexit-safe/run.sh` → all assertions pass.
- Full 56-step CI-ordered run was not independently re-run this round: the engineer's hand-off reports it green, the rework's blast radius is two narrow, mechanically-isolated bugs in `bin/log-run.sh` and `bin/check-run.sh` (both already exercised directly above, by `tests/check-run/run.sh`, and by every consumer suite via `check-acs.sh`'s aggregate pass), and nothing in this delta surfaced a reason to distrust that report.

## 5 — seam gates re-confirmed

- `bash bin/check-intent.sh .shell-team/specs/T-1011-telemetry-event-rows.md .shell-team/todo.md` → `aligned: T-1011 v2 (e5cc39266d60bb6c28ea9142f9c5c86d596aa84d)` — no re-freeze, as expected (both fixes are narrow bug corrections inside the frozen intent, not new decisions about D1–D8).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1011.md` → conformant, **6** decision entries (up from 4 — two round-1 rework decisions added), 0 sentinel. Read both new entries: grounded in the actual code (`bin/log-run.sh`'s `SEEN` guard line; `bin/check-run.sh`'s reordered `unq=` strip), consistent with the reproduction and fix described above.
- `bash bin/check-interventions.sh --task T-1011 .shell-team/interventions/T-1011.md` → conformant, 1 entry, 0 sentinel — unchanged from round 1 (no new intervention was recorded for this rework round, which is correct: a Codex `REQUEST_CHANGES` → `engineer` route-back is ordinary loop machinery, not a human intervention).

## Risk notes for reviewer

- Carries forward unchanged from the round-1 record above: the two mutation self-checks I did not independently re-run in round 1 (span-only-key removal → AC16; per-type-requiredness removal → AC14), and the AC10 v2 fixture-enumeration completeness note.
- The full 56-step CI-ordered run was not independently re-executed this round (see item 4) — a deliberate, disclosed scope-narrowing consistent with the T-1010 round-2 delta precedent, not an oversight.
- `git status --short` was clean throughout this delta pass (all mutation fixtures built under `$TMPDIR`, on scratch copies confirmed byte-identical to the real files before and after) apart from this append and the board update.

---

### Codex Review verdict: APPROVE
- Task: T-1011
- Codex model: gpt-5-codex
- Verification mode: static-only
- Diff scope: 6 files, +180/-3 lines (`156e775..a1a2e3c`, round-2 delta only)

Round-2 primary パスを `codex exec ... review --base 156e775 ...` で実行し、round-1 の tip (`156e775`) と round-2 の tip (`a1a2e3c`) の差分だけに絞って再レビューした。findings なし（"no functional regressions were identified"）。私自身もこのホストで両修正を直接再現・確認した上で以下の判定を行っている（Codex の報告を鵜呑みにしていない）。

#### Blockers
- なし。

#### Major
- なし。round-1 の Major #1（`bin/log-run.sh:117`）・Major #2（`bin/check-run.sh:189-196`）はいずれも修正済みで確認できた — 詳細は下記。

#### Minor / nits
- round-1 で Minor に格下げした discriminator の空白/重複キー耐性（`out-of-input-space`）は本ラウンドで変更されておらず、判定は変わらない（再掲不要 — round-1 の disposition のまま）。

#### Adversarial findings
- 本ラウンドは新規の Blocker/Major が0件のため、devil's-advocate パス（opt-in トリガ）は起動していない。round-1 の adversarial パス（`SEEN` 配列、discriminator、`is_span_row` 独立コピー）で得られた知見は変更対象コードに影響しないため再実行していない。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes |

round-1 の ledger（discriminator の空白/重複キー耐性を out-of-input-space で minor に格下げ）は本ラウンドでは対象外（コード側は変更されていない）のためここでは再掲しない。

#### Round-2-specific checks（coordinator の(1)(2)を確認）

**#1 — `bin/log-run.sh` の `seen()` 空配列ガード**
- 修正差分: `[[ "${#SEEN[@]}" -gt 0 ]] || return 1` を `for f in "${SEEN[@]}"` の直前に追加。既存の `bin/rollup-runs.sh`/`bin/cluster-failures.sh` の `"${#LINES[@]}" -gt 0` イディオムと同型。
- **このホストで直接再現・確認済み**（bash 3.2.57、PATH `bash` と `/bin/bash` の両方）: `bash bin/log-run.sh shell-team`（loop_id のみ、フラグ皆無）→ 修正前は `line 115: SEEN[@]: unbound variable` / exit 1、修正後は `log-run: missing required --run-id` / exit 2。
- **rework diff がこの1点以外に空配列展開を新規導入していないか**を確認するため、`bin/log-run.sh`/`bin/check-run.sh` の全 `[@]` 使用箇所を再列挙: `SPAN_ONLY_FLAGS`/`EVENT_ONLY_FLAGS`（log-run.sh）、`REQUIRED_KEYS`/`REQUIRED_EVENT_KEYS`/`SPAN_ONLY_KEYS`（check-run.sh）はいずれも静的な非空リテラル配列であり、実行時に空になり得ない。新規追加された空配列展開は他にない。
- `shellcheck bin/log-run.sh bin/check-run.sh tests/check-run/run.sh` → clean。

**#2 — `bin/check-run.sh` の unbalanced-quote 判定のストリップ順序**
- 修正差分: `unq="${line//\\\"/}"` の前に `unq="${line//\\\\/}"`（`\\` のペアを先に剥がす）を追加。
- **バックスラッシュ/クォートの組み合わせを網羅的に再現・確認済み**（このホストで直接、writer→checker のラウンドトリップ）:
  - 末尾が単一バックスラッシュ（`\\` → jesc が正しくエスケープ）: span 側・event 側とも writer exit 0、checker exit 0（修正前は exit 1 の誤検出だった箇所）。
  - 末尾がクォート（`"` → jesc が `\"` にエスケープ、AC8 相当）: writer/checker とも exit 0（回帰なし）。
  - 末尾がバックスラッシュ+クォートの組み合わせ（リテラル `\` の直後にリテラル `"` — jesc は `\\\"` の4文字にエスケープ）: writer/checker とも exit 0（自分で追加検証、AC/回帰テストには含まれないが手動確認）。
  - 末尾が2連続バックスラッシュ（jesc は `\\\\` にエスケープ）: writer/checker とも exit 0（自分で追加検証）。
  - 本物の切り詰め（`"label":"truncated}` のように閉じクォートが無い行）: 修正後も引き続き exit 1、`unbalanced double-quotes (truncated string?)` — 誤検出の是正が本物の切り詰め検出を弱めていないことを確認。
- **AC10 v2（base-ref 7フィクスチャとの互換性）への影響なし**を再確認: `git show 08ebaea:bin/check-run.sh` と現行 `bin/check-run.sh` を七つの base-ref フィクスチャ全てに対して実行し、exit code が一致すること（5件以上が非ゼロという非自明性込み）を自分で再実行して確認 — 修正前と結果は変わらない。既存の genuine-truncation フィクスチャ（`tests/check-run/fixtures/unbalanced.jsonl`）も両チェッカーとも exit 1 のまま。
- 新規回帰テスト（`tests/check-run/run.sh` に追加された4ブロック: span側/event側のトレーリングバックスラッシュ・ラウンドトリップ、`--line` での両形状、本物の切り詰めの negative control、そしてゼロフラグ呼び出しの2 bash バイナリでの確認）を自分で実行し、全て PASS を確認（`bash tests/check-run/run.sh` → `OK`）。

**rework diff がこの2箇所以外に触れた範囲の確認**（`156e775..a1a2e3c`、6ファイル）:
- `.shell-team/provenance/T-1011.md`: 2件の新規 decision entry（rework round 1, Codex Major #1/#2）が追加され、`check-provenance.sh` は conformant（6 decisions, 0 sentinel）。内容を読み、実装・再現手順・grounding が正確であることを確認。
- `.shell-team/reviews/T-1011.md`: round-1 の私の verdict が別ファイル（`T-1011-codex.md`）から本ファイルへ orchestrator によって逐語的に移設されたことをボードのオーケストレーターのコミットメッセージと本ファイルの内容突合で確認 — 内容は自分がround-1で書いたものと一致（見出し・ラベル・ledger を含め欠落なし）。QA round-2 delta セクションも読み、記載の再現・mutation self-check の内容は自分の直接検証と矛盾しない。
- `.shell-team/todo.md`: T-1011 エントリに round-1 codex-reviewer / rework / orchestrator / qa round-2 delta の4サブブレットが追加。純粋な追記であり、既存のブレット（engineer/qa-verifier round-1）は変更されていないことを diff で確認（`## Done` セクションや他タスクの記述にも影響なし）。
- `bin/check-run.sh`/`bin/log-run.sh`/`tests/check-run/run.sh` 以外の `bin/` スクリプト（`rollup-runs.sh`/`cluster-failures.sh`/`skills/run/SKILL.md`）はこのラウンドで一切変更されていない（`git diff --name-status 156e775..HEAD` で確認済み）。

#### Recommendation
- APPROVE → `.shell-team/todo.md` を `READY_FOR_MERGE` に進める（orchestrator の作業）。round-1 の2件の Major はいずれも狭い範囲のバグ修正で正しく直っており、この rework が新たな Blocker/Major/回帰を持ち込んでいないことを自分で直接確認した。

#### Fast-follow disposition
- round-1 の fast-follow 宣言（discriminator 空白/重複キー耐性 = won't-fix、`is_span_row` 独立コピーの将来乖離リスク = file-an-issue 意図）は disposition 変更なしのため据え置き — 本ラウンドでは再掲しない。
