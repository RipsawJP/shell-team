### Codex Review verdict: REQUEST_CHANGES
- Task: T-1067
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: 5 files, +537/-0 lines (`git diff --stat 3d72e5b...HEAD`)

`### Verdict scope` の適用: 本タスクは operator-ratified の凍結節（spec 39-47 行）に従い、(1) 算出値がソースを名乗り再現するか、(2) 結論の向きが数値に支持されているか、の 2 点のみをゲートとする。接続的散文（要約・特徴づけの不正確さ）はゲートしない — 以下 Blocker/Major/Minor はこの区分で分類している。

#### Blockers
- `docs/loop-engineering/context-lifecycle.md:104` と `:109` — `axis-subagent-wall-clock` が「measured directly」と断言しながら、実際に引用しているのは `tokens`（385176→893361）であって `duration_ms`（実際の壁時計時間）ではない。テレメトリを直接再実行して確認: T-1060 engineer span の `duration_ms` は round1(iteration0)=4,979,526ms（約83分）、round2以降は536,832〜941,953msの範囲で変動し平均約705,752ms（約11.8分）— tokens の単調増加とは全く異なる形（round1が突出して高く、以降はむしろ低い）。この `axis-subagent-wall-clock` の figure は `per-round-fresh-subagents`（not-recommended）と `status-quo-carry-everything`（recommended、`:109`が同じ tokens 数値を「wall-clock totals」と再利用）の両方の disposition-reason の根拠になっており、3案中2案の中心的な causal accounting がこの取り違えに依存している。Codex 敵対的パス（`.shell-team/reviews/T-1067-codex-adversarial.txt`）の結論も Blocker — 「文字通り wall-clock と読む読者を誤解させない読みは見つけられない」「これは label mismatch ではなく、結論の根拠となるメトリクスのすり替え」。DP6 は3軸を別々に価格付けすることを要求しており、tokens を2つの軸（`axis-orchestrator-tokens` と `axis-subagent-wall-clock`）で使い回すのは DP6 の設計そのものに反する。値自体（385176/893361/duration_ms群）はすべて独立に再現したので「computed value が誤っている」のではなく、「axis の意味づけ（何を測ったか）」が数値の裏付けを欠いたまま結論の方向を主張している — Verdict scope の(2)に抵触する。

#### Major
- `docs/loop-engineering/context-lifecycle.md:104` — `385176`（T-1060 round1の`tokens`）という figure に、`## What was measured` 中のいずれの `- measure:` ブロックにも対応する `- command:`/`- source:` サブバレットが存在しない。独立に再実行して値自体は再現した（`grep '"run_id":"20260811T062721Z-t1060"' ... | grep '"span":"engineer"' | head -1` = `"tokens":385176`）が、Verdict scope (1) の「each figure names the source it was read from and reproduces when the command quoted beside it is re-run」を字義通り満たさない — この figure だけ出典コマンドが note のどこにも書かれていない。

#### Minor / nits
- `docs/loop-engineering/context-lifecycle.md:38-43`（S1 の3本の `- command:`）および `:60-64`（S2 の"nine rounds"解決の4本）— 各ブロック内の2本目以降の `- command:` 行が、1本目のコマンドで設定した `$T`／`$SD` シェル変数に依存しており、その1行だけを独立したシェルへ貼り付けると `find: : No such file or directory` 等で失敗する。ブロック全体を順に一つのシェルで実行すれば全数値が厳密に再現する（本レビューで実測: 34/116358176/15674957/4444、いずれも一致）ため、これは computed value の誤りではなくコマンドの自己完結性（可搬性）の欠如であり Verdict scope の(1)を厳密に満たさない体裁上の隙間。各行を `T=...; find ...` の形で毎回再導出する形に直すことを推奨するが、値そのものは正しいので non-blocking。
- `docs/loop-engineering/context-lifecycle.md:91`（connective prose・non-gating） — 「two class-B re-freeze routing calls」という4件の非一致 effect 行の内訳の記述が不正確。20件の抽出結果を自分で確認したところ、非一致の4件は T-1055（background-job race check）・T-1058（class-B re-freeze routing call）・T-1060（board-repair）・T-1062（`.shell-team/interventions/T-1062.md` 自身が明示的に「within class-M bounds」と書いている repair-mechanism-redesign call）であり、class-B に該当するのは T-1058 の1件のみ（T-1062 は class-M）。母集団・ERE一致数（20件中16件=80%、strict majority）・`supported` 判定はいずれも独立に再現し、この特徴づけの誤りはそれらを一切動かさないため、Verdict scope の下で non-gating な note。

#### Adversarial findings
Blocker 項目について `codex exec` を用いた devil's-advocate パスを実施（対象: `## Reset options and their cost` の `per-round-fresh-subagents`/`status-quo-carry-everything` ブロック）。結論は Blocker — 「両方向に読める」余地はあるが（round1がcold-start込みの代表値なら fresh 不利という論理は成立し得る、後続ラウンドが遅くなっていない以上「蓄積で遅くなるから reset」という根拠もない、等）、note 自身が `axis-subagent-wall-clock: measured directly` と断定しながら実際には壁時計データを一度も引用していない点、および実測の `duration_ms` パターン（round1が後続平均の約7倍）を note が一切開示していない点は、字義通りの読者を誤解させない読みが存在しないと判断した。詳細は `.shell-team/reviews/T-1067-codex-adversarial.txt`。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | axis-subagent-wall-clock が tokens を「measured directly」の壁時計値として提示し、実際の duration_ms（round1突出高・後続は低位変動）と乖離している | primary: major → adversarial: blocker | blocker | 独立にテレメトリを再実行し両フィールドを確認、敵対的パスの推論も追試して同意 — 2つの中心的な disposition-reason がこの取り違えに依存し、Verdict scope (2) を動かす |
| 2 | `385176` の出典コマンドが note のどこにも書かれていない | primary: major | major | 値自体は独立再現したが Verdict scope (1) の「命令が併記され再現する」を字義通り満たさない構造的欠落として維持 |
| 3 | `$T`/`$SD` を再利用する2本目以降の `- command:` 行が単独では再現しない | primary: major | minor | ブロック全体を順に実行すれば全数値が厳密に一致すると本レビューで確認済み — computed value 自体の誤りではなく体裁上の自己完結性の欠如に格下げ |
| 4 | 4件の非一致 effect 行の内訳を「two class-B routing calls」とする特徴づけが不正確（実際は class-B 1件・class-M 1件） | primary: nit | minor（non-gating note） | 20件の母集団・16件のERE一致・`supported`判定はいずれも動かない connective prose の特徴づけ誤り。Verdict scope の non-gating 規定どおり note として記録 |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` T-1067 entry back to `READY_FOR_ENG` with the linked findings above (Blocker #1 と Major #2 の修正が必須; Minor #3/#4 は任意)。

#### Fast-follow disposition
- Minor #3（`$T`/`$SD` チェーンする `- command:` の非自己完結性）— file-an-issue（rationale: `docs/loop-engineering/` 配下の今後のノートでも同型の「1本目で定義した変数を2本目以降が黙って再利用する」パターンが再発しうる、汎用的な著述規約の欠落であり、lessons corpus 昇格候補になり得る）。
- Minor #4（class-B/class-M の特徴づけ誤り）— won't-fix（reason: 純粋な物語的記述の誤りで、20/16の値にも `supported` 判定にも影響しない。本レビュー記録に既に正確な内訳を記載済みであり、再発防止のための一般化可能な教訓は特にない）。

---

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1067
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: 3 files, +32/-19 lines (`git diff --stat 1da2cce...bf17bbd`); branch tip `bf17bbd`, round-1 review at `1da2cce`

`### Verdict scope` の適用は round 1 と不変（凍結節・spec 39-47行）: (1) 算出値の出典・再現性、(2) 結論の向きの支持、のみがゲート。

#### Round 1 findings — disposition
- **Blocker（axis-subagent-wall-clock が tokens を wall-clock として提示）— 解消。** `## What was measured` の S2 ブロックが `tokens` と `duration_ms` の両系列を9ラウンド分すべて記載し、ラベル付き bounded-finding 段落（"tokens and duration_ms move in different shapes"）で両者の乖離を明示。独立に再実行し両系列・rounds2-9平均（705751ms）とも厳密一致を確認。`axis-subagent-wall-clock` は現在 `duration_ms` を直接引用し、「confounded, not decisive」と正直に格下げされている。
- **Major（385176の出典欠落）— 解消。** S2ブロックの `- command:`/`- source:` が tokens系列全体（385176を含む）を裏付ける。
- **Minor（`$T`/`$SD` チェーンする command の非自己完結性）— 解消。** 該当4ブロックとも各 `- command:` 行が `T=`/`SD=` を再導出する形に修正済み。独立に1行を単独実行し `15674957` を再現。
- **Minor（class-B/M特徴づけの誤り）— 解消。** 「one class-B re-freeze routing call to pm-spec (T-1058)... a repair-mechanism-redesign call that its own record states is "within class-M bounds" (T-1062)」に修正され、20件の母集団・16件のERE一致とも再現。

#### Blockers（新規）
- `docs/loop-engineering/context-lifecycle.md:113` — `axis-rehydration` が T-1060 round1 の `tokens` 総量（385176）を「a fresh launch pays before doing any new work」の "measured floor" と断定しているが、これは同じ note 自身の `## Limits and what is not computable`（:125）が明記する制約と両立しない — 「`tokens` は 'carried context' と 'new work' を分離できない」という開示済みの識別不能性を、まさにこの figure に対してだけ無視している。T-1060 round1 は実際にはその task の初期実装の大半を担う round であり（git 履歴上も同round相当のコミットで大量の追加行がある）、385176 の大半は「新規作業前の再水和」ではなく実質的な新規実装コストであり得る。さらに `per-round-fresh-subagents` の disposition-reason（:114）は、wall-clock 軸が「confounded, not decisive」に正直化された結果として「the disposition stands on the rehydration axis as its primary support」と明言しており、この一次的根拠が上記の理由で成立しない。round5等の fresh relaunch が実際に 385176 相当を払うという反実仮想も観測されていない（T-1060 rounds2-9はいずれも resumed invocation であり、fresh relaunch の直接観測は本 corpus に存在しない）。Codex 敵対的パス（`.shell-team/reviews/T-1067-codex-adversarial.txt`）も同じ結論に到達し、"tokens×9=3,466,584 vs 実測resumed合計6,054,319" という試算まで示した上で「floor が反復するという事実だけでは総コスト比較の方向すら決まらない」と結論、Blocker 判定。数値自体（385176/406982/duration_ms系列）はすべて再現するため gate (a) は通るが、この figure が支持すると note が主張する結論の向き（`per-round-fresh-subagents — not-recommended` の primary support）は数値が実際に支持する範囲を超えており gate (b) に抵触する。
  - 是正案（敵対的パスと同意見）: 少なくとも次のいずれかへ変更 — `per-round-fresh-subagents` を `undetermined` に戻す、または disposition-reason を「実測コストは不明だが、保守的な政策判断として reset しない」という定性的な政策論に明示的に格下げし、"measured floor" や "primary empirical support" という数量的断定の語を落とす。

#### Major
（新規なし）

#### Minor / nits
- `docs/loop-engineering/context-lifecycle.md:119`（connective prose・non-gating） — status quo の disposition-reason 「no round-over-round wall-clock growth from accumulated context is observed at all」は、持続的・単調な悪化傾向が無いという骨子は defensible だが、"at all" は強すぎる。実測では round3→4（539159→889445）・round6→7（536832→852735）のように隣接ラウンド間の増加も存在する（母集団20件・16件一致・`supported` 判定や `status-quo-carry-everything — recommended` の disposition 自体は動かない）。「no monotonic or persistent upward trend tied to round number is observed」程度への言い換えを推奨するが non-blocking。

#### Adversarial findings
新規 Blocker について `codex exec` の devil's-advocate パスを実施（対象: `axis-rehydration` および `per-round-fresh-subagents` の disposition-reason）。結論は Blocker — 慈善的に読んでも「fresh relaunch は何らかの再水和コストを毎回払う」という定性的主張までしか救済できず、現在の disposition-reason が明言する「tokens-measured floor cost repeated every round」という定量的な primary support は成立しないとした。詳細は `.shell-team/reviews/T-1067-codex-adversarial.txt`（round 2 分で上書き）。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | axis-rehydration が round1 tokens総量を「新規作業前のfloor」と断定し、note自身のLimits開示（tokens は carried-context と new-work を分離不能）と矛盾する | primary: major → adversarial: blocker | blocker | 敵対的パスの試算（floor×9 vs 実測合計）まで独立に追試して同意。round1修正後、この軸が`per-round-fresh-subagents`の唯一の primary support になった時点で内部矛盾の影響が最大化した |
| 2 | status quo disposition-reasonの「at all」が隣接ラウンド間の局所的増加を字義的に否定しすぎている | primary: minor | minor（non-gating note） | 20/16/`supported`・`status-quo-carry-everything — recommended` のいずれも動かない wording precision の指摘 |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` T-1067 entry back to `READY_FOR_ENG` with the linked findings above（新規 Blocker の是正が必須; Minor は任意）。

#### Fast-follow disposition
- Minor（"at all" のwording）— won't-fix（reason: 純粋な wording precision で、20/16の値にも `supported`/`recommended` 判定にも影響しない。本レビュー記録に既に正確な指摘を記載済み）。
