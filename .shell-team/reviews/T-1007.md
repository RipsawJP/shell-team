# QA verification — T-1007 (the lessons ledger is Scope-typed and the shipping boundary is machine-enforced)

**Branch**: `feature/scope-typed-ledger` (base `develop` at `72b6e8b`, HEAD `93b6e23`)
**Verdict**: PASS → `READY_FOR_REVIEW`

## 1. `check-acs.sh` — 19/19 PASS

`bash bin/check-acs.sh .shell-team/specs/T-1007-scope-typed-ledger.md` run live: all 19 acceptance criteria PASS (AC1–AC19), ~54s total wall time — no single `check:` approached `check-acs.sh`'s 120s cap. Dry-run confirmed 0 unrecognized labels first.

## 2. `check-intent.sh` — aligned v1

```
check-intent: aligned: T-1007 v1 (f90e62d35c97d69f9dbc99a040831cd17c57a6c0) matches .shell-team/specs/T-1007-scope-typed-ledger.md
```

Verified the engineer's claim about the frozen intent boundary directly: `<!-- BEGIN intent-block: T-1007 -->` is at spec line 37, `<!-- END intent-block: T-1007 -->` at line 433, and `## Notes from engineer` (line 746) sits well outside it. `check-intent` reporting `aligned v1` after that section was added confirms the checker is genuinely marker-scoped, not merely lucky.

## 3. Hygiene checks — all green

- `check-handoff.sh .shell-team/todo.md` → exit 0
- `check-prompt-sync.sh` → `all registered prompt blocks in sync`
- `check-pii-shapes.sh --base develop` → `clean`
- `check-commit-identity.sh --base develop` → `clean`
- `check-provenance.sh .shell-team/provenance/T-1007.md` → `conformant: 3 decision entries, 0 sentinel`. Read all 3 decisions: (1) the `Bound-in` fenced-example wording fix on `tests/playbook-promote/fixtures/lessons-base.md` only (grounded in the exact AC10/AC2 check-line collision, non-circular), (2) the `check-playbook.sh` header-comment rewording to remove the banned-token substring while keeping the "no runtime dependency" meaning (grounded in the exact AC17 grep pattern and the pre-existing base-ref line), (3) the design choice to run the `Bound-in` shape check independent of `Scope`'s own validity (grounded in DP-d's under-specification and the file's existing independent-check pattern). All three name concrete spec/code loci, not restatements of the decision itself.
- `check-interventions.sh .shell-team/interventions/T-1007.md` → `conformant: 0 entries, 1 sentinel`

## 4. Full CI-ordered run, in `.github/workflows/check-handoff.yml`'s own file order

All 45 steps (shellcheck with the exact CI argument list, every fixture suite, every dogfood step) run locally in file order — **0 failures**. The two `origin/${GITHUB_BASE_REF:-develop}`-based steps were run against local `develop` as the equivalent local proxy (no network `origin` access in this sandbox; `check-pii-shapes.sh --base develop` and `check-commit-identity.sh --base develop` both independently confirmed clean above). Full step list, all PASS: shellcheck; board-template lint; check-handoff suite; 3× loop-contract lints + check-contract suite; loop-guard; check-run; log-run; team-init; discover-work; check-acs; check-design-note; goal-state; rework-digest; check-retro; retro-inputs suite + invariants; retro-inputs dogfood; check-retro dogfood; check-readme-version suite + dogfood; rollup-runs; rollup-track; consolidate-proposals; cluster-failures; review-gate; close-out; check-prompt-sync suite + dogfood; **check-playbook suite; gen-playbook-blocks suite; playbook-promote suite** (the three T-1007 suites, all green with their labelled cases); team-paths suite + dogfood; install; check-intent suite; check-provenance suite; check-interventions suite + dogfood; interventions-reminder suite; check-board-headings; **errexit-safe regression suite** (the exact class that cost T-1006 a rework round — green here); codex-skeleton-hygiene; check-pii-shapes suite + diff dogfood; check-commit-identity suite + gitignore-raw-dumps + diff dogfood.

## 5. Empirical probes — the checker's new boundary, on QA's own corpora (not the spec's fixtures)

Built independent minimal fixtures under `$TMPDIR` for every probe (never reused the shipped `tests/*/fixtures/*` content as the corpus body, only as scaffolding where the generator requires a directory shape):

- **(a1) `Scope` fail-closed, all three modes**, on a QA-authored 2-entry (one loop, one maintainer) corpus:
  - Both `Scope` bullets removed → exit 1, `missing required field: Scope` ×2 (both entries independently caught).
  - `Scope: all` → exit 1, `unknown Scope value: 'all'`.
  - Maintainer entry's `Bound-in` removed → exit 1, `Scope is 'maintainer' but Bound-in is missing`.
  - `Bound-in` added to the loop entry → exit 1, `Scope is 'loop' but Bound-in is present`.
  - `Bound-in: /etc/hosts` → exit 1, `Bound-in must be a repository-relative path: '/etc/hosts'`.
  - `Bound-in: ~/x.md` → exit 1, `Bound-in must be a repository-relative path: '~/x.md'`.
- **(a2) All four supersession directions**, on a QA-authored 4-entry corpus (retired loop → active loop, retired maintainer → active loop, active maintainer, retired maintainer → active maintainer): validates green as a whole (all three legal directions in one corpus, including `maintainer → loop`). Repointing only the retired-loop entry's `Superseded-by` at the active-maintainer entry (the forbidden direction) makes exactly that entry fail: `Superseded-by crosses Scope: a 'loop' entry may not be superseded by a 'maintainer' entry (...)`, exit 1 — the other three entries stay clean.
- **(b) Generator, QA-authored fixture** (one loop + one maintainer entry, distinct sentinels `QAPROBE-LOOP-SENTINEL` / `QAPROBE-MAINTAINER-SENTINEL`, `Applies-to: all` on both): after `gen-playbook-blocks.sh --root`, the maintainer sentinel is absent (count 0) from all four `playbook-*.md` blocks and the loop sentinel is present (count 1) in all four. Matches AC6's shape exactly on an independent corpus.
- **(c) Promote end-to-end**: `--scope maintainer --bound-in CONTRIBUTING.md` appends correctly and the file re-validates green via `check-playbook.sh` directly. `--scope maintainer` **without** `--bound-in` exits **1** (the re-validation's exit code, confirmed via `check-playbook.sh`'s own stderr: `Scope is 'maintainer' but Bound-in is missing`) — **not** promote's own exit 2 — and the lessons file is byte-identical to its pre-run copy (`cmp -s` confirmed). This is the exact DP-e "one authority for the schema" property: promote's own required-field check (`--scope` omitted) is exit 2, but an invalid `Scope`×`Bound-in` combination is caught only downstream, at exit 1.
- **(d) Wording-fix diffs read directly**: `git diff 72b6e8b -- tests/playbook-promote/fixtures/lessons-base.md` shows only the `## Format` fence gaining `Scope`/`Bound-in` documentation lines (the `Bound-in` line deliberately spelled without the `- **Bound-in**: ` substring, as disclosed) plus `- **Scope**: loop` on the pre-existing entry — no semantic change. `bin/check-playbook.sh`'s header comment: `# Reads only. Pure bash + coreutils — no jq/yq/python.` (base ref) → `# Reads only. Pure bash + coreutils; no external interpreter is invoked.` (current) — same claim, banned tokens removed. Both confirmed wording-only.
- **(e) `git diff --name-only 72b6e8b`**: exactly 14 files — `.shell-team/interventions/T-1007.md`, `.shell-team/provenance/T-1007.md`, `.shell-team/specs/T-1007-scope-typed-ledger.md`, `.shell-team/test-recipe.md`, `.shell-team/todo.md`, `bin/check-playbook.sh`, `bin/gen-playbook-blocks.sh`, `bin/playbook-promote.sh`, `tests/check-playbook/fixtures/valid-base.md`, `tests/check-playbook/run.sh`, `tests/errexit-safe/run.sh`, `tests/gen-playbook-blocks/fixtures/root/tasks/lessons.md`, `tests/gen-playbook-blocks/run.sh`, `tests/playbook-promote/fixtures/lessons-base.md`, `tests/playbook-promote/run.sh` — every one inside AC16's allow-list. `git diff --name-only 72b6e8b -- bin/` is exactly the 3 intended files, 0 additions.

Additional probes beyond the checklist's minimum:

- **Fence-immunity, QA's own fixture**: a fenced block containing `- **Scope**: all` and `- **Bound-in**: /etc/hosts` (both would be violations if parsed as real fields), followed by one clean real entry → validates green. Confirms fenced content is fully excluded from field parsing regardless of which field name appears inside it (pre-existing `in_fence` machinery, unmodified, correctly still applies to the two new fields).
- **Marker-string collision in `Bound-in`**: a value containing `<!-- BEGIN prompt-block:` → exit 1, `field 'Bound-in' contains the reserved marker string '<!-- BEGIN prompt-block:'`. Confirms `check_structural` genuinely runs on `Bound-in` (not just claimed).
- **`--scope` whitespace-padding**: `--scope '  loop  '` → promotes successfully, emits the trimmed `- **Scope**: loop` (not the padded value), re-validates green. Confirms the existing `trim` machinery applies to the new flag as the spec's Input space class 7 requires.
- **Real invocation forms**: `./bin/check-playbook.sh`, `PATH=...:$PATH check-playbook.sh` (bare name), `./bin/gen-playbook-blocks.sh --help`, `./bin/playbook-promote.sh --help` — all exit 0/correct, confirming exec bits are set and the scripts are not accidentally `bash`-wrapper-dependent.

## 6. Independent mutation spot-check — 2 of the engineer's mutations reproduced from scratch, plus the state-leakage fix independently confirmed

All mutations applied to a **file-copy backup** of `bin/check-playbook.sh` under `$TMPDIR`, restored via `cp` (never `git checkout`/`restore`), and verified byte-identical with `diff` after restore; `git status --short bin/check-playbook.sh` confirmed empty after each restore.

1. **Disabled the `Scope` required-field check** (commented out line 395's `emit ... "missing required field: Scope"`): re-ran `check-acs.sh` — **AC1: FAIL (exit 1)**, matching the engineer's claimed mutation #1 exactly. Restored, byte-identical.
2. **Inverted DP-b** (`[ "$e_scope" = "loop" ] && [ "$target_scope" = "maintainer" ]` → `[ "$e_scope" = "maintainer" ] && [ "$target_scope" = "loop" ]` at line 769): re-ran `check-acs.sh` — **AC5: FAIL (exit 1)**, because the inverted rule now rejects the *positive control* corpus's legitimate `maintainer → loop` entry, matching the engineer's claimed mutation #6 exactly. Restored, byte-identical.
3. **The engineer's own cross-entry-state-leakage mutation, reproduced independently**: removed the per-heading `seen_scope=0; seen_bound_in=0` reset (line 554, inside the canonical-heading branch — confirmed this reset is genuinely per-*heading*, not per-*file*, by reading the surrounding code first). On a QA-authored 2-entry fixture (a `maintainer` entry with `Bound-in: CONTRIBUTING.md` immediately followed by a `loop` entry with no `Bound-in` of its own): with the reset disabled, the second entry is **wrongly flagged** `Scope is 'loop' but Bound-in is present` — the exact false positive the engineer described (the loop entry inherits the prior entry's `seen_bound_in=1`). Restored the reset; re-ran the identical fixture — **exits 0, `all entries valid`**. This independently confirms the fix is genuinely in place, not merely claimed in the hand-off/provenance prose.

## 7. Adversarial fixture synthesis checklist — coverage disclosure

This task's deliverable is three executable `bin/` scripts with a real input surface (a ledger parser/validator and a CLI). Checklist classes exercised:

1. **Beyond happy-path**: exercised for every boundary — missing field, closed-enum violation, mandatory/forbidden conditional-field violation, absolute/`~` path shape, the DP-b directional graph edge, CLI required-flag omission, CLI invalid combination. Payload-shape sub-axis fully covered (malformed/empty/whitespace-only/marker-collision values); filesystem/environment-state sub-axis covered by AC4/DP-d (checker is provably filesystem-blind, verified from two different cwds in AC4's own check and independently by DP-d's design rationale); resource/budget-competition sub-axis is **structurally inapplicable** — this checker has no budget/limit concept to exhaust; time/liveness sub-axis is **structurally inapplicable** — every script here is a single-pass synchronous read with no network/subprocess/wait state to hang on.
2. **Regex/charset anchoring**: probed the `- **Scope**: loop`/`all` substring against the fenced `## Format` documentation (which deliberately spells `<loop | maintainer>`, not a bare token) — confirmed no collision, both by reading AC1's own `sed` pattern and by independent fence probe (§5, fence-immunity). Probed the reserved-marker-string collision on `Bound-in`. Did not find a legal real-vocabulary value that collides with the `Scope`/`Bound-in` field-name greps themselves beyond what AC2's own criterion already exercises (`Zzz` rename, catch-all allow-list isolation).
3. **Markdown/CommonMark structure parsing**: fence handling itself is pre-existing, untouched infrastructure (per spec Non-goal #5) with its own dedicated suite coverage; verified only that the two *new* fields inherit that pre-existing immunity correctly (§5 fence-immunity probe), not the fence grammar itself.
4. **Test-harness short-circuit**: ran every canonical command form — `bash bin/<script>.sh` (all `check:` lines, all suites), `./bin/<script>.sh` direct exec, and PATH bare-name invocation (§5). Confirmed exec bits are set on all three touched scripts.

No class was skipped for an environmental reason; nothing here is `environmentally-unverified`.

## 8. Not re-litigated

Everything above passed cleanly. No regression, AC violation, or unexpected edge-case behavior was found anywhere in this task.

### QA verdict: PASS
- Task: T-1007 → READY_FOR_REVIEW
- Tests: 19/19 acceptance criteria PASS; 45/45 CI-ordered steps PASS (0 failures); all hygiene checks green; all empirical probes and 3 independent mutation spot-checks (including the DP-b directional rule and the state-leakage fix) matched the spec's contract exactly.
- Acceptance criteria: 19/19 checked off (AC1–AC19)
- Edge cases tried: fenced-content immunity for the two new fields, marker-string collision in `Bound-in`, `--scope` whitespace trimming, real invocation forms (`./script`, PATH bare name), all four supersession directions on an independently-authored corpus, generator sentinel exclusion on an independently-authored corpus, promote's exit-1-vs-exit-2 split on an invalid combination.
- Risk notes for reviewer: (1) This task ships zero corpus by design (T-1008 is the corpus half) — nothing here proves a *correctly scoped* real entry, only that the mechanism enforces the schema; that judgment call is explicitly out of this task's scope per its own "Input space"/"boundary of what it proves" sections. (2) The two engineer-disclosed wording fixes (AC10 fixture fence line, check-playbook.sh header comment) are genuinely wording-only, independently confirmed by reading the diffs — no behavior change. (3) `Superseded-by crosses Scope` and the `Bound-in` shape-check both run independent of the entry's own `Scope` validity (provenance decision 3) — this is a deliberate, spec-consistent design choice (report every violation found, not just the first), not a gap.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1007
- Codex model: gpt-5-codex
- Verification mode: static-only
- Diff scope: 16 files, +1365/-55 lines (base `72b6e8b`, HEAD `3b94202`)

Intent-block read as canonical: `.shell-team/specs/T-1007-scope-typed-ledger.md` lines 37–433 (frozen v1, `f90e62d35c97d69f9dbc99a040831cd17c57a6c0`), matching QA's `check-intent` result. `## Notes from engineer` (line 746) sits outside the block, confirmed out-of-band and read only as disclosure. No provenance-file re-check performed beyond what QA already recorded (`.shell-team/provenance/T-1007.md`, 3 decisions, read by QA); this pass additionally cited provenance decision 3's independence claim directly against the round-2 finding below. All 4 local suites (`check-playbook`, `gen-playbook-blocks`, `playbook-promote`, `errexit-safe`) re-run here and green before invoking Codex.

#### Blockers
- None.

#### Major
- `bin/playbook-promote.sh:192,300-302` — an explicitly-supplied but blank/whitespace `--bound-in` (e.g. `--scope loop --bound-in ""`, or a wrapper script whose variable is unexpectedly empty) is silently converted to "no `Bound-in` bullet at all" before the candidate ever reaches `bin/check-playbook.sh`. The stored entry is schema-valid (DP-a's `loop`-forbids-`Bound-in` rule is presence-tested, and no bullet is present), so nothing invalid ships. But this contradicts the frozen spec's own text at two points: the Goal section says `--bound-in` "is passed through to the emitted entry" (spec line 76), and DP-e repeats "`--bound-in` is optional at the CLI level and passed through into the emitted entry" (spec line 537) — both stated as unconditional pass-through, with the `Scope`×`Bound-in` combination left entirely to the checker as "the single authority." The current implementation makes a CLI-side decision (blank → omit) before the checker ever sees the value, which removes exactly the signal (bullet presence) DP-a's own rationale calls load-bearing ("a blank bullet is still a bullet," spec line 456). Practically: a caller who intended to attach a real binding but hit an empty-variable bug gets a silent success with a `loop`-scoped entry and no trace in the diff that a `--bound-in` was ever supplied — the exact "maintainer-only knowledge shipped as loop" misclassification signal T-1007 exists to catch, reaching around the checker rather than through it. Reachable input: spec's own Input space class 7 explicitly lists "`--bound-in` supplied and omitted under each scope value," so this is not an out-of-scope synthetic extreme. Suggested fix: track whether `--bound-in` was supplied on the command line independent of its trimmed value (e.g. a `BOUND_IN_SEEN` flag set at parse time), and emit the bullet whenever the flag was supplied — even blank — so the existing fail-closed re-validation is the one that rejects it, preserving DP-e's single-authority design rather than duplicating the combination rule in the promoter.

#### Minor / nits
- `bin/check-playbook.sh:405-445` — the `Bound-in` shape check (absolute-path / `~`-prefix) and the header comment's claim that it "runs whenever the bullet is present, independent of Scope's own validity, so a shape violation is never masked by an unrelated Scope problem" is not fully accurate: when `Scope` is missing or holds an unknown value, a genuinely blank/whitespace-only `Bound-in` bullet produces zero `Bound-in`-specific violations (the maintainer-mandatory non-empty test only runs inside the `entry_scope = "maintainer"` branch; the shape-only block below tests only `/*` and `~*`, not emptiness). Verified live: a fixture with `Scope` missing entirely, or `Scope: all`, plus a blank `Bound-in` bullet, reports only the `Scope` violation and exits 1 — correct exit code, but an incomplete diagnostic (the second, independent problem in the same entry is not surfaced until `Scope` is fixed and the checker is re-run). No fail-open: `check-playbook.sh`'s own required/enum check on `Scope` still forces exit 1 regardless, and `gen-playbook-blocks.sh`'s preflight (`bin/gen-playbook-blocks.sh:207`) means no half-annotated entry ever reaches generation. This is squarely in the declared Input space (class 4, "a half-annotated corpus... carrying an empty or whitespace-only Scope") so it is not downgraded for being out-of-scope — it is simply low-severity because the overall verdict stays correct. Also contradicts provenance decision 3's stated intent (independent execution of the shape check) more than it contradicts any acceptance criterion — none of AC1–AC19 exercises this specific combination. Suggested fix (non-blocking): move a `[ -n "$biv" ] || emit … "Bound-in is present but blank"`-style check into the unconditional shape block so it fires regardless of `Scope`'s own validity, or narrow the header comment's independence claim to the cases it actually covers.
- Two engineer-disclosed wording fixes (`tests/playbook-promote/fixtures/lessons-base.md`'s fenced `Bound-in` line; `bin/check-playbook.sh`'s header comment) read as genuinely wording-only against `git diff 72b6e8b`, matching both the engineer's own account and QA's independent confirmation (§5(d) of the QA record) — no further finding here.

#### Adversarial findings
Ran a devil's-advocate pass on `bin/playbook-promote.sh` and `bin/check-playbook.sh` specifically on the two findings above (the sole trigger, per the Major finding). The adversarial pass independently reproduced both behaviors from the real files (not from the paraphrase in the prompt) and argued, with direct spec-line citations (Goal §"carries the retro's classification" at spec line 76, DP-e at spec line 529–537, DP-a's presence rationale at spec line 446–456), that the blank-`--bound-in` behavior is a genuine Major-level conformance gap against the frozen spec's explicit "passed through" language — not merely a CLI-intent nicety — because it breaks DP-e's premise that the checker sees every candidate bullet the CLI was given. It also independently confirmed the second finding's Minor severity, citing that `check-playbook.sh:206`'s preflight in the generator and the unconditional `Scope` enum/required check both guarantee no invalid entry ever reaches a shipped state or a green exit regardless of diagnostic completeness. Both conclusions match this review's own synthesis; no new finding surfaced beyond the two already listed.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | blank `--bound-in` silently omits the bullet before the checker sees it (`bin/playbook-promote.sh:192,300-302`) | primary: major (Codex-native P2) / adversarial: major | major | Draft reasoning during this review's own first pass leaned toward rejecting this as a non-issue (the *stored* entry is schema-valid, so no invariant on the file itself is broken) — before running the adversarial pass. The adversarial pass, reading the frozen spec text directly, showed the deviation is against the spec's literal "passed through to the emitted entry" contract (spec lines 76, 537) and DP-a's presence-based rationale (spec line 456), not merely a CLI-UX nicety; that argument is grounded in the frozen intent block and is accepted as final. |
| 2 | `Bound-in` shape check omits a blank-value violation when `Scope` is itself missing/unknown (`bin/check-playbook.sh:405-445`) | primary: minor (Codex-native P3) / adversarial: minor | minor | No change — both passes agree the overall exit code stays fail-closed (1) via the independent `Scope` violation and the generator's preflight, so this is a diagnostic-completeness gap, not a shipped-defect risk. |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the Major finding above linked (`bin/playbook-promote.sh`'s blank-`--bound-in` pass-through gap). The Minor finding may be fixed in the same pass or deferred per the fast-follow disposition below — engineer's choice, since it does not gate the schema's correctness.

#### Fast-follow disposition
- `bin/check-playbook.sh:405-445` — the `Bound-in` blank-value diagnostic-completeness gap when `Scope` is missing/unknown (Minor, above): intended disposition is **file-an-issue** (rationale: it is a real, spec-adjacent (provenance decision 3) discrepancy between the documented "independent of Scope's own validity" design intent and the actual behavior in one specific sub-case; low urgency since no fail-open results, but worth a small follow-up fix distinct from this task's Major so it does not get lost once the Major is addressed and this review's board line closes).


---

## Delta verification — rework round 1 (2026-07-31)

**Scope**: Codex round 1 REQUEST_CHANGES (Major: `bin/playbook-promote.sh` silently dropped an explicitly-blank `--bound-in` before the re-validation, contradicting DP-e's frozen "passed through unconditionally" pass-through contract). No re-freeze (fix stays inside AC16's allow-list; restores the frozen contract rather than changing it). Commit range verified: `3b94202` (my round-1 PASS) → `43e6618` (Codex REQUEST_CHANGES, board to REWORK) → `f35abcf` (engineer rework), HEAD now `f35abcf`.

### (1) `tests/playbook-promote/run.sh` — green, including the new labelled case; blank-`--bound-in` probed myself end-to-end on both scopes

```
$ bash tests/playbook-promote/run.sh
...
PASS: T-1007: an explicitly-blank --bound-in is not silently dropped and is rejected by the re-validation
...
All playbook-promote assertions passed.
```

Independently reproduced the fix's effect on QA-built fixtures (not reusing the engineer's suite case), covering **both** scopes as requested (the engineer's own labelled case covers only `loop`):

- `--scope loop --bound-in ""` → exit **1**, stderr `Scope is 'loop' but Bound-in is present` (the forbidden-**presence** check — DP-a's "a blank bullet is still a bullet" rule), lessons file byte-identical to its pre-run copy (`cmp -s`).
- `--scope maintainer --bound-in ""` → exit **1**, stderr `Scope is 'maintainer' but Bound-in is missing` (the mandatory-**value** check — a blank trimmed value reads the same as absence, DP-a's other half), lessons file byte-identical.

Both confirm the bullet now genuinely reaches the checker in both directions, and the checker's own pre-existing DP-a distinction (presence-tested on `loop`, value-tested on `maintainer`) is what actually adjudicates it — no new logic was added to the promoter itself, exactly as DP-e requires.

### (2) `check-acs.sh` → 19/19, `check-intent.sh` → aligned v1

- `bash bin/check-acs.sh .shell-team/specs/T-1007-scope-typed-ledger.md` → **19 passed, 0 failed, 0 skipped, 0 unrecognized**, live against `f35abcf`.
- `bash bin/check-intent.sh .shell-team/specs/T-1007-scope-typed-ledger.md .shell-team/todo.md` → `check-intent: aligned: T-1007 v1 (f90e62d35c97d69f9dbc99a040831cd17c57a6c0) matches …` — unchanged hash, correctly: this rework touched only `bin/playbook-promote.sh` and its test suite, neither inside the frozen intent-block markers.

### (3) Diff-scope confirmation

`git diff --name-only 43e6618..f35abcf` → exactly four files: `.shell-team/provenance/T-1007.md`, `.shell-team/todo.md`, `bin/playbook-promote.sh`, `tests/playbook-promote/run.sh` — matching the coordinator's claim exactly, and all four remain on AC16's original allow-list (no re-freeze needed, confirmed by AC16 itself passing inside the 19/19 above). Read the `bin/playbook-promote.sh` diff directly: a new `BOUND_IN_SEEN` flag set at parse time (`--bound-in) BOUND_IN="$2"; BOUND_IN_SEEN=1 ;;`), and the entry-construction gate changed from `[ -n "$BOUND_IN" ]` to `[ "$BOUND_IN_SEEN" -eq 1 ]` — exactly the described fix, nothing else touched in the file. Read the provenance diff: one new (4th) decision entry, grounded in the Goal/DP-e pass-through language, DP-a's presence-vs-value distinction, and the Codex round-1 review file itself — non-circular, ties to concrete loci.

### (4) Hygiene

- `bash bin/check-pii-shapes.sh --base develop` → `clean`
- `bash bin/check-commit-identity.sh --base develop` → `clean`
- `bash bin/check-provenance.sh .shell-team/provenance/T-1007.md` → `conformant: 4 decision entries, 0 sentinel`
- `shellcheck bin/playbook-promote.sh tests/playbook-promote/run.sh` → clean
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0

### (5) Full-CI re-run judgment: **narrow, not full** — spot-checked instead

Judged a full 45-step CI re-run **not necessary** this round. Reasoning: the entire diff is four files, and `bin/playbook-promote.sh` is the only one with genuine cross-suite reach; it carries no `file:line`-pinned dependency anywhere in the repo (`grep -n "playbook-promote.sh:" tests/errexit-safe/run.sh` → no hits — unlike `gen-playbook-blocks.sh`, which is what cost T-1006 a round), and the only other suites mentioning `playbook-promote` (`tests/errexit-safe/run.sh`, `tests/gen-playbook-blocks/run.sh`) do so in static comments/candidate-scan content, not as a runtime dependency on this script's behavior — confirmed by reading both hits directly. `tests/playbook-promote/run.sh` (the direct consumer) was re-run live above with 100% pass including the new case. Spot-checked `tests/errexit-safe/run.sh` anyway, for reassurance, since it is the exact regression class (stale file:line pins) that cost a prior round — **all assertions passed**, confirming this narrow, single-script edit introduced no accumulator/warning-site regression.

### (6) Independent mutation spot-check — the revert, reproduced myself

Reverted the fix on a **file-copy backup** (`cp bin/playbook-promote.sh` under `$TMPDIR`, restored via `cp`, never `git checkout`/`restore`): changed line 323's gate back from `[ "$BOUND_IN_SEEN" -eq 1 ]` to `[ -n "$BOUND_IN" ]`. Result on my own probe fixture: `--scope loop --bound-in ""` → **exit 0**, entry silently appended, `file byte-untouched: NO` — the exact bug Codex caught, reproduced independently. The engineer's own labelled suite case also went red: `FAIL: T-1007: --scope loop --bound-in '' must exit 1 (the re-validation's code), got 0`. Restored from the backup copy; `diff` confirmed byte-identical; re-ran the suite and my own probe — both green again, `git status --short` empty throughout.

### (7) Fast-follow disclosure — not re-litigated

The Minor (a `Bound-in` shape-check diagnostic gap when the same entry's `Scope` is also invalid) is deliberately left untouched this round, per provenance decision 3 (already reviewed in my original PASS, §3 above) and the coordinator's message — the orchestrator files it as a fast-follow at the recording step. Not a blocker for this delta.

### Delta verdict: PASS

The Codex Major is resolved exactly as claimed, through the correct mechanism (restoring the frozen DP-e pass-through contract rather than changing it — no re-freeze needed). Verified independently on both scopes (`loop` and `maintainer`), not just the engineer's single labelled case. No new regression introduced; all 19 ACs, intent alignment, hygiene, and the direct consumer suite are green. Recommending `READY_FOR_REVIEW`.

---

### Codex Review verdict (round 2 — delta): APPROVE
- Task: T-1007
- Codex model: gpt-5-codex
- Verification mode: static-only
- Diff scope (delta): `43e6618..f35abcf`, 4 files, +58/-6 lines (`bin/playbook-promote.sh`, `tests/playbook-promote/run.sh`, `.shell-team/provenance/T-1007.md`, `.shell-team/todo.md`); HEAD verified at `7e8b1f1` (QA's delta-PASS commit) with no further tracked changes on top.

Intent-block re-check: `check-intent` (re-run live) still reports `aligned: T-1007 v1 (f90e62d35c97d69f9dbc99a040831cd17c57a6c0)` — unchanged hash, correctly, since this rework touches only `bin/playbook-promote.sh` and its test suite, neither inside the frozen intent-block markers (spec lines 37–433). No re-freeze was needed or claimed. Provenance file re-read directly: the new (4th) decision entry grounds the fix in the frozen spec's Goal/DP-e "passed through" language, DP-a's presence-testing rationale, and my own round-1 review file by name — non-circular, cites concrete loci, `check-provenance.sh` confirms `4 decision entries, 0 sentinel`.

#### Round-1 Major — resolved
`bin/playbook-promote.sh` now tracks `BOUND_IN_SEEN` (set at CLI-parse time, independent of the trimmed value) and gates bullet emission on it rather than on non-emptiness. Verified independently, not just by re-reading the diff:

- Ran `tests/playbook-promote/run.sh` live — green, including the new labelled case (`T-1007: an explicitly-blank --bound-in is not silently dropped and is rejected by the re-validation`).
- Reproduced QA's own two-scope probe myself: `--scope loop --bound-in ""` → exit 1, `Scope is 'loop' but Bound-in is present` (forbidden-presence, DP-a); `--scope maintainer --bound-in ""` → exit 1, `Scope is 'maintainer' but Bound-in is missing` (mandatory-value, DP-a's other half) — both via `--lessons` override, both leaving the lessons file byte-identical (`cmp -s`).
- Traced `$LESSONS` through the script: whether set by `--lessons` or by the default resolver (lines 258–272), every downstream step (candidate build, real write) reads the same single variable — there is exactly one code path, so "does `--lessons` override change anything" resolves to no, by construction, not by a separate check that could itself be missed.
- Codex's own round-2 pass (`codex exec`, direct file reads, same conclusion, could not run the live suite itself inside its read-only sandbox — temp-dir creation was denied there — but static tracing plus my own live runs cover that gap) confirms: no remaining path where an explicit CLI `--bound-in` signal is dropped or altered before `bin/check-playbook.sh`'s re-validation sees it.

**Question (2), arg-parsing soundness — probed live, all sound:**
- Repeated `--bound-in` (`--bound-in "docs/first.md" --bound-in ""`): last value wins for both `$BOUND_IN` and `$BOUND_IN_SEEN` (both fields are plain scalar overwrites, not accumulators) — verified: candidate correctly built with the blank last value, rejected at exit 1 with `Scope is 'loop' but Bound-in is present`, lessons file untouched.
- `--bound-in` as the last argument with no value following: `[ "$#" -ge 2 ] || die "missing value for $1"` fires before any assignment — verified live: `missing value for --bound-in`, exit 2, no partial state (`$BOUND_IN_SEEN` never flips).
- `--bound-in=value` (equals-sign form): not matched by the exact-match `case` arm, falls to `*) die "unknown argument: $1"` — verified live: `unknown argument: --scope=loop`, exit 2. This is pre-existing behavior across every flag in this script (no `=`-form is supported anywhere), not something this fix changed or needed to change — no silent misparse.

**Question (3), anything else in the new bytes:** No. The new test case pins exit code, the checker-sourced reason string, and byte-untouched-on-reject in one assertion — a genuine regression lock, not a vacuous one (independently reproduced going red on a file-copy revert of the fix by both QA and, per its own account, the engineer). The provenance addition is grounded and non-circular. Nothing in the diff reopens or narrows the round-1 analysis.

#### Round-1 Minor — untouched, as instructed
`bin/check-playbook.sh`'s `Bound-in` blank-value diagnostic gap when `Scope` is itself missing/unknown is confirmed still present (unchanged code, unchanged behavior) and was correctly left untouched this round per the coordinator's disposition — it does not gate this delta's verdict since the overall exit code stays fail-closed via the `Scope` violation regardless.

#### Adversarial findings
Not run this round — no blocker/major surfaced in the round-2 pass (the sole trigger condition), and the round-1 Major under review here is the one already adversarially tested in round 1 (`.shell-team/reviews/T-1007-codex-adversarial.txt`), whose conclusion (real defect, spec-grounded, Major) was what round 1 required be fixed; this round confirms the fix rather than re-litigating the original severity call.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Major (blank `--bound-in` silently dropped) | round-2 primary: resolved | resolved — closed | Fix verified independently (live suite run, two-scope manual reproduction, `$LESSONS`-path tracing, arg-parsing edge probes) and confirmed by Codex's own round-2 read of the real files; no remaining path found. |
| 2 | round-1 Minor (`Bound-in` diagnostic gap when `Scope` invalid) | round-2 primary: unchanged | minor (fast-follow, per round-1 disposition) | Explicitly left untouched by instruction this round; round-2 Codex pass independently re-confirmed the code is unchanged and the gap remains fail-closed overall (no severity change). |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`.

#### Fast-follow disposition
- Carried forward unchanged from round 1: `bin/check-playbook.sh:405-445`'s `Bound-in` diagnostic-completeness gap when `Scope` is also missing/unknown — intended disposition remains **file-an-issue** (same rationale as round 1: real, provenance-adjacent discrepancy between documented intent and one specific sub-case's behavior; low urgency, no fail-open). No new deferrals this round.

