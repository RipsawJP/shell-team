# T-1009 — QA verification

**Verdict: PASS** → board to `READY_FOR_REVIEW`

## Mechanical layer

- `bash bin/check-acs.sh .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md`: **19 PASS, 0 FAIL, 1 SKIP** (AC20, by design — matches the spec's own disclosed layering).
- `bash bin/check-intent.sh .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md .shell-team/todo.md`: `aligned: T-1009 v1 (28fedc019a685a3ea4a5ff98446d54d09486ec9e)`.
- Structural check on the frozen intent block: `<!-- BEGIN intent-block: T-1009 -->` at line 51, `<!-- END intent-block: T-1009 -->` at line 540. `## Resolved design decisions` starts at 542 and `## Pointer sweep` at 679 — both strictly below the END marker, so the engineer's/pm-spec's non-frozen additions never touch the frozen Goal/Non-goals/AC/Input space.
- Full CI-ordered run, in `.github/workflows/check-handoff.yml`'s file order (the exact pinned-arg shellcheck step at 0.11.0 — matches CI's pin exactly — every fixture suite, every "Dogfood …" step, both PR-diff steps run against local `develop` since origin is unreachable from this sandbox): **0 failures** across every step I could exercise locally.
- Hygiene: `check-handoff.sh` on the resolved board, `check-board-headings.sh --base develop`, `check-pii-shapes.sh --base develop`, `check-commit-identity.sh --base develop`, `check-provenance.sh .shell-team/provenance/T-1009.md` (conformant, 2 decisions, 0 sentinel), `check-interventions.sh .shell-team/interventions/T-1009.md` (conformant, 0 entries, 1 sentinel) — all exit 0.
- `git diff --name-only 0906862` matches AC19's allow-list exactly (10 files, verified both mechanically and by eye). `git status --short` was clean throughout verification, apart from this review file and the board append.
- `bin/team-paths.sh --get specs` / `--get reviews` actually run and resolve to `.shell-team/specs` / `.shell-team/reviews` — the resolver-form paths the rewritten `docs/workflow.md`/`.ja.md` bullets point at are not aspirational.
- `check-handoff.sh` resolves as a bare command on `PATH` (plugin loaded globally) — the DP-2 claim underlying the corrected `Rule`/`How to apply` wording is not aspirational either.

## The judgment layer (residual, not mechanically provable)

**1. Cross-language semantic equivalence** — read `docs/workflow.ja.md`'s `## 規約` against `docs/workflow.md`'s `## Conventions`, bullet by bullet, including the newly-added PR-body-diff-stats bullet. All five bullets carry matching meaning (branch: direct feature-branch work, worktree only on orchestrator opt-in, naming convention deferred to `CONTRIBUTING.md`; specs/reviews: same resolver-form paths; PR diff stats: same re-measure-before-merge caveat, same "silently stale" reasoning; never-edit-`.claude/agents/*`: same). The Japanese reads as natural prose, not token-preserving filler. The engineer's own mutation self-check (rewriting the ja branches bullet to assert the *opposite* claim while keeping every substring AC1/AC2 grep for) is a genuine, confirmed blind spot: I did not need to re-run it myself, since the current tree already demonstrably is *not* that mutated state (confirmed by direct reading plus the current 19-PASS run), and re-deriving the same demonstration would not add information beyond independently reading both sections, which I did.

**2. The corrected corpus entry's residual truth** — read `Rule`/`Why`/`How to apply` against the frozen replacement table (`git diff 0906862 -- .shell-team/lessons.md` matches the table's three substitutions byte-for-byte). `Why`'s narration is genuinely past-tense and attributed ("the repository where the incident happened"); a clause inside it ("ran over the live board automatically... in the repository where the incident happened") momentarily reads like an automated-gate description, but the very next clause ("whether any given repository automates it that way is its own workflow's decision") scopes it back to the one historical incident and explicitly declines to generalize — no adopter or this-repository automation is implied. `Rule`'s spliced form ("...is a format mismatch the hand-off linter (`check-handoff.sh`) rejects") states only what the linter does when invoked, never that anything invokes it automatically — true in an arbitrary adopter repository with the plugin loaded, by construction.

**3. The workflow docs' rewritten bullets against the tree** — the branches bullet keeps the loop content (feature branch; worktree only on orchestrator opt-in) and asserts no naming form; `CONTRIBUTING.md:102` and `README.md:164`/`README.ja.md:164` all still point at `docs/workflow.md` accurately (confirmed by direct `grep`); `bin/team-paths.sh --get specs|reviews` both actually resolve (confirmed above).

**4. Mutation spot-check, independently reproduced** (never `git checkout` — file-copy backups under `$TMPDIR`, restored and confirmed byte-identical with `diff` before re-running `check-acs.sh`):
  - Group A: restored `feat/T-XXX-<slug>` into `docs/workflow.md`'s branches bullet → **AC1 FAIL** as predicted. Restored from backup, `diff` confirmed byte-identical, AC1 back to **PASS**.
  - Group B: retitled the corpus entry's heading (`The board` → `The BOARD-MUTATED`) → **AC6, AC7, AC9 all FAIL** as predicted (byte-lock broken, meta-fields diverge, numstat no longer `3/3`). Restored from backup, `diff` confirmed byte-identical, all three back to **PASS**; full re-run confirmed 19 PASS / 1 SKIP / 0 FAIL and `git status --short` clean.

## AC20 — pointer sweep, independently re-run and audited

Re-ran the same seven-token sweep (`grep -rn --exclude-dir=.git -F -- "<token>" .`) from the repository root. My raw hit counts run higher than the engineer's reported numbers (e.g. `board-lint`: 24 vs. reported 17; `Lint tasks/todo.md`: 34 vs. reported 15). Traced the delta: the excess hits are entirely in **git-ignored, untracked local Codex-review trace files** (`.shell-team/reviews/*.jsonl` / `*.txt` raw dumps from past task rounds, present on this persistent checkout but not part of the shipped tree) plus this task's own spec/board/provenance quoting the defect for description — both categories are already covered by the spec's own classification reasoning ("frozen records" / "this task's own hand-off, permitted"), even though the untracked trace files weren't individually enumerated in the reported count. This is a **minor completeness gap in the reported raw numbers**, not a classification error: the load-bearing mechanical property — that neither `board-lint`, `Lint tasks/todo.md`, nor `the same check CI runs` survives anywhere under `agents/ templates/ skills/ bin/` — is independently confirmed via AC11/AC14 (both PASS, re-run by me), which is the only place the spec's Non-goals and the corpus's shipping surface actually require zero hits. No hit outside the allow-list that I found was misclassified.

## Edge cases tried

- Confirmed the diff's blast radius by eye: exactly the ten allow-listed paths, no `bin/` file touched, `templates/prompt-blocks/playbook-{qa-verifier,tech-lead}.md` and `agents/{qa-verifier,tech-lead}.md` untouched (byte-identical to `0906862`, AC13 PASS + my own `cmp`).
- Ran `bin/team-paths.sh --get specs|reviews` for real (not read from the doc) to confirm the resolver-form paths in the rewritten bullets are not aspirational (checklist class 4 — test-harness short-circuit).
- Ran `which check-handoff.sh` to confirm the bare-command reference in the corrected `Rule`/`How to apply` resolves on `PATH` for real, matching DP-2's grounding.
- Two independent mutation reproductions (one Group A, one Group B) from fresh file-copy backups, both restored byte-identical — see above.
- This task's deliverable is prose/corpus-only (no new `bin/` script, no new verification mechanism); the adversarial fixture-synthesis checklist's regex/CommonMark-parsing classes do not apply structurally (no new parser was authored — `check-playbook.sh`/`check-prompt-sync.sh` etc. are pre-existing and unchanged), consistent with the spec's own DP-4 task classification.

## Risk notes for reviewer

- **Minor, non-blocking**: AC20's reported pointer-sweep counts in the board hand-off undercount the raw repo-wide grep by roughly the untracked local Codex-trace scratch files present in this checkout (git-ignored `.shell-team/reviews/*.jsonl`/`*.txt`). The substantive property — zero forbidden-token survival on any shipped surface (`agents/ templates/ skills/ bin/`) — is independently confirmed and unaffected; recording for awareness only.
- The one genuinely unverifiable-by-command property (§1/§2 above — cross-language meaning equivalence, and whether the `Why`'s wording smuggles in an automation assumption) was read by hand as this review's own primary contribution; both hold up on inspection. No `out-of-input-space` exemption was needed — no exploratory edge case I synthesized relied on an out-of-scope synthetic extreme.

## Codex Reviewer round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1009
- Codex model: gpt-5-codex (via `codex exec`, codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 12 files, +1009/-13 (develop 0906862...HEAD f1c4ad0)

#### Blockers
- None.

#### Major
- `docs/workflow.md:101-102` and `docs/workflow.ja.md:99-100` — the new "Specs"/"仕様" and "Review artifacts"/"レビュー成果物" bullets tell the reader `<specs dir>` / `<reviews dir>` is `bin/team-paths.sh --get specs|reviews` (literal `bin/`-prefixed path). In an adopter repository scaffolded by `team-init`, `bin/team-paths.sh` does not exist locally — `docs/adopting.md` itself documents that the plugin lives centrally and each adopting repo holds only the per-repo *instances*; the plugin's `bin/` scripts are exposed as bare `PATH` commands, never copied into the adopter's tree. A path string containing `/` bypasses `PATH` lookup in bash, so an adopter following this instruction verbatim gets "no such file or directory," not a resolved path. Every other agent-facing surface in this repo phrases the identical resolver reference as `team-paths.sh --get X` (on PATH when the plugin is loaded; else `bin/team-paths.sh`) — e.g. `agents/codex-reviewer.md:10`, `agents/engineer.md:10`, `agents/qa-verifier.md:10`, `agents/scrum-master.md:13`. `docs/workflow.md` is exactly the dual-audience surface T-1009's own Goal targets (`README.md:164` sends adopters here, `CONTRIBUTING.md:102` sends contributors here), and the spec's Goal states the corrected wording must be "true in *any* repository running this loop... true in an adopter's repository too" — this phrasing reintroduces a maintainer-environment assumption (this dev checkout's own `bin/` directory) into the exact class of document the task exists to fix. AC2's `check:` only greps for the substring `team-paths.sh --get specs`/`--get reviews` by containment, so it passes regardless of the `bin/` prefix and does not catch this — a mechanical-check gap, not evidence the wording is fine.
  - Suggested fix: reword both bullets (English and Japanese) to `team-paths.sh --get specs` / `team-paths.sh --get reviews` (on PATH when the plugin is loaded; else `bin/team-paths.sh`, e.g. in this dev checkout), matching the pattern already established in every `agents/*.md` operating-paths note. AC2 could additionally be tightened to reject an unconditional `bin/team-paths.sh --get` form, but the wording fix is the load-bearing part.

#### Minor / nits
- QA's already-logged Minor (AC20 raw pointer-sweep counts run higher than the reported board-hand-off numbers) — independently re-read; QA's tracing of the delta to gitignored local Codex-trace scratch files plus this task's own spec/board/provenance self-reference is correct and the load-bearing property (zero forbidden-token survival under `agents/ templates/ skills/ bin/`, AC11/AC14) is unaffected. No further action needed from this review.

#### Adversarial findings
- Re-ran Codex in devil's-advocate mode specifically against the `bin/team-paths.sh` wording, cross-referencing `docs/adopting.md` and every `agents/*.md` operating-paths note. It independently confirmed the same defect on both `docs/workflow.md:101` and `docs/workflow.ja.md:99` (both major, matching the primary pass) and reported no further, distinct regression in `.shell-team/lessons.md`, `agents/pm-spec.md`, `agents/engineer.md`, or either `playbook-{pm-spec,engineer}.md` block — those four shipped-surface files' new `check-handoff.sh` references correctly use the bare, PATH-exposed command form (matching DP-2's own grounding and AC10's PASS).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | no severity changes | — | — | primary reported P2 → major; adversarial independently reproduced the same major on both files; no upgrade, downgrade, or rejection between passes |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the finding above linked.

#### Fast-follow disposition
- QA's AC20 raw-count Minor: won't-fix — the delta is fully traced to gitignored local scratch artifacts and this task's own self-referential prose, both already covered by the spec's own classification reasoning; the mechanically load-bearing property is independently green via AC11/AC14 and needs no further work.

---

**Independent verification performed by this reviewer (beyond delegating to Codex):** re-ran `bash bin/check-acs.sh .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md` (19 PASS / 0 FAIL / 1 SKIP, matching QA) and `bash bin/check-intent.sh .shell-team/specs/... .shell-team/todo.md` (`aligned: T-1009 v1 28fedc019a685a3ea4a5ff98446d54d09486ec9e`); read the frozen intent block (Goal/Non-goals/AC/Input space) and `.shell-team/provenance/T-1009.md` as the receiving canon for DP-1 (in-place edit vs. supersession) and DP-2 (no-CI-claim, bare-command naming) — both hold against the corpus's own `Superseded-by` semantics and the disposition ledger's stated five-column contract (`docs/loop-engineering/lessons-import-disposition.md`); confirmed the "nothing enforces ledger⇄corpus key equality" measurement is accurate (no `bin/` script or CI step names the ledger); diffed `.shell-team/todo.md` against `git show develop:.shell-team/todo.md` and confirmed the T-1009 entry is a pure insertion (0 removed lines); spot-checked the pointer-sweep classification table and AC11/AC14's positive-control non-vacuity. No `bin/check-intent.sh`/`bin/check-provenance.sh` execution was performed by this role beyond confirming their exit codes as part of the AC re-run — the semantic judgment of whether DP-1/DP-2's grounding is sound was read directly, per this role's brief.

## QA delta verification (rework round 1)

**Verdict: PASS** → board to `READY_FOR_REVIEW`

### Diff scope
`git diff fd25fdf..HEAD --stat`: exactly `docs/workflow.md`, `docs/workflow.ja.md`, `.shell-team/provenance/T-1009.md`, `.shell-team/todo.md` — nothing else. Read the diff whole (not just the stat): both doc files' change is confined to the two "Specs"/"Review artifacts" (仕様/レビュー成果物) bullets; provenance gained a third decision/reason/grounding triple; the board carries the status flip plus the engineer's rework hand-off bullet.

### The fix closes Codex round 1's Major
`grep -n "team-paths.sh" docs/workflow.md docs/workflow.ja.md` shows the only remaining `bin/team-paths.sh` token in each file sits inside the parenthetical fallback — `(on `PATH` when the plugin is loaded; else `bin/team-paths.sh`)` in English, `（プラグインが読み込まれていれば `PATH` 上にある。そうでなければ `bin/team-paths.sh`）` in Japanese — never as the primary instruction. This matches `agents/engineer.md:10`'s precedent phrasing verbatim in structure (`team-paths.sh --get X` (on PATH when the plugin is loaded; else `bin/team-paths.sh`)), which is exactly what Codex round 1's "Suggested fix" asked for. Confirmed the resolver form is not aspirational: `bin/team-paths.sh --get specs` / `--get reviews` actually resolve on this checkout.

### Cross-language equivalence
Read both ja bullets against the en originals — "仕様" and "レビュー成果物" carry the identical resolver-plus-fallback structure and mean the same thing as their English counterparts, in natural Japanese prose (not token-preserving filler). This is the blind spot the round-1 review flagged as unverifiable-by-command; read by hand here, holds.

### Mechanical (all re-run fresh in this round)
- `bash bin/check-acs.sh .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md`: **19 PASS, 0 FAIL, 1 SKIP** (AC20, by design — unchanged from round 1).
- `bash bin/check-intent.sh .shell-team/specs/T-1009-doc-drift-and-false-ci-claim.md .shell-team/todo.md`: `aligned: T-1009 v1 (28fedc019a685a3ea4a5ff98446d54d09486ec9e)` — same frozen intent hash as round 1, unaffected by the rework (the intent block sits above both the doc bullets and the provenance/board additions).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1009.md`: **conformant (3 decision entries, 0 sentinel)** — the new third triple's grounding is non-circular: it cites the round-1 review's own Major, `agents/engineer.md:10`'s precedent phrasing, and AC2's `check:` line (all three verifiable independently of the decision text itself).
- `bash bin/check-pii-shapes.sh --base develop`: clean.
- `bash bin/check-commit-identity.sh --base develop`: clean.
- `bash bin/check-handoff.sh "$(team-paths.sh --get todo)"`: exit 0.
- `bash bin/check-board-headings.sh "$(team-paths.sh --get todo)" --base develop`: exit 0.
- `bash bin/check-interventions.sh .shell-team/interventions/T-1009.md`: conformant (0 entries, 1 sentinel) — unchanged.

### Full-vs-delta judgment
The engineer ran delta-only (no full CI re-run) and grounded that as reasonable since the round's diff touches no `bin/` script, no checker, no board mechanics, no agent/template surface. Independently audited that judgment: `grep -rl "docs/workflow" tests/` and a broader `grep -rl "workflow" tests/ bin/` turn up only incidental uses of the English word "workflow" in unrelated fixtures/scripts (`close-out.sh`, `check-prompt-sync.sh`, `discover-work.sh`, `gen-playbook-blocks.sh`, `playbook-promote.sh`, `consolidate-proposals.sh`) — none of them load or reference `docs/workflow.md`/`docs/workflow.ja.md` content. Ran the suites most plausibly reachable anyway as a spot-check: `tests/errexit-safe/run.sh` (all assertions passed), `tests/codex-skeleton-hygiene/run.sh` (OK), `tests/check-readme-version/run.sh` (all assertions passed), `tests/check-contract/run.sh` (OK) — all green, no regression. No test suite in this repo has an input path through either doc file, so the engineer's delta-only judgment is sound, not merely asserted.

### Independent mutation spot-check (never `git checkout` — file-copy backup under `$TMPDIR`)
Copied `docs/workflow.ja.md` to a `$TMPDIR` backup, confirmed byte-identical, then reverted its two bullets' primary form to the pre-fix literal `bin/team-paths.sh --get specs`/`--get reviews` (matching the `fd25fdf` wording exactly, via `git show fd25fdf:docs/workflow.ja.md`). Re-ran `check-acs.sh`: still **19 PASS / 0 FAIL** — no AC catches the reverted form, confirming AC2's containment-only gap is symmetric across both language files (the engineer's provenance entry claimed this for `docs/workflow.md` alone; this independently proves it also holds for `docs/workflow.ja.md`). Restored from the backup, `diff` confirmed byte-identical, `git status --short` clean, re-confirmed 19 PASS / 0 FAIL. This detector gap stays with codex-reviewer as the finding's holder (per the round-1 Major, mechanically unclosed by design — the wording fix is the load-bearing remediation, not a tightened AC2).

### Edge cases tried
- Confirmed `agents/qa-verifier.md` and `agents/tech-lead.md` remain byte-identical to `0906862` (untouched by this rework round, as Codex's adversarial pass also found for round 1).
- Grepped for any test-suite reference to either doc file before accepting the engineer's no-full-CI judgment (see above) rather than taking the claim at face value.

### Risk notes for reviewer
- The AC2 containment-only gap (does not distinguish `bin/team-paths.sh --get X` from `team-paths.sh --get X (... else bin/team-paths.sh)`) is now independently confirmed symmetric across both `docs/workflow.md` and `docs/workflow.ja.md`. This is a known, disclosed detector gap from round 1 — not a new finding — and stays with codex-reviewer as the appropriate holder (AC2 could be tightened per Codex's own suggestion, but the wording fix is what closes the Major).
- No new out-of-input-space edge case was synthesized this round; the delta is a narrow two-file prose fix and the spot-checks above cover its full blast radius.
