# Codex cross-provider review — T-1146

## Round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1146
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 9 files, +487/-12 lines (`develop...HEAD`)

#### Blockers

- none

#### Major

- `bin/check-codex-agents.sh:167` — The legacy-file remedy hint now unconditionally appends `--roles "$ROLES"` (this invocation's own resolved role list) and still claims `(removes this file by name)`. When the checker was itself invoked with a `--roles` value that excludes `code-reviewer` (a reachable shape under the spec's own Input space item 6 — "a checker invocation scoped with `--roles` to a subset of the five"), the checker still flags the legacy occupant (the "owned" test only checks membership in the resolved role list, unrelated to the removal gate), and the printed remedy carries that same excluding `--roles` value. Running that literal command does **not** remove the file, because `bin/gen-codex-agents.sh`'s own removal step is gated on `code-reviewer` being among the *generator* invocation's requested roles (Input space item 7) — the command silently exits 0, emits **zero** removal-notice bytes, and the legacy symlink is still present afterward. I reproduced this live end-to-end (seeded a dangling symlink under `--roles 'pm-spec engineer'`; the checker flagged it and printed `--roles "pm-spec engineer" (removes this file by name)`; running that exact printed command left the symlink in place with empty stderr). This is a regression this task's own fix introduces: before this diff, the printed remedy never carried `--roles` at all, so the bare command always fell through to the generator's own *default* role list — which always includes `code-reviewer` — so the pre-existing remedy was always truthful (if over-widening, which is exactly what issue #546 asked to narrow). Narrowing the scope to match the invocation, as this task does, is the right fix for the widening complaint, but it was not cross-checked against the case where that narrower scope itself excludes `code-reviewer` — a case neither AC5's nor AC6's own fixture exercises (`SCOPED_ROLES='pm-spec engineer code-reviewer'` in both `tests/codex-agents/run.sh` includes `code-reviewer`). Suggested fix: have the checker's hint distinguish whether `code-reviewer` is in `$ROLES_ARR` before choosing the hint text — e.g. print the scoped command plus an explicit note that it will not remove the file because `code-reviewer` is not in this invocation's role list, and name the actual removal command (with `code-reviewer` added) or a manual `rm` as the real remedy — and add a regression fixture that executes the extracted hint under a `--roles` value excluding `code-reviewer`, asserting the file is still present and no notice was printed, so the assumption is pinned rather than silently reintroduced.

#### Minor / nits

- `bin/check-codex-agents.sh:167` — The same reused hint text (`(removes this file by name)`) is also emitted for a legacy **FIFO**, **socket**, or **directory** occupant, none of which the generator's removal gate ever removes (it only unlinks `-f`/`-L` occupants). **Pre-existing, unrelated to this diff**: this exact phrase already existed, unconditionally, for every occupant type before T-1146 (the removal gate was already `-f`-only, already false for these occupant types, already reachable through the pre-existing `[ -e "$f" ] || continue` glob guard). Not introduced or worsened here. Worth a future occupant-type-aware hint (e.g. "will not remove: not a regular file or symlink").
- `bin/check-codex-agents.sh:90` — `ROOT="${ROOT%/}"` followed by `[ -n "$ROOT" ] || ROOT="."` means an exact `--root /` is normalized to `.` (the current directory) in the checker, while `bin/gen-codex-agents.sh`'s own root handling (unread in this round beyond what the spec cites) is not necessarily identical — a discrepancy that could point the checker's audit at the wrong tree for that one input. **Pre-existing, entirely untouched by this diff** (no line in this region is part of the reviewed change).
- `bin/check-codex-agents.sh:167` — `$GENERATOR`, `$ROOT` and `$OUT_DIR` are interpolated into the printed remedy inside double quotes without shell-escaping; a path containing `$`, a backtick, an embedded quote or a backslash would produce a re-run command that parses differently than intended when copied. **Explicitly out of scope by the frozen spec's own Input space** ("Out-of-scope synthetic extremes" item 4: "`--roles` values carrying shell metacharacters ... Role-token validation is the generator's own existing surface, unchanged by this task; the hint quotes what it was given" — and Non-goals/threat-model item 5 states the operator and working directory are trusted). This interpolation shape (`--root \"$ROOT\" --out-dir \"$OUT_DIR\"`) also predates this task; T-1146 only adds one more interpolated value (`--roles`) to an existing pattern.

#### Adversarial findings

A dedicated devil's-advocate pass over `bin/gen-codex-agents.sh`, `bin/check-codex-agents.sh` and `tests/codex-agents/run.sh` (framed to specifically hunt for more of the same class once the Major above was found) independently reproduced the identical Major finding above and surfaced the three Minor findings listed above; it did not find anything beyond those four, and in particular found no case where the generator's widened `[ -f … ] || [ -L … ]` predicate or the checker's widened `[ -e "$f" ] || [ -L "$f" ] || continue` guard match or remove/report the *wrong* occupant — both predicates were confirmed to only ever affect the one hardcoded legacy basename, consistent with AC4's occupant matrix and this review's own independent read of the diff.

#### Synthesis audit ledger

no severity changes

#### Recommendation

- REQUEST_CHANGES → set back to READY_FOR_ENG with the Major finding above linked (`bin/check-codex-agents.sh:167`: the `--roles`-scoped legacy-file remedy claims removal it will not perform when the resolved role list excludes `code-reviewer`).

#### Fast-follow disposition

- `bin/check-codex-agents.sh:167` — same misleading `(removes this file by name)` claim for a legacy FIFO/socket/directory occupant. Pre-existing, unrelated to this diff. Intent: file-an-issue (occupant-type-aware hint wording is a small, well-scoped follow-up; not this task's regression to fix).
- `bin/check-codex-agents.sh:90` — `--root /` normalizes to `.` in the checker; possible mismatch against the generator's own root handling. Pre-existing, unrelated to this diff. Intent: file-an-issue (worth confirming/aligning the two scripts' root normalization in a future task; low likelihood of real-world impact).
- `bin/check-codex-agents.sh:167` — unescaped interpolation of `$GENERATOR`/`$ROOT`/`$OUT_DIR`/`$ROLES` into the printed remedy. Intent: won't-fix (the frozen spec's own Input space names this exact class — role-token and path-metacharacter shapes — as an explicit out-of-scope synthetic extreme, and the Non-goals' threat model states the operator and working directory are trusted; nothing here crosses a trust boundary).

## Verification performed this round

Independently re-run against this working tree before judging the findings above (not trusted from the hand-off/QA summaries blind):

- `bash tests/codex-agents/run.sh` → exit 0, all assertions pass including the 3 `T-1146`-labelled fixtures.
- `bash tests/check-binding/run.sh` → exit 0, all assertions pass including `cb-refusal-matrix-complete`.
- `shellcheck bin/gen-codex-agents.sh bin/check-codex-agents.sh bin/check-binding.sh tests/codex-agents/run.sh tests/check-binding/run.sh` → exit 0, all five files.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1146-codex-agents-migration-followups.md` → `check-acs: 14 passed, 0 failed, 0 skipped, 0 unrecognized`.
- Live reproduction of the Major finding (seeded a dangling symlink at `shell-team-codex-reviewer.toml` under a scratch out-dir generated with `--roles 'pm-spec engineer'`; ran `bin/check-codex-agents.sh` with the same scoped `--roles`; extracted and ran the exact printed remedy command; confirmed the symlink survives and no removal notice is printed).
- Read the full `git diff develop...HEAD` for all three `bin/` scripts and both fixture suites against the frozen spec's Goal/Non-goals/Input-space; confirmed the widened predicates touch only the one hardcoded legacy basename, the two adjacent owned-role guards (`gen-codex-agents.sh:414`, `check-codex-agents.sh:119`) are byte-unchanged, and `check-binding.sh`'s diff is confined to the header comment block (adds `collision (1)`, nothing else).
- Review questions (a)/(c)/(d) from the briefing: confirmed correct by direct read and reproduction — (a) `rm -f` on a symlink never follows to its target, confirmed both by code read and by AC4's own live occupant matrix; (c) `cb-refusal-matrix-complete` performs a live `calls()`/`hdr()` extraction against the file as it exists at run time (not a pinned literal set), so a future `refuse <newtoken> <exit>` call site added without a matching header entry would genuinely red the case; (d) the class enumeration in the spec and QA's independently-re-derived fixture-vacuity checks (both new codex-agents fixtures and `cb-refusal-matrix-complete` shown to fail against the base-ref scripts) are honest and non-vacuous. Review question (b) is answered by the Major finding above: the extraction/`eval` path in AC6 itself does not break on any declared-input-space shape, but the printed remedy's own truthfulness does, on a shape (`--roles` excluding `code-reviewer`) that AC5/AC6's own fixtures never exercise.
- Environmentally-unverified: GitHub issues #546/#545's actual canonical text — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation), same as pm-spec's and QA's own disposition of this pair.

## Per-pass provenance

**Round-2 correction (code-reviewer):** the `pass-role`/`briefing-fidelity` field values immediately below, and a `raw-capture` field per pass, are corrected/added this round to conform to `bin/check-review-input.sh`'s closed vocabulary (`generation`/`confirmation` for `pass-role`; `carried`/`not-carried`/`not-applicable` as `briefing-fidelity`'s first token) — round 1's own record was written with descriptive prose in those two fields instead, and carried no `raw-capture` line at all, which `bash bin/check-review-input.sh --record <this file> --task T-1146` reds on the round-1-only blob (`5f95ca97`) with `field-missing`, confirmed live before this correction (`bin/close-out.sh` wires this checker into its own merge-gate check, so an unfixed record would have refused close-out later rather than here). Nothing about round 1's verdict, findings or judgment changes below; the descriptive prose these two fields carried is preserved as a plain sentence immediately above each field block instead.

Round 1's primary pass ran the `codex exec … review --base develop --json` subcommand — a base-diff review with no free-form prompt, as prescribed by the default fresh-review path.

- pass-role (primary): generation
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <repo-root>/.shell-team/reviews/T-1146-codex-primary.txt > <repo-root>/.shell-team/reviews/T-1146-codex-primary.jsonl 2>&1`
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; ran exactly the role file's primary-pass skeleton with no deviation.
- raw-capture (primary): T-1146-codex-primary
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Round 1's adversarial pass was a devil's-advocate pass on the two edited scripts, triggered by the Major finding the primary pass surfaced, per the role file's step-5 mandate.

- pass-role (adversarial): generation
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/T-1146-codex-adversarial.txt "Play devil's advocate on the diff between develop and HEAD in this repo, focused on bin/gen-codex-agents.sh and bin/check-codex-agents.sh (run: git diff develop...HEAD -- bin/gen-codex-agents.sh bin/check-codex-agents.sh tests/codex-agents/run.sh). A prior review pass already found: the checker's legacy-file remedy hint now unconditionally appends --roles \"$ROLES\" ... and claims '(removes this file by name)' — but when the invocation's own --roles excludes code-reviewer, running that literal printed command does NOT remove the file ... Confirm or refute that this is a genuine defect ..., assess its severity ..., and look for anything ELSE that could break in the same class ... Read the files yourself with git show / sed / nl -ba — do not expect any diff or excerpt in this prompt. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <repo-root>/.shell-team/reviews/T-1146-codex-adversarial.jsonl 2>&1`
- briefing-fidelity (adversarial): carried — the primary pass's own finding (the exact excluding-`--roles` shape and its reproduction) was relayed into the prompt as context, per step 5's own instruction to target "those specific files"; not a verbatim copy of any fixed template since the role file leaves this pass's exact wording to the reviewer.
- raw-capture (adversarial): T-1146-codex-adversarial
- self-detected-host (adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Raw captures: `T-1146-codex-primary.{txt,jsonl}`, `T-1146-codex-adversarial.{txt,jsonl}` in this directory.

## Round 2

### Codex Review verdict: APPROVE
- Task: T-1146
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 10 files, +783/-12 lines (`develop...HEAD`, cumulative); this round's own delta since round 1's `REQUEST_CHANGES` HEAD (`5dd4dca1`): 4 files, +228/-2 lines (`bin/check-codex-agents.sh`, `tests/codex-agents/run.sh`, `.shell-team/provenance/T-1146.md`, `.shell-team/todo.md`)

#### Blockers

- none

#### Major

- none

#### Minor / nits

- No new code finding. Round-1 Minor 1 (a legacy FIFO/socket/directory occupant's hint falsely implied `(removes this file by name)`) is **resolved**, not merely fixed-adjacent: cells 2 and 4 of the round-3 four-way branch both drop the removal claim for any occupant that is neither `-f` nor `-L`, independently reproduced live (see below). Round-1 Minors 2 (`--root /` normalization) and 3 (unescaped interpolation) are untouched by this round's diff — carried forward unchanged, see `#### Fast-follow disposition`.
- `.shell-team/reviews/T-1146.md` (this record itself, not the code under review) — round 1's own "## Per-pass provenance" block used `pass-role`/`briefing-fidelity` values outside `bin/check-review-input.sh`'s closed vocabulary (`generation`/`confirmation`; `carried`/`not-carried`/`not-applicable`) and carried no `raw-capture` field at all. Confirmed live: `bash bin/check-review-input.sh --record <round-1-only blob at 5f95ca97> --task T-1146` → `field-missing`, and `bin/close-out.sh` wires this same checker into its own merge-gate check — left unfixed, this record would have refused close-out at merge time with no review round left to catch it. Compared this record's shape against every recent sibling record (`T-1104`, `T-1120`, `T-1130`, `T-1135`, `T-1140`–`T-1145`), all of which conform, confirming this was an isolated defect in round 1's own record rather than a broader convention mismatch. Fixed in place this round (see the corrected `## Per-pass provenance` blocks above and the correction note at the head of round 1's own block) — no verdict, finding or judgment changed, only field grammar; independently re-verified `bash bin/check-review-input.sh --record .shell-team/reviews/T-1146.md --task T-1146` → exit 0 after the fix.

#### Adversarial findings

Not run this round as a Codex pass: the primary pass reported no Blocker/Major, so the opt-in adversarial trigger (role file step 5, "sole trigger") did not fire. In its place, the same-class re-verification the round's own scope calls for was performed by hand, going beyond the engineer's and QA's own fixtures: live-reproduced all four cells of the `(code-reviewer requested) × (occupant is -f or -L)` matrix directly against `bin/check-codex-agents.sh`/`bin/gen-codex-agents.sh` at HEAD, plus two occupant shapes neither the engineer's five fixtures nor QA round 3's own two extra probes used — a **FIFO** under cell 4 (excluded × non-removable) and a **symlink-to-directory** under cell 3 (excluded × removable). Both matched the frozen Goal exactly: the FIFO's hint under cell 4 claimed neither `(removes this file by name)` nor `add code-reviewer to --roles and re-run` as a working remedy, and widening `--roles` to include `code-reviewer` and regenerating left the FIFO in place with zero removal-notice bytes; the symlink-to-directory's hint under cell 3 offered `add code-reviewer to --roles and re-run`, and executing that remedy removed only the link (`not a symlink` afterward) while the link's target directory survived untouched. No counterexample found across six live probes (the four fixture-pinned cells plus these two additional shapes).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Minor 1 — FIFO/socket/directory occupant's hint implies a removal that will not happen | Minor (round 1 primary + adversarial) | resolved, not carried forward as a fast-follow | the round-2/round-3 fix's cells 2 and 4 both state the occupant-type reason and offer only "remove it by hand" for any non-`-f`/non-`-L` occupant, closing the exact defect shape this Minor named; independently reproduced live with a FIFO occupant (see Adversarial findings) rather than accepted on the hand-off's word |

#### Recommendation

- APPROVE → set tasks/todo.md to READY_FOR_MERGE

#### Fast-follow disposition

- `bin/check-codex-agents.sh:90` — `ROOT="${ROOT%/}"` followed by `[ -n "$ROOT" ] || ROOT="."` means an exact `--root /` is normalized to `.` in the checker; unverified whether `bin/gen-codex-agents.sh`'s own root handling matches. Pre-existing, unaffected by this round's diff. Intent: file-an-issue (carried forward unchanged from round 1).
- `bin/check-codex-agents.sh` legacy-hint block — `$GENERATOR`/`$ROOT`/`$OUT_DIR`/`$ROLES` are interpolated into the printed remedy inside double quotes without shell-escaping. Pre-existing, unaffected by this round's diff. Intent: won't-fix (carried forward unchanged from round 1 — the frozen spec's own Input space names this exact class as an explicit out-of-scope synthetic extreme, and the Non-goals' threat model states the operator and working directory are trusted).

## Verification performed this round

Independently re-run against this working tree before judging the findings above (not trusted from the hand-off/QA summaries blind):

- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1146-codex-agents-migration-followups.md 2>&1 | tail -3` → `check-acs: 14 passed, 0 failed, 0 skipped, 0 unrecognized`.
- `bash tests/codex-agents/run.sh` → exit 0, all assertions pass, including all 8 `T-1146`-labelled fixtures (`grep -c '^PASS: T-1146'` → `8`).
- `shellcheck bin/check-codex-agents.sh tests/codex-agents/run.sh` → exit 0 both (the two files this round's own diff touches).
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1146.md` → `conformant: .shell-team/provenance/T-1146.md (11 decision entries, 0 sentinel)`.
- `bash bin/check-intent.sh .shell-team/specs/T-1146-codex-agents-migration-followups.md .shell-team/todo.md` → `aligned: T-1146 v1 (b8088cf1a267b3b90cabf5f38ad548bf060e32d4)` — intent block untouched since round 1, no re-freeze of any class occurred, matching this round's brief.
- `git diff 766d6afb...HEAD --name-only` → exactly `.shell-team/provenance/T-1146.md`, `.shell-team/todo.md`, `bin/check-codex-agents.sh`, `tests/codex-agents/run.sh` — matches the engineer's and QA's own round-3 diff-scope claim; nothing outside this set moved this round.
- Read `bin/gen-codex-agents.sh`'s removal gate (`[ "$legacy_role_requested" -eq 1 ] && { [ -f "$LEGACY_CODEX_REVIEWER_TOML" ] || [ -L "$LEGACY_CODEX_REVIEWER_TOML" ]; }`) against `bin/check-codex-agents.sh`'s round-3 four-way branch (`[ "$code_reviewer_requested" -eq 1 ] && { [ -f "$f" ] || [ -L "$f" ]; }` / `elif [ "$code_reviewer_requested" -eq 1 ]` / `elif [ -f "$f" ] || [ -L "$f" ]` / `else`) directly, line by line: the two gate components (`code-reviewer` requested; occupant `-f` or `-L`) are computed identically in both scripts, and the four branches are mutually exclusive and jointly exhaustive over the resulting 2×2 by construction — no fifth, unreachable case exists.
- Live class-closure re-derivation, own scratch out-dirs (not the QA's, not the engineer's — independently seeded): cell 1 (requested × removable, dangling symlink) — claims `(removes this file by name)`; verified separately with a **symlink-to-directory** occupant instead of QA round 3's own dangling-symlink probe — same claim, and executing the remedy removed only the link, leaving the target directory (`/tmp/…/tgtdir`) present and untouched (`test -d` after removal → true). Cell 2 (requested × non-removable) — verified with a **FIFO** occupant (round 3 QA used the same shape): claims neither forbidden phrase, offers "remove it by hand" only. Cell 3 (excluded × removable) — verified with the same symlink-to-directory occupant: offers `add code-reviewer to --roles and re-run, or remove it by hand`; executing that remedy (`--roles 'pm-spec engineer code-reviewer'`) removed the symlink, emitted the removal notice once, and left the target directory in place. Cell 4 (excluded × non-removable) — verified with a **FIFO** occupant (QA round 2/3 both used a directory; this pass used the differently-shaped FIFO): claims neither `(removes this file by name)` nor `add code-reviewer to --roles and re-run`; widening `--roles` to include `code-reviewer` and regenerating exited 0, emitted zero removal-notice bytes, and left the FIFO in place (`test -p` after → true).
- Read the full round-2 (`git diff 5dd4dca1...766d6afb`) and round-3 (`git diff 766d6afb...HEAD`) diffs directly, and the provenance entries recording each round's own design rationale, confirming the round-2 three-way branch's defect (the excluded-branch offered a removal-claim-adjacent remedy without re-checking occupant type) and the round-3 restructuring into a true four-way 2×2, each branch's message content, against the class QA round 2 named.
- Confirmed no file outside the round-3 allow-list moved, and that `bin/gen-codex-agents.sh`, `bin/check-binding.sh`, `tests/check-binding/run.sh`, `docs/adopting.md` and `docs/adopting.ja.md` are untouched since round 1 (`git diff 5dd4dca1...HEAD -- bin/gen-codex-agents.sh bin/check-binding.sh tests/check-binding/run.sh docs/adopting.md docs/adopting.ja.md` → empty).
- Environmentally-unverified: GitHub issues #546/#545's actual canonical text — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation), unchanged from round 1.
- `bash bin/check-review-input.sh --record .shell-team/reviews/T-1146.md --task T-1146` → exit 0 (found this record's own round-1 field-grammar defect first via a non-zero exit against the pre-correction blob, fixed it in place, then re-ran to confirm — see the Minor/nits note above).

## Per-pass provenance

- pass-role (primary-r2): generation
- executor-invocation (primary-r2): `codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <RAW_OUT> > <RAW_JSONL> 2>&1`
- briefing-fidelity (primary-r2): not-applicable — the `review` subcommand takes no free-form prompt argument; re-run against the current HEAD (post round-3 fix) with no deviation from the role file's primary-pass skeleton.
- raw-capture (primary-r2): T-1146-codex-primary-r2
- self-detected-host (primary-r2): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

No adversarial pass this round, and deliberately no field-line naming an "adversarial" pass id here (round 1 already owns that pass id; reusing it across rounds would collide under `check-review-input.sh`'s whole-record field count): the primary pass surfaced no Blocker/Major, so the role file's step-5 "sole trigger" did not fire. The equivalent same-class scrutiny was performed by hand instead (see `## Verification performed this round` and `#### Adversarial findings` above).

Raw captures: `T-1146-codex-primary-r2.{txt,jsonl}` in this directory. No adversarial capture this round.
