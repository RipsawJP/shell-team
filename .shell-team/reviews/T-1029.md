# T-1029 — QA verification record

## QA verdict: PASS

**Scope**: verify the engineer's hand-off (commits `ddb8ad3`/`482a85c`/`4b1b1b1`,
plus the orchestrator's interventions-sentinel commit `33c102d`) against the
frozen spec (`.shell-team/specs/T-1029-claim-fidelity-qa-step.md`, intent-hash
v1 `7a38559cf7eebb6c88273eae9f193834ee7c4fa5`), branch
`feature/133-claim-fidelity-qa-step` (base `2570d68`),
`READY_FOR_QA` -> `READY_FOR_REVIEW`. Every check below was re-run live against
the real checkout; none of the engineer's reported numbers or the hand-off's
mutation outcomes were taken on trust.

**Lens**: this task's deliverable is prose-only — the F1/F2/F3 insertions are
frozen text in `agents/pm-spec.md` and `agents/qa-verifier.md`, no `bin/`
script, checker, parser or state tracker is written or extended (confirmed by
AC8, byte-identical). Per QA's own applicability paragraph, the **Adversarial
fixture synthesis checklist is structurally inapplicable** here — there is no
executable artifact and no documented runnable procedure of the kind that
checklist targets. My lens is empirical/execution-based: I re-ran every
`check:` line live, independently re-derived several of them on scratch
copies/worktrees, and — because this task edits my own agent definition
(`agents/qa-verifier.md`) — I exercised the new F3 item exactly as written
against this spec's own `## Summarized sources` section, which is its first
live input.

### Mandatory conformance gates

- `bash bin/check-acs.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md`
  -> **10 passed, 0 failed, 1 skipped (AC11, runtime by design)** — exact match
  to the required 10/0/1 shape.
- `bash bin/check-intent.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md .shell-team/todo.md`
  -> `aligned: T-1029 v1 (7a38559c...) matches ...`
- `bash bin/check-intent.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/todo.md`
  -> `aligned: T-1027 v1 (828a7194...) matches ...` — stacked neighbour's ledger undisturbed.
- `bash bin/check-intent.sh .shell-team/specs/T-1028-class-m-refreeze.md .shell-team/todo.md`
  -> `aligned: T-1028 v2 (465f91ff...) matches ...` — second stacked neighbour's ledger undisturbed.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1029.md` -> `conformant: ... (3 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1029 -- .shell-team/interventions/T-1029.md`
  -> `conformant: ... (0 entries, 1 sentinel)` — judged structurally only, per this
  gate's own remit (the "no interventions occurred" sentinel's semantic
  correctness is the orchestrator's call, not mine).

### 1. `check-prompt-sync.sh` and scratch-tree regeneration

- `bash bin/check-prompt-sync.sh` -> `all registered prompt blocks in sync`.
- Independently re-ran the registry's own scratch-tree regeneration procedure
  (the exact CI dogfood step, re-derived by hand rather than trusted): copied
  the 8 registry-named files (`agents/{engineer,pm-spec,qa-verifier,tech-lead}.md`
  + their 4 `templates/prompt-blocks/playbook-*.md` counterparts) into a fresh
  scratch tree, ran `bin/gen-playbook-blocks.sh --root <scratch> --lessons
  .shell-team/lessons.md`, and `cmp`'d all 8 — **all 8 byte-identical**. The
  two non-fatal block-size stderr warnings (`playbook-engineer.md`,
  `playbook-pm-spec.md`, both pre-existing per the Assumptions note) appeared
  and were correctly non-fatal.
- Re-derived AC5's marker-region byte-identity independently of the checker:
  `agents/pm-spec.md`'s `language`/`playbook-pm-spec` and
  `agents/qa-verifier.md`'s `language`/`careful-execution`/`playbook-qa-verifier`
  regions all `cmp`-identical to the same regions at `2570d68`.
- Confirmed the `contain`-mode strings are alive: every non-empty line of
  `flag-enum.md`/`board-line-format.md` present verbatim in `agents/pm-spec.md`,
  and every non-empty line of `flag-enum.md`/`verdict-labels.md`/
  `operating-paths-core.md` present verbatim in `agents/qa-verifier.md` (both
  via AC6's own re-derivation, re-run rather than trusted).

### 2. Self-application of the new mechanism — the load-bearing item

Executed F3 exactly as written against this spec's own `## Summarized sources`
section (the spec is simultaneously Input-space class 3 and 4, and its own
first live input):

- **`.shell-team/lessons.md` — E1 entry** (`## 2026-08-04 — A summary of
  another document preserves the source's own distinctions, checked against
  the source and not the summary`, lines 901–909): opened in full and matched
  clause by clause against the spec's claim.
  - Heading string — **exact byte match**.
  - "splits its mechanism by role in its `How to apply` field" — **confirmed**:
    line 909 explicitly assigns "pm-spec names the source document..." vs "QA
    treats a summary it has not compared against the source as unverified,"
    a genuine per-role split.
  - "pm-spec names the source and the distinction at the point the summary is
    written" / "writes an acceptance criterion requiring the verifying role to
    open that source" — **confirmed**, near-verbatim against line 909's first
    clause (the spec's restatement drops the trailing "rather than re-derive
    the claim from the spec's wording," but that nuance is not lost — it
    reappears as F3's own prohibition clause, so nothing is flattened).
  - "QA treats a summary it has not compared against the source as unverified"
    — **exact match** to line 909's second clause.
  - "`Applies-to` is exactly `pm-spec, qa-verifier`" — **exact byte match** to
    line 903; engineer correctly excluded.
  - "`Rule` field states the discipline but assigns neither step" — **defensible
    but the loosest of the six sub-claims**: line 907's Rule sentence does say
    "the verifying role confirms that by opening the source document and
    matching it clause by clause," which already gestures at the QA-side
    action, even though it assigns no timing/authoring obligation to pm-spec
    and never states QA's "treats a summary it has not compared... as
    unverified" epistemic stance. Not a flattening of a count or a
    requiredness contract (the kind of defect this whole task exists to
    close) — it is a difference in how much of the mechanism sits in the
    general `Rule` sentence versus the specific `How to apply` sentence, and
    D3/D4's own reliance on the *per-role split* rather than the Rule
    sentence alone is what actually matters and is accurately claimed.
  - **Verdict: PASS**, one loose-but-non-flattening clause noted above.
- **GitHub issue #133** — relayed to pm-spec, not opened by pm-spec (no
  network access in that role, disclosed correctly in Assumptions). QA has
  network access to `api.github.com` in this session (an allow-listed host);
  `gh` itself failed on a sandbox TLS-trust error exactly as the engineer's
  provenance record describes, so I used the same `curl` path the engineer
  used, independently:
  `curl -s -H "Accept: application/vnd.github+json" https://api.github.com/repos/RipsawJP/shell-team/issues/133`
  — fetched and read the issue's own bytes directly (not the engineer's
  transcription). Its `## Proposal` states exactly two items: "(1) The spec
  template gains a slot that names the source documents being summarized, so
  the requirement is visible at freeze time" and "(2) `qa-verifier` gains an
  instruction: when that slot is populated, open each named source and verify
  the frozen summary preserves the source's own distinctions — not against
  the spec's paraphrase, but against the source bytes" — **both match the
  spec's two-part characterization clause by clause**, and no checker is
  requested anywhere in the issue body (consistent with D5's instruction-only
  scope). **Verdict: PASS, measured** — not `environmentally-unverified` for
  this role, since the path was actually reachable this session (same
  disposition the engineer's provenance decision 2 reached, independently
  reproduced rather than trusted).

### 3. Empirical spot checks (F1/F2/F3 placement)

- `grep -n '```markdown\|^## Problem\|^## Summarized sources\|^## Goal' agents/pm-spec.md`
  confirms F1's heading (line 31) sits inside the **first**
  ` ```markdown ` fence (line 21), after `## Problem` (28), before `## Goal` (34).
- `git diff 2570d68 HEAD -- agents/pm-spec.md` shows F2 as a pure insertion,
  the new last bullet of `## Spec completion self-check`, immediately before
  `## Frozen intent freeze`.
- `git diff 2570d68 HEAD -- agents/qa-verifier.md` shows F3 as a pure
  insertion, the physical line immediately after the interventions bullet and
  immediately before the "Never destroy the working tree (T-073)" bullet —
  the mandatory-item cluster stays contiguous.
- No machine-written region changed (AC5/AC6 both PASS, re-derived above
  independently of the checkers).

### 4. AC11 audit for substance

- **(a) Directory/glob-scanning CI steps, re-run rather than reasoned about**:
  `bash bin/check-retro.sh .shell-team/retros/*.md` -> exit 0;
  `bash bin/check-interventions.sh <file>` looped over all 27
  `.shell-team/interventions/*.md` files -> all conformant (was 26 before this
  task's own sentinel landed — consistent with the hand-off);
  `bash bin/retro-inputs.sh --base HEAD` -> exit 0, now reports 33 provenance
  files and 27 intervention records (picking up this task's new files);
  `bash bin/check-pii-shapes.sh --base origin/develop` -> clean;
  `bash bin/check-commit-identity.sh --base origin/develop` -> clean;
  `bash bin/check-playbook.sh <resolved lessons path>` -> all entries valid;
  the four `team-paths.sh --get {base,todo,specs,lessons}` dogfood assertions
  all resolve as shipped. Named-file steps (shellcheck's fixed list, the
  `tests/*/run.sh` fixture suites) are correctly inapplicable per AC8's
  measured byte-identity of `bin/`, `templates/`, `tests/`, `skills/`, `docs/`,
  `.github/` against the base ref — I did not re-run all ~50 named-file steps
  myself (a prose-only, `bin/`-untouched diff gives no mechanism by which they
  could newly fail), but the directory/glob-scanning ones were run directly,
  matching the split-disclosure discipline the task requires.
- **(b) Mutation-3 asymmetry (provenance decision 1), independently
  re-verified on a disposable `git worktree` (never the real working tree)**:
  reproduced both readings of "delete the phrase `not subject to the
  out-of-input-space exemption` from F3 in both the spec and the agent file":
  - **Symmetric** (identical deletion in both files) -> `AC3: PASS`,
    `AC4: FAIL` — confirms AC3's byte-equality lock is blind to an identical
    edit applied to both sides.
  - **Asymmetric** (spec's F3 only) -> `AC3: FAIL`, `AC4: FAIL` — matches
    AC11(b)'s stated expectation exactly.
  Both outcomes reproduce the engineer's provenance decision 1 findings
  byte-for-byte; the worktree was removed afterward
  (`git worktree remove --force`), and `git status --short` on the real
  checkout shows no residue.
- **(c)/(d)**: covered by §2 above (my own independent read, not a re-read of
  the engineer's hand-off prose).

### Edge cases tried

- **Legal-input collision fixture**: the spec deliberately carries a second,
  literal `## Summarized sources` heading inside F1's frozen block (as
  markdown template text, not a section). AC10's own extraction logic stops
  at the first section boundary specifically to avoid reading F1/F2's text as
  the declaration — verified this holds (AC10 PASS) and did not trip on the
  collision.
- **Mutation adversariality**: beyond the assigned mutation-3 re-derivation, I
  confirmed the checker's AC3/AC4 split (equality vs containment) actually
  behaves asymmetrically rather than assuming it from the spec's prose.
- Both edge cases are grounded in the spec's own declared Input space (self-
  hosting collision explicitly called out in Assumptions; the AC3/AC4
  asymmetry is explicitly named in AC11(b)) — no out-of-input-space finding to
  disclose.

### Risk notes for reviewer

- The one loose (not wrong) claim in the spec's `## Summarized sources`
  section — "the `Rule` field... assigns neither step" — is defensible on a
  fair reading (Rule states the general discipline; the specific per-role
  timing/authoring split lives only in `How to apply`), but a stricter reader
  could argue Rule already gestures at the QA-side action. Flagging for the
  cross-provider round's judgment on the wording itself, per this task's own
  classification note (frozen wording is the one place to review at the
  higher standard).
- AC5/AC7/AC8 are explicitly merge-point-scoped to `2570d68` and will go stale
  the moment another PR lands ahead of this one in the sprint-train batch
  merge — expected and already called out in the spec; noting it here so it
  is not mistaken for a live defect if re-run later.
- I did not re-run all ~50 named-file CI steps individually (see §4(a)); this
  is deliberate given the byte-identity ground, not an omission.

---

# T-1029 — Codex round 1 (cross-provider review)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1029
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 7 files changed, +491/-0 lines (`agents/pm-spec.md`, `agents/qa-verifier.md`, `.shell-team/specs/T-1029-claim-fidelity-qa-step.md`, `.shell-team/todo.md`, `.shell-team/provenance/T-1029.md`, `.shell-team/interventions/T-1029.md`, `.shell-team/reviews/T-1029.md`), diff range `2570d68..HEAD`.

**Method**: one foreground `codex exec review --base 2570d68 --json` primary pass over the real working tree (`.shell-team/reviews/T-1029-codex-primary.{jsonl,txt}`), followed by a devil's-advocate adversarial pass targeting F1/F2/F3 specifically, triggered because the primary pass returned a P1/P2 pair (`.shell-team/reviews/T-1029-codex-adversarial.{jsonl,txt}`). I independently re-opened `.shell-team/lessons.md`'s E1 entry (lines 901–909), fetched GitHub issue #133 directly with `curl` (not relayed), diffed `agents/pm-spec.md` / `agents/qa-verifier.md` against `2570d68`, and confirmed `agents/codex-reviewer.md` / `agents/engineer.md` / `agents/tech-lead.md` / `.shell-team/lessons.md` are byte-identical to base (empty `git diff`), before synthesizing severities below — several of Codex's raw findings are downgraded from its own label, with reasons in the ledger.

#### Blockers
- None.

#### Major
- **F3's verification scope binds only to the `## Summarized sources` declaration line, not to wherever the source's facts are actually restated in the frozen Goal/Acceptance-criteria prose** (`agents/qa-verifier.md:65`). F3 reads: "confirm the spec's summary preserves the distinctions the source itself draws, matching it clause by clause against the source's own bytes," and its prescribed verdict shape ties the per-source line to "the distinction the spec claimed to carry over" — language that tracks the one-line entry in `## Summarized sources` (F1's `<path-or-locator> — <the distinction being carried over>` format), not the Goal/AC text that is where T-1023 and T-1026's actual flattening lived (a frozen AC line, not a declaration line — neither incident's spec even had a `## Summarized sources` section). A QA agent can open the source, confirm the declared one-liner is accurate, emit a PASS, and never notice that a *different* sentence inside the frozen Goal or an Acceptance Criterion restates the same source fact with the distinction dropped. Both my primary and adversarial Codex passes converged on this independently (primary `[P1]`, adversarial `major`), and my own reading of F3's text agrees a literal-minded QA reading is genuinely available. **Suggested fix** (for a re-freeze proposal, not a rework item — see Recommendation): require QA to locate every place in the frozen Goal/Non-goals/Acceptance-criteria text that restates a fact from a named source, and compare *those* clauses — not only the declaration line — against the source bytes.

#### Minor / nits
- **F3's prescribed `READY_FOR_ENG` routing does not itself carve out the case where the flattened distinction lives inside frozen (intent-block) prose**, which the engineer role cannot edit (`agents/engineer.md:51`'s existing Japanese rule: intent-block content is pm-spec's property, and a rework touching it goes through pm-spec + human re-freeze, never a direct engineer edit; line 22 similarly directs "if any criterion is unclear, stop and route back to pm-spec"). Both Codex passes flagged this as `major`/`[P2]`; I downgrade it because this generic escalation path already exists and already covers every other mandatory QA item in this same file (intent/provenance/interventions), none of which special-case this either — F3 is consistent with existing repo style rather than introducing a new gap.
- **The `not applicable`/missing-section fallback gives a careless *future* pm-spec no QA-side catch if it skips the section entirely post-T-1029** (no version/adoption marker distinguishes a grandfathered pre-T-1029 spec from a new one that simply forgot). Real, but this is the identical accepted tradeoff already shipped for the `## Input space` precedent (same "consuming gate falls back to prior behavior" convention, same absent adoption marker) — not a new or worse gap this task introduces.
- **The `## Summarized sources` declaration is deliberately mutable outside the intent hash (D1), so a source can be removed or downgraded to the sentinel mid-rework while the frozen Goal/AC's claim derived from it stays hashed and unchanged**, silently disarming F3 for that source. `out-of-input-space`: this is exactly the limitation the spec itself discloses twice — Non-goals ("Detecting a false declaration... omits one it did") and Input space's Out-of-scope bullet ("A spec that lies in its slot — one naming a source it did not summarize, or omitting one it did... detectable only by a reading gate").
- **`environmentally-unverified` never forces a FAIL even when the unreachable source backs a frozen normative claim.** Deliberate and explicitly discussed (D3(iv), AC11(d)); reuses the existing `environmentally-unverified` convention (Adversarial fixture synthesis checklist / codex-reviewer's issue-canon rule) "for the identical reason" — not an oversight.
- **A false or negligently incomplete `## Summarized sources` declaration remains an undetected bypass.** `out-of-input-space`: explicitly disclosed twice, Non-goals and Input space Out-of-scope (same lines cited above).
- **The spec's own claim "the `Rule` field... assigns neither step" (`## Summarized sources`, second bullet) is loose.** E1's Rule (`.shell-team/lessons.md:907`) does state one step explicitly ("the verifying role confirms that by opening the source document and matching it clause by clause") — it assigns the QA-side action, just not pm-spec's naming-at-write-time step nor QA's "treats an unverified summary as unverified" epistemic framing. I agree with QA's own risk note: defensible on a fair reading, not a flattening of a count or requiredness contract (the class of defect this task exists to close), so Minor rather than a blocking finding.

#### Adversarial findings
- The devil's-advocate pass, run against `agents/pm-spec.md`/`agents/qa-verifier.md` plus the T-1029 spec, independently reproduced the primary pass's scope concern (F3 binds to the declaration, not the frozen prose) and surfaced three further findings not raised by the primary pass: the `READY_FOR_ENG` routing question, the missing-section adoption-boundary gap, and the mutable-declaration/frozen-hash asymmetry — all captured above with their final (post-synthesis) severities. Full raw output: `.shell-team/reviews/T-1029-codex-adversarial.{jsonl,txt}`.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | F3 verification scope (declaration-only vs. frozen Goal/AC prose) | primary: blocker (labeled `[P1]`) · adversarial: major | major | Genuine, independently-confirmed gap; downgraded from blocker because the mechanism still raises the floor (forces an explicit declaration) and a diligent QA — as this very cycle's own QA round demonstrated — can and did read the check broadly. Not a complete failure of the stated goal. |
| 2 | `READY_FOR_ENG` routing for a frozen-prose defect | primary: major (labeled `[P2]`) · adversarial: major | minor | `agents/engineer.md:22,51` already provide a generic route-back-to-pm-spec escalation for exactly this class, applying uniformly to every other mandatory QA item in the same file; not a new gap T-1029 introduces. |
| 3 | Missing-section fallback has no adoption-boundary marker | adversarial: major | minor | Identical accepted tradeoff already shipped for the `## Input space` precedent (same fallback convention, same absent marker) — parity, not regression. |
| 4 | Mutable declaration can disarm F3 for a removed/downgraded source | adversarial: major | minor (`out-of-input-space`) | Grounds on Input space's Out-of-scope bullet "A spec that lies in its slot ... or omitting one it did ... detectable only by a reading gate" — explicitly, doubly disclosed as an accepted limit (also in Non-goals). |
| 5 | `environmentally-unverified` never forces a FAIL | adversarial: major | minor | Deliberate, explicitly discussed (D3(iv), AC11(d)), reuses an existing repo-wide convention for the identical stated reason — not an oversight. |
| 6 | False/incomplete declaration is an undetected bypass | adversarial: major | minor (`out-of-input-space`) | Same Input-space Out-of-scope bullet as row 4; explicitly disclosed twice in the spec (Non-goals + Input space). |
| 7 | Spec's own claim "`Rule` field... assigns neither step" | QA's own risk note (not a Codex finding) | minor | Independently re-read E1 line 907 myself: loose but defensible, matches QA's disposition; not a flattening of a count/requiredness contract. |

#### Recommendation
- REQUEST_CHANGES → set back to `READY_FOR_ENG` with the linked Major finding (row 1). Given the finding is a scope/precision gap in F3's own frozen wording rather than an implementation defect, the actual correction is a **class-B re-freeze proposal routed through pm-spec** (per this task's own R1 pre-commitment: "wording disagreements route to pm-spec as re-freeze proposals"), not an engineer-side code change — there is no implementation to rework here, only frozen prose. This is round 1; R2's two-consecutive-rounds threshold for a split proposal is not yet met.

#### Fast-follow disposition
- Row 2 (`READY_FOR_ENG` routing clarity) — file-an-issue: a low-priority prose clarification (spell out the pm-spec-reroute path explicitly in F3 or in the shared mandatory-item preamble) worth doing eventually, but not urgent since `agents/engineer.md`'s existing generic escalation already prevents an actual stuck state.
- Row 3 (missing-section adoption boundary) — won't-fix: identical accepted design tradeoff as the shipped `## Input space` precedent; fixing it here alone would create an inconsistency with established precedent, and any fix should be considered fleet-wide (covering both sections) if ever revisited, not scoped to this task.
- Row 4 (mutable declaration / frozen-hash asymmetry) — won't-fix: explicitly, deliberately disclosed as an honest limit of the mechanism in both Non-goals and Input space (Out-of-scope); reopening it means reopening D1/D5's settled decisions.
- Row 5 (`environmentally-unverified` non-blocking) — won't-fix: reuses an established repo-wide convention for the identical stated reason; overriding it here would create asymmetric behavior against that token's existing semantics elsewhere.
- Row 6 (false/incomplete declaration bypass) — won't-fix: same grounds as row 4, explicitly disclosed twice in the frozen spec.
- Row 7 (loose `Rule`-field claim) — won't-fix: defensible on a fair reading per my own independent check of the source bytes; not a flattening, and QA already surfaced it for this round's judgment rather than leaving it silent.


---

# T-1029 — QA verification record, round 2

## QA verdict: PASS

**Scope**: re-verify the rework round 1 hand-off (commits `2d8eacb` v1→v2
re-freeze, `5dbecc4` re-splice, `19f95c3` board hand-off) against the
re-frozen spec (intent-hash **v2** `aa47eaa0525af8a73cf89b9b3655b310e62bdadb`),
branch `feature/133-claim-fidelity-qa-step` (base `2570d68`, HEAD `19f95c3`),
in response to Codex round 1's one Major (F3's verification scope bound only
to the `## Summarized sources` declaration line, not to wherever the source's
facts are actually restated in the frozen Goal/Acceptance-criteria prose).
`READY_FOR_QA` -> `READY_FOR_REVIEW`. Every check below was re-run live
against the real checkout; the working tree was left clean throughout except
for this record and the board hand-off (`git status --short` confirmed
empty before and after every mutation experiment, all of which ran on
disposable scratch copies under `$TMPDIR`, never in the checkout).

**Lens**: unchanged from round 1 — prose-only deliverable, no `bin/` script,
checker, parser or state tracker written or extended (re-confirmed live by
AC8, byte-identical). The Adversarial fixture synthesis checklist stays
**structurally inapplicable**. My round-2 lens is the same execution-based
re-derivation, focused specifically on empirically closing the one Major:
does the widened F3 actually stop the narrow declaration-line-only reading,
and does the mechanism it demands actually run once, over both restatement
sites the frozen region and the declaration line carry?

### 1. Full re-run

- `bash bin/check-acs.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md`
  -> **10 passed, 0 failed, 1 skipped (AC11, runtime by design)**.
- `bash bin/check-intent.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md .shell-team/todo.md`
  -> `aligned: T-1029 v2 (aa47eaa0525af8a73cf89b9b3655b310e62bdadb) matches ...`
- `bash bin/check-intent.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/todo.md`
  -> `aligned: T-1027 v1 (828a719487a686c824715363126cbaa4a91662e0) matches ...`
- `bash bin/check-intent.sh .shell-team/specs/T-1028-class-m-refreeze.md .shell-team/todo.md`
  -> `aligned: T-1028 v2 (465f91ff975fdbb4fd40cd6dde72487e522cf2c9) matches ...`
- `bash bin/check-provenance.sh .shell-team/provenance/T-1029.md` -> `conformant: ... (3 decision entries, 0 sentinel)`
  (unchanged from round 1 — no new decision was needed for a mechanical re-splice).
- `bash bin/check-interventions.sh --task T-1029 -- .shell-team/interventions/T-1029.md`
  -> `conformant: ... (0 entries, 1 sentinel)`.
- `bash bin/check-prompt-sync.sh` -> `all registered prompt blocks in sync`.
- Scratch-tree `bin/gen-playbook-blocks.sh` regeneration re-run by hand: all 8
  registry-named files (`agents/{engineer,pm-spec,qa-verifier,tech-lead}.md`
  + their 4 `templates/prompt-blocks/playbook-*.md` counterparts) `cmp`-identical
  after regeneration — **byte-identical**, re-derived independently of AC6.
- Board hygiene: `bash bin/check-handoff.sh .shell-team/todo.md` (exit 0),
  `bash bin/check-board-headings.sh .shell-team/todo.md --base 2570d68` (exit 0),
  `bash bin/check-pii-shapes.sh --base 2570d68` (clean); `git diff --numstat
  2570d68 -- .shell-team/todo.md` -> `26 0` after appending this round's own
  board bullet (zero deletions, confirmed twice: once before my board edit,
  once after).

### 2. Per-finding closure, empirically

Extracted v2's F3 and F2 out of the spec's own `<!-- frozen-begin: X --> …
<!-- frozen-end: X -->` fences and `cmp`'d each against its corresponding live
line in `agents/qa-verifier.md` / `agents/pm-spec.md`, independent of AC2/AC3's
own machine check (a second, hand-run derivation rather than trusting the
checker's report):

- **F3** — `sed`-extracted line from the spec vs. the matched line in
  `agents/qa-verifier.md` -> `cmp` **identical** (0 diff).
- **F2** — same procedure against `agents/pm-spec.md` -> `cmp` **identical**
  (0 diff).

Confirmed the v1 declaration-line-only reading is **no longer available**:
the live F3 text explicitly enumerates the site classes — "the Goal sentence,
a Non-goal, an Acceptance Criterion's prose, an Input-space clause, a
settled-decision paragraph inside the marker pair, and the declaration line
itself" — and states the declaration "is one such site and never the whole
set," plus the explicit clause "Confirming the declaration line is accurate
while a criterion two sections away drops the same distinction is the exact
miss this item exists to close." This is a different sentence shape than the
v1 text Codex quoted ("confirm the spec's summary preserves the distinctions
... matching it clause by clause against the source's own bytes," with the
verdict tied to "the distinction the spec claimed to carry over" — language
that tracked the declaration's own one-line format and named no other site).
The site-enumeration clause and the "never the whole set" clause are new
strings not present in the v1 wording the round-1 review record quotes, and
AC4 now pins the scope clause as one of 7 fixed strings (up from 6 in v1 —
confirmed by counting the `for s in ...` list in AC4's live `check:` line).

### 3. Self-application re-executed under the v2 scope — the load-bearing item

For each of this spec's two `## Summarized sources` entries, located **every**
restatement site inside the frozen region (not only the declaration line) and
matched each against the source's own bytes.

**Source 1 — `.shell-team/lessons.md`, the E1 entry**
(`## 2026-08-04 — A summary of another document preserves the source's own
distinctions, checked against the source and not the summary`, lines 901–909)

- Sites checked: the declaration line (## Summarized sources, bullet 1); D3(ii)'s
  reliance on "that per-role split, not on the `Rule` sentence alone"; D4's
  explicit restatement `E1's Applies-to is pm-spec, qa-verifier`; AC10's
  presence/anchor check (`- **Applies-to**: pm-spec, qa-verifier` verified
  present verbatim in the source, plus the `Rule` bullet's presence); AC11(c)'s
  meta-restatement (per-role split in `How to apply`, what `Rule` does/does not
  assign, the `Applies-to` value).
- Distinction claimed: the entry splits its mechanism by role in `How to apply`
  (pm-spec names the source and distinction at write time and writes the
  verifying AC; QA treats an unopened summary as unverified); `Rule` states the
  discipline and names the QA-side action but assigns pm-spec no
  naming-at-write-time step and does not carry QA's "unverified until
  compared" stance; `Applies-to` is exactly `pm-spec, qa-verifier`.
- Result: **PASS** at every site — each restatement matches the source's own
  bytes. One clause ("the `Rule` field... assigns neither step") remains
  defensible-but-loose rather than a flattening, as both this task's own
  round-1 QA record and Codex round 1's row 7 already independently judged
  (Rule's sentence does gesture at the QA-side action, just not the specific
  per-role timing/authoring split) — not re-litigated here since nothing
  about F3's re-freeze changed this specific sub-claim or its wording.

**Source 2 — GitHub issue #133**

- Sites checked: the declaration line (bullet 2); the Goal sentence's two-clause
  restatement (the `agents/pm-spec.md` requirement clause and the
  `agents/qa-verifier.md` matching-item clause); AC10 (naming check only — no
  content claim); AC11(d) (the `environmentally-unverified` expectation).
- Distinction claimed: the issue's proposal has two parts — (1) a spec-template
  slot naming summarized sources, visible at freeze time; (2) a qa-verifier
  instruction to open each named source and verify preserved distinctions
  against the source's bytes, not the paraphrase.
- Result: fetched the issue **live**, independently of the round-1 fetch:
  `curl -s -H "Accept: application/vnd.github+json"
  https://api.github.com/repos/RipsawJP/shell-team/issues/133` -> HTTP 200
  (network reachable this session, same as round 1). Its `## Proposal` section
  states exactly: "1. The spec template gains a slot that names the source
  documents being summarized, so the requirement is visible at freeze time."
  and "2. `qa-verifier` gains an instruction: when that slot is populated,
  open each named source and verify the frozen summary preserves the source's
  own distinctions — not against the spec's paraphrase, but against the
  source bytes." Both match the declaration bullet's two-clause
  characterization and the Goal sentence's two-clause restatement, clause by
  clause. **PASS, measured** — not `environmentally-unverified` for this role.
  The widened "every site in the frozen region" scope (vs. the issue's own,
  narrower two-item proposal) is the spec's **own** design elaboration
  (D3(ii)), never attributed to the issue itself, so there is no flattening
  risk in that gap — the spec does not claim the issue asked for the wider
  scope, and it does not need to.

No flattening found at either source, at any site checked, under the widened
v2 scope — confirming the mechanism the Major demanded actually ran, not just
that its wording now permits it to.

### 4. AC11(b) revert-lock reproduced

On disposable scratch copies under `$TMPDIR` (never the real working tree —
`git status --short` on the real checkout confirmed clean before and after):
copied `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` and
`agents/qa-verifier.md`, then applied the exact narrowing mutation named in
AC11(b) — F3's scope clause re-narrowed from "every site in the frozen region
that restates, quotes or summarizes those facts — [site enumeration] — and
the declaration line itself, which is one such site and never the whole set
(for a spec with no intent-block markers, ...)" back to a bare
"the `## Summarized sources` declaration line" — applied **symmetrically**,
once in each scratch copy (confirmed exactly one substitution per file before
running the checks). Then re-ran AC3's and AC4's own `check:` logic, with the
`SP`/`A` variables pointed at the scratch copies:

- **AC3** (byte-equality + adjacency lock): **rc=0 — stayed GREEN**. The
  mutated spec block and the mutated agent line are still byte-identical to
  each other (an identical edit to both sides preserves the equality this
  criterion checks), so this criterion is blind to the narrowing.
- **AC4** (fixed-string scope-clause lock): **rc=1 — went RED**, specifically
  on the missing fixed string `every site in the frozen region that restates,
  quotes or summarizes those facts`, absent from both the mutated frozen block
  and the mutated agent file after the substitution.

This matches AC11(b)'s stated expectation exactly ("AC4 red on the scope
clause alone, AC3 still green in the symmetric case, which is why AC4 rather
than AC3 is this property's lock") and confirms the revert lock actually
catches the specific regression this whole round-2 exists to guard against —
a future edit silently re-narrowing F3 back to the v1 reading.

### 5. Regression spot-probes

- `bash bin/check-retro.sh .shell-team/retros/*.md` -> exit 0.
- `bash bin/retro-inputs.sh --base HEAD` -> exit 0; now reports **33**
  provenance files and **27** intervention records (picks up this task's
  files, consistent with round 1's numbers before my own board edit added
  nothing further to either directory).
- `bash bin/check-pii-shapes.sh --base origin/develop` -> clean.
- `bash bin/check-commit-identity.sh --base origin/develop` -> clean.
- `bash bin/check-playbook.sh .shell-team/lessons.md` -> all entries valid.
- Swept **all 27** `.shell-team/interventions/*.md` files individually through
  `check-interventions.sh` (not only the required T-1029 one) — all
  conformant.
- **Skipped, and why**: the ~50 named-file CI steps (shellcheck's fixed file
  list, the `tests/*/run.sh` fixture suites, README/board-template dogfoods)
  were **not** re-run individually this round either. Ground: AC8, re-run live
  this round, confirms `bin/`, `templates/`, `tests/`, `skills/`, `docs/`,
  `.github/` and every non-goal agent file are still byte-identical to the
  base ref — a prose-only, `bin/`-untouched diff gives no mechanism by which a
  named-file step could newly fail between round 1 and round 2. This is the
  same disclosed omission as round 1, re-confirmed rather than assumed.
- Did not re-run the other six AC11(b) mutation catalogue entries (word
  change, adjacency move, phrase deletion, blank-line insertion, sixth-bullet
  append, marker-region edit) this round — round 1 already reproduced two of
  them independently (the phrase-deletion asymmetry) and the task's round-2
  scope named specifically the re-narrowing mutation as the one to reproduce.

### Edge cases tried

- **Legal-input collision fixture** (carried over from round 1, re-checked):
  the spec's own second, literal `## Summarized sources` heading inside F1's
  frozen block did not trip AC10's first-section-boundary extraction — still
  correctly stops at the first `## ` boundary. Grounded in the spec's own
  declared Input space (Assumptions calls this out explicitly) — not an
  out-of-input-space finding.
- **Symmetric-vs-asymmetric mutation collision**: confirmed AC3's equality
  lock is blind to a symmetric edit applied identically to both files (§4) —
  this is the mechanism AC11(b) itself names and expects, not a new finding.

### Risk notes for reviewer

- The one loose-but-defensible claim about E1's `Rule` field (identified in
  round 1, echoed in Codex round 1's row 7) is unchanged by this re-freeze and
  was not re-litigated here — it does not concern F3's scope (the Major this
  round closes), and both prior rounds already judged it non-blocking.
- AC5/AC7/AC8/AC9 remain merge-point-scoped to `2570d68` and will go stale the
  moment another PR lands ahead of this one in the sprint-train batch merge —
  expected, called out in the spec, and unchanged from round 1.
- I did not re-run the ~50 named-file CI steps individually this round either
  (see §5) — deliberate, on the AC8 byte-identity ground, not an omission.
- No `out-of-input-space` or `environmentally-unverified` findings to
  disclose this round — both sources named in `## Summarized sources` were
  reachable and matched clause by clause at every restatement site.


---

# T-1029 — Codex round 2 (cross-provider review)

### Codex Review verdict: APPROVE
- Task: T-1029
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: branch `feature/133-claim-fidelity-qa-step`, base `2570d68`, HEAD `7d3bc38` — 7 files changed, +786/-0（round 1 と同じファイル集合。新規ファイル・削除なし）。round 2 の対象コミットは `2d8eacb`（v1→v2 再凍結、spec のみ 2 ファイル差分）、`5dbecc4`（再splice、`agents/pm-spec.md` / `agents/qa-verifier.md` 各 1 行のみ変更）、`19f95c3`（board hand-off）、`7d3bc38`（QA round 2 PASS）。

**Method**: 1 回の foreground `codex exec review --base 2570d68 --json` を実況の作業ツリーに対して実行（`.shell-team/reviews/T-1029-codex-primary.{jsonl,txt}`）。primary パスが 1 件（P2、severity-normalization 表で major 相当）を返したため、その 1 件だけを対象に devil's-advocate の adversarial パスを追加実行（`.shell-team/reviews/T-1029-codex-adversarial.{jsonl,txt}`）。加えて、round 2 のスコープ (a)–(d) を自分自身でも独立に再測定した: `bash bin/check-acs.sh` → 10 passed/0 failed/1 skipped（QA の報告と一致）、`bash bin/check-intent.sh` → `aligned: T-1029 v2 (aa47eaa0…)`、`bash bin/check-prompt-sync.sh` → green、`git diff e7c1736 2d8eacb -- .shell-team/specs/...` で v1→v2 の spec 差分を自分の目で読んだ（re-freeze コミットが spec ファイルだけを触っていることを `git show --stat` で確認）、`git show --stat 5dbecc4/19f95c3/7d3bc38` で各コミットの変更ファイル数・行数を確認、`grep -n` で `agents/qa-verifier.md:65` の生きた F3 テキストが frozen block と byte-for-byte 一致することを目視確認。

## (a) Major の解消確認 — F3 の検証スコープ

Round 1 の Major は「F3 が `## Summarized sources` の宣言行だけに検証を縛っており、フローズン領域内の別の場所（Goal 文や AC の地の文）で同じ事実が flatten されていても declaration 行さえ合っていれば PASS してしまう」というものだった。

v2 の F3 は宣言行だけを見る読み方をもう成立させない。ライブテキストを実際に読んだ結果: 「open it, locate **every site in the frozen region that restates, quotes or summarizes those facts** — the Goal sentence, a Non-goal, an Acceptance Criterion's prose, an Input-space clause, a settled-decision paragraph inside the marker pair, and the declaration line itself, which is one such site and never the whole set」— サイトの列挙が明示され、「宣言行はそのうちの一つであり全体ではない」という文が加わっている。AC4 は scope 節 `every site in the frozen region that restates, quotes or summarizes those facts` を固定文字列として 7 個中の 1 個としてピン留めしており（v1 は 6 個）、`bin/check-acs.sh` を自分で再実行して AC4 PASS を確認した。QA round 2 は AC11(b) の re-narrowing mutation（scope 節を宣言行だけに戻す）をスクラッチコピー上で再現し、AC4 が red（AC3 は byte-equality lock のため symmetric edit には blind で green のまま）になることを実測している — これは v2 が「宣言行だけに戻す」変更を機械的に検知できることの直接証拠であり、私は QA の手順自体は再実行していないが、記載されたコマンド・期待挙動を読み、AC4 の `check:` 行のロジック（`for s in ...` の固定文字列リスト）を自分でも読んで整合していることを確認した。

**結論: Major はクローズ。** どのような読み方をしても、宣言行だけを確認して他のフローズン領域内の書き換え箇所を無視する読解はもはや利用できない。

## (b) v2 の frozen-region delta のスコープ確認

`git diff e7c1736 2d8eacb -- .shell-team/specs/T-1029-claim-fidelity-qa-step.md agents/pm-spec.md agents/qa-verifier.md .shell-team/todo.md` を自分で読んだ。`2d8eacb` がタッチしたファイルは `.shell-team/specs/...` と `.shell-team/todo.md` の 2 つだけ（`git show --stat 2d8eacb` で確認、agent ファイルは 0 差分）。spec 内の変更点は board のratification 行が要約する内容と一致する: Goal 文の scope 節追加、D3 の五→六 properties 化（新設は scope 節）、F2 への一文追加（宣言はフローズン領域への索引であり全体ではない、という symmetry 文）、F3 のスコープ拡張と「どこにあるか言う」escalation 節の追加、AC4 の固定文字列 6→7、AC11(b) の re-narrowing mutation 追加、AC11(c) の「every site this spec restates it」への強化、Input space への reachable class 追加（restatement sites の per-source 数）と Non-goals への out-of-scope 境界追加（「Restatement sites outside the frozen region」）、Body-to-AC 対応表への行追加。それ以外の一文一句の変更は見当たらない — D1・D2・D4・D5 の本文、F1、Non-goals の他の箇条書き、Input-space の他の箇条書きはすべて byte-for-byte 不変。

**結論: v2 の delta は board のratification 行が記述する範囲に閉じている。** スコープの拡大・縮小・無関係な書き換えは見つからなかった。

## (c) 再splice コミットの regression パス

`5dbecc4`（`git show --stat`: `agents/pm-spec.md` 1 行、`agents/qa-verifier.md` 1 行、計 2 insertions/2 deletions）を確認。F2/F3 が spec の frozen block から抽出したバイト列とライブファイルの該当行を自分でも `grep -n` で突き合わせ、一致を確認した（QA も `cmp` で独立に再導出済み）。F1 はコミットに現れない＝v1→v2 で不変のまま再利用されたことが `git diff` の非存在自体で裏付けられる。`19f95c3`（board のみ、3 insertions/1 deletion）と `7d3bc38`（reviews 記録の追記＋board、234 insertions/1 deletion、いずれも追記でありコードは無変更）を確認。`bin/check-acs.sh`・`bin/check-intent.sh`・`bin/check-prompt-sync.sh` を自分で再実行し、いずれも QA の報告と一致する結果を得た（上記 Method 参照）。AC5/AC6/AC8（マーカー領域・生成ブロック・非対象ファイルの byte-identity）はいずれも自分の再実行で PASS。

**結論: regression なし。** 変更は re-freeze が要求した 2 行の re-splice に厳密に限定されている。

## (d) Round 1 の Minor 6 件の処分再確認

- **row 2（`READY_FOR_ENG` routing の明示性）— 解消と判定（自然解消の申告どおり）。** v2 の F3 は「Report a flattened distinction as a finding (kick back to `READY_FOR_ENG`), not as a wording nit — and when it sits inside the frozen region, say where, since the repair is a class-B re-freeze routed through pm-spec rather than an engineer edit to hashed bytes.」という一文を新たに持つ。round 1 では `agents/engineer.md` の汎用エスカレーション経路に依存するだけだったが、v2 は F3 自身がルーティング（宣言だけでなくフローズン領域内のどこに flattening があるかを名指しし、それが pm-spec 経由の class-B re-freeze であることを明示）を持つに至った。解消と認める。
- **row 7（`Rule` field の "assigns neither step" という主張の緩さ）— 解消と判定（自然解消の申告どおり）。** spec の宣言行自体が精緻化され、「Its `Rule` field states the discipline **and does name the QA-side action** ("the verifying role confirms that by opening the source document and matching it clause by clause"), but it assigns pm-spec no naming-at-write-time step and does not carry QA's "unverified until compared" stance — both of those live only in `How to apply`」という一文に変わった。round 1 の QA・Codex 双方が「defensible だが緩い」と評価した箇所そのものが訂正されている。解消と認める。
- **row 3（missing-section fallback の adoption-boundary が無いこと）— 未変更、won't-fix のまま。** `## Input space` 精度と同じ既存の許容トレードオフであり、v2 はこの箇所に触れていない。根拠は変わらず有効。
- **row 4（宣言が rework 中に可変で、frozen hash 側の主張との非対称を生みうること）— 未変更、won't-fix のまま。** Non-goals の該当箇条書き（「A spec that lies in its slot」）は v2 でも byte-for-byte 不変。
- **row 5（`environmentally-unverified` が FAIL を強制しないこと）— 未変更、won't-fix のまま。** D3(v)（旧番号、v2 では六properties 中の (v)）は内容不変、番号のみ (vi) に対し (v) のまま維持。
- **row 6（虚偽・不完全な宣言が検出されない bypass）— 未変更、won't-fix のまま。** row 4 と同じ Non-goals 箇条書きに根拠を置き、v2 でも不変。

**結論: round 1 の Minor 6 件のうち 2 件（row 2, row 7）は v2 によって実質的に解消され、残り 4 件は disclosed limits のまま妥当に won't-fix。**

#### Blockers
- なし。

#### Major
- なし。

#### Minor / nits
- **F3・D3(ii)・Goal 文が、宣言行を「フローズン領域内の一サイト」として扱っている一方、D1 は宣言セクションを intent block の**外**に明示的に配置しており、Input space の Non-goals 箇条書き（「Restatement sites outside the frozen region」）は scope を「the intent block ... **plus** the declaration line」という加算的な言い方で正しく区別している（`.shell-team/specs/T-1029-claim-fidelity-qa-step.md:122`）。つまり 3 箇所（Goal 文、D3(ii)、F3 本文 — `agents/qa-verifier.md:65`）は宣言行をカテゴリ上「フローズン領域の一部」であるかのように記述しており、D1/Input-space の記述と厳密には整合しない。Codex の primary パス（`[P2]`）が独立に指摘し、devil's-advocate パスにも同じ finding を評価させた: 「declaration line itself」は当該文の中で明示的に列挙されており、直後の文が「Confirming the declaration line is accurate...」で始まり、F3 自身が verdict に「the sites you checked」を要求する — したがって文字どおりに F3 を実行する QA が宣言行の検証を**省略する**実害は乏しい、というのが adversarial パス自身の結論であり、私も同意する。実際 QA round 2 の実行記録は宣言行を列挙された一サイトとして明示的にチェックしており、混乱の形跡はない。**用語上のカテゴリ不整合であって、機能的な検証漏れではない** — 将来の precision re-freeze で「plus the declaration line, which sits outside the intent block」のような加算的な言い回しに揃えるのが望ましいが、ブロッキングではない。

#### Adversarial findings
- Devil's-advocate パス（上記 Minor 1件を対象に実行）は、prior な severity 判断（Codex 自身の primary `[P2]`）に反論する形で走らせた結果、「terminology-only interpretation の方が説得力がある」と自己反証し、Minor へのdowngradeに同意した。生の出力: `.shell-team/reviews/T-1029-codex-adversarial.{jsonl,txt}`。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Round 1 Major — F3 が宣言行だけに検証を縛っていた | round 1 primary: blocker (`[P1]`) → round 1 final: major | closed (verified) | v2 が scope 節を明示（Goal/D3(ii)/F3/AC4/AC11(b)/AC11(c) の同時sweep）。`check-acs.sh` AC4 の 7 個目の固定文字列、AC11(b) の re-narrowing mutation が revert lock として機能することを QA が実測し、私も `check:` 行を自分で読んで整合を確認した。 |
| 2 | Round 1 Minor row 2 — `READY_FOR_ENG` routing の明示性 | round 1: minor (won't-fix 保留ではなく fast-follow) | closed (resolved by v2) | F3 が「kick back to `READY_FOR_ENG`」「say where」「class-B re-freeze routed through pm-spec」を自ら明文化。汎用escalationへの依存から自己完結型の記述に変わった。 |
| 3 | Round 1 Minor row 7 — `Rule` field の "assigns neither step" の緩さ | round 1: minor (won't-fix) | closed (resolved by v2) | 宣言行自体が「does name the QA-side action ... but assigns pm-spec no naming-at-write-time step and does not carry QA's ... stance」に精緻化。 |
| 4 | Round 1 Minor rows 3, 4, 5, 6 | round 1: minor (won't-fix, out-of-input-space grounds) | unchanged (still minor, won't-fix) | 根拠となる Non-goals / D3(v) の箇条書きは v2 で byte-for-byte 不変。 |
| 5 | Round 2 new — declaration line の「frozen region 内の一サイト」というカテゴリ記述が D1/Input-space の加算的記述と整合しない | primary: `[P2]` (major 相当) → adversarial: minor | minor | 宣言行は当該文中に明示的に列挙され、直後の文・verdict 要件（sites you checked）が省略を防ぐ。adversarial パス自身が「terminology-only interpretation の方が説得力がある」と結論。QA round 2 の実行が宣言行を実際にチェックした事実とも整合。R2 の two-consecutive-round トリガーは Major/Blocker が条件であり、本件は Minor のため不成立。 |

#### Recommendation
- APPROVE → `.shell-team/todo.md` を `READY_FOR_MERGE` に設定する。Round 1 の唯一の Major はクローズ済みで新たな Blocker/Major は見つかっていない（R2 の two-consecutive-round トリガーは不成立のまま）。v2 の delta は ratification 行が記述する範囲に厳密に閉じており、re-splice コミットに regression はない。

#### Fast-follow disposition
- 上記 Minor（declaration line のカテゴリ記述不整合、`agents/qa-verifier.md:65` ほか Goal 文・D3(ii)）— file-an-issue: 将来の precision re-freeze で「... a settled-decision paragraph inside the marker pair — plus the declaration line itself, which sits outside the intent block and is never the whole set」のような加算的な言い回しに揃えることを提案する低優先度の precision fix。実害（検証漏れ）は無いことを adversarial パスと QA round 2 の実行記録の双方から確認済みであり、緊急性はない。
- Round 1 の Minor row 3（missing-section fallback の adoption-boundary 欠如）— won't-fix: 変更なし。根拠（`## Input space` 精度との整合的な既存トレードオフ）は round 1 のまま有効。
- Round 1 の Minor row 4（宣言の可変性と frozen hash 側の非対称）— won't-fix: 変更なし。Non-goals の disclosure が根拠のまま。
- Round 1 の Minor row 5（`environmentally-unverified` の非blocking性）— won't-fix: 変更なし。既存の repo 規約の踏襲。
- Round 1 の Minor row 6（虚偽・不完全な宣言の bypass）— won't-fix: 変更なし。row 4 と同じ Non-goals disclosure が根拠。
