# Codex Review — T-1080

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1080
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 10 files, +464/-7 lines (base `cf643a9d7513bf745337b164f31619ebe4c31d0a` = `git merge-base feature/1079-tier2-judge HEAD`)

This is a `no-mechanism`, `user-visible: no` deliverable: seven role-contract amendments written as preconditions on a `concurrent-review-window` no default path enters today, plus three errata folded into `docs/loop-engineering/agent-concurrency.md`. Per the spec's own `### Verdict scope and per-artifact review depth`, `agents/codex-reviewer.md`, `agents/qa-verifier.md`, `skills/run/SKILL.md` and `bin/loop-guard.sh` get adversarial depth with no carve-out; `docs/loop-engineering/agent-concurrency.md` gets note depth (connective-prose findings only are downgraded there).

**Mechanical verification, independently re-run rather than trusted to the hand-off:** `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh .shell-team/specs/T-1080-depth-axis-contract.md` → `18 passed, 0 failed, 1 skipped` (AC19 SKIP by design). `bash tests/loop-guard/run.sh` → exit 0, `grep -c '^PASS:' <log>` → `30`. `bash bin/check-intent.sh .shell-team/specs/T-1080-depth-axis-contract.md .shell-team/todo.md` → `aligned: T-1080 v2 (3b0e76e69b728f894b755c1f7e6f45fb3a91ada4)`. I also independently re-derived the class-B re-freeze classification rather than accepting the board's transcription of it: `git show 966131e:.shell-team/specs/T-1080-depth-axis-contract.md > v1.md; bash bin/check-refreeze-class.sh v1.md .shell-team/specs/T-1080-depth-axis-contract.md` → `check-refreeze-class: class-b: differing line at intent-block index 80 ... exit 1` — confirms the board's `class-B` classification and the interventions file's `assumption-contradicted` entry independently. AC5's own base-relative-delta repair (HEAD = base+1, base read from the blob, step-6-region clause) is sound: it repairs a genuinely unsatisfiable frozen premise (base count measured 2, not 0, from already-merged T-1074/T-1077 `both-gates-green` lines) without touching the amendment's own substance. I have no objection to this re-freeze; the intent trajectory (v1 → engineer BLOCKED → intervention → pm-spec v2 → orchestrator re-verify → QA PASS) is intact and each hop is independently checkable.

A single foreground `codex exec … review --base <branch-point> --json` pass ran clean; its findings, checked against the actual files myself, are folded in below. Because it surfaced Major-candidate findings on the S6/S7 isolation surfaces, I ran the opt-in adversarial pass (devil's-advocate framing, targeted at the same two surfaces) — it independently confirmed all three, unprompted, at its own top severity. I read the full text of both `agents/qa-verifier.md` and `agents/codex-reviewer.md` (not only the diff) to confirm each finding against surrounding context before folding it in.

#### Blockers

None. Nothing here silently changes today's shipped serial behavior (verified above: `bin/loop-guard.sh`'s executable text is unchanged per AC7's comment-stripped comparison, and the fixture suite still passes 30/30; `skills/run/SKILL.md`'s numbered phase order is preserved; the reviewer's `READY_FOR_REVIEW` gate and its unpinned-working-tree read both survive verbatim as the serial default). Every gap below is confined to text that only takes effect inside a window nothing in this repository can enter today.

#### Major

- **S6's "no working-tree edit at all" reads as forbidding qa-verifier's own required board hand-off, which the surrounding contract still requires inside the same window.** `agents/qa-verifier.md:57` states: "so inside that window you make **no** working-tree edit at all, including this otherwise-permitted test-only one." Taken at face value this also covers the mandatory PASS/FAIL append to the board (`tasks/todo.md`) that the Output section (lines 29-53) requires every round, and that the *un-amended* T-073 rule (line 66, still carrying "apart from your own hand-off append to the board... `git status --short` shows nothing beyond that board edit") and the new S7 sentence right beneath it (line 67, "evaluate this clean-tree assertion... against that same pinned SHA") both presuppose still happens. The three sentences do not agree on what "no working-tree edit at all" excludes: S6 reads absolute, while T-073's still-unedited license and S7's own text both assume the board edit persists. This is exactly the "the amendment is descriptive where it needed to be prescriptive" class the spec's own verdict-scope section marks as full-severity, not the "connective prose" class that gets a pass. Fix: make the board hand-off append an explicit, named exception inside S6's own sentence (mirroring T-073's own phrasing), so a reader does not have to reconcile three separate bullets to recover the intended scope.
- **S7 defers the reviewer's one-file *commit* exception but not the other file writes the role's own write-boundary rule requires regardless of that exception.** `agents/codex-reviewer.md:159` only instructs "you do **not** exercise it [the commit exception]... Return the review record to the coordinating session instead." But the same write-boundary rule (same bullet, a few sentences earlier) and the "Save the raw Codex output" instruction (line 139) both mandate writing `T-XXX.md`, `T-XXX-codex-primary.{txt,jsonl}` (and, when triggered, `T-XXX-codex-adversarial.{txt,jsonl}`) into the shared reviews dir as an ordinary part of doing the review — independent of whether a commit follows. None of this is deferred by S7. If `codex-reviewer` runs this concurrently while `qa-verifier` is mid-Validate, these untracked writes land in the *same* shared checkout `qa-verifier`'s own T-073 clean-tree assertion inspects (`git status --short` "shows nothing beyond that board edit") — and an untracked review-record file is exactly something beyond that board edit. Deferring the git-commit half of the exception does not close the isolation gap S6/S7 exist to close; it only closes the git-history half of it. Fix: either instruct the reviewer to buffer these writes under `$TMPDIR` until both gates return (matching S7's own "return it to the coordinating session" framing already used for the commit), or explicitly widen QA's clean-tree assertion inside the window to tolerate the reviewer's own declared write paths.
- **The `pinned-read` instruction is prose only; the actual invocation recipe in step 3 is not amended to read anything other than the moving working tree.** `agents/codex-reviewer.md`'s new sentence says the reviewer should "pin your read to the commit SHA the launch brief names — checking out or reading that pinned commit rather than the moving working tree" — but the concrete `codex exec --sandbox read-only --cd <repo> review --base <base> --json -o <file>` block immediately following it, and the free-form adversarial-pass prompt later in the same step ("Review the diff between `<base>` and HEAD... Read the files yourself with `git diff`... do not expect any diff in this prompt"), are both untouched and both still literally target the live, moving working tree via `--cd <repo>`. This document's own convention elsewhere (T-097/T-107) is to keep the executable bash blocks in lockstep with the prose describing them; this amendment breaks that convention by leaving the mechanism entirely to inference. Fix: name the concrete mechanism (e.g. a `git worktree add --detach <scratch> <SHA>` used as `--cd` in place of `<repo>`, the same pattern this very task's own Blast-radius section already uses for a base-side read) in the actual invocation recipe, not only in the surrounding prose.

#### Minor / nits

- **Erratum (b)'s corrected `agent-tool-concurrent-launch` row omits the scope qualifier its own cited evidence carries, breaking this document's own established convention.** `docs/loop-engineering/harness-agent-concurrency.md` (the note this errata cites) explicitly discloses that its measurement covers only `subagent_type=general-purpose` agents and states verbatim: "unobserved: whether general-purpose Agent-tool concurrency extends to a plugin-role subagent type (e.g. a role bound through this repository's own `agents/*.md` definitions)" — and `qa-verifier`/`codex-reviewer` are exactly such plugin-role subagents, the very type S1-S7 concern. The corrected row in `agent-concurrency.md` states unqualified "this harness genuinely executes concurrent Agent-tool sub-agent invocations from one orchestrator session," with no scope caveat, even though sibling rows in the same section are careful to scope their own evidence (e.g. `background-agent-launch — provided — **evidence scoped to Bash-level backgrounding only**`). I judge this Minor rather than Major: `docs/loop-engineering/agent-concurrency.md` is note-depth per the spec's own pricing, the frozen **AC12** does not require a scope qualifier (only the two citation literals), and the caveat itself is prominently disclosed one hop away in the very note being cited — so nothing here is a false claim, only an incomplete one relative to this document's own convention. Worth a one-line tightening as a fast-follow.

#### Adversarial findings

A second `codex exec` pass, devil's-advocate framed at the S6/S7 isolation contract specifically (raw output: `.shell-team/reviews/T-1080-codex-adversarial.{txt,jsonl}`), independently surfaced all three Major findings above at its own top severity, unprompted beyond the general framing: (1) the board-hand-off/S6 conflict, citing the same three sites (Output section, T-073 rule, new S6 sentence); (2) the deferred-commit-vs-deferred-write gap, naming the same three artifact basenames; (3) the pinned-read-is-prose-only gap, citing both the primary invocation block and the adversarial-pass prompt as unamended. No new finding beyond these three.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | S6's "no working-tree edit at all" conflicts with QA's still-required board hand-off | primary: P1 (→ blocker by the normalization map); adversarial: "blocker" (own vocabulary), independently corroborated citing the same three sites | major | Real self-contradiction inside inert conditional text (the window nothing enters today), not a break in the shipped serial default (verified byte-unchanged above) — downgraded from blocker because no adopter-facing path is affected today, but must be fixed before the window is ever enabled. |
| 2 | S7 defers the commit exception but not the reviewer's other mandated writes into the shared checkout | primary: P1 (→ blocker); adversarial: "blocker", independently corroborated naming the same artifact basenames | major | Same reasoning as #1 — a genuine isolation-contract gap in not-yet-enabled text, not a defect in today's default path. |
| 3 | `pinned-read` is a prose instruction only; the concrete invocation recipe is unamended | primary: P1 (→ blocker); adversarial: "blocker", independently corroborated citing both unamended command sites | major | Same reasoning — real gap in what should be a prescriptive contract, but the mechanism is already demonstrated elsewhere in this task's own records (the Blast-radius section's `git worktree add --detach`), so this is a documentation-completeness gap rather than an unrecoverable design hole. |
| 4 | Erratum (b)'s `agent-tool-concurrent-launch` row omits the plugin-role scope qualifier its own cited source discloses | primary: P2 (→ major) | minor | `docs/loop-engineering/agent-concurrency.md` is note-depth per the spec's own verdict-scope pricing; frozen AC12 does not require a scope qualifier; the caveat is disclosed one hop away in the cited note itself — incomplete relative to this document's own convention, not false. |

#### Recommendation

- REQUEST_CHANGES → set the board's T-1080 entry back to `READY_FOR_ENG` with the three Major findings above as the required fixes: (1) name the board hand-off as an explicit exception to S6's "no working-tree edit at all" in `agents/qa-verifier.md`; (2) either buffer the reviewer's review-record and capture-file writes under `$TMPDIR` until both gates return, or explicitly widen QA's clean-tree assertion inside the window to tolerate them, in `agents/codex-reviewer.md`/`agents/qa-verifier.md`; (3) name the concrete pinned-read mechanism (e.g. a detached scratch checkout used as `--cd`) in the actual invocation recipe of `agents/codex-reviewer.md` step 3, not only in the surrounding prose. None of the three touches this spec's frozen intent block (the seven-surface table, the token vocabulary, the Goal/Non-goals/Input space) — all three are textual tightenings of the already-frozen amendments' own prose, not a re-freeze.

#### Fast-follow disposition

- Erratum (b)'s missing plugin-role scope qualifier on the `agent-tool-concurrent-launch` row — file-an-issue. Rationale: a real, if narrow, precision gap relative to this document's own convention and to the underlying evidence's own disclosed limits; worth a one-line fix, but not load-bearing for this task's own frozen AC12, and cheap enough to fold into the same rework pass that fixes the three Majors above without requiring its own round.

---

## Round 2 (codex)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1080
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 5 files, +39/-4 lines (rework diff `git diff 2275cce...HEAD`, engineer rework `0f63e26` + QA round-2 PASS `7b443ad`, on top of round-1's reviewed HEAD `2275cce`)

Scope of this round is narrow by design, per the coordinator's brief and the no-ratchet rule: verify round-1's three Majors + one Minor are actually fixed, and sweep the rework diff for anything the fixes broke — not a re-litigation of anything round 1 already passed. I read the rework diff myself (`git diff 2275cce...HEAD -- agents/qa-verifier.md agents/codex-reviewer.md docs/loop-engineering/agent-concurrency.md .shell-team/provenance/T-1080.md`) and the full current text of both agent files before invoking Codex, and independently re-ran the full no-regression battery: `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh` → `18 passed, 0 failed, 1 skipped`; `bash bin/check-intent.sh` → `aligned: T-1080 v2 (3b0e76e69b728f894b755c1f7e6f45fb3a91ada4)` — same hash as round 1, and `git diff 2275cce...HEAD --name-only -- .shell-team/specs/T-1080-depth-axis-contract.md` is empty, confirming the spec (and its frozen intent block) is untouched by this rework; `bash tests/loop-guard/run.sh` → exit 0, 30/30 `PASS:`; `bash bin/check-prompt-sync.sh` → in sync.

**(1) S6 board hand-off exception — verified fixed.** `agents/qa-verifier.md:57` now reads "...so inside that window you make **no** working-tree edit at all, including this otherwise-permitted test-only one, **apart from your own hand-off append to the board (`tasks/todo.md`), exactly as the T-073 Rule below already licenses** — and record `test-edit-deferred`..." I read all three sentences together (this one, the un-edited T-073 Rule at line 66, and the S7 sentence at line 67): they now agree — the board append is named as an explicit, in-line exception rather than left for a reader to reconcile across three separate bullets. Round-1's Major #1 is closed.

**(2) S7 write-buffering — the intent is right, the text is not yet operational for half of what it defers.** `agents/codex-reviewer.md`'s rewritten write-boundary Rule now explicitly names both halves of round-1's gap (git-history and working-tree) and instructs: "Inside the window, write all of these under `$TMPDIR` instead of the reviews dir... rather than landing any of them in the shared checkout." I checked this against `bin/codex-capture.sh` itself, which the same document's own step-3 recipe (a few lines above, unmodified this round) instructs the reviewer to run for exactly the artifacts S7 names (`T-XXX-codex-primary.{txt,jsonl}` / `T-XXX-codex-adversarial.{txt,jsonl}`). That script's own header states its design explicitly (`DP-b`): "`--alloc` creates its two raws INSIDE the resolved reviews dir — same filesystem as the canonical target... `--publish` enforces this as a fail-closed PRECONDITION... it refuses (exit 2, no `mv` ever attempted) unless both raws' parent directory resolves to the SAME resolved reviews dir" — and names `$TMPDIR` by name as the exact anti-pattern this design exists to refuse ("never a cross-fs copy that could silently degrade the DP-2 atomicity guarantee (e.g. a raw living under `$TMPDIR`)"). I verified this in the script's own code, not only its comments: lines 199-204 (`--alloc` `mktemp`s both raws inside `$reviews_dir`) and lines 229-232 (`--publish` dies with exit 2 if either raw's parent directory isn't the resolved reviews dir, before any `mv`). So the new S7 sentence, as written, is unreconciled with the very recipe blocks sitting a few lines above it in the same file: an agent following the recipe unmodified still writes into the shared reviews dir regardless of the window, and an agent who tried to relocate the raws to `$TMPDIR` first would hit `--publish`'s own fail-closed refusal. The good news, which both my own read and the adversarial pass below surfaced: the script already exposes a `--reviews-dir <dir>` override (usage line, and threaded through both `--alloc` and `--publish`'s own resolution at lines 133-135/170-176), so passing `--reviews-dir "$TMPDIR"` to both calls for every stem inside the window would genuinely achieve what S7 intends — the fix is one flag away, not a redesign — but the current text names no such mechanism, so as shipped it does not close the gap for the capture-file half. Separately, and correctly: the review-record file `T-XXX.md` itself is written directly (the reviewer's own Write tool, never through `codex-capture.sh`), so it has no such conflict and genuinely is deferrable to `$TMPDIR` as S7 now states.

**(3) Pinned-read mechanism — verified fixed, both recipes.** Both `codex exec --cd <repo> ...` blocks in step 3 — the primary `review --base <base>` invocation and the free-form adversarial-pass prompt — now carry matching inline comments naming the concrete mechanism (`git worktree add --detach <scratch> <the launch brief's own pinned SHA>`, substituting `<scratch>` for `<repo>`), citing the same pattern this task's own Blast-radius section already used for a base-side read, and each closes with the serial-path guarantee stated explicitly ("In serial operation — the shipped default and the only mode today — `<repo>` is simply this checkout, unchanged, and no scratch worktree is created" / "`<repo>` is unchanged"). Round-1's Major #3 — prose describing pinning without the invocation blocks actually reflecting it — is closed in both sites, not only one.

**(4) Erratum (b) scope qualifier — substance fixed, one residual wording nit.** The rewritten `agent-tool-concurrent-launch` row in `docs/loop-engineering/agent-concurrency.md` now mirrors `harness-agent-concurrency.md`'s own disclosed limit close to verbatim: `subagent_type=general-purpose` scoping, and the "unobserved: whether general-purpose Agent-tool concurrency extends to a plugin-role subagent type..." sentence, quoted correctly. I independently re-verified the wording nit QA's own round-2 note already flagged: the new sentence attributes this quote to that note's "Limits section," but the sentence's actual, verbatim home is `## Agent-type boundary` (line 374) — `## Limits and what is not computable` (line 390) only cross-references the same fact one bullet down ("...is unobserved (`## Agent-type boundary` above), not inferred"), it does not itself state it verbatim. This is exactly the note-depth "label mismatch" class the spec's own verdict-scope section names as non-gating (the corrected *value* is right; only the section-name attribution is one heading off). Recorded as a note, matching QA's own disposition — not required before merge.

A single foreground `codex exec` pass targeted at the rework diff surfaced the (2) gap independently and unprompted (raw output: `.shell-team/reviews/T-1080-codex-primary.{txt,jsonl}`). Because it is a Major, I ran the adversarial pass (raw output: `.shell-team/reviews/T-1080-codex-adversarial.{txt,jsonl}`), which confirmed the same gap and additionally named the `--reviews-dir` override as the available fix path the primary pass had not identified by name — I verified that flag's mechanics myself directly in the script before folding it into the recommendation below.

#### Blockers

None. Nothing here changes today's shipped serial behavior — the same invariant round 1 established still holds (`check-intent` unchanged hash, spec/frozen-intent-block untouched, `bin/loop-guard.sh` not touched this round at all, fixture suite still 30/30).

#### Major

- **S7's `$TMPDIR`-buffering instruction is unreconciled with `bin/codex-capture.sh`'s own fail-closed design for the two canonical raw-capture basenames.** `agents/codex-reviewer.md`'s new S7 text ("write all of these under `$TMPDIR` instead of the reviews dir") does not account for `codex-capture.sh`'s `--alloc` (always allocates inside the resolved reviews dir) and `--publish` (refuses, exit 2, no `mv`, unless both raws' parent directory is that same resolved reviews dir) — verified directly in the script's own code (lines 199-204, 229-232), not only inferred from its comments. As written, an agent running the unmodified step-3 recipe still writes `T-XXX-codex-primary.{txt,jsonl}` (and, when triggered, the adversarial pair) into the shared reviews dir regardless of the window, and relocating them afterward is refused by `--publish` itself. The script already carries the fix (`--reviews-dir <dir>`, threaded through both `--alloc` and `--publish`), so this is a documentation gap, not a design flaw needing a new mechanism — but the current text does not name it. Fix: in both of step 3's recipe blocks, inside the window, pass `--reviews-dir "$TMPDIR"` (or an equivalent launch-brief-provided buffer directory) to every `codex-capture.sh --alloc` and matching `--publish` call for both stems, and state that the coordinating session moves the resulting `$TMPDIR`-resident canonical files into the real reviews dir once both gates return — mirroring how the review record itself is already handled. (The review-record file `T-XXX.md` has no such conflict — it is written directly, not via `codex-capture.sh` — and is correctly fixed as-is.)

#### Minor / nits

- **Erratum (b)'s scope-qualifier fix attributes its verbatim quote to the wrong section name.** The new sentence in `docs/loop-engineering/agent-concurrency.md` says the quoted "unobserved..." sentence comes from `harness-agent-concurrency.md`'s "Limits section," but that sentence's actual home is `## Agent-type boundary` (line 374); `## Limits and what is not computable` only cross-references it. The quoted content itself is byte-accurate — this is a section-name attribution slip, matching this round's own QA note and the note-depth "label mismatch" carve-out. Worth a one-word fix ("its `## Agent-type boundary` section" in place of "its own Limits section") folded into the same pass that fixes the Major above; not gating on its own.

#### Adversarial findings

The adversarial pass (devil's-advocate framed specifically at the S7 fix and `bin/codex-capture.sh`'s own design) independently confirmed the Major above at its own top severity, citing the same DP-b comment and the same `--publish` precondition lines I had already verified myself, and additionally named the `--reviews-dir` override as the available, unused fix path — a refinement the primary pass had not made explicit (it only said "use a launch-brief-provided buffer" without naming the flag). It also confirmed, independently, that the directly-written `T-XXX.md` review record has no conflict and is correctly deferrable to `$TMPDIR` as S7 states. No further finding beyond this one.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Round-1 Major #1 (S6 board-hand-off conflict) | not re-raised by either pass; independently verified fixed by direct read of all three sentences | resolved | The board-append exception is now stated inline in S6's own sentence, byte-matching T-073's phrasing; no remaining disagreement among the three sentences. |
| 2 | Round-1 Major #2 (S7 defers commit but not other writes) | primary: major (recipes still target the shared reviews dir); adversarial: major, independently confirmed citing the same DP-b/`--publish` lines and additionally naming the `--reviews-dir` fix path | major (carried forward, new text) | Genuine remaining gap verified directly in `bin/codex-capture.sh`'s own code: the fix's intent is right and the tool already has the flag needed (`--reviews-dir`), but the current S7 text names no mechanism reconciling it with the unmodified step-3 recipe — the capture-file half of the isolation gap is not yet closed, though the review-record half now is. |
| 3 | Round-1 Major #3 (pinned-read is prose only) | not re-raised by either pass; independently verified fixed by direct read of both invocation blocks | resolved | Both the primary and adversarial-pass `codex exec` blocks now carry the concrete `git worktree add --detach` mechanism and an explicit serial-path-unchanged statement. |
| 4 | Round-1 Minor (erratum (b) scope qualifier) | not re-raised by either pass (out of this round's primary prompt scope); independently re-verified via direct read, matching QA's own round-2 risk note | minor / note | Substance genuinely fixed (qualifier mirrors the cited source's disclosed limit verbatim); one residual section-name attribution slip, exactly the note-depth "label mismatch" class — non-gating. |

#### Recommendation

- REQUEST_CHANGES → set the board's T-1080 entry back to `READY_FOR_ENG` with the one Major above as the required fix: in both of `agents/codex-reviewer.md` step 3's recipe blocks, pass `--reviews-dir "$TMPDIR"` (or the equivalent launch-brief-provided buffer) to every `codex-capture.sh --alloc`/`--publish` call inside the window, for both the primary and adversarial stems, and state that the coordinating session moves the resulting files into the real reviews dir once both gates return. Fold the Minor section-attribution fix into the same pass opportunistically. Neither touches the spec's frozen intent block (confirmed above: the spec file is untouched by this rework) — this is a further textual tightening of already-frozen amendment prose, not a re-freeze, and Majors #1/#3 need no further action.

#### Fast-follow disposition

- Erratum (b)'s "Limits section" vs "Agent-type boundary" attribution slip — won't-fix as a blocking item, worth folding into the same commit that fixes the Major above. Rationale: the quoted content is byte-accurate; only the section-name label is one heading off, and this is cheap enough to fix opportunistically without its own round.

## Round 3 (codex)

### Codex Review verdict: APPROVE
- Task: T-1080
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 7 files, +25/-6 lines (`git diff 714efa7...HEAD`, engineer drop-execution `345a38a` + QA round-3 PASS `a612852`, on top of round-2's reviewed HEAD `714efa7`)

This round is the final gate: it does not fix S7 again, it executes the frozen Pre-commitment's drop **①** — round 2's Major was the second consecutive independent Major against surface S7 (round 1: deferred the commit but not the other mandated writes; round 2: the `$TMPDIR`-buffering repair itself unreconciled with `bin/codex-capture.sh`'s own fail-closed design), so the factual trigger fired and the orchestrator executed the drop exactly as frozen rather than requesting a third repair attempt. My job this round is to judge whether the drop executed faithfully, not to re-litigate S7's substance — the frozen Pre-commitment text explicitly decided this disposition at freeze time "precisely to remove that in-the-moment judgment" (per the interventions record), and second-guessing the decision to drop rather than continue repairing would be exactly the kind of judgment the pre-commitment exists to take out of anyone's hands, including mine.

**(1) The drop's execution against the frozen disposition — verified faithful.** I read the round diff myself (`git diff 714efa7...HEAD -- agents/qa-verifier.md agents/codex-reviewer.md skills/run/SKILL.md docs/loop-engineering/agent-concurrency.md .shell-team/specs/T-1080-depth-axis-contract.md .shell-team/provenance/T-1080.md`) and the full current text of both agent files. `grep -c record-commit-deferred agents/qa-verifier.md agents/codex-reviewer.md skills/run/SKILL.md` → `0` in all three, confirmed myself. `agents/codex-reviewer.md`'s write-boundary Rule bullet is now **byte-identical** to its pre-T-1080 branch-point blob (`cmp` against `git show cf643a9d…:agents/codex-reviewer.md`'s own extracted bullet returns equal) — the entire S7 addition (both the original commit-deferral sentence and round 1's later write-buffering repair) is gone as one clean unit, not a stub. `agents/qa-verifier.md`'s standalone S7 bullet (the clean-tree/pinned-base duty) is deleted outright — the T-073 Rule immediately above it (line 66) is untouched and matches base exactly. `skills/run/SKILL.md`'s combined launch-brief-duties bullet is re-labelled `S1/S2/S6` (from `S1/S2/S6/S7`) with the S7 sub-clause removed and a one-line disclosure pointer to issue #309 appended. The spec's new `- dropped-surface: s7 — ` line (inside the explicitly mutable `## Pre-commitment disposition record` section, confirmed sitting well after `<!-- END intent-block: T-1080 -->` — `git diff 714efa7...HEAD -- .shell-team/specs/T-1080-depth-axis-contract.md | grep -c 'BEGIN intent-block\|END intent-block'` → `0`) carries a substantive disposition naming both rounds' findings by content, not a bare pointer — satisfying the frozen paragraph's own "as its requirement list rather than being re-derived there" requirement. **AC9 confirmed firing genuinely through the waiver**, not incidentally: I independently re-ran `check-acs.sh` at HEAD and read AC9's own check body directly — its `if grep -qE '^- dropped-surface: s7 — .' "$SP"; then exit 0; fi` branch is what returns `PASS (exit 0)` before any of the positive-obligation body (which now requires `record-commit-deferred` present, correctly zero everywhere) ever runs. QA's own round-3 mutation probe (delete the line on a scratch worktree → AC9 goes red; restore → green) is exactly the right two-sided test for this, and its logic checks out by direct reading of the check body — I did not need to re-run the scratch-tree mutation myself to trust it, since the branch is simple enough to verify by inspection and I confirmed the "present" half live myself.

**(2) Surviving surfaces — S1–S6 and every serial-path sentence confirmed byte-intact.** `agents/qa-verifier.md:57`'s S6 sentence (`test-edit-deferred`, the board-hand-off exception fixed in round 1) is unchanged from round 2. `agents/codex-reviewer.md`'s pinned-read mechanism — the two inline comments naming `git worktree add --detach <scratch> <SHA>` in both the primary `review` invocation and the adversarial free-form-prompt invocation (fixed in round 1) — are both present, unedited by this round's diff (confirmed: `git diff 714efa7...HEAD -- agents/codex-reviewer.md` touches only the write-boundary-rule bullet, nothing above line 168). `bin/loop-guard.sh` is untouched this round (not in the diff at all — S5's comment-only carve-out survives from round 1 with no further action needed). `skills/run/SKILL.md`'s S1/S2/S6 duties (window declaration, pinned SHA, no-QA-hand-off instruction, no-edit instruction) read as one coherent, un-truncated sentence after the S7 sub-clause's removal — I read it end to end myself and it is grammatically and substantively complete without S7's clause.

**(3) No-ratchet — respected.** Rounds 1–2 already passed S1–S6 and the three errata substantively; this round's scrutiny is confined to what the drop-execution diff actually touched (the S7 removal sites, the disposition line, and the round-2 residual Minor's fold-in), not a re-audit of unchanged content. The round-2 Minor (erratum (b)'s "Limits section" vs. `## Agent-type boundary` attribution) is folded in correctly: I re-read `docs/loop-engineering/harness-agent-concurrency.md` directly and confirmed the quoted sentence's real home is `## Agent-type boundary` (line 374), and the corrected row now names that section explicitly while also noting the Limits section's own cross-reference — matching what I asked for in round 2.

**(4) Mechanical — clean, independently re-run.** `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh` → `18 passed, 0 failed, 1 skipped` (AC9 green via the waiver, confirmed above, not via S7's now-removed positive obligations). `bash bin/check-intent.sh` → `aligned: T-1080 v2 (3b0e76e69b728f894b755c1f7e6f45fb3a91ada4)` — same hash as every prior round, confirming no re-freeze occurred. `bash bin/check-prompt-sync.sh` → in sync. `bash tests/loop-guard/run.sh` → exit 0, 30/30. `.shell-team/interventions/T-1080.md` carries the new `work-deferred` entry, read directly — it names the two-consecutive-Majors trigger, the orchestrator's attestation against both review records, and the drop's effect, matching what actually happened in this review's own rounds 1–2.

A single foreground `codex exec` pass targeted at the drop-execution diff surfaced one Minor-severity finding, which I evaluated and reject below (raw output: `.shell-team/reviews/T-1080-codex-primary.{txt,jsonl}`). No Blocker or Major surfaced, so the adversarial pass is not triggered this round (per step 5's condition).

**Environmentally-unverified issue-canon note.** This round's disposition turns partly on issue #309's own body matching what the spec/board claim it carries. I attempted `gh issue view 309 --json title,body,state` directly; it fails closed in this sandbox (`tls: failed to verify certificate: x509: OSStatus -26276`), the same known environmental limitation recorded in this task's own round-1/round-2 reviews and in T-1079's. **internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).** What I *could* check — the spec's `- dropped-surface: s7 — ` line and the board's own round-2/round-3 entries, both naming "round 1's deferred-writes-not-deferred-commits gap and round 2's `codex-capture.sh`/`--reviews-dir` reconciliation gap" — is fully internally consistent with my own two Major findings from rounds 1 and 2 verbatim.

#### Blockers

None.

#### Major

None.

#### Minor / nits

- **Rejected finding, recorded for the audit trail (see ledger):** Codex's primary pass flagged `skills/run/SKILL.md:85`'s trailing parenthetical — "(S7's own duty here — deferring the reviewer's record-commit exception — was dropped by this task's own pre-commitment; see issue #309.)" — as an "orphaned S7 cross-reference" to delete. I disagree: this sentence carries no duty, no instruction and no action for anyone to perform — it is a deliberate provenance disclosure explaining why this bullet's own label narrowed from `S1/S2/S6/S7` (which every prior round's records show) to `S1/S2/S6`, and where S7's requirements went. The frozen Pre-commitment paragraph requires S7's **contract text** (the actual duty) removed "from... the orchestrator's side" — which is done, confirmed above — not that every mention of S7 having existed be erased; removing this sentence would strip exactly the kind of institutional-memory pointer this document's own established convention relies on at every other correction site (e.g. "corrected (T-1080 erratum b)", "Codex round-2 Major 2"), and would leave a future reader of `skills/run/SKILL.md` alone with no way to discover that S7 ever lived here or where to find its carried-forward requirements.

#### Adversarial findings

Not run this round — no Blocker/Major surfaced in the primary pass, so step 5's trigger condition was not met.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `skills/run/SKILL.md`'s trailing S7 disclosure parenthetical is an "orphaned cross-reference" that should be deleted | primary: minor | rejected | The sentence is a deliberate provenance disclosure (no duty, no action implied), consistent with the frozen Pre-commitment's requirement that the removed surface's text — not its history — be gone, and with this document's own established convention of citing IDs/history at correction sites; removing it would reduce documentation quality without closing any actual gap. |

#### Recommendation

- APPROVE → set the board's T-1080 entry to `READY_FOR_MERGE`. The pre-commitment drop executed faithfully against its own frozen text: S7's contract text is fully and cleanly removed from all three shipped files with no residue, the AC9 waiver fires genuinely (verified by direct reading of the check body, not merely trusted), no re-freeze occurred (intent-block markers untouched, same hash across all three rounds), S1–S6 and the three errata survive byte-intact, and the round-2 residual Minor is correctly folded in. Rounds 1 and 2's Major findings are preserved as follow-up issue #309's requirement list rather than silently lost.

#### Fast-follow disposition

- no fast-follow deferrals

## Round 4 (codex)

### Codex Review verdict: APPROVE
- Task: T-1080
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 4 files, +156/-70 lines (`git diff 9c0e984...HEAD`, engineer fix `0e0febf` + close-out/retro/board record commits + QA round-4 PASS `3aa4546`, on top of round-3's reviewed HEAD `9c0e984`)

This round is a narrow, single-question round: a CI red surfaced **post-APPROVE** on the branch tip — `tests/codex-skeleton-hygiene/run.sh`'s `agentmd-fence-mutation` case broke because round-1's pinned-read inline comments (my own Major #3 from round 1, verified fixed in rounds 2–3) sat *between* the `# T-107-step: codex` marker line and its command line inside the fence, violating that suite's marker→command adjacency contract. The fix relocates both notes to sit as plain prose immediately *above* their fences (dropping the `#` prefix, wording adjusted only for the new position) rather than as bash comments inside them. My job is to judge whether this relocation still satisfies round 1's original requirement, and whether anything previously passed broke — not to re-litigate the substance of S1–S6 or the S7 drop, both settled in prior rounds.

**The core question — does the relocation still bind the mechanism to the invocation recipe?** I read the diff myself (`git diff 9c0e984...HEAD -- agents/codex-reviewer.md`) and the full current text of step 3, not only the diff. At both sites, the relocated sentence explicitly names the block it modifies by direction — `agents/codex-reviewer.md:39`: "before running the **`codex` block immediately below**... substitute that `<scratch>` path for `<repo>` **in that block**"; `:63`: "the same `<scratch>`-for-`<repo>` substitution named at the primary read path above applies here too — **Codex then runs** `git diff`/`nl -ba` against the pinned scratch worktree." Both sentences sit with zero intervening content between them and the fence they modify, and both are self-disambiguating about direction (they say "below"/"then runs," never leaving a reader to guess whether the note describes the preceding `alloc` block or the following `codex` block). This is not a reversion to round 1's original defect — that defect was *disconnection* (the mechanism named only in a distant, general paragraph, with the actual bash blocks saying nothing at their own site); here the instruction stays tightly bound to the specific block it modifies, immediately adjacent, just outside the triple-backtick fence rather than inside it as a comment. I judge this a faithful, substance-preserving repair: the mechanism (`git worktree add --detach <scratch> <SHA>`, `<scratch>`-for-`<repo>`) and the serial-path-unchanged guarantee are both present verbatim in substance at both sites, and the wording changes ("before this invocation" → "before running the `codex` block immediately below"; "below" → "in that block") are exactly what relocating outside the fence requires, not a substantive loss.

**Mechanical confirmation, independently re-run rather than trusted to the hand-off.** `bash tests/codex-skeleton-hygiene/run.sh` → exit 0 at HEAD, and I read the `agentmd-fence-mutation` output directly: `PASS: agentmd-fence-mutation — site1 (codex-reviewer.md primary): each of the 5 mutations (assign/merge/delmarker/preline/connector), applied ALONE, is independently caught` and the matching `site2 (codex-reviewer.md adversarial)` line — both of the exact two sites this fix touches. `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh` → `17 passed, 1 failed, 1 skipped`, `FAILED: AC16` — I independently confirmed this is AC16's own documented merge-point staleness, not a new defect: AC16's frozen body states verbatim "this criterion is merge-point-scoped and is expected to go stale after merge... it must not be merge-ranged, re-based per rework round or otherwise kept evergreen," and the intervening `retro: sprint engineer-parallel` commit (`d01d125`) landed `.shell-team/retros/2026-08-17.md` on this same branch between rounds 3 and 4, outside AC16's frozen allow-list — exactly the criterion doing its own job. `bash bin/check-intent.sh` → `aligned: T-1080 v2 (3b0e76e69b728f894b755c1f7e6f45fb3a91ada4)` — same hash as every prior round; `git diff 9c0e984...HEAD --name-only -- .shell-team/specs/T-1080-depth-axis-contract.md` is empty, confirming the spec (and its frozen intent block) untouched this round too. `bash bin/check-prompt-sync.sh` → in sync. `bash tests/loop-guard/run.sh` → exit 0, 30/30.

**No regression on anything previously verified.** `grep -c record-commit-deferred agents/qa-verifier.md agents/codex-reviewer.md skills/run/SKILL.md` → `0/0/0` — S7 stays fully removed, the round-3 drop is untouched by this round's diff (which does not even touch `agents/qa-verifier.md` or `skills/run/SKILL.md` at all — confirmed via the diffstat, only `agents/codex-reviewer.md` among the shipped surfaces was edited). `pinned-read` (codex-reviewer.md 1, SKILL.md 1), `test-edit-deferred` (qa-verifier.md 1, SKILL.md 1) and `concurrent-review-window` (5/1/3/2 across the four shipped files) all present and unchanged from round 3.

A single foreground `codex exec` pass, prompted with the same four judgment questions the coordinator posed (does the relocation still satisfy the original requirement; is there directional ambiguity about which block the note modifies; did wording lose meaning; is marker→command adjacency now clean at both sites), returned an empty finding set (raw output: `.shell-team/reviews/T-1080-codex-primary.{txt,jsonl}`). I did not stop at that — I independently re-read the exact sentences at both sites myself (quoted above) specifically to check the directional-ambiguity question, since that is the one substantive risk a relocation like this could introduce, and confirmed both sentences are self-disambiguating by explicit forward reference. No Blocker or Major surfaced, so the adversarial pass is not triggered this round (step 5's condition).

The retro (`.shell-team/retros/2026-08-17.md`), close-out and board-record commits in this round's diff range are records, not deliverable, per the coordinator's own framing and this task's `### Verdict scope` — I did not audit them at adversarial depth; I spot-checked the board's own round-4 entries against my independent mechanical re-runs (the CI-red root cause, the 48/48 CI-wired list, the AC16 staleness account) and found them fully consistent with what I measured myself, with no need for further scrutiny at note depth.

#### Blockers

None.

#### Major

None.

#### Minor / nits

None.

#### Adversarial findings

Not run this round — no Blocker/Major surfaced in the primary pass, so step 5's trigger condition was not met.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes |

#### Recommendation

- APPROVE → set the board's T-1080 entry to `READY_FOR_MERGE`. The CI-red fix genuinely resolves the marker→command adjacency contract (independently re-run: `agentmd-fence-mutation` PASS at both sites) while preserving round 1's substantive requirement — the pinning mechanism stays tightly, unambiguously bound to the specific bash block it modifies, just outside the fence rather than inside it as a comment. Nothing previously verified (S1–S6, the S7 drop, the frozen intent block, the shipped serial path) regressed.

#### Fast-follow disposition

- no fast-follow deferrals
