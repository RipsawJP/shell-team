# Codex Review — T-1080

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1080
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 10 files, +464/-7 lines (base `cf643a9d7513bf745337b164f31619ebe4c31d0a` = `git merge-base feature/1079-tier2-judge HEAD`)

This is a `no-mechanism`, `user-visible: no` deliverable: seven role-contract amendments written as preconditions on a `concurrent-review-window` no default path enters today, plus three errata folded into `docs/loop-engineering/agent-concurrency.md`. Per the spec's own `### Verdict scope and per-artifact review depth`, `agents/codex-reviewer.md`, `agents/qa-verifier.md`, `skills/run/SKILL.md` and `bin/loop-guard.sh` get adversarial depth with no carve-out; `docs/loop-engineering/agent-concurrency.md` gets note depth (connective-prose findings only are downgraded there).

**Mechanical verification, independently re-run rather than trusted to the hand-off:** `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh .shell-team/specs/T-1080-depth-axis-contract.md` → `18 passed, 0 failed, 1 skipped` (AC19 SKIP by design). `bash tests/loop-guard/run.sh` → exit 0, `grep -c '^PASS:' <log>` → `30`. `bash bin/check-intent.sh .shell-team/specs/T-1080-depth-axis-contract.md .shell-team/todo.md` → `aligned: T-1080 v2 (3b0e76e69b728f894b755c1f7e6f45fb3a91ada4)`. I also independently re-derived the class-B re-freeze classification rather than accepting the board's transcription of it: `git show 966131e:.shell-team/specs/T-1080-depth-axis-contract.md > v1.md; bash bin/check-refreeze-class.sh v1.md .shell-team/specs/T-1080-depth-axis-contract.md` → `check-refreeze-class: class-b: differing line at intent-block index 80 ... exit 1` — confirms the board's `class-B` classification and the interventions file's `assumption-contradicted` entry independently. AC5's own base-relative-delta repair (HEAD = base+1, base read from the blob, step-6-region clause) is sound: it repairs a genuinely unsatisfiable frozen premise (base count measured 2, not 0, from already-merged T-1074/T-1077 `both-gates-green` lines) without touching the amendment's own substance. I have no objection to this re-freeze; the intent trajectory (v1 → engineer BLOCKED → intervention → pm-spec v2 → orchestrator re-verify → QA PASS) is intact and each hop is independently checkable.

A single foreground `codex exec … review --base <branch-point> --json` pass ran clean; its findings, checked against the actual files myself, are folded in below. Because it surfaced Major-candidate findings on the S6/S7 isolation surfaces, I ran the opt-in adversarial pass (devil's-advocate framing, targeted at the same two surfaces) — it independently confirmed all three, unprompted, at its own top severity. I read the full text of both `agents/qa-verifier.md` and `agents/codex-reviewer.md` (not only the diff) to confirm each finding against surrounding context before folding it in.

#### Blockers

None. Nothing here silently changes today's shipped serial behavior (verified above: `bin/loop-guard.sh`'s executable text is unchanged per AC7's comment-stripped comparison, and the fixture suite still passes 30/30; `skills/run/SKILL.md`'s numbered phase order is preserved; the reviewer's `READY_FOR_REVIEW` gate and its unpinned-working-tree read both survive verbatim as the serial default). Every gap below is confined to text that only takes effect inside a window nothing in this repository can enter today.

#### Major

- **S6's "no working-tree edit at all" reads as forbidding qa-verifier's own required board hand-off, which the surrounding contract still requires inside the same window.** `agents/qa-verifier.md:57` states: "so inside that window you make **no** working-tree edit at all, including this otherwise-permitted test-only one." Taken at face value this also covers the mandatory PASS/FAIL append to the board (`tasks/todo.md`) that the Output section (lines 29-53) requires every round, and that the *un-amended* T-073 rule (line 66, still carrying "apart from your own hand-off append to the board... `git status --short` shows nothing beyond that board edit") and the new S7 sentence right beneath it (line 67, "evaluate this clean-tree assertion... against that same pinned SHA") both presuppose still happens. The three sentences do not agree on what "no working-tree edit at all" excludes: S6 reads absolute, while T-073's still-unedited license and S7's own text both assume the board edit persists. This is exactly the "the amendment is descriptive where it needed to be prescriptive" class the spec's own verdict-scope section marks as full-severity, not the "connective prose" class that gets a pass. Fix: make the board hand-off append an explicit, named exception inside S6's own sentence (mirroring T-073's own phrasing), so a reader does not have to reconcile three separate bullets to recover the intended scope.
- **S7 defers the reviewer's one-file *commit* exception but not the other file writes the role's own write-boundary rule requires regardless of that exception.** `agents/codex-reviewer.md:159` only instructs "you do **not** exercise it [the commit exception]... Return the review record to the coordinating session instead." But the same write-boundary rule (same bullet, a few sentences earlier) and the "Save the raw Codex output" instruction (line 139) both mandate writing `T-XXX.md`, `T-XXX-codex-primary.{txt,jsonl}` (and, when triggered, `T-XXX-codex-adversarial.{txt,jsonl}`) into the shared reviews dir as an ordinary part of doing the review — independent of whether a commit follows. None of this is deferred by S7. If `codex-reviewer` runs this concurrently while `qa-verifier` is mid-Validate, these untracked writes land in the *same* shared checkout `qa-verifier`'s own T-073 clean-tree assertion inspects (`git status --short` "shows nothing beyond that board edit") — and an untracked review-record file is exactly something beyond that board edit. Deferring the git-commit half of the exception does not close the isolation gap S6/S7 exist to close; it only closes the git-history half of it. Fix: either instruct the reviewer to buffer these writes under `$TMPDIR` until both gates return (matching S7's own "return it to the coordinating session" framing already used for the commit), or explicitly widen QA's clean-tree assertion inside the window to tolerate the reviewer's own declared write paths.
- **The `pinned-read` instruction is prose only; the actual invocation recipe in step 3 is not amended to read anything other than the moving working tree.** `agents/codex-reviewer.md`'s new sentence says the reviewer should "pin your read to the commit SHA the launch brief names — checking out or reading that pinned commit rather than the moving working tree" — but the concrete `codex exec --sandbox read-only --cd <repo> review --base <base> --json -o <file>` block immediately following it, and the free-form adversarial-pass prompt later in the same step ("Review the diff between `<base>` and HEAD... Read the files yourself with `git diff`... do not expect any diff in this prompt"), are both untouched and both still literally target the live, moving working tree via `--cd <repo>`. This document's own convention elsewhere (T-097/T-107) is to keep the executable bash blocks in lockstep with the prose describing them; this amendment breaks that convention by leaving the mechanism entirely to inference. Fix: name the concrete mechanism (e.g. a `git worktree add --detach <scratch> <SHA>` used as `--cd` in place of `<repo>`, the same pattern this very task's own Blast-radius section already uses for a base-side read) in the actual invocation recipe, not only in the surrounding prose.

#### Minor / nits

- **Erratum (b)'s corrected `agent-tool-concurrent-launch` row omits the scope qualifier its own cited evidence carries, breaking this document's own established convention.** `docs/loop-engineering/harness-agent-concurrency.md` (the note this errata cites) explicitly discloses that its measurement covers only `subagent_type=general-purpose` agents and states verbatim: "unobserved: whether general-purpose Agent-tool concurrency extends to a plugin-role subagent type (e.g. a role bound through this repository's own `agents/*.md` definitions)" — and `qa-verifier`/`codex-reviewer` are exactly such plugin-role subagents, the very type S1-S7 concern. The corrected row in `agent-concurrency.md` states unqualified "this harness genuinely executes concurrent Agent-tool sub-agent invocations from one orchestrator session," with no scope caveat, even though sibling rows in the same section are careful to scope their own evidence (e.g. `background-agent-launch — provided — **evidence scoped to Bash-level backgrounding only**`). I judge this Minor rather than Major: `docs/loop-engineering/agent-concurrency.md` is note-depth per the spec's own pricing, the frozen **AC12** does not require a scope qualifier (only the two citation literals), and the caveat itself is prominently disclosed one hop away in the very note being cited — so nothing here is a false claim, only an incomplete one relative to this document's own convention. Worth a one-line tightening as a fast-follow.

#### Adversarial findings

A second `codex exec` pass, devil's-advocate framed at the S6/S7 isolation contract specifically (raw output: `.shell-team/reviews/T-1080-codex-adversarial.{txt,jsonl}`), independently surfaced all three Major findings above at its own top severity, unprompted beyond the general framing: (1) the board-hand-off/S6 conflict, citing the same three sites (Output section, T-073 rule, new S6 sentence); (2) the deferred-commit-vs-deferred-write gap, naming the same three artifact basenames; (3) the pinned-read-is-prose-only gap, citing both the primary invocation block and the adversarial-pass prompt as unamended. No new finding beyond these three.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | S6's "no working-tree edit at all" conflicts with QA's still-required board hand-off | primary: P1 (→ blocker by the normalization map); adversarial: "blocker" (own vocabulary), independently corroborated citing the same three sites | major | Real self-contradiction inside inert conditional text (the window nothing enters today), not a break in the shipped serial default (verified byte-unchanged above) — downgraded from blocker because no adopter-facing path is affected today, but must be fixed before the window is ever enabled. |
| 2 | S7 defers the commit exception but not the reviewer's other mandated writes into the shared checkout | primary: P1 (→ blocker); adversarial: "blocker", independently corroborated naming the same artifact basenames | major | Same reasoning as #1 — a genuine isolation-contract gap in not-yet-enabled text, not a defect in today's default path. |
| 3 | `pinned-read` is a prose instruction only; the concrete invocation recipe is unamended | primary: P1 (→ blocker); adversarial: "blocker", independently corroborated citing both unamended command sites | major | Same reasoning — real gap in what should be a prescriptive contract, but the mechanism is already demonstrated elsewhere in this task's own records (the Blast-radius section's `git worktree add --detach`), so this is a documentation-completeness gap rather than an unrecoverable design hole. |
| 4 | Erratum (b)'s `agent-tool-concurrent-launch` row omits the plugin-role scope qualifier its own cited source discloses | primary: P2 (→ major) | minor | `docs/loop-engineering/agent-concurrency.md` is note-depth per the spec's own verdict-scope pricing; frozen AC12 does not require a scope qualifier; the caveat is disclosed one hop away in the cited note itself — incomplete relative to this document's own convention, not false. |

#### Recommendation

- REQUEST_CHANGES → set the board's T-1080 entry back to `READY_FOR_ENG` with the three Major findings above as the required fixes: (1) name the board hand-off as an explicit exception to S6's "no working-tree edit at all" in `agents/qa-verifier.md`; (2) either buffer the reviewer's review-record and capture-file writes under `$TMPDIR` until both gates return, or explicitly widen QA's clean-tree assertion inside the window to tolerate them, in `agents/codex-reviewer.md`/`agents/qa-verifier.md`; (3) name the concrete pinned-read mechanism (e.g. a detached scratch checkout used as `--cd`) in the actual invocation recipe of `agents/codex-reviewer.md` step 3, not only in the surrounding prose. None of the three touches this spec's frozen intent block (the seven-surface table, the token vocabulary, the Goal/Non-goals/Input space) — all three are textual tightenings of the already-frozen amendments' own prose, not a re-freeze.

#### Fast-follow disposition

- Erratum (b)'s missing plugin-role scope qualifier on the `agent-tool-concurrent-launch` row — file-an-issue. Rationale: a real, if narrow, precision gap relative to this document's own convention and to the underlying evidence's own disclosed limits; worth a one-line fix, but not load-bearing for this task's own frozen AC12, and cheap enough to fold into the same rework pass that fixes the three Majors above without requiring its own round.

---
