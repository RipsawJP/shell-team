# T-1005 QA verification record

**Task**: T-1005 — the residual merge gate fires on consequence, not on the word "merge"
**Branch**: `feature/tuning-oversight-merge-gate` (base `develop`), HEAD `3967a50`
**Spec**: `.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md` (intent v1, `b5e7a08919313353979deac6f3f366f7eb38719b`)
**Verdict**: PASS → `READY_FOR_REVIEW`

## 1. Mechanical acceptance criteria

`bash bin/check-acs.sh .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`
run live (not trusted from any prior report): **16 passed, 0 failed, 0 skipped,
0 unrecognized.** Dry-run first (`--dry-run`) showed 0 unrecognized labels and
matched the live command set exactly.

## 2. Intent alignment

`bash bin/check-intent.sh .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md .shell-team/todo.md`
→ `aligned: T-1005 v1 (b5e7a08919313353979deac6f3f366f7eb38719b) matches
.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`.

## 3. Provenance conformance

`bash bin/check-provenance.sh .shell-team/provenance/T-1005.md` → `conformant:
.shell-team/provenance/T-1005.md (2 decision entries, 0 sentinel)`. Both
decisions read for grounding, not just structure: (1) AC7-clause and
AC5/AC6-paragraph placement in the "worth keeping" section rather than inside
the Fixed section or the example — grounded in the spec's Measured inventory
table and DP-3's own placement reasoning; (2) writing new prose as single
unwrapped physical lines to avoid a `grep -qF` anchor split across a
hand-wrapped line boundary — grounded in the spec's own declared out-of-scope
line-width item. Both are non-trivial, genuinely load-bearing decisions, not
boilerplate.

`bash bin/check-interventions.sh .shell-team/interventions/T-1005.md` →
`conformant: .shell-team/interventions/T-1005.md (0 entries, 1 sentinel)` — the
zero-entry sentinel form, correctly used since this task's board handling
carried no captured human intervention.

## 4. Board / prompt-sync / PII / commit-identity / diff-scoped checks

- `bash bin/check-handoff.sh templates/todo-template.md` → exit 0
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0
- `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`
- `bash bin/check-pii-shapes.sh --base develop` → `clean`
- `bash bin/check-commit-identity.sh --base develop` → `clean`
- `shellcheck --version` → 0.11.0, matching CI's pinned `SHELLCHECK_VERSION`.

## 5. Full CI suite, run locally in `.github/workflows/check-handoff.yml`'s own order

`shellcheck` with the exact CI argument list (49 paths) → exit 0, no findings.

Every suite step and every named dogfood one-liner in the workflow file was run
locally, in the file's declared order — the fixture suites (`tests/*/run.sh`
for check-handoff, check-contract, loop-guard, check-run, log-run, team-init,
discover-work, check-acs, check-design-note, goal-state, rework-digest,
check-retro, retro-inputs (+ `invariants.sh`), check-readme-version,
rollup-runs, rollup-track, consolidate-proposals, cluster-failures,
review-gate, close-out, check-prompt-sync, check-playbook, gen-playbook-blocks,
playbook-promote, team-paths, install, check-intent, check-provenance,
check-interventions, interventions-reminder, check-board-headings,
errexit-safe, codex-skeleton-hygiene, check-pii-shapes, check-commit-identity,
gitignore-raw-dumps), the three contract lints (`shell-team.contract.yaml`,
`loop-contract-template.yaml`, `goal.contract.yaml`), and the named dogfood
steps: `bin/retro-inputs.sh --base HEAD`, `bin/check-retro.sh
.shell-team/retros/*.md`, `bin/check-readme-version.sh README.md
README.ja.md`, `bin/check-prompt-sync.sh`, the three `team-paths.sh --get`
assertions (base/todo/specs), the per-file `check-interventions.sh` loop over
`.shell-team/interventions/*.md` (4 files found, all conformant, including
T-1005's own), `bin/check-pii-shapes.sh --base origin/develop`, and
`bin/check-commit-identity.sh --base origin/develop` (both matching the
workflow's `origin/${GITHUB_BASE_REF:-develop}` form, resolvable locally since
`origin/develop` exists on this checkout).

**Result: every single one of the above passed, 0 failures.** No suite is
broken, no environment quirk hit beyond what `.shell-team/test-recipe.md`
already documents (nothing new needed recording for this task, consistent with
the spec's Non-goals — no test-recipe entry was expected or added).

## 6. Human-lens checks (prose-only task, thin execution-detection surface)

**(a) en/ja side by side, section by section.** Read both files fully.
Every changed section (Fixed section's authority/interruption paragraph, the
"fewer interruptions" intro line, the example preamble, the split third
bullet + records-only exemption, the AC16 substitution note, the "worth
keeping" first bullet + records-only counter-case, the reason paragraph, the
second-order-reason paragraph, and the trap paragraph) says the same thing in
Japanese as in English — no drift found. The fenced blocks are confirmed
byte-identical mechanically (AC9); this read covers the surrounding prose,
which is the boundary the spec itself declares (AC9's disclosed boundary; the
correspondence table's matching info-only row).

**(b) Four sites in page order, both reading orders.** Read
Fixed → "fewer interruptions" intro+block → "worth keeping" (reason + trap) →
"more checkpoints" in that order, then re-read starting from the example. Both
orders reach the same conclusion: the Fixed section states authority (the loop
never merges on its own) and explicitly distinguishes it from interruption
(the tunable layer); the example's preamble uses the identical
"the loop never merges on its own" phrase; the split third bullet and the
"worth keeping" first bullet both use consequence framing (what-runs vs.
records-only) rather than the word "merge"; the reason and trap paragraphs
generalize the same criterion. No contradiction between the fixed-first and
example-first reading orders — this is the class the pre-change page failed,
and it now converges. Independently confirmed the diff hunks never touch the
Fixed section's `READY_FOR_REVIEW`/`READY_FOR_MERGE` claim or
"no personal setting relaxes it" (both substrings present, unmodified in
sense — the hunk at `docs/tuning-oversight.md:9-16` only replaces the
merge-framing sentence, leaving the completion-gate sentence's substance
intact).

**(c) "more checkpoints" stays a genuine opposite.** Confirmed byte-identical
to `develop` both by AC10 and independently via `git diff develop --
docs/tuning-oversight.md docs/tuning-oversight.ja.md`, whose highest hunk
(`@@ -85,12 +90,18 @@` / `@@ -71,9 +79,15 @@`) ends well before this
section's heading. Read in context: the surrounding page still presents it as
a parallel, live example ("For a team that wants to be consulted earlier:"),
not a deprecated one — its unconditional "Get agreement before pushing,
opening or merging a pull request, or filing an issue" bullet reads as an
intact, real option. Input space class 5 holds.

**(d) `git diff --name-only develop`** → exactly:
`.shell-team/interventions/T-1005.md`, `.shell-team/provenance/T-1005.md`,
`.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`,
`.shell-team/todo.md`, `docs/tuning-oversight.ja.md`, `docs/tuning-oversight.md`
— nothing else (the review record `.shell-team/reviews/T-1005.md` itself did
not exist yet at diff time; it is inside AC13's allow-list
`reviews/T-1005[^/]*` and this hand-off appends it plus the board sub-bullet
after this check was run, both permitted by the same allow-list).

**(e) DP-6 protected region byte-unchanged.** Confirmed by AC11 and
independently: `## Limits` starts at en line 105 / ja line 91; the highest
diff hunk in either file ends at old-file line ~96 (en) / ~79 (ja) — no hunk
enters the protected region in either file.

## 7. Independent mutation spot-check (adversarial fixture, not merely trusting the engineer's self-report)

Chose the AC7 class (branch-agnostic clause / `develop`-count lock) rather than
re-running all five the engineer already reported. Protocol: captured
`sha256sum` of `docs/tuning-oversight.md` before mutating, appended the
sentence "We merge into develop by default." to the reason-paragraph line via
`sed -i ''`, confirmed `grep -cF -- 'develop' docs/tuning-oversight.md` rose to
1 (AC7's negated half now fails as expected), restored the file from a
pre-mutation copy, and confirmed byte-identical restoration via `sha256sum`
match, `diff`, and a clean `git status --short`. Re-ran the full
`check-acs.sh` afterward: still 16/16 PASS. Working tree was never left
modified at any point outside this hand-off's own board/review edits — no
`git checkout`/`restore`/`reset` was used at any point, only file-copy
capture/restore.

## 8. Regressions / adjacent code paths

None found. This is a prose-only, two-file change with a fully-scoped diff
(§6d). No `bin/`, `agents/`, `skills/`, `templates/`, `.github/`, `README*`, or
`docs/distribution*` files are touched (confirmed by AC14 and independently by
`git diff --name-only develop -- templates/ bin/ agents/ skills/ .github/
README.md README.ja.md CLAUDE.md docs/distribution.md docs/distribution.ja.md`
returning empty).

## 9. Edge cases tried beyond the spec

- Verified fence balance and fence-language markers (`markdown`, `json`) are
  unchanged and structurally sound (6 fence lines / 3 blocks per file, matching
  AC9) — Input space class 6 (Markdown rendering) holds; no new fence type or
  HTML introduced.
- Verified the fenced "fewer interruptions" block reads as a self-contained,
  sensible `CLAUDE.local.md` document with no surrounding prose (Input space
  class 3) — it carries its own criterion sentence (the what-runs / records-only
  split) rather than only a bare path list.
- Confirmed `.shell-team/interventions/T-1005.md` and
  `.shell-team/provenance/T-1005.md` are both new, correctly-shaped records and
  not stray copies of another task's file (paths and task-id markers both
  checked).

**Applicability of the adversarial fixture synthesis checklist**: this task's
deliverable (`docs/tuning-oversight.md` / `.ja.md`) is prose-only with no
runnable claim — the fenced blocks are illustrative `CLAUDE.local.md` /
settings-file content the reader pastes elsewhere, not a canonical command
this page itself asserts works. Per the checklist's own scope rule, it does
not apply to this task; the human-lens checks in §6 and the mutation spot-check
in §7 are the applicable verification for this artifact class.

## Risk notes for reviewer

- None of my exploratory checks landed on an out-of-input-space finding this
  round — no `out-of-input-space` or `environmentally-unverified` items to
  disclose.
- The engineer's own five-mutation self-check (ja fence one-char, "more
  checkpoints" one-word addition, Limits-paragraph reflow, `develop`
  reintroduction, em dash → hyphen) was not independently re-run in full; one
  representative mutation from the same class family (AC7 / `develop`
  reintroduction) was independently reproduced and confirmed to fire, with a
  verified byte-identical restore. The other four classes are taken on the
  engineer's report plus the fact that all 16 criteria pass green today on the
  unmutated tree, which is consistent with (but does not independently prove)
  each specific mutation firing its named criterion.
- Semantic equivalence of the Japanese prose is a reviewer's read per the
  spec's own stated boundary (AC9's disclosed limit); my read in §6a found no
  drift, but this is not a mechanical guarantee.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1005
- Codex model: `gpt-5.6-sol` (`~/.codex/config.toml`'s configured model, via codex-cli 0.145.0; no per-event model field was echoed in either JSONL capture to cite verbatim)
- Verification mode: static-only
- Diff scope: 7 files, +891/-15 lines (`develop...HEAD`)

pm-spec の intent-freeze 訂正（AC9–AC12 の `mktemp` テンプレート明示化、pass/fail 開示の実測値への訂正）は board 記載（`.shell-team/todo.md:25`）どおりであることを、現在の spec 本文（AC9–AC12 の check 行、および L127–137 の pass/fail 開示文）と突き合わせて確認した。意味保存であり、board の記述は正確。

internal consistency verified / issue-canon match environmentally-unverified（relies on orchestrator's primary confirmation）— このタスクの根拠となる GitHub issue #26 は sandbox から `gh`/API 到達不可（TLS 検証エラー）のため直接確認できていない。spec の Problem 節・board の `source` サブバレットが issue #26 の内容を引用する形で自己完結しているため、それらの内部整合性は確認したが、issue 本体の正典テキストとの一致は未検証。

#### Blockers
- なし

#### Major
- `docs/tuning-oversight.md:64-66` / `docs/tuning-oversight.ja.md:53-55`（かつ `worth keeping` 節の対応箇所、`docs/tuning-oversight.md:93` / `docs/tuning-oversight.ja.md:82`）— "fewer interruptions" 例の consequence-based path list（`bin/`, `agents/`, `skills/`, `templates/prompt-blocks/`, `CLAUDE.md`, or the workflow）から `.shell-team/loops/shell-team.contract.yaml` が漏れている。`skills/run/SKILL.md:63` がこのファイルを `bin/loop-guard.sh` に渡し、そこで BUDGET/STOP・no-progress 判定という実行時の安全機構を制御していることを実際にファイルを開いて確認した（このリポジトリに実在する、日常的に merge され得る real-data-reachable な入力であり、spec の Input space の "Out-of-scope synthetic extremes" が対象とする「実データが作り得ない仮想的極端例」には該当しない）。"in this repository that means anything under ... or the workflow" という一文は `.github/workflows/check-handoff.yml`（単数形 "the workflow"）を指しており、`.shell-team/loops/` を含意しない。AC16 の補足文（"substitute the surfaces that execute in your own repository"）はこのブロックの外側にあり、かつ「別リポジトリへ持ち出すときの注意」であって、この repo 自身のインスタンス化リストが不完全である点を免責しない。修正は AC1 が固定するバイト列の変更を伴うため、T-1004 の v1→v2 前例と同じ形（pm-spec による再凍結、intent-hash の更新）が必要と判断する。
  - 提案: path list に `.shell-team/loops/`（または loop 契約ファイルを指す語）を追加する一文修正。

#### Minor / nits
- `docs/tuning-oversight.md:67-68` / `docs/tuning-oversight.ja.md:56-57`（and the "worth keeping" 節の counter-case）— "a board close-out ... needs no stop: nothing takes effect" という断定。`.shell-team/todo.md` は `skills/run/SKILL.md` のフェーズ判定や opt-in の interventions-reminder-hook の in-flight 判定が読む machine-read な状態であり、厳密には「何も取って代わらない」は言い過ぎ、という指摘自体は成立する。ただし、正当な close-out（両ゲートを通過済みのタスクの記録）が対象であれば、"take effect" する実質的な出来事はその前段の code-merge 側で既に人間の stop を経ており、close-out 自体は結果の記録に過ぎない、という spec の主張は擁護可能。加えて spec の Input space「Out-of-scope synthetic extremes」item 3 は「criterion 自体の adversarial な境界事例をこれ以上細かく争わない」ことを明示的に宣言し、「named case が本当に誤っていれば新規 issue」という受け皿を用意している。この一文をブロッキングにするほどの実害（code が別の形で実行される、という意味での consequence）は確認できなかったため minor とし、fast-follow に回す。
  - adversarial pass が追加で挙げた `bin/close-out.sh` 自身の設計（既存 flag が `READY_FOR_MERGE` であることを事前検証せず `READY_FOR_MERGE` へ書き換える）は、T-1005 の diff（prose のみ）とは無関係な既存ツールの話であり、本レビューの対象外として扱う（別途 issue 化の価値はあるかもしれない、という程度の観察に留める）。

#### Adversarial findings
- 上記 Major/Minor の両方を対象に "play devil's advocate" パスを実行。両方とも `still_valid: true` と判定され、Major の finding は独立に `skills/run/SKILL.md:63` → `loop-guard.sh` の grounding を再確認した上で severity `major`、"or the workflow" や AC16 の補足文では救済されないと結論。Minor の finding も `still_valid: true`・severity `major`（adversarial pass 内の評価）だったが、上記のとおり `bin/close-out.sh` 自身の未検証な事前条件チェックの欠如という diff 外の論点まで持ち込んでおり、この PR の prose の正しさそのものへの反証としては弱いと判断し、synthesis で minor へ収めた。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | consequence-based path list omits `.shell-team/loops/shell-team.contract.yaml` | primary: Codex-native `[P1]`（正規化表では blocker）／adversarial: major | major | 助言的なドキュメント文言の欠落であり、機構的に強制される gate を壊すものではない（criterion 自体の文章は正しく述べられている）。ただし repo 実在のガバナンス関連ファイルの実務上の欠落であり、merge 前修正を要すると判断し blocker ではなく major に据えた |
| 2 | "a board close-out ... needs no stop: nothing takes effect" の断定 | primary: Codex-native `[P2]`（正規化表では major）／adversarial: major | minor | spec の Input space「Out-of-scope synthetic extremes」item 3 が criterion 自体の adversarial な境界事例のこれ以上の深掘りを declined としており、「named case が誤っていれば新規 issue」という受け皿を明示している。ただしこの item は「実データが作り得ない仮想的極端例」の定義には厳密には合致しない（board close-out は日常的に real-data-reachable）ため、機械的な out-of-input-space 降格ではなく、正当な close-out は前段の code-merge で既に人間の stop を経ているという主張の擁護可能性に基づく通常の編集判断として minor に収めた |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` の T-1005 を `READY_FOR_ENG` に戻し、上記 Major の finding を紐付ける

#### Fast-follow disposition
- "a board close-out ... needs no stop" の断定を、正当な（両ゲート通過済みの）close-out に限定する形へ精緻化する件 — file-an-issue（理由: spec 自身が境界事例のこれ以上の深掘りを declined としており、この PR をブロックする実害は確認できなかったため、次スプリント以降の精緻化候補として issue 化が妥当）
- `bin/close-out.sh` が既存 flag を検証せずに `READY_FOR_MERGE` へ書き換える点（adversarial pass の副産物、T-1005 の diff とは無関係） — file-an-issue（理由: 別ツールの既存動作であり、この PR の scope 外だが、独立に検証する価値がある観察として記録）

Raw Codex session artifacts: `.shell-team/reviews/T-1005-codex-primary.jsonl` / `.txt`（review パス、`--base develop`）、`.shell-team/reviews/T-1005-codex-adversarial.jsonl` / `.txt`（devil's-advocate パス）。

---

## Delta verification — rework round 1 (post Codex REQUEST_CHANGES, v1→v2 re-freeze)

**Context since the record above**: Codex round 1 returned `REQUEST_CHANGES`
(one Major: the consequence-based instantiation list omitted
`.shell-team/loops/`, whose contract yaml `skills/run/SKILL.md` hands to
`bin/loop-guard.sh` — a real, executing surface of this repository). The
maintainer ratified a v1→v2 re-freeze (intent-hash
`99c5ebf28dde429dab814b7e93f4da2eb3f92caa`); the engineer then updated both
docs files at both sites (fenced block third bullet, byte-copied from the
spec; the "worth keeping" first bullet's enumerated list, both languages),
committed as `47caa42`, and returned the board flag to `READY_FOR_QA`.

**Verdict for this delta: PASS → `READY_FOR_REVIEW`.**

### D1. Mechanical criteria and intent, re-run

- `bash bin/check-acs.sh .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`
  → **16 passed, 0 failed, 0 skipped, 0 unrecognized.** (AC15 now passes on its
  own terms too, since `.shell-team/interventions/T-1005.md` has existed since
  the prior round — it was already passing before this delta as well.)
- `bash bin/check-intent.sh .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md .shell-team/todo.md`
  → `aligned: T-1005 v2 (99c5ebf28dde429dab814b7e93f4da2eb3f92caa) matches
  .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`.

### D2. Delta scope: `git diff acdfabc..47caa42 -- docs/`

Confirmed the delta is **exactly** the two-site insertion, in both languages,
and nothing else:

- `docs/tuning-oversight.md`: the fenced block's third bullet gains
  `` `.shell-team/loops/`, `` immediately before `` `CLAUDE.md` `` (one hunk),
  and the "worth keeping" first bullet's enumerated list gains the identical
  insertion at the identical ordinal position (one hunk). No other line in
  either file's diff.
- `docs/tuning-oversight.ja.md`: the same two insertions, same ordinal
  position (immediately before `` `CLAUDE.md` ``/`` `CLAUDE.md`、 ``), in the
  fenced block and in the "worth keeping" bullet's Japanese enumerated list.
  No other line in the diff.

**Fenced-block byte-identity (AC9 premise), re-verified independently**: fence
count is 6/6 in both files (unchanged); extracting every fenced block from
each file with the same `awk` toggle used by AC9 and comparing with `cmp`
confirms byte-identical output between `docs/tuning-oversight.md` and
`docs/tuning-oversight.ja.md` on the current tree.

**AC10/AC11/AC12 regions, re-verified against `develop` on the current
tree**: `git diff develop -- docs/tuning-oversight.md
docs/tuning-oversight.ja.md` shows the highest hunk in each file
(`@@ -85,12 +90,18 @@` en / `@@ -71,9 +79,15 @@` ja) ending with the "## Limits"
/ "## 限界" heading line appearing only as **unchanged trailing context**, one
line after the last actually-changed line in each hunk — the Limits region and
everything after it is untouched, and the "more checkpoints" section (en line
75, ja line 64 on the current tree) sits entirely below every hunk's range.
This matches AC10/AC11/AC12's own mechanical proof, re-derived independently
rather than trusted.

### D3. Semantic equivalence of the two changed lines (en/ja)

- Fenced-block third bullet: en `` anything under `bin/`, `agents/`, `skills/`,
  `templates/prompt-blocks/`, `.shell-team/loops/`, `CLAUDE.md`, or the
  workflow. `` / ja `` このリポジトリでは `bin/`、`agents/`、`skills/`、
  `templates/prompt-blocks/`、`.shell-team/loops/`、`CLAUDE.md`、ワークフローの
  配下です `` — same seven-item list, same insertion position (between
  `templates/prompt-blocks/` and `CLAUDE.md`), same meaning.
- "Worth keeping" first bullet: en `` **A merge that changes what runs** —
  anything under `bin/`, `agents/`, `skills/`, `templates/prompt-blocks/`,
  `.shell-team/loops/`, `CLAUDE.md`, or the workflow, in this repository's own
  terms. `` / ja `` **実行されるものを変えるマージ** — このリポジトリでは
  `bin/`、`agents/`、`skills/`、`templates/prompt-blocks/`、`.shell-team/loops/`、
  `CLAUDE.md`、ワークフローの配下です。 `` — same insertion, same ordinal
  position, same meaning. The engineer matched the Japanese insertion's list
  position to the English one rather than appending at the end; this is the
  correct read since AC4 pins the bullet's two anchor substrings but not
  internal item order, and a mismatched ordinal (present in one language,
  differently placed in the other) would have been a legitimate finding.
  `check-provenance.sh` confirms 3 decision entries (up from 2), and this
  exact ordinal-matching decision is the third, grounded correctly.

### D4. Diff-scoped checks, re-run against new commits

- `bash bin/check-pii-shapes.sh --base develop` → `clean`
- `bash bin/check-commit-identity.sh --base develop` → `clean`
- `bash bin/check-provenance.sh .shell-team/provenance/T-1005.md` →
  `conformant: .shell-team/provenance/T-1005.md (3 decision entries, 0
  sentinel)`
- `bash bin/check-interventions.sh .shell-team/interventions/T-1005.md` →
  unchanged, still `conformant (0 entries, 1 sentinel)`

### D5. Full CI-suite re-run: judged skippable, and why

**Judgment: skip the full CI-suite re-run for this delta.** Reason: `git grep
-rl "tuning-oversight" tests/ bin/` returns **no matches** — no fixture suite
or `bin/` script reads either doc file's content, so no suite's assertions can
react to this delta regardless of its bytes. The delta itself touches only
`docs/tuning-oversight.md` and `docs/tuning-oversight.ja.md` (plus this task's
own `.shell-team/` records) — confirmed via `git diff --name-only develop`
on the current tree, which lists the same six/seven allow-listed paths as
before (now plus `.shell-team/reviews/T-1005.md`, inside AC13's allow-list) and
nothing under `bin/`, `agents/`, `skills/`, `templates/`, or `.github/`. The
mechanical criteria that *do* pin this content (AC1, AC4, AC9) were re-run live
above (§D1–D2) and are the load-bearing checks for exactly this delta's shape
— a full re-run of suites with zero reachability to the changed bytes would
not add verification value proportional to its cost. The full suite (including
`shellcheck` on the exact CI argument list) was already run in full, green, in
the earlier round of this same QA pass, on a tree differing from the current
one only by this two-site, two-file textual insertion.

### D6. Regressions

None. The delta is strictly additive (one substring inserted at each of two
sites, in both languages) and does not remove, reorder, or alter any other
byte in either file (confirmed by the hunk-level diff in §D2).

---

### Codex Review verdict (round 2 — delta): APPROVE
- Task: T-1005
- Codex model: `gpt-5.6-sol` (`~/.codex/config.toml`'s configured model, via codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 7 files, +1055/-15 lines (`develop...HEAD`, includes round 1's records; round-2 delta itself is `dc06d59` + `47caa42` + `b8b5640`)

前提: round 1 の Major（consequence-based instantiation list から `.shell-team/loops/shell-team.contract.yaml` が漏れていた件）は、人間 ratify 済みの v1→v2 再凍結（`dc06d59`, intent-hash v2 `99c5ebf28dde429dab814b7e93f4da2eb3f92caa`）とその適用（`47caa42`）により解消済みと判断する。

#### (1) v1→v2 再凍結（`dc06d59`）のスコープ確認
`git diff dc06d59^..dc06d59` を自分でも読み、変更点が以下の3箇所のみであることを確認した: spec の canonical block 第三バレット（`.shell-team/loops/` を追加）、AC1 の check 行に対応する grep セグメント1個の追加、board の ledger サブバレット（`intent-hash (v1)` → `intent-ratified` + `intent-hash (v2)`）。それ以外の spec 本文・AC・Non-goals・Input space には触れていない。意味保存: criterion 自体（"whether the merge changes what runs"）は変わらず、this repository's own instantiation の欠落を埋めただけ。Codex round-2 primary パスも同じ結論（`dc06d59 re-freeze scope`: pass、`dc06d59 meaning preservation`: pass、正規化した intent block のハッシュが記録済み v2 ハッシュと一致）。

#### (2) 修正パッチ（`47caa42`）の適用確認
`git diff 47caa42^..47caa42 -- docs/` を読み、両サイト（fenced example block の第三バレット、"worth keeping" 節の第一バレット）× 両言語（en/ja）に、同じ序数位置（`CLAUDE.md` の直前）へ `` `.shell-team/loops/` `` が挿入されたことを確認した。他のバレット項目の削除・並べ替えはない。自分でも `bash bin/check-acs.sh` を再実行し **16/16 PASS**、`bash bin/check-intent.sh` は `aligned: T-1005 v2 (99c5ebf2…)`。AC9（fenced block byte-identity, 両ファイル6フェンス行）、AC10（"more checkpoints" 節が `develop` とバイト同一）、AC11（`## Limits` 〜 EOF が `develop` とバイト同一）を自分でも再現し、すべて PASS。Round-1 の Major が指摘した2箇所とも、狙った1行のみが変わっており、新たな欠落や保護領域の破壊は確認できなかった。

`47caa42` は docs 2箇所4行の他に `.shell-team/provenance/T-1005.md`（decision 1件追加）と `.shell-team/todo.md`（flag `REWORK`→`READY_FOR_QA`、engineer hand-off サブバレット追加）も変更しているが、これはこのプロジェクトの標準的な rework hand-off の形（AC13 の allow-list 内、必須アーティファクト）であり、懸念には当たらない。

#### (3) round-2 primary パスが新たに挙げた懸念（instantiation list の残存する不完全性）
Codex round-2 の primary パスは severity `major` として「`.shell-team/test-recipe.md`（engineer/QA が読んで従うことを要求される tracked な instruction source）、`.shell-team/specs/*.md`、`.shell-team/todo.md` もリストに漏れており、`templates/shell-team.contract.yaml` / `templates/test-recipe.md` も `templates/prompt-blocks/` の外にある」と指摘した。

この finding に対して devil's-advocate パスを追加実行し、自分でも独立に検証した:
- `templates/shell-team.contract.yaml` / `templates/test-recipe.md` は `bin/team-init.sh`（新規 repo への scaffold コピー元）以外どこからも直接読まれていないことを確認した（adversarial pass の grep 結果と一致）。**この repo 自身の現在の loop の実行には影響しない** — claim は無効。
- `.shell-team/specs/*.md` と `.shell-team/todo.md` は「consult されている」ことは事実だが、spec 自身の Input space「Out-of-scope synthetic extremes」item 3 が名指しで declined としている「a merge that edits a `check:` line in a frozen spec」のクラスに極めて近く、`board close-out` の扱い（round-1 で minor と判定済み）と同種の議論の蒸し返しである。
- `.shell-team/test-recipe.md` は round-1 の `.shell-team/loops/shell-team.contract.yaml` とは**異なるクラス**である: round-1 の欠落は `bin/loop-guard.sh` という shell script が機械的にパースして CONTINUE/STOP を判定する enforcement 入力だったのに対し、`test-recipe.md` は LLM エージェント（`agents/engineer.md:18`, `agents/qa-verifier.md:17`）が読んで「従う」ことを指示される advisory な instruction であり、既にリストにある `agents/` / `skills/` / `CLAUDE.md` カテゴリと同種（ただしパスとしては literal に列挙されていない）。実在する tracked ファイルであり、"named case が本当に誤っていれば新規 issue" という spec の受け皿には該当しうるが、round-1 と同じ扱い（再凍結を要する frozen-bytes 修正）を要求するほどの mechanical enforcement ではないと判断する。
- spec の Non-goals（"No exhaustive taxonomy of what changes what runs means in an arbitrary repository ... tells the reader to substitute their own executing surfaces"）と Input space の Out-of-scope item 3（"Escalating ever-finer cases against this prose is out of scope for this task; a genuinely wrong answer in a named case is a new issue"）は、まさにこの種の finding のエスカレーションを明示的に declined としている。

以上により、この新規 finding は severity **minor** に格下げし、T-1005 のこのラウンドの merge をブロックしない。round-1 の Major（機構的根拠が明確・repo に実在・re-freeze で是正済み）とは異なるクラスの、より弱いドキュメント完全性の指摘として fast-follow に回す。

#### Blockers
- なし

#### Major
- なし（round 1 の Major は解消を確認済み）

#### Minor / nits
- `.shell-team/test-recipe.md` が consequence-based instantiation list に literal には含まれていない件（新規、round-2 primary パス + adversarial パスの双方が「real な tracked instruction source ではあるが、round-1 の loop-guard.sh 級の mechanical enforcement ではない」と判定）。fast-follow へ。
- round-1 の2件の Minor（"a board close-out ... needs no stop" の断定の精緻化、`bin/close-out.sh` の事前 flag 未検証）は宣言済みのまま変更なし — round-1 の記載を参照。

#### Adversarial findings
- round-2 の新規 major 候補（instantiation list の残存する不完全性）に対して devil's-advocate パスを実行。`templates/shell-team.contract.yaml` / `templates/test-recipe.md` への言及は repo 実測により無効と判定（`bin/team-init.sh` 以外どこからも読まれない）。`.shell-team/specs/*.md` / `.shell-team/todo.md` への言及は spec 自身の Out-of-scope item 3 に該当し partially valid だが minor 止まり。`.shell-team/test-recipe.md` への言及のみ factually 有効だが、round-1 とは異なる（enforcement ではなく advisory な）クラスであり、v2→v3 再凍結には unproportionate と判定 — この結論は自分の独立判断と一致した。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Major（`.shell-team/loops/` 欠落） | round-1 primary: major／round-1 adversarial: major | resolved（`dc06d59`+`47caa42` で修正確認、re-open なし） | ratify 済み v1→v2、両サイト・両言語での修正をバイト単位で確認、AC1/AC9/AC10/AC11 すべて再現 PASS |
| 2 | instantiation list が `.shell-team/test-recipe.md` / `specs/` / `todo.md` / `templates/*` についても不完全 | round-2 primary: major／round-2 adversarial: partially valid, minor（templates/* の主張は invalid） | minor | spec の Non-goals（"No exhaustive taxonomy ... for an arbitrary repository"）と Input space Out-of-scope item 3（"Escalating ever-finer cases ... is out of scope; a genuinely wrong answer in a named case is a new issue"）に該当。round-1 の mechanical-enforcement 級の根拠（`bin/` script が実際にパースして STOP/CONTINUE を決める）とは異なるクラスであり、`templates/*` への言及は repo 実測（`bin/team-init.sh` 以外未読）により無効と確認した |

#### Recommendation
- APPROVE → `.shell-team/todo.md` の T-1005 を `READY_FOR_MERGE` に

#### Fast-follow disposition
- round-1 で宣言した2件（"a board close-out ... needs no stop" の精緻化、`bin/close-out.sh` の事前 flag 未検証）は round-1 の記載を参照 — 変更なし、orchestrator が READY_FOR_MERGE 記録時に issue 化予定
- `.shell-team/test-recipe.md` が consequence-based instantiation list に literal に含まれていない件 — file-an-issue（理由: `agents/engineer.md` / `agents/qa-verifier.md` が読んで従うことを要求する tracked な instruction source であり round-1 の懸念と部分的に同種だが、`bin/loop-guard.sh` 級の mechanical enforcement ではなく、spec の Non-goals が明示的にこれ以上のリスト拡張を declined としているため、この PR をブロックする実害はない。次回の instantiation list 見直し時にまとめて検討する候補として issue 化が妥当）

Raw Codex session artifacts (round 2): `.shell-team/reviews/T-1005-codex-round2.jsonl` / `.txt`（delta review パス）、`.shell-team/reviews/T-1005-codex-round2-adversarial.jsonl` / `.txt`（devil's-advocate パス）。
