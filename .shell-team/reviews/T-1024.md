# T-1024 — QA verification record

## QA verdict: FAIL

**Scope**: verify the frozen v1 spec (`.shell-team/specs/T-1024-check-line-mktemp-guard.md`,
intent-hash `87c496383df2cf019cff2983bb449e6ec1b8c05e`) against the engineer's hand-off
(commits `846eb21` audit document, `452ee89` recipe/board, `9c942f7` interventions sentinel),
`READY_FOR_QA` -> `READY_FOR_ENG` (rework).

### Mandatory conformance gates — all green

- `bash bin/check-acs.sh .shell-team/specs/T-1024-check-line-mktemp-guard.md` ->
  **10 passed, 0 failed, 2 skipped** (AC1–AC10 PASS, AC11/AC12 SKIP as predicted; no
  `CHECK_ACS_TIMEOUT` elevation used, per the spec's own statement that none is needed).
- `bash bin/check-intent.sh .shell-team/specs/T-1024-check-line-mktemp-guard.md .shell-team/todo.md`
  -> `aligned: T-1024 v1 (87c496383df2cf019cff2983bb449e6ec1b8c05e)`.
- `bash bin/check-intent.sh .shell-team/specs/T-1023-block-size-deferral-record.md .shell-team/todo.md`
  -> `aligned: T-1023 v2 (4434f8892603db06739d7902c525c566125dda02)` — the batch-mode neighbour
  integrity proof; unaffected by this task's board insertion.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1024.md` -> `conformant` (3 decision
  entries, 0 sentinel — all three read and judged below).
- `bash bin/check-interventions.sh --task T-1024 .shell-team/interventions/T-1024.md` ->
  `conformant` (0 entries, 1 sentinel) — judged structurally only.

### Independent re-derivation of the accounting (the completeness lesson)

Re-ran the pinned command myself (`grep -nE -- '^[[:space:]]*- check:.*mktemp' .shell-team/specs/*.md`):
**214 lines, 26 files** — matches the document's `- population: 214 — files: 26` exactly, and
matches every one of the 26 per-file `sites:` counts against the live per-file `cut -d: -f1 | sort |
uniq -c` breakdown, one for one, with no discrepancy.

Sample-classified 20+ population lines by DP2's ladder myself, across files and classes, and
compared against the document's rows — all matched:

- **T-1011:110** (the disclosed (c)-not-(b) call): `( cd "$d" && bash "$p/bin/check-run.sh" ... )` —
  bare `cd "$d"` reference, no path segment appended anywhere on the line. Correctly (c), not (b).
- **T-1004:220** (an (a) guarded site outside T-1020/T-1023, per the Assumptions caution):
  `d="$(mktemp -d ...)" && mkdir -p "$d/shim" ...` — the `&&` chain short-circuits before
  `"$d/shim"` is ever composed on `mktemp` failure. Correctly (a).
- **T-1000:242** (a, `&&`-chained, closed pre-attestation task): same shape, correctly (a).
- **T-1002** (11 sites: 10 `&&`-chained assignments correctly (a); line 298 is a mention-only
  occurrence inside a negative-grep character-class pattern, correctly (c)) — matches a=10 c=1.
- **T-1006:194** (`F="$(mktemp -d ...)"; ok=0; { mkdir -p "$F/fresh" ...`, `;` not `&&`, path
  composed) — correctly (b), matching the file's a=0 b=9.
- **T-1019** (all 3 sites, `;`-separated, path composed) — correctly (b) x3, matching the file's
  a=0 b=3, and this is the rule-2 strongest-not-apply row (AC5 already re-verifies its ground
  live against the source text).
- **T-1010** (8 sites, mixed): line 84 is `&&`-chained (a); the other 7 use `;` and compose a path
  (b) — matches the file's a=1 b=7 exactly.
- **T-1024's own 9 population lines** (AC1, AC3, AC4, AC5, AC6 = mention-only via filename/heading
  text, no real invocation, correctly (c); AC2, AC7, AC8, AC10 = real `$(mktemp -d ...) || exit 1`
  invocations, correctly (a)) — matches the file's own row (a=4, c=5) exactly, including AC6's
  deliberately assembled, non-self-matching token (confirmed: the only "mktemp" substring on
  AC6's own line is inside the filename mention, not the assembled `printf` token).

Reconciliation identity (36+168+10+0=214) holds under my own re-derivation, and every sampled
per-file row's `sites:`/`classes:` sum matches the live corpus. No discrepancy found anywhere I
looked.

### The three disclosed provenance decisions, judged against DP2's own words

1. **`&&`-chained assignment = guarded (class a).** DP2 rung (1) is written as a functional test —
   "a failure exits non-zero before any path composed from the resulting variable is used" — not a
   textual match against DP5's specific `|| exit 1` idiom. A failed `&&` left side genuinely
   satisfies that functional description. **Sound**, grounded in DP2's own wording; no rule-4
   escalation warranted. Impact (36 vs a smaller count under a stricter reading) disclosed
   honestly.
2. **Mention-only lines land in class (c), not off-ladder.** DP2 rung (3)'s own wording — "mktemp
   present with no temp-path variable composed into a later path" — covers the mention-only case
   literally (mktemp is present; nothing is composed). The alternative (inventing a fifth,
   off-ladder "not applicable" category) is explicitly rejected in the provenance reasoning, and
   correctly so — DP2 defines exactly four classes. **Sound.**
3. **Bare `"$d"` reference ≠ composed path.** DP2 rung (2) uses the verb "composed... into... a
   path," which itself implies concatenation, not bare reference; the Problem section's own worked
   example (`"$d/raw.txt"` expanding to `/raw.txt`) is a segment-appended-to-empty-string hazard,
   materially different from a bare `cd ""` (which fails outright rather than silently
   root-anchoring). **Sound**, and consistent with the audit document's own "composed path" Terms
   definition.

All three are non-trivial interpretive calls but are faithful readings of DP2's own frozen
language, disclosed with reasoning and (where relevant) quantified count impact — not
undisclosed rule-4 gaps. Flagged for the reviewer's own second look (S4 territory), not a QA
finding.

### AC11 discharge (the reading pass no grep can perform)

Read every one of the 26 `- file:` rows' `reason:` fields in full.

- Every reason is a statement about that file's own sites (site count, which sub-shape applies,
  which `&&`/`;` pattern), never a bare category restatement — several rows go further and
  explicitly *disclaim* the obvious misreading ("not because the sites are safe," "not because the
  shape is harmless," "not because guarding it again would be redundant," "not because touching it
  would be redundant"). No reason and no prose sentence overclaims: nothing asserts class (b) is
  harmless, nothing claims `bin/`/`tests/` were re-measured here, nothing claims the static scan is
  exhaustive over indirect invocations — the document's own "What this audit does not claim"
  section states all three disclaimers explicitly and the `- carve-out:`/`- limit:` lines are
  present exactly once each (AC1 mechanically locks this).
- The hit-list-read-by-eye finding is stated, with its own targeted re-check commands named
  (`(command|env)[[:space:]]+mktemp`, backtick-`mktemp`, `$MK`/`MKTEMP=` variable spellings) and
  the result ("none present in this population at this ref") — satisfies AC11's requirement that
  the finding be stated either way, not merely asserted.
- Every number in the document's prose (214, 26, 36, 168, 10, 0, and every per-file count) is
  either produced by the pinned command and re-derived by AC2, or is the reconciliation identity
  itself — none is transcribed from the tech-lead's or pm-spec's prose estimates (the document's
  closing bullet says so explicitly, and I independently re-derived the top-line numbers myself
  above rather than taking that statement on the producer's word).

**AC11: discharged, no findings.**

### Mutation self-checks — both reproduced independently

Reproduced on a fresh `git clone` under `$TMPDIR` (never on the tracked checkout):

1. **Delete one `- file:` row** (`T-1000-operating-conventions.md`'s row) — AC2 went red
   (`FAIL (exit 1)`); restored byte-identical (`diff` confirmed) — AC2 back to `PASS (exit 0)`.
2. **Shift one row's `sites:` by one** (`T-1001-retro-input-acquisition.md`, 1 → 2, `classes:`
   left unchanged) — both AC2 and AC3 went red; restored byte-identical — both back to `PASS`.

Matches the engineer's disclosed self-check results exactly. The real checkout (`git status --short`)
was untouched throughout — confirmed clean before and after.

### AC12 discharge — FAIL

AC12 requires: *"Every CI-wired step in `.github/workflows/check-handoff.yml` whose input this
task's diff actually touches was run locally, in workflow order, and **the hand-off names both
what was run and what was skipped with the reason**... A hand-off that claims the full list without
running it, or that **skips the applicable steps silently, fails this criterion**."*

I determined the applicable CI-wired steps myself from the workflow file, gated on AC10's proof
that `bin/`, `tests/`, `.github/**`, `templates/`, `README*`, `CHANGELOG*`, `CONTRIBUTING.md`,
`.shell-team/lessons.md` and the three sibling `docs/loop-engineering/` records are all
byte-identical to the base ref (so every step whose sole input is one of those is inapplicable):

**Applicable and re-run by me — all green:**
- `bash bin/check-handoff.sh "$B"` on the real board — PASS (also exercised inside AC8's own
  `check:` line).
- `bash bin/check-pii-shapes.sh --base origin/develop` ("check-pii-shapes on the PR diff") — clean
  (also re-checked at `--base 1e31600`, same result).
- `bash bin/check-commit-identity.sh --base origin/develop` ("check-commit-identity on the PR
  commits") — clean (also re-checked at `--base 1e31600`, same result). **This step is not
  exercised by any of this spec's own `check:` lines** — AC8 covers check-handoff, check-board-
  headings, check-pii-shapes and check-intent, but never check-commit-identity.
- The "Dogfood check-interventions — every committed interventions file... is conformant" loop,
  re-run over all 22 committed files including the new `T-1024.md` — all conformant.

**Inapplicable (AC10-grounded, correctly out of scope):** shellcheck lint, the board-template lint,
every `tests/*/run.sh` fixture suite, check-retro/retro-inputs dogfood, check-readme-version
dogfood, check-prompt-sync dogfood, check-playbook/gen-playbook-blocks dogfood, team-paths dogfood
— none of their inputs (`bin/`, `tests/`, `templates/`, `README*`, `.shell-team/lessons.md`,
retros) were touched by this diff.

**The finding: none of this is in the hand-off.** I read the full board entry (`git diff 1e31600
HEAD -- .shell-team/todo.md`), the provenance record, and the interventions record end to end.
The board's "engineer hand-off" sub-bullet states the `check-acs.sh` result (`AC11–AC12 SKIP as
predicted`) and stops there — it never names a single CI-wired step as run, never names one as
skipped, and never states an AC10-grounded reason for any omission. This is exactly the "skips the
applicable steps silently" shape AC12 names as a failing case, not a "claims the full list without
running it" shape (I verified independently that the applicable steps do in fact pass when run —
the gap is purely that the hand-off records none of this).

**This is a real, mandatory acceptance-criterion violation** — not exempt under the
out-of-input-space rule (AC violations are never exempted), and not something I can silently patch
myself (AC12's own deliverable is hand-off prose the engineer owns, not a test fixture).

### Board hygiene under batch mode

- `git diff --numstat 1e31600 -- .shell-team/todo.md` -> `15  0` (15 added, 0 deleted) — pure
  insertion, confirmed independently (also confirmed by grepping the diff for `^-` lines
  excluding the `--- a/...` header: zero).
- T-1023's `## Done` entry is untouched — confirmed by the zero-deletion diff above and by
  `check-intent.sh` reporting `T-1023 v2` still `aligned`.

### Regressions / adjacent code paths

None applicable — no `bin/`, `tests/`, or checker code was touched (AC10 locks this, mechanically
re-verified). The only artifacts are the new audit document, the test-recipe append, the board
entry and this task's own records.

### Edge cases tried

- **T-1011:110's bare-`cd` classification** — the one site in the entire population where the
  ladder's rung-2/rung-3 boundary is genuinely ambiguous on first read; independently re-derived
  and confirmed correct (see provenance judgment above).
- **T-1024's own AC6 self-non-membership** — verified the assembled `printf '$(mk%s -d ' temp`
  token does not itself contain the literal substring `mktemp`, so AC6's check line is correctly
  excluded from the population it measures (design intent per DP11, confirmed by direct
  inspection rather than taken on trust).
- This task's deliverable is an inventory document plus a recipe append — a prose/ledger artifact
  with no parser or attacker-influenced input surface of its own; the adversarial fixture synthesis
  checklist's classes 1–3 (payload-shape / regex-anchoring / CommonMark-parsing adversarial input)
  have no applicable execution surface beyond the ledger-grammar greps already exercised by AC2–AC3
  and the two mutation self-checks above. Class 4 (test-harness short-circuit) is exactly where
  this round's finding lives — the AC12 "was a command actually run and disclosed" check caught a
  real, undisclosed gap.

### Risk notes for reviewer

- The single finding this round is documentary (AC12), not a defect in the audit document's
  content, numbers, or classifications — every number and every sampled classification I
  independently re-derived matches the shipped document exactly.
- The three provenance decisions (§ above) are sound readings of DP2's own frozen wording but are
  non-trivial interpretive calls; worth a second independent read at review time even though I
  found no basis to escalate them under rule-4 myself.
- No `out-of-input-space` exemption was needed — the AC12 finding is a mandatory acceptance-
  criterion violation, not a synthetic extreme.

### Suggested next step

Add a short paragraph to the board's "engineer hand-off" sub-bullet (or a new sub-bullet directly
under it) naming, in workflow order: the four applicable CI-wired steps and their result (all
green, per the list above), and the AC10-grounded reason each inapplicable step was skipped
(`bin`/`tests`/`.github`/`templates`/`README*`/`CHANGELOG*`/`CONTRIBUTING.md`/
`.shell-team/lessons.md`/the sibling `docs/loop-engineering/` records are byte-identical to the
base ref, so no CI-wired step whose sole input is one of those has anything new to check). No
code, document content, or classification needs to change — this is the cheapest possible rework
round.

---

## QA round 2 (2026-08-04)

### QA verdict: PASS

**Scope**: verify the round-1 rework responding to my AC12 FAIL (`b4c032f`, board-only), against
the still-frozen v1 spec (intent-hash unchanged: `87c496383df2cf019cff2983bb449e6ec1b8c05e`),
`READY_FOR_QA` -> `READY_FOR_REVIEW`.

### Mandatory conformance gates — unchanged, re-confirmed live

- `bash bin/check-acs.sh .shell-team/specs/T-1024-check-line-mktemp-guard.md` -> **10 passed, 0
  failed, 2 skipped** — identical to round 1 (the rework touched only `.shell-team/todo.md`, and
  no criterion inspects the board's own sub-bullet prose beyond AC8's numstat/lint checks, already
  re-run below).
- `bash bin/check-intent.sh .shell-team/specs/T-1024-check-line-mktemp-guard.md .shell-team/todo.md`
  -> `aligned: T-1024 v1 (87c496383df2cf019cff2983bb449e6ec1b8c05e)` — unchanged.
- `bash bin/check-intent.sh .shell-team/specs/T-1023-block-size-deferral-record.md .shell-team/todo.md`
  -> `aligned: T-1023 v2 (4434f8892603db06739d7902c525c566125dda02)` — unchanged, the neighbour's
  frozen record still validates.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1024.md` -> `conformant` (3 decision
  entries, 0 sentinel) — byte-unchanged since round 1 (confirmed: `git diff e09b58f..HEAD --
  .shell-team/provenance/T-1024.md` is empty).
- `git diff e09b58f..HEAD --stat` -> `.shell-team/todo.md | 3 +-` only — the audit document, the
  test-recipe append, the spec, the provenance record and the interventions record are all
  byte-identical to my own round-1 commit; the rework touched nothing else.
- Board hygiene: `git diff --numstat 1e31600 -- .shell-team/todo.md` -> `17  0` (pure insertion,
  0 deletions re-confirmed by grepping for `^-` lines excluding the diff header).

### 1. Does the new sub-bullet satisfy AC12's contract?

Read the full text of the new "engineer hand-off, rework round 1" sub-bullet. It names, in a
numbered list, the four applicable CI-wired steps and their result, plus the AC10-grounded reason
every other listed CI-wired step is inapplicable (naming the same byte-identical-surface set AC10
locks). This is a genuine named ran-list + reasoned skip-list, not a bare "SKIP as predicted" note
— it resolves round 1's finding on its own terms.

**Spot-verified all four steps myself, live** (the task asked for at least two; I ran all four,
since re-running is nearly free once the environment is already set up):

- `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` -> exit 0, matches "PASS".
- `bash bin/check-pii-shapes.sh --base develop` (the literal invocation named in the sub-bullet;
  local `develop` resolves to `7a64def`, same as `origin/develop`) -> `clean`, matches "clean".
- `bash bin/check-commit-identity.sh --base develop` -> `clean`, matches "clean".
- The check-interventions dogfood loop, re-run over all 22 committed `.shell-team/interventions/
  *.md` files -> all `conformant`, matches "all conformant".

Measurements, not memories — none of these four results were taken on the engineer's report; I
ran each command myself against the current tree.

**One nit, not a FAIL driver.** AC12's text reads "...was run locally, **in workflow order**, and
the hand-off names both what was run and what was skipped..." The most natural parse binds "in
workflow order" to the execution clause ("was run locally, in workflow order"), not to the
narrative enumeration clause that follows it — but read as a check on the *prose* order too: the
sub-bullet's own numbering (1 check-handoff, 2 check-pii-shapes, 3 check-commit-identity, 4
check-interventions) does not match the workflow file's actual step order (`.github/workflows/
check-handoff.yml` line numbers: check-handoff at line 34, check-interventions dogfood at line
200, check-pii-shapes-on-diff at line 222, check-commit-identity-on-commits at line 231 —
check-interventions belongs 2nd, not 4th). All four steps were still correctly identified,
correctly run, and correctly reported; nothing about *which* steps ran or *what* their results were
is wrong — only the enumeration's internal ordering in the prose. AC12's own named failure modes
are "claims the full list without running it" and "skips the applicable steps silently" — this
enumeration-order slip is neither. Judged a cosmetic nit, not a criterion violation; flagged for
the reviewer below rather than escalated to a third rework round over prose ordering.

**AC12: discharged.**

### 2. No provenance entry added — judged sound, no disagreement

The engineer's reasoning ("no new non-trivial decision was made in fixing this") holds up under
scrutiny, including against the T-1015 precedent it cites. I checked the two places where a
judgment call could plausibly have been hiding:

- **Which four steps are "applicable."** This is a deterministic consequence of AC10's own
  byte-identical-surface lock (already frozen, already mechanically re-verified this round and
  round 1) — not a two-viable-alternatives choice. I independently derived the same four-step set
  in round 1 before the engineer's rework existed, by the same method (cross-referencing the
  workflow file against AC10's untouched-surface list), and arrived at the identical set. No
  discretion was exercised in drawing that boundary.
- **The choice of `--base develop` for the two ad-hoc reproductions.** Unlike T-1015's round-2
  precedent (a genuine two-option scope choice — the full 59-step list vs. a T-1010-precedent-
  reasoned narrower subset, both textually licensed by an ambiguous frozen AC) — here there is no
  ambiguity to resolve: the workflow's own literal invocations are `--base "origin/
  ${GITHUB_BASE_REF:-develop}"`. Faithfully reproducing "check-pii-shapes on the PR diff" and
  "check-commit-identity on the PR commits" **as CI actually runs them** has exactly one correct
  answer (`develop`); using this task's own `1e31600` pin instead would have been unfaithful to the
  named CI step, not an equally-valid alternative reading. (I re-ran both at `--base 1e31600` too,
  in round 1, and the result happened to be identical either way in this instance — but that
  doesn't retroactively make `develop` a discretionary choice; it was the only faithful one.)

Neither point clears the bar T-1015's precedent actually sets (a genuine, textually-licensed
two-option choice with a consequence). **I agree with the omission.**

### 3. Regressions / adjacent code paths

None — confirmed via `git diff e09b58f..HEAD --stat`: only `.shell-team/todo.md` changed, and
within it only the status flag (one line) and one new sub-bullet (one line) were added; nothing
else in the board, the audit document, the recipe, the spec, or the provenance/interventions
records moved.

### Edge cases tried

- Re-ran all four named steps against a base other than the one the sub-bullet names
  (`--base 1e31600` instead of `--base develop`) to confirm the result is not base-sensitive in
  this instance (it isn't — both bases report clean/PASS), which is what let me confidently judge
  point 2 above as "no ambiguity to resolve" rather than merely "no ambiguity observed once."

### Risk notes for reviewer

- **Nit, not blocking**: the round-1 rework's hand-off sub-bullet numbers its four named steps in
  a different order than they appear in `.github/workflows/check-handoff.yml` (check-interventions
  belongs 2nd, listed 4th). All four are correctly identified and correctly reported; only the
  prose enumeration order is off. Not escalated to a further rework round.
- No `out-of-input-space` exemption was needed this round — no candidate finding depended on an
  out-of-scope synthetic extreme.

---

## Codex Reviewer round 1 (2026-08-04)

### Codex Review verdict: APPROVE
- Task: T-1024
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 7 files, +674/-0 lines, base `1e31600`

One synchronous foreground `codex exec --sandbox read-only --cd . review --base 1e31600 --json`
pass, Codex reading the real files itself. Raw capture: `.shell-team/reviews/T-1024-codex-primary.{txt,jsonl}`.
Because the primary pass surfaced findings I normalize to at least one Major (below), the opt-in
devil's-advocate adversarial pass fired: `.shell-team/reviews/T-1024-codex-adversarial.{txt,jsonl}`
(a free-form prompt targeting the two primary findings plus an open hunt over the ladder/rule
application).

Independently re-derived, myself, before reading Codex's output: the pinned command
(`grep -nE -- '^[[:space:]]*- check:.*mktemp' .shell-team/specs/*.md`) → 214 lines / 26 files,
matching the document exactly; `check-acs.sh` → 10 PASS/0 FAIL/2 SKIP; both `check-intent.sh`
ledgers (T-1024 v1 `87c496383df2cf019cff2983bb449e6ec1b8c05e`, neighbour T-1023 v2) `aligned`;
`.shell-team/todo.md` and `.shell-team/test-recipe.md` diffs against `1e31600` both pure insertion
(`18 0` / `18 0`); zero lines touched under `bin`, `tests`, `.github`, `agents`, `skills`,
`templates`, `README*`, `CHANGELOG*`, `CONTRIBUTING.md`, `.shell-team/lessons.md`; and rule-1's
"zero CI reachability" ground re-verified directly against `.github/workflows/check-handoff.yml`
— `check-acs.sh` is invoked only against `tests/check-acs/run.sh`'s fixtures (line 72) and by
shellcheck (line 29), never against a real `.shell-team/specs/*.md` path.

Spot-re-sampled a subset of QA's classifications from the primary sources myself (T-1011:110's bare
`cd "$d"`, T-1004's five `&&`-chained sites, T-1010:84 vs T-1010's `;`-separated sites, T-1005's
four sites) — all matched the document's rows exactly.

#### Blockers

None.

#### Major

None survive independent re-check (see Synthesis audit ledger — two Codex-native P2/major-mapped
findings and two adversarial-pass "major" findings were all downgraded to Minor or rejected outright,
with reasons).

#### Minor / nits

- `docs/loop-engineering/check-line-mktemp-guard-audit.md:36` — the Classification section's "Live
  finding" sentence ("no population line in this corpus carries more than one real invocation…
  checked directly") is factually false. `.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`
  lines 270 (2 invocations), 283/291/298 (4 invocations each) each carry multiple real `mktemp`
  invocations — verified directly by reading the lines. Every invocation on each of those four
  lines is individually `&&`-guarded, so the weakest-invocation tie-break still resolves to class
  (a) either way — T-1005's row (`a=4`), the corpus-wide `36/168/10/0` split, and the 214-line
  reconciliation are all unaffected. Suggested fix: correct the sentence to state that T-1005 has
  four multi-invocation lines, all uniformly class (a), so the tie-break's *outcome* was never
  tested by this corpus rather than claiming no multi-invocation line exists.
- `.shell-team/specs/T-1024-check-line-mktemp-guard.md` DP2 rung (1) / `docs/loop-engineering/check-line-mktemp-guard-audit.md`'s
  Terms section — DP2 rung (1)'s literal wording ("is every mktemp invocation on the line
  guarded…") is vacuously true for a zero-invocation (mention-only) line under a strict reading,
  which would place all 9 mention-only sites in class (a) rather than (c) via rung (3) — a reading
  that would move the corpus totals to roughly `a=45 b=168 c=1 d=0`. The audit's own Terms section
  ("mktemp present (as an invocation or a mention)") and the explicit statement that "a mention has
  nothing to guard, by construction" make clear this is the substantively correct, intended
  reading, and I agree the current classification (mention → class c) should stand — but unlike the
  spec's other three disclosed provenance decisions (each of which names its rejected alternate and
  quantifies the impact), this fourth interpretive call was never disclosed as such. Suggested fix:
  add a fourth `.shell-team/provenance/T-1024.md` entry disclosing this reading and the alternate it
  rejects, for completeness — no reclassification needed.
- `.shell-team/todo.md`'s rework-round-1 hand-off sub-bullet numbers its four re-run CI-wired steps
  1–4 in an order that does not match `.github/workflows/check-handoff.yml`'s actual step order
  (check-interventions runs 2nd in the real workflow, listed 4th in the prose). Same finding QA
  already surfaced and judged non-blocking in round 2 — I agree: AC12's own named failure modes
  ("claims the full list without running it" / "skips the applicable steps silently") are about
  disclosure honesty, not narrative enumeration order; every step's identity and result is correctly
  reported, and both QA and I independently re-ran all four steps ourselves with matching results.
  A literal reading of AC12's "run… in workflow order" binds to execution, not to how the hand-off
  lists them afterward, and nothing in the record contradicts that the actual local execution
  happened correctly.
- `docs/loop-engineering/check-line-mktemp-guard-audit.md`'s closing "What this audit does not
  claim" bullet ("every number in this document … is produced by the pinned command … and
  re-derived live by AC2") is imprecise for the class-a/b/c/d counts specifically: the pinned `grep`
  produces the population hit list, not the classification counts, which are a manual application
  of DP2's ladder; AC2 mechanically checks that those counts sum correctly against the live
  population and file totals, not that each count is independently regenerated. DP10 itself already
  permits "attributed at its own claim site" as an alternative to "produced by the pinned command,"
  and the Classification section is exactly that claim site (it discloses the ladder and the manual
  application), so this is a wording-precision nit rather than a substantive overclaim — no
  correction is required for AC1/AC11 to remain satisfied, but the sentence would read more
  precisely as "produced by the pinned command, manually classified by the ladder, and cross-checked
  for arithmetic consistency by AC2."
- `docs/loop-engineering/check-line-mktemp-guard-audit.md`'s Population section's targeted re-check
  for indirect `mktemp` forms conflates two different limits: `command mktemp` / `env mktemp` *are*
  visible to the population regex (the literal substring `mktemp` is present on the line), so they
  are not actually an instance of the population-invisible class the `- limit:` line describes —
  they would only need extra syntax handling at the invocation/mention boundary, not escape
  detection entirely. Zero real corpus instances of this shape exist today (independently
  re-verified), so there is no live consequence; a future corpus entry using this exact prefix form
  would still be caught by DP1's population regex, just possibly mis-triaged at the classification
  step rather than missed outright.

#### Adversarial findings

The devil's-advocate pass, prompted specifically to stress-test the two primary findings and to hunt
independently for anything a first pass could plausibly miss, surfaced five additional items beyond
confirming the primary two: (1) the DP2-rung-1 vacuous-truth ambiguity above (initially tagged
`major`, downgraded — see ledger); (2) a claim that rule-2 (T-1019's strongest-not-apply ground)
should also apply to other `merge-point-scoped` sites elsewhere in the corpus (e.g.
`.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`'s own AC10–AC12, which do carry the
same "merge-point-scoped… do not widen or re-derive" language) — **rejected**, see ledger: DP9
explicitly scopes rule-2's disclosure requirement to exactly one "strongest-not-apply" site, and DP4
itself names T-1019's AC11 as "the issue's highest-severity site," so the frozen spec deliberately
picked one exemplar rather than requiring the engineer to tag every matching site; broadening it
would be the engineer re-opening a decision rule the spec forbids re-opening. (3) the AC2-overclaim
precision nit above; (4) the class-(d)-is-structurally-unreachable observation (see below); (5) the
command/env-prefix nit above. Confirmed independently: no line in the population carries invocations
of mixed severity, so DP2's weakest-invocation tie-break, while textually present, is never actually
exercised by this corpus regardless of how the multi-invocation lines are read.

One further observation, not a finding requiring rework: class (d) is structurally unreachable given
DP1's population definition (bare-substring match on `mktemp`) combined with DP2's own rung (3)
wording ("mktemp present with no temp-path variable composed into a later path") — every population
line, by definition, has `mktemp` present, so any line that fails rungs (1)–(2) necessarily satisfies
rung (3)'s catch-all. `class-d-other: 0` is therefore a guaranteed consequence of the frozen ladder's
construction, not an artifact of how carefully this population was read. This is a property of DP1/DP2
as pm-spec froze them (this task's engineer had no discretion to redesign the ladder), so it is not
actionable inside this task — noted for visibility, matching the review's own question about whether
`d=0` "flattered the ladder": it did not, because no other outcome was ever reachable.

Issue-canon note: internal consistency verified / issue-canon match environmentally-unverified
(relies on orchestrator's primary confirmation) — this sandbox has no GitHub access (`gh issue view`
fails with a TLS error) to independently re-read issue #109's literal text against the spec's
Problem-section paraphrase.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Audit doc:36 "no line carries >1 invocation" claim is false (T-1005:270/283/291/298) | primary: major (Codex P2) | minor | Falsifies a self-verification sentence, but every affected line is uniformly guarded either way — T-1005's `a=4`, the corpus `36/168/10/0` split and the 214-line reconciliation are all unaffected; a prose-accuracy fix, not a count or decision defect. |
| 2 | `.shell-team/todo.md` CI-step enumeration order vs. workflow order | primary: major (Codex P2) | minor | Matches QA's own round-2 non-blocking judgment: AC12's named failure modes are about disclosure honesty (claims-without-running / silent-skip), not narrative order; every step's identity and result is independently reproduced correct by both QA and me. |
| 3 | DP2 rung-1 vacuous-truth reading could move 9 mention-only sites from (c) to (a) | adversarial: major | minor | Current classification (mention → c) is the substantively correct, intended reading per the audit's own Terms section ("a mention has nothing to guard, by construction"); no recount needed. Gap is a missing 4th provenance disclosure (the other 3 interpretive calls were disclosed with their rejected alternates; this one was not), not a live defect. |
| 4 | AC2 "produced by pinned command and re-derived live" overclaim for class counts | adversarial: major | minor | DP10 permits "attributed at its own claim site" as an alternate provenance path, and the Classification section is that claim site; a wording-precision nit, not a substantive overclaim — AC1/AC11 remain satisfied as written. |
| 5 | rule-2 should also cover T-1005's (and others') `merge-point-scoped` sites | adversarial: unlabeled (folded into major finding text) | rejected | DP9 explicitly limits rule-2's disclosure requirement to exactly one "strongest-not-apply" site, and DP4 itself names T-1019 as that exemplar; broadening it is the engineer re-opening a frozen decision rule, which the spec forbids. Decision outcome (not-apply) is unaffected for every file regardless. |
| 6 | class (d) = 0 — real or flattering? | adversarial: minor (structural-unreachability observation) | minor (non-actionable observation) | Confirmed independently: DP1+DP2's own wording makes rung (3) a catch-all for anything with `mktemp` present, so (d) can never be populated under this frozen ladder — a property of the frozen spec's design, not of how the engineer read the corpus; not actionable inside this task. |
| 7 | `command`/`env` mktemp prefix conflated with population-invisible forms | adversarial: minor | minor | Zero real corpus instances; `command mktemp`/`env mktemp` are population-visible (literal substring present), so the audit's own targeted re-check already covers this case correctly in practice even if the `- limit:` line's framing is imprecise. |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition

- file-an-issue: a small prose-accuracy pass on `docs/loop-engineering/check-line-mktemp-guard-audit.md`
  — correct the false "no line carries more than one invocation" claim at the Classification section
  (item 1 above), and add a fourth `.shell-team/provenance/T-1024.md` entry disclosing the DP2-rung-1
  mention-only interpretive call and its rejected alternate (item 3 above). Rationale for filing
  rather than fixing now: neither change alters any count, classification, or decision in the shipped
  document — reopening a third rework round over prose accuracy in a document whose numbers,
  identity, and decisions all independently re-verify correct is disproportionate to this task's own
  stated calibration ("inventory + one document… NOT verification-mechanism work"); a follow-up PR is
  the cheaper, correctly-scoped venue.
- won't-fix: the CI-step enumeration order in the board's rework-round-1 hand-off sub-bullet (item 2
  above) — cosmetic prose ordering only; AC12's own named failure modes don't cover it, and the
  underlying execution results are independently confirmed correct by both QA and me. Not worth a
  further board edit.
- won't-fix: the `command`/`env`-prefix nuance in the audit document's Population/Terms sections
  (item 7, adversarial finding 5) — zero real corpus instances today, already effectively covered by
  the document's own targeted re-check; the imprecision has no live consequence.
