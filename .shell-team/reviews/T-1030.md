# T-1030 — QA verification record

## QA verdict: PASS

**Scope**: verify the engineer's hand-off (commits `9b363ec` agent file,
`f752b39` board + provenance, plus the orchestrator's interventions-sentinel
commit `523a3f2`) against the frozen spec
(`.shell-team/specs/T-1030-reviewer-board-write-boundary.md`, intent-hash v1
`d0f73437565c9211c998e78a863948eb23e27972`), branch
`feature/134-reviewer-board-write-boundary` (base `53955c5`),
`READY_FOR_QA` -> `READY_FOR_REVIEW`. Every check below was re-run live against
the real checkout; none of the engineer's reported numbers or the hand-off's
mutation outcomes were taken on trust.

**Lens**: the deliverable is four lines of prose spliced into one agent file
(`agents/codex-reviewer.md`) — no `bin/` script, checker, parser or state
tracker is written or extended (AC9: `bin/`, `tests/`, `templates/`, `skills/`,
`docs/`, `.github/` all byte-identical, tracked and untracked). `agents/*.md`
is agent-read guidance, not a documented runnable procedure a user executes
verbatim, so it does not fall under the QA prompt's class-4 "canonical
runnable procedure" either. Per qa-verifier's own applicability paragraph, the
**Adversarial fixture synthesis checklist is structurally inapplicable** here.
My lens is empirical/execution-based: re-ran every `check:` line live,
independently re-derived the claim-fidelity restatements against their named
sources (issue #134, commit `9d82a49`, the tech-lead's DP-1/DP-2/DP-3/DP-5
measurements) by reading the sources myself, and reproduced two of the
engineer's seven AC12(b) mutations independently on `$TMPDIR` scratch copies.

### Mandatory conformance gates

- `check-acs.sh .shell-team/specs/T-1030-reviewer-board-write-boundary.md`
  -> **11 passed, 0 failed, 1 skipped (AC12, runtime by design)** — exact
  match to the required 11/0/1 shape.
- `check-intent.sh .shell-team/specs/T-1030-reviewer-board-write-boundary.md .shell-team/todo.md`
  -> `aligned: T-1030 v1 (d0f73437...) matches ...`
- `check-intent.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/todo.md`
  -> `aligned: T-1027 v1 (828a7194...)` — stacked neighbour's ledger undisturbed.
- `check-intent.sh .shell-team/specs/T-1028-class-m-refreeze.md .shell-team/todo.md`
  -> `aligned: T-1028 v2 (465f91ff...)` — second stacked neighbour's ledger undisturbed.
- `check-intent.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md .shell-team/todo.md`
  -> `aligned: T-1029 v2 (aa47eaa0...)` — third stacked neighbour's ledger undisturbed.
- `check-provenance.sh .shell-team/provenance/T-1030.md` -> `conformant: ... (3 decision entries, 0 sentinel)`.
- `check-interventions.sh --task T-1030 -- .shell-team/interventions/T-1030.md`
  -> `conformant: ... (0 entries, 1 sentinel)` — judged structurally only, per
  this repo's rule (not whether "no interventions" was the correct class to
  claim — that judgment belongs to the retro).

### Acceptance criteria (11 mechanical, 12 total)

All 11 `check:` lines re-run live, exit 0, exactly as the spec authors them
(`check-acs.sh`'s own recount above is the authoritative tally). Spot-verified
by hand beyond the mechanical pass:

- **AC1/AC2 (F1 adjacency, R1/R2/R3 placement)** — read the live file directly:
  R1 is the first non-empty `## Rules` line; F1 is the physical line
  immediately after it, no blank line between; R2 sits inside the `## Output`
  fence immediately before the pre-existing `- If you defer nothing, write
  exactly: no fast-follow deferrals` line; R3 is present verbatim inside
  `## Rules`.
- **AC3 (old phrasings gone)** — `grep -n "you have no board\|only read, run Codex" agents/codex-reviewer.md`
  returns nothing; `Cross-Provider Reviewer` positive control present.
- **AC4 (directive cross-references)** — all 13 fixed strings present in both
  the frozen F1 block and the live file; `Board and write boundary` occurs
  exactly once, `a rule you follow, not a capability you lack` exactly four
  times, `board-and-write-boundary Rule` exactly three times; F1/R1/R2 carry
  zero `APPROVE`/`REQUEST_CHANGES`, R3 carries exactly the one inherited pair.
- **AC5 (whole-file delta)** — `git diff --numstat 53955c5 -- agents/codex-reviewer.md`
  = `4  3`. Independently re-derived the `comm -23`/`comm -13` set comparison
  by hand: base-only set is exactly the three old phrasing lines, work-only
  set is byte-identical to the sorted four frozen blocks.
- **AC6 (preservation)** — frontmatter still `tools: Read, Grep, Glob, Bash`;
  T-1028's class-M bullet extracted from the base blob by anchor and confirmed
  present verbatim exactly once in the live file (line content identical,
  position shifted only by F1's insertion, as expected).
- **AC7 (registry)** — `check-prompt-sync.sh` -> `all registered prompt blocks
  in sync`; both `language-codex-reviewer.md` and `operating-paths-core.md`
  lines verbatim in the file; zero `<!-- BEGIN/END prompt-block:` markers.
- **AC8/AC9 (scope, records, board hygiene, non-goals)** — changed set is
  exactly the six-path allow-list; provenance and interventions files
  conformant; board zero-deletion (`--numstat` deletion column = 0);
  `check-handoff.sh`, `check-board-headings.sh --base 53955c5`,
  `check-pii-shapes.sh --base 53955c5` all green; all nine other `agents/*.md`,
  `.shell-team/lessons.md`, and `bin/tests/templates/skills/docs/.github` show
  zero tracked diff and zero untracked additions.
- **AC10 (stacked neighbours)** — all three neighbour specs re-confirmed
  `aligned` against the edited board (see gates above).
- **AC11 (self-application + corrected-claim anchors)** — `## Summarized
  sources` heading precedes the intent-block marker, carries 5 entry bullets
  (not the sentinel), names `#134`/`9d82a49`/`bin/close-out.sh`;
  `bin/close-out.sh` contains the doubled-backslash anchor
  `^- \[ \] \*\*` literally at line 165, the failure string `not found as a
  top-level entry in ## Active` at line 185, and zero occurrences of the token
  `revert`; `bin/check-handoff.sh` contains `Only validate unfinished` at
  line 156. All confirmed by direct `grep`/`sed` reads of the real files.

### Claim-fidelity check (this task's own new mandatory item)

Every named source in `## Summarized sources`, matched against the source's
own bytes at every restatement site in the frozen region (Problem, Goal, D1,
D5, D7, F1's provenance and backstop clauses, Input space):

- **GitHub issue #134** — fetched live: `curl -s -H "Accept:
  application/vnd.github+json" https://api.github.com/repos/RipsawJP/shell-team/issues/134`
  (this sandbox's `curl` reaches `api.github.com`; `gh` does not — matches the
  task's own stated environment). Body opens: *"During the v1.4.0 cycle the
  reviewer prematurely flipped a task's `- [ ]` checkbox... The close-out pass
  detected and reverted it (git commit `9d82a49`...)."* **Verdict: MATCH** —
  the issue genuinely does carry the detect-and-revert premise the spec
  corrects. No restatement site in the frozen region repeats this false
  premise; every site (Problem, Goal, D5, F1's backstop clause, Input space)
  states the corrected manual-repair story consistently — confirmed by direct
  read of each cited location, not by trusting the hand-off's own claim of
  consistency.
- **Commit `9d82a49`'s message** — `git show -s --format=%B 9d82a49` returns
  `board: close out T-1025 ... (also reverts the reviewer's premature checkbox
  flip — the close-out\nscript owns the [x] transition; recorded for the
  retro)` — a hard-wrapped body. F1's quoted fragment ("also reverts the
  reviewer's premature checkbox flip — the close-out script owns the [x]
  transition") matches character-for-character once the physical line wrap is
  joined to a single space — including the em-dash and apostrophe. **Verdict:
  MATCH (whitespace-only normalization, disclosed in provenance decision 2,
  not a content difference)**. D2's precedent claim and D4's citation-shape
  claim both check out against the actual commit.
- **The tech-lead's routing measurements** — DP-1 re-derived: `grep -n
  "^tools:" agents/*.md` shows all nine lines are bare tool-name lists, no
  path qualifier anywhere; `agents/codex-reviewer.md:4` is exactly `tools:
  Read, Grep, Glob, Bash`. **MATCH.** DP-2 re-derived: the three
  capability-absence lines at the base ref (`git show 53955c5:...`) land at
  physical lines 131, 156, 167 exactly as the spec states. **MATCH.** DP-3
  re-derived: `bin/codex-capture.sh`'s `--alloc`/`--publish` contract creates
  both raws inside the resolved reviews dir and prints their paths, with a
  `cleanup` trap removing them — matches F1's enumerated write surface.
  **MATCH.** DP-5 (two claims, correctly kept apart): "no revert logic" —
  `grep -c revert bin/close-out.sh` = 0, **confirmed true**. The double
  `- [x]`-prefix claim — read `bin/close-out.sh`'s scan (`^- \[ \] \*\*`,
  line 165) and the Done-entry builder (lines ~319-321): the Active-scan
  rejects an already-flipped line before the builder is ever reached
  (`a_count=0` -> `fail "... not found as a top-level entry ..."`, line 185),
  so the Done-entry builder is provably unreachable for a flipped line.
  **The spec's correction (this claim is false) is itself confirmed true** —
  no acceptance criterion or frozen text asserts the latent-bug reading, as
  the spec requires.

No site in the frozen region restates either of the two corrected false
premises (the issue's detect-and-revert claim, or the tech-lead's
double-prefix claim). The `#134` entry is **measured/confirmed** in this
session (network-reachable sandbox), not `environmentally-unverified` — same
outcome the engineer's own pass reached, independently re-derived here rather
than taken on trust.

### Empirical spot checks

- F1 adjacency: confirmed by direct file read — F1 is the line immediately
  following R1 with no blank line, exactly as AC1 requires.
- R2 inside the `## Output` fence, immediately followed by the pre-existing
  `- If you defer nothing, write exactly: no fast-follow deferrals` line —
  confirmed by direct read; the fence's other template lines are undisturbed.
- The three old phrasings absent — confirmed via direct `grep`, zero hits.
- Whole-file delta 4 added / 3 removed — confirmed via `git diff --numstat`.
- T-1028's class-M bullet byte-identical — confirmed: base-blob line and
  live-file line are textually identical (only their line numbers differ, due
  to F1's insertion above them, which is expected and does not affect this
  criterion).
- `check-prompt-sync.sh` green, plus manual re-derivation of both `contain`
  blocks' lines against the live file — all present.

### AC12 audit for substance

**(a)** Re-ran, live, both CI directory/glob-scanning steps this task's own
added files implicate: the `.shell-team/interventions/*.md` dogfood loop
(`.github/workflows/check-handoff.yml`'s "Dogfood check-interventions" step) —
28 files scanned including the new `T-1030.md`, all conformant; and the
"Dogfood check-handoff — this repository's own board" step (`team-paths.sh
--get todo` + `check-handoff.sh`) — green. No other directory/glob-scanning
step in the workflow touches `.shell-team/provenance/` or `.shell-team/reviews/`
(confirmed by `grep -n provenance\|reviews/ .github/workflows/check-handoff.yml`).
Since `bin/`/`tests/` are byte-identical (AC9), the ~50 other named-file steps
(shellcheck's fixed list, `tests/*/run.sh`) were not individually re-run — this
is the same measured-byte-identity ground the spec's own AC9 and the prior
T-1028/T-1029 QA rounds used, not an unexamined skip.

**(b)** Independently reproduced 2 of the engineer's 7 reported mutations, on
disposable `$TMPDIR` scratch copies (never the working tree, confirmed via
`git status --short` clean throughout): mutation (3) — deleted "You run no
mutating git subcommand" from F1 symmetrically in both a scratch spec copy and
a scratch agent copy: AC1-style byte-equality stayed **green** (count=1, both
sides mutated identically), AC4-style directive-presence check went **red**
(phrase absent from both) — exactly the asymmetry AC12(b) predicts, and the
reason AC4 rather than AC1 is the directive lock. Mutation (6) — appended one
unrelated line elsewhere in a scratch agent copy only: the AC5-style
`comm`-based set comparison against the real base ref (`53955c5`) went **red**
(work-only set no longer equals the sorted four frozen blocks), while
AC1-style byte-equality and AC3-style old-phrase-absence stayed **green** —
confirmed AC5 is a whole-file lock and not a targeted one, catching a mutation
the narrower locks are blind to. Both mutations chosen because they are the
two the spec calls out as "aimed at the detector's own blind spots."

**(c)** Relayed claims independently re-measured (not taken from the
provenance file's report): commit `9d82a49` and issue `#134`, both above —
both confirmed, matching the engineer's provenance decisions 2 and 3 exactly.

**(d)** `## Summarized sources` restatement-site check performed independently
above (Claim-fidelity check section) — same conclusion as the engineer's
report: every restatement site states the corrected story, no site anywhere
restates a false premise.

### Regressions and adjacent-code checks

- `check-prompt-sync.sh` green (registry obligations over `agents/codex-reviewer.md`
  intact).
- All three stacked-neighbour specs (T-1027, T-1028, T-1029) still `aligned`
  against the edited board — no collateral damage to a sibling task's frozen
  ledger.
- `bin/`, `tests/`, `templates/`, `skills/`, `docs/`, `.github/` all
  byte-identical to base (tracked and untracked) — no accidental edit spilled
  outside the one agent file.
- No other `agents/*.md` touched — `agents/drift-evaluator.md`'s precedent
  paragraph (cited as the shape F1 follows) is untouched, confirmed by
  `git diff --name-only`.

### Edge cases tried

- A legal, in-scope collision: the spec's own body text quotes the phrases
  `you have no board` and `only read, run Codex` (in the Problem statement,
  D2, the symmetry table, and AC5's clause list) — AC3's negative grep is
  correctly scoped to `agents/codex-reviewer.md` alone, so the spec's own
  self-referential quoting does not trip it. Confirmed by direct read: the
  scoped grep genuinely finds zero hits in the target file.
- The doubled-backslash regex literal in `bin/close-out.sh`'s awk source
  (`^- \\[ \\] \\*\\*`) versus a naive single-backslash reading — confirmed
  the doubled form is the one that actually appears in the file (a
  single-backslash `grep -qF` would fail vacuously), matching the Assumptions
  section's explicit warning about this trap.
- Hard line-wrap inside a quoted commit message (`9d82a49`) — confirmed the
  wrap is whitespace-only and the quoted fragment survives character-for-
  character once joined, rather than accepting "looks similar" as a match.

### Out-of-input-space / environmentally-unverified notes

- None of my newly-synthesized edge cases relied on an out-of-scope synthetic
  extreme; nothing here needed the `out-of-input-space` exemption.
- Nothing was `environmentally-unverified` in this session: `#134` was
  network-reachable via `curl` (unlike pm-spec's role), so it was measured
  directly rather than recorded as unreachable.
- The **Adversarial fixture synthesis checklist is structurally inapplicable**
  to this prose-only deliverable (no executable artifact, no documented
  runnable procedure) — stated explicitly here rather than silently skipped;
  this is a lens choice, not a coverage gap, per the spec's own task
  classification and qa-verifier's applicability paragraph.
- Not independently re-run: the ~50 named-file CI steps unaffected by this
  diff (shellcheck's fixed list, `tests/*/run.sh` suites) — skipped on the
  measured byte-identity ground (AC9), not a silent omission.

### Risk notes for reviewer

- D7's git-mutation exception (committing the role's own review record on
  explicit instruction) is a genuinely new behavioral carve-out for this role;
  its wording is worth a close read for whether "one file, never
  self-initiated" is unambiguous enough in practice, though the frozen bytes
  themselves read tightly to me.
- The Open question in the spec (whether `skills/run/SKILL.md` /
  `skills/goal/SKILL.md`'s matching phrasing should join this task) was
  already resolved by the coordinating session before freeze (declined,
  tracked as issue #143) — visible on the board, not reopened here.
- This is the first live use of the git-mutation-exception clause's
  "exactly one exception" wording in review-record-committing practice; worth
  watching in the retro whether the exception is invoked cleanly in future
  rounds.

Task: T-1030 → READY_FOR_REVIEW

---

## Codex round-1 — Cross-provider review

### Codex Review verdict: APPROVE
- Task: T-1030
- Codex model: gpt-5.6-sol (config default; no `-m` override passed)
- Verification mode: static-only
- Diff scope: 6 files changed, +581/-3 total (`git diff --stat 53955c5...HEAD`); the one production file, `agents/codex-reviewer.md`, is +4/-3 (`git diff --numstat`); the remainder (`.shell-team/specs/T-1030-...md`, `.shell-team/todo.md` +21/-0, `.shell-team/provenance/T-1030.md`, `.shell-team/reviews/T-1030.md`, `.shell-team/interventions/T-1030.md`) are additive spec/board/record artifacts, not code.

**Self-referential disclosure**: the diff under review edits this role's own boundary rule, and this checkout runs the plugin from the working tree — so the F1/R1/R2/R3 text below is already the instruction this review is running under. I read my own `## Rules` and `## Output` sections as delivered (not from the base ref) while performing this review, and I am observing D7's directed-commit exception myself in this very round (see (e) below and the Recommendation).

Both a foreground `codex exec review --base 53955c5 --json` pass and a triggered adversarial "devil's advocate" pass were run (the primary pass returned one P2 finding, which per this role's severity-normalization map defaults to `major` and mandates the adversarial follow-up). Raw captures: `.shell-team/reviews/T-1030-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1030-codex-adversarial.{txt,jsonl}`.

#### (a) Rule quality

Read F1, R1, R2, R3 as shipped in `agents/codex-reviewer.md` (lines 156-158, 168) directly, plus both Codex passes targeting them.

- **Checkbox flip**: no reading, however literal, permits it. F1 states it three separate ways — "you never edit the board", "you do not flip a `- \[ \]` checkbox to `- \[x\]`, change a status flag, or add, edit, reorder or delete any board line", and, closing the exact rationalization the incident actually used, "not even when your own verdict is what would justify the change, and not even to record that verdict." The adversarial pass, asked explicitly to construct a permissive reading, could not: "there is no plausible permission to flip a checkbox after ... 'not even when your own verdict is what would justify the change'."
- **Self-initiated / broadened commit**: also confirmed airtight by the adversarial pass. F1's exception is scoped by three independent conditions that must all hold — externally directed ("when the coordinating session's task prompt for this round tells you to"), single-file ("that one file and nothing else in the same commit"), and non-self-initiated ("A commit you decided to make on your own initiative is outside the exception, and so is any commit carrying a second path"). The adversarial pass's verbatim conclusion: "self-initiated or broader commits are expressly excluded by 'A commit you decided to make on your own initiative is outside the exception, and so is any commit carrying a second path.'"
- **One genuine, narrower finding (both passes independently agreed, P2/major by the normalization map)**: "Write these paths and no others" is, read as an isolated clause, an unqualified universal claim, and the mandatory `codex exec` subprocess this role must invoke can write incidental runtime/session/cache state under `$CODEX_HOME` or `~/.codex` — outside the enumerated list and outside the checkout entirely. Read in isolation this is a literal-truth gap of exactly the shape this task exists to remove (an absolute claim that isn't quite true). See the Synthesis audit ledger for why I hold this at `minor` in the final verdict rather than the normalized `major`.

#### (b) Honesty

- `agents/codex-reviewer.md` carries **zero** occurrences of `you have no board` and `only read, run Codex` (`grep -c` on the live file, both 0; `Cross-Provider Reviewer` positive control present) — the three false capability-absence phrasings are gone, replaced in place by R1/R2/R3, each pointing at F1 by the fixed phrase `board-and-write-boundary Rule`.
- Backstop honesty checked directly against the two named scripts (not taken from the spec's own claim):
  - `bin/close-out.sh` — `grep -c revert bin/close-out.sh` = 0 (zero occurrences of the token anywhere in the file). Its Active-entry scan (line ~165, awk) matches only the anchored pattern `^- \[ \] \*\*<task>\*\* ` — an already-flipped `- [x]` line yields `a_count=0` and the script calls `fail "$TASK not found as a top-level entry in ## Active of $BOARD"` (confirmed present verbatim). No detect-and-revert logic exists anywhere in the script; F1's claim is exactly right.
  - `bin/check-handoff.sh` — confirmed the `Only validate unfinished (\`- [ ]\`) top-level checklist lines` comment and the adjacent `[[ "$content" != "- [ ]"* ]] && continue` guard: a flipped line is skipped, never validated, exactly as F1 states ("passes the lint unseen").
  - `bin/codex-capture.sh` — confirmed `--alloc`/`--publish` create/consume raws inside the resolved reviews dir (`team-paths.sh --get reviews`), matching F1's enumerated write surface exactly (canonical basenames, the two per-invocation raws, and the `cleanup` removal).
- Commit `9d82a49` — `git show -s --format=%B 9d82a49` re-confirmed independently: F1's quoted fragment matches character-for-character once the commit body's own hard line-wrap is joined to one line (whitespace-only normalization, disclosed already in the provenance file — not silently smoothed over).

#### (c) Claim fidelity of the spec's own summarized sources

Independently re-fetched, in this sandbox, both corrected premises against their sources (network-reachable here, same as QA's environment — not `environmentally-unverified` for this round):

- **Issue #134** — `curl -H "Accept: application/vnd.github+json" https://api.github.com/repos/RipsawJP/shell-team/issues/134` (title: *"codex-reviewer flipped a board checkbox that close-out.sh owns — make the write boundary structural, not detect-and-revert"*). Body confirms the issue genuinely does assert the detect-and-revert premise the spec corrects ("The close-out pass detected and reverted it (git commit `9d82a49` ...)"), **and** its own Outcome-2 fallback text repeats the same false premise a second time ("keep close-out's detect-and-revert as the enforcement backstop") — which the shipped text correctly declines to carry forward, stating instead the true, weaker backstop ("Nothing detects the flip when it is made and nothing reverts it; that repair was a human's"). No restatement site in the frozen region (Problem, Goal, D5, F1's backstop clause, Input space) repeats either false premise. The issue's Outcome-1/Outcome-2 framing is itself matched exactly by D1's audit-and-record disposition ("Either way the decision should be recorded, not left implicit" — D1 records it in both the spec and, per the spec, the PR body).
- **The routing map's double-prefix claim (DP-5)** — read `bin/close-out.sh` directly: the anchored scan (line ~165) rejects an already-flipped line before the Done-entry builder (~line 319) is ever reached, so the builder is provably unreachable for that input. The spec's correction ("re-measured and found false") holds up against the real script, not just against the spec's own restatement of it.

#### (d) Blast radius

Re-derived independently (not trusted from AC5/AC6/AC7/AC9's own `check:` text):

- Changed-and-added file set (tracked + untracked) is exactly the six paths the spec's allow-list names: `agents/codex-reviewer.md`, the spec, the board, plus the three per-task record files. Nothing else.
- `git diff --numstat 53955c5 -- agents/codex-reviewer.md` = `4  3` — matches the Goal's four-added/three-removed contract exactly.
- `git diff --name-only 53955c5 -- 'agents/*.md' | grep -v codex-reviewer.md` — empty. No other agent file touched.
- T-1028's class-M review item (`- **Verify a class-M re-freeze's frozen-region delta...`) is present verbatim, unreflowed, still the item immediately before the two Japanese-language bullets.
- Both Japanese-language prose bullets (凍結 intent / 決定 provenance) are present, byte-for-byte, untouched.
- `.shell-team/todo.md`'s diff is `21  0` against the base ref — a pure insertion into an empty `## Active`, no deletion.

#### (e) D7's exception vs. the run skill — observed live, this round

`skills/run/SKILL.md` and `skills/goal/SKILL.md` do **not** instruct the reviewer to commit anything at all (`grep -n "commit.*review\|codex-reviewer"` over both files shows only "read-only on the board, with no \`gh\`/API access" / "The reviewer is read-only on the board (no \`gh\`/API access)" — both still the deferred false-capability phrasing, exactly as D8/AC9 predicted and lock byte-identical). D7's exception is instead grounded in the **coordinating session's task prompt for a given round**, and this round's own task prompt is a live instance of exactly that: it names this file, instructs "Append your review ... and per the directed-commit exception you may commit exactly that one file" — one file, externally directed, nothing else. I am observing the exception as written: I will stage and commit only `.shell-team/reviews/T-1030.md` in one commit, and nothing else, per F1's own limits. Separately confirmed: the two deferred orchestrator-facing lines (D8) are tracked as **issue #143** ("open", re-confirmed live via the GitHub API), not silently dropped.

#### Blockers

- None.

#### Major

- None (see Synthesis audit ledger for the one finding both passes raised at normalized-`major`, held at `minor` in the final verdict).

#### Minor / nits

- `agents/codex-reviewer.md:157` — F1's "**Write these paths and no others**" clause, read as an isolated sentence fragment, is a universal claim that is not quite literally true: the mandatory `codex exec` subprocess this role must invoke can write incidental runtime/session/cache state under `$CODEX_HOME` or `~/.codex`, outside both the enumerated list and the checkout. Read alongside R1 ("everything else in **this checkout** is read-only for you") and F1's own next clause ("Everything else **in the checkout** is read-only for you"), the intended scope is checkout-local writes this role deliberately makes — not a claim about every byte a required subprocess might touch — and no downstream checker or practical incident risk turns on the literal-isolation reading. Suggested (non-blocking) tightening for a future re-freeze: insert "in the checkout" immediately after "no others," matching the qualifier both neighboring sentences already carry.

#### Adversarial findings

- Devil's-advocate pass (targeted at F1/R1/R2/R3 specifically) could not construct a permissive reading for either the checkbox-flip or the self-initiated/broadened-commit questions, and said so explicitly ("no plausible permission to flip a checkbox...", "self-initiated or broader commits are expressly excluded...").
- The same pass independently surfaced and confirmed the `$CODEX_HOME`/`~/.codex` write-surface tension above (P2), and additionally argued R1's "Your write surface is the closed list" phrasing "incorporates F1's globally phrased list and therefore worsens the strict-reading tension" rather than resolving it — a sharper framing than my own first read, which is why the ledger below records this as a documented disagreement rather than a silent override.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `agents/codex-reviewer.md:157` — "Write these paths and no others" doesn't literally carve out incidental `$CODEX_HOME`/`~/.codex` writes by the mandatory `codex exec` subprocess | primary: P2 (→ major, per normalization map) · adversarial: P2, explicitly rejects my initial view that R1 resolves the tension, and still recommends a wording fix | minor | Both providers independently confirm the literal-isolation reading is real, but (i) it is orthogonal to the incident class this task exists to close — the adversarial pass itself confirms the checkbox-flip and self-initiated-commit boundaries are airtight; (ii) the practical effect is nil: `$CODEX_HOME` bookkeeping is the CLI tool's own incidental state, never a write this role "decides" to make, and no checker or operator treats it as a boundary violation; (iii) per this task's own R-1 pre-commitment, any wording change to F1 is a re-freeze proposal routed to pm-spec, not a merge-blocking rework item for this round. This is not an `out-of-input-space` downgrade (no Input-space Out-of-scope line covers `$CODEX_HOME` precisely) — it is an ordinary judgment call, recorded here rather than silently applied. |

No other finding from either pass required a severity change or rejection beyond the row above.

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- `agents/codex-reviewer.md:157`'s "Write these paths and no others" wording precision (the `$CODEX_HOME`/`~/.codex` isolation gap, see ledger row 1) — **file-an-issue** (rationale: it's a real, if narrow, wording-precision gap that a future re-freeze should close by inserting an explicit "in the checkout" / tool-runtime-state exemption near the enumeration; per R-1 this is a re-freeze proposal routed to pm-spec, not something an engineer patches on this branch, so it should not block this merge but should not be silently dropped either).
