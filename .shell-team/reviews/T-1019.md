# T-1019 QA verification record

- Verdict: PASS
- Commit reviewed: `037367b` (suite + fixtures + CI registration + recipe +
  provenance) + `805ceda` (interventions sentinel), on top of `a9d7869`
  (spec v1 frozen). Base `develop`.
- Board: T-1019 → READY_FOR_REVIEW
- Frozen intent: `bash bin/check-intent.sh
  .shell-team/specs/T-1019-is-span-row-parity.md .shell-team/todo.md` →
  `aligned: T-1019 v1 (418066a64da6fcdb4c5bd58eb1a3ff81d0079ee8) ...` (run
  directly, not taken on the engineer's word).

## Mechanical acceptance criteria

`bash bin/check-acs.sh --dry-run .shell-team/specs/T-1019-is-span-row-parity.md`
then live `bash bin/check-acs.sh .shell-team/specs/T-1019-is-span-row-parity.md`:
**12 passed, 1 failed (AC13), 1 skipped (AC14), 0 unrecognized.** AC13 failed
*only* on `test -r .shell-team/reviews/T-1019.md` (this file did not exist
yet) — the other AC13 sub-clauses (provenance file readable, check-handoff,
check-board-headings, check-pii-shapes) were confirmed passing independently
before this file was written (see below). Re-running AC13 after this commit
is expected to flip to PASS.

- AC1 (fixture shape + attributability reduction): PASS.
- AC2 (D4 expected column against the real scripts, independent of the
  suite): PASS.
- AC3 (suite green, six assertion ids present in file and output): PASS.
- AC4 (mutation direction 1, rollup-runs.sh patched): PASS via `check-acs.sh`
  AND independently re-verified by hand below.
- AC5 (mutation direction 2, cluster-failures.sh patched): PASS via
  `check-acs.sh` AND independently re-verified by hand below.
- AC6 (single-source invariant): PASS — confirmed by hand that the marker
  `RUN-ISR-PARITY` appears outside `tests/is-span-row-parity/` only in
  `.shell-team/specs/T-1019-is-span-row-parity.md` and
  `.shell-team/provenance/T-1019.md`, both outside AC6's scanned directory
  set (`bin tests templates skills agents .github`) and both anticipated by
  the spec's own self-hosting note in Assumptions.
- AC7 (CI registration, exactly 2 occurrences): PASS — confirmed by direct
  grep of `.github/workflows/check-handoff.yml`.
- AC8 (shellcheck-clean): PASS — local `shellcheck --version` reports
  `0.11.0`, matching the CI pin exactly (test-recipe's documented
  precondition).
- AC9 (no `eval`, black-box positive control): PASS — re-verified with the
  authoritative `bash -c` execution path per the task's item 5 (see below).
- AC10 (preservation lock): PASS — `git diff --name-only develop -- bin
  tests/rollup-runs tests/cluster-failures` is empty; both existing suites
  still green.
- AC11 (scope lock): PASS — `git diff --name-only develop` plus untracked
  additions equal exactly the required file set (fixtures, suite, CI
  workflow, test-recipe, spec, board) with only the three permitted extras
  (`provenance/interventions/reviews` under T-1019) beyond it.
- AC12 (test-recipe entry): PASS — `## Appended by tasks` carries a T-1019
  entry naming `tests/is-span-row-parity/run.sh` and the mutation procedure.
- AC13 (records + board lints): FAIL pre-this-file, as predicted; expected
  PASS once this file and the board flag are committed (structural gates
  below independently re-run and confirmed).
- AC14 (runtime, full-CI + by-hand mutations): SKIP by design (no `check:`).
  Verified below by actually running the whole CI-wired list, and by hand
  re-running both mutation directions independently of `check-acs.sh`.

## Structural gates

- `bash bin/check-provenance.sh .shell-team/provenance/T-1019.md` →
  `conformant: .shell-team/provenance/T-1019.md (3 decision entries, 0
  sentinel)`.
- `bash bin/check-interventions.sh --task T-1019
  .shell-team/interventions/T-1019.md` → `conformant: ... (0 entries, 1
  sentinel)`.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base develop` →
  exit 0.
- `bash bin/check-pii-shapes.sh --base develop` → `clean`.
- `bash bin/check-commit-identity.sh --base develop` → `clean`.
- Scope lock spot-check: `git diff --name-only develop -- bin
  tests/rollup-runs tests/cluster-failures` is empty (repeated, independent
  of AC10's own check line).

## Positive control — mutation-based anti-vacuity, both directions, on a
## fresh `$TMPDIR` scratch clone (never the working tree), independent of
## the spec's own AC4/AC5 `check:` commands

Two separate scratch clones (`bin/` + `tests/is-span-row-parity/` only),
one per direction, built and mutated by hand:

- **Control (both clones):** `bash "$scratch/tests/is-span-row-parity/run.sh"`
  → exit 0, all six `PASS: parity-*` lines, before any patch — proves the
  scratch tree runs the suite at all.
- **Direction 1 — `bin/rollup-runs.sh` patched only** (`sed 's/== "span"/!=
  "zzz"/'`), `bin/cluster-failures.sh` confirmed still carrying `== "span"`
  in the scratch copy: re-running the suite → **exit 1**, output:
  `FAIL: parity-kind-event: bin/rollup-runs.sh kind-event.jsonl: expected
  skipped, got counted` — names the patched script and the fixture, exactly
  as D5 requires.
- **Direction 2 — `bin/cluster-failures.sh` patched only**, `bin/rollup-runs.sh`
  confirmed still unpatched in that scratch copy: re-running the suite →
  **exit 1**, output: `FAIL: parity-kind-event: bin/cluster-failures.sh
  kind-event.jsonl: expected skipped, got counted` — names the patched
  script and the fixture.

Both directions independently measured (not inferred from one). This
confirms the suite's detection is real, not vacuous, in both directions.

## AC9 authoritative execution path (the engineer's provenance-entry-3
## disclosure re-verified)

Per the task's item 5: `bash -c "grep -qE '(^|[^[:alnum:]_])eval([^[:alnum:]_]|\$)'
tests/is-span-row-parity/run.sh"` → exit 1 (no match, lock holds), matching
`check-acs.sh`'s own execution route. Confirms the D3 header-comment rewording
recorded in provenance entry 3 (avoiding the literal `eval` token) actually
holds under the real host `grep`, not only under an interactive shell's
`grep` wrapper.

## Full CI-wired list, run locally in workflow order

Ran every one of the 57 executable steps `.github/workflows/check-handoff.yml`
lists (59 named steps minus `actions/checkout` and the shellcheck-install
step, substituted with the already-pinned local `shellcheck 0.11.0`), in the
file's order, including the new suite's own `run:` step
(`bash tests/is-span-row-parity/run.sh`) and its shellcheck argument-list
entry. **Result: 57 passed, 0 failed.** This includes both existing suites
(`tests/rollup-runs/run.sh`, `tests/cluster-failures/run.sh`, both green,
confirming AC10's preservation claim independently of the AC10 `check:`
line), the new `tests/is-span-row-parity/run.sh`, and every dogfood step
(`check-pii-shapes --base develop`, `check-commit-identity --base develop`,
etc.).

## Regressions / adjacent code paths

No `bin/` file changed (confirmed by AC10 and the scope-lock spot-check
above), so there is no adjacent production code path to regress. The two
existing suites (`tests/rollup-runs/run.sh`, `tests/cluster-failures/run.sh`)
were run and are green, confirming the new suite's addition did not disturb
them.

## Edge cases tried

- Verified the fixture set against the Input space's reachable classes
  directly (not just via the suite): grepped the six fixtures' raw content
  to confirm exact byte shapes match D4's table (`kind` field position,
  no key collisions, the hybrid probe row's `"kind":"event"` +
  span-shaped payload). All within the spec's declared Input space —
  no out-of-scope synthetic extreme was needed to exercise this task's
  boundary (D5's failure-naming boundary is exactly what AC4/AC5 and my
  independent re-run above exercise).
- Full-tree grep for `RUN-ISR-PARITY` and each fixture basename, to confirm
  D2/D6's single-source invariant holds beyond AC6's own scanned directory
  set (the spec's self-referential mentions in the spec file and provenance
  file are the only additional hits, both anticipated and out of AC6's
  scope by design).
- Confirmed AC9's `eval`-token negative lock via the authoritative `bash -c`
  path rather than only an interactive shell (this task's explicit item 5,
  and the class of miss the engineer's own provenance entry 3 discloses).

## Adversarial fixture synthesis checklist

Deliverable is an executable artifact (`tests/is-span-row-parity/run.sh`,
plus CI wiring). Checklist applied:

1. **Beyond happy-path**: the mutation-based anti-vacuity procedure (AC4/AC5)
   is exactly this class of check — crossing the discriminator boundary in
   both directions — independently re-run above, asserting the final
   defended state (the specific `FAIL:` line naming script + fixture), not
   merely that some failure fired. Payload-shape sub-axis is covered by the
   six fixtures themselves (`kind-malformed.jsonl` = non-string value,
   `kind-unknown.jsonl` = unrecognized string). Filesystem/environment-state
   and resource/budget-competition sub-axes are **structurally impossible**
   for this artifact: the suite takes no external path/config input and
   manages no budget — it reads six fixed, committed fixture files by a
   `$REPO_ROOT`-derived path. Time/liveness (a hang/timeout) is likewise
   **structurally impossible**: the suite implements no timeout mechanism
   of its own to exercise, and both consumer scripts complete in
   well under a second on a one-row fixture.
2. **Regex/character-set anchoring**: exercised via the full-tree grep audit
   above (AC6's own marker scope, and the fixture-basename search), and via
   AC9's negative-lock re-verification through the authoritative `bash -c`
   route rather than an interactive-shell proxy.
3. **Markdown/CommonMark structure parsing**: **structurally impossible** —
   this artifact parses neither Markdown nor fenced blocks; it reads JSONL
   fixture lines and script stdout only.
4. **Test-harness short-circuit**: ran every documented canonical command
   exactly as CI documents it (`bash tests/is-span-row-parity/run.sh`, the
   suite's only documented invocation form — test suites in this repo are
   never invoked via `PATH` or `./`, unlike `bin/` scripts), as part of the
   full 57-step CI-list run above. Confirmed the suite exercises the real
   `bin/` scripts with no stub: the mutation experiments patch the actual
   `bin/rollup-runs.sh` / `bin/cluster-failures.sh` in a scratch copy and
   observe the suite's real output change in response.

No `environmentally-unverified` or `out-of-input-space` items to record —
every applicable class was structurally reachable and was exercised.

## Risk notes for reviewer

- AC13 will read FAIL until this file and the board's status-flag update are
  committed together — that is expected and by design (the spec predicts it
  explicitly), not a defect.
- The spec's declared residual detection boundary (Non-goals: a
  same-direction change to both `is_span_row` copies, whose effect falls
  entirely outside the six covered classes, stays undetectable) is an
  accepted, explicitly-declared limit, not a gap found during this
  verification.
- `bin/` is untouched (AC10/AC11 both confirm this independently); nothing
  in the shipped runtime behavior of `rollup-runs.sh` or `cluster-failures.sh`
  changed.

---

### Codex Review verdict: APPROVE
- Task: T-1019
- Codex model: gpt-5.6-sol
- Verification mode: static-only
- Diff scope: 14 files, +580/-1 (develop...HEAD, commits a9d7869..e46518f)

#### Blockers
- なし。

#### Major
- なし（Codex 一次パスは 2 件を P2 として提示したが、下記シンセシス台帳のとおり両方とも Minor へ引き下げた）。

#### Minor / nits
- `tests/is-span-row-parity/run.sh:96`（`assert_parity` の第3比較）— この行は到達不能な死コードである。94行目・95行目がそれぞれ `rc_class == expected` / `cc_class == expected` を fail-fast で保証してからでないと96行目に到達できず、到達した時点で両者は推移律により必ず等しい。したがって「両consumerの食い違いを名指しする」という第3比較の存在理由は、94/95行目にすでに包含されており、単独では絶対に発火しない。実害はゼロ（検出力は AC4/AC5 の実機ミューテーション再実行で確認済みのとおり94/95行目が完全に担保している）。ヘッダコメントと provenance entry 1 が「3つの独立した検査」と説明している点が、実装の実態と食い違っているだけ。
  - 修正案: 96行目とその周辺コメントを削除するか、parity 比較を94/95行目より前に動かして両者の分類を先に比較する設計に組み替える（Codex アドバーサリアルパスの提案）。
- `.shell-team/specs/T-1019-is-span-row-parity.md:77,79,91`（AC4/AC5/AC11 の `check:` 行）— `d=$(mktemp -d ...)` の直後に `rc=0` を無条件でセットしており、`mktemp` 自体の失敗を検知しない。`mktemp` が失敗すると `$d` が空文字になり、`$d/tests` や `$d/raw.txt` が `/tests` や `/raw.txt` に展開される。実測（非rootのTMPDIR不正指定）では AC4/AC5 は `mkdir -p /tests` の権限エラーで結果的に fail-closed する一方、AC11 は `grep -c ... || true` の連鎖により `comm`/書込み失敗が0件へ丸められ得るため、シナリオによっては scope lock が false PASS しうる。ただし: (a) このリポジトリの実CI（GitHub-hosted ubuntu-latest, 非root）ではこの spec 自体を `check-acs.sh` に通す CI ステップは無く、実行は開発者のローカル `check-acs.sh` 呼び出しに限られる。(b) 全く同じ `d=$(mktemp -d ...); rc=0;` の形は T-1011・T-1012・T-1013 の既存かつ既にレビュー・出荷済みの spec に計12件既存しており、今回 T-1019 が新規に追加したのは2件のみ（横断的な既存イディオムであり、この task 固有の新規ハザードではない）。
  - 修正案: 該当 `check:` 行を `d=$(mktemp -d "${TMPDIR:-/tmp}/...") || exit 1; rc=0;` に統一し、AC11 は `comm`/書込み系コマンドの失敗も明示的に `rc=1` へ反映する。T-1019 単体でなく、develop 上の既存12件も含めた横断的な棚卸しとして扱うのが筋。

#### Adversarial findings
- 上記2件について、Codex に「本当に発火する実行経路があるか」「実CI・root/非rootでの実害度」を再検証させた（`codex exec` アドバーサリアルパス、read-only sandbox で実ファイルを直接読ませる形）。
  - 第3比較（96行目）: bin側単独パッチ・fixture 側パッチのどの組み合わせを試しても、94/95行目のいずれかで先に `fail()` するため、96行目が `rc_class != cc_class` で発火する経路は「実行中の親シェル自体を外部から改変する」ような経路を除いて存在しないと確認。severity は `nit` 相当まで下げられると評価された。
  - `mktemp` 未ガード: 実際に非rootで壊れた `TMPDIR` を与えて実測し、AC4/AC5 は権限エラーで fail-closed、AC11 は `grep -c ... || true` を介して潜在的に false PASS になり得ることを確認。さらに `develop` 全体を grep し、同一パターンが T-1011/T-1012/T-1013 に計12件既存することを確認（このタスク固有の新規危険パターンではない旨、独立に裏付け）。severity は `minor` と評価された。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `tests/is-span-row-parity/run.sh:96` の第3比較が到達不能な死コード | 一次: P2（正規化=major）／アドバーサリアル: nit | minor | AC4/AC5 の実機ミューテーション再実行（本レビューで独立に再実施済み）で検出力そのものは94/95行目により完全に担保されていることを確認済みであり、実際の欠陥ではなくコメント・provenance の説明と実装の乖離（コード清潔性）に留まるため |
| 2 | spec の AC4/AC5/AC11 `check:` 行が `mktemp` 失敗を無視する | 一次: P2（正規化=major）／アドバーサリアル: minor | minor | 実CI（非root）では fail-closed に倒れることを実測確認し、かつ同一パターンが T-1011/T-1012/T-1013 に計12件既存する横断的な既存イディオムであってこの task 固有の新規ハザードではないため。AC11 経路の潜在的 false PASS は実害の芽があるため won't-fix ではなく file-an-issue 扱いとする |

#### Recommendation
- APPROVE → `.shell-team/todo.md` を READY_FOR_MERGE へ。

#### Fast-follow disposition
- `tests/is-span-row-parity/run.sh:96` の死コード除去/re-order — file-an-issue（理由: 修正自体は軽微だが、`assert_parity` の設計意図（3つの独立した検査）と実装の乖離を残したままマージ後に放置するとレビュー時の誤解を招くため、追跡可能な形にしておく）。
- spec の `mktemp` 未ガード（AC4/AC5/AC11、T-1011/T-1012/T-1013 の既存12件込み）横断ハードニング — file-an-issue（理由: T-1019 固有ではなく複数の既出荷 spec にまたがる横断的な hygiene 改善であり、1タスクのスコープで直すより棚卸しイシューとして一括対応する方が筋が良いため）。

内部整合性は検証したが、GitHub issue #80 本文そのものへの到達はこのサンドボックスからは行っていない — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation)。
