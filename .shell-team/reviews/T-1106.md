# Codex Review — T-1106

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1106
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line, measured this round rather than relayed; no `-m` override was passed to either invocation this round.
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 7 files, +757/-6 lines (base `01df99f719c7fc99efee985d8596110d4c8473bb` = tip of the stacked, still-open predecessor `feature/1105-review-executor-resolution`, resolved per the spec's own `- base-ref-discriminator:` expression; that branch resolves in `refs/heads/`, re-confirmed here via `git show-ref --verify --quiet` before use)

Two Codex passes ran this round: a primary pass (`codex exec … review --base 01df99f --json`, reading the real working tree directly) and a targeted devil's-advocate pass on the three deliverables (the body cross-check, the empty-value refusals, and the population guard), run because the primary pass's one finding normalizes to major severity under the closed P2→major map. Both raw outputs are saved (paths at the bottom). Independently, alongside both Codex passes, I re-ran `bash tests/check-binding/run.sh` (28/28 `PASS:` lines, `all cases passed`), `shellcheck bin/check-binding.sh tests/check-binding/run.sh` (0.11.0, exit 0 on both), and the spec's own `- check:` lines for **AC1** (three mutation shapes refused `lock-structural`/2, zero stdout, lock bytes unchanged), **AC7** (population guard, live), **AC8** (the one production consumer, `bin/resolve-executor.sh`, unaffected), **AC12** (all six merged T-1054/T-1055/T-1057 criteria re-run as their own committed check lines, all exit 0) and **AC13** (scope lock) — all pass exactly as the engineer/QA hand-offs report. I additionally spot-measured five of the `## Blast radius` table's named rows myself (`T-1105` **AC15**/**AC16**/**AC18**, `T-1054` **AC13**, `T-1055` **AC14**) directly against `HEAD` via `bin/check-acs.sh`, all FAIL exactly as the corrected table claims — matching the engineer's `ad42b52`/`20e1409` correction and QA's own independent re-measurement.

I also independently reproduced the specific adversarial edge cases the launch briefing named for item (ii) — a repeated `--config` flag with the empty value in either position, a `--` terminator preceding the flags, `--help` appearing before vs. after an empty value, and `--adapters ""` given as a second occurrence after a valid first `--config` — every shape refuses (or, for `--help` genuinely first, exits 0 by design) exactly as the parse-arm design implies; no bypass found. I also independently reproduced the QA-flagged nuance for item (iii): `git grep -l` (no `--untracked`) does not see a genuinely untracked, unstaged `bin/*.sh` caller — confirmed with a throwaway probe script, cleaned up immediately after (`git reset`, `rm`).

#### Blockers

None.

#### Major

- **`tests/check-binding/run.sh:664` (the `cb-adapters-not-forwarded-population` guard, issue #221) — the `|| true` on the live `git grep -l` enumeration is not scoped to the documented "no match" case; it swallows every exit status uniformly, including a fatal producer error.** The line reads `(cd "$REPO_ROOT" && git grep -l -e 'check-binding.sh' -- 'bin/*.sh' '.github/workflows/*.yml') > "$popall" || true`, and its own comment at `:659-663` states the guard is meant to "absorb only that 'no match' status" (git grep's exit 1) so a producer's genuine failure is never hidden — but a bare `||` cannot distinguish exit 1 from a fatal exit 2+ (git's own documented convention: 0 = match, 1 = no match, ≥2 = fatal error); both are silently treated as success. Two consequences, independently verified: (a) if `git grep` dies fatally with zero output already written, `[ -s "$popall" ]` catches it — a fail-closed outcome, but reported as "the live enumeration returned nothing," misdiagnosing an operational git failure as a legitimate empty population; (b) if `git grep` dies fatally *after* emitting a partial (non-empty) match list — plausible on a corrupted object, a resource limit, or a `git` version incompatibility mid-scan — the guard proceeds to judge that truncated population as if it were complete, and the two present-member positive controls (`bin/resolve-executor.sh`, `.github/workflows/check-handoff.yml`) would very likely still be in a partial list built from a lexicographically-ordered small population, masking the loss of any later-enumerated member that might actually forward `--adapters`. This is exactly the falsifiability direction the frozen `## Reviewer scope` asks for ("does it fail when its own search form is broken") and exactly the class this repository's own doctrine names ("Checkers must fail closed … suppressing a non-zero exit status to keep output tidy converts a real failure into a false pass" — `CLAUDE.md`). Both the primary Codex pass and the targeted adversarial pass independently converged on this finding (native `P2`, both times) without my prompting either toward it in the primary pass.
  - Fix: capture the exit status explicitly and only treat `1` as the documented empty-match case; any other non-zero status should `fail` loudly. E.g. `set +e; (cd "$REPO_ROOT" && git grep -l -e 'check-binding.sh' -- 'bin/*.sh' '.github/workflows/*.yml') > "$popall"; grc=$?; set -e; [ "$grc" -eq 0 ] || [ "$grc" -eq 1 ] || fail "cb-adapters-not-forwarded-population: git grep exited $grc (not a clean no-match)"`. This is a same-file, same-suite fix confined entirely to `tests/check-binding/run.sh` — it does not touch `bin/check-binding.sh`, does not change any refusal token, and does not require a re-freeze.
  - Not a design-premise defeat: this is the guard's **first** review round (the spec's own `## Pre-commitment` never-dropped/droppable framework triggers only on two consecutive rounds of new defects on the same component), so it routes back to engineering for a targeted fix rather than to the drop path.

#### Minor / nits

- **`## Input space`'s disclosed bound for the population guard names only the "indirection" class, not the narrower and more concrete "untracked file" class.** `git grep -l` (no `--untracked` flag) enumerates tracked and staged content only; a genuinely untracked, unstaged new `bin/*.sh` caller forwarding `--adapters` is invisible to the guard until `git add`-ed — independently reproduced (a throwaway `bin/fake-caller-probe.sh` was `NOT-FOUND` before `git add`, `FOUND` after). This never manifests at a merged commit or in CI (every file `actions/checkout` materializes is tracked by construction), which is why QA's own risk note correctly calls it "consistent with how a merged PR's files actually reach CI, not a gap" rather than a security hole — but it is a distinct reachable shape from the "indirection" class `## Input space`'s "Out-of-scope synthetic extremes" bullet actually names (a call site built at runtime or reached through a variable/`eval`), and a reader relying on that bullet's wording alone would not learn about the tracked-only boundary. Wording-only; no behavior change is owed.

#### Adversarial findings

A second, devil's-advocate `codex exec` pass (raw output: `.shell-team/reviews/T-1106-codex-adversarial.{txt,jsonl}`) targeted the three deliverables directly: the `read_lock()` embedded-body cross-check (issue #219 — tried to find a header-field ordering, duplicate-line, CRLF, or empty-body shape that either defeats the cross-check or wrongly refuses a legitimate lock), the three empty-value parse-arm refusals (issue #220 — tried repeated flags, a flag given twice, a `--` terminator, and combined flag forms), and the population guard's `|| true` (issue #221). It returned exactly one finding — the `|| true` fail-open gap above, independently corroborated at the same native severity (`P2`) as the primary pass. It found nothing against the body cross-check or the empty-value refusals, matching my own independent adversarial probing of those two surfaces (index-based exclusion survives arbitrary header-field position and permutation shape; every empty-value row refuses regardless of flag repetition order, a `--` terminator only affects post-terminator positional args as expected, and `--help`'s early exit is ordinary CLI precedent that no criterion depends on the absence of).

#### Synthesis audit ledger

no severity changes

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1106 entry to `READY_FOR_ENG` with the Major finding above linked.

#### Fast-follow disposition

- `## Input space`'s population-guard bound naming only the "indirection" class, not the narrower "untracked file" class (Minor / nits, above) — recommend **file-an-issue**: a low-cost wording addition to the spec's `## Input space` section (or a follow-up task's own Input space, since this spec's intent block is frozen and a wording-only re-freeze is disproportionate to reopen this task over) naming the tracked/staged-only boundary explicitly; not blocking because the actual behavior is already correct for the case that matters (a merged commit is always tracked) and QA's own risk note already surfaces it to any future reader of the board.

Raw Codex output saved at `.shell-team/reviews/T-1106-codex-primary.{txt,jsonl}` (primary review pass) and `.shell-team/reviews/T-1106-codex-adversarial.{txt,jsonl}` (devil's-advocate pass on the three deliverables).

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base 01df99f --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-carried - the shipped `review --base` subcommand takes no prompt argument at all (agents/codex-reviewer.md), so this round's own launch-prompt Reviewer-scope verdict-scope briefing (items (i)-(v)) has no channel into this pass; the same disclosed gap T-1104's and T-1105's own records already name for this identical subcommand shape
- raw-capture (primary): T-1106-codex-primary

- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate against bin/check-binding.sh and tests/check-binding/run.sh (branch feature/1106-check-binding-consistency, diff against base 01df99f719c7fc99efee985d8596110d4c8473bb). Read the files yourself with git diff / nl -ba -- do not expect any diff pasted here. Focus especially on: (1) the read_lock() embedded-body cross-check added for issue #219 -- try to find an input shape (header-field ordering, duplicate lines, CRLF, empty body) that either defeats the cross-check (a mutated body accepted) or wrongly refuses a legitimate lock; (2) the three empty-value parse-arm refusals for issue #220 -- try to find an invocation shape (repeated flags, flag given twice, -- terminator, combined short/long forms) that bypasses the refusal or produces an inconsistent message; (3) the cb-adapters-not-forwarded-population guard added for issue #221 in tests/check-binding/run.sh around its 'git grep -l ... > $popall || true' line -- assess whether that '|| true' can mask a genuine producer failure (not just the documented empty-match case) as a passing population guard, and how severe that is for a shipped CI-wired checker. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <RAW_JSONL> 2>&1
- pass-role (adversarial): confirmation
- briefing-fidelity (adversarial): carried - the three focus areas (body cross-check, empty-value refusals, population-guard fail-open), the branch name and the base-ref SHA were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (adversarial): T-1106-codex-adversarial

---

### Codex Review verdict: APPROVE — Round 2 (rework verification)
- Task: T-1106
- Codex model: gpt-5.6-luna — re-measured fresh from `~/.codex/config.toml`'s `model = ` line this round (unchanged from round 1); no `-m` override passed to either invocation this round.
- Codex CLI: codex-cli 0.149.1 — re-measured fresh via `codex --version` this round (unchanged from round 1).
- Verification mode: executed-tests — every claim below was independently re-executed by this reviewer (the fixture suite, the full spec sweep, both directions of the fix's own probe on a scratch worktree, and a base-ref worktree re-run of the two new Blast-radius rows), not accepted from the engineer's or QA's own numbers.
- Diff scope: `8c3af0c..HEAD`, 3 files, +74/-8 lines (`tests/check-binding/run.sh` — the fix; `.shell-team/specs/T-1106-check-binding-consistency.md` — two Blast-radius corrections, `5d53f2a` then `60e29f6`; `.shell-team/todo.md` — board appends). `bin/check-binding.sh` itself untouched — confirmed via `git diff --stat 8c3af0c..HEAD -- bin/check-binding.sh` (empty). Round 1's own settled ground (the body cross-check, the three empty-value refusals) was not re-opened.

#### Round-1 finding, closed

1. **Major (`cb-adapters-not-forwarded-population`'s `|| true` fail-open, issue #221) — CLOSED.** `tests/check-binding/run.sh:668-672` (`fcd0c76`) now captures `git grep`'s exit status explicitly (`set +e` / `grc=$?` / `set -e`) and accepts only `0` or `1`; any other status `fail`s loudly with the exit code named, **before** the population is ever judged (line 672 runs before the `[ -s "$popall" ]` non-empty check at 673) — so a fatal error emitting partial output before dying is still caught, regardless of what ended up in `$popall`. Independently reproduced both directions myself on a standalone probe of the exact fixed code (not accepted from the hand-off): a no-match pattern (`ZZZ_NO_SUCH_STRING…`) → `grc=1`, absorbed, graceful `FAIL: … the live enumeration returned nothing`; an invalid/fatal pattern (`git grep -e '['`, unbalanced bracket) → `grc=128`, **not** absorbed, loud `FAIL: … git grep exited 128 (not a clean no-match)`. Both match the engineer's and QA's own reproductions exactly. Both round-2 Codex passes (primary `review --base 8c3af0c` and a targeted adversarial pass on this exact fix) independently confirm the fix is sound — the primary pass returned no findings at all ("the change correctly distinguishes git grep's no-match status from fatal errors, and the modified shell script passes syntax and shellcheck validation"), and the adversarial pass, asked specifically to hunt residual gaps in this fix, found none in `tests/check-binding/run.sh` itself (its two findings are both about the spec text — see below, both evaluated and neither reopens this fix).

#### Full re-verification (independently re-run, not accepted from any hand-off)

`bash tests/check-binding/run.sh 2>&1 | tee <log>; grep -c '^PASS:' <log>` → **63**, `grep -c '^FAIL' <log>` → **0**. **Self-correction**: round 1's own record above states "28/28 `PASS:` lines" — that figure was read off a truncated `tail` of the suite's output rather than an explicit `grep -c`, and was wrong; the suite has always had 63 cases (unchanged case count across both rounds), never 28. Recorded here rather than silently fixed, per this repository's own quantity-claim discipline (the same class QA's own addendum self-caught and corrected in its first Blast-radius rewrite, below). `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1106-check-binding-consistency.md 2>&1 | tail -3` → `check-acs: 15 passed, 0 failed, 1 skipped, 0 unrecognized` (unchanged, still the required 15/0/1). `shellcheck bin/check-binding.sh tests/check-binding/run.sh` (0.11.0) → exit 0 on both. `bash bin/check-intent.sh .shell-team/specs/T-1106-check-binding-consistency.md .shell-team/todo.md` → `aligned: T-1106 v1 (b4033f899f442577a8aad3672c6bcdfc926a2232)` (unchanged hash — the Blast-radius corrections sit outside the frozen intent block, confirmed). `bash bin/check-handoff.sh .shell-team/todo.md`, `bash bin/check-board-headings.sh .shell-team/todo.md --base 01df99f719c7fc99efee985d8596110d4c8473bb`, `bash bin/check-pii-shapes.sh --base 01df99f719c7fc99efee985d8596110d4c8473bb` → all exit 0 / clean. Re-extracted and re-ran the spec's own **AC8** (resolve-executor consumer unaffected), **AC12** (all six merged T-1054/T-1055/T-1057 check lines) and **AC13** (scope lock) check lines directly — all exit 0, unchanged from round 1.

#### Blast-radius table corrections — verified honest against QA's own primary addendum, not against the spec's summary of it

Read `### QA addendum — T-1106` (`.shell-team/todo.md`, board commit `a1ef5d3`) first-hand rather than trusting the spec's transcription of it. **The corrected arithmetic matches the addendum's own lines exactly**: `predicted-red: 5` = 3 already-predicted `T-1105` rows (**AC15**/**AC16**/**AC18**) + 2 newly-found-by-QA's-completed-sweep rows (`T-1019` **AC10**, `T-1100` **AC13**) — both confirmed `base: PASS`, `head: FAIL` independently by me on a fresh `git worktree add --detach` at the base ref (`01df99f719c7fc99efee985d8596110d4c8473bb`) plus a live re-run at `HEAD`, matching the table's "newly reddened" classification exactly for both. The not-caused disposition (`7 = 4 + 2 + 1`: 4 telemetry-corpus-artifact specs, 2 board-checkbox-drift specs, 1 flaky) and its direction (the 6 methodology artifacts `base: FAIL → head: PASS`, favorable; only the 1 flaky, `T-1032`, in the opposite direction and non-reproducible in isolation) both match the addendum's own tally line (`.shell-team/todo.md:171`) and per-candidate bullets (`:167-169`) verbatim. **Noteworthy, and disclosed rather than smoothed over**: the first correction commit (`5d53f2a`) got this wrong — it described the 6 methodology artifacts as "five" (not six) and in the wrong direction (base-PASS→head-FAIL instead of base-FAIL→head-PASS), an error `pm-spec` itself caught and fixed one commit later (`60e29f6`) after re-reading the addendum at the primary artifact instead of through a relay of an interim summary. That correction chain is a proximate demonstration of exactly the discipline this repository's own lessons corpus already names (never trust a quantity claim reached through a relay) — the final state (what `HEAD` carries) is what I verified, and it is correct.

#### New findings this round (both from the round-2 adversarial Codex pass) — evaluated, neither reopens the Major

- **Codex-native `major`, `.shell-team/specs/T-1106-check-binding-consistency.md:94` (AC7's own inline `- check:` line) — evaluated and rejected as out-of-scope-for-Major.** Codex correctly observes that AC7's own inline check line still runs `git grep -l ... > "$T/all"` with no exit-status capture, so the same class of gap (fatal error vs. no-match, undistinguished) exists there too. This is real, but this spec's own `## Reviewer scope` — written and frozen before this task's first review round — explicitly adjudicates exactly this class: "Out of scope for a Major: adversarial guard-completeness of this spec's own inline `- check:` lines against hypothetical defeats … those lines are arm-B instruments whose subject is a `bin/` script's own behaviour, and the behaviour itself is covered under arm A by `tests/check-binding/run.sh`, which is where a completeness finding belongs." AC7's inline check line is precisely such an arm-B instrument (it re-derives the same property AC7 requires of the shipped suite, for freeze-time verification purposes only), and the real, adopter-and-CI-facing guarantee is the shipped suite's own case — which this round's fix already hardens. Rejected as a Major per the spec's own governing text, not by this reviewer's own discretion; not a nit worth a fix either, since strengthening a freeze-time-only verification instrument after the freeze is exactly the "no repair of any merged criterion" posture this task's own `## Non-goals` already declines for other specs' criteria, applied here to its own.
- **Codex-native `minor`, spec `:71` (Non-goals' "committed bytes" phrasing) — not new, already known and already filed.** This is the same tracked/staged-only `git grep` visibility gap round 1 already found, disclosed as Minor and deferred as fast-follow (filed as issue **#387** by the coordinating session, per this round's launch brief — relayed premise, environmentally-unverified: this role holds no `gh`/network access to confirm the issue exists or its number independently). Codex rediscovered the same underlying gap independently (attached to a different sentence — the Non-goals "committed bytes" phrase rather than round 1's `## Input space` bullet) without being told about round 1's finding, which is corroboration, not new information. No new disposition; #387 already covers it.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC7's own inline `- check:` line lacks the same exit-status guard the shipped suite now has | adversarial (round 2): `major` | rejected (not a Major, no fix owed) | the spec's own frozen `## Reviewer scope` explicitly excludes adversarial guard-completeness of its own inline check lines from Major severity — an arm-B instrument whose subject behaviour is covered under arm A by the shipped, now-fixed suite |
| 2 | `git grep`'s tracked/staged-only visibility, restated against the Non-goals "committed bytes" phrase | adversarial (round 2): `minor` | minor (unchanged) — already filed as issue #387 | round 2's own independent rediscovery of round 1's already-disclosed, already-deferred finding; no new severity, no new action |

#### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1106 entry to `READY_FOR_MERGE`.

#### Fast-follow disposition

- No new deferrals this round. The one carried-over item (the tracked/staged-only `git grep` visibility gap) was already dispositioned in round 1 as file-an-issue and — per this round's launch brief — has already been filed as issue #387 by the coordinating session; this round's independent rediscovery (finding 2 above) does not change that disposition or require a second filing.

Raw Codex output saved at `.shell-team/reviews/T-1106-codex-primary-r2.{txt,jsonl}` (round-2 primary review pass, delta `--base 8c3af0c`) and `.shell-team/reviews/T-1106-codex-adversarial-r2.{txt,jsonl}` (round-2 devil's-advocate pass on the fix itself).

- executor-invocation (primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base 8c3af0c --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (primary-r2): generation
- briefing-fidelity (primary-r2): not-carried - the shipped `review --base` subcommand takes no prompt argument at all (agents/codex-reviewer.md), so this round's own launch-prompt duties (re-reproduce round-1 Major, verify nothing new, confirm table-correction honesty) have no channel into this pass; the same disclosed gap round 1's own record and T-1104's/T-1105's records already name for this identical subcommand shape
- raw-capture (primary-r2): T-1106-codex-primary-r2

- executor-invocation (adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate against tests/check-binding/run.sh's fix for the cb-adapters-not-forwarded-population guard (round 2 of a review; the round-1 finding was: 'git grep -l ... > $popall || true' swallowed every git-grep exit status uniformly, masking a fatal error as a passing guard). Read the CURRENT file yourself with git diff 8c3af0c..HEAD -- tests/check-binding/run.sh, and nl -ba tests/check-binding/run.sh around the cb-adapters-not-forwarded-population case -- do not expect any diff pasted here. The fix now reads roughly: set +e; (cd $REPO_ROOT && git grep -l -e 'check-binding.sh' -- 'bin/*.sh' '.github/workflows/*.yml') > $popall; grc=$?; set -e; [ $grc -eq 0 ] || [ $grc -eq 1 ] || fail '...git grep exited $grc...'; [ -s $popall ] || fail '...returned nothing'. Try to find any residual gap: (1) does this still mask a fatal error if git grep emits PARTIAL matches before dying (does the exit-status check fire before or after the population is judged, and does it matter)? (2) any edge case in the exit-status capture itself (subshell exit-status propagation, the 'set +e'/'set -e' pairing, whether $? is captured from the right command)? (3) is there any other file this round's diff touched that I should also scrutinize (check git diff 8c3af0c..HEAD --stat yourself)? (4) any documentation/comment vs. behavior mismatch remaining. Return findings as a JSON array of {severity, file, line, issue, suggestion}. If you find nothing, return an empty array." > <RAW_JSONL> 2>&1
- pass-role (adversarial-r2): confirmation
- briefing-fidelity (adversarial-r2): carried - the round-1 finding being verified closed, the exact fixed code shape, and all four specific residual-gap questions were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (adversarial-r2): T-1106-codex-adversarial-r2
