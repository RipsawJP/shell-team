# Codex Review — T-1072

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1072
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 13 files, +880/-32 lines (`efc35e2...HEAD`)

#### Blockers
- none

#### Major
- `skills/run/SKILL.md:137-141` / `docs/loop-engineering/telemetry-span-discriminator.md:104-114` — the rehydration-checkpoint convention's own literal, `--parent-span-id <the span it belongs to>`, names no resolvable value. The row schema has no `span_id` field at all (confirmed: the frozen 21-key span row carries only `parent_span_id`, never a `span_id`); `span` is a repeatable role label, not a unique identifier, so two rows sharing one `--span` value in the same run (already reachable today across retries/iterations of one role, and squarely reachable once the fan-out this task's own discriminator exists for lands) cannot be told apart by this pointer. As written, a reader cannot actually reconstruct which specific parent span row a rehydration child belongs to — the convention promises a resolvable parent-child edge over an existing field and, as specified, does not deliver one. This maps to the pre-commitment's droppable **D5** (AC11); this is round 1 of that trigger. Suggested fix (cheap, text-only): point the placeholder at a composite that already exists on every row — the parent's own `run_id`+`seq` pair (e.g. `--parent-span-id <parent's seq value in this run_id>`) — rather than a bare role name.
- `templates/loop-replay.html:413-439,1104` — the new gate/orchestration-cost convention's literal, `--span orchestrator --phase gate`, collides with an explicit, load-bearing invariant this already-frozen (Non-goal, byte-identical per AC7) template documents about itself: *"orchestrator/human stay structural, never derived, since they are this loop's topology and emit no spans themselves"* (line 413-415). `RING_AGENTS`/`ringSeen` are seeded only from `KNOWN_RING_AGENTS`, which deliberately excludes `"orchestrator"`; a span row with `span:"orchestrator"` (which `adaptRow` turns into a `type:"work", agent:"orchestrator"` event at line 394) is not yet `ringSeen`, so it gets pushed into `RING_AGENTS` as a *dynamically discovered* ring agent (line 435-437) — even though `"orchestrator"` is already a fixed structural node. `ALL_NODE_IDS = RING_AGENTS.concat(["orchestrator","human"])` (line 1104) then lists `"orchestrator"` twice with no dedup; both entries resolve to the same `worldPos("orchestrator")===ORCH_POS`, so the first real gate-span row this convention produces draws two overlapping/duplicate orchestrator circles (duplicate hit-targets too) and inflates `RING_AGENTS.length`, skewing `RING_ANGLE_STEP` and therefore the screen position of every other legitimate ring agent (tech-lead/pm-spec/engineer/qa-verifier/codex-reviewer/ui-designer). This is a genuine, deterministic regression the first time the convention this task ships is actually followed — not a hypothetical fan-out scenario — and it was not caught by **AC7**, which only proves byte-identity plus a `rollup-runs.sh`/`cluster-failures.sh`/`check-run.sh` behavioral check; it never exercises `gen-loop-replay.sh`/`templates/loop-replay.html` against the new *span-name value* this task introduces (the spec's own Reader-inventory/Non-goals reasoning for this file is scoped to "an unknown **key**", never to "a new **value** of the existing `span` field"). This maps to the pre-commitment's droppable **D3** (AC10), gated behind D4+D5 already having gone; this is round 1 of the D3/D4/D5 trigger, reported now rather than waiting. Fixing this without touching the frozen template means choosing a convention span-name that isn't a reserved structural id (e.g. `orchestrator-gate` instead of `orchestrator`) — which touches the frozen Goal/AC10 literal and would need the ratified class-B re-freeze path; alternatively, disclose the limitation explicitly and fast-follow a `loop-replay.html` fix.

Both findings are about the two droppable prose/convention components the spec's own pre-commitment already anticipated (D3, D5); neither touches the never-dropped floor — **AC1-AC4** (discriminator field), **AC9** (parity suite, re-run green), or **AC2/AC6/AC7** (backward-compat, all independently re-verified green by this review, see below). All 17 checkable ACs, `shellcheck`, and the three touched suites (`tests/log-run/run.sh`, `tests/check-run/run.sh`, `tests/is-span-row-parity/run.sh`) were independently re-run by this review and are green.

#### Minor / nits
- `README.md:173` / `README.ja.md:170` — "a per-instance fan-out's hand-off records stay attributable to the instance that emitted them" reads as though attribution is delivered now; in fact `instance` is span-only (never on the `--event handoff`/`rework` row itself, per Non-goals), so attribution rides an *implicit* nearest-preceding-span correlation (the same pattern T-1058's `review:` line already uses) that this task documents nowhere and no consumer implements (Non-goals: "nothing consumes `instance` in this task"). Not a defect in scope, but the wording could be read as overclaiming; a one-clause hedge ("...records stay attributable once a consumer correlates them by proximity") would remove the ambiguity.
- `bin/rollup-runs.sh` (unedited, per-run aggregation) — the new `--span orchestrator --phase gate` / `--span engineer-rehydration` rows are counted as ordinary sub-agent spans by the existing, untouched aggregation (`n_spans`, `phases`, `tok_partial`/`dur_partial` flags): a gate-span with no `--tokens` will mark an otherwise-complete run `(partial)`, and the rollup's `spans:` population silently becomes a mixture of sub-agent calls, gate bookkeeping and rehydration checkpoints. Nothing breaks (AC7 verified this stays `exit 0`), but the per-run summary's meaning shifts subtly. Worth a follow-up: a validated span "class" (`agent` / `orchestration` / `checkpoint`) so `rollup-runs.sh` can report populations separately.
- `skills/run/SKILL.md:129-135` — the gate-span row and its paired `--event gate`/`--event human` row are documented as "emitted alongside" sharing `run_id` and an incrementing `seq`, but no explicit adjacency/correlation rule is spelled out for a reader trying to join them (which `--duration-ms` belongs to which gate outcome). Low current impact (no consumer joins them yet), but worth spelling out explicitly before any consumer is built.
- `templates/loop-replay.html` — `instance` and `parent_span_id` are both dropped by the existing fixed projection, so concurrent same-role spans render as one indistinguishable node and `engineer-rehydration` renders as an unrelated sibling agent rather than a nested child. Already substantially covered by this task's own disclosed Non-goal ("no replay UI for the new field"); noted here only so the eventual replay-support follow-up has the concrete gap list.

#### Adversarial findings
A dedicated `codex exec` "devil's advocate" pass was run against `skills/run/SKILL.md`, `docs/loop-engineering/telemetry-span-discriminator.md`, `bin/log-run.sh` and `bin/check-run.sh` (triggered by the primary pass's Major finding, per this role's contract). It returned 11 findings, native-labelled `high`/`medium`. The two genuinely load-bearing ones are folded into `#### Major` above (the `parent_span_id` resolvability gap, independently corroborated by the primary pass, and the `loop-replay.html` orchestrator-node collision, independently verified by this review by reading the template's own JS). The remainder either restate this task's own disclosed Non-goals/Input-space carve-outs (concurrent-writer/duplicate-`seq` semantics belong to the successor locking car; nothing consumes `instance` yet by design) or are speculative concerns about a fan-out capability that does not exist operationally yet (issue #277 preconditions 2/3 are still open, undesigned) — see the ledger below for the full per-finding disposition.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `parent_span_id` has no resolvable value for the rehydration convention | primary: P2 (→major); adversarial: high (→major) | major | corroborated by both passes and by this reviewer's own reading of the row schema (no `span_id` key exists anywhere); kept at the mapped severity. |
| 2 | `check-run.sh` performs no cross-line `(run_id,seq)` uniqueness/parent-existence validation | adversarial: high (→major) | minor (out-of-input-space) | the spec's own `## Input space` Out-of-scope line: "Concurrent writers appending to one telemetry file... duplicate `seq` values and lock contention are the successor car's subject by explicit split; a finding in this task demanding a lock, a retry or a `--seq` assignment mode is out-of-input-space and belongs on that car's issue." |
| 3 | canonical span command never passes `--instance`; a compliant fan-out could leave it null everywhere | adversarial: high (→major) | minor (out-of-input-space) | Input space Out-of-scope: "Consumption of the new field... a finding that nothing reads it yet is a correct description of this task's declared scope, not a defect in it," plus the Non-goal declining any `agents/*.md`/prompt-block edit in this task. |
| 4 | `instance` is span-only, so hand-off/rework events can't carry it, arguably contradicting the README's "stay attributable" wording | adversarial: high (→major) | minor | judgment downgrade: the underlying nearest-preceding-span correlation mirrors T-1058's own precedent and is not a fresh contradiction, only an unstated-in-prose mechanism; folded into `#### Minor / nits` as a wording-precision note. |
| 5 | gate-span row and its paired event row have no explicit correlation key beyond adjacent `seq` | adversarial: high (→major) | minor | judgment downgrade: no consumer joins them yet (Non-goals/Input-space both disclaim consumption and enforcement of this convention); folded into `#### Minor / nits`. |
| 6 | `rollup-runs.sh`/`cluster-failures.sh` grouping semantics break under concurrent fan-out (crossed instance/reviewer correlation) | adversarial: high (→major) | minor (out-of-input-space) | Input space Out-of-scope: "Consumption of the new field... nothing consumes it... not a defect," and concurrent fan-out is not an operational capability yet (issue #277 preconditions 2/3 still open, undesigned). |
| 7 | `rollup-runs.sh` counts new orchestration/checkpoint spans as ordinary spans, polluting per-run population/partial flags | adversarial: high (→major) | minor | judgment downgrade: real but non-blocking side effect (AC7 confirms `exit 0` throughout, nothing corrupted); folded into `#### Minor / nits` as a fast-follow. |
| 8 | gate-span `status` semantics undefined relative to the paired event's outcome label | adversarial: medium (→minor) | minor | kept at mapped severity, no change. |
| 9 | `loop-replay.html`'s fixed projection drops `instance`/`parent_span_id`, so fan-out spans and the rehydration child render without nesting | adversarial: high (→major) | minor | judgment downgrade: already substantially covered by this task's own disclosed Non-goal ("no replay UI for the new field"); folded into `#### Minor / nits`. |
| 10 | `templates/loop-replay.html`'s `ringSeen`/`RING_AGENTS` derivation double-registers `"orchestrator"` once a real `--span orchestrator` row exists, corrupting ring layout | adversarial: high (→major) | major | independently verified by this reviewer reading the template's own JS (lines 413-439, 1104); kept and promoted into `#### Major` above. |
| 11 | `loop-replay.html`'s `eventBySeq` overwrites on duplicate `seq`, which the real corpus already contains | adversarial: high (→major) | minor (out-of-input-space) | Input space Out-of-scope line for duplicate-`seq`/concurrent-writer issues (same citation as row 2); pre-existing and unrelated to this task's own edits — `gen-loop-replay.sh`/`loop-replay.html` are verified byte-identical to the branch point (AC7). |
| 12 | `loop-replay.html` forces concurrent/nested spans into a serial cumulative timeline, double-counting duration once nested children exist | adversarial: high (→major) | minor | judgment downgrade: speculative future concern — concurrent fan-out and the rehydration child's own duration-bearing usage are not operational yet; folded into `#### Minor / nits` as a longer-horizon fast-follow. |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings

This review's verdict is **REQUEST_CHANGES**, on the two Major findings above. Independent verification performed by this review (not merely re-reading the engineer's/QA's claims): all 17 checkable ACs re-run live via `check-acs.sh` (17 passed, 0 failed, 1 skipped, 0 unrecognized); `shellcheck` on all four touched files (clean); `tests/log-run/run.sh`, `tests/check-run/run.sh`, `tests/is-span-row-parity/run.sh` (all exit 0); `check-intent.sh`, `check-provenance.sh`, `check-interventions.sh` (all conformant). The frozen-prefix, count-settlement, event-row-freeze and backward-compatibility criteria (AC1-AC9) are solid; the gap is confined to the two prose conventions this task's own pre-commitment already flagged as its most speculative surface.

#### Fast-follow disposition
- `README.md`/`README.ja.md` "stay attributable" wording precision (Minor #4 above) — won't-fix for this round (cosmetic wording; the underlying correlation mechanism is sound and matches existing T-1058 precedent), but worth a one-clause hedge whenever either README's telemetry bullet is next touched.
- Gate-span/event correlation key (Minor #5) — file-an-issue: needed before any consumer actually joins a gate-span row to its paired event row; no consumer exists yet so no urgency.
- `rollup-runs.sh` counting orchestration/checkpoint spans as ordinary spans (Minor #7) — file-an-issue: a validated span "class" so per-run populations stay meaningful once gate-span/rehydration rows are common in real corpora.
- Gate-span `status` semantics vs. the paired event outcome label (Minor #8) — file-an-issue: define this before any failure-clustering consumer reads gate-span rows.
- `loop-replay.html` dropping `instance`/`parent_span_id` from its fixed projection (Minor #9) — won't-fix for now (already disclosed in this task's own Non-goals as "no replay UI for the new field"); revisit together with any future replay-support follow-up.
- `check-run.sh` cross-line `(run_id,seq)` validation and duplicate-`seq` `eventBySeq` overwrite in `loop-replay.html` (Minor #2, #11) — won't-fix here: both are explicitly the successor append-locking/`--seq`-assignment car's subject per this task's own Non-goals and interventions record.
- `loop-replay.html`'s serial-cumulative timeline under concurrent/nested spans (Minor #12) — file-an-issue: relevant only once real concurrent fan-out ships (#277 preconditions 2/3), not before.
- `instance` never populated by the canonical span-emission command, and `rollup-runs.sh`/`cluster-failures.sh` fan-out grouping semantics (Minor #3, #6) — won't-fix here: both are explicitly declared out of this task's scope in `## Non-goals`/`## Input space` (no agent/prompt-block edit, no new consumption of the field in this task).

---

## Round 2 — closure verification

### Codex Review verdict: APPROVE
- Task: T-1072
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 3 files, +193/-5 lines (`c4efc16..9b79701`, rework round 1 against round 1's review)

#### Blockers
- none

#### Major
- none (both round-1 Majors closed — see below)

#### Closure judgment

**Major 1 (D5, AC11 — `--parent-span-id` had no resolvable value): CLOSED.** The rework redefines the field honestly as a **name-reference within the run, not a pointer** — the parent's own `--span` value, explicitly grounded against the writer (`bin/log-run.sh`'s `PARENT="$2"` capture and unconditional `jstr parent_span_id "$PARENT"` emission, quoted verbatim in the note; independently re-verified by this review — no `=~` check exists anywhere between capture and emission, confirmed). Critically, the new text also drops the round-1 overclaim: it now says explicitly "best-effort correlation... **never a guaranteed unique edge**." That disclaimer is the load-bearing repair — round 1's actual defect was a false claim of resolvability, not merely an imprecise one, and that false claim is gone.
- **New finding surfaced this round, folded in below at Minor**, not Major: the disambiguation clause (scope by `run_id`, then by `iteration`/`attempt`) never cites `instance` — the field this same task ships — as an available disambiguator, even though `iteration`/`attempt` provably cannot distinguish two *concurrent* same-role instances (they necessarily share one iteration/attempt pair), which is exactly the case `instance` exists to solve. Both a round-2 Codex primary pass and this reviewer's own reading independently confirm the underlying technical gap is real. **Severity resolution:** a second, adversarial-framed Codex pass argued the counter-position and this reviewer independently agrees with it on reflection — because the text already, explicitly, disclaims a guaranteed unique edge, a reader is never told a false certainty; omitting `instance` is a **precision gap in an already-honest disclaimer**, not a repeat of round 1's overclaiming defect. That distinction is what keeps this Minor rather than Major. (This reviewer's two Codex invocations disagreed here — the strict closure-framed primary pass called it `major`/`OPEN`, the steelman-framed adversarial pass called it `minor` after arguing both sides; this review's own judgment, reasoned above, sides with the adversarial pass's severity, not the primary pass's.) See `#### Minor / nits` below.

**Major 2 (D3, AC10 — `--span orchestrator` collides with `templates/loop-replay.html`'s structural-node invariant): CLOSED via disclosure + follow-up.** This is not a code fix (the template stays frozen, correctly, per AC7/Non-goals) but a **measured**, not merely predicted, disclosure. This reviewer independently reproduced the measurement from scratch — a standalone Node script re-implementing `adaptRow`/`RING_AGENTS`/`ringSeen`/`ALL_NODE_IDS` verbatim from the current template, run against a synthetic payload shaped like the note's own description — and got the identical numbers the note and board both report: `RING_AGENTS.length` 6→8, `RING_ANGLE_STEP` 60°→45°, `"orchestrator"` appearing twice in `ALL_NODE_IDS`. The severity sentence ("real and worse than purely cosmetic, not fatal") matches this independent measurement exactly — not rounded up, not rounded down. Given the fix requires either violating this task's own Non-goal (editing the frozen template) or a class-B re-freeze of the AC10 literal (neither of which is a same-cost rework), disclosure-plus-a-tracked-follow-up is the judgment call this review offered as an acceptable alternative in round 1's own finding text, and it is accepted as such here.

#### Pre-commitment trigger — stated explicitly, as asked
**Did not fire.** The trigger requires two consecutive rounds of independent Blocker/Major findings against the *same component*. D3 closed clean this round (no Major). D5 produced a second round of scrutiny, but this review's own severity judgment (above) places its round-2 residual at Minor, not Major — so D5 does not accumulate a second Major round either. Both droppable components (D4 — never built in this task, declined at spec-authoring time, vacuously already "gone" — and D5) remain available should a genuine third-round Major recur; neither is consumed by this round.

#### Minor / nits
- `skills/run/SKILL.md:139` / `docs/loop-engineering/telemetry-span-discriminator.md:148-153` — the rehydration disambiguation clause cites `run_id` + `iteration`/`attempt` but never `instance`, even though `instance` is the one field that actually distinguishes two concurrent same-role parents sharing one iteration/attempt (the exact fan-out case this task's discriminator exists for). Cheap fix: extend the scoping clause and the example command to also carry/match on the parent's `--instance` value (the same way `iteration`/`attempt` are already carried contextually, not as a producer-identity claim). Not required this round — no consumer reads this correlation yet and today's serial (one-instance-per-role) operation already resolves unambiguously without it — but worth closing before any real fan-out ships.
- (carried over, unresolved from round 1, still accurate) `bin/rollup-runs.sh` counting new orchestration/checkpoint spans as ordinary spans (round-1 Minor #7) and gate-span/paired-event correlation-key ambiguity (round-1 Minor #5/#8) remain open fast-follows; the round-1 disposition stands unchanged.
- `tests/gen-loop-replay/` — independently confirmed this review: no existing fixture in that suite exercises a `"span":"orchestrator"` row, so the collision measured in this round's rework is not (and, per this task's Non-goals, is not meant to be) caught by CI. Worth a fixture the moment the template-side fix is scheduled.

#### Adversarial findings
A second `codex exec` pass, framed to steelman *both* positions on the `instance`-disambiguator gap, argued: (for "fine as-is") serial per-role operation today is unambiguous and blindly copying a parent's `instance` onto an orchestrator-emitted checkpoint row risks implying the checkpoint was produced by that instance rather than being merely about it; (against) `iteration`/`attempt` structurally cannot discriminate concurrent same-role parents, which is exactly the case `instance` exists to solve. Its own final judgment: **minor**, "a documentation/design limitation rather than a merge-blocking Major," because no consumer or operational fan-out exists yet and current serial correlation already works. This reviewer's synthesis (above) adopts that judgment over the stricter round-2 primary pass's `major`/`OPEN` call, for the reason stated: the text's own explicit "never a guaranteed unique edge" disclaimer means no false certainty is shipped, which is what actually matters given a human, not a checker, reads this convention.

#### Environmentally-unverified disclosure
QA's own hand-off names one item honestly: "page stays navigable" was confirmed only at the Node/JS-logic level (the extracted `adaptRow`/`RING_AGENTS` derivation re-run headlessly), never against a full browser DOM render. This review could not close that gap either — no browser automation is available in this role's read-only sandbox, and running one is outside this role's scope regardless. Recorded here rather than silently assumed: the "no JS exception, page stays navigable" claim is **environmentally-unverified at the full-DOM level** (mirroring qa-verifier's own convention for the identical reason — unreachable, not nonexistent) and rests on the Node-level logic reproduction, which this review did independently re-derive from scratch and which matches.

#### Independent re-verification performed this round
`check-acs.sh` on the full spec (17 passed, 0 failed, 1 skipped, 0 unrecognized); `check-intent.sh` (aligned, hash unchanged `fe964bf4...`, confirming the frozen block itself was untouched by this rework); `shellcheck` on all four touched scripts (clean); `tests/log-run/run.sh`, `tests/check-run/run.sh`, `tests/is-span-row-parity/run.sh`, `tests/gen-loop-replay/run.sh` (all exit 0); `git diff --stat efc35e2...HEAD -- templates/loop-replay.html bin/gen-loop-replay.sh` (empty — still byte-identical, AC7 intact); a from-scratch Node reproduction of the ring-layout collision (matches the note/board's numbers exactly). The full CI-wired workflow re-run and the AC18 mutation matrix were not independently re-run this round (proportionate to a closure-verification pass over a text-only, 3-file rework — the mechanical surfaces that could have regressed from this rework are the ones re-verified above).

#### Synthesis audit ledger (round 2)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Major 1 (`parent_span_id` unresolvable) | — | closed | rework repairs the overclaim with an honest name-reference + explicit non-uniqueness disclaimer, independently verified against the writer's own code. |
| 2 | missing `instance` citation in the rehydration disambiguation clause | round-2 primary: major/OPEN; round-2 adversarial: minor | minor | this reviewer's own judgment sides with the adversarial pass: the text already disclaims a guaranteed unique edge, so no false certainty ships; the gap is a precision improvement for a not-yet-operational fan-out case, not a repeat of round 1's overclaiming defect. |
| 3 | round-1 Major 2 (`--span orchestrator` vs. frozen `loop-replay.html`) | — | closed (disclosure + follow-up) | independently re-measured from scratch (Node reproduction) and matches the note/board exactly; the severity sentence is accurate, not softened; the frozen-template constraint makes disclosure+follow-up the correct disposition, per this review's own round-1 offered alternative. |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings

Verdict: **APPROVE**. The board flag is left unchanged by this review, per this role's write boundary; the orchestrator records the transition.

#### Fast-follow disposition
- Rehydration disambiguation clause omitting `instance` (Minor, this round) — file-an-issue: cheap text-only fix (extend the scoping clause and example command in both `skills/run/SKILL.md` and the note), needed before any real same-role fan-out ships; not urgent today since serial per-role operation already resolves unambiguously.
- `templates/loop-replay.html` orchestrator/rehydration ring-layout collision (round-1 Major 2, now disclosed) — file-an-issue: this task's own text already commits to filing this follow-up; recorded here so the orchestrator's close-out step has this review's independent confirmation that the disclosure is accurate and the follow-up is warranted.
- Carried over from round 1 unchanged: `bin/rollup-runs.sh` span-class pollution (Minor #7), gate-span/event correlation key (Minor #5), gate-span `status` semantics (Minor #8), `loop-replay.html` dropping `instance`/`parent_span_id` from its projection (Minor #9), serial-cumulative timeline under concurrent spans (Minor #12) — all file-an-issue or won't-fix exactly as dispositioned in round 1; no change this round.
- `tests/gen-loop-replay/` gaining a `"span":"orchestrator"` fixture — file-an-issue: bundle with the `loop-replay.html` template fix above rather than filing separately.
