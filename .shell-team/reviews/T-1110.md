### Codex Spec-Review verdict: REQUEST_CHANGES

## Spec review

**Mode**: Specify-seam spec-review (T-1092), round 1. No implementation exists yet — this reviews the frozen-candidate intent block of `.shell-team/specs/T-1110-freeze-version-derivation.md` plus its board declaration region, per the Routing Map's `spec-review — cross-provider` election for this task.

- Task: T-1110
- Codex model: gpt-5.6-luna (model_reasoning_effort=max, resolved from `~/.codex/config.toml` default; no `-m` override was passed)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `.shell-team/specs/T-1110-freeze-version-derivation.md` intent block + declaration region; `.shell-team/todo.md`'s `**T-1110**` entry; `.shell-team/interventions/T-1109.md` entries 5-6 (read first-hand); `CONTRIBUTING.md:110-132`; `CHANGELOG.md` top entries; `skills/run/SKILL.md:51-61` (freeze-sweep branch, read first-hand to locate the T-1103 precedent this spec cites at `:56`).
- raw-capture: `T-1110-codex-specreview.jsonl` / `T-1110-codex-specreview.txt`
- executor-invocation: `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Review the spec document at .shell-team/specs/T-1110-freeze-version-derivation.md for its DOMAIN premises, not its prose polish. Read the file yourself — do not expect its text in this prompt. Also read .shell-team/interventions/T-1109.md (entries 5-6), CONTRIBUTING.md's '## What a version number encodes' section, and CHANGELOG.md's top entries for version-tier precedent. Judge (1) whether its correctness rests on a fact this repository can verify rather than an unverifiable assumption about deployment order, a rollback precondition, or a system outside this repository's reach, and (2) whether its acceptance criteria test the right thing rather than merely testable things. Specifically also judge: whether the spec's claim that this mechanism is 'the existing scope-change stop-and-raise applied at the correct moment rather than a fourth human gate' is actually grounded in any pre-existing SHIPPED (public repo) mechanism, or whether it borrows an unshipped private-operator convention and presents it as pre-existing; and independently re-derive whether this task's own shipped version-tier claim (PATCH) is correct per CONTRIBUTING's joint headline/default-reachability test. Return findings as a JSON array of {severity, criterion, issue, suggestion}." > "<RAW_JSONL>" 2>&1`

### Independent primary reading (before consulting Codex's output)

I read the spec's intent block end to end (`## Goal` through `## Input space` line 118), `.shell-team/interventions/T-1109.md` in full (6 entries), `CONTRIBUTING.md:110-132`, `CHANGELOG.md`'s top ~9 releases, and `skills/run/SKILL.md:51-61` first-hand (grepped for the five sibling gates and the T-1103 gate at `:56`, and for `class-M` at `agents/pm-spec.md:102` / `agents/codex-reviewer.md:222`), before reading Codex's output, specifically to attack the two load-bearing premises the brief named (A-7 and the derived PATCH tier) and the "not a fourth human gate" claim independently.

**Independent finding on the "existing scope-change stop-and-raise" claim.** I grepped the shipped, public surface for this phrase and its close variants:
```
grep -rli "scope-change\|scope change\|stop-and-raise\|stop and raise" skills agents docs CONTRIBUTING.md README.md .shell-team/lessons.md
```
— zero matches anywhere in the shipped repository. `skills/run/SKILL.md:187` and `:207` do declare an `invariant-lock: human-gate-set` naming exactly **three** standing human gates ("the sprint's own batch GO, planning approval, and confirmation before a destructive or irreversible operation") — this is genuinely shipped — but none of the three, as shipped, describes a mid-task freeze-time stop triggered by a version-tier mismatch against an approved plan. The only place the phrase "scope-change stop-and-raise" appears anywhere in this repository is `.shell-team/interventions/T-1109.md` entry 6, which explicitly attributes it to "the standing rule (CLAUDE.local: stop-and-raise applies to scope changes)" — `CLAUDE.local.md` being, by this repository's own stated convention, a gitignored, per-operator private file, never a shipped artifact. Codex's own independent read (below) reached the same conclusion via its own repo-wide grep, run before I compared notes with it.

I concur this is a genuine domain-premise gap, not a wording nit: the Goal sentence and Non-goals both use "the existing scope-change stop-and-raise" as the ground for "not a fourth human gate", and that ground is baked verbatim into a **shipped, adopter-facing artifact** (`skills/run/SKILL.md` via AC1, and both `docs/adopting.md`/`.ja.md` mirrors via AC10) that every adopter of this plugin will read — not only this operator's own checkout. AC6's mechanical half (`- templates/` diff empty, `check-handoff.sh` green) verifies the flag-enum and board-grammar surface are untouched, which is true and necessary but not sufficient: it cannot verify the truth of the *prose claim* that this is an application of something pre-existing, because no shipped artifact anywhere states that a mid-task, unconditional (`user-visible: yes`, no opt-in), freeze-time stop-and-ask-the-human mechanism already existed. For an adopter with no equivalent private convention, this fixed step is, in every operationally meaningful sense, a **new** interruption point wired unconditionally into the shipped default loop.

**Independent version-tier re-derivation.** Re-applying `CONTRIBUTING.md`'s joint test from the measured precedent table (`CHANGELOG.md:8,21,37,43,53`) and the spec's own two-reading argument: headline test — met (a stop-and-tell-the-adopter-the-work-outgrew-the-plan benefit is real and nameable). Default-reachability test — genuinely contestable, not "not established" beyond dispute: the test's letter is about an "explicit opt-in", and this mechanism has none; the spec's "precondition-latency" reading (a benefit gated on an artifact — an approved planning premise — the project neither scaffolds, configures, nor documents) is a reasonable, disclosed extension of the test's *purpose* rather than its literal wording, and A-7 honestly flags itself as the attackable premise. I do not have grounds to force this to a firm MINOR verdict, but I agree with Codex (below) that PATCH is the defensible-conservative rather than the demonstrably-correct answer, and that the spec's own bias disclosure, while honest, understates how load-bearing A-7's unverified "majority of adopters" claim actually is for the joint test's second leg.

### Codex's independent findings (raw JSON in `T-1110-codex-specreview.txt`)

Codex was launched fresh (no prior exposure to my own reading above) and independently re-derived the version tier and independently grepped the repository for the "scope-change stop-and-raise" claim (visible in its own tool-call trace inside the JSONL) before returning six findings. Verbatim severities from Codex's own output already used this project's four-level vocabulary directly (`Blocker`/`Major`/`Minor`), so no P0/P1-style relabeling was needed; each is carried through the normalization map unchanged (a native `Blocker`/`Major`/`Minor` label maps to itself as the direct, unambiguous reading of "must-fix" / non-must-fix-but-serious / lower-priority, respectively — no rounding-up was required since no out-of-map vocabulary appeared).

1. **[blocker]** `AC6; Goal; Non-goals` — the claimed pre-existing "scope-change stop-and-raise" is not a shipped public mechanism; public workflow/skill docs enumerate exactly three human gates and none is a scope-change stop; the cited authority (`T-1109` entry 6) points to gitignored private `CLAUDE.local.md`. A freeze-time mismatch stop is therefore a new human interruption point, in substance a fourth gate. — **Independently corroborated above; upheld as blocker.**
2. **[major]** `A-2; Goal; AC4, AC7, AC8, AC13` — the approved-version premise this mechanism compares against is session-held prose, never a tracked or machine-readable repository input; the no-premise path lets its own absence erase the comparison, so the claimed approval-lapse protection rests on an external operator convention this repository cannot verify. — *Downgraded from Codex's own `Blocker` rating to major*, reasoned below.
3. **[major]** `AC1-AC4, AC7, AC12, AC13, AC15` — the acceptance criteria are almost entirely presence/grammar/ordering greps over the gate bullet's own prose; none demonstrates the domain behavior itself (a real match, a real mismatch that actually stops work, a real no-premise outcome), and AC15 explicitly makes the independent re-derivation and premise re-measurement reporting-only with no acceptance verdict turning on them. — upheld as major.
4. **[minor]** `base-ref-discriminator; AC11, AC12, AC14; A-3` — the discriminator's third arm computes `develop`'s merge-base whenever neither expected predecessor ref resolves, and cannot distinguish a genuinely-gone predecessor from a fetch-depth gap, a rebase, or a force-push. — *Downgraded from Codex's `Major` rating to minor*, reasoned below.
5. **[minor]** `DP-1; AC9; AC15(e,g)` — the prose-only/no-checker decision (DP-1 Arm A) rests on a relayed GitHub issue qualifier that is never independently preserved as an authoritative repository record; absence checks show no checker was *added*, not that deferring one is the *correct* decision. — upheld as minor, Codex's own rating unchanged.
6. **[major]** `## This task's own version derivation (lines 151-158); A-7; AC15(f)` — independent re-derivation: headline=met; default-reachability is not *provable* from shipped evidence one way or the other, because A-7 ("most adopters lack an approved planning premise") is explicitly unverified; PATCH is the defensible-conservative tier but is not proven, and treating the premise as ordinary default planning input would flip the joint test to MINOR. — upheld as major, matches my own independent re-derivation above.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | "existing scope-change stop-and-raise" / not-a-fourth-gate claim ungrounded in any shipped mechanism | Codex: blocker; my independent primary reading: blocker (concurred before comparing) | blocker | Both passes independently grepped the whole shipped repo and found zero matches for the cited "existing" mechanism outside this operator's private, gitignored `CLAUDE.local.md`; the claim is load-bearing for the frozen Goal sentence and ships verbatim into public, adopter-facing text (SKILL.md + both adopting-guide mirrors) that AC6 cannot verify the truth of, only its presence. |
| 2 | approved-premise is unverifiable session-held prose, undermining approval-lapse enforcement | Codex: blocker | major | The spec's own Non-goals explicitly anticipates and rejects exactly this objection ("A machine-readable planning-premise file... a finding that the gate 'should parse a premise file' is a scope proposal, not a defect"), and this residual is disclosed on the same honest-disclosure footing the five already-shipped sibling declaration gates use for their own unauthenticatable, agent-produced records. A disclosed, previously-litigated, house-style trade-off is a major concern about the mechanism's real-world bite, not a blocker on this spec's own text. |
| 3 | acceptance criteria test wording/grammar, not match/mismatch/no-premise behavior | Codex: major | major | Consistent with `verification-class: no-mechanism` and DP-1's prose-arm resolution (both disclosed, cited against issue #400's own cost qualifier), but the operator's own ruling for this task explicitly demanded a *structural* fix for a *diligence*-class failure; a purely prose-verified gate risks re-enacting the diligence gap in a new location. Kept at major rather than upheld silently. |
| 4 | base-ref-discriminator third-arm silent fallback to `develop` | Codex: major | minor | This is inherited, byte-identical boilerplate from the already-shipped, already-reviewed T-1081 gate (`skills/run/SKILL.md:52`), used unchanged by numerous already-merged specs; the spec's own prose (Assumption row, base-ref-discriminator declaration) explicitly disclaims silent fallback and requires the reader to classify an unresolvable-in-either-namespace predecessor as a "fetch-depth gap" routed back to planning, never as an accepted third-arm value. A systemic critique of a project-wide convention is out of this one task's scope (Non-goals do not touch base-ref mechanics) and belongs as a fast-follow against T-1081's own gate, not a blocker on T-1110. |
| 5 | DP-1's relayed qualifier not durably preserved as an authoritative record | Codex: minor | minor | No change — Codex's own rating stands; consistent severity, no rounding needed. |
| 6 | own version-tier derivation (PATCH) rests on an unverified A-7 majority claim | Codex: major; my independent re-derivation: same conclusion (PATCH defensible, not provable) | major | Two independent reads reached the same conclusion by different routes; kept at major since the spec's own text already invites exactly this attack (A-7's own disclosure names it as the surface "a reviewing gate should attack first") and both reviewers found the invitation well-placed. |
| 7 (mine) | AC2's "a mis-classified class-M that did move the declaration is caught rather than waved through" | primary only | minor | Verified against `agents/pm-spec.md:102` / `agents/codex-reviewer.md:222`: class-M is confined to `- check:` lines by `bin/check-refreeze-class.sh`'s own classifier, so a declaration-moving edit is `class-b` by that checker, not `mechanics` — the *primary* defense against a mis-classified class-M is `check-refreeze-class.sh` itself, not this new gate. This gate only "catches" the case where the moved declaration also happens to flip the derived tier against the recorded premise; it is not a categorical backstop as worded. Not blocking — a wording precision issue on an already-disclosed defense-in-depth claim. |

### Independent version-tier verdict

**PATCH — concur with the spec's own derivation, but flag it as defensible-conservative rather than settled.** Headline test met; default-reachability test genuinely contested (A-7's unverified "majority of adopters have no approved planning premise" is load-bearing and the spec's own text already flags it as the correct attack surface). Both this review's primary pass and Codex's independent pass reached PATCH by the same route and flag the same residual uncertainty. Recommend the freeze run's mandatory AC15(f) independent re-derivation treat A-7 as a live, unresolved question rather than a formality, and recommend citing v2.2.1 (`CHANGELOG.md:21` — a freeze-time cross-check between two governance-relevant values that refuses/notifies on mismatch, PATCH, default-reached with no opt-in) as a structurally closer precedent than v2.0.1's simple declaration-presence gate, since v2.2.1 is the same *shape* of mechanism (a comparison + refusal/notice) this task ships.

### Route-back

**Verdict: REQUEST_CHANGES** (round 1; bare token, no `(round <N>)` suffix, per this being the first spec-review round for this task).

Resolved per the design pattern shared with the Conformance-read confirmation gate and the Specify-seam spec-review gate (never off the board's `- dispatch:` transcription, which per this task's own Assumption row does not yet exist at this seam — the intent hash has not been recorded): `pm-spec`'s own `- entry-mode: pm-authored` hand-off field, as read from the task's board entry (the only committed location this field appears at this seam, since no separate provenance/interventions record exists yet for T-1110 — both `.shell-team/provenance/T-1110.md` and `.shell-team/interventions/T-1110.md` are absent, confirmed). **Disclosure**: I was not separately given `tech-lead`'s Routing Map text as printed at Plan (only the spec, the board entry, and `.shell-team/interventions/T-1109.md` per this round's launch brief), so the two-source cross-check this route-back's own design pattern calls for could not be independently performed within this round's available context; the board's `- entry-mode: pm-authored` value is internally consistent with the task's own `- source:` line (issue #400 relayed to `pm-spec`) and I found no contradicting evidence. Routing on the available, unambiguous, uncontradicted value: **route back to `pm-spec`**.

### Fast-follow disposition

- The base-ref-discriminator third-arm silent-fallback concern (ledger row 4) — file-an-issue: this is a systemic convention shared by T-1081's already-shipped gate and every spec that copies it byte-identically (per this spec's own `## Assumptions` row A-6 disclosure of the "byte-identical" requirement); fixing it piecemeal inside T-1110 would touch a Non-goal (no change to base-ref mechanics) and would not fix the other already-merged specs carrying the same pattern. Worth a dedicated follow-up against the shared convention rather than a per-task patch.
- DP-1's relayed-but-unpreserved cost qualifier (ledger row 5) — won't-fix for this spec specifically: the spec's own Non-goals already disclose "no relay checker" as a deliberate DP-4 resolution citing the promoted lesson's own stated hook; asking this one task to additionally build durable preservation for issue-relay qualifiers in general is a mechanism-scope change this `no-mechanism`-classed spec correctly declines (would force `verification-class: mechanism` per its own stated Non-goals #1 reasoning).

---

### Codex Spec-Review verdict: APPROVE (round 2)

## Spec review — round 2

**Mode**: Specify-seam spec-review (T-1092), round 2 — a converging round verifying whether the round-1 rework actually discharges round 1's `REQUEST_CHANGES` findings, against the reworked spec at HEAD `f6a0e9d` (branch `feature/1110-freeze-version-derivation`). Still no implementation exists — this again reviews the frozen-candidate intent block plus the mutable `## This task's own version derivation` and `## Directive correspondence` sections that carry the round-2 delta, not delivered code.

- Task: T-1110
- Codex model: gpt-5.6-luna (model_reasoning_effort=max, resolved from `~/.codex/config.toml` default; no `-m` override was passed)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `.shell-team/specs/T-1110-freeze-version-derivation.md` intent block (Goal/Non-goals/AC1-AC15/Input space) plus `## This task's own version derivation`, `## Directive correspondence` (D38-D40), `## Assumptions` (A-2, A-3, A-6), `## Notes for engineer` (Trap 8, round-2 delta note); `skills/run/SKILL.md:41-61` and `:185-210` (freeze-sweep branch and both `invariant-lock: human-gate-set` lines, read first-hand); `docs/workflow.md:70-82`; `CONTRIBUTING.md`'s `## Cutting a release` and `## What a version number encodes`; `CHANGELOG.md`'s top ~10 releases (v2.4.0 down through v2.0.0); `agents/pm-spec.md:86-102`; `.shell-team/todo.md`'s `**T-1110**` entry.
- raw-capture: `T-1110-codex-specreview-r2.jsonl` / `T-1110-codex-specreview-r2.txt`
- executor-invocation: `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "This is ROUND 2 of a spec-review at the Specify seam for task T-1110, converging on a prior round-1 REQUEST_CHANGES. Read .shell-team/specs/T-1110-freeze-version-derivation.md yourself (its frozen intent block: Goal, Non-goals, AC, Input space) plus .shell-team/reviews/T-1110.md (round 1's recorded findings). Do not expect either text pasted here. Also read skills/run/SKILL.md lines 41-61 and 185-210 (grep for 'invariant-lock: human-gate-set' and the freeze-sweep gate bullets), docs/workflow.md around line 79, CONTRIBUTING.md's '## What a version number encodes' and '## Cutting a release' sections, and CHANGELOG.md's top ~10 releases (v2.4.0 down through v2.0.0) for tier precedent. Then verify, independently and skeptically, whether round 2's rework actually discharges round 1's findings: (1) Blocker ... (2) Major ... (3) Major ... (4) Major (tier precedent) ... (5) Minors ... (6) Round 2 also removed every line-number citation ... Return a JSON array of findings, each {severity, criterion, issue, suggestion}, and for each of the 6 items above state explicitly whether you judge it DISCHARGED, PARTIALLY DISCHARGED, or NOT DISCHARGED, plus your own independent version-tier verdict as one of the fields." > "<RAW_JSONL>" 2>&1` (full prompt text, six numbered items spelled out in full in the live invocation, elided above only for this record's own readability — the live JSONL/txt capture carries every word verbatim)

### Independent primary reading (before consulting Codex's output)

Read, first-hand and before opening Codex's round-2 output: the full spec (`## Goal` through `## Notes for engineer`, all 264 lines), `skills/run/SKILL.md:41-61` and the two `invariant-lock: human-gate-set` lines at `:187`/`:207`, `docs/workflow.md:70-82`, `agents/pm-spec.md:86-102`, `CONTRIBUTING.md:105-140`, `CHANGELOG.md`'s top ~10 releases, and `.shell-team/todo.md`'s live `**T-1110**` entry.

**Item 1 (Blocker — ungrounded non-invention claim), independently verified.** `skills/run/SKILL.md:187` and `:207` both carry `invariant-lock: human-gate-set — declared — the three human gates (the sprint's own batch GO, planning approval, and confirmation before a destructive or irreversible operation) are untouched`, and `docs/workflow.md:79` restates the same three ("the batch GO before a merge, sprint planning approval, and any destructive or irreversible operation"). This is genuinely shipped, genuinely says "planning approval" is one of exactly three named gates, and the reworked Goal/Non-goals/AC6/AC10 now ground the non-invention claim on this literal anchor rather than on the round-1 "existing scope-change stop-and-raise" phrasing. I independently grepped the whole shipped surface (`skills`, `agents`, `docs`, `templates`, `bin`, `CONTRIBUTING.md`, `README.md`, `.shell-team/lessons.md`) for `scope-change`/`stop-and-raise` and found zero matches, matching both my own round-1 reading and Codex's independent round-2 re-scan below. Of the six remaining occurrences of the old phrase inside the spec+review corpus, two sit inside the frozen intent block itself (Non-goals and AC6's own criterion prose) — I checked this precisely, since the brief's "Problem/sources/dispositions" framing undersells that two of the six are Non-goals/AC6 rather than only Problem/Summarized-sources/Notes. Both frozen occurrences use the phrase strictly as a quoted negative ("this ground is deliberately not the round-1 draft's `X`, which the review found... to exist in no shipped file"), never as a live requirement, and neither AC6's nor AC10's own `- check:` line asserts the old phrase as a required literal (verified by reading both check lines in full). **Discharged.**

**Item 2 (Major — `A-2` session-held premise), independently verified with one residual noted.** AC7's grammar and AC13's dogfood assertion now require the `premise=` segment to be non-empty, name one of `MAJOR`/`MINOR`/`PATCH`, and contain no ` — ` sequence. This genuinely closes round 1's specific complaint — a record whose `premise=` was a bare, contentless pointer — and AC13's own prose is honest that this is a declared **static proxy** for substance rather than a semantic guarantee ("whether the ground stated beside it is the *right* ground is a reading rather than a grep... declared here as an explicit static proxy"). Codex's round-2 pass (below) surfaces a real residual: a value like `premise=see approval record #400; expected tier PATCH` satisfies AC13's regex mechanically while still reading as a pointer with a tier word appended. I agree this is a real gap in the mechanical check, but it is a **narrower** gap than round 1's — the record can no longer be silently empty or point at nothing at all, and the spec's own text already discloses the check's proxy nature rather than overselling it. Per this round's own scope (inline `- check:` line review here is for substantive instrument defects, with full mutation-adversarial depth left to QA), I record this as a fast-follow observation rather than a blocking finding: **discharged as to round 1's specific complaint, with a disclosed residual**.

**Item 3 (Major — behavioural-verification disclosure), independently verified.** The added Non-goal ("No behavioural verification of the gate's own three outcome paths") names all three unproven paths by name, states the honest ceiling explicitly, and names the dogfood instance (AC13 + AC15(f)) as the one piece of real evidence without dressing it up as coverage. **Discharged.**

**Item 4 (Major — tier precedent), independently re-derived a third time.** I re-read `CHANGELOG.md`'s v2.4.0/v2.2.1/v2.1.0/v2.0.3/v2.0.1/v2.0.0 entries first-hand. v2.2.1's headline bullet ("The loop now refuses, at freeze time, a task whose recorded entry mode disagrees between its two written sources... A new fail-closed checker...") is indeed a freeze-time comparison-and-refuse mechanism, shipped with a checker, on the default path, PATCH — a materially closer mechanism-shape match to this task's own comparison-and-stop gate than any other precedent in the corpus, exactly as the spec's round-2 addition claims. v2.0.1's declaration-only freeze gate (prose, no checker, PATCH) remains the closest artifact-shape match. v2.1.0's MINOR is attributable to a different, genuinely default-reached, no-opt-in bullet (`:35`, verify-phase fan-out), not to that same release's own prose-only freeze gates (`:37`) — confirmed by reading both bullets side by side. Applying CONTRIBUTING's joint test myself: headline test met; default-reachability test genuinely contested, turning entirely on **A-7**'s disclosed, unverified "majority of adopters have no approved planning premise" claim. Four independent reads now (round-1 primary, round-1 Codex, this round's primary, and this round's Codex, below) all land on the same place: **PATCH is defensible-conservative, not proven**, and A-7 remains the correct, already-disclosed attack surface. This is not a new or newly-discovered gap — it is the same residual round 1 already flagged and the spec already discloses at length (`## This task's own version derivation`'s "Bias disclosure" paragraph) — so I do not treat repeated re-confirmation of an already-priced-in disclosed uncertainty as grounds to withhold approval. **My own independent version-tier verdict: PATCH, concur, defensible-conservative rather than settled** — consistent across all four independent reads to date.

**Item 5 (Minors — three dispositions), independently verified.** `agents/pm-spec.md:102` does state the class-M boundary is "machine-checked by `bin/check-refreeze-class.sh`", confirming AC2's reworded "second net" framing is grounded rather than invented. The base-ref-discriminator's inherited third-arm boilerplate and its prose disclaimer against silent fallback are present exactly as claimed (read in the spec's own declaration-region text). The cost-qualifier's two committed carriers (`## Summarized sources`' issue-#400 bullet, and the board's own `- source:` sub-bullet) are both real and both quote the qualifier verbatim, confirmed by reading the live board entry. **Discharged, all three.**

**Item 6 (line-number-citation removal), independently verified.** I grepped the frozen intent block (`BEGIN`/`END intent-block: T-1110` markers) for any `SKILL.md:<N>` citation and found none. The remaining `:NN` citations inside the frozen block (`tests/trial-recipe/run.sh:36-37`, `templates/prompt-blocks/registry.txt:35-54`, `CONTRIBUTING.md:130`, inside AC12 and the verification-class declaration) all point at files this task does not edit and whose line numbers this task's own insertion cannot move — consistent with Trap 8's claim being scoped to `skills/run/SKILL.md` specifically rather than to every line-number citation in the document. **Discharged.**

### Codex's independent findings (raw JSON in `T-1110-codex-specreview-r2.txt`)

Codex was launched fresh against the same six items, with its own tool calls visible in the JSONL (a live repeat of the round-1 shipped-surface grep, plus a new count of the exact and component-variant phrase occurrences and a frozen-block-scoped sub-count). Six findings returned, one per item, already carrying `status: DISCHARGED / PARTIALLY DISCHARGED` fields directly:

1. **[blocker → DISCHARGED]** Confirms both `invariant-lock: human-gate-set` lines exist and each name exactly three gates including planning approval; confirms the exact compound phrase occurs 14 times, only inside the T-1110 spec and review record, with 2 of those inside the frozen block as an explicit historical negative; confirms component-variant phrases (51 occurrences, 11 files) are confined to `.shell-team` history/prose and have zero matches on the shipped surface. Notes the new gate bullet does not exist yet pre-implementation, so this verifies grounding, not runtime behavior — a scope caveat I concur with.
2. **[major → PARTIALLY DISCHARGED]** Same residual I found independently above: `premise=see approval record #400; expected tier PATCH` would pass AC13's mechanical check while still reading as a pointer. Additionally flags that AC13's own regex requires a tier literal even for the honest `no-premise-on-record` case's own would-be premise value — I checked this and confirm it is not actually a live defect: `no-premise-on-record` is the value of the **`verdict=`** field, not of `premise=`, and the spec's own consequent-record example (`## This task's own version derivation`, near the end) shows a `no-premise-on-record` record still carries a substantive `premise=` value (a stated absence and its reason), so this second sub-point in Codex's finding does not hold up under a closer read of the grammar; the first sub-point (the gameable pointer-plus-tier-word case) does.
3. **[major → DISCHARGED]** Matches my own reading exactly.
4. **[major → PARTIALLY DISCHARGED, independent tier verdict UNDECIDABLE, `patch_defensible: true`]** Independently re-derives the same four-precedent picture I did (v2.1.0's MINOR earned elsewhere, v2.0.1 and v2.2.1 both PATCH on different axes, v2.0.3's breadth-is-not-currency bar) and reaches the identical practical conclusion under different label: MINOR cannot be established, MAJOR is unsupported, PATCH is the defensible-conservative reading pending A-7.
5. **[minor → DISCHARGED]**, all three sub-dispositions confirmed individually, matching my own reading.
6. **[info → DISCHARGED]** Confirms zero `SKILL.md:<N>` citations in the frozen block, confirms current anchor occurrence counts (`Frozen-intent confirmation`: 1, `invariant-lock: human-gate-set`: 2, borrowed scope sentence: 1), and correctly notes the new gate's own title anchor does not exist yet pre-implementation and is intended as the unique locator once it lands.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Blocker (ungrounded non-invention claim) | my independent read: discharged; Codex: discharged | discharged (closed) | Both passes independently re-grepped the shipped surface for the old phrase (zero matches) and independently confirmed the two shipped `invariant-lock: human-gate-set` lines the reworked claim now points at; no required check-line literal still demands the old phrase. |
| 2 | round-1 Major (`A-2` unverifiable premise) — residual: `premise=` grammar admits a gameable pointer-plus-tier-word value | Codex: major (partially discharged); my independent read: minor, fast-follow | minor | Round 2 closes the specific round-1 complaint (a silently empty/bare-pointer record); the newly surfaced residual is narrower, already disclosed by AC13's own "static proxy" prose, and is an inline-check-line mutation-adversarial concern this round's own scope assigns to QA rather than to full-depth review here. Recorded as a fast-follow rather than blocking. |
| 3 | round-1 Major (acceptance criteria test grammar, not behavior) | Codex: discharged; my independent read: discharged | discharged | The added Non-goal names all three unproven paths explicitly and does not oversell the dogfood instance as coverage. |
| 4 | round-1 Major (own tier derivation rests on unverified A-7) | Codex: partially discharged / UNDECIDABLE, patch defensible; my independent read: PATCH, defensible-conservative | discharged (residual already priced in) | Four independent reads across two rounds (round-1 primary, round-1 Codex, round-2 primary, round-2 Codex) all land on the same practical conclusion: PATCH is defensible, not proven, and A-7 is the correct, already-disclosed attack surface. Repeated re-confirmation of an already-disclosed, already-priced residual is not grounds to withhold approval a second time. |
| 5 | round-1 Minors (base-ref third arm, cost-qualifier durability, class-M defense claim) | Codex: discharged (all three); my independent read: discharged (all three) | discharged | Verified against primary bytes: `agents/pm-spec.md:102`, the board's own `- source:` sub-bullet, and the spec's declaration-region prose. |
| 6 | Codex sub-point: AC13 requires a tier literal even for a `no-premise-on-record` record | Codex: raised as part of finding 2 | rejected | `no-premise-on-record` is the `verdict=` field's value, not `premise=`'s; the spec's own consequent-record example shows a `no-premise-on-record` verdict still carrying a substantive `premise=` value naming the expected tier and the reason it wasn't met. Checked directly against the grammar and the worked example; does not hold up. |

### Independent version-tier verdict

**PATCH — concur with the spec's own round-2 derivation, still defensible-conservative rather than settled**, on the fourth independent re-derivation to reach this exact conclusion (round-1 primary, round-1 Codex, round-2 primary above, round-2 Codex above). Headline test met; default-reachability test turns entirely on **A-7**'s disclosed, unverified "majority of adopters lack an approved planning premise" claim. The two round-2-added precedents (v2.2.1 by mechanism shape, v2.0.1 by artifact shape) both independently re-confirmed against primary `CHANGELOG.md` bytes in this round, both genuinely PATCH, both genuinely closer matches than any MINOR precedent in the corpus. Recommend, as round 1 did, that **AC15(f)**'s freeze-time independent re-derivation treat A-7 as still live and unresolved rather than as a formality.

### Route-back

Not applicable this round — **verdict is APPROVE**, so no findings route back to the spec's author. The round-1 Blocker and both round-1 Majors that required rework are judged discharged or acceptably disclosed above; the freeze sweep may proceed.

### Fast-follow disposition

- The `premise=` grammar's residual gameability (ledger row 2) — file-an-issue: a value like `premise=see approval record #400; expected tier PATCH` satisfies AC13's mechanical check (non-empty, names a tier, no ` — `) while still reading as a pointer rather than substance. This is exactly the class of gap the deferred `bin/check-version-derivation.sh` follow-up (already filed per this task's own DP-1/AC15(g)) is the natural home for — a machine check that can tell a genuine ground from a pointer-plus-tier-word needs actual parsing beyond a presence grep, which is precisely the mechanism this task's own Non-goals defer. Recommend folding this specific residual into that same follow-up issue's requirement list rather than opening a fourth one.
- The tier-derivation residual (A-7, ledger row 4) — won't-fix for this spec specifically, on the same grounds round 1 already gave: A-7 is explicitly named in `## Open questions` as the correct attack surface, `## Non-goals` and the "Bias disclosure" paragraph both already state the residual honestly, and **AC15**(f) already requires the freeze run's own independent re-derivation to record its own verdict "even where it overturns this reading" — the mechanism for catching a wrong PATCH call already exists inside this task; a fifth independent re-derivation inside this same review round would not add information the four already-performed ones have not already supplied.

## Board observation (not a spec-review finding; reported for the coordinating session's attention)

Read `.shell-team/todo.md`'s live `**T-1110**` entry (line 14) at HEAD: the flag reads `` `READY_FOR_ENG` ``, and the entry carries no `- intent-hash (v1):` sub-bullet. Per `skills/run/SKILL.md`'s own phase list, `pm-spec` sets `READY_FOR_ARCH` at Specify and the flag only advances past the freeze sweep (intent-hash recorded) once Implement is about to begin — so a `READY_FOR_ENG` flag with no recorded `- intent-hash` line is inconsistent with this task still being at the pre-freeze Specify-seam spec-review stage this round's own launch brief describes ("still a spec review — no implementation exists yet"). This is a board-state observation outside this mode's mandate (domain premises and acceptance-criteria correctness of the spec document itself), not a finding against the spec's own text, and is not a condition of this round's `APPROVE` verdict — flagged here so the coordinating session can confirm whether the flag was set intentionally ahead of the freeze completing, or should be corrected back to `READY_FOR_ARCH` pending the freeze sweep and intent-hash append this approval now clears the way for. Separately, and pre-existing rather than round-2-introduced: the leftover `### QA verification — T-1108 (qa-verifier)` heading block Trap 8 disclosed still sits directly beneath T-1110's own five sub-bullets with no intervening top-level `- [` line, so any future extraction of "T-1110's own board-entry block" (as **AC13**'s check line performs) scans across roughly 1,950 lines of unrelated T-1108 QA content before reaching the next top-level entry — functionally inert to AC13's specific regex today, but worth a board-hygiene fast-follow independent of this task.
