### Codex Review verdict: APPROVE (round 1)
- Task: T-1090
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` field, `model_reasoning_effort = "max"` — the event stream carries no model identifier, so this is the primary-source read)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 6 files, +368/-7 lines (`git diff --stat 6dc38c11a265b5ea619b45e23d737a51e4e2fbcd...HEAD`)

**Scope premise.** Per this task's own `## Verdict scope`: the operator's ratified review-depth re-pricing for dev-scaffold specs applies — inline `- check:` shell one-liners are read for computed values, conclusion direction and real instrument defects; adversarial guard-completeness against unnamed mutation shapes is explicitly out of scope (Input space, out-of-scope class 5) and is not a Major class here. In scope for a Major: a wrong measured value in shipped wording; a sign-safety rule that is itself incorrect or leaves part of the class open; an inconsistency between the brief's obligation and the note's limit; an unpredicted regression in a predecessor criterion; anything reaching `bin/`. This task touches no `bin/` path at all.

**Mechanics note (disclosed rather than smoothed over).** The primary read path (`codex exec ... review --base <sha> --json`) stalled for 32+ minutes with zero new JSONL events after its 41st tool call, confirmed across two independent real-time waits including one uninterrupted 580-second foreground block — not the documented stdin-hang (stdin was closed with `< /dev/null` per Mechanics) but a distinct stall, most plausibly this host's configured `model_reasoning_effort = "max"` compounding with the `review` subcommand's large default diff-context radius. That attempt's raw captures were discarded (never published; the stalled process was left running in the background, untouched). Both passes were then retried successfully with a tightly-scoped free-form prompt (same model, same effort, same read-only sandbox, same repo — only the amount of context asked for shrank) under the canonical `-r1` stems this task's Mechanics section specifies: `T-1090-codex-unguided-r1.{txt,jsonl}` and `T-1090-codex-confirmation-r1.{txt,jsonl}`, both in `.shell-team/reviews/`. Both retries completed in well under two minutes each and both did genuine, visible tool-call work (see below) rather than a fast non-answer.

**Independent re-derivation (my own lens, before reading either Codex transcript in full).**

(i) **Live arithmetic re-derivation, this host** (`bash --version` → GNU bash 3.2.57(1)-release, arm64-apple-darwin25 — same shell as every prior round's record):
`$((10#010))`→`10`; `$((10#00))`→`0`; `$((10#-010))`→`-8` (rc=0); `$((10#-08))`→ stderr `value too great for base` (rc=1); bare forms unchanged: `$((010))`→`8`; `$((08))`→ error; `$((00))`→`0`; `$((-010))`→`-8`. All eight match the spec's `## Assumptions` freeze-run record and the board's hand-off exactly — zero contradictions.

(ii) **Full sign-separate-then-`10#`-normalize-then-reapply algorithm, traced across the whole reachable spelling space** (`0 00 08 09 010 -0 -00 -08 -09 -010`): every unsigned form normalizes to its correct decimal value (`0,0,8,9,10`); every signed form correctly reapplies the sign after magnitude normalization (`0,0,-8,-9,-10`) — the `-0`/`-00` forms collapse to `0` rather than `-0`, which is arithmetically correct. The rule closes **both** failure modes (silent-wrong and hard-error) for the entire class the frozen pattern `^-?[0-9]+$` admits, not merely the two spellings the spec names.

(iii) **Mechanical sweeps re-run independently, matching the hand-off exactly**: `check-acs.sh` on T-1090's own spec → `6 passed, 0 failed, 1 skipped` (AC7 SKIP by design); `check-intent.sh` → `aligned: T-1090 v1 (57648354158b398b7d58fde2429306394ea86673)`; `check-pii-shapes.sh --base develop` → `clean`. T-1089's spec re-run at HEAD → `10 passed, 2 failed, 1 skipped`, `FAILED: AC11 AC12` — matches the predicted-profile table and the board's own corrected AC7(b) figure exactly (the QA-caught, engineer-corrected tally). T-1086's spec re-run at HEAD → `17 passed, 3 failed, 1 skipped`, `FAILED: AC18 AC19 AC20` — matches. No unpredicted regression found in either predecessor's criterion set.

(iv) **Brief/note consistency, read side by side**: both describe the same class (`^-?[0-9]+$`, zero-padded, `10#`-normalization risk), the same two failure modes (silent-wrong `-8`≠`-10`, hard-error on `-08`), and the same remedy (sign-separate, normalize the magnitude, reapply). No divergence between the two landing sites.

(v) **The one editorial nit QA already flagged** (README.md:101, "silently yields the wrong magnitude `-8`") — the noun is loose (a magnitude is conventionally unsigned; `-8` is a signed value, not a magnitude), but the value `-8` itself is exactly what `$((10#-010))` evaluates to, so this is not a "wrong measured value" (in-scope Major class 1) — it is informal phrasing that does not misstate the arithmetic. Non-gating, matching QA's own judgment.

**Codex's own retries, both empty (`[]`), both doing real work before answering**: the unguided pass independently re-derived the sign-separated algorithm live (`sign=-; digits=08; magnitude=$((10#$digits)); echo "$sign$digits => $((-magnitude))"` → `-8`, `-9`, `-10`, `0` for `08/09/010/00`) and confirmed the diff at HEAD byte-for-byte against the pre-image; the confirmation pass ran the full ten-spelling sweep (`0 00 08 09 010 -0 -00 -08 -09 -010`) plus the two literal direct evaluations `$((10#-010))`→`-8` and `$((10#-08))`→hard error, matching this review's own independent numbers exactly. Neither Codex transcript found a wrong value, an inconsistency, or a class left open. Raw captures: `.shell-team/reviews/T-1090-codex-unguided-r1.{txt,jsonl}`, `.shell-team/reviews/T-1090-codex-confirmation-r1.{txt,jsonl}`.

**Addendum (same round, post-commit — provenance disclosed in full).** The first (stalled, abandoned) confirmation-pass invocation, whose target raw files I had already deleted and moved on from, finished very late in the background and recreated a stray file at its old path (`.codex-capture.T-1090-codex-confirmation.out.LscVru`, never a canonical basename, never published). Its content raised a real point: `subject-ac 5`'s pattern `^-?[0-9]+$` places no length bound on the digit run, so a value near or beyond bash's signed 64-bit range silently wraps rather than erroring, and the sign-reapply step can then read out the *wrong sign* on an already-wrapped magnitude. I independently reproduced this live before trusting it (discarded-run output is not evidence on its own): `bash -c 'echo $((10#9223372036854775808))'` → `-9223372036854775808` (silent wraparound, no error) and, applying the shipped sign-separate-then-normalize-then-reapply rule to `-9223372036854775809`, the result is `9223372036854775807` — the wrong value *and* the wrong sign, silently. This is real. It is also **explicitly out of this spec's scope**: Input space, out-of-scope class 2, names "integers beyond 64 bits" verbatim as a synthetic extreme already declined and answered by the existing `- claim-limit: contract-conformance-unbounded` line (`docs/loop-engineering/tier2-subject-harness.md:144`), which itself names "an integer beyond 64 bits" as one of the unenumerated violators it — not a twelfth oracle case, not this task's sign-safety rule — is responsible for disclosing. Per this role's own input-space grounding rule, this downgrades to `minor` and does not drive `REQUEST_CHANGES` on its own; it is not a security/trust-boundary finding (a dev-scaffold CSV fixture harness, not attacker-facing input) and the reachable class this task actually closes (Input space class 3: `-0, -00, -08, -09, -010` and their unsigned siblings) is unaffected — every value in that reachable class closes correctly, confirmed in (ii) above. Verdict unchanged: **APPROVE**.

#### Blockers

None.

#### Major

None. No wrong measured value, no incorrect or partially-closed sign-safety rule, no brief/note inconsistency, no unpredicted predecessor regression, and no `bin/` path touched.

#### Minor / nits

- `docs/loop-engineering/subjects/subject-01/README.md:101` — "silently yields the wrong magnitude `-8`" uses "magnitude" loosely (a magnitude is conventionally unsigned; the actual claim is about the wrong *signed value*). The value itself is correct and matches live measurement; this is phrasing only. Matches QA's own round-1/round-2 disposition (non-gating).
- `docs/loop-engineering/subjects/subject-01/README.md:99` — the sign-separate-then-`10#`-normalize-then-reapply rule silently produces a wrong-sign, wrong-value result on inputs beyond bash's signed 64-bit range (e.g. `-9223372036854775809` → `9223372036854775807`, live-verified). Out-of-input-space per Input space out-of-scope class 2 ("integers beyond 64 bits"), already disclosed by the existing `- claim-limit: contract-conformance-unbounded` line; not a defect in this task's own scope. See addendum above.

#### Adversarial findings

Not run this round: per this task's own step-3/step-5 trigger, the parallel adversarial (devil's-advocate) pass is opt-in and fires only on a blocker/major from the primary read. Neither the unguided nor the confirmation pass surfaced one, so no adversarial pass was launched. Both passes did, however, function as independent re-derivations rather than passive reads (see (i)–(v) above and the Codex-own-retries paragraph) — the substance judgment `## Verdict scope` prices to a human/reviewer read and to the cross-provider review's own re-derivation, both of which land on the same answer: the rule is correct and complete.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | 64-bit-overflow wrong-sign result on the sign-safety rule (reviewer's own live-verified addendum, not from either published Codex pass) | reviewer-own: minor (self-assessed on discovery, matching the discarded run's own severity label) | minor, out-of-input-space | Input space out-of-scope class 2 ("integers beyond 64 bits") names this exact class verbatim and routes it to the existing `- claim-limit: contract-conformance-unbounded` line; not a defect in this task's own reachable-class scope (Input space class 3) |

#### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1090 line to `READY_FOR_MERGE`.

#### Fast-follow disposition

- README.md:101's "wrong magnitude" wording nit — **won't-fix** (the value is correct, the noun is informal-but-not-misleading in context, and QA already judged it non-gating across two rounds; not worth a third rework cycle on a task whose own pre-commitment exists specifically to stop that pattern).
- The 64-bit-overflow wrong-sign result — **won't-fix here** (already disclosed and routed to issue #307 by the existing `- claim-limit: contract-conformance-unbounded` line, which names "an integer beyond 64 bits" verbatim as an unenumerated violator it — not a new oracle case or a new sign-safety rule — is responsible for; no new tracker item, since #307 already carries this residual conformance obligation).
