# T-1026 — QA verification record

## QA verdict: PASS

**Scope**: verify the frozen v1 spec (`.shell-team/specs/T-1026-skill-md-doc-completeness.md`,
intent-hash `af401eaba2c571142814ab72a7730e866e66081b`) against the engineer's hand-off
(commits `b50f021` F2, `5e349f5` F1, `bf66e7d` board+provenance, `f7e263a` interventions
sentinel), `READY_FOR_QA` -> `READY_FOR_REVIEW`.

### Mandatory conformance gates — all green

- `bash bin/check-acs.sh .shell-team/specs/T-1026-skill-md-doc-completeness.md` ->
  **8 passed, 0 failed, 2 skipped** (AC1–AC8 PASS, AC9–AC10 SKIP, exactly the predicted
  post-implementation shape). `--dry-run` run first — neither AC1's nor AC3's long frozen
  literal (backticks, em-dashes, `<P>`-style angle brackets) was mangled.
- `bash bin/check-intent.sh .shell-team/specs/T-1026-skill-md-doc-completeness.md .shell-team/todo.md`
  -> `aligned: T-1026 v1 (af401eaba2c571142814ab72a7730e866e66081b)`.
- Three stacked neighbours, re-checked independently rather than trusted from AC8's own
  `check:` line: `aligned: T-1023 v2 (4434f8892603db06739d7902c525c566125dda02)`,
  `aligned: T-1024 v1 (87c496383df2cf019cff2983bb449e6ec1b8c05e)`,
  `aligned: T-1025 v1 (092f1cbe51c1b7f7d3a2a66292772e7811349ac9)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1026.md` -> `conformant` (0 decision
  entries, 1 sentinel) — structural conformance only, per this gate's scope.
- `bash bin/check-interventions.sh --task T-1026 .shell-team/interventions/T-1026.md` ->
  `conformant` (0 entries, 1 sentinel) — judged structurally only, class-token choice not
  second-guessed.

### Mutations reproduced independently (2 of the engineer's 4, on scratch copies under `$TMPDIR`, real tree untouched throughout — `git status --short` clean before and after)

1. **(2) — F1 moved to the end of the `attestation` line, after the `drift-detected` item**
   (required reproduction, proves AC1's position clause is non-vacuous): AC1 check ->
   `FAIL (exit 1)`; AC2 check on the same mutated copy -> stayed `PASS (exit 0)` — confirms AC1
   is asserting *position*, not mere presence, and that AC2 is genuinely independent of it.
2. **(1) — deleted one space from `check-intent: attestation: `** in a fresh scratch copy:
   AC4 check -> `FAIL (exit 1)` — the trailing-space literal is in fact load-bearing and no
   reviewer could have caught this one by eye.

Both match the engineer's disclosed self-check results exactly.

### F1 / F2 as shipped — read directly, sanity layer only (DP9)

Read both additions in the live `skills/run/SKILL.md` (lines 46 and 115) byte for byte against
the spec's frozen fenced blocks — identical, and in the positions DP2/DP3 require (F1 between
the `aligned` and `drift-detected` items on the single `attestation` line; F2 as a new
indented line directly after the `human` bullet, before `release`). **This reading is QA's
sanity layer only, not the semantic verdict** — per DP9, judging F1 and F2 as *statements*
against the real implementations (`bin/check-intent.sh`'s pace rule/remedy, `templates/
loop-replay.html`'s `adaptRow`/`flagFromLabel`) is Codex's job (AC10), and a round-2 finding
on wording is the intended second gate firing, not a QA miss.

### AC9 — reproduced #105's mechanism empirically on a scratch board (the core of this round)

Built a fixture spec+board pair outside the checkout (`$TMPDIR`, guarded `mktemp -d`, never the
tracked tree; task id `T-0900`, inside this repo's own fixture range) with a declared
`intent-hash (v3)` (two ratifications, v1→v2→v3 — "v2 or later"), a well-formed
`freeze-attestation (v1)` already present, and **v2 and v3's attestation records both missing**
— exactly Input space's "two or more versions' records missing... whenever a ratified re-freeze
advanced the version while an attestation was never recorded." Ran `bin/check-intent.sh` against
the real, unmodified checker. All four observed classification lines, verbatim:

**(i) first refusal — `attestation` naming the first missing version (v2):**
```
check-intent: attestation: expected exactly one freeze-attestation record for T-0900 v2 in <scratch>/board.md (found 0; a freeze is refused unless the board carries a conformant freeze-attestation record for the version being recorded — write:
  - freeze-attestation (v2, YYYY-MM-DD): lines=2/2 sweep=mutual-satisfiability verdict=<P>P/<F>F owner=<who ran them>
)
```
rc=2

**(ii) after appending a conformant v2 record, re-check — `attestation` again, naming the next missing version (v3):**
```
check-intent: attestation: expected exactly one freeze-attestation record for T-0900 v3 in <scratch>/board.md (found 0; a freeze is refused unless the board carries a conformant freeze-attestation record for the version being recorded — write:
  - freeze-attestation (v3, YYYY-MM-DD): lines=2/2 sweep=mutual-satisfiability verdict=<P>P/<F>F owner=<who ran them>
)
```
rc=2

**(iii) after appending a conformant v3 record, re-check — terminal outcome:**
```
check-intent: aligned: T-0900 v3 (ca62c09722a06afec626a5c71d97181974925ee2) matches <scratch>/spec.md
```
rc=0

**(iv) the trap is real — a copy of (i)'s printed remedy line, placeholders left exactly as printed, appended and re-checked, is refused again** (tested on a separate scratch copy, at the point of refusal (i)):
```
check-intent: attestation: 1 malformed freeze-attestation record(s) for T-0900 in <scratch>/board.trap.md (expected '- freeze-attestation (vN, YYYY-MM-DD): lines=<ran>/<total> sweep=mutual-satisfiability verdict=<P>P/<F>F owner=<value>')
```
rc=2

This is F1's exact described sequence: `attestation` -> fill -> `attestation` again (naming the
next version) -> fill -> terminal `aligned`, plus the negative control that a verbatim copy of
the remedy line does not pass. No `check:` exists for AC9 by design; this reproduction is the
runtime evidence in place of one.

### Live locks re-run standalone, against the real (unmodified-by-me) live file

- `bash tests/codex-skeleton-hygiene/run.sh` -> `OK`, all cases PASS, including
  `livefile-broken-invocation` ("DP-7 extended regex matches ZERO lines in the live consumer
  files (agents/qa-verifier.md, skills/run/SKILL.md, skills/goal/SKILL.md)") and
  `livefile-stateful-trace` ("DP-8 extended regex matches ZERO lines in the live
  skills/run/SKILL.md") — both directly against the now-live F1/F2 text.
- `bash bin/check-prompt-sync.sh` -> `check-prompt-sync: all registered prompt blocks in sync`.

### Board hygiene under batch mode

- `git diff --numstat 9d82a49 -- .shell-team/todo.md` -> `15  0` (15 added, 0 deleted) — pure
  insertion; the whole diff is one hunk appending under `## Active`, confirmed by reading it end
  to end. No existing line, including any of T-1023's/T-1024's/T-1025's `## Done` entries, is
  touched.
- `git diff --numstat 9d82a49 -- skills/run/SKILL.md` -> `2  1` — exactly DP4's authorized shape
  (F2 pure addition + F1's single-line `del=1 add=1` extension).
- All three stacked neighbours independently re-confirmed `aligned` above (not merely inherited
  from AC8's own `check:` line).

### Scope lock (AC7), spot-checked

`git diff --name-only 9d82a49` plus untracked files -> exactly `skills/run/SKILL.md`,
`.shell-team/specs/T-1026-skill-md-doc-completeness.md`, `.shell-team/todo.md`, plus this
task's own `provenance`/`interventions` records — matches the spec's allow-list. `bin`, `tests`,
`templates`, `skills/goal/SKILL.md`, T-1014's and T-1018's specs are byte-identical to `9d82a49`
(re-confirmed via `git diff --quiet`).

### Regressions / adjacent code paths

None applicable — `bin/`, `tests/`, `templates/` are byte-identical to the base ref (AC6/AC7,
independently re-checked), and the only production-shaped artifact touched is
`skills/run/SKILL.md`, at exactly the two positions DP2/DP3 authorize.

### Adversarial fixture synthesis checklist

This task's deliverable is a prose documentation edit to `skills/run/SKILL.md`, but it
documents a canonical, runnable procedure (the `check-intent.sh` branch enumeration and its
remedy line), so the checklist's **class 4 (test-harness short-circuit)** applies and is the
one this round exists to close — discharged above by actually running `bin/check-intent.sh`
against a live scratch board rather than reading and agreeing with F1's prose (AC9). **Classes
1–3** (adversarial payload shape, regex/charset anchoring, CommonMark structure parsing) target
a parser's or checker's *own* grammar; no new checker, parser or regex is written or extended by
this task (DP10, AC7's `bin`/`tests` byte-identity) — the underlying grammar those classes would
stress (`bin/check-intent.sh`'s `ATTEST_FULL_RE` etc.) is frozen and out of this task's scope.
Structurally not applicable to this task's own deliverable, and not skipped for lack of trying —
my AC9 reproduction incidentally exercised two adjacent boundary shapes of that grammar anyway
(a well-formed-but-out-of-scope attestation ordering, and a malformed placeholder-shaped line),
both handled correctly by the (unmodified) checker.

### Edge cases tried

- AC9's two-missing-version scenario itself is the edge case the spec exists to close — see
  above.
- The remedy-line-copy trap (iv) — confirms the negative control, not just the positive path.
- Read F1/F2 in situ rather than trusting the spec's quoted fenced blocks alone, to catch a
  transcription slip a byte-exact `check:` grep could theoretically still pass around (it
  didn't — identical).

### Risk notes for reviewer

- No `out-of-input-space` exemption was needed — no candidate finding depended on an
  out-of-scope synthetic extreme (the spec's declined "ever-deeper attestation ladders" class);
  AC9's two-missing-version fixture is squarely inside the declared reachable Input space.
- AC5/AC6/AC7/AC8 are merge-point-scoped against `9d82a49` per the spec's own text; I did not
  widen or re-derive that base ref.
- The semantic judgment of F1/F2 as *statements* against the real implementations is Codex's
  gate (AC10, DP9) — my read above is a byte/position sanity check only, not a substitute for
  that pass.
