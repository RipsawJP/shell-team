# QA review — T-1002

**Verdict: PASS**

## Structural gates (all green)

- `bash bin/check-acs.sh --dry-run .shell-team/specs/T-1002-intervention-capture-channel.md` → 0 unrecognized.
- `bash bin/check-acs.sh .shell-team/specs/T-1002-intervention-capture-channel.md` → 28/28 PASS, run live (not self-reported).
- `bash bin/check-intent.sh .shell-team/specs/T-1002-intervention-capture-channel.md .shell-team/todo.md` → aligned (v1, `da98929ccbe6de00b7c8dbe068f91bfb47ca0d3f`).
- `bash bin/check-prompt-sync.sh` → all registered prompt blocks in sync.
- `bash bin/check-handoff.sh .shell-team/todo.md` → clean.
- `bash bin/check-pii-shapes.sh --base develop` / `bash bin/check-commit-identity.sh --base develop` → clean.
- `shellcheck` (0.11.0, matching CI's pin) clean on the **exact** CI argument list from `.github/workflows/check-handoff.yml`.
- Every suite named in `.github/workflows/check-handoff.yml` run locally: green, including the two new interventions steps (`bash tests/check-interventions/run.sh`, and the dogfood loop over `.shell-team/interventions/*.md` with `found -ge 1`).
- Working tree: `git status --short` clean throughout (all adversarial fixtures built under `$TMPDIR`, mutation experiments applied to scratch copies only and reverted; no destructive git operation used).

## Fixture-label read (item 3) — `tests/check-interventions/run.sh` read in full

All sixteen `case: …` labels checked against their actual assertions; each tests what its name says (both rc and the stderr classification token asserted throughout, per this repo's own fixture-synthesis discipline). The missing-required-field case (case 4) carries the engineer's self-chosen regression fixture for the counter-leak mutation (a second, fieldless entry following a complete first entry) — see below, independently reproduced.

## Independent mutation-audit 1 — the counter-leak fix (item 3)

Copied `bin/check-interventions.sh` to a `$TMPDIR` scratch file and removed the three `..._count_cur=0` resets inside the `ENTRY_RE` branch (reproducing the exact bug class the engineer's self-chosen mutation found). Built the exact regression fixture (complete first entry + fieldless second entry) and ran it directly against the mutated copy:

- Mutated copy: `check-interventions: conformant … (2 entries, 0 sentinel)`, exit 0 — the false-conformant bug reproduces exactly as described.
- Real (fixed) `bin/check-interventions.sh` against the same fixture: `schema: … entry_count=2 sentinel_count=0 violation=1`, exit 1 — correct.
- Ran the full fixture suite against the mutated copy in an isolated scratch tree: goes red at case 1 ("well-formed entries are conformant") before even reaching case 4, because *any* multi-entry file trips the same leaked-counter arithmetic once the resets are gone (the counters accumulate past 1, tripping the "duplicated field" check on the second entry's own first field). This is a stronger, not weaker, confirmation — the regression the engineer fixed is caught by more than one case in the suite, not only by the case purpose-built for it.

## Independent mutation-audit 2 — the DP-8 lock extension (item 4)

Read `tests/codex-skeleton-hygiene/run.sh`'s `NEW_AC3`/`NEW_AC15`/`OLD_AC3`/`OLD_AC15` regex definitions and its own `oldmiss`/`livefile-mutation-*-interventions` cases (already present and green in the suite run above). Independently re-verified with fixtures of my own wording (not the shipped ones), injected into fresh `$TMPDIR` copies of the scanned live files:

- My own coercive-invocation line (`bash bin/check-interventions.sh` on its own line) → caught by `NEW_AC3` (rc=0, match).
- My own stateful-trace sentinel (`interventions   gate:AC7`) → caught by `NEW_AC15` (rc=0, match).
- Both my probes → missed by the frozen `OLD_AC3`/`OLD_AC15` (T-077 literal, provenance-only), confirming the `oldmiss` cases are testing a genuinely narrower frozen regex, not a strawman.

**Orthogonal, non-blocking finding**: a coercive invocation wrapped directly in markdown backticks with no preceding space — `` `bash bin/check-interventions.sh` `` (backtick immediately adjacent to `bash`) — is **not** caught by either the extended or the frozen regex, because both require whitespace or beginning-of-line immediately before the interpreter word, and a backtick satisfies neither. Confirmed this is not a T-1002 regression: the identical blind spot exists in the original T-077 `OLD_AC3` regex against an equivalent provenance-flavored probe, so it is a pre-existing, symmetric property of the whole lock mechanism, unrelated to and unwidened by this task's extension. No AC claims coverage of this shape; DP-8's own stated goal is "extend to both regexes," not "hard the regex's own anchoring." Worth a follow-up issue, not a FAIL driver here.

## Independent verification of the engineer's flagged asymmetry (item 6)

Reproduced both directions live against the real tree (each mutation applied, checked, and reverted before the next):

- **Canonical-side deletion** (removed `- intervention: unclassified` from `templates/prompt-blocks/interventions-classes.md`): `grep -c` drops to 6 (AC2's direct count assertion correctly fails), but `bin/check-prompt-sync.sh` stays green (`exit 0`) — confirmed. `contain` mode only checks canonical→consumer containment; it cannot see the canonical file shrink.
- **Consumer-side deletion** (removed the same line from `bin/check-interventions.sh`'s header): `bin/check-prompt-sync.sh` correctly reports `1 violation(s)` (`exit 1`), naming the missing phrase.
- Judgment: this does not undermine any AC. AC2's own `check:` line counts the canonical file directly and independently rejects an 8th or a 6th — it does not route through `check-prompt-sync` for that direction. AC2 and `check-prompt-sync` together (not `check-prompt-sync` alone) hold the enum pinned in both directions, exactly as the engineer's hand-off note states. Confirmed accurate, not a defect.

## Dogfood items (item 5)

- `.shell-team/interventions/T-1002.md`: read in full — two entries (`human-correction`, `assumption-contradicted`), both non-trivial and specific to this task's own design history (the hook withdrawal and the check-prompt-sync asymmetry correction). `bash bin/check-interventions.sh --task T-1002 .shell-team/interventions/T-1002.md` → `conformant` (2 entries, 0 sentinel). File left untouched, as instructed (orchestrator is the producer).
- `.shell-team/provenance/T-1002.md`: read in full — six decision/reason/grounding triples, each grounded in a specific spec section or file location, none boilerplate. `bash bin/check-provenance.sh .shell-team/provenance/T-1002.md` → `conformant` (6 decisions, 0 sentinel).
- `bash bin/check-provenance.sh tasks/provenance/T-1001.md`-equivalent (AC28's existing-suite regression) also green.

## Regression / scope discipline — all clean

- `git diff --name-only develop` matches AC27's allow-list exactly (18 paths, verified by direct listing, not just via `check-acs`).
- `git diff develop -- agents/engineer.md` / `docs/tuning-oversight.md` / `bin/goal-state.sh` → all three byte-empty diffs, confirmed directly (not only through the AC26/AC23 `check:` lines).
- `agents/qa-verifier.md`'s new mandatory item (AC22) read directly: matches the spec's required wording verbatim, and I applied it as part of this very verification (ran `check-interventions.sh --task T-1002` and treated `usage`/`schema`/`structural` alike as FAIL, judging structure only).
- `skills/run/SKILL.md`'s gate paragraph (line 60) and producer-discipline paragraph (line 28) read directly: both match every required substring from AC18/AC19, both single physical lines, no `tasks/`/`.shell-team/` literal on the gate line.

## Adversarial edge cases of my own construction (item 7 / adversarial-fixture checklist)

All exercised directly against the real `bin/check-interventions.sh` (fixtures under `$TMPDIR`):

- Dangling symlink as the path argument → `usage` (exit 2), no hang.
- A FIFO as the path argument → `usage` (exit 2) immediately (the `-f` type check rejects it before any read is attempted — no hang risk).
- 40 well-formed entries in one file (beyond the ~10 measured scale in the spec's Input space) → `conformant`, 40 entries reported.
- A field value containing literal tab characters → `conformant` (tabs are not treated as record separators, correctly).
- `--task=<value>` (`=`-attached form) → both matching and disagreeing forms behave identically to the space-separated form.
- `--` end-of-options marker with an oddly-named file (`--weird-name.md`) → handled correctly, no flag-injection confusion.
- A fenced code block (```` ``` ````) placed inside the marker region, containing a fake second entry → correctly fails closed (`schema`, exit 1): the grammar has no fence-awareness by design (unlike `check-playbook.sh`, which parses an external, free-prose lessons file) — everything inside the region must be the sentinel, an entry anchor, or an indented field continuation, and a fence line is none of those. This is the correct, safe behavior for this artifact's narrower model, not a gap — CommonMark fence-tolerance (checklist class 3) is **structurally not applicable** here.

No genuinely unexercised class found. Time/liveness (checklist class 1's hang-defense sub-axis): verified via the FIFO/dangling-symlink cases above (both return immediately) — no `environmentally-unverified` items to record.

## Input-space and out-of-scope judgment

The spec's own `## Input space` bounds what a finding may drive: adversarially large inputs, non-UTF-8/NUL/Unicode-line-separator bytes, a hostile author, calendar-invalid dates, markdown-notation attacks on the marker match, concurrent writers, and local-agent-transcript reads are all explicitly declined with reasons, and none of my own edge cases above crossed into those declined classes (my 40-entry fixture is within the declared "tens of entries" scale, not the declined "thousands/megabyte" extreme). The one orthogonal finding recorded above (backtick-adjacent coercive form) is a pre-existing, symmetric limitation of the whole lock mechanism predating this task — not an AC violation, not a regression, and not a real-data-reachable trust-boundary gap distinct from what T-077 already accepted — so it is recorded as a non-blocking risk note per the Rules, not a FAIL driver.

## Verdict

All 28 acceptance criteria hold, independently re-run live. `check-intent` reports `aligned`. `check-provenance`/`check-interventions` dogfood both `conformant`. No regression found in any suite named by CI. The one asymmetry the engineer flagged for QA (AC2 vs `check-prompt-sync`) was independently reproduced in both directions and judged not to undermine any AC's claim, exactly as hand-off argued. One orthogonal, non-blocking finding recorded above for the reviewer's attention (pre-existing backtick-adjacency blind spot in the coercive-invocation regex, symmetric across provenance/interventions, out of this task's stated scope).

**PASS** → `READY_FOR_REVIEW`.

## Cross-provider review (codex-reviewer) — round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1002
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 19 files changed, +2220/-28 lines (`develop...feature/intervention-capture-channel`)

Raw Codex output: `.shell-team/reviews/T-1002-codex-primary.{txt,jsonl}` (primary pass, `codex exec review --base develop --json`) and `.shell-team/reviews/T-1002-codex-adversarial.{txt,jsonl}` (adversarial devil's-advocate pass, triggered by the primary pass's blocker/major findings).

#### Blockers

- **`skills/run/SKILL.md:28`** — the standing "Interventions producer discipline" paragraph never states the entry's actual grammar: it names the seven class tokens but never mentions the `<!-- BEGIN interventions: <id> -->` / `<!-- END interventions: <id> -->` markers or the required indented `date:` / `summary:` / `effect:` fields that `bin/check-interventions.sh` enforces. Worse (adversarial pass), its literal instruction — "append the entry at that moment" — is actively wrong the first time a task transitions from the zero-entry sentinel to a real entry: AC5 rejects the sentinel and an entry coexisting in either order, so blind appending after an existing `no interventions occurred` line trips a `schema` violation. Because the gate at line 60 is explicitly unconditional and forbids auto-retry ("nothing is auto-retried... escalate immediately to the human"), an orchestrator following only this paragraph is very likely to hit a human escalation on the very first real intervention of essentially every future task — the mechanism's core, everyday case, not an edge case. Suggested fix: give the paragraph (or a nearby fenced example) the literal marker + field template, and an explicit branch for "the file currently holds the sentinel: replace it, don't append after it."

- **`skills/goal/SKILL.md:78`** (compare `skills/run/SKILL.md:28`) — the `/goal` completion gate checks `check-interventions.sh` for conformance, but no paragraph in `skills/goal/SKILL.md` mirrors the producer discipline that tells the orchestrator *when* to append an entry (trigger 1 mid-tick, or triggers 3/5 at phase checkpoints). The spec's own "Mirroring symmetry audit" claims the goal loop "has the same seam and the same producer" as `skills/run/SKILL.md` and states as its own premise that "nothing is silently omitted," but only the gate (AC23) was mirrored — the producer discipline (AC19) was not, and no Non-goal names this omission. In practice an autonomous `/goal` tick can satisfy the gate by writing (or leaving) the zero-entry sentinel with no standing instruction ever prompting the orchestrator to notice a real trigger during the tick, silently defeating the mechanism for this operating mode. Suggested fix: mirror an equivalent producer-discipline paragraph (or an explicit cross-reference to `skills/run/SKILL.md:28`) into `skills/goal/SKILL.md`.

#### Major

- **`bin/check-interventions.sh:162-166`** — `--task`'s validation is incomplete in two related ways, both silently defeating DP-4's wrong-file protection (`conformant`, exit 0, no error): (a) an explicitly empty value (`--task ""` or `--task=`) leaves `TASK=""`, and the later `[ -n "$TASK" ]` guard then treats the flag as though it were never given; (b) a repeated `--task` flag lets the last value silently win with no duplicate-detection. Reproduced directly:
  ```
  $ check-interventions.sh --task "" <file-for-T-900>
  check-interventions: conformant: ... (0 entries, 1 sentinel)   # should reject as usage
  $ check-interventions.sh --task T-999 --task T-1002 <T-1002-file>
  check-interventions: conformant: ... (0 entries, 1 sentinel)   # T-999 silently dropped
  ```
  Suggested fix: reject an explicitly empty `--task` value and a repeated `--task` flag as `usage` (exit 2).

- **`bin/check-interventions.sh:218-233`** — the END-marker uniqueness check is structurally asymmetric with the BEGIN check: BEGIN is matched by a generic any-id regex (`BEGIN_ANY_RE`), so a second BEGIN of *any* id is caught, but END only counts lines matching the literal string derived from the already-resolved BEGIN id — an extraneous, well-formed END marker for a *different* id trailing after the true END line is never counted and never flagged. Reproduced:
  ```
  <!-- BEGIN interventions: T-900 -->
  no interventions occurred
  <!-- END interventions: T-900 -->
  <!-- END interventions: T-901 -->
  ```
  reports `conformant`, both with and without `--task T-900`. This is exactly the class of hand-edited/copy-pasted marker corruption the spec's own Input space class 4 declares reachable and in-scope, and it contradicts the checker's own documented "exactly one marker pair" claim underlying AC6. Suggested fix: count all lines matching a generic END-any-id regex, require exactly one, and only then compare its captured id against `begin_id`.

- **`skills/run/SKILL.md:58,60`** (adversarial pass) — the T-073 "no uncommitted implementation diff" guard and the new interventions gate share the same Implement→Validate seam, but nothing in this diff specifies whether/when an orchestrator-authored interventions-file edit (made "at that moment," possibly after the engineer's own commit) must itself be committed relative to the T-073 check. As written, T-073's check is over the whole working tree ("`git status --short` shows nothing to commit"), so an uncommitted interventions append at that exact moment would make the check non-empty for a reason the guard's own prose does not name. (In this task's own commit history the orchestrator handled this correctly — see `1e391e4`, its own atomic commit for a mid-task intervention entry — but nothing in the shipped instructions guarantees a future orchestrator repeats that discipline.) Suggested fix: add one sentence disposing of the interaction (e.g., "commit the interventions-file edit immediately, the same way the engineer commits its own work, before this check").

- **`skills/goal/SKILL.md:78`** (adversarial pass) — the phrase "you resolve yourself... before the next tick" implies a record-only interventions fix should not need a fresh implement pass, but the loop's own "Each tick" structure has no branch to skip re-invoking `engineer` (step 1) when the *only* non-green completion-gate layer was the interventions check. A documentation-only fix therefore consumes a full iteration against `loop-guard.sh`'s budget, risking a spurious `STOP:no_progress` / `STOP:max_iterations_reached` for a gap unrelated to the code under test. Suggested fix: describe a same-tick repair-and-recheck path for this layer specifically, or exempt it from the next tick's implement-pass requirement.

#### Minor / nits

- **`skills/run/SKILL.md:60`** — the gate resolves the interventions *directory* via `team-paths.sh --get interventions` and then refers to "<that file>" without ever writing out the `<dir>/<task-id>.md` construction, unlike `skills/goal/SKILL.md:78`'s explicit `"$(team-paths.sh --get interventions)/<task-id>.md"`. Inferable, but a literal reading risks passing a bare directory (a `usage` exit).
- **`bin/check-interventions.sh:186`** — NUL-byte truncation via Bash 3.2's `read -r` (a NUL mid-line silently truncates the line). Out-of-input-space: the spec's own `## Input space` § Out-of-scope item 2 explicitly declines "Non-UTF-8 or mixed encodings, NUL bytes, Unicode line separators (U+2028/U+2029) and bidirectional control characters," reasoning that none is a record separator for any POSIX text tool — this finding's only trigger is exactly that declared class, and it is not a trust-boundary finding (this artifact's own declared trust boundary is a trusted, committed author, per the spec's Out-of-scope item 3). Capped at minor per the grounding rule.
- **`bin/check-interventions.sh:244-250`** — no separator is required after a field's colon (`date:2026-07-30` without a space is accepted), looser than the prose's `key: value` illustration. Not a correctness or safety issue (more permissive, not less safe) and no AC pins a stricter grammar; noted for completeness only.

#### Adversarial findings

Re-ran Codex with a devil's-advocate framing against `bin/check-interventions.sh`, `skills/run/SKILL.md`, and `skills/goal/SKILL.md`, explicitly instructed not to re-report the primary pass's four findings. Results, and my independent verification of each where mechanically checkable:

- Sentinel→first-entry replace-not-append trap — folded into Blockers above (`skills/run/SKILL.md:28`).
- T-073/interventions-commit-timing interaction — folded into Major above (`skills/run/SKILL.md:58,60`).
- Goal-loop tick-cost / no-progress risk on a record-only gate failure — folded into Major above (`skills/goal/SKILL.md:78`).
- Repeated `--task` masking (last value wins) — folded into Major above (`bin/check-interventions.sh:162`), merged with the primary pass's empty-value finding as one root cause.
- Missing colon separator, and NUL-byte truncation — folded into Minor/nits above.
- One claim was **rejected** after direct construction: that `[[:space:]]` (vs `[[:blank:]]`) lets CR/VT/FF pass as line indentation, "permitting corrupted mixed-line-ending files" to be miscounted as conformant. `trim()` strips the identical `[[:space:]]` character class from every captured field value regardless of which class matched the line's indentation, so no fixture I constructed produced a wrongly-accepted corrupted value — this is redundant matching, not an exploitable gap.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | sentinel→first-entry replace-not-append trap (`skills/run/SKILL.md:28`) | adversarial: high (maps to major) | blocker | sharpens and merges into the primary finding's grammar-omission: shows the gap is not just incomplete documentation but an actively wrong instruction on the mechanism's single most common transition, guaranteed to trip the unconditional, no-auto-retry gate on ordinary use |
| 2 | goal-loop tick consumes a full implement pass to fix a record-only gate failure (`skills/goal/SKILL.md:78`) | adversarial: medium (maps to minor) | major | risks a spurious `loop-guard.sh` `STOP:no_progress` / `STOP:max_iterations_reached` for a documentation/record gap unrelated to the code under test — a failure class this repo's own instructions already treat as serious enough to warrant dedicated escalation machinery (same-class-2, rework-digest) elsewhere in this same file |
| 3 | repeated `--task` flag lets the last value silently win (`bin/check-interventions.sh:162`) | adversarial: medium (maps to minor) | major | merged with the primary finding (empty `--task` value bypass) — identical root cause (incomplete `--task` validation) and identical impact (silently defeats DP-4's wrong-file protection), so scored as one major finding rather than independently at the lower adversarial mapping |
| 4 | no separator required after a field's colon (`bin/check-interventions.sh:244`) | adversarial: medium (maps to minor) | nit | makes the grammar strictly more permissive, not less safe; no AC or spec prose requires a stricter separator, and no false-conformance of otherwise-invalid data results |
| 5 | `[[:space:]]` (vs `[[:blank:]]`) allegedly lets CR/VT/FF pass as indentation, permitting corrupted mixed-line-ending files | adversarial: medium (maps to minor) | rejected | `trim()` applies the identical `[[:space:]]` strip to every captured field value regardless of which class matched the line's indentation; direct construction found no fixture where a corrupted value survives to a `conformant` verdict — redundant matching, not an exploitable gap |
| 6 | NUL-byte truncation via `read -r` (`bin/check-interventions.sh:186`) | adversarial: low (maps to minor, unchanged) | minor, tagged `out-of-input-space` | grounded on the spec's `## Input space` § Out-of-scope item 2, which declines exactly this byte class with the stated reasoning that none is a record separator for any POSIX text tool; not a trust-boundary/security finding under this artifact's own declared trust boundary (Out-of-scope item 3), so capped at minor per the grounding rule |

The two findings the orchestrator flagged as already-known-and-dispositioned (the `check-prompt-sync` contain-mode asymmetry, and the backtick-adjacent coercive-invocation blind spot) were not re-reported by either Codex pass and are not repeated here.

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1002 entry back to `READY_FOR_ENG` with the findings above linked to this review record.

#### Fast-follow disposition

- `bin/check-interventions.sh:244` (no colon-separator requirement) — won't-fix: strictly more permissive than the documented grammar, not a safety regression; no AC pins a stricter separator.
- `bin/check-interventions.sh:186` (NUL-byte truncation) — won't-fix: the spec's own Out-of-scope item 2 already declines this input class with reasoning that applies unchanged here (no POSIX tool treats a NUL as a record separator, so it cannot forge an entry or marker line).
- `skills/run/SKILL.md:60` (bare-directory path ambiguity) — won't-fix as a standalone follow-up: expected to be resolved incidentally by the same rework that fixes the Blocker at this same paragraph (`skills/run/SKILL.md:28`); if that rework does not naturally cover it, raise it again at the next review round.

## QA verification — round 2 (rework delta against round-1 review findings)

Scope: the round-1 rework commit `00c49f1` against the two repeated Codex classes (`checker-input-validation-gap`, `gate-interaction-unspecified`), plus regression. Round-1 QA findings above stand and are not relitigated here.

### Findings-closed table (a–h)

| # | finding | closure verified | how |
|---|---------|-------------------|-----|
| a | `--task ""` / `--task=` | closed | `check-interventions.sh --task "" <file>` and `--task= <file>` both → `check-interventions: usage: --task requires a non-empty value`, exit 2, live against the real (non-scratch) `bin/check-interventions.sh` |
| b | repeated `--task` | closed | `--task T-999 --task T-900 <file>` → `usage: --task given more than once`, exit 2. Also tested the AGREEING-repeat form (`--task T-900 --task T-900`, same value twice) → same rejection (unconditional, not value-comparing) — matches provenance decision #8's documented judgment; judged correct (see below) |
| c | trailing extraneous END for a different id | closed | reviewer's exact reproduction (`BEGIN T-900` / `END T-900` / trailing `END T-901`) → `structural: expected exactly one '<!-- END interventions: <id> -->' marker (... found 2)`, exit 2 |
| d | entry template + sentinel-replace-not-append in the run skill and canonical block, contain-checked into both consumers | closed | `grep`-confirmed the two sentences verbatim in `templates/prompt-blocks/interventions-classes.md`, `bin/check-interventions.sh`'s header, and `skills/run/SKILL.md`; `bash bin/check-prompt-sync.sh` green (contain-mode holds both consumers) |
| e | goal skill reference-form producer-discipline paragraph + new hygiene lock | closed | anchor `Interventions producer discipline (T-1002, referenced not restated)` present in `skills/goal/SKILL.md`; `goalskill-producer-discipline-reference` case present in `tests/codex-skeleton-hygiene/run.sh` and green; independently mutation-audited (see below) |
| f | T-073 interaction sentence in both skills | closed | `grep`-confirmed the "commit that edit immediately... before this check runs" sentence in both `skills/run/SKILL.md` and `skills/goal/SKILL.md` |
| g | goal skill's record-only same-tick repair path | closed | `grep`-confirmed the sentence: repair-and-recheck in the SAME tick, explicit "do not re-invoke `engineer`", explicit "never consumes a fresh implement pass... never... `STOP:no_progress`" in `skills/goal/SKILL.md`'s Completion-gate bullet |
| h | gate paragraph spells out `<dir>/<task-id>.md` | closed | `grep`-confirmed literal `<dir>/<task-id>.md` construction in `skills/run/SKILL.md`'s Interventions-gate bullet, replacing "<that file>" |

### Same-class-2 inventory completeness audit (item 2)

Independently swept `bin/check-interventions.sh`'s argument parsing and marker walk (full read of the script, not just the diff) and constructed fixtures beyond the engineer's own inventory:

- Repeated `--task` with an **agreeing** value (`--task T-900 --task T-900`) — rejected as `usage` (unconditional "given more than once"), same as disagreeing. This is the documented judgment call in provenance decision #8 (rework1): rejecting outright rather than special-casing "repeated but agreeing" is simpler and symmetric with the empty-value rejection, and no spec/AC requires tolerating an agreeing repeat. Judged **correct** — a caller passing `--task` twice, agreeing or not, has a malformed invocation; there is no stated benefit to a second comparison path.
- `--task=T-1002`-equals-form, both matching and disagreeing against the real BEGIN id, behave identically to the space-separated form (conformant / `structural` respectively).
- A second BEGIN marker for a **different** id after the true pair (`BEGIN T-900 ... BEGIN T-901 ... END T-901`) → `structural` (found 2), symmetric with the END-side fix.
- Markers with trailing whitespace after `-->` → exact full-line regex does not match, `begin_count`/`end_count` fall to 0 → `structural` (found 0) — fails closed by design (the exact-full-line-compare discipline predates this task), not a silently-accepted gap.
- An extraneous well-formed END for a different id appearing **before** BEGIN (not just after, the reviewer's own placement) → `structural` (found 2) — confirms the fix is not position-sensitive.
- Options-after-positional (`check-interventions.sh <file> --task T-900`) → `usage: unexpected extra argument: --task` — fails closed, not silently ignored.

No inventory gap found beyond what the engineer already covered. The "agreeing-repeat" judgment call is the one place a plausible alternative existed (tolerate agreeing repeats) and it is adequately grounded in the provenance file; not a FAIL driver.

### Independent mutation audits (item 4)

1. **END_ANY_RE revert**: copied `bin/check-interventions.sh` to a scratch file and replaced the new generic-any-id END matching block with the pre-rework literal-derived-from-`begin_id` compare (byte-for-byte the old logic from the diff). Ran it directly against the reviewer's exact fixture: reports `conformant` (false-positive reproduces exactly). Also copied `bin/` + `tests/` into an isolated scratch tree with the reverted script substituted for `bin/check-interventions.sh` and ran `tests/check-interventions/run.sh` there: fails at the new sub-assertion ("trailing extraneous END marker (different id): expected exit 2 (structural), got 0") — the regression lock is load-bearing, not vacuous.
2. **Goal-skill anchor removal**: `rsync`-copied the whole repository (minus `.git`) into a scratch tree, deleted the "Interventions producer discipline (T-1002, referenced not restated)" bullet line from the scratch copy's `skills/goal/SKILL.md`, and ran `tests/codex-skeleton-hygiene/run.sh` from that scratch tree (its `REPO_ROOT` resolves relative to the script's own location, so the whole suite ran against the mutated tree). Result: fails exactly at `goalskill-producer-discipline-reference` ("missing anchor ... in skills/goal/SKILL.md"), rc=1 — the lock is load-bearing, not vacuous.

### Regression / full suite (item 3)

- `bash bin/check-acs.sh .shell-team/specs/T-1002-intervention-capture-channel.md` → 28/28 PASS, run live.
- `bash bin/check-intent.sh .shell-team/specs/T-1002-intervention-capture-channel.md .shell-team/todo.md` → `aligned` (v1, `da98929ccbe6de00b7c8dbe068f91bfb47ca0d3f`, hash unchanged from round 1).
- `bash bin/check-prompt-sync.sh`, `bash bin/check-pii-shapes.sh --base develop`, `bash bin/check-commit-identity.sh --base develop` → all clean.
- `shellcheck` (0.11.0, matching CI's pin) clean on the exact CI argument list.
- Every suite named in `.github/workflows/check-handoff.yml` (36 suites + dogfood steps) run locally: all green, including `tests/check-interventions/run.sh` (now 16 case-classes with the two new regression sub-assertions) and `tests/codex-skeleton-hygiene/run.sh` (now including `goalskill-producer-discipline-reference`).
- Dogfood: `.shell-team/interventions/T-1002.md` → `conformant` (4 entries, unchanged — file untouched this round, as instructed); `.shell-team/provenance/T-1002.md` → `conformant` (12 decision entries — 6 new rework1 triples, each read for grounding quality, not just structure).
- AC2's canonical-block count still exactly 7 (the two new template sentences were folded into prose, never starting a line with `- intervention:`, confirmed by `grep -c -- '^- intervention: ' templates/prompt-blocks/interventions-classes.md` = 7, matching AC2's own check).
- AC13's `CASES_EXPECTED=16` intact (no new case CLASS was added — the two new regression fixtures were folded into existing case classes 11 and 12, per the engineer's own accounting).
- The two extended live-file locks (`livefile-mutation-ac3-interventions`, `livefile-mutation-ac15-interventions`, and their `oldmiss` counterparts) still green and ZERO-match the frozen T-077 literals against the live files.
- Working tree: `git status --short` clean throughout this verification (all fixtures/mutations built and run under `$TMPDIR`/scratch copies, real repo untouched; no destructive git operation used).

### Verdict

**PASS** → `READY_FOR_REVIEW`. All 6 round-1 findings (2 Blockers, 4 Majors) independently reproduced as closed by direct reconstruction of the reviewer's own repro steps, not by re-reading the engineer's hand-off claims. Same-class-2 inventory audited independently with no gap found beyond the engineer's own coverage; the one documented judgment call (agreeing-repeat `--task` rejected unconditionally) is adequately grounded and not a FAIL driver. Both new regression locks independently mutation-audited and confirmed load-bearing (not vacuous). No regression in any CI-named suite. Round-1's one orthogonal non-blocking finding (backtick-adjacent coercive-invocation blind spot) is unaffected by this rework and stands as previously recorded.

## Cross-provider review (codex-reviewer) — round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1002
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 19 files changed, +2504/-30 lines (`origin/develop...feature/intervention-capture-channel`), rework delta = commit `00c49f1`

Raw Codex output: `.shell-team/reviews/T-1002-codex-primary.{txt,jsonl}` (primary pass, `codex exec review --base origin/develop --json`, full task diff) and `.shell-team/reviews/T-1002-codex-adversarial.{txt,jsonl}` (adversarial devil's-advocate pass, triggered by the primary pass's finding 1).

#### Round-1 findings-closure verification (independent, reading the rework commit `00c49f1` directly, not the engineer's or QA's hand-off prose)

All 6 round-1 findings confirmed genuinely CLOSED, not narrowed:

- **Blocker 1** (`skills/run/SKILL.md:28` grammar omission + sentinel-replace trap): closed. The entry template (marker pair + field quad) and the sentinel-replace-not-append sentence are now in the canonical `templates/prompt-blocks/interventions-classes.md` block, verified into `bin/check-interventions.sh`'s header and `skills/run/SKILL.md` (line 61) via `check-prompt-sync.sh` contain-mode (confirmed green).
- **Blocker 2** (`skills/goal/SKILL.md:78` missing producer discipline): closed. A reference-form paragraph (not a verbatim duplicate — correctly avoids becoming a third `check-prompt-sync` consumer, which would have broken AC17's two-consumer registry pin) is present, locked by a new mutation-tested anchor case (`goalskill-producer-discipline-reference`) in `tests/codex-skeleton-hygiene/run.sh`. Confirmed present and non-vacuous by reading the suite's own removal-mutation assertion.
- **Major 1** (`--task` accepts empty/repeated values): closed with a **generic fix**, not a literal special-case — confirmed by reading `bin/check-interventions.sh:174-186` directly: `TASK_GIVEN` is a real state guard (not e.g. a hardcoded string comparison), `[ -n "$2" ]` / `[ -n "$TASK" ]` reject empty in both the space-separated and `=`-attached forms. Reproduced both `--task ""` and `--task=` → `usage`, exit 2; a repeated flag (agreeing or disagreeing) → `usage`, exit 2.
- **Major 2** (END-marker asymmetry): closed with the fix explicitly demanded — `END_ANY_RE` is a **generic any-id regex** (`^<!-- END interventions: (T-[0-9]+|no-task) -->$`), mirroring `BEGIN_ANY_RE`'s own shape exactly, with the id-agreement check performed only after uniqueness is established. This is not "another literal derivation" — confirmed by reading the diff hunk directly. Reproduced the reviewer's own fixture (trailing extraneous `END interventions: T-901` after a true `T-900` pair) → `structural`, exit 2.
- **Major 3** (T-073/interventions-commit-timing interaction, at the named seam): closed for the literal scope the round-1 finding named — a disposing sentence is present in both `skills/run/SKILL.md:58` and `skills/goal/SKILL.md:52-53`. See the new Blocker below: the fix is correctly scoped to what was asked, but reading it against the standing producer-discipline paragraph's own broader claim ("Self-check at each phase transition, at every `STOP:` escalation, and when a task is aborted") surfaces that the *same underlying class* (commit-timing left unspecified) remains open everywhere else.
- **Major 4** (goal-loop tick-cost on a record-only repair): closed. `skills/goal/SKILL.md`'s Completion-gate bullet now has an explicit same-tick repair-and-recheck sentence with "do not re-invoke `engineer`" and "never... `STOP:no_progress`" stated directly.

No inventory-completeness gap found in `bin/check-interventions.sh`'s reworked marker walk or argument parsing beyond what the engineer's own inventory and QA's independent sweep already covered (agreeing-repeat `--task`, `=`-form, second-BEGIN-for-a-different-id, trailing whitespace, END-before-BEGIN, options-after-positional — all re-checked directly against the live script, all fail closed as documented).

#### Blockers

- **`skills/run/SKILL.md:58` and `skills/goal/SKILL.md:52-53`** — the new "commit that edit immediately... before this check runs" sentence the rework added is scoped **only** to an interventions-file edit made "at this same seam" (the Implement-to-Validate T-073 check). But the standing "Interventions producer discipline" paragraph (`skills/run/SKILL.md:28`) directs capturing an entry at *every* phase transition, at *every* `STOP:` escalation, and at task abort — moments that mostly do **not** coincide with the one named seam (a `STOP:` escalation during Validate/Review/rework routing, a human-stop mid-conversation, a final task abandonment). Read the whole of both skill files directly (not just the rework's diff hunk): there is no other standing instruction anywhere in either file, and no other mechanical pre-merge check (`bin/close-out.sh`, the "Done" step, any clean-working-tree gate), that ever tells the orchestrator to commit an interventions-file edit made at one of those other moments. Because a PR is commit-based, an uncommitted edit at one of those moments is not merely at risk of being torn down by a later destructive git operation (which this repo's own CLAUDE.local.md preservation-check discipline would catch) — it can simply never become part of any commit and therefore never reach `develop` at all, with nothing in this mechanism's own gates ever detecting the omission. This is exactly the silent-loss failure mode this whole task exists to close (the issue's own words: "a log preserves what happened, it does not preserve what mattered"), now displaced from the checker layer (which this rework correctly hardened) to the workflow-discipline layer that surrounds it. Adversarially re-verified: no mitigating mechanism found anywhere in the repository; the CLAUDE.local.md destructive-operation safety net does not apply (no destructive operation needs to occur — the record can be lost by simple omission from a normal PR). Suggested fix: move the commit-timing instruction into the standing producer-discipline paragraph itself ("commit an interventions-file edit immediately, at the same moment you write it, regardless of which phase or checkpoint you are at"), rather than leaving it attached only to the one named seam; keep the T-073-seam sentence as the specific case that also explains *why* an uncommitted edit would corrupt that particular check.

#### Major

- (none new this round)

#### Minor / nits

- **`skills/run/SKILL.md:60`** and **`agents/qa-verifier.md:64`** — the taskless substitution is incomplete: the prose says to use `no-task.md` in place of `<task-id>.md` when no board task is active, but the same sentence's `--task <task-id>` argument is never told to substitute to `--task no-task` in that case (the checker fully supports it — verified directly: `check-interventions.sh --task no-task <a no-task.md fixture>` → `conformant`). This is a pre-existing gap, not introduced by this rework (the identical ambiguity was present in the original `b016bce` text before the rework replaced "<that file>" with the literal `<dir>/<task-id>.md` construction); it fails closed and visibly (a `usage`/`structural` exit, not a silent bypass) if a caller gets it wrong, so it is not severity-comparable to the Blocker above.
- **`.github/workflows/check-handoff.yml:156`** — the CI dogfood step runs `bin/check-interventions.sh "$f"` with no `--task`, so it never re-validates that a *future* interventions file's BEGIN/END marker id still matches its own filename (the wrong-file protection `--task` exists for). This is a **frozen spec decision**, not an implementation defect: AC24's own check-line (`.shell-team/specs/T-1002-intervention-capture-channel.md:427`) literally requires this exact flagless invocation, and AC25 separately dogfoods `--task T-1002` against this task's own file. Confirmed by reading AC24's check-line directly.

#### Adversarial findings

Re-ran Codex with a devil's-advocate framing specifically against the commit-timing claim above (plus the two minors), instructed to try to break it by finding any mitigating mechanism elsewhere in the repository:

- Commit-timing gap: **confirmed, not broken** — Codex's own independent pass rated it P1 ("Neither later gates, QA's clean-tree convention, generic repository guidance, nor any mechanical check commits or requires committing an append made during Validate, Review, rework, STOP, or abort... CLAUDE.local.md lines 15-18 slightly reduce the chance of destructive local loss, but they are checkout-local, apply only before destructive operations, and do not make the edit part of a commit or PR, so they do not lower the severity"). This matches my own independent reading of both skill files and `bin/close-out.sh` (no clean-tree / pre-merge check exists outside the T-073 seam).
- `--task no-task` substitution gap: confirmed real, rated P3 (minor) — the checker already supports the correct invocation, so this is a documentation clarity gap, not a functional one.
- CI dogfood `--task` scope: confirmed real but explicitly grounded in AC24's own frozen check-line, not an implementation defect against any AC.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | interventions commit-timing is unspecified outside the named T-073 seam (`skills/run/SKILL.md:58`, `skills/goal/SKILL.md:52-53`) | primary: P2 (major) → adversarial: P1 (blocker) | blocker | adversarial pass found no mitigating mechanism anywhere in the repo (no other clean-tree check, no generic commit-immediately rule); the gap threatens this mechanism's core durability promise on what is, by this task's own commit history, the common case (most interventions occur outside the one narrow seam); CLAUDE.local.md's destructive-op preservation rule does not apply since no destructive operation is needed for the omission to occur |
| 2 | `--task <task-id>` is never told to substitute to `--task no-task` for the taskless case (`skills/run/SKILL.md:60`, `agents/qa-verifier.md:64`) | primary: P2 (major) → adversarial: P3 (minor) | minor | the checker already supports the correct invocation and rejects an incorrect one closed/visibly (`usage`/`structural`, not a silent bypass); pre-existing, not introduced by this rework |
| 3 | CI dogfood step omits `--task`, so it never re-validates filename/marker-id agreement for any interventions file other than what AC25 separately pins for T-1002.md (`.github/workflows/check-handoff.yml:156`) | primary: P2 (major) → adversarial: P3 (minor, frozen-spec-grounded) | minor | AC24's own check-line (`.shell-team/specs/T-1002-intervention-capture-channel.md:427`) requires this exact flagless shape; this is a frozen spec-level scope decision (AC25 covers this task's own file), not a defect against any acceptance criterion |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1002 entry back to `READY_FOR_ENG` with the Blocker above linked to this review record. The six round-1 findings are confirmed closed and do not need to be revisited; this round's rework should additionally fold in Minor 2 (cheap, touches the same paragraphs already being edited for the Blocker) while it is open — round-1's own precedent for this exact paragraph ("if [the rework] does not naturally cover it, raise it again at the next review round") is exactly what is happening here.

#### Fast-follow disposition

- `.github/workflows/check-handoff.yml:156` (CI dogfood omits `--task`, so filename/marker-id agreement is unchecked for any interventions file beyond T-1002's own) — file-an-issue: this requires reconsidering and re-freezing AC24's own scope (widening the dogfood loop to derive each expected id from its filename and pass `--task`), which is a spec-level decision for a future task, not a same-round prose fix — the same pattern this spec already used for T-1003/T-1004.

