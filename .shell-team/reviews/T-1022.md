# T-1022 QA verification record

- Verdict: PASS
- Commits reviewed: `eec6b8e` (#101 screen + header prose), `9863e6c` (#98 source-line gate + differential harness), `672e0d8` (`--help` extent conversion + errexit-safe pin re-measurement), `788da8d` (harness self-collision fix, found live during the engineer's own AC9 run), `727c602` (provenance + hand-off), plus `d26eebb` (interventions sentinel). Base `develop` @ `fa05f29` — confirmed by `git merge-base HEAD develop` == `fa05f29bf7a0c86c1fe762be3e1c62ea117c523d`, matching the frozen literal exactly (not taken on the spec's word).
- Board: T-1022 `READY_FOR_QA` → `READY_FOR_REVIEW`
- Frozen intent: v1 `8bebc90b3bf02518401b559ef1a08eacd62eb4e7`, zero re-freezes. `bash bin/check-intent.sh .shell-team/specs/T-1022-close-out-gate-symmetry.md .shell-team/todo.md` → `aligned: T-1022 v1 (8bebc90b3bf02518401b559ef1a08eacd62eb4e7) matches .shell-team/specs/T-1022-close-out-gate-symmetry.md` — run directly, not taken on the engineer's word.

## Mandatory structural gates

- `bash bin/check-intent.sh .shell-team/specs/T-1022-close-out-gate-symmetry.md .shell-team/todo.md` → `aligned` (exit 0), v1 hash unchanged.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1022.md` → `conformant: .shell-team/provenance/T-1022.md (3 decision entries, 0 sentinel)`. Read all three decision triples: (1) capturing the source-line gate's stderr into a variable rather than a second temp file, grounded in D11's minimal-trap-list reasoning and mirroring the existing interventions gate's `CK_ERR` pattern; (2) classifying `notlocated` in the differential harness by grepping close-out's own real stderr rather than a harness-local copy of the locator regex, grounded directly in D9/D1's "never a second copy of the grammar" prohibition — this is the strongest of the three, since a locator-regex copy would have been exactly the defect class #98 exists to fix, just at a different site; (3) reason F's bare (unprefixed) `<file>:<line>:` diagnostic shape mirroring `check-handoff.sh`'s own `emit()`, a narrow deliberate departure from the file's `close-out: ` convention, grounded in D4's own note-line framing. All three name two structurally valid alternatives and ground the choice in a specific spec clause or an existing sibling pattern — structurally sound, non-vacuous.
- `bash bin/check-interventions.sh --task T-1022 -- .shell-team/interventions/T-1022.md` → `conformant: .shell-team/interventions/T-1022.md (0 entries, 1 sentinel)`.

## check-acs.sh

`CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh --dry-run .shell-team/specs/T-1022-close-out-gate-symmetry.md` reviewed first (22 lines of dry-run output, all fixture construction confined to `mktemp -d "${TMPDIR:-/tmp}/t1022.XXXXXX"` scratch roots plus read-only `git show`/`git diff` calls against this repo — no command touches this checkout's own board or bin/ files outside read access. Safe to execute live.

Live run *before* this file existed:

```
CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1022-close-out-gate-symmetry.md
...
AC19: FAIL (exit 1)
AC20: SKIP (runtime — no check:)
check-acs: 18 passed, 1 failed, 1 skipped, 0 unrecognized (.shell-team/specs/T-1022-close-out-gate-symmetry.md)
```

AC19 failed solely because this review file did not exist yet (provenance and interventions records already existed) — exactly the predicted post-implementation shape recorded in the engineer's hand-off.

**Final re-run required after writing this file and the board sub-bullet below** (not yet re-run at the time this section was drafted — see the board sub-bullet for the post-write confirmation).

## 1. The differential harness is the completeness instrument — audited, not trusted

Read `lineshape_case()` in `tests/close-out/run.sh` (lines 992-1038) end to end.

- **The oracle verdict is live-computed, never hardcoded.** `oracle_rc` comes from `bash "$REPO_ROOT/bin/check-handoff.sh" "$dir/oracle.md"` run fresh per corpus line against an independently-synthesized single-entry board (lines 1014-1023); the classification (`ls_refused`/`ls_accepted`/`ls_notlocated`) is a live branch on that measured `oracle_rc`, never a pre-recorded verdict per line. Grepped the whole `lineshape_case` function body for a bare `=0`/`=1` literal assignment to a per-line verdict variable — none exists; every verdict traces to a subprocess exit status measured in that same invocation. Confirms the engineer's disclosed blind-spot claim: a hardcoded verdict would mask a flipped real verdict, and the shipped code has none.
- **Mismatch detection asserts final state, not just exit code.** The accepted branch additionally requires `grep -q '^- \[x\] \*\*T-901\*\*' "$dir/todo.md"` (the entry actually moved) and the refused branch requires `cmp -s "$dir/todo.md" "$dir/todo.orig"` (the board actually untouched) — an exit-code-only check would have missed a close-out that exits 0 without actually rewriting the board.
- **D10 axis recount**, independently re-derived from the corpus (`LS_LINES` + the two CRLF cases), not transcribed from the spec's own axis list:

| axis | represented? | evidence |
|---|---|---|
| title shape (normal/padded/ws-spaces/ws-tab) | yes | lines 961/962/971-973 |
| flag token (7 allowed / lowercase / near-miss / spaces) | yes | all 7 flags present (961-970), lowercase (974), near-miss `READY_FOR_MERGED`/`readyforqa` (975/980), spaced token (976) |
| trailing whitespace after path (none/space) | yes (space); **tab not separately instantiated** | line 966 (space); no distinct tab-after-path fixture found by grep |
| line terminator (LF/CRLF) | yes | 27 LF cases + 2 explicit CRLF cases (1047-1049) |
| title content (plain/backticked/decoy) | yes | 963 (backtick), 967 (decoy separator) |
| path shape (x.md / docs/specs / .shell-team/specs / no .md) | yes | 964/965/977/987 |
| separator shape (em-dash/hyphen-minus/no-space) | yes | 978 (hyphen-minus), 979 (no surrounding space) |
| checkbox/id shape (`[x]`/indented/double-spaced/mismatched id) | yes | 982/983/984/985 |

Two minor, non-blocking completeness nits found and independently confirmed to be behaviorally inert (not defects): (1) a **literally zero-length title** (vs. the tested 2-/4-space and tab whitespace-only variants) is not separately instantiated; (2) a **trailing tab specifically after the `.md` path** (vs. the tested trailing space) is not separately instantiated. Both are named in D10's axis list but, per `LINE_RE`'s own grammar (`.*[^[:space:]].*` for the title, `\.md$` end-anchor for the path), are provably in the *same* rejection bucket as their already-tested siblings — verified empirically, not just by regex inspection:

```
$ printf -- '- [ ] **T-901** — `READY_FOR_QA` — spec: x.md\n' ...   # zero-length title
close-out exit=1, reason F, board byte-identical (same as the 2-/4-space variants)
$ printf -- '- [ ] **T-901** demo — `READY_FOR_QA` — spec: x.md\t\n' ...   # trailing tab after path
close-out exit=1, reason F, board byte-identical (same as the trailing-space variant)
```
Recorded as `out-of-input-space`-adjacent but not actually out of scope (both are reachable board content) — disclosed here as a completeness nit against D10's letter, not a functional gap against the escape surface, since the underlying behavior is already covered by an adjacent tested value.

- **Floor recount, live**: `bash tests/close-out/run.sh` → `closeout-lineshape-differential corpus=29 refused=14 accepted=11 notlocated=4 mismatches=0`. Against the spec's floors (`corpus>=24`, `refused>=6`, `accepted>=8`, `notlocated>=3`, `mismatches=0`): all five honest, none at the floor (headroom of 5/8/3/1 respectively over the stated minimums) — not a shrunken corpus dressed up to just clear the bar.

## 2. Behavioural probes beyond the AC fixtures (4 run, all landed on a defined, correct outcome)

1. **CRLF-terminated whole board** (not just the synthesized corpus fixture) with an accepted line: `close-out.sh` exit 0, entry moved, `check-handoff.sh` green on the result. Confirms the line-terminator handling generalizes past the differential harness's own per-line fixture shape to a real multi-line CRLF board.
2. **Decoy separator inside a title, with a continuation sub-bullet carrying its own backtick and em-dash**: `- **T-901** decoy — \`x\` — spec: y.md but continues — \`READY_FOR_QA\` — spec: real.md`. Close-out refuses (exit 1, `unknown status flag 'x'`, board byte-identical via `cmp`). Cross-checked directly against a live independent oracle invocation on the identical content: same rejection, same reported flag. **No T-1022 defect** — this actually surfaces a pre-existing quirk in the *consumed* `bin/check-handoff.sh` contract (`FLAG_RE`'s unanchored match prefers the leftmost `— \`...\`` — spec:` occurrence, while `LINE_RE`'s own greedy `$`-anchored match would resolve the split differently), but `bin/check-handoff.sh` is explicitly out of scope and inviolable for this task (AC15/Non-goals), and D1's whole point is that close-out defers entirely to whatever that checker decides — which it does here, correctly, with zero disagreement against the oracle. Recorded as an issue candidate for a future task against `check-handoff.sh`'s `FLAG_RE`, not a finding against T-1022.
3. **Legacy `tasks/todo.md` layout root** (`tasks/loops/shell-team.contract.yaml` present, the real legacy-layout marker — team-paths.sh resolution confirmed, not assumed): both an accepted line (exit 0, entry moved to `tasks/todo.md`) and a rejected line (`ready_for_qa`, exit 1, reason F naming `tasks/todo.md:5:`, board byte-identical) behave identically to the default-layout fixtures.
4. **A continuation line containing a markdown table** (`| col1 | col2 |` / `|---|---|` / `| a | b |`): close-out exit 0, the whole table moved verbatim under the Done entry, per T-1016's continuation canon — no special-casing broke on table syntax.

## 3. The 5 commits' separation

Extracted `bin/close-out.sh` + `tests/close-out/run.sh` (+ fixtures + the CI workflow) at commit `eec6b8e` alone into a scratch copy **outside this checkout** and ran that commit's own test suite against that commit's own script: `shellcheck` clean, `53 PASS, 0 FAIL` (all of AC1-AC9/AC11/AC12 from the T-038 contract plus the 5 new `#101` screen cases — `closeout-handoff-sibling-{absent,nonregular,dangling-symlink,unreadable}-exit2` and the positive control). Confirms commit 1 is shippable alone with no source-line gate present, per the pre-commitment's stated carve-out requirement — the screen alone, with no dependency on commit 2's gate.

One disclosed consequence, not a defect: at commit `eec6b8e`'s own state, `tests/errexit-safe/run.sh`'s pin (still literally `close-out.sh:382` at that point, unchanged from the base ref) already no longer points at the real pinned line (verified: `sed -n '382p'` at that commit yields unrelated content, since the header prose alone shifted the file's line numbers) — exactly what the spec's own pre-commitment note anticipates ("the errexit-safe pins still need re-measuring... because commit 1's header prose alone shifts the line numbering"). This is why the pin fix is deferred to commit 3 and the three commits ship together; it is not evidence against carve-out-ability of commit 1's own code+tests.

`788da8d` (harness self-collision fix) read in full: the section-header `printf`'s decorative text originally contained the literal substring `closeout-lineshape-differential`, which is exactly the id string AC9's `grep -F -- 'closeout-lineshape-differential' | head -1` searches for — so the header line, not the real summary line, would have been picked up first. Judged this does **not** weaken the harness: the header line carries no `key=digit` tokens, so the pre-fix state would have made AC9's own parsing assertions (`test -n "$n"` etc.) fail loudly rather than silently pass with wrong numbers — a correctness bug that surfaced as a real failure when the engineer ran the check live, not a latent false-pass risk. Confirmed on the current tree: the summary line and its floors parse correctly and match a direct differential-harness run.

## 4. Compat surfaces (AC3, AC5) re-run individually, base-ref mechanism audited

`git rev-parse --verify fa05f29` and `git merge-base HEAD develop` both resolve to `fa05f29bf7a0c86c1fe762be3e1c62ea117c523d` — the base-ref literal is genuinely this branch's merge point, not stale. `git show fa05f29:bin/close-out.sh` extracts 357 non-blank lines (comfortably clears AC3/AC5's `-gt 100` positive control) and is confirmed to lack the sibling screen and the source-line gate (`grep -qF 'check-handoff.sh sibling'` — no match), i.e. it really is the pre-implementation script, not an accidental self-comparison.

- **AC3 individually**: `AC3: PASS (exit 0)` on live re-run. Base-ref `close-out.sh` (no screen) exits `1` with a missing sibling; current `close-out.sh` exits `2` on the identical byte-for-byte fixture; both boards left byte-identical; both restored-sibling positive controls exit `0`. D8(b)'s compat surface (`1`→`2`) is measured, not asserted.
- **AC5 individually**: `AC5: PASS (exit 0)` on live re-run. Current behavior refuses an out-of-vocabulary flag (exit 1, `unknown status flag`, reason F, board byte-identical); base-ref behavior on the identical fixture exits `0` and launders the flag to `READY_FOR_MERGE` in the written board (measured, confirmed present via `grep -qF -- 'READY_FOR_MERGE'`); all 7 allowed flags positive-control at exit `0` on the current script.

## 5. errexit-safe re-grounding

Re-derived the pin independently with the suite's own extraction shape: `grep -oE 'close-out\.sh:[0-9]+' tests/errexit-safe/run.sh | sed 's/^.*://' | sort -u` → exactly one number, `444`. `sed -n '444p' bin/close-out.sh` byte-compared against the pinned content (the `NOT_APPLY` heredoc entry's text after the `close-out.sh:444:` prefix) — identical.

One disclosed discrepancy against the spec's own narrative (not against AC11's mechanical requirement): the spec's D13/Notes-for-engineer describe the pin as appearing in "six lines and seven occurrences" (counting the mutation `sed` pattern's line, L377, as carrying the pin twice — the escaped match pattern and its literal replacement). Running the exact extraction regex AC11's own `check:` line uses (`grep -oE 'close-out\.sh:[0-9]+'`) against L377 finds only **one** match: the escaped form `close-out\.sh:444` (a literal backslash sits between `close-out` and `.sh` in the raw bytes, by construction of the sed pattern) does not satisfy that same regex's literal-dot requirement at that position, so only the replacement half matches. Live count: **6 lines, 6 occurrences** (not 7) by AC11's own counting method. This does not affect AC11's outcome — its floor is `-ge 6`, met either way — and does not indicate a missed site (all six narrated sites are present and each contributes exactly the number of occurrences the check's own regex actually finds). Recorded as a narrative-precision correction to the spec/hand-off, not a functional gap.

`bash tests/errexit-safe/run.sh` → `errexit-safe: all assertions passed`, including the reverse staleness check (`comm -13` — "every NOT_APPLY entry still matches a real candidate line byte-for-byte") and the suite's own internal mutation self-check (sed-rewriting the pinned `close-out.sh:444:` line's text while keeping the same file:line key) — confirmed caught by the content-aware audit, i.e. the pin move to `444` did not break that suite's own self-verification.

## 6. Independent mutation reproduction (4 of the engineer's 13, verified myself on a scratch copy outside the checkout — `git archive HEAD` into `$TMPDIR`, never on this working tree)

1. **Local `LINE_RE` copy replacing the sibling-checker delegation.** Replaced the gate's `bash "$HANDOFF_LINT" "$SYN_BOARD"` invocation with an inline regex match reproducing today's grammar. Result: with `check-handoff.sh` stubbed to `exit 2`, the mutated script produces exit `1` with the *pre-write interlock's* reason ("rewritten board would fail check-handoff.sh") instead of exit `2` with reason G ("cannot verify the Active line") — AC7 arm 2 goes red exactly as the spec predicts, because the stub's exit status is never consulted by the local-regex path.
2. **Screen moved below the source-line gate.** Moved the `-f`/`-r` sibling screen to after the gate's `case`/`esac`. Result: with `check-handoff.sh` absent, `bash "$HANDOFF_LINT"` fails inside the gate before the screen ever runs, and (because `HANDOFF_LINT` is referenced with `set -u` before its own definition in this ordering) the run crashes with an "unbound variable" error and close-out exits `1` (not `2`), with none of reason E's text present — AC2's reason-E clause goes red.
3. **errexit-safe pin off-by-one.** Shifted the pin's four visible occurrences from `444` to `445` (leaving the real `bin/close-out.sh` untouched at `444`). Result: `tests/errexit-safe/run.sh` fails two assertions — the completeness self-audit finds the real `close-out.sh:444:` candidate unaccounted for, and the reverse staleness check finds the `445` `NOT_APPLY` entry matches no real candidate — confirming AC11's content comparison is sensitive to an off-by-one, not just to a missing pin.
4. **(my own choice) Reason F naming the synthesized temp board instead of the real board.** Changed `printf '%s:%s: ...' "$BOARD" "$A_START"` to `"$SYN_BOARD" "$A_START"`. Result: on the whitespace-only-title fixture, close-out still refuses (exit 1) but AC4's `grep -qF -- "$d/bad/todo.md:5:"` clause finds no match — the stderr now names the temp file's path instead of the real board's line.

All four confirmed red under the mutation, then confirmed the corresponding real-repo behavior is correct (not mutated) in the sections above. **Disclosed: 4-of-13 independently reproduced by QA; the remaining 9 (delete-the-gate, exit-2→1 remap, stderr-text classification, reason-F-before-note ordering, `-e`-instead-of-`-f`/`-r` screen, second-trap addition + existing-trap-drop pair, corpus-shrink + hardcoded-verdict pair, `--help` hand-bumped range) are taken on the engineer's own reported self-check, not independently re-run by QA.**

## Full CI-wired list (57 steps, workflow order) — run independently by QA, not taken on the engineer's word

All 57 non-CI-infra steps from `.github/workflows/check-handoff.yml` (excluding checkout and the shellcheck-install step) run locally in the exact order the workflow lists them: **57/57 OK, 0 FAIL** (includes `shellcheck` against the full pinned file list, the shipped-board-template lint, both `check-handoff.sh` dogfood steps, every fixture suite including `tests/close-out/run.sh` and `tests/errexit-safe/run.sh`, and the PII/commit-identity dogfood steps against `develop`).

## Edge cases tried beyond the spec

- Truly empty title and trailing-tab-after-path (see §1) — confirmed behaviorally inert, not defects.
- A title containing a single unmatched backtick before the real flag separator (odd backtick count): accepted by both the oracle and close-out, in agreement — `LINE_RE`'s title slot has no backtick-balance requirement, so this is correct, consistent behavior, not a parsing break.

## Adversarial fixture synthesis checklist

- **Class 1 (beyond happy path)**: payload shape — extensively covered by AC2/AC4/AC5/AC7 plus my own probes (decoy separator, unbalanced backtick, empty title, trailing tab). Filesystem/environment state — covered (dangling symlink, non-regular file, mode-000, legacy layout root). Resource/budget competition — **structurally not applicable**: `close-out.sh` has no budget/limit concept for this gate to defend. Time/liveness — **structurally not applicable**: the script makes no documented timeout/hang-protection claim for the sibling invocation to exercise (a hang in `check-handoff.sh` would simply hang `close-out.sh` too, which is the same behavior the pre-write interlock already has and is out of this task's scope to change).
- **Class 2 (regex/charset anchoring)**: near-miss flags, lowercase, spaced tokens, decoy separators, unbalanced backticks, hyphen-minus/no-space separators — all exercised, all confirmed to land on the *specific* classification (`format mismatch` vs. `unknown status flag`), not just "some rejection".
- **Class 3 (CommonMark structure)**: not applicable — the source line is provably single-line (structurally unreachable to embed a newline, per the spec's own Input space); the one table-continuation probe (§2.4) is the closest analog and passed.
- **Class 4 (test-harness short-circuit)**: the full CI-wired list was run exactly as the workflow specifies it, in order, with real invocations (`bash tests/...`, `bash bin/...`) — no stubbed/canned command was substituted for a real one.

## Edge-case classification per the QA rules

No finding in this record is grounded solely in an out-of-scope synthetic extreme; the two minor D10-axis nits (§1) are within the spec's own declared reachable input space but confirmed behaviorally inert against an already-tested sibling value, so they are disclosed as non-blocking observations rather than treated as FAIL drivers.

---

## Codex Reviewer round 1 (2026-08-03)

### Codex Review verdict: APPROVE
- Task: T-1022
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 9 files, +934/-18 lines (`git diff --stat fa05f29...HEAD`)

One synchronous foreground `codex exec --sandbox read-only --cd . review --base fa05f29
--json` pass, reading the real files itself (its own tool calls: `git status`/`git diff
--stat`/`--find-renames`, a full-context `git diff --unified=100` on `bin/close-out.sh`,
several inline read commands, `bash bin/close-out.sh --help | tail -12`, `bash -n` on all
three touched scripts, `check-provenance.sh`, `check-interventions.sh`, `check-handoff.sh`,
a wide-context diff on the board/test-recipe/errexit-safe files, and a final `git diff
--check` + `git status --short`). Final message: "The source-line gate consistently
delegates validation to check-handoff.sh, preserves the no-write refusal paths, and
correctly screens the sibling checker. No actionable regressions were identified in the
changed code." No Blocker/Major surfaced, so the adversarial pass's sole trigger did not
fire — not run, per the opt-in design. Captured via `codex-capture.sh --alloc`/`--publish`
to the canonical `.shell-team/reviews/T-1022-codex-primary.{txt,jsonl}` (publish exit 0).

Independently re-verified myself, executing real commands against this checkout (not just
reading QA's prose or Codex's summary):

- `CHECK_ACS_TIMEOUT=600 bash tests/close-out/run.sh` and `bash tests/errexit-safe/run.sh`
  both green, live. The differential harness's own summary line, reproduced exactly:
  `closeout-lineshape-differential corpus=29 refused=14 accepted=11 notlocated=4
  mismatches=0` — matching QA's figure byte-for-byte, not taken on the record's word.
- `bash bin/check-intent.sh .shell-team/specs/T-1022-close-out-gate-symmetry.md
  .shell-team/todo.md` → `aligned` (v1 hash unchanged); `bash bin/check-provenance.sh
  .shell-team/provenance/T-1022.md` → `conformant (3 decision entries, 0 sentinel)`, all
  three triples read (canon-reuse for stderr capture, canon-reuse for the `notlocated`
  classifier, the bare-diagnostic-shape departure for reason F) — each names two
  structurally valid alternatives and grounds the choice in a specific spec clause or an
  existing sibling pattern.
- `git diff --name-only fa05f29 -- bin/check-handoff.sh` → empty (AC15 honored, byte-for-
  byte); `git diff fa05f29...HEAD -- bin/close-out.sh tests/close-out/run.sh` grepped for
  `LINE_RE`/`ALLOWED_FLAGS`/`FLAG_RE` — every occurrence is a comment or an assertion-id
  string quoting the term, never a regex literal reproducing the grammar. **D1's canon-
  reuse claim is genuinely load-bearing, not just declared**: the gate's only judgment
  mechanism is `bash "$HANDOFF_LINT" "$SYN_BOARD"`'s exit status.
- **AC7's stub design does detect a local-copy substitution.** Read the gate's `case
  "$SL_RC" in 0|1|*)` block directly: row (i) traces to the checker's measured exit status
  only, so a stub that returns a fixed status regardless of the synthesized board's own
  content — the shape a reintroduced local grammar copy would produce — is exactly what
  QA's independently-reproduced mutation #1 (local `LINE_RE` copy, AC7 arm 2 goes red)
  demonstrates. Confirms the detector, not just the design intent.
- **D3's exit mapping is total** — read the `case` statement: `0` passes, `1` takes row
  (i) (`exit 1`, reason F), everything else (including an unanticipated status) falls into
  the `*` arm via `die` (`exit 2`, reason G) — no status value can escape classification,
  and the fail-closed floor is keyed on the numeric status, never on stderr text.
- The three-part refusal order (D4) is enforced by plain sequential `printf`s each guarded
  `|| true` before the terminal `exit 1` — matches this repo's errexit-safe convention
  (confirmed no unguarded `printf … >&2` followed by further code was introduced; `tests/
  errexit-safe/run.sh`'s completeness self-audit, which fails closed on any unguarded site,
  stayed green).
- **The sibling screen (D5) is genuinely singular and ahead of the first invocation.** Read
  the script top-to-bottom: T-068's disposition gate, then T-1017's interventions gate
  (unchanged), then the one `HANDOFF_LINT` screen, then the source-line gate, then the
  pre-write interlock re-using the same already-screened `$HANDOFF_LINT` variable — no
  second `-f`/`-r` test anywhere in the diff, and the pre-write interlock cannot reach an
  unbound-variable state the way QA's mutation #2 (screen moved below the gate) produced.
- **The differential harness's `notlocated` branch is not a second locator-regex copy.**
  Read `lineshape_case()` directly: it greps the real `close-out.sh` invocation's own
  stderr for the literal `not found as a top-level entry` string, and the accepted/refused
  branches call `bash bin/check-handoff.sh` fresh, per corpus line, on an independently-
  built single-entry board — no `=0`/`=1` literal verdict assignment exists anywhere in
  the function body. `mismatches=0` is not vacuous: both non-zero classes (`refused=14`,
  `accepted=11`) clear their floors (`>=6`/`>=8`) with headroom, so the two-way implication
  is exercised in both directions on a real, non-trivial corpus.
- **errexit-safe re-grounding, re-measured myself**: `grep -noE 'close-out\.sh:[0-9]+'
  tests/errexit-safe/run.sh` → six matches, all `444`, at lines 312/371/377/381/382/384;
  `sed -n '444p' bin/close-out.sh` byte-compares against the pinned `NOT_APPLY` content.
  **QA's narrative-precision finding, independently reproduced**: the spec's own D13 prose
  claims "six lines and seven occurrences" (counting the mutation `sed` pattern's escaped
  match half as a second occurrence at L377); the AC11 `check:` line's own extraction
  regex (`grep -oE 'close-out\.sh:[0-9]+'`, literal dot) does not match `close-out\.sh:444`
  (the sed pattern's escaped half, which carries a literal backslash between `close-out`
  and `.sh`) — so the mechanically-measured count is six lines / six occurrences, not
  seven. This does not affect AC11's outcome (`-ge 6` is met either way, and all six real
  sites are present with correct, byte-identical content) and is prose-only against the
  spec's own D13 narrative, not against any graded `check:` line. Agree with QA's
  classification: minor, non-blocking, a narrative-precision correction rather than a
  functional gap.
- **Compat surfaces (AC3/AC5) and the board structural diff.** `git diff fa05f29...HEAD --
  .shell-team/todo.md` shows a pure insertion under `## Active` — zero `## `-heading lines
  touched, no accidental heading replacement. `bin/check-handoff.sh` untouched (AC15) means
  the compat measurements (exit `1`→`2` on a missing sibling; laundering→refusal on an
  out-of-vocabulary flag) are read correctly against the real base ref, not a moving one.

### The pre-existing `check-handoff.sh` FLAG_RE quirk — judged, not rediscovered

QA's behavioural probe #2 (a decoy-separator title with a continuation sub-bullet carrying
its own backtick/em-dash) surfaces a real ambiguity in `FLAG_RE`'s unanchored match against
a line with two `— \`...\`— spec:` occurrences. I agree with QA's disposition on both
counts: (1) **not a T-1022 defect** — `bin/check-handoff.sh` is this task's inviolable,
byte-unchanged Non-goal (AC15 confirms zero diff), D1's whole design is to defer entirely
to whatever that checker decides, and it decided here exactly as an independent oracle
invocation on the identical content also decided — zero disagreement, which is the
correctness property D1 exists to guarantee, not a gap in it. (2) **it does merit a fast-
follow issue against `check-handoff.sh` itself** — a decoy separator inside a title is
real, reachable board content (this repository's own board carries backticked tokens and
em-dashes in titles), and `FLAG_RE`'s leftmost-match behavior on such a line is a genuine,
previously-undocumented ambiguity in the consumed contract, independent of this task.

### Issue-canon note

This task's Goal/D1/D2 text is my receiving-side canon for judging whether the
implementation answers GitHub issues #98 and #101 correctly, and the spec quotes both
issues' content in detail (`## Problem`, board `- source:` sub-bullet). This sandbox has
no GitHub access, so: **internal consistency verified / issue-canon match
environmentally-unverified (relies on orchestrator's primary confirmation).** Everything
graded here is checked against the spec's own quoted text and the frozen intent block, not
against a live re-read of the issues themselves.

### Blockers

None.

### Major

None.

### Minor / nits

- **`.shell-team/specs/T-1022-close-out-gate-symmetry.md`, D13/Notes for engineer** — the
  "six lines and seven occurrences" narrative is off by one against the AC11 `check:`
  line's own mechanical count (six lines, six occurrences); already found and disclosed by
  QA, independently reproduced above. Does not affect AC11's outcome (floor `-ge 6` is met
  either way). Spec prose only, not a graded artifact.
- **`bin/check-handoff.sh`'s `FLAG_RE`** — unanchored match on a title containing two
  `— \`...\`— spec:` occurrences resolves to the leftmost one, which can differ from
  `LINE_RE`'s own greedy `$`-anchored resolution of the same line. Found by QA, judged
  above: correctly out of this task's scope (the file is byte-unchanged and inviolable),
  but real and reachable, so worth a fast-follow issue against a future `check-handoff.sh`
  task.

### Adversarial findings

Not run — the primary pass surfaced no Blocker/Major, so the sole trigger condition for
the opt-in adversarial pass did not fire (per this role's step 3/5 design: the parallel
devil's-advocate pass stays opt-in, never launched speculatively). My own independent
reading (the case-statement totality check, the canon-reuse grep sweep, the differential
harness's live-oracle audit, and re-running both owning suites myself) served the same
function this round: no second Blocker/Major-shaped finding surfaced from any of the two
independent readings (Codex's, mine).

### Synthesis audit ledger

no severity changes

### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1022 entry to `READY_FOR_MERGE`.

### Fast-follow disposition

- `bin/check-handoff.sh`'s `FLAG_RE` leftmost-match ambiguity on a decoy-separator title
  (Minor, above) — **file-an-issue**: real, reachable board content can trigger it, it is
  independent of this task's own scope, and QA's probe is a ready-made reproduction case
  for whoever picks it up.
- `tests/errexit-safe/run.sh`'s `file:line:content` pin-keying redesign — **file-an-issue**,
  echoing the spec's own Non-goals/Open-questions disposition (recorded there as an issue
  candidate, not filed from pm-spec's role): six tasks (T-1006, T-1007, T-1008, T-1016,
  T-1017, T-1022) have now each paid one re-measurement to this same mechanism.
- The spec's D13 "six lines / seven occurrences" narrative-precision slip (Minor, above) —
  **won't-fix** as a standalone action: it is frozen spec prose describing a measurement
  whose actual `check:`-enforced floor (`-ge 6`) is unaffected either way, and this repo's
  own precedent (T-1021's board-prose slip) is not to amend frozen/historical text
  retroactively — the correction lives in this review section instead.
- QA's two disclosed D10-axis completeness nits (a literally zero-length title; a trailing
  tab specifically after the `.md` path, neither separately instantiated in the
  differential corpus) — **won't-fix**: QA's own record shows both landing on the identical
  refusal outcome as their already-tested sibling values, by direct empirical probe with
  shown output, so instantiating them as new corpus lines would add coverage with no
  behavioural difference to detect.
