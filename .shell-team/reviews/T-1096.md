# T-1096 — Codex Reviewer records

## Spec review

### Codex Spec-Review verdict: REQUEST_CHANGES
- Task: T-1096
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Mode: Spec-review (specify seam, T-1092)
- Consumer class: adopter-executed `bin/` scripts, an added refusal class inside `bin/close-out.sh`, two skill gate bullets, an agent producer-contract paragraph, and two `docs/adopting.md` sections — full adversarial depth per this spec's own `## Verdict scope` (the 2026-08-22 dev-scaffold carve-out does not apply to these deliverables; it does still govern this spec's own inline `- check:` lines, which were read at the lighter depth accordingly and produced no findings worth reporting at that depth).
- Input reviewed: `.shell-team/specs/T-1096-selection-trust-gates.md` in full (frozen intent block, `## Assumptions`, `## Blast radius`, `## Verdict scope`, `## Notes for engineer`), cross-read against `agents/codex-reviewer.md` (lines 90–224, especially the append-only citations at :195/:197/:212 and the write-boundary at :206), `skills/run/SKILL.md` (the Conformance-read confirmation gate at :51 and the Specify-seam spec-review gate at :52, plus the dispatch mechanics at :42/:44), `agents/pm-spec.md` (:108–133, the Conformance-formatter-mode and Spec-review hand-off sections), the real producer output in `.shell-team/reviews/T-1092.md`, `T-1093.md`, `T-1094.md`, `T-1095.md`, and the live board `.shell-team/todo.md`. No `--base`, no branch diff — this pass runs before any implementation exists, per this task's own `- verification-ceiling:` and `## Blast radius` framing.
- issue-canon note: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — the mandate turns partly on issues #336, #341 and #344's own acceptance sketches and defeat-class lists, quoted into the spec from a relayed scratch copy; this role holds no GitHub-reading tool. The spec's own `## Assumptions` records the scratch copy's fetch-time provenance (per-issue `updated_at` timestamps, no edits since fetch) as the primary confirmation this note defers to.

#### Domain-premise findings (Codex primary pass, normalized and independently re-derived against real repository state)

1. **Major** (mandate item 2 — does `tamper-arm-rule-v1` actually decide?). Applied literally, question 1 ("is the obligation executed by a script this plugin ships, and does its verdict gate a loop transition?") cannot be evaluated for a genuinely new, not-yet-built obligation: "is it executed by a script" has no answer before an implementation choice has already been made, so reading the question as observational always routes an unbuilt obligation to arm B by default (the "otherwise" branch), regardless of whether its verdict would gate a loop transition. This contradicts the Goal sentence's own claim — "one criticality rule that decides, for any record-checking obligation, whether it lands as a tested `bin/` primitive or stays an enumerated-case instrument" — and the re-evaluation trigger this rule ships with cannot rescue it, since the trigger fires on a *defeat* of an existing checker or a *third promoted* obligation, never on this circularity. Applied post-hoc to #341/#344 (both already decided by the operator's own planning ratification to be scripts) the rule reads correctly, but that is because the arm was chosen elsewhere first and the rule only labels the result — exactly the "describes rather than decides" failure mode this task's own `## Verdict scope` names as the thing to probe. Codex's own independent pass reached the identical conclusion unprompted (converging evidence, not a restated finding).
   - Route-back ask: rephrase question 1 as a prescriptive test on the *consequence*, not the *current implementation state* — e.g. "will the obligation's verdict gate a loop transition? If so, it must be executed by a shipped script (arm A), and a missing script is an implementation gap, not evidence for arm B." Keep the two existing clauses (arm follows who executes/what gates; reclassification) — they are sound and do not need to change.

2. **Blocker** (mandate item 4 — §3's two-source design). §3's own body states "Source 1 is `- entry-mode: pm-authored|operator-authored`, transcribed as a sub-bullet on the task's Active entry by `pm-spec`" — i.e. a literal, durable board sub-bullet a script can read. But `agents/pm-spec.md:119–125` (unedited by this task except for the disclosure-sentence-count correction) only instructs pm-spec to emit `- entry-mode:` **in its hand-off** — the conversational return value the *coordinating session* reads to run the existing prose gate — and `skills/run/SKILL.md:51` explicitly contrasts "that hand-off" against "the board's own `- dispatch:` record" as two different things, i.e. under the *current* contract `- entry-mode:` is not written onto the board at all. I independently confirmed this against the live repository: `grep -n '^  - entry-mode:' .shell-team/todo.md` across the **entire** board returns exactly **one** hit — this task's own self-referential entry (line 17) — and the identical single-hit pattern holds for `- spec-review:` (line 26). Neither has ever appeared as a literal board sub-bullet on any other task in this repository's history; both were apparently added to T-1096's own entry specifically to satisfy this task's own **AC17** dogfooding requirement, not because any shipped instruction produces them for tasks in general. As specified, `bin/check-entry-mode.sh` reads source 1 off the board, but nothing in this task's diff (per its own `## Body-to-AC correspondence` and `AC20` allow-list) adds an instruction telling `pm-spec` to persist `- entry-mode:` onto the board for every task going forward. Per **AC10**'s own matrix, "source 1 absent exits 1 (never a silent pass)" — so once `skills/run/SKILL.md`'s step-2 region wires this gate as a real exit code (**AC13**), every future task's pre-freeze gate would refuse by default, including entirely legitimate `pm-authored` work, until someone notices and patches `agents/pm-spec.md` outside this task's own scope. This is a functional gap in the shipped mechanism, not a theoretical one, and Codex's independent pass raised the identical mismatch (citing `agents/codex-reviewer.md:197` and `skills/run/SKILL.md:51`'s hand-off-not-board language against the spec's board-only fixture design).
   - Route-back ask: add an explicit instruction to `agents/pm-spec.md` that `pm-spec` writes `- entry-mode:` as a literal sub-bullet on the board entry it creates/updates (mirroring what it already does for `- dispatch:` sub-bullets at the seam), and extend an acceptance criterion (most naturally **AC14**, which already touches this file's disclosure sentences) to assert that instruction's presence in the shipped bytes. This closes the gap without touching the two-source *design* itself — source 1 stays the board sub-bullet, it just needs a writer.

3. **Major** (mandate item 3 — is the closed four-form grammar genuinely immune to all six named classes, or is there a bypass?). The requirement floor's defeat class 6 ("Missing/non-ASCII separator") is stated generically, and the shipped fixtures (**AC5**) exercise it only against the `## Spec review` **heading** (`##Spec review`, a U+00A0 heading separator) — never against the **verdict line's own** separator, the literal ASCII space between `verdict:` and the token that the collection step's prefix match (`^### Codex Spec-Review verdict: `) depends on exactly as much as the old heading match did. A line reading `### Codex Spec-Review verdict:<U+00A0>REQUEST_CHANGES` fails that exact prefix match and is therefore never collected at all — not refused as an unrecognised tail (which **AC6** correctly tests), simply invisible — so if it is the true latest round and an earlier, well-formed `APPROVE` line precedes it, the scan's "last collected line" is the stale `APPROVE` and the gate **PASSes** a task whose real latest verdict was a refusal: the same dangerous direction as defeat classes 1, 2 and 6, just relocated from the heading (which the redesign correctly stopped consulting) to the verdict-line's own separator (which the redesign still implicitly trusts). Separately, and independently found: a naive `while IFS= read -r line; do … done < "$RECORD"` implementation — a common bash idiom for "read every line" — silently skips a final line that has no trailing newline, because `read`'s own exit status (used as the loop's continuation test) goes non-zero on that last partial read; a record whose true final verdict line happens to be the file's very last byte with no trailing `\n` would be dropped by that idiom specifically, again resurrecting an earlier stale verdict.
   - Classification flag, not a decision I make unilaterally: whether this is a **restatement of already-named defeat class 6** (fixable by adding collection-time guarding plus a fixture, no spec rework needed) or **a new independent text-extraction defeat class** matters materially, because per this spec's own §4 pre-commitment a new independent class against §2's core "does **not** buy a patch round… it stops the task and returns it to planning" (citing `.shell-team/lessons.md:1325`). I read it as the former (same root cause — an exact-byte separator assumption defeated by encoding — merely untested at a second location) since the requirement floor's own class-6 wording is not heading-scoped, but this classification call should be made explicitly by whoever routes this finding, not silently assumed either way.
   - Route-back ask: require the collection step to explicitly refuse (not silently skip) a column-zero line beginning with the verdict stem `### Codex Spec-Review verdict:` whose next byte is not exactly one ASCII space, and require whatever primitive reads the record to be EOF-safe for a final line lacking a trailing newline (e.g. `grep`/`awk` over the whole file rather than a `while read` loop, or an explicit final-read handled the same as any other). Add both fixtures to **AC5**/**AC6**.

4. **Major** (mandate item 4 — the id-paired flagged-gap/flagged-gap-resolution grammar). **AC12**'s matrix covers missing, orphaned, duplicated and empty-after-colon resolutions, but not a **malformed marker near-miss** — e.g. `- flagged-gap (g1) : <text>` with a stray space before the colon, or a marker using a delimiter other than parentheses. A strict extractor that requires the exact `- flagged-gap (<id>): ` shape would simply fail to recognise such a line as a gap at all, so it contributes to neither the gap set nor the resolution set; the record then reads as "zero gaps, zero resolutions" — **AC12**'s own explicitly-permitted conformant nothing-to-answer case — silently converting a genuinely flagged, genuinely unanswered gap into the pass case. This is the same "absence versus a failed read" failure shape defeat class 5 names for the CRLF case, applied here to the gap-marker grammar rather than to line-ending handling.
   - Route-back ask: have the script scan for the marker **stem** (`- flagged-gap`, `- flagged-gap-resolution`) independently of the exact parenthesis/colon spacing, and refuse (rather than silently ignore) any line that carries the stem but does not parse into a clean `(<id>): <text>` shape. Add a malformed-spacing fixture to **AC12**.

5. **Major** (mandate item 5 — is the pre-commitment coherently scoped?). §4's "Droppable 1st" disposition sends §3's checker "back to issue #341" as a "zero-regression carve-out," but **AC10**–**AC17** are frozen acceptance criteria requiring `bin/check-entry-mode.sh` to exist and its fixtures to pass, and the Goal sentence itself states that both obligations ship. Removing or rewriting seven acceptance criteria and the Goal's "both gates ship" claim is, by `agents/pm-spec.md:102`'s own classification, unambiguously a **class-B re-freeze** ("any delta touching the Goal sentence, Non-goals, a criterion's prose, or Input space — always needs the human GO"), yet §4 states the drop "is executed on the loop's own authority, not an option presented for negotiation" with no mention of the class-B re-freeze this execution would necessarily require, and no stated abort/re-scope mechanics for the frozen ACs left behind. T-1092's own precedent for this exact carve-out shape (issue #344's own genesis) was in fact "operator-ruled" — a human decision — which is in tension with "executed on the loop's own authority" as currently worded. Codex's independent pass raised the identical inconsistency.
   - Route-back ask: state explicitly that executing the droppable-1st disposition requires a class-B re-freeze (removing/rewriting **AC10**–**AC17** and the Goal sentence's "both gates ship" claim) and that this re-freeze needs human GO per the standing class-B rule — i.e. the *decision to drop* is the loop's own to make per the pre-commitment, but *executing* it still routes through the existing class-B mechanism rather than skipping it.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `tamper-arm-rule-v1` circular for a not-yet-built obligation | Codex-native (map: major); independently re-derived by this role before reading Codex's output | major | contradicts the Goal's own decisiveness claim; fixable by rephrasing question 1 as a prescriptive consequence test rather than an observational one — does not require redesigning the two existing clauses |
| 2 | §3 source 1 is specified as a board sub-bullet the current `agents/pm-spec.md`/`skills/run/SKILL.md` contract never writes there | Codex-native (map: blocker); independently confirmed via `grep -n '^  - entry-mode:' .shell-team/todo.md` returning exactly one hit, on this task's own self-referential entry | blocker | would make the shipped gate refuse every future task's freeze by default once wired as a real exit code (**AC10**'s own "absent = refusal, never silent pass" rule), until an out-of-scope patch closes it; cheaply fixable by adding one instruction plus one AC-extension, not a redesign |
| 3 | verdict-line's own separator is exact-byte-trusted the same way the heading's separator used to be, and a `while read` idiom would drop a final line lacking a trailing newline | Codex-native (map: major); independently re-derived and extended (the EOF/trailing-newline half is this role's own addition, not in Codex's raw output) | major | same dangerous direction as defeat classes 1/2/6, at an untested location; classification as "class 6 restated" vs. "a new independent class" is flagged rather than decided unilaterally, since the pre-commitment's stop-and-return-to-planning rule turns on that classification |
| 4 | malformed flagged-gap marker near-miss reads as the conformant zero-gaps case | Codex-native (map: major) | major | same "absence vs. failed read" shape as defeat class 5, applied to the gap-marker grammar instead of line endings; closable with a stem-scan plus one fixture |
| 5 | droppable-1st disposition's execution mechanics (class-B re-freeze needed) are unstated | Codex-native (map: major); independently corroborated against `agents/pm-spec.md:102`'s own class-B definition and T-1092's own operator-ruled precedent for this exact carve-out | major | the *decision* to drop stays the loop's own per the pre-commitment; *executing* it still needs the standing class-B human-GO mechanism, which the current text does not name |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (per the spec-review-mode Route-back paragraph, `agents/codex-reviewer.md:197`: this task's own `- entry-mode: pm-authored` board sub-bullet — the one instance that exists in the whole board, on this task's own entry — agrees with the Routing Map's printed `specify — pm-authored` decision transcribed at `- dispatch: specify — pm-authored — …`, so both sources agree on `pm-authored` and the route-back target is `pm-spec`).
- Two Blockers (findings 2 and 5) must close before the freeze proceeds; the three Majors (findings 1, 3, 4) should close in the same round since they are each cheap, targeted corrections rather than design-level rework.
- Per this task's own §4 pre-commitment: this is the first round against the never-dropped §1/§2 core and the first round against the droppable-1st §3 component. Findings 2, 4 and 5 land on §3 (droppable-1st); findings 1 and 3 land on §1/§2 (never-dropped) but are, on this reading, cheap corrective edits rather than a new independent defeat class triggering the stop-and-return-to-planning rule — finding 3 explicitly flags that classification call for whoever routes this rather than assuming it.
- This APPROVE/REQUEST_CHANGES gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- no fast-follow deferrals

Raw Codex output: `.shell-team/reviews/T-1096-codex-specreview.txt` (final message), `.shell-team/reviews/T-1096-codex-specreview.jsonl` (full event stream).

---

### Codex Spec-Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1096
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Mode: Spec-review (specify seam, T-1092), round 2 — dispositions of round 1's five findings
- Consumer class: unchanged from round 1 — full adversarial depth on the shipped-surface deliverables; the dev-scaffold carve-out still governs only this spec's own inline `- check:` lines.
- Input reviewed: the revised `.shell-team/specs/T-1096-selection-trust-gates.md` in full (424 lines, re-read end to end per this role's own "read a records-only deliverable end to end" rule, not diffed against round 1 — the file is untracked working tree with no prior commit to diff against), cross-read against `agents/pm-spec.md:100–135` and `skills/run/SKILL.md:40–58` to confirm those files are genuinely unmodified (pre-implementation) and the spec's own claims about them are read as design proposals, not as claims about current repository state. `git status --short agents/pm-spec.md skills/run/SKILL.md bin/check-entry-mode.sh bin/check-spec-review.sh` confirmed empty — no implementation has landed; this remains a pre-implementation spec review.
- Scope: verify each round-1 finding's disposition actually closes it, hunt for new-defect introduced by the rework, and do not re-open settled ground (append-only-ness, the fenced-code-block Non-input, residuals (i)/(iii), and the no-live-firing residual stay out of scope, unchanged from round 1).

#### Disposition of round 1's five findings

1. **F1 (was Blocker) — CLOSED.** §1's question 1 is rebuilt as two prescriptive conjuncts ("will the obligation's verdict gate a loop transition" + "is the judgment mechanically executable from committed bytes"), explicitly stated as consequence tests rather than observations of what currently exists ("where no shipped script executes an arm-A obligation yet, that is an implementation gap to close, and never evidence for arm B"). Re-probed independently with a hypothetical obligation the spec never mentions (a commit-message-format checker gating a merge status advance): both conjuncts return a determinate arm A with no further judgment needed, confirming the rule is genuinely decisive rather than merely descriptive. The three worked probe cases added to the spec's own text (advisory-verdict checker → arm B; orchestrator prose duty → arm B via the second conjunct; tamper-relevant arm-B instrument → arm B then reclassified to arm A) each independently re-checked out correctly. Codex's own independent probe (a hypothetical release-manifest checker) reached the same conclusion.
2. **F2 (was Blocker) — CLOSED at the spec-design level.** §3 now states explicitly that source 1 needs a writer and ships the producer duty in the same diff (`agents/pm-spec.md` and `skills/run/SKILL.md` both gain the instruction to transcribe `- entry-mode:` as a board sub-bullet), and **AC14** was correctly extended to assert the new fragment `as a sub-bullet onto the board entry` present at HEAD and absent at the branch point in both files — with the absence independently re-confirmed at the branch point by this task's own freeze run (row 18, `## Assumptions`). Codex's raw pass on this item returned `partially-closed / blocker`, citing that `agents/pm-spec.md` and `skills/run/SKILL.md` "currently contain zero occurrences of the required board-sub-bullet wording and have no diff from the branch point" — **this is judging the wrong artifact for this seam.** This is a spec-review pass at the Specify seam, before `engineer` is ever invoked; independently confirmed via `git status --short` that neither file has been touched (no implementation exists yet), exactly as `agents/pm-spec.md`, `skills/run/SKILL.md` and `docs/loop-engineering/record-tamper-resistance.md` are *all* correctly absent/unmodified pre-implementation (the spec's own "freeze notes" disclose AC2–AC17/19–21 as expected pre-implementation reds for the identical reason). What this pass judges is whether the *design* the spec proposes would close the gap once implemented and whether an acceptance criterion actually verifies that it did — both hold. Overriding Codex's raw disposition on this item: **closed**, not blocker.
3. **F3 (was Major) — CLOSED.** The collection net is widened from stem-plus-space to the stem alone, with the separator validated as part of the remainder (**AC5** gains case (6b), **AC6** gains the separator-axis assertion). EOF-safety for a final line with no trailing newline is stated as an explicit correctness constraint with its own fixture (**AC5** case (7)). The classification of this finding as "defect class 6 restated at an untested location, not a new independent class" is correct and independently re-confirmed: class 6's own verbatim wording in the six-defeat-class list names a missing-or-non-ASCII separator defeating a match as the root cause, with no heading-scoping in that wording — only its two illustrative examples happened to be headings. Codex's independent pass reached the identical classification judgment.
4. **F4 (was Major) — CLOSED.** §3 now requires stem-scanning with a malformed-marker refused rather than silently ignored, and **AC12** gained fixtures for the stray-space, wrong-delimiter and no-id near-misses on both the gap marker and the resolution marker, plus an explicit stem-ordering-trap fixture (`- flagged-gap-resolution` tested before `- flagged-gap`, since the former is a superstring of the latter) that only passes under the correct ordering.
5. **F5 (was Major) — CLOSED.** §4's revised text cleanly separates what is the loop's own authority (stopping patch rounds, assembling and proposing the drop package) from what is not (ratifying the resulting Goal/AC delta, which — per `agents/pm-spec.md:102`'s own class-B definition, unchanged — always needs human GO through the standing class-B re-freeze mechanism). This resolves the tension without inventing a new gate or bypassing the existing one, and correctly reconciles with T-1092's own precedent (an operator-ruled carve-out) rather than contradicting it.

#### New findings this round (round-1's settled ground is not reopened by any of these)

1. **Blocker — `## Notes for engineer`'s "reader's algorithm" paragraph still instructs the OLD, defeated collection net.** The paragraph (near the end of the file) reads: "Collect **every** line matching `` `^### Codex Spec-Review verdict: ` `` — do not filter by tail at collection time" — this is the stem-**plus-one-literal-ASCII-space** net that §2's own body (line 81) now explicitly names and rejects as "the third correction to the candidate design" (the same net finding 3 showed relocates, rather than closes, defect class 6). If an engineer implements literally per this note rather than per §2's corrected body text, the shipped script would fail to collect a latest verdict line whose separator is missing, a tab, or non-ASCII — exactly reintroducing the false-PASS this round's own **AC5(6b)** and **AC6** separator-axis fixtures were added to catch. The blast radius is bounded (an engineer who copied this note literally would hit a red **AC5**/**AC6** at QA and have to reconcile the contradiction themselves), but a contradiction between the frozen §2 design and this task's own most literal implementation instruction is not a state that should ship past a freeze. Independently confirmed by Codex's own pass (its `new-1`, identical root cause and identical location).
   - Route-back ask: change the note's collection instruction to `^### Codex Spec-Review verdict:` (stem alone, no separator assumption), with the remainder validated as exactly one ASCII space plus the closed grammar — matching §2's own corrected wording verbatim.
2. **Major — `## Assumptions`' count-premises preamble contradicts its own row 18.** The preamble (just above the count-premises table) states: "Row 18 was added when spec-review round 1's finding 2 introduced a new zero-at-base premise, and it is owed its measurement before the hash is recorded — **recorded as outstanding** rather than left to look measured." Row 18 itself then states a completed value: "branch point 2e4d99c: **0** files repository-wide (implying 0 in each named file) — **freeze run 2026-08-24**." One of these two statements is stale. The measured value itself looks correct and consistent with **AC14**'s own requirement (0 at the branch point for both files), so this reads as a preamble sentence left over from before the measurement was actually run rather than a wrong measurement — but a spec that prices its own count-discipline as rigorously as this one's `## Acceptance criteria` preamble does (the row-14/row-15 "command was the defect, not the premise" correction) should not carry an unresolved self-contradiction about whether its own newest premise has been measured. Independently confirmed by Codex's own pass (its `new-2`).
   - Route-back ask: delete or reword the preamble's "recorded as outstanding" clause to match row 18's own completed state (or, if the measurement genuinely has not run, remove row 18's "freeze run" value and its specific figure until it has).
3. **Major — the frozen Goal sentence still describes `tamper-arm-rule-v1` using the exact circular framing F1 fixed.** `## Goal`'s `**Goal (one sentence).**` paragraph — inside the frozen intent block, hashed by `check-intent.sh` — reads "arm A when a **plugin-shipped script executes the obligation** and its verdict gates a loop transition, arm B otherwise." This is the **observational** ("is it executed by a script") framing §1's own rebuilt rule explicitly moved away from (§1's "Two clauses" paragraph now reads "never by which scripts happen to exist"), preserved verbatim in the single most load-bearing, most-quoted sentence of the whole spec. The `## Body-to-AC correspondence` table's row 2 ("The rule is a decision procedure whose first question is **who executes the obligation** and what its verdict gates") carries the identical stale framing at a lower-stakes location (an index table no criterion checks, rather than hashed frozen prose). A future reader who quotes only the Goal sentence — which this repository's own convention treats as canonical — would reconstruct the exact circular rule F1 was written to remove. This is a distinct, more precisely located finding than Codex's own `new-3` (which named the same root cause but did not distinguish the Goal sentence's frozen, hashed status from the correspondence table's unhashed one).
   - Route-back ask: rewrite the Goal sentence's arm-A clause to state the same two prescriptive conjuncts §1 now uses ("when the obligation's verdict gates a loop transition and its judgment is mechanically executable from committed bytes"), and correct Body-to-AC row 2 to match.
   - Pre-commitment note, flagged rather than decided: round 1 landed an independent Blocker against §1 (the rule's own circularity) and this round lands an independent Major against §1 (the Goal sentence's stale restatement of that same circularity) — two consecutive rounds, arguably the same component. Whether this satisfies §4's factual trigger for §1 specifically is a call for whoever routes this finding: §4's stated stop-consequence text is worded narrowly around "a new independent **text-extraction** defeat class against **§2**," and this is neither a text-extraction defeat nor against §2 — it is a documentation-propagation gap against §1's own restatement, not a flaw in §1's operative rule (which is confirmed sound this round). I read it as not triggering the stop-and-return-to-planning consequence, but say so explicitly rather than deciding it silently, per the same norm §2's own text uses for its class-6-restatement call.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| F2 | source-1 durability gap disposition | Codex-native (this round's pass): partially-closed / blocker | closed | Codex judged the unmodified `agents/pm-spec.md`/`skills/run/SKILL.md` as a live defect; this is a pre-implementation spec-review seam (confirmed via `git status --short`, zero diff on any deliverable file) where the correct question is whether the *design* closes the gap and whether an AC verifies it once implemented — both hold, matching every other deliverable's identical, correctly-disclosed pre-implementation absence |
| new-3 | Goal-sentence self-contradiction vs. Body-to-AC row 2 | independently found by this role; Codex's own pass folded the same root cause into a single lower-severity `minor` finding (its `new-3`) without distinguishing the Goal sentence's frozen/hashed status | major | the Goal sentence sits inside the intent block and is hashed by `check-intent.sh`; a stale restatement there is more consequential than the same staleness in an unhashed correspondence-table row, so this role's severity (major) supersedes Codex's (minor) for the Goal-sentence instance specifically |

#### Recommendation
- REQUEST_CHANGES (round 2) → route back to **pm-spec** (same resolution as round 1: this task's own `- entry-mode: pm-authored` board sub-bullet agrees with the Routing Map's printed `specify — pm-authored` decision).
- One Blocker (Notes-for-engineer's stale collection-net instruction) and two Majors (the Assumptions preamble/row-18 contradiction; the Goal sentence's stale circular-framing restatement) must close before the freeze proceeds. All three are targeted textual corrections inside an already-sound design — none require redesigning §1, §2, §3 or §4's own operative logic, which this round confirms is sound.
- Round 1's five findings (F1–F5) are confirmed closed; none of round 1's settled ground (append-only-ness, the fenced-code-block Non-input, residuals (i)/(iii), the no-live-firing residual) is reopened by this round's findings.
- Per §4's pre-commitment: this round finds no new independent defeat against §2's core, and finds no defect against §3's droppable-1st component. The Goal-sentence finding lands a second consecutive round's independent finding against §1 (never-dropped); whether that satisfies the factual trigger for §1 specifically is flagged above for whoever routes this finding, not decided here.
- This APPROVE/REQUEST_CHANGES gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- no fast-follow deferrals

Raw Codex output: `.shell-team/reviews/T-1096-codex-specreview.txt` (final message, this round's capture — the canonical basename is reused across rounds per this role's own naming rule, so round 1's raw capture is not separately preserved), `.shell-team/reviews/T-1096-codex-specreview.jsonl` (full event stream, this round).

---

### Codex Spec-Review verdict: APPROVE (round 3)
- Task: T-1096
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Mode: Spec-review (specify seam, T-1092), round 3 — dispositions of round 2's three new findings, plus the orchestrator's pre-commitment classification ruling
- Consumer class: unchanged from rounds 1–2 — full adversarial depth on the shipped-surface deliverables; the dev-scaffold carve-out still governs only this spec's own inline `- check:` lines.
- Input reviewed: the revised `.shell-team/specs/T-1096-selection-trust-gates.md` in full (424 lines), read end to end again per this role's own "read a records-only deliverable end to end" rule. Note on this round's Codex invocation: the first attempt was launched as a background Bash call and stalled unrecoverably (its JSONL capture stopped growing for 14+ minutes with no terminal marker and no final-message file written) — a background-process-untracked-by-the-harness failure the coordinating session identified and directed me to recover from by re-running in the foreground with an explicit long timeout rather than continuing to wait; the stale partial capture was discarded before the successful foreground rerun below, so nothing from the stalled attempt is cited in this record.

#### Disposition of round 2's three findings

1. **NEW-1 (was Blocker) — CLOSED.** `## Notes for engineer`'s reader's-algorithm paragraph is rewritten to collect the column-zero stem `^### Codex Spec-Review verdict:` alone (no trailing space, no separator assumption), validate the separator and grammar in the remainder, read the whole file with a `grep`/`awk`/`tail` shape rather than a `while read` loop (explicitly naming the EOF-drop failure mode a `while read` loop would hit), and keep three distinguishable refusal messages (zero collected / in-grammar refusal / unrecognised remainder). It now explicitly names the old stem-plus-space net and states it is "the defeated form §2's own body names and rejects" — independently confirmed exactly two literal mentions of that rejected form remain in the whole file (§2's own body at the point it was corrected, and this Notes-for-engineer paragraph), both unambiguous that the form is rejected rather than current instruction. Matches Codex's independent finding exactly.
2. **NEW-2 (was Major) — CLOSED.** `## Assumptions`' count-premises preamble now reads "All eighteen rows have been measured at `2e4d99c`," states row 18's measurement "has since been run and recorded, so nothing in this table is now owed a value," and states the entailment direction explicitly and correctly: "a zero over the whole tree entails a zero in each of the two named files, whereas a per-file zero would not have entailed the repository-wide claim." Row 18's own recorded value (`0` at the branch point, `freeze run 2026-08-24`) is untouched from round 2. No contradiction remains.
3. **NEW-3 (was Major) — CLOSED, and the same-class sweep is confirmed genuinely repo-wide.** The `## Goal` sentence's arm-A clause is rebuilt to the two prescriptive conjuncts ("will gate a loop transition **and** its judgment is mechanically executable from committed bytes... a missing script is an implementation gap and never evidence for arm B"), matching §1's own already-corrected rule text. `## Body-to-AC correspondence` rows 2 and 3 are independently confirmed rewritten to the same prescriptive framing ("whether the obligation's verdict will gate a loop transition and whether its judgment is mechanically executable," "chosen by what the verdict gates and whether the judgment is mechanical, never... by which scripts happen to exist") — this closes the site pm-spec's own sweep found and the round-2 record did not report (row 3, alongside the reported row 2). A whole-file sweep for every remaining instance of the old observational framing (`plugin-shipped script executes`, `who executes the obligation`, `is it executed by a shipped script`) turns up exactly one surviving occurrence, at §1's own "Why question 1 is phrased as a consequence test" paragraph, and it is unambiguously framed as the rejected alternative reading being explained away, not as an operative description. Matches Codex's independent whole-file sweep exactly (zero unfixed instances).

#### The orchestrator's pre-commitment classification ruling

Recorded on the board (`.shell-team/todo.md`, T-1096's own entry): "the Goal-sentence finding is not a second consecutive independent finding against §1 and the pre-commitment's factual trigger does not fire... same class and root as round 1's F1 — circular framing — a propagation gap from an incompletely-applied fix... not a new defect in §1's operative rule." **I do not contest this classification.** Independent reasoning: round 1's F1 was a defect in §1's actual decision procedure (question 1 was circular for a not-yet-built obligation); round 2's redesign fixed that procedure, and I independently re-probed the fixed rule against a fresh hypothetical obligation both in round 2 and again implicitly this round (the rule's own text, unchanged since round 2, still decides correctly) — the operative logic is confirmed sound, twice now, by two independent means (my own re-probe, Codex's independent re-probe). Round 2's Goal-sentence finding was never a claim that the rule's *logic* had regressed; it was that one summary restatement of that logic, in a different location, had not been updated to match the fix already verified sound elsewhere in the same document. That is a documentation-propagation gap, not "evidence about the design premise" in the sense §4's stop rule cites `.shell-team/lessons.md:1325` for — the design premise (the two-conjunct prescriptive rule) has not been independently defeated a second time; the same already-fixed defect merely had one un-swept restatement. Codex's own independent pass reached the identical conclusion unprompted, additionally noting correctly that the Goal sentence's hashed status "increases impact, but not independence."

#### No-new-defect confirmation

Acceptance criteria remain exactly 21 with 21 corresponding `- check:` lines (re-counted directly). **AC1**'s prose and check line are byte-identical to the text read in rounds 1 and 2. The round-2-to-round-3 diff is confined to the four locations named above (`## Goal`, `## Body-to-AC correspondence` rows 2–3, the `## Assumptions` preamble, `## Notes for engineer`'s reader's-algorithm paragraph) plus this round's own dispositions — none of which is a numbered acceptance criterion, so no criterion's substantive claim moved this round. Codex's independent pass confirmed the same scoping and reported all 42 numbered criterion/check-line pairs byte-identical to the prior round.

#### Synthesis audit ledger
no severity changes

#### Recommendation
- APPROVE → the freeze may proceed (subject to every other first-freeze gate `skills/run/SKILL.md` runs independently of this mode — the Conformance-read confirmation gate, the Adopter-facing-documentation gate, the Stacked-branch and Verification-ceiling gates — none of which this mode substitutes for).
- All eight findings across three rounds (F1–F5 from round 1; NEW-1–NEW-3 from round 2) are confirmed closed. The pre-commitment's factual trigger has not fired at any point in this three-round series: round 1 found independent Blockers/Majors on first contact with the design (not a repeat), and round 2's three findings were propagation gaps from round-1 fixes rather than independent re-defeats, as reasoned above.
- This APPROVE gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- no fast-follow deferrals

Raw Codex output: `.shell-team/reviews/T-1096-codex-specreview.txt` (final message, this round's capture), `.shell-team/reviews/T-1096-codex-specreview.jsonl` (full event stream, this round).

---
