# T-1062 — Codex cross-provider review

## Round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1062
- Codex model: gpt-5-codex (provider-configured; no model flag passed per `agents/codex-reviewer.md` / `templates/adapters/codex-cli.txt`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 6 files, +253/-1 lines (`git diff --stat 383e9af5...HEAD`) — `docs/templates/release-notes-template.md` (the deliverable, +3/-1), `.shell-team/specs/T-1062-release-notes-compare-link.md` (new spec, 181 lines), `.shell-team/todo.md` (board hand-offs, +30), `.shell-team/provenance/T-1062.md` (new, 10 lines), `.shell-team/interventions/T-1062.md` (new, 6 lines), `.shell-team/test-recipe.md` (+23 append)

Diff base used throughout: `git merge-base feature/1061-adopter-docs-gate HEAD` = `383e9af56f50634c189e872251e340a8a5c538c2`, confirmed equal to `git rev-parse feature/1061-adopter-docs-gate` and unequal to `git rev-parse develop` (`d01ab30ea454b23292c12df6f80dceb151d5c42f`) — the stacked-branch premise the spec itself measures.

#### Blockers
- none

#### Major
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48` (AC1 check line) — The bullet-boundary extraction (`awk '/^<!-- BEGIN release-body -->$/{exit} /^- /{f=(index($0,"## Install / update")>0)} f'`) only resets `f` on a line that literally begins `^- `. A paragraph placed directly after the target bullet — separated by a blank line, not itself a new `- `/`*`/`+` list item — leaves `f` true and is silently absorbed into the extracted "bullet," so the literal-presence checks still pass even though the exception has moved out of the actual Markdown list item and back into the "footnote" placement AC1 exists to reject. This is not hypothetical: the engineer's own AC7(a) mutation-1 note records that exactly this shape ("a naive 'paragraph right after the bullet' attempt") **failed to redden AC1**, and they had to move the paragraph between two *other* bullets instead to get a red — i.e., the gap was found empirically during this task's own mutation testing and worked around rather than fixed. Confirmed by direct read of the check line, corroborated independently by both the primary and adversarial Codex passes.
  - Suggested fix (mechanics only, semantics of AC1 unchanged): terminate the extraction at the actual Markdown list-item boundary — reset `f` on a blank line followed by non-indented content, or on any CommonMark unordered-list marker (`^[[:space:]]{0,3}[-+*][[:space:]]`), not only on the literal `^- ` this criterion happens to use elsewhere.
- `.shell-team/specs/T-1062-release-notes-compare-link.md:54` (AC3 check line) — The "outside-the-bullet survival" clause is one-directional and whole-file-scoped: it extracts the branch-point preamble lines outside the target bullet, then asserts each still appears *somewhere* in the working file (`grep -qxF -- "$l" "$F"`, searched against the whole file, not the corresponding region). Two consequences, both real: (a) a brand-new paragraph or bullet inserted anywhere else in the preamble, while every original line survives untouched, still passes — so AC3 does not actually establish its own stated property ("one bullet moved and nothing else did"); (b) an untouched rule can be *relocated* — deleted from its original spot and pasted verbatim inside the target bullet or after the body — and still "survive" by the whole-file membership test, even though the criterion's own prose is about confinement, not mere presence. Confirmed by direct read of the check line, corroborated independently by both Codex passes.
  - Suggested fix: extract the non-target preamble from *both* the base and the working file with the same boundary rule, and compare those two bounded extractions for equality (not membership) — this catches additions and relocations, not just deletions.
- Both AC1 and AC3 sit inside this task's own frozen intent block (`68c4b19cf9771b1439825bbea1c9a844db8ca2a9`, no re-freeze in any round). Tightening their check-line mechanics without touching the criterion's stated meaning ("the exception lives in the same bullet," "only the one preamble bullet moves") is a class-M repair, not a class-B one — it does not touch the Goal sentence, Non-goals, a criterion's prose, or Input space, and it is this task's own criterion rather than another task's merged intent, so `## Non-goals`'s "no repair … of any merged criterion" clause does not apply to it. This is squarely a rework-round fix, not a fast-follow.

#### Minor / nits
- `.shell-team/todo.md:43` — the `### Local test result — T-1062` section (added by commit `4809d80`, before the real intervention entry landed) still reads `check-interventions.sh … conformant (0 entries, 1 sentinel, unchanged from the orchestrator's original)`. That is now false: the intervention file was amended with a real entry at commit `b5aa59e`, and both the QA verdict line directly above it (`.shell-team/todo.md:36`, "1 entry, 0 sentinel") and a live re-run of `check-interventions.sh` in this review (`1 entries, 0 sentinel`) confirm the current, correct count. The board's own audit trail now contradicts itself between two adjacent sections — an earlier line left stale by a later, correct one — a record-consistency defect distinct from a wording nit. Found independently in this review's own read-through and corroborated by the primary Codex pass.
- AC1 checks six literals for presence (`## Install / update`, `## Links`, `vPREV`, `vX.Y.Z`, case-insensitive `compare`, case-insensitive `previous`) but not their pairing — a bullet that reversed which token takes the previous vs. this release's tag would still satisfy every grep. Downgraded from the adversarial pass's raw P1: this is the same "honestly bounded to instruction-presence, never semantic self-consistency" trade-off this project's own corpus repeatedly makes and discloses on the record (T-1059 AC5's own design rationale, quoted three times in this spec). It is not a defect unique to T-1062, and the actual shipped bullet's semantic correctness was independently confirmed by manual read, by AC7(g)'s rendered-markdown check, and by QA's own "rule 4" bullet read — so nothing about *this* deliverable is actually ambiguous.
- AC2's preamble extraction (`.shell-team/specs/T-1062-release-notes-compare-link.md:51`) is bounded by `!f` around the two body markers, which also captures anything appended *after* `<!-- END release-body -->` (there is currently nothing there). A comment block dropped after the END marker containing all seven literals would count toward the "preamble" survival count. Requires a deliberately adversarial edit with no current trigger; the project's own Input-space section already declines to defend the template against "adversarial markdown … authored in this repository under review."
- AC4's set-equality clause (`.shell-team/specs/T-1062-release-notes-compare-link.md:57`) reads `git ls-files`, which lists tracked paths from the index regardless of whether the file still exists on disk (verified live: `rm`-ing a committed, un-`git rm`'d file leaves it in `git ls-files`). Requires a dirty working tree at verification time — a precondition every other clause in this spec already assumes is clean — and is generic to every `ls-files`-based population check in the corpus, not unique to T-1062.
- AC5's scope lock (`.shell-team/specs/T-1062-release-notes-compare-link.md:60`) unions the committed range with untracked files but not staged/unstaged changes to already-tracked paths, so a staged-but-uncommitted rename of the template to a disallowed path at verification time would not show in either measured set. Real hardening opportunity, but it requires an uncommitted, dirty tree at hand-off — the same generic gap recurs in many other specs' identically-shaped scope locks, so it reads as a corpus-wide checker-pattern issue rather than a T-1062-specific one.
- AC6's PII clause (`.shell-team/specs/T-1062-release-notes-compare-link.md:63`) runs `check-pii-shapes.sh --base <branch-point>`, which reads the committed diff — matching AC6's own stated scope ("covers the edited template before its bytes reach pushed history"). PII added to the working file after the last commit but before push would not be caught by this clause alone. Requires a dirty tree at verification time; consistent with how PII checks are run elsewhere in this repo's convention.
- Test-design observation: neither the engineer's 8 named AC7(a) mutations nor QA's 3-of-8 spot-audit happened to probe the specific shapes that actually break AC1/AC3 above (a trailing unmarked paragraph immediately after the bullet; an addition elsewhere in the preamble with every original line intact) — the mutation *methodology* itself has a coverage gap distinct from the two Major findings it happened not to catch.

#### Adversarial findings
- Primary pass (read-only `codex exec … review --base <branch-point> --json`): independently surfaced the AC1 placement-boundary gap, the AC3 one-directional-survival gap, and the same stale `### Local test result — T-1062` interventions-count line — three findings, zero of which needed a pasted excerpt to find (all read the real files directly). Full text: `.shell-team/reviews/T-1062-codex-primary.txt` / `.jsonl`.
- Adversarial pass ("what edit would violate each criterion's stated intent while still passing its literal check?" against AC1–AC6): corroborated both Major findings with additional exploit shapes (a sibling list item using `*`/`+` instead of `-` also evades AC1's reset condition; a relocated-not-just-added line evades AC3 the same way) and surfaced the five Minor/nit items above (token-mapping order, AC2 post-body content, AC4 ls-files-vs-filesystem, AC5 staged changes, AC6 working-tree PII). The adversarial pass labeled several of these P1 (blocker-mapped); this review's own judgment (documented in the ledger below) holds most of them at Minor because they require either a deliberately adversarial edit or an already-dirty working tree at verification time — conditions this project's own Input-space and hand-off conventions already decline to defend against elsewhere in the corpus. Full text: `.shell-team/reviews/T-1062-codex-adversarial.txt` / `.jsonl`.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---|---|---|---|
| 1 | AC1 bullet-boundary extraction gap (paragraph right after the bullet evades the awk reset) | primary: major (P2) / adversarial: blocker (P1, plus the `*`/`+` sibling variant) | major | Confirmed real and reachable by an honest editor with no adversarial intent (the engineer's own mutation testing hit this exact gap), but the current shipped deliverable is unaffected and the fix is a bounded, in-scope class-M mechanics repair to this task's own not-yet-merged criterion — not severe enough to be a blocker, but too concrete to defer. |
| 2 | AC3 one-directional / whole-file survival gap | primary: major (P2) / adversarial: blocker (P1, plus the relocation variant) | major | Same reasoning as #1: reachable without adversarial intent, undermines AC3's own stated "nothing else moved" claim, fixable in-scope before merge. |
| 3 | AC1 token-mapping order not checked | adversarial: blocker (P1) | minor | Downgraded: this is the corpus's own repeatedly-disclosed "presence, not semantic self-consistency" trade-off (T-1059 AC5 precedent), and the shipped bullet's actual semantics were independently confirmed correct by manual read, AC7(g)'s render check, and QA's rule-4 read. Not a T-1062-specific defect. |
| 4 | AC2 preamble extraction includes content after the END marker | adversarial: major (P2) | minor | Downgraded: requires a deliberately adversarial edit with no current trigger (nothing exists after the marker); mirrors the Input-space section's own declared stance of not defending against this repository's own contributors. |
| 5 | AC4 `git ls-files` vs. filesystem existence | adversarial: major (P2) | nit | Downgraded: requires a dirty working tree (tracked file deleted without `git rm`) at verification time — a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based population checks. |
| 6 | AC5 scope lock omits staged/unstaged changes | adversarial: major (P2) | minor | Kept at minor rather than major: real hardening value, but requires an uncommitted dirty tree at verification and the same gap recurs in many sibling specs' identically-shaped scope locks — a corpus-wide pattern, not unique to this task. |
| 7 | AC6 PII check is committed-diff-only | adversarial: blocker (P1) | minor | Downgraded: matches AC6's own documented scope (a pre-push, committed-diff check) and requires a dirty tree; consistent with how PII checks run elsewhere in the repo. |
| 8 | `.shell-team/todo.md:43` stale interventions count contradicting the later QA verdict line | primary: minor (P3) / this review's own independent read | minor | No change — both the primary pass and this review's own first-line-to-last read of the board entry independently landed on minor; corroboration, not a disagreement to resolve. |

#### Review-scope items addressed directly
- **(a) Closure by the filer's own standard** — confirmed. The rewritten bullet (`docs/templates/release-notes-template.md:31-33`) reads unambiguously in both directions: the compare URL is explicitly excepted with both tokens' meanings stated, and `## Install / update` plus the two changelog links are still unambiguously copy-unchanged (nothing in the new text touches them). Issue #240's own review-record origin (`.shell-team/reviews/T-1059.md` round-1 Minor, "a one-line clarification") is satisfied by the shipped text itself; the Major findings above are about the *acceptance criteria's* enforcement robustness, not about the shipped prose's own ambiguity.
- **(b) Merged-criteria side effects, measured not defended** — spot-checked independently in this review: `T-1059-docs-release-notes.md` AC5 = PASS at HEAD (live re-run, matching the spec's prediction and the "criterion most at risk" framing); `T-1061-adopter-docs-gate.md` AC13 = FAIL at HEAD (live re-run, matching the self-declared merge-point staleness); population 66→67 confirmed by live re-count on both sides. No row in the table defends a prediction against a measurement — the two corrected rows (T-1003 AC19, T-1019 AC10) were independently re-run in this review and both measure PASS/PASS at HEAD, matching the engineer's correction exactly. This closes QA's own `environmentally-unverified: AC7(c) T-1003 AC19 / T-1019 AC10 re-verification` risk note — independently confirmed in this round, not merely re-cited.
- **(c) Declaration consistency** — confirmed. `bin/team-init.sh`'s six `copy_template`/`copy_template_protected` calls all source from `$TEMPLATES_DIR` (`templates/`); a repo-wide search for `release-notes-template` finds it named only in `CONTRIBUTING.md`, the template itself, and this task's own records — nothing under `docs/` reaches an adopting repository through `team-init.sh`. The `- user-visible: no —` rationale is accurate.
- **(d) Blast-radius population is a live derivation** — confirmed. 66 at the branch point and 67 at HEAD both independently re-derived live in this review, matching the engineer's AC7(c) figures and the corrected board line.
- **QA's `environmentally-unverified` risk notes** — AC7(c) (T-1003 AC19 / T-1019 AC10) is now closed by this review's own independent re-run (see (b) above); no further re-verification needed before merge. AC7(a) mutation items 2/3/5/6/8 (delete `previous`, delete `omitted`, insert an HTML comment inside the region, reword an untouched bullet, add a file under `templates/`) remain unverified by QA, but this review judges them acceptably covered for merge purposes: each is a single literal-presence or byte-identity check whose mechanics are simple and were not implicated by either Codex pass's adversarial analysis (which instead found the *unrelated* AC1/AC3 gaps above — precisely the "test-design coverage gap" Minor note records). No independent re-verification of those five items is required as a merge condition.

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the two Major findings above (AC1 bullet-boundary extraction, AC3 one-directional survival). Both are class-M mechanics repairs to this task's own not-yet-merged intent block — tightening the check lines' boundary handling without altering either criterion's stated meaning — and should be re-frozen and re-verified (including a mutation test against the specific shapes named above) before the next review round.

#### Fast-follow disposition
- AC1's literal-presence check not verifying the `vPREV`/`vX.Y.Z` token-to-release mapping — won't-fix. Rationale: matches this corpus's own repeatedly-disclosed presence-only trade-off (T-1059 AC5 precedent); the shipped bullet's actual semantics are already independently confirmed correct by three separate reads (engineer, QA, this review).
- AC2's preamble extraction picking up content appended after the `<!-- END release-body -->` marker — won't-fix. Rationale: requires a deliberately adversarial edit this project's own Input-space section already declines to defend against; nothing currently exists after that marker.
- AC4's `git ls-files` population check not verifying on-disk existence — won't-fix. Rationale: requires an already-dirty working tree at verification time, a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based checks, not a T-1062-specific gap worth a standalone fix.
- AC5's scope lock omitting staged/unstaged changes to tracked paths — file-an-issue. Rationale: a real, corpus-wide hardening opportunity (many merged specs use the same commit-range-plus-untracked-files scope-lock shape), worth tracking once as a general checker-pattern issue rather than patched piecemeal per spec.
- AC6's PII check running only against the committed diff — won't-fix. Rationale: matches AC6's own documented scope as a pre-push, committed-diff check; consistent with how PII checks run elsewhere in the repo; requires a dirty tree to matter.
- `.shell-team/todo.md:43`'s stale `### Local test result — T-1062` interventions count contradicting the adjacent, later QA verdict line — won't-fix. Rationale: purely cosmetic staleness in an ephemeral per-task audit-trail section, not machine-parsed and not covered by `check-intent.sh`'s hash; the correct information already sits one section above it in the same board entry; retroactively editing an already-attested hand-off record for a zero-impact staleness is worse than leaving it.

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1062
- Codex model: gpt-5-codex (provider-configured; no model flag passed per `agents/codex-reviewer.md` / `templates/adapters/codex-cli.txt`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 7 files, +327/-1 lines (`git diff --stat 383e9af5...HEAD`) — one file more than round 1's count because this branch now also carries round 1's own review record (`.shell-team/reviews/T-1062.md`, +65, committed at `61d8190`). The deliverable itself, `docs/templates/release-notes-template.md`, carries **zero delta** since round 1's own HEAD (`b5aa59e`): `git diff b5aa59e..HEAD -- docs/templates/release-notes-template.md` is empty, confirmed live. This round's actual content is the class-M mechanics re-freeze v1→v2 (commit `9acb734`, touching only the AC1/AC3 `- check:` lines), QA round 2's verdict, and this record.

#### Mandatory class-M re-freeze verification (v1→v2)
Both required halves were checked independently rather than trusted from the board's own claim, per this role's mandatory item for a declared re-freeze.

- **Delta confinement.** Reconstructed the pre-edit spec from the git blob at `4809d80` (the last commit that touched the spec before `9acb734`) and ran `bash bin/check-refreeze-class.sh --old-hash 68c4b19cf9771b1439825bbea1c9a844db8ca2a9 <reconstructed> .shell-team/specs/T-1062-release-notes-compare-link.md` → `mechanics: T-1062 68c4b19c... -> 24c7bba8e86ad6e2adfaf0521639f141c3de5e77 differing=2`, hash-identical to the board's `refreeze-class` record and to QA round 2's own independent reproduction. Every differing line is a `- check:` line on both sides (AC1's and AC3's) — confirmed by direct diff of the two blobs, not merely by the classifier's own report.
- **Meaning preservation.** Read AC1's and AC3's prose directly against both the old and new check lines. The new AC1 line still bounds extraction to the target bullet rather than searching the whole preamble, and the new AC3 line still compares the non-bullet preamble as a bounded, ordered extraction rather than an unordered whole-file membership test — both changes are strictly in the direction their own criterion's prose already demanded, and neither weakens on any input this file's own real content can produce. This half passes.
- **Named exploit-variant closure.** Independently reproduced both exploit shapes this review's own round-1 adversarial pass named, in a detached scratch worktree at `9acb734` (never the main checkout), removed after use:
  - AC1, `*`/`+` sibling item: moved the exception clause into a separate list item marked `*` (not `-`) directly after the boilerplate-claim bullet. The reconstructed **old** AC1 check line still exits `0` (the exact false pass the round-1 review named); the **new** AC1 check line exits `1`. Restored and re-observed green.
  - AC3, relocation: reordered two untouched, adjacent preamble bullets (`## Highlights` and `## Migration notes`) with every original line's bytes unchanged, leaving the target bullet untouched. The reconstructed **old** AC3 check line still exits `0` (unordered whole-file membership can't see a reordering); the **new** AC3 check line exits `1` (its `cmp -s` sequence comparison catches it). Restored and re-observed green.

  Both results match the board's own class-M evidence field and QA round 2's independent reproduction exactly. **The re-freeze v1→v2 is correctly classified as mechanics and its own stated meaning is preserved — it is not reverted by this review.**

- **However, this round's own fresh adversarial reading of the *repaired* lines surfaces two further gaps in the same mechanism, neither of which is one of the shapes named in round 1** (so their existence does not retroactively invalidate the re-freeze's classification), **but both of which are real, reachable-by-an-honest-editor placement violations that the just-repaired check lines still do not catch.** See `#### Major` below. Both were independently reproduced by direct execution against the actual check-line text plus `pandoc -f gfm -t html` rendering, not merely reasoned about.

#### Blockers
- none

#### Major
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48,54` (AC1 and AC3 check lines, shared boundary logic) — **A whitespace-only "blank" line does not reset the extractor, but GFM treats it as blank anyway.** The new guard resets `f` only on a line matching `!/^[[:space:]]/`; a line that is empty resets `f` (an empty string does not match `^[[:space:]]`), but a line consisting of a single trailing space does **not** reset `f` (it does match `^[[:space:]]`) even though CommonMark/GFM defines a blank line as "no characters, or only spaces/tabs" — both are blank for rendering purposes. Combined with the second gap — the guard only reacts to *zero* indentation, not to indentation narrower than the `- ` marker's two-column continuation width — a paragraph placed after a one-space "blank" line and itself indented by one space is absorbed into the bullet extraction by both AC1 and AC3, while GFM renders it as a separate paragraph outside the list item. Verified directly: mutated `docs/templates/release-notes-template.md` in a detached scratch worktree at `9acb734` so the boilerplate bullet reads `... copy them unchanged.` alone, followed by a line containing exactly one space, followed by three lines each indented by exactly one space carrying the exception text; `bash bin/check-acs.sh` reports **AC1 PASS, AC3 PASS** (all 6 check-bearing criteria PASS) against this mutation; `pandoc -f gfm -t html` on the same file renders `<li>…copy them unchanged.</li></ul>` followed by a separate, sibling `<p>except the compare URL…</p>` — outside the list entirely. Independently surfaced by this review's own adversarial probing before Codex's own primary pass gestured at the same shape (Codex's own stated mechanism — "keeps `f` set because it only terminates on a completely non-indented line" — is imprecise: a *truly* empty line does terminate it; the gap requires the "blank" line to itself carry trailing whitespace, which is exactly the shape an ordinary editor produces without meaning to and which no lint in this repository's convention currently flags). Confirmed independently a second time by the adversarial Codex pass, byte-for-byte matching mutated region and `pandoc` verification.
  - Suggested fix: terminate the bullet extraction on any line matching CommonMark's own definition of blank (`^[[:space:]]*$`), not only a literal empty line; and require continuation content to be indented by at least the marker's two-column content offset (i.e., resetting on `^[[:space:]]{0,1}[^[:space:]]` as well as on the existing zero-indent and marker-line conditions), rather than only on zero indentation.
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48,54` (AC1 and AC3 check lines, shared boundary logic) — **The extraction re-arms on any later marker line that happens to repeat the target phrase, rather than terminating unconditionally after the first target bullet.** `f=(index($0,"## Install / update")>0)` is recomputed on *every* list-marker line, including one that starts an entirely new, separate list item; if that later item's own text also contains the literal `## Install / update` — an entirely plausible way to word an "exception to the boilerplate" sentence — `f` is set true again and the new item's content (and its continuation lines) are absorbed into both AC1's `bul` extraction and AC3's `nb`-exclusion, even though it is a structurally distinct list item. Verified directly: mutated the file so the target bullet reads only `... copy them unchanged.` and is immediately followed (after a real blank line) by a separate `* Exception to **`## Install / update`** and **`## Links`** boilerplate: the compare URL takes `vPREV`...` item; `bash bin/check-acs.sh` reports **AC1 PASS, AC3 PASS**; `pandoc -f gfm -t html` renders two separate `<ul>` elements (a `-` marker followed by a `*` marker starts a new list in GFM), each with its own `<li>` — the exception item is unambiguously not inside the boilerplate-claim item. Found by the adversarial Codex pass and independently reproduced byte-for-byte by this review, including the `pandoc` confirmation.
  - Suggested fix: make the extraction single-shot — start capturing only at the *first* line whose marker mentions `## Install / update`, and terminate unconditionally and permanently at the next list-marker line regardless of that line's own content (never recompute `f` a second time from a later marker).
- Both findings sit inside this task's own frozen intent block, inside the check lines the class-M re-freeze v1→v2 just repaired. Tightening them further without touching AC1's or AC3's stated meaning is again a class-M repair (mechanics only), not a class-B one, and is squarely a rework-round fix rather than a fast-follow — the same reasoning round 1 gave for the two findings this round confirms are now closed.

#### Minor / nits
- No new Minor/nit findings this round. Round 1's five (AC1 token-mapping order, AC2 post-END-marker content, AC4 `git ls-files`-vs-filesystem, AC5 staged/unstaged scope-lock gap, AC6 committed-diff-only PII scope) stand unchanged — nothing in this round's actual diff (the v1→v2 re-freeze, QA round 2's verdict, this record) touches any of the criteria or reasoning those five findings rest on.

#### Adversarial findings
- Primary pass (`codex exec … review --base 383e9af5 --json`, read-only, read the real spec file itself): surfaced one finding, P2, on the AC1 check line at line 48 — the shape now recorded above as the first Major, though this review's own follow-up execution found the primary pass's own stated mechanism ("terminates only on a completely non-indented line") to be imprecise; a truly empty line *does* terminate the extraction, and the reachable exploit requires the "blank" separator to itself carry trailing whitespace. Full text: `.shell-team/reviews/T-1062-codex-primary.txt` / `.jsonl`.
- Adversarial pass (devil's-advocate framing against the just-repaired AC1/AC3 boundary logic specifically, explicitly asked to distinguish the awk script's notion of "blank line" from GFM/CommonMark's, and to verify any finding against real `pandoc` rendering rather than reasoning alone): returned two findings, both `major`, both independently reproduced byte-for-byte by this review with matching `pandoc` output — the whitespace-only-blank-line gap (corroborating and sharpening the primary pass's P2) and the re-arming-sibling-marker gap (a new shape the primary pass did not surface). Full text: `.shell-team/reviews/T-1062-codex-adversarial.txt` / `.jsonl`.
- Both Codex passes' severities (`P2`, and `major` directly) map onto or already equal this project's `major` without adjustment.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---|---|---|---|
| 1 | Round-1 Major #1 (AC1 bullet-boundary: unmarked paragraph right after the bullet) | this review: independently reproduced as closed | closed, no longer open | Old AC1 check line exits `0` on the reconstructed pre-repair blob against this exact mutation; new AC1 check line exits `1`. Matches the board's own class-M evidence and QA round 2's independent reproduction. |
| 2 | Round-1 Major #2 (AC3 one-directional survival: relocation) | this review: independently reproduced as closed | closed, no longer open | Old AC3 check line exits `0` on a reordering of two untouched preamble bullets; new AC3 check line exits `1`. Matches the board's own class-M evidence and QA round 2's independent reproduction. |
| 3 | Whitespace-only blank line + under-indented continuation evades AC1/AC3 | primary: major (P2, imprecise mechanism as stated) / adversarial: major (precise mechanism, pandoc-verified) / this review: independently reproduced | major | Not one of round 1's named exploit variants, so it does not reopen or invalidate the v1→v2 re-freeze's own classification, but it is a real, reachable-without-adversarial-intent placement violation the repair does not close. Kept at major, matching round 1's own precedent for this exact class of finding (current shipped deliverable unaffected; check-line mechanics need another bounded, in-scope tightening). |
| 4 | Re-arming sibling marker that repeats "## Install / update" evades AC1/AC3 | adversarial: major (pandoc-verified) / this review: independently reproduced | major | Same reasoning as #3: reachable by an ordinary, plausible wording choice, not adversarial markdown under this spec's own Input-space declarations, real placement violation, class-M in-scope fix. |
| 5 | Primary pass's stated mechanism for its own P2 finding ("terminates only on a completely non-indented line") | primary's own framing | corrected, not downgraded | This review's direct execution shows a truly empty line *does* terminate the extraction; the reachable exploit requires the blank separator to carry trailing whitespace. The underlying finding (#3 above) is not weakened by this correction — only the primary pass's own description of the mechanism is refined. |

#### Review-scope items addressed directly
- **Same-class-2 inventory transcription (board, `.shell-team/todo.md`, rework round 1 entry)** — checked against this review's own round-1 ledger, faithfully, not softened: the board's apply/not-apply table names AC1 and AC3 apply (repaired), and AC2/AC4/AC5/AC6 not-apply with reasons that match this review's ledger rows #4 (AC2, won't-fix), #5 (AC4, won't-fix), #6 (AC5, file-an-issue, actioned at the disposition step), and #7 (AC6, won't-fix) respectively, substance for substance — no reason was reworded to sound less concerning than the original ledger row.
- **Records read end to end, not diff-shaped** — `.shell-team/provenance/T-1062.md` (2 decisions), `.shell-team/interventions/T-1062.md` (1 entry), the full T-1062 spec (all sections, not only the AC1/AC3 delta), and the full board entry from its first freeze sweep line through QA round 2's verdict, were each read start to finish for this round rather than only the lines the re-freeze diff touched. No contradiction between an earlier and a later line was found beyond the one QA round 2 already disclosed (the fast-follow-disposition line's unbalanced nested backticks — cosmetic, not machine-parsed, not new).
- **Deliverable zero-delta** — confirmed live: `git diff b5aa59e..HEAD -- docs/templates/release-notes-template.md` is empty. Round 1's `#### Review-scope items addressed directly` (a)-(d) therefore all still hold unchanged and are not re-litigated here.
- **check-intent.sh / check-acs.sh, live** — `check-intent: aligned: T-1062 v2 (24c7bba8e86ad6e2adfaf0521639f141c3de5e77)`; `check-acs: 6 passed, 0 failed, 1 skipped, 0 unrecognized` against the real, unmutated deliverable — the two new Major findings above are about the check lines' ability to catch *other* inputs, not about the current shipped bullet, which remains correct.

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the two Major findings above (both in AC1's and AC3's shared bullet-boundary check-line mechanics: the blank-line/indentation gap, and the re-arming-marker gap). **This does not revert the class-M re-freeze v1→v2** — that re-freeze's own classification (mechanics, meaning-preserved) and its closure of the two round-1-named exploit variants are both independently confirmed above and stand. What is needed is a further, in-scope class-M mechanics repair (v2→v3) to the same two check lines, verified against the two new mutation shapes named above before the next review round.

#### Fast-follow disposition
- Restated unchanged from round 1 (none of this round's findings touch these five):
  - AC1's literal-presence check not verifying the `vPREV`/`vX.Y.Z` token-to-release mapping — won't-fix. Rationale: matches this corpus's own repeatedly-disclosed presence-only trade-off (T-1059 AC5 precedent); the shipped bullet's actual semantics are already independently confirmed correct by three separate reads (engineer, QA, this review).
  - AC2's preamble extraction picking up content appended after the `<!-- END release-body -->` marker — won't-fix. Rationale: requires a deliberately adversarial edit this project's own Input-space section already declines to defend against; nothing currently exists after that marker.
  - AC4's `git ls-files` population check not verifying on-disk existence — won't-fix. Rationale: requires an already-dirty working tree at verification time, a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based checks, not a T-1062-specific gap worth a standalone fix.
  - AC5's scope lock omitting staged/unstaged changes to tracked paths — file-an-issue. Rationale: a real, corpus-wide hardening opportunity (many merged specs use the same commit-range-plus-untracked-files scope-lock shape), worth tracking once as a general checker-pattern issue rather than patched piecemeal per spec.
  - AC6's PII check running only against the committed diff — won't-fix. Rationale: matches AC6's own documented scope as a pre-push, committed-diff check; consistent with how PII checks run elsewhere in the repo; requires a dirty tree to matter.
- No new deferrals this round: both new findings are Major, routed into the recommended rework round above, not deferred as fast-follows.

## Round 3

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1062
- Codex model: gpt-5-codex (provider-configured; no model flag passed per `agents/codex-reviewer.md` / `templates/adapters/codex-cli.txt`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 7 files, +405/-1 lines (`git diff --stat 383e9af5...HEAD`) — `docs/templates/release-notes-template.md` (+4/-1, **zero delta since round 1's own HEAD** `b5aa59e`, confirmed live: `git diff --stat b5aa59e..HEAD -- docs/templates/release-notes-template.md` produces no output), `.shell-team/specs/T-1062-release-notes-compare-link.md` (+181, cumulative; the only delta since round 2's v2 hash is the two AC1/AC3 `- check:` lines, confirmed live: `git diff --stat 9acb734..f17827a -- .shell-team/specs/T-1062-release-notes-compare-link.md` → `1 file changed, 2 insertions(+), 2 deletions(-)`), `.shell-team/todo.md` (+46, board hand-offs), `.shell-team/provenance/T-1062.md` (+10, unchanged since round 1), `.shell-team/interventions/T-1062.md` (+10, the new `assumption-contradicted` entry at `9c639c3` added since round 2), `.shell-team/reviews/T-1062.md` (+132, rounds 1–2's own records), `.shell-team/test-recipe.md` (+23, unchanged since round 1). This round's actual content is the second class-M mechanics re-freeze v2→v3 (commit `f17827a`), the `assumption-contradicted` interventions entry (`9c639c3`), QA round 3's verdict (`9f15f22`), and this record.

#### Mandatory class-M re-freeze verification (v2→v3)
Both required halves were checked independently, and the further "does the repair close round 2's findings" item was checked with fresh mutations, per this role's mandatory item for a declared re-freeze.

- **Delta confinement.** Reconstructed the pre-edit spec from the git blob at `9acb734` (`git show 9acb734:.shell-team/specs/T-1062-release-notes-compare-link.md`) and ran `bash bin/check-refreeze-class.sh --old-hash 24c7bba8e86ad6e2adfaf0521639f141c3de5e77 <reconstructed> .shell-team/specs/T-1062-release-notes-compare-link.md` → `mechanics: T-1062 24c7bba8... -> 3771a89e... differing=2`, hash-identical to the board's own `refreeze-class` record and to QA round 3's independent reproduction. Every differing line is a `- check:` line on both sides (AC1's and AC3's), confirmed by direct diff of the two blobs.
- **Meaning preservation.** Read AC1's and AC3's prose directly against both the v2 and v3 check lines. AC1's prose describes the bullet as running "until the next top-level `- ` line ... whichever comes first"; the v3 boundary (first line that is not an indented continuation) terminates at least as early as that description on every input this repository's own content produces — a tightening, not a meaning change. AC3's prose ("one bullet moved and nothing else did") is now read against a boundary pair `(s,e)` shared identically by both AC1's bullet extraction and AC3's complement — a line is in exactly one of the two extractions by construction, which is a stricter and more faithful mechanical form of the same claim than v2's independently-recomputed pair. This half passes.
- **Named exploit-variant closure, independently reproduced in a detached scratch worktree at `f17827a`** (`git worktree add --detach`, removed after use with `git worktree remove --force`; main checkout's `git status --short` clean before, during and after):
  - **Round 2's whitespace-only-blank-line exploit**: mutated the bullet to `... copy them unchanged.` followed by a line containing exactly one space, followed by a one-space-indented paragraph carrying the exception. The reconstructed OLD (v2) AC1 check line exits `0` (the exact false pass round 2 named — `^[[:space:]]/` matches a whitespace-only line, so the v2 state machine never resets and absorbs the indented paragraph); the live NEW (v3) AC1 check line exits `1`. `pandoc -f gfm -t html` on the mutation confirms the exception renders as a sibling `<p>` **outside** the `<ul>` entirely (`</ul>` closes before the `<p>` opens) — the round-2 finding's own claimed defect, independently reconfirmed.
  - **Round 2's re-ignition exploit**: mutated the bullet to `... copy them unchanged.` with no exception, followed by a separate `*`-marked item elsewhere carrying the literal `## Install / update` plus the exception text. OLD (v2) AC1 exits `0` (the state machine recomputes its flag on every marker line and re-ignites); live NEW (v3) AC1 exits `1` (the `head -n 1` latch pins the start at the first matching line only). `pandoc` confirms two separate `<ul>` elements, each with its own `<li>` — structurally distinct lists, matching round 2's own finding.
  - Both results match the board's own `refreeze-class (v2→v3)` evidence field exactly. **Round-1 regression spot-check**: re-applied round 1's own shape (an unmarked, non-indented paragraph after a real blank line) against the live v3 AC1 line → exit `1`, no regression of the earlier closure. All mutations restored to the byte-identical original (`cmp` confirmed) and `bash bin/check-acs.sh` re-observed `6 passed, 0 failed, 1 skipped` before the worktree was removed.

  **Both of round 2's named exploits are correctly closed, and the v2→v3 re-freeze is correctly classified as mechanics with its own stated meaning preserved — it is not reverted by this review.**

- **However, this round's own fresh adversarial reading of the *re-repaired* lines — corroborated independently by both the primary and adversarial Codex passes and independently reproduced with `pandoc` confirmation — surfaces two further gaps, neither of which is one of the six shapes named across rounds 1–2** (so their existence does not retroactively invalidate the v2→v3 re-freeze's own classification), **but both are real, reachable-by-an-ordinary-editor placement/survival violations that the third repair still does not close.** See `#### Major` below.
- **On the honesty and sufficiency of the v3 sweep's own bounded closure claim**: the sweep's disclaimer — closure of "the six named shapes ... and the two mechanism families (unterminated boundary, recomputed state) — not ... every conceivable CommonMark divergence" — is honest in intent and correctly declines to claim universal closure; this review's second new finding below (an order-blind complement comparison) is a **third** mechanism family the sweep never named, so the disclaimer legitimately covers it. But the sweep's own **family-level** phrasing for the first family is more totalizing than what was actually tested: "unterminated boundary" is narrowed (the zero-indent and whitespace-only-blank-line members are now closed) but not eliminated, since a marker line indented by 1–3 spaces is a further member of that same family and is not closed (see Major #1 below). A future freeze-sweep record naming a "family" closed should scope that word to the specific members mutation-tested, not to the family as a mathematical class — a record-precision note, not a re-classification of this re-freeze.

#### Blockers
- none

#### Major
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48,54` (AC1 and AC3 check lines, shared boundary logic) — **A list-marker line indented by 1–3 spaces is treated as a continuation of the target bullet, but CommonMark/GFM treats it as a new, sibling top-level block.** The v3 end-of-bullet test is "first line not matching `^[[:space:]]+[^[:space:]]`" — any line carrying at least one leading space and a following non-space character is absorbed as continuation, regardless of whether that non-space character is itself a list marker (`-`, `*`, `+`) starting a structurally new item. CommonMark permits a block-starting marker indented by 0–3 spaces to still count as a new top-level block rather than a continuation (only 4+ spaces of indentation, relative to the containing context, forces a code block or genuine continuation). Verified directly: mutated the boilerplate bullet in a detached scratch worktree at `f17827a` to read `... copy them unchanged.` alone, immediately followed (no blank line) by a **one-space-indented** sibling item ` - Except the compare URL in `` `## Links` ``: `` `vPREV` `` takes the previous release tag and `` `vX.Y.Z` `` this release tag.` — the full `bash bin/check-acs.sh` run against this mutation reports **6 passed, 0 failed, 1 skipped, 0 unrecognized** (every check-bearing criterion, not merely AC1/AC3, is fooled); `pandoc -f gfm -t html` on the same file renders the exception as a **separate `<li>`**, sibling to the boilerplate claim's own `<li>`, inside the same `<ul>` — structurally a different list item, not the same bullet AC1's own prose requires ("Requiring these in the *bullet* rather than anywhere in the preamble is the whole point"). AC3 is fooled identically and for the same underlying reason: its complement extraction shares the identical `(s,e)` boundary pair, so the sibling item is excluded from AC3's "outside" comparison exactly as it is included in AC1's "inside" one. Independently surfaced by both the primary Codex pass (`codex exec … review --base 383e9af5 --json`, P2) and the adversarial pass (P1, corroborating and sharpening the same mechanism), and independently reproduced by this review with the full-suite run and `pandoc` confirmation above.
  - Suggested fix: terminate the bullet extraction on **any** line whose first non-space character is a CommonMark block-starting marker (`-`, `*`, `+`, or a digit followed by `.`/`)`) at 0–3 leading spaces, in addition to the existing zero-indent and blank-line terminators — not only on exactly zero indentation. A genuine prose continuation of the target bullet in this repository's own convention is always indented by exactly two spaces and never itself begins with a list-marker character; the fix can special-case that shape rather than attempting a full CommonMark parse.
- `.shell-team/specs/T-1062-release-notes-compare-link.md:54` (AC3 check line only) — **The outside-the-bullet complement comparison is blind to a bullet relocating *across* the target's own boundary, because both the base-side and the working-side `(s,e)` windows are computed independently and each excludes whatever currently sits in the target's position, canceling the relocation's own signature out of the diff.** Verified directly: in a detached scratch worktree at `f17827a`, moved the untouched `## Migration notes` bullet from its original position **immediately before** the target `## Install / update` bullet to **immediately after** it, with every line's bytes otherwise unchanged and the target bullet itself untouched. `bash bin/check-acs.sh .shell-team/specs/T-1062-release-notes-compare-link.md` on this mutation reports **AC1: PASS, AC3: PASS**. The reason is structural rather than a boundary-regex gap like the finding above: the base blob's own `(s,e)` pair correctly excludes only the base's real target bullet from its complement (`no-version-heading, Highlights, Migration-notes`), and the working file's own `(s,e)` pair correctly excludes only the working file's (unmoved) target bullet from its complement — but because `Migration notes` now sits in a different position relative to the target, and the complement is built by simple line-range exclusion rather than by comparing each side's content in a position-aware way, the two three-item complements happen to be byte-identical strings even though a real preamble bullet changed its position relative to the edited one. This directly contradicts AC3's own stated property, "the mechanical form of 'one bullet moved and nothing else did'" — here, a *second* bullet also moved, and AC3 does not notice. Surfaced by the adversarial Codex pass (P1: "moving the entire unchanged `## Migration notes` instruction bullet from immediately before Install/Links to immediately after it produces identical `.kp` files") and independently reproduced by this review with the exact mutation and check-line run above. This is a **third** mechanism family, distinct from both "unterminated boundary" and "recomputed state" named in the v2→v3 evidence field — the closure claim's own disclaimer ("not every conceivable CommonMark divergence") legitimately covers it, since neither round 1 nor round 2 nor the v3 sweep ever tested a relocation that crosses the target bullet's own boundary (round 2's own relocation test moved two bullets that were *both already on the same side* of the target).
  - Suggested fix: build each side's complement as an explicit, order-tagged sequence (e.g., "prefix before the bullet" and "suffix after the bullet" compared separately, or each remaining line tagged with its own line number before concatenation) rather than a single concatenated stream, so a bullet crossing from one side of the target to the other produces a detectable difference instead of two independently-`grep -v`-filtered strings that happen to collide.
- Both findings sit inside this task's own frozen intent block, inside the check lines the class-M re-freeze v2→v3 just repaired for the second time. Tightening them further without touching AC1's or AC3's stated meaning is, for a third consecutive round, a class-M repair (mechanics only) rather than a class-B one — squarely a rework-round fix. Given that three consecutive rounds have each found real, non-adversarial gaps in the same two check lines' line-anchored regex approximation of CommonMark list-item structure, this review flags — as an observation for the coordinating session's own judgment, not a mandate — that a fourth increment of the same line-oriented approach may keep finding new members of the same family; a structurally different approach (e.g., parsing the file's actual GFM/CommonMark structure, as the adversarial pass itself suggested, rather than approximating it with `awk` regexes over raw lines) may be worth weighing against a fourth patch at the next rework round.

#### Minor / nits
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48` (AC1 check line) — **The first-match latch identifies the target bullet purely by the literal `## Install / update`, without also requiring the boilerplate-claim language (`static boilerplate`, `copy them unchanged`) inside the same match.** Verified directly: in a detached scratch worktree at `f17827a`, inserted a decoy bullet *before* the real target — `- Note: the `` `## Install / update` `` section links to `` `## Links` ``, which carries `` `vPREV` `` and `` `vX.Y.Z` ``, the compare target and the previous release's tag.` — and simultaneously reverted the real target bullet to its pre-fix, fully defective wording (`... copy them unchanged.`, no exception at all). `AC1` alone exits `0` on this mutation, latching onto the decoy and never inspecting the real (now-defective) bullet. However, `AC3` independently exits `1` on the identical mutation, because AC3's own `(s,e)` computation (run separately against the base blob, which has no decoy) excludes the *real* bullet from the base's complement while the working file's `(s,e)` — fooled the same way as AC1 — excludes the *decoy* instead, leaving the real (now-defective) bullet present in the working complement with no base-side counterpart; the byte comparison catches the discrepancy. This review could not construct a variant where AC1's decoy additionally fools AC3 without a byte-level collision as contrived as the finding directly above's own third-family gap, and doing so requires deliberately authoring a decoy sentence packed with all six of AC1's required literals — a construction this spec's own Input space already declines to defend against ("this file is authored in this repository under review"). Downgraded from the adversarial pass's own P1 because the compound gate (`check-acs.sh`'s full run, which is what actually gates merge) is not fooled by any construction this review found, even though AC1 read in isolation does not itself verify what its own prose claims.
  - Suggested fix (if ever picked up): require the extracted bullet to also contain a case-insensitive match on `boilerplate` or `copy` alongside the six existing literals, anchoring the latch to the claim's own language rather than to `## Install / update` alone.
- Round 1's five (AC1 token-mapping order, AC2 post-END-marker content, AC4 `git ls-files`-vs-filesystem, AC5 staged/unstaged scope-lock gap, AC6 committed-diff-only PII scope) stand unchanged — nothing in this round's diff touches any of the criteria or reasoning those five findings rest on.
- This review's own additional probing found one further mutation shape that is **not a defect**, disclosed for transparency rather than as a finding: over-indenting a genuine continuation of the target bullet by four extra spaces (six total, versus the template's own two-space convention) causes GFM to render the continuation as a `<pre><code>` block — verified with `pandoc` — but that code block remains **inside the same `<li>`** as the boilerplate claim, before `</ul>` closes. AC1's own prose requires only that the exception "lives in the same preamble bullet," which this satisfies; the code-block rendering is a readability quality issue, not the placement violation AC1 exists to reject, and the shape itself requires a deliberately unusual editing choice (doubling the indentation beyond the file's own established two-space convention) with no plausible honest-mistake path to it. Not counted in the ledger below because it was judged, on inspection, not to be a violation of AC1's stated meaning at all.

#### Adversarial findings
- Primary pass (`codex exec … review --base 383e9af5 --json`, read-only, read the real spec file itself): one finding, P2, on the AC1 check line at line 48 — the 1–3-space sibling-marker boundary gap, now recorded above as Major #1. Full text: `.shell-team/reviews/T-1062-codex-primary.txt` / `.jsonl`.
- Adversarial pass (devil's-advocate framing against the just-re-repaired AC1/AC3 boundary logic specifically, explicitly asked to focus on CommonMark's 0–3-space list-marker indentation rule and to verify findings with `pandoc` rendering): returned four findings, all P1. Two ((a) the 1–3-space sibling-marker gap on AC1, and (c) the same gap's AC3-side manifestation) corroborate and sharpen the primary pass's finding and are folded into Major #1 above as one finding spanning both check lines, matching this task's own established convention for shared-boundary-logic findings. One (d) is the order-blind relocation-across-the-target-boundary gap on AC3, recorded above as Major #2, independently reproduced. One (b) is the latch-not-anchored-to-claim-language gap on AC1, recorded above as the new Minor, independently reproduced and found to be backstopped by AC3 in every construction this review attempted. Full text: `.shell-team/reviews/T-1062-codex-adversarial.txt` / `.jsonl`.
- All Codex-native `P1`/`P2` labels above are mapped per this role's normalization table: `P2` → major (Major #1's primary-pass label), `P1` → blocker-mapped by the table but placed at major or minor per this review's own synthesis, documented row by row in the ledger below with a reason for every placement that is not blocker.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---|---|---|---|
| 1 | Round-2 Major #1 (whitespace-only blank line + under-indented continuation) | this review: independently reproduced as closed | closed, no longer open | Reconstructed old (v2) AC1 check line exits `0` on the exact mutation; live new (v3) AC1 line exits `1`; `pandoc` confirms the round-2-named defect (sibling `<p>` outside the `<ul>`) no longer reproduces. Matches the board's own class-M evidence and QA round 3's independent reproduction. |
| 2 | Round-2 Major #2 (re-arming sibling marker repeating the literal) | this review: independently reproduced as closed | closed, no longer open | Reconstructed old (v2) AC1 check line exits `0` on the exact mutation; live new (v3) AC1 line exits `1` (the `head -n 1` latch pins the start permanently). Matches the board's own class-M evidence and QA round 3's independent reproduction. |
| 3 | 1–3-space-indented sibling list-marker line treated as a bullet continuation, evading AC1 and AC3 identically | primary: major (P2) / adversarial: blocker (P1, both the AC1 and AC3 manifestations) | major | This role's normalization table maps adversarial `P1` toward blocker by default, but this finding is placed at major to match this task's own three-round precedent for reachable-without-adversarial-intent placement violations that are class-M-fixable in-scope before merge and do not affect the currently shipped deliverable — the same severity this review gave to structurally identical findings in rounds 1 and 2. Confirmed the full `check-acs.sh` suite (not only AC1/AC3) is fooled, ruling out any other criterion acting as a backstop. |
| 4 | AC3's order-blind complement comparison misses a bullet relocating across the target bullet's own boundary | adversarial: blocker (P1) | major | Same reasoning as #3: reachable by an ordinary, plausible editorial reordering choice (not adversarial markdown under this spec's own declared Input-space boundary), a real violation of AC3's own "nothing else moved" claim, confirmed the full suite is fooled (AC1 correctly indifferent, since it never inspects the complement), class-M in-scope fix available. Placed at major rather than blocker for the same corpus-precedent reason as #3. |
| 5 | AC1's first-match latch does not require the boilerplate-claim language inside the matched bullet, only the literal `## Install / update` | adversarial: blocker (P1) | minor | Downgraded: this review constructed the compound mutation the finding implies (a decoy bullet plus the real bullet reverted to fully defective wording) and found `AC3` independently catches it every time in this review's own testing, because AC3's base-side and working-side `(s,e)` computations diverge asymmetrically when a decoy is introduced only on the working side. Bypassing the *whole* gate (not just AC1 in isolation) would require deliberately authoring a decoy sentence packed with all six of AC1's literals — a construction this spec's own Input-space section already declines to defend against. Matches this task's own round-1 precedent for downgrading findings that require deliberately contrived content. |

#### Review-scope items addressed directly
- **Deliverable zero-delta** — confirmed live a second time: `git diff --stat b5aa59e..HEAD -- docs/templates/release-notes-template.md` produces no output. Round 1's `#### Review-scope items addressed directly` (a)–(d) and round 2's confirmations therefore all still hold unchanged and are not re-litigated here.
- **check-intent.sh / check-acs.sh, live, against the real unmutated deliverable** — `check-intent: aligned: T-1062 v3 (3771a89eba8308acf5dd9547af193b150d2acc2e)`; `check-acs: 6 passed, 0 failed, 1 skipped, 0 unrecognized`. The findings above are entirely about the check lines' ability to catch *other* inputs; the currently shipped bullet remains correct.
- **Merged criterion at risk, re-measured live** — `.shell-team/specs/T-1059-docs-release-notes.md` **AC5**: PASS (exit 0), matching QA round 3's own claim and every prior round's.
- **Blast-radius spot-check, re-measured live** — `.shell-team/specs/T-1061-adopter-docs-gate.md` **AC13**: FAIL (exit 1), matching the self-declared merge-point staleness and every prior round's claim; not a regression to repair.
- **Board and records hygiene, re-run live** — `check-handoff.sh` (exit 0), `check-board-headings.sh --base 383e9af5` (exit 0), `check-pii-shapes.sh --base 383e9af5` (clean), `check-provenance.sh .shell-team/provenance/T-1062.md` (conformant, 2 decision entries, 0 sentinel), `check-interventions.sh --task T-1062 -- .shell-team/interventions/T-1062.md` (conformant, 2 entries, 0 sentinel — the new `assumption-contradicted` entry counted).
- **The `assumption-contradicted` interventions entry (commit `9c639c3`)** — read directly against the board's own account and against round 2's actual verdict text. The entry's summary and effect faithfully record what round 2 actually found (two further exploitable shapes in the same boundary logic; the v1→v2 re-freeze itself upheld, not reverted; the repair mechanism changed rather than patched a third time) and correctly states the cumulative class count as of round 2 (`x4`, `r1 x2, r2 x2`). No discrepancy between this entry and this review's own reading of round 2's record.
- **QA round 3's board append (`9f15f22`)** — read directly. Its own independent reproduction of the v2→v3 class-M repair (both differential mutations, the `check-refreeze-class.sh` reconstruction, the AC1 prose-vs-mechanics read) matches this review's own independent reproduction above, byte for byte on every measured value (hashes, exit codes, `differing=2`).
- **Records read end to end, not diff-shaped** — the full T-1062 spec (all sections), the full board entry from its first freeze-sweep line through QA round 3's verdict, `.shell-team/provenance/T-1062.md` (2 decisions), and `.shell-team/interventions/T-1062.md` (2 entries) were each read start to finish for this round. No contradiction between an earlier and a later line was found beyond the ones round 2 already disclosed (the fast-follow-disposition line's unbalanced nested backticks — cosmetic, not machine-parsed, not new).

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the two Major findings above (AC1's and AC3's shared boundary logic: the 1–3-space sibling-marker gap; and AC3's order-blind complement comparison across the target's own boundary). **This does not revert the class-M re-freeze v2→v3** — that re-freeze's own classification (mechanics, meaning-preserved) and its closure of both of round 2's named exploit variants are independently confirmed above and stand. What is needed is a third, in-scope class-M mechanics repair (v3→v4) to the same two check lines, verified against the two new mutation shapes named above (and, given this is now the third consecutive round finding real gaps in this same mechanism, the observation above about weighing a structurally different approach) before the next review round.

#### Fast-follow disposition
- Restated unchanged from rounds 1–2 (none of this round's findings touch these five):
  - AC1's literal-presence check not verifying the `vPREV`/`vX.Y.Z` token-to-release mapping — won't-fix. Rationale: matches this corpus's own repeatedly-disclosed presence-only trade-off (T-1059 AC5 precedent); the shipped bullet's actual semantics are already independently confirmed correct by three separate reads across rounds.
  - AC2's preamble extraction picking up content appended after the `<!-- END release-body -->` marker — won't-fix. Rationale: requires a deliberately adversarial edit this project's own Input-space section already declines to defend against; nothing currently exists after that marker.
  - AC4's `git ls-files` population check not verifying on-disk existence — won't-fix. Rationale: requires an already-dirty working tree at verification time, a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based checks, not a T-1062-specific gap worth a standalone fix.
  - AC5's scope lock omitting staged/unstaged changes to tracked paths — file-an-issue. Rationale: a real, corpus-wide hardening opportunity (many merged specs use the same commit-range-plus-untracked-files scope-lock shape), worth tracking once as a general checker-pattern issue rather than patched piecemeal per spec.
  - AC6's PII check running only against the committed diff — won't-fix. Rationale: matches AC6's own documented scope as a pre-push, committed-diff check; consistent with how PII checks run elsewhere in the repo; requires a dirty tree to matter.
- New this round: AC1's first-match latch not anchoring to the boilerplate-claim language, only to the literal `## Install / update` — won't-fix. Rationale: every construction this review attempted to bypass the *whole* gate with this gap was independently caught by AC3's own asymmetric base/working `(s,e)` computation; a genuine bypass requires deliberately authoring a decoy sentence packed with all six of AC1's required literals, a construction this spec's own Input-space section already declines to defend against.
- No new deferrals beyond the one above this round: both new Major findings are routed into the recommended v3→v4 rework round, not deferred as fast-follows.

## Round 4

### Codex Review verdict: APPROVE
- Task: T-1062
- Codex model: gpt-5-codex (provider-configured; no model flag passed per `agents/codex-reviewer.md` / `templates/adapters/codex-cli.txt`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 7 files, +495/-1 lines (`git diff --stat 383e9af5...HEAD`) — `docs/templates/release-notes-template.md` (+3/-1, **zero delta since round-1 HEAD** `e0f23c7`, confirmed live: `git diff --stat b5aa59e..HEAD -- docs/templates/release-notes-template.md` and `git diff --stat e0f23c7..HEAD -- docs/templates/release-notes-template.md` both produce no output), `.shell-team/specs/T-1062-release-notes-compare-link.md` (+182 cumulative; this round's actual delta is the operator-ratified class-B re-freeze v3→v4, commit `ff90838`), `.shell-team/todo.md` (+54, board hand-offs including QA round 4), `.shell-team/provenance/T-1062.md` (+10, unchanged since round 1 — 2 decisions, confirmed), `.shell-team/interventions/T-1062.md` (+14, 3 entries — the STOP-resolution `human-correction` entry added since round 3), `.shell-team/reviews/T-1062.md` (+209, rounds 1–3's own records), `.shell-team/test-recipe.md` (+23, unchanged since round 1). This round's actual content: the operator-ratified class-B rescope v3→v4 (`ff90838`), the fresh AC7(a) sweep against the rescoped text (v4 freeze sweep), QA round 4's independent verdict (`d6e8167`), and this record.

#### Class-B rescope verification (v3→v4) — this round's mandatory item
Per the STOP resolution recorded in the coordinating session's task prompt (operator ratified rescope option A, 「A案を承認GO」, 2026-08-13), this round reviews that ratified re-freeze rather than a fourth boundary patch. Both required halves were checked independently rather than trusted from the board's own claim.

- **Faithful execution of the ratified delta.** Reconstructed the pre-edit (v3) spec from the git blob at `f17827a` (`git show f17827a:.shell-team/specs/T-1062-release-notes-compare-link.md`) and ran `bash bin/check-refreeze-class.sh --old-hash 3771a89eba8308acf5dd9547af193b150d2acc2e <reconstructed> .shell-team/specs/T-1062-release-notes-compare-link.md` → `class-b: differing line at intent-block index 3 ... is not a '- check:' line on both sides` (exit 1) — the correct classifier result for a delta that touches criterion prose, matching the board's own record and QA round 4's independent reproduction (both cite the same hash pair). Read the actual commit diff (`git show ff90838`) line by line: every hunk is confined to the Goal's preamble-conservation clause, AC1's prose+check line, AC3's prose+check line, AC7(a)'s mutation list, DP2 (annotated, not deleted), DP7 (new), and correspondence rows 1/2/3/7/13 — nothing else in the spec or the board changed in that commit beyond the board's own append. Nothing beyond the ratified delta was smuggled in.
- **Withdrawn claims are delegated, never silently dropped.** AC1's new prose states in its own text that the placement claim is "verified by AC7's rendered-markdown item and QA's direct read of the bullet ... stated so the bound is a delegation and not a silent drop". AC3's new prose states the narrowing "stated in its own text as a delegation" to the same two surfaces. DP2 is annotated ("Enforcement form superseded by DP7") rather than silently rewritten, and DP7 records the ratification's own grounds. Correspondence rows 3 and 7 (placement / confinement) now point at **AC7(g) + QA direct read** instead of a mechanical AC1/AC3 clause, matching the delegation exactly.
- **No surviving prose still claims a withdrawn mechanical assertion.** `grep -n "bullet-scoped\|asserts placement\|outside-the-bullet\|only the one preamble bullet moves\|the exception lives in the \*\*same preamble bullet\*\*" .shell-team/specs/T-1062-release-notes-compare-link.md` against the live spec returns nothing — the only surviving hit for the old DP2 wording is inside the annotation itself, quoting it as superseded rather than asserting it. Independently confirms QA's own identical grep result.
- **AC1/AC3 internally sound at the presence bound.** Ran the live `- check:` lines via `bash bin/check-acs.sh` against the real, unmutated deliverable: `check-acs: 6 passed, 0 failed, 1 skipped, 0 unrecognized`. Confirmed `check-intent.sh` reports `aligned: T-1062 v4 (b36ff7dc1a529b8b18fe661f1d9a17edf718c404)`, matching the board's recorded hash. Independently reproduced, in a detached scratch worktree (`git worktree add --detach`, removed after use, main checkout `git status --short` clean throughout), two of the six v4-named AC7(a) mutations: deleting `previous` from the preamble → live AC1 check line exits `1`; inserting an HTML comment line inside the marker region → live AC3 check line exits `1`. Both restored to byte-identical originals (`cmp` confirmed) before the worktree was removed.
- **The withdrawn placement bound is genuinely gone, not merely relaxed.** Constructed the shape the operator's own rescope explicitly permits — moved the exception sentence entirely out of the `## Install / update` bullet into an unrelated footnote paragraph elsewhere in the preamble (after `## Title`), leaving the bullet reading only "copy them unchanged." — and ran the live AC1 check line against the mutation: exits `0`. This is not a new gap; it is the ratified trade-off (presence, never placement) made visible, exactly as AC1's own prose now discloses and exactly as this review's scope (b) requires judging it — at the presence bound, not the withdrawn placement bound.
- **Rounds 1–3's six Majors are genuinely mooted by withdrawal, not repaired.** All six (R1: bullet-boundary paragraph-absorption, one-directional whole-file survival; R2: whitespace-only-blank-line/under-indented-continuation, re-arming sibling marker; R3: 1–3-space sibling-marker continuation, order-blind relocation-across-boundary) were findings against the v1–v3 mechanisms this rescope deleted outright: v4's AC1 has no bullet-boundary extraction of any kind (a flat preamble-wide literal search, no start/end computation, no state machine to re-ignite or mis-terminate), and v4's AC3 has no complement/outside-the-bullet comparison at all (region-only byte-identity, the one clause that never failed in any round). None of the six exploit shapes has a surviving mechanism to apply to — they are not fixed, they are structurally absent. See the synthesis ledger below for the row-by-row disposition.
- **AC7(a)'s fresh mutation list matches the rescoped criteria.** All six v4-named shapes (delete `previous`, delete `omitted`, delete the region literal, HTML comment in-region, empty `.ja.md`, file under `templates/`) are the presence/byte-identity/set-equality shapes the rescoped AC1–AC5 actually assert; the two withdrawn placement shapes (relocate the exception, reword an untouched bullet) are named in the item's own text as deliberately absent, with (g)'s rendered-markdown read designated their catcher — matching AC7(g)'s continued presence unchanged since round 1.

#### Blockers
- none

#### Major
- none

#### Minor / nits
- No new findings this round. Rounds 1–3's five pre-existing Minor/nit items (AC1 token-mapping order — unaffected in kind by the rescope, since AC1 is still a presence check, now broader in scope but still not verifying token-to-release pairing; AC2 post-END-marker content; AC4 `git ls-files`-vs-filesystem; AC5 staged/unstaged scope-lock gap; AC6 committed-diff-only PII scope) stand unchanged — nothing in this round's diff touches the criteria or reasoning those five rest on.
- Round 3's sixth item (AC1's first-match latch not anchored to the boilerplate-claim language) is **superseded by the rescope, not restated** — see the synthesis ledger below. The mechanism it critiqued (a `head -n 1` latch onto the literal `## Install / update`) no longer exists in v4's AC1, which performs no latching or bullet extraction at all.

#### Adversarial findings
- Primary pass (`codex exec … review --base 383e9af5 --json`, read-only, read the real repository directly): "The release-note template now clearly identifies both compare-link substitutions while preserving the copyable release-body region. No actionable regressions were found in the changed files." Zero findings, Blocker or otherwise. Full text: `.shell-team/reviews/T-1062-codex-primary.txt` / `.jsonl`.
- No separate adversarial (devil's-advocate) pass was run this round: per this role's step-3/step-5 design, that pass is opt-in, triggered only by a Blocker/Major surfacing in this round's own findings. None did. In its place, this round's mandatory class-B verification item (above) supplied the adversarial scrutiny this task's three-round history called for — independently reproducing the rescope's own claimed mutation results, constructing and running the specific withdrawal-confirming mutation (footnote relocation) the ratified design predicts should now pass, and grepping the full spec for any surviving mechanical placement claim. This is the same rigor level rounds 1–3's mandatory re-freeze items applied to the mechanics re-freezes; it substitutes for a second Codex pass here because the primary pass itself, reading the real files directly, returned nothing to adversarially test further.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---|---|---|---|
| 1 | Round-1 Major #1 (AC1 bullet-boundary: unmarked paragraph absorbed into extraction) | this review: independently confirmed mooted | closed, mooted by withdrawal | v4's AC1 performs no bullet-boundary extraction of any kind (flat preamble-wide grep) — there is no start/end computation left for this shape to exploit. Not repaired; the mechanism it targeted no longer exists. |
| 2 | Round-1 Major #2 (AC3 one-directional whole-file survival gap) | this review: independently confirmed mooted | closed, mooted by withdrawal | v4's AC3 has no complement/outside-the-bullet comparison at all — region-only byte-identity remains, the clause this exact finding never touched. |
| 3 | Round-2 Major #1 (whitespace-only blank line + under-indented continuation) | this review: independently confirmed mooted | closed, mooted by withdrawal | Same reasoning as #1 — the boundary-terminator logic this shape exploited was deleted with the bullet-extraction mechanism, not patched further. |
| 4 | Round-2 Major #2 (re-arming sibling marker repeating the literal) | this review: independently confirmed mooted | closed, mooted by withdrawal | Same reasoning as #1 — no state machine remains to re-ignite; v4's check is a single non-stateful literal search. |
| 5 | Round-3 Major #1 (1–3-space-indented sibling marker treated as continuation) | this review: independently confirmed mooted | closed, mooted by withdrawal | Same reasoning as #1 — no continuation-vs-new-block distinction is computed anywhere in v4's AC1. |
| 6 | Round-3 Major #2 (AC3 order-blind complement comparison misses relocation across the target boundary) | this review: independently confirmed mooted | closed, mooted by withdrawal | Same reasoning as #2 — the complement comparison this finding was about was withdrawn outright, not hardened. |
| 7 | Round-3 Minor (AC1's first-match latch not anchored to boilerplate-claim language, decoy-bullet risk) | this review: judged superseded, not restated | superseded by rescope | The specific mechanism this finding named — a `head -n 1` latch onto the literal `## Install / update`, exploitable by a decoy bullet placed before the real one — does not exist in v4's AC1 (no latch, no per-line matching at all; a flat preamble-wide grep). The generalized concern (content anywhere in the preamble satisfying the six literals without the real bullet being fixed) is not a new, distinct gap — it is exactly the disclosed, ratified presence-only trade-off DP7 records and this round's own footnote-relocation test confirmed by direct construction. Not carried forward as a live fast-follow because restating the old mechanism's wording would misdescribe the current check line. |

#### Review-scope items addressed directly
- **(a) The rescope executes the operator's ruling faithfully** — confirmed above (class-B verification section): delta matches the ratification exactly (Goal clause, AC1, AC3, AC7(a), DP2 annotation, DP7, correspondence rows 1/2/3/7/13; nothing else touched), QA's independent `check-refreeze-class.sh` reproduction matches this review's own reproduction byte-for-byte (same hash pair, same classifier output), and no surviving prose anywhere in the spec still claims a withdrawn mechanical assertion (grep confirmed independently).
- **(b) The rescoped criteria are internally sound at their own declared bound** — confirmed: `check-acs.sh` 6/6 PASS against the real deliverable; two of the six AC7(a) mutations independently re-reproduced in a fresh scratch worktree with matching exit codes; the withdrawn placement bound genuinely no longer exists (footnote-relocation test, above) rather than being merely under-tested. This review did not re-litigate the withdrawn placement claim against new CommonMark shapes, consistent with the scope note that doing so would contradict the ratified decision this round verifies.
- **(c) Deliverable zero-delta** — confirmed live a fourth time: `git diff --stat b5aa59e..HEAD -- docs/templates/release-notes-template.md` and `git diff --stat e0f23c7..HEAD -- docs/templates/release-notes-template.md` both produce no output. Issue #240's own closure-by-the-filer's-standard (round 1's item (a)) stands unchanged and is not re-litigated.
- **(d) Records** — QA round 4's board append (`d6e8167`) read directly and cross-checked against this review's own independent measurements (both cite the identical `check-refreeze-class.sh` invocation, the identical intent-hash, the identical AC7(a) mutation set, the identical `pandoc`/rule-4 rendering confirmation); no discrepancy found. `.shell-team/interventions/T-1062.md` read end to end — 3 entries: two `human-correction` (the "lightweight framing" correction and the STOP-escalation ruling that directed the class-B rescope) and one `assumption-contradicted` (round 2's closure-claim contradiction); each entry's summary and effect faithfully match this review's own reading of the corresponding review round and board record. `.shell-team/provenance/T-1062.md` unchanged since round 1 (2 decisions, both still grounding correctly — the rescope decision itself is correctly recorded in interventions rather than provenance, since it is an operator-directed process ruling rather than an implementation choice this task's own executing role made). Board hygiene re-derived independently: exactly one `- intent-hash` line (v4), exactly four `- freeze-attestation` lines (v1 3P/3F, v2/v3/v4 5P/1F each), exactly three `- intent-ratified` lines (v1→v2 and v2→v3 class=mechanics each paired with a `- refreeze-class` line, v3→v4 class=B carrying no `- refreeze-class` bullet — the correct shape, matching T-1061's own class-B precedent this task's board record cites). `check-handoff.sh`, `check-board-headings.sh --base 383e9af5`, `check-pii-shapes.sh --base 383e9af5` (clean), `check-provenance.sh` (conformant, 2 entries), `check-interventions.sh --task T-1062` (conformant, 3 entries) all re-run live and green.

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`. The operator-ratified class-B rescope v3→v4 is verified faithful to the ratification, internally sound at its own declared (presence/byte-identity) bound, and all six of rounds 1–3's Majors are genuinely mooted by withdrawal rather than merely patched again. The deliverable itself has carried zero delta since round 1 and remains correct by every read this task's history has applied to it (this review's own `pandoc` rendering, QA's rule-4 read, AC7(g)'s rendered-markdown item).

#### Fast-follow disposition
- Restated unchanged from rounds 1–3 (none of this round's findings touch these five):
  - AC1's literal-presence check not verifying the `vPREV`/`vX.Y.Z` token-to-release mapping — won't-fix. Rationale: matches this corpus's own repeatedly-disclosed presence-only trade-off (T-1059 AC5 precedent); the shipped bullet's actual semantics are already independently confirmed correct by four separate reads across rounds (engineer, QA, this review, and now this round's own rendering re-check).
  - AC2's preamble extraction picking up content appended after the `<!-- END release-body -->` marker — won't-fix. Rationale: requires a deliberately adversarial edit this project's own Input-space section already declines to defend against; nothing currently exists after that marker.
  - AC4's `git ls-files` population check not verifying on-disk existence — won't-fix. Rationale: requires an already-dirty working tree at verification time, a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based checks, not a T-1062-specific gap worth a standalone fix.
  - AC5's scope lock omitting staged/unstaged changes to tracked paths — file-an-issue. Rationale: a real, corpus-wide hardening opportunity (many merged specs use the same commit-range-plus-untracked-files scope-lock shape), worth tracking once as a general checker-pattern issue rather than patched piecemeal per spec.
  - AC6's PII check running only against the committed diff — won't-fix. Rationale: matches AC6's own documented scope as a pre-push, committed-diff check; consistent with how PII checks run elsewhere in the repo; requires a dirty tree to matter.
- Dropped, not restated: round 3's Minor on AC1's first-match latch not anchoring to boilerplate-claim language — mooted rather than deferred, per the synthesis ledger row above (the mechanism it named no longer exists in v4). Not a fast-follow because there is nothing left to file or to decline to fix.
- No new deferrals this round.
