# T-1062 — Codex cross-provider review

## Round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1062
- Codex model: gpt-5-codex (provider-configured; no model flag passed per `agents/codex-reviewer.md` / `templates/adapters/codex-cli.txt`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 6 files, +253/-1 lines (`git diff --stat 383e9af5...HEAD`) — `docs/templates/release-notes-template.md` (the deliverable, +3/-1), `.shell-team/specs/T-1062-release-notes-compare-link.md` (new spec, 181 lines), `.shell-team/todo.md` (board hand-offs, +30), `.shell-team/provenance/T-1062.md` (new, 10 lines), `.shell-team/interventions/T-1062.md` (new, 6 lines), `.shell-team/test-recipe.md` (+23 append)

Diff base used throughout: `git merge-base feature/1061-adopter-docs-gate HEAD` = `383e9af56f50634c189e872251e340a8a5c538c2`, confirmed equal to `git rev-parse feature/1061-adopter-docs-gate` and unequal to `git rev-parse develop` (`d01ab30ea454b23292c12df6f80dceb151d5c42f`) — the stacked-branch premise the spec itself measures.

#### Blockers
- none

#### Major
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48` (AC1 check line) — The bullet-boundary extraction (`awk '/^<!-- BEGIN release-body -->$/{exit} /^- /{f=(index($0,"## Install / update")>0)} f'`) only resets `f` on a line that literally begins `^- `. A paragraph placed directly after the target bullet — separated by a blank line, not itself a new `- `/`*`/`+` list item — leaves `f` true and is silently absorbed into the extracted "bullet," so the literal-presence checks still pass even though the exception has moved out of the actual Markdown list item and back into the "footnote" placement AC1 exists to reject. This is not hypothetical: the engineer's own AC7(a) mutation-1 note records that exactly this shape ("a naive 'paragraph right after the bullet' attempt") **failed to redden AC1**, and they had to move the paragraph between two *other* bullets instead to get a red — i.e., the gap was found empirically during this task's own mutation testing and worked around rather than fixed. Confirmed by direct read of the check line, corroborated independently by both the primary and adversarial Codex passes.
  - Suggested fix (mechanics only, semantics of AC1 unchanged): terminate the extraction at the actual Markdown list-item boundary — reset `f` on a blank line followed by non-indented content, or on any CommonMark unordered-list marker (`^[[:space:]]{0,3}[-+*][[:space:]]`), not only on the literal `^- ` this criterion happens to use elsewhere.
- `.shell-team/specs/T-1062-release-notes-compare-link.md:54` (AC3 check line) — The "outside-the-bullet survival" clause is one-directional and whole-file-scoped: it extracts the branch-point preamble lines outside the target bullet, then asserts each still appears *somewhere* in the working file (`grep -qxF -- "$l" "$F"`, searched against the whole file, not the corresponding region). Two consequences, both real: (a) a brand-new paragraph or bullet inserted anywhere else in the preamble, while every original line survives untouched, still passes — so AC3 does not actually establish its own stated property ("one bullet moved and nothing else did"); (b) an untouched rule can be *relocated* — deleted from its original spot and pasted verbatim inside the target bullet or after the body — and still "survive" by the whole-file membership test, even though the criterion's own prose is about confinement, not mere presence. Confirmed by direct read of the check line, corroborated independently by both Codex passes.
  - Suggested fix: extract the non-target preamble from *both* the base and the working file with the same boundary rule, and compare those two bounded extractions for equality (not membership) — this catches additions and relocations, not just deletions.
- Both AC1 and AC3 sit inside this task's own frozen intent block (`68c4b19cf9771b1439825bbea1c9a844db8ca2a9`, no re-freeze in any round). Tightening their check-line mechanics without touching the criterion's stated meaning ("the exception lives in the same bullet," "only the one preamble bullet moves") is a class-M repair, not a class-B one — it does not touch the Goal sentence, Non-goals, a criterion's prose, or Input space, and it is this task's own criterion rather than another task's merged intent, so `## Non-goals`'s "no repair … of any merged criterion" clause does not apply to it. This is squarely a rework-round fix, not a fast-follow.

#### Minor / nits
- `.shell-team/todo.md:43` — the `### Local test result — T-1062` section (added by commit `4809d80`, before the real intervention entry landed) still reads `check-interventions.sh … conformant (0 entries, 1 sentinel, unchanged from the orchestrator's original)`. That is now false: the intervention file was amended with a real entry at commit `b5aa59e`, and both the QA verdict line directly above it (`.shell-team/todo.md:36`, "1 entry, 0 sentinel") and a live re-run of `check-interventions.sh` in this review (`1 entries, 0 sentinel`) confirm the current, correct count. The board's own audit trail now contradicts itself between two adjacent sections — an earlier line left stale by a later, correct one — a record-consistency defect distinct from a wording nit. Found independently in this review's own read-through and corroborated by the primary Codex pass.
- AC1 checks six literals for presence (`## Install / update`, `## Links`, `vPREV`, `vX.Y.Z`, case-insensitive `compare`, case-insensitive `previous`) but not their pairing — a bullet that reversed which token takes the previous vs. this release's tag would still satisfy every grep. Downgraded from the adversarial pass's raw P1: this is the same "honestly bounded to instruction-presence, never semantic self-consistency" trade-off this project's own corpus repeatedly makes and discloses on the record (T-1059 AC5's own design rationale, quoted three times in this spec). It is not a defect unique to T-1062, and the actual shipped bullet's semantic correctness was independently confirmed by manual read, by AC7(g)'s rendered-markdown check, and by QA's own "rule 4" bullet read — so nothing about *this* deliverable is actually ambiguous.
- AC2's preamble extraction (`.shell-team/specs/T-1062-release-notes-compare-link.md:51`) is bounded by `!f` around the two body markers, which also captures anything appended *after* `<!-- END release-body -->` (there is currently nothing there). A comment block dropped after the END marker containing all seven literals would count toward the "preamble" survival count. Requires a deliberately adversarial edit with no current trigger; the project's own Input-space section already declines to defend the template against "adversarial markdown … authored in this repository under review."
- AC4's set-equality clause (`.shell-team/specs/T-1062-release-notes-compare-link.md:57`) reads `git ls-files`, which lists tracked paths from the index regardless of whether the file still exists on disk (verified live: `rm`-ing a committed, un-`git rm`'d file leaves it in `git ls-files`). Requires a dirty working tree at verification time — a precondition every other clause in this spec already assumes is clean — and is generic to every `ls-files`-based population check in the corpus, not unique to T-1062.
- AC5's scope lock (`.shell-team/specs/T-1062-release-notes-compare-link.md:60`) unions the committed range with untracked files but not staged/unstaged changes to already-tracked paths, so a staged-but-uncommitted rename of the template to a disallowed path at verification time would not show in either measured set. Real hardening opportunity, but it requires an uncommitted, dirty tree at hand-off — the same generic gap recurs in many other specs' identically-shaped scope locks, so it reads as a corpus-wide checker-pattern issue rather than a T-1062-specific one.
- AC6's PII clause (`.shell-team/specs/T-1062-release-notes-compare-link.md:63`) runs `check-pii-shapes.sh --base <branch-point>`, which reads the committed diff — matching AC6's own stated scope ("covers the edited template before its bytes reach pushed history"). PII added to the working file after the last commit but before push would not be caught by this clause alone. Requires a dirty tree at verification time; consistent with how PII checks are run elsewhere in this repo's convention.
- Test-design observation: neither the engineer's 8 named AC7(a) mutations nor QA's 3-of-8 spot-audit happened to probe the specific shapes that actually break AC1/AC3 above (a trailing unmarked paragraph immediately after the bullet; an addition elsewhere in the preamble with every original line intact) — the mutation *methodology* itself has a coverage gap distinct from the two Major findings it happened not to catch.

#### Adversarial findings
- Primary pass (read-only `codex exec … review --base <branch-point> --json`): independently surfaced the AC1 placement-boundary gap, the AC3 one-directional-survival gap, and the same stale `### Local test result — T-1062` interventions-count line — three findings, zero of which needed a pasted excerpt to find (all read the real files directly). Full text: `.shell-team/reviews/T-1062-codex-primary.txt` / `.jsonl`.
- Adversarial pass ("what edit would violate each criterion's stated intent while still passing its literal check?" against AC1–AC6): corroborated both Major findings with additional exploit shapes (a sibling list item using `*`/`+` instead of `-` also evades AC1's reset condition; a relocated-not-just-added line evades AC3 the same way) and surfaced the five Minor/nit items above (token-mapping order, AC2 post-body content, AC4 ls-files-vs-filesystem, AC5 staged changes, AC6 working-tree PII). The adversarial pass labeled several of these P1 (blocker-mapped); this review's own judgment (documented in the ledger below) holds most of them at Minor because they require either a deliberately adversarial edit or an already-dirty working tree at verification time — conditions this project's own Input-space and hand-off conventions already decline to defend against elsewhere in the corpus. Full text: `.shell-team/reviews/T-1062-codex-adversarial.txt` / `.jsonl`.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---|---|---|---|
| 1 | AC1 bullet-boundary extraction gap (paragraph right after the bullet evades the awk reset) | primary: major (P2) / adversarial: blocker (P1, plus the `*`/`+` sibling variant) | major | Confirmed real and reachable by an honest editor with no adversarial intent (the engineer's own mutation testing hit this exact gap), but the current shipped deliverable is unaffected and the fix is a bounded, in-scope class-M mechanics repair to this task's own not-yet-merged criterion — not severe enough to be a blocker, but too concrete to defer. |
| 2 | AC3 one-directional / whole-file survival gap | primary: major (P2) / adversarial: blocker (P1, plus the relocation variant) | major | Same reasoning as #1: reachable without adversarial intent, undermines AC3's own stated "nothing else moved" claim, fixable in-scope before merge. |
| 3 | AC1 token-mapping order not checked | adversarial: blocker (P1) | minor | Downgraded: this is the corpus's own repeatedly-disclosed "presence, not semantic self-consistency" trade-off (T-1059 AC5 precedent), and the shipped bullet's actual semantics were independently confirmed correct by manual read, AC7(g)'s render check, and QA's rule-4 read. Not a T-1062-specific defect. |
| 4 | AC2 preamble extraction includes content after the END marker | adversarial: major (P2) | minor | Downgraded: requires a deliberately adversarial edit with no current trigger (nothing exists after the marker); mirrors the Input-space section's own declared stance of not defending against this repository's own contributors. |
| 5 | AC4 `git ls-files` vs. filesystem existence | adversarial: major (P2) | nit | Downgraded: requires a dirty working tree (tracked file deleted without `git rm`) at verification time — a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based population checks. |
| 6 | AC5 scope lock omits staged/unstaged changes | adversarial: major (P2) | minor | Kept at minor rather than major: real hardening value, but requires an uncommitted dirty tree at verification and the same gap recurs in many sibling specs' identically-shaped scope locks — a corpus-wide pattern, not unique to this task. |
| 7 | AC6 PII check is committed-diff-only | adversarial: blocker (P1) | minor | Downgraded: matches AC6's own documented scope (a pre-push, committed-diff check) and requires a dirty tree; consistent with how PII checks run elsewhere in the repo. |
| 8 | `.shell-team/todo.md:43` stale interventions count contradicting the later QA verdict line | primary: minor (P3) / this review's own independent read | minor | No change — both the primary pass and this review's own first-line-to-last read of the board entry independently landed on minor; corroboration, not a disagreement to resolve. |

#### Review-scope items addressed directly
- **(a) Closure by the filer's own standard** — confirmed. The rewritten bullet (`docs/templates/release-notes-template.md:31-33`) reads unambiguously in both directions: the compare URL is explicitly excepted with both tokens' meanings stated, and `## Install / update` plus the two changelog links are still unambiguously copy-unchanged (nothing in the new text touches them). Issue #240's own review-record origin (`.shell-team/reviews/T-1059.md` round-1 Minor, "a one-line clarification") is satisfied by the shipped text itself; the Major findings above are about the *acceptance criteria's* enforcement robustness, not about the shipped prose's own ambiguity.
- **(b) Merged-criteria side effects, measured not defended** — spot-checked independently in this review: `T-1059-docs-release-notes.md` AC5 = PASS at HEAD (live re-run, matching the spec's prediction and the "criterion most at risk" framing); `T-1061-adopter-docs-gate.md` AC13 = FAIL at HEAD (live re-run, matching the self-declared merge-point staleness); population 66→67 confirmed by live re-count on both sides. No row in the table defends a prediction against a measurement — the two corrected rows (T-1003 AC19, T-1019 AC10) were independently re-run in this review and both measure PASS/PASS at HEAD, matching the engineer's correction exactly. This closes QA's own `environmentally-unverified: AC7(c) T-1003 AC19 / T-1019 AC10 re-verification` risk note — independently confirmed in this round, not merely re-cited.
- **(c) Declaration consistency** — confirmed. `bin/team-init.sh`'s six `copy_template`/`copy_template_protected` calls all source from `$TEMPLATES_DIR` (`templates/`); a repo-wide search for `release-notes-template` finds it named only in `CONTRIBUTING.md`, the template itself, and this task's own records — nothing under `docs/` reaches an adopting repository through `team-init.sh`. The `- user-visible: no —` rationale is accurate.
- **(d) Blast-radius population is a live derivation** — confirmed. 66 at the branch point and 67 at HEAD both independently re-derived live in this review, matching the engineer's AC7(c) figures and the corrected board line.
- **QA's `environmentally-unverified` risk notes** — AC7(c) (T-1003 AC19 / T-1019 AC10) is now closed by this review's own independent re-run (see (b) above); no further re-verification needed before merge. AC7(a) mutation items 2/3/5/6/8 (delete `previous`, delete `omitted`, insert an HTML comment inside the region, reword an untouched bullet, add a file under `templates/`) remain unverified by QA, but this review judges them acceptably covered for merge purposes: each is a single literal-presence or byte-identity check whose mechanics are simple and were not implicated by either Codex pass's adversarial analysis (which instead found the *unrelated* AC1/AC3 gaps above — precisely the "test-design coverage gap" Minor note records). No independent re-verification of those five items is required as a merge condition.

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the two Major findings above (AC1 bullet-boundary extraction, AC3 one-directional survival). Both are class-M mechanics repairs to this task's own not-yet-merged intent block — tightening the check lines' boundary handling without altering either criterion's stated meaning — and should be re-frozen and re-verified (including a mutation test against the specific shapes named above) before the next review round.

#### Fast-follow disposition
- AC1's literal-presence check not verifying the `vPREV`/`vX.Y.Z` token-to-release mapping — won't-fix. Rationale: matches this corpus's own repeatedly-disclosed presence-only trade-off (T-1059 AC5 precedent); the shipped bullet's actual semantics are already independently confirmed correct by three separate reads (engineer, QA, this review).
- AC2's preamble extraction picking up content appended after the `<!-- END release-body -->` marker — won't-fix. Rationale: requires a deliberately adversarial edit this project's own Input-space section already declines to defend against; nothing currently exists after that marker.
- AC4's `git ls-files` population check not verifying on-disk existence — won't-fix. Rationale: requires an already-dirty working tree at verification time, a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based checks, not a T-1062-specific gap worth a standalone fix.
- AC5's scope lock omitting staged/unstaged changes to tracked paths — file-an-issue. Rationale: a real, corpus-wide hardening opportunity (many merged specs use the same commit-range-plus-untracked-files scope-lock shape), worth tracking once as a general checker-pattern issue rather than patched piecemeal per spec.
- AC6's PII check running only against the committed diff — won't-fix. Rationale: matches AC6's own documented scope as a pre-push, committed-diff check; consistent with how PII checks run elsewhere in the repo; requires a dirty tree to matter.
- `.shell-team/todo.md:43`'s stale `### Local test result — T-1062` interventions count contradicting the adjacent, later QA verdict line — won't-fix. Rationale: purely cosmetic staleness in an ephemeral per-task audit-trail section, not machine-parsed and not covered by `check-intent.sh`'s hash; the correct information already sits one section above it in the same board entry; retroactively editing an already-attested hand-off record for a zero-impact staleness is worse than leaving it.

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1062
- Codex model: gpt-5-codex (provider-configured; no model flag passed per `agents/codex-reviewer.md` / `templates/adapters/codex-cli.txt`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 7 files, +327/-1 lines (`git diff --stat 383e9af5...HEAD`) — one file more than round 1's count because this branch now also carries round 1's own review record (`.shell-team/reviews/T-1062.md`, +65, committed at `61d8190`). The deliverable itself, `docs/templates/release-notes-template.md`, carries **zero delta** since round 1's own HEAD (`b5aa59e`): `git diff b5aa59e..HEAD -- docs/templates/release-notes-template.md` is empty, confirmed live. This round's actual content is the class-M mechanics re-freeze v1→v2 (commit `9acb734`, touching only the AC1/AC3 `- check:` lines), QA round 2's verdict, and this record.

#### Mandatory class-M re-freeze verification (v1→v2)
Both required halves were checked independently rather than trusted from the board's own claim, per this role's mandatory item for a declared re-freeze.

- **Delta confinement.** Reconstructed the pre-edit spec from the git blob at `4809d80` (the last commit that touched the spec before `9acb734`) and ran `bash bin/check-refreeze-class.sh --old-hash 68c4b19cf9771b1439825bbea1c9a844db8ca2a9 <reconstructed> .shell-team/specs/T-1062-release-notes-compare-link.md` → `mechanics: T-1062 68c4b19c... -> 24c7bba8e86ad6e2adfaf0521639f141c3de5e77 differing=2`, hash-identical to the board's `refreeze-class` record and to QA round 2's own independent reproduction. Every differing line is a `- check:` line on both sides (AC1's and AC3's) — confirmed by direct diff of the two blobs, not merely by the classifier's own report.
- **Meaning preservation.** Read AC1's and AC3's prose directly against both the old and new check lines. The new AC1 line still bounds extraction to the target bullet rather than searching the whole preamble, and the new AC3 line still compares the non-bullet preamble as a bounded, ordered extraction rather than an unordered whole-file membership test — both changes are strictly in the direction their own criterion's prose already demanded, and neither weakens on any input this file's own real content can produce. This half passes.
- **Named exploit-variant closure.** Independently reproduced both exploit shapes this review's own round-1 adversarial pass named, in a detached scratch worktree at `9acb734` (never the main checkout), removed after use:
  - AC1, `*`/`+` sibling item: moved the exception clause into a separate list item marked `*` (not `-`) directly after the boilerplate-claim bullet. The reconstructed **old** AC1 check line still exits `0` (the exact false pass the round-1 review named); the **new** AC1 check line exits `1`. Restored and re-observed green.
  - AC3, relocation: reordered two untouched, adjacent preamble bullets (`## Highlights` and `## Migration notes`) with every original line's bytes unchanged, leaving the target bullet untouched. The reconstructed **old** AC3 check line still exits `0` (unordered whole-file membership can't see a reordering); the **new** AC3 check line exits `1` (its `cmp -s` sequence comparison catches it). Restored and re-observed green.

  Both results match the board's own class-M evidence field and QA round 2's independent reproduction exactly. **The re-freeze v1→v2 is correctly classified as mechanics and its own stated meaning is preserved — it is not reverted by this review.**

- **However, this round's own fresh adversarial reading of the *repaired* lines surfaces two further gaps in the same mechanism, neither of which is one of the shapes named in round 1** (so their existence does not retroactively invalidate the re-freeze's classification), **but both of which are real, reachable-by-an-honest-editor placement violations that the just-repaired check lines still do not catch.** See `#### Major` below. Both were independently reproduced by direct execution against the actual check-line text plus `pandoc -f gfm -t html` rendering, not merely reasoned about.

#### Blockers
- none

#### Major
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48,54` (AC1 and AC3 check lines, shared boundary logic) — **A whitespace-only "blank" line does not reset the extractor, but GFM treats it as blank anyway.** The new guard resets `f` only on a line matching `!/^[[:space:]]/`; a line that is empty resets `f` (an empty string does not match `^[[:space:]]`), but a line consisting of a single trailing space does **not** reset `f` (it does match `^[[:space:]]`) even though CommonMark/GFM defines a blank line as "no characters, or only spaces/tabs" — both are blank for rendering purposes. Combined with the second gap — the guard only reacts to *zero* indentation, not to indentation narrower than the `- ` marker's two-column continuation width — a paragraph placed after a one-space "blank" line and itself indented by one space is absorbed into the bullet extraction by both AC1 and AC3, while GFM renders it as a separate paragraph outside the list item. Verified directly: mutated `docs/templates/release-notes-template.md` in a detached scratch worktree at `9acb734` so the boilerplate bullet reads `... copy them unchanged.` alone, followed by a line containing exactly one space, followed by three lines each indented by exactly one space carrying the exception text; `bash bin/check-acs.sh` reports **AC1 PASS, AC3 PASS** (all 6 check-bearing criteria PASS) against this mutation; `pandoc -f gfm -t html` on the same file renders `<li>…copy them unchanged.</li></ul>` followed by a separate, sibling `<p>except the compare URL…</p>` — outside the list entirely. Independently surfaced by this review's own adversarial probing before Codex's own primary pass gestured at the same shape (Codex's own stated mechanism — "keeps `f` set because it only terminates on a completely non-indented line" — is imprecise: a *truly* empty line does terminate it; the gap requires the "blank" line to itself carry trailing whitespace, which is exactly the shape an ordinary editor produces without meaning to and which no lint in this repository's convention currently flags). Confirmed independently a second time by the adversarial Codex pass, byte-for-byte matching mutated region and `pandoc` verification.
  - Suggested fix: terminate the bullet extraction on any line matching CommonMark's own definition of blank (`^[[:space:]]*$`), not only a literal empty line; and require continuation content to be indented by at least the marker's two-column content offset (i.e., resetting on `^[[:space:]]{0,1}[^[:space:]]` as well as on the existing zero-indent and marker-line conditions), rather than only on zero indentation.
- `.shell-team/specs/T-1062-release-notes-compare-link.md:48,54` (AC1 and AC3 check lines, shared boundary logic) — **The extraction re-arms on any later marker line that happens to repeat the target phrase, rather than terminating unconditionally after the first target bullet.** `f=(index($0,"## Install / update")>0)` is recomputed on *every* list-marker line, including one that starts an entirely new, separate list item; if that later item's own text also contains the literal `## Install / update` — an entirely plausible way to word an "exception to the boilerplate" sentence — `f` is set true again and the new item's content (and its continuation lines) are absorbed into both AC1's `bul` extraction and AC3's `nb`-exclusion, even though it is a structurally distinct list item. Verified directly: mutated the file so the target bullet reads only `... copy them unchanged.` and is immediately followed (after a real blank line) by a separate `* Exception to **`## Install / update`** and **`## Links`** boilerplate: the compare URL takes `vPREV`...` item; `bash bin/check-acs.sh` reports **AC1 PASS, AC3 PASS**; `pandoc -f gfm -t html` renders two separate `<ul>` elements (a `-` marker followed by a `*` marker starts a new list in GFM), each with its own `<li>` — the exception item is unambiguously not inside the boilerplate-claim item. Found by the adversarial Codex pass and independently reproduced byte-for-byte by this review, including the `pandoc` confirmation.
  - Suggested fix: make the extraction single-shot — start capturing only at the *first* line whose marker mentions `## Install / update`, and terminate unconditionally and permanently at the next list-marker line regardless of that line's own content (never recompute `f` a second time from a later marker).
- Both findings sit inside this task's own frozen intent block, inside the check lines the class-M re-freeze v1→v2 just repaired. Tightening them further without touching AC1's or AC3's stated meaning is again a class-M repair (mechanics only), not a class-B one, and is squarely a rework-round fix rather than a fast-follow — the same reasoning round 1 gave for the two findings this round confirms are now closed.

#### Minor / nits
- No new Minor/nit findings this round. Round 1's five (AC1 token-mapping order, AC2 post-END-marker content, AC4 `git ls-files`-vs-filesystem, AC5 staged/unstaged scope-lock gap, AC6 committed-diff-only PII scope) stand unchanged — nothing in this round's actual diff (the v1→v2 re-freeze, QA round 2's verdict, this record) touches any of the criteria or reasoning those five findings rest on.

#### Adversarial findings
- Primary pass (`codex exec … review --base 383e9af5 --json`, read-only, read the real spec file itself): surfaced one finding, P2, on the AC1 check line at line 48 — the shape now recorded above as the first Major, though this review's own follow-up execution found the primary pass's own stated mechanism ("terminates only on a completely non-indented line") to be imprecise; a truly empty line *does* terminate the extraction, and the reachable exploit requires the "blank" separator to itself carry trailing whitespace. Full text: `.shell-team/reviews/T-1062-codex-primary.txt` / `.jsonl`.
- Adversarial pass (devil's-advocate framing against the just-repaired AC1/AC3 boundary logic specifically, explicitly asked to distinguish the awk script's notion of "blank line" from GFM/CommonMark's, and to verify any finding against real `pandoc` rendering rather than reasoning alone): returned two findings, both `major`, both independently reproduced byte-for-byte by this review with matching `pandoc` output — the whitespace-only-blank-line gap (corroborating and sharpening the primary pass's P2) and the re-arming-sibling-marker gap (a new shape the primary pass did not surface). Full text: `.shell-team/reviews/T-1062-codex-adversarial.txt` / `.jsonl`.
- Both Codex passes' severities (`P2`, and `major` directly) map onto or already equal this project's `major` without adjustment.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---|---|---|---|
| 1 | Round-1 Major #1 (AC1 bullet-boundary: unmarked paragraph right after the bullet) | this review: independently reproduced as closed | closed, no longer open | Old AC1 check line exits `0` on the reconstructed pre-repair blob against this exact mutation; new AC1 check line exits `1`. Matches the board's own class-M evidence and QA round 2's independent reproduction. |
| 2 | Round-1 Major #2 (AC3 one-directional survival: relocation) | this review: independently reproduced as closed | closed, no longer open | Old AC3 check line exits `0` on a reordering of two untouched preamble bullets; new AC3 check line exits `1`. Matches the board's own class-M evidence and QA round 2's independent reproduction. |
| 3 | Whitespace-only blank line + under-indented continuation evades AC1/AC3 | primary: major (P2, imprecise mechanism as stated) / adversarial: major (precise mechanism, pandoc-verified) / this review: independently reproduced | major | Not one of round 1's named exploit variants, so it does not reopen or invalidate the v1→v2 re-freeze's own classification, but it is a real, reachable-without-adversarial-intent placement violation the repair does not close. Kept at major, matching round 1's own precedent for this exact class of finding (current shipped deliverable unaffected; check-line mechanics need another bounded, in-scope tightening). |
| 4 | Re-arming sibling marker that repeats "## Install / update" evades AC1/AC3 | adversarial: major (pandoc-verified) / this review: independently reproduced | major | Same reasoning as #3: reachable by an ordinary, plausible wording choice, not adversarial markdown under this spec's own Input-space declarations, real placement violation, class-M in-scope fix. |
| 5 | Primary pass's stated mechanism for its own P2 finding ("terminates only on a completely non-indented line") | primary's own framing | corrected, not downgraded | This review's direct execution shows a truly empty line *does* terminate the extraction; the reachable exploit requires the blank separator to carry trailing whitespace. The underlying finding (#3 above) is not weakened by this correction — only the primary pass's own description of the mechanism is refined. |

#### Review-scope items addressed directly
- **Same-class-2 inventory transcription (board, `.shell-team/todo.md`, rework round 1 entry)** — checked against this review's own round-1 ledger, faithfully, not softened: the board's apply/not-apply table names AC1 and AC3 apply (repaired), and AC2/AC4/AC5/AC6 not-apply with reasons that match this review's ledger rows #4 (AC2, won't-fix), #5 (AC4, won't-fix), #6 (AC5, file-an-issue, actioned at the disposition step), and #7 (AC6, won't-fix) respectively, substance for substance — no reason was reworded to sound less concerning than the original ledger row.
- **Records read end to end, not diff-shaped** — `.shell-team/provenance/T-1062.md` (2 decisions), `.shell-team/interventions/T-1062.md` (1 entry), the full T-1062 spec (all sections, not only the AC1/AC3 delta), and the full board entry from its first freeze sweep line through QA round 2's verdict, were each read start to finish for this round rather than only the lines the re-freeze diff touched. No contradiction between an earlier and a later line was found beyond the one QA round 2 already disclosed (the fast-follow-disposition line's unbalanced nested backticks — cosmetic, not machine-parsed, not new).
- **Deliverable zero-delta** — confirmed live: `git diff b5aa59e..HEAD -- docs/templates/release-notes-template.md` is empty. Round 1's `#### Review-scope items addressed directly` (a)-(d) therefore all still hold unchanged and are not re-litigated here.
- **check-intent.sh / check-acs.sh, live** — `check-intent: aligned: T-1062 v2 (24c7bba8e86ad6e2adfaf0521639f141c3de5e77)`; `check-acs: 6 passed, 0 failed, 1 skipped, 0 unrecognized` against the real, unmutated deliverable — the two new Major findings above are about the check lines' ability to catch *other* inputs, not about the current shipped bullet, which remains correct.

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the two Major findings above (both in AC1's and AC3's shared bullet-boundary check-line mechanics: the blank-line/indentation gap, and the re-arming-marker gap). **This does not revert the class-M re-freeze v1→v2** — that re-freeze's own classification (mechanics, meaning-preserved) and its closure of the two round-1-named exploit variants are both independently confirmed above and stand. What is needed is a further, in-scope class-M mechanics repair (v2→v3) to the same two check lines, verified against the two new mutation shapes named above before the next review round.

#### Fast-follow disposition
- Restated unchanged from round 1 (none of this round's findings touch these five):
  - AC1's literal-presence check not verifying the `vPREV`/`vX.Y.Z` token-to-release mapping — won't-fix. Rationale: matches this corpus's own repeatedly-disclosed presence-only trade-off (T-1059 AC5 precedent); the shipped bullet's actual semantics are already independently confirmed correct by three separate reads (engineer, QA, this review).
  - AC2's preamble extraction picking up content appended after the `<!-- END release-body -->` marker — won't-fix. Rationale: requires a deliberately adversarial edit this project's own Input-space section already declines to defend against; nothing currently exists after that marker.
  - AC4's `git ls-files` population check not verifying on-disk existence — won't-fix. Rationale: requires an already-dirty working tree at verification time, a precondition every other clause in this spec assumes clean; generic to the whole corpus's `ls-files`-based checks, not a T-1062-specific gap worth a standalone fix.
  - AC5's scope lock omitting staged/unstaged changes to tracked paths — file-an-issue. Rationale: a real, corpus-wide hardening opportunity (many merged specs use the same commit-range-plus-untracked-files scope-lock shape), worth tracking once as a general checker-pattern issue rather than patched piecemeal per spec.
  - AC6's PII check running only against the committed diff — won't-fix. Rationale: matches AC6's own documented scope as a pre-push, committed-diff check; consistent with how PII checks run elsewhere in the repo; requires a dirty tree to matter.
- No new deferrals this round: both new findings are Major, routed into the recommended rework round above, not deferred as fast-follows.
