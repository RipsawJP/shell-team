### Codex Review verdict: REQUEST_CHANGES
- Task: T-1136
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests
- Diff scope: 33 files, +560/-100 lines (`git diff --stat develop...HEAD`)

Own verification before invoking Codex (own-run, not transcribed from the hand-off): `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1136-invocation-policy.md` → `check-acs: 12 passed, 0 failed, 0 skipped, 0 unrecognized`. `bash bin/check-prompt-sync.sh` → all registered prompt blocks in sync. `bash tests/codex-agents/run.sh` → 37 PASS / 0 FAIL (own recount via `grep -c '^PASS:'`/`grep -c '^FAIL:'`). `bash tests/codex-skeleton-hygiene/run.sh` → exit 0. `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0. Independently re-derived AC1's whitespace-normalized nine-spelling sweep over the same pathspec by hand (own `git ls-files` → per-file `tr '\n' ' ' | tr -s ' '` flatten → `grep -Fic`) → 0 matches, confirming AC1/QA's own number. Read the full diff for every touched file (agents/*.md, docs/{adopting,distribution,workflow}.{md,ja.md}, README pair, CLAUDE.md, the two canonical prompt blocks and their registry rows, `bin/gen-codex-agents.sh`'s trap rewrite, `.shell-team/provenance/T-1136.md`).

Two Major defects were found by directly reproducing what the shipped diff claims, neither caught by AC-grammar-level checks (both are documented, in the spec's own words, as checking grammar/presence rather than truth-of-content or executability):

1. **`bin/codex-capture.sh:85-88`'s rewritten comment is now factually false about the script's own resolution code.** The AC11 sweep replaced an accurate comment ("bare on PATH when the plugin is loaded, else the cwd-relative `bin/team-paths.sh`") with one claiming the reviews-dir default is "invoked as `bash \"<plugin root>/bin/team-paths.sh\"`, never assumed to be on PATH." Read `:169-182`: the actual resolver only tries `command -v team-paths.sh` (bare PATH) or `-f bin/team-paths.sh` (cwd-relative) — there is no `<plugin root>` sibling resolution at all, before or after this diff. Confirmed live: I ran `bash bin/codex-capture.sh --alloc --stem …` from this checkout with no `--reviews-dir` and it resolved only via the cwd-relative fallback, exactly as the *old* comment said. In an installed-plugin deployment (the adopter's own target repo as cwd, `bin/` not on `PATH`), `agents/codex-reviewer.md`'s own shown `codex-capture.sh --alloc --stem …` recipe line (untouched by this diff, no `--reviews-dir`) would `die 2 "cannot resolve reviews dir"` — the comment now describes a code path that isn't there. This is not the same defect class as "resolving `<plugin root>` for the reader," which the spec's own Non-goals correctly declines to fix; it is a plain documentation-accuracy regression in a shipped `bin/` script's own comment, introduced by choosing generic sweep phrasing without checking it against this specific script's actual resolver. Codex's adversarial pass independently confirmed this and rated it `major`.
   - Suggested fix: revert the comment to accurately describe the actual PATH-or-cwd-relative fallback (adding a pointer to the general no-PATH-assumption policy in prose is fine; asserting a resolution mechanism the code doesn't implement is not).

2. **Both of `.shell-team/provenance/T-1136.md`'s `- reproduce:` lines (`:25`, `:62`) are broken as literally shown.** Each embeds unescaped backticks inside a double-quoted `git grep` pattern passed as a `--set` value (e.g. `"on \`PATH\` when the plugin is loaded"` with the backticks bare, not `\`\`\`...\`\`\``-escaped). `bin/derive-populations.sh` re-evaluates each `--set` value through a second shell layer; unescaped backticks there are parsed as command substitution rather than literal characters. I copied line 25 verbatim (stripping the `- reproduce: ` prefix) and ran it: it exits 1 — `derive-populations: set 'onpath-btick-loaded' exited with unaccepted status 1` — and aborts *before* emitting any derivation block. The same defect is present in line 62's Japanese counterpart (three of its four `--set` values carry the identical unescaped-backtick pattern). This directly contradicts AC10's own stated purpose ("whoever reads this task's record re-runs the command instead of trusting a number") for exactly the two blocks AC10 exists to protect. The *auto-emitted* `- set: … — command: …` rows a few lines below each block correctly show escaped backticks (`\`PATH\``), which is strong evidence the engineer's actual working invocation (when the recorded counts were produced) used proper escaping, and the hand-copied `- reproduce:` line simply dropped it when transcribing. `bin/check-provenance.sh`'s own grammar-only contract (confirmed by reading its header) cannot catch this — it checks presence/ordering, never executability — so nothing in the shipped verification chain would have caught it short of actually running the line. The failure is loud (a clear shell/tool error, not a silent miscount) and the underlying recorded counts are independently correct (I re-verified the actual population by hand above), so this doesn't cast doubt on the sweep's substance — but the record's own core "reproduce it yourself" promise is broken for both blocks as shipped. Codex's own adversarial pass rated this `minor` on the strength of the loud-failure/correct-data distinction; I keep it at `major` because AC10's stated contract is specifically about mechanical reproducibility, and that contract fails for 100% of this record's reproduce lines, not an edge case.
   - Suggested fix: escape the backticks in both `- reproduce:` lines (or quote the `--set` values so the nested shell layer cannot reinterpret them), then re-run both lines end-to-end and confirm they emit the exact blocks already embedded before landing the fix.

#### Blockers

None.

#### Major

- `bin/codex-capture.sh:85-88` — comment now asserts a `<plugin root>` sibling-resolution mechanism the code does not implement; see finding 1 above. Suggested fix as stated above.
- `.shell-team/provenance/T-1136.md:25,62` — both `- reproduce:` lines fail when run exactly as shown, due to unescaped backticks re-interpreted by `derive-populations.sh`'s nested shell evaluation; see finding 2 above. Suggested fix as stated above.

#### Minor / nits

- `skills/loop-triage/SKILL.md:13-16` (and the same pattern Codex's primary pass names in `skills/goal/SKILL.md`, `skills/run/SKILL.md`, `skills/review-response/SKILL.md`) — the shown command substitution `$(team-paths.sh --get KEY)` stays bare; the new `<plugin root>/bin/` reference form sits only in a trailing parenthetical, not inline in the executed text. Codex's primary pass rated this `P1` (blocker-mapped) reading it as a literally-executed script; I read it as a Minor documentation-polish item instead, because these `SKILL.md`/`agents/*.md` files are prose an LLM agent reads and adapts into its own actual bash calls (the identical parenthetical-only pattern is used throughout every other Operating-paths block this same diff touches, including in `codex-reviewer.md`'s own untouched code fences), not a shell script executed verbatim — and AC2's own frozen scope only requires the reference string to appear somewhere in the file, not inline with every command substitution.
- `.shell-team/specs/T-1136-invocation-policy.md:77` (and the identically-shaped AC2/AC11 checks) — the whitespace-normalization step only maps LF→space and squeezes literal ASCII spaces; a tab character between words would survive unnormalized and could theoretically hide an instance of the claim. No tracked file in the swept pathspec actually contains a tab (`git ls-files … | xargs grep -lP '\t'` → no matches, confirmed), so this is a robustness gap in the check's generality rather than a confirmed miss, and it parallels the spec's own declined "engineered against normalization" out-of-scope class (NUL/form-feed/zero-width-space) — a tab is the same flavor of edge case.

#### Adversarial findings

Ran a targeted devil's-advocate pass on the two Major findings above (`bin/codex-capture.sh`'s comment and both provenance `- reproduce:` lines), asking Codex to argue the strongest case each is *not* a real defect and then test that defense by actually running the reproduction. Result: both defenses failed once tested. On finding 1, Codex confirmed the code has no `<plugin root>` resolution branch and that an installed-like run without `--reviews-dir` exits 2 with "cannot resolve reviews dir" before any capture is allocated — independently landing on `major`. On finding 2, Codex's own execution of the reproduce lines also failed (in its own sandboxed environment, for a related reason — `mktemp`/read-only-sandbox friction — plus its own supplemental run of the inner commands reproduced the same backtick-as-command-substitution failure I found), and it explicitly noted the failure is loud rather than silently wrong, landing on `minor` rather than `major`. No further, previously-unseen findings were surfaced by the adversarial pass beyond corroborating and re-scoring these two.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `bin/codex-capture.sh` comment claims a `<plugin root>` resolution the code doesn't implement | primary: P1 (maps to blocker) / adversarial: explicit `major` | major | Downgraded from the primary's blocker-mapped P1: it is an internal implementation comment the spec's own AC11 rationale already characterizes as "not an instruction anyone follows" (droppable component 1, lowest pre-commitment priority), and the underlying functional gap (no plugin-root resolution in `codex-capture.sh`) pre-dates this diff and was never in this task's scope to fix — only the comment's *accuracy* was. Kept at major rather than minor because it is a confirmed, reproducible regression from true to false documentation in a shipped `bin/` script, not a stylistic nit. |
| 2 | Both provenance `- reproduce:` lines fail as shown (unescaped backticks) | primary: P2 (maps to major) / adversarial: explicit `minor` | major | Kept at major over the adversarial pass's own `minor` self-rating: AC10's stated contract is specifically "re-run the command instead of trusting a number," and the reproduce line fails for both of the record's two derivation blocks (100% of AC10's own subject), not an edge case. The loud-failure / correct-underlying-data mitigations the adversarial pass cites are real and are why this isn't a blocker, but they don't reduce it below major given the contract is broken outright as shipped. |
| 3 | `skills/loop-triage/SKILL.md` (and sibling skills) keep the reference form in a trailing parenthetical rather than inline with the shown command substitution | primary: P1 (maps to blocker) | minor | Downgraded: these files are LLM-agent-consumed prose, not literally-executed shell scripts — the identical parenthetical-only convention is used throughout every other Operating-paths block this same diff touches (including ones I independently confirmed correct), and AC2's own frozen check only requires the reference string to appear in the file, not inline with every command substitution. Not a functional regression this diff introduced. |
| 4 | AC1/AC2/AC11's whitespace normalization doesn't collapse tabs | primary: P2 (maps to major) | minor | Downgraded: verified no tracked file in the swept pathspec actually contains a tab, so this is a theoretical robustness gap in the check's generality, not a confirmed miss in the current sweep — parallels the spec's own explicitly out-of-scope "engineered against normalization" class. |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG`, linked to the two Major findings above (`bin/codex-capture.sh:85-88`'s comment; `.shell-team/provenance/T-1136.md:25,62`'s reproduce lines).

#### Fast-follow disposition

- `skills/loop-triage/SKILL.md` and sibling skills' parenthetical-only reference form (Minor 3 above) — file-an-issue, rationale: this is a documentation-polish improvement applicable across the whole corpus of agent/skill Operating-paths blocks (not unique to this task's diff), better tracked as its own small follow-up than folded into this task's rework round.
- AC1/AC2/AC11's tab-insensitive whitespace normalization (Minor 4 above) — won't-fix for this task, reason: no tracked file in the swept population is affected today, and the spec's own Non-goals already decline hardening the check against engineered/adversarial whitespace in this repository's own trusted, authored prose; worth revisiting only if a real instance is ever found.

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o .shell-team/reviews/.codex-capture.T-1136-codex-primary.out.C6nFfe > .shell-team/reviews/.codex-capture.T-1136-codex-primary.jsonl.5Hsuuo 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (primary): T-1136-codex-primary
- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1136-codex-adversarial.out.I7F5B1 "Play devil's advocate on two specific findings already confirmed in T-1136's diff (base develop, HEAD current branch). Read the real files yourself with git diff / nl -ba -- do not expect any diff pasted here. Finding A: bin/codex-capture.sh's comment at lines ~85-88 now claims the reviews-dir default resolution is invoked as bash <plugin root>/bin/team-paths.sh, never assumed to be on PATH, but the actual code (around lines 169-182) only tries command -v team-paths.sh (bare PATH) or the cwd-relative ./bin/team-paths.sh file, with no plugin-root sibling resolution at all -- is this comment now factually false about the code, and if so how severe is that in an adopter/installed-plugin deployment where the caller does not pass --reviews-dir? Finding B: .shell-team/provenance/T-1136.md's two reproduce lines (around line 25 and line 62) embed unescaped backticks inside the --set argument's double-quoted grep pattern -- try actually running each reproduce command exactly as shown and report whether it succeeds or fails, and if it fails whether the failure is loud or silently produces wrong counts. For both findings, argue the strongest case that these are NOT real defects and then say whether that defense holds up once you have run the reproduction. Return a JSON array of {severity, file, line, issue, suggestion}. < /dev/null > .shell-team/reviews/.codex-capture.T-1136-codex-adversarial.jsonl.JS1XxX 2>&1
- pass-role (adversarial): generation
- briefing-fidelity (adversarial): carried — both suspected findings from the primary pass (codex-capture.sh's comment inaccuracy, the provenance reproduce lines' unescaped backticks) were composed verbatim into the prompt argument this invocation passed to Codex, with an explicit instruction to test each by actually running the reproduction
- raw-capture (adversarial): T-1136-codex-adversarial
