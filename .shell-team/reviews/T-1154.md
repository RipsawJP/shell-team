### Codex Review verdict: APPROVE
- Task: T-1154
- Codex model: gpt-6-luna
- Codex CLI: codex-cli 0.156.1
- Verification mode: static-only
- Diff scope: 6 files changed, +479/-11 lines (full task commit range `3698c136...HEAD`); the reviewed code surface per this round's scope is `bin/codex-capture.sh` (+38/-8, the reviews-dir resolver block and its header paragraph) and `tests/codex-skeleton-hygiene/run.sh` (+178, six new labelled fixture cases); the remaining files in the diff are this task's own spec, provenance, interventions and board records.

#### Evidence ledger
| # | perspective | source | result | evidence |
|---|---|---|---|---|
| 1 | T1 — correctness against spec ACs | evidence pass | holds | `bin/codex-capture.sh:217`, `bin/codex-capture.sh:271`, `bin/codex-capture.sh:281`, `bin/codex-capture.sh:292` |
| 2 | T2 — security / trust boundary | evidence pass | holds | `bin/codex-capture.sh:281`, `bin/codex-capture.sh:284`, `bin/codex-capture.sh:286` — `[ -f ... ]` excludes directories and other non-regular targets; hostile-sibling substitution is an explicitly out-of-scope synthetic extreme (spec `## Input space`, out-of-scope item 2) |
| 3 | T3 — edge cases / boundary inputs the tests miss | evidence pass | defect (fixture-completeness, not a functional defect — see Synthesis audit ledger) | `tests/codex-skeleton-hygiene/run.sh:560,565,593,627,633,645,657` — none of the six new labelled cases invokes the script bare through `PATH` (Input space class 1's last item); this reviewer independently reproduced that exact input class live (own scratch fixture, not the engineer's or QA's) and confirmed no defect — see round notes below |
| 4 | T4 — design smells / structural issues | evidence pass | holds | `bin/close-out.sh:117`, `bin/close-out.sh:127`, `bin/codex-capture.sh:281`, `bin/codex-capture.sh:284`, `bin/codex-capture.sh:292` — the symlink-resolution block duplicates `bin/close-out.sh`'s existing pattern rather than sharing it, a deliberate, low-risk duplication (no new shared dependency between two already-independent scripts); a failing sibling's fail-closed `die 2` (no fall-through) is consistent with the pre-existing PATH branch's own fail-closed treatment; nothing from `bin/codex-capture.sh:292` onward is touched |
| 5 | T5 — devil's advocate | evidence pass | holds | `bin/codex-capture.sh:272`, `bin/codex-capture.sh:279`, `bin/codex-capture.sh:282`, `.shell-team/specs/T-1154-codex-capture-sibling-team-paths.md:95` — multi-hop/cyclic symlinks are an explicitly declared out-of-scope extreme (Input space, out-of-scope item 1) and the single-hop case is airtight (both absolute- and relative-target symlinks verified by AC3's own check line); no macOS/BSD-vs-GNU `readlink` divergence applies here since the script never relies on `readlink -f` or a `-f`-only GNU extension, only a plain `readlink` read inside its own loop |
| 6 | S-AC1 | spec AC1 | holds | `bin/codex-capture.sh:217` (`--reviews-dir` parsed and still wins), `bin/codex-capture.sh:281` (sibling tried first), `bin/codex-capture.sh:284` (PATH fallback only reached when no sibling file), `bin/codex-capture.sh:286` (cwd fallback last); independently re-derived from the diff, not merely accepted from the spec |
| 7 | S-AC2 | spec AC2 | holds | `bin/codex-capture.sh:282` (`bash "$sibling_team_paths" --get reviews`, no `--root`, no `cd` of the process — runs inside a `$(...)` command substitution only), `bin/codex-capture.sh:292` (unchanged tail), `bin/team-paths.sh:77` (`ROOT` defaults to `.`, i.e. the caller's cwd) |
| 8 | S-AC3 | spec AC3 | holds | `bin/codex-capture.sh:271-279` — the `while [ -L ... ]; readlink` loop resolves both absolute (`/*`) and relative symlink targets, entirely through `cd "$(dirname ...)" && pwd` subshells so the process's own cwd never moves |
| 9 | S-AC4 | spec AC4 | holds | `bin/codex-capture.sh:281-283` (a sibling that exists but exits non-zero is `die 2`, never falls through to the `elif` branches below it — confirmed by the `if`/`elif`/`elif`/`else` chain's own structure, not merely by the die message) |
| 10 | S-AC5 | spec AC5 | holds | `bin/codex-capture.sh:292` onward — confirmed by direct reading of the diff hunk (`git diff 3698c136...HEAD -- bin/codex-capture.sh`): the changed region ends exactly at the `[ -d "$reviews_dir" ] \|\| die 2 ` line, nothing after it appears in the diff at all |
| 11 | S-AC6 | spec AC6 | holds | `bin/codex-capture.sh:142-152` (header paragraph now names sibling before PATH, still names cwd and 2026-06-17; `plugin-root-relative fallback` / `deliberately cwd-relative` both removed, confirmed absent by direct read), `bin/codex-capture.sh:289` (single die line naming sibling/PATH/cwd/`--reviews-dir`) |
| 12 | S-AC7 | spec AC7 | not-applicable — this role's own sandbox is read-only and does not execute the fixture suites; QA's hand-off (`.shell-team/todo.md`, QA verdict — T-1154) already reports both suites green live with all six new `PASS: reviews-dir-*` labels present, and this reviewer accepts that live report as the primary source for the runtime claim | n/a |
| 13 | S-AC8 | spec AC8 | holds | `.shell-team/interventions/T-1154.md`, `.shell-team/provenance/T-1154.md`, `.shell-team/specs/T-1154-codex-capture-sibling-team-paths.md`, `.shell-team/todo.md`, `bin/codex-capture.sh`, `tests/codex-skeleton-hygiene/run.sh` — matches the full commit-range diff stat (6 files) measured independently at the top of this round |
| 14 | S-AC9 | spec AC9 | not-applicable — shellcheck was not re-run independently by this role this round (its own sandbox does not need to duplicate a static-analysis exit code QA already reproduced live: `shellcheck bin/codex-capture.sh tests/codex-skeleton-hygiene/run.sh` → exit 0) | n/a |
| 15 | S-AC10 | spec AC10 | not-applicable — same reason as S-AC9; QA's hand-off reports `bash bin/check-adopter-docs.sh` exit 0 with zero bytes on both streams, live | n/a |
| 16 | S-RD | spec `## Review depth` | holds | `bin/codex-capture.sh:271-292`, `tests/codex-skeleton-hygiene/run.sh:537-685` — every item this section calls out for full-depth review (real-invocation death/misresolution, silent success on a failing/absent sibling, symlink + space-in-path resolution, cwd/relative-`--reviews-dir` meaning, the `:266`-onward tail) was independently checked above and holds |
| 17 | S-IS | spec `## Input space` | holds | `bin/codex-capture.sh:271`, `bin/codex-capture.sh:281`, `bin/codex-capture.sh:292` — every reachable class in the spec's list is either covered by an AC1-AC4 check line, the six new fixture cases, or (the bare-PATH-through-`PATH` class) independently verified live by this reviewer to work correctly despite lacking automated coverage (see row 3 / Synthesis audit ledger); every declared out-of-scope synthetic extreme is correctly left untouched |

#### Blockers
- none

#### Major
- none

#### Minor / nits
- **Fixture-completeness gap, no functional defect**: none of the six new `tests/codex-skeleton-hygiene/run.sh` fixture cases exercises "a bare `codex-capture.sh` resolved through `PATH`" — the last item of Input space class 1, which the spec itself declares reachable. QA's own hand-off already disclosed this exact gap (Adversarial fixture synthesis, item 3) and spot-checked it manually. This reviewer independently reproduced the same input class from scratch (a separate live scratch fixture, not a re-run of the engineer's or QA's own probe): a copy of `bin/codex-capture.sh` plus its sibling `bin/team-paths.sh` placed in a directory on `PATH`, invoked bare (`codex-capture.sh --alloc --stem …`, no `bash` prefix, execed via its own `#!/usr/bin/env bash` shebang) from an unrelated adopter cwd, with a decoy `team-paths.sh` placed earlier on `PATH` than the real sibling's directory (to disambiguate "the sibling really won" from "the PATH fallback happened to give the same answer"): the run landed both allocated files in the adopter's own `.shell-team/reviews` and left the decoy directory empty — `BASH_SOURCE[0]` does carry the full PATH-resolved path in this invocation shape, not a bare relative name, so `script_dir` is computed correctly and the sibling wins as designed. No defect exists in the code; the gap is that this reachable class has no committed, repeatable regression fixture — a QA/records-hygiene item under this round's own severity calibration, not a code correctness issue.

#### Adversarial findings
- The evidence pass (T3, see ledger row 3) is the only adversarial finding, and it resolves to the fixture-completeness note above rather than a functional defect, following this reviewer's own independent live reproduction of the input class in question.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | T3 — bare-`PATH`-invocation class untested by the six new fixture cases | adversarial: defect | minor (fast-follow) | Independently reproduced the exact input class live in a fresh scratch fixture (not the engineer's or QA's own probe) with a disambiguating decoy `team-paths.sh` earlier on `PATH`; the code resolves correctly. The finding is real as a fixture/regression-coverage gap but not as a functional defect, and per this round's severity calibration a test-completeness gap that does not invalidate the deliverable is minor at most, not Major. |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE

#### Fast-follow disposition
- Add an automated `reviews-dir-sibling-bare-path` (or similarly named) fixture case to `tests/codex-skeleton-hygiene/run.sh` that invokes `codex-capture.sh` bare through `PATH` (no `bash` prefix) from an adopter-shaped cwd, with a decoy `team-paths.sh` earlier on `PATH`, so this reachable input-space class has a committed regression test rather than resting on a one-off manual QA/reviewer spot-check. Intent: file-an-issue. Rationale: the class is explicitly listed as reachable in the spec's own `## Input space`, was manually verified correct by both QA and this review independently, but currently has zero committed automated coverage — low risk today, cheap to close permanently with one more fixture case, and worth tracking rather than re-verifying by hand on every future touch of this resolver.
