### Codex Review verdict: REQUEST_CHANGES
- Task: T-1073
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 6 files, +1188/-0 lines (`.shell-team/interventions/T-1073.md`, `.shell-team/provenance/T-1073.md`, `.shell-team/specs/T-1073-harness-agent-concurrency.md`, `.shell-team/test-recipe.md`, `.shell-team/todo.md`, `docs/loop-engineering/harness-agent-concurrency.md`) against branch point `f8371eb` (`git merge-base feature/1072-telemetry-span-discriminator HEAD`)

#### Blockers
- none

#### Major
- `docs/loop-engineering/harness-agent-concurrency.md:449-452` — AC16(b)'s frozen text ("the execution-context matrix for every command this task's note and records tell a reader to run … where a command is genuinely repository-local and has no adopter-shaped cell, say so per command rather than as one blanket sentence") is not discharged. The note's `### (b) Execution-context matrix` section instead gives one blanket "Not applicable — no execution-context matrix is owed here" for the whole item, even though the note and `.shell-team/test-recipe.md`'s own new T-1073 entry quote and require running multiple ad hoc repository-local commands (`git merge-base --is-ancestor`, the `echo $(( … ))` arithmetic reproductions, `grep -c`, plus `git show`, the `awk` section extractions and `cmp -s` named in the recipe). This is the identical "compressed/whole-set-claim" defect class QA already found and required fixed twice this task (AC16(f)/(g), round 4) — AC16's own frozen text states "a compressed summary does not discharge it," and a blanket exemption is exactly that. Suggested fix: enumerate each such command individually and state "repository-local, no adopter-shaped cell" per command, matching AC16(b)'s own escape hatch, rather than exempting the section as a whole. This is a small, engineer-fixable, no-re-probe rework — the same shape as round 4's own fix for the sibling sub-items.

#### Minor / nits
- `docs/loop-engineering/harness-agent-concurrency.md:350` — the `clock-source-monotonic` licence condition is marked `met` using `python3 time.time_ns()` (wall-clock epoch nanoseconds). Live-verified on this host (`python3 -c 'import time; print(time.get_clock_info("time")); print(time.get_clock_info("monotonic"))'`): `time` → `clock_gettime(CLOCK_REALTIME)`, `monotonic=False, adjustable=True`; `monotonic` → `mach_absolute_time()`, `monotonic=True, adjustable=False` — a true monotonic source was available on the probe host and not used. The note's own text at this line already discloses the gap honestly ("verified in-practice monotonic … disclosed as wall-clock epoch time rather than a dedicated CLOCK_MONOTONIC read"), so this is not a concealment. Given the measured overlap margins are ≈172–270 s, a wall-clock adjustment large enough to spuriously produce that margin across three arms and two repetitions in a ~24-minute single-session probe is implausible, so this does not plausibly flip the `concurrent-overlapping` conclusion. Per this review's frozen scope ("record-prose wording = notes unless it invalidates the deliverable"), this is a note, not a gate.
- `docs/loop-engineering/harness-agent-concurrency.md:27` — the byte-locked `## Probe evidence` section records one templated watchdog-launch command (`bash ~/.claude-dotfiles/scripts/agent-watchdog.sh <that-agent's-transcript.output> "" 5 20 5`) standing in for all nine healthy-arm watchdog launches, rather than nine distinct lines. The frozen protocol text (Goal, "Watchdog liveness evidence") says "each watchdog's launch command verbatim." AC13's own text explicitly licenses recording the path home-relative rather than home-absolute (a deliberate, disclosed PII accommodation — not a defect), but the further compression of nine launches into one template is a residual gap against the protocol's literal "each … verbatim," and the criteria assigned to this row in the spec's own coverage table (AC7, AC13) do not mechanically enforce per-instance distinctness — a pre-existing coverage gap in the frozen criteria rather than something engineer- or QA-introduced. Does not touch any computed value or the verdict.
- `.shell-team/specs/T-1073-harness-agent-concurrency.md:96` and the board's `refreeze-class (v1→v2)` record — independently re-verified: `git show 6e424a1:.shell-team/specs/T-1073-harness-agent-concurrency.md` hashes to `5fe2d557998f29ae3bf11913416cabd4e0396bc4` (matches the recorded `old-hash`); `bash bin/check-refreeze-class.sh --old-hash 5fe2d557998f29ae3bf11913416cabd4e0396bc4 <v1> <HEAD>` reports `mechanics: … differing=1`, matching the board record exactly; `diff` confirms the *only* difference between v1 and HEAD is AC3's own check-line constant, both occurrences of `-ge 10` → `-ge 7`. Both halves of the re-freeze verification hold: every differing line is a `- check:` line, and the replacement (`-ge 7`) still asserts a meaningful lower bound that matches the frozen region's actual measured size (7 non-blank lines, independently re-confirmed). The disclosed residual — AC3's own prose still reads "at least ten lines" against a check that now asserts `-ge 7` — is real and already disclosed on the board as a pending one-word class-B fix ("ten"→"seven"), explicitly withheld from this mechanics-only re-freeze per the class boundary (a mechanics delta may touch check lines only). This residual does not gate: the check's semantic intent (assert the region isn't truncated) is unchanged and still correctly enforced at the corrected, accurate constant; only the prose numeral lags. Judgment: the class-M re-freeze is sound and correctly classified.
- Evidence-anchor ordering (interventions entry 3, `docs/loop-engineering/harness-agent-concurrency.md` `## Probe evidence`) — noted for the record since the interventions entry explicitly invites review judgment on this point, and this reviewer does **not** overrule it. Chronologically, the board's current `probe-evidence-sha` (`954e2eeff0699f27d3cfb4084d9583e955210105`) postdates engineer round 2's own commit (`cba49d2`), which read the prior evidence commit (`88a5e0e`) — so the literal claim "the evidence is committed before the engineer is invoked" is not exactly true for the one string this redaction touched. However: the redaction is orchestrator-authored (not engineer-authored), isolated to exactly one non-measurement string (a `git clone` command's source-path argument), changes zero measurement bytes, re-runs zero arms, and both engineer round 3 and QA independently confirmed via `awk`-isolated `diff`/`cmp` that the evidence sections differ by that one line only. The frozen "invalid-evidence rule" bounds *re-probes* (re-executions changing measurement bytes); a PII-mandated string substitution with no re-execution is a different category, and AC2's own mechanical check (byte-identity between the working tree and whatever the board currently anchors) is satisfied and has been re-verified at every subsequent round. Judgment: the orchestrator's "not a second re-probe" ruling is sound; this is a documented ordering imprecision in the prose, not a violation that should stop the task.

#### Adversarial findings
- Attacked the `concurrent-overlapping` inference directly (time-slicing vs. genuine overlap): the note discloses 8 logical cores (`os.cpu_count()`) and that per-agent durations stretch under contention (serial ≈158.2 s → pair ≈177–185 s → width-n4 ≈285–289 s, "under this arm's own four-way contention"). This growth is exactly the expected signature of genuinely overlapping CPU-bound processes contending for shared cores — **not** evidence against concurrency. The decisive discriminator is the `batch-vs-sum` channel: `width-n4`'s batch wall-clock (≈283.7 s) is ≈25% of the four-agent serial sum (≈1147.8 s) — a time-sliced-but-labeled-concurrent hypothesis predicts batch ≈ sum, which this data flatly contradicts. Own re-derivation of all margins (`echo $(( … ))` on the note's own `first=`/`last=` digits) matches the note's stated `margin_ns` values exactly for all three overlap-bearing arms. Conclusion: the evidence robustly supports `concurrent-overlapping` over a serialized/time-sliced alternative; no overclaiming found in the note's own wording, which stays scoped to the closed five-value vocabulary term rather than asserting a stronger physical mechanism.
- Re-ran the clock-source and evidence-anchor-ordering questions with a second, independently-prompted Codex pass (adversarial framing): both were reaffirmed as real, disclosed, non-conclusion-flipping gaps — see Minor findings above — and the second pass additionally live-verified the `CLOCK_REALTIME`/`mach_absolute_time` distinction on this host, strengthening (without escalating the severity of) the clock-source note.
- Sibling-note byte-identity and scope lock independently re-verified: `git diff --stat f8371eb...HEAD -- docs/loop-engineering/agent-concurrency.md docs/loop-engineering/phase-multiplexing.md` is empty (zero bytes changed in either sibling); the branch diff touches no `bin/`, `tests/`, `skills/`, `agents/`, `templates/`, `.github/` or `README*` path.
- The `## Implications for T-1074` table matches the frozen spec's own implications prose verbatim in substance (concurrent-overlapping → #277 precondition 2 MET, remaining gates #285/#274 named rather than waved at; both umbrellas stay open, this task closes neither).
- The negative-control in-window alert proof (file `mtime` at epoch 1786790322, 5 s before the agent's own end second 1786790327, well inside the agent's running window) is arithmetically sound and the 5-second margin comfortably exceeds `mtime`'s 1-second resolution; no finding.
- The three-miss / one-permitted-re-probe / redaction-only-amendment history is fully recorded across 3 interventions entries, the board's freeze-attestation/refreeze-class/intent-ratified lines, and the note's own `## Limits`/`## AC16` sections; no undisclosed residual found beyond the AC3 prose numeral already covered above.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Evidence anchor `954e2ee` postdates engineer round-2 commit `cba49d2` | primary: blocker (P1, "keep pre-engineer evidence immutable") | minor | redaction touches zero measurement bytes, is orchestrator-authored, isolated to one non-measurement string, independently `cmp`/`awk`-audited by both engineer round 3 and QA; the frozen invalid-evidence rule targets re-probes (re-executions), not PII-mandated string substitution — orchestrator's "not a second re-probe" ruling judged sound on inspection |
| 2 | `clock-source-monotonic` licence condition uses wall-clock `time.time_ns()`, not a true monotonic read | primary + adversarial: blocker (P1, "cannot be marked met") | minor | gap is honestly disclosed in the note's own prose; measured margins (≈172–270 s) vastly exceed any plausible wall-clock step in a ~24-minute single-session probe, so the conclusion direction is not at risk — a note, not a gate, per this review's frozen scope |
| 3 | One templated watchdog-launch command stands in for nine distinct healthy-arm launches | primary: major (P2, "record actual watchdog launch commands") | minor | AC13 explicitly licenses home-relative (not home-absolute) recording for PII reasons — that half is not a defect; the further nine-into-one compression is a pre-existing gap in the frozen criteria's own mechanical coverage (AC7/AC13 do not enforce per-instance distinctness), not an engineer- or QA-introduced defect, and touches no computed value |
| 4 | AC3's positive-control prose still says "at least ten lines" against a check now asserting `-ge 7` | primary: major (P2, "match AC3's guard to the frozen region") | minor | independently re-verified: `check-refreeze-class.sh` confirms `mechanics … differing=1`, the diff is exactly the two `-ge 10`→`-ge 7` occurrences, and the corrected constant (7) matches the region's actual measured size — already disclosed on the board as a pending, deliberately-withheld one-word class-B fix; the check's semantic intent is unchanged and correctly enforced at the accurate constant |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` T-1073 back to `READY_FOR_ENG` with the AC16(b) finding above as the linked rework item (enumerate each repository-local command individually rather than one blanket "not applicable" sentence — no re-probe, no measurement change, no board/evidence edit required; the same narrow shape as round 4's own fix for AC16(f)/(g))

#### Fast-follow disposition
- clock-source-monotonic gap (Minor #2 above) — file-an-issue: suggest that any future probe of this shape (T-1073's own sibling investigations, or a later re-run) use a genuine monotonic clock read (e.g. `time.monotonic_ns()`) for agent-timestamp evidence, kept alongside wall-clock epoch time for human-readable correlation, rather than wall-clock epoch time alone. Rationale for not fixing in this task: this task's evidence section is byte-locked and already frozen under the invalid-evidence rule; the gap does not plausibly affect this run's conclusion given the size of the measured margins, so re-probing solely to change clock source would not be worth invalidating already-sound evidence.
- watchdog-launch-command template compression (Minor #3 above) — won't-fix: the byte-locked raw evidence already commits to one template line per protocol design choice made at probe time; the nine launches are structurally identical modulo the transcript-path argument, so re-probing solely to spell out nine near-duplicate lines would change no measurement and isn't worth invalidating already-sound evidence. Worth carrying into a future probe protocol's authoring as a lesson: require distinct per-instance lines from the start if this shape recurs.

---

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1073
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: engineer round 5 (`d22cbbc`), 2 files, +102/-9 lines (`.shell-team/todo.md`, `docs/loop-engineering/harness-agent-concurrency.md`); reviewed against the closure claim for round 1's sole gating Major

#### Closure check on round 1's Major (AC16(b) blanket exemption)
Substantially, genuinely closed: the blanket "Not applicable" sentence is gone; Categories A–D (18 rows) now give real per-row execution in both contexts, including a live spot-check that separates a tool's own portability from one invocation's repo-local arguments (Category D's `bin/derive-populations.sh`, matching this sprint's own `bin/derive-populations.sh` precedent). This is genuine progress, not a re-wording of the same exemption — confirmed by direct read of `git diff 2141932..d22cbbc -- docs/loop-engineering/harness-agent-concurrency.md`.

However, auditing the matrix for cells that are **reasoned rather than executed** (this round's explicit charge) surfaces a new, independent Major: the fix does not fully generalize to every category.

#### Major
- `docs/loop-engineering/harness-agent-concurrency.md:522-534` (Category E, 4 rows) and `:536-545` (Category F, 3 rows) — these two categories drop the per-row `Context (ii)` column entirely and replace it with **one category-level blanket prose sentence** before each table ("context (ii): **not applicable** — ..."), reverting to the exact pattern AC16(b)'s frozen text forbids ("say so per command rather than as one blanket sentence"). This is not defensible as "identical reasoning stated once": Category E's four rows do **not** share one reason — the `git clone`+checkout is repository/history-local, the `bash bin/check-acs.sh` row combines a genuinely portable, shipped `bin/` tool with repository-local arguments (the same tool-vs-argument distinction Category D correctly drew for `bin/derive-populations.sh`, and incorrectly omits here), and the two watchdog rows are **operator/session-local** (`~/.claude-dotfiles/scripts/agent-watchdog.sh` is the operator's own dotfiles script, outside this repository entirely) — a different non-applicability class than "repository-local," which AC16(b)'s escape hatch does not textually cover. Independently confirmed by a second, adversarially-prompted Codex pass, which additionally flagged that Category F is "unambiguously in scope because `test-recipe.md` directs QA to perform its three commands," making the blanket exemption there indefensible as well. Suggested fix: give every row in E and F its own `Context (ii)` cell and reason; for the `check-acs.sh` row specifically, actually execute it bare-name against an adopter-shaped fixture spec (mirroring Category D's own treatment of `derive-populations.sh`) rather than folding it into the same-shape watchdog/clone rows.
- `docs/loop-engineering/harness-agent-concurrency.md:520` — Category D's second row (`bin/derive-populations.sh --label t1073-r4-spec-populations ...`) states its `Context (ii)` disposition as "same disposition as the row above" — reasoned by analogy from row 1's independently-executed test, not independently executed itself. Lower severity than the E/F finding (the underlying claim — same tool, same repo-local-argument shape — is very likely correct and the reasoning is sound), but it is a genuine "reasoned, not executed" cell in a round whose entire premise was "every cell executed rather than reasoned about."

#### Minor / nits
- `docs/loop-engineering/harness-agent-concurrency.md:449-452` — the stated population total ("**27** distinct commands") is off by one. Independently counted (and independently confirmed by both a Codex primary pass and my own direct count): Categories A–F hold **11 + 5 + 3 + 2 + 4 + 3 = 28** table rows, not 27. The uncounted row is Category C's own third row (the "Round 3 addendum" `git merge-base --is-ancestor 88a5e0e... 954e2ee...` check) — it is genuinely executed and correctly tabulated, but its source (`docs/loop-engineering/harness-agent-concurrency.md:179-180`, free prose inside "Round 3 addendum") does not use the `- command:` or `- reproduce:` grammar the counting methodology (`grep -c` on those two prefixes, plus a fixed "3 recipe commands") scans for — a genuine blind spot in the counting method itself, missed independently by both the engineer and QA's own recount (QA round 3's commit message also states "27... matching exactly"). All 28 rows are genuinely present and individually executed; nothing is hidden from the reader — only the roll-up arithmetic label is wrong. Suggested fix: correct "27" to "28" and its 2/3/4/18 decomposition to reflect Category C's 3 rows.
- `docs/loop-engineering/harness-agent-concurrency.md:342` (span-population disclosure, 12 vs. 10) — judged honest and sufficient: the underlying "strict subset" claim is correctly shown to survive at both 10 and 12, and the disclosure proactively flags that the raw `grep -c` count is not stable over time rather than leaving a future reader to discover this unexplained. One small overclaim (also flagged independently by Codex): the sentence states a future reader "will see" a larger number — this asserts more certainty than is available (a later span could use a different `iteration` value, or the run could end); "may see" would be accurate without weakening the disclosure's substance.
- The four Minors from round 1 (evidence-anchor ordering, `clock-source-monotonic`'s wall-clock-vs-monotonic gap, the templated watchdog-launch commands, and AC3's "ten"-vs-"seven" prose residual) are **unchanged** — confirmed via `git diff 2141932..d22cbbc` touching only the `(b)` section and one board line. All four dispositions stand as previously reasoned; none is folded into this round's fix (consistent with the coordinator's own note that the engineer read them and judged none trivially foldable into this round's narrow scope). No new information changes any of the four judgments.

#### Pre-commitment trigger — explicit statement, flagged rather than resolved
The frozen Pre-commitment's factual trigger ("two consecutive review rounds landing an independent Blocker or Major against the same component") is, on the letter of its own text, **satisfied this round**: round 1's Major and this round's new Major both land against the same `## AC16(b) Execution-context matrix` component, and the trigger's text carries no probe-safety qualifier limiting it to arm-level findings. A second, adversarially-prompted Codex pass reached the same reading independently and added: the trigger's own defined disposition (drop `plugin-role-confirmation`, then `width-n4`) does not logically fit a documentation-reporting defect, and is additionally hard to apply as written — `plugin-role-confirmation` never ran (nothing to retroactively drop) and `width-n4` already ran and is part of the never-dropped N=2-adjacent measured record. But the disposition's poor fit does not, by the frozen text's own wording, cancel the trigger, and the final instruction ("defeated twice, the task stops and returns to planning rather than being patched a third time") remains literally in force. **This reviewer does not decide this question**: whether the arm-dropping mechanism was ever meant to reach a documentation-completeness component (as opposed to the probe-safety findings it was written for, per the Goal section's own history), and whether continuing to a round-6 rework requires an explicit operator amendment first, is a task-disposition call outside this review's scope (this review gates the deliverable's correctness, not the loop's own continuation rules). Flagged here explicitly, prominently, and by name so the orchestrator/operator resolves it before any further engineer round starts — not silently narrowed away by an engineer or reviewer choosing the convenient reading.

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` T-1073 back to `READY_FOR_ENG` **only after** the pre-commitment trigger question above is resolved by the orchestrator/operator (per the frozen rule's own "contextual trigger: before a third round of rework on that component starts"). If continuation is authorized: fix (1) Categories E/F's blanket-sentence reversion with genuine per-row `Context (ii)` dispositions (correcting the `check-acs.sh` tool-vs-argument omission), (2) Category D's second row's reasoned-not-executed cell, and (3) the 27→28 population count — all three are narrow, no-re-probe, no-measurement-change fixes.

#### Fast-follow disposition
- AC3's "ten"→"seven" prose residual (round 1, still open) — file-an-issue: track the pending one-word class-B correction explicitly so it is not silently lost across further rounds; it does not gate on its own (the check's semantic intent is already correctly enforced at the accurate constant).
- Clock-source-monotonic wall-clock gap (round 1) — file-an-issue (already declared in round 1; unchanged, restated here for continuity since the coordinator asked for final intents).
- Watchdog-launch-command template compression (round 1) — won't-fix (already declared in round 1; unchanged, restated here for continuity).
- Span-count disclosure's "will see" → "may see" wording (this round) — won't-fix: a one-word precision nit with no substantive effect; fold into whatever round next touches this file rather than tracking separately.

---

## Round 3

### Codex Review verdict: APPROVE
- Task: T-1073
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: engineer round 6, operator-authorized surgical round (`8adff83`), engineer's own delta isolated as `git diff 235e429..8adff83 --stat` = 2 files, +112/-30 lines (`.shell-team/todo.md`, `docs/loop-engineering/harness-agent-concurrency.md`); plus the separately-ratified class-B re-freeze `235e429` (spec `v2→v3`, AC3 prose only)

#### Closure check on round 2's Major (Categories E/F blanket-sentence reversion) and Minor (Category D row 2 analogy)
Both genuinely closed, confirmed by direct read of `git diff 235e429..8adff83 -- docs/loop-engineering/harness-agent-concurrency.md` and by a third, independently-prompted Codex pass:
- **Category E** (4 rows) and **Category F** (3 rows) now each carry their own per-row `Context (ii)` cell — the category-level blanket sentence is gone from both. The `check-acs.sh` row's tool-vs-argument split is now genuinely **executed on both halves**: bare-name `check-acs.sh` against an adopter-shaped fixture spec in a scratch dir with `bin/` on `PATH` → exit `0`, `AC1: PASS`; the literal repository-specific argument (`.shell-team/specs/T-1044-test-infra-bundle.md`) tried verbatim in the same dir → `ERROR: cannot read spec file`, exit `2` — a real, executed refusal, mirroring Category D's own precedent for `bin/derive-populations.sh` exactly. Category F's `awk` boundary and `cmp -s` rows are likewise genuinely executed against fresh scratch fixtures (a heading-less file; two arbitrary files), not merely asserted.
- **Category D's second row** is now independently executed with its own distinct failure shape (`awk` finds no heading in a nonexistent file → empty `grep '^-'` input → exit `1`, tripping the tool's pipefail refusal) — genuinely different from row 1's failure (`git ls-files` exit `128` outside a git repository) — closing round 2's Minor. The board's `engineer round 6` sub-bullet and `qa-verdict round 4` both independently re-ran this exact invocation and matched.
- **Population count**: corrected 27→28, with the grammar-miss explanation (Category C's third row sourced from free prose, not the `- command:`/`- reproduce:` grammar the original count scanned for) and a tool-derived re-count (`bin/derive-populations.sh`, `union: items: 28`, byte-verified against a fresh live re-run per this sprint's own T-1071 norm). Independently re-verified by this reviewer: `11+5+3+2+4+3 = 28`, matching exactly.

**One residual noted, not gating, and not new**: Category E row 4's `Context (i)` cell still reads "same disposition as the row above" rather than an independently-reported execution (flagged by this round's Codex pass). On inspection this is not a fresh gap — it is the same, already-dispositioned round-1 Minor (the byte-locked evidence section's own templated watchdog-launch command, `docs/loop-engineering/harness-agent-concurrency.md:27-30`) surfacing again in the matrix's restatement of that same evidence, not an independent defect the operator's nine-cell authorization was scoped to touch (the nine cells were E's four `Context (ii)` cells, F's three `Context (ii)` cells, D row 2's `Context (ii)` cell, and the count correction — never E row 4's `Context (i)`). Re-running the identical watchdog script against a second, equally-nonexistent placeholder transcript path is very unlikely to produce a materially different result (unlike Category D's two rows, whose distinct arguments trigger genuinely different internal validation paths in `bin/derive-populations.sh`), so treating this as a blocking gap would be disproportionate — especially against a task that already required one operator override past the frozen pre-commitment's own stop-after-two-rounds rule. Carried forward under the same fast-follow disposition as round 1's original watchdog-template Minor (below).

#### Pre-commitment trigger — confirmed correctly resolved and attributed
The board's `operator ruling` sub-bullet resolves round 2's flagged question explicitly and correctly: it states the factual trigger "had fired literally while its arm-drop disposition did not fit a reporting defect, as the review itself noted," and records an explicit **operator override** ("c GO, class-B GO") authorizing exactly **one** further surgical round, scoped to the nine cells named, past both the loop-guard cap and the frozen pre-commitment's no-third-patch clause. This is attributed correctly — an operator decision, not a decision this review, QA, or the engineer made unilaterally — and is bounded (one round, explicitly scoped, no standing extension). QA round 4's own record independently reaches the same reading ("this was resolved by the operator's explicit … ruling … which is the correct locus for that call — not this QA pass, and not the engineer"). No further action needed from this review on this question; nothing here suggests the override was applied more broadly than authorized (the engineer's own delta touches only the two files and the nine named cells, confirmed by QA's own scope audit and by this reviewer's direct read of the diff).

#### AC3 prose residual (round 1) — confirmed resolved
Independently re-verified: the spec's AC3 prose now reads "at least seven lines" (`grep -n 'at least ten lines' .shell-team/specs/T-1073-harness-agent-concurrency.md` → zero matches), matching the already-corrected check constant (`-ge 7`) from the v1→v2 class-M re-freeze. `bash bin/check-intent.sh --print-hash` on the current spec → `880c0ee374240c8f49cbeaf0867b51828049e692`, matching the board's `intent-hash (v3)` line exactly. The board's v2→v3 ledger is correctly shaped for a class-B repair: one `intent-hash (v3)` line, one `intent-ratified` line with `class=B`, three `freeze-attestation` records (v1/v2/v3), and **zero** new `refreeze-class` bullets (only the pre-existing v1→v2 class-M one remains) — consistent with this repo's own convention that a prose-only fix gets no refreeze-class classifier record. No longer a fast-follow item; fully closed.

#### Minor / nits
- Nothing new beyond the Category E row 4 residual already addressed above (folded into the pre-existing round-1 watchdog-template Minor rather than treated as a separate item).

#### Nothing gates
Restating explicitly, per this round's charge: **nothing in this deliverable still gates merge.** Round 1's Major (AC16(b) blanket exemption) and round 2's Major (Categories E/F reversion) are both genuinely closed; round 2's Minor (Category D row 2 analogy) is closed; the AC3 prose residual is closed; the pre-commitment trigger question is resolved and correctly attributed to an explicit, bounded operator override. The three remaining Minors below are notes, not gates, and were judged non-gating in rounds 1–2 on grounds that still hold (none touches a computed value or the `concurrent-overlapping` conclusion's direction).

#### Recommendation
- APPROVE → set `.shell-team/todo.md` T-1073 to `READY_FOR_MERGE`

#### Fast-follow disposition (final intents)
- Evidence-anchor ordering (round 1, `## Probe evidence`, redaction-only amendment `954e2ee` postdating engineer round 2's `cba49d2`) — **won't-fix**: the orchestrator's "not a second re-probe" ruling was judged sound in round 1 and re-confirmed here; zero measurement bytes changed, the change is isolated to one non-measurement string, and re-litigating a settled, independently-audited (engineer round 3 + QA + this review) redaction would not improve the deliverable.
- Clock-source-monotonic wall-clock gap (round 1, `docs/loop-engineering/harness-agent-concurrency.md:350`, `time.time_ns()` vs. a true `CLOCK_MONOTONIC`/`time.monotonic_ns()` read) — **file-an-issue**: track as a methodology improvement for any future probe of this shape (this task's own evidence is byte-locked and the measured margins, ≈172–270 s, are far too large for this gap to have affected the `concurrent-overlapping` conclusion, so it is not worth re-probing this task's own evidence for).
- Watchdog-launch-command template compression (round 1, `docs/loop-engineering/harness-agent-concurrency.md:27-30`, one templated line standing in for nine healthy-arm launches; recurring in this round as Category E row 4's `Context (i)`) — **won't-fix**: the byte-locked raw evidence already commits to this shape from the original probe session; independently re-running the same script against a second, equally-nonexistent placeholder path is very unlikely to reveal new information, and re-probing solely for this completeness gap is disproportionate. Worth carrying as a lesson for any future probe protocol's authoring: require one distinct, independently-executed line per watchdog instance from the start.
