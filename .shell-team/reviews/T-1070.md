# Codex Review — T-1070

### Codex Review verdict: APPROVE
- Task: T-1070
- Codex model: gpt-5.6-sol (config.toml `model`, `model_reasoning_effort = "high"`; no per-run override passed)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests — `bin/check-acs.sh` was run against the frozen spec (13/13 PASS, 1 SKIP by design), `tests/check-handoff/run.sh`, `tests/close-out/run.sh`, `tests/errexit-safe/run.sh`, `tests/check-board-headings/run.sh` and `tests/machine-tokens/run.sh` were all run to completion, `shellcheck bin/check-handoff.sh tests/check-handoff/run.sh bin/check-board-headings.sh` was run directly, and the live board was timed against `bin/check-handoff.sh` — none of this was taken from the hand-off's report.
- Diff scope: `git diff --stat aa28e9a...HEAD` (branch point `chore/lesson-promotion-2026-08-15`'s tip, per this task's own stacked-branch discriminator) — 15 files changed, 1026 insertions(+), 3 deletions(-): `bin/check-handoff.sh` (+35/-3, the two real edits), `bin/check-board-headings.sh` (one comment line), `docs/loop-engineering/check-handoff-scaling.md` (new, 448 lines), six new fixtures, `tests/check-handoff/run.sh` (additive), plus records (`.shell-team/todo.md`, `.shell-team/test-recipe.md`, `.shell-team/provenance/T-1070.md`, `.shell-team/interventions/T-1070.md`, the spec itself).

#### Blockers

None.

#### Major

None.

#### Minor / nits

- `tests/check-handoff/run.sh`'s `active-eof.md` assertion (the block ending `PASS: T-1070 active-eof — a strand as the last line of a file with no closing heading is still reported (exit 1, line 15)`) greps only for `:15: stranded continuation line`, but the fixture actually produces **three** separate strand violations (lines 15, 16 and 17 — confirmed by running `bash bin/check-handoff.sh tests/check-handoff/fixtures/active-eof.md` directly). The comment's own claim — "a strand **as the very last line in the file**... must still be reported even though nothing follows it" — is about line 17, the actual EOF line, but the assertion checks line 15 instead. The exit-code assertion (`rc -eq 1`) does cover the file as a whole, and this is pre-existing D4 strand semantics this task does not touch (each stranded continuation line is its own violation, unchanged), so nothing here is wrong — the assertion is just not as tight as its own prose implies. Not gating.
- One-space transcription nit inside the note's verbatim quote of issue #256's body (already disclosed by QA round 2 and re-confirmed here, not new).

#### Adversarial findings

Codex's own investigation (22 executed commands under `--sandbox read-only`, git-diff read, fixture-suite run, shellcheck run, spec/AC cross-check, and a secrets scan of the diff) went beyond the spec's own AC4 locale corpus: it fuzzed the blank-test equivalence (`${content//[[:space:]]/}` empty-test vs. the new `[[ "$content" =~ ^[[:space:]]*$ ]]`) against **10,000 random multi-character strings** drawn from the full Unicode codepoint space, and separately against **every single Unicode codepoint classified as whitespace or Unicode category `Z*`**, under `LC_ALL=en_US.UTF-8` — zero mismatches in either sweep. This is a strictly larger equivalence class than AC4's own U+3000/U+00A0/ASCII corpus and found no divergence, which is meaningfully stronger evidence for the "no verdict change" claim than the spec's own criterion requires. No adversarial pass on specific files was triggered per this role's own contract, since no Blocker/Major was found in round 1 to trigger it.

Independent verification performed by this role beyond re-reading Codex's output: re-derived the AC1 dominant-stage arithmetic (1027/1137 = 90.3% ≥ 50%) and the AC3 scaling-gradient arithmetic (done axis 4.00x bytes → 1.74x before-runtime, sub-linear; active axis 3.84x bytes → 23.69x before-runtime, super-linear; after-fix both axes flat) by hand from the note's own pasted numbers, ran `bash bin/check-acs.sh` against the frozen spec myself (13 passed / 0 failed / 1 skipped, matching the hand-off's own report), ran the fixture suite, `shellcheck`, `check-board-headings`, `check-pii-shapes`, `check-provenance`, `check-interventions` myself, and timed the live board directly (`bash bin/check-handoff.sh "$BD"` → 0.027s wall, consistent with the note's claimed after-arm figures). Read the full diff of `bin/check-handoff.sh` line by line against the base blob: the awk `exit` replacing the flag-clear preserves `NR`-based original-file line numbers (awk's `NR` is untouched by `exit`), preserves the no-`## Active` fallback (the exit branch is only reachable once `in_active` is true, so a file that never opens the section is read to EOF exactly as before), and preserves the `!seen` single-Active rule even more strongly than before (the program terminates entirely once the first Active section closes, so a second `## Active` heading is never even reached). Read the blank-test replacement and confirmed it is an anchored whole-string match against the same `[[:space:]]` bracket expression the old substitution used, not a byte-oriented rewrite — the locale-dependent verdict is preserved by construction, and this is now confirmed empirically well beyond the spec's own corpus (see Adversarial findings above). Manually inspected all six new fixtures (`no-active.md`, `active-last.md`, `empty-active.md`, `active-eof.md`, `dup-active.md`, `long-line.md`) and confirmed each exercises the section-structure shape its name claims and produces the exit code / stderr content the suite asserts.

#### Synthesis audit ledger

no severity changes

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE

#### Fast-follow disposition
- `tests/check-handoff/run.sh`'s `active-eof.md` assertion checks line 15 rather than the actual last line (17) its own comment describes — won't-fix (the fixture and exit-code assertion already cover the regression this fixture exists to guard; tightening the grep to also check line 17 is optional test polish with no behavior at stake, not worth a tracked issue).
