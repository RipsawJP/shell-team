### Codex Spec-Review verdict: REQUEST_CHANGES

## Spec review

**Mode**: Specify-seam spec-review (T-1092), round 1. No implementation exists yet on `feature/1112-parallel-default-and-executor-hosting` (HEAD `03db804`) — this reviews the frozen intent block (`## Goal` through `## Input space`) of `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md`, plus the board's `**T-1112**` declaration region, per the Routing Map's `spec-review — cross-provider` election for this task.

- Task: T-1112
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` (full 234-line intent block through Input space); `.shell-team/todo.md`'s `**T-1112**` entry (lines 16-27); `docs/loop-engineering/phase-multiplexing.md:343-392` (the dispatch-axis/rule lines this task flips, read at HEAD to confirm the spec's base-state claims); `templates/prompt-blocks/fanout-orchestration.md` (full file, read at HEAD); `templates/prompt-blocks/dispatch-record.md` (full file, read at HEAD); `README.md:174`, `README.ja.md:173-175`; `.shell-team/interventions/T-1109.md` (entries containing the "three consecutive elections" and the deviation-notice ruling, read first-hand to check AC6's fidelity).
- raw-capture: `T-1112-codex-specreview.jsonl` / `T-1112-codex-specreview.txt`
- executor-invocation: `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Fast, targeted spec review. [...seven attack items, full text in the raw capture...] Return findings as a JSON array of objects: {severity, criterion, issue, suggestion}." > "<RAW_JSONL>" 2>&1` (prompt elided here for readability only — a first attempt with an exhaustive full-file-read prompt was run twice and killed by the harness's own Bash-call timeout before Codex could emit a final message, once at 590000ms and once at the tool's 600000ms ceiling, both times with Codex still mid-read of `.shell-team/interventions/*.md` in full; the prompt was narrowed to named `sed`-range reads and a stated ban on executing the spec's own `- check:` lines, which completed in-budget on the third attempt. This narrowing is disclosed because it is why this round's own reading of the interventions files went through the spec's quotations of them rather than a fresh independent read by Codex itself — the primary-source cross-check against `T-1109.md` for AC6's fidelity below was done by me, not delegated to Codex.)

### Independent primary reading (before consulting Codex's output)

Read first-hand, before opening Codex's output: the spec end to end (234 lines); the current (pre-freeze) state of `docs/loop-engineering/phase-multiplexing.md:343-392`, `templates/prompt-blocks/fanout-orchestration.md` (full), `templates/prompt-blocks/dispatch-record.md` (full), `README.md`/`README.ja.md`'s opt-in bullets, and the board entry — confirming the spec's own factual claims about "what exists today" are accurate (the opt-in sentences, the agent-shaped `:355` precondition, the `no phase is rewired`/`N instances of one role` literals, the seven invariant-lock ids, and the dispatch grammar's closed value sets all read exactly as the spec's `## Summarized sources` describes, measured directly rather than trusted from the spec's own quotation).

**AC6 fidelity check against primary interventions bytes** (attack item 6). `.shell-team/interventions/T-1109.md` entry 2 (`:12-13`) reads: "the un-discharged `:375` follow-up was load-bearing for three consecutive elections and should have been discharged as its own item instead of cited" — this is the primary-source ground for the spec's first briefing rule ("citing the same unverified precondition three or more times without discharging it is itself verification avoidance and makes that precondition its own backlog item"). The same entry's summary records the host's verbatim rejection of a "recommended serial continuation" after "nam[ing] the recurrence itself as the defect" — the primary-source ground for the second rule ("a recommendation issued immediately after the operator names a recurrence of a ruled class leads with the corrected default rather than continuing as-is"). Both rules are faithful restatements of what the primary source actually says; neither invents a new process gate. This closes attack item 6 with no finding.

**Tier re-derivation (independent of the spec's own `## Notes for engineer`).** Applying `CONTRIBUTING.md`'s joint test: headline test — met (the measured ~92 min against a ~6h serial projection on a 113-member population is a real, nameable operational improvement on the phase every task in this loop waits on). Default-reachability test — met: the thing that moves is the shipped default itself (`tech-lead` applies `phase-multiplexing.md`'s dispatch rules at every Plan, in every repository loading this plugin), and the correction is not optional prose-only — the adopter-facing "Opt-in only" sentence in both READMEs has to be deleted because it becomes false the moment this lands, which is exactly the "ships but stays latent behind an opt-in" case `CONTRIBUTING`'s PATCH bullet excludes once the latency itself ends. **I concur with the spec's own MINOR reading.** The stated PATCH counter-reading (benefit latent behind orchestrator practice) is weaker here than it was for T-1110's own contested PATCH call, because unlike that case, an adopter-facing shipped claim is being falsified and corrected in the same diff — that is a stronger default-reachability signal than T-1110 had, not a weaker one. The sprint's approved v2.4.1 PATCH premise is genuinely broken by this reading, consistent with what the board and interventions record already show fired and was resolved by the operator (v2.5.0, continuation approved, detection-timing defect routed to retro) — I re-derive the same tier independently rather than take that resolution as given, and it holds.

### Codex's independent findings (raw JSON in `T-1112-codex-specreview.txt`)

Codex read the spec and the named line ranges of the four grounding files itself (a narrower read-set than my own, per the prompt-narrowing disclosed above) and returned seven findings, all P1/P2 in its own vocabulary.

1. **[P1]** `AC1` — the two trigger literals (`not constructible in-session`, `authoring threshold`) are checked for presence anywhere in the file, never anchored to the actual `trigger:` line; the rule also doesn't forbid extra triggers or define measurable thresholds.
2. **[P1]** `AC4` — checks a bag of disconnected substrings (`coordinating session`, `never sub-agent-hosted`, `execution-host`, `<repo-root>`, etc.) anywhere in the file rather than one scoped rule binding them together; launch-record field placement, normalization and the judgment-work exclusion are unverified as a coherent unit.
3. **[P1]** `AC3` — removing the exact agent-shaped phrase doesn't prevent an equivalent agent-concurrency gate reworded differently; requiring the `judgment` substring doesn't prove the depth-axis question stays genuinely undecided.
4. **[P1]** `AC8` — since the bound is "at most 2 non-blank lines changed," a compliant edit can delete the false opt-in sentence and insert the required `execution-host`/`verify-mechanism` tokens elsewhere in the file while leaving the actual bullet's own surrounding prose ambiguous or silent about the new default.
5. **[P1]** `AC2`/`AC7`/`AC9` — none of the three preservation locks protects non-target `- dispatch-rule:` lines (specifically, `implement`'s three rule lines); an edit could change an out-of-scope axis's rule value while every stated lock still passes.
6. **[P2]** `AC6` — the two briefing-rule sentences are themselves faithful restatements of the cited rulings (Codex did not have my primary-source cross-check but reached the same conclusion from the spec's own text), but the check itself is loose substring presence rather than an anchored sentence pattern.
7. **[P1]** `AC1-AC13` — the non-empty positive controls block the simplest empty-extraction vacuous pass and the ten expected-red/three expected-green criteria are not mutually contradictory, but `AC5` can pass on unchanged content (it checks containment, not new content), `AC11` doesn't require an actual `END` marker or a real regenerated-output comparison, and `AC12`/`AC13` validate a bare digit-run or token rather than the sweep's real output or a real recipe append.

### My own synthesis of Codex's findings

I independently re-verified every finding above directly against the spec's own embedded `- check:` bodies (not against Codex's paraphrase) before scoring severity. Two calibration notes govern the severities below: (a) this task's own launch brief asks for **full adversarial depth on the rule text and the README claim change**, and only **value-correctness depth on inline check lines** in general — so a finding that is really about whether the shipped *rule text* (in `phase-multiplexing.md` or the READMEs) will read as intended is held to a higher bar than a finding that is purely about a check body's own substring looseness; (b) per this repository's own methodology, almost every AC in this entire spec corpus verifies via literal/substring grep rather than a semantic parser — a finding that says "a substring check cannot prove an arbitrary rewording is absent" is an inherent, corpus-wide limitation, not a defect unique to this spec, and I do not escalate that generic critique to Major on its own.

Applying that split:

- **Finding 1 (AC1, vacuous-pass) → Major.** Verified directly: the check extracts `$T/base` and `$P` for the byte-identity clauses, but the two trigger-literal existence checks (`grep -qF -- "$s" "$P"`) search the *whole file*, never the extracted trigger line (`$T/rh`-style extraction is not used here, unlike AC3). A compliant edit could place a well-formed-looking `trigger: ` clause on the rule line while the two required literals live anywhere else in the document (a `dispatch-note`, a parenthetical, even this spec's own future citation of itself once merged prose references it) — this is not a generic substring-looseness complaint, it is a specific failure to anchor the assertion to the object the criterion's own prose says it verifies ("The new conditional's triggers are real rather than nominal"), which is exactly what item 7's mandate calls an instrument defect rather than a completeness gap.
- **Finding 5 (no lock on `implement`'s own rule lines) → Major, and the more serious of the two.** Verified directly: AC1's sibling byte-identity clause extracts only `^- dispatch-rule: verify — ` and `^- dispatch-rule: verify-fixture — ` lines; no criterion anywhere in AC1-AC13 ever extracts or byte-locks `^- dispatch-rule: implement — `. Given the diff-scope criterion (AC10) allows edits to `docs/loop-engineering/phase-multiplexing.md` at all (it is a required-touched file), nothing in this criteria set would go red if the `implement` axis's own three rule lines were silently rewritten in the same edit — directly contradicting the Non-goals' "only the `verify-mechanism` refinement flips" commitment with no mechanical check standing behind it.
- **Finding 2 (AC4, disconnected substrings) → Major**, per the full-adversarial-depth calibration on rule text this task asked for: the execution-host rule is exactly the kind of rule-text object the launch brief singled out, and the check genuinely cannot distinguish "one coherent host rule stating the operator's ruling" from "the same seven-odd tokens scattered non-adjacently across the file to satisfy grep." AC4 does at least anchor the `audit-only` clause to `execution-host` co-occurring on one line (the `$T/ao` extraction) and does protect the seven invariant-lock ids and the launch-record grammar tokens verbatim — so the finding is real but narrower than Codex's framing suggests; I keep it Major rather than Blocker because the surrounding launch-record grammar (which the rule must sit beside) is independently protected.
- **Finding 4 (AC8, README honesty gap) → Major**, the highest-priority item per this task's own attack list (item 4 names this exact class first). Verified directly: the bounded-rewrite clause (`m -le 2`) plus the required-token clauses (`execution-host` present, `verify-mechanism` strictly more frequent) do not require the replacement text make an affirmative statement of the new default anywhere. A change that deletes the opt-in sentence, inserts `execution-host` and one extra `verify-mechanism` mention in a technically-correct but easy-to-miss spot, and leaves the bullet's remaining prose reading as neutral or ambiguous about whether fan-out is now the default would pass AC8 as written. This is precisely the "deletion alone leaves a misleading silence" risk the launch brief names, on the one surface (README) held to full adversarial depth.
- **Finding 3 (AC3, reworded-gate risk) → Minor**, downgraded from Codex's P1. Verified: unlike AC1, AC3 *does* extract the specific `- recommendation: tier1-verification-fanout — ` line into `$T/rh` and checks all five required literals against that extraction specifically, so it is properly anchored to the correct line (Codex's framing understates this). The residual risk — a reworded-but-equivalent agent-gate elsewhere in the file — is the generic substring-verification limitation named in the calibration note above, not a defect unique to AC3, and value-correctness depth is the right bar here.
- **Finding 6 (AC6, loose substring check) → Minor**, consistent with the value-correctness-depth calibration for inline check lines; the underlying rule text itself is faithful to the primary sources (see the fidelity check above), so this is a check-hardening suggestion, not a domain-premise defect.
- **Finding 7 (AC5/AC11/AC12/AC13 instrument softness) → Minor**, each individually: AC5's containment-only check is a genuine gap only if the mirrors could stay unchanged while still discharging AC5 — but AC5 is jointly read alongside AC4/AC6, whose own required literals are absent from the base blobs of the mirrored consumers, so a no-op edit could not simultaneously satisfy AC4/AC6 and leave AC5 vacuously true; the risk is real only in isolation, not compositely. AC11's missing `END`-marker requirement and AC12/AC13's bare-token validation are legitimate fast-follow hardening items but do not on their own let a false claim survive freeze, since AC14(d)/(h)'s reporting duties (SKIP-by-design but read by the human/QA chain) require the sweep's real output to be narrated regardless of what AC11-AC13's own grep can distinguish.

**One additional finding of my own, not raised by Codex**, from the mutual-exhaustiveness half of attack item 1: the rule-*design* (as opposed to AC1's check) is structurally exhaustive and non-overlapping by construction of `dispatch-record.md`'s own unchanged Precedence rule ("apply the most specific conditional trigger... fall back to the unconditional default only when no conditional trigger applies") — with exactly one conditional (`serial`) and one unconditional default (`tier1-fanout`) on this axis, every case is covered and no ambiguity about which rule wins can arise. This part of item 1 is **not** a finding; it is confirmed sound. The trigger wording itself, however (`authoring threshold` with no stated number, `not constructible in-session` with no named operational test), is judgment-laden prose a future election could read either way — **Minor**, a drafting nit for the freeze author rather than a blocking defect, since the same qualitative-threshold style is used throughout this note's existing (unedited) `implement` axis triggers too.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC1 trigger literals not anchored to the rule line | Codex: P1 | Major | verified directly against the check body — genuine vacuous-pass risk, not a generic substring limitation |
| 2 | AC4 disconnected-substring binding | Codex: P1 | Major | full-adversarial-depth calibration applies to rule text; check does not bind the tokens into one coherent rule |
| 3 | AC3 reworded-equivalent-gate risk | Codex: P1 | Minor (downgraded) | AC3 is correctly anchored to the recommendation line, unlike AC1; residual risk is the corpus-wide substring-verification limitation, held to value-correctness depth |
| 4 | AC8 README honesty gap | Codex: P1 | Major | top-priority item per this task's own attack list; verified the bounded-rewrite clause has no affirmative-statement requirement |
| 5 | No lock on `implement`'s own dispatch-rule lines | Codex: P1 | Major | verified no criterion in AC1-AC13 ever extracts `implement`'s rule lines; most severe finding, since it is a silent-scope-escape with zero mechanical detection |
| 6 | AC6 loose substring check | Codex: P2 | Minor | underlying rule text verified faithful to primary sources (T-1109.md); check-hardening only |
| 7 | AC5/AC11/AC12/AC13 instrument softness | Codex: P1 | Minor | AC5's gap is not compositely exploitable given AC4/AC6's own requirements; AC11-AC13 gaps are real but covered downstream by AC14(d)/(h)'s human-read reporting duties |
| 8 | Trigger wording vagueness (`authoring threshold`, `not constructible in-session`) | mine (not raised by Codex) | Minor | qualitative-threshold style matches the note's existing unedited `implement`-axis triggers; drafting nit, not a blocking defect |

### Route-back

**REQUEST_CHANGES routes to `pm-spec`.** Resolved off the board's own `- entry-mode: pm-authored` field (`.shell-team/todo.md`, T-1112 entry, first sub-bullet) — a closed, unambiguous value in the `pm-authored`/`operator-authored` pair, read directly rather than inferred. **Disclosed limitation**: this round could not cross-check that field against `tech-lead`'s own Routing Map `specify`-axis decision text, because that Plan-time output was not part of this round's provided context and no `- dispatch: specify — …` board sub-bullet exists yet at this pre-freeze stage (consistent with this mode always firing before the intent-hash is recorded, per this role's own operating contract) — recorded as `entry-mode-crosscheck: environmentally-unverified` rather than silently assumed. The `- entry-mode:` value itself is unambiguous and is what this round routes on.

### Fast-follow disposition

- AC3's reworded-equivalent-gate risk (Minor #3) — won't-fix at this spec: the same substring-verification limitation applies corpus-wide and is not unique to this criterion; not worth a bespoke fix here.
- AC6's loose substring check (Minor #6) — won't-fix at this spec: the underlying rule text is verified faithful; hardening the check body is optional polish, not required before freeze.
- AC5/AC11/AC12/AC13 instrument softness (Minor #7) — won't-fix at this spec: each gap is either non-compositely-exploitable (AC5) or backstopped by AC14's human-read reporting duties (AC11-AC13); a future spec-authoring hardening pass, not this one.
- Trigger wording vagueness (Minor #8) — won't-fix at this spec: matches the note's own existing unedited style; a drafting nit for the freeze author to consider, not a filed issue.


---

### Codex Spec-Review verdict: APPROVE (round 2)

## Spec review — round 2

**Mode**: Specify-seam spec-review (T-1092), round 2 (converging). Still pre-implementation on `feature/1112-parallel-default-and-executor-hosting` (HEAD `2c4f268`, prior review at `03db804` / `c8e0967`). Subject is the round-2 rework of `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` against round 1's four Major findings.

- Task: T-1112
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `git diff c8e0967..2c4f268 -- .shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` (58-line diff, single file); `.shell-team/reviews/T-1112.md` round-1 record (73 lines, read in full before scoring); the current spec's `- [ ] **AC1**`–`**AC14**` block (`sed -n '57,100p'`) and Non-goals (`sed -n '44,56p'`); direct shell verification of the AC4 example host-rule sentence's seven literals and the AC8 JA affirmative literal's non-substring status against the base blob (run independently, not delegated to Codex).
- raw-capture: `T-1112-codex-specreview-r2.jsonl` / `T-1112-codex-specreview-r2.txt`
- executor-invocation: `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "$(cat <prompt-file>)" > "<RAW_JSONL>" 2>&1 < /dev/null` (prompt held to four named `sed` ranges plus the round-1 review record, explicitly forbidding a broader read and forbidding execution of any `- check:` line, after round 1's exhaustive-read prompts twice hit the harness timeout; completed clean in one foreground pass this round, `exit_code:0`/`status:"completed"` in the JSONL trailer, ~299k input tokens mostly cache hits)

### Independent verification of each claimed discharge (check bytes read directly, not the spec's own prose summary)

1. **Major 1 (AC1 trigger literals unanchored) — verified fixed.** The check now does `grep -- '^- dispatch-rule: verify-mechanism — serial — conditional — trigger: ' "$P" > "$T/tr"`, asserts `grep -c . "$T/tr"` = `1`, and only then searches the two trigger literals inside `$T/tr` rather than inside `$P`. A trigger clause whose literals live in a `- dispatch-note:` or elsewhere no longer satisfies it. Genuinely closed.
2. **Major 2 (no lock on `implement`'s own rule lines) — verified fixed.** **AC2** now additionally extracts `grep -- '^- dispatch-rule: ' | grep -v -- '^- dispatch-rule: verify-mechanism — ' | sort` from both base and `HEAD` and `cmp`s the two sorted sets, with a positive control requiring at least one `- dispatch-rule: implement — ` line survive in the base-side extraction. Order-invariant (sort neutralizes reordering) and content-sensitive (cmp catches any rewrite). Traced the filter against both the two base-side and two head-side `verify-mechanism` line forms — all four are excluded by the same anchored prefix, so the set comparison never accidentally includes or races against the axis this task is flipping. Genuinely closed, and the fix generalizes to any axis a future note adds without rewriting this criterion.
3. **Major 3 (AC4 disconnected substrings) — verified fixed.** `grep -F -- 'execution host' "$P" > "$T/hr"` (spaced, distinct from the `execution-host` field name), asserted to be exactly one line, then all seven bound literals (`verify-mechanism`, `coordinating session`, `harness-tracked background shell slice`, `isolated staged worktree`, `never sub-agent-hosted`, `until a pilot`, `single point of failure`) are required within that one extraction. Independently verified the spec's own offered example sentence ("**The execution host.** A mechanism-class fan-out … runs as N harness-tracked background shell slices launched by the coordinating session over isolated staged worktrees … and is never sub-agent-hosted: … as a single point of failure, and stays prohibited until a pilot measures it.") does contain all seven literals on one physical line by running the literal greps against it directly — genuinely writable, not just asserted writable.
4. **Major 4 (AC8 deletion-only bypass) — verified fixed.** For each README, `grep -F -- "$a" "$f" | grep -F -- 'execution-host' | grep -F -- 'verify-mechanism' | grep -c .` requires the affirmative literal (`fan-out by default` / `既定で fan-out`) and both field tokens on the *same* physical line, with the affirmative literal separately asserted absent from the base blob. Verified the JA literal `既定で fan-out` is genuinely zero at base (the base sentence carries the intervening `この` — `既定でこの fan-out` — so it is not a hidden substring match) by running the grep against the actual base sentence text directly. A pure-deletion edit with tokens dropped in elsewhere no longer trivially satisfies this — see residual note below.
5. **Minor 6 (AC6 loose substrings) — verified fixed** to the same one-line co-location pattern as items 1–4 above (two separately-extracted single lines, each required to carry its sibling literals).
6. **Minor 7 / AC11 self-match — verified fixed, and confirmed the defect was real.** Read the AC11 bullet's own prose: it quotes `` `<!-- BEGIN derivation: t1112-read-set -->` `` and `` `- reproduce: bash bin/derive-populations.sh --label t1112-read-set` `` verbatim, inline, inside its own single long paragraph line — the round-1 form's unanchored `grep -qF` over the *whole file* could therefore self-satisfy against the criterion's own text before the derivation block was ever written. The round-2 form switches to `grep -cxF` (whole-line exact match) for the markers and `^-`/`^<!--` anchors for the field lines and region extraction; since the AC11 bullet's own line does not consist *solely* of the marker text (it's embedded mid-sentence), `-x` no longer matches it, and the `^- reproduce: …` anchor doesn't match a line that starts with `- [ ] **AC11**` instead. Traced this by hand against the actual bullet text rather than taking the disposition's own diagnosis on faith. Genuinely closed.
7. **Minors 3/8 and AC5/AC12/AC13 disposed, no change** — confirmed the diff touches none of AC3, AC5, AC12, AC13's check bytes, consistent with "disposed on round-1's own grounds, not re-litigated."
8. **Tier — unchanged, correctly not relitigated.** No line in this round's diff touches `## Notes for engineer` or any tier-derivation text; the settled MINOR reading from round 1 (and the operator's `feedback_premise-break-is-approval-lapse` continuation ruling) stands untouched. Check honesty confirmed: nothing here re-argues or re-derives a settled question.
9. **Scope — confirmed spec-only.** `git diff c8e0967..2c4f268 --stat` shows exactly one file, `.shell-team/specs/T-1112-parallel-default-and-executor-hosting.md` (38 insertions, 20 deletions). Nothing else in the rework moved.
10. **Mutual satisfiability of AC1/AC2/AC4/AC8/AC11 — confirmed.** AC1's rule-text edit is confined to the two `verify-mechanism` lines, which AC2's filter explicitly excludes from its preserved set, so the two criteria cannot contradict each other. AC4's host-rule sentence and AC8's README bullet target disjoint files. AC11 targets this spec's own Blast-radius section, disjoint from the other four. Ran the AC4 example sentence and the AC8 JA literal through actual `grep` rather than eyeballing it (see fragments above) — both hold.

### Codex's independent findings (raw JSON in `T-1112-codex-specreview-r2.txt`)

Codex was given the same round-1-to-round-2 diff, the round-1 record, and the reworked AC1–AC14 block (via named `sed` ranges only, per the narrowed prompt) and asked specifically whether each of the four claimed fixes genuinely closes its finding, whether the rework introduces any new gap, and whether the reworked criteria are mutually satisfiable. It returned two findings, no others:

1. **[major, per Codex]** `AC8` — "the triple-grep only requires a matching line somewhere in each README, not the intended Within-phase fan-out bullet; it also accepts a negated sentence."
2. **[minor, per Codex]** `AC4` — "the audit field itself is not anchored as a `- execution-host:` field, and `<repo-root>` is checked anywhere."

Codex explicitly did not flag any gap in the AC1, AC2, or AC6/AC11 fixes, and did not report a mutual-satisfiability contradiction among AC1/AC2/AC4/AC8.

### My own synthesis of Codex's two findings

- **Finding 1 (AC8, Codex: major) → downgraded to Minor.** Re-verified against the actual README source (`README.md:174`, `README.ja.md:174`): each bullet is written as **one single continuous physical line** (bold header, opt-in claim, and everything else run together with no hard wrap), the same authoring convention this corpus uses throughout (matching AC4's own host-rule design). Rewriting *that* line — as the spec's own `- adopter-surface:` disclosure explicitly instructs — to replace the opt-in claim with the affirmative statement naturally produces the header, the affirmative claim and the three required tokens all on the one line the bullet already occupies; satisfying the co-location clause by instead inserting an unrelated new sentence elsewhere in the file would be a deliberately contrived edit visible on inspection of the diff, not a natural or cheap way to satisfy the check the way round 1's actual gap (zero enforcement of any affirmative statement, anywhere) was. The negation-immunity half of the finding ("it also accepts a negated sentence") is the same corpus-wide literal/substring-verification limitation round 1's own Minor-3 disposition already named and declined to escalate on its own — every literal-presence check in this corpus is symmetrically blind to polarity. I concur a residual gap exists (the check does not pin the co-located line to the specific bullet by e.g. also requiring the `Within-phase fan-out` / `フェーズ内 fan-out` header token), but it is materially weaker than round 1's actual Major-4 defect (complete absence of any affirmative-statement requirement) and does not clear the bar for another round. Recorded as a fast-follow, not a blocker.
- **Finding 2 (AC4, Codex: minor) → confirmed Minor, unrelated to this round's convergence target.** Directly tested the audit-only/`execution-host` co-location logic against a synthetic positive and negative case (`grep -F 'audit-only' | grep -qF 'execution-host'` genuinely does enforce same-line co-location — Codex's framing overstates this half of its own finding). The genuine residual is narrower than Codex states: the co-located line is not required to actually be a `^- execution-host:` field declaration (prose containing both substrings would pass), and `<repo-root>` is checked file-wide rather than tied to that field. This softness predates round 2 — round 1 never flagged it, and it is orthogonal to the Major-3 finding round 2 was tasked with discharging (that finding was about the *rule* sentence, not the *metadata field's* own normalization convention). Confirmed Minor, fast-follow.

No Major or Blocker survives. Both residual findings are check-instrument softness on already-fixed criteria, of the same character round 1's own calibration note already established should not gate this repeatedly-converging round.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC8 co-location not pinned to the specific bullet; immune to negation | Codex: major | Minor | each README bullet is one physical line by this corpus's own convention, so satisfying the check by natural rewrite of that line is the cheap path and a contrived misplacement is visible in the diff; negation-immunity is the same corpus-wide literal-check limitation round 1's Minor-3 already declined to escalate |
| 2 | AC4 audit-only line not required to be an actual `- execution-host:` field declaration; `<repo-root>` unanchored | Codex: minor | Minor | co-location itself is genuinely enforced (verified by direct test); the field-syntax gap predates round 2, is orthogonal to the Major-3 rule-sentence fix this round targeted, and was never escalated in round 1 either |

### Route-back

Not applicable — this round's verdict is **APPROVE**, not `REQUEST_CHANGES`; no route-back is triggered.

### Fast-follow disposition

- AC8's co-location-not-pinned-to-bullet gap (Finding 1) — won't-fix at this spec: the file's one-line-per-bullet convention already makes the natural, cheap edit the compliant one; pinning the check to the `Within-phase fan-out` / `フェーズ内 fan-out` header token would close the theoretical gap fully but is optional hardening on a surface already reviewed by the standard `user-visible: yes` README-diff review path.
- AC4's audit-only field-syntax / `<repo-root>` anchoring gap (Finding 2) — won't-fix at this spec: pre-existing softness orthogonal to this round's convergence target; a future spec-authoring hardening pass, not this one.

**Recommendation**: APPROVE — round-2 rework genuinely discharges all four round-1 Major findings and Minor 6/7's AC11 sub-part; freeze may proceed on the current spec text.

---

### Codex Review verdict: APPROVE

- Task: T-1112
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 13 files, +665/-11 lines (`git diff --stat dd7ce8154b3bcf3a23530075959286f802728e60...HEAD`)

**Mode**: delivered-change review, round 1 (distinct axis from the two Specify-seam spec-review rounds above; QA already returned PASS at `READY_FOR_REVIEW`). Read directly: `docs/loop-engineering/phase-multiplexing.md`, `templates/prompt-blocks/fanout-orchestration.md` + its mirror `skills/run/SKILL.md`, `templates/prompt-blocks/dispatch-record.md` + its mirrors `agents/tech-lead.md`/`skills/run/SKILL.md`, `README.md`/`README.ja.md`, `.shell-team/test-recipe.md`, the spec, the board entry (declarations, dispatch rows, version-derivation, freeze-attestation, engineer hand-offs), and both interventions entries. `raw-capture`: `T-1112-codex-primary.jsonl` / `T-1112-codex-primary.txt` (narrow, targeted prompt — the first full-diff attempt exceeded the tool's 590s foreground budget mid-read and was killed; the raw capture and cleanup blocks for that aborted attempt were discarded per the skeleton's diagnose/cleanup discipline before the successful narrower run).

#### Independent version-tier re-derivation
Applying CONTRIBUTING's headline test and default-reachability test jointly, independently of the board's own row: **headline reads met** (mechanism-class verification sweeps now run in parallel by default with a recorded execution host — a real, adopter-felt wall-clock benefit on the phase an adopter waits longest for) and **default-reachability reads met** (the thing that moves is the shipped default itself; both READMEs' "Opt-in only" sentence had to be deleted because it stopped being true). Derived tier: **MINOR**. This matches the board's own `- version-derivation (v1, 2026-09-01): verdict=match — derived=MINOR` row against the operator-amended v2.5.0 premise. Concur — no further deviation notice owed.

#### Blockers
(none)

#### Major
(none)

#### Minor / nits
- `docs/loop-engineering/phase-multiplexing.md:370` — Codex flagged the new `verify-mechanism — serial — conditional` trigger ("slice-level isolation … not constructible in-session", "fan-out authoring threshold") as not carrying a numeric/audited measurement. Downgraded from Codex's own `major` on review: this qualitative phrasing is the pre-existing house convention this same document already uses, byte-for-byte, in the unedited sibling `verify — serial — unconditional` and `verify-fixture — serial — unconditional` rules ("too small for fan-out overhead to be worth authoring", locked byte-identical by AC2) — this document is Plan-time reasoning prose a human/tech-lead judgment reads, not a machine-executed numeric gate, and this task did not introduce or worsen the pattern.
- `docs/loop-engineering/phase-multiplexing.md:355` — Codex flagged that the edited `recommendation: tier1-verification-fanout` line still says the recommendation is "gated … on the `per-instance-telemetry-discriminator` invariant … carried as a follow-up" while `verify-mechanism`'s own `tier1-fanout` rule is now unconditional, with no visible trigger check for that invariant. Downgraded from Codex's own `major`: the runtime gate is not this prose line but `bin/check-fanout-instances.sh`, already shipped (by earlier T-1074/T-1076 work, unedited here) and unconditionally enforced on every fan-out regardless of execution host — it refuses any fan-out whose telemetry rows lack a conforming discriminator, so no fan-out can proceed undetected even though this specific sentence's own "follow-up" framing looks stale next to that shipped enforcement. The staleness itself pre-dates this task (the "carried as a follow-up" clause survives from base, T-1112 only removed the now-discharged agent-launch half) and is not a functional gap — fast-follow, not a blocker.
- `docs/loop-engineering/phase-multiplexing.md:366`/`:367` — the unedited (AC2-locked) `verify`/`verify-fixture` dispatch-rule trigger clauses still read "both of `recommendation: tier1-verification-fanout`'s own named preconditions … exist", but the recommendation line this task edited now names only one. Purely textual: since the discriminator precondition is undischarged, "both … exist" still correctly evaluates false and those two axes correctly stay on `serial`, so there is no live behavioral divergence — but the cross-reference is confusing to a future reader and worth tidying in a follow-up.
- `README.md:174` / `README.ja.md:174` — both mirrors (consistent with each other) still carry "using fan-out for other verification work is still an explicit per-task choice made inside the Validate phase", inherited byte-identical from base (confirmed via `git show dd7ce81:README.md`/`README.ja.md`). This is in tension with `skills/run/SKILL.md`'s own later "Where this fits above" cross-reference, which places the actual decision at `tech-lead`'s Plan step, transcribed onto the board at the Specify-to-Implement seam — Validate merely applies the recorded choice. Pre-existing (not introduced by this task, and out of this task's own narrowly-scoped README edit, confirmed by AC8's own ≤2-line incidental-diff tolerance) — fast-follow.

#### Adversarial findings
None triggered — no Blocker/Major survived synthesis, so the opt-in adversarial pass (step 5) was not run, consistent with this repo's own opt-in trigger.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `:370` serial-trigger not machine-auditable | Codex: major | minor | identical qualitative phrasing pre-exists byte-locked in sibling `verify`/`verify-fixture` rules this task did not touch; this is Plan-time judgment prose, not a CI-executed gate |
| 2 | `:355` discriminator precondition looks ungated on the now-unconditional branch | Codex: major | minor | the actual runtime gate is the already-shipped, unconditionally-enforced `bin/check-fanout-instances.sh`, not this prose sentence; no functional gap demonstrated, staleness pre-dates this task |
| 3 | `:366`/`:367` "both" preconditions cross-reference now stale | Codex: minor | minor | textual only — the stale wording still evaluates correctly (discriminator undischarged → condition false → axes stay serial), no behavior change |
| 4 | README "chosen inside Validate" vs. actual Plan-time decision point | Codex: minor | minor | byte-identical inheritance from base per `git show dd7ce81`; outside this task's own narrowly-scoped README edit |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE

#### Fast-follow disposition
- Finding 2 (`:355` stale "carried as a follow-up" framing next to the already-discharged runtime discriminator enforcement) — file-an-issue: worth a small doc-accuracy pass across `docs/loop-engineering/phase-multiplexing.md`'s own follow-up bullets so a reader doesn't have to cross-reference `skills/run/SKILL.md`/`bin/check-fanout-instances.sh` to learn a "follow-up" is already shipped.
- Finding 3 (`:366`/`:367` stale "both preconditions" cross-reference) — won't-fix at this spec: no behavioral divergence, byte-locked by this task's own AC2 scope, and cheap to fold into the same doc-accuracy pass as Finding 2 rather than opening separately.
- Finding 4 (README "chosen inside Validate" vs. Plan-time decision) — file-an-issue: adopter-facing accuracy gap, pre-existing, worth a small follow-up correcting both README mirrors to name the Plan-time decision point `skills/run/SKILL.md` itself already documents.
- Finding 1 (`:370` qualitative trigger phrasing) — won't-fix: matches this document's own established, unedited convention across all three fan-out axes; not specific to this task's diff.
