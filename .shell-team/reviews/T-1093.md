# T-1093 — Codex Reviewer records

## Spec review

### Codex Spec-Review verdict: REQUEST_CHANGES
- Task: T-1093
- Codex model: gpt-5-codex
- Mode: Spec-review (specify seam, T-1092), first live invocation
- Consumer class: adopter-executed prompt surfaces and an adopter-facing declaration grammar — shipped-surface depth (the dev-scaffold re-pricing in the operator's local config does not apply)
- Input reviewed: `.shell-team/specs/T-1093-verification-ceiling.md` (frozen intent block + declaration region, read in full including Input space, Blast radius, Open questions, Verdict scope), cross-read against `.shell-team/runs/t1093-routing-map.md`. No `--base`, no branch diff — this pass runs before any implementation exists.
- issue-canon note: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — the mandate's item 2 turns partly on whether the spec's acceptance criteria test what issue #333 actually asked for; this role holds no GitHub-reading tool. The spec's own `## Assumptions` records that the coordinating session already read issue #333 live via GitHub MCP at freeze time (state open, body verbatim-identical to the relayed text), which is the primary confirmation this note defers to.

#### Domain-premise findings (Codex primary pass, normalized)

1. **Major** (mandate item 1 — Q1 vocabulary adequacy). The closed `unit-and-static` / `real-environment` pair is spec-level, but a spec's criteria can span more than one real-environment capability class at different cost (e.g. one criterion needs only a staging storage→queue→worker path, actually exercised; another needs a production deploy or credentialed work, not exercised). The grammar's own generic wording ("under **each** criterion that sits above the declared ceiling") does not obviously exclude an `- above-ceiling:` sub-bullet under a `real-environment`-declared spec, but the Input-space's own worked example for the `real-environment` reachable class states unconditionally that "criteria that would sit above a unit-and-static ceiling sit below this one" — i.e. it reads as an all-or-nothing declaration with no room for a spec that exercised *some* real capability classes but not others. As written, a spec facing this mixed case has no honest way to declare: staying `unit-and-static` would misclassify the criterion that genuinely *was* exercised in a real environment as merely human-owned above-ceiling work, and declaring `real-environment` overstates coverage of the criterion that was not exercised. This is exactly the class the spec's own Verdict scope pre-declares in scope for a Major ("a closed pair presented as closed while the prose admits a third reading" / declaration boundedness) — it does not ask for a third ceiling value (which the Input space correctly declines) and is not covered by that decline.
   - Route-back ask: either state explicitly (in the Goal's grammar paragraph and in the Input-space's `real-environment` bullet) that `- above-ceiling:` sub-bullets remain available under a `real-environment` declaration for any criterion whose actually-reached capability falls short of what it names, and correct the one Input-space bullet that currently reads as all-or-nothing; or, if the team judges this combination does not arise for this repository's own tasks, add that reasoning as an explicit Non-goal/residual the way Q1's other edge cases already are, rather than leaving the gap silent.

2. **Minor** (mandate item 1 — staleness between freeze and QA execution). A declared ceiling can go stale if credentials, environments or reachable runtime paths change between first freeze and QA's actual run. Downgraded from Codex's raw severity: this is a property already shared by every first-freeze declaration this loop ships (`base-ref-discriminator`, `verification-class`, `user-visible`), all of which the loop's existing class-B/class-M re-freeze mechanism already covers; nothing about `verification-ceiling` makes this task's own premise more exposed than its siblings.

3. **Minor** (mandate item 2 — AC2/AC4 test literal presence, not behavior). A spec/engineer could satisfy every required literal in the `pm-spec` checklist bullet or the run-skill gate while still misapplying the substance (inferring a value, skipping the mismatch judgment, gating at the wrong lifecycle point). Downgraded: the spec's own frozen text already discloses this as residual (iii) ("this criterion needs capabilities above the declared ceiling is a reading judgment, not a machine-checkable state") and residual (ii) ("the read itself is verified by nothing") — a known, disclosed limitation rather than a hidden one.

4. **Minor** (mandate item 2 — AC5/AC6 provenance). A non-empty PASS field and a "same line" rule do not prove QA actually read the spec or that the board append matches. Downgraded for the same reason as #3 (residual (ii) already covers this); substantially overlaps with finding 3.

5. **Minor** (mandate item 3 — "green up to" record readability). `above it: none` risks being read as "no remaining work" rather than "no work above the declared ceiling," and a bare `real-environment` value does not say which capability class was exercised. This is a fair, cheap-to-fix wording concern for `agents/qa-verifier.md`'s rule text (state explicitly that below-ceiling work is what the green flag actually verified) rather than a structural defect in the record design.

6. **Minor** (mandate item 4 — trusted-principal residual). Largely restates residuals already disclosed in the spec's own Input space and Non-goals (dishonest declaration and unverified-read are named and scoped there, with the stated reasoning that this repository's operator/coordinating-session/agents are trusted principals and that the declaration raises the cost of a mistake rather than of deliberate misstatement). The forward-looking caution that future automation must not treat the field as an authorization to waive verification is reasonable but speculative — this task builds no such automation and makes no claim that it does. Recorded as a fast-follow note, not a defect in this spec.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | real-environment partial-capability-class ambiguity | Codex: P1 (blocker per normalization map) | major | spec's own Verdict scope pre-classifies declaration-boundedness gaps of this shape as in-scope-for-Major, not blocker; not a request for a third value so the Input space's decline of that class does not apply |
| 2 | declaration staleness vs QA execution timing | Codex: P2 (major per normalization map) | minor | generic property of every first-freeze declaration gate this loop ships, already covered by the existing class-B/class-M re-freeze mechanism |
| 3 | AC2/AC4 assert literals, not behavior | Codex: P1 (blocker per normalization map) | minor | already disclosed by the spec's own residuals (ii)/(iii); a known limitation, not a hidden one |
| 4 | AC5/AC6 no provenance proof | Codex: P1 (blocker per normalization map) | minor | same disclosed residual (ii); overlaps finding 3 |
| 5 | "above it: none" / bare value ambiguity for a later reader | Codex: P1 (blocker per normalization map) | minor | valid wording-clarity suggestion, cheaply fixable at implementation, not a structural defect |
| 6 | trusted-principal residual too broad | Codex: P1 (blocker per normalization map) | minor | largely restates already-disclosed residuals (i)/(ii); the future-automation caution is speculative and outside this task's stated scope |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (per the spec-review-mode Route-back paragraph: pm-spec's own `- entry-mode:` hand-off field for this task was not independently readable from a persisted artifact at this pre-intent-hash seam — it is a live hand-off value, not yet committed to a file — so per the fail-closed rule this routes to pm-spec by default; this also agrees with the Routing Map's own printed `specify — pm-authored` decision, so the two sources do not disagree on the destination even though only one was directly readable).
- Ask: close finding 1 (Major) before the freeze proceeds — either extend/clarify the grammar and the Input-space `real-environment` bullet to confirm per-criterion `- above-ceiling:` sub-bullets remain available under a `real-environment` declaration, or add an explicit, reasoned Non-goal if the team judges the mixed-capability-class case does not arise for this repository's own tasks.
- This APPROVE/REQUEST_CHANGES gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- Finding 6 (trusted-principal residual / future-automation caution): won't-fix for this task — the spec makes no claim that this declaration authorizes waiving verification and builds no automation that reads it that way; worth restating as an explicit caution if a future task ever builds automation consuming `- verification-ceiling:` programmatically, but not a defect in this spec.
- Findings 2, 3, 4, 5 (Minor): won't-fix as blocking items for this freeze — each is either a property already shared by the loop's existing first-freeze gates (finding 2) or an already-disclosed residual (findings 3, 4), or a cheap wording improvement pm-spec/engineer may fold into the same revision that addresses finding 1 (finding 5) rather than requiring a separate round.

Raw Codex output: `.shell-team/reviews/T-1093-codex-specreview.txt` (final message), `.shell-team/reviews/T-1093-codex-specreview.jsonl` (full event stream).

---

### Codex Spec-Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1093
- Codex model: gpt-5-codex
- Mode: Spec-review (specify seam, T-1092), round 2
- Consumer class: adopter-executed prompt surfaces and an adopter-facing declaration grammar — shipped-surface depth (unchanged from round 1)
- Input reviewed: `.shell-team/specs/T-1093-verification-ceiling.md`, current working-tree state (still uncommitted; intent-hash still not recorded), re-read in full — the Goal's grammar paragraph, Non-goals, both halves of Input space, Q1, and Verdict scope. No `--base`, no branch diff.
- Round-2 scope, per the coordinator's own framing: are round 1's Major and Minor 5 genuinely closed by pm-spec's edits, and did those edits introduce any new domain-premise defect.

#### Round-1 Major — closed
The grammar paragraph now states the semantics symmetrically for both declared values ("the declared value states what the gate reaches for every criterion not marked above it... the `- above-ceiling:` sub-bullet marks per-criterion exceptions under **either** declared value"); the Input space gained a dedicated "mixed case" reachable-class bullet with the exact staging-vs-production worked example; the previously all-or-nothing `real-environment` example bullet was corrected to state it applies only when *every one of* the spec's criteria is reached; the mismatch case is now generalized to either value; the out-of-scope bullet now explicitly distinguishes a declined "competing per-criterion **value**" from a reachable "per-criterion **exception**"; and Q1 in Open questions absorbs the clarification with a note that this was the round-1 finding. Four acceptance criteria (AC2, AC4, AC5, AC7) now lock the literal `either declared value` into the actual shipped prompt/skill/doc text, so the semantics cannot be silently dropped at implementation. Codex's independent read agrees: "Closed. Per-criterion exceptions now explicitly apply under either declared value, with a concrete mixed-case class and corresponding AC locks." I confirm: **closed**.

#### Round-1 Minor 5 — closed
The record's documented shape now pins the explicit empty-case phrase `none above this ceiling` (never a bare value, never a bare `none`) alongside `at or below` language stating what the green flag actually verified, both locked as literals into AC5's rule-anchor text. Codex agrees: "Closed. The record now states coverage at or below the ceiling and uses the unambiguous empty case `none above this ceiling`." I confirm: **closed**.

#### New finding — Major (vacuous/degenerate declaration under the broadened symmetric grammar)
Both my own reading and Codex's independent judgment converge on the same gap the fix's own broadening opens: nothing in the spec's text prevents a spec from declaring `real-environment` while marking **every** one of its criteria `- above-ceiling:`. Under that shape the declared top-level value makes no positive claim about anything actually reached — it becomes interchangeable with declaring `unit-and-static` with the identical set of per-criterion exceptions, since the symmetric rule ("the declared value states what the gate reaches for every criterion not marked above it") is vacuously satisfied when the "every criterion not marked above it" set is empty. This is a genuine boundedness gap in the declaration, not a request for a third value and not covered by the Input space's decline of a "competing per-criterion value" — it is the pair's own honesty being underspecified at its empty-set boundary. It falls squarely in the spec's own Verdict-scope Major class ("the declaration's honesty and boundedness... a closed pair presented as closed while the prose admits a third reading").

I considered whether this finding, being a Major on the same general component (the declaration's vocabulary/grammar boundedness) as round 1's Major, satisfies the pre-commitment's factual trigger ("two consecutive cross-provider review rounds landing an independent Blocker or Major against the same component"). I judge that it does **not**: round 1's specific claim (the mixed case has no honest declaration) was genuinely and fully closed by this round's edits rather than re-defeated, and this round's finding is a narrower, distinct defect that the round-1 fix itself newly introduced by broadening the grammar — not a second independent failure to close the same claim. Treating every subsequent finding on a shared component as an automatic trigger would make any iterative fix-then-scrutinize cycle undoable after one round; the trigger is meant to catch a design premise repeatedly defeated on the same specific claim, not the ordinary cost of a second, narrower gap surfacing after a first is closed. The pre-commitment therefore does **not** fire here, and I proceed with an ordinary route-back rather than a stop-and-return-to-planning.

- Route-back ask: either require the exception set to be a proper, non-empty-complement subset (i.e. a declared value must have at least one criterion actually verified at that level; a spec with every criterion above-ceiling should declare the lower value instead), or explicitly document the all-above-ceiling case and state its canonical declared value, so a reader can never construct a case where the top-level value is uninformative.

#### Findings 2–4, 6 (Minor) — left as-is
Per the coordinator's own note, these were left unaddressed in this round by design (my own round-1 downgrade/mapping to already-disclosed residuals). No new information changes that disposition.

#### Synthesis audit ledger (round 2)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | vacuous/all-above-ceiling declaration under either value | Codex: P2 (major per normalization map) | major | spec's own Verdict scope classifies declaration-boundedness/honesty gaps of this shape as in-scope-for-Major; both my own reading and Codex's independent pass converge on the identical gap |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (same route-back reasoning as round 1: `- entry-mode:` is not independently readable from a persisted artifact at this pre-intent-hash seam, so the fail-closed default applies; this again agrees with the Routing Map's printed `specify — pm-authored` decision).
- Pre-commitment status: **does not fire** — see reasoning above. This is treated as an ordinary second round, not a repeated defeat of the same claim.
- This verdict gates the freeze only, per the mode's own scope; it is not one of the two both-gates-green signals and does not substitute for either.

#### Fast-follow disposition
- no fast-follow deferrals — the one new finding this round is a Major that blocks the freeze, not a deferrable item.

Raw Codex output (round 2, overwrote round 1's canonical capture per the naming-consistency rule — round 1's findings remain preserved above): `.shell-team/reviews/T-1093-codex-specreview.txt` (final message), `.shell-team/reviews/T-1093-codex-specreview.jsonl` (full event stream).

---

### Codex Spec-Review verdict: REQUEST_CHANGES (round 3)
- Task: T-1093
- Codex model: gpt-5-codex
- Mode: Spec-review (specify seam, T-1092), round 3
- Consumer class: adopter-executed prompt surfaces and an adopter-facing declaration grammar — shipped-surface depth (unchanged)
- Input reviewed: `.shell-team/specs/T-1093-verification-ceiling.md`, current working-tree state (still uncommitted; intent-hash still not recorded), re-read in full — the Goal's grammar paragraph, Non-goals, both halves of Input space (including the new "vacuous declaration" and "floor case" bullets), Q1, Verdict scope, and the non-vacuity literal lists inside AC2/AC4/AC7. No `--base`, no branch diff.
- Round-3 scope, per the coordinator's own framing: is round 2's Major genuinely closed; do the two dispositions (refused above the floor, disclosed at the floor) introduce a new gap; is the floor-case board line still honest to a later operator.

#### Round-2 Major — closed
The non-vacuity rule ("a declared value attests `at least one criterion at it`") directly and explicitly closes the round-2 gap. An all-above-`real-environment` declaration can no longer exist as a conformant state: it is refused at first freeze with its canonical resolution named in the refusal itself (`unit-and-static` with the same exceptions marked). The rule is stated positively ("declare the highest value at which at least one criterion is actually verified"), pinned as the literal `at least one criterion at it` into AC2, AC4 and AC7's shipped-surface content, and Q1's answer absorbs it with an explicit cross-reference to both prior review rounds. Codex's independent read agrees the vacuity case itself is closed; I confirm: **closed**.

#### New finding — Major (the floor-case disclosure does not propagate to the record the operator actually reads)
The mandatory rationale disclosure for the floor case (unit-and-static declared while every criterion sits above even that) is required to live in the **spec's own** `- verification-ceiling:` declaration line. Nothing in AC5/AC6's shipped-surface requirements obliges that same disclosure — or an equivalent explicit marker — to propagate into the QA PASS block or the board's `READY_FOR_REVIEW` append, which is the artifact this whole task's Goal names as the actual reader-facing surface ("carried onto QA's hand-off and the board's `READY_FOR_REVIEW` record so a green flag reads 'green *up to*'"). In the floor case, the PASS block's `- Verification ceiling: unit-and-static` line together with the "verifies every criterion at or below that level" phrasing is **vacuously true when nothing was verified at all** — a later operator scanning the board with no memory of the run could read "PASS — Verification ceiling: unit-and-static" as confirmation that baseline automated checks passed, without registering that the accompanying above-ceiling enumeration happens to be the spec's *entire* criteria set. This is precisely the "green = done" misreading issue #333 exists to eliminate, recreated at the one degenerate boundary the design chose to document rather than refuse. Codex's independent pass converged on the identical concern. This is squarely in scope for a Major under the spec's own Verdict-scope wording ("a floor-case declaration a surface permits **without the mandatory rationale disclosure**" — the gap here is that the disclosure is permitted to stay confined to the spec and never required in the record).
- Route-back ask: require the floor-case's mandatory rationale disclosure (or an equivalent fixed, explicit marker such as "verified at declared level: none") to be **carried forward into the QA PASS block and the board append**, not merely readable from the spec. This does not ask QA to re-judge the vacuity determination — that stays correctly at authoring/freeze time, per the design's own stated reason for excluding AC5 from the non-vacuity literal lock — it only asks that the already-decided disclosure text be faithfully copied forward, consistent with the existing principle that QA "reads the declared value from the spec and never invents it."

#### Rejected finding (Codex, disagreed)
Codex raised a claimed internal contradiction: that the Verdict scope's "a `unit-and-static` declaration whose own criteria demand more" line conflicts with permitting the floor case. On a complete read, I disagree and reject this finding — that line is explicitly self-referential ("this spec included — check it against its own rule"), i.e. an instruction to the reviewer to verify *T-1093's own* declaration is internally consistent, not a general rule that any unit-and-static spec with an above-ceiling-marked criterion is a defect. The same Verdict-scope sentence separately and explicitly names the actual defect as "a floor-case declaration a surface permits **without** the mandatory rationale disclosure," which affirms rather than contradicts the floor case's legitimacy when the disclosure is present. Recorded here per the Rules' instruction to say so when Codex is clearly wrong, rather than silently dropping the finding.

#### Downgraded findings (Codex, agreed but re-severitized)
- Codex flagged that AC2/AC4/AC7 pin only the literal `at least one criterion at it` rather than asserting the full behavioral content (the highest-value rule, the refusal-with-redirect, the floor-case-permitted-with-disclosure) — a surface could in principle satisfy the literal checks while omitting a disposition. **Downgraded to Minor**: this is the identical, already-disclosed AC-literal-vs-behavior residual round 1 found (Minor 3/4) and the spec's own residuals (ii)/(iii) price this in for the whole task, not a fresh gap specific to this round's edit.
- Codex flagged a zero-criteria-spec edge case (no criterion at all, so the floor exception and `none above this ceiling` could both read as satisfied vacuously). **Downgraded to Minor / won't-fix**: a spec with zero acceptance criteria is a synthetic extreme real authoring practice in this repository cannot produce — every spec this loop's own methodology produces requires at least one AC to exist at all — so this does not warrant a criterion or a design change.

#### Pattern note (not a pre-commitment trigger, but recorded transparently)
Three consecutive spec-review rounds have each landed an independent Major on this same declaration/record-honesty area, each one closed by the author's edit while exposing a new, adjacent, previously-undiscussed degenerate case. I judge the pre-commitment's factual trigger does **not** fire this round, on the same reasoning as round 2: each round's *specific* claim (round 1's mixed-case inexpressibility, round 2's real-environment vacuity) was genuinely and fully closed by the following round's edit rather than re-defeated, and this round's floor-case-propagation gap is a distinct, novel concern the round-3 fix itself newly introduced by naming the floor case explicitly for the first time — not a second failure to close an existing claim. That said, the pattern itself (three rounds, three genuine Majors, each fix exposing an adjacent gap) is worth surfacing plainly: if a round 4 also lands an independent Major on this same declaration/record-honesty component, the orchestrator should weigh invoking the pre-commitment even under this narrower same-specific-claim reading, since three-in-a-row is already a strong whack-a-mole signal.

#### Synthesis audit ledger (round 3)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | floor-case disclosure not propagated to QA/board record | Codex: P1 (blocker per normalization map) | major | matches my own independent read; squarely named as Major-worthy by the spec's own updated Verdict scope wording, and is the exact "green=done" misreading #333 exists to prevent |
| 2 | claimed contradiction between "unit-and-static declaration whose own criteria demand more" and the permitted floor case | Codex: P1 (blocker per normalization map) | rejected | the cited line is self-referential to this spec's own declaration ("this spec included"), not a general rule; no actual contradiction once read alongside "without the mandatory rationale disclosure" |
| 3 | AC2/AC4/AC7 lock only the literal, not the full disposition content | Codex: P1 (blocker per normalization map) | minor | identical, already-disclosed AC-literal-vs-behavior residual from round 1 (Minor 3/4); not a fresh gap |
| 4 | zero-criteria spec edge case | Codex: P2 (major per normalization map) | minor / won't-fix | synthetic extreme real spec-authoring practice in this repository cannot produce (every spec requires at least one AC) |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (same route-back reasoning as rounds 1–2: `- entry-mode:` is not independently readable from a persisted artifact at this pre-intent-hash seam, so the fail-closed default applies; agrees with the Routing Map's printed `specify — pm-authored` decision).
- Pre-commitment status: **does not fire** this round — see the Pattern note above for the reasoning and the explicit warning for a hypothetical round 4.
- This verdict gates the freeze only; it is not one of the two both-gates-green signals and does not substitute for either.

#### Fast-follow disposition
- no fast-follow deferrals — the one new finding this round is a Major that blocks the freeze, not a deferrable item.

Raw Codex output (round 3, overwrote round 2's canonical capture per the naming-consistency rule — rounds 1 and 2's findings remain preserved above): `.shell-team/reviews/T-1093-codex-specreview.txt` (final message), `.shell-team/reviews/T-1093-codex-specreview.jsonl` (full event stream).

---

### Codex Spec-Review verdict: APPROVE (round 4)
- Task: T-1093
- Codex model: gpt-5-codex
- Mode: Spec-review (specify seam, T-1092), round 4
- Consumer class: adopter-executed prompt surfaces and an adopter-facing declaration grammar — shipped-surface depth (unchanged)
- Input reviewed: `.shell-team/specs/T-1093-verification-ceiling.md`, current working-tree state (still uncommitted before this round; intent-hash not yet recorded at review time), re-read in full — the Goal's grammar paragraph, AC1/AC2/AC4/AC5/AC6/AC7 and their literal lists, Q1–Q3, the Open questions' "Accepted residuals" bullets, the Input space's new zero-criteria out-of-scope bullet, and Verdict-scope item 1. No `--base`, no branch diff.
- Round-4 scope, per the coordinator's own framing: is round 3's Major genuinely closed; does the copying mechanism introduce a new gap; are the two Minor dispositions sound; is there a fourth independent Major on the declaration-semantics component.

#### Round-3 Major — closed
The floor-case disclosure is now a machine-readable, mandatory, fixed token (`no criterion verified at this ceiling`) required immediately after the declared value on a floor-case declaration, with absence refused at first freeze exactly as a vacuous real-environment declaration is refused. The QA PASS block and the board append are both required to carry the declared value **transcribed verbatim, including that token** — copying, never judging: QA never determines vacuity, never adds or drops the token, so the enforcement boundary (the determination is made once, at authoring/freeze) is unchanged. This directly and comprehensively answers round 3's specific ask: the disclosure now reaches the actual artifact an operator reads (PASS block and board line) rather than staying confined to the spec. The fix is dogfooded negatively on this spec itself — AC1 now asserts the token appears **zero** times on T-1093's own declaration line, since T-1093 is not a floor case, giving the criterion a live distinguishing test rather than only prose. The token is pinned as a literal across five shipped surfaces (AC2 authoring duty, AC4 freeze gate requiring presence and refusing absence, AC5 QA rule requiring `transcribed verbatim` plus the token, AC6 the board half specifically via `including that token`, AC7 adopter documentation). Codex's independent read agrees the mechanism closes the round-3 gap. I confirm: **closed**.

#### No fourth independent Major
Per the coordinator's explicit request to flag this clearly: **I find no fourth independent Major on the declaration-semantics component this round.** Both my own reading and Codex's independent pass converge on Minor-level findings only (below). The three-round pattern noted in round 3 does not continue into a fourth Major; this round's fix holds up under scrutiny.

#### Minor findings (this round)
1. **Minor** (copying-mechanism, downgraded from Codex's raw severity). The token's placement and cardinality rules are stated in prose ("immediately after the declared value... ahead of the rest of its rationale") but no criterion asserts an exact-once-iff-floor-case invariant or gives a single canonical PASS/board line template combining the token with the existing `at or below` and `none above this ceiling` record-shape literals — a shipped surface could in principle carry a malformed or misplaced token and nothing mechanically catches it. Downgraded to Minor: this is validation-completeness polish on an already-sound design, not a domain-premise defect, and is covered by the same already-accepted "agent-produced text this read cannot authenticate" residual (i) the whole task's no-mechanism pricing already prices in. Suggested (non-blocking) fix: add one canonical worked-example line to `docs/adopting.md`'s new section showing the token combined with `at or below` and the above-ceiling enumeration in one line, so engineers have a template to copy rather than composing the phrasing independently.
2. **Minor** (matches Codex's own severity — a verified factual correction). The Input space's new zero-criteria out-of-scope bullet states that "`bin/check-acs.sh` and `bin/check-intent.sh` both have a criterion population to count and a spec with none fails the loop." I independently verified this against both scripts: `bin/check-acs.sh:213–215` does exit 2 when zero acceptance criteria are found — that half is accurate. But `bin/check-intent.sh:420`'s own comment states plainly "a spec with zero `- check:` lines is a legitimate 0, not an error" — that checker does **not** reject a zero-criteria spec. The bullet's citation of `check-intent.sh` is factually wrong. The disposition's **conclusion** (a zero-criteria spec cannot reach this task's freeze gate) still holds on `check-acs.sh` alone, so this does not reopen the Minor as a design gap — but the frozen text should not carry an inaccurate citation. Ask (non-blocking, cheap): correct the bullet to ground the claim only in `bin/check-acs.sh`'s exit-2 behavior, dropping or correcting the `check-intent.sh` reference.

#### Minor dispositions from round 3 — accepted as recorded
- The AC-literal-vs-behavior residual, disposed as an accepted, already-disclosed limitation riding on issue #250's future mechanical gate: **sound**, consistent with this task's no-mechanism pricing throughout.
- The zero-criteria-spec disposition: **sound in conclusion**, with the one factual citation correction noted above as finding 2.

#### Synthesis audit ledger (round 4)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | token placement/cardinality not mechanically invariant | Codex: P2 (major per normalization map) | minor | validation-completeness polish on an already-sound, already-closed design; covered by the same already-accepted no-mechanism residual (i) |
| 2 | zero-criteria disposition mis-cites `bin/check-intent.sh` | Codex: P3 (minor per normalization map) | minor | independently verified against both scripts (`check-acs.sh:213–215` exits 2 on zero ACs; `check-intent.sh:420` explicitly treats zero `- check:` lines as legitimate) — conclusion still holds via check-acs.sh alone, citation should be corrected |

#### Recommendation
- APPROVE → this Specify-seam spec-review gate is satisfied; the intent-hash may now be recorded. This verdict gates the freeze only — it is not one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.
- Pre-commitment status: does not apply — no Major/Blocker landed this round, so the two-consecutive-rounds factual trigger has no new round to count.
- Non-blocking ask carried forward: correct the `bin/check-intent.sh` citation in the zero-criteria out-of-scope bullet before or shortly after freezing (finding 2 above); add a canonical worked-example record line if convenient (finding 1 above). Neither blocks this APPROVE.

#### Fast-follow disposition
- Finding 1 (no canonical token-placement template): won't-fix as a blocking item — cheap documentation polish an engineer may fold into `docs/adopting.md`'s new section at implementation time; not required before freeze.
- Finding 2 (`bin/check-intent.sh` mis-citation): file-an-issue is not warranted — this is a one-line textual correction inside this spec's own frozen prose, not a mechanism gap; recommend pm-spec correct it as a trivial follow-up edit rather than tracking it externally.

Raw Codex output (round 4, overwrote round 3's canonical capture per the naming-consistency rule — rounds 1–3's findings remain preserved above): `.shell-team/reviews/T-1093-codex-specreview.txt` (final message), `.shell-team/reviews/T-1093-codex-specreview.jsonl` (full event stream).
