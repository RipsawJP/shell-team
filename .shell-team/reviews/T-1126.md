### Codex Review verdict: BLOCKED
- Task: T-1126
- Codex CLI: codex-cli 0.149.1
- Diff scope: 8 files, +37/-30 lines (docs/adopting.ja.md, docs/adopting.md, docs/workflow.ja.md, docs/workflow.md, skills/goal/SKILL.md, skills/run/SKILL.md, templates/prompt-blocks/fanout-orchestration.md, templates/prompt-blocks/means-ends-reflection.md)

#### Blocking error

`codex exec --sandbox read-only --cd <repo> review --base develop --json -o <RAW_OUT>` exited 1. The captured JSONL event stream's terminal error, verbatim:

```
{"type":"error","message":"{\"type\":\"error\",\"status\":400,\"error\":{\"type\":\"invalid_request_error\",\"message\":\"The 'gpt-6-astra' model requires a newer version of Codex. Please upgrade to the latest app or CLI and try again.\"}}"}
{"type":"item.completed","item":{"id":"item_6","type":"agent_message","text":"Review was interrupted. Please re-run /review and wait for it to complete."}}
{"type":"turn.failed","error":{"message":"{\"type\":\"error\",\"status\":400,\"error\":{\"type\":\"invalid_request_error\",\"message\":\"The 'gpt-6-astra' model requires a newer version of Codex. Please upgrade to the latest app or CLI and try again.\"}}"}}
```

Preceding warnings from the same run, also verbatim: `Configured service tier \`priority\` is not advertised as supported for model \`gpt-6-astra\`` and `Model metadata for \`gpt-6-astra\` not found. Defaulting to fallback metadata; this can degrade performance and cause issues.`

Per this role's Rules, the Codex CLI (`codex-cli 0.149.1`) is configured to target a model (`gpt-6-astra`) the server rejects as requiring a newer CLI/app version — this is not the sandbox-EPERM (exit 71) shape, and it is not a missing-CLI/auth failure either, but it is the same class of stop: Codex itself cannot complete the review, so this role does not substitute another executor or fall back to a Claude-only read. Raw capture was not published (`codex exec` itself exited non-zero; per-invocation raw files were removed after diagnosis, per the `diagnose`/`cleanup` branch of the codex-capture skeleton).

No adversarial pass was run — round 1 produced no findings to trigger it.

#### Recommendation
- The coordinating session needs to resolve the CLI/model mismatch (upgrade the Codex CLI, or repoint its configured model to one this CLI version supports) before this review can run. Board stays at `READY_FOR_REVIEW`; do not advance to `READY_FOR_MERGE` off this record.

#### Fast-follow disposition
- no fast-follow deferrals

### Codex Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1126
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 8 files, +57/-42 lines (docs/workflow.md, docs/workflow.ja.md, docs/adopting.md, docs/adopting.ja.md, skills/run/SKILL.md, skills/goal/SKILL.md, templates/prompt-blocks/means-ends-reflection.md, templates/prompt-blocks/fanout-orchestration.md)

#### Blockers
- `templates/prompt-blocks/means-ends-reflection.md:4` (canonical source, mirrored verbatim into `skills/run/SKILL.md:85,105,239,259`, `skills/goal/SKILL.md:245`, `templates/prompt-blocks/fanout-orchestration.md:19`, `docs/workflow.md:78-84`, `docs/workflow.ja.md:79-84`, `docs/adopting.md:875-887`, `docs/adopting.ja.md:838-861`) — every reworded site asserts "it stops before a destructive or irreversible operation" as part of "what the loop actually guarantees," on the same footing as the authority claim ("it never merges on its own"). This is not true of the shipped generic loop: `templates/shell-team.contract.yaml:41-44` declares only `merge` and `push` under `human_gate` — no destructive/irreversible entry exists. The cited authority document itself, `docs/tuning-oversight.md`, places "Anything git cannot undo" under `## What is worth keeping in either direction` (an advisory example for the operator's own `CLAUDE.local.md`), not under `## What is fixed, and what is yours` (lines 10-16, which fixes only the completion gate and the merge-authority claim), and states explicitly (`:101`) "Naming it is a discipline the operator imposes on themselves, not a rule the loop enforces." T-1126's own Goal was to replace a misleading sprint-specific three-gate assertion with "what the loop actually guarantees" — but this clause reintroduces the same over-claim in new words: an adopter who never configured anything could read every one of these eight files and believe the shipped loop mechanically intercepts `rm -rf`, `git reset --hard`, or an overwrite, when nothing in the generic contract does. Both a primary `codex exec review --base develop` pass and a targeted adversarial pass (independently reading `templates/shell-team.contract.yaml` and `docs/tuning-oversight.md` in full) converge on this finding across all eight sites.
  - Suggested fix: scope the clause to what is actually fixed — the merge-authority claim plus the completion gate — and state the destructive/irreversible-operation stop as either (a) the narrower, already-shipped T-1095 means-ends rule (escalation is mandatory when the pre-priced disposition itself is destructive or irreversible, at the three named firing points) or (b) explicitly part of "whatever the operator's own oversight configuration adds," matching `docs/tuning-oversight.md`'s own classification, rather than asserting it as a loop guarantee equal to the never-merges claim.

#### Major
- none beyond the Blocker above.

#### Minor / nits
- `.shell-team/reviews/T-1126.md:11-13` (round 1's BLOCKED record, already committed at `8840df0`) — the round-1 verdict block quotes the raw Codex JSONL error text verbatim (model name, service-tier warning, CLI/model-mismatch message) inside a tracked, non-gitignored `.md` file. `CLAUDE.md`'s Hygiene section asks that "audit output carrying real values" not land in this public repo. This content is CLI/model configuration diagnostics, not PII, employer data or a session transcript, and it was written under this same role's own rule to "return BLOCKED with the exact error" — so it is a tension between two of this role's own governing documents rather than a defect in the reworded deliverable, and it concerns a `.shell-team/**` loop record the task description itself names as "not the deliverable." Flagged for the orchestrator to reconcile (or waive) rather than blocking this wording-only merge.
- `.shell-team/specs/T-1126-method-neutral-human-gates.md`'s `## Non-goals` line ("`.shell-team/**` ... are untouched — historical records...") read literally contradicts the same branch modifying `.shell-team/todo.md`, `.shell-team/reviews/T-1126.md`, `.shell-team/provenance/T-1126.md` and `.shell-team/interventions/T-1126.md` — every task in this repository's history creates exactly these lifecycle records as standard convention, so in context this Non-goal is clearly about not rewording *other, pre-existing* `.shell-team/**` documents, not about this task's own required board/spec/provenance/review trail. No actual defect; worth a tighter Non-goals phrasing in a future spec ("other than this task's own lifecycle records") so a future cross-provider reader does not have to infer the scope from convention.

#### Adversarial findings
- Targeted `codex exec` pass independently confirmed the Blocker above at all 8 in-scope sites plus reading `templates/shell-team.contract.yaml` and `docs/tuning-oversight.md` in full; it additionally judged the Japanese mirror (`docs/workflow.ja.md:81`, "必ず停止する") as strengthening the unsupported claim rather than merely mirroring it, and proposed narrower wording anchored to the shipped T-1095 firing-point rule for every site. Raw capture: `T-1126-codex-adversarial-r2.{txt,jsonl}`.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | destructive/irreversible-operation stop stated as a loop guarantee at all 8 sites | primary: P1 (blocker) / adversarial: high (major, all 8 sites + contract.yaml + tuning-oversight.md) | blocker | pervasive across the entire deliverable, contradicts the shipped generic contract and the cited authority doc's own advisory framing, and reintroduces the exact over-claim T-1126 exists to remove; taking the higher of the two native labels rather than the lower |
| 2 | raw Codex JSONL error text embedded in the tracked `.shell-team/reviews/T-1126.md` round-1 record | primary: P2 (major) | minor | CLI/model config diagnostics, not PII/secrets/employer data; content mandated by this role's own BLOCKED-verdict transcription rule; concerns a `.shell-team/**` loop record the task explicitly scopes out of "the deliverable" |
| 3 | spec's `## Non-goals` `.shell-team/**`-untouched clause read as contradicted by this task's own board/spec/provenance/review records | primary: P2 (major) | minor | standard repo-wide convention (every task creates these records); no actual behavioral defect, only a phrasing gap in the frozen spec worth tightening in future specs |

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with the Blocker above linked. The two Minor items are disclosed for orchestrator awareness and do not gate this round's disposition.

#### Fast-follow disposition
- Spec `## Non-goals` phrasing ambiguity (ledger #3) — file-an-issue: a future-facing wording tightener for `pm-spec`'s Non-goals convention (scope a `.shell-team/**`-untouched clause to "other than this task's own lifecycle records" by default), not urgent enough to hold this merge.
- Raw-diagnostics-in-tracked-review-record tension (ledger #2) — file-an-issue: reconcile `CLAUDE.md`'s Hygiene rule with `agents/codex-reviewer.md`'s BLOCKED-verdict exact-error transcription requirement (e.g., permit CLI/model diagnostic text explicitly, or route it to a gitignored side-file even on a BLOCKED verdict).
