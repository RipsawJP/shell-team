# QA verification — T-1033

**Scope**: verify `chore/promote-retro-2026-08-05` (base `76bbf5c`) against
`.shell-team/specs/T-1033-promote-retro-2026-08-05.md`, independently of the
engineer's hand-off on the board. `READY_FOR_QA` -> `READY_FOR_REVIEW`.

## 1. Mandatory conformance gates

- `bash bin/check-acs.sh .shell-team/specs/T-1033-promote-retro-2026-08-05.md`
  -> **14 passed, 0 failed, 1 skipped (AC15 SKIP)** — measured twice: before
  this review record existed and after it was written (`.shell-team/reviews/T-1033.md`
  is not read by any of T-1033's own `check:` lines — none of AC1–AC14
  enumerates the `reviews/` directory — so the shape is identical both times:
  **14P/0F/1S before, 14P/0F/1S after**).
- `bash bin/check-intent.sh .shell-team/specs/T-1033-promote-retro-2026-08-05.md .shell-team/todo.md`
  -> `aligned: T-1033 v1 (f2d2f4240b7287dad88c44170a559d581a8ceab7)`.
- `bash bin/check-intent.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/todo.md`
  -> `aligned: T-1027 v2 (8f4a08deee3d91132e396637ab586cce3df933c7)`.
- Independent re-run of the class-B classification: materialised
  `git show 76bbf5c:.shell-team/specs/T-1027-promote-retro-2026-08-04.md` into
  a scratch file and ran
  `bash bin/check-refreeze-class.sh -- <scratch> .shell-team/specs/T-1027-promote-retro-2026-08-04.md`
  -> `class-b: differing line at intent-block index 36 ... is not a '- check:'
  line on both sides` — **exit status 1, verified explicitly via `$?` in an
  `if`, not swallowed** — confirming class-B independently of the frozen `R`
  block's own claim.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1033.md` ->
  `conformant: 3 decision entries, 0 sentinel`.
- `bash bin/check-interventions.sh --task T-1033 -- .shell-team/interventions/T-1033.md`
  -> `conformant: 2 entries, 0 sentinel`.

## 2. Full CI-wired suite (`.github/workflows/check-handoff.yml`, workflow order)

Every named step ran, all green — the entire workflow file's list, in order,
including the directory/glob-scanning steps run rather than reasoned about
(this diff adds files under `.shell-team/provenance/`, `.shell-team/reviews/`
and `.shell-team/interventions/`):

- `shellcheck` (0.11.0, matching CI's pinned version) over the exact
  file list on the workflow's one `run:` line -> clean.
- Board template lint, dogfood board lint, `tests/check-handoff/run.sh`,
  loop-contract lints (x3), `tests/check-contract/run.sh`,
  `tests/loop-guard/run.sh`, `tests/check-run/run.sh`, `tests/log-run/run.sh`,
  `tests/team-init/run.sh`, `tests/discover-work/run.sh`,
  `tests/check-acs/run.sh`, `tests/check-design-note/run.sh`,
  `tests/goal-state/run.sh`, `tests/rework-digest/run.sh`,
  `tests/check-retro/run.sh`, `tests/retro-inputs/run.sh` + its invariants
  lock, dogfood `retro-inputs.sh --base HEAD`, dogfood `check-retro.sh` on
  this repo's retros and on the shipped template, `tests/machine-tokens/run.sh`,
  `tests/gen-loop-replay/run.sh`, `tests/check-readme-version/run.sh` + dogfood,
  `tests/rollup-runs/run.sh`, `tests/rollup-track/run.sh`,
  `tests/consolidate-proposals/run.sh`, `tests/cluster-failures/run.sh`,
  `tests/is-span-row-parity/run.sh`, `tests/review-gate/run.sh`,
  `tests/close-out/run.sh`, `tests/check-prompt-sync/run.sh` + dogfood,
  `tests/check-playbook/run.sh`, `tests/gen-playbook-blocks/run.sh`,
  `tests/playbook-promote/run.sh`, dogfood `check-playbook.sh` on the real
  corpus, dogfood `gen-playbook-blocks.sh` scratch-tree regeneration
  (**directory/glob step, run**: registry-driven 8-file copy into a scratch
  tree, regenerated, `cmp`'d byte-identical, **matched 8/8** — this run also
  independently reproduced the engineer's disclosed re-measurement: the
  block-size warning fires for exactly **two** roles, `playbook-engineer.md`
  and `playbook-pm-spec.md`, not the "third role" the spec's own Notes for
  engineer predicted), `tests/team-paths/run.sh` + dogfood, `tests/install/run.sh`,
  `tests/check-intent/run.sh`, `tests/check-provenance/run.sh`,
  `tests/check-interventions/run.sh`, `tests/interventions-reminder/run.sh`,
  dogfood `check-interventions` (**directory-scanning step, run**: looped
  `.shell-team/interventions/*.md`, **found=31**, every file conformant
  including the new `T-1033.md`), `tests/check-board-headings/run.sh`,
  `tests/errexit-safe/run.sh`, `tests/codex-skeleton-hygiene/run.sh`,
  `tests/check-pii-shapes/run.sh` + `check-pii-shapes.sh --base 76bbf5c` on
  the real diff -> `clean`, `tests/check-commit-identity/run.sh` +
  `check-commit-identity.sh --base 76bbf5c` on the real commit range ->
  `clean`, `tests/gitignore-raw-dumps/run.sh`,
  `tests/check-refreeze-class/run.sh` (30/30).
- All green. Zero steps skipped or reasoned about instead of run.

## 3. Independent re-derivation of the class-B claim

See §1 above. Exit code checked explicitly (`ec=$?; test "$ec" = "1"`), not
inferred from `set -e` or from output text. Confirmed **class-b**, matching
`R`'s own claim and AC6's own re-derivation.

## 4. Claim-fidelity check (issue #136)

`api.github.com` was reachable this session. Fetched
`https://api.github.com/repos/RipsawJP/shell-team/issues/136` independently
(not inherited from the engineer's report) — title, state (`open`) and body
retrieved. Body matches the spec's `## Summarized sources` restatement
clause for clause: the "missing outright" / T-1024 / "an acceptance
criterion's discharge failed the round" framing, the "existed but
under-inclusive" / T-1025 / "caught by QA re-derivation... held at nit"
framing, the chronological reading judged "defensible" (a clarification, not
a factual correction), and the fix routed through pm-spec as a re-freeze
proposal. All confirmed against the live filed text.

## 5. Summary-fidelity re-read (2026-08-04 E1 discipline)

Every clause of N1/N2/N3's `Why` fields and of the reworded `W` checked
against the primary artifacts myself (not inherited from the engineer's
provenance record):

- **N1** vs `.shell-team/reviews/T-1032.md` (as the spec's own AC15(c) text
  names): **T-1032.md contains no tmpfile/EXIT-trap/subshell incident at
  all** — confirmed by direct grep, zero hits. N1's real grounding is
  `.shell-team/reviews/T-1028.md`'s `#### Major` section, Major-1 bullet
  (`extract_region()`'s `TMP_FILES` array appended inside a `$( )` command
  substitution; the parent shell's `EXIT` trap sees an empty array;
  "before=1330 after=1332" measured live). Read clause by clause against
  N1's `Why` field: "newly shipped checker" / "two temp files... every
  ordinary invocation" / "helper... reached through a command substitution"
  / "append ran in the subshell and never reached the parent" / "EXIT trap
  read an empty registry... removed nothing" / "past thirteen hundred files"
  / "fixtures passed and the exit codes were right" — every clause matches
  the review record's own words. The retro's own candidate-2 citation
  (`.shell-team/retros/2026-08-05.md`) independently confirms
  `.shell-team/reviews/T-1028.md`（Major 1）as N1's real source, not
  T-1032.md. **This is a genuine locator error inside the spec's frozen
  AC15(c) prose and its `## Summarized sources` declaration — see §7.**
- **N2** vs `.shell-team/reviews/T-1032.md`: confirmed exactly. Two
  independent sites (its Majors 2 and 3) label a result "measured at
  `<ref>`" while the printed command reads the working tree; the
  cross-provider round raised both as one class; the same-class-2 full-diff
  re-derivation (QA round 2) found a third site (its own AC15(c) discovery)
  whose file count (33) was "borrowed from a different measurement" (the
  mktemp-population count) — matches N2's "a file count carried over from
  an unrelated population" clause for clause.
- **N3** vs `.shell-team/interventions/T-1028.md` entry 3
  (`human-correction`, 2026-08-04): the orchestrator classified a
  background pm-spec resume as "watchdog 対象外" and waited on the
  completion notification alone; the operator corrected the monitoring
  posture. Matches N3's "background role stalled... the launching side
  treated its own completion notification as the monitoring mechanism...
  nothing observed the gap until a human noticed... and corrected the
  operating rule" clause for clause.
- **W** vs `.shell-team/reviews/T-1024.md` (AC12 FAIL: the CI-wired-step
  disclosure was never made at all in the engineer's hand-off, and QA's own
  AC12 discharge failed round 1 on that absence) and
  `.shell-team/reviews/T-1025.md` (the AC8 blanket byte-identity rationale
  did not literally cover two live directory-scanning dogfood steps —
  `check-interventions`, `retro-inputs` — that the task had added a file
  under; QA re-ran both, both clean, held at **nit**, not a FAIL driver).
  W's two clauses ("no disclosure at all... failed on that absence" /
  "did disclose, but... under-inclusive... found only by re-running both
  steps itself and recorded at nit rather than failing the round") match
  both source records clause for clause, and the two failure shapes are
  genuinely distinct — the defect the reword exists to fix.

## 6. Two engineer-disclosed findings, independently re-confirmed

**(a) T-1027's spec measures 8P/4F, not the relayed 10P/2F.**
Re-ran `bash bin/check-acs.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md`
myself -> **8 passed, 4 failed (AC5, AC7, AC10, AC12), 1 skipped** — matches
the engineer's disclosure exactly. Root cause independently measured, not
merely read: `git diff --numstat 96acd96 -- .shell-team/lessons.md` ->
`63 2` (AC5 pins `33 2`; the +30 delta is exactly this task's own three
10-line corpus appends — T-1033's own doing, not a defect). Current block
bullet counts -> `engineer=44 qa-verifier=32 tech-lead=23 pm-spec=53` (AC7
pins `41/29/22/51`; the +3/+3/+1/+2 delta is exactly DP2's propagation of
N1/N2/N3 — again this task's own doing). `agents/codex-reviewer.md` differs
from `96acd96` (AC10; caused by T-1030, unrelated to this task, as relayed).
All four reds trace to declared merge-point staleness (DP8) — none is a
defect this task introduced beyond the expected, disclosed kind.

**(b) AC15(c)'s source-pointer discrepancy — judged, see §7.**

## 7. Judgment on the AC15(c) / Summarized-sources locator discrepancy

The spec's frozen `AC15(c)` text and its (non-frozen) `## Summarized
sources` declaration both name only `.shell-team/reviews/T-1032.md` and
`.shell-team/interventions/T-1028.md` as the grounding set "for the new
entries" (N1, N2, N3). Independently confirmed: this list is **incomplete
and partly wrong**. N2's real source is `T-1032.md` (correct as named). N3's
real source is `.shell-team/interventions/T-1028.md` (correct as named).
**N1's real source is `.shell-team/reviews/T-1028.md`** (a different file —
the *review*, not the *interventions* record, for the same task number) —
a file the frozen text never names at all. Verified via the retro's own
per-candidate `Source:` citations
(`.shell-team/retros/2026-08-05.md`, `## Lesson candidates`), which
correctly attribute candidate 2 (N1) to `.shell-team/reviews/T-1028.md`
（Major 1）, candidate 3 (N2) to `.shell-team/reviews/T-1032.md`（Major
2/3）, and candidate 5 (N3) to `.shell-team/interventions/T-1028.md`
（entry 3）.

This is a genuine flattened distinction inside frozen prose (T-1028's
*review* record and its *interventions* record are conflated into one
"T-1028" reference, and the third grounding source is missing outright) —
not a wording nit. Per this loop's mandatory claim-fidelity discipline, a
flattened distinction inside the frozen region is ordinarily a kick-back
finding. I am not kicking this task back to `READY_FOR_ENG` for it, for
three independent reasons specific to this case:

1. **The finding carries no machine-checked consequence.** AC15 has no
   `check:` line (`SKIP by design`); no other criterion reads this
   sentence's file list.
2. **It is not engineer-actionable.** The text sits inside T-1033's own
   frozen `<!-- BEGIN/END intent-block: T-1033 -->` region; per this spec's
   own R1, an engineer may not edit it — repair is a class-B re-freeze
   routed through pm-spec, exactly as R1 prescribes for every other frozen
   text on this branch. Routing to `READY_FOR_ENG` would hand the engineer
   a defect they are contractually forbidden from touching.
3. **The substantive requirement AC15(c) exists to enforce was met, not
   evaded.** The engineer did not paper over the mismatch by falsely
   claiming N1 was confirmed against the (wrong) named file; they went to
   the retro's own citation, found and read the correct source, confirmed
   every clause against it, and disclosed the discrepancy explicitly in a
   dedicated provenance decision (`.shell-team/provenance/T-1033.md`,
   second entry) and again in the board hand-off. Blind literal compliance
   with the frozen sentence was structurally impossible (T-1032.md contains
   no matching incident) and would have forced either a false claim or a
   silent substitution — the engineer did neither.

**Disposition: does not block PASS. Recorded as a finding for the next
pm-spec touch** (a class-B re-freeze of T-1033's `AC15(c)` sentence, and an
ordinary non-freeze edit to `## Summarized sources`' matching sentence,
which the spec itself states sits outside the intent block and can be
corrected without a re-freeze) — naming `.shell-team/reviews/T-1028.md` as
N1's actual third source alongside the two it already names.

## 8. Mutation reproduction (AC15(b) audit)

Engineer's report (6 mutations, all on a scratch `git worktree`, all as
predicted) is plausible on its face and consistent with every fixed-count
criterion in the spec. Independently reproduced **one** of the six myself,
on my own scratch `git worktree` (`git worktree add --detach`, never the
real working tree):

- Mutated N1's `Rule` line in the corpus (`subshell` -> `SUBSHELL_MUTATED`)
  -> `bash bin/check-acs.sh` AC1: **PASS before mutation -> FAIL after
  mutation -> PASS after restore (`git checkout --`, inside the scratch
  worktree only)**.
- Worktree removed immediately after (`git worktree remove --force`); real
  checkout's `git status --short` confirmed clean before, during (via a
  separate worktree, not the main tree) and after — no destructive git
  command touched the real checkout at any point.

## 9. Cross-checks against other named sources (Summarized sources)

- `.shell-team/todo.md`'s T-1027 `## Done` entry, `AC13(c)` sub-bullet —
  read directly; its T-1024/T-1025 phrasing is byte-identical to what the
  spec's `## Summarized sources` quotes it as.
- `bin/gen-playbook-blocks.sh` header comment — confirmed line 14: "`Why` /
  `How to apply` are NEVER transcribed", matching the spec's characterization.
- `bin/check-intent.sh` — `HASH_FULL_RE`, `RATIFIED_FULL_RE`,
  `ATTEST_FULL_RE` read directly; grammar matches the spec's description.
- `bin/check-playbook.sh` / `bin/playbook-promote.sh` — `KNOWN_FIELD_NAMES`
  and the emitted field-order (`Category`, `Applies-to`, `Scope`, ...) read
  directly; matches DP1's claim.
- The operator's `CLAUDE.local.md` ground (checkout-local, no `check:`
  clause by design, per the spec) — read as part of this session's own
  system context; its "承認ゲートの範囲" section matches R's third ground
  ("a freeze or re-freeze proceeds on orchestrator attestation and that no
  new human gate is invented") verbatim in substance.

## 10. Edge cases tried

- N3's general-form negative clauses (no `.sh` token, no `~/`/`/Users/`/
  `/home/` token) — confirmed via AC3's own live run rather than by reading
  the check line; not separately re-planted, since the spec's own Notes for
  engineer already require this as part of the freeze-time sweep and AC3's
  live PASS demonstrates the negative clauses actually fire on the shipped
  bytes (a vacuous always-pass would show as a false PASS regardless of
  content — confirmed by inspecting the extracted body used by the clause).
- Confirmed the board's only deletion is the T-1027 `intent-hash (v1)`
  record (AC13) and that `## Active` was empty at the base ref (AC13/AC14's
  pure-insertion claim) — both independently re-derived, not read off the
  hand-off.

## Real checkout hygiene

`git status --short` clean throughout this verification except for this
review record and the board flag flip. No `git checkout --`, `restore`,
`reset --hard`, `clean` or `stash` was ever run against the real checkout;
all destructive/mutation probing happened on a `git worktree` under
`$TMPDIR`, removed immediately after use.

### QA verdict: PASS
- Task: T-1033 → READY_FOR_REVIEW
- Tests: full CI-wired suite green (see §2); `check-acs.sh` 14P/0F/1S for
  T-1033 (before and after this record); 8P/4F for T-1027 (merge-point
  staleness, disclosed, confirmed genuine)
- Acceptance criteria: 14/14 checked off mechanically (AC1–AC14 all PASS);
  AC15 (runtime) verified by hand across all five sub-items (a)–(e)
- Edge cases tried: N3 general-form negative-clause live confirmation;
  board pure-insertion re-derivation; one independent mutation reproduction
  outside the six the engineer reported
- Risk notes for reviewer: (1) the AC15(c)/Summarized-sources locator
  discrepancy in §7 — not a FAIL driver, but a real finding that needs a
  small pm-spec follow-up touch (class-B re-freeze of the frozen sentence,
  ordinary edit of the non-frozen one); (2) items (3) "the sprint v1.6.0
  planning GO approved #136 inside scope" and (4) "T-1034/T-1035 own
  candidates 1/4" of AC15(d)'s five relayed claims are relayed-and-attested
  by the coordinating session and not independently measurable by either
  the engineer or this QA pass (no access to the planning conversation) —
  disclosed as such, not silently absorbed; (3) T-1027's 8P/4F merge-point
  staleness will read differently again after this branch merges — that is
  intended per DP8 and must not be "fixed" at a later touch.
