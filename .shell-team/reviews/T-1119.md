### Codex Spec-Review verdict: REQUEST_CHANGES

## Spec review

**Mode**: Specify-seam spec-review (T-1092), round 1. No implementation exists yet — this reviews the frozen intent block (`## Goal` through `## Input space`, including the nested `probe-protocol` region) of `.shell-team/specs/T-1119-effort-application.md`, plus the declaration region and the surrounding mutable sections (`## Release-note sentence`, `## Live-run evidence` scaffold, `## Blast radius`, `## Body-to-AC correspondence`, `## Decision points`, `## Pre-commitment`, `## Verdict scope`, `## Assumptions`, `## Open questions`, `## Notes for engineer`), per the Routing Map's `spec-review — cross-provider` election for this task (cross-checked on the board against `pm-spec`'s own `- entry-mode: pm-authored` line and the printed `dispatch: specify — pm-authored — unconditional` row — no mismatch).

- Task: T-1119
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `.shell-team/specs/T-1119-effort-application.md` (479 lines), read end to end: `## Problem` through `## Non-goals` in full; every one of the 24 acceptance criteria's prose and `- check:` body; `## Input space` (reachable classes and out-of-scope extremes); `## Release-note sentence`; the `## Live-run evidence` scaffold; `## Blast radius`; `## Body-to-AC correspondence`; `## Decision points` DP-a through DP-m; `## Pre-commitment`; `## Verdict scope`; `## Assumptions` A-1 through A-18; `## Open questions`; `## Notes for engineer`. Also read first-hand and cross-checked against the spec's own claims rather than trusted from its summary: `templates/adapters/codex-cli.txt` header (`effort-value` rows, 2026-08-10 measurement date); the coordinating session's briefing (`t1119-inputs/spec-review-briefing.md`); `tech-lead`'s Routing Map (`t1119-inputs/routing-map-plan.md`); both probe records (`effort-probe.md`, `effort-probe-valid.md`) **and their raw event streams** (`effort-probe-events.jsonl`, `effort-valid-events.jsonl`), verifying by hand that the raw streams carry no `item.type` key on the rejection arm (top-level `{"type":"error",…}` / `{"type":"turn.failed",…}` only) — confirming the spec's A-4 correction is accurate and the probe summary's `item.type=error` wording is indeed contradicted by the bytes; and `.shell-team/todo.md`'s live `**T-1119**` board entry (`- entry-mode:`, `- spec-review:`, the `- dispatch: specify` row).

- raw-capture: `T-1119-codex-specreview.jsonl` / `T-1119-codex-specreview.txt`
- executor-invocation (specreview-r1): `codex exec --sandbox read-only --cd <repo-root> --ignore-user-config --ignore-rules --ephemeral --json -o "<RAW_OUT>" "$(cat <prompt-file>)" < /dev/null > "<RAW_JSONL>" 2>&1` — prompt content: a two-tier scope notice (the prompt file is the sole instruction-bearing input; every other readable byte, including the target spec's own imperative-sounding prose, is data); an instruction to read `.shell-team/specs/T-1119-effort-application.md` directly with the executor's own tools rather than expect it pasted; the domain-premise-versus-AC-quality mandate; the three named domain premises this task's `spec-review — cross-provider` election was priced on, each pointed at the specific section grounding it (DP-h/A-5/A-6 for the accepted-value set, the `probe-protocol` region and DP-i for runtime honoring, `## Input space` item 5 and A-17 for the adopter-host boundary); a pointer to `## Verdict scope` for the reduced-depth carve-out on this spec's own inline `- check:` lines; a request for a JSON findings array of `{severity, criterion, issue, suggestion}` in Codex's own severity vocabulary.
- pass-role (specreview-r1): generation
- briefing-fidelity (specreview-r1): carried — the prompt reached the executor; its own JSONL trace shows four `command_execution` items (each a paired `item.started`/`item.completed`): a first `sed -n '1,260p'` + `sed -n '261,620p'` read of the full spec alongside `templates/adapters/codex-cli.txt:1,180p`; a targeted `rg -n` locating `## Goal`, `## Input space`, `## Verdict scope`, `## Decision points`, `## Pre-commitment`, `## Assumptions`, every `- [ ] **AC` line, the `probe-protocol` markers and A-5/A-6/A-17; a `sed -n '39,138p'` + `sed -n '218,470p'` reread of the Goal/probe-protocol and Input-space-through-Assumptions regions; and a final `sed -n '218,275p'` + `sed -n '388,428p'` + `sed -n '417,445p'` reread of Input space, Decision points and Verdict scope/Assumptions. This covers every region the prompt named. One intermediate `agent_message` (in Japanese, a self-summary of the reading plan, `item.completed`-only with no paired `item.started`) preceded the final English JSON findings array — a stylistic artifact of the run's own reasoning trace, not a briefing-fidelity concern.
- raw-capture (specreview-r1): T-1119-codex-specreview

### Invocation hygiene observations (non-licensing)

- **Skill/plugin/instruction sources reported beyond the briefing**: none self-reported — this pass's prompt did not request an `injection-observed=` or `skills-visible=` self-report line (an omission in this round's own prompt construction relative to the T-1118 precedent's practice), so nothing on that axis was collected. No plugin, hook or app-connector name appeared unprompted anywhere in the JSONL.
- **`item.type=error` plugin/hook items**: none. The stream carries one `thread.started`, one `turn.started`, four `command_execution` items (four `item.started`/`item.completed` pairs), two `agent_message` items (`item.completed`-only, no paired `item.started`) and one `turn.completed` — thirteen lines total (excluding the CLI's own "Reading additional input from stdin..." banner line, printed even under `< /dev/null`), zero `error`-typed items — consistent with the isolation flags' measured effect on this host at this CLI version.
- **First request's `usage.input_tokens`**: one `turn.completed` event only, `input_tokens=245006`, `cached_input_tokens=175744` for the whole turn (eight tool calls). Not compared against any baseline reading — no baseline was requested in this round's prompt.
- No stall, refusal or incident on this pass; `codex` exited 0 and `--publish` validated and published cleanly.

### Independent primary reading (before consulting Codex's output)

**The three domain premises the election was priced on, checked one by one, against the live repository and the raw probe streams rather than the spec's paraphrase.**

1. **The provider's accepted `model_reasoning_effort` set and its stability across CLI versions.** Verified first-hand: the raw stream `effort-probe-events.jsonl` carries the provider's own 400 response enumerating `'none', 'minimal', 'low', 'medium', 'high', 'xhigh', and 'max'` — exactly the seven `effort-value` rows `templates/adapters/codex-cli.txt` has declared since a 2026-08-10 measurement on `codex-cli 0.145.0`, and this round's own `codex --version` reads `codex-cli 0.149.1`, three weeks and one minor-version step later, with no drift. The spec is careful to frame this as a **measured-not-drifted** snapshot rather than a stability guarantee — DP-h's counterfactual rule ("had the set moved, those rows would be updated inside this task") and the "drift guard, not a live path" framing (DP-e, DP-h, the evidence-boundary's required `drift guard` literal) are the honest form of this premise: the fail-closed gate does not depend on the set never changing, only on today's declared rows matching today's provider, with a BLOCKED escalation (never a silent drop or a retry at another tier) as the designed response to future drift. Sound.
2. **Whether the provider honours a passed value at all.** This is the premise the spec handles most carefully. The in-set raw stream (`effort-valid-events.jsonl`) confirms no `--json` field echoes the effort setting and `usage.reasoning_output_tokens` reads `0` — the spec does not claim more than "the clause reaches the invocation and is accepted", explicitly and repeatedly distinguishing that from "the provider executed at that tier" (Goal's "What the run measures, and what it cannot" paragraph, DP-i, the evidence-boundary's required `not observable` / `what was passed` / `reasoning tier` literals, AC4's literal requirements, AC16's channel pins). This is an honest evidence boundary, not an overclaim — see Finding 2 below for one gap in what the boundary's *stated* content actually names.
3. **What an adopter's own Codex install does under `--ignore-user-config` on a different host/version.** Disclosed as unmeasured, explicitly and without hedging into a false generalization (`## Input space` out-of-scope item 5, A-17). Sound — matches the disclosure discipline the spec applies throughout.

**Cross-checked against the live repository, not merely against the spec's own citations**: `templates/adapters/codex-cli.txt`'s seven `effort-value` rows do read `none`/`minimal`/`low`/`medium`/`high`/`xhigh`/`max`, matching A-5's transcription; the file's header does cite `codex-cli 0.145.0`, confirming the staleness A-6 and AC17 exist to repair; `docs/loop-engineering/task-envelope.md` does carry exactly two `- normative: ` lines (T-1055 AC6's lock), confirming DP-c's premise for using a distinct `- applied-at:` key; `templates/binding-default.conf` does carry `-` in every effort column and binds `tech-lead` to `claude-cli`, confirming DP-j's two-opt-in PATCH-tier argument. None of the spot-checked claims were found inaccurate.

**One point not raised by Codex, on the AC-quality half of the mandate.** The Goal states the resolved row's effort value "is applied to the real invocation... inserted at one named point on the frozen command line", and DP-b explains the value travels via `bin/resolve-executor.sh --role`'s field 5. No criterion in the 24 connects those two claims end to end: AC7 tests the resolver's own field-5 output in isolation (a scratch `binding.conf` against a scratch resolver, never invoking `codex exec`); AC1/AC3/AC4 test the recipe's prose statically (the clause literal and its named insertion point are present as text, never executed with a substituted value); the live-run protocol tests that a `-c model_reasoning_effort=<value>` clause at the named position is provider-acceptable, using a **fixed literal** (`low`) chosen for reproducibility, not a value obtained by actually running `bin/resolve-executor.sh --role tech-lead` against a `codex`/`codex-cli`-bound scratch config and threading its field-5 output into the live invocation. No criterion, and no line of the evidence-boundary's required content (which names five specific disclosures, none of them this one), states that the connective step — an invoking agent correctly reading the prose and substituting the *actual resolved* value at the *actual named position* — is itself untested and rests on the same self-attestation boundary every other freeze-then-run gate in this repository accepts. This is a genuine, narrow gap between what is claimed (Goal) and what the criteria as a set demonstrate.

### Codex's independent findings (raw JSON in `T-1119-codex-specreview.txt`)

Codex read the spec directly (eight of its own `sed`/reading tool calls spanning the file, no pasted excerpt) and returned 4 findings, using its own (non-project) severity vocabulary (P0–P3, style) — normalized below per this role's severity-normalization map (P1→major, P2→minor).

1. (Codex: P1 → major) `probe-protocol` / AC13–AC16 / DP-i: the live probe manually invokes `codex exec -c model_reasoning_effort=...` with a hand-chosen value; it never exercises the shipped recipe's own `applies-effort` lookup or a resolved field-5 value, so probe/resolver criteria can pass even if the recipe fails to carry an actually-resolved value into a real invocation.
2. (Codex: P1 → major) DP-i / AC11 / AC12 / AC16: the evidence establishes only that a value was passed and accepted, while `clause-applied`, the adopter-doc requirement and the release-note sentence say the value is "applied" — provider acceptance does not demonstrate the backend honored the requested tier, which the spec itself declares unobservable.
3. (Codex: P1 → major) DP-h / A-5 / A-6 / Input-space item 5 / A-17: the accepted-value set is grounded in one provider response from one CLI/backend version; an adopter's different host/version is explicitly excluded; the resolver treats the seven rows as universally authoritative, and runtime fail-closed handling covers only removal of a formerly accepted value, not newly accepted values or changed `-c` semantics.
4. (Codex: P2 → minor) AC4 / AC11: these criteria mainly require selected literals or a nearby word-pair window, which can pass with contradictory or swapped per-adapter attribution (e.g., prose that names both adapters and both verbs but assigns the outcomes to the wrong adapter).

### My own synthesis of Codex's findings

**Finding 1 — upheld as major, and independently re-derived above (Independent primary reading, final paragraph) before I opened Codex's output.** This is the round's most consequential finding. Traced by hand against the actual `- check:` bodies: AC7 verifies `bin/resolve-executor.sh`'s field-5 resolution in isolation; AC1/AC3/AC4 verify the recipe's prose in isolation; the `probe-protocol`'s frozen command shapes (spec lines 74–79) use the literal `-c model_reasoning_effort=low`, not a value obtained by running the resolver against a scratch `codex-cli` binding and threading its output into the invocation. The protocol's own text ("the argv the evidence records is the argv the recipe composes") asserts the insertion *position* matches, which is true and independently verifiable — but the Goal's claim is about the *resolved value* reaching the invocation, and no criterion or evidence-boundary line closes that specific seam. This does not undermine the shipped mechanism's likely correctness (the position claim is genuinely verified, and an agent composing the invocation by reading the prose is the same self-attestation act every other freeze-then-run gate in this repository already accepts) — but the gap is currently **unnamed**, and the evidence-boundary's required-content list (five specific items) does not include it. Cheap to close either way: add one evidence-boundary line naming this seam as self-attested rather than mechanically bound, or strengthen the live-run protocol so the in-set arm's `-c` value is obtained from an actual `bin/resolve-executor.sh --role tech-lead --print-resolved`-style read against a scratch `codex`/`codex-cli` binding rather than a hand-picked literal.

**Finding 2 — downgraded to minor, largely already addressed.** Re-reading DP-i, the Goal's "What the run measures, and what it cannot" paragraph, and AC4's required literals (`not observable`, `what was passed`, `reasoning tier`) together: the spec is unusually careful to define "applied" as "reaches the invocation and is accepted by the provider" and to state the corollary — the telemetry value is "an observation of what was passed, never of what the provider did" — in the shipped prose itself, not only in this spec's internal reasoning. AC11's per-adapter window test requires both the `applied` and `recorded` words to co-occur, which is the mechanical form of forcing this exact two-sided honesty into the adopter docs. Codex's finding restates a distinction the spec already draws explicitly and requires the shipped prose to draw too; it is not a hidden overclaim. Worth one clarifying note if pm-spec wants extra insurance (e.g., an explicit sentence in the release-note or DP-i stating "applied" means "passed to the provider", never "executed at that tier"), but not blocking on its own.

**Finding 3 — downgraded to minor, largely already disclosed.** The "newly accepted value" scenario Codex raises is not a safety gap: an adopter setting an effort value the local adapter file does not yet declare is refused at resolution (`capability-unsupported`, before any invocation), which is the conservative, safe-by-design direction — nothing runs at an unintended tier. The "changed `-c` semantics" scenario (the flag's meaning itself shifting, not just the accepted-value enumeration) is a genuinely unmeasured risk, but it is the same adopter-host/later-version premise `## Input space` item 5 and A-17 already name and explicitly decline to assume closed ("unmeasured either way and is stated as such rather than assumed"). This is a real but already-disclosed premise, not an undisclosed one — the mandate here is whether the evidence boundary is honest and complete, and on this specific point it already is.

**Finding 4 — upheld as minor.** Re-checked against AC11's actual window test (spec lines 172–173): the 400-character flowed window around the first `cli-config-override` occurrence must contain both adapter tokens and both verb words, order-free — genuinely satisfiable by prose that names both adapters and both outcomes but pairs them backwards (e.g., attributing "applied" to `claude-cli` and "recorded" to `codex-cli`). This is a plausible authoring accident during a prose rewrite, not merely a hostile-adversary construction the spec's Non-goals item 9 correctly declines to defend against — and `## Verdict scope` places the four adopter documents at full adversarial depth, where "whether the amended prose overshoots in either direction" is explicitly named as in-scope. Codex's inclusion of **AC4** in this finding does not hold up on inspection: AC4's required literals (`BLOCKED`, `capability-unsupported`, `never a retry at a different tier`, etc.) are global escalation-rule statements with no per-adapter attribution to swap, so AC4 is not exposed to this failure mode — narrowed to AC11 alone.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | live-run protocol never threads an actual resolved field-5 value through to the invocation it measures | Codex: major (P1); my independent read, reached before opening Codex's output: confirmed against AC7/AC1/AC3/AC4 and the probe-protocol's fixed-literal command shapes | major | Names a real, currently-unstated seam between the Goal's claim (resolved value reaches invocation) and what the 24 criteria as a set demonstrate; cheap to close by evidence-boundary disclosure or by deriving the live-run value from an actual scratch resolution. |
| 2 | "applied" language overclaims relative to the measured-unobservable tier-honoring premise | Codex: major (P1); my independent read: the distinction is already drawn explicitly in the Goal's own prose, DP-i, and AC4/AC11's required literals | minor | Not a hidden overclaim — the spec already forces the passed-vs-executed distinction into the shipped prose it ships; one optional clarifying sentence would remove residual reading risk but nothing is currently missing. |
| 3 | accepted-value set and adopter-host behavior rest on a single-version, single-host measurement | Codex: major (P1); my independent read: the removal case is fail-safe by design (refused at resolution, never silently misapplied) and the host/version premise is already named as unmeasured in Input-space item 5 and A-17 | minor | Already disclosed rather than assumed closed; the genuinely unmeasured half (adopter host/CLI version) is stated as such, not smoothed over. |
| 4 | AC11's window test can pass with per-adapter attribution swapped; AC4 was incorrectly bundled into the same claim | Codex: minor (P2), scoped to AC4/AC11 jointly; my independent read: confirmed for AC11 only, AC4 has no per-adapter attribution to swap | minor | A plausible authoring accident (not a hostile construction) at a surface `## Verdict scope` places at full adversarial depth; narrowed to AC11 since AC4's required literals carry no adapter-to-outcome mapping. |

### Route-back

**Verdict: REQUEST_CHANGES** (round 1; bare token, no `(round <N>)` suffix — this is the first spec-review round for this task).

Resolved per the design pattern shared with the Conformance-read confirmation gate and the Specify-seam spec-review gate: read `pm-spec`'s own `- entry-mode:` hand-off field, cross-checked against the `specify` decision `tech-lead`'s Routing Map printed at Plan. `.shell-team/todo.md`'s live `**T-1119**` entry carries `- entry-mode: pm-authored` and its printed `- dispatch: specify — pm-authored — unconditional` row agrees — no mismatch to escalate. **Route back to `pm-spec`.**

Blocking item for the rework: Finding 1 (name the resolved-value-to-invocation seam explicitly on the evidence-boundary line, or strengthen the live-run protocol so the in-set arm's `-c model_reasoning_effort=<value>` is obtained from an actual scratch resolution against a `codex`/`codex-cli`-bound `binding.conf` rather than the fixed literal `low`, so the recorded argv is traceably the resolver's own output and not merely a representative value chosen for reproducibility).

### Fast-follow disposition

- Finding 2 ("applied" wording) — recommend, not blocking: one clarifying sentence in DP-i or the release-note tying "applied" explicitly to "passed to the provider" rather than "executed at that tier", bundled into the same rework pass as Finding 1 if pm-spec judges it worth the line. Won't-fix as blocking: the distinction is already present and required in the shipped prose via AC4's literals.
- Finding 3 (single-version/single-host grounding) — won't-fix as blocking: the removal direction is fail-safe by construction and the host/version premise is already disclosed as unmeasured rather than assumed; no new disclosure is owed.
- Finding 4 (AC11 attribution-swap gap) — recommend, not blocking: tighten AC11's window test to require adapter-token-to-verb adjacency (e.g., assert `codex-cli` appears nearer the `applied`/`適用` occurrence than the `claude-cli` token does, or vice versa for `recorded`/`記録`) as a fast-follow if a future round touches AC11 anyway. Not itself blocking this freeze.

### Codex Spec-Review verdict: APPROVE (round 2)

## Spec review — round 2

**Mode**: Specify-seam cross-provider spec-review (T-1092), round 2, against pm-spec's rework commit `9ddd9b8` (with a corrective interventions entry at `94607b3`, branch HEAD). No implementation exists yet — this remains a pre-freeze review of the spec document itself. Read `## Prior review findings` (added by this commit) first, per its own instruction, so a decision is met there rather than re-derived, then re-verified every changed region against the live check bodies and the live repository rather than trusting the table's own account: the new protocol **step 0** (scratch installed tree, `.shell-team/binding.conf`, `bin/resolve-executor.sh --print-resolved`), the `- resolution:` evidence line and its `effort-field`, the verdict function's new equality conjunct, the sixth `resolved-row` channel, **AC11**'s nearest-attribution test, **AC15**/**AC16**'s grammar updates, **DP-i**'s added sentence, `## Input space` item 18, and the `.shell-team/interventions/T-1119.md` third entry (the `TEAM_RUN_BASE` correction).

- Task: T-1119
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `.shell-team/specs/T-1119-effort-application.md` at `94607b3` (round-1 subject was `91281e6`'s pre-image at the same path; the round-1 rework diff is `9ddd9b8`, 46 insertions / 20 deletions). Read: the full `git show 9ddd9b8 -- .shell-team/specs/T-1119-effort-application.md` diff end to end (not only the post-image); the new `## Prior review findings` table in full; **step 0**'s complete prose in the `probe-protocol` region; **AC11**, **AC15**, **AC16**'s complete prose and `- check:` bodies, hand-traced arm by arm; **DP-i**'s added sentence; `## Input space` item 18; `## Notes for engineer`'s AC11 addition; and `.shell-team/interventions/T-1119.md`'s third entry. Independently verified against the live repository rather than trusted from the spec's own account: `bin/team-paths.sh:105`–`:133` (`validate_base`, confirming an absolute `TEAM_RUN_BASE` is refused and `ROOT="."` is the default at `:77`); `bin/resolve-executor.sh:319` (`HOST_CONFIG="$BASE_DIR/binding.conf"`); and a live `bash bin/resolve-executor.sh --print-resolved` run against the real shipped default, confirming the row shape is exactly `resolved <role> <provider> <model> <effort> <adapter>` (six space-separated fields, effort at position 5 via `awk '$5'`) — all three match the spec's own claims byte for byte.

- raw-capture: `T-1119-codex-specreview-r2.jsonl` / `T-1119-codex-specreview-r2.txt`
- executor-invocation (specreview-r2): `codex exec --sandbox read-only --cd <repo-root> --ignore-user-config --ignore-rules --ephemeral --json -o "<RAW_OUT>" "$(cat <prompt-file>)" < /dev/null > "<RAW_JSONL>" 2>&1` — prompt content: the same two-tier scope notice as round 1; a summary of round 1's Major and upheld Minor with an instruction to read `## Prior review findings` first and then verify pm-spec's own account against the live spec bytes rather than trust the table; five numbered verification items (does step 0 genuinely obtain the in-set value from a real resolution and does AC16's equality conjunct get recomputed from two independent lines or is it trivially satisfiable; cross-check the claimed `bin/team-paths.sh`/`bin/resolve-executor.sh` mechanism against the real files, with permission to run commands; does AC11's nearest-attribution awk logic have a gap; are AC15/AC16's grammar and DP-i's sentence internally consistent; is Input-space item 18 accurate); an explicit ask to separate "independently-new" findings (introduced by this rework) from "restatement" findings (round 1 not actually closed); a request for a JSON findings array of `{severity, criterion, issue, label, suggestion}`.
- pass-role (specreview-r2): generation
- briefing-fidelity (specreview-r2): carried — the prompt reached the executor; its own JSONL trace shows five completed `command_execution` items: an initial `git status --short` combined with a read of an unrelated skill file (see hygiene note below); `sed -n`/`rg -n` reads of `## Prior review findings`, the `probe-protocol` markers, and AC11/AC15/AC16/DP-i/Input-space-18 locations; `nl -ba` reads of `bin/team-paths.sh:70-140` and `bin/resolve-executor.sh:300-...`; `sed -n` reads of the AC11 diff region, `## Input space`, and the corresponding `bin/` line ranges; `awk` extractions of AC15's and AC16's own `- check:` lines; and a `git diff 9ddd9b8^ 9ddd9b8` scoped to the added-line prefixes naming the round-2 vocabulary. A sixth attempted command — a multi-statement scratch-tree build-and-resolve (mirroring this review's own independent verification) — was rejected by the sandbox's destructive-operation guard (the same "multi-statement `bash` block with an inline `mktemp -d`... denied outright" quirk this spec's own `## Notes for engineer` documents), which Codex self-reported and worked around by relying on the direct code-path reads instead. This covers every region the prompt named; the rejected command reduced depth on live-execution confirmation only, not on the static read the findings are actually grounded in.
- raw-capture (specreview-r2): T-1119-codex-specreview-r2

### Invocation hygiene observations (non-licensing)

- **Skill/plugin/instruction sources reported beyond the briefing**: yes, and this time grounded in an actual command execution rather than free-floating self-report. The run's first tool call reads `<home>/.agents/skills/hard-task-protocol/SKILL.md` — an absolute path **outside this repository, under the operator's home directory**, naming a skill (`hard-task-protocol`) that belongs to this operator's own Claude Code environment, not to Codex, and that was never mentioned in this pass's own prompt. This is consistent with the CLI's already-disclosed "read access everywhere" design (T-1118 A-4) and with `--ignore-user-config`'s narrower, already-disclosed scope (it drops `$CODEX_HOME/config.toml`, not the whole filesystem) — it is a live, first-hand reproduction of the exact residual-discovery-path class T-1118's own spec review already measured and disclosed as `undetermined`, not a new class of leak this task's own domain premises depend on (T-1119's live-run protocol tests provider effort-value acceptance, not read-scope containment). Recorded here as the same class, on a different host state, for the record.
- **`item.type=error` plugin/hook items**: none. The stream carries no `error`-typed item; the rejected scratch-tree command surfaced as a raw (non-JSON) multi-line stderr dump embedded directly in the output stream rather than as a structured `error` event — an artifact of how this CLI version reports a sandbox-denied `exec_command`, not a plugin/hook error.
- **First request's `usage.input_tokens`**: one `turn.completed` event, `input_tokens=438598`, `cached_input_tokens=357248` for the whole turn (five completed tool calls plus one rejected one). Not compared against any baseline — none was requested in this round's prompt.
- **Incident, disclosed**: one sub-command (a scratch-tree build-and-resolve, run to independently confirm step 0's mechanism live) was rejected by the sandbox's destructive-operation guard mid-turn. This is not a stall or a pass failure — the turn continued, self-recovered (Codex's own trace states, in its own words, that it would rely on direct code-path reading instead of execution, and that the review's own conclusion turns on the ACs' own counterexamples rather than a live run), and completed with a well-formed final JSON message. `codex` itself exited `0` and `--publish` validated and published cleanly. Recorded for the coordinating session's interventions ledger per the standing convention; not itself a reason to distrust this round's findings, which are grounded in direct reads of `bin/team-paths.sh` and `bin/resolve-executor.sh` that this review's own independent primary reading (below) reproduces and confirms match.

### Independent primary reading (before consulting Codex's output)

**Verifying pm-spec's own `## Prior review findings` account against the live bytes, not trusting the table.** Re-read `9ddd9b8`'s full diff end to end. The account is accurate: **step 0** genuinely adds a scratch-installed-tree resolution (`bin/`+`templates/` copied, a real `.shell-team/binding.conf` binding `tech-lead codex provider-configured low codex-cli`, resolved with `bash bin/resolve-executor.sh --print-resolved` run from inside that tree); the `- resolution:` evidence line is a new, required family in **AC15**'s grammar with its own internal row-vs-column recompute; the verdict function's clause 2 genuinely gained an equality conjunct (`the in-set arm's argv token value **equals** the - resolution: line's effort-field`), and **AC16**'s `- check:` body genuinely extracts both `EF` (from `- resolution:`) and `TV` (from `- argv:`) independently and compares them before allowing `clause-applied`; a sixth `resolved-row` channel is added and pinned `licensing`; **AC11**'s window test gained a second `awk` pass computing nearest-token distances (`dxa<dxr && dlr<dla`), which does reject the specific swapped-attribution shape round 1 named; **DP-i** gained the clarifying sentence tying "applied" to "passed to the provider and accepted, never executed at that tier"; and `## Input space` item 18 accurately describes the `bin/team-paths.sh`/`bin/resolve-executor.sh` mechanism, confirmed against the live files above (`validate_base`'s absolute-path refusal, `ROOT="."`, `$BASE_DIR/binding.conf`).

**Independently verified, not merely re-read**: ran `bash bin/resolve-executor.sh --print-resolved` against the real shipped default myself; the row shape is exactly six space-separated fields (`resolved tech-lead claude opus - claude-cli`, etc.), confirming the `awk '$5'` extraction claim and the shape AC15's regex partially — but not fully — pins (see synthesis below). Read `bin/team-paths.sh:60`–`:133` directly: `validate_base` does reject an absolute `TEAM_RUN_BASE` with the exact message the interventions entry quotes, confirming the third interventions entry (`94607b3`) is an accurate correction and not merely asserted.

**One point checked independently, matching Codex's own Finding 1/2 substance before opening its output.** **AC15**'s new `- resolution:` regex (`row=resolved tech-lead codex .* codex-cli`) does not pin the middle two fields (`$4`, `$5`) to any specific value — only the row's start and end tokens and the row-vs-`effort-field` internal recompute are checked, and **AC16**'s equality conjunct only requires the argv token to equal whatever `effort-field` says, not that `effort-field` equals the frozen protocol text's own prescribed value (`low`, from step 0's own `bind tech-lead codex provider-configured low codex-cli` literal, itself byte-locked by **AC14**'s protocol-region lock). This is real: a self-consistent but fabricated resolution row (e.g., claiming the scratch resolution produced `xhigh` when the frozen binding conf would actually produce `low`) would satisfy AC15/AC16's mutual-consistency checks. Judged **not** blocking (see synthesis) because it is the same self-attestation boundary this document explicitly and freshly draws at this exact seam — `## Non-goals` item 6 was amended in this same rework to say precisely this: "What **is** in scope, and is now checked, is their **mutual consistency**... a transcription that disagrees with itself fails, while one that is internally consistent and wrong about the world is the same already-disclosed self-attestation boundary every other evidence line stands on." The round-2 fix's own stated scope is mutual consistency, not absolute grounding against the frozen literal — it does not overclaim past what it checks.

### Codex's independent findings (raw JSON in `T-1119-codex-specreview-r2.txt`)

Codex read the spec and the live repository directly (five completed tool calls, one sandbox-rejected) and returned 4 findings, using its own severity vocabulary (P0–P3, style) — normalized per this role's severity-normalization map (P1→major, P2→minor) — each pre-labelled by Codex itself as `independently-new` or `restatement`.

1. (Codex: P1 → major; labelled `restatement`) AC15/AC16: step 0 fixes the scratch binding to `provider-configured low`, but neither AC15 nor AC16 requires the recorded row to actually contain that value — a self-consistent but fabricated value (e.g. `xhigh`) placed identically in the row, `effort-field`, and the argv token would still produce `clause-applied`. The equality conjunct proves mutual agreement between two transcribed lines, not agreement with the frozen resolution step 0 prescribes.
2. (Codex: P2 → minor; labelled `independently-new`) AC15: the new resolved-row grammar's regex accepts any row beginning `resolved tech-lead codex` and ending `codex-cli` with an unconstrained middle, while the resolver's real output is a fixed six-field shape; extra or misplaced fields could pass.
3. (Codex: P2 → minor; labelled `restatement`) AC11: the nearest-attribution test still uses only the *first* occurrence of each token/word in the whole 400-character window, so a constructed document with an early glossary-like mention followed by a later, correctly-attributed statement (or vice versa) could satisfy the distance inequalities while a real, later, swapped statement sits unexamined.
4. (Codex: P2 → minor; labelled `independently-new`) AC15/AC16: cross-field contradictions are unchecked — AC16 never compares the in-set `- run: ... value=` field against `effort-field`/the argv token, and AC15's regex allows `clause-present=yes` with `source=absent`, which should be mutually exclusive.

### My own synthesis of Codex's findings

**Finding 1 — downgraded to minor, non-blocking, and independently re-derived above before opening Codex's output.** Confirmed real by direct inspection of AC15/AC16's actual `- check:` bodies. Not upheld as blocking because it re-litigates a self-attestation boundary this exact rework explicitly and freshly draws: the amended `## Non-goals` item 6 states in so many words that mutual consistency between the resolved row and the argv token is now in scope and checked, while whether the recorded resolution reflects an honest run of the frozen binding is the same self-attestation every other evidence line in this document already carries (Non-goals item 9's "trusted operator" threat model, restated document-wide). Round 1's Major demanded that *some* mechanism connect a resolved value to the invocation rather than none at all — that demand is met. Demanding in addition that the mechanism be immune to a self-attesting party who fabricates all three lines consistently would be demanding this one seam authenticate itself more strongly than every other evidence line in the spec, which the document's own stated scope does not attempt anywhere. Worth a cheap tightening as a fast-follow (Codex's own Finding 2 gives the concrete fix), not worth blocking a second round over.

**Finding 2 — upheld as minor, concrete and cheap.** Independently confirmed: AC15's regex genuinely leaves `$4`/`$5` unconstrained beyond the row's start/end anchors and the row-vs-column internal recompute. Tightening to require `NF==6`, `$4==provider-configured`, and `$5==` the recomputed `effort-field` (Codex's own suggested fix) would close this and simultaneously narrow Finding 1's residual gap for the common case (a malformed-but-plausible row), though not the fully-adversarial self-attestation case Finding 1 names. A reasonable fast-follow, not blocking.

**Finding 3 — downgraded to minor, non-blocking, largely the same disposition as round 1's Minor #4.** The specific swap this criterion was tightened to catch (round 1's finding: two adapters and two verbs present but paired backwards, in one straightforward two-clause statement) is genuinely closed by the nearest-attribution comparison. Codex's residual scenario — a "glossary-like prefix" naming both tokens and both verbs before the real, later sentence — is a more elaborate construction than an honest engineer following `## Notes for engineer`'s explicit prescription ("write the clauses, not a summary... one clause per adapter with each verb adjacent to its own adapter token") would plausibly produce, and sits closer to the adversarially-constructed-document class `## Input space` out-of-scope item 9 explicitly declines to defend against ("no reachable authoring path that would produce it... defends against accidents"). Worth naming as a residual for a future tightening pass if AC11 is touched again for other reasons, not blocking this freeze.

**Finding 4 — upheld as minor, both halves genuine and cheap.** Independently confirmed against AC16's actual variable extraction (`SI, SO, CI, PI, PO, RS, UL, VD, EF, TV` — no variable for the in-set `- run: ... value=` field) and against AC15's `- argv:` regex (`source=(command-line-record|stream-item|absent)` unconditionally, not conditioned on `clause-present`). Both are real, narrow completeness gaps in the new grammar — redundant-field consistency and a state that should be unreachable but isn't excluded — worth a fast-follow tightening, not blocking: the verdict-relevant fields (`effort-field`, the argv token) are already checked against each other, so these gaps sit on fields the verdict function does not itself read.

### Pre-commitment bookkeeping

Round 1 landed **one Major** (live-run protocol / resolved-value-to-invocation seam) and **one Minor, upheld** (AC11 attribution-swap gap) against components of this task. Both are judged **closed** by this rework, not merely patched: the Major's core demand (some mechanism, not none, connecting a resolved value to the measured invocation) is met by step 0 plus the equality conjunct, independently verified against the live `bin/team-paths.sh` and `bin/resolve-executor.sh` bytes rather than trusted from the spec's own account; the Minor's core demand (reject the specific swapped-attribution shape) is met by the nearest-attribution comparison. **No round-2 finding, from Codex or from my own independent reading, is judged a Major against either component.** Finding 1 — the closest candidate — is judged non-blocking because it demands a strength of authentication (immunity to a self-attesting party fabricating three consistent lines) that this document's own explicitly-amended scope (Non-goals item 6) does not claim to provide anywhere in the evidence section, on this seam or any other. **The pre-commitment's two-consecutive-round trigger does not fire.** One bookkeeping gap noted for completeness, not blocking: the `## Pre-commitment` section's own "Round tally against the trigger" line still reads "No review round has run. Nothing is dropped." — stale since round 1 in fact ran and landed the Major and Minor addressed above. Recommended as a fast-follow so a hypothetical round 3 reads an accurate count rather than re-deriving one.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | resolution row's value isn't pinned against the frozen protocol's own `low` literal; a self-consistent fabrication could pass | Codex: major (P1), labelled restatement; my independent read, reached before opening Codex's output: confirmed against AC15/AC16's actual bodies | minor | Re-litigates a self-attestation boundary this exact rework explicitly and freshly draws (amended Non-goals item 6: mutual consistency is in scope, absolute grounding against the world is not, document-wide); round 1's actual demand (some mechanism, not none) is met. |
| 2 | AC15's resolved-row regex leaves the middle two fields unconstrained | Codex: minor (P2), labelled independently-new; my independent read: confirmed live against a real `--print-resolved` run | minor | Real, cheap, concrete completeness gap; also narrows Finding 1's residual for the non-adversarial case. Fast-follow. |
| 3 | AC11's nearest-attribution test still reads only first occurrences, leaving a constructed multi-mention document unclosed | Codex: minor (P2), labelled restatement; my independent read: the round-1 swap shape is genuinely closed, the residual scenario is more elaborate than the prescribed authoring shape | minor | Closer to the adversarially-constructed-document class `## Input space` item 9 explicitly declines to defend against than to a plausible authoring accident; worth naming, not blocking. |
| 4 | AC15/AC16 leave two narrow cross-field states unchecked (run.value vs effort-field; clause-present=yes with source=absent) | Codex: minor (P2), labelled independently-new; my independent read: confirmed against AC16's variable extraction and AC15's regex | minor | Real but on fields the verdict function itself does not read; the verdict-relevant fields are already mutually checked. Fast-follow. |

### Route-back

**Verdict: APPROVE (round 2).**

No Major or Blocker stands against this rework. Round 1's blocking Major and upheld Minor are both closed on independent verification (see Pre-commitment bookkeeping above); no independently-new Major surfaced in this round from either Codex's reading or my own. Freeze may proceed. The four Minor items above (Codex Finding 1's residual, Finding 2, Finding 3's residual, Finding 4) and the stale round-tally line are recommended as a single cheap fast-follow pass — bundle them if the spec is touched again for any other reason, but none blocks this freeze.

### Fast-follow disposition

- Finding 1 residual (resolution row not pinned against the frozen `low` literal) — recommend, not blocking: tighten AC15 per Finding 2's own fix (require `NF==6`, `$4==provider-configured`, `$5==`recomputed `effort-field`) as the cheapest available narrowing; the fully self-attesting case stays open by the document's own explicit design and needs no further action.
- Finding 2 (AC15 row-shape looseness) — recommend, not blocking: adopt Codex's suggested `awk` field-count-and-value check verbatim if AC15 is touched again.
- Finding 3 (AC11 residual multi-mention gap) — recommend, not blocking: out of the document's own stated threat model (accidents, not adversarial constructions); worth a note if AC11 is revisited for other reasons.
- Finding 4 (AC15/AC16 unchecked cross-fields) — recommend, not blocking: require `run.value == resolution.effort-field == argv.token` for the in-set arm, and condition `source`'s alternation on `clause-present`, if this grammar is touched again.
- Pre-commitment round-tally staleness — recommend, not blocking: update the "Round tally against the trigger" line to reflect round 1's actual outcome (one Major, one Minor, both closed) so a hypothetical round 3 reads an accurate count.

### Codex Review verdict: REQUEST_CHANGES

## Diff review (default fresh-review mode)

- Task: T-1119
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 16 files, +1075/-24 lines (`61a99f9...HEAD`; `git diff --stat`)

- raw-capture: `T-1119-codex-primary.jsonl` / `T-1119-codex-primary.txt`, `T-1119-codex-adversarial.jsonl` / `T-1119-codex-adversarial.txt`
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> --ignore-user-config --ignore-rules --ephemeral review --base 61a99f9105eab33b5440a8d11b7ef51714620eb5 --json -o "<RAW_OUT>" < /dev/null > "<RAW_JSONL>" 2>&1`
- pass-role (primary): generation
- briefing-fidelity (primary): carried — the `review` subcommand reads the working tree and the named base ref directly with its own tools (`git status`, `git diff --stat`/`git diff` against `61a99f9…`, `git diff --name-only`, targeted `sed`-range reads of the amended recipe block, adopter docs and `bin/resolve-executor.sh`); every amended file this task touches was read at least once across its four `command_execution` items before the final `agent_message` finding.
- raw-capture (primary): T-1119-codex-primary
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --ignore-user-config --ignore-rules --ephemeral --json -o "<RAW_OUT>" "$(cat <prompt-file>)" < /dev/null > "<RAW_JSONL>" 2>&1` — prompt content: a two-tier scope notice (the prompt file is the sole instruction-bearing input); an instruction to read the diff and named files directly rather than expect them pasted; the devil's-advocate framing against the applies-effort/effort-application claims across the recipe, the CI step and the four adopter docs, explicitly naming the `codex-reviewer` wrapper-hosted dispatch shape as a scenario to probe; a request for a JSON findings array of `{severity, file, line, issue, suggestion}`.
- pass-role (adversarial): generation
- briefing-fidelity (adversarial): carried — five `command_execution` items, each completed: `git status --short` + `git diff --stat`; `nl -ba` reads of `templates/binding-default.conf`, `bin/resolve-executor.sh`, `bin/check-invocation-path.sh`, `agents/codex-reviewer.md` plus a wide-context diff of the docs; an `rg -n` sweep for `appl|effort|適用|wrapper|codex-reviewer|invocation` across every named surface; full `sed`-ranged reads of README.md/README.ja.md/docs/adopting.md/docs/adopting.ja.md/skills/run/SKILL.md/check-handoff.yml; and a final targeted read of `agents/codex-reviewer.md`'s bare `codex exec` lines plus the CI step and the recipe's `applies-effort` region. This covers every surface the prompt named. (This is the pass's second invocation — the first attempt inline-quoted the prompt on the command line rather than delivering it from a file via `"$(cat …)"`, a hygiene deviation from this loop's own convention; it was discarded rather than counted, and the properly file-delivered re-run above is what this record's findings and raw-capture stem are drawn from.)
- raw-capture (adversarial): T-1119-codex-adversarial

### Invocation hygiene observations (non-licensing)

- **Non-repo read under `--ignore-user-config --ignore-rules`**: the primary (`review --base`) pass's first `command_execution` item read `<home>/.agents/skills/pr-review/SKILL.md` — a file outside this repository, under the invoking operator's own home directory — despite both ignore flags being present on the invocation. This matches the same observation the spec-review round already recorded for this task (`specreview-r1`'s own hygiene note in this file): the `codex exec review` subcommand's built-in review behavior appears to consult an operator-configured skill file regardless of `--ignore-user-config`/`--ignore-rules`, which govern the CLI's provider configuration and hooks, not a separately-discovered skill directory. The adversarial pass (a free-form prompt, not the `review` subcommand) showed no such read — every command in its trace stayed inside the repository. No instruction from that skill file appears to have influenced the returned findings (both passes' findings are grounded in files this repository ships and are independently corroborated below), but the read itself is recorded here as required.
- **Stdin-EOF diagnostic, expected**: both passes' raw `.jsonl` opens with the line `Reading additional input from stdin...` before the first JSON event, despite `< /dev/null` being present on both invocations. This is the same benign artifact this task's own frozen probe protocol already documents (`## Live-run evidence`: "Both 2026-09-03 probes carried this redirection and both raw streams open with that line") — the CLI briefly attempts a stdin read, receives immediate EOF, logs the line, and proceeds; neither pass hung or stalled.
- **Usage**: adversarial pass `turn.completed.usage`: `input_tokens=324003, cached_input_tokens=248448, cache_write_input_tokens=0, output_tokens=2227, reasoning_output_tokens=425`. The primary pass's stream carried no separate `usage` event distinct from its item sequence in this reading.
- No mutating flag, no `-c sandbox_permissions=…`, no `-m`/model flag, and no licensing-relevant injection beyond the skill-file read above appeared in either stream.
- No stall, hang or incident on either pass.

### Independent primary reading (before consulting Codex's output)

Read the diff directly (`git log --oneline`, `git diff --stat`, per-file `git diff` for the recipe block and its `contain` mirror, the two adapter headers, the CI step, all four adopter docs) and locally re-ran the new `Dogfood applies-effort` CI step against the real shipped files — it passes (`t=claude-cli m=in-process ok`; `t=codex-cli m=sandbox-read-only ok`). Cross-checked `bin/log-run.sh`'s `--effort -` → `"effort":null` claim and the two `effort-mechanism` declarations (`codex-cli` = `cli-config-override`, `claude-cli` = `cli-flag`) against the real files — both match the amended prose exactly. Independently noticed, before opening either Codex pass's output, that the amended README/adopting paragraphs sit inside a sentence explicitly scoped to "every role other than `tech-lead`" and then state, unscoped, that "on a `codex-cli` row the value is now applied" — and that `templates/binding-default.conf:43` binds the shipped default's `codex-reviewer` role to `codex`/`codex-cli`, while `agents/codex-reviewer.md`'s own three bare `codex exec` lines (AC18-locked) carry no `-c model_reasoning_effort=` argument and never will under this diff, because `codex-reviewer` is dispatched `wrapper-hosted` (its own file hosts the call), not through the `sandbox-read-only` recipe this task amends. Both Codex passes independently converged on the same core defect before I compared notes with either output.

#### Blockers

None.

#### Major

- `README.md:216-220`, `README.ja.md:218-232`, `docs/adopting.md:277-298`, `docs/adopting.ja.md:290-310` — the amended per-adapter effort sentence ("on a `codex-cli` row the value is ... applied") is stated unscoped by dispatch shape, inside a clause explicitly framed as being "for every role other than `tech-lead`." It is false for the shipped default's own `codex-reviewer`/`codex-cli` row: `templates/binding-default.conf:43` binds `codex-reviewer` to `codex`/`codex-cli` by default, `bin/check-invocation-path.sh` admits it `wrapper-hosted` (its own `agents/codex-reviewer.md` hosts the `codex exec` calls itself), and none of that file's three bare `codex exec` lines carries `-c model_reasoning_effort=`. An adopter who sets a non-`-` effort on `codex-reviewer`'s row would pass resolution and still get the value recorded, never applied — exactly the "declared but applied to no call" claim this task exists to stop being falsely-general, now newly false for a second, already-shipped path. Fix: rescope the clause to name `tech-lead`'s alternate path specifically and disclose that `codex-reviewer`'s own `codex-cli` row stays recorded-not-applied like every `claude-cli` row.

#### Minor / nits

- `templates/prompt-blocks/alternate-executor-invocation.md:18` (`applies-effort codex-cli cli-config-override`) — the declaration is adapter-wide in name while, mechanically, both of its consumers (the CI step's `invocation-recipe`-row iteration, and the recipe's own prose two paragraphs below it) already scope it correctly to the `sandbox-read-only`/`tech-lead` dispatch; `codex-reviewer`'s own file never reads it. Fast-follow: fix-now-adjacent but not required — rename to make the scoping explicit in the token itself, e.g. `applies-effort codex-cli sandbox-read-only cli-config-override`.
- `.github/workflows/check-handoff.yml:307` (`Dogfood applies-effort`) — checks declaration-vs-declaration agreement only, never the composed argv or wrapper-hosted commands. This is the spec's own stated design choice (`## Decision points`'s "why the agreement is a machine check rather than prose" paragraph; `verification-ceiling: unit-and-static`), not an unnoticed gap, so it is not a defect in this diff. Fast-follow: strengthen to an executed-argv assertion, or rename the step to make its declaration-only scope explicit, if this area is touched again.
- `README.ja.md` / `docs/adopting.ja.md` — their closing "except `tech-lead`'s alternate path" sentence was not extended with the English mirrors' added "and, on a `codex-cli` row, its effort column is applied too" clause. Subsumed by the Major fix above, which rewords this exact sentence in all four surfaces together; no separate action needed.

#### Adversarial findings

Primary (`review --base`, Codex's own `[P2]` label): "Limit effort-application claims to the tech-lead path — README.md:216-220 — When a host assigns an effort to the default `codex-reviewer` row, that role still runs through its existing Claude-hosted wrapper and the new `tech-lead` recipe never composes `-c model_reasoning_effort=...`; only `wires-role codex-cli tech-lead` exists. Saying that any `codex-cli` row applies the value therefore gives adopters an incorrect runtime guarantee."

Adversarial pass (devil's-advocate framing, `{severity, file, line, issue, suggestion}`), three findings, all Codex's own `"major"`:
1. `docs/adopting.md:287` (also named: `README.md:218`, `README.ja.md:225`, `docs/adopting.ja.md:300`) — the applied-on-any-`codex-cli`-row claim is false for the shipped `codex-reviewer` combination; contradicts the immediately preceding "execution for every non-tech-lead role remains unchanged" sentence.
2. `templates/prompt-blocks/alternate-executor-invocation.md:18` — the adapter-wide declaration cannot represent two dispatch shapes sharing one adapter token; suggests keying by adapter and admission kind.
3. `.github/workflows/check-handoff.yml:307` — the CI step checks declaration-vs-declaration agreement only, never the composed argv or wrapper-hosted commands.

Findings 2 and 3 are corroborated but downgraded from Codex's `major` on independent verification — see the ledger.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | the amended per-adapter effort claim ("on a codex-cli row the value is applied") is stated unscoped by dispatch shape in all four adopter docs, false for the shipped default's `codex-reviewer`/`codex-cli` row | primary: major (P2); adversarial: major, ×4 surfaces named; my independent read, reached before opening either Codex output: confirmed against `templates/binding-default.conf:43` and `agents/codex-reviewer.md`'s three bare `codex exec` lines | major | Confirmed real and independently reproduced; directly contradicts this task's own Goal/DP-g "no overshoot in either direction" for a role the shipped default itself binds to this adapter; not caught by AC11's window test because that test checks local word co-location, never claim scope against a second dispatch shape. Blocking. |
| 2 | `applies-effort codex-cli cli-config-override` is a single adapter-wide declaration standing for two dispatch shapes (recipe-composed vs. wrapper-hosted) that share the adapter token | adversarial: major | minor | Mechanically the declaration is read only by the CI step's `invocation-recipe` iteration and by the recipe's own prose, both correctly scoped to the `sandbox-read-only`/`tech-lead` dispatch in context; `codex-reviewer`'s own file never reads this declaration at all. No live path is actually broken; worth a fast-follow rename for clarity. |
| 3 | the new CI step checks declaration-vs-declaration agreement only, never the composed argv or wrapper-hosted commands | adversarial: major | minor | Matches the spec's own stated design (`## Decision points`, `verification-ceiling: unit-and-static`) — declaration-only scope was chosen deliberately, not overlooked. A reasonable strengthening to file forward, not a defect in this diff. |

#### Recommendation

- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings.
- The fix: rescope the "on a `codex-cli` row the value is applied" clause in `README.md`, `README.ja.md`, `docs/adopting.md` and `docs/adopting.ja.md` to name the `tech-lead` alternate path specifically (e.g., "on `tech-lead`'s `codex-cli` alternate path" rather than "on a `codex-cli` row"), and add one clause disclosing that `codex-reviewer`'s own shipped `codex-cli` row is dispatched `wrapper-hosted` and stays recorded-not-applied like every `claude-cli` row — the same three-way disclosure (tech-lead / codex-reviewer / everyone else) the paragraph's opening "reporting-only axis" framing already gestures at but does not carry through. AC11's existing window-and-attribution check is compatible with this fix (the applied/recorded word pair and both adapter tokens still co-locate correctly); no AC needs to be reopened.

#### Fast-follow disposition

- Finding 2 (rescope `applies-effort codex-cli cli-config-override` by admission kind, e.g. `applies-effort codex-cli sandbox-read-only cli-config-override`) — file-an-issue: cheap, mechanical, closes the residual ambiguity noted above for a future editor of the block; not urgent enough to hold this merge.
- Finding 3 (strengthen or rename the `Dogfood applies-effort` CI step to make its declaration-only scope explicit, or add an executed-argv assertion) — file-an-issue: the spec's own `verification-ceiling: unit-and-static` line already disclosed this scope choice deliberately; a fast-follow to tighten it is reasonable but not a defect in this diff.
- AC11's UTF-8 byte-window fragility (already disposed per the QA hand-off and this review's briefing) — no new disposition here; carried forward as already recorded.

