<!-- BEGIN review: T-1041 -->

# QA verification — T-1041

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

## check-acs.sh

`bash bin/check-acs.sh --dry-run .shell-team/specs/T-1041-freeze-ux.md` → 0 unrecognized, all 23 ACs recognized.
`bash bin/check-acs.sh .shell-team/specs/T-1041-freeze-ux.md` → **23 passed, 0 failed, 0 skipped**, run live twice (~18.7s each, matching the engineer's measured ~19s — no `CHECK_ACS_TIMEOUT` elevation needed).

## Frozen-intent alignment (v2)

`bin/check-intent.sh .shell-team/specs/T-1041-freeze-ux.md .shell-team/todo.md` → **`aligned: T-1041 v2 (1d73523a74960f736acf4cced501254e86d22c8c)`**, exit 0.

## `--print-hash` cross-checked against real specs, not just fixtures

Ran `bin/check-intent.sh --print-hash <spec>` on three real specs and compared byte-for-byte against this board's hand-computed ledger values (computed before `--print-hash` existed — the strongest available end-to-end check of pipeline identity):

| Spec | `--print-hash` output | Board ledger value | Match |
|---|---|---|---|
| T-1039 | `368984d49f33b97fac6bf3f403aa1d63faec40ff` | `368984d49f33b97fac6bf3f403aa1d63faec40ff` | yes |
| T-1040 | `09c3efff620f7ede4abbe2412ef77ddc22c041bb` | `09c3efff620f7ede4abbe2412ef77ddc22c041bb` | yes |
| T-1041 (self) | `1d73523a74960f736acf4cced501254e86d22c8c` | `1d73523a74960f736acf4cced501254e86d22c8c` | yes |

Stdout purity confirmed independently of the fixture suite: 41 bytes total, trailing byte `0a` (single LF), stderr 0 bytes. Fail-closed confirmed on a spec with no intent block at all: exit 2, `check-intent: structural: no '**Task ID**: T-NNN' line found in spec: ...`, 0 stdout bytes.

## Class-M re-freeze (v1→v2), verified independently of the board's own claim

`git diff dc1b5a6:.shell-team/specs/T-1041-freeze-ux.md HEAD:.shell-team/specs/T-1041-freeze-ux.md` shows **exactly one** changed line: AC8's `check:` line, where the tenth enumerated id's `-task-id-` segment is respelled `-taskid-` (unhyphenated) to break the `sk-`+16-char token shape the hyphenated spelling collided with (this record deliberately does not re-transcribe either full id — the frozen spec's own AC8 line, and `tests/check-intent/run.sh`'s `PH_TASKID_ID` variable, are the reference for the real value). Nothing else in the spec differs.

`bin/check-refreeze-class.sh --old-hash 70881806ed34c7af97b570c658ffbde630b56997 <v1 spec via dc1b5a6> <current spec>` → **`mechanics: T-1041 70881806… -> 1d73523a… differing=1`**, exit 0 — matches the board's `refreeze-class (v1→v2)` line exactly (`mechanics`, `differing=1`).

The redaction on the board's `old[1]:` field resolves and is accurate: `dc1b5a6` is a reachable, readable commit, and its blob for the spec at line 92 really does carry the superseded (hyphenated `-task-id-`) id — confirmed by grepping that exact blob directly (`git show dc1b5a6:.shell-team/specs/T-1041-freeze-ux.md | sed -n '92p'`), without re-transcribing the id itself here. A grep for that same superseded id's literal text against the current board returns zero matches — the board itself never transcribes the RE_TOKEN-colliding shape verbatim, consistent with the disclosed reason for the redaction. The four v2 ledger lines (`intent-ratified`, `refreeze-class`, `freeze-attestation (v2)`, `intent-hash (v2)`) are present on the board and `check-intent.sh`'s own grammar validation (`aligned`, exit 0, above) is the mechanical proof that the B-record shapes it parses (`intent-hash`, `freeze-attestation`) are conformant; the two class-M-only lines (`intent-ratified`'s `class=mechanics` field, `refreeze-class`) are, per `CONTRIBUTING.md`:98, inert to that checker by design and were checked by hand instead — both present, well-formed, and their `old[1]`/`new[1]` pair count (`lines=1`) matches the single-line diff measured above.

## Suites

- `bash tests/check-intent/run.sh` — **all 22 named assertions PASS**, plus its own shellcheck self-check (checker + runner), clean.
- `bash tests/errexit-safe/run.sh` — PASS (zero-arg contract at line 217 unaffected; part (iii)'s candidate registry gained no entry, confirmed by the suite's own completeness self-audit).
- `bash tests/codex-skeleton-hygiene/run.sh` — PASS (DP-7/DP-8 live-file locks confirm the new sweep line trips neither).
- `bash bin/check-prompt-sync.sh` — all registered prompt blocks in sync.
- `bash bin/check-pii-shapes.sh --base origin/develop` — **clean**, run after this record's own commit as well (see below) — this class fired twice this sprint (T-1040's CI incident, and this same task's mid-flight AC8 finding), so it was re-measured rather than assumed.
- Full CI-wired list per `.github/workflows/check-handoff.yml`, every `run:` step, in workflow order, scratch-path-sharing suites run strictly sequentially (not parallel) — **59/59 steps PASS** (shellcheck at the pinned 0.11.0, confirmed locally before trusting the claim; every fixture suite; every Dogfood step including the two directory-enumerating ones). Total wall time ~3m35s.
- `bin/check-provenance.sh .shell-team/provenance/T-1041.md` → conformant (4 decision entries).
- `bin/check-interventions.sh --task T-1041 .shell-team/interventions/T-1041.md` → conformant (1 entry).

## Mutation self-checks, spot-verified by hand (2 of the engineer's 6: AC9, AC13)

- **AC9** (pace-rule readability anchor): on a scratch copy of `bin/check-intent.sh` with the anchor sentence `the lines= counts above are measured, not an example` replaced, the same fixture's refusal carries **0** occurrences of that sentence while the sibling literal `sweep=mutual-satisfiability` still appears (1 occurrence) — confirms the lock tracks the real sentence, not an unrelated string. On the real, unmodified script the same fixture's refusal carries **1** occurrence. RED → GREEN confirmed by hand, independent of AC10's own automated counterfactual.
- **AC13** (row (4)/row (10) shared message, call-site count pinned at 2): baseline on the real script — message occurs **1** time, call-site count (`fail_hash_structural` as a lone-line call) is **2**. Simulated the exact defect this lock guards against — splitting the shared message into two functions and re-pointing one call site (`fail_hash_structural_row10`) — and re-measured: call-site count under the original name drops to **1**, which the criterion's own arithmetic (`nc -eq 2`) correctly flags red. Restored (baseline re-measured on the untouched script) → green.

## Adversarial fixtures beyond the shipped suite (spec's Input space)

- **Filesystem/environment state (Input space class 4), print mode**: a dangling symlink as the spec argument → `usage`, exit 2, `spec path is not a regular file`, 0 stdout bytes. An existing-but-unreadable file (`chmod 000`) as the spec argument → `usage`, exit 2, `cannot read spec file`, 0 stdout bytes. Both beyond the shipped directory-argument fixture (AC4), both fail closed with the correct classification.
- **Regex/character-set anchoring (Input space class 3)**: a `**Task ID**: T-1041` line moved inside a blockquote (`> **Task ID**: T-1041`) → `structural`, exit 2, `no '**Task ID**: T-NNN' line found`, 0 stdout bytes — confirms the full-line-anchored `TASK_ID_RE` correctly rejects a legal token in a non-target position, beyond the shipped trailing-garbage fixture (AC3).
- **Test-harness short-circuit**: exercised beyond the fixture suite by the real-spec `--print-hash` cross-checks above (plain `bash bin/check-intent.sh --print-hash <spec>` invocation, not a stub), and by AC6's own three invocation-style fixture (`bash bin/…`, `./bin/…`, PATH-bare via symlink), all re-run live.
- **Structurally inapplicable, disclosed rather than silently skipped**: Markdown/CommonMark fenced-block parsing (the checker parses an HTML-comment marker pair and one `**Task ID**:` line — no fenced-block grammar exists in its input surface); time/liveness (no long-running subprocess in the print-mode path — `awk`/`sed`/`git hash-object` over a spec-sized file); resource/budget competition beyond the already-declared-out-of-scope check-line-count ceiling (Input space explicitly declines a 10,000-line probe; nothing here widens that bound).

## Known non-findings, confirmed rather than assumed

- **AC4, AC6, AC20's pre-implementation vacuous PASSes**: read in the spec's own `## Assumptions` — each is bounded by a sibling criterion (AC1/AC2 for AC4 and AC6; AC14/AC18 for AC20) that stays red until the deliverable exists, so once implemented the bound's own green makes the vacuous criterion exercise the real thing. Confirmed this is exactly what happened: AC1/AC2/AC14/AC18 are all measured PASS post-implementation (above), so AC4/AC6/AC20 are no longer vacuous at this gate.
- **Phantom-anchor interventions entry**: `.shell-team/interventions/T-1041.md` — the orchestrator's own record of a memory-transferred quote misattributed across specs, correctly classified `assumption-contradicted`, disclosed rather than absorbed. Structurally conformant per `check-interventions.sh` above; this task's own repair was unaffected (the mktemp defect was measured, not inferred from the phantom anchor).
- **Merge-point-scoped criteria (AC20, AC21)**: both carry their own staleness disclosure in-body and are correctly left un-widened, un-merge-ranged and un-re-derived — consistent with this spec's own instruction and this task's stated scope.
- **Same-class-2 pre-commitment**: not triggered — this is the first QA round for this task, with no prior FAIL round on this board entry. AC22 (verified above) confirms all three dispositions are recorded verbatim, outside any `- check:` line.

## `## Summarized sources` — verified against primary bytes, not against the spec's paraphrase

| Source | Sites checked | Distinction claimed | Result |
|---|---|---|---|
| `bin/check-intent.sh` (whole file @ base `8049120`) | Lines 192 (`-*` unknown-flag), 586–591/596/716 (`fail_hash_structural`, 2 call sites), 626–627 (malformed, abstract grammar only), 649 (pace-rule `lines=${counted_total}/${counted_total}`), 685 (range check), 694 (`ran≠total`), 697 (`P+F≠ran`), 704 (version-N cross-check `total=$at`) | Six refusal paths print measured values; only the malformed-record path prints an abstract grammar with no spec-derived count; `fail_hash_structural` shared byte-identically by exactly 2 call sites; `-*` rejected as unknown flag | Matches exactly, line-for-line |
| `.shell-team/interventions/T-1036.md`, 4th entry | Opened in full; located the 4th `assumption-contradicted` entry (the T-1029/AC2 cross-join finding) | "the sweep decomposed each criterion but did not cross-join disclosed-reds × requirement-sets"; lesson sentence quoted verbatim | Matches exactly, including the verbatim lesson sentence |
| `.shell-team/retros/2026-08-06.md`:47 | Line 47 (Try, candidate 3) | The Try sentence names "same-class-2 の判定ロジック" while its `Owning:` field names "freeze-gate 相互充足性スイープ" — two different subjects, ratified scope is the latter | Matches exactly |
| `skills/run/SKILL.md`:45/:46/:48 (base `8049120`) | Line 45 (structural/bootstrap), 46 (attestation, `check-intent: attestation: ` with trailing space present), 48 (class-M) at base; same lines at HEAD | :45 is the definitive compute instruction; :46/:48 delegate to it in words with no recipe of their own; trailing-space literal sits on :46 | Matches exactly at base; at HEAD, :45 is rewritten to name `--print-hash`, :46 is byte-identical, the new cross-join sub-bullet is inserted as line 47 (after :46, not into it) |
| `tests/check-intent/run.sh`:102–110 | `compute_hash()` function body and its preceding comment (98–101) | Declared fixture-construction tooling, not code under test | Matches exactly |
| `tests/errexit-safe/run.sh`:217, :290–309 | Line 217 (`check-intent.sh` zero-arg pin); `derive_candidates()` P1/P2/P3 (301, 305, 308) | Zero-arg exit 2 pinned; all three candidate shapes require `>&2` | Matches exactly |
| `tests/codex-skeleton-hygiene/run.sh`:1330–1336 | Lines 1330 (`NEW_AC3`), 1334 (`NEW_AC15`) | Exact regexes for interpreter-prefixed invocation and gate-sentinel/loop-guard phrase | Matches exactly |
| `CONTRIBUTING.md`:90–98 (base `8049120`) | Lines 90 (heading), 94–97 (two record shapes + classifier), 98 ("neither record shape is parsed by `bin/check-intent.sh`") | Documents mechanics only; neither shape parsed by the checker | Matches exactly at base; the new bullet added at HEAD (line 99) states where the hash *value* comes from and does not contradict line 98's board-grammar claim |
| `bin/team-paths.sh`:81 | Lines 79–81 (`set_mode`, the leading comment) | A repeated/conflicting mode flag is a clear `die`, not silent last-one-wins | Matches exactly |
| `.shell-team/specs/T-1026-skill-md-doc-completeness.md` AC4 | Lines 47 (DP5), 77 (AC4) | Thirteen frozen literals named individually; `check-intent: attestation: ` (trailing space) called out as the one no reviewer can confirm by eye | Matches exactly — 5 + 3 + 5 = 13, as claimed |
| GitHub issue #166 | Attempted via `gh issue view 166` | Relayed, not opened by pm-spec | `environmentally-unverified` — `gh` present but the sandbox's TLS interception fails cert verification against `api.github.com`'s GraphQL endpoint; same constraint pm-spec itself declared |

No flattened distinction found at any site (declaration line or restatement site). One risk note: `agents/pm-spec.md`:86's and `skills/run/SKILL.md`'s HEAD-side prose (D6/AC16/AC14) were checked as part of the AC verification above rather than re-listed here as a separate source row, since AC16/AC14 already pin them byte-for-byte against the base blob.

## Risk notes for reviewer

- **`environmentally-unverified`: GitHub issue #166.** No primary confirmation possible from this session (sandbox TLS blocks `gh`/API access to GitHub's GraphQL/REST beyond what the network allowlist permits for plain HTTPS git). Consistent with pm-spec's own disclosure; not a new gap.
- The class-M rename (v1→v2) touches AC8's frozen `check:` line only; I independently re-derived the classifier's verdict rather than trusting the board's own claim, and it matches (`mechanics`, `differing=1`).
- `bin/check-pii-shapes.sh --base origin/develop` scans the whole stacked-train diff since `253f4b7` (develop is intentionally frozen mid-sprint per this checkout's `CLAUDE.local.md`), not just this task's own commits — clean at the time of this check, and re-confirmed after this review record's own commit (see hand-off).

<!-- END review: T-1041 -->

<!-- BEGIN review: T-1041-codex -->

### Codex Review verdict: APPROVE
- Task: T-1041
- Codex model: gpt-5.1-codex-max (config default; no `-m` override passed; confirmed via the capture's own model field)
- Verification mode: executed-tests — independently re-ran, live, on this checkout: `bash bin/check-acs.sh .shell-team/specs/T-1041-freeze-ux.md` (23/23 PASS, matching engineer/QA exactly), `bash tests/check-intent/run.sh` (22/22 named assertions + shellcheck, PASS), `bash tests/errexit-safe/run.sh` and `bash tests/codex-skeleton-hygiene/run.sh` (PASS), `bin/check-intent.sh` two-arg mode (`aligned: T-1041 v2 (1d73523a…)`), `bin/check-prompt-sync.sh`, `bin/check-pii-shapes.sh --base origin/develop` (clean), `bin/check-provenance.sh`, `bin/check-interventions.sh`, `bin/check-handoff.sh`, `bin/check-board-headings.sh --base origin/develop`, `bin/check-commit-identity.sh --base origin/develop`, and `shellcheck` on both changed files — all independently green, not accepted on the board's word. Also independently re-ran `bin/check-refreeze-class.sh --old-hash 70881806… <v1 via dc1b5a6> <current>` on a scratch copy (`mechanics … differing=1`, matching the ledger) and confirmed commit separation for AC20 via `git show --stat` on both `skills/run/SKILL.md` commits. Ran a from-scratch adversarial fail-closed probe of `--print-hash` beyond the shipped 22 fixtures (symlink-to-real-file, dangling symlink, FIFO, `chmod 000`, unwritable `TMPDIR`, UTF-8 BOM, a marker line corrupted with only a trailing space, binary bytes inside the block body, a genuinely closed downstream pipe, and `ulimit -f`), and delegated a second, independent adversarial pass to Codex itself (below).
- Diff scope: 11 files, +825/-9 (`git diff --stat 8049120...HEAD`)

#### Mandatory disclosure item — the class-M re-freeze (v1→v2), verdict

Verified independently rather than accepted on the board's or QA's word, on all four axes the task asked for:

1. **Classifier exit** — reproduced live on a scratch copy of the v1 blob: `bin/check-refreeze-class.sh --old-hash 70881806ed34c7af97b570c658ffbde630b56997 <v1 via dc1b5a6> <current spec>` → `mechanics: T-1041 70881806… -> 1d73523a… differing=1`, exit 0. Matches the board's `refreeze-class (v1→v2)` line exactly. Confirmed the delta really is confined to one `- check:` line (git diff between the two blob versions shows exactly one changed line, itself a `- check:` line on both sides) and does not fall into the disclosed `crc-blindspot-swapped-checks` blind spot (that blind spot needs a *swap between two different criteria*; this is a same-position rename inside one criterion).
2. **Ledger grammar** — the `intent-ratified` line matches `CONTRIBUTING.md`'s stated shape field-for-field (date, `vK→vK+1`, `class=mechanics`, standing-grant pointer, reason). The `refreeze-class` line matches the shape too, **except one field**: `CONTRIBUTING.md`'s grammar says `old[1]: <the superseded line>`, which on its literal text means verbatim inline text, and this record's `old[1]` substitutes a redaction plus a git-commit locator instead. This is a real, disclosed departure from the documented grammar's letter — see the ledger row below (downgraded from Codex's own P2/major to minor: the property that grammar exists to protect, auditability, is independently confirmed intact — see next item — so this is a documentation-lag gap, not a defect in what was recorded).
3. **Redaction's auditability** — confirmed accurate and reachable myself, independently of QA's identical check: `dc1b5a6` is a readable commit, and its blob for the spec really carries the superseded id at the AC8 line (read directly via `git show dc1b5a6:… | sed -n '92p'`, without re-transcribing it into this record). A repo-wide `git grep` for that superseded id's literal text returns zero hits in tracked files — the board, the spec, and the test suite are all clean of it post-repair. The auditability property the verbatim-pair requirement exists to protect therefore holds end-to-end, just via one extra `git show` hop instead of an inline read.
4. **Is `contradictory` an honest token, or a stretch?** — Honest, with one worth-tightening edge. The closed vocabulary (`broken-as-command` / `vacuous` / `contradictory`) has no token for "this line is a valid, functioning, non-vacuous assertion that nonetheless collides with an unrelated repo-wide CI gate's own pattern" — `docs/tuning-oversight.md`'s own definition text scopes `contradictory` to "another frozen criterion or its own prose," which is written with intra-spec contradictions in mind, not an external merge-gate scanner. But the underlying fact the token asserts — genuine, measured mutual unsatisfiability between AC8's literal id text and `check-pii-shapes.sh`'s `RE_TOKEN` pattern (confirmed myself: `sk-[A-Za-z0-9_-]{16,}` unboundaried, and the superseded id's `-task-id-` segment spells exactly that shape) — is real and was measured live (`bin/check-pii-shapes.sh` finding, twice), not asserted from a hunch. `contradictory` is the only one of the three tokens that could honestly describe this defect (it is not a syntax/runtime failure, and it is not vacuous), and the board's own prose discloses precisely and honestly what it means by it ("a standing CI criterion") rather than hiding the stretch. **Fast-follow, not blocking**: widen `docs/tuning-oversight.md` / `CONTRIBUTING.md`'s definition of `contradictory` to explicitly include a standing CI/verification gate as a valid contradiction partner, since this is disclosed as the fourth sprint-wide instance of this exact class and the next instance will face the identical vocabulary gap.

**Environmentally-unverified note (per role convention)**: GitHub issue #166's body was not independently re-read against its primary source by this role either (no GitHub read access from this sandbox) — internal consistency verified against the spec/prompt text I was given; issue-canon match environmentally-unverified (relies on the orchestrator's primary confirmation), consistent with pm-spec's and QA's own identical disclosure.

#### Blockers
- none

#### Major
- none (see Synthesis audit ledger — four Codex-native P1/P2 findings and one of my own were all downgraded to minor after independent verification; none survive as major)

#### Minor / nits
1. **`--print-hash`'s write is unreachable via SIGPIPE for a genuinely closed downstream pipe** (`bin/check-intent.sh:359`, D3) — found independently by both my own primary pass and Codex's primary pass. `printf '%s\n' "$computed_hash" || fail_usage "..."` cannot run its `fail_usage` fallback when the write dies to Bash's default SIGPIPE disposition: measured live, `bash bin/check-intent.sh --print-hash <spec> | true` exits **141** (SIGPIPE), not the "classified usage(2) exit through the existing `die`" D3's own prose promises for a write failure. Confirmed this is **not novel to this diff**: the pre-existing `aligned:` line at :828, which is explicitly `|| true`-guarded and which D3 itself cites as "the courtesy print whose guard is correct," dies identically (`RC=141`) under the same closed-pipe condition when I ran it side-by-side. Input Space item 9 ("a stdout that is a closed or already-broken pipe") is declared reachable, but none of the 22 shipped fixtures exercises it. The core safety property — no partial value reaches a reader, and the exit code is never 0 — holds throughout; only the *specific classification* D3 promises does not fire for this one input class. Fast-follow.
2. **`ulimit -f` mid-extraction dies via SIGXFSZ (exit 153), not a classified `usage` exit** (`bin/check-intent.sh:340`, shared extraction pipeline, both modes) — same class as (1), found in my own adversarial pass: `( ulimit -f 1; bash bin/check-intent.sh --print-hash <spec> )` → exit 153, a raw `awk`/`sed` "Filesize limit exceeded" dump on stderr rather than `check-intent: usage: …`. Pre-existing (the write this hits is in the extraction step shared by both modes, untouched by this diff) and Input Space item 7 explicitly declares this class reachable. No partial stdout, never exit 0. Fast-follow, folds naturally into the same signal-handling fast-follow as (1).
3. **`--help` anywhere in the argument list, including alongside `--print-hash`, still short-circuits to the full help text on stdout with exit 0** (`bin/check-intent.sh:206`, found by Codex's adversarial pass, reproduced by me live: `bash bin/check-intent.sh --print-hash --help` prints ~5KB of help text and exits 0). This is **pre-existing script-wide precedent, not a new gap `--print-hash` introduces**: confirmed the identical short-circuit already held at the base ref (`bash <base-ref-checkout> --help extra-arg` also exits 0 with help text, ignoring the invalid extra argument) — `--print-hash` simply inherits the same "help wins over everything else in this loop" convention uniformly, rather than being singled out. Practical risk is low: the output is immediately, visibly not a 40-hex value (thousands of bytes vs. 41), so no automated producer or human transcribing a ledger line would mistake it for a hash. None of AC1/AC4's fixtures exercise this exact flag combination, so it is a genuine, if narrow, coverage gap relative to AC1's "prints exactly 41 bytes" framing. Fast-follow (add a fixture; optionally make mode-exclusivity checking flag-order-independent).
4. **`computed_hash` is accepted on `git hash-object`'s exit status alone, with no `^[0-9a-f]{40}$` shape check before either mode uses it** (`bin/check-intent.sh:342`, found by Codex's adversarial pass) — on a repository initialized with git's SHA-256 object format, `git hash-object --stdin` returns 64 hex characters, not 40; `--print-hash` would then print a 64-hex value and exit 0, breaking AC1's "exactly 40 lowercase hex characters" contract for that environment. Verified this is **whole-checker pre-existing, not introduced here**: `HASH_FULL_RE='^[[:space:]]+- intent-hash \(v([0-9]{1,4})\): ([0-9a-f]{40})$'` already hardcodes `{40}` at the base ref, unrelated to this diff, and both modes share the identical `git hash-object` call site — this repo (`git rev-parse --show-object-format` → `sha1`) and the overwhelming majority of git repositories today are unaffected. If it ever manifested, the two-argument mode's own board-grammar parsing would refuse the resulting record as malformed rather than silently misverify it — the system stays fail-closed end-to-end, just with a less specific refusal. Fast-follow (validate the shape explicitly and fail closed with a clear message, rather than relying on downstream refusal).
5. **Inherited `BASH_ENV` reaches the checker before any of its own code can run, on every documented producer surface, including the one `skills/run/SKILL.md`:45 rewrites** (found by Codex's adversarial pass) — `bash bin/check-intent.sh …` is subject to Bash's own `BASH_ENV` startup-file sourcing before check-intent.sh's shebang body executes at all, so a hostile inherited hook can print bytes to stdout or redirect it before the script starts; the script cannot defend against this by construction. Verified **byte-for-byte pre-existing**: the base ref's `skills/run/SKILL.md`:45 already read "run `check-intent.sh <spec> <board>`" with the identical bare invocation, unrelated to this diff's rewrite (which only changes what happens once inside the `structural` branch, not how the checker is invoked); and this repository's own `tests/check-intent/run.sh` already carries dedicated BASH_ENV-leak fixtures elsewhere in the file, showing this is a known, separately-tracked class rather than a defect newly introduced by T-1041. Input Space item 7 declares it reachable, and "handled correctly" here is best read as parity with the two-argument mode's own identical, pre-existing exposure — which holds. Fast-follow (a repo-wide `env -u BASH_ENV` wrapper on every check-intent.sh producer call site, or an explicit documented carve-out, is a broader question than this one task).
6. **The new cross-join sweep step is indented as a sibling of the exit-code branches (`aligned`/`structural`/`attestation`/`drift-detected`) rather than nested under the `attestation` branch whose sweep it extends** (`skills/run/SKILL.md:47`, found independently by both my own reading and Codex's adversarial pass). D7's own text calls it "a new indented sub-bullet," and it is indented — but at the *same* list depth as the other branch bullets, not one level deeper under `attestation` specifically, so a reader could read it as an unconditional action rather than part of that branch's sweep. Read in context it is very likely fine (its own prose reuses "sweep"/"decomposition" vocabulary that only the `attestation` branch defines, and the `class-M` branch's "exactly as the attestation branch above requires" phrasing means the cross-join step needs to apply there too — which the flat, sibling placement actually supports better than nesting it one level deeper would have). This is prose consumed by an LLM orchestrator, not parsed by any grammar (AC19's DP-7/DP-8 regex locks both independently confirmed to match zero lines), so the practical risk is low. Fast-follow (an explicit one-clause scoping phrase would remove the ambiguity for a human skimming the list).

#### Adversarial findings
Triggered by the role's own default rule (a P2/major-normalized finding — SIGPIPE — surfaced in the primary pass). Ran a second, independent `codex exec` pass with an explicit devil's-advocate framing against `bin/check-intent.sh`'s new mode, the two refusal-message changes, the new `skills/run/SKILL.md` line, and the class-M ledger record, told about the already-confirmed SIGPIPE finding and asked not to merely restate it. Codex returned four new findings (P1 `--help` short-circuit, P1 object-format/64-hex dependency, P1 `BASH_ENV`, P2 class-M `old[1]` redaction) — all four independently reproduced or independently re-derived by me before acceptance (live flag-combination tests; a read of the pre-existing `HASH_FULL_RE`/base-ref `SKILL.md` bytes; a repo-wide `git grep` for the redacted id). None was accepted on Codex's word alone; all four are addressed in the Minor/nits section and the mandatory-disclosure-item section above, with every downgrade recorded in the ledger below.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | SIGPIPE bypasses `\|\| fail_usage` on a closed stdout pipe (Codex primary: P2 → major by the normalization map; also found independently in my own primary pass) | primary: major | minor | Independently reproduced (`RC=141`); confirmed identical, pre-existing behavior on the base ref's own `aligned:` line under the same condition — not novel to or worsened by this diff; the core "no partial value + exit 0" property still holds (process dies, exit ≠ 0). |
| 2 | `ulimit -f` mid-extraction dies via SIGXFSZ, not a classified exit (found in my own adversarial probing, same class as #1) | primary/self: minor (assigned directly, no Codex-native label) | minor | Same reasoning as #1: pre-existing, shared extraction step, no partial-value-at-exit-0 violation. |
| 3 | `--help` anywhere short-circuits to help text + exit 0, including combined with `--print-hash` (Codex adversarial: P1 → blocker by the normalization map) | adversarial: blocker | minor | Reproduced live; confirmed byte-for-byte identical short-circuit already present at the base ref for `--help` combined with any other argument — a pre-existing, uniformly-applied script convention, not a new gap singled out for `--print-hash`. Output is immediately, visibly non-hash-shaped; no shipped or plausible automated caller would record it. |
| 4 | `computed_hash` never validated against `^[0-9a-f]{40}$`; a SHA-256-object-format repo could print 64 hex chars at exit 0 (Codex adversarial: P1 → blocker) | adversarial: blocker | minor | Confirmed `HASH_FULL_RE`'s `{40}` assumption is whole-checker and pre-existing (base ref, unrelated to this diff); this repository is SHA-1; a mismatch would be refused downstream by the two-argument mode's own board-grammar check rather than silently misverified — system stays fail-closed end-to-end. |
| 5 | Inherited `BASH_ENV` can contaminate or redirect stdout before the checker's own code runs, on the producer surface `skills/run/SKILL.md`:45 names (Codex adversarial: P1 → blocker) | adversarial: blocker | minor | Confirmed the base ref's `skills/run/SKILL.md`:45 already invoked the checker the identical, unguarded way before this diff touched that line; confirmed this repository's own test suite already carries separate, pre-existing BASH_ENV-leak fixtures — a known, tracked class, not introduced or worsened here. Declared reachable by Input Space item 7; "handled correctly" reads as parity with the (identical) two-argument-mode exposure, which holds. |
| 6 | Class-M `refreeze-class` record's `old[1]` field substitutes a redaction + git locator instead of the verbatim superseded line `CONTRIBUTING.md`'s grammar literally specifies (Codex adversarial: P2 → major) | adversarial: major | minor | Independently reconfirmed the redaction resolves accurately and the superseded id is absent from every tracked file post-repair (auditability, the property the verbatim requirement protects, is intact via one extra `git show` hop); the gap is that `CONTRIBUTING.md`'s grammar text was never updated to bless this alternative, a documentation-lag question rather than a defect in what was recorded. Recorded as its own mandatory-disclosure-item finding, not only here. |
| 7 | Cross-join step indented as a sibling of the exit-code branches rather than nested under `attestation` (found independently by both my own reading and Codex's adversarial pass, no Codex-native severity given — treated as style/nit by Codex, minor by me) | adversarial: style/nit | minor | Practical risk assessed low (LLM-consumed prose, shared "sweep" vocabulary ties it to the right branch in context; the flat placement plausibly serves the `class-M` branch's own back-reference better than deeper nesting would); AC19's structural locks independently confirmed unaffected. |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- SIGPIPE / SIGXFSZ signal-death instead of a classified exit for `--print-hash`'s own write and the shared extraction step (Minor findings 1–2) — **file-an-issue**: either trap/ignore the relevant signal around these two write sites so `fail_usage`'s classified path is reachable, or narrow D3's prose to disclose the signal-death case explicitly. Low urgency (pre-existing, no safety violation).
- `--help` short-circuit over `--print-hash`'s mode exclusivity (Minor finding 3) — **file-an-issue**: add a fixture for the flag-combination gap; optionally make help-precedence explicit in the D1 prose rather than leaving it implicit script convention.
- Unvalidated `computed_hash` shape / git object-format dependency (Minor finding 4) — **file-an-issue**: add an explicit `^[0-9a-f]{40}$` shape check before either mode consumes `computed_hash`, failing closed with a clear message instead of relying on downstream board-grammar refusal.
- Inherited `BASH_ENV` on every check-intent.sh producer call site (Minor finding 5) — **file-an-issue**: fold into whatever broader BASH_ENV-hardening ticket this repo already tracks (this file's own test suite shows the class is known); not specific to T-1041.
- Class-M ledger grammar has no documented content-addressed alternative to a verbatim `old[i]`/`new[i]` pair (Minor finding 6 / mandatory-disclosure-item 2 and 4) — **file-an-issue**: widen `CONTRIBUTING.md`'s "Re-freezing a frozen intent block" section and `docs/tuning-oversight.md`'s `contradictory` definition to explicitly permit a redaction-plus-locator form when transcribing the superseded line would itself re-trip a standing CI gate, and to name a standing CI/verification gate as a valid contradiction partner — this is the fourth sprint-wide instance of this exact class (issue #178) and the next instance will hit the identical documentation gap.
- Cross-join step's sibling indentation in `skills/run/SKILL.md` (Minor finding 7) — **file-an-issue**: add one clause making the step's scope (runs as part of the attestation branch's own sweep, wherever that sweep is invoked including from class-M) explicit in the prose itself, removing the reliance on shared vocabulary alone.

<!-- END review: T-1041-codex -->
