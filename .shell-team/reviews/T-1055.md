### Codex Review verdict: REQUEST_CHANGES
- Task: T-1055
- Codex model: gpt-5-codex (via `codex exec`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: T-1055's own 5 commits (`fc81038`..`be12199`, isolated from the stacked `feature/1054-binding-config`/`feature/1053-...` history per the task prompt) — 12 files changed, +2088/-1 lines

All 16 frozen `- check:` lines were independently re-run live and PASS (matches the engineer/QA hand-off exactly); `bash tests/check-adapter/run.sh` (65/65), `shellcheck` on both new files, and `bin/check-intent`/`check-provenance`/`check-interventions` were also independently re-confirmed clean. The scope, records, and the two live effort-mechanism measurements (`codex --version` → `codex-cli 0.145.0`; `claude --version` → `2.1.226 (Claude Code)`) all check out. Issues #203 and #221 were opened live this round (`api.github.com`) and match the spec's characterization of design points 1/4/5 and DP9 respectively — no issue-canon disclosure is needed this round.

The problem is not in what the frozen criteria test — it's in what they don't. This is verification-mechanism work whose entire value proposition is "never returns success on input it could not fully evaluate," and a battery of direct reproductions against `bin/check-adapter.sh` (mine) plus an independent adversarial Codex pass (which converged on several of the same gaps and surfaced others) show that guarantee does not hold in several concrete, reproducible ways.

#### Blockers

- `bin/check-adapter.sh:262-266` (`load_adapters_registry`) — A malformed row in `templates/binding-adapters.txt` (T-1054's shipped, plugin-owned adapter allowlist, read by every mode of this checker) is **silently skipped** rather than refused: `[ "${#f[@]}" -eq 2 ] || continue`. No character-class check, no duplicate-adapter-token check. Reproduced against the **real shipped file**: appending `claude-cli claude extra-junk` to `templates/binding-adapters.txt` and running `bash bin/check-adapter.sh` still prints `check-adapter: valid: ...` and exits `0`, while `bash bin/check-binding.sh --config templates/binding-template.conf` (parsing the *identical* file) correctly refuses `registry-malformed` at exit `2`. This is exactly DP12's own stated threat model ("a stray edit" to a plugin-shipped file) and directly contradicts the Goal's central claim. Fix: mirror `check-binding.sh`'s strict two-token/character-class/duplicate validation of this same file (folding into the existing `contract-unreadable` token per provenance decision #1's own precedent, or a symmetrical extension of `contract-malformed`, is enough — no 22nd token is obviously required, but the spec's "closed 21-token" freeze means this needs a provenance-recorded decision, not an ad hoc fix).

#### Major

- `bin/check-adapter.sh:365-372` (`load_contract`'s completeness gate) — Only `field`, `channel`, and per-role `role-board-authority` non-emptiness are enforced. `error-class` rows and the mandatory `none` `effort-mechanism` row have **no enforced minimum** at all. Reproduced: stripping every `error-class` line from a copy of `templates/task-envelope.txt` and running `--contract` against it still validates `0`. Nothing in the checker itself protects the "four required error classes" / "one `effort-mechanism none` row" invariants AC2's frozen test currently only happens to observe because the *shipped* file is correct today.
- `bin/check-adapter.sh:318-320` (`schema` parsing) — No `SUPPORTED_SCHEMA_VERSIONS` allowlist analogous to `check-binding.sh`'s `schema_version_supported()`. Reproduced: a scratch contract with `schema 2` plus matching `envelope-schema 2` definitions validates cleanly even though this script's own logic is schema-1-specific. A future, genuinely incompatible schema bump that happens to keep the existing row shapes would pass silently.
- `bin/check-adapter.sh:533-545` (`validate_all_definitions`) — Default (no `--adapter`/`--binding`) validation only checks whatever `*.txt` files are *present*, never that the file set equals T-1054's two-row allowlist. Reproduced: a definitions directory containing only `claude-cli.txt` (i.e. `codex-cli.txt` accidentally deleted) still validates `0`. This directly **falsifies the spec's own `## Input space` claim** ("no definition file for a bound adapter... is reachable and fails closed") for exactly this mode — it fails closed for `--adapter`/`--binding` (which look up a specific file), but not for the default whole-directory mode AC2 and the CI dogfood step actually exercise.
- `bin/check-adapter.sh:506-511` (`field-coverage-gap`) — Only checks that a `carries` row *exists* for each field, never that a **required** field's declared channel is meaningfully carried. `not-carried` is a legitimate channel value (correctly, for genuinely-uncarried fields), but nothing stops a definition from mapping every required `in` field — `task-id`, `role`, `request` — to `not-carried` and still passing. Reproduced against `claude-cli.txt` with `task-id` remapped to `not-carried`.
- `bin/check-adapter.sh:578-594` (`--binding` mode) — The delegated canonical binding from `check-binding.sh --print-binding` is trusted for shape: non-`bound` lines are silently skipped, and there is no re-assertion of "exactly six unique-role rows." Currently unreachable via the real, correctly-tested sibling script, but this is exactly the delegation-confusion class the review was asked to probe — a future bug in `check-binding.sh` that emitted a truncated or partial `bound` set would produce a false-successful cross-check here rather than a refusal.
- **AC8's "two-directional set equality" is materially weaker than DP2 claims.** It compares only the *field-name* token set between the prose and the registry — never each field's declared direction or requiredness, and never the `channel`/`error-class`/`effort-mechanism`/`role-board-authority` tables at all. Reproduced live: editing the doc's `task-id` bullet from `(in, required)` to `(out, required)` (registry unchanged) left `AC8: PASS` — a genuine drift between the two contract forms that the review specifically asked about, undetected. (Change reverted immediately after the test; `git status --short` confirmed clean before and after.)
- **The `status` field's "closed vocabulary" has no machine form at all.** `docs/loop-engineering/task-envelope.md:76-79` promises status comes "from the contract's closed status vocabulary," but unlike `channel`/`error-class`/`effort-mechanism` (each a real, extensible directive type in `templates/task-envelope.txt`), there is no `status-value` directive anywhere — confirmed by grep (`CONTRACT_DIRECTIVES` has no such entry, and `templates/task-envelope.txt` has no such rows). T-1056 will have to reopen this contract (add a new directive type to both the registry and the checker) to get concrete status tokens, exactly what this task's own stated purpose was to prevent.
- **The per-adapter `carries` mapping cannot correctly express `board-transition` across all roles an adapter may legally bind to.** `role-board-authority` is role-scoped (`writes` vs `proposes`); `carries` is adapter-scoped. Nothing in `check-binding.sh`'s grammar restricts which of the six roles may bind to which adapter (only provider/adapter pairing is checked) — reproduced: a binding config assigning `codex-reviewer` (a `proposes`-authority role) to `claude`/`claude-cli` is accepted by both `check-binding.sh` and `check-adapter.sh --binding`. `templates/adapters/claude-cli.txt` unconditionally declares `carries board-transition board` (i.e. this adapter *writes* the board itself), which is simply wrong for a `proposes` role bound to it. This is a real, currently-reachable cross-cutting gap between two of the contract's own design axes, not a hypothetical.

#### Minor / nits

- `bin/check-adapter.sh:338,344,350,482` — Duplicate `channel`/`error-class`/`effort-mechanism`/`effort-value` rows are silently deduplicated (`in_list ... || SET+=(...)`) rather than refused, unlike `role-board-authority`'s explicit duplicate check a few lines below. Low-severity but inconsistent within the same file.
- `bin/check-adapter.sh:567-568` (`--binding`) — Any nonzero exit from `check-binding.sh --print-binding` — a genuine content refusal *or* an unrelated internal crash (e.g. its own `team-paths.sh` becoming unreadable) — is collapsed uniformly into `binding-invalid`. Fine and well-tested for genuine refusals (DP10/AC7); would mislabel an unrelated infrastructure failure as a host config mistake. Low likelihood given the sibling script's own maturity.

#### Adversarial findings

An independent adversarial Codex pass (devil's-advocate framing, same files) converged on 6 of the 8 Major findings above without prompting (the malformed-registry-row gap, the schema-version gap, the whole-directory-completeness gap, the vacuous-`not-carried`-coverage gap, the delegated-output-shape-trust gap, and AC8's weak field-tuple comparison — the last extended to note the `channel`/`error-class`/`effort-mechanism`/`role-authority` tables aren't cross-checked between prose and registry at all, beyond field names) and additionally surfaced the `board-transition`/role-authority cross-cutting mismatch and the duplicate-row dedup minor, both folded in above. No adversarial finding was rejected; all were corroborated either by my own direct reproduction or by code reading matching the described mechanism exactly.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `status` field's vocabulary has no machine-checkable form | primary: P1 (→ blocker per map) | major | Honestly disclosed in the doc's own text ("this cut does not freeze the status vocabulary's membership") and no envelope instance exists yet to misbehave against (DP1) — real design-completeness gap for T-1056, but not a currently-observable incorrect behavior, so downgraded from the mapped blocker to major. |
| 2 | Malformed rows in `templates/binding-adapters.txt` silently accepted | primary: P2 (→ major per map); adversarial: major | blocker | Empirically reproduced against the real shipped file in every mode of the checker, with a direct, working side-by-side contrast against the sibling validator (`check-binding.sh`) correctly refusing the identical corruption. Directly and concretely violates the task's central fail-closed claim on a file this checker depends on in every mode — upgraded past the mapped major. |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1055 entry back to `READY_FOR_ENG` with these findings linked. Given the volume and mutual corroboration of Major findings against one component (`bin/check-adapter.sh`'s fail-closed guarantees), the spec's own pre-commitment section's factual trigger ("two consecutive rework rounds of new independent Blocker/Major findings against one component") is worth the engineer reading before starting rework, even though this is round 1.

#### Fast-follow disposition
- Duplicate `channel`/`error-class`/`effort-mechanism`/`effort-value` rows silently deduplicated instead of refused — file-an-issue (low severity, low likelihood of accidental triggering, but a real inconsistency with the file's own `role-board-authority` duplicate-detection a few lines away; cheap to fix alongside the Major registry-completeness item above rather than in this round in isolation).
- `--binding`'s uniform collapse of any `check-binding.sh` nonzero exit (genuine refusal or unrelated crash) into `binding-invalid` — file-an-issue (intentional and correct for genuine refusals per DP10/AC7; the residual risk is masking an unrelated infrastructure failure as a host config mistake, which is low-likelihood given the sibling script's own maturity, but worth a small hardening note for whoever next touches this delegation).

---

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1055
- Codex model: gpt-5-codex (via `codex exec`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: round 2's rework, `be12199`..`403670c` (class-B re-freeze v1→v2, `7ebc028`; implementation R1–R9, `0437f11`; records+board, `aa53f26`; QA verdict, `403670c`) — 12 files changed, +905/-131 lines

All 17 frozen `- check:` lines were independently re-run live and PASS (matches QA's 17/17); `bash tests/check-adapter/run.sh` (88/88, matches QA's count exactly), `shellcheck` on both files, `check-intent` (aligned at v2, `8299ddec78a1ef127355a32df3338406106627ed`), `check-provenance` (8 entries) and `check-interventions` (4 entries) were all independently re-confirmed clean, and the CI `--doc` dogfood step is wired. Issues #203/#221 were re-confirmed live in round 1 and nothing in this round's diff touches that ground.

**Verifying each of round 1's nine findings against the actual diff (`git show 0437f11` + live reproduction on the current tree, every mutation performed on scratch copies, tracked files re-checked clean with `git status --short` after each):**

| Round-1 finding | Status | Evidence |
|---|---|---|
| Blocker — malformed `binding-adapters.txt` rows silently skipped | **Closed** | Reproduced against the real shipped file: appending a 3-field row now yields `registry-malformed`, exit 2 (was exit 0). |
| Major — no error-class/effort-mechanism-`none` completeness floor | **Partially closed** — see new M2 below | Zero `error-class` rows now correctly refused `contract-incomplete`; but a single *arbitrary* error-class row (not one of the four AC2-required tokens) still validates. |
| Major — no contract schema-version allowlist | **Closed** | A scratch `schema 2` contract with matching `envelope-schema 2` definitions now refuses `contract-malformed`/"unsupported schema version". |
| Major — whole-directory mode doesn't check file-set completeness | **Closed** | A definitions directory missing `codex-cli.txt` now refuses `definition-missing`. |
| Major — `field-coverage-gap` satisfiable via `not-carried` on a required field | **Closed** | `task-id` mapped to `not-carried` now refuses `field-coverage-gap`. |
| Major — `--binding` trusts the delegated output's shape | **Partially closed** — see new M3 below | Truncated/incomplete `bound` sets are now refused; a missing/malformed leading `schema` line, and the bound row's own `provider`/`model` tokens, are not. |
| Major — AC8 (now `--doc`) compares field names only, not direction/requiredness | **Closed for the primary case, gap remains** — see new M5 below | The exact round-1 reproduction (`task-id` direction flip) now refuses `doc-drift`; a *duplicated, contradicting* bullet for the same field does not. |
| Major — `status` field's vocabulary has no machine form | **Closed** — see new M1 below for a residual gap | `status-value` directive shipped, exactly-one-`success` enforced; token-level uniqueness is not. |
| Major — `board-transition`'s per-adapter `carries` can't express per-role authority | **Closed, foundation sound — see the Blocker below for the residual gap** | The `board` channel is retired from the shipped registry, `claude-cli.txt` now carries `board-transition` via `stdout`, `authority-channel-conflict` fires correctly on the `not-carried` join, and the cross-bind scenario I reproduced in round 1 (`codex-reviewer` bound to `claude-cli`) is now correctly unproblematic by construction rather than by a new special case. |

**DP13 as a contract foundation for T-1056/T-1057**: the redefinition itself — a `carries` row states a return path, never an authority, with the authority question left entirely to the role-scoped `role-board-authority` table — is the right fix and a sound foundation. The residual gap (below) is that the *implementation* doesn't yet make the channel vocabulary itself authority-neutral by construction; it only removes the one authority-implying token that happened to exist in v1.

A fresh Codex pass (primary + adversarial, both `gpt-5-codex`) against the round-2 diff, corroborated by my own live reproductions, found the fixes are real but incomplete in several of the same places:

#### Blockers

- `bin/check-adapter.sh:414-419` (`channel` parsing) — The channel vocabulary has **no semantic constraint at all**: any token matching `TOKEN_RE` is accepted, including the literal retired `board` token or an equivalent authority-implying synonym (e.g. `writes-board`). Reproduced live: a scratch contract declaring `channel board` plus `templates/adapters/claude-cli.txt`'s `carries board-transition` remapped to `board` validates cleanly in **both** default mode and `--binding` mode — including with `codex-reviewer` (a `proposes`-authority role) bound to it, reproducing the exact v1 category error DP13 was written to eliminate. The only protection today is `tests/check-adapter/run.sh`'s `ca-board-channel-retired` case, which is a regression assertion on the *shipped* file (real and working — it would catch a reintroduction in `templates/task-envelope.txt` itself before merge), not a structural refusal in the checker. DP13's own stated design principle — "making the wrong state unrepresentable is preferred here over detecting it after the fact" — is not actually achieved by the current implementation for a `--contract`-overridden or future-registry-edited case. Fix: either explicitly refuse the literal `board` token wherever a channel is declared (closing the exact reproduced case, cheap and testable), or design a durable authority-neutral channel-kind distinction if a stronger guarantee is wanted.

#### Major

- `bin/check-adapter.sh:420-426` (`status-value` parsing) — No token-uniqueness check. `status-value ok success` and `status-value ok failure` can both be declared for the same token `ok`; the completeness gate counts row-*kinds* in aggregate (exactly one `success`, at least one `failure`) without ever checking that one token isn't claimed as both. Reproduced live against the real shipped contract plus one added contradicting row.
- `bin/check-adapter.sh:451-452` (error-class completeness) — Only non-emptiness is enforced (`[ "${#ERRORCLASS_SET[@]}" -gt 0 ]`), not the four specific tokens AC2's own frozen test requires (`executor-unavailable`, `capability-unsupported`, `invocation-failed`, `contract-violation`). Reproduced live: a contract with a single arbitrary `error-class zzz-arbitrary` row (none of the four required) still validates `0`. Same residual-gap shape as round 1's finding — narrowed, not closed.
- `bin/check-adapter.sh:718-750` (`--binding` delegated-output reassertion) — Two gaps. (a) `binding_schema_seen` is set when a `schema` line is seen but **never checked at the end** — a delegated output with zero schema lines and six otherwise-valid `bound` rows is never refused for that. Reproduced live against a fake sibling `check-binding.sh` (swapped in via a scratch `bin/`+`templates/` tree so the real files are untouched) that emits exactly that shape: `check-adapter.sh --binding` reports `valid`. (b) The bound row's own `provider`/`model` tokens (`bf[2]`/`bf[3]`) are extracted into the array positions but never read or cross-checked anywhere in the reassertion loop — confirmed by code reading (only `bf[1]` role, `bf[4]` effort, `bf[5]` adapter are ever referenced).
- `bin/check-adapter.sh:804-809` (`--doc`, field-bullet extraction) — Runs under `set -euo pipefail` with no guard against a zero-match `grep`. Reproduced live: a document whose `## Envelope fields` section is non-empty but has zero bullets matching the expected shape makes the script abort with a **bare, non-classified exit 1** — no `doc-drift` token, no message at all, on stderr or stdout — a direct violation of "every refusal prints exactly one token, from a closed set."
- `bin/check-adapter.sh:816-818` (`--doc`, per-field tuple comparison) — Only the *first* matching bullet (`head -n1`) is compared per field name; a second, directly-contradicting bullet for the same field is never read. Reproduced live: inserting a duplicate `task-id` bullet (`out, conditional`, contradicting the correct `in, required` one immediately above it) still passes `--doc`.
- `docs/loop-engineering/task-envelope.md:65-66` — Stale prose in `## The generalized invocation pattern` (outside both the canon region and the `## Envelope fields` bullets, so `--doc` never reads it) still lists "the board itself" as an example return channel for an adapter's `out` fields — directly contradicting this same document's own later `## Carries declares a return path, never an authority (DP13)` section, which says the `board` channel "is retired... entirely." A reader implementing T-1056 from this document alone would see the contract contradict itself on the exact point DP13 exists to settle.

#### Minor / nits

- `bin/check-adapter.sh:794-798` (`--doc` canon-region extraction) — Marker matching (`awk '/BEGIN contract-canon/.../END contract-canon/'`) is substring-based rather than exact-line, and the subsequent command-substitution comparison (`CANON_RAW`/`PRINTED`) strips trailing newlines from both sides — the "byte-for-byte" claim in the header comment is very close but not quite exact at these two edges.
- Duplicate `effort-mechanism` rows (e.g. two literal `effort-mechanism none` rows) are still silently deduplicated rather than refused — the same class as round 1's already-disclosed dedup minor, now also reachable through `status-value`/error-class duplicate rows via the same mechanism; no new fast-follow needed, folds into the existing one.

#### Adversarial findings

A second Codex pass, framed to assume none of the round-2 fixes actually held and hunt specifically at the five weak points the coordinator named, independently reproduced 6 of the 7 findings above (all but the stale-prose one, which it also found independently and I list under Major) with no findings rejected — every one matched either my own live reproduction or a direct code reading of the exact mechanism described.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Duplicate/contradicting `status-value` tokens (same token both `success` and `failure`) | primary: P1 (→ blocker per map); adversarial: major (direct label) | major | Not reachable against the shipped file today (no envelope instance exists yet to misinterpret the ambiguity, matching round 1's reasoning for the analogous status-vocabulary gap); the two Codex passes disagreed with each other, and my own judgment sides with the adversarial pass's direct "major" label over the primary pass's mapped blocker. |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1055 entry back to `READY_FOR_ENG`. **This is the spec's own pre-commitment factual trigger's second data point**: round 1 and round 2 have each produced new, independent Blocker/Major findings against the same component (`bin/check-adapter.sh`'s fail-closed guarantees). If round 3 also produces new independent Blocker/Major findings, the spec's own pre-commitment section (droppable items 1–3, never-dropped set) is the engineer's and pm-spec's own pre-agreed procedure for that point — worth reading again before starting this round's rework, not after a third round begins.

#### Fast-follow disposition
- Duplicate `channel`/`error-class`/`effort-mechanism`/`effort-value`/`status-value` rows silently deduplicated instead of refused — file-an-issue (unchanged from round 1; now additionally reachable via `status-value`, same root mechanism, still low severity/low likelihood, still cheap to fix alongside the completeness-floor Major above).
- `--binding`'s uniform collapse of any `check-binding.sh` nonzero exit into `binding-invalid` — file-an-issue (unchanged from round 1; the `--binding` reassertion gaps found this round (missing schema-line requirement, unchecked provider/model tokens) are a related but distinct, more urgent concern already listed under Major above, not part of this deferred item).
- `--doc`'s canon-region marker matching and trailing-newline handling not being fully byte-exact — file-an-issue (new this round; low severity, the practical byte-for-byte guarantee holds for every case the suite actually exercises).

---

## Round 3

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1055
- Codex model: gpt-5-codex (via `codex exec`)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: round 3's rework, `403670c`..`1f51504` (class-B re-freeze v2→v3, `244b22b`; implementation V0–V5, `823fd6e`; records, `53f9e1a`; board+QA, `df8aa18`/`1f51504`) — 10 files changed, +487/-200 lines

All 17 frozen `- check:` lines independently re-run live and PASS (matches QA's 17/17); `bash tests/check-adapter/run.sh` (91/91, matches QA's count exactly, and matches the arithmetic QA disclosed: 88 − 6 `--doc` cases + 9 new); `shellcheck` clean on both files; `check-intent` aligned at v3 (`43608768d7861a6094c59da165f344445010e79b`); `check-provenance` (9 entries) and `check-interventions` (5 entries) conformant; CI wiring confirmed to have removed the `--doc` dogfood step entirely (only the two pre-v2 dogfood invocations remain). Issue #224 was opened live this round and read in full.

**Verifying each round-2 finding's disposition (nothing silently dropped):**

| Round-2 finding | Disposition | Verified |
|---|---|---|
| Blocker — channel vocabulary has no semantic constraint; retired `board` re-enters via `--contract` | **Retained scope, fixed (V1/DP17)** | Reproduced live: the exact round-2 PoC (`channel board` + `carries board-transition board`, including `codex-reviewer` bound to it) now refuses `retired-channel-declared`, exit 2, in both default and `--binding` modes — via a compiled-in `RETIRED_CHANNELS` set, not a fact about the shipped file. Reverse direction also verified: removing the registry's own `retired-channel board` row now refuses `contract-incomplete` (the bidirectional lock). |
| Major — `status-value` token uniqueness unchecked | **Retained scope, fixed (V2)** | Reproduced live: the round-2 PoC now refuses `contract-malformed`. |
| Major — error-class completeness checks non-emptiness, not the four required tokens | **Retained scope, fixed (V3)** | Reproduced live: a single arbitrary error-class row now refuses `contract-incomplete`, naming the missing required token. |
| Major — `--binding` reassertion doesn't require a schema line; provider/model tokens unread | **Retained scope, partially fixed (V4) — see new Major below** | Schema-line *absence* now refused (reproduced against a fake sibling emitting 6 valid rows + zero schema lines); provider/model shape-checked and provider cross-checked against the allowlist (reproduced against a fake sibling with a provider/adapter mismatch). Schema-line *content* validation is the residual gap, below. |
| Major — `--doc` pipefail hazard (bare unclassified exit on zero-match bullets) | **Carved out to #224, verbatim** | Confirmed via `api.github.com`: issue #224's body quotes this finding as its first requirement-list item, word for word. |
| Major — `--doc` first-match-only duplicate-bullet leak | **Carved out to #224 as a requirement, AND independently closed at spec-check time (AC8)** | Confirmed via the issue body's own disclosure ("requirement 2 above is already closed at spec-check time... this issue's value is making it a standing gate"); reproduced live: the exact round-2 duplicate-`task-id`-bullet PoC now fails AC8 (`bash bin/check-acs.sh`), where it previously passed. |
| Major — stale "board itself" prose (I reported 1 site; pm-spec's sweep found 2 more) | **Retained scope, fixed (V5), all 3 sites** | A scoped word-boundary `grep` over `## The generalized invocation pattern` now returns `0` (was `3` before the rewrite, per the board's own pre-fix measurement). The authority sentence was moved to the DP13 section rather than deleted — confirmed present there, naming `codex-reviewer` as the measured instance. |
| Minor — canon-marker substring matching / trailing-newline stripping | **Moot for this checker (mode removed by V0); travels with #224** | Confirmed: the `--doc` implementation carrying this code no longer exists in `bin/check-adapter.sh`. Issue #224's body explicitly notes it as "live again for whoever implements this" — correctly disposed, not lost. |
| Minor (round 1) — duplicate `channel`/`error-class`/`effort-mechanism`/`effort-value`/`status-value` rows silently deduplicated | **Untouched, confirmed** | The board's own V6 item states this explicitly; `effort-mechanism`/`channel`/`effort-value` duplicate rows are still silently deduplicated by code reading (only `status-value` gained a uniqueness check, per V2's own scope). |
| Minor (round 1) — `binding-invalid`'s uniform collapse of any sibling nonzero exit | **Untouched, confirmed** | No code change to this collapsing behavior found in the v2→v3 diff. |

**Was the carve-out the right execution of the pre-commitment?** Yes. The v2 spec's own pre-commitment (re-read and re-stated at v2 freeze time, before round 2 started) named the `--doc` mode as item (1) specifically *because* it was the newest mechanism, had zero consumers, and dropping it "retreats to something strictly better than v1 rather than to nothing" (AC8's own independent comparison). Round 2 then produced 1 new Blocker + 6 new Major against `bin/check-adapter.sh` as a whole — the factual trigger's literal condition — but critically, only 2 of those 7 findings (the pipefail hazard and the duplicate-bullet leak) were actually failures *of the `--doc` mechanism itself*; the other 5 (channel semantics, status uniqueness, error-class floor, binding reassertion, stale prose) were first-round-of-their-own-kind defects in retained, never-dropped machinery. Dropping item (1) and patching the other 5 in the same round is therefore consistent with the pre-commitment's actual purpose (stop a runaway patch-and-re-review cycle on *one specific recurring failure*, not freeze all further work) rather than a rationalized workaround. The board's own cross-check — verifying the drop didn't weaken anything on the never-dropped list (closing the Blocker *strengthens* "the board channel stays retired" rather than weakening it) — is exactly the right question to have asked, and the answer is correct.

**Confirming no regression**: all 12 criteria the board claims are byte-identical to v2 (AC1/AC3/AC4/AC5/AC6/AC9/AC10/AC11/AC14/AC15/AC16/AC18) pass live exactly as before; AC12/AC13 now correctly assert `--doc`'s *absence* (confirmed: `--doc` appears zero times across `bin/*.sh`, `skills/*/SKILL.md`, `agents/*.md`, and — the widened pathspec the pre-freeze sweep itself added — `tests/*/run.sh`).

#### Major (new this round)

- `bin/check-adapter.sh:786-790` (`--binding`, schema-line reassertion) — The delegated output's leading `schema` line is recognized by its **first token alone**; V4 fixed *absence* (round 2's finding) but not **content**. A row of `schema` (no version), `schema 999` (unsupported version), or `schema 1 extra` (wrong field count) still sets `binding_schema_seen=1` and is accepted. Reproduced live against a fake sibling `check-binding.sh` (swapped into a scratch `bin/`+`templates/` tree, real files untouched) emitting `schema 1 extra-junk` plus six otherwise-valid `bound` rows: `check-adapter.sh --binding` reports `valid`. By contrast, every *other* row field this reassertion reads is now genuinely shape-checked or set-checked (role against the six-role set with exact-six-count, provider/model against `TOKEN_RE`/`MODEL_RE` plus the allowlist's own pairing, adapter against the allowlist, effort against the adapter's declared value set) — a second Codex pass, framed specifically to hunt this class of gap, independently confirmed the schema line is the *only* field still checked for presence but not content. This is the same "trust the shape without validating it" pattern that produced round 2's finding in this exact mechanism, now recurring in a narrower form; worth naming explicitly as a candidate for the pre-commitment's own two-consecutive-rounds bookkeeping should a fourth round find anything else in this specific reassertion block.

#### Adversarial findings

A targeted adversarial pass, scoped to the `--binding` reassertion block specifically (given it has now had a finding in two consecutive rounds), independently reproduced the schema-line-content gap above and explicitly confirmed no other row field in that block has the same "presence checked, content ignored" defect — role, adapter and effort are each protected by set-membership or definition-driven checks that a malformed value cannot pass regardless. No new finding beyond the one above.

#### Minor / nits

None new this round.

#### Synthesis audit ledger

no severity changes

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1055 entry back to `READY_FOR_ENG`, scoped to a single, narrow fix (require `schema`'s field count and version against `SUPPORTED_SCHEMA_VERSIONS` before setting `binding_schema_seen`, with a stub-sibling test case for each of the three malformed shapes above). This is a much smaller rework surface than rounds 1–2: one finding, one mechanism, no design decision to re-open, and every other part of round 2's disposition (the V1–V3/V5 fixes, the #224 carve-out) is confirmed sound and does not need to be revisited.

#### Fast-follow disposition
- Duplicate `channel`/`error-class`/`effort-mechanism`/`effort-value` rows silently deduplicated instead of refused — file-an-issue (unchanged since round 1; `status-value` alone gained a uniqueness check this round per V2's own scope, the rest are untouched).
- `--binding`'s uniform collapse of any `check-binding.sh` nonzero exit into `binding-invalid` — file-an-issue (unchanged since round 1).
- `--doc`'s canon-region marker matching and trailing-newline handling not being fully byte-exact — **lands in issue #224**, not a separate fast-follow: the mode this affected was carved out of this checker entirely (V0), and #224's own body already documents this minor explicitly as "live again for whoever implements this." No further action needed from this task.
