### Codex Review verdict: REQUEST_CHANGES
- Task: T-1131
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Verification mode: executed-tests
- Diff scope: 10 files, +780/-7 lines (`421428a...HEAD`; `git diff --stat`, base = predecessor tip `421428a`, per the task brief — PR #460/T-1130 is stacked and still open, so this is not a `develop`-based diff) — `.shell-team/interventions/T-1131.md`, `.shell-team/provenance/T-1131.md`, `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md`, `.shell-team/todo.md`, `agents/pm-spec.md`, `bin/check-entry-mode.sh`, `docs/tuning-oversight.ja.md`, `docs/tuning-oversight.md`, `skills/run/SKILL.md`, `tests/check-entry-mode/run.sh`.

**No re-freeze occurred in this task** (no class-M/class-B record on the board entry), so the mandatory re-freeze verification item does not fire.

Independent execution before reading either Codex pass's findings: `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1131-refreeze-ratifier-declaration.md` (`check-acs: 7 passed, 0 failed, 0 skipped, 0 unrecognized` — AC1–AC7 all PASS), `bash tests/check-entry-mode/run.sh` (exit 0, `grep -c '^PASS'` = 84, `grep -c '^FAIL'` = 0, closing `All check-entry-mode assertions passed.`, matching the engineer's and QA's own counts exactly), `shellcheck bin/check-entry-mode.sh tests/check-entry-mode/run.sh` (both exit 0), `bash bin/check-prompt-sync.sh` (`all registered prompt blocks in sync`, exit 0), `bash bin/check-intent.sh .shell-team/specs/T-1131-refreeze-ratifier-declaration.md .shell-team/todo.md` (`aligned: T-1131 v1 (36d0ab3eee63eefe7321a893eb7235b1889c6da5)`), `bash bin/check-provenance.sh .shell-team/provenance/T-1131.md` (`conformant: … (5 decision entries, 0 sentinel)`), `bash bin/check-interventions.sh --task T-1131 .shell-team/interventions/T-1131.md` (`conformant: … (0 entries, 1 sentinel)`), `bash bin/check-entry-mode.sh --board .shell-team/todo.md --task T-1131` (exit 0, correct — T-1131's own entry is `pm-authored` and carries no `refreeze-ratifier` sub-bullet), `bash bin/check-pii-shapes.sh --base develop` (`clean`, exit 0). All match the engineer's and QA's hand-off claims exactly.

Own read of the diff against the frozen intent's Goal and Notes-for-engineer, plus direct execution against the real script in a `$TMPDIR` scratch board (not merely re-reading the shipped suite): the new family arm sits after the two-source (`- entry-mode:` vs `- dispatch: specify`) mismatch check and before `- flagged-gap:`, exactly as the frozen Notes-for-engineer instruct, and does not disturb `ENTRY`/`EM_VALUE`/`D_VALUE` (confirmed by reading the diff hunk directly). Manually re-derived the stem/full regex pair against every reachable input class the spec's `## Input space` names (empty value, space-before-colon, doubled-space-after-dash, case variant, label suffix, near-miss) and all behaved as documented: collected by the stem, refused by the full grammar. Confirmed live that a two-source mismatch on an entry that also carries a malformed/pm-authored `refreeze-ratifier` sub-bullet reports the mismatch first (correct precedence — the more fundamental disagreement, not a masking bug), that a duplicated `refreeze-ratifier` sub-bullet on a CRLF-terminated board is still refused with the duplicate-count message (the CR-strip and the new arm interact correctly), and that `fail()` exits before any code path could double-report. Also independently reproduced, against the real script (not merely inferred from the regex text), the missing-colon gap Codex's primary pass raised — see Major 2 below — and confirmed the identical characteristic already exists in the shipped, previously-reviewed `- dispatch-reflection:` family (T-1109), so this is an inherited pre-existing pattern, not a new-to-this-diff regression, though it is squarely inside this review's stated priority scope (item 1: "whether any spelling that means this family can slip past the stem net and read as the conformant absent case").

- raw-capture: `T-1131-codex-primary.jsonl` / `T-1131-codex-primary.txt`
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base 421428a --json -o "<RAW_OUT>" > "<RAW_JSONL>" 2>&1`
- pass-role (primary): generation
- briefing-fidelity (primary): not-carried — the `review` subcommand takes no custom prompt; Codex explored the working tree and the named base ref with its own tools (`git status --short`, `git diff --stat`/`--name-status` against `421428a`, targeted full reads of `bin/check-entry-mode.sh`, `tests/check-entry-mode/run.sh`, both edited skill/agent files, both oversight documents, the frozen spec, and neighbouring repository files such as `docs/workflow.md`, `bin/check-handoff.sh`, `templates/todo-template.md`, `.shell-team/AGENTS.md` for local convention) rather than receiving this round's Focus list, canon or spec text injected; this is the subcommand's standard shape, not a defect.
- raw-capture (primary): T-1131-codex-primary

- raw-capture: `T-1131-codex-adversarial.jsonl` / `T-1131-codex-adversarial.txt`
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Play devil's advocate on the diff between base commit 421428a and HEAD… [full free-form prompt naming the two candidate Major findings and the interaction risks to probe] …Return findings as a JSON array of objects with keys severity, file, line, issue, suggestion." < /dev/null > "<RAW_JSONL>" 2>&1`
- pass-role (adversarial): confirmation
- briefing-fidelity (adversarial): carried — the free-form prompt named both candidate Major findings verbatim (the pm-spec hand-off gap and the missing-colon stem gap), the specific files/lines to read, and the additional interaction risks (arm placement/masking, CRLF, duplicate-line, pm-authored refusal) this role's own manual testing above also covered; Codex read the files itself (96 `command_execution` events: `git diff`, `nl -ba` on the four named files, isolated regex/control-flow probes) rather than trusting the prompt's restatement.
- raw-capture (adversarial): T-1131-codex-adversarial
- **Operational note, disclosed for transparency, not a finding about the diff**: a first invocation of this same adversarial prompt (same wording, stdin not explicitly closed) returned zero `command_execution` events and an off-topic two-line agent message unrelated to this task, preceded by a `Reading additional input from stdin...` CLI notice — apparently the CLI waited on stdin and received something other than the intended prompt. That capture was discarded (never published) and the pass was re-run with `< /dev/null` on stdin, which produced the full 96-command-execution read reflected above. Recorded here so a future round knows to close stdin explicitly on a free-form `codex exec` invocation, not because it bears on this diff's correctness.

**Issue-canon disclosure**: this record's reading of GitHub issue #459's text relies entirely on the relayed text given in the spec's `## Problem` (the same verbatim block the spec quotes); this role has no GitHub access in this sandbox and could not open the issue's own bytes. internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Blockers
- none.

#### Major
- `agents/pm-spec.md:123` (producer-duty sentence) / `agents/pm-spec.md:119` (hand-off grammar) / `skills/run/SKILL.md:45` (Board-transcription-order paragraph) — the "author declares" half of the mechanism has no defined channel. The new sentence states `pm-spec` "transcribes the value the spec's author declared as this entry's `refreeze-ratifier` sub-bullet … never choosing one itself", but no file in this diff or repository defines where or how the author communicates that choice to `pm-spec` in the first place: the "Hand-off grammar for this mode" sentence at `:119` still enumerates only `- entry-mode:` and `- flagged-gap:`, unchanged; no new hand-off field, spec declaration, or Routing-Map axis was added for `refreeze-ratifier`; and the frozen spec's own `## Notes for engineer` explicitly instructed extending both `:123` and `:119` ("Extend the T-1096 board-transcription paragraph at `:123` and the hand-off grammar at `:119`"), but only `:123` was touched (confirmed directly against the diff — `agents/pm-spec.md` shows exactly one changed paragraph). Contrast with the sibling `budget.extension_ratifier` mechanism (T-1130): that value is a loop-contract YAML field the operator edits directly, with no `pm-spec` transcription step in the loop at all, so it never needed this channel. This mechanism, by deliberate design, routes the value through `pm-spec`'s transcription duty — and that duty currently has no input. Confirmed independently by an adversarial pass (below) reading the same three files. Practical effect: nothing in the diff breaks — an author who never gets a declaration mechanism simply reaches the safe `operator` default, exactly as today — but the `authoring-session` capability issue #459 asked for has no documented way to be exercised as delivered, which reintroduces one layer removed the exact "the run session invents the procedure at fire time" problem the issue named. Fix: define the channel (a spec-level declaration, a hand-off field addition at `:119`, or an explicit instruction in the task brief this rule can point to) and extend `:119` and/or `:45`'s transcription-order paragraph to name it.
- `bin/check-entry-mode.sh:239-264` (`rr_stem_re`) / `tests/check-entry-mode/run.sh` (the new T-1131 arms) — a completely missing colon slips past the stem net and reads as the conformant absent case. `rr_stem_re='^[[:space:]]*-[[:space:]]+refreeze-ratifier[[:space:]]*:[[:space:]]*'` requires a literal `:` character to match at all; a line such as `  - refreeze-ratifier authoring-session` (dash-space-fieldname-space-value, no colon anywhere) is never collected, so `RR_COUNT` stays 0 and the script exits 0 exactly as if the line were absent — confirmed live against the real script in a scratch board (`bash bin/check-entry-mode.sh --board … --task T-900` → exit 0). This is the exact defect class this review's own scope names as priority: a malformed spelling that means this family silently reading as the conformant absent case, rather than being collected wide and refused strict as this script's own recurring-defect-repair history requires. Mitigating factor, independently confirmed: the identical characteristic already exists in the shipped, previously cross-provider-reviewed `- dispatch-reflection:` family (T-1109) — `refl_stem_re` also requires a literal colon (confirmed live: a missing-colon `- dispatch-reflection …` line also silently passes) — so this is an inherited pattern from the precedent the spec explicitly instructed copying ("the same shape the `- dispatch-reflection:` family already uses"), not a worse-than-baseline regression invented here. It is still a real gap in the exact input class this review was asked to check, and neither AC2's eight rejected shapes nor the test suite's rejected-arm cases include a no-colon variant. Fix: widen `rr_stem_re` to recognize the bare field name as a boundary even without a colon present (e.g. requiring only the word `refreeze-ratifier` followed by a non-word-character or end-of-line, not a literal colon), keep the full grammar strict, and add a no-colon rejected-shape test; the same repair is worth considering for `dispatch-reflection`'s stem too, though that is outside this task's own scope.

#### Minor / nits
- `skills/run/SKILL.md:45` (pre-freeze routing sentence) — this sentence explicitly enumerates which side a non-zero `check-entry-mode.sh` exit routes to, by family (`pm-spec` for `- entry-mode:`/`- flagged-gap:`, the coordinating session for `- dispatch:`/`- dispatch-reflection:`, either side for a mismatch), but was never extended to name the new `refreeze-ratifier` family — a 4th failure class the same script now emits with no named remediation route in this table. Low practical impact: every `fail()` message the new arm emits already names the sub-bullet, the specific violation, and (for the `pm-authored`-with-line case) the reason it disagrees with itself, so a reader can reasonably infer the `pm-spec` route by analogy with `- entry-mode:` even without this sentence naming it explicitly. Not raised to Major because it is a documentation-completeness gap in a routing table, not an incorrect enforcement or a false result.

#### Adversarial findings
- Both candidate Major findings above were put to a dedicated adversarial `codex exec` pass (files read directly: `bin/check-entry-mode.sh`, `tests/check-entry-mode/run.sh`, `agents/pm-spec.md`, `skills/run/SKILL.md`) asked explicitly to break, confirm, or reject them. Result: both confirmed independently. On the hand-off-channel gap: "the hand-off grammar exposes no refreeze-ratifier field, yet pm-spec must transcribe a value the author supposedly declared… authoring-session selection has no documented propagation path." On the missing-colon gap: "rr_stem_re does not collect `- refreeze-ratifier authoring-session`, so RR_COUNT remains zero and an otherwise valid entry reaches exit 0 as if the field were absent… T-1109 has the same pre-existing requirement… so this is a copied inherited pattern, not a new sibling regression" — matching this role's own independent live-script confirmation exactly, including the T-1109 inheritance judgment. The pass found nothing new beyond these two (no masking/misattribution defect, no CRLF or duplicate-line interaction defect), consistent with this role's own manual probes of those same interactions (all confirmed correct).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | pm-spec hand-off has no declaration channel for the author's ratifier choice | primary: Major / adversarial: Major (confirmed) | Major | both passes agree; independently corroborated by this role reading `agents/pm-spec.md:119` (unchanged) and the frozen spec's own Notes-for-engineer instructing an edit that was not made — no downgrade, no rejection |
| 2 | missing-colon spelling slips past the stem net, reads as absent | primary: Major / adversarial: Major (confirmed, adds the T-1109-inheritance judgment) | Major | both passes agree; independently reproduced live against the real script by this role; kept at Major (not downgraded for being inherited) because it sits squarely in this review's named priority scope and the repository's fail-closed-checker invariant, even though it is not a new-to-this-diff regression |
| 3 | `skills/run/SKILL.md:45` routing sentence not extended for the new family | primary: Major (as raised) | downgraded to Minor | the primary pass's phrasing ("the gate can therefore block without a defined remediation route") reads more severely than the actual effect: every `fail()` message the new arm emits is self-explanatory and names the family, so the missing routing-table entry is a documentation-completeness gap rather than an enforcement defect or a case where the freeze genuinely has no path forward; not independently escalated by the adversarial pass, which found nothing new beyond findings 1 and 2 |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` to READY_FOR_ENG with linked findings (the two Major items above; the Minor item may ride along in the same round given the shared file, or be dispositioned separately per the Fast-follow disposition below)

#### Fast-follow disposition
- `skills/run/SKILL.md:45` (routing sentence not naming the new `refreeze-ratifier` family) — file-an-issue if the rework round addressing the two Major findings does not also touch this sentence: it is a small, precise documentation-completeness gap (a routing table missing a 4th enumerated family) with no functional defect behind it (fail() messages already self-explain the route), so it does not need to block the round on its own, but it should not be silently dropped either since it is a real gap in adopter-facing procedure text.

---

### Codex Review verdict: APPROVE (round 2)
- Task: T-1131
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line (`model_reasoning_effort = "max"`), measured this round rather than relayed; no `-m` override was passed to either invocation and neither JSONL event stream carries a per-turn model identifier to cross-check against, so the config-resolved value is the primary source.
- Codex CLI: codex-cli 0.149.1
- Verification mode: executed-tests
- Diff scope: 11 files, +958/-9 lines (`421428a...HEAD`; `git diff --stat`, base = predecessor tip `421428a`, unchanged from round 1 — PR #460/T-1130 is still open and stacked) — `.shell-team/interventions/T-1131.md`, `.shell-team/provenance/T-1131.md`, `.shell-team/reviews/T-1131.md`, `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md`, `.shell-team/todo.md`, `agents/pm-spec.md`, `bin/check-entry-mode.sh`, `docs/tuning-oversight.ja.md`, `docs/tuning-oversight.md`, `skills/run/SKILL.md`, `tests/check-entry-mode/run.sh`. The round-2 rework itself (`b59df33`, delta `839e2f0..HEAD`) touches only `agents/pm-spec.md`, `bin/check-entry-mode.sh`, `skills/run/SKILL.md`, `tests/check-entry-mode/run.sh`, plus the board and this record — the frozen spec is untouched (confirmed: `git diff 839e2f0..HEAD -- .shell-team/specs/T-1131-refreeze-ratifier-declaration.md` is empty).

**No re-freeze occurred in this round either** — `check-intent.sh` reports `aligned: T-1131 v1 (36d0ab3eee63eefe7321a893eb7235b1889c6da5)`, the same hash round 1 recorded, and the frozen spec's own bytes are byte-identical to round 1 (confirmed above). The mandatory re-freeze verification item does not fire.

**Round 1's disposition**: `REQUEST_CHANGES` — 2 Major (Major 1: `agents/pm-spec.md`'s hand-off duty had no defined channel for the author to declare the ratifier; Major 2: `rr_stem_re` required a literal colon, so `- refreeze-ratifier authoring-session` silently read as absent) and 1 Minor (`skills/run/SKILL.md:45`'s routing sentence did not name the new family in its failure-routing parenthetical).

**Round 2 rework, read against round 1's own fix instructions**:
- **Major 1 (hand-off channel) — closed.** `agents/pm-spec.md:119`'s hand-off grammar now explicitly carries the optional `- refreeze-ratifier:` line, and a new paragraph at `:121` (**"The channel the author uses to declare that value"**) defines the missing piece: a top-level `- refreeze-ratifier: <value>` line in the supplied spec's own declaration region (between the intent block's `BEGIN` marker and `## Non-goals`, beside `- user-visible:` / `- verification-class:` / `- verification-ceiling:` / `- base-ref-discriminator:`), read there, echoed verbatim in the hand-off, transcribed onto the board, never chosen by `pm-spec` itself, with an out-of-vocabulary value routed to `- flagged-gap:` rather than silently corrected. `:125`'s board-transcription paragraph and `skills/run/SKILL.md:45` were both updated to reference this channel, closing the exact gap round 1 named (the frozen spec's own Notes for engineer at `:549`–`:554` had asked for edits at both `:119` and `:123`; round 1 made only the `:123` edit, round 2 completes both plus the new explanatory paragraph). Internally consistent with the frozen Goal's own "transcribes … never chooses … absence is legal and means `operator`" language and with `docs/tuning-oversight.md`'s new "Which human the class-B GO is addressed to" section, which frames the sub-bullet as changing only *whom the escalation message names*, never *whether* a human GO is required — confirmed by direct read of both files.
- **Major 2 (colon-anchored stem) — closed as a class, own live-script confirmation.** `rr_stem_re` widened from `'^[[:space:]]*-[[:space:]]+refreeze-ratifier[[:space:]]*:[[:space:]]*'` to `'^[[:space:]]*-[[:space:]]*refreeze[-_ ]?ratifier'`, collected case-insensitively (`grep -Ei`), full grammar unchanged and still strict/case-sensitive. Independently re-derived and executed against the real script in scratch boards (not merely re-reading the shipped suite, and not merely re-running it): the reported missing-colon case (`- refreeze-ratifier authoring-session`) now collects and refuses (exit 1) — confirmed live. Also independently probed every variant this round's own review-scope brief names: `=` for the colon, doubled colon, no space after the dash, a tab there, title-case and upper-case field names, `_`- and space-joined field names, no space after the colon, a trailing comment — all refuse (matches the 11 new suite arms `tests/check-entry-mode/run.sh` adds, all named `T-1131 ratifier-rejected`, all confirmed present in this round's own 97-PASS run). Over-collection checked both ways: a `- source: … refreeze-ratifier …` value-mention and a `- flagged-gap (g1): refreeze-ratifier …` value-mention are correctly NOT collected (the family name must sit immediately after the dash, not merely appear on the line — confirmed live); a genuine sibling-shaped key, `- refreeze-ratifier-note: something`, IS collected (no trailing-boundary requirement on the stem) and then refused as malformed — confirmed live, and judged correct: no such sibling field exists today, and over-collecting an unusual near-miss key and refusing it is the safe direction this arm's own "collect wide, parse strict" design states, matching QA's own independently-recorded `refreeze-ratifierx` probe and judgment.
  - **Residual, not closed, disclosed rather than escalated**: a zero-width space (U+200B) or a line-leading BOM inserted before the bullet dash still evades the widened stem net and reads as the conformant absent case — confirmed live against the real script by this role (`- refreeze<U+200B>ratifier: operator` → exit 0; a BOM-prefixed dash line → exit 0). A Unicode non-breaking hyphen (U+2011) in place of the ASCII hyphen behaves the same way, which QA's own hand-off already measured and tagged `out-of-input-space`, citing the spec's "Adversarial encodings of the value" out-of-scope bullet as the closest analogue even though that bullet's literal wording scopes to the accepted **value** ("operator"/"authoring-session"), not the field-**key** spelling. This role concurs with QA's practical judgment (real board authoring — by an AI agent or a human editing markdown — essentially never inserts a raw zero-width character or BOM mid-file, and the spec's Non-goals explicitly declines "zero-width or homoglyph substitutes" as an adversarial-encoding class in spirit) but notes the literal-scope gap for the record rather than silently accepting QA's citation as exact: this is a genuine, narrow, disclosed residual in the same defect class Major 2 named, not a class that is fully closed. Independently confirmed by the adversarial pass below (`severity: info`) with the same reasoning. **Not escalated to Major** per this round's own calibration note (a residual that does not invalidate the deliverable, in an input class the spec's own Out-of-scope section speaks to at least in spirit) — see Minor 2 and the Fast-follow disposition below. An alternate bullet marker (`*`/`+` instead of `-`) also evades collection, but this matches the existing `- entry-mode:`/`- dispatch:` families' own inherited convention (none of them support alternate markers either) — not a new-to-this-diff regression, no finding.
- **Minor (round 1) — closed.** `skills/run/SKILL.md:45`'s failure-routing parenthetical now reads "`pm-spec` for a missing/malformed/duplicated `- entry-mode:`, `- flagged-gap:`/`- flagged-gap-resolution:`, or `- refreeze-ratifier:` marker" — the new family is named. Confirmed by direct read of the diff hunk.

**New item raised by this round's primary Codex pass, independently corroborated**: the frozen spec's own `## Notes for engineer` (`:521`–`:522`) still prescribes the superseded colon-anchored stem regex verbatim, now contradicting the shipped `rr_stem_re` at `bin/check-entry-mode.sh:280`. Confirmed by direct read — the spec file is untouched in this round (see Diff scope above), so this contradiction pre-dates round 2 and was simply never corrected once the shipped code moved past it. This text sits **outside** the frozen intent block (`BEGIN`/`END intent-block: T-1131` spans `:54`–`:328`; `## Notes for engineer` starts at `:514`), so it is mutable and editing it needs no re-freeze; no shipped process re-reads this paragraph as authoritative once implementation is done, so it is a stale record, not a live enforcement path. Graded **Minor**, not Major, per this round's own calibration ("record hygiene … not Blocker/Major, unless it invalidates the deliverable") — the deliverable itself (the shipped script and suite) is correct and independently verified; only the spec's own historical guidance text is out of date.

**Class-B circularity question, judged, not escalated**: the new `- refreeze-ratifier:` declaration line lives inside the spec's frozen declaration region, so a later change to it is itself a class-B re-freeze — and neither this diff nor `docs/tuning-oversight.md`'s new section discusses who ratifies a change to the ratifier declaration itself. Judged **not a gap requiring action**: `docs/tuning-oversight.md`'s "Who may re-freeze a frozen intent block" section states the class-B rule unconditionally ("always needs your GO") with no carve-out keyed to the ratifier's own value, and the new section clarifies the sub-bullet changes only *whom the escalation message addresses*, never *whether* human involvement is required — so a change to the ratifier field itself is human-gated exactly like every other class-B delta, regardless of what the field said beforehand. The adversarial pass below was asked this question directly and raised no finding, consistent with this reading.

**No regression**: this round's own execution of `tests/check-entry-mode/run.sh` (97 PASS, 0 FAIL) confirmed a byte-for-byte diff of the test file against round 1's committed version (`839e2f0`) is strictly additive — the only `-`-prefixed line in `git diff 839e2f0..HEAD -- tests/check-entry-mode/run.sh` is the diff header itself, zero real lines removed — so every one of round 1's 84 PASS-producing assertions is untouched and still passing verbatim inside this round's 97. The `- entry-mode:` / `- dispatch:` / `- flagged-gap:` / `- dispatch-reflection:` stems are untouched in this round's `bin/check-entry-mode.sh` diff (confirmed by direct read of the diff hunks — only the `refreeze-ratifier` arm's comments and two regex lines changed).

Independent execution before reading either Codex pass's findings (this round): `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1131-refreeze-ratifier-declaration.md` (`check-acs: 7 passed, 0 failed, 0 skipped, 0 unrecognized`), `bash tests/check-entry-mode/run.sh` (exit 0, 97 PASS / 0 FAIL, matching the engineer's and QA's own round-2 counts), `shellcheck bin/check-entry-mode.sh tests/check-entry-mode/run.sh` (both exit 0), `bash bin/check-prompt-sync.sh` (`all registered prompt blocks in sync`), `bash bin/check-intent.sh .shell-team/specs/T-1131-refreeze-ratifier-declaration.md .shell-team/todo.md` (`aligned: T-1131 v1 (36d0ab3eee63eefe7321a893eb7235b1889c6da5)`), `bash bin/check-provenance.sh .shell-team/provenance/T-1131.md` (`conformant: … (8 decision entries, 0 sentinel)`), `bash bin/check-interventions.sh --task T-1131 .shell-team/interventions/T-1131.md` (`conformant: … (0 entries, 1 sentinel)`), `bash bin/check-entry-mode.sh --board .shell-team/todo.md --task T-1131` (exit 0), `bash bin/check-pii-shapes.sh --base develop` (`clean`). All match the engineer's and QA's round-2 hand-off claims exactly.

**Issue-canon disclosure (carried from round 1)**: this record's reading of GitHub issue #459's text still relies entirely on the relayed text given in the spec's `## Problem`; this role has no GitHub access in this sandbox and could not open the issue's own bytes this round either. internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

- raw-capture: `T-1131-codex-primary-r2.jsonl` / `T-1131-codex-primary-r2.txt`
- executor-invocation (primary-r2): `codex exec --sandbox read-only --cd <repo-root> review --base 421428a --json -o "<RAW_OUT>" > "<RAW_JSONL>" 2>&1`
- pass-role (primary-r2): generation
- briefing-fidelity (primary-r2): not-carried — the `review` subcommand takes no custom prompt; Codex explored the working tree and the named base ref with its own tools (`git branch --show-current`, `git log`, `git diff --stat`/`--name-status` against `421428a`, targeted full reads of `bin/check-entry-mode.sh`, `tests/check-entry-mode/run.sh`, both edited skill/agent files, the frozen spec, the board, `.shell-team/lessons.md`, `bin/check-refreeze-class.sh`, `CONTRIBUTING.md` and `docs/tuning-oversight.md`) rather than receiving this round's Focus list, canon or spec text injected; this is the subcommand's standard shape, not a defect.
- raw-capture (primary-r2): T-1131-codex-primary-r2

- raw-capture: `T-1131-codex-adversarial-r2.jsonl` / `T-1131-codex-adversarial-r2.txt`
- executor-invocation (adversarial-r2): `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Play devil's advocate on the diff between base commit 421428a and HEAD… [named the primary pass's stale-Notes-for-engineer finding verbatim, this role's own live-confirmed zero-width/BOM/Unicode-hyphen/sibling-key/alternate-bullet probes, and the class-B circularity question] …Return findings as a JSON array of objects with keys severity, file, line, issue, suggestion." < /dev/null > "<RAW_JSONL>" 2>&1`
- pass-role (adversarial-r2): confirmation
- briefing-fidelity (adversarial-r2): carried — the free-form prompt named the primary pass's finding verbatim, the exact corner-case probes this role had already run live against the real script, and the class-B circularity question; Codex read the files itself (`git branch`/`git log`, `nl -ba` on this review record, the board, the frozen spec, `bin/check-refreeze-class.sh`, `CONTRIBUTING.md`/`docs/tuning-oversight.md`, `.shell-team/lessons.md`) rather than trusting the prompt's restatement, and returned two findings (`minor` on the stale Notes-for-engineer text, `info` on the Unicode/BOM/bullet-marker probes) matching this role's own severity judgments exactly; it raised no finding on the class-B circularity question, consistent with this role's own "not a gap requiring action" reading.
- raw-capture (adversarial-r2): T-1131-codex-adversarial-r2

#### Blockers
- none.

#### Major
- none. Both round-1 Majors and the round-1 Minor are closed (see above).

#### Minor / nits
- `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md:521`–`:522` (`## Notes for engineer`) — still prescribes the superseded colon-anchored `rr_stem_re`, now contradicting the shipped, widened regex at `bin/check-entry-mode.sh:280`. Outside the frozen intent block, mutable, no functional effect on the shipped deliverable — but stale guidance a future reader could mistakenly follow. Fix: update the quoted regex in the Notes to the shipped `'^[[:space:]]*-[[:space:]]*refreeze[-_ ]?ratifier'` form (case-insensitive collection note included), keeping the "full grammar strict" half unchanged.
- `bin/check-entry-mode.sh:280` (`rr_stem_re`) — a zero-width space (U+200B) or a leading BOM immediately before the bullet dash, and (per QA's own already-disclosed risk note) a Unicode non-breaking hyphen (U+2011) in place of the ASCII hyphen inside the field name, each still evade the widened collection net and read as the conformant absent case. Narrow, undisclosed-in-code (though QA disclosed the hyphen case on the board) residual in the same defect class Major 2 named; not escalated per this round's calibration (does not invalidate the deliverable; the spec's own Out-of-scope section speaks to adversarial value/key encodings in spirit even though its literal wording scopes to the value). Fix, if pursued: strip zero-width characters and a leading BOM from each candidate line before matching, and/or explicitly extend the spec's Input space's "Adversarial encodings" bullet to name field-key encodings, not only the value's.

#### Adversarial findings
- Dedicated adversarial `codex exec` pass on the primary pass's finding plus this role's own live-confirmed corner-case probes and the class-B circularity question (files read directly: this review record, the board, the frozen spec, `bin/check-refreeze-class.sh`, `CONTRIBUTING.md`/`docs/tuning-oversight.md`, `.shell-team/lessons.md`). Result: `{"severity":"minor", ... "The engineer notes still prescribe the old colon-anchored stem regex... The notes are outside the frozen intent block..., are mutable, and no repository process treats them as authoritative; this is misleading guidance, not a blocking defect."}` and `{"severity":"info", ... "U+200B, U+2011, a leading BOM, and * or + bullets evade collection and therefore read as absent. The alternate bullets follow existing family conventions; the Unicode and BOM forms are outside the declared input space and are already noted as residual risk. The refreeze-ratifier-note sibling is correctly collected and rejected by strict parsing."}` — both match this role's own independent severity judgments exactly. No finding was returned on the class-B circularity question, consistent with this role's own "not a gap requiring action" reading.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Major 1 (hand-off channel undefined) | closed by rework, re-confirmed by direct read this round (not re-raised by either pass) | closed | `agents/pm-spec.md:119`/`:121`/`:125` and `skills/run/SKILL.md:45` now define and reference the declaration-region channel; no downgrade — verified closed, not merely claimed |
| 2 | round-1 Major 2 (colon-anchored stem missed spellings) | closed by rework, re-confirmed by direct live-script execution this round (not re-raised by either pass) | closed (residual noted, see #4) | widened stem confirmed via 11 new suite arms plus this role's own additional probes; the class is closed for every spelling the spec's Input space and this round's own review scope name, with one disclosed residual carried forward as Minor 2 rather than as an open Major |
| 3 | round-1 Minor (`SKILL.md:45` routing sentence) | closed by rework, re-confirmed by direct read | closed | the failure-routing parenthetical now names the `refreeze-ratifier` family |
| 4 | zero-width/BOM/Unicode-hyphen residual in the widened stem | primary: not raised (out of its own scope) / adversarial: `info` / this role's own live probes: confirmed evades collection | Minor (not escalated to Major) | `info` from the adversarial pass reads as low-severity by that pass's own vocabulary, which this role's normalization table has no direct P-label mapping for (a free-form JSON severity, not a P0–P3 tag) — read conservatively as at most Minor per the map's "round up when unmapped" instruction applied to the lower end of a `minor`/`info` pair, and independently justified by this round's own calibration note (does not invalidate the deliverable; addressed to a synthetic/adversarial encoding class the spec's own Out-of-scope section speaks to in spirit) |
| 5 | stale `## Notes for engineer` regex (new this round) | primary: `P2` (→ `major` by the mechanical normalization table) / adversarial: `minor` | Minor | downgraded from the table's mechanical `major` mapping: the P2 finding is about a mutable, non-frozen documentation paragraph with no shipped process re-reading it as authoritative, and the shipped deliverable it describes is independently confirmed correct — this is "record hygiene" under this round's own explicit calibration note ("process bookkeeping, QA-audit completeness, record hygiene, provenance prose → note/fast-follow, not Blocker/Major, unless it invalidates the deliverable"), and the adversarial pass's own independent severity judgment (`minor`) agrees with the downgrade rather than the raw table mapping |
| 6 | class-B circularity in the ratifier-declaration's own frozen placement | primary: not raised / adversarial: not raised (asked directly, returned nothing) / this role: judged not a gap | not a finding | the blanket class-B rule in `docs/tuning-oversight.md` already covers a change to the ratifier field itself unconditionally, regardless of the field's own value — no downgrade, since nothing was raised to downgrade; recorded here per the ledger's own "no severity changes" convention would be wrong to omit, since a reader auditing this round needs to see the question was asked and answered, not merely silently absent |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- `.shell-team/specs/T-1131-refreeze-ratifier-declaration.md:521`–`:522` (stale `## Notes for engineer` regex) — file-an-issue: small, precise, no functional defect behind it, but real stale guidance that should not be silently left to mislead a future reader of this spec.
- `bin/check-entry-mode.sh:280` (zero-width/BOM/Unicode-hyphen residual in the widened `rr_stem_re`) — file-an-issue: narrow and low-likelihood in practice, but it is the same defect *class* (a spelling that means the family reads as absent) this task exists to close, just not the same instance; worth a follow-up pass to either normalize the candidate line (strip zero-width/BOM bytes, fold common Unicode hyphen variants) before matching, or to extend the spec's own Input space "Adversarial encodings" bullet to explicitly name the field-key spelling, not only the accepted value.
