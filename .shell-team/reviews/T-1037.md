<!-- BEGIN review: T-1037 -->

# QA verification — T-1037

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

## check-acs.sh

`CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1037-checker-retro-precision.md`
→ **13 passed, 0 failed, 1 skipped (AC14, runtime)** — matches required shape exactly. Wall-clock ~97s (well inside the 900s elevation).

## Frozen-intent alignment

`bin/check-intent.sh` run independently against the board for all 10 stacked-train specs — all `aligned`:
T-1024 v2, T-1027 **v3 (63b1ba129a956bdd42aa30de1829cfd2b8b471ec)**, T-1029 v5, T-1030 v2, T-1031 v1, T-1033 v1, T-1034 v1, T-1035 v1, T-1036 v5, T-1037 **v1 (f13c82d42cea99e34fd29a2c38901472ad5a14a2)**. Hashes match exactly what the board records.

## Independent class-B re-derivation

`check-refreeze-class.sh -- <materialised 8d47eed blob> <working T-1027 spec>` → **exit 1** (class-b), independently reproduced (not copied from the board): "differing line at intent-block index 105 for T-1027 is not a `- check:` line on both sides".

## Mutation replays (scratch git worktree, restored and re-verified green each time)

- (i) Invalid fixture, decoy slot deleted → NEW anchored G1 red (count=0); OLD (base-8d47eed) substring guard false-passes (still derives a line number); `bin/check-handoff.sh` itself still exits 1 with the identical stderr message, line, no format-mismatch, exactly one violation — every pre-existing behavioural assertion still green, only the anchor catches the de-shape. Confirmed exactly as spec/board claim.
- (ii) Invalid fixture, two slots swapped → NEW anchor red (count=0); OLD guard false-passes; checker itself flips to exit 0 (double-catch, as DP-4 predicts).
- (iii) Valid fixture, token relocated into the title (out of the separator shape) → NEW anchor red (count=0); OLD guard false-passes; `bin/check-handoff.sh` stays exit 0 with empty stderr — **caught only by the anchor**, the single most load-bearing case. Confirmed.
- (iv) `agents/scrum-master.md` line 78 (S1's site) reverted to base-8d47eed bytes → AC1 (three-way identity) red as required by this QA task's own instruction; AC2/AC5/AC6 stayed green (unaffected, as expected since they compare spec blocks / other sections). Restored → all 13 green again.

All probes: red observed, then restored, then re-confirmed green (`git status --short` clean in both scratch worktrees and in the main checkout throughout; worktrees removed with `git worktree remove --force`).

## Recursive shapes (independently re-run, not copied from the board)

- T-1027: **8 PASS** (AC1,AC2,AC3,AC4,AC6,AC8,AC9,AC11) / **4 FAIL** (AC5,AC7,AC10,AC12 — all inside the permitted merge-point-scoped set) / **1 SKIP** (AC13). AC8 (load-bearing) = PASS. Matches the spec's/board's predicted 8P/4F/1S exactly.
- T-1031: **14 PASS** (incl. all six required: AC5,AC10,AC11,AC12,AC13,AC14) / **2 FAIL** (AC15,AC16 — both permitted) / **2 SKIP** (AC17,AC18). Matches predicted 14P/2F/2S exactly.

## Anchor audit

- G1/G2 are full-line ERE (`^…$`), pin the entry id, a backtick-free title, both `` — `FLAG` — spec: <path> `` slots, and the token in its required slot; non-token slot matched as `[A-Z_]+` (cannot match the digit-carrying token). Line-number derivation (`decoy_bad_line`) reuses the same `$decoy_bad_anchor` variable as the guard, with a `-eq 1` uniqueness count preceding the `cut` extraction.
- S1/S2 (verified with `sed -n '78p;110p' agents/scrum-master.md | grep -oE ...`): none of `spot-check`/`where possible`/`if possible`/`optionally`/`` `gh` ``/`origin/`/`git log`/`git rev-list` present — anti-optionality and no-network locks both hold.
- `shellcheck 0.11.0` on `tests/check-handoff/run.sh` → clean (exit 0); the `SC2016` directives on both anchor assignments are doing their job.

## Suites / CI

- `bash tests/check-handoff/run.sh` → green (both `bash` and direct `./tests/check-handoff/run.sh` exec forms — confirms provenance decision 2's exec-bit-restore rationale).
- Full CI-wired list (`.github/workflows/check-handoff.yml`): **61 named steps total, 59 run locally in workflow order (2 inapplicable: Checkout, Install shellcheck) — all 59 green**, including both glob/directory-scanning steps run rather than reasoned about (`check-retro.sh .shell-team/retros/*.md`, the `for f in .shell-team/interventions/*.md` dogfood loop). Matches the board's claim exactly.
- `check-prompt-sync.sh` green; scratch-tree playbook regeneration → zero diff on all 8 files (independently reproduced the same broken-`IFS`-in-interactive-shell quirk the board's AC14(a) note discloses — corroborates that disclosure).
- `check-pii-shapes.sh --base 8d47eed` clean; `check-board-headings.sh --base 8d47eed` exit 0; board hand-off lint exit 0.
- `check-provenance.sh .shell-team/provenance/T-1037.md` → conformant, 2 decision entries. `check-interventions.sh --task T-1037 -- .shell-team/interventions/T-1037.md` → conformant, sentinel.

## Exec-bit

`git ls-files -s tests/check-handoff/run.sh` → `100755`. `tests/bin-exec-bit/run.sh` green (36/36 `bin/` entries). Direct `./tests/check-handoff/run.sh` invocation confirmed working (see Suites/CI above).

**Disclosed gap, not fixed here, correctly out of scope**: `tests/bin-exec-bit/run.sh` only locks `bin/`; `tests/` has no equivalent exec-bit lock, which is exactly why the class fired on `tests/check-handoff/run.sh` in the first place (caught only by the engineer's own diligence, not by any checker). Non-goals/DP-8 explicitly forbid building a new checker here, and the T-1034 lock's `bin/`-only scope is a pre-existing, separate design decision this task correctly did not touch. This is a legitimate residual-risk item for the reviewer/next-planning-input, not a defect in this task.

## Claim-fidelity — `## Summarized sources` vs primary bytes

- `.shell-team/specs/T-1027-promote-retro-2026-08-04.md` — opened in full. AC8/AC9/DP5 clauses, the F1–F4 site descriptions, and the "only F3/F4 prescribe reconciliation" claim all confirmed against the live file. **Finding**: the bullet's opening claim "its frozen keys are eight blocks" undercounts — `grep -oE '<!-- frozen-begin: [A-Za-z0-9]+ -->' | sort -u` yields **nine** distinct keys (E1, E2, E3, O, F1, F2, F3, F4, F5), independently corroborated by the coordinating session's own freeze-run note on the same board entry ("Frozen-block extraction line counts... E1=9, E2=9, E3=9, O=11, F1–F5=1 each" — nine names). This sits **outside** T-1037's own frozen intent block (the section explicitly declares itself so: "naming a further source during a rework is an ordinary edit, not a re-freeze"), does not drive any AC's check-line arithmetic (AC4/AC6, which are inside the frozen region, correctly enumerate all 9 keys and pass), and does not touch any shipped byte. Route: an ordinary pm-spec prose correction at next touch, not a re-freeze and not an engineer rework.
- `.shell-team/todo.md` — T-1027's/T-1031's `## Done` entries read directly at base `8d47eed`. Confirmed: T-1027 at v2 with both `freeze-attestation (v1, 2026-08-04)` and `(v2, 2026-08-05)` present, one `intent-ratified (2026-08-05): v1→v2` line, hash `8f4a08deee3d91132e396637ab586cce3df933c7`, and the #135 fast-follow-disposition wording quoted verbatim — all match byte-for-byte.
- `.shell-team/specs/T-1031-check-handoff-flag-anchor.md` — AC12, AC14, AC17(b) read directly; all three match the spec's summary exactly (AC12's exactly-once `T-901` anchor; AC14's suite-green-plus-both-basenames; AC17(b)'s blind-spot-mutation requirement).
- `agents/scrum-master.md` — read in full before/after; the four `--last-n` sites and the five `contain`-mode blocks confirmed live.
- `templates/prompt-blocks/retro-inputs.md` — read in full: 13 lines, 0 occurrences of `--last-n`. Confirmed.
- `templates/prompt-blocks/registry.txt` — read directly: confirmed exactly 4 `marker`-mode `playbook-*` rules and exactly 5 `contain`-mode rules naming `agents/scrum-master.md`.
- `tests/check-handoff/run.sh` fixtures and DP-4's opposite-slot claim — read directly; confirmed invalid fixture carries the token in the LAST slot, valid fixture in the FIRST slot.
- `bin/check-refreeze-class.sh` — header contract read and the tool run live (see above).
- **GitHub issues #135 and #146 — fetched directly via `curl https://api.github.com/repos/RipsawJP/shell-team/issues/{135,146}` this session (network reachable); not `environmentally-unverified`.** Both bodies read in full and match the spec's summaries exactly. #135's own wording is literally "spot-checks the returned commits' identity" — DP-2 deliberately declines that exact phrase (`spot-check` is machine-forbidden by AC3) in favour of the stronger, mandatory "you must also match" / "Reconcile identity as well as count, always" — a disclosed, reasoned, and *faithful* divergence (identity checking became mandatory rather than optional-sounding, which is strictly stronger than the issue's own ask, not a missed requirement). #146's body matches exactly; the shipped anchors (pinning slot position, not just separator shape) go further than the issue's own minimal proposal, as DP-4 states.

## Edge cases / adversarial fixtures tried (beyond the spec's own AC8/AC14(b) set)

- Independently reproduced 3 of the 4 AC8 de-shape probes by hand (decoy-slot-deletion, slot-swap, title-relocation) against real fixtures with the real `bash bin/check-handoff.sh`, not just via the spec's own `check:` line — confirming the anchor/behavioural-assertion asymmetry DP-4 is built on.
- Direct-invocation launch shape (`./tests/check-handoff/run.sh`, not only `bash tests/check-handoff/run.sh`) confirmed working, validating the exec-bit-restore provenance decision.
- Regex-anchoring class: confirmed the anti-optionality/no-network negative-token sweep on the live `agents/scrum-master.md` lines directly (not only via AC3's own check line).
- CommonMark/markdown-structure class: not applicable — the fixtures are single-line, unfenced boards; no fenced-block or heading-variance parsing is in play for this artifact.

## Risk notes for reviewer

1. **eight-vs-nine block miscount** in T-1027's `## Summarized sources` prose (see Claim-fidelity above) — non-load-bearing, non-frozen, no shipped-byte impact; flag for an ordinary pm-spec correction at next touch.
2. **`tests/bin-exec-bit` coverage gap** (bin/-only) is disclosed and correctly out of this task's scope, but is a live residual risk: any future `tests/*.sh` exec-bit regression will again go undetected by CI until an engineer notices by hand, as happened here.
3. The T-1027 v2→v3 re-freeze is flagged per AC14(e) as a **mandatory disclosure item** for the cross-provider round; the reviewer keeps an independent veto over it.

## Acceptance criteria: 13/13 checked (12 check-line PASS + AC14 runtime disclosure independently verified above)
## Tests: 13 passed / 0 failed / 1 skipped (check-acs.sh); 59/59 CI-wired steps green; tests/check-handoff/run.sh green (both invocation forms); tests/bin-exec-bit/run.sh green

<!-- END review: T-1037 -->

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1037
- Codex model: gpt-5.6-sol (codex-cli 0.145.0, model_reasoning_effort=high, per `~/.codex/config.toml`)
- Verification mode: static-only
- Diff scope: 8 files, +479/-9 lines (`git diff --stat 8d47eed...HEAD`)

#### Mandatory disclosure item — T-1027 v2→v3 class-B re-freeze

No veto on procedural/mechanical grounds. Independently re-verified, not merely re-read from the board:
- `bin/check-refreeze-class.sh --old-hash 8f4a08deee3d91132e396637ab586cce3df933c7 --new-hash 63b1ba129a956bdd42aa30de1829cfd2b8b471ec -- <materialised 8d47eed blob of T-1027's spec> <live T-1027 spec>` → exit 1 (class-b), diagnostic identical to the board's own record ("differing line at intent-block index 105 for T-1027 is not a `- check:` line on both sides").
- `bash bin/check-intent.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/todo.md` → `aligned: T-1027 v3 (63b1ba129a956bdd42aa30de1829cfd2b8b471ec)`.
- Ledger structure read by hand at `.shell-team/todo.md`'s T-1027 entry: `freeze-attestation` v1, v2 and v3 all retained; one `intent-ratified (2026-08-05): v2→v3` line naming issue #135 and grounding it in the same three standing-authority grounds used for v1→v2; exactly one `- intent-hash` line present (v3); `grep -n '^\s*- intent-hash (v2): 8f4a08de'` against the live board returns nothing (the old v2 line is genuinely gone, not merely superseded-in-place). `git diff --numstat 8d47eed -- .shell-team/todo.md` = 32 insertions / 1 deletion, and the single deleted line is exactly the old v2 hash record — matches DP-3/AC10's "only board deletion" claim exactly.
- `git diff 8d47eed...HEAD -- .shell-team/specs/T-1027-promote-retro-2026-08-04.md` confirmed the delta is confined to the F3 and F4 fenced blocks; no other byte of that spec moved.

My REQUEST_CHANGES verdict below is grounded in a substantive content defect discovered in the S1/S2 text this re-freeze produced, not in any doubt about the re-freeze's mechanics, scope or authority — so it does **not** trigger the byte-restore v3→v4 consequence. Remediation, given S1/S2 are now frozen bytes, is itself a further class-B re-freeze through pm-spec (mirroring T-1027's own v1→v2→v3 history and the T-1029 precedent: Major finding → re-freeze, not revert).

#### Blockers
- None.

#### Major
- `agents/scrum-master.md:78` (S1, `## Loop` step 2) and `agents/scrum-master.md:110` (S2, `## Rules`) — the newly-frozen identity-reconciliation text lists "the pull-request numbers, task IDs or merge SHAs" as equally valid identity material to match against the `cycle-window` sub-bullets. But `bin/retro-inputs.sh`'s actual sub-bullet format (read directly: `bin/retro-inputs.sh:303` builds the merge list with `git log --merges --first-parent --pretty=format:'%H%x09%s'`, and line 344-346 formats each shown row as `${hash:0:12} subject`) is a 12-character merge SHA plus the merge commit's subject line — which in this repository's own convention (`git log --merges --first-parent -5`, confirmed live) is always `Merge pull request #N from owner/branch`, i.e. a PR number, never a bare task ID. When an agent's task framing carries only task IDs (T-NNN) — no PR numbers, no merge SHAs — the reconciliation described cannot actually be performed: a task ID cannot be matched against the ledger's SHA+subject bullets. The one mechanism that could bridge this — `pr-metadata`'s task-ID extraction at `agents/scrum-master.md:77` (step 3) — is a different, optional, `gh`-auth-gated stream, documented for a different purpose (locating review artifacts) and never joined back to step 2's reconciliation. Because task IDs are explicitly listed among the identity material the framing may carry (T-1037's own `## Input space`, line 147: "pull-request numbers, task IDs and merge SHAs ... in any quantity including none"), this sits squarely inside the spec's own declared reachable input class, not an out-of-scope synthetic extreme — the `## Input space`-grounded severity-downgrade rule does not apply here. Confirmed via Codex's primary pass (native `[P2]` → major) and reproduced/extended by the adversarial devil's-advocate pass, which additionally found that neither line defines what a *complete* reconciliation looks like: a single coincidental identity match (one expected PR present among N-1 unrelated commits — exactly the moving-base scenario issue #135 itself describes) currently satisfies the text as written, since "the framing carries no identity material" is the only failure mode the fallback defines. Raw captures: `.shell-team/reviews/T-1037-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1037-codex-adversarial.{txt,jsonl}`.
  - Suggested direction (synthesized from the adversarial pass): qualify which identifier types are actually matchable against a bare cycle-window bullet — a merge SHA directly; a PR number only when it is literally present in the subject; a task ID only via a `pr-metadata`-mediated, unambiguous T-NNN→#PR mapping, itself conditional on `gh` availability — and require the same "unverified, record in `## Notes`" disposition whenever any returned commit cannot be positively accounted for by the identity material actually usable, not only when the framing carries zero identity material of any kind.

#### Minor / nits
- `.shell-team/specs/T-1027-promote-retro-2026-08-04.md`'s `## Summarized sources` (outside its frozen intent block) says "eight blocks" where nine `<!-- frozen-begin: X -->` keys exist (E1, E2, E3, O, F1–F5) — already disclosed by QA and correctly routed as an ordinary pm-spec prose correction; non-frozen, no check-line-arithmetic impact. Independently re-confirmed unchanged in this diff (this task's own diff to that spec touches only the F3/F4 fenced blocks).
- `tests/bin-exec-bit/run.sh`'s lock covers `bin/` only; the exec-bit drop this task's own splice mechanism (awk-redirect+`mv`) caused on `tests/check-handoff/run.sh` was caught only by engineer diligence (`b9d0cc8`), not by any checker. Correctly disclosed, correctly left unfixed under this task's own Non-goals (no new checker), and a legitimate residual-risk item — agree with QA's classification.

#### Adversarial findings
- Devil's-advocate pass (run specifically against the two Major findings above, `--sandbox read-only`, reading `agents/scrum-master.md` and `bin/retro-inputs.sh` itself) did not defeat them. It reproduced the primary finding with a more precise account of why the `pr-metadata` bridge doesn't already close the gap (optional, `gh`-auth-gated, its query is never joined to the resolved first-parent window), and it independently surfaced a second, related Major: the text never defines exhaustive/unambiguous reconciliation, so a partial coincidental match currently reads as satisfying it. Both are folded into the single Major entry above rather than listed separately, since they concern the same two frozen lines.
- Separately, my own mutation testing against `tests/check-handoff/run.sh`'s G1/G2 anchors in a scratch `git worktree` (a duplicate matching line to probe the `-eq 1` uniqueness guard, an extra backtick-delimited token inserted before the two required slots, a token mutated to share a leading-byte prefix with the real decoy string, e.g. `T1031_DECOY_BADX`) found no false-accept: every attempted de-shape either correctly tripped the guard's `fail()` or was rejected by the anchor's own character-class boundaries (`[^\`]*`, `[A-Z_]+`). No new Blocker/Major against the #146 surface.

#### Synthesis audit ledger
no severity changes

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1037 entry back to `READY_FOR_ENG`, linked to the two Major findings above (`agents/scrum-master.md:78`, `agents/scrum-master.md:110`). Because S1/S2 are frozen text, the remediation path is a pm-spec class-B re-freeze (of this task's own intent block, taking it to a v2, or an equivalent fresh follow-on task re-freezing T-1027's now-v3 F3/F4 again — whichever venue the coordinating session judges correct), not an engineer-side edit to hashed bytes, and not a revert of the T-1027 v2→v3 re-freeze this task already executed (that re-freeze's own mechanics and authority are independently verified sound above and are not in question).

#### Fast-follow disposition
- The "eight vs nine blocks" miscount in T-1027's `## Summarized sources` — file-an-issue: a low-priority, non-blocking pm-spec prose correction at the next natural touch of that spec; no shipped byte or check-line arithmetic depends on it.
- The `tests/bin-exec-bit/run.sh` `bin/`-only coverage gap (silently drops exec bits outside `bin/`, as it did in this very task's history) — file-an-issue: a real, disclosed residual risk, explicitly out of this task's own Non-goals (no new checker); same disposition QA already recorded, appropriate as a candidate for the next planning input.

<!-- BEGIN review: T-1037 (QA round 2) -->

# QA verification — T-1037 (round 2, post-Codex-rework re-verification)

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

## Scope of this round

Rework landed as **T-1037 v1→v2** (`03bf759c40aeaba06dac26e97691d447bfaa4287`) and **T-1027 v3→v4** (`ec47522655eba1d8a4fc1b55fd108fb95475c2db`), both class-B, ledger recorded at `88a7fcf`; the engineer re-spliced `S1`/`S2` into `agents/scrum-master.md:78,110` (`80136c1`, `d4cf241`). Round-1's Codex verdict (`add35c3`) raised two Majors against the frozen S1/S2 text itself (task IDs listed as matchable identity material `bin/retro-inputs.sh` cannot emit; no whole-window coverage requirement). This round re-verifies the repaired bytes at the source, not by re-reading the board's own account of the repair.

## The Majors' closure — verified at the source, not from the board's account

Read `bin/retro-inputs.sh`'s cycle-window builder directly (lines 302–348, not the spec's paraphrase):
- Window built with `git log --merges --first-parent --pretty=format:'%H%x09%s'` (line 303) — merge commits, first-parent only.
- Each row emitted as `printf '%s %s\n' "${hash:0:12}" "$subject"` (lines 344–347) — **first twelve characters of the SHA, one space, the merge subject, and nothing else.** No PR-number field, no task-ID field, no branch field.
- Confirmed the repository's own merge convention live: `git log --merges --first-parent --pretty=format:'%H%x09%s' -5 8d47eed` → every subject is `Merge pull request #N from owner/branch` — a PR number is visible only because the merge-commit workflow writes it into the subject; a task ID is never present.

Both v2 S1/S2 claims about this format are **TRUE**, confirmed against the tool's own bytes, not restated from the spec.

Read `agents/scrum-master.md:78` and `:110` directly (not from a diff summary):
- **Task-ID-as-matchable is gone.** Line 78: "a task ID is framing-side only — it never appears in a sub-bullet, so treat it as the label your framing maps a SHA or a pull-request number from, never as a token you match." Line 110: "a task ID never appears in a sub-bullet at all and is therefore the label your framing maps a SHA or a pull-request number from rather than a token you match." Neither line lists a task ID as equivalent identity material anymore — the round-1 defect (three equivalent identifiers) is structurally absent, not just reworded.
- **Whole-window coverage is now requirement-shaped.** Line 78: "Reconcile the whole window rather than one hit: every returned sub-bullet must end up either matched to the cycle's own material or explicitly flagged as foreign." Line 110: "Account for the whole window, not one hit: every returned sub-bullet is either matched to the cycle's own material or explicitly flagged as foreign." Both state an exhaustive, per-sub-bullet obligation — a single coincidental match no longer reads as satisfying the norm, which is exactly the second round-1 Major (the "one hit passes" gap the adversarial pass found).

**Both Majors are closed at the source**, independently confirmed against `bin/retro-inputs.sh`'s actual output shape and the live agent-file bytes, not by trusting the ratification prose.

## Three-way identity, live

- `S1` (this spec's v2 block) == T-1027 v4's `F3` == `agents/scrum-master.md:78` — `AC1` check-line, re-run: **PASS**.
- `S2` == `F4` == `:110` — `AC2` check-line, re-run: **PASS**.
- Both `cmp`-joined byte-identical on the programmatic extraction (`awk`/`sed` fence-stripped), not a visual diff.

## check-acs.sh

- `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1037-checker-retro-precision.md` → **13 passed, 0 failed, 1 skipped (AC14, runtime)**. Matches the required post-repair shape exactly (all four rework-affected criteria — AC1, AC2, AC3, AC13 — back to PASS; every other criterion unaffected).
- `bash bin/check-acs.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md` (declared set only) → **8 passed** (AC1, AC2, AC3, AC4, AC6, AC8, AC9, AC11), **4 failed** (AC5, AC7, AC10, AC12 — all inside the permitted merge-point-scoped set), **1 skipped** (AC13). AC8 (load-bearing) = PASS. Exact match to the spec's/board's predicted 8P/4F/1S.

## Frozen-intent alignment

`bin/check-intent.sh` run independently against the board for all stacked neighbours plus this task and T-1027 — all `aligned`: T-1024 v2, **T-1027 v4 (`ec47522655eba1d8a4fc1b55fd108fb95475c2db`)**, T-1029 v5, T-1030 v2, T-1031 v1, T-1033 v1, T-1034 v1, T-1035 v1, T-1036 v5, **T-1037 v2 (`03bf759c40aeaba06dac26e97691d447bfaa4287`)**. Hashes match the board's own ledger records byte for byte.

## Classifier re-derivations (independent, not copied from the board)

Materialised the pre-round-1-rework blobs from `88a7fcf~1` (the commit immediately before the round-1 re-freeze) and ran `bin/check-refreeze-class.sh` against the current spec files:

- T-1037 v1→v2: `check-refreeze-class: class-b: old-spec and new-spec intent blocks have different line counts for T-1037 (old=125 new=126)` → **exit 1**.
- T-1027 v3→v4: `check-refreeze-class: class-b: differing line at intent-block index 105 for T-1027 is not a '- check:' line on both sides` → **exit 1**.

Both re-derivations independently confirm class-B for the round-1 rework itself.

## Mutation replay (scratch git worktree, restored and re-verified)

`git worktree add --detach` under `$TMPDIR`'s scratchpad, never in the working tree:

- Reverted `agents/scrum-master.md:78` (S1's site) to its base-`8d47eed` bytes.
- Re-ran AC1's and AC3's own `check:` lines against the mutated worktree: **AC1 exit 1 (red)**, **AC3 exit 1 (red)** — both fired, as required by this QA task's own instruction (and stronger than the minimal "one word changed" mutation, since a full-line revert to base bytes was used).
- Restored line 78 (`git checkout -- agents/scrum-master.md` inside the scratch worktree only), re-ran both check-lines: **AC1 exit 0, AC3 exit 0** — both green.
- `git worktree remove --force` after; `git status --short` on the **main checkout** clean throughout and after (no mutation ever touched the real working tree).

## Regression net

- `bash tests/check-handoff/run.sh` → all 22 assertions PASS, exit 0. `./tests/check-handoff/run.sh` (direct exec form) → identical, exit 0.
- `git diff 267d078..HEAD -- tests/` → **empty** (byte-identical to round 1; the #146 half is untouched, as the spec declares).
- `shellcheck 0.11.0 tests/check-handoff/run.sh` → clean, exit 0.
- `bash bin/check-prompt-sync.sh` → all registered prompt blocks in sync, exit 0.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 8d47eed` → exit 0.
- `bash bin/check-pii-shapes.sh --base 8d47eed` → clean, exit 0.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1037.md` → conformant (2 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1037 -- .shell-team/interventions/T-1037.md` → conformant (0 entries, 1 sentinel).
- Anti-optionality / no-network sweep, re-run directly against the live lines: `sed -n '78p;110p' agents/scrum-master.md | grep -oE 'spot-check|where possible|if possible|optionally|`gh`|origin/|git log|git rev-list'` → no match (exit 1). Confirms neither line reads as a suggestion and neither implies a network/remote-tracking read.
- `git ls-files -s tests/check-handoff/run.sh` → `100755`. `bash tests/bin-exec-bit/run.sh` → 36/36 `bin/` entries PASS, both non-vacuity controls PASS.

## Ledger

- **T-1027**'s `## Done` entry: `freeze-attestation` present for v1, v2, v3, v4 (all four retained); ratification lines for v1→v2 (issue #136, unrelated to this task), v2→v3 (`R27`, issue #135), v3→v4 (`R28`, issue #135 round-2 repair) each present; exactly **one** `intent-hash` line, at **v4**: `ec47522655eba1d8a4fc1b55fd108fb95475c2db`, matching `check-intent.sh`'s live report.
- **T-1037**'s own entry: `freeze-attestation` v1 and v2 both present; one `intent-ratified (2026-08-06): v1→v2` line; exactly **one** `intent-hash` line, at **v2**: `03bf759c40aeaba06dac26e97691d447bfaa4287`, matching `check-intent.sh`'s live report.
- Board deletion count vs base `8d47eed`, re-measured: `git diff --numstat 8d47eed -- .shell-team/todo.md` deletion column = **1**; the single removed line is exactly the base's own `- intent-hash (v2): 8f4a08deee3d91132e396637ab586cce3df933c7` record (T-1027's v3 and T-1037's v1 records never existed at the base, so their replacement is invisible to a base-relative diff — matches AC10's own accounting exactly).

## Claim-fidelity spot checks (round-2-relevant sources)

- `bin/retro-inputs.sh` — re-read directly at the cycle-window builder (see "Majors' closure" above); the spec's claims about the sub-bullet format match the tool's own bytes exactly.
- `.shell-team/specs/T-1027-promote-retro-2026-08-04.md` — `grep -oE '<!-- frozen-begin: [A-Za-z0-9]+ -->' | sort -u` yields exactly **nine** distinct keys (E1, E2, E3, F1–F5, O); the spec's v2 Summarized-sources correction ("nine blocks", superseding round 1's disclosed "eight") is confirmed accurate against the live file (one further `X` grep hit is prose using `X` as a placeholder variable name in the AC preamble, not a tenth real key).
- `.shell-team/todo.md`'s T-1027 entry — read directly (see Ledger above); matches the spec's `R27`/`R28` frozen bytes and the version-chain narrative exactly.
- GitHub issue #135 — re-fetched via `curl https://api.github.com/repos/RipsawJP/shell-team/issues/135` this session (network reachable); title and body opening match the spec's summary.

## Edge cases / adversarial fixtures tried

- Independently reproduced the line-78 revert-to-base mutation (stronger than "one word changed") and confirmed AC1/AC3 both fire and both restore clean.
- Confirmed the repository's actual merge-commit subject convention live (`Merge pull request #N from owner/branch`) rather than trusting the spec's own characterization of it.
- Confirmed no live sentence lists a task ID as matchable and both lines state exhaustive (not partial) window coverage — the two properties the round-1 Majors specifically found missing.
- CommonMark/markdown-structure class: not applicable — this round's deliverable is single-line prose replacements in an already-structured file; no fenced-block or heading-variance parsing is in play.
- The #146 half (guards) drew zero findings in round 1 and is untouched this round (`git diff 267d078..HEAD -- tests/` empty, confirmed above) — not re-adjudicated beyond the regression-net run.

## Risk notes for reviewer

1. This round's fix is confined to two frozen prose lines (`S1`/`S2`) and their T-1027 mirror (`F3`/`F4`); the #146 half, the board ledger mechanics, and every preservation lock are unchanged since round 1's PASS.
2. T-1027's own merge-point-scoped criteria (AC5, AC7, AC10, AC12) remain red by design (DP-7) — disclosed, not chased, exactly as round 1.
3. The T-1027 v3→v4 re-freeze (this round's own repair) is a **mandatory disclosure item** for the cross-provider round, same as v2→v3 was in round 1; the reviewer keeps an independent veto over it, per `R28`.

## Acceptance criteria: 13/13 checked (12 check-line PASS + AC14 runtime disclosure independently verified above)
## Tests: 13 passed / 0 failed / 1 skipped (check-acs.sh, T-1037); 8 passed / 4 failed / 1 skipped (check-acs.sh, T-1027, declared set only); tests/check-handoff/run.sh green (both invocation forms); tests/bin-exec-bit/run.sh green

<!-- END review: T-1037 (QA round 2) -->
