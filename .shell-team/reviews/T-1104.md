## Spec review

### Codex Spec-Review verdict: REQUEST_CHANGES
- Task: T-1104
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line (`model_reasoning_effort = "max"`); no `-m` override was passed to either invocation this round and no per-turn model identifier appears in either JSONL event stream to cross-check against, so this is the config-resolved value, measured this round rather than relayed.
- Codex CLI: codex-cli 0.149.1
- Mode: spec-review (T-1092 seam, before implementation and before the intent-hash is recorded)
- Spec reviewed: `.shell-team/specs/T-1104-review-input-fidelity.md` (378 lines; intent block + declaration region + Non-goals + Input space + Decision points DP-1..DP-12 read in full)
- Verification mode: static-only

Two Codex passes ran this round: a primary spec-review pass reading the whole spec against its own mandate, and a targeted devil's-advocate pass on the single highest-stakes claim (the verbatim-invocation PII residual), run because the primary pass returned that finding at blocker severity. Both raw outputs are saved (see bottom of this section). Every finding below was independently re-verified against the actual repository text by this role before being kept, downgraded or rejected — the synthesis audit ledger records every place my final judgment differs from Codex's own.

#### Major

- **The verbatim `executor-invocation` field is a real, if partially disclosed, PII residual, and the spec's own citation of its mitigation is incomplete.** `## Goal`'s "highest-value PII surface" paragraph names `bin/check-pii-shapes.sh`'s shape set as `home-path`, `home-path-win`, `home-encoded`, `temp-session` and `email-nonnoreply` (five classes) — but that script's own header (read first-hand at `bin/check-pii-shapes.sh:15`–`:83`) documents **seven**, including a `private-key` shape and a `token` shape (GitHub `gh[oprs]_`, AWS `AKIA`, OpenAI-style `sk-`) that are exactly the two most relevant to a field whose whole content is a real argv. Because the spec's own inventory of its cited mitigation is incomplete, its residual-risk reasoning is under-informed by its own primary source — the exact "factual claim not verified against the primary document" class this round's mandate asked to sweep for. A dedicated devil's-advocate pass on this specific claim (`.shell-team/reviews/T-1104-codex-adversarial.txt`) confirmed the practical effect: the *narrow* claim ("no mechanical protection exists at all") is defeated — known-shape credentials and PEM headers genuinely are caught by the existing diff-scoped CI gate — but the *broader* claim survives: the spec's field contract never states that this protection is a **finite known-shape screen, not comprehensive secret detection**, and the Goal's eleven-item disclosure enumeration has no item naming the residual that actually remains (an unsupported secret prefix, an arbitrary internal API key/password/webhook/bearer token, a token body below the regex's own length minimums, an `sk-` token missing its required left-hand boundary, or a non-PEM private key — all named directly by the adversarial pass's own `residual_after_defense`). This residual is also unlike the raw-capture files this same mechanism deliberately keeps untracked: it lands the invocation string in **permanently tracked** git history rather than a gitignored temp dump.
  - Fix: correct the `## Summarized sources` citation to name the full seven-shape set (or at minimum `token` and `private-key`), and add a twelfth disclosed-not-closed item stating precisely what the diff-scoped shape scan does and does not catch, in the same enumeration style items (i)–(xi) already use.

- **`AC17`'s own pure-insertion premise is factually wrong at the declared base ref, and the corresponding `## Assumptions` row asserts the same false zero.** Measured directly: `git show "$(git merge-base feature/1103-oversight-profiles HEAD):.shell-team/todo.md"`'s `## Active` section carries **two** existing `- [ ] **T-` entries (`T-1089`, `T-1086`, both `BLOCKED`), not zero. `## Assumptions` row 20 states "the board's `## Active` section carries **zero** `- [ ]` top-level entry lines at the branch point, so this task's entry is a pure insertion and AC17's `+1` delta is exact" — that premise is false as written. AC17's own check line is not literally broken by this (it computes both sides at run time as `AB`/`AH` rather than hardcoding a `0`, so the `+1` relative delta still holds regardless of `AB`'s real value), but the false "pure insertion" framing hides a real coverage gap Codex's own reading of AC17 independently raised: a net `+1` cannot distinguish "one entry added" from "one existing entry removed and two added" — and now that the base genuinely carries other live entries this task's own diff must not touch, that ambiguity is a reachable failure mode rather than a theoretical one.
  - Fix: correct row 20's premise to state the true count (two), and strengthen AC17 to assert that every one of the base's existing `## Active` entries still occurs at HEAD (a superset check), not only a net `+1` count.

- **`AC10`'s close-out gate wiring is a co-occurrence check unanchored to any specific block, the exact "satisfiable on the wrong line" defect T-1103's own round 4 found and closed in a sibling criterion.** The check line asserts `check-review-input.sh` occurs at least twice, and that `fail `, `die ` and the phrase `missing or unreadable next to close-out.sh` each occur **somewhere** in `bin/close-out.sh` — never that they occur together, in the same gate block, wired to this checker's own exit-status mapping. `bin/close-out.sh` already contains many other `fail`/`die` call sites for its other gates, so an implementation could satisfy every literal in this criterion by pasting the phrase and the two literals anywhere in the file while never actually wiring `check-review-input.sh`'s `1`→`fail`/other→`die` mapping the criterion's own prose claims to verify. This is the identical shape as T-1103 round 4's Major on `AC2`'s `check-refreeze-class.sh`/`check-oversight.sh` co-occurrence (closed there by anchoring to the more specific `--old-hash` phrase) — this is now a **second occurrence of that same defect class** across this task family, which is worth naming explicitly for the coordinating session's own same-class-2 judgment even though this is a spec-review round rather than a rework round.
  - Fix: extract the specific gate block this task adds (e.g. by first isolating the lines between a distinctive anchor this task's own diff introduces) before asserting the co-occurrence, or require the three literals to occur within a bounded number of lines of each other.

- **`AC12`'s briefing-duty check has the same unanchored shape, one line away from the actual `:98` bullet it claims to verify.** The check asserts `executor-invocation`, `pass-role`, `briefing-fidelity` and `bin/check-review-input.sh` each occur **somewhere** in `skills/run/SKILL.md`, never that they occur inside the specific briefing bullet at line 98 the criterion's own prose names. Lower practical risk than AC10 today (these are genuinely new tokens with no plausible pre-existing occurrence elsewhere in the file), but the criterion as written would equally pass an implementation that appended all four literals to an unrelated bullet elsewhere in the same file, leaving line 98's actual briefing duty untouched — the same defect class as AC10, a third instance in this one spec.
  - Fix: extract the line-98 bullet's own bounded region (e.g. between its own list marker and the next top-level bullet) before asserting the four literals occur inside it.

- **`AC2`'s fail-closed matrix does not test everything the Goal promises it refuses.** The Goal's own parenthetical lists four classes that must exit 2: "an unreadable record, a non-regular occupant at the record path, an unresolvable reviews directory, a usage error." AC2's four fixture cases cover an unknown flag (usage), a nonexistent record path, a directory occupant, and a dangling symlink — all mapped to `record-unreadable` or `usage`, but **no case exercises "an unresolvable reviews directory"** at all, and no criterion or Note anywhere describes the concrete mechanism by which the checker would ever need to resolve a reviews directory on its own (every fixture in every other criterion invokes it with an explicit `--record <path>`). This is prose in the Goal promising a behavior with no corresponding engineering requirement or test anywhere in the twenty-one criteria — the "prose wider than the locked check line" class the mandate asked to sweep for.
  - Fix: either name the concrete code path that triggers this refusal (e.g. a `--task`-only invocation that must resolve the record's own default location through `bin/team-paths.sh`) and add a fixture for it, or narrow the Goal's parenthetical to the three cases actually specified and tested.

#### Minor / nits

- **The raw-capture stem's pass-name half of the "closed at three stems" vocabulary is never mechanically enforced by `check-review-input.sh` itself.** The Goal only requires the checker to validate a `raw-capture` value's task-id prefix and its uniqueness within the record (**AC7**); nothing validates that the stem's pass-name component is one of `codex-primary`/`codex-adversarial`/`codex-specreview`. The "stays closed at three" claim is enforced only in `agents/codex-reviewer.md`'s prose, and **AC11** only proves the one now-known deviation (`codex-impl`) is absent from that one file — a future, differently-named deviation would pass both the checker and every current criterion.
- **`AC4` never exercises the corpus's own most common multi-pass shape: two passes inside one verdict section.** Input space item 4 names this class explicitly ("a primary and, when a blocker or major fired, an adversarial companion"), and it is exactly the shape most of `.shell-team/reviews/T-1103.md`'s implementation-mode sections take — yet AC4's third case tests two *sections* each with one block, never one section with two. `AC5`'s duplicate-field case likewise generalizes "any one field written twice" from a single tested field (`pass-role`); the other three fields' duplicate case is untested.
- **`AC14` verifies only one of the field contract's four prohibited classes reaches the adopter-facing pages.** The Body-to-AC correspondence table's own row 29 discloses this split candidly ("AC15 (the header literals), AC14 (`never a credential` in both pages)"), but the practical effect is that an adopter reading `docs/adopting.md`/`.ja.md` is guaranteed to see only the credential prohibition; the environment-dump, absolute-path and identity prohibitions are verified only inside the script's own header comment, which no adopter reads.
- **`AC20`'s population-derivation check proves a marker block and a reproduce line exist together, not that the block's content is the tool's actual output.** A hand-typed block matching the `BEGIN derivation:`/`END derivation:` shape with a plausible-looking body would satisfy every assertion AC20 makes; nothing diffs the committed block against a live re-run of the named `- reproduce:` command.
- **`AC16`'s declaration-region extraction never asserts the file's own `END intent-block` marker actually falls inside the region it extracts.** The `sed` range runs from `BEGIN intent-block` to the first `## Non-goals` line regardless of where `END intent-block` physically sits; the criterion separately asserts `END` occurs exactly once *somewhere* in the file, never that it occurs between `BEGIN` and `## Non-goals` in the expected order.
- **Four "read first-hand, all N lines" claims in `## Summarized sources` are off by one against a direct re-measurement.** `wc -l` on the branch-point-identical current copies gives `agents/codex-reviewer.md` 223 (claimed 224), `bin/codex-capture.sh` 283 (claimed 284), `tests/gitignore-raw-dumps/run.sh` 144 (claimed 145), and `docs/loop-engineering/record-tamper-resistance.md` 147 (claimed 148); all four files end with a trailing newline, so this is not a counting-method artifact. None of these gate an actual criterion — the only checks that reference these totals (`## Assumptions` rows 16–17, AC1's and AC11's non-vacuity floors) are all `-ge` floors well under the true counts — but they are exactly the "factual claim not verified against the primary document" class this round's mandate named.

#### Domain-premise questions answered directly (per this round's mandate)

1. **Blast radius of amending `agents/codex-reviewer.md:212` against unmeasurable adopter-side reliance.** Adequately, honestly bounded. The spec *measures* rather than assumes that no committed script implements the stated ground, names the exact five tracked files that do reference the literal, and frames the cost candidly ("prose plus proving the hygiene suite's eight literals unmoved, not a rewrite of a detection mechanism"). It does not claim the adopter-side population is safe — it states the population is unmeasurable from here and proceeds on that basis, which is this repository's own established posture for a genuinely unmeasurable premise (the same shape `## Assumptions` uses for every relayed item). No fresh gap found beyond what is already named.
2. **Verbatim-invocation safety in adopter environments.** Partially bounded — see the Major finding above. The field contract, no-echo discipline and the existing diff-scoped PII gate are real and meaningfully reduce the risk (more than either Codex's primary pass or my own first reading assumed, once the full seven-shape set is accounted for), but the spec's own citation of that mitigation is incomplete and the disclosure enumeration omits the specific residual that survives even the full shape set. This is a real, fixable gap, not a foot-gun shipped with no mitigation at all.
3. **Does the closed four-field grammar make the three T-1086 gaps checkable after the fact?** Partially, and unevenly across the three. Gap 1 (briefing not reaching the primary pass) and gap 2 (confirmation vs. seeded pass indistinguishable) become genuinely, mechanically legible from committed record bytes — the `briefing-fidelity` and `pass-role` fields plus the checker's grammar validation are real, verified teeth for those two. Gap 3 (raw captures overwritten across rounds) is only partly closed: the naming-rule amendment (`AC11`) closes the *structural cause* of the overwrite independently of the checker, and `AC7`'s collision/prefix check catches an accidental same-stem duplicate inside one record — but neither catches a cross-record collision (already disclosed, item viii) nor a case where the recorded stem is distinct while the underlying raw was itself overwritten by an unrelated bug. The Goal's own one-sentence framing ("checkable after the fact") should distinguish "recorded and legible" from "verified," specifically for gap 3.
4. **Is duty C's arm-B disposition honestly disclosed, and is AC7 sufficient teeth?** Yes, honestly disclosed, at the same standard this repository's own convention already applies to duty-C-shaped residuals elsewhere (T-1103's own duty C was accepted at this standard). AC7's collision/prefix check is real, if modest, mechanical teeth against the narrowest slice of gap 3 (the same-record duplicate case) — the Goal's summary sentence is the only place worth tightening, per point 3 above.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | verbatim `executor-invocation` field carries an undetected-secret risk into permanent tracked history | primary (Codex pass): blocker; adversarial (targeted devil's-advocate pass on this claim): claim-partially-holds | major | the narrow "zero protection" reading is defeated — `bin/check-pii-shapes.sh` actually ships seven shapes, not the five the spec cites, including `token` and `private-key`, which catch known credential prefixes; the broader "citation incomplete, residual undisclosed" reading survives and is kept as a Major |
| 2 | base-ref residual (rebase/force-push/squash/fetch-depth/open-PR state not provable from committed bytes) | primary (Codex pass): major | rejected (no defect) | the exact same residual list is already stated verbatim in the spec's own `base-ref-discriminator:` declaration ("a predecessor rebased, force-pushed or squash-merged... is a route-back... a checkout that never fetched the predecessor... is a fetch-depth gap and a route-back") — not a fresh gap |
| 3 | adopter-side reliance on the amended naming rule is unmeasurable from this repository | primary (Codex pass): major | rejected (no defect) / affirmed as domain-premise answer 1 | this is exactly the premise the task's own dispatch line elects cross-provider review for, and the spec already measures and discloses it as unmeasurable rather than overclaiming safety — matches this repo's own accepted disclosure standard |
| 4 | raw-capture stem's pass-name vocabulary not enforced by the checker | primary (Codex pass): major | minor | the Goal only ever promises task-id-prefix and collision validation for `raw-capture` (AC7); a closed-vocabulary stem-name check was never a stated requirement, so the gap is real but narrower than framed |
| 5 | Goal's four-field record does not prove execution, delivery or role-truthfulness | primary (Codex pass): major | minor (wording precision only) | the substance is already covered by the Goal's own items (i)/(iii)/(iv) of its eleven-item disclosure; only the one-sentence summary's phrasing for gap 3 specifically merits tightening (folded into domain-premise answer 3) |
| 6 | duty C / AC7 raw-presence and collision teeth insufficient against gap 3 | primary (Codex pass): major | rejected as a fresh defect (already disclosed at items v/vi/viii); same wording point folded into finding 5 | the spec's own eleven-item enumeration already states exactly this limit; nothing new found beyond the summary-sentence wording addressed above |
| 7 | AC19's runtime evidence is self-reported with no tracked artifact | primary (Codex pass): minor | rejected (no defect) | this is the standing, already-accepted convention for every `unit-and-static`-ceiling runtime `SKIP` item in this repository (see T-1103's own board self-report for its equivalent item), not a T-1104-specific gap |
| 8 | `## Assumptions` row 21's "four obligations" claim vs. its own command's literal count of three headings | primary (Codex pass): major | rejected (no defect) | the row's own parenthetical already discloses the mismatch ("read the section headings, not only the count") — a reader following the instruction correctly reconciles it |
| 9 | AC14 doc-contract check is a global, unanchored search | primary (Codex pass): major | minor | the searched literals are genuinely novel tokens with no plausible pre-existing false-positive location today, unlike AC10/AC12's real collision risk; kept as a narrower minor about single-class coverage and unverified placement, not the wrong-line defect class |
| 10 | AC20 population-derivation block could be hand-forged | primary (Codex pass): major | minor | the block-plus-reproduce-line requirement already satisfies this repository's own derived-population discipline; the deeper forgeability concern is real but a narrow Tier-1 edge case rather than a structural gap |
| 11 | AC4/AC5/AC21 input-space coverage gaps (same-section two-pass, generalized duplicate-field claim, heading-family exercise) | primary (Codex pass): major | minor | real coverage gaps, but modest risk since a natural single-loop implementation would likely already handle the untested shapes correctly; the AC21 heading-family point is dropped from this list since it depends on when the dogfood check runs relative to this task's own record, not on a fixture defect |

#### Recommendation

- REQUEST_CHANGES → route back to the spec's author for a meaning-preserving correction to the domain premises and the named criteria (never by weakening or removing a criterion a finding is about). This task's board entry reads `- entry-mode: pm-authored`, cross-checked against the Routing Map's own `- dispatch: specify — pm-authored` line printed at Plan — the two agree, so the route-back target is **pm-spec**. Close or disclose the two Majors on `AC10` and `AC12` (unanchored co-occurrence), the `AC17`/Assumptions-row-20 factual error, the `AC2` fail-closed matrix gap, and the verbatim-invocation PII citation/disclosure gap before the freeze sweep proceeds. The Minor items are recommended for inclusion in the same correction, not deferred past it.
- **Same-class-2 flag, for the coordinating session's own judgment, not a call this role makes.** The "co-occurrence check satisfiable on a line other than the one it claims to verify" defect (`AC10`, `AC12` here) is the identical class T-1103's own round 4 found and closed on `AC2`'s `check-refreeze-class.sh`/`check-oversight.sh` pair. This is a spec-review round rather than a rework round, so this task's own pre-commitment trigger (which fires on rework rounds against the same component) does not literally apply — but the class itself has now recurred across two consecutive tasks in this family, which is worth this task's author naming explicitly when correcting, so the pattern is caught by construction (e.g. a shared review checklist item) rather than needing a third occurrence to notice.
- Provenance note (environmentally-unverified issue-canon reliance): this verdict's reading of the requirement is internally consistent with the spec's own cited sources (`agents/codex-reviewer.md`, `bin/codex-capture.sh`, `/.gitignore`, `tests/gitignore-raw-dumps/run.sh`, `tests/codex-skeleton-hygiene/run.sh`, `bin/close-out.sh`, `bin/check-commit-identity.sh`, `bin/check-pii-shapes.sh`, `.github/workflows/check-handoff.yml`, `docs/loop-engineering/record-tamper-resistance.md`, `.shell-team/reviews/T-1103.md`, `.shell-team/todo.md` and the two git ref files), every one of which this role opened first-hand rather than relaying; issue **#335** itself was not independently read by this role (no GitHub-reading tool in this sandbox) — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Fast-follow disposition

- The raw-capture stem's pass-name vocabulary being unenforced by the checker (ledger finding 4) — recommend **won't-fix for this task, revisit if a fourth stem is ever proposed**: the Goal never promised this enforcement, and adding it now would be scope growth beyond what issue #335 or this task's own duties require; worth a one-line Non-goal-style acknowledgment rather than a code change.
- AC20's forgeability against a hand-typed derivation block (ledger finding 10) — recommend **file-an-issue**: this is a repository-wide gap in how every spec's derived-population blocks are checked, not specific to T-1104, and belongs in the same mechanism this repo would build once `bin/derive-populations.sh`'s own output gets a verifiable signature or re-run comparison.
- Everything else in Minor/nits above is recommended for inclusion in the same correction this round's route-back already requires, not deferred past it.

Raw Codex output saved at `.shell-team/reviews/T-1104-codex-specreview.{txt,jsonl}` (primary spec-review pass) and `.shell-team/reviews/T-1104-codex-adversarial.{txt,jsonl}` (targeted devil's-advocate pass on the verbatim-invocation PII claim).

## Spec review — round 2

### Codex Spec-Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1104
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line (`model_reasoning_effort = "max"`); no `-m` override was passed and no per-turn model identifier appears in the JSONL event stream to cross-check against, so this is the config-resolved value, measured this round rather than relayed.
- Codex CLI: codex-cli 0.149.1
- Mode: spec-review (T-1092 seam, before implementation and before the intent-hash is recorded)
- Spec reviewed: `.shell-team/specs/T-1104-review-input-fidelity.md` (424 lines; `## Spec-review round 1 corrections (2026-08-29)` section, the intent block, all twenty-one acceptance criteria, `## Assumptions` rows 20 and 23–31, and `## Reviewer scope (two-tier)` read in full)
- Verification mode: static-only

One Codex pass ran this round (a single synchronous `codex exec` free-form prompt reading the spec, the round-1 review record and the cited primary sources directly); no adversarial pass was triggered, because this round's own findings are Majors on the same already-flagged criteria rather than a fresh blocker-shaped claim. Before invoking Codex, this role independently re-measured every one of round 1's five factual corrections against their cited primary sources with its own file-reading tools — the PII shape count, the base-ref `## Active` entries, the `bin/close-out.sh` anchor-literal counts and the two shipped 16-line precedent spans, the `skills/run/SKILL.md` anchor, `bin/check-spec-review.sh`'s resolution shape, and the `agents/codex-reviewer.md` line-195/`-r<N>` counts. Every one of round 1's five Majors is confirmed genuinely, factually closed on the measured ground it now cites. Codex's own pass corroborated this independently and surfaced additional findings, all folded into the ledger below with this role's own severity judgment recorded against Codex's.

#### Major

- **`AC10`'s narrowed 30-line window proves co-occurrence of the checker-qualified literals, `fail`/`die`/`1)`, not that the `1`→`fail`/other→`die` mapping is actually wired to an invocation of `check-review-input.sh`.** The window requires `A2`, a `fail ` call, a bare `1)` arm and two `die ` calls to sit near the `A1` anchor, but nothing in the check line requires a `bash "$SCRIPT_DIR/check-review-input.sh"`-shaped invocation, or that the `fail`/`die` calls consume *that* invocation's own captured return code, inside the window. Decorative text carrying the right literals near a repurposed pre-existing `case` arm from an unrelated gate could satisfy every assertion without the gate doing anything. This is a **narrower** version of round 1's own Major 3 finding — not a fresh site, but evidence the fix reduced the false-positive surface (from "anywhere in a 600+-line file, several literals already present at baseline" to "a specific 30-line region, all literals absent at baseline") without eliminating the underlying "co-occurrence stands in for wiring" pattern the corrections table's own prose claims to have closed ("proves the `1`→`fail`/other→`die` mapping sits in this gate's own block" — `AC10`'s own words).
  - Fix: within the window, additionally require a line matching an invocation of `check-review-input.sh` (e.g. `bash "$SCRIPT_DIR/check-review-input.sh"` or the sibling `$CHECKER` variable pattern the shipped precedents use) that precedes the `case` arm, and require the `case` variable to be the same token the invocation's exit status was captured into — mirroring the two shipped precedents' own `SR_ERR=$(bash "$SPEC_REVIEW_CHECKER" ...)`/`case "$SR_RC" in` shape literally.

- **`AC11`'s same-line co-occurrence check does not tie the `-r<N>`/`from the second round onward` pair to the naming-rule bullet's own identity or location, and the "at most one base line missing" bound does not require that missing line to be line 212.** An implementation could delete the naming-rule bullet outright (satisfying the "amended... never a bare stem" zero-count for the prohibition phrase, and consuming the one permitted missing-base-line budget) while writing the co-occurring pair on an unrelated new line elsewhere in the file, disconnected from any actual amendment — passing every literal assertion while the naming rule the criterion's own title claims is "amended" is in fact simply gone, with a decoy line standing in for it. This is a **fresh, independently-new** instance of the identical class round 1 dispositioned as occurrence 1 on `AC10`/`AC12`: `AC11`'s same-line fix is itself new work from round 1's own vacuity sweep (it was not one of round 1's original five Majors), and it exhibits the same "prose claims a specific location, check proves only an unlocated co-occurrence" shape.
  - Fix: require the missing base line to be, specifically, the line that contained `never vary the filename or directory per round` (compare its line number or its surrounding context rather than only counting misses), and require the co-occurring `-r<N>`/`from the second round onward` line to appear within a small window of that same location — not anywhere satisfying the file-wide grep.

- **`AC12`'s bullet-region extractor stops only at a 3-space bullet, a numbered step, or a `## ` heading — not at any line less indented than the anchor.** A line inserted directly after `:98` with different indentation (a bare paragraph, a differently-marked list item) is included in the extracted region without being genuine continuation of that bullet, so the four literals could land in dead space between the anchor and the next recognized boundary rather than inside the briefing bullet itself. This is the same pattern as `AC11` above and is likewise a fresh, independently-new instance (this extractor is new work from round 1's own Major 4 fix, narrower than the original whole-file search but not airtight).
  - Fix: stop the region also on any non-blank line whose leading whitespace is not deeper than the anchor's own three spaces, so only genuine, more-deeply-indented continuation is captured.

- **`AC14`'s positional extraction never verifies that the final `^## ` heading actually is `## Operating rules` / `## 運用ルール`.** Because the additive-only check is presence-based rather than order-based, a change that reordered an existing heading to sit last (displacing Operating Rules) while inserting the new section as the new penultimate heading would still satisfy every assertion `AC14` makes, even though the adopter-surface promise ("placed immediately before each page's `## Operating rules` section") would be violated. This is the same "positional proxy stands in for identity" pattern, freshly instanced here (this page's placement fix was round 1's ledger-9 minor, not one of the five majors, and is new anchoring work).
  - Fix: additionally assert the last `^## ` heading's literal text equals `## Operating rules` (`docs/adopting.md`) and `## 運用ルール` (`docs/adopting.ja.md`) before extracting the penultimate section.

**Classification, written down before any trigger is judged, per this task's own pre-commitment discipline.** `AC10` and `AC12` above are a **propagation gap from round 1's own fix**, not a fresh occurrence: the same two criteria round 1 already flagged, now narrower but not fully closed. `AC11` and `AC14` are **independently new**: both anchoring fixes are new work product from round 1's correction pass (never part of the original five Majors), and both exhibit the identical "co-occurrence proxy for a located, structural claim" defect shape round 1 named explicitly as a recurring class and recorded at occurrence 1 (`AC10`+`AC12`, combined, per the board's recorded judgment). Two independently-new sites in this round meet the "second INDEPENDENT occurrence" description the task's own procedural framing names — flagged here for the coordinating session's own same-class-2 judgment, exactly as round 1 flagged its own first firing; this is not a call this role makes.

#### Minor / nits

- **`AC2`'s real-directory positive control does not prove path composition or the `$TEAM_REVIEWS_DIR`-unset fallback.** The control uses a valid, well-formed record already placed at the composed path and asserts exit 0; a checker that returned 0 for any `--task`-only invocation against a directory, without ever composing or reading `<reviews>/<task>.md`, would pass this control. The `else` arm (falling back to the sibling `bin/team-paths.sh --get reviews` when `$TEAM_REVIEWS_DIR` is unset) is likewise never exercised by any fixture. Kept at Minor rather than Major: `AC2`'s own prose claims only that the positive control attributes the refusal to "the non-directory... not... `--task`-only being an unsupported form," a narrower claim the control does satisfy — this is a coverage gap beyond what the prose promises, not a contradiction of it.
  - Fix: replace the positive-control record with one that is well-formed but has a distinguishing marker, and additionally assert the checker's stdout or exit behaviour differs from a record placed at a *different* path under the same directory (proving composition occurred); add a case with `$TEAM_REVIEWS_DIR` unset and a scratch `bin/team-paths.sh` shim returning a real directory.
- **Four citations of `credential`'s two occurrences in `docs/adopting.ja.md` (Goal, `## Summarized sources`, `## Assumptions` row 28, and the round-1 corrections table) state the second occurrence at `:504`; a direct re-measurement (`grep -n`) places it at `:503`.** `:504` begins `real-environment`. This is the identical "factual claim not verified against the primary document" class round 1's own corrections pass swept for and corrected in four other places (the line-count claims) — it recurred a fifth time, inside the very corrections meant to close the class, and in all four places the citation appears. It gates no check line (`AC14`'s check greps for the literal `credential`'s presence in the extracted region, never a line number), so it is a prose-precision defect only.
  - Fix: correct all four citations to `:488` and `:503`.
- **The acceptance-criteria preamble's claim that "every zero-count assertion below is anchored to a line-start form and scoped to named files, never to this spec" is not literally true of every zero-count in the file.** `AC11`'s zero-counts (`never vary the filename or directory per round`, `no -round2 suffix`, `completion-detection greps these canonical basenames`, `codex-impl`) are plain `grep -Fc` substring counts with no `^` anchor (scoped to a named file, so this is harmless in practice, but not "anchored to a line-start form" as stated); and `AC20`'s `^DERIVATION-PENDING` zero-count is explicitly run against `.shell-team/specs/T-1104-review-input-fidelity.md` — this spec itself — which the preamble's "never to this spec" clause reads as forbidding, though `AC16` also legitimately targets this spec's own declaration region by design. The preamble's generalization appears to describe a narrower, real protection (this spec's own prose mentioning a governed token mid-line, inside backticks, must not accidentally satisfy a zero-count meant for some *other* file's content) rather than a categorical ban on any criterion reading this spec file at all.
  - Fix: state the intentional exceptions explicitly (self-targeting criteria like `AC16`/`AC20`, and unanchored-but-file-scoped counts like `AC11`'s), rather than leaving a preamble claim that a careful reader can falsify against the criteria immediately below it.
- **The two shipped-precedent spans `bin/close-out.sh:505`–`:521` and `:543`–`:559`, cited as "span 16 lines from their sibling screen's `die` to their `esac`," are 17 lines wide inclusive** (re-measured this round: `die` at `:505`, `esac` at `:520`, and `die` at `:543`, `esac` at `:558` — the citation's own line numbers are off by one at the closing end, and the inclusive count is 16 lines by endpoint *distance*, 17 by inclusive line count). Gates nothing (`AC10`'s `30`-line window is not computed from this number), a wording-precision nit only.
  - Fix: say "16 lines apart" or restate the inclusive span as 17 lines, and correct the closing line numbers.

#### Domain-premise question re-answered (only where the corrections moved it)

**Is the verbatim-invocation PII residual disclosure (new item xii) honest and complete now?** Yes. Independently re-measured against `bin/check-pii-shapes.sh:15`–`:86`: seven shapes exist, matching the corrected citation exactly (`home-path`, `home-path-win`, `home-encoded`, `temp-session`, `email-nonnoreply`, `private-key`, `token`), and the `sk-` left-boundary disclosure Item (xii) cites is genuinely at `:78`–`:86`. The five residual classes item (xii) names (an unsupported credential prefix, an arbitrary key name carrying a secret as its value, a sub-minimum token body, an `sk-` token missing its left boundary, a non-PEM private key) are each grounded in that file's own documented shape set rather than supposed, and the compensating-control framing (contract plus pre-commit read plus redaction remaining conformant) is consistent with `AC8`, `AC9`, `AC14` and `AC15` as cross-referenced. Codex's independent pass reached the same conclusion without prompting on this specific point. No residual gap found; this domain premise's answer from round 1 is unchanged and now more firmly grounded.

#### Synthesis audit ledger

| # | finding | primary (Codex pass) | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `AC10`'s 30-line window doesn't verify actual invocation wiring, only literal co-occurrence | major | major | kept as-is; this role's own re-reading of the check line confirms the gap Codex names, and round 1's own precedent (its Major 3, under the identical Reviewer-scope Tier-2 framing already present in the pre-correction spec) treated this exact "co-occurrence stands in for a located, structural claim" pattern as Major rather than a downgradeable guard-completeness gap |
| 2 | `AC11`'s same-line assertion doesn't tie the pair to the naming-rule bullet's identity; deletion-plus-decoy passes | major | major | kept; independently re-verified the deletion-plus-decoy path is reachable given the check line's own logic (exactly one base line may go missing, and nothing requires it to be line 212) |
| 3 | `AC12`'s awk extractor's stop conditions miss a dedented, unrecognized line shape | major | major | kept; independently re-verified against the actual file's bullet indentation (`   - `) and confirmed the extractor's three stop patterns do not cover an inserted line at any other shape |
| 4 | `AC14` never verifies the final heading's identity, only that the new section precedes whatever heading happens to be last | major | major | kept; independently re-verified the check line reads `LL` positionally with no literal-text assertion on it |
| 5 | `AC2`'s real-directory positive control doesn't prove path composition; the `$TEAM_REVIEWS_DIR`-unset fallback is untested | major | minor | downgraded — `AC2`'s own prose claims a narrower property (attributing the refusal to the non-directory rather than to `--task`-only being unsupported) than the one Codex's finding measures it against, so this is guard-completeness beyond the stated claim rather than a contradiction of it, and the operator's Tier-2 re-pricing for this spec's own inline check lines applies |
| 6 | the `docs/adopting.ja.md` `credential` citation states `:504`, actual is `:503` | nit | minor | raised one level — this is the fifth instance of the exact factual-citation-not-verified-at-source class round 1's own corrections pass named and swept for, recurring inside the corrections themselves and in four places in the frozen text, which is worth more weight than a single stray line-number typo even though it gates no check |
| 7 | preamble's "anchored to line-start... never to this spec" claim is not literally true of `AC11`'s and `AC20`'s own zero-counts | minor | minor | kept; a documentation-precision issue with no functional consequence, since the actual criteria behave correctly and safely despite the overbroad prose |
| 8 | the two shipped 16-line precedent spans are 17 lines inclusive | nit | nit | kept; gates no check line, pure wording precision |

#### Recommendation

- REQUEST_CHANGES (round 2) → route back to the spec's author for a meaning-preserving correction to `AC10`, `AC11`, `AC12` and `AC14` (never by weakening or removing a criterion a finding is about). This task's board entry reads `- entry-mode: pm-authored`, cross-checked against the Routing Map's own `- dispatch: specify — pm-authored` line printed at Plan — the two agree, so the route-back target is **pm-spec**, unchanged from round 1. The four Minor/nit items are recommended for inclusion in the same correction, not deferred past it, matching round 1's own convention.
- **Same-class-2 flag, restated and sharpened for the coordinating session's own judgment, not a call this role makes.** Round 1 recorded the "co-occurrence check satisfiable on a line other than the one it claims to verify" class at occurrence 1 (`AC10`+`AC12`, combined). This round finds that class's fix incomplete on those same two criteria (a propagation gap, not a fresh occurrence) **and** two fresh, independently-new instances of the identical class on `AC11` and `AC14` — both of which are new anchoring work product from round 1's own correction pass rather than pre-existing sites. Two independently-new instances within one round meets the "second INDEPENDENT occurrence" description this task's own procedural framing names for firing same-class-2. Worth naming for the author explicitly, per this task's own pre-commitment discipline ("the classification independently-new versus propagation gap ... is written down explicitly" before a trigger is judged) — done above. Whether this recurrence across two consecutive rounds of the same task family, now on four sites rather than two, changes the coordinating session's own disposition (e.g. whether the underlying anchoring methodology — narrow-window textual co-occurrence standing in for located, structural claims — needs a shared, reusable checklist item or helper pattern rather than a fourth per-criterion bespoke fix) is this role's observation to raise, not its call to make.
- Provenance note (environmentally-unverified issue-canon reliance): unchanged from round 1 — this verdict's reading is internally consistent with every source this role opened first-hand this round (`bin/check-pii-shapes.sh`, `.shell-team/todo.md` and the two git ref files, `bin/close-out.sh`, `skills/run/SKILL.md`, `bin/check-spec-review.sh`, `agents/codex-reviewer.md`, `docs/adopting.md`/`.ja.md`, and `.shell-team/reviews/T-1104.md`'s own round-1 section); issue **#335** itself was not independently read by this role (no GitHub-reading tool in this sandbox) — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Fast-follow disposition

- No fast-follow deferrals this round: all four Majors and four Minor/nits above are recommended for inclusion in the same correction the route-back already requires, not deferred past it. Round 1's own two fast-follow dispositions (the raw-capture stem's pass-name vocabulary, `AC20`'s forgeability) are unchanged by this round and are not restated here.

Raw Codex output saved at `.shell-team/reviews/T-1104-codex-specreview-r2.{txt,jsonl}` (the `-r2` suffix, per the convention this spec itself formalizes for a task's second round of a given pass family).

## Spec review — round 3

### Codex Spec-Review verdict: REQUEST_CHANGES (round 3)
- Task: T-1104
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line (`model_reasoning_effort = "max"`); no `-m` override was passed and no per-turn model identifier appears in the JSONL event stream to cross-check against, so this is the config-resolved value, measured this round rather than relayed.
- Codex CLI: codex-cli 0.149.1 — this round's own `codex --version` run, transcribed verbatim.
- Mode: spec-review (T-1092 seam, before implementation and before the intent-hash is recorded)
- Spec reviewed: `.shell-team/specs/T-1104-review-input-fidelity.md` (449 lines at this round; the intent block, all twenty-one acceptance criteria, `## Reviewer scope (two-tier)`, `## Pre-commitment`, and both prior corrections sections read in full)
- Verification mode: static-only

One Codex pass ran this round (a single synchronous `codex exec` free-form prompt, run to completion with `< /dev/null`, reading the spec and the round-1/round-2 review record directly); no adversarial pass was triggered — this round's mandate was narrower (re-verify four already-fixed Majors, check authority attribution, hunt fresh defects under the operator's Tier-2 calibration) and none of Codex's own fresh findings reached blocker-shaped stakes. Before invoking Codex, this role independently re-measured all four round-2 identity anchors against the real repository files with its own file-reading tools — the `bin/close-out.sh` sibling-path assignments and the 18-line/16-line precedent spans (`:503`–`:520`, `:541`–`:558`), `agents/codex-reviewer.md:212`'s composition and its `:211`/`:213` neighbours (each occurring exactly once), `skills/run/SKILL.md:98`'s three-leading-space measurement and the anchor phrase's single occurrence, and both adopter pages' final headings (`## Operating rules` at `docs/adopting.md:707`, `## 運用ルール` at `docs/adopting.ja.md:675`, each the last of fourteen `^## ` headings) — and additionally hand-traced AC10's `<CHK>`/`<RC>` extraction logic against the real shipped `bin/close-out.sh` precedent (substituting `check-spec-review.sh`'s own anchor for the not-yet-existing `check-review-input.sh` one) to confirm the awk/sed mechanics correctly bind to `SPEC_REVIEW_CHECKER`/`SR_RC` and correctly order the invocation before the `case`. All four round-2 Majors are confirmed genuinely, factually closed on the measured ground they now cite; nothing from rounds 1–2 is reopened here.

**Severity calibration applied, per the operator's 2026-08-29 ruling recorded in this spec's own `## Reviewer scope (two-tier)` section.** Findings below about this spec's own inline `- check:` lines are priced as guard-completeness (a contrived edit the guard doesn't catch) versus real instrument defects (broken as a command, vacuous, or measured-contradictory with the criterion's own prose or another frozen criterion) exactly as that section states. Tier 1 — the shipped mechanism design, `bin/check-review-input.sh`'s design, its suite, the close-out wiring design, the CI step, the two agent/skill amendments and the two adopter-doc sections — keeps full severity throughout.

#### Major

- **`AC5`'s own prose contradicts itself on the case count: "Nine cases in one run" in its opening sentence, "the complete four-field block **the six cases are each derived from**" in its closing positive-control sentence.** This is not a guard-completeness gap in an inline check — it is the criterion's own prose disagreeing with itself, the exact "measured-contradictory with its own prose" class the operator's calibration keeps at full severity. Traced to its likely origin: round 1's own corrections table records "**Case count restated from six to nine**" for this exact criterion (round-1 corrections table, Minor ledger-11-second-half row), confirming the criterion's case count was deliberately changed from six to nine when round 1 widened the duplicate-field test from one field to all four — the opening sentence was updated, the closing positive-control clause was not. The actual check line runs 1 positive fixture + 4 missing-field fixtures + 4 duplicate-field fixtures + 2 charset-invalid fixtures (`for bad in 'P1' 'p 1'`) = 10 refusal invocations, which is consistent with reading "nine" as nine *categories* (grouping the two charset sub-values under one "pass id outside the charset" case, as the parenthetical "(an uppercase letter and a space are each tried)" implies) — but under no reading of the check line does "six" name anything real.
  - Fix: correct the closing clause to read "the nine cases are each derived from" (or otherwise match whatever the corrected category count is), and, since this is now the second time this criterion's own numbers have drifted from its check line, re-read `AC5`'s prose against its check line once more in full before freezing.

- **`AC10`'s `tests/close-out/run.sh` clause claims a "strict superset," but its check line only proves the base's dispatch cases were not lost — never that a new one was gained.** The prose: "`tests/close-out/run.sh` gains at least one `^dispatch_case ` call site or an equivalently anchored new case, measured as a strict superset of the base blob's own set of that anchored form." The check line computes `comm -23 "$T/dcb" "$T/dch"` (base-minus-head) and asserts it empty — a **retention** test, proving head ⊇ base. A strict superset additionally requires head to contain at least one element base does not (`comm -13` non-empty, or `|head| > |base|`), and nothing in the check line asserts that half. An implementation that adds zero new `dispatch_case` lines to the fixture suite — leaving the close-out gate entirely untested by any dedicated fixture — passes this clause as written, which directly contradicts the clause's own "gains at least one" and "strict superset" language. This is vacuous relative to the criterion's own stated claim, not a guard-mutation-coverage nicety.
  - Fix: add `comm -13 "$T/dcb" "$T/dch" > "$T/gained"; test "$(grep -c . "$T/gained" || true)" -gt 0 || rc=1` (or an equivalent head-count-greater-than-base-count assertion) alongside the existing retention check.

- **Input space item 9 and the Goal's own phrase "the record's own task id" name a reachable input class — a record whose internally-declared task id disagrees with the `--task` CLI argument — that no acceptance criterion defines the resolution rule for, and no fixture exercises.** Every fixture that touches a task-id relationship (`AC7`'s `raw-capture-stem-mismatch` case, `AC2`'s composition control) compares a `raw-capture` stem or a resolved path against the **`--task` CLI argument** alone; none reads or compares against a record's own internal `- Task:` line. `AC3` additionally invokes the checker with `--record` and **no** `--task` at all, and its fixture's stem (`T-000-codex-primary`) happens to agree with the record's own filename (`T-000.md`) — so nothing in the twenty-one criteria ever forces a decision between "the record's own task id" meaning the `--task` argument, the `--record` path's filename, an internal `- Task:` line, or some required agreement among them. This is a genuine gap in the mechanism's own definitions (Input space explicitly names the class as reachable), not a hunt for a contrived edit against an existing rule — there is no rule yet to defeat.
  - Fix: state explicitly what "the record's own task id" means when `--task` is omitted (filename-derived, presumably), whether an internal `- Task:` line is read or ignored, and what happens when a `--task` argument and a record's own internal declaration disagree (silent CLI-argument precedence, or a new refusal token) — then add a fixture for whichever rule is chosen.

#### Minor / nits

- **`AC10`'s invocation pattern (`bash "$<CHK>"`) does not require the invocation to carry `--task "$TASK"`, though `DP-3` and the Goal's own "locates the record itself... when it is given a task id" language depend on the caller supplying one.** A wiring that satisfies every literal in the identity chain (assignment, sibling screen, `bash "$CHK"` with no arguments, status capture, matching `case`) would pass `AC10` while making every close-out invocation fail on `check-review-input.sh`'s own usage refusal. This is exactly the "a sufficiently contrived edit the guard could still defeat" shape the operator's calibration prices as Minor rather than Major — the identity-chain fix `AC10` already carries (round 2's own repair) is intact and un-defeated; this is a further, narrower mutation shape the same window does not yet close.
  - Fix: additionally require a line matching `bash "\$<CHK>" --task "\$TASK"` (or the equivalent argument the implementation settles on) within the window, tying the invocation to the task id close-out already holds.
- **`DP-5` freezes the pass id as "unique within the record," but no acceptance criterion names a dedicated refusal token for a duplicated pass id, and no fixture writes two complete blocks sharing one id.** In practice this is very likely already covered as a side effect of `AC5`'s per-`(field, id)` duplicate-detection, if the implementation keys duplication on the field-name-plus-id pair rather than on byte-identical line content — but that is an inference about the likely implementation shape, not a tested property, and `DP-5`'s own "unique" promise has no acceptance criterion citing it by name. Priced as Minor because the likely-correct implementation shape already closes the gap incidentally; this is closer to a documentation/traceability gap between `DP-5` and the correspondence table than to an untested functional hole.
  - Fix: either add a one-line fixture (two complete blocks, distinct field values, same pass id) and a named token, or add a correspondence-table row tying `DP-5`'s "unique within the record" clause to `AC5` explicitly so a reader is not left inferring the coverage.
- **`AC1`'s shellcheck-enumeration check requires the two new paths to occur on the single `run: shellcheck` line, but never requires the branch-point blob's own pre-existing paths on that line to survive.** A rewrite of the enumeration that kept only the two new paths (dropping every other script and suite this repository already lints) would pass `AC1` as written. This is the identical shape as the `AC10` superset finding above but for a different file, and — unlike that one — `AC1`'s own prose never claims a "superset" or "gains" property for this specific line, so it is priced as Minor rather than Major: the check under-verifies a coverage property nobody promised in prose, rather than contradicting a promise it makes.
  - Fix: assert the branch-point blob's own `run: shellcheck` line's path set is a subset of HEAD's, alongside the two new paths' presence.

#### Findings considered and not kept

- **Codex's own "AC12 verifies phrase uniqueness but not that the phrase remains on the measured bullet line" (Minor in Codex's primary pass) is not a real gap and is dropped rather than downgraded.** `AC12`'s check requires the anchor phrase `Verdict scope and severity calibration` to occur **exactly once** in the whole file (`grep -Fc … -eq 1`) before extracting the region with `awk`'s `index($0,A)`, which begins extraction at *wherever that single occurrence sits*. Because uniqueness is a precondition and the extraction is driven by locating that same unique occurrence, "the phrase's location" and "the phrase's content" are the same fact under this check, not two facts that could drift apart — there is no line-number literal anywhere in the check for a future edit to leave stale. Verified directly against the file: line 98 carries the phrase, begins with exactly three leading spaces (confirmed by direct byte inspection, not relayed), and the phrase's file-wide count is exactly one.

#### Domain-premise questions (only re-answered where round 3 changes them)

All four are unchanged from round 2's answers; round 3 introduced no new information bearing on any of them. The verbatim-invocation PII residual, the `:212` amendment's adopter-side blast radius, the four-field grammar's coverage of issue #335's three gaps, and duty C's arm-B disposition are all as round 2 left them.

**Is the authority attribution (pre-commitment vs. operator ruling) stated honestly, with no blurring?** Yes, independently re-verified against both cited locations. `## Pre-commitment`'s `authority:` line reads "AI self-discipline — self-imposed by this loop at spec time, **never operator-ratified**," unchanged by the `trigger-fired (2026-08-29)` line appended beneath it, which itself states "the authority the remaining rounds proceed under is **that operator ruling, not this pre-commitment** — this pre-commitment's own authority line is AI self-discipline, never operator-ratified; it neither authorized round 3 nor priced the review depth." `## Reviewer scope (two-tier)`'s own operator-ruling paragraph independently states "this is an **operator ruling** dated 2026-08-29 — not AI self-discipline, and not this task's own pre-commitment, whose authority line remains self-imposed." The two authorities are named distinctly in both places they are stated, with no sentence attributing round 3's authorization or the Tier-2 pricing to the pre-commitment.

#### Synthesis audit ledger

| # | finding | primary (Codex pass) | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `AC5`'s prose states "Nine cases" and later "the six cases" for the same case set | major | major | kept; independently traced to round 1's own "case count restated from six to nine" note, confirming a real leftover from an incomplete edit rather than a stylistic ambiguity |
| 2 | `AC10`'s `tests/close-out/run.sh` clause claims a strict superset but the check only proves retention (no `comm -13`/gained-count assertion) | major | major | kept; independently re-read the check line and confirmed no assertion anywhere requires a new `dispatch_case` line to exist |
| 3 | `AC10`/`DP-3`: the invocation pattern accepts a no-argument `bash "$<CHK>"`, so a wiring that never passes `--task` would still pass | major | minor | downgraded — this is a contrived-edit-defeats-the-guard shape on a check the operator's Tier-2 calibration prices as guard-completeness, not a broken/vacuous/contradictory check in itself; the identity-chain fix `AC10` already carries is intact and this is a narrower, further mutation shape |
| 4 | `DP-5`'s "unique within the record" pass-id promise has no dedicated acceptance criterion or fixture | major | minor | downgraded — very likely already closed as a side effect of `AC5`'s per-`(field, id)` duplicate detection if implemented straightforwardly; treated as a documentation/traceability gap between `DP-5` and the correspondence table rather than an untested functional hole, and this class of gap is what the operator's ruling prices down |
| 5 | Input space item 9 / Goal's "the record's own task id" names a reachable mismatch class with no defined resolution rule and no fixture | major | major | kept — independently verified no fixture in `AC2`, `AC3` or `AC7` ever reads or compares an internal `- Task:` line against the `--task` argument, and no criterion states which source wins; this is a definitional gap in the mechanism itself (Input space explicitly commits to the class as reachable), not a hunt for a contrived edit against an existing, stated rule |
| 6 | `AC1`'s shellcheck-enumeration check doesn't require the branch-point's pre-existing paths on that line to survive | minor | minor | kept; a real but narrower coverage gap than `AC10`'s superset finding, since `AC1`'s own prose never claims a superset property for this specific line |
| 7 | `AC12` verifies phrase uniqueness but not that the phrase remains on the measured bullet line | minor | rejected (no defect) | the exactly-once uniqueness precondition and the awk extraction's own `index($0,A)` location-finding are the same mechanism — there is no separate "measured line" for a future edit to drift away from, since the check locates wherever the (necessarily singular) occurrence is, rather than checking a hardcoded line number |

#### Recommendation

- REQUEST_CHANGES (round 3) → route back to the spec's author for a meaning-preserving correction to `AC5`, `AC10` and the Input-space-9/Goal task-id definition (never by weakening or removing a criterion a finding is about). This task's board entry reads `- entry-mode: pm-authored`, cross-checked against the Routing Map's own `- dispatch: specify — pm-authored` line printed at Plan — the two agree, so the route-back target is **pm-spec**, unchanged from rounds 1 and 2. The Minor items are recommended for inclusion in the same correction, not deferred past it, matching rounds 1 and 2's own convention.
- **On the operator's 2026-08-29 severity-calibration ruling**: applied as stated. Two of Codex's five primary findings were priced down from Major to Minor under it (the `AC10`/`--task`-argument gap and the `DP-5` duplicate-pass-id gap), both because they are guard-mutation-completeness hunts against checks whose own load-bearing property (the round-2 identity-chain fix; the per-field duplicate-detection mechanism) is intact and un-defeated. The three findings kept at Major (the `AC5` self-contradiction, the `AC10` superset vacuity, and the Input-space-9 definitional gap) are each either a check line's own prose disagreeing with itself, a check line proving less than its own explicit "strict superset"/"gains" claim, or an undefined term in the Goal/Input-space text itself — none is a hunt for a contrived edit against a rule that already exists and already holds.
- No same-class-2 concern this round: none of the three kept Majors are instances of the `unanchored-co-occurrence-check` class rounds 1–2 tracked (all four of that class's sites — `AC10`, `AC11`, `AC12`, `AC14` — are independently confirmed closed above); these are three unrelated defect shapes (a self-contradiction, an incomplete superset assertion, an undefined term), so this task's pre-commitment trigger, which fires on a *repeated* class, does not apply here.
- Provenance note (environmentally-unverified issue-canon reliance): unchanged from rounds 1–2 — this verdict's reading is internally consistent with every source this role opened first-hand this round (`.shell-team/specs/T-1104-review-input-fidelity.md` in full, `bin/close-out.sh`, `agents/codex-reviewer.md`, `skills/run/SKILL.md`, `docs/adopting.md`/`.ja.md`, and `.shell-team/reviews/T-1104.md`'s own rounds 1–2); issue **#335** itself was not independently read by this role (no GitHub-reading tool in this sandbox) — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Fast-follow disposition

- No fast-follow deferrals this round: all three Majors and two Minors above are recommended for inclusion in the same correction the route-back already requires, not deferred past it. Rounds 1–2's own fast-follow dispositions (the raw-capture stem's pass-name vocabulary, `AC20`'s forgeability) are unchanged by this round and are not restated here.

Raw Codex output saved at `.shell-team/reviews/T-1104-codex-specreview-r3.{txt,jsonl}` (the `-r3` suffix, per the convention this spec itself formalizes for a task's third round of a given pass family).
