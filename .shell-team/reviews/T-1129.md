### Codex Review verdict: APPROVE
- Task: T-1129
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.149.1
- Verification mode: executed-tests
- Diff scope: 8 files, +290/-2 lines (`e00f560...HEAD`; `git diff --stat`) — `.shell-team/interventions/T-1129.md`, `.shell-team/provenance/T-1129.md`, `.shell-team/specs/T-1129-team-init-ignored-base-warning.md`, `.shell-team/todo.md`, `bin/team-init.sh`, `docs/adopting.ja.md`, `docs/adopting.md`, `tests/team-init/run.sh`

Independent execution before reading either Codex pass's findings: `bash tests/team-init/run.sh` (exit 0, 34 `PASS`, 0 `FAIL`, closing line present — matches QA's own re-run exactly), `bash bin/check-acs.sh .shell-team/specs/T-1129-team-init-ignored-base-warning.md` (5/5 PASS), `bash bin/check-intent.sh …` (aligned, hash `2d1f22678625a103b1fd70d6ebd7ed465c747f26`), `bash bin/check-provenance.sh …` (conformant, 3 entries), `bash bin/check-interventions.sh --task T-1129 -- …` (conformant, 2 entries), `bash bin/check-handoff.sh .shell-team/todo.md` (exit 0), `shellcheck` 0.11.0 on both edited executing surfaces (both exit 0), a repo-wide grep of the diff's added lines for all five T-1046 tokens (none found), and a byte-diff of `tests/team-init/run.sh:581-601` against predecessor `3d0a948` (`IDENTICAL`). All match the engineer's and QA's hand-off claims exactly.

**Operational note (transparency, not a finding on the deliverable):** the primary pass's first invocation completed (`codex` exit 0) with two findings, but its raw capture files were mistakenly `rm`'d before publish instead of published — an error in this round's own execution of the codex-capture skeleton, not a codex/publish failure. The primary pass was re-run cleanly; its published raw capture (`T-1129-codex-primary.{txt,jsonl}`) carries one of the two findings the deleted run had also surfaced (the two-pass run is independently reproducible from the transcript already reviewed at the time, and is folded into synthesis below for completeness rather than discarded).

- raw-capture: `T-1129-codex-primary.jsonl` / `T-1129-codex-primary.txt`, `T-1129-codex-adversarial.jsonl` / `T-1129-codex-adversarial.txt`
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base e00f560 --json -o "<RAW_OUT>" > "<RAW_JSONL>" 2>&1`
- pass-role (primary): generation
- briefing-fidelity (primary): not-carried — the `review` subcommand takes no custom prompt; Codex explored the working tree and the named base ref with its own tools (`git status --short`, `git diff --stat`/`--name-status` against `e00f560`, targeted reads of `bin/team-init.sh`, `tests/team-init/run.sh`, `docs/adopting.md`, `.shell-team/AGENTS.md`, `CONTRIBUTING.md`) rather than receiving this round's canon, Focus list or spec text injected; this is the subcommand's standard shape, not a defect.
- raw-capture (primary): T-1129-codex-primary
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Play devil's advocate against this delivered change (branch feature/452-team-init-ignored-base-warning vs base e00f560). Read the files yourself with git diff e00f560..HEAD -- bin/team-init.sh tests/team-init/run.sh docs/adopting.md docs/adopting.ja.md, and read the full files at their current HEAD state -- do not expect any diff or file content pasted in this prompt. Two candidate defects were raised by an earlier review pass and need your independent adversarial judgment, plus you should look for anything else that could break this: (1) bin/team-init.sh's new printf line (around line 390) tells the adopter, if the resolved base dir named by \$TEAM_RUN_BASE does not appear in git status, to see the 'Where the operating files live' section of docs/adopting.md for the one-line re-include -- but is the concrete example given in that section (\`!.shell-team/\`) actually correct/actionable for an adopter using the legacy tasks/ layout or a custom TEAM_RUN_BASE override, where the resolved base is not literally .shell-team? (2) the new 'T-1129 pointer-line' fixture arm appended near the end of tests/team-init/run.sh captures team-init's output with 'init \"\$PL\" > \"\$PL/out\" 2>&1' (stdout and stderr merged) and then greps the merged file for the anchor phrase and for the literal 'docs/adopting.md' -- does merging streams, and grepping the whole file rather than just the matched anchor line, materially weaken what the fixture's own PASS message claims to prove, given AC1's existing check in the frozen spec at .shell-team/specs/T-1129-team-init-ignored-base-warning.md separately captures stdout/stderr and greps only the matched line? For each of the two candidates, and anything else you find, return a JSON array of {severity, file, line, issue, suggestion}, and be explicit about whether each candidate is a real, verdict-relevant defect in the *delivered diff* or a pre-existing / out-of-scope condition." > "<RAW_JSONL>" 2>&1`
- pass-role (adversarial): confirmation
- briefing-fidelity (adversarial): carried — the free-form prompt directly conveyed the diff scope, the base ref, the two candidate findings' exact file/line targets and reasoning, an explicit request to classify each as delivered-diff-relevant versus pre-existing/out-of-scope, and the JSON output shape; Codex's returned findings address both named candidates plus one additional item, all grounded in the two named files at their current HEAD state.
- raw-capture (adversarial): T-1129-codex-adversarial

**Issue-canon disclosure**: this record's reading of GitHub issue #452's 2026-09-07 re-scope comment relies entirely on the relayed text given in this round's briefing (the same text the spec, provenance and board already quote); this role has no GitHub access in this sandbox and could not open the comment's own bytes. internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Blockers
- none.

#### Major
- none. (See Synthesis audit ledger — two Codex-native P2 findings normalize to `major` per the severity table but are downgraded to `minor` on independent review; reasons below.)

#### Minor / nits
- `docs/adopting.md:47-48` (mirror `docs/adopting.ja.md:49`) — the new pointer sentence at `docs/adopting.md:50-53`/`docs/adopting.ja.md:52-55` (this task's own addition) directs every adopter to "Where the operating files live" for "the one-line re-include," but that section's own concrete example is hardcoded to `!.shell-team/`. For a reachable input class the frozen spec's own `## Input space` names as one the implementation "must handle correctly" (item 4: the legacy `tasks/` layout and a custom `TEAM_RUN_BASE` override), following that concrete example literally does not re-include the resolved base (`tasks/`, or the custom name) — a legacy/custom-base adopter who acts on the sentence's example would still see their base dir hidden. A correctly generalized form of the same instruction already exists elsewhere in the same document (`docs/adopting.md:407`: "`!.shell-team/` in the default layout, `!tasks/` in the legacy layout"), so the information exists in the file, just not duplicated into the section this task's new sentence points at.
- `tests/team-init/run.sh:716-719` (new `T-1129 pointer-line` arm) — `init "$PL" > "$PL/out" 2>&1` merges stdout and stderr, and the second assertion (`grep -qF -- 'docs/adopting.md' "$PL/out"`) searches the whole merged file rather than the matched anchor line. The arm's own `pass` message claims the pointer line itself "prints exactly one static pointer line naming docs/adopting.md," but as written the fixture would not catch a regression that moved the new line to stderr, or that dropped `docs/adopting.md` from the anchor line specifically — the pre-existing `Next: run … See docs/adopting.md.` line in the same output would still satisfy the grep. This does not weaken the shipped guarantee: **AC1** (never-dropped, `bin/check-acs.sh`-enforced) independently and more strictly checks exactly this property with separated stdout/stderr capture and an anchor-line-scoped extraction, and I re-ran it directly above with a clean PASS.
- `tests/team-init/run.sh:713` (adversarial-pass-only finding) — the new fixture arm exercises only the default `.shell-team` base; it does not add coverage asserting the pointer line names the correctly-resolved base under the legacy layout or a custom `TEAM_RUN_BASE` (the suite's existing legacy/override-layout fixtures discard `team-init`'s stdout). The spec's own `verification-ceiling: unit-and-static` and the engineer's own "notes for QA" already disclose these other reachable classes as not separately fixture-tested this round, so this is a coverage-completeness observation rather than an unnoticed gap.

#### Adversarial findings
- Independently re-confirmed the docs-pointer mismatch (finding 1) as real and delivered-diff-relevant, citing the same generalized instruction already present at `docs/adopting.md:407` as the fix template.
- Independently re-classified the fixture's stream-merging/whole-file-grep looseness (finding 2) as "a real fixture defect, but not verdict-relevant to the overall functional gate," citing **AC1**'s separated-stream, matched-line check as the actual guarantee.
- Surfaced one additional item beyond the two named candidates: the new fixture's default-base-only coverage (folded in above as the third Minor).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `docs/adopting.md:47-48`/`.ja.md:49` — pointer sentence directs adopters to a section whose concrete re-include example is hardcoded to `!.shell-team/`, not layout-generic | primary: P2 (→ major, both the deleted and the published run) / adversarial: Major | minor | the flagged prose predates this diff and was not edited by it; the frozen **AC4** check requires only the anchor phrase + literal `team-init` inside the section (both present) and says nothing about generalizing the pre-existing example; the new `printf` line itself correctly interpolates `$TEAM_RUN_BASE` for every layout per Input-space item 4; a correct generalized form of the same instruction already exists elsewhere in the same document (`:407`); cheap to close as a fast-follow without touching any frozen criterion |
| 2 | `tests/team-init/run.sh:716-719` — new fixture arm merges stdout/stderr and greps the whole file rather than the matched line, weaker than its own `pass` message's claim | primary: P2 (→ major) / adversarial: Minor ("not verdict-relevant to the overall functional gate") | minor | **AC1** (never-dropped, mechanically enforced, independently re-run clean above) already and more strictly enforces the exact same property with separated streams and an anchor-line-scoped grep, so the actual shipped guarantee is unaffected by this arm's looseness; adversarial pass concurs |
| 3 | `tests/team-init/run.sh:713` — new fixture arm covers only the default base, not the legacy/override layouts | adversarial: Minor | minor | no change — carried through at Codex's own native severity; a disclosed, not hidden, coverage gap per the spec's own stated `verification-ceiling` and the engineer's hand-off notes |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE

#### Fast-follow disposition
- Ledger #1 (`docs/adopting.md:47-48`/`.ja.md:49` non-generalized re-include example) — file-an-issue: duplicate or link the already-correct generalized phrasing from `docs/adopting.md:407` into the "Where the operating files live" section itself (and its Japanese mirror), so the section this task's own new line points at gives actionable guidance for the legacy and custom-base layouts too. Narrow-audience (legacy-layout / custom-`TEAM_RUN_BASE` adopters only), cheap, and orthogonal to every frozen criterion in this task, whose scope was deliberately kept minimal after two prior defeated designs on this same topic (T-1042, T-1046).
- Ledger #2 (`tests/team-init/run.sh:716-719` fixture stream-merging/whole-file-grep) — file-an-issue: capture stdout/stderr separately in the `T-1129 pointer-line` arm and scope the `docs/adopting.md` assertion to the extracted anchor line, matching AC1's own rigor, so the arm's `pass` message and its actual assertions agree. Not urgent — the property it under-tests is already fully covered by AC1.
- Ledger #3 (`tests/team-init/run.sh:713` default-base-only fixture coverage) — file-an-issue: extend the new arm (or add sibling arms) asserting the pointer line names the correctly-resolved base under the legacy `tasks/` layout and a `TEAM_RUN_BASE` override, using the suite's existing legacy/override fixtures as the base to extend.
