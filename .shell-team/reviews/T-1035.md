# QA review — T-1035

## Verdict: PASS — READY_FOR_QA → READY_FOR_REVIEW

## check-acs.sh — T-1035's own spec
- Measured before writing this record: 13 passed / 0 failed / 1 skipped (AC1–AC13 PASS, AC14 SKIP by design).
- Measured again after writing this record (this file now exists at `.shell-team/reviews/T-1035.md`): 13 passed / 0 failed / 1 skipped — unchanged, since AC11's allow-list treats this file as optional/permitted rather than required, and AC5/AC9/AC10 do not enumerate the reviews dir.

## check-acs.sh — T-1024's re-frozen spec (`CHECK_ACS_TIMEOUT=600`)
- 8 passed / 2 failed / 2 skipped: AC1,AC3,AC4,AC5,AC6,AC8,AC9 PASS; AC7 and AC10 FAIL; AC11,AC12 SKIP.
- Reds are exactly the disclosed merge-point-scoped pair, both verified by hand:
  - **AC7** (byte-level frozen-region invariant against `1e31600`): red because roughly a dozen spec files (T-1025..T-1035) were added to `.shell-team/specs/` since `1e31600`, so `git diff --name-only 1e31600 -- .shell-team/specs` names more than T-1024's own spec.
  - **AC10** (scope lock against `1e31600`): red because six merged tasks and a release sit between `1e31600` and this branch, so the changed-file set is far wider than the four-file allow-list that criterion checks against.
  - **AC8** (board pure-insertion against `1e31600`) came back **green**, not red as the spec's own pre-implementation prediction expected (predicted `FAIL its AC2, AC7, AC8, AC10`). This is a deviation from the pre-implementation prediction but not a violation of T-1035's own AC7, which requires the FAIL set to be a subset of {AC7, AC8, AC10}, not exactly that set. Confirmed the reason: T-1027's v1 hash line (the train's only board deletion before this task) postdates `1e31600`, so the deletion column against that ref is 0 and the board is a pure insertion from AC8's point of view.

## check-intent.sh alignment
- T-1035 v1 → `aligned: T-1035 v1 (8b0d4f42f30fd086f2ae579e76aa3b9e9814c16d)` — matches the hash given in-task.
- T-1024 v2 → `aligned: T-1024 v2 (4b8ab5ee96fabde20a1b37f481b98fe22b62b2a3)` — matches.
- T-1033 v1 → `aligned: T-1033 v1 (f2d2f4240b7287dad88c44170a559d581a8ceab7)` — matches.
- T-1027 v2 → `aligned: T-1027 v2 (8f4a08deee3d91132e396637ab586cce3df933c7)` — matches.
- All four aligned, exit 0.

## Independent class-B re-derivation
- Materialised `git show 03e6250:.shell-team/specs/T-1024-check-line-mktemp-guard.md`, ran `check-refreeze-class.sh -- <base blob> <current spec>`.
- Result: `class-b: old-spec and new-spec intent blocks have different line counts for T-1024 (old=91 new=92)`, **exit status exactly 1**. Matches AC5's requirement.

## Independent audit re-derivation
- Ran the frozen `P` command myself (extracted with an exact-line-match `awk` — a naive substring-regex `awk` extraction over-matches the literal `<!-- frozen-begin: P -->`/`<!-- frozen-end: P -->` strings that also appear, quoted, inside AC6's own `- check:` line; this is worth flagging as a trap for anyone re-deriving by hand).
- My own run: **280 lines / 36 distinct files** — matches the document's declared `- population: 280 — files: 36 —` line exactly.
- Class sums: `class-a-guarded: 97`, `class-b-unguarded-path-composed: 168`, `class-c-no-path-composed: 15`, `class-d-other: 0`; 97+168+15+0 = 280, matching population. Matches the `- reconcile: population 280 = 97 + 168 + 15 + 0` line.
- Per-file `sites:` sum: 280 across 36 `- file:` rows — matches file total.
- Spot-checked 5 of the 10 new rows (T-1025, T-1027, T-1031, T-1032, T-1034) by cutting my own raw hit list per file and comparing counts to the declared `sites:` values: 1/9/5/6/9 respectively — all match.
- Confirmed all 26 pre-existing rows are byte-unchanged versus the base blob at `03e6250` (`comm -23` between base-row-set and current-row-set is empty).
- Exactly one `- population:`, one `- measured-at: 03e6250 …`, one `- stale-at: …` line; both `Authoring ref` (`1e31600`) and `Re-derivation ref` (`03e6250`) labels present; neither `against the working tree` nor the superseded `(214, 26, 36, 168, 10, 0,` enumeration survive anywhere in the document.
- Re-derived the multi-invocation tie-break finding independently: `T-1031-check-handoff-flag-anchor.md:347` has 2 raw `mktemp` occurrences (both real invocations); `T-1032-audit-prose-accuracy.md:388` has 3 raw occurrences but only 2 are in invocation position (the third names the audit document's own filename inside a `printf`'d allow-list, a mention) — matches the document's own disclosed raw-vs-invocation discrepancy and the provenance record's stated reasoning.

## check-prompt-sync.sh / pm-spec.md diff
- `check-prompt-sync.sh`: green.
- `git diff 03e6250 -- agents/pm-spec.md`: exactly two added lines (D1, D2 bullets, byte-identical to the spec's frozen blocks), zero deleted. Both marker regions (`playbook-pm-spec`, `language`) untouched by the diff.

## Board / records
- `check-provenance.sh .shell-team/provenance/T-1035.md`: conformant, 4 decision entries.
- `check-interventions.sh --task T-1035 -- .shell-team/interventions/T-1035.md`: conformant, 1 entry.
- `check-handoff.sh`, `check-board-headings.sh --base 03e6250`, `check-pii-shapes.sh --base 03e6250`: all clean/exit 0.
- Full CI-wired step list (`.github/workflows/check-handoff.yml`, in workflow order) run locally, including `shellcheck` (local 0.11.0 matches the CI pin) over the full pinned file list, both board dogfood steps, `check-prompt-sync`/`check-playbook`/`gen-playbook-blocks` dogfood steps (the directory/glob-scanning steps this task's own Notes for engineer flags as needing to actually run), and every `tests/*/run.sh` suite in file order — all green, 0 failures. Ran the multi-line for-loop steps as standalone script files per the engineer's own noted trap (an inline multi-line for-loop mis-split once during my own ad hoc testing of the `gen-playbook-blocks` dogfood step, matching the exact artifact the engineer's hand-off independently reports hitting and re-running around).

## Mutation reproduction (scratch `git worktree`, real checkout untouched throughout — confirmed `git status --short` clean before/after both worktrees)
- Mutation A: bumped the T-1025 row's `sites: 1` to `sites: 2` in a scratch worktree → T-1024's own `AC2` went **red**, as predicted.
- Mutation B: deleted the audit document's `- stale-at:` line in a scratch worktree → T-1024's own `AC2` went **red** (its `nsa` clause), as predicted.
- Mutation C (targeted at the AC8 blind-spot judgment below): deleted the T-1034 `- file:` row in a scratch worktree → T-1024's own `AC2` went red (`nf` mismatch, 35≠36) → cascaded into T-1035's own `AC7` red — but **T-1035's own AC8 stayed green**, confirmed by direct measurement.

## AC8 blind-spot judgment (T-1035's own AC14(b) mutation #5 / this task's item 9)
T-1035's own `AC8` check derives the "gained rows" set as `comm -13 <base file list> <current file list>` and validates grammar/site-membership/apply-count only for whatever is actually present in that difference — it never independently asserts that the gained-row *count* equals `(re-derived file total) − (base file total)`. Deleting a gained row simply removes it from both the current-file list and therefore from the computed "new" set; the loop has nothing left to iterate over for that file and reports no problem. I reproduced this exactly: after deleting the T-1034 row, T-1035's own `check-acs.sh` run reports `AC8: PASS` while `AC7: FAIL` (because T-1024's own AC2's file-total identity — `nf` vs the live re-derived file total — does independently catch it, and T-1035's AC7 requires T-1024's AC1–AC6 to all be PASS).

This is not a discovery on my part — it is disclosed, in the same words, in the engineer's own hand-off (board item 19, mutation (5)): *"disclosed finding, not fixed (frozen text, P3 routes it to pm-spec): this spec's own AC8 stayed green on this mutation ... a deleted row is caught only via the AC2→AC7 cascade, not by AC8's own clause."* My independent reproduction on a fresh scratch worktree confirms the engineer's own measurement exactly.

Separately, worth flagging for whoever routes this to pm-spec: T-1035's own frozen `AC14(b)` prose (inside its intent block) *predicts* "delete one gained `- file:` row (**AC7 and AC8 both red**)" — that prediction is itself inaccurate for AC8, per the measurement above. Since `AC14` sits inside the frozen intent block, a defect in its prose is out of contract for engineer rework per P3 and routes to pm-spec as a re-freeze proposal; the engineer correctly declined to touch it and disclosed the discrepancy instead of silently "fixing" the prediction text.

**Judgment: acceptable documented redundancy boundary, not a FAIL driver.** The property this task's Non-goals/DP-B actually require — that the deletion of a gained row is *caught* somewhere in the machine-checkable surface — holds, via the AC2→AC7 cascade, empirically confirmed. Building an AC8 that independently asserts a gained-row *count* would require extending a frozen check inside T-1035's own intent block (verification-mechanism work DP-B explicitly declines for this task) or a future re-freeze; recording it as an issue candidate for the next planning input (as the board's `issue-filing disposition` line already does for the sibling `- stale-at:` grammar validator) is the right disposition, not a rework kickback now.

## Claim-fidelity — `## Summarized sources` vs. primary bytes
Per-source check (each opened and matched clause by clause against the spec's stated distinction, not against the spec's own paraphrase):

- `.shell-team/specs/T-1024-check-line-mktemp-guard.md` — opened in full. AC2's body carries only identity/anti-vacuity checks and no population literal (confirmed, no `- population:`-shaped literal anywhere in T-1024's own text); AC1's check line contains `grep -qF -- '1e31600' "$A" || rc=1` (the literal-presence assertion, confirmed byte for byte); AC4's body requires exactly one `apply` row (T-1024's own) and zero closed+apply rows (confirmed in its check line); AC7's check body `continue`s past exactly `.shell-team/specs/T-1024-check-line-mktemp-guard.md` and no other file (confirmed); DP8 states nine fields, em-dash separated, reason "about that file's sites" (confirmed at line 44); intent block carries exactly 10 `- check:` lines across 12 column-0 AC bullets (confirmed by direct `awk`/`grep` count). **Match: exact.**
- `docs/loop-engineering/check-line-mktemp-guard-audit.md` — opened in full, both the current re-derived version and (via `git show 03e6250:…`) the pre-re-derivation base blob. Base blob confirmed to carry the single `- population: 214 — files: 26 — command: …` line, the unqualified `**Base ref**: 1e31600` header, the sentence "re-run live against the working tree by AC2" (line 20), and the closing enumeration `(214, 26, 36, 168, 10, 0, and each per-file count)` (line 98) — all exactly as T-1035 describes them existing pre-task. Current version confirmed to carry the split `Authoring ref`/`Re-derivation ref` labels, exactly one `- measured-at: 03e6250 …` and one `- stale-at: …` line, and neither of the two retired phrases surviving. T-1005's `- measurement:` line (`for n in 270 283 291 298; do git show 1e31600:… ; done`) confirmed present in the base blob exactly as described, reading a committed ref already, not the working tree. **Match: exact.**
- `.shell-team/todo.md`'s `## Done` entries for T-1024, T-1033, T-1027 — read directly (board is `.shell-team/todo.md`, confirmed via `team-paths.sh --get todo`). T-1024's `## Active` entry (this branch's in-flight entry, not yet in `## Done`) carries exactly one `- intent-hash (v1): …` (working tree; this task replaces it) and one `- freeze-attestation (v1, 2026-08-04): … verdict=3P/7F` line, both retained/replaced per DP-G as the spec claims. **Match.**
- `.shell-team/specs/T-1033-promote-retro-2026-08-05.md` — read directly (per this task's own AC9 check invoking `check-intent.sh` on it; separately confirmed `aligned` above). Not independently re-derived clause by clause against T-1035's specific claim about its AC6/AC13 shape in this pass beyond the alignment check, since T-1035's own claims about it are about *pattern precedent*, not about facts this task's machine-checkable ACs assert; the engineer's own hand-off (item 22) reports having opened it directly and confirmed the pattern — I did not find reason to doubt this and did not re-derive it further given it carries no assertable AC in T-1035 itself.
- `.shell-team/interventions/T-1035.md` — read directly (reproduced in full above). Its stated outcome ("resolve AC2's drift by updating the declared values, with a stale-at trigger... option D adopted... re-derived at the branch base ref via committed-blob reads") is consistent with what actually shipped: the audit document is re-derived at `03e6250` via `git show`, T-1024's AC2 is re-frozen to read that ref, and a `- stale-at:` trigger line was added — confirmed above. **Consistent.**
- `.shell-team/retros/2026-08-05.md` — not independently opened in this pass; the spec's claim about it (candidate 4 assigned to pm-spec, "disclosed only, not fixed", and its own 254/33 / 260/34 figures being of a different tree than this branch's base) is not load-bearing for any of this task's 13 machine-checkable ACs, and I did not find a reason during verification to doubt the engineer's own AC14(e) report of having opened it directly. Recording this as **not independently re-opened by QA**, distinct from a checked-and-matched source, per the mandatory per-source disclosure requirement.
- `.shell-team/lessons.md` — confirmed byte-identical to `03e6250` (AC13, measured, PASS); the specific 2026-08-05 entry's presence was not independently re-opened by QA in this pass (no AC in T-1035 asserts its *content*, only its file-level byte-identity, which is machine-verified).
- `agents/pm-spec.md` — opened, diffed against `03e6250` directly (see check-prompt-sync section above): base-ref section confirmed to carry exactly five `- **` bullets before the edit, matching the claim. **Match.**
- `templates/prompt-blocks/registry.txt` — read directly; confirmed `agents/pm-spec.md` is named as a consumer of exactly `marker language.md`, `marker playbook-pm-spec.md`, `contain flag-enum.md`, `contain board-line-format.md`, and no rule covering `## Spec completion self-check`. **Match.**
- `bin/check-acs.sh`, `bin/check-intent.sh`, `bin/check-refreeze-class.sh` — their behavior was confirmed operationally through the full CI-wired suite run and the mutation reproductions above rather than by re-reading every regex line; this matches the depth the engineer's own hand-off (item 22) discloses for the same three files, and no AC in this spec asserts a specific regex byte sequence for QA to check independently.
- Routing Map (R1–R9) and the sprint v1.6.0 planning GO — **relayed, not opened**, by design (this role has no channel to either either); restated here as such rather than silently dropped, matching the spec's own instruction and the engineer's parallel disclosure.

No flattened distinction found in any site checked against its primary bytes.

## Edge cases tried
- Perturbing a single new row's declared `sites:` count by one (T-1024 AC2 identity, red as predicted).
- Removing the sole `- stale-at:` trigger line from the audit document (T-1024 AC2, red as predicted).
- Deleting a gained `- file:` row entirely (the AC8 blind-spot probe above).
- Re-extracting the frozen `P` block with a naive substring-regex `awk` match instead of an exact-line-match `awk` match, which over-matched into unrelated check-line text quoting the same marker strings — not a defect in the shipped artifact (the spec's own checks correctly use exact-line matching throughout), but a real trap for anyone re-deriving by hand; noted above.
- Re-derived the multi-invocation tie-break finding from scratch over the whole 280-line population rather than trusting the document's own six-line enumeration; matched exactly, including the disclosed raw-count-vs-invocation-count nuance on `T-1032:388`.

## Risk notes for reviewer
- T-1024's own `AC8` measured **green** rather than the spec's predicted **red** at the freeze; this is disclosed and explained (T-1027's v1-hash deletion postdates `1e31600`, so the deletion column against that older ref is 0) and is not a violation of T-1035's own AC7 (subset requirement, not exact-set requirement) — flagging so it isn't mistaken for an unexplained deviation.
- T-1035's own frozen `AC14(b)` prediction text ("delete one gained `- file:` row (AC7 and AC8 both red)") is itself measurably inaccurate for the AC8 half; this sits inside the intent block, so the correction path is a re-freeze proposal to pm-spec (P3), not an engineer edit — already correctly disclosed rather than silently patched.
- `.shell-team/retros/2026-08-05.md` and the specific content of `.shell-team/lessons.md`'s 2026-08-05 entry were not independently re-opened by QA in this pass (see Claim-fidelity above); neither is load-bearing for any machine-checkable AC in this spec, but recording this explicitly rather than folding it into a blanket "sources checked" statement.
