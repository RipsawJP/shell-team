### QA round 1 — T-1085

**Verdict: PASS → READY_FOR_REVIEW.**

**Mechanical sweep (own run, live).** `export CHECK_ACS_TIMEOUT=300; bash bin/check-acs.sh .shell-team/specs/T-1085-default-path-firing.md 2>&1 | tail -1` → **`check-acs: 18 passed, 0 failed, 1 skipped, 0 unrecognized`** (AC1–AC18 all PASS, AC19 SKIP by design — matches hand-off). `bash bin/check-intent.sh .shell-team/specs/T-1085-default-path-firing.md .shell-team/todo.md` → `check-intent: aligned: T-1085 v2 (249b76b2a6aa19f4135807ff80bb2b7179859e67) matches ...`. `bash bin/check-interventions.sh --task T-1085 -- .shell-team/interventions/T-1085.md` → `conformant: .shell-team/interventions/T-1085.md (6 entries, 0 sentinel)`. `bash bin/check-provenance.sh .shell-team/provenance/T-1085.md` → `conformant: .shell-team/provenance/T-1085.md (6 decision entries, 0 sentinel)`. `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0. `B=$(git merge-base feature/1084-dispatcher HEAD); bash bin/check-pii-shapes.sh --base "$B"` → `check-pii-shapes: clean (no PII-shaped bytes found)`. All five of AC17's sub-checkers therefore independently confirmed green, not accepted from the hand-off unverified.

**Working-tree discipline.** `git status --short` → empty, before and after this round's probing (all probing done via read-only reads and `bash bin/derive-populations.sh` re-runs, whose output was diffed against the note's embedded blocks and discarded — no scratch file left in the real tree). `git rev-parse feature/1084-dispatcher` = `9c950d76290cb0d70f0a01a11bdfe3e901b67043`; `git rev-parse develop` = `9b9f79a2e4699f69397dbd839967217b22ca7a80` (≠ branch point); `git branch --show-current` = `feature/1085-default-path-firing` — all three match the hand-off's **AC19(e)** values exactly.

**Frozen-intent alignment (item 2).** `check-intent.sh` reports `aligned` at v2, confirmed above — not a `drift-detected`/`structural`/`usage` result. Read the intent-block v1→v2 delta myself: the `firing-protocol` region (lines 53–83) holds exactly 16 non-blank lines (`awk '/BEGIN firing-protocol/,/END firing-protocol/' ... | grep -c .` → `16`), consistent with the v2 threshold (`-ge 14`) and inconsistent with the superseded v1 threshold (`-ge 20`) — the class-B re-freeze the interventions record describes is real and the region itself is untouched (still 53/83).

**AC19 audit, item by item (mandatory — SKIP-by-design, no `- check:` line).** Enumerated all nine items (a)–(i) from the spec's own text before reading the hand-off.

| Item | Owner | Hand-off claim | QA disposition |
|---|---|---|---|
| (a) Mutation self-check, all 18 checks + AC6/AC7 direction probes | engineer | Every one of AC1–AC18 individually mutated/observed-red/restored/observed-green, on an out-of-tree scratch worktree; AC6/AC7 direction-B probe disclosed as vacuous for AC6 specifically (an honest, load-bearing finding, not glossed); one self-caught tooling defect (label-anchoring in the extraction helper) corrected before trusting results. | **Confirmed by shape**: all 18 ACs individually named with their own mutation, plus both required AC6/AC7 direction probes present. Not independently re-run (task brief scopes mutation-arm re-runs out of QA's own re-verification; the narrative's specificity — including a self-disclosed vacuity finding a fabricated report would have no reason to include — is corroborating, not conclusive). |
| (b) Execution-context matrix | engineer | Cell 1 (checkout root, no `PATH`) confirmed for all 18 checks + 3 reproduce commands; "no adopter-shaped cell exists" stated per-command with a stated reason (every target is this repo's own committed task artifact by literal path), not as one blanket sentence. | **Confirmed by shape and independently re-verified**: I ran the checks and the two derivation `- reproduce:` commands myself directly (`bash bin/...`, repo-relative, no bare-`PATH` reliance) and both reproduced byte-identically (below). The per-command "no adopter cell" reasoning is sound: none of the 3 reproduce commands or the 18 checks target anything outside this exact commit's own file set. |
| (c) Full CI-equivalence | engineer | 76 `- name:` steps, 2 infra-only, 74 run locally in workflow order, 74/74 PASS. | **Step count independently confirmed** (`grep -c '^      - name:' .github/workflows/check-handoff.yml` → `76`). Did not re-run all 74 steps myself (would duplicate a ~1-hour run); AC17's five sub-checkers (a strict subset of those 74) were independently re-run above and all green. |
| (d) Blast radius production | engineer | 90-unit full-population sweep at both refs, key reconciliation 1473/1492/0/19, 5 raw flips individually re-verified, 3 genuine new reds (`T-1084` AC14/AC15/AC16) attributed to stacking. | **Population derivation independently re-run and byte-identical** (below); did not re-run the full 90-unit base-vs-head sweep myself (this is "the arms" the task brief explicitly scopes out of QA re-verification — a ~55-minute serial pass per side). The provenance file's decision entries document the reuse-and-spot-check method in detail and five of the six spot-checked criteria were named with their own live command and result. |
| (e) Measured 40-hex values | orchestrator | `feature/1084-dispatcher`=`9c950d7...`, `develop`=`9b9f79a...`, evidence commit=`6937e19...`, `git show-ref` exits 0, `git branch --show-current`=`feature/1085-default-path-firing`. | **Independently re-run, all match** (rev-parses above; evidence-commit sha matches board's `- probe-evidence-sha (T-1085):` line, cross-checked via `check-intent`/`check-acs` **AC3**/**AC4** passing live, which resolve through that same sha). |
| (f) Per-source report, all 15 `## Summarized sources` entries | engineer | Every source opened first-hand this round, each cited distinction confirmed verbatim. | **Independently spot-checked against the actual source bytes** — see the dedicated Summarized-sources table below. No flattened distinction found. |
| (g) Firing's own execution conditions | orchestrator | Clock source `/bin/date +%s%N` (19-digit); 8 logical cores, `TEAM_FANOUT_MAX` unset; `subagent_type`=`shell-team:qa-verifier`, model=`sonnet`; 600000 ms per-call timeout; `CHECK_ACS_TIMEOUT=300` both arms; pinned commit `09cdf36d...`. | **Cheaply re-run items confirmed**: `getconf _NPROCESSORS_ONLN` → `8`; `$TEAM_FANOUT_MAX` unset in this session — both match. The clock source, per-call timeout and pinned-commit values are one-off session facts from the firing itself and cannot be independently re-measured after the fact; taken as reported, consistent with the launch record's own quoted `- cap-ground: measured-cores=8` line. |
| (h) Post-arm cleanliness and venue confirmations | orchestrator | `git status --short`/`git ls-files --others --exclude-standard` both empty at the seam; each arm its own fresh `git clone --no-hardlinks`, no `git worktree add` for any arm; runs directory default-resolved (`TEAM_RUNS_DIR` unset). | **Re-run now, still holds**: `git status --short` empty in the real checkout at this QA seam too (above). Venue/clone claims are one-off session facts from the firing itself, consistent with the note's own "Firing evidence" narrative and the launch record's quoted paths under `$TMPDIR`. |
| (i) Relayed premises re-measured | orchestrator | PR #325 `open`, CI `completed`/`success` at head_sha = branch point; predecessor resolves in `refs/heads/`; issue #277 `open`; sprint plan release-condition wording recorded, `v2.1.0`. | **Independently re-verified live via `api.github.com`** (network path available in this session, unlike some prior QA rounds): PR #325 `state: open`, `head_sha` = `9c950d76290cb0d70f0a01a11bdfe3e901b67043` (exact match to the branch point); its check-run at that sha reports `status: completed`, `conclusion: success`. Issue #277: `state: open`. `git show-ref --verify --quiet refs/heads/feature/1084-dispatcher` exits 0 at this seam too. All four match. |

**Empirical re-derivation of the note's own quoted arithmetic (item 4 — never accepted from the note's prose).**
- Overlap window, re-derived independently from the eight `- agent-timestamp:` lines with bash `10#` integer arithmetic (own script, not copy-pasted from the note's own command): `maxf=1787185721254292000` (qa-8), `minl=1787186599452998000` (qa-8), `margin=878198706000` (≈878.199 s), `spread=34494850000` (≈34.495 s) — **matches the note's `- overlap:` line exactly**; window is non-empty (`maxf < minl`).
- All eight `- heartbeat:` values independently checked against that same re-derived window — **all eight strictly inside** `(maxf, minl)`.
- Saving direction, re-derived independently: fanned duration = `completed-epoch − launched-epoch` = `1821152499000` ns; serial duration = `last − first` (serial-1) = `3351333776000` ns; `serial_dur > fanned_dur` ⇒ `faster` — **matches the note's `- saving-direction: faster` line**. Saving percentage independently computed: `(3351333776000−1821152499000)/3351333776000 ≈ 45.66%` — matches the note's `- saving:` line's own stated `≈45.66%` (and is correctly distinguished throughout from `phase-multiplexing.md`'s own 43.84% pilot ceiling, **AC14** confirming the attribution count on every line mentioning either figure).
- `t1085-verdict-region-parity` derivation block: re-ran the embedded `- reproduce:` command myself → byte-identical to the note's embedded block (`3227498628 16205`, single bucket `FAN+SERIAL`).
- `t1085-assign-vs-part` derivation block: re-ran the embedded `- reproduce:` command myself → byte-identical to the note's embedded block (all 8 `qa-*` ids in one bucket).
- **AC16**'s `t1085-spec-population` derivation block (spec's own Blast radius section): re-ran the embedded `- reproduce:` command myself → byte-identical (89 base / 90 head, the `+1` being this task's own spec file).

**Verdict-scope pricing (item 5), applied as the spec's own `## Verdict scope` directs.** This is an internal analysis artifact; the mandate is computed values and conclusion direction, and the presence-and-structure shape of most criteria is the correct pricing, not a defect. I read the one claim priced higher — the `opt-in; no phase is rewired to use it by default` reading and the existence-capability reading of `phase-multiplexing.md:364`'s second conjunct — against the actual source bytes (`skills/run/SKILL.md:161`, `phase-multiplexing.md:355,364`, quoted above in the Summarized-sources table) and find both readings textually grounded in the source's own words ("Using this step is always an explicit choice," "able to … exist," "can actually launch"). I do not find either reading wrong on the evidence read this round. Per the spec's own coupling and this corpus's quantitative-conclusion-settlement entry, **this PASS gates progression but does not settle either reading or the saving's direction** — the cross-provider review independently re-derives both from the same recorded evidence; flagging this explicitly for the reviewer rather than treating my own read as final.

**Known disclosed items — verified as records, not re-reported as findings.** All five checked present and consistent between the interventions file, the note's "Five disclosures" paragraph and the provenance file: (1) the qa-3 `T-1050` duplicate-claim dedup, pristine pre-correction part file preserved (interventions entry 5, note disclosure 3, provenance decision not needed — resolved before commit); (2) the two harness-backgrounded 600 s continuations (`T-1040` on qa-2, `T-1050` on qa-3 — note disclosure 2); (3) the `- launched-epoch:` audit-only-stamp latency (note disclosure 1, provenance decision entry 3, which is also why the genuine-overlap margin is stated against launch dispersion rather than `first − launched-epoch`); (4) the `grep -a` binary-misclassification extraction-defect class fix, one carrier swept across all eight part files (note disclosure 4); (5) T-1084's own AC14/AC15/AC16 merge-point-caused new reds — confirmed disclosed in this spec's own `## Blast radius` "Measured, correcting the prediction" subsection and attributed to T-1084's own frozen locks, not repaired here, consistent with this spec's Non-goals.

**Summarized sources — verified against the source itself, not the spec's paraphrase (mandatory item).**

| Source | Sites checked | Result |
|---|---|---|
| `skills/run/SKILL.md` | Lines 42, 70, 85, 161, 166, 207 read directly | All six match the spec's/note's quoted or paraphrased text verbatim (transcription seam, Validate-after-Implement ordering, step-5 mechanics pointer, `opt-in` parenthetical, 120 s timeout + bare-`&` death, precedence-rule consumer copy). |
| `templates/prompt-blocks/fanout-orchestration.md` | Byte-identity to branch point (`AC1`, live) | PASS — the spec's claim that this file is byte-identical to its branch-point blob is mechanically enforced and confirmed green this round. |
| `templates/prompt-blocks/dispatch-record.md` | Byte-identity to branch point (`AC1`, live); grammar cross-checked against the board's own two live `- dispatch:` records | PASS; grammar (`- dispatch: <axis> — <value> — <unconditional\|conditional> — <ground>`) matches both live board instances. |
| `docs/loop-engineering/phase-multiplexing.md` | Lines 355, 359, 364, 370, 371, 375 read directly; byte-identity to branch point (`AC1`, live) | All six lines match verbatim, including the exact "able to … exist" / "can actually launch" wording the Goal's existence-capability reading turns on. |
| `docs/loop-engineering/agent-launch-fanout.md` | Lines 186, 200, 217, 242, 247 read directly | All five match verbatim, including the `subagent_type` declared-label epistemic boundary and its stated counterfactual, and the `- umbrella: 277` line naming T-1085 as owed the live-firing half. |
| `bin/aggregate-verdicts.sh` (lines 1–100) | Part-file grammar (lines 19–21), `design-note-T-1012.md` sentinel citation (line 32), `verdict-region` byte-identity property (line 55 area) | All confirmed present verbatim. |
| `bin/check-fanout-instances.sh` | Usage/success-line grammar, mixed-scope `missing-instance` warning (lines 62, 82, 175–176) | Confirmed present verbatim. |
| `bin/derive-populations.sh` | `--set` identifier grammar, `lines: 0 — items: 0` semantics, block grammar | Confirmed present verbatim; also exercised live three times this round (population, assign-vs-part, verdict-region-parity), each byte-identical. |
| `bin/check-acs.sh` | `AC_RE`, `--root`, `${CHECK_ACS_TIMEOUT:-120}` fallback | Confirmed present verbatim. |
| `bin/team-paths.sh` | Ten-key `--get` set, `TEAM_RUNS_DIR` override | Confirmed present verbatim. |
| `.shell-team/.gitignore` line 4 | `runs/` | Confirmed exact byte match. |
| `bin/check-intent.sh` | `--print-hash`, `marker_count()` whole-line-equality + `\r$` strip | Confirmed present verbatim. |
| `agents/*.md` frontmatter (all nine) | Every `tools:` line read directly | Confirmed: zero `Agent`/`Task` tokens across all nine; `qa-verifier` carries exactly `Read, Grep, Glob, Bash` + `model: sonnet`. |
| `.shell-team/specs/T-1083-agent-launch-fanout.md` | `probe-protocol` marker existence, AC8's byte-lock shape | Confirmed present and structurally analogous to this spec's own AC4. |
| `.shell-team/specs/T-1084-dispatch-routing-record.md` | Lines 40–41 (declaration region), AC16 (lines ~106), Blast radius table | Confirmed present verbatim; T-1084's own board record (`- dispatch: verify — serial — unconditional — ...never fired on the default path...`) cross-checked directly against the live board and matches the spec's characterization of the divergence exactly. |
| `.shell-team/todo.md` (lines 1–14, this task's own entry) | Active-line format, this task's own two `- dispatch:` records | Confirmed present verbatim (checked live above under AC8). |
| `agents/pm-spec.md` | `## Spec completion self-check` heading | Confirmed present. |

No flattened distinction found at any checked site.

**Regressions.** No stray files or uncommitted state introduced by this round's probing — `git status --short` empty throughout and after. No suite touched by this task's zero-shipped-diff (`AC1`, confirmed PASS) can regress, since nothing under `skills/`, `templates/`, `agents/`, `bin/`, `tests/` or `.github/` changed.

**Edge cases tried.** (1) Re-derived the overlap window and saving-direction arithmetic independently from raw quoted values rather than trusting the note's own computed lines — both matched. (2) Cross-checked the board's live T-1084 divergence record against this spec's own characterization of it, rather than trusting the paraphrase — matched exactly. (3) Verified PR #325/issue #277 live states over the network (available this session) rather than accepting the freeze-time snapshot as still current — both still consistent with the recorded values. None of these surfaced a defect; none is `out-of-input-space` (all are within the spec's own declared reachable classes — real timestamps, a real board record, real tracker state).

**Risk notes for reviewer.** (1) Per Verdict scope, my PASS on the `opt-in`/existence-capability readings and on **AC13**'s saving-direction conclusion gates progression only — the cross-provider review is the settling read for both. (2) AC19(a)'s full 18-mutation self-check and AC19(c)'s full 74-step CI-equivalence run were audited by shape and partially spot-checked (per the task brief's own scoping of "the arms" out of QA re-verification), not fully independently re-executed end to end — flagging this depth distinction explicitly rather than folding it into a blanket "confirmed" claim. (3) The three genuine new reds against `T-1084`'s own AC14/AC15/AC16 are real, permanent (until T-1084's owners re-freeze), and correctly attributed to stacking rather than to this task's own diff — consistent with this task's own AC1/AC18 passing.
