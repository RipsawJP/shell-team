### QA round 1 — T-1085

**Verdict: PASS → READY_FOR_REVIEW.**

**Mechanical sweep (own run, live).** `export CHECK_ACS_TIMEOUT=300; bash bin/check-acs.sh .shell-team/specs/T-1085-default-path-firing.md 2>&1 | tail -1` → **`check-acs: 18 passed, 0 failed, 1 skipped, 0 unrecognized`** (AC1–AC18 all PASS, AC19 SKIP by design — matches hand-off). `bash bin/check-intent.sh .shell-team/specs/T-1085-default-path-firing.md .shell-team/todo.md` → `check-intent: aligned: T-1085 v2 (249b76b2a6aa19f4135807ff80bb2b7179859e67) matches ...`. `bash bin/check-interventions.sh --task T-1085 -- .shell-team/interventions/T-1085.md` → `conformant: .shell-team/interventions/T-1085.md (6 entries, 0 sentinel)`. `bash bin/check-provenance.sh .shell-team/provenance/T-1085.md` → `conformant: .shell-team/provenance/T-1085.md (6 decision entries, 0 sentinel)`. `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0. `B=$(git merge-base feature/1084-dispatcher HEAD); bash bin/check-pii-shapes.sh --base "$B"` → `check-pii-shapes: clean (no PII-shaped bytes found)`. All five of AC17's sub-checkers therefore independently confirmed green, not accepted from the hand-off unverified.

**Working-tree discipline.** `git status --short` → empty, before and after this round's probing (all probing done via read-only reads and `bash bin/derive-populations.sh` re-runs, whose output was diffed against the note's embedded blocks and discarded — no scratch file left in the real tree). `git rev-parse feature/1084-dispatcher` = `9c950d76290cb0d70f0a01a11bdfe3e901b67043`; `git rev-parse develop` = `9b9f79a2e4699f69397dbd839967217b22ca7a80` (≠ branch point); `git branch --show-current` = `feature/1085-default-path-firing` — all three match the hand-off's **AC19(e)** values exactly.

**Frozen-intent alignment (item 2).** `check-intent.sh` reports `aligned` at v2, confirmed above — not a `drift-detected`/`structural`/`usage` result. Read the intent-block v1→v2 delta myself: the `firing-protocol` region (lines 53–83) holds exactly 16 non-blank lines (`awk '/BEGIN firing-protocol/,/END firing-protocol/' ... | grep -c .` → `16`), consistent with the v2 threshold (`-ge 14`) and inconsistent with the superseded v1 threshold (`-ge 20`) — the class-B re-freeze the interventions record describes is real and the region itself is untouched (still 53/83).

**AC19 audit, item by item (mandatory — SKIP-by-design, no `- check:` line).** Enumerated all nine items (a)–(i) from the spec's own text before reading the hand-off.

| Item | Owner | Hand-off claim | QA disposition |
|---|---|---|---|
| (a) Mutation self-check, all 18 checks + AC6/AC7 direction probes | engineer | Every one of AC1–AC18 individually mutated/observed-red/restored/observed-green, on an out-of-tree scratch worktree; AC6/AC7 direction-B probe disclosed as vacuous for AC6 specifically (an honest, load-bearing finding, not glossed); one self-caught tooling defect (label-anchoring in the extraction helper) corrected before trusting results. | **Confirmed by shape**: all 18 ACs individually named with their own mutation, plus both required AC6/AC7 direction probes present. Not independently re-run (task brief scopes mutation-arm re-runs out of QA's own re-verification; the narrative's specificity — including a self-disclosed vacuity finding a fabricated report would have no reason to include — is corroborating, not conclusive). |
| (b) Execution-context matrix | engineer | Cell 1 (checkout root, no `PATH`) confirmed for all 18 checks + 3 reproduce commands; "no adopter-shaped cell exists" stated per-command with a stated reason (every target is this repo's own committed task artifact by literal path), not as one blanket sentence. | **Confirmed by shape and independently re-verified**: I ran the checks and the two derivation `- reproduce:` commands myself directly (`bash bin/...`, repo-relative, no bare-`PATH` reliance) and both reproduced byte-identically (below). The per-command "no adopter cell" reasoning is sound: none of the 3 reproduce commands or the 18 checks target anything outside this exact commit's own file set. |
| (c) Full CI-equivalence | engineer | 76 `- name:` steps, 2 infra-only, 74 run locally in workflow order, 74/74 PASS. | **Step count independently confirmed** (`grep -c '^      - name:' .github/workflows/check-handoff.yml` → `76`). Did not re-run all 74 steps myself (would duplicate a ~1-hour run); AC17's five sub-checkers (a strict subset of those 74) were independently re-run above and all green. |
| (d) Blast radius production | engineer | 90-unit full-population sweep at both refs, key reconciliation 1473/1492/0/19, 5 raw flips individually re-verified, 3 genuine new reds (`T-1084` AC14/AC15/AC16) attributed to stacking. | **Population derivation independently re-run and byte-identical** (below); did not re-run the full 90-unit base-vs-head sweep myself (this is "the arms" the task brief explicitly scopes out of QA re-verification — a ~55-minute serial pass per side). The provenance file's decision entries document the reuse-and-spot-check method in detail and five of the six spot-checked criteria were named with their own live command and result. |
| (e) Measured 40-hex values | orchestrator | `feature/1084-dispatcher`=`9c950d7...`, `develop`=`9b9f79a...`, evidence commit=`6937e19...`, `git show-ref` exits 0, `git branch --show-current`=`feature/1085-default-path-firing`. | **Independently re-run, all match** (rev-parses above; evidence-commit sha matches board's `- probe-evidence-sha (T-1085):` line, cross-checked via `check-intent`/`check-acs` **AC3**/**AC4** passing live, which resolve through that same sha). |
| (f) Per-source report, all 15 `## Summarized sources` entries | engineer | Every source opened first-hand this round, each cited distinction confirmed verbatim. | **Independently spot-checked against the actual source bytes** — see the dedicated Summarized-sources table below. No flattened distinction found. |
| (g) Firing's own execution conditions | orchestrator | Clock source `/bin/date +%s%N` (19-digit); 8 logical cores, `TEAM_FANOUT_MAX` unset; `subagent_type`=`shell-team:qa-verifier`, model=`sonnet`; 600000 ms per-call timeout; `CHECK_ACS_TIMEOUT=300` both arms; pinned commit `09cdf36d...`. | **Cheaply re-run items confirmed**: `getconf _NPROCESSORS_ONLN` → `8`; `$TEAM_FANOUT_MAX` unset in this session — both match. The clock source, per-call timeout and pinned-commit values are one-off session facts from the firing itself and cannot be independently re-measured after the fact; taken as reported, consistent with the launch record's own quoted `- cap-ground: measured-cores=8` line. |
| (h) Post-arm cleanliness and venue confirmations | orchestrator | `git status --short`/`git ls-files --others --exclude-standard` both empty at the seam; each arm its own fresh `git clone --no-hardlinks`, no `git worktree add` for any arm; runs directory default-resolved (`TEAM_RUNS_DIR` unset). | **Re-run now, still holds**: `git status --short` empty in the real checkout at this QA seam too (above). Venue/clone claims are one-off session facts from the firing itself, consistent with the note's own "Firing evidence" narrative and the launch record's quoted paths under `$TMPDIR`. |
| (i) Relayed premises re-measured | orchestrator | PR #325 `open`, CI `completed`/`success` at head_sha = branch point; predecessor resolves in `refs/heads/`; issue #277 `open`; sprint plan release-condition wording recorded, `v2.1.0`. | **Independently re-verified live via `api.github.com`** (network path available in this session, unlike some prior QA rounds): PR #325 `state: open`, `head_sha` = `9c950d76290cb0d70f0a01a11bdfe3e901b67043` (exact match to the branch point); its check-run at that sha reports `status: completed`, `conclusion: success`. Issue #277: `state: open`. `git show-ref --verify --quiet refs/heads/feature/1084-dispatcher` exits 0 at this seam too. All four match. |

**Empirical re-derivation of the note's own quoted arithmetic (item 4 — never accepted from the note's prose).**
- Overlap window, re-derived independently from the eight `- agent-timestamp:` lines with bash `10#` integer arithmetic (own script, not copy-pasted from the note's own command): `maxf=1787185721254292000` (qa-8), `minl=1787186599452998000` (qa-8), `margin=878198706000` (≈878.199 s), `spread=34494850000` (≈34.495 s) — **matches the note's `- overlap:` line exactly**; window is non-empty (`maxf < minl`).
- All eight `- heartbeat:` values independently checked against that same re-derived window — **all eight strictly inside** `(maxf, minl)`.
- Saving direction, re-derived independently: fanned duration = `completed-epoch − launched-epoch` = `1821152499000` ns; serial duration = `last − first` (serial-1) = `3351333776000` ns; `serial_dur > fanned_dur` ⇒ `faster` — **matches the note's `- saving-direction: faster` line**. Saving percentage independently computed: `(3351333776000−1821152499000)/3351333776000 ≈ 45.66%` — matches the note's `- saving:` line's own stated `≈45.66%` (and is correctly distinguished throughout from `phase-multiplexing.md`'s own 43.84% pilot ceiling, **AC14** confirming the attribution count on every line mentioning either figure).
- `t1085-verdict-region-parity` derivation block: re-ran the embedded `- reproduce:` command myself → byte-identical to the note's embedded block (`3227498628 16205`, single bucket `FAN+SERIAL`).
- `t1085-assign-vs-part` derivation block: re-ran the embedded `- reproduce:` command myself → byte-identical to the note's embedded block (all 8 `qa-*` ids in one bucket).
- **AC16**'s `t1085-spec-population` derivation block (spec's own Blast radius section): re-ran the embedded `- reproduce:` command myself → byte-identical (89 base / 90 head, the `+1` being this task's own spec file).

**Verdict-scope pricing (item 5), applied as the spec's own `## Verdict scope` directs.** This is an internal analysis artifact; the mandate is computed values and conclusion direction, and the presence-and-structure shape of most criteria is the correct pricing, not a defect. I read the one claim priced higher — the `opt-in; no phase is rewired to use it by default` reading and the existence-capability reading of `phase-multiplexing.md:364`'s second conjunct — against the actual source bytes (`skills/run/SKILL.md:161`, `phase-multiplexing.md:355,364`, quoted above in the Summarized-sources table) and find both readings textually grounded in the source's own words ("Using this step is always an explicit choice," "able to … exist," "can actually launch"). I do not find either reading wrong on the evidence read this round. Per the spec's own coupling and this corpus's quantitative-conclusion-settlement entry, **this PASS gates progression but does not settle either reading or the saving's direction** — the cross-provider review independently re-derives both from the same recorded evidence; flagging this explicitly for the reviewer rather than treating my own read as final.

**Known disclosed items — verified as records, not re-reported as findings.** All five checked present and consistent between the interventions file, the note's "Five disclosures" paragraph and the provenance file: (1) the qa-3 `T-1050` duplicate-claim dedup, pristine pre-correction part file preserved (interventions entry 5, note disclosure 3, provenance decision not needed — resolved before commit); (2) the two harness-backgrounded 600 s continuations (`T-1040` on qa-2, `T-1050` on qa-3 — note disclosure 2); (3) the `- launched-epoch:` audit-only-stamp latency (note disclosure 1, provenance decision entry 3, which is also why the genuine-overlap margin is stated against launch dispersion rather than `first − launched-epoch`); (4) the `grep -a` binary-misclassification extraction-defect class fix, one carrier swept across all eight part files (note disclosure 4); (5) T-1084's own AC14/AC15/AC16 merge-point-caused new reds — confirmed disclosed in this spec's own `## Blast radius` "Measured, correcting the prediction" subsection and attributed to T-1084's own frozen locks, not repaired here, consistent with this spec's Non-goals.

**Summarized sources — verified against the source itself, not the spec's paraphrase (mandatory item).**

| Source | Sites checked | Result |
|---|---|---|
| `skills/run/SKILL.md` | Lines 42, 70, 85, 161, 166, 207 read directly | All six match the spec's/note's quoted or paraphrased text verbatim (transcription seam, Validate-after-Implement ordering, step-5 mechanics pointer, `opt-in` parenthetical, 120 s timeout + bare-`&` death, precedence-rule consumer copy). |
| `templates/prompt-blocks/fanout-orchestration.md` | Byte-identity to branch point (`AC1`, live) | PASS — the spec's claim that this file is byte-identical to its branch-point blob is mechanically enforced and confirmed green this round. |
| `templates/prompt-blocks/dispatch-record.md` | Byte-identity to branch point (`AC1`, live); grammar cross-checked against the board's own two live `- dispatch:` records | PASS; grammar (`- dispatch: <axis> — <value> — <unconditional\|conditional> — <ground>`) matches both live board instances. |
| `docs/loop-engineering/phase-multiplexing.md` | Lines 355, 359, 364, 370, 371, 375 read directly; byte-identity to branch point (`AC1`, live) | All six lines match verbatim, including the exact "able to … exist" / "can actually launch" wording the Goal's existence-capability reading turns on. |
| `docs/loop-engineering/agent-launch-fanout.md` | Lines 186, 200, 217, 242, 247 read directly | All five match verbatim, including the `subagent_type` declared-label epistemic boundary and its stated counterfactual, and the `- umbrella: 277` line naming T-1085 as owed the live-firing half. |
| `bin/aggregate-verdicts.sh` (lines 1–100) | Part-file grammar (lines 19–21), `design-note-T-1012.md` sentinel citation (line 32), `verdict-region` byte-identity property (line 55 area) | All confirmed present verbatim. |
| `bin/check-fanout-instances.sh` | Usage/success-line grammar, mixed-scope `missing-instance` warning (lines 62, 82, 175–176) | Confirmed present verbatim. |
| `bin/derive-populations.sh` | `--set` identifier grammar, `lines: 0 — items: 0` semantics, block grammar | Confirmed present verbatim; also exercised live three times this round (population, assign-vs-part, verdict-region-parity), each byte-identical. |
| `bin/check-acs.sh` | `AC_RE`, `--root`, `${CHECK_ACS_TIMEOUT:-120}` fallback | Confirmed present verbatim. |
| `bin/team-paths.sh` | Ten-key `--get` set, `TEAM_RUNS_DIR` override | Confirmed present verbatim. |
| `.shell-team/.gitignore` line 4 | `runs/` | Confirmed exact byte match. |
| `bin/check-intent.sh` | `--print-hash`, `marker_count()` whole-line-equality + `\r$` strip | Confirmed present verbatim. |
| `agents/*.md` frontmatter (all nine) | Every `tools:` line read directly | Confirmed: zero `Agent`/`Task` tokens across all nine; `qa-verifier` carries exactly `Read, Grep, Glob, Bash` + `model: sonnet`. |
| `.shell-team/specs/T-1083-agent-launch-fanout.md` | `probe-protocol` marker existence, AC8's byte-lock shape | Confirmed present and structurally analogous to this spec's own AC4. |
| `.shell-team/specs/T-1084-dispatch-routing-record.md` | Lines 40–41 (declaration region), AC16 (lines ~106), Blast radius table | Confirmed present verbatim; T-1084's own board record (`- dispatch: verify — serial — unconditional — ...never fired on the default path...`) cross-checked directly against the live board and matches the spec's characterization of the divergence exactly. |
| `.shell-team/todo.md` (lines 1–14, this task's own entry) | Active-line format, this task's own two `- dispatch:` records | Confirmed present verbatim (checked live above under AC8). |
| `agents/pm-spec.md` | `## Spec completion self-check` heading | Confirmed present. |

No flattened distinction found at any checked site.

**Regressions.** No stray files or uncommitted state introduced by this round's probing — `git status --short` empty throughout and after. No suite touched by this task's zero-shipped-diff (`AC1`, confirmed PASS) can regress, since nothing under `skills/`, `templates/`, `agents/`, `bin/`, `tests/` or `.github/` changed.

**Edge cases tried.** (1) Re-derived the overlap window and saving-direction arithmetic independently from raw quoted values rather than trusting the note's own computed lines — both matched. (2) Cross-checked the board's live T-1084 divergence record against this spec's own characterization of it, rather than trusting the paraphrase — matched exactly. (3) Verified PR #325/issue #277 live states over the network (available this session) rather than accepting the freeze-time snapshot as still current — both still consistent with the recorded values. None of these surfaced a defect; none is `out-of-input-space` (all are within the spec's own declared reachable classes — real timestamps, a real board record, real tracker state).

**Risk notes for reviewer.** (1) Per Verdict scope, my PASS on the `opt-in`/existence-capability readings and on **AC13**'s saving-direction conclusion gates progression only — the cross-provider review is the settling read for both. (2) AC19(a)'s full 18-mutation self-check and AC19(c)'s full 74-step CI-equivalence run were audited by shape and partially spot-checked (per the task brief's own scoping of "the arms" out of QA re-verification), not fully independently re-executed end to end — flagging this depth distinction explicitly rather than folding it into a blanket "confirmed" claim. (3) The three genuine new reds against `T-1084`'s own AC14/AC15/AC16 are real, permanent (until T-1084's owners re-freeze), and correctly attributed to stacking rather than to this task's own diff — consistent with this task's own AC1/AC18 passing.

## Cross-provider review — round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1085
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 7 files, +1416/-1 (base `9c950d7`)

Codex（primary pass: `codex exec --sandbox read-only review --base 9c950d7 --json`／adversarial pass: 自由記述プロンプトで class-B re-freeze の統治とopt-in読み替えを追撃)の所見を、私自身の独立した再導出・実測と突き合わせた結果。生の Codex 出力は `.shell-team/reviews/T-1085-codex-primary.{txt,jsonl}` と `.shell-team/reviews/T-1085-codex-adversarial.{txt,jsonl}` に保存済み。

**Verdict scope の適用**: このタスクは内部分析ノート（release-gate 計測）であり、評価対象は「計算値と結論の方向性が frozen intent（v2）と spec 自身の criteria から正しく follow するか」。criterion が structure-only であること自体は defect ではない（spec 自身の Verdict scope 節どおり）。ただし今回見つかった Blocker は spec の criteria の shape の話ではなく、**intent block そのものの再凍結（re-freeze）手続きがこの repo のガバナンス文書に違反しているかどうか**という、frozen intent の正当性そのものに関わる論点であり、Verdict scope の除外対象（process bookkeeping / QA-audit completeness）には当たらない。

#### Blockers

- `.shell-team/todo.md:36`（T-1085 board entry の `intent-ratified (2026-08-20): v1→v2`）— **class-B re-freeze が人間（host/operator）の GO なしに orchestrator の自己 attestation のみで実施されている。** 実変更は AC4 の positive control（`.shell-team/specs/T-1085-default-path-firing.md:117-118`）の prose `at least twenty` → `at least fourteen` と check 行 `-ge 20` → `-ge 14` の2箇所。board の記録自身が `bin/check-refreeze-class.sh --old-hash 279536b0… --new-hash 249b76b2… -- <v1 copy> <spec>` の実行結果を「Classified **class-B**」（exit 1）と明記している。私自身も同じコマンドを独立に再実行し、同一の `class-b`（exit 1、"differing line at intent-block index 79 ... is not a '- check:' line on both sides"）を再現した。一方 `docs/tuning-oversight.md:107-109`（"Who may re-freeze a frozen intent block"）は「class-B re-freeze は常に per-instance human GO が必要で、standing grant で委任できるのは class-M mechanics repair だけ。grant が無ければ、どちらのクラスも per-instance human GO のまま」と明記し、`skills/run/SKILL.md:53`（class-M branch のパラグラフ）も「`mechanics`（exit 0）だけがこの branch を許可する。`class-b`（exit 1）と全エラー（exit 2）は `pm-spec` への human-ratified re-freeze に回る」「Goal 文・Non-goals・criterion の prose・Input space に触れるデルタは class B であり、他に何が一緒に動いても human のまま」と明記している。しかし `.shell-team/interventions/T-1085.md:22-25` は本件を「the loop's class-M mechanics path (the T-1083 v1→v2 precedent)」「re-frozen as v2 under the standing grant」と記述しており、これは checker 自身が返した `class-b` 判定と正面から矛盾する。`.shell-team/provenance/T-1085.md` の6件の decision entry のどこにも、この v1→v2 に対する human GO の記録は無い。`CLAUDE.local.md` の host ruling（「凍結（初回・mechanics 再凍結とも）にホスト GO を取らない」）も対象を明示的に「初回・mechanics 再凍結」に限定しており、class-B をカバーしない。Codex の adversarial pass も同一の一次資料（`tuning-oversight.md:107,109`、`SKILL.md` の class-M branch、`interventions/T-1085.md:22,25`、`provenance/T-1085.md`）を独立に読み、`[P1]` として同一の結論に達した。
  - 補足（実体面）: 測定された矛盾自体は本物である。frozen `firing-protocol` 領域（spec 53–83行目）は evidence commit `6937e19` と HEAD の両方で厳密に16 non-blank lines（自分で `awk`/`grep -c .` を実行して確認済み）であり、v1 の `-ge 20` は構造的に充足不可能だった。修正（twenty→fourteen）は意味を変えず、2行の安全マージンを保ったまま充足可能にする穏当な repair であり、`firing-protocol` 領域自体（マーカー行53/83、抽出内容)は不変（`git diff 30460b0 da70d9b` で確認—AC4 のcriterion文と check 行以外に差分なし）。つまり欠陥は完全に手続き上のものであり、repair の中身自体は正しい。
  - **Disposition**: この項目への REQUEST_CHANGES は、spec の revert procedure に従い、v1 を新しい ratified version としてバイト単位で復元し、standing grant をホストレビュー保留のsuspendedとして扱うことを意味する。修正版では、同じ repair 内容（twenty→fourteen, -ge 20→-ge 14）を正規の class-B 経路（human GO を得たうえでの re-freeze）で再度行うか、`interventions`/`provenance` に実際の human GO 記録を追加してから v2 を再承認することを推奨する。

#### Major

- `docs/loop-engineering/default-path-firing.md:645`（`licence-condition: genuine-overlap`）— frozen `firing-protocol` の `## The verdict.` パラグラフ（spec 71行目）は `genuine-overlap` の定義として「a non-empty intersection window ... with its margin against **the maximum single launch latency** stated」を要求しているが、実際の note は `- launched-epoch:` が複数インスタンスの `first` より**後**であるという disclosure（disclosure 1）を理由に、この latency 指標を計算不能として、代わりに「launch dispersion（≈34.495 s）に対する比率（≈25.5×）」という**別の指標**を使い、それでも licence condition を `met` としている。disclosure 自体は誠実だが、frozen text が要求する具体的な指標は実際には一度も計算・記録されていない。Codex primary pass も同一箇所を `[P1]` として指摘（"proves a different condition"）。ただし実体面では、8インスタンス全ての `agent-timestamp`/`heartbeat` を自分で再計算し、window `[1787185721254292000, 1787186599452998000]`（margin_ns=878198706000 ≈878.199 s）に全 heartbeat が厳密に収まることを確認済みであり、real overlap が起きたこと自体は疑いようがない。よって reachability 判定全体を覆すものではないが、frozen wording を字義通り満たしていないまま `met` と記録している点は是正が必要（正規の class-M/B 経路での wording 修正、または licence condition 側に disclosure 注記を追加するなど）。
- `docs/loop-engineering/default-path-firing.md:679-681`（`- saving:`, `- arm-wall-clock:`）— fanned arm の duration は orchestrator 側の外側境界（`launched-epoch`→`completed-epoch`）、serial arm の duration は instance 内側の `agent-timestamp first/last` で計算されており、比較基準が両アームで一致していない（apples-to-apples ではない）。Codex primary pass が `[P2]`（正規化後 major）として指摘。自分で inner-timestamp 基準（`min(first)`〜`max(last)`、全8インスタンス）に揃えて fanned arm の duration を再計算したところ 1,779,893,739,000 ns となり、揃えた場合の saving は `(3351333776000−1779893739000)/3351333776000 ≈ 46.90%` — 方向（`faster`）も符号も変わらず、むしろ僅かに大きくなる。したがって結論の方向性は揺るがないが、報告されている `≈45.66%` という具体的な数値の算出根拠が両アーム非対称であることは note のどこにも disclosure されておらず（cache-warmth の非対称性は明記されているのに、この非対称性は無言）、これは是正または明記が必要。

#### Minor / nits

- `docs/loop-engineering/default-path-firing.md:666-677`（`t1085-verdict-region-parity` derivation）— `sort -u | cksum` による32bitチェックサム＋行数比較で `verdict-region-parity: identical` を判定しており、frozen protocol が要求する厳密な byte-identity の証明としては理論上衝突の余地がある（Codex `[P2]`）。ただし自分で両アームの `<!-- BEGIN verdict-region -->`〜`END` ブロックを全文読み、内容が文字通り同一であることを直接確認済みであり、今回の実データについては defect ではない。将来の再利用に向けたチェッカー硬化の fast-follow。
- `.shell-team/specs/T-1085-default-path-firing.md:284,290`（Blast radius の `t1085-spec-population` derivation）— `git ls-files -- .shell-team/specs/*.md` のように specs ディレクトリを `bin/team-paths.sh --get specs` 経由でなく直書きしている（Codex `[P2]`）。ただしこの spec 自身の criteria はこの repo 自身の commit 履歴・board に対する自己参照的な一回性チェックであり、この repo は自身の CLAUDE.md により常に shipped default layout で運用される旨が明記されている。legacy layout の adopter checkout でこれらの check 行が実行される想定経路が無いため、実害は無い。
- `.shell-team/specs/T-1085-default-path-firing.md:108,161`（AC1/AC18 の changed-set gate）— `git diff --name-only <base>...HEAD`（コミット済み差分）と `git ls-files --others --exclude-standard`（未追跡ファイル）の和集合のみを見ており、**既に追跡済みのファイルへの未コミットの変更**（`git diff`/`git diff --cached` のワーキングツリー差分）を検出しない（Codex `[P2]`）。今回のラウンドは `git status --short` が終始 empty（レビュー時点でも再確認済み）であり実害は無いが、この "zero shipped byte" gate パターンはこの corpus の他 spec にも再利用される形なので、汎用的な硬化の価値はある。
- `.shell-team/specs/T-1085-default-path-firing.md:134`（AC9）と `:139-143`（AC11/AC12）— AC9 は `- assign:` の instance-id 集合と `- part:` 名の集合が一致することしか見ておらず、`- assign:` の unit-id 集合や `achieved-n` を実際の `- population:` ファイルの内容と突き合わせていない。AC11/AC12 も「少なくとも2件」の `agent-timestamp`/`liveness` レコードしか要求せず、launch record の `achieved-n`（本ラウンドでは8）との厳密な集合一致を求めていない（Codex `[P1]`、ただし私はこれを検証手続きの堅牢性のギャップと判断し major から降格。理由は synthesis audit ledger 参照）。今回の実データ自体は独立に確認済みで正しい（`t1085-assign-vs-part` derivation で8インスタンス全一致、note 本文にも8インスタンス分の `agent-timestamp`/`heartbeat`/`liveness` が実在）。将来同型の spec が「一部インスタンスの証跡だけで通過する」ケースを防ぐための fast-follow。

#### Adversarial findings

Codex に「この2点を壊しにいけ」と明示的に依頼した devil's-advocate pass の結果:
1. class-B re-freeze のガバナンス違反 — `[P1]`。上記 Blocker と同一結論に独立到達（`SKILL.md` の class-M branch の文言という、私が primary pass では引いていなかった一次資料を追加で引用）。
2. `opt-in; no phase is rewired to use it by default` の読み替えと `phase-multiplexing.md:364` の existence-capability 読み替え — 反証できず、`severity: none`。`skills/run/SKILL.md:85`（board の `dispatch: verify` record が決定を所有する）と `:161`（"always an explicit choice"）、`phase-multiplexing.md:355`（"can actually launch"）/`:364`（"able to launch ... exist"）の文言を実際に読んだ上で、"過去に一度発火したか" を要求する文言ではないと結論。T-1083 の実 Agent launch + aggregation exit 0 の記録（`agent-launch-fanout.md:185-186`）も傍証として引用。私自身も同じ一次資料を読み、同じ結論に至った。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC9: assign-set が population file の中身と突き合わされていない | primary: blocker (P1) | major→fast-follow (minor扱い、deferred) | 今回の実データ（90ユニット・8インスタンス全一致）は `t1085-assign-vs-part` derivation で独立に確認済みで正しく、この round の結論を無効化しない。チェッカー自体の頑健性のギャップであり、process/audit-completeness 側の指摘として fast-follow に格下げ |
| 2 | AC11/AC12: "少なくとも2件" が achieved-n との集合一致を要求しない | primary: blocker (P1) | major→fast-follow (minor扱い、deferred) | 同上。実データは8インスタンス全件が実在し独立確認済み |
| 3 | genuine-overlap が frozen wording（launch latency margin）でなく代替指標（launch dispersion 比）で `met` と記録 | primary: blocker (P1) | major | frozen text の字義を実際には満たしておらず、is-met の記録根拠に実質的なギャップがある。ただし overlap の実体（878s window、8インスタンス全heartbeat 内包）は独立再確認済みで疑いなく、reachability 判定全体は覆らないため blocker でなく major |
| 4 | saving 45.66% が両アーム非対称な境界（orchestrator epoch vs instance timestamp）で算出 | primary: major (P2) | major（変更なし） | 独立再計算（inner-timestamp 基準に統一）で方向は不変・数値はむしろ増加（≈46.90%）と確認したが、非対称性自体が note に disclosure されておらず、計算根拠の透明性の欠如は是正が必要と判断し major を維持 |
| 5 | verdict-region-parity が cksum ベースで byte-identity の理論的代替に留まる | primary: major (P2) | minor (fast-follow) | 両アームの verdict-region 全文を直接読み、実際に byte-identical であることを独立確認済み。今回は defect でなくチェッカー硬化の fast-follow |
| 6 | Blast radius の spec-population derivation が specs ディレクトリを直書き（team-paths.sh 非経由） | primary: major (P2) | minor (won't-fix) | この spec の criteria は本 repo 自身の commit 履歴に対する自己参照的な一回性チェックであり、本 repo は常に shipped default layout で運用される（CLAUDE.md）。legacy layout の adopter がこの check 行を実行する経路が存在しないため実害なし |
| 7 | AC1/AC18 が「既追跡ファイルへの未コミット変更」を検出しない | primary: major (P2) | minor (fast-follow) | レビュー時点で `git status --short` が empty であることを直接確認済みで、今回の delivered 状態には影響しない。ただしこのゲートパターンは corpus 内で再利用されるため汎用的な硬化価値あり |
| 8 | class-B re-freeze が human GO なしに自己 attestation のみで実施 | primary: N/A（primary pass のプロンプトでは扱えなかった）、adversarial: blocker (P1) | blocker（変更なし） | 私自身の独立読解（`docs/tuning-oversight.md`、`SKILL.md` の class-M branch、`check-refreeze-class.sh` の再実行）と Codex adversarial pass の独立読解が完全に一致。frozen intent の正当性そのものに関わり、Verdict scope の除外対象（process bookkeeping）に当たらないため blocker を維持 |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_ENG` に戻し、上記 Blocker と Major を紐付けて記録する。

具体的な是正案:
1. class-B re-freeze（AC4 の twenty→fourteen 修正）を、human/operator の GO を得たうえで正規の class-B 経路（`docs/tuning-oversight.md` の grant テンプレート／`skills/run/SKILL.md` の "stop and escalate" 経路）でやり直す。中身（repair の内容）自体は正しいので、手続きの正当化だけで足りる可能性が高い。
2. `genuine-overlap` licence condition の記録方法を、frozen wording（launch latency margin）を字義通り満たす形に是正するか、正規の re-freeze でその文言自体を是正する。
3. saving 45.66% の算出に使う両アームの時刻境界を揃える（instance-level agent-timestamp 同士、または orchestrator-level epoch 同士で統一する）か、非対称性を cache-warmth と同様に明示的に disclosure する。

#### Fast-follow disposition

- AC9/AC11/AC12 の集合一致検証の頑健性強化（population file の中身・achieved-n との厳密な突き合わせ）— file-an-issue（理由: この AC パターンは fan-out 系 spec で再利用される可能性が高く、将来の誤った population/instance 主張を防ぐ価値がある）
- `t1085-verdict-region-parity` のような derivation を cksum でなく直接 byte 比較にする硬化 — file-an-issue（理由: 同上、再利用パターンの頑健性向上）
- AC1/AC18 の changed-set gate に既追跡ファイルの未コミット差分（`git diff`/`git diff --cached`）を含める硬化 — file-an-issue（理由: zero-shipped-byte gate は複数 spec で再利用される核心的な安全装置であり、汎用的に閉じておく価値が高い）
- Blast radius の spec-population derivation の `.shell-team/specs` 直書き — won't-fix（理由: この spec の criteria は本 repo 自身の commit 履歴への自己参照であり、本 repo は常に shipped default layout で運用されるため legacy layout 経路が存在しない）

## QA round 2 — T-1085

**Verdict: FAIL → REWORK.** Delta round over cross-provider review round 1's two Majors (above), verifying the engineer's rework (`9b385bc`). Read first-hand before verifying: the review round-1 Major text, the board's `### Engineer rework — T-1085 (round 3, ...)` sub-section with its two class-sweep tables, `git show 9b385bc --stat` and the full diff.

**1. Major 1 closure (`genuine-overlap`) — arithmetic independently re-derived, PASS.** Own bash `10#` re-derivation of `d_i = first_i − launched-epoch` (`launched-epoch=1787185735135713000`) for all eight instances:
```
qa-1: d=-48376271000   qa-2: d=-43471126000   qa-3: d=-38782944000   qa-4: d=-33502671000
qa-5: d=-28480260000   qa-6: d=-23231502000   qa-7: d=-18527896000   qa-8: d=-13881421000
```
All eight match the note's quoted values exactly; the maximum (closest to zero) is `qa-8`'s `-13881421000` ns, matching the licence-condition line. The negative value is disclosed as such — its cause (the audit-only `launched-epoch` stamp postdating every instance's own `first`) is named, not hidden — and the `met` reading rests explicitly on "the non-empty window itself, together with this frozen metric having actually been computed ... and disclosed," not on the metric being a usable positive margin. This is an honest discharge of the frozen wording's literal requirement, closing the review's Major 1 finding as stated.

**2. Major 2 closure (arm-clock basis) — arithmetic independently re-derived, PASS.** Own re-derivation of the symmetric inner-instance basis:
```
minf=1787185686759442000 (qa-1)  maxl=1787187466653181000 (qa-3)  fanned_dur=1779893739000
serial_dur=3351333776000
python3 -c "print((3351333776000-1779893739000)/3351333776000*100)" → 46.88998894271879
```
Both `- arm-wall-clock:` lines (`fanned — start=1787185686759442000 — end=1787187466653181000`; `serial` unchanged) match this re-derivation exactly; `- saving-direction: faster` agrees with the recomputed sign (`serial_dur > fanned_dur`); the ≈46.89% figure is arithmetically correct (46.88998...% rounds to 46.89%); the superseded mixed-basis ≈45.66% figure is disclosed inline in both the `- saving:` line and `## Implications for the release decision`, not silently overwritten. Closes the review's Major 2 finding as stated.

**3. Class-sweep spot-checks, 3 rows from each table, independently confirmed against the doc's own text.**
- Table 1, row 3 (`real-plugin-role-instances`, "N ≥ 2" formula) — confirmed at line 644: ground is `- requested-n: 8` / `- achieved-n: 8`, the literal metric, not a substitute.
- Table 1, row 5 (`real-population`, no formula) — confirmed at line 646: ground is the 90-unit count and both arms' `summary: units: 90 — verdicts: 88 — sentinels: 2` lines (also present verbatim at lines 188 and 377).
- Table 1, row 11 (`instances-launched`, references the unchanged window value) — confirmed at line 606: the chain-link text cites the eight `agent-timestamp` lines and the re-derived window, never the disputed launch-latency/dispersion comparator.
- Table 2, row 2 (population-fixed's 90/88/2 cross-check, same tool both arms) — confirmed: both `summary:` lines (188, 377) are produced by the same `bin/aggregate-verdicts.sh` command per arm, no orchestrator-vs-instance mismatch.
- Table 2, row 3 (`t1085-verdict-region-parity`, cksum of sorted content, same extraction both arms) — confirmed at lines 664–675: unchanged `awk`/`sort -u`/`cksum` extraction over both arms' own verdict-region blocks.
- Table 2, row 5 (pre-commitment's 90-minute trigger, serial arm's own single duration only) — confirmed at line 684: the check reads only the serial arm's own measured duration, no fanned-arm value enters the comparison.

All six spot-checked rows are accurate as characterized.

**4. Regression — a genuine sibling-line miss the class-sweep did not catch (FAIL-driving finding).** `grep -n "against the instances' own launch dispersion" docs/loop-engineering/default-path-firing.md` still returns a hit **outside** the two rewritten paragraphs/lines:
```
$ grep -n "dispersion" docs/loop-engineering/default-path-firing.md
636:...(round-2 text stated the margin against the instances' own launch dispersion instead...)... [fixed paragraph, correctly historical]
645:...The launch-dispersion figure (...) is carried alongside purely as a secondary, informational cross-check and is not what this `met` value rests on. [fixed line, correctly demoted]
712:- Sub-second overlap discrimination: the overlap claim this note makes (`## Default-path chain and aggregation analysis` above) is a non-empty intersection window with a stated margin against the instances' own launch dispersion, never a claim at finer timing resolution (Input space).
```
Line 712, in `## Limits and what is not computable`, still asserts in the present tense that "the overlap claim this note makes ... is a non-empty intersection window with a stated margin against the instances' own launch dispersion" — directly contradicting the now-corrected lines 636/645, which state the margin is against the frozen wording's own metric (maximum single launch latency) and that dispersion "carries no weight in the licence condition's `met` determination." `git log` confirms this line predates the round-3 rework (present unchanged since round 2's `ba3a776`; `9b385bc`'s diff never touches it):
```
$ for c in $(git log --format=%H -- docs/loop-engineering/default-path-firing.md); do git show "$c:docs/loop-engineering/default-path-firing.md" 2>/dev/null | grep -q "Sub-second overlap discrimination" && echo "$c has the line"; done
9b385bcc9a21e9b4d8781803ebfb184190e5b0a7 has the line
ba3a7761bef1e83e076f53634995850ceefc0bf6 has the line
6937e19c203a7323b89862d0fa2bcbd4cf4b5d21 has the line
7b3826ce10f4e8c9aa1a4359e8a46c924739182c has the line
```
Both the commit message ("Both fixes include a class-sweep inventory over every sibling line/comparison in the note") and provenance decision entry 8 ("every other cross-arm comparison in the note ... was read for the same basis-symmetry defect class") claim a completeness the actual sweep did not deliver: the class-sweep tables scoped themselves to the licence-condition/chain-link lines (Finding 1) and the cross-arm comparison lines (Finding 2), but never walked the `## Limits and what is not computable` prose section, which also restates the same disputed comparator as if it were still the live basis. This is not a cosmetic nit — it leaves a materially self-contradictory claim in a release-citable analysis note (the note now says two different things about what its own overlap margin is stated against), which is exactly the class of defect this round exists to close, one level up (the fix itself is correct; the sweep that was supposed to propagate it was not exhaustive).

**Mechanical sweep (own run, live).** `export CHECK_ACS_TIMEOUT=300; bash bin/check-acs.sh .shell-team/specs/T-1085-default-path-firing.md 2>&1 | tail -1` → **`check-acs: 18 passed, 0 failed, 1 skipped, 0 unrecognized`** (AC3's evidence byte-lock confirmed green as part of this same run). `bash bin/check-intent.sh .shell-team/specs/T-1085-default-path-firing.md .shell-team/todo.md` → **`check-intent: aligned: T-1085 v2 (249b76b2a6aa19f4135807ff80bb2b7179859e67) matches ...`**. `bash bin/check-handoff.sh .shell-team/todo.md; echo rc=$?` → **`rc=0`**. `bash bin/check-provenance.sh .shell-team/provenance/T-1085.md` → **`check-provenance: conformant: .shell-team/provenance/T-1085.md (10 decision entries, 0 sentinel)`**. `bash bin/check-interventions.sh --task T-1085 -- .shell-team/interventions/T-1085.md` → **`check-interventions: conformant: .shell-team/interventions/T-1085.md (7 entries, 0 sentinel)`**. `B=$(git merge-base feature/1084-dispatcher HEAD); bash bin/check-pii-shapes.sh --base "$B"` → **`check-pii-shapes: clean (no PII-shaped bytes found)`**. All mechanical gates green; the failing item is a prose-consistency defect no `- check:` line covers, exactly the kind a human/agent reviewer must catch by reading.

**Working-tree discipline.** `git status --short` empty before and after this round's probing; no file was edited by this role other than this review append and the board hand-off/flag flip below (no destructive git operation used).

**Edge cases tried.** (1) Grepped the whole note for every remaining occurrence of "dispersion" rather than trusting the two diff hunks alone — surfaced the finding above. (2) Grepped for stray leftover references to the superseded `1821152499000`/`45.66` figures and the old `arm-wall-clock` values — none found outside the intentionally-disclosed "superseded, round 2" paragraph. (3) Confirmed the `Sub-second overlap discrimination` line predates this round's diff via `git log`/`git show` per-commit content check, ruling out a fresh regression versus a pre-existing miss the class-sweep should have caught this round. None of these is `out-of-input-space` — this is a real-data-reachable inconsistency inside the note's own committed prose, not a synthetic extreme.

### QA verdict: FAIL
- Task: T-1085 → READY_FOR_ENG (rework)
- Failure: the rework's own class-sweep claims ("every sibling line/comparison in the note") do not actually cover the whole note — `docs/loop-engineering/default-path-firing.md:712` (`## Limits and what is not computable`, "Sub-second overlap discrimination") still asserts the overlap margin is "stated against the instances' own launch dispersion," directly contradicting the round-3-corrected `## Default-path chain and aggregation analysis` (line 636) and `## Verdict and licence conditions` `genuine-overlap` line (line 645), which now state the margin is against the frozen wording's own metric (maximum single launch latency) with dispersion demoted to a non-load-bearing secondary cross-check.
- Reproduction:
  $ grep -n "dispersion" docs/loop-engineering/default-path-firing.md
  636:...(round-2 text stated the margin against the instances' own launch dispersion instead...) [fixed, correctly historical]
  645:...is carried alongside purely as a secondary, informational cross-check and is not what this `met` value rests on. [fixed, correctly demoted]
  712:- Sub-second overlap discrimination: ... is a non-empty intersection window with a stated margin against the instances' own launch dispersion, never a claim at finer timing resolution (Input space). [STALE — contradicts 636/645]
  Expected: line 712 states the margin the same way lines 636/645 now do (against the maximum single launch latency, with dispersion carrying no weight), or is reworded so it makes no claim about which metric the margin rests on.
- Suggested next step: re-run the class-sweep discipline against the `## Limits and what is not computable` section (and any other prose outside the two tables already swept) for the same substitute-metric defect class, fix line 712 accordingly, and re-grep the whole file for "dispersion" to confirm every remaining mention is consistent with the corrected licence-condition text before re-submitting for QA round 3.
