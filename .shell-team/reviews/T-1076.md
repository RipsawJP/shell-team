### Codex Review verdict: REQUEST_CHANGES
- Task: T-1076
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 9 files changed, +786/-43 lines (`git diff a78a8e2...HEAD`, this task's own commits only — stacked on T-1073's tip)

Codex was invoked twice: a primary `codex exec review --base a78a8e2 --json` pass (read-only, reading the working tree directly), and — triggered by that pass's own Major-normalized findings — an adversarial "play devil's advocate" pass focused on `bin/log-run.sh`'s lock/`--seq auto` logic, which independently reproduced the two flagged issues with live trap-timing probes and surfaced a third, more severe variant of one of them. Raw captures: `.shell-team/reviews/T-1076-codex-primary.{txt,jsonl}` and `T-1076-codex-adversarial.{txt,jsonl}`.

I read the frozen spec (`.shell-team/specs/T-1076-log-run-locking.md`, intent-hash `78064c1e721bbf86250d8a9b1133e33dfd75ac4d`), `bin/log-run.sh` end to end at HEAD, `tests/log-run/run.sh` end to end, the decision provenance and board hand-off myself before and after reading Codex's output, and confirmed each finding below directly against the source rather than taking Codex's line numbers on faith.

#### Blockers
- `bin/log-run.sh:519-561` (the `LOCK_ACQUIRED` guard flag around the mkdir-acquire and `rmdir`-release paths) — Two independent, narrow-but-real Bash trap-timing races on the single `LOCK_ACQUIRED` flag that guards `release_lock()`:
  (a) **acquire-side** — Bash defers a trapped INT/TERM signal received while waiting on the external `mkdir` until that command returns; if the signal lands after `mkdir` succeeds (loop exits, D2's bounded wait ends) but before the very next statement `LOCK_ACQUIRED=1` executes, `on_lock_signal` fires with the flag still `0`, so `release_lock()` skips its `rmdir` and the lock directory is orphaned permanently — an ordinary INT/TERM can now produce the same stale-lock outcome the spec's own D3 discloses as a **SIGKILL-only** residual case, not a signal the writer is supposed to handle cleanly.
  (b) **release-side, worse** — inside `release_lock()` itself, if a signal lands after `rmdir "$LOCK_DIR"` succeeds but before the following `LOCK_ACQUIRED=0` reset, a re-entrant `on_lock_signal` call sees the flag still `1` and calls `rmdir` again — on whatever now occupies that path, which by then can be a **different process's freshly-created lock directory** (a successor that acquired the instant this process's own lock was freed). That is a direct violation of D3's "never steal" invariant: this writer would delete another live writer's lock out from under it, defeating the exact interleaved-append failure this whole task exists to prevent.
  The adversarial pass reproduced both stale-flag states with deterministic trap probes rather than reasoning about them abstractly; `tests/log-run/run.sh`'s own `lock-released-on-signal` fixture (`:424-455`) injects its `sleep 5` delay *after* `LOCK_ACQUIRED=1` is already set, so it structurally cannot exercise either window.
  - Suggested fix (Codex's, which I find sound): make the acquire/release-ownership transition itself signal-safe — an explicit acquiring/held/releasing state machine, or defer INT/TERM delivery across the single `mkdir`-then-flag and `rmdir`-then-flag transitions and re-deliver any pending signal once the flag is consistent. Add a probe that signals from the acquire and release commands themselves before they return, including a waiting successor during the release-side probe.

#### Major
- `bin/log-run.sh:400-417` (`compute_auto_seq`) — The per-`run_id` scan matches the file's **jesc-escaped** stored `run_id` text against the caller's **raw, unescaped** `$RUN_ID`. A `run_id` containing a backslash or double-quote is captured doubled/truncated by the boundary-anchored regex and can never equal the raw comparison value, so `--seq auto` silently restarts at `1` for that run instead of continuing the count — the exact "no duplicate, no gap" guarantee D4/AC2 exist to provide, defeated silently rather than refused loudly. `--run-id` carries no character-class validation anywhere in the script (only non-empty), unlike `--instance`/`--provider`/`--adapter`/`--effort`, which all reuse `BINDING_TOKEN_RE`. Confirmed not reachable through any *current* in-repo caller (`bin/close-out.sh` uses a generated safe id + explicit `--seq 1`; `skills/run/SKILL.md` recommends timestamp-style ids and still uses explicit numeric `seq`), but reachable by a direct adopter invocation or a future `--seq auto` caller the moment one exists, and nothing in the Input space's "Out-of-scope synthetic extremes" names this as declined — it isn't a declared boundary, it's an unnoticed gap.
  - Suggested fix: compare against the escaped form (re-derive it with `jesc` before comparing), or decode the captured value before comparing, rather than comparing raw-vs-escaped.
- `tests/log-run/run.sh:356-377` (`run_contention_writer`) — The bare `wait` at the end of the helper discards each background writer's individual exit status; a writer that fails outright (argv/process-launch/filesystem failure — plausible given the negative control's own 16 KiB `--error` payload) would still let the helper return cleanly, and a resulting short line count would then be classified as **the mutant's** detection rather than as a broken fixture. This doesn't put this task's own recorded outcome in doubt — the `seq-set-mismatch` the engineer, QA and the adversarial pass all independently reproduced is a genuine TOCTOU signature, not a line-count artifact — but AC10's own "measurably fail, judged honestly" contract is weaker than it should be for future runs of this suite.
  - Suggested fix: track each background writer's PID and require every exit status to be zero before classifying the run; treat any non-zero as a fixture failure distinct from `detected`/`not-detected`.

#### Minor / nits
- None beyond the above — the review depth requested (shipped-`bin/` norm) turned up nothing at nit level worth a separate line; everything found rolled up into the two Major items and the one Blocker.

#### Adversarial findings
- Reproduced both `LOCK_ACQUIRED` signal-timing windows (acquire-side and the more severe release-side variant) with deterministic Bash trap probes rather than by inspection alone — this is what promoted the release-side case from a documentation gap to a Blocker.
- Independently confirmed `--run-id` has no character-class validation and traced the escape/compare mismatch to its root cause (the `[^\"]*` capture terminating early on an escaped quote, and a doubled backslash never matching a single one).
- Surfaced the `run_contention_writer` bare-`wait` soundness gap in the negative control as a third finding beyond the two it was asked to re-assess, in direct response to this round's own "could the negative control false-positive `detected`" angle.
- Did not find anything to flag on exit-code collisions (3 is genuinely distinct from 0/1/2, asserted three ways in AC5), or on macOS/Linux `mkdir`+`date` portability (both plain, POSIX-portable).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | signal-timing race on `LOCK_ACQUIRED` (acquire-side only) | primary: major (P2, "close the signal window after successful mkdir") | blocker | adversarial pass reproduced the acquire-side window deterministically **and** found a second, more severe release-side window that actively violates the never-steal invariant (can `rmdir` a successor's live lock) rather than only orphaning one — the combined finding is escalated to blocker on the strength of that reproduction, not accepted at the primary pass's own P2 label |
| 2 | `--seq auto` run_id escaping mismatch | primary: major (P2) — adversarial: major (P2, "real medium-severity bug… affects observability rather than application execution") | major | both passes agree; adversarial pass additionally confirmed no in-repo caller currently reaches it, which keeps this at major rather than escalating to blocker, but the gap is real and undeclared in the spec's Input space so it is not downgraded either |
| 3 | negative-control `wait` soundness | adversarial only (P2, new — not raised by the primary pass) | major | accepted as raised; the actual AC10 outcome for this diff is independently triple-confirmed (engineer, QA, and the adversarial pass's own code reading) so it does not itself force REQUEST_CHANGES on its own, but it stands as a major finding on the fixture's soundness going forward |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1076 entry back to `READY_FOR_ENG` with these findings linked. The lock's own core `LOCK_ACQUIRED` transition needs to be made signal-safe before this ships to every adopter running this script on `PATH`; the `--seq auto` escaping bug and the negative-control soundness gap should be fixed in the same round since they are localized and already fully diagnosed above.
- Note for the next round, given this task's own pre-commitment clause: this is round 1 of review (one round of an independent Blocker/Major so far, not two), so the pre-commitment's drop-`--seq auto`-to-a-follow-up trigger is **not** active yet — it fires only if an independent round 2 lands another Blocker/Major on the same component.
- No `## Input space` out-of-scope line covers any of the three findings above (the escaping gap is a genuine, undeclared edge in the frozen Input space rather than a declared boundary), so the `out-of-input-space` downgrade rule does not apply to any of them.

#### Fast-follow disposition
- no fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1076
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 10 files changed, +1276/-43 lines (`git diff a78a8e2...HEAD`, this task's own commits only — full task diff, not just this round's rework)

Codex was invoked twice: a primary `codex exec review --base a78a8e2 --json` pass (read-only, reading the working tree directly), and — triggered by that pass's own P1-tagged finding — an adversarial "play devil's advocate" pass targeting the exact areas the rework's own hand-off flagged (trap masking correctness, the widened `compute_auto_seq` regex, and the new `signal-race-release-side` test's timing assumptions). Raw captures: `.shell-team/reviews/T-1076-codex-primary-r2.{txt,jsonl}` and `T-1076-codex-adversarial-r2.{txt,jsonl}`.

I read `bin/log-run.sh`'s full diff since round 1 (`git diff eb95432..HEAD -- bin/log-run.sh`), `tests/log-run/run.sh`'s full diff (`git diff eb95432..HEAD -- tests/log-run/run.sh`), the updated provenance record (5 new entries, round 2), the frozen spec's `## Input space` and pre-commitment text, and the exact lines Codex's two passes cited, myself and independently, before and after reading Codex's output — including manually tracing the acquire-side and release-side trap/mask control flow statement-by-statement and working out the timing probabilities behind the newly-flagged test race by hand.

#### Blockers
- `tests/log-run/run.sh:752-757` (`relrace_choreograph`, the new `signal-race-release-side` regression added this round to pin round 1's Blocker fix) — `kill -TERM "$p1_pid"` fires the instant P1's post-`rmdir` marker is observed, **without first confirming P2 has actually reacquired the just-freed lock path**. The subsequent "give the successor a moment" wait (line 756) runs only *after* the kill, so it cannot retroactively fix the ordering. Traced by hand and independently confirmed by the adversarial pass: whether the OLD-shape mutant's reentrant `rmdir` actually steals a *live* P2 lock (the assertion at line 775 requires `survives=0`) depends on whether P2's own 1-second poll cadence happens to have already grabbed the path in the window between P1's `rmdir` and the test's kill — a window whose width is itself governed by the test's own 1-second marker-poll granularity. Both timing sources are on the same ~1-second scale with no synchronization between them, so this is not a rare corner case: it is close to a coin flip on every invocation. When P2 has *not* yet reacquired, P1's second `rmdir` finds nothing to steal, P2 subsequently acquires the now-genuinely-free path undisturbed, and the test's own theft assertion fails — a `FAIL:` line in a CI-wired suite (T-1076 spec's own Summarized sources item confirms `tests/log-run/run.sh` is already a wired CI step) produced by scheduling alone, on code that is behaving exactly as intended. This is precisely the kind of harness soundness gap round 1's Major #2 was about, but for the fixture that is supposed to demonstrate round 1's Blocker fix itself, and its failure mode is a spurious red on an otherwise-correct implementation rather than a missed real regression — no less disruptive to a repo whose whole discipline is fail-closed, measured checks.
  - Suggested fix (Codex's, which I agree is the right shape): have P2 write its own explicit "I hold the lock now" marker (or poll `$lockdir`'s presence with a barrier the test waits on) *before* `kill -TERM` is sent to P1, so the choreography stops depending on two independent, unsynchronized 1-second poll cadences landing in the right relative order.

#### Major
- `tests/log-run/run.sh:843-858` (AC10's negative-control token derivation, re-examined this round) — the lock-disabled mutant arm has no explicit barrier forcing two of its 8 concurrent writers through the scan-then-append window at the same instant; the race that produces `detected` is scheduling-dependent rather than structurally guaranteed. **AC10**'s own check line re-runs `tests/log-run/run.sh` live and requires the freshly-measured token to equal the token already committed in `.shell-team/provenance/T-1076.md` (`detected`) — so a future re-run of this exact suite, on this exact code, that happens not to hit the race would fail **AC10** outright, not because anything regressed but because a nondeterministic fixture disagreed with a static record of one earlier outcome. In practice I judge this **low-probability**, not urgent: 8 processes doing 20 rounds each of an unsynchronized read-scan-then-append cycle against one shared file, with zero serialization (the mutant is `if true; then` — no lock attempt at all), makes at least one collision across 160 total appends close to certain on a real multi-core host, and the provenance record already reports it was observed on two independent runs. The frozen spec's own D6/AC10 text explicitly anticipates and provides a disposition for the opposite failure mode (a permanent `not-detected`, escalated via a `- limit:` line) — it does not anticipate flakiness in the other direction (an intermittent `not-detected` after `detected` was already recorded), which is the gap Codex is naming. Worth closing in the same pass as the Blocker above since both are in the same file and the same general area, but not, on its own, forcing this verdict.
  - Suggested fix: add an explicit synchronization barrier (e.g., all N writers block on a shared marker/FIFO until every one has performed its own scan, then release them to append together) so the race is structurally forced rather than probabilistically likely; or, short of that, note the residual nondeterminism explicitly in the provenance record's own text.

#### Minor / nits
- `bin/log-run.sh:526-528` (`lock_mtime`) — on GNU/Linux, `stat -f '%Sm' "$d"` does not fail as the header comment assumes: GNU `stat` parses `%S` as one directive (filesystem block size, valid in `-f` mode) and treats the trailing `m` as a literal character, so the command **succeeds** with a nonsensical value (e.g. `4096m`) instead of falling through to the correct GNU-form `stat -c '%y'` line below it. This is real (I traced GNU `stat -f`'s own format-string grammar to confirm it) and would make the refusal message's "existing lock mtime" field garbled on the project's own CI host (Linux), rather than a real timestamp or the honest `unknown` fallback. I am downgrading this from Codex's own P2/major rating (see ledger): it is a best-effort diagnostic field only, no acceptance criterion inspects its content (**AC5**'s stderr assertion checks only for the lock path string), it carries no security or correctness consequence, and the remedy the message exists to convey (the lock path plus the `rmdir` command) is printed correctly regardless. Worth a one-line fix (capture the BSD probe's own exit status and only use its output on success) but not blocking.
  - Suggested fix: `local m; m="$(stat -f '%Sm' "$d" 2>/dev/null)" && { printf '%s' "$m"; return 0; }; stat -c '%y' "$d" 2>/dev/null && return 0; printf 'unknown'` (or equivalent) — capture-then-check rather than relying on `stat`'s own exit status to discriminate the two format dialects.
- `bin/log-run.sh:400-417` (`compute_auto_seq`'s widened extraction regex, re-examined this round) — the adversarial pass and I independently traced `jesc`'s own two-step transform (backslash-double, then quote-escape) against `(\\.|[^"\\])*` and found no defect: every stored escape shape `jesc` can actually emit (doubled backslashes, escaped quotes, any run of consecutive backslashes) is consumed correctly, and a malformed odd-length trailing-backslash JSON string is not a shape `jesc` can produce. This closes round 1's Major #1 cleanly. The one open item is coverage, not correctness: the new `seq-auto-escaping` regression covers `a\b`, `a"b`, `a\"b`, `a\\b` but not a run_id ending in two or three consecutive raw backslashes — worth pinning as a fast-follow, not a defect.
- `bin/log-run.sh:567-603` (the `trap '' INT TERM` masking, adversarial angle) — the mask is correct for the intended (local POSIX filesystem) case: `mkdir`/`rmdir` are each inside an `if`/guarded context with no early-exit path that could strand the mask, and clearing `LOCK_ACQUIRED` before `rmdir` creates no new window (a successor cannot see the path as free until `rmdir` itself completes, by which point the flag is already correctly cleared). The adversarial pass's one caveat — an external `mkdir`/`rmdir` that blocks indefinitely on an unhealthy NFS/FUSE mount would inherit `SIG_IGN` and become uninterruptible for that duration — is real but squarely inside the frozen spec's own declared boundary: `## Input space`'s first "Out-of-scope synthetic extremes" line names exactly this ("Cross-host or network-filesystem concurrency... Declined outright; the lock's correctness is claimed for a POSIX local filesystem only"), so I record it here for completeness rather than as a scored finding.

#### Adversarial findings
- Independently reproduced, by direct trace rather than by inspection of the primary pass's claim alone, the same root cause and the same "close to a coin flip" timing analysis for the `signal-race-release-side` test's P1-kill-before-P2-confirmed-acquisition ordering — this is what keeps that finding at Blocker rather than accepting it at a lower confidence level.
- Confirmed no defect in the widened `compute_auto_seq` regex against every escape shape `jesc` can actually emit; suggested (not required) additional boundary regression cases for multi-backslash run_ids.
- Surfaced the SIG_IGN-inheritance-on-a-blocking-filesystem-call angle on the trap masking, which traces to a declared out-of-scope boundary (NFS/FUSE) rather than a live defect against this task's actual Input space.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `signal-race-release-side` test kills P1 before confirming P2's reacquisition, making the OLD-shape theft assertion scheduling-dependent | primary: P1 (→ blocker per map) — adversarial: major | blocker | both passes agree on the mechanism; my own hand-traced timing analysis (two independent ~1-second poll cadences with no synchronization between them) shows this is a frequent, not rare, flip — high enough incidence on a CI-wired suite to keep at blocker rather than accept the adversarial pass's own lower major rating |
| 2 | AC10 negative-control token derivation has no explicit synchronization barrier, so a live re-run could in principle produce `not-detected` against a committed `detected` provenance record | primary: P2 (→ major per map) | major | kept at major (the soundness gap is real and the AC10 check line as written would genuinely fail on a flip); noted as low-probability in practice given the near-certain empirical reproduction under 8×20 unsynchronized concurrency and the frozen spec's own disclosed escalation path for the opposite (permanent not-detected) outcome — does not independently force this verdict, `signal-race-release-side` already does |
| 3 | `lock_mtime`'s BSD `stat -f '%Sm'` probe succeeds with a garbled value on GNU/Linux instead of falling through to the correct GNU form | primary: P2 (→ major per map) | minor | downgraded on my own judgment: diagnostic-only best-effort field, no acceptance criterion inspects its content, no security/correctness consequence, and the message's actionable part (lock path + `rmdir` remedy) is unaffected — real bug, low stakes |
| 4 | SIG_IGN inheritance on a blocking `mkdir`/`rmdir` (unhealthy NFS/FUSE) could leave the mask effectively indefinite | adversarial: minor | minor | no change; grounded in `## Input space`'s first "Out-of-scope synthetic extremes" line ("Cross-host or network-filesystem concurrency... Declined outright; the lock's correctness is claimed for a POSIX local filesystem only") — recorded for transparency, not scored down from anything since it was already minor |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1076 entry back to `READY_FOR_ENG` with these findings linked. Fix the `signal-race-release-side` test's P1-kill-before-P2-confirmed-acquisition ordering (the Blocker) — this is a test-harness soundness gap, not a production-code defect: round 1's actual Blocker (the `LOCK_ACQUIRED` signal races in `bin/log-run.sh` itself) is correctly closed, and I found no fault in the masking logic itself against this task's own declared Input space. Fix the AC10 negative-control barrier and the `lock_mtime` GNU/Linux probe in the same round since both are small, localized, and already fully diagnosed above.
- **Pre-commitment check (explicit, per this round's own instruction):** none of this round's findings land against the `--seq auto` component itself. Round 1's Major #1 (the `run_id` escaping/regex bug in `compute_auto_seq`) is confirmed closed this round — both the primary and the adversarial pass independently traced the widened regex against every escape shape `jesc` can emit and found no defect. This round's Blocker and Major are about the lock's signal-handling *test fixture* (`signal-race-release-side`) and the negative-control fixture's synchronization, respectively — neither is `--seq auto`'s own derivation or escaping logic. **The pre-commitment's drop-`--seq auto`-to-a-follow-up trigger is therefore not active**, and the orchestrator does not need to execute it after this round.
- No `## Input space` out-of-scope line covers findings 1–3 above; finding 4 is explicitly grounded in a declared out-of-scope line (see ledger) and is recorded rather than scored as a live defect.

#### Fast-follow disposition
- `compute_auto_seq`'s escaping regression (`tests/log-run/run.sh`'s `seq-auto-escaping` case) does not yet pin a run_id ending in two or three consecutive raw backslashes, even though no defect was found against that shape. Intended disposition: **file-an-issue** — cheap to add, but not required to close this round's findings, and bundling it into the current rework risks scope creep on a round already fixing a Blocker and a Major.
- The SIG_IGN-on-a-blocking-NFS/FUSE-mkdir observation (Minor/nits, ledger row 4) is grounded in a line the frozen spec already declares out of scope. Intended disposition: **won't-fix** — the spec's own Input space already makes this an accepted, disclosed limitation for a POSIX-local-filesystem-only lock; revisiting it would be re-litigating a frozen, closed design decision (D1) rather than responding to a live gap.

---

### Codex Review verdict: REQUEST_CHANGES (round 3)
- Task: T-1076
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 5 files changed, +271/-22 lines (`git diff 9f99927..0243072`, round 3's own bounded-rework delta — the orchestrator's loop-guard STOP record and QA's board commit are records, not engineer code, and sit outside this range)

Codex was invoked twice: a primary `codex exec review --base 9f99927 --json` pass (read-only, reading the working tree directly against the round-3 delta), and — triggered by that pass's own finding — an adversarial "play devil's advocate" pass targeting the three files/fixtures round 3 touched (the new `lock-mtime-legible` test, the reworked `signal-race-release-side` P2-mutant choreography, and the AC10 disclosure comment). Raw captures: `.shell-team/reviews/T-1076-codex-primary.{txt,jsonl}` and `T-1076-codex-adversarial.{txt,jsonl}` (canonical basenames, overwritten this round per the naming-consistency rule — a correction from round 2's own `-r2`-suffixed deviation, which I did not repeat and did not otherwise touch).

I read the full round-3 diff myself (`git diff 9f99927..0243072` for all five files), the orchestrator's loop-guard STOP record and its authorized scope, QA round 3's scope audit and independent tallies, and — before accepting Codex's single primary finding — independently confirmed it against three first-hand sources: the committed `lock-mtime-legible` assertion text (`tests/log-run/run.sh:480-485`), this repository's own CI workflow (`.github/workflows/check-handoff.yml` — `runs-on: ubuntu-latest`, and a `Run log-run resolution suite` step that runs `bash tests/log-run/run.sh` directly and unconditionally), and GNU coreutils `stat -c '%y'`'s well-documented ISO-style output shape (`YYYY-MM-DD HH:MM:SS.nnnnnnnnn +ZZZZ` — no month abbreviation, and the string ends in the timezone offset, not a 4-digit year). I could not execute GNU `stat` directly from this macOS sandbox (`gstat` is not installed here), so this is a static, first-principles trace of the assertion regexes against a documented, unambiguous output format, not a live-executed reproduction — recorded honestly per this round's own verification-mode line.

#### Blockers
- `tests/log-run/run.sh:480-485` (the new `lock-mtime-legible` regression, added this round to close round 2's Minor) — **This is a genuinely new defect, introduced by this round's own fix, and it will deterministically break this repository's CI.** `lock_mtime`'s round-3 rework (correctly) selects the GNU dialect branch on GNU/Linux via `stat -c '%y'`, whose output has no month abbreviation, no `HH:MM:SS` in the exact form the ISO string's fractional-seconds field can spoof but whose trailing content is a UTC offset rather than a bare 4-digit year, and whose year sits mid-string rather than at the end. All three of the new test's assertions (`grep -qE '(Jan|Feb|...)'`, the HH:MM:SS pattern, and `(19|20)[0-9]{2}$` anchored at end-of-string) were written and verified only against this session's own macOS/BSD host output; none of them hold against the GNU shape the repo's own `ubuntu-latest` CI runner will actually produce when this suite's `Run log-run resolution suite` step executes `bash tests/log-run/run.sh`. `fail()` calls `exit 1` under this suite's own `set -euo pipefail`, so this is not a soft warning — the whole CI job fails, on every push and on this PR's own next CI run, for code that is functioning exactly as the round-3 fix intended. The test's own header comment already discloses "only the BSD branch is exercised... from this repo's own dev host," but that disclosure is scoped to the code path (which dialect the local dev host takes), not to the test's OWN portability across the dialect the CI runner actually exercises — the gap is in the test, not in `lock_mtime` itself, which I re-read and find correct.
  - Suggested fix (Codex's, which I find sound and would add as the minimum bar): make the assertion dialect-aware — either detect which branch `lock_mtime` itself took (mirroring the same `stat --version` probe) and check the matching shape, or relax the check to accept either shape while still explicitly rejecting `unknown` and the specific garbled value (`4096m`) the round-2 finding named. The PASS message's own text ("selects the BSD form on this host") should also stop asserting a host-specific claim as if it were the suite's general execution bound.

#### Major
- `tests/log-run/run.sh:829-846` (the reworked `signal-race-release-side` choreography's fixed-width `sleep 8`/`sleep 12` margins, adversarial finding) — the round-3 fix correctly makes the *ordering* deterministic (the kill is now gated on P2's own acquired-marker rather than a race against P1's marker), which is what closes round 2's actual Blocker. What remains, and what the adversarial pass surfaced, is that the *widths* of the two artificial delay windows (P1's mutants' `sleep 8`, P2's own `sleep 12`) are still fixed wall-clock budgets, not states the parent shell confirms before acting: if the full pre-kill choreography (P1 acquire → release → P2 poll-acquire, each on ~1s polling cadences) is slow enough under CI contention to consume more than 8 of P1's 8 seconds before the kill is sent, `on_lock_signal` would see `LOCK_ACQUIRED` already reset to `0` and correctly do nothing — the OLD-shape mutant's own bug would not even be exercised in that run, and the test's own `survives=0` assertion for the OLD-shape arm would fail on scheduling grounds, on code (the OLD-shape mutant, deliberately) behaving as its own code dictates given a late signal. This is a materially different mechanism from round 2's Blocker (round 2 had *no* ordering guarantee at all — a near-coin-flip on two independent unsynchronized ~1s polls with zero margin; round 3 *does* now enforce ordering, and only a margin-under-extreme-load residual remains) — I record this distinction explicitly for the class-ledger's own honesty discipline rather than assert it is or is not "the same class" as round 2's; either reading is defensible and I leave the final call to whoever picks this up. In practice I judge this materially lower-probability than round 2's own defect (P2's real acquire-retry cadence is `sleep 1` per attempt in `bin/log-run.sh`'s own loop, so reacquisition after a near-instant `rmdir` should typically land within 1-3 poll cycles, leaving several seconds of margin) but not zero, and this task's own three-round history is now three-for-three on scheduling-dependent test defects in this exact fixture family, which I read as a signal about the fixed-sleep-margin design pattern itself rather than about this one instance.
  - Suggested fix (Codex's, which I agree is the more durable fix): replace the fixed-width sleep margins with parent-controlled rendezvous gates — hold P1 after its release marker and P2 after its acquired-marker on an explicit signal from the test driver, confirm both states are live, act, then release both — rather than sizing a sleep long enough to "comfortably outlast" a variable-duration setup phase.

#### Minor / nits
- `tests/log-run/run.sh:898-905` (the AC10 disclosure comment's wording, adversarial finding) — the comment is honest in substance (it correctly states the arm is empirical rather than structurally forced, and correctly points at AC10's own two-token grammar as the escalation path) but overclaims in a few words: "guarantees" in its own header framing, "near-certain... on any real multi-core host" (an unqualified universal claim from a sample of runs on one host), and the framing that a barrier would necessarily "restructur[e] the very TOCTOU mechanism" (a rendezvous-after-derive-before-append barrier is a plausible middle ground that would not need to touch the mechanism itself). None of this changes what the test actually does or its actual pass/fail behavior — it is a rigor-of-self-disclosure nit, in the same spirit as this task's own existing honest-disclosure norm (e.g., the GNU-branch-unverified note), not a new gap in that norm.
  - Suggested fix: soften to state only the measured evidence ("detected in every recorded run on this host, across N total observations; not established for other hosts or schedulers") and describe the omitted barrier as a scope choice for this bounded rework rather than a technical necessity.

#### Adversarial findings
- Independently reproduced the primary pass's `lock-mtime-legible` Blocker by direct trace of GNU `stat -c '%y'`'s documented output shape against the three committed regexes — confirms this at Blocker, not a lower confidence level.
- Surfaced the fixed-width-margin residual in the reworked `signal-race-release-side` choreography (see Major above) — a finding the primary `codex review` pass did not raise, since it required a devil's-advocate framing on how the parent process observes state versus how it waits.
- Surfaced the disclosure-comment wording overclaim (see Minor above).
- Did not find any remaining defect in `lock_mtime`'s own dialect-probe logic in `bin/log-run.sh` itself (the GNU/BSD branch selection is correct; the defect is entirely in the test's assertion shape, not the production code the round-2 Minor was about).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `lock-mtime-legible` test asserts a BSD-only timestamp shape; fails deterministically on the repo's own `ubuntu-latest` CI (GNU `stat -c '%y'`) | primary: P1 (→ blocker per map) — adversarial: blocker | blocker | both passes agree; my own independent trace of GNU `stat`'s documented output format against the three committed regexes confirms the mismatch is total (none of the three assertions can pass), so this is accepted at blocker without any downgrade |
| 2 | `signal-race-release-side`'s fixed `sleep 8`/`sleep 12` margins remain a wall-clock budget rather than a parent-confirmed state, leaving a lower-probability residual scheduling dependency | adversarial only (major, not raised by the primary pass) | major | accepted at major on my own judgment — the ordering defect round 2 actually forced is now closed (kill is gated on P2's own marker), so this is a narrower, lower-probability residual than round 2's defect, not a full recurrence of it; recorded as a Major rather than escalated to blocker because it requires an additional, larger scheduling delay than round 2's zero-margin case to manifest |
| 3 | AC10 disclosure comment overclaims ("guarantees," "near-certain... on any real multi-core host") relative to its own single-host evidence | adversarial only (minor) | minor | accepted as raised — wording-only, does not change the test's actual behavior or this round's pass/fail outcome |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1076 entry back to `READY_FOR_ENG` with these findings linked — **but per this round's own explicit instruction, this disposition itself now exceeds what an engineer rework round can self-authorize.** This round surfaces (a) a NEW Blocker-class finding — a fresh, unambiguously deterministic CI-breaking defect (`lock-mtime-legible`'s dialect-shape assumption) that did not exist before this round's own rework introduced it — and (b) a Major finding in the same fixture family (`signal-race-release-side`'s residual fixed-margin dependency) that is at minimum closely adjacent to, and arguably a partial recurrence of, round 2's ledgered `regression-test-timing-race` class (I record both readings above rather than force a single one). Either condition alone satisfies the loop-guard STOP record's own stated halt trigger ("A recurrence of any ledgered class after this pass, or a NEW Blocker-class finding in the next review round, exceeds what this session attests and halts to the operator") — **this task halts to the human operator rather than proceeding to a further bounded-rework round under this session's own standing authorization.**
- **Pre-commitment check (explicit, per this round's own instruction): no finding this round touches the `--seq auto` component** (`compute_auto_seq`, its escaping/derivation logic, or its regression coverage) — all three findings are in `lock_mtime`'s new test coverage and in test-fixture scaffolding around the signal-race and negative-control arms. The pre-commitment's drop-`--seq auto`-to-a-follow-up trigger remains **not active**; round 1's Major #1 on that component stays confirmed closed as of round 2 and is untouched by anything found this round.
- No `## Input space` out-of-scope line covers any of the three findings above — the repository's own `ubuntu-latest` CI runner is the shipped, in-scope execution environment (not a synthetic extreme), so the `out-of-input-space` downgrade rule does not apply to finding 1; findings 2 and 3 are test-fixture soundness/wording matters with no Input-space bearing either way.
- internal consistency verified — this verdict does not depend on reading any linked GitHub issue's canonical text; it rests entirely on this repository's own committed CI workflow file, the committed test source, and GNU coreutils' documented `stat` behavior, all read directly.

#### Fast-follow disposition
- The AC10 disclosure-comment wording overclaim (Minor, ledger row 3) does not itself gate this halt and could reasonably be folded into whatever rework round eventually addresses the Blocker in the same file family, or deferred. Intended disposition: **file-an-issue** — cheap, non-blocking wording tightening, but distinct from the two findings that force this round's halt, so it should not be allowed to expand the scope of whatever fix addresses the Blocker.
- Everything else in this review (the Blocker and the Major) gates this round's disposition directly and is not a fast-follow deferral.

---

### Codex Review verdict: REQUEST_CHANGES (round 4)
- Task: T-1076
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.147.0
- Verification mode: executed-tests
- Diff scope: 4 files changed, +295/-74 lines in `tests/log-run/run.sh` plus record-only churn in `.shell-team/provenance/T-1076.md` (+12), `.shell-team/test-recipe.md` (+89) and `.shell-team/todo.md` (+6/-1) — `git diff d4efd22..8a10090`, this round's own surgical rework delta. `bin/log-run.sh` re-confirmed byte-unchanged since the operator's ruling (`git diff d4efd22..HEAD -- bin/log-run.sh | wc -l` = 0), independently of the engineer's and QA's own identical claim.

This round is the operator-ratified "one surgical round" fixing round 3's Blocker (`lock-mtime-legible` BSD-only assertions), Major (`signal-race-release-side` fixed-width `sleep 8`/`sleep 12` budgets) and Minor (AC10 disclosure wording). I read the frozen spec's full `## Input space`/pre-commitment text, my own rounds 1–3 above, the round-3 HALT and operator-ruling board entries, the full round-4 diff (`git diff d4efd22..8a10090` for every changed file), all 4 new provenance decision entries, and the round-4 QA record in full (including its two disclosed risk notes) before invoking Codex. Codex was invoked twice: a primary `codex exec review --base d4efd22 --json` pass (read-only, reading the working tree directly against this round's own delta), and — triggered by that pass's own finding — an adversarial "play devil's advocate" pass targeting the reworked `signal-race-release-side` choreography and the rewritten `lock-mtime-legible` assertion pair. Raw captures: `.shell-team/reviews/T-1076-codex-primary.{txt,jsonl}` and `T-1076-codex-adversarial.{txt,jsonl}` (canonical basenames, unchanged from round 3).

**Beyond reading, I directly executed reproductions of the shipped mutation technique myself** (this round's verification mode is `executed-tests`, not `static-only`), because Codex's primary pass raised a timing claim about the reworked `signal-race-release-side` fixture that neither engineer nor QA's own hand-off disclosed as a defect, and a claim about process-exit latency is exactly the kind of thing static reading alone can get wrong in either direction:
1. Extracted the real, shipped `bin/log-run.sh` and applied only the `REL_START`/`REL_END` OLD-shape replacement the test itself performs (same `replace_range` awk logic, copied verbatim), then ran the resulting OLD-shape mutant, sent it `SIGTERM` once it reached its own post-`rmdir` marker (mirroring `relrace_choreograph`'s own pre-kill synchronization), and timed the interval from `kill -TERM` to `wait $pid` returning, first with a scaled-down analog (a 5-iteration cap: confirmed `rc=143`, `elapsed=6s`, with stderr proving the reentrant nested `release_lock` call), then against the **real production `bin/log-run.sh` with only the release-side block mutated** (a 10-iteration cap, to bound the check's own wall-clock cost): confirmed `p1_rc=143`, **exactly 10 seconds elapsed between the kill and the process actually exiting** — not "essentially instant."

#### Blockers
- None. Nothing in this round breaks CI, fails any acceptance criterion, or produces an incorrect verdict on either arm of any fixture.

#### Major
- `tests/log-run/run.sh:967-982` (`relrace_choreograph`'s `expect_rc`-gated `p1proceed` write) and `tests/log-run/run.sh:804-812`/`819-828` (`REL_OLD_REPL`/`REL_FIXED_REPL`'s marker-wait loops) — **the OLD-shape arm's kill-to-exit latency is not "essentially instant" as the round's own shipped commentary claims three separate times (the code comment at `:972-978`, the provenance decision entry, and the test-recipe entry); it is unconditionally the full 60-iteration anti-hang cap (~60 wall-clock seconds), every single run.** Mechanism, confirmed by direct execution against the real code (not merely by reading): the OLD-shape mutant's `release_lock` fires once naturally (via `trap release_lock EXIT`, after the row is written), enters its own marker-wait loop unmasked — this is the *outer* call. The test's own `kill -TERM` lands while that outer call is suspended inside this loop's `sleep 1`; because OLD-shape deliberately never masks INT/TERM (that is the whole point of the fixture — reproducing the pre-round-2 vulnerable shape), `on_lock_signal` fires and calls `release_lock` a *second*, re-entrant time while `LOCK_ACQUIRED` is still `1`. This re-entrant call performs the genuine theft `rmdir` (this part is correct and is decided within about a second of the kill, which is why the "successor survives=0" assertion itself is NOT flipped by any of this), writes its own copy of the probe marker, and then **also** enters the identical wait loop for `rel-proceed-marker` — the very marker `relrace_choreograph` deliberately never writes for `expect_rc != 0` (the OLD-shape call). That re-entrant wait loop therefore always runs its full 60 iterations before `LOCK_ACQUIRED=0` is set and `on_lock_signal` can finally call `exit 143`, which is the only thing that lets the driver's `wait "$p1_pid"` return. The code comment's own claim — "the kill above is delivered essentially instantly and the process is gone... well before any marker file could matter" — is therefore false for the one case (`expect_rc != 0`) it is specifically talking about. This directly, quantitatively explains a fact neither engineer nor QA diagnosed: the engineer's own reported 5-consecutive-iteration tally for this fixture (~82s/iteration average) and QA's own independent tally (~76-85s/iteration) are both consistent with "a healthy few seconds of real choreography plus one unconditional ~60s stall," not with the "no finite width can be exhausted... does not flip any assertion's verdict" self-description the round's own commentary asserts at `:918-922`.
  - The adversarial Codex pass independently reproduced this same root cause and additionally raised a related, lower-confidence concern I could not confirm as exploitable under the current numbers: P2's own independent 60-iteration cap on `p2proceed` (`tests/log-run/run.sh:900`) starts counting from *before* the kill is even sent (P2 enters its own wait loop as soon as it re-acquires the freed path, which the driver confirms via `p2marker` *before* signalling P1), so in principle P2 could self-time-out and cleanly release its own lock before the driver ever writes `p2proceed`. Tracing the actual relative timing: the theft `rmdir` is decided within ~1s of the kill (long before either 60s cap could expire), so under the numbers as shipped this does not currently flip the `survives` assertion — the directory is already absent (from the genuine theft) well before P2's own cap could matter. I record this as a *disclosed, unreproduced* secondary risk rather than an independently-confirmed defect: it shows the "two independent unsynchronized timeout-driven caps" pattern from rounds 2–3 is still latent in this fixture's design, one step removed from actually mattering rather than eliminated.
  - Suggested fix: for the `expect_rc != 0` (OLD-shape) call, either write `p1proceed` immediately as well (since OLD-shape's own signal handler does not check it before its own exit path — it is only the *re-entrant* call that reads it) or, more precisely, distinguish the *first* (natural, non-signal) invocation of `release_lock` from a *re-entrant* (signal-handler-driven) invocation and skip the marker-wait entirely on re-entry, since a re-entrant call exists specifically to finish and let the process die, not to hold a window open. Either change removes the ~60s tax without reintroducing round 2's original ordering defect. Correct the three shipped claims ("essentially instantly," "not sized to budget any correctness-relevant transition," "does not flip any assertion's verdict") to describe what actually happens, or fix the mechanism so the claims become true.
- Class lineage, stated explicitly per this round's own instruction: **this is a recurrence of a ledgered class**, though not an identical mechanism to the ones ledgered before it. Round 2's ledgered Blocker in this exact fixture was a pure *ordering* defect (no synchronization at all between the kill and P2's reacquisition); round 3's ledgered Major in this exact fixture was *fixed-sleep-margin wall-clock budgets* (`sleep 8`/`sleep 12`) that could be exhausted under contention. Round 3's own synthesis explicitly read the pattern across those two rounds as "a signal about the fixed-sleep-margin design pattern itself, not about any one instance." This round's fix genuinely removed every literal `sleep N` from this fixture — but the underlying pattern (a wall-clock-bounded assumption baked into this exact fixture family, described in the shipped commentary as non-load-bearing when it is not) recurs in a new, more absolute form: not "exhausted under contention" (probabilistic) but "always exhausted for this one arm, unconditionally" (deterministic). This is the fourth consecutive round in which `signal-race-release-side` draws a timing-adjacent finding. I read this as satisfying the operator's own stated halt trigger ("a recurrence of a ledgered class... halts to the operator again") at the level of the pattern, even though the concrete mechanism differs from any single prior instance and even though — unlike rounds 2 and 3 — nothing here is a correctness-flip on the shipped assertions. I state this explicitly rather than deciding it myself, mirroring this task's own round-3 precedent of leaving a class-lineage judgment call to whoever picks this disposition up next.

#### Minor / nits
- `tests/log-run/run.sh:500` (the rewritten `lock-mtime-legible` HH:MM:SS + `(19|20)[0-9]{2}` pair, unanchored at both ends) — both the adversarial Codex pass and QA's own round-4 disclosure independently found the same acceptance hole: a constructed string mixing an unrelated colon-time and a year substring (Codex's own examples: `"garbage 2024 99:99:99"`, `"12024 x1:00:00x"`; QA's own: `"error 12:00:00 occurred in year 2024 during scan"`) is wrongly accepted, because the pair is not a timestamp grammar, only a "these two shapes appear somewhere in the string" test. I concur with QA's own judgment (independently reached, not merely deferred to): `lock_mtime()` itself can only ever emit genuine GNU `stat -c '%y'` output, genuine BSD `stat -f '%Sm'` output, or the literal string `unknown` — it has no code path that could construct a prose-wrapped diagnostic sentence, so this hole has no real trigger anywhere this function is actually called. Accepted as a real but non-exploitable test-strictness gap, matching Codex's own "medium" rating (→ minor under this review's normalization map).
  - Suggested fix (already offered independently by QA and Codex, converging on the same idea): either assert the exact GNU/BSD shape per-dialect rather than an unanchored pair, or accept the current pair's simplicity as an intentional trade-off and record that trade-off explicitly in the test's own comment (which currently reads as a stronger legibility grammar than it is).
- `.shell-team/reviews/T-1076.md`'s round-4 QA record itself notes it could not locate the engineer's own documented record of a "disabling `p1proceed` → FAIL" mutation self-check in provenance or test-recipe, and separately found that self-check racy for reasons unrelated to my own finding above (P2's `-lt 60` backstop can race P1's own disabled-marker 60s backstop in a *further*, QA-authored mutation, not in the shipped code). This is QA due-diligence tooling, not a shipped artifact — I read it, concur with QA's own "hypothetical-future-regression, not gating" disposition, and take no further action on it.

#### Adversarial findings
- Independently reproduced the exact same root cause as my own direct execution for the OLD-shape ~60s stall (see Major above), and additionally surfaced the P2-self-timeout secondary risk, which I traced but could not confirm as currently exploitable given the shipped relative timing (theft is decided within ~1s of the kill, well before either 60s cap could expire).
- Independently reproduced the `lock-mtime-legible` unanchored-pair prose-collision acceptance hole (see Minor above), matching QA's own disclosed risk note with different example strings, which strengthens confidence this is a real (if non-exploitable) gap rather than an artifact of one particular constructed input.
- Did not find any defect in the AC10 disclosure wording rewrite, nor in the `lock-mtime-legible` pair's behavior against either real dialect's genuine output (only against constructed, non-emittable strings).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | OLD-shape arm's kill-to-exit latency is an unconditional ~60s stall via a re-entrant nested wait on a marker that is deliberately never written for that arm, contradicting the round's own "essentially instant" / "not correctness-relevant" claims in three shipped artifacts | primary: P2 (→ major per map) — adversarial: blocker | major | I directly executed both a scaled-down analog and a full reproduction against the real, shipped `bin/log-run.sh` with only the release-side block mutated (10-iteration cap: confirmed exactly 10s elapsed post-kill before exit), confirming the mechanism beyond doubt. Kept at major rather than escalated to the adversarial pass's own blocker rating because nothing here flips any assertion's actual pass/fail outcome, breaks CI, or fails any acceptance criterion — the theft `rmdir` itself is still decided within ~1s of the kill; the defect is a false self-description plus a real, undisclosed ~60s-per-run cost, not a correctness break |
| 2 | P2's own independent 60-iteration cap on `p2proceed` starts before the kill is sent, so it could in principle self-time-out and cause the `survives` check to read an absence for a reason unrelated to genuine theft | adversarial only (folded into its blocker-rated finding) | major (disclosed, unreproduced, not separately scored) | traced the actual relative timing myself: P1's re-entrant wait always starts several seconds after P2's own wait began, so P1's cap will nearly always still be running when P2's would otherwise expire, and the theft `rmdir` (which is what `survives` actually measures) is already decided within ~1s of the kill — long before either cap matters. Recorded as a disclosed secondary risk under finding 1's own Major rather than a second independently-confirmed defect |
| 3 | `lock-mtime-legible`'s unanchored time+year pair accepts constructed prose strings that mix an unrelated colon-time and year substring | adversarial: medium (→ minor per map) — also independently disclosed by QA's own round-4 record with different example strings | minor | accepted at minor; both Codex and QA agree the hole is real but `lock_mtime()` itself has no code path that could emit such a string, so it has no live trigger — a test-strictness gap, not an exploitable defect |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1076 entry back to `READY_FOR_ENG` with this round's Major finding linked. **This is not the operator-ratified surgical round's own scope re-opening on the same three findings** — round 3's Blocker, Major and Minor are all genuinely, correctly closed this round (the `lock-mtime-legible` GNU-shape fix is solid, confirmed by two independently-built stubs with different sample values against the real `lock_mtime()` function; the AC10 wording fix is a clean, verified wording-only change). This is a **new** defect introduced by the fix for round 3's Major, in the same file, in the same fixture, in the same review pass that closed that Major.
- **Halt-trigger flag (explicit, per this round's own instruction):** I judge this Major to be a recurrence, at the pattern level, of the ledgered "fixed-sleep-margin design pattern" class from round 3's own synthesis — see the class-lineage discussion under Major above. Per the operator's own standing ruling ("a recurrence of a ledgered class after this round... halts to the operator again"), I flag this as very likely meeting that trigger, which would mean a further rework round on this specific fixture is not self-authorized under the existing GO and should return to the operator rather than proceeding as an ordinary bounded rework — but the actual halt determination and its mechanics are the orchestrator's to make against the loop-guard's own record, which I do not have authoritative access to from this review. I state the flag; I do not execute the halt.
- **`--seq auto` is untouched by this round's finding.** The Major and the Minor above are both entirely within `tests/log-run/run.sh`'s `signal-race-release-side` and `lock-mtime-legible` fixtures; neither touches `compute_auto_seq`, its escaping/derivation logic, or its regression coverage. The frozen pre-commitment's two-consecutive-rounds trigger on that component remains **not active** — round 1's Major #1 on `--seq auto` stays confirmed closed as of round 2, untouched by anything in round 3 or this round.
- No `## Input space` out-of-scope line applies to either finding above: both are about the accuracy and soundness of the test harness's own self-description and assertion strictness, not about a production input class this spec's Input space declares in or out of scope, so the `out-of-input-space` downgrade rule has no bearing here either way.
- internal consistency verified — this verdict does not depend on reading any linked GitHub issue's canonical text; it rests entirely on this repository's own committed test source (read directly, and directly executed in a controlled reproduction), the frozen spec, and this task's own prior review/provenance/board records, all read first-hand.

#### Fast-follow disposition
- The `lock-mtime-legible` unanchored-pair prose-collision acceptance hole (Minor, ledger row 3) does not gate this round's disposition — it is real but non-exploitable given `lock_mtime()`'s own restricted output space, and would be reasonable to fold into whichever round eventually addresses the Major above, in the same file. Intended disposition: **file-an-issue** — cheap to tighten (either dialect-aware assertions or an explicit comment narrowing the claimed grammar), but distinct from the Major that forces this round's disposition, so it should not expand that fix's own scope.
- The QA-authored mutation self-check's own raciness (Minor/nits, not separately ledgered) is a due-diligence tooling artifact, not a shipped defect. Intended disposition: **won't-fix** — nothing in `tests/log-run/run.sh` itself needs to change to resolve a raciness in a probe that was never shipped as part of the suite.

---

### Codex Review verdict: REQUEST_CHANGES (round 5)
- Task: T-1076
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.147.0
- Verification mode: executed-tests
- Diff scope: round 5's own delta `git diff d726e80..e5afdce` — 4 files, +275/-278 lines (`tests/log-run/run.sh` +182/-288 net across 470 changed lines; `.shell-team/provenance/T-1076.md` +6; `.shell-team/test-recipe.md` +71; `.shell-team/todo.md` +6/-1). Whole-task scope `git diff a78a8e2...HEAD`: 10 files, +1973/-43. `bin/log-run.sh` independently re-confirmed byte-unchanged since round 4's QA pass (`git diff 11f8f7e..3f23e64 -- bin/log-run.sh` empty), matching the engineer's, orchestrator's, and QA's own identical claim — a fourth independent confirmation.

Read the operator's "b GO" ruling (board record `d726e80`), the frozen spec's Input space, my own rounds 1-4 above (in particular round 4's Major and the three false claims it found), both round-5 provenance decision entries, the round-5 test-recipe entries (including QA's disclosed risk notes), the full `tests/log-run/run.sh` round-5 diff line-by-line, and re-read `bin/log-run.sh:565-640` (the real acquire/release/signal-handler code) directly, before invoking Codex. Codex was invoked twice: a primary `codex exec review --base d726e80 --json` pass (read-only, reading the working tree directly against this round's own delta), and — triggered by that pass's own Major finding — an adversarial "play devil's advocate" pass targeting the new `signal-mask-shape-pin` test and the retained FIXED-shape fixtures specifically. Raw captures: `.shell-team/reviews/T-1076-codex-primary.{txt,jsonl}` and `T-1076-codex-adversarial.{txt,jsonl}` (canonical basenames, unchanged from rounds 3-4).

**What round 5 did right, confirmed by direct reading, not merely by trusting the hand-off:**
- The Major (60s stall) is resolved by removal, not patch: `git diff d726e80..e5afdce -- tests/log-run/run.sh` shows the entire OLD-shape mutant construction, its A/B assertions, and the re-entrant-release choreography gone; no trace of the retired code remains (confirmed `grep -n "ACQ_OLD\|REL_OLD\|expect_rc\|expect_survive"` against the current file returns nothing).
- All three false claims are corrected, independently verified: `grep -n "essentially instant\|NOT sized to budget\|does not flip any assertion" tests/log-run/run.sh` returns no matches (claim (a), the code comment, disappeared with the retired code, exactly as the hand-off states); the provenance and test-recipe diffs each show a new, clearly-labeled round-5 entry correcting the round-4 entry rather than editing it in place — the append-only discipline is honored.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1076.md` (16 decision entries, 0 sentinel), `bash bin/check-intent.sh` (aligned, hash unchanged `78064c1e...`), and `shellcheck tests/log-run/run.sh` (clean, 0.11.0) all independently re-run and match QA's own reported numbers exactly.
- `signal-mask-shape-pin`'s own arithmetic checks out against the real `bin/log-run.sh`: 2 masked `trap '' INT TERM` lines (599, 630), 1 `trap release_lock EXIT` (607), 3 INT-arming and 3 TERM-arming lines each (604/620/634, 605/621/635) — the counts the pin asserts are exactly the counts the real file carries.

#### Blockers
- None.

#### Major
- `tests/log-run/run.sh:921-935` (the new `signal-mask-shape-pin`, and by extension the retained `signal-race-acquire-side`/`signal-race-release-side` fixtures at `tests/log-run/run.sh:711-870` that this round frames as sharing the ongoing-protection role) — **the pin verifies presence and global counts of the masking/re-arm/exit statements, never their position or order relative to `mkdir`, `LOCK_ACQUIRED`, and `rmdir`.** Confirmed by direct reading of both `bin/log-run.sh` and the pin's own logic: `SMP_MASK_COUNT`/`SMP_EXIT_TRAP_COUNT`/`SMP_INT_ARM`/`SMP_TERM_ARM` are plain `grep -c` tallies over the whole file. A future edit that silently *reorders* these exact lines — for example moving the acquire-side `trap '' INT TERM` (line 630) to after `LOCK_ACQUIRED=1` (line 632) instead of before `mkdir` — reintroduces precisely the acquire-side orphan race rounds 1-2 fixed, while leaving every count the pin checks unchanged; the pin would keep passing. This is not merely a theoretical reading of the pin in isolation: the retained dynamic fixtures do not close the gap either. `ACQ_REPL`/`REL_REPL` (`tests/log-run/run.sh:729-741`, `784-799`) are **hardcoded, known-good replacement bodies** written by the test itself and substituted via `replace_range` at the anchored range — they prove "if the code between these anchors looks like this hardcoded text (plus an injected sleep), a real `kill -TERM` is handled correctly," never "the real file's code between these anchors currently has this shape." This has been true of the FIXED-shape mutant since round 2 (the pre-round-5 `ACQ_FIXED_REPL`/`REL_FIXED_REPL` were the identical hardcoded-body technique, just renamed this round) — round 5 did not introduce this substitution pattern, but round 5 is the round that newly retires the OLD-vs-FIXED comparison and explicitly reframes this combination (FIXED-shape tests + shape pin) as **"equivalent forward protection... without needing to construct or run a mutant at all"** (`.shell-team/provenance/T-1076.md`, round-5 decision entry). That framing overstates what is actually delivered: it is equivalent protection against the masking being *removed* (which the pin's mutation self-check this round genuinely confirms), but not against it being *reordered* — a materially different failure mode that reintroduces the identical high-impact race this whole task exists to close, and that neither the pin's counts nor the substitution-based dynamic fixtures would catch. I checked for any other mechanical backstop that might close this specific gap: `lock-released-on-signal` (`tests/log-run/run.sh:571-604`, pre-existing, unmodified this round) does execute the real, unmutated file, but injects its sleep at the critical-section marker — well outside the narrow acquire/release masked windows — so it validates the general "killed while holding the lock" path via `on_lock_signal`/the EXIT trap, not the specific transition ordering the pin exists to protect; `shellcheck` and `bin/check-acs.sh` do not check statement order either. Both the primary review pass and an independent adversarial "play devil's advocate" pass (targeting this exact test and asked explicitly to try to break or confirm the claim) reached the same conclusion by direct trace against the real file, at the same severity.
  - Suggested fix (both passes converge on the same shape): strengthen the pin to check statement *order*, not just presence/count — e.g., extract each guarded transition's own line range (the same anchor technique already used elsewhere in this file) and assert the mask line, the guarded statement(s), and the re-arm lines appear in that relative order within the extracted range, rather than grepping the whole file for each independently. Separately, consider rewriting the dynamic fixtures to inject only the sleep/marker into the *actually-extracted* production body (as `lock-released-on-signal` already does) rather than substituting a hardcoded replacement body, so they exercise production's real current shape rather than an assumed-correct stand-in for it. Neither fix requires reintroducing the retired OLD-shape mutant construction or its timing-sensitive choreography.
- Class lineage, stated explicitly per this task's own convention: **this is a NEW finding class, not a recurrence of the ledgered timing/width-budget class** that drew a finding in each of rounds 1-4 (signal races, scheduling-dependent kills, fixed-sleep budgets, the marker-wait stall). Nothing about this finding depends on timing, scheduling, or wall-clock budgets — it is a static coverage-adequacy gap in a grep-level check and in a longstanding (since round 2) test-substitution pattern. It also does not recur in the specific mechanism of any prior finding (rounds 1-2's orphan/theft races, round 3's fixed-sleep margins, round 4's re-entrant-wait stall) — it is about what the *replacement* regression protection can and cannot detect going forward, a question this task had not previously been asked to answer because the retired OLD-vs-FIXED comparison (whatever its own timing fragility) at least demonstrated the vulnerable shape and the fixed shape behave differently, whereas the new pin+substitution combination never reads production's real order at all. I flag this distinction explicitly so the disposition below is not miscategorized as "the fifth consecutive timing finding."

#### Minor / nits
- `tests/log-run/run.sh:921-922` (the pin's literal `grep -Fx` matching) — QA's own disclosed risk note, which I concur with: a semantically-inert reformat of the matched lines (trailing whitespace, or `TERM INT` argument-order swap in the trap install lines) would falsely trip the pin even though production is unchanged in substance. This is the opposite direction from the Major above (a false positive, fail-closed, immediately visible as a broken CI run) rather than a false negative (silent, undetected) — QA's "non-gating" judgment is correct and I adopt it unchanged.
- `.shell-team/provenance/T-1076.md`'s round-5 decision entry's framing ("equivalent forward protection... without needing to construct or run a mutant at all") is narrower in fact than in wording — see the Major above for the substance; noted here only as the wording half of the same issue, since the underlying gap is already captured as a Major and I do not want to double-count it.

#### Adversarial findings
- Independently reproduced the Major above via a dedicated adversarial pass instructed to try to break or confirm the claim as harshly as possible: confirmed the claim accurate against the actual current file contents, confirmed no other test in the file or check in the repo (contention fixture, `lock-released-on-signal`, AC7, `check-acs.sh`, shellcheck, CI) would catch a silent reordering of these specific statements, and independently rated it Major for the same reason (real forward-looking gap, not a current production defect, given `bin/log-run.sh` is unchanged and rounds 1-4 already reproduced the present code correct many times over).
- Did not surface any defect beyond the Major above; found no problem with the `signal-mask-shape-pin`'s count arithmetic itself (all four counts are correct against the real file), the retirement's completeness (no OLD-shape residue), or the three-false-claims correction.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `signal-mask-shape-pin` (and the retained FIXED-shape fixtures' hardcoded-substitution technique) verify presence/count, never order, so a silent reordering of the masking statements would reintroduce the original race undetected | primary: P2 (→ major per map) — adversarial: major, independently confirmed | major | no severity change; both passes and my own direct reading of `bin/log-run.sh` agree the mechanism is real, current production is unaffected (byte-unchanged, extensively validated rounds 1-4), and no other repository check closes the gap — accepted as raised |
| 2 | pin's literal `grep -Fx` matching false-trips on semantically-inert reformats (QA's own disclosed risk note) | QA-disclosed, not separately scored by either Codex pass | minor | no change — QA's own "non-gating, fail-closed, narrow" judgment independently concurred with; opposite-direction risk (false positive, not silent miss) from finding 1 |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1076 entry back to `READY_FOR_ENG` with the Major above linked. **This does not reopen anything round 5 was actually asked to fix** — the retirement is complete and correct, all three false claims are genuinely corrected, and the design-premise change itself (per the operator's "b GO" ruling) is sound. This is a **new** finding about the adequacy of the *replacement* regression-protection mechanism round 5 introduces, in the same file, surfaced by the review round that first evaluates it.
- **Halt-trigger determination (explicit, per this round's own instruction): this finding does NOT satisfy the operator's stated halt condition.** It is Major, not a NEW Blocker. It is also not a recurrence of the ledgered timing/width-budget class that produced four consecutive prior findings in this fixture family — it depends on no timing, scheduling, or wall-clock budget at all, and it is not the same mechanism as any of rounds 1-4's findings (see Class lineage above). Per the operator's own ruling ("A further NEW Blocker or ledgered-class recurrence halts to the operator again"), a new, non-ledgered Major does not require a further HALT — this is an ordinary bounded engineer-rework disposition, not a third escalation to the operator.
- **`--seq auto` is untouched by this finding.** The Major above is entirely about `signal-mask-shape-pin`'s and the retained fixtures' coverage of the acquire/release signal-masking transitions; it has no bearing on `compute_auto_seq`, its escaping/derivation logic, or its regression coverage. The pre-commitment's two-consecutive-rounds trigger on that component remains **not active**.
- No `## Input space` out-of-scope line applies: this finding is about test-coverage adequacy for a future maintenance regression, not about any reachable-or-declined production input class, so the `out-of-input-space` downgrade rule has no bearing here either way.
- internal consistency verified — this verdict does not depend on reading any linked GitHub issue's canonical text; it rests entirely on this repository's own committed test source and production code (read directly and independently re-executed against real checkers), the frozen spec, and this task's own prior review/provenance/board records.

#### Fast-follow disposition
- no fast-follow deferrals — the Major above gates this round's disposition directly and is not deferred; the Minor (QA's disclosed false-positive risk) was already dispositioned non-gating by QA in round 5 and I concur without adding a new deferral of my own.
