# T-1086 QA review

<!-- QA verification record, round 1 -->

## Verdict: PASS → READY_FOR_REVIEW

Full mechanical sweep re-run live (own numbers, not accepted from the hand-off); both gate checkers re-run; the deliverable exercised empirically in fresh scratch venues (not a re-read of the engineer's own claims); the fan-out audit's classification independently spot-checked; the two flagged provenance judgment calls read against the note's own bytes; the mandatory `## Summarized sources` sweep performed against the actual source bytes, one finding disclosed below.

## Mechanical sweep (own run)

- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1086-subject-harness.md 2>&1 | tail -3` → `check-acs: 20 passed, 0 failed, 1 skipped, 0 unrecognized` (AC21 SKIP by design, matches the engineer's own round-2 report).
- `bash bin/check-intent.sh .shell-team/specs/T-1086-subject-harness.md .shell-team/todo.md` → `aligned: T-1086 v1 (ffaae699e173103c519f126c6cff0601a8315e52)`.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1086.md` → `conformant: ... (7 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1086 -- .shell-team/interventions/T-1086.md` → `conformant: ... (0 entries, 1 sentinel)`.
- `bash bin/check-pii-shapes.sh --base $(git merge-base chore/lessons-2026-08-20 HEAD)` → `clean`.
- `shellcheck docs/loop-engineering/subjects/subject-01/acceptance.sh docs/loop-engineering/subjects/subject-01/regenerate.sh` → exit 0, no output.
- `grep -c '^        run: shellcheck ' .github/workflows/check-handoff.yml` → `1`, and the single line names both harness scripts (confirmed by reading it directly).

## Empirical exercise of the deliverable (own venues, own runs)

- **AC5 substance, own reproduction.** Regenerated twice into two fresh `mktemp` directories; `cksum`-plus-path listings, `LC_ALL=C`-sorted, `cmp`-ed byte-identical. Confirmed deterministic regeneration independently of the `- check:` line.
- **AC6 substance, own reproduction.** `bash acceptance.sh <fresh-venue>` → 7 cases, `acceptance: 0 passed, 7 failed`, exit 1 — non-vacuous, matches the required ≥5-case floor and ≥1-fail floor.
- **Oracle-satisfiable, independently re-derived (not trusted from the engineer's or the arm's own self-check).** Wrote a fresh, never-committed reference implementation of `csvstats` from scratch, reading only `interface.md`'s frozen contract — never the engineer's own throwaway implementation, never the note's prose. Scored `acceptance: 7 passed, 0 failed`, exit 0 against the committed oracle. This is a second, wholly independent confirmation that the subject is genuinely satisfiable, on top of the two real reps' own 7/7 results.
- **Input space class 3 (incomplete candidate tree).** Built a partial candidate implementation (handles only the missing-file case). Oracle reported `1 passed, 6 failed`, exit 1 — failing cases reported cleanly, no crash.
- **Input space class 4 (extra files beyond the manifest).** Regenerated a venue, added a stray directory and a stray temp file. Oracle still reported the expected `0 passed, 7 failed` — unaffected by extra content, as the manifest's own design requires.
- **Boundary/adversarial fixtures on the driver and oracle themselves (adversarial fixture synthesis checklist, class 1).** `regenerate.sh` with no argument → usage message, exit 2. `acceptance.sh` with no argument → usage message, exit 2. `regenerate.sh` into a directory that does not yet exist → created it and populated it, exit 0. `regenerate.sh` into a non-empty pre-existing directory (containing an unrelated stray file) → added the four harness-copy files alongside the stray file without disturbing it, exit 0 — no destructive overwrite of pre-existing content.
- **AC9 self-containment, own re-derivation.** `git ls-files -- docs/loop-engineering/subjects/subject-01` lists exactly the five required files; a literal sweep of both scripts for `.shell-team`, `docs/loop-engineering` and a `bin/`/`tests/`-rooted path reference (the criterion's own anchored regex) returns zero for all three, over both files.

Test-harness short-circuit check (adversarial fixture synthesis checklist, class 4): every command above was run exactly as documented (`bash <path>`, the design's own stated invocation form — no executable-bit-based invocation applies here since the Non-goals explicitly waive that requirement, and no bare-PATH-name invocation applies either, since these are `docs/`-tree scripts never intended to be reached that way).

## Fan-out interpretation audit

- Aggregation summary re-confirmed: `grep -c '^- verdict: ' .shell-team/runs/fanout-t1086qa.aggregation` → `1509`; `grep -c '^- sentinel: ' ...` → `3`; `grep '^- summary:' ...` → `units: 92 — verdicts: 1509 — sentinels: 3`. Matches the stated summary.
- **T-1086's own 5 clone-side fails (AC2/8/17/19/20), spot-checked resolved.** All five are base-ref-reading criteria; the real checkout's own full sweep (above) shows all 20 criteria green — confirming these five resolve once the pinned clone's two-arm discriminator has the local branch state the real checkout has. No further action needed.
- **T-1001, own uncontended re-run in the real checkout:** `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` → 12 failed (`AC2 AC3 AC6 AC10 AC13 AC14 AC16 AC18 AC21 AC23 AC25 AC26`) — matches the orchestrator's own uncontended-re-run baseline (12) exactly. Confirms the fan-out's 13th fail is a contention artifact, as classified.
- **T-1002, own uncontended re-run in the real checkout:** same command against `T-1002-intervention-capture-channel.md` → 4 failed (`AC15 AC23 AC26 AC27`), one fewer than the orchestrator's own cited clone-baseline of 5 (`AC15/23/26/27/28`). AC28's own check body calls `bash bin/check-board-headings.sh ... --base develop`; a plain `git clone` (unlike a `git worktree add`, which shares refs with the real checkout) does not carry a local `develop` branch ref unless one is explicitly fetched, so AC28 fails closed in *any* clone venue of this class independent of content — the same "missing local branch refs" property already named for this venue class. This is a refinement of the already-disclosed venue-artifact class, not a new defect, and does not change the 8-vs-11 contention-artifact classification for AC9–14.
- **T-1051, root-caused, not merely re-observed.** Real checkout: 4 failed (`AC7 AC12 AC14 AC16`). Base ref, read via a fresh `git worktree add --detach` at the branch point: identical, 4 failed (`AC7 AC12 AC14 AC16`) — confirming these four are pre-existing at the branch point, unrelated to this task's diff. The prior firing's own aggregation (`fanout-t1085fan.aggregation`) recorded the same spec at `check-acs: 12 passed, 4 failed` — matching the worktree-based figure, not the clone-based one. AC6's own check body calls `bash bin/check-pii-shapes.sh --base develop`, and a `git worktree add` venue (used by the engineer's own Blast radius sweep and by the T-1085 fan-out) shares the real checkout's local refs including `develop`, while a plain `git clone` (used by this QA fan-out) does not carry `develop` as a local ref. This fully explains the 4→5 delta as a **venue-mechanism artifact** (clone vs. worktree), not a genuine regression and not something T-1086's own Blast radius sweep (which used worktrees) could have caught, since the affected criterion never fires in that venue type at all.
- **Conclusion:** no genuine T-1086-attributable regression was found beyond what the spec's own `## Blast radius` `**Measured**` paragraph already discloses (3 genuine disclosed-class flips, 8 methodology artifacts, 1 pre-existing unrelated artifact). The additional venue-artifact instances found here (T-1051 AC6, T-1002 AC28) sit in the same disclosed class ("the base-side scratch venue silently missing environment state the real checkout has") the engineer already named for a different subset of criteria (the gitignored runs corpus) — same root cause shape, different missing input (a local branch ref rather than a runs directory), found by this round's own venue (a clone rather than a worktree). Not the engineer's omission: the engineer's own Blast radius sweep used worktrees throughout and never hit this class.
- The two disclosed contention notes (a shared-scratch write collision one instance self-recovered from; NUL-corrupted bytes in one fan-out unit's capture) do not contaminate any committed record: the raw part files and the aggregation itself live under gitignored `.shell-team/runs/`, and the only place either could leak into committed bytes is this task's own span-row quotes in `## Baseline evidence` — which are independently confirmed to originate from the baseline arm's own run id (`t1086arm-20260820T145456Z`), never from the QA fan-out's own run id. No leak found.

## Judgment-call audit (provenance entries 6–7, round 2)

- **`teardown-non-destructive` — accepted as sound.** The frozen `baseline-protocol` region's own sentence scopes the property to "no git verb of any kind is ever aimed at the real checkout" — it says nothing about whether the scratch clones themselves are fully deleted. The note's own evidence line (`docs/loop-engineering/tier2-subject-harness.md` line 95) discloses the retained clones honestly and in the same breath as the `met` verdict, rather than hiding the incompleteness. The manner-of-teardown reading is textually supported by the frozen sentence itself; the stricter completion reading would need a different id name (the provenance entry says so explicitly). Accepted.
- **`real-mechanism` — accepted as sound.** This arm performs no landing (stated explicitly in `## Baseline protocol as executed`), so T-1078's own landing-tool-based evidence has no counterpart here. The note substitutes the actually-relevant property — the committed, unmodified oracle and driver invoked from their own committed path, never a venue-local copy — which is exactly what **AC6**'s own check mechanically verifies. This is a faithful re-grounding of an inherited id's substance for an arm shape that structurally cannot evidence it the original way, not a weakening. Accepted.

## Summarized-sources verification (mandatory item)

Twelve sources are named. Representative numeric/anchor-bearing ones re-opened and matched clause by clause against their own bytes (not against the spec's paraphrase):

1. `docs/loop-engineering/phase-multiplexing.md` (both citations, lines ~200–390) — `271772 ms` anchor, `0.7390810688673338` anchor, the basis paragraph, the wall-clock definition (`t0 is the orchestrator's own launch call...`), and the `458–490 ms` landing-span range (verified against the actual seven measured values `468, 490, 475, 458, 459, 475, 462` — min 458, max 490, an accurate range) — all confirmed byte-for-byte.
2. `docs/loop-engineering/phase-multiplexing.md` lines 174–191 (judge scoring contract) — 4 judge-inputs (candidate-handoff optional; candidate-diff/frozen-criteria/check-results required), 4 ranked judge-criterion lines, 5 judge-output fields, the `frozen-criteria` quote — all confirmed verbatim.
3. `.shell-team/specs/T-1078-tier3-pilot.md` line 74 — the Non-goal sentence quoted in the spec's `## Problem` and Summarized-sources — confirmed byte-identical to the source.
4. **`.shell-team/interventions/T-1079.md` — FINDING.** The spec's own Summarized-sources declaration line (line 22) states "read in full (four entries)". `grep -c '^- intervention:' .shell-team/interventions/T-1079.md` → **3**, not 4. This is a genuine miscount in the declaration line itself. It is **not** a flattening inside the frozen intent block: the declaration line sits in `## Summarized sources`, which is before the `<!-- BEGIN intent-block: T-1086 -->` marker, and the actual restated content used in `## Problem` (entry 1's own substance) is accurate and unaffected by the miscount — no criterion or frozen sentence depends on the total entry count. Disposition: a minor, non-blocking editorial correction ("four entries" → "three entries") owed to the spec's own mutable prose, not a class-B re-freeze (fixing it does not touch the hashed intent-block bytes and would not move `check-intent.sh --print-hash`'s value). Recommend batching into a future editorial pass rather than a rework round on its own.
5. `.shell-team/specs/T-1085-default-path-firing.md` — the idioms borrowed (nested marker pair, evidence-sha sub-bullet, two-arm discriminator, two-round cycle) are all present and used identically in T-1086's own spec — spot-confirmed by re-reading both specs' own matching sections side by side.
6. `.github/workflows/check-handoff.yml` — the shellcheck line is a single explicit enumeration (confirmed, `grep -c` = 1); `docs/interventions-reminder-hook.sample.sh` is present on that line (confirmed); no `run: bash tests/*/run.sh`-shaped glob exists for suite steps (confirmed — the only globs present are unrelated, `.shell-team/retros/*.md`, `.shell-team/interventions/*.md`, and a `derive-populations.sh --set` pathspec); infra steps are exactly `Checkout` and `Install shellcheck` at `SHELLCHECK_VERSION: "0.11.0"` (confirmed).
9. `bin/check-acs.sh` — `AC_RE` at line 141 matches the spec's quoted regex exactly; `${CHECK_ACS_TIMEOUT:-120}` fallback with an explicit warning message on an invalid value, both confirmed present.

Sources 7 (`tests/bin-exec-bit/run.sh`), 8 (`bin/derive-populations.sh`), 10 (`bin/check-intent.sh`), 11 (`.shell-team/todo.md`), 12 (`agents/pm-spec.md`) were read during the mechanical/gate verification above (their own mechanisms were exercised directly — `check-intent.sh`, `derive-populations.sh` via AC20, the board's own grammar via `check-handoff.sh`) rather than re-opened for a separate line-by-line prose comparison in this round; no discrepancy surfaced during their mechanical exercise.

## Adversarial fixture synthesis checklist — coverage disclosed

- Class 1 (beyond happy-path): exercised — partial implementation, extra files, no-arg invocation, non-existent and non-empty target directories. Resource/budget-competition and time/liveness sub-axes are **structurally inapplicable** to this deliverable: the two scripts take no budget/quota parameter and the oracle's own fixtures are fixed-size and instantaneous (no hang surface — no network call, no unbounded loop over adopter-supplied input).
- Class 2 (regex/charset anchoring): the `.shell-team`/`docs/loop-engineering`/`bin`+`tests`-path sweep (AC9) was independently re-run above and confirmed to anchor correctly (zero on both scripts); its positive control (`#!/usr/bin/env bash` count) was also independently re-confirmed.
- Class 3 (CommonMark structure parsing): not applicable — this deliverable's own checkers use `awk` range extraction anchored on exact whole-line heading matches inside a note this task itself authors under controlled conditions, not a general-purpose markdown parser exposed to adversarial input.
- Class 4 (test-harness short-circuit): every documented command was run exactly as documented, `bash <path>`, with no stub or override in place — see "Empirical exercise" above.

## Risk notes for the reviewer

1. The Summarized-sources miscount (T-1079.md, "four entries" vs. actual three) is disclosed above; it does not block this PASS, per the reasoning given.
2. Per the spec's own `## Verdict scope`, my PASS on `- harness-legitimacy: repetition-variance`/`oracle-satisfiable`'s own reading of the raw evidence, and on the two judgment-call provenance entries, gates progression; the cross-provider review is the settling read for both, as is customary.
3. This task's own `## Blast radius` `**Measured**` paragraph already discloses 3 genuine new reds against other merged specs (`T-1003` AC19, `T-1020` AC14, `T-1085` AC16) as a structural, predicted-in-advance consequence of the shellcheck-line edit and the new `docs/loop-engineering/` files — real, permanent (until those specs' own owners re-freeze), and correctly attributed to stacking rather than to a T-1086 defect.
4. This round newly identified that the "missing environment state in a scratch venue" artifact class the engineer already named (for the gitignored runs corpus) also has a second instance (missing local branch refs) affecting at least two criteria on two different merged specs (T-1051 AC6, T-1002 AC28) when measured via a `git clone` venue rather than a `git worktree add` venue — worth naming in a future retro as a venue-methodology note, not a defect in this task's own diff.

## Adopted from precedent

T-1085's own review record ("won't-fix" disposition for a similarly-shaped count nit in its own QA verdict prose) is the precedent this round follows for the Summarized-sources finding above: disclose in full, do not block progression on a non-frozen, zero-downstream-impact miscount.

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1086
- Codex model: gpt-5.6-luna（`~/.codex/config.toml` の `model` 設定値。イベントストリーム自体にはモデル識別子が出力されないため、当ラウンドの `codex --version` 実行と合わせて設定ファイルの一次値を採用）
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 13 files, +1107/-2 lines (`git diff --stat 0a018e8681c1371440e8901201bcaf2d5ddfc042...HEAD`)

**スコープの前提。** 本レビューはタスク説明の「Verdict scope」節に従い、凍結 intent block・spec 自身のランタイム criteria（AC1–AC20 全 green、AC21 SKIP by design）・今ラウンドで新規に発見した finding に対する成果物自体の正しさに限定する。プロセス的な記帳・QA 監査の網羅性・記録衛生に関する指摘は verdict を gate しない、という較正済みの前提のもとで判定した。以下の Blocker/Major は全て「成果物自体の正しさ」（committed なオラクル・spec の frozen commensurability 主張・`- check:` 行自体の堅牢性）に関するものであり、記帳系の指摘ではない。

**独立再導出（自分の lens）。** 以下を自分で検証した。(i) wall-clock 算術: rep1 `304309112000` ns（epoch 差分を自分で筆算し確認、`8000608423000 − 7696299311000 = 304309112000`）、rep2 `590394472000` ns（同様に確認）、平均 `447351.792` ms → `447352` ms、スプレッド `286085.36` ms → `286085` ms、いずれも note の記載と一致。(ii) `python3 -c "print((271772+34142822/43)/1442049)"` を自分で実行し `0.7390810688673338` を再現、note の引用と一致。(iii) `docs/loop-engineering/subjects/subject-01/{acceptance,regenerate}.sh` を自分で全文読み、`interface.md`/`README.md`/`manifest.txt` と突き合わせて敵対的に検討した。(iv) 3 本の `- commensurability:` 行の妥当性を `phase-multiplexing.md` の該当行（205, 226, 257, 262–263）を自分で読んで検証した。(v) `## Blast radius` の開示（3 件の genuine disclosed flip、8 件の methodology artifact、1 件の pre-existing unrelated artifact）は QA の独自スポットチェックと整合しており、追加の反証は見つからなかった。

**Codex 起動記録。** primary pass を `codex exec --sandbox read-only --cd <repo> review --base 0a018e8681c1371440e8901201bcaf2d5ddfc042 --json -o ...` で 2 回実行（1 回目は cleanup を publish 前に誤って実行したため raw を破棄し、2 回目を正規の alloc→codex→publish 経路で実施 — 生データはいずれも `.shell-team/reviews/T-1086-codex-primary.{txt,jsonl}` に確定版が残る）。両ラウンドで同種の P1 finding（オラクルの byte-exact 比較欠如、machine-time/wall-clock の基準混在）が独立に再現され、Blocker/Major が出たため adversarial pass（devil's-advocate プロンプト、同じ read-only sandbox）を追加実行し `.shell-team/reviews/T-1086-codex-adversarial.{txt,jsonl}` に確定。adversarial pass は 4 つの主張のうち 4 つとも "breaks" と判定し、primary の finding を裏付けた。

#### Blockers

- `docs/loop-engineering/tier2-subject-harness.md:69-76,119` / `docs/loop-engineering/phase-multiplexing.md:205,262-263` — **commensurability claim `discard-cost-term: supported` の基準（basis）不一致。** この baseline の headline 値 `447352 ms`（`- baseline-arm:` 行）は launch-to-completion の **wall clock** 平均であり、同じ note が同時に開示している per-instance の **duration_ms**（span-row の `262141`/`540535`、平均 `401338 ms`）とは別の量である。一方、比較対象の `cost-input: engineer-phase-cost — 271772 ms` は `phase-multiplexing.md:205` の注記「T-1078's own serial-arm per-instance engineer runtimes」および同 262 行目の `per-instance agent runtimes (harness usage, ms): serial 303524 / 240020` から、明確に **per-instance の実働時間**（wall clock ではなく、同 263 行目で個別に計算されている launch-plus-notification overhead を除いた値）を基準に算出されている（`(303524+240020)/2 = 271772` で一致）。つまり `engineer-phase-cost` は T-1086 の `duration_ms` 系列に対応する量であって、`baseline-arm` の headline（wall clock 平均 `447352`）に対応する量ではない。しかし frozen な `baseline-protocol` region（spec 68 行目、note 119 行目）は「this baseline measures the same quantity `cost-input: engineer-phase-cost` records」と明言しており、この一致主張は測定基準（basis）の面で成立しない。T-1088 が将来 `(N−1) × engineer-phase-cost` の discard 項をこの baseline の headline 数値に対して素朴に price すると、launch/notification overhead 分（rep1 で約 42168 ms、rep2 で約 49859 ms）だけ過大な比較になる。raw evidence 自体（wall clock と duration_ms の両方）は note に開示済みで失われていないため再測定は不要だが、この一致主張の文言は frozen intent block（`baseline-protocol` region、AC18 で byte-lock）の中にあり、単純な mechanics-only 修正では直せない — 主張の意味内容（どちらの量を「comparable」と呼ぶか）に触れる class-B 相当の再凍結が必要になる。Codex primary が両ラウンドとも独立に `[P1]` として検出し、adversarial pass でも "breaks" と確認した。
  - 提案: `- commensurability: discard-cost-term` の記述を、比較対象を duration_ms 系列（`401338 ms` 相当）に限定するよう修正するか、baseline の headline 自体を per-instance duration ベースに再定義する（wall clock は別途 disclosed metric として残す）。いずれも frozen protocol region の文言変更を伴うため、正規の再凍結プロセスを通す。

#### Major

- `docs/loop-engineering/subjects/subject-01/acceptance.sh:67-185`（特に 71-72, 80-85, 100-116, 134-150）— **committed acceptance oracle が frozen interface の一部境界を検証していない。** 7 ケースはいずれも非負整数のみを使用しており、`interface.md:9`/`README.md` の `subject-ac: 5`（`^-?[0-9]+$`、負数を含む）や truncation-toward-zero の負数側挙動を一切試験しない。さらに case1/3/4 は stderr を部分一致（`grep -qF`）でしか検証せず、case2/5/6 は stdout のみを厳密比較して stderr の空性を検証しない。結果として、①先頭の空白をトリムしてから数値判定する、②負数列を非数値として拒否する、③想定外の debug 出力を stderr/stdout に追加で出す、といった `interface.md` 違反の candidate 実装が 7/7 で oracle を通過し得る。加えて `subject-ac: 4`（「全データ行が mismatch でも no-data-rows と同じ扱い」）についても、case3 は header-only の場合のみを試験しており「全行 mismatch」パスは未試験。将来の Tier-2 judge が `check-results`（ac-pass-completeness、ランク 1 の judge-criterion）をこの oracle の pass/fail に依拠させる設計である以上、この検証弱さは「defective oracle が後続 arm の figure を静かに壊す」という Verdict scope が名指しした最重要リスクそのものに該当する。Codex primary が 2 ラウンドとも `[P1]`/`[P2]` として検出し、adversarial pass でも "breaks" として whitespace-trim・負数拒否の具体例が示された。
  - 提案: 負の整数を含む列、truncation-toward-zero が非自明になる負の合計（例: 奇数個の負数合計）、全行 field-count-mismatch のケース、stdout/stderr の完全一致比較（余分な出力を許さない）を oracle に追加する。
- `.shell-team/specs/T-1086-subject-harness.md:133-134`（**AC12** の `- check:` 行）— **AC12 は rep の一意性・arm-id 整合・oracle-exit と pass/fail 件数の整合を検証しない。** 現状の check は `- baseline-outcome:` 行の総数を `reps=` の値と数の上でのみ突き合わせており、各行の `rep=` 値が `1..reps` の連番かつ重複なしか、`arm-id` が `- baseline-arm:` 行と一致するか、`oracle-exit=0` のとき `passed == total` になっているか、は一切検証していない。Codex adversarial pass が具体的に示した通り、note の `- baseline-outcome:` 行の一方の `rep=2` を `rep=1` に書き換えるだけで、行数と `reps=2` の数は一致したままになり AC12 は green のまま通る（AC17 が後続の byte-lock 比較で検知し得るとしても、AC12 自体は「捏造されたエビデンスを弾く」設計として機能しない）。この spec の他の criteria（特に AC13 の two-directional coupling や各所の positive control）は同種の fabrication-resistance に極めて高い水準の厳密さを払っており、AC12 だけがこの水準に届いていない。なお、今回実際にコミットされた evidence 自体（rep=1/2 が重複なく、arm-id 一致、oracle-exit と 7/7 が整合）は目視・機械双方で正しいことを確認済みであり、今ラウンドの verdict `harness-ready: met` 自体を無効化するものではない。
  - 提案: `rep=` 値の集合が `1..reps` の重複なし連番であること、各行の `arm-id` が `- baseline-arm:` 行の arm-id と一致すること、`oracle-exit=0` のとき `passed`/`total` が一致することを AC12 の check に追加する。

#### Minor / nits

- `docs/loop-engineering/tier2-subject-harness.md:108-110`（`- reuse-step: 1`）— pre-commitment の droppable 2nd（`regenerate.sh` を落とす場合のフォールバック）として `## Reuse procedure` セクションが機能するはずだが、reuse-step 1 は文字通り `regenerate.sh` の実行を指示しており、ドライバが落とされた世界線ではこの手順自体が成立しない。ただし spec 自身の pre-commitment 節が「Executing either drop costs a ratified re-freeze of the criteria that name the dropped artifact — AC5, AC7, AC9 and AC16」と明記しており、AC16（reuse-step の内容を検証する criterion）の再凍結が drop 実行のコストとして既に価格付けされている。したがって、この不整合は現時点では潜在的なもので、実際にドライバが落とされる局面が来れば AC16 再凍結の中で当然修正対象になる。fast-follow disposition: **won't-fix**（現ラウンドでは regenerate.sh は落とされておらず committed 済みであり、spec 自身が将来の drop 時に AC16 再凍結を義務付けているため、今追加で手当てする理由がない）。
- 敵対的候補（malicious/adversarial candidate）が `acceptance.sh` 実行中に venue/`$TMPDIR` 外へ副作用を書き込みうる点（adversarial pass が指摘、`acceptance.sh:61-64`）— これは spec 自身の `## Input space` / Out-of-scope synthetic extremes 項目 1「An adversarial or malicious candidate: an instance that deliberately defeats the oracle rather than implementing the subject... Instances launched by this loop are trusted; the oracle is a measurement instrument, not a sandbox.」で明示的に宣言除外されている入力クラスであり、`out-of-input-space` として **minor** に格下げする。fast-follow disposition: **won't-fix**（spec が意図的に宣言した設計境界であり、candidate は信頼された instance という前提のもとで oracle はサンドボックスではなく計測器として設計されている）。
- QA が既に開示している `.shell-team/interventions/T-1079.md` の Summarized-sources 行の「four entries」記載ミス（実際は 3 entries、frozen intent block の外側、AC には無関係）— QA レビューの当該箇所と T-1085 の前例（`.shell-team/reviews/T-1085.md` の同種カウント nit の "won't-fix" 判断）をそのまま踏襲する。fast-follow disposition: **won't-fix**（非 frozen・ゼロ影響の編集ミス、今回まとめて直す理由がない）。

#### Adversarial findings

devil's-advocate プロンプトで `acceptance.sh` / `regenerate.sh` / `interface.md` / `README.md` / spec の AC12 check body / note の baseline evidence・commensurability section を Codex 自身に再読させ、4 つの主張を個別に破壊させた（詳細生データ: `.shell-team/reviews/T-1086-codex-adversarial.{txt,jsonl}`）。

1. 「oracle は frozen interface 違反を必ず弾く」→ **breaks**。先頭空白をトリムしてから数値判定する candidate は `README.md:76-82`（`subject-ac: 5`）に違反しつつ 7/7 で通過しうる（具体例: ` 10,100` の ` 10` を数値列として扱う）。
2. 「commensurability claim discard-cost-term は engineer-phase-cost と同じ量を測っている」→ **breaks**。上記 Blocker のとおり、wall clock（`447352`）と per-instance duration（`401338`、engineer-phase-cost の基準に対応）は異なる量。
3. 「AC12 の check は捏造・重複エビデンスを弾ける」→ **breaks**。`rep=2` を `rep=1` に書き換える最小改変で AC12 は現状のまま green を維持する。
4. 「acceptance.sh/regenerate.sh は自己完結でプロセスレベルの副作用も無い」→ **breaks as worded**（パス参照の自己完結性自体は保持されているが、`acceptance.sh` が candidate CLI を無制限に実行する以上、プロセスレベルの副作用隔離は AC9 の検証範囲外）。ただし前述のとおりこれは spec の Input space が明示的に宣言除外した敵対的候補クラスであり、minor / out-of-input-space に格下げ済み。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | acceptance.sh の byte-exact 比較欠如（whitespace/負数/余分な出力） | primary: Codex-native `[P1]`→機械的マッピングでは blocker；adversarial: `high`→major | major | 現行の 7/7 recorded evidence 自体の真偽は損なわれておらず、fixture 追加という non-frozen な修正で解消できるため blocker から major へ降格 |
| 2 | commensurability claim の basis 不一致（wall-clock vs per-instance duration） | primary: `[P1]`→blocker；adversarial: `high`→major | blocker | frozen intent block（`baseline-protocol` region、AC18 byte-lock）内の主張であり、修正には class-B 相当の意味内容変更を伴う再凍結が必要なため、adversarial pass の major 判定より重く見て blocker のまま据え置いた |
| 3 | AC12 の check が rep 一意性/arm-id/oracle-exit 整合を検証しない | primary: `[P2]`→major；adversarial: `high`→major | major | 変更なし。実データは正しいが check 自体の fabrication-resistance が spec の他所の水準に届いていない |
| 4 | candidate プロセスの副作用隔離が無い | adversarial のみ: `medium`→minor | minor（out-of-input-space） | spec の Input space Out-of-scope 項目 1（敵対的候補は宣言除外、instance は信頼される前提）を根拠に、real-data-reachable ではない synthetic extreme として据え置き格下げ |
| 5 | reuse-step 1 が regenerate.sh 依存（droppable-2nd フォールバックとして機能しない） | primary: `[P2]`→major | minor | spec 自身の pre-commitment 節が AC16 の再凍結をドライバ drop 時のコストとして既に価格付けしており、現時点では潜在的不整合に留まるため降格 |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1086 を `READY_FOR_ENG` に戻し、上記 Blocker 1 件・Major 2 件を紐付ける。特に Blocker（commensurability basis 不一致）は frozen `baseline-protocol` region の意味内容に触れるため、単純な mechanics-only 再凍結では閉じられない点に注意。

#### Fast-follow disposition

- reuse-step 1 の regenerate.sh 依存（droppable-2nd フォールバック不整合）— **won't-fix**（spec 自身が AC16 再凍結をドライバ drop 時のコストとして既に価格付け済みのため、現時点で追加対応する理由がない）。
- candidate プロセスの副作用隔離欠如 — **won't-fix**（spec の Input space が明示的に宣言除外した敵対的候補クラスであり、instance は信頼される前提の設計）。
- `.shell-team/interventions/T-1079.md` の Summarized-sources カウント誤記（four entries → 実際は three） — **won't-fix**（T-1085 の前例を踏襲、非 frozen・ゼロ影響の編集ミス）。

---

## QA round 2 (verifying the rework that closes codex round-1 REQUEST_CHANGES)

### Verdict: PASS → READY_FOR_REVIEW

Read end to end before judging: this file's own codex round-1 section, the operator-ratified v1→v2 class-B delta (`2853acb`), and the round-3 engineer rework hand-off (`d455912`, `d5528a2`). All three closed findings independently re-verified against live commands and fresh fixtures — not accepted from the hand-off's own narration.

### 1. Full sweep, decomposed

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1086-subject-harness.md 2>&1 | tail -3` → **`check-acs: 19 passed, 1 failed, 1 skipped, 0 unrecognized`**, `FAILED: AC18` — exactly the conformant profile.

AC18 decomposed by hand (own commands, not accepted from the check's own exit code alone): read the evidence-commit (`5860078a…`) spec blob — `bin/check-intent.sh --print-hash` on it → `ffaae699…` (40-hex, non-empty); its extracted `baseline-protocol` region → 14 non-blank lines (≥12 ✓), contains `- harness-ready:` ✓. Same region at HEAD → 14 non-blank lines (≥12 ✓), contains `- harness-ready:` ✓. HEAD's own `--print-hash` → `b782ea39…` (40-hex, non-empty) ✓. The only failing clause is the final `cmp` between the two regions; `diff` on the two extracted regions shows **exactly one differing paragraph** — the `**Commensurability, fixed before the measurement…**` paragraph, rewritten to name the `duration_ms` comparable — and nothing else in the 14-line region moved. The red is exactly the one clause the board's `- known-red by ratification` record names, and precisely attributable.

### 2. Gate checkers

- `check-intent` → `aligned: T-1086 v2 (b782ea39ec7aa6935f000468c5a0072c6dfa1316)`.
- `check-handoff` → exit 0.
- `check-pii-shapes --base $(git merge-base chore/lessons-2026-08-20 HEAD)` → `clean`.
- `check-provenance` → `conformant (9 decision entries, 0 sentinel)`.
- `check-interventions --task T-1086` → `conformant (2 entries, 0 sentinel)` — both read: the operator's mid-gate corrections on ratification-package framing (plain-language explanation owed; recommendation-not-obligation framing).
- `check-refreeze-class.sh --old-hash ffaae699… <v1> <v2>`, independently reconstructed from the two commits either side of `2853acb`, own run → `class-b`, matching the board's own classification.

### 3. Oracle strengthening, audited as a class

Read `interface.md` (5 interface lines) and `README.md` (7 `subject-ac:` lines, 4 `candidate-rule:` ids) independently, then read the full, current `acceptance.sh` (305 lines, 11 cases) line by line and matched every clause in the hand-off's own coverage table against what the script actually does. All rows confirmed accurate: case8 (all-rows-mismatched, ordinal skip lines in order, then the final `error: no data rows`) genuinely covers `subject-ac: 4`'s general clause; case9 genuinely exercises truncation-toward-zero on a negative quotient (`sum=-7, count=2` → bash's own `$(( -7 / 2 ))` = `-3`, confirmed live on this host, not floor's `-4`); case10/case11 genuinely test leading/trailing whitespace against the frozen `^-?[0-9]+$` pattern; every one of the 11 cases now asserts both streams byte-exactly (`cmp` or an explicit empty-stream assertion), confirmed by reading each case block. The "deliberately not covered" rows (cli-invocation flags/stdin, blank-line-in-the-middle, the four candidate-rule ids, a `+`-prefixed integer, simultaneous multi-column omission, a near-integer-range sum) each carry a stated reason I accept: none was named by the review or by pm-spec, and README's own text assigns the four candidate-rule ids to a future judge, never this oracle.

Exercised empirically, own fixtures:
- Fresh regenerated venue → `bash acceptance.sh <fresh>` → `acceptance: 0 passed, 11 failed`, exit 1 — non-vacuous at the new count.
- **Own adversarial candidate 1** (whitespace-trimmed before the numeric test — the exact review-cited exploit): `acceptance: 9 passed, 2 failed`, failing **exactly** `case10`/`case11` and nothing else — matches the hand-off's own re-broken-variant claim, independently reproduced from a candidate I wrote myself, not the engineer's.
- **Own adversarial candidate 2** (an otherwise-correct implementation that prints one extra debug line to stderr after processing): `acceptance: 3 passed, 8 failed` — fails every case where the frozen contract requires stderr to be empty (case2/5/6/7/9/10/11) and passes only the cases that exit before the debug line is ever reached (case1/4/8) — confirms the byte-exact-stderr strengthening genuinely closes the review's third adversarial example (extra debug output), not merely the whitespace one.

### 4. Re-score claims, verified against the actual retained venues

Both `/tmp/claude-502/t1086-rep1-venue` and `/tmp/claude-502/t1086-rep2-venue` exist on disk (confirmed `ls`, five committed-file copies plus each rep's own `lib/`/`cli/`/`check/`). `bash docs/loop-engineering/subjects/subject-01/acceptance.sh <each venue>` (the committed HEAD path, never a venue-local copy) → **`acceptance: 11 passed, 0 failed`, exit 0, both** — matches the hand-off exactly.

The note's `## Baseline analysis and provenance` section (own read) states explicitly that "The evidence section's own `7/7` results (both reps) are, and remain, a measurement against the oracle *at evidence commit* `5860078a…`… that record is never edited or overwritten," then separately reports the HEAD-oracle re-score as "a finding about the candidates… not a re-measurement of the arm itself." The two figures are distinguished by oracle identity throughout, confirmed by reading the actual prose rather than trusting the hand-off's summary. **AC17**'s own `cmp` (evidence-commit `## Baseline evidence` section vs. HEAD's) decomposed by hand: both extracted sections are 12 non-blank lines, `cmp` exits 0 — byte-identical, confirming the round-3 rework never touched the byte-locked evidence.

### 5. Commensurability arithmetic, independently re-derived

- `duration_ms` mean: `(262141 + 540535) / 2` = **401338.0** — matches.
- Rep 1 overhead: `304309112000 ns → 304309.112 ms` minus `262141` = **≈42168.11 ms** — matches the note's "≈42168 ms".
- Rep 2 overhead: `590394472000 ns → 590394.472 ms` minus `540535` = **≈49859.47 ms** — matches the note's "≈49859 ms".
- Naive overpricing: `447352 − 401338` = **46014.0** — matches the note's "≈46014 ms" exactly.

The two `duration_ms` span-row values (`262141`, `540535`) were re-extracted directly from the note's own JSON span rows (`grep -o '"duration_ms":[0-9]*'`), not retyped from the hand-off's prose, and match the byte-locked evidence section's own text confirmed under AC17.

### 6. Fan-out disposition — ratified, no re-fan-out needed

`git diff --name-only 31f300a HEAD` shows the full round-3+refreeze diff touches exactly: `.shell-team/interventions/T-1086.md`, `.shell-team/provenance/T-1086.md`, `.shell-team/reviews/T-1086.md`, `.shell-team/specs/T-1086-subject-harness.md`, `.shell-team/todo.md`, `docs/loop-engineering/subjects/subject-01/acceptance.sh`, `docs/loop-engineering/tier2-subject-harness.md`. `git diff 31f300a HEAD -- .shell-team/todo.md | grep -E '^[+-]- \[[ x]\] \*\*T-'` returns **empty** — no other task's top-level board line was touched, and no new spec file was added (the spec-population derivation, `docs/loop-engineering/subjects/` and `tier2-subject-harness.md` were already established in round 1's own Blast radius as new paths no merged criterion can name). Since the touched set is a strict subset of paths already assessed unreachable by any other merged spec's criterion, and no new top-level entry or spec file exists to newly trip the merge-point-scoped population/board-count class, **I ratify the delta-read disposition — no second 92-unit fan-out is needed for this round.**

### 7. Class ledger — independently confirmed closed, no recurrence

- **frozen-claim-basis** (Blocker) — closed at the ratified v2 freeze; AC18's own decomposition above confirms the only byte that moved in the frozen region is the corrected commensurability paragraph.
- **oracle-contract-coverage** (Major 1) — closed this round; independently re-audited via my own clause inventory and two of my own adversarial fixtures (§3).
- **check-fabrication-resistance** (Major 2) — closed this round; independently re-verified via three of my own mutations against AC12 v2 (duplicate-rep, alien arm-id, exit/counts incoherence — all three correctly FAIL).

No recurrence of any of the three classes found in this round's own new work; the same-class-2 full-inventory trigger does not fire.

### Risk notes for reviewer

1. Per the spec's own `## Verdict scope`, this PASS on the corrected commensurability claim's own arithmetic direction is necessary but not sufficient — the cross-provider review's independent re-derivation is the settling read for the quantitative conclusion, per the lessons-corpus convention for recall-written numeric prose.
2. AC18's permanent red is a designed, ratified consequence (interpretive-claim correction, no arm re-run) and should not be re-litigated by the reviewer as an unresolved defect.
3. The round-1 Summarized-sources finding (T-1079.md "four entries" vs. actual three) remains open and non-blocking, unchanged this round (also disposed `won't-fix` in the codex round-1 review's own Minor/nits list).
4. This round's fan-out disposition (delta-read, no re-fan-out) is my own ratification, grounded in the diff enumeration in §6 above — flagged for the reviewer's independent sanity check given it departs from the standard full-population sweep.

### Codex Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1086
- Codex model: gpt-5.6-luna（`~/.codex/config.toml` の `model` 設定値。round 1 と同一の一次値取得方法）
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 13 files, +1427/-3 lines (`git diff --stat 0a018e8681c1371440e8901201bcaf2d5ddfc042...HEAD`)

**round 1 の 3 finding、クローズを確認。** 独立に再検証した。

1. **Blocker（commensurability basis 不一致）** — `bin/check-refreeze-class.sh --old-hash ffaae699… <v1> <v2>` を自分で v1/v2 の実バイト（`2853acb^`/`2853acb` の spec blob）から再構成して実行し、`class-b`（exit 1、intent-block index 33 が両側とも `- check:` 行でない）を独立に再現、board の記録と一致。v1→v2 の diff を全行チェックし、frozen intent block 内で変更されたのは Goal の commensurability 段落・AC12・AC15・AC18 の 4 箇所のみ（Non-goals・Input space は無傷）であることを確認 — ratification が主張する範囲に正確に収まっている。note 側の算術も自分で再導出: `(262141+540535)/2=401338`、rep1 overhead `304309.112−262141≈42168`、rep2 overhead `590394.472−540535≈49859`、naive overpricing `447352−401338=46014` — いずれも note の記載と一致。**AC15 の `duration_ms` アンカー**も現行 sweep で green。この finding は解消したと判断する。
2. **Major 1（oracle 網羅性）** — `acceptance.sh` の diff を読み、7→11 ケースへの拡張（case8: 全行 mismatch、case9: 負数と truncation-toward-zero、case10/11: 先頭/末尾空白）と、全ケースでの stdout **かつ** stderr の byte-exact 比較（`cmp`/`empty_ok`）を確認。フレッシュ venue で `bash acceptance.sh` → `acceptance: 0 passed, 11 failed`、exit 1（non-vacuous、再現済み）。retained な rep1/rep2 venue（`/tmp/claude-502/t1086-rep1-venue`, `.../t1086-rep2-venue` — QA の記録どおり現存を確認）に対して HEAD の oracle を実行 → 両方とも `acceptance: 11 passed, 0 failed`、exit 0 を自分で再現。この finding は解消したと判断する。
3. **Major 2（AC12 fabrication-resistance）** — AC12 の check 本体を自分で抽出し、3 種類の mutation を自作の scratch tree（`docs/loop-engineering/tier2-subject-harness.md` の相対パスを持つ scratch cwd、check 内部の `N=` ハードコードを踏まえて cwd 経由で差し替え）に対して実行した: ①`rep=2`→`rep=1` への書き換え（重複 rep）→ **rc=1（FAIL、正しく検出）**、②`baseline-outcome:` の arm-id を `alien-arm-id` に書き換え → **rc=1（FAIL、正しく検出）**、③`oracle-exit=0` のまま `7/7`→`6/7` に書き換え（passed/total 不整合）→ **rc=1（FAIL、正しく検出）**。round 1 の finding（`rep=2`→`rep=1` で AC12 が green のまま通る）はもう成立しない。この finding は解消したと判断する。

**再凍結そのものの検証（このラウンドの必須項目）。** 上記 1. で実施済み: classifier の再現、v1→v2 delta の範囲確認（Non-goals/Input space 無傷）、算術の独立再導出。加えて AC18 の red を自分で分解した: evidence-commit（`5860078a…`）側の `baseline-protocol` region と HEAD 側を `awk` で抽出し `diff` → 差分は commensurability 段落 1 箇所のみ（他 13 行は同一）、`- harness-ready:` の文言も両側に存在、両ハッシュ（`ffaae699…`/`b782ea39…`）とも 40-hex — board の `- known-red by ratification` 記述と完全に一致し、"an interpretive-claim correction invalidates nothing the arm measured" という disposition の主張を自分の分解で裏付けた。

**QA の delta-read 承認（sanity check 依頼分）に対する所見。** `git diff --name-only 31f300a HEAD` を自分で実行し、変更ファイルが 7 件（spec・board・provenance・interventions・review・`acceptance.sh`・note）に限定されること、`git diff 31f300a HEAD -- .shell-team/todo.md | grep -E '^[+-]- \[[ x]\] \*\*T-'` が空（T-1086 自身のフラグ変更以外に他タスクの board 行が触れられていない）ことを独立に確認した。`docs/loop-engineering/subjects/subject-01/` と `tier2-subject-harness.md` は round 1 の Blast radius で既に「他 spec の criterion が到達できない新規パス」と確定済みであり、今回の diff はその既存パスの非 frozen 部分の修正と `acceptance.sh` 自体の変更に限られる（`bin/`・`tests/`・`skills/`・`templates/`・`agents/`・共有インフラファイルへの変更なし）。QA の判断（92-unit の再 fan-out は不要）に**同意する**。

**新規に検出した 3 件（primary pass 2 ラウンドとも同種の脆弱性クラスを検出、adversarial pass で全 3 件とも "breaks" 確認、自分でも独立に再現）。**

#### Pre-commitment trigger（frozen self-discipline、spec:72 — 最重要事項）

spec の frozen `baseline-protocol` region（72 行目）が自ら定めた pre-commitment 条項:「**Factual trigger**: 同一コンポーネントに対して独立した Blocker/Major が 2 回連続の review round で着地すること」「**Never dropped** [components]: a committed subject (brief, interface contract, manifest, **oracle**) plus one measured serial baseline carrying its venue, basis and wall-clock provenance [= AC12 が担う fabrication-resistance を含む]」「Defeated twice on any never-dropped component, the task **stops and returns to planning** rather than being patched a third time; there is no carve-out to execute for a never-dropped component, because **the same mechanism defeated across consecutive rounds is evidence about the design premise rather than about craft**」。

この条項が今回、**文字通り**発火していると判断する。理由:
- **oracle（never-dropped component）**: round 1 で Major（whitespace-trim・負数拒否・byte-exact 不足の 7 ケース）、round 2 で独立した別の Major（zero-padded octal、`010` を bash 算術が 8 と誤解釈しうるが 11 ケースいずれも検出しない）— 2 ラウンド連続で独立した Major が同一コンポーネント（oracle）に着地している。
- **AC12/baseline provenance（never-dropped component の一部）**: round 1 で Major（`rep=2`→`rep=1` の書き換えで green のまま通過）、round 2 で独立した別の Major（`rep=01` と `rep=1` が `sort -u` 上は別文字列として扱われ、`reps=2` の一意性チェックを通過してしまう ── 自分の mutation test で再現済み）— こちらも 2 ラウンド連続で独立した Major。

いずれも spec 自身の文言どおり「同じメカニズムが連続ラウンドで撃破される＝craft の問題ではなく design premise の問題」というパターンに該当する。具体的には: 有限個の手書き fixture で「あらゆる不正な candidate 実装」を弾き切ろうとする example-based oracle の設計、および正規表現ベースの文字列一意性チェックで数値の等価性を保証しようとする AC12 の設計は、いずれも「もう一つ穴を塞げば終わる」ものではなく、次のラウンドでもまた別の具体例（例: `+5`・64bit 超の巨大整数・CRLF・重複ヘッダ名 等）が見つかる構造的なクラスである可能性が高い。

**この pre-commitment は「self-imposed by this loop at freeze time, never operator-ratified」と spec 自身が明記している** — つまり人間オペレーターが約束した規律ではなく、チーム自身が凍結時に自らに課した規律である。したがって私はこれを覆す権限も、代わりに「もう一度直してください」と要求する権限も持たない。私の役割は、この frozen text 自身が定める発火条件が実際に満たされていることを独立に確認し、報告することに限られる。**Recommendation はこの発火を根拠に REQUEST_CHANGES とするが、通常の「直して出し直す」第 4 ラウンドではなく、spec 自身の文言どおり planning への route-back を検討することを推奨する**（具体的な処理方法の決定はオーケストレーターの権限であり、私が代行するものではない）。

#### Blockers

- `.shell-team/specs/T-1086-subject-harness.md:72`（frozen pre-commitment region）— 上記「Pre-commitment trigger」参照。oracle と AC12/baseline-provenance の両 never-dropped component が、2 ラウンド連続で独立した Major を受けている。spec 自身の文言に従えば、第 3 のパッチラウンドではなく planning への route-back が disposition である。

#### Major

- `docs/loop-engineering/tier2-subject-harness.md:47`（`- baseline-basis:` 行）— round 3 のリワークで `## Commensurability` 段落と `## Baseline analysis and provenance` の補正段落（101 行目）は正しく修正されたが、より前段の `## Baseline protocol as executed` 内の `- baseline-basis:` 行は未修正のまま残っており、「this arm is a single serial instance, so its machine-time and calendar-clock figures coincide」と、今も明確に誤った主張をしている。同じ note の後段（101 行目、`- commensurability: discard-cost-term`、123 行目）は wall-clock（`447352 ms`）と duration_ms（`401338 ms`）が **別の量** であると明言しており、直接矛盾する。frozen block の外（`## Baseline protocol as executed` は byte-lock 対象外）にあるため、再凍結なしで直せる non-frozen な自己矛盾。note を通読する読者が先に 47 行目だけを読んで later correction に到達しなければ誤った理解のまま残る。
  - 提案: 47 行目を「この arm の headline（launch-to-notification wall clock）と `engineer-phase-cost` 相当の per-instance `duration_ms` は異なる量であり、後段の commensurability 補正を参照」という趣旨に書き換える。
- `.shell-team/specs/T-1086-subject-harness.md:134`（AC12 の `- check:` 行）— rep 一意性チェックが生文字列に対して `sort -u` を行った後で `10#` 正規化するため、`rep=01` と `rep=1` はどちらも decimal 1 を指すにもかかわらず 2 つの「distinct」な文字列として扱われ、`reps=2` の一意性要件を満たしてしまう。結果として実際には rep 2 の証跡が一切存在しないのに AC12 は green のまま通る（自分の scratch mutation test で再現、rc=0）。
  - 提案: `rep=` の値を `10#` で正規化してから重複排除するか、`sed`/`grep` の抽出正規表現を `[1-9][0-9]*`（先頭ゼロなし）に制限する。
- `docs/loop-engineering/subjects/subject-01/{README.md,acceptance.sh}`（README.md:76-82 の `subject-ac: 5` 正規表現、acceptance.sh の 11 ケース）— frozen pattern `^-?[0-9]+$` は `010` のようなゼロパディング済みの値を数値として受理するが、bash の算術評価では `$(( 010 ))` は 8 進数として解釈され `8` になる（このホスト上で実測: `bash -c 'echo $(( 010 ))'` → `8`）。11 ケースのいずれもゼロパディング値を使っておらず、`sum=$(( sum + value ))` のような素朴な実装が `010` を誤って合計してもオラクルは検出できない。
  - 提案: ゼロパディングされた整数列（例: `010`）を含む fixture を追加し、期待される sum/min/max/avg を decimal 解釈で厳密に検証する。

#### Minor / nits

- （round 1 から継続、変更なし）reuse-step 1 の regenerate.sh 依存 — **won't-fix**（未変更）。
- （round 1 から継続、変更なし）candidate プロセスの副作用隔離欠如 — **won't-fix**（未変更、Input space Out-of-scope 項目 1 が根拠）。
- （round 1 から継続、変更なし）`.shell-team/interventions/T-1079.md` の Summarized-sources カウント誤記 — **won't-fix**（QA も round 2 で同じ disposition を確認）。

#### Adversarial findings

devil's-advocate プロンプトで `acceptance.sh`（11 ケース）・`interface.md`/`README.md`・AC12 の check body・note の baseline-basis / commensurability-correction 段落を Codex 自身に再読させ、3 つの主張を検証させた（詳細生データ: `.shell-team/reviews/T-1086-codex-adversarial.{txt,jsonl}`）。3 つとも **breaks** と判定され、上記 Major 3 件と完全に一致した。

1. 「note は wall-clock/machine-time の一致性について内部矛盾がない」→ **breaks**（47 行目 vs 101 行目の矛盾）。
2. 「AC12 の rep 一意性チェックは数値的に重複した rep 値を弾ける」→ **breaks**（`rep=01`/`rep=1` の zero-padding で通過）。
3. 「11 ケースの oracle は frozen pattern が受理するあらゆる数値を正しく分類できる」→ **breaks**（`010` の octal 誤解釈が未検出）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | note の baseline-basis 段落と commensurability 補正の内部矛盾 | primary: Codex-native `[P1]`→機械的マッピングでは blocker；adversarial: `major` | major | frozen block の外側（`## Baseline protocol as executed` は byte-lock 対象外）で修正可能、かつ実際にガバナンスする frozen な commensurability claim 自体は既に正しく補正済みのため、blocker から major へ降格 |
| 2 | AC12 の rep 一意性チェックが zero-padding で回避可能 | primary: `[P2]`→major；adversarial: `major` | major | 変更なし。自分の mutation test（rc=0 で通過）で独立に再現済み |
| 3 | oracle が zero-padded octal を検出できない | primary: `[P2]`→major；adversarial: `major` | major | 変更なし。自分の bash 算術実測（`$(( 010 ))` → `8`）で独立に再現済み |
| 4 | pre-commitment trigger（oracle・AC12 双方が 2 ラウンド連続で独立 Major を受領） | Codex からの入力ではなく、round 1 と round 2 の finding 1〜3 を突き合わせた自分自身の synthesis judgment | blocker | spec 自身の frozen pre-commitment 条項（72 行目）の文言どおりの発火条件（"defeated twice on any never-dropped component"）が満たされていると判断したため。ledger の通常運用（primary/adversarial からの severity 変更記録）とは性質が異なるため、参考として記載する |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1086 を `READY_FOR_ENG` に戻すことは可能だが、**通常の第 4 パッチラウンドではなく**、spec 自身の frozen pre-commitment（`.shell-team/specs/T-1086-subject-harness.md:72`）が定める「defeated twice on any never-dropped component → stops and returns to planning」への route-back を、オーケストレーターに検討することを強く推奨する。この pre-commitment はチーム自身が凍結時に自らに課した self-imposed discipline であり、私にそれを執行する権限はない — 発火条件が満たされていることの報告に留める。

#### Fast-follow disposition

- reuse-step 1 の regenerate.sh 依存 — **won't-fix**（round 1 から変更なし）。
- candidate プロセスの副作用隔離欠如 — **won't-fix**（round 1 から変更なし）。
- `.shell-team/interventions/T-1079.md` の Summarized-sources カウント誤記 — **won't-fix**（round 1・QA round 2 から変更なし）。
- 上記 Blocker/Major 4 件は fast-follow ではなく、pre-commitment の発火に伴う planning route-back の対象として扱うべきであり、merge 後に回すものではない。
