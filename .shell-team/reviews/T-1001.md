# QA review — T-1001

**Verdict: FAIL**

## Structural gates (all green — not the finding)

- `bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` → 30/30 PASS.
- `bash bin/check-intent.sh .shell-team/specs/T-1001-retro-input-acquisition.md .shell-team/todo.md` → aligned (v1, `d335048e1d55751c7be60b0e93220e2a2f546f4e`).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1001.md` → conformant (8 decisions, 0 sentinel).
- `bash bin/check-prompt-sync.sh` → all registered prompt blocks in sync.
- `bash bin/check-handoff.sh .shell-team/todo.md` → clean.
- Every suite in `.github/workflows/check-handoff.yml`'s job (all steps, including the two new dogfood steps `bash bin/retro-inputs.sh --base HEAD` and `bash bin/check-retro.sh .shell-team/retros/*.md`) run locally: green. `shellcheck` clean on the exact CI argument list (0.11.0, matching the pinned version).
- `bash bin/check-pii-shapes.sh --base develop` / `bash bin/check-commit-identity.sh --base develop`: clean.
- Working tree: `git status --short` clean throughout (my own fixtures were built entirely under `$TMPDIR`, outside this repository).

## The single most important thing (AC12, degenerate-history classification) — independently reproduced, all correct

Built five throwaway `git init` repositories myself (not the shipped suite's fixtures) and ran `bin/retro-inputs.sh` against each directly:

1. Non-resolving `--base totally-bogus-ref-xyz` → `unavailable — detail: base ref does not resolve locally: totally-bogus-ref-xyz`. Correct.
2. Zero merges, non-shallow (squash-style history) → `empty — detail: 0 merge commits reachable from main (first-parent), likely a squash-merge history, not a truncation`. Correct.
3. **Shallow, zero merges in the boundary** (dummy `.git/shallow`) → `unavailable — detail: shallow repository: 0 merge commits found within the boundary on main (cannot distinguish no merges from merges beyond the shallow boundary)`. This is the case the spec calls out as the whole reason the task exists, and it is correct: never silently reported as `empty`.
4. Shallow, with merges → `read — detail: 1 merge commits from main (first-parent); shallow clone truncates history at the boundary`. Correct — `read`, not conflated with the other two, truncation declared.
5. Failing `git log --merges` (my own stub, independent of the shipped `tests/retro-inputs/fixtures/git`) → exit 0, complete 8-line ledger, `cycle-window — status: unavailable — detail: git invocation failed while listing merge commits on main`. Correct.

Also independently verified: `--last-n` cap reads distinctly from a shallow truncation (`capped at --last-n N (a declared cap, not a truncation)` vs `shallow clone truncates history at the boundary`); an adversarial merge subject containing the ledger grammar and a backtick cannot forge a second ledger line, and the emitted ledger — with the adversarial material still in it — passes `bin/check-retro.sh` when embedded in a retro (reproduced with a payload not in the shipped suite, including one with an embedded raw newline in the merge message, which is neutralized by git's own subject-line folding before the script even runs `sanitize()`); `--lessons PATH` behavior (`unavailable` when omitted, `read` with a non-empty line count when supplied).

## Fixture-label criteria (AC7, AC9, AC11, AC12, AC13, AC14) — read against their labels, not just grepped

Read `tests/retro-inputs/run.sh` in full. Every "case: …" label is attached to an assertion that actually tests what its name says:

- AC7's layout case builds three separate repos (default `.shell-team/`, legacy `tasks/`, `TEAM_RUN_BASE` override) and asserts the resolved `review-artifacts` path differs correctly in each.
- AC9's two cases assert `pr-metadata` status + exit code (gh absent) and assert the stub's request log never contains `body` (gh present) — a real runtime guard, not just a source grep.
- AC11's four cases assert the actual ledger line content for develop-default, HEAD-fallback (with "fell back to HEAD" text), the `--last-n` cap wording, and ledger completeness (8 ids, once each).
- AC12's five cases are the five degenerate-history behaviors above, each with its own purpose-built repository — I reproduced all five independently in fixture repos of my own construction and got matching results.
- AC13's two cases build an adversarial repo with a merge subject that embeds the ledger grammar, a backtick, and an em dash, and assert (a) the ledger stays 8 top-level lines with no backtick surviving and (b) the resulting ledger, embedded in a full retro, passes `bin/check-retro.sh`.
- AC14's two cases assert `unavailable`/`read` status text for lessons-omitted/lessons-supplied.

No label/assertion mismatch found in any of the six.

## Fail-closed ledger rule (AC16) — mutation-proved

Copied `bin/check-retro.sh` to a scratch file and neutralized rule 4 (forced `inputs_violations=0` unconditionally after computation). Re-ran all seven `fail-inputs-*.md` fixtures against the neutralized copy:

- `fail-inputs-missing-section` still failed (rc=1) — because rule 2's heading check independently catches a missing `## Retro inputs` heading.
- The other six (`unknown-status`, `unknown-id`, `missing-id`, `duplicate-id`, `empty-detail`, `stray-line`) all went **silently to rc=0** — confirming rule 4 is genuinely load-bearing for those six violation shapes and is not vacuous.

Independently reproduced the CRLF-tolerance and grammar-quoting cases with my own fixtures (different quoted line, different id, than the shipped suite's): both passed correctly, confirming `has_exact_line()`'s CRLF handling and the leftmost-`match()` field extraction both work as documented.

## Regression locks, scope discipline, adopter-environment coercion — all clean

- AC17 (KPT/Traps constants byte-identical) — confirmed via `check-acs`; also read `bin/check-retro.sh` by hand, only the *comparison* mechanism (`has_exact_line`) changed, the five string constants are untouched.
- AC24 (dead-pointer set unchanged), AC25 (Japanese-paren count unchanged vs. `develop`), AC26 (diff confined to allow-list, tree committed) — all green via `check-acs`; spot-checked AC26 by hand (`git status --short` clean, so the untracked-file blind spot the criterion has does not apply here).
- No `--base main` anywhere under `agents bin skills templates`. No mandatory-`gh` path (`command -v gh` / `gh auth status` both gated, `pr-metadata` degrades to `unavailable` on any failure, exit stays 0). No mandatory-`develop` (`develop` default, `HEAD` fallback, ref named in the ledger). No new Japanese required heading (AC25's paren count is unchanged).
- `agents/scrum-master.md`'s `## Inputs you read` / `## Loop` / hand-off block read coherently after the rewrite; the three `contain`-registered canonical files (`language.md`, `flag-enum.md`, `operating-paths-core.md`) are intact (`check-prompt-sync` green covers this mechanically, and I read the file end to end).
- `.shell-team/retros/2026-07-28.md` backfill: read the diff — it is additive only (the `## Retro inputs` block + one `## Notes` bullet), and every status/detail is traceable to a sentence the retro's own pre-existing `## Notes`/`## Keep`/`## Orchestrator attest` sections already contained (2 merges, 3 review artifacts, 3 provenance files transcribed; `specs`/`run-telemetry` correctly `unavailable` rather than counting today's tree, which now holds more files than existed on 2026-07-28 because later tasks have since added their own spec/provenance/review files). Honest, per DP-5's own framing.
- Vocabulary: grepped the *diffs only* (not whole-file) of every changed file against `凍結|フリーズ|ロック|批准|改ざん` about the intent-block mechanism. Clean. (The whole-file grep on `.shell-team/retros/2026-07-28.md` turns up pre-existing, out-of-T-1001-diff occurrences that are either generic non-mechanism usage — "ロックする回帰 fixture" — or meta-discussion quoting issue #8's own forbidden words while describing that they were removed; neither is new material this task introduced.)
- `bin/` purity / CI wiring: shellcheck clean on the exact CI list; new scripts + both fixture stubs are on that list; the suite + both new dogfood steps are wired into the workflow; no new runtime dependency (`jq`/`yq`/`python`/`perl`/`node` all absent, confirmed both by AC8's check and by reading the script).

## Real defect found, not a FAIL driver by itself — documentation-only, flagged for the engineer/reviewer

`docs/templates/retro-template.md`'s `## Retro inputs` section carries, immediately after the heading and before the worked-example ledger lines, four lines of instructional prose ("`bin/retro-inputs.sh` prints this heading and its ledger lines together — paste its output here verbatim... Example shape..."). Running `bash bin/check-retro.sh docs/templates/retro-template.md` shows this prose trips rule 4's fail-closed "unrecognised line inside ## Retro inputs" check four times, in addition to the pre-existing (out-of-scope, already-known) bare Lesson-候補 bullet violation. Provenance decision 1 frames the routing as "the actual ledger under the heading is a separate, well-formed worked example" — true for the eight `- input:` lines themselves (I confirmed they are individually well-formed against the closed enum), but the section as shipped also contains this prose paragraph, which is not well-formed and would fail if a real retro literally kept it. This does not break any AC (AC21 only asserts heading placement; the spec's own Non-goal already declines making the template pass `check-retro.sh` at all, and no CI step requires it) and does not affect production retros (the scrum-master agent is instructed to paste `retro-inputs.sh`'s literal output — heading + 8 lines only, no prose — replacing this section's content wholesale). Labelling as a **documentation nit**, not a blocker.

## FAIL driver: `--last-n 0` crashes the script under BSD `head` (macOS), violating its own documented exit-code contract

`bin/retro-inputs.sh`'s own header states the exit-code contract precisely:

```
# Exit codes:
#   0  success (including every degenerate-history / gh-absent case...)
#   2  argument / usage error
```

`--last-n` accepts any non-negative integer per the script's own validation (`*[!0-9]*` rejects only non-digit characters; `"0"` passes). `0` is not an out-of-scope synthetic extreme — it is the ordinary zero-boundary value of a documented, in-input-space flag (AC11), exactly the "resource / budget competition (zero remaining budget)" class the adversarial-fixture checklist calls for, and it was not in the shipped test suite (which only exercises `--last-n 1`).

### Reproduction

```
$ mkdir -p /tmp/repro && cd /tmp/repro
$ git init -q -b main && git config user.email test@example.com && git config user.name Test
$ printf x > f && git add f && git commit -qm init
$ git checkout -qb feat && printf y >> f && git commit -qam feat
$ git checkout -q main && git merge -q --no-ff -m "Merge pull request #1 from example/feat" feat
$ bash /path/to/shell-team/bin/retro-inputs.sh --base main --last-n 0
```

Observed:
```
## Retro inputs
head: illegal line count -- 0
$ echo $?
1
```

Expected (per the script's own contract): exit 0, with a complete 8-line ledger reporting `cycle-window` as `read` with 0 merge commits shown and the cap declared (analogous to the `--last-n 1` case, just capped to zero), never a bare exit 1 with a truncated one-line ledger and no closed statuses for the other seven inputs.

Root cause: `bin/retro-inputs.sh:224` — `shown="$(printf '%s\n' "$log_out" | head -n "$last_n")"`. On BSD `head` (macOS, confirmed on this machine: `head -n 0 </dev/null` → `head: illegal line count -- 0`, exit 1), `-n 0` is rejected outright, unlike GNU `head` where `-n 0` is legal and simply emits nothing. Under `set -euo pipefail`, the failing `head` in the pipeline aborts the whole script immediately (mid-emission — only the `## Retro inputs` heading had been printed), so every field after `cycle-window` (all seven remaining inputs) never gets reported at all — a materially worse failure than "one field wrong," since the ledger stops being a ledger.

This is `bin/`-scripted, adopter-facing (macOS is a normal dev platform for this project — this very sandbox is Darwin), and squarely inside the reachable input space AC11 declares (`--last-n N` is a documented, un-excluded flag). It is a genuine regression risk: a caller who passes `--last-n 0` (a plausible mistake, or a deliberate "give me nothing, just the shape" call) gets a crash instead of the documented soft-degradation behavior, on any platform with BSD coreutils.

### Suggested next step

Guard the cap arithmetic so `head` is never invoked with `-n 0` — e.g. skip the `head` call entirely when `last_n` is `0` and set `shown=""` directly (mirroring the zero-merge branch's `CW_MATERIAL=""` handling), or use a portable line-count-limiting idiom that both BSD and GNU `head` accept for the zero case. Add a `--last-n 0` case to `tests/retro-inputs/run.sh` (on a repo with ≥1 merge commit, to actually cross the boundary — a fixture that never reaches the cap does not prove the boundary is handled) so this class of zero-budget input is locked going forward.

---

# QA round 2 — T-1001 rework verification (commit `59a0549`)

**Verdict: PASS**

Scope of this round: the delta only (`bin/retro-inputs.sh`'s `compute_cycle_window()` zero-branch, `docs/templates/retro-template.md`'s prose relocation, `tests/retro-inputs/run.sh`'s new case), checked for regressions against round 1's findings — round 1's own fixture reproductions (AC12's five degenerate histories, AC16's mutation-proof, the six fixture-label criteria) are not re-litigated here except where the delta could plausibly have disturbed them.

## Structural gates (re-run, all green)

- `bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` → 30/30 PASS.
- `bash bin/check-intent.sh` → aligned (v1, `d335048e1d55751c7be60b0e93220e2a2f546f4e`, unchanged).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1001.md` → conformant (10 decision entries, 0 sentinel — the two new ones read well-grounded, non-trivial, and specific to this round's changes).
- `bash bin/check-prompt-sync.sh` → green. `bash bin/check-handoff.sh .shell-team/todo.md` → clean.
- `shellcheck` on `bin/retro-inputs.sh tests/retro-inputs/run.sh tests/retro-inputs/fixtures/gh tests/retro-inputs/fixtures/git bin/check-retro.sh tests/check-retro/run.sh` → clean.
- `tests/retro-inputs/run.sh`, `tests/check-retro/run.sh`, `tests/team-paths/run.sh`, `tests/team-init/run.sh`, `tests/check-prompt-sync/run.sh`, `bash bin/retro-inputs.sh --base HEAD`, `bash bin/check-retro.sh .shell-team/retros/*.md` → all green.
- Working tree clean throughout (all my own fixtures built under `$TMPDIR`, outside this repository; the one scratch edit to `bin/retro-inputs.sh` used to mutation-test the fix — see below — was restored byte-identical before re-checking `git status`, confirmed via `git diff --stat` showing no change).

## Item 1 — cap-0 / empty / unavailable are not conflated (own fixtures, three states side by side)

Built three fresh throwaway repos and ran the rebuilt `bin/retro-inputs.sh` against each directly:

- Real merges (1 merge commit), `--last-n 0` → `cycle-window — status: read — detail: 0 merge commits from main (first-parent); capped at --last-n 0 (a declared cap, not a truncation)`.
- Zero merges, non-shallow, no `--last-n` → `cycle-window — status: empty — detail: 0 merge commits reachable from main (first-parent), likely a squash-merge history, not a truncation`.
- Shallow, zero merges within the boundary, no `--last-n` → `cycle-window — status: unavailable — detail: shallow repository: 0 merge commits found within the boundary on main (...)`.

All three distinguishable by `CW_STATUS`, never by the digit alone, exactly as the engineer's provenance decision claims. Confirmed correct.

## Item 3 — the 0-branch does not leak into the shallow / git-failure paths

Re-ran the shallow-with-merges and git-invocation-failure cases from round 1 with `--last-n 0` added on top:

- Failing `git log --merges` (my own stub) + `--last-n 0` → still exit 0, complete 8-line ledger, `cycle-window — status: unavailable — detail: git invocation failed while listing merge commits on main` — unaffected by the 0-handling addition. (Code read confirms why: both the shallow-zero and git-failure branches `return 0` before the code ever reaches the `last_n` cap block.)

## Item 2 — the new test case genuinely crosses the boundary (independent mutation, not the engineer's self-report)

Copied `bin/retro-inputs.sh` to a scratch file and reverted only the round-1 fix (replaced the `if [ "$last_n" -eq 0 ]` branch with the original unconditional `head -n "$last_n")"` call, verified `bash -n` syntax-clean and diff-identical to the pre-fix logic otherwise). Then, in the real repo's working tree, swapped `bin/retro-inputs.sh` for this reverted copy and ran the actual, committed `tests/retro-inputs/run.sh`:

```
FAIL: case: --last-n 0 is a degenerate but valid cap and must not crash (exit code 1)
... (all other cases still PASS)
FAIL: case: --last-n 0 is a degenerate but valid cap and must not crash (exit code 1)
```

Restored the real file (`git diff --stat bin/retro-inputs.sh` empty afterward, confirming an exact restore), re-ran the suite: green again, all cases including the new one. This is independent confirmation — not a re-run of the engineer's own mutation claim — that the new case is genuinely load-bearing against the shipped fix.

## Item 4 — the template prose relocation is not a hidden deletion, but is not byte-identical either (worth naming precisely)

Read `git diff 6ea822c..59a0549 -- docs/templates/retro-template.md` directly. Finding: no content was deleted — every sentence of the original four-line paragraph survives in the relocated version — but the paragraph was **reworded in place** (`"prints this heading"` → `"prints the `` `## Retro inputs` `` heading below"`, `"right after this heading"` → `"right after that heading"`, the trailing `:` before the list became a `.`) and had **one new sentence appended** explaining *why* it now sits outside the section (`"This paragraph and everything above it stays OUTSIDE the `` `## Retro inputs` `` section on purpose: ..."`). So the engineer's board framing ("何も削除せず、見出し境界を1つ手前へ動かしただけ") is correct in substance — nothing was cut, and the net change is additive/clarifying, not lossy — but it is not a mechanical, byte-exact "move" the way the phrase might suggest. Re-ran `bash bin/check-retro.sh docs/templates/retro-template.md`: exactly one violation remains (the pre-existing, out-of-scope Lesson-候補 bullet), zero `## Retro inputs`-origin violations — confirms the relocation achieves its stated purpose. Not a defect; recorded here only so the distinction ("no deletion" vs. "byte-identical move") is on the record precisely, per this repo's own lesson about insert/move operations that turn out to be lossy — this one is not.

## Item 5 — regression locks re-checked non-vacuous (real, non-trivial values, not just green)

- AC17: all five KPT/Traps string constants present byte-exact in `bin/check-retro.sh`; `bash bin/check-retro.sh .shell-team/retros/2026-07-28.md` passes (dogfood).
- AC24: dead-pointer set is exactly `docs/loop-engineering/loop-traps.md docs/loop-engineering/model-tiering.md` — unchanged, two entries, not zero (would be vacuous) and not three (would be scope creep).
- AC25: `（` count in `bin/check-retro.sh` is 14 on both sides of the comparison (current tree vs. `develop`) — non-zero, so the equality is not vacuously satisfied by an empty pattern match.
- AC26: `git diff --name-only develop` (run against the committed, clean tree) lists 27 real paths, all inside the allow-list, confirming this criterion is reading the actual change set, not a stale or empty one.
- AC27: the full existing-suite list re-run above, all green.

## Item 6 — `--last-n 007` verbatim echo: read as an accepted, non-blocking quirk

Reproduced independently on a 3-merge-commit repo with `--last-n 002` (needed a cap value smaller than the repo's real merge count to actually trigger the capping branch — my first attempt, `--last-n 007` against repos with only 1–3 merges, never entered the cap branch at all since `007` normalizes arithmetically to `7` in the `-gt` comparison and was larger than the available merges):

```
- input: cycle-window — status: read — detail: 002 merge commits from main (first-parent); capped at --last-n 002 (a declared cap, not a truncation)
```

Confirmed: the zero-padded string is echoed verbatim in both `total_used` and the qualifier, consistently with each other (no mismatch between the two occurrences), the line stays a well-formed three-field ledger entry, and AC6's "a `read` detail must contain a digit" rule is satisfied trivially (leading zeros are still digits). **I read this as acceptable, not a defect**: `--last-n` with a zero-padded value is an edge of the input space nobody is likely to hit by ordinary use (nobody normally types `007` for a repeat count), it does not misrepresent anything (the displayed cap matches what was actually requested and shown), and normalizing it would be pure polish with no correctness benefit. Recording the engineer's own explicit, on-the-record decision to leave it as-is (`.shell-team/provenance/T-1001.md`, round-1-rework decision) as reviewed and accepted, per the coordinator's request that this not be silently passed over either way.

## Verdict

All items pass; the round-1 FAIL driver is fixed and independently confirmed (not merely re-checked via self-report); the round-1 non-blocking finding is resolved; no new regression found in the delta. **T-1001 → `READY_FOR_REVIEW`.**

---

# Codex Review — T-1001 (round 1)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1001
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 29 files changed, +2296/-43 lines (`develop...HEAD`; commits `0388b91`, `6ea822c`, `59a0549`, `c8cffaa`)

Primary pass: `codex exec --sandbox read-only review --base develop --json`, raw capture `.shell-team/reviews/T-1001-codex-primary.{txt,jsonl}`. Adversarial pass (triggered by the primary pass's own blockers, focused on the fail-closed ledger mechanism in `bin/check-retro.sh` and `bin/retro-inputs.sh`): `.shell-team/reviews/T-1001-codex-adversarial.{txt,jsonl}`. Every finding below that is load-bearing for the verdict was independently reproduced by the reviewer (not merely read off Codex's output) against throwaway `git init` repositories under `$TMPDIR`, outside this repository's working tree.

All 30 `check:` lines, the full CI suite list, `check-acs`/`check-intent`/`check-provenance`/`check-prompt-sync`, and shellcheck were re-run live and are green, matching QA's round-2 record — none of the findings below are caught by any existing acceptance criterion or fixture; they are new gaps this review surfaces beyond the shipped suite.

#### Blockers

- `bin/check-retro.sh:160` (rule 4, ledger-section detection) — The anchored pattern `/^## Retro inputs$/` used to open the ledger region is **not CRLF-tolerant**, unlike rule 2's heading checks, which were deliberately rewritten to use the CRLF-tolerant `has_exact_line()` helper for exactly this reason (see this task's own provenance decision on `has_exact_line()`). On a CRLF-terminated retro file, `$0` for the heading line is `"## Retro inputs\r"`, which the anchored `$`-terminated pattern never matches, so `seen` is never set to `1`. Two direct consequences, both reproduced: (1) every line inside the section is treated as ordinary prose rather than parsed as a ledger line, and (2) the `END` block's missing-id sweep is itself gated on `if (seen)`, so it is skipped entirely. **The result is that rule 4 — the entire mechanism this task exists to build — becomes a silent no-op for any CRLF-terminated retro.** Reproduction: a deliberately malformed ledger (one line, `- input: cycle-window` with no status/detail fields, the other 7 ids entirely absent) correctly produces 9 violations and exit 1 under LF line endings; the byte-identical content converted to CRLF (`sed 's/$/\r/'`) produces **zero violations and exit 0**. AC16's own "case: a ledger with CRLF line endings still passes" fixture only exercises a *well-formed* CRLF ledger, so it passes for the wrong reason (the check never ran) and never caught this. Fix: extend `has_exact_line()` (or an equivalent CR-stripping compare) to the `## Retro inputs` region-open pattern, the same way rule 2 already handles CRLF.
  - Suggested fix: change the awk pattern from `/^## Retro inputs$/` to a CR-tolerant match (e.g., strip a trailing `\r` from `$0` before testing, mirroring `has_exact_line()`'s own normalization), and add a fixture case asserting that a **malformed** CRLF ledger fails, not only that a well-formed one passes.

- `bin/retro-inputs.sh:293-313` (`report_dir_input()` / `count_files()`) — A directory that is **readable but not searchable** (POSIX mode `0400`: the `r` bit is set, but the missing `x`/search bit prevents traversing or `stat`-ing entries) passes the `[ ! -r "$dir" ]` guard, then `count_files()`'s glob silently expands to zero matches (nullglob, no error), so the input is reported as **`empty`** — even though the directory demonstrably holds files that could not be enumerated. This is the exact central invariant this whole task exists to guarantee (never report `empty` when the truth is `unavailable`), violated for a directory-permission cause rather than a git-history cause. Reproduction: a throwaway repo with `.shell-team/reviews/` containing two real `.md` files, `chmod 0400` applied to the directory itself → `bin/retro-inputs.sh --base main` reports `- input: review-artifacts — status: empty — detail: 0 review artifacts in .shell-team/reviews`. The adversarial pass independently flagged the same root cause from a different angle (TOCTOU: the pre-condition checks and the enumeration are two separate operations, so *any* enumeration failure after the checks — not just the missing-execute-bit case — collapses to the same false `empty`); both point at the same underlying defect: `count_files()` has no channel to report "enumeration failed" distinct from "enumeration found 0 entries."
  - Suggested fix: check search/execute permission explicitly (or, better, capture the exit status of the enumeration itself — e.g. via `find "$dir" -maxdepth 1 ...` with its exit code checked — rather than trusting the glob's silent zero-match behaviour), and report `unavailable` whenever enumeration cannot be proven to have succeeded, not just when the pre-condition checks fail.

#### Major

- `agents/scrum-master.md:25` and `agents/scrum-master.md:76` — Both instruct invoking the literal relative path `` `bin/retro-inputs.sh` `` as the primary command, rather than the bare `` `retro-inputs.sh` `` name that this same file's own operating-paths note (line 13) and every other agent file in this repository (`engineer.md`, `qa-verifier.md`, `codex-reviewer.md`, `drift-evaluator.md`, `triage-orchestrator.md`, `ui-designer.md`) use as the established "bare name on PATH when the plugin is loaded; else `bin/<script>.sh`" convention. A command containing a `/` bypasses `PATH` lookup entirely. In an **adopting** repository — the primary deployment target this plugin ships to, per this repo's own `CLAUDE.md` ("this ships to repositories we know nothing about") — the plugin adds its own `bin/` directory to `PATH`, but does not create a `bin/retro-inputs.sh` inside the adopter's own repository tree, so the literal relative-path invocation fails, and the agent silently falls back to "generate a partial retro" (line 48) — quietly defeating this task's entire purpose (a reliable, git-derived acquisition mechanism replacing the broken `gh`/`--base main` one) for the audience it was built for. It happens to work in this dogfood repository only because `bin/` is coincidentally a real subdirectory of this repo's own root. Not classified as a blocker because the degradation is graceful (an explained partial retro, not a crash or a silent misreport) rather than because the defect is narrow. No acceptance criterion catches this: AC30 only greps for the substring `retro-inputs.sh`, which is present either way.
  - Suggested fix: change both invocation points to the bare `retro-inputs.sh` form, matching this same file's own line 13 and every sibling agent file's phrasing.

- `bin/retro-inputs.sh:190-195` (`compute_cycle_window()`, shallow detection) — Shallow-repository detection resolves `gitdir="$(git rev-parse --git-dir)"` and then tests `"$gitdir/shallow"`. Inside a **linked worktree** (`git worktree add`), `--git-dir` returns the worktree-*private* git directory (e.g. `.git/worktrees/<name>`), which never carries the `shallow` marker — the marker lives only in the *common* directory (`git rev-parse --git-common-dir`), per git's documented repository layout. Reproduced directly: in a `git worktree add --detach` checkout of a repo carrying a simulated `.git/shallow` marker, `--git-dir` and `--git-common-dir` diverge as described, and `git rev-parse --is-shallow-repository` (a worktree-aware plumbing command) correctly reports `true` where the script's own `"$gitdir/shallow"` check would find nothing. Run from inside such a worktree, the script silently computes `shallow=0` for a genuinely shallow history, again risking the exact `empty`-for-`unavailable` substitution (or a `read` status that omits the truncation warning) this task exists to prevent — the same central invariant as the two blockers above, just gated behind a less common combination (shallow clone **and** linked worktree together), which is why this is Major rather than Blocker.
  - Suggested fix: use `git rev-parse --is-shallow-repository` (or resolve the shallow marker via `git rev-parse --git-path shallow`, never by hand-appending `/shallow` to `--git-dir`).

#### Minor / nits

- `bin/retro-inputs.sh:240-244` — When a repository is **both** shallow **and** the requested `--last-n` cap is smaller than the merges visible within the boundary, only the "shallow clone truncates history at the boundary" qualifier is emitted; the "capped at --last-n N" qualifier is silently dropped (the `if`/`elif` chain only ever emits one). Reproduced: a 3-merge shallow repo with `--last-n 2` reports `2 merge commits ... shallow clone truncates history at the boundary`, with no mention that `2` came from the cap, not the boundary. `CW_STATUS` stays correctly `read` either way, so this is an informational precision loss, not a status misclassification.
- `bin/retro-inputs.sh:210-249` — The "`develop` not found locally, fell back to HEAD" qualifier is appended only in the non-zero-merge (`read`) branch of `compute_cycle_window()`; the zero-merge (`empty`) and shallow-zero (`unavailable`) early-return branches never declare that the ref used was a fallback rather than an explicit ask. Reproduced: a `develop`-less, zero-merge repo reports `0 merge commits reachable from HEAD ..., not a truncation` with no fallback note. AC11's own fixture case ("no develop branch falls back to HEAD and declares the fallback") is built only against a merge-having repo, so this gap is invisible to the shipped suite.
- `bin/check-retro.sh:160` (region-open logic) — A **second** `## Retro inputs` heading later in the same document is never re-opened for validation (`!seen` blocks it permanently once the first section has been seen and closed), so a duplicated or forged second ledger is silently skipped. Grounded out-of-input-space: the spec's own Non-goal/Out-of-scope item 3 explicitly declines to harden the section parser against "a retro crafted to defeat the section parser," and this is not reachable via untrusted git/PR content — `sanitize()` already prevents a forged raw `##`-prefixed line from ever being emitted from merge-subject/PR-title material (it is only ever embedded as an indented sub-bullet). Downgraded to minor per the input-space rule; see ledger.
- `bin/check-retro.sh:201` — The detail-emptiness check only rejects a zero-length string (`de == ""`); a detail consisting entirely of whitespace passes. Not reachable via `retro-inputs.sh`'s own output (every emitted detail carries substantive text) or via untrusted git content; only a hand-crafted malformed document could trigger it.
- `bin/retro-inputs.sh:176` — The default-`develop` probe (`git rev-parse --verify --quiet`) does not distinguish "ref genuinely absent" from "an operational git error" before falling back to `HEAD`; both take the same "else" branch. The practical blast radius is narrow: if git itself is broken (`safe.directory` refusal, unreadable object store), the very next line re-verifies `HEAD` with the same command and correctly reports `unavailable` when that also fails, so the only scenario this actually loses precision on is a narrow ambiguous-ref case where `develop`'s probe fails for a reason other than absence while `HEAD` resolves fine.
- `bin/retro-inputs.sh:140-144` (`sanitize()`) — Strips CR, LF, TAB, backtick, and replaces the em-dash separator, but not other Unicode line-separator or bidi-control characters (U+2028, U+2029, ESC, bidi overrides) from untrusted merge-subject/PR-title text. Confirmed this does **not** defeat `bin/check-retro.sh`'s machine parsing (awk's record splitting is unaffected by non-ASCII "line-like" characters, so the ledger stays structurally correct), but is a legitimate hardening gap for the same prompt-injection-adjacent concern this file already names for PR bodies, since the sanitized material sub-bullets are read directly by the scrum-master LLM agent.

#### Adversarial findings

Devil's-advocate pass against `bin/check-retro.sh` rule 4 and `bin/retro-inputs.sh`'s empty-vs-unavailable acquisition invariant, given the two already-confirmed blockers as known findings not to re-surface. Six findings returned; five distinct new observations after merging one duplicate root cause:

1. A second `## Retro inputs` heading is never re-validated (merged into the Minor/nits duplicate-heading item above).
2. A whitespace-only `detail:` passes the emptiness check (Minor/nits above).
3. The `develop`-probe's git-error-vs-absence conflation (Minor/nits above).
4. The linked-worktree shallow-detection bypass (promoted to Major above; independently reproduced by the reviewer, not merely accepted from Codex's text).
5. Enumeration-failure-swallowed / TOCTOU in `report_dir_input()`/`count_files()` — folded into the Blocker above as the general form of the same defect (the fix needs to capture enumeration success/failure, not just gate on pre-condition permission bits).
6. Unicode line-separator / bidi-control characters not stripped by `sanitize()` (Minor/nits above).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | scrum-master.md relative-path invocation (`bin/retro-inputs.sh`) | primary: P1 (→ blocker per map) | major | Degrades gracefully to an explained partial retro rather than crashing or misreporting; downgraded one level from the mapped default on that basis. |
| 2 | unsearchable-but-readable directory reported `empty` | primary: P2 (→ major per map) | blocker | Escalated: this is a direct, reproduced instance of the exact central invariant ("never report `empty` when the truth is `unavailable`") the task exists to guarantee, for one of the five directory-backed inputs the review brief named explicitly. |
| 3 | shallow-repo + `--last-n` cap: cap qualifier dropped | primary: P2 (→ major per map) | minor | Status stays correctly `read` in both branches; the loss is an informational qualifier only, not a status misclassification — no central-invariant break. |
| 4 | `develop`-fallback not declared outside the `read` branch | primary: P2 (→ major per map) | minor | Same reasoning as #3: status (`empty`/`unavailable`) is still correct; only the "this came from a fallback" annotation is missing in two of three branches. |
| 5 | duplicate `## Retro inputs` heading defeats region-reopen | adversarial: high (→ major per map) | minor (out-of-input-space) | Grounds on spec `## Input space` → Out-of-scope synthetic extremes item 3: "A retro crafted to defeat the section parser... this task adds a section, it does not harden markdown parsing." Not attacker-reachable via untrusted git/PR content since `sanitize()` already blocks a forged raw heading line from ever being emitted. |
| 6 | enumeration-failure-swallowed / TOCTOU in `count_files()` | adversarial: high (→ major per map) | folded into blocker #2 | Same root cause as the already-Blocker finding (`count_files()` cannot distinguish "0 found" from "enumeration failed"); listing it separately would double-count one defect under two severities. |
| 7 | linked-worktree shallow-detection bypass | adversarial: high (→ major per map) | major | Matches the mapped default; independently reproduced by the reviewer (git-dir/common-dir divergence, `--is-shallow-repository` mismatch), kept at major rather than escalated to blocker because it requires the narrower shallow-clone-and-worktree combination rather than a single common condition. |
| 8 | whitespace-only `detail:` passes emptiness check | adversarial: medium (→ minor per map) | minor | Matches the mapped default; no change. |
| 9 | `develop`-probe git-error vs absence conflation | adversarial: medium (→ minor per map) | minor | Matches the mapped default; no change. |
| 10 | Unicode line-separator/bidi controls not stripped by `sanitize()` | adversarial: medium (→ minor per map) | minor | Matches the mapped default; confirmed it does not defeat the machine-parsed ledger (awk record splitting unaffected), consistent with minor rather than a higher severity. |

**Issue-canon note**: this spec's Problem/Non-goals sections characterize GitHub issue #28's proposed directions, trigger points, and prior measurements (e.g., "nine merged pull requests") in some detail. This review has no GitHub access and did not independently fetch issue #28 to confirm those characterizations against its actual body — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). None of the findings above depend on issue #28's exact wording; all are directly reproduced against the shipped code.

#### Recommendation

- REQUEST_CHANGES → set T-1001 back to READY_FOR_ENG with the two blockers and two majors above. The minor/nit items are not required for this round; see Fast-follow disposition below for each.

#### Fast-follow disposition

- Shallow+capped qualifier loss (`bin/retro-inputs.sh:240-244`): file-an-issue — small, well-defined fix (declare the cap even inside the shallow branch), low urgency since the status itself stays correct.
- `develop`-fallback not declared outside the `read` branch (`bin/retro-inputs.sh:210-249`): file-an-issue — same family and same low urgency as the item above; a natural pair to fix together.
- Duplicate `## Retro inputs` heading defeats region-reopen (`bin/check-retro.sh:160`): won't-fix — the spec's own Non-goal declines to harden the section parser against parser-defeating markdown, matching this repository's existing precedent (`bin/check-handoff.sh`'s identically-declared weakness).
- Whitespace-only `detail:` passes the emptiness check (`bin/check-retro.sh:201`): file-an-issue — trivial regex tightening (`^[[:space:]]*$`), low urgency since it is not reachable via any automated path.
- `develop`-probe git-error vs absence conflation (`bin/retro-inputs.sh:176`): won't-fix — the very next resolution check already re-verifies and correctly reports `unavailable` if git itself is broken; the residual gap is a narrow ambiguous-ref edge with no observed practical impact.
- `sanitize()` does not strip Unicode line-separator/bidi-control characters (`bin/retro-inputs.sh:140-144`): file-an-issue — legitimate hardening item in the same family as this file's own PR-body prompt-injection avoidance; does not affect the machine-parsed ledger, so it is not urgent enough to block this merge.

---

# QA round 3 — T-1001 v2 verification (commits `af5b71a` + `6699013`)

**Verdict: PASS**

Scope of this round, per the coordinator's framing: not a delta review. v1's own status-determination structure was replaced (design-premise reversal after Codex `REQUEST_CHANGES` + rework-digest `same-class-repetition (x3)`), so this round re-verifies that the properties confirmed sound in round 1/2 (the git-derived cycle window, gh demotion, the eight-input ledger, the region walk) still hold under the new implementation, and specifically audits whether the inversion (`unavailable`-first, promotion-gated `read`/`empty`) actually closes the omission class rather than adding a fourth guard.

## Structural gates (re-run, all green)

- `bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` → 33/33 PASS.
- `bash bin/check-intent.sh` → aligned (v2, `e95d27620821eee6448e0009eb1b4352fbcd1432`).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1001.md` → conformant (15 decision entries, 0 sentinel — the 5 new ones are well-grounded and specific to this round's redesign, including the two empirically-found errexit/bash-3.2 bugs).
- `bash bin/check-prompt-sync.sh`, `bash bin/check-handoff.sh .shell-team/todo.md` → green/clean.
- `shellcheck` on `bin/retro-inputs.sh tests/retro-inputs/run.sh tests/retro-inputs/fixtures/gh tests/retro-inputs/fixtures/git bin/check-retro.sh tests/check-retro/run.sh` → clean.
- `tests/retro-inputs/run.sh`, `tests/check-retro/run.sh`, `tests/team-paths/run.sh`, `tests/team-init/run.sh`, `tests/check-prompt-sync/run.sh`, `bash bin/retro-inputs.sh --base HEAD`, `bash bin/check-retro.sh .shell-team/retros/*.md` → all green.
- Working tree clean throughout; all scratch fixtures built under `$TMPDIR`, outside this repository.

## Item 1 — the inversion sweep: no bypass path found outside the two promotion functions

Read the whole script for every write to an `ST_*` (status) variable. Exactly three sites: the init loop (`for id in $IDS; do var="ST_${id//-/_}"; printf -v "$var" '%s' "unavailable"; done`, runs once, before any probe), `promote_read()`, and `promote_empty()`. `note()` — the only other function that touches per-input state — writes exclusively to `DE_*` (detail) variables and never to `ST_*`. `emit_all` only *reads* `${!st_var}` via indirect expansion; it does not write. There is exactly one place that formats a ledger line (`printf -- '- input: %s — status: %s — detail: %s\n'`, one occurrence, matching AC2's own positive control). No `eval` touches these variables (the only `eval` in the file is the pre-existing `team-paths.sh --export` resolver plumbing, unrelated). The init loop runs textually before every probe call and is not re-entered afterward (only one `for id in $IDS` initializing block; the second occurrence, in `emit_all`, is the read-only emission loop). I did not find an initialization-order hole, a direct `emit_ledger`-shaped path (that function no longer exists — grepped for it, zero hits), or any route from a probe to a benign status that skips `promote_read`/`promote_empty`.

One design note, not a defect: the "8 call sites" are not 8 independently-implemented code paths. The five directory-backed inputs (review-artifacts, provenance, specs, run-telemetry, previous-retro) share ONE function (`report_dir_input`) and thus ONE textual `promote_read`/`promote_empty` call site each (2 of the 8), invoked 5 times at runtime with different arguments. This is actually a strength relative to v1, not a gap: v1's defect was N independently-hand-written guards where "N right and 1 wrong" was possible; v2's directory-input class is a single, shared, tested code path used identically by all five ids, so there is no room for four of five directory inputs to behave correctly while a fifth diverges.

## Item 2 — DS fixtures verified against their real assertions, not their labels; DS-4 and DS-8 independently reproduced

Read `tests/retro-inputs/run.sh` in full and reproduced DS-4 and DS-8 (the two the coordinator flagged) with fixtures of my own construction, not the shipped ones:

- **DS-4** (a directory with only non-matching files, made non-traversable via `chmod 0600`): built my own throwaway repo with a `.shell-team/reviews/notes.txt` (non-`.md`) and `chmod 0600` on the directory. `bin/retro-inputs.sh` reports `review-artifacts — status: unavailable — detail: directory entries could not all be stat'ed (0/1 enumerated) -- not traversable`. Confirms the guard catches the "would look empty" shape specifically, not only the "would look non-empty" shape DS-3 already covers.
- **DS-8** (a `gh pr list` failure, distinct from a confirmed-empty result): built my own `gh` stub where `auth status` succeeds but `pr` exits 1 with no output. `bin/retro-inputs.sh` reports `pr-metadata — status: unavailable — detail: gh pr list failed` — never conflated with the "0 merged pull requests returned by gh" (`empty`) case, which is exactly the distinction a naive "empty output ⇒ confirmed zero" bug would erase.

Also spot-checked DS-1 (shallow-probe-unanswerable, via the shipped `git` stub) and DS-3 (matching-file, non-traversable directory, my own fixture): both independently reproduced correctly.

## Item 3 — round 1's five degenerate-history classifications, re-run against the rewritten script

Reused round 1's own throwaway repos (missing ref, zero-merge-non-shallow, shallow-zero, shallow-with-merges, git-log-failure) against the current `bin/retro-inputs.sh` — all five still classify correctly (`unavailable`/`empty`/`unavailable`/`read`-with-truncation/`unavailable`, in that order, all exit 0, all complete 8-line ledgers). Additionally, and this is the more important confirmation for this round: **built a fresh linked-worktree fixture of my own** (`git worktree add`, shallow marker on the common directory, zero merges) and ran it against both the current script and the round-2 script pulled from git history (`59a0549`) side by side:

- Round-2 script (old `$(git rev-parse --git-dir)/shallow` probe, worktree-blind): reports `cycle-window — status: empty — detail: 0 merge commits ... confirmed complete (not a truncation)` — **the exact defect the spec describes, concretely reproduced**, not merely theoretical.
- Current script (`git rev-parse --is-shallow-repository`): reports `cycle-window — status: unavailable — detail: shallow repository: 0 merge commits found within the boundary ...` — correct.

Did the same side-by-side for the non-traversable-directory defect (my own DS-4-shaped fixture): the round-2 script reports `review-artifacts — status: empty — detail: 0 review artifacts in .shell-team/reviews` (wrong — a read-permission-only check cannot see the directory isn't traversable), the current script reports `unavailable` (correct). And for the CRLF defect, with a broken ledger of my own construction (one id present with no status, one line, seven ids missing): the round-1-era `check-retro.sh` reports 9 violations on the LF file and **0 violations, exit 0** on the CRLF-converted copy of the *same* broken file (the defect, concretely reproduced); the current `check-retro.sh` reports the same 9 violations on both the LF and CRLF copies.

## Item 4 — AC17's mutation self-check: genuinely runs, never touches the real script

Read `tests/check-retro/run.sh`'s mutation block and reproduced it independently: copied `bin/check-retro.sh` to a scratch file, applied the same `sed` range (`/# Rule 4:/,$`) targeting only the `sub(/\r$/, "", line)` that appears *after* the `# Rule 4:` marker — confirmed there are exactly two occurrences of that literal in the whole file (line 144, rule 3's Lesson-候補 walk, *before* the marker; line 201, rule 4's region walk, *after* it), so the `sed` range provably cannot touch rule 3's or `has_exact_line`'s own CR handling. Ran the mutated copy against my own broken-CRLF fixture: still reports the violation (via the agreement backstop — heading found by `has_exact_line`, region never entered by the crippled awk, so "section heading present but ledger region could not be read" fires) — confirming the backstop is load-bearing, independent of the suite's own broken-ledger fixture. Went one step further than the shipped suite: ran the mutated copy against a **well-formed** CRLF ledger too (`pass-canonical.md` converted to CRLF) — the real script passes it (rc=0, correctly), the mutated copy correctly fails it too (rc=1, via the same backstop plus "ledger-shaped line outside the section" for every now-unexamined line), demonstrating the backstop fires on *any* CRLF file once the region walk's own CR handling regresses, not only the one shape the suite happens to fixture. `git status --short bin/check-retro.sh` confirmed empty throughout — the real script was never touched.

## Item 5 — the two empirically-found bug classes: swept for recurrence, and the existing regression suite does not cover them

Grepped `bin/retro-inputs.sh`, `bin/check-retro.sh`, and `tests/retro-inputs/run.sh` for both patterns:

- `cmd; rc=$?` (unguarded, pre-`if`): zero occurrences outside comments describing the pattern by name. All three actual sites (the default-ref probe, the ref-verify, the shallow-probe) use the safe `if cmd; then rc=0; else rc=$?; fi` idiom.
- `"${arr[@]}"` array expansion: exactly one occurrence (`count_dir_entries`'s `"${names[@]}"`), already guarded by `[ "$DIR_N_NAMES" -gt 0 ]` before entry.

Checked whether `tests/errexit-safe/run.sh` (the coordinator's suggested cross-reference) covers either class: **it does not.** That suite is T-096's regression harness for a *different* errexit-safety class entirely (a `printf ... >&2; exit N` die/fail shape breaking under closed stderr) — its completeness self-audit does not scan `bin/retro-inputs.sh` at all (not in its candidate-file list), and neither pattern this round found (the bare `cmd; rc=$?` capture-after-set-e-trip, and the bash-3.2 empty-array unbound-variable issue) is anything its audit looks for. **This is a real, non-blocking gap**, not a FAIL driver: both actual occurrences in the touched files are fixed and I found no recurrence, but there is no dedicated, named regression lock for either class the way T-096 has one for its own — the only protection against a future regression reintroducing either pattern is direct code review (this round's, and the reviewer's) plus the incidental fact that a reintroduced `cmd; rc=$?` bug would likely crash `tests/retro-inputs/run.sh`'s own DS-1 / ref-absent / shallow-cases outright (a hard, noisy failure) rather than passing silently, since none of those command substitutions are `||`-guarded in the test script itself. Flagging this as a fast-follow candidate for whoever owns cross-cutting `bin/` hygiene, not as something this task must additionally fix.

## Item 6 — the declined Unicode-line-separator/bidi-control minor: reasoning read and accepted

Read the Non-goals section's three stated reasons and the machine-parsed-surface claim independently: confirmed `awk`/`grep`'s record separator is LF only, and neither U+2028/U+2029 nor a bidirectional control character is a record separator for either tool — so AC13's "cannot forge a ledger line" property is genuinely unaffected by leaving them unstripped (nothing about a ledger line's *parseability* depends on this). Confirmed `sanitize()` is byte-identical to the pre-v2 script (diffed the function body directly: identical). The residual concern (a human reader seeing a re-ordered/hidden line in a rendered retro) is real but is a different requirement from what this mechanism defends, and correctly scoped to a repository-wide content guard rather than one emitter — a retro is a committed, human-reviewed file, and a hostile bidi character could be introduced by hand regardless of what `retro-inputs.sh` does. The scope-discipline argument (changing `sanitize()`'s tool, since `tr -d` is byte-oriented and cannot delete a multi-byte code point, would reopen the injection-relevant surface in the same round as the status-determination redesign) is sound and consistent with this round's own discipline elsewhere. **Read and accepted, not a defect.**

## Item 7 — AC26/AC28/AC29 (merge-point-scoped) re-checked non-vacuous

- AC26: both positive controls fire (`git show develop:agents/scrum-master.md` still contains the pre-task `--base main` blob; the recursive `grep -rqF -- 'retro-inputs' agents bin skills templates` finds real content, proving the directories are actually being read) and the negative (`--base main` absent from the whole operative surface) holds.
- AC28: `（` count is 14 on both sides (current tree vs. `develop`) — non-zero, not a vacuous empty-pattern match.
- AC29: `git diff --name-only develop` lists 32 real paths (grew from round 1/2's 27, reflecting the new CRLF/duplicate-section/line-outside-section fixtures), all inside the allow-list, confirming the criterion reads the actual, current change set on a clean, committed tree.

## Additional independent checks (Major fix, round-1 fixes surviving the rewrite)

- **AC33 (bare-name invocation, the round-3 Major)**: independently re-counted in `agents/scrum-master.md` — `bin/retro-inputs.sh` occurs 2 times, `bash bin/retro-inputs.sh` occurs 2 times (every relative-path mention is preceded by `bash `), and bare `` `retro-inputs.sh` `` occurs 5 times. Matches AC33's mechanical check exactly.
- **Round 1's `--last-n 0` fix**: still correct in the rewritten script (`read`, 0 shown, cap declared) — reconfirmed as part of item 1's three-state check.
- **Round 1's template-prose relocation**: still correct — `bash bin/check-retro.sh docs/templates/retro-template.md` reports only the pre-existing, out-of-scope Lesson-候補 bullet violation; zero `## Retro inputs`-region violations.

## Verdict

No same-class recurrence found: the inversion sweep is complete (verified by reading, not by trusting the count), all fixture-label claims checked match their assertions (including independent reproductions with my own fixtures, not the shipped ones), all three originally-reproduced defects are concretely demonstrated fixed (old-vs-new side by side, not merely asserted), the mutation self-check is genuinely load-bearing, and the declined minor's reasoning holds up. One real, non-blocking gap recorded above (item 5 — no dedicated regression lock for the two empirically-found bug classes) as a fast-follow candidate, not a defect in this deliverable. **T-1001 → `READY_FOR_REVIEW`.**

---

# Codex Review — T-1001 (round 3, v2 design-premise reversal)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1001
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: cumulative `develop...HEAD` 32 files, +3358/-43 (commits `eebb98d` spec v2, `af5b71a` implementation, `6699013` provenance/board, `d65b996` board); round-to-round `6016bd3..HEAD` 13 files, +1545/-563

Primary pass and adversarial pass (triggered by the primary pass's own findings), both against the v2 diff: `.shell-team/reviews/T-1001-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1001-codex-adversarial.{txt,jsonl}` (canonical basenames, reused from round 1 per the naming convention — these now hold round-3 content, not round-1's; round 1's content is preserved unmodified in this same review file, above). Every load-bearing finding below — both the round-2 fixes confirmed sound and the two new blockers — was independently reproduced by the reviewer against throwaway `git init` repositories and a bare-bones function extraction, not merely read off Codex's output.

## Round-2 items: verified fixed, independently reproduced

The coordinator asked five specific things to be checked. All five hold up under independent reproduction, not just re-reading the code:

1. **The inversion sweep has no bypass.** Grepped the script myself for every write to an `ST_*` variable: exactly three sites (the init loop, `promote_read`, `promote_empty`), matching AC2's own positive control. No `eval`/`declare` touches these variables. Matches QA round 3's own finding exactly.
2. **`eval "$RESOLVER_EXPORTS"` (line 352) is not a same-class recurrence and not a new threat vector.** Independently confirmed: it is the only `eval` in the script; every value `team-paths.sh --export` emits is `%q`-quoted (verified in `team-paths.sh` directly); the only externally-influenceable input feeding it is `$TEAM_RUN_BASE`, an operator/CI-level environment variable (not attacker/PR-content), already validated by `validate_base()` (rejects absolute paths, `..`, `~`, whitespace) before use. Also confirmed this is a **pre-existing** repository convention, not something T-1001 introduced: `bin/team-init.sh:146` already does the identical capture-then-eval against the same resolver output. Concur with the coordinator's own read: unreachable as a threat vector, and orthogonal to the "unevaluable-condition-reported-as-benign" class this rework targets (a different threat category — arbitrary-eval — not a status-misclassification one).
3. **All eight DS rows match their implementation.** Read each of DS-1 through DS-8 against the actual code paths that implement them; no declaration/implementation gap found. Independently reproduced DS-1 (unanswerable shallow probe, via the shipped `git` stub), DS-3/DS-4 (non-traversable directory, my own `chmod 0400` fixture with 2 real files — confirmed `unavailable`, not `empty`), and DS-7/DS-8 (gh-unauthenticated vs. gh-list-failure, both correctly distinct from a confirmed-empty result).
4. **AC17's mutation self-check is genuine.** Confirmed the `sed -i '/# Rule 4:/,$ ...'` range in `tests/check-retro/run.sh` operates on a `cp`-created scratch file (never the real `bin/check-retro.sh`) and targets only the rule-4 occurrence of `sub(/\r$/, "", line)` (line 201) — the identical literal at line 144 belongs to rule 3 and sits *before* the `# Rule 4:` marker the `sed` range starts from, so it is provably untouched.
5. **The two empirically-found bash-3.2/errexit bugs from round 2 do not recur, and `tests/errexit-safe` genuinely does not cover this class.** Independently grepped for the bare `cmd; rc=$?` shape (zero hits outside a comment describing it by name) and the unguarded `"${arr[@]}"` shape (one hit, already guarded by a length check). Read `tests/errexit-safe/run.sh`'s own completeness-audit pattern directly (`>&2; *exit [0-9]+` and siblings) — it targets a structurally different bug shape (T-096's die/fail inline-exit class) and does not scan `bin/retro-inputs.sh` at all. **Ran the actual script under the real `/bin/bash` 3.2 on this machine** (confirmed `GNU bash, version 3.2.57`) against a genuinely empty directory — the specific shape that would trip the pre-fix unbound-variable hazard — and it passed cleanly. Concur with QA: a real, non-blocking gap (no dedicated regression lock for either bug class), fast-follow, not a defect in this deliverable.

Also independently re-confirmed, with fresh fixtures of my own construction (not the shipped suite's): the CRLF fail-closed fix (a malformed ledger — one id, no status/detail, seven ids missing — now reports the same 9 violations under both LF and CRLF, where it reported 0 under CRLF before), the mode-0400 non-traversable-directory fix (2 real files → `unavailable`, not `empty`), the linked-worktree shallow-detection fix (`git rev-parse --is-shallow-repository` correctly reports `true` from inside a `git worktree add` checkout, where the old `$(git rev-parse --git-dir)/shallow` check missed it — reproduced the exact `empty`-for-`unavailable` substitution on the *old* script side-by-side against the *current* script from the same worktree), the shallow+cap dual-qualifier fix (both "shallow clone truncates..." and "capped at --last-n N..." now appear together), the fallback-declaration fix (now appended in the `empty` and `unavailable` branches too, not only `read`), the duplicate-heading fix (a forged second `## Retro inputs` section is now flagged both as "duplicated ## Retro inputs section heading" and as a "ledger-shaped line outside the section"), the whitespace-only-detail fix, and the bare-name invocation fix in `agents/scrum-master.md` (both occurrences of `bin/retro-inputs.sh` are now preceded by `bash `, and the bare `retro-inputs.sh` form is primary). Also verified a genuinely empty *and* non-traversable directory (mode 0400, 0 real entries) still correctly reports `empty`, not `unavailable` — confirming the fix determines enumerability rather than over-correcting into a new false-`unavailable`.

Also independently re-confirmed: `bash bin/check-acs.sh` 33/33, `check-intent` aligned (v2), `check-provenance` conformant (15), `check-prompt-sync` green, all suites green, shellcheck clean, gh/develop/main/merge-commit/non-shallow/Japanese-heading requirements not reintroduced (grepped directly), Japanese-paren count in `check-retro.sh` unchanged at 14 vs. `develop`.

## Blockers — new this round, a same-class recurrence in a different form

**Both of the following are the same underlying failure the whole v2 rework exists to eliminate — a probe that should leave the safe `unavailable` default standing instead aborts the ENTIRE script — recurring through plumbing the v2 rewrite introduced but did not itself stress-test for this specific hazard.** Both were found by Codex (primary pass found the first; the adversarial pass, run because of it, found the second) and both are independently reproduced below with a real, unmodified copy of the script — not a synthetic snippet.

- `bin/retro-inputs.sh:320` (`compute`/`probe_cycle_window`, the `--last-n` capping branch) — `shown="$(printf '%s\n' "$log_out" | head -n "$last_n")"` crashes the **entire script** with exit 141 (SIGPIPE) and **zero output** — not even a partial ledger — whenever the merge-commit history is large enough that `printf`'s output exceeds the pipe buffer and `head` closes its read end after satisfying `-n` before `printf` finishes writing. `set -o pipefail` combined with `set -e` propagates `printf`'s SIGPIPE-death as a fatal script error. **Reproduced against a real repository**: built a throwaway repo with 1200 real merge commits via `git commit-tree` plumbing (not a synthetic string) and ran the unmodified script with `--base main --last-n 5` — exit 141, completely empty stdout and stderr. Confirmed the same repo works fine (exit 0, correct ledger) *without* `--last-n`, isolating the trigger to the capping pipe specifically. Bisected the practical threshold on this machine: 400 and 700 merges do not trigger it, 1200 does — a scale entirely plausible for a multi-year, actively-maintained open-source project (this plugin's stated deployment target), not a synthetic extreme. Verified a fix: replacing the pipe with a here-string (`head -n "$last_n" <<< "$log_out"`) avoids the crash and produces the correct capped output, because a here-string has no live upstream writer process that can receive SIGPIPE.
- `bin/retro-inputs.sh:383` (`count_dir_entries`, shared by all five directory-backed inputs) — `case "$f" in *"$suffix") [ -f "$f" ] && DIR_N_MATCH=$((DIR_N_MATCH + 1)) ;; esac` crashes the entire script with exit 1 and **zero output** whenever a directory entry matches the expected suffix (`.md`/`.jsonl`) but is not a regular file — a directory literally named e.g. `subdir.md`, a broken symlink, a FIFO, or a socket. Found by the adversarial pass; independently reproduced three ways with increasing isolation: (1) against the real, unmodified `bin/retro-inputs.sh` with a throwaway repo whose `.shell-team/reviews/` contains only a directory named `subdir.md` and a broken symlink `brokenlink.md` — exit 1, empty output; (2) the same with only the directory (no symlink) — exit 1, empty output, isolating the trigger to the directory case specifically; (3) a verbatim, standalone extraction of `count_dir_entries` exactly as written, called directly against the same fixture — reaches the same crash with total certainty about the root cause. Since all five directory-backed inputs (`review-artifacts`, `provenance`, `specs`, `run-telemetry`, `previous-retro`) share this one function with no per-id branching, this is reachable from any of the five, on a repository that is otherwise completely ordinary (no scale, no permission tricks, no worktree needed — a single misplaced directory or dangling symlink in any of the five watched directories is enough). Verified a fix: replacing the `&&`-chained test-then-increment with an explicit `if [ -f "$f" ]; then DIR_N_MATCH=$((DIR_N_MATCH + 1)); fi` — the false branch then completes normally rather than making the whole `&&`-list (and, empirically, the enclosing `case`/`for`/function) exit non-zero under `set -e`.

**Why this matters beyond "two more bugs to fix":** both crash sites are inside code v2 wrote fresh for this rework (the capping-and-material-composition logic around `head`, and the entirely new `count_dir_entries` function) to fix the *previously reported* defects. The inversion's own determination logic — which of `read`/`empty`/`unavailable` a probe promotes to, once it runs to completion — is sound and was audited thoroughly this round (see above). What was not audited to the same standard is whether the **plumbing surrounding each determination** (a pipe whose downstream consumer can exit early, a `&&`-chain's short-circuit interacting with `set -e` inside nested `case`/`for`/function contexts) can itself abort the whole process before a determination — or even the safe default — is ever reached. That is a sibling hazard to the one DP-2 names explicitly ("a probe that returns early, aborts under errexit, or has an unhandled branch produces unavailable rather than nothing"), but it manifests as *no ledger at all* rather than one wrong line, which is a strictly worse failure than any of the three original defects (at least those preserved a complete-but-wrong ledger `empty` in six of eight cases; this produces nothing, on stdout or stderr, and an undocumented exit code the script's own header does not list as a valid outcome).

## Minor / nits (from the primary and adversarial passes)

- `bin/retro-inputs.sh:513` (`emit_material_block`'s here-string) — Codex flagged that bash historically implements a here-string via a temporary file, which could fail under `TMPDIR` unavailability. **Tested directly and did not reproduce**: ran the real script with `TMPDIR=/nonexistent-...` and separately with an existing-but-`chmod 0500` `TMPDIR`, both against a repo with real cycle-window material to emit — exit 0, correct output, in both cases, under the actual `/bin/bash` 3.2 on this machine. Not escalated; recorded because a zero-cost hardening (replacing the here-string with an equivalent `printf | while read` pipe, which the script already uses in two other places) would remove the theoretical dependency entirely even though the failure mode itself could not be reproduced here.
- `bin/retro-inputs.sh:445` (`probe_lessons`, `grep -c '.'`) — a lessons file whose non-blank lines are only whitespace (e.g. all-space or all-tab lines) is promoted to `read` with a nonzero count, since `.` matches a space. Real but low-severity and definitional: the file genuinely was read successfully (this is a `read`-vs-`empty` distinction, not a `benign`-vs-`unavailable` one, so it is orthogonal to the central invariant this rework protects), and it matches the code's own literal, consistent definition of "non-blank".
- `bin/retro-inputs.sh:311` (`--last-n` cap comparison) — a `--last-n` value exceeding the shell's integer range makes `[ "$total" -gt "$last_n" ]` error ("integer expression expected") rather than crash (the failing test is in an `&&`-list's first position, the same shape as the directory-crash blocker above, but here it degrades to "cap silently not applied" rather than aborting, because the surrounding context differs), so an absurd cap is silently ignored rather than rejected with exit 2 at argument-validation time. Extremely low practical likelihood (a 19+-digit `--last-n` value), non-crashing, no central-invariant impact.
- `bin/retro-inputs.sh:161-165` (`sanitize()`) — confirmed directly on this system (`ja_JP.UTF-8` locale) that `tr -d '\r\n\t\`'` exits nonzero ("Illegal byte sequence") on an invalid UTF-8 byte, which — via the same `pipefail`/`set -e` mechanism as the two blockers above — could in principle crash a caller if an untrusted merge subject, PR title, branch name, or path ever contains one. Grounded out-of-input-space: the spec's own `## Input space` → Out-of-scope synthetic extremes item 2 explicitly names "Non-UTF-8 or mixed-encoding retro files and commit messages" as a declined synthetic extreme. Downgraded to minor on that basis, **with a caveat**: the spec's stated reason for declining it (cannot forge a ledger line) addresses a *parsing/security* concern, not this *crash-safety* concern specifically — worth revisiting in the same follow-up as the two blockers above, since all three share one root cause (an external tool's exit status trusted implicitly inside a pipeline under `set -e`/`pipefail`) and one remedy shape (guard the tool's own exit status explicitly, or run it byte-oriented with `LC_ALL=C` so it cannot fail on this input at all).

## Adversarial findings

Devil's-advocate pass focused on `bin/retro-inputs.sh` and `bin/check-retro.sh`, run because the primary pass's SIGPIPE finding was itself a blocker, asking specifically whether any *other* probe in the script could abort the whole process under `set -e`/`pipefail` rather than gracefully leaving `unavailable` standing. Two findings returned:

1. The `count_dir_entries` non-regular-file crash (promoted to Blocker above; independently reproduced by the reviewer three ways, including a verbatim standalone extraction, not merely accepted from Codex's text).
2. The locale-sensitive `tr` crash on invalid UTF-8 (Minor above; independently reproduced on this exact system's locale, then grounded out-of-input-space per the spec's own Non-goals).

## Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | SIGPIPE crash via `head` piped from a large `printf` under `--last-n` | primary: P1 (→ blocker per map) | blocker | Matches the mapped default; independently reproduced against a real 1200-merge-commit repository built via git plumbing, with a verified fix. |
| 2 | `emit_material_block`'s here-string depends on a working `TMPDIR` | primary: P2 (→ major per map) | rejected | Tested directly under two adverse `TMPDIR` conditions (nonexistent; existing but `chmod 0500`) against the real script with real material to emit — did not reproduce on this bash 3.2 / Darwin environment. Not escalated; noted as a free hardening opportunity regardless. |
| 3 | whitespace-only lessons lines counted as non-blank | primary: P2 (→ major per map) | minor | The file was genuinely read; this is a `read`-vs-`empty` boundary question, not a `benign`-vs-`unavailable` one — orthogonal to the central invariant this round's redesign protects. |
| 4 | oversized `--last-n` silently ignored rather than rejected | primary: P3 (→ minor per map) | minor | Matches the mapped default; extremely low practical likelihood, degrades gracefully (no crash, no status misclassification). |
| 5 | non-regular directory entry matching suffix crashes `count_dir_entries` | adversarial: high (→ major per map) | blocker | Escalated: independently reproduced three ways including a verbatim standalone function extraction: a genuine, realistic (no scale or permission trick needed), same-class recurrence of "a probe aborts the whole script instead of leaving `unavailable` standing," reachable from any of the five directory-backed inputs. |
| 6 | locale-sensitive `tr` crash on invalid UTF-8 in untrusted text | adversarial: medium (→ minor per map) | minor (out-of-input-space, with caveat) | Grounds on spec `## Input space` → Out-of-scope synthetic extremes item 2 ("Non-UTF-8 or mixed-encoding retro files and commit messages... is declared a Non-goal above"). Confirmed reproducible on this system's locale; not escalated because the input class is explicitly named out of scope, though the spec's stated rationale (forgery/parsing) does not itself address the crash angle — flagged as a fast-follow alongside the two blockers, since all three share a root cause and remedy shape. |

**Issue-canon note**: as in round 1, this spec's Problem/Non-goals sections characterize GitHub issue #28 in some detail; this review has no GitHub access and did not independently fetch the issue — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). No finding above depends on issue #28's exact wording.

#### Recommendation

- REQUEST_CHANGES → set T-1001 back to READY_FOR_ENG with the two blockers above. Both have a verified, minimal fix (here-string instead of pipe; `if`/`fi` instead of `&&`-chain) already confirmed in isolation in this review, so a fourth round should be small in scope — but given this is the second consecutive round to surface a same-class recurrence, a brief mutation/fuzz pass over every *other* new pipe, `&&`-chain-inside-case-or-loop, and external-tool call this rework introduced (not only the two now found) is worth the coordinator's explicit consideration before the next round, rather than patching only the two instances named here.

#### Fast-follow disposition

- Non-crashing `TMPDIR`/here-string dependency in `emit_material_block` (`bin/retro-inputs.sh:513`): file-an-issue — zero-cost hardening (swap the here-string for the same `printf | while read` idiom already used twice elsewhere in this script), even though the failure mode itself did not reproduce here.
- Whitespace-only lessons lines counted as non-blank (`bin/retro-inputs.sh:445`): file-an-issue — small, well-defined (a whitespace-aware predicate), low urgency since it is a `read`/`empty` boundary call, not a central-invariant issue.
- Oversized `--last-n` silently ignored (`bin/retro-inputs.sh:311`): won't-fix — degrades gracefully with no crash and no status misclassification; the input is an extreme nobody realistically types.
- Locale-sensitive `tr` crash on invalid UTF-8 (`bin/retro-inputs.sh:161-165`): file-an-issue — recommend bundling with the two blockers' fix (same root cause: an external tool's exit status trusted implicitly inside a pipeline under `set -e`/`pipefail`), since fixing the blockers is already going to touch this exact discipline; not blocking this round on its own because the input class is explicitly out of scope per the spec.
- The `tests/errexit-safe` coverage gap for the `cmd; rc=$?` and bash-3.2 empty-array bug classes (carried over from QA round 3's own finding, independently reconfirmed above): file-an-issue — cross-cutting `bin/` hygiene, not specific to this task's deliverable.

---

# QA round 4 — T-1001 v3 verification (commits `55a2734` + `9ba2977`)

**Verdict: PASS**

Scope of this round, per the coordinator's framing: v3 is a scope cut (33→30 ACs, 6 retired / 3 added), not a new mechanism, so the audit's primary job was (a) confirming the two plumbing fixes (AC8, AC9) and the new bounded regression lock (AC10) are genuine and correctly scoped, and (b) confirming the six retired v2 criteria's underlying artifacts were not silently reverted alongside their assertions.

## Structural gates (re-run, all green)

- `bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` → 30/30 PASS.
- `bash bin/check-intent.sh` → aligned (v3, `1308789fb1b11d6fb6f5f349731ab2df66bdaed8`).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1001.md` → conformant (20 decision entries, 0 sentinel).
- `bash bin/check-prompt-sync.sh`, `bash bin/check-handoff.sh .shell-team/todo.md` → green/clean.
- `shellcheck` on `bin/retro-inputs.sh tests/retro-inputs/run.sh tests/retro-inputs/invariants.sh tests/retro-inputs/fixtures/gh tests/retro-inputs/fixtures/git` → clean.
- `tests/retro-inputs/run.sh`, `tests/retro-inputs/invariants.sh` (new), `tests/check-retro/run.sh`, `tests/team-paths/run.sh`, `tests/team-init/run.sh`, `tests/check-prompt-sync/run.sh`, `bash bin/retro-inputs.sh --base HEAD`, `bash bin/check-retro.sh .shell-team/retros/*.md` → all green.
- `bash bin/check-commit-identity.sh --base develop` → clean.
- One correction to a number in the coordinator's framing, not a defect: `git diff --name-only develop` currently lists **33** paths (not 32) — the new `tests/retro-inputs/invariants.sh` file adds one over v2's count. AC26's own check (re-run directly) still confirms the full 33-path diff is confined to the unchanged allow-list pattern; the pattern already covers any new file under `tests/retro-inputs/`, so no widening was needed.

## Item 1 — the "both fixes necessary" claim for AC8, mutation-tested independently (all four combinations)

Built four variants of `count_dir_entries()` (baseline/shipped; case-arm reverted only; `return 0` removed only; both reverted) and ran each against a throwaway repo with a directory named `T-1.md` and a broken symlink named `T-2.md` inside `.shell-team/reviews/` (my own fixture, mirroring but independent of the shipped one). First attempt gave a false "no crash on any variant" result because my scratch copies couldn't resolve their sibling `team-paths.sh` (a test-harness mistake on my part, corrected by mirroring `team-paths.sh` alongside each variant) — recording this so the correction is visible, not just the final number:

| Variant | Result |
|---|---|
| Shipped (both fixes) | exit 0, 8 lines, `review-artifacts` correctly `empty` |
| Case-arm reverted to `&&`-chain, `return 0` **kept** | exit 0, 8 lines — does **not** crash |
| Case-arm kept as `if`, `return 0` **removed** | exit 0, 8 lines — does **not** crash |
| **Both** reverted (the exact pre-fix shape) | **exit 1, 0 lines** — crashes |

This confirms the engineer's claim precisely: only reverting *both* changes together reproduces the original defect; either one alone is independently sufficient to prevent the crash on this exact reproduction. `return 0` is not decorative — it masks whatever exit status the `for`/`if` construct above it would otherwise propagate, exactly as claimed. Also swapped the fully-reverted variant into the real `bin/retro-inputs.sh` location and ran the real `tests/retro-inputs/invariants.sh` against it directly: state 1 ("a directory whose name ends in .md") failed with the exact message `exit status 1 is neither 0 nor 2`, confirming the lock catches the reproduced defect; restored the real file afterward (`git diff --stat bin/retro-inputs.sh` empty, confirmed clean before and after).

## Item 2 — AC10's invariant lock and its nine states

Independently mutation-tested (as above): the shipped fix passing / the reverted defect failing state 1 specifically, with the exact failure message. Read `tests/retro-inputs/invariants.sh` in full for whether each state genuinely constructs the state its label claims, rather than trusting the labels:

- States 1–2, 4–8 (directory-named-`.md`, broken-symlink, empty directory, non-traversable directory, shallow repo, linked worktree, unresolvable `--base`) each build a real throwaway repo/directory/worktree with the claimed property — read and confirmed each construction is genuine, matching the technique already independently verified in rounds 1–3 for the decision-layer equivalents.
- **State 3 (merge log > pipe buffer with `--last-n`)**: independently reproduced the byte-size claim on a fixture of my own construction (200 merges, 300-byte-padded branch names) — measured **76,183 bytes** for the resulting `git log --merges --first-parent` output, confirming the coordinator's ~76 KB figure and exceeding the ~64 KB pipe-buffer threshold the spec cites. **However**: I could not reproduce the underlying SIGPIPE crash itself on this sandbox (macOS/BSD), with either the pre-fix v2 script or a bare `printf | head` construct, even at payloads up to 10 MB — BSD `head` on this platform drains its entire stdin before exiting rather than closing early once it has enough lines, which structurally prevents the writer from ever receiving `SIGPIPE` here, unlike GNU `head` (the actual CI runner's coreutils flavour). This is `environmentally-unverified` on my part, not a finding against the fix: the byte-size trigger and the mechanism (a reader that can exit before its writer finishes) are independently sound and match the description; the fix itself (a here-string, which structurally has no live writer process to signal, regardless of which `head` flavour is on `PATH`) does not depend on reproducing the crash to be correct, and I confirmed no `| head` remains anywhere in the script (`grep -n '| *head'` — zero hits, only comments).
- State 9 (`--last-n 0`): re-confirmed correct (`read`, 0 shown, cap declared, exit 0).

**AC9's here-string cap verified semantically equivalent to the old `head`-based cap across all three regimes**, on a repo with real merge commits: `--last-n 1` (cap reached, below total) selects the same top-of-log commit `head -n 1` would have; `--last-n 0` yields an empty, correctly-capped result with no crash; `--last-n 10` (cap not reached, above total) leaves the full, uncapped count and omits the "capped" qualifier entirely. The here-string (`<<<`) is a bash builtin construct, not an external dependency, so it stays within this repository's "pure bash" rule.

**AC10's self-limitation is enforced in code, not just described in a comment**: read `assert_invariant()` directly — it checks exactly three things (exit 0-or-2, the heading present, exactly 8 `- input: ` lines) and touches no per-state status content anywhere. This is a genuine architectural boundary, not a hedge: keeping the assertion generic and shared across all nine states is what prevents this file from silently regrowing into a second, overlapping copy of `tests/retro-inputs/run.sh`'s decision-layer fixtures — exactly the scope-creep class that cost three rounds. Read and accepted as load-bearing, per the engineer's framing.

## Item 3 — the six retired v2 criteria: work confirmed NOT reverted

Checked each named artefact directly, independent of the unchanged-allow-list argument:

| Retired v2 AC | Artefact checked | Result |
|---|---|---|
| AC8 (zero-dependency) | `grep` for `jq/yq/python/perl/node` in `bin/retro-inputs.sh` | clean |
| AC20 (trust-boundary sentence) | the exact sentence in `bin/check-retro.sh`'s header | present, byte-exact |
| AC22 (`team-init.sh` provenance scaffold) | `bin/team-init.sh` + `tests/team-init/run.sh` | both present, suite green |
| AC23 (template section order) | `docs/templates/retro-template.md` | `## Retro inputs` still precedes `## Keep（…）` |
| AC27 (dead-pointer set) | `agents/scrum-master.md` + template | still exactly the two known-dead paths |
| AC28 (paren count, now superseded by AC21) | the five heading constants in `bin/check-retro.sh` | all five present, byte-exact |

None of the six was reverted. This was the single most easily missed risk this round and it holds.

## Item 4 — SIGPIPE fix equivalence and the `bin/` pure-bash rule

Covered under item 2 above (the three cap regimes, verified on a real fixture with actual merge commits, not just the big-log fixture). The here-string is a bash builtin (`<<<`), introduces no new external tool, and is consistent with this repository's "pure bash, zero-dependency" rule.

## Item 5 — the self-hitting-grep comment reword: read for accuracy, not just for the fact of a fix

Re-read both reworded comments (in `count_dir_entries()` and in `probe_cycle_window()`'s fallback-note site) end to end: both still accurately describe the shape they replace ("a boolean-AND-chained assignment... conditions the assignment on a preceding test") and the mechanism of the defect (case-arm/if-construct exit-status propagation into the bare call site under `errexit`), with no loss of explanatory content from the rewording. Independently confirmed zero remaining `&&...=` matches anywhere in the file (`grep -n '&&.*='` — no hits), so the self-hit is genuinely gone, not merely relocated.

## Item 6 — legitimate `&&` usage not swept up

Diffed `af5b71a..55a2734` directly rather than trusting a description: exactly three assignment-shaped `&&` chains were rewritten (the `fb_note` assignment, the two `qualifiers` assignments, and the `DIR_N_MATCH` assignment inside the case arm). The `&&`-inside-an-`if`-condition site (`if [ -n "$last_n" ] && [ "$total" -gt "$last_n" ]; then`) and the two pre-existing, unrelated `[ -n "$x" ] && printf ...` guards in the material-emission loops (cycle-window and pr-metadata sub-bullet construction) do not appear anywhere in the diff — confirmed byte-for-byte unchanged from v2. (Investigated whether the latter two guards carry the same crash risk as the case-arm defect, since they are also `&&`-chained: they are not reachable in a failing shape from real `git`/`gh` output, since the hash/PR-number field they guard is never empty for real subject lines — this is a defensive, non-load-bearing guard on data that is always well-formed in practice, unlike the directory-enumeration case where a non-regular-file entry is an ordinary, reachable condition. Not a defect; noted for completeness since I went looking.)

## A defect I introduced and fixed myself, not the engineer's

`bash bin/check-pii-shapes.sh --base develop` reported one finding: `.shell-team/reviews/T-1001.md:80`, a placeholder email (a local part at a two-letter registrable domain rather than an RFC 2606 reserved one) I wrote into my own round-1 reproduction snippet, which does not match the checker's RFC-2606 reserved-domain allow-list. This has been sitting in the committed tree since the engineer's round-1 rework commit (which picked up my then-uncommitted review file wholesale) and none of rounds 1–3 re-ran `check-pii-shapes.sh --base develop` to catch it. Fixed directly in this round (changed to `test@example.com`, the domain this repository's own fixtures already use and which the reserved-domain allow-list recognizes) — this edit is QA-record hygiene, not production code, and is why `check-pii-shapes.sh` still reports the finding against the *committed* `HEAD` copy until this review file is next committed (the checker reads via `git cat-file -p HEAD:path`, not the working tree, by design). Flagging this plainly so it is not mistaken for an engineer-side defect, and so whoever commits next confirms the fix landed.

## Verdict

No same-class recurrence found (`shell-construct-aborts-before-ledger-emitted` closed by AC8/AC9's fixes and AC10's lock, mutation-confirmed; the decision-layer class from v2 remains closed and untouched by v3). All six retired criteria's underlying work is intact. One real caveat recorded above (the SIGPIPE reproduction is environment-bound and not directly observable on this sandbox) and one QA-side hygiene defect found and fixed (not an engineer defect). **T-1001 → `READY_FOR_REVIEW`.**
