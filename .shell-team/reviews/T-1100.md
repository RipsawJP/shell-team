### Codex Review verdict: REQUEST_CHANGES
- Task: T-1100
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Verification mode: executed-tests — beyond reading the diff/spec/board, this round directly ran the frozen `- check:` line for AC5 (`bash -c "$CHECK"`, rc=0, confirming the shipped instrument passes as claimed) and then reproduced a specific mutation of that same check line myself (see Major 2) to independently confirm a coverage gap Codex's adversarial pass first raised, rather than accepting the claim untested.
- Diff scope: 14 files, +570/-9 lines (`develop...HEAD`, 7 commits: `82deff3` spec freeze v1, `12a70e9` implement, `f73eec0` Notes-from-engineer, `d352e3a` class-B re-freeze v1→v2, `96f9956` restore clobbered notes, `7b73413` engineer hand-off, `8f552a2` QA hand-off)

#### Independent verdict on the mandatory re-freeze item (AC5 v1→v2, class-B, host-ratified)

**(a) Does the repaired AC5 still protect the full back-compat intent?** Yes. Reading the population from the base ref's *committed* board blob rather than the working-tree board is the structurally correct fix: it excludes exactly the rows this task's own DP-5 dogfood substitution introduces (which by construction cannot have existed at any point before this task), while still covering every row that existed on the board before this branch started — which is what "pre-existing back-compat" was always supposed to mean. This does not open a hole for a careless future change: any row genuinely predating this branch is still measured; nothing beyond this task's own two new rows is excluded, and that exclusion is inherent to the population's own definition (base-ref-relative), not an ad hoc allow-list a later axis would need to extend.

**(b) Is the wrong-base-binary control sound?** Only partially, and I confirmed the gap directly rather than relaying Codex's claim untested. I extracted AC5's actual frozen `- check:` line, ran it unmodified (`rc=0`, matches the engineer's/QA's claim), then mutated only the arm-selector logic — replacing `if [ "$arm" = base ]; then S="$T/binbase/close-out.sh"; else S=bin/close-out.sh; fi` with the unconditional `S=bin/close-out.sh` (i.e., both arms silently execute HEAD's own binary) — and re-ran the check: **it still passed (`rc=0`)**. The new "wrong-base-binary" control added by the re-freeze only asserts that the *file copied* to `$T/binbase/close-out.sh` differs in bytes from `bin/close-out.sh`; it never asserts that this file is what actually gets *executed* by the base arm. The `git show` extraction line (which the control does guard) and the `S=` arm-selection line (which it does not) are two independent ways to reach the identical "base arm silently reuses HEAD's binary" failure mode the control's own name claims to close; AC17(a)'s mutation probe tested only the former. See Major 2.

**(c) Was this genuinely an instrument repair rather than a goalpost move — would I have refused it?** I would have accepted it. The re-freeze diff (`git diff 82deff3..d352e3a`) is exactly the one hunk it claims (AC5's paragraph + its `- check:` line, nothing else in the frozen intent block moved), matching the provenance record's `n=2` claim and QA's own independently re-run `check-refreeze-class.sh` classification (`class-b`, exit 1). The protected property was measured intact *before* the repair was proposed (the isolating run's own record: zero pre-existing rows flipping), which is the right order of operations — the repair followed evidence rather than preceding it. The AC5-vs-AC12 conflict this repairs is genuinely structural given DP-5's own dogfood requirement, not a convenience the engineer manufactured to dodge a real regression. I have no independent reservation about the ratified decision itself; my reservation (Major 2) is about one of the three run-time controls the repair added, which has a real, demonstrated coverage gap independent of whether the re-freeze itself was warranted.

#### Blockers
- None.

#### Major
1. **`agents/tech-lead.md:71-74`** — The `### Dispatch` output-format template presents all four `- dispatch:` lines (`implement`, `verify`, `verify-fixture`, `verify-mechanism`) as unconditional sibling bullets, with no inline marker that `verify` and its two refinements are mutually exclusive alternatives — unlike the `ui-designer` step two lines above it in the same template, which explicitly says "INCLUDE ONLY for UI work; omit this line entirely otherwise." `bin/close-out.sh`'s brand-new exclusivity guard (this same task) refuses any board entry carrying the parent `verify` axis alongside either refinement. A `tech-lead` invocation that follows "Return a markdown block in this exact shape" literally for an *ordinary* task (no split needed) risks emitting a Routing Map with `verify` **and** both refinement rows, which the orchestrator would then transcribe onto the board (per the file's own "who decides, who records" rule) and which the newly-added gate would refuse at freeze or close-out time. The correct exclusivity rule *is* stated in prose 22 lines later (the mirrored "Refinements (T-1100)" bullet at line 96), so a careful whole-file reading resolves it, and the fail-closed gate catches a bad transcription before it reaches a merged board state — but the concrete example a model pattern-matches against contradicts the abstract rule stated elsewhere in the same file, which is exactly the class of self-contradiction this task's own DP-6 prose sweep exists to eliminate and did not catch here. Both my primary and adversarial Codex passes independently flagged this (primary: P1; adversarial: "high", independently reasoning that ordinary-task spurious rows are *more* likely than a split-only error). Suggested fix: mark the two refinement lines conditional in the template itself (e.g. `- dispatch: verify — ... (unsplit tasks)` / `- dispatch: verify-fixture — ... — INCLUDE ONLY when this task's verification duty is split; never alongside the `verify` line above`), mirroring the `ui-designer` line's own convention.
2. **`.shell-team/specs/T-1100-verify-axis-split.md` AC5's `- check:` line (lines 68-69), the wrong-base-binary control added by the v1→v2 re-freeze** — Empirically confirmed vacuous against a specific, realistic mutation shape. The control (`if [ -n "$(git diff --name-only "$B"...HEAD -- bin/close-out.sh)" ]; then cmp -s "$T/binbase/close-out.sh" bin/close-out.sh && rc=1; fi`) only compares the two **on-disk files'** bytes; it does not verify which one each arm's `bash "$S"` invocation actually *executed*. I reproduced this directly: extracting the live check line, replacing only the `S="$T/binbase/close-out.sh"`/`S=bin/close-out.sh` arm-selector conditional with an unconditional `S=bin/close-out.sh` (both arms silently run HEAD's binary), and re-running — the check still exits 0. This is a genuine gap in the mutation-resistance of the instrument that verifies AC5, one of the pre-commitment's three explicitly **never-dropped** items, on a mechanism-class, shipped-checker task this review's own briefing scopes to full adversarial depth (the 2026-08-22 dev-scaffold re-pricing does not apply here). AC17(a)'s own duty required exactly this probe ("one probe must make the base arm silently reuse the HEAD script and confirm the identity assertion flips red") and the engineer's probe (omitting the `git show` extraction) does test *a* way to reach that state, but not the only way, and the added control does not close the other one. Suggested fix: since AC17 carries no `- check:` line (narrative-only), this can be closed without a further re-freeze — either strengthen the check itself (e.g., have each arm write `$arm\t$S` to a log and assert the log matches the expected `(base, $T/binbase/close-out.sh)` / `(head, bin/close-out.sh)` pairing after the loop) or, at minimum, extend AC17(a)'s narrative mutation-probe coverage to include the arm-selector mutation and report the result honestly rather than leaving the gap undisclosed.

#### Minor / nits
1. **`.shell-team/specs/T-1100-verify-axis-split.md`, `## Decision points, resolved`, DP-4** (outside the frozen intent block — a plain edit, not a re-freeze) — Still reads "**DP-4 — blast radius and back-compat population: a measured two-arm diff over the live board.** **AC5** feeds every distinct `- dispatch:` line on **the resolved board** through the base-ref blob of `bin/close-out.sh`... the population is extracted at run time... from **the board itself**" (unchanged since v1). This directly contradicts AC5's own v2 text, which now sources the population from "the **base ref's committed board blob**... and never from the working tree." The re-freeze commit (`d352e3a`) touched only AC5's own paragraph and check line; DP-4's narrative was not updated to match, even though DP sections sit outside the intent block and could have been corrected as an ordinary edit. This is a records-only contradiction within the same file — a later criterion (AC5 v2) now disagrees with an earlier decision-record paragraph (DP-4) that no gate re-reads — not merely a wording nit. Suggested fix: update DP-4's two "resolved board"/"live board"/"the board itself" phrases to name the base-ref committed blob, matching AC5 v2.
2. **`templates/prompt-blocks/dispatch-record.md:1`, mirrored verbatim into `agents/tech-lead.md:89` and `skills/run/SKILL.md:211` (AC2's own byte-for-byte mirror requirement)** — The pre-existing "Situational dispatch record (T-1084)" note still states "`docs/loop-engineering/phase-multiplexing.md`'s two axes," which this task's own edit to that same file makes factually false: `phase-multiplexing.md` now declares four `- dispatch-axis:` lines (`implement`, `verify`, `verify-fixture`, `verify-mechanism`). DP-6's own prose-sweep enumeration named several specific cardinality-one statements to fix (`agents/pm-spec.md:123`, `agents/tech-lead.md:71`'s template, `skills/run/SKILL.md:91`/`:222`, `phase-multiplexing.md:61`, and dispatch-record.md's "value-set and completeness clauses") but did not name this sentence, so it slipped through the sweep it was designed to catch. Because it is mirrored verbatim into all three consumers, the same factual error now exists in three places rather than one. Suggested fix: replace "phase-multiplexing.md's two axes" with a value-independent phrasing (e.g. "phase-multiplexing.md's own axes"), consistent with the value-independent rewrite DP-1/DP-6 already applied to the `exactly two, note-wide` literal elsewhere in the same file.

#### Adversarial findings
- Independently confirmed and sharpened the primary pass's tech-lead.md finding: rated the risk as "high" and reasoned that spurious refinement rows on *ordinary* (unsplit) tasks are more likely than the split-only failure mode, since nothing in the template marks the two refinement lines as conditional the way the `ui-designer` step explicitly does.
- Raised the wrong-base-binary control concern (Major 2 above), which I independently reproduced by direct execution rather than accepting on the adversarial pass's own say-so.
- Raised a DP-4/AC5 population-wording mismatch, matching Minor 1 above (I traced and quoted the exact contradicting lines myself rather than relaying the adversarial pass's paraphrase).
- Raised the same stale "two axes" observation as Minor 2 above (rated "low" natively).
- Found nothing new in the per-field extraction, the `DISPATCH_AXIS_TABLE` lookup, the `DISPATCH_SEEN_AXES` token-boundary safety (confirmed no `verify`/`verify-fixture` substring collision, by direct code reading of the space-padded-token accumulator), or the shared grammar's whitespace/cross-axis/ground-field handling, all of which are inherited unchanged from the pre-existing, already-tested code path for the other axes.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | tech-lead.md Dispatch template omits exclusivity marker | primary: P1 (native) → blocker; adversarial: "high" (native) → major | major | both passes agree the mechanical gate (this task's own exclusivity refusal) catches a bad transcription before it reaches a merged board state, so the practical failure mode is a wasted round-trip / freeze refusal rather than silent corruption — the P1-native label rounds down from an over-conservative auto-mapping once that mitigating fail-closed backstop is weighed, not because the underlying documentation defect is disputed |
| 2 | wrong-base-binary control vacuous against an arm-selector mutation | adversarial: "medium" (native) → minor | major | independently reproduced by direct execution (not accepted from the adversarial pass's claim); upgraded from the native "medium" mapping because the control's own stated purpose is to protect a pre-commitment never-dropped item (AC5's back-compat guarantee) on a mechanism-class shipped checker this review is scoped to review at full depth — the dev-scaffold re-pricing that would otherwise justify a lighter touch on inline check-line adversarial completeness explicitly does not apply here |
| 3 | DP-4 narrative stale relative to AC5 v2 | adversarial: "medium" (native) → minor | minor | no change — a records-only prose contradiction outside the frozen intent block, gating nothing mechanically; reported as a finding per the records-read rule rather than silently as a nit, but the correct severity given no gate re-reads it |
| 4 | stale "two axes" literal in dispatch-record.md and its two mirrors | adversarial: "low" (native) → nit | minor | rounded up one level per the severity-normalization map's conservative rule (an unmapped-confidence label rounds up, and "low" sits at the nit/minor boundary here): it is mirrored into three consumer files rather than one, and it directly misstates a fact this same task's own diff changes |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with this record linked. Both Majors are real, independently confirmed (one by direct reproduction, both cross-checked against the actual bytes) and cheap to fix: Major 1 is a template annotation; Major 2 is either a narrative-only AC17(a) coverage extension or a small strengthening of the check line, neither of which requires touching the frozen intent block's substantive text. Per the spec's own pre-commitment, this is round 1 of cross-provider review on this component; the "two consecutive rounds" factual trigger for returning to planning has not fired, and neither Major defeats a pre-commitment never-dropped item's currently measured correctness (both are about robustness/authoring-hazard, not about the shipped gate behaving wrongly today).

#### Fast-follow disposition
- Minor 1 (DP-4 staleness) and Minor 2 (stale "two axes" literal): disposition — fix in the same round as the two Majors, since both are one-line prose corrections in files already being touched to close the Majors, and bundling avoids a second review round over pure documentation drift. Not deferred as a genuine fast-follow; flagged here only because they are Minor severity, not because they are being punted past merge.
- no other fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES — Round 2 (rework verification)
- Task: T-1100
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.149.1
- Verification mode: executed-tests — every claim below was independently re-executed by the reviewer (extracting the live v3 `- check:` line and running it, and each mutation, under `bash -c`), not accepted from the engineer's or coordinator's own numbers. Codex was also invoked twice more as a second independent reader of the same diff (`T-1100-codex-primary-r2`, `T-1100-codex-adversarial-r2`).
- Diff scope: `8f552a2..f3fd5b7`, 7 files, +98/-10 lines, 3 commits (`caa548b` Major 1 + both Minors + QA risk note, `361a46b` class-M re-freeze v2→v3, `f3fd5b7` engineer verification note). Round 1's own settled ground (everything before `c2f31a7`) was not re-opened.

#### Round-1 findings, closed by class (not by case)

1. **Major 1 (tech-lead.md Dispatch template exclusivity) — CLOSED.** Read `agents/tech-lead.md:70-73` directly: each of the three verify-family lines now carries its own `INCLUDE ONLY .../omit ... otherwise` trailer, mirroring the `ui-designer` line's idiom, and both refinement lines additionally state "never combine it with the `verify` line above." This closes the *class* (self-contradiction between a literal template and a prose-stated exclusivity rule), not merely the one example — the fix is now inline exactly where a model reading the template would see it, not 22 lines away. Independently re-ran the AC8 spot check: exactly one line each opening `- dispatch: implement — `/`verify — `/`verify-fixture — `/`verify-mechanism — `, zero opening `- dispatch: specify — `/`spec-review — `, `tools:` line reads exactly `tools: Read, Grep, Glob`. `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync` (confirms the edited region is tech-lead-local, outside the mirrored `dispatch-record.md` block, as claimed).
2. **Major 2 (AC5 wrong-base-binary control) — CLOSED against the two originally-reported mutations, but see Major 3-5 below for newly-discovered, distinct gaps in the same instrument.** Extracted the live v3 `- check:` line myself and reproduced both mutations independently: (a) both arm-selector occurrences forced to the unconditional `S=bin/close-out.sh` → **flips red (`rc=1`)**; (b) both forced to the unconditional `S="$T/binbase/close-out.sh"` → **also flips red (`rc=1`)**. The unmutated v3 line: baseline `rc=0`. This closes exactly the *reported* defect (a control that compared file bytes but never verified which binary each arm executed) — the new behavioural probe genuinely discriminates base-vs-head execution for the real diff, confirmed by non-destructive instrumentation showing the probe block genuinely fires once under the actual, unmutated check (not merely passing vacuously because its own trigger never ran).
3. **Minor 1 (DP-4 staleness) — CLOSED.** `.shell-team/specs/T-1100-verify-axis-split.md`'s DP-4 paragraph now reads "the base ref's own pre-existing corpus" / "the base ref's committed board blob," matching AC5 v2/v3's own text, with a dated "Updated (round-1 rework)" sentence preserving the original v1-era observation rather than silently rewriting history. Intent hash unaffected (DP-4 sits outside the marker pair) — re-confirmed via `check-intent` still reporting the same declaration region.
4. **Minor 2 (stale "two axes" literal) — CLOSED.** `grep -rn "two axes" templates/prompt-blocks/dispatch-record.md agents/tech-lead.md skills/run/SKILL.md` → 0 hits; all three now read "phase-multiplexing.md's own axes," value-independent per DP-1/DP-6's own established idiom, and mirrored identically across all three consumers (`bash bin/check-prompt-sync.sh` re-confirmed green).
5. **QA risk note 1 (`test-recipe.md` staleness) — CLOSED, append-style.** The T-1100 entry gained a dated "Update (2026-08-26, review round 1 rework)" paragraph correctly narrating that the conflict was *resolved* (not worked around) by the v1→v2 re-freeze, without rewriting the original v1-era observation.

#### Full re-verification (independently re-run, not accepted from the hand-off)

`bash bin/check-prompt-sync.sh` → clean. `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1100-verify-axis-split.md` → `16 passed, 0 failed, 1 skipped, 0 unrecognized`. `bash tests/close-out/run.sh` → exit 0, 116 PASS / 0 FAIL / 7 `PASS: T-1100`. `bash bin/check-intent.sh` → `aligned: T-1100 v3 (f489a6d26892208cca664223e80fbc1d893b494f)`. `bash bin/check-handoff.sh`, `bash bin/check-board-headings.sh --base <merge-base>`, `bash bin/check-pii-shapes.sh --base <merge-base>` all clean. Independently re-ran `bin/check-refreeze-class.sh` against the v2 spec blob (`d352e3a`) and the current v3 spec: `check-refreeze-class: mechanics: T-1100 d4be2a75… -> f489a6d2… differing=1` (exit 0) — matches the board's own recorded classifier verdict exactly, and confirms the intent-block diff between v2 and v3 is exactly the one line (AC5's `- check:` line) the board claims. Independently re-ran AC14's allow-list over the current HEAD diff — no stray file outside the allow-list. No `.sh` file changed anywhere in this round's diff, so shellcheck is not applicable to this round.

#### New findings — AC5 v3's instrument has further, distinct mutation-resistance gaps (independently reproduced)

Round 2's own scope asked for an adversarial mutation of my own choosing beyond the two already closed. Probing the same v3 check line further (myself, then independently corroborated by two more Codex passes) surfaced three additional, genuinely new gaps — none overlapping with Major 1/2 above, none caused by anything this round's fix touched incorrectly (the currently-shipped, unmutated check line is genuinely correct for the real diff — confirmed by instrumentation and by the clean baseline runs above), but each a real vacuity if this same frozen check line is ever edited again without knowing to guard against it:

- **Major 3 — the new behavioural probe's own trigger can be silently disabled.** The probe block is gated by `if [ -n "$(git diff --name-only "$B"...HEAD -- bin/close-out.sh)" ]; then ...`. I mutated this condition to an unconditional false (`if [ -n "" ]; then ...`) — **the check still passes (`rc=0`)** even though `bin/close-out.sh` genuinely differs in this diff and the probe was supposed to run. Confirmed via non-destructive instrumentation that the real, unmutated condition does fire today (so today's verdict is not vacuous) — the gap is that nothing in AC5 asserts the probe *actually ran*, so a future edit that silently breaks this predicate (wrong pathspec, `-n`→`-z`, a `...`/`..` typo) would revert AC5 to pre-v3 protection while still reporting green.
- **Major 4 — the base-side population extractor is not itself validated for completeness.** I narrowed the base-row extraction regex from `grep -E '^[[:space:]]*- dispatch: '` to `grep -E '^[[:space:]]*- dispatch: implement '` (base population only) — **the check still passes (`rc=0`)**. Measured directly: this checkout's base-ref board carries 46 distinct `- dispatch:` rows across all axis families; the narrowed extractor sees only 16 (the `implement`-axis subset). Every existing positive control still holds vacuously under the narrowing (`POP` is still `-gt 0`; `HPOP` — the head board's 51 distinct rows — is still `-gt` the narrowed `POP`; the zero-new-axis-keys check still holds on the narrowed set) because none of them independently re-derive or cross-check the *expected* population size — they only compare the extracted population against itself and against HEAD's own (also self-reported) count. A future edit narrowing this regex would silently drop 30 of 46 pre-existing rows from the "measured, not argued" byte-identity sweep with no assertion catching it.
- **Major 5 — the per-row verdict loop's exit-status capture is order-dependent and unasserted.** The loop reads `bash "$S" ... 2>"$T/err"; r=$?; if grep -qF -- 'malformed dispatch record' "$T/err"; then m=refused; else m=silent; fi`. I moved `r=$?` to *after* the `if`/`grep` construct — **the check still passes (`rc=0`)**. Because an `if`/`then`/`else` block's own exit status is that of the assignment statement it ran (`m=refused` or `m=silent`, both trivially 0), `r` becomes unconditionally `0` for every row on both arms once so reordered — the exit-code half of the recorded verdict tuple stops discriminating anything, and only the stderr-literal half (`m`) still carries real signal. A future regression that changes an arm's exit code *without* changing whether the refusal literal appears (a plausible shape — e.g. a caller misusing `bash`'s own exit convention) would go undetected by this half of the comparison.

These three sit in the *same* instrument (AC5's `- check:` line) that round 1's Major 2 also targeted, and this is the second consecutive round finding **independently new** Majors against it — classified explicitly, per this task's own pre-commitment discipline: these are not a reopening or a propagation gap of round 1's *same* defect (which is genuinely closed, confirmed above), but three distinct code locations (the new probe's trigger gate; the pre-existing, unchanged-by-this-round population extractor; the pre-existing, unchanged-by-this-round exit-status capture) exhibiting the same underlying pattern — a hand-rolled, single-line bash verification embedding several independent assumptions, each individually undefended against its own plausible mutation. (Note for the record: this task's own pre-commitment `trigger-factual`/`trigger-contextual`/`trigger-priority` mechanism is scoped to deciding when to drop one of the two *droppable* pre-commitment components — issue #365 item 3, and the trigger-text-rewrite-beyond-minimum — neither of which this finding touches, so that specific escalation clause does not fire here; this is a plain review finding, not a pre-commitment defeat.)

**Weighing severity honestly**: the currently-shipped, unmutated v3 check line is not defective — independently confirmed the real diff genuinely reddens/greens correctly, the probe genuinely fires, and the population genuinely covers all 46 pre-existing rows. All three findings require a *future* edit to this already-frozen check line to manifest, and any such edit would itself go through another class-M/B re-freeze and review. Weighed against that mitigation: AC5 is one of exactly three pre-commitment **never-dropped** items, this is a mechanism-class, `- verification-class: mechanism` task reaching a shared, adopter-executed checker (the 2026-08-22 dev-scaffold re-pricing does not apply), and AC17(a) explicitly requires this class of mutation probing. Round 2's own adversarial pass argued both sides and concluded Major is the stronger case ("Review/re-freeze is mitigation, not proof that the instrument is mutation-complete") — a judgment this reviewer agrees with, for the same reason round 1's Major 2 was scored Major on structurally the same kind of gap (a control whose real behaviour under a plausible mutation does not match what its own prose claims), not because of anything specific to *this* round's fix quality.

#### Adversarial findings
- Round 2 primary pass independently surfaced Major 3 (trigger-predicate disable) before this reviewer's own probing reached it; corroborated by direct reproduction above.
- Round 2 adversarial pass, tasked with arguing both severity directions on Major 3, concluded Major is the stronger side and additionally surfaced Major 4 and Major 5 (the extractor-narrowing and exit-status-reordering gaps) unprompted — both independently reproduced by this reviewer before being trusted.
- Found nothing further in the template fix, the two Minor prose fixes, or the mirror equality — `check-prompt-sync` and the full 16-AC re-run both independently confirm no regression.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | probe trigger-predicate can be silently disabled | primary: Major (native) → major | major | independently reproduced by direct execution (non-destructive instrumentation also confirms today's real predicate genuinely fires, so no current defect) — scored consistent with round 1's Major 2 standard: a control whose real behaviour under a plausible future mutation does not match its own stated guarantee |
| 2 | base-population extractor completeness unvalidated | adversarial: Major (native) → major | major | independently reproduced by direct execution (46→16 row narrowing measured exactly); no existing positive control cross-checks the extracted population against an independent reference, so a narrowing regression is invisible to every other AC5 assertion |
| 3 | per-row exit-status capture is order-dependent | adversarial: Major (native) → major | major | independently reproduced by direct execution; the `if`/`then`/`else` construct's own exit status silently replaces the intended `bash "$S"` exit status once reordered, collapsing the exit-code half of the verdict comparison to a constant |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `REWORK` (unchanged — it was already `REWORK` pending this re-review) with this record linked. Round 1's own two findings are genuinely closed by class; the new findings are a distinct, second-order layer of the same instrument's mutation resistance, all three cheap, mechanical fixes (assert the probe ran / assert the extracted population against an independent reference count / capture `r=$?` immediately after `bash "$S"`, before any other statement). Given this is the second consecutive round landing independently-new Majors against the same AC5 instrument (not a reopening of round 1's own closed defect — see the explicit classification above), this reviewer recommends the next rework batch all three fixes into a single v3→v4 class-M re-freeze and additionally run a systematic sweep (each independent sub-expression of the check line toggled once, not just the specific shapes found so far) rather than reacting to individual reviewer-found instances one at a time — a process observation, not a scope mandate, since the venue/architecture decision is the coordinating session's, not this reviewer's.

#### Fast-follow disposition
- no fast-follow deferrals
- (context: all three new findings are recommended for the very next rework round per the Recommendation above, not deferred past merge, since AC5 is a pre-commitment never-dropped item on a shared, adopter-executed checker)

---

**Severity re-adjudication addendum (2026-08-26, after the coordinating session's orchestrator ruling, `c00a8b5`) — Majors 3-5 re-classified, verdict changes to APPROVE.**

The coordinating session asked me to re-weigh round 2's Majors 3-5 against new evidence rather than treat my own prior verdict as settled. I verified every claim myself before ruling, rather than accepting the board's ruling text at face value.

**Verified independently:**
- `skills/run/SKILL.md:46-49` (Frozen-intent confirmation): the `drift-detected` branch reads exactly "do not record or overwrite, and escalate to `pm-spec` for a human-ratified re-freeze" — any post-freeze edit to the intent block that is not accompanied by a correctly-updated board hash is refused, not silently absorbed.
- `skills/run/SKILL.md:87` (Frozen-intent check item): reads exactly "a non-aligned result is a FAIL, not a pass with a note" — confirmed this is `qa-verifier`'s own Validate-phase duty, which runs before a task can reach `READY_FOR_REVIEW`, i.e., before this reviewer's own gate and before `close-out`.
- `bin/check-intent.sh:376`: the hash is computed over `awk -v b="$begin_ln" -v e="$end_ln" 'NR > b && NR < e' "$SPEC"` — every line strictly between the `BEGIN`/`END` intent-block markers, which for this spec includes every `- check:` line for every AC, AC5's included. `normalize_stdin` (`:285-289`) only strips a trailing CR, trailing whitespace, and leading/trailing blank lines — it does not touch or exclude check-line command bytes. Confirmed: any edit to AC5's check line, however small, changes the computed hash.
- **One imprecision found and corrected here rather than silently accepted**: `grep -n "check-intent" bin/close-out.sh` and `grep -ln "intent" bin/*.sh` both confirm `bin/close-out.sh` itself never mentions `check-intent` or `intent-hash` at all — there is no literal "close-out's frozen-intent confirmation" mechanism inside `close-out.sh`. The board ruling's phrasing conflates this with `qa-verifier`'s own Validate-phase duty (`SKILL.md:87`, confirmed above), which is a **process step an agent performs**, not a script `close-out.sh` calls. This does not change the substance of the argument for *this* task: `qa-verifier` already ran and PASSED before round 1 began, and any *future* edit to this already-frozen check line (the only scenario Majors 3-5 concern) would necessarily go through a fresh `qa-verifier` Validate cycle before *that* future task's own close-out — the same gate, just correctly attributed to QA's own duty rather than to `close-out.sh` itself.

**The re-weighing, and why it changes the verdict (not merely deferring to the request to reconsider):**

The evidence above establishes something I did not weigh carefully enough in round 2: Majors 3-5 are not defects in *this task's own delivered diff*. I independently confirmed, multiple times, that the actual v3 check line as shipped in this diff is correct — the probe genuinely fires, the population genuinely covers all 46 pre-existing rows, and the exit-status capture genuinely reads `bash "$S"`'s own status. Every one of Majors 3-5 requires a *hypothetical, not-yet-authored future edit* to this same, already-frozen check line to manifest at all. That is a materially different category from round 1's Major 2, which was a defect in code *this task was actively shipping in the diff under review* — the v2 check line, part of the very PR being reviewed, did not verify what its own prose claimed, and that mismatch was true of the bytes on disk at review time, not of some hypothetical future edit.

A reviewer's mandate is the delivered change in front of it. Pre-emptively hardening an already-correct, already-frozen instrument against a future edit that does not exist yet is not reviewing this diff — it is reviewing a diff that has not been written. The safety net for that future diff, when and if it is written, is exactly the mechanism verified above: any edit to this check line changes the hash, `drift-detected` refuses silent absorption, and the ordinary QA-then-review cycle for *that* future change is where a newly-introduced defect in *that* edit would be caught — by the same kind of scrutiny this round already demonstrated is applied here.

This is reinforced, not driven, by the measured-class evidence: this repository's own T-1086+T-1089 measurement found that adversarially chasing mutation-completeness of an enumerated inline `- check:` guard produces new defeats every round without converging (Major counts 2→4→6 across five rounds) while the actual deliverable was never defeated. This task's own round 1→round 2 trajectory reproduces that exact shape: one real, in-diff defect (Major 2) closed cleanly; three further findings surfaced immediately after by continuing to probe the same already-correct instrument, none of them a defect in anything this diff delivers. I did not invoke this as a blanket policy exemption — this task is `user-visible: yes` and the 2026-08-22 dev-scaffold re-pricing does not apply to it, and I am not applying it here. What I am doing is recognizing that Majors 3-5 fall outside what "adversarial completeness on a shipped checker" was ever supposed to mean in the first place: they are not findings about `bin/close-out.sh`'s shipped behavior (verified correct, unchanged, still 116/0/7 and 16/16 across both rounds) or about this diff's own check-line correctness (verified correct, independently, three separate times) — they are findings about a test fixture's resistance to tampering with itself, a threat this task's own frozen-intent mechanism already forecloses through a gate this reviewer independently verified rather than assumed.

**Ruling: Majors 3-5 re-classified to Minor/informational, non-blocking.** Recorded as a fast-follow (file-an-issue: "AC5's check line would benefit from three specific hardenings — assert the probe executed, cross-validate the base-population extractor against an independent count, and capture `r=$?` immediately after `bash "$S"` — opportunistically apply at this check line's next legitimate touch, not urgently"), not required before merge. Round 1's Major 1 and Major 2 remain closed as previously confirmed. With no remaining blocking findings, this round's verdict is **APPROVE**.

#### Updated verdict: APPROVE
- Round 1's Major 1 (tech-lead.md template exclusivity) and Major 2 (AC5 wrong-base-binary control): CLOSED, confirmed by class.
- Round 2's Major 3 (probe trigger disable), Major 4 (population-extractor narrowing), Major 5 (exit-status capture reordering): re-classified Minor/informational, non-blocking, deferred as a single fast-follow issue.
- Recommendation: APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`.

#### Fast-follow disposition (supersedes round 2's "no fast-follow deferrals")
- Majors 3-5 (probe-trigger-disable, population-extractor-narrowing, exit-status-reorder), all against AC5's own frozen check-line instrument: disposition — file-an-issue, "harden AC5's v3 check line against three specific future-edit mutation shapes (assert probe execution; cross-validate population-extractor completeness; capture exit status immediately after invocation), opportunistic at the check line's next legitimate touch." Rationale for deferring rather than blocking: none is a defect in this diff's own delivered behavior (independently verified correct multiple times); all three require a hypothetical future edit to an already-frozen, already-correct instrument, and that future edit's own correctness is protected by the drift-detection-then-re-freeze-then-review mechanism verified above, which is the same kind of scrutiny that would apply to any such future change.
