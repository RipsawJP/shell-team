### Codex Review verdict: REQUEST_CHANGES
- Task: T-1134
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests
- Diff scope: 16 files, +1557/-6 lines (`git diff --stat 93311ab...HEAD`)

Round 1, review of the delivered change against frozen intent v1
(`1f9e1aa6c61f5d8b67d042cd8d59bd66acf83d3b`) at QA `READY_FOR_REVIEW`
(round 4, commit `4129eb1`).

Verdict scope note (per this round's briefing): `bin/gen-codex-agents.sh`,
`bin/check-codex-agents.sh`, `templates/prompt-blocks/host-dispatch.md`
(+ `skills/run/SKILL.md`, `templates/prompt-blocks/alternate-executor-invocation.md`),
`templates/prompt-blocks/registry.txt`, `.github/workflows/check-handoff.yml`,
`.gitignore`, `docs/adopting.md`/`.ja.md` are adopter-executed/adopter-read
shipped code and prose — full review depth, adversarial pass included.
`tests/codex-agents/run.sh` is development scaffolding — judged on computed
values and conclusions, not adversarial completeness of its inline checks
(per this round's briefing and `CLAUDE.local.md`'s dev-scaffold calibration).

Own independent re-runs before invoking Codex: `bash tests/codex-agents/run.sh`
→ exit 0, 20 PASS / 0 FAIL (matches QA's own count exactly); `CHECK_ACS_TIMEOUT=600
bash bin/check-acs.sh .shell-team/specs/T-1134-codex-host-slice1.md` → 9
passed, 0 failed, 1 skipped; `bash bin/check-intent.sh` → aligned v1, hash
unchanged; `bash bin/check-prompt-sync.sh` → all registered prompt blocks in
sync; AC6's four-read union (`git diff --no-renames --name-only
93311ab...HEAD -- agents/`, `--cached`, unstaged, `git ls-files --others
--exclude-standard -- agents/`) → all four empty, confirmed independently.

#### Blockers

None.

#### Major

- `bin/gen-codex-agents.sh:241-243` — **the control-character refusal omits
  NUL (0x00), so a role-file body containing a NUL byte is silently accepted
  and copied into `developer_instructions`, producing an invalid,
  unparseable TOML file.** Reproduced independently (not accepted from
  Codex's report): appended `a\x00b\n` to a scratch copy of
  `agents/pm-spec.md`, ran `bin/gen-codex-agents.sh` against it — exit 0,
  4/4 files written, the NUL byte present in the emitted
  `shell-team-pm-spec.toml` (confirmed byte search), and
  `python3 -c "import tomllib; tomllib.load(...)"` on that file raises
  `TOMLDecodeError("Found invalid character '\x00' ...")`. The character
  class in the script is `[\x01\x02...\x1f\x7f]` — it starts at `\x01` and
  never includes `\x00`. This is a genuine violation of the spec's own
  Input space class 7 ("A source file containing a control character other
  than tab and newline... Refused rather than silently emitted") and of
  AC3's own text ("the class, not one reported case"), and it lands on
  AC3 — a **never-dropped** component per this spec's Pre-commitment. It is
  not covered by the Non-goals' "hand-crafted adversarial `agents/*.md`"
  carve-out, because that carve-out is about deliberately adversarial
  engineering against the extraction mechanism (a line that is itself
  `developer_instructions = '''`, for instance) — a stray NUL byte from an
  editor/encoding mishap in an otherwise ordinary role file is exactly the
  "lone carriage return"-shaped accidental-corruption class the same
  Input-space item already commits to refusing.
  - Suggested fix: extend the byte-range check to include `\x00`
    (`[\x00\x01...\x1f\x7f]`), and add a fixture for it alongside the
    existing form-feed/carriage-return probes.

- `docs/adopting.md:404-408` / `docs/adopting.ja.md` mirror — **step 2
  ("Grant the repository Codex trust before starting a session in it")
  states the precondition but gives no actionable command or procedure for
  satisfying it.** Read the full section directly (`sed -n
  '375,470p' docs/adopting.md`) and grepped for `trust_level` /
  `--skip-git-repo-check` / any concrete trust-granting instruction across
  `docs/adopting.md`, `docs/adopting.ja.md` and
  `templates/prompt-blocks/host-dispatch.md` — the only hit anywhere is the
  passive mention of `--skip-git-repo-check` as what an adopter must *not*
  do; the actual mechanism the spec's own AC10 recipe uses
  (`-c 'projects."<abs path>".trust_level="trusted"'`, per `##
  Assumptions` item 3 and every QA round's own live-run command) never
  appears in adopter-facing prose. AC9's fixed-string check is satisfied
  (the word `trusted` occurs), but the substance of the adopter-surface
  obligation — "the repository-trust precondition project-level agent
  discovery requires" (AC9's own `- adopter-surface:` line) — is not: an
  adopter reading this step learns a precondition exists, not how to meet
  it, and is left to independently discover Codex's own trust
  configuration.
  - Suggested fix: state the measured `-c 'projects."<repo
    path>".trust_level="trusted"'` config-override form (or Codex's
    interactive trust-prompt procedure on first launch in the repo,
    whichever this task actually exercised) in step 2, in both languages.

- Adopter's own repository never gets a documented instruction to add
  `.codex/agents` to **its own** `.gitignore`, which can trip the loop's
  T-073 clean-tree/uncommitted-diff gate at the Implement-to-Validate seam
  the first time an adopter runs this on a real repository. This is not a
  new observation — QA's own round-2 record already flagged it verbatim as
  a risk note ("A third-party adopter's own repository needs its own
  `.gitignore` entry for `.codex/agents`... worth a decision on whether it
  belongs in the docs' remaining steps") and QA's own AC10 live run needed
  to manually add this entry to the fixture's `.gitignore` to get the run
  through cleanly — direct evidence that without it, the documented happy
  path leaves untracked files sitting in the adopter's working tree at
  exactly the seam this loop's own T-073 guard inspects
  (`skills/run/SKILL.md`'s Implement step: "confirm there is no uncommitted
  implementation diff... `git status --short` shows nothing to commit" —
  `git status --short` reports untracked files too). The risk note was
  carried, undecided, across all four QA rounds and never resolved in this
  round's diff. `grep -n "gitignore" docs/adopting.md docs/adopting.ja.md`
  confirms the only `.gitignore` mentions in the new section are about
  *this repository's own* file, never an instruction for the adopter's.
  - Suggested fix: add one sentence to step 1 or step 5 telling the
    adopter to add `.codex/agents` to their own repository's `.gitignore`
    (mirroring this repository's own comment), closing the QA-flagged risk
    note rather than deferring it a fifth round.

#### Minor / nits

- `bin/gen-codex-agents.sh` — the control-byte check is not a UTF-8
  validator; an invalid high-byte sequence (e.g. a lone `0xFF`) round-trips
  into the TOML unrefused, at which point Codex-side TOML parsing would
  fail on the invalid encoding. Judged **out of the frozen Input space** as
  currently scoped — the repository's own stated security invariant treats
  `agents/*.md` as a trusted instruction channel (operator configuration),
  and the Non-goals' "hand-crafted adversarial `agents/*.md`... an attack
  on a trusted instruction channel" carve-out reads naturally onto
  malformed-encoding injection the same way it does onto a
  `developer_instructions = '''` line planted by hand. Recording as a
  fast-follow rather than a Major, since real shipped role files are
  reviewed prose and are not the threat model this task declines to
  defend.
- `bin/gen-codex-agents.sh:190,273-304` — `--roles` is word-split
  (`ROLES_ARR=($ROLES)`) with no validation of individual tokens; an
  adopter's own malformed override (a token containing a glob character, a
  path separator, or a quote) could produce a surprising filename or
  destination. Low severity because `--roles` is supplied by the same
  local operator who already controls `--root`/`--out-dir` on their own
  machine — a self-inflicted risk shape, not a remote/adversarial-input
  one — but worth a token-shape validation as low-cost hardening.
- `bin/gen-codex-agents.sh:301-304` — the final install loop writes each
  role's file with a direct `cat > destination` per role, sequentially,
  rather than staging into a sibling temp dir and swapping in atomically.
  A mid-loop physical I/O failure (disk full after role 2 of 4) would
  leave a partial out-dir with a nonzero exit — a narrow reliability edge,
  self-recovering because `check-codex-agents.sh` already treats a missing
  expected file as a violation on the next run. Not the class AC3's
  no-partial-output text targets (that text is about *validation*
  refusals, which are already fully resolved in pass 1 before any write in
  pass 2), so not scored as a Major, but a reasonable hardening
  fast-follow (build into a scratch dir, `mv` into `--out-dir` only once
  every file is staged).
- `bin/gen-codex-agents.sh:304` / `bin/check-codex-agents.sh:116,128-129` —
  neither script special-cases a destination or comparison target that is
  itself a symlink (a redirect-through-symlink write in the generator; a
  dangling-symlink extra file silently skipped by the checker's `[ -e ]`
  glob-guard). Judged **out of the frozen Input space**: the spec's own
  Out-of-scope list already declines "An out-dir path that is a symlink...
  The generator performs no symlink-escape analysis... an adopter naming
  such a path gets whatever the filesystem does" — the same reasoning
  reads naturally onto an individual file inside an otherwise-ordinary
  out-dir. Recording as a fast-follow for anyone who later wants tighter
  guarantees, not a defect against this spec.
- `bin/gen-codex-agents.sh:218-226` — a role file with a missing or
  malformed `tools:` field silently derives `sandbox_mode = "read-only"`
  rather than refusing. This is the *safe* direction (fails toward the more
  restrictive declarative value, and the value is declarative-only per
  `D3-sandbox`/AC4's own "not an enforcement boundary" comment), and none
  of the four shipped role files are missing this field — not scored as a
  defect, noted for completeness.
- `docs/adopting.md`/`.ja.md` — a Codex adversarial-pass claim that the
  "no flags at all" parenthetical in step 1 instructs running a bare
  `gen-codex-agents.sh` (relying on `PATH`, which the same section already
  says is false) does **not hold up on a direct re-read**: the parenthetical
  describes the flag *defaults* ("`gen-codex-agents.sh`'s own `--root`
  default is its own plugin root...") rather than presenting a copy-paste
  command that drops the `bash "<plugin root>/..."` prefix shown just
  above it. Verified by reading `docs/adopting.md` lines 375-408 directly;
  rejected as a finding (see the ledger below).
- `docs/adopting.md`/`.ja.md` — step 5's checker example
  (`bash "<plugin root>/bin/check-codex-agents.sh"`) omits `--out-dir` and
  does not restate that it must be run from the adopted repository's own
  root (the checker's own default is `$PWD/.codex/agents`, mirroring the
  generator's default, so an unstated cwd assumption carries over from
  step 1's context but is not repeated). A clarity nit, not a functional
  gap.
- `docs/adopting.md`/`.ja.md` — the regeneration triggers named (a role
  edit, a plugin upgrade) do not mention a `binding.conf`/`TEAM_RUN_BASE`
  change, even though the generator's `model`/`model_reasoning_effort`
  emission depends on the resolved binding. Mechanically harmless —
  `check-codex-agents.sh` already detects this as ordinary drift on its
  next run, since it always regenerates fresh against the current binding
  before comparing — so this is a completeness nit in the trigger list,
  not a gap in the mechanism.
- `docs/adopting.md`/`.ja.md` — the "Honest limits" paragraph states the
  `sandbox_mode` non-enforcement disclosure but does not separately spell
  out that the shipped default binding likewise emits no `model`/
  `model_reasoning_effort` key, so a generated agent inherits the parent
  Codex session's own model rather than any Claude-family pin named
  elsewhere in the document. True and already stated in the generator's
  own header comment and in AC4/D3-model; a documentation clarity
  fast-follow, not a defect.

#### Adversarial findings

Full list Codex returned (a devil's-advocate pass against
`bin/gen-codex-agents.sh`, `bin/check-codex-agents.sh` and the two new docs
sections, told about the two Major findings above so it would not
re-report them): UTF-8/high-byte-sequence non-validation; unvalidated
`--roles` token splitting; a missing `tools:` field silently defaulting to
`read-only`; non-atomic sequential file installation; a symlinked
destination file being written through; the checker following symlinks in
its comparison and its extra-file glob; a misreading of the "no flags"
parenthetical as a bare-`PATH`-reliant instruction; the trust-granting gap
(independently corroborated above and promoted to Major); a regeneration-
trigger list omitting binding changes; undocumented model/effort
inheritance; and the checker example omitting `--out-dir`/cwd context. Each
was independently investigated above rather than accepted at face value;
the disposition (Major, Minor, or rejected) for each is recorded in the
Minor/nits section and the ledger below.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | control-character refusal omits NUL (AC3 gap) | primary: major | major | independently reproduced (rc=0, NUL survives, TOML fails to parse) — a real violation of a never-dropped AC's own declared refusal class, not downgraded |
| 2 | adopter's own repo has no `.gitignore` instruction for `.codex/agents` | primary: blocker (Codex-native P1, mapped per the closed table) | major | downgraded one level from the table's default mapping: the gap is real and already flagged by QA (round 2) as an open risk note across four rounds, but it is a one-line, discoverable, non-destructive documentation addition rather than a mechanism failure — treated as gating this round's REQUEST_CHANGES at Major rather than Blocker |
| 3 | step 2 gives no actionable trust-granting command | adversarial: medium | major | upgraded from the adversarial pass's own "medium" label: AC9's own `- adopter-surface:` line names this precondition explicitly as something the adopter must be told how to satisfy, and the shipped text gives no command at all — a substantive gap in a declared adopter-surface obligation, not merely a wording nit |
| 4 | UTF-8/high-byte non-validation | adversarial: high | minor (fast-follow) | downgraded — covered by the spec's own trusted-instruction-channel security invariant and the Non-goals' hand-crafted-adversarial-source carve-out, which reads naturally onto malformed-encoding injection |
| 5 | unvalidated `--roles` token splitting | adversarial: high | minor (fast-follow) | downgraded — local-operator-supplied CLI flag on the adopter's own machine, a self-inflicted-risk shape rather than an adversarial-input one |
| 6 | non-atomic sequential install | adversarial: high | minor (fast-follow) | downgraded — AC3's no-partial-output text targets validation refusals (already fully resolved before any write), not a physical I/O failure mid-write; the checker's own missing-file detection self-recovers |
| 7 | symlinked destination write-through / checker symlink handling | adversarial: high / medium | minor (fast-follow) | downgraded — the spec's own Out-of-scope list already declines symlink-escape analysis over the out-dir; read as covering an individual file inside it too |
| 8 | missing/malformed `tools:` silently defaults read-only | adversarial: medium | minor (not a defect) | the safe-direction default, and `sandbox_mode` is declarative-only by the spec's own design (D3-sandbox) — no shipped role file is missing this field |
| 9 | "no flags" parenthetical instructs a bare, PATH-reliant `gen-codex-agents.sh` | adversarial: medium | rejected | direct re-read of `docs/adopting.md:375-408` shows the parenthetical describes flag defaults, not a copy-paste command dropping the `bash "<plugin root>/..."` prefix shown immediately above it |
| 10 | regeneration triggers omit binding/`TEAM_RUN_BASE` changes | adversarial: medium | minor (fast-follow) | mechanically harmless — `check-codex-agents.sh` already treats this as ordinary drift on its next run — a completeness nit in the trigger list only |
| 11 | model/effort inheritance not separately stated in "Honest limits" | adversarial: medium | minor (fast-follow) | true but already stated in the generator's own header comment and in AC4/D3-model; a documentation clarity addition, not a defect |
| 12 | checker example omits `--out-dir`/cwd restatement | adversarial: medium | minor (fast-follow) | clarity nit; the cwd context carries over from step 1 and the checker's own default already mirrors the generator's |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings

This round is REQUEST_CHANGES: route back to READY_FOR_ENG with the three
Major findings above (the NUL-byte fail-closed gap, the missing
trust-granting command, and the adopter's-own-`.gitignore` instruction).

#### Fast-follow disposition
- UTF-8/high-byte non-validation in `bin/gen-codex-agents.sh` — won't-fix:
  out of the frozen Input space per the repository's own trusted-
  instruction-channel security invariant; real shipped role files are
  reviewed prose, not the threat model this task's Non-goals declines to
  defend.
- Unvalidated `--roles` token splitting — file-an-issue: low-cost hardening
  (reject tokens outside a safe role-identifier shape) worth doing
  whenever the role list next changes (e.g. slice 2), not urgent enough to
  block this merge.
- Non-atomic sequential install in the generator's pass 2 — file-an-issue:
  stage into a scratch dir and swap in atomically; a narrow reliability
  edge with a self-recovering symptom today.
- Symlinked destination handling in both scripts — won't-fix: covered by
  the spec's own declared Out-of-scope carve-out on out-dir symlink
  handling; revisit only if a future task narrows that carve-out.
- Regeneration-trigger list omitting binding/`TEAM_RUN_BASE` changes —
  file-an-issue: a one-sentence docs addition, low priority since the
  mechanism already self-detects the drift.
- Model/effort inheritance not spelled out in "Honest limits" — file-an-
  issue: a one-sentence docs clarity addition, low priority.
- Checker example omitting `--out-dir`/cwd restatement — won't-fix: minor
  clarity nit inferred correctly from context; not worth a dedicated
  fast-follow on its own, fold into whichever future docs pass touches
  this section next.

#### Review-input fidelity (bin/check-review-input.sh)

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base 93311ab --json -o .shell-team/reviews/.codex-capture.T-1134-codex-primary.out.iQoPtK > .shell-team/reviews/.codex-capture.T-1134-codex-primary.jsonl.bnUhR3 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (primary): T-1134-codex-primary
- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1134-codex-adversarial.out.o5cEgO "Play devil's advocate against this changeset (branch feature/codex-host, base 93311ab — read the diff yourself with git diff 93311ab...HEAD). Focus your adversarial attack on bin/gen-codex-agents.sh, bin/check-codex-agents.sh, and the new docs/adopting.md and docs/adopting.ja.md 'Using shell-team from Codex CLI' sections. Read the files yourself; nothing is pasted here. Two known findings from a prior pass, which you should NOT re-report but should build on/around: (1) the adopter's own repository never gets a documented instruction to add .codex/agents to its own .gitignore, so the generated files can trip a clean-tree/uncommitted-diff gate elsewhere in the loop; (2) bin/gen-codex-agents.sh's control-character refusal regex omits NUL (0x00), so a role file containing NUL bytes is silently accepted and produces invalid, unparseable TOML. Find additional bypasses of the generator's fail-closed guarantees (AC1/AC3/AC4 in .shell-team/specs/T-1134-codex-host-slice1.md), additional TOML-injection or malformed-output shapes, and any other adopter-facing correctness gap in the two doc sections. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > .shell-team/reviews/.codex-capture.T-1134-codex-adversarial.jsonl.bW3QC3 2>&1
- pass-role (adversarial): generation
- briefing-fidelity (adversarial): carried — the two primary-pass findings, the target files and the AC1/AC3/AC4 focus were composed verbatim into the prompt argument this invocation passed to Codex
- raw-capture (adversarial): T-1134-codex-adversarial

---

### Codex Review verdict (round 2): REQUEST_CHANGES
- Task: T-1134
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests
- Diff scope: 17 files, +1918/-6 lines (`git diff --stat 93311ab...HEAD`); round-2 delta specifically `git diff e5f50de..8b91847` — 6 files, +97/-12 lines (`bin/gen-codex-agents.sh`, `docs/adopting.md`, `docs/adopting.ja.md`, `tests/codex-agents/run.sh`, `.shell-team/provenance/T-1134.md`, `.shell-team/todo.md`).

Round 2, review of the delivered change against frozen intent v1 (`1f9e1aa6c61f5d8b67d042cd8d59bd66acf83d3b`, hash unchanged since round 1 — re-confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1134-codex-host-slice1.md .shell-team/todo.md`) at QA `READY_FOR_REVIEW` (round 5, commit `8b91847`), answering my own round-1 `REQUEST_CHANGES` (`.shell-team/reviews/T-1134.md`, three Majors) and QA's round-5 PASS closing them.

#### Round-1 Majors — independently re-verified closed (not accepted from the hand-off)

- **Major 1 (NUL refusal) — closed.** Read `bin/gen-codex-agents.sh:199-262` directly: the new byte-count check (`wc -c` on the source vs. `wc -c` after `LC_ALL=C tr -d '\000'`) sits immediately after the readability check and before every other read (`head`, `awk`, `tail`), so it also covers a NUL sitting inside the `description:` line, not only the body. Reproduced independently on a scratch fixture outside this checkout: `printf 'a\000b\n' >> agents/pm-spec.md` → `rc=1`, zero files written. Multi-role whole-run guarantee probed directly (not just single-role): put the NUL only in the `description:` line of the **last** role in a two-role run (`pm-spec qa-verifier`) — `rc=1`, `--out-dir` never created, confirming the no-partial-output guarantee holds even when the defect is in the last-processed role. Mutation-tested the fix myself (removed the new guard, reproduced the exact pre-fix bug — `rc=0`, invalid TOML with an embedded NUL byte, confirmed with `tomllib.load` raising `TOMLDecodeError` at the NUL — then restored and re-confirmed the fix). No false positive against this repo's own real, Japanese-prose `agents/*.md` (`tests/codex-agents/run.sh` → 21 PASS / 0 FAIL). One adjacent, **pre-existing** (present unchanged since `e5f50de`, not introduced by round 2) observation: on a bash-3.2 host, the `fail()`-triggered `EXIT` trap's `rm -f "${CONTENT_FILES[@]}"` throws `unbound variable` when no role has yet populated `CONTENT_FILES` (confirmed against both HEAD and the round-1 commit) — cosmetic (rc and zero-files-written are unaffected), not a new defect this round, filed as a fast-follow below.
- **Major 2 (trust command) — closed.** `docs/adopting.md`/`.ja.md` step 2 now states `-c 'projects."<repo>".trust_level="trusted"'` plus the `config.toml` equivalent, matching the spec's own `## Assumptions` item 3 and every QA round's own live-run command verbatim. Read directly, not summarized.
- **Major 3 (adopter `.gitignore`) — closed as literally requested, but see the new Major below.** Step 1 now instructs adding `.codex/agents` to the adopter's own `.gitignore`, closing the QA-flagged risk note.

`tests/codex-agents/run.sh` → 21 PASS / 0 FAIL, own live run. `shellcheck bin/gen-codex-agents.sh bin/check-codex-agents.sh tests/codex-agents/run.sh` → exit 0. `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1134-codex-host-slice1.md` → 9 passed, 0 failed, 1 skipped (AC10 by design). `bash bin/check-provenance.sh .shell-team/provenance/T-1134.md` → conformant (13 decisions). `bash bin/check-interventions.sh --task T-1134 .shell-team/interventions/T-1134.md` → conformant (3 entries).

#### Blockers

None.

#### Major

- **New — `docs/adopting.md`/`.ja.md`, "Using shell-team from Codex CLI" step 1 (lines 374-381 / 384-391) — no documented way for a real adopter to install this plugin into Codex CLI at all.** `README.md`'s and `README.ja.md`'s entire `## Install` section is Claude-Code-slash-command-only (`/plugin marketplace add RipsawJP/shell-team`, `/plugin install shell-team@ripsawjp`) — neither file names the Codex CLI equivalent anywhere. `docs/adopting.md`'s Codex section starts from "locate the installed plugin root" and never addresses getting from "not installed" to "installed, enabled" in the first place — it presupposes exactly the state the spec's own precondition 9 measured on the maintainer's own already-configured host. Verified live on this machine (`codex plugin --help`, `codex plugin marketplace add --help`, `codex plugin add --help`): the real subcommands are `codex plugin marketplace add <owner/repo|url>` and `codex plugin add <plugin>@<marketplace>` — a straightforward two-line addition, mirroring the README's existing Claude Code block, is missing entirely in both languages.
  - Suggested fix: add a mirrored Codex install block to `README.md`/`.ja.md`'s `## Install` section (`codex plugin marketplace add RipsawJP/shell-team` then `codex plugin add shell-team@ripsawjp`), and/or a forward reference from `docs/adopting.md` step 1's opening sentence.
- **New — `docs/adopting.md`/`.ja.md` step 1 (lines 378-381 / 388-391) — the claimed Codex plugin cache path is factually wrong on the exact version this spec claims to have measured it against.** The doc states the Codex-side plugin root is found at `<home>/.codex/plugins/cache/<marketplace>/shell-team/<version>/`. Verified live on this machine, same `codex-cli 0.154.0` this task cites throughout: `codex plugin list` reports `shell-team@ripsawjp`'s `SOURCE` as `/Users/<home>/.codex/.tmp/marketplaces/ripsawjp` — no `plugins/cache` segment, no per-plugin subdirectory, no version segment (the marketplace's own git checkout root **is** the plugin, since shell-team's marketplace and plugin are the same repository). The Claude Code half of the same sentence, by contrast, is correct: `~/.claude/plugins/cache/ripsawjp/shell-team/2.5.5/` genuinely exists on this host, independently confirmed with `ls`. The Codex half reads as an extrapolation by analogy from the (correct) Claude Code pattern rather than an independent measurement of Codex's own actual storage layout, despite the spec's own precondition 9 text framing both halves as measured. A real Codex adopter following this sentence literally has no valid path to construct, on top of the missing install instructions above — step 1, the first and most load-bearing step of this entire feature, is not executable as written for anyone but the maintainer's own already-configured host.
  - Suggested fix: replace the fixed-pattern parenthetical with "the exact `SOURCE` column value `codex plugin list` prints for this plugin" (no assumed shape), matching what the doc already does correctly for Claude Code only if that pattern is re-verified too, or state both as "read `SOURCE` directly, do not assume a fixed layout" for both hosts.
- **New (round-2 rework's own addition, `docs-missing-adopter-procedure` class) — `docs/adopting.md`/`.ja.md` step 1's new `.gitignore` sentence (the direct answer to my own round-1 Major 3) creates the exact class of gap it was added to close.** Telling the adopter to add `.codex/agents` to their own tracked `.gitignore` is itself an edit to a tracked file; the sentence never tells them to commit that edit, nor mentions the untracked `.git/info/exclude` alternative. Followed literally, in a repository with a pre-existing tracked `.gitignore`, this leaves `.gitignore` modified-but-uncommitted, which `git status --short` reports non-empty — the identical T-073 clean-tree trip this sentence exists to prevent, just moved from an untracked directory to an uncommitted tracked-file diff. This is my own round-1 suggested fix's own incompleteness surfacing on its literal implementation, not a new defect the engineer introduced independently — owned here rather than pinned on the rework.
  - Suggested fix: append "commit this change before starting the loop (or add the entry to `.git/info/exclude` instead, which needs no commit)" to the same sentence, in both languages.

#### Minor / nits

- `bin/gen-codex-agents.sh`'s `cleanup()` `EXIT` trap (`rm -f "${CONTENT_FILES[@]}"`) throws a cosmetic `unbound variable` on bash 3.2 when `fail()` triggers before any role has populated `CONTENT_FILES` (e.g. the very first role in a run fails NUL/control-char/frontmatter validation) — pre-existing since `e5f50de` (round 1), not introduced this round, `rc` and the zero-files-written guarantee are unaffected. Fast-follow: guard the trap body with `[ "${#CONTENT_FILES[@]}" -gt 0 ] && rm -f "${CONTENT_FILES[@]}"` or declare the array with `CONTENT_FILES=("")` sentinel-stripped, whichever this repo's own bash-3.2 idiom elsewhere prefers.
- The role prose copied verbatim into each generated agent's `developer_instructions` (`agents/pm-spec.md`, `agents/engineer.md`, `agents/qa-verifier.md`) invokes dozens of `bin/*.sh` scripts and `team-paths.sh` by bare name or relative path, assuming either PATH-availability or a checkout-rooted `cwd` — neither of which the shipped Codex-host docs or `templates/prompt-blocks/host-dispatch.md` ever tell a real adopter how to satisfy for a **spawned** agent's own ongoing command execution, as opposed to the one-time `gen-codex-agents.sh` bootstrap in step 1, which precondition 9 does address. QA's own round-2 AC10 live run needed an out-of-band briefing naming `bin/`'s absolute path to work at all — text that is not part of the shipped dispatch. Judged **not a Major** here: the frozen spec's own Non-goals explicitly anticipates this exact class ("If a role's own text turns out to need a host qualifier, that is a finding to route back, not an edit to slip in") and `agents/**` is barred from editing (AC6, never-dropped); this also reads as a pre-existing, both-host limitation rather than something this task newly created — precondition 9's own measurement is scoped only to the one-time bootstrap, and a genuine Claude-Code adopter (installed, non-dogfood) shares the same "`bin/` not on `PATH`" measurement. Recording as a fast-follow per the spec's own routing instruction, with one documentation addition owed regardless (see below).
  - Suggested fix: file the routing issue the Non-goals anticipates, and add one "Honest limits" sentence in `docs/adopting.md` disclosing that a dispatched role's own commands assume `bin/` is reachable from its own execution context, pointing at that issue — matching the paragraph's existing practice of stating other limitations plainly (`sandbox_mode` non-enforcement, the `.git`/`.codex` refusals) rather than leaving this one undisclosed.
- `docs/adopting.md`/`.ja.md` step 2's trust-granting command (`-c 'projects."<repo>".trust_level="trusted"'`) is not robust for an absolute repository path containing an apostrophe, double quote, or backslash — the shell single-quoting and the TOML double-quoted key would need escaping neither the doc nor the equivalent `config.toml` form addresses. Low-probability edge case (rare in real paths); fast-follow, not a fix owed this round.
- Adversarial finding rejected on independent re-read: the round-5 hand-off's "confirmed exhaustive by `git diff e5f50de --stat -- . ':!.shell-team/todo.md'`" was flagged as inconsistent with the same list then naming `.shell-team/todo.md` as a changed file. Not a defect: the pathspec exclusion deliberately omits the one file that trivially always changes (the hand-off append itself), and the list names it separately as "this entry" — self-evident, not something the diff command needed to discover. This is the identical idiom every prior round's hand-off in this task uses (rounds 2-5 all follow the same shape); rejecting as a misreading of an established, consistent convention, not a new inconsistency.

#### Adversarial findings

Full list from the devil's-advocate pass against the round-2 diff and the two candidate findings the primary pass surfaced (told about both so it would build on/around them): the missing Codex CLI plugin-install instructions (promoted to Major above); the stale/wrong plugin-cache-path parenthetical (promoted to Major above, from the pass's own "Minor" label — independently verified against a live `codex plugin list` on this exact host/version rather than accepted at face value); the trust-override command's shell/TOML quoting gap for paths with special characters (kept Minor); and the board hand-off's `git diff --stat` exclusion pathspec appearing to contradict its own file list (investigated and rejected — see ledger).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Major 1 (NUL refusal) | primary: resolved | resolved — closed | independently reproduced, mutation-tested, multi-role last-position probed; no false positive on real role files |
| 2 | round-1 Major 2 (trust command) | primary: resolved | resolved — closed | matches spec's `## Assumptions` item 3 and every QA round's live-run command, both languages |
| 3 | round-1 Major 3 (adopter `.gitignore`) | primary: resolved as literally requested | reopened as a new Major (commit/exclude gap) | the fix answers the letter of the round-1 finding but creates the same class of risk it closes — an uncommitted tracked-file diff at the same T-073 seam — owned as my own round-1 suggestion's incompleteness |
| 4 | no documented Codex CLI plugin-install procedure | adversarial: Major | major | verified live — `README.md`/`.ja.md` are Claude-slash-command-only; `docs/adopting.md` presupposes an already-installed plugin; real `codex plugin marketplace add`/`codex plugin add` subcommands confirmed via `--help` |
| 5 | stale/wrong Codex plugin cache-path parenthetical | adversarial: Minor | major | upgraded from the pass's own label — independently verified factually wrong against a live `codex plugin list` on the exact cited version (real SOURCE has no `plugins/cache` segment), while the parallel Claude Code claim in the same sentence is independently confirmed correct; a real adopter has no valid path to construct |
| 6 | bash-3.2 `unbound variable` on the cleanup trap when the first role fails validation | primary (own investigation, prompted by re-tracing the NUL fix) | minor (fast-follow) | pre-existing since `e5f50de`, cosmetic only (rc/no-partial-output unaffected), not introduced this round |
| 7 | spawned role's own `bin/*.sh` invocations have no documented PATH/cwd resolution on the Codex host | primary (own investigation) | minor (fast-follow) | the spec's own Non-goals explicitly routes this class forward rather than in-task, `agents/**` is barred from editing (never-dropped AC6), and it reads as a pre-existing both-host limitation, not new to this task; one docs-disclosure sentence is still owed |
| 8 | trust command's shell/TOML quoting for special-character paths | adversarial: Minor | minor (fast-follow) | low-probability edge case, no fix owed this round |
| 9 | round-5 hand-off's exclusion pathspec vs. its own file list | adversarial: Minor | rejected | the established, consistent idiom across every round of this task's own hand-offs — not a genuine inconsistency |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings

This round is REQUEST_CHANGES: route back to READY_FOR_ENG with the three Major findings above (the adopter's-own-`.gitignore` commit/exclude gap reopening round-1 Major 3's class; the missing Codex CLI plugin-install instructions; and the stale/wrong plugin-cache-path parenthetical). None of these three touch a never-dropped component (the generator, sync checker, `SKILL.md` indirection, or AC10's own check line are all untouched by this finding set) — this is not a same-class-2 stop-the-task trigger, and this task should proceed to a further rework round rather than escalate to planning. Disclosure: none of this round's findings are a second independent generator (`bin/gen-codex-agents.sh`) defect — the one cosmetic, pre-existing trap issue found during this round's own re-tracing of the NUL fix is not new to round 2 and not scored as a Major.

#### Fast-follow disposition
- Bash-3.2 `unbound variable` on the cleanup trap's first-role-failure path — file-an-issue: low-cost hardening (`[ "${#CONTENT_FILES[@]}" -gt 0 ] &&` guard), cosmetic only today.
- Spawned role's own `bin/*.sh` PATH/cwd resolution on the Codex host — file-an-issue: the spec's own Non-goals names this exact routing path; needs its own task since `agents/**` cannot be touched here. The one-sentence "Honest limits" disclosure addition is owed within this task's remaining rework, not deferred.
- Trust command's shell/TOML quoting for special-character repository paths — won't-fix: low-probability edge case, revisit only if a real adopter hits it.
- UTF-8/high-byte non-validation, unvalidated `--roles` token splitting, non-atomic sequential install, symlinked destination handling, regeneration-trigger list completeness, model/effort-inheritance documentation, checker example `--out-dir`/cwd restatement — unchanged from round 1's dispositions (`.shell-team/reviews/T-1134.md` round 1, `#### Fast-follow disposition`); not re-litigated this round, none of this round's diff touches them.
- No fast-follow deferral for the three new Majors above — they gate this round's REQUEST_CHANGES rather than being deferred.

#### Review-input fidelity (bin/check-review-input.sh)

- executor-invocation (primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base 93311ab --json -o .shell-team/reviews/.codex-capture.T-1134-codex-primary-r2.out.hm8OZv > .shell-team/reviews/.codex-capture.T-1134-codex-primary-r2.jsonl.g8Yj91 2>&1
- pass-role (primary-r2): generation
- briefing-fidelity (primary-r2): not-applicable — the `review` subcommand takes no free-form prompt argument
- raw-capture (primary-r2): T-1134-codex-primary-r2
- executor-invocation (adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1134-codex-adversarial-r2.out.6dQOJX "Play devil's advocate against this changeset (branch feature/codex-host, base 93311ab, current HEAD is round 5 of T-1134's rework — read the diff yourself with git diff 93311ab...HEAD, and the round-2 delta specifically with git diff e5f50de..HEAD). This is T-1134, Codex host slice 1 (spec at .shell-team/specs/T-1134-codex-host-slice1.md). Two known findings from a prior primary pass this round, which you should NOT re-report but should build on/around: (1) role prose in agents/pm-spec.md, agents/engineer.md, agents/qa-verifier.md is copied verbatim into each generated Codex custom agent's developer_instructions (by design, agents/** is never edited/forked), and that prose invokes dozens of bin/*.sh scripts (bin/check-acs.sh, bin/check-intent.sh, bin/check-provenance.sh, bin/check-interventions.sh, bin/derive-populations.sh, team-paths.sh, etc.) by bare name or relative path, assuming either PATH-availability or a checkout-rooted cwd -- neither of which docs/adopting.md's shipped 'Using shell-team from Codex CLI' section or templates/prompt-blocks/host-dispatch.md ever tells a real (non-dogfood) adopter how to satisfy for the SPAWNED agent's own ongoing command execution (as opposed to the one-time bin/gen-codex-agents.sh bootstrap in step 1, which IS addressed); the QA round-2 live Codex run that passed AC10 needed an out-of-band briefing naming bin/'s absolute path, which is not part of the shipped dispatch text. (2) docs/adopting.md and docs/adopting.ja.md step 1 (round-5 addition, closing a prior Codex-review Major) now tell the adopter to 'add .codex/agents to your own repository's .gitignore before running this loop in it', but never tells them to commit that edit or use .git/info/exclude instead, so following this instruction literally leaves an uncommitted tracked-file modification (.gitignore itself) that will itself show up in 'git status --short' and can trip the loop's own T-073 clean-tree gate at the Implement-to-Validate seam -- the exact class of risk this sentence was added to close. Read bin/gen-codex-agents.sh, bin/check-codex-agents.sh, docs/adopting.md, docs/adopting.ja.md, templates/prompt-blocks/host-dispatch.md, skills/run/SKILL.md, and tests/codex-agents/run.sh yourself -- nothing is pasted here. Specifically hunt for: (a) any OTHER adopter-facing procedure the shipped Codex-host docs assume without stating a concrete command (in the shape of findings 1/2 above), (b) any additional gap in the round-5 NUL-byte refusal fix (bin/gen-codex-agents.sh lines around 199-262, the byte-count comparison via LC_ALL=C tr -d and the pre-existing control-character grep) that a reproduction would expose, (c) anything in the trust-granting command text (docs step 2, -c 'projects.\"<repo>\".trust_level=\"trusted\"') that could be wrong or incomplete for a real adopter's absolute path, (d) whether the four PASS/FAIL/round history claims in .shell-team/todo.md's round-5 hand-off and QA round-5 verification match what you find when you independently regenerate/re-run. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > .shell-team/reviews/.codex-capture.T-1134-codex-adversarial-r2.jsonl.VkhTZb 2>&1
- pass-role (adversarial-r2): generation
- briefing-fidelity (adversarial-r2): carried — the two primary-pass candidate findings, the target files, and the four hunt directives were composed verbatim into the prompt argument this invocation passed to Codex
- raw-capture (adversarial-r2): T-1134-codex-adversarial-r2
