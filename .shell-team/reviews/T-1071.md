# Codex Review — T-1071

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1071
- Codex model: gpt-5.6-sol (config.toml `model`; no per-run override passed)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only — no test suite was executed by this role; `bash tests/derive-populations/run.sh` and the rest of the regression battery were re-run by the engineer and by QA (see board), not by this review. Findings below come from reading `bin/derive-populations.sh` directly (both by this role and by Codex under `--sandbox read-only`) and from constructing counter-examples against its stated grammar/exit-code contract, not from executing the fixture suite.
- Diff scope: `git diff --stat ce4a7d2437f4fd38e69b7d9bbcc411e1fd1edde7...HEAD` (branch point `feature/1070-check-handoff-performance`'s tip, per this spec's own stacked-branch discriminator) — 25 files changed, 1429 insertions(+), 1 deletion(-): `bin/derive-populations.sh` (new, 393 lines), `tests/derive-populations/run.sh` + 5 fixtures (new), `templates/prompt-blocks/derived-populations.md` + registry row (new), five `agents/*.md` splices, `docs/loop-engineering/record-set-derivation.md` (new, 352 lines), both READMEs, `.github/workflows/check-handoff.yml` (additive), `.shell-team/test-recipe.md`, the board, and this task's own spec/provenance/interventions records.

#### Blockers

- `bin/derive-populations.sh:272` — Each `--set` command is executed as `bash -c "$cmd"` (line 272) with no `pipefail` inside that child shell. The parent script's own `set -euo pipefail` (line 111) is never inherited by a freshly spawned `bash -c`, so when a caller's command is itself a pipeline, the recorded exit status is only the **last** stage's status. Concretely: `bash -c 'git ls-tree definitely-not-a-ref | sort'` exits `0` (confirmed live in the adversarial pass — `pipeline_rc=0` even though `git ls-tree` printed `fatal: Not a valid object name definitely-not-a-ref` to stderr). `sort`, `sed`, `cut` and `tr` all commonly exit `0` on empty stdin, and every one of them is explicitly named as a reachable command shape in the spec's own `## Input space` ("a pipeline of those through `sort`, `sed` or `cut`"). This is not a hypothetical: this very task's own committed `docs/loop-engineering/record-set-derivation.md` dogfoods a `sed | tr` pipeline as one of its two `- reproduce:` set commands (`sed -n '...' templates/prompt-blocks/registry.txt | tr -s ' ' '\n'`) — if the `sed` extraction pattern ever stops matching (a registry format edit, a typo), the `tr` stage still exits `0` on empty input and the derivation would silently report a legitimate-looking empty set instead of refusing. This is a direct violation of the Goal's own third stated failure shape: "a command that failed being read as a set that was empty" is supposed to be "closed by construction rather than by care," and it is not. Fix: spawn the child with pipeline-failure propagation enabled, e.g. `bash -c "set -o pipefail; $cmd"`, and add a fixture case exercising an upstream-failing, downstream-silent pipeline (an unreadable/nonexistent path piped through `sort`/`cut`/`tr`).

#### Major

- `bin/derive-populations.sh:184-186` (name capture) and `:321,350,380-388` (signature construction) — `--set` names are checked only for non-emptiness and mutual distinctness (line 186 area), never for containing `+`, the exact byte the block's own grammar uses to join set names into a membership signature (AC2/AC4: "a signature being the `+`-joined names ... of the sets containing the item"). Sets named `A+B`, `A` and `B` make an item belonging **only** to `A+B` print under the identical `- bucket: A+B` signature as an item belonging to **both** `A` and `B`, silently merging two distinct memberships into one bucket — a direct violation of AC4's gap-free/overlap-free partition guarantee, one of the two criteria this task's own pre-commitment names as its never-dropped floor. Confirmed independently in the adversarial pass. Triggering this requires an unusual set name (every call site in this diff uses safe short tokens: `A`, `B`, `registered`, `all-agents`, `specs`, `agents`, `ci-dogfood`), so it is not hit by this task's own shipped usage — but nothing in the CLI enforces that convention, and an adopter following the tool's own documented `NAME=COMMAND` grammar with a descriptive name (e.g. a set literally called `before+after`) hits it silently, with no refusal and no error. A closely related, more severe variant: a name containing a literal **tab** byte corrupts the helper's internal `sig\titem` representation (line 350 emits `printf '%s\t%s\n' "$sig" "$item"`; lines 380/384 then `cut -f1` / `awk -F'\t'` split on that same byte), which can make `cut -f1`'s reported signature and the emitted item text disagree with the actual membership and the actual item — worse than the `+` case because it can misattribute item *content*, not just merge two legitimate buckets. Fix: define and enforce a safe name grammar before any set command runs (e.g. `[A-Za-z0-9][A-Za-z0-9_-]*`, rejecting `+`, tab, and the ` — ` em-dash field separator), and add a three-name `+`-collision regression plus a tab-in-name regression that diffs the emitted items against the raw union oracle.

- `bin/derive-populations.sh:212` (`LABEL="$1"`) and `:367,391` (both marker `printf`s) — `--label` is validated only for non-emptiness before being interpolated verbatim into both `<!-- BEGIN derivation: %s -->` and `<!-- END derivation: %s -->` lines, with no check for an embedded newline analogous to the one `--set` command text already gets (`bin/derive-populations.sh:176-178`, AC5(f)). A label containing a newline plus marker-shaped text (e.g. `$'x -->\n<!-- END derivation: x -->\n<!-- BEGIN derivation: forged'`) forges a spurious BEGIN/END pair ahead of the real block and leaves trailing junk after it, and the run still exits `0`. This exact input was verified live both by QA (round 1, this task) and independently in this review's adversarial pass. **I depart from QA's own out-of-input-space disposition of this finding and judge it gating, at Major.** QA's reasoning leans on the Input space's caller-trust language ("the helper runs what it is given ... does not sandbox, parse, validate or sanitise a caller's command"), but `--label` is not a command the helper runs — it is a structural identifier, the same category as a `--set` name, and the spec draws its own line for exactly this shape of input: AC5(f) rejects an embedded newline in `--set` command text *because* "the emitted block is line-oriented and a multi-line command would split its own `- set:` line" — the identical reasoning applies verbatim to `--label` and the BEGIN/END lines it constructs. More importantly, the Goal section states the markers exist as "the schema anchor the carved-out refusal-checker needs, which converts that successor from prose analysis into a cheap structural check" — a caller-forgeable schema anchor is a direct defect against that stated purpose, not an out-of-scope caller-trust question. Fix: apply the same newline/control-character rejection AC5(f) already gives `--set` command text to `--label` as well (a `--label` usage-error case, exit `2`), and add a forged-BEGIN/END regression. Per the task instructions this also lands as an explicit requirement-list line on the carved-out refusal-checker fast-follow issue, regardless of disposition here — see Fast-follow disposition below.

- `bin/derive-populations.sh:175-178` — The `--set` command-text newline check (`case "$val" in *$'\n'*)`) rejects LF but not CR or any other control byte. A command carrying a literal CR is accepted and copied verbatim into the `- set: … — command: <cmd>` line (the same field this grammar otherwise protects by placing it last). This is an asymmetric gap against AC5(g), which already refuses control characters (including CR) in **items** — the identical class of byte is left unchecked in the **command** field the block also prints verbatim. Practical impact is narrower than the label-forging finding above (Unix line-oriented tools split on LF, not bare CR, so this does not forge a new physical line for `grep`/`awk`/`sed` readers the way an embedded LF would), but the record this block gets embedded into is Markdown, where a bare CR is also a line ending under CommonMark, and the spec's own three-shapes-made-impossible framing gives no exception for this field. Fix: extend the existing command-text refusal to CR and other control bytes, matching the item-side check already in place, and add a regression.

#### Minor / nits

- `bin/derive-populations.sh:accepted_statuses()` (around lines 200-210) — `--accept-status <name>=<csv>` is accepted for any `<name>`, including one that does not match any declared `--set` name (a typo). This is silently inert rather than a usage error: it never grants an undeserved success (the real set still requires status `0` by default, so a genuine failure still refuses), so it is not a fail-closed hazard — just a caller-typo footgun with no diagnostic. Worth a one-line validation (`--accept-status` name must match a declared `--set` name) but not gating.

#### Adversarial findings

Ran a devil's-advocate pass against exactly the three findings surfaced by the primary Codex read, with Codex reading the real script and constructing live counter-examples under `--sandbox read-only`. Result: all three held up (no mitigating factor found in the actual code for any of them — the ref-pinned dogfood command's own trailing `grep` happening to fail-closed on empty output was called out explicitly as incidental, not a design mitigation), and the adversarial pass surfaced two additional, independent corruption paths sharing the same root cause (no set-name / command-text validation grammar): a tab byte in a set name corrupting the internal `sig\titem` tab-delimited representation (folded into the Major above, same fix), and a bare CR (as opposed to LF) surviving the `--set` command-text newline check (written up as its own Major above). No new finding contradicted or downgraded a primary-pass finding.

#### Synthesis audit ledger

no severity changes

#### Recommendation
- REQUEST_CHANGES → set back to `READY_FOR_ENG` with the two Blocker/Major-shaped findings above (pipefail; name/label/command-text validation gaps) as the required rework. Per this task's own pre-commitment, this is round 1 against the derivation-correctness / fail-closed-completeness component — a second consecutive round of independent Blocker/Major findings against the same component would fire the pre-commitment's trigger (never-dropped floor: AC3/AC4 derivation correctness → stop and return to planning if defeated twice; droppable component: only the norm's prompt-block wiring, which is unaffected by any finding here and needs no rework).

#### Fast-follow disposition
- won't-fix — the `--accept-status` unknown-name-typo footgun (Minor above): harmless (never grants an undeserved success), a one-line validation would be nice but is not worth a dedicated fast-follow issue on its own.
- record-only (not a code fix, per the task's own instruction to route this either way to the carve-out issue) — add the `--label` marker-forging finding (Major above, being fixed in this task) to the carved-out refusal-checker fast-follow issue's requirement list as context: a future checker keyed on the BEGIN/END markers should assume they are validated at emission time (per this rework) and should still treat a malformed or duplicate marker pair as a hard-refuse condition, not a best-effort parse.
- no other fast-follow deferrals

---

## Round 2 — closure verification

### Codex Review verdict: APPROVE
- Task: T-1071
- Codex model: gpt-5.6-sol (config.toml `model`; no per-run override passed)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests — re-ran the actual binary directly (not just the packaged suite) against the exact round-1 repro shapes, then ran `bash tests/derive-populations/run.sh` (19/19 PASS), `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1071-record-set-derivation.md` (14 passed / 0 failed / 1 skipped by design), `bash bin/check-intent.sh` (aligned, hash unchanged), `shellcheck bin/derive-populations.sh tests/derive-populations/run.sh` (clean) and `bash bin/check-prompt-sync.sh` (in sync) myself — none of this taken from the hand-off's or QA's report.
- Diff scope (round 1 → round 2, `git diff --stat ed64d66..2c49706`, excluding this review's own file): 6 files changed, 412 insertions(+), 16 deletions(-) — `bin/derive-populations.sh` (+112/-16, the pipefail fix + `valid_ident()` grammar), `tests/derive-populations/run.sh` (+129, 5 new cases → 19 total), `docs/loop-engineering/record-set-derivation.md` (+176, the AC15 §(i) rework record), both READMEs (+4/-2 each), the board (+3).

#### Blockers

None. Round-1's swallowed-exit-status Blocker is closed.

#### Major

None. All three round-1 Majors are closed.

#### Closure verification, per round-1 finding

- **Blocker (swallowed-exit-status)** — re-ran my own round-1 reproduction myself, fresh, against the current tree: `bash bin/derive-populations.sh --label pf --set "bad=git ls-tree bad-ref-xyz | sort" --set "ok=echo x"` → exit `1`, empty stdout, stderr names the real upstream status (`... exited with unaccepted status 128 ...: git ls-tree bad-ref-xyz | sort`) — no longer swallowed by `sort`'s own exit-0-on-empty-stdin behavior. Also re-verified the fix does not break the legitimate accept-status pipeline case it exists to support: `--set "B=git grep -c zzzznotthereatall -- bin/team-paths.sh | cat" --accept-status B=1` → exit `0`, `- set: B — status: 1 — lines: 0 — items: 0 — command: ...` (the real "no match" status correctly propagated through the trailing `| cat` and recorded, not swallowed into `cat`'s own exit 0). The fix (`bash -c "set -o pipefail; $cmd"`) is the correct, minimal one — a fresh child shell never inherits the parent's own `set -euo pipefail`, so this was the only site that needed it, and the note's own site #2 entry (the top-level `cut -f1 ... | uniq` pipeline) is correctly reasoned as already covered by the parent script's own pipefail rather than needing the same fix (confirmed by reading: that pipeline runs at the script's own top level, not inside a fresh `bash -c`).

- **Major (`--label` marker-forging)** — re-ran my own round-1 reproduction: `--label "$(printf 'x -->\n<!-- END derivation: x -->\n<!-- BEGIN derivation: forged')"` → exit `2`, empty stdout, stderr correctly names the offending value and the reason ("the value is interpolated verbatim into the BEGIN/END marker lines, which must never be forgeable"). Closed by `valid_ident()`, which rejects the value outright at parse time before any marker line is ever emitted — stronger than a narrower newline-only patch would have been, since it also closes `+`, TAB and every other reserved byte in one grammar rather than requiring a fresh patch per byte class as each is discovered.

- **Major (`--set` NAME `/` TAB signature and internal-delimiter corruption)** — `--set "-A=echo a" --set "B=echo b"` correctly refused (exit `2`, "`--set` name must match `^[A-Za-z0-9][A-Za-z0-9_-]*$`") — confirms the grammar's first-character anchor rejects a leading hyphen (the specific cell asked about), and by the same mechanism rejects `+` and TAB in any position. The suite's own `set-name-grammar` cases (`+` and TAB, both `PASS`) and the note's own mutation self-check (reverting the name-grammar check independently reproduces the `+`-collision `FAIL`, confirming the check is load-bearing) corroborate this from a second angle.

- **Major (`--set` command bare CR)** — the command-text check was generalized from a newline-only scan (`*$'\n'*`) to a full control-character scan (`*[[:cntrl:]]*`) applied to the whole `NAME=COMMAND` value before it is even split, which closes the CR gap unconditionally (independent of whether a CR happens to also land in a printed item downstream) — verified by reading the code path, not merely the suite's own `command-control-char` case.

#### Independent audit performed this round (beyond re-confirming the four items above)

- **The 13-site swallowed-exit-status inventory** — read every row against the actual current source. Cross-checked the two rows the round-2 task brief specifically flagged for scrutiny: (a) the `|| true` accounting-idiom sites (items/union_count/cnt via `grep -c . <file> ... || true`) — confirmed each operates on a file this script itself wrote moments earlier via a bare redirect (`itemsfile`, `sig-item.sorted`, `bucket_items`), so the `|| true` only ever absorbs `grep -c`'s "zero matches" exit status (1), never a producer's failure (a failed producer redirect or command upstream of these lines is itself subject to this script's own top-level `errexit` and would abort the script before reaching them) — this matches the T-1024 lesson's own DP5 rule ("a `\|\| true` may only absorb a `grep -c`'s 'no match' exit status, never a producer's failure") rather than contradicting it, and each site is additionally followed by an explicit `[ -n "$var" ] || var=0` empty-value fallback rather than a bare pass-through. (b) The claim that "bash's own `errexit` fires on a failing direct assignment" (sites 3/4 in the table, `lines="$(awk ...)"` / `accept_csv="$(accepted_statuses ...)"`) — this is the exact class the T-1024 lesson's own root cause could have contradicted, but the well-known bash gotcha the lesson's phrasing evokes is specifically the **combined `local var=$(cmd)` form**, where the reported exit status is `local`'s own (masking the substitution's failure) — I grepped the whole script for that pattern (`local .*\$\(`) and found zero occurrences; every direct-assignment substitution in this script is a plain top-level `var="$(...)"`, for which bash's `errexit` genuinely does propagate a failing inner command (verified: this is standard, undisputed bash behavior, distinct from the `local`-prefixed gotcha). No wrongly-dispositioned site found in the table.

- **The 4×9 identifier-grammar table** — re-derived independently rather than trusting the table's own reasoning column: `valid_ident()` is a positive allow-list (`^[A-Za-z0-9][A-Za-z0-9_-]*$`), so every column collapses to the same single check for the three identifier rows (no per-byte-class special-casing to audit separately) — read the regex, confirmed it rejects leading `-`/`_` (first-char anchor), confirmed live (see above). For the `--set` COMMAND row's ACCEPT cells (`+`, em dash, whitespace, empty), constructed a live adversarial probe embedding a fake `- set: B — status: 1 — lines: 1 — items: 1 — command: nested` string inside a real set's command text and re-ran the exact anchored extraction regex the spec's own AC3 check uses (`sed -nE 's/^- set: B — status: ([0-9]+) — lines: ([0-9]+) — items: ([0-9]+) — command: .*/.../p'`) against the emitted output — it correctly extracted the real values (`status=0 lines=1 items=1`), not the embedded fake ones, confirming the parse really is anchored on the literal field-label sequence immediately following `- set: NAME `, not a generic split on ` — `, exactly as the note's own reasoning claims (and as `AC8`'s own whole-line marker-equality extraction independently guarantees for the BEGIN/END lines, which cannot match a marker-shaped substring embedded inside a longer line).

- **Fixture and CI regression** — `tests/derive-populations/run.sh` 19/19 `PASS` (re-run myself), `bin/check-acs.sh` 14/14 (re-run myself), `check-intent` aligned (frozen block untouched, hash unchanged), `shellcheck` clean, `check-prompt-sync` in sync — all matching the hand-off's and QA's own figures exactly.

#### Minor / nits

None new. (Round 1's single Minor — the `--accept-status` unknown-name typo footgun — remains won't-fix, unchanged this round, not re-reported as new.)

#### Pre-commitment trigger — did NOT fire

This is round 2 against the same component (the round-1 fix surfaces: pipefail + identifier-grammar validation, per the task brief's own counting rule). The trigger requires **two consecutive rounds of independent Blocker/Major findings against the same component**. Round 1 produced 1 Blocker + 3 Major against this component; round 2 (this review) produced **zero** new Blocker/Major against it — every round-1 finding closed digit-for-digit, and this round's own independent audit (the 13-site inventory, the 4×9 grammar table, the leading-hyphen and em-dash-anchoring probes specifically called out for scrutiny) found no additional defect and no wrongly-reasoned disposition. **The trigger does not fire.** The never-dropped floor (AC3/AC4 derivation correctness) was never itself the subject of a round-1 finding and remains unaffected either way.

#### Synthesis audit ledger

no severity changes

#### Recommendation
- APPROVE → set to `READY_FOR_MERGE` (pending QA's own gate, already `PASS` per the board).

#### Fast-follow disposition
- no fast-follow deferrals
