# Codex Review — T-1053

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1053
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 6 files, +270/-1 lines (base `298129c4b67262cb06b843ed9bd0a4e5bd1308f2` = `git merge-base develop HEAD`)

All 9 recognized `check:` lines of the spec re-ran green in this sandbox (`bash bin/check-acs.sh .shell-team/specs/T-1053-retro-mechanization.md` → 9 passed, 0 failed, 1 skipped), `check-intent.sh` reports `aligned` at v1, and the five moved/flipped blast-radius rows (T-1020 AC14 PASS→FAIL, T-1041 AC15 PASS→FAIL, T-1050 AC5 `1/1`→`2/2`, T-1003 AC19 FAIL→PASS, T-1004 AC2 FAIL→PASS) were each independently re-measured here and match the spec's own table. Byte-exactness, single-copy and no-generated-region-contamination — the three properties the Goal actually promises — all hold as claimed; `bin/check-prompt-sync.sh` is green.

The two seams that carry their own blocker-routing text (POST and SING, spliced into `skills/run/SKILL.md`'s `attestation` branch) read correctly at their anchors: POST's "treat that as a freeze blocker" clause sits right before the branch's existing generic "route a broken … line back to `pm-spec`" sentence, and SING supplies its own explicit routing clause because the generic sentence doesn't cover a determiner finding. Both correctly distinguish "freeze blocker" from "wording nit deferred to review" — i.e., from becoming a finding in *this* role's queue later, which is the right ownership call.

The third seam — BLAST, appended to `agents/pm-spec.md`'s `## Spec completion self-check` — is where this review lands its required changes, together with a records-accuracy defect independent of any of the three splices.

#### Blockers

None.

#### Major

- **`agents/pm-spec.md:204` (spec `## Assumptions`) and `.shell-team/todo.md:20` — a stale, uncorrected misattribution.** Both lines state that this task "reddens" `T-1052-records-editorial.md` AC9 and `T-1051-inspection-ux-polish.md` AC12 "by construction," i.e. that editing `agents/pm-spec.md` is what turns those two criteria red. The spec's own, more carefully measured `## Blast radius` table (lines 190–197) and `.shell-team/provenance/T-1053.md`'s third decision say the opposite: both criteria were already FAIL at this branch's own base ref, for an unrelated, pre-existing cause (PR #215's commit `23908e1`, which regenerated `agents/pm-spec.md`'s generated region before this branch was cut) — neither verdict moves (FAIL→FAIL). The engineer hand-off (`.shell-team/todo.md:32`) gets this right ("already FAIL at base for an unrelated, pre-existing cause"). So the record now says two contradictory things about the same fact, and the less accurate one is the one still standing in the spec's `## Assumptions` and in the board's own pm-spec-authored bullet. Neither location is inside the frozen intent block (`## Assumptions` is below `<!-- END intent-block -->`, and the board line is ordinary mutable prose), so this is a same-cost textual correction, not a re-freeze. Given this task exists specifically to stop exactly this kind of unenumerated/inaccurate claim from surviving in records, shipping it with one live in its own paperwork is a real defect, not a nit — fix the two lines to match the Blast-radius table's own, already-correct wording before merge.
- **`.shell-team/todo.md:32` — AC10's own anti-compression clause is violated by the record that is AC10's only verification surface.** AC10 has no `check:` line by design (DP5 — "no command can prove a command was run"), and its own preamble is explicit: "reported item by item, in this order, one line per item … a compressed summary of it does not discharge it." The engineer hand-off reports (a)–(c) item by item as required, then collapses (d)–(g) into one sentence ("AC10(d)–(g) fully re-measured and reported in the hand-off"). QA's own entry (`.shell-team/todo.md:34`) catches this and independently reconstructs the substance of (e)/(f)/(g), judging it sound, then dispositions it as "a hand-off completeness gap, not a substantive defect" — i.e. does not send it back. I disagree with that disposition: AC10 is the one criterion in this spec that a machine can never re-check, its own text was written to specifically forbid this exact shortcut, and even QA's compensating paragraph still reports (e)/(f)/(g) as one bundled block rather than three separate item lines, so the record still doesn't discharge AC10 as its own preamble demands even after QA's pass. This is cheap to close — append explicit, separately-stated (d), (e), (f), (g) lines to the board (QA's own paragraph already contains the substance; it only needs to be split into the mandated shape) — so ask for that rather than accepting the gap.

#### Minor / nits

- **AC10(a) mutation-5 divergence (`git grep -l` file-vs-occurrence blind spot)** — correctly disclosed in `.shell-team/interventions/T-1053.md` and the spec's `## Notes for engineer`; AC3's separate occurrence-count check still catches the safety property. No action needed; noted only so it isn't re-discovered as new.
- **`## Blast radius`'s population line reports "58 spec files" while one entry (`design-note-T-1012.md`) carries no `- [ ] **ACn**` criteria at all** — disclosed in the same section ("produces no differing row"); `check-acs.sh` reports the identical "no acceptance criteria found" message at both faces for it, so it doesn't distort the diff. Wording-only; not worth a fix.

#### Adversarial findings

A second, devil's-advocate `codex exec` pass (raw output: `.shell-team/reviews/T-1053-codex-adversarial.{txt,jsonl}`) targeted the two files above plus the frozen splices generally. It corroborated the misattribution finding independently, corroborated the AC10 compression gap independently (and recommended blocking on it, which is the disposition adopted above), and raised one further point I evaluated and rejected: that the Blast-radius population construction (58 files at HEAD vs. 57 at the base-ref worktree, this task's own new spec counted in the population total but excluded from the base-vs-head diff) is an inconsistent "violat[ion of] its own full-population contract." This is not a new defect — it is the exact tension `.shell-team/provenance/T-1053.md`'s first decision already reasons through and discloses (the spec's own new file cannot be diffed against a base ref where it doesn't exist, but AC9's frozen `check:` line re-derives the population with no self-exclusion clause and would fail the moment a filtered count diverged from a literal `.shell-team/specs/*.md` enumeration). I independently re-ran AC9 live and it passes on exactly this construction, and the adversarial pass's proposed alternative (define the population from the base ref only) would break that frozen, currently-passing criterion. Rejected as already-reasoned and already-verified, not left standing as an open question.

It found nothing new about byte-exactness, single-copy enforcement, or generated-region contamination beyond what AC1–AC9's live re-run already confirms.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `agents/pm-spec.md:85` BLAST item is phase/actor-ambiguous (imperative "enumerate/run/difference" wording in a checklist pm-spec runs pre-`READY_FOR_ARCH`, with no shell and no post-implementation HEAD, unlike its neighboring items which describe spec-text properties rather than live execution) | primary: P1 (→ blocker by the normalization map); adversarial: "high" (→ major), independently corroborated | major, disposed as fast-follow (not blocking this merge) | Real forward-looking ambiguity in normative text meant to generalize to future specs, but the text is inside the frozen intent block (editing it needs a re-freeze), this task's own current deliverable applies the discipline correctly (engineer, not pm-spec, performed the actual base-vs-HEAD diff, matching the section's established "spec-text-property" convention read in context), and AC1–AC9 all pass live — so it is a real risk to *future* applications, not a defect in what is shipped now. |
| 2 | `agents/pm-spec.md:204` / `.shell-team/todo.md:20` misattribute two pre-existing, unrelated FAILs as caused "by construction" by this task, contradicting the spec's own `## Blast radius` table and provenance decision #3 | primary: P2 (→ major); adversarial: "medium" (→ minor) on the same two lines | major, required change | The primary read (major) is the one I adopt: this is a factual contradiction inside the task's own records, cheap to fix (mutable text, no re-freeze), and exactly the class of uncorrected claim this task's own disciplines exist to prevent — a "medium" reading undersells that irony and the trivial cost of the fix. |
| 3 | Blast-radius population construction (58 vs. 57, this task's own spec counted but not diffed) is an inconsistent "violation" of the full-population method | adversarial only: "high" (→ major) | rejected | Already reasoned and disclosed in `.shell-team/provenance/T-1053.md`'s first decision; consistent with AC9's own frozen, no-self-exclusion `check:` line, which I independently re-ran and confirmed PASS on this exact construction. The adversarial pass's proposed alternative would break that passing, frozen criterion. |
| 4 | `.shell-team/todo.md:32` engineer hand-off bundles AC10(d)–(g) into one sentence, against AC10's own explicit anti-compression clause | QA (on the board, `.shell-team/todo.md:34`): flagged, disposed as "not a substantive defect"; adversarial: "high" (→ major), recommends blocking | major, required change (overrides QA's disposition) | AC10 has no executable check — the record is its only verification surface, and its own text was written specifically to forbid this shortcut. QA's own compensating paragraph still doesn't split (e)/(f)/(g) into separate item lines, so the record still fails AC10's stated shape even after QA's review. Independently corroborated by the adversarial pass. Fix is cheap (reformat existing substance into the mandated per-item shape) and does not require re-opening the freeze. |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1053 entry back to `READY_FOR_ENG` with these two required fixes: (1) correct the misattribution at spec `## Assumptions` (~line 204) and board line 20 to match the Blast-radius table's own wording (already FAIL at base, unrelated cause `23908e1`, verdict unmoved); (2) expand the board's AC10 record so (d), (e), (f), (g) each get their own reported line per AC10's preamble, using the substance QA's paragraph already contains. Neither fix touches the frozen intent block or any `check:` line; both are mutable-section edits, so no re-freeze and no re-run of the freeze-attestation pace rule is triggered.

#### Fast-follow disposition

- `agents/pm-spec.md:85` BLAST item's phase/actor ambiguity (who runs the full-population diff and when, given pm-spec has no shell and no post-implementation HEAD at `READY_FOR_ARCH` time) — file-an-issue. Rationale: this is forward-only normative text inside a frozen intent block; fixing the wording needs a meaning-preserving correction and a re-freeze, which is disproportionate to bundle into this task's own merge. This task's own worked application (engineer performs the diff post-implementation, pm-spec only authors the method + a live-re-deriving population criterion) is usable prior art for whoever writes the next `## Blast radius` disclosure in the meantime, so the risk is real but not urgent.

---

## Round 2 (rework `b8bfe92`)

### Codex Review verdict: APPROVE
- Task: T-1053
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope (round 2 only): 2 files touched by the rework commit (`b8bfe92`: `.shell-team/specs/T-1053-retro-mechanization.md`, `.shell-team/todo.md`), 9 insertions / 3 deletions; a follow-up records commit (`6c00d1b`) adds this review file plus the QA round-2 verdict bullet. HEAD is now `6c00d1bbf4c11b400a515e2df2c25b8a42f44413`.

Scope of this round: verify round 1's two required changes are genuinely discharged, confirm no regression, re-verdict. Read `git show b8bfe92` directly (not the commit message or board summaries), the current spec/board text, and ran a fresh `codex exec` confirmation pass plus my own independent checks.

**Required change 1 (misattribution) — discharged, verified independently.** The spec's `## Assumptions` bullet (line 204) and the board's "blast radius, disclosed not chased" sub-bullet (line 20) now both read: "Measured, not assumed: both are already FAIL at this branch's own base ref, for an unrelated, pre-existing cause (PR #215's commit `23908e1`...) — neither verdict moves (FAIL at base, FAIL at head); this task's own edit does not cause either criterion's redness, only adds a further, distinct divergence on top." This matches the `## Blast radius` table's own wording exactly and no longer claims causation. I independently swept every mutable T-1053 record (`.shell-team/specs/T-1053-retro-mechanization.md`, `.shell-team/todo.md`'s T-1053 section, `.shell-team/provenance/T-1053.md`, `.shell-team/interventions/T-1053.md`) for `redden`/`by construction` myself — every remaining hit is either unrelated prose (the BLAST item's generic text, the AC10(a) mutation-5 finding, the AC9 placeholder-red freeze note, the DP6/AC4 line-accounting note) or the engineer/QA narrative bullets *quoting* the old phrase to describe what was fixed — no third live occurrence of the misattribution anywhere.

**Required change 2 (AC10 anti-compression) — discharged, verified independently.** The board's "AC10(d)–(g) fully re-measured and reported in the hand-off" blanket sentence is now four separate, explicitly-labelled lines (`engineer hand-off AC10(d)`, `(e)`, `(f)`, `(g)`), each stating what was measured and the result, immediately below the main hand-off paragraph. This is the one-line-per-item shape AC10's preamble demands.

**Regression check.** `bash bin/check-intent.sh` still reports `aligned` (v1, `c515edce6c1fb6b76b9812f0e58ba8666177bae3` — unchanged, confirming both fixes sit below `<!-- END intent-block -->`); `bash bin/check-acs.sh` still reports 9/9 recognized PASS, AC10 SKIP; `bash bin/check-handoff.sh` and `bash bin/check-pii-shapes.sh --base 298129c4b67262cb06b843ed9bd0a4e5bd1308f2` both exit 0. `git diff --name-only <base>...HEAD` shows only the allow-listed files (the two frozen surfaces, the spec, the board, provenance, interventions, and this review record) — no scope creep.

**One new, narrow finding from the round-2 Codex pass (raw: `.shell-team/reviews/T-1053-codex-round2.{txt,jsonl}`).** The board's new `engineer hand-off AC10(e)` line re-measures every premise in the spec's `## Assumptions` *except* one specific clause of the first bullet: "the branch point carries PR #215's merge commit specifically," which that bullet itself flags as "the relayed part that remains relayed." AC10(e)'s line doesn't measure it or name it as still relayed — it's silently absent. I verified the underlying fact myself (`git show -s --format='%s' 298129c4b67262cb06b843ed9bd0a4e5bd1308f2` → `Merge pull request #215 from RipsawJP/docs/lesson-promotion-2026-08-09`, confirming the premise is true and in fact trivially measurable, not merely relayed). This is a real, narrow completeness gap under the same AC10(e) mandate round 1 already enforced once — but I am not escalating it to a second blocking round: it is a single omitted sub-clause (not a wholesale compression), the spec's own Assumptions bullet says this specific half is "the only half that any criterion depends on" is the *other* half (the corpus-content-holds half, already measured), and both of round 1's substantive, required fixes are otherwise clean. Recording it as a Minor, non-blocking finding rather than reopening the freeze-attestation cycle over one clause.

#### Synthesis audit ledger (round 2)

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Round-2 Codex pass labelled the AC10(e) PR-#215-merge-commit omission "Major" | round-2 codex pass: Major | minor, non-blocking | Single omitted sub-clause (not a compression of multiple items, unlike round 1's finding); the underlying fact is true and I re-verified it directly; the spec's own Assumptions bullet states no criterion depends on this specific half. Escalating a third round over one clause would be disproportionate given both of round 1's substantive fixes are clean. |

#### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1053 entry to `READY_FOR_MERGE`.

#### Fast-follow disposition

- `agents/pm-spec.md:85` BLAST item's phase/actor ambiguity (round 1 finding, unchanged this round — the rework correctly left it untouched, as its own commit message states) — file-an-issue. Same rationale as round 1: frozen intent-block text, fixing it needs a re-freeze, and this task's own worked application is usable prior art in the meantime.
- Board `engineer hand-off AC10(e)` line's silent omission of the "PR #215 merge-commit specifically" sub-premise (round 2 finding, this round) — won't-fix. Rationale: the fact is true and trivially reproducible (`git show -s --format='%s' <base>`) if ever needed; the spec's own text says no criterion depends on this half of the premise; not worth a third review round over one clause.
