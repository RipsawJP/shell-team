### Codex Review verdict: REQUEST_CHANGES
- Task: T-1066
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: 5 files, +513/-0 lines (`git diff --stat 0cbc2d5b20a669ee21b411ec2b20814e42fa7876...HEAD`)

#### Blockers

- `docs/loop-engineering/effort-time-trend.md:61-66` — `rounds-to-approve`（および、それを分母に使う `wall-clock-per-round`）が、issue #248 が求めた「レビューが何ラウンドかかったか」という指標を、telemetry の `codex-reviewer` span 件数から算出しており、同じタスクの git 管理下レビュー記録（`.shell-team/reviews/T-NNNN.md` の `### Codex Review verdict:` 見出し数＝正典）と食い違う。実測で確認: `T-1058` は自身のレビュー記録では **1 ラウンドで `APPROVE`**（`.shell-team/reviews/T-1058.md` に見出しは1個のみ、値は `APPROVE`）だが、note は「2 rounds, last verdict REQUEST_CHANGES — a floor（下限）」と報告している。方向が逆で、telemetry 側は下限どころか**過大カウント**であり、note 自身の「未確認」という注記も誤った理由付けになっている。さらに `T-1053`（正典2ラウンド）/`T-1054`（正典2）/`T-1055`（正典5）/`T-1059`（正典2）はこの指標の表から丸ごと落ちている — spec 本体の `## Notes for engineer` は明示的に「reviews-only series is the honest answer for the window's early tasks... Say so explicitly rather than dropping the task from the table」と指示しており、同じ ledger の隣接指標（`freeze-versions`・`criteria-count`）はまさにこの reviews/board フォールバックで13タスク全件をカバーできているのに、`rounds-to-approve` だけそれをせず沈黙で欠落させている。`wall-clock-per-round` はこの誤りを継承し、T-1058 の値が `3,739,143 ms (62.3 min, 2 rounds)` と報告されるが、正しい分母（1 round）では `7,478,286 ms (124.6 min)` になる — 大きく異なる数値。
  - 提案: `rounds-to-approve` は各タスクの `.shell-team/reviews/T-NNNN.md` の `### Codex Review verdict:` 見出し数を正典として再計算し、T-1053/1054/1055/1059 も表に含める（telemetry の `codex-reviewer` span 件数は "logging-completeness の参考値" として併記するに留める）。`wall-clock-per-round` の分母も同様に修正する。

#### Major

- `docs/loop-engineering/effort-time-trend.md:80-81` — `overhead-share` の declined 理由が「corpus 内のあらゆる `span` 値は issue #248 が言う『producer』roleのいずれかである」と述べているが、これは事実と異なる。実測で確認: 分析対象コーパスに `{"run_id":"20260730T112947Z-i26","span":"close-out","phase":"done","model":"none","duration_ms":null,...}` という行が実在し、`close-out` はオーケストレーション／クローズ処理であって producer role ではない。最終的な disposition（declined）自体は、この行の `duration_ms` が `null` で使えないため変わらないが、declined の**理由文**が誤っている。
  - 提案: 「corpus には producer 以外の `close-out` span が1件存在するが、`duration_ms` を持たないため overhead-share の分子として使えない」という、実態に即した理由に書き換える。
- `docs/loop-engineering/effort-time-trend.md:59,73-74,70-71` — `## Metrics` ledger の複数の `- command:` 行（`engineer-wall-clock-total` の `<run_id>` プレースホルダ、`runtime-duty-items` の `<AC-block-range>`、`criteria-count` の `<task's own spec path>`）が、13タスク分を一般化した疑似コードであり、DP2 が約束する「the quoted command is what a later reader re-runs to check the number（後から読む人がそのまま再実行して数値を検算できる）」を字義通りには満たさない。読者はまず13個の外部値を自分で解決してから初めて実行可能になる。AC3 の機械チェックは「非空であること」しか要求しないため frozen 基準は通過するが、DP2 の再現性の約束は弱まっている。
  - 提案: 各タスクについて実際に走らせた具体的なコマンド文字列（少なくとも代表例1個）を quote するか、外側のループも含めて完全にコピペ可能な形にする。
- `docs/loop-engineering/effort-time-trend.md:78,89` — `duration-per-1k-tokens` の日付別系列は比率のみが示され、issue #248 自身の判定ルール（「tokens が横ばいで比率が上昇なら raw latency、tokens/criteria が上昇して比率が横ばいなら rigor growth」）を実際に適用するのに必要な「日付別トークン総量」が一度も報告されていない。note の最終結論（「ノイズが大きく方向性を主張しない」）自体は誇張していないため致命的ではないが、note が引用する診断ルールをそもそも適用できていない。
  - 提案: 同じ awk 集計から日付別 `tokens` 合計も出力し、`## Rigor vs raw latency` でそれを踏まえて比率の変動を読み解く（あるいは、トークン量が未報告であることを `## Limits` に明記する）。

#### Minor / nits

- `docs/loop-engineering/effort-time-trend.md:22` — non-task run_id の列挙で `20260730T112947Z-i24` と `20260730T140209Z-i24` が重複表記されており、note 自身が `(sic)` として違和感を記録している。実測では `20260730T112947Z` の行は `run_id` サフィックスが `-i26`（`-i24` ではない）で、`## Attribution and de-duplication rules` 側の6件のリスト（`pii, i23, i24, i25, i26, v170`）は正しい。件数（6）自体には影響しない、表示上のラベル誤り。
  - 提案: `20260730T112947Z-i24` を `20260730T112947Z-i26` に修正し、`(sic)` 注記を外す。

#### Adversarial findings

- Codex に「これを壊すには？」という敵対的読みを依頼した結果、上記 Blocker（rounds-to-approve の誤り）と Major（close-out span の誤認）を独立に再現・裏付け。具体的な再現値: `T-1053=2, T-1054=2, T-1055=5, T-1058=1, T-1059=2`（レビュー記録の見出し数、正典）に対し telemetry ベースの note の値は `T-1058=2(floor), T-1059=0` で食い違う。T-1058 について「telemetry の追加行はリリース後のイベントで、対応するレビュー見出しがない＝2は floor ではなく overcount」という具体的な機序も指摘された。`close-out` span の `phase:"done"`, `model:"none"`, `duration_ms:null` という属性も独立に確認された。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | rounds-to-approve / wall-clock-per-round の telemetry 由来の誤り | primary: P1 → adversarial: P1（独立再現、T-1058=1 round が正、wall-clock-per-round も誤り継承と確認） | blocker | 正規化マップ通り P1→blocker。両パスが独立に同一の誤りを実測込みで再現し、spec 本体の明示指示（reviews-only fallback）にも反するため、格上げ・格下げなしでそのまま blocker として採用。 |
| 2 | 実行可能でないプレースホルダ形式の `- command:` 行 | primary: P1（"Replace placeholders with executable metric commands"） | major | 正規化マップでは P1→blocker だが、こちらは読み手側の検算の手間を増やすだけで、実際の数値そのものは（QAと私自身の再検証で）複数一致しており、AC3の機械基準（非空）も満たしている。誤った数値を生んでいる finding 1 とは性質が異なるため、1段階格下げして major とした。 |
| 3 | overhead-share declined 理由の `close-out` span 見落とし（producer role 誤認） | primary: P2 → adversarial: P2（独立再現、実際の行のバイトを確認） | major | 正規化マップ通り P2→major。disposition 自体（declined）は変わらないが、理由文が事実と異なるため deliverable-correctness の finding として major を維持。 |
| 4 | duration-per-1k-tokens に日付別トークン総量が欠落 | primary: P2（"Report token volume alongside the latency ratio"） | major | 正規化マップ通り P2→major。note 自身の結論は誇張していないが、issue #248 の診断ルールを実際には適用できていない完成度の欠落として据え置き。 |
| 5 | non-task run_id の `i24`/`i26` 表記重複 | primary: P3（"Correct the i26 run ID"） | minor | 正規化マップ通り P3→minor。件数に影響せず、note 自身が `(sic)` で自己申告済みのラベル誤り。 |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` を `READY_FOR_ENG` に戻し、上記 Blocker（rounds-to-approve / wall-clock-per-round の再計算）と 3件の Major（close-out span 理由の訂正、command 行の再現性向上、日付別トークン量の追加）を紐付けて差し戻す。Minor の `i24`/`i26` ラベル修正も同じ差分内で一緒に直すのが安価。

#### Fast-follow disposition
- no fast-follow deferrals

#### Additional notes

- **issue-canon reliance**: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — この sandbox から `gh issue view 248 --repo RipsawJP/shell-team` を実行したが `tls: failed to verify certificate` で失敗し、QA が既に記録した既知の sandbox 制約と一致した。issue #248 の relayed 本文とこの spec の記述内容との内部整合性は確認したが、GitHub 上の正典本文そのものへの到達はできていない。
- **Blast radius（T-1065 AC8/AC10）**: 自ら `bash bin/check-acs.sh .shell-team/specs/T-1065-task-class-verification-pricing.md` を `HEAD` で実行し、AC8/AC10 が共に `FAIL` であることを直接確認した（spec の Blast radius セクションの開示と一致）。DP9（disclosed, not repaired）は守られている。
- **population/self-measurement exclusion**: `wc -l` を自分で実行し、QA 時点の1306から現在1309（this task 自身の追加行のみ）まで伸びているが、`t1066` slug を除外した後の値は 1298 で note の宣言値と一致し続けることを直接確認した。設計通りの挙動。
- **duplicate-seq / non-task run_id**: 両方の duplicate-`seq` ペア、および6件の non-task run_id を生データから再確認し、件数はすべて note と一致（表記誤りを除く）。

### Codex Review verdict (round 2): REQUEST_CHANGES
- Task: T-1066
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 3 files, +58/-22 lines (`git diff --stat 8841957..835591b`)

#### Blockers

（なし）

#### Major

- `docs/loop-engineering/effort-time-trend.md:88,111` — `## Metrics` の `duration-per-1k-tokens` と `## Rigor vs raw latency` の `Latency-side reading` が、issue #248 自身の判定ルール（「tokens が横ばいで比率が上昇なら raw latency、tokens/criteria が上昇して比率が横ばいなら rigor growth」）を「どの時点でも／どこにも成立しない」と断言しているが、これは note 自身が報告した by-date 系列と矛盾する。実測で再現: `07-31`→`08-01` は tokens が `2116695`→`11764876`（+455.9%）と急伸する一方で比率は `3397.1`→`3380.5`（−0.5%）とほぼ不変であり、ルールの「rising tokens, flat ratio」の形にほぼ正確に一致する。同様に `08-03`→`08-05` は tokens が `8927471`/`8123260`/`8639702`（互いに約9%以内）とほぼ横ばいの一方、比率は `4342.2`→`5852.4`（+34.8%）と上昇しており、「flat tokens, rising ratio」の候補区間になる。note は別の場所（`08-10`→`08-11` の `duration_ms` 変化 `42566832`→`38917210`、約−8.6%）を「roughly flat」と表現しており、「flat」の許容範囲を場所によって使い分けている（約9%は一方では flat 扱い、他方では扱われない）ため、"neither shape holds anywhere" という断定は一貫した閾値の適用の結果ではない。Codex に敵対的読みを依頼した結果も、両区間（`07-31`→`08-01` および `08-03`→`08-05`）を独立に同一の反例として再現し、断定を "stronger than stated" と評価した。
  - 提案: 「flat」の数値閾値を1つ定義してから系列全体に一貫して適用するか、少なくとも `07-31`→`08-01`（rising-tokens/flat-ratio の候補）と `08-03`→`08-05`（flat-tokens/rising-ratio の候補）の両区間をローカルな一致例として明示的に論じ、「窓全体としては持続的な傾向を裏付けない」という window-level の結論に書き換える。
- `docs/loop-engineering/effort-time-trend.md:67` — `rounds-to-approve` の「logging-completeness reference only」ブロックの結び「Telemetry disagrees with canon on exactly the two tasks ... ; every other task's logging is complete.」が、同じブロックの直前で列挙されている `T-1053 NO-TELEMETRY` / `T-1054 NO-TELEMETRY` / `T-1055 NO-TELEMETRY` と矛盾する。この3タスクは telemetry の `codex-reviewer` span が一切存在しない（0件）のであり、これは「complete（完全）」なロギングの対極であって、"every other task" を13タスク全体に対して読めば偽である。Codex の敵対的読みも同じ結論に到達し、"partly overstated but survives in its material part" と評価した（disagree の定義を telemetry を持つ10タスクに限定すれば "exactly the two tasks" 自体は成立しうるが、"every other task's logging is complete" という結び文は書き換えが必要）。
  - 提案: 「telemetry を持つ10タスクのうち、reviewer-span 件数が正典と一致しないのは T-1058/T-1059 の2件で、残り8件は一致する。T-1053〜T-1055 は telemetry が一切存在せず比較対象にならない」という形に書き換える。
- `docs/loop-engineering/effort-time-trend.md:87-88,107-113` — `duration-per-1k-tokens` の by-date 系列（19日分、`07-27`〜`08-14`）は self-measurement exclusion 済みの**コーパス全体**（`$T/corpus`）から集計されており、`## Metrics` 冒頭が明言する「wall-clock 系のみが `T-1056`–`T-1065` に限定され、`rounds-to-approve`/`freeze-versions`/`criteria-count`/`runtime-duty-items` は 13タスクの window に限定される」という population の切り分けの、どちらにも該当しない — この系列は 13タスクの window にも `T-1056`–`T-1065` の telemetry-bearing サブセットにも絞り込まれておらず、attribution 節で言及される `t1016`/`t1022`/`t1024`/`t1032` 等の window 外タスクの span も含んだ**コーパス全体**の日別集計である。にもかかわらず `## Rigor vs raw latency` は `107` 行目で「the numbers above bear on...」、`113` 行目で「per this window's own... 19 calendar dates」と、この系列を 13タスク window の rigor 指標（`criteria-count`/`freeze-versions`）と同じ母集団であるかのように直接対比しており、比較の前提となる母集団が実際には一致していない。Codex の敵対的読みも独立にこの population mismatch を検出し、「the rigor-versus-latency comparison unsupported」と評価した。
  - 提案: by-date 系列を明示的に「コーパス全体のベースライン」として言明し直すか、window タスクの `run_id` にフィルタしてから日別集計を取り直す。少なくとも `## Rigor vs raw latency` の文中で両者の母集団が異なることを明記する。

#### Minor / nits

- `docs/loop-engineering/effort-time-trend.md:111` — Codex の敵対的読みが追加で指摘: 「`criteria-count`/`runtime-duty-items` が `08-10`/`08-11` で spike していない」という一文は、note 自身が日別の rigor 集計をどこにも構築していないため未検証であり、`T-1056`/`T-1057`（`08-10` の `run_id`）は window 最高値の `criteria-count`=19 を記録している（`109` 行目）。より狭い「`08-10`→`08-11` にかけて rigor が増加していない」という主張自体はおそらく真だが、その根拠となる日別集計が存在しない。
  - 提案: タスク→日付の対応規則（`T-1058` のように複数日にまたがる run をどう扱うか含む）を明示したうえで日別 rigor 集計を組むか、この日付限定の主張自体を削除する。
- `docs/loop-engineering/effort-time-trend.md:109` — `Rigor-side reading` の「`freeze-versions` spikes hard at two outliers (T-1055 at v7, T-1060 at v5) against a v1–v3 baseline everywhere else in the window」という記述を、`## Metrics` の `freeze-versions` 結果行（`73` 行目）と突き合わせて自ら再検証した結果、外れ値2件（T-1055=v7, T-1060=v5）を除いた残り11タスクの実際の値域は `v1`〜**v4**（`T-1062`=v4）であり、note 自身の結び「v1–v3 baseline」は `T-1062` の v4 を勘定に入れ損ねている（件数自体・結論の方向性には影響しない、範囲表記のみの誤り）。round 1 のレビューでも今回のリワークでも触れられていない、round 2 で独立に見つけた新規の軽微な指摘。
  - 提案: 「v1–v3 baseline」を「v1–v4 baseline」に修正する。

#### Adversarial findings

- Codex に「これを壊すには？」という敵対的読みを依頼した結果、上記2件の Major（flatness 閾値の不整合／completeness の過大主張）を独立に再現した上で、前者については note 自身の記述よりも**さらに強い**反例（`07-31`→`08-01`: tokens +455.9%, ratio −0.5% で「rising tokens, flat ratio」にほぼ正確に一致）を追加で提示し、"the blanket claim... is contradicted by the reported series" と評価した。また独立に第三の Major（by-date 系列の母集団が 13タスク window とも telemetry-bearing サブセットとも一致しない population mismatch）を新規に検出した。Minor 1件（`08-10`/`08-11` の rigor 非スパイク主張が未検証）も独立に追加で検出した。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes — primary の2件（[P2]→major 相当）と adversarial が独立に確認・追加した3件目（新規 major）・4件目（新規 minor）はいずれも判定どおりの重大度で採用し、格上げ・格下げ・棄却はいずれも行っていない |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_ENG` に戻し、上記3件の Major（flatness 閾値の一貫適用、completeness 文の書き換え、by-date 系列の母集団明示）を紐付けて差し戻す。2件の Minor（`v1–v3`→`v1–v4` の表記修正、`08-10`/`08-11` rigor 非スパイク主張の削除または裏付け）は同じ差分内で一緒に直すのが安価。

#### Fast-follow disposition
- no fast-follow deferrals

#### Additional notes（round 2）

- **round 1 の Blocker/Major/Minor の解消状況**: 独立に再現・検証済み。(1) `rounds-to-approve`/`wall-clock-per-round` を正典（`.shell-team/reviews/T-NNNN.md` の見出し数）から再算出する修正 — `T-1053/1058/1059/1064` を含む全13タスクについて自ら同じ `grep -cE '^### Codex Review verdict'` を実行し、note の値と完全一致を確認。(2) `overhead-share` の `close-out` span 見落とし修正 — 該当行（`run_id":"20260730T112947Z-i26"`, `duration_ms":null`）を自ら `grep` で再確認し、span inventory（142/138/123/103/59/3/1/1、合計570）も一致を確認。(3) プレースホルダのコマンド行の実行可能化 — 複数のコマンドを自ら実行し再現。(4) by-date token totals の追加 — 同じ awk/paste パイプラインを自ら再実行し、19日分すべての `tokens`/`duration_ms`/比率が note の値と1桁まで一致することを確認。(5) `i24`/`i26` ラベル修正 — 目視確認済み。engineer 自身が追加で見つけた `freeze-versions` の T-1064 アンカー不備（unanchored だと v3, anchored だと v1）も、全13タスクについて自ら anchored/unanchored 両方を再実行し、T-1064 のみが異なることを確認 — round 1 の指摘対象は全て正しく閉じている。今回の3件の Major は、その修正自体ではなく、修正によって新たに追加・書き換えられた分析本文（`duration-per-1k-tokens` の by-date 読解と `Rigor vs raw latency` の narrative）に生じた、別クラスの新規欠陥である。
- **issue-canon reliance**: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — 本ラウンドでも `gh issue view 248 --repo RipsawJP/shell-team` を実行したが `tls: failed to verify certificate` で失敗し、round 1 と同じ sandbox 制約に一致した。issue #248 の relayed 本文とこの note の記述内容との内部整合性は確認したが、GitHub 上の正典本文そのものへの到達はできていない。
- **AC 再検証**: `bash bin/check-acs.sh .shell-team/specs/T-1066-effort-time-telemetry.md` を自ら `HEAD` で実行し、AC1〜AC13 全て `PASS`（13 passed, 0 failed）であることを確認した。今回の3件の Major はいずれも AC の機械チェック対象外（`## Rigor vs raw latency` の narrative の内容自体を検証する AC は存在しない）であり、これは spec の設計どおり — 分析内容の妥当性はレビュアーの判断領域である。

### Codex Review verdict (round 3): REQUEST_CHANGES
- Task: T-1066
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 3 files, +25/-8 lines (`git diff --stat a167adf..5e5955d`)

#### Blockers

（なし）

#### Major

- `docs/loop-engineering/effort-time-trend.md:89,118,121` — round2 の Major 3（母集団の混同）を閉じるために追加された `window-restricted` 系列（telemetry を持つ10タスク `T-1056`–`T-1065` のみ）の population label が、「`criteria-count`/`freeze-versions`/`runtime-duty-items` と同じ母集団」と明言しているが、これは事実と異なる。実測で確認: `criteria-count`/`freeze-versions`/`runtime-duty-items` の各結果行（`73`/`76`/`79` 行目）は `T-1053`（criteria-count=10）・`T-1054`（=16）・`T-1055`（=18）を含む**13タスク全件**を報告しており、これら3タスクは telemetry を一切持たない（`NO-TELEMETRY`）。つまり「10タスクの window-restricted 系列」と「13タスクの rigor 指標」は同じ母集団ではない。この誤ラベルは結び（`121` 行目）にも伝播しており、「within the 13-task window's own dates」という表現で window-restricted 系列（実際は10タスク）をあたかも13タスク全体の window であるかのように記述している。Codex の敵対的読みも独立にこの矛盾を再現し、"the population-matching claim is false" と評価した。これは round 2 で指摘した「母集団の混同」（whole-set-claim-under-enumeration）と同じ欠陥クラスが、修正そのものの中に別の形で再発したものである。
  - 提案: `89`/`118`/`121` 行の記述を「13タスク window のうち telemetry を持つ10タスクのサブセット」という正確な表現に修正するか、真に母集団を一致させたい場合は rigor 側の指標も同じ10タスクに絞って再集計する。
- `docs/loop-engineering/effort-time-trend.md:115,117` — `## Rigor vs raw latency` の flatness sweep が「機械的に一貫適用した（mechanically swept... rather than read by eye）」と明言し、corpus-wide 系列の反例を「これで全3件（all three of these instances）」と結論しているが、実際には引用された `awk` コマンドは隣接する単日遷移のみを機械的に掃引しており、`08-03`→`08-05` の3日間スパンの Shape A 事例だけは本文中で手動に追加されたものであって、同じ手法を体系的に他のスパンへ適用してはいない。note 自身の convention（`111` 行目、「span-internal」の変化を明示的に許容）を同じ基準で3日以上の全区間に適用すると、少なくとももう1件の Shape B 候補が見つかる: `07-31`→`08-02`（tokens `2116695`→`10137553`、+378.9%、rising 閾値超過）の区間で、比率3値（`3397.1`/`3380.5`/`3560.4`）の max/min スプレッドは 5.3%（flat 閾値以内）。自ら算術を再現し、閾値判定（rising/flat）が正しいことを確認した。Codex の敵対的読みも独立にこの算術を再現し、"the arithmetic in the prior finding is correct" と評価した上で、3日以上の全連続区間を機械的に再掃引し「この2件（`08-03`→`08-05` と `07-31`→`08-02`）以外に見落としはない」と確認した。「機械的に掃引した」という完全性の主張自体が、round 2 で修正したはずの「読み手による目視の断定」と同じ性質の欠陥（未検証の網羅性の主張）を、形を変えて再発させている。
  - 提案: 隣接遷移だけでなく3日以上の全連続区間を体系的に掃引する `awk`/スクリプトに置き換え、`07-31`→`08-02` の Shape B を含めた計4件（既存の隣接 Shape B 2件＋`08-03`→`08-05` の Shape A＋`07-31`→`08-02` の Shape B）として結論を書き直す。あるいは convention を「隣接遷移のみ」に明示的に限定し、手動追加した3日スパンの Shape A 主張を削除する。

#### Minor / nits

- `docs/loop-engineering/effort-time-trend.md:3` — イントロの `git diff --stat ... .shell-team/todo.md ... .shell-team/reviews` の再実行結果を「13 lines, 0 deletions」と記載しているが、自ら再実行すると現在は `15 +++++++++++++++`（0 deletions は変わらず）になる。これは QA round 3 が既に開示済みの、spec の `## Input space` が明示的に許容する「board の `## Active`/`## Done` エントリは走行中に成長する」という環境依存の自然増加（このタスク自身の hand-off 追記）であり、この一文が支える不変条件（0 deletions・他タスクのレビューファイルは変更されない）自体は依然として成立している。どの計算値もこの「13」という数字には依存していない。ブロッキングとはしないが、round を跨いで積み残さないよう次回の差分で一緒に更新するのが安価。
  - 提案: 次のラウンドでこの行を再実行結果に合わせて更新するか、「re-run at time of writing; grows monotonically with this task's own hand-off records, see `## Input space`」のような、値そのものを断定しない表現に変える。

#### Adversarial findings

- Codex に「これを壊すには？」という敵対的読みを依頼した結果、上記2件の Major を独立に再現・確証した。(1) 母集団の誤ラベル — telemetry を持つ10タスクの系列を13タスクの rigor 指標と同一母集団であるかのように扱う3箇所（`89`/`118`/`121` 行目）の矛盾を独立に指摘。(2) 網羅性の主張の不備 — `07-31`→`08-02` の Shape B 候補の算術（tokens +378.9%, ratio スプレッド 5.3%）を独立に再現・確認した上で、3日以上の全連続区間を自ら体系的に再掃引し、「見落としはこの1件のみ、他に候補区間はない」ことも確認した。両方とも、round 2 で修正されたはずの「完全性・母集団一致の未検証な断定」という同じ欠陥クラスが、今回の修正の中で別の形をとって再発したものであるとの評価だった。なお Codex の敵対的読みは両方に "medium" という自己ラベルを付けたが、正規化マップ上 "medium" は minor 相当であり、これは Synthesis audit ledger で扱う（下記）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | window-restricted 系列（10タスク）を13タスクの rigor 指標と「同じ母集団」と誤って言明（`89`/`118`/`121`） | primary: [P2]（major相当）→ adversarial: "medium"（正規化マップ上は minor 相当） | major | primary と adversarial のラベルが割れたため、独立の判断でここに記録する。adversarial 自身は事実誤認自体は "false" と明確に認めており（severity ラベルの弱さは finding の妥当性を否定するものではない）、この誤りは (a) round 2 の Major 3 を閉じるために追加された仕組みそのものの中に生じている、(b) note の結び（`## Rigor vs raw latency` の最終段落）にまで伝播している、という2点で deliverable の中心的結論の正確性に直接影響するため、adversarial の弱いラベルを採らず primary の major を維持する。 |
| 2 | 「機械的に掃引し全3件」という網羅性の主張が実際には非網羅（隣接遷移のみ機械掃引＋1件を手動追加）で、少なくとも1件（`07-31`→`08-02`）の Shape B を見落とし（`115`/`117`） | primary: [P2]（major相当）→ adversarial: "medium"（正規化マップ上は minor 相当、算術は correct と確証、他の見落としはないと確認） | major | 同上の理由でラベルの相違を独立判断する。この finding は round 2 で「read by eye ではなく機械的に掃引した」という完全性の担保そのものを新設した箇所に生じており、その担保自体が実際には成立していなかった（未検証の網羅性の断定という、round 2 と同じ欠陥クラスの再発）ため、adversarial の弱いラベルを採らず primary の major を維持する。 |
| 3 | イントロの base-ref diff-stat「13 lines」が現在の再実行で「15 lines」に変化（`3`行目） | primary: [P3]（minor相当） | minor | 正規化マップ通り P3→minor。QA round 3 が同じドリフトを既に「environmentally-expected」として開示済みであり、どの計算値もこの数字に依存しないため、blocker/major には格上げしない。 |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_ENG` に戻し、上記2件の Major（population label の訂正、多日スパンを含む網羅的な flatness sweep への置き換え）を紐付けて差し戻す。Minor（base-ref diff-stat の再実行値の更新）は同じ差分内で一緒に直すのが安価。

#### Fast-follow disposition
- no fast-follow deferrals

#### Additional notes（round 3）

- **round 2 の Major/Minor の解消状況**: 独立に再現・検証済み。(1) flatness 閾値（flat ≤10%、rising/falling >25%）— 自ら corpus-wide（18遷移）・window-restricted（4遷移）の両テーブルを生の telemetry から再構築し、note 自身が引用する `awk` パイプラインをそのまま実行した結果、両テーブルとも note の記述と1行単位で完全一致（corpus-wide: Shape B 2件、単日 Shape A 0件；window-restricted: 0件）。両系列が重複する5日分（`08-10`〜`08-14`）についても byte-for-byte で一致することを自ら `diff` で確認した。(2) totality claims の是正（8/2/3 の accounting、`v1–v3`→`v1–v4`）— 両方とも生の telemetry・reviews corpus・board から独立に再算出し、note の記述と完全一致を確認した。(3) 母集団の分離（corpus-wide と window-restricted の明示的なラベル付け）— 概ね達成されているが、上記 Major 1 のとおり一部のラベル付け自体に新たな誤りが生じている。全体として、round 2 で指摘した3件の Major と2件の Minor はいずれも「別の欠陥として再指摘するに値する残存」を残さず正しく閉じられているが、その修正作業自体が新たに2件の Major（population label の誤り、網羅性の主張の不備）を生んでいる。
- **issue-canon reliance**: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — 本ラウンドでは `gh issue view 248` の再試行は行っていない（round 1・round 2 と同一の sandbox TLS 制約が変化したと考える理由がないため）。issue #248 の relayed 本文とこの note の記述内容との内部整合性は確認したが、GitHub 上の正典本文そのものへの到達はできていない。
- **AC 再検証**: `bash bin/check-acs.sh .shell-team/specs/T-1066-effort-time-telemetry.md` を自ら `HEAD`（`5e5955d`）で実行し、AC1〜AC13 全て `PASS`（13 passed, 0 failed）であることを確認した。今回の2件の Major はいずれも AC の機械チェック対象外（`## Rigor vs raw latency` の narrative の内容自体を検証する AC は存在しない）であり、round 1・round 2 と同様、分析内容の妥当性はレビュアーの判断領域である。
- **同一欠陥クラスの再発（記録目的、裁定はオーケストレータ側の裁量）**: round 2 の board エントリは「whole-set-claim-under-enumeration」（列挙の全称主張が実際のデータと矛盾するクラス）を same-class-2 として記録し、`reconsider-design-premise` を推奨アクションとしていた。今回の round 3 の2件の Major は、形は違えど同じクラス（「完全性・母集団一致を機械的に検証したと主張しながら、実際には一部が未検証または誤り」）に属するように見える。このクラス判定・disposition の決定は本ロールの射程外（board の same-class 記録はオーケストレータの判断領域）だが、パターンとして記録しておく。
