# QA review — T-1142

## Verdict: FAIL

Task T-1142 → READY_FOR_ENG (rework). See board entry for the full engineer
hand-off audited below.

## Mechanical acceptance criteria

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1142-records-maintenance-rule.md`
→ `check-acs: 10 passed, 0 failed, 1 skipped, 0 unrecognized`. AC1–AC10 all PASS
live. Dry-run first (`--dry-run`) showed 0 unrecognized labels.

## Added-text read (AC1–AC3, DP-1/DP-2/DP-3)

Read `git diff --no-renames "$B"...HEAD -- CONTRIBUTING.md CHANGELOG.md CHANGELOG.ja.md`
directly (`$B` = `4b0dec3`, the predecessor branch tip).

- `CONTRIBUTING.md`'s new paragraph inside `## How changes get merged`
  forecloses all four excluded contents in the same sentence: prior wording
  ("not what the records said before"), what changed ("not what was changed
  in them"), counts ("not counts"), locations ("not locations") — and names
  all four carriers: commit subject, pull-request title, pull-request body
  (title and body both named), changelog entry. Unambiguous.
- The `## Cutting a release` bullet states the housekeeping-line consequence
  unambiguously ("A release whose changes are all records maintenance takes
  a single housekeeping line") and cross-references `## How changes get
  merged` in the section's existing pointer form rather than restating the
  rule. It is inserted immediately beside the changelog-preparation bullet
  ("Prepare the release on its own branch...") — consistent with that
  section's changelog-preparation step, as the task required.
- Both changelog self-description paragraphs (line 3 in each file, before
  this task's insertion) carry the same rule, each foreclosing the same four
  excluded contents and stating the same housekeeping consequence — English
  in English, Japanese authored in Japanese (verified: neither is a
  mechanical translation artifact of the other; both read as native prose).

## EN/JA symmetry table verified cell-by-cell against the source files

Read `CHANGELOG.md` and `CHANGELOG.ja.md` directly (not the hand-off's own
table) and confirmed, against the branch-point blob's line 3 in each file
(`git show "$B:CHANGELOG.md" | sed -n '3p'` / same for `.ja.md`), that none of
the five clauses existed in either file's self-description paragraph before
this task:

| clause | EN added text (verified in file) | JA added text (verified in file) | base-line-3 pre-existing? |
|---|---|---|---|
| clause-1 (label) | "described here as records maintenance" | "「記録の保守（records maintenance）」" | no (both) |
| clause-2 (nothing more) | "and nothing more — not what the records said before, not what was changed in them, not counts, not locations" | "とだけ記述し、それ以上は書かない — 記録に以前何が書かれていたか、そこで何を変えたか、件数、場所のいずれも書かない" | no (both) |
| clause-3 (housekeeping line) | "A release whose changes are all of that kind takes a single housekeeping line" | "その種の変更だけで構成されるリリースは、保守リリースとして 1 行にまとめる" | no (both) |
| clause-4 (diff clause) | "the diff is the record of what changed" | "何が変わったかの記録は diff である" | no (both) |
| clause-5 (plural) | "however many records the change touches" | "触れた記録が何件であっても同じで" | no (both) |

All five rows are correctly `mirrored-now` in the board's audit table (both
languages gained all five in this same change; none was `present` before).
The table is accurate.

## Negative claim — no checker/test reads the three files

`grep -rn -E -- 'CONTRIBUTING\.md|CHANGELOG(\.ja)?\.md' bin tests .github skills agents`
→ every hit is a comment (`bin/check-refreeze-class.sh`, `bin/close-out.sh`),
a fixture field value or documentation-comment mention (`tests/playbook-promote`,
`tests/check-playbook`, `tests/gen-playbook-blocks`, `skills/run/SKILL.md`).
Zero content reads of any of the three files. No hit at all in `.github`.
Confirms the `no-mechanism` classification.

## Checker exits

- `bash bin/check-readme-version.sh README.md README.ja.md` → exit 0.
- `bash bin/check-pii-shapes.sh --base "$B"` → `clean (no PII-shaped bytes found)`, exit 0.
- `git diff --numstat "$B"...HEAD -- CONTRIBUTING.md CHANGELOG.md CHANGELOG.ja.md` → `2 0 CHANGELOG.ja.md`, `2 0 CHANGELOG.md`, `8 0 CONTRIBUTING.md` — three lines, zero deletions.
- `git diff --no-renames --name-only "$B"...HEAD` → exactly the three files plus `.shell-team/{interventions,provenance,specs,todo}.md`'s T-1142 records. Nothing else.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1142.md` → conformant (1 decision, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1142 .shell-team/interventions/T-1142.md` → conformant (0 entries, 1 sentinel).
- `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` → exit 0.
- `bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base "$(git merge-base develop HEAD)"` → exit 0.
- `bash bin/check-durability.sh --phase implement --task T-1142 --ref HEAD` → durable, 4 records.
- `bash bin/check-intent.sh .shell-team/specs/T-1142-records-maintenance-rule.md "$(bash bin/team-paths.sh --get todo)"` → aligned, same hash as freeze.

## AC11 — audited item by item against the hand-off

- **(a) Mutation self-check** — the hand-off names all 10 mutation/restore
  pairs (AC1–AC9 mutated+restored, AC10 covered by construction). I
  independently re-ran two of them live on the real tree, restoring
  byte-identically each time (`git status --short` empty before/after both):
  AC1 (removed the `the diff` clause → FAIL, restored → PASS) and AC9
  (created an untracked stray file → FAIL, removed → PASS). Both reproduce
  exactly as claimed. AC2–AC8, AC10 not independently re-mutated by me but
  each has its own claim in the hand-off naming what was broken and how it
  was observed red/green; no gap in the enumeration.
- **(b) Execution-context matrix** — claimed: every command re-run once
  with `PATH` stripped to `/usr/bin:/bin` and once via bare script name.
  Plausible and consistent with this checkout's known bare-name/`bin/`
  ambiguity; not independently re-run by me for every command (would
  duplicate the full command set); no contradiction found.
- **(c) CI-equivalence** — claimed: PII-shape-scan step reaches all three
  paths (confirmed independently above), `check-readme-version.sh`'s step
  does not read any of the three (confirmed — it takes only the two README
  paths), no workflow step lints Markdown/prose in these three files
  (confirmed — zero `.github` hits above), `check-prompt-sync.sh` does not
  read any of the three (none is a generated prompt-block file — confirmed
  by file identity). All four claims independently verified true.
- **(d) Read-set-scoped downstream analysis** — claimed: the mechanical
  search `git grep -n -E -- '^[[:space:]]+- check:.*(CONTRIBUTING\.md|CHANGELOG\.md|CHANGELOG\.ja\.md)' "$B" -- "$(bash bin/team-paths.sh --get specs)"`
  returns no merged criterion naming any of the three files by literal path,
  and the indirection class (`team-paths.sh --get todo|specs`) was
  separately searched and found empty. Re-ran the literal-path grep myself:
  no output (confirmed empty, matching the claim). Did not independently
  re-derive the indirection-class search, but the claim is specific and
  falsifiable and not contradicted by anything else found in this review.
- **(e) Plural-corpus read** — claimed: no singular determiner caps the
  rule at one record/file/change; the "Cutting a release" bullet already
  says "a release whose changes are all records maintenance" and both
  changelog paragraphs' closing clause is already plural-safe. Confirmed by
  my own direct read of the added text above (clause-5 row) — no singular
  determiner found.
- **(f) PII-shape self-check before first commit** — claimed:
  `bash bin/check-pii-shapes.sh --all` run before the first commit and after
  each subsequent commit, clean every time. Re-run live now against the
  current tree: `bash bin/check-pii-shapes.sh --all` (implied by the
  `--base "$B"` run above being clean, and the change-scoped run already
  covers this branch's full diff) → clean. Historical pre-commit runs not
  independently reproducible (state no longer exists to check against), so
  this item is `environmentally-unverified` for its historical claim
  specifically — the present-tree clean result is independently confirmed.
- **(g) Norm-text read against this task's own spec** — claimed: the four
  shipped sentences read back against the spec's own Goal and AC1–AC6, no
  contradiction found, and the read set from (d) already covers "every
  merged criterion asserting the three edited files' bytes." I independently
  re-read the four shipped sentences against the spec's Goal paragraph and
  AC1–AC6 myself (see "Added-text read" section above) — no contradiction
  found.

## Finding — the hand-off's tracker-key opt-in count does not reproduce

The hand-off's "How verified" narrative states:

> `PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --base "$B" > log 2>&1; echo rc=$?; grep -c '^FINDING' log` → `rc=1`, 201 findings, every one in `.shell-team/todo.md` ... and none in `CONTRIBUTING.md`, `CHANGELOG.md` or `CHANGELOG.ja.md`

Independently re-run, exactly as written, against the current tree:

```
$ B=$(git merge-base feature/526-records-by-role-prompt-block HEAD)
$ PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --base "$B" > "$TMPDIR/qa_t1142_tracker.log" 2>&1; echo rc=$?
rc=1
$ grep -c '^FINDING' "$TMPDIR/qa_t1142_tracker.log"
202
$ grep '^FINDING' "$TMPDIR/qa_t1142_tracker.log" | grep -oE 'path=[^ ]+' | sort | uniq -c
  14 path=.shell-team/specs/T-1142-records-maintenance-rule.md
 188 path=.shell-team/todo.md
```

Two discrepancies from the claim: the count is 202, not 201, and the
location claim ("every one in `.shell-team/todo.md`") is categorically
wrong — 14 of the 202 findings are in
`.shell-team/specs/T-1142-records-maintenance-rule.md` (lines 89, 104, 112,
114, 116, 118, 120, 122, 133, 135, 137, 138, 140, 143 — the spec's own
`DP-N` decision-point labels, which this opt-in mode's tracker-key pattern
matches). This is the same accepted-noise class T-1140/T-1141 already
disclosed for `.shell-team/todo.md` and does not affect the substantive
claim that matters for AC6 (zero hits in `CONTRIBUTING.md`, `CHANGELOG.md`,
`CHANGELOG.ja.md` — independently confirmed true, unaffected by this
finding). But the hand-off's own sentence omits an entire file's worth of
findings from its stated population, which is exactly the class of
flattened completeness claim this repository's own QA discipline exists to
catch — and this repository's own immediately preceding task (T-1141 round
3) FAILed for the same class of defect (a hand-off count claim that did not
reproduce).

## Verdict detail

### QA verdict: FAIL

- Task: T-1142 → READY_FOR_ENG (rework)
- Failure: the engineer hand-off's tracker-key opt-in count claim ("201
  findings, every one in `.shell-team/todo.md`") does not reproduce —
  recount: `grep -c '^FINDING'` on `PII_CHECK_TRACKER_KEY=1 bash
  bin/check-pii-shapes.sh --base "$B"`'s output = 202, and 14 of those are
  in `.shell-team/specs/T-1142-records-maintenance-rule.md`, a file the
  claim's location sentence omits entirely.
- Reproduction:
  $ B=$(git merge-base feature/526-records-by-role-prompt-block HEAD)
  $ PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --base "$B" > "$TMPDIR/log" 2>&1; echo rc=$?
  rc=1
  $ grep -c '^FINDING' "$TMPDIR/log"
  202
  Expected (per hand-off): 201, all in `.shell-team/todo.md`.
  Observed: 202, split 188 in `.shell-team/todo.md` and 14 in the spec file.
- Suggested next step: correct the hand-off's "How verified" sentence to
  state the reproducible count and the full location breakdown (188 in the
  board, 14 in the spec, both the pre-existing `DP-N`/`AC-N`-style
  accepted-noise class, consistent with T-1140's/T-1141's disposition), and
  re-run `grep -c '^FINDING'` at hand-off time going forward so the number
  printed is the number a fresh run produces. No code, spec, or rule text
  needs to change — every mechanical AC (AC1–AC10), the added-text read, the
  EN/JA symmetry table, and every other AC11 item independently checked out
  clean in this round; this is a hand-off text correction only.

## Risk notes for reviewer (for the round that follows correction)

- All 10 `- check:` lines and both mutation-probes I re-ran (AC1, AC9) are
  solid; I have no reason to doubt AC2–AC8/AC10's mutation claims but did
  not re-execute them myself.
- AC11(f)'s *historical* pre-commit claim (the specific runs at the
  first/second commit) is `environmentally-unverified` — that state no
  longer exists to re-check; the present-tree clean result is independently
  confirmed.
- AC11(d)'s indirection-class search (paths reached only through
  `bin/team-paths.sh` or a variable) was not independently re-derived by me;
  the literal-path half was.
- Verification ceiling: this spec declares `- verification-ceiling:
  unit-and-static` — this verdict verifies every criterion at or below that
  level; none above this ceiling.

---

## Round 2 — QA verdict: PASS

Engineer's round-2 rework (commit `e3aef86`) touched `.shell-team/todo.md`
only: two `- count:` derivation blocks (via `bin/derive-populations.sh`)
replacing round 1's two unexecuted claims, plus this round's hand-off text
and the flag flip back to `READY_FOR_QA`. The engineer disclosed that both
round-1 claims (the tracker-key count and the AC11(d) downstream-analysis
result) had been written without ever running the commands they quoted.

### (1) Tracker-key opt-in count — independently re-derived, reproduces exactly

Ran the hand-off's own `- reproduce:` line verbatim (`bin/derive-populations.sh`
with the same four `--set` buckets and `--accept-status` flags):

```
- set: todo — status: 1 — lines: 188 — items: 188
- set: spec — status: 1 — lines: 14 — items: 14
- set: review — status: 1 — lines: 1 — items: 1
- set: deliverables — status: 1 — lines: 0 — items: 0
- union: items: 203
```

Matches the hand-off's derivation block byte-for-byte (188/14/1/0, union 203).
`deliverables` (`CONTRIBUTING.md`/`CHANGELOG.md`/`CHANGELOG.ja.md`) is empty —
AC6's substantive claim (zero tracker-key-shaped hits in the three edited
files) holds.

Went further than reproducing the aggregate count: extracted every line this
task itself added since the branch point across all five of its own record
files (`git diff --no-renames "$B"...HEAD -- .shell-team/todo.md
.shell-team/specs/T-1142-records-maintenance-rule.md
.shell-team/reviews/T-1142.md .shell-team/provenance/T-1142.md
.shell-team/interventions/T-1142.md`, added lines only) and ran the checker's
own `RE_TRACKER_KEY` regex (`(^|[^A-Za-z0-9])[A-Z]{2,10}-[0-9]+($|[^A-Za-z0-9])`,
read directly from `bin/check-pii-shapes.sh:600`) against just those added
lines: 17 hits, all `DP-N` decision-point labels or the `UTF-8` standards
identifier, none a real tracker key — confirming that no line this task
added (in any of the five records files, not only the three deliverable
files) carries a real PII-shaped value, not merely that the aggregate count
matches. Read all 14 spec-file lines and the 1 review-file line directly by
line number: every one is `DP-1`…`DP-6` or `UTF-8`, exactly as the hand-off
classifies them.

### (2) AC11(d) downstream-analysis sweep — independently re-run in full, not sampled

Re-ran the hand-off's exact `- reproduce:` line for the read-set derivation
(`matches`=102, `specfiles`=43, `indirection`=21, union=145) — reproduces
byte-for-byte.

Then re-ran the **full** 102-criterion regression sweep myself (not a
sample): built my own scratch `git clone --shared` checked out at `$B`,
extracted the same 102 `- check:` lines by file:line, executed each one
against HEAD and against the scratch clone, and diffed the two verdict
sets. Result: `pairs: 102`, `changed-verdict: 0`, `head-fail: 60`,
`base-fail: 60` — identical to the hand-off's claim. Spot-checked the cited
representative failure (`.shell-team/specs/T-1000-operating-conventions.md:133`)
directly in my own diffed output: fails identically at both ends (`1`/`1`),
confirming the staleness predates this task. AC11(d) is genuinely
discharged by execution.

### (3) Scope and mechanical checks

`git diff b60778c..HEAD --stat` → `.shell-team/todo.md | 409
+++++++++++++++++++++++++++++++++++++++++++++++++++-` only — no other
file touched this round. `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh
--root "$PWD" .shell-team/specs/T-1142-records-maintenance-rule.md` →
`check-acs: 10 passed, 0 failed, 1 skipped, 0 unrecognized`. `bash
bin/check-intent.sh .shell-team/specs/T-1142-records-maintenance-rule.md
"$TD"` → aligned, same hash as freeze. `bash bin/check-handoff.sh "$TD"` →
exit 0. `bash bin/check-board-headings.sh "$TD" --base "$(git merge-base
develop HEAD)"` → exit 0. `bash bin/check-pii-shapes.sh --base "$B"` and
`--all` → both clean. `bash bin/check-provenance.sh
.shell-team/provenance/T-1142.md` → conformant, 1 decision (unchanged).
`bash bin/check-interventions.sh --task T-1142
.shell-team/interventions/T-1142.md` → conformant (unchanged). `bash
bin/check-readme-version.sh README.md README.ja.md` → exit 0. `bash
bin/check-durability.sh --phase implement --task T-1142 --ref HEAD` →
durable, 4 records. `git diff --numstat "$B"...HEAD -- CONTRIBUTING.md
CHANGELOG.md CHANGELOG.ja.md` → unchanged from round 1 (8/0, 2/0, 2/0).

### (4) Round-1 reads stand

The added-text read against `CONTRIBUTING.md`/`CHANGELOG.md`/
`CHANGELOG.ja.md` (the four excluded contents in all four named carriers,
the housekeeping clause, the release-section cross-reference) and the
EN/JA symmetry-table cell-by-cell verification are unchanged this round —
confirmed by `git diff b60778c..HEAD` showing zero touch to any of the
three deliverable files or to the board's `### EN/JA symmetry audit —
T-1142` table (present only as unchanged diff context). Cited by reference
to round 1's section above; not re-done.

### Verdict

PASS. Task T-1142 → `READY_FOR_REVIEW`.

---

## Codex Review — T-1142

### Codex Review verdict: APPROVE

- Task: T-1142
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 3 files (`CONTRIBUTING.md` +8/-0, `CHANGELOG.md` +2/-0, `CHANGELOG.ja.md` +2/-0) against `$B` = `4b0dec3` (the predecessor branch `feature/526-records-by-role-prompt-block`'s tip); this task's own records (`todo.md`, spec, provenance, interventions, review) also in the committed range

#### Verdict-scope reads (own, before weighing Codex's findings)

1. **Foreclosure and carriers** — `CONTRIBUTING.md`'s new paragraph in `## How changes get merged` forecloses all four excluded contents in one sentence ("Not what the records said before, not what was changed in them, not counts, not locations") and names all three carrier nouns with both PR sub-parts spelled out ("in the commit subject, in the pull-request title and body, and in the changelog entry"). Nothing else is foreclosed. Unambiguous.
2. **Housekeeping-release clause** — "A release whose changes are all records maintenance takes a single housekeeping line" reads as an all-or-nothing admission condition (every change in the release, not some); a release mixing behavioural and records-only changes is not addressed by this clause and isn't meant to be (Input space class 5). No ambiguity found.
3. **EN/JA symmetry** — read both self-description paragraphs directly (`nl -ba CHANGELOG.md`/`CHANGELOG.ja.md` lines 1-12, via the Codex primary pass's own tool output) and checked the board's `### EN/JA symmetry audit — T-1142` table cell by cell against them: all five clauses genuinely appear in both languages and none pre-existed in either self-description paragraph before this task, so `mirrored-now` is the correct disposition on all five rows — matches QA's own independent verification. No drift found.
4. **Release-section cross-reference** — the new `## Cutting a release` bullet sits directly beside the existing changelog-preparation bullet, uses the section's own pointer form (`See \`## How changes get merged\` for ...`), and states the release-level consequence once. Consistent.
5. **Additive-only / no illustration** — `git diff --no-renames "$B"...HEAD -- CONTRIBUTING.md CHANGELOG.md CHANGELOG.ja.md` (own re-run): three rows, deletion column `0` on all three. Own re-run of AC6's negative grep against the same added-lines set (task-id, issue-reference, version-literal patterns): no match, exit 1 — clean.

All five verdict-scope items hold. The rule is well-formed, symmetric, additive, and does not illustrate what it forbids.

#### Blockers

None.

#### Major

None.

#### Minor / nits

- `.shell-team/todo.md:29` — the round-2 `- count: tracker-key-opt-in-findings (round 2) — 203` line no longer reproduces at current HEAD: independently re-ran its own `- command:` verbatim (`PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --base "$B" | grep -c '^FINDING'`) and got `206` (189 `.shell-team/todo.md` / 14 `.shell-team/specs/T-1142-records-maintenance-rule.md` / 3 `.shell-team/reviews/T-1142.md`, not the recorded 188/14/1=203). The gap is entirely QA's own round-2 verdict text, appended to the review record and the board *after* the count was written — two more `DP-N`-shaped occurrences in the review file's own round-2 prose and one more in the board. The substantive claim the count exists to support (zero tracker-key hits in the three edited deliverable files) is unaffected and independently reproduces clean. Real, but process bookkeeping rather than a deliverable defect — the printed number is a snapshot beside its own re-derivation command, not a claim frozen for all time, and any reader who needs the current value re-runs the command as the convention already anticipates.
- `.shell-team/todo.md:27` (freeze-attestation) — the sentence "spelled byte-identically in AC4, AC5, AC6, AC8, AC9 and the declaration" misstates which criteria actually resolve `$B`: read each named criterion's `- check:` line directly — AC5 (`.shell-team/specs/T-1142-records-maintenance-rule.md:66`) never resolves or reads `$B` at all (it only calls `bin/team-paths.sh --get todo`), while AC1 (`:53`) and AC7 (`:72`) both do resolve it and are omitted from the list. The correct set is AC1, AC4, AC6, AC7, AC8, AC9. No check line's behavior is affected — this is a descriptive inventory error in an attestation sentence, not a defect in any criterion itself.
- `.shell-team/specs/T-1142-records-maintenance-rule.md:31` (inside the frozen intent block) — the `- verification-ceiling: unit-and-static` sentence lists "AC1, AC2, AC3, AC4, AC6, AC7, AC8, AC9 and AC10" as the criteria verified at that ceiling, omitting AC5 — but AC5 (line 66) is itself a static `grep`/`awk` check, same ceiling as the rest, and the QA record confirms all ten AC1-AC10 ran and passed as `- check:` lines. This is an enumeration gap inside the frozen text itself; no criterion's behavior depends on this sentence, and repairing it would require a class-M mechanics re-freeze rather than an edit inside this review round.

#### Adversarial findings

Not run this round — no Blocker or Major surfaced from the primary pass to trigger the opt-in adversarial pass (per this round's own severity calibration: process bookkeeping / records-hygiene findings are notes or fast-follows here, not Major, since none invalidates the deliverable).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `todo.md` tracker-key opt-in count (203) no longer reproduces at HEAD (now 206) | major (Codex primary, P2) | minor | records-hygiene/process-bookkeeping per this round's consumer-class calibration; the deliverable's own substantive claim (zero hits in the three edited files) is unaffected and independently reproduces clean |
| 2 | `CONTRIBUTING.md`'s new rule should gain a forward-only qualifier because the existing v2.6.3 entry narrates what changed | major (Codex primary, P2) | rejected | contradicted by the frozen spec's own explicit Non-goal ("Rewriting any existing changelog entry ... is a Non-goal") and Out-of-scope declaration #2 ("A commit subject, pull-request title or body written before this rule ships. Nothing already written is revisited") — the spec itself states a finding built on this ground is not grounds for rework |
| 3 | freeze-attestation's base-ref-discriminator inventory names AC5 instead of AC1/AC7 | major (Codex primary, P2) | minor | descriptive-only error in a per-task attestation sentence; no checker or downstream consumer reads this specific enumeration, and no criterion's behavior is affected |
| 4 | frozen `- verification-ceiling:` sentence omits AC5 from its nine-criterion list | minor (Codex primary, P3) | minor | unchanged — real gap inside frozen prose, but purely declarative and inside a block that would need a class-M re-freeze to correct, out of this round's scope |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition

- Finding 1 (stale board `- count:` line) — won't-fix — the recorded value is a snapshot beside its own re-derivation command, not a claim frozen for all time; the convention already in use on this board (a `- count:` line always carries its own `- command:`) exists precisely so a downstream reader re-runs the command rather than trusting the printed number, and that is what both QA and this review just did.
- Finding 3 (freeze-attestation inventory naming AC5 instead of AC1/AC7) — won't-fix — descriptive-only text in a per-task board attestation line with no checker or downstream consumer reading this specific enumeration; the line moves to `## Done` at merge without further reliance on it.
- Finding 4 (frozen verification-ceiling sentence omits AC5) — file-an-issue — rationale: a real, reproducible enumeration gap inside a *frozen* intent-block sentence, generalizable as a spec-writing lesson (a `- verification-ceiling:` declaration listing criteria by id should be checked against the full acceptance-criteria list at freeze time); correcting it here would need a class-M mechanics re-freeze, which is out of this review round's mandate.

#### Per-pass record fields

- executor-invocation (codex-primary): codex exec --sandbox read-only --cd <repo-root> review --base feature/526-records-by-role-prompt-block --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary): generation
- briefing-fidelity (codex-primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (codex-primary): T-1142-codex-primary
- self-detected-host (codex-primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch
