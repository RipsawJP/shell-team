# T-1023 — QA verification record

## QA verdict: PASS

**Scope**: verify the frozen v1 spec (`.shell-team/specs/T-1023-block-size-deferral-record.md`,
intent-hash `be03c06e1a550a9b4f85979563bc0e9162c329dd`) against the engineer's hand-off
(commits `3e91668` docs-only, `18e5dd6` printf/provenance/board), `READY_FOR_QA` -> `READY_FOR_REVIEW`.

### Mandatory conformance gates

- `bash bin/check-intent.sh .shell-team/specs/T-1023-block-size-deferral-record.md .shell-team/todo.md`
  -> `aligned: T-1023 v1 (be03c06e1a550a9b4f85979563bc0e9162c329dd)`, hash unchanged.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1023.md` -> `conformant` (2 decision
  entries, both read and judged sound below).
- `bash bin/check-interventions.sh --task T-1023 .shell-team/interventions/T-1023.md` ->
  `conformant` (3 entries, 0 sentinel) — judged structurally only, per the checker's own
  remit (no judgment on whether `human-correction` was the right class token).

### check-acs.sh

`CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1023-block-size-deferral-record.md`:
before this record existed, **13 PASS, AC14 FAIL (solely `.shell-team/reviews/T-1023.md` missing),
AC15/AC16 SKIP by design**. After writing this record: **14 PASS, AC15/AC16 SKIP by design** —
0 failed, exactly the shape the spec predicted (AC1–AC13 measured green, AC14 gated only on this
file).

### AC15 — prose-reading fidelity item (performed by reading)

Read `docs/loop-engineering/playbook-block-size-deferral.md` end to end, claim by claim and
signal by signal, against the eleven canonical lines locked by AC2–AC6.

- No line converts "unmeasured and unobserved" into a smallness claim. The `## What this
  record is not` section's first bullet states the disclaimer in the same affirmative shape
  claim-1 already uses ("the mechanism is real, and its harm has never been measured, in
  either direction") rather than a negated form — matches the provenance record's disclosed
  reasoning and independently confirmed by reading.
- No line converts the signal-driven trigger into a plan or schedule. The "Not a plan" bullet
  states re-evaluation depends only on whether a signal has fired, and explicitly disclaims a
  "choice to defer action to a later point in time" — consistent with claim-5/D4, no schedule
  vocabulary anywhere (also mechanically confirmed, AC7 PASS).
- No line presents the 40 as having a basis. The "Not a defense of the number 40" bullet
  restates claim-2's "no technical basis" without softening it.
- No line-number citation into any spec anywhere in the document (mechanically re-confirmed:
  `grep -nE '\.md:[0-9]+|line [0-9]+|Line [0-9]+'` over the document returns nothing).

**D8 citation soundness (task-specific item 3):** the two citations into
`.shell-team/specs/T-1020-supersede-adjudication.md` quote heading text with the numeral
section prefix stripped:
- doc says `"What this accounting does and does not establish"` — real heading is
  `### 3.4 What this accounting does and does not establish`.
- doc says `"Observations recorded, not acted on"` — real heading is
  `## 8. Observations recorded, not acted on`.

Both quoted strings are exact substrings of the real headings (confirmed with
`grep -n '^#' .shell-team/specs/T-1020-supersede-adjudication.md`), and the cited content
matches what the citations claim: section 3.4 is indeed where the two sweeps are named as
"different instruments pointing the same way" and section 8 is indeed where the remedy text
is noted as "presum[ing] a redundancy this corpus does not have" (read both sections in full
to confirm).

**Judgment on stripping the numeral prefix (D8-soundness):** sound. D8's own stated rationale
for citing by heading text rather than line number is that a line-number citation is "exactly
the citation a later rework invalidates silently" — a numbered section prefix (`3.4`, `8.`) is
subject to the identical hazard if a future rework inserts or removes a numbered section
before it, while the heading's descriptive title is stable under that kind of edit. Stripping
the numeral extends D8's own rationale one step further and does not change which two sections
are pointed at. This matches the reasoning already disclosed in
`.shell-team/provenance/T-1023.md`'s second decision, independently verified rather than taken
on the producer's word.

### The live warning render (dogfood)

Ran `bash bin/gen-playbook-blocks.sh --root <scratch> --lessons .shell-team/lessons.md` against
a scratch copy of every registered consumer + block file, outside the checkout
(`git archive HEAD`-independent `cp`-built scratch tree, exact CI dogfood-step shell logic run
via `bash` — not zsh's default word-splitting, which silently breaks the unquoted `for f in
$files` loop):

- Two warnings fired, on **stderr only** (stdout carries only `regenerated <path>` lines, no
  warning text): `playbook-engineer.md is 41 lines (threshold 40)` and `playbook-pm-spec.md is
  51 lines (threshold 40)` — both **re-measured directly**, matching the spec's Input space
  claim exactly (41 / 51).
- Both warnings carry the new tail verbatim: `— do not launch a supersede sweep on this signal
  alone; the judgment inputs are recorded in the shell-team repository at
  docs/loop-engineering/playbook-block-size-deferral.md`.
- All 4 canonical block files (`templates/prompt-blocks/playbook-{engineer,pm-spec,
  qa-verifier,tech-lead}.md`) and all 4 registered consumers
  (`agents/{engineer,pm-spec,qa-verifier,tech-lead}.md`) regenerated **byte-identical**
  (`cmp -s`) to the committed files — no drift, and no warning text spliced into any generated
  block (confirmed by inspecting the block files directly: the warning is stderr-only and
  never enters the block's own content).

### Full CI-wired suite list (AC16 discharge)

Ran all 59 suite/dogfood steps from `.github/workflows/check-handoff.yml`, **in the workflow's
own order**, from a clean shell (checkout + shellcheck-install steps excluded as
environment-only) — **0 failed**. `shellcheck` run at the locally-installed **0.11.0**, matching
the workflow's pinned `SHELLCHECK_VERSION`, clean on the full CI arg list (including
`bin/gen-playbook-blocks.sh` and `tests/gen-playbook-blocks/run.sh`). The dogfood
`gen-playbook-blocks` regeneration step (byte-identical to committed files) is the same
measurement reported above under "the live warning render", re-run inside the ordered list
rather than only ad hoc.

### Mutation self-checks — 2-of-3 independently reproduced

Reproduced on a scratch copy outside the checkout (`git archive HEAD` extraction under
`$TMPDIR`), red -> restore -> green each time:

1. **Line-467 revert.** Reverted `bin/gen-playbook-blocks.sh` line 467 to the base ref's
   original tail (`— consider superseding stale entries`). Confirmed **both owning suites
   (`tests/errexit-safe/run.sh`, `tests/gen-playbook-blocks/run.sh`) stay green under this
   mutation** — matching D1's own claim that neither suite is sensitive to the tail text (the
   pin is on line 468, the message-head match only checks the head). The mutation is instead
   caught by **AC9's own equality lock**: reproduced AC9's `check:` clause against the mutated
   file — red (the frozen full-line literal no longer matches); restored line 467 to committed
   content — green again.
2. **Canonical-line deletion.** Deleted `claim-1` entirely from
   `docs/loop-engineering/playbook-block-size-deferral.md`. Reproduced AC2's `check:` clause
   against the mutated doc — red (`grep -c '^- claim-1: '` = 0); restored the doc byte-for-byte
   (`diff` confirmed identical to the pre-mutation backup) — green again.

**2-of-3 disclosed** — the spec's own hand-off/prompt named these two explicitly; a third
class (e.g. a shift in AC10's no-shift lock) was not independently reproduced this round.

### Vocabulary discipline (D5/AC8)

AC8 measured green live (diff-scoped negative check + `pre-publication` positive control).
Additionally confirmed by eye: this review record and its board sub-bullet (below) use
"pre-publication" throughout and never the bare abbreviated repository name — same discipline
D5 requires of every role landing content in this diff.

### Regressions / adjacent code paths

`bin/gen-playbook-blocks.sh`'s diff is a single physical line (467); `git diff --stat` shows
`1 changed, 1 insertion(+), 1 deletion(-)` for that file — no other line touched, confirmed by
AC10's byte-identical-except-467 lock (PASS) and by the dogfood regeneration step above
(all 4 generated blocks + 4 consumers byte-identical, so no downstream drift in any consumer).
No other `bin/` file, test file, `.github/**`, skill, agent, template, or adopter-facing
document changed (AC12/AC13 PASS).

### Edge cases tried

- The `for f in $files` unquoted word-splitting loop in the workflow's own
  `gen-playbook-blocks` dogfood step depends on running under `bash`, not the session's default
  `zsh` (which does not word-split unquoted expansions by default) — first attempt at the
  dogfood step silently misbehaved (`cp` received a multi-line string as one argument) until
  re-run explicitly through `bash`. Not a defect in the shipped step (CI always runs it under
  `bash`), but worth flagging: this is exactly the "test-harness short-circuit" hazard the
  playbook warns about, caught here by re-running with the real interpreter.
- Both mutation self-checks above are themselves adversarial fixtures crossing the defended
  boundary (a genuine tail-text reversion, a genuine canonical-line deletion), not near-miss
  variants that "just barely" fail.
- This task's deliverable is prose (a decision record) plus a one-line message-string edit with
  no parser, checker or attacker-influenced input surface (Input space explicitly rules out
  role-name/percent injection, em-dash rendering, threshold values, and adversarial near-miss
  forms of the canonical lines as out-of-scope synthetic extremes) — the adversarial fixture
  synthesis checklist's classes 1–3 (payload-shape/regex-anchoring/CommonMark-parsing
  adversarial input) have no applicable execution surface here beyond the equality-lock greps
  already exercised by AC2–AC9 and the two mutation self-checks above; class 4
  (test-harness short-circuit) is exercised directly by both the dogfood run and the full
  CI-wired list, run by their real invocation forms.

### Risk notes for reviewer

- Only 2 of the engineer's disclosed 3 mutation self-checks were independently reproduced this
  round (the third, unspecified class, was not identified from the hand-off text available on
  the board and was not independently constructed) — disclosed, not faked.
- AC10's absolute one-line equality lock and AC9's frozen-literal equality lock are the two
  places doing real work in this task (per the spec's own task-classification note); both
  measured green live and independently re-derived (not taken on the producer's report).
- No `out-of-input-space` exemption was needed — no candidate finding this round depended on
  an out-of-scope synthetic extreme.

---

## Codex round 1 review (2026-08-04)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1023
- Codex model: gpt-5.6-sol
- Verification mode: executed-tests
- Diff scope: 7 files changed, +506/-1 lines (`bin/gen-playbook-blocks.sh`, `docs/loop-engineering/playbook-block-size-deferral.md`, `.shell-team/todo.md`, `.shell-team/specs/T-1023-block-size-deferral-record.md`, `.shell-team/provenance/T-1023.md`, `.shell-team/interventions/T-1023.md`, `.shell-team/reviews/T-1023.md`)

Codex CLI（`gpt-5.6-sol`）を `codex exec review --base 7a64def --json` で実リポジトリに対して直接走らせた一次パスと、Major が出たため追加で走らせた devil's-advocate 形式の二次パス（`codex exec --json`、対象ファイルを自分で開かせる形）の結果を、自分自身で `.shell-team/specs/T-1020-supersede-adjudication.md` の該当節と `.shell-team/lessons.md` の実バイトを読み直して裏取りした上でまとめる。

機械ゲートは自分で再実行して確認済み: `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1023-block-size-deferral-record.md` → **14 passed, 0 failed, 2 skipped**（QA の報告と一致）。line 467 の diff は tail のみの置換で single-quoted format string 内、apostrophe/backtick/新規 `%` は無し、line 468 は base ref とバイト一致、`tests/errexit-safe/run.sh:313` のピン（`gen-playbook-blocks.sh:468:        "$role" "$line_count" "$LINE_WARN_THRESHOLD" >&2`）は `sed -n '468p' bin/gen-playbook-blocks.sh` の実バイトと一致 — AC9/AC10 の主張を自分で再deriveして確認、コード面に欠陥は見つからなかった（Codex 一次パスもこの1行には何も指摘していない）。D8 の見出し引用（numeral prefix 除去）も `.shell-team/specs/T-1020-supersede-adjudication.md` の実見出し（`### 3.4 What this accounting does and does not establish` / `## 8. Observations recorded, not acted on`）と実際に一致することを自分で確認し、QA の D8-sound 判定に独立に同意する。`bin/install` を自分で読み、`docs/` はインストール対象に含まれない（コピーされるのは `agents/*.md` のみ）ことを確認した上で、Input space の「adopter checkout に本ファイルが無い」宣言を out-of-scope とする判断は妥当と判断する（このパスは `bin/gen-playbook-blocks.sh` をリポジトリ本体を持つ側だけが実行するものであり、その側には `docs/loop-engineering/` が定義上存在する）。

一方で、レビューの主眼である claim fidelity（D3, review focus #1）に、機械ゲートを通過している実質的な欠陥を2件見つけた。両方を devil's-advocate 二次パスにかけ、二次パスも独立に "REAL DEFECT" と判定した。

#### Blockers
なし。

#### Major
- **`docs/loop-engineering/playbook-block-size-deferral.md:45`（claim-3、`.shell-team/specs/T-1023-block-size-deferral-record.md` の AC4 が凍結する frozen literal そのもの）— T-1020 の測定手法の記述が不正確。** claim-3 は「T-1020 adjudication ... scored 24 candidates against a stricter three-condition predicate and found exactly one ratifiable pair」と書くが、`T-1020-supersede-adjudication.md` を自分で読み直すと、24個の numbered block のうち実際に (i)/(ii)/(iii) の三条件で scored されたのは **22件（C-01〜C-22）のみ**であり、残り2件（C-23, C-24）は C1 pre-filter によって「(i) not evaluated — C1 pre-filter」「(ii) not evaluated」「(iii) not evaluated」と明記された上で **評価前に候補から除外**されている（§3.1 step 3、§3.2、C-23/C-24 の各エントリ本文）。さらに T-1020 §7 自身が「Of 24 numbered candidates ... — **22 pairs adjudicated against all three predicate conditions, plus 2 excluded by the C1 pre-filter before any successor was considered**」と、まさにこの区別を明示的に書き分けている。claim-3 はこの区別を消して "24 candidates ... scored against ... predicate" と一体化しており、測定の厳密さを実際より広く見せている。
  - **これは T-1020 自身が一度踏んだ轍の再発である**: T-1020 の Codex round 1 レビュー（`.shell-team/reviews/T-1020.md` 215行目）は当時ほぼ同じ表現（"24 adjudicated candidates" が pair 判定と C1 除外を混同）を **nit** として扱い降格したが、それは T-1020 の spec 本文では該当箇所の直後に各エントリ自身の見出し（"excluded from candidacy by C1"）が並んでおり、読者がその場で区別を復元できたためだった。T-1023 の ported record にはその disambiguating な文脈が無く、claim-3 は D3 が明言するとおり「a future reader and a future checker are entitled to trust」単独引用可能な canonical line として設計されている。文脈による救済が失われた状態で同じ不正確な言い回しを再導入したことは、nit ではなく major に格上げされる十分な理由になる。
  - devil's-advocate 二次パスも独立に "REAL DEFECT" と判定（"'scored 24 … against [the] three-condition predicate' describes a specific operation that the source expressly says happened only 22 times... T-1020 had already adopted the precise 22-plus-2 wording to avoid exactly this flattening"）。
  - **注意（environmentally-unverified）**: claim-3 の前半（pre-publication repository の 74-entry / zero-domination 測定）は issue #112 からの引用（`pm-spec-taken-from-source`）であり、spec の Assumptions 自身が「本リポジトリでは再測定不可能」と明記している。この前半部分について internal consistency verified / issue-canon match environmentally-unverified（relies on orchestrator's primary confirmation）— 本 Major は claim-3 の**後半**（T-1020 由来、repo-reachable かつ実際に検証済み）に対するものであり、この caveat の対象ではない。
  - 修正には claim-3 の frozen literal 自体の書き換えが必要で、これは `.shell-team/specs/T-1023-block-size-deferral-record.md` の intent-block 内（AC4）にあるため、**単純なドキュメント修正では済まず spec の v2 re-freeze が必要**。推奨する修正文言（例）: 「the T-1020 adjudication of 2026-08-03 in this repository scored 22 candidates against a stricter three-condition predicate and excluded 2 further candidates from scoring under a machine pre-filter, and found exactly one ratifiable pair among the 22, which is Scope: maintainer and therefore has zero block-line impact.」— カウントと結論は変えず、22/2の区別だけを回復する。

- **`docs/loop-engineering/playbook-block-size-deferral.md:12-13`（`## Why this document exists`、AC-lock の対象外の周辺 prose）— 文書の冒頭が自己矛盾している。** 「that remedy has now been checked twice ... and both times found nothing to supersede」と書くが、T-1020 は実際に C-01（`2026-07-25 — Editing a file-line-pinned bin/ script needs an explicit check that cross-suite registries still match`）を ratify して retire しており（`.shell-team/lessons.md` 724–730行目で `Superseded-by` が実際に付与済み）、これは同じ文書内の claim-3 が正確に述べる「found exactly one ratifiable pair」と直接矛盾する。「found nothing to supersede」は文字どおり偽であり、正しくは「found nothing that would reduce the generated-block overage（or 相当の限定）」であるべき。
  - D3 の設計意図（「Paraphrase decay is the specific hazard... leaving every word of the doc individually defensible」）そのものに触れる欠陥であり、AC15（QA のプローズ読み込み項目）はこの具体的な劣化形を検出しなかった — AC15 が名指しで確認した3つの劣化形（smallness / schedule / basis-for-40）のいずれにも該当しないため、grep も QA の読みも素通りした。
  - devil's-advocate 二次パスも独立に "REAL DEFECT" と判定（"'Found nothing to supersede' is literally false and conflicts with the document's own accurate 'exactly one ratifiable pair' account"）。
  - この行は AC1 の等価ロック対象外の自由記述 prose であり、**spec の re-freeze なしで直接修正可能**（例: 「and both times found no retirement stock that would reduce the generated-block overage」）。

#### Minor / nits
- **Minor** — `.shell-team/todo.md`（T-1023 エントリ、"Predicted pre-implementation shape" の行）: ここでは "PASS AC11, AC12 (2); FAIL AC1–AC10, AC13, AC14 (12)" と書かれているが、凍結済みの spec 本体（Assumptions 節、"Freeze-run corrections, 2026-08-04" 以降）は同じ pre-implementation baseline を **"PASS AC8, AC11 (2)"** に訂正済みで、AC12 はむしろ pre-implementation **FAIL**（its own positive control requires exactly one changed `bin/` file）だったと明記している。board のこの行は訂正前の古い予測をそのまま残しており、同じ文書内の凍結済み訂正と食い違う。実装物・記録された `check-acs.sh` の実測結果（14/14, 2P/12F の attestation）自体には影響しないため、Major には格上げしない。

#### Adversarial findings
`codex exec --sandbox read-only`（`gpt-5.6-sol`、ファイル自身を読ませる形）に上記2件の Major を devil's-advocate として当てた結果:
- Finding A（claim-3 の22+2混同）について: 最も強い反論は「24 numbered block 全部に disposition が付いている以上 "scored 24 candidates" は妥当な要約だ」という立場だが、二次パスはこれを「'dispositioned 24 numbered candidates' would be defensible; 'scored 24 … against [the] three-condition predicate' describes a specific operation that the source expressly says happened only 22 times」として却下し、**REAL DEFECT** と結論した。
- Finding B（「found nothing to supersede」の自己矛盾）について: 最も強い反論は「文脈上 'nothing with block-size impact to supersede' という意味だと補って読める」という立場だが、二次パスは「the sentence says 'nothing to supersede,' not 'nothing with generated-block impact to supersede'... 'nothing' and 'exactly one' are contradictory counts until the reader supplies an unstated block-impact qualification」として却下し、**REAL DEFECT** と結論した。
- 両判定とも「基礎となる deferral の決定（第三次 sweep をしない）自体は揺らがない」ことを明記しており、これは自分の判断とも一致する。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | claim-3 が T-1020 の22件scored＋2件C1除外を「24件scored」と一体化している | primary: major (`[P2]`) / adversarial: REAL DEFECT | major（変更なし） | T-1020 本文（§3.1/§3.2/§7、C-23/C-24 の各エントリ）を自分で読み直して独立に確認。T-1020自身が一度 nit として片付けた同種の不正確さが、disambiguating context を失った canonical line として再発しているため、nit ではなく major に留める（格上げの根拠を明記） |
| 2 | 「found nothing to supersede」が同一文書内 claim-3 の「exactly one ratifiable pair」と矛盾する | primary: major (`[P2]`) / adversarial: REAL DEFECT | major（変更なし） | `.shell-team/lessons.md` の C-01 retiree エントリに実際に `Superseded-by` が付与されていることを自分で確認し、文字どおり偽であると判断。AC15 が名指しする3つの劣化形のいずれにも該当しないため機械にも QA 読みにも捕まらなかったことを併記 |
| 3 | board の pre-implementation baseline 予測が凍結済み spec 本体の訂正後の値と食い違う | primary: major (`[P2]`) | minor（降格） | 正規化表では P2→major が既定だが、これは配布された record／code の内容そのものではなく board 上のブックキーピング上の食い違いであり、`check-acs.sh` の実測結果（14/14）や AC4 等の判定内容には影響しない。同ラウンドで直せば十分と判断し、自分の裁量で minor に降格した |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_REVIEW` から `READY_FOR_ENG` へ差し戻し、上記 Major 2件（claim-3 の 22+2 表現の回復、"found nothing to supersede" の自己矛盾の解消）を対応させる。claim-3 の修正は intent-block 内の AC4 frozen literal を書き換えることになるため、spec の **v2 re-freeze が必要**（本タスクの interventions 記録により、orchestrator は自身の権限で re-freeze してよい）。「found nothing to supersede」の修正は AC1 の等価ロック対象外の自由記述であり、re-freeze なしで直接修正可能。同じラウンドで Minor 1件（board の pre-implementation baseline 行の訂正）も併せて直すことを推奨する（同じエントリを触るため追加コストはごく小さい）。
- 一次・二次パスとも、修正後に claim-3・「found nothing to supersede」以外の canonical eleven lines、signal 4件、`bin/gen-playbook-blocks.sh` の1行編集、D8 の見出し引用には欠陥を見つけていない。今回の差し戻しは claim-3 の測定手法記述と冒頭 prose の自己矛盾という、claim fidelity 領域の2点に限定される。

#### Fast-follow disposition
no fast-follow deferrals

---

## QA round 2 (2026-08-04)

### QA verdict: PASS

**Scope**: independent re-verification of the round-1 rework (`79023d4`) responding to Codex round
1's `REQUEST_CHANGES`, against the v2-frozen spec (`.shell-team/specs/T-1023-block-size-deferral-record.md`,
intent-hash `4434f8892603db06739d7902c525c566125dda02`), `READY_FOR_QA` -> `READY_FOR_REVIEW`.
Diff verified: `a424c94..79023d4` (spec v2 + doc rework + provenance + board).

### Mandatory conformance gates

- `bash bin/check-intent.sh .shell-team/specs/T-1023-block-size-deferral-record.md .shell-team/todo.md`
  -> `aligned: T-1023 v2 (4434f8892603db06739d7902c525c566125dda02)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1023.md` -> `conformant` (3 decision
  entries — the round-1 rework's D8 entry from QA round 1 plus one new entry for the three
  original-wording prose fixes, both read and judged sound).
- `bash bin/check-interventions.sh --task T-1023 .shell-team/interventions/T-1023.md` -> `conformant`
  (3 entries, 0 sentinel — unchanged since round 1, no new human-correction this round; judged
  structurally only).
- `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1023-block-size-deferral-record.md`
  -> **14 passed, 0 failed, 2 skipped** — every AC re-derived live against v2, not taken on the
  producer's report.

### 1. Both Codex round-1 Majors closed — independently re-verified against the primary records

**(a) claim-3's 22-scored/2-pre-filter-excluded/24-total accounting.** Read
`.shell-team/specs/T-1020-supersede-adjudication.md` §3.1 step 3, §3.2, C-23/C-24's full bodies,
and §7 myself (not taken on the producer's citation). §7's own sentence is byte-for-byte the
source of the fix: *"Of 24 numbered candidates over the whole corpus — 22 pairs adjudicated
against all three predicate conditions, plus 2 excluded by the C1 pre-filter before any successor
was considered — exactly one satisfies the predicate."* §3.1 step 3 confirms the pre-filter pass
found exactly two entries (C-23, C-24); both entries' own bodies literally read `(i) not evaluated
— C1 pre-filter`, `(ii) not evaluated — C1 pre-filter`, `(iii) not evaluated — C1 pre-filter`. The
doc's rewritten claim-3 ("scored 22 of its 24 numbered candidates against a stricter
three-condition predicate — the other 2 were excluded from scoring by a machine pre-filter before
any successor was considered — and found exactly one ratifiable pair among the 22") matches this
source exactly, with no residual flattening. AC4's frozen literal is byte-identical to the doc's
live line (confirmed by AC4's own `check:` PASS).

**(b) the opening prose no longer contradicts claim-3.** `## Why this document exists` now reads
"neither time found retirement stock that would shrink a generated block" — checked against
`.shell-team/lessons.md` directly: the C-01 retiree
(`2026-07-25 — Editing a file-line-pinned bin/ script needs an explicit check that cross-suite
registries still match`, lines 724–730) carries `Status: superseded` and
`Superseded-by: 2026-08-01 — A bin/ edit's completion checklist runs the full CI-wired suite list,
not a self-selected subset` — the retirement is real and live in the corpus. The retiree's own
`Scope: maintainer` field (confirmed by reading the same lines) is why it never entered a
generated block, which is exactly the qualification the new sentence states. Accurate.

### 2. The bulk pass's three additional corrections — each verified against its cited source

- **claim-1's attribution.** New line: harm "unmeasured and unobserved at the pre-publication
  re-evaluation," and the T-1020 side "measured nothing about it either." Grepped
  `.shell-team/specs/T-1020-supersede-adjudication.md` for `dilution` myself: **zero hits** — the
  T-1020 adjudication never measures or discusses dilution harm at all, confirming the new
  attribution is an absence, not a second assertion, exactly as claimed.
- **claim-2's dropped headcount.** "a judgment call" replaces "a judgment call by one engineer."
  The removed headcount cannot be independently re-verified against issue #112 in this sandbox
  (no GitHub access) — **environmentally-unverified**, same class as the spec's own
  `pm-spec-taken-from-source` caveat on the pre-publication numbers. The fix itself is safe
  regardless of the exact source wording: dropping an unsourced count from a frozen literal cannot
  introduce a new inaccuracy, only remove a risked one.
- **signal-3's rewording.** "the roughly 80 active entries the 2026-08-03 adjudication covered"
  replaces "measured here." T-1020's own §3.2 disposition table states the population directly:
  "83 entries — 80 `active`, 3 `superseded`" at the base ref, with the adjudication's own accounting
  running over exactly those 80 active entries. Matches.

### 3. AC4's new decay-class negative grep — load-bearing, with one residual

Reproduced the engineer's own disclosed mutation on a scratch copy outside the checkout
(`git archive HEAD`-derived tree, refreshed to the current tip): appended
`found nothing to supersede` (one of the four frozen forbidden shapes) — AC4's negative grep fired,
red (rc=1); restored byte-identical — green again.

Then tried a phrase of my own choosing, deliberately not matching any of the four literal shapes
(`nothing to supersede`, `found nothing`, `came out empty`, `no retirement stock at all`): appended
*"neither sweep turned up anything worth retiring, in the end."* — **this slipped past the grep**
(rc=0, unexpected green). Restored byte-identical.

**Judgment: residual, not a FAIL.** This is a genuine gap in AC4's mechanical coverage — the
negative grep is a fixed list of four literal shapes, and English has more than four ways to
flatten "one retirement, zero block impact" into "nothing." But the residual's severity is bounded
by two things measured directly: (a) the shipped document does **not** contain this or any other
unflagged flattening phrase — confirmed by my own full AC15 re-read below, which covers exactly the
prose a grep cannot see; (b) this is the same division of labor D3/AC15 already state explicitly —
"AC15 is the prose-reading item ... which is what covers decay in the surrounding prose that no
grep can see." A mechanical lock with a bounded literal set was never going to be complete against
free-form paraphrase; the reading pass is the backstop, and it holds. Recorded as a residual
finding for the reviewer, not escalated to FAIL.

### 4. AC15 full re-read (with round-1's failure class specifically in mind)

Read the entire document end to end again, this time weighted toward the exact decay class Codex
round 1 caught (count-flattening / self-contradiction), not only the three v1-named shapes
(smallness / schedule / basis-for-40):

- **The three rewritten prose sites** — `## Why this document exists`, `## Provenance of the two
  measurements` bullet 2, `## What this record is not` bullet 3 — are now mutually consistent and
  consistent with claim-3: each states "found retirement stock that would shrink a generated block"
  (the opening and the third `## What this record is not` bullet) or the full accounting (the
  provenance bullet), never a bare "nothing"/"empty" claim.
- **`## Provenance of the two measurements` bullet 2's claim-site accounting**, specifically named
  in this round's brief: carries population total (24 numbered candidates), method (22 scored
  against the three-condition predicate), and exclusion reason (2 excluded by a machine pre-filter
  before any successor was considered) — satisfying the corpus lesson `2026-08-03 — A completeness
  claim attaches its accounting` at the claim site, as the spec's Notes require.
- Grepped the whole document myself for every remaining occurrence of `found`/`nothing`/`empty`/
  `no retirement` (nine total): each reads correctly qualified on inspection — none reintroduces the
  flattening in different words (full listing checked: lines 13, 23, 30, 45, 47, 67, 79, 82, 86 of
  the current document).
- Re-confirmed the three v1-named decay shapes still hold under the new wording: no line converts
  "unmeasured and unobserved" into smallness (claim-1's rewrite states an absence of measurement on
  the T-1020 side, not a measured-small finding); no line converts the signal-driven trigger into a
  plan (unchanged, `## What this record is not` bullet 2 untouched); no line presents 40 as having a
  basis (claim-2's rewrite drops a headcount, not the "no technical basis" claim itself).
- No line-number citation into any spec anywhere (re-confirmed: `grep -nE '\.md:[0-9]+|line
  [0-9]+|Line [0-9]+'` over the document returns nothing, unchanged from round 1).

No further decay found.

### 5. AC16 — full 59-step CI-wired list: SKIPPED this round, decision stated explicitly

**Decision: skip.** Reasoning, not a silent reuse of anyone else's run:

- `git diff --name-only bb85bd7..79023d4 -- bin tests .github` returns **empty** — zero files
  under `bin/`, `tests/`, or `.github/**` have changed since **my own** full 59-step run in QA
  round 1 (commit `bb85bd7`, 0 failures, workflow order, shellcheck 0.11.0 clean). This round's
  entire diff (`a424c94..79023d4`) is confined to `.shell-team/specs/T-1023-*.md`,
  `docs/loop-engineering/playbook-block-size-deferral.md`, `.shell-team/provenance/T-1023.md` and
  `.shell-team/todo.md` — prose and markdown only.
- None of the 59 CI-wired steps parse, grep, or execute
  `docs/loop-engineering/playbook-block-size-deferral.md`'s content. The only consumer of that
  file's prose is `check-acs.sh`'s own AC1–AC9 grep-based checks (re-run live this round, all
  PASS) and my own AC15 reading pass (done above) — neither is a CI-wired step.
- Re-ran, individually, the pieces of the full list that *do* have a plausible input surface here:
  both owning suites standalone (`tests/errexit-safe/run.sh`, `tests/gen-playbook-blocks/run.sh`,
  both green via AC11's live PASS), `shellcheck` on `bin/gen-playbook-blocks.sh` +
  `tests/gen-playbook-blocks/run.sh` + `tests/errexit-safe/run.sh` directly (clean, 0.11.0), and
  AC8's diff-scoped vocabulary check (live PASS).
- This is **not** the engineer's round-1 full-list run standing in — the engineer this round
  explicitly ran only the two owning suites plus AC8 and flagged the decision to me; the full-list
  baseline I am relying on is **my own** round-1 run at `bb85bd7`, re-validated as still current by
  the empty `bin`/`tests`/`.github` diff above.

Given a genuinely empty diff over every directory the 59-step list exercises, a full re-run would
re-execute the identical suites against identical `bin/`/`tests/` bytes already measured clean —
stated explicitly rather than silently assumed.

### AC8 vocabulary discipline — in my own additions

This QA round-2 section and the board sub-bullet below use "pre-publication" and never the bare
abbreviated repository name — confirmed by eye and re-confirmed with `grep -nw` after writing.

### Regressions / adjacent code paths

`bin/gen-playbook-blocks.sh` is untouched this round (confirmed: zero diff `bb85bd7..79023d4` on
`bin/`); AC9/AC10 re-measured live, PASS. `.shell-team/provenance/T-1023.md` gained exactly one new
decision/reason/grounding triple (the three original-wording prose fixes), read and judged sound —
grounded correctly in the spec's own Notes-for-engineer itemization and AC4's check line.

### Edge cases tried

- The residual paraphrase in item 3 above (a genuinely adversarial fixture crossing AC4's
  boundary in a shape the frozen literal set doesn't anticipate) — reported as a residual, not a
  FAIL, per this round's explicit instruction and consistent with the `out-of-input-space`-adjacent
  reasoning that a mechanical lock's bounded coverage is not itself a defect in the shipped
  document.
- Re-checked the full document once more for any *other* unflagged flattening phrase beyond the
  three the review named — none found (item 4 above).

### Risk notes for reviewer

- **Residual**: AC4's negative grep has a bounded literal set (four shapes) and a reasonable writer
  could construct a fifth flattening paraphrase that slips past it mechanically — demonstrated
  directly this round. Not merge-blocking: the shipped document does not contain such a phrase, and
  AC15's reading pass is the design's own stated backstop for exactly this decay class. Worth
  naming as a fast-follow candidate (extend AC4's literal set, or accept that this decay class is
  reading-pass-only by design) rather than reworking again this round.
- **AC16 was not re-run as a full 59-step list this round** — explicitly skipped, reasoning stated
  in item 5 above (empty `bin`/`tests`/`.github` diff since my own last full run). Flagged for the
  reviewer to weigh, per this round's own instruction not to let a skip pass silently.
- claim-2's headcount removal (`by one engineer`) is grounded in issue #112's wording, which is
  **environmentally-unverified** in this sandbox (no GitHub access) — the fix's safety does not
  depend on that verification (removing an unsourced count cannot introduce inaccuracy), but the
  sourcing claim itself is unconfirmed by me directly.
- No `out-of-input-space` exemption was needed for any AC-relevant finding this round.
