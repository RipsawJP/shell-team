### Codex Review verdict: REQUEST_CHANGES
- Task: T-1098
- Codex model: not reported in this round's JSONL event stream (codex-cli's configured default; the CLI does not print a model field in `codex exec --json` output)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only — read-only reads of the delivered diff, `tests/trial-recipe/run.sh`, `tests/trial-recipe/extract-recipe.sh`, the frozen spec, the board hand-off, the provenance record and `.github/workflows/check-handoff.yml`; no scratch-repository execution was performed by this role — QA's own full 12-case run (103/103 full-population two-arm diff, 17/17 ACs PASS) is read and relied on rather than repeated
- Diff scope: `git diff --stat cbf6890...HEAD` — 8 files, +1118/-2 (`tests/trial-recipe/extract-recipe.sh` new, 137 lines; `tests/trial-recipe/run.sh` new, 541 lines; `.github/workflows/check-handoff.yml` +5/-2; `.shell-team/specs/T-1098-trial-recipe-execution.md` new, 290 lines; `.shell-team/test-recipe.md` +82; `.shell-team/todo.md` +41/-… no, see stat; `.shell-team/provenance/T-1098.md` new, +21; `.shell-team/interventions/T-1098.md` new, +3)
- Commit range reviewed: `cbf6890..09d0365` (branch `feature/1098-trial-recipe-execution`, QA full-population PASS 103/103 → `READY_FOR_REVIEW`)
- Spec-review round: none — axis elected `none` with recorded ground (this spec's own `- spec-review:` hand-off), so this delivered-change round is the ONLY cross-provider check this task ever receives; scope and depth are calibrated accordingly (full mandatory-probe coverage below, no reliance on an earlier spec-review pass having already screened the acceptance criteria's own premises)
- Consumer class (per this spec's own two-tier `## Verdict scope`): **Tier 1** — the suite's own assertions (`tests/trial-recipe/run.sh`), the extractor (`tests/trial-recipe/extract-recipe.sh`) and the CI wiring — full strength, the CLAUDE.local.md dev-scaffold carve-out does **not** apply. **Tier 2** — this spec's own inline `- check:` lines — the 2026-08-22 dev-scaffold pricing (computed-value correctness, conclusion direction, real instrument defects; guard-mutation completeness is not a Major here).

#### Positive confirmation, stated before the findings
QA's own full-population, two-arm diff (base `cbf6890` vs HEAD, 103/103 specs, both arms complete) found exactly the flips this task's own `## Blast radius` table predicted, plus one already-disclosed collateral flip on a different, already-merged spec's brittle exact-delta assertion (T-1096 AC15) — none of it real (none touches `docs/adopting.md`, `docs/adopting.ja.md` or `bin/team-init.sh`/`bin/team-paths.sh` behavior). I re-derived the same structural facts independently: `git diff --stat cbf6890...HEAD` names exactly the eight files the board's own hand-off lists, no path under `bin/` and neither `docs/adopting.md` nor `docs/adopting.ja.md` appears anywhere in the diff (probe 6, confirmed clean). The CI wiring (probe 5) is correct at both sites: the single `shellcheck` argument list carries both new paths, and a new `- name: Run trial-recipe fixture suite` / `run: bash tests/trial-recipe/run.sh` step sits immediately after the pre-existing `Run team-init fixture suite` step, which the diff leaves untouched — no sibling suite perturbed. The suite's discharge of the three floor requirements (probe 2) is faithful, not just present: `legacy-marker`/`marker-vacuity-control` measure the resolved-path difference from the resolver's own outputs with a genuine vacuity control (a bare-directory fixture proven to resolve identically to the default layout); `hermetic-global`/`adverse-excludes` establish a feature-detected scratch global-config scope, prove the hostile plant has teeth via `git check-ignore`, and assert the plain `git add` refusal leaves the index byte-identical before/after (not just a non-zero exit); `both-paths-committed`/`specs-arg-control` assert both resolved paths tracked in both layouts with a legacy-only negative control that reproduces the exact regression (dropping the second `git add` argument) and confirms the specs path is scaffolded-but-untracked while the base path stays tracked. The flag path (probe 4) is compared to the manual path by actual end-state — tracked-path list, branch name, both resolved paths — not by a separate hand-written expectation, and both fixtures' tracked-path lists are asserted non-empty first so two empty lists can't agree vacuously.

My own hostile-mutant pass against the extractor (probe 1), independent of the four AC2 already ships, found no way to defeat the fail-closed contract inside this task's own declared Input space: a document that shrinks in total line count anywhere (not just inside the section) is caught by the whole-document floor, which the engineer's provenance record documents was added specifically because two of AC2's own mutants (`m2`, `m3`) mutate an unrelated fenced block earlier in the file and a section-scoped read alone cannot see that; the fence-count and per-block line floors prevent a silent zero-command extraction on any of the four probed shapes (unmatched heading, fence-count drift, short block, missing invocation-form span); and the flag-span's own duplication case (the bare token legitimately appearing twice) is correctly handled by scoping the "found exactly once" assertion to the invocation-form string, not the bare token. One genuine, but out-of-scope, edge case I did find: `grep -cF` counts matching **lines**, not occurrences, so a single line carrying the invocation-form string twice would still report `matches=1` and pass. This is squarely the kind of "document rewritten specifically to defeat the extractor" case `## Input space`'s own out-of-scope item 7 declines to guard against (fail-closed on the shapes real edits produce, not adversary-proof), so I am not raising it as a finding.

Probe 3's two named design decisions both hold up under scrutiny. The whole-document line-count floor creates no false-red hazard for legitimate growth (it is a floor, never a ceiling), and the one theoretical false-green gap I traced — a prose-only deletion inside the trial section, offset by growth elsewhere in the document, small enough to stay above both the whole-document floor and the section's own ≥10-line floor — would have to avoid touching any fence delimiter or command line to survive (both of which are separately floor-checked), so it could only ever drop narrative prose, never a command the recipe executes; I judge this a real but low-severity residual, not worth raising above a hardening note. AC11's git-level teardown claim is an honest rendering: the engineer's provenance record traces, by direct measurement, why a raw `[ ! -e ]` filesystem check would fail on every run regardless of correctness (the shipped `runs/` gitignore rule means `<base>/runs/.gitkeep` is never tracked by the documented `git add` line in the first place, so a branch switch cannot remove it), why `git check-ignore` cannot attribute the survivor either (the `.gitignore` that would explain it is itself removed by the same switch), and lands on the assertion that actually matches the doc's own claim ("destroys every commit reachable only from it") — no tracked content survives, and any filesystem survivor is confined by path to `runs/*`. I read this as good-faith, measured engineering rather than a rationalization of a shortcut.

#### Blockers
(none)

#### Major

1. **The adverse-excludes case's `git add -f` remedy is hand-reconstructed, not extracted from the shipped document — a regression that silently weakens or drops the documented remedy would not be caught.** (`tests/trial-recipe/run.sh:395-397`) `ADV_ADD_F_LINE="git add -f \"$ADV_BASE\" \"$ADV_SPECS\""` is built entirely from the suite's own resolved-path variables; it never reads the remedy sentence out of `docs/adopting.md` the way the setup/teardown fences and the flag invocation are extracted. If a future edit to the shipped remedy sentence dropped `-f`, dropped the second (specs) argument, or otherwise weakened it, this case would keep constructing and running the *original, correct* form and keep passing — silently defeating exactly the class of regression this task exists to lock down (the primary record's own round-2 Major 2 is precisely "a regression that silently drops the second `git add` argument would still pass"). This is new code under this task's own Tier-1 full-strength scope (the suite's adverse-excludes case), not a re-litigation of T-1094's frozen static AC10, and it is independently confirmed by a second, adversarial Codex pass run specifically against this file (see `.shell-team/reviews/T-1098-codex-adversarial.txt`). It is fixable entirely within this task's own declared surface (`tests/trial-recipe/extract-recipe.sh` + `tests/trial-recipe/run.sh`) without touching the frozen spec text: the same pattern already used for the flag invocation (extracting a specific prose-embedded span rather than a fenced block) generalizes to the remedy sentence.
   - Suggestion: add a fourth extractor mode (e.g. `remedy`) that locates and returns the `git add -f "$(team-paths.sh --get base)" "$(team-paths.sh --get specs)"` span from the section's own prose, fail-closed on the same "found exactly once" contract the flag mode already uses; substitute and execute that extracted line in the adverse-excludes case instead of `ADV_ADD_F_LINE`.

2. **The spec's own `## Summarized sources` cites a machine-local, session-scoped absolute path, violating this repository's own public-repo hygiene rule.** (`.shell-team/specs/T-1098-trial-recipe-execution.md:26`) The citation reads `/private/tmp/claude-502/-Users-sawamatsu-workspace--my-repos-shell-team/048d116a-ac54-425f-8242-4def526c49d9/scratchpad/t1098-routing-map.md` — a local username-derived directory name plus a session UUID, committed verbatim into a permanent, public-repository artifact. Every comparable predecessor task (T-1088, T-1087, T-1092, T-1094, T-1097) cites its routing map at the established, repo-relative, gitignored-but-tracked-path convention `.shell-team/runs/tNNNN-routing-map.md` instead — confirmed by grep across the spec corpus. This is exactly the class of content `CLAUDE.md`'s Hygiene section names ("Do not add private context... session transcripts... audit output carrying real values") and `CLAUDE.local.md`'s PII-handling rule treats as a hard invariant for this public repository, not a style preference. It carries no functional consequence for the recipe or the suite, but it is a cheap, in-scope fix (the citation sentence, not the frozen intent block's own Goal/Non-goals/AC/Input-space text) and should not ship as written.
   - Suggestion: replace the citation with the established convention (`.shell-team/runs/t1098-routing-map.md`, disclosed as gitignored/per-run) or a generic description ("the coordinating session's own Routing Map for this task, a per-run artifact"), matching how every sibling task in this corpus already cites its own routing map.

#### Minor / nits

1. **`run_lines_in_repo()` evals extracted `docs/adopting.md` prose without an allow-list, but this is not a live CI compromise as shipped.** (`tests/trial-recipe/run.sh:85-90`) A primary-pass Codex read flagged this as a P1 supply-chain concern (arbitrary command execution from mutable documentation). A dedicated adversarial pass, run specifically against this concern, confirmed the mechanism is real but downgraded the severity: the workflow triggers on `pull_request` (not `pull_request_target`) with no explicit elevated `permissions:` block, so any edit to `docs/adopting.md` capable of tripping this is itself part of the same reviewed PR diff that would carry the change — no different in kind from any other repo-tracked test fixture being executed by CI. The design is also the explicit, frozen point of extraction-over-transcription (`## Goal`: "extracted from the shipped file, never retyped"). I concur with the adversarial pass's downgrade to Minor: a hardening improvement worth doing, not a merge-blocking security defect.
   - Suggestion: at the author's discretion — parse/allow-list the expected command verbs before `eval`, or accept as intentional test-code behavior given the PR-review gate already covers the input.
2. **`run_lines_in_repo()` does not clear `TEAM_RUN_BASE`/`GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` before eval'ing extracted lines, unlike the `tp()`/`ti()` wrappers' `env -u TEAM_RUN_BASE`.** (`tests/trial-recipe/run.sh:85-90`) Real as an inconsistency with the suite's own established hermeticity rigor (it isolates global git config carefully via a feature-detected scratch scope, but not these four process-environment variables for this one execution path) — but a second adversarial Codex pass, run specifically against this concern, confirmed none of the four variables is set anywhere in the checked-in `.github/workflows/check-handoff.yml`, so it is not reachable in the shipped CI path as configured; it would require a non-standard runner or a modified workflow. I downgrade this from the primary pass's Blocker-equivalent framing to Minor: a real, cheap hardening gap, not a currently-exploitable one.
   - Suggestion: wrap `run_lines_in_repo()`'s `eval` in the same `env -u TEAM_RUN_BASE -u GIT_DIR -u GIT_WORK_TREE -u GIT_INDEX_FILE` pattern the `tp()`/`ti()` wrappers already use, for defense in depth against a future non-default runner.

#### Adversarial findings
A dedicated adversarial pass (`.shell-team/reviews/T-1098-codex-adversarial.txt`), triggered by the primary pass's two P1 findings on `tests/trial-recipe/run.sh`, was asked to break or confirm those two findings with concrete reasoning grounded in the actual CI configuration, and to independently hunt for a third defect. Result: both P1s were downgraded (see Minor 1 and 2 above, with the reasoning traced to `.github/workflows/check-handoff.yml`'s actual trigger and permissions), and the pass independently reconfirmed the remedy-reconstruction gap (Major 1 above) without being told about it by name in advance — it was given both prior findings to evaluate and one open-ended instruction to find a third defect, and it converged on the same remedy-line gap my own reading had already flagged.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `eval` of extracted, mutable `docs/adopting.md` text in `run_lines_in_repo()` | primary: blocker (P1) → adversarial: minor | minor | the workflow triggers on `pull_request` (not `pull_request_target`) with no elevated permissions declared, so any exploit requires the same PR-diff review gate every other change goes through; this is also the frozen spec's own explicit design (extraction over transcription), not an engineer-introduced gap |
| 2 | `run_lines_in_repo()` doesn't clear `TEAM_RUN_BASE`/`GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE` before `eval` | primary: blocker (P1) → adversarial: nit | minor | confirmed unreachable in the checked-in CI config (none of the four variables is set anywhere in `.github/workflows/check-handoff.yml`); kept above nit rather than at nit because it is a real inconsistency with the suite's own established hermeticity rigor elsewhere, worth a cheap hardening fix |
| 3 | adverse-excludes remedy line hand-reconstructed rather than extracted from the shipped doc | primary: major (P2) → adversarial: major, independently reconfirmed | major, unchanged | two independent Codex passes and my own reading agree: a regression weakening the shipped remedy sentence would not be caught, which is structurally the same silent-defeat class this task's own primary record (round 2 Major 2) already names once |
| 4 | machine-local scratchpad path committed in `## Summarized sources` | primary: major (P2) | major, unchanged | not covered by the adversarial pass (out of that pass's scope); kept at the primary read's severity given this repository's own explicit, repeatedly-stated public-repo hygiene invariant and the established sibling-task citation convention this spec departs from |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1098 entry back to `READY_FOR_ENG` with the two Majors above linked. Both are fixable entirely within this task's own declared surface (`tests/trial-recipe/extract-recipe.sh`, `tests/trial-recipe/run.sh` for Major 1; the spec's own `## Summarized sources` citation sentence, outside the frozen intent block, for Major 2) — neither requires touching the frozen `## Goal`/`## Non-goals`/`## Acceptance criteria`/`## Input space` region or a re-freeze.

#### Fast-follow disposition
- Minor 1 (`eval` of extracted doc text without an allow-list) — intended disposition: **won't-fix**. Reason: the mechanism is real but not a security boundary given this repo's own PR-review gate and the workflow's `pull_request` (not `pull_request_target`) trigger with no elevated permissions; it is also the frozen spec's own deliberate design choice (extraction over transcription), so "fixing" it would mean redesigning a frozen decision for a risk this repo's existing controls already cover.
- Minor 2 (environment-variable isolation gap in `run_lines_in_repo()`) — intended disposition: **file-an-issue**. Reason: cheap, low-risk hardening (wrap the `eval` call site in the same `env -u` pattern already used elsewhere in this file) that is not urgent enough to hold up this merge given it is unreachable in the shipped CI configuration, but is worth tracking so it doesn't get lost.
