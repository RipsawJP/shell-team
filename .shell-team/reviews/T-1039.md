<!-- BEGIN review: T-1039 -->

# QA verification — T-1039

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

## check-acs.sh

`bash bin/check-acs.sh --dry-run .shell-team/specs/T-1039-promote-retro-2026-08-06.md` then
`bash bin/check-acs.sh .shell-team/specs/T-1039-promote-retro-2026-08-06.md` →
**14 passed, 0 failed, 1 skipped (AC15, runtime)** — matches the predicted post-freeze shape exactly
(AC1–AC14 PASS, AC15 SKIP by design).

## Frozen-intent alignment

`bash bin/check-intent.sh .shell-team/specs/T-1039-promote-retro-2026-08-06.md .shell-team/todo.md` →
`aligned: T-1039 v1 (368984d49f33b97fac6bf3f403aa1d63faec40ff)`, exit 0.

## Structural checkers on the new records

`bash bin/check-provenance.sh .shell-team/provenance/T-1039.md` → conformant (2 decision entries).
`bash bin/check-interventions.sh --task T-1039 -- .shell-team/interventions/T-1039.md` → conformant
(0 entries, 1 sentinel). `bash bin/check-pii-shapes.sh --base 253f4b7` → clean. Per this task's own
governing rule, the *structural* conformance of provenance is what's asserted here — the semantic
accuracy of its grounding claims is judged separately below and is explicitly out of this gate's
FAIL scope.

## Scope / allow-list

`{ git diff --name-only 253f4b7; git ls-files --others --exclude-standard; } | sort -u` — 13 files,
exactly the 11 required paths plus `.shell-team/provenance/T-1039.md` and
`.shell-team/interventions/T-1039.md` (no `reviews/T-1039.md` yet at QA-entry time, as expected).
`bin/`, `tests/`, `.github/`, `docs/`, `skills/`, every other `agents/*.md`, every other spec and
the retro file are untouched. Relayed premise #1 (`253f4b7` is `develop`'s tip and this branch's
real branch point) independently re-measured: `git rev-parse --verify 253f4b7` == `git merge-base
253f4b7 HEAD` == `253f4b73c9d1f886adb3132e815c5b95f1d0e564`, matches `develop`'s tip. Confirmed true.

## CI-wired suite list — run live, in workflow order

Ran every step `.github/workflows/check-handoff.yml` lists, in that file's order, from the real
checkout root (per the retro's Problem 4 constraint, `check-playbook`, `gen-playbook-blocks`,
`playbook-promote` and `retro-inputs` were run strictly sequentially — no concurrency anywhere in
this run). shellcheck (pinned invocation), every fixture suite, and every "Dogfood" step —
including the two literal directory-scanning dogfood steps (`team-paths` default-layout resolution,
and `check-interventions` over every committed `.shell-team/interventions/*.md`, run exactly as CI
invokes them, no `--task` flag) — **all green, zero failures**. This satisfies AC15(a)'s
"any step that enumerates a directory or a glob is run rather than reasoned about" requirement
directly, since I ran it myself rather than trusting the hand-off's summary claim.

## Mutation probes — reproduced myself, scratch `git worktree` only, real checkout never touched

Created `git worktree add` off `HEAD` under `$TMPDIR`; every mutation below ran there and the
worktree was removed afterward (`git worktree remove --force`). `git status --short` on the real
checkout was clean before, during and after.

- **Probe 1** (one-word edit inside N1's `Rule`, corpus regenerated before re-checking):
  reproduced the predicted shape exactly — AC1, AC6, AC8, AC12 all flip FAIL; AC2–AC5, AC7, AC9–AC11,
  AC13–AC14 stay PASS. Matches the spec's AC15(b) prediction and the provenance's own account of this
  probe byte for byte.
- **Probe 2, AC6's own blind-spot** (move N1 from its appended position to immediately after the
  `## 2026-04-29 — Bootstrap` entry, leaving N2/N3 appended at the end — the exact construction the
  provenance file describes for its second decision): reproduced AC1–AC5 PASS and AC6 FAIL, matching
  both the spec's stated prediction ("AC6 red while AC1–AC5 may stay green") and the provenance's
  claim on that half. **However**, my reproduction also flips **AC7 FAIL** (`gen-playbook-blocks.sh`
  performs a full in-order regeneration — confirmed by reading its own header comment and by diffing
  a regenerated block, which visibly reorders `playbook-engineer.md`'s spliced lines) — a red the
  provenance file's own account of this probe does not mention ("AC1–AC5 all PASS, AC6 alone FAIL").
  This is **not** a spec violation: the spec's own AC15(b) description commits to nothing about AC7
  for this probe, so nothing here contradicts the frozen text. It **is** an inaccuracy in the
  provenance file's grounding narrative for its second decision. Per this task's own governing rule
  on provenance review, judging the semantic accuracy of a grounding claim is explicitly out of this
  gate's FAIL scope (structural `conformant` only) — recorded here as a disclosed finding for the
  reviewer and as retro input, not as a blocking defect.

## AC15(c) — spot-checked myself against the primary records the retro cites

Opened `.shell-team/interventions/T-1033.md`, `T-1034.md`, `T-1035.md`, `T-1036.md` directly (none of
these were opened by pm-spec — see the spec's own `## Summarized sources` disclosure) and checked
every clause of N1's and N2's `Why` fields against them:

- N1's four claimed contradicted premises — "two predictions about the shape of another task's check
  lines" (T-1033.md's two entries, both about T-1027's predicted check shape), "a conflation of two
  different populations of the same kind of location" (T-1034.md — the 47-address ledger's location
  conflated between `check-intent.sh` and `errexit-safe.sh`), "a claim about which criterion a pair
  of counts had been frozen into" (T-1035.md — 214/26 claimed frozen into T-1024's AC2, actually
  living in an audit doc) — all four independently confirmed accurate, and the "at least four, two in
  one task" count/ordering distinction survives.
- N2's "three instances of a single coupling defect in a row… only after the third… did the work
  switch to a full inventory" — confirmed word-for-word against `T-1036.md`'s 2nd/3rd/5th entries
  ("population-blind global counts; own-version coupling; now repair-count coupling… crosses the
  same-class-2 threshold"). The "consecutive within a single task" distinction survives.

## `## Summarized sources` — verified against the sources themselves

- `.shell-team/retros/2026-08-06.md` — read in full. Candidate count = 7 (confirmed: the `##
  Lesson candidates` section lists exactly seven bullets). The two load-bearing distinctions the spec
  claims to carry over — candidate 1's "at least four" contradicted premises (retro enumerates exactly
  four, two in one task) and candidate 2's "three consecutive within a single task" — both confirmed
  present in the retro's own prose, not flattened.
- `.shell-team/specs/T-1033-promote-retro-2026-08-05.md`, AC10 — confirmed the literal `44 / 32 / 23
  / 53` bullet-count claim verbatim.
- `.shell-team/specs/T-1027-promote-retro-2026-08-04.md`, `E3` frozen block — confirmed the heading
  text the spec quotes matches byte for byte.
- `.shell-team/lessons.md`, `## Format` — confirmed the `all` = four-IN-roles gloss, the `Scope`
  shipping-decision sentence, the "two field bullets kept outside the fenced example"
  (`Superseded-by`/`Extended by`), `Extended by`'s never-schema-checked status, and `Superseded-by`'s
  equality-resolution rule — all verbatim matches.
- `bin/playbook-promote.sh` — confirmed the emitted field order, the one-blank-line-before-append
  behaviour (`printf '\n'` before `cat "$ENTRY_FILE"`), the loop-scope no-`Bound-in` behaviour, and
  `--title`'s heading-suffix-only contract.
- `bin/gen-playbook-blocks.sh` — confirmed the emitted line shape
  `- <rule> (<pointer-path>, <date> — <title>)` matches the real `printf` format string.
- `bin/check-playbook.sh` — confirmed `KNOWN_ROLES="engineer qa-verifier tech-lead pm-spec all"` and
  the other three closed sets match the spec's characterisation verbatim.
- `templates/prompt-blocks/registry.txt` — confirmed `agents/tech-lead.md` is registered under three
  `marker` rules (`language.md`, `careful-execution.md`, `playbook-tech-lead.md`) plus one `contain`
  rule, correcting the relayed premise of two — matches the spec's corrected claim exactly.
- `agents/tech-lead.md` — confirmed `## Rules` (line 73) precedes the first
  `<!-- BEGIN prompt-block: -->` (line 82), with `M1` as the section's last bullet.
- `bin/check-board-headings.sh` usage — confirmed via a successful live invocation with the claimed
  `<board-file> [--base <ref>]` form.
- The host's 2026-08-06 adjudication — relayed, not observed, correctly disclosed as such throughout;
  no independent confirmation is possible from this session and none was attempted.

No flattened distinction found in any checked source.

## Relayed claims (spec `## Assumptions`) — re-measured

`253f4b7` = `develop` tip and real branch point (confirmed above); `## Active` was empty at
`253f4b7` (confirmed: `git show 253f4b7:.shell-team/todo.md` shows an empty `## Active` section);
candidate 7's covering entry (`2026-08-05 — A background agent is monitored…`) is present and
`Status: active` in the corpus (confirmed at .shell-team/lessons.md:951). The remaining two relayed
items (the host's adjudication itself, and the T-1043/#168 + T-1040 ownership ids) are, by their own
nature, not independently confirmable from this session — recorded as `environmentally-unverified`
below rather than silently accepted.

## Edge cases tried

- Reproduced two of the seven AC15(b) mutation probes end to end on scratch worktree copies (see
  above) rather than trusting the hand-off's "all 7 reproduced" claim at face value.
- Checked whether the corpus's schema validator enforces chronological entry ordering (it does not —
  confirmed by the AC6-blind-spot probe passing `bin/check-playbook.sh` with N1 relocated earlier
  than its own date position); this is consistent with the corpus's documented grammar, not a gap.
- Confirmed the real, un-mocked `git worktree` mechanism (never a working-tree mutation) throughout —
  `git status --short` on the real checkout stayed empty across every probe.

## Risk notes for the reviewer

1. **Provenance narrative inaccuracy (non-blocking, disclosed above).** The provenance file's second
   decision entry claims the AC6-blind-spot mutation probe leaves "AC1–AC5 all PASS, AC6 alone FAIL".
   Reproducing the exact construction it describes also flips AC7 (order-sensitive full
   regeneration). The spec's own AC15(b) prediction is unaffected (it makes no claim about AC7 for
   this probe) and no shipped byte is wrong — only the provenance's own account of *why* the
   mutation was rebuilt undersells its actual blast radius. Per this task's own governing rule on
   provenance review (structural conformance only, not semantic judgment), this is not a QA-gate
   FAIL — flagging for the reviewer and as a retro-input candidate (ironic, given the task's own
   subject is claim fidelity).
2. **AC15(a)/(c)/(d)/(e)'s board hand-off is a compressed summary, not an itemized report.** The
   spec's own text for AC15 repeatedly says "the hand-off states, per entry…" / "…the measured value
   beside each" / "per-source report" — i.e. the itemized substance is meant to live on the board.
   What's actually on the board (`.shell-team/todo.md`'s engineer-summary sub-bullet) is one
   compressed sentence asserting completion, not the itemized breakdown. I independently
   reconstructed and verified the substance myself (CI suite run live, two probes reproduced, four
   named records opened and Why-clauses checked, every source in `## Summarized sources` checked
   against its own bytes, the checkable relayed claims re-measured) and found it sound apart from
   finding (1) above — so I am not blocking on this, but the gap between "reported in the hand-off"
   (what AC15 asks for) and "asserted in the hand-off" (what's actually there) is real and worth a
   retro note: a criterion that is SKIP specifically so a human/QA can judge the substance needs the
   substance written down, not summarized past recognition.
3. **`environmentally-unverified`**: the host's 2026-08-06 adjudication (which candidates were
   promoted/mechanised/dropped) and the T-1043/#168, T-1040 ownership ids are relayed premises this
   session has no primary channel to confirm (no access to the planning conversation or to the
   issue tracker's live state). Disclosed per the spec's own Assumptions section; not a blocker.

<!-- END review: T-1039 -->

<!-- BEGIN review: T-1039-codex -->

### Codex Review verdict: APPROVE
- Task: T-1039
- Codex model: gpt-5.6-sol (config default, `model_reasoning_effort = "high"`; no `-m` override passed)
- Verification mode: executed-tests — re-ran `bash bin/check-acs.sh` live (14 passed, 0 failed, 1 skipped, matching QA's own re-run) and reproduced QA's AC15(b) probe 2 (the AC6 blind-spot construction) myself end to end in a scratch `git worktree` (never the working tree; removed afterward, `git status --short` on the real checkout confirmed clean before/after)
- Diff scope: 14 files, +596/-0 (`git diff --stat 253f4b7...ebd60c7`)

#### Mandatory disclosure — frozen-intent re-freeze check
**No re-freeze occurred on this branch.** The board's T-1039 entry carries exactly one `intent-hash (v1)` line and exactly one `freeze-attestation (v1)` line, and zero `intent-ratified` lines anywhere in its sub-bullets (`grep -c intent-ratified .shell-team/todo.md` finds 47 occurrences repo-wide, all attributable to other tasks; none inside T-1039's own entry). `check-intent.sh` reports `aligned: T-1039 v1 (368984d49f33b97fac6bf3f403aa1d63faec40ff)`. This matches the task prompt's claim exactly — verified directly against the board and the checker, not accepted on the prompt's say-so.

#### Blockers
- none

#### Major
- none

#### Minor / nits
- **`.shell-team/todo.md:15`** — AC15's own spec text asks the board hand-off to state, per entry, which `Why` clauses were confirmed against a record and which were generalised, plus a per-source report for `## Summarized sources`. The engineer's board sub-bullet (line 15) is a compressed one-liner asserting completion rather than that itemization; QA's own spot-check (line 30, and `.shell-team/reviews/T-1039.md`'s `AC15(c)` section) itemizes N1 and N2 but does not open the records candidate 4/N3 cites (`.shell-team/reviews/T-1035.md`, `.shell-team/reviews/T-1036.md`, `.shell-team/reviews/T-1033.md`), leaving N3's `Why` unverified by QA specifically. I opened all three myself and confirmed N3's three `Why` clauses against them: "a design-decision sentence came to contradict the criterion it introduced" matches `T-1035.md`'s Major finding M1 (T-1024's DP1 contradicting its re-frozen AC2); "a goal sentence named a version number that a neighbouring task's later re-freeze moved past" matches `T-1036.md`'s first Minor/nit (T-1036's own frozen Goal sentence narrating T-1029 v2→v3 after T-1029 reached v4); "a sentence citing a fact's origin named the wrong record" matches `T-1033.md`'s synthesis-ledger row 1 (AC15(c) misnaming N1's source as `reviews/T-1032.md` instead of `reviews/T-1028.md`). All three hold up — the substance is sound, and my check closes the specific gap Codex flagged (N3 "unauditable"). What remains is a reporting-format gap, not a substantive one, and it is the same gap QA's own risk note 2 already disclosed as non-blocking.
- **`.shell-team/provenance/T-1039.md:9`** (and the mirrored board line 30) — the second decision entry's grounding narrative states the AC6-blind-spot probe leaves "AC1–AC5 all PASS, AC6 alone FAIL." I independently reconstructed the identical probe (N1's block relocated to immediately after the `## 2026-04-29 — Bootstrap` entry, N2/N3 left appended at the end) in a scratch worktree and confirmed AC1–AC5 PASS / AC6 FAIL **and AC7 FAIL** — `bin/gen-playbook-blocks.sh` performs a full in-corpus-order regeneration, so relocating N1 earlier in the corpus reorders the spliced lines in every generated block. This matches QA's own disclosed finding exactly. No shipped byte is affected (AC7 is green on the real, unmutated tree; confirmed by my own `check-acs.sh` run above) and the spec's own AC15(b) prediction makes no claim about AC7 for this probe, so nothing here contradicts frozen text — it is a narrative gap in an already-committed decision record. Per this repo's own precedent for the identical class of defect (`.shell-team/reviews/T-1033.md`'s synthesis-ledger row 1, which downgrades a Codex-tagged "major" `## Summarized sources` mis-citation to minor because "not engineer-actionable — repair is a pm-spec re-freeze" and "the substantive requirement was actually discharged"), this is not a merge blocker.
- **`.shell-team/todo.md:30`** — the QA verdict line states "13 files: the 11 required plus provenance + interventions; this review record makes 12 -> still exactly on the allow-list." 13 + 1 = 14, not 12; measured directly (`{ git diff --name-only 253f4b7; git ls-files --others --exclude-standard; } | sort -u | wc -l` = 14 right now). Pure arithmetic-label typo in a QA prose sub-bullet — the actual `AC14` check (which independently re-derives the allow-list membership rather than reading this sentence) still passes, confirmed by my own live re-run above. No functional consequence.

#### Adversarial findings
- Not launched. No Blocker or Major finding survived synthesis (see ledger below), which is this role's sole trigger for the opt-in adversarial pass — running one here would be launching it speculatively, which the role definition rules out.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC15 board hand-off is a compressed summary rather than the itemized per-entry/per-source report AC15's text asks for (Codex primary: `[P2]`, `.shell-team/todo.md:15`) | primary: major (P2) | minor | Independently re-verified: I opened all three of N3's cited records (the one gap QA's own spot-check left open) and confirmed its `Why` clauses hold; the substance is sound and matches QA's own already-disclosed risk note 2. AC15 is `SKIP` by design (no `check:` line can enforce the itemization), so this is a reporting-format gap, not a shipped defect — not engineer-actionable within this branch, and not a re-freeze target since no frozen text is wrong |
| 2 | Provenance's 2nd decision entry omits AC7 flipping when reproducing the AC6-blind-spot probe (Codex primary: `[P2]`, `.shell-team/provenance/T-1039.md:9`) | primary: major (P2) | minor | Independently reproduced myself in a scratch worktree; matches QA's own disclosed finding exactly. No shipped byte affected (AC7 green on the real tree, confirmed live); repo precedent (`.shell-team/reviews/T-1033.md` ledger row 1) downgrades this identical defect class — a narrative gap in an already-committed decision record with no `check:`-line consequence — to minor |
| 3 | Board QA line's file-count arithmetic ("makes 12") is inconsistent with the current 14-file changed set (Codex primary: `[P3]`, `.shell-team/todo.md:30`) | primary: minor (P3) | minor | No change — confirmed the arithmetic error directly; the actual `AC14` check independently re-derives the allow-list and passes regardless of this sentence's wording, so it is cosmetic |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- AC15 itemization reporting gap (finding 1) — **file-an-issue**: a recurring shape (a `SKIP`-by-design criterion asking the hand-off for itemized substance, and the hand-off compressing it past recognition) that QA's own risk note already named as retro input; worth a lesson candidate or a convention change at the next planning round rather than a same-branch fix, since nothing here is wrong, only under-itemized.
- Provenance narrative gap re: AC7 (finding 2) — **won't-fix**: the provenance file is a contemporaneous decision record, and rewriting it now to add the AC7 detail after the fact would misrepresent what was reasoned at commit time rather than correct it; the gap is already fully disclosed twice over (QA's review record and this one), which is the right remedy for a narrative omission with no shipped-byte consequence. Retro input, not a code change.
- Board arithmetic typo (finding 3) — **won't-fix**: cosmetic label inside an already-recorded QA prose sub-bullet, no functional impact (the mechanical `AC14` check does not read this sentence), not worth a tracked issue.

<!-- END review: T-1039-codex -->
