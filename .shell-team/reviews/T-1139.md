# Codex Review — T-1139

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1139
- Codex model: gpt-5-codex (codex-cli 0.154.0)
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 7 files, +375/-5 lines (`docs/adopting.md`, `docs/adopting.ja.md`, `agents/pm-spec.md`, `.shell-team/specs/T-1139-codex-runbook-gaps.md`, `.shell-team/todo.md`, `.shell-team/provenance/T-1139.md`, `.shell-team/interventions/T-1139.md`; base `feature/reviewer-host-self-classification` = `ecab477`)

Two Codex passes ran this round: a primary pass (`codex exec … review --base feature/reviewer-host-self-classification --json`, reading the real working tree directly) and a targeted devil's-advocate pass on the three most contested primary findings, run because the primary pass's findings normalize to blocker/major severity under the closed P1/P2→blocker/major map. Both raw outputs are saved (paths at the bottom). I independently re-read the spec (`.shell-team/specs/T-1139-codex-runbook-gaps.md`), the board hand-offs, the provenance file, and — beyond what either Codex pass cited — `templates/shell-team.contract.yaml`, `templates/goal.contract.yaml`, `templates/loop-contract-template.yaml`, `bin/loop-guard.sh`, `bin/check-oversight.sh:644-663`, `docs/tuning-oversight.md` in full and `bin/check-durability.sh:67-74` before synthesizing the verdict below.

#### Blockers
- `docs/adopting.md:571-574` / `docs/adopting.ja.md:577-580` (gap 3, D4) — The new sentence states "the session pushes the branch and opens the pull request against the base the spec names; the merge itself, and the merge GO, stay the human's" — i.e. only merge is human-gated. This contradicts this project's own shipped default loop-contract template: `templates/shell-team.contract.yaml:24` ("must not merge or push without a human gate (see human_gate below)") and `:43-45` (`human_gate: [merge, push]`), mirrored in `templates/goal.contract.yaml:27,46`. D4's own reasoning (`docs/tuning-oversight.md:99`, "the first stop fires on whether the merge changes what runs") never cross-checked itself against the contract template's own explicit inclusion of `push`, even though this task's own AC9 locks `templates/` unedited — so the new sentence ships a firm claim in an area two of this project's own normative artifacts disagree on. `human_gate` is documented as advisory/unenforced metadata (`templates/loop-contract-template.yaml:63-68`, "ignored by the linter"; `bin/loop-guard.sh` never reads it) rather than a machine gate, but that does not rescue the claim: the runbook is telling the reader what the loop *does*, and the shipped default contract an adopter actually gets from `team-init` says push is gated too. This is exactly the "sentence contradicting the shipped … human GO" class of defect this review was briefed to watch for. Fix: either soften the claim to point at the operator's own oversight configuration (`docs/tuning-oversight.md`) for whether push/PR-open needs a stop, or reconcile it explicitly against `human_gate: [merge, push]` rather than asserting push is unconditionally automatic.

#### Major
- `docs/adopting.md:399-408` / `docs/adopting.ja.md:406-414` (gap 1, D1) — The new version-check sentence sits in the same numbered step as, and directly after, "Skip this step if the plugin already shows `installed, enabled`." The sentence itself is technically unambiguous on a full read ("even when STATUS already reads `installed, enabled`" explicitly negates the skip), and the remedy for a still-stale VERSION after running `upgrade` is already covered by the retained `step 2` cross-reference (consistent with D1's no-duplication ruling) — so this is not a factual error. But placing the fix immediately after a "skip this step if X" trigger, for a reader who matches X, creates a real risk of exactly the skim-and-move-on failure this whole task exists to close (b run #4's own operator missed a materially similar staleness gap on a first read-through). Recommend moving the sentence to open with its own clause rather than trailing the skip-condition paragraph, so a reader who correctly determines "skip the install commands" is not also primed to stop reading the paragraph before the version-check instruction.
- `agents/pm-spec.md:16` vs `agents/pm-spec.md:58` (gap 2 authoring site, AC6 — the pre-commitment's droppable item) — Item 3 of `## Your job` now requires `<specs dir>/<task-id>-<slug>.md`, but the `## tasks/todo.md entry format` template a few lines below still shows the legacy `spec: docs/specs/<slug>.md` unedited. Before this task the two lines matched exactly; this diff is what introduces the divergence, and AC6's own body explicitly permits leaving it (a reasoned, but incomplete, tradeoff — `.shell-team/provenance/T-1139.md` entry 3 weighs the `board-line-format.md` two-fragment hazard but never checks this specific downstream consumer). Traced independently and confirmed by the adversarial pass: `bin/check-oversight.sh:644-658` extracts the board's own literal `spec:` pointer via regex and runs `git log -1 --format=%H -- "$SPEC_PATH"`; for a path that does not exist, that command exits 0 with empty output, so the `[ -n "$LAST_TOUCH" ]` guard is false and the pre-merge freshness check silently no-ops rather than failing. This only bites a host that has opted into the `governance-controlled` oversight profile (the shipped default `autonomous` never reaches this code path) and requires `pm-spec` to copy the stale literal template rather than substitute the path it actually used — a real but narrow risk, not a certain one. Recommend a one-line fix to the `:58` template now that the risk is concretely traced, rather than leaving it for a future same-class-2 trigger.

#### Minor / nits
- `docs/adopting.md:663-669` / `docs/adopting.ja.md:670-676` (gap 4, D5) — The new class-M pointer paragraph correctly avoids transcribing the grant text (confirmed: no verbatim copy of `docs/tuning-oversight.md:116-124`'s grant block appears in either file), but it tells a Codex-CLI-hosted adopter to "record your own grant in your own checkout's `CLAUDE.local.md`" without addressing whether a Codex-hosted role ever reads that file. Independently traced, and confirmed by the adversarial pass: the only shipped auto-loading convention for `CLAUDE.local.md` is Claude Code's own "reads instructions from broadest to most specific" mechanism (`docs/tuning-oversight.md:33-45`); nothing in `skills/run/SKILL.md`'s class-M branch (`:61`) or `templates/prompt-blocks/host-dispatch.md` instructs a spawned Codex custom agent, or a raw Codex CLI orchestrator session, to explicitly open and read `CLAUDE.local.md`. The sentence is plausibly a dead letter on a genuine Codex-only host. This fails safe, though — the observable failure mode is "the grant is never consulted, so every re-freeze still asks a human," not a silently-skipped gate — so it does not rise to Major. Worth a fast-follow disclosure rather than a blocking fix.
- `.shell-team/specs/T-1139-codex-runbook-gaps.md:31` (the `- user-visible: no` declaration, inside the frozen intent block) — "already honoured by `bin/check-refreeze-class.sh`" overstates what that script does. `docs/tuning-oversight.md:134-136` ("### The class-M boundary is mechanical; the trigger is not") explicitly disclaims that the checker "never proves the loop actually consulted it before taking the class-M path at all." The checker classifies a delta as `mechanics`/`class-b`/structural; it says nothing about whether a grant exists or was honored. This is an internal process-record imprecision, not adopter-facing prose (the actual runbook sentences, AC4/D5, are careful and never make this overclaim) — fixable as ordinary mutable wording under this sprint's mode-A2 disposition without a re-freeze ceremony. Recommend rewording to "the class-M boundary is already machine-classified by `bin/check-refreeze-class.sh`" or similar.
- QA already disclosed a stale locator (spec's board-line citation `:5341` → actual `:5364`) — editorial, non-frozen, no action needed beyond what QA already recorded.

#### Adversarial findings

A second, devil's-advocate `codex exec` pass (raw output: `.shell-team/reviews/T-1139-codex-adversarial.{txt,jsonl}`) targeted the three most contested primary findings: the push/PR-open human-gate claim, the `agents/pm-spec.md` board-template drift, and the class-M grant's Codex-host reach. It independently traced `templates/shell-team.contract.yaml`, `templates/goal.contract.yaml`, `templates/loop-contract-template.yaml`, `bin/loop-guard.sh`, `bin/check-oversight.sh:644-663`, and `skills/run/SKILL.md`/`templates/prompt-blocks/host-dispatch.md`, and returned: claim A (push-gate contradiction) "partially-holds" — the vocabulary-token framing (`push-go`) is a misread, but the shipped default *contract template*'s `human_gate: [merge, push]` genuinely conflicts, severity major; claim B (board-template drift → silent freshness-check skip) "holds", severity major; claim C (`CLAUDE.local.md` cross-host reach) "holds", severity minor (fails safe). I independently re-verified claim A's contract-template citation myself by reading `templates/shell-team.contract.yaml:24,43-45` directly, which is what promoted that finding to Blocker in the synthesis below (a step neither Codex pass nor my own first-pass read took on its own).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | gap-1 "skip this step" ordering risk (docs/adopting.md step 1) | primary: blocker (P1 default map) | major | Text is technically unambiguous on a full read and the remedy is already cross-referenced (D1); this is a placement/emphasis risk, not a factual error, so it does not meet the blocker bar — but it targets a never-dropped component (AC1) so it still must be fixed. |
| 2 | push/PR-open human-gate claim (docs/adopting.md step 10, D4) | primary: blocker (cited SKILL.md `push-go` vocabulary, an imprecise citation) → my own first pass: near-rejection as a misread of optional vocabulary → adversarial: partially-holds, citing `templates/shell-team.contract.yaml`'s shipped default `human_gate: [merge, push]`, severity major | blocker | Independently re-verified the adversarial pass's citation by reading `templates/shell-team.contract.yaml:24,43-45`, `templates/goal.contract.yaml:27,46` and `templates/loop-contract-template.yaml:63-68` myself: the shipped default loop-contract template genuinely lists `push` as a human gate and states "must not … push without a human gate." Codex's own original evidence chain (SKILL.md's `push-go` reason-vocabulary) was not the right citation — that part is a misread, since the vocabulary is optional and unrelated to the fixed default — but the conclusion (a real contradiction exists) holds once the actually-shipped default contract is checked, which neither the primary pass, my own first-pass read, nor the frozen spec's own D4/Blast-radius analysis did. Upgraded from the adversarial pass's own "major" to "blocker" given the shipped-contract contradiction is unambiguous once traced and touches a never-dropped component (AC3/AC5). |
| 3 | `agents/pm-spec.md:58` stale template vs `check-oversight.sh` freshness-check bypass | primary: blocker (P1 default map) | major | Adversarial pass confirmed the mechanism trace (`git log -1` on a nonexistent path exits 0 empty, silently skipping the freshness check) and rated it major, not blocker; the risk is real but gated behind the opt-in `governance-controlled` profile and requires a specific literal-copy failure mode, so it is a genuine, newly-introduced risk worth fixing now rather than a certain failure on the shipped default path. |
| 4 | class-M grant paragraph, `CLAUDE.local.md` on a Codex-only host | primary: major (P2 default map) | minor | Adversarial pass independently rated this minor with the same reasoning I reached first: the failure mode fails safe (an unconsumed convenience — every re-freeze still asks a human — never a silently-skipped gate), so it does not meet the major bar despite being a genuine, disclosed-nowhere cross-host gap. |
| 5 | spec's own `- user-visible: no` line overstates what `bin/check-refreeze-class.sh` "honours" | primary: major (P2 default map) | minor | This is an internal process-record imprecision inside the frozen intent block's declaration region, not adopter-facing prose — the actual shipped runbook sentences (AC4/D5) are careful and make no such overclaim. Fixable as mutable wording under this sprint's mode-A2 disposition without a re-freeze. Downgraded per the same "records hygiene" calibration that applies to the spec's own stale board-line citation. |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1139 entry back to `READY_FOR_ENG` with the findings above linked (Blocker: push/PR-open human-gate claim in `docs/adopting.md`/`docs/adopting.ja.md` step 10, contradicting `templates/shell-team.contract.yaml`'s shipped default `human_gate: [merge, push]`; Major: gap-1 skip-step ordering risk, `agents/pm-spec.md:58` stale template vs `check-oversight.sh`'s silent freshness-check skip)

#### Fast-follow disposition
- Class-M grant paragraph's cross-host reach on a genuine Codex-only host (`docs/adopting.md:663-669`/`.ja.md:670-676`) — file-an-issue: no shipped mechanism yet instructs a Codex-hosted role to read `CLAUDE.local.md`, so the promised convenience is a dead letter there; worth its own issue rather than blocking this task, since it fails safe and the runbook could optionally add one disclosing sentence instead if the engineer prefers a same-round fix.
- Spec's own `- user-visible: no` line's "already honoured by `bin/check-refreeze-class.sh`" overstatement (`.shell-team/specs/T-1139-codex-runbook-gaps.md:31`) — won't-fix as a blocking item: internal process record, not adopter-facing, correctable as ordinary mutable wording without a re-freeze whenever it is next touched; not worth a dedicated issue.

Raw Codex output saved at `.shell-team/reviews/T-1139-codex-primary.{txt,jsonl}` (primary review pass) and `.shell-team/reviews/T-1139-codex-adversarial.{txt,jsonl}` (devil's-advocate pass).

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base feature/reviewer-host-self-classification --json -o <RAW_OUT> < /dev/null > <RAW_JSONL> 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-carried - the shipped `review --base` subcommand takes no prompt argument at all (agents/codex-reviewer.md), so this round's own launch-briefing (the consumer-class framing, the seven named spots for an independent read, the severity calibration) has no channel into this pass; the same disclosed gap prior rounds' records (T-1104 through T-1113) already name for this identical subcommand shape
- raw-capture (primary): T-1139-codex-primary
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before either codex exec invocation was launched

- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on the following three claims about docs/adopting.md, docs/adopting.ja.md and agents/pm-spec.md (diff base: feature/reviewer-host-self-classification, HEAD is the current worktree). Read the real files yourself with git diff/git show/nl -ba -- do not expect any diff pasted here. Claim A ... Claim B ... Claim C ... Return a JSON array of {claim, verdict, evidence, severity_if_valid}." < /dev/null > <RAW_JSONL> 2>&1
- pass-role (adversarial): confirmation
- briefing-fidelity (adversarial): carried - the three contested claims (push/PR-open gate, pm-spec.md template drift, class-M grant cross-host reach), the exact files and the exact prior citations to verify or rebut were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (adversarial): T-1139-codex-adversarial
- self-detected-host (adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before either codex exec invocation was launched

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1139
- Codex model: gpt-5-codex (codex-cli 0.154.0)
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 8 files, +514/-13 lines (`docs/adopting.md`, `docs/adopting.ja.md`, `agents/pm-spec.md`, `.shell-team/interventions/T-1139.md`, `.shell-team/provenance/T-1139.md`, `.shell-team/reviews/T-1139.md`, `.shell-team/specs/T-1139-codex-runbook-gaps.md`, `.shell-team/todo.md`; base `feature/reviewer-host-self-classification` = `ecab477`; HEAD = `1f8fdca`)

Round 2. Since round 1 (`fbf5659`, `REQUEST_CHANGES`): a class-B re-freeze v1→v2 (board `intent-ratified (2026-09-13)`, `intent-hash (v2): 59c85b23bc197cab0f3dda674cefa2e08d0f431b`) corrected the Goal's gap-3 sentence and the `- user-visible: no` "honoured" phrase, followed by round-2 engineer rework (`d7d5265`) and QA PASS (`1f8fdca`). Two Codex passes ran this round: a primary pass (`codex exec … review --base feature/reviewer-host-self-classification --json`, reading the real working tree directly) and a targeted devil's-advocate pass on the primary pass's two findings, run because both normalize to major/blocker severity under the closed P1/P2→blocker/major map. Both raw outputs are saved (paths at the bottom). I independently re-read the full spec (`.shell-team/specs/T-1139-codex-runbook-gaps.md`), the board's `intent-ratified`/`freeze-attestation (v1, v2)`/`version-derivation (v1, v2)` records, `.shell-team/provenance/T-1139.md`, both engineer hand-offs and both QA verdicts, `templates/shell-team.contract.yaml`, `templates/goal.contract.yaml`, `templates/loop-contract-template.yaml:63-68`, `docs/tuning-oversight.md:90-138`, `skills/run/SKILL.md` (grepped for every `human_gate`/push/merge-pledge mention), and every `bin/*.sh` occurrence of `human_gate` (none) before synthesizing the verdict below.

#### Mandatory item — the class-B re-freeze v1→v2

Verified, not merely trusted. Re-derived the v1 spec from `git show 990aef9:.shell-team/specs/T-1139-codex-runbook-gaps.md` (the pre-round-1 commit) and ran `bash bin/check-refreeze-class.sh --old-hash a1ce50bac50909ca704d869d2baf13721fb81d52 <v1 copy> .shell-team/specs/T-1139-codex-runbook-gaps.md` → `check-refreeze-class: class-b: differing line at intent-block index 1 for T-1139 is not a '- check:' line on both sides` — matches the board's own `class=B` record; a class-B re-freeze correctly still took a documented ground rather than a per-instance human GO waiver, per this checkout's standing propose-and-proceed ruling the board cites. `bash bin/check-intent.sh .shell-team/specs/T-1139-codex-runbook-gaps.md .shell-team/todo.md` → `check-intent: aligned: T-1139 v2 (59c85b23bc197cab0f3dda674cefa2e08d0f431b) matches .shell-team/specs/T-1139-codex-runbook-gaps.md`. Read the v2 Goal sentence against `templates/shell-team.contract.yaml:24` ("must not merge or push without a human gate") and `:43-45` (`human_gate: [merge, push]`), and `templates/goal.contract.yaml:27,46` — the shipped default genuinely gates both `merge` and `push`, so v2's "under the shipped default contract both `merge` and `push` are human gates, so the session stops there and the human pushes… and opens the pull request" is now **true of the shipped default**, closing round 1's Blocker. The ratification record's ground is honest: it names `templates/shell-team.contract.yaml:24,43-45` as the citation that forced the correction and states plainly "Root: issue #509's ask 3 was authored by the coordinating session from this checkout's own private sprint practice (the session opens the PR), not from the shipped template — the coordinating session's own authoring defect, not pm-spec's" — attributing the original flawed ask to its actual origin rather than to the authoring role. **This item does not revert the re-freeze**; it stands.

#### Blockers
- `docs/adopting.md:396-409` / `docs/adopting.ja.md:393-419` (gap 1, **AC1**, never-dropped) — Primary Codex finding, independently re-verified by reading the exact sentence: "Only once that comparison is done, skip the install commands above; if the plugin was not yet installed at all, run them, then see step 2 for the one further check and the remedy if the version is still stale." The clause that governs the already-`installed, enabled`-but-possibly-stale branch conditions the skip on **having performed** the VERSION comparison, never on its **result** — a reader who runs `codex plugin marketplace upgrade` + compares and finds a genuine mismatch is told nothing different from a reader who finds a match; both are licensed to "skip the install commands above." The "see step 2 for… the remedy if the version is still stale" clause is grammatically the tail of the *not-yet-installed* branch ("if the plugin was not yet installed at all, run them, **then** see step 2…"), not of the already-installed branch, so it does not actually rescue the mismatch case even on a generous reading — and even if it did, step 2's own remedy is the presence test this very sentence says "cannot see a version gap." This is the exact failure mode gap 1 exists to close (b run #4's operator silently ran a stale plugin because nothing forced a re-check), reproduced by the corrected text's own residual ambiguity. I confirm this is **not** rescued by D2's Non-goal (no assertion about what `upgrade` does to a following `add`): D2 forbids asserting an *effect*, not withholding an *instruction* to act on a measured mismatch, and D1's Goal itself commits to explaining *why* step 2's test can't see this condition — which only sharpens the reader's need for an explicit next step the text does not give. A targeted adversarial pass concurred (`holds`, `major`, independent of round 1) before I applied this task's own severity calibration (a residual ambiguity on the never-dropped component whose entire reason for existing is closing this exact operator-turn) and rated it **blocker**. **New, independent from round 1's finding on this same component** (round 1: `instruction-ordering`, Major — the sentence trailed the skip-condition trigger; this round: `incomplete-remedy` — the corrected, reordered sentence still never conditions the skip on the comparison's result). Fix: make the skip conditional on a matching `VERSION`, and give an explicit action for a mismatch (even if only "re-run `codex plugin add shell-team@ripsawjp`" per step 2's own already-shipped remedy sentence, restated as applicable to this branch too, not only the not-yet-installed one).

#### Major
- `docs/adopting.md:570-580` / `docs/adopting.ja.md:576-591` (gap 3, **AC3**, never-dropped) — Primary Codex finding. The new sentence's second clause — "a contract whose `human_gate` list omits `push` lets the session push the branch and open the pull request itself" — describes a conditional behaviour with no shipped implementation anywhere in this repository. Independently verified: `grep -rn "human_gate" bin/` returns nothing (confirming round 1's own established fact that `human_gate` is "ignored by the linter" per `templates/loop-contract-template.yaml:64`), and `skills/run/SKILL.md`'s own "Done" step (7) is "summarize what changed for the user in 3–5 bullets" — no push, no PR-open instruction, conditional or otherwise, anywhere in that skill. Unlike the class-M standing-grant convention this document cites elsewhere (`docs/tuning-oversight.md:111`–`124`), which is a *complete*, documented procedure an acting session can follow end to end even though unenforced by code, no file in this repository — not `skills/run/SKILL.md`, not `CONTRIBUTING.md`, not `docs/tuning-oversight.md` — documents *how* a session would push/open a PR on its own when `human_gate` omits `push`. The sentence asserts a specific adopter-configurable behavioural consequence of this project's own loop that this project has not actually built or written down anywhere else. The devil's-advocate pass concurred (`holds`, `major`, independent of round 1) after independently confirming the same `bin/`/`SKILL.md` absence and explicitly weighing the class-M analogy, rejecting it on the same distinction I drew. I keep this at **major** rather than blocker: the practical failure mode is under- rather than over-promising (an adopter who customizes their contract this way gets a session that still pauses, not one that silently pushes unreviewed), so it does not carry the "silent stale-plugin" risk of the Blocker above. **New, independent from round 1's finding on this same component** (round 1: `shipped-default-contradiction`, Blocker — a single fixed claim contradicted the shipped default; this round: the *replacement's second clause*, which is now internally consistent with the shipped default but describes a mechanism this project has never built). Fix: either add the disclosing clause this repository uses elsewhere ("a discipline the operator's own tooling must implement — this project does not read `human_gate` itself"), or drop the omit-`push` clause and state only the shipped default's behaviour plus a pointer to `docs/tuning-oversight.md`-style operator responsibility.

#### Minor / nits
- Gap 4's `CLAUDE.local.md` pointer on a Codex-only host (`docs/adopting.md:663-669`ish / `.ja.md` mirror) — unchanged since round 1, correctly left untouched this round per the round-1 fast-follow disposition (engineer's round-2 hand-off confirms it was deliberately not touched). No new finding.
- The spec's own `- user-visible: no` "honoured" phrasing — corrected in the v2 re-freeze's own delta (now reads "the mechanics/class-B boundary it rests on is already drawn by `bin/check-refreeze-class.sh`"). Confirmed fixed; no residual issue.

#### Adversarial findings
A targeted devil's-advocate `codex exec` pass (raw: `.shell-team/reviews/T-1139-codex-adversarial-r2.{txt,jsonl}`) tested both primary findings against the strongest counter-reading available for each — for Claim A, whether D2's Non-goal or AC1's own "judged by review" clause meant no explicit remedy was actually owed; for Claim B, whether the class-M standing-grant convention's own unenforced, instruction-file-only precedent excused the same property here. It independently re-read `bin/`, `skills/run/SKILL.md`, `docs/tuning-oversight.md:107-111,136`, and the frozen intent block, and returned both `holds` at `major`, both `same_component_as_round1: true` and `independent_of_round1_finding: true` — matching my own synthesis before I saw its output (I read the files first-hand and reached the same conclusions independently, then ran this pass to check them, per this role's own devil's-advocate discipline rather than accepting a first pass at face value).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | gap-1 step-1 mismatch-remedy ambiguity (`docs/adopting.md:396-409`) | primary: [P1]→blocker (default map); adversarial: holds, major | blocker | Independently re-read the exact clause structure myself: the skip is conditioned on having *performed* the comparison, not on its *result*, and the "see step 2" clause is grammatically scoped to the not-yet-installed branch only. This reproduces the exact silent-stale-plugin failure mode gap 1 exists to close, on AC1 — a never-dropped component whose whole reason for existing is this failure mode — so I keep it at blocker rather than the adversarial pass's major, per this task's own calibration (an instruction that "sends a Codex-host reader wrong" on a never-dropped component). |
| 2 | gap-3 step-10 omit-`push` clause has no shipped implementation (`docs/adopting.md:570-580`) | primary: [P2]→major (default map); adversarial: holds, major | major | Independently confirmed `human_gate` is read by zero `bin/` scripts and `skills/run/SKILL.md`'s own "Done" step never mentions push/PR-opening at all, conditional or otherwise — unlike the class-M grant, which is a *complete* documented procedure elsewhere in this repo. Kept at major, not blocker: the failure direction is under- rather than over-promising, so it carries no silent-risk consequence the way finding 1 does. |
| 3 | (none rejected) | — | — | Both findings held under my own independent read and the adversarial pass; nothing in this round was downgraded or rejected. |

**Pre-commitment trigger, named rather than left implicit.** Both findings above land on **never-dropped** components — AC1/gap 1 and AC3/gap 3 — and both are independent, new defects relative to round 1's own findings on those same two components (round 1: `instruction-ordering` Major on AC1, `shipped-default-contradiction` Blocker on AC3; this round: `incomplete-remedy` Blocker on AC1, `unimplemented-conditional` Major on AC3). Per the spec's own Pre-commitment section: "Trigger (same-class-2): two consecutive review rounds producing independent new defects in the same component… A trigger on a never-dropped component stops the task and returns it to planning" rather than buying a third patch round, per the operator-ratified ruling of 2026-09-06 (T-1121) the spec itself cites. Both conditions are met on both components: round 1 (`fbf5659`) and this round are the two consecutive review rounds, and each component drew a new, independent finding in each. **I am naming this rather than recommending a third rework round.**

#### Recommendation
- The generic REQUEST_CHANGES disposition (set back to `READY_FOR_ENG` with findings linked) is **superseded here** by the spec's own Pre-commitment trigger above: this task's own operator-ratified rule requires it **stop and return to planning** rather than receive a third engineering round, because both never-dropped components AC1 and AC3 each drew an independent new defect in two consecutive review rounds. The mandatory re-freeze item is separately confirmed sound and is **not** reverted by this verdict — only the same-class-2 trigger governs the disposition here.

#### Fast-follow disposition
- Gap 4's `CLAUDE.local.md` pointer's cross-host reach on a genuine Codex-only host (`docs/adopting.md`/`.ja.md`) — unchanged from round 1's declared intent: file-an-issue, if not already filed by the coordinating session from round 1's own disposition.
- No new fast-follow deferrals this round (both new findings above are Blocker/Major, not deferred).

Raw Codex output saved at `.shell-team/reviews/T-1139-codex-primary-r2.{txt,jsonl}` (primary review pass) and `.shell-team/reviews/T-1139-codex-adversarial-r2.{txt,jsonl}` (devil's-advocate pass).

- executor-invocation (primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base feature/reviewer-host-self-classification --json -o <RAW_OUT> < /dev/null > <RAW_JSONL> 2>&1
- pass-role (primary-r2): generation
- briefing-fidelity (primary-r2): not-carried - the shipped `review --base` subcommand takes no prompt argument at all (agents/codex-reviewer.md), so this round's own launch-briefing (the mandatory re-freeze item, the six numbered spots for an independent read, the severity calibration) has no channel into this pass; the same disclosed gap round 1's record and T-1104-through-T-1113's records already name for this identical subcommand shape
- raw-capture (primary-r2): T-1139-codex-primary-r2
- self-detected-host (primary-r2): claude-code — CODEX_THREAD_ID was not set in this shell before either codex exec invocation was launched

- executor-invocation (adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on two claims a first review pass made about docs/adopting.md ... Claim A ... Claim B ... Return a JSON array of objects: {claim, verdict, evidence, severity_if_valid, same_component_as_round1, independent_of_round1_finding}." < /dev/null > <RAW_JSONL> 2>&1
- pass-role (adversarial-r2): confirmation
- briefing-fidelity (adversarial-r2): carried - the two contested claims (gap-1 mismatch-remedy ambiguity, gap-3 unimplemented omit-push clause), the exact files, the D1/D2/D4 rulings and the class-M analogy to weigh were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (adversarial-r2): T-1139-codex-adversarial-r2
- self-detected-host (adversarial-r2): claude-code — CODEX_THREAD_ID was not set in this shell before either codex exec invocation was launched
