# T-1107 — Codex Reviewer records

## Spec review

### Codex Spec-Review verdict: REQUEST_CHANGES
- Task: T-1107
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line, measured this round rather than relayed; the JSONL event stream for this free-form pass carries no explicit model-identifier field (the same disclosed gap T-1093's own round-2 record names for this identical invocation shape), so the config-file value is the primary source; no `-m` override was passed.
- Codex CLI: codex-cli 0.149.1 (verbatim `codex --version` output this round)
- Mode: Spec-review (specify seam, T-1092), first live invocation
- Consumer class: a shipped `bin/` script adopters execute directly at the pre-merge boundary (`docs/adopting.md:698-700`), plus its CI-wired fixture suite and the differential harness that keeps its accept/refuse surface honest — full adversarial depth applies, per the spec's own `## Reviewer scope`; the operator's 2026-08-22 dev-scaffold review-depth re-pricing explicitly does not apply.
- Input reviewed: `.shell-team/specs/T-1107-close-out-gates.md`, read in full — the tamper-arm adjudication, Problem, Summarized sources, the frozen intent block (Goal/Non-goals/Input space), all fifteen acceptance criteria and their inline `- check:` lines, Assumptions, Domain premises, Body-to-AC correspondence, Reviewer scope, Blast radius (including the embedded `t1107specs` derivation block) and Pre-commitment. No `--base`, no branch diff — this pass runs before any implementation exists (measured: `git status --short` and `git diff --stat` at branch start showed no engineer commits; `bin/close-out.sh`, `tests/close-out/run.sh` and the two fixtures are all still at their T-1022-shipped bytes).
- issue-canon note: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — this task's grounding rests partly on the actual bodies of GitHub issues #53 and #266 and on the 2026-08-29 discovery that issue #366 was still open after the v2.3.0 batch merge. The spec's own `## Assumptions` row A-1 records these as **RELAYED** into `pm-spec`'s launch prompt (that role holds no `gh`, no network); this role holds neither either. What I did verify independently, against the working tree rather than the spec's own transcription of it: every primary-source file:line citation the two Domain-premise arguments rest on (`agents/codex-reviewer.md:131`, `docs/adopting.md:753`, `docs/adopting.md:698-700`, `CONTRIBUTING.md:55`, `bin/close-out.sh:642` (`esac`), `:691` (`if [ -n "$ISSUE" ]; then`), `bin/check-handoff.sh:33-37` (`ALLOWED_FLAGS`), and the exact two `tests/` occurrences of `--issue` at `tests/close-out/run.sh:76,149`) — all measured byte-accurate, none stale.

#### Findings (independent re-derivation of both domain premises, plus mechanism findings)

1. **Major — the differential harness's own self-check claim for "first-match vs. rightmost-slot" is not actually true for the decoy line it names (Reviewer-scope item (i)).** `## Notes for engineer` and `AC15(a)`'s mutation #3 both point at the existing corpus line `tests/close-out/run.sh:997` (`'- [ ] **T-901** decoy separator — \`x\` — spec: y.md but continues — \`READY_FOR_REVIEW\` — spec: real.md'`) as the case that "punishes a first-match extraction" and claim that mutating close-out.sh to extract by first match rather than rightmost slot shows up as "AC3 red through the harness's own mismatch counter." Independently traced the mechanism this rests on (`tests/close-out/run.sh:1019-1067`, the current, already-shipped `lineshape_case` function): `ls_mismatches` is driven purely by an **exit-code-level accept/refuse comparison** between close-out.sh's actual behavior and the (to-be-composed) oracle's verdict — it never cross-checks the *specific flag token* close-out.sh's stderr names. Under this decoy line, the first backticked token (`x`) is **not** a member of `ALLOWED_FLAGS` and the rightmost one (`READY_FOR_REVIEW`) is a member but **not** `READY_FOR_MERGE`. So under the new composed gate, a correct rightmost-slot extraction (`READY_FOR_REVIEW` ≠ `READY_FOR_MERGE` → refuse) and a buggy first-match extraction (`x` ≠ `READY_FOR_MERGE` → refuse) reach the **identical** accept/refuse verdict — the harness cannot distinguish them, because both land in "refuse." AC3(iii)/(iv)'s own assertions are purely static grep-presence checks on the `lineshape_case` function body and the corpus's flag-axis coverage; neither requires the harness to assert the *specific* extracted token. This directly undercuts Reviewer-scope item (i) ("does it extract the flag by the rightmost-slot rule so a decoy backticked title token cannot fool it") as a claimed, verified property of the shipped mechanism — the self-check's own predicted defeat (AC15(a) mutation 3) would not actually fire.
   - Suggested fix: add (or repurpose) a corpus row whose **first** backticked token is itself a valid `ALLOWED_FLAGS` member distinct from its rightmost token's value (e.g., first = `READY_FOR_ARCH`, rightmost = `READY_FOR_MERGE`, or the reverse) — that is the only shape under which a first-match bug and a correct rightmost-match implementation diverge on the accept/refuse decision itself, which an exit-code-only oracle can actually see. Alternatively, extend the composed oracle to assert that the specific flag token named in close-out.sh's stderr on refusal equals the independently, correctly rightmost-extracted value, not merely that a refusal happened.
   - This is a genuine gap in the *verification mechanism's own claimed coverage*, not necessarily evidence the shipped gate itself would be wrong (the Goal's own text points the engineer at reusing the already-correct, already-rightmost-anchored `:463` regex rather than writing a new one) — but it is exactly the class of defect full adversarial depth on a CI-wired differential harness exists to catch, and it blocks this freeze until closed.

2. **Minor (downgraded from Codex's raw `major`) — Domain-premise 1 (#266 warn vs. refuse): I would not decide differently, on a narrower reading of the evidence than the spec's own grounds fully establish.** Ground 1 (near-zero `--issue` usage — independently confirmed: exactly two occurrences reach `close-out.sh`, both inside `tests/close-out/run.sh`, zero in any adopter-facing doc) measures this repository's own test-authoring habits, not adopter usage, and the spec's own `## Domain premises` section already discloses exactly this bound in its own words ("that inference is not measured and cannot be") and pairs it with a re-evaluation trigger (a fourth recorded recurrence flips the decision to refuse, as a new task). Re-deriving the counter-argument independently: the three recorded recurrences (2026-07-28, 2026-08-13, 2026-08-29) all happened while **no disclosure at all** existed — they are evidence that silence fails, not evidence that WARN specifically fails, since WARN has never been tried. Read that way, the decision is a defensible, bounded one rather than a premise dressed up as settled fact. Not blocking: the mandate asks for an independent re-derivation and a plain statement of whether I'd decide differently, and my answer is no, for the reason above, which is narrower than (and partly a rebuttal of) the spec's own framing of the same evidence.
   - Residual noted, not new: the escalation trigger ("a fourth occurrence…") has no mechanical detector — a fourth occurrence must be discovered and reported exactly as the first three were. This is the same trusted-operator/disclosed-limit posture the spec's own `## Input space` already states for the adjacent "operator who does not read stdout" bullet, not a fresh gap.

3. **Info (matches Codex's own severity) — Domain-premise 2 (#53 allowed set = `{READY_FOR_MERGE}`): I would not decide differently.** Independently verified both citations byte-for-byte against the working tree: `agents/codex-reviewer.md:131` reads `APPROVE → set tasks/todo.md to READY_FOR_MERGE`; `docs/adopting.md:753` reads `A task is done only when the Codex reviewer sets \`READY_FOR_MERGE\`…`; `CONTRIBUTING.md:55` documents the `--issue N --pr N` close-out form the spec cites. The rejected alternative (`{READY_FOR_MERGE, READY_FOR_REVIEW}`) is correctly rejected — `READY_FOR_REVIEW` is precisely the premature state issue #53 exists to catch, and admitting it into the allowed set would gut the gate's own purpose. No finding.

4. **Minor (downgraded from Codex's raw `major`) — `## Blast radius`'s method vs. AC14/AC15.** Codex's raw finding conflated two distinct things and I reject the conflated half: the declared `- population: 112 spec files` figure is a **corpus-completeness bookkeeping total** (how many `.shell-team/specs/*.md` files exist at all, tracked plus untracked, so nothing is silently missed from the enumeration) — it is **not** a claim that the untracked T-1107 itself is diffed for Blast-radius purposes. `AC10`'s own text explicitly excludes "this spec's own file" from the merged-criteria population it actually runs, and `A-4` explicitly names the 112 figure as "not... a `bin/derive-populations.sh` derivation" at authoring time, to be corrected by the freeze run — the 111-tracked/1-untracked split is honest and internally consistent, not inflated. The part of Codex's finding I do **not** reject: `AC14`'s own check line is textual/count-based only (marker presence, the population-count equality, `- predicted-red:` vs. the count of `head: FAIL` rows) and never itself executes the two-arm diff or proves a specific `base: PASS`→`head: FAIL` transition — but the spec's own text is explicit that this is `AC15(c)`'s job, a `SKIP`-by-design, explicitly-narrated **runtime** report ("no command can prove a command was run"), matching this repository's own already-litigated `verification-ceiling` reporting-criterion class (T-1093, four cross-provider rounds). Not a hidden gap; already disclosed at the correct place.

5. **Minor (downgraded from Codex's raw `major`) — the `- base-ref-discriminator:` expression's three named residual cases.** Codex is right that the expression does not itself *fail* on a rebase, a squash-merge, or a fetch-depth gap for the predecessor branch — but the spec's own declaration explicitly names all three as disclosed, route-back-on-detection residuals rather than silent failure modes, and `AC15(d)`'s own reporting duty (measuring `git rev-parse` on both candidate refs and whether `git merge-base --is-ancestor` holds) is precisely the mechanism by which such a mismatch would surface at freeze/QA time. This is also byte-identical, in shape, to the two-arm pattern already frozen and previously reviewed in this task's own immediate predecessor specs (T-1092, T-1093) — T-1093's own cross-provider review scored an adjacent gap in the identical pattern as a shared-convention fast-follow rather than a defect specific to that task, and I apply the same reasoning here.

6. **Minor (downgraded from Codex's raw `major`) — `AC9`'s claimed suite-derivation vs. its hardcoded check line.** `AC9`'s prose states "The suite list is the one derived by reverse-mapping each edited path to the suites that read it, not a guessed set," but its check line runs exactly three items (`tests/close-out/run.sh`, `tests/check-oversight/run.sh`, `shellcheck`). `tests/errexit-safe/run.sh`, which Codex also flagged, is in fact **already** covered — by `AC6`'s own check line, not `AC9`'s — so that half is not a real gap, only a cross-reference a reader has to make themselves. `tests/trial-recipe/run.sh` is a genuine miss: it is CI-wired (`.github/workflows/check-handoff.yml:68-69`) and reads `docs/adopting.md` (one of the four files `AC7` edits) via `tests/trial-recipe/extract-recipe.sh`, and is named nowhere in this spec — neither run nor excluded with a stated reason. Independently verified this is low-risk in practice: every fenced (```` ``` ````-delimited) block in `docs/adopting.md` — the regions `trial-recipe` actually extracts and byte-compares against `docs/adopting.ja.md` — sits at or before line 386, well clear of the `:698-706` region `AC7`'s new prose section targets, so the addition is very unlikely to disturb the fence-parity assertion. Recorded as a cheap, non-blocking ask rather than a blocking Major.
   - Suggested fix: add `bash tests/trial-recipe/run.sh` to `AC9`'s check line (it is already fast), or name it explicitly as an accepted-unmeasured-but-low-risk exclusion, matching this spec's own disclosure convention used for the Blast-radius Class 1/2 rows.

7. **Minor (downgraded from Codex's raw `major`) — `AC4`/`AC7`/`AC8`'s literal/count checks vs. full behavioral proof.** For `AC7` and `AC8` this is not a hidden gap: the spec's own text explicitly names both as an intentional "static proxy… declared as one rather than dressed up," matching this repository's already-established, already-priced-in AC-literal-vs-behavior residual class (T-1093's four-round precedent). A narrower, not-yet-disclosed miss specific to `AC4`: the prose promises "stdout carries **a single line**" for the no-`--issue` and empty-`--issue` cases, but the check line never asserts line-count singularity — only substring presence/absence. Low materiality: the existing negative checks (absence of `gh issue close` in the no-issue cases, absence of the note prefix in the `--issue`-present case) already rule out the main confusion risk a second, contradictory line could cause.

#### Rejected sub-finding (Codex, disagreed)
- Within finding 4 above: Codex's specific claim that the declared population figure "cannot obtain its base blob" for the untracked T-1107 conflates the corpus-completeness bookkeeping total with the merged-criteria population actually diffed for Blast-radius purposes — the latter explicitly excludes this spec's own file (`AC10`), so no base-blob read is ever attempted or needed for it. Rejected as a misread of the spec's own two-population distinction, not a real gap.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Domain-premise 1 (#266 warn vs. refuse) — would you decide differently | Codex: major | minor | spec already discloses the ground-1 inference bound and pairs it with a re-evaluation trigger; the three recorded recurrences are evidence silence fails, not that WARN fails, since WARN was never tried |
| 2 | Blast radius method vs. AC14/AC15 (population figure + AC14 not itself executing the diff) | Codex: major | minor (one sub-claim rejected outright) | the untracked-file/base-blob sub-claim is a misread of the bookkeeping-total-vs-diffed-population distinction (rejected); the remaining "AC14 doesn't execute the diff itself" observation is already honestly disclosed as AC15(c)'s SKIP-by-design reporting duty |
| 3 | base-ref-discriminator does not fail on rebase/squash/fetch-gap | Codex: major | minor | all three cases are explicitly named, disclosed, route-back-on-detection residuals in the spec's own text, and AC15(d)'s reporting duty is exactly the detection mechanism; byte-identical pattern already reviewed the same way in this task's own predecessor specs (T-1092/T-1093) |
| 4 | AC9's suite list vs. its "derived, not guessed" claim | Codex: major | minor | errexit-safe is a false alarm (already covered by AC6); trial-recipe.sh is a genuine, CI-wired, unexcluded miss, but independently verified low-risk since its fence-parity extraction targets regions well clear of this task's edit site |
| 5 | AC4/AC7/AC8 test literal/count presence, not full behavior | Codex: major | minor | AC7/AC8's static-proxy nature is already explicitly self-disclosed and matches this repo's established AC-literal-vs-behavior residual class; AC4's "single line" claim is a narrower, undisclosed but low-materiality gap given its existing negative checks |
| 6 | verification-ceiling declaration's "including AC15" scope claim vs. A-1/A-2's gh-dependent freeze-run duties | Codex: major | minor | confined to AC15's own reporting-duty scope claim, not to any of the 14 real machine-checked acceptance gates, which stay genuinely reproducible offline; the gh-dependent duties are already explicitly marked RELAYED and delegated to a network-capable role |

Note: findings 3–6 above (Codex's original items covering `Goal/base-ref-discriminator`, `AC9`, `AC4/AC7/AC8`, and `Goal declaration/verification-ceiling`) are folded into ledger rows 3–6 respectively.

#### Tamper-arm classification — independently re-derived, no correction owed
Read `docs/loop-engineering/record-tamper-resistance.md` end to end (all five prior promotions, `## What decides the arm, and what never does`, the re-evaluation trigger). I concur with the spec's own adjudication (reading (ii), narrowing): each of the five prior promotions (`#341`/`#344`, T-1099, T-1103 duty A, T-1104 duty A) introduced a judgment absent from any existing obligation entirely (a genuinely new fact to check). T-1107's obligation A instead narrows *which values* of the *same already-checked datum* (the flag token, at the identical venue, gating the identical transition) are acceptable — a predicate-width narrowing on an identical judgment, not a new kind of judgment. No correction to that document is owed, and `AC12`'s exclusion of `docs/loop-engineering/` from the changed-and-added set is correct as written.

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (per the spec-review-mode Route-back paragraph: this task's `- entry-mode:` board hand-off field reads `pm-authored`, cross-checked against the Routing Map's own printed `dispatch: specify — pm-authored — …` decision on the same board entry — the two sources agree, so the route-back target is unambiguous).
- Ask: close finding 1 (Major — the decoy corpus line does not actually discriminate a first-match extraction bug from a correct rightmost-slot one) before the freeze proceeds, by strengthening the existing decoy row or its assertion per the suggested fix above.
- Non-blocking, cheap to fold into the same revision: name `tests/trial-recipe/run.sh` in `AC9` (finding 6/ledger row 4) and narrow the verification-ceiling declaration's "including AC15" sentence to exclude the gh-dependent Assumption-remeasurement sub-items (ledger row 6). Neither blocks this round on its own.
- This verdict gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- Finding 2 / ledger row 1 (Domain-premise 1 escalation trigger has no mechanical detector): won't-fix — the same disclosed, trusted-operator residual the spec's own `## Input space` already names for an adjacent bullet; recording it here is sufficient.
- Ledger row 2 (AC14/AC15's reporting-only proof of base→head transitions): won't-fix — already correctly scoped to AC15(c)'s own SKIP-by-design narrated report, matching this repo's established verification-ceiling reporting-criterion class.
- Ledger row 3 (base-ref-discriminator residual cases): won't-fix for this task specifically — the same disclosed-residual class T-1093's own review already flagged as a shared-convention fast-follow across every spec using this pattern (at minimum T-1091, T-1092, T-1093, now T-1107); recommend folding this narrower rebase/squash-detection observation into that same shared-convention follow-up rather than opening a second one.
- Ledger row 4 (AC9/trial-recipe): file-an-issue is not warranted — this is a one-line addition to an already-existing check line, cheap enough to fold into the same revision that closes the Major finding above, not a separate tracked item.
- Ledger row 5 (AC4's "single line" phrasing): won't-fix — low materiality given the existing negative-absence checks; AC7/AC8's static-proxy nature is already accepted repository-wide.
- Ledger row 6 (verification-ceiling "including AC15" scope claim): won't-fix as a separately tracked item — cheap wording narrowing pm-spec may fold into the same revision that addresses the Major finding.

Raw Codex output: `.shell-team/reviews/T-1107-codex-specreview.txt` (final message), `.shell-team/reviews/T-1107-codex-specreview.jsonl` (full event stream).

- executor-invocation (specreview): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Review the spec document at .shell-team/specs/T-1107-close-out-gates.md for its DOMAIN premises, not its prose polish. Read the file yourself -- do not expect its text in this prompt. Judge (1) whether its correctness rests on a fact this repository can verify rather than an unverifiable assumption about deployment order, a rollback precondition, or a system outside this repository's reach, and (2) whether its acceptance criteria test the right thing rather than merely testable things. Focus especially on the two domain premises recorded in the spec's own '## Domain premises' section: Premise 1 (issue #266 takes the WARN shape rather than the REFUSE shape, on four measured grounds about adopter usage this repository cannot fully observe) and Premise 2 (the #53 gate's allowed set is exactly {READY_FOR_MERGE}, a breaking change accepted deliberately). For each premise, independently re-derive the conclusion from the recorded grounds rather than merely checking internal consistency, and state plainly whether you would decide differently and on what measured ground. Also assess whether the '## Blast radius' full-population-diff method and the '## Acceptance criteria' AC1-AC15 test the actual behavior described in the Goal rather than a convenient proxy, and whether the declaration region (user-visible/verification-class/base-ref-discriminator/verification-ceiling in the '## Goal' section) is honest against the spec's own content. Return findings as a JSON array of {severity, criterion, issue, suggestion}." > <RAW_JSONL> 2>&1
- pass-role (specreview): generation
- briefing-fidelity (specreview): carried - both domain premises, the reviewer-scope's blast-radius/AC-honesty axis and the declaration-region axis, and the spec's own file path were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (specreview): T-1107-codex-specreview

---

### Codex Spec-Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1107
- Codex model: gpt-5.6-luna — re-measured fresh from `~/.codex/config.toml`'s `model = ` line this round (unchanged from round 1); this pass's JSONL event stream again carries no explicit model-identifier field, so the config-file value stays the primary source; no `-m` override passed.
- Codex CLI: codex-cli 0.149.1 — re-measured fresh via `codex --version` this round (unchanged).
- Mode: Spec-review (specify seam, T-1092), round 2
- Consumer class: unchanged from round 1 — full adversarial depth, dev-scaffold re-pricing does not apply.
- Input reviewed: `.shell-team/specs/T-1107-close-out-gates.md`, current working-tree state (still uncommitted; pm-spec's round-2 corrections in place, orchestrator re-ran the freeze sweep: unchanged 5P/9F/1S, same implementation-owed classification). Re-read in full the changed regions: `AC3` (now five ways, item (v) added), `AC4` (exactly-one-line clause), `AC9` (now runs `tests/trial-recipe/run.sh`), `AC15(a)` mutation 3 (rewritten to target the two new divergent rows), the `- verification-ceiling:` declaration (narrowed), `## Open questions` (item 2, the rejected token-asserting-oracle alternative), `## Domain premises` (Premise 1's counter-reading), `## Reviewer scope` item (i) (re-aimed at divergence), and `## Notes for engineer` (trap 6, ja-fence-parity). No `--base`, no branch diff — still pre-implementation.

#### Round-1 Major — genuinely closed
Independently traced the actual mechanism rather than accepting the author's claim: `tests/close-out/run.sh`'s `lineshape_case`/`ls_mismatches` logic (`:1019-1067`, unchanged this round) compares only the exit-code-level accept/refuse verdict. The new `AC3(v)` requires two rows whose first and rightmost backticked tokens are swapped between `READY_FOR_MERGE` and a different allowed flag — Row A (first=`MERGE`, rightmost=non-`MERGE`: a first-match bug **falsely accepts**, correct rightmost-slot **correctly refuses**) and Row B (the mirror: a first-match bug **falsely refuses** a legitimate close-out, correct rightmost-slot **correctly accepts**). Traced both directions by hand: for Row A the composed oracle (check-handoff.sh clean + flag≠MERGE) expects refuse, so a buggy accept is a genuine, catchable divergence; for Row B the oracle expects accept, so a buggy refuse is equally catchable. `AC15(a)` mutation 3's rewritten text cites the harness's own two actual printf strings (`tests/close-out/run.sh:1059`, `:1065` — independently re-confirmed present, byte-accurate), and the rejected alternative (asserting the specific stderr-named flag token rather than accept/refuse) is recorded in `## Open questions` item 2 with a sound, stated reason (message-wording coupling would redden the harness on an unrelated reword — a real engineering tradeoff, not a dodge). A second, independent Codex pass this round (fresh prompt, reading the current file bytes directly) reached the identical conclusion unprompted. **Confirmed closed**, both by my own trace and by an independent second pass.

One residual, correctly scored Minor and not reopening the Major: `AC3(v)`'s own check line (`spec:83`) establishes token-ordering-somewhere-on-a-line via two whole-file `grep -E` patterns, not "exactly two distinct, unambiguous rows" — a sufficiently contrived single line (reusing the old `:997` two-separator trick) could in principle satisfy both greps without the engineer having authored two genuinely-shaped rows. This is squarely the class the spec's own frozen `## Reviewer scope` already prices out of Major severity ("adversarial guard-completeness of this spec's own inline check lines... arm-B instruments whose subject behaviour is covered under arm A"): the *actual* discriminating power is `AC15(a)` mutation 3's runtime probe, which the engineer will run against whatever they actually wrote, not against this static check's own laxity. Won't-fix.

#### New finding — Major (the same-shape defect round 1 found, recurring at a different scale: the pre-existing suite's OWN fixtures directly assert the pre-#53 behavior, and the spec's engineer guidance does not disclose this)
A second, independent Codex pass — asked to ground every claim against the real current bytes of `bin/close-out.sh`, `tests/close-out/run.sh`, `tests/check-oversight/run.sh` and `tests/trial-recipe/run.sh` — surfaced a gap neither pm-spec's round-2 edit nor my own round-1 review had caught: `## Notes for engineer`'s "fixture-residency trap" (trap 3) names only the two **committed fixture files** (`tests/close-out/fixtures/todo-legacy.md`, `todo-default.md`) as needing re-flagging to `READY_FOR_MERGE`. It says nothing about the dozens of **inline, in-suite-synthesized** boards scattered through `tests/close-out/run.sh`'s own body that hardcode `READY_FOR_QA` (44 occurrences total) as an incidental "some valid flag" choice while testing an unrelated property — move mechanics, interventions-record handling, env-var overrides, malformed-line interlocks — and currently expect the close-out to **succeed**.

Independently traced the codebase myself, past the interventions-check → source-line-gate → [new gate] → Done-entry-build pipeline order (interventions/dependency/disposition-gate failures at `:353-452` all fire *before* the source-line gate at `:600-642`, which fires before the new gate this task inserts, which fires before the pre-write interlock at `:678`), to separate confirmed breaks from cases that already refuse earlier for an unrelated reason and are therefore unaffected. **Confirmed, concrete breaks** (board is otherwise "clean" — conformant interventions record, valid-vocabulary flag, so it reaches the new gate — and the test currently asserts success or a specific *other* refusal reason that the new gate would now preempt):
- `tests/close-out/run.sh:333` (M1), `:360` (M2), `:379` (M3), `:395` (M4) — four move-mechanics fixtures, each asserting `close-out should exit 0`.
- `:450` (`interventions-happy-path-unchanged`) — asserts "should still exit 0."
- `:664` (`interventions-env-override-precedence`, first invocation) — asserts "must let close-out succeed."
- `:685` (`interventions-legacy-layout-resolution`) — asserts "must close the task out."
- `:1115` (`closeout-done-section-loose-entry-unaffected`) — asserts exit `0` and `T-901` moving to Done.
- `:866-871` (`closeout-sourceline-flag-vocabulary-positive-control`) — the single most direct instance: a `for f in READY_FOR_ARCH READY_FOR_ENG READY_FOR_QA READY_FOR_REVIEW READY_FOR_MERGE BLOCKED REWORK` loop **explicitly asserts each of the seven allowed flags closes out with exit `0`** — the literal, pre-existing, currently-green assertion of the exact behavior this task exists to invalidate. This one cannot be "re-flagged" at all; it needs restructuring (e.g., splitting into a `READY_FOR_MERGE`-only success case plus a six-flag refusal case, which is essentially what this task's own new `AC1` already re-derives — the two overlap and should likely be reconciled, not independently maintained).
- A subtler, distinct break at `:1100` (`closeout-unrelated-active-line-interlock-unchanged`): T-901's own line here is conformant and valid-vocabulary (`READY_FOR_QA`), so it reaches the new gate *before* ever reaching the pre-write interlock at `bin/close-out.sh:678` this test is named for. The test still exits `1` (matching), but its specific stderr assertion (`grep -qF -- 'rewritten board would fail check-handoff.sh'`) would no longer be satisfied — the new gate's own distinct message fires first and masks the interlock's reason, the same distinguishability concern `AC2` already defends for one axis (malformed vs. premature) recurring, unaddressed, on this other axis (unrelated-line-interlock vs. new-gate).

This is **not** a hole in the shipped mechanism's correctness (`AC9`, as a mechanical gate, would legitimately go red on an unfixed suite — the mechanism catches this) and it is **not** the same failure mode as round 1's Major (that was about a self-check's discriminating power; this is about the spec's own task-sizing and engineer guidance materially understating what "green suite" requires). It matters because: (a) the size of the true remediation is an order of magnitude larger than "re-flag two files," risking rework rounds the engineer could have avoided with an accurate map; (b) the one loop that directly contradicts this task's purpose (`:866-871`) needs a **design decision** (restructure vs. fold into `AC1`), not a mechanical re-flag, and that decision belongs at spec time, not left for the engineer to invent under review pressure; and (c) the masked-interlock-reason case at `:1100` is exactly the kind of distinguishability regression `AC2` was written to prevent on a different axis, recurring here unnoticed.
- Suggested fix: add a "Notes for engineer" item (or expand trap 3) that (i) states plainly that `tests/close-out/run.sh` itself, not only its two fixture files, carries fixtures assuming a non-`READY_FOR_MERGE` flag succeeds, and directs the engineer to inventory every close-out invocation in that file currently expecting exit `0` against a conformant/valid-vocabulary board (not just the two named files); (ii) makes an explicit call on `:866-871`'s flag-vocabulary-positive-control loop — most naturally, narrow it to assert only that `READY_FOR_MERGE` succeeds while the other six now refuse via the new gate (overlapping, and ideally reconciled, with the new `AC1`), rather than leaving its disposition to be improvised; (iii) names the `:1100` interlock-masking case explicitly, since it is exactly the "two refusal reasons must stay distinguishable" concern `AC2` already treats as important, recurring on a different pair of gates.

#### Ceiling narrowing, AC4, AC9/trial-recipe fixes — confirmed sound, one wording correction owed (unchanged severity: minor)
Independently confirmed the `- verification-ceiling:` narrowing is sound: `AC15(d)` already covers the local/static sub-parts (branch name, `git rev-parse`, `merge-base`, ancestry), and only `A-1`'s issue-state and `A-2`'s PR-state re-measurements are delegated to the network-capable coordinating session — no acceptance verdict turns on `AC15`, so the ceiling stays honest. `AC4`'s exactly-one-line clause is coherent and closes my own round-1 Minor cleanly. `AC9` now running `tests/trial-recipe/run.sh` closes that gap — but the accompanying `## Notes for engineer` trap 6 **overstates** the actual risk: independently traced `tests/trial-recipe/extract-recipe.sh` and confirmed it scans **only** the bounded `## Trying the team on one ticket` section (`docs/adopting.md:320`–`361`, ending at the next `## ` heading), while `AC7`'s planned new section sits at `:698-706`, under `## Choosing an oversight profile` — well outside the scanned section. A fenced block added at the planned edit site would be **invisible** to this check regardless of language-parity, so trap 6's warning as worded ("If the new documentation section adds a fenced code block...") could send the engineer chasing a risk that does not exist at the planned location. Minor, non-blocking: narrow trap 6's wording to name the bounded section explicitly, or drop the warning since the planned edit site is safely outside it (matches, and slightly sharpens, my own round-1 low-risk assessment).

#### Other minor findings (Codex, this round; none change the overall verdict)
- `AC9`'s suite list still omits `tests/check-readme-version/run.sh` (CI-wired, reads `README.md`/`README.ja.md` — two of `AC7`'s four edited files, via its own dogfood step). Confirmed present and CI-wired. Materiality is low: that check only compares a version badge against `plugin.json`, a token `AC7`'s prose addition would not plausibly touch — won't-fix, matching the same low-risk reasoning I applied to `trial-recipe` in round 1 before it was added.
- `AC15(e)`'s freeze-run remeasurement commands for `A-1`/`A-2` don't capture everything the Assumptions rows describe (issue-body comparison, CI-conclusion data) — a fair observation about `AC15`'s own reporting thoroughness, but `AC15` is `SKIP`-by-design and no acceptance verdict turns on it; won't-fix, consistent with round 1's treatment of adjacent `AC15` concerns.

#### Synthesis audit ledger (round 2)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Major (decoy-line discrimination) | Codex round-2 pass: info/closed | closed | independently traced both directions of the fix by hand plus confirmed by a second, independent Codex pass — genuinely closed |
| 2 | `AC3(v)` check line's own static laxity (single contrived line could satisfy both greps) | Codex: minor | minor (unchanged) | matches the spec's own frozen carve-out for adversarial completeness of its inline check lines; real discriminating power sits in `AC15(a)`'s runtime probe, not this static check |
| 3 | `tests/close-out/run.sh`'s own inline fixtures (beyond the two committed files) assert the pre-#53 behavior, undisclosed in engineer guidance | Codex: major | major | independently confirmed at 8+ distinct, concrete assertions across 7 named test blocks, including the direct `:866-871` seven-flags-succeed loop and a subtler stderr-masking case at `:1100`; blocks this round |
| 4 | `## Notes for engineer` trap 6 overstates `trial-recipe`'s actual scan scope | Codex: minor | minor (unchanged) | independently confirmed the scanned section (`docs/adopting.md:320-361`) does not reach the planned edit site (`:698-706`); wording correction only, no gate affected |
| 5 | `AC9` also omits `tests/check-readme-version/run.sh` | Codex: minor | minor (unchanged) | confirmed CI-wired and reads two AC7-edited files, but the check tests only a version-badge token AC7's edit would not plausibly touch — low materiality, won't-fix |
| 6 | `AC15(e)`'s remeasurement commands don't capture every premise the Assumptions rows describe | Codex: minor | minor (unchanged) | `AC15` is SKIP-by-design and gates nothing; consistent with round 1's treatment |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (same cross-check as round 1: `- entry-mode: pm-authored` agrees with the Routing Map's printed `dispatch: specify — pm-authored` decision on the same board entry).
- Ask: close finding 3 above (the undisclosed, much larger true scope of `tests/close-out/run.sh`'s own fixture remediation, and an explicit disposition for the `:866-871` flag-vocabulary-positive-control loop) before the freeze proceeds. Round-1's Major is genuinely closed and does not need to be revisited.
- This verdict gates the freeze only; it is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.
- Pre-commitment status: not applicable to this axis — the spec's own `## Pre-commitment` governs engineering-round defects on a delivered change, not spec-review rounds before implementation exists; not invoked here.

#### Fast-follow disposition
- Ledger row 2 (`AC3(v)` check-line static laxity): won't-fix — same disclosed, spec-accepted arm-B-instrument-completeness carve-out as round 1.
- Ledger row 4 (trap 6 wording overstatement): won't-fix as a separately tracked item — cheap wording correction pm-spec may fold into the same revision that addresses finding 3.
- Ledger row 5 (`AC9`/`check-readme-version` omission): won't-fix — low materiality, matches round 1's disposition of the analogous `trial-recipe` gap before it was fixed.
- Ledger row 6 (`AC15(e)` remeasurement completeness): won't-fix — `AC15` is SKIP-by-design; no acceptance verdict depends on it.

Raw Codex output (round 2): `.shell-team/reviews/T-1107-codex-specreview-r2.txt` (final message), `.shell-team/reviews/T-1107-codex-specreview-r2.jsonl` (full event stream). Round 1's canonical stem (`T-1107-codex-specreview.{txt,jsonl}`) is preserved above per the naming-consistency rule.

- executor-invocation (specreview-r2): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Round 2 of a cross-provider spec review, before any implementation exists. Re-read .shell-team/specs/T-1107-close-out-gates.md yourself -- do not expect its text pasted here. Round-1 scope: I (a prior Codex pass) had found the differential harness's decoy corpus line (tests/close-out/run.sh:997) does not actually distinguish a first-match flag-extraction bug from a correct rightmost-slot one under the new #53 gate, because the harness's mismatch counter only compares accept/refuse exit-code-level verdicts, and both a buggy and a correct extraction happen to produce 'refuse' for that specific decoy line's values. The author's fix, now in the spec: a new AC3(v) requires TWO divergent corpus rows -- Row A (first backticked token READY_FOR_MERGE, rightmost slot a different, non-MERGE allowed flag: first-match accepts, rightmost-slot refuses) and Row B (the mirror). AC3(v) explicitly states the old :997 row is NOT such a shape. AC15(a) mutation 3 was rewritten to target these two new rows specifically, citing the harness's own two actual printf MISMATCH message strings, and adds an explicit presence-vs-falsifiability honesty clause. A rejected alternative (token-asserting oracle) is recorded in a new Open questions item 2 with a stated coupling reason. Separately, three smaller fixes: ceiling narrowing delegating AC15(e)'s gh-dependent remeasurement to the coordinating session; AC4's exactly-one-line clause; AC9 now also running tests/trial-recipe/run.sh with a new engineer trap about ja-fence-parity. Please verify, independently and adversarially: (1) is AC3(v)'s two-row design genuinely sufficient to discriminate a first-match bug -- read the actual check line and harness code yourself; (2) does this fix or any of the three smaller fixes introduce ANY new gap, inconsistency or false claim -- read the actual current bytes of bin/close-out.sh, tests/close-out/run.sh, tests/check-oversight/run.sh and tests/trial-recipe/run.sh yourself; (3) is the ceiling narrowing's reasoning sound; (4) is there a fourth, independent, previously-unseen gap in the spec's domain premises or acceptance criteria that a fresh read surfaces. Return findings as a JSON array of {severity, criterion, issue, suggestion}. If round-1's Major is genuinely and fully closed with no new gap, say so plainly in at least one array entry." > <RAW_JSONL> 2>&1
- pass-role (specreview-r2): confirmation
- briefing-fidelity (specreview-r2): carried - round-1's specific finding, the author's exact fix shape (AC3(v)'s two rows, the rewritten AC15(a) mutation 3, the rejected alternative), and all three smaller fixes were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (specreview-r2): T-1107-codex-specreview-r2

---

### Codex Spec-Review verdict: REQUEST_CHANGES (round 3)
- Task: T-1107
- Codex model: gpt-5.6-luna — re-measured fresh from `~/.codex/config.toml`'s `model = ` line this round (unchanged); JSONL again carries no model-identifier field, config value stays primary. No `-m` override.
- Codex CLI: codex-cli 0.149.1 — re-measured fresh via `codex --version` this round (unchanged).
- Mode: Spec-review (specify seam, T-1092), round 3
- Consumer class: unchanged — full adversarial depth, dev-scaffold re-pricing does not apply.
- Input reviewed: `.shell-team/specs/T-1107-close-out-gates.md`, current working-tree state (still uncommitted; pm-spec's round-3 correction under the orchestrator's stated depth-3 constraint — zero new mechanisms, zero new claims, primary-sources-first — in place; sweep unchanged, dry-run 0 unrecognized). Re-read in full the two edited regions (`## Notes for engineer` trap 3's replacement — the gate-order classification rule plus its ten-row table — and trap 6's self-correction) plus `AC1`'s amended prose (check line untouched). No `--base`, no branch diff — still pre-implementation.

#### (i) Is the ten-row table complete against my round-2 enumeration? — Yes against what I wrote down; no against the actual file
Against the specific ten sites my own round-2 record named, the table is complete and each per-site conversion decision is correctly grounded in that block's own read purpose — I re-verified the cited line numbers and gate-order claims (`bin/close-out.sh:458` `MAIN_LINE=`, `:464` the rewrite's `BASH_REMATCH[1]`, `:492` the spec-review election gate, `:541` `OVERSIGHT_CHECKER=`, `:642` `esac`, `:678` the pre-write interlock message) and all are byte-accurate. But re-reading the **entire** file myself this round (not only the ten rows), I found **five more** genuine sites the table does not name, none of which fall into either bucket its own dismissal claims covers ("most hits are in the `LS_LINES` differential corpus... or in refusal fixtures where the flag is deliberately malformed"):
- `tests/close-out/run.sh:415` (`M5/bad.md`, `move-refuses-stranded-board`) — conformant record, `READY_FOR_QA`, currently expects exit `1` **for a different reason** (a stranded continuation line, `grep -qF -- 'stranded continuation line'`). Since the new gate at `:642` fires before the interlock that produces that message, the new gate's own refusal would preempt and mask it — the identical masking pattern the table's own `:1100` row already names, recurring here unaddressed.
- `:431` (`M5/good.md`, the same test's positive control) — expects exit `0`; would flip to exit `1`.
- `:810-812` (`closeout-handoff-sibling-positive-control`) — `$HS_ROOT`'s board (set up at `:767`, conformant record, `READY_FOR_QA`) expects exit `0`-and-moved.
- `:845-846` (a standalone `sl_case whitespace-title-good`, **not** part of the `:867-871` loop the table already addresses) — same shape, expects exit `0`-and-moved.
- `:931-934` (`closeout-sourceline-stub-zero-positive-control`) — `$STUB_SL_ROOT`'s board (set up at `:903`, conformant record, `READY_FOR_QA`) expects exit `0`-and-moved.

A second, independent Codex pass this round — given my five sites and asked whether a sixth exists — confirmed all five as real and found **two more** on its own: `T-150` (`tests/close-out/run.sh:239`, injected `READY_FOR_REVIEW`, exercised successfully at `:270-276` — the "resolved fast-follow disposition lets close-out succeed" positive control) and `T-151` (`:295`, injected `READY_FOR_REVIEW`, exercised successfully at `:307-309` — the "prose quoting the anchor does not false-positive-block close-out" regression lock). I independently re-confirmed both: both boards carry conformant interventions records, both flags are valid-vocabulary-but-non-`MERGE`, both currently expect success, neither is named anywhere in the table. **Total confirmed sites beyond the ten-row table: seven**, found by two independent readers in one round, with neither reader's pass claiming exhaustiveness.

This is a Major finding, not because the mechanism is unsound (`AC9` still mechanically catches an unfixed suite) but because **this is now the third consecutive spec-review round to find a genuine, previously-missed gap in this exact "make the suite's fixtures line up with the new gate" effort** — round 2 found the effort was needed at all (beyond the two committed files); round 3's own fix, produced under careful, purpose-read, per-site classification, is itself incomplete by at least seven sites on the very first re-read. Two independent full re-reads (mine and a fresh Codex pass) in the same round each surfaced sites the other had not found, which is a strong signal that hand-enumeration of this population is not converging through further review rounds alone.
- Ask: either (a) extend the table with these seven additional rows (and continue the same careful per-site read-then-convert discipline that produced the first ten), or (b) — my stronger recommendation, independently reached by both readers this round — invert the strategy: default every incidental "some valid flag" choice in `tests/close-out/run.sh` to `READY_FOR_MERGE`, and enumerate only the much smaller, more tractable **exception roster** that must intentionally keep a non-`READY_FOR_MERGE` flag: the `LS_LINES` differential corpus, the `sl_case` malformed/invalid-flag tests, the `:867-872` seven-flag loop, and `todo-legacy.md:14`'s (`T-101`) own residency fixture. A default-flip structurally removes the omission class this and the prior round both drew from, rather than asking a fourth round to find an eighth site.

#### (ii) Is the `:867-871`/`:867-872` conversion decision sound? — Yes, confirmed by both readers; no break named
Independently traced both directions (accept-`MERGE`, refuse-the-other-six-without-either-source-line-literal) and confirm the design is purpose-preserving: it keeps proving the source-line gate accepts all seven vocabulary members while making the per-flag refusal reason attributable to the new gate specifically, with no overlap risk against the two source-line-gate literals in practice (this fixture's board is otherwise clean, so no other gate can produce the refusal). The second Codex pass reached the identical conclusion independently and named no break. One small, non-blocking correction: the loop's actual line bounds, re-measured, are `:867-872` (Codex's own count), not `:866-871` as the spec's citation reads — a one-line-off anchor, worth fixing but affecting no verdict.

#### (iii) Does the disclosed-not-mechanized limit sit within the arm-B carve-out? — No, on independent agreement with the second Codex pass
The spec's frozen `## Reviewer scope` carve-out reads: "adversarial guard-completeness of this spec's own inline `- check:` lines against hypothetical defeats... those lines are arm-B instruments whose subject is a `bin/` script's own behaviour, and the behaviour itself is covered under arm A by `tests/close-out/run.sh`." That text is scoped to **this spec's own** inline check lines, on the premise that the shipped suite (arm A) is the trustworthy backstop behind them. The disclosed gap here is a different thing: a hole **in that same backstop's own self-protection** (nothing catches a future, unrelated edit silently deleting the `:867-872` loop; `AC9` stays green regardless, since it only asserts the suite exits `0`, never that a specific assertion still exists). The carve-out's own reasoning does not extend to excusing a gap in the thing it treats as trustworthy. A second, independent Codex pass reached the identical reading. I upgrade this from a Minor/fast-follow (my own initial instinct, given the round's stated zero-new-mechanism constraint) to a **Major**, agreeing with the independent pass: the fix is cheap and does not require a new script or gate — one additional line inside `AC3` or `AC9`'s existing check (a `grep`/`awk` count confirming the loop and both its `MERGE`-success and non-`MERGE`-refusal branches still exist) is a `- check:` line, not a new mechanism, so it should fit inside the round's own stated constraint rather than conflict with it.

#### Minor finding (this round, Codex, independently corroborated)
- `## Notes for engineer` trap 3's own gate-order classification rule ("the disposition, dispatch and interventions gates (`:353-455`)... and the source-line gate (`:610-642`) all fire before the new gate") omits two pre-`:642` gates I independently confirmed present: the `T-1104` review-input gate (`check-review-input.sh`, `bin/close-out.sh:579-597`) and the sibling screen at `:599-608`. This changes no site's classification (neither gate's own refusal fixtures use a non-`MERGE` flag in a way that interacts with any site named above), but the rule's own text claims to be an exhaustive gate-order enumeration and currently is not — worth completing so a future reader trusts the rule at face value rather than re-deriving it.

#### Pattern note (not a pre-commitment trigger — this axis is spec-review, not delivered-change review — but recorded transparently, per the same convention T-1093's own round 3 record used)
Three consecutive spec-review rounds have each landed an independent, genuine Major on components of this one task: round 1 on the differential harness's own discriminating power; round 2 on the suite's fixture-scope understatement; round 3 (this round) on that same round-2 fix's own incompleteness, found independently twice in one round. This spec's own `## Pre-commitment` section governs delivered-change review rounds, not spec-review rounds before implementation exists, so it does not fire here and I do not invoke it. But the pattern itself is worth surfacing plainly, exactly as T-1093's own round-3 record did for an analogous three-in-a-row: if a round 4 also lands an independent Major on this same "suite fixtures vs. the new gate" component, the orchestrator should weigh whether continued incremental review is the right instrument at all, versus adopting the structural default-flip recommended above before the next round.

#### Synthesis audit ledger (round 3)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | ten-row table omits seven more real sites (M5 bad/good, HS positive, whitespace-title-good, stub-zero-positive, T-150, T-151) | Codex: major | major | independently confirmed all seven by direct read; two independent full re-reads this round each found sites the other missed — recommend a structural default-flip over a fourth incremental patch |
| 2 | `:867-871`/`:867-872` seven-flag loop conversion design | Codex: info/sound | sound (no change) | both readers independently traced the design and found no break; only a one-line citation-bounds correction owed |
| 3 | loop-survival gap ("disclosed rather than mechanized") vs. the arm-B carve-out | Codex: major | major (upgraded from my own initial Minor) | the carve-out is textually scoped to this spec's own inline check lines, not to a gap in the shipped suite's own self-protection; both readers agree; the fix is a cheap check-line addition, not a new mechanism |
| 4 | gate-order classification rule omits the T-1104 review-input gate and sibling screen (pre-`:642`) | Codex: minor | minor (unchanged) | changes no site's classification, but the rule's own claim to exhaustiveness is currently false |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (same cross-check as rounds 1-2: `- entry-mode: pm-authored` agrees with the Routing Map's printed `dispatch: specify — pm-authored` decision on the same board entry).
- Ask: close finding 1 (the seven additional sites, and the recurring-omission pattern behind them — strongly recommend the default-flip strategy over a fourth incremental table update) and finding 3 (a cheap, non-vacuous check-line assertion that the seven-flag loop and both its branches survive) before the freeze proceeds. Findings 2 and 4 do not block.
- This verdict gates the freeze only; it is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.
- Pre-commitment status: not applicable — this axis is spec-review, before implementation exists, not a delivered-change review round; see the Pattern note above for the reasoning and the explicit warning for a hypothetical round 4.

#### Fast-follow disposition
- Finding 2 (loop citation bounds `:866-871` vs. measured `:867-872`): won't-fix as a separately tracked item — cheap wording correction pm-spec may fold into the same revision that addresses findings 1 and 3.
- Finding 4 (gate-order rule omits two pre-`:642` gates): won't-fix as a separately tracked item — same rationale; a cheap completion of an already-present list, not a new claim.

Raw Codex output (round 3): `.shell-team/reviews/T-1107-codex-specreview-r3.txt` (final message), `.shell-team/reviews/T-1107-codex-specreview-r3.jsonl` (full event stream). Rounds 1-2's canonical stems are preserved above per the naming-consistency rule.

- executor-invocation (specreview-r3): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Round 3 of a cross-provider spec review, before any implementation exists. Re-read .shell-team/specs/T-1107-close-out-gates.md yourself (the '## Notes for engineer' section, trap 3's ten-row table and trap 6's correction, plus AC1's amended prose) -- do not expect its text pasted here. Context: round 2 found that tests/close-out/run.sh (44 occurrences of READY_FOR_QA used as an incidental 'some valid flag' choice) has dozens of inline fixtures that will break once T-1107's new #53 gate lands, beyond the two committed fixture files. pm-spec's round-3 fix replaced trap 3 with a gate-order classification rule plus a ten-row table naming sites and per-site conversion decisions (M1-M4 move-mechanics, IH, OV1/OV2, LL, the seven-flags-succeed loop converted to a per-flag discriminator, UAL re-flagging only T-901's line, DSL). One limit is explicitly disclosed rather than mechanized: no check line asserts the loop's continued existence. I independently re-read the ENTIRE file myself and found what looks like at least five more sites the table does not cover: (1) :415 (M5/bad.md, stranded-board refusal masked by the new gate firing first); (2) :431 (M5/good.md, positive control flips); (3) :810-812 (handoff-sibling-positive-control); (4) :845-846 (standalone sl_case whitespace-title-good); (5) :931-934 (stub-zero-positive-control). Please independently verify, reading the actual current bytes yourself: (1) are these five sites real and do they genuinely reach the new gate; (2) is there a SIXTH site anywhere in the file I've also missed; (3) is the seven-flag loop conversion design sound or does it break something concrete; (4) is 'no check asserts the loop's survival, disclosed rather than mechanized' actually within the spec's own frozen carve-out for adversarial completeness of this spec's own inline check lines, or is that carve-out scoped only to this spec's check lines rather than to gaps in the shipped suite's own self-protection; (5) given this is now the third consecutive spec-review round to find a genuine gap in this exact effort, is a structural default-flip (READY_FOR_MERGE everywhere except an enumerated exception roster) worth recommending over continuing to patch one row at a time. Return findings as a JSON array of {severity, criterion, issue, suggestion}." > <RAW_JSONL> 2>&1
- pass-role (specreview-r3): confirmation
- briefing-fidelity (specreview-r3): carried - all five of my own found sites, the table's exact conversion decisions, the disclosed loop-survival limit, and all five specific verification questions were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (specreview-r3): T-1107-codex-specreview-r3

---

### Codex Spec-Review verdict: APPROVE (round 4)
- Task: T-1107
- Codex model: gpt-5.6-luna — re-measured fresh from `~/.codex/config.toml`'s `model = ` line this round (unchanged); JSONL again carries no model-identifier field, config value stays primary. No `-m` override.
- Codex CLI: codex-cli 0.149.1 — re-measured fresh via `codex --version` this round (unchanged).
- Mode: Spec-review (specify seam, T-1092), round 4
- Consumer class: unchanged — full adversarial depth, dev-scaffold re-pricing does not apply.
- Input reviewed: `.shell-team/specs/T-1107-close-out-gates.md`, current working-tree state (still uncommitted; sweep unchanged, dry-run 0 unrecognized). Re-read in full `## Notes for engineer` trap 3 (the site table is gone, replaced by the rule, the four-entry exception roster E1-E4, and the engineer's mechanical `grep` verification step), `AC1`'s amended prose, and `AC2`'s extended check line (the loop-survival lock). No `--base`, no branch diff — still pre-implementation.

#### (a) Does the rule + roster + grep step close the non-convergence structurally? — Yes, independently confirmed
Re-verified the roster against the actual file bytes rather than the spec's own transcription of them:
- **E1** correctly scopes to only the `LS_LINES` rows that are the flag axis's subject (`AC3`(iv)'s one-row-per-allowed-flag set — `:998` `READY_FOR_ARCH`, and its siblings — plus `AC3`(v)'s two new divergent decoy rows); every other corpus row (title/path/separator shape, the two CRLF cases) is correctly left to the default flip, matching trap 5's own pre-existing, unchanged guidance for the `accepted` floor.
- **E2**'s named instances (`ready_for_qa` at `:857`, `READY_FOR_MERGED`, `READY FOR QA`, `readyforqa`, `REWORK `) are genuinely out-of-vocabulary and are the source-line gate's own negatives — correctly exempted since their whole point is the malformed value itself.
- **E3**'s header (`bin/close-out.sh:867`... no, `tests/close-out/run.sh:867`) is byte-identical to the file's actual current line 867 (independently confirmed with `sed -n '867,872p'`), and line 872 is exactly `done`.
- **E4** is a different file (`tests/close-out/fixtures/todo-legacy.md:14`) and I independently confirmed `T-101`'s own flag value (`READY_FOR_ARCH`) is never separately quoted or asserted inline anywhere in `tests/close-out/run.sh` outside its own fixture file (`grep -n 'T-101\|READY_FOR_ARCH' tests/close-out/run.sh` shows only `E1`'s corpus row at `:998`, `E3`'s loop header at `:867`, and two `awk` patterns matching `T-101` **by task id**, never by flag value) — no fifth exemption class is needed. A second, independent Codex pass reached the identical conclusion on all four roster entries and found no fifth class either.
- Independently ran `grep -nE 'READY_FOR_[A-Z]+|BLOCKED|REWORK' tests/close-out/run.sh` myself (69 hits) and spot-checked that every hit outside `E1`/`E2`/`E3` is exactly the shape the rule predicts — an incidental `READY_FOR_QA` (or similar) in a board-construction `printf`, none of them asserted against elsewhere by their specific value (confirmed via `grep -n "grep.*READY_FOR_QA" tests/close-out/run.sh` outside the differential-corpus range: zero hits) — so flipping every one of them to `READY_FOR_MERGE` changes no other assertion's outcome.
- This structurally closes what two prior rounds could not converge on by enumeration: the rule needs no site list at all, and the mechanical grep step re-derives the population from the file itself rather than from any record's memory of it.

#### The deliberate narrowing (excluding "refuses at an earlier gate anyway" as an exemption) — sound, independently confirmed
Traced this against the two examples the orchestrator named (`OV2`, the legacy-layout negative half) plus, independently, `M5`'s stranded-board case, `T-150`'s negative case and every other earlier-refusing site I found across rounds 2-3: none of their refusal reasons, exit codes, or byte-identity assertions depend on the specific flag value — each refuses at the interventions/dispatch/disposition gate (or the pre-write interlock, for `M5`) regardless of whether the flag reads `READY_FOR_QA` or `READY_FOR_MERGE`. Flipping these to `READY_FOR_MERGE` changes nothing about their outcome and removes the need to reason about gate order per site at all — which is exactly what eliminates the recurring omission class rounds 2 and 3 each rediscovered a new instance of. A second, independent Codex pass confirmed the same conclusion by name against `OV2`, the legacy-negative half, `M5`, and `UAL`.

#### (b) Is AC2's extension a faithful mechanization of round-3 finding (iii)? — Yes for the concern that was actually raised; one further finding evaluated and rejected
Independently ran the extended check line against the current (unconverted) working tree myself (not merely trusting the orchestrator's report): exit `1` (red at freeze, as predicted — today's loop body contains neither the `READY_FOR_MERGE` literal in an if/else branch nor the `L2` absence-assertion), no syntax error, quotes balanced. Round 3's finding (iii) was specifically that **nothing at all** watched whether the loop still existed — `AC9` alone would stay green even if a future edit deleted it outright. `AC2`'s extension closes exactly that: header-presence-exactly-once plus non-empty-body plus both literals present is a genuine, non-vacuous check that the loop (a) still exists, (b) still enumerates all seven flags in its header, and (c) its body still references both the success marker and the discriminator literal.

A second, independent Codex pass raised a **narrower** residual concern I evaluated and reject as a blocking Major: that a "gutted" conversion — one that keeps the two required literal strings present as inert text (a comment, a vacuous `printf`) without the body actually enforcing per-flag behavior — could still satisfy this static check. This is real in the abstract, but I judge it is not a new risk this task introduces and falls squarely inside the spec's own frozen carve-out ("adversarial guard-completeness of this spec's own inline check lines against hypothetical defeats... arm-B instruments... covered under arm A"): every `fail()`-gated assertion in this entire file (and every other shipped `tests/*/run.sh` suite in this repository) is equally vulnerable to a sufficiently motivated actor gutting it while preserving surface text — this is an ambient property of the whole bash-`- check:`-line verification architecture this repo runs on everywhere, not a defect specific to this loop, and this repository's own Input-space convention elsewhere states plainly that a finding the mechanism "does not stop a forger" is out of scope for this class of trusted-operator tooling. Round 3's actual, concrete finding — a total absence of any watch on this property — is genuinely closed; demanding mutation-probe-level defense against a hypothetical dishonest conversion here, when no other check line in this spec or this repository is held to that bar, would be an inconsistent escalation of scope this late in the review. Rejected as a blocking Major; recorded rather than silently dropped, per this role's own rule for disagreeing with Codex.

#### Synthesis audit ledger (round 4)
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | roster completeness / fifth exemption class | Codex: info/none found | confirmed, no change | independently re-verified all four roster entries against the file's actual bytes; no fifth class needed, matching Codex's own independent conclusion |
| 2 | "earlier gate's refusal" narrowing soundness | Codex: info/sound | confirmed, no change | independently traced against every earlier-refusing site found across rounds 2-4; none depend on the specific flag value |
| 3 | AC2 extension could be satisfied by a "gutted" conversion preserving only surface text | Codex: major | rejected (not blocking) | matches the spec's own frozen carve-out for adversarial completeness of its own inline check lines; an ambient, repo-wide property of every bash `fail()`-gated assertion, not a defect this task introduces; round 3's actual finding (total absence of any watch) is genuinely closed |

#### Recommendation

APPROVE

- APPROVE → this Specify-seam spec-review gate is satisfied; the intent-hash may now be recorded. This verdict gates the freeze only — it is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.
- Pre-commitment status: does not apply — no Blocker/Major that I sustain landed this round (the one Major Codex raised is independently rejected above with a stated reason), so the two-consecutive-rounds factual trigger has no new round to count against this axis; this axis is spec-review, before implementation exists, not a delivered-change review round in any case.
- Non-blocking, carried forward for whoever implements: the mechanical `grep -nE 'READY_FOR_[A-Z]+|BLOCKED|REWORK' tests/close-out/run.sh` verification step is exactly as cheap and re-derivable as the spec claims — I ran it myself this round (69 hits) and it is a good, fast sanity check for the engineer to re-run after the conversion.

#### Fast-follow disposition
- no fast-follow deferrals — the one finding this round (the "gutted conversion" concern) is rejected outright as out-of-scope for this task, not deferred as a future fix.

Raw Codex output (round 4): `.shell-team/reviews/T-1107-codex-specreview-r4.txt` (final message), `.shell-team/reviews/T-1107-codex-specreview-r4.jsonl` (full event stream). Rounds 1-3's canonical stems are preserved above per the naming-consistency rule.

- executor-invocation (specreview-r4): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Round 4 (likely final) of a cross-provider spec review, before any implementation exists. Re-read .shell-team/specs/T-1107-close-out-gates.md yourself (the '## Notes for engineer' trap 3 section, AC1's amended prose, and AC2's extended check line) -- do not expect its text pasted here. Background: three prior rounds each found a genuine gap in how tests/close-out/run.sh's own inline fixtures will interact with T-1107's new gate. pm-spec's round-4 fix replaced the whole site table with a RULE (every inline fixture flag defaults to READY_FOR_MERGE; a token stays non-MERGE only when that value IS the surrounding assertion's subject), a four-entry EXCEPTION ROSTER (E1 LS_LINES flag-axis rows, E2 out-of-vocabulary negatives, E3 the seven-flag loop converted not deleted, E4 the fixture-file T-101 resident), and a MECHANICAL VERIFICATION STEP (grep -nE 'READY_FOR_[A-Z]+|BLOCKED|REWORK' tests/close-out/run.sh, everything outside E1-E3 must read READY_FOR_MERGE). A previously-offered 'earlier gate already refuses' exemption was deliberately excluded and all such flags flip to MERGE too. AC2's existing check line was extended with a loop-survival lock (header transcribed byte-for-byte, body extraction, both new assertions). Please verify independently, reading the actual current bytes yourself: (1) is the rule+roster+grep step sufficient to close the non-convergence structurally, any roster entry wrong/incomplete; (2) is excluding the earlier-gate-refusal exemption sound, does flipping those flags break anything concrete; (3) does AC2's extended check line actually mechanically verify the loop's survival and both branches, or could a gutted conversion still pass it; (4) is there a fifth exemption class missing; (5) after three prior rounds of Majors on this exact area, would you APPROVE the freeze now, or is there a concrete remaining defect. Return findings as a JSON array of {severity, criterion, issue, suggestion}. If you find no remaining defect, say so explicitly and state you would approve." > <RAW_JSONL> 2>&1
- pass-role (specreview-r4): confirmation
- briefing-fidelity (specreview-r4): carried - the complete rule/roster/grep-step design, the deliberate narrowing and its stated reason, and AC2's exact extension shape were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (specreview-r4): T-1107-codex-specreview-r4

---

## Codex review (delivered change)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1107
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line, measured fresh this round (`model_reasoning_effort = "max"`); the JSONL event streams for both passes this round carry no explicit model-identifier field, so the config-file value stays the primary source. No `-m` override was passed either invocation.
- Codex CLI: codex-cli 0.149.1 (verbatim `codex --version` output this round)
- Verification mode: executed-tests — this round independently ran `bash bin/check-acs.sh` on the frozen v2 spec (14 passed / 0 failed / 1 skipped, matching QA), `bash tests/close-out/run.sh` (125 PASS / 0 FAIL, `corpus=34 refused=20 accepted=10 notlocated=4 mismatches=0`, matching QA byte-for-byte), `shellcheck bin/close-out.sh tests/close-out/run.sh` (version `0.11.0`, exit 0), `bash tests/errexit-safe/run.sh`, `bash tests/check-oversight/run.sh` and `bash tests/trial-recipe/run.sh` (all exit 0), plus this role's own from-scratch mutation probe (below) on a detached `git worktree`, never the working tree.
- Diff scope: 14 files, +1126/-70 lines (`git diff --stat 8a27308...HEAD`)

#### Mandatory item — the host-ratified class-B re-freeze (v1→v2): verified, ACCEPT

Re-ran the classifier myself from the v1 bytes rather than trusting the board's own transcription of it. The briefing's claim that v1 bytes are recoverable (and the QA hand-off's contrary claim is mistaken) is confirmed: `git show 0054aab:.shell-team/specs/T-1107-close-out-gates.md` returns the full v1 file cleanly.

```
bash bin/check-refreeze-class.sh --old-hash 1d17746708bb6ceaf10fa527b76a4e0352587991 \
  --new-hash dd576023b6bf6ee3a1e8805b532f87de556cb59c -- <v1-file> .shell-team/specs/T-1107-close-out-gates.md
→ check-refreeze-class: class-b: differing line at intent-block index 3 for T-1107 is not a '- check:' line on
  both sides ... (exit 1)
```

Both supplied hashes were accepted without a mismatch error, confirming the v1/v2 bytes I read are exactly the ones the board's ratification record cites. A line-by-line `diff` of the two normalized intent blocks shows **exactly two** differing lines: (1) the `- base-ref-discriminator:` declaration's reader-set sentence, `seven` → `eight` with **AC10** added to the list of criteria reading a base-side blob; (2) **AC10**'s own `- check:` line, from an unconditional "every row in the derived population must exit 0" form to a two-arm `git worktree add --detach` base-vs-HEAD comparison (only base-green members must stay green at HEAD; base-red members are recorded, not required; `bg -ge 1` non-vacuity control). Both are exactly the delta the board's `intent-ratified` record describes — no more, no less.

Judged whether the v2 check line still serves the byte-identical AC10 prose ("still passes... each must exit `0`... **Green at freeze and required to stay green**" and "That derivation is exactly the class this task **must not disturb**"): yes. "Must not disturb" names the *population derivation* (the live `- check:` extraction filtered on `close-out.sh`+`READY_FOR_MERGE`+none of the six other flags), and that extraction logic is byte-identical between v1 and v2 — confirmed in the diff above, only the *execution* of the extracted population changed. "Still passes" presupposes the member already passes; a member that was already failing at the base ref (for a cause predating this task) was never inside the domain "still passes" describes, so narrowing the requirement to the base-green subset is a *more faithful* rendering of the prose than the v1 form's vacuous "every member, including two members already broken for unrelated reasons, must exit 0" — which is not "still passes" at all for those two, it is "must newly start passing something that was never asked of this task."

Independently reran the shipped v2 check line myself (not the engineer's or QA's transcription of its result):
```
$ bash -c "$(the AC10 check line extracted verbatim from the current spec)"
AC10 base-red, recorded and not required at HEAD: rc=0; export LC_ALL=C; C=bin/close-out.sh; ...
AC10 base-red, recorded and not required at HEAD: rc=0; export LC_ALL=C; B=$(if git show-ref ...
exit=0
```
Exactly **two** base-red disclosures, matching the board's and `.shell-team/test-recipe.md`'s own account of the two pre-existing, unrelated causes (T-1092's backstop assertion re-added by T-1096; T-1100's templates-less scratch-`bin/` copy under T-1103's unconditional gate) — the lost enforcement (a vacuous absolute-green demand over two members that could never pass) is honestly disclosed, not smuggled through. **ACCEPT** — no correction owed to this delta.

#### Blockers
(none)

#### Major
- `bin/close-out.sh:739` (the `#266` note's `else` branch of `if [ -n "$ISSUE" ]`) — **the note's own suggested primary recovery is impossible on every invocation that prints it.** Confirmed independently, reading the actual control flow: the board write (`cat "$TMP_BOARD" > "$BOARD"`, `bin/close-out.sh:716`) happens *before* the note prints (`:739`), so by the time an operator reads "run close-out.sh again with `--issue N`", the task's Active entry has already moved to `## Done`. A second invocation with the same `--task` hits the pre-existing, unrelated refusal at `bin/close-out.sh:211` ("`$TASK is already in ## Done — refusing to create a duplicate entry`") before it can ever reach the `--issue`-present procedure — the suggested command cannot succeed, ever, for the class of run that prints it. Found first by the primary Codex pass (`codex exec review --base`), independently confirmed by this role reading `bin/close-out.sh:211` and `:686-716` directly, and reproduced conceptually against the shipped control flow (no mutation needed — the ordering is unconditional).
  - This is genuinely new adopter-visible text introduced by this task (not a pre-existing message), so it is this task's defect to fix, not a disclosed-and-inherited one.
  - No acceptance criterion catches this: **AC4** pins only two substrings (`close-out: note: no --issue given`, `develop merges do NOT auto-close`) and the spec's own Body-to-AC correspondence explicitly leaves the rest of the sentence unpinned ("pinning a full sentence would make an improvement in wording a criterion failure") — but this is not a wording *improvement* question, it is a factual claim about a recovery path that cannot execute.
  - Suggested fix, with a constraint the fix must respect: **AC4** cases (a)/(c) also assert stdout does **not** contain the substring `gh issue close` for the no-`--issue` runs — so the fix must not simply copy the `--issue`-present branch's `gh issue close N …` command literal into the note. A conformant fix drops the "run close-out.sh again" suggestion (which cannot work regardless of wording) and instead tells the operator the task is already in `## Done` and points at a manual path that does not reuse that literal substring (e.g., referencing the `CONTRIBUTING.md` close command, or paraphrasing the GitHub MCP `issue_write` form already used in the sibling branch) — a one-line prose fix, no mechanism change, no re-freeze (this sentence sits outside the intent-block region and outside every check line's substring pins).

#### Minor / nits
- `bin/close-out.sh` (whole-script property, pre-existing, not introduced by this diff) — a TOCTOU-shaped read-then-write race: the Active flag is captured once (`MAIN_LINE`/`FOUND_FLAG`, `:472`) and the board is read again independently for the final rewrite (`:682`, `:703`); a concurrent writer changing the board between those two reads could produce a rewrite based on a stale snapshot. Verified this exact multi-read-without-lock shape is **byte-identical in structure** at the base ref (`git show 8a27308:bin/close-out.sh`, same read sites at the corresponding line numbers) — this task's diff only added the `FOUND_FLAG` capture at the *same, already-existing* read point and a gate check immediately after; it introduced no new read-write window. This repository's own `bin/` scripts are single-operator, sequential-invocation tools (this spec's own `## Input space` already prices "an operator who edits the board and then runs the close-out" as `out-of-input-space` on a TRUSTED-operator posture), and no shipped `bin/` script in this repo takes a board file lock today. Not a regression this task introduces; a pre-existing, repo-wide property. Downgraded from Codex's raw `major` for this reason.
- `bin/close-out.sh` parse arm (`:118-129`) — repeated `--issue` values silently overwrite (`--issue 5 --issue 6` records only `6`). Confirmed pre-existing and explicitly frozen unchanged by this task: **AC8** pins the exact parse-arm accepted-flag line byte-identical between base and HEAD as a Non-goal ("No new flag... parse arm byte-unchanged"), so this task neither introduced nor was asked to fix last-wins argument semantics.
- `tests/close-out/run.sh:1013-1093` (**AC3**(v)'s two divergent decoy rows) — Codex's adversarial pass raised a narrower extraction bug (selecting the literal last backtick pair rather than the grammar's rightmost *slot*) that could in principle satisfy both rows without implementing the correct grammar. This is the identical class the spec's own frozen `## Reviewer scope` already prices out of Major ("adversarial guard-completeness of this spec's own inline `- check:` lines against hypothetical defeats... arm-B instruments... covered under arm A") and the identical alternative (asserting the exact stderr-named flag token) already rejected with a stated, sound reason in `## Open questions` item 2 (message-wording coupling). Won't-fix, matching established precedent from this task's own round-4 spec review.
- `docs/adopting.md:705-728` / `docs/adopting.ja.md:692-712` — the "exact complements of one condition" sentence does not restate that this holds only for a *successful* run reaching the post-write branch (a refused run, `--help`, or a validation failure never reaches either branch). Low materiality: the surrounding section is already framed around the pre-flip gate's own success/refusal split, and **AC4**'s own check line is scoped identically (it only ever exercises `READY_FOR_MERGE` scratch boards). A precision nit, not a factual error.

#### Adversarial findings
Ran a devil's-advocate pass (`codex exec`, free-form prompt, reading the real files directly) focused on the confirmed Major above plus: gate bypass/argument-ordering tricks against the new pre-flip gate, the genuine discriminating power of **AC3**(v)'s two divergent rows, and whether the note/procedure stay exact complements across every invocation shape. It surfaced the TOCTOU race (evaluated above, downgraded — pre-existing, not a regression), the duplicate-`--issue` last-wins behavior (evaluated above, downgraded — pre-existing, frozen unchanged by **AC8**), a narrower AC3(v) extraction-bug variant (evaluated above, matches an already-priced carve-out), and the doc-precision nit above. It found **no** way to bypass or defeat the new pre-flip gate itself (no argument-ordering trick, no whitespace/encoding trick, no empty-string bypass beyond the one **AC4** already tests) — the gate's own fail-closed correctness is not in question from either pass.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | the `#266` note's "run close-out.sh again" recovery is impossible (task already in Done) | primary: major (P2) | major | confirmed independently against the real control flow (board write at `:716` precedes the note at `:739`; a second invocation hits the pre-existing `:211` already-Done refusal); genuinely new adopter-visible text this task introduces, not inherited |
| 2 | TOCTOU race between flag capture and final board rewrite | adversarial: major | minor | verified byte-identical read-then-write shape already present at the base ref; this task added no new read-write window; repo-wide, pre-existing, trusted-operator-posture property, not a regression |
| 3 | duplicate `--issue` values silently last-wins | adversarial: minor | minor (unchanged) | confirmed pre-existing; **AC8** freezes the parse arm byte-identical as a stated Non-goal, so this task neither introduced nor owns a fix |
| 4 | AC3(v)'s two rows could in principle be satisfied by a subtler extraction bug (last-backtick vs. rightmost-slot) | adversarial: minor | minor (unchanged) | matches the spec's own frozen arm-B carve-out and the already-rejected token-asserting-oracle alternative (`## Open questions` item 2) |
| 5 | "exact complements" doc sentence doesn't restate the successful-run scope | adversarial: minor | minor (unchanged) | low materiality; the surrounding section and **AC4**'s own check line are already scoped to the success path throughout |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the Major finding above linked.
- Ask: fix the `#266` note's broken "run close-out.sh again with `--issue N`" recovery clause (a one-line prose edit, no mechanism change, no re-freeze — the sentence sits outside the intent-block region and outside every check line's substring pins) so it no longer promises a command that always fails; respect **AC4**'s existing constraint that no-`--issue` stdout must not contain the substring `gh issue close`.
- Everything else this round — the mechanism itself (the pre-flip gate's fail-closed correctness, the composed differential oracle, the `#266` warn/refuse asymmetry, the class-B re-freeze) — is independently confirmed sound; a fresh round after the one-line fix lands should close cleanly.

#### Fast-follow disposition
- Ledger row 2 (TOCTOU race across `bin/close-out.sh`'s whole read-then-write shape): file-an-issue — this spans every close-out invocation in this repo, not only T-1107's own diff, and fixing it (a board lock plus snapshot revalidation) is a real design change out of proportion to this task's own scope; worth a dedicated task rather than folding into this one's fix.
- Ledger row 3 (`--issue` last-wins on repeated flags): won't-fix — pre-existing, and **AC8** explicitly freezes the parse arm byte-identical as this task's own stated Non-goal; not this task's to change.
- Ledger row 4 (AC3(v) adversarial-completeness residual): won't-fix — already priced out by the spec's own frozen carve-out and a already-rejected, reasoned alternative.
- Ledger row 5 (doc-precision nit on "exact complements" scope): won't-fix — low materiality, matches this repo's established static-proxy/doc-precision tolerance elsewhere in this same spec (**AC7**/**AC8**).

Raw Codex output: `.shell-team/reviews/T-1107-codex-primary.txt` (final message), `.shell-team/reviews/T-1107-codex-primary.jsonl` (full event stream, canonical `review --base` path); `.shell-team/reviews/T-1107-codex-adversarial.txt` (final message), `.shell-team/reviews/T-1107-codex-adversarial.jsonl` (full event stream, free-form devil's-advocate pass).

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base 8a27308 --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-carried - the shipped `review --base` subcommand takes no prompt argument at all (agents/codex-reviewer.md), so this round's own launch-prompt Briefing (consumer class, verdict scope items (i)-(vi), the domain-premise re-derivation duty) has no channel into this pass; the same disclosed gap T-1104's/T-1105's/T-1106's own records already name for this identical subcommand shape
- raw-capture (primary): T-1107-codex-primary

- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on the diff between commit 8a27308 and HEAD in this repo (branch feature/1107-close-out-gates). Read the files yourself with git diff / nl -ba -- do not expect any diff pasted here. Focus especially on bin/close-out.sh and tests/close-out/run.sh. A prior review pass already found one confirmed defect: when a successful close-out run omits --issue, the printed note at bin/close-out.sh (the 'no --issue given' branch) tells the operator to 'run close-out.sh again with --issue N' once they have the issue number -- but by that point the Active board entry has ALREADY been moved to ## Done (the board write happens earlier in the script), so a second invocation with the same --task would hit the script's own 'is already in ## Done -- refusing to create a duplicate entry' refusal before ever reaching the --issue-present procedure. The suggested recovery is therefore impossible for every invocation that prints it. Given that confirmed finding, adversarially look for: (1) any OTHER shipped adopter-facing message (stdout/stderr, in bin/close-out.sh, README.md, README.ja.md, docs/adopting.md, docs/adopting.ja.md) that gives advice which cannot actually be followed given the script's real control flow and gate ordering; (2) any way the new pre-flip gate (refusing a close-out unless the Active flag is exactly READY_FOR_MERGE) could be bypassed, defeated, or made to accept a wrong flag -- argument ordering tricks, whitespace/encoding tricks in the board line, empty-string edge cases, race conditions between the flag-capture regex and the later board rewrite, or a decoy backtick token; (3) any gap between the differential harness's two divergent AC3(v) corpus rows (rows literally named 'divergent decoy row A'/'divergent decoy row B' in tests/close-out/run.sh's LS_LINES array) and what they actually prove -- do they truly diverge under a first-match vs rightmost-slot extraction bug, or could a subtly different bug slip through; (4) whether the note and the --issue-present procedure are genuinely exact complements of one condition across every invocation shape, including --issue '' from an unset variable and repeated/duplicate --issue flags. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <RAW_JSONL> 2>&1
- pass-role (adversarial): confirmation
- briefing-fidelity (adversarial): carried - the primary pass's confirmed finding, the exact control-flow reasoning behind it, and all four specific adversarial-focus questions were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (adversarial): T-1107-codex-adversarial
