# T-1008 — QA verification

**Verdict: PASS** → board to `READY_FOR_REVIEW`

## Mechanical layer

- `bash bin/check-acs.sh .shell-team/specs/T-1008-lessons-corpus-import.md`: **25 PASS, 0 FAIL, 2 SKIP** (AC12/AC13, by design — matches the spec's own disclosed layering).
- `bash bin/check-intent.sh .shell-team/specs/T-1008-lessons-corpus-import.md .shell-team/todo.md`: `aligned: T-1008 v1 (b133c95469cea7327cc8db69970163202c5e3fbc)`.
- Structural check on the frozen intent block: `<!-- BEGIN intent-block: T-1008 -->` at line 45, `<!-- END intent-block: T-1008 -->` at line 591, `## Notes from engineer` at line 1055 — strictly below the END marker. The engineer's only spec edit was that appended section; the frozen Goal/Non-goals/AC/Input space are untouched.
- Full CI-ordered run, in `.github/workflows/check-handoff.yml`'s file order, all 52 steps (the exact-arg-list shellcheck step, every fixture suite, every "Dogfood …" step, both PR-diff steps run against local `develop`, since `origin/develop` == `develop` == base `902c465` in this checkout): **0 failures**. `shellcheck --version` on this host is 0.11.0, matching CI's pin exactly.
- Hygiene: `check-prompt-sync.sh`, `check-handoff.sh .shell-team/todo.md`, `check-pii-shapes.sh --base develop`, `check-commit-identity.sh --base develop`, `check-provenance.sh .shell-team/provenance/T-1008.md` (conformant, 4 decisions), `check-interventions.sh .shell-team/interventions/T-1008.md` (conformant, 0 entries/1 sentinel), `check-board-headings.sh .shell-team/todo.md` — all exit 0.
- `git diff --name-only 902c465` — 20 files, exactly AC27's allow-list (verified both mechanically via AC27's own check and by eye). `git status --short` was clean throughout verification except for this review file and the board append.
- AC26's `bin/` diff read by eye: comment-only (header prose in `bin/gen-playbook-blocks.sh`), confirming #58 direction (b) with no logic change.
- Tool-wrapper-residue sweep re-run independently over every changed file: one hit, in the spec's own prose at line 1042, which is the spec quoting the grep pattern itself as a worked example (matches the engineer's own disclosure) — not residue.

## AC13 — qa-verifier's independent semantic-fidelity audit

**1. Completeness audit (all 79 rows).** Built an independent (date, per-date-sequence) key extraction from the real source file and confirmed the key sets between source and ledger are byte-identical (79/79, both directions) — this reproduces AC7/AC8's mechanical multiset check from scratch rather than trusting it. Spot-checked the ledger's non-monotonic date ordering against the source's actual file order at two points (the `2026-07-19` sequence-3 and sequence-6 rows, both later-superseded entries) and confirmed the ledger's sequence numbers track true source file position, not a re-sorted view — strong evidence the ledger was built by reading the file, not approximated. All three of the source's `superseded` entries (one at `2026-06-13`, two at `2026-07-19`) resolve through their `Superseded-by` pointer to the correct shipped `active` entry; confirmed by reading both source and shipped corpus.

**2. Adversarial deep-read, three entries** (spec's own selection criteria): the longest `Rule` in the source (`2026-06-14`, ~1275 chars, the runtime user-verify AC entry) — read in full against the shipped English; no narrowing, no dropped condition, all three enumerated traps preserved, `Source` correctly stripped to `n/a` per DP-4. A second entry doubling as one of the two `|`-in-title cases (`2026-06-18`, CI shellcheck idiom) — Category/Applies-to/Status carried unchanged, `Scope: maintainer` + `Bound-in: CONTRIBUTING.md` correctly added, no narrowing. A third entry with heavy parenthetical qualification (`2026-07-26`, the two-stage verification-mechanism self-check entry) — the source's four-item parenthetical blind-spot list translates 1:1 into the shipped English's four-item list, verbatim in substance. Closed-enum values (`Category`, `Applies-to`, `Status`) confirmed carried unchanged on all three, plus ten additional entries sampled at even intervals across the file (13/13 correct).

**Finding (Minor, non-blocking):** the source carries roughly a dozen `[[wiki-style]]` cross-references between entries (e.g. the `2026-06-14` entry links to two related entries by title). None of these survive into the shipped English corpus — `grep -c '\[\['` on the shipped corpus is 0. This is a navigational/completeness loss (a "see also" pointer), not a narrowing of any `Rule`'s substantive meaning or applicability, and the spec's Input space names the shape only as something translation must not corrupt on, not as a property an AC requires preserved. Recording it for the reviewer's awareness; not a FAIL driver.

**3. Independent six-class entity sweep.** Derived my own search terms (not transcribed here) and swept the corpus, the ledger, all four blocks, all five changed `agents/*.md` files, and the one changed skill file for: employer/customer name shapes, internal-ticket-identifier shapes (JIRA-style `LETTERS-digits`, honorifics), hostname shapes, personal-name markers, pre-publication task/issue-number shapes, and home-directory absolute-path shapes. Zero hits across all twelve files and six classes, with one false positive (`ISO-8601` matching the ticket-shape regex, confirmed benign by inspection). The out-of-band source path was never written to any tracked file or into this review.

**4. Regen-diff non-vacuity.** Mutated one real entry's `Rule` line in `.shell-team/lessons.md` (not the `## Format` fence's placeholder), ran the extracted `Dogfood gen-playbook-blocks` step verbatim: it printed 4 "regenerated" lines and **exited 1** (correctly detected the drift). Restored `.shell-team/lessons.md` from a pre-mutation file copy (never `git checkout`), confirmed byte-identical via both `diff` and `cmp`, confirmed `git status --short` was clean, then re-ran the same step and confirmed **exit 0**.

## Two engineer claims, adversarially verified

**AC8-foreclosure claim.** Reproduced the mechanics independently on a scratch copy: copied `.shell-team/lessons.md` to a scratch path, ran `bin/playbook-promote.sh --scope loop --lessons <scratch>` to append one throwaway candidate entry, then re-ran AC8's own destination/heading extraction against the real ledger (`docs/loop-engineering/lessons-import-disposition.md`, untouched) paired with the mutated scratch corpus: 74 retained destinations vs. 75 corpus headings — `cmp` reports EOF and exits 1. Confirms the claim mechanically: any promotion through the promoter necessarily adds a corpus heading dated with the retro's own date, which no import-table row destines, and AC8's two-directional multiset equality cannot tolerate that under the current 79-row/74-heading contract. The real repository tree was never touched by this probe (`git status --short` clean before and after).

Read all nine retro candidates in `.shell-team/retros/2026-07-28.md` against the ledger's candidate-table dispositions. All nine read as honest, individually-justified reasons, not reverse-engineered to hit a count: one `already-covered` genuinely restates an already-imported `2026-07-26` entry (confirmed by reading both); one `operator-global` (git stash `-u` behavior) is a genuine cross-project tool fact; the remaining seven `drop`s each carry a distinct, plausible reason (a direct prose-edit recommendation rather than a rule statement, three genuinely-novel-but-mechanically-foreclosed candidates, one folded into `.shell-team/test-recipe.md` instead per DP-11, one distinct from an existing entry, one sourced only from an external tracker item per DP-4). **Minor finding**: `.shell-team/provenance/T-1008.md`'s first decision says "Two of the nine candidates" are genuinely new but then names three (the fail-open/fail-closed reframing heuristic, the self-referential-gate dogfooding recommendation, and the full-content-search recommendation) — an off-by-one counting slip in the prose, not a misrepresentation of the underlying mechanical claim, which I independently confirmed true above. Per this task's provenance rule, the semantic soundness of a decision's grounding is not itself an item I fail on; recording it for the reviewer. I found no candidate that plainly deserved promotion despite AC8 — the mechanical foreclosure is a structural property of the current 79-row contract (there is no "spare" import-table row for a retro-dated heading), not a defect in the engineer's judgment, and it is disclosed transparently rather than worked around.

**`|`-in-title rewording.** Found both source titles with a literal `|`: `2026-06-18`'s title (containing the `` `A && B || C` `` idiom) and `2026-07-13`'s title (containing `` `上流 | grep -q` ``). Confirmed both shipped titles (lines 167 and 310 of the shipped corpus) are reworded to remove the pipe character while preserving meaning (`"A-and-B-or-C" idiom`, `` piping into `grep -q` ``), and confirmed zero `|` characters remain in either heading line.

## Edge cases tried beyond the spec

- Mutated a real `Rule` (not the fence's placeholder) to prove the regen-diff lock is load-bearing rather than vacuously green — see AC13 §4 above.
- Attempted an actual promotion via `bin/playbook-promote.sh` against a scratch copy to independently prove the AC8-foreclosure claim rather than trusting the engineer's fixture description.
- These are within the spec's own Input space (item 8, the nine retro candidates; the regen-diff mutation is explicitly required by AC13) — no out-of-input-space exemption needed.

## Risk notes for reviewer

- Two Minor, non-blocking findings recorded above: (1) systematic loss of `[[wiki-style]]` cross-references across the corpus (navigational, not substantive); (2) a "two vs. three" counting slip in the provenance file's first decision entry (the underlying mechanical claim is correct).
- AC26/AC27 are merge-point-scoped against `902c465` and expected to go stale after merge, per the spec's own disclosure — not a defect.

---

### Codex Review verdict: APPROVE
- Task: T-1008
- Codex model: gpt-5-codex (via Codex CLI 0.145.0)
- Verification mode: static-only
- Diff scope: 22 files, +2416/-270 lines (`git diff --stat develop...HEAD`)

#### Blockers
- none

#### Major
- none (see Synthesis audit ledger — one primary-pass finding normalized to major was downgraded to minor after adversarial re-check)

#### Minor / nits
- `bin/gen-playbook-blocks.sh`'s `--help` (`sed -n '2,48p'`) now truncates two lines earlier than on `develop`, additionally hiding part of the `--lessons` description (`--blocks-dir` and the `Exit:` block were already hidden on `develop`). Caused by this task's #58 header edit adding two net comment lines without widening the fixed sed range. Real but cosmetic — no functional or AC impact — and this task's own frozen `AC26` requires the `bin/` diff to be comment-only, so fixing the sed range (a non-comment line) here would breach that frozen constraint. The general defect class (fixed-sed-range `--help` truncation across 8+ `bin/` scripts) is already tracked separately at issue #12 (filed 2026-07-28, T-113). Recommend folding this specific instance into #12 rather than reopening scope here.
- QA's two previously-recorded Minors (dropped `[[wiki-style]]` cross-references; the "two vs. three" counting slip in `.shell-team/provenance/T-1008.md`'s first decision) — read and concur with QA's own disposition; not blocking, no further action beyond what QA already recorded.

#### Adversarial findings
- Codex's primary pass (`codex exec review --base develop --json`) raised two P-labeled findings. Both were adversarially re-checked against a devil's-advocate prompt directing Codex to read the frozen spec's `DP-6`/`AC8`/`AC26` text and the board's `issue #12` reference directly, rather than accepting the primary read at face value.
  - **P1** ("promote eligible retro candidates instead of dropping them", citing `docs/loop-engineering/lessons-import-disposition.md:137`): the adversarial pass concluded this **does not hold up** — `DP-6` explicitly permits a `[common]`-labeled candidate to resolve to `drop` at the engineer's own per-candidate judgment (the table only forbids `[common]→maintainer` and `[target-specific]→loop`), and `AC8`'s two-directional byte-equality check mechanically forecloses adding any corpus heading the 79-row import table does not destine — a constraint independently mechanically verified by both the engineer (a minimal fixture) and qa-verifier (a live `bin/playbook-promote.sh` reproduction) before this review. Codex's primary pass misread the routing rule's general `drop` description without incorporating `DP-6`'s explicit permission table.
  - **P2** ("`--help` regression"): the adversarial pass **confirmed** the regression is real (reproduced `git show develop:bin/gen-playbook-blocks.sh` vs. the branch's `--help` output directly) but assessed it as **minor**, not major/blocker — fixing it in-task would touch a non-comment `bin/` line, conflicting with this task's own frozen `AC26` comment-only lock, and the underlying defect class is already tracked separately (issue #12).
- Independently, I confirmed the same two conclusions myself before commissioning the adversarial pass: reproduced both `--help` outputs byte-for-byte, read `DP-6`'s explicit `[common]→drop` permission and `AC8`'s check line directly, and located the pre-existing `issue #12` fast-follow entry on the board (filed 2026-07-28, T-113, "fixed-sed-range help extraction hides header content from `--help` across 8+ `bin/` scripts").

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Candidates 3/6/7 dropped instead of promoted to `loop` (`lessons-import-disposition.md:137`) | primary: P1 (normalized: blocker) → adversarial: does not hold up, nit | rejected (no finding) | `DP-6` explicitly authorizes `[common]→drop` at engineer judgment; `AC8`'s structural foreclosure is independently mechanically verified; the primary pass misread the routing rule without `DP-6`'s permission table |
| 2 | `bin/gen-playbook-blocks.sh --help` truncates worse than on `develop` | primary: P2 (normalized: major) → adversarial: holds up, minor | minor | regression is real but cosmetic-only, fixing it would breach this task's own frozen `AC26` comment-only lock, and the defect class is already tracked cross-task at issue #12 |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- `bin/gen-playbook-blocks.sh --help`'s worsened truncation (finding #2 above) — won't-fix this round: fixing the sed range would touch a non-comment `bin/` line, breaching this task's own frozen `AC26` comment-only constraint, and the general defect class is already tracked cross-task at issue #12 (filed 2026-07-28). Recommend the class-wide fix at #12 pick up this specific instance rather than a new issue.
- QA's `[[wiki-style]]` cross-reference loss — won't-fix: navigational-only loss, not a `Rule` narrowing; the spec's Input space names this shape only as something translation must not corrupt, not as a property an AC requires preserved.
- QA's provenance "two vs. three" counting slip (`.shell-team/provenance/T-1008.md`, first decision entry) — won't-fix: a one-line prose miscount in a committed provenance file; the underlying mechanical claim it describes is independently confirmed correct by both qa-verifier and this review, so the slip does not misrepresent the decision it grounds.
