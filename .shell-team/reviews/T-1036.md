# T-1036 — QA verification record

## QA verdict: PASS

**Scope**: verify the branch `feature/141-143-144-wording-batch` (base
`5a5c09b`, T-1035's tip) against the frozen spec
(`.shell-team/specs/T-1036-wording-batch-141-143-144.md`, intent-hash **v4**
`9a01addd489b75bae29a13ba8cba9bcd0d182881`), including the two cross-task
re-freezes it made (T-1029 v2→v3→**v4**, T-1030 v1→**v2**) and the unusual
freeze history recorded in `.shell-team/interventions/T-1036.md` (6 entries).
`READY_FOR_QA` → `READY_FOR_REVIEW`. Nothing below is taken on the engineer's
or the coordinating session's report; every check was re-run live against the
real checkout, including a scratch `git worktree` mutation reproduction. The
working tree stayed clean throughout (`git status --short` empty) apart from
this record and my own board append.

**Lens**: per the spec's own "Task classification" — not verification-mechanism
work (no `bin/`/`tests/` change; AC12 locks it) but not a plain pure-addition
either (two already-merged tasks' frozen regions edited, two board deletions,
three criteria depending on other checkers' exit-status semantics). The
Adversarial fixture synthesis checklist is structurally inapplicable to this
prose-only deliverable (no executable artifact, no documented runnable
procedure), per its own applicability paragraph and the spec's stated lens —
so my surface is the criterion walkthrough, the classifier re-derivations, the
ledger audit, and mutation reproduction on scratch copies.

### Mandatory conformance gates

- `check-acs.sh .shell-team/specs/T-1036-wording-batch-141-143-144.md` →
  **13 passed, 0 failed, 1 skipped (AC14, runtime by design)**, both **before**
  and **after** writing this record — exact match to the required 13/0/1.
- `check-intent.sh` **aligned (exit 0)** for all eight specs on the stacked
  train:
  - T-1036 v4 (`9a01addd489b75bae29a13ba8cba9bcd0d182881`)
  - T-1029 v4 (`b3869290de2f93554259cdd30daa1f94e8f43ee5`)
  - T-1030 v2 (`a830968ca2ea4df5095fe90bd1cba8039cdd8fc5`)
  - T-1033 v1 (`f2d2f4240b7287dad88c44170a559d581a8ceab7`)
  - T-1027 v2 (`8f4a08deee3d91132e396637ab586cce3df933c7`)
  - T-1034 v1 (`8aebc0e66ff98ae0892fd301137f7030ac28e45a`)
  - T-1024 v2 (`4b8ab5ee96fabde20a1b37f481b98fe22b62b2a3`)
  - T-1035 v1 (`8b0d4f42f30fd086f2ae579e76aa3b9e9814c16d`)
- `check-provenance.sh .shell-team/provenance/T-1036.md` → **conformant**
  (3 decision entries, 0 sentinel).
- `check-interventions.sh --task T-1036 .shell-team/interventions/T-1036.md` →
  **conformant** (6 entries, 0 sentinel) — judged structurally only, per the
  gate's own rule; the class token choice (all "assumption-contradicted" plus
  one "human-stop") is a retro judgment, not a QA gate.

### Independent class-B classifier re-derivations (×3, plus one cumulative)

Materialized base blobs at `$TMPDIR`-equivalent scratch paths outside the
checkout and ran `bin/check-refreeze-class.sh` myself, `$?`-checked each time:

- T-1029: `git show 5a5c09b:.shell-team/specs/T-1029-claim-fidelity-qa-step.md`
  vs. current → **exit 1** ("differing line at intent-block index 1 ... not a
  '- check:' line"). Matches the board's own freeze-run reconciliation note
  (index 1).
- T-1030: `git show 5a5c09b:.shell-team/specs/T-1030-reviewer-board-write-boundary.md`
  vs. current → **exit 1** ("index 17"). Matches the board's reconciliation
  note (index 17).
- T-1036 v3→v4: `git show 65a4e33:<spec>` vs. current → **exit 1** ("index 98").
- T-1036 cumulative v1→v4: `git show 4c57920:<spec>` (the original freeze
  commit) vs. current → **exit 1** as well — the whole ratification chain
  still classifies as class-b end to end, not just the last hop.

All four exits are exactly 1 (class-b), never 0 (mechanics) or 2 (structural),
as the spec requires.

### The four shipped lines

`check-acs.sh` AC1–AC6 all PASS (three-way byte identity, occurrence counts,
adjacency, old-phrase extinction, positive pins). Eyeballed independently
against the frozen blocks:

- `agents/qa-verifier.md:65` — Q1, immediately after the intervention-record
  bullet (line 64) — byte match confirmed by eye.
- `agents/codex-reviewer.md:156` — Q2, immediately after R1 (`You do not edit
  production code or tests.`, line 155) — byte match confirmed by eye.
- `skills/run/SKILL.md:81` — Q3, strictly between `6. **Review**` (line 76)
  and `7. **Done**` (line 84) — inside T-1028's AC10 window.
- `skills/goal/SKILL.md:233` — Q4, confirmed by AC4's neighbour-offset check.
- Old-phrase extinction (`grep -rn` for all three superseded appositions in
  `agents/qa-verifier.md` + T-1029's spec, and `read-only on the board` in
  both skill files) → **zero hits**, confirmed independently of AC5.

### Ledger audit

- `git diff --numstat 5a5c09b -- .shell-team/todo.md` → `38  2` — exactly
  **two** deletions.
- `git diff -U0 5a5c09b -- .shell-team/todo.md` removed lines are exactly the
  two superseded hash records (`intent-hash (v1): d0f73437...` and
  `intent-hash (v2): aa47eaa0...`) — nothing else deleted anywhere on the
  branch.
- T-1029's board entry: ratifications v1→v2 (historic, this task's
  predecessor round), v2→v3 (this task, #141), v3→v4 (this task, unblocking
  repair) — attestations v1, v2, v3, v4 all present — exactly one `intent-hash`
  record (v4). Matches the spec's required shape.
- T-1030's board entry: ratification v1→v2 (this task, #144) — attestations
  v1, v2 both present — exactly one `intent-hash` record (v2). Matches.
- T-1036's own board entry: ratifications v1→v2→v3→v4, attestations v1–v4 all
  present, exactly one `intent-hash` record (v4).
- `R29`/`R30` frozen blocks `cmp`-identical, byte for byte, to their board
  occurrences (verified with `cmp`, not just `check-acs`'s own `grep`).

### Full suites / CI-wired steps run live

`bin/check-prompt-sync.sh`, `tests/codex-skeleton-hygiene/run.sh` (full suite,
including the `livefile-broken-invocation` / `livefile-stateful-trace` checks
over the three touched consumer files), `bin/check-pii-shapes.sh --base
5a5c09b`, `bin/check-board-headings.sh .shell-team/todo.md --base 5a5c09b`,
`bin/check-handoff.sh .shell-team/todo.md`, `shellcheck` at the pinned 0.11.0
over the full CI-wired file list, `bin/check-retro.sh .shell-team/retros/*.md`
and over the template, `tests/check-interventions/run.sh`,
`tests/check-board-headings/run.sh`, `tests/bin-exec-bit/run.sh`,
`bin/check-commit-identity.sh --base develop`, and the test suites for every
checker this task's own criteria exercise (`tests/check-provenance/run.sh`,
`tests/check-intent/run.sh`, `tests/check-refreeze-class/run.sh`,
`tests/check-pii-shapes/run.sh`, `tests/check-acs/run.sh`,
`tests/gen-playbook-blocks/run.sh`, `tests/check-prompt-sync/run.sh`) — **all
green**. I did not re-run the full ~50-step named-file list item by item a
second time (the engineer/coordinating-session hand-off already reports that
full run); I re-ran the directory/glob-scanning steps and the checkers this
task's own mechanism depends on myself rather than trusting the report.

### Recursive shapes (per-red causes verified)

- T-1029 (recursive `check-acs.sh`): **7 passed, 3 failed, 1 skipped** — reds
  are exactly `AC5, AC7, AC8`, the declared merge-point trio (base `2570d68`).
  Matches the spec's required shape exactly.
- T-1030 (recursive `check-acs.sh`): **9 passed, 2 failed, 1 skipped** — reds
  are exactly `AC8, AC9`, the declared merge-point pair (base `53955c5`);
  AC9 (the `skills/` byte-identical lock) is red **by design** because #143
  edited that tree. Matches the spec's required shape exactly.

### Mutation reproduction (scratch `git worktree`, never the working tree)

Created `git worktree add … HEAD --detach` outside the checkout.

1. **Rewritten mutation (the one this task's history specifically motivates)**:
   reverted T-1029's `F3` block to its base-ref (`5a5c09b`) bytes while
   leaving the rest of T-1029's v4 delta in place. Result: `AC1` → **FAIL**
   (three-way identity broken), `AC7` → **FAIL** (the `Q1`↔`F3` byte-identity
   clause), but the classifier sub-clause inside AC7 — run standalone —
   still reported **class-b, exit 1**. This confirms the spec's own claimed
   distinction: the remaining delta still classifies as class-b even with F3
   reverted, which is exactly why AC7 pins the identity clause separately
   from the classifier verdict. Restored; re-verified 13/0/1.
2. One word changed inside Q1 in `agents/qa-verifier.md` alone (`summarized`
   → `summarised`) → `AC1` → **FAIL** (equality lock, not containment).
   Restored; re-verified 13/0/1.

Real checkout (`shell-team/`) `git status --short` stayed empty throughout;
all mutation work happened in a detached worktree under
`scratch_t1036/wt`, removed with `git worktree remove --force` afterward.

### Judgment items

**(a) Invariance principle (AC7) and the final-state rewrites — no weakening
found.** AC7 no longer pins a spec-file line-count literal; instead it pins
(i) the intent block's `- check:` line count (10) and column-0 AC-bullet count
(11) — so nothing was silently added, removed, or de-recognised; (ii) the F3↔Q1
byte identity; (iii) the classifier exit status. All three are repair-count
agnostic and I confirmed all three measure as claimed. AC8's row-presence
assertion (`grep -qF` for the carve-out row's own anchor text, exactly once)
replaces the superseded "four added, three removed" whole-file diff-count
form. I judge the new form **equivalent-or-stronger** for the promotion
property it protects: a diff-count is an aggregate that could pass vacuously
if an unrelated addition and removal happened to cancel out elsewhere in the
same file, whereas a direct anchor-presence check on the specific row's own
text cannot be satisfied by anything except that row actually being there.
The trade is real (AC8 no longer detects "something else in the file also
changed by the same net amount") but that property was never what DP-5's
1:1-promotion claim was about, and AC5/AC6 already re-derive T-1030's four
pinned strings and three occurrence counts independently, so no coverage gap
opened. No promised property (classification, byte identity, criterion-count
integrity, pinned-string survival) was dropped; only the repair-count-coupled
proxy was, which is the whole point of the same-class-2 fix.

**(b) Ledger history accuracy against the interventions file.** Cross-checked
all 6 interventions entries against the corresponding board ratification
prose (v1→v2 AC9 population figures: intervention says board history carries
15, board ratification cites "the (v2)-shaped population is 15" — matches;
v2→v3 structural self-reference: intervention's stated root cause matches the
ratification's "coupled to T-1036's own intent-block version" text; T-1029
v3→v4 unblock: intervention's F2-anchor description matches the ratification's
"F2 exactly once inside the section... stable under the section's by-design
tail growth"; T-1036 v3→v4 same-class-2 full inventory: intervention names
AC7/AC9 as the two re-reddened criteria and commits to inventorying the whole
class, and the board ratification's scope (AC7/AC8/AC9/AC14) is a superset —
consistent with "sweep the entire intent block", not a fabrication; human-stop
entry's WIP commit claim matches the actual git log, `02080ac` carries exactly
the message the intervention describes). No inaccuracy found.

**(c) WIP commit `02080ac`.** Diffed `02080ac` against the final `3bc14a1` —
further edits landed after the WIP commit (confirmed: the Assumptions section
and AC7/AC8/AC9/AC14 prose kept changing between the two). `check-intent.sh`
reports **aligned** for T-1036 v4 against the *current* file bytes, which is
the only claim that matters: the intermediate WIP bytes were never
independently hashed or ratified, and the final v4 hash/attestation describe
exactly the bytes on disk now (verified above). Nothing from the WIP pass
survived as an unratified frozen claim.

### Summarized sources — verified against the sources themselves, not the spec's paraphrase

- `.shell-team/specs/T-1029-claim-fidelity-qa-step.md` — opened in full.
  Checked: Goal sentence (now reads "...together with the `## Summarized
  sources` declaration line itself, which sits outside that region by design
  and is checked in addition to those sites rather than in place of them" —
  the fixed wording, not the superseded "one such site and never the whole
  set" apposition); D3(ii) (now "plus the ... declaration line, which D1
  deliberately places outside that region and which is therefore an
  additional checked site rather than a member of it, and never a substitute
  for the sites above" — fixed); F3 (byte-identical to Q1, confirmed above);
  AC4's scope clause (`every site in the frozen region that restates, quotes
  or summarizes those facts`) — present, unmoved, confirmed by direct grep;
  F2 (`agents/pm-spec.md`'s bullet) — still reads "The declaration is an index
  into the frozen region, not the only place the summary lives" — confirmed
  unmoved, no defect; Input-space bullet — still reads "The QA item's scope
  is the intent block ... plus the declaration line" — confirmed unmoved, no
  defect; intent-block check-line/AC-bullet counts (10/11) — confirmed by
  direct `awk`/`grep` re-derivation, not `check-acs.sh` alone. **Result: the
  spec's carried-over distinction holds; both of DP-3's negative findings
  (F2, Input space carry no defect) independently re-confirmed.**
- `.shell-team/specs/T-1030-reviewer-board-write-boundary.md` — opened in
  full. Checked: F1 (four-member enumeration + carve-out, byte-matches Q2,
  confirmed above); D3 heading — now reads "...with one bounded carve-out for
  the tools it must run; everything not on it is read-only" (the carve-out is
  now folded into the heading — moved, as DP-5 requires); Input-space
  reachable-classes bullet — now names "every mandatory `codex exec`
  invocation writing the Codex CLI's own state directory... as a side effect
  this role neither requests nor controls" — moved, as DP-5 requires; Goal
  sentence — unchanged, still "closes the list of paths this role may write
  (...)" with no everything-else-is-read-only claim — confirmed **not**
  moved, as DP-5 requires; AC4's thirteen pinned strings + three occurrence
  counts — all re-derived directly against the file (not only via AC6);
  AC5's whole-file delta lock (four added / three removed against `53955c5`)
  — confirmed via the recursive `check-acs.sh` run (AC5 PASS, `add=4 del=3`
  measured, not just reported); AC9 (skills/ byte-identical to `53955c5`) —
  confirmed FAIL by design, since #143 edited that tree; check-line/AC-bullet
  counts (11/12) — confirmed by direct re-derivation. **Result: holds, no
  flattening found.**
- `.shell-team/specs/T-1028-class-m-refreeze.md` (AC10 only) — opened at
  AC10: confirmed live-lock window requirement text and the two suite
  names (`tests/codex-skeleton-hygiene/run.sh`, `bin/check-prompt-sync.sh`)
  match what T-1036's spec claims verbatim. **Result: holds.**
- `tests/codex-skeleton-hygiene/run.sh` (live-file lock section) — opened
  directly: `NEW_AC3` and `NEW_AC15` regex literals match the shapes T-1036's
  spec quotes verbatim, byte for byte. **Result: holds.**
- `.shell-team/specs/T-1035-spec-template-staleness-locks.md` — opened at its
  T-1033-referencing passage: the described re-freeze pattern (frozen
  ratification block, occurrence count of one, `check-refreeze-class.sh`
  against a materialised base blob requiring exit exactly 1, a
  deletion-column lock identifying the removed line, GO grounds in the
  ratification line itself) matches T-1036's spec's restatement of the
  pattern verbatim. **Result: holds.**
- `.shell-team/todo.md` (`## Done` entries for T-1029/T-1030/T-1035) —
  read directly as part of the ledger audit above; hash versions and
  attestation-line shapes confirmed. **Result: holds.**
- `bin/check-refreeze-class.sh` (header contract) — opened directly: exit
  contract (0=mechanics / 1=class-b / 2=usage-or-structural) and the
  same-line-count / differing-index rules match the spec's restatement.
  **Result: holds.**
- `agents/qa-verifier.md` (line 65), `agents/codex-reviewer.md` (lines
  156–157), `skills/run/SKILL.md` (line 81), `skills/goal/SKILL.md` (line
  233) — the four shipped lines — checked directly above (byte match,
  adjacency, marker-region exclusion via AC12's scratch regeneration, which
  I did not re-derive independently beyond re-running the same script — this
  one clause rests on `check-acs.sh`'s own AC12 machinery rather than a
  second independent read).
- **GitHub issues #141, #143, #144 — `environmentally-unverified`.** No
  network access in this session: `gh issue view 141` failed with a sandbox
  TLS-trust error (`x509: OSStatus -26276`), consistent with this
  environment's known sandbox network restriction. This is the outcome the
  spec itself predicts for any role without network access and is recorded
  here rather than silently omitted; it does not by itself drive a FAIL.
- **The tech-lead's measured facts and the 2026-08-05 planning GO — relayed,
  not independently verifiable from this session** (no access to the
  conversational record that produced them). Cross-checked instead against
  the interventions file (judgment item (b) above), which is the strongest
  available re-measurement per the spec's own Assumptions section.

### Acceptance criteria

13/13 machine-checked criteria PASS (AC1–AC13), AC14 SKIP by design (runtime,
no `check:` line) — all independently re-run and eyeballed as detailed above,
not taken from the hand-off's report.

### Edge cases tried

- The rewritten mutation (F3 reverted to base bytes) as a synthesized fixture
  aimed at the exact failure mode this task's own freeze history produced
  (AC7's identity clause vs. its classifier sub-clause) — both surfaces
  behaved exactly as the spec predicts.
- A repository network-boundary check (issue fetch attempt) to confirm the
  `environmentally-unverified` disposition is genuinely forced by environment
  and not merely assumed.
- Cross-task ledger interference check: confirmed the two re-frozen tasks'
  (T-1029, T-1030) own recursive `check-acs.sh` shapes match their spec's
  stated required PASS/FAIL sets exactly, so nothing this task's re-freezes
  did leaked into those specs' other criteria.

No edge case here is grounded only in an out-of-scope synthetic extreme; the
spec's own Non-goals/Input-space out-of-scope list (escalating scale,
adversarial paths, non-GNU tool variants, a future `$CODEX_HOME` rename) was
not needed to produce a finding, because none was found.

### Risk notes for reviewer

- `environmentally-unverified: GitHub issues #141, #143, #144` — no network
  access this session; the spec itself predicts and accepts this outcome for
  any role without `gh`/network access.
- The four shipped lines' marker-region exclusion (AC12) rests on
  `bin/gen-playbook-blocks.sh`'s own scratch regeneration, re-run by me but
  not independently re-derived by hand a second time beyond the script.
- AC8's row-presence assertion trades detection of "unrelated changes
  elsewhere in the file that happen to net out" for a direct, non-vacuous
  anchor check — judged equivalent-or-stronger for the specific promotion
  property it protects (judgment item (a) above); flagged for the reviewer's
  own read since it is a genuine trade, not a pure strengthening.
- This task's freeze history (four ratified repairs to its own AC9/AC7/AC8/
  AC14, three of them firing the same coupling class before the same-class-2
  fix) is unusually deep for a wording-only task; I read the whole
  interventions trail and cross-checked it against the board (judgment item
  (b)), and found it internally consistent, but the reviewer may want to read
  it independently given the volume.

Full record: this file.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1036
- Codex model: gpt-5.6-sol (OpenAI Codex CLI v0.145.0, reasoning effort high)
- Verification mode: executed-tests
- Diff scope: 11 files, +742/-17 lines (`git diff --stat 5a5c09b...HEAD`)

Independent verification performed before invoking Codex: re-ran `bin/check-acs.sh` on
`.shell-team/specs/T-1036-wording-batch-141-143-144.md` live → **13 passed, 0 failed, 1
skipped**, matching QA's report exactly. Re-ran `bin/check-intent.sh` for T-1029 (v4),
T-1030 (v2) and T-1036 (v4) → all **aligned**. Read the full diffs of the four shipped
lines (Q1–Q4) and both re-frozen specs (T-1029, T-1030) directly against the spec's frozen
`Q1`–`Q4`/`R29`/`R30` blocks — all three-way byte identities and the shipped bytes match the
spec exactly, as QA reported.

Invoked Codex twice: a primary `codex exec review --base 5a5c09b --json` pass (foreground,
exit 0), and — because the primary pass returned P2/Major findings — an adversarial
devil's-advocate pass targeting the same-class risk in T-1029's AC2/AC7/AC11 and T-1036's
AC9 (foreground, exit 0). Two of the adversarial pass's claims were independently
reproduced live against the actual checkout (not taken on Codex's word):

1. **T-1029 AC2 false-pass, reproduced**: copied `agents/pm-spec.md` to a scratch file,
   deleted line 74 (the `Body-to-AC 1:1 promotion check` bullet) while leaving line 76's
   cross-reference prose ("This is orthogonal to the Body-to-AC 1:1 promotion check
   above") intact, and ran AC2's actual `check:` line against the mutated copy — **it
   reported PASS (rc=0)** despite the guarded bullet being gone. The check's
   `grep -nF -- "$s" "$T/sec" | head -n 1` is an unanchored substring search, so a
   surviving mention of the bullet's title elsewhere in the section satisfies it.
2. **T-1036 AC9 attestation-collision, reproduced**: `grep -n "freeze-attestation (v1,
   2026-08-04): lines=10/10" .shell-team/todo.md` returns **two** hits — T-1029's own v1
   attestation (the one AC9 means to check survives) and an unrelated, already-merged
   task's (T-1024's) attestation line, which happens to be byte-identical through the
   pinned prefix. AC9's "named base-line survival" clause is a board-wide `grep -qF`, so
   deleting T-1029's real v1 attestation would still satisfy this specific sub-clause
   because of the collision.

Both are the same root-cause class: an accounting/presence `check:` line keyed on an
**unanchored substring or whole-board search** rather than an anchored, entry-scoped
match. This class now has **two independent occurrences within this branch's own newly
authored or repaired check lines** (T-1029's AC2, rewritten by this branch's v3→v4
"unblocking repair"; T-1036's AC9, rewritten across this branch's own v1→v2→v3→v4 chain).
Per `skills/run/SKILL.md`'s own same-class-2 rule, this crosses the threshold that should
switch the rework instruction from "fix the reported spot" to a full-inventory-and-bulk-
apply pass across every check line this branch wrote or rewrote (at minimum AC2 and AC9;
AC7/AC8's cardinality-only proofs and AC3/AC4/AC10's unchanged, pre-existing instances of
the same shape in T-1029 are worth including in that one inventory pass rather than being
patched piecemeal across further rounds) — flagging this explicitly since it is a
procedural trigger this repo's own loop is supposed to act on, not merely a stylistic note.

#### Blockers

None. Per this task's own pre-commitment (P2), a re-freeze only reverts on a **second**
consecutive round of independent Blocker/Major findings against the same surface; this is
round 1.

#### Major

- `.shell-team/specs/T-1029-claim-fidelity-qa-step.md:89` (AC2's `check:` line, part of the
  disclosed T-1029 v3→v4 "unblocking repair") — **reproduced false-pass**: the ordering
  lock's four required-bullet lookups (`Body-to-AC 1:1 promotion check`, `Negative-AC
  guidance`, `Input-space definition check`, `Diff-scope-closure staleness norm`) are each
  found with unanchored `grep -nF ... | head -n 1` against the whole `## Spec completion
  self-check` section, and the ≥5-bullet floor doesn't protect any individual member (the
  section already carries 7 bullets on the live file, so the floor is slack). Deleting the
  real `Body-to-AC 1:1 promotion check` bullet while leaving the Input-space bullet's own
  cross-reference to that phrase intact makes AC2 report PASS — confirmed live against a
  scratch copy of the real file. **Suggested fix**: anchor each lookup to the bullet's own
  heading shape (e.g. `^- \*\*<label>\*\*:`) rather than a bare substring, and locate the
  anchored line rather than the first substring match, before comparing positions. This is
  frozen-region text inside T-1029's intent block; the repair routes through pm-spec as a
  further class-B re-freeze (T-1029 v4→v5), not an engineer edit.
- `.shell-team/todo.md` (AC9's attestation-survival clause in
  `.shell-team/specs/T-1036-wording-batch-141-143-144.md:133`, rewritten across this
  branch's own T-1036 v1→v2→v3→v4 chain) — **reproduced collision, partially mitigated**:
  the exact string `freeze-attestation (v1, 2026-08-04): lines=10/10` occurs twice on the
  live board (T-1029's own entry and T-1024's, an unrelated already-merged task), so AC9's
  whole-board `grep -qF` for that fixed string would still pass if T-1029's real v1
  attestation were deleted. Mitigating factor, measured rather than assumed: AC11 re-runs
  `bin/check-intent.sh` per task, which scopes to that task's own board entry and requires
  a conformant attestation for every version v1..vN, so the same deletion would separately
  fail AC11 — the end-to-end property (an attestation cannot silently vanish) is not
  actually broken today, only AC9's own stated proof of it is vacuous in isolation.
  Recorded as Major rather than Minor because it is live and reproduced (not hypothetical)
  and because AC9's own prose presents "named base-line survival" as its own affirmative
  proof rather than as a clause explicitly deferring to AC11. **Suggested fix**: scope the
  survival grep to the task's own board entry (the same entry-extraction AC9 already uses
  for R29/R30's occurrence count) rather than the whole board.

#### Minor / nits

- T-1036's frozen Goal sentence (`.shell-team/specs/T-1036-wording-batch-141-143-144.md:32`)
  still narrates "T-1029 goes v2 → v3" with one new attestation; T-1029 subsequently
  reached v4 via a separate, disclosed unblocking repair later in the same branch. No
  `check:` line depends on this sentence's version count (AC7/AC11 are deliberately
  version-count-agnostic per the invariance principle), so this is a documentation-accuracy
  gap rather than a verification gap. Codex's primary pass rated this P2/Major; downgraded
  here (see ledger).
- `.shell-team/specs/T-1029-claim-fidelity-qa-step.md:215` ("Order of work" step 2 still
  says "append F2 as the last bullet") contradicts the Traps section's now-corrected
  "AC2 is an ordering lock, not a tail pin" text a few dozen lines below it. This sits
  outside the frozen intent block and outside the lines DP-3/DP-5 name, so touching it is
  arguably outside this task's own Non-goals scope; it is historical narration of a step
  already completed, not a live instruction. Codex's primary pass rated this P2/Major;
  downgraded here (see ledger).
- `agents/codex-reviewer.md:157` — the carve-out's interaction with an in-checkout
  `$CODEX_HOME` is genuinely ambiguous by the letter of the text, but this exact scenario is
  named verbatim in the spec's own Input-space Out-of-scope list ("A future rename of
  `$CODEX_HOME`, a change to the Codex CLI's state layout, or an environment where
  `$CODEX_HOME` is set inside the checkout") — downgraded per the out-of-input-space rule
  (see ledger); it is not a security/trust-boundary finding (no attacker-controlled input),
  so the downgrade applies.
- T-1036's AC7/AC8 cardinality-only proofs (check-line-count + AC-bullet-count + byte
  identity + classifier exit) don't verify per-criterion identity, only totals — Codex's
  adversarial pass rated this Major; downgraded to Minor here. This is an already-disclosed,
  reasoned trade-off (QA's own review record, judgment item (a), flags exactly this
  trade for AC8 and judges it equivalent-or-stronger for the property it actually
  protects), not a live-reproduced exploit, and hardening it further risks reintroducing
  the repair-count-coupling fragility this same task fought three rounds to eliminate.
  Worth folding into the same-class-2 inventory above rather than fixing in isolation.
- T-1036's AC14(b) mutation-oracle text ("moving Q3 outside its window ... T-1028's own
  AC10 red") does not match the measured mutation result recorded on the board (T-1028's
  AC10 in fact stayed PASS, because it pins a distinct line, F4, not Q3) — but the board's
  own QA mutation-reproduction entry already discloses this exact discrepancy
  ("Measured discrepancy from the spec's own AC14(b) prediction..."), so this is a
  transparently-recorded prediction miss, not a silent one. AC14 is `SKIP`-by-design
  (no `check:` line). Codex's adversarial pass rated this Major; downgraded to Minor here.
- T-1029's AC3 (adjacency), AC4 (vocabulary-reuse count) and AC10 (self-application
  accounting) exhibit the identical unanchored-substring shape as AC2, per the adversarial
  pass — but these three criteria are **unchanged by this branch** (byte-identical to the
  pre-`5a5c09b` state, part of T-1029's earlier, already-reviewed v1/v2 freeze), so they
  are out of scope for this round's verdict on this branch's diff. Recorded here as a fast-
  follow candidate for the same inventory pass named above, not as a finding against this
  branch.

#### Adversarial findings

Devil's-advocate pass targeting T-1029's AC2/AC7/AC11 and T-1036's AC9, explicitly primed
with the AC2 exploit already found in the primary pass, and asked to find further instances
of the same shape. It returned 10 findings; two were independently reproduced live (see
above, now folded into Major); the remainder (AC7/AC8 cardinality gaps, AC14's stale
mutation-oracle text, and AC3/AC4/AC10's pre-existing instances of the same shape) are
folded into Minor above with reasoning for each downgrade. No adversarial finding was
rejected outright — every one pointed at a real shape in the file, even where I judged the
practical severity lower than Codex's own label.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | T-1036 Goal sentence narrates T-1029 at v3, actual is v4 | primary: P2 (major) | minor | no `check:` line depends on the Goal sentence's version count (AC7/AC11 are deliberately version-agnostic); documentation-accuracy gap, not a verification gap |
| 2 | T-1029 "Order of work" step 2 contradicts the Traps section on F2 placement | primary: P2 (major) | minor | historical narration of an already-completed step, outside the frozen intent block and outside DP-3/DP-5's named delta; fixing it is arguably outside this task's own Non-goals |
| 3 | `agents/codex-reviewer.md`'s carve-out ambiguous for an in-checkout `$CODEX_HOME` | primary: P2 (major) | minor, out-of-input-space | the spec's Input-space Out-of-scope list names this exact scenario verbatim ("an environment where `$CODEX_HOME` is set inside the checkout"); not a security/attacker-controlled-input finding, so the downgrade applies |
| 4 | T-1036 AC7 / T-1030-scoped AC8 cardinality-only proofs don't verify per-criterion identity | adversarial: major | minor | already disclosed and judged equivalent-or-stronger by QA's own review record (judgment item (a)) for the specific property protected; not live-reproduced; further hardening risks reintroducing the eliminated repair-count-coupling class |
| 5 | T-1036 AC14(b)'s mutation-oracle text doesn't match the measured T-1028/AC10 result | adversarial: major | minor | the board's own QA record already discloses this exact discrepancy transparently; AC14 is `SKIP`-by-design with no `check:` line |
| 6 | T-1029 AC3/AC4/AC10 share AC2's unanchored-substring shape | adversarial: minor | minor, out of scope for this round | all three are byte-identical to the pre-`5a5c09b` state (unchanged by this branch); real, but not this branch's defect — recorded as a fast-follow candidate for the same inventory pass |
| 7 | T-1029 AC2 unanchored-substring false-pass (reproduced) | primary: P2 (major); adversarial: major (elaborated to all four bullets) | major | kept — live-reproduced against the real file, unbackstopped by any other criterion |
| 8 | T-1036 AC9 attestation-survival board-wide collision (reproduced) | adversarial: minor | major | **raised**, not downgraded — live-reproduced against the real board, and AC9's own prose claims this as its affirmative proof rather than deferring to AC11; the AC11 backstop mitigates the end-to-end risk but does not make AC9's own clause sound, which is why this is recorded above Codex's own label rather than at it |

#### Recommendation

- REQUEST_CHANGES → set back to READY_FOR_ENG (routed through pm-spec, since both Major
  findings sit inside frozen `check:` lines: T-1029's AC2 inside its intent block, and
  T-1036's AC9 inside its own intent block). Per this task's own P2 pre-commitment, this
  round's findings do not by themselves revert either re-freeze — a revert triggers only on
  a second consecutive round of independent new Blocker/Major findings against the same
  surface. Given the same-class-2 threshold is already crossed across two surfaces in one
  round, recommend the rework instruction be a full-inventory-and-bulk-apply pass (per
  `skills/run/SKILL.md`'s own same-class-2 rule) covering every check line this branch wrote
  or rewrote, rather than two isolated point-fixes.

#### Fast-follow disposition

- T-1029's AC3/AC4/AC10 unanchored-substring shape (ledger #6) — **file-an-issue**:
  candidate for the same hardening pass as the two Major findings above, but explicitly out
  of this branch's diff, so it should not block this round; rationale is that patching only
  the two surfaces this branch touched while leaving three pre-existing instances of the
  identical shape in the same file is an incomplete fix once the class is named.
- T-1036's Goal-sentence version staleness (ledger #1) and T-1029's "Order of work" stale
  placement note (ledger #2) — **won't-fix for this round, file-an-issue for a future
  editorial pass**: both are narrative/documentation-accuracy gaps with no machine-checked
  dependency; re-freezing either now would be a seventh and eighth ratified repair to specs
  that have already absorbed six, for a purely cosmetic gain, which is disproportionate
  given this task's own demonstrated fragility history. A dedicated low-stakes editorial
  cleanup pass (covering both, plus any other frozen-block prose that drifted after a later
  re-freeze) is the more proportionate venue.
- The `$CODEX_HOME`-inside-checkout ambiguity (ledger #3) — **won't-fix**: the spec's own
  Input-space Out-of-scope list already declines this scenario by name; no further action
  needed unless that declaration itself is revisited.
- T-1036's AC7/AC8 cardinality-only proofs (ledger #4) and AC14's stale mutation-oracle text
  (ledger #5) — **file-an-issue**: fold into the same hardening pass as AC2/AC9 above (same-
  class-2 inventory), rather than separate issues, since they share the identical root
  cause (accounting checks proving counts/labels rather than identity/content).

