# Codex cross-provider review — T-1150

## Round 1

### Codex Review verdict: APPROVE
- Task: T-1150
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 12 files, +502/-17 lines (`daf43bcb...806bd684`)

#### Blockers

None.

#### Major

None. Both raw Codex findings below were downgraded on independent judgment, confirmed by a follow-up adversarial pass.

#### Minor / nits

- `templates/prompt-blocks/host-dispatch.md:7-8` (spliced into `skills/run/SKILL.md`) — class `unmeasured-loading-path` (raw label `[P1]`). The primary pass observed that `bin/gen-codex-agents.sh` generates each Codex custom-agent TOML from `agents/<role>.md` only, while `templates/prompt-blocks/registry.txt` registers `host-dispatch.md`'s `contain`-mode consumer copy only into `skills/run/SKILL.md` — a Claude Code skill file — so nothing in this diff demonstrates that the two new paragraphs reach whatever process is actually driving `spawn_agent` calls on a Codex CLI host. This is factually accurate as far as it goes (confirmed by both this review's own read and the adversarial pass, citing `bin/gen-codex-agents.sh:2-10,274-276,339-385` and `templates/prompt-blocks/registry.txt:52-57`), but it names a pre-existing, disclosed architectural limitation rather than a defect this diff introduces: the identical gap already existed for the Codex-host dispatch paragraph these two new paragraphs sit beside, shipped unchanged since T-1134/T-1135 (confirmed against `177ca55f^:templates/prompt-blocks/host-dispatch.md:5`, byte-unedited by this task). The spec's own `## Input space` names this exact class as an out-of-scope synthetic extreme — "What a Codex CLI orchestrator loads as instruction, at any path and in any form. Unmeasurable from this repository, disclosed rather than asserted, and forbidden as a claim by AC18" (`.shell-team/specs/T-1150-codex-wait-and-docs-parity.md:185`) — and `README.md:84`'s own standing disclosure ("loads automatically is unmeasured from this repository") makes the same admission independently of this task. AC20 explicitly places "an orchestrator reading the shipped dispatch text and runbook step 10" above this task's own verification ceiling, owned by "the operator's next Codex-host adopter run" (`:155-156`). Separately, and independently of the loading-path question, this diff's primary delivery surface for the rule is `docs/adopting.md`/`.ja.md` runbook step 10's own body prose (AC6) — human-facing documentation the Goal statement frames as "the runbook step they actually follow" — which states the substance of the wait rule directly, with no dependency on any automatic-loading mechanism.
  - Suggested fix: none owed by this task. A durable fix (making a Codex-host orchestrator's own instruction-loading path measurable, or wiring the rule into `bin/gen-codex-agents.sh`'s generation path) is a mechanism change this spec's own Non-goals and AC18 explicitly decline to make, and would need its own spec.
- `docs/templates/release-notes-template.md:64-67` — class `pointer-completeness` (raw label `[P2]`). The primary pass observed that the fresh-install Codex CLI block gives only two `codex plugin` commands with no mention of running `bin/gen-codex-agents.sh`, granting repository trust, or the sandbox writable-root/network grants `docs/adopting.md`'s runbook requires, and that the "Already installed?" guidance under-restates the runbook's own staleness-detection steps. Read against the template's own committed shape and the spec's D-9 design decision, this is consistent with, not a violation of, the shipped design: the section's own instructions call `## Install / update` static boilerplate copied unchanged (`docs/templates/release-notes-template.md:31-34`), D-9 states explicitly that "the generator step is pointed at rather than restated" (`.shell-team/specs/T-1150-codex-wait-and-docs-parity.md:64`), and the immediately-preceding Claude Code block in the same file is equally terse about that host's own onboarding (no mention of authentication, trust, or any Claude-side prerequisite either). The shipped "Already installed?" sentence does link to the adopting runbook by name for the regeneration step, which is what AC16's own `adopting` token requirement asks for. The adversarial pass, briefed on both halves of this finding, reached the same reject verdict independently, citing `docs/templates/release-notes-template.md:49-60` (the parallel Claude Code terseness) and `docs/adopting.md:443-505`/`:592-605` (where the full generator/trust/sandbox/staleness detail actually lives).
  - Suggested fix: none required for this task's own acceptance criteria. A one-line pointer to the generator step immediately after the fresh-install commands (not only in the "Already installed?" branch) would be a small, independent improvement to the template's own usefulness — filed as a fast-follow, not a defect in this diff.

#### Adversarial findings

Triggered by the primary pass's two raw `[P1]`/`[P2]` findings (blocker/major on the raw severity-normalization table). Framed as devil's-advocate on both candidates directly, naming the specific files, line ranges and the spec's own Non-goals/D-9/Input-space text to weigh them against, and asked for an explicit agree-as-major / downgrade-to-minor / reject verdict on each with citations. Rejected both as gating findings, independently confirming this review's own reading: Finding A ("these paragraphs don't reach the parent Codex orchestrator") is "a disclosed, pre-existing architectural limitation—not a new P1 regression," present identically in the parent commit before this task; Finding B (release-notes template completeness) is "consistent with... this section's explicit shipped design as a byte-identical, forward-only pointer to the runbook rather than a restatement of the runbook's own onboarding steps," matching the Claude Code block's own equal terseness in the same file. No new finding was surfaced by the adversarial pass beyond confirming the reject verdict on both candidates.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `host-dispatch.md`/`SKILL.md` new wait/re-freeze paragraphs "do not reach the parent Codex orchestrator" | primary: `[P1]`/blocker; adversarial: reject | minor, `out-of-input-space` | matches the spec's own `## Input space` Out-of-scope item "What a Codex CLI orchestrator loads as instruction, at any path and in any form. Unmeasurable from this repository, disclosed rather than asserted, and forbidden as a claim by AC18" (`.shell-team/specs/T-1150-codex-wait-and-docs-parity.md:185`); identical gap pre-exists this task's diff (confirmed against `177ca55f^`); AC20 places this above the declared verification ceiling; the runbook's own step-10 body prose independently delivers the rule's substance with no dependency on the disputed loading path |
| 2 | release-notes template's Codex block omits generator/trust/sandbox/staleness detail present in the full runbook | primary: `[P2]`/major; adversarial: reject | minor | consistent with D-9's explicit design decision ("the generator step is pointed at rather than restated") and with the Claude Code block's own equal terseness in the same file; not one of `## Review depth`'s five enumerated Major-trigger classes (parity overclaim, command-byte mismatch, missing host qualifier, Claude-Code-dispatch regression, Codex-loading claim) |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE.

#### Fast-follow disposition

- Finding 1 (host-dispatch.md/SKILL.md paragraphs' reach onto whatever process actually drives a Codex-host `spawn_agent` orchestrator) — Intent: won't-fix. This is not a gap this task created; it restates an already-standing, explicitly named architectural boundary this repository has declined to close across at least three prior tasks (this spec's own Input-space item, T-1148's Non-goal, and the broader "unmeasured from this repository" disclosure at `README.md:84`). Filing a new issue would duplicate a decision already on record rather than surface anything new.
- Finding 2 (release-notes template's Codex block has no fresh-install pointer to the generator step, only an "already installed" one) — Intent: file-an-issue, folded into issue #573 (the release-notes-template group this task's own droppable-order already names). Rationale: cheap, low-risk documentation addition (one sentence, one link) that would make the template a slightly more complete quick reference without touching any AC-pinned literal; not required by this task's own frozen criteria or design decisions, so it is deferred rather than done in this round.

## Per-pass provenance

### Round 1

Round 1's primary pass ran the `codex exec … review --base daf43bcb --json` subcommand — a base-diff review with no free-form prompt, as prescribed by the default fresh-review path. The base ref is `daf43bcb` (= `develop`), per this task's own frozen `base-ref-discriminator: not-applicable` — the branch was cut directly from `develop` with no open predecessor pull request.

- pass-role (primary): generation
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base daf43bcb --json -o <repo-root>/.shell-team/reviews/T-1150-codex-primary.txt > <repo-root>/.shell-team/reviews/T-1150-codex-primary.jsonl 2>&1`
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; ran exactly the role file's primary-pass skeleton with no deviation.
- raw-capture (primary): T-1150-codex-primary
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Round 1's adversarial pass was triggered by the primary pass's two raw `[P1]`/`[P2]` findings (blocker/major on the raw severity-normalization table), per the role file's step-5 mandate; it was framed as a devil's-advocate pass naming both candidates verbatim, their concrete grounding (the generator/registry read for Finding A, the template's own committed shape and D-9 for Finding B), the specific spec sections to weigh them against (Non-goals, Input space, D-1, D-9), and an explicit agree-as-major / downgrade-to-minor / reject instruction per finding.

- pass-role (adversarial): generation
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/T-1150-codex-adversarial.txt "Play devil's advocate on two findings from a prior review pass, against this repository's actual committed files (read them yourself with git show / sed / cat -- do not expect any file content in this prompt). Finding A (raised P1/blocker): ... [full Finding A text, citing templates/prompt-blocks/host-dispatch.md, skills/run/SKILL.md, docs/adopting.md's Codex CLI section, README.md's 'loads automatically is unmeasured' disclosure, and the spec's Non-goals/Input-space/D-1] ... Finding B (raised P2/major): ... [full Finding B text, citing docs/templates/release-notes-template.md's diff, the spec's D-9 and its release-notes-template Non-goal, and the adjacent Claude Code block] ... For each finding return a verdict: agree-as-major, downgrade-to-minor, or reject, with your reasoning grounded in what you actually read in the repository's files (cite paths/lines)." > <repo-root>/.shell-team/reviews/T-1150-codex-adversarial.jsonl 2>&1`
- briefing-fidelity (adversarial): carried — both candidates, their concrete grounding, and the specific spec sections and files to weigh them against were all relayed verbatim into the prompt, per step 5's own instruction to target "those specific files."
- raw-capture (adversarial): T-1150-codex-adversarial
- self-detected-host (adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Raw captures: `T-1150-codex-primary.{txt,jsonl}`, `T-1150-codex-adversarial.{txt,jsonl}` in this directory.

## Independent verification performed this round (beyond the two Codex passes)

- Read the frozen spec in full (`.shell-team/specs/T-1150-codex-wait-and-docs-parity.md`), the board's freeze-attestation, both engineer hand-offs (round 1 and round 2), the QA verdict, `.shell-team/provenance/T-1150.md` (2 decision entries) and `.shell-team/interventions/T-1150.md` (1 `work-abandoned` entry, no bytes written to the tree by the stalled job) before reading any diff.
- `git diff daf43bcb...HEAD` read directly, file by file, for all eight edited shipped files — not summarized from the engineer's or QA's own reports — confirming the two new host-dispatch.md paragraphs sit exactly where D-2 requires (after the existing `On the **Codex CLI** host:` paragraph, before `**The review step, on the Codex CLI host (T-1135).**`), that the `skills/run/SKILL.md` consumer copy is byte-identical to the canonical file's new lines, that both READMEs' four AC8-AC11 surfaces read as the spec describes, that `docs/distribution.md` has zero diff against base (Non-goal respected), and that `docs/templates/release-notes-template.md`'s new Codex block sits between the existing marker comments without touching them.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1150-codex-wait-and-docs-parity.md 2>&1 | tail -5` (own run) → `check-acs: 19 passed, 0 failed, 1 skipped, 0 unrecognized` — matches the engineer's round 2 and QA's own reported runs.
- `git check-ignore -v` (own run) confirming the raw Codex capture files this review itself publishes (`T-1150-codex-{primary,adversarial}.{txt,jsonl}`) are covered by `.gitignore:15`/`:17`, so they do not appear in AC19's `git ls-files --others --exclude-standard` read and do not require allow-listing.
- Reviewed the spec's `## Review depth` section's explicit five-item Major-trigger list and confirmed neither of the primary pass's two raw findings matches any of the five (parity claim exceeding what ships per host; printed command differing from its shipped source; host-behaviour sentence with no host qualifier; wait rule changing the Claude Code host's own dispatch; a claim about what a Codex CLI host loads) — the closest, Finding 1, is a concern about loading-path reachability rather than a claim made in the shipped text about what is loaded, and is independently covered by the spec's own Input-space Out-of-scope item and AC20's above-ceiling marker.
- Nothing this review could not judge from the static, read-only sandbox this round: AC20 (a real Codex CLI host actually waiting on a spawned role per the shipped text) stays above this task's own declared verification ceiling — the spec's own `- above-ceiling:` marker names "the operator's next Codex-host adopter run" as its owner, and this review does not substitute a claim for that measurement.
