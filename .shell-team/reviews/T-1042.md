### Codex Review verdict: REQUEST_CHANGES
- Task: T-1042
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: static-only
- Diff scope: 15 files, +938/-15 lines (`git diff --stat 6439eb6...HEAD`)

Two `codex exec` passes were run, both reading the real working tree in a read-only sandbox (no diff or source pasted into either prompt): a primary `review --base 6439eb6 --json` pass, and — triggered by the primary pass's own Major findings, per this role's step-5 rule — an adversarial "what could break `warn_ignored_base()`" pass scoped to `bin/team-paths.sh` and `bin/team-init.sh`. Every finding below was re-derived independently in a scratch `git init` fixture before being kept in this verdict (four separate live repros run by this reviewer, not just re-stated from Codex's text) — the two `internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation)` items below are the exception, since GitHub issues #167/#67 are unreachable from this sandbox.

Mandatory review items from the routing map, checked directly against the diff (not delegated):
- **(a) `--export`/`--get` immunity** — confirmed: `warn_ignored_base` is called only from the `print)` case arm of `bin/team-paths.sh`'s mode dispatch (line 264), never from `export)` or `get)`. Those two modes' stdout and exit status are untouched by this task. The *other* half of (a) — that undeterminable can never be silently classified as "not ignored" — is where this round's Major findings live (see M2, M3 below): the probe *can* be silently classified wrong in two distinct, reproduced ways.
- **(b) D2's advisory-not-fail-closed argument** — read in full; it is argued (checkers-vs-resolvers distinction, README/docs/adopting.md's existing "ignoring the base dir is supported" language, D6 carrying the fail-closed discipline's applicable form), not assumed. No finding.
- **(c) retro-inputs.sh's inverted-default invariant** — confirmed intact: the only line touched is the `report_dir_input review-artifacts` call's suffix argument (`".md"` → `""`), reusing the existing `case "$f" in *"$suffix")` arm so an empty suffix degenerates to match-all; `-f "$f"` and the nullglob-without-dotglob enumeration are unchanged, so dotfiles/subdirectories stay excluded and `note`/`promote_empty`/`promote_read`'s control flow is byte-identical. No finding.
- **(d) Problem statement's measured-vs-relayed discrimination** — confirmed: `## Problem` and D1 explicitly state the relayed `specs`-symptom mechanism "does not hold" and give the measured `TEAM_SPECS_DIR`/line-372/line-454 discrimination in its place. No finding.
- **(e) Board diff structural purity** — `git diff 6439eb6...HEAD -- .shell-team/todo.md` is a pure addition of one new `## Active` entry for T-1042; no other task's entry, heading, or ordering is touched. No finding.

#### Blockers
(none)

#### Major
- `bin/team-paths.sh:84` and `bin/team-init.sh:87` (mirrored) — **M1: pathspec metacharacters in `$base` defeat the tracked-file and ignore checks.** `validate_base` explicitly permits glob-shaped names (its own comment: "a base like 'a*' is safe" — true for shell word-splitting, not for git). Live repro: a repo where `abc/file.txt` is tracked and a directory `a-star-dir/` is the *actual* ignored, untracked target — `git ls-files -- "a*"` returns `abc/file.txt` (an unrelated tracked file), making `tracked` non-empty and silently suppressing the notice; independently, `git check-ignore -q -- "a*"` itself answers "not ignored" (exit 1) even though `a-star-dir/` really is. Suggested fix: force literal pathspec interpretation (`:(literal)$base` or `-c core.literalPathspecs=true`) in both scripts' `ls-files` and `check-ignore` calls.
- `bin/team-paths.sh:74-84` and `bin/team-init.sh:77-87` (mirrored) — **M2: `git rev-parse --is-inside-work-tree`'s exit status alone is not a reliable N2/N3 discriminator.** The probe only checks the command's exit status and discards stdout. Live repro: inside a **bare** repository, `rev-parse --is-inside-work-tree` exits 0 while printing `false` — the probe treats this as "inside a work tree," falls through to `check-ignore`, which then fails with exit 128 ("fatal: this operation must be run in a work tree"), landing in the `*)` branch and emitting **N3** ("undeterminable") instead of the correct **N2** ("outside a git work tree"). The same unchecked-exit-status logic runs the opposite direction for "dubious ownership" / permission-denied / corrupted-repo failures: those make `rev-parse` exit non-zero for a reason that is *not* "outside a work tree," yet the current `case` sends every non-zero exit straight to N2, misclassifying an undeterminable state as a definite one. Suggested fix: capture and check stdout (`out=$(git ... rev-parse --is-inside-work-tree 2>/dev/null); rc=$?`) — treat `rc=0 && out=true` as inside, `rc=0 && out=false` as N2, and any other outcome as N3 rather than N2.
- `bin/team-paths.sh` (`--print`, line ~88) — **M3: `--print` silently misses the docs/adopting.md-recommended directory-form ignore rule when the base dir does not yet exist on disk.** Live repro: a fresh repo whose committed `.gitignore` reads `.shell-team/` (the directory form D6/AC7's `LA4` fixture and `docs/adopting.md` both recommend) — `git check-ignore -q -- ".shell-team"` returns exit **1** ("not ignored") when the `.shell-team` directory has not been created yet, and exit **0** once it exists. `bin/team-init.sh`'s call site is immune by construction (D9: probed *after* scaffolding creates the dir), but `--print` has no scaffolding step and can genuinely be invoked before the base dir exists (e.g., an operator inspecting a fresh clone). AC7's own `LA4` fixture always `mkdir -p`s the directory before invoking `--print`, so this gap is untested as well as unfixed — this is exactly the "silence means not ignored" failure mode mandatory item (a) asks this review to rule out, and it is not ruled out for the one documented, recommended ignore-rule form. Suggested fix: normalize the check-ignore query to an explicit directory form regardless of on-disk existence (e.g. `check-ignore -- "./${base%/}/"` or a synthetic descendant probe), and add a fixture where the base dir is absent.
- `bin/team-paths.sh:99` / `bin/team-init.sh:102` (the frozen N1 body) — **M4: N1's "specs" claim is false for the legacy split layout.** In legacy mode `BASE="tasks"` while `SPECS="docs/specs"` (a sibling top-level directory, confirmed at `bin/team-paths.sh:203-205`) — the *only* one of the five nouns N1 lists that is not always `"$BASE/…"`. If only `tasks/` is ignored (and nothing under it tracked) while `docs/specs` is not, the fired N1 notice still asserts "...the board, specs, provenance, interventions and review records written there cannot be committed..." — false for specs in exactly this state. `bin/team-init.sh` itself already carries adjacent, specs-split-aware logic a few lines below this same call site (line 311-312's `if [ "$TEAM_SPECS_DIR" != "$TEAM_RUN_BASE/specs" ]` printing a separate "legacy split-root layout" line), so the codebase is demonstrably aware of exactly this split — the new notice just doesn't consult it. Because D8 freezes N1's bytes verbatim across both scripts with no room for conditional wording, a clean fix touches frozen intent-block text (class-B/re-freeze territory) rather than being a same-round drop-in patch — flagging for a scoped decision (re-freeze N1's wording, or split the message to name only records actually rooted under `$base`) rather than prescribing the byte change from this role.

#### Minor / nits
- `bin/team-paths.sh:84` / `bin/team-init.sh:87` — `git ls-files`'s own errors are discarded (`2>/dev/null || true`), so an unreadable/corrupt index could in principle produce a false N1 (if `check-ignore` then answers 0) or a false silence (if it answers 1) rather than N3. Downgraded from Codex's adversarial-pass "high" tag: triggering it requires an already-broken git index/permission state that would likely break many other git operations in the same repo first, so the blast radius beyond this probe is limited.
- `bin/team-paths.sh` / `bin/team-init.sh` — a tracked symlink or submodule gitlink placed exactly at the resolved base path can make `ls-files -- "$base"` register an entry that is not really "a tracked file under the base" (or, for an untracked symlink plus a directory-only ignore rule, make `check-ignore` answer from the symlink's own index shape rather than the directory it points through). Downgraded from "high": no documented `TEAM_RUN_BASE` example is a symlink or gitlink, and an operator would have to deliberately construct this base-dir shape.
- `bin/team-paths.sh` / `bin/team-init.sh` — the tracked-file check and the `check-ignore` call are two unlocked reads; a `git add`/`.gitignore` edit landing between them could combine into a state that never really existed. Advisory-only feature, no security boundary, and the window is a few milliseconds within one already-fast script invocation.
- `bin/team-paths.sh` / `bin/team-init.sh` — ambient `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, `GIT_LITERAL_PATHSPECS`, `GIT_GLOB_PATHSPECS` etc. in the caller's environment could redirect which repository/index the probe actually inspects. Requires an already-unusual invocation environment; no adopter-facing guidance suggests exporting these around a shell-team invocation.

#### Adversarial findings
A `codex exec` pass framed as "what could break `warn_ignored_base()`'s N1/N2/N3/silence classification" on `bin/team-paths.sh` + `bin/team-init.sh`, scoped by the primary pass's Major findings, returned 7 items (5 tagged "high", 2 "medium" in Codex's own vocabulary). It independently reproduced and sharpened the primary pass's pathspec-literal and rev-parse-boolean findings (M1, M2 above), surfaced M3 (the `--print`/pre-existence directory-form gap) and M4 (the legacy split-specs false claim) as new findings the primary pass had not raised in those exact terms, and additionally flagged the four items now listed under Minor / nits (ls-files error-swallowing, symlink/gitlink bases, the tracked-vs-ignored TOCTOU race, and ambient `GIT_*` env vars). Full raw output: `.shell-team/reviews/T-1042-codex-adversarial.{txt,jsonl}`.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `git ls-files` errors silently discarded (`2>/dev/null \|\| true`) | adversarial: high (→major) | minor | requires an already-corrupt/unreadable git index or permission failure — a repo state that breaks many other git operations first, not an independently-likely trigger |
| 2 | symlink/gitlink placed at the resolved base path | adversarial: high (→major) | minor | no documented `TEAM_RUN_BASE` example is a symlink/gitlink; requires a deliberately exotic base-dir shape outside anything the docs suggest |
| 3 | frozen N1's "specs" claim is false for the legacy split layout | primary: P2 (→major); adversarial: medium (→minor) | major | independently confirmed against `bin/team-init.sh:311-312`'s own adjacent specs-split-aware branch, proving the split is live, deliberate, still-shipping behaviour the new notice ignores — a factually wrong claim in a frozen, user-facing message outweighs the adversarial pass's lower tag; kept at the primary pass's severity |

#### Recommendation
- REQUEST_CHANGES → leave `.shell-team/todo.md`'s T-1042 entry at `READY_FOR_REVIEW`; route to `READY_FOR_ENG` with M1-M4 above as the findings to close. M1-M3 are implementation-only fixes inside `warn_ignored_base()` (no frozen text changes, no re-freeze). M4 needs a scoped decision at the next freeze-adjacent step, because a byte-accurate fix touches N1's frozen wording — this round's finding count is the first of the spec's own "same-class-2" two-round threshold for the Half A mechanism (`## Same-class-2 pre-commitment`); it is not yet at that threshold, but a second consecutive round drawing new Major findings on this same mechanism would be.

#### Fast-follow disposition
- ls-files error-swallowing (minor #1 above) — file-an-issue: cheap fix (check `ls-files`'s own exit status before trusting its output), worth tracking alongside M1/M2's git-robustness cluster rather than losing it once this round's majors are fixed.
- symlink/gitlink base defeating the predicates (minor #2 above) — file-an-issue: same rationale as above; the fix (require the resolved base to be a real directory, or fold this shape into N3) is small and belongs in the same follow-up as the ls-files fix.
- tracked-vs-ignored TOCTOU race (minor #3 above) — won't-fix: advisory-only feature with no security boundary; a real fix needs index-locking coordination disproportionate to the risk it closes.
- ambient `GIT_*` environment-variable interference (minor #4 above) — won't-fix: requires an already-adversarial or misconfigured invocation environment external to anything shell-team documents or controls.

---

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1042
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: static-only
- Diff scope (this round): `git diff --stat 3530baa..HEAD` — 8 files, +317/-52 lines (round-1 rework: M1-M4 fixes + N1 re-freeze). Full task diff scope unchanged: `git diff --stat 6439eb6...HEAD`.

Two fresh `codex exec` passes were run against the current working tree (HEAD `28d7e09`), reading the real files directly. The primary pass's one finding and every item in the adversarial pass were independently reproduced live in scratch fixtures before being kept (7 separate live repros this round, listed inline below) — this round drew heavily on direct verification rather than trusting Codex's text, because several of its claims (especially the `core.literalPathspecs` one) needed an empirical answer this reviewer could get and Codex could not (it cannot execute code in this sandbox).

#### Round-1 fixes verified (M1-M4 + re-freeze), independently, with my own fixtures
- **M1 (pathspec literal, `ls-files`)** — confirmed fixed for the glob-metacharacter case that was originally reported: a literal `a*` directory, ignored, beside an unrelated tracked `abc/file.txt` under a glob-colliding prefix — the notice now fires correctly (previously silently suppressed). See NM2 below for a related but distinct gap this same fix left open.
- **M2 (rev-parse boolean + no-repo-anywhere preservation)** — confirmed fixed: a bare `git init --bare` repository is now classified N2 (was wrongly N3); the genuinely-no-repo-anywhere case (`LA6`'s shape) still correctly resolves to N2; a stubbed `git` simulating a "detected dubious ownership" fatal (exit 128, non-"not a git repository" message) is now correctly routed to N3 (previously any nonzero rev-parse exit landed on N2). All three sub-cases reproduced live by this reviewer, matching the engineer's and QA's claims exactly.
- **M3 (`--print` pre-existence gap)** — confirmed fixed: a directory-form-only `.gitignore` rule now fires the notice even when the base dir does not exist on disk yet; a bare-form-only rule still fires both with the base dir absent and present (no regression from the unconditional `"${base%/}/"` normalization).
- **Minor #1 (ls-files error-swallowing), closed in the same round** — confirmed: `ls-files`'s own exit status (`tracked_rc`) is now checked and routes to `undeterminable` on failure, no longer discarded via `2>/dev/null || true` alone.
- **The class-B re-freeze of N1 (v1→v2)** — the coordinator's message explicitly invited a substantive veto on this item, so it is scored rather than waved through. Independently re-derived rather than trusted: `bin/check-refreeze-class.sh` on the old spec blob (extracted from `3530baa`) against the current spec returns `class-b … differing line at intent-block index 24 … is not a '- check:' line`, exit 1 — matching the board's "classifier class-b" claim exactly. `git diff 3530baa..HEAD -- .shell-team/specs/…` shows exactly the one N1 line changed, nothing else in the intent block or outside it. `bin/check-intent.sh` reports `aligned: T-1042 v2 (1396c6b2c5743484af3e56c184c8db1fe422c30f)`, matching the board's recorded hash exactly. **Wording judged accurate**: v2 drops the specific "specs" noun and instead scopes the claim to "everything the loop writes under it … among them", which I confirmed against `bin/team-paths.sh:213-220` is true in every layout (`TODO`/`LOOPS`/`RUNS`/`RETROS`/`REVIEWS`/`PROVENANCE`/`INTERVENTIONS`/`LESSONS` are all unconditionally `"$BASE/…"`; only `SPECS` escapes `$BASE` in legacy mode, and v2 no longer names it). **No objection to the re-freeze or its process** — it is not reverted.

#### NEW Majors this round (distinct from verifying round-1's fixes)
- **NM1 — the `not a git repository` text match is locale-dependent.** Live repro: `LANGUAGE=fr LC_ALL=fr_FR.UTF-8`, a target directory with no git repository anywhere in its ancestry — `git`'s fatal message becomes `"fatal : ni ceci ni aucun de ses répertoires parents n'est un dépôt git : .git"`, which does not contain the English substring, so the genuinely-outside-any-repository case (`LA6`'s own scenario) is misclassified as **N3** instead of the correct **N2**. `bin/team-paths.sh --root <target> --print` under this locale reproduces it directly. This affects both scripts (the same substring check is mirrored in `bin/team-init.sh`).
- **NM2 — `core.literalPathspecs=true` does not exist as an effective Git configuration variable, so `check-ignore`'s pathspec-magic vulnerability is not actually closed.** The code's own comment claims this is "the only mechanism it accepts at all" for forcing literal interpretation — this is factually wrong. Live proof: `git -C <fx> check-ignore -q -- ":(icase)foo/"` and `git -C <fx> -c core.literalPathspecs=true check-ignore -q -- ":(icase)foo/"` produce the *identical* error (`"pathspec magic not supported by this command: 'icase'"`, exit 128) — the config has zero observable effect. Concretely, a `$base` value with a leading colon (e.g. `:foo`, which `validate_base` does not reject) can defeat the ignore check: a fixture with an ignored, untracked directory literally named `:foo` and `TEAM_RUN_BASE=':foo'` produces **no notice at all** (silence) against the current code — reproduced live. The practical trigger (a colon-leading base-dir name) is far outside anything the docs suggest (`.shell-team`, `tasks`, `.ops`, `ops/team`), so the real-world likelihood is very low, but the shipped code's own reasoning for why this is safe does not hold, and `M1` is therefore not fully closed as claimed.
- **NM3 — merging `rev-parse`'s stdout and stderr makes the `"true"` boolean comparison fragile against any concurrent Git output on either stream, not just failure diagnostics.** Live repro: `GIT_TRACE=1 bash bin/team-paths.sh --root <healthy, non-ignored work tree> --print` — Git's trace preamble lands on stderr, gets merged into the captured `wt_out`, `wt_out` no longer equals the literal string `true`, and a directory that should be **completely silent** (no ignore rule matches it at all) instead fires the **N3** "undeterminable" notice — a false alarm in a state that was correct before this round's fix. `GIT_TRACE` is a common Git debugging/CI environment variable, not an exotic operator choice, which is why this is rated Major rather than folded into the already-accepted ambient-env-var risk below.

#### Minor / nits (round 2)
- Ambient `GIT_LITERAL_PATHSPECS`, `GIT_GLOB_PATHSPECS`, `GIT_NOGLOB_PATHSPECS`, `GIT_ICASE_PATHSPECS` can independently defeat the `ls-files`/`check-ignore` predicates (e.g. `GIT_LITERAL_PATHSPECS=1` would make `ls-files` search for the literal text `:(literal)<base>` rather than stripping the magic). This is the same already-disclosed, won't-fix risk class as round 1's "ambient `GIT_*` environment-variable interference" minor (requires an already-unusual invocation environment); folded into that existing disposition rather than opened as a new line.

#### Adversarial findings
A `codex exec` pass framed as "what else could be broken in this round's fixed code, given the locale issue is already known" on the current `warn_ignored_base()` returned 4 items (2 "high", 2 "medium"). It surfaced NM2 (the `core.literalPathspecs` inertness) and NM3 (the `GIT_TRACE`/merged-stream fragility) as genuinely new findings this reviewer had not reached yet, and separately flagged the broader ambient-pathspec-env-var class (folded into the existing minor above) and a variant of the locale issue (git also says "not a git repository" for some corrupted/misconfigured-`GIT_DIR` states, which would misclassify corruption as N2 instead of N3 — the mirror-image of NM1, same root cause, not counted as a fifth separate finding). Full raw output: `.shell-team/reviews/T-1042-codex-adversarial.{txt,jsonl}` (this round's capture; overwrites round 1's per the canonical-basename convention — round 1's adversarial findings are preserved in prose above and in the Round 1 section of this file).

#### Same-class-2 pre-commitment — FACTUAL TRIGGER HAS FIRED
Per `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md`'s `## Same-class-2 pre-commitment`: the trigger is "the Half A warning mechanism draws new Blocker or Major findings on two consecutive cross-provider review rounds." Round 1 drew 4 new Majors (M1-M4). This round (round 2) draws 3 further new Majors (NM1-NM3) on the same mechanism (`warn_ignored_base()`), distinct from — not a re-statement of — verifying round 1's fixes. **Two consecutive rounds of new Majors on Half A is now a fact, not a risk.** Per the spec's own priority rule ("the factual condition governs … even if a third round has already started"), the three dispositions are presented here with their content rather than as a request for a fifth round of targeted patches:

1. **Split Half A into its own task, ship Half B alone.** Half B (`bin/retro-inputs.sh`'s open-extension rule, the legacy-marker doc repair) has drawn zero findings across both rounds and is independently verifiable by AC1-AC6 with no criterion depending on Half A (D10). This ships the stable half now and gives Half A's git-diagnostics-dependent classification the design room it appears to need — each round has found a different class of fragility (pathspec syntax, then locale text, then stream-merging), which is itself a signal that "keep patching the discriminator" has not converged after two tries.
2. **Reduce Half A to the `bin/team-init.sh` site only, by class-B re-freeze, dropping `--print` and the symmetry table.** This does not resolve NM1/NM2/NM3 (the fragile classification logic is shared, not `--print`-specific), but it does halve the exposed surface and drops M3's `--print`-only pre-existence concern entirely.
3. **Drop the two-part warning condition to the ignore test alone, accepting the tracked-base false positive and removing `LA3`.** Orthogonal to NM1-NM3 — this disposition simplifies the *tracked-file* check, not the *rev-parse classification* logic where this round's new findings live, so on its own it would not close any of NM1-NM3.

None of the three, taken alone, resolves the specific findings from this round — NM1-NM3 all live in the `rev-parse`-output-classification logic, which every disposition either keeps as-is (2, 3) or defers to a follow-up task with more room to redesign it (1). This reviewer's read: disposition (1) is the one that actually gives the fragile discriminator a chance to be redesigned properly (e.g. `LC_ALL=C LANGUAGE=C` on the git invocations to eliminate locale-dependence, plus capturing stdout and stderr on genuinely separate streams to eliminate the `GIT_TRACE` fragility) rather than accepting a fifth round of narrow patches — but the decision among the three is pm-spec's / the coordinating session's to make, not this role's.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | ambient `GIT_LITERAL_PATHSPECS`/`GIT_GLOB_PATHSPECS`/`GIT_NOGLOB_PATHSPECS`/`GIT_ICASE_PATHSPECS` interference | adversarial: medium (→minor) | minor (folded into round 1's existing item) | same already-disclosed, won't-fix risk class as round 1's ambient-`GIT_*`-env-var minor; not a new disposition |
| 2 | `GIT_TRACE`/merged-stream boolean-match fragility | adversarial: medium (→minor) | major | upgraded on my own judgment: unlike the ambient-pathspec-env-var class, `GIT_TRACE` is a common debugging/CI variable rather than an exotic deliberate choice, and the live repro shows it produces a false alarm in an otherwise-correct, completely-silent state — closer to the mechanism's core correctness contract than a theoretical environment-tampering scenario |

#### Recommendation
- REQUEST_CHANGES → leave `.shell-team/todo.md`'s T-1042 entry at `READY_FOR_REVIEW`; route to `READY_FOR_ENG`, but with the same-class-2 dispositions above presented rather than a plain "fix NM1-NM3 and resubmit" instruction — per the spec's own pre-commitment text, that decision belongs to pm-spec / the coordinating session, not to a fifth patch-and-resubmit cycle initiated unilaterally.

#### Fast-follow disposition
- No new fast-follow deferrals this round beyond round 1's four (ls-files error-swallowing and symlink/gitlink-base: file-an-issue, per round 1 — Minor #1 is now closed, not deferred; symlink/gitlink remains open; TOCTOU race and ambient `GIT_*` env-var interference, including this round's more specific variable names: won't-fix, reasons unchanged from round 1).
