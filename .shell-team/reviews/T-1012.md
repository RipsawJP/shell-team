# QA verification — T-1012

Branch `feature/77-part2-gen-loop-replay`, HEAD `b13872a` (base `develop` @ `d08393b`).
Spec: `.shell-team/specs/T-1012-loop-replay-generator.md` (intent v2, `47cc2c0310e40ac744a64a316be6508cbd382db4`).
Working tree kept clean throughout (`git status --short` shows nothing beyond this
review file and the board edit); all mutation testing below ran on file copies
under `$TMPDIR`, never on the real checkout.

## 1. Mechanical acceptance criteria

```
$ bash bin/check-acs.sh --dry-run .shell-team/specs/T-1012-loop-replay-generator.md
30 recognized, 0 unrecognized

$ bash bin/check-acs.sh .shell-team/specs/T-1012-loop-replay-generator.md   (BEFORE creating this file)
check-acs: 27 passed, 1 failed, 2 skipped, 0 unrecognized
check-acs: FAILED: AC27
```

Confirmed the pre-state matched the expected shape exactly: AC1-AC26, AC28 all PASS;
AC27 FAIL solely because `.shell-team/reviews/T-1012.md` did not exist yet;
AC29/AC30 SKIP by design (no `check:`).

After creating this file:

```
$ bash bin/check-acs.sh .shell-team/specs/T-1012-loop-replay-generator.md   (AFTER)
check-acs: 28 passed, 0 failed, 2 skipped, 0 unrecognized (AC29/AC30 SKIP)
```

AC27's own sub-components verified individually:
```
$ bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"        -> rc=0
$ bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base develop -> rc=0
$ bash bin/check-pii-shapes.sh --base develop                             -> "clean (no PII-shaped bytes found)", rc=0
```
Confirms the H8 self-trigger the engineer flagged (their prose quoting the
home-path shape literally) is resolved by the orchestrator's reword — the
checker is clean against `develop` on this branch.

## 2. Full CI-ordered local run (`.github/workflows/check-handoff.yml`)

Ran every step in the workflow's own order, serially (no concurrent suites,
per the engineer's noted `$TMPDIR` contention), each as its own command with
rc captured. All 55 steps (`shellcheck` line + every fixture suite + every
dogfood step) passed:

```
PASS  shellcheck (full pinned argument list incl. bin/gen-loop-replay.sh, tests/gen-loop-replay/run.sh) — 12s, rc=0
PASS  Lint the shipped board template (check-handoff.sh templates/todo-template.md)
PASS  Run check-handoff fixture suite
PASS  Lint shipped shell-team / generic / goal loop contracts (check-contract.sh x3)
PASS  Run check-contract / loop-guard / check-run / log-run / team-init / discover-work fixture suites
PASS  Run check-acs / check-design-note / goal-state / rework-digest / check-retro fixture suites
PASS  Run retro-inputs fixture suite + bounded invariants lock
PASS  Dogfood retro-inputs --base HEAD
PASS  Dogfood check-retro (this repo's retros; the shipped retro template)
PASS  Run machine-tokens fixture suite
PASS  Run gen-loop-replay fixture suite      <- this task's new suite (tests/gen-loop-replay/run.sh)
PASS  Run check-readme-version fixture suite + dogfood on README.md/README.ja.md
PASS  Run rollup-runs / rollup-track / consolidate-proposals / cluster-failures / review-gate / close-out fixture suites
PASS  Run check-prompt-sync fixture suite + dogfood (bin/check-prompt-sync.sh)
PASS  Run check-playbook / gen-playbook-blocks / playbook-promote fixture suites
PASS  Dogfood check-playbook (real lessons corpus) + dogfood gen-playbook-blocks (scratch-copy round-trip)
PASS  Run team-paths fixture suite + dogfood (default-layout resolution)
PASS  Run install fixture suite
PASS  Run check-intent / check-provenance / check-interventions / interventions-reminder fixture suites
PASS  Dogfood check-interventions (every committed interventions file, incl. T-1012's)
PASS  Run check-board-headings fixture suite
PASS  Run errexit-safe regression suite
PASS  Run codex-skeleton-hygiene suite
PASS  Run check-pii-shapes fixture suite
PASS  check-pii-shapes on the PR diff --base develop   (H8 confirmed resolved, rc=0)
PASS  Run check-commit-identity fixture suite
PASS  Run gitignore-raw-dumps lock suite
PASS  check-commit-identity on the PR commits --base develop
```

Total: 55/55 steps green, 0 failures, no flakes observed on a full serial pass.
`shellcheck --version` on this host = 0.11.0, matching the workflow's pinned
`SHELLCHECK_VERSION`. `develop` and `origin/develop` are identical locally
(`d08393b1f2b561bc2739c37f9c6855dafb0a9132`), so the `--base develop` runs above
are equivalent to the workflow's `--base origin/${GITHUB_BASE_REF:-develop}`.

## 3. Producer-run mutation self-check — independently reproduced (2 of 5)

Both reproduced on a **file copy** under `$TMPDIR` (never `git checkout`; the
real `bin/gen-loop-replay.sh` was never touched — confirmed clean `git status`
throughout).

**Mutation A — remove the `<`/`>`/`&` escaping step (D2's measured top risk).**
Replaced `escape_payload()`'s body with a pass-through (`printf '%s' "$s"`) in
the copy. Regenerated the `hostile` fixture's page:
```
p = payload extracted from the mutated output
printf '%s' "$p" | grep -q '[<>&]'          -> MATCH (raw metacharacters present)
printf '%s' "$p" | grep -qF -- 'u003c/script' -> NOT FOUND (escaped form absent)
```
AC9's own assertion (`if printf '%s' "$p" | grep -q '[<>&]'; then ok=0; fi`)
goes red under this mutation, exactly as the engineer's self-check claims.
Restoring is trivial (the mutation lives only in the `$TMPDIR` copy; the real
file was never edited).

**Mutation B — remove the `bin/check-run.sh` delegation (D5).** Replaced the
validation loop's body with a no-op in the copy. Regenerated:
```
$ bash <mutated>/bin/gen-loop-replay.sh 20260801T000000Z-bad --runs-dir tests/gen-loop-replay/fixtures/malformed --out o.html
rc=0, o.html CREATED   (spec/AC4 requires rc=1, no output file)

$ bash <mutated>/bin/gen-loop-replay.sh 20260801T000000Z-mixed --runs-dir <bogus-kind fixture> --out o.html
rc=0, o.html CREATED   (spec/AC19 requires rc=1, no output file)
```
Both AC4 and AC19 go red under this mutation (the malformed row and the
`"kind":"bogus"` row both reach the template instead of being rejected),
exactly as the engineer's self-check claims.

Mutations C (placeholder-arity check removal), D (temp-then-move removal) and
E (output-guard removal) were not independently re-run (2 of 5 required; time
was spent instead on the AC29 verification below). AC6/AC3/AC8's own live
`check:` runs above already exercise the real, unmutated code paths those
mutations target.

## 4. Fail-closed paths, empirically (via `check-acs` above, all against the real script)

- Unknown run id (AC3): exit 1, no output file, reason names the id. PASS.
- Malformed row, whole-file-before-selection (AC4): exit 1, no output file,
  the malformed line carries no run id. PASS.
- Unreadable template (AC5): exit 2, no output file. PASS.
- Placeholder arity 0 and 2 (AC6): both exit 2, no output file; shipped
  template carries exactly 1. PASS.
- Argument hostility incl. empty id, `../escape`, `a/b`, `x;y`, `.`, unresolvable
  runs dir, `--help` exit 0 (AC7): all correct. PASS.
- Output-target guard: dangling symlink, directory, regular-file-overwrite
  (AC8): all correct, dangling symlink's target still absent afterward. PASS.

## 5. AC29 — the browser check

**No GUI/headless browser was reachable in this sandbox.** Attempted headless
Chrome (`Google Chrome 151.0.7922.71`, already installed, zero-install) via
`--headless --disable-gpu --no-sandbox --dump-dom`, including with a scratch
`--user-data-dir` and an overridden `$HOME`. Every attempt failed identically:
Chrome's crashpad service unconditionally tries to open
`(Chrome's Crashpad settings file under the operator's home Library directory — literal path scrubbed as a PII shape; see check-pii-shapes home-path)`
regardless of `--user-data-dir`/`$HOME`, which sits outside this sandbox's
write allow-list (`bootstrap_check_in ... Permission denied`, exit 21 every
time). This is a genuine environmental limitation, not a workaround I declined
to try — no `dangerouslyDisableSandbox` override is available to this session
either. No `puppeteer`/`jsdom` is installed and this task's zero-dependency
`bin/` contract plus the isolated-install discipline rule out installing one
ad hoc for a single QA pass.

**Reporting per the instructions: I did NOT see pixels, so I am not claiming
AC29 verified by observation.** `environmentally-unverified: AC29` for every
item that requires an actual rendered/interactive view:
- devtools network panel (zero requests) — not observed live; AC11's static
  grep for network primitives is a strong proxy but not the same claim.
- transport play/pause/seek/speed/jump-to-rework — not observed.
- camera reset/follow/auto-orbit/expand + double-click focus — not observed.
- JA/EN toggle actually switching rendered chrome (as opposed to the
  dictionary structurally existing) — not observed.
- `prefers-reduced-motion` honoured at render time — not observed (code path
  exists statically, see below).
- Visual marking of a non-success span, and the visual dashed/neutral
  treatment of an unrecognised agent — not observed (data-level and code-level
  confirmed, see below).

**What I did verify instead — logic-level execution + code reading (not
grep-only), against the real generator's real output for real fixtures:**

Generated the real, unmutated pages for `mixed`, `span-only` and `adopter`
with `bin/gen-loop-replay.sh` and wrote a small Node harness
(`$TMPDIR/t1012-harness/harness.js`) that quotes the template's own
`adaptRow`, roster-building loop and degradation-classification logic
**verbatim** (not reimplemented) and runs them against the extracted real
payloads:

```
adopter.html  -> roster: [...6 known..., "builder-bot", "reviewer-bot"]
                 unknown-agents-in-roster: ["builder-bot","reviewer-bot"]   <- never dropped, matches D3/spec
span-only.html -> classification: {"hasSpan":true,"hasEvent":false,"degradedKey":"degradedNoEvents"}
mixed.html     -> non-success spans (never dropped): 1 ["error"]
                  spans with model===null: 1, spans with verdict===null: 3
```

Also confirmed directly with `node -e`: `JSON.parse` on the raw,
un-injected `templates/loop-replay.html`'s placeholder text
(`"__LOOP_REPLAY_DATA__"`) throws (`Unexpected token '_'`), which the
template's own unconditional `catch` branch (source lines 322-345) turns
into an explicit `role="alert"` error banner and a changed `<title>` — this
confirms the "un-injected template shows the explicit error state, not a
blank page" AC item mechanically, without needing to see it rendered.

Confirmed statically (grep, all present): every design-note DOM/CSS anchor
(`#degradedChip`, `#railEmptyNote`, `.rail-empty`, `.chip.status-danger`/
`.status-warning`, `--agent-unknown` in all four theme blocks), all six new
dictionary keys in both `ja:`/`en:` blocks, `prefers-reduced-motion` /
`reduceMotion` gating code present, and that `row.label`/`e.label` is never
wrapped in `T()` anywhere (operator text stays verbatim in both languages).

**One nuance found and judged not to rise to a FAIL, disclosed for the
reviewer:** the design note's §5 acceptance hook says a null `model`/`verdict`
should render "`—` must appear in its place" via the exact pattern
`field != null ? field : "—"` in **both** `showTip()` and `drawMiniCard()`'s
single-span branch. Reading the shipped code: `drawMiniCard()`'s `model`/
`tokens`/`tool_uses`/`duration` correctly use that exact ternary-to-`"—"`
pattern (line ~1130-1133); but `drawMiniCard()`'s `verdict` (line 1134) and
**both** of `showTip()`'s `model`/`verdict` lines (lines 947, 951) instead use
an "omit the whole line if falsy" guard — which never prints the literal word
`null` (satisfying the AC's core safety requirement) but also never shows a
`—` placeholder (not matching the design note's literal instruction). Tracing
`showTip()`'s only live call site (line 1412, fed exclusively by
`nodeTipData()`, which returns `{label, outcome, agentId}` — never
`model`/`verdict`/`tokens`), this appears to be dead/vestigial code left from
before `nodeTipData` replaced the tooltip's data source; a user's actual path
to seeing a span's `model` is the detail panel's stat grid
(`current.model || "—"`, line 935, **pre-existing, unedited, ratified code**),
which already shows `—` correctly. I could not visually confirm this reading
(no browser), so I record it as a risk note rather than a FAIL: **the
literal word "null" never appears anywhere I traced**, but the tooltip/
mini-card verdict placeholder does not match the design note's letter. A
human/reviewer with browser access should hover a span with a null model in
practice to settle this beyond code-reading.

## 6. Frozen intent + seam gates

```
$ bash bin/check-intent.sh .shell-team/specs/T-1012-loop-replay-generator.md .shell-team/todo.md
check-intent: aligned: T-1012 v2 (47cc2c0310e40ac744a64a316be6508cbd382db4) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1012.md
check-provenance: conformant: .shell-team/provenance/T-1012.md (8 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1012 .shell-team/interventions/T-1012.md
check-interventions: conformant: .shell-team/interventions/T-1012.md (1 entries, 0 sentinel) -> rc=0
```

Spot-checked the 8 provenance decisions' grounding: each cites a specific
spec heading/line or a specific template code location; sampled 3
(human-event-onto-gate-branch, `source` array = all-scanned-not-just-matched,
`TOTAL_MS` zero-guard) against the actual template code — all check out as
described.

## 7. Regressions / adjacent code paths

`AC25`'s byte-for-byte diff-against-base check covers the two ignore files,
`bin/close-out.sh`, `bin/log-run.sh`, `bin/check-run.sh`, `skills/run/SKILL.md`,
`skills/goal/SKILL.md`, both READMEs, both CHANGELOGs and `docs/` — all
unchanged, confirmed by the live `check:` run. No other `bin/` script imports
or greps `gen-loop-replay.sh`, so no adjacent consumer exists to regress.

## 8. Edge cases tried beyond the spec's own fixtures

- CRLF/blank-line/no-final-newline tolerance: already exercised by T-1011's
  own suite (out of this task's scope per Non-goals; not re-tested here).
- Null `model`/`verdict` on a real span row: exercised via the Node harness
  above (§5) — data survives, no crash, no page-level defect found beyond the
  showTip/mini-card nuance already disclosed.
- These stay inside the spec's declared reachable Input space (nullable
  fields, adopter-shaped agent names); no out-of-input-space synthetic
  extreme was needed to find the one nuance above.


---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1012
- Codex model: gpt-5.1-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 22 files changed, +3017/-1 (develop `d08393b` … HEAD `ec11072`)

Codex CLI をラウンド1回（`codex exec review --base develop --json`、フォアグラウンド同期実行）で起動し、実体ファイルを直接読ませた。Blocker/Major が出たため、手順どおり devil's-advocate の adversarial パスも実行（対象: `templates/loop-replay.html` / `bin/gen-loop-replay.sh`）。加えて、Codex の指摘を裏取りするため `bash bin/check-pii-shapes.sh --base develop` / `bash bin/check-acs.sh` / `bash tests/gen-loop-replay/run.sh` / `shellcheck` を自分でも実際に実行し、コード読解と突き合わせた（このラウンドは static-only ではなく executed-tests）。

#### Blockers

- **`.shell-team/reviews/T-1012.md:143` — 追跡ファイルに実在の個人ホームディレクトリパスがコミットされている（PII 漏洩、かつ CI ブロッカー）。**
  QA が AC29 の節（143 行目）に書いた Chrome crashpad の設定ファイルパスは、H8 が指摘した「PII 形状を説明するための引用」ではなく、**実在するユーザー名を含む本物の POSIX ホームディレクトリ絶対パス**（本レビューでは形状の記述に留め、値そのものは再転記しない）。このブランチに対して `bash bin/check-pii-shapes.sh --base develop` を実行すると再現性ゼロ秒で `FINDING pattern=home-path path=.shell-team/reviews/T-1012.md line=143`（rc=1）が出る。結果として **AC27 は現在 FAIL**（`bash bin/check-acs.sh` を実行すると `27 passed, 1 failed`＝board が主張する「28/28 PASS」と食い違う）。CI の独立ステップ `check-pii-shapes on the PR diff` も同じ理由でこのブランチに対し赤くなる。このリポジトリの Hygiene 規約（本物の値は転記しない）にも反する。修正: 当該パスをリテラル値のまま書かず、`~/Library/...` や `<home>/Library/...` のような一般化された形状記述に書き換える（H8 の修復と同じ方向）。

- **フリーフォームなテレメトリ値が `innerHTML` へ無エスケープで到達し、DOM ベースのスクリプト注入が成立する（`templates/loop-replay.html` 複数箇所）。**
  もっとも直接的な経路: `templates/loop-replay.html:814-818` — ロースター（`RING_AGENTS`）に含まれる未知の `span` 値（＝ adopter の agent id）が、そのまま `row.innerHTML = '...'+id+' — '+T().unknownAgent;` に連結される。`agentLabel()`（:873-877）も未知 id の場合は生の id 文字列をそのまま返し、`agentChip`/`agentDot`（:915-916,924,930）経由で detail chip の `innerHTML`（:930）に、`phase`（:926,930）、`model`（:935-937）、`verdict`（:906,927,930）、`from`/`to`（:925,930）も同じ経路で到達する。`bin/gen-loop-replay.sh:317-324`（`escape_payload`）は D2 が定義した **JSON エンベロープの単一注入点**（`<script>` の破壊防止）だけを守っており、`JSON.parse` が復元した後の値をテンプレート自身の JS がどう描画するかという**別の信頼境界**は一切カバーしていない（AC9/AC10 もこの経路を検証していない）。`span`/`phase`/`model`/`verdict`/`from`/`to`/`label` はいずれも `bin/log-run.sh` の `jstr`/`jesc`（`\` と `"` のみエスケープ）を通るだけの自由記述で、**未知の `span` 値は D3 が明示的に「削除しない・第一級でサポートする」と定めた到達可能な入力**（コミット済み `adopter` フィクスチャで実証可能）。Input space の除外規定（synthetic extremes）には該当しないため降格は適用されない。`error` フィールドのみ `textContent` を使っており安全（:938-940）。修正: 該当する全 sink を `textContent`/DOM 構築に置き換えるか、`innerHTML` へ入れる前に例外なく HTML エスケープする。

- **コミット済み `event-only` フィクスチャで、レンダリング/アニメーションループが実行時にクラッシュする（`templates/loop-replay.html`）。**
  `RING_POS`（:727-731）は「既知 6 id ＋ work-span として現れた agent id」（:407-415）にしか位置が入らない。`buildStepsB` の `rework` 分岐（:663, 678-680）は `ringHolder` が初期値 `null` のまま `move(ringHolder, e.to, ...)` を呼び得る。**これは他ならぬコミット済み `tests/gen-loop-replay/fixtures/event-only/shell-team.jsonl` そのもので発生する**（seq2 の `rework` が、`ringHolder` を設定する work/human-gate 分岐より先に来る）。生成された `move` ステップは `from:null`。`computeSceneB`（:743-744）が `lerp3(worldPos(step.from), worldPos(step.to), progress)` を呼ぶと、`worldPos(null)`（:734）は `undefined` を返し、`lerp3`（:735）がその `.x` に触れて `TypeError` で例外送出——描画/アニメーションループが壊れる。同じ壊れ方は、`from`/`to` が「span として一度も現れない id」を指す `handoff`/`release` イベントでも起きる（adopter-shaped run として Input space が明示的に到達可能とする形）。これは D3 の契約「the page must load, render the rows it has」を、AC15 が保証しようとしている **event-only 縮退ケースそのもの**で破る。AC15 の `check:` はペイロードの静的な行数しか見ず JS を実行しないため機械検証は素通りし、QA の Node ハーネスも `adaptRow`/roster/degradation ロジックのみを検証して `buildStepsB`/`computeSceneB` は実行していないため、この回でも検出されなかった。

#### Major

- **6 体を超える agent がリング上で座標重複する（`templates/loop-replay.html:407-415, 727-730`）。** リングの角度刻みが固定値 `Math.PI/3`（= 2π/6）のため、7 番目（index 6）以降の distinct agent は前の agent と厳密に同じ角度に乗る。**コミット済み `tests/gen-loop-replay/fixtures/adopter/my-loop.jsonl` で実証**: `builder-bot`（index 6）は `tech-lead`（index 0）と、`reviewer-bot`（index 7）は `pm-spec`（index 1）と完全に重なり、ヒットテストは先にある既知ノードを拾うため adopter ノードがフォーカス不能になり、既知ノードも隠れる。D3 の「未知 agent は削除しない・視認できる形で表示する」という意図に反する（データは残るが視覚的に判別不能）。修正: 角度刻みを `RING_AGENTS.length` から導出する。

- **`from` が文字列 `"human"` である `gate` イベントが、実際の `human` イベントと区別されない（`templates/loop-replay.html:362-363, 565-569, 681-685`）。** `adaptRow`（:362-363）はどちらも `{type:"gate", agent:"human"}` という同一形状に変換し、`buildStepsB`（:681-685）は両方を human 確認/settle 分岐で描画し、縮退分類器（:565-569）も両方を同じ `"human"` 語彙 id として数える。`from` はクローズドな語彙ではなく自由記述であり到達可能。結果として D3 が凍結したはずの affordance マッピング（`gate` → 「at で発生するゲート信号、`label` は verdict」と `human` → 「human レイヤーの settle」を区別する、という Settled Decision）が黙って壊れ、`degradedPartialEvents` の判定も歪む可能性がある。修正: adaptation の際にイベント種別そのものを保持する（`agent==="human"` からの復元に頼らない）。

#### Minor / nits

- **design-note §5 のプレースホルダ規約からの乖離（`templates/loop-replay.html:947,951` の `showTip`、`:1134` の `drawMiniCard`）。** design note は「null になり得るフィールドは必ず `field != null ? field : "—"` で埋める」と定めているが、この2箇所は「falsy なら行ごと省略」という異なるガードを使っている。文字列 `"null"` が表示されることは無い（AC29 の核心の安全要件は満たす）ため FAIL にはしない——QA 自身の disclosed nuance（`reviews/T-1012.md` §5）と同じ結論。`showTip` 側は唯一のライブ呼び出し元 `nodeTipData()`（:971-974）が `model`/`verdict` を渡さないため実質的に死コードだが、`drawMiniCard` の verdict 行はライブで、design note の文言と食い違ったまま。
- **`SOURCE_ITEMS` がファイル名に含まれる `"`/`\` をエスケープしない（`bin/gen-loop-replay.sh:255-265`）。** `runs` ディレクトリ内の `.jsonl` ファイル名に文字どおりの `"` が含まれていた場合、`\"$(basename -- "$f")\"` の連結だけでは JSON 構造が壊れ得る。テレメトリの `label` 等とは異なりファイルシステム側（operator/system 管理下）の入力であり、壊れ方も fail-safe（`JSON.parse` が例外を投げ、既存の `catch` がエラー画面を出す——D2/D3 が設計した経路どおり）で、XSS には至らない。

#### Adversarial findings

devil's-advocate プロンプトで `templates/loop-replay.html` / `bin/gen-loop-replay.sh` を再読させた結果、上記4点（innerHTML 注入・リング座標重複・`worldPos`/`lerp3` クラッシュ・gate/human 混同）を独立に確認・再現し、一次パスの P1/P2 評価と一致した。加えて一次パスにはなかった精緻化: (1) innerHTML 注入は `phase`/`model`/`span` id/`from`・`to`/`verdict`/未知 agent の legend 行を含む複数 sink に及ぶが、`error` は `textContent` のため安全と明記。(2) クラッシュ経路は `rework`（`ringHolder` が `null` のまま）だけでなく、span として一度も現れない id を参照する `handoff`/`release` でも同じ形で起き得ると指摘（より広いクラス）。(3) 「`from` が human でない `gate`」は signal 止まりで安全と明記し、危険な経路を rework/handoff/release に絞り込んだ。

#### Synthesis audit ledger

no severity changes

（Codex 一次パスの P1 は本ロールのマッピング表どおり blocker に、P2 は major に対応させただけで、独自の格上げ・格下げは行っていない。PII 漏洩・`SOURCE_ITEMS` のエスケープ欠落は本ロール自身が実行した `check-pii-shapes.sh`/コード読解で新規に発見したもので、Codex の既存判定を変更した事例ではない。design-note §5 の乖離は QA が既に non-FAIL と判定した内容をそのまま踏襲しており、格下げ・格上げはしていない。）

#### Recommendation

- REQUEST_CHANGES → board を `READY_FOR_ENG` に戻し、上記 Blocker 3件（PII 漏洩／innerHTML 注入／event-only クラッシュ）を最優先で修正。Major 2件（リング座標重複／gate-human 混同）も同ラウンドでの修正を推奨（同じファイルを触るため）。

#### Fast-follow disposition

- design-note §5 のプレースホルダ乖離（`drawMiniCard` の verdict 行、`templates/loop-replay.html:1134`）— file-an-issue（理由: 1行の三項演算子修正で済む低コストな修正だが、今回の Blocker 修正で同ファイルに触れないまま見送られる可能性があるため追跡用に起票を推奨。`showTip` 側は現状死コードのため対象外）。
- `SOURCE_ITEMS` のファイル名エスケープ欠落（`bin/gen-loop-replay.sh:255-265`）— file-an-issue（理由: fail-safe な壊れ方でテレメトリ内容起因の攻撃面ではないため今回はブロックしないが、防御的観点で追跡する価値がある）。

---

## Round 2 — delta re-verification (2026-08-01)

Scope per the T-1010/T-1011 precedent: the rework delta only (engineer
`34541ca`, fixing Codex round-1 REQUEST_CHANGES: 3 Blockers, 2 Majors, 2
Minors — see the Codex verdict above). Note on history: round 1's own
commit was amended by the orchestrator (`4f4d1cb`) to scrub a real,
committed home-directory path Codex correctly flagged as a Blocker in
§AC29's write-up above — every reference to that path in this round's own
text below is a **shape description**, never a literal value, per that
same finding.

### 1. Mechanical acceptance criteria

```
$ bash bin/check-acs.sh .shell-team/specs/T-1012-loop-replay-generator.md
check-acs: 28 passed, 0 failed, 2 skipped, 0 unrecognized (AC29/AC30 SKIP)
```
Spec unchanged this round (`git diff b13872a 34541ca -- .shell-team/specs/`
is empty) — same intent v2, same 28 mechanical criteria, all still green.

### 2. Touched-suite + PII re-run

```
$ bash tests/gen-loop-replay/run.sh
... PASS [mixed]      computeSceneB() executed cleanly, 13 events, roster size 6
... PASS [span-only]   computeSceneB() executed cleanly, 4 events, roster size 6
... PASS [event-only]  computeSceneB() executed cleanly, 5 events, roster size 6
... PASS [adopter]     computeSceneB() executed cleanly, 5 events, roster size 9
... PASS [adopter]     ring positions are pairwise distinct across 9 agents
... PASS [adopter]     agentChip()/agentLabelHtml() render the <img onerror> span id INERT
... PASS [hostile]     computeSceneB() executed cleanly, 7 events, roster size 6
... PASS [hostile]     a gate event with from="human" and a genuine human event survive as DISTINCT types
... PASS [hostile]     agentLabelHtml() renders the <img onerror> event `to` value INERT
... PASS [multi-run]   computeSceneB() executed cleanly, 4 events, roster size 6
gen-loop-replay: all checks passed   (rc=0)

$ bash tests/machine-tokens/run.sh
machine-tokens: all tokens present in their consuming files   (rc=0)

$ bash bin/check-pii-shapes.sh --base develop
check-pii-shapes: clean (no PII-shaped bytes found)   (rc=0)
```

This directly, mechanically confirms every one of Codex round-1's Blockers
and Majors: `event-only` no longer crashes; `adopter`'s 9-agent roster no
longer collides; the `<img onerror>`-shaped `span` id (adopter) and event
`to` value (hostile) both render inert; the `gate`-with-`from:"human"` row
(hostile, new this round) is not conflated with a genuine `human` event.
`shellcheck` on `bin/gen-loop-replay.sh tests/gen-loop-replay/run.sh` and
the suite run are both folded into AC20/AC21 above (28/28 PASS already
covers this).

### 3. Mutation self-checks — independently reproduced on file copies

Both on copies under `$TMPDIR` (never `git checkout`; real checkout stayed
clean throughout, confirmed via `git status --short` before and after).

**Mutation C — revert `agentLabelHtml()`'s escaping** (`return
escapeHtml(agentLabel(id));` → `return agentLabel(id);`, no other change).
Generated the `adopter` fixture's page from the mutated template (real,
unmutated `bin/gen-loop-replay.sh --template <mutated-copy>`), then ran the
suite's own embedded `node-harness.js` (extracted verbatim from
`tests/gen-loop-replay/run.sh`'s heredoc) against it:
```
PASS [adopter-mutA]: computeSceneB() executed cleanly ... roster size 9
PASS [adopter-mutA]: ring positions are pairwise distinct across 9 agents
FAIL [adopter-mutA]: agentChip()/agentLabelHtml() emitted a RAW <img for the
                     adopter fixture's XSS-shaped span id — injection not neutralised
adopter-mutA: 1 violation(s)   (rc=1)
```
Confirms Blocker #2's fix is load-bearing: removing the escaping step alone
reopens the raw-`<img` injection, exactly as the provenance entry claims.

**Mutation D — revert both Blocker #3 sub-fixes together** (`worldPos()`'s
`RING_POS[id] || ORCH_POS` fallback reverted to `RING_POS[id]`, **and** the
`rework` branch's `ballAt`-comparison reverted to the original
`ringHolder`-comparison). Reverting *only* the `worldPos()` fallback while
leaving the `ballAt` fix in place did **not** reproduce a crash on the
committed `event-only` fixture — every `from`/`to` in that fixture already
resolves to a known ring/structural id, so the `ballAt` fix alone already
keeps a valid origin flowing into `move()`/`worldPos()` for this specific
fixture (this asymmetry is itself informative: the two sub-fixes are
independently load-bearing for different reachable shapes, matching the
provenance entry's own framing of them as "two independent improvements to
the same branch," not one fix wearing two hats). With **both** reverted:
```
$ bash bin/gen-loop-replay.sh 20260801T000000Z-events --runs-dir tests/gen-loop-replay/fixtures/event-only --template <mutated-copy> --out <tmp>.html
$ node node-harness.js <tmp>.html event-only-mutB2
FAIL [event-only-mutB2]: computeSceneB() threw at simMs=0.165: Cannot read
                          properties of undefined (reading 'x')
event-only-mutB2: 1 violation(s)   (rc=1)
```
This reproduces the exact failure mode and the exact `simMs≈0.165` value the
provenance entry itself reports for its own mutation self-check — strong
independent confirmation, not just a plausible-looking crash.

### 4. Original Codex reproductions confirmed gone

Both already shown mechanically in §2 above against the real, unmutated
code: the `event-only` fixture generates and its `computeSceneB()` executes
cleanly across the full timeline sweep (200 samples); the `adopter`
fixture's 9 ring positions are confirmed pairwise distinct by the harness's
own coordinate-collision check.

### 5. AC29 — still environmentally-unverified; deepest available substitute re-run

No GUI/headless browser became reachable in this sandbox between rounds
(same environmental wall as round 1: a sandboxed filesystem write
restriction outside this session's control, not a workaround I declined to
attempt — not re-attempted this round since the underlying constraint is
unchanged). **I am not claiming AC29 verified by observation.**

What changed for the better this round: the deepest available substitute is
no longer my own ad hoc round-1 harness (which only exercised
`adaptRow`/roster/degradation-classification logic) — it is now the
engineer's own `node-harness.js`, embedded in `tests/gen-loop-replay/run.sh`
and run as part of the committed suite, which actually **executes**
`computeSceneB()`/`getCurrentStepB()` across a 201-sample sweep of the full
playback timeline for all 6 fixtures that produce a page, plus the
ring-collision, gate/human-conflation and injection-escaping checks. This
is strictly stronger evidence than what I had in round 1: it catches a
runtime crash and a DOM-injection failure that a payload-shape check alone
cannot. I re-ran it above (§2) against the real, unmutated code (all green)
and reproduced it going red under two independent mutations (§3).

What still stays **humanly unverified** (unchanged from round 1, restated
precisely): the devtools network panel showing zero live requests; the
transport's play/pause/seek/speed/jump-to-rework controls; the camera's
reset/follow/auto-orbit/expand controls and double-click focus; the JA/EN
toggle's actual rendered chrome-switching (dictionary structure and
toggle-persistence code confirmed statically, not observed);
`prefers-reduced-motion` actually suppressing animation at render time
(the `reduceMotion` gating code path confirmed statically, not observed);
the pixel-level visual marking of a non-success span and the dashed/neutral
styling of an unrecognised agent (data reaching the render path is now
proven by execution — §2 — but the actual pixels are not). The human
verifying visually at merge time, as the coordinator states, is the
correct disposition for these — I have not faked any of them.

**On the `showTip()` waiver (provenance's Minor #1 disposition) — evaluated
and accepted.** Re-confirmed `showTip()`'s only live call site is still fed
exclusively by `nodeTipData()` (`{label, outcome, agentId}` — never
`model`/`tokens`/`tool_uses`/`duration_ms`/`verdict`), unchanged by this
round's diff. The reasoning holds: applying the design note's
always-show-a-placeholder pattern there would add five permanently-`"—"`
lines to a working, unrelated feature (the agent-identity hover tooltip)
rather than fixing a reachable defect, since none of those fields ever
reach that code path in practice. `drawMiniCard()`'s `verdict` line — which
**is** live/reachable — was correctly switched to the literal
`field != null ? field : "—"` pattern this round; I read the change and it
matches the design note's letter exactly. I accept this waiver.

### 6. Frozen intent + seam gates (re-run)

```
$ bash bin/check-intent.sh .shell-team/specs/T-1012-loop-replay-generator.md .shell-team/todo.md
check-intent: aligned: T-1012 v2 (47cc2c0310e40ac744a64a316be6508cbd382db4) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1012.md
check-provenance: conformant: .shell-team/provenance/T-1012.md (15 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1012 .shell-team/interventions/T-1012.md
check-interventions: conformant: .shell-team/interventions/T-1012.md (1 entries, 0 sentinel) -> rc=0
```
Interventions file unchanged since round 1 (this round's rework was driven
by cross-provider review findings, not a human intervention — no new entry
expected or needed).

Spot-checked 3 of the 7 new provenance decisions against the actual code
diff (all matched what's described): (1) the `human`-gets-its-own-type
reversal — confirmed `adaptRow`'s `case "human"` now returns `type:"human"`
with no `agent` field, and `buildStepsB` branches on it separately from
`gate`; (2) the `worldPos()`/`ballAt` fix — confirmed both changes exist
exactly as described and independently reproduced their necessity in §3
above; (3) the ring-angle-step fix — confirmed `RING_ANGLE_STEP = (Math.PI
* 2) / RING_AGENTS.length` replaces the fixed `Math.PI/3`, and confirmed via
the harness that 9 agents now sit at pairwise-distinct angles.

### Round-2 verdict

All delta items pass. No regression found in the touched surface; both
reproduced mutations behave exactly as the engineer's provenance entries
claim; AC29 stays honestly environmentally-unverified rather than faked.

