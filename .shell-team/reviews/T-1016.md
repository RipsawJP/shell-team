# T-1016 QA verification record

- Verdict: PASS
- Task: T-1016 — One canonical board-entry boundary, and a stranded
  continuation line that fails closed
- Board: T-1016 → READY_FOR_REVIEW (this record's first commit satisfies
  AC17's first component)
- Commits reviewed: `4600f14`, `2b9d808`, `9ef2232` on `fix/89-close-out-stranding`,
  base `develop` @ `a3ee283`. Board at `d12b14d` (`BLOCKED` → `READY_FOR_QA`).
- Lens declaration: this task is squarely **executable-artifact / verification-mechanism**
  work — four `bin/` parsers changed, one new gate added, one new CI dogfood
  step. The adversarial fixture synthesis checklist applies in full, and the
  spec's own AC5/AC11 already encode most of its class-1/class-2 boundary
  probes as machine `check:` lines.

## Mandatory conformance gates

- `bin/check-intent.sh .shell-team/specs/T-1016-close-out-entry-boundary.md
  .shell-team/todo.md` → `aligned: T-1016 v2 (3ec8dabcac0542da56c91dd49de1b341c4c89093)
  matches ...` (exit 0).
- `bin/check-provenance.sh .shell-team/provenance/T-1016.md` →
  `conformant (4 decision entries, 0 sentinel)` (exit 0). Read all four
  decisions against the diff: (1) canon-marker placement in close-out.sh's
  `--help` header rather than beside the pass-1 awk — matches the spec's own
  Body-to-AC correspondence override for that one site; (2) `TASK_LINE_RE`
  vs. `LINE_RE` distinction for the strand check's entry-opening test — matches
  the Terms table's "task line" definition and D4's "one defect, one message";
  (3) `|| true` on the four `grep -c` zero-count assertions in
  `tests/close-out/run.sh` — matches test-recipe.md's own T-1016 lesson about
  `grep -c` under `pipefail` aborting on a genuine zero; (4) AC10's frozen
  fixture reported BLOCKED rather than silently rewritten — matches the
  Assumptions item 2 anticipation and the ratified v2 correction. All four
  groundings check out against the cited files.
- `bin/check-interventions.sh --task T-1016 .shell-team/interventions/T-1016.md`
  → `conformant (2 entries, 0 sentinel)` (exit 0).

## Spec's own gate (`bin/check-acs.sh`)

- `bash bin/check-acs.sh --dry-run <spec>` → 0 unrecognized, all 18 AC labels
  parsed, no `check:` line begins/ends with a backtick.
- `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh <spec>` (before writing this
  record) → **16 passed, 1 failed (AC17), 1 skipped (AC18)** — matches the
  expected shape exactly. AC17 failed only on
  `.shell-team/reviews/T-1016.md` not existing yet.
- Re-run after this record's first commit → AC17's remaining components
  (`check-handoff` on the live board, `check-board-headings --base develop`,
  `check-pii-shapes --base develop`) all pass; full re-run confirms **17
  passed, 0 failed, 1 skipped**.

## The four owning suites + errexit-safe

All five run green:

- `tests/check-handoff/run.sh` — OK, including
  `strand-accepts-table-row`/`-whitespace-only`/`-tab-indent`/`-crlf` and
  `strand-clean-board-positive-control`.
- `tests/close-out/run.sh` — all assertions passed, including
  `move-tab-indented-subbullets`, `move-refuses-stranded-board` (+ positive
  control), `move-refusal-names-the-reason`.
- `tests/check-intent/run.sh` — all assertions passed, including the
  non-vacuous counterfactuals for D2's blank-line neutrality.
- `tests/check-board-headings/run.sh` — OK, including both non-vacuous
  counterfactual locks for the predicate-alignment edit.
- `tests/errexit-safe/run.sh` — all assertions passed (the re-grounded pin
  registry from the v2 re-freeze round is green, confirming the finding in the
  first `BLOCKED` hand-off was resolved rather than papered over).

All 25 frozen assertion ids from AC11 were independently grepped against each
suite's own `PASS:` output lines (not merely against the file text) — all 25
found.

`shellcheck 0.11.0` (matches the CI-pinned version) against all four edited
`bin/` scripts and all five touched suites — clean, exit 0.

## Incident reproduction beyond the AC fixtures

Built a fresh fixture in a scratch root outside this checkout, shaped like the
real T-1013 incident: a task entry with an internal blank line, a markdown
table (header + separator + data row), and a second sub-bullet after the
table, followed by trailing blanks before the next section.

- **(a) NEW `close-out.sh` moves it whole.** Ran the current
  `bin/close-out.sh` (via `TEAM_TODO=` override, cwd inside the scratch root)
  against this fixture: exit 0, `## Active` left with **zero** non-blank
  lines, the table and the second sub-bullet both survive verbatim in the
  moved `## Done` entry.
- **(b) OLD `close-out.sh` strands it.** First attempt (copying only
  `git show a3ee283:bin/close-out.sh` alone) produced a *false* refusal —
  the old script resolves `check-handoff.sh` via `$SCRIPT_DIR`, and with no
  sibling `check-handoff.sh` present that resolution itself failed, which
  would have been a vacuous "reproduction." Corrected by also checking out
  `check-handoff.sh` from `a3ee283` into the same directory so the old
  pre-write gate runs the *old* checker (no strand check yet) rather than
  failing on a missing file. With that pairing, the OLD `close-out.sh` exits
  0 and **silently** leaves the table and the second sub-bullet stranded in
  `## Active` (4 non-blank lines remaining, no error, no stderr) — the exact
  T-1013 incident shape.
- **(c) NEW `check-handoff.sh` rejects the OLD-produced stranded board.** Ran
  the current `bin/check-handoff.sh` against the board (b) produced: exit 1,
  four `stranded continuation line (no task entry above it in this section)`
  lines at the correct line numbers (table header/separator/data-row lines
  and the second sub-bullet line).

This before/after pair is the proof the incident class is closed, not merely
that the AC7/AC9 fixtures (which use a cleaner, hand-authored shape) pass.

## Boundary probes beyond the AC fixtures (4, all as expected)

1. A strand as the **very first line** of `## Active`, no blank line after
   the section heading — reported, exit 1, correct line number.
2. An entry whose **only** continuation line is a table row — accepted, exit
   0, no stderr.
3. **CRLF board through `close-out.sh` end-to-end** (AC6 only exercises CRLF
   through `check-handoff.sh`; this is the mover) — moves the whole entry,
   zero strand, board still lints clean, CRLF preserved on the untouched
   lines.
4. A `_(none)_` placeholder **immediately followed** by a task line (no blank
   line between them) — accepted, exit 0; the placeholder stays a boundary
   and the task line opens its own entry normally.

## Verification-depth disclosure — engineer's mutation self-checks

The hand-off's Notes-for-engineer lists 8 mutation classes (one of which
spans all four board-parsing sites). Independently reproduced 2 of them on a
fresh `git clone` of this checkout (not the working tree), each confirmed
red, then restored and confirmed green again:

- **"Delete the strand check entirely while leaving the canon marker in
  place."** Neutralized the `emit(...)` call inside the strand branch of
  `bin/check-handoff.sh` (canon-marker comment block left untouched). Result:
  **AC4 → FAIL**, **AC1 stayed PASS** — matches the claim exactly (AC1 is a
  marker anchor, not a behavior proxy). Restored → both PASS again.
- **"Revert `check-intent.sh`'s blank-line neutrality."** Changed the
  blank-line branch's no-op (`:`) back to `in_entry=0`. Result: **AC10 →
  FAIL**; manually re-ran the three probe boards and confirmed `b=1`
  (drift, not the pre-fix `2`) while **`g` stayed `1`** — matches the claim
  ("the invariance assertion must go red *and* its `g` control must stay at
  `1`"). Restored → PASS again.

The remaining mutations (predicate revert at the other three sites,
strand-check-first-line-only, whitespace-only-as-continuation,
entry-extent-trim deletion, close-out gate swallowing stderr again, deleting
the new CI step) were **not** independently re-run — they remain the
producer's own claim, disclosed as such per the corpus discipline.

## Hand-off claims checked against reality

Re-ran, exactly as the workflow writes them, 4 of the CI-wired dogfood steps
(the "56 steps green" claim was not re-run in full — see below):

- `bash bin/check-handoff.sh templates/todo-template.md` → exit 0.
- The live-board dogfood step (`team-paths.sh --get todo` then
  `check-handoff.sh` on the resolved path) → exit 0.
- `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`,
  exit 0.
- The check-playbook live-corpus dogfood step (`team-paths.sh --get lessons`
  then `check-playbook.sh` on the resolved path) → `all entries valid`,
  exit 0.

**Not re-run** (disclosed, not faked): the remaining ~50 CI steps — the full
`shellcheck` argument list beyond the 9 files spot-checked above, every
unrelated fixture suite (team-init, discover-work, check-retro,
retro-inputs, rollup-*, consolidate-proposals, cluster-failures, review-gate,
gen-playbook-blocks, playbook-promote, team-paths, install,
check-provenance/-interventions fixture suites, interventions-reminder,
codex-skeleton-hygiene, check-pii-shapes fixture suite, check-commit-identity,
gitignore-raw-dumps, machine-tokens, gen-loop-replay), the
check-retro/check-readme-version/team-paths/check-interventions dogfood
steps, and the PR-diff-scoped `check-pii-shapes`/`check-commit-identity`
steps (no PR exists yet to diff against). This is a spot-check of the
dogfood claims the task asked for, not a full CI re-run.

## Environment / working-tree discipline

- `git status --short` on the real checkout showed nothing beyond this
  record's own commit and the subsequent board-flag edit throughout — every
  fixture, probe, and mutation ran against scratch copies under `$TMPDIR`
  (a fresh `git clone` for the mutation self-checks, plain `mktemp -d` roots
  for the incident/boundary probes) or read-only against the real tree. No
  `git checkout`/`restore`/`reset`/`clean`/`stash` was run against the real
  working tree.
- `shellcheck --version` confirmed `0.11.0`, matching the CI-pinned version
  the workflow installs, before trusting the shellcheck-clean spot check
  above.

## Edge cases tried (input-space-grounded)

All edge cases synthesized above are within the spec's declared "Reachable
input classes" (indented non-dash lines, internal blank lines, section/EOF
boundaries, CRLF, `_(none)_` placeholder, boundary-shape variety) — nothing
here relies on an out-of-scope synthetic extreme, so the `out-of-input-space`
exemption does not need to be invoked for any finding (there were no adverse
findings to invoke it for).

## Risk notes for reviewer

- AC10 and AC16 both went through a human-ratified v1→v2 re-freeze
  mid-implementation (frozen-region defects: AC10's fixture lacked a
  `**Task ID**` line; AC16's allow-list omitted `tests/errexit-safe/run.sh`).
  Both are re-verified green above under the v2 hash — worth a close read of
  the ratification record on the board rather than taking the hash match
  alone as sufficient.
- `tests/errexit-safe/run.sh`'s line-pin re-grounding (the second engineer
  hand-off) is exactly the kind of edit that is easy to get subtly wrong
  (right line number, stale content, or vice versa); it is independently
  green here and its own suite includes a mutation self-check of its own
  detector, but it is worth the reviewer's own glance at the diff.
- The full CI run (all ~56 steps) has not been exercised as a whole by
  anyone outside the engineer's own local run in this task; the spec's own
  Assumptions section already flags this as a standing limitation of local
  verification (single developer's OS/coreutils) and defers to the real CI
  result as primary evidence. Recorded here, not re-litigated.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1016
- Codex model: gpt-5-codex (`codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: 21 files changed, +1081/-73 lines (`develop...HEAD`, base `a3ee283`)

内部整合性は検証済み・issue 正典との突合はここでは未検証: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — 本タスクは issue #89 の内容を直接 read できる形では検証していない（spec の「## Problem」「Source」記述との内部整合のみ確認）。

#### Blockers

- `bin/check-handoff.sh:110-128`(TASK_LINE_RE と format-validation ゲートの強度不一致)— 新設のストランド検知が、D4「一つの欠陥に一つのメッセージ」という凍結済み Settled Decision に違反する形で、実際に再現できるカスケード誤検知を起こす。`TASK_LINE_RE='^- \[[x ]\] '`（チェックボックス閉じ括弧の直後に半角スペースを要求）は、フォーマット検証を発火させる緩いリテラルプレフィクス判定 `content != "- [ ]"* `より厳格。閉じ括弧の直後にスペースが無いトップレベル行（例: `- [ ]broken title`）は `in_entry` を 1 にできないため、その行自身の正当な継続行（サブバレット）が軒並み「stranded continuation line」として誤検知される。チェック済みタスク行（`- [x]done ...`）の場合はさらに悪く、フォーマット検証自体が発火しない（`"- [ ]"*` は unchecked 行にしか適用されないため）ので、根本原因の行に一切メッセージが出ないまま、正当な子行だけがストランド扱いされる。両方とも実機で再現済み（下記 fixture）。この入力形状は spec の Input space に "A malformed top-level line (a `- [ ]` line that fails `LINE_RE`) followed by its own continuation lines" として明示的に reachable と宣言されており、out-of-input-space の対象ではない。
  - 再現コマンド（unchecked 版）:
    ```
    printf '# Tasks\n\n## Active\n\n- [ ]broken no space after bracket\n  - a note under the malformed line\n  - another note\n\n## Done\n' > fixture.md
    bash bin/check-handoff.sh fixture.md
    ```
    実際の出力: `:5: format mismatch: ...` に続けて `:6:`/`:7:` の 2 行が誤って `stranded continuation line` として出る（正しくは format mismatch 1 件のみのはず）。
  - 再現コマンド（checked 版、根本原因メッセージ皆無）:
    ```
    printf '# Tasks\n\n## Active\n\n- [x]done no space after bracket\n  - a closed note\n\n## Done\n' > fixture2.md
    bash bin/check-handoff.sh fixture2.md
    ```
    実際の出力: `:6: stranded continuation line ...` の 1 行のみ。`- [x]done...` 自体へのメッセージは一切出ない。
  - suggestion: エントリを開く判定に使う正規表現を、フォーマット検証の対象選定と揃った緩い形（例: `^- \[[x ]\]` — 末尾スペース不要）にするか、両者を同じ土台（例えば既存の `"- [ ]"*` プレフィックス＋ `- \[x\]` チェック済み版を包含する共通述語）から導出する。合わせて、`tests/check-handoff/fixtures/bad-format.md` に子行を持つ malformed トップレベル行のケースを追加し、「stranded continuation line が 0 件」を明示的に assert する回帰テストを足す（下記 Major 参照）。

#### Major

- `tests/check-handoff/run.sh:67-79`(bad-format 回帰ロックの検証不足)— spec の Body-to-AC correspondence 表は「A malformed top-level line still opens an entry (one defect, one message) | D4 | AC12 — ...bad-format fixture must keep reporting exactly its known violation set; a cascade of strand messages under it fails the suite」と明記しているが、実際の `bad-format.md` フィクスチャには malformed 行の下に継続行が一つも無く、このテストは rc≠0 と `format mismatch` 文字列の存在としか assert していない。つまり AC12 はこの一文が主張する保護を実際には提供しておらず、上記 Blocker のバグは 4 つの既存スイート全てが green のまま(見逃されたまま)出荷されるところだった。これは spec 自身が AC14 で経験済みの「anchor が言明する保護を実際には測っていない」vacuity と同種のクラス。

#### Minor / nits

- `bin/close-out.sh:187` 付近(`MAIN_LINE` の flag-rewrite 正規表現)— この行は本タスクの diff に含まれておらず(pre-existing)、spec の Non-goals が明示的に「the flag rewrite」を対象外としているため、本レビューの合否には影響させない。ただし Codex の devil's-advocate パスが指摘した通り、この正規表現は `check-handoff.sh` の `LINE_RE` より緩く、タイトル欠落・不明フラグなどを許容したまま `## Active` から `## Done` へ書き換えてしまえる余地がある。ストランド検知が `## Active` のみを見る設計（D4 で意図的にそう決められている）と組み合わさると、close-out が書き出した後の `## Done` 側は本タスクの新設ゲート群のどれからも見えなくなる。T-1016 の scope 外だが、同じ「境界を締める」目的のタスクとして次スプリント候補にする価値がある。

#### Adversarial findings

- Primary pass (`codex exec review --base develop`): 上記 Blocker と同一の欠陥を独立に発見(`- [ ]broken` 形での malformed-line カスケード)。Codex 自身のラベルは `P2`。
- Adversarial pass(“devil's advocate” framing, 同ファイルを対象): 同じ欠陥の追加形状(空の `- [ ]`、tab 区切り `- [ ]\tbroken`、閉じ括弧直後の句読点)を指摘、および上記 Minor(`close-out.sh` の flag-rewrite の緩さ)と Major(`bad-format.md` 回帰ロックの検証不足)を独立に指摘。Blocker の一般化(あらゆる「開始トークンは通すが厳格な entry-opener には落ちる」形状が同じクラスの誤検知を生む)を確認。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `check-handoff.sh` TASK_LINE_RE/format-gate mismatch causing cascading strand false positives | primary: major(Codex `P2`); adversarial: 追加形状で再確認、severity 明示なし | blocker | D4 という凍結済み Settled Decision に明示違反し、Input space が reachable と宣言する入力形状で実機再現済み。誤検知は根本原因を隠す(checked 版はゼロメッセージ)ため、正規化表の `P2→major` を上回って昇格する。 |
| 2 | `close-out.sh` MAIN_LINE flag-rewrite looseness | adversarial: `high` | minor | この diff に含まれない pre-existing コードであり、spec の Non-goals が明示的に「the flag rewrite」を対象外にしている。今回のレビュー対象ではないため、fast-follow へ降格。 |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1016 を `READY_FOR_REVIEW` から `READY_FOR_ENG` へ戻し、上記 Blocker(と、それを検出できていなかった Major の回帰テスト不足)を紐付けて再着手させる。

#### Fast-follow disposition

- `bin/close-out.sh`(`MAIN_LINE` flag-rewrite 正規表現の緩さ)— file-an-issue。理由: 本タスクの diff に含まれない pre-existing 挙動で、spec の Non-goals が明示的に対象外としているため今回のブロッカーにはしないが、close-out が書き込んだ後の `## Done` は本タスクが強化した `## Active` 専用ゲート群のどれからも見えないという構造的な観測は、次スプリントの「境界を締める」系バックログの入力として残す価値がある。

---

## Round 2 — rework verification (post Codex REQUEST_CHANGES, engineer rework round 1)

- Verdict: PASS
- Scope: focused re-verification of the rework delta (`bin/check-handoff.sh`'s
  `TASK_LINE_RE` loosening + `tests/check-handoff/fixtures/bad-format.md` +
  `tests/check-handoff/run.sh`'s new regression lock) plus regression across
  the four owning suites, not a full repeat of round 1.
- Commits reviewed: `62e0f5e` (Codex `REQUEST_CHANGES`), `9a94872` (rework
  round 1), `29eb588` (board `READY_FOR_REVIEW` → `READY_FOR_QA` after
  rework).

### 1. Reproduced Codex's two Blocker cases

- Unchecked near-miss (`- [ ]broken no space after bracket` + 2 sub-bullets):
  before the fix, `format mismatch` + 2 false `stranded continuation line`
  messages; measured against the fixed tree: **exactly 1 `format mismatch`
  message, 0 strand messages** — matches the engineer's claim.
- Checked near-miss (`- [x]done no space after bracket` + 1 sub-bullet):
  before the fix, 1 false strand message with zero message for the root
  cause; measured against the fixed tree: **exit 0, zero output** — matches
  the engineer's claim.

### 2. Adversarial probes on the loosened entry-opener (over-opening check)

`TASK_LINE_RE` is now `^- \[[x ]\]` (checkbox-shaped prefix, no trailing-space
requirement, but the bracket content is still constrained to exactly `x` or a
literal space by the `[x ]` character class). Three near-miss boundary shapes
that are NOT checkbox-shaped, each with a continuation line under it:

| probe | shape | result | reading against frozen Terms/D4 |
|---|---|---|---|
| A | `- plain prose bullet` | continuation reported as **strand** (exit 1) | Not a task line under any definition — the Terms table's task line requires the `\[[x ]\]` bracket shape, which this line has no trace of. Correctly does not open an entry; D4's malformed-line clause does not reach it because it was never checkbox-shaped to begin with. |
| B | `-- separator` | continuation reported as **strand** (exit 1) | Same reasoning — no `- [` prefix at all, so `TASK_LINE_RE` cannot match regardless of the trailing-space loosening. A boundary line, not a task line. |
| C | `- [z] bogus checkbox` | continuation reported as **strand** (exit 1) | Has the `- [...]` shape but the bracket's single character (`z`) is outside the `[x ]` class the Terms table's task line and D4's malformed-line clause both anchor on (D4 talks about a line "beginning `- [ ]`" that fails the *format* grammar, i.e. checkbox-shaped with `x` or space inside — not an arbitrary bracket payload). Correctly excluded from opening an entry. |

None of the three over-opens. The loosening is scoped precisely to dropping
the trailing-space requirement on an otherwise still-checkbox-shaped
(`x`-or-space bracket content) line — it does not widen what counts as
checkbox-shaped at all. No new Blocker-class finding here.

### 3. Suites + spec gate + check-intent

- All five owning suites re-run green: `check-handoff`, `close-out`,
  `check-intent`, `check-board-headings`, `errexit-safe`.
- The two new `tests/check-handoff/run.sh` assertions confirmed present and
  passing in the suite's own output: `AC2 bad-format.md exits 1, stderr names
  line 6` and `D4/Blocker regression lock — a malformed checkbox-shaped line
  with no space after the bracket still opens an entry (...); exact
  violation set asserted (2 total)`.
- `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh
  .shell-team/specs/T-1016-close-out-entry-boundary.md` → **17 passed, 0
  failed, 1 skipped (AC18)** — matches the expected shape.
- `bash bin/check-intent.sh .shell-team/specs/T-1016-close-out-entry-boundary.md
  .shell-team/todo.md` → `aligned: T-1016 v2 (3ec8dabc...)` (exit 0).
- `shellcheck 0.11.0` (version confirmed) clean on `bin/check-handoff.sh` and
  `tests/check-handoff/run.sh`.

### 4. AC5's four strand cases still fire

Re-ran AC5 through `check-acs.sh` directly: **PASS** — placeholder, prose,
`### ` sub-heading, and CommonMark `* [ ]` bullet all still correctly report
as strands (4 messages, at the 4 expected line numbers). The CommonMark case
in particular starts with `*`, not `-`, so it was never reachable by
`TASK_LINE_RE` regardless of the trailing-space loosening — confirmed
unaffected.

### 5. Independently re-ran the engineer's new mutation self-check

On a fresh `git clone` (not the working tree): reverted `TASK_LINE_RE` back
to the trailing-space form (`^- \[[x ]\] `). `bash tests/check-handoff/run.sh`
→ **exit 1**, the new regression lock's `FAIL:` line reproduces the exact
cascade Codex found (a `format mismatch` on the unchecked near-miss plus 3
false `stranded continuation line` messages, including one for the checked
near-miss's sub-bullet with zero message for the malformed line itself).
Restored the original file → **exit 0**, byte-identical, all suites green
again. Matches the claimed shape exactly.

### Disclosure

- This round did not re-verify round-1 items already confirmed PASS and
  unaffected by this delta (the incident reproduction, the four other
  boundary probes, AC1–AC18 beyond AC5/AC17/AC18, the full CI-wired list) —
  scoped per the coordinator's instruction to the rework delta plus
  regression. Round 1's findings for those items stand.
- The Minor (`bin/close-out.sh`'s `MAIN_LINE` flag-rewrite looseness) is
  explicitly out of this task's scope (pre-existing, non-goal) and was left
  as a fast-follow-file-an-issue candidate by Codex's own recommendation —
  not re-litigated here.

---

### Codex Review verdict (round 2): APPROVE
- Task: T-1016
- Codex model: gpt-5-codex (`codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope (rework round 1 delta, `62e0f5e..HEAD`): 5 files changed, +157/-6 lines (`bin/check-handoff.sh`, `tests/check-handoff/fixtures/bad-format.md`, `tests/check-handoff/run.sh`, `.shell-team/todo.md`, `.shell-team/reviews/T-1016.md`). Cumulative diff scope unchanged from round 1 (`develop...HEAD`, base `a3ee283`).

内部整合性は検証済み・issue 正典との突合はここでは未検証: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — round 1 と同様、issue #89 本体は直接 read していない。

#### Round-1 findings — closed, re-verified live

- **Blocker(round1)を実機で再現・解消を確認**: `TASK_LINE_RE` が `'^- \[[x ]\] '`(末尾スペース必須)から `'^- \[[x ]\]'`(末尾スペース不要、フォーマットゲートと同じ緩いチェックボックス形状プレフィクス)に緩められた。自分の round-1 再現コマンドをそのまま再実行:
  - unchecked 版(`- [ ]broken no space after bracket` + 子行2つ): `format mismatch` が1件のみ、`stranded continuation line` は0件(round1 は format mismatch 1件 + 誤検知2件)。
  - checked 版(`- [x]done no space after bracket` + 子行1つ): exit 0、出力なし(round1 は根本原因メッセージ皆無のまま子行だけ誤検知)。
  - 両方とも報告どおり修正済みであることを確認。
- **Major(round1)を確認**: `tests/check-handoff/fixtures/bad-format.md` に unchecked/checked 両方の near-miss 行が追加され、それぞれに子行(空行を挟んだもの・テーブル行という非ダッシュ形状のものを含む)が付与された。`tests/check-handoff/run.sh` に新しい回帰ロックが追加され、「checked near-miss はメッセージ0件」「unchecked near-miss は format mismatch 1件のみ」「stranded continuation line は全体で0件」「総違反数は正確に2件」を明示的に assert している。実行して green を確認(`tests/check-handoff/run.sh` 全体 exit 0)。この回帰ロックを退行させる mutation(TASK_LINE_RE を末尾スペース必須形に戻す)を独立に試し、ロックが red になることも確認 → 元に戻して green。

#### Devil's-advocate — 緩めた opener 自体への攻撃

以下、`TASK_LINE_RE='^- \[[x ]\]'` がチェックボックス形状でない行まで誤ってエントリを開いてしまわないか、実機で個別に確認(いずれも意図どおり strand として報告され、エントリは開かれない):

- 素のプローズ dash 箇条書き(`- [note] this is prose, not a checkbox`)→ strand(正しい)。
- `--` 区切り線(`-- separator run, not a checkbox --`)→ strand(正しい)。
- チェックボックス内側が不正な形(`- [z]`、`- [xx]`、大文字 `- [X]`)→ いずれも strand(正しい。文字クラス `[x ]` は小文字 `x` か半角スペース1文字のみを許容するため、これらは全て非マッチ)。
- CRLF ボードでの同型の近似崩れ(`- [zz] ...\r\n`)→ strand(正しい、CR は既存の `content="${content%$'\r'}"` で除去済みのため崩れない)。
- 逆方向のケース(正しいチェックボックス形状の直後に余分な文字列が続く `- [ ][z] extra brackets...` + 子行2つ)→ `format mismatch` 1件のみ、子行への cascade なし(D4 の意図どおり)。

QA round 2 が独立に試した3つの adversarial probe(prose bullet／`--`区切り／`- [z]` bogus checkbox)とも一致する結果。

#### AC5 の4境界クラスと `emit()`/violations カウンタ契約への影響

`CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh .shell-team/specs/T-1016-close-out-entry-boundary.md` を実行し、AC1〜AC18 で **17 PASS / 0 FAIL / 1 SKIP(AC18)** を確認。AC5(placeholder／bare prose／`###` 見出し／CommonMark `* [ ]` bullet の4境界クラスがそれぞれ正しく strand 判定されること)、AC4・AC11(`emit()` 経由の1違反1メッセージ・violations カウンタ)ともに変化なく green。5つの所有スイート(`check-handoff`/`close-out`/`check-intent`/`check-board-headings`/`errexit-safe`)を全て実行し green。`shellcheck 0.11.0` で `bin/check-handoff.sh`・`tests/check-handoff/run.sh` ともに clean。`check-intent`/`check-provenance`/`check-interventions` の3ゲートも green(spec は v2 ハッシュのまま無変更)。

#### Same-class-2 判定(明示)

Round 1 の根本原因クラス(「2つの matcher が同じ判定を異なる厳しさで行い food 不一致を起こす」境界述語ミスマッチ)に該当する **新規の Blocker/Major は見つからなかった**。close-out.sh のタスク特定用正規表現(特定タスクIDの厳密一致)、check-board-headings.sh の ID 抽出用正規表現、check-intent.sh の TOP_RE(このタスク自身のスコープ再オープン専用)は、いずれも「任意の行が一般的にタスク行として entry を開くか」を判定する汎用ロジックではなく round-1 のバグとは異なる役割を持つため、同一クラスの再発は確認されなかった。**full-inventory switch はトリガーしない**。

#### Blockers

なし。

#### Major

なし。

#### Minor / nits

- `bin/check-handoff.sh:13`(ファイル冒頭の一般説明コメント)— 「no task line, `^- \[[x ]\] `, seen above it」という記述が、round-1 の修正で実際の述語が末尾スペース不要形になった後も更新されておらず、古いリテラルのまま残っている(TASK_LINE_RE 定義直上の詳しいコメントブロックは正しく更新済みで、この冒頭コメントだけが取り残された)。挙動には影響しない、コメントの陳腐化のみ。
- `tests/check-handoff/run.sh:105` 付近(新規回帰ロックの fail メッセージ文言)— 「the two malformed top-level lines, one message each」という説明が、実際には(a) 既存の bold-ID 欠落行1件 + (b) 新設 unchecked near-miss 行1件、で「2件」を構成しており、checked near-miss 行はメッセージ0件という assertion 自体の意図とはやや紛らわしい書き方になっている。assert 自体(件数=2、strand 0件、checked near-miss は無出力)は正しく機能している。表現のみの nit。

Codex 自身の primary/adversarial 両パスもこの2点(コメント陳腐化、テストの fail メッセージ文言)のみを minor として指摘し、それ以外の correctness regression は「見つからなかった」旨を明示。

#### Adversarial findings

- Primary pass(`codex exec review --base develop`、累積 diff 全体対象): 「No actionable correctness regressions were identified in the changed production logic. The continuation-boundary behavior is consistently implemented across the affected parsers and covered by focused fixtures.」
- Adversarial pass(devil's-advocate framing、round-1 の修正箇所に的を絞ったプロンプト): 上記2件の Minor(コメント陳腐化・テスト文言)のみ。「prose bullets, `--` runs, `[z]`, `[xx]`, uppercase `[X]`, indented look-alikes, and their CRLF variants do not open entries. The four AC5 boundary classes and emit()/violations behavior are unchanged.」と明記。

#### Synthesis audit ledger

no severity changes

#### Recommendation

- APPROVE → `.shell-team/todo.md` の T-1016 を `READY_FOR_MERGE` に進めてよい。

#### Fast-follow disposition

- (round 1 より再掲・変更なし)`bin/close-out.sh`(`MAIN_LINE` flag-rewrite 正規表現の緩さ)— file-an-issue。理由: 本タスクの diff に含まれない pre-existing 挙動で、spec の Non-goals が明示的に対象外としているため今回もブロッカーにはしないが、close-out が書き込んだ後の `## Done` は本タスクが強化した `## Active` 専用ゲート群のどれからも見えないという構造的な観測は、次スプリントの「境界を締める」系バックログの入力として残す価値がある。
- `bin/check-handoff.sh:13` の陳腐化した冒頭コメント — won't-fix。理由: 挙動に影響しない純粋な文言のずれで、次にこの述語自体に手を入れる回でついでに直る性質のもの。単独の issue を立てるほどの実害はない。
- `tests/check-handoff/run.sh:105` 付近の fail メッセージ文言 — won't-fix。理由: assertion 自体は正しく機能しており、fail 時にしか表示されない説明文の精度の問題にとどまる。単独の issue を立てるほどの実害はない。
