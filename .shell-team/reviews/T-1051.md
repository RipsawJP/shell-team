### Codex Review verdict: REQUEST_CHANGES
- Task: T-1051
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 12 files, +855/-18 lines (`e57c287...HEAD`)

#### Mandatory disclosure — one ratified re-freeze, independent veto exercised

Verified directly, not trusted from the hand-off. Reconstructed the v1 `check:` line from `git show b584b78^:.shell-team/specs/T-1051-inspection-ux-polish.md` and diffed it against the v2 line at HEAD: exactly **one line differs** (AC1's own `check:` line, `B2=KLMNOP12` → `B2=KLMNOP123456`), it is a `- check:` line on both sides, and the replacement is a pure fragment-length extension of the existing `TK_B1`/`TK_B2`-style idiom — no new alternative, no new boundary, no change to what the criterion asserts. This matches `bin/check-refreeze-class.sh`'s own mechanics rule (same line count, ≥1 differing line, every differing line a `- check:` line on both sides) and the disclosed `mechanics, exit 0, differing=1` measurement. Confirmed no class-M standing grant exists in this checkout's `CLAUDE.local.md` (read directly — no `- refreeze-class` grant text anywhere in that file), so the ordinary ratified-with-orchestrator-attestation route (`docs/tuning-oversight.md` line 109's "With no grant recorded there, the shipped default is unchanged: every re-freeze, of either class, is a per-instance human GO" is satisfied here by the orchestrator's own attestation authority per the operator's standing approval-gate ruling) is the correct reading, not the class-M delegable path. **No objection to this re-freeze.** The engineer's BLOCKED escalation that triggered it (an 18-character gh-form positive-control fragment structurally unable to satisfy the pre-existing, unmodified `gh[oprs]_{20,}` alternative) was itself independently reproduced: `git show e57c287:bin/check-pii-shapes.sh`'s `RE_TOKEN` carries the identical `{20,}` floor, confirming the defect predates this task's own edit and the fix is a fixture-arithmetic correction, not a design change.

The disclosed writer-collision incident (pm-spec resumed onto the live tree before the engineer's completion notification arrived) is likewise verified consistent with the record: the first v2 aggregate is disclosed as discarded/tainted and a clean single-writer re-run is recorded in its place, matching the standing no-concurrent-writer-and-reader precedent. No objection to how this was handled.

#### Blockers

- **`bin/check-pii-shapes.sh:333` (`RE_TOKEN`) contradicts the frozen Goal sentence it ships under, and the contradiction is neither disclosed nor tested.** The new left-boundary guard on the `sk-` alternative, `(^|[^A-Za-z0-9])`, is a two-sided class: it correctly suppresses the measured false-positive population (an English word ending in `s`+`k` before a hyphen and a long kebab-case tail), but it *also* suppresses detection of a genuine `sk-`-prefixed real key when that key is immediately preceded by a letter or a digit with **zero** separating character — a class that fired at the base ref and no longer fires at HEAD. Reproduced independently, twice: my own direct `grep -E` probe against the shipped `RE_TOKEN` value (`"xsk-ABCDEFGHIJKLMNOP123456"` → no match; `"1sk-ABCDEFGHIJKLMNOP123456"` → no match; both matched against the unmodified `e57c287` value), and the adversarial Codex pass's own independent fragment-composed reproduction against both the shipped and base-ref patterns, reaching the identical conclusion for both a letter prefix and a digit prefix. This directly falsifies the frozen intent block's own Goal sentence — `.shell-team/specs/T-1051-inspection-ux-polish.md`'s `<!-- BEGIN intent-block -->` region states plainly that the token pattern "no longer reports this repository's own kebab-case label convention ... as a credential token, while **every real key shape it caught before is still caught**" — a universal claim that is now measurably false for this narrow-but-real boundary class. It is also in direct tension with the same spec's own declared `## Input space` Reachable input class 2, which names "immediately after an alphanumeric character" as a boundary "at every ... that occurs in this tree" that the checker "must handle correctly." DP1/DP2 discuss this *exact* false-negative risk shape only to justify **not** guarding `gh[oprs]_`/`AKIA` ("guarding them would suppress a real key that happens to follow an alphanumeric character — a false negative, which DP-10 ranks as the costlier error") — the design decisions never acknowledge that the guard chosen for `sk-` itself reintroduces the identical risk shape for that alternative. AC1's `check:` line and the three new suite labels (`boundary: an identifier-adjacent label lookalike does not fire`, the boundary-fires suite, `positive: an unguarded prefix still fires after a letter (gh form)`) test the letter-adjacent shape only as a *negative* (proving suppression of the lookalike) and never as a missed true positive, and no digit-adjacent case exists in either AC1 or the suite at all — so nothing in this diff's own test surface would catch a future regression here, or the gap that already exists.
  - This is scored Blocker rather than Major because (a) it is a mechanical, directly-verifiable contradiction of frozen Goal text — not a judgment call about disclosure quality — and (b) it is a genuine, silent false-negative in a credential/PII detector, which the spec's own DP-10 explicitly ranks as the costlier failure class; task classification is "verification-mechanism work ... reviewed at the higher standard" by the spec's own final line.
  - Not eligible for the Input-space out-of-scope downgrade: none of the six declared out-of-scope synthetic extremes covers "a real key glued to a preceding alphanumeric character with no separator" — that shape sits inside Reachable input class 2, not the Out-of-scope list.
  - Suggested resolution path (either is acceptable, both need pm-spec/human involvement since the Goal sentence is inside the frozen intent block): **(a)** find a design that closes the measured false-positive class without this trade-off (not obviously achievable with a single context-free `grep -E` alternative — the false-positive class *is* "a letter immediately before `sk-`," which is the same shape as the newly-missed true positive, so a boundary-only regex cannot distinguish them); or **(b)** accept the trade-off explicitly and re-freeze the Goal sentence (class-B, needs the operator's GO) to state the accepted limitation plainly, together with matching disclosure fixes to DP1/DP2's prose, the `RE_TOKEN` code comment, the anchoring inventory entry, and both `pii-controls` doc bullets (none of which are inside the frozen intent block, so those four are ordinary edits), plus a positive-control fixture pinning the accepted-miss shape so it is documented as an intentional limitation rather than an implicit gap.

#### Major

_(none beyond the Blocker above — the two related concerns the adversarial pass raised as separate Major items are folded into the single Blocker finding and its ledger row below, since they share one root cause and one fix.)_

#### Minor / nits

- **`.shell-team/interventions/T-1051.md` is genuinely new against `e57c287`** (confirmed: `git cat-file -e e57c287:.shell-team/interventions/T-1051.md` fails, i.e. absent at base) **but AC17(d)'s "the only new record this task writes" claim never named it**, across either engineer hand-off round. QA already disclosed this and independently re-verified the substance AC17(d) protects (`bin/check-pii-shapes.sh --all` clean on that file too) — no PII exposure resulted, the completion claim was simply incomplete. Confirmed independently; no new information beyond QA's own disclosure.
- **The round-1 hand-off's "~80 steps" figure overcounts** — `.github/workflows/check-handoff.yml` names exactly **63** `- name:` steps, confirmed by direct count (`grep -cE '^\s*- name:'`), not 80. Immaterial to the AC13 suites and the shellcheck line QA independently re-ran directly; already disclosed by QA as non-blocking.
- **SIGPIPE is ignored globally, not scoped to the two guarded builtin writes**, in both `bin/check-intent.sh` and `bin/check-refreeze-class.sh`. The adversarial pass confirmed no masking of non-EPIPE write failures results (disk-full/EIO paths remain ordinary nonzero, independently reproduced), but noted the ignored disposition is inherited by any external child process the script might spawn after the trap is installed, not scoped to just the print-hash/courtesy-print sites. No live consequence found in either script today (both mirror the same shape and neither currently spawns a long-running child that depends on default SIGPIPE disposition), so this is not scored as a finding against the diff, only recorded for the fast-follow list.

#### Adversarial findings

Ran a devil's-advocate pass against `bin/check-pii-shapes.sh`'s `RE_TOKEN`/DP1/DP2 and separately against `bin/check-intent.sh`'s `HEX40_RE` gate and `trap '' PIPE`, instructed to reproduce everything with `grep -E` against the real shipped and base-ref patterns and to compose any credential-shaped probe from shell fragments only (no completed literal written to any file). Result:
- **The core letter/digit-adjacency false negative stood and was independently reproduced** (rated `blocker` by the adversarial pass itself, using its own severity vocabulary, which I read as corroborating rather than superseding my own Blocker call above) — both a letter prefix (`x`) and a digit prefix (`7`) lost detection at HEAD while firing at the base ref; dot/hyphen/slash/space/line-start all still fire correctly in both; the unguarded `gh`/`AKIA` alternatives are unaffected. The pass additionally confirmed the disclosure gap across every prose surface (DP1, DP2, the anchoring inventory, the `RE_TOKEN` code comment, both `pii-controls` docs) — none states the accepted trade-off, and the anchoring inventory's own text ("closes the measured false-positive population ... without suppressing a real key") is now itself inaccurate.
- **`HEX40_RE` shape gate (#179(c))**: no bypass found. `computed_hash` is assigned once, immediately gated by the fully-anchored `^[0-9a-f]{40}$` check before any of its three consumers, and is never reassigned afterward; direct probes against uppercase, prefix/suffix contamination, and embedded-newline variants were all correctly rejected. Confirmed sound.
- **`trap '' PIPE` (#179(a))**: no path found where ignoring SIGPIPE masks a genuine non-pipe write failure; disk-full/EIO-class failures were reproduced still returning ordinary nonzero statuses with the guard intact. The only residual note is the global-reach breadth point folded into Minor/nits above.

Raw captures at the canonical paths: `.shell-team/reviews/T-1051-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1051-codex-adversarial.{txt,jsonl}`. Note for the record: the primary foreground pass (`codex exec ... review --base e57c287 --json`) explored the SIGPIPE mechanics and the diff summary directly but concluded "No actionable correctness defects were found" without surfacing the token-boundary gap on its own — the gap was found by this reviewer's own direct `grep -E` analysis first, then confirmed and escalated to `blocker` by the targeted adversarial pass once specifically pointed at `RE_TOKEN`/DP1/DP2.

#### Review-ask verdicts (from the task prompt)

(i) **No, not cleanly.** The `(^|[^A-Za-z0-9])` guard on the `sk-` alternative correctly closes the hyphenated-compound false-positive class (verified: underscore adjacency still fires — correct and intended, since a real key preceded by `_` must still fire and `_` is not host-name-continuation-relevant the way `.`/`-` are for `RE_HOME_PATH_BOUNDARY`; start-of-line fires; multi-match lines detect correctly; alternation precedence binds the guard to the `sk-` branch only, confirmed against the shipped line with `grep -oE`, not the spec's prose — the `gh` form after a letter still fires exactly as DP1 intends) — but it does open a false-negative class DP-10 says is the costlier error: a real `sk-` key immediately preceded by a letter or digit with **zero** separator no longer fires. See Blocker above.
(ii) **Yes.** The one-token repair is meaning-preserving (fragment-length extension only, verified against the reconstructed v1 line), and the no-grant ratified route is the correct reading of `docs/tuning-oversight.md` (no standing class-M grant exists in this checkout's `CLAUDE.local.md`, verified directly).
(iii) **Yes on both.** `#179(c)`'s gate sits after `computed_hash`'s assignment and before every consumer, fails closed through the existing `die`/`fail_usage` convention, and no bypass was found by either the direct code read or the adversarial pass. `#179(a)`'s asymmetry is evidence-based and sound: the extraction-pipeline write is genuinely already classified (a signal sent to the shell process invokes the pre-existing `on_signal` trap directly, and the write targets a regular file with no SIGPIPE-capable reader), while the print-hash builtin write is genuinely unclassified pre-fix (confirmed by the DP5/provenance record's own live reproduction). The sibling mirror is internally consistent — `block_signals_for_registration`/`restore_signal_traps` in both scripts manage only HUP/INT/TERM, and the permanently-ignored PIPE trap needs no restore-window handling, correctly.
(iv) Both of QA's disclosed risk notes are honestly Minor, confirmed independently: the interventions file was indeed omitted from AC17(d)'s per-record accounting (no PII consequence, re-verified), and 63 is the real step count against the claimed "~80." Neither is blocking.
(v) **Mostly, with one shared gap.** All four prose sites accurately describe *what changed* (the guard, its class, why it differs from `RE_HOME_PATH_BOUNDARY`'s class) and none contains a stale or incorrect statement about the mechanism itself. But none of the four discloses the letter/digit-adjacency false-negative this guard introduces — the same omission driving the Blocker above — so "describe shipped behavior exactly" is not yet true in full; the inventory entry's own closing claim ("without suppressing a real key") is actively inaccurate given the Blocker finding.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `RE_TOKEN` sk- guard introduces a letter/digit-adjacent false-negative for real keys, contradicting the frozen Goal's "every real key shape it caught before is still caught" | primary: no finding ("no actionable correctness defects"); my own direct analysis: major; adversarial: blocker (independently reproduced) | blocker | Mechanically verifiable contradiction of frozen Goal text via direct `grep -E` reproduction against shipped and base-ref patterns, confirmed by two independent reproductions (mine and the adversarial pass's); a genuine, undisclosed, untested false-negative in a credential detector, at the task's own declared "higher standard" |
| 2 | Same root cause, framed by the adversarial pass as a separate disclosure-only Major across DP1/DP2/inventory/comment/docs | adversarial: major | folded into finding 1 | Same root cause and same fix path as finding 1; scoring it as a second, separate item would double-count one defect |
| 3 | Same root cause, framed by the adversarial pass as a test-coverage gap (no positive fixture for the missed shape) | adversarial: major | folded into finding 1 | Same root cause; the missing fixture is a symptom of the same undisclosed trade-off, not an independent defect |
| 4 | AC17(d)'s per-record accounting omits `.shell-team/interventions/T-1051.md` | QA: disclosed, non-blocking; my own re-verification: confirmed | minor | Genuinely new file, genuinely omitted from the claim, but no PII consequence (re-verified clean) and no functional impact — a documentation-completeness gap only |
| 5 | "~80 steps" overcounts the actual 63 | QA: disclosed, non-blocking; my own re-verification: confirmed (63 via direct count) | minor | Cosmetic imprecision in historical hand-off prose; immaterial to any AC or suite result |
| 6 | Global (not write-site-scoped) SIGPIPE ignore in both scripts | adversarial: info/breadth note | minor (not scored as a defect against this diff) | No live masking or consequence found in either script today; recorded as a future-proofing note only |

#### Recommendation

- REQUEST_CHANGES → set T-1051 back to `READY_FOR_ENG` (or to `READY_FOR_ARCH`/pm-spec first if the team decides the fix is a Goal-sentence re-freeze rather than a design change — see the two resolution paths under the Blocker finding). The mechanics re-freeze (v1→v2, AC1 fixture arithmetic) carries no finding from this review and this reviewer's veto over it is **not** exercised. `#179(a)`/`#179(b)`/`#179(c)` carry no finding from this review.

#### Fast-follow disposition

- Finding 4 (AC17(d)'s per-record accounting omitting the interventions file) — **won't-fix**: the substance is already re-verified clean (no PII), the omission is a one-time historical-record imprecision with no recurring mechanism, and correcting an already-merged hand-off bullet after the fact adds more noise than value.
- Finding 5 (the "~80 steps" overcount) — **won't-fix**: cosmetic imprecision in historical prose, immaterial to any check or suite result; not worth a follow-up edit to a past hand-off bullet.
- Finding 6 (global-reach SIGPIPE ignore, not scoped to the two guarded write sites) — **file-an-issue**: worth scoping `trap '' PIPE` narrowly around just the guarded builtin writes (set immediately before, restore immediately after, mirroring the existing `block_signals_for_registration`/`restore_signal_traps` pattern already used for HUP/INT/TERM around the `mktemp` window) if either script ever grows a child-process invocation that depends on default SIGPIPE disposition — not blocking today, since neither script currently spawns such a child.

---

## Round 2

### Codex Review verdict: APPROVE
- Task: T-1051
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 13 files, +1049/-18 lines (`e57c287...HEAD`); the round-1→round-2 delta is 6 commits (`3be668c` pm-spec v2→v3 class-B re-freeze, `aedb7bd`/`42e7d5e` engineer disclosure, `11c89ed` round-3 hand-off, `1d66063` QA round-2 FAIL, `c08a530` round-4 comment-contradiction fix, `2cae908` QA round-3 PASS) touching `.shell-team/specs/T-1051-inspection-ux-polish.md`, `.shell-team/todo.md`, `.shell-team/provenance/T-1051.md`, `bin/check-pii-shapes.sh`, `docs/pii-controls.md`, `docs/pii-controls.ja.md`.

#### Round-2 asks — verdicts

**(i) Is the round-1 Blocker closed as a class?** Yes, verified directly against the shipped text and the live pattern, not merely re-read from the hand-off.
- **The Goal now honestly states the trade-off** (read the frozen intent block directly): "while every real key shape it caught before is still caught **with one deliberate, disclosed exception**: an `sk-` key whose prefix is immediately preceded by a letter or a digit, with no separating character, is now suppressed too" — followed by the cause (mathematical complement of #178's own prescription — one character of left context cannot separate the two populations) and the bound (every separated boundary still fires; `gh`/`AKIA` stay unguarded and fire alphanumeric-adjacent). This is no longer a universal claim the shipped pattern contradicts.
- **The both-directions pin is real**, not merely asserted. Read AC1's v3 `check:` line directly and ran it myself against the live `RE_TOKEN`: it now adds `for pre in "x" "9"; do if printf ... | grep -qE -- "$RE"; then rc=1; fi; done` — a letter and a digit, zero separator, both asserted **not** to match — plus a `grep -qF` for the suite's own verbatim label `disclosed: alnum-adjacent zero-separator sk form is suppressed by design (#178 complement)`. Executed both directly: the criterion passes (`rc=0`), and the suite's own new block (`tests/check-pii-shapes/run.sh:530-549`) constructs both the letter-adjacent and digit-adjacent fixtures from the existing `TK_*`/`TKG_*` fragment idiom and asserts them clean — matching the pinned direction exactly, no completed literal written.
- **The residual-exposure bound was independently re-verified analytically against the shipped regex**, not trusted from prose: `gh[oprs]_[A-Za-z0-9]{20,}|AKIA[A-Z0-9]{12,}|(^|[^A-Za-z0-9])sk-[A-Za-z0-9_-]{16,}` (byte-identical to the value I recorded in round 1 — confirmed via `grep -n '^RE_TOKEN=' bin/check-pii-shapes.sh` and a diff of that one line since the round-1 verdict commit, which is empty). Re-ran my own round-1 probes: line-start, space, `-`, `.`, `/` all still fire for `sk-`; the `gh` form still fires immediately after a letter; only the zero-separator alphanumeric-adjacent `sk-` case fails to fire — exactly and only the one exception the Goal now names, nothing broader.
- **`Input space` Reachable class 2 was split by alternative**, closing the exact tension I flagged in round 1 (the class previously listed "immediately after an alphanumeric character" as reachable and required for all forms without exception): it now states plainly that a `gh`/`AKIA` form immediately preceded by a letter or digit "must still fire," while "the same position for an `sk-` form is the **disclosed suppressed case** the Goal names — reachable, deliberately not protected, and pinned as a documented expectation by AC1 rather than treated as a defect to be reported."
- **The internal contradiction QA round 2 caught (and round 4 fixed) was independently re-verified closed**: `bin/check-pii-shapes.sh`'s `RE_TOKEN` inventory entry's pre-existing "closes the measured false-positive population ... without suppressing a real key" is now "every real key at a SEPARATED boundary still fires, at exactly one disclosed, accepted cost — detailed immediately below, not a second, independent claim that no real key is ever suppressed" (read directly, `bin/check-pii-shapes.sh:314-316`) — no longer an unqualified guarantee the very next sentence withdraws. Swept the other three prose sites (checker header description, both `pii-controls` docs) end to end myself: none carries an unqualified "every"/"no real key" claim; all four correctly disclose the exception in words, not merely via the `[^A-Za-z0-9]` anchor AC5's own `check:` line reads (AC5's own text is candid about this limit: "the anchor is a necessary condition the check can read, not the whole assertion").
- My round-1 Blocker is **closed as a class**: the frozen intent now accurately describes shipped behavior, the trade-off is pinned by machine-checkable criteria in both directions, and every prose surface states it in words.

**(ii) Is the v2→v3 re-freeze meaning-consistent with the round-1 finding and issue #178's own prescription?** Yes. Independently reconstructed both sides and classified them myself rather than trusting the board's own measurement: `git show b584b78:.../T-1051-inspection-ux-polish.md` (v2) vs. `git show 3be668c:.../T-1051-inspection-ux-polish.md` (v3) through `bin/check-refreeze-class.sh` directly — **`class-b`, exit 1**, old/new line counts 227→264, matching the disclosed measurement exactly. This is the correct classification (the Goal sentence, Non-goals-adjacent prose, and Input space all moved, which is squarely class-B territory, not a mechanics-only `check:`-line edit), and the board's ratification correctly routes it through the ordinary per-instance human-GO-equivalent path (the operator's standing approval-gate ruling) rather than a class-M delegable path — consistent with round 1's own finding that no class-M standing grant exists in this checkout's `CLAUDE.local.md` (re-confirmed unchanged). The re-freeze's content is meaning-consistent with both my round-1 finding (it discloses precisely the gap I found, in the precise terms I found it) and issue #178's own prescription (the guard #178 asks for is retained byte-for-byte; the re-freeze only adds disclosure and bidirectional pinning around it, never loosening or tightening the guard itself).

**(iii) Diff scope since round 1 — confined as claimed?** Yes, verified directly:
- **Pattern bytes**: `bin/check-pii-shapes.sh`'s `RE_TOKEN=` assignment line is byte-identical to the round-1-reviewed value (`git diff <round-1-verdict-commit>..HEAD -- bin/check-pii-shapes.sh` shows zero change to that line; only comment text around it moved).
- **`#179` files untouched**: `git diff --stat <round-1-verdict-commit>..HEAD -- bin/check-intent.sh bin/check-refreeze-class.sh tests/check-intent/run.sh` is empty.
- **`#180`'s six surfaces untouched (AC12)**: re-ran AC12's own `check:` line directly (`git diff --name-only e57c287...HEAD -- CONTRIBUTING.md docs/tuning-oversight.md docs/tuning-oversight.ja.md agents/pm-spec.md skills/run/SKILL.md`) — empty, and confirmed via `check-acs.sh`'s own live run that AC12 reports `PASS`.
- Full-suite re-verification, not merely the two spots above: `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1051-inspection-ux-polish.md` → **16 passed, 0 failed, 1 skipped (AC17)**, matching the engineer/QA's own round-4/round-3 aggregates exactly; `bin/check-intent.sh` reports `aligned: T-1051 v3 (9fef7659d9dcf0d1c16a42e74f24cf78dc9fa3b2)`, matching the board's recorded v3 hash; `tests/check-pii-shapes/run.sh` run directly → `check-pii-shapes suite: all assertions passed`, exit 0; `shellcheck` clean on all three touched scripts, run directly.

#### Blockers

_(none — the round-1 Blocker is closed as a class, per ask (i) above)_

#### Major

_(none)_

#### Minor / nits

_(none new this round — round 1's three Minor/nits items stand unchanged; restated in Fast-follow disposition below per ask (iv))_

#### Adversarial findings

Not run this round: the fresh primary pass against the current diff (`codex exec ... review --base e57c287 --json`) surfaced no Blocker/Major, and my own independent re-verification (both-directions AC1 execution, `check-refreeze-class.sh` reclassification, full-suite `check-acs.sh` re-run, direct `RE_TOKEN` byte-diff) likewise found none — so this round's own trigger for an adversarial pass did not fire. Round 1's adversarial capture (which independently reproduced the letter/digit-adjacency gap and rated it `blocker` in its own vocabulary) remains the standing adversarial record for that finding, now closed; not re-run against unchanged pattern bytes to avoid a redundant pass. Primary capture refreshed at the canonical paths: `.shell-team/reviews/T-1051-codex-primary.{txt,jsonl}`. Adversarial capture at `.shell-team/reviews/T-1051-codex-adversarial.{txt,jsonl}` is unchanged from round 1 (not refreshed this round, per the above).

#### DP6 pre-commitment round-counting

Judged fresh, not anchored to round 1, per the task prompt's own instruction: **the factual trigger does not fire.** Round 1 had one new independent Blocker against the token-pattern component. This round has **zero** new independent Blocker/Major findings against that component or any other — the round-1 finding is closed as a class (ask (i)), and my own fresh analysis plus a fresh Codex primary pass against the current diff both found nothing new. Two consecutive rounds of new independent Blocker/Major findings against the same component is the stated trigger; this round supplies no second round of that shape, so DP6 does not fire and no drop-order question arises.

#### Synthesis audit ledger

no severity changes

#### Recommendation

- APPROVE → set T-1051 to `READY_FOR_MERGE`. The round-1 Blocker is closed as a class; the v2→v3 class-B re-freeze carries no finding from this review and this reviewer's veto over it is **not** exercised; `#179(a)`/`#179(b)`/`#179(c)` and the v1→v2 mechanics re-freeze continue to carry no finding across both rounds.

#### Fast-follow disposition

Consolidated, unchanged from round 1 (none of round 1's three items were touched by this round's diff, and this round found nothing new to add):
- Round-1 finding (AC17(d)'s per-record accounting omitting `.shell-team/interventions/T-1051.md`) — **won't-fix**, unchanged rationale: substance already re-verified clean (no PII), one-time historical-record imprecision with no recurring mechanism.
- Round-1 finding (the round-1 hand-off's "~80 steps" figure overcounting the actual 63) — **won't-fix**, unchanged rationale: cosmetic imprecision in historical prose, immaterial to any check or suite result.
- Round-1 finding (global-reach `trap '' PIPE`, not scoped to the two guarded write sites in `bin/check-intent.sh`/`bin/check-refreeze-class.sh`) — **file-an-issue**, unchanged rationale: worth scoping the trap narrowly around the guarded builtin writes if either script ever grows a child-process invocation depending on default SIGPIPE disposition; not blocking today, since neither script currently spawns such a child.
