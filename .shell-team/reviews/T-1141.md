# QA review — T-1141

## check-intent

`bash bin/check-intent.sh .shell-team/specs/T-1141-record-portability-block.md .shell-team/todo.md`
→ `check-intent: aligned: T-1141 v1 (7ba5edea98ee8357a4dfecdf9f9ac9f6b869276a) matches .shell-team/specs/T-1141-record-portability-block.md`

## check-acs (live, round 1)

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1141-record-portability-block.md`
→ `check-acs: 11 passed, 2 failed, 1 skipped, 0 unrecognized`
→ `check-acs: FAILED: AC12 AC13`

AC1–AC11 PASS live. AC12/AC13 FAIL only because `.shell-team/reviews/T-1141.md` did not yet exist at the time of that run (both criteria's own required-file check names it explicitly) — expected, per the engineer's own disclosed note. Re-run after this file exists, below.

## Independent spot-checks (beyond trusting the checker's own exit code)

- **Block content** (`templates/prompt-blocks/record-portability.md`): opens with `## Record portability`; contains `never by name`, `measured values`, all four generic nouns; no `'''`; no `.ja.md` counterpart. Confirmed by direct read, not only AC1's grep.
- **Six consumer regions byte-identical**: extracted each of the six files' `<!-- BEGIN prompt-block: record-portability -->` … `<!-- END -->` region with `sed` and diffed by eye against the canonical file — all six identical, independent of `check-prompt-sync.sh`'s own verdict.
- **Zero-deletion per file**: `git diff --no-renames --numstat "$B"...HEAD -- <file>` for each of the six role files — all show `N 0` (insertions only, `0` deletions): scrum-master 10/0 (block + relay-boundary paragraph), the other five 8/0 (block only).
- **`bash bin/check-prompt-sync.sh`** → `all registered prompt blocks in sync`. **`bash tests/check-prompt-sync/run.sh`** → 18 PASS, 0 FAIL (`grep -c '^PASS:'`/`grep -c '^FAIL:'` on the captured log).
- **Retro template**: `**Cycle scope**`…`## Retro inputs` region contains `the adopter repository` and `orchestrator record`; all five `<!-- retro-section: * -->` markers occur exactly once each and each is immediately followed by its `## ` heading (independently re-derived line numbers, not the check's own numbers); the nine `- input:` ledger lines are `cmp`-identical to the branch-point blob.
- **`bash bin/check-retro.sh docs/templates/retro-template.md`** → exit 0. Population re-derived at run time: `R=$(bash bin/team-paths.sh --get retros)` → `.shell-team/retros`; `git ls-tree -r --name-only HEAD -- "$R/" | grep -c '\.md$'` → 31; looped `bash bin/check-retro.sh <each>` individually → 0 failures. **`bash tests/check-retro/run.sh`** → 43 PASS, 0 FAIL.
- **`bash tests/check-pii-shapes/run.sh`** → 124 PASS, 0 FAIL (`grep -c '^PASS:'`/`grep -c '^FAIL:'`). **`bash bin/check-pii-shapes.sh --base "$B"`** and **`--all`** → both `clean (no PII-shaped bytes found)`. **`PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all`** (informational, opt-in is off by default) → many pre-existing findings across the tree (tracker-key-shaped substrings already committed in prior tasks' own interventions/provenance/spec/test files, e.g. `.shell-team/interventions/T-1003.md`, `tests/rework-digest/run.sh`); none of these findings fall inside this task's own new prose in the six consumer regions, `templates/prompt-blocks/record-portability.md`, or `docs/templates/retro-template.md`'s new lines — confirmed by cross-referencing the finding list against this task's changed-file set.
- **`bash tests/codex-agents/run.sh`** → 37 PASS, 0 FAIL. Scratch regeneration: `bash bin/gen-codex-agents.sh --root . --out-dir "$D"` into a fresh `mktemp -d` → non-empty TOML for each of the five generated roles, each containing the fixed string `record-portability`; `bash bin/check-codex-agents.sh --root . --out-dir "$D"` → `in sync with ./agents`.
- **`bash bin/check-provenance.sh .shell-team/provenance/T-1141.md`** → `conformant (4 decision entries, 0 sentinel)`. **`bash bin/check-interventions.sh --task T-1141 .shell-team/interventions/T-1141.md`** → `conformant (0 entries, 1 sentinel)`. **`bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"`** → exit 0. **`bash bin/check-durability.sh --phase implement --task T-1141 --ref HEAD`** → `durable: 4 record(s) for phase implement, task T-1141, observed in HEAD`.
- **`shellcheck tests/check-pii-shapes/run.sh`** (0.11.0, the workflow-pinned version; this file is edited by this task and is in the workflow's `shellcheck` argument list) → exit 0. **`bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base "$B"`** → exit 0.

## Execution-context matrix (AC14(b))

Re-ran `check-prompt-sync.sh` two ways: `env PATH="$PWD/bin:/usr/bin:/bin" check-prompt-sync.sh` (bare name) and `env PATH="/usr/bin:/bin" bash bin/check-prompt-sync.sh` (no plugin `bin/` on `PATH`) — both reproduced `all registered prompt blocks in sync`. Confirms the engineer's own AC14(b) claim rather than accepting it as reported.

## Mutation self-check (AC14(a)) — independently reproduced in a scratch clone, real tree untouched

Per the "never destroy the working tree" rule, all mutation below was done in `git clone --shared . "$TMPDIR/t1141-mutation-clone"` at the same HEAD, never on the live checkout. `git status --short` on the real repo confirmed clean before and after.

- **AC1's guard**: mutated `templates/prompt-blocks/record-portability.md` in the clone (`measured values` → `mutated away`) → AC1's own check line, run against the clone, `rc=1` (red, as expected). Restored the file byte-for-byte (`cmp` confirmed identical) → same check line `rc=0` (green). Confirms the guard is live, not vacuous.
- **New PII-fixture controls, naming facet**: added a tracker-key-shaped line (the same two-letter-namespace-plus-hyphen-plus-digits shape `RP_POS_LINE` restores, described here by construction rather than quoted — see the engineer's round-3 correction to this bullet) to the *negative*-control fixture (`RP_CLEAN_REPO`) in the clone → re-ran the suite → the negative control itself now `FAIL`s with `FINDING pattern=tracker-key path=retro-external-run.md line=3` — proving the negative control's own label corresponds to logic that actually fires on a real shape, not a control that would pass regardless of content.
- **New PII-fixture controls, wiring facet**: removed the restored line (`$RP_POS_LINE`) from the *positive*-control fixture (`RP_POS_REPO`) in the clone, leaving it identical to the negative fixture → re-ran the suite → the positive control now `FAIL`s with `rc=0 out=check-pii-shapes: clean (no PII-shaped bytes found)` — proving the assertion's execution path genuinely depends on the restored shape being present, rather than passing regardless of fixture content.
- Both mutations reverted in the clone before discarding it; real repository working tree (`git status --short`) empty throughout.

## Summarized sources — verified against the sources themselves (not the spec's paraphrase)

Every named source in `## Summarized sources` was opened and its cited fact matched clause-by-clause against the file:

| Source | Sites checked | Result |
|---|---|---|
| `templates/prompt-blocks/registry.txt` | header scope-limit comment; non-comment row count at branch point (26, board's own `- count:` line, re-derivable); `derived-populations.md` row (`:46`) names 5 role consumers | Matches |
| `bin/check-prompt-sync.sh` | `check_marker()`/`check_contain()` presence; exit 0/1/2 via `die()` and the two explicit `exit` statements | Matches |
| `bin/gen-playbook-blocks.sh:217` | `ROLES="engineer qa-verifier tech-lead pm-spec"` | Matches, byte-exact |
| `templates/prompt-blocks/careful-execution.md` / `derived-populations.md` | marker-mode-with-heading vs. contain-mode-as-sentences shapes | Matches |
| `agents/scrum-master.md:23–51` | nine `- input:` ids, three statuses, per-input notes, PR body/comments never fetched | Matches |
| `agents/scrum-master.md:104–121` | de-identification rule scoped to lessons only, its own authoring-time-discipline-not-detector line | Matches |
| `docs/templates/retro-template.md:72–97`, `:103–151` | Cycle-scope/PRs/Generated-by lines; five section markers each immediately followed by an H2 (re-derived at their *current*, post-edit line numbers 106/112/118/124/153 — a +2/+5 shift from the cited pre-edit numbers is the expected, disclosed consequence of this task's own two added lines, not a flattening) | Matches |
| `bin/check-retro.sh:63–74` | usage line, exit-code table | Matches |
| `.github/workflows/check-handoff.yml` (four cited ranges) | each names the correct two-step pair (retro dogfood ×2, prompt-sync suite+dogfood, pii-shapes suite+diff-scoped, codex-agents suite+dogfood) | Matches |
| `bin/gen-codex-agents.sh:132`, `:262–311`/`:351–374` | `ROLES="tech-lead pm-spec engineer qa-verifier codex-reviewer"`; NUL/control-char/no-trailing-newline refusal logic | Matches |
| `.gitignore:37` | `.codex/agents` | Matches, byte-exact |
| `bin/check-pii-shapes.sh:87–110`, `:482–556`, `:214–218` | `host-local`/`tracker-key` doc comments, DP-1 | Matches |
| `tests/check-pii-shapes/run.sh:5–59`, `:87–133`, `:146–183`, `:916–919` | runtime-fragment-assembly framing comment; `new_repo`/`add_fixture_line`/`add_fixture_lines`/`assert_finding`/`assert_clean`/`assert_positive_reports` all present as named; the `export`/`unset` bracketing idiom | Matches |
| `bin/team-paths.sh:36–41`, `:188` | resolver usage comment; `retros)` case arm | Matches |
| `bin/check-provenance.sh:67–68`, `bin/check-interventions.sh:99–100`, `bin/check-handoff.sh:2`/`:24` | usage lines / default board path | Matches |
| `bin/check-durability.sh:68` | `<task-id>-<slug>.md` spec-filename rule | Matches |

No flattened distinction found in any of the sixteen sources.

## AC14 — item-by-item audit against the engineer's hand-off

AC14 carries no `check:` line; its subject is the engineer's and QA's own procedure. Enumerated items (a)–(f), each read against the hand-off's own text (`.shell-team/todo.md` lines 36–42, the T-1141 engineer hand-off block — confirmed to be exactly that range and no other task's content, since the board interleaves per-task history under `## Active`):

| Item | Hand-off claim | QA finding |
|---|---|---|
| (a) Mutation self-check | Labelled `AC14(a)`; claims every `- check:` line broken/restored/re-broken, and both naming/wiring facets of the two new PII controls probed | **Confirmed** — independently reproduced above in a scratch clone (AC1's guard, and both facets of the new controls) |
| (b) Execution-context matrix | Labelled `AC14(b)`; claims every command re-run with `PATH` stripped and via bare name | **Confirmed** — independently reproduced above for `check-prompt-sync.sh` |
| (c) CI-equivalence | **Not present anywhere in the hand-off** — no `AC14(c)` label, no mention of `shellcheck`, no reverse-mapping of edited paths to workflow steps | **Incomplete.** Independently performed by QA (see table above and spot-checks): `tests/check-pii-shapes/run.sh` is in the workflow's `shellcheck` argument list and is edited by this task — `shellcheck tests/check-pii-shapes/run.sh` exits 0, but the engineer never ran or reported it. This is a genuine gap in the hand-off, not merely a labelling omission (grepped for `shellcheck`, `commit`, `reverse-map` across the full T-1141 board entry: zero matches). |
| (d) Downstream impact | Not labelled `AC14(d)` explicitly, but Notes-for-QA item (4) states the substance: `## Blast radius`'s predictions are the release-time sweep's concern, none independently re-run here | **Confirmed in substance** (this is what the spec's own AC14(d) text requires under this cycle's operating mode — not re-running the sweep per task is correct, not a gap) |
| (e) Plural-corpus read | Labelled `AC14(e)` (twice, in Decisions-recorded and Notes-for-QA item 3); claims the block's own text was re-read against six role files (one copy each) and against a relay describing more than one external run per cycle, no singular determiner found | **Confirmed** — independently re-read: the naming bullet's own explicit clause ("even where the governed thing occurs more than once… no singular phrasing here should be read as capping the count at one") and the `scrum-master.md` relay-boundary paragraph's closing sentence ("This applies to each such relay individually, however many a cycle carries") both hold up under a consumer where records/repositories/relays occur more than once |
| (f) PII-shape self-check before first commit | **Not present anywhere in the hand-off** — no mention of running `check-pii-shapes.sh` against the spec or the new records before their first commit | **Incomplete.** Substantively covered by AC9 (this branch's change set and the whole tree are clean under the shape checker, independently re-confirmed above), but the engineer never reported having performed the pre-commit self-check as its own item, so the *procedure* AC14(f) requires is not shown to have been discharged. |

Items (c) and (f) are absent from the hand-off, not merely thinly described. Per this task's own AC14 wording ("Verification duties discharged and reported, **each as its own named item in the hand-off**"), an item the hand-off never reports is a gap, not something QA fills in on the engineer's behalf.

## Verdict

FAIL — see board hand-off below for the reproduction and required fix.

---

## Round 2 — QA re-verification

Engineer's round-2 fix (commit `121ac67`): reporting-only, board-only. `git diff 9442f29..HEAD --stat` → `.shell-team/todo.md | 21 ++++++++++++++++++++-` — one file. `git status --short` empty throughout this round.

### check-acs (live, round 2)

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1141-record-portability-block.md` → `check-acs: 13 passed, 0 failed, 1 skipped, 0 unrecognized`. AC12/AC13 now PASS (this review file exists). AC1–AC11 unchanged from round 1 (no code/spec/record content changed since then, only this board).

### check-handoff / check-intent (live, round 2)

- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-intent.sh .shell-team/specs/T-1141-record-portability-block.md .shell-team/todo.md` → `aligned: T-1141 v1 (7ba5edea98ee8357a4dfecdf9f9ac9f6b869276a)`.

### AC14 item-by-item audit, round 2

Round 1's mechanical results (fixture-suite counts, prompt-sync/retro/codex-agents/provenance/interventions/durability/board-headings results, the summarized-sources cross-check, the mutation self-checks) stand unchanged and are cited by reference — no code, spec, or record content changed since round 1's commit, so nothing that produced those results could have changed either. Items newly reported this round, (c) and (d)'s label, plus (f), were independently re-verified rather than accepted on the hand-off's own say-so:

| Item | Round-2 hand-off claim | QA finding |
|---|---|---|
| (a) Mutation self-check | Unchanged from round 1 | **Confirmed** (round 1 result stands; no artifact it exercises changed) |
| (b) Execution-context matrix | Unchanged from round 1 | **Confirmed** (round 1 result stands) |
| (c) CI-equivalence | Full reverse-map, one row per edited path, every applicable step run, every inapplicable step named with its reason | **Confirmed, spot-checked independently**: (1) `gen-playbook-blocks.sh` dogfood step replicated exactly (note: this checker's own default shell is zsh, whose unquoted-variable word-splitting differs from bash's — the CI step's `for f in $files` loop needs bash's splitting semantics, so this was re-run under an explicit `bash -c` wrapper to match the workflow's actual execution shell) → `matched=8 total=8`, `record-portability.md`'s row correctly excluded by the `$2 ~ /^playbook-/` filter, exactly as claimed. (2) `grep -cE '^[[:space:]]*codex exec ' agents/codex-reviewer.md` → 3 (the claimed count; a naive unanchored-whitespace grep undercounts to 1 by missing two indented occurrences — the hand-off's own claim uses the suite's real anchor and is correct). (3) `grep -c -- '--alloc --stem' agents/codex-reviewer.md` → 3. (4) `bash tests/machine-tokens/run.sh` → exit 0, `all tokens present in their consuming files`. (5) `bash tests/check-invocation-path/run.sh` → 26/0. (6) `bash tests/codex-skeleton-hygiene/run.sh` → 40/0. (7) `check-interventions` dogfood loop, replicated exactly (`for f in .shell-team/interventions/*.md`, a glob — safe under either shell) → `found=133 fail=0`, matching. (8) `check-board-headings` dogfood, replicated with its exact `--base "origin/${GITHUB_BASE_REF:-develop}"` form → exit 0. (9) `tests/install/run.sh`'s hits on `agents/pm-spec.md` — confirmed every one is `$T1/.claude/agents/pm-spec.md`, a scratch-copied path used for checksum/mtime assertions, never this repo's own file — inapplicable claim holds. (10) `tests/derive-populations/run.sh`'s hit on `tests/check-pii-shapes/run.sh` — confirmed it is a documentation-comment line only ("whose runtime-generation exists solely…") — inapplicable claim holds. |
| (d) Downstream impact | Now explicitly labelled `AC14(d)`; substance unchanged from round 1's Notes-for-QA item 4 | **Confirmed** — same substance QA already accepted round 1, now correctly labelled per the round-1 finding |
| (e) Plural-corpus read | Unchanged from round 1 | **Confirmed** (round 1 result stands) |
| (f) PII-shape self-check before first commit | Reports pre-commit runs before `f0a58c4` and `e300154`, plus a fresh run against the current tree | **Confirmed, independently substantiated**: checked out both historical commits in a scratch clone (`git clone --shared`, real tree untouched) and ran `bin/check-pii-shapes.sh --all` against each commit's own committed tree — both `clean (no PII-shaped bytes found)`. This directly backs the claim that the content committed at each point was already clean, which is the substantive property the pre-commit discipline exists to guarantee, independent of trusting the hand-off's narrative of when the command was run. |

All six items now confirmed. No gap remains.

### PII-shape self-check on this round's own edit

`bash bin/check-pii-shapes.sh --all` → `clean (no PII-shaped bytes found)`, run before staging/committing this round's board edit.

### Verdict

PASS.

## Codex Review — T-1141

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1141
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 15 files, +573/-1 lines

#### Blockers
- none

#### Major
- **A committed positive-control fixture line carries a real `tracker-key` shape as a bare literal, breaking this exact suite's own no-real-shape invariant, in three tracked files.** `tests/check-pii-shapes/run.sh:1701` assigns the new positive-control variable as a single, bare, committed string — not assembled at runtime from fragments, unlike the pre-existing `TK_KEY_POS_LINE` convention a few hundred lines earlier in the same file, which fragments its namespace and its digit run into separate variables before interpolating them. This same task's own provenance decision (`.shell-team/provenance/T-1141.md:15`) names that exact pre-existing fragmented convention as the model it says it followed, but the new line does not actually follow it — it reproduces the finished shape directly. Independently reproduced in a scratch clone of this branch: `PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all` reports `pattern=tracker-key` findings at `tests/check-pii-shapes/run.sh` line 1701, at `.shell-team/provenance/T-1141.md` line 15, and at `.shell-team/reviews/T-1141.md` line 38 — the fixture's own committed text is reproduced verbatim in both of those record files. The `.shell-team/reviews/T-1141.md` occurrence is a review record quoting the flagged bytes of a pattern-based-checker finding verbatim, which is exactly what the pre-existing rule in `agents/qa-verifier.md`/`agents/tech-lead.md` (2026-09-12 lesson) forbids, in a file the same checker itself scans. This breaks `tests/check-pii-shapes/run.sh`'s own header-documented invariant ("No PII-shaped byte ever enters this tree ... every fixture that carries a REAL shape ... is assembled AT RUNTIME from short fragments"). The frozen **AC9** still passes exactly as written (the tracker-key opt-in is off by default, confirmed clean by direct re-run), so no frozen criterion reds — but this is a genuine, avoidable violation of the design invariant this exact suite depends on, introduced by this task's own new content, not a pre-existing condition. Fix: split the new positive-control literal into fragments the way `TK_KEY_POS_LINE` already does, and rewrite the two record mentions to describe the mutation by location and pattern id rather than quoting the matched text.

#### Minor / nits
- **QA's own audit claim is factually inaccurate.** `.shell-team/reviews/T-1141.md`'s "Independent spot-checks" section states that the informational tracker-key `--all` run's findings, cross-referenced against this task's changed-file set, fall nowhere inside this task's own new prose. That is incorrect for the three sites named in the Major above, all of which are inside this task's own changed-file set (`tests/check-pii-shapes/run.sh`, `.shell-team/provenance/T-1141.md`, `.shell-team/reviews/T-1141.md` itself). Worth a correction to the record; not a new gate.
- **AC13's board-path resolution is fail-open, not fail-closed.** `.shell-team/specs/T-1141-record-portability-block.md:98`'s check line resolves the board path as `bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"` with no status check on the substitution; if the resolver fails, the empty argument makes `check-handoff.sh` (`FILE="${1:-tasks/todo.md}"`) silently fall back to its own hard-coded legacy default, so a resolver failure in a legacy-layout checkout carrying a real `tasks/todo.md` could pass silently rather than fail closed — contrary to this repo's own `CLAUDE.md` rules ("Checkers must fail closed", "Resolve operating paths through `bin/team-paths.sh`; never hardcode them"). **Confirmed pre-existing, not introduced by this task**: the identical unchecked form already exists verbatim in `.shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md` and several other merged specs (grepped: `T-1111`, `T-1114`, `T-1115`, `T-1116`, `T-112`, `T-113`, `T-1136`, `T-1137` all carry the same shape) — systemic boilerplate this task inherited rather than introduced.
- **AC13 hardcodes `.shell-team/provenance|interventions|reviews` paths** instead of resolving them via `bin/team-paths.sh`, contrary to the same repeated project rule and contrary to this exact task's own stated design premise (reachable input class #7: "Both supported layouts, every operating path resolved through `bin/team-paths.sh` rather than hardcoded"). Same pre-existing-boilerplate caveat as the row above — confirmed shared by the same set of prior specs, not unique to this task.
- **A prior task's own board heading was silently deleted.** Commit `e300154` (this task's own engineer hand-off) replaced the pre-existing line `### Engineer hand-off — T-1140 (engineer, mode A2 — 5-line form)` with `### Engineer hand-off — T-1141 (engineer, mode A2 — 5-line form)`, instead of inserting the new heading before the unchanged old one. T-1140's own round-1 hand-off body (its Files-changed / Tests-added / ... bullets) is still fully present and unedited, but now sits under no heading of its own — confirmed: `grep -n "Engineer hand-off — T-1140" .shell-team/todo.md` at HEAD returns only the round-2 heading, while `git show 6f606ff:.shell-team/todo.md` shows both headings present at the branch point. `bin/check-board-headings.sh` does not catch this (both the engineer's and QA's own re-runs of it reported exit 0 in both rounds). No content is lost, only the heading line; cheap to repair — worth folding into the same rework round as the Major above.

#### Adversarial findings
Re-ran with a devil's-advocate framing on the affected files. It reasoned the tracker-key regex match through directly for all three sites (its own read-only sandbox blocked live execution; independently confirmed by direct execution above). It confirmed the two AC13 path-resolution gaps are real but pre-existing/widespread across many older merged specs, not unique to this task. It found no additional Blocker/Major beyond what the primary pass already surfaced: the six consumer regions are byte-identical, the retro-template's five markers and ledger lines are untouched, and the new fixture's negative control carries no real shape of its own.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC13's board-path resolved via an unchecked substitution that fails open to `check-handoff.sh`'s own legacy default | major / major | minor | independently confirmed the identical unchecked form is pre-existing, verbatim, in T-1140 and several other merged specs — systemic boilerplate this task inherited, not introduced; does not invalidate this task's own deliverable |
| 2 | AC13 hardcodes provenance/interventions/reviews paths instead of resolving them via `bin/team-paths.sh` | major / major | minor | same reasoning as row 1 — confirmed pre-existing across the same set of prior specs |

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings (the Major on the committed tracker-key-shaped literal across three files; the deleted board heading can be folded into the same round).

#### Fast-follow disposition
- AC13's fail-open board-path resolution and hardcoded record paths (both Minor, both pre-existing repo-wide boilerplate) — file-an-issue: worth fixing at the shared convention/template level (so every spec carrying this boilerplate is fixed at once) rather than patching this one task's frozen criterion; low incremental risk in this repo's own current layout.
- QA's inaccurate "no overlap" audit claim — won't-fix as a separately tracked item: naturally superseded once the Major above is fixed and this review's correction is read.
- The deleted `### Engineer hand-off — T-1140` heading — won't-fix as a separately tracked item: recommended to fold into the same rework round as the Major above (a one-line repair) rather than tracked independently.

- executor-invocation (codex-primary): codex exec --sandbox read-only --cd . review --base feature/525-pii-shapes-tracker-key-host-local --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1141-codex-primary.out.VqCbcj
- pass-role (codex-primary): generation
- briefing-fidelity (codex-primary): not-applicable — the `review` subcommand is a fixed built-in review flow with no prompt-injection channel; this round's specific briefing (verdict scope, the Tier-2 calibration for the spec's own inline check lines) was not and could not be carried into its context, though its own default scope (correctness against the spec, edge cases, design smells) covers overlapping ground and is what surfaced the finding above.
- raw-capture (codex-primary): T-1141-codex-primary
- self-detected-host (codex-primary): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

- executor-invocation (codex-adversarial): codex exec --sandbox read-only --cd . --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1141-codex-adversarial.out.3CIILi "Review the diff between feature/525-pii-shapes-tracker-key-host-local and HEAD in this repo (T-1141: a new canonical prompt block for record portability, registered in six role files, plus a retro-template external-run form, plus a new fixture pair in tests/check-pii-shapes/run.sh). Read the files yourself with git diff / nl -ba — do not expect any diff in this prompt. A prior pass found: (1) the new positive-control fixture line in tests/check-pii-shapes/run.sh is a bare committed literal carrying a real tracker-key shape landing in the tracked source file, violating this suite's own header-documented invariant that every real-shape fixture must be assembled AT RUNTIME from fragments (see the pre-existing TK_KEY_POS_LINE convention a few hundred lines earlier in the same file, which fragments its namespace/number into separate variables); the same text is also reproduced verbatim in .shell-team/provenance/T-1141.md and .shell-team/reviews/T-1141.md, the latter of which is a report from a pattern-based PII/shape checker and should never quote the flagged bytes verbatim per an existing project rule in agents/qa-verifier.md and agents/tech-lead.md (2026-09-12 lesson) — read the fixture's own source line yourself rather than expecting it quoted here. (2) the spec's AC13 check line resolves the board path via an unchecked $(bash bin/team-paths.sh --get todo) substitution whose failure silently falls back to check-handoff.sh's own hardcoded tasks/todo.md default, and separately hardcodes .shell-team/provenance|interventions|reviews paths instead of resolving them via bin/team-paths.sh, contrary to this repo's own CLAUDE.md rule to never hardcode operating paths. Play devil's advocate: (a) verify finding (1) by actually running, if you can, PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all (or reasoning precisely through the regex against the fixture line you read yourself) to confirm these exact three sites would report as findings, without quoting the matched text yourself in your own response either; check whether this specific pattern (hardcoded paths in an inline spec check: line, or the board-path fallback) is a pre-existing convention already used by many merged specs (grep other .shell-team/specs/*.md for the identical check: line shape) rather than something unique to T-1141, and say so if it is; (b) look for any OTHER new Blocker/Major-severity defect in the six role-file prompt-block insertions, templates/prompt-blocks/registry.txt, docs/templates/retro-template.md, or the new fixture pair, that the prior pass may have missed — especially any case where the six consumer regions are not actually byte-identical, or where the new retro-template lines could break bin/check-retro.sh's own five-marker/ledger contract, or where the new fixture's negative control could accidentally still carry a real shape. Return findings as a JSON array of {severity, file, line, issue, suggestion}, describing any matched text by location and pattern id only, never quoting it. [note: this pass's real invocation named the fixture's own flagged text directly in the "(1)" clause above; this record paraphrases that one clause by location and pattern id rather than quoting it again, per the same no-verbatim-quoting rule this pass itself was asked to follow — every flag, argument and their order otherwise verbatim]"
- pass-role (codex-adversarial): generation
- briefing-fidelity (codex-adversarial): carried — the round's specific mandate (the primary pass's own three findings, described by location and pattern id, plus a request to confirm systemic-vs-novel status and to look for any other missed Blocker/Major) was written directly into this pass's prompt.
- raw-capture (codex-adversarial): T-1141-codex-adversarial
- self-detected-host (codex-adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass
