### Codex Review verdict: REQUEST_CHANGES
- Task: T-1052
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 8 files changed, +431/-4 lines (a804703...HEAD)

#### Blockers
- (none)

#### Major
- `agents/engineer.md:54` (E171) and `agents/qa-verifier.md:67` (Q171) — the shipped #171 norm's frozen text reads "is the **only** criterion no command verifies" / "audit a runtime acceptance criterion" in language that reads as singular, but this repository already has specs with **more than one** no-`check:` criterion in the same spec — measured live: `bash bin/check-acs.sh .shell-team/specs/T-1026-skill-md-doc-completeness.md` reports **AC9: SKIP** and **AC10: SKIP**, two independent runtime criteria in one spec. Both the primary and adversarial Codex passes independently reached CONFIRMED-REAL on this: a literal reader could discharge/audit only the first SKIP found and consider the norm satisfied, silently missing the second — the exact "unenumerated whole-set claim ships incomplete and reads complete" failure class this whole task exists to close, now living inside the norm meant to prevent it. Suggested fix: redraft the trigger clause to an explicit universal ("for each criterion carrying no `- check:` sub-bullet…", not "the one"/"the only"), and require the hand-off/verdict to enumerate every SKIP criterion in the spec, not just one. This sits inside T-1052's own frozen intent block (`E171`/`Q171`), so the fix is a v2→v3 class-B re-freeze through pm-spec plus a corresponding one-line edit to each of the two already-shipped agent files — not a hand-edit to the merged prose.
- Secondary design note on the same surface, worth folding into the same redraft: `Q171`'s text never explicitly restates `E171`'s own fallback branch ("where the criterion enumerates no items, walk each distinct assertion its prose demands instead") — it only says "read the criterion's own enumeration first." The spec's `## Norm symmetry table` (C3 row) asserts this is intentional ("a second copy of the fallback would be the copy that goes stale"), but a *cross-reference* to the fallback is not the same as *no mention of it at all*; a QA reader following `Q171` literally has no textual instruction for what to audit against when a criterion enumerates nothing. Not independently confirmed against a live incident in this corpus (this task's own AC16 does enumerate items a–g), so it rides with the Major above rather than as its own line item — but the same redraft pass should close it.

#### Minor / nits
- `.shell-team/specs/T-1052-records-editorial.md:283` (`## Pinner inventory`, the T-1029 row) — self-contradictory: the row's leading per-criterion list states "AC5 PASS" at both base and HEAD, while the same row's parenthetical note states the FAILED set is `{AC5, AC7, AC8}` — i.e. AC5 is asserted PASS and FAIL in the same sentence. Measured live (`bash bin/check-acs.sh .shell-team/specs/T-1029-claim-fidelity-qa-step.md`): **AC5: FAIL**, confirming the parenthetical and refuting the leading list. Both Codex passes confirmed this independently; the adversarial pass downgraded it to Minor and I agree — it sits in an ordinary (non-frozen) fill-in section, moves no `- check:` result, and is isolated to one of 14 rows, but it is a plain factual error the task's own #190a-adjacent accuracy goal exists to prevent. Fix: correct "AC5 PASS" to "AC5 FAIL" in that row; no re-freeze needed (outside the intent block).
- `.shell-team/todo.md:21` (T-1052's board entry, pm-spec's pre-implementation freeze note) — states "T-1050's AC15… and AC14 go red by construction, as do T-1050's AC11/AC16 and T-1051's AC14/AC16 — all labelled merge-point-scoped." The spec's own, later-measured `## Blast radius` table (and the QA audit that re-confirmed it) shows T-1050's **AC11 and AC16 measured green both at base and HEAD** — the opposite of "go red." This is a superseded pre-implementation prediction left standing beside a corrected measurement, with no cross-reference tying the two together on the board itself; a reader who only sees line 21 is misled about this task's actual blast radius. Per this repo's own convention the historical line is not rewritten — the fix is a short correcting note appended near the Blast radius discussion (as T-1034's L644 divergence was handled), not an edit to line 21 itself.

#### Adversarial findings
- Re-run against the same three files/sections with a "what could break this" framing: all three primary findings were independently reproduced as CONFIRMED-REAL (T-1026's two SKIPs measured live; T-1029's AC5 measured FAIL at both refs; T-1050's AC11/AC16 measured green both at base and HEAD, contradicting the board line). The adversarial pass's own severity read differed from the primary pass on findings 2 and 3 (MINOR vs. the primary's P2) — see the synthesis ledger below for how that was resolved.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | E171/Q171 singular "the one"/"the only" framing risks under-auditing a spec with 2+ no-`check:` criteria | primary: P2 (major); adversarial: MAJOR | major | confirmed real via live measurement (T-1026 AC9+AC10 both SKIP); no downgrade — this is the higher-scrutiny surface (a shipped, frozen agent-prompt insertion) and the defect is the exact failure class the whole task exists to close |
| 2 | T-1029 Pinner-inventory row states "AC5 PASS" while its own FAILED-set note lists AC5 | primary: P2 (major); adversarial: MINOR | minor | confirmed factually wrong (AC5 measured FAIL both refs) but sits in ordinary non-frozen fill-in prose, affects no `- check:` result, isolated to 1 of 14 rows — adopted the adversarial pass's lower-blast-radius read |
| 3 | Board line 21 claims T-1050 AC11/AC16 "go red by construction," contradicted by this task's own later-measured Blast radius table (green both sides) | primary: P2 (major); adversarial: MINOR | minor | confirmed contradiction, but line 21 is a superseded pre-implementation prediction in ordinary board narrative, corrected elsewhere in the same task's own records and by QA's audit; historical-record convention (never rewrite) means the remedy is a fresh note, not a rewrite — adopted the adversarial pass's lower-blast-radius read |

#### Recommendation
- REQUEST_CHANGES → set back to `READY_FOR_ENG` with linked findings. The Major requires a v2→v3 class-B re-freeze of `T-1052`'s own `E171`/`Q171` blocks (routed through pm-spec) plus the matching one-line edit to each of `agents/engineer.md` and `agents/qa-verifier.md`; the two Minors are ordinary, non-frozen text corrections with no re-freeze needed and can land in the same round.

#### Additional review notes (outside the Blocker/Major/Minor structure, per the task's specific review asks)
- **(i) T-1034 re-freeze class-completeness and DP4 soundness**: confirmed sound. `git diff a804703...HEAD -- .shell-team/specs/T-1034-refreeze-hardening-execbit.md` shows exactly `L5`/`L15` appended verbatim to AC5/AC15, byte-matching the spec's own frozen fences; AC11 carries zero occurrences of the `MPS` phrase (DP4 holds); no `- check:` line, criterion count or `- stale-at:` count moved. DP4's third ground — that T-1044's AC13 asserts `AC11: PASS` inside an exact partition — is independently verified: `.shell-team/specs/T-1044-test-infra-bundle.md:162`'s AC13 lists `AC11` among the PASS entries. No unlabelled same-class sibling survives; L5/L15 read as honest disclosures of a decaying quantity, not as weakenings (the underlying `- check:` line is untouched).
- **(ii) #171's two texts**: see the Major above — the norm is *mostly* complementary (C1/C2/C4/C5/C6 mirror cleanly, confirmed by direct reading) but the C3 fallback and the singular/plural framing are the two places the mirroring breaks down, one severe enough to be a Major.
- **(iii) #176 adjudication**: confirmed sound. `agents/scrum-master.md:78/109-110/113-114` do carry the task-ID-matchability and attacker-controlled-input disclosures exactly as `RR176` claims (verified by direct read); the `- **Cite or remove.**` bullet is immediately followed by the cycle-window bullet, matching T-1027 AC8's adjacency pin; `agents/scrum-master.md` is confirmed byte-identical to `a804703` (`git diff`/`git ls-files --others` both empty). The re-evaluation trigger is concrete (a mis-labelled retro window, or a merge workflow that starts writing task IDs into merge subjects). Rejected side (the behaviour-changing tightening) is recorded with reasons.
- **(iv) AC14's repaired `sp=` line**: confirmed self-consistent post-repair (`git show df80222` — the exclusion list now names all three specs, matching the prose). Swept this spec's other `grep -vxF` exclusion lists (AC6's attestation-survival grep, AC14's own `extra=`/`agents/` clauses) for the same self-reference-gap shape: none found; the `extra=` sub-clause already allow-listed T-1052's own spec from v1. The sweep claim in provenance ("exactly one carried the gap") holds.
- **(v) QA's disclosed reduced-depth items**: judged honest and proportionate, not a finding. 7 of 8 mutation probes and 61 of 63 CI steps were not independently re-run, but QA (a) named this explicitly in its own "Risk notes for the reviewer" rather than omitting it, (b) reproduced the one mutation the hand-off itself flagged as most ambiguous (mutation 5's two-observer split), (c) independently confirmed the step-count (63) and the shellcheck-pin claim, and (d) re-ran the subset of the 63 CI steps that actually overlap this task's own deliverables. This is disclosed reduced-depth verification, not a hidden gap.
- **Issue-canon check, independently verified rather than left environmentally-unverified**: this sandbox had outbound access to `api.github.com`. Fetched #171, #176 and #190 live: all three confirmed `open`, and their titles/bodies match this spec's description exactly, including the disclosed discrepancy — `agents/pm-spec.md:79`'s "#171" citation is genuinely a different matter (issue #171's real title is "Runtime (SKIP) acceptance criteria: the board hand-off states completion claims where the spec asked for per-item decision material," not the full-width-glyph escalation example `agents/pm-spec.md:79` cites). This matches both QA's and the engineer/freeze-gate's own measurement.

#### Fast-follow disposition
- `agents/pm-spec.md:79`'s stale "#171" cross-reference (named in the task prompt as a candidate) — **won't-fix in this train**: the file is locked byte-identical by T-1051's merged AC12, so fixing it here would redden a merged criterion for a documentation-only gain; independently confirmed live (issue #171's real subject is the runtime-criterion reporting norm, not the full-width-glyph escalation example the citation actually illustrates) — file a small follow-up issue for a future task with `agents/pm-spec.md` in scope to repoint the citation or drop the issue-number reference.
- no other fast-follow deferrals — the three findings above (one Major, two Minor) are recommended for the same rework round rather than deferred, since none needs more than a small, targeted edit.
