# T-1058 — Codex cross-provider review

## Round 1

### Codex Review verdict: APPROVE
- Task: T-1058
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: 15 files, +772/-20 lines (`feature/1057-loop-integration..feature/1058-telemetry` — this task's own seven commits `c86015e`→`70a0d67`, stacked six deep)

**Pre-commitment status (mandatory disclosure).** This is round 1. Never-dropped: ①telemetry stays best-effort (never stops the loop) ②two-generation fail-closed (no fabricated cross-provider verdicts) ③resolver write-path zero. No Blocker or Major found this round (by this reviewer's own extensive static and live verification, or by Codex's primary pass), so the factual trigger (two consecutive rounds) cannot fire and does not. The QA round-1 FAIL is correctly treated as history rather than as a finding against this component — it was a frozen-prose defect in the spec's own Non-goals/DP7 text (asserting `bin/rollup-track.sh`'s behavior without that file ever having been opened), not an implementation defect, and the class-B re-freeze v1→v2 that repaired it is itself part of what this round verifies.

**The v1→v2 re-freeze's own records are sound and independently re-verified, not merely read.** Diffing the raw spec blobs at `c86015e` (v1) and `eaaa1b7` (v2) confirms the ratification's own clause-level accounting exactly: only Goal item (5), DP7, the `rollup-track.sh` Non-goals bullet, AC9's allow-list, the new AC13, and the Input space reachable-class list changed inside the intent block; every other AC (AC1–AC8, AC10, AC11, AC12) is byte-identical between the two versions, confirmed directly rather than trusted. `bin/check-intent.sh` reports `aligned` at the unchanged-since-v2 hash `e82f98d50d70481a8e4d5e0967ee4214116c7796`. The Route (1)-over-Route (2)/(3) rejection reasoning (accept the capture and bound its content, rather than filter at the wrapper or extend the wrapper's own preamble) is sound on its own terms: a Route (2) filter would have to be reverted a second time inside `bin/rollup-track.sh` the moment droppable (1) fires, which is exactly the "dirtier disposition under pressure" the ratification names as the deciding reason, and the drop-order text in the current `## Pre-commitment` confirms AC13 is named for whole deletion alongside AC4/AC5's derivation clauses, consistent with that reasoning. The tripwire rule the root-cause entry states — "a frozen clause asserting what a file does requires that file to appear in `## Summarized sources`" — is a genuinely narrower and more actionable lesson than "join up your own summaries", and the board's own record-disposition line correctly self-corrects an earlier misattribution (the file was never described anywhere in v1, not "described but not joined") rather than leaving the softer, incorrect version standing.

**Every board-claimed count independently reproduced, not merely read.** `bash bin/check-acs.sh` — 12/12 PASS (AC12 SKIP by design). `shellcheck bin/log-run.sh bin/check-run.sh bin/rollup-runs.sh` — clean. Suite counts match exactly: `tests/log-run/run.sh` 18, `tests/check-run/run.sh` 50, `tests/rollup-runs/run.sh` 27. The full regression set (`rollup-track`, `is-span-row-parity`, `cluster-failures`, `gen-loop-replay`, `machine-tokens`, `errexit-safe`, `check-prompt-sync` + its suite) all exit 0. The disclosed blast-radius reddenings are exactly as predicted and no more: `bash bin/check-acs.sh .shell-team/specs/T-1011-telemetry-event-rows.md` shows **AC6 FAIL** (the span-key-sequence lock, unavoidable) while AC10/AC16/AC19/AC22/AC25/AC30/AC32 all PASS as predicted; `.shell-team/specs/T-1057-loop-integration.md`'s AC16 and AC18 both FAIL as predicted. The working tree is clean and no stray `.shell-team/rollups/` artifact remains from QA's own disclosed verification runs (confirmed directly — QA's note is accurate and already resolved, not merely trusted).

#### Blockers

None.

#### Major

None.

#### Minor / nits

Static/formulaic verification against every item in this round's lens, each independently reproduced or reasoned through rather than assumed:

- **Two reviewer spans back-to-back with no authoring span between them** — reproduced live (`engineer/claude` → `codex-reviewer/codex` → `codex-reviewer/claude`, one run): both reviewer spans correctly compare against the *same* nearest-preceding authoring span (`engineer/claude`), yielding `codex-reviewer#2=cross-provider codex-reviewer#3=same-provider`. This matches DP6's "nearest preceding authoring span in input order" literally — nothing requires each reviewer to have a distinct preceding author, and the implementation does not assume one. Not a defect.
- **A reviewer span as the first row, duplicate `seq` values, malformed provider values reaching the printf** — all three are within the spec's own declared boundaries: the first is AC4's own `f5` case (confirmed `undetermined` via `last_author_seen=0`); duplicate/non-monotonic `seq` is explicitly `out-of-input-space` (Input space: "a statement about the writer's caller rather than about this deliverable"); and a malformed provider value could in principle defeat AC13's own strict-shape regex on the tracked artifact, but reaching the log at all requires bypassing `bin/log-run.sh`'s own `BINDING_TOKEN_RE` validation — the same "validate once at the writer, trust at every reader" boundary `status`, `verdict`, `phase` and every other span field already rely on, and `bin/rollup-track.sh`'s own PII/secret guard (unchanged, AC7-locked) remains the backstop regardless. Recorded as a fast-follow observation below, not a finding against this task, since it is not a new gap this task introduces — it is the pre-existing trust boundary DP12 states explicitly.
- **AC13's regex pair** — confirmed the loose prefix is a genuine superset of the strict shape (both anchored on the identical `^  providers: ` / `^  review: ` prefix, with the strict pattern additionally `$`-anchored and character-class-restricted), and confirmed a strictly-matching line cannot carry a hostile payload: the token class `[a-z][a-z0-9-]*` structurally excludes every shape AC13's own preamble worries about (no `@`, no `/`, no uppercase, so no email-like, path-like or secret-prefix-like value can satisfy it). Reproduced the mutation-probe technique myself independently of the shipped fixture and QA's own account: appending a free-text suffix to `rollup-runs.sh`'s `providers:` printf and running the mutated pair through `rollup-track.sh` for real produces a tracked artifact where the loose-prefix count (1) and strict-shape count (0) disagree — confirming the criterion genuinely bites.
- **The rewritten T-1011 AC30 test** — audited for equivalence, not just presence. The old form (`with-events.jsonl` vs a separately-fixtured `clean.jsonl`) only worked because `rollup-runs.sh` never printed `seq`; the two fixtures' span rows carry different `seq` sequences (1/3/5/7 vs 1/2/3/4), confirmed by direct read. The new form (`with-events.jsonl` vs itself with event rows grepped out) is a more direct statement of AC30's own literal property and loses no coverage: `clean.jsonl` remains heavily used elsewhere in the same suite (7 other assertions), so nothing is orphaned, and no code path exists in `rollup-runs.sh` that would behave differently based on whether its input was authored standalone versus derived from another file by `grep`.
- **The `SPAN_ONLY_FLAGS`/`SPAN_ONLY_KEYS` two-spelling lock** — both arrays confirmed single-line (`^SPAN_ONLY_FLAGS=(...)$` / `^SPAN_ONLY_KEYS=(...)$`), matching AC2's own extraction mechanism exactly; AC2 measured PASS live.
- **T-1011 AC6's disclosed reddening** — confirmed genuinely disclosed rather than quietly repaired: `bash bin/check-acs.sh .shell-team/specs/T-1011-telemetry-event-rows.md` shows AC6 red on this branch, and the spec's own `## Blast radius` names it as `predicted-red`, unavoidable, and explicitly not to be fixed (Non-goals: "T-1011's AC6 is the pre-decided, unavoidable one").
- **Backward-compat parsing: `field_str`'s boundary anchors vs. values containing `","`** — the extraction regex `[\{,]\"$key\":\"([^\"]*)\"` requires the literal key name immediately after a structural `{`/`,` boundary, and `jesc` (the writer's own escaper) always escapes an embedded `"` before a value reaches the log, so a free-text value's own commas or key-shaped substrings can never satisfy the boundary-anchored match — confirmed by AC4's own `f9` case (an `error` value containing literal `,"provider":"codex"` text, correctly not read as a real `provider` key) and re-verified by direct reading of `jesc`/`field_str` together.
- **DP6's role-pair definition against multi-round histories** — re-verified against this repository's own real, gitignored `.shell-team/runs/shell-team.jsonl` (1013 lines, all pre-task): every one of 52 run blocks correctly prints `providers: (none)`, and every reviewer token in the 49 runs carrying one reads `undetermined`, never `same-provider`/`cross-provider` — confirming the two-generation fail-closed guarantee holds against this repository's own dogfooded history, not only against synthetic fixtures.

#### Adversarial findings

Not run this round: no Blocker/Major surfaced by the primary pass (Codex: "No actionable correctness defects were found... internally consistent") or by this reviewer's own extensive static and live verification, which is the sole trigger for the adversarial pass per this role's default workflow.

#### Synthesis audit ledger

no severity changes.

#### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1058 entry to `READY_FOR_MERGE`. No findings block merge; the fast-follow items below are deferred by explicit disposition, not silently dropped.

#### Fast-follow disposition

- A malformed `provider`/`adapter`/`effort` value bypassing `bin/log-run.sh`'s writer-side validation (via a future/alternate/non-conforming writer, never via the current shipped writer) could in principle defeat AC13's strict-shape regex on the tracked artifact — **won't-fix**. This is the pre-existing, explicitly-stated "validate once at the writer, trust at every reader" trust boundary every other span field already relies on (DP12's threat model), not a gap this task introduces; `bin/rollup-track.sh`'s own PII/secret guard remains the backstop regardless of what defeats the shape regex.
- QA's disclosed procedural note (twice created stray `.shell-team/rollups/` files during verification, cleaned) — **won't-fix**, already resolved. Independently confirmed this round: working tree clean, no `.shell-team/rollups/` directory present. Recorded here per this round's instruction to dispose of every carried-forward item explicitly rather than let it silently vanish.
- The re-scoped fast-follow this spec itself names (extending `bin/rollup-track.sh`'s own written preamble to list the two new fields, deferred at the v1→v2 re-freeze under Non-goals (b)) — **file-an-issue**, carrying the disposition the spec's own `## Notes for engineer` already states: the incompleteness is deliberate and the load-bearing "PII-free by construction" sentence stays true regardless, so this is a documentation completeness item, not a correctness gap.
- The pre-committed droppable-(1) successor issue (the derived `providers:`/`review:` output and DP6's full role-pair definition, to be filed only if a future round's findings trigger the drop) — **not applicable this round**: the trigger did not fire, so no issue is filed. Recorded here so a later reader does not need to re-derive whether round 1 should have triggered it.

