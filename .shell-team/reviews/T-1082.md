### Codex Review verdict: APPROVE
- Task: T-1082
- Codex model: gpt-5-codex (via codex-cli)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 13 files, +1488/-3 lines (base `91c52fe0b114d6432925dfd5d964bbd24ff4e670` = predecessor `feature/1081-freeze-sweep-hardening` の tip、`git diff --stat "$B"...HEAD`)

**検証範囲について。** spec の `## Verdict scope` を踏襲し、consumer class は adopter-executed `bin/` code（`bin/check-fanout-instances.sh` 本体）に置いている。本体スクリプトについては、Codex primary pass による diff・全ファイル読解に加え、自分自身でも実プロデューサ（`bin/aggregate-verdicts.sh` / `bin/log-run.sh`）から組み立てた fixture で `check-fanout-instances.sh` を直接実行し、ok ケースが仕様どおり `check-fanout-instances: ok: ...` を一行だけ stdout に出し exit 0 になることを一次確認した。四プロパティ（presence/grammar/uniqueness/consistency）・11 クラスの check order・boundary-anchored なキー選択・`LC_ALL=C` pin・em dash セパレータ処理・`--iteration`/`--attempt` の narrower・QA round 1/2 で発見・修正された non-regular-file ガード（ディレクトリ/FIFO/`/dev/null`/symlink）について、スクリプト本文と QA の 2 ラウンド分の記録を読み込んだ上で、独自の formulaic/boundary 観点の再検討でも追加の欠陥は見つからなかった。

#### Blockers
- None.

#### Major

- `skills/run/SKILL.md:168`（`templates/prompt-blocks/fanout-orchestration.md:9` にミラー）— 新しい検証ブレットは `bin/check-fanout-instances.sh --telemetry <telemetry-file> ... --aggregation <aggregation-file> ...` という文言で `--aggregation <aggregation-file>` を要求するが（チェッカー自身の AC7 は存在しないパスを exit 2 `usage` で拒否する）、直前の文（T-1074 由来・本タスクの diff 対象外）は `bin/aggregate-verdicts.sh --label <label> ... [--part ...]` の標準出力をどこにも保存せよと書いていない。この手順に文字どおり従うオーケストレータにとって、`<aggregation-file>` がどこから来るのか本文だけでは自明ではない。Codex adversarial pass 自身の発見（P1 相当）。ただし AC10 のチェック体は `'bin/check-fanout-instances.sh'` / `'--telemetry'` / `'--aggregation'` / `'does not advance the phase'` という部分文字列の存在のみを検証しており、この語りの完全性までは検査しない裁量領域にある。有能な LLM オーケストレータであれば `> <file>` へのリダイレクトを自明に補う可能性が高く、チェッカー本体の正しさには一切影響しないため、Blocker には格上げしない。Suggested fix: 新ブレットまたはその直前の文に「`aggregate-verdicts.sh` の標準出力を `<aggregation-file>` として保存する」旨を一文足す。

- `skills/run/SKILL.md:168` / `templates/prompt-blocks/fanout-orchestration.md:9` — 新ブレットは `bin/check-fanout-instances.sh` という repo-relative パスを literal に含む。`CLAUDE.md` の Dogfood 節は「plugin がロードされている状態では `bin/` は PATH に乗るので、裸の名前（`check-handoff.sh …`）で呼ぶ」ことを求めており、adopter がこの plugin をロードした自分のリポジトリでこの相対パスをそのまま実行すると、cwd に `bin/` サブディレクトリが存在しない限り解決に失敗する。ただし本タスク固有の新規の欠陥ではない: (1) この文字列は本タスク自身の凍結 **AC10** が literal に要求しており（`grep -qF -- 'bin/check-fanout-instances.sh' "$P"`）、エンジニアに "bin/" を外す裁量がなかった。(2) 直前の既存文（T-1074 が出荷し、本タスクの diff 対象外）もすでに同一の `bin/aggregate-verdicts.sh` という形を使っており、本ファイル自体、`bin/log-run.sh` と `log-run.sh`、`bin/land-worktree.sh` と `land-worktree.sh` のように bare 形式と `bin/` 付き形式が随所で混在している既存の一貫性問題の一部にすぎない。Codex primary pass は当初 P1 として報告したが、adversarial pass 自身の再検討で「本タスク固有のブロッカーではない」と評価を下げた（下記 ledger 参照）。

#### Minor / nits

- `skills/run/SKILL.md:168` / `templates/prompt-blocks/fanout-orchestration.md:9` — プレースホルダ命名が本文書の既存慣例と食い違う: 他の箇所（例: `log-run.sh shell-team --run-id <run_id> ...`）は一貫して `<run_id>`（アンダースコア）を使うのに対し、新ブレットは `--run-id <run-id>`（ハイフン）と書く。`<telemetry-file>` という語もこの文書の他所で定義されておらず、実際のテレメトリファイルは `<runs>/<loop_id>.jsonl` という形である。動作には影響しない表記ゆれ。
- `skills/run/SKILL.md:168` / `templates/prompt-blocks/fanout-orchestration.md:9` — `[--iteration <n>] [--attempt <n>]` の角括弧記法自体は妥当だが、同一フェーズが直列ラウンドとファンアウトラウンドを混在させている場合にこれらを省略すると `missing-instance` で refuse される、という運用上の注意がブレットの散文には書かれていない。ただしチェッカー自身の `--help` 出力と本タスクの `## Input space` にはすでに明記されており、fail-closed な拒否メッセージ自体がリカバリの道筋（`--iteration`/`--attempt` で絞り込め）を示すため実害は小さい。

#### Adversarial findings

`codex exec` の devil's-advocate パスを `skills/run/SKILL.md`（Within-phase fan-out 節）と `templates/prompt-blocks/fanout-orchestration.md` の新ブレットに対して実行（raw capture: `T-1082-codex-adversarial.{txt,jsonl}`）。primary pass が P1 とした `bin/` プレフィックス問題を、(a) 直前の T-1074 由来のブレットにすでに同一の表記が出荷・承認済みであること、(b) 本タスク自身の凍結 AC10 がその文字列を literal に要求していること、の 2 点を踏まえて P2 相当に自己修正し、代わりに `--aggregation <aggregation-file>` の実体がどこから来るか本文中で説明されていないという、より本タスク固有性の高い新規の欠陥（P1 相当）を発見した。プレースホルダの命名不整合と `--iteration`/`--attempt` 運用注意の欠落も追加で指摘し、いずれも上記 Minor に反映した。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `bin/check-fanout-instances.sh` という repo-relative パス表記（PATH 解決前提の bare 形式ではない） | primary: P1（blocker 相当）/ adversarial: P2（major 相当） | major | 本タスク自身の凍結 **AC10** が当該部分文字列を literal に要求しており、エンジニアに裁量がなかった。かつ直前の T-1074 由来の既存ブレット（本タスクの diff 対象外）にも同一の表記がすでに出荷・承認済みで、本ドキュメント全体で bare 形式と `bin/` 付き形式が随所で混在している既知の慣例の一部にすぎない。merge を止める理由にはならないと判断し格下げした。 |
| 2 | `--aggregation <aggregation-file>` の実体を用意する手順がブレットの散文に書かれていない | adversarial のみ: P1（blocker 相当） | major | AC10 のチェック体が検証しない裁量領域（部分文字列の存在のみを検証）にある本文の完全性の欠落であり、エンジニアが実際に書いた新規の散文の中で回避可能だった。ただし有能な LLM オーケストレータが `> <file>` を自明に補う可能性が高く、チェッカー本体の正しさには一切影響しないため、merge を止める Blocker には格上げしない。 |

#### Recommendation
- APPROVE → `.shell-team/todo.md` を READY_FOR_MERGE に進める。

#### Fast-follow disposition
- Finding 1（`bin/` プレフィックス表記の repo 全体的な不整合。`bin/aggregate-verdicts.sh` を含む既存ブレットも同様） — file-an-issue（本タスク固有ではなく `skills/run/SKILL.md` / `templates/prompt-blocks/fanout-orchestration.md` 全体の bare/`bin/`-prefix 混在を横断的に整理する独立した編集タスクとして提案。修正は本タスク自身の凍結 AC10 の文字列要求とも整合させる必要があり、単独の小修正では閉じない）。
- Finding 2（`--aggregation <aggregation-file>` の由来を説明する一文の欠落） — file-an-issue（次の小規模な編集ラウンドで、新ブレットまたはその直前の T-1074 由来の文に "reduce ... to one authoritative verdict, saved to a file" 相当の一文を足すことを提案。チェッカー本体の正しさには影響しないため won't-fix ではなく file-an-issue とする — adopter を実際に混乱させ得る具体的な gap のため）。
- プレースホルダ命名不整合（`<run-id>` vs `<run_id>`、`<telemetry-file>` の未定義） — won't-fix（動作に影響しない純粋な表記ゆれで、repo 全体を見ても同種のゆれが多数存在し、この 1 箇所だけを直す実利が乏しい）。
- `--iteration`/`--attempt` の運用注意の欠落 — won't-fix（チェッカー自身の `--help` と本タスクの `## Input space` にすでに同等の説明があり、fail-closed な拒否メッセージ自体がリカバリ手順を示すため、二重に書く必要性が薄い）。

#### Environmentally-unverified issue-canon reliance
internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — 本レビューの verdict は issue #277 の本文の正典性に依存していない（spec 自身が 2026-08-18 の freeze 時点で GitHub API 経由の一次確認をすでに記録済み）ため、完全性のために記録するのみで、verdict の判断材料にはしていない。
