### Codex Review verdict: REQUEST_CHANGES
- Task: T-1135
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 13 files, +1004/-135 lines (`git diff --stat develop...HEAD`)

Round 1、凍結 intent v1（`c377ee2131d920aff01fdb33f2ab84c9c94fa8ca`）に対するレビュー。QA `READY_FOR_REVIEW`（round 1、commit `86575c0`、addendum `e617e6a`）時点の branch `feature/codex-both-gates`（HEAD `e617e6a`、base `develop` `801c1c7`）を対象。

**独自の裏取り（Codex 起動前に自分で実行、hand-off の転記ではない）**: `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1135-codex-host-slice2.md` → 12 passed / 0 failed / 1 skipped（AC13 は設計上 SKIP）。`bash tests/codex-agents/run.sh` → 32 PASS / 0 FAIL。`shellcheck bin/gen-codex-agents.sh bin/check-codex-agents.sh tests/codex-agents/run.sh` → exit 0。`bash bin/check-prompt-sync.sh` / `bash bin/check-provenance.sh .shell-team/provenance/T-1135.md` / `bash bin/check-interventions.sh --task T-1135 .shell-team/interventions/T-1135.md` / `bash bin/check-pii-shapes.sh --base develop` → 全て exit 0。`bash bin/check-invocation-path.sh --role codex-reviewer` → `admitted codex-reviewer codex-cli wrapper-hosted <repo>/agents/codex-reviewer.md`（AC3 のまま）。docs のヘッダ数（`grep -c '^## '`）は EN/JA とも base 20 → HEAD 20 で一致（AC8 の mirror 不変）。

**自分で行った追加の実機プローブ（Notes for engineer の「材料と判断すれば自分で probe しろ」に応じて実施、$0.15 未満）**: 出荷された `claude -p` recipe 行を、このホストの実 Claude Code CLI `2.1.268` に対して、チェックアウト外の scratch cwd と本チェックアウトの両方で走らせた。(1) `--allowedTools` の git-only allowlist は、許可されたサブコマンドだけの複合コマンド（`git log --oneline -3 && git diff --stat HEAD~1 .`）は通し、許可されていないサブコマンドが混じる複合コマンド（`git rev-parse ...; echo ...`）は丸ごと拒否する — D4 の「`--permission-mode dontAsk` が実際の閉じ込め機構」という主張どおりに機能していることを確認（ツールリストは `["Bash","Glob","Grep","Read"]`、Agent ツールなし、`mcp_servers:[]`、`touch` は `permission_denials` で拒否）。(2) bare `git rev-parse --short HEAD`（単独）は正しく `e617e6a` を返した — 本チェックアウトの `git status --short` はプローブ前後で無変更。この部分は D4 の設計どおりであり、新規の欠陥ではない。

Codex primary pass（`review --base develop --json`）と、round 1 が Blocker/Major を出したため実施した adversarial pass（3 点の疑義を名指しした devil's-advocate プロンプト）の両方の指摘を、下の Blockers/Major/Minor に正規化・統合した。Codex 由来の重大度ラベル（P1/P2、または adversarial pass 自身の `blocker`/`major` 自己申告）は自分の判断への入力として扱い、最終判定は自分で下した（変更点は Synthesis audit ledger に記録）。

#### Blockers

- `templates/prompt-blocks/host-dispatch.md:9` — **`<PROMPT>` プレースホルダをダブルクォート内へ文字列置換する方式はシェル安全でない。** バッククォート、`$(...)`/`$VAR`、埋め込みダブルクォートは、置換前提のシェル（この行を実際に組み立てて実行するのは、Codex ホスト上で spawn された `shell-team-codex-reviewer` エージェント自身であり、それ自体が任意のレビュー指示文字列を `<PROMPT>` に代入して合成する）によって展開・解釈されうる。`<RAW_OUT>`/`<RAW_JSONL>` の置換規約（単純な絶対パス文字列で、シェルメタ文字を含まない）とは性質が違う: `<PROMPT>` は「レビュー指示」という自由記述テキストで、しかもこのブロック自身が何を代入すべきかを一切定義していない。Codex の adversarial pass 自身の再現でも同じ箇所を独立に `blocker` として検出しており（「バッククォートと `$...` は展開され、埋め込みダブルクォートは引数を終端しうる。構文破壊またはコマンドインジェクションを許す」）、これは理論上の懸念ではなく実際にシェル injection へつながりうる設計上の欠落。
  - Suggested fix: プロンプト本文はテンポラリファイルへ書き出して `< "$PROMPT_FILE"`（stdin 経由）で渡す、または `codex-capture.sh --alloc` の `<RAW_OUT>`/`<RAW_JSONL>` と同じ「呼び出し元が絶対パスを一つ用意し、それだけを置換する」規約に寄せる。少なくとも、`<PROMPT>` に何を代入するかの定義と、安全な埋め込み方法をブロック自身が明記すべき。

- `agents/codex-reviewer.md`（Host-conditional note、primary path 直後 = 現在の `:61` 相当）/ `templates/prompt-blocks/host-dispatch.md:7` — **Host-conditional な indirection は primary read path しかカバーしておらず、adversarial/fallback read path（`agents/codex-reviewer.md` の「For the adversarial / fallback read path」段落、および `## Your loop` step 5）は無条件に `codex exec` を実行し続ける。** この spec のゴール文そのもの（「both gates green... with the cross-provider property preserved because the review runs on Claude」）と、D2/D4 が丁寧に潰した「Codex が Codex 自身の出力を Codex でレビューする」という、この二段スライス全体が存在する理由そのものの回避策が、adversarial pass のトリガー条件（`## Your loop` step 5:「triggered only when round 1 surfaces a blocker/major」）が発火するたびに再発する。Blocker/Major は — この review そのものが今回それを検出しているとおり — 決して稀なケースではなく、通常の運用で頻繁に起こる。Codex ホストで spawn された `shell-team-codex-reviewer` が primary（Claude 経由）の結果に Blocker/Major を見つけた場合、その agent は自分自身の instructions（この同じファイル）に従って adversarial pass を `codex exec` として実行しようとする — Codex セッションの内側から Codex を再度呼び出すことになり、cross-provider の保証が黙って失われる（`BLOCKED` にもならず、エラーも出ず、ただ Codex が Codex をレビューする)。Codex の primary pass・adversarial pass の双方が、文言は違えど同一箇所を独立に検出している（primary: P1「adversarial pass だけ host-conditional でない」、adversarial: `major`「line 63 のノートは primary の 2 ブロックしかスコープしておらず、adversarial の段落と step 5（line 104）は無条件に codex exec のまま」）。
  - Suggested fix: `agents/codex-reviewer.md` の adversarial/fallback read path 段落にも同種の host-conditional 参照を追加し、`templates/prompt-blocks/host-dispatch.md` の Claude recipe に adversarial 用のプロンプト・stem を明示する（primary と同じ仕組みを adversarial にも複製）。

#### Major

- `docs/adopting.md:497-503`（"Grant the sandbox network access..." の step、EN 側; JA 側も同じ趣旨） — **`claude -p` 実行に必要な「Claude Code CLI 自体がインストール・認証済みであること」という前提が runbook のどこにも書かれていない。** 現状の文言は「`Not logged in` はネットワーク拒否の症状であって認証の問題ではなく、`/login` では直らない」と断定しているが、これは「sandbox の network_access=false によって拒否された」ケースにのみ真であり、この task が想定する読者（Codex CLI 専業のオペレータで、Claude Code をこれまで使っていないかもしれない — intent block の「An operator running a Codex CLI session」という書き出し自体がそれを示唆する）が、そもそも Claude Code CLI をインストールしていない/認証していないケースでは、`network_access=true` にしても同じ `Not logged in` が出て、しかも今度は本当に `/login` が必要になる。runbook の断定的な書き方は、この現実的に起こりうる（むしろ Codex 専業ホストではデフォルトのシナリオである）失敗モードで読者を誤誘導する。
  - Suggested fix: 「Claude Code CLI がインストール済みで認証済みであること」を明示的な前提条件として追加し、「Not logged in」の 2 通りの原因（未認証 vs. sandbox のネットワーク拒否）を切り分ける診断手順を書く。

- `docs/adopting.md:365-367`（"Using shell-team from Codex CLI" 冒頭段落） — **「no Claude Code *process* anywhere in the dispatch」という書き出しの主張が、同じセクションの step 7/9 で `claude -p` を実行する内容と矛盾している。** `claude -p` は文字通り Claude Code のプロセス起動であり、"process" という語のイタリック強調だけでは「対話的な Claude Code ホストセッション/Agent tool 経由の起動ではない」という意図を読者に正しく伝えられない。Codex primary pass（P2）と adversarial pass（`major`、文言もほぼ一致）の双方が独立に同一箇所を指摘。
  - Suggested fix: 「Claude Code のホストセッションや Agent tool 経由のオーケストレーションは存在しない」という趣旨に書き換え、`claude -p` の子プロセスが実際に起動すること・その前提条件を明示する。

#### Minor / nits

- `bin/gen-codex-agents.sh:368` 付近 — `mv "$STAGE_FILE" "$OUT_DIR/shell-team-$role.toml"` は、目的地パスが既にディレクトリとして存在する場合、`mv` がステージファイルをそのディレクトリの中へ移動して exit 0 を返し、期待される TOML ファイルが実際には作られない（generator は成功したと報告するが、生成物は discover 不能）。実運用でこの状態が発生する可能性は低い（`--out-dir` は通常このジェネレータ自身が管理する）が、防御的にはリネーム前に `[ ! -d "$OUT_DIR/shell-team-$role.toml" ]` 等の一行チェックを足す価値がある。
- `bin/gen-codex-agents.sh:212`（`ROLES_ARR=($ROLES)`） — クォートなし配列代入はパス名展開（glob）も行う。実機で `--roles '*'` をこのリポジトリのルートから実行して確認したところ、`CHANGELOG.ja.md` 等へ展開され、validation loop がその文字種（`.` を含む）で正しく拒否した（rc=2、0 ファイル）ため、この repo では fail-closed のまま実害はなかったが、これは「たまたま展開先の全トークンが `^[a-z][a-z0-9-]*$` を満たさなかった」ことに依存しており、原理的には安全側の設計になっていない。`set -f` を敷くか `while read` ベースの分割に置き換えるのが望ましい。
- `templates/prompt-blocks/host-dispatch.md`（AC12 の `--tokens` 記述）/ `tests/codex-agents/run.sh` fixture 8 — 実際の Claude JSON result の `usage` は単一の `total_tokens` を持たず、`input_tokens`/`output_tokens`/`cache_creation_input_tokens`/`cache_read_input_tokens` に分かれている（自分の live probe でも同じ形を確認）。block の文言「`--tokens` from that result's `usage` field」は、実データに対してどう単一の数値を導出するかを一切示しておらず、fixture もその不一致を隠したまま「フィールド名の存在」だけを検証している。AC12 自体は明示的にフィールド形状をロックしないと宣言しており（droppable component 3、Pre-commitment 上も優先度最低）、この task の凍結 AC としては違反していないが、実際に `log-run.sh --tokens` を本物の Claude 結果から埋めようとする者は、この記述だけでは何を渡せばよいか分からない。

#### Adversarial findings

Round 1 の primary pass が Blocker/Major を出したため、`agents/codex-reviewer.md`・`templates/prompt-blocks/host-dispatch.md`・`docs/adopting.md` の 3 点の疑義（(1) host-conditional note の適用範囲、(2) `<PROMPT>` 置換の shell 安全性、(3) "no Claude Code process" の内的整合性）を名指しして devil's-advocate プロンプトで再実行。結果は上の Blockers/Major に統合済み。追加で見つかった新規指摘はなし（3 点とも primary pass の指摘を独立に裏付け、うち 1 点（`<PROMPT>` 置換）は severity を major から blocker へ引き上げる根拠を提示した）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `<PROMPT>` のダブルクォート内置換がシェル非安全 | primary: P1（既定マッピングで blocker）/ adversarial: 明示的に `blocker`（コマンドインジェクションの具体的経路を提示） | blocker | 両者一致、自分の判断でも実害（injection）の可能性が高く据え置き |
| 2 | adversarial/fallback read path が host-conditional でない | primary: P1（既定マッピングで blocker）/ adversarial: 明示的に `major` | blocker | この task 全体の Goal（cross-provider 保存）を、自己文書化された通常発生条件（Blocker/Major 発生時）下で無言のまま破る構造的欠落と判断し、adversarial 側の major 自己申告より重く見た |
| 3 | Claude CLI 未インストール/未認証の前提が runbook にない | primary: P1（既定マッピングで blocker） | major | 検証済みの happy path（このレビュー自身のレビュー担当ホストは Claude Code 認証済み）を壊すものではなく、別のアダプタ persona（Claude Code 未認証の Codex 専業ホスト）に限定した runbook 完全性の欠落と判断し、blocker から major へ格下げ |
| 4 | `mv` の directory-collision（`gen-codex-agents.sh:368`） | primary: P2（既定マッピングで major） | minor | 発生条件が極めて限定的（`--out-dir` 配下に同名ディレクトリが既に存在する必要がある）で、通常運用では到達しないため major から minor へ格下げ |
| 5 | `--roles` の glob 展開（`gen-codex-agents.sh:212`） | primary: P2（既定マッピングで major） | minor | 実機再現の結果、この repo では validation loop が fail-closed のまま機能した（0 ファイル生成で拒否）ことを確認しており、悪用可能性は理論的にとどまるため major から minor へ格下げ |
| 6 | AC12 の `usage.total_tokens` 記述が実スキーマと不一致 | primary: P2（既定マッピングで major） | minor | spec 自身が AC12 をフィールド形状非ロックの droppable component 3 と明示的に宣言しており、凍結 AC への違反ではなくドキュメント完全性の欠落にとどまるため major から minor へ格下げ |
| 7 | "no Claude Code process" の内的矛盾（`docs/adopting.md:365`） | primary: P2（既定マッピングで major）/ adversarial: 明示的に `major` | major | 両者一致、変更なし |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_ENG` に戻し、上記 Blockers（2 件）と Major（2 件）を紐づける。

#### Fast-follow disposition

- Minor 3 件（`mv` directory-collision、`--roles` の glob 展開、AC12 usage フィールドの記述不一致）はいずれも今回の rework round で Blocker/Major と同じファイル群への追記であり、次の round へ延期せず **このタスク内で対応すべき（owed within this task, not deferred）**。post-merge への繰り延べは提案しない。
- no fast-follow deferrals

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o .shell-team/reviews/.codex-capture.T-1135-codex-primary.out.TDqDDQ > .shell-team/reviews/.codex-capture.T-1135-codex-primary.jsonl.YWIUWN 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (primary): T-1135-codex-primary
- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1135-codex-adversarial.out.mQrKML "Play devil's advocate on the diff between develop and HEAD in this repo (git diff develop...HEAD). Read the files yourself with git diff / nl -ba -- do not expect any diff in this prompt. Focus specifically on these three claims and try to break each one: (1) that agents/codex-reviewer.md's host-conditional note (search for 'Host-conditional note (T-1135)') actually covers BOTH the primary read path AND the adversarial/fallback read path further down in the same file when this role runs as a spawned Codex custom agent on a Codex CLI host -- read both the note's own wording and the adversarial-pass paragraph that follows it, and determine whether the adversarial pass still unconditionally says 'run codex exec' with no host-conditional branch; (2) that templates/prompt-blocks/host-dispatch.md's '<PROMPT>' placeholder in the claude -p recipe line can be safely substituted with a real, markdown-heavy multi-line review instruction (containing backticks, dollar signs, and quotes) inside the surrounding double quotes without an executing shell mangling it, and whether the block documents a safe way to do this; (3) whether docs/adopting.md's Using-shell-team-from-Codex-CLI section is internally consistent about 'no Claude Code process anywhere' given it also instructs running 'claude -p' in the same section. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > .shell-team/reviews/.codex-capture.T-1135-codex-adversarial.jsonl.4LWZT4 2>&1
- pass-role (adversarial): generation
- briefing-fidelity (adversarial): carried — the three suspected findings from the primary pass's own review (host-conditional note scope, `<PROMPT>` substitution safety, "no Claude Code process" wording) were composed verbatim into the prompt argument this invocation passed to Codex
- raw-capture (adversarial): T-1135-codex-adversarial

---

### Codex Review verdict (round 2): APPROVE
- Task: T-1135
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests
- Diff scope (round 2 delta): 9 files, +383/-74 lines (`git diff --stat 0555cb4..HEAD`); full branch diff since `develop`: 14 files, +1329/-190 lines

Round 2、rework `bf9873d`（round 1 REQUEST_CHANGES への回答）。QA round 2 は `bf9873d` に対し full loop を再度歩き、`pii-shape-violation` を 1 件検出（`7414879` で orchestrator が修正、記録のみの正規化）。QA round 3 が `33fc9f4` で PASS。HEAD `33fc9f4`、base `develop`（`801c1c7`）。round 1 と同じ scope/深度校正（bin/templates/docs full depth、fixtures/inline check lines は scaffold depth）、mode A2。

**独自の裏取り（round 1 と同様、hand-off の転記ではない）**: `bash tests/codex-agents/run.sh` → 35 PASS / 0 FAIL（QA の申告と一致）。`CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1135-codex-host-slice2.md` → 12 passed / 0 failed / 1 skipped（AC13 は設計上 SKIP、AC3・AC6 とも引き続き PASS）。`shellcheck bin/gen-codex-agents.sh bin/check-codex-agents.sh tests/codex-agents/run.sh` → exit 0。`bash bin/check-prompt-sync.sh` / `bash bin/check-invocation-path.sh --role codex-reviewer`（`wrapper-hosted` のまま）/ `bash bin/check-pii-shapes.sh --base develop`（clean）/ `bash bin/check-provenance.sh .shell-team/provenance/T-1135.md`（17 decisions, 0 sentinel）→ 全て green。round 2 の diff（`git diff 0555cb4..HEAD`）を `agents/codex-reviewer.md`・`templates/prompt-blocks/host-dispatch.md`・`bin/gen-codex-agents.sh`・`docs/adopting.md`/`.ja.md`・`tests/codex-agents/run.sh` の全ファイルについて自分の目で読み、round 1 の 2 Blocker・2 Major それぞれの修正箇所を hand-off の主張どおりに独立確認した。

**独自の実機プローブ（B1 の再確認、$0.1 未満）**: 出荷された修正後の recipe（`claude -p "$(cat "<PROMPT_FILE>")" ...`）を、バッククォート・`$(echo hi)`・`$HOME`・埋め込みダブルクォートの 4 種のハザード文字を含む prompt file に対して、このホストの実 Claude Code CLI `2.1.268` で単独実行し、`result` フィールドに 4 種全てが未展開のまま逐語的に現れることを確認（exit 0）。QA round 2 の probe (a)/(a2)（実際に spawn された `shell-team-codex-reviewer` 自身の `claude -p` 呼び出しを生の `stream-json` capture から機械的に確認）と合わせて、B1 は独立に二重で閉じている。

#### Round 1 の各指摘の解消状況（class 別、re-opened か新規かを明示）

- **Blocker 1（`<PROMPT>` のシェル非安全な置換）— CLOSED、re-open なし。** `templates/prompt-blocks/host-dispatch.md:9` の recipe 行が `claude -p "$(cat "<PROMPT_FILE>")" ...` へ変更され、`:11` の段落がその理由（command substitution の出力は再パースされない）を明記。`skills/run/SKILL.md` も同型に mirror（`check-prompt-sync.sh` green）。この機構は T-1118 の `<BRIEFING_FILE>` recipe と同一の実証済みパターンの再利用（provenance decision 10 参照）。上記の自分自身の probe と QA round 2 probe (a)/(a2) の双方が、独立に、実際の spawn 経路を通じてハザード文字が逐語的に届くことを確認。
- **Blocker 2（adversarial/fallback read path が host-conditional でない）— CLOSED、re-open なし。** `agents/codex-reviewer.md` に 3 箇所の参照（primary note 直後 `:63` 付近、adversarial 段落末尾、`## Your loop` step 5 末尾）、`templates/prompt-blocks/host-dispatch.md`/`skills/run/SKILL.md` に adversarial 用の `<PROMPT_FILE>` テンプレートと専用 stem（`T-<task-id>-codex-adversarial`）が追加された。`:180` 付近の spec-review mode（T-1092、別 seam）は意図的に対象外のままで、この判断は正当（provenance decision の inventory 参照：spec-review mode はこの凍結 spec の Goal/Non-goals/D-points/AC のいずれにも現れず、round 1 の指摘自体も primary/adversarial/step 5 のみを名指ししていた）。QA round 2 probe (b) が、実際に spawn された reviewer に対して意図的に破壊した fixture diff をぶつけ、primary pass が Blocker を検出 → adversarial pass が **2 回目の `codex exec` ではなく** Claude recipe を `T-<task-id>-codex-adversarial` stem の下で実行したことを、生の event stream から機械的に確認済み。この Blocker は最も core な Goal（cross-provider 保存）に触れる指摘だったため、QA の機械的な実地確認は特に重い証拠として扱った。
- **Major 1（Claude CLI インストール/認証前提の欠如）— CLOSED（本体）だが、**下の新規指摘参照**。** `docs/adopting.md`/`.ja.md` に新しい step 7 が追加され、Codex sandbox の外での sanity check コマンド（`claude -p "reply with the single word ok"`）と、`/login` が実際に効くケースの明示が入った。EN/JA とも 11 step に renumber され、既存の全 cross-reference（`step 3`/`6`/`7`/`8`/`9`/`10`/`11`）を自分で再走査し、stale な参照が無いことを確認（下記 Round 2 の新規指摘参照）。
- **Major 2（"no Claude Code process" の内的矛盾）— CLOSED、re-open なし。** 冒頭段落が「no Claude Code **host session** and no Claude Code Agent-tool orchestration」+「A `claude -p` **child process** does run...」という、実態と整合する記述に書き換えられた。
- **Minor m1（`mv` の directory-collision）— CLOSED、re-open なし。** pass 2 の書き込みループの前に、全ロール分の target path を事前検査する専用ループが追加され（`bin/gen-codex-agents.sh`、pass 2 直前）、fixture arm（"a directory at the target path is refused, not silently moved into"）で確認済み。自分でも `mkdir -p` でディレクトリを事前作成した上でデフォルト実行 → 拒否・0 ファイル書き込みを再現確認。
- **Minor m2（`--roles` の glob 展開）— CLOSED、re-open なし。** `set -f` / `set +f` で `ROLES_ARR=($ROLES)` の直前直後を挟み、glob 展開のみを抑制（word-splitting は温存）。`--roles '*'` をこの repo のルートから再実行し、拒否メッセージが展開後のファイル名でなく**リテラルの `*` 自体**を名指しすることを確認（`set -f` が機能している証拠）。
- **Minor m3（AC12 usage フィールドの記述不一致）— CLOSED、re-open なし。** `--tokens` の定義が「`usage.input_tokens` + `usage.output_tokens` + `usage.cache_creation_input_tokens` + `usage.cache_read_input_tokens` の合計」へ書き換えられ、`skills/run/SKILL.md` の Telemetry (Observe) 段落の `subagent_tokens` と同じ量を指すことも明記。fixture 8 も実スキーマに沿って再構築済み。

#### Round 2 の新規指摘（前回未報告、Blocker/Major/Minor いずれもゼロに収束）

Primary pass と adversarial pass（此度の primary pass 自身が 1 件の指摘を出したため、opt-in 条件を満たし実施）の双方の指摘を、自分の判断で検証・正規化した。

- **却下（採用せず）— `templates/prompt-blocks/host-dispatch.md` の capture stem が常に無印（round-suffix 抜き）で、`agents/codex-reviewer.md:216` の「2 round 目以降は `-r<N>` を付ける」規約に反する（adversarial pass 自身は `blocker` と自己申告）。** 検証の結果、これは誤り。round-suffix の規約はこのファイルの `## Rules` 節に**ホスト非依存の一般規則として一度だけ**書かれており、Codex 側の既存 primary/adversarial ブロック（`codex-capture.sh --alloc --stem T-XXX-codex-primary` 等）も同様に、ブロック自体のリテラル文言には round-suffix の再掲がない——両者とも「呼び出す側（reviewer role 自身）が Rules 節を読んで適用する」という同一の設計であり、今回の T-1135 の Claude recipe が特別に壊れているわけではない。事実、自分自身の今回の round 2 実行そのもの（`T-1135-codex-primary-r2` 等）と、QA round 2 の scratch stem（`T-1135-probea2-hazard` 等）の両方が、ブロック文言の再掲なしにこの規約を正しく適用できることを示している。
- **却下 — `<PROMPT_FILE>` テンプレートが `executor-invocation`/`pass-role`/`briefing-fidelity`/`raw-capture` の 4 フィールドを明示的に運ばず、"reviewer Rules" が要求するそれらのフィールドが欠落している（adversarial pass 自身は `major` と自己申告）。** これはアーキテクチャの誤解に基づく指摘と判断。この 4 フィールドは spawn された Claude プロセス自身が返す verdict 本文の一部ではなく、**reviewer role（今回の私自身がそうしているように）が、パスの実行後に自分の手で review record へ書き足す bookkeeping フィールド**である。Codex 側の既存 primary/adversarial パスも、spawn した `codex exec` 自身にこれらのフィールドを生成させてはいない——同じ設計をこの task が単に踏襲しているだけであり、新規の欠陥ではない。
- **格下げ（欠陥として不採用、観察のみ）— adversarial 用 `<PROMPT_FILE>` テンプレートが primary pass の具体的な finding 文言を埋め込まず、「break each of them」という指示が空虚になりうる（adversarial pass 自身は `major` と自己申告）。** 検証の結果、これは既存の Codex 側 adversarial 指示（`## Your loop` step 5:「'play devil's advocate...' framing on those specific files」）も同程度に汎用的であり、具体的な finding 文言を埋め込むかどうかは呼び出す側の裁量に委ねられている——この task 固有の後退ではない。実際、QA round 2 の probe (b) はこの汎用的な枠組みのまま実際の adversarial pass を発火させ、`eval` ベースの本物の脆弱性を新規に検出しており、実地で機能することが示されている。
- **採用（Minor）— `templates/prompt-blocks/host-dispatch.md:9` / `skills/run/SKILL.md:179` の `claude -p` 行自体には `> "<RAW_JSONL>" 2>&1` という文字どおりのリダイレクトが無く、直後の散文でのみ「stdout はリダイレクトされる」と説明されている（primary pass: P1、"This blocks the advertised path to READY_FOR_MERGE" と重大に自己申告 / adversarial pass: `nit`、「QA の 4 件の生 capture がこの周辺の実行がリダイレクトを補えることを示しており、recipe が本質的に機能しないことの証明にはならない」）。** 自分で該当行を直接確認し、Codex 側の 2 つの `codex exec` ブロックはいずれも `> "<RAW_JSONL>" 2>&1` を**ブロック自身の中に文字どおり**含んでいるのに対し、この Claude recipe の行だけがそれを欠いていることを確認——これは実在する記述上の非一貫性。ただし QA round 2 の 4 件の独立した生 capture（T-1136 本走行、probe a/a2、probe b の primary/adversarial 双方）が、この recipe が実際には正しく publish まで到達することを機械的に確認済みであり、「READY_FOR_MERGE への到達を妨げる」という primary pass 自身の重大な結論は事実に反する。severity は Minor と判断——実害の記録が皆無であることと、これが presentation/consistency の問題であって機構上の欠陥ではないことによる。
  - Suggested fix: 例示コマンド行自体に `> "<RAW_JSONL>" 2>&1` を文字どおり含め、Codex 側 2 ブロックと同じ「コピーしてそのまま動く」形にする。
- **採用（Minor）— `bin/gen-codex-agents.sh` の EXIT cleanup trap が、pass 2 の `STAGE_FILE`（`mktemp "$OUT_DIR/.gen-codex-agents.$role.XXXXXX"`）を対象にしていない（adversarial pass: `minor`）。** 自分でコードを確認: `cleanup()` は `CONTENT_FILES`（pass 1 のスクラッチ）のみを `rm -f` し、pass 2 で作られる `STAGE_FILE` は対象外。`cat ... > "$STAGE_FILE"` または直後の `mv` が（例えばディスク逼迫等で）失敗した場合、`set -euo pipefail` により即座に exit し、`--out-dir` に `.gen-codex-agents.<role>.XXXXXX` という残骸ファイルが残りうる——今回の round が D6 の atomic-write hardening で新規に導入したコード経路であり、round 1 には存在しなかった新規指摘。発生条件は狭い（I/O 失敗のタイミング依存）が実在する欠陥。
  - Suggested fix: `cleanup()` に現在の `STAGE_FILE` パス（グローバル変数として保持）も対象へ加え、`mv` 成功後にクリアする。
- **採用（Nit）— `docs/adopting.md`/`.ja.md` step 7 の段落内「The review pass (steps 8 and 10)」という参照が、実際には step 7 自身も別目的で `claude -p` を実行することと相まって、やや誤読を招きうる（adversarial pass: `nit`、EN/JA とも同型）。** 実害はない（steps 8/10 の役割分担自体は正確）が、文言としては「step 7 の sanity check・step 8 の network 許可・step 10 の実際の review 実行」と 3 者を明示したほうが親切。
- **採用（Minor、round 1 の Major と同一 component だが新規の instance）— `docs/adopting.md:503-508` / `docs/adopting.ja.md` step 7 が「Claude Code CLI が未インストールの場合も、未認証の場合も、同じ `Not logged in` で失敗する」と書いているが、これは未インストールのケースでは事実と異なる（そのバイナリ自体が存在しないため、シェルは `command not found` 相当のエラーを返すのであって、アプリケーション側の `Not logged in` メッセージにはそもそも到達しない）。adversarial pass 自身も同一箇所を独立に検出（`minor`）。** これは round 1 の Major「Claude CLI インストール/認証前提が runbook に無い」と**同一 component（docs のこの同じ段落）**についての**新規の instance**である——round 1 の指摘は「前提が全く書かれていない」ことそのものであり、今回の指摘は「round 2 で新しく書き加えられたその前提文の記述精度」についての、別の具体的な誤り。coordinator の same-class-2 判定（同一 component への 2 round 連続の独立した新規欠陥）の対象になりうるかどうかは orchestrator の判断に委ねるが、この instance 自体の severity は Minor と判断する——実運用上、未インストールのユーザーも "resolve that first...your organization's own Claude Code provisioning" という残りの remedy 文言に沿って結局正しい対処（インストール）へ誘導されるため、実害は限定的。
  - Suggested fix: 「未インストール」と「未認証」を明確に切り分け、前者はバイナリの存在確認（`command -v claude` 等）を、後者のみ `Not logged in`/`/login` を割り当てる。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | capture stem が round-suffix 規約に反する | adversarial: 明示的に `blocker` | 却下（欠陥ではない） | round-suffix 規約は `## Rules` に一度だけ書かれたホスト非依存の一般規則であり、Codex 側の既存ブロックも同じ形（ブロック内に再掲なし）。自分自身の round 2 実行と QA の scratch stem の双方が、再掲なしで規約が正しく適用されることを示す |
| 2 | `<PROMPT_FILE>` テンプレートに executor-invocation 等 4 フィールドが無い | adversarial: 明示的に `major` | 却下（欠陥ではない） | この 4 フィールドは spawn された Claude 自身の返答ではなく reviewer role 自身が事後に書き足す bookkeeping フィールド。Codex 側の既存パスも同じ設計 |
| 3 | adversarial プロンプトが primary の具体的 finding を埋め込まない | adversarial: 明示的に `major` | 観察のみ（欠陥として不採用） | Codex 側の既存 adversarial 指示も同程度に汎用的であり、具体性の埋め込みは呼び出し側の裁量。QA probe (b) が汎用のまま実地で機能することを実証済み |
| 4 | `claude -p` 行にリテラルな `>` リダイレクトが無い | primary: P1（既定マッピングで blocker、"blocks READY_FOR_MERGE" と自己申告）/ adversarial: 明示的に `nit` | minor | QA の 4 件の独立した生 capture が実際には publish まで到達することを機械的に示しており、primary pass 自身の重大な結論（READY_FOR_MERGE を妨げる）は事実に反する。presentation の非一貫性として minor に |
| 5 | `bin/gen-codex-agents.sh` の cleanup trap が STAGE_FILE を対象外 | adversarial: 明示的に `minor` | minor | 変更なし。実在するが発生条件が狭い新規コード経路の欠陥 |
| 6 | "steps 8 and 10" の参照文言がやや誤読を招く | adversarial: 明示的に `nit` | nit | 変更なし |
| 7 | docs step 7 の「未インストールも `Not logged in`」記述が未インストールのケースで事実と異なる | adversarial: 明示的に `minor` | minor | 変更なし。ただし round 1 の Major と同一 component の新規 instance である旨を本文で明示し、orchestrator の same-class-2 判定に委ねる |

#### Recommendation

- **APPROVE** → `.shell-team/todo.md` を `READY_FOR_MERGE` に設定。round 1 の Blocker 2 件・Major 2 件は全て独立に確認された証拠（自分自身の再検証、provenance の生プローブ、QA round 2/3 の機械的な実地確認）に基づき CLOSED。round 2 で新たに検出された指摘は Minor 3 件・Nit 1 件のみで、いずれも merge を妨げるものではない。

#### Fast-follow disposition

- Minor（`claude -p` 行のリテラルなリダイレクト欠如、`docs/adopting.md:9` / `skills/run/SKILL.md:179`）— **file-an-issue**（このリポジトリの adopter 向け recipe ブロックの一貫性の問題。実害の記録は無いため、post-merge の docs 整備で十分）。
- Minor（`bin/gen-codex-agents.sh` の cleanup trap が STAGE_FILE を対象外）— **file-an-issue**（発生条件が狭い I/O 失敗依存の残骸ファイル問題。次の `bin/gen-codex-agents.sh` 改修時にまとめて対応するのが合理的）。
- Minor（docs step 7 の未インストール/未認証の記述精度）— **file-an-issue**（#488 や #487 のような既存の pick-up issue と同種の、adopter ドキュメントの precision fast-follow。round 1 の Major と同一 component の新規 instance であることを issue 本文に明記するよう orchestrator に推奨）。
- Nit（"steps 8 and 10" の参照文言）— **won't-fix**（実害なし、次の docs 改稿タイミングで自然に整理されれば十分）。

- executor-invocation (primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o .shell-team/reviews/.codex-capture.T-1135-codex-primary-r2.out.XJGWEo > .shell-team/reviews/.codex-capture.T-1135-codex-primary-r2.jsonl.9V99M6 2>&1
- pass-role (primary-r2): generation
- briefing-fidelity (primary-r2): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (primary-r2): T-1135-codex-primary-r2
- executor-invocation (adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1135-codex-adversarial-r2.out.zquD78 "Play devil's advocate on T-1135's round-2 rework (branch feature/codex-both-gates, base develop -- read the diff yourself with git diff develop...HEAD, and this round's own delta specifically with git diff 0555cb4..HEAD). This is T-1135, a prior cross-provider review round (recorded at .shell-team/reviews/T-1135.md) left two Blockers and two Majors; this round's rework claims to close all four. A separate primary pass just this round already flagged one NEW issue: templates/prompt-blocks/host-dispatch.md line 9's claude -p invocation has no literal '>' redirection into a capture file in the shown command line -- only a later paragraph's prose says stdout is redirected into the .jsonl raw. Before treating that as fatal, note: QA's own live testing (recorded in .shell-team/todo.md's T-1135 round-2 QA verdict) mechanically confirmed, from raw stream-json captures on disk (not self-reports), that the actual spawned shell-team-codex-reviewer agent DID successfully produce non-empty published capture files across four separate live invocations using this exact recipe -- so treat this as a presentation/robustness question (should the block show the literal redirect the way the two codex exec blocks elsewhere in agents/codex-reviewer.md do?) rather than assuming it never works. Your job: (1) independently assess whether that redirect gap is likely to actually cause a failure in a competent executor's hands, or whether it is a documentation-consistency nit; (2) hunt specifically in the NEW prompt-file templates (the 'Constructing <PROMPT_FILE> -- the primary pass' and '-- the adversarial pass' paragraphs in templates/prompt-blocks/host-dispatch.md) for whether they actually give the spawned Claude process everything agents/codex-reviewer.md's own ## Output section requires (headings, verdict labels, executor-invocation/pass-role/briefing-fidelity fields) or whether something is missing that would make the returned verdict fail bin/check-review-input.sh; (3) check docs/adopting.md's renumbered steps (1-11) and docs/adopting.ja.md's mirror for any stale step-number cross-reference or EN/JA divergence; (4) hunt for anything else broken by this round's diff. Read the files yourself -- nothing is pasted here. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > .shell-team/reviews/.codex-capture.T-1135-codex-adversarial-r2.jsonl.jhi7la 2>&1
- pass-role (adversarial-r2): generation
- briefing-fidelity (adversarial-r2): carried — the primary pass's own new finding (the missing literal redirect), the QA round-2 mechanical evidence contradicting its severity, and the three specific hunt directives (prompt-file template completeness, docs renumbering, general new-defect hunt) were composed verbatim into the prompt argument this invocation passed to Codex
- raw-capture (adversarial-r2): T-1135-codex-adversarial-r2
