# QA verification — T-1143

## Verification items run

- `bash bin/check-intent.sh .shell-team/specs/T-1143-handoff-shape-scan-step.md .shell-team/todo.md` → `aligned: T-1143 v1 (8348a4d48a63169ff9dc656f841eda21d68150f5)`.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1143-handoff-shape-scan-step.md` → `check-acs: 10 passed, 0 failed, 1 skipped, 0 unrecognized` — AC1–AC10 PASS, AC11 SKIP (runtime — no check:), re-run independently by me, not quoted from the hand-off.
- `bash bin/check-interventions.sh --task T-1143 .shell-team/interventions/T-1143.md` → `conformant: .shell-team/interventions/T-1143.md (1 entries, 0 sentinel)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1143.md` → `conformant: .shell-team/provenance/T-1143.md (1 decision entries, 0 sentinel)`.
- `TD=$(bash bin/team-paths.sh --get todo); bash bin/check-handoff.sh "$TD"` → exit 0.
- `bash bin/check-board-headings.sh "$TD" --base "$(git merge-base develop HEAD)"` → exit 0.

## CI-wired suites (workflow order, plus reverse-mapped reached set)

All green, own-run:

- `bash tests/check-prompt-sync/run.sh` → `All check-prompt-sync assertions passed.`
- `bash bin/check-prompt-sync.sh` (dogfood) → `check-prompt-sync: all registered prompt blocks in sync`
- `bash tests/check-pii-shapes/run.sh` → `check-pii-shapes suite: all assertions passed`; the three `PASS: hand-off shape scan: …` labels AC6 names all present verbatim, confirmed with `grep -F "hand-off shape scan"` over the run's own stdout.
- `bash bin/check-pii-shapes.sh --base "$(git merge-base develop HEAD)"` → `check-pii-shapes: clean (no PII-shaped bytes found)`.
- `shellcheck tests/check-pii-shapes/run.sh` → exit 0 (shellcheck 0.11.0 available on this workstation).
- Reverse-mapped reached suites (`grep -rl -- <path> tests/*/run.sh` for each of the 9 edited/added deliverable paths): `bash tests/gen-playbook-blocks/run.sh`, `bash tests/machine-tokens/run.sh`, `bash tests/codex-skeleton-hygiene/run.sh`, `bash tests/check-invocation-path/run.sh`, `bash tests/codex-agents/run.sh` — all exit 0, all green. `tests/derive-populations/run.sh` maps to `tests/check-pii-shapes/run.sh` only through a comment (`grep -n check-pii-shapes tests/derive-populations/run.sh` → line 6, a precedent-citing comment, not an execution dependency) — judged inapplicable, confirmed by reading the line. `bash tests/derive-populations/run.sh` still run for completeness → exit 0.
- Also re-run (own inputs untouched by this diff, sanity check): `bash tests/check-model-pins/run.sh`, `bash bin/check-model-pins.sh`, `bash tests/check-playbook/run.sh`, `bash bin/check-playbook.sh "$(bash bin/team-paths.sh --get lessons)"`, `bash tests/playbook-promote/run.sh` — all exit 0.

## Summarized-sources verification (spec's own claims checked against the named source, not against the spec's paraphrase)

- `.shell-team/retros/2026-09-15-records-publishable.md:43` — read the `## Problem` section directly; the four enumerated sites `(1)`–`(4)` match the spec's summary (a QA review record; a reviewer-confirmed Major spanning a fixture/provenance/QA-record triple of one origin; a hand-off that re-fired the class; two board lines from the predecessor task handled as records maintenance). Independently reproduced the board's own `count:` command (`awk … | grep -o '([1-4])' | sort -u | wc -l`) → `4`, matching. Result: accurate.
- `.shell-team/retros/2026-09-15-records-publishable.md:51,68` — read `## Try` and `## Lesson candidates`; both name the three candidate homes (`agents/engineer.md`, `agents/qa-verifier.md`, `agents/scrum-master.md`) or "promoting the opt-in mode to a required pre-hand-off step", and state the firing condition in the spec's own words. Result: accurate.
- `.shell-team/interventions/T-1141.md` — read in full; the `effect:` field's closing clause is a near-verbatim match to the spec's citation ("carried to the retro as a design-premise candidate for a pre-commit shape check in opt-in mode"), and the entry's `intervention:` type is `assumption-contradicted`, consistent with the spec's "design-premise failure, not an execution slip" framing. Result: accurate.
- `bin/check-pii-shapes.sh:95-110,200-212` — read both comment blocks; the tracker-key outline (upper-case letters only, 2–10 chars, hyphen, digit run; off by default; the accepted-noise ground) matches the spec's claim word for word in substance. Result: accurate.
- `bin/check-pii-shapes.sh:229-239` — read the usage/exit-code block; `--base`/`--all`/`--help`, exit 0/1/2 with 2 defined as "a check that cannot evaluate its input never exits 0" (paraphrased in spec as "a run that cannot evaluate its input never reports clean"). Result: accurate.
- `bin/check-pii-shapes.sh:744-774` vs `:811-859` — read both code paths live: `--all` reads from disk (`cp`, `readlink` on symlinks); change-scoped reads `git cat-file -p "HEAD:$path"` — the **committed blob at HEAD**, not at the diff base. This is the load-bearing distinction the spec's DP2 and AC6(b) build on, and it holds exactly as claimed.
- `bin/check-pii-shapes.sh:862-868` — read the `FINDING pattern=%s path=%s line=%s` printf sites (lines 652, 677 in the current tree — line numbers shift from the base-ref citation because this is a base-ref-relative source citation against a script this task does not touch, so absolute current-tree line numbers differ while the base-ref-cited content is unchanged); confirmed the exact three-field grammar and the any-finding-exits-1 rule. Result: accurate.
- `docs/pii-controls.md:70-97` and `docs/pii-controls.ja.md:64-91`, plus `:103` (EN) — read the base-ref blobs at `d5884ff` directly (`git show d5884ff:docs/pii-controls.md`): the three invocation forms, the opt-in variable, the exit codes, the no-echo rule, and (at base-ref line 103, confirmed present at that exact line) the accepted-noise/triage-not-allow-list sentence for standards identifiers under the opt-in. Result: accurate, including the specific line-number citation.
- `templates/prompt-blocks/registry.txt:1-29,57` — read the header comment (mode definitions, scope-limit note) and row 57 (`marker record-portability.md agents/scrum-master.md agents/pm-spec.md agents/engineer.md agents/qa-verifier.md agents/codex-reviewer.md agents/tech-lead.md`) — six consumers including scrum-master and codex-reviewer, as claimed. Result: accurate.
- `templates/prompt-blocks/record-portability.md:5` — read directly; the "authoring-time discipline, not a detector" sentence is exactly there, and `AC5`'s `cmp` clause (independently re-run as part of AC5) confirms the file is untouched by this task. Result: accurate.
- `agents/{pm-spec,tech-lead,ui-designer,engineer,qa-verifier,scrum-master,codex-reviewer}.md:4` — read each frontmatter `tools:` line directly: the first three carry no `Bash`, the last four do. Result: accurate.
- `CONTRIBUTING.md:113,116` and `CHANGELOG.md:3` — read both directly: "a release does not run the loop and gets no board entry" and "new release entries land … as part of the release procedure" are both present in substance. Result: accurate.
- `tests/check-pii-shapes/run.sh:5-28,68-69,98-133,1720-1744` — read the file's own header, the `pass`/`fail` helpers, `new_repo`/`add_fixture_line`/`add_fixture_lines`, and the closing `all assertions passed` line; every claimed contract element is present as described. Result: accurate.
- `.github/workflows/check-handoff.yml:152-156,244-245,247-248` — read directly: the check-prompt-sync fixture-suite step and dogfood step, the check-pii-shapes fixture-suite step, and the diff-scoped step with no `PII_CHECK_TRACKER_KEY` anywhere in the file (`grep -n PII_CHECK_TRACKER_KEY .github/workflows/check-handoff.yml` → no output). Result: accurate.
- `bin/check-prompt-sync.sh:1-34,64-90` — read the usage/exit-code header block: exit 0/1/2 as claimed, check-only (never rewrites), marker-mode exact BEGIN/END pair with CRLF/trailing-whitespace normalization. Result: accurate.
- `bin/team-paths.sh:183-195` — read the `--get` case statement: the ten keys (`base, todo, loops, runs, retros, reviews, specs, provenance, interventions, lessons`) match exactly. Result: accurate.
- `bin/check-provenance.sh:12-38` and `bin/check-handoff.sh:1-24` — read both headers: provenance checker takes exactly one positional argument (usage class); hand-off linter's first positional argument defaults to `tasks/todo.md` (`FILE="${1:-tasks/todo.md}"`), confirming the claim that its own default is the other (legacy) layout. Result: accurate.

No named source was environmentally unreachable; every one was opened and read directly from this checkout.

## Fixture-round read (item 4)

Read `git diff d5884ff...HEAD -- tests/check-pii-shapes/run.sh` in full. The added round:

- (a) writes an untracked record with `printf` (never `add_fixture_line`, which commits) into a repo from `new_repo`, then runs the documented `--all` invocation and asserts `rc=1` plus a `pattern=tracker-key path=<relpath>` line — a genuine positive.
- (b) is a real negative control, not a tautology: the record is never committed (no `add_fixture_line`/`git add`/`git commit` touches it after being written), so `--base <the repo's own base commit>` — which change-scoped mode resolves via `git diff --raw <base> HEAD`, confirmed by direct code read above — sees zero changed paths between that base and HEAD (no commit occurred after the base), and the assertion checks both `rc=0` and no `^FINDING` line. This exercises the exact mechanism DP2 describes (a diff-scoped read is blind to anything that never became a commit), not an assertion that would pass regardless of the record's state.
- (c) rewrites the same file with a fragment-free, in-words description and re-runs `--all`, asserting no `FINDING` line names that path — matches the block's "fixed in place" disposition.
- Shape assembly: `HOS_NS1`/`HOS_NS2`/`HOS_NUM` are separate variables concatenated at runtime (`HOS_NS="${HOS_NS1}${HOS_NS2}"`, `HOS_LINE="reference ${HOS_NS}-${HOS_NUM} filed"`); no finished-shape literal appears in the diff as committed text — confirmed by my own read of the diff and independently by AC8(b)'s own regex re-run inside the acs run above. The design-point reference in the added comment is spelled "decision point twelve", the non-hyphenated convention this spec itself uses, avoiding a self-match.
- The scratch repository comes from `new_repo` (`HOS_REPO="$(new_repo)"`), so the suite's own temp-hygiene assertion (independently confirmed: `PASS: temp hygiene: … (69 repos, all under $WORK)`) covers it.

## Adopter docs (item 5)

Read both new sections end to end (quoted in full above during the run, not reproduced here). Byte-exact headings confirmed by AC7's own live check. Substance agreement: both state that the four shelled roles (engineer, QA, scrum-master, cross-provider reviewer) run the checker once more before hand-off, in `--all` mode with the opt-in enabled, because the change-scoped default cannot see an uncommitted record; both state the result is filtered by `path=` and quoted as one `- shape-scan:` line; both state the step is not wired into CI. The Japanese section reads as this file's own authored voice (matches the surrounding document's register and doesn't carry translation artifacts such as literal English word order or untranslated connective phrases) rather than a mechanical rendering of the English paragraph.

## AC11 audit, item by item

- (a) **Mutation self-check**, all ten claimed in the hand-off — I independently re-executed two live and confirmed exact restoration and re-green:
  - AC1: mutated `templates/prompt-blocks/record-shape-scan.md`'s `fixed in place` anchor → check-acs reported `AC1: FAIL (exit 1)`; restored from a saved copy, `diff` reported byte-identical, `git status --short` empty, check-acs reported `AC1: PASS (exit 0)` again. Confirmed.
  - AC3: perturbed `agents/engineer.md`'s block region text → `bash bin/check-prompt-sync.sh` itself reported `drift in prompt-block 'record-shape-scan'` and check-acs reported `AC3: FAIL (exit 1)`; restored from a saved copy, `diff` byte-identical, `git status --short` empty, both `bash bin/check-prompt-sync.sh` (`all registered prompt blocks in sync`) and check-acs `AC3: PASS (exit 0)` confirmed. Confirmed.
  - AC2, AC4–AC10 mutation claims: not independently re-executed by me (two of ten is the item's stated floor); read the hand-off's own description of each and found each internally consistent with the criterion it targets (a field mutation for AC2's row shape, an excluded-role marker injection for AC4, a deletion for AC5's numstat guard, a wrong-pattern-id retarget for AC6 isolating one of three assertions, a wrong-anchor substitution for AC7, two separate probes for AC8's (a) and (b), a marker-strip for AC9, a stray file for AC10) — none described a mutation that would leave its target criterion vacuously green.
- (b) **Execution-context matrix**: re-ran the prescribed invocation and `check-prompt-sync.sh`/`check-pii-shapes.sh --all` myself both as `bash bin/<script>` and with this checkout's `bin/` prepended to `PATH` and the bare name — identical output and exit codes both ways. Confirmed.
- (c) **CI-equivalence**: independently reverse-mapped and re-ran (see the CI-wired-suites section above); the reached set I derived matches the hand-off's claimed set exactly, including the `tests/derive-populations/run.sh` comment-only judgment. Confirmed.
- (d) **Full-population downstream inventory**: this checkout's release-sweep deferral is a standing local operating policy (`CLAUDE.local.md` "軽い運用 A2"), disclosed in the hand-off rather than silently skipped; the population-enumeration command (`ls -1 … | wc -l`) reproduces `142` when I re-run it myself. The literal-path heuristic count (442) is a lower bound the hand-off itself labels as such; I did not re-run the full two-arm population sweep (deferred to the release sweep per the disclosed local policy, consistent with the spec's own AC11(d) alternative-discharge clause). This is the one AC11 item this round leaves to the release sweep rather than fully discharging now — disclosed, not silent.
- (e) **Plural-corpus read**: read the shipped block myself; every governed noun is plural or explicitly zero-inclusive ("record path(s)", "finding(s)", "a round in which you wrote no record file still carries the line, with `<N>` zero"). Confirmed.
- (f) **PII-shape self-check before first commit**: the hand-off's `- shape-scan:` line and its command were independently re-run by me (see the next section) and reproduce exactly. Confirmed.
- (g) **Norm-text read**: read the block's own wording against `record-portability.md:5`'s "not a detector" sentence myself; the two govern different rules (naming-by-role vs. this shape-detection step) and do not contradict each other; `record-portability.md` is confirmed byte-identical to base (AC5). Confirmed.

## Independent re-run of the opt-in scan, filtered to this round's touched paths (item 7)

`PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all` run fresh by me:

- Total findings, whole tree: `grep -c '^FINDING' <scan-output>` → `1940` (matches the hand-off's figure exactly).
- Findings restricted to this round's 11 touched files (`grep -E 'path=(…eleven paths…) '`): `216` (matches the hand-off's figure exactly). Per-path breakdown, each counted separately with its own `grep -F "path=<p> " | wc -l`: `agents/qa-verifier.md` 1, `agents/codex-reviewer.md` 1, `tests/check-pii-shapes/run.sh` 25, `.shell-team/todo.md` 189, all other seven paths 0 — identical to the hand-off's breakdown.
- Diff-added-lines check (AC8(b), re-derived line ranges independently rather than trusting the hand-off's disjointness claim): the finding lines inside `tests/check-pii-shapes/run.sh` fall at line numbers ≤ 1682; this round's added "hand-off shape scan" section occupies lines 1714–1782 in `git diff d5884ff...HEAD`'s `+` hunk — disjoint, confirmed. The finding at `agents/qa-verifier.md` line 81 and at `agents/codex-reviewer.md` line 3 both sit well outside those files' newly-added marker regions (`agents/qa-verifier.md:175-188`, `agents/codex-reviewer.md:241-254`) — pre-existing, confirmed. The 189 findings inside `.shell-team/todo.md` all sit at line numbers ≥ 63, strictly below (i.e. later in, since the board lists newest first) this round's own T-1143 entry, which occupies lines 14–46 — pre-existing board history, confirmed by direct line-number comparison, not accepted on the hand-off's word.
- All 216 findings, on independent path/pattern inspection, are `pattern=tracker-key` and fall into the two accepted-noise classes the checker's own documents name (this project's hyphenated decision-point label convention, and a same-outline text/digest standards identifier) — none is a real quotation.
- Split claim confirmed: zero new findings on lines this round's diff added (the AC8(b) added-lines guard is independently satisfied, per the line-range comparison above); 216 findings on pre-existing lines of touched files (accepted noise, unrelated to this round's edits).

## My own review record's shape-scan (item 7's final clause — this block is verified by its own first live use)

Before committing this review record and the board hand-off, I ran the block this task ships on my own record files:

- shape-scan (pre-append run, before this file's own commit): rc=1 — 2 record path(s) written this round (`.shell-team/reviews/T-1143.md`, `.shell-team/todo.md`) — 189 finding(s) on them, all 189 classified accepted noise (0 real quotations) — command: `D=$(mktemp -d "$TMPDIR/qa1143shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=(\.shell-team/reviews/T-1143\.md|\.shell-team/todo\.md) ' "$D/scan.txt" | wc -l`

Breakdown by path, each its own count: `.shell-team/reviews/T-1143.md` 0 (this file's own citations of the checker's accepted-noise classes are description, never a completed shape — none of this file's own added lines carried a `pattern=tracker-key` match); `.shell-team/todo.md` 189 (unchanged pre-existing count from the section above, since my board append has not yet been written at the moment this scan ran). A second, final run after appending the board hand-off is reported in the board's own `- shape-scan:` line below, since that is the one whose own content this second run needs to cover.

## Verdict

PASS. All ten scriptable acceptance criteria (AC1–AC10) hold on independent live re-run; AC11's runtime items are individually audited above, with AC11(d)'s full population sweep explicitly deferred to the release sweep under this checkout's disclosed standing local policy rather than silently skipped. No regression found in the four role files, the registry, the fixture suite, or either adopter document. Two edge cases probed beyond the spec's own enumeration: (1) whether the round's own new record files carry any newly-introduced shape (checked directly with the opt-in scan filtered to added-diff line ranges — clean); (2) whether the negative control (AC6(b)) is a real mechanism test rather than a tautology (confirmed by direct code read of the change-scoped diff resolution). Both are within the spec's own Input-space reachable classes (2, 3, 6), not synthetic extremes.

### Codex Review verdict: REQUEST_CHANGES

- Task: T-1143
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 15 files, +628/-0 lines (`git diff --stat d5884ff...HEAD`)

#### Blockers

None.

#### Major

1. `templates/prompt-blocks/record-shape-scan.md:6` — the code sample's redirect target `$out` is never defined anywhere in the block, and its own snippet, if run literally as the fenced block invites, fails before the checker even executes: `bash -c 'unset out; echo hi > "$out"'` → `bash: : No such file or directory`, exit 1 (verified live). Under the block's own contract two bullets later — "a `0` or a `1` is a scan that completed; a `2` is a scan that did not complete" — this failure mode is indistinguishable from a genuine completed-and-clean or completed-and-matched run, since no `FINDING` line follows either way and the exit status is `1`, never `2`. This directly defeats the block's own stated safety invariant (a non-completing scan must never read as an absence of findings). The same line carries a second, related fragility: `... --all > "$out" 2>&1; rc=$?` on one statement breaks under `set -e` too — if the checker legitimately exits 1 (a real finding) and this snippet is composed into a script or shell session with `set -e` active, the shell aborts at the checker's own non-zero exit before `rc=$?` ever runs. Neither failure mode fired in this round's two live hand-offs, because both the engineer and QA substituted their own scratch-file idiom (`$D/scan.txt`, no `set -e`) instead of the literal snippet — but the shipped exemplar itself doesn't demonstrate that safer form, and a future round that follows it literally would silently lose the guarantee the block exists to provide. Fix: define `out` to a fresh path before the invocation (e.g. `out="$(mktemp)"`) and capture `rc` without relying on `set -e` neutrality, matching this repository's own `- check:` idiom elsewhere (`if PII_CHECK_TRACKER_KEY=1 bash ... --all > "$out" 2>&1; then rc=0; else rc=$?; fi`).

2. `docs/pii-controls.md:110-112` and `docs/pii-controls.ja.md:102-104` — both adopter-facing sections describe the disposition of a hit on the writer's own file only as "fixes any hit on its own file in place" / "自分のファイルへの検出は…その場で直す", omitting the canonical block's own accepted-noise carve-out ("A hit the checker's own documents declare as **accepted noise**… is classified as such… instead of being edited away"). An adopter reading only the adopter-facing documentation — the intended audience for these two files — would reasonably conclude every hit on their own file must be edited away, including a legitimate design-point label or standards identifier the checker's documented accepted-noise class exists specifically to protect from exactly that rewrite. **AC7** does not test for this (its required-anchor list for the extracted region does not include "accepted noise"), so this cross-artifact contradiction between the shipped mechanism and its own summary passed the frozen criteria untouched. Present identically in both languages. Fix: add the same carve-out clause to both sections (e.g. "except a hit the checker documents as accepted noise, which is classified rather than edited").

3. `tests/check-pii-shapes/run.sh`'s "hand-off shape scan" assertion (b) (the block starting around `HOS_REPO="$(new_repo)"`) does not demonstrate the mechanism it is labeled to prove, verified empirically. `HOS_BASE` is captured as the scratch repository's HEAD *before* the untracked record is written, and nothing is committed afterward, so `HOS_BASE == HEAD` (the identical SHA) at the moment `--base "$HOS_BASE"` runs. `check-pii-shapes.sh`'s change-scoped path enumeration is `git diff --no-color --no-renames --raw -z "$POINT" HEAD` (`bin/check-pii-shapes.sh:811`) — a diff between two identical commits is empty regardless of any file's tracked state or content, so this assertion passes for a reason unrelated to the "reads committed blobs" claim its own label makes ("the same uncommitted record is invisible to the change-scoped default, which reads committed blobs"). The spec's own body text for AC6 names the real, weaker cause directly: "the untracked file is not in the diff at all." The genuinely demonstrative case — a *tracked* file locally modified but not yet committed, which `git diff --raw <base> HEAD` **would** list as changed while `git cat-file -p HEAD:<path>` still returns the stale pre-edit content — is never exercised by this fixture, and is also the input class the spec's own `## Input space` reachable-class 2 names explicitly alongside the untracked case ("untracked, or tracked and modified but uncommitted"). I constructed and ran both scenarios directly against `bin/check-pii-shapes.sh` on a disposable scratch repository: the untracked/`HEAD==BASE` case (matching the shipped fixture exactly) reports clean under `--base` for the trivial reason above; a tracked file carrying the same shape as an uncommitted local edit also reports clean under `--base` and is correctly found under `--all` — the real DP2 mechanism, never exercised by the shipped fixture. The spec's own pre-commitment separately prices this exact criterion as `droppable-1st`, the cheapest component to cut — consistent with, not contradicting, this finding. Fix (either serves): rewrite the fixture's negative control to use a tracked-and-locally-modified file (matching the spec's own declared reachable input class) so it actually demonstrates the committed-blob mechanism, or invoke the spec's own pre-commitment to drop AC6(b) and file the gap as its own issue per the drop-disposition clause, rewording AC6(b)'s label so it no longer claims a mechanism it does not test.

#### Minor / nits

- The canonical block does not explicitly require a fresh re-run of the scan after fixing a hit, before quoting the hand-off's summary line — a role could in principle fix a hit and then quote the pre-fix run's stale counts. Not exercised this round (both hand-offs reported zero unfixed real quotations, only accepted noise), and the block's own bullet ordering ("fixed in place… before the hand-off is written" then "quote the run's summary") plausibly implies re-running as the intended sequence, but it is not stated as an explicit step.
- `docs/pii-controls.md:101` / `docs/pii-controls.ja.md:94` — "run this checker once more of their own accord" / "自ら走らせる" reads as discretionary, while the canonical block's own wording ("Before you write your hand-off, scan…") is an imperative duty; a reader of the adopter doc alone could reasonably read the step as optional.
- `docs/pii-controls.md:101` — "the four record-writing roles that carry a shell" restates DP4's necessary condition (has `Bash`) but omits the second, independent exclusion ground DP4 also states (triage-orchestrator and drift-evaluator have a shell too but are excluded for not being phases in the loop's per-round hand-off chain) — a reader could reasonably wonder why those two roles are absent.
- The `- shape-scan:` line's counts (`<N>`, `<M>`, `<K>`, and any further per-path breakdown, as both this round's hand-offs supplied) are not each individually reproducible from the one prescribed command alone — both hand-offs derived them with additional ad hoc `grep`/`wc -l` composition invented independently. Not a functional defect (both derivations were independently confirmed correct on my own re-run), but the block does not specify a derivation, leaving each role to invent its own.
- Out of this task's scope (Non-goal #1 / Out-of-scope item 2: "any behaviour of `bin/check-pii-shapes.sh` … beyond the two modes' documented reading targets"), reported for disclosure rather than as a finding against this diff: `.gitignore:15-17` excludes `.shell-team/reviews/*.txt`, `*.json` and `*.jsonl` from tracking, and `--all` enumerates only tracked-plus-untracked-but-not-ignored paths (confirmed by direct read of `bin/check-pii-shapes.sh`'s `--all` path), so a codex-reviewer round's own raw capture files are structurally invisible to this very task's own prescribed self-scan. If a raw Codex capture ever echoed a flagged shape verbatim, the prescribed `--all` scan run by codex-reviewer on its own round would not catch it in that file. This is squarely a `bin/check-pii-shapes.sh` behavior question this task's Non-goals correctly place out of scope, not a defect in this task's own deliverables.

#### Adversarial findings

Triggered by the three Major findings above, against `templates/prompt-blocks/record-shape-scan.md`, `docs/pii-controls.md`, `docs/pii-controls.ja.md`, the four role files and `tests/check-pii-shapes/run.sh`. The adversarial pass independently surfaced: the same `$out`/`set -e` fragility (folded into Major 1); the gitignore blind spot on codex-reviewer's own raw captures (folded into the out-of-scope Minor above); the missing tracked-modified-uncommitted case in the fixture's negative control (independently corroborating, and the trigger for my own empirical verification that became Major 3); the missing explicit re-run-after-fix step (folded into the first Minor above); and the discretionary-sounding doc wording (folded into the second Minor above). No new Major or Blocker beyond what is already listed.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `$out` undefined in the canonical block's code sample; literal execution silently reports `rc=1` (indistinguishable from a completed scan) instead of failing loudly | major (Codex primary, native P1) | major | Kept at major rather than escalating to blocker: demonstrated non-blocking in this round's two live hand-offs (both roles substituted their own scratch-file idiom rather than the literal snippet), and no acceptance criterion depends on the snippet's literal executability — but kept as major, not downgraded further, because the failure mode, once triggered, silently defeats the block's own stated safety invariant |
| 2 | Same line's `; rc=$?` form breaks under `set -e` | major (Codex adversarial, native) | folded into #1 | Same location, same root cause (a fragile literal code sample), same remedy — not counted as an independent defect |
| 3 | `docs/pii-controls.md` and `.ja.md` omit the canonical block's accepted-noise carve-out | major (Codex primary, native P2) | major | Unchanged — genuine cross-artifact contradiction on the Tier-1 adopter-facing surface, confirmed by direct read of both files against the block's own text |
| 4 | AC6(b)'s negative control does not test the tracked-modified-uncommitted case (DP2's actual mechanism) | minor (Codex adversarial, native) | major | Escalated on my own empirical verification (two scratch-repo scenarios run directly against `bin/check-pii-shapes.sh`): the shipped fixture's untracked/`HEAD==BASE` case passes for a cause unrelated to the "reads committed blobs" claim its own label makes, and the load-bearing case the spec's own Input space class 2 names is untested. Escalated because this is the fixture the spec itself calls "the load-bearing negative control" and the block's mode-choice justification (DP2) rests on it |
| 5 | codex-reviewer's own raw `.txt`/`.jsonl` captures are gitignored, so `--all` cannot see them under this task's own prescribed step | major (Codex adversarial, native) | out-of-scope minor | Confirmed via direct read of `.gitignore:15-17`, but this is a behavior of `bin/check-pii-shapes.sh`'s own `--all` enumeration, which this spec's Non-goal #1 and Out-of-scope item 2 explicitly place outside this task's scope lock — reported as disclosure and a fast-follow candidate, not a finding against this diff |
| 6 | Block does not require a fresh re-run after fixing a hit, before quoting the hand-off's summary | major (Codex adversarial, native) | minor | Not exercised this round (zero unfixed real quotations in either hand-off, only accepted noise); the block's own bullet ordering plausibly implies re-running as the intended sequence even though it is not stated as an explicit step |
| 7 | "of their own accord" / "自ら走らせる" reads as discretionary against the block's own imperative wording | minor (Codex primary and adversarial, agree) | minor | Unchanged |
| 8 | "four record-writing roles that carry a shell" omits DP4's second, independent exclusion ground | minor (Codex adversarial, native) | minor | Unchanged |
| 9 | `- shape-scan:` counts not mechanically reproducible from one documented pipeline | minor (Codex adversarial, native) | minor | Unchanged — both hand-offs derived correct counts by their own composition; no functional defect observed |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG` with the three Major findings above (canonical block's `$out`/`set -e` fragility; the docs' missing accepted-noise carve-out in both languages; AC6(b)'s negative control not testing its claimed mechanism)

#### Fast-follow disposition

- Finding 5 (codex-reviewer's own gitignored raw captures invisible to `--all`) — file-an-issue — rationale: a real, reproducible gap in `bin/check-pii-shapes.sh`'s `--all` enumeration relative to this project's own gitignore patterns for review-record raw captures; out of this task's scope lock (`bin/check-pii-shapes.sh` is untouchable here per the first Non-goal), but concrete and actionable enough to file rather than let it sit undisclosed.
- Finding 6 (no explicit re-run-after-fix step in the canonical block) — file-an-issue — rationale: a real ordering ambiguity in a shipped, adopter-facing canonical block that could produce a stale hand-off summary in a future round where an actual quotation needs fixing (not exercised this round); a cheap, generalizable wording fix.
- Finding 7 (discretionary-sounding doc wording) — won't-fix — rationale: the canonical block (the operative mechanism) is unambiguous and imperative; the docs are a summary rather than the operative instruction, and the gap is stylistic rather than behavior-affecting.
- Finding 8 (DP4's second exclusion ground omitted from the adopter docs) — won't-fix — rationale: the full DP4 rationale is already disclosed in the frozen spec and this task's Non-goals; the adopter docs are a summary and are not required to restate every design-point ground, and no adopter action depends on knowing the second exclusion reason.
- Finding 9 (manual count derivation for the `- shape-scan:` line) — won't-fix — rationale: both hand-offs in this round demonstrated correct, independently-reproducible derivations; the block's own convention (every count carries the command that produced it) already provides the audit trail without needing to prescribe one fixed pipeline.

#### Per-pass record fields

- executor-invocation (codex-primary): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary): generation
- briefing-fidelity (codex-primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (codex-primary): T-1143-codex-primary
- self-detected-host (codex-primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch
- executor-invocation (codex-adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1143-codex-adversarial.out.fSIKx0 "Play devil's advocate on the diff between develop and HEAD in this repo, focused specifically on these files: templates/prompt-blocks/record-shape-scan.md, docs/pii-controls.md, docs/pii-controls.ja.md, agents/engineer.md, agents/qa-verifier.md, agents/scrum-master.md, agents/codex-reviewer.md, tests/check-pii-shapes/run.sh. Read the files yourself with git diff / nl -ba -- do not expect any diff pasted here. This is a review of a new 'record shape scan before hand-off' step added to four agent prompt files, backed by a canonical prompt block registered against them, a new fixture round, and adopter documentation in English and Japanese. Two known issues already found: (1) the canonical block's code snippet redirects output to an undefined $out variable; (2) docs/pii-controls.md and its Japanese counterpart describe fixing a hit on your own file in place without repeating the block's own accepted-noise carve-out (a hit in the checker's declared accepted-noise class should be classified, not edited away), which could mislead an adopter reading only the docs into rewriting a legitimate design-point label or standards identifier. What else could break this in practice: ambiguity in the four-role wording, the marker-mode registration, the fixture's negative control, cross-language disagreement between the English and Japanese doc sections, or any other correctness gap. Return findings as a JSON array of {severity, file, line, issue, suggestion}."
- pass-role (codex-adversarial): generation
- briefing-fidelity (codex-adversarial): carried — the round's own two confirmed findings (the `$out` fragility and the docs' missing accepted-noise carve-out) were written directly into this pass's prompt, along with an open-ended devil's-advocate mandate over the same file set
- raw-capture (codex-adversarial): T-1143-codex-adversarial
- self-detected-host (codex-adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

#### Record shape scan (own record, this pass)

- shape-scan: rc=1 — 5 record path(s) written this round (`.shell-team/reviews/T-1143.md`, `.shell-team/reviews/T-1143-codex-primary.txt`, `.shell-team/reviews/T-1143-codex-primary.jsonl`, `.shell-team/reviews/T-1143-codex-adversarial.txt`, `.shell-team/reviews/T-1143-codex-adversarial.jsonl`) — 0 finding(s) on them, 0 classified accepted noise — command: `D=$(mktemp -d "$TMPDIR/t1143codexshapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=(\.shell-team/reviews/T-1143\.md|\.shell-team/reviews/T-1143-codex-primary\.txt|\.shell-team/reviews/T-1143-codex-primary\.jsonl|\.shell-team/reviews/T-1143-codex-adversarial\.txt|\.shell-team/reviews/T-1143-codex-adversarial\.jsonl) ' "$D/scan.txt" | wc -l`. Disclosure: the four `.txt`/`.jsonl` raw-capture paths in this list are gitignored (`.gitignore:15-17`) and confirmed absent from the whole scan's output entirely (`grep -c "T-1143-codex" "$D/scan.txt"` → `0`), not merely absent of a match — this is exactly the `--all`-enumeration blind spot this review's own fast-follow finding 5 discloses, so their zero count here is not independent evidence they are clean, only that this scan cannot see them. Only `.shell-team/reviews/T-1143.md` is actually covered by this run, and its own 0 is a genuine scan result (present in the scan's tracked-plus-untracked-but-not-ignored population, confirmed via `git status --short .shell-team/reviews/`).

### QA verdict: PASS — T-1143 (round 2)

## Verification items run (round 2, against commit `6ea3345`)

- `bash bin/check-intent.sh .shell-team/specs/T-1143-handoff-shape-scan-step.md .shell-team/todo.md` → `aligned: T-1143 v1 (8348a4d48a63169ff9dc656f841eda21d68150f5)` — same hash as round 1; the engineer's round-2 change touched only mutable `## Notes for engineer` prose.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1143-handoff-shape-scan-step.md 2>&1 | tail -1` → `check-acs: 10 passed, 0 failed, 1 skipped, 0 unrecognized` — AC1–AC10 PASS, AC11 SKIP (runtime — no check:), independently re-run by me.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1143.md` → `conformant: .shell-team/provenance/T-1143.md (5 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1143 .shell-team/interventions/T-1143.md` → `conformant: .shell-team/interventions/T-1143.md (1 entries, 0 sentinel)`.
- `TD=$(bash bin/team-paths.sh --get todo); bash bin/check-handoff.sh "$TD"` → exit 0.
- `bash bin/check-board-headings.sh "$TD" --base "$(git merge-base develop HEAD)"` → exit 0.
- Non-goals: `git diff --name-only "$(git merge-base develop HEAD)"...HEAD | grep -E 'bin/check-pii-shapes.sh|workflows|CHANGELOG|MANIFEST|README'` → no output, exit 1 (none of these touched).

## CI-wired suites (own-run, round 2)

- `bash tests/check-prompt-sync/run.sh` → `All check-prompt-sync assertions passed.`
- `bash bin/check-prompt-sync.sh` (dogfood, both `bash bin/<script>` and PATH-bare-name forms) → `check-prompt-sync: all registered prompt blocks in sync` both ways.
- `bash tests/check-pii-shapes/run.sh` → `check-pii-shapes suite: all assertions passed` — all four `hand-off shape scan` labels PASS, including the new assertion (d).
- `bash bin/check-pii-shapes.sh --base "$(git merge-base develop HEAD)"` → `check-pii-shapes: clean (no PII-shaped bytes found)`.
- `shellcheck tests/check-pii-shapes/run.sh` → exit 0.
- Reverse-mapped reached suites, all exit 0: `bash tests/gen-playbook-blocks/run.sh`, `bash tests/machine-tokens/run.sh`, `bash tests/codex-skeleton-hygiene/run.sh`, `bash tests/check-invocation-path/run.sh`, `bash tests/codex-agents/run.sh`, `bash tests/derive-populations/run.sh`.

## Class A (Codex Major 1 — `$out`/`set -e` fragility) — independently re-verified

- Ran the literal fenced snippet (`out="$(mktemp)"` then `if PII_CHECK_TRACKER_KEY=1 bash "<plugin root>/bin/check-pii-shapes.sh" --all > "$out" 2>&1; then rc=0; else rc=$?; fi`) in a fresh `bash -e -u` shell against this checkout: `rc=1`, `$out` non-empty (160436 bytes). Confirmed the fixed form neither aborts nor silently succeeds.
- Reproduced the pre-fix failure modes myself, isolated from the checker: (a) `bash -e -u -c 'unset out; echo hi > "$out"'` → `out: unbound variable`, exit 1, before any scan runs; (b) `bash -e -u -c 'out="$TMPDIR/t.$$"; false > "$out" 2>&1; rc=$?; echo "rc=$rc"'` → aborts silently under `set -e` with no output at all when the command exits 1 (stand-in for the checker legitimately exiting 1 on a real finding) — exactly the defeat Codex described.
- All four consumer regions (`agents/engineer.md`, `agents/qa-verifier.md`, `agents/scrum-master.md`, `agents/codex-reviewer.md`) carry the corrected snippet byte-identically (`grep -n 'out=' <block region>` in each → `out="$(mktemp)"`, identical), and `bash bin/check-prompt-sync.sh` confirms sync mechanically.

## Class B (Codex Major 2 — missing accepted-noise carve-out) — independently re-verified

- Read both `docs/pii-controls.md` (lines 118–123) and `docs/pii-controls.ja.md` (lines 108–115) directly. Both now carry the accepted-noise carve-out sentence, inside the AC7-extracted region (before the next `## ` heading), stating the classification-not-edit-away disposition in substance identical to the canonical block's own bullet.
- AC5's zero-deletion numstat confirms no existing sentence was reworded in either file (0 deletions on both paths, independently re-run).
- Both languages read naturally as this file's own authored voice, not a mechanical translation.

## Class C (Codex Major 3 — vacuous negative control) — independently re-verified

- Read the rebuilt "hand-off shape scan" round in `tests/check-pii-shapes/run.sh` (lines 1739–1831) directly: `HOS_BASE` is captured immediately after `new_repo`, **before** `add_fixture_line` commits an innocuous version of the record — so `HOS_BASE != HEAD` and the record's path genuinely enters the `HOS_BASE..HEAD` diff. The shape-bearing edit is then written to disk and left uncommitted.
- Confirmed by direct code read (`bin/check-pii-shapes.sh:798-849`) that change-scoped mode's path enumeration is `git diff --no-color --no-renames --raw -z "$POINT" HEAD`, and content is read via `git cat-file -p "HEAD:$path"` — the **committed blob at HEAD**, never the working tree.
- Constructed and ran the engineer's defeat scenario myself, independently, on a disposable scratch repo under `$TMPDIR` (never committed to this tree): built the same repository shape, but **committed** the shape-bearing edit instead of leaving it uncommitted, then ran `PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --base "$BASE"` — result: `FINDING pattern=tracker-key path=uncommitted-record.md line=1`, `rc=1`. This confirms the corrected control's PASS on the shipped fixture is caused by the edit being uncommitted, not by an inherently empty diff.
- Assertions (a) and (c) each re-read for the same property and confirmed each still passes for its own stated reason (not a shared cause with (b)); assertion (d)'s wholly-untracked variant is honestly labeled as the weaker never-entered-any-diff mechanism, distinct from (b).
- No committed literal of the shape: `AC8`(b)'s own regex re-run over the diff's added lines (independently re-run as part of the AC8 check above) confirms no finished-shape literal in the added diff; direct read of the added lines confirms `HOS_NS1`/`HOS_NS2`/`HOS_NUM` fragment assembly, matching the suite's existing convention.

## Mutation self-checks (independently re-executed)

- **AC3**: mutated `agents/engineer.md`'s block region text (`never by quoting the bytes again` → `DELIBERATELY-MUTATED-TEXT`) → `bash bin/check-prompt-sync.sh` reported `drift in prompt-block 'record-shape-scan'`, exit 1. Restored from a saved copy: `diff` byte-identical, `git status --short` empty, `bash bin/check-prompt-sync.sh` reports `all registered prompt blocks in sync` again.
- **AC6**(b) assertion isolation: mutated the fixture's expected `rc` for assertion (b) from `0` to `99` → only that one assertion FAILed (`FAIL: hand-off shape scan: the same uncommitted record is invisible to the change-scoped default…`); assertions (a), (c) and (d) all still PASSed. Restored from a saved copy: `diff` byte-identical, `git status --short` empty, `bash tests/check-pii-shapes/run.sh` reports all assertions passed again including all four hand-off shape scan labels.
- The remaining eight mutation claims in the round-2 hand-off (AC1, AC2, AC4, AC5, AC7, AC8, AC9, AC10) were read against their target criteria and found internally consistent (each mutation targets the exact anchor/field/count the criterion checks); not independently re-executed by me this round — two of ten plus round 1's own two (AC1, AC3 in round 1) is the floor the spec's AC11(a) states, and round 1 already independently re-executed AC1 and AC3's mutations once each.

## Non-goals and scope lock

- `git diff --name-only "$(git merge-base develop HEAD)"...HEAD` (11 files touched this round, plus the round-1 file set) excludes `bin/check-pii-shapes.sh`, `.github/workflows/check-handoff.yml`, both CHANGELOG files, `manifest.json`/plugin manifest, and both READMEs — confirmed by the grep above returning no hits.
- AC10 (scope lock over the full branch changed-and-added set) PASSes mechanically, independently re-run.

## Edge cases tried (beyond the spec's own enumeration)

1. The mktemp-template quirk in this session's own sandboxed shell (bare `mktemp` with no template fails under `TMPDIR` isolation) — not a defect in the shipped block: every hand-off command in this task's own history uses a template (`mktemp "$TMPDIR/…XXXXXX"` or `mktemp -d`), and the block's own snippet (`out="$(mktemp)"`) is a bare call that works correctly on an unconstrained system (confirmed once `$TMPDIR` write access was available in this session). Recorded here as an environment quirk of this QA session's sandbox, not a finding against the diff.
2. Independently constructed the "committed defeat" scenario for AC6(b) (see Class C above) rather than trusting the engineer's own description of it.

Both are within the spec's declared verification scope; neither is a synthetic extreme outside `## Input space`.

## Verdict (round 2)

PASS. All three Codex Majors are independently confirmed fixed on live re-run and direct code/fixture inspection — Class A's fenced snippet now completes correctly under both failure modes Codex demonstrated, Class B's carve-out now appears in both adopter documents inside the AC7-checked region, and Class C's negative control now genuinely exercises the committed-blob read (confirmed by an independently constructed defeat scenario, not merely by re-reading the engineer's own account). AC1–AC10 all PASS live; AC11's items are consistent with round 1's audit, re-confirmed on the two directly re-executed mutation probes. No regression found. `- shape-scan:` line for this record and the board hand-off is on the board entry below.

### Codex Review verdict: REQUEST_CHANGES (round 2)

- Task: T-1143
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 14 files, +875/-0 lines (`git diff --stat d5884ff...HEAD`); round-2 rework scope: `git diff --stat 92d62ec...HEAD` — 12 files, +216/-21 lines

#### Blockers

1. `templates/prompt-blocks/record-shape-scan.md:6` — the round-2 fix (`out="$(mktemp)"`) is not portable and fails outright on this project's own primary development platform under a restricted temp directory. Empirically reproduced live, twice, in this exact session's default (unelevated) sandboxed shell — no special conditions invented:
   ```
   $ out="$(mktemp)"
   mktemp: mkstemp failed on /var/folders/.../T/tmp.XXXXXXXX: Operation not permitted
   $ echo "rc=$?"; echo "out=[$out]"
   rc=1
   out=[]
   ```
   On macOS/BSD (`mktemp` with no template argument), the binary does not consult `$TMPDIR` at all — it resolves the system's Darwin per-user temp directory directly and ignores the exported environment variable, confirmed with `TMPDIR=/tmp/claude-502 mktemp` failing identically. Two concrete consequences, both verified live: (a) under a plain shell, `out=""`, the subsequent redirect (`> "$out"`) fails with "no such file or directory", and the `if/then` structure the round-2 fix introduced still lands on the `else` branch with `rc=1` — indistinguishable, under the block's own stated contract two bullets later ("a `0` or a `1` is a scan that completed"), from a genuine completed-and-matched run; (b) under `bash -e -u` (a defensive mode this project's own suites use throughout), the entire snippet aborts silently at the `mktemp` assignment itself, before the `if` is ever reached, and produces no output at all. This is the identical safety-invariant defeat round 1's Major 1 already named ("a scan that did not complete must never read as an absence of findings") — round 2's fix moved the trigger from an unguarded `$out` reference to an unguarded `mktemp` call, but did not close the class, and this time the trigger is the *default* behavior of the platform this repository itself runs on (`CLAUDE.md`: `Platform: darwin`), not a hypothetical. It is escalated to Blocker rather than kept at Major (round 1's calibration for the sibling defect) because this round's own QA record shows the literal snippet was actually attempted for the first time and failed under the standard environment — QA's "Class A" section reports success only "confirmed once `$TMPDIR` write access was available in this session," an elevation QA does not otherwise justify as representative, and separately documents the identical failure ("mktemp: mkstemp failed…") as an "edge case," dismissed as "not a defect in the shipped block… works correctly on an unconstrained system" without re-testing whether the *default*, unconstrained-by-design session is itself the constrained one. Fix: use a portable explicit template (`out="$(mktemp "${TMPDIR:-/tmp}/pii-scan.XXXXXXXX")"`) and treat allocation failure as its own guarded, loud path (e.g. `rc=2` semantics) rather than falling through to the checker's own exit-code branch.

#### Major

2. `templates/prompt-blocks/record-shape-scan.md:6-8` — even granting a successful `mktemp`, the fenced snippet emits nothing: neither `$out`'s path, its contents, nor `$rc` is printed before the block ends. This project's own Bash execution model does not share shell state across separate invocations (stated plainly in this very role's tool description: "The working directory persists between commands, but shell state does not"), and the block's very next bullet ("Judge the filtered lines... Read the `FINDING pattern=<id> path=<path> line=<n>` lines") presumes the executor can still see the scan's output — which, the moment the fenced snippet and that reading step run as two separate calls, they cannot. Both of this round's live hand-offs worked around this by inventing their own single, self-contained command combining the scan and the `grep` filter (`$D/scan.txt` idiom) rather than running the shipped snippet as shown — precisely the workaround pattern round 1's own Minor 4 already noted for a different reason (the summary line's counts). The shipped exemplar itself still does not demonstrate a self-contained, working form. Kept at Major rather than Blocker: it degrades usability and reproducibility rather than by itself producing a false-clean result (that compound failure is Blocker 1, above), and both live hand-offs this round again avoided it by substitution. Fix: fold the emission into the same fenced block (e.g. append `echo "rc=$rc"; cat "$out"` inside the same code fence) so the block is a single, self-contained, copy-pasteable unit that survives being run as one call.

3. `.shell-team/specs/T-1143-handoff-shape-scan-step.md:265` — the spec's own "proposed wording for the canonical block" (`## Notes for engineer`, introduced at line 258) still quotes the **pre-round-1-fix** snippet verbatim — `PII_CHECK_TRACKER_KEY=1 bash "<plugin root>/bin/check-pii-shapes.sh" --all > "$out" 2>&1; rc=$?` — the exact undefined-`$out`, no-`if`-guard form round 1 flagged as Major and the engineer corrected in the shipped canonical block. Round 2's own amendment (the paragraph beginning "Amended round 2 (Codex REQUEST_CHANGES, Major 3…)" immediately below, at line 286) rewrites the sibling fixture-recipe bullets to reflect the round-2 fix, but never touches this blockquote a few lines above it, so the spec now visibly contradicts its own shipped, corrected artifact — precisely the "a later append contradicting an earlier line that no gate re-reads" defect class this task's own originating retro exists to catch, in the spec that ships this very mechanism. Mitigated somewhat by line 258's own disclaimer ("Treat it as a starting draft, not as text to paste unread"), which is why this is Major rather than Blocker — but a stale draft sitting uncorrected two review rounds after its content was fixed elsewhere is exactly the kind of drift that disclaimer cannot fully protect against for a future reader who trusts the "Notes for engineer" section as a shortcut. Fix: update the blockquote at lines 260-271 to the current shipped form (or replace it with a pointer to the canonical block file, since it is now a second copy of text that has already drifted once).

#### Minor / nits

- Carried from round 1, still unaddressed (tracked as fast-follow finding 6 there, re-confirmed independently this round by the adversarial pass): the block does not require a fresh re-run of the scan after fixing a hit, before quoting the hand-off's summary line, nor after the final hand-off draft is written. Not escalated: neither hand-off this round reported an unfixed real quotation.
- New this round (adversarial pass, native P2): a successful run of the block's snippet leaves its `$out` temp file on disk, neither read nor removed by the block itself — a minor cleanup gap, not a correctness defect (the file carries no committed content and sits in the executor's own ephemeral temp area).
- Round 1's remaining three Minors (7, 8, 9) are unchanged by this round's diff; carried forward unmodified below.

#### Adversarial findings

Triggered by the Blocker/Major findings above, against the same file set as round 1 plus the spec. The adversarial pass independently reproduced both of the primary pass's findings by running the literal snippet itself in this environment (confirming the mktemp failure under `bash -e -u` and the plain-shell `rc=1` misclassification), and surfaced one new finding on its own: the spec's stale pre-fix snippet at `.shell-team/specs/T-1143-handoff-shape-scan-step.md:265` (folded into Major 3). It also independently found the leftover-temp-file hygiene gap (folded into Minor, above) and re-raised the missing re-run-after-fix step (already tracked from round 1).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Bare `out="$(mktemp)"` fails on macOS/BSD under a restricted temp dir (no template, ignores `$TMPDIR`); failure is silently misclassified as `rc=1`, a "completed" scan under the block's own contract | blocker (Codex primary, native P1; corroborated by adversarial, native P1) | blocker | Held at the map's native ceiling rather than downgraded to major (round 1's calibration for the sibling `$out`-undefined defect): unlike round 1, this exact failure was empirically triggered by me in this session's own default, unelevated sandbox and separately experienced (and dismissed) by this round's own QA pass, so "not exercised this round" no longer holds |
| 2 | Scan result (`$out`, `rc`) never emitted inside the fenced snippet; cannot survive a second, separate Bash invocation reading the next bullet's instructions | major (Codex primary, native P1) | major | Kept at major, not escalated to blocker: this defect degrades usability/reproducibility rather than by itself producing a false-clean result; both live hand-offs this round avoided it via their own substitute idiom |
| 3 | The spec's own `## Notes for engineer` "proposed wording" blockquote at line 265 still quotes the pre-round-1-fix snippet, contradicting the shipped, corrected canonical block; round 2's amendment touched the sibling fixture bullets but missed this one | major (Codex adversarial, native P1) | major | Not escalated to blocker: line 258's own disclaimer ("not text to paste unread") mitigates direct reintroduction risk, but the contradiction is real and matches this task's own originating defect class |
| 4 | No explicit re-run-after-fix / after-final-draft step | minor (Codex adversarial, native P2) | minor | Unchanged from round 1's disposition (already tracked as fast-follow finding 6); re-confirmed, not newly weighted |
| 5 | `$out` temp file leaked (never read or removed) on a successful run | minor (Codex adversarial, native P2) | minor | New this round; a hygiene gap, not a correctness defect — the leaked file carries no committed content |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG` with the Blocker and two Major findings above: (1) the canonical block's `mktemp` call is not portable and fails on this project's own default macOS/darwin sandbox, silently misclassified as a completed scan; (2) the same block's snippet never emits its own result, so it cannot be read across a separate invocation; (3) the spec's own "Notes for engineer" still quotes the pre-round-1-fix snippet, contradicting the shipped, corrected block.

#### Fast-follow disposition

- Round 1, finding 5 (codex-reviewer's own gitignored raw captures invisible to `--all`) — file-an-issue — unchanged from round 1; rationale carried forward verbatim: a real, reproducible gap in `bin/check-pii-shapes.sh`'s `--all` enumeration relative to this project's own gitignore patterns for review-record raw captures, out of this task's scope lock but concrete and actionable enough to file.
- Round 1, finding 6 / this round's Minor (no explicit re-run-after-fix step) — file-an-issue — unchanged from round 1, now independently re-raised by this round's own adversarial pass; rationale carried forward: a real ordering ambiguity in a shipped, adopter-facing canonical block that could produce a stale hand-off summary in a future round where an actual quotation needs fixing.
- Round 1, finding 7 (discretionary-sounding doc wording) — won't-fix — unchanged; rationale carried forward: the canonical block (the operative mechanism) is unambiguous and imperative; the docs are a summary rather than the operative instruction.
- Round 1, finding 8 (DP4's second exclusion ground omitted from the adopter docs) — won't-fix — unchanged; rationale carried forward: the full DP4 rationale is already disclosed in the frozen spec and this task's Non-goals; no adopter action depends on knowing the second exclusion reason.
- Round 1, finding 9 (manual count derivation for the `- shape-scan:` line) — won't-fix — unchanged; rationale carried forward: both hand-offs in both rounds demonstrated correct, independently-reproducible derivations.
- This round's new Minor (leaked `$out` temp file on success) — won't-fix — rationale: the file carries no committed or sensitive content and sits in the executor's own ephemeral temp area, cleaned by the OS/session in the ordinary course; not worth a dedicated tracking item on its own, and the fix for Major 2 (folding emission into the same fenced block) is a natural place to also add a `rm -f "$out"` if a future round chooses to.

#### Per-pass record fields

- executor-invocation (codex-primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary-r2): generation
- briefing-fidelity (codex-primary-r2): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (codex-primary-r2): T-1143-codex-primary-r2
- self-detected-host (codex-primary-r2): claude-code — CODEX_THREAD_ID was not set in this shell before any launch
- executor-invocation (codex-adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on the diff between develop and HEAD in this repo, focused specifically on these files: templates/prompt-blocks/record-shape-scan.md, docs/pii-controls.md, docs/pii-controls.ja.md, agents/engineer.md, agents/qa-verifier.md, agents/scrum-master.md, agents/codex-reviewer.md, tests/check-pii-shapes/run.sh, .shell-team/specs/T-1143-handoff-shape-scan-step.md. Read the files yourself with git diff / nl -ba... [round-2 findings and defeat-scenario framing, full text in T-1143-codex-adversarial-r2.jsonl] ... Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <RAW_JSONL> 2>&1
- pass-role (codex-adversarial-r2): generation
- briefing-fidelity (codex-adversarial-r2): carried — the primary pass's own two confirmed findings (mktemp portability, unemitted scan result), plus a request to verify or refute them by literal execution, were written directly into this pass's prompt, along with an open-ended devil's-advocate mandate over the same file set plus the spec
- raw-capture (codex-adversarial-r2): T-1143-codex-adversarial-r2
- self-detected-host (codex-adversarial-r2): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

#### Record shape scan (own record, this pass)

- shape-scan: rc=1 — 1 record path(s) written this round (`.shell-team/reviews/T-1143.md`) — 0 finding(s) on it, 0 classified accepted noise — command: `D=$(mktemp -d "$TMPDIR/t1143codexr2shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=\.shell-team/reviews/T-1143\.md ' "$D/scan.txt" | wc -l`. Disclosure: the four raw-capture paths this round (`T-1143-codex-primary-r2.{txt,jsonl}`, `T-1143-codex-adversarial-r2.{txt,jsonl}`) are gitignored and confirmed absent from the whole scan's output entirely (`grep -c "T-1143-codex" "$D/scan.txt"` → `0`), the same `--all`-enumeration blind spot round 1's own fast-follow finding 5 discloses — their absence here is not evidence they are clean, only that this scan cannot see them.

### QA verdict: PASS — T-1143 (round 3)

## Verification items run (round 3, against commit `5d0a01e`)

- `bash bin/check-intent.sh .shell-team/specs/T-1143-handoff-shape-scan-step.md .shell-team/todo.md` → `aligned: T-1143 v1 (8348a4d48a63169ff9dc656f841eda21d68150f5)` — same hash as rounds 1-2; round 3 touched only mutable `## Notes for engineer` prose (confirmed by direct diff: the only removed spec text is the stale blockquote, and no byte inside the `<!-- BEGIN/END intent-block: T-1143 -->` markers changed).
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1143-handoff-shape-scan-step.md 2>&1 | tail -3` → `check-acs: 10 passed, 0 failed, 1 skipped, 0 unrecognized` — AC1-AC10 PASS, AC11 SKIP, independently re-run by me against the committed tree.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1143.md` → `conformant: .shell-team/provenance/T-1143.md (9 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1143 .shell-team/interventions/T-1143.md` → `conformant: .shell-team/interventions/T-1143.md (1 entries, 0 sentinel)`.
- `TD=$(bash bin/team-paths.sh --get todo); bash bin/check-handoff.sh "$TD"` → exit 0.
- `bash bin/check-board-headings.sh "$TD" --base "$(git merge-base develop HEAD)"` → exit 0.
- Non-goals: `git diff --name-only "$(git merge-base develop HEAD)"...HEAD | grep -E 'bin/check-pii-shapes.sh|workflows|CHANGELOG|MANIFEST|README|manifest.json'` → no output, exit 1 (none touched, cumulative diff).
- `git status --short` → empty throughout this round's verification (all mutation probes restored byte-identically before commit).

## CI-wired suites (own-run, round 3)

- `bash tests/check-prompt-sync/run.sh` → `All check-prompt-sync assertions passed.`
- `bash bin/check-prompt-sync.sh` → `check-prompt-sync: all registered prompt blocks in sync`.
- `bash tests/check-pii-shapes/run.sh` → `check-pii-shapes suite: all assertions passed` — all four `hand-off shape scan` labels PASS, unchanged from round 2 (this round touched no fixture line — confirmed: `git diff --stat 6ea3345..5d0a01e -- tests/check-pii-shapes/run.sh` produces no output).
- `bash bin/check-pii-shapes.sh --base "$(git merge-base develop HEAD)"` → `check-pii-shapes: clean (no PII-shaped bytes found)`.
- `shellcheck tests/check-pii-shapes/run.sh` → exit 0. `shellcheck bin/check-pii-shapes.sh` → exit 0 (untouched, but part of the workflow's shellcheck argument list — CI parity).
- Reverse-mapped reached suites, mechanically derived (`grep -rl -- <path> tests/*/run.sh` for each of the 14 files this branch's cumulative diff touches, plus the workflow's own steps naming a path): `tests/check-invocation-path/run.sh`, `tests/codex-skeleton-hygiene/run.sh`, `tests/codex-agents/run.sh` (from `agents/codex-reviewer.md`); `tests/check-prompt-sync/run.sh`, `tests/gen-playbook-blocks/run.sh` (from `agents/engineer.md`, `templates/prompt-blocks/registry.txt`); `tests/machine-tokens/run.sh` (from `agents/scrum-master.md`); `tests/derive-populations/run.sh` (from `tests/check-pii-shapes/run.sh`, comment-only reference, run anyway) — all six run fresh this round, all exit 0. No reverse-mapped hit for `docs/pii-controls.md`/`.ja.md` or `templates/prompt-blocks/record-shape-scan.md` (no `tests/*/run.sh` names either path).

## Item 2 — the block followed literally, as a role would, in this sandbox

Read `templates/prompt-blocks/record-shape-scan.md` and executed its first bullet exactly as written, choosing (as the prose explicitly leaves to the role) `mktemp -d "$TMPDIR/qa1143-role-run.XXXXXX"` for the scratch directory (a bare `mktemp` is refused in this sandbox — reproduced live: `mktemp` alone → `mktemp: mkstemp failed on /var/folders/.../T/tmp.XXXXXXXX: Operation not permitted`, exit 1 — confirming Blocker 1's platform fact independently, one more time, on this session). Ran `PII_CHECK_TRACKER_KEY=1 bash "$PWD/bin/check-pii-shapes.sh" --all` with `<plugin root>` substituted for the checkout root, redirected stdout+stderr into a file inside that directory, read the file back (`wc -c` confirmed non-empty, 160438 bytes), and recorded `rc=1` — a scan that completed, per the block's own contract. `grep -c '^\s*```'` on the block file returns `0` (no fenced code block remains). The prose requires no invention beyond the file-creation choice it explicitly delegates; every other step (capture target, exit-status classification, `path=` filter, disposition, summary-line grammar) is stated as an executable instruction, not left to guesswork.

Also ran the exact form printed in both adopter documents (`PII_CHECK_TRACKER_KEY=1 bin/check-pii-shapes.sh --all`, no `bash` prefix, matching this doc's own pre-existing `## Running it` convention) from the checkout root: `rc=1`, identical population. Confirmed all four consumer regions are byte-identical to the canonical file (`bash bin/check-prompt-sync.sh` green, plus a direct `diff` of the extracted marker region in `agents/scrum-master.md` against the canonical file — byte-identical).

## Item 3 — Blocker 1 / Major 2 closure as a class: every inline command enumerated

- `templates/prompt-blocks/record-shape-scan.md` and its four byte-identical consumer regions: the only executable command is `PII_CHECK_TRACKER_KEY=1 bash "<plugin root>/bin/check-pii-shapes.sh" --all` (inline code inside prose, not a fenced snippet). Ran it (with `<plugin root>` substituted) under `bash -e -u`: completes cleanly, `rc=1`, no abort. Every other backtick span in the block (`--all`, `mktemp`, `2`, `0`, `1`, `FINDING pattern=<id> path=<path> line=<n>`, `path=`, the `- shape-scan: rc=<rc> — …` grammar template) is prose naming a value, a flag or a report grammar — none is a command fragment presented as runnable.
- `docs/pii-controls.md` / `docs/pii-controls.ja.md`, `## When the loop runs it` / `## ループがこれを走らせる時点` region: the only command is `PII_CHECK_TRACKER_KEY=1 bin/check-pii-shapes.sh --all`, identical in both languages, matching the doc's own pre-existing `## Running it` style (relative path, no `bash` prefix, executable bit already confirmed set). Ran it from the checkout root: `rc=1`, completes cleanly.
- No copyable command fragment anywhere in these six files fails as written; the class Blocker 1 (portability) and Major 2 (unemitted result) named is closed by removing the only place a fragile fenced fragment existed, not by patching that fragment a fourth time — consistent with the provenance record's own stated reasoning.

## Item 4 — Major 3 closure

`.shell-team/specs/T-1143-handoff-shape-scan-step.md`'s `## Notes for engineer` no longer carries any copy of the block's text: `grep -n '\$out'` over the whole spec returns no output (previously present at the stale blockquote). The blockquote is replaced with "**The canonical block's shipped text.** The shipped `templates/prompt-blocks/record-shape-scan.md` is the canonical text and this note carries no second copy of it…", pointing at the shipped file and the provenance record. Read `## Decision points, resolved` (DP1-DP6) and every Gotchas bullet directly: the remaining `mktemp`/`out=`/`rc=$?` occurrences in the file are all inside `- check:` lines (AC4/AC5/AC6/AC7/AC8/AC10's own instrumentation, using the templated `${TMPDIR:-/tmp}/…XXXXXX` form) or the fixture-recipe bullet "Create the repository with `new_repo` — never with a bare `mktemp`" — none is a restatement of the block's own invocation, and none contradicts the shipped, twice-corrected block. No remaining sentence contradicts the shipped block.

## Item 5 — adopter docs, clause by clause; insertion-only

Read both new paragraphs end to end (both quoted in full above during the run). Round 3 adds one paragraph to each document, immediately after the existing accepted-noise sentence, stating: (a) the did-not-complete contract (a redirect/file-creation error or exit `2` is a scan that did not complete and is never read as an absence of findings), and (b) the run-after-fix ordering (the quoted `- shape-scan:` line is always from a run made after any fix, never the run that found the hit). Confirmed both clauses present, in substance-equivalent EN/JA wording, inside the AC7-checked region (before the next `## ` heading). `diff` against the round-2 commit (`git show 6ea3345:docs/pii-controls.md` vs. HEAD, same for `.ja.md`) shows a pure 6-7 line insertion in each file, no line removed or reworded — matches AC5's own zero-deletion numstat, independently re-run: 8 numstat rows, all deletion columns `0`, all insertion columns ≥1. AC7 headings/anchors independently re-confirmed live (`AC7: PASS`).

## Item 6 — mutation self-checks (AC1, AC3), independently re-executed this round

- **AC1**: mutated the `fixed in place` anchor in `templates/prompt-blocks/record-shape-scan.md` (`fixed in place` → `FIXED-IN-PLACE-MUTATED`) → `AC1: FAIL (exit 1)`. Restored from a saved copy: `diff` byte-identical, `git status --short` empty, `AC1: PASS (exit 0)` again.
- **AC3**: mutated `agents/engineer.md`'s block region (`never by quoting the bytes again` → `QA-MUTATED-TEXT-PROBE`) → `bash bin/check-prompt-sync.sh` reported `drift in prompt-block 'record-shape-scan'`, `AC3: FAIL (exit 1)`. Restored from a saved copy: `diff` byte-identical, `git status --short` empty, `bash bin/check-prompt-sync.sh` reports `all registered prompt blocks in sync` again, `AC3: PASS (exit 0)`.
- The remaining eight mutation claims (AC2, AC4-AC10) are unchanged from rounds 1-2's own independent re-execution and description audits; this round's diff does not touch any of their target files' checked properties (registry row, consumer-set exclusions, numstat population beyond the two already-verified paths, fixture labels, provenance/board schema, scope-lock allow-list), so no new mutation probe is owed against them this round.

## Item 7 — independent re-derivation of the opt-in scan split, and the self-caught quote

`PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all` run fresh by me, filtered to the round's 10 touched files:

- Total findings on the 10 files (`grep -E 'path=(...) '`): **193** — matches the hand-off's figure exactly (re-derived, not copied: `D=$(mktemp -d "$TMPDIR/qa1143-item7.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=(templates/prompt-blocks/record-shape-scan\.md|agents/engineer\.md|agents/qa-verifier\.md|agents/scrum-master\.md|agents/codex-reviewer\.md|docs/pii-controls\.md|docs/pii-controls\.ja\.md|\.shell-team/specs/T-1143-handoff-shape-scan-step\.md|\.shell-team/provenance/T-1143\.md|\.shell-team/todo\.md) ' "$D/scan.txt" | wc -l` → `193`).
- Per-path breakdown, each counted separately: `templates/prompt-blocks/record-shape-scan.md` 0, `agents/engineer.md` 0, `agents/qa-verifier.md` 1, `agents/scrum-master.md` 0, `agents/codex-reviewer.md` 1, `docs/pii-controls.md` 0, `docs/pii-controls.ja.md` 0, `.shell-team/specs/T-1143-handoff-shape-scan-step.md` 2, `.shell-team/provenance/T-1143.md` 0, `.shell-team/todo.md` 189 — identical to the hand-off's own breakdown, independently re-derived rather than copied.
- Confirmed the two `pattern=tracker-key` hits at `.shell-team/specs/…md:96` and `:112` are the same `## Input space` standards-identifier lines outside `## Notes for engineer` (lines 241-320, the region this round amended) — direct read of both lines confirms accepted noise, no new hit introduced by this round's edit. Confirmed `agents/qa-verifier.md:81` and `agents/codex-reviewer.md:3` sit outside the marker regions (175-183, 241-249) and are pre-existing accepted-noise vocabulary (a task-id-shaped example token and a model-family identifier, each matching this project's own design-point/standards-identifier accepted-noise classes), read directly and reported here by class rather than by quoting the matched bytes.
- Confirmed the self-caught quote is gone: `grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | awk -F'line=' '{print $2}' | awk '$1>=85 && $1<=113'` → no output — zero findings inside this round's own added board range (lines 85-113), matching the hand-off's own re-scan claim that the round-3-self-caught hit at line 99 was fixed in place and confirmed absent on re-run.

## AC11 audit, item by item (round 3)

- (a) **Mutation self-check**: AC1 and AC3 independently re-executed this round (above); the remaining eight are carried, consistent with round 1-2's own audits and this round's untouched-target reasoning.
- (b) **Execution-context matrix**: independently re-ran the block's prescribed invocation as `bash "$PWD/bin/check-pii-shapes.sh" --all` (absolute path form), as `bash bin/check-pii-shapes.sh --all` (relative, checkout-root form), and with this checkout's `bin/` prepended onto `PATH` and the bare script name (`PII_CHECK_TRACKER_KEY=1 check-pii-shapes.sh --all`) — identical `rc=1` in all three forms. Confirmed.
- (c) **CI-equivalence**: independently reverse-mapped (see CI-wired-suites section above) and re-ran; matches the hand-off's claimed reached set exactly.
- (d) **Full-population downstream inventory**: unchanged disposition from rounds 1-2 — deferred to the release sweep under this checkout's disclosed standing local operating policy, not silently skipped. Not re-litigated this round since the disclosure and its authority are unchanged.
- (e) **Plural-corpus read**: unchanged; the round-3 rewrite did not alter any governed noun's number. Confirmed by direct read of the rewritten block: "record path(s)", "finding(s)", the zero-file-round sentence, all still present.
- (f) **PII-shape self-check before first commit**: this round's own `- shape-scan:` board line and its command independently re-run by me (item 7, above) — reproduces exactly.
- (g) **Norm-text read**: read the rewritten block against `record-portability.md:5`'s "not a detector" sentence and against this spec's own Goal/AC text — no contradiction; `record-portability.md` confirmed byte-identical to base (AC5, independently re-run). Also confirms the specific contradiction Major 3 named (the spec's own Notes quoting a superseded snippet) is gone — the norm text and the shipped mechanism now agree.

## Non-goals and scope lock (round 3)

- `git diff --name-only "$(git merge-base develop HEAD)"...HEAD | grep -E 'bin/check-pii-shapes.sh|workflows|CHANGELOG|MANIFEST|README|manifest.json'` → no output, exit 1 (none touched, cumulative branch diff).
- AC10 (scope lock over the full branch changed-and-added set) PASSes mechanically, independently re-run.
- AC5's numstat (8 rows, 0 deletions, ≥1 insertion each) independently re-run against the cumulative branch diff — unchanged shape from round 2, confirmed again this round since the round-3 diff itself, while containing deletions inside already-added lines (rewriting the block's own prose), nets to zero deletions against the base ref across all eight paths.

## Edge cases tried (round 3, beyond the spec's own enumeration)

1. Reproduced the bare-`mktemp` platform failure independently, a second time, in this exact QA session's own sandboxed shell (the same fact Blocker 1 named and the round-3 hand-off's own live role-shaped run reproduced) — not a defect in the shipped text, since the block's prose explicitly delegates the file-creation mechanism to the role rather than prescribing an incantation.
2. Ran the doc's own simplified invocation form (`bin/check-pii-shapes.sh --all`, no `bash` prefix) literally, confirming it matches the pre-existing `## Running it` convention in the same file and executes correctly with the checked-in executable bit.

Both within the spec's declared Input space (class 7, both supported invocation forms), not synthetic extremes.

## Verdict (round 3)

PASS. Blocker 1 (mktemp portability), Major 2 (unemitted scan result) and Major 3 (stale spec blockquote) are all independently confirmed closed by design-premise change rather than patch: the fenced code sample is removed entirely from the canonical block, the invocation and its capture/exit contract are stated in prose with the file-creation mechanism explicitly left to the role, and the stale spec draft is replaced with a pointer to the shipped file. Live role-shaped execution of the shipped prose, in this sandbox with no elevated access, confirms the prose is executable exactly as written with no residual invention beyond the delegated file-creation choice, and that the platform fact Blocker 1 named (bare `mktemp` refused here) no longer has anywhere to bite, because there is no prescribed incantation left. AC1-AC10 all PASS live; AC11's items are consistent with rounds 1-2's audit, re-confirmed on the two directly re-executed mutation probes this round. No regression found in the four role files, the registry, the fixture suite (unchanged, confirmed untouched), or either adopter document. `- shape-scan:` line for this record and the board hand-off is on the board entry below.

### Codex Review verdict: REQUEST_CHANGES (round 3)

- Task: T-1143
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 14 files, +1068/-0 lines (`git diff --stat d5884ff...HEAD`); round-3 rework scope: `git diff --stat 631a420...HEAD` — 12 files, +175/-60 lines

#### Blockers

None.

#### Major

1. `tests/check-pii-shapes/run.sh` — the "hand-off shape scan" fixture's fourth assertion (`HOS_UT_*` prefix, "a wholly untracked record") depends on `bin/check-pii-shapes.sh --all`'s internal `git ls-files -z --others --exclude-standard` (`bin/check-pii-shapes.sh:744`) to enumerate the untracked record — and that call honors whatever `core.excludesFile` is active in the ambient git environment, which `new_repo()` never pins. Reproduced empirically on a disposable scratch repo (a per-repo `core.excludesFile` pointed at a file matching `*.md`, the same mechanism as a contributor's real global excludes): the untracked record becomes invisible to `git ls-files --others --exclude-standard` outright, before `check-pii-shapes.sh` ever runs. A contributor whose own global excludes match `*.md` (a plausible, unremarkable choice for scratch notes) or the literal filename `uncommitted-record.md` would see this specific assertion fail locally while this repository's CI stays green (`.shell-team/test-recipe.md`'s own words: runners carry no global excludes, "the least useful direction for the discrepancy to run"). This is exactly the class `.shell-team/test-recipe.md`'s "Environment quirks" section already names and requires pinning against — "Assertions about ignore behavior must pin the excludes file... Pin it with `git -c core.excludesFile=…`" — with two existing precedents in this very codebase (`tests/rollup-track/run.sh`, `tests/gitignore-raw-dumps/run.sh`). Neither precedent's exact `-c` idiom transfers unmodified here, because those two call `git check-ignore` directly while this fixture calls `bash "$BIN" --all` (a wrapping script whose own internal `git` calls do not inherit a `-c` flag given to the outer `bash` invocation) — the fix has to pin the *repository's own* `core.excludesFile` config (e.g. `git -C "$HOS_UT_REPO" config core.excludesFile /dev/null` immediately after `new_repo`, or an equivalent env-level override that this project's own maintainers judge propagates correctly to `check-pii-shapes.sh`'s internal git calls), not merely copy the `-c`-on-`git-check-ignore` idiom verbatim. Not a `bin/check-pii-shapes.sh` behavior question (Non-goal #1 / Out-of-scope item 2) — the checker's own enumeration logic is unchanged and correct; the defect is this task's own new fixture code failing to control an ambient input the project's own documented convention already says must be controlled. Assertions (a)–(c) are unaffected (round 2's rework moved them onto tracked-and-locally-modified files, read via `git cat-file -p HEAD:<path>`, which does not consult excludes at all).

#### Minor / nits

- `templates/prompt-blocks/record-shape-scan.md:4` — the "Judge the filtered lines" bullet narrows to files "you wrote this round," and the summary-line bullet narrows to "record path(s) **written** this round," while the block's own opening bullet scopes the whole step to files "you wrote **or edited** this round." A role reading only the later bullets in isolation could underscope the filter and the count to exclude an edited-but-not-newly-created record. Not escalated: the opening bullet's broader scope is stated first and a reader who reads the whole (five-bullet) block would reasonably carry it forward; no round's actual hand-off this task produced under-scoped its own count. Fix: say "wrote or edited" consistently in all three places.
- `templates/prompt-blocks/record-shape-scan.md:2` — "`--all` reports every path in the tree" overstates the mode's actual reach: `--all` enumerates tracked-plus-untracked-but-**not-ignored** paths (confirmed by direct code read, `bin/check-pii-shapes.sh:744`), not literally every path — a gitignored record (this very review's own `.txt`/`.jsonl` raw captures, per `.gitignore:15-17`) is invisible to it regardless of content. This is the same underlying gap round 1's fast-follow finding 5 already discloses and carries as file-an-issue, but that finding was framed around `bin/check-pii-shapes.sh`'s enumeration behavior (correctly placed out of this task's scope); this is the narrower, in-scope observation that the *block's own prose*, which this task does ship, overclaims coverage it does not have. Fix: replace "every path in the tree" with "every tracked or untracked-but-not-ignored path" or similarly precise wording — a pure textual correction, touching no other file.
- `templates/prompt-blocks/record-shape-scan.md:3` and both adopter docs (`docs/pii-controls.md`, `docs/pii-controls.ja.md`, the paragraph round 3 added) — the did-not-complete contract distinguishes "a redirect or file-creation failure" from "an exit status of `0` or `1`" as separate categories, but does not spell out *how* a role tells them apart when a failed redirect can itself leave the compound command's `$?` at `1` (the same numeric value a genuine, completed, one-finding scan produces). In practice this is very likely caught anyway, since the same bullet also requires "a real file you own and can read back" — a redirect/file-creation failure typically leaves no file to read back at all, so the read-back step (independently demonstrated this round by both QA's and my own literal execution of the block) supplies the operational distinction the prose itself does not spell out. Not escalated to Major: no live round's execution has actually produced this ambiguous case, and the read-back requirement is a real, already-present mitigation, not a hypothetical one this finding invents. Fix (optional, cheap): add one clause naming the read-back step as the operative test for "did not complete" rather than the exit code alone.
- Carried from round 1 as still open (finding 7, discretionary-sounding doc wording) and finding 8 (DP4's second exclusion ground) — unchanged this round; see Fast-follow disposition below.

#### Adversarial findings

Triggered by the Major finding above (the fixture's git-excludes fragility), run against the same file set as prior rounds. The adversarial pass independently corroborated the excludes-fragility finding on its own reading (native minor; escalated to major here — see ledger) and surfaced three further points, all folded into the Minor list above after my own review: the "wrote" vs. "wrote or edited" scoping drift, the "--all reports every path in the tree" overclaim against the gitignore blind spot, and the redirect-failure-vs-exit-1 classification ambiguity (raised both against the canonical block and, in substance-identical form, against both adopter docs). No new Blocker.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `tests/check-pii-shapes/run.sh`'s "wholly untracked record" assertion depends on `git ls-files --others --exclude-standard`, which honors the ambient `core.excludesFile`; unpinned, against this project's own documented convention and two existing precedents | major (Codex primary, native P2); minor (Codex adversarial, native) | major | Held at major, the primary pass's own native level, rather than the adversarial pass's lower native label: I independently reproduced the failure on a disposable scratch repo before either pass ran, confirming it is a real, reproducible defect against a documented project convention with existing precedent code showing the correct pattern — not a hypothetical. Not escalated to blocker: it does not affect this project's own CI (runners carry no global excludes) or any of this round's actual test runs (none of engineer's, QA's or my own environment happens to carry a matching exclude), so unlike round 3's own Blocker 1 (which fired in this project's *default* unelevated environment), this defect is latent for a subset of possible contributor environments rather than already-observed here |
| 2 | The block's own bullets narrow "wrote or edited" to "wrote" in two later places | minor (Codex adversarial, native major) | minor | Downgraded from the adversarial pass's native "major": the block's opening bullet states the broader scope first, and no round's actual hand-off under-scoped its count in practice; a real textual inconsistency, not a demonstrated functional defect |
| 3 | "`--all` reports every path in the tree" overstates coverage relative to gitignored paths | minor (Codex adversarial, native major) | minor | Downgraded: the underlying gap is already disclosed and out-of-scope per round 1's fast-follow finding 5 (a `bin/check-pii-shapes.sh` behavior question); only the prose's overclaim is newly in-scope here, and it is a wording precision issue, not a new functional gap |
| 4 | Redirect/file-creation failure vs. a genuine exit-1 scan are not mechanically distinguished by exit code alone | major (Codex adversarial, native, raised against both the block and both adopter docs) | minor | Downgraded: the block's own "read that file back" requirement is a real, already-present operational check that would catch a redirect/file-creation failure in the overwhelming majority of realistic executions (no file to read back), and no live round's execution has produced this ambiguous case; kept as minor rather than dismissed, since the prose does not spell this mitigation out explicitly |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG` with the one Major finding above: `tests/check-pii-shapes/run.sh`'s "wholly untracked record" assertion must pin the scratch repository's `core.excludesFile` (or an equivalent control) so the assertion's outcome does not depend on the ambient contributor environment, per this project's own documented convention.

#### Fast-follow disposition

- Round 1, finding 5 (codex-reviewer's own gitignored raw captures invisible to `--all`) — file-an-issue — carried forward, rationale unchanged; sharpened this round by this record's own Minor 2 above (the block's prose overclaims `--all`'s coverage relative to this exact gap) — worth folding into the same issue as an additional line item rather than filing separately.
- Round 1, finding 6 (no explicit re-run-after-fix step) — **retire — resolved in-band**. Round 3's rework added the clause directly to the shipped block ("Quote the run's summary in your hand-off as one line, from a run made after any fix") and to both adopter documents ("The `- shape-scan:` line quoted in the hand-off is always from a run made after any hit on the role's own file has been fixed, never from the run that found it"). No issue needed; the gap this finding named no longer exists in the shipped text.
- Round 1, finding 7 (discretionary-sounding doc wording, "of their own accord" / "自ら走らせる") — won't-fix — unchanged; rationale carried forward: the canonical block is unambiguous and imperative, the docs are a summary.
- Round 1, finding 8 (DP4's second exclusion ground omitted from the adopter docs) — won't-fix — unchanged; rationale carried forward: fully disclosed in the frozen spec's Non-goals, no adopter action depends on it.
- Round 1, finding 9 (manual count derivation for the `- shape-scan:` line) — won't-fix — unchanged; rationale carried forward: both hand-offs across all three rounds demonstrated correct, independently-reproducible derivations.
- Round 2's new Minor (leaked `$out` temp file on success) — **retire — moot by design-premise change**. The fenced snippet that could leak a file named `$out` no longer exists; file-creation and cleanup are now the role's own delegated discretion ("not a specific `mktemp` incantation"), so there is no longer a shipped snippet to critique for a specific leak.
- This round's new Minor 2 ("wrote" vs. "wrote or edited" scoping drift) — file-an-issue — rationale: a real, cheap-to-fix textual inconsistency in a shipped, adopter-facing canonical block; low urgency since no round has actually under-scoped a count because of it.
- This round's new Minor 3 ("every path in the tree" overclaim) — folded into round 1's finding 5's issue (see above), not filed separately.
- This round's new Minor 4 (redirect-failure vs. exit-1 ambiguity) — file-an-issue — rationale: a real interpretive gap in the shipped safety contract, currently mitigated in practice by the read-back requirement but not spelled out as the operative test; worth a precise wording fix at low cost.

#### Per-pass record fields

- executor-invocation (codex-primary-r3): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary-r3): generation
- briefing-fidelity (codex-primary-r3): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (codex-primary-r3): T-1143-codex-primary-r3
- self-detected-host (codex-primary-r3): claude-code — CODEX_THREAD_ID was not set in this shell before any launch
- executor-invocation (codex-adversarial-r3): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on the diff between develop and HEAD in this repo, files: templates/prompt-blocks/record-shape-scan.md, docs/pii-controls.md, docs/pii-controls.ja.md, agents/engineer.md, agents/qa-verifier.md, agents/scrum-master.md, agents/codex-reviewer.md, tests/check-pii-shapes/run.sh. Read files yourself with git diff/nl -ba. Confirmed finding: tests/check-pii-shapes/run.sh's wholly-untracked-record assertion (HOS_UT_ prefix, ~line 1810) uses check-pii-shapes.sh --all, which calls git ls-files -z --others --exclude-standard -- this honors the operator's global core.excludesFile... [full text in T-1143-codex-adversarial-r3.jsonl] ... Return a JSON array of {severity, file, line, issue, suggestion}, nothing else." > <RAW_JSONL> 2>&1
- pass-role (codex-adversarial-r3): generation
- briefing-fidelity (codex-adversarial-r3): carried — the primary pass's own confirmed finding (the excludes fragility, already independently reproduced by me before this pass ran) was written directly into this pass's prompt, along with an open-ended devil's-advocate mandate over the same file set
- raw-capture (codex-adversarial-r3): T-1143-codex-adversarial-r3
- self-detected-host (codex-adversarial-r3): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

#### Record shape scan (own record, this pass)

- shape-scan: rc=1 — 5 record path(s) written this round (`.shell-team/reviews/T-1143.md`, `.shell-team/reviews/T-1143-codex-primary-r3.txt`, `.shell-team/reviews/T-1143-codex-primary-r3.jsonl`, `.shell-team/reviews/T-1143-codex-adversarial-r3.txt`, `.shell-team/reviews/T-1143-codex-adversarial-r3.jsonl`) — 0 finding(s) on them, 0 classified accepted noise — command: `D=$(mktemp -d "$TMPDIR/t1143codexr3finalscan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=(\.shell-team/reviews/T-1143\.md|\.shell-team/reviews/T-1143-codex-primary-r3\.txt|\.shell-team/reviews/T-1143-codex-primary-r3\.jsonl|\.shell-team/reviews/T-1143-codex-adversarial-r3\.txt|\.shell-team/reviews/T-1143-codex-adversarial-r3\.jsonl) ' "$D/scan.txt" | wc -l`. Disclosure: the four `.txt`/`.jsonl` raw-capture paths are gitignored (`.gitignore:15-17`) and confirmed absent from the whole scan's output entirely (`grep -c "T-1143-codex" "$D/scan.txt"` → `0`), the same `--all`-enumeration blind spot round 1's fast-follow finding 5 discloses and this round's own Minor 3 sharpens — their zero count is not independent evidence they are clean, only that this scan cannot see them. Only `.shell-team/reviews/T-1143.md` is genuinely covered, and its 0 is a real result (run after this section was appended, immediately before commit, confirmed via `git status --short .shell-team/reviews/`).

### QA verdict: PASS — T-1143 (round 4)

## Verification items run (round 4, against commit `9fea333`)

- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1143-handoff-shape-scan-step.md 2>&1 | tail -1` → `check-acs: 10 passed, 0 failed, 1 skipped, 0 unrecognized` — AC1-AC10 PASS, AC11 SKIP, independently re-run against commit `9fea333`.
- `bash bin/check-intent.sh .shell-team/specs/T-1143-handoff-shape-scan-step.md .shell-team/todo.md` → `aligned: T-1143 v1 (8348a4d48a63169ff9dc656f841eda21d68150f5)` — unchanged hash; this round touches no spec byte (confirmed: `git diff 5d0a01e...HEAD -- .shell-team/specs/` produces no output).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1143.md` → `conformant: .shell-team/provenance/T-1143.md (12 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1143 .shell-team/interventions/T-1143.md` → `conformant: .shell-team/interventions/T-1143.md (2 entries, 0 sentinel)`.
- `TD=$(bash bin/team-paths.sh --get todo); bash bin/check-handoff.sh "$TD"` → exit 0.
- `B=$(git merge-base develop HEAD); bash bin/check-board-headings.sh "$TD" --base "$B"` → exit 0.
- `git diff --no-renames --stat 0b95e39...HEAD` → 3 files changed (`tests/check-pii-shapes/run.sh`, `.shell-team/provenance/T-1143.md`, `.shell-team/todo.md`), confirming the round's own claimed scope; `git status --short` empty throughout this verification round (all mutation probes performed in a disposable `git clone --shared` scratch copy under `$TMPDIR`, never on the real working tree).

## CI-wired suites (own-run, round 4)

- `bash tests/check-pii-shapes/run.sh` → `check-pii-shapes suite: all assertions passed` — all five `hand-off shape scan` labels PASS, including the new control (`grep -cF 'hand-off shape scan' "$D/out.txt"` = 5, `grep -c '^PASS: hand-off shape scan'` = 5, 0 FAILs).
- `shellcheck tests/check-pii-shapes/run.sh` → exit 0 (shellcheck 0.11.0, matches `.shell-team/test-recipe.md`'s CI-parity pin and `.github/workflows/check-handoff.yml`'s `SHELLCHECK_VERSION`).
- `bash bin/check-prompt-sync.sh` → `check-prompt-sync: all registered prompt blocks in sync`; `bash tests/check-prompt-sync/run.sh` → `All check-prompt-sync assertions passed.`
- `bash bin/check-pii-shapes.sh --base "$(git merge-base develop HEAD)"` → `check-pii-shapes: clean (no PII-shaped bytes found)`.
- Reverse-mapped reached set (this round's own diff touches only `tests/check-pii-shapes/run.sh`, `.shell-team/provenance/T-1143.md`, `.shell-team/todo.md`; `grep -rl -- tests/check-pii-shapes/run.sh tests/*/run.sh` → `tests/derive-populations/run.sh`; the cumulative-branch reached set from round 3 is unchanged since no other edited path gained a new line this round): `tests/check-prompt-sync/run.sh`, `tests/gen-playbook-blocks/run.sh`, `tests/machine-tokens/run.sh`, `tests/codex-skeleton-hygiene/run.sh`, `tests/check-invocation-path/run.sh`, `tests/codex-agents/run.sh`, `tests/derive-populations/run.sh` — all seven run fresh, all exit 0.

## Class D closure, independently re-derived

Item-by-item confirmation of the round-4 hand-off's own Class D inventory and the round-3 Major's own defeat scenario, none of it accepted on the hand-off's word alone:

**(i) Literal-filename hostile config, full suite.** Built `GIT_CONFIG_GLOBAL` pointing at a `.gitconfig` whose `core.excludesFile` matches the record's own literal filename (`uncommitted-record.md`) in a scratch excludes file, independently of the engineer's own fixture. `GIT_CONFIG_GLOBAL=<that gitconfig> bash tests/check-pii-shapes/run.sh` → exit 0, all five `hand-off shape scan` labels PASS including the new control — the round-3 Major's own reported scenario now survives.

**(ii) Broader `*.md` hostile config, abort-site confirmation.** `GIT_CONFIG_GLOBAL=<a gitconfig whose excludesFile matches "*.md"> bash tests/check-pii-shapes/run.sh` → exit 1. Located the exact abort: `git -C "$PH_REPO" add placeholders.md` at `tests/check-pii-shapes/run.sh:1151` (`git add` refuses the ignored path, aborting the whole script under `set -e`) — the "placeholder forms are not findings" fixture (comment header `tests/check-pii-shapes/run.sh:1133`, T-1101), 582 lines before the `hand-off shape scan` section starts (`:1715`). Confirmed this abort site is outside this branch's diff: `git diff d5884ff...HEAD -- tests/check-pii-shapes/run.sh | grep -c placeholder` → `0` (no match at all — the branch diff never touches the token). Since the script dies under `set -e` before the interpreter ever reaches line 1739 (`printf '\n--- hand-off shape scan ---\n'`), no assertion this task added is the one that failed — nothing in the `hand-off shape scan` section printed a `FAIL:` line or any output at all; the run's own stdout+stderr shows the "placeholder forms" section's output truncated mid-fixture by git's own refusal message ("The following paths are ignored by one of your .gitignore files: placeholders.md").

**(iii) Class D inventory, row by row.** Read the fixture section (`tests/check-pii-shapes/run.sh:1739`–`:1886`) and `new_repo()`/`add_fixture_line()` (`:98`–`:117`) directly, not from the hand-off's account:
  - `new_repo()` (`:98`–`:106`): `git init` + `git commit --allow-empty` — no `git add`, not ignore-consulting. Confirmed untouched by this round's diff (identical bytes to round 3).
  - `HOS_REPO` — serves (a)/(b)/(c). Read mechanism: (a)/(c) via `--all`'s `ls-files -s --cached` branch (tracked-file listing, not excludes-sensitive once tracked); (b) via `git cat-file -p "HEAD:$path"` (reads a committed blob directly, never consults excludes). Neither read is excludes-sensitive — but the repo's *setup* calls `add_fixture_line` (`:1764`), whose `git add -- "$relpath"` (`:114`) **is** ignore-consulting; under the `*.md` hostile config this call is exactly what aborts the whole section (confirmed in (ii) above). Pinned at `:1758`, immediately after `new_repo()` and before the `add_fixture_line` call — correctly ordered.
  - `HOS_UT_REPO` — serves (d). Read mechanism: `--all`'s `ls-files -z --others --exclude-standard` branch (`bin/check-pii-shapes.sh:744`), which **is** excludes-sensitive — this is the round-3 Major's own originally reported site. Pinned at `:1844`, immediately after `new_repo()` and before the record is written to disk.
  - `HOS_UT_HOSTILE_REPO` — the new control. Deliberately given a *hostile* `core.excludesFile` (matching the record's own literal filename, written into a repo-scoped file, not the global one) rather than a neutral pin, to prove the neutral pin elsewhere has something real to defeat.
  - Complete and minimal: no other `git add`, `git ls-files --others`, or other ignore-consulting call exists anywhere in this round's diff outside these three repos — confirmed by reading the full diff (`git diff 0b95e39...HEAD -- tests/check-pii-shapes/run.sh`, reproduced above) line by line; every added line belongs to one of the three repos' own setup/read/control blocks. Two pins (`HOS_REPO`, `HOS_UT_REPO`) plus one control (`HOS_UT_HOSTILE_REPO`) is the complete, minimal fix for the Major as extended by the engineer's own live-tested second exposure — independently agreed, not merely accepted.

**(iv) Mutation, independently performed (not the engineer's own restoration taken on faith).** Both mutations run in a disposable `git clone --shared` of this checkout's own working tree, made into a scratch copy under `$TMPDIR`, checked out to this branch — the real working tree was never touched (`git status --short` in the real repo confirmed empty before, during and after).
  - Commented out `HOS_UT_REPO`'s pin line (`:1844`) only. Re-ran under the literal-filename hostile `GIT_CONFIG_GLOBAL` from (i): assertion (d) **FAILed** exactly as predicted (`all_rc=0` where `1` was expected — the untracked record became invisible), the other four labels stayed PASS. Restored the file from `git show HEAD:tests/check-pii-shapes/run.sh` in the scratch clone: byte-identical (`diff` clean), `git status --short` empty in the scratch clone, suite green again.
  - Commented out the control's own hostile pin line (`:1874`) only, re-ran with no `GIT_CONFIG_GLOBAL` override (ambient environment). The control **FAILed** as predicted (`rc=1`, the record *was* reported once nothing hid it — `FINDING pattern=tracker-key path=uncommitted-record.md line=1`), proving the control is not vacuous. Restored byte-identically, confirmed via `diff` against `git show HEAD:…`, `git status --short` empty, suite green again.

## Control label honesty, AC8(b), `new_repo()`

- The control's label text ("a hostile excludesFile matching this record's own name makes it invisible to --all's untracked enumeration, proving the neutral pin above has teeth") and its surrounding comment (`:1862`–`:1871`) frame the demonstrated behaviour as the checker's own real, disclosed blind spot for ignored paths (round 1 fast-follow finding 5 / Non-goal #1 / out-of-scope item 2) — never as a defect in `bin/check-pii-shapes.sh`'s enumeration logic. Read directly: honest, not overclaiming, consistent with the Non-goals.
- AC8(b) (no committed literal of the opt-in namespace shape anywhere this round's diff adds) is asserted mechanically by the spec's own `- check:` line and independently re-confirmed PASS via `check-acs.sh` above; the new control and pin lines introduce no new namespace-shaped literal (`HOS_NS`/`HOS_NUM` fragments are reused unchanged from rounds 1-3, and the new lines contain only paths, comments and config values, no assembled shape).
- `new_repo()` (`tests/check-pii-shapes/run.sh:98`–`:106`) is byte-identical to round 3 — confirmed by direct read; this round's diff (`git diff 0b95e39...HEAD -- tests/check-pii-shapes/run.sh`) touches no line inside that function.

## AC5, AC10, `record-portability.md`

- AC5 (8 shipped paths, deletion column 0, ≥1 insertion each) — PASS live via `check-acs.sh` above; the cumulative branch diff over `tests/check-pii-shapes/run.sh` (the only one of the eight paths this round edits) still nets to zero deletions against the base ref, because every line this round rewrote sits inside code this task itself added since `d5884ff` (never present in the base blob) — confirmed by reading the round-4 diff: every changed hunk is either a pure insertion (the two `git config` pin lines, the new control block) or a comment inserted immediately above an already-task-owned line.
- `templates/prompt-blocks/record-portability.md` — untouched this round (not in the round-4 diff at all); byte-identity to base confirmed as part of AC5's own `cmp` clause.
- AC10 (scope lock) — PASS live via `check-acs.sh` above; `git diff --no-renames --name-only d5884ff...HEAD` confirms the cumulative changed-and-added set is exactly the allow-listed thirteen paths, `bin/check-pii-shapes.sh` and `.github/workflows/check-handoff.yml` both absent.

## Records

- `bash bin/check-provenance.sh .shell-team/provenance/T-1143.md` → `conformant (12 decision entries, 0 sentinel)` — the 3 new round-4 entries read directly against the code and the review record: (i) per-repo local config vs. an outer `-c` flag, (ii) the pin's extension to `HOS_REPO` beyond the Major's own literal scope, (iii) the literal-filename-vs-glob and separate-repo choices for the control — all three match what (iii)/(iv) above independently confirmed.
- `bash bin/check-interventions.sh --task T-1143 .shell-team/interventions/T-1143.md` → `conformant (2 entries, 0 sentinel)` — unchanged from round 3 (this round records no new stall or resume; the round-4 hand-off's own "Notes for QA" bullet states the file was not touched, confirmed by `git diff 0b95e39...HEAD -- .shell-team/interventions/` producing no output).
- `bash bin/check-handoff.sh "$TD"` and `bash bin/check-board-headings.sh "$TD" --base "$B"` both exit 0 (above).

## AC11 audit, item by item (round 4)

Round 4's own diff touches only `tests/check-pii-shapes/run.sh` (a fixture the block/role files/adopter docs never reference), provenance and the board — no `- check:` line, role file, or adopter document changed. Per item:

- (a) **Mutation self-check**: not owed fresh against AC1-AC10's own `- check:` lines (none of their target files changed this round); independently performed the two mutations this round's own new code invites (item "Class D closure" (iv) above) — both confirmed non-vacuous, both restored.
- (b) **Execution-context matrix**: unchanged from rounds 1-3's own audits (no invocation form changed); not re-owed this round.
- (c) **CI-equivalence**: re-derived and re-run fresh this round (CI-wired suites section above) — matches the round-4 hand-off's claimed reached set.
- (d) **Full-population downstream inventory**: unchanged disposition, deferred to the release sweep under the checkout's disclosed standing policy; not re-litigated.
- (e) **Plural-corpus read**: unchanged; this round touches no governed-noun text.
- (f) **PII-shape self-check before first commit**: independently re-derived below (this record's own shape-scan line).
- (g) **Norm-text read**: unchanged; this round adds no norm text.

## Edge cases tried (round 4, beyond the spec's own enumeration)

1. The broader `*.md` hostile config (item (ii) above) is not one of the two forms the round-4 briefing asked for verbatim reproduction of — it is this session's own independently constructed variant, chosen specifically to test whether the abort site the engineer disclosed is really outside this task's diff rather than accepting the disclosure at face value. Within the spec's declared Input space (class 6, "a repository whose corpus is clean… and this repository, where it exits 1 on pre-existing accepted noise"), not a synthetic extreme.
2. Mutating each of the two new pins independently (rather than both at once) to confirm each one's own, separate necessity — neither mutation was requested verbatim by the round-3 Major or the round-4 hand-off; both are this session's own probes.

## Verdict (round 4)

PASS. The round-3 Major (excludes-fragility in the "wholly untracked record" assertion) is independently confirmed closed, and closed more completely than the Major's own illustrative text required: the engineer's live testing found and closed a second exposure (`add_fixture_line`'s `git add` on `HOS_REPO`) the Major's text didn't name, and I independently reproduced both the original defeat and the extended one, then independently mutated each of the two pins and the new control to confirm none is vacuous. The broader `*.md` hostile-config abort is a real, pre-existing, out-of-scope condition (T-1101's "placeholder forms" fixture, unrelated to this task's diff) — disclosed accurately by the engineer and independently confirmed to sit outside this branch's diff. AC1-AC10 all PASS live; AC11 items audited above against their own enumeration, nothing incomplete. No regression: `new_repo()` untouched, `record-portability.md` untouched, AC5's zero-deletion invariant holds, AC10's scope lock holds. `git status --short` on the real repository empty throughout.

## This record's own shape scan (self-check before hand-off, item AC11(f))


- shape-scan: rc=1 — 2 record path(s) written this round (`.shell-team/reviews/T-1143.md`, `.shell-team/todo.md`) — 189 finding(s) on them, all 189 classified accepted noise (0 real quotations) — command: `D=$(mktemp -d "$TMPDIR/qa1143r4reviewfinal.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=(\.shell-team/reviews/T-1143\.md|\.shell-team/todo\.md) ' "$D/scan.txt" | wc -l`. Breakdown: `.shell-team/reviews/T-1143.md` 0 — a self-caught `pattern=home-path` finding at line 497 (a verbatim absolute local-checkout-path literal in this record's own item (iv)) was found on the pre-final scan, fixed in place before this line was written by describing the checkout generically instead of quoting the path, per this task's own fifth bullet (report by class, never by quoting the flagged bytes), and confirmed absent on re-scan; `.shell-team/todo.md` 189 — unchanged pre-existing count, entirely outside this round's own newly-added board range (confirmed via `grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | awk -F'line=' '{print $2}' | awk '$1>=135 && $1<=144'` → no output).

### Codex Review verdict: APPROVE (round 4)

- Task: T-1143
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 14 files, +1307/-0 lines (`git diff --stat develop...HEAD`); round-4 rework scope: `git diff --stat 0b95e39...HEAD` — 3 files, +77/-1 lines (`tests/check-pii-shapes/run.sh`, `.shell-team/provenance/T-1143.md`, `.shell-team/todo.md`)

#### Blockers

None.

#### Major

None. The round-3 Major (`tests/check-pii-shapes/run.sh`'s "wholly untracked record" assertion depending on the ambient `core.excludesFile`) is independently confirmed **closed as a class**, not merely patched at its own illustrative case:

- Reproduced the original defeat live (a per-repo `core.excludesFile` matching the record's literal filename `uncommitted-record.md`, via `GIT_CONFIG_GLOBAL`): the round-4 suite exits 0, all five `hand-off shape scan` labels PASS — matches the round-4 hand-off's own claim exactly.
- Independently mutated all three of this round's own new lines, each restored byte-identically afterward and each confirmed non-vacuous: (1) commenting out `HOS_UT_REPO`'s pin — assertion (d) FAILs under the hostile config exactly as predicted (`all_rc=0` where `1` was expected); (2) commenting out `HOS_REPO`'s pin — the whole script aborts under `set -e` at `add_fixture_line`'s `git add`, confirming the engineer's own extension beyond the Major's literal scope is load-bearing, not defensive padding; (3) commenting out the control's own hostile pin — the control itself FAILs (the record becomes visible again), proving it is a real, non-tautological control.
- Independently reproduced the broader `*.md` hostile-config disclosure: the suite aborts, but at `git -C "$PH_REPO" add placeholders.md` inside the pre-existing "placeholder forms are not findings" fixture (T-1101, line 1133) — confirmed outside this branch's diff (`git diff d5884ff...HEAD -- tests/check-pii-shapes/run.sh | grep -c placeholder` → `0`) and 582 lines before the `hand-off shape scan` section starts. Disclosed accurately by the engineer, not a defect this task introduces or owns.
- Confirmed the pin idiom is mechanically correct and answers round-4 Question 2 as far as the *realistic* ambient-environment threat model goes: `git ls-files -z --others --exclude-standard` and `add_fixture_line`'s `git add` both run with the scratch repo as their working directory (no `-C`/`GIT_DIR` override anywhere in the checker or the fixture), so a per-repo local `git config core.excludesFile /dev/null` outranks any ambient `core.excludesFile` set at the global or system config layer, which is the class the round-3 Major actually named (a contributor's own `~/.gitconfig`) and the class this project's own `GIT_CONFIG_GLOBAL` convention (`.shell-team/test-recipe.md`) simulates.
- Live re-ran all ten scriptable acceptance criteria (`bash bin/check-acs.sh`) — AC1-AC10 all PASS; re-ran `bash tests/check-pii-shapes/run.sh` (129 PASS, 0 FAIL), `bash bin/check-prompt-sync.sh`, `bash bin/check-pii-shapes.sh --base <merge-base>`, `shellcheck tests/check-pii-shapes/run.sh`, `bash bin/check-handoff.sh`, `bash bin/check-board-headings.sh`, `bash bin/check-provenance.sh`, `bash bin/check-intent.sh` — all green, all independently re-run rather than accepted from the hand-off. Confirmed `new_repo()` untouched, `record-portability.md` untouched, and the round-4 diff's own scope is exactly the three files (`tests/check-pii-shapes/run.sh`, provenance, board) the hand-off claims — the block, the four role regions, and both adopter documents are byte-identical to round 3 (`git diff 5d0a01e...HEAD -- templates/prompt-blocks/ agents/ docs/` empty).

#### Minor / nits

1. `templates/prompt-blocks/record-shape-scan.md:1,6` — the block's summary-line grammar (bullet 4) requires quoting "the exact command you ran," and bullet 1's own illustrative invocation form is `bash "<plugin root>/bin/check-pii-shapes.sh" --all`. Reachable input class 8 ("A role invoked with its plugin loaded from an installed snapshot") names a real deployment where `<plugin root>` resolves to a genuine home-directory-shaped path (per `docs/adopting.md`'s own substitution convention). If a role follows the literal form and honestly records the resolved command, the resulting `- shape-scan:` line's own command field would carry a `home-path`-shaped literal — a **default-mode** pattern (`scan_content_file()` calls `report_pattern_lines home-path …` unconditionally, confirmed by direct code read, `bin/check-pii-shapes.sh:687`), unlike the opt-in `tracker-key` shapes this whole step is otherwise about — which the CI diff-scoped scan (no opt-in needed) would then flag on this very commit. No round of this task's own four rounds ever triggered it (every hand-off's own `- shape-scan:` command field used a portable relative form instead), and the general discipline the block's own fifth bullet already states ("never quoting the flagged bytes... a verbatim quote adds a new site the fix has to reach too") plausibly extends to this case for a careful reader — but the block does not say so explicitly for the one field a role writes *after* the last scan run covers it. Fix (cheap, one clause): state that the recorded command keeps `<plugin root>` as the portable placeholder (or an equivalently portable form), never a resolved absolute path.
2. `docs/pii-controls.md` and `docs/pii-controls.ja.md`, throughout `## Running it` (pre-existing) and this task's own new `## When the loop runs it` — both sections use a bare relative-path invocation form (`bin/check-pii-shapes.sh …`, no `bash` prefix, no `<plugin root>` substitution), which does not literally match `docs/distribution.md:29`'s stated adopter-facing convention (`bash "<plugin root>/bin/<script>"`, "never assumed to be on `PATH`"). **Not a finding against this task's diff**: `## Running it` predates T-1143 and AC5's zero-deletion lock forbids rewording it; this task's own new section deliberately mirrors that pre-existing style for document-internal consistency (independently confirmed by round-2 QA). Disclosed for the record as a pre-existing documentation-convention gap in the file this task extends, not a defect this task introduces or could fix within its scope lock.
3. My own live reproduction, in answer to round-4 Question 2: `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_0`/`GIT_CONFIG_VALUE_0` (a git config layer that outranks local `.git/config` in git's own precedence order — documented git behavior, not malformed input) still defeats the round-4 pin. Reproduced live: `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.excludesFile GIT_CONFIG_VALUE_0=<hostile-excludes-file>` set ambiently reproduces the identical abort at `add_fixture_line`'s `git add` on `$HOS_REPO`, exactly as the unpinned mutation case did. Calibrated Minor rather than Major, on the same "latent, not observed" ground round 3's own Major used for its own calibration: this project's CI never sets these env vars (confirmed, `grep -n GIT_CONFIG .github/workflows/check-handoff.yml` → no hits beyond an unrelated doc mention), no round's actual execution environment carries them, and — unlike the round-3 Major's own target — this specific precedence layer would equally defeat the two *precedent* fixtures (`tests/rollup-track/run.sh`, `tests/gitignore-raw-dumps/run.sh`) this round's own pin correctly followed as convention, making it a project-wide convention gap rather than something unique to this task's diff.
4. `templates/prompt-blocks/record-shape-scan.md:4,6` — Codex's primary pass independently re-raised the "wrote" vs. "wrote or edited" scoping drift across the whole branch diff. Not new: already tracked as round-3's positional Minor 1 (file-an-issue, unchanged disposition — see Fast-follow disposition below).
5. Carried unchanged from round 3 (no new diff touches these): the sharpened round-1 finding 5 (gitignored raw captures invisible to `--all`), round-1 findings 7-9 (won't-fix), and round-3's positional Minor 3 (redirect-failure vs. exit-1 ambiguity, file-an-issue) — see Fast-follow disposition below.

#### Adversarial findings

Not run this round. Step 5's sole trigger (a blocker/major finding) did not fire: the round-3 Major is independently confirmed closed, and every other finding this round settles at Minor or below on my own judgment (see Synthesis audit ledger).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Recorded `- shape-scan:` command field could embed a `home-path`-shaped literal via a literal `<plugin root>` substitution, in the reachable installed-snapshot input class | blocker (Codex primary, native `[P1]`, normalized per the severity map) | minor | Downgraded three levels on my own judgment: no round of this task's own four rounds ever triggered it (every hand-off used a portable command form instead); it is a default-mode pattern but the general "never requote a flagged shape" discipline the block already states plausibly covers it for a careful reader; and the self-check discipline this very round's briefing points at (a final re-scan right before commit) has already demonstrated it catches this class in practice (QA's own home-path catch this round, a different literal instance of the same mechanism). A real, cheap-to-close documentation gap, not a functional defect that fired or is likely to fire unnoticed |
| 2 | `docs/pii-controls.md`/`.ja.md` use a bare relative-path convention that does not literally match `docs/distribution.md`'s adopter-facing `bash "<plugin root>/bin/<script>"` form | major (Codex primary, native `[P2]`, normalized per the severity map) | rejected as a finding against this diff (disclosed instead) | The flagged section (`## Running it`) predates T-1143 and AC5's zero-deletion lock forbids rewording it; this task's own new section deliberately mirrors that pre-existing style for document-internal consistency, a design choice round-2 QA already independently confirmed. A pre-existing documentation-convention gap outside this task's scope lock, not a defect in this diff |
| 3 | "wrote" vs. "wrote or edited" scoping drift, re-raised by Codex's primary pass on the whole-branch diff | major (Codex primary, native `[P2]`, normalized per the severity map) | minor (merged into the existing, unchanged tracked item) | Not a new finding: already identified and disposed at round 3 (positional Minor 1, file-an-issue). Re-confirmed independently, no re-litigation needed; disposition unchanged |
| 4 | `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_n`/`GIT_CONFIG_VALUE_n` env-level config injection still defeats the round-4 pin | n/a — not raised by either Codex pass; my own independent live reproduction, constructed in direct answer to round-4 Question 2 | minor | Assigned directly (no prior judgment to compare against): real and reproducible, but latent rather than observed in any actual round's environment (matching round 3's own calibration ground for its Major), and shared equally by the two precedent fixtures this round's pin correctly followed — a project-wide convention gap, not unique to this task's new code |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition

- Round 1, finding 5 (codex-reviewer's own gitignored raw captures invisible to `--all`), sharpened by round-3's positional Minor 2 (the block's prose overclaiming `--all`'s coverage) — file-an-issue — unchanged; rationale carried forward verbatim from round 3: a real, reproducible gap in `bin/check-pii-shapes.sh`'s `--all` enumeration relative to this project's own gitignore patterns for review-record raw captures, out of this task's scope lock (Non-goal #1) but concrete and actionable enough to file.
- Round 1, finding 7 (discretionary-sounding doc wording, "of their own accord" / "自ら走らせる") — won't-fix — unchanged; the canonical block is unambiguous and imperative, the docs are a summary.
- Round 1, finding 8 (DP4's second exclusion ground omitted from the adopter docs) — won't-fix — unchanged; fully disclosed in the frozen spec's Non-goals, no adopter action depends on it.
- Round 1, finding 9 (manual count derivation for the `- shape-scan:` line not mechanically reproducible from one pipeline) — won't-fix — unchanged; all four rounds' hand-offs demonstrated correct, independently-reproducible derivations.
- Round 3, positional Minor 1 ("wrote" vs. "wrote or edited" scoping drift) — file-an-issue — unchanged; re-confirmed independently by this round's own Codex primary pass on the whole-branch diff; a real, cheap-to-fix textual inconsistency in a shipped, adopter-facing canonical block.
- Round 3, positional Minor 3 (redirect/file-creation failure vs. a genuine exit-1 scan not mechanically distinguished by exit code alone) — file-an-issue — unchanged; a real interpretive gap in the shipped safety contract, mitigated in practice by the read-back requirement but not spelled out as the operative test.
- This round's new Minor 1 (recorded shape-scan command could embed a `home-path` literal via a literal `<plugin root>` substitution, reachable installed-snapshot class) — file-an-issue — rationale: a real, cheap wording fix (keep the recorded command portable) closing a gap in the one field this block's own prescribed scan run cannot cover, in a reachable (not synthetic) input class this spec's own Input space names.
- This round's new Minor 3 (`docs/pii-controls.md`/`.ja.md`'s bare-form convention vs. `docs/distribution.md`'s adopter convention) — file-an-issue — rationale: a real, pre-existing documentation-convention gap in a file this task extends but cannot reword (AC5), worth a dedicated project-wide pass rather than sitting undisclosed; explicitly out of this task's own diff, filed for the document as a whole.
- This round's new Minor 4 (`GIT_CONFIG_COUNT`/`KEY`/`VALUE` env-level config injection defeats the `git config core.excludesFile` pin idiom) — file-an-issue — rationale: a real, reproducible gap shared by this project's whole excludes-pinning convention (including the two precedent fixtures), worth a single project-wide issue rather than a per-fixture patch; latent in every environment observed so far, not blocking.

#### Per-pass record fields

- executor-invocation (codex-primary-r4): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary-r4): generation
- briefing-fidelity (codex-primary-r4): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (codex-primary-r4): T-1143-codex-primary-r4
- self-detected-host (codex-primary-r4): claude-code — CODEX_THREAD_ID was not set in this shell before any launch

#### Record shape scan (own record, this pass)


- shape-scan: rc=1 — 1 record path(s) written this round (`.shell-team/reviews/T-1143.md`) — 0 finding(s) on it, 0 classified accepted noise — command: `D=$(mktemp -d "$TMPDIR/codexr4finalselfscan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=\.shell-team/reviews/T-1143\.md ' "$D/scan.txt" | wc -l`. Disclosure: `T-1143-codex-primary-r4.txt`/`.jsonl` (this round's own raw capture, no adversarial pass run) are gitignored (`.gitignore:15-17`) and confirmed absent from the whole scan's output entirely (`grep -c "T-1143-codex" "$D/scan.txt"` → `0`), the same `--all`-enumeration blind spot round 1's fast-follow finding 5 discloses — absent here is not evidence they are clean, only that this scan cannot see them.
