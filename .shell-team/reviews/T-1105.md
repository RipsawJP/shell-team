# Codex Review — T-1105

### Codex Review verdict: APPROVE
- Task: T-1105
- Codex model: gpt-5.6-luna — read from `~/.codex/config.toml`'s `model = ` line, measured this round rather than relayed; no `-m` override was passed to either invocation this round.
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 12 files, +580/-32 lines (base `c7b9053062d55042f7457d5f07bf4a6e1485a034` — the still-open stacked predecessor `docs/ja-brushup` tip, resolved per the spec's own `- base-ref-discriminator:` expression, cross-checked here against `git show-ref --verify --quiet refs/heads/docs/ja-brushup` before use)

Two Codex passes ran this round: a primary pass (`codex exec … review --base c7b9053 --json`, reading the real working tree directly) and a targeted devil's-advocate pass on the two remaining edited surfaces (the skill-file wiring and the registry widening), run because the primary pass's one finding normalizes to blocker severity under the closed P0/P1→blocker map. Both raw outputs are saved (paths at the bottom). Independently, before and alongside both Codex passes, I re-ran the spec's own 18 checkable `- check:` lines live (`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1105-review-executor-resolution.md` → `18 passed, 0 failed, 2 skipped`, matching QA's own tally exactly), re-ran `bash bin/check-prompt-sync.sh` (exit 0) and `bash bin/check-intent.sh` (`aligned: T-1105 v1 (08697b93a287c634c641e42d7711ad13fda2e879)`, unchanged since the freeze), and hand-verified the four documentation surfaces' delivered prose against the spec's own `## Claim→source rows` / "exact bytes to transcribe" blockquotes byte-for-byte (script-assisted, both languages) — all four match, with the one apparent mismatch on `docs/adopting.ja.md` traced to a pre-existing hard-wrap line-break inherited unchanged from the base file rather than anything this diff introduced.

This invocation's own executor resolution (`resolved codex-reviewer codex provider-configured - codex-cli out-of-process`, exit 0, no refusal) is itself data for **AC8**'s positive-control arm — the orchestrator's own attestation duty, not mine, but recorded here since this session is the arm that produced it: resolution succeeded and this pass proceeded to a real Codex call exactly as the carried paragraph in `skills/review/SKILL.md` describes.

#### Blockers

None.

#### Major

- **`.shell-team/specs/T-1105-review-executor-resolution.md:133` (AC20 item (a)) — the frozen criterion's own "remove one consumer from the `executor-resolution.md` row" probe predicts an observation that is impossible given the shipped checker.** AC20 item (a) requires reporting, as a mutation self-check item, that removing one consumer from the `executor-resolution.md` registry row makes "**AC4** red on the field count, **AC5** red on the dogfood." I re-measured this myself in a scratch copy: removing a consumer does flip AC4 red (the field-count assertion), but `bin/check-prompt-sync.sh`'s own walk loop (confirmed by reading it) validates only the consumers a row still lists — a shrunk row stays internally consistent and the checker exits `0`, so AC5 stays green rather than going red. This is the identical defect QA already found and disclosed in round 1 (`.shell-team/todo.md`, qa-verifier round-1 entry: "the frozen parenthetical is measured-contradictory prose … needs a class-B re-freeze correction routed through pm-spec"), independently corroborated here by Codex's primary pass at native severity P1 (→ blocker under the closed normalization map). The rework that answered QA's FAIL (`fcde653`) was scoped only to the `## Blast radius` section per the coordinator's own briefing and did not touch this AC20 clause, so the wrong prediction is still live in the frozen intent block at merge time.
  - Fix: a class-B re-freeze correction to AC20 item (a)'s parenthetical (drop or reword the "AC5 red on the dogfood" clause to match the measured behavior — the checker validates only listed consumers, so a shrunk row alone never reddens it), routed through `pm-spec`. Not blocking this merge: AC20 carries no `- check:` line by its own above-ceiling design, the wrong prediction affects no other criterion's verdict (independently confirmed: AC4, AC5, and every other checkable AC re-ran green above), and QA already triaged this exact defect as "dischargeable as written" across two rounds.

#### Minor / nits

- **AC4's containment check (`.shell-team/specs/T-1105-review-executor-resolution.md:84`) accepts a stray or corrupted consumer path via substring matching rather than exact-token matching.** Adversarial-pass finding, independently assessed: the check counts `NF-2` fields and separately greps each of the four expected literals as a substring anywhere in the row line; a row holding, e.g., `skills/review/SKILL.md.bak` alongside the other three correct paths would satisfy every assertion (field count 4, all four substrings found) despite the third consumer being wrong. The actually-delivered registry row is verified correct (exact four paths, no stray or corrupted entry), so this is a theoretical robustness gap in the check line rather than a defect in what shipped, and it would be caught in most real cases anyway by AC5's separate `bash bin/check-prompt-sync.sh` exit-0 requirement (a genuinely nonexistent consumer file makes that checker itself exit 1).
- **`skills/review-response/SKILL.md`'s new telemetry paragraph (Tail 3, transcribed from `templates/prompt-blocks/telemetry-binding.md`) never specifies the concrete `loop_id`/run-id/`seq`/`span`/`phase`/`iteration`/`attempt`/`status` context `bin/log-run.sh` needs, so a standalone `/shell-team:review-response` invocation has no stated source for any of those values if it ever emits a span.** Adversarial-pass finding. This is inherited from the canonical `telemetry-binding.md` block itself (byte-identical to base per **AC15**, zero edits by this task's own Non-goals), not something this diff introduces or could fix without editing a file the frozen Goal forbids touching. The spec's own row 42 and Non-goal already frame emission here as best-effort and optional, so the gap is real but scoped to a component this task correctly declined to touch.

#### Adversarial findings

The devil's-advocate pass (raw output: `.shell-team/reviews/T-1105-codex-adversarial.{txt,jsonl}`) was asked specifically not to re-report the AC20 defect already found by the primary pass and QA, and to instead probe (1) whether the canonical blocks are truly carried byte-identical with the tail outside the core, (2) whether the re-widened prose over- or under-claims anything about invocation routing, an alternate-executor path, or unconditional telemetry, (3) edge cases in the registry-row widening the frozen AC4 check might not catch, and (4) any other design smell in the skill-file or registry/doc edits. It returned exactly the two Minor findings above and nothing else — no over/under-claim was found on the honesty axis, and no fork or non-verbatim drift was found in either canonical block (both independently confirmed byte-identical to the base ref's committed blob via `AC1`/`AC2`/`AC15`'s own live re-run above).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC20 item (a)'s registry-removal probe predicts an impossible observation ("AC5 red on the dogfood") | primary (Codex pass): P1 (→ blocker by the normalization map) | major, disposed as fast-follow (not blocking this merge) | independently re-measured and confirmed real, and already independently found and triaged by QA across two rounds as "dischargeable as written" (AC20 carries no `- check:` line; no other criterion's verdict is affected); fixing it is a class-B re-freeze of frozen intent-block prose, disproportionate to reopen this twice-QA'd task over |
| 2 | AC4's substring-based containment check would accept a stray/corrupted consumer path in a narrow shape | adversarial: minor | minor (unchanged) | confirmed structurally possible in the check line's own logic, but the actually-delivered row is verified correct and AC5's paired dogfood backstops most real drift shapes |
| 3 | Telemetry paragraph on `skills/review-response/SKILL.md` lacks concrete run/loop/span context for a standalone caller | adversarial: minor | minor (unchanged) | real but scoped entirely to the shared canonical `telemetry-binding.md` block this task is expressly forbidden from editing (`## Non-goals`, **AC15**) |

#### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1105 entry to `READY_FOR_MERGE`.

#### Fast-follow disposition

- AC20 item (a)'s impossible registry-removal prediction (ledger finding 1) — recommend **file-an-issue**: a class-B re-freeze correction to this spec's frozen intent-block prose, scoped narrowly to rewording/dropping the "AC5 red on the dogfood" clause; not proportionate to bundle into reopening this already-twice-QA'd task, and QA's own round-1 disposition already named the same fix path.
- AC4's substring-matching robustness gap (ledger finding 2) — recommend **won't-fix**: the loophole requires a coincidentally-substring-matching, corrupted-yet-still-existing consumer file, the delivered content is verified exactly correct, and AC5's paired dogfood backstops the realistic drift shapes; hardening AC4 would itself need a class-B re-freeze for a very narrow theoretical gain.
- Telemetry context-field gap on the shared canonical block (ledger finding 3) — recommend **file-an-issue**, scoped against `templates/prompt-blocks/telemetry-binding.md`'s own design (or against whichever future task first tries to make a standalone-command telemetry span real) rather than against T-1105: this task correctly declined to touch that canonical file, and the gap predates it.

Raw Codex output saved at `.shell-team/reviews/T-1105-codex-primary.{txt,jsonl}` (primary review pass) and `.shell-team/reviews/T-1105-codex-adversarial.{txt,jsonl}` (devil's-advocate pass on the skill-file/registry edits).

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base c7b9053 --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-carried - the shipped `review --base` subcommand takes no prompt argument at all (agents/codex-reviewer.md), so this round's own launch-prompt Reviewer-scope verdict-scope briefing (items (i)-(v)) has no channel into this pass; the same disclosed gap T-1104's own record already names for this identical subcommand shape
- raw-capture (primary): T-1105-codex-primary

- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on this change. Read the files yourself with git diff c7b9053...HEAD, git show, and nl -ba -- do not expect any diff pasted in this prompt. The change wires per-role executor resolution into skills/review/SKILL.md and skills/review-response/SKILL.md, widens two registry.txt 'contain' rows (executor-resolution.md and telemetry-binding.md) to include the new consumers, and re-widens the boundary-clause prose in README.md, README.ja.md, docs/adopting.md and docs/adopting.ja.md. The spec is .shell-team/specs/T-1105-review-executor-resolution.md (frozen intent block, hash 08697b93a287c634c641e42d7711ad13fda2e879). A prior review already found one confirmed defect: AC20 item (a) in that spec predicts that removing a consumer from the executor-resolution.md registry row would flip bin/check-prompt-sync.sh's dogfood red ('AC5 red on the dogfood'), but the checker only validates listed consumers, so removing one keeps it green -- only AC4's exact-count assertion goes red. Do not re-report that same finding; instead try to break this change some other way. Focus specifically on: (1) whether the canonical blocks in templates/prompt-blocks/executor-resolution.md and templates/prompt-blocks/telemetry-binding.md are truly carried byte-identical with only a role-specific tail on the same physical line in both skills files, with no fork; (2) whether the re-widened prose on all four doc surfaces (including both Japanese ones) actually matches what the spec's '## Claim->source rows' section prescribes, or overclaims/underclaims anything about whether resolution routes an invocation, whether an alternate-executor path exists, or whether telemetry is unconditional; (3) any edge case in the registry.txt row widening (whitespace, ordering, a stray consumer) that the frozen AC4 check might not actually catch; (4) any other design smell or correctness gap in the two skill-file edits or the four registry/doc edits. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <RAW_JSONL> 2>&1
- pass-role (adversarial): confirmation
- briefing-fidelity (adversarial): carried - the prior QA-found AC20 defect (named so it would not be re-reported), the four specific focus areas, and the spec's own frozen hash were stated directly in the prompt argv itself, which this pass's invocation channel supports
- raw-capture (adversarial): T-1105-codex-adversarial
