# T-1004 review record

## qa-verifier — PASS → READY_FOR_REVIEW (2026-07-30)

**Scope**: full spec verification (18 ACs, intent NOW AT v2
`cda6c0ec5ddf2a52f60e1a1828e2626fcafa5f60`, the human-ratified v1→v2 mechanics
repair of AC2), plus regression across the CI-named suite list, plus
independent adversarial fixture construction of my own, plus independent
reproduction of the engineer's five mutations.

### The v1→v2 chain, verified myself

- `bash bin/check-intent.sh .shell-team/specs/T-1004-optin-hook-sample.md
  .shell-team/todo.md` → `aligned: T-1004 v2 (cda6c0ec…) matches …` (exit 0).
- Read the spec's live AC2 check line: `git diff --name-only
  --diff-filter=A develop -- agents/ skills/ commands/` (added files only) —
  matches the board's v1→v2 delta narrative exactly (the check line changed
  from a whole-diff `grep` to `--diff-filter=A`, and the prose word changed
  "adds nothing" → "adds no new file").
- `git diff develop -- agents/scrum-master.md` shows exactly one line
  changed (the AC16-mandated clause) — confirms AC16 and the narrowed AC2
  are now jointly satisfiable, closing the frozen-set contradiction the
  engineer reported at BLOCKED.

### Mechanical gates (all re-run live, none trusted from the hand-off)

- `check-acs.sh` (live): **18/18 PASS**.
- `check-prompt-sync.sh`: clean.
- `check-handoff.sh` on `templates/todo-template.md` and the live board: both
  exit 0.
- `check-pii-shapes.sh --base develop`: clean.
- `check-commit-identity.sh --base develop`: clean.
- `shellcheck` (0.11.0, matches CI's pinned version) on the exact CI
  argument list (49 paths, including the two new T-1004 files): clean.
- Every suite + dogfood step named in `.github/workflows/check-handoff.yml`,
  run in the file's own order: all green, including the new
  `tests/interventions-reminder/run.sh` (12/12 cases), both retro-inputs
  dogfood steps, and the check-interventions dogfood loop (3 files found —
  T-1002.md, T-1003.md, T-1004.md — all conformant).
- `bash bin/check-interventions.sh --task T-1004
  .shell-team/interventions/T-1004.md` → `conformant` (3 entries, 0
  sentinel).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1004.md` →
  `conformant` (3 decisions, 0 sentinel) — read all three for grounding: the
  install-snippet JSON nesting, the unconditional stdin-drain placement, and
  the anti-vacuity-control symmetry extension to no-op cases are all
  non-trivial, correctly grounded against the spec's own DP-4/Notes text and
  the actual diff, not free choices dressed up as forced ones.
- `git diff --name-only develop`: exactly the 12 files the spec's Measured
  inventory + AC18's allow-list name; nothing outside it.

### Independently reproduced (not trusted from the engineer's hand-off) — five mutations in a `git worktree add --detach` scratch copy, never the live tree

1. One-character reminder edit (sample only) → **AC3 and AC4 both fire red**
   (both compare against the literal spec bytes; the mutated file no longer
   matches either).
2. In-flight ERE replaced with a bare flag-token grep → **AC5 fires red**
   specifically on its Done-only discrimination case (a `- [x]` line
   carrying `READY_FOR_MERGE` now matches the bare grep and produces a false
   emission).
3. Resolver call deleted, path hardcoded to `.shell-team/todo.md` → **AC8
   fires red** on both halves (the no-hardcode negated grep now matches the
   literal path, and the legacy-layout fixture correctly produces a silent
   no-op instead of the reminder, since the hardcoded path doesn't exist
   under the legacy fixture's cwd).
4. A diagnostic `printf` added on the resolver-missing failure path → **AC7
   fires red** (the `noresolver` case's stdout is no longer byte-empty).
5. Self-chosen: a single `\n` byte inserted on a no-op exit path. My first
   attempt (the `[ -n "$board" ]` empty-string branch) did **not** fire any
   criterion, because `team-paths.sh --get todo` always returns a non-empty
   path string regardless of whether the file exists — that branch is
   defensive but not reachable through any of this suite's fixtures.
   Retargeting the byte at the `[ -f "$board" ]` branch (the one the "no
   board at all" AC5/AC9 case actually exercises) **fires both AC5 and AC9
   red**, confirming the byte-empty-vs-blank-line distinction is genuinely
   tested, not vacuously passed. Reported as found — a minor correction to
   which branch carries this mutation, not a defect.

All five mutations restored to a diff verified byte-identical to the
original (`diff` against the pre-mutation backup); `check-acs.sh` back to
18/18 after each restoration; the scratch worktree removed cleanly
(`git worktree remove`) afterward; `git worktree list` and `git status
--short` on the real checkout show nothing beyond this hand-off's own board
edit throughout.

### Independent fixtures of my own construction (not the shipped suite's)

- **In-flight board** (my own task id/title, `READY_FOR_MERGE`): stdout
  byte-identical to the spec's pinned literal plus exactly one trailing
  `\n` (confirmed via `cmp`), empty stderr, exit 0. Also parsed with
  `python3 json.loads` — exactly one top-level key (`hookSpecificOutput`)
  containing exactly two keys (`hookEventName`, `additionalContext`),
  matching the external contract shape.
- **No board at all**: byte-empty stdout and stderr, exit 0.
- **Done-only board** (only `- [x]` lines + legend, empty `## Active`):
  byte-empty stdout and stderr, exit 0.
- **Legacy `tasks/` layout**: fires, with the reminder resolved through the
  shimmed resolver, not a hardcoded path.
- **Marker test**: a board title carrying a distinct board-marker and a
  `prompt` field carrying a distinct prompt-marker — neither marker appears
  anywhere in stdout or stderr; the fixed payload's own opening phrase is
  present (non-vacuity control).
- **Four degradation states**, each with a fixture I built myself: an
  unreadable board (`chmod 000`), the board path as a directory, the
  resolver absent from `PATH`, and non-JSON garbage on stdin combined with
  an unreadable board — all four byte-empty stdout/stderr, exit 0.
- **Bonus confirmation, not a defect**: garbage stdin combined with a
  *normal, readable, in-flight* board still fires the reminder normally —
  independently confirms DP-4's claim that stdin content never affects the
  outcome (the board is the only signal), in either direction.

### Adversarial / regex-anchoring probes beyond the shipped suite (checklist classes 1–2)

- **Time/liveness (class 1)**: fed the sample an open pipe whose write end
  is never closed (no EOF ever sent) as stdin, with a valid in-flight
  board. The script **blocks indefinitely** on its `cat >/dev/null` stdin
  drain (confirmed: process still running after 2s under `kill -0`, killed
  manually to clean up). This is a genuine liveness risk against DP-5's own
  stated design goal ("a hook that errors, hangs, or prints a diagnostic
  degrades the session … must never break it"). Judged **out-of-input-space**:
  the spec's Input space class 3 enumerates stdin states exhaustively
  (documented payload / arbitrary text / garbage / empty / **closed**
  stdin) and does not include "stdin never closes"; the external hook
  contract is declared an Assumption ("verified against the official Claude
  Code hooks documentation … 2026-07-30") not provable by this repo's
  tests, and a harness that spawns a subprocess and writes+closes stdin
  (the documented contract) would never produce this state — it would
  require a genuine harness-side bug external to this task's control.
  Recorded here for the reviewer, not as a FAIL driver.
- **Regex-vocabulary-collision (class 2), non-finding**: a check-handoff-conformant
  in-flight line whose *title* carries a fake, backtick-wrapped flag token
  earlier in the line, with the real flag+spec suffix intact at the true
  end — fires correctly on the real flag, not confused by the embedded
  fake one.
- **Regex-vocabulary-collision (class 2), disclosed finding**: a line that
  is genuinely **malformed** per `bin/check-handoff.sh`'s own `LINE_RE`
  (confirmed independently: `bin/check-handoff.sh` reports `format
  mismatch`, exit 1, on this exact line) but contains an embedded, fully
  well-formed flag+spec segment earlier in the line — the sample's
  unanchored (no `$`) `grep -E -q` still fires on it. This is a real
  violation of DP-2's stated guarantee ("a board line that does not
  conform reads as not-in-flight … never toward a false alarm"), but its
  only trigger is a line that fails `check-handoff.sh`'s own CI-enforced
  grammar, so it can never survive into a CI-clean board — matching this
  spec's own out-of-scope item 1 verbatim ("a crafted line built to make
  the ERE backtrack. Real boards are … linted by `bin/check-handoff.sh` in
  CI."). Judged **out-of-input-space**; not a FAIL driver, but disclosed
  for the reviewer since it is a real, reproducible gap against a stated
  design claim (DP-2), not a hypothetical.
- **CommonMark-adjacent probe (class 3), disclosed finding**: a board whose
  `## Active` section is genuinely empty, but which contains a fenced code
  block elsewhere illustrating the board's line format with a *concrete*
  numeric task id and a real flag token (rather than the shipped
  templates' placeholder tokens `T-XXX` / `<STATUS_FLAG>`) — the sample
  fires a false reminder, because its `grep` is whole-file, not scoped to
  `## Active`. Confirmed the actual shipped `templates/todo-template.md`
  and this repository's own live board do **not** trigger this (their
  Format-section example uses placeholders that don't match the numeric-id
  / literal-flag grammar), so the shipped defaults are unaffected. This is
  a real, if narrow, residual of the same whole-file-scan class DP-2 set
  out to fix (it fixed the Done-line case specifically, not the general
  case) — not covered by any AC, and not named in the spec's Input space
  either as reachable or out-of-scope. Disclosed as a risk note, not a
  FAIL driver: it requires an adopter to hand-write a concrete example in
  board documentation prose rather than use the shipped placeholder
  convention.

### Test-harness short-circuit checks (checklist class 4)

- `./docs/interventions-reminder-hook.sample.sh` (direct exec, bare form) →
  `permission denied`, exit 126 — confirms inertness by construction (AC1),
  not merely by grep on the git index mode.
- Confirmed `tests/interventions-reminder/run.sh` invokes the real
  production file directly (`$REPO_ROOT/docs/interventions-reminder-hook.sample.sh`),
  not a copy or stub — the suite exercises production code, not a test
  double.
- The docs prose parity check (below) was read as prose, not
  grepped for keyword presence alone.

### Docs prose parity — read side by side, not just grepped (my own judgment)

Read `docs/tuning-oversight.md`'s new `## The one sample hook, and why it
ships inert` section and `docs/tuning-oversight.ja.md`'s `## 唯一のサンプル
hook と、それが無効のまま出荷される理由` section side by side, sub-section by
sub-section (`### What it does` / `### 何をするか`; `### Install it yourself,
and read it first` / `### 自分でインストールし、まず読む`; `### The
capture-fidelity asymmetry, stated honestly` / `### 捕捉忠実度の非対称性を、
正直に述べる`). **Judgment: the ja text says what the en text says** — no
paragraph is missing, softened, or added on either side; the install
snippet's fenced JSON block is byte-identical across both files (confirmed
mechanically by AC15's grep-count-1 pin, and read to confirm it is the same
snippet, not a coincidentally-matching substring). No prose drift found.

### Protected invariants and scope

- AC17 (nine protected paths, byte-unchanged against `develop`): re-run
  live, passes.
- AC2 (zero activation footprint): re-verified each half independently —
  no `hooks` key in `.claude-plugin/plugin.json`; `.claude-plugin/`
  byte-unchanged; `find . -type d -name hooks` empty; the sample committed
  at git index mode `100644` and non-executable in the working tree
  (`-rw-r--r--`); `git diff --name-only --diff-filter=A develop -- agents/
  skills/ commands/` empty (only an *edit* to `agents/scrum-master.md`, no
  new file).
- AC18 allow-list: `git diff --name-only develop` matches exactly the
  spec's twelve declared paths.

### Working tree

Clean throughout. All fixtures and mutations were built and run under
`$TMPDIR`/scratch copies (a `git worktree add --detach` scratch checkout for
the five mutations, `mktemp -d` fixture directories for the empirical
exercise) — never `cp -r` over the live tree, and never a live-tree
destructive git operation. `git status --short` on the real repo showed
nothing beyond this hand-off's own board edit throughout.

## codex-reviewer — APPROVE (2026-07-30)

- Task: T-1004
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 13 files changed, +1528/-3 (`git diff --stat 415bcd8...HEAD`), 8 commits on `feature/optin-hook-sample`

### The frozen delta (v1 → v2), independently re-verified

Diffed the spec against its own v1 commit (`6a48ceb`) directly (`diff` against
`git show 6a48ceb:.shell-team/specs/T-1004-optin-hook-sample.md`), not trusted
from the board's narrative. The only bytes that changed inside the frozen
`<!-- BEGIN/END intent-block: T-1004 -->` region are, in AC2:

- prose: `the diff adds nothing under` → `the diff adds no new file under`
- check line: the tail changed from `test -z "$(printf '%s\n' "$d" | grep -E -- '^(agents|skills|commands)/')"` to `test -z "$(git diff --name-only --diff-filter=A develop -- agents/ skills/ commands/)"`

Nothing else in the frozen region differs; the only other diff hunk is the
appended, out-of-region `## Notes from engineer` section (correctly outside
the markers). The new form still holds the property AC2 exists for: an
*added* file under `agents/`/`skills`/`commands/` still fails the check
(catching a plugin-shipped active hook arriving as a new file — the Goal
paragraph's stated concern), while an *edit* to an existing file
(`agents/scrum-master.md`, AC16's mandate) no longer collides with it. I
independently confirmed the mechanism: `git diff --name-only --diff-filter=A
develop -- agents/ skills/ commands/` returns empty on this branch, and `git
diff develop -- agents/scrum-master.md` is exactly one changed line. Meaning
preserved — **not** a REQUEST_CHANGES trigger.

### Cross-provider Codex pass (primary, `codex exec ... review --base develop --json`)

One finding, Codex-native `P2`:

- `docs/tuning-oversight.md:136` / `docs/tuning-oversight.ja.md:103` — the
  install snippet's JSON `"command": "bash $HOME/.claude/hooks/interventions-reminder.sh"`
  leaves `$HOME` unquoted. If the adopter's home directory path contains a
  space, the shell that Claude Code invokes to run a shell-form hook command
  word-splits the expansion, and the hook fails to run as written.

### Adversarial pass (triggered by the above P2/major-mapped finding)

Re-ran Codex with a devil's-advocate framing against the two doc files and
the sample, and against AC15's frozen literal. Its own judgment, after
reading the files itself: the defect is real (confirmed by word-splitting
through the exact expansion), but overstated in the primary framing —
exposure is low (default macOS/Linux home paths have no spaces; Windows
`cmd`/PowerShell hosts are already out of scope per the spec's Input space
item 9, though Git Bash is a plausible edge case), blast radius is limited to
this one optional/inert feature (no regression to any existing behavior, no
data/security exposure), and "silently never runs" overstates the failure
mode — current Claude Code hook-error handling surfaces a notice rather than
swallowing the failure outright. AC15's frozen literal (`.shell-team/specs/T-1004-optin-hook-sample.md:317`)
constrains any fix: correcting the quoting requires re-baselining that
`check:` line's pinned string in the same way AC2 was re-baselined at v1→v2,
not a silent doc edit.

### Synthesis

I weigh the adversarial pass's real-world-impact case over the primary
pass's raw `P2` label: the finding is genuine and worth fixing, but its
low likelihood, contained blast radius (an opt-in, inert, already-optional
mechanism — never a regression to the shipped default's behavior), and
partial diagnosability move it below the bar for blocking this merge.

### Blockers

None.

### Major

None.

### Minor / nits

- `docs/tuning-oversight.md:136` and `docs/tuning-oversight.ja.md:103` — the
  install snippet's `"command": "bash $HOME/.claude/hooks/interventions-reminder.sh"`
  is not robust to a `$HOME` path containing a space (unquoted shell-form
  expansion word-splits). Suggested fix: `"command": "bash \"$HOME/.claude/hooks/interventions-reminder.sh\""`
  in both files, plus re-baselining AC15's pinned literal (`.shell-team/specs/T-1004-optin-hook-sample.md`'s
  `check:` line and its quoted `$C` string) to match — a mechanics-only,
  meaning-preserving repair of the same shape as this task's own AC2 v1→v2
  precedent.

### Adversarial findings

- See "Adversarial pass" above — the sole material output of the adversarial
  round was a severity correction on the one primary finding, not a new
  defect. No additional issue surfaced when Codex was asked to break the two
  doc sections and the sample on its own terms.

### Issue-canon verification (not environmentally-unverified this round)

`github.com` / `api.github.com` were reachable from this sandbox this round
(`curl` succeeded where `gh api` failed on a local TLS-verify error). I
fetched issue #44 and issue #37 directly rather than relying solely on the
spec's and board's paraphrase:

- **#44** ("scrum-master's citation rule does not name an interventions-file
  reference..."): its stated fix shape — "one clause added to the citation
  sentence... bundleable into whichever task next touches that Output
  section — T-1004 is the natural carrier" — matches AC16's implementation
  exactly (one clause, `agents/scrum-master.md`'s existing sentence, T-1004
  as carrier). Confirmed, not paraphrase-trusted.
- **#37** (the parent tracker, "the retro has no channel for the moments a
  human intervened"): its own account of directions 1–3 and the T-1001-era
  intervention evidence matches this spec's Problem section framing. Confirmed.

### Standing, dispositioned items — reviewed, not re-litigated

- **QA's three out-of-input-space risk notes** — I agree with all three
  classifications:
  1. Open-stdin-never-closes hang: correctly out of scope. The spec's Input
     space class 3 enumerates stdin states exhaustively (documented payload /
     arbitrary text / garbage / empty / **closed**) and does not include
     "never closes"; the external hook contract is a declared, untestable
     Assumption, and a harness that writes-then-closes stdin (the documented
     contract) does not produce this state.
  2. The check-handoff-malformed line with an embedded well-formed segment:
     correctly out of scope, matching the spec's own out-of-scope item 1
     verbatim ("a crafted line built to make the ERE backtrack... linted by
     `bin/check-handoff.sh` in CI") — QA independently confirmed the line
     fails `check-handoff.sh`'s own grammar, so it cannot survive into a
     CI-clean board.
  3. The fenced-code documentation example with a concrete id/flag: this one
     is genuinely **not addressed** by the spec's Input space in either
     direction (neither declared reachable nor declared out-of-scope), which
     is exactly how QA characterized it — a disclosed risk note, not a FAIL
     driver, since it requires an adopter to hand-author non-placeholder
     board-format prose. I concur this is correctly non-blocking as scoped,
     and record it here rather than promote it, since neither the spec nor
     QA staked out ground to promote it against.
- **Contain-mode canonical-shrink asymmetry, backtick-lock blind spot (#41),
  AC6/AC8-vs-AC7 boundary** — pre-existing, T-1003-recorded, unrelated to
  this diff. No new instance found in this task's changes.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `$HOME` unquoted in the install snippet's `"command"` value (both docs) | major (Codex-native `P2`) / adversarial judgment: "minor portability/documentation defect, not blocker" | minor | Real, reproducible shell word-splitting defect, but low real-world likelihood (default macOS/Linux home paths), contained blast radius (one optional/inert, already-opt-in feature — no regression to any shipped default), and partial diagnosability (Claude Code's hook-error notice, not true silent failure) together move it below the bar the primary label alone suggested. |

### Recommendation

- APPROVE → set tasks/todo.md to READY_FOR_MERGE

### Fast-follow disposition

- `$HOME` unquoted in the install snippet (`docs/tuning-oversight.md:136`,
  `docs/tuning-oversight.ja.md:103`) — file-an-issue. Rationale: genuine
  portability defect with a small, well-understood fix
  (`"bash \"$HOME/.claude/hooks/interventions-reminder.sh\""` in both
  language files), but the fix touches AC15's frozen literal and therefore
  needs the same mechanics-repair re-freeze path this task's own AC2 v1→v2
  change already established — not a same-session doc edit, and not urgent
  enough (low likelihood, no regression to the shipped default) to hold this
  merge for.

