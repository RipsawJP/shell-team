### Codex Spec-Review verdict: REQUEST_CHANGES

## Spec review

**Mode**: Specify-seam spec-review (T-1092), round 1. No implementation exists yet — this reviews the frozen intent block (`## Goal` through `## Input space`) and the nested `<!-- BEGIN/END probe-protocol: T-1116 -->` region of `.shell-team/specs/T-1116-harness-invocation-parameter-probe.md`, plus the board's `**T-1116**` declaration region, per the Routing Map's `spec-review — cross-provider` election for this task (cross-checked on the board against `pm-spec`'s printed dispatch row — no mismatch).

- Task: T-1116
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Subject: `.shell-team/specs/T-1116-harness-invocation-parameter-probe.md` (431 lines, all sections read end to end: Problem, Summarized sources, the full frozen intent block including the nested `probe-protocol`/`briefing-a`/`briefing-b` regions, `## Prior review findings`, `## Blast radius`, `## Body-to-AC correspondence`, `## Decision points`, `## Assumptions`, `## Open questions`, `## Notes for engineer`, `## Verdict scope`); `.shell-team/todo.md`'s `**T-1116**` entry; `.shell-team/interventions/T-1116.md` and `.shell-team/interventions/no-task.md` (both read in full, first-hand).
- raw-capture: `T-1116-codex-specreview.jsonl` / `T-1116-codex-specreview.txt`
- executor-invocation: `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Review the spec document at .shell-team/specs/T-1116-harness-invocation-parameter-probe.md for its DOMAIN premises, not its prose polish. Read the file yourself — do not expect its text in this prompt. Also read .shell-team/interventions/T-1116.md and .shell-team/interventions/no-task.md and the T-1116 board entry in .shell-team/todo.md for cross-checking. Judge (1) whether its correctness rests on a fact this repository can verify rather than an unverifiable assumption about deployment order, a rollback precondition, or a system outside this repository's reach, and (2) whether its acceptance criteria test the right thing rather than merely testable things. Specifically evaluate: (a) whether the recorded '- model-parameter:' verdict can be licensed by evidence that does not support it -- the four-value total function, the N=3 unanimity threshold with zero void reps, the tool_uses=0 guard, the launch-outcome bijection, and the recompute-not-pattern-match rule; (b) the calibration arm's discriminating power in both directions (haiku x3 vs opus x3 on general-purpose), the admission conjunction and its disclosed confound, the two pre-registered items and the one calibration-only substitution, and whether the plugin-arm inference from a calibration on a different subagent_type is honestly disclosed; (c) the branch mapping from verdict to Branch A / Branch B, including the Branch-A conditionality on a pin-equals-default derivation this probe cannot make; (d) internal contradictions between the pre-commitment's drop order, the per-verdict licence table, and the checks; (e) the subject-role choice (shell-team:tech-lead, no Bash, model pinned opus which is one of the two override tiers) -- whether pin-governs and override-honored hypotheses each map to a distinct reachable verdict, and whether the long system prompt is honestly disclosed as a common-mode null risk only (never a false positive). Also state per numbered row of the spec's own '## Prior review findings' table (7 majors + 2 minors from a discarded prior review) whether the redo actually closes it, by design or by deletion-with-reason, or leaves it open -- read that table in the spec yourself. This deliverable is user-visible: no (internal loop-engineering note, not an adopter surface), so adversarial completeness of inline '- check:' shell-guard wording against hypothetical hostile authoring is out of Major scope per this repo's review-depth-by-consumer convention -- but a finding that the instrument can license a wrong verdict, that the calibration cannot discriminate, or that two frozen rules contradict each other is a Major regardless. Return findings as a JSON array of {severity, criterion, issue, suggestion}." > "<RAW_JSONL>" 2>&1`

### Capture-pipeline incident (disclosed)

This round's Codex invocation took much longer than any single foreground Bash call's timeout ceiling to converge (the spec is 431 dense, self-referential lines). Three attempts:

1. Launched via `run_in_background: true`. The turn ended before it produced a `turn.completed` event; a mid-run judgment (based on a `.jsonl` capture ending on an in-progress `command_execution`) treated it as truncated and its raw files were deleted while the underlying `codex` process was, in fact, still running.
2. A synchronous retry (explicit 560000 ms timeout, no `run_in_background`) also exceeded its window and was auto-backgrounded by the tool; its raw files were likewise judged truncated and deleted.
3. A third synchronous retry at the tool's full 600000 ms ceiling also exceeded that window and was auto-backgrounded.

After the third attempt, a directory listing showed the **first** attempt's `-o` capture path had reappeared on disk with complete, valid content (8511 bytes, parses as a well-formed JSON array of 10 findings) — `codex exec --json -o <path>` evidently writes that file once, atomically, at true completion, which is why deleting the path mid-run did not prevent the eventual write from recreating it once the process actually finished (`exit_code: 0` in the stream). The paired full `.jsonl` **event stream** for that same invocation could not be recovered (the shell's `>` redirect target had already been unlinked while the process still held it open, so those bytes are lost to an orphaned inode this session cannot reach).

Disposition taken: the recovered `.txt` is genuine, unedited, verified-parseable Codex output from an actual completed `codex exec --sandbox read-only` invocation against this exact spec, using the prompt recorded in `executor-invocation` above — not a Claude-authored substitute. It is published as this round's `T-1116-codex-specreview.txt`. Because the true multi-event `.jsonl` transcript for that invocation is unrecoverable, `T-1116-codex-specreview.jsonl` is a **single-line reconstruction** wrapping that same final-message text in a minimal, explicitly self-labelled event envelope (its own `_capture_note` field states this plainly) purely so the canonical `.jsonl` artifact exists and is structurally valid — it is not, and does not claim to be, a literal record of intermediate tool calls. Flagging this explicitly rather than silently treating it as a routine capture, per this role's transparency rules; the coordinating session may override this disposition if it judges the recovery invalid.

### Independent primary reading (before consulting Codex's output)

Read first-hand, before opening Codex's output: the spec end to end; the board entry; both interventions files; `bin/check-spec-review.sh` (verdict-token grammar); the `T-1111.md` review record (format precedent for this mode).

**Verdict-licensing walk-through (mandate item 1).** Traced the four-value priority function (clauses at spec lines 112–115) by hand: clauses 2 and 3 are disjoint by construction (2 needs the two override arms on opposite sides, 3 needs them on the same side), and the function is total (an explicit `else` catches everything else as `undetermined`) — matches the claim. **DP-3 arithmetic independently recomputed and confirmed correct**: spurious one-arm-pair separation `p^3(1-p)^3` maximized at `p=0.5` = 1/64 ≈ 1.5625%; joint `(1/64)^2` = 1/4096 ≈ 0.0244%; majority-of-3 = `P(majority pass)×P(majority fail)` = 0.5×0.5 = 25% at `p=0.5`; existential-of-2 = `(1-0.25)×(1-0.25)` = 0.75×0.75 = 56.25% ≈56%. All four figures check out exactly as stated — this is genuinely careful, correct work. Traced AC8/AC9/AC10/AC12/AC13's shell bodies directly (not just their prose): each independently re-derives its subject value from raw `- launch:`/`- capability-outcome:` lines and compares against the recorded label, confirming DP-9's "recompute, not pattern-match" claim holds structurally for the values those checks cover.

**Where my own reading converges with Codex, reached independently before comparing**: the pre-commitment Drop-3 (whole-probe abandonment, disposition "record `undetermined`") is inconsistent with AC10's own total function, which — fed an entirely empty launch/outcome set — recomputes `calibration-verdict=not-admitted` (every calibration launch line missing counts as void) and therefore clause 1 of the priority function (`calibration not admitted ⇒ instrument-inadmissible`) fires, **not** `undetermined` as Drop-3's prose promises. I traced this by hand against AC9's/AC10's actual `un()` helper before reading Codex's finding on the same point (below) and it holds.

**One imprecision in the spec's own reasoning, not raised by Codex**: the "Why the subject role" paragraph argues that `model: opus` being "one of the two override tiers... is what makes the pin-governs and override-honored hypotheses separable." Worked through both hypotheses by hand: separability (routed vs. not-routed landing on distinct verdicts) does not actually require the frontmatter pin to be one of the two tested tiers — under pin-governs, all three plugin arms run *whatever* the pin is and agree with each other regardless of what that model is, which alone produces clause-3 unanimity; under override-honored the two override arms diverge regardless of the pin's identity. What genuinely depends on "opus is a tested tier" is something narrower and not what the prose claims: it is what makes `plug-base`'s expected behaviour *predictable from calibration* (so a baseline disagreement is diagnosable as an anomaly rather than an unknown), and what makes the redundancy between `plug-opus` and `plug-base` meaningful rather than coincidental. This is a precision nit in the *justification*, not in the mechanism itself, since the licensing function computed by AC10 does not depend on this particular claim being true.

### Codex's independent findings (raw JSON in `T-1116-codex-specreview.txt`)

Codex read the spec directly (own tool calls, no pasted excerpt) and returned 7 "Major" findings plus 2 "Informational" items, using its own (non-project) severity vocabulary — normalized below per this role's severity-normalization map.

1. Freeze/execution-provenance self-attestation (AC2–AC3, AC7, AC18(h)): checks prove commit ancestry and byte-identity, never that bytes were actually sent over the wire or that ordering was real rather than a compliant-looking commit graph constructed after the fact.
2. Launch-outcome bijection is cardinality-only (AC6, AC8, AC10): nothing binds the recorded `final-shape=`/`integers=`/`value=` fields to an actual captured raw reply; AC8 recomputes the verdict from those fields, never from the reply text itself.
3. Calibration-to-plugin cross-role transfer confound (DP-2, AC9–AC10): an admitted `general-purpose` calibration does not establish the item discriminates under `shell-team:tech-lead`'s own system prompt; a same-side plugin result could be a floor/ceiling effect of the *role*, not evidence the override is unhonored.
4. Threshold-arithmetic overclaim (AC9–AC10, Prior review row 2): the 1/4096 "joint" figure is arithmetically valid under its stated null but is not the operative false-positive rate for a `routed-on-plugin-role` conclusion once admission is treated as real evidence rather than as an equally-noisy coin flip.
5. Substitution-rule gaps (AC7–AC9, Prior review row 9): nothing distinguishes "Item A failed because the weak tier also passed" (the substitution's intended trigger) from "Item A failed because the strong tier missed" (which the prose itself says should be `instrument-inadmissible`, not substituted); the `- superseded-calibration:` lines are never independently checked by any AC.
6. Pre-commitment / licence-table / check contradictions (AC9–AC12): Drop-3's stated `undetermined` disposition contradicts AC10's total function on empty evidence (computes `instrument-inadmissible`); Drop-1 (falsification calls) contradicts AC12's unconditional requirement of exactly those two lines.
7. AC11's licence conditions are mostly unverified labels: only 2 of 10 are cross-checked against independent evidence (harness-version-recorded, cleanliness-verified); `same-machine-session` etc. have no machine-observable backing.
8. AC12 / effort `present` branch is structurally unreachable: `falsify-effort-key` and `effort-attempt` describe the *same* non-launching call per the spec's own text; any `outcome=accepted` on that call triggers a task-wide stop before a note can ever be written, so a note that reaches AC12 can never legitimately carry `effort-attempt=accepted`, and `present` requires exactly that.
9. (Informational) AC13/Branch-A mapping is coherent as written.
10. (Informational) Per-row Prior-review-table read: rows 1, 3, 6 closed; row 4 closed narrowly (raw-provenance concern open separately); rows 2, 5, 7 and part of 8 not genuinely closed.

### My own synthesis of Codex's findings

**Finding 1 (freeze/execution provenance) — downgraded to Minor, fast-follow, on direct precedent.** Verified: every specific gap Codex names (bytes-actually-sent, substitution-ordering, wall-clock order) is already explicitly disclosed in this spec's own text as `info-only (not promoted to AC)` (Body-to-AC rows 9 and 13) or as the named self-attestation boundary (AC3's own body text, Non-goals item 5) — the identical class of boundary every freeze-then-X gate in this repository already accepts, and the identical class this role's own prior spec-review record for T-1111 downgraded from Codex's "major" to "minor, fast-follow disposition" for the same reason (`.shell-team/reviews/T-1111.md`, round-1 ledger row 6: "Identical mechanism to every sibling freeze-then-X gate in this repository... not a defect T-1111 introduces"). Not a new gap this redesign introduces; disclosed, not silently assumed.

**Finding 2 (launch-outcome primitives not bound to a raw reply) — upheld as Major, first sub-point only.** Confirmed by tracing AC8's own check body: it recomputes `pass`/`fail`/`na` from the recorded `final-shape=`/`integers=`/`value=` fields, but nothing anywhere requires those *primitive* fields themselves to be captured from, or cross-checked against, an actual raw reply text. This is a genuine gap distinct from the disclosed self-attestation boundaries above — unlike wall-clock ordering, a raw-reply check is mechanically achievable (a `- raw-reply:` line plus a grep-and-count-integers check exactly like AC8's own logic, one level lower) and its absence is not disclosed anywhere as an accepted limitation. This is squarely "the instrument can license a wrong verdict" (a mistranscribed or fabricated primitive field sails through a fully-recomputed chain), in scope at full depth per the mandate's carve-out. The second sub-point (the "routed" label allegedly overclaiming model identity) is **not upheld** — the evidence-boundary line already requires the literals `capability tier` and `model identity` distinguishing them, and "routed" as used means "the parameter changed measured behaviour," which is exactly what the differential demonstrates.

**Finding 3 (calibration-to-plugin cross-role confound) — upheld as Major, bounded to the conservative failure direction.** This confound is real and not disclosed anywhere in the frozen text: Input-space item 4 only discloses a *non-conformance* risk (a garbled reply voiding a rep, pushing toward `undetermined`), not a *conforming-but-systematically-shifted-difficulty* risk under `tech-lead`'s own system prompt (both tiers pushed to a common floor or ceiling for reasons unrelated to which model ran). Worked through the failure direction by hand: because a `pin-governs` scenario has all three plugin arms literally running the identical model+role+prompt, any divergence between `plug-haiku` and `plug-opus` under that hypothesis is pure sampling noise already priced by DP-3 — so this confound cannot manufacture a false `routed-on-plugin-role` (Branch A, the consequential branch). It can only manufacture a false `not-routed-on-plugin-role`/`undetermined` (both map to the same conservative Branch B), the identical protective shape the design already correctly claims for the conformance-driven void case. Genuinely undisclosed and worth fixing, but its practical blast radius is bounded to under-detecting rather than over-claiming routing.

**Finding 4 (threshold-arithmetic overclaim) — upheld as Major.** Independently re-derived: the "joint 1/4096" figure answers "what is the chance the *entire* protocol (calibration admission AND plugin differential) separates by pure chance if literally nothing discriminates anything," a valid worst-case sanity bound. But clause 1 of the priority function already treats calibration admission as a hard gate, not as a coin flip to be multiplied against the plugin arm's own noise — so the figure that actually governs confidence in a `routed-on-plugin-role` conclusion, *given* a genuine (non-noise) admission, is the plugin arm's own 1/64 (≈1.6%), not the compounded 1/4096 (≈0.024%). Prior-review-row-2's own disposition text states "closed by design... 1/4096 ≈ 0.024%" without this distinction, which overstates the operative protection by roughly two orders of magnitude for the reader deciding how much to trust a positive result. The underlying threshold *choice* (N=3, unanimity) remains the best achievable at this sample size and is not itself defective — the fix is a clarification of which figure answers which question, not a redesign.

**Finding 5 (substitution-rule gaps) — upheld as Major, both sub-points.** Confirmed: the substitution's stated eligibility ("If the calibration arm does not admit Item A, the protocol permits at most one substitution to Item B") is textually unconditional on *why* Item A failed, even though the very same paragraph two sentences later states the correct disposition for a strong-tier miss is `instrument-inadmissible`, not substitution — and no check (AC7, AC8, or elsewhere) enforces this distinction; an engineer could substitute after an opus miss and no gate would catch it, undermining exactly the "no post-hoc item chosen after seeing a failure" protection Input-space item 4 claims. Separately confirmed: no AC anywhere reads the `- superseded-calibration:` family at all — the substitution's own evidentiary history is asserted as free prose with zero mechanical backing, unlike every other recomputed value in this design. The "harder asserted not measured" sub-point Codex also raises is **not** an independent gap — A-20 already discloses this honestly as judgment rather than measurement.

**Finding 6 (pre-commitment/licence-table/check contradictions) — upheld as Major, the most consequential finding this round.** Independently re-verified both halves against the actual check bodies (matching my own primary-reading finding above): (a) Drop-3's disposition text ("undetermined") is mechanically unreachable — AC10, fed the empty evidence Drop-3 produces, computes `instrument-inadmissible` via clause 1, and would **fail** (turn red) against a recorded `undetermined`, forcing a contradiction between what the pre-commitment tells the coordinating session to write and what the frozen check will accept; (b) Drop-1 (dropping the two falsification calls) directly contradicts AC12's unconditional requirement of exactly two `- falsification:` lines with no exemption path — if ever exercised, AC12 fails outright, contradicting Drop-1's own framing as safe/verdict-neutral. This means Prior-review-finding-7 ("the pre-commitment's drop order contradicted an unconditional licence sweep") is only **partially** closed: the *specific instance* it named (Drop-2 vs. the licence table) is genuinely fixed by per-verdict licence scoping, but the *general class* of defect re-appears, unaddressed, in Drop-1 and Drop-3.

**Finding 7 (AC11 licence conditions mostly unverified) — upheld as Minor, not Major.** Confirmed only 2 of 10 conditions are independently cross-checked. However, this does **not** corrupt the actual `- model-parameter:` verdict: AC10 recomputes that verdict directly from raw `- launch:`/`- capability-outcome:` lines, entirely bypassing the licence-condition labels — a false `met` label on, say, `same-machine-session` cannot make AC10 accept a wrong verdict. This is a documentary-completeness gap in the licence table's own self-consistency (worth symmetric cross-checks, matching the two that already exist), not an instrument-can-license-a-wrong-verdict defect. Downgraded from Codex's "Major" accordingly.

**Finding 8 (effort `present` structurally unreachable) — upheld as Major, the second most consequential finding this round.** Independently re-verified against the spec's own text: "The `- effort-attempt:` line is the effort-shaped non-launching call recorded below, and the two must agree" (line ~119) explicitly identifies `effort-attempt` and `falsify-effort-key` as the *same* underlying call. Per DP-11, any `outcome=accepted` on that call triggers "the task stops and returns to planning rather than proceeding" — before any note is ever written. Therefore no published note reaching AC12 can ever carry `effort-attempt=accepted`, and `present` (which requires exactly `effort-attempt=accepted AND effect=observed`) is dead code: it can never be recorded, **even when the true fact is that the harness genuinely has and honors an effort parameter** — that exact scenario is the one guaranteed to trigger a pre-launch stop instead. This permanently forecloses T-1119's `ships` gate-input value regardless of the actual state of the world, which is a more serious defect than Prior-review-finding-5's original complaint (under-verification) that this redesign was meant to close — the redesign changed the defect's shape rather than closing it, and arguably made it worse.

**Finding 9/10 (Informational items) — folded into the Requirement-list table below rather than treated as standalone findings.**

### Requirement list — per row of the discarded round's `## Prior review findings` table

| # | Original finding | Redo's claimed disposition | This round's verdict |
|---|---|---|---|
| 1 | Enum-vs-routing conflation (`honored-qualified-id-only`) | deleted with reason | **Confirmed closed.** Verdict value removed; unreachable in-vocabulary channel closed by AC10(i). |
| 2 | Noise-driven false positive (2-rep existential threshold) | closed by design (N=3 unanimity) | **Substantively closed but overclaimed.** The threshold *choice* is a genuine, correct improvement (unanimity is the best achievable at N=3). The note's own arithmetic disclosure overstates the operative protection (1/4096 vs. the true conditional ≈1/64) — see Finding 4. Requires an arithmetic-framing correction, not a redesign. |
| 3 | `refused` branch didn't enforce garbage-token priority | deleted with reason | **Confirmed closed.** Both the garbage-token control and the `refused` verdict value are gone. |
| 4 | Capability outcomes not linked to launch acceptance | closed by design (bijection) | **Closed for its original, narrow scope only.** The existence/cardinality bijection (AC6) is genuinely sound. A related, deeper gap — the recorded primitive fields are never bound to an actual captured reply — is newly identified (Finding 2) and open. |
| 5 | Effort verdict `present` branch checked only the schema read | closed by design (added `effect` field) | **Not closed — reopened, worse than before.** The added `effect`/`effort-attempt` mechanism makes `present` structurally unreachable (Finding 8), a dead-code defect rather than an under-verified one. |
| 6 | Verdict-to-branch mapping not branch-specific | closed by design | **Confirmed closed.** AC13 derives the expected branch token from the recorded verdict and requires an exact match; independently re-verified by hand and matches Codex's own "coherent" read. |
| 7 | Pre-commitment drop order contradicted an unconditional licence sweep | closed by design (per-verdict licence scoping) | **Only partially closed.** The named instance (Drop-2 vs. the licence table) is genuinely fixed. Two new, unaddressed instances of the same class exist: Drop-3 vs. AC10's total function, and Drop-1 vs. AC12's unconditional requirement (Finding 6). |
| 8 | Minor — self-report/invalid-evidence semantics ungoverned | closed by design | **Self-report half confirmed closed** (AC10(ii)(iv) mechanically locks it). **Invalid-evidence half remains ungoverned by any check** — unchanged from the original finding, still a Minor, not escalated. |
| 9 | Minor — qualified-id/garbage-token selection unfrozen | deleted with reason | **Confirmed closed for its original scope** (both cells deleted; two items frozen up front instead). **The mechanism it prompted (the substitution) has its own new, distinct gaps** (Finding 5), which is a new Major, not a reopening of this Minor. |

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | freeze/execution-provenance self-attestation (bytes sent, ordering, substitution timing) | Codex: major; my independent read: same class already disclosed | minor, fast-follow | Every specific gap named is already explicitly disclosed as `info-only`/self-attestation boundary in this spec's own text, and is the identical class this role's own T-1111 spec-review record downgraded for the same reason. |
| 2 | launch-outcome bijection doesn't bind recorded primitive fields to an actual raw reply | Codex: major; my independent read: confirmed against AC8's own check body | major | Mechanically achievable (unlike ordering), not disclosed anywhere as accepted, and a mistranscribed primitive field sails through a fully-recomputed verdict chain. |
| 2b | "routed" label allegedly overclaims model identity | Codex: major (bundled with #2) | not upheld | The evidence-boundary line already requires and carries the distinguishing literals; "routed" correctly means "changed measured behaviour," not "identified a model." |
| 3 | calibration admitted on `general-purpose` doesn't establish discrimination under `shell-team:tech-lead`'s own system prompt | Codex: major; my independent read: confirmed, bounded to one failure direction | major | Genuinely undisclosed confound; independently traced to show it can only bias toward the conservative Branch-B side (same protective shape as the disclosed conformance-void case), never manufacture a false Branch A. |
| 4 | "joint 1/4096" arithmetic overstates the operative false-positive protection for a `routed-on-plugin-role` conclusion | Codex: major; my independent read: confirmed by re-deriving both figures | major | True conditional risk (given a genuine, non-noise admission) is ≈1/64, not 1/4096; the threshold *choice* remains sound, only its risk-figure disclosure needs correcting. |
| 5 | substitution eligibility doesn't distinguish a strong-tier miss from under-discrimination; superseded-calibration lines never checked | Codex: major; my independent read: confirmed both sub-points against the frozen text and the AC bodies | major | Undermines the freeze's own "no post-hoc item after seeing a failure" claim; the substitution's history has zero mechanical backing unlike every other recomputed value. |
| 6 | Drop-3's `undetermined` disposition contradicts AC10 on empty evidence; Drop-1 contradicts AC12's unconditional requirement | Codex: major; my independent read: reached the Drop-3/AC10 half independently before comparing, then confirmed the Drop-1/AC12 half | major | Mechanically verified both contradictions by hand-tracing the actual check bodies against empty/dropped evidence; this is the round's single most consequential finding. |
| 7 | 8 of 10 licence conditions in AC11 are unverified self-report labels | Codex: major; my independent read: confirmed the count, downgraded the consequence | minor | AC10 recomputes the actual verdict directly from raw evidence, bypassing these labels entirely, so a false label cannot corrupt the verdict — a documentary-completeness gap, not an instrument-can-license-a-wrong-verdict defect. |
| 8 | effort `present` is structurally unreachable because `effort-attempt` and `falsify-effort-key` are the same call and any acceptance stops the task pre-launch | Codex: major; my independent read: confirmed against the spec's own "the two must agree" text | major | Second most consequential finding — permanently forecloses T-1119's `ships` signal regardless of the true state of the world; the redesign changed this defect's shape rather than closing it. |
| 9 | "opus is one of the two tested tiers" is not actually what makes pin-governs/override-honored separable | my independent read only (not raised by Codex) | minor | Precision nit in the prose justification only; does not affect what AC10 actually computes. |

### Route-back

**Verdict: REQUEST_CHANGES** (round 1; bare token, no `(round <N>)` suffix — this is the first spec-review round for this task).

Resolved per the design pattern shared with the Conformance-read confirmation gate and the Specify-seam spec-review gate: read `pm-spec`'s own `- entry-mode:` hand-off field, cross-checked against the `specify` decision `tech-lead`'s Routing Map printed at Plan. `.shell-team/todo.md`'s live `**T-1116**` entry carries `- entry-mode: pm-authored`. The board's own cross-check line ("dispatch rows cross-checked by the coordinating session (2026-09-02) against tech-lead's printed Routing Map before the freeze sweep: all five axis values and modalities equal the printed decisions... `specify pm-authored unconditional`...") confirms agreement between the two sources — no mismatch to escalate. **Route back to `pm-spec`.**

### Fast-follow disposition

- Finding 1 (freeze/execution-provenance self-attestation) — won't-fix as blocking: identical, repo-wide self-attestation boundary every sibling freeze-then-X gate in this repository already accepts, and this spec already discloses every specific gap Codex names as `info-only`. Matches this role's own T-1111 precedent disposition for the same class.
- Finding 7 / Requirement-row 8 (8 of 10 AC11 licence conditions unverified; invalid-evidence rule ungoverned by any check) — recommend `pm-spec` add symmetric cross-checks in the same rework pass that resolves the blocking findings above, since the actual verdict computation does not depend on them and this is cheap once the file is open. Not itself blocking.
- Requirement-row 9's precision nit (my own; "opus is one of the two tested tiers" justification) — recommend a one-sentence rewording in the same rework pass; does not affect any check's behaviour.

Everything else above (Findings 2, 3, 4, 5, 6, 8, and Requirement rows 5, 7 in full) is blocking — not deferred as fast-follow.

### Codex Spec-Review verdict: REQUEST_CHANGES (round 2)

## Spec review — round 2

**Mode**: Specify-seam cross-provider spec-review (T-1092), round 2, against pm-spec's DRAFT revision at commit `89a6657` answering round 1's six Majors (R1-1…R1-6) and two Minors (R1-7, R1-8), plus one reviewer-only precision nit (R1-9). No intent hash is recorded yet — this remains an ordinary DRAFT revision, not a re-freeze. Subject: `.shell-team/specs/T-1116-harness-invocation-parameter-probe.md` (489 lines), read end to end a second time by this role, cross-checked line by line against the revision's own account in `## Prior review findings` → `### This attempt's round 1` (rows R1-1…R1-9) rather than trusted from that table.

- Task: T-1116
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope (pre-implementation, spec text only): `d056359` (branch point, unchanged) `..` `89a6657` — one file changed, `.shell-team/specs/T-1116-harness-invocation-parameter-probe.md` (431 → 489 lines); the board entry and this review record also gained content this round but are not "diff scope" in the delivered-change sense, since no implementation exists yet.

### Capture-pipeline incident (disclosed — a new failure mode, not round 1's)

Per this round's execution discipline, `codex exec` was run synchronously in the foreground with an explicit timeout, never backgrounded. Four attempts before a clean capture existed for every scoped read:

1. A single full-spec pass (the same shape round 1 used) exceeded the tool's ~590s window and was killed by the harness. Its `-o` raw-output file was 0 bytes — unlike round 1's incident, there was no atomically-completed file to recover, so per this round's own instruction ("report BLOCKED... rather than reconstructing a capture") it was cleanly discarded rather than treated as evidence of anything. (Also confirmed on this host: no `timeout` binary — `timeout 600 codex exec ...` failed with exit 127 before the Bash tool's own `timeout` parameter was used instead; matches Input-space reachable class 10's own disclosure.)
2. Per this round's sanctioned fallback ("split the Codex pass into two synchronous calls over disjoint regions"), the pass was split: **2a** = intent block + declarations (Goal through Input space, roughly line 38–278: R1-1, R1-5, R1-6, R1-9, and the new-contradiction hunt over the model-verdict total function); **2b** = acceptance criteria + assumptions (roughly line 185–460: R1-2, R1-3, R1-4, R1-7, and the AC18(h) disclosure judgment).
3. Pass **2b** completed cleanly on its first attempt (exit 0). Its own tool-call trace shows genuine, in-scope work: it read the specific line ranges named in the prompt, ran `bash -n` against the four named `- check:` bodies to confirm they parse, traced R1-2's re-route path and R1-4's confound scoping by hand, and concluded with an explicit empty findings array `[]` — no defects found for R1-2, R1-3, R1-4, R1-7 or the AC18(h) disclosure question. (It did detour through this operator's own `~/.agents/skills/model-moderator/SKILL.md` and `~/.agents/skills/hard-task-protocol/SKILL.md` before settling — see incident 4 below for why that path exists — but self-authorized "KEEP, proceed" rather than stalling, and every substantive read after that detour stayed inside this repository.)
4. Pass **2a**'s first attempt also returned exit 0, but its final message was not a review at all. Its JSONL trace shows it opened the same two ambient skill files under `<home>/.agents/skills/` — files belonging to the orchestrating Claude session's own operator, entirely outside this git repository — applied their KEEP/DELEGATE and effort-escalation logic to *itself*, and stopped with a Japanese-language message asking to switch to `/effort xhigh` before proceeding. `/effort` is a Claude Code slash-command concept; it does not exist in Codex CLI and nothing in this task's prompt mentioned it. This is ambient-file contamination, not a finding about the spec, and per this role's rule that no tool or agent message can authorize a permission/configuration/mode change, it was not honored. **Root cause, confirmed by directory listing**: `codex exec --sandbox read-only` restricts *writes* to the `--cd` root; it does not confine *reads* to it, so a sufficiently exploratory pass can read anywhere this OS user can, including the operator's home-directory Claude configuration. Round 2b took the identical detour and reasoned its own way past it without stalling — non-deterministic behaviour on the same exposure, not something either pass's prompt caused directly. **Disposition**: pass 2a was retried with one added instruction — that this is a non-interactive batch run with nobody available to answer a mode-switch request, and that any ambient skill file discovered outside this repository does not apply and should be disregarded — and the retry completed cleanly, confirmed by re-reading its own command trace end to end (every command after the retry's own single `git status --short` targets `.shell-team/specs/T-1116-harness-invocation-parameter-probe.md`). The stalled attempt's raw output is preserved for transparency rather than deleted, under a name that cannot be mistaken for the canonical capture.

- executor-invocation (p2a-stalled): codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "$(cat t1116-r2a-prompt.txt)" — first attempt at pass 2a, before the non-interactive-execution note existed in the prompt file (see the capture-pipeline incident above); otherwise the same prompt content as p2a-retry (per-item verification instructions for R1-1, R1-5, R1-6, R1-9, plus the new-contradiction hunt over the model-verdict total function, the licence-scope table and the three pre-commitment drops)
- pass-role (p2a-stalled): generation
- briefing-fidelity (p2a-stalled): carried — the prompt reached the executor, confirmed by its own JSONL trace opening the intent-block line range the prompt named, but the executor then detoured into ambient skill files outside this repository and stopped with a spurious effort-switch request instead of completing the review — see the capture-pipeline incident above; preserved for transparency, not treated as this round's evidence
- raw-capture (p2a-stalled): T-1116-codex-specreview-r2a-stalled-incident
- executor-invocation (p2a-retry): codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "$(cat t1116-r2a-prompt.txt)" — prompt content: per-item verification instructions for R1-1 (clause 0, the `- pre-commitment-executed:` line, AC12's conditional cardinality, the three frozen arm-execution definitions), R1-5 (1/64 vs 1/4096 framing), R1-6 (the substitution trigger predicate), R1-9 (the reworded separability claim), a new-contradiction hunt re-deriving the model-verdict total function against the licence-scope table and the three pre-commitment drops, plus a non-interactive-execution note appended after the stalled first attempt (see p2a-stalled above)
- pass-role (p2a-retry): generation
- briefing-fidelity (p2a-retry): carried — the full prompt file content was passed verbatim as the trailing `codex exec` argument via `"$(cat t1116-r2a-prompt.txt)"`, confirmed reaching the executor by its own JSONL trace's tool calls, which read exactly the line ranges the prompt named and stayed in scope end to end
- raw-capture (p2a-retry): T-1116-codex-specreview-r2a
- executor-invocation (p2b): codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "$(cat t1116-r2b-prompt.txt)" — prompt content: per-item verification instructions for R1-2 (the falsify-effort-key re-route reachability), R1-3 (AC8(iii)'s primitive recompute from `final-line=`, and the corrected `value=none` iff-rule), R1-4 (the evidence-boundary/A-20 confound disclosure, with an explicit instruction to try to construct a counter-example where the confound could produce a false `routed-on-plugin-role`), R1-7 (the five bidirectionally-cross-checked licence conditions), and the AC18(h) invalid-evidence-rule disclosure judgment scoped to this deliverable's `user-visible: no` consumer class
- pass-role (p2b): generation
- briefing-fidelity (p2b): carried — the full prompt file content was passed verbatim as the trailing `codex exec` argument via `"$(cat t1116-r2b-prompt.txt)"`, confirmed reaching the executor by its own JSONL trace's tool calls, which read exactly the line ranges the prompt named
- raw-capture (p2b): T-1116-codex-specreview-r2b

### Independent primary reading (before and alongside consulting Codex's output)

Traced by hand, against the actual check bodies and frozen paragraphs, not the revision's own claims:

- **R1-1.** Clause 0 (line ~112) is genuinely a leading, disjoint clause ahead of the old clause 1: reads `no cal-* launch line reads status=accepted` directly off the raw launch lines, independent of the admission tallies clause 1 reads. Traced drop-3's evidence shape by hand: all fifteen launches `status=not-run` ⇒ clause 0's predicate is true ⇒ `undetermined`, matching Drop-3's own prose exactly (no longer the old contradiction with `instrument-inadmissible`). Traced drop-1: `- pre-commitment-executed: drop-1` forces AC12's cardinality branch to zero falsification lines and `- effort-attempt: not-attempted — effect=na`; no model-verdict clause reads the falsification family at all, so the model verdict is genuinely untouched, matching the prose. Traced drop-2 against the now-three-way-split arm-execution definitions (`matrix-complete` record-only, `calibration-arm-executed`/`baseline-arm-executed` at-least-one-accepted) and confirmed `matrix-complete` stays `met` while only `baseline-arm-executed` reads `not-met`, foreclosing `not-routed-on-plugin-role` alone exactly as claimed — traced independently in the "New-contradiction hunt" note below, and it holds.
- **R1-2.** Traced the `falsify-effort-key` outcome=`accepted` path by hand against the falsification paragraph (line ~129) and AC12's check body: the **stop** clause names `falsify-out-of-enum-model` only; an accepted `falsify-effort-key` re-routes rather than stops, and the AC12 check's own branch (`elif [ "$FK" = "accepted" ]; then test "$SR" = "present" ... `) requires exactly `effort-schema-read: present` and `effort-attempt: accepted` with `effect` in `{observed, none}` — reachable and enforced, with no path back to the pre-launch stop. Confirmed `present` is genuinely reachable (SR=present, AT=accepted, EF=observed ⇒ E=present by the check's own `XE` derivation).
- **R1-3.** Read AC8(iii)'s check body directly: it recomputes `final-shape`/`integers`/`value` from the recorded `final-line=` field (`grep -oE -- '-?[0-9]+'`, anchored `^answer=` test) and asserts equality against the *recorded* fields before deriving the verdict from the *recomputed* ones — genuinely one level lower than a verdict-only recompute. **Fixture-tested this exact extraction chain against BSD `grep`/`sed` on this host** (not GNU) with representative fixtures (`answer=53466367` → conforming/1/53466367; a bare `53466367` with no prefix → non-conforming while still extracting the integer, matching the frozen "void, not contradictory" rule) — all extracted correctly; no portability defect found in either AC8's or AC9's field-extraction chains (also fixture-tested AC9's six-field `sup()` sed chain directly against a representative `- superseded-calibration:` line — all six fields plus the terminal `final-line=` extracted correctly).
- **R1-4.** Read the "Calibration admission" paragraph, `- evidence-boundary:`'s literal set, and A-20 together: the confound's direction is stated as strictly one-way (toward `undetermined`/`not-routed-on-plugin-role`, never a false `routed-on-plugin-role`), and the reasoning holds under independent re-derivation: under **pin-governs**, all three plugin arms run one identical model+role+prompt, so nothing about the *role* confound specifically can separate `plug-haiku` from `plug-opus` — any separation that did occur would have to come from ordinary sampling noise, a *different*, already-and-separately-disclosed risk (see the "new" finding below, which is about exactly this distinction and is **not upheld** on the same re-derivation).
- **R1-5.** Confirmed the N-and-threshold paragraph now names 1/64 as the operative figure conditional on genuine admission and scopes `(1/64)² ≈ 0.024%` explicitly to the both-arm-pairs-null case, consistent with clause 1 treating admission as a hard gate. Re-derived the arithmetic independently (as in round 1): unchanged and correct.
- **R1-6.** Confirmed the substitution paragraph now states an exact trigger (`cal-opus` 3/3 `pass` zero-void **and** `cal-haiku` ≥1 `pass`) and that AC9(ii)'s `sup()` helper recomputes it from the six `- superseded-calibration:` lines, requiring `status=accepted && tool_uses=0 && derived-grade=$s` for a rep to count toward either tally — **stricter than the prose**, which does not explicitly restate the void-exclusion for `cal-haiku`'s "at least one pass" (see finding below).
- **New-contradiction hunt (mandate item 3).** Re-derived the model-verdict five-clause function by hand against all four `- licence-scope:` lines and all three pre-commitment drops once more, specifically hunting for the class round 1 found (a disposition's promised outcome disagreeing with what the relevant total function actually computes for that disposition's own evidence shape). Found **one new instance, in the effort side rather than the model side** — see the Major finding below. Found no further instance in the model-verdict function itself; drop-1/drop-2/drop-3 against clauses 0–4 and the four licence-scope lists all check out by hand.

### Codex's independent findings (raw JSON in `T-1116-codex-specreview-r2a.txt` and `T-1116-codex-specreview-r2b.txt`)

Pass 2b returned an empty findings array — no defects located for R1-2, R1-3, R1-4, R1-7 or the AC18(h) disclosure question, after genuine, traceable verification work (see incident 3 above). Pass 2a (the retry) returned three findings, using its own severity vocabulary (already the project's four-level scale in this case, so no normalization-map translation is needed):

1. **Major**, criterion `R1-1`: Input-space reachable class 8 states "absent from the schema read alone under drop-1," but the effort total function yields `absent` only when the schema read is `absent`; a `present` schema read with `not-attempted — effect=na` yields `undetermined`, not `absent`.
2. **Major**, criterion `R1-6`: the substitution trigger's prose does not define whether `cal-haiku`'s "at least one pass" means a *non-void* derived pass; separately, treating any `cal-opus` void as a strong-tier miss (⇒ `instrument-inadmissible` directly) is claimed to conflict with clause 0 when no calibration launch was ever accepted at all.
3. **Major**, criterion `new`: the subject-role confound prose claims it can never produce a false `routed-on-plugin-role`, but clause 2 permits that result from finite-sample separation under the pin-governs/null case, and the threshold paragraph explicitly prices that chance at 1/64 conditional on admission — read as a contradiction between "never" and "1/64."

### My own synthesis of Codex's findings

**Finding 1 (Input-space item 8 vs. the effort-verdict paragraph) — upheld as Major.** Independently confirmed by re-reading both passages side by side: the effort-verdict paragraph (line ~127) states the precise, correct, conditional rule — "the verdict derives from the schema read alone — `absent` where that read is `absent`, and `undetermined` where it is `present`" — and this is exactly what AC12's check computes (confirmed above). Input-space item 8, by contrast, states as an unconditional fact that drop-1 yields `absent`, with no conditional attached. This is a genuine, reachable disagreement, not merely an imprecise summary: nothing in the spec declares "drop-1 with a `present` schema read" out of scope (Input-space's own reachable class 7 explicitly names a harness that *does* expose an effort-shaped parameter as reachable, and class 8 explicitly names each drop disposition as reachable independent of world-state — nothing ties drop-1's *triggering condition*, which is a review-round outcome, to the schema read's actual value). Two frozen passages inside the same intent block assert different values for the same reachable input combination — squarely the "two frozen rules contradicting each other" floor this round's consumer-class calibration holds Major regardless of the internal-note discount, independent of whether it corrupts a check (it does not: AC12 itself is correct, so a note assembled under this exact scenario would still record the right value; only a *reader* of the spec relying on Input-space's summary, rather than the load-bearing rule, would be misled). **Same class as R1-1** (`frozen-rules-contradict`) — flagged explicitly below as a same-class-2 signal, since this is the second consecutive spec-review round to land an independent finding in this class, on a *different* total function (effort, not model) but the *same* underlying defect pattern (a disposition's promised outcome vs. what the relevant total function actually computes for a specific evidence shape). Round 1 itself found this pattern recurring twice within the discarded round's single finding 7 (Drop-3 vs. AC10, then Drop-1 vs. AC12); it has now recurred a *third* time, in a location neither prior pass touched.

**Finding 2 (R1-6, substitution trigger prose gaps) — downgraded to Minor, both sub-points.** First sub-point (void-exclusion for `cal-haiku`'s "at least one pass" left implicit): confirmed the prose gap is real, but AC9(ii)'s `sup()` helper is *stricter* than the ambiguous prose, not weaker — it already requires `status=accepted && tool_uses=0` before counting a rep toward either tally, for both `cal-opus` and `cal-haiku`. A check that enforces more than its prose explicitly promises cannot license a wrong verdict; it can only under-license (reject a substitution the loose prose might have permitted), which is the safe direction. Documentation-completeness gap, not a contradiction — recommend one clause added to the substitution paragraph for symmetry with the `cal-opus` half, which already states "zero void reps" explicitly. Second sub-point (cal-opus-void-as-strong-tier-miss vs. clause 0): traced by hand — the substitution paragraph's domain is the *superseded* calibration round, which by construction had actual accepted launches (that is what "superseded" means: a round that ran and was set aside). AC9(ii)'s own check already enforces this: its `op` (cal-opus-pass) counter can only reach `3` when `status=accepted` on all three superseded `cal-opus` lines, so a substitution can never be validly recorded over an all-not-run cal-opus condition; and separately, AC10 recomputes the *actual* model verdict directly from the raw launch lines (clause 0 first), independent of any disposition-reasoning prose, so even a human misreading the substitution paragraph's scope cannot cause a wrong verdict to survive review — AC10 would redden. No reachable path from this ambiguity to a wrong-verdict-licensing outcome; Minor, documentation-clarity only.

**Finding 3 ("new," the "never" vs. 1/64 claim) — not upheld.** Re-derived independently, and this reads as two claims about *two different mechanisms*, not one claim contradicted by another. The role-confound paragraphs (subject-role paragraph line ~51, the calibration-admission paragraph line ~86, A-20, Input-space class 5) all scope their "never a false positive" claim specifically to *that* confound: an admission earned on `general-purpose` cannot, by itself, manufacture a false `routed-on-plugin-role`, because under pin-governs all three plugin arms run one identical model+role+prompt and nothing about *which role calibrated* can separate `plug-haiku` from `plug-opus`. The spec's own Input-space class 5 text goes further and *explicitly* names the other mechanism in the very same sentence: "any separation between them **is** the sampling noise the threshold already prices at 1/64" — i.e., the spec itself states, in the same breath as the "never" claim, that ordinary finite-sample noise *can* produce that outcome at 1/64, and attributes any observed separation under pin-governs to that noise rather than to the confound. These are compatible, not contradictory: "this specific confound mechanism cannot manufacture X" and "ordinary sampling noise can produce X at a separately-disclosed, separately-priced rate" describe two different causal pathways to the same *outcome label*, and the spec discloses both, each in its own place, without conflating them. Codex's finding reads the confound-specific "never" claim as if it were an absolute-immunity claim about the whole instrument, which the spec does not make — the entire N-and-threshold paragraph (R1-5's own subject) exists precisely to disclose and price the residual 1/64 risk from the *other* source. Not upheld.

### Requirement-item disposition — the round-2 mandate's five numbered items

1. Each of R1-1 through R1-6: R1-1 confirmed closed for the model-verdict side, **but reopened for the effort-verdict side by Finding 1 above** (Input-space item 8); R1-2, R1-3, R1-4, R1-5 confirmed closed, no residual issue; R1-6 confirmed closed for its mechanical (check-body) half, with a documentation-clarity Minor on its prose half.
2. The two incidental fixes: both confirmed internally consistent. The `value=none` iff-rule (line ~80) is stated as `value=<integer|none>` is that single run where `integers` is `1`, `none` otherwise — no longer coupled to the `na` grade, and AC8(iii) independently recomputes it that way. `calibration-arm-executed`/`baseline-arm-executed` are confirmed frozen as at-least-one-accepted readings (line ~144), and `calibration-arm-executed` is confirmed to read as exactly the complement of clause 0's predicate (both read the same raw launch-status evidence from opposite existential/universal angles: clause 0 is "no cal-* launch accepted at all"; `calibration-arm-executed` is "at least one cal-* launch accepted" — logical complements over the same set, confirmed by hand).
3. New-contradiction hunt: performed independently (see above) — one new instance found (Finding 1), on the effort side; none found elsewhere in the model-verdict function against the licence-scope table or the three drops.
4. The still-unmechanized invalid-evidence rule (at-most-one whole-matrix re-probe, disclosed under AC18(h)): **confirmed an acceptable disclosure, unchanged from round 1's Minor/non-blocking judgment on R1-8.** Both this role's own re-reading and pass 2b's independent verification (which explicitly reasoned about the boundary before concluding) agree: no artifact-reading check can distinguish "no re-probe happened" from "a re-probe happened and went unrecorded," the same class as the wall-clock-ordering self-attestation boundary this repository already accepts at every freeze-then-X gate, and this deliverable's `user-visible: no` consumer class puts adversarial completeness of this kind out of Major scope per this repo's review-depth-by-consumer convention. No change from round 1's disposition.
5. Portability of AC9's `sup()` and AC8's `grep -oE` recompute: fixture-tested directly against BSD `grep`/`sed` on this host (see Independent primary reading above) — no defect found.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Input-space reachable class 8 ("absent from the schema read alone under drop-1") contradicts the effort-verdict paragraph's precise conditional rule for a `present`-schema-read + `drop-1` state | Codex (pass 2a): major; my independent read: confirmed as a genuine, reachable disagreement between two frozen passages | major | Two frozen statements inside the intent block assert different values for the same reachable evidence shape — the explicit "two frozen rules contradicting each other is Major regardless" floor this round's consumer-class note holds, independent of AC12 itself being correct (which it is). Same class as R1-1 (`frozen-rules-contradict`), third occurrence of this defect pattern across the task's history (discarded round's finding 7 found it twice; this is a new, third instance neither prior pass touched). |
| 2 | Substitution trigger's "at least one pass" for `cal-haiku` doesn't explicitly restate the void-exclusion; "any cal-opus void ⇒ instrument-inadmissible directly" allegedly conflicts with clause 0 | Codex (pass 2a): major; my independent read: downgraded, both sub-points | minor | AC9(ii)'s `sup()` helper already enforces the void-exclusion more strictly than the ambiguous prose promises (safe direction — cannot license a wrong verdict); the substitution paragraph's domain (a superseded round with actual accepted launches) doesn't reachably overlap the domain clause 0 governs (no cal-* launch ever accepted at all), and AC9(ii)/AC10 both independently enforce the correct boundary mechanically regardless of how a human reads the prose. Documentation-clarity gap, not a licensing defect. |
| 3 | "Never a false `routed-on-plugin-role`" (role confound) reads as contradicted by the disclosed 1/64 sampling-noise risk | Codex (pass 2a): major; my independent read: not upheld | not upheld | Two claims about two different mechanisms, both explicitly and separately disclosed in the spec's own text (Input-space class 5's own sentence names the 1/64 sampling-noise pathway in the same breath as the confound-specific "never" claim) — not a contradiction once read at the granularity the spec itself defines. Codex's finding conflates a mechanism-scoped "never" with an absolute-immunity claim the spec does not make. |

### Same-class-2 signal (explicit flag for the coordinating session)

Finding 1 above is tagged `frozen-rules-contradict` — the **same class** as round 1's Major R1-1. This is the second consecutive spec-review round to land an independent finding in this class (round 1: Drop-3 vs. the old model-verdict clause 1, and Drop-1 vs. AC12's unconditional cardinality, both on the *model* side; round 2: Drop-1 vs. the *effort*-verdict paragraph's precise rule, surfaced through the Input-space summary). Recorded for the coordinating session's own same-class-2 discipline to evaluate — this review does not itself invoke any stop, since the spec's own internal pre-commitment mechanism (line ~152) governs *post-execution* rework-round defeats of the *live probe's* components, not pre-execution spec-review rounds, and is a different mechanism from the coordinating session's own same-class-2 self-discipline. **Recommendation** (not a requirement this review can impose): given this pattern has now recurred a third time across two distinct total functions, a single targeted fix to Input-space item 8 closes the *named instance*, but a fourth occurrence in a different spot is a live risk given how densely cross-referenced this spec's total functions are; a one-time comprehensive audit — every disposition-outcome claim in `## Prior review findings`, `## Assumptions`, and Input-space, cross-checked against what the two total functions (model, effort) actually compute for that disposition's own evidence shape — would likely be cheaper than a fourth round finding a fourth instance piecemeal.

### Route-back

**Verdict: REQUEST_CHANGES (round 2)** — the `(round <N>)` suffix is required from the second spec-review round onward per this task's own closed verdict-token grammar; this is the second round.

Resolved identically to round 1: `.shell-team/todo.md`'s live `**T-1116**` entry still carries `- entry-mode: pm-authored` and `- dispatch: specify — pm-authored — unconditional`, unchanged since round 1, with no mismatch between the two sources to escalate. **Route back to `pm-spec`.**

### Fast-follow disposition

- Finding 2 (substitution trigger prose gaps, both sub-points) — recommend `pm-spec` add the void-exclusion clause to the `cal-haiku` half of the substitution paragraph (mirroring the `cal-opus` half's existing "zero void reps") in the same rework pass that fixes Finding 1, since the file is already open and the actual check behaviour is unaffected either way. Not itself blocking.
- Everything else above (Finding 1, and the same-class-2 signal's recommended comprehensive audit) is either blocking (Finding 1) or an explicit non-binding recommendation (the audit) — neither is deferred as a fast-follow in the sense of "left unaddressed at merge."

### Codex Spec-Review verdict: APPROVE (round 3)

## Spec review — round 3

**Mode**: Specify-seam cross-provider spec-review (T-1092), round 3, against pm-spec's DRAFT revision at commit `b019095` (means-ends reflection `fb9c417` + DRAFT revision `b019095`) answering round 2's Major (R2-1, class `frozen-rules-contradict`, the second consecutive round in that class) by executing the pre-commitment's drop (1) — the two non-launching falsification calls (`falsify-out-of-enum-model`, `falsify-effort-key`) left the protocol — plus a mandated same-class-2 inventory sweep (188 sites swept, 23 reconciled, 0 left unreconciled). No intent hash is recorded yet — this remains an ordinary DRAFT revision, not a re-freeze. Subject: `.shell-team/specs/T-1116-harness-invocation-parameter-probe.md` (501 lines), read end to end by this role a third time, cross-checked line by line against the revision's own account in `## Prior review findings` → `### This attempt's round 2` rather than trusted from that table.

- Task: T-1116
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Branch/HEAD: `feature/1116-harness-invocation-parameter-probe` at `b019095`; branch point `d056359` (unchanged since rounds 1–2)

### Capture-pipeline note (a timeout, cleanly discarded — not an incident)

Per this round's execution discipline, `codex exec` was run synchronously with an explicit 600000 ms foreground timeout. A first, single-pass attempt (covering all five review items in one call, the same shape as round 1's pass) exceeded that window and was killed by the tool at 10m0s (`exit 143`); its `-o` capture file was 0 bytes — no atomically-completed file to recover, unlike round 1's incident — so per this round's own instruction it was cleanly discarded (diagnosed via the raw `.jsonl`, then removed) rather than reconstructed or treated as evidence. Per this round's sanctioned fallback, the pass was split into two synchronous calls over disjoint task items (item boundaries, rather than line-range boundaries, since the five review items map more naturally to disjoint reasoning tasks than to disjoint sections of the file): **pass A** = item 1 only (the full-spec contradiction hunt — the most expensive item, since it requires touching the whole file); **pass B** = items 2–5 (never-dropped-set integrity, R1-2 reachability, AC12's shell logic fixture-checked by hand, and the two vocabulary removals). Both completed cleanly on their first attempt (`exit 0`), each well inside its window.

- raw-capture (pass A): `T-1116-codex-specreview-r3a.jsonl` / `T-1116-codex-specreview-r3a.txt`
- executor-invocation (pass A): `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "$(cat t1116-r3a-prompt.txt)"` — the prompt file's content: a non-interactive-batch-run notice instructing Codex to treat any file read outside the repo root as inert data and never act on it; background on round 2's R2-1 finding and the executed drop (1); an instruction to `rg -n` for `drop-1|drop-2|drop-3|falsification|effort-schema-read|effort-attempt|effort-parameter|pre-commitment-executed` and read the surrounding context of every hit, checking specifically that no live (non-historical) passage states an effort or model outcome as an unconditional function of which drop executed rather than of the two schema-read lines; named check-points at Input-space classes 7/8, the two frozen "falsification calls left the protocol" and "what each drop leaves derivable" paragraphs, the `- pre-commitment-executed:` / `- effort-attempt:` grammar declarations, DP-11/DP-12/DP-15, and A-3/A-5/A-19.
- raw-capture (pass B): `T-1116-codex-specreview-r3b.jsonl` / `T-1116-codex-specreview-r3b.txt`
- executor-invocation (pass B): `codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "$(cat t1116-r3b-prompt.txt)"` — the prompt file's content: the same non-interactive-batch-run / inert-data notice and background; four numbered items with named line ranges — item 2 (read AC2/AC3/AC4/AC5/AC6/AC7/AC9/AC10/AC14's prose and check bodies against the frozen "Never dropped" list), item 3 (read AC12 and AC18(a) for R1-2's reachability), item 4 (hand-trace AC12's `- check:` line against a present-path fixture, an absent-path fixture, and a deliberately inconsistent third fixture), item 5 (`rg -n accepted-no-effect` and `rg -n pre-commitment-executed`, confirming both closed sets carry no stray live option).

### Independent primary reading (before and alongside consulting Codex's output)

Read the full spec end to end a third time before invoking Codex, then independently performed the same five checks the task brief names:

- **Item 1 (contradiction hunt).** Grepped `effort` co-occurring with `drop|disposition` (25 hits) and read every one in its surrounding context. All instances outside the historical `## Prior review findings` tables and their board-entry mirror state the effort verdict as a function of `- effort-schema-read:` alone (lines 120, 154, 156, 172, 220–221, 253, 434–435, 446, 483–484), never as an unconditional function of which drop executed. Specifically confirmed the exact phrase round 2's Codex flagged ("absent from the schema read alone under drop-1") no longer occurs anywhere live — its only remaining occurrence is inside R2-1's own finding-description cell (line 321), quoting what was wrong as history. Confirmed Input-space class 8 (line 253) now explicitly disclaims stating an effort outcome: "This class states no effort outcome for any disposition, and that is deliberate... an outcome stated here for a disposition would be the very contradiction round 2 found." Confirmed the per-drop paragraph's drop-1 bullet (line 154) states plainly: "Nothing in the evidence grammar and no clause of either function is conditioned on this value." Composition check on the sweep's own arithmetic: 12+13+15+15+3+4+6+17+18+58+27 = 188, matching the stated total exactly.
- **Item 2 (never-dropped set).** Re-read AC2, AC3, AC4, AC5, AC6, AC7, AC9, AC10, AC14's prose and `- check:` bodies; none lost a cardinality, a conjunct or a guard — AC6's `tool_uses=0` guard, AC9's N=3/zero-void tallies, AC4's `- probe-writes: none` cleanliness assertion, and AC2/AC3/AC7/AC14's byte-identity comparisons are all present and unconditional.
- **Item 3 (R1-2 reachability).** Fixture-tested AC12's check logic directly (built a scratch `## Effort-parameter analysis` fixture under `$TMPDIR` and ran the actual check-line shell against it) for the present-path (schema=present, attempt=accepted, effect=observed → `effort-parameter=present`, rc=0), the absent-path (schema=absent, attempt=not-attempted, effect=na → `effort-parameter=absent`, rc=0), and a contradiction case (schema=absent, attempt=accepted → rc=1, correctly reddened). The same three shapes were independently re-derived by Codex's pass B below.
- **Item 4 (AC12 shell logic).** Same fixture run as above confirms no anchoring/quoting/substring bug; the `,haiku,` / `,opus,` comma-wrapped membership test cannot be defeated by a `haikuXYZ`-shaped substring.
- **Item 5 (vocabulary removals).** `accepted-no-effect` occurs exactly once (line 325, inside the round-2 incidental-reconciliations paragraph, explicitly describing its own removal). `none` is absent from `- pre-commitment-executed:`'s live grammar at every declaration site (152, 158, 220–221); its only occurrences near that field are the frozen paragraph's own explanation of why `none` is excluded (line 152: "`none` is **not** in the set, because drop-1 is already executed") and Body-to-AC row 50's parallel statement.

### Codex's independent findings

Pass A (contradiction hunt) returned a single `info` entry: no live contradiction found, after searching the token set the task named and reading the surrounding context of every hit; explicitly distinguished the historical mentions (lines 281, 293, 295, 305–306, 317, 321, 325) as clearly marked retrospective material, not live claims.

Pass B (items 2–5) returned four `info` entries, one per item, each stating: (2) no weakening found across AC2/AC3/AC4/AC5/AC6/AC7/AC9/AC10/AC14; (3) AC12 computes `present` only under the schema-present / attempt-accepted / effect-observed conjunction with no remaining falsification-call reference, and AC18(a) carries the paired positive control; (4) all three hand-traced fixtures (present, absent, refused-while-present) produced the correct `rc`, with field extraction anchored, variables quoted, and comma-wrapped membership immune to a `haikuXYZ`-shaped false match; (5) `accepted-no-effect` occurs only in historical prose, and the live `- pre-commitment-executed:` and `- effort-attempt:` grammars carry no stray `none` / `accepted-no-effect` option.

### My own synthesis

All five items hold, independently confirmed by two separate reading passes reaching the same conclusion by different methods — this role's own primary reading (grep-plus-context tracing and direct shell-fixture execution) and Codex's own two-pass reading (targeted `rg` search plus hand-traced fixtures). No finding from either Codex pass requires a severity assignment beyond `info` — there is nothing here to uphold, downgrade or reject.

One non-blocking observation, not raised by Codex: the sweep's own **"sites swept: 188, reconciled: 23"** figure (line 321, mirrored on the board) is a hand-composed sum rather than a `bin/derive-populations.sh` derivation — its stated composition (12 Input-space reachable classes + 13 out-of-scope items + 15 Non-goals + 15 decision points + 3 pre-commitment drops + 4 licence-scope rows + 6 channel pins + 17 AC-prose-vs-check pairs + 18 Notes-for-engineer bullets + 58 Body-to-AC rows + 27 board sub-bullets) sums to exactly 188, so the arithmetic is internally consistent and not fabricated, but it is not machine-reproducible the way this repository's `- count:` / derivation-block convention asks of a population claim. This is a narrative sweep count inside a review table, not a `- count:` line `bin/check-count-claims.sh` validates, so it does not rise to Major under this deliverable's `user-visible: no` calibration — recorded as a fast-follow rather than blocking.

### Requirement-item disposition — the round-3 mandate's five numbered items

| # | Item | Verdict |
|---|---|---|
| 1 | `frozen-rules-contradict` closed as a class (not just the two round-2 spots) | **Confirmed closed.** No live contradiction found by two independent passes (this role's own reading and Codex's pass A) across the total functions, the per-drop paragraph, both closed-vocabulary declarations, DP-11/12/15, A-3/5/19, Input-space classes 7–8, and Body-to-AC rows 24/25/39/50. |
| 2 | Never-dropped set not weakened by drop (1) | **Confirmed intact.** AC2/AC3/AC4/AC5/AC6/AC7/AC9/AC10/AC14 all retain their cardinalities and guards. |
| 3 | R1-2 (effort `present` reachability) still closed | **Confirmed closed**, one moving part lighter, exactly as R2-1's own resolution claims; AC18(a)'s paired positive control is present. |
| 4 | AC12's rewritten `- check:` line, fixture-tested | **Confirmed sound** on a present-path, an absent-path and a deliberately inconsistent fixture, by both this role's direct shell execution and Codex's independent hand-trace. |
| 5 | The two vocabulary removals (`accepted-no-effect`, `none`) | **Confirmed sound** — both tokens survive only in clearly historical prose, never in a live grammar declaration. |

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|

no severity changes

### Route-back

**Verdict: APPROVE (round 3)** — the `(round <N>)` suffix is required from the second spec-review round onward per this task's own closed verdict-token grammar; this is the third round.

This verdict gates the freeze, never `READY_FOR_MERGE`, and is not one of the two `both-gates-green` gates (qa-verifier's PASS and the default-mode reviewer's own APPROVE on the delivered change) — neither of which exists yet, since no implementation has been written for this task. `pm-spec` may proceed to record the intent hash and freeze.

### Fast-follow disposition

- The 188/23 sweep-count figure (see "My own synthesis" above) — recommend `pm-spec` re-derive it through `bin/derive-populations.sh` if a fourth spec-review round or a future rework touches this same inventory, so the count is machine-reproducible rather than hand-summed. Not itself blocking: the arithmetic is internally consistent as stated, and the underlying sweep's actual conclusion (0 unresolved) was independently spot-checked and held on both this role's read and Codex's two passes.
