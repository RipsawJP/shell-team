# T-1003 review record

## qa-verifier — PASS → READY_FOR_REVIEW (2026-07-30)

**Scope**: full spec verification (22 ACs, intent frozen v1 `8052b26e…`), plus
regression across the CI-named suite list, plus independent adversarial
fixture construction of my own.

### Mechanical gates (all re-run live, none trusted from the hand-off)

- `check-acs.sh` (dry-run then live): 22/22 PASS.
- `check-intent.sh`: `aligned: T-1003 v1 (8052b26ea1d27bb52afd53e55d25d1bca7e25857)`.
- `check-prompt-sync.sh`: clean.
- `check-handoff.sh` on `templates/todo-template.md` and the live board: both exit 0.
- `check-pii-shapes.sh --base develop`: clean.
- `check-commit-identity.sh --base develop`: clean.
- `shellcheck` (0.11.0, matches CI's pinned version) on the exact CI argument
  list (46 paths): clean.
- Every suite + dogfood step named in `.github/workflows/check-handoff.yml`,
  run in the file's own order: all green, including both retro-inputs
  dogfood steps (`bash bin/retro-inputs.sh --base HEAD`,
  `bash bin/check-retro.sh .shell-team/retros/*.md`) and the
  check-interventions dogfood loop (2 files found: T-1002.md, T-1003.md,
  both conformant).
- `git diff --name-only develop`: exactly the 22 files the spec's Measured
  inventory + AC20's allow-list name; nothing outside it.

### Independently reproduced (not trusted from the engineer's hand-off)

All three named mutations, replayed myself in scratch copies (`git archive`
into `$TMPDIR`, never the live tree):

1. Removing the `interventions` line from a **copy** of `pass-canonical.md`
   → `bin/check-retro.sh` reports exactly `missing Retro inputs id:
   interventions`, exit 1. Unmutated original still passes, exit 0.
2. Deleting the `report_dir_input interventions` call from a scratch copy of
   `bin/retro-inputs.sh` → the `interventions` line reverts to `status:
   unavailable — detail: not determined`, and AC8's exact check-line fails
   (exit 1) against the mutated script. Restored → passes again.
3. Reverting the ledger-completeness `-eq 9` (line 108) back to `-eq 8` in a
   scratch copy of `tests/retro-inputs/run.sh` → the suite fails loudly
   (`expected 8 complete ledger lines, got 9` — inverted, correctly fails on
   the mutated assertion). Restored → suite green again.

The engineer's self-chosen fourth mutation (swap `TEAM_INTERVENTIONS_DIR` for
another dir key) — reproduced independently by swapping it for
`TEAM_RETROS_DIR`:
- AC6's exact-argument grep: fails (exit 1) — correctly catches it.
- AC7 (resolver called, no hardcoded path): **passes** (exit 0) — does NOT
  catch it, exactly as flagged. The call still invokes `team-paths.sh` and
  contains no literal path, so AC7's structural test alone is blind to
  *which* key was read.
- AC8 (detail-suffix pattern): fails (exit 1) — the detail's directory no
  longer ends in `.../interventions`.
Judgment: the boundary is real and accurately described. Not a defect today
— AC6 and AC8 are both live and both do catch this exact miswiring, so no
criterion overclaims. Flagged again below for the reviewer's attention as a
rework-time risk (if a future round weakens AC6/AC8 while leaving AC7
untouched, this exact class could reopen silently).

### Independent fixtures of my own construction (read/empty/unavailable trichotomy, item 2 of my brief)

Built three throwaway git repos under `$TMPDIR` (not the shipped fixtures),
each exercised by running the real `bin/retro-inputs.sh` from cwd inside
them:
- A repo with a real `.shell-team/interventions/T-9001.md` (one entry) →
  `status: read — detail: 1 intervention records in .shell-team/interventions`.
- A repo with `.shell-team/interventions/.gitkeep` only → `status: empty —
  detail: 0 intervention records in .shell-team/interventions`.
- A repo with no `.shell-team/` at all → `status: unavailable — detail:
  directory not found: .shell-team/interventions`.
`empty` and `unavailable` were never substituted for each other in any of
these three states.

Two further adversarial states, also of my own construction, specific to the
new `interventions` key (Input space class 1):
- `chmod 600` (readable-listing bit only, no traverse) on the interventions
  dir → `status: unavailable — detail: directory entries could not all be
  stat'ed (0/1 enumerated) -- not traversable` — never `empty`.
- `chmod 000` (fully unreadable) → `status: unavailable — detail: directory
  not readable` — never `empty`.

### Files-not-entries counting (item 3 of my brief)

Built a fixture directory with `T-9001.md` (3 entries: correction / interrupt
/ work-deferred) plus the reserved `no-task.md` (1 entry) — 2 files, 4
entries total. Ledger reported `2 intervention records`, matching the file
count; no class token, no summary text, no entry byte of any kind reached
the ledger line. Confirms DP-3 / AC6 / AC8 under a fixture I built myself,
not the shipped ones.

### Regex/vocabulary-collision probes (own fixtures, item 4 checklist class 2)

- A blockquoted `> - input: interventions — status: read — detail: 99 fake
  records...` appended to a copy of `pass-canonical.md`, outside the ledger
  section → passes clean (correctly ignored — blockquote prefix, matching
  the template's own legend convention, never mistaken for a real ledger
  line).
- The same line WITHOUT the blockquote prefix (bare `- input: interventions
  — status: read — detail: ...`), stranded after the ledger section closes
  → correctly caught: `ledger-shaped line outside the ## Retro inputs
  section`, exit 1. Confirms the pre-existing outside-section detection
  still works correctly for the NEW ninth id specifically, not just the
  original eight.
- A fenced code block containing a fake `## Retro inputs` heading + ledger
  line → still fails closed (`duplicated ## Retro inputs section heading` +
  `ledger-shaped line outside the section`, exit 1). This is a pre-existing,
  symmetric, fence-unaware limitation of the checker's design (already
  disclosed in T-1002's QA record for the same shared mechanism) — not
  introduced or regressed by this task, which touches only the enum, never
  the region-detection grammar.

### Read for content, not just structure

- `.shell-team/interventions/T-1003.md` → `conformant` (1 entry). Read: a
  genuine assumption-contradicted trigger (the delegation brief's "three
  stale criteria" vs. pm-spec's re-measured "seven"), matching the board's
  own account.
- `.shell-team/provenance/T-1003.md` → `conformant` (2 decisions). Both
  grounded concretely: decision 1 (Loop step placement + the two `steps
  2–5` cross-reference updates) matches the actual diff in
  `agents/scrum-master.md`; decision 2 (`grep -c '.'` over captured stderr,
  not `wc -l`, which undercounts a no-trailing-newline capture) matches
  `assert_violations`'s actual implementation in `tests/check-retro/run.sh`.
- `.shell-team/retros/2026-07-28.md`'s backfilled line: `unavailable`, never
  `empty`/`read`, reason states "the channel did not exist until T-1002" —
  honest, matches DP-6. Diff to this file is additive-only, one line.
- `agents/scrum-master.md`: read end-to-end, not just the two pinned `steps
  2–5` cross-references — no other stale numeric cross-reference found
  beyond those two. Six input items, Loop renumbered 1–12 consistently, no
  numeric threshold or metric attached to `unclassified` anywhere in the
  file.
- T-1001's board entry: staleness note present verbatim, matching AC22's
  pinned text exactly.

### Protected invariants

`bin/check-interventions.sh`, `templates/prompt-blocks/interventions-classes.md`,
`.shell-team/specs/T-1001-retro-input-acquisition.md`, `docs/tuning-oversight.md`,
`.github/workflows/check-handoff.yml`, `bin/team-paths.sh` — all byte-unchanged
against `develop` (AC19, re-confirmed). `lessons` still `unavailable`.
T-1001's AC2 promotion-site pin (2 functions, 8 textual call sites)
re-verified via AC6's live check.

### Edge cases tried beyond the spec's own list

- Absent base via `TEAM_RUN_BASE` pointed at a nonexistent relative dir
  (matches AC9's own construction) — reconfirmed live.
- Three fresh scratch repos for the read/empty/unavailable trichotomy
  (above) — none reused from the shipped test suite.
- The two regex/vocabulary-collision probes and one CommonMark-fence probe
  (above).

None of these edge cases are out-of-input-space by the spec's own `##
Input space` section — all are inside its declared reachable classes (1, 2,
5). No `out-of-input-space` exemption needed for anything found.

### Non-blocking, for the reviewer

- The AC6/AC8-catch-but-AC7-does-not boundary on a swapped resolver-key
  argument (above) — genuine and correctly disclosed by the engineer,
  independently reproduced by me. Not a defect against any current
  criterion; worth a second look if a future rework touches AC6 or AC8 in
  isolation.
- The fenced-code-block fail-closed behavior (above) is a pre-existing,
  shared limitation, not something this task introduces or could fix within
  its own Non-goals (it does not touch `bin/check-retro.sh`'s
  region-detection grammar).

### Working tree

Clean throughout. All fixtures and mutations were built and run under
`$TMPDIR`/scratch copies (`git archive HEAD` into a scratch tree, never
`cp -r` over the live tree, and never a live-tree `git checkout`/`reset`/
`clean`/`stash`). `git status --short` on the real repo showed nothing
beyond this hand-off's own board edit throughout.
