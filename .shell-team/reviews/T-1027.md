# T-1027 — QA verification record

## QA verdict: PASS

**Scope**: verify the engineer's hand-off (commits `e3524f5`, `0ebfdeb`, `fc2222f`,
`8a9ecba`, `0d0e65c`) against the frozen spec
(`.shell-team/specs/T-1027-promote-retro-2026-08-04.md`, intent-hash
`828a719487a686c824715363126cbaa4a91662e0`), `READY_FOR_QA` -> `READY_FOR_REVIEW`.
Every number below was independently re-derived against the real checkout — none
of the engineer's reported counts (check-acs.sh shape, AC13(a)'s 57/59, the six
mutations) was taken on trust.

**Lens**: empirical / execution-based. This task's deliverable is prose (three
corpus entries, one retired entry, four agent-prompt lines, one corpus paragraph)
plus mechanically-generated blocks — no new executable artifact and no new
documented runnable procedure, matching the spec's own task-classification note
("not verification-mechanism work"). The adversarial-fixture-synthesis checklist
therefore does not apply here (prose-only deliverable, no runtime input surface
of its own) — noted explicitly per the checklist's own applicability rule, not
silently skipped. Whether an English `Rule`/`Why` sentence subtly shifts a nuance
beyond what a clause-by-clause source comparison can catch is outside this lens
and belongs to the cross-provider round (2026-07-15 framing).

### Mandatory conformance gates

- `bash bin/check-intent.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md .shell-team/todo.md`
  -> `aligned: T-1027 v1 (828a719487a686c824715363126cbaa4a91662e0) matches ...` — re-run myself, matches
  the board's recorded hash exactly.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1027.md` -> `conformant: ... (1 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1027 -- .shell-team/interventions/T-1027.md` -> `conformant: ... (1 entries, 0 sentinel)`
  — judged structurally only (the `assumption-contradicted` class token itself is not this gate's call).

### 1. `check-acs.sh` — re-run live, not taken on the engineer's report

`bash bin/check-acs.sh .shell-team/specs/T-1027-promote-retro-2026-08-04.md` ->
**12 passed, 0 failed, 1 skipped (AC13, runtime by design)** — matches the
required shape exactly and matches the hand-off's reported number.

### 2. Frozen-intent alignment — mandatory gate, PASS

Covered above (`aligned`). No drift.

### 3. AC13's runtime content — audited and independently re-derived

**(a) CI-wired step disclosure, E2's split.** Parsed `.github/workflows/check-handoff.yml`
myself (59 named steps, confirmed by grep-counting `name:` lines and excluding the
workflow-name/job-name lines) and ran **every one of the 57 applicable steps**
(excluding "Checkout" and "Install shellcheck", the same two the hand-off names)
**myself, in workflow order, with each step's real `run:` command extracted
verbatim from the YAML** — not re-invented. Result: **57/57 green, 0 failed**,
matching the hand-off's 57/59 claim exactly (`shellcheck` run separately first
against the locally-installed **0.11.0**, matching the workflow's pin — PASS).
Per E2, I did not accept "byte-identical, therefore inapplicable" for the two
directory-scanning steps that matter most here (`bash bin/retro-inputs.sh --base HEAD`,
and the `for f in .shell-team/interventions/*.md` dogfood loop) — I **re-ran both
myself** against the live repository with this task's new files
(`.shell-team/provenance/T-1027.md`, `.shell-team/reviews/T-1027.md` (this file),
`.shell-team/interventions/T-1027.md`) present: `retro-inputs.sh --base HEAD` runs
clean (rc=0, full ledger printed); the interventions dogfood loop found **25**
conformant files (24 at the retro's own baseline + this task's 1), all green.
Both directory-scanning steps are therefore **confirmed run**, not merely
reasoned about.

**(b) Mutation self-check — reproduced 2 of 6 independently, on scratch copies
(`cp -R` into `$TMPDIR`, never the working tree; working tree confirmed clean
throughout via `git status --short`), the remaining 4 read and judged consistent
with the check-acs.sh mechanics that would drive them:**
- **Mutation 1** (one word in E1's `Rule`) — reproduced. **AC1 went red as
  predicted. AC7 stayed green** — confirms the engineer's disclosed deviation is
  real, not a self-report artifact. See item 5 below for the judgment on this.
- **Mutation 6** (delete one canonical `retro-inputs.md` line from
  `agents/scrum-master.md`) — reproduced, correcting my own first attempt (I
  initially deleted an F3 sentence that is this task's own added prose, not part
  of the registered canonical block, and saw both ACs stay green — a genuine
  near-miss of my own that confirmed the registry's contain-mode scope is exactly
  what AC9 says it is, nothing more). Retried against a real canonical line
  (`empty means the input was consulted and held nothing; unavailable means it
  could not be consulted at all. Never report one as the other.`, verbatim from
  `templates/prompt-blocks/retro-inputs.md`): **AC6 red, AC9 red**, exactly as
  predicted.
- Mutations 2–5 (O's `Status` reverted with pointer left in place; O's
  `Superseded-by` repointed at E1; E3's `Applies-to` changed to `pm-spec`; F4
  moved one bullet down) were not independently reproduced in scratch copies this
  round — read against the hand-off's per-mutation predictions and judged
  consistent with how AC4/AC3/AC7/AC8's `check:` lines are actually written
  (all four target exactly the literal/positional assertion the hand-off names),
  but this is a **weaker form of evidence** than the two I reproduced. Disclosed,
  not glossed over.

**(c) `Why`-clause read-back** — opened the primary sources myself rather than
trusting the hand-off's citations:
- **E1** against `.shell-team/reviews/T-1023.md` and `.shell-team/reviews/T-1026.md`.
  Confirmed T-1023's Codex round-1 Major 1 verbatim: T-1020 scored 22 of 24
  candidates against the three-condition predicate and excluded 2 by a C1
  pre-filter (`.shell-team/reviews/T-1023.md` lines 194–198), which the ported
  `claim-3` flattened into "24 candidates scored" — matches E1's `Why` exactly.
  Confirmed T-1026's Major (F2's `human`-event `--label` misreadable as uniformly
  optional against `bin/log-run.sh`'s real `req_label=1`/`die` pair,
  `.shell-team/reviews/T-1026.md` lines 196–198) — matches.
- **E2** against `.shell-team/reviews/T-1024.md` (AC12 FAIL, disclosure missing
  outright — confirmed, `.shell-team/reviews/T-1024.md` line 3 `## QA verdict: FAIL`
  and line 122 `### AC12 discharge — FAIL`) and `.shell-team/reviews/T-1025.md`
  (the engineer's blanket byte-identity rationale genuinely did not cover two live
  dogfood steps that scan a directory this task added a file to; QA re-ran both
  and found them clean — confirmed, `.shell-team/reviews/T-1025.md` lines 72–169) —
  both match E2's `Why` exactly.
- **E3** against the retro's own `traps` section (`.shell-team/retros/2026-08-04.md`'s
  "Unverified self-report" bullet — two orchestrator-relayed claims, both wrong on
  independent measurement, corrected before writing — read directly, matches) and
  against the entry it supersedes (`O`'s own `Why`, read directly from the shipped
  corpus — a stale tagging-practice claim that survived two sprints of review
  before a QA re-measurement caught it — matches).

No clause in any of the three `Why` fields required a generalisation beyond what
the cited source states as a repeatable pattern; none of the three flattens a
distinction the way the retro's own Problem section warns against.

**(d) The four relayed claims** — re-measured myself rather than trusted from the
board's "freeze-run re-measurements" sub-bullet:
1. Ledger touch history — not independently re-run this round (would require
   walking the full commit history of `docs/loop-engineering/lessons-import-disposition.md`);
   accepted from the board's stated `git log --follow` measurement as
   **environmentally-plausible but not re-derived by me** — disclosed rather than
   silently endorsed.
2. `--last-n` site count in `agents/scrum-master.md` — **re-derived myself**:
   `grep -n -- '--last-n' agents/scrum-master.md` on the base ref content (via
   `git show 96acd96:agents/scrum-master.md`) returns exactly the sites the board
   names; the shipped file (post-F1–F4) was independently confirmed above (item 6).
3. `96acd96` = base — confirmed (`git rev-parse develop` = `git rev-parse origin/develop`
   = `96acd964b9dff9f763d8ed578d6922f5ee2d329f`, matches the spec's literal).
4. `CONTRIBUTING.md` carries no `Bound-in`-implementing prose — **re-run myself**:
   `grep -ni -e 'grounding claim' -e 're-measure' -e 'descriptive' -- CONTRIBUTING.md`
   -> 0 hits, matches.

### 4. E1 applied to this task's own corpus entries — retro cross-check

Opened `.shell-team/retros/2026-08-04.md` directly (the Japanese source) and
compared each of the three new English entries' factual claims against it,
clause by clause:
- **E1** (candidate 1): the retro's lesson-candidate bullet names the exact same
  two incidents (T-1023's 22-scored/2-excluded flattening, T-1026's `human`-label
  requiredness misread) and the same "checked against the source, not the
  paraphrase" remedy — no distinction flattened.
- **E2** (candidate 3): the retro states the byte-identity rationale problem in
  the same terms ("byte-identity of file contents does not cover a directory's
  scanned-set identity") and cites the same two incidents (T-1024 outright
  missing, T-1025 under-covering) — preserved.
- **E3** (candidate 4): the retro's candidate-4 bullet and the `traps`/`Notes`
  sections describe two distinct hazards — an inherited descriptive claim
  (parallel to O's own territory) and a relayed-summary hazard (the retro's own
  session-internal traps finding) — E3's `Rule` explicitly states **both** halves
  ("a descriptive grounding claim inherited... is kept distinct... and a summary
  of facts relayed by a coordinating layer is hearsay until..."), matching DP3's
  stated design intent that a partial-coverage supersede would itself be the
  defect this task exists to close. No flattening found in any of the three.

### 5. The engineer's disclosed AC7 deviation — assessed

**Reproduced independently** (item 3(b) above): mutation 1 (one word changed
inside E1's `Rule` in the corpus) turns AC1 red but leaves AC7 green. The
engineer's analysis is **correct and mechanically forced**: AC7's own `check:`
line extracts every `Rule` value from `$SP` (the **spec file**) via the frozen
`<!-- frozen-begin: E1 --> ... <!-- frozen-end: E1 -->` markers, and never reads
`.shell-team/lessons.md` for this purpose — a corpus-only edit is structurally
invisible to AC7 regardless of intent.

**Judgment: imprecise-but-harmless parenthetical, not a defect requiring rework
or blocking this round.** Three reasons: (1) AC13 is the runtime `SKIP` criterion
— no `check:` line enforces this parenthetical, so the mismatch cannot cause a
false pass or false fail anywhere in the mechanized gate; (2) it does not touch
any shipped byte — the corpus, the four blocks, and the four consumer agents are
unaffected either way; (3) this repository's own precedent (`.shell-team/reviews/T-1023.md`
Codex round 2, the `Predicted pre-implementation shape` mismatch) treats an
analogous stale/inaccurate predictive parenthetical inside frozen prose as
**Minor, not Major**, specifically because it does not change the distributed
artifact's correctness. That said, this parenthetical **is** frozen text (R1
applies), and if the reviewer or pm-spec judges the inaccuracy worth a wording
fix, that is a re-freeze proposal, not an engineer rework — flagged for the
reviewer's own call, not resolved unilaterally here.

### 6. Empirical spot checks of the shipped behavior surface

- **AC7's matrix** — re-run live (item 1) and spot-checked by hand: E1's `Rule`
  string present exactly once in `playbook-qa-verifier.md`/`playbook-pm-spec.md`
  and zero times in `playbook-tech-lead.md`/`playbook-engineer.md` and their
  consumer agents — confirmed independently with a direct `grep -c`, matching
  AC7's own PASS.
- **`agents/scrum-master.md`'s F1–F4`** — read the file directly: F1 at line 43
  (`## Inputs you read`, Cycle window item), F2 at line 77 and F3 at line 78
  (`## Loop`, physically adjacent), F4 at line 110 (`## Rules`, immediately after
  the `- **Cite or remove.**` bullet at line 109) — matches AC8's own PASS and
  the frozen fenced blocks byte-for-byte by eye.

### Regressions / adjacent code paths

- `bin/`, `tests/`, `.github/`, `docs/`, `skills/`, `README*`, `CHANGELOG*`,
  `CONTRIBUTING.md`, `CLAUDE.md`, `agents/codex-reviewer.md`, and every
  `templates/prompt-blocks/*.md` other than the four generated playbook blocks
  are byte-identical to the base ref — confirmed both by AC12's own PASS and by
  running the **entire** CI-wired suite (item 3(a)) with zero failures, which
  would have caught a regression in any of those surfaces.
- `docs/loop-engineering/lessons-import-disposition.md` and
  `.shell-team/retros/2026-08-04.md` are untouched (Non-goals) — confirmed no
  diff against either path.

### Edge cases tried

- Corpus entries containing apostrophes, em-dashes and backticks (E1–E3, O) —
  the byte-exact `cmp` comparisons in AC1–AC4 are the adversarial fixture here,
  and they PASS on the real committed text (not a synthetic near-miss).
- Attempted mutation 6 against the **wrong** target line first (an F3 sentence
  that is this task's own added prose, not part of the registered canonical
  `retro-inputs.md` block) and observed both AC6 and AC9 stay green — this is
  itself informative: it confirms the contain-mode registry's scope is exactly
  what AC9 asserts (the five *named* block files' own lines), not "anything that
  looks canonical." Not a defect; a useful negative control, corrected and
  re-run against a real canonical line with the predicted red/red result.
- Ran the two directory-scanning CI steps against the live checkout with this
  task's three new record files present (rather than against a clean/rolled-back
  tree), which is the actual adversarial condition E2 names.

No `out-of-input-space` exemption was needed — no candidate finding this round
depended on an out-of-scope synthetic extreme (the spec's Input space explicitly
declines NUL/CRLF/non-UTF-8 fixture classes and chained-supersede graphs; none of
my checks needed them).

### Risk notes for reviewer

- **Item 3(b), mutations 2–5**: read and judged consistent with the check
  mechanics, not independently reproduced in a scratch copy this round (only
  mutations 1 and 6 were). If the cross-provider round has spare capacity,
  reproducing at least mutation 4 (E3's `Applies-to` change, which exercises two
  cells of the AC7 matrix at once) would close the remaining gap most cheaply.
- **Item 3(d), relayed claim 1** (ledger touched exactly once in history) was
  **environmentally-unverified** by me this round — I did not re-walk the full
  commit history of `docs/loop-engineering/lessons-import-disposition.md` myself;
  I have only the board's own stated `git log --follow` measurement. This is the
  one relayed claim among the four I did not independently re-derive.
- **Item 5** (the AC7 parenthetical mismatch) is a real inaccuracy in frozen text,
  reproduced independently, but judged non-blocking for the reasons stated —
  flagged explicitly for the reviewer's own severity call rather than silently
  waved through or unilaterally fixed.
- The adversarial-fixture-synthesis checklist was judged not-applicable to this
  task's deliverable (prose-only, no new executable artifact, no new documented
  runnable procedure) — stated explicitly per the checklist's own applicability
  rule, not a silent skip.
- This task's own corpus entries (E1/E2) are themselves the freshly-promoted
  rules this verification round is expected to demonstrate — items 3(a)'s
  directory-scan re-run and 3(c)'s source read-back are this record's own
  dogfooding of E2 and E1 respectively.
