### Codex Review verdict: REQUEST_CHANGES
- Task: T-1063
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: 9 files, +398/-16 lines (`git diff --stat f63c46b...HEAD`), 8 commits

このラウンドは実際に `bin/check-acs.sh` / `bin/check-refreeze-class.sh` / `bin/check-prompt-sync.sh` / `bin/check-provenance.sh` / `bin/check-interventions.sh` / `bin/check-pii-shapes.sh` / `bin/check-board-headings.sh` / `bin/check-handoff.sh` を自分で実行し、その結果と GitHub issue の実体（#226/#211/#210/#196/#257、いずれも直接 `curl` で確認、network access あり）を突き合わせた。Codex primary/adversarial 両パスも実行済み。

#### Blockers
- `.shell-team/todo.md:34`（T-1063 自身の board entry 内、QA verdict の一節）— この一節は文字列 `measured **empty** at this HEAD` を **そのままバイト単位で引用**している。この文字列は AC8 が「entry 外での出現数が base/head で等しいこと」を要求している対象そのもの。base 側の entry 外出現数は 5（板全体 6 − T-1047 entry 内 1）、head 側の entry 外出現数は 6（板全体 6 − T-1047 entry 内 0）— T-1047 の entry からは正しく 1→0 に消えたが、QA 自身の verdict 文がその場で新しい 1 件を作り出し、板全体の総数は 6/6 のまま変わらないように見えて、entry 外の等式だけが崩れている。実測: `bash bin/check-acs.sh .shell-team/specs/T-1063-editorial-batch.md` → **9 passed, 1 failed (AC8), 1 skipped (AC11)** — board の同じ verdict 文が主張する「10 passed, 0 failed, 1 skipped, matching the required shape exactly」と矛盾する。Codex adversarial pass も独立に同じ算術（base outside-T-1047=5, head outside-T-1047=6）を再現し severity `high` で確認済み（`.shell-team/reviews/T-1063-codex-adversarial.txt` 参照）。
  - 修正案: QA verdict 文中のこの引用をバイト完全一致の引用ではなく言い換え（例: 「the target phrase」）に変更し、`check-acs.sh` を再実行して AC8 の PASS を確認した上で、verdict 文自体の pass/fail/skip 件数も新しい実測値に更新する。この修正は T-1063 の frozen intent block（AC8 の criterion 文そのもの）には触れない — board 側の mutable な hand-off prose のみの訂正で足りる。

#### Major
- QA 自身の hand-off が、`agents/qa-verifier.md` の 2 つの **mandatory** 項目を未完了のまま PASS を記録している。(1) 「Audit every runtime acceptance criterion item by item」— AC11(b) の full-population blast-radius diff は 67 spec のうち 9〜10 件しか走っていない（本人も disclose 済み）。(2) 「Verify every summarized source against the source itself」— 一部の `## Summarized sources` を「time budget の都合で再オープンしなかった」と明記している。いずれの rule も **"not subject to the out-of-input-space exemption"** と明記された mandatory item であり、唯一許される免除は `environmentally-unverified`（missing credentials / no runtime path / path outside checkout 等の **environmental** な理由）のみで、"time budget" はこの定義に当たらない。実際に指示された 4 件の予測（T-1061 AC2 / T-1052 AC10 / T-1051 AC12 / T-1060 AC11）と追加で見つかった 3 件（T-1052 AC15 / T-1050 AC6 / T-1047 AC11）はすべて私自身が再実測して一致を確認できたので、**現時点で見落とされた実害は見つかっていない** — だが手続き上、mandatory rule への準拠が取れていない。
  - 推奨: 同じラウンドで、either (a) 残り約 57 spec の population sweep と残りの summarized-source を実際に完了する、または (b) 「time budget」という unsanctioned な言い訳ではなく、operator の 2026-08-13 proportionality ruling を根拠にした明示的な provenance 決定として「この監査は意図的に縮小した」ことを記録する — のいずれかで手続きを閉じる。
- `.shell-team/specs/T-1063-editorial-batch.md` の AC1〜AC9 の `- check:` 行はすべて `BPREF=feature/1062-release-notes-compare-link` を `git rev-parse --verify --quiet "$BPREF^{commit}"` で解決し、失敗したら即 `exit 1` する。この branch ref が将来削除されれば AC1〜AC9 は **恒久的に** fail-closed になるが、spec 冒頭（L7）は「merge-point-scoped criterion（AC10 のみ）に対しては意図された挙動」と述べるのみで、同じ前提に依存する残り 9 criteria には同種の disclosure（`- stale-at:` 相当）が一切無い。Codex adversarial pass も同じ構造を独立に指摘（severity `medium`）。この pattern は T-1063 単独の問題ではなく、T-1055/T-1052/T-1060/T-1061/T-1062 全体が同じ「one base-ref discriminator」規約を共有しており、実測でも `chore/lesson-promotion-2026-08-11` のような数スプリント前の predecessor branch が今も削除されずに残っている（`git branch -a` で確認済み）ため、**現時点で実害が発生する可能性は低い**。
  - 推奨: T-1063 単独で直すのではなく、corpus 全体の規約として「merge-point-scoped でない criteria も同じ branch 依存の前提を持つ場合は同じ disclosure を要求する」か「durable な commit SHA を resolve する」かを issue 化して検討する。

#### Minor / nits
- `agents/pm-spec.md:89`（engineer の AC11(c) で既に disclose 済み）— 「carved out of this task's own droppable component」という一文が、読み手の task を T-1061 ではなく "この task" と誤読させる書き方のまま残っている。T-1063 の Non-goals・engineer note で明示的に scope 外・no criterion reads it と記録済みであり、追加のアクションは不要。

#### Adversarial findings
- Codex devil's-advocate pass（`.shell-team/reviews/T-1063-codex-adversarial.txt`）は上記 Blocker（`measured **empty** at this HEAD` の自己言及的引用による AC8 崩壊）と Major #2（AC1-AC9 の branch-ref 依存で merge-point-scoped 相当の disclosure が無いこと）の両方を、私の直接実行結果とは独立に、根拠となる算術（entry 内外の出現数カウント）とファイル読解のみから再現・確認した。sandbox の read-only 制約で `mktemp` 自体は実行できなかったため check-acs のライブ実行値そのものは再現できていないが、その限界も自ら明記しており、算術的な結論（AC8 は現状で FAIL のはず）は独立に導出されている。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC11(b) full-population sweep incomplete (9-10/67) | primary: P1 (→blocker per normalization map) | Major | mandatory item は未完了だが、spec が明示的に要求した4予測＋追加3件はすべて私自身が再実測して一致確認済みで実害未発見。proportionality ruling を踏まえ、単独では merge を止めない — ただし手続きとして次ラウンドで解消が必要 |
| 2 | AC11(i) summarized-sources not all reopened | primary: P1 (→blocker per normalization map) | Major | 同上の理由。読み落としの実害は見つかっていないが、qa-verifier.md の mandatory rule への非準拠は残る |
| 3 | AC1-AC9 の living-branch-ref 依存、merge-point-scoped disclosure 欠落 | primary: P1 (→blocker per normalization map); adversarial: medium (→major) | Major（fast-follow へ振り分け） | corpus 全体（T-1055/T-1052/T-1060/T-1061/T-1062）が共有する既存パターンであり T-1063 単独の新規欠陥ではない。実測で predecessor branch が数スプリント後も削除されずに残っている現状の運用実態から、当面の実害リスクは低い |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings（上記 Blocker 1 件・Major 2 件の解消を求める。Blocker はボード上の mutable な QA verdict 文の言い換えのみで解消でき、いずれの frozen intent block にも触れない）

#### Fast-follow disposition
- Major #3（AC1-AC9 の living-branch-ref 依存、および T-1055/T-1052/T-1060/T-1061/T-1062 全体が共有する同種パターン）— file-an-issue: この spec family 全体で「merge-point-scoped でない criteria も同じ branch 依存の前提を持つ場合の disclosure」または「durable な commit SHA 解決への切替」を検討する issue を起票することを推奨する。T-1063 単独で直しても corpus 全体のクラスは閉じないため、この task の範囲外として issue 化する。
- `agents/pm-spec.md:89` の「carved out of this task's own droppable component」の自己言及的な曖昧さ — won't-fix for this task: 既に T-1063 の Non-goals・engineer AC11(c) で正しく scope 外と判定・記録済み。追加の issue は不要。
