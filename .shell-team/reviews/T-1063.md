### Codex Review verdict: REQUEST_CHANGES
- Task: T-1063
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: executed-tests
- Diff scope: 9 files, +398/-16 lines (`git diff --stat f63c46b...HEAD`), 8 commits

このラウンドは実際に `bin/check-acs.sh` / `bin/check-refreeze-class.sh` / `bin/check-prompt-sync.sh` / `bin/check-provenance.sh` / `bin/check-interventions.sh` / `bin/check-pii-shapes.sh` / `bin/check-board-headings.sh` / `bin/check-handoff.sh` を自分で実行し、その結果と GitHub issue の実体（#226/#211/#210/#196/#257、いずれも直接 `curl` で確認、network access あり）を突き合わせた。Codex primary/adversarial 両パスも実行済み。

#### Blockers
- `.shell-team/todo.md:34`（T-1063 自身の board entry 内、QA verdict の一節）— この一節は文字列 `measured **empty** at this HEAD` を **そのままバイト単位で引用**している。この文字列は AC8 が「entry 外での出現数が base/head で等しいこと」を要求している対象そのもの。base 側の entry 外出現数は 5（板全体 6 − T-1047 entry 内 1）、head 側の entry 外出現数は 6（板全体 6 − T-1047 entry 内 0）— T-1047 の entry からは正しく 1→0 に消えたが、QA 自身の verdict 文がその場で新しい 1 件を作り出し、板全体の総数は 6/6 のまま変わらないように見えて、entry 外の等式だけが崩れている。実測: `bash bin/check-acs.sh .shell-team/specs/T-1063-editorial-batch.md` → **9 passed, 1 failed (AC8), 1 skipped (AC11)** — board の同じ verdict 文が主張する「10 passed, 0 failed, 1 skipped, matching the required shape exactly」と矛盾する。Codex adversarial pass も独立に同じ算術（base outside-T-1047=5, head outside-T-1047=6）を再現し severity `high` で確認済み（`.shell-team/reviews/T-1063-codex-adversarial.txt` 参照）。
  - 修正案: QA verdict 文中のこの引用をバイト完全一致の引用ではなく言い換え（例: 「the target phrase」）に変更し、`check-acs.sh` を再実行して AC8 の PASS を確認した上で、verdict 文自体の pass/fail/skip 件数も新しい実測値に更新する。この修正は T-1063 の frozen intent block（AC8 の criterion 文そのもの）には触れない — board 側の mutable な hand-off prose のみの訂正で足りる。

#### Major
- QA 自身の hand-off が、`agents/qa-verifier.md` の 2 つの **mandatory** 項目を未完了のまま PASS を記録している。(1) 「Audit every runtime acceptance criterion item by item」— AC11(b) の full-population blast-radius diff は 67 spec のうち 9〜10 件しか走っていない（本人も disclose 済み）。(2) 「Verify every summarized source against the source itself」— 一部の `## Summarized sources` を「time budget の都合で再オープンしなかった」と明記している。いずれの rule も **"not subject to the out-of-input-space exemption"** と明記された mandatory item であり、唯一許される免除は `environmentally-unverified`（missing credentials / no runtime path / path outside checkout 等の **environmental** な理由）のみで、"time budget" はこの定義に当たらない。実際に指示された 4 件の予測（T-1061 AC2 / T-1052 AC10 / T-1051 AC12 / T-1060 AC11）と追加で見つかった 3 件（T-1052 AC15 / T-1050 AC6 / T-1047 AC11）はすべて私自身が再実測して一致を確認できたので、**現時点で見落とされた実害は見つかっていない** — だが手続き上、mandatory rule への準拠が取れていない。
  - 推奨: 同じラウンドで、either (a) 残り約 57 spec の population sweep と残りの summarized-source を実際に完了する、または (b) 「time budget」という unsanctioned な言い訳ではなく、operator の 2026-08-13 proportionality ruling を根拠にした明示的な provenance 決定として「この監査は意図的に縮小した」ことを記録する — のいずれかで手続きを閉じる。
- `.shell-team/specs/T-1063-editorial-batch.md` の AC1〜AC9 の `- check:` 行はすべて `BPREF=feature/1062-release-notes-compare-link` を `git rev-parse --verify --quiet "$BPREF^{commit}"` で解決し、失敗したら即 `exit 1` する。この branch ref が将来削除されれば AC1〜AC9 は **恒久的に** fail-closed になるが、spec 冒頭（L7）は「merge-point-scoped criterion（AC10 のみ）に対しては意図された挙動」と述べるのみで、同じ前提に依存する残り 9 criteria には同種の disclosure（`- stale-at:` 相当）が一切無い。Codex adversarial pass も同じ構造を独立に指摘（severity `medium`）。この pattern は T-1063 単独の問題ではなく、T-1055/T-1052/T-1060/T-1061/T-1062 全体が同じ「one base-ref discriminator」規約を共有しており、実測でも `chore/lesson-promotion-2026-08-11` のような数スプリント前の predecessor branch が今も削除されずに残っている（`git branch -a` で確認済み）ため、**現時点で実害が発生する可能性は低い**。
  - 推奨: T-1063 単独で直すのではなく、corpus 全体の規約として「merge-point-scoped でない criteria も同じ branch 依存の前提を持つ場合は同じ disclosure を要求する」か「durable な commit SHA を resolve する」かを issue 化して検討する。

#### Minor / nits
- `agents/pm-spec.md:89`（engineer の AC11(c) で既に disclose 済み）— 「carved out of this task's own droppable component」という一文が、読み手の task を T-1061 ではなく "この task" と誤読させる書き方のまま残っている。T-1063 の Non-goals・engineer note で明示的に scope 外・no criterion reads it と記録済みであり、追加のアクションは不要。

#### Adversarial findings
- Codex devil's-advocate pass（`.shell-team/reviews/T-1063-codex-adversarial.txt`）は上記 Blocker（`measured **empty** at this HEAD` の自己言及的引用による AC8 崩壊）と Major #2（AC1-AC9 の branch-ref 依存で merge-point-scoped 相当の disclosure が無いこと）の両方を、私の直接実行結果とは独立に、根拠となる算術（entry 内外の出現数カウント）とファイル読解のみから再現・確認した。sandbox の read-only 制約で `mktemp` 自体は実行できなかったため check-acs のライブ実行値そのものは再現できていないが、その限界も自ら明記しており、算術的な結論（AC8 は現状で FAIL のはず）は独立に導出されている。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC11(b) full-population sweep incomplete (9-10/67) | primary: P1 (→blocker per normalization map) | Major | mandatory item は未完了だが、spec が明示的に要求した4予測＋追加3件はすべて私自身が再実測して一致確認済みで実害未発見。proportionality ruling を踏まえ、単独では merge を止めない — ただし手続きとして次ラウンドで解消が必要 |
| 2 | AC11(i) summarized-sources not all reopened | primary: P1 (→blocker per normalization map) | Major | 同上の理由。読み落としの実害は見つかっていないが、qa-verifier.md の mandatory rule への非準拠は残る |
| 3 | AC1-AC9 の living-branch-ref 依存、merge-point-scoped disclosure 欠落 | primary: P1 (→blocker per normalization map); adversarial: medium (→major) | Major（fast-follow へ振り分け） | corpus 全体（T-1055/T-1052/T-1060/T-1061/T-1062）が共有する既存パターンであり T-1063 単独の新規欠陥ではない。実測で predecessor branch が数スプリント後も削除されずに残っている現状の運用実態から、当面の実害リスクは低い |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings（上記 Blocker 1 件・Major 2 件の解消を求める。Blocker はボード上の mutable な QA verdict 文の言い換えのみで解消でき、いずれの frozen intent block にも触れない）

#### Fast-follow disposition
- Major #3（AC1-AC9 の living-branch-ref 依存、および T-1055/T-1052/T-1060/T-1061/T-1062 全体が共有する同種パターン）— file-an-issue: この spec family 全体で「merge-point-scoped でない criteria も同じ branch 依存の前提を持つ場合の disclosure」または「durable な commit SHA 解決への切替」を検討する issue を起票することを推奨する。T-1063 単独で直しても corpus 全体のクラスは閉じないため、この task の範囲外として issue 化する。
- `agents/pm-spec.md:89` の「carved out of this task's own droppable component」の自己言及的な曖昧さ — won't-fix for this task: 既に T-1063 の Non-goals・engineer AC11(c) で正しく scope 外と判定・記録済み。追加の issue は不要。

## Round 2

### Codex Review verdict: APPROVE
- Task: T-1063
- Codex model: gpt-5.6-sol（primary pass の JSONL イベントストリーム自体には非印字。`~/.codex/config.toml` の既定値としてこの環境で確認済み）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +450/-16 lines (`git diff --stat f63c46b...HEAD`), 15 commits

このラウンドは operator の binding scoping ruling（`.shell-team/interventions/T-1063.md` entry 3・4、commit `69bff85`/`64cb6b4`/`6ec9273`/entry 4 の 2026-08-13 裁定）どおり、**WORDING のみ**を検証対象とする。mechanism は一切変わっていない（今回の diff は spec prose・2 件の prompt citation/attribution 修正・1 件の reference clause・board prose のみ）ので、population 再実行や CI 相当再実行、mutation probe は行わない。

Codex primary pass（`codex exec --sandbox read-only review --base f63c46b --json`、`.shell-team/reviews/T-1063-codex-primary.{txt,jsonl}`）を実際に実行し、その結果と自分自身の直接実測（`bash bin/check-acs.sh` のライブ実行、`bash bin/check-intent.sh` ×3、`git diff` による凍結領域の不動確認、複数箇所の `grep`/`awk` によるワーディング検証、GitHub issue #226/#211/#210/#196 の実体確認）を突き合わせた。

#### 検証項目 (1)-(4)（この round の verdict-gating scope）

**(1) round-1 Blocker（AC8 self-pollution）は解消したか — 解消・実測確認済み。** `bash bin/check-acs.sh .shell-team/specs/T-1063-editorial-batch.md` をライブ実行: **10 passed, 0 failed, 1 skipped (AC11, runtime)** — round-1 が指摘した AC8 の矛盾は消えている。`.shell-team/todo.md` の該当ブロック（QA round-1 verdict、commit `aed1f8c` が touched した行）を直接読み、`measured **empty** at this HEAD` のバイト完全一致引用が「the AC8 target phrase」という言い換えに置き換わっていることを確認した。修正は board の mutable prose 1 行のみで、frozen intent block には触れていない（diff は `.shell-team/todo.md` の 1 行のみ、round-1 review が要求した通り）。

**(2) round-1 Major の operator-ruling disposition は忠実に記録されているか — 忠実。** Major 1（AC11(b)/(i) の mandatory audit 未完了）は completion ではなく **operator-ruling cutoff** として記録されている。board の accounting（22 specs fully run + 1 targeted = 23 touched、67−23=44 not-run）を直接読み、算術の内部整合性を確認した：`.shell-team/interventions/T-1063.md` commit `69bff85` を正しく引用し、「cheap read-set claim は成立しない」という誠実な disclosure（追加で約 2 dozen の not-run spec が同じ6ファイルを引く check-line を持つが未再実行）も残っている — false-complete を装っていない。この disposition の**当否**は operator の裁定であり、この round の再評価対象ではない（task 指示どおり、私が選んだ disposition かではなく忠実に記録されているかだけを判定した）。Major 2（branch-ref durability、corpus 全体パターン）は round-1 の fast-follow 宣言（file-an-issue）のまま変更なし — 下記 Fast-follow disposition で restate する。

**(3) 修正された文は今も真実を語っているか — spot-check した範囲ですべて真。** 抽出した個別の grep/read 検証:
- (d) Q171x — `grep -c 'walk each distinct assertion its prose demands instead' agents/qa-verifier.md agents/engineer.md` → `agents/qa-verifier.md:0` / `agents/engineer.md:1` — 参照であって再記述でないことを確認。`agents/engineer.md:56` は実際に「where a criterion enumerates no items, walk each distinct assertion its prose demands instead」という該当ルールを持つ — Q171x の記述は真。
- (e)/(f) PM79new/PM89new — `grep -c '#171' agents/pm-spec.md` = `0`、`grep -c 'full-width' agents/pm-spec.md` = `1`、`grep -c '^  - freeze-attestation' .shell-team/todo.md` = `113`、そのうち owner が `coordinating session`/`engineer` でない行 = `0` — いずれも QA の主張と一致。
- (g) T60anchor — `README.md`/`README.ja.md` を直接 `awk 'NR==205,NR==210'` で読み、両ファイルとも L207 で段落終了・L208 空行を確認 — 「named by its content rather than by a line number」の記述が指す内容（reviewer 行の default-and-rationale）は実在し、記述は真。
- (a) R52new/DDnew/A17new/L17new — T-1055 の AC11（L159、`templates/binding-template.conf` の 6 つの `bind` 行を inner-loop role ごとに再導出）と AC18（L175、`check-binding.sh` の scratch fixture として同じ 6 role を構築する `- check:` 行）を直接読み、R52new の主張（AC11 が 6 role を再導出する）と一致することを確認。
- (h) T-1047 board-entry corrections — AC8 の check line が期待する base=6/head=5 のカウント（`measured **empty** at this HEAD` の entry 外出現数）を自分でも再確認し、QA の記述と一致。

上記でカバーしなかった (b)/(c) は round-1・round-2 とも既に QA が個別に直読・再実行して確認しており、今回新たな疑義は見つからなかったので再検証は割かなかった。

**(4) frozen block の外側の bytes 以外は何も動いていないか — 確認済み。** `git diff d778f92..HEAD -- .shell-team/specs/T-1055-adapter-envelope.md .shell-team/specs/T-1052-records-editorial.md .shell-team/specs/T-1063-editorial-batch.md` は**空**（round-1 review 記録時点からこの 3 spec ファイルは 1 バイトも動いていない）。`bash bin/check-intent.sh` を T-1063 v1 / T-1055 v7 / T-1052 v4 の 3 件で再実行し、いずれも `aligned`、hash 完全一致。rework の 2 コミット（`aed1f8c`、`9cf3525`）と QA round-2 のコミット（`376bf1f`）はいずれも `git show --stat` で確認した通り `.shell-team/todo.md` 1 ファイルのみを touch している。

#### Blockers
（なし）

#### Major
（この round の verdict-gating scope では新規の Major なし。round-1 の Major 2 は restate — 下記参照）

#### Minor / nits
- `agents/qa-verifier.md:68`（Q171x、Codex primary pass の指摘・P2 → 正規化で major 相当）— この文が読み手（qa-verifier ロールを実行する側）に指示する「`agents/engineer.md`'s matching rule」への相対パス参照は、adopter が plugin をインストールした環境では `agents/` が adopter リポジトリではなく plugin 側に残る（`docs/distribution.md` L39/L49）ため、bare な相対パス表記のままでは cwd 次第で解決に失敗しうる、という指摘。**ただしこの指摘は T-1063 が新規に持ち込んだパターンではない** — `agents/pm-spec.md:69`（`agents/drift-evaluator.md` と `agents/codex-reviewer.md` を同じ形で参照）と `agents/codex-reviewer.md:165`（`agents/qa-verifier.md`'s Adversarial fixture synthesis checklist を同じ形で参照）は branch point（`f63c46b`）以前から既に同じ規約で書かれており、T-1063 はその既存規約を `agents/qa-verifier.md` にもう一箇所適用しただけである。文自体は真（`agents/engineer.md:56` は実際に該当ルールを持つ、既に (3) で確認済み）。operator の binding scoping ruling（この round の verdict-gating scope は wording-truth のみで、"surrounding machinery" の実行可否は対象外）に従い、この round の verdict は gate しない — report-as-note として記録し、下記 Fast-follow disposition で扱う。
- `agents/pm-spec.md:89` の「carved out of this task's own droppable component」という自己言及的にやや曖昧な表現 — round-1 Minor のまま変更なし（この task が touch した箇所は misattribution の修正部分であり、この表現自体は round-1 の disposition どおり won't-fix のまま残っている。round-1 レビュー時点と同一文言であることを diff で確認）。
- QA round-2 自身が記録した 2 件の process-bookkeeping note（"six" と書いて 5 件しか列挙していない labeling slip／board のフラグ literal が `READY_FOR_REVIEW` のまま continuous でラウンド遷移を bullet で追っていない）は、operator の binding scoping ruling により明示的に report-as-note・non-gating と位置付けられている項目そのものであり、私もこの round の verdict を左右しないことに同意する。QA の記述を直接読み、算術（17+5=22、"22 specs fully run" と整合）に矛盾がないことも確認した。

#### Adversarial findings
Codex primary pass（`codex exec --sandbox read-only --cd <repo> review --base f63c46b --json`）が実際に diff を自ら読んで返した唯一の finding が上記 Minor の `agents/qa-verifier.md:68` 指摘であり、他に blocker/major 相当の finding は返らなかった。この finding が blocker/major の水準に達しなかったため、devil's-advocate framing での追加の adversarial pass（agent instructions のステップ 5）はトリガーされず実行していない。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Blocker（AC8 self-pollution） | round-1: blocker | resolved（Blocker 解消） | commit `aed1f8c` が board の mutable prose 1 行を言い換え、`check-acs.sh` ライブ実行で 10 passed/0 failed/1 skipped を実測確認 |
| 2 | round-1 Major 1（AC11(b)/(i) mandatory audit 未完了） | round-1: major | report-as-note（non-gating） | operator の binding scoping ruling（interventions entry 3・4、2026-08-13）が「completion ではなく operator-ruling cutoff」として明示的に扱うことを命じており、accounting の内部整合性を直接読んで確認済み — この round の再評価対象外 |
| 3 | 新規: `agents/qa-verifier.md:68` の相対パス参照が adopter installed-plugin 環境で解決しない可能性がある（Codex primary pass, P2 → 正規化で major） | primary: P2（→major） | minor（non-gating、fast-follow） | 文自体は真、かつ T-1063 が新規に持ち込んだパターンではなく `agents/pm-spec.md:69`/`agents/codex-reviewer.md:165` の既存規約の延長 — operator の scoping ruling（wording-truth のみを gate、"surrounding machinery" の実行可否は対象外）に該当し、この round の verdict は左右しない |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE

#### Fast-follow disposition
- round-1 Major 2（`.shell-team/specs/T-1055-adapter-envelope.md`/`T-1052-records-editorial.md`/`T-1060-adopter-binding-docs.md`/`T-1061-adopter-docs-gate.md`/`T-1062-release-notes-compare-link.md` が共有する living-branch-ref 依存、merge-point-scoped でない criteria の disclosure 欠落）— **file-an-issue（restate）**: この spec family 全体で「merge-point-scoped でない criteria が同じ branch 依存の前提を持つ場合の disclosure」または「durable な commit SHA 解決への切替」を検討する issue の起票を推奨する。orchestrator が READY_FOR_MERGE の recording step で起票する。
- 新規: `agents/qa-verifier.md:68`（および corpus 全体で同じ規約を使う `agents/pm-spec.md:69`、`agents/codex-reviewer.md:165`）の相対パス経由でのエージェント間相互参照が、plugin がインストールされた adopter 環境で resolve 可能かどうかが未検証 — **file-an-issue**: 上記 Major 2 の issue と同一 issue に相乗りさせるか、独立した issue として「エージェント定義ファイル間の相互参照を adopter 環境でどう resolve するか」を検討することを推奨する。理由: T-1063 単独の欠陥ではなく corpus 全体の既存規約であり、単発の editorial task の範囲で直すべきものではない。
- `agents/pm-spec.md:89` の「carved out of this task's own droppable component」の自己言及的な曖昧さ — **won't-fix for this task（restate）**: round-1 の disposition どおり、T-1063 の Non-goals・engineer AC11(c) で正しく scope 外と判定・記録済みであり、追加の issue は不要。
