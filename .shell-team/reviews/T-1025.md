# T-1025 — QA verification record

## QA verdict: PASS

**Scope**: verify the frozen v1 spec (`.shell-team/specs/T-1025-assert-parity-dead-comparison.md`,
intent-hash `092f1cbe51c1b7f7d3a2a66292772e7811349ac9`) against the engineer's hand-off
(commit `ada9187` deletion + comment rewrite + provenance, `0935552` interventions sentinel),
`READY_FOR_QA` -> `READY_FOR_REVIEW`.

### Mandatory conformance gates — all green

- `bash bin/check-acs.sh .shell-team/specs/T-1025-assert-parity-dead-comparison.md` ->
  **7 passed, 0 failed, 1 skipped** (AC1–AC7 PASS, AC8 SKIP as predicted; no `CHECK_ACS_TIMEOUT`
  elevation needed, per the spec's own statement).
- `bash bin/check-intent.sh .shell-team/specs/T-1025-assert-parity-dead-comparison.md .shell-team/todo.md`
  -> `aligned: T-1025 v1 (092f1cbe51c1b7f7d3a2a66292772e7811349ac9)`.
- `bash bin/check-intent.sh .shell-team/specs/T-1023-block-size-deferral-record.md .shell-team/todo.md`
  -> `aligned: T-1023 v2 (4434f8892603db06739d7902c525c566125dda02)` — unaffected neighbour.
- `bash bin/check-intent.sh .shell-team/specs/T-1024-check-line-mktemp-guard.md .shell-team/todo.md`
  -> `aligned: T-1024 v1 (87c496383df2cf019cff2983bb449e6ec1b8c05e)` — unaffected neighbour.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1025.md` -> `conformant` (1 decision
  entry, 0 sentinel). The single decision/reason/grounding triple cites
  `.shell-team/provenance/T-1019.md` by durable path, exactly as D2 requires — no line numbers.
- `bash bin/check-interventions.sh --task T-1025 .shell-team/interventions/T-1025.md` ->
  `conformant` (0 entries, 1 sentinel) — judged structurally only.

### Suite / lint re-run, independently

- `bash tests/is-span-row-parity/run.sh` -> all six ids PASS, banner printed, exit 0.
- `shellcheck tests/is-span-row-parity/run.sh` (0.11.0, matches the CI pin) -> clean.
- The full CI-wired shellcheck one-liner (`.github/workflows/check-handoff.yml` line 29's
  complete pinned arg list, ~50 files including `tests/is-span-row-parity/run.sh`) -> clean.
- `bash bin/check-pii-shapes.sh --base eae0667` and `--base develop` -> clean, both bases.
- `bash bin/check-commit-identity.sh --base eae0667` and `--base develop` -> clean, both bases.
- `bash bin/check-handoff.sh .shell-team/todo.md` and `bash bin/check-board-headings.sh
  .shell-team/todo.md --base eae0667` -> both exit 0.

### AC8's reading half — the comments, read end to end myself

Read the header comment (lines 1–32) and the `assert_parity` doc comment (lines 86–92) of
`tests/is-span-row-parity/run.sh` as shipped:

- Header: "Each of D4's six classes is asserted **twice**... A third comparison... would be dead
  by transitivity under fail-fast (both already equalling the same frozen value already implies
  they equal each other) — do not add one; see issue **#108**." Truthful against the code as it
  now stands (the third line is in fact gone, confirmed above); uses "third", never "three";
  the durable pointer reads as a real deterrent — it names the mechanism (transitivity) and the
  consequence (do not add one) rather than a bare prohibition, and it survives independent of
  whether a future reader opens the provenance file (D2's whole point).
- Doc comment: "Two checks per D4/D5: the frozen expected answer against each consumer
  independently. A direct parity comparison between the two consumers is not present — it would
  be dead by transitivity... A divergence names the drifted script and the fixture (D5)." The
  `D5` marker survives verbatim, and the message-shape claim is accurate — both remaining `fail`
  calls do name exactly one script plus `$fixture`.
- No stray "three checks" phrasing found anywhere by eye, consistent with AC2's negative grep.

### Mutation self-checks — 2 of 4 reproduced independently (includes (c))

Reproduced on a `cp -R` scratch copy under `$TMPDIR`, outside the tracked checkout, real repo
untouched throughout (`git status --short` clean before and after):

1. **(a) Re-added the deleted third comparison line** (`[ "$rc_class" = "$cc_class" ] || fail
   "..."`) -> AC1 went red (`FAIL (exit 1)`); restored byte-identical -> AC1 back to
   `PASS (exit 0)`.
2. **(c) Deleted the transitivity sentence from the doc-comment region only**, leaving the
   header's intact -> AC2 went red (`FAIL (exit 1)`), confirming the per-region `awk` loop — not
   a whole-file grep — is what catches a single-region regression; restored byte-identical ->
   AC2 back to `PASS (exit 0)`.

Matches the engineer's disclosed self-check results for both mutations exactly.

### AC8 discharge — my own ran/skipped list, re-derived rather than inherited

The engineer's hand-off names 5 steps run (shellcheck, board dogfood, the parity suite,
check-pii-shapes-on-diff, check-commit-identity-on-commits) and blankets everything else as
"inapplicable... byte-identical to `eae0667`". I re-derived the applicable set myself against
the live diff rather than trusting that blanket claim, and found it slightly under-inclusive:

**Applicable and re-run by me — all green** (in workflow order):
- shellcheck (full pinned list) — clean (already above).
- "Dogfood check-handoff — this repository's own board" — PASS.
- "Dogfood check-interventions — every committed interventions file in this repository is
  conformant" — **the engineer's blanket reason does not actually cover this step**: the
  interventions directory is *not* byte-identical to `eae0667` (`.shell-team/interventions/
  T-1025.md` is a new addition, confirmed via `git diff --name-status eae0667 --
  .shell-team/interventions/`), and this step's loop iterates every committed file in that
  directory, so its outcome genuinely depends on the new file. Re-ran the literal CI loop myself
  -> 23 files, all `conformant` (including the new `T-1025.md`).
- "Run is-span-row-parity fixture suite" — PASS, all six ids (already above).
- "Dogfood retro-inputs — the cycle window resolves against this repository"
  (`bin/retro-inputs.sh --base HEAD`) — **same gap**: this reads live git history (three new
  commits since `eae0667`), the live provenance/specs/interventions/reviews directories (each
  gained a new file this task), so it is not actually gated on byte-identity either. Ran it ->
  exit 0, reports 31 specs / 29 provenance / 23 interventions / 28 review artifacts read, no
  error.
- "check-pii-shapes on the PR diff" — clean, both `--base eae0667` and `--base develop`.
- "check-commit-identity on the PR commits" — clean, both bases.

**Inapplicable (AC6/AC7-grounded, correctly out of scope)**: shellcheck's own board-template
lint, every `tests/*/run.sh` fixture suite other than is-span-row-parity, check-retro/
retro-inputs *fixture suites* (as opposed to the two live dogfood steps above), check-readme-
version dogfood, check-prompt-sync dogfood, check-playbook/gen-playbook-blocks dogfood,
team-paths dogfood, install/check-intent/check-provenance/check-board-headings fixture suites —
none of their inputs (`bin/`, the other `tests/` suites, `templates/`, `README*`, retros,
lessons.md) were touched by this diff, and none of them iterate a directory this task added a
file to.

**Verdict on this gap**: both of the two steps I added are genuinely applicable by a stricter
reading than the engineer's, but **both pass** when actually run — there is no functional
defect, only an imprecise blanket justification in the hand-off (the same shape as T-1024's own
round-2 nit: correct steps run, correct results, but the disclosed reasoning for the "skipped"
set doesn't hold up perfectly under a literal reading). Not escalated to rework: the task's own
framing hands AC8's discharge to QA precisely because it cannot be machine-checked, and I have
now independently exercised the two steps the blanket reason missed with clean results. Flagged
below for the reviewer as a documentation-precision nit, not a defect.

### Board hygiene under batch mode

- `git diff --numstat eae0667 -- .shell-team/todo.md` -> `14  0` (14 added, 0 deleted) — pure
  insertion, confirmed by reading the full diff (only one new hunk, appending under `## Active`;
  no existing line touched).
- T-1023's and T-1024's `## Done`/board entries are untouched — confirmed by the same diff (no
  other hunk exists) and by both neighbours' `check-intent.sh` still reporting `aligned`.

### Regressions / adjacent code paths

None applicable. `bin/` and everything under `tests/rollup-runs/`, `tests/cluster-failures/`,
T-1019's spec and provenance are byte-identical to `eae0667` (AC6, re-verified live via
`git diff --name-only`). The only production-shaped artifact touched is the one file the spec
scoped to, and the diff inside it is exactly the three places D4 confines (delete line 96,
rewrite the header paragraph, rewrite the doc-comment paragraph) — verified by reading the file
end to end, not merely by the passing `check:` lines.

### Edge cases tried

- Re-derived AC8's ran/skipped list independently rather than accepting the engineer's blanket
  reasoning at face value (see above) — found and closed a documentation-precision gap with no
  functional consequence.
- Confirmed the deliberately-permitted word "third" appears only in the durable-pointer sentence
  and never in a "three ..." shape (AC2's own negative grep already locks this; read it by eye
  too, per this task's own instruction to make AC8's reading pass the backstop for the bounded
  grep's declared paraphrase residual).
- This task's deliverable is a one-file test-suite comment/dead-code edit with no runtime input
  surface of its own (Input space: "not applicable — the subject... is the suite's own code
  structure and comments, not its treatment of any input"); the adversarial fixture synthesis
  checklist's classes 1–3 (payload-shape / regex-anchoring / CommonMark-parsing adversarial
  input) have no applicable execution surface here beyond the literal-grep boundaries AC1/AC2
  already exercise via the mutation self-checks above. Class 4 (test-harness short-circuit) is
  exactly where this round's AC8 finding lives, and it is closed above with a clean result.

### Risk notes for reviewer

- **Documentation-precision nit, not a FAIL driver**: the engineer's AC8 blanket reasoning
  ("everything else is byte-identical to `eae0667`, so nothing new to check") does not literally
  hold for two live "dogfood" steps (`Dogfood check-interventions`, `Dogfood retro-inputs`) that
  read directories this task added a new file to, rather than reading only byte-identical
  surfaces. Both steps pass when actually run (verified above) — worth a glance at review time,
  not worth a rework round over a passing result.
- No `out-of-input-space` exemption was needed this round — no candidate finding depended on an
  out-of-scope synthetic extreme, and the one finding above is a disclosure-precision nit with a
  clean underlying result, not an acceptance-criterion violation.
- AC6/AC7 are explicitly merge-point-scoped against `eae0667` per the spec's own text; I did not
  widen or re-derive that base ref, per the spec's instruction.

### Suggested next step (non-blocking)

If a future rework round touches this board entry for any other reason, tighten the AC8
sub-bullet's blanket-inapplicable sentence to carve out live dogfood steps that iterate a
directory this task added a file to. Not worth a dedicated round on its own — cheapest handled
at review time or folded into the next touch of this entry.
