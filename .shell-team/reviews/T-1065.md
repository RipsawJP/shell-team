### Codex Review verdict: REQUEST_CHANGES
- Task: T-1065
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 9 files, +400/-0 lines (`git diff --stat 464c1fbfbd07a270e6bc6757946a018df5ceb81e...HEAD`), 16 commits

This round ran the primary `codex exec --sandbox read-only --cd <repo> review --base 464c1fbfbd07a270e6bc6757946a018df5ceb81e --json` pass (`.shell-team/reviews/T-1065-codex-primary.{txt,jsonl}`) plus a devil's-advocate adversarial pass on both of its findings (`.shell-team/reviews/T-1065-codex-adversarial.{txt,jsonl}`), triggered because the primary pass's first finding normalizes to `blocker` (Codex-native `[P1]`) per the severity map. Independently of Codex I re-ran all 11 static acceptance criteria live (`bash bin/check-acs.sh .shell-team/specs/T-1065-task-class-verification-pricing.md` → **11 passed, 0 failed, 1 skipped (AC12, runtime), 0 unrecognized**, matching the engineer's and QA's own reports exactly) and `bash bin/check-intent.sh .shell-team/specs/T-1065-task-class-verification-pricing.md .shell-team/todo.md` → **aligned: T-1065 v1 (c6c1b84690e83ff4466f6fd3355982cb13b1d108)**, unchanged since freeze — no class-B/M re-freeze occurred this round, so that item does not apply. I independently re-derived the read-set population (`git grep -lF -e agents/pm-spec.md -e skills/run/SKILL.md -e skills/goal/SKILL.md -- '.shell-team/specs/*.md'` = 45, matching the committed `- read-set-specs: 45 specs` line) and then independently probed the primary finding by widening that same derivation to also search `docs/adopting.md` / `docs/adopting.ja.md` (the two other non-record paths this task edits), which is what surfaced `.shell-team/specs/T-1046-ignored-base-verdict.md` as an omitted row — see Minor/nits below. I also independently confirmed, via `git show`, that `docs/adopting.md` at this branch's own base ref (`464c1fb…`) already differs from the blob `.shell-team/specs/T-1046-ignored-base-verdict.md` AC2 pins (`d825293`), i.e. that criterion is pre-existing FAIL, not newly broken by this task. Issue-canon match: **verified directly** — `curl -fsSL https://api.github.com/repos/RipsawJP/shell-team/issues/258` (state `open`, title `"Verification protocol needs a task-class tier: prose/editorial tasks inherit code-task fixed costs unpriced"`) and `.../pulls/267` (state `open`, `head.ref=chore/lesson-promotion-2026-08-13`, `base.ref=develop`, `merged=false`) both match this spec's claims exactly, over the network this sandbox's host allowlist permits (`api.github.com`) — not environmentally-unverified.

The tier text (`TIER`), the briefing norm (`BRIEF`/`BRIEFG`) and the DP1 scoping of the existing Downstream-impact-inventory item all read soundly: AC1/AC2 confirm `TIER` and `BLAST` are distinct, both scoping literals (`Downstream-impact inventory method`, `governs unchanged`) are present, and no reader is left choosing between the two norms — a `mechanism` task follows `BLAST` unchanged, a `no-mechanism` task follows `TIER`'s narrower method, with `TIER` explicitly forbidding a `grep`-of-mentions substitute where a full-population diff is owed. AC3/AC4 confirm `BRIEF` and `BRIEFG` are one norm on two surfaces (`BRIEF` occurs zero times in `skills/goal/SKILL.md`); reading `BRIEF`'s text adversarially, it explicitly forbids asking the reviewer to withhold a finding, lower a severity, or leave a surface unexamined, and requires an out-of-scope finding to be "recorded and dispositioned rather than discarded" — it does not invite suppression. `agents/codex-reviewer.md` and `agents/qa-verifier.md` are byte-identical to base, as Non-goals require.

#### Blockers
(none)

#### Major
- `docs/adopting.md:84-89` / `docs/adopting.ja.md:86-96` — the new section states the `- verification-class:` declaration as something "every spec's own frozen intent block" carries, unqualified. The actual shipped rule (`agents/pm-spec.md`'s new self-check item, same diff) is explicitly forward-only — "it governs specs written from here on and never re-opens a criterion already merged into a frozen spec" — and only 1 of the corpus's ~70 specs (T-1065 itself) currently carries the field. This directly contradicts the shipped agent-prompt text in the very same diff, and it is inconsistent with this same file's own established precedent for the identical kind of declaration: the T-1061 adopter-docs section two headings below reads "Every spec **frozen from T-1061 onward** declares..." (`docs/adopting.md:286`) — explicitly forward-scoped wording that this new section should have mirrored but does not. An adopter reading the unqualified claim could reasonably conclude the other 69 frozen specs are malformed, or attempt to retrofit the field onto an already-merged spec, which the forward-only rule explicitly forbids. Fix: qualify both pages' new section with the same "frozen from T-1065 onward" framing the T-1061 section already models — a prose-only correction; AC6's check line does not pin this wording either way, so no re-freeze is required.

#### Minor / nits
- `.shell-team/specs/T-1065-task-class-verification-pricing.md` AC8 / AC12(c) — the read-set-scoped derivation this task dogfoods on itself (`git grep -lF -e agents/pm-spec.md -e skills/run/SKILL.md -e skills/goal/SKILL.md -- '.shell-team/specs/*.md'`) only searches the three edited *norm/prompt* paths, not the two adopter-doc paths (`docs/adopting.md`, `docs/adopting.ja.md`) this task also edits, even though the tier's own general text (`TIER`) defines the method as covering "any path this task edits." Widening the search to include the two adopter-doc paths surfaces `.shell-team/specs/T-1046-ignored-base-verdict.md` **AC2**/**AC4** as a merged criterion this task's own `## Blast radius` table never names. This is a real completeness gap in the disclosure, but not a newly-caused break: T-1046 AC2 (byte-identity of `docs/adopting.md` against a specific base blob) is independently confirmed **already FAIL at this task's own base ref** (`464c1fb`'s `docs/adopting.md` already differs from the blob T-1046 AC2 pins), so this task only deepens a pre-existing red exactly like the several other rows the same table already discloses as "already red, deepened not caused" — it does not flip a passing criterion to failing. T-1046 AC4 (a token-absence sweep over `docs/`, among other trees) stays PASS, since the new section introduces none of its forbidden tokens. AC8's own check line is part of the frozen v1 intent and cannot be widened without a class-B re-freeze, so the actionable fix here is a one-line disclosure addition to the (unfrozen, Runtime/SKIP) AC12(c) narrative and the `## Blast radius` table naming this exclusion and T-1046's pre-existing state — consistent with DP2's own "run or disclose" discipline, applied to this additional gap the same way it is already applied to the indirection class.
- `agents/pm-spec.md`'s new `TIER` item defines the `mechanism` default's enumeration as "any path under `bin/`, `tests/`, `templates/`, a CI workflow, or the semantics of a checker." A non-CI file under `.github/` (an issue template, `CODEOWNERS`, a Dependabot config) is not literally covered by "a CI workflow." This task does not touch any such file, so it is purely a forward-looking wording gap for a future task's classification judgment, not a defect in this deliverable.

#### Adversarial findings
Devil's-advocate pass on both primary-pass findings (`.shell-team/reviews/T-1065-codex-adversarial.txt`):
- On the read-set-derivation gap: confirmed the gap is "formally real" against `TIER`'s own general wording, confirmed via direct blob comparison (`957acc5…` vs `efb6754…` for the English page, `9e6aa4b…` vs `cd49d23…` for the Japanese page) that T-1046 AC2 was already FAIL at the base ref and stays FAIL at HEAD — no newly-introduced verdict flip — and confirmed T-1046 AC4 stays PASS. Concluded "an incomplete dogfood/inventory claim worth correcting, but not a demonstrated harmful omission warranting a merge block" (severity: minor).
- On the adopter-docs overstatement: confirmed the unqualified wording conflicts materially with `agents/pm-spec.md`'s explicit forward-only rule, independently found and cited the exact precedent this same page already sets — "Every spec frozen from T-1061 onward" at `docs/adopting.md:286` — and concluded the omission "can make adopters treat 69 valid frozen specs as malformed or attempt forbidden retrofits" (severity: major, would block merge).

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Read-set derivation (AC8/AC12(c)) excludes `docs/adopting.md`/`.ja.md`, omitting T-1046 AC2/AC4 from the Blast radius table | primary: Codex-native `[P1]` (→blocker per normalization map); adversarial: partially agree (→minor) | minor | formally real gap, but no newly-introduced verdict change — T-1046 AC2 is independently confirmed already FAIL at the base ref (pre-existing, unrelated to this task) and AC4 stays PASS; this is QA-audit-completeness/records-hygiene territory under this review's own severity calibration, and AC8's check line is frozen v1 (cannot be widened without a re-freeze), so it is downgraded to a disclosure/narrative correction rather than a merge-gating defect |
| 2 | `docs/adopting.md`/`.ja.md`'s new section overstates the declaration as carried by "every spec," contradicting the shipped forward-only rule and this same file's own T-1061 precedent phrasing | primary: Codex-native `[P2]` (→major); adversarial: agree (→major) | major | real, material inaccuracy in adopter-facing documentation shipped in this same diff — squarely deliverable-correctness, not process bookkeeping — directly contradicted by `agents/pm-spec.md`'s own shipped text and inconsistent with this repository's own existing precedent for the identical kind of declaration; cheap to fix (prose-only, no re-freeze needed) |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the Major finding above (docs/adopting.md's forward-only qualifier)

#### Fast-follow disposition
- Minor finding #1 above (read-set derivation's disclosure gap re: T-1046 AC2/AC4) — recommended to be folded into the same rework pass rather than deferred, since the round is already going back to `engineer` for the Major finding and the fix is a one-line addition to the unfrozen AC12(c) narrative and Blast-radius table. Not treated as a fast-follow deferral.
- Minor/nit re: `.github/` non-CI files not literally covered by the `mechanism` default's "a CI workflow" wording — **won't-fix for this task**: this task touches no `.github/` file, the boundary is a forward-only judgment call DP4 already declares un-mechanizable, and the gap is purely theoretical until a future task actually needs it.
- no other fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1065
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope (since round 1's review, `bf3040e..d5312cb`): 4 files, +12/-6 lines (`.shell-team/specs/T-1065-task-class-verification-pricing.md`, `.shell-team/todo.md`, `docs/adopting.ja.md`, `docs/adopting.md`); rework commit `72e5a05` + QA round-5 board commit `d5312cb`. Frozen intent hash unchanged (`c6c1b84690e83ff4466f6fd3355982cb13b1d108`, v1) — no re-freeze this round.

This round ran the primary `codex exec --sandbox read-only --cd <repo> review --base bf3040e --json` pass (scoped to the delta since round 1's own review commit — `.shell-team/reviews/T-1065-codex-primary.{txt,jsonl}`, overwritten per the canonical-basename rule) plus a devil's-advocate adversarial pass on its one finding (`.shell-team/reviews/T-1065-codex-adversarial.{txt,jsonl}`, likewise overwritten). Independently of Codex: `bash bin/check-acs.sh` → **11 passed, 0 failed, 1 skipped (AC12, runtime), 0 unrecognized**, AC6 and AC8 individually confirmed PASS; `bash bin/check-intent.sh .shell-team/specs/T-1065-task-class-verification-pricing.md .shell-team/todo.md` → **aligned**, hash unchanged. `bash bin/check-pii-shapes.sh --base 464c1fb…`, `bash bin/check-provenance.sh`, `bash bin/check-interventions.sh --task T-1065` all re-run and match QA round 5's own report exactly (clean; 3 decision entries/0 sentinel; 1 entry/0 sentinel).

**Round-1 Major, independently confirmed closed.** Read both new opening sentences directly: `docs/adopting.md:84-86` now reads "Every spec **frozen from T-1065 onward** additionally carries, on one line inside its own frozen intent block, a declaration..."; `docs/adopting.ja.md:86-87` mirrors it ("**T-1065 以降に凍結するすべての spec** は、凍結された intent block 内の 1 行で..."). Both now match the grammatical shape of this same page's own T-1061 precedent (`docs/adopting.md:286` "Every spec frozen from T-1061 onward declares...", `docs/adopting.ja.md:289`'s mirror) exactly. Checked consistency across all three surfaces that state a forward-only scope, as asked: `agents/pm-spec.md`'s `TIER` item (unchanged since freeze, already explicitly "(forward-only)" in its own title and closing sentence), `docs/adopting.md`, `docs/adopting.ja.md` — all three now agree; no unqualified "every spec" survives anywhere in this diff. `check-acs.sh` AC6 confirmed still PASS (the wording isn't pinned, only tokens/anchors/adjacency/ordinals/byte-identity are).

**Round-1 Minor, closed for the specific row it named but the fix under-enumerates its own widened population — new finding, see Major below.** The rework added `.shell-team/specs/T-1046-ignored-base-verdict.md` AC2 (disclosed, `base:FAIL→head:FAIL`, pre-existing) and AC4 (`PASS→PASS`) to the `## Blast radius` table with the widened derivation command stated verbatim, and added a `Path-exclusion disclosure` bullet. Independently re-ran the exact widened command (`git grep -lF -e agents/pm-spec.md -e skills/run/SKILL.md -e skills/goal/SKILL.md -e docs/adopting.md -e docs/adopting.ja.md -- '.shell-team/specs/*.md'`) → **47 specs**, matching the disclosed count.

#### Blockers
(none)

#### Major
- `.shell-team/specs/T-1065-task-class-verification-pricing.md:226` (`## Blast radius`, the round-1 rework's new row) — the widened derivation the rework itself ran and cites returns **two** specs beyond the original 45, not the one it discloses. Independently re-derived (`comm -23` between the 47-spec and 45-spec result sets): `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` (via its **AC16**, a scope-lock criterion comparing `git diff --name-only 6439eb6` against a fixed allow-list that omits `docs/adopting.md`/`.ja.md`) **and** `.shell-team/specs/T-1046-ignored-base-verdict.md` (AC2/AC4, the one actually disclosed). The rework's own text and its new table rows name only T-1046, silently omitting T-1042 AC16 even though the same command it ran and quoted also returns it. Independently confirmed no newly-caused breakage: at this task's own base ref (`464c1fbfbd07a270e6bc6757946a018df5ceb81e`), `git diff --name-only 6439eb6 464c1fb…` already contains 199 paths, 189 of them outside AC16's allow-list — T-1042 AC16 is unequivocally, massively pre-existing FAIL, unrelated to and merely deepened by this task, exactly like T-1046 AC2. T-1042's own spec text carries a carve-out ("This criterion is merge-point-scoped and is expected to go stale after merge... a finding that it should be [kept evergreen] is out of contract") — but, confirmed on direct read, that carve-out is about not re-deriving/re-measuring AC16 as an ongoing obligation; it does not exempt a *different* task (T-1065) from naming a result its *own* claimed-complete widened derivation actually surfaces. Classified as a recurrence of the same defect shape this task's QA history already spent four rounds closing for a different artifact (`whole-set-claim-under-enumeration` — a population/enumeration claim that turns out, on independent re-derivation, to be short by one member) — this time in the Blast-radius disclosure rather than the board narrative. Fix: add the T-1042 AC16 row (disclosed, pre-existing/deepened, same shape as T-1046 AC2) to the same table and the AC12(c)/path-exclusion-disclosure text; bounded, prose-only, no re-freeze (this is exactly the fix already applied once for T-1046, applied one more time).

#### Minor / nits
(none this round — the round-1 Minor is now folded into the Major above as a same-class recurrence)

#### Adversarial findings
Devil's-advocate pass on the Major above (`.shell-team/reviews/T-1065-codex-adversarial.txt`): agreed the finding is valid — independently confirmed the widened command returns exactly T-1042 and T-1046, confirmed T-1042 AC16 is unequivocally FAIL at both base (199 changed paths, 189 outside the allow-list) and HEAD (203 changed paths, 193 outside), and confirmed the carve-out in T-1042's own spec is about not re-deriving that criterion going forward, not about exempting a different task's own claimed-complete derivation from naming it. The adversarial pass itself rated this **minor** ("causes no verdict flip or product defect, needs only a bounded disclosure correction") — see the ledger below for why this review's own synthesis keeps it at Major instead.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Round-1 Minor (T-1046 AC2/AC4 undisclosed) | — | closed | independently confirmed fixed: both rows now present in the `## Blast radius` table, matching the round-1 finding exactly |
| 2 | Round-1 Major (adopter-docs forward-only overstatement) | — | closed | independently confirmed fixed on both pages, consistent with `agents/pm-spec.md`'s `TIER` text and this page's own T-1061 precedent phrasing; AC6 stays PASS |
| 3 | The rework's widened-derivation disclosure names only T-1046, omitting T-1042 AC16, which the same command also surfaces | primary: Codex-native `[P2]` (→major per normalization map); adversarial: agree, severity **minor** (no verdict flip, no product defect) | **major** | overriding the adversarial pass's raw material-impact-only severity: this is a second, independent occurrence of the identical `whole-set-claim-under-enumeration` defect shape inside this same task (first occurrence: QA rounds 1-3 against the board narrative, closed by premise change 2; here: round 2 against the Blast-radius disclosure, one round after the analogous round-1 gap was "closed"). This repo's own same-class-2 convention (`skills/run/SKILL.md`) treats a second occurrence of one class as warranting a full, bounded-but-complete fix rather than a spot patch — reported at `major` here so that discipline applies, even though the underlying criterion (T-1042 AC16) is independently confirmed pre-existing-FAIL and no new breakage results |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the Major finding above (add the missing T-1042 AC16 disclosure row to the `## Blast radius` table and the path-exclusion-disclosure text, mirroring the T-1046 fix already applied)

#### Fast-follow disposition
- no fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES (round 3)
- Task: T-1065
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope (since round 2's review, `58e55ca..29881a3`): 2 files, +7/-2 lines (`.shell-team/specs/T-1065-task-class-verification-pricing.md`, `.shell-team/todo.md`); operator ruling `02c59b2` (option B: bounded rework + issue #268 filed) + rework `04c773f` + QA round-6 `29881a3`. Frozen intent hash unchanged (`c6c1b84690e83ff4466f6fd3355982cb13b1d108`, v1) — still no re-freeze across all three rounds.

This round ran the primary `codex exec --sandbox read-only --cd <repo> review --base 58e55ca --json` pass (scoped to the delta since round 2's own review commit — `.shell-team/reviews/T-1065-codex-primary.{txt,jsonl}`, overwritten) plus a devil's-advocate adversarial pass on its one finding (`.shell-team/reviews/T-1065-codex-adversarial.{txt,jsonl}`, overwritten). Independently confirmed issue #268 live (`curl -fsSL https://api.github.com/repos/RipsawJP/shell-team/issues/268` → open, title `"Set-population claims in loop records need machine derivation, not hand-written prose"` — matches the coordinator's description exactly, directly verified over `api.github.com`, not environmentally-unverified). `bash bin/check-acs.sh` → **11 passed, 0 failed, 1 skipped (AC12, runtime), 0 unrecognized**; `bash bin/check-intent.sh` → **aligned**, hash unchanged.

**Round-2 Major, independently reproduced and confirmed closed for the delta it named.** Re-ran the exact `comm -13` derivation the operator ratified: `git grep -lF -e agents/pm-spec.md -e skills/run/SKILL.md -e skills/goal/SKILL.md -- '.shell-team/specs/*.md' | LC_ALL=C sort` vs. the five-path-widened equivalent, `LC_ALL=C comm -13` between them → exactly two lines, `.shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` and `.shell-team/specs/T-1046-ignored-base-verdict.md` — matching the spec's own disclosure verbatim. Both now carry Blast-radius rows (T-1046 AC2/AC4 from round 1's rework; T-1042 AC16 newly added this round). `bash bin/check-acs.sh .shell-team/specs/T-1042-ignored-base-and-retro-ledger.md` → AC16 FAIL, matching the row's `head:FAIL`.

**New finding this round — while independently re-verifying the T-1042 AC16 row's own arithmetic, found a cardinality-labeling error the row (and its three verbatim repeats on the board) carries forward.**

#### Blockers
(none)

#### Major
- `.shell-team/specs/T-1065-task-class-verification-pricing.md:243` (the new T-1042 AC16 row), repeated verbatim at `.shell-team/todo.md:33,41,48` — the row describes T-1042 AC16's own allow-list as a "ten-path allow-list... of which 193 sit outside." Independently counted T-1042's own AC16 prose (`bin/team-paths.sh`, `bin/retro-inputs.sh`, `tests/retro-inputs/run.sh`, `tests/team-paths/run.sh`, `.shell-team/todo.md`, "this spec" [T-1042's own spec path], `.shell-team/provenance/T-1042.md`, `.shell-team/reviews/T-1042.md`, `.shell-team/interventions/T-1042.md`, `.shell-team/reviews/T-1042-codex-adversarial.md`, `.shell-team/test-recipe.md`) — **eleven** distinct paths, not ten. Independently cross-referenced against the live 203-path diff (`git diff --name-only 6439eb6 | wc -l` = 203): 10 of the 11 allow-listed paths are present (`grep -Fxf`), 193 are outside (`grep -vFxf`) — the **193-outside figure is numerically correct**, but "ten-path allow-list" conflates the allow-list's own cardinality (11) with its intersection with the current diff (10); the eleventh member, `.shell-team/reviews/T-1042-codex-adversarial.md`, was apparently never created as a tracked file, so it is a permitted-but-absent allow-list member, not evidence the allow-list itself has ten entries. Correct wording: "eleven-path allow-list, of which ten are present, 193 outside." Classified as a **third, independent recurrence of `whole-set-claim-under-enumeration`** in this task's own records — the identical general defect shape (a set's size stated in prose rather than mechanically counted) that already produced round 2's Major, an operator-fired STOP escalation, a ratified machine-derivation discipline, and issue #268 filed specifically against it. Notably, even QA round 6's own re-verification bullet states "AC16's own 11-path allow-list" correctly in one clause while approvingly quoting the row's "ten-path allow-list" phrase in the same sentence — the arithmetic was checked, the cardinality label was not, showing how this specific sub-error slips through even careful re-derivation. No verdict flip or product defect results (AC16's own PASS/FAIL determination and the 193-outside count are both accurate); reported at Major for class-recurrence consistency with this review's own round-2 classification and the operator's already-demonstrated sensitivity to this exact defect shape, not for material impact. Fix: correct "ten-path" to "eleven-path" (or equivalent — "an eleven-path allow-list, ten of which are present in the diff") at all four occurrences (the spec row plus the three board repeats), mechanically re-counting T-1042's own AC16 prose (e.g. a `grep -c` or explicit enumeration) rather than re-typing the figure by hand.

#### Minor / nits
(none this round)

#### Adversarial findings
Devil's-advocate pass on the Major above (`.shell-team/reviews/T-1065-codex-adversarial.txt`): agreed the finding is valid and independently re-derived the same numbers (203 total, 11 allow-listed, 10 present, 193 outside, sole absent member `.shell-team/reviews/T-1042-codex-adversarial.md`). Explicitly rated it **major** on its own — not minor — reasoning that while the row's intrinsic product impact is minor, consistency with this review's own round-2 elevation of the prior record-only recurrence to Major (on repeated-class grounds) warrants the same treatment here; this round, unlike round 2, the adversarial pass agreed with Major directly rather than requiring a synthesis override.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Round-2 Major (T-1042 AC16 omitted from widened-delta disclosure) | — | closed | independently reproduced the operator-ratified `comm -13` derivation exactly ({T-1042, T-1046}); the T-1042 AC16 row is now present with the correct base/head verdict |
| 2 | New T-1042 AC16 row mislabels its own allow-list as "ten-path" when it is eleven-path (ten present, 193 outside — the outside count itself is correct) | primary: Codex-native `[P2]` (→major per normalization map); adversarial: agree, major (no override needed) | major | third independent recurrence of `whole-set-claim-under-enumeration` in this task's own records — the identical defect shape issue #268 was filed against; reported at Major for class-recurrence consistency with round 2's own classification, even though material/product impact is minor (the numeric 193-outside figure and the AC16 PASS/FAIL verdict are both correct) |

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the Major finding above (correct "ten-path" to "eleven-path" at all four occurrences — the spec's Blast-radius row and the three board repeats — by mechanically re-counting T-1042's own AC16 allow-list rather than re-typing the figure)

#### Fast-follow disposition
- Round-1 nit re: `.github/` non-CI files not literally covered by the `mechanism` default's "a CI workflow" wording (`agents/pm-spec.md`'s `TIER` item) — **won't-fix for this task**: this task touches no `.github/` file, the boundary is a forward-only judgment call DP4 already declares un-mechanizable, and the gap is purely theoretical until a future task actually needs it. (Carried forward unchanged from round 1; still the only standing deferral for this task.)
- no other fast-follow deferrals

---

### Codex Review verdict: APPROVE (round 4)
- Task: T-1065
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope (since round 3's review, `347f8a1..a830ce8`): 2 files, +9/-3 lines (`.shell-team/specs/T-1065-task-class-verification-pricing.md`, `.shell-team/todo.md`); operator ruling `97179e3` (options B+C combined) + rework `6b091f5` + QA-round-7-triggered fix `e4437b7`/`f341ad5` + QA round-8 `a830ce8`. Frozen intent hash unchanged (`c6c1b84690e83ff4466f6fd3355982cb13b1d108`, v1) — no re-freeze across all four rounds.

This round ran the primary `codex exec --sandbox read-only --cd <repo> review --base 347f8a1 --json` pass (`.shell-team/reviews/T-1065-codex-primary.{txt,jsonl}`, overwritten) plus a devil's-advocate adversarial pass on both of its findings (`.shell-team/reviews/T-1065-codex-adversarial.{txt,jsonl}`, overwritten). Independently confirmed the operator-ratified option-C calibration text at `97179e3` matches the coordinator's own description exactly (direct read, not relayed). `bash bin/check-acs.sh` → **11 passed, 0 failed, 1 skipped (AC12, runtime), 0 unrecognized**; `bash bin/check-intent.sh` → **aligned**, hash unchanged; `check-pii-shapes.sh --base 464c1fb…` clean; `check-provenance.sh` conformant (3/0); `check-interventions.sh --task T-1065` conformant (1/0) — all matching QA round 8 exactly.

**Round-3 Major, independently confirmed closed.** Re-counted T-1042 AC16's own allow-list from its primary-source prose (`sed -n '146,147p' .shell-team/specs/T-1042-ignored-base-and-retro-ledger.md`) myself: **11** distinct paths, not 10. Cross-referenced against the live 203-path diff from `6439eb6`: 10 present, 193 outside, 1 permitted-but-absent (`.shell-team/reviews/T-1042-codex-adversarial.md`) — matches the spec's corrected row and all four board sites exactly, with cardinality (11) and diff-intersection (10) now stated as two separate numbers everywhere.

**Two candidate findings surfaced by the primary pass this round; both investigated to ground and found non-gating.**

#### Blockers
(none)

#### Major
(none)

#### Minor / nits
- `.shell-team/todo.md:46` (AC12(c) narrative's indirection-class discharge) — the discharge described (checking whether each of the 13 board-indirection specs *also* references one of the three edited norm files, finding zero) is a narrower check than DP2's literal "run those criteria too, or disclose what stays unmeasured" — a genuine, if small, methodological rigor gap in how the discharge is described (Codex-native `[P1]`, normalizes to blocker by the severity map). Independently investigated all 13 named specs (T-1003, T-1006, T-1010, T-1016, T-1017, T-1019, T-1022, T-1023, T-1024, T-1025, T-1038, T-1059, T-1062) at the primary source and found their board-touching criteria fall into three buckets, none representing live risk from T-1065's own board edits: (a) live re-derivable checks (`bash bin/check-handoff.sh`/`bash bin/check-board-headings.sh` against the current board) that pass as long as the board stays well-formed, independently confirmed clean every round; (b) specific-other-entry text-presence checks (T-1003, T-1006) that stay satisfied because T-1065's own AC9 guarantees no existing top-level board entry is altered — independently confirmed both target strings still present; (c) merge-point-scoped criteria comparing the board (or the whole changed-file set) against an old historical ref, already massively pre-existing FAIL for reasons entirely unrelated to and predating T-1065 — independently confirmed: T-1024 AC8's board-diff since `1e31600` already shows `1949/2` (non-zero deletion column, already FAIL); T-1025's board-diff since `eae0667` already shows `1928/3` (already FAIL); T-1038's full changed-file-set scope-lock since `2dc423b` already has 220 extra files beyond its allow-list (already FAIL). Downgraded to minor/note: a real audit-completeness gap in the discharge narrative, not an undisclosed deliverable risk — consistent with this review's own severity calibration (process-bookkeeping/audit-completeness findings are notes unless they invalidate the deliverable) and confirmed independently by the adversarial pass ("a misleading discharge narrative and audit-completeness gap, not an undisclosed deliverable risk... do not withdraw READY_FOR_REVIEW").
- `.shell-team/todo.md:33` (the round-3 STOP-escalation bullet, historical/append-only) — Codex flagged (`[P3]`, already minor by the severity map) that the bullet's "measured at 6 occurrences" figure includes 2 unrelated `T-1046` "ten-pathspec" hits from a raw, unqualified grep. Investigated and found this is already fully resolved: the bullet itself, at the time it was written, honestly flagged the figure as provisional ("more than the review's stated 4 — the fix, if authorized, derives its own occurrence set mechanically"), and the actual rework this round's own diff carries already re-swept with a word-bounded regex, explicitly named and excluded the T-1046 "ten-pathspec" hits as unrelated, and corrected the three live sites. The historical bullet is accurately preserved append-only history of what a cruder measurement showed at STOP time, consistent with this task's own established convention for other historical quotes in the same bullet (never rewritten, disclosed as superseded). No action needed — the adversarial pass agreed directly ("nothing remains to fix... rewriting the old STOP record would make the append-only history less accurate, not more").

#### Adversarial findings
Devil's-advocate pass on both candidates above (`.shell-team/reviews/T-1065-codex-adversarial.txt`): independently reproduced the 13-spec set, the `41/0` scope-lock diff, and the three already-broken merge-point-scoped criteria's own historical deltas (`1949/2`, `1928/3`, 220-extra-paths); rated finding 1 minor ("a misleading discharge narrative and audit-completeness gap, not an undisclosed deliverable risk") and finding 2 nit/no-action ("nothing remains to fix... rewriting the old STOP record would make the append-only history less accurate").

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Round-3 Major (T-1042 AC16 "ten-path" mislabel) | — | closed | independently recounted T-1042's own allow-list prose at the primary source (11, not 10); the corrected row and all four board sites now state cardinality and diff-intersection as two separate, correct numbers |
| 2 | AC12(c)'s indirection-class discharge checks a narrower condition (also-references-norm-files) than DP2's literal run-or-disclose requirement | primary: Codex-native `[P1]` (→blocker per normalization map); adversarial: partially agree, minor | minor | independently investigated all 13 named specs at the primary source; none represents live risk (live-rederivable / other-entry-preserved / already-pre-existing-broken-and-unrelated, in three confirmed buckets) — a real audit-completeness gap in the discharge narrative's rigor, not an undisclosed deliverable risk, per this review's own process-bookkeeping calibration |
| 3 | The round-3 STOP bullet's historical "6 occurrences" figure includes 2 unrelated T-1046 hits | primary: Codex-native `[P3]` (already minor per normalization map); adversarial: disagree, nit | nit, no action | the bullet itself already flagged the figure as provisional at STOP time; the actual rework already re-swept with a word-bounded regex and correctly excludes the T-1046 hits; the historical quote is accurately preserved append-only history, consistent with this task's own established convention |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- Round-1 nit re: `.github/` non-CI files not covered by the `mechanism` default's "a CI workflow" wording (`agents/pm-spec.md`'s `TIER` item) — **won't-fix for this task**: this task touches no `.github/` file, the boundary is a forward-only judgment call DP4 already declares un-mechanizable, and the gap is purely theoretical until a future task actually needs it.
- Round-4 note re: AC12(c)'s indirection-class discharge describing a narrower check than a literal run-or-disclose (Minor above) — **won't-fix for this task**: independently investigated to ground and found no live risk in any of the 13 named specs; a future no-mechanism task whose read-set-scoped method also needs to reason about the board file's own edits could tighten the discharge wording, but nothing here invalidates this task's own deliverable, and the underlying structural pattern (hand-described discharge methodology in prose) is already the subject of issue #268's broader remit.
- Round-4 note re: the round-3 STOP bullet's historical "6 occurrences" figure (nit above) — **won't-fix, no issue needed**: fully resolved by the existing rework; the historical bullet is accurate append-only history of a provisional measurement, not a live claim.
