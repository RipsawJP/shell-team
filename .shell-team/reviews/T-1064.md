### Codex Review verdict: APPROVE
- Task: T-1064
- Codex model: gpt-5.6-sol (configured default in `~/.codex/config.toml`; not printed verbatim in the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 11 files, +420/-11 lines (`git diff --stat f6a87d0...HEAD`), 10 commits

This round ran the primary `codex exec --sandbox read-only --cd <repo> review --base f6a87d0 --json` pass (`.shell-team/reviews/T-1064-codex-primary.{txt,jsonl}`) plus a devil's-advocate adversarial pass triggered by its one finding (`.shell-team/reviews/T-1064-codex-adversarial.{txt,jsonl}`). Independently of Codex, I re-ran every one of this spec's 11 static acceptance criteria live (`bash bin/check-acs.sh .shell-team/specs/T-1064-shipped-docs-accuracy.md` → **11 passed, 0 failed, 1 skipped (AC12, runtime)**, matching the engineer's and QA's own reports exactly), re-ran `bin/check-refreeze-class.sh` on both re-frozen pairs myself (both `class-b`, exit 1, with a byte-identical control pair independently confirmed exit 2), re-ran `bin/check-intent.sh` for all three specs (all `aligned`), re-ran `bin/check-prompt-sync.sh`, `bin/check-handoff.sh`, `bin/check-board-headings.sh`, `bin/check-pii-shapes.sh`, `bin/check-provenance.sh` and `bin/check-interventions.sh` (all clean/conformant), and independently reconstructed the exact single-clause diff of both re-frozen specs (`git diff f6a87d0...HEAD -- .shell-team/specs/T-1043-*.md .shell-team/specs/T-1050-*.md`) — confirmed exactly one clause changed inside each frozen `K2`/`R186` block, with every `- check:` line, criterion-bullet count and `- stale-at:` count untouched. I also independently confirmed Git's own behaviour with a throwaway scratch repo (`git grep -n <pattern> -- <pathspec>` and `git grep -n . -- <symlink-path>` both miss a tracked symlink's target content *and* its own link text), and fetched issues #206, #54 and #198 live via `api.github.com` (network reachable this round) — all three open, titles and bodies matching this task's stated sourcing; #206's own suggested-fix wording ("skips a tracked symlink entry entirely") matches the shipped `K2new` text verbatim, and #54's own "Candidate resolution" text explicitly names both the option this task took (generalize the substitute-instruction prose) and the option it declined (add the path to the pasted list) as "either is defensible" — so this task's choice is issue-canon-sanctioned, not merely internally consistent. Issue-canon match: **verified directly against the issues themselves**, not environmentally-unverified.

#### Blockers
(none)

#### Major
(none)

#### Minor / nits
- `docs/tuning-oversight.md:73` / `docs/tuning-oversight.ja.md:62` — #54's boundary decision lands only in the prose register (the generalizing clause on the substitute instruction, plus the two-enforcement-strengths paragraph two sections below); the fenced `CLAUDE.local.md` example block a reader copy-pastes still does not name `.shell-team/test-recipe.md`. A reader who copies the block verbatim without reading the surrounding prose still has the practical gap #54 originally identified. This is not a truth violation — every sentence added is accurate — and it is one of #54's own two candidate resolutions, explicitly disclosed as such in DP2 rather than silently chosen; T-1005's own DP3 already establishes that this page's block-vs-prose split does not carry surrounding prose with a copied block, so the limitation predates this task and is a property of the page's existing architecture, not a new defect T-1064 introduced.
- `.shell-team/specs/T-1064-shipped-docs-accuracy.md` DP2 (mutable prose outside the frozen intent block, and the parallel framing in `## Blast radius`) states the fenced block is "byte-locked in two directions by merged criteria" citing T-1005's AC1 and AC9. Read first-hand, T-1005's AC1 only pins specific continuation lines' *presence* (`grep -qF` checks) — it does not forbid adding a new line — and AC9 only requires the two files' fenced bodies to be byte-identical **to each other**, plus an exact fence-marker count that this same spec's own `## Blast radius` row already discloses as pre-existing FAIL/FAIL (8 fence lines measured on both sides, not 6, unrelated to and unmoved by this task). Neither T-1005 criterion would have been broken by adding the same new line to both fenced blocks in step. The actual base-blob byte-lock preventing that is this task's own freshly-written **AC7**, not an external, pre-existing constraint. The decision itself (leave the block untouched, generalize the prose) is fine and issue-canon-sanctioned per #54's own text; only the *reason given* for it is imprecise. No shipped/adopter-facing text is affected — DP2 is this spec's own design-rationale narrative, not frozen intent and not adopter-facing documentation.
- `agents/pm-spec.md:70` (and its two frozen twins, `K2` in T-1043's spec and `R186` in T-1050's spec) — the corrected clause "skips a tracked symlink entry entirely — reading neither the link's target nor the link text" is accurate for every ordinary enumeration/pathspec-scoped `git grep` invocation, which is the only form this Rule's own text ever recommends for a `- check:` absence assertion (`git grep -n -- '<pattern>' -- '<pathspec>'`, `--cached`, or a bare recursive search) — confirmed both by my own scratch-repo probe and by the adversarial pass. The adversarial pass found one falsifying invocation shape outside that scope: `git grep -F -n -e <pattern> <rev>:<path>` (explicit blob addressing) resolves straight to the symlink entry's own blob via `OBJ_BLOB`, bypassing the tree-walk's symlink-skip logic entirely, and does read the link's own text. Nobody in this codebase's own `- check:` line conventions writes a `- check:` line this way, so this is not a live miss for how the K2 convention is actually used — the sentence's wording is simply broader than it needs to be. #206's own suggested-fix wording is exactly what shipped, so this task correctly implemented its origin issue; the residual over-broadness is new information this review surfaced, not something #206 asked for.

#### Adversarial findings
Devil's-advocate pass on the two items above (`.shell-team/reviews/T-1064-codex-adversarial.txt`), triggered because the primary pass's one finding normalized to major (Codex-native `P2`) before synthesis:
- Confirmed (and sharpened) the DP2 attribution point above by reading T-1005's AC1/AC9 text directly and cross-referencing this spec's own Blast-radius row for T-1005 AC9.
- Surfaced the `git grep <rev>:<path>` direct-blob-addressing counter-example to the K2 mechanism claim, with a Git-source citation (`builtin/grep.c`), and explicitly stated it could not otherwise break either claim (no fabricated finding).
- Did not find any way to make a plain, pathspec/tree/index/worktree-scoped `git grep` invocation read a tracked symlink's target or link text, confirming the corrected claim as it is actually used in this Rule.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `docs/tuning-oversight.md:73`/`.ja.md:62` — fenced block still omits `.shell-team/test-recipe.md` for a verbatim-copy reader | primary: P2 (→major); adversarial: medium (→minor) | minor | the added prose is true; the chosen resolution is one of #54's own two issue-canon-sanctioned options (verified against the issue body directly), disclosed as a deliberate choice in DP2, and the block-vs-prose gap is a pre-existing property of T-1005's own architecture (its DP3), not a new defect this task introduced |
| 2 | `.shell-team/specs/T-1064-shipped-docs-accuracy.md` DP2 misattributes the fenced-block lock to T-1005 AC1/AC9 rather than to this task's own AC7 | adversarial: medium (→minor) | minor | verified by reading T-1005 AC1/AC9 directly: neither forbids the addition Codex proposed; the real lock is AC7, written by this same task. No shipped or frozen text is affected — DP2 is mutable design-rationale prose outside the intent block |
| 3 | K2/R186's corrected symlink claim is falsifiable via `git grep <rev>:<path>` direct-blob addressing | adversarial: low (→minor) | minor | true but out of the scope every `- check:` line in this codebase actually uses (pathspec/tree/index/worktree-scoped forms only); #206's own suggested-fix wording shipped verbatim, so the origin issue's canon is fully satisfied — this is new, additional information beyond #206's scope |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE

#### Fast-follow disposition
- Finding #1 (`docs/tuning-oversight.md`/`.ja.md` fenced block omits the advisory-source path for a verbatim-copy reader) — **won't-fix for this task**: #54's own issue body already names this exact trade-off and calls both options "defensible"; it also names issue #52 (already open) as the natural place to revisit the instantiation-list boundary again in the future. No new issue needed.
- Finding #2 (DP2 misattributes the fenced-block lock to T-1005 AC1/AC9 instead of this task's own AC7) — **won't-fix for this task**: harmless, non-frozen, non-adopter-facing design-rationale prose; the criterion it describes (AC7) is itself correct and green. Correcting the attribution after the fact buys nothing functional.
- Finding #3 (K2/R186's claim is over-broad for the `git grep <rev>:<path>` direct-blob-addressing shape) — **file-an-issue**: mirrors the precedent that produced #206 itself (a Minor cross-provider fast-follow, picked up at the next touch of the K2 line). Rationale: low practical relevance today (no `- check:` line in this codebase uses that invocation shape), but worth tightening the wording the next time K2/R186 is re-frozen for any other reason, consistent with the re-evaluation-trigger pattern this same spec already uses for T-1050's declined DP6 clause (DP3).
