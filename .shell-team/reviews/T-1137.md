### Codex Review verdict: REQUEST_CHANGES
- Task: T-1137
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests
- Diff scope: 7 files, +421/-3 lines (`git diff --stat feature/invocation-policy...HEAD`)

Own verification before invoking Codex (own-run, not transcribed from the hand-off): `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1137-excludes-detection.md` → `check-acs: 9 passed, 0 failed, 0 skipped, 0 unrecognized` (9/9, own command). Re-ran AC8's provenance `- reproduce:` line myself and diffed it byte-for-byte against the embedded `<!-- BEGIN/END derivation: t1137-refusal-text-readers -->` block — identical. Confirmed AC3(c)'s forbidden-literal sweep directly: `grep -v '^[[:space:]]*#' bin/check-durability.sh | grep -E 'check-ignore|\.shell-team|(^|[^-])tasks/'` → no match (exit 1). Read `bin/check-durability.sh` whole file, both README bullets in both languages, the frozen intent block of `.shell-team/specs/T-1048-handoff-durability-barrier.md` (not only the three ACs the spec's own `## Summarized sources` cites, but its DP6 design point at `:64`, inside the same frozen block), and the spec's own Scope ruling / Non-goals / Pre-commitment for T-1137 itself.

Codex's primary pass, corroborated independently by a targeted adversarial pass (live reproduction, not argument alone), surfaced three findings — one that breaches another already-merged task's frozen intent, and two recurrences of the exact `docs-overclaim` class this task's own round-1 QA FAIL already named once. None is caught by any `- check:` line in this spec, T-1048's, or the shared fixture suite, because all three are substring/fixed-string reads that cannot see a line's terminal shape, an alternate branch of a shared reason token, or a remedy clause's logical implication.

1. **`bin/check-durability.sh:111` no longer ends its failure line with `this hand-off is not durable.` — a breach of `.shell-team/specs/T-1048-handoff-durability-barrier.md`'s frozen intent, inside its `BEGIN/END intent-block: T-1048` markers.** That spec's DP6 (`:64`) states the contract in prose: "Every failure prints to stderr a line beginning `check-durability: not-durable: <reason>: ` and ending `— this hand-off is not durable.`" Its own AC3 (`:91`) repeats it: the checker "ends its sentence with the plain-language `this hand-off is not durable.`" Read the shipped `printf` at HEAD: the sentence now has ~180 more characters after it (the new disjunctive explanation and the `docs/adopting.md` pointer). The mechanical checks that read this string — `tests/check-durability/run.sh:112` and T-1048's own AC3 check line — both use `grep -qF` (substring), so they stay green; but the line no longer literally *ends* with the phrase two independent places inside T-1048's frozen block say it must. T-1137's own `## Summarized sources` entry for T-1048 cites only AC1–AC3 (`:87`–`:92`); it never reads DP6 (`:64`), which sits in the same frozen block one section earlier — the gap that let this through. This is the identical class T-1137's own D1/D1a refuse to touch for gap 2 (amending another task's merged frozen intent needs human GO), except this one wasn't refused — it was done inadvertently, by appending after rather than reading the full contract first. Codex's adversarial pass confirmed this directly by reading DP6 and AC3 against the live output and concluded "the defense fails."
   - Suggested fix: reorder the `printf` so the appended clause sits *before* `— this hand-off is not durable.` (the terminal position T-1048 fixes), or obtain a re-freeze of T-1048's DP6/AC3 the same way this task treats T-1046/T-1129 — never edit around it silently.

2. **The new disjunctive sentence overclaims for a reachable `no-recorded-commit` sub-case QA's round-2 five-token reproduction didn't test: an invalid `--ref` in an otherwise fully-committed repository.** `bin/check-durability.sh:211-213` fires `no-recorded-commit` whenever `git rev-parse --verify "${REF}^{commit}"` fails to resolve — this includes both an unborn repo (QA's tested case, where "never committed" holds vacuously) *and* a mistyped or stale branch name passed to a repository with a full, ordinary commit history elsewhere. `--ref` is always caller-supplied and never inferred (the script's own header), and the checker's threat model explicitly defends against accidents, not just the unborn-repo case. In the second sub-case none of the sentence's three disjuncts — "never committed," "edited since," or the hidden-base-dir "misconfiguration" clause — is true: the record may well be committed, unedited, and untouched by any ignore rule; the only problem is the ref pointer itself. T-1137's own Input space item 6 frames `no-recorded-commit` narrowly as "on an unborn repository" and never lists this variant under `## Non-goals`' out-of-scope synthetic extremes, so the spec's own downgrade-eligible carve-out does not apply here — this is an omission from the reachable-input enumeration, not a declined synthetic extreme. Codex's adversarial pass built exactly this fixture live (a checkout with real commit history and all four T-1137 records committed, then queried against a nonexistent ref) and confirmed the sentence asserts a cause with no supporting evidence.
   - Suggested fix: make the shared sentence neutral about the specific cause (e.g., drop the disjunctive causal claim in favor of "for one of several reasons" language), or special-case `no-recorded-commit` triggered by an unresolvable `--ref` separately from the four record-level tokens.

3. **README.md:60 / README.ja.md:60's remedy clause overclaims what choosing the repo-level `.gitignore` route avoids.** The bullet's new clause says the global route "makes every durability gate this loop runs report `not-durable` permanently there, with every hand-off staying local-only to that machine — prefer the repo-level `.gitignore` line above if you want to stay untracked without that cost." `bin/check-durability.sh` observes only whether a record is a committed blob at the named ref (`git ls-tree`); it has no notion of *why* a file is untracked. An adopter who follows the suggested remedy (repo-level `.gitignore` instead of global excludes) still leaves `.shell-team/` untracked in this repo, so every durability gate still reports `not-durable` permanently and every hand-off still stays local-only — identically to the global route. Repo-scoping avoids only the cross-repo, machine-wide spillover (hiding the base dir in *other* repos where the board is wanted), the cost named in the clause immediately before it, not the durability/local-only cost named immediately before "that cost." The Japanese mirror (`その代償を避けたいだけなら repo 側の .gitignore を優先する`) carries the identical ambiguity/overclaim, so this is not an English/Japanese drift — both languages assert the same thing, and it is not true of the durability consequence either way. Codex's adversarial pass confirmed this by tracing the checker's own blob-presence logic and concluded the repo-level route "avoids only cross-repo machine-wide spillover."
   - Suggested fix: split the remedy clause so it is unambiguous that repo-level `.gitignore` avoids only the machine-wide spillover, not the durability/local-only-hand-off consequence — which only tracking the base dir, or an explicit `working-tree-only` opt-out, actually avoids. Apply the same fix to both languages together per this spec's own Pre-commitment ("Japanese and English halves drop together or not at all").

**Own supplementary observation (Minor, not independently run through Codex):** the same disjunctive sentence's "never committed" / "edited since" split does not cleanly cover a `missing-working-file` fired by a record that *was* previously committed and is later deleted from the working tree without staging the deletion (the check order is working-file-existence first, `git ls-tree` second, so this reaches `missing-working-file` rather than `uncommitted-change`). QA's and the engineer's own five-token reproduction tested only the "record never authored" sub-case. This reads as a stretch of "edited since" rather than a clearly false claim, and I could not construct as sharp a counter-example as findings 1–3 above, so I keep it at Minor pending a firmer test.

#### Blockers

- `bin/check-durability.sh:111` — the failure line no longer ends with `this hand-off is not durable.`, breaching `.shell-team/specs/T-1048-handoff-durability-barrier.md`'s frozen DP6 and AC3 (finding 1 above). Suggested fix as stated above.

#### Major

- `bin/check-durability.sh:111` (triggered via `:211-213`) — the shared sentence overclaims for `no-recorded-commit` fired by an invalid `--ref` on an otherwise fully-committed repository; live-reproduced by the adversarial pass (finding 2 above). Suggested fix as stated above.
- `README.md:60` and `README.ja.md:60` — the remedy clause implies repo-level `.gitignore` avoids the durability/local-only consequence, when it only avoids cross-repo spillover; both languages carry the same overclaim (finding 3 above). Suggested fix as stated above.

#### Minor / nits

- `bin/check-durability.sh:111` — the "never committed / edited since" split does not cleanly describe a `missing-working-file` case where the record was previously committed and later deleted from the working tree uncommitted (own observation, not independently corroborated by Codex; see above).

#### Adversarial findings

Ran a targeted devil's-advocate pass on all three findings above, each with an explicit instruction to construct and actually run a live fixture rather than argue in the abstract. Result: all three defenses failed once tested. Finding 1: Codex read T-1048's DP6 and AC3 directly against the live emitted line and confirmed new text follows the frozen terminal phrase — "the defense fails." Finding 2: Codex built a live checkout with real prior commit history and all four T-1137 records committed, queried it against a nonexistent `--ref`, and got `no-recorded-commit` with none of the three disjuncts evidenced — "the defense fails on the reachable path." Finding 3: Codex traced `check-durability.sh`'s blob-presence logic and confirmed the repo-level route "avoids only cross-repo machine-wide spillover," not the durability consequence — "that defense does not survive." No new, previously-unseen findings were surfaced beyond corroborating and holding the severity of these three.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes — all three Codex-sourced findings held their normalized severity (finding 1: primary P1 / adversarial P1, both blocker-mapped; findings 2 and 3: primary P2 / adversarial P2, both major-mapped) across both passes; the own-supplementary observation was entered directly at Minor with no prior pass to compare against |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG`, linked to the Blocker and both Major findings above.

#### Fast-follow disposition

- The own-supplementary Minor observation (missing-working-file after a prior, uncommitted deletion) — won't-fix for this task, reason: the split-cause rework already in flight for findings 1–3 is likely to touch the same sentence again, and re-testing this narrower sub-case against whatever wording lands there is cheaper than filing it separately now; worth raising again only if the rework's new wording still reads as a false claim for this sub-case.

- executor-invocation (primary): codex exec --sandbox read-only --cd <repo-root> review --base feature/invocation-policy --json -o .shell-team/reviews/.codex-capture.T-1137-codex-primary.out.eIKPcm > .shell-team/reviews/.codex-capture.T-1137-codex-primary.jsonl.NkywAM 2>&1
- pass-role (primary): generation
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (primary): T-1137-codex-primary
- executor-invocation (adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1137-codex-adversarial.out.OKDEV5 "Play devil's advocate on three specific findings already raised on T-1137's diff (base feature/invocation-policy, HEAD current branch). Read the real files yourself with git diff / nl -ba -- do not expect any diff pasted here. Finding A: bin/check-durability.sh's fail_not_durable() printf (around line 111) appends new explanatory text AFTER the sentence 'this hand-off is not durable.', so the emitted stderr line no longer ENDS with that sentence. Read .shell-team/specs/T-1048-handoff-durability-barrier.md's frozen intent block (between the BEGIN/END intent-block T-1048 markers) at its DP6 design point (around line 64) and its AC3 (around line 91) -- both say the failure line 'ends' / 'ends its sentence with' that exact phrase. Does the current bin/check-durability.sh output actually still end with that phrase, or does new text follow it? Is breaching a design point/AC prose inside another task's already-merged frozen intent block a real defect even though the mechanical grep check (a substring test) still passes? Finding B: the same printf's new sentence is a three-way disjunction ('never committed, edited since, or, where a hidden base dir is the cause, a misconfiguration'). Trace bin/check-durability.sh's no-recorded-commit branch (around line 211-213): it fires whenever git rev-parse --verify fails to resolve the given ref to a commit -- construct a fixture where a repository has normal prior commits on other refs, but the caller passes a --ref value that does not exist (a typo branch name, not an unborn repo) -- run it live and check whether 'never committed' is actually true in that case, given the repository has real committed history elsewhere. Finding C: read README.md's current global-excludes bullet (around line 60) and its Japanese mirror README.ja.md (around line 60). It says choosing the global route hides the base dir and makes durability gates report not-durable permanently with hand-offs staying local-only, then says prefer the repo-level .gitignore line above if you want to stay untracked without that cost. Does choosing the repo-level .gitignore route instead of global actually avoid the not-durable/local-only-hand-off consequence, or does it only avoid the cross-repo machine-wide spillover, given bin/check-durability.sh only observes whether a record is a committed blob and does not care why it is untracked? For all three findings, argue the strongest case that these are NOT real defects, then say whether that defense survives you actually reading and running the code. Return a JSON array of {severity, file, line, issue, suggestion}. < /dev/null > .shell-team/reviews/.codex-capture.T-1137-codex-adversarial.jsonl.jjOyOV 2>&1
- pass-role (adversarial): generation
- briefing-fidelity (adversarial): carried — all three findings from the primary-pass-plus-own-reading synthesis above (the DP6 terminal-position breach, the invalid-`--ref` no-recorded-commit overclaim, the README remedy-clause overclaim) were composed verbatim into the prompt argument this invocation passed to Codex, with an explicit instruction to construct and actually run a live fixture for each rather than argue in the abstract
- raw-capture (adversarial): T-1137-codex-adversarial

---

### Codex Review verdict: APPROVE (round 2)
- Task: T-1137
- Codex model: gpt-5-codex (provider-configured)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests
- Diff scope: 7 files, +559/-2 lines (`git diff --stat feature/invocation-policy...HEAD`) — records (board, spec, provenance, interventions, round-1 review) plus two README lines; `bin/check-durability.sh` does not appear in the diff at all (byte-identical revert to base, confirmed with `cmp`)

Own verification before invoking Codex (own-run): `git show $(git merge-base feature/invocation-policy HEAD):bin/check-durability.sh | cmp - bin/check-durability.sh` → identical, exit 0 (the round-1 Blocker's and Major 2's subject is mechanically gone). `grep -c 'core.excludesFile\|not-durable\|!.shell-team/'` against `README.md`/`README.ja.md` → exactly 1 each, matching AC1. Read `README.md:60`/`README.ja.md:60` clause by clause against the v2 frozen Goal and `bin/check-durability.sh:316`–`:334`'s base-blob predicate (working-file readability → `git ls-tree` blob presence → `git hash-object` content match) — every clause holds: the durability/local-only cost is stated as a property of any untracked base dir: the repo-level `.gitignore` line carries the same one; the global route's own extra cost is scoped correctly to cross-repo spillover only. Live-verified the git-precedence claim myself rather than trusting the prose (`!.shell-team/` in a repo-level `.gitignore` un-ignores a path a global `core.excludesFile` entry hides — confirmed with `git check-ignore -v` before/after and `git status --short --ignored`, matching README.md:60/README.ja.md:60's "repo-level patterns outrank the global file" claim). Read the full v2 frozen intent block (Goal, four declarations, Non-goals, Pre-commitment, AC1–AC9, Input space), the board's `- means-ends reflection`, `- intent-ratified (v1→v2)`, and `- freeze-attestation (v2)` sub-bullets, `.shell-team/interventions/T-1137.md`'s three entries, and `.shell-team/provenance/T-1137.md`'s six decision entries — no internal contradiction found between any of these records and the round-3 QA verdict's own clause-by-clause reading, which independently reaches the same conclusions this pass reaches.

**Round-1 findings, re-verified against the current tree, all resolved:**

1. **Blocker (T-1048 DP6/AC3 terminal-phrase breach) — moot.** `bin/check-durability.sh` is reverted byte-for-byte to its base blob (confirmed above), so the disputed sentence no longer exists; the line ends exactly with `— this hand-off is not durable.` as T-1048's frozen DP6/AC3 require. The mechanism that made this possible — the spec's own pre-declared Pre-commitment naming AC2 droppable, the same-class-2 trigger firing for real reasons (QA r1's docs-overclaim finding plus review r1's own Blocker and Major 2), and the disposition executed exactly as pre-declared (issue #502 opened as AC2's destination, carrying all three rounds' findings) — is a legitimate, host-ratified loop mechanism (`agents/pm-spec.md`'s corpus-sourced "droppable"/"never-dropped" convention, `.shell-team/lessons.md` 2026-08-08; the operator-ratified ruling of 2026-09-06, T-1121, cited directly in this spec's own Pre-commitment section), not an ad hoc workaround.
2. **Major 2 (unresolvable-`--ref` overclaim) — moot,** for the same reason: the disjunctive sentence that overclaimed no longer exists in the shipped file.
3. **Major 3 (README remedy-clause overclaim) — resolved.** The clause-by-clause read above confirms the corrected sentence in both languages states the durability/local-only cost as a property of any untracked base dir and scopes the repo-level route's advantage to cross-repo spillover only, matching the v2 Goal (D6) and the checker's actual blob-presence predicate. No overclaim survives in either language; both carry matching semantic force.
4. **Minor (missing-working-file after a prior uncommitted deletion) — not independently re-tested this round** (its own round-1 disposition was won't-fix pending the in-flight rework, and the rework that landed was a full revert rather than a further wording change, so the sentence this Minor concerned no longer exists either — moot by the same mechanism as findings 1–2, not by a fresh reproduction).

**New finding this round (primary pass, corroborated by a targeted adversarial pass):**

1. **`.shell-team/specs/T-1137-excludes-detection.md:87`–`:88` (AC2) is now a permanently-failing frozen acceptance criterion, with no machine-readable "dropped" state distinguishing it from an ordinary regression.** AC2 requires the fixed strings `misconfiguration` and `docs/adopting.md` inside `bin/check-durability.sh`'s shared `printf`; the revert removes both, so `check-acs.sh` reports AC2 FAILED and will continue to every time this frozen spec is re-run, forever. The board discloses this fully and correctly (the engineer hand-off, the QA round-3 verdict, `- means-ends reflection`, and both interventions entries all name it as an intentional, pre-committed drop, not a regression) — but `agents/qa-verifier.md`'s own written PASS/FAIL rule ("Anything failed → set status back to READY_FOR_ENG", `agents/qa-verifier.md:25`–`:27`) carries no explicit carve-out for this case, and `bin/check-acs.sh` has only `pass`/`fail`/`skip`/`unrecognized` statuses — no "dropped-by-precommitment" status distinct from an ordinary FAIL. Primary pass read this as a live Blocker-level QA-gate violation. The adversarial pass, instructed to argue the strongest case for that reading and then test it against the actual files (`agents/qa-verifier.md`, `bin/check-acs.sh`, the spec's frozen Pre-commitment section, the board's disposition records, and `.shell-team/lessons.md`'s 2026-08-08 corpus entry), concluded the argument does not survive: "it does not survive the file review as a T-1137 defect. The frozen pre-commitment explicitly makes AC2 droppable, records the same-class-2 trigger and issue destination, and the board/intervention records show that disposition was executed. Lessons and the means-ends rules establish this as the intended loop mechanism. The real gap is that QA and check-acs have no machine-readable dropped state, forcing a disclosed intentional drop to appear as FAIL." My own reading agrees: the disposition was legitimate, pre-declared before the first review round (not invented after the fact to excuse a shortfall), mechanically traced (issue #502, the revert's byte-identity, AC3/AC9 staying green), and disclosed everywhere a reader would look. The gap this surfaces is real but sits one level up, in the loop's own machinery (`agents/qa-verifier.md` / `bin/check-acs.sh`) rather than in this task's delivered change — and per this round's own calibration instruction, whether dropping AC2 was the right call is the orchestrator's recorded judgment, not a review finding.

#### Blockers

- None.

#### Major

- None.

#### Minor / nits

- None carried over as gating findings this round; see the mechanism-gap finding above, held at Minor/fast-follow per the calibration below.

#### Adversarial findings

Ran a targeted devil's-advocate pass on the one new finding above, instructed to argue the strongest Blocker-level case first and then test it against the actual files rather than argue in the abstract. Result: the strongest case for treating this as a T-1137 defect does not survive the file read — see the finding's own text above for the adversarial pass's verbatim conclusion. No new, previously-unseen findings were surfaced.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 (r2) | AC2 permanently FAIL with no dropped-annotation status in `check-acs.sh`/`agents/qa-verifier.md` | primary: P1 (blocker-mapped) / adversarial: "Fast-follow" (Codex's own downgrade after file-reading) | Minor / fast-follow, non-gating | this round's task brief fixes the verdict-gating list explicitly (false README claim, EN/JA divergence, a `bin/` edit beyond the revert, an AC5/AC6/AC7 breach) and states "whether the executed disposition was the right call is the orchestrator's recorded judgment, not a review finding — you may note disagreement as a note"; the disposition is additionally host-ratified (T-1121, 2026-09-06) and pre-declared in the frozen spec before any review round ran, so this is process/mechanism bookkeeping rather than a deliverable defect |
| — (r1, unchanged) | round-1 Blocker + 2 Major + 1 Minor | see round-1 ledger above | all four moot/resolved this round | carried forward for continuity; not re-litigated |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`.

#### Fast-follow disposition

- Re-declaring round 1's Minor (missing-working-file after a prior uncommitted deletion): **moot**, not a fast-follow — the sentence it concerned no longer exists after the full revert; no further action.
- **New this round**: `agents/qa-verifier.md` and `bin/check-acs.sh` have no machine-readable way to represent a pre-commitment-driven "dropped" acceptance criterion distinct from an ordinary regression FAIL, which forces every future re-run of `check-acs.sh` against `.shell-team/specs/T-1137-excludes-detection.md` (and any future spec that exercises the same droppable/never-dropped mechanism) to show a disclosed, intentional drop as an undifferentiated failure — file-an-issue, rationale: this is a recurring mechanism gap (the same droppable/never-dropped convention is already established via `.shell-team/lessons.md`'s 2026-08-08 entry and the T-1121 ruling and will fire again on future tasks), not specific to this task's deliverable, and the fix (a narrow annotation convention plus a QA carve-out gated on verifying the frozen pre-commitment, the trigger, and the destination issue) is a design decision for `pm-spec`/`qa-verifier` maintainers rather than something this review round should adjudicate or implement.

- executor-invocation (primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base feature/invocation-policy --json -o .shell-team/reviews/.codex-capture.T-1137-codex-primary-r2.out.OJ47Hz > .shell-team/reviews/.codex-capture.T-1137-codex-primary-r2.jsonl.mB2d82 2>&1
- pass-role (primary-r2): generation
- briefing-fidelity (primary-r2): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (primary-r2): T-1137-codex-primary-r2
- executor-invocation (adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o .shell-team/reviews/.codex-capture.T-1137-codex-adversarial-r2.out.Zxr39A "Play devil's advocate on one specific finding already raised on T-1137's diff (base feature/invocation-policy, HEAD current branch), round 2 of this task's review. Read the real files yourself with git show / cat -- do not expect any diff pasted here. The finding: T-1137's spec .shell-team/specs/T-1137-excludes-detection.md AC2 (around lines 87-88, inside the frozen intent block between the BEGIN/END intent-block: T-1137 markers) is a live, mechanically-checked acceptance criterion requiring the strings 'misconfiguration' and 'docs/adopting.md' to appear in bin/check-durability.sh's fail_not_durable() printf. The engineer reverted bin/check-durability.sh byte-for-byte to its base blob this round (confirm with: git show $(git merge-base feature/invocation-policy HEAD):bin/check-durability.sh | cmp - bin/check-durability.sh), so AC2 now fails when run via bin/check-acs.sh .shell-team/specs/T-1137-excludes-detection.md (confirm this yourself by running it). The board .shell-team/todo.md (grep for 'T-1137' and read the Pre-commitment section of the spec, and the board's own 'means-ends reflection' and 'intent-ratified' sub-bullets for T-1137) records this as an intentional, pre-committed drop: the spec's own frozen Pre-commitment section (before any implementation existed) pre-declared AC2 as droppable, named the trigger (two consecutive rounds of independent new defects on the same component) and the destination (a new GitHub issue, #502, carrying AC2's text and findings) BEFORE the first review round ran. The trigger fired for real reasons (QA round 1 found a docs-overclaim defect in the message text; cross-provider review round 1 found a T-1048 frozen-contract breach and an unresolvable-ref overclaim in the same message text), and the disposition was executed exactly as pre-declared: bin/check-durability.sh reverted to base, AC2 now reads FAIL by design, issue #502 opened as AC2's destination. The QA verifier (agents/qa-verifier.md) marked the task PASS -> READY_FOR_REVIEW at round 3 despite AC2 failing 8 passed 1 failed. agents/qa-verifier.md's own written PASS/FAIL rule (around lines 25-27) says 'Anything failed -> set status back to READY_FOR_ENG' with no explicit carved-out exception for a pre-commitment-driven droppable-criterion drop -- read that file yourself to confirm whether such an exception is written anywhere in it. Also read bin/check-acs.sh yourself to confirm whether it has any 'dropped' or annotated-skip status distinct from FAIL for exactly this situation, or only pass/fail/skip/unrecognized. Argue the strongest case that this is a real Blocker-level QA-gate violation that should force REQUEST_CHANGES on this review round, notwithstanding the pre-commitment mechanism. Then say whether that case survives you actually reading the files: is leaving a permanently-failing, frozen acceptance criterion in a spec (disclosed on the board, but with no machine-readable 'dropped' annotation in the spec or check-acs.sh itself) a defect in THIS delivered change (README.md/README.ja.md two-line fix, bin/check-durability.sh revert), or is it a disclosed, correctly-executed instance of an established, host-ratified loop mechanism (search .shell-team/lessons.md and CLAUDE.local.md-style docs for 'droppable' and 'same-class-2' precedent) that exposes a genuine but separate gap in agents/qa-verifier.md / bin/check-acs.sh's own written conventions (i.e., a fast-follow on the mechanism, not a defect in this task's deliverable)? Return a JSON array of {severity, file, line, issue, suggestion}." > .shell-team/reviews/.codex-capture.T-1137-codex-adversarial-r2.jsonl.62Jpvg 2>&1
- pass-role (adversarial-r2): generation
- briefing-fidelity (adversarial-r2): carried — the primary pass's finding (AC2 permanently failing, no dropped-annotation status, QA's PASS verdict potentially conflicting with `agents/qa-verifier.md`'s literal written rule) was composed verbatim into the prompt argument this invocation passed to Codex, with an explicit instruction to argue the strongest Blocker-level case first and then test it against the actual files
- raw-capture (adversarial-r2): T-1137-codex-adversarial-r2
