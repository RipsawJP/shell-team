### Codex Review verdict: REQUEST_CHANGES
- Task: T-1061
- Codex model: not printed by this CLI's `codex exec --json` event stream (no `model` field observed in either capture) — recorded as unavailable rather than guessed
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 14 files, +1157/-0 lines (`7764f96...HEAD`)

このレビューは Codex CLI (codex-cli 0.145.0) に実ファイルを直接読ませた2パス構成
（primary: `codex exec review --base 7764f96 --json`、adversarial: 3件の一次候補を
「壊せるか確認せよ」というフレームで再読）で行った。生キャプチャ:
`.shell-team/reviews/T-1061-codex-primary.{txt,jsonl}` /
`T-1061-codex-adversarial.{txt,jsonl}`。

#### Blockers
(none)

#### Major

- `bin/check-adopter-docs.sh:331-358`(discharge-marker scan) — `- adopter-surface:`
  と `- adopter-docs-waiver:` の検索がフェンス外という条件だけで**ファイル全体**を
  走査しており、その行が「intent block 内」「Acceptance criteria 節」「実在する AC
  の直下」のいずれであるかを一切確認していない。Goal 本文は discharge の条件を
  「an acceptance criterion carries an indented `- adopter-surface:` line」と明記
  しているが、実装はそれを検証しない。Codex adversarial pass が実際に最小 fixture
  で確認済み: `yes` 宣言 + 実在の AC1（`- adopter-surface:` を持たない）+
  「## Notes for engineer」内の未フェンスの例示行 `  - adopter-surface: docs/adopting.md`
  だけで exit `0` になる（本来は `obligation-undischarged` で refuse すべき形）。
  この spec 自身のコーパス実測（64/66 spec がインデント付き `- key:` 形の
  sub-bullet を計 1122 行持つ）から、未フェンスの例示や Notes/Assumptions への
  貼り忘れは「敵対的入力」ではなく通常の accident の射程内 — Input space の
  「A hostile spec author」除外には当たらない（脅威モデルは明示的に accidents）。
  waiver 側（`- adopter-docs-waiver:` の同種スキャン）にも同じ穴がある。
  一次ソース: Goal 本文の discharge 定義、Non-goals「A hostile spec author」段落。

- `bin/check-adopter-docs.sh:349-355`(surface 値の集約) — `surface_value` は
  **最初に見つかった occurrence の値だけ**を保持し、2 個目以降は `surface_count`
  を増やすだけで値を破棄する。この spec の設計は複数 `- adopter-surface:` 行を
  明示的に合法としている（重複用の refusal token が10トークンのクローズドセット
  に存在しない ＝ 複数 AC がそれぞれ独自の surface を持つことを許容する設計)。
  結果として、先頭の surface 行が空白のみで2番目が正当な値を持つ場合は
  `obligation-undischarged` で誤って refuse され、同じ2行を逆順にすると exit `0`
  で pass する — 判定が行順に依存する。Codex adversarial pass が両順序を実際に
  構築して確認済み（順序A: exit 1 / 順序B: exit 0、`surface_count` はどちらも
  `2`）。これは「先頭 AC の surface を書きかけのまま放置し、後続 AC に正しい
  surface を書く」という現実的な authoring error で到達する。fail-closed を
  謳うゲートが、正しく discharge された spec を行順という無関係な要因で
  非決定的に refuse するのは設計目標(Goal「discharged by an indented
  `- adopter-surface:` line under an acceptance criterion」)と矛盾する。

#### Minor / nits

- `agents/pm-spec.md:89` の新規 self-check bullet の書き出し「when the
  freeze-time blocker above fires, the spec's own frozen intent block must
  also carry the machine-readable declaration …」は、単独で読むと
  「user-visible な spec だけが宣言を要求される」ように誤読できる（実際に
  Codex primary pass はこの誤読で defect と報告した）。しかし同じ bullet 後半の
  無条件の一文「Run `check-adopter-docs.sh <spec.md>` yourself and require exit
  `0` before setting `READY_FOR_ARCH`」を含めて全体を読めば、internal spec
  （`no` 宣言）にも declaration が要求されることは変わらない — チェッカー自体
  `declaration-missing` で無条件に refuse するため、機構的な fail-closed 性は
  保たれている。Codex adversarial pass の再検討で NOT-A-DEFECT と判定し、
  私もこれに同意する。文言の冒頭を「every first-freeze spec declares …」のような
  無条件表現に変えれば誤読の余地が消える、という程度の wording nit。

- `agents/pm-spec.md`「## Frozen intent freeze (v0.3.0 Phase A)」節（このタスクの
  変更対象外、行 91-95 付近）は、`- intent-hash (v1):` を記録するための**別の**
  手順を独立に記述しており、`check-adopter-docs.sh` には一切言及していない。
  T-1061 自身の freeze はこの節ではなく `skills/run/SKILL.md` step 2 の
  attestation→bootstrap 手順（今回ゲートが正しく組み込まれている側）に沿って
  実施されたことがボードの freeze 記録（一致する文言）から確認でき、この
  repository の実運用は後者に従っているとみられる。したがって実際に迂回された
  形跡はないが、同一の機械的操作（hash 記録）を説明する文章が2箇所に存在し、
  一方にだけゲートが刺さっている、という corpus 上の冗長性は残る。Codex
  adversarial pass はこれを「保証全体への迂回経路」として `CONFIRMED` 寄りに
  報告したが、実運用の証拠（T-1061 自身の freeze 記録の文言が run-skill 側の
  手順と一致している点）と、pm-spec.md 側の自己申告的な self-check 節が
  同一ファイル内でこの Frozen-intent-freeze 節より先に置かれている点を踏まえ、
  私の判断としては「確認された実害」ではなく「将来もし pm-spec.md の当該節だけを
  単独で参照する運用に切り替わった場合に露出しうる、未然に閉じておくべき corpus
  の冗長性」に格下げして Minor とする。一文の相互参照（例:
  「declaration and gate: see ## Spec completion self-check above」）で無償に
  近いコストで閉じられる。

#### Adversarial findings

Codex に「この3件の一次候補を壊せるか確認せよ、加えて保証(『これ以降に凍結する
すべての spec は adopter-facing capability の有無を宣言し、`yes` なら実際の
ドキュメントか正直な waiver を伴う』)を破る別の欠陥がないか探せ」という
devil's-advocate framing で再読させた（`.shell-team/reviews/T-1061-codex-adversarial.txt`）。
結果:
(1) discharge-marker のファイル全体スキャン — **CONFIRMED**（具体的な fixture 付き、上記 Major に採用）。
(2) pm-spec.md の条件文誤読疑い — **NOT-A-DEFECT**（bullet 全体を読めば無条件、上記 Minor に格下げして採用）。
(3) surface 値の先頭だけ保持 — **CONFIRMED**（具体的な二行 fixture 付き、上記 Major に採用）。
(4) 追加で提起された「`check-intent.sh` が `aligned` を返す経路が T-1061 ゲートを
迂回しうる」という指摘は、pm-spec.md の Frozen-intent-freeze 節の冗長性という
実在する corpus 上の弱点までは確認したが、Blocker と評価するにはこの repo の
実際の freeze 運用（run-skill 側の手順に従っている形跡）との整合性が取れず、
私の判断で Minor に格下げした（上記 Minor 参照）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | discharge-marker whole-file scan (not scoped to a real AC/intent block) | primary: P1 / adversarial: CONFIRMED | major | 両パス一致、具体的 fixture で再現確認済み、Goal 本文の discharge 定義と矛盾する実装ギャップ |
| 2 | pm-spec.md self-check bullet allegedly gates declaration on user-visible only | primary: P2 / adversarial: NOT-A-DEFECT | minor | adversarial pass が bullet 全文の無条件の後半文を根拠に defect ではないと再判定し、私もコード側の fail-closed 挙動と突き合わせて同意 — wording nit に格下げ |
| 3 | first-only surface-value aggregation, order-dependent verdict | primary: P2 / adversarial: CONFIRMED | major | 両パス一致、二行の順序入れ替え fixture で非決定的な結果を再現確認済み |
| 4 | `agents/pm-spec.md`「## Frozen intent freeze」節の冗長な未ゲート手順（`aligned` 経路の迂回可能性、adversarial pass 独自提起） | adversarial: CONFIRMED（本人評価は blocker） | minor | 実運用の証拠（T-1061 自身の freeze 記録が run-skill 側手順と一致）と対比し、確認された実害ではなく未然の corpus 冗長性と判断して降格 |

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings (Major #1, #2 above; the two Minor items are recommended but not release-blocking on their own)

#### Class accounting (round 1 — pre-commitment)

これは T-1061 に対する最初の独立レビューラウンドである。累積 class はまだ無い。
本タスクの `## Pre-commitment` は「同一コンポーネントが2ラウンド連続で新規の
独立した欠陥を出したら」を発火条件としており、**droppable** はスタンドアロン
チェッカー本体＋suite＋CI配線（`bin/check-adopter-docs.sh`,
`tests/check-adopter-docs/run.sh`, 3つの workflow 編集）、**never-dropped** は
declaration-line grammar と freeze-sweep item である。今回の2件の Major
（discharge-marker のスコープ漏れ、surface 値集約バグ）はどちらも droppable
コンポーネント（`bin/check-adopter-docs.sh` の discharge-marker 判定ロジック）
内の欠陥であり、never-dropped の対象（declaration line 自体の grammar ——
exactly-one・placement window・yes/no・rationale の必須化——は今回の指摘で
一切破られていない）には触れていない。ラウンド数はまだ1であり、
pre-commitment の発火条件（2ラウンド連続）には遠く及ばない。次ラウンドで
再度この droppable コンポーネントに新規の独立した欠陥が見つかった場合のみ、
再読の上でこの pre-commitment セクションの disposition（fast-follow issue へ
切り出し）を適用すべきである。

#### Fast-follow disposition
- `agents/pm-spec.md`'s self-check bullet wording (line 89, the "when the freeze-time blocker above fires" opener) — file-an-issue: 機能的な defect ではない（fail-closed gate は無条件に効いている）が、Codex primary pass 自身が誤読した実例がある以上、次に spec を書く human/agent も同じ誤読をしうる。冒頭を無条件表現に直す1行修正で閉じられるため、今ラウンドで一緒に直されなければ小さな fast-follow issue として残す。
- `agents/pm-spec.md`'s独立した「## Frozen intent freeze」節に `check-adopter-docs.sh` への相互参照が無い件 — file-an-issue: 実害は確認していないが（T-1061自身の freeze はゲート付きの run-skill 手順に従った形跡がある）、同一操作を説明する未ゲートの重複手順が corpus に残ること自体がこのタスクの Problem 文（「author memory の穴」）と同じ形をしているため、1行の相互参照で先回りして閉じる価値があると判断する。

---

## Round 2 (rework verification)

### Codex Review verdict (round 2): REQUEST_CHANGES
- Task: T-1061
- Codex model: not printed by this CLI's `codex exec --json` event stream (no `model` field observed in either capture) — recorded as unavailable rather than guessed
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope (cumulative, `7764f96...HEAD`): 15 files, +1466/-0 lines. Round-2-only delta: `bin/check-adopter-docs.sh` (+58 lines, discharge-marker rescoping + `pwd -P`), `tests/check-adopter-docs/run.sh` (+78 lines, 35→42 cases), `agents/pm-spec.md` (+3/-1, both Minors), plus this review record and board hand-off.

生キャプチャ: `.shell-team/reviews/T-1061-r2-codex-primary.{txt,jsonl}`（`codex exec review --base 7764f96 --json`、フルファイル読み直し）/
`T-1061-r2-codex-adversarial.{txt,jsonl}`（round-1の2 Major修正の class-closure 監査＋新境界の敵対的プローブを指示）。
adversarial パスの sandbox は完全 read-only で `mktemp` が使えなかったため、Codex 自身は入力検証部を一時的にストリーム対応へ差し替えて
解析本体（207-444行目、未変更）だけを走らせる代替手段で観測した、と自己申告している。そのため **(d) の一件は私自身が
実際の・無改変の `bin/check-adopter-docs.sh` を使い、実ファイルで独立再現した**（下記）。

#### 1. Major #1（discharge-marker のスコープ）class-closure 監査 — 要件網羅は確認、しかし新しい穴を発見

エンジニアの7要件棚卸し（declaration×2・waiver×2・surface×3）を凍結済み Goal/Non-goals/Input-space 本文と実装の両方に照らして
自分で数え直した。フリーズされた本文が明示的に要求する位置的制約は7件で過不足なく、8件目の見落としは無い（waiver に AC-nesting
要件が無いのも本文の非対称な言い回し通りで正しい）。ここまでは round-1 の修正と QA round-2 の監査に同意する。

しかし、この7要件を実装する**スコープ追跡ステートマシン自体**（round-1で新規導入されたコード）に、Codex adversarial pass が
指摘し、私が実際の未改変スクリプトで独立再現した欠陥がある:

**新規欠陥（Major）: フェンスがスコープの close イベントを隠蔽する** — `bin/check-adopter-docs.sh:387-391`。フェンス内の行は
無条件に `continue` されるため、状態遷移が一切走らない。もし「フェンスで囲まれた prose」が**フェンスを外したら**通常の
非インデント・非 AC-bullet 行として `in_ac` を閉じるはずの内容だった場合、そのフェンスを skip する `continue` は `in_ac` の
値を素通りさせる。結果、フェンスの直後にある、本来は別の（無関係な）criterion に属するはずの unindented/unfenced な
`- adopter-surface:` 行が、フェンスより前の AC の scope に誤って帰属してしまう。

再現（実ファイル・無改変の `bin/check-adopter-docs.sh` に対して自分で構築): `yes` 宣言 + 自分の surface を持たない
`- [ ] **AC1**` + フェンスで囲った「本来ならスコープを閉じるはずの地の文」+ フェンス直後の unindented/unfenced な
`  - adopter-surface: docs/adopting.md` → **exit 0（誤って pass）**。同一内容からフェンスだけを取り除いた対照実験
（同じ地の文を fence なしでそのまま置く）→ 正しく **exit 1 `obligation-undischarged`**。フェンスの有無だけが結果を反転させており、
実装コメント自身が謳う「any other non-indented, non-blank line closes scope」という不変条件に、フェンスでスキップされた行は
決して参加できないため、フェンスが closing イベントを隠す限りこの不変条件は成立しない。

既存の `cad-fence-*` / `cad-scope-*` テストはこの方向（フェンスに包まれた close 候補が scope を素通りさせる方向）を
一切カバーしていない — QA round 2 自身の追加プローブ（「AC-bullet 自体がフェンス内」）とは異なる形で、より広く踏み込んで
いない。

#### 2. Major #2（value retention）class-closure 監査 — 再確認して CLOSED

Codex に discharge-marker ループ全体を再読させ、surface / waiver のどちらにも "first match wins" のパターンが残っていないか
監査させた。両方とも各 occurrence でその場で trim・非空なら `*_has_value=1` に立てるのみで、ループ後に「最初の値」を
読み返すコードは存在しない。QA round 2 の4方向実測（surface blank→valid/valid→blank、waiver 同様）を私も机上で再確認し、
一致した。**Major #2 はクラスとして閉じている。**

#### 3. 新境界の敵対的プローブ（コーディネータ指定の5項目）

| 項目 | 観測 | 判定 |
|---|---|---|
| (a) BEGIN直後/END直前のマーカー | 両方とも意図通り検出 | 正しい |
| (b) AC bullet 配下、3段階ネストした surface | 検出される（フリーズ文言はネスト深さを制約していない） | 正しい |
| (c) フェンス内の AC bullet + フェンス外の surface（QA が既にプローブ済みの形） | `obligation-undischarged`（scope は開かない） | 正しい（独立に再確認、私自身の round-1 レビュー時の手計算とも一致） |
| (d) 実在の AC bullet + （外したら close 相当の地の文を含む）フェンス + フェンス外の surface | **exit 0（誤 pass）** | **新規欠陥（Major）。上記1で詳述、実ファイルで独立再現済み** |
| (e) declaration の重複/misplaced ロジックへの回帰 | 該当コード（248-360行目）は round-1 の diff に一切現れず、diff上でも不変を確認 | 回帰なし |

#### 4. Minors の確認

- `agents/pm-spec.md:89` — 実際の diff hunk (`65b2961`) を読んだ。冒頭が「every first-freeze spec's own frozen intent block
  carries ... — unconditionally, not only when the freeze-time blocker above fires ...」に書き換わっており、round-1で
  指摘した誤読の余地は解消されている。**確認・完了**。
- `## Frozen intent freeze` への相互参照 — 同じ diff hunk で、生成領域 `<!-- BEGIN prompt-block: playbook-pm-spec -->` の
  **手前**に1行追加されているだけで、既存行の削除・書き換えは無い（純追加）。AC2 の byte-identity ロックはこの領域を
  対象にしており、この追加はその外側にあるため抵触しない — 自分で diff hunk を読んで確認した（AC2 のグリーンな結果を
  鵜呑みにしていない）。**確認・完了**。

#### 5. Pre-commitment に対する class accounting — トリガー発火

`.shell-team/specs/T-1061-adopter-docs-gate.md` の `## Pre-commitment` を再読した上で、明示的に会計する。

- **droppable component**: スタンドアロンチェッカー本体＋suite＋CI配線（`bin/check-adopter-docs.sh`,
  `tests/check-adopter-docs/run.sh`, 3つの workflow 編集）。
- **never-dropped component**: declaration-line grammar（`- user-visible:` 行そのものの exactly-one・配置・yes/no・
  rationale 必須）と freeze-sweep item。**これらはラウンド1・2を通じて一度も破られていない** — 上記(e)で確認した通り、
  declaration 側のコード自体は round-1 の diff に一切触れられておらず、両ラウンドの Major はすべて discharge-marker
  （waiver/surface）側、つまり droppable component の内部に限定される。
- **ラウンド1**: droppable component（discharge-marker のスコープ検証・値保持ロジック）に独立した新規欠陥2件
  （whole-file scan／first-occurrence-only）を発見。
- **ラウンド2（本ラウンド）**: 同じ droppable component（discharge-marker のスコープ検証を実装する、round-1で新規導入
  されたステートマシンそのもの）に、round-1の2件とは異なる根本原因を持つ独立した新規欠陥1件（フェンスによる
  close-event の隠蔽）を発見・実ファイルで再現確認した。

**したがって、Pre-commitment の factual trigger「同一コンポーネントが2ラウンド連続で新規の独立した欠陥を出す」が
成立する。** これは default threshold であり、個々の欠陥の深刻度で緩めない、と本文自身が明記している
（"the repository's default threshold, not loosened"）。Re-read step の指示通りこの節を読み直した上で明記する:
**この状態でエンジニアに3回目のパッチを依頼することは、Pre-commitment が明示的に避けようとしている「一回だけ追加パッチ」
の罠そのものである。**

一方、**never-dropped component（declaration-line grammar・freeze-sweep item）はどちらのラウンドでも一度も defeat
されていない** ため、「両ラウンド連続で never-dropped が破られたら planning に戻す」という、より重い方の escalation は
**発火していない**。発火しているのは droppable component 側の、より軽い disposition（fast-follow issue へ切り出し）
のほうである。

**推奨（実行は私の権限外 — spec/board は編集しない）**: Pre-commitment が事前決定した disposition —
「スタンドアロンチェッカー＋suite＋CI配線を fast-follow issue に切り出し、両ラウンドの findings（round-1の2 Major +
round-2の1 Major、計3件）をその issue の requirement list として持ち越す。declaration grammar・pm-spec self-check・
run-skill sweep item・adopter documentation はこのタスクでそのまま出荷する」を適用すべきだと判断する。ただし
`skills/run/SKILL.md` の新規 sweep item は現状 `check-adopter-docs.sh` の実行を前提にした文言であり、チェッカー自体を
落とす場合はこの一文もあわせて書き直す必要がある — この矛盾は Pre-commitment 自身の disposition 文には明記されて
いないため、実行時に pm-spec が解決すべき差分として名指ししておく。AC1/AC4-AC10/AC13/AC15 等、チェッカーの存在を
直接検証する複数の Acceptance Criteria の削除・書き換えを伴うため、これは通常のエンジニアリング rework では完結せず、
class-B 相当の再凍結（human-ratified）が必要になると考えられる — この判断・実行は pm-spec / coordinator の権限であり、
私は board も spec も編集しない。

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG **かつ** pm-spec への再ルーティングが必要（Pre-commitment のトリガーが
  発火したため、次の一手は「discharge-marker ロジックの3回目のパッチ」ではなく、droppable component を fast-follow へ
  切り出す spec レベルの意思決定であるべきだと考える）。

#### Fast-follow disposition
- droppable component 全体（`bin/check-adopter-docs.sh`, `tests/check-adopter-docs/run.sh`, 3つの CI 編集）を
  fast-follow issue へ切り出す場合の requirement list — file-an-issue: (1) round-1 Major #1（discharge-marker の
  whole-file scan、修正済みだが参考として記録）, (2) round-1 Major #2（first-occurrence-only value retention、修正済み
  だが参考として記録）, (3) round-2 で新規発見・未修正の Major（フェンスに包まれた close-event の隠蔽によるスコープ
  漏れ — 上記1参照、最小 fixture は本記録に記載）。これは Pre-commitment 自身が指示する「ラウンドの findings をその
  まま持ち越す」の実行であり、私からの新規提案ではない。
- Codex round-2 primary pass が追加で報告した3件（tilde フェンス非対応・`## Non-goals` 見出し欠落時のフォールバックが
  window を無制限に広げる・区切り文字の空白を要求しない寛容な separator 判定）は、いずれも **round-1 以前から不変の
  コード**であり、今回のリワークが持ち込んだものではない。tilde フェンスは Input space の "no adversarial markdown"
  除外に該当し、この corpus の実際の慣行（例示は常にバッククォートフェンス）を踏まえて minor に留める。`## Non-goals`
  欠落フォールバックは「well-formed でない spec」でしか発火せず、`check-acs.sh`/`check-intent.sh`/pm-spec の
  Frozen-intent-freeze 手続きが Non-goals 見出しの存在を別途要求する多層防御があるため実害は低いと判断し minor。
  separator の空白寛容性はむしろ意図的な設計判断（rationale が em dash を再度引用してもロバストであるための設計）
  であり、defect ではないと判断した。この3件は file-an-issue: 上記 droppable-component の fast-follow issue に
  「ついでに」まとめて記載してよい程度の軽微な項目として、同じ issue の requirement list に加える。
