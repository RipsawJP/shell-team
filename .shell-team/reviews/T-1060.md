### Codex Review verdict: REQUEST_CHANGES
- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 9 files, +558/-0 lines (`d01ab30...HEAD`)

#### Blockers

- `.shell-team/todo.md:57-58,60` — このブランチの T-1060 凍結コミット（`c623a15`）が、すでに `READY_FOR_MERGE` で完了済みの **T-1059** のボード entry に T-1060 自身の `- freeze-attestation (v1, 2026-08-11): lines=11/11 ... verdict=3P/8F` と `- intent-hash (v1): 7fac5b1138219e2182038f52d379afabbc47322b` を誤挿入している。実測: base ref `d01ab30` では `bash bin/check-intent.sh .shell-team/specs/T-1059-docs-release-notes.md .shell-team/todo.md` は `aligned: T-1059 v1 (6df163c62176b355b16b5a9d9b8165aa77e10de9)` で exit 0。HEAD では同コマンドが `structural: expected exactly one well-formed intent-hash record for T-1059 ... (found valid=3 malformed=0)` で **exit 2**（`bin/check-intent.sh` 自身が定める「1 タスクにつき正当な intent-hash record は正確に 1 本」という不変条件への違反）。矛盾する正典: `bin/check-intent.sh` 自身の構造的契約（1 タスク=1 hash）と、T-1059 自身の実際の frozen intent-hash（`6df163c6...`）。
- `.shell-team/todo.md:98-99,101`（同一の誤挿入パターン）— 同じ 7fac5b... hash と重複した freeze-attestation 行が、すでに完了済みの **T-1058** の entry にも入っている。実測: base では `bash bin/check-intent.sh .shell-team/specs/T-1058-telemetry-binding.md .shell-team/todo.md` が `aligned: T-1058 v2 (e82f98d5...)` で exit 0、HEAD では `structural: ... found valid=3` で **exit 2**。
- `.shell-team/todo.md`（T-1057 entry, 該当箇所 ~line 142）— 同型の誤挿入が **T-1057** にも及ぶ。実測: base `aligned: T-1057 v2 (cd984875...)` exit 0 → HEAD `structural: ... found valid=2` **exit 2**。

  3 タスクとも本来は無傷であるべき完了レコードであり、この破損は T-1060 自身の diff（`.shell-team/todo.md` への編集）だけが原因であることを base/HEAD 両方の実測で確認済み。spec のどこにもこの副作用は開示されておらず（`## Blast radius` は `bin/check-acs.sh` の spec 基準の verdict のみを扱い、`bin/check-intent.sh` のボード側整合性は対象外）、QA の `bash bin/check-handoff.sh` / `bash bin/check-board-headings.sh` も対象外（両者ともボードの構造とトップレベル `- [ ]` 行の生存のみを見る）。将来の再凍結・監査でこの 3 タスクが `check-intent.sh` に引っかかるため、マージ前の必須修正。

#### Major

- `docs/adopting.md:135-141`（および `docs/adopting.ja.md:137-143` の対応箇所）— 新セクションは「An adopter edit can reach four fail-closed refusals from `resolve-executor.sh`」として `contract-violation` を含む 4 つを並列に挙げているが、出荷済みの `templates/adapters/claude-cli.txt` と `templates/adapters/codex-cli.txt` はいずれも `carries board-transition` を carried（`stdout`/`file`）として宣言しており、`templates/binding-adapters.txt` の allowlist もこの 2 adapter に閉じている。したがって現行の出荷物では、adopter がどの role をどちらの adapter に bind し直しても `contract-violation` には到達できない — Codex の敵対的パスでも同じ結論（他の adapter/provider 組み合わせを探しても到達経路は見つからない）。矛盾する正典: `templates/adapters/claude-cli.txt` / `templates/adapters/codex-cli.txt` の `carries board-transition` 宣言と `templates/binding-adapters.txt` の 2 行の allowlist。AC7 はトークンと exit code の存在のみを検査し、この到達可能性の主張までは検査していないため、この過大主張は現行のどの `- check:` にも捕捉されない。

#### Minor / nits

- `README.md:22-23`, `docs/adopting.md:84-86`（および両 `.ja.md` 対応箇所）— 「`team-init` がまだ走っていない場合は `templates/binding-template.conf` を手動でコピーする」というフォールバック手順が、そのパスがプラグイン自身のインストールディレクトリ（`bin/team-init.sh` の `REPO_ROOT`/`TEMPLATES_DIR` 解決）を指すことを明示していない。adopter 自身のリポジトリ直下には `templates/` が存在しないため、素朴に `cp templates/binding-template.conf ...` を試みると失敗する — AC12(g) がすでに捕捉・修正した `bin/`-prefix 欠陥と同じ形の execution-context gap だが、この行はコードフェンス内の実行可能コマンドではなくプロセ内の相対パス表記のため、AC12(g) の「every command the four new sections print」の対象からは外れて見過ごされた。
- `docs/adopting.md:143-146`（および `.ja.md` 対応箇所）— 4 つの fail-closed refusal の段落は、直前で案内している検査コマンド `resolve-executor.sh --print-resolved`（手順 4）が「no availability probe at all」であること（`bin/resolve-executor.sh` の `## Usage` ブロック自身の記述、かつ spec の `## Summarized sources` が明示的に carry over を指示した distinction）に触れていない。`executor-unavailable` は `--role <role>` モードでのみ検出され、手順 4 の `--print-resolved` では検出されない。現状の並びだと、読者が「手順 4 を実行すれば executor の到達可能性まで確認できる」と誤解しうる。

#### Adversarial findings

- Codex の devil's-advocate パスは Blocker の対象を T-1059/T-1057 の 2 件から **T-1058 を含む 3 件**へ拡張して独立に確認した（私自身も `bash bin/check-intent.sh` で base/HEAD 双方を再実測し、この拡張を確認済み）。
- 同パスは Major の contract-violation 過大主張についても、私と同じ結論（現行の 2-adapter allowlist では到達不能）に独立に到達し、加えて「`--print-resolved` は probe を行わない」という同じ nuance を独立に指摘した（このレビューの Minor 2 件目と一致）。
- DP4 の confidence-boundary 軸（over-claim「rebind が invocation を配線する」/ under-claim「same-family review は不可能」）については、敵対的パスも私自身の英日両言語の直接読みも、四面すべてで違反を発見しなかった。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `.shell-team/todo.md` の T-1059/T-1057 への誤挿入 | primary: blocker（T-1059, T-1057 の 2 件） | blocker（T-1059, T-1058, T-1057 の 3 件に拡張） | adversarial pass が T-1058 も同一パターンで破損していることを独立に発見し、自分の再実測（base exit 0 → HEAD exit 2）で確認できたため対象を追加した。severity 自体は変更なし。 |
| 2 | contract-violation の到達可能性の過大主張 | primary: major | major | adversarial pass が同じ結論（到達不能）に到達し、severity の変更は無し。追加された `--print-resolved` no-probe の nuance は既存の Minor 2 件目として別立てで保持。 |

上記 2 行以外に severity を変更・却下した finding はない。

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings（`.shell-team/todo.md` の破損 3 件の復元と、docs/adopting 両言語の contract-violation 過大主張・fallback パス・`--print-resolved` no-probe nuance の是正）

#### Fast-follow disposition
- no fast-follow deferrals（Blocker/Major と同一セクションを触る Minor 2 件も、同じ rework ラウンドで一括修正することを推奨するため、merge 後への繰り延べはしない）

#### Confidence-boundary trigger accounting
- このラウンドの finding は **すべて confidence-boundary-class ではない**（DP4 の over-claim/under-claim 軸には違反なし）。したがって pre-commitment の「同一 subsystem に対する 2 ラウンド連続の confidence-boundary-class defect」トリガーは発火していない — 今回の Blocker（ボード整合性）と Major（refusal 到達可能性）はいずれも別カテゴリの defect であり、drop order の対象にもならない。

#### Notes
- Primary judgement（DP4 confidence-boundary、英日両言語独立読み）: **クリーン**。`README.md`/`README.ja.md`/`docs/adopting.md`/`docs/adopting.ja.md` の "honest boundary" 段落はいずれも `templates/prompt-blocks/executor-resolution.md`（"a role's actual invocation still routes through that role's own pinned model value, not through this resolution"; "records the bound model as telemetry, not an independently verified executed one"）と `docs/loop-engineering/task-envelope.md`（"never a silent substitution of a same-family reviewer" — invocation 時点の silent substitution のみを fail-close する）に忠実。over-claim（"rebinding makes the role run on the other executor"）も under-claim（"same-family review is impossible"）もいずれの言語・いずれの面にも見つからなかった。
- Secondary judgement (a) DP3: `templates/binding-default.conf`（shipped default）と `<base>/binding.conf`（host override）は 4 面すべてで明確に区別されており、T-1059 round-1 の conflation は再発していない。
- Secondary judgement (b) 事実整合性: `templates/binding-default.conf` の 6 role、`claude-cli`（5 値）/`codex-cli`（7 値）の effort vocabulary、`bin/resolve-executor.sh` ヘッダの refusal token + exit code（`binding-unresolved (2)` / `capability-unsupported (1)` / `contract-violation (1)` / `executor-unavailable (1)`）、`bin/team-init.sh` の rename-not-copy scaffold — いずれも実ファイルと突き合わせて一致を確認済み（contract-violation の**到達可能性**主張のみが Major で指摘した過大主張）。
- Secondary judgement (c) 実行コンテキスト: README.md の `## Replaying a run` 節（bare-name primary + `bin/`-prefixed fallback）と同型のパターンが 4 面の `check-binding.sh`/`resolve-executor.sh` 呼び出しに正しく適用されている（AC12(g) の修正を確認）。ただし `templates/binding-template.conf` 手動コピーの fallback 行は Minor で指摘の通り同種の gap を残す。
- Secondary judgement (d) 英日等価性: 逐語訳ではなく同じ事実の日本語表現として機械トークン・honest boundary anchor とも一致（DP8 のコードブロック byte-identity も確認済み）。
- Secondary judgement (e) Non-goals: diff は `bin/`・`tests/`・`templates/`・`agents/`・`skills/`・`.github/` のいずれにも触れていない（`git diff --stat d01ab30...HEAD` で確認）。`README.md` の reviewer design-choices bullet（現在は line 255）は base ref と byte-identical（`cmp` で確認）。
- Secondary judgement (f) blast-radius 開示の完全性: `T-1059-docs-release-notes.md` **AC10**（predicted, confirmed: 実測でも base PASS→head FAIL）と `T-1020-lessons-supersede-sweep.md` **AC14**（unpredicted, confirmed: `git diff --name-only develop -- bin .github tests skills docs CONTRIBUTING.md README.md templates/todo-template.md templates/prompt-blocks/registry.txt` が実際に `README.md docs/adopting.ja.md docs/adopting.md` を返すことを自分で再現）— いずれも spec の記載通り、disclosed のみで repair されていないことを確認。
- 既知の context（再掲不要、再フラグしない）: `README.md`/`README.ja.md` の Non-goals bullet の "line 208" 引用の staleness（現在line 255、QA既disclosure）、QA 自身の `git checkout --` procedural misuse（作業ロス無し）は、prompt の指示どおり再指摘していない。
- issue-canon 照合について: このサンドボックスから GitHub API へは到達していないため、issue #244・#236 の本文そのものは私自身では再確認できていない — internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation)。ただしボードの engineer/QA hand-off に記録された `api.github.com` 実測（state=open、タイトル文言）とは整合している。
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（`codex exec review --base d01ab30 --json`）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（Blocker/Major 2 件への devil's-advocate プロンプト）。

---

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +683/-0 lines (`d01ab30...HEAD`, commits `9158891`/`673e2df`/`225a134`/`daa551b` on top of round 1's tree)

#### Round-1 findings — closure status（すべて独立再実測、hand-off の記述への信頼のみでは判定していない）

- **Blocker（board 破損, T-1057/T-1058/T-1059）— CLOSED**。`225a134`（coordinating session による純粋 7 行削除）を独立に再検証: 3 タスクの Done entry block を `git show d01ab30:.shell-team/todo.md` と working tree からそれぞれ抽出して `cmp` — **3 件すべて byte-identical**。`bash bin/check-intent.sh` を 4 spec（T-1057/T-1058/T-1059/T-1060）すべてに対して実行し、**全件 `aligned`, exit 0**（T-1057 v2 `cd9848757c...`／T-1058 v2 `e82f98d5...`／T-1059 v1 `6df163c6...`／T-1060 v1 `7fac5b11...`）。この Blocker はエンジニアの修正対象ではなく（board 破損は coordinating session 自身の凍結コミットの defect であり、engineer/QA いずれの責でもない）、`daa551b` の `assumption-contradicted` intervention 記録もその帰属と整合する。
- **Major（contract-violation の到達可能性過大主張）— CLOSED**。`templates/adapters/claude-cli.txt`（`carries board-transition stdout`）・`templates/adapters/codex-cli.txt`（`carries board-transition file`）・`templates/binding-adapters.txt`（2 行の closed allowlist）を再実測し、現行の 2-adapter 体制ではどの role→adapter 組み替えでも `contract-violation` に到達できないことを再確認。英日 4 面を読み直し、`contract-violation` の記述は「enforced for any role ... bound to an adapter that does not carry a board-transition channel. Both shipped adapters ... declare carries board-transition, so binding a role to either one cannot reach this refusal today」に書き換わっており、reachable な 3 refusal（`binding-unresolved`／`capability-unsupported`／`executor-unavailable`）と明確に分離・最後尾に配置されている。トークン・exit code は不変（AC7 green を維持）。
- **Minor 1（`templates/binding-template.conf` フォールバックパスの解決基準地）— CLOSED**。4 面すべてで「the plugin's own `templates/binding-template.conf` (resolved from the plugin's installed directory, not a path under your own repository)」（日本語面も対応する言い回し）に修正されていることを `grep` で確認。
- **Minor 2（`--print-resolved` の no-probe 明示）— CLOSED**。4 面すべてで `--print-resolved` が「runs no availability probe at all」であり、`executor-unavailable` の検出には `resolve-executor.sh --role <role>` が必要である旨が明記されていることを確認。
- **Same-class sweep（6 サイト, engineer 自己申告）— 独立に 2 サイトを spot-check、いずれも申告どおり**: site 3（`agents/<role>.md` pin の "from the plugin's own agent definitions" 修飾, apply）は `docs/adopting.md:138` / `docs/adopting.ja.md:141` に確認済み。site 4（`bin/team-paths.sh --get base` の descriptive parenthetical, not-apply）は 4 面すべてで裸のまま（`README.md:184` ほか）であることを確認 — この not-apply の根拠（`## Replaying a run`/`## Design choices` が同じ descriptive/non-runnable 文脈で同一パターンを既に使っている）は妥当と判断。site 5/6（`templates/binding-default.conf`/`binding-template.conf` の初出はすでに修飾済み・実行者アクション不要, いずれも not-apply）はテキストの読み直しで矛盾なし。
- **item 4（DP4 confidence-boundary・DP3 conflation の再確認）— CLEAN**。honest boundary の文言は round 1 から不変（"resolves"/"telemetry" は変わる、"invocation path" は変わらない、#236 が該当する退役）で、4 面・英日いずれにも over-claim/under-claim を検出せず。DP3（shipped default vs host override）も conflation なし。
- **item 5（Non-goals: `bin/`/`tests/`/`templates/`/`agents/`/`skills/`/`.github/` 不変）— 確認済み**。`git diff --name-only d01ab30...HEAD` は `.shell-team/{interventions,provenance,reviews,specs,test-recipe.md,todo.md}` と `README{,.ja}.md`／`docs/adopting{,.ja}.md` の 10 ファイルのみ。

#### Blockers

（なし）

#### Major

- `docs/adopting.md:146-149`（および `docs/adopting.ja.md` 対応箇所）— **新規発見**。`binding-unresolved` の記述が「an occupant at `<base>/binding.conf` that is not a regular file」のみに限定されているが、`bin/resolve-executor.sh` 自身のヘッダは `binding-unresolved` が「a malformed delegated canonical form, a row whose field count is wrong, a value outside its governing closed set (a channel token not declared by the contract, an authority or capability value outside its own closed set)」も含む、すなわち **bind 行の通常のタイプミス（フィールド数の誤り・未知の adapter トークンなど）も同じ refusal を引く**ことを明示している。spec の `## Input space` の Out-of-scope 該当行は「a binding config that is malformed in a way **no adopter edit produces**（CRLF・非対応 schema バージョン・埋め込み改行・非 UTF-8 バイト）」であり、"通常の bind 行タイプミス" はこの除外対象に含まれない（adopter edit が普通に produce しうる誤りのため）— よって out-of-input-space の降格規則は適用されない。Codex の敵対的パスも独立に同結論（Major, 同一箇所）に到達。矛盾する正典: `bin/resolve-executor.sh` の `binding-unresolved` 定義（"malformed... a row whose field count is wrong... AND a non-regular-file occupant"）。

#### Minor / nits

- `docs/adopting.md:161-162`（および `docs/adopting.ja.md` 対応箇所）— `contract-violation` の説明末尾「it stays part of the closed set because a future **or custom** adapter could declare otherwise」の "custom adapter" という言い回しが、実在しない adopter 向け拡張経路を示唆しうる。`templates/binding-adapters.txt` 自身が「custom adapters are explicitly out of scope」「the only override is `bin/check-binding.sh`'s own `--adapters PATH` flag, documented there as **a testing affordance, not an adopter-facing knob**」と明記しており、かつ `bin/resolve-executor.sh` はその `--adapters` を一度も転送しない（`grep` で確認済み・0 件）。Codex の敵対的パスも独立に同結論（Minor, 同一箇所）に到達。"a future or custom adapter" → "a future shipped adapter" 等への訂正を推奨。

#### Adversarial findings

- Codex の devil's-advocate パス（`bin/resolve-executor.sh`・`bin/check-binding.sh`・`templates/binding-adapters.txt`・spec の `## Input space` を実読）は、上記 Major・Minor いずれについても独立に同じ severity・同じ箇所へ到達した。加えて DP4 confidence-boundary 軸の再チェックでも新規の over-claim/under-claim は発見されなかった。
- 運用上の note: この round の敵対的パス初回試行は `codex exec` が stdin 待ちでハング（`"Reading additional input from stdin..."`）し 30 分超停止した。原因は前段の呼び出しで stdin を明示的にクローズしていなかったこと。`< /dev/null` を付けて再実行し正常終了。孤立した raw capture（`.codex-capture.T-1060-codex-adversarial.{out,jsonl}.Wwxr2J`/`.LlWf6v`）は削除済み。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-1 Blocker（board 破損 T-1057/58/59） | primary: closed（byte-identity + check-intent aligned 実測） | closed | 敵対的パス不要（純粋な再実測での確認、判断の余地なし）。severity 変更・却下なし。 |
| 2 | round-1 Major（contract-violation 到達可能性） | primary: closed（reword を実ファイルと突き合わせ確認） | closed | 同上。severity 変更・却下なし。 |
| 3 | round-1 Minor ×2（fallback path／`--print-resolved` no-probe） | primary: closed | closed | 同上。 |
| 4 | binding-unresolved の記述範囲不足（新規） | primary: major, adversarial: major | major | 一致、変更なし。 |
| 5 | "custom adapter" の言い回し（新規） | primary: minor, adversarial: minor | minor | 一致、変更なし。 |

上記以外に severity を変更・却下した finding はない。

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings（`docs/adopting.md`/`docs/adopting.ja.md` の `binding-unresolved` 説明範囲の拡充と、`contract-violation` 説明末尾の "custom adapter" 表現の訂正のみ。README 側は refusal 一覧を持たないため対象外。round 1 の Blocker/Major/Minor は全て CLOSED で再指摘不要）

#### Fast-follow disposition
- no fast-follow deferrals（Major 1 件・Minor 1 件とも同一セクション内の小さな訂正であり、次の rework ラウンドで一括修正することを推奨。merge 後への繰り延べはしない）

#### Confidence-boundary trigger accounting
- この round の finding も **すべて confidence-boundary-class ではない**（DP4 の over-claim/under-claim 軸には違反なし — round 2 の新規 Major/Minor はいずれも refusal-reachability/refusal-description の正確性の軸であり、DP4 の invocation-path 軸とは別カテゴリ）。したがって pre-commitment の「同一 subsystem（confidence-boundary-class）に対する 2 ラウンド連続の新規 defect」トリガーは、round 1・round 2 を通じて **一度も発火要件を満たしていない**（2 ラウンドとも 0 件）。この round の貢献は「confidence-boundary-class 0 件」であり、trigger の累積カウントに寄与しない。

#### Notes
- item 1〜5（coordinator 指定の検証項目）はいずれも本文中で個別に記録済み（上記「Round-1 findings — closure status」節）。
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（round 2 の `codex exec review --base d01ab30 --json`、round 1 の内容を上書き — 正典ファイル名は round ごとに変えない規約のため）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（round 2 の devil's-advocate プロンプト、同様に round 1 の内容を上書き）。

---

## Round 3

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +791/-0 lines (`d01ab30...HEAD`, commits `f539cbe`/`60e606f`/`36d86cf` on top of round 2's tree)

（本ラウンドの Codex 呼び出しは 2 回ともフォアグラウンドの単一 `codex exec` 呼び出しとして同期実行し、`< /dev/null` で stdin を明示的にクローズした — round 2 でのハングの再発なし。）

#### Round-2 findings — closure status（すべて独立再実測）

- **Major（`binding-unresolved` の記述範囲不足）— CLOSED**。`bin/resolve-executor.sh` のヘッダを再読し、実際のコード（329-330行目: `check-binding.sh --print-binding` の delegation 失敗を捕捉して `binding-unresolved` にフォールドする分岐）まで直接確認。round-3 の書き換えは「an occupant ... that is not a regular file」と「the config itself is malformed in a way check-binding.sh's own grammar refuses, such as a bind row with the wrong field count or an unrecognized provider/adapter/role token」の **2 つの adopter-reachable 原因のみ**を列挙しており、ヘッダが挙げる **plugin-internal な第三の原因**（"the delegated contract" 側・"a bound adapter's definition" 側の internal-consistency — resolve-executor.sh 476-508 行目、adapter 定義ファイル自身の internal-consistency チェックで、通常の `<base>/binding.conf` 編集では到達不能）は正しく記載から除外されている。coordinator の要求どおり「no more, no less」を満たしていることを確認。加えて `check-binding.sh --config ... (step 3) reports the more specific underlying reason` という追記も、`bin/check-binding.sh` の実際の refusal トークン（`unparseable-line "a bind row must have exactly six fields..."`／`bad-token "malformed role/provider/model/effort token"` 等）と整合することを直接確認。
- **Minor（"custom adapter" 表現）— CLOSED**。`grep -in "custom adapter|カスタム" README.md README.ja.md docs/adopting.md docs/adopting.ja.md` — **0 件**。"future shipped adapter" へ訂正済み、`templates/binding-adapters.txt` の out-of-scope 宣言と整合。
- **Inventory coverage（5-token grep）— 独立再実測、QA の申告と完全一致**: `usage`／`binding-unresolved`／`capability-unsupported`／`contract-violation`／`executor-unavailable` を4面全てに対して `grep -n` した結果、QA の報告した site（`usage` は false-positive のみ／`binding-unresolved` 2箇所／`capability-unsupported` 2箇所／`contract-violation` 2箇所／`executor-unavailable` 4箇所）と一致。escaped した refusal-semantics claim はなし。
- **item 4（DP4/DP3）— CLEAN**、**item 5（Non-goals）— 確認済み**（`bin/`/`tests/`/`templates/`/`agents/`/`skills/`/`.github/` に触れず、round 2 と同じ 10 ファイルのみ）。

#### Blockers

（なし）

#### Major

- **[新規, 新クラス]** `docs/adopting.md:97-101`・`README.md`（同ステップ 1）・両 `.ja.md` — `templates/binding-template.conf`（`binding.conf.example` として scaffold される）の 6 行すべてが実在しないプレースホルダーのモデルトークン（`claude` 系ロール5つに `model-1`、`codex-reviewer` に `model-2`）で埋まっていることを確認（`cat templates/binding-template.conf` で実測）。`bin/resolve-executor.sh` は host `<base>/binding.conf` が regular file として存在する場合、その**ファイル全体**を effective binding として採用する（ロール単位で `templates/binding-default.conf` と layering/merge する仕組みは存在しない — ヘッダ・コードのいずれにも見当たらず、`check-binding.sh` 自身も「6 role 全て埋まっていること」を要求するため部分ファイルはただ `binding-unresolved` で refuse されるだけ）。新セクションの導入文「Author a `<base>/binding.conf` when you want to assign a specific executor to **one or more** of the six inner-loop roles」および三段階の how-to は、読者に「気にする 1 role の行だけ編集すればよい」と読ませる書き方だが、実際にそれを行うと **残り 5 role が `model-1`/`model-2` という無意味なプレースホルダーのまま resolve/telemetry の値になる**——このリスクについて 4 面のどこにも警告がない。Codex の敵対的パスも独立に同結論（Major, `same_class_as_round2: false` — round 2 の "refusal-semantics-vs-resolver" とは異なる新規クラス）に到達。矛盾する正典: `templates/binding-template.conf` の実際の内容（`model-1`/`model-2`）と `bin/resolve-executor.sh` の「host file が存在する場合はそれを effective binding として採用、per-role merge なし」という実際の解決ロジック。
- **[新規, "refusal-semantics-vs-resolver" クラスの 3 件目]** `docs/adopting.md:98-101,116-118,159-162`・`README.md`（該当ステップ 3）・両 `.ja.md` — `--print-resolved` は probe を行わないため `executor-unavailable` を検出できず、それには `resolve-executor.sh --role <role>` が必要、という記述があるが、`bin/resolve-executor.sh` の実コード（`role` モードの `case "$PROBE_KIND" in in-process) : ;;`——実際に読んで確認）は **in-process provider（`claude`）に対しては一切の availability 判定を行わない**（コメント自身: "This script makes no availability claim it cannot ground"）。既定の 6 role のうち 5 role（`tech-lead`/`pm-spec`/`engineer`/`qa-verifier`/`ui-designer`）は `claude`（in-process）であり、`--role` はこれら 5 role については実質的に何のチェックにもならない（out-of-process の `codex` のみ `command -v` + `codex --version` という浅い probe が走る）。したがって「`--role` が reachability を確認する」という対比的な記述は、6 role のうち 5 role について実態と食い違う。Codex の敵対的パスも独立に同結論（Major, `same_class_as_round2: true`）に到達。矛盾する正典: `bin/resolve-executor.sh` の `role` モード実装（in-process 分岐が no-op である事実そのもの）。

#### Minor / nits

（なし — 今回発見の 2 件はいずれも Major。round 2 由来の Minor は closed。）

#### Adversarial findings

- Codex の devil's-advocate パスは、上記 2 件の Major についていずれも独立に同一severity・同一箇所へ到達し、"refusal-semantics-vs-resolver" クラスの判定（Finding A=`false`／Finding B=`true`）も私自身の判断と完全に一致した。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-2 Major（binding-unresolved 記述範囲） | primary: closed | closed | `bin/resolve-executor.sh` の実コードまで直接確認、round-1 の reachability discipline と整合。severity 変更・却下なし。 |
| 2 | round-2 Minor（custom adapter 表現） | primary: closed | closed | grep 0 件で確認。severity 変更・却下なし。 |
| 3 | binding.conf.example のプレースホルダー全ロール上書き（新規） | primary: major, adversarial: major | major | 一致、変更なし。新クラス（"refusal-semantics-vs-resolver" ではない）。 |
| 4 | `--role` reachability 過大主張（新規） | primary: major, adversarial: major | major | 一致、変更なし。**"refusal-semantics-vs-resolver" クラスの 3 件目**（round 2 の Major＋Minor に続く）。 |

上記以外に severity を変更・却下した finding はない。

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings、ただし **finding 4 は通常の 4 回目パッチとして処理しない**（下記トリガー参照）。finding 3（binding.conf.example のプレースホルダー問題）は新規クラスの初回発見であり、通常のプローズ修正（例: `templates/binding-template.conf` を実際の shipped default 値で埋める、または「6 行すべてを置き換える必要があり per-role fallback は無い」旨の明示的な警告を追加する、あるいは両方）で対処可能。

#### Fast-follow disposition
- no fast-follow deferrals（finding 3 は merge 前に修正必須。finding 4 は下記トリガーにより design 側の対応が必要で、merge 後への単純な繰り延べという形にはしない）

#### Confidence-boundary trigger accounting
- この round の finding（finding 3・finding 4）はいずれも **confidence-boundary-class ではない**（DP4 の over-claim/under-claim 軸には違反なし — round 1〜3 を通じて DP4 軸の defect は累積 0 件のまま）。

#### 同クラス（"refusal-semantics-vs-resolver"）trigger accounting — 明示的な発火宣言
- round 2 で **2 件**（Major: binding-unresolved 記述範囲不足／Minor: custom adapter 表現）が同クラスとして発火済み。
- この round の **finding 4（`--role` reachability 過大主張）はこのクラスの 3 件目**であることを、Codex 本体パス・敵対的パスの双方で確認し、ここに明示します。
- spec 本文の frozen `## Pre-commitment` 節は「confidence-boundary-class findings against the four new sections」という**別の**、名指しされた subsystem を対象にしており、"refusal-semantics-vs-resolver" 用の pre-decided droppable はどこにも定義されていません。しかし `## Pre-commitment` の冒頭の **repository-default トリガー文**（"the same subsystem draws two consecutive rounds of independent new defects, evaluated before a third round of rework starts"）はこの task 固有の named subsystem に限定されない一般規則であり、coordinator が round 2〜3 を通じて "refusal-semantics-vs-resolver" という別の subsystem に対して同じ一般規則を適用していることは妥当な運用と判断します。
- 結論: **finding 4 は 2 ラウンド連続の同クラス defect に続く 3 件目であり、repository-default トリガーの発火条件を満たします**。このクラス専用の pre-decided droppable が本 spec に存在しない以上、正しい対応は「4 回目のプローズパッチを試みる」ことではなく、**設計判断（tech-lead/pm-spec への差し戻し）へ回付する**ことだと判断します — 具体的には、この doc セクションが `resolve-executor.sh` の probe 機構の粒度（in-process/out-of-process の非対称性）をどこまで prose で説明しようとするか自体を再検討する必要があり、これは codex-reviewer の権限外の設計判断です。この判断はここで宣言するのみで、私自身は再設計を行いません。

#### Notes
- item 1〜5（coordinator 指定の検証項目）は上記「Round-2 findings — closure status」節および本文中で個別に記録済み。
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（round 3 の内容で上書き）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（同様に round 3 の内容で上書き）。

---

## Round 4

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +1090/-0 lines (`d01ab30...HEAD`, commits `6e84c4d`/`ce55c71`/`ab89080`/`df206e2` on top of round 3's tree)

（本ラウンドの Codex 呼び出しは 2 回ともフォアグラウンドの単一 `codex exec` 呼び出しとして同期実行し、`< /dev/null` で stdin を明示的にクローズした。）

#### Round-4 の背景 — 設計ラウンドの成果物を検証

round 3 で "refusal-semantics-vs-resolver" クラスが 3 件連続でトリガー発火した後、round 4 は **設計ノート**（`.shell-team/specs/T-1060-adopter-binding-docs.md` `## Round-4 design note`, frozen intent block の外側）による構造的対応: 36 行の claim→source table を作成し、エンジニアはそこから**転記のみ**を行う（"authoring" から "transcribing" へ職務変更）。以下、独立再実測で検証。

#### Round-3 findings — closure status（すべて独立再実測）

- **Major 1（binding.conf.example のプレースホルダー全ロール上書き）— CLOSED**。4面すべてで、rename ステップの直後に「Its six rows carry placeholder model tokens — model-1 on the five claude rows, model-2 on codex-reviewer — that name no real model: replace every row before relying on it, or transcribe the shipped default's rows below for any role you are not changing. Editing one row and stopping there ships five placeholder bindings into resolution and telemetry.」という警告を確認（対応する日本語も確認）。**ただしこの警告文自身に新しい defect を発見**（下記 Major 参照）。
- **Major 2（`--role` reachability 過大主張）— CLOSED**。4面すべてで `--role` の probe が provider kind で分岐すること（in-process=`claude` は no-op、out-of-process=`codex` のみ `command -v`+`codex --version` の浅い probe）が明記され、「Under the shipped default, five of the six roles bind claude, so resolve-executor.sh --role codex-reviewer is the only one of the six invocations that actually probes anything」という帰結も明記されていることを確認。`bin/resolve-executor.sh` の実コード（537-543行目の `in-process) : ;;` no-op 分岐、220-223行目の `PROBE_TABLE_TEXT`）と直接照合し、記述が正確であることを再確認。

#### item 2（class 構造的閉鎖の spot-audit）— claim→source table のロー検証

table の 36 行から **5 行**を独立にソースと照合（要求の 3 行を超過）:
- **row 2**（whole-file adoption）: `bin/resolve-executor.sh` 316-326 行目を直読 — `HOST_CONFIG` が regular file なら `CONFIG_PATH=$HOST_CONFIG`、そうでなければ `CONFIG_PATH=$TEMPLATES_ROOT/.../binding-default.conf`、単一の `CONFIG_PATH` 変数のみで merge なし。table の引用行と完全一致。
- **row 3**（exactly-six-rows）: 375-383行目 `[ "${#BOUND_ROLE[@]}" -eq 6 ] || refuse binding-unresolved 2 "...does not carry exactly six bound rows..."` と各 role 欠落チェックを直読、一致。
- **row 9/10**（probe table・in-process no-op）: 220-223行目 `PROBE_TABLE_TEXT`（`claude in-process`／`codex out-of-process codex --version`）と 537-543行目の `in-process) : ;;` 分岐を直読、一致。
- **row 27**（プレースホルダートークン）: `templates/binding-template.conf` 21-26行目を直読（`model-1`×5, `model-2`×1）、一致。

**escape チェック（behavioral sentence に row が無いものがないか）**: 4面の新セクション全文を読み直し、`bin/resolve-executor.sh`／`bin/check-binding.sh` に関する挙動記述をすべて table の row に対応付け、対応漏れは検出せず。forbid 対象（row 13: 「no compiled-in probe-table entry」／row 24: `--adapters` を adopter knob として示す表現／row 34: adopter が adapter を登録できるという表現）は `grep` で 4面 0件を確認。

#### item 3（DP4/DP3 — 新規プローズの再点検）— CLEAN

"A host config is adopted whole..." と probe-scope の新規文（in-process/out-of-process 分岐）はいずれも resolution/telemetry のメカニクスに関する記述であり、DP4 の invocation-path 軸（rebind が invocation を配線する／same-family review が不可能）にはどちらの方向にも抵触しない。DP3（shipped default の所在＝`templates/binding-default.conf`／host override＝`<base>/binding.conf`）も新規文がむしろ「host config が丸ごと採用される（default との per-role merge が無い）」ことを明示して補強しており、conflation なし。

#### item 4（Non-goals）— 確認済み

`git diff --name-only d01ab30...HEAD` / `git ls-files --others --exclude-standard` は round 1〜3 と同じ 10 ファイルのみ。`bin/`/`tests/`/`templates/`/`agents/`/`skills/`/`.github/` に触れず。`bash bin/check-acs.sh` は 11/11 PASS を維持、`bash bin/check-intent.sh` は hash 不変で `aligned`。

#### item 5（既知・裁定済み、再指摘なし）

`## Input space` の stale reachability 節（class B, human ratification 待ち、editorial batch task へ）と "line 208" の stale locator（同 batch）はいずれも spec 本文で既に disclosed・裁定済みであることを確認し、再指摘していません。

#### Blockers

（なし）

#### Major

- **[新規, 新クラス — "refusal-semantics-vs-resolver" ではない]** `docs/adopting.md:113-114`（`README.md` 同ステップ1・両 `.ja.md` 対応箇所）— round 3/4 で追加されたプレースホルダー警告文「replace every row before relying on it, or **transcribe the shipped default's rows below** for any role you are not changing」の "below" が指す先は、数行後の**grammar example（デモンストレーション用の config）**であり、これは **実際の `templates/binding-default.conf` の内容ではない**。実測で比較: example は `tech-lead`/`pm-spec` の effort を `high`（実際の shipped default は `-`）、`codex-reviewer` の model を `gpt-5`（実際は `provider-configured`）にしている。この指示を文字通り実行した読者は、変更しないつもりの role に**間違った値**を「shipped default」だと思って書き込んでしまう——round 3/4 で追加した安全策（プレースホルダー問題への対処）自体が、別の思い込みを生む新しい欠陥を内包していた。Codex の敵対的パスも独立に同結論（Major, `same_class_as_rounds1to3: false`）に到達。矛盾する正典: `templates/binding-default.conf` の実際の6行（`bind tech-lead claude opus - claude-cli` 等）と、doc が "shipped default's rows" として指す grammar example の実際の6行の不一致。

#### Minor / nits

- **[新規, 新クラス — "refusal-semantics-vs-resolver" ではない]** `docs/adopting.md:157-160`（両 `.ja.md` 対応箇所）— 「`resolve-executor.sh` enforces four fail-closed refusals from a closed set」という導入文が、closed set の総数を「4」と誤読させる可能性がある。実際の closed set は **5 トークン**（`usage` を含む）であり、設計ノートの claim→source table row 17 はこれを明示し、`usage` を意図的に adopter 向けリストから除外する理由（"a bad invocation, not a config state"）も記録済み。文としては「4 refusals **from** a closed set」であり厳密には5メンバーの集合と矛盾しないが、曖昧で誤読を招きやすい。Codex の敵対的パスも独立に同結論（Minor, `same_class_as_rounds1to3: false` — 既に row 17 で正当化された scoping であり、resolver との直接的な矛盾ではない）に到達。

#### Adversarial findings

- Codex の devil's-advocate パスは、上記 Major・Minor いずれについても独立に同一severity・同一箇所へ到達し、"same_class_as_rounds1to3" 判定（Major=`false`／Minor=`false`）も私自身の判断と完全に一致した。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-3 Major×2（binding.conf.example 全ロール上書き／`--role` reachability） | primary: closed | closed | claim→source table の該当行を実コードと直接照合し確認。severity 変更・却下なし。 |
| 2 | "shipped default's rows below" が実は grammar example を指す（新規） | primary: major, adversarial: major | major | 一致、変更なし。新クラス（"refusal-semantics-vs-resolver" ではない）。 |
| 3 | "four ... from a closed set" の曖昧さ（新規） | primary: minor, adversarial: minor | minor | 一致、変更なし。design note row 17 で既に正当化された scoping であり resolver との直接矛盾ではないため minor。 |

上記以外に severity を変更・却下した finding はない。

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings。finding 2（Major）は「the rows below」の参照先を実際の `templates/binding-default.conf` に向け直すか、その6行をこのセクション内に別途複製して明示的に "shipped default" とラベルする（grammar example を default と呼ばない）ことで解消可能。finding 3（Minor）は文言の明確化（"5-member closed set のうち4つの config-condition refusal を documents している" 等）で解消可能。両者とも通常のプローズ修正で対応可能であり、design 側への回付は不要です。

#### Fast-follow disposition
- no fast-follow deferrals（Major 1件・Minor 1件とも同一セクション内の訂正であり、次の rework ラウンドで一括修正することを推奨。merge 後への繰り延べはしない）

#### Confidence-boundary（Trigger A）trigger accounting
- この round の finding（finding 2・finding 3）はいずれも **confidence-boundary-class ではない**（DP4 の over-claim/under-claim 軸には違反なし）。round 1〜4 を通じて Trigger A の累積は **0 件のまま**。

#### "refusal-semantics-vs-resolver"（Trigger B）trigger accounting — 明示的な発火判定
- round 1〜3 で **3 件**（r1: contract-violation reachability／r2: binding-unresolved cause coverage + custom adapter wording／r3: `--role` availability scope）が発火済み・**Trigger B は round 3 で既に発火済み**（round-4 design note により対応）。
- **この round の 2 件の新規発見（finding 2・finding 3）は、いずれも "refusal-semantics-vs-resolver" クラスではないと判断します**: 両方とも「`bin/resolve-executor.sh` または `bin/check-binding.sh` の挙動についての、shipped code と矛盾する文」ではありません。finding 2 は「どのテーブルが shipped default を表しているか」という**参照先の誤り**（resolver の挙動そのものとは無関係）、finding 3 は claim→source table の row 17 で**すでに正当化されたスコープ選択**の曖昧な言い回し（resolver との直接的な矛盾ではない）です。Codex の敵対的パスも両方について独立に `same_class_as_rounds1to3: false` と判定しており、一致します。
- 結論: **round 4 は "refusal-semantics-vs-resolver" クラスの新規発火をゼロ件で終えており、droppable B1 は実行されません**。累積は round 3 の 3 件のまま（round-4 design note による対応で trigger 自体は解消済み）。

#### Notes
- claim→source table によるアプローチ自体を高く評価します: 36 行のうち 5 行を実ファイルと直接照合し、すべて正確であることを確認しました。この table 方式は round 1〜3 で 3 回連続発生した「plausible だが transcribed ではない」欠陥のクラスを構造的に閉じる設計として機能しており、今回発見した 2 件（finding 2・3）はいずれも**このテーブル自体が対象としていない領域**（テーブル外の相互参照・スコープの言い回し）で発生した、別クラスの新規欠陥です。
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（round 4 の内容で上書き）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（同様に round 4 の内容で上書き）。

---

## Round 5

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +1221/-0 lines (`d01ab30...HEAD`, commits `87e20d4`/`c667340`/`709b989` on top of round 4's tree)

（本ラウンドの Codex 呼び出しは 2 回ともフォアグラウンドの単一 `codex exec` 呼び出しとして同期実行し、`< /dev/null` で stdin を明示的にクローズした。）

#### Round-4 findings — closure status（すべて独立再実測）

- **Major（"shipped default's rows below" が grammar example を指す問題）— CLOSED**。4面すべてで「transcribe the actual rows from `templates/binding-default.conf` (**not** the grammar example below, which is a custom-binding illustration with different values)」（日本語も対応する言い回し）に修正されていることを確認。**第二の fenced block は追加されていない**ことを確認: `awk` で section 内の fence 数を数えると README.md/docs/adopting.md いずれも 2 個（＝1 block）のまま。`bash bin/check-acs.sh` を再実行し **AC1 含め 11/11 PASS**（AC1 は fenced block 内の bind 行数を `templates/binding-default.conf` の行数と一致させる検査を含むため、第二の block を追加していれば 12≠6 で red になっていたはず — 実測で確認済み）。
- **Minor（"four ... from a closed set" の曖昧さ）— CLOSED**。両adopting面で「`resolve-executor.sh`'s refusal set is closed and has **five** tokens; `usage` is a bad invocation (a CLI-argument error), not a config state, so it sits outside this adopter workflow. The other four are config-condition refusals...」に修正されていることを確認。design-note row 17（`The refusal set is closed and has five members: usage (2), binding-unresolved (2), capability-unsupported (1), contract-violation (1), executor-unavailable (1)` + Bound「usage is deliberately not in the adopter list」）を再読し、転記が忠実であることを確認。`bin/resolve-executor.sh` ヘッダの実測「a closed set of five」とも一致。

#### item 3（早期ラウンドの regression spot-check）

4面の新セクション全文を読み直し、以下がいずれも無変更・正確であることを確認:
- **r1**（contract-violation framing）: 「Both shipped adapters ... declare `carries board-transition`, so binding a role to either one cannot reach this refusal today」— 無変更、`templates/adapters/{claude-cli,codex-cli}.txt` の宣言と一致。
- **r2**（binding-unresolved causes）: 「Two ordinary-edit causes: an occupant ... that is not a regular file ... or the config itself is malformed ...」— 無変更、`bin/resolve-executor.sh` 329-330行目の delegation-failure 分岐と一致。
- **r3**（whole-file adoption + probe branching）: 「A host `<base>/binding.conf` is adopted whole ... it must carry exactly one bind row for each of the six roles」「for an in-process provider (claude) it performs no availability check at all」— 無変更、316-326行目・375-383行目・537-543行目と一致。

#### item 4（DP4/DP3 — 変更文の再点検）

今回変更された2文（shipped-default 参照の redirect、closed-set の文言）はいずれも「どのファイル/table を参照すべきか」「refusal set の総数」に関する記述であり、DP4 の invocation-path 軸には触れていない——ただし **round 5 の Codex 本体パスが、DP4 の honest boundary paragraph 自体（round 1 から無変更のテキスト）に新たな over-claim を発見**（下記 Major 参照）。DP3（shipped default vs host override）は今回の変更でむしろ補強されており（`templates/binding-default.conf` を明示的に名指し）、conflation なし。

#### item 5（Non-goals）— 確認済み

`git diff --name-only d01ab30...HEAD` は round 1〜4 と同じ 10 ファイルのみ。`bin/`/`tests/`/`templates/`/`agents/`/`skills/`/`.github/` に触れず。`bash bin/check-acs.sh` 11/11 PASS、`bash bin/check-intent.sh` hash 不変で `aligned`。

#### item 6（既知・裁定済み、再指摘なし）

`## Input space` の stale reachability 節（class B）と "line 208" の stale locator（同 batch）はいずれも spec 本文で既に disclosed・裁定済み。再指摘していません。

#### Blockers

（なし）

#### Major

- **[新規 — round 1〜4 のいずれの既知クラスとも異なる。DP4 confidence-boundary-class の初回発生]** `README.md:247`（`README.ja.md:248`／`docs/adopting.md:203`／`docs/adopting.ja.md:209`）— **honest boundary 段落自体（round 1 から無変更のテキスト）に over-claim を発見**。「It does not wire an alternate-executor invocation path ... The retirement that would change that is issue **#236**.」という一文が、issue #236 の実際のスコープを過大に主張している。`.shell-team/specs/T-1057-loop-integration.md` の DP6 を実読して確認: #236（DP6 Branch B のブロッカーとして issue 化）が対象とするのは **5つの `claude-cli` 紐付け role の frontmatter model pin の退役のみ**（それらの invocation が resolved binding の model を使うようになる）であり、**`codex-reviewer` は明示的に除外**（"its model: sonnet configures the Claude wrapper that shells out to the Codex CLI, not the model that reviews"）、かつ **provider/adapter レベルの invocation routing には一切触れていない**。したがって #236 が実現しても、codex-reviewer の invocation は無影響であり、いずれの role も「別 executor（provider を含む）への invocation 経路」が swappable になるわけではない——#236 は "the retirement that would" 一般的な「no alternate-executor invocation path is wired」という broad claim を解消するものとして提示するには**スコープが狭すぎる**。spec 自身の DP4 が「(d) the retirement that would change (c) is issue #236」として (c)=broad claim にこの issue を明示的に紐付けているため、これは DP4 のまさに核心（rebind の boundary）についての誤った将来像を adopter に与える。Codex の敵対的パスも独立に同結論（Major, `is_dp4_confidence_boundary_class: true`）に到達し、理由も同一（"DP4 expressly makes the #236 sentence anchor (d) of the honest boundary and says that retirement would change the broad claim in (c). The disputed sentence therefore inflates confidence about when alternate-executor invocation becomes real"）。矛盾する正典: `.shell-team/specs/T-1057-loop-integration.md` DP6（#236 の実際のスコープ）。

#### Minor / nits

（なし）

#### Adversarial findings

- Codex の devil's-advocate パスは、上記 Major について独立に同一severity・同一箇所へ到達し、"is_dp4_confidence_boundary_class: true" という class 判定も、私自身が独立に到達した判断と完全に一致した（両者とも「DP4 の弾劾対象の2文型は例示であり網羅的定義ではない」という同じ根拠を挙げている）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-4 Major（"shipped default's rows below" 参照先誤り） | primary: closed | closed | 4面で修正確認、AC1（fenced block 数）も再実行して確認。severity 変更・却下なし。 |
| 2 | round-4 Minor（closed-set の曖昧さ） | primary: closed | closed | design note row 17 との一致を確認。severity 変更・却下なし。 |
| 3 | issue #236 のスコープ過大主張（新規、honest boundary 段落自体） | primary: major, adversarial: major | major | 一致、変更なし。**DP4 confidence-boundary-class（Trigger A）の初回発生**と判定。 |

上記以外に severity を変更・却下した finding はない。

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings。修正は honest boundary 段落の #236 に関する一文を、DP6 の実際のスコープ（5つの claude-cli role の model pin 退役のみ、codex-reviewer 除外、provider レベルの invocation routing には触れない）に合わせて再スコープするか、より一般的な invocation-routing issue を別途引用することで解消可能です（Codex 本体パスの提案文言を参考にできます）。通常のプローズ修正で対応可能であり、design 側への回付は不要と判断します。

#### Fast-follow disposition
- no fast-follow deferrals（Major 1件のみで、honest boundary 段落内の一文の訂正であり、次の rework ラウンドで修正することを推奨。merge 後への繰り延べはしない）

#### Confidence-boundary（Trigger A）trigger accounting — 明示的な発火判定
- round 1〜4 を通じて Trigger A（confidence-boundary-class）の累積は **0 件**でした。
- **この round の finding（#236 スコープ過大主張）は、DP4 confidence-boundary-class の finding と判定します** — Codex の敵対的パスも独立に同じ結論（`is_dp4_confidence_boundary_class: true`）に到達しています。理由: DP4 自身が「(d) #236 is the retirement that would change (c)」として、(c)="no alternate-executor invocation path is wired" という broad claim にこの issue citation を明示的に結びつけており、DP4 の弾劾対象2文型（"rebinding makes the role run on the other executor" / "same-family review is impossible"）は例示であって網羅的定義ではないため、この過大主張も同じ axis 上の違反と判断します。
- **結論: Trigger A はこの round で初回発生（1件）となり、まだ発火（2ラウンド連続）していません**。次ラウンドで同クラスの新規 finding が出れば、その時点で発火します。私自身、過去4ラウンド（round 1〜4）でこの specific citation（#236 のスコープ）を DP4 の canonical source（`templates/prompt-blocks/executor-resolution.md`／`docs/loop-engineering/task-envelope.md`）とのみ照合し、`.shell-team/specs/T-1057-loop-integration.md` DP6 という**第三の必要な正典**を照合していなかったことを認めます — 今回はそれを実行し、発見に至りました。

#### "refusal-semantics-vs-resolver"（Trigger B）trigger accounting
- round 1〜3 で 3 件発火・closed（round-4 design note により対応）。round 4・round 5 とも新規発生 **0 件**。累積は 3 件のまま、変化なし。

#### round-4 の2クラスの再発チェック（coordinator 指定）
- **crossref-target-mismatch**（round 4 Major）: round 5 で fix 済み、**再発 0 件**（1回目の発生のみ、2回目には至っていない）。
- **closed-set-scope-wording**（round 4 Minor）: round 5 で fix 済み、**再発 0 件**（同上）。

#### Notes
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（round 5 の内容で上書き）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（同様に round 5 の内容で上書き）。

---

## Round 6

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +1469/-0 lines (`d01ab30...HEAD`, commits `8e6d954`/`bd2c13d`/`4268be8`/`84354d3`/`ad672ca`/`32f37f9` on top of round 5's tree)

（本ラウンドの Codex 呼び出しは2回ともフォアグラウンドの単一 `codex exec` 呼び出しとして実行した。adversarial pass の1回目の呼び出しコマンド文字列内に backtick を含めてしまい、外側 shell がそれをコマンド置換として評価してエラー（`command not found: --effort`）を出したが、`codex exec` 自体は実行され実ファイルを読んで正常に完了したことを jsonl のツールコール記録で確認した——プロンプトの構築ミスであり、Codex 自身の分析はリポジトリの直接読み取りに基づいている。以後、prompt に backtick を含めないよう注意する。）

#### Round-5 finding — closure status（4層すべてを独立再実測）

- **(a) 4面の boundary paragraph が model/executor の2つの limit を分離して記述している — CLOSED**。4面すべてで「It does not wire an alternate-executor invocation path, and that is two separate limits. The **model**: a role's invocation still takes its model from that role's own `agents/<role>.md` pin, not from the resolved row — issue **#236** tracks retiring those pins, for the five `claude-cli`-bound roles only, and deliberately excludes `codex-reviewer`, whose pin configures the Claude wrapper that shells out to the Codex CLI rather than the model that reviews. The **executor**: which provider and adapter a role is actually invoked through is not routed by resolution at all, for any role, and no issue tracks changing that...」を確認。この文言を `.shell-team/specs/T-1057-loop-integration.md` の DP6（"its `model: sonnet` configures the Claude wrapper that shells out to the Codex CLI, not the model that reviews" — ほぼ verbatim に一致）、board の T-1057 fast-follow filing 行（line 303: "filed as issue #236 (DP6 Branch B's deferred pin retirement...)"）、`templates/prompt-blocks/executor-resolution.md`（"a role's actual invocation still routes through that role's own pinned model value, not through this resolution"）と直接照合し、いずれも正確であることを確認。日本語面も同じ2限界構造を正確に反映（README.ja.md:246、docs/adopting.ja.md:207 付近）。
- **(b) frozen Goal v2 の appositive 修正 — CLOSED**。`bd2c13d` の diff を直読し、Goal 文が「two separate limits, stated separately on every surface: a role's invocation still takes its model from that role's own agents/<role>.md pin rather than from the resolved row, and issue #236 is the tracked retirement of those pins for the five claude-cli-bound roles only, codex-reviewer deliberately excluded; and which executor a role is invoked through is not routed by resolution at all, with no issue tracking that」に修正されていることを確認。
- **(c) board のratification records が conformant — CLOSED**。`bash bin/check-intent.sh` は `aligned: T-1060 v2 (857937e001daeee81d843e03b77bf24fa79ac672)` を返す（exit 0）。board entry（`.shell-team/todo.md` T-1060）を直接確認: `- intent-hash (v2):` は **1本のみ**、`- freeze-attestation` は v1・v2 各1本ずつ（計2本）、`- intent-ratified` record 1本（class=B, operator GO, 両 delta bundled）を確認 — round-1 の board 破損クラス（同一 hash/attestation の重複挿入）が再発していないことを、`check-intent.sh` が `structural` refusal ではなく `aligned` を返すこと自体で確認。
- **(d) mutable section が v2 と整合 — CLOSED**。DP4（"(d) two separate limits, per the ratified v2 Goal..."）、claim row 35（"present (amended v2)"）、新設 row 37（"present (new, v2)"）、Body-to-AC row 9（"two limits (v2)"）・9b（新設、info-only）をすべて直読し、v2 の内容と整合していることを確認。これらは frozen intent block の**外側**（`## Decision points`／`## Body-to-AC correspondence`）にあり、`32f37f9` による discharge 後も `check-intent.sh` は依然 `aligned`（hash 不変）——frozen region への影響なし。

#### item 2（Bundled Delta 2 — Input space の reachability 節）— CLOSED

`## Input space` の Out-of-scope bullet 2 が「of the four refusals named in AC3/AC4/AC7, three — `binding-unresolved`, `capability-unsupported`, `executor-unavailable` — are reachable by an ordinary adopter edit, while `contract-violation` is not...」に修正されていることを確認 — round 1〜2 で確立された「3つ reachable・contract-violation は unreachable」という事実と整合。

#### item 4（bilingual equivalence, anchors, non-goals）

日本語文（README.ja.md/docs/adopting.ja.md）を通読し、英語面と同じ2限界構造（model限界＝#236がトラック、5role限定、codex-reviewer除外／executor限界＝resolution が経路制御せず、追跡issueなし）を正確に表現していることを確認。anchor（`#236`・`invocation path`/`呼び出し経路`・`telemetry`/`テレメトリ`）は4面すべてに存在（`grep -c` で確認）。`git diff --name-only`／`git ls-files --others --exclude-standard` は round 1〜5 と同じ 10 ファイルのみ、`bin/`/`tests/`/`templates/`/`agents/`/`skills/`/`.github/` に触れず。`bash bin/check-acs.sh` 11/11 PASS。

#### item 6（既知・裁定済み、再指摘なし）

"line 208" の stale locator（editorial batch）と、operator の "provider-routing issue は filed しない" という裁定（"no issue tracks changing that" が ratified wording）はいずれも再指摘していません。

#### Blockers

（なし）

#### Major

- **[新規 — DP4 confidence-boundary-class と判定。Trigger A の 2 回目連続発生（occurrence 2）]** `README.md:246`（`README.ja.md:246`／`docs/adopting.md:202`／`docs/adopting.ja.md:207`）— round 6 で修正された boundary paragraph 自体が「It does not wire an alternate-executor invocation path, and **that is two separate limits**」と、model と executor の2つに**網羅的に**分解して見せているが、**effort もまったく同じ形（resolution+telemetry のみで、実際の invocation には一切適用されない）の第三の limit** であり、この文はそれを開示していない。実測で確認: `grep -rn` で `agents/*.md`／`bin/*.sh`／`skills/*/SKILL.md` を検索した結果、`--effort` を実際の invocation 呼び出しに使っている箇所は**皆無**（唯一の実際の consumer は `bin/log-run.sh`——telemetry writer）。`skills/run/SKILL.md`／`skills/goal/SKILL.md` 自身の「Telemetry binding (T-1058)」段落は「Every value recorded this way is the resolved binding **as declared, never an independently verified observation of what executed**」と明記しており、これは model について DP4 が既に述べているのと**全く同じ caveat** を provider/effort/adapter にも及ぼしている。かつ本 spec 自身の Goal 文は "executor" を **provider + model + effort + adapter の4要素**として定義しているため、round-6 の row 37 が "executor" を provider+adapter のみに狭めたことで、effort がどちらの limit にも属さず**宙に浮いている**。`.shell-team/specs/T-1057-loop-integration.md` の DP4/DP6/Goal を確認したが、effort の invocation-time application については一切測定・決定されておらず（"Nothing invokes an executor: no argv is composed..."）、model と同型の未解決事項として存在する。
  - **classification（class 判定）— 両論を検討した上での結論**: **Reading Y（Trigger A の対象外、新しい別クラス）の論拠**: DP4 の "line" は元来「rebind が **どの executor** に routing するか（identity）」についての境界であり、effort は identity ではなく既に確定した executor の invocation パラメータ（fidelity）の問題であり、round 6 の ratification（v2）は #236 の scope 過大主張という**一点のみ**を修正する狭い scope で、「two separate limits」は「この ratification が対処する2つ」を指すという読み方も可能。**Reading X（Trigger A の2回目発生）の論拠**: 実際の文言は「that is **two separate limits**」という、直前の一般的な claim（invocation path が配線されない）を**網羅的に分解して見せる**構文であり、"この ratification が対処するのは2つ" という限定を一切書いていない。round 5 の判定時点で、DP4 の弾劾対象2文型は例示であり網羅的定義ではないと既に確定させており、この一貫性を保つなら、"two separate limits" という**完全性の主張**自体が、真の限界の全体像を過小に見せている以上、DP4 の同じ axis 上の違反と扱うべきである。**私の結論**: Reading X を採用します。Codex の敵対的パスも、両論を明示的に重み付けした上で同じ結論（`trigger_a_occurrence_2`）に独立に到達しました。matched conclusion, not merely deferred to it.
  - 矛盾する正典: `skills/run/SKILL.md`／`skills/goal/SKILL.md` の Telemetry binding (T-1058) 段落（effort/provider/adapter は "as declared, never an independently verified observation of what executed"）、本 spec自身の Goal 文の executor 定義（provider+model+effort+adapter）、および `.shell-team/specs/T-1057-loop-integration.md`（effort の invocation-time application が一切測定・決定されていないこと）。

#### Minor / nits

- `.shell-team/todo.md:19`（T-1060 board entry 冒頭の task 概要行）— round 6 で shipped surface 側は全て修正されたにもかかわらず、board 自身の最初期の概要文が**修正前の過大主張**（"and does **not** wire an alternate-executor invocation path (#236 is the retirement that would)"）をそのまま残している。この行は4つの adopter-facing surface（README/adopting）のいずれでもなく、`- check:` にも紐付かないため DP4/Trigger A の対象外と判断しますが、将来のセッションがこの board 行を信頼して同じ過大主張を再導入するリスクがあるため、早めの訂正を推奨します。
- `.shell-team/specs/T-1060-adopter-binding-docs.md:176`（claim→source table の summary 行）— row 37 の追加後も「**36 rows.** 19 `present`, 8 `add`, 1 `correct`, 3 `forbid`, 5 `available`」のままで、実際には37行（19+1=20 `present` 相当のはず — row 35 は "present (amended v2)"、row 37 は "present (new, v2)"）に更新されていません。design note 自身のブックキーピングの誤りであり、どの surface にも `- check:` にも影響しません。

#### Adversarial findings

- Codex の敵対的パスは、effort-limit の finding について、Reading X／Reading Y 双方の最強の論拠を明示的に書き出した上で、私自身と同じ結論（`trigger_a_occurrence_2`）に到達しました。加えて、修正済みの boundary paragraph に新たな over-claim/under-claim（DP4 の別方向）がないことも確認済みです。
- **運用上の note**: 1回目の敵対的パス呼び出しは、プロンプト文字列内の backtick を outer shell がコマンド置換として評価してしまい shell エラー（`command not found: --effort`）を出しましたが、`codex exec` 自体は jsonl のツールコール記録上、実際に `git status`/`git diff` を実行し実ファイルを読んで正常に完了していることを確認しました——プロンプト構築のミスであり、分析結果自体はリポジトリの直接読み取りに基づいています。今後は prompt 文字列に backtick を含めないよう注意します。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-5 Major（#236 スコープ過大主張） | primary: closed | closed | 4層（surfaces/Goal/board records/mutable sections）すべてを独立実測し確認。severity 変更・却下なし。 |
| 2 | effort が第三の未開示 limit（新規） | primary: major, adversarial: major | major, **Trigger A occurrence 2** | 一致。class 判定は両論検討の末 `trigger_a_occurrence_2` を採用（Codex 独立判定と一致）。 |
| 3 | board の stale #236 概要行（新規） | primary: major（Codexラベル）, 自己判断: minor | minor | Codex は severity を major としているが、4つの shipped surface にも `- check:` にも触れない board 内部記述であるため、私は minor に downgrade しました（Trigger A の対象範囲＝4 surfaces 外）。 |
| 4 | claim table の summary 行数の陳腐化（新規） | primary: minor（P3）, adversarial: 言及なし | minor | 一致（severity 表記のみ調整、P3→minor）。 |

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG **ではなく、pre-commitment の pre-decided disposition を実行するための計画ラウンドへ**。**Trigger A（confidence-boundary-class）が round 5・round 6 の2ラウンド連続で新規 defect を出し、発火しました**。spec 自身の `## Pre-commitment` の "Drop order" は Trigger A 用に既に用意されています: **(1)** README の grammar example と three-step how-to を `## Design choices` 近くの一段落のポインタに縮小し、詳細は `docs/adopting.md` と `resolve-executor.sh --help` に委譲、両ラウンドの findings（effort-limit gap を含む）を requirement list として新しい fast-follow issue へ **(2)** `<base>/` layout-tree の追加行（DP6, AC5）も同じ fast-follow issue へ dropped。**Never dropped** の2項目（① DP4 の honest boundary が4面すべてに残ること ② bilingual parity）はこの drop でも保持されます。この disposition の実行判断・実施は tech-lead/pm-spec の設計権限に属し、私（codex-reviewer）が代わりに実行するものではありません——ここでは trigger の発火とその pre-decided disposition を明示するのみです。

#### Fast-follow disposition
- **board 概要行の stale #236 文言**（`.shell-team/todo.md:19`）— file-an-issue ではなく **won't-fix as a fast-follow**: 4 shipped surface のいずれにも影響しない board 内部記述であり、Drop order 実行時に board entry 自体が大きく書き換わる可能性が高いため、個別の fast-follow issue を今起票する必要はないと判断します。次の rework/re-planning ラウンドで合わせて訂正することを推奨します。
- **claim table の summary 行数**（37行/20presentへの更新）— 同様に **won't-fix as a fast-follow**: 次の re-planning ラウンドで table 自体が変更される可能性が高く、今訂正しても Drop order 実行時に再度変わる可能性があるため、その時点で合わせて更新することを推奨します。
- effort-limit の gap（Major, Trigger A occurrence 2）そのものは fast-follow ではなく、上記 Recommendation の pre-decided disposition（re-planning + fast-follow issue 起票）に従って処理されるべきものであり、単純な繰り延べの対象ではありません。

#### Confidence-boundary（Trigger A）trigger accounting — 明示的な発火宣言
- round 1〜4: 0件。round 5: 1件目（#236 スコープ過大主張、CLOSED）。**round 6: 2件目（effort limit の未開示、新規） — これで2ラウンド連続となり、Trigger A が発火しました。**
- 発火に伴い、spec 自身の pre-decided disposition（Drop order 1+2）が実行対象になります。これは codex-reviewer の実行権限外であり、設計層（tech-lead/pm-spec/coordinating session）への回付が必要です。

#### "refusal-semantics-vs-resolver"（Trigger B）trigger accounting
- round 1〜3 で3件発火・closed（round-4 design note で対応）。round 4〜6 とも新規発生 **0件**。累積は3件のまま、変化なし。

#### round-4 の2クラス／round-5クラスの再発チェック
- crossref-target-mismatch（round4）・closed-set-scope-wording（round4）: round5・round6 とも再発 **0件**。
- round-5クラス（#236 scope over-claim, Trigger A occurrence 1 自体）: round6で修正確認済み（本ラウンドの「Round-5 finding — closure status」節）、再発ではなく新規（effort）が Trigger A の 2件目としてカウントされています。

#### Notes
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（round 6 の内容で上書き）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（同様に round 6 の内容で上書き、prompt 構築ミスの経緯を含む）。

---

## Round 7

### Codex Review verdict: REQUEST_CHANGES — Never-dropped property ① defeated; task stops for planning

- Task: T-1060
- Codex model: provider-configured（`--model` フラグを渡さない測定済みの呼び出し規約 — `agents/codex-reviewer.md` および `templates/adapters/codex-cli.txt` と同じ境界）
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 10 files, +1687/-0 lines (`d01ab30...HEAD`, commits `84e1712`/`99cc2e9`/`6f39726`/`851d4cd`/`bc966a3`/`79b2da8`/`f59ebdf` on top of round 6's tree)

（本ラウンドの Codex 呼び出しは2回ともフォアグラウンドの単一 `codex exec` 呼び出しとして同期実行し、`< /dev/null` で stdin を明示的にクローズした。2回目の敵対的パスは prompt 文字列から backtick を完全に除去して構築し、round 6 で発生した shell コマンド置換事故の再発なし。）

#### Round-6 finding — closure status（item 1）

- **CLOSED**。4面すべてで effort が axis-2 の具体例として明記されていることを確認: 「a declared effort is recorded on the span but applied to no call, its only other effect being the `capability-unsupported` refusal above — an adapter definition declares an effort *mechanism*, and declaring one is not applying it」。`bin/resolve-executor.sh` 505-514行目を直読し、effort の唯一の実効果が「`-` なら resolve」「非宣言値なら `capability-unsupported`」の2択のみであり、実際の invocation には一切渡されないことを確認。`--effort`/`reasoning_effort` の実際の consumer を repo 全体で再検索し、`bin/log-run.sh`（telemetry writer）以外に存在しないことを再確認（round 6 と同じ結果）。

#### item 2（class の構造的閉鎖 — universal axis-2 claim の反証試行）

「binding が変えるのは resolve-executor.sh が報告する値と telemetry が記録する値**だけ**」という universal claim を反証しようと試みた: `resolve-executor.sh`／`skills/run/SKILL.md`／`skills/goal/SKILL.md`／`bin/log-run.sh`／`bin/check-run.sh`／`bin/rollup-runs.sh`／`agents/codex-reviewer.md` を横断的に検索し、resolved binding の値を telemetry（記録・検証・集計報告）以外の目的で消費する箇所は**皆無**であることを確認。「illustrative, not an enumeration」という言い回しにより、未列挙のインスタンス自体は non-finding——反証に至らなかったため、universal claim（axis 2）はこの意味で構造的に閉鎖していると判断します。

#### item 3（axis 1 の正確性）

「resolution runs before any invocation, and a refusal is a blocker that stops the phase rather than falling back... an ordinary edit can reach `binding-unresolved`, `capability-unsupported` and `executor-unavailable`」を `templates/prompt-blocks/executor-resolution.md`（"resolve that role's executor immediately before invoking it... treat every refusal... as a blocker that stops the phase... Never substitute another executor... never continue with any role left unresolved"）と照合し、3つの reachable token も round 1/5/6 の確立と整合。**この claim 自体は正確ですが、その適用範囲（universal であるという主張）に重大な問題があります — 下記 Blocker 参照**。

#### item 5（frozen/board hygiene）

`bash bin/check-intent.sh` → `aligned: T-1060 v3 (3ecd38de014511f125b60ec41e2d9d1cd85931e1)`。board entry を直接確認: `- intent-hash` は**v3のみ1本**（`grep -c "^\s*- intent-hash"` は全体で63件だが、T-1060 entry 内では実際のsub-bulletは1本のみ、他は別task/prose中の言及）、`- freeze-attestation` はv1/v2/v3の**3本**、`- intent-ratified` はv1→v2・v2→v3の**2本**——round-1破損クラスの再発なし（`check-intent.sh` が `aligned` を返すこと自体で確認）。`git diff --name-only`／`git ls-files --others --exclude-standard` は round 1〜6 と同じ10ファイルのみ。`bash bin/check-acs.sh` 11/11 PASS。

#### item 6（既知・裁定済み、再指摘なし）

"line 208" のstale locator（editorial batch）と、operator の settled-design 裁定（README縮小・restoration issue なし）は再指摘していません。JA の in-page anchor-slug 懸念（`[設計上の選択](#設計上の選択)`）については、この session にレンダリング手段（ブラウザ）がなく、engineer が既に開示した内容（round 1 以来不変の形式、GitHub の CJK anchor-slug 挙動は未検証）を追加で ground することはできませんでした——`environmentally-unverified` として扱い、新規 finding としては報告しません。

#### Blockers

- **[新規・最重要 — 私の判断: `defeats_never_dropped_1`]** `README.md:190`（`README.ja.md:189`）／`docs/adopting.md:202`（`docs/adopting.ja.md:207`）— round7で新設された universal axis-1 claim「the binding gates **whether** a call proceeds — resolution runs first and a refusal stops the phase rather than falling back... Resolution runs before any invocation」が、**6つの bindable role すべての invocation に対する無条件の保証として提示されている**が、実際には **`codex-reviewer` と `pm-spec` の実在する adopter-facing invocation 経路がこの gate を完全にバイパスする**ことを実測で確認しました。
  - `skills/review/SKILL.md`（全9行）: 「Invoke the `codex-reviewer` sub-agent... skips PM/Engineer/QA — just the cross-provider review... without going through the full team pipeline」と明記し、`resolve-executor.sh` への言及は**ゼロ**（`grep -c` で確認）。
  - `skills/review-response/SKILL.md` の Step 2（"Codex cross-evaluation of each finding"）: `codex-reviewer` agent を Agent tool で直接 invoke し、この file 全体でも `resolve-executor.sh` への言及は**ゼロ**。
  - `templates/prompt-blocks/registry.txt:43` — `contain executor-resolution.md skills/run/SKILL.md skills/goal/SKILL.md`。これは **machine-checked registry** であり、resolution block が **正確に2つのskill（run/goal）にのみ** contain されていることの構造的・決定的な証拠です。`skills/review`／`skills/review-response`／`templates/CLAUDE-routing-snippet.md`（conversational routing での pm-spec 直接委譲）はいずれも対象外です。
  - したがって、malformed/refused な `binding.conf` は `/shell-team:review`・`/shell-team:review-response` の Step 2・conversational routing 経由の呼び出しに対しては**何も gate しません** — universal claim「resolution runs before any invocation」は、これらの実在する経路について**偽**です。
  - **両論の検討**: **Reading B**（対象外・別クラス）の論拠 — canonical source `templates/prompt-blocks/executor-resolution.md` 自身は「for each of the six bound roles **a phase invokes**」と loop phase にスコープしており、`skills/review`／`skills/review-response` は「full team pipeline をスキップする shortcut」と自称しているため、resolution gate のバイパスも shortcut の当然の結果と読める。**Reading A**（defeats never-dropped ①）の論拠 — **v3 の再設計自体が** DP4 を「universal かつ list ではない」形へ意図的に拡張し、frozen Goal・DP4・claim row 35 のいずれも entry-point の限定を一切書かずに「resolution runs before any invocation」と述べている。canonical source の狭い scoping 言語は、**出荷された文言のどこにも転記されていない**ため、暗黙の qualifier を読者に期待させることはできない。"shortcut" は「PM/Engineer/QA をスキップする」とだけ書かれており、「binding gate もスキップする」とは書かれていない——読者は合理的に、かつ誤って、これらのコマンドでも refused binding が block すると推測しうる。
  - **私の結論**: Reading A を採用します。Codex の敵対的パス（backtickを含まない prompt で再実行、shell事故なし）も、両論を同程度の深さで検討した上で独立に同じ結論（`defeats_never_dropped_1`, severity: blocker）に到達しました。
  - **pre-commitment の該当箇所**（`.shell-team/specs/T-1060-adopter-binding-docs.md:645`, Firing record）: 「Third-occurrence disposition, pre-decided at ruling 2(a): a third confidence-boundary finding on the **shrunken pointer** defeats never-dropped ① and **stops the task for planning** — item 2 is not the answer to a boundary defect.」— この記述と完全に一致する状況です。
  - 矛盾する正典: `skills/review/SKILL.md`、`skills/review-response/SKILL.md`、`templates/prompt-blocks/registry.txt:43`（すべて machine-checked / 直接読解による確認）。

#### Major / Minor / nits

（なし — 本ラウンドの唯一の新規 finding は上記 Blocker であり、それ以外に新規発見はありません。）

#### Adversarial findings

- Codex の devil's-advocate パスは、Reading A／Reading B 双方について私と同等以上に深く検討し（具体的な行番号を自ら引用: `README.md:181-196`、`docs/adopting.md:202-212`、`skills/review/SKILL.md:2-5`、`skills/review-response/SKILL.md:5-13,34-47`、`templates/prompt-blocks/registry.txt:43` 等 — いずれも私自身が独立に確認した箇所と一致）、独立に同じ結論（`class_call: defeats_never_dropped_1`, severity: **blocker**）に到達しました。加えて「v3 の universal 化自体が、round 6 で修正した axis-2 の enumeration 問題を解決する過程で、axis-1 の gating claim も意図せず universal にしてしまった」という因果関係も、私自身の分析と完全に一致する形で指摘しています。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | round-6 finding（effort の未開示、Trigger A occurrence 2） | primary: closed | closed | 4面・resolve-executor.sh 505-514・effort consumer sweep を再実測し確認。severity 変更・却下なし。 |
| 2 | axis-1 universal gating claim が review/review-response 経路で偽（新規） | primary: blocker, adversarial: blocker | blocker, **defeats never-dropped ①** | 一致。両論を明示的に検討した上で Reading A（defeat）を採用。severity は Major ではなく Blocker——pre-commitment 自身がこの状況を「task stops for planning」と明示的に pre-decide しているため。 |

#### Recommendation
- **REQUEST_CHANGES — ただし通常の READY_FOR_ENG 差し戻しではありません**。pre-commitment の firing record（`.shell-team/specs/T-1060-adopter-binding-docs.md:645`）が明示的に pre-decide する通り、**この finding は never-dropped ① を defeat し、タスクは patch ではなく planning へ stop します**。次に必要なのは、architecture-level の決定です: (a) `resolve-executor.sh` による resolution を `skills/review`・`skills/review-response`・conversational routing にも wire するか、(b) honest boundary の gating claim を「`run`／`goal` loop 経由の呼び出しに限定される」ことを明示する形にスコープし直すか、あるいは (c) 他の設計判断。**この判断は codex-reviewer の権限外であり、tech-lead/pm-spec への回付が必要です**。私はここで trigger の発火とその pre-decided disposition を明示するのみで、architecture の決定自体は行いません。

#### Fast-follow disposition
- no fast-follow deferrals（本 finding は fast-follow への繰り延べの対象ではなく、pre-commitment 自身が定める「planning へ stop」という disposition に従うべきものです）

#### Confidence-boundary（Trigger A）trigger accounting — 最終状態
- round 5: 1件目（#236 スコープ過大主張）。round 6: 2件目（effort 未開示）——2ラウンド連続で **Trigger A 発火**、drop order item 1（README 縮小）が実行され、v3 として再設計されました。
- **round 7（本ラウンド）: v3 の shrunken pointer 上で3件目の confidence-boundary-class finding を検出** — axis-1 universal gating claim の review/review-response 経路における過大主張。pre-commitment 自身の pre-decided ruling（2(a)）により、これは「4回目のパッチ」としては処理されず、**never-dropped ① の defeat として task 全体が planning へ stop します**。

#### "refusal-semantics-vs-resolver"（Trigger B）trigger accounting
- round 1〜3で3件発火・closed。round 4〜7とも新規発生 **0件**。累積は3件のまま、変化なし（本ラウンドの finding はこのクラスに属しません — refusal token の意味論ではなく、resolution gate 自体の適用範囲の問題です）。

#### Notes
- 生の Codex 出力: `.shell-team/reviews/T-1060-codex-primary.{txt,jsonl}`（round 7 の内容で上書き）および `.shell-team/reviews/T-1060-codex-adversarial.{txt,jsonl}`（同様に round 7 の内容で上書き）。
