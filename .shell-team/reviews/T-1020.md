# T-1020 QA verification record

- Verdict: PASS
- Commit reviewed: `0eeaaf8` (corpus edit + regen + provenance), on top of
  `8e4ae2e` (spec v1 freeze), `8ccab31` (pair-ratification),
  `baf54bc` (interventions), `91718f1` (spec skeleton + companion). Base
  `develop` @ `392b934`.
- Board: T-1020 → READY_FOR_REVIEW
- Frozen intent: v1 `304e22fca012f5fc2eeeb7fefd9ebb916939779f`, zero
  re-freezes. `bash bin/check-intent.sh
  .shell-team/specs/T-1020-lessons-supersede-sweep.md .shell-team/todo.md` →
  `aligned: T-1020 v1 (304e22fca012f5fc2eeeb7fefd9ebb916939779f) matches
  ...` — run directly, not taken on the engineer's word.

## Mandatory structural gates

- `bash bin/check-intent.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md .shell-team/todo.md` → aligned (exit 0).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1020.md` → conformant (0 decision entries, 1 sentinel; the zero-decision sentinel is correct — the ratification pre-decided the set, leaving no non-trivial implementation-side judgment to record).
- `bash bin/check-interventions.sh --task T-1020 .shell-team/interventions/T-1020.md` → conformant (1 entry, 0 sentinel — the operator's mid-gate history-search directive).
- `bash bin/check-playbook.sh .shell-team/lessons.md` → all entries valid (chain/dangling/direction/self-reference all checked mechanically by the validator).

## check-acs.sh

`bash bin/check-acs.sh --dry-run ...` reviewed first (read-only). Live run
*before* this file existed: **14 passed, 1 failed (AC12), 1 skipped (AC13)**
— AC12 failed only on `test -r .shell-team/reviews/T-1020.md` (this file did
not exist yet), matching the engineer's own predicted post-implementation
shape exactly.

**Final measured shape, re-run after writing this file and the board flag
sub-bullet below (the promised re-run, actually performed and recorded
here):** `bash bin/check-acs.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md`
→ **15 passed, 0 failed, 1 skipped, 0 unrecognized** — AC1 through AC12 and
AC14 through AC16 all PASS, AC13 SKIP (runtime — no `check:`, by design).

## Task-specific load-bearing check — applied set == ratified set

Independently diffed the corpus against `develop` (not taken from the
hand-off's numbers):

```
$ git diff develop -- .shell-team/lessons.md
@@ -725,7 +725,8 @@
 - **Applies-to**: engineer, qa-verifier
 - **Scope**: maintainer
 - **Bound-in**: CONTRIBUTING.md
-- **Status**: active
+- **Status**: superseded
+- **Superseded-by**: 2026-08-01 — A bin/ edit's completion checklist runs the full CI-wired suite list, not a self-selected subset
 - **Source**: n/a
```

This is the **only** hunk in the file's diff. It falls inside the single
retiree entry (`2026-07-25 — Editing a file-line-pinned` `bin/` `script
needs an explicit check that cross-suite registries still match`), the exact
candidate ratified as C-01 in the "Ratified retirement set" table. The two
added/changed lines are byte-exact matches to the ratified pair's `Status`
and `Superseded-by` bytes. No other entry in the file gained or lost
anything — since the diff has exactly one hunk, the 23 keep-verdict
candidates (C-02 through C-24, minus the two C1-excluded) are provably
untouched, including the two declined-override candidates (C-05, C-06),
which remain `Status: active` with no `Superseded-by` of their own.

Confirmed the successor exists and is well-formed (independent of
`check-playbook.sh`): `grep -n "^## 2026-08-01 — A bin/ edit" .shell-team/lessons.md`
finds it at line 785, with `Scope: loop`, `Status: active`, and no
`Superseded-by` field of its own (not itself retired — satisfies C1 and C4).
`maintainer` → `loop` is a legal C2 direction.

## Append-preservation

`git diff --numstat develop -- .shell-team/lessons.md` → `2  1
.shell-team/lessons.md` (2 additions, 1 deletion) — matches AC5's frozen
1-deletion/2-addition shape exactly (the file is all-bullet-lines, so the
deletion column, not `^-[^-]`, is the correct instrument). The single
deleted line is the literal `- **Status**: active`; the two added lines are
exactly `- **Status**: superseded` and the `Superseded-by` line. No prose
line touched.

## Generated-output authenticity (independent re-run)

Re-ran the same scratch-regeneration procedure as CI's "Dogfood
gen-playbook-blocks" step, from a fresh shell, against the current working
tree (i.e. with the corpus edit already applied):

```
files: agents/engineer.md agents/pm-spec.md agents/qa-verifier.md
       agents/tech-lead.md templates/prompt-blocks/playbook-engineer.md
       templates/prompt-blocks/playbook-pm-spec.md
       templates/prompt-blocks/playbook-qa-verifier.md
       templates/prompt-blocks/playbook-tech-lead.md
count: 8
gen exit: 0
---stderr---
gen-playbook-blocks: warning: playbook-engineer.md is 41 lines (threshold 40) — consider superseding stale entries
gen-playbook-blocks: warning: playbook-pm-spec.md is 51 lines (threshold 40) — consider superseding stale entries
---byte compare---
IDENTICAL: agents/engineer.md
IDENTICAL: agents/pm-spec.md
IDENTICAL: agents/qa-verifier.md
IDENTICAL: agents/tech-lead.md
IDENTICAL: templates/prompt-blocks/playbook-engineer.md
IDENTICAL: templates/prompt-blocks/playbook-pm-spec.md
IDENTICAL: templates/prompt-blocks/playbook-qa-verifier.md
IDENTICAL: templates/prompt-blocks/playbook-tech-lead.md
```

All 8 registered files (registry count asserted = 8) come back byte-identical
to the committed working tree, confirming the zero-block-impact claim (C-01's
retiree is `Scope: maintainer`, C5). The two expected threshold warnings
(pm-spec 51, engineer 41) fired on stderr exactly as predicted — this is the
AC9 overage the board's acceptance line names and prices.

## Prohibited-surface and scope checks

- `git diff --name-only develop -- bin .github` → empty (no `bin/` or
  `.github/**` change).
- `git diff --stat develop -- docs/loop-engineering/lessons-import-disposition.md`
  → empty (AC14's ledger byte-identical, per D4).
- Full changed-file set: `.shell-team/interventions/T-1020.md`,
  `.shell-team/lessons.md`, `.shell-team/provenance/T-1020.md`,
  `.shell-team/specs/T-1020-lessons-supersede-sweep.md`,
  `.shell-team/specs/T-1020-supersede-adjudication.md`,
  `.shell-team/todo.md` — no untracked files, all within AC11's required +
  permitted list, and none of the eight generated files appear (AC11's
  negative half).
- `grep -c "T-1020 overage accepted" .shell-team/todo.md` → **1** — the
  anchor phrase occurs exactly once, on the real acceptance line, per the
  spec's own self-reference-hazard gotcha.

## Full CI-wired suite + dogfood list

Ran every `shellcheck` invocation, every fixture suite, and every dogfood
step from `.github/workflows/check-handoff.yml`, in the exact order the
workflow lists them (shellcheck 0.11.0 — the pinned version, confirmed
matching locally installed — through `check-commit-identity` on the PR
commits against `develop`), from a fresh shell.

**Result: 0 failures across all ~63 steps.** This independently confirms the
engineer's own "0 failures" claim rather than accepting it as self-reported.

## Board and PII/commit-identity checks

- `bash bin/check-handoff.sh .shell-team/todo.md` → clean.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base develop` → clean.
- `bash bin/check-pii-shapes.sh --base develop` → clean.
- `bash bin/check-commit-identity.sh --base develop` → clean.

## Adversarial fixture synthesis checklist — not applicable

This task's deliverable is a corpus data edit (two line-level operations in
`.shell-team/lessons.md`) plus board/spec/companion prose, verified by
existing, unmodified checkers (`bin/check-playbook.sh`,
`bin/gen-playbook-blocks.sh`). No `bin/*.sh` script is created or modified,
no new parser/validator/consumer ships, and no new runnable procedure is
documented — the task explicitly changes no checker and adds no suite (board
classification: "corpus-judgment work, not verification-mechanism work").
The checklist's qualifier therefore does not trigger; this is recorded
explicitly rather than silently skipped.

## Edge cases tried

- Verified the two declined-override candidates (C-05, C-06, priced in the
  companion's section 6) remain untouched: since the corpus diff has exactly
  one hunk, both are provably still `Status: active` with no
  `Superseded-by` of their own.
- Verified the successor's own `Superseded-by` absence directly (not merely
  via the checker), to rule out a validator regression masking a broken
  chain (D2/C1 re-measured independently, per AC4's own design).
- Verified `git status --short` shows nothing beyond my own review-file
  addition throughout — no residue from any of the scratch-copy
  regeneration runs (all writes were under `$TMPDIR`).

## Risk notes for reviewer

- This task is corpus-judgment work with a fully human-pre-ratified single
  retirement (N = 1); there is no code-path or executable-artifact
  regression surface beyond the existing, unmodified
  `bin/check-playbook.sh` / `bin/gen-playbook-blocks.sh`, both of which were
  exercised live rather than assumed.
- The remaining overage (pm-spec 51 lines, engineer 41 lines, both over the
  40-line threshold) is accepted on the board under D3 with a stated,
  measured reason (two independent exhaustive sweeps, zero further
  retirement stock) — this is a deliberate non-closure, not a gap.
- Two items were explicitly deferred as out of this task's scope and are
  recorded as issue candidates on the board (a schema path for
  mechanization-obsolete retirements; other prompt-economy levers) — neither
  is an open finding against this task.

---

## Codex round 1 review (2026-08-03)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1020
- Codex model: gpt-5.6-sol
- Verification mode: executed-tests
- Diff scope: 7 files changed, +806/-1 lines (`.shell-team/lessons.md`, `.shell-team/todo.md`, `.shell-team/specs/T-1020-lessons-supersede-sweep.md`, `.shell-team/specs/T-1020-supersede-adjudication.md`, `.shell-team/provenance/T-1020.md`, `.shell-team/interventions/T-1020.md`, `.shell-team/reviews/T-1020.md`)

本タスクはコーパス判断作業（corpus-judgment work）であり、レビューの主眼はコードではなく `.shell-team/specs/T-1020-supersede-adjudication.md` の判断内容そのもの。以下は Codex CLI（`gpt-5.6-sol`）を `codex exec review --base develop --json` で実リポジトリに対して直接走らせた一次パスと、Major/Blocker が出たため追加で走らせた devil's-advocate 形式の二次パスの結果を、自分自身で該当エントリの実バイトを読み直して裏取りした上でまとめたもの。

機械的なゲートはすべて green であることを自分で再実行して確認済み: `bash bin/check-playbook.sh .shell-team/lessons.md` → all entries valid、`bash bin/check-intent.sh` → aligned、`bash bin/check-provenance.sh` → conformant (0 decision, 1 sentinel)、`bash bin/check-interventions.sh` → conformant、`bash bin/check-acs.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md` → **15 passed, 0 failed, 1 skipped**（AC1–AC16 全件を自分で走らせて確認、AC13 は設計どおり SKIP）。C-01 の retiree／successor 双方の実エントリ本文を自分で読み、(i-a) occasion・(i-b) protection・(ii) Applies-to・(iii) Why の四条件それぞれについて、companion の引用が実バイトと一致することを確認した。特に (i-b) の「means はグレップだが protection は stale pin 検出そのもの」という主張は、`tests/errexit-safe/run.sh` が自分自身の `NOT_APPLY` レジストリを `file:line:content` で自己監査する実装になっていることを直接読んで裏取りでき、成立していると判断する。C-05・C-06・C-16・C-17・C-18 のスポット監査でも、companion が引用する `Rule`/`Why`/`Applies-to` の文言はすべて実エントリと一致しており、判定ロジック（occasion/protection/role-coverage/incident-class）も corpus 内の他の keep 判定（C-04・C-19・C-21 などの「予防 vs 検知は同一 occasion ではない」というパターン）と整合していた。生成 8 ファイルのバイト完全一致、AC8/AC11 の相互充足性の事前修正、AC16 の自己参照トラップ回避も実際に diff で確認済みで、これらは健全。

#### Blockers
なし。

#### Major
- **`.shell-team/specs/T-1020-supersede-adjudication.md`（356行目付近）／`.shell-team/todo.md`（27行目 `T-1020 overage accepted`）— 「網羅的」という主張の裏付けが無い。** companion は 80 件の active エントリのうち 24 候補だけを判定し、「exactly one satisfies the predicate」「no retirement stock」と結論しているが、**なぜこの 24 件が候補空間の全体を尽くしているのかを示す選定手法・会計が本文のどこにも書かれていない**（grep で `candidate` `methodology` `selection` 等を確認したが、該当する記述は無い）。§6 の「promotion discipline がすでに write time で重複排除している」という説明は非冗長性の *理由* であって、24 件の選定が完全であることの *証明* にはならない。board の `T-1020 overage accepted` 行はこれを「二つの独立した exhaustive measurement」の一つとして扱っているが、比較対象の 2026-07-26 事前公開版 74 件 pairwise domination sweep は全ペアを機械的に走査した本当の網羅的スイープであるのに対し、本タスクの 24 件はそうではない。issue #97 自体が task 1 で「Inventory active entries whose ground is now covered by a newer entry」と明記しており、これは体系的な棚卸しを要求している ── その要求に対して選定手法の記述が欠けているのは、issue の要求水準そのものを満たしていない。
  - Codex 一次パス（`[P2]`）と二次（devil's-advocate）パスの双方が、独立に同じ欠落を指摘した。二次パスはさらに具体的な「見落とし候補」として `2026-07-14 — A new bin/ script's sibling-resolution code should reuse the repo's existing resolver` (retiree 候補) と `2026-07-14 — A launch-shape-dependent script's fixtures must cover all three invocation forms` (successor 候補) を提示したが、両エントリの実テキストを自分で読み直した結果、**この特定ペアは RETIRE には該当しないと判断する**（後述のレビュー用ジャッジ参照）。ただし、この特定ペアが誤りであること自体は「24件で尽くしている」という主張の弱さを免罪しない ── もっともらしい未判定ペアを構成できてしまうこと自体が、会計の欠如を裏付けている。
  - 推奨修正: (a) companion §6 または board の該当行から「exhaustive」という語を外すか、実際に使った選定手順（例: `Category`/`Rule`/`Why` の語彙一致による絞り込みなど）を明記して選定の完全性を主張可能な形にする。(b) 少なくとも、80 件の active エントリそれぞれについて「候補として検討したが除外した理由」または「候補として検討していない」を明示する一文を添える。

#### Minor / nits
- **Minor** — `.shell-team/reviews/T-1020.md`（28–29行目）: QA record 自身が「Re-run after writing this file and the board flag (final command, below) is required to confirm **15/15 PASS + AC13 SKIP**」と約束しているが、このファイル自体の中に、その最終実行結果は一度も現れない（ファイルはそのまま183行目の "Risk notes for reviewer" で終わる）。ファイル内に記録されている唯一の live run は 25–27行目の「14 passed, 1 failed (AC12), 1 skipped」のみ。実体は正しい（この最終状態を自分で `bash bin/check-acs.sh` を再実行して独立に再現し、15 passed / 0 failed / 1 skipped を確認済み。board 側の QA コメントに書かれている数字とも一致する）ため corpus 側の欠陥ではないが、このファイル単体を監査する将来の読者には約束された証跡が欠けている。次回このファイルに触るタイミングで、約束した最終コマンドの実行結果をこのファイル自身に追記することを推奨する。
- **Minor** — `.shell-team/todo.md`: `T-1020 overage accepted` という anchor 文字列が board 内に **2 回**出現する（`grep -c` で確認）。1 回目（27行目）は本物の受理宣言行だが、2 回目（31行目）は QA 自身のハンドオフ文の中で、その anchor を検証したことを説明する際にこの語句をそのまま引用してしまっている。AC9 の check は `grep -F ... | head -1` で最初の一致行だけを見るため、実際のファイル順で本物の行が先に来ることに救われて機械的には green のままだが、これはまさに frozen spec 自身の "Notes for engineer" Gotchas 節が名指しで警告している自己参照ハザード（"if the prose ever quotes the anchor verbatim, AC9 passes on a description of an acceptance rather than an acceptance"）そのものである。今回は行の並び順が偶然味方しただけで、構造的に安全になったわけではない。QA の当該一文をアンカーの言い換え（パラフレーズ）に直すことを推奨する。
- **Nit** — `.shell-team/specs/T-1020-supersede-adjudication.md`（§6, 356行目）: 「24 adjudicated candidates」という総称が、3条件すべてを評価した 22 件の実ペア判定と、C1 プレフィルタで successor 評価前に除外された C-23/C-24 の 2 件（`**Successor**: n/a`）を一緒くたに数えている。各エントリ自身の見出し（"excluded from candidacy by C1"）で区別は明示されており実害はないが、§6 のサマリでは「22 pairs adjudicated + 2 excluded by C1」のように分けて記載する方が正確。

#### Adversarial findings
Devil's-advocate パス（`gpt-5.6-sol`、`.shell-team/lessons.md` と companion を実際に読ませた）が提示した「見落とし候補」を自分で検証した:

- 提案: retiree `2026-07-14 — A new bin/ script's sibling-resolution code should reuse the repo's existing resolver`（Applies-to: engineer）を successor `2026-07-14 — A launch-shape-dependent script's fixtures must cover all three invocation forms`（Applies-to: engineer, qa-verifier）が supersede しているのではないか、という指摘。
- 自分で両エントリの実本文を読んで D1 の三条件を当てはめた結果: **RETIRE には該当しないと判断する。** (i-a) の occasion が一致しない ── retiree の occasion は「sibling-resolution コードを *書く* 瞬間」（実装時点）であるのに対し、successor の occasion は「launch-shape 依存スクリプトの *fixture を書く* 瞬間」（検証時点）で、別の行為・別のタイミング。(i-b) の protection も異なる ── retiree の protection は「最初から正しいリゾルバを使うことで壊れたコードを *書かせない*」という予防（prevention）であるのに対し、successor の protection は「もし壊れていたら fixture が *検出する*」という検知（detection）であり、means の違いではなく protection の質そのものが違う。この「予防 timing と検知 timing は同一 occasion ではない」という区別は、companion 自身が C-04・C-19・C-21 で明示的に採用しているパターンと一致しており、companion の判定基準を一貫して適用すればこのペアも KEEP に着地するはずである。
- したがって、この特定の見落とし候補の指摘自体は棄却する（synthesis audit ledger 参照）。ただし Major で指摘した「選定手法の会計欠如」という本質的な懸念は、この特定候補が外れたことでは解消されない。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Sibling-resolution retiree × launch-shape-fixture successor が見落とされた retire 候補である | adversarial: high | rejected（finding として不採用） | 実エントリ本文を自分で読み直し D1 を再適用した結果、occasion（実装時 vs 検証時）と protection（予防 vs 検知）がいずれも一致せず、companion 自身が C-04/C-19/C-21 で使っている予防-検知区分パターンに従えば KEEP に着地する。もっともらしい候補を構成できたこと自体は Major の会計欠如の懸念を裏付けるが、この個別候補自体は誤検出と判断。 |
| 2 | 24候補の選定が候補空間全体を尽くしていることの会計が無い | primary: major (`[P2]`) / adversarial: high | major（変更なし） | 一次・二次パス双方が独立に同じ欠落へ収束し、自分でも companion 全文と issue #97 本文を確認して該当する選定手法の記述が存在しないことを確認した。個々の 24 判定（特に C-01・C-05・C-06・C-16・C-17・C-18）自体は実バイトと整合しており誤りは見つからなかったため、blocker まではエスカレートしない。 |
| 3 | QA record に約束された最終 check-acs.sh 結果が記録されていない | primary: minor (`[P3]`) | minor（変更なし） | 正規化表どおり P3→minor。自分で `bash bin/check-acs.sh` を再実行し、実体（15 passed/0 failed/1 skipped）が真であることを独立に確認できたため、エスカレートする根拠はない。 |
| 4 | 「24 adjudicated candidates」という数え方が pair 判定と C1 除外を混同している | adversarial: medium | nit（降格） | 正規化表では medium→minor が既定だが、各エントリ自身の見出しで区別が明示されており実際に読者を誤導するリスクが極めて低いと判断し、nit まで自分の裁量で降格した。 |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` を `READY_FOR_REVIEW` から `READY_FOR_ENG` へ差し戻し、上記 Major の指摘（選定手法の会計、または「exhaustive」表現の是正）を対応させる。あわせて Minor の 2 件（QA record の証跡追記、board のアンカー二重出現の言い換え）も同じラウンドで直すことを推奨する（同じセクションを触るため追加コストはごく小さい）。
- Blocker/Major ではない C-01 自体の判定（retire）とその実装（コーパス2行編集・再生成・provenance）には欠陥を見つけていない。今回の差し戻しは「24件が尽くされているか」という主張の裏付け不足に限定される。issue #97 は依然としてこの pull request が指すブランチで解決される想定でよく、issue #112（既存の deferral record の移植）は本タスクの範囲外のまま。

#### Fast-follow disposition
- n1（「24 adjudicated candidates」の数え方の精度）— **won't-fix**: 各エントリ見出しで既に判定種別が明示されており実害がないため、別 issue は起票せず、次にこのファイルを編集する機会があれば ついでに直す程度でよいと判断する。
- 上記以外の指摘（Major 1件・Minor 2件）は今回の REQUEST_CHANGES の対応範囲に含めており、merge 後に持ち越す fast-follow ではない。

---

## QA round 2 (2026-08-03) — re-verification of the rework delta

- Verdict: PASS
- Delta reviewed: `5960793` (Codex round 1 REQUEST_CHANGES), `e49dd97` (my own
  round-1 rework share), `c9f34f5` (pm-spec's round-1 rework share — new
  companion §3 "Candidate selection: the method actually used, and a
  complete accounting over all 80 active entries"; old §3–§7 renumbered
  §4–§8; board sub-bullet). HEAD at verification start: `c9f34f5`.
- Board: T-1020 READY_FOR_QA → READY_FOR_REVIEW

### 1. The accounting's arithmetic against the base ref — independently re-derived, not spot-checked

Measured against `git show develop:.shell-team/lessons.md` (per the
companion's own warning that the working tree is off by one post-C-01), not
the working tree.

- **80 active entries confirmed** at the base ref (`grep -c '^## [0-9]...'`
  filtered by `Status: active` → 80, matching §3.2's total).
- **Candidate-space identity independently re-derived from the raw `Scope`/
  `Applies-to` fields** (not taken from the companion's arithmetic): pm-spec
  bullets = 49, engineer bullets = 39, intersection = 23, union (the
  65-entry retiree candidate space) = 65. Matches the companion's own
  restated identity exactly.
- **Full mechanical recount of all 80 active entries into the claimed
  buckets**, using the strongest form the coordinator asked for
  (comm/sort-equivalent set reconciliation in a script, not a sample):
  parsed all 24 `**Retiree**`/`**Successor**` lines from companion §4–§6 to
  derive bucket (a), and all 44 named entries in §3.2's bucket (b1)/(b2) and
  §3.3's table to derive (b1)/(b2)/(b3), matching truncated (`…`) names by
  prefix against the real 80-entry base list. **Result: every one of the 80
  active entries lands in exactly one bucket, none is missing, none is
  double-bucketed, and the bucket sizes are exactly 36 / 13 / 22 / 9 —
  36+13+22+9 = 80.**
- **One quoting inaccuracy found and run to ground, not a bucket-membership
  error**: my exact-string cross-check flagged one apparent mismatch —
  C-17's retiree is quoted in the companion (originally, since section 6 of
  pass 1, unchanged by this rework) as `` `2026-07-26 — Run a spec's check:
  lines live and reconcile them before recording an intent hash` ``, but the
  real corpus heading is `` `2026-07-26 — Run a spec's `check:` lines live
  and reconcile them before recording an intent hash` `` — the companion's
  prose dropped the backticks around `` `check:` ``. Re-ran the recount with
  backticks stripped from both sides: the entry resolves cleanly to the same
  bucket (a) member, and the bucket-size recount is unaffected (36/13/22/9
  either way). This predates the rework diff (C-17's write-up is unchanged
  by `c9f34f5` — only §3 is new and §4–§8 are a renumber) and never enters
  a `Superseded-by` value or any `check:` line (C-17 is a KEEP verdict, never
  applied to the corpus), so it has no effect on any acceptance criterion or
  the ratified set. Flagged here as a documentation nit for the reviewer's
  attention, not a QA finding against this rework round.
- **The two arithmetic identities in §3.2 confirmed exactly**: bucket (a)'s
  36 entries split 34 inside the candidate space + 2 outside it — and the 2
  outside are precisely C-01's `maintainer` retiree and C-22's `maintainer`
  successor, matching the companion's own named pair. 34 + 22 + 9 = 65,
  the candidate space exactly.

### 2. The two disclosed §3.3 items

- **The Codex-adversarial sibling-resolver × launch-shape pair** is recorded
  in §3.3's table (row 6) with the refuting test stated
  (`2026-07-14 — A new bin/ script's sibling-resolution code should reuse
  the repo's existing resolver …` against
  `2026-07-14 — A launch-shape-dependent script's fixtures must cover all
  three invocation forms`, refuted on (i-a) implementation-time vs
  fixture-writing-time and (i-b) prevention vs detection) — confirmed
  present, matching Codex's own disposition exactly.
- **The inconsistently-applied block-creation criterion** is recorded in
  §3.3's table (row 3):
  `2026-07-12 — Regression fixtures must cross the boundary …` against its
  near-successor `2026-07-25 — A new lock or guard should get a
  producer-run mutation self-check before its first review round`, refuted
  on (ii) (qa-verifier lost), recorded as KEEP-in-place with the honest
  admission in §3.1 that the block-writing bar was applied inconsistently
  once. Confirmed present as claimed.
- **No candidate block was added**: `grep -c '^### C-[0-9][0-9] '
  .shell-team/specs/T-1020-supersede-adjudication.md` → **24**, unchanged.
- **The ratified set is untouched**: `.shell-team/specs/T-1020-lessons-supersede-sweep.md`'s
  "Ratified retirement set" section still reads N = 1, C-01 only, verified
  byte-identical to the round-1 commit (below).

### 3. Frozen surfaces unchanged since `42c90e8` (my round-1 PASS commit)

- `git diff 42c90e8 HEAD -- .shell-team/specs/T-1020-lessons-supersede-sweep.md`
  → empty (spec, including its intent block, byte-identical).
- `git diff 42c90e8 HEAD -- .shell-team/lessons.md` → empty (corpus
  untouched by this rework round).
- `bash bin/check-intent.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md
  .shell-team/todo.md` → `aligned: T-1020 v1 (304e22fca012f5fc2eeeb7fefd9ebb916939779f)
  ...` — same hash as round 1, zero re-freezes.
- The pair-ratification and `T-1020 overage accepted` acceptance-anchor
  lines on the board are unchanged (confirmed by reading both lines
  directly at their known line numbers; only pm-spec's own new sub-bullets
  were appended after the existing ones).
- Anchor occurrence: `grep -cF -- 'T-1020 overage accepted' .shell-team/todo.md`
  → **1** (unchanged from round 1's fix).

### 4. Full re-run of all mandatory gates

- `bash bin/check-intent.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md .shell-team/todo.md` → aligned.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1020.md` → conformant (0 decisions, 1 sentinel).
- `bash bin/check-interventions.sh --task T-1020 .shell-team/interventions/T-1020.md` → conformant.
- `bash bin/check-playbook.sh .shell-team/lessons.md` → all entries valid.
- `bash bin/check-handoff.sh .shell-team/todo.md` → clean.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base develop` → clean.
- `bash bin/check-pii-shapes.sh --base develop` → clean.
- `bash bin/check-commit-identity.sh --base develop` → clean.
- `bash bin/check-acs.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md` → **15 passed, 0 failed, 1 skipped, 0 unrecognized** (AC10's `nb -ge 24` and its seven equality tests confirmed unaffected by the new §3 prose).
- **Full CI-wired suite + dogfood list from `.github/workflows/check-handoff.yml`**, re-run in full a second time, in workflow order, from a fresh shell: **0 failures across all ~63 steps.**

### 5. Round-1 fixes still hold

Re-read my own record's `## check-acs.sh` section: the final measured shape
(15 passed / 0 failed / 1 skipped) is stated inline (added in `e49dd97`),
and no stale text elsewhere in my own sections contradicts it — the only
other place the old "14 passed, 1 failed" text appears is inside Codex's
own appended review, correctly describing the pre-fix state as a historical
finding, not a live claim.

### Edge cases tried (round 2)

- Cross-checked bucket membership using the strongest available form
  (independent set reconciliation of all 80 entries against all 44+36
  named/derived keys), not a sample — this surfaced the C-17 backtick
  quoting nit that a sample would likely have missed.
- Re-verified the two arithmetic identities (34+22+9=65 and 36+13+22+9=80)
  from first principles against the raw corpus fields, not by re-deriving
  the companion's own restated numbers.

### Risk notes for reviewer

- The C-17 backtick-quoting inaccuracy in the companion (§4–§6, pre-existing
  since pass 1, not part of this rework's diff) is real but inert: C-17 is
  a KEEP verdict, its retiree text never becomes a `Superseded-by` value or
  enters a `check:` line, and it does not affect any acceptance criterion
  or the ratified set. Worth a follow-up nit fix next time that section is
  open, not a blocker.
- pm-spec's own board bullet flags accurately that §3's counts are
  `pm-spec-measured-by-reading` and asks for re-measurement before round 2 —
  this record is that re-measurement, done independently rather than by
  re-deriving pm-spec's own arithmetic.

---

## Codex round 2 review (2026-08-03)

### Codex Review verdict: APPROVE
- Task: T-1020
- Codex model: gpt-5.6-sol
- Verification mode: executed-tests
- Diff scope（round1差戻し `5960793` からの rework 分）: 3 files changed, +231/-13 lines（`.shell-team/specs/T-1020-supersede-adjudication.md`、`.shell-team/todo.md`、`.shell-team/reviews/T-1020.md`）。`.shell-team/lessons.md` と frozen spec 本体（intent block 含む）はこのラウンドで無変更。

ラウンド1の REQUEST_CHANGES を受けた rework（QA share `e49dd97`、pm-spec share `c9f34f5`）と、それを再検証した QA round 2（`d4b9ab9`、HEAD `d4b9ab9`）の差分を対象に、`codex exec review --base 5960793 --json`（`gpt-5.6-sol`、round-1 REQUEST_CHANGES commit を base にした差分限定レビュー）を実走させた上で、自分自身でも companion 全文・board 全文・実コーパスの生バイトを読み直し、特に §3 の新規会計（disposition accounting）を**自分でゼロから独立に再構築**して検証した。

#### Round 1 の指摘の解消状況

- **Major（「exhaustive」の裏付け不足）— 解消と判断する。** companion に新設された §3「Candidate selection: the method actually used, and a complete accounting over all 80 active entries」を自分で読み、その主張を鵜呑みにせず、`develop`@`392b934` の生コーパスから独立に awk/comm で再構築した: pm-spec ブロック所属 49 件・engineer ブロック所属 39 件・積集合 23 件・和集合（65 件候補空間）を独自に導出して一致を確認し、さらに companion の §4–§6 に列挙された `**Retiree**`/`**Successor**` の 24 ブロック分（重複除去後 union 36 件）と、§3.2 の bucket (b1) 13 件・(b3) 9 件（テーブルの truncated 表記を実タイトルへ前方一致で解決）をすべて突き合わせた結果、**bucket (a) 36 + (b1) 13 + (b2) 22 + (b3) 9 = 80、重複ゼロ・欠落ゼロの真正な分割**であることを確認した。34+22+9=65 の恒等式も一致。§3.4 が「これは機械的な全ペア走査（3,160通り）ではない」と自ら明記している点も含め、開示は誠実。issue #97 本文（`Inventory active entries whose ground is now covered by a newer entry`）が要求する棚卸しの水準を、この §3 は実際に満たしている。よって「exhaustive」は今や *主張* ではなく companion 自身が定義した意味で *検証可能* になったと判断し、Major を close する。
- **Minor 1（QA record に約束された最終 check-acs.sh 結果が無い）— 解消を確認。** `e49dd97` により `.shell-team/reviews/T-1020.md` の `## check-acs.sh` 節に最終計測値（**15 passed, 0 failed, 1 skipped, 0 unrecognized**）がインラインで記録されたことを直接確認した。
- **Minor 2（board アンカーの二重出現）— 解消を確認。** `grep -cF -- 'T-1020 overage accepted' .shell-team/todo.md` を自分で実行し **1** を確認。QA の当該一文は anchor を verbatim 引用せず言い換えに変わっている。
- **Nit（「24 adjudicated candidates」の数え方）— 解消を確認。** §7 が「22 pairs adjudicated against all three predicate conditions, plus 2 excluded by the C1 pre-filter」に修正され、§0 の古い節番号参照（旧 §5→§7）も直っていることを確認した。

#### 機械ゲート再実行（自分で再実行、round 2 後の HEAD `d4b9ab9` に対して）
`bash bin/check-playbook.sh .shell-team/lessons.md` → all entries valid／`bash bin/check-intent.sh` → aligned（v1、ハッシュ不変）／`bash bin/check-provenance.sh` → conformant／`bash bin/check-interventions.sh` → conformant／`bash bin/check-pii-shapes.sh --base develop` → clean／`bash bin/check-handoff.sh` → exit 0／`bash bin/check-board-headings.sh --base develop` → exit 0／`bash bin/check-acs.sh .shell-team/specs/T-1020-lessons-supersede-sweep.md` → **15 passed, 0 failed, 1 skipped**。frozen surfaces（コーパス2行編集・spec の intent block・ratified retirement set・overage-accepted 行）は round 1 からバイト不変であることを `git diff` で確認済み。

#### Blockers
なし。

#### Major
なし。

#### Minor / nits
- **Minor（新規、二次パスで発見・自分で降格して確定）** — `.shell-team/specs/T-1020-supersede-adjudication.md` §3.2 の bucket (b1) の定義文言「outside the 65-entry retiree candidate space … and not needed as any candidate's successor」が、§3.3 の "near-successor" 用語と衝突して読める。一次パス（`gpt-5.6-sol`、`[P2]`）は、(b1) に列挙された `2026-07-20 — An advisory-only evaluation path report …`（§3.3 row 1 の near-successor）と `2026-08-02 — A review-record appendix is committed by the round that writes it`（§3.3 row 7 の near-successor）が、字面どおり読むと「someone の successor として *needed*」に見え、bucket 定義自体と矛盾すると指摘した。自分でこの 2 件が companion §4–§6 の 24 ブロックいずれの `**Successor**:` 値としても一切登場しないことを直接 grep で確認し（＝実際の二重登録は無い）、80件の分割自体は健全であると判断した。ただし §3.2 の「candidate's successor」（＝24個の numbered block の Successor 値）と §3.3 の「near-successor」（＝(b3) 候補retireeに対して検討され却下された比較対象）という**同じ "successor" という語の 2 通りの意味用法**が、注記なしで並存しているのは事実であり、これは高性能な二次モデルすら誤読するほどの曖昧さである。§3.2 の当該句に「（すなわち §4–§6 の numbered block の `**Successor**:` 値としては、の意）」という一文を足すだけで解消できる、低コストな明確化事項として記録する。
- **Nit** — `2026-07-26 — Run a spec's `` `check:` `` lines live …`（C-17 の retiree）を companion §4–§6 のプロースが `` `check:` `` の周りのバックティックを落として引用している（pass 1 由来、今回の rework 差分外）。QA round 2 が既に同一の不整合を独立に発見・記録済みで、C-17 は KEEP 判定であり `Superseded-by` 値にも `check:` 行にも一切使われないため、機械チェックへの影響はゼロ（自分でも `git grep` で確認済み）。
- **Nit** — `.shell-team/todo.md`（35行目、pm-spec の rework round 1 share）: 「the flag still reads `READY_FOR_REVIEW` while Codex round 1 returned `REQUEST_CHANGES`」という一文が、**同じコミット `c9f34f5` 自身がヘッダーの flag を `READY_FOR_QA` に書き換えた直後に書かれており**、コミット時点で既に自己矛盾していた（このコミットの diff を見ると、見出し行は `READY_FOR_REVIEW`→`READY_FOR_QA` に変わっているのに、その下のプロースは「flag still reads READY_FOR_REVIEW」と主張している）。その後 QA round 2 が別の正当な理由（QA PASS）でヘッダーを再び `READY_FOR_REVIEW` に戻したため、現在は偶然「文言どおり」に見えるが、この一文が説明する因果関係（「round 1 の REQUEST_CHANGES scope 制限のせいで flag が動いていない」）自体はすでに history として陳腐化しており、将来の読者を誤導しうる。機能的な影響はゼロ（`check-handoff.sh` 等はプロースの内容までは検査しない）。

#### Adversarial findings
今回は Blocker/Major が最終的に確定しなかったため、opt-in の二次（devil's-advocate）パスは追加起動していない。ラウンド内で実施した一次パス（`codex exec review --base 5960793 --json`）の指摘は上記 Minor に反映済み。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | bucket (b1) の定義文言が §3.3 の "near-successor" 用語と字面上矛盾する | primary: major (`[P2]`) | minor（降格） | 自分で companion §4–§6 の 24 ブロック全件の `**Successor**:` 値を grep し、指摘された2件がどちらにも一切登場しないことを直接確認した。80件の分割（36/13/22/9、重複ゼロ・欠落ゼロ）は健全で、指摘は用語運用の曖昧さに留まり実際のパーティション破綻ではないため major まで維持する根拠がないと判断した。 |
| 2 | round 1 の Major（exhaustive 主張の未検証性） | round 1 primary: major / round 1 adversarial: high | resolved（クローズ） | companion §3 で選定手法（6ステップ）と80件全件の会計が開示され、自分で raw コーパスからゼロベースで独立再構築して一致を確認した。issue #97 が求める inventory の水準を実際に満たしていると判断する。 |
| 3 | C-17 backtick 引用のずれ | (QA round 2 が独立発見・記録) | nit（変更なし） | pass 1 由来・今回差分外・KEEP 判定で機械チェックに一切影響しないことを自分でも確認したため、nit のまま。 |
| 4 | board 35行目のプロースの陳腐化 | (自分の発見) | nit | コミット `c9f34f5` 内での自己矛盾を直接 diff で確認したが、機能的影響はゼロと判断した。 |

#### Recommendation
- APPROVE → `.shell-team/todo.md` を `READY_FOR_REVIEW` から `READY_FOR_MERGE` へ進める判断はオーケストレータの担当（本ロールは board を編集しない）。C-01 の retire 判定・実装・§3 の新規会計・全機械ゲートに欠陥は見つからなかった。

#### Fast-follow disposition
- Minor（bucket (b1) 定義の "successor" 用語の曖昧さ）— **won't-fix**: 実害（二重登録・欠落）が無いことを確認済みであり、次に companion を編集する機会があれば一文追記する程度で足りるため、別 issue は起票しない。
- Nit（C-17 backtick 引用）— **won't-fix**: QA round 2 の記録どおり、次にこのセクションを触るタイミングで直せば十分。
- Nit（board 35行目の陳腐化したプロース）— **won't-fix**: 過去ラウンドの hand-off ログとしての性質上、事後的に書き換えるより「当時そう書いた」という記録価値の方が高く、機能的影響もゼロのため、そのまま履歴として残すことを推奨する。
