# T-1078 review

<!-- BEGIN review: T-1078 -->
## Round 1 — QA

**Verdict: PASS → READY_FOR_REVIEW.** Full sweep run live this round (own numbers, nothing accepted from the hand-off unverified). Branch point `B = git merge-base feature/1077-worktree-reconcile HEAD` = `ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` throughout.

### Gates (all run live)

- `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1078-tier3-pilot.md 2>&1 | tail -1` → **`check-acs: 17 passed, 0 failed, 0 skipped, 0 unrecognized`** — own command, matches the engineer's claim exactly.
- `bash bin/check-intent.sh .shell-team/specs/T-1078-tier3-pilot.md .shell-team/todo.md` → `aligned: T-1078 v1 (01b2715902aa1760840033ee755443cb03ac1c52)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1078.md` → `conformant: ... (4 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1078 -- .shell-team/interventions/T-1078.md` → `conformant: ... (0 entries, 1 sentinel)`.
- `bash bin/check-pii-shapes.sh --base ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` → `clean (no PII-shaped bytes found)`.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` → exit 0.
- `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`.

### Computed-value audit (verdict-gating, re-derived from the evidence region, not from the prose)

- `python3 -c "print((334625+278772)/2)"` → `306698.5` → note's `serial-n1` mean `306699` (half-up rounding, consistent with `parallel-n2`'s own rounding below).
- `python3 -c "print((456099+515770)/2)"` → `485934.5` → note's `parallel-n2` mean `485935`.
- `python3 -c "print((485934.5-306698.5)/306698.5*100)"` → `58.44…` → note's "≈58% slower" — direction and magnitude both confirmed.
- `python3 -c "print((236690+293220)/2)"` → `264955.0` → note's `rework-parallel-n2` mean `264955` (exact).
- `python3 -c "print((220822+221039)/2)"` → `220930.5` → note's `rework-serial-n1` mean `220931` (half-up, consistent).
- Spread fields: `334625-278772=55853`, `515770-456099=59671`, `293220-236690=56530`, `221039-220822=217` — all four `- pilot3-arm:` spread values match their own two reps exactly.
- Launch-plus-notification overhead: `334625-303524=31101`, `278772-240020=38752` — both match the note's own "serial rep1 31101 ms, serial rep2 38752 ms" derivation exactly.
- Instance count: serial (1×2)+parallel (2×2)+rework-serial (1×2)+rework-parallel (2×2) = 12 — matches "Twelve real Agent-tool engineer instances". Landing count: parallel (2×2)+rework-parallel (2×2) = 8 — matches "8 of 8 landings".
- **AC11 byte-identity, run by hand** (not only through check-acs): `git show 443ce84:docs/loop-engineering/phase-multiplexing.md`, extracted the `pilot3-evidence` region with the same `awk` boundary check-acs itself uses, and `cmp`'d against the region extracted from HEAD — byte-identical, 21 non-blank lines (≥20 floor met).

### Conclusion-direction audit (verdict-gating)

- Corrected `- break-even-condition:` (read at HEAD, line ~240): states the sum of `partition-overhead` (authoring + serialization) and `rework-amplification`, carries zero occurrences of `rebase`, and carries `#277` — matches D6(i)'s required shape and AC2's own base-vs-head assertions (re-run independently, PASS).
- Corrected `- break-even: reconcile-cost` (line ~237): describes composing trees and a two-parent commit through `bin/land-worktree.sh`, "the two agent gates run once, against the final composed tip" — reads consistent with `skills/run/SKILL.md` lines 177/179 (read first-hand, quoted verbatim in Summarized sources and confirmed matching byte-for-byte on the load-bearing clauses).
- `- recommendation: tier3-work-splitting — not-yet —` line's reason ("this pilot measured a negative wall-clock saving... reconcile-cost itself priced sub-second and is no longer this tier's blocker... the disposition now rests on partition-overhead and rework-amplification, both measured") follows directly from the measured figures above — no unsupported leap.
- Six `- pilot3-legitimacy:` lines, each checked against its own stated evidence inside the evidence region: `production-unit` (12/12 instances, evidence region's `instances:` line), `real-mechanism` (8/8 landings through unmodified `bin/land-worktree.sh`, corroborated independently by AC14's byte-identity PASS on that exact file), `same-machine-session` (one continuous session, D3), `disjoint-partition-fixed-before-launch` (partition manifest committed at `pilot-base` before any instance launched, per the `pinned-base` line), `arm-completion` (12/12 instances returned, 8/8 landed), `repetition-variance` (every `- pilot3-arm:` line above discloses `reps=2` and a spread) — all six `met` verdicts are grounded in evidence-region facts, not asserted for free.

### Summarized-sources audit (per source, site checked, distinction claimed, result)

- `docs/loop-engineering/phase-multiplexing.md` (base blob) — sites: the base `- break-even-condition:` and `- break-even: reconcile-cost` lines (via `git show <B>:…`), the base heading, base Terms section. Distinction claimed: erratum shape (omits `rework-amplification`, cites `partition-overhead` only in excuse clause) and rebase-shaped `reconcile-cost`. Result: **matches** — confirmed both via AC2/AC3's own live re-run and by reading the base blob directly.
- `.shell-team/specs/T-1069-phase-multiplexing.md` AC1/AC4/AC5/AC6/AC8 — sites: each criterion's own prose, read first-hand. Distinction claimed: zero-occurrence lock on measured Tier-3 saving (AC6), nine-heading lock (AC1), no-`git worktree add` lock plus five pilot-safety ids (AC5), population/cores/`n`-vs-cores invariant (AC4), enumerated git-verb allow-list (AC8). Result: **matches** verbatim, and independently re-run at both refs (PASS at base for AC1/AC6, FAIL at HEAD for both — by-design reds, confirmed live).
- `.shell-team/specs/T-1077-worktree-reconcile.md` — sites: branch-point discriminator paragraph (lines 7–11, read directly), AC9's check line (`grep`'d for `eng-2`), AC12's byte-identity list (`grep`'d for `phase-multiplexing`), AC13's scope lock. Result: **matches** — the discriminator's "explicit-branch-existence fallback" reasoning is reused verbatim one car forward; AC9 does grep `"instance":"eng-2"`; AC12 does name `docs/loop-engineering/phase-multiplexing.md`. AC12 and AC13 independently re-run: AC12 PASS→FAIL, AC13 PASS→FAIL at base vs HEAD — matches the corrected Blast-radius row exactly.
- `skills/run/SKILL.md` lines 173–193 — read directly (`sed -n '170,195p'`). Distinction claimed: no-rebase composing-tree mechanism, gate policy (mechanical per-landing, two agent gates once at final composed tip), telemetry convention (`--instance … --span orchestrator --phase reconcile`, orchestrator-emitted). Result: **matches** byte-for-byte on every quoted clause.
- `bin/land-worktree.sh` header/flags — read directly (lines 1–60, plus a `grep` over the flag-parsing block). Distinction claimed: refuses `structural`/exit 2 when `--onto` is checked out anywhere; flags exactly `--base --onto --worker [--worker-tree] [--check-only]`. Result: **matches**.
- `docs/loop-engineering/harness-agent-concurrency.md` — read directly (lines 111–125, 286–312, 373). Distinction claimed: launch latency ≈14.3–32.8 s, mean ≈20.7 s (ten launches); telemetry emitted by the orchestrator, never the instance; a bare-`&` watchdog dies with its foreground call, only a harness-tracked background Bash survives. Result: **matches** verbatim.
- `.shell-team/interventions/T-1066.md` + `.shell-team/specs/T-1068-agent-concurrency.md` lines 39–47 — read directly. Distinction claimed: the operator-ratified verdict-scope ruling (computed values + conclusion direction gate; connective prose is a note; no escalation ratchet). Result: **matches** — T-1078's own `### Verdict scope` section is a faithful, correctly-adapted restatement (subject changed from "feasibility investigation" to "quantitative research note", substance identical).
- `.shell-team/todo.md` lines 1–30 — read directly. Distinction claimed (at the time the spec's Summarized sources was written, i.e. before this task's own board insertion): empty `## Active` list, two `## Done` headings (issue #301, pre-existing). Result: the two-`## Done`-heading structural fact still **matches** at HEAD (`grep -n '^## ' .shell-team/todo.md` → lines 173 and 1709); the "empty Active" claim describes the pre-this-task state and is correctly superseded by this task's own insertion, not contradicted by it.

### Blast radius audit (independently re-run, not trusted from the hand-off)

A scratch worktree (`git worktree add --detach <scratch-path> ae1fb07d…`, this task's own branch point) supplied every base-side read; the current checkout supplied every head-side read. `CHECK_ACS_TIMEOUT=300`.

- `T-1074-fanout-orchestration.md` **AC14**: base **FAIL**, head **FAIL** — matches the corrected row ("already FAIL at base, unrelated to phase-multiplexing.md").
- `T-1075-fanout-adoption-versioning.md` **AC14**: base **FAIL**, head **FAIL** — matches the corrected row.
- `T-1077-worktree-reconcile.md` **AC13** (its own scope lock): base **PASS**, head **FAIL** — matches the corrected row exactly (flip caused by this task's own files landing on T-1077's still-fresh tip).
- `T-1077-worktree-reconcile.md` **AC12** (byte-identity list): base **PASS**, head **FAIL** — matches the un-corrected "PASS→FAIL" row.
- `T-1069-phase-multiplexing.md` **AC1**, **AC6** (by-design reds): base **PASS**/**PASS**, head **FAIL**/**FAIL** — matches.
- `T-1076-log-run-locking.md` **AC13**/**AC15**/**AC16**: base **FAIL/FAIL/FAIL**, head **FAIL/PASS/FAIL** — the **AC15** base→head flip is the disclosed gitignored-runs-corpus artefact (the scratch worktree carries no `.shell-team/runs/shell-team.jsonl`, `ls -la` confirms it is absent there and present, non-empty, in this long-lived checkout) — matches the hand-off's Note (3) exactly, not an effect of this task's own diff.

### Derived-population block (mandatory item — flagged, not blocking)

Re-ran the embedded `- reproduce:` command for the `t1078-read-set` derivation verbatim and byte-compared against the block frozen in the spec: **mismatch**. Re-running today gives `literal-readers=8` (was 7), `board-readers=69` (was 68), and `self` now falls into the `literal-readers+board-readers+self` bucket instead of standing alone, because `T-1078-tier3-pilot.md`'s own body — written *after* the population block was filled at spec-freeze — now itself contains the literal string `phase-multiplexing` and the literal `team-paths.sh --get todo` (inside its own AC check lines and Blast-radius prose), so a fresh grep now self-matches. This is disclosed by the block's own header ("filled at the freeze (2026-08-17) by the coordinating session") — it is an honest point-in-time snapshot, not a claimed live invariant. Materially, the **enumerated buckets of *other* specs** (61 `board-readers` + 7 `literal-readers+board-readers` = 68 external specs) are **unaffected** — the self-match reclassification only concerns this task's own file, which the Blast-radius method deliberately excludes from the sweep either way. I did **not** re-count this by eye — this finding is itself the re-run-and-byte-compare the rule requires, and its own arithmetic is stated with the exact commands above. Recorded here as a disclosed distinction for the reviewer; it does not change which specs were or should have been swept, and it touches no verdict-scope-gated content, so it is not treated as FAIL-worthy on its own.

### Regression check

- `git diff ae1fb07d14ec9c90680081e1111c5fc1d5bbc070...HEAD -- bin/ tests/ templates/ agents/ skills/ .github/` — empty (re-confirmed via AC14's own live PASS, which asserts this same range empty).
- `docs/loop-engineering/agent-concurrency.md` and `docs/loop-engineering/harness-agent-concurrency.md` — byte-identical to the branch point (part of AC14's live PASS, confirmed via the same `cmp -s` loop for both files individually).

### Edge cases tried

- Manual (non-check-acs) byte comparison of the `pilot3-evidence` region against the named evidence commit, rather than trusting AC11's own exit code alone.
- Independent re-derivation of every headline mean/spread/percentage from the raw per-rep numbers inside the evidence region, rather than accepting the prose's arithmetic.
- Live re-run of 8 distinct (spec, AC) Blast-radius rows at both refs via a real scratch worktree, rather than accepting the measured table.
- Live re-run of the derived-population's own `- reproduce:` command rather than trusting the frozen block — this is the one item that surfaced a disclosed, non-blocking mismatch (see above).

### Adversarial fixture synthesis checklist

Not applicable: this task's deliverable is prose-only (a `docs/` note plus the spec/provenance/board records) — no executable artifact is added or changed (`bin/land-worktree.sh` is executed, not modified, confirmed byte-identical by AC14) and no canonical, runnable procedure is documented for a future reader to execute verbatim (the `- command:` lines inside the note are historical evidence of what the pilot already ran, and the one representative `git worktree add` line is explicitly disclosed in provenance decision 4 as a "representative form," not a transcript meant for reuse). Per this checklist's own scope rule, it is skipped rather than forced.

### Risk notes for reviewer

- The derived-population self-match drift above (literal-readers 7→8, board-readers 68→69) is disclosed and judged non-blocking; flag it to a future spec-authoring convention if this pattern recurs (a population derivation embedded inside the very file it scans will always be liable to this once the file's own later sections are written).
- `same-machine-session` and `disjoint-partition-fixed-before-launch` legitimacy conditions rest partly on operator testimony about the execution environment (single continuous session, partition committed before launch) that this QA role cannot independently re-observe after the fact — recorded as `environmentally-unverified: pilot3-legitimacy / same-machine-session, disjoint-partition-fixed-before-launch` (the underlying commit-ordering evidence for the latter — `pinned-base` preceding any instance launch — is corroborated by the evidence region's own text, but the live session-continuity claim itself is not independently re-observable).
- The Blast-radius "board-readers" bucket (68 files) was verified by the engineer at representative scope (2 of 68 full suites, plus both universal checkers) rather than exhaustively; this QA round did not re-run the full 68×2 sweep either, and neither claims to.

<!-- END review: T-1078 -->

<!-- BEGIN review: T-1078-round2 -->
## Round 1 (codex)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1078
- Codex model: gpt-5-codex (as invoked via `codex exec`; not independently re-queried for a model-identity string beyond the CLI's own default)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 7 files, +616/-13 lines (branch point `ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` → HEAD `bd2b5a1`)

Both passes read the real files directly (`codex exec ... review --base ...` for the primary pass; a free-form `codex exec` devil's-advocate prompt scoped to `docs/loop-engineering/phase-multiplexing.md`'s Tier-3 section for the adversarial pass, triggered by the primary pass's own Blocker). Raw captures: `.shell-team/reviews/T-1078-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1078-codex-adversarial.{txt,jsonl}`.

#### Blockers

- `docs/loop-engineering/phase-multiplexing.md` — corrected `- break-even-condition:` line (D6, this task's core deliverable) — **the line's own stated arithmetic does not support its own stated conclusion.** By the line's own definitions: `partition-overhead` = authoring upper bound (139000 ms) + serialization (8 landing durations, ≈3760 ms total) ≈ 142760 ms; `rework-amplification`, defined in this same line as "the extra wall clock a split-induced rework round costs beyond what the same defect would have cost a single serial engineer," is the delta `rework-parallel-n2` mean − `rework-serial-n1` mean = 264955 − 220931 = 44024 ms. Sum ≈ 186784 ms, which is **below** the serial-n1 baseline (306699 ms) — meaning the condition, taken literally, is **met** (splitting would break even), directly contradicting the note's own prose: "found the condition unmet... together exceeded the serial baseline rather than staying below it." Re-derived independently (own arithmetic, not accepted from Codex) and confirmed via a dedicated adversarial pass that specifically tried to find a legitimate reconciling reading and found none: the only way the stated "unmet" conclusion holds is by substituting the *raw* rework-parallel mean (264955 ms, not the delta) for `rework-amplification` — which contradicts the very definition given on the same line. The corrected inequality also never re-introduces a term for the actual dominant cost this pilot found (owner-b's own critical-path wall clock, ≈396914–470048 ms, alone comparable to the whole serial baseline) — the base model's "N engineers' own combined implement-phase wall clock" term was dropped entirely rather than corrected, so the inequality cannot represent why splitting actually lost.
  Suggested fix: either (a) restate the inequality's own worked substitution using the delta-consistent arithmetic and accept that this specific formula, so defined, does not itself demonstrate "unmet" (grounding "not-yet" instead directly in the measured `- saving:` figure, which is separately correct), or (b) add back an explicit term for the split's own critical-path implementation wall clock (or state plainly that `partition-overhead`/`rework-amplification` are overhead-only terms layered on top of a separately-reported, not-summed critical-path figure) so the stated sum is not presented as the whole comparison against the serial baseline.

- `docs/loop-engineering/phase-multiplexing.md` — the `rework-amplification` term's own **unit is inconsistent between D5 and the break-even-condition line**. D5 (spec, `.shell-team/specs/T-1078-tier3-pilot.md`) defines `rework-amplification` as "whether a finding that spans the partition boundary reworks one owner or N" — an owner-count ratio (1 vs 2 here), dimensionless. The corrected `- break-even-condition:` line instead uses it as a millisecond wall-clock delta (44024 ms above), with no stated conversion rule between the two. The `- saving: ... rework-amplification:` sub-bullet reports both facts (owner count 2-of-2 vs 1-of-1, and the ms means) side by side without reconciling which one the inequality's own summed term actually is. This compounds the arithmetic Blocker above rather than being independent of it: the term the inequality sums is not unambiguously defined by the note's own text.
  Suggested fix: name the owner-count ratio and the wall-clock delta as two distinct, separately labelled quantities, and state explicitly which one (if either alone) the break-even-condition's own sum uses.

#### Major

(none — the strongest additional findings, on independent review, are documentation-precision gaps rather than defects that move a computed value or a conclusion's direction; see Minor/nits and the ledger below for what was downgraded from Codex's own raw severity and why.)

#### Minor / nits

- `docs/loop-engineering/phase-multiplexing.md` — the `pilot3-legitimacy: production-unit — met` claim ("12 real Agent-tool engineer instances... never a shell-process proxy") sits in the same note as an *unedited*, unqualified Limits bullet inherited from the Tier-1 pilot ("this task's own pilot exercised only shell-process-level concurrency"). Now that the note documents two pilots with genuinely different production-unit realities, that bullet's unqualified "this task"/"this pilot" wording is ambiguous enough that a reader (and Codex, on first pass) can misattribute it to Tier-3 and read it as contradicting `production-unit`. I do not find independent evidence that T-1078's own units were in fact shell processes — the claim is consistent with the task's designed execution-actor split (only the orchestrating session can launch real Agent-tool instances) and was read and accepted by QA against the evidence region. Recommend adding a tier-qualifier to the inherited Limits bullet.
- `docs/loop-engineering/phase-multiplexing.md` — the one disclosed, representative `- command: git worktree add "$SCRATCH/clone/owner-a" -b owner-a-work` line (D2/AC10) relies on an explanatory parenthetical ("run once per owner, inside `$SCRATCH/clone`") rather than on the command's own syntax (`git -C "$SCRATCH/clone" worktree add ...` or an explicit `cd`) to keep it confined to the clone. Provenance decision 4 already marks this line as "representative form," not a transcript meant for execution, and QA's own Adversarial fixture checklist ruled it out of the "canonical runnable procedure" class for the same reason — so this is a defense-in-depth documentation improvement, not a safety violation that occurred during the actual pilot (the `pilot3-safety: worktree-add-clone-only` claim is evidenced by the `venue` line in the frozen evidence region, not by this illustrative command).
- `docs/loop-engineering/phase-multiplexing.md` — the "No experiment was run for this tier" opening sentence and the "Multi-worktree integration sketch" paragraph (rebase-shaped, unbuilt-reconcile framing) survive unedited directly under the just-renamed "(evaluated — T-1078 pilot)" heading, now literally false and in visible tension with the pilot content immediately above and the corrected `reconcile-cost` line immediately below. Provenance decision 2 records this as a deliberate scope choice (Notes for engineer's own narrow "the four lines you rewrite in place, and nothing else"). Per the operator-ratified Verdict-scope ruling this is a stale narrative aside — it does not touch a computed value or any of the four gated conclusion elements (the two prices, the corrected inequality's terms, the saving's confidence word, the recommendation) — so it is a note, not a Blocker/Major, but it is genuine and worth a documentation follow-up.
- `docs/loop-engineering/phase-multiplexing.md` — the frozen `pilot3-evidence` region's own `- telemetry: 18 span rows` claim does not match the real runs corpus: independently verified against `.shell-team/runs/shell-team.jsonl` on this host, the `t1078-20260816T190505Z` run id carries **19** span rows and **5** event rows (24 records total), of which only **4** are `phase:"reconcile"` spans. This is a factual inaccuracy inside the D8 byte-frozen evidence region (not editable by engineer or QA without breaking AC11's byte-identity requirement), off by one on a summary count no acceptance criterion checks, and it does not touch any of the four gated conclusion elements — recorded as a disclosed inaccuracy rather than a blocking finding.
- `docs/loop-engineering/phase-multiplexing.md` — the "roughly 31–39 s... every arm... per owner" launch-plus-notification overhead framing is only actually derived (shown as a computed residual) for the serial arm's own two reps; the parallel and rework arms' analogous residuals are not shown, and a back-of-envelope check (arm wall-clock minus longest instance runtime) gives noticeably larger residuals for those arms (consistent with their residuals also folding in landing time and multi-instance notification handling, but not shown as such). A disclosure-precision gap in supporting analysis prose, not a demonstrated contradiction; the recommendation's direction is unaffected either way.
- `docs/loop-engineering/phase-multiplexing.md` — the corrected `- break-even: reconcile-cost — measured —` line's "sub-second every time" claim is scoped to the landing helper's own composing/commit step (458–490 ms), not to the full gate-policy cost (mechanical verification per landing, the two agent gates once at the final composed tip) the same line also describes in prose. AC3 only requires stating that gate-policy design where the parameter is priced, not separately measuring its cost, so this is correctly scoped rather than a defect — noted for completeness since Codex's adversarial pass raised it.

#### Adversarial findings

Devil's-advocate pass on `docs/loop-engineering/phase-multiplexing.md`'s Tier-3 pilot section and break-even-condition line, framed to specifically hunt for a legitimate reconciling reading of the arithmetic Blocker above. Result: **no legitimate reconciling reading found** — confirmed the contradiction stands under every textually-supportable definition tried. Additional findings from the same pass: the rework-amplification unit mismatch (folded into the Blockers above), a partition-overhead/rework-amplification potential double-count (the 4 rework landings' own wall clock is summed once inside `partition-overhead`'s serialization half and again embedded inside the rework arms' own wall clocks that `rework-amplification`'s delta is derived from — small in absolute magnitude, sub-second scale, but a genuine methodological imprecision contributing to why the corrected inequality's own arithmetic cannot be trusted at face value), and the evidence-region span-count and launch-overhead-scope items listed under Minor/nits above.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `pilot3-legitimacy: production-unit` vs. Tier-1-era shell-process-only Limits bullet | blocker (Codex-native P1) | minor | Ambiguous cross-reference to an unedited, unqualified Limits bullet inherited from Tier-1; no independent evidence T-1078's own units were shell processes — consistent with the task's designed execution-actor split and unchallenged by QA's own evidence-region reading. |
| 2 | Representative `git worktree add` command not self-contained-safe (`-C`/`cd` missing) | blocker (Codex-native P1) | minor | Explicitly disclosed as a "representative form" illustrative line (provenance decision 4), not a runnable transcript; QA's own Adversarial checklist already ruled this line out of the "canonical runnable procedure" class. |
| 3 | Run the full 68-file board-reader blast-radius sweep at both refs | blocker (Codex-native P1) | rejected | Directly priced down and disclosed by this task's own operator-ratified `- verification-class: no-mechanism` declaration and the Blast radius section's own stated representative-scope method (additive-diff property + two universal checkers + two full-suite age-extreme samples); this is the declared price, not an unstated one. |
| 4 | Stale "No experiment was run for this tier" / rebase-sketch paragraph under the renamed heading | major (Codex-native P2) / major (adversarial) | minor | Genuine and confirmed, but per the operator-ratified Verdict-scope ruling a stale narrative aside that touches none of the four gated conclusion elements is connective prose, not verdict-gating. |
| 5 | Evidence region's `- telemetry: 18 span rows` claim vs. the real runs corpus | major (adversarial) | minor | Independently verified against `.shell-team/runs/shell-team.jsonl` (19 span + 5 event rows, 4 reconcile-phase spans) and confirmed off by one — but it sits inside the D8 byte-frozen evidence region (uneditable without breaking AC11) and no acceptance criterion checks this count; touches none of the four gated conclusion elements. |
| 6 | "31–39 s... every arm... per owner" launch-overhead framing only derived for the serial arm | moderate (adversarial, rounded up to major per the conservative label-mapping rule before synthesis) | minor | A disclosure-precision gap in supporting analysis prose, not a demonstrated contradiction of a gated value; the recommendation's direction is unaffected. |
| 7 | `reconcile-cost` "sub-second" claim scoped to the landing step, not the full gate-policy cost | major (adversarial) | minor | AC3 only requires stating the shipped gate-policy design where the parameter is priced, not separately measuring the two-agent-gate cost; the claim is correctly scoped as written. |
| 8 | `rework-amplification` defined as an owner-count ratio in D5 but used as a ms wall-clock delta in the break-even-condition line | major (adversarial) | blocker | Upgraded: this is not an independent documentation nit but a second facet of the same D6 defect — the break-even-condition's own definition of one of its two named terms is internally inconsistent with D5's own definition of that term, which is exactly the "corrected inequality's terms" the operator-ratified Verdict scope names as gated. |

#### Recommendation

- REQUEST_CHANGES → set back to READY_FOR_ENG with the two Blockers above (the break-even-condition's own arithmetic and its `rework-amplification` term's unit inconsistency with D5) as the linked findings. Everything else in this round is disclosed as a note or explicitly rejected per the ledger above and does not, on its own, require a rework round.

#### Fast-follow disposition

- Ambiguous Tier-1-era Limits bullet vs. `production-unit — met` (ledger #1) — file-an-issue: add a tier-qualifier so the shared Limits section disambiguates which pilot's shell-process-only limitation it describes, now that two pilots with different production-unit realities coexist in one note.
- Representative `git worktree add` command not self-contained-safe (ledger #2) — file-an-issue: adopt `-C`/`cd`-scoped form for any future illustrative worktree command in this note, as a defense-in-depth documentation convention.
- Stale "No experiment was run for this tier" / rebase-sketch paragraph (ledger #4) — file-an-issue: a prose-only cleanup pass over the two paragraphs immediately below the renamed heading, out of this round's own enumerated scope (provenance decision 2) and not gated by any acceptance criterion.
- Evidence-region `18 span rows` vs. actual 19 span/5 event rows (ledger #5) — file-an-issue: note the discrepancy for whoever next authors a `pilot3-evidence`-shaped region, since the region itself cannot be corrected without breaking AC11's byte-freeze; no action against this shipped record is required.
- Launch-overhead framing only derived for the serial arm (ledger #6) — file-an-issue: a follow-up pass computing and disclosing the analogous per-arm residual for `parallel-n2` and the rework arms explicitly, rather than generalizing the serial-arm figure to "every arm."

## Round 2 (QA)

**Verdict: PASS → READY_FOR_REVIEW.** Narrowly scoped per the orchestrator's routing (do not re-litigate round-1-verified properties). Branch point `B = git merge-base feature/1077-worktree-reconcile HEAD` = `ae1fb07d14ec9c90680081e1111c5fc1d5bbc070`. Rework commit `0159f96` — diff confined to exactly the three declared files (`git diff --name-only 92b243d 0159f96` → `.shell-team/provenance/T-1078.md`, `.shell-team/todo.md`, `docs/loop-engineering/phase-multiplexing.md`).

### Blocker 1 closed — the corrected line's own substitution now matches its own stated conclusion

Own arithmetic (own commands, not accepted from the engineer's hand-off), against the byte-frozen `pilot3-evidence` region's own figures:

- `python3 -c "print((396914+470048)/2)"` → `433481.0` — matches the line's "N-way critical-path implement" term (owner-b's own per-instance runtime, `max_i` over workers, per rep, then averaged — never a naive sum).
- `python3 -c "print(468+490+475+458+459+475+462+475)"` → `3762` — the eight landing durations (seven exact + one disclosed mtime-approximation, taken at its stated ≈475 ms), matching the line's serialization figure exactly.
- `python3 -c "print(139000+3762)"` → `142762` — matches `partition-overhead` (139 s authoring upper bound, converted to ms, plus serialization).
- `python3 -c "print(433481+142762+264955)"` → `841198.0` — split-side sum, matching the line's own stated `841198 ms`.
- `python3 -c "print(306699+220931)"` → `527630` — serial-side sum, matching the line's own stated `527630 ms`.
- `841198 > 527630` → **unmet**, and the line's own prose states "unmet" — the substitution and the conclusion now agree. Blocker 1 (self-contradicting arithmetic) is closed.
- Robustness re-check (the engineer's own claimed mutation self-check, re-run independently): `python3 -c "print(433481+142762+44024, 433481+142762+44024 > 527630)"` → `620267.0 True` — substituting the delta (44024 ms, the quantity Blocker 1's original wrong reading had used) instead of the whole-round wall clock (264955 ms) still yields `unmet`; the choice between the two forms does not change the conclusion's direction for this pilot's own numbers, only its consistency with D5 (see Blocker 2 below).

### Blocker 2 closed — `rework-amplification` unit reconciled against D5

D5 (`.shell-team/specs/T-1078-tier3-pilot.md`, read directly) defines the term by the owner-count fact itself: "whether a finding that spans the partition boundary reworks one owner or N." The corrected `- break-even-condition:` line now prices `rework-amplification-input`'s ms carrier as the seeded round's own **whole measured wall clock** (`rework-parallel-n2` mean, 264955 ms), reasoning explicitly, on the line itself, that "when N owners are genuinely forced the round's own critical path is bounded by all N of them together, so the round's raw wall clock is the honest ms carrier of that fact, not a subtraction" — this names the owner-count ratio and the wall-clock quantity as distinct things and states which one the sum uses, exactly what Codex's suggested fix asked for. The `- saving:` block's own `rework-amplification` sub-bullet (line 81) still reports both the owner-count fact (2-of-2) and the two ms means, now with a pointer clause to the corrected line rather than the two figures standing side by side unreconciled — read directly, consistent, no contradiction with the corrected line's own reasoning.

Every term on the corrected line and in the Worked substitution block carries a unit, an evidence-region source (`per-instance agent runtimes`, `partition-overhead, authoring half`, the landing durations, `rework-parallel-n2`/`rework-serial-n1`/`serial-n1` arm-timing lines — all read directly, all inside the byte-frozen region), and a shown substitution, per the operator-ratified class-closure scope.

### Regression / boundary checks (own commands)

- `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh --dry-run .shell-team/specs/T-1078-tier3-pilot.md 2>&1 | tail -1` → `check-acs: dry-run only — nothing executed, 0 unrecognized` (read every generated command before live run).
- `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1078-tier3-pilot.md 2>&1 | tail -1` → **`check-acs: 17 passed, 0 failed, 0 skipped, 0 unrecognized`**.
- `bash bin/check-intent.sh .shell-team/specs/T-1078-tier3-pilot.md .shell-team/todo.md` → `aligned: T-1078 v1 (01b2715902aa1760840033ee755443cb03ac1c52)` — hash unchanged from the freeze.
- `B=$(git merge-base feature/1077-worktree-reconcile HEAD); bash bin/check-pii-shapes.sh --base "$B"` → `clean (no PII-shaped bytes found)`.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base "$B"` → exit 0.
- `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1078.md` → `conformant: ... (5 decision entries, 0 sentinel)` — read all 5 entries; the two new ones (this round's own critical-path-term and rework-unit choices, with the mutation self-check) are non-trivial and grounded in the corrected line, the evidence region, and this review file's own Round 1 (codex) Blockers.
- `bash bin/check-interventions.sh --task T-1078 -- .shell-team/interventions/T-1078.md` → `conformant: ... (0 entries, 1 sentinel)` — file untouched by this round, confirmed by the diff-scope check below.
- **AC11 byte-identity, re-run by hand** (not only through check-acs): `git show 443ce84:docs/loop-engineering/phase-multiplexing.md`, extracted the `pilot3-evidence` region with the same boundary markers, `cmp -s` against the region extracted from HEAD — byte-identical, 23 lines. The frozen region and `.shell-team/interventions/T-1078.md` are confirmed untouched by this round.
- Diff-scope confinement: `git diff --name-only 92b243d 0159f96` → exactly the three declared files; `git status --short` → clean (nothing outstanding before this hand-off's own board append).

### Consistency checks (this round's own scope)

- `- saving:` block's `rework-amplification` sub-bullet (read at HEAD, line 81): still reports the owner-count fact and the two ms means, now pointing at the corrected line rather than leaving them unreconciled — consistent, no substance change beyond the pointer clause, matching the hand-off's own claim.
- Telemetry snapshot-vs-live clarification (line 220, outside the frozen region): states the frozen `18 span rows` figure is as-of the evidence commit (`443ce84`) only, and an independent count at review time found 19 spans + 5 events, 4 `phase:"reconcile"` — this is new prose, not a re-derivation on my part this round (Round 1 codex's own adversarial pass already independently measured 19/5/4 against the real runs corpus and it is unchanged since); the sentence sits outside the `<!-- BEGIN/END pilot3-evidence -->` markers, confirmed by `grep -n` (line 220 is after `<!-- END pilot3-evidence: T-1078 -->` at line 183).

### Edge cases tried

- Independently re-derived every term of the corrected `- break-even-condition:` line's Worked substitution from the raw evidence-region figures, rather than accepting the block's own printed arithmetic.
- Re-ran the engineer's own claimed robustness/mutation check (substituting the delta 44024 ms for the whole-round wall clock 264955 ms) independently, confirming the conclusion's direction is unaffected by that specific substitution (620267 > 527630, still unmet) while the D5-consistency reasoning is what actually closes Blocker 2 — not the direction-invariance alone.
- Re-verified the frozen `pilot3-evidence` region's byte-identity against evidence-commit `443ce84` by hand, rather than trusting AC11's exit code alone, given this round rewrote text immediately adjacent to (though outside) that region.

### Risk notes for reviewer

- The reconciliation of `rework-amplification`'s unit against D5 is a reasoned justification (why the whole-round wall clock is "the honest ms carrier" of an owner-count fact), not a literal count→ms conversion formula; I judge it coherent and responsive to Codex's own suggested fix ("name the owner-count ratio and the wall-clock delta as two distinct... quantities, and state explicitly which one... the sum uses"), but a reviewer may want to weigh whether prose reasoning discharges Blocker 2 as fully as an explicit formula would.
- All other round-1-verified properties (Summarized-sources audit, Blast-radius audit, derived-population disclosure, computed-value audit of the arm means/spreads themselves) are unchanged by this round's diff (confined to the break-even-condition line, its pointer clause, and the telemetry sentence) and were not re-litigated per the orchestrator's own scope — see `## Round 1 — QA` above for that coverage.
- This task's deliverable remains prose-only (docs/spec/provenance/board records); the Adversarial fixture synthesis checklist does not apply, unchanged from Round 1's own disposition.

## Round 2 (codex)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1078
- Codex model: gpt-5-codex (as invoked via `codex exec`; not independently re-queried for a model-identity string beyond the CLI's own default)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 4 files, +72/-3 lines (Round 1 verdict `e435e5f` → HEAD `e295331`)

Both passes read the real files directly (`codex exec ... review --base e435e5f --json` for the primary pass; a free-form `codex exec` devil's-advocate prompt scoped to the corrected `- break-even-condition:` line and the pre-existing `rework-amplification-input — modeled —` line for the adversarial pass, triggered by two Blocker-level findings surfaced on my own re-derivation of the Worked substitution). Raw captures: `.shell-team/reviews/T-1078-codex-primary.{txt,jsonl}` (this round's capture, canonical basename, overwrites Round 1's per the no-per-round-suffix rule), `.shell-team/reviews/T-1078-codex-adversarial.{txt,jsonl}` (this round's capture; see the ledger below for which of the two candidate findings the adversarial pass actually reconciled).

Own re-derivation first (own commands, not accepted from QA's hand-off): reproduced QA's arithmetic exactly — `python3 -c "print((396914+470048)/2, 468+490+475+458+459+475+462, 139000+3762, 433481+142762+264955, 306699+220931)"` → `433481.0 3287 142762 841198.0 527630`, confirming `841198 > 527630` ⇒ unmet, matching the line's own stated conclusion. Round-1 Blocker 1 (self-contradicting arithmetic) is genuinely closed — the corrected line's own substitution now agrees with its own stated conclusion, and it re-introduces the split's critical-path implement term Round 1's own first corrected draft had dropped. That part of QA's Round 2 verdict is confirmed by independent re-derivation.

But re-deriving each addend's own evidence source (not just the sum) surfaces two problems the QA round did not catch, one of which the Codex primary pass caught independently and one of which my own line-by-line reproduction of the "Worked substitution" caught reading against the frozen intent block's own Non-goals.

#### Blockers

- `docs/loop-engineering/phase-multiplexing.md` — Worked substitution, `partition-overhead`'s serialization addend — **a specific, unobserved figure is presented as a computed value's term, in direct tension with this spec's own frozen Non-goal.** The line reads: "the 8 landing durations summed: 468+490+475+458+459+475+462 ≈ 3287 ms exact, plus the one mtime-approximated landing at ≈475 ms, ≈3762 ms total." The byte-frozen `pilot3-evidence` region (confirmed byte-identical against evidence-commit `443ce84` by hand this round, same method QA used) discloses seven explicit landing durations and states the eighth's "timer was lost to a shell error and is mtime-approximated **at under one second**" — it never states 475 ms or any other specific figure for that landing anywhere, and I found no other evidence-region line, span-row verbatim, or provenance entry that supplies one either (checked the real runs corpus directly: the one reconcile-phase span with a missing timer records `duration_ms: null`, confirming the loss rather than supplying a substitute value). `.shell-team/specs/T-1078-tier3-pilot.md`'s frozen intent block (hash unchanged, `check-intent.sh` confirms) states in `## Non-goals`: "No estimated, interpolated or carried-over figure. A value the pilot did not observe is `undetermined` with its reason, never a number." Presenting "≈475 ms" — which happens to duplicate two of the seven already-observed values rather than deriving from any disclosed measurement — as a term in a sum the line itself claims is "read directly from this pilot's own measured lines above" fails that Non-goal directly. A dedicated adversarial pass, asked specifically to find a legitimate reconciling reading (e.g., a real mtime delta or telemetry figure that actually is 475 ms for this landing), found none: "the `475 ms` figures belong to observed landings, not the missing one... Finding A stands." The magnitude (≤1000 ms against an 841198 ms sum) cannot flip the conclusion's direction, but the operator-ratified verdict scope gates "computed values reproduce" independently of direction — a term whose own evidence source is fabricated does not reproduce, regardless of what it sums to.
  Suggested fix: state the addend as a bound rather than a point figure — e.g. "+ under 1000 ms for the one mtime-approximated landing (not observed as a specific figure, licensed only as a bound per D5)" — and carry that bound through `partition-overhead`'s own total as a range (≈3287–4262 ms serialization, ≈142287–143262 ms total) rather than a single number, so no term in the Worked substitution states a figure the pilot did not observe.

#### Major

- `docs/loop-engineering/phase-multiplexing.md` — the corrected `- break-even-condition:` line's own definition of `rework-amplification-input` ("the wall clock a split-induced rework round costs... priced as that round's own measured whole wall clock") is stated as the term's general meaning, with no scoping qualifier in the doc's own prose limiting it to "this pilot's own worked substitution only" — that scoping lives in `.shell-team/provenance/T-1078.md`'s decision entry, not in the note itself. The document's other, pre-existing, unedited `- break-even: rework-amplification-input — modeled —` line characterizes the same named term generically via the `rounds-to-approve` count series (never as ms) and states explicitly that this pilot's own seeded data "is deliberately not substituted into this generic, real-gate-driven parameter." Read side by side without the provenance file open, a reader can reasonably see the new line as redefining what the term means everywhere, in tension with the older line's own claim that pilot data is never substituted into it. Independently, Codex's own primary pass flagged the identical concern unprompted (P1: "Assigning it the raw 264955 ms parallel rework duration supplies no conversion and evaluates a different quantity"). On reflection — and per the dedicated adversarial pass run specifically against this candidate finding — a careful reader who reads both lines together **can** reconcile them: the generic parameter line is untouched and still says "modeled," and the corrected line's own qualifier ("for this pilot's own worked substitution") is consistent with instantiating the same symbolic term at a scenario-specific value rather than redefining it wholesale. The adversarial pass's own verdict: "yes [reconcilable]... The notation could be clearer, but a careful reader can reconcile the two without treating them as a direct contradiction." I agree a reconciling reading exists, which is why this is Major rather than Blocker, but the ambiguity is real enough — and important enough, since it's this task's core deliverable's own central term — that I would not accept it as fully closed without the doc's own prose (not just provenance) stating the scoping qualifier explicitly.
  Suggested fix: add one clause to the corrected line itself (not only to provenance) naming this a pilot-specific instantiation of the generic term, e.g. "...priced, for this pilot's own worked substitution only, as that round's own measured whole wall clock (the generic, real-gate-driven `rework-amplification-input` parameter above stays modeled and unchanged)."

#### Minor / nits

- `docs/loop-engineering/phase-multiplexing.md` — the Worked substitution's own disclosed "small, disclosed imprecision" (the 8 landing durations inside `partition-overhead` already include the 4 rework landings' own cost, which also sits inside `rework-parallel-n2`'s own arm wall clock) is adequately disclosed with its own magnitude bound stated inline; Codex's primary pass raised the same point independently (P2) without having seen the note's own disclosure sentence first. No action needed beyond what is already there.
- `docs/loop-engineering/phase-multiplexing.md` — the critical-path implement term (433481 ms, drawn from `per-instance agent runtimes`, i.e. raw harness runtime) and the serial-side term (`serial-n1` mean, 306699 ms, the full arm wall clock including launch-plus-notification overhead) are computed on different bases: the split side's critical-path term excludes the parallel arm's own launch/notification overhead that the parallel arm's own wall clock (485935 ms mean) would include, while the serial side's term includes it. Codex's primary pass raised this independently (P2, "Include the split arm's launch overhead"). Worked through: correcting this asymmetry would only make the split-side sum larger (it currently omits real cost on the more expensive side), so it strengthens rather than threatens the "unmet" conclusion — consistent with, and a concrete arithmetic instance of, Round 1's already-deferred fast-follow on launch-overhead framing (ledger #6). No new deferral needed; folds into the existing one.

#### Adversarial findings

Devil's-advocate pass run specifically against my own two candidate Blocker/Major findings above (not a fresh open-ended hunt, since Round 1's own open-ended adversarial pass already covered the corrected line's arithmetic broadly). Result: the fabricated-figure finding (this round's Blocker) stands — no reconciling reading found, confirmed independently against the real runs corpus (the one reconcile span with a lost timer records `duration_ms: null`, not 475). The `rework-amplification-input` term-reuse finding (this round's Major) does have a reconciling reading available to a careful reader, which is why it settled at Major rather than Blocker rather than being rejected outright.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Worked substitution's `≈475 ms` addend for the one mtime-approximated landing is not sourced from any disclosed observation, only a bound ("under one second") | blocker (own re-derivation) / blocker (adversarial: "no reconciling reading found") | blocker | Confirmed against the real runs corpus directly (`duration_ms: null` for that landing) and against the frozen intent block's own Non-goal ("never a number" for an unobserved value); magnitude too small to move the conclusion's direction, but the verdict scope gates term-level evidence-sourcing independently of direction. |
| 2 | `rework-amplification-input` defined as ms wall-clock in the corrected line, in tension with the pre-existing `— modeled —` line's rounds-to-approve, non-ms characterization of the same term | major (Codex-native P1, unprompted) / major (own re-derivation) | major (not blocker) | A dedicated adversarial pass found a legitimate reconciling reading (scoping the new line to this pilot's own worked-substitution instance, distinct from the untouched generic parameter) — real but resolvable ambiguity, not a hard self-contradiction; downgraded from my own initial instinct to raise this at blocker. |
| 3 | Partition-overhead / rework-amplification double-count (4 rework landings counted in both terms) | major (Codex-native P2) / already self-disclosed in the doc | minor | Magnitude explicitly bounded inline by the doc itself as "a few hundred ms against an 841198 ms sum"; Codex's own primary pass raised it without having read that disclosure sentence, which already states the same conclusion. |
| 4 | Critical-path term omits parallel arm's launch/notification overhead the serial-side term includes | major (Codex-native P2) | minor | Worked through directionally: correcting the asymmetry only enlarges the split-side sum, strengthening rather than threatening "unmet" — the same class as Round 1's already-deferred fast-follow (ledger #6), not a new one. |

#### Recommendation

- REQUEST_CHANGES → set back to READY_FOR_ENG with the one Blocker above (the Worked substitution's fabricated `≈475 ms` figure for an unobserved value) as the linked finding, plus the Major (the `rework-amplification-input` term's still-implicit scoping, which the doc's own prose — not only provenance — should state explicitly). Round 1's two original Blockers are confirmed closed by this round's rework and are not reopened.

#### Fast-follow disposition

- Ambiguous Tier-1-era Limits bullet vs. `production-unit — met` (Round 1 ledger #1, unchanged this round) — file-an-issue: add a tier-qualifier so the shared Limits section disambiguates which pilot's shell-process-only limitation it describes.
- Representative `git worktree add` command not self-contained-safe (Round 1 ledger #2, unchanged this round) — file-an-issue: adopt `-C`/`cd`-scoped form for any future illustrative worktree command in this note.
- Stale "No experiment was run for this tier" / rebase-sketch paragraph under the Tier-3 heading (Round 1 ledger #4, confirmed still present unedited this round at lines 227/229) — file-an-issue: a prose-only cleanup pass over the two paragraphs immediately below the renamed heading.
- Evidence-region `18 span rows` vs. actual 19 span/5 event rows (Round 1 ledger #5, unchanged this round; the clarifying sentence added in the rework round sits outside the frozen region and does not resolve the frozen figure itself) — file-an-issue: note the discrepancy for whoever next authors a `pilot3-evidence`-shaped region.
- Launch-overhead framing only derived for the serial arm, now also visible as a basis-asymmetry inside the Worked substitution itself (Round 1 ledger #6, sharpened but not superseded by this round's Minor/nits item above) — file-an-issue: a follow-up pass computing and disclosing the analogous per-arm residual for `parallel-n2` and the rework arms explicitly.
<!-- END review: T-1078-round2 -->

<!-- BEGIN review: T-1078-round3 -->
## Round 3 (QA)

**Verdict: FAIL → READY_FOR_ENG.** Narrowly scoped per the orchestrator's routing (figure-provenance audit, term-site audit, regression) against rework commit `48203cb` (Codex Round 2 Blocker + Major).

### Figure-provenance audit — clean, own re-derivation

- Independently re-grepped every numeric addend the round-2 diff touches; recomputed the bound arithmetic from scratch rather than reading the doc's own printed sums: `python3 -c "b_lo=139000+3287; b_hi=139000+4287; print(b_lo, b_hi, 433481+b_lo+264955, 433481+b_hi+264955, 306699+220931)"` → `142287 143287 840723 841723 527630` — matches the doc's own `partition-overhead ∈ [142287, 143287) ms`, split-side sum `∈ [840723, 841723) ms`, serial-side `527630 ms`, exactly.
- Confirmed the eighth landing's null timer directly against the real runs corpus rather than trusting the hand-off's characterization of it: `grep '"run_id":"t1078-20260816T190505Z"' .shell-team/runs/shell-team.jsonl | grep '"phase":"reconcile"'` → 4 rows (seq 7 `468`, seq 8 `490`, seq 11 `null`, seq 12 `475`) — seq 11 (`eng-a-r2`) is the landing round 1 had fabricated as `≈475 ms`; it is genuinely `duration_ms: null`, confirming the Blocker was real and the round-2 bound fix (`under 1000 ms`, never a point figure) is the correct repair.
- `grep -n '142762\|841198\|620267\|3762\|≈475' docs/loop-engineering/phase-multiplexing.md` → 0 hits outside the byte-frozen `pilot3-evidence` region (the one hit inside that region, line 167, is a pre-existing, untouched, legitimately-observed `475 ms` for a different, exact landing — confirmed by `cmp` byte-identity against evidence-commit `443ce84`, unchanged). No stale consumer of the fabricated figure remains.
- Mutation-check range re-verified: `python3 -c "print(433481+44024+142287, 433481+44024+143287)"` → `619792 620792`, both `> 527630` — matches the provenance entry's corrected range exactly.
- This half of the rework is **closed**: every figure reproduces from its own disclosed evidence, the fabricated point value is gone, and the bound is carried consistently through every downstream consumer.

### Term-site audit — discrepancy found

- Own command, run independently of the hand-off: `grep -n 'rework-amplification' docs/loop-engineering/phase-multiplexing.md | wc -l` → **`10`**, at lines 26, 67, 74, 81, 237, 238, 240, 246, 265, 282. Cross-checked with a case/spacing-variant sweep (`grep -nio 'rework[ _-]amplification[a-z-]*'`) — same 10 lines, no eleventh site under any reasonable reading (10 lines / 15 total token occurrences across the two spellings `rework-amplification` (8) and `rework-amplification-input` (7)).
- Both `.shell-team/todo.md` ("Term-site inventory... → **11 sites**, each disposed:") and `.shell-team/provenance/T-1078.md` ("`grep -n rework-amplification`, **11 sites**") state the count as 11. Both records' own disposition tables enumerate exactly **10** numbered items (1–10) — no row 11 exists in either table. The "11 sites" claim is thus not only unreproducible against my own independent command, it is internally inconsistent with the very table printed directly beneath it in the same two records.
- The disposition table's own 10 entries are otherwise accurate and complete: each of the 6 not-apply reasons checks out against the text at its cited line, and one consistent definition story now holds (generic `rework-amplification-input — modeled —` parameter, untouched; the corrected `break-even-condition` line's own pilot-specific worked-substitution instantiation, explicitly scoped in the doc's own prose at sites 6 and 7) — this is the substantive fix Codex's Major asked for, and it is done correctly. The defect is confined to the summary count itself.

### Regression / boundary checks (own commands)

- `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1078-tier3-pilot.md 2>&1 | tail -1` → `check-acs: 17 passed, 0 failed, 0 skipped, 0 unrecognized` — 17/17 (own command: `... | tail -1`).
- `bash bin/check-intent.sh .shell-team/specs/T-1078-tier3-pilot.md .shell-team/todo.md` → `aligned: T-1078 v1 (01b2715902aa1760840033ee755443cb03ac1c52)` — hash unchanged.
- AC11 byte-identity re-run by hand (not only via check-acs): extracted `pilot3-evidence` from `git show 443ce84:docs/loop-engineering/phase-multiplexing.md` and from HEAD with matching `awk` boundary markers, `cmp -s` → byte-identical, 23 lines.
- `B=$(git merge-base feature/1077-worktree-reconcile HEAD); bash bin/check-pii-shapes.sh --base "$B"` → `clean (no PII-shaped bytes found)`.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base "$B"` → exit 0.
- `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1078.md` → `conformant: ... (7 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1078 -- .shell-team/interventions/T-1078.md` → `conformant: ... (0 entries, 1 sentinel)` — file untouched this round.
- Diff-scope confinement: `git diff --name-only e373b7b 48203cb` → exactly `.shell-team/provenance/T-1078.md`, `.shell-team/todo.md`, `docs/loop-engineering/phase-multiplexing.md` (matches the commit's own declared 3 files).
- `git status --short` → clean before this hand-off's own board/review append.

### Edge cases tried

- Re-derived the bound arithmetic from raw addends rather than trusting the printed sums (caught nothing — reproduces exactly).
- Cross-checked the eighth landing's null-duration claim against the real runs corpus directly, rather than accepting the doc's characterization of "lost timer" at face value.
- Ran a case/spacing-variant sweep on the term-site grep, to rule out the "11" being explained by a near-miss spelling my plain grep would miss (found none — 10 holds under every reading tried).

### Risk notes for reviewer

- This task's deliverable remains prose-only (docs/spec/provenance/board records); the Adversarial fixture synthesis checklist does not apply, unchanged from prior rounds' own disposition.
- The fix required is narrow: correct "11 sites" → "10 sites" in both `.shell-team/todo.md`'s Term-site inventory line and `.shell-team/provenance/T-1078.md`'s matching decision-entry header. No change to the doc's own analysis, the disposition table's substance, or any AC-gated content is needed.

<!-- END review: T-1078-round3 -->

<!-- BEGIN review: T-1078-round4 -->
## Round 4 (QA)

**Verdict: PASS → READY_FOR_REVIEW.** Scope narrowed per the orchestrator's routing (T-1050 precedent): confirm the intra-round completion fix for the round-3 FAIL's single finding (site-count transcription, "11"→"10"), against the engineer's commit `050ac07`. Round 3 already verified everything else; not re-audited here.

### Count-correction audit — confirmed

- Own command, independent of the hand-off: `grep -n 'rework-amplification' docs/loop-engineering/phase-multiplexing.md | wc -l` → **`10`** (lines 26, 67, 74, 81, 237, 238, 240, 246, 265, 282) — matches the count now printed at both corrected sites.
- `grep -n "Term-site inventory" .shell-team/todo.md` → line 74 now reads "`grep -n rework-amplification docs/loop-engineering/phase-multiplexing.md` → 10 sites, each disposed:". `grep -n "grep -n rework-amplification" .shell-team/provenance/T-1078.md` → line 22 now reads "(`grep -n rework-amplification`, 10 sites)" and line 24's grounding clause now reads "(10 matching lines, enumerated above)" — both places corrected, as the fix commit's own diff shows (`.shell-team/todo.md` +1/-1 hunk at the Term-site inventory heading; `.shell-team/provenance/T-1078.md` two hunks, the decision header and its grounding line).
- `grep -rn '11 sites' .shell-team/todo.md .shell-team/provenance/T-1078.md .shell-team/reviews/T-1078.md` → 5 hits, all inside the round-3 FAIL record (board line 16, review lines 275/301) and the completion-fix bullet itself (board line 15) quoting the old, now-corrected value as part of the historical description of what was wrong and what was fixed — none of them is a live, uncorrected claim. No stale "11 sites" remains anywhere the fix was supposed to reach.

### Re-derived-claims spot-check (own commands, 5 of the table's entries — exceeds the 3+ asked for)

- `mean(396914, 470048)` → `python3 -c "print((396914+470048)/2)"` → `433481.0`, matches the critical-path implement figure used throughout.
- 7-landing sum → `python3 -c "print(468+490+475+458+459+475+462)"` → `3287`, matches the `partition-overhead`/Worked-substitution figure.
- saving-cap delta → `python3 -c "print(485935-306699)"` → `179236`, matches the `- saving: saving-cap` line.
- Bound arithmetic (`partition-overhead`, split-side sum, margin) → `python3 -c "b=[139000+3287, 139000+4287]; s=[433481+x+264955 for x in b]; print(b, s, [x-527630 for x in s])"` → `[142287, 143287] [840723, 841723] [313093, 314093]`, matches the corrected Worked substitution bound exactly.
- Term-site count itself (above) → `10`, matches.

None of the five spot-checked figures drifted; the engineer's "all reproduce exactly" claim holds for every entry sampled.

### Diff-scope confinement and regression (own commands, all green)

- `git show --stat 050ac07` → `.shell-team/provenance/T-1078.md | 4 ++--` and `.shell-team/todo.md | 5 +++--`, exactly 2 files.
- `git diff 050ac07~1 050ac07 -- docs/loop-engineering/phase-multiplexing.md | wc -l` → `0` — the doc itself untouched by the fix, as required.
- `git diff --name-only db88057 050ac07` (round-3 FAIL commit to the fix commit) → exactly `.shell-team/provenance/T-1078.md` and `.shell-team/todo.md`, matching the commit's own declared scope.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1078.md` → `conformant: ... (7 decision entries, 0 sentinel)`.
- `bash bin/check-intent.sh .shell-team/specs/T-1078-tier3-pilot.md .shell-team/todo.md` → `aligned: T-1078 v1 (01b2715902aa1760840033ee755443cb03ac1c52)` — hash unchanged from the freeze.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `B=$(git merge-base feature/1077-worktree-reconcile HEAD); bash bin/check-pii-shapes.sh --base "$B"` → `clean (no PII-shaped bytes found)`.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base "$B"` → exit 0.
- `bash bin/check-interventions.sh --task T-1078 -- .shell-team/interventions/T-1078.md` → `conformant: ... (0 entries, 1 sentinel)` — file untouched this round.
- `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1078-tier3-pilot.md 2>&1 | tail -1` → `check-acs: 17 passed, 0 failed, 0 skipped, 0 unrecognized` — 17/17, unchanged (expected: the fix touched no AC-gated content).
- `git status --short` → clean before this hand-off's own board/review append.

### Edge cases tried

- Swept for a near-miss "11" surviving anywhere in the T-1078 records under any phrasing (`grep -rn '11 sites'`) rather than trusting only the two cited locations — confirmed the remaining 3 hits are historical quotation, not live drift.
- Re-derived 5 (not 1) of the numeric-claims table's entries independently, spanning both the intro-paragraph figures and the Worked-substitution bound, rather than accepting the hand-off's blanket "all reproduce exactly."

### Scope not re-audited this round (by design, per the routing)

- Full acceptance-criteria walkthrough, Summarized-sources cross-check against `docs/loop-engineering/phase-multiplexing.md`, and the Adversarial fixture checklist disposition (prose-only, not applicable) were completed in rounds 1–3 and are unaffected by this fix — the diff touches neither the spec nor the summarized source document. Not repeated here per the task's own minimal-scope instruction.

### Risk notes for reviewer

- This task's deliverable remains prose-only (docs/spec/provenance/board records); the Adversarial fixture synthesis checklist does not apply, unchanged from prior rounds.
- The completion fix is exactly as narrow as advertised: two files, four lines, zero AC-gated content moved. The remaining "11 sites" strings in the record are historical narration of the FAIL and its fix, not residual drift — worth a glance if anyone later greps this file for the string without the surrounding context.

<!-- END review: T-1078-round4 -->

<!-- BEGIN review: T-1078-round3-codex -->
## Round 3 (codex)

### Codex Review verdict: APPROVE
- Task: T-1078
- Codex model: gpt-5-codex (as invoked via `codex exec`; not independently re-queried for a model-identity string beyond the CLI's own default)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 4 files, +186/-12 lines (Round 2 verdict `93be077` → HEAD `6927228`, 6 commits)

Primary pass read the real files directly (`codex exec --sandbox read-only --cd <repo> review --base 93be077 --json -o ...`), scoped to the round-2→HEAD diff (rework commit `48203cb`, QA round-3 FAIL `db88057` / completion-fix `050ac07`, QA round-4 PASS `6927228`). Own re-derivation performed independently of both Codex's output and the hand-off (own `python3`/`grep` commands against the byte-frozen `pilot3-evidence` region and the doc's own text). No Blocker/Major surfaced by either pass, so the adversarial-pass trigger (step 3/5 of this role) did not fire this round. Raw captures: `.shell-team/reviews/T-1078-codex-primary.{txt,jsonl}` (this round's capture, canonical basename, overwrites Round 2's per the no-per-round-suffix rule).

#### Audit 1 — Blocker (fabricated `≈475 ms` figure) closure

Closed. Own re-derivation, not accepted from QA: `python3 -c "b=[139000+3287, 139000+4287]; s=[433481+x+264955 for x in b]; print(b, s, [x-527630 for x in s])"` → `[142287, 143287] [840723, 841723] [313093, 314093]` — matches the doc's own stated `partition-overhead ∈ [142287, 143287) ms`, split-side sum `∈ [840723, 841723) ms`, and margin exactly. The eighth landing's own timer confirmed `duration_ms: null` in the real runs corpus (independently re-grepped: `seq 11`, `eng-a-r2`), so the bound-based repair is the correct fix, not merely a plausible one. `grep -n '142762\|841198\|620267\|3762\|≈475'` over the doc → zero hits outside the byte-frozen `pilot3-evidence` region (the one in-region `475` at line 167 is a pre-existing, legitimately-observed figure for a *different*, exact landing, confirmed unchanged by `cmp` against evidence-commit `443ce84`). No stale point-figure consumer of the old Blocker remains anywhere in the analysis prose. The verdict-invariance claim ("unmet" holds across the whole bound, not merely at a point) is shown explicitly, not merely asserted, and I confirm it independently: `840723 > 527630` at the bound's own least-favorable end.

#### Audit 2 — Major (rework-amplification term-definition ambiguity, second occurrence) closure

Closed at the doc level. Own command, independent of the hand-off: `grep -n 'rework-amplification' docs/loop-engineering/phase-multiplexing.md | wc -l` → `10` (lines 26, 67, 74, 81, 237, 238, 240, 246, 265, 282) — matches the board's and provenance's now-corrected "10 sites" claim exactly (both previously misstated "11" against their own 10-row tables; the round-3→round-4 completion fix corrected the transcription, verified in Audit 3 below). Read all 10 sites directly against the disposition table's own reasons (`.shell-team/todo.md` lines 74-90): every "not-apply" reason holds against the cited line's actual text, and the two "apply" sites (6, 7 — the generic `- break-even: rework-amplification-input — modeled —` parameter and the corrected `- break-even-condition:` line) now carry explicit, in-prose scoping ("this line is not redefined... a second, distinct, pilot-specific value" / "for this pilot's own worked substitution only... stays `modeled` and unchanged by this substitution") — precisely what Codex round 2's suggested fix asked for, stated in the doc's own text rather than only in provenance. One consistent definition story holds at every site checked.

One residual imprecision found, confined to the *provenance record's own rationale*, not the doc: `.shell-team/provenance/T-1078.md` line 23 glosses the shared symbolic term as naming "one quantity (\"the extra cost a split-induced cross-partition rework round imposes, relative to a single serial engineer\")" before explaining the two instantiations — but the pilot's own worked-substitution instantiation is priced as the *raw whole-round wall clock* (264955 ms), not a delta relative to the serial engineer's own rework cost (which would be 44024 ms). Codex's primary pass caught this independently (P2, citing the same line). The doc's own load-bearing text (sites 6/7 above) never uses this "extra cost relative to..." framing and is internally consistent on its own; the loose gloss is confined to a single explanatory parenthetical in an already-closed provenance decision entry, touches no AC-gated content, and does not reopen the Major the doc-level fix closed. Rated Minor — see the ledger and Fast-follow disposition below.

#### Audit 3 — Intra-round completion-fix routing (QA round-3 FAIL → engineer fix → QA round-4 PASS)

Routing judged sound. The single finding routed this way — the board's and provenance's Term-site inventory line stating "11 sites" against a table that itself lists exactly 10 numbered entries — is a records-only transcription slip: it lives in the board and provenance (not the doc, which check-acs.sh gates), touches no computed value, and moves no conclusion. The fix commit (`050ac07`) is exactly as narrow as advertised: own `git show --stat 050ac07` → 2 files (`.shell-team/todo.md`, `.shell-team/provenance/T-1078.md`); own `git diff 050ac07~1 050ac07 -- docs/loop-engineering/phase-multiplexing.md | wc -l` → `0` (doc untouched). QA round 4 was a genuine, thorough re-verification, not a rubber stamp: independent re-grep of the term-site count, an `11 sites` residue sweep confirming the only remaining occurrences are historical narration of the FAIL and its fix, a 5-of-N spot-check of unrelated numeric claims (mean/sum/delta/bound arithmetic), and the full regression suite (check-acs 17/17, check-intent hash unchanged, AC11 byte-identity, diff-scope confinement).

The routing's cited precedent (T-1050's own "orchestrator routing — QA round-4 findings are intra-round completion fixes") is analogous in shape (a summary-prose arithmetic/count error in non-frozen text, fixed trivially, re-verified without a new Codex round) but differs in provenance: T-1050's non-escalation was licensed by an explicit, task-specific operator extension grant ("a further new-class Blocker/Major re-escalates rather than extending again"), which the orchestrator was applying narrowly; T-1078 carries no such extension grant on record (no `max_iterations` cap found anywhere in this spec — confirmed by grep), so this round's routing rests on the orchestrator's own standing propose-and-proceed authority generalizing the shape of that precedent, not on an equivalent task-specific authorization. Worth naming precisely for any future reader who cites "the T-1050 precedent" as if it always carried the same operator-backed weight — it does not, here. This is a documentation-precision observation, not an objection to the outcome: the spec's own operator-ratified Verdict-scope rule independently covers this exact class ("a stale count in a narrative aside... does not on its own produce REQUEST_CHANGES or FAIL, provided every computed value it touches is correct and no conclusion's direction moves"), and the routing produced a materially correct result confirmed by a real QA round, not a skipped one.

#### Audit 4 — Regression

- AC2 (own read of the corrected `- break-even-condition:` line, line 240): contains `partition-overhead`, contains `rework-amplification` (as a substring of `rework-amplification-input`), contains `#277`, zero occurrences of `rebase` — matches.
- AC3 (own read of the corrected `- break-even: reconcile-cost` line, line 237): zero `rebase`, contains `bin/land-worktree.sh`, contains `two-parent`; evaluated Tier-3 section carries the literal `final composed tip` (same line 237) — matches.
- AC4 (own count): 4 `- break-even: ` lines (`engineer-phase-duration`, `partition-overhead`, `reconcile-cost`, `rework-amplification-input`), each exactly once; `engineer-phase-duration` reads `undetermined` with a non-empty reason; `- saving: tier3-work-splitting — measured —` present and `- break-even: partition-overhead — measured —` present (coupling holds) — matches.
- AC11 byte-identity: own diff read confirms the round-2→HEAD doc diff (lines 79-81, 236-249) sits entirely outside the `pilot3-evidence` region (lines 161-183); nothing else in the doc, spec, agents, bin, tests, or the frozen intent block moved. Diff-scope confinement: `git diff --stat 93be077...HEAD` → exactly the 4 declared files (provenance, reviews, todo, the doc), matching every round's own hand-off claim.

#### Blockers

(none)

#### Major

(none)

#### Minor / nits

- `.shell-team/provenance/T-1078.md` line 23 — the rework round 2 decision's own rationale glosses the shared `rework-amplification`/`rework-amplification-input` term as naming "the extra cost... relative to a single serial engineer" (a delta-shaped description) immediately before explaining that this pilot's own instantiation is priced as the raw whole-round wall clock (not a delta) — an internal looseness confined to this one explanatory parenthetical, caught independently by Codex's own primary pass. The doc's own load-bearing text (the two "apply" sites this round's rework touched) never uses this framing and is unambiguous on its own; no AC-gated content is affected. Suggested fix: reword the gloss to something like "one quantity — the wall-clock cost a split-induced cross-partition rework round imposes on the split — with two distinct, separately-scoped instantiations" (dropping "relative to a single serial engineer," which describes a comparison the break-even inequality performs elsewhere, not the term itself).
- The five Round 1/Round 2 fast-follow Minors are unchanged this round (restated below) and remain correctly deferred: no edit to any of the five sites this round's diff touches.

#### Adversarial findings

Not run this round — no Blocker/Major surfaced by either the primary Codex pass or my own independent re-derivation, so the sole trigger for the adversarial pass (per this role's own step 3/5 discipline) did not fire. Round 1's own open-ended adversarial pass (arithmetic broadly) and Round 2's own targeted adversarial pass (the fabricated-figure and term-reuse candidates) remain the standing adversarial record for this note's core deliverable; neither is re-run against text this round's diff left unchanged.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `.shell-team/provenance/T-1078.md`'s own rework-amplification gloss ("extra cost... relative to a serial engineer") vs. the pilot's actual whole-round-wall-clock instantiation | major (Codex-native P2) | minor | Confined to a single explanatory parenthetical in an already-closed provenance decision entry, not the doc's own load-bearing text (which is internally consistent at every one of the 10 sites, independently re-checked); touches no AC-gated content and does not reopen the doc-level Major this round's rework closed. |
| 2 | Orchestrator's completion-fix routing for the site-count transcription (11 vs 10) | (own audit item, not from Codex) | sound, no severity assigned | The routing produced a materially correct, thoroughly re-verified result; flagged only a documentation-precision distinction (the cited T-1050 precedent carried an explicit operator extension grant this round's routing has no equivalent of) — not an objection to the outcome. |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`. Both of Round 2's Blockers/Majors are confirmed closed by independent re-derivation; the one new finding this round (the provenance gloss) is Minor and does not gate; the intra-round completion-fix routing is judged sound.

#### Fast-follow disposition

- Ambiguous Tier-1-era Limits bullet vs. `production-unit — met` (Round 1 ledger #1, unchanged) — file-an-issue: add a tier-qualifier so the shared Limits section disambiguates which pilot's shell-process-only limitation it describes.
- Representative `git worktree add` command not self-contained-safe (Round 1 ledger #2, unchanged) — file-an-issue: adopt a `-C`/`cd`-scoped form for any future illustrative worktree command in this note.
- Stale "No experiment was run for this tier" / rebase-sketch paragraph under the Tier-3 heading (Round 1 ledger #4, confirmed still present unedited at lines 227/229) — file-an-issue: a prose-only cleanup pass over the two paragraphs immediately below the renamed heading.
- Evidence-region `18 span rows` vs. actual 19 span/5 event rows (Round 1 ledger #5, unchanged; the frozen figure itself cannot be corrected without breaking AC11) — file-an-issue: note the discrepancy for whoever next authors a `pilot3-evidence`-shaped region.
- Launch-overhead framing only derived for the serial arm (Round 1 ledger #6, unchanged) — file-an-issue: compute and disclose the analogous per-arm residual for `parallel-n2` and the rework arms explicitly.
- Provenance line 23's own rework-amplification gloss (this round's new Minor) — won't-fix: the line is a historical decision-rationale entry for an already-closed rework round, the doc's own text (which is what a future reader or checker actually consumes) is unambiguous on its own, and reopening a closed provenance entry to reword one parenthetical is disproportionate to the imprecision's own reach.

<!-- END review: T-1078-round3-codex -->
