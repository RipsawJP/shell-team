# T-1078 review

<!-- BEGIN review: T-1078 -->
## Round 1 — QA

**Verdict: PASS → READY_FOR_REVIEW.** Full sweep run live this round (own numbers, nothing accepted from the hand-off unverified). Branch point `B = git merge-base feature/1077-worktree-reconcile HEAD` = `ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` throughout.

### Gates (all run live)

- `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1078-tier3-pilot.md 2>&1 | tail -1` → **`check-acs: 17 passed, 0 failed, 0 skipped, 0 unrecognized`** — own command, matches the engineer's claim exactly.
- `bash bin/check-intent.sh .shell-team/specs/T-1078-tier3-pilot.md .shell-team/todo.md` → `aligned: T-1078 v1 (01b2715902aa1760840033ee755443cb03ac1c52)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1078.md` → `conformant: ... (4 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1078 -- .shell-team/interventions/T-1078.md` → `conformant: ... (0 entries, 1 sentinel)`.
- `bash bin/check-pii-shapes.sh --base ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` → `clean (no PII-shaped bytes found)`.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` → exit 0.
- `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`.

### Computed-value audit (verdict-gating, re-derived from the evidence region, not from the prose)

- `python3 -c "print((334625+278772)/2)"` → `306698.5` → note's `serial-n1` mean `306699` (half-up rounding, consistent with `parallel-n2`'s own rounding below).
- `python3 -c "print((456099+515770)/2)"` → `485934.5` → note's `parallel-n2` mean `485935`.
- `python3 -c "print((485934.5-306698.5)/306698.5*100)"` → `58.44…` → note's "≈58% slower" — direction and magnitude both confirmed.
- `python3 -c "print((236690+293220)/2)"` → `264955.0` → note's `rework-parallel-n2` mean `264955` (exact).
- `python3 -c "print((220822+221039)/2)"` → `220930.5` → note's `rework-serial-n1` mean `220931` (half-up, consistent).
- Spread fields: `334625-278772=55853`, `515770-456099=59671`, `293220-236690=56530`, `221039-220822=217` — all four `- pilot3-arm:` spread values match their own two reps exactly.
- Launch-plus-notification overhead: `334625-303524=31101`, `278772-240020=38752` — both match the note's own "serial rep1 31101 ms, serial rep2 38752 ms" derivation exactly.
- Instance count: serial (1×2)+parallel (2×2)+rework-serial (1×2)+rework-parallel (2×2) = 12 — matches "Twelve real Agent-tool engineer instances". Landing count: parallel (2×2)+rework-parallel (2×2) = 8 — matches "8 of 8 landings".
- **AC11 byte-identity, run by hand** (not only through check-acs): `git show 443ce84:docs/loop-engineering/phase-multiplexing.md`, extracted the `pilot3-evidence` region with the same `awk` boundary check-acs itself uses, and `cmp`'d against the region extracted from HEAD — byte-identical, 21 non-blank lines (≥20 floor met).

### Conclusion-direction audit (verdict-gating)

- Corrected `- break-even-condition:` (read at HEAD, line ~240): states the sum of `partition-overhead` (authoring + serialization) and `rework-amplification`, carries zero occurrences of `rebase`, and carries `#277` — matches D6(i)'s required shape and AC2's own base-vs-head assertions (re-run independently, PASS).
- Corrected `- break-even: reconcile-cost` (line ~237): describes composing trees and a two-parent commit through `bin/land-worktree.sh`, "the two agent gates run once, against the final composed tip" — reads consistent with `skills/run/SKILL.md` lines 177/179 (read first-hand, quoted verbatim in Summarized sources and confirmed matching byte-for-byte on the load-bearing clauses).
- `- recommendation: tier3-work-splitting — not-yet —` line's reason ("this pilot measured a negative wall-clock saving... reconcile-cost itself priced sub-second and is no longer this tier's blocker... the disposition now rests on partition-overhead and rework-amplification, both measured") follows directly from the measured figures above — no unsupported leap.
- Six `- pilot3-legitimacy:` lines, each checked against its own stated evidence inside the evidence region: `production-unit` (12/12 instances, evidence region's `instances:` line), `real-mechanism` (8/8 landings through unmodified `bin/land-worktree.sh`, corroborated independently by AC14's byte-identity PASS on that exact file), `same-machine-session` (one continuous session, D3), `disjoint-partition-fixed-before-launch` (partition manifest committed at `pilot-base` before any instance launched, per the `pinned-base` line), `arm-completion` (12/12 instances returned, 8/8 landed), `repetition-variance` (every `- pilot3-arm:` line above discloses `reps=2` and a spread) — all six `met` verdicts are grounded in evidence-region facts, not asserted for free.

### Summarized-sources audit (per source, site checked, distinction claimed, result)

- `docs/loop-engineering/phase-multiplexing.md` (base blob) — sites: the base `- break-even-condition:` and `- break-even: reconcile-cost` lines (via `git show <B>:…`), the base heading, base Terms section. Distinction claimed: erratum shape (omits `rework-amplification`, cites `partition-overhead` only in excuse clause) and rebase-shaped `reconcile-cost`. Result: **matches** — confirmed both via AC2/AC3's own live re-run and by reading the base blob directly.
- `.shell-team/specs/T-1069-phase-multiplexing.md` AC1/AC4/AC5/AC6/AC8 — sites: each criterion's own prose, read first-hand. Distinction claimed: zero-occurrence lock on measured Tier-3 saving (AC6), nine-heading lock (AC1), no-`git worktree add` lock plus five pilot-safety ids (AC5), population/cores/`n`-vs-cores invariant (AC4), enumerated git-verb allow-list (AC8). Result: **matches** verbatim, and independently re-run at both refs (PASS at base for AC1/AC6, FAIL at HEAD for both — by-design reds, confirmed live).
- `.shell-team/specs/T-1077-worktree-reconcile.md` — sites: branch-point discriminator paragraph (lines 7–11, read directly), AC9's check line (`grep`'d for `eng-2`), AC12's byte-identity list (`grep`'d for `phase-multiplexing`), AC13's scope lock. Result: **matches** — the discriminator's "explicit-branch-existence fallback" reasoning is reused verbatim one car forward; AC9 does grep `"instance":"eng-2"`; AC12 does name `docs/loop-engineering/phase-multiplexing.md`. AC12 and AC13 independently re-run: AC12 PASS→FAIL, AC13 PASS→FAIL at base vs HEAD — matches the corrected Blast-radius row exactly.
- `skills/run/SKILL.md` lines 173–193 — read directly (`sed -n '170,195p'`). Distinction claimed: no-rebase composing-tree mechanism, gate policy (mechanical per-landing, two agent gates once at final composed tip), telemetry convention (`--instance … --span orchestrator --phase reconcile`, orchestrator-emitted). Result: **matches** byte-for-byte on every quoted clause.
- `bin/land-worktree.sh` header/flags — read directly (lines 1–60, plus a `grep` over the flag-parsing block). Distinction claimed: refuses `structural`/exit 2 when `--onto` is checked out anywhere; flags exactly `--base --onto --worker [--worker-tree] [--check-only]`. Result: **matches**.
- `docs/loop-engineering/harness-agent-concurrency.md` — read directly (lines 111–125, 286–312, 373). Distinction claimed: launch latency ≈14.3–32.8 s, mean ≈20.7 s (ten launches); telemetry emitted by the orchestrator, never the instance; a bare-`&` watchdog dies with its foreground call, only a harness-tracked background Bash survives. Result: **matches** verbatim.
- `.shell-team/interventions/T-1066.md` + `.shell-team/specs/T-1068-agent-concurrency.md` lines 39–47 — read directly. Distinction claimed: the operator-ratified verdict-scope ruling (computed values + conclusion direction gate; connective prose is a note; no escalation ratchet). Result: **matches** — T-1078's own `### Verdict scope` section is a faithful, correctly-adapted restatement (subject changed from "feasibility investigation" to "quantitative research note", substance identical).
- `.shell-team/todo.md` lines 1–30 — read directly. Distinction claimed (at the time the spec's Summarized sources was written, i.e. before this task's own board insertion): empty `## Active` list, two `## Done` headings (issue #301, pre-existing). Result: the two-`## Done`-heading structural fact still **matches** at HEAD (`grep -n '^## ' .shell-team/todo.md` → lines 173 and 1709); the "empty Active" claim describes the pre-this-task state and is correctly superseded by this task's own insertion, not contradicted by it.

### Blast radius audit (independently re-run, not trusted from the hand-off)

A scratch worktree (`git worktree add --detach <scratch-path> ae1fb07d…`, this task's own branch point) supplied every base-side read; the current checkout supplied every head-side read. `CHECK_ACS_TIMEOUT=300`.

- `T-1074-fanout-orchestration.md` **AC14**: base **FAIL**, head **FAIL** — matches the corrected row ("already FAIL at base, unrelated to phase-multiplexing.md").
- `T-1075-fanout-adoption-versioning.md` **AC14**: base **FAIL**, head **FAIL** — matches the corrected row.
- `T-1077-worktree-reconcile.md` **AC13** (its own scope lock): base **PASS**, head **FAIL** — matches the corrected row exactly (flip caused by this task's own files landing on T-1077's still-fresh tip).
- `T-1077-worktree-reconcile.md` **AC12** (byte-identity list): base **PASS**, head **FAIL** — matches the un-corrected "PASS→FAIL" row.
- `T-1069-phase-multiplexing.md` **AC1**, **AC6** (by-design reds): base **PASS**/**PASS**, head **FAIL**/**FAIL** — matches.
- `T-1076-log-run-locking.md` **AC13**/**AC15**/**AC16**: base **FAIL/FAIL/FAIL**, head **FAIL/PASS/FAIL** — the **AC15** base→head flip is the disclosed gitignored-runs-corpus artefact (the scratch worktree carries no `.shell-team/runs/shell-team.jsonl`, `ls -la` confirms it is absent there and present, non-empty, in this long-lived checkout) — matches the hand-off's Note (3) exactly, not an effect of this task's own diff.

### Derived-population block (mandatory item — flagged, not blocking)

Re-ran the embedded `- reproduce:` command for the `t1078-read-set` derivation verbatim and byte-compared against the block frozen in the spec: **mismatch**. Re-running today gives `literal-readers=8` (was 7), `board-readers=69` (was 68), and `self` now falls into the `literal-readers+board-readers+self` bucket instead of standing alone, because `T-1078-tier3-pilot.md`'s own body — written *after* the population block was filled at spec-freeze — now itself contains the literal string `phase-multiplexing` and the literal `team-paths.sh --get todo` (inside its own AC check lines and Blast-radius prose), so a fresh grep now self-matches. This is disclosed by the block's own header ("filled at the freeze (2026-08-17) by the coordinating session") — it is an honest point-in-time snapshot, not a claimed live invariant. Materially, the **enumerated buckets of *other* specs** (61 `board-readers` + 7 `literal-readers+board-readers` = 68 external specs) are **unaffected** — the self-match reclassification only concerns this task's own file, which the Blast-radius method deliberately excludes from the sweep either way. I did **not** re-count this by eye — this finding is itself the re-run-and-byte-compare the rule requires, and its own arithmetic is stated with the exact commands above. Recorded here as a disclosed distinction for the reviewer; it does not change which specs were or should have been swept, and it touches no verdict-scope-gated content, so it is not treated as FAIL-worthy on its own.

### Regression check

- `git diff ae1fb07d14ec9c90680081e1111c5fc1d5bbc070...HEAD -- bin/ tests/ templates/ agents/ skills/ .github/` — empty (re-confirmed via AC14's own live PASS, which asserts this same range empty).
- `docs/loop-engineering/agent-concurrency.md` and `docs/loop-engineering/harness-agent-concurrency.md` — byte-identical to the branch point (part of AC14's live PASS, confirmed via the same `cmp -s` loop for both files individually).

### Edge cases tried

- Manual (non-check-acs) byte comparison of the `pilot3-evidence` region against the named evidence commit, rather than trusting AC11's own exit code alone.
- Independent re-derivation of every headline mean/spread/percentage from the raw per-rep numbers inside the evidence region, rather than accepting the prose's arithmetic.
- Live re-run of 8 distinct (spec, AC) Blast-radius rows at both refs via a real scratch worktree, rather than accepting the measured table.
- Live re-run of the derived-population's own `- reproduce:` command rather than trusting the frozen block — this is the one item that surfaced a disclosed, non-blocking mismatch (see above).

### Adversarial fixture synthesis checklist

Not applicable: this task's deliverable is prose-only (a `docs/` note plus the spec/provenance/board records) — no executable artifact is added or changed (`bin/land-worktree.sh` is executed, not modified, confirmed byte-identical by AC14) and no canonical, runnable procedure is documented for a future reader to execute verbatim (the `- command:` lines inside the note are historical evidence of what the pilot already ran, and the one representative `git worktree add` line is explicitly disclosed in provenance decision 4 as a "representative form," not a transcript meant for reuse). Per this checklist's own scope rule, it is skipped rather than forced.

### Risk notes for reviewer

- The derived-population self-match drift above (literal-readers 7→8, board-readers 68→69) is disclosed and judged non-blocking; flag it to a future spec-authoring convention if this pattern recurs (a population derivation embedded inside the very file it scans will always be liable to this once the file's own later sections are written).
- `same-machine-session` and `disjoint-partition-fixed-before-launch` legitimacy conditions rest partly on operator testimony about the execution environment (single continuous session, partition committed before launch) that this QA role cannot independently re-observe after the fact — recorded as `environmentally-unverified: pilot3-legitimacy / same-machine-session, disjoint-partition-fixed-before-launch` (the underlying commit-ordering evidence for the latter — `pinned-base` preceding any instance launch — is corroborated by the evidence region's own text, but the live session-continuity claim itself is not independently re-observable).
- The Blast-radius "board-readers" bucket (68 files) was verified by the engineer at representative scope (2 of 68 full suites, plus both universal checkers) rather than exhaustively; this QA round did not re-run the full 68×2 sweep either, and neither claims to.

<!-- END review: T-1078 -->

<!-- BEGIN review: T-1078-round2 -->
## Round 1 (codex)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1078
- Codex model: gpt-5-codex (as invoked via `codex exec`; not independently re-queried for a model-identity string beyond the CLI's own default)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 7 files, +616/-13 lines (branch point `ae1fb07d14ec9c90680081e1111c5fc1d5bbc070` → HEAD `bd2b5a1`)

Both passes read the real files directly (`codex exec ... review --base ...` for the primary pass; a free-form `codex exec` devil's-advocate prompt scoped to `docs/loop-engineering/phase-multiplexing.md`'s Tier-3 section for the adversarial pass, triggered by the primary pass's own Blocker). Raw captures: `.shell-team/reviews/T-1078-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1078-codex-adversarial.{txt,jsonl}`.

#### Blockers

- `docs/loop-engineering/phase-multiplexing.md` — corrected `- break-even-condition:` line (D6, this task's core deliverable) — **the line's own stated arithmetic does not support its own stated conclusion.** By the line's own definitions: `partition-overhead` = authoring upper bound (139000 ms) + serialization (8 landing durations, ≈3760 ms total) ≈ 142760 ms; `rework-amplification`, defined in this same line as "the extra wall clock a split-induced rework round costs beyond what the same defect would have cost a single serial engineer," is the delta `rework-parallel-n2` mean − `rework-serial-n1` mean = 264955 − 220931 = 44024 ms. Sum ≈ 186784 ms, which is **below** the serial-n1 baseline (306699 ms) — meaning the condition, taken literally, is **met** (splitting would break even), directly contradicting the note's own prose: "found the condition unmet... together exceeded the serial baseline rather than staying below it." Re-derived independently (own arithmetic, not accepted from Codex) and confirmed via a dedicated adversarial pass that specifically tried to find a legitimate reconciling reading and found none: the only way the stated "unmet" conclusion holds is by substituting the *raw* rework-parallel mean (264955 ms, not the delta) for `rework-amplification` — which contradicts the very definition given on the same line. The corrected inequality also never re-introduces a term for the actual dominant cost this pilot found (owner-b's own critical-path wall clock, ≈396914–470048 ms, alone comparable to the whole serial baseline) — the base model's "N engineers' own combined implement-phase wall clock" term was dropped entirely rather than corrected, so the inequality cannot represent why splitting actually lost.
  Suggested fix: either (a) restate the inequality's own worked substitution using the delta-consistent arithmetic and accept that this specific formula, so defined, does not itself demonstrate "unmet" (grounding "not-yet" instead directly in the measured `- saving:` figure, which is separately correct), or (b) add back an explicit term for the split's own critical-path implementation wall clock (or state plainly that `partition-overhead`/`rework-amplification` are overhead-only terms layered on top of a separately-reported, not-summed critical-path figure) so the stated sum is not presented as the whole comparison against the serial baseline.

- `docs/loop-engineering/phase-multiplexing.md` — the `rework-amplification` term's own **unit is inconsistent between D5 and the break-even-condition line**. D5 (spec, `.shell-team/specs/T-1078-tier3-pilot.md`) defines `rework-amplification` as "whether a finding that spans the partition boundary reworks one owner or N" — an owner-count ratio (1 vs 2 here), dimensionless. The corrected `- break-even-condition:` line instead uses it as a millisecond wall-clock delta (44024 ms above), with no stated conversion rule between the two. The `- saving: ... rework-amplification:` sub-bullet reports both facts (owner count 2-of-2 vs 1-of-1, and the ms means) side by side without reconciling which one the inequality's own summed term actually is. This compounds the arithmetic Blocker above rather than being independent of it: the term the inequality sums is not unambiguously defined by the note's own text.
  Suggested fix: name the owner-count ratio and the wall-clock delta as two distinct, separately labelled quantities, and state explicitly which one (if either alone) the break-even-condition's own sum uses.

#### Major

(none — the strongest additional findings, on independent review, are documentation-precision gaps rather than defects that move a computed value or a conclusion's direction; see Minor/nits and the ledger below for what was downgraded from Codex's own raw severity and why.)

#### Minor / nits

- `docs/loop-engineering/phase-multiplexing.md` — the `pilot3-legitimacy: production-unit — met` claim ("12 real Agent-tool engineer instances... never a shell-process proxy") sits in the same note as an *unedited*, unqualified Limits bullet inherited from the Tier-1 pilot ("this task's own pilot exercised only shell-process-level concurrency"). Now that the note documents two pilots with genuinely different production-unit realities, that bullet's unqualified "this task"/"this pilot" wording is ambiguous enough that a reader (and Codex, on first pass) can misattribute it to Tier-3 and read it as contradicting `production-unit`. I do not find independent evidence that T-1078's own units were in fact shell processes — the claim is consistent with the task's designed execution-actor split (only the orchestrating session can launch real Agent-tool instances) and was read and accepted by QA against the evidence region. Recommend adding a tier-qualifier to the inherited Limits bullet.
- `docs/loop-engineering/phase-multiplexing.md` — the one disclosed, representative `- command: git worktree add "$SCRATCH/clone/owner-a" -b owner-a-work` line (D2/AC10) relies on an explanatory parenthetical ("run once per owner, inside `$SCRATCH/clone`") rather than on the command's own syntax (`git -C "$SCRATCH/clone" worktree add ...` or an explicit `cd`) to keep it confined to the clone. Provenance decision 4 already marks this line as "representative form," not a transcript meant for execution, and QA's own Adversarial fixture checklist ruled it out of the "canonical runnable procedure" class for the same reason — so this is a defense-in-depth documentation improvement, not a safety violation that occurred during the actual pilot (the `pilot3-safety: worktree-add-clone-only` claim is evidenced by the `venue` line in the frozen evidence region, not by this illustrative command).
- `docs/loop-engineering/phase-multiplexing.md` — the "No experiment was run for this tier" opening sentence and the "Multi-worktree integration sketch" paragraph (rebase-shaped, unbuilt-reconcile framing) survive unedited directly under the just-renamed "(evaluated — T-1078 pilot)" heading, now literally false and in visible tension with the pilot content immediately above and the corrected `reconcile-cost` line immediately below. Provenance decision 2 records this as a deliberate scope choice (Notes for engineer's own narrow "the four lines you rewrite in place, and nothing else"). Per the operator-ratified Verdict-scope ruling this is a stale narrative aside — it does not touch a computed value or any of the four gated conclusion elements (the two prices, the corrected inequality's terms, the saving's confidence word, the recommendation) — so it is a note, not a Blocker/Major, but it is genuine and worth a documentation follow-up.
- `docs/loop-engineering/phase-multiplexing.md` — the frozen `pilot3-evidence` region's own `- telemetry: 18 span rows` claim does not match the real runs corpus: independently verified against `.shell-team/runs/shell-team.jsonl` on this host, the `t1078-20260816T190505Z` run id carries **19** span rows and **5** event rows (24 records total), of which only **4** are `phase:"reconcile"` spans. This is a factual inaccuracy inside the D8 byte-frozen evidence region (not editable by engineer or QA without breaking AC11's byte-identity requirement), off by one on a summary count no acceptance criterion checks, and it does not touch any of the four gated conclusion elements — recorded as a disclosed inaccuracy rather than a blocking finding.
- `docs/loop-engineering/phase-multiplexing.md` — the "roughly 31–39 s... every arm... per owner" launch-plus-notification overhead framing is only actually derived (shown as a computed residual) for the serial arm's own two reps; the parallel and rework arms' analogous residuals are not shown, and a back-of-envelope check (arm wall-clock minus longest instance runtime) gives noticeably larger residuals for those arms (consistent with their residuals also folding in landing time and multi-instance notification handling, but not shown as such). A disclosure-precision gap in supporting analysis prose, not a demonstrated contradiction; the recommendation's direction is unaffected either way.
- `docs/loop-engineering/phase-multiplexing.md` — the corrected `- break-even: reconcile-cost — measured —` line's "sub-second every time" claim is scoped to the landing helper's own composing/commit step (458–490 ms), not to the full gate-policy cost (mechanical verification per landing, the two agent gates once at the final composed tip) the same line also describes in prose. AC3 only requires stating that gate-policy design where the parameter is priced, not separately measuring its cost, so this is correctly scoped rather than a defect — noted for completeness since Codex's adversarial pass raised it.

#### Adversarial findings

Devil's-advocate pass on `docs/loop-engineering/phase-multiplexing.md`'s Tier-3 pilot section and break-even-condition line, framed to specifically hunt for a legitimate reconciling reading of the arithmetic Blocker above. Result: **no legitimate reconciling reading found** — confirmed the contradiction stands under every textually-supportable definition tried. Additional findings from the same pass: the rework-amplification unit mismatch (folded into the Blockers above), a partition-overhead/rework-amplification potential double-count (the 4 rework landings' own wall clock is summed once inside `partition-overhead`'s serialization half and again embedded inside the rework arms' own wall clocks that `rework-amplification`'s delta is derived from — small in absolute magnitude, sub-second scale, but a genuine methodological imprecision contributing to why the corrected inequality's own arithmetic cannot be trusted at face value), and the evidence-region span-count and launch-overhead-scope items listed under Minor/nits above.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `pilot3-legitimacy: production-unit` vs. Tier-1-era shell-process-only Limits bullet | blocker (Codex-native P1) | minor | Ambiguous cross-reference to an unedited, unqualified Limits bullet inherited from Tier-1; no independent evidence T-1078's own units were shell processes — consistent with the task's designed execution-actor split and unchallenged by QA's own evidence-region reading. |
| 2 | Representative `git worktree add` command not self-contained-safe (`-C`/`cd` missing) | blocker (Codex-native P1) | minor | Explicitly disclosed as a "representative form" illustrative line (provenance decision 4), not a runnable transcript; QA's own Adversarial checklist already ruled this line out of the "canonical runnable procedure" class. |
| 3 | Run the full 68-file board-reader blast-radius sweep at both refs | blocker (Codex-native P1) | rejected | Directly priced down and disclosed by this task's own operator-ratified `- verification-class: no-mechanism` declaration and the Blast radius section's own stated representative-scope method (additive-diff property + two universal checkers + two full-suite age-extreme samples); this is the declared price, not an unstated one. |
| 4 | Stale "No experiment was run for this tier" / rebase-sketch paragraph under the renamed heading | major (Codex-native P2) / major (adversarial) | minor | Genuine and confirmed, but per the operator-ratified Verdict-scope ruling a stale narrative aside that touches none of the four gated conclusion elements is connective prose, not verdict-gating. |
| 5 | Evidence region's `- telemetry: 18 span rows` claim vs. the real runs corpus | major (adversarial) | minor | Independently verified against `.shell-team/runs/shell-team.jsonl` (19 span + 5 event rows, 4 reconcile-phase spans) and confirmed off by one — but it sits inside the D8 byte-frozen evidence region (uneditable without breaking AC11) and no acceptance criterion checks this count; touches none of the four gated conclusion elements. |
| 6 | "31–39 s... every arm... per owner" launch-overhead framing only derived for the serial arm | moderate (adversarial, rounded up to major per the conservative label-mapping rule before synthesis) | minor | A disclosure-precision gap in supporting analysis prose, not a demonstrated contradiction of a gated value; the recommendation's direction is unaffected. |
| 7 | `reconcile-cost` "sub-second" claim scoped to the landing step, not the full gate-policy cost | major (adversarial) | minor | AC3 only requires stating the shipped gate-policy design where the parameter is priced, not separately measuring the two-agent-gate cost; the claim is correctly scoped as written. |
| 8 | `rework-amplification` defined as an owner-count ratio in D5 but used as a ms wall-clock delta in the break-even-condition line | major (adversarial) | blocker | Upgraded: this is not an independent documentation nit but a second facet of the same D6 defect — the break-even-condition's own definition of one of its two named terms is internally inconsistent with D5's own definition of that term, which is exactly the "corrected inequality's terms" the operator-ratified Verdict scope names as gated. |

#### Recommendation

- REQUEST_CHANGES → set back to READY_FOR_ENG with the two Blockers above (the break-even-condition's own arithmetic and its `rework-amplification` term's unit inconsistency with D5) as the linked findings. Everything else in this round is disclosed as a note or explicitly rejected per the ledger above and does not, on its own, require a rework round.

#### Fast-follow disposition

- Ambiguous Tier-1-era Limits bullet vs. `production-unit — met` (ledger #1) — file-an-issue: add a tier-qualifier so the shared Limits section disambiguates which pilot's shell-process-only limitation it describes, now that two pilots with different production-unit realities coexist in one note.
- Representative `git worktree add` command not self-contained-safe (ledger #2) — file-an-issue: adopt `-C`/`cd`-scoped form for any future illustrative worktree command in this note, as a defense-in-depth documentation convention.
- Stale "No experiment was run for this tier" / rebase-sketch paragraph (ledger #4) — file-an-issue: a prose-only cleanup pass over the two paragraphs immediately below the renamed heading, out of this round's own enumerated scope (provenance decision 2) and not gated by any acceptance criterion.
- Evidence-region `18 span rows` vs. actual 19 span/5 event rows (ledger #5) — file-an-issue: note the discrepancy for whoever next authors a `pilot3-evidence`-shaped region, since the region itself cannot be corrected without breaking AC11's byte-freeze; no action against this shipped record is required.
- Launch-overhead framing only derived for the serial arm (ledger #6) — file-an-issue: a follow-up pass computing and disclosing the analogous per-arm residual for `parallel-n2` and the rework arms explicitly, rather than generalizing the serial-arm figure to "every arm."

<!-- END review: T-1078-round2 -->
