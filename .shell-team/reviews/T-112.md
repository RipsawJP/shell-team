# T-112 QA verification record

- Verdict: PASS
- Commits reviewed: `fdfd5f9` (commit-identity gate + ignore lock),
  `6e10a08` (own follow-up fix, `tests/check-handoff/run.sh`'s bare-`mktemp`
  portability class). Branch HEAD at verification time: `3b4fa55`.
- Board: T-112 → READY_FOR_REVIEW
- Coordination note: T-111 and T-112 share one branch and their deliverables
  interlock (T-112's AC17 runs T-111's checker; both edit the same shellcheck
  argument line, `docs/pii-controls.md`/`.ja.md`, and
  `.github/workflows/check-handoff.yml`), so both were verified in one QA
  pass — this is T-112's own, separate record and verdict; T-111's is in
  `.shell-team/reviews/T-111.md`.

## Structural gates

- `bash bin/check-acs.sh .shell-team/specs/T-112-commit-identity-and-ignore-lock.md`
  → **25 passed, 0 failed, 0 unrecognized** (exit 0). Dry-run first (0
  unrecognized), then live.
- `bash bin/check-intent.sh .shell-team/specs/T-112-commit-identity-and-ignore-lock.md
  .shell-team/todo.md` → `aligned: T-112 v2 (900861c5081bd23899ab68e21b830d7c0d43005b)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-112.md` → `conformant
  (4 decision entries, 0 sentinel)`. Read all 4 against the implementation:
  `--base` as a mandatory argument rather than a default-resolution chain
  (grounded in DP-1's range definition never describing a default chain, and
  CI always passing `--base` explicitly); the allow-list mutation technique for
  AC12 (neutralising the *allowed* regexes rather than a deny-list finding
  regex, since this checker is allow-list-based, unlike T-111's); AC13's
  fixture reuse (the NEG fixture already *is* the "positive with a conformant
  identity" object AC13 describes, so no second fixture pair is needed); and
  AC19's legacy-layout coverage being a resolver-correctness check rather than
  a semantic ignore-lock duplicate (the raw-dump patterns are a self-hosting
  addition to this repo's own root `.gitignore`, not part of the shared
  `templates/shell-team.gitignore`). All 4 are genuinely non-trivial and each
  is grounded in a specific spec line or a checked file, not restated-obvious.

## Fixture suites

- `bash tests/check-commit-identity/run.sh` → all assertions passed (exit 0).
- `bash tests/gitignore-raw-dumps/run.sh` → all assertions passed (exit 0),
  including the resolver-derivation checks (default layout →
  `.shell-team/reviews`, a legacy-layout fixture → `tasks/reviews`), the three
  ignore-behavior locks (raw dumps ignored, `.codex-review.json` ignored,
  curated `.md` notes NOT ignored), and the hostile-`core.excludesFile`
  control (proving the three locks above are not vacuously true because the
  hostile file was silently ineffective) — the same three-check idiom
  `tests/rollup-track/run.sh` established, applied to the reviews dir.

## Probe the mechanism empirically, in throwaway git repos (not taking the suite's word for it)

Built each scenario by hand, capturing the base SHA **before** adding the
probe commit (matching the suite's own convention, and the same discipline
T-111's round required):

| # | Scenario | Result |
|---|---|---|
| (a) | A non-merge (branch) commit whose author is a mailbox-shaped identity at a real, non-noreply domain | `FINDING ... side=author-identity` **and** `side=committer-identity` (committer inherited the same non-conformant address in my fixture), rc=1 |
| (b) | A conformant `<id>+<login>@users.noreply.github.com` identity on both author and committer | clean, rc=0 |
| (c) | A merge commit whose author is a real-domain mailbox and whose committer is a conformant noreply identity | clean, rc=0 — merge commits are excluded from the range (`--no-merges`) wholesale, as DP-1 requires |
| (d1) | A non-merge commit: committer = plain web-flow `noreply@github.com`, author = conformant `+login` form | clean, rc=0 — the web-flow identity is allowed on the committer side |
| (d2) | The same shapes swapped: author = plain web-flow `noreply@github.com`, committer = conformant `+login` form | `FINDING ... side=author-identity`, rc=1 — the web-flow allowance is committer-side only, exactly as AC8/AC9 require |
| (e) | A suffix-confusable domain (`evil` prefix directly concatenated onto the noreply domain, no separator) on both sides | `FINDING` on both sides, rc=1 — anchoring holds; a substring test would have missed this |
| (f) | An empty range (`HEAD` == the merge-base, no commits to judge) | clean, rc=0 — "nothing to judge is clean, not an error" (AC11) |
| (g) | An unresolvable `--base` ref | `structural: unresolvable base ref: ...`, exit 2 |
| (h) | A finding-producing commit with a unique marker in the local part of its (rejected) identity | Finding output names only `commit=<sha> side=author-identity` / `side=committer-identity` — the marker string never appears in stdout or stderr |

All eight probes behave exactly as DP-1/DP-2/DP-3 describe. No echoed
identity value anywhere in any of the eight runs' output.

## Mutation-prove the identity pattern and the fixture-side vacuity guard, independently

Working from a fresh scratch copy under `mktemp -d`, never touching the
checkout:

- **Detector side**: neutralised both allowed-identity regexes
  (`NOREPLY_ID_RE`, `NOREPLY_PLAIN_RE`, rewritten to a non-matching
  placeholder) in a throwaway copy, then re-ran a conformant `+login` commit
  (built independently, not a suite fixture) against it: the real checker
  reports clean (rc=0); the mutated copy reports findings on **both** sides
  (rc=1) — the allow-list rule is load-bearing for detection on both the
  author and committer paths at once, as expected for an allow-list-based
  detector.
- **Fixture side (suite's own meta-assertion, AC13)**: confirmed by reading
  `tests/check-commit-identity/run.sh`'s "meta: neutralised positive fixture
  makes the assertion FAIL" section — it reuses each side's own NEG
  (conformant) repo as the "neutralised positive" object per the provenance's
  documented reasoning, and the suite run above shows this assertion passing.

## Self-application (AC16)

`bash bin/check-commit-identity.sh --base develop` → `clean (no non-conformant
commit identity found in range)`, exit 0 — every one of this branch's own
commits carries the conformant noreply identity on both sides.

## The gitignore lock (AC18/AC19), read and independently probed

- `git -C <repo> -c core.excludesFile=<hostile> check-ignore -q
  .shell-team/reviews/dummy.{txt,json,jsonl}` → ignored (all three extensions);
  `.codex-review.json` → ignored; `.shell-team/reviews/T-100.md` → **not**
  ignored (curated notes stay tracked) — reproduced by hand outside the
  suite, same results.
- The control repo (no `!.shell-team/` re-include) confirms the hostile
  `core.excludesFile` genuinely has teeth: `.shell-team/reviews/dummy.md` is
  ignored there, so the three locks above are not vacuously true.
- `bin/team-paths.sh --root <this repo> --get reviews` →
  `.shell-team/reviews`; `bin/team-paths.sh --root <legacy fixture> --get
  reviews` → `tasks/reviews` — the reviews dir is never hardcoded.
- `git diff --quiet develop -- .gitignore .shell-team/.gitignore` → exit 0,
  both files byte-unchanged against the base branch.

## Docs

- T-112's two canonical lines (the merge-commit exclusion statement, and the
  forward-looking/operator-side-remediation statement) present verbatim, in
  both languages, in `docs/pii-controls.md` / `docs/pii-controls.ja.md` —
  confirmed as part of T-111's live AC18/AC19/AC21 runs (the files are shared;
  T-112's own AC21 re-asserts the same two lines independently).
- T-111's "shapes only" sentence still present after T-112's edits; no
  badge-shaped completeness claim anywhere in the docs pair or either README
  (AC22, confirmed live).

## CI wiring

- `bin/check-commit-identity.sh`, `tests/check-commit-identity/run.sh` and
  `tests/gitignore-raw-dumps/run.sh` are all three present in the single
  shellcheck argument list; ran the exact CI argument list (all ~34
  scripts, shellcheck 0.11.0 pinned) myself → exit 0, no warnings.
- `Run check-commit-identity fixture suite`, `Run gitignore-raw-dumps lock
  suite`, and `check-commit-identity on the PR commits` (the required,
  `--base`-driven step) all present and correctly ordered in
  `.github/workflows/check-handoff.yml`.

## PII hygiene

Shared finding with T-111's round (same branch, same diff): `bin/check-pii-shapes.sh
--base develop` is clean, all 15 commits on the branch carry the conformant
noreply identity on both author and committer, and no PII appears in commit
messages (`git log develop..HEAD --format='%B' | grep -iE
'sawamatsu|marketenterprise|@gmail|@yahoo|/Users/[a-z]+/'` → no matches).

## Regression

All green, independently re-run: `bash tests/rollup-track/run.sh`,
`bash tests/check-handoff/run.sh` (confirms the mktemp-class fix landed at
`6e10a08` now passes — the 12 bare `mktemp` call sites converted to explicit
`${TMPDIR:-/tmp}` templates), `bash bin/check-prompt-sync.sh`,
`bash tests/errexit-safe/run.sh`, `bash bin/check-board-headings.sh
.shell-team/todo.md`, `bash bin/check-handoff.sh .shell-team/todo.md`,
`bash tests/team-paths/run.sh`.

## Edge cases tried beyond the assigned list

- A commit whose author and committer email differ in case only (not
  explicitly probed as a separate scenario — the allowed-form regexes are not
  anchored case-insensitively, and neither GitHub's own noreply domain nor
  this project's own commit history produces a mixed-case variant; this is a
  synthetic extreme, not a reachable input per the spec's Input space, so it
  is not grounds for rework).
- Verified `git log -1 --format='%ae'`/`%ce` on the merge commit built for
  probe (c) actually reads the shapes I intended (a real-domain author, a
  noreply committer) before trusting the "excluded" result — confirmed by
  direct `git log` inspection, not assumed from the merge command's arguments
  alone.

## Scope note

T-111 is verified separately in `.shell-team/reviews/T-111.md` (same round,
same branch). T-113 remains `READY_FOR_ARCH` and was not verified or flagged
as missing — out of scope for this pass.

---

## Codex Reviewer（クロスプロバイダー）— T-112

### Codex Review verdict: APPROVE
- Task: T-112
- Codex model: gpt-5-codex（codex-cli 0.144.6）。`codex exec --sandbox read-only review --base develop --json`（primary）。今回のラウンドは T-111 と同じブランチ・同じ primary パスで一括レビューしたが、T-112 固有の blocker/major は検出されなかったため、devil's-advocate の adversarial パスは（本ロールの手順どおり）起動していない——step 5 の起動条件は「そのタスク自身の blocker/major」であり、T-111 側の blocker（`bin/check-pii-shapes.sh` の別ファイル）を理由に T-112 の adversarial パスを追加することはしていない。
- Verification mode: static-only（read-only sandbox での静的レビュー。`bin/check-commit-identity.sh` の許可済み識別子パターン `NOREPLY_ID_RE` / `NOREPLY_PLAIN_RE` が T-111 側で見つかったサブドメイン過剰除外と同種の穴を持つか自分で正規表現を抽出して確認したが、これらは両端アンカー `^...$` の全文一致であり——T-111の `RE_NOREPLY_DOMAIN` のようにドメイン部分だけを `(^|\.)` で緩めていない——サブドメイン/接頭辞トリックは成立しないことを確認した）
- Diff scope: `bin/check-commit-identity.sh`（169行）、`tests/check-commit-identity/run.sh`（441行）、`tests/gitignore-raw-dumps/run.sh`（145行）、`docs/pii-controls.md` / `.ja.md` への追記2行、`.shell-team/provenance/T-112.md`、`.github/workflows/check-handoff.yml` の該当箇所。

#### Blockers
なし。

#### Major
なし。commit range の導出（`merge-base` + `--no-merges`）、両端アンカーされた許可パターン、merge-commit / web-flow の disposition、no-leak、gitignore lock の control-repo による非空証明はいずれも QA round の実証記録と私自身の静的読解で一致しており、T-111 で見つかったクラスの穴（ドメインの部分アンカリング、diff-text解析への依存)はこのチェッカーには存在しない。

#### Minor / nits
- **`tests/check-commit-identity/run.sh:96-99, 211, 227`（`new_repo()` / `NONGIT` / `RANGE_REPO`)** — これらはいずれも `mktemp -d "${TMPDIR:-/tmp}/check-commit-identity-*.XXXXXX"` で **`$WORK` の兄弟として** 使い捨てリポジトリを作成しているが、`cleanup() { rm -rf "$WORK"; }` の EXIT trap は `$WORK` しか削除しない。1回のローカル実行で作成される約14個の使い捨てリポジトリがすべて `$TMPDIR` 直下に残留する。これは T-111 の v4 再実装（`tests/check-pii-shapes/run.sh`）がまさに Codex round 1 の指摘を受けて修正した「すべての使い捨てリポジトリを `$WORK` の中に作る」という同一の教訓の、T-112 側での再発である（Codex primary パスが `[P3]` として検出、実機で確認済み: 各 `new_repo` 呼び出しのパスが `$WORK` 配下ではないことをソース上確認）。CIランナーではジョブ終了時に消えるため実害はないが、ローカルで繰り返し実行すると蓄積する。

#### Adversarial findings
実行していない（上記の起動条件説明のとおり、T-112 自体の blocker/major が無いため）。

#### Synthesis audit ledger
no severity changes

#### Recommendation
- APPROVE → `.shell-team/todo.md` の T-112 を READY_FOR_MERGE に。ただし T-111 が REQUEST_CHANGES で READY_FOR_ENG に戻るため、実運用としては T-111 の再修正後に AC17（「T-111 のチェッカーがこのブランチの diff で引き続き exit 0 であること」)を再確認してからのマージが安全——T-111 の Blocker 修正（`RE_NOREPLY_DOMAIN` のアンカリング）が T-112 の diff や自己適用結果を変える可能性は低いと判断するが(T-112 自身のファイルにサブドメイン形状の noreply アドレスは存在しない)、機械的な再確認はT-111再検証時にまとめて行うのが合理的。

#### Fast-follow disposition
- 使い捨てリポジトリが `$WORK` の外（`$TMPDIR` 直下）に作成され蓄積する件（Minor） — file-an-issue（`new_repo()` / `NONGIT` / `RANGE_REPO` の生成先を `$WORK` 配下に変えるだけの軽微な修正。T-111 v4 の `tests/check-pii-shapes/run.sh` にすでに存在する同等の実装をそのまま踏襲できる）
