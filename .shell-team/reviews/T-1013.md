# QA verification — T-1013

Branch `feature/77-part3-docs-wiring`, HEAD `460d626` (base `develop` @
`5cc9049`). Spec: `.shell-team/specs/T-1013-loop-replay-docs-wiring.md`
(intent v1, aligned, 15 ACs — 13 mechanical, AC14/AC15 SKIP by design).
Working tree kept clean throughout (`git status --short` shows nothing
beyond this review file and the board edit); all mutation testing ran on
file copies under `$TMPDIR`, never on the real checkout. No real filesystem
path is quoted anywhere in this file — `$TMPDIR`/`/tmp/...` placeholders
only, matching the shapes the shipped README/spec themselves already use.

## 1. Mechanical acceptance criteria

```
$ bash bin/check-acs.sh --dry-run .shell-team/specs/T-1013-loop-replay-docs-wiring.md
15 recognized, 0 unrecognized

$ bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md   (BEFORE creating this file)
check-acs: 12 passed, 1 failed, 2 skipped, 0 unrecognized
check-acs: FAILED: AC12
```
Confirmed the pre-state matched the expected shape exactly: AC1–AC11, AC13
all PASS; AC12 FAIL solely because `.shell-team/reviews/T-1013.md` did not
exist yet; AC14/AC15 SKIP by design.

AC12's own sub-components verified individually before creating this file:
```
$ bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"        -> rc=0
$ bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base develop -> rc=0
$ bash bin/check-pii-shapes.sh --base develop                             -> "clean (no PII-shaped bytes found)", rc=0
```

After creating this file:
```
$ bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md   (AFTER)
check-acs: 13 passed, 0 failed, 2 skipped, 0 unrecognized (AC14/AC15 SKIP)
```

## 2. Full CI-ordered local run (`.github/workflows/check-handoff.yml`)

Ran every step in the workflow's own order, serially. All 55 steps passed
(0 failures). `shellcheck --version` on this host = 0.11.0, matching the
workflow's pin; `develop`/`origin/develop` identical locally.

Special attention to `bin/check-readme-version.sh`, per the coordinator's
ask:
```
$ bash tests/check-readme-version/run.sh   -> PASS (rc=0)
$ bash bin/check-readme-version.sh README.md README.ja.md   -> PASS (rc=0)
```
The badge is untouched in both READMEs, confirmed.

## 3. Mutation self-checks — independently reproduced

Both on file copies under `$TMPDIR` (never `git checkout`; real checkout
confirmed clean throughout).

**Mutation #1 — delete `--event` from `README.ja.md`'s Telemetry bullet
only.** Removed the token from the copy's bullet line, keeping everything
else byte-identical.
```
grep -F -- 'bin/check-run.sh' <mutated README.ja.md> | grep -qF -- '--event'  -> NO MATCH (AC1's own assertion goes red)
```
Re-ran AC6's own projection logic (sorted anchor-hit lists, `cmp`) against
the mutated pair:
```
cmp <en.sorted> <ja.sorted>  -> differ at line 1: "--event" (EN has it, JA does not)
```
Confirms the exact predicted double failure (AC1 red **and** AC6 red — the
projection asymmetry), matching the engineer's own mutation-table claim
precisely.

**Mutation #4 — replace `loop_id=goal` with `loop_id=notgoal` in the
`goal-loop` telemetry row, keeping the two new anchors.** On a copy of
`docs/loop-engineering/goal-loop.md`:
```
count of 'bin/log-run.sh' on the row: 1 (unchanged)
'/shell-team:run' present: yes
'bin/gen-loop-replay.sh' present: yes
'loop_id=goal' present: NO (mutated to loop_id=notgoal)
```
Confirms AC2's preservation control actually bites — the criterion reads
the telemetry row itself, not merely a line that happens to mention the
generator, exactly as the engineer's mutation-table entry #4 claims.

Mutations #2, #3, #5, #6, #7 and the blind-spot attacks were not
independently re-run this round (2 of 7 required); time was spent instead
on the AC15 human-read items below, which the coordinator named as the
core of QA for a docs task.

## 4. AC15 — the human-read items (read, not grepped)

### (1) EN/JA semantic fidelity

Read all six changed passages side by side, sentence by sentence:

- **README `## Replaying a run` / `## run のリプレイ`** — all four
  paragraphs (what it is / how to generate / the caveat / the demo) carry
  the same claims in the same order in both languages; nothing is added,
  dropped, or softened/strengthened across the language boundary. Every
  machine token, path and flag (`gen-loop-replay.sh`, `<run-id>`,
  `<runs>/replay-<run-id>.html`, `bin/team-paths.sh --get runs`,
  `git-ignore`, `--out`, `READY_FOR_ARCH`…`READY_FOR_MERGE`, `--label`,
  `handoff`, `skills/run/SKILL.md`, the fixture id and directory) appears
  verbatim in English inside the Japanese section — confirmed no token was
  translated, matching T-1010's operator/machine-token boundary.
- **`goal-loop` telemetry row** — EN ("one span per sub-agent call
  (`loop_id=goal`); event rows come from `/shell-team:run`, not from the
  goal loop itself; `bin/gen-loop-replay.sh` reads either kind back as a
  replay page") and JA ("サブエージェント呼び出しごとに 1 span
  (`loop_id=goal`)。event 行は goal loop 自身ではなく `/shell-team:run`
  から来る。`bin/gen-loop-replay.sh` がどちらの行種別も replay ページとして
  読み戻す") say the identical thing in the identical order — a faithful
  translation, not a paraphrase that drifts.
- **`workflow.md` / `.ja.md` per-cycle-artifact note** — both extend the
  existing paragraph with the same three claims (operator `label` text
  verbatim in the page; the page's UI dictionary carries both languages;
  machine tokens — the rail's five stops, the verdict labels — stay
  verbatim English; the shipped template itself is English shipped
  surface) in the same order, in both languages.

**No semantic drift found** in any of the three sites — this is a
straightforward, accurate finding to report (not every review turns one
up, and this one did not).

### (2) Rendered-markdown sanity

- **Tree figure alignment**: measured the `#` comment column across the
  `bin/` block in both READMEs (`awk`'s `index($0,"#")` on lines
  134–142). Every comment in both files lands at column 46 (the parent
  `bin/` line itself at column 44, consistent with its different
  indentation depth) — the new `gen-loop-replay.sh` line's comment aligns
  exactly with its siblings in both languages. Alignment is not broken.
- **Fenced blocks**: both new sections' two ```bash fences are balanced,
  properly closed, and render as code blocks under a raw read; no stray
  backtick or unterminated fence.
- **Heading placement**: confirmed by direct line-number read that
  `## Replaying a run` / `## run のリプレイ` land between `## Operating
  loop` / `## 運用ループ` and `## Design choices` / `## 設計上の選択` in
  both files, exactly as D2 requires.
- **Table rows**: both `goal-loop` telemetry rows render as single,
  correctly pipe-delimited table lines (confirmed by reading the
  surrounding table structure), not a broken or wrapped row.

### (3) Factual accuracy against the shipped code

- **Usage line**: `bash bin/gen-loop-replay.sh --help` confirms the real
  CLI is `gen-loop-replay.sh <run-id> [--runs-dir DIR] [--out PATH]
  [--template PATH]` — matches the README's documented usage and demo
  flags exactly.
- **Output path / git-ignore claim**: confirmed `bin/team-paths.sh --get
  runs` resolves a real directory and `git check-ignore` reports the
  `replay-<run-id>.html` shape as ignored under it, with zero new ignore
  patterns (`runs/` already present in both `.gitignore` files) — matches
  D4 exactly.
- **`goal-loop` closure**: confirmed `skills/goal/SKILL.md` invokes
  `log-run.sh` with `--span` only, no `--event` instruction anywhere in the
  file — the row's "spans only, event rows come from `/shell-team:run`"
  claim is true at this base ref.
- **Rail caveat vs. `flagFromLabel`/`SKILL.md:110`**: `flagFromLabel`
  performs exactly the byte-exact `FLAG_STOPS.indexOf` match the caveat
  describes, and `SKILL.md:110` documents exactly the `handoff`-event,
  bare-token, `--label` convention the caveat points at. **However, found
  a genuine, subtle factual overclaim**: the README's caveat says "the rail
  **only** lights for a run whose `handoff` events carry the board flag" —
  the word "only" asserts exclusivity to `handoff` events. Per T-1014 D1,
  the derivation is uniform across **every** `kind:"event"` row, not just
  `handoff` — confirmed directly in `templates/loop-replay.html`'s
  `adaptRow` (every one of the five `case` branches derives `flag`
  uniformly) and by T-1014's own `flag-rail` fixture, whose `human`-kind
  row with a bare token derives a flag (the `rail-uniform-kind` assertion,
  independently verified during T-1014's QA round). So a `human` event
  with a bare `READY_FOR_MERGE` label *also* lights the rail, contradicting
  the word "only." **This is not a T-1013-introduced error**: D3's own
  frozen spec text uses the identical "handoff-scoped" framing
  (`.shell-team/specs/T-1013-loop-replay-docs-wiring.md` D3: "The rail
  lights only when the orchestrator put the board flag it just set onto
  the same `handoff` event's `--label`..."), which itself mirrors the same
  handoff-scoped framing I flagged as a documentation asymmetry in
  `skills/run/SKILL.md` during T-1014's QA round. The engineer correctly
  implemented what the frozen D3 said; the imprecision is inherited from
  the spec text, traceable to the same root cause across three tasks now.
  **Not FAIL-driving** (D3 is frozen; the engineer has no standing to
  rewrite it; the claim is true for the dominant, documented path and only
  overclaims exclusivity on a path the spec itself never asked to be
  documented) — flagged here for the reviewer/human as a cross-task
  pattern worth a single, coordinated fast-follow across `SKILL.md`, this
  README caveat, and T-1013's own D3 text.
- **The demo command, run verbatim** — **found a real, reproducible defect,
  external to this task's scope.** Ran the documented bare-form command
  exactly as written (`gen-loop-replay.sh 20260801T000000Z-flagrail
  --runs-dir tests/gen-loop-replay/fixtures/flag-rail --out
  /tmp/replay-demo.html`, then separately the `./bin/gen-loop-replay.sh`
  form) from this repository's root, with `bin/` confirmed on `$PATH`
  (`check-acs.sh` and other `bin/` scripts resolve via bare name in this
  same session). **Both the bare form and the `bin/`-prefixed direct form
  fail with a permission-denied shell error** — neither is what the
  README's own parenthetical claims ("the bare form above and the
  `bin/`-prefixed form both work"). Root cause, confirmed mechanically:
  `bin/gen-loop-replay.sh` is missing its executable bit —
  `ls -la bin/gen-loop-replay.sh` shows `-rw-r--r--` where every sibling
  script (`log-run.sh`, `check-acs.sh`, etc.) shows `-rwxr-xr-x`. Confirmed
  via `git ls-tree` that the blob and mode (`100644`, non-executable) are
  **identical at `develop`, at this task's own base ref (`5cc9049`), and at
  HEAD** — this predates T-1013 entirely (inherited from T-1012 or its
  T-1014 rework) and is explicitly **not** a T-1013 regression; `AC11`'s own
  byte-identity lock on `bin/` (confirmed PASS) is consistent with this,
  since it only detects content diffs, not that a pre-existing mode defect
  was carried forward unnoticed. Also confirmed this is part of a **wider
  pattern**, not unique to this one file: `bin/check-commit-identity.sh`
  and `bin/check-pii-shapes.sh` are the only other two `bin/*.sh` files
  sharing the same non-executable mode at `develop` — three files total,
  all pre-existing. **Why no mechanical check caught this**: every single
  `run:` step in `.github/workflows/check-handoff.yml` that invokes a
  script does so with an explicit `bash <script>` prefix (confirmed: zero
  bare/`./`-prefixed invocations in the whole workflow file), and AC8's own
  `check:` line does the same (`bash bin/gen-loop-replay.sh ...`) — neither
  the CI list nor any mechanical criterion ever exercises the exact
  invocation shape the README's own prose promises. Ran the demo again
  through the working (`bash`-prefixed) form to close out the "does the
  demo actually work" half of the ask: exit 0, page written, non-empty,
  carrying the one data block — then deleted the output. **Disposition**:
  this is squarely a `bin/` defect (a missing `chmod +x`, not an interface
  mismatch), which T-1013's own Non-goals forbid fixing here ("If the
  documented interface turns out to be wrong, that is a `BLOCKED` report
  and a scope question — not a fix to make here"). **Not FAIL-driving for
  T-1013** on that basis — the documentation itself is accurate about the
  *interface* (flags, exit codes, output path) and the underlying claim
  will become true the moment the exec bit is restored elsewhere — but this
  is a real, high-severity, pre-release finding that needs **immediate
  human attention**, separate from this task's own merge decision: right
  now, a reader who pastes the README's own bare-form command verbatim
  gets a permission-denied error, and this is the last feature task before
  v1.2.0. Recommend an urgent fast-follow issue to restore the executable
  bit on all three affected `bin/` files (a one-line `chmod +x` each, no
  code change) before or immediately alongside this release.

## 5. Engineer's disclosed items — evaluated

- **AC4/AC5 awk fenced-block blind spot — confirmed dormant.** Ran the
  same slice-extraction `awk` command the criteria use against both new
  sections and grepped the extracted slice for any line starting with
  `## `: none found in either language. The blind spot is real in the
  abstract (a future edit adding a fenced block whose content starts a line
  with `## ` would truncate the slice early) but is not triggered by the
  content actually shipped.
- **AC6/AC3 redundancy note — confirmed accurate.** `gen-loop-replay.sh`
  appears four times in `README.md` (the tree line, the Telemetry bullet,
  the usage example, the demo command). A mutation removing only the tree
  line's mention would leave the substring present elsewhere in the file,
  so AC6's whole-file `grep -qF` would still pass — confirmed AC6 cannot
  independently catch that specific removal. AC3's own `awk` condition
  (a line containing **both** `gen-loop-replay.sh` **and** `──`) is the
  only one of the two that would actually drop to zero matches, since none
  of the other three occurrences sit on a box-drawing tree line. The
  engineer's claim that AC3, not AC6, is the real backstop for the tree
  line is correct.

## 6. Frozen intent + seam gates

```
$ bash bin/check-intent.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md .shell-team/todo.md
check-intent: aligned: T-1013 v1 (7a455b4d6ae35d9cb662271b92339bd0e6d46967) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1013.md
check-provenance: conformant: .shell-team/provenance/T-1013.md (1 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1013 .shell-team/interventions/T-1013.md
check-interventions: conformant: .shell-team/interventions/T-1013.md (0 entries, 1 sentinel) -> rc=0
```

Read the one provenance decision (the `workflow.md` paragraph-extension
call): confirmed directly against the actual file diff — both
`docs/workflow.md` and `.ja.md` extend the existing `**Per-cycle
artifacts**` paragraph with new lines appended directly after the prior
last line, no blank line inserted (so it renders as one markdown paragraph
block, not two), and the deletion count against the base ref is 1 in each
file (well inside AC10's cap of 5) — matches the decision's own claim
exactly.

## 7. Regressions / adjacent code paths

AC11's byte-for-byte diff-against-base check covers all of `bin/`,
`templates/`, `skills/`, `tests/`, `.github/`, `agents/`, both CHANGELOGs,
the plugin manifest directory, every `docs/` file outside the four in
scope, and T-1011's/T-1012's/T-1014's frozen specs — all unchanged,
confirmed by the live `check:` run, with the positive control (four named
paths actually exist) also passing.

## 8. Edge cases tried beyond the spec's own fixtures

- Ran the documented demo command through **both** of its claimed
  invocation forms (bare, `bin/`-prefixed direct) as well as the
  CI/AC8-tested `bash`-prefixed form — this is what surfaced the exec-bit
  finding in §4(3). This stays inside the spec's own declared Input space
  ("The documented command as a reader would paste it... Both must be
  literally correct against the generator's real interface"), not a
  synthetic extreme.
- Checked whether the exec-bit gap was unique to one file or systemic
  (found two more affected files) and whether CI's own step list would
  ever exercise the failing invocation shape (found it does not, for any
  script) — grounding the disposition recommendation in measurement rather
  than a guess.

## Verdict

All mechanical items pass; both independently-reproduced mutations behave
exactly as the engineer's provenance/hand-off claims; the two disclosed
items (awk blind spot, AC6/AC3 redundancy) both check out. The EN/JA
semantic-fidelity and rendered-markdown reads found no defects. The
factual-accuracy read found two things worth surfacing: a minor,
spec-inherited "only" overclaim in the rail caveat (not FAIL-driving,
traced to the same root cause as a T-1014 finding), and a real,
high-severity, pre-existing `bin/` executable-bit defect that makes the
README's own bare-form demo command fail as literally documented — squarely
out of this task's fixable scope per its own Non-goals, but requiring
urgent, separate human attention before v1.2.0 given this task's position
as the last feature task before release.


---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1013
- Codex model: gpt-5.1-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 11 files changed, +667/-6 (develop `5cc9049` … HEAD `8b6dbc2`)

Codex CLI を `codex exec review --base develop --json`(フォアグラウンド同期、1回)で実行し、実ファイルを直接読ませた。指摘に P1(→blocker)が含まれたため devil's-advocate の adversarial パスも実行。加えて自分でも `bash bin/check-acs.sh`(13/13 PASS)・`bash bin/check-pii-shapes.sh --base develop`(clean)・`bash bin/check-readme-version.sh`(PASS)を実行し、QA が報告した exec-bit の欠落を `git ls-tree`/`git archive` による**クリーンな抽出**で独立に再現した(自分の作業ツリーの `bin/gen-loop-replay.sh` にはローカルの副作用で実行ビットが付いていたため、それに頼らず `git archive HEAD` で抽出したコピーで再検証——`-rw-r--r--`、直接実行で `exit 126` を確認)。EN/JA の該当6ファイルの diff も全て自分で読み、内容の正確性を照合した(`Verification mode: executed-tests`)。

#### Blockers

- **README の新セクションが示す一次コマンド(裸形式・`bin/` 接頭形式)が、この時点の `develop`/このブランチで文字どおり実行不能であり、「両方とも動く」という本文の断定が現在は事実でない(`README.md`/`README.ja.md` 両方に同一の欠陥)。** `git ls-tree develop -- bin/gen-loop-replay.sh` は `100644`(非実行)を報告し、`git archive HEAD` で抽出したクリーンなコピーで直接実行すると `exit 126`(permission denied)を再現した——QA の発見と完全に一致し、独立して裏取り済み。原因は `bin/` 側の pre-existing なモードビット欠落(T-1012/T-1014 由来、T-1013 の回帰ではない、修正 PR `fix/86-bin-exec-bits`(issue #86)が既に別ブランチで進行中、develop にはまだ未マージ)であり、`bin/` 側を直接直すことは T-1013 のスコープ外(AC11 のバイト同一ロックに抵触する)。**しかし、README 側の文言修正はスコープ内かつ低コストで、マージ順序という不確実な要因に依存せずにこの問題を無条件に解消できる**——devil's-advocate パスで Codex 自身に確認させたとおり、両コマンドを `bash bin/gen-loop-replay.sh ...` 形式に変更し、「裸形式・`bin/` 接頭形式の両方が動く」という断定文を書き換える/削除するだけでよく、この変更は spec が凍結しているアンカー(AC4/AC5 が要求する部分文字列 `gen-loop-replay.sh <run-id>` 等)を一切壊さない(`bash gen-loop-replay.sh <run-id>` はこの部分文字列を含むため)。#86 のマージ順序に賭けるのではなく、この安価な自己完結した修正を今回のラウンドで行うことを推奨する。この指摘は EN/JA 両方の README に対称的に存在する(D7 の対称性上、両方に同じ修正が要る)。

#### Major

なし。Codex 一次パスは「rail が `handoff` イベントでのみ点灯する」という caveat の "only" 表現の過大主張を P2(→既定マッピングでは major)としたが、devil's-advocate と QA・私自身の独立読解を突き合わせて Minor に格下げする(理由は Synthesis audit ledger 参照)。

#### Minor / nits

- **README の caveat が「rail は `handoff` イベントでのみ点灯する」("only" という排他性の主張)と書いているが、`templates/loop-replay.html` の `flagFromLabel()` は5つの event kind すべてに一様に適用され、`human` イベントの bare token でも実際に点灯する(T-1014 のコミット済み `flag-rail` フィクスチャの `rail-uniform-kind` で実行ベースで証明済み)。** QA も Codex 一次パスも同一箇所を独立に指摘し、収束している。ただしこの不正確な "handoff のみ" という言い回しは T-1013 の engineer が新規に書いたものではなく、**この spec自身の凍結された D3 本文が全く同じ表現を使っている**(`.shell-team/specs/T-1013-loop-replay-docs-wiring.md` D3: "The rail lights only when the orchestrator put the board flag it just set onto the same `handoff` event's `--label`...")——さらに `skills/run/SKILL.md` 側の同種の非対称性は issue **#84** としてすでに T-1014 のマージゲートで起票済み(私自身が T-1014 のレビューでこの根本原因を Minor として指摘した箇所と同一クラス)。凍結された D3 の文言を engineer が独断で書き換える標準は無く(このリポジトリの慣例: 「誤りだと思う凍結領域は報告するもの、無断で書き換えるものではない」)、実害も低い(実装は正しく一様に動作しており、prose の排他性の表現のみが不正確)。**disposition: #84 を、`skills/run/SKILL.md`・この README caveat・T-1013 自身の D3 本文という3箇所すべてを束ねる「一つの協調された fast-follow」の受け皿として拡張することを推奨**(新しい別 issue は起票しない)。

#### Adversarial findings

Codex 自身に、一次パスの2指摘に対して devil's-advocate の反論を行わせた。結果: (1) exec-bit の指摘について——「#86 のマージが保証されているなら T-1013 の prose に手を入れる必要は無く、マージ順序の問題として扱うべき」としつつ、「自己完結した修正が必要なら、両コマンドを `bash bin/gen-loop-replay.sh ...` にし『両方とも動く』という断定文を書き換えるのが最も安価で正しい修正であり、凍結アンカーは一切壊れない」と結論——私はマージ順序を保証できない(read-only なレビュアーであり、`fix/86-bin-exec-bits` はまだ未マージの別ブランチと確認済み)ため、無条件に正しいこの安価な自己完結の修正を採用する判断を維持する。(2) "only" の過大主張について——「Minor が妥当、#84 を協調の受け皿とすべき」と自ら降格。(3) 追加調査: exec-bit 前提のコマンドは README.md/.ja.md の両方(デモコマンド含む)に重複して存在するが、他の4つの変更ファイルはコマンドを実行せず生成器の名前を挙げるだけなので対象外。"handoff のみ" の過大主張も両 README に重複するが、他の変更箇所(Telemetry bullet が「hand-off ごとに1 event 行」と述べつつ5種類全てを列挙する箇所)は同種の排他性主張ではなく新規指摘には値しないと確認。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | README caveat の "only ... handoff" 排他性の過大主張(D1 の一様な derivation と food matched しない) | primary: Codex P2(既定マッピングでは major)/ adversarial: Codex 自身が Minor へ降格を提案 | minor | 実装は正しく一様に動作しており(T-1014 で機械検証済み)、prose の不正確さのみ。凍結された spec の D3 本文に由来し engineer に書き換える標準がない。issue #84 という既存の協調された受け皿があり、QA の non-FAIL 判断とも一致するため major を採用しない |

#### Recommendation

- REQUEST_CHANGES → board を `READY_FOR_ENG` に戻し、上記 Blocker(README の2コマンドを `bash` 接頭形式に変更し「両方とも動く」という断定を修正、EN/JA 両方)を修正。Minor の "only" 過大主張は今回ブロックしない——#84 への協調的な追記を fast-follow として推奨。

#### Fast-follow disposition

- README caveat・`skills/run/SKILL.md`・T-1013 自身の D3 本文にまたがる "handoff のみ" 過大主張の三箇所同時修正 — file-an-issue の代わりに **既存の issue #84 への追記/scope 拡張を推奨**(新規 issue は起票しない。理由: 同一根本原因のクラスとして一つの場所で束ねて直すのが、3箇所を別々の issue で追いかけるより保守しやすい)。

---

## Round 2 — delta re-verification (2026-08-01)

Scope per the T-1010/T-1011/T-1012/T-1014 precedent: the rework delta only
(engineer `748898b`, fixing Codex round-1 REQUEST_CHANGES: 1 Blocker — the
README's runnable examples failing verbatim, the exec-bit issue this QA
round found in round 1). The Minor (the rail caveat's "only" overclaim) was
deliberately left untouched, dispositioned to issue #84 alongside
`skills/run/SKILL.md`'s matching asymmetry — confirmed below, not
re-litigated.

### 1. Mechanical acceptance criteria

```
$ bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md
check-acs: 13 passed, 0 failed, 2 skipped, 0 unrecognized (AC14/AC15 SKIP)
```
Spec unchanged this round (only `README.md`/`README.ja.md` prose and the
provenance/board/reviews records changed) — same intent v1, same 13
mechanical criteria, all green, including AC12 (fully green now that this
review file exists from round 1 — no longer gated).

### 2. The original Blocker shape — confirmed gone

Ran the README's documented demo command **exactly as now written**
(`bash bin/gen-loop-replay.sh 20260801T000000Z-flagrail --runs-dir
tests/gen-loop-replay/fixtures/flag-rail --out <path>`):
```
exit=0
page exists, non-empty
data block present (id="loop-replay-data")
```
Output deleted immediately after (never committed, real checkout confirmed
clean before and after via `git status --short`).

Confirmed **no bare-form runnable example remains** in either new section:
```
$ awk '<section-slice for "## Replaying a run">' README.md | grep -n "gen-loop-replay.sh"
7:  bash bin/gen-loop-replay.sh <run-id>
17: bash bin/gen-loop-replay.sh 20260801T000000Z-flagrail --runs-dir tests/gen-loop-replay/fixtures/flag-rail --out /tmp/replay-demo.html

$ awk '<section-slice for "## run のリプレイ">' README.ja.md | grep -n "gen-loop-replay.sh"
7:  bash bin/gen-loop-replay.sh <run-id>
17: bash bin/gen-loop-replay.sh 20260801T000000Z-flagrail --runs-dir tests/gen-loop-replay/fixtures/flag-rail --out /tmp/replay-demo.html
```
Both runnable lines in both languages now use the `bash bin/…` form; no
bare-form invocation remains anywhere in either section.

For completeness (not required, but confirms the fix is targeted at the
README and not masking a real environment change): re-confirmed the
underlying `bin/` defect is still present and unchanged on this branch —
`git ls-tree HEAD bin/gen-loop-replay.sh` still reports `100644`
(non-executable), and the bare-form invocation still fails with a
permission-denied error. This is correct and expected: the mode-bit fix is
`PR #87`'s business (`fix/86-bin-exec-bits`, issue #86), not this branch's,
and AC11 confirms `bin/` stays byte-identical to the base ref here (see
§5 below) — the README's own wording no longer depends on that fix landing
in any particular order, which is exactly the provenance decision's stated
rationale.

### 3. EN/JA symmetry on the changed prose

Both changed runnable command blocks are literally identical strings in
both files (code isn't translated) — confirmed via direct read, already
covered above.

The replaced parenthetical, read side by side:
- EN: "(the `bash bin/…` form works regardless of `PATH` or the executable
  bit — the same form the CI workflow itself always uses.)"
- JA: "（`bash bin/…` の形は `PATH` にも実行ビットにも依存せずに動く — CI
  ワークフロー自身が常に使っている形と同じ。）"

Translated back, the JA reads: "the `bash bin/…` form works without
depending on either `PATH` or the executable bit — the same form the CI
workflow itself always uses." This is a faithful, meaning-preserving
translation of the EN — same two independence claims (`PATH`, the
executable bit), same comparison to the CI workflow's own invocation
shape, same order. No semantic drift. Every technical token (`bash`,
`bin/…`, `PATH`, `CI`) stays verbatim in the Japanese sentence, matching
the machine/operator-language boundary.

`check-acs.sh`'s AC4/AC5 (slice-scoped anchors) and AC6 (the anchor
projection) all stayed green through this rewrite (confirmed in §1) —
`gen-loop-replay.sh <run-id>` is a literal substring of `bash bin/gen-loop-
replay.sh <run-id>`, so the frozen anchor survives the prefix addition in
both languages, exactly as the provenance entry claims.

### 4. PII shapes and README-version guard

```
$ bash bin/check-pii-shapes.sh --base develop
check-pii-shapes: clean (no PII-shaped bytes found)   (rc=0)

$ bash bin/check-readme-version.sh README.md README.ja.md
(rc=0)
```

### 5. AC11 byte-identity re-confirmed

```
$ git diff --name-only 5cc9049 -- bin templates skills tests .github agents CHANGELOG.md CHANGELOG.ja.md .claude-plugin ...
(empty)
$ bash bin/check-acs.sh ...  AC11: PASS (exit 0)
```
`bin/` (and every other non-goal surface) stays byte-identical to the base
ref on this branch. The underlying `bin/gen-loop-replay.sh` mode-bit defect
is unchanged here (still `100644` at HEAD) — confirmed this is correctly
left to `PR #87`/issue #86, not something this branch touches or needs to
touch, per its own Non-goals.

### Seam gates (re-run)

```
$ bash bin/check-intent.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md .shell-team/todo.md
check-intent: aligned: T-1013 v1 (7a455b4d6ae35d9cb662271b92339bd0e6d46967) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1013.md
check-provenance: conformant: .shell-team/provenance/T-1013.md (2 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1013 .shell-team/interventions/T-1013.md
check-interventions: conformant: .shell-team/interventions/T-1013.md (0 entries, 1 sentinel) -> rc=0
```
Read the new (2nd) provenance decision: it matches what was actually
changed (both README files' two runnable lines rewritten to `bash bin/…`,
the "both forms work" claim replaced with a PATH/exec-bit-independence
claim), its stated reasoning (making the README unconditionally correct
rather than depending on `PR #87`'s merge order, which this read-only role
cannot guarantee) is sound, and its grounding citations (this review's own
§4(3) from round 1, the Codex verdict's Blocker/Adversarial sections) check
out against what is actually in this file.

### Round-2 verdict

All delta items pass. The original Blocker (README demo commands failing
verbatim) is confirmed gone — both runnable examples in both languages now
use the `bash bin/…` form and actually work when run exactly as
documented. EN/JA symmetry holds on the changed prose (faithful
translation, tokens verbatim, frozen anchors survive). `bin/` stays
byte-identical (AC11) — the underlying mode-bit defect is correctly left
to its own fix (issue #86 / PR #87), not this branch's problem. The Minor
("only" overclaim) remains deliberately untouched and correctly
dispositioned to issue #84.


---

## Round 2 — Codex delta re-review (2026-08-01)

スコープ: rework 差分 `8b6dbc2..72902b7`(round 1 の REQUEST_CHANGES から、rework commit `748898b` + QA round 2 の delta 追記 `72902b7` まで)。Codex CLI を `codex exec review --base 8b6dbc2 --json`(フォアグラウンド同期、1回)で実行し、実ファイルを直接読ませた。**新規の Blocker 級指摘が出たため** devil's-advocate の adversarial パスも実行。加えて自分でも `bash bin/check-acs.sh`(13/13 PASS)・`bash bin/check-pii-shapes.sh --base develop`(clean)・`bash bin/check-readme-version.sh`(PASS)を実行し、`git archive HEAD` によるクリーンな抽出コピーで round 2 のデモコマンドを検証(exit 0、データブロック確認済み)。さらに、Codex が指摘した「adopter シナリオでの cwd/PATH 前提」を、実際の PATH 操作と実際のスクリプトを使って自分で3パターン再現した(下記 Blocker 参照)。

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1013
- Codex model: gpt-5.1-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 5 files changed, +198/-6 (round-1 QA `8b6dbc2` … round-2 QA `72902b7`; rework commit `748898b`)

#### Blockers

- **round 1 で私が推奨した `bash bin/gen-loop-replay.sh …` という修正は、exec-bit 問題を解消する一方で、この README セクションの一次コマンド(汎用の使用例 `gen-loop-replay.sh <run-id>`)に新たな回帰を持ち込んでいる——adopter が自分自身の(shell-team とは別の)リポジトリでプラグインをロードして実行した場合に失敗する。** Codex 一次パスが独立に発見し、私も実際の PATH 操作で再現した: (1) このリポジトリ自身の `bin/` を `PATH` に載せた状態で、shell-team のチェックアウトとは無関係な cwd に移動し、`bash bin/gen-loop-replay.sh --help` を実行すると `rc=127`(`bin/` はその cwd に存在しないため)。(2) 同じ状況で `bash gen-loop-replay.sh --help`(裸のファイル名、`bin/` 接頭辞なし)を実行すると `rc=0`——`bash <裸のファイル名>` はスラッシュを含まない引数に対して `$PATH` を検索し、しかもこの検索は実行ビットを要求しない(この host の `/bin/bash`(3.2.57)で、実行ビットを外したファイルでも `bash <PATH 上の名前>` が正しく解決することも実際に確認済み)。これはこのリポジトリ自身の確立された規約(`CLAUDE.md` の Dogfood 節: 「プラグインをロードした状態では `bin/` は `PATH` に載る。裸の名前でスクリプトを呼ぶこと…adopter が実行するものと一致するように」)とも整合する——**round 2 の現在の文言は、まさにこの規約が想定する「plugin がロードされた任意の repo」というケースで壊れている。** これは exec-bit の元の Blocker とは別の、round 1 の修正自体が導入した新しい回帰であり(round 1 では shell-team 自身のリポジトリルートからしか検証しておらず、adopter の別リポジトリという主要なシナリオを見落としていた——QA も私自身もその回だけでは見つけられなかった)。**修正**: 汎用の使用例コマンドを `bash bin/gen-loop-replay.sh <run-id>` から `bash gen-loop-replay.sh <run-id>`(`bin/` を外す)に変更し、括弧書きの説明も「`PATH` にも実行ビットにも依存しない」ではなく「プラグインがロードされていれば `PATH` 経由でこのスクリプトを解決し、実行ビットには依存しない」という趣旨に書き直す。凍結アンカー `gen-loop-replay.sh <run-id>`(AC4/AC5)はこの変更後も部分文字列として保持され、壊れない(確認済み)。EN/JA 両方(`README.md`/`README.ja.md`)の同一コマンドに対称的に適用が必要。

#### Major

なし。

#### Minor / nits

- **デモコマンド(`gen-loop-replay.sh 20260801T000000Z-flagrail --runs-dir tests/gen-loop-replay/fixtures/flag-rail --out …`)自体は `bash bin/gen-loop-replay.sh …` のままで問題ない——`--runs-dir` 引数自体がこのリポジトリのコミット済み fixture への相対パスであり、どちらの呼び出し形式を使っても本質的に「このリポジトリ自身のチェックアウトから実行する」ことが前提になっているため、汎用コマンドと同じ cwd/PATH の懸念はここには当てはまらない(adversarial パスで Codex 自身がこの区別を明確化)。** ただし、読者に「このデモはリポジトリのチェックアウトから実行する前提」であることを明示する一文を添えると親切——現状は暗黙のままなので、明確化のための軽微な追記を推奨(欠陥ではなく、読みやすさの改善)。

#### Adversarial findings

Codex 自身に、一次パスの指摘に対して devil's-advocate の反論を行わせた。結果: (1) より良い修正案として「プラグインロード済み(adopter repo 含む)は `bash gen-loop-replay.sh <run-id>`、プラグイン未ロードで shell-team 自身をチェックアウトしている場合は `bash bin/gen-loop-replay.sh <run-id>`」という2ケースの書き分けも提案しつつ、「README で単一のコマンドとして書くなら `bash gen-loop-replay.sh` の方が primary な adopter シナリオに合う」と結論。(2) デモコマンドの `--runs-dir` 引数自体がこのリポジトリのチェックアウトに固有のため、汎用コマンドの懸念はデモには本質的に当てはまらないと明確化(「chekcout root から実行する」旨を明示する軽微な追記のみで十分)。(3) 当初の P1 は「2つのコマンドを混同して影響を過大に一般化していた」として、自ら P2/Major への格下げを提案。**私はこの格下げに同意せず、汎用コマンド側は Blocker のまま維持する**(理由は Synthesis audit ledger 参照——このセクションの一次コマンドが adopter の主要な利用シーンで壊れることは、v1.2.0 直前の adopter 向け文書としては merge をブロックするに値すると判断)。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | 汎用使用例コマンド(`bash bin/gen-loop-replay.sh <run-id>`)が adopter 自身の別リポジトリでプラグインロード時に `rc=127` で失敗する(round 1 の修正が導入した回帰) | primary: Codex P1(blocker)/ adversarial: Codex 自身が「2コマンドの混同」を理由に P2/major へ格下げを提案 | blocker | adversarial の「デモコマンドは元々 checkout 前提だから影響が限定的」という区別自体は正しく採用したが、汎用コマンド側は区別後もなお「adopter が自分のリポジトリで実行する一次コマンドが壊れる」という事実は変わらない。v1.2.0 直前の adopter 向け README のオンボーディング一次コマンドが主要な利用シーンで動かないことは、実害・可視性ともに blocker 相当と判断し、Codex 自身の格下げは採用しない |

#### Recommendation

- REQUEST_CHANGES → board を `READY_FOR_ENG` に戻し、汎用使用例コマンドを `bash gen-loop-replay.sh <run-id>` に変更(`bin/` を外す。EN/JA 両方)、括弧書きの説明文を新しい仕組みに合わせて書き直す。デモコマンドは現状維持でよいが、「このリポジトリのチェックアウトから実行する前提」である旨の一文追記を推奨(Minor、ブロックしない)。

#### Fast-follow disposition

round 1 から変更なし: rail caveat の "only" 過大主張は issue #84 へのポインタのまま(未変更)。

---

## Round 3 — delta re-verification + inventory coverage audit (2026-08-01)

Scope: the rework-round-2 delta (engineer `c80f4b0`), fixing Codex round-2
REQUEST_CHANGES (one Blocker — round 1's own `bash bin/gen-loop-replay.sh`
fix regressed in the primary adopter context; same-class-2 fired across
two consecutive `doc-command-unrunnable-in-context` Blockers, mandating a
full inventory-and-bulk-apply rather than a third spot-fix, human-ratified
at the escalation this round's interventions entry records). This round's
own job, per the coordinator, is auditing that the engineer's 9-site
inventory is **complete**, not merely that the two applied fixes work.

### 1. Coverage audit — independent re-enumeration

Read the full `git diff 5cc9049` for all six in-scope files myself (not the
new sections alone — the whole diff, since `README.md`/`.ja.md` also touch
the `bin/` tree line and the Telemetry bullet outside the new section).

- **`docs/loop-engineering/goal-loop.md`/`.ja.md`, `docs/workflow.md`/
  `.ja.md`**: confirmed zero fenced code blocks anywhere in these four
  files' diffs (`grep -E '^\+```' ` over the diff returns nothing) — every
  changed line is table-cell or paragraph prose naming tool names, with no
  runnable command example of any kind. Matches the engineer's sites 8–9
  dispositioned NOT APPLICABLE.
- **`README.md`/`README.ja.md`**: counted fenced-block open markers added
  in the diff (`grep -E '^\+```bash'`): **exactly 4** — 2 in `README.md`
  (generic-usage, demo), 2 in `README.ja.md` (mirrors). No fifth fence
  exists anywhere in either file's diff.
- Swept every backtick-quoted span containing `--` across the whole
  six-file diff (`grep -oE '`[^`]*--[^`]*`'`) to catch any invocation-shaped
  **inline** code outside a fence that might have been missed: found only
  bare flag mentions in prose (`` `--event` ``, `` `--label` ``,
  `` `--span` ``, `` `--out` ``, `` `--out <path>` ``) and one inline
  script-plus-flag mention, `` `bin/team-paths.sh --get runs` `` — the
  latter matching the engineer's site 5 exactly (descriptive reference
  inside a sentence explaining internal resolution, not framed as an
  instruction to paste and run, no fence, no additional args offered).

**Result: my independent sweep finds exactly the same 4 actual runnable
command sites the engineer's table lists as sites 1–4 (all APPLIED), and
no additional site among 5–9 that should have been APPLIED instead of
NOT APPLICABLE.** The 9-site inventory is confirmed **complete** — no site
was missed.

### 2. Context B reproduction — the fix's load-bearing proof

Built a real empty directory outside the checkout (not a description) and
ran both forms with `PATH="<repo>/bin:/usr/bin:/bin:/usr/sbin:/sbin"`
(simulating an adopter repo with the plugin loaded, no `bin/` in their
cwd):
```
NEW bare form:          bash gen-loop-replay.sh --help          -> rc=0  (help text printed correctly)
OLD bin/-prefixed form: bash bin/gen-loop-replay.sh --help      -> rc=127
```
Confirms the fix is provably load-bearing: the bare form succeeds and the
form it replaced genuinely fails in exactly the adopter context the
Blocker named.

For additional corroboration (not strictly required by the delta items,
but it directly spot-checks the round's own 3rd provenance decision, which
cites a specific rc=127/rc=0 matrix across two contexts): reproduced
**Context A** exactly as the engineer's own definition (checkout root,
`PATH` explicitly stripped of this repo's `bin/`, `PATH=/usr/bin:/bin:/usr/
sbin:/sbin`):
```
bare form:          bash gen-loop-replay.sh --help       -> rc=127
bin/-prefixed form: bash bin/gen-loop-replay.sh --help   -> rc=0
```
This matches the provenance decision's own grounding text byte-for-byte
(the full 2×2 matrix: bare form needs `bin/` on `PATH` and doesn't care
about cwd; the `bin/`-prefixed relative-path form needs the right cwd and
doesn't care about `PATH`) — independently reproduced, not merely read.

### 3. Demo command, run verbatim from the checkout root

```
$ bash bin/gen-loop-replay.sh 20260801T000000Z-flagrail --runs-dir tests/gen-loop-replay/fixtures/flag-rail --out <path>
exit=0
page exists, non-empty
data block present (id="loop-replay-data")
```
Output deleted immediately after (real checkout confirmed clean before and
after via `git status --short`). The demo command's own form (kept as
`bash bin/…`, per the engineer's site-3/4 disposition) still works exactly
as documented, unchanged from round 2.

### 4. Mechanical checks

```
$ bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md
check-acs: 13 passed, 0 failed, 2 skipped, 0 unrecognized (AC14/AC15 SKIP)

$ bash bin/check-pii-shapes.sh --base develop
check-pii-shapes: clean (no PII-shaped bytes found)   (rc=0)

$ bash bin/check-readme-version.sh README.md README.ja.md
(rc=0)
```

**EN/JA parity on the changed lines and the added sentence** — read side
by side:
- Generic-usage fence: `bash gen-loop-replay.sh <run-id>` — identical
  literal string in both files (code, not translated).
- Its parenthetical: EN "(with the plugin loaded, `bin/` is on `PATH`, so
  `bash gen-loop-replay.sh` resolves it there regardless of the executable
  bit — no `bin/` prefix and no `PATH` setup of your own needed.)" / JA
  "（プラグインをロードしていれば `bin/` は `PATH` に載るので、`bash
  gen-loop-replay.sh` はそこから解決される — 実行ビットには依存しない。
  `bin/` 接頭辞も自前の `PATH` 設定も不要。）" — translated back, the JA
  makes the identical three claims in the identical order (plugin-loaded →
  `bin/` on `PATH` → bare name resolves from there; independent of the
  executable bit; no `bin/` prefix or own `PATH` setup needed). No drift.
- Demo fence: identical literal string in both files.
- Its new clarifying sentence: EN "(run this from a shell-team checkout
  root, since the fixture path below is this repository's own)" / JA
  "（下の fixture パスはこのリポジトリ自身のものなので、shell-team の
  チェックアウトルートから実行すること）" — same claim, same reasoning
  (the fixture path is this repository's own, therefore run from a
  checkout root), same order. No drift.
- Every technical token (`bash`, `bin/…`, `PATH`, `gen-loop-replay.sh`,
  `<run-id>`, the fixture id/path, `--out`, `--runs-dir`) stays verbatim in
  the Japanese text in both changed spots.

### 5. Seam gates

```
$ bash bin/check-intent.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md .shell-team/todo.md
check-intent: aligned: T-1013 v1 (7a455b4d6ae35d9cb662271b92339bd0e6d46967) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1013.md
check-provenance: conformant: .shell-team/provenance/T-1013.md (3 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1013 .shell-team/interventions/T-1013.md
check-interventions: conformant: .shell-team/interventions/T-1013.md (1 entries, 0 sentinel) -> rc=0
```

Read the 3rd (this round's) provenance decision in full: its central claim
is the rc=127/rc=0 matrix across Context A and Context B for both command
forms — independently reproduced exactly, byte-for-byte, in §2 above,
including the specific PATH string and cwd it names. Its reasoning (the
generic-usage site's claimed context is genuinely "any reader with the
plugin loaded," which the bare form serves correctly and the `bin/`-
prefixed form does not; the demo site's `--runs-dir` argument is itself
checkout-relative, so it never claimed a broader context in the first
place) matches what the coverage audit in §1 independently found.

Read the interventions entry (1 entry, `human-correction`): a governance/
process record about how the same-class-2 escalation was communicated
(option (c) presented as a label only, corrected before proceeding) — not
a claim about the deliverable itself, so nothing here to spot-check
against code; recorded faithfully as a process note.

### Round-3 verdict

The coverage audit is the substantive finding of this round: my
independent, mechanical re-enumeration of every runnable command example
in the branch's diff (fences and invocation-shaped inline code, across all
six files, both languages) finds **exactly the same 4 real sites** the
engineer's 9-site table lists as APPLIED, and confirms none of the
remaining 5 sites should have been treated as a runnable example instead
of prose. The inventory is complete. Context B's reproduction confirms the
fix is genuinely load-bearing (not merely claimed); Context A's
reproduction independently corroborates the provenance decision's own
rc=127/rc=0 matrix exactly. The demo command still works verbatim from the
checkout root. All mechanical checks green; EN/JA parity holds on every
changed and newly-added span.

