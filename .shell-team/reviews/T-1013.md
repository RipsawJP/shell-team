# QA verification — T-1013

Branch `feature/77-part3-docs-wiring`, HEAD `460d626` (base `develop` @
`5cc9049`). Spec: `.shell-team/specs/T-1013-loop-replay-docs-wiring.md`
(intent v1, aligned, 15 ACs — 13 mechanical, AC14/AC15 SKIP by design).
Working tree kept clean throughout (`git status --short` shows nothing
beyond this review file and the board edit); all mutation testing ran on
file copies under `$TMPDIR`, never on the real checkout. No real filesystem
path is quoted anywhere in this file — `$TMPDIR`/`/tmp/...` placeholders
only, matching the shapes the shipped README/spec themselves already use.

## 1. Mechanical acceptance criteria

```
$ bash bin/check-acs.sh --dry-run .shell-team/specs/T-1013-loop-replay-docs-wiring.md
15 recognized, 0 unrecognized

$ bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md   (BEFORE creating this file)
check-acs: 12 passed, 1 failed, 2 skipped, 0 unrecognized
check-acs: FAILED: AC12
```
Confirmed the pre-state matched the expected shape exactly: AC1–AC11, AC13
all PASS; AC12 FAIL solely because `.shell-team/reviews/T-1013.md` did not
exist yet; AC14/AC15 SKIP by design.

AC12's own sub-components verified individually before creating this file:
```
$ bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"        -> rc=0
$ bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base develop -> rc=0
$ bash bin/check-pii-shapes.sh --base develop                             -> "clean (no PII-shaped bytes found)", rc=0
```

After creating this file:
```
$ bash bin/check-acs.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md   (AFTER)
check-acs: 13 passed, 0 failed, 2 skipped, 0 unrecognized (AC14/AC15 SKIP)
```

## 2. Full CI-ordered local run (`.github/workflows/check-handoff.yml`)

Ran every step in the workflow's own order, serially. All 55 steps passed
(0 failures). `shellcheck --version` on this host = 0.11.0, matching the
workflow's pin; `develop`/`origin/develop` identical locally.

Special attention to `bin/check-readme-version.sh`, per the coordinator's
ask:
```
$ bash tests/check-readme-version/run.sh   -> PASS (rc=0)
$ bash bin/check-readme-version.sh README.md README.ja.md   -> PASS (rc=0)
```
The badge is untouched in both READMEs, confirmed.

## 3. Mutation self-checks — independently reproduced

Both on file copies under `$TMPDIR` (never `git checkout`; real checkout
confirmed clean throughout).

**Mutation #1 — delete `--event` from `README.ja.md`'s Telemetry bullet
only.** Removed the token from the copy's bullet line, keeping everything
else byte-identical.
```
grep -F -- 'bin/check-run.sh' <mutated README.ja.md> | grep -qF -- '--event'  -> NO MATCH (AC1's own assertion goes red)
```
Re-ran AC6's own projection logic (sorted anchor-hit lists, `cmp`) against
the mutated pair:
```
cmp <en.sorted> <ja.sorted>  -> differ at line 1: "--event" (EN has it, JA does not)
```
Confirms the exact predicted double failure (AC1 red **and** AC6 red — the
projection asymmetry), matching the engineer's own mutation-table claim
precisely.

**Mutation #4 — replace `loop_id=goal` with `loop_id=notgoal` in the
`goal-loop` telemetry row, keeping the two new anchors.** On a copy of
`docs/loop-engineering/goal-loop.md`:
```
count of 'bin/log-run.sh' on the row: 1 (unchanged)
'/shell-team:run' present: yes
'bin/gen-loop-replay.sh' present: yes
'loop_id=goal' present: NO (mutated to loop_id=notgoal)
```
Confirms AC2's preservation control actually bites — the criterion reads
the telemetry row itself, not merely a line that happens to mention the
generator, exactly as the engineer's mutation-table entry #4 claims.

Mutations #2, #3, #5, #6, #7 and the blind-spot attacks were not
independently re-run this round (2 of 7 required); time was spent instead
on the AC15 human-read items below, which the coordinator named as the
core of QA for a docs task.

## 4. AC15 — the human-read items (read, not grepped)

### (1) EN/JA semantic fidelity

Read all six changed passages side by side, sentence by sentence:

- **README `## Replaying a run` / `## run のリプレイ`** — all four
  paragraphs (what it is / how to generate / the caveat / the demo) carry
  the same claims in the same order in both languages; nothing is added,
  dropped, or softened/strengthened across the language boundary. Every
  machine token, path and flag (`gen-loop-replay.sh`, `<run-id>`,
  `<runs>/replay-<run-id>.html`, `bin/team-paths.sh --get runs`,
  `git-ignore`, `--out`, `READY_FOR_ARCH`…`READY_FOR_MERGE`, `--label`,
  `handoff`, `skills/run/SKILL.md`, the fixture id and directory) appears
  verbatim in English inside the Japanese section — confirmed no token was
  translated, matching T-1010's operator/machine-token boundary.
- **`goal-loop` telemetry row** — EN ("one span per sub-agent call
  (`loop_id=goal`); event rows come from `/shell-team:run`, not from the
  goal loop itself; `bin/gen-loop-replay.sh` reads either kind back as a
  replay page") and JA ("サブエージェント呼び出しごとに 1 span
  (`loop_id=goal`)。event 行は goal loop 自身ではなく `/shell-team:run`
  から来る。`bin/gen-loop-replay.sh` がどちらの行種別も replay ページとして
  読み戻す") say the identical thing in the identical order — a faithful
  translation, not a paraphrase that drifts.
- **`workflow.md` / `.ja.md` per-cycle-artifact note** — both extend the
  existing paragraph with the same three claims (operator `label` text
  verbatim in the page; the page's UI dictionary carries both languages;
  machine tokens — the rail's five stops, the verdict labels — stay
  verbatim English; the shipped template itself is English shipped
  surface) in the same order, in both languages.

**No semantic drift found** in any of the three sites — this is a
straightforward, accurate finding to report (not every review turns one
up, and this one did not).

### (2) Rendered-markdown sanity

- **Tree figure alignment**: measured the `#` comment column across the
  `bin/` block in both READMEs (`awk`'s `index($0,"#")` on lines
  134–142). Every comment in both files lands at column 46 (the parent
  `bin/` line itself at column 44, consistent with its different
  indentation depth) — the new `gen-loop-replay.sh` line's comment aligns
  exactly with its siblings in both languages. Alignment is not broken.
- **Fenced blocks**: both new sections' two ```bash fences are balanced,
  properly closed, and render as code blocks under a raw read; no stray
  backtick or unterminated fence.
- **Heading placement**: confirmed by direct line-number read that
  `## Replaying a run` / `## run のリプレイ` land between `## Operating
  loop` / `## 運用ループ` and `## Design choices` / `## 設計上の選択` in
  both files, exactly as D2 requires.
- **Table rows**: both `goal-loop` telemetry rows render as single,
  correctly pipe-delimited table lines (confirmed by reading the
  surrounding table structure), not a broken or wrapped row.

### (3) Factual accuracy against the shipped code

- **Usage line**: `bash bin/gen-loop-replay.sh --help` confirms the real
  CLI is `gen-loop-replay.sh <run-id> [--runs-dir DIR] [--out PATH]
  [--template PATH]` — matches the README's documented usage and demo
  flags exactly.
- **Output path / git-ignore claim**: confirmed `bin/team-paths.sh --get
  runs` resolves a real directory and `git check-ignore` reports the
  `replay-<run-id>.html` shape as ignored under it, with zero new ignore
  patterns (`runs/` already present in both `.gitignore` files) — matches
  D4 exactly.
- **`goal-loop` closure**: confirmed `skills/goal/SKILL.md` invokes
  `log-run.sh` with `--span` only, no `--event` instruction anywhere in the
  file — the row's "spans only, event rows come from `/shell-team:run`"
  claim is true at this base ref.
- **Rail caveat vs. `flagFromLabel`/`SKILL.md:110`**: `flagFromLabel`
  performs exactly the byte-exact `FLAG_STOPS.indexOf` match the caveat
  describes, and `SKILL.md:110` documents exactly the `handoff`-event,
  bare-token, `--label` convention the caveat points at. **However, found
  a genuine, subtle factual overclaim**: the README's caveat says "the rail
  **only** lights for a run whose `handoff` events carry the board flag" —
  the word "only" asserts exclusivity to `handoff` events. Per T-1014 D1,
  the derivation is uniform across **every** `kind:"event"` row, not just
  `handoff` — confirmed directly in `templates/loop-replay.html`'s
  `adaptRow` (every one of the five `case` branches derives `flag`
  uniformly) and by T-1014's own `flag-rail` fixture, whose `human`-kind
  row with a bare token derives a flag (the `rail-uniform-kind` assertion,
  independently verified during T-1014's QA round). So a `human` event
  with a bare `READY_FOR_MERGE` label *also* lights the rail, contradicting
  the word "only." **This is not a T-1013-introduced error**: D3's own
  frozen spec text uses the identical "handoff-scoped" framing
  (`.shell-team/specs/T-1013-loop-replay-docs-wiring.md` D3: "The rail
  lights only when the orchestrator put the board flag it just set onto
  the same `handoff` event's `--label`..."), which itself mirrors the same
  handoff-scoped framing I flagged as a documentation asymmetry in
  `skills/run/SKILL.md` during T-1014's QA round. The engineer correctly
  implemented what the frozen D3 said; the imprecision is inherited from
  the spec text, traceable to the same root cause across three tasks now.
  **Not FAIL-driving** (D3 is frozen; the engineer has no standing to
  rewrite it; the claim is true for the dominant, documented path and only
  overclaims exclusivity on a path the spec itself never asked to be
  documented) — flagged here for the reviewer/human as a cross-task
  pattern worth a single, coordinated fast-follow across `SKILL.md`, this
  README caveat, and T-1013's own D3 text.
- **The demo command, run verbatim** — **found a real, reproducible defect,
  external to this task's scope.** Ran the documented bare-form command
  exactly as written (`gen-loop-replay.sh 20260801T000000Z-flagrail
  --runs-dir tests/gen-loop-replay/fixtures/flag-rail --out
  /tmp/replay-demo.html`, then separately the `./bin/gen-loop-replay.sh`
  form) from this repository's root, with `bin/` confirmed on `$PATH`
  (`check-acs.sh` and other `bin/` scripts resolve via bare name in this
  same session). **Both the bare form and the `bin/`-prefixed direct form
  fail with a permission-denied shell error** — neither is what the
  README's own parenthetical claims ("the bare form above and the
  `bin/`-prefixed form both work"). Root cause, confirmed mechanically:
  `bin/gen-loop-replay.sh` is missing its executable bit —
  `ls -la bin/gen-loop-replay.sh` shows `-rw-r--r--` where every sibling
  script (`log-run.sh`, `check-acs.sh`, etc.) shows `-rwxr-xr-x`. Confirmed
  via `git ls-tree` that the blob and mode (`100644`, non-executable) are
  **identical at `develop`, at this task's own base ref (`5cc9049`), and at
  HEAD** — this predates T-1013 entirely (inherited from T-1012 or its
  T-1014 rework) and is explicitly **not** a T-1013 regression; `AC11`'s own
  byte-identity lock on `bin/` (confirmed PASS) is consistent with this,
  since it only detects content diffs, not that a pre-existing mode defect
  was carried forward unnoticed. Also confirmed this is part of a **wider
  pattern**, not unique to this one file: `bin/check-commit-identity.sh`
  and `bin/check-pii-shapes.sh` are the only other two `bin/*.sh` files
  sharing the same non-executable mode at `develop` — three files total,
  all pre-existing. **Why no mechanical check caught this**: every single
  `run:` step in `.github/workflows/check-handoff.yml` that invokes a
  script does so with an explicit `bash <script>` prefix (confirmed: zero
  bare/`./`-prefixed invocations in the whole workflow file), and AC8's own
  `check:` line does the same (`bash bin/gen-loop-replay.sh ...`) — neither
  the CI list nor any mechanical criterion ever exercises the exact
  invocation shape the README's own prose promises. Ran the demo again
  through the working (`bash`-prefixed) form to close out the "does the
  demo actually work" half of the ask: exit 0, page written, non-empty,
  carrying the one data block — then deleted the output. **Disposition**:
  this is squarely a `bin/` defect (a missing `chmod +x`, not an interface
  mismatch), which T-1013's own Non-goals forbid fixing here ("If the
  documented interface turns out to be wrong, that is a `BLOCKED` report
  and a scope question — not a fix to make here"). **Not FAIL-driving for
  T-1013** on that basis — the documentation itself is accurate about the
  *interface* (flags, exit codes, output path) and the underlying claim
  will become true the moment the exec bit is restored elsewhere — but this
  is a real, high-severity, pre-release finding that needs **immediate
  human attention**, separate from this task's own merge decision: right
  now, a reader who pastes the README's own bare-form command verbatim
  gets a permission-denied error, and this is the last feature task before
  v1.2.0. Recommend an urgent fast-follow issue to restore the executable
  bit on all three affected `bin/` files (a one-line `chmod +x` each, no
  code change) before or immediately alongside this release.

## 5. Engineer's disclosed items — evaluated

- **AC4/AC5 awk fenced-block blind spot — confirmed dormant.** Ran the
  same slice-extraction `awk` command the criteria use against both new
  sections and grepped the extracted slice for any line starting with
  `## `: none found in either language. The blind spot is real in the
  abstract (a future edit adding a fenced block whose content starts a line
  with `## ` would truncate the slice early) but is not triggered by the
  content actually shipped.
- **AC6/AC3 redundancy note — confirmed accurate.** `gen-loop-replay.sh`
  appears four times in `README.md` (the tree line, the Telemetry bullet,
  the usage example, the demo command). A mutation removing only the tree
  line's mention would leave the substring present elsewhere in the file,
  so AC6's whole-file `grep -qF` would still pass — confirmed AC6 cannot
  independently catch that specific removal. AC3's own `awk` condition
  (a line containing **both** `gen-loop-replay.sh` **and** `──`) is the
  only one of the two that would actually drop to zero matches, since none
  of the other three occurrences sit on a box-drawing tree line. The
  engineer's claim that AC3, not AC6, is the real backstop for the tree
  line is correct.

## 6. Frozen intent + seam gates

```
$ bash bin/check-intent.sh .shell-team/specs/T-1013-loop-replay-docs-wiring.md .shell-team/todo.md
check-intent: aligned: T-1013 v1 (7a455b4d6ae35d9cb662271b92339bd0e6d46967) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1013.md
check-provenance: conformant: .shell-team/provenance/T-1013.md (1 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1013 .shell-team/interventions/T-1013.md
check-interventions: conformant: .shell-team/interventions/T-1013.md (0 entries, 1 sentinel) -> rc=0
```

Read the one provenance decision (the `workflow.md` paragraph-extension
call): confirmed directly against the actual file diff — both
`docs/workflow.md` and `.ja.md` extend the existing `**Per-cycle
artifacts**` paragraph with new lines appended directly after the prior
last line, no blank line inserted (so it renders as one markdown paragraph
block, not two), and the deletion count against the base ref is 1 in each
file (well inside AC10's cap of 5) — matches the decision's own claim
exactly.

## 7. Regressions / adjacent code paths

AC11's byte-for-byte diff-against-base check covers all of `bin/`,
`templates/`, `skills/`, `tests/`, `.github/`, `agents/`, both CHANGELOGs,
the plugin manifest directory, every `docs/` file outside the four in
scope, and T-1011's/T-1012's/T-1014's frozen specs — all unchanged,
confirmed by the live `check:` run, with the positive control (four named
paths actually exist) also passing.

## 8. Edge cases tried beyond the spec's own fixtures

- Ran the documented demo command through **both** of its claimed
  invocation forms (bare, `bin/`-prefixed direct) as well as the
  CI/AC8-tested `bash`-prefixed form — this is what surfaced the exec-bit
  finding in §4(3). This stays inside the spec's own declared Input space
  ("The documented command as a reader would paste it... Both must be
  literally correct against the generator's real interface"), not a
  synthetic extreme.
- Checked whether the exec-bit gap was unique to one file or systemic
  (found two more affected files) and whether CI's own step list would
  ever exercise the failing invocation shape (found it does not, for any
  script) — grounding the disposition recommendation in measurement rather
  than a guess.

## Verdict

All mechanical items pass; both independently-reproduced mutations behave
exactly as the engineer's provenance/hand-off claims; the two disclosed
items (awk blind spot, AC6/AC3 redundancy) both check out. The EN/JA
semantic-fidelity and rendered-markdown reads found no defects. The
factual-accuracy read found two things worth surfacing: a minor,
spec-inherited "only" overclaim in the rail caveat (not FAIL-driving,
traced to the same root cause as a T-1014 finding), and a real,
high-severity, pre-existing `bin/` executable-bit defect that makes the
README's own bare-form demo command fail as literally documented — squarely
out of this task's fixable scope per its own Non-goals, but requiring
urgent, separate human attention before v1.2.0 given this task's position
as the last feature task before release.

