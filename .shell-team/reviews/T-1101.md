# Review record: T-1101

Created by the engineer at hand-off (AC17/AC18) so the file exists before
the cross-provider review runs. Cross-provider review rounds are appended
below by `codex-reviewer`, each as its own `### Codex Review verdict: ...`
section, oldest first.

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1101
- Codex model: GPT-5 (config alias `gpt-5.6-luna`)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 11 files, +798/-13 lines (`058ec58...HEAD`, this task's own diff surface — the branch point of the stacked predecessor `feature/1100-verify-axis-split`, whose own already-reviewed diff is excluded)

This round reviews `bin/check-pii-shapes.sh` and `tests/check-pii-shapes/run.sh` at Tier 1 (full adversarial strength, per the round's own two-tier briefing) and everything else — this spec's own inline `- check:` lines, the docs pair, the record files — at Tier 2 (computed-value correctness and real instrument defects only). A manual spot-check preceded the Codex passes: `bash bin/check-pii-shapes.sh --all` (clean, matching engineer/QA), `bash tests/check-pii-shapes/run.sh` (88/88 PASS, matching engineer/QA), `shellcheck bin/check-pii-shapes.sh` (clean). The self-reference trap is closed for a structural reason (the placeholder `<...>` forms are non-matches by construction, confirmed live via `--all` against the tree including the header's own prose), and the two live redaction sites (`.shell-team/reviews/T-1098.md:69`, `.shell-team/todo.md`) both correctly carry the placeholder form with no other byte disturbed — spot-confirmed directly against the diff.

#### Blockers

None.

#### Major

- `bin/check-pii-shapes.sh` — `RE_HOME_ENCODED='[-_](Users|home)[-_][A-Za-z][A-Za-z0-9_.]{2,}[-_]'` — this is a genuine, reproduced coverage gap against the round's own explicit question ("does the new pattern fire on the leak's whole shape family, not only the one spelling this machine's own generator emits"). The name-continuation class requires the first character to be an ASCII letter and the whole name to run at least 3 characters before the closing separator can be reached. Two independent, live-reproduced consequences: (1) a short account name (2 characters or fewer, e.g. `-Users-<ab>-`, or a hyphenated name whose first component is under 3 characters, e.g. `-Users-<a-b>-`) is silently not detected; (2) a name beginning with a digit or underscore (e.g. `-Users-<1234>-`, or `-Users-<_www>-` — the real macOS system-account naming convention) is silently not detected, because the class requires `[A-Za-z]` first. Both were confirmed live in a scratch fixture (`bash bin/check-pii-shapes.sh --base <scratch-base>` against `-Users-<a-b>-`, `-Users-<_www>-`, `-Users-<1234>-`: all three exit 0 clean, while a control line with a ≥3-char letter-led name at the same position fires correctly). This matters because the already-shipped sibling rule for the *same* conceptual class — `RE_HOME_PATH_RAW='/(Users|home)/[A-Za-z0-9_.-]+'`, the slash-delimited form — has no such length floor and no such leading-character restriction; the new encoded-form rule is measurably less capable than the rule it is meant to extend coverage alongside, for account-name shapes that are common in practice (short names, and the leading-underscore system-account convention) rather than synthetic extremes. Neither gap is named in the spec's `## Input space` out-of-scope list (which excludes undashed-hex UUIDs, non-`-`/`_` separators, non-ASCII names, and unusual root-word case — not short/digit/underscore-led ASCII names), and neither is documented as a declared limitation in `docs/pii-controls.md`/`.ja.md`. Suggested fix: broaden the name-continuation class to match the shipped `home-path` rule's own `[A-Za-z0-9_.-]+`-shaped tolerance (adjusted to still exclude the encoding separator itself from the continuation class, and to keep the `<`/`>` exclusion that makes the placeholder form a non-match), and add regression fixtures for a short name and a leading-underscore/leading-digit name.

#### Minor / nits

- `bin/check-pii-shapes.sh` — `RE_TEMP_SESSION_ROOT='(/private/tmp/|/tmp/|/var/folders/)[A-Za-z0-9_./-]*'` — the prefix character class between the temp root and the UUID excludes some rare-but-possible path characters (e.g. `=`, an embedded space), so a temp/session path whose intermediate directory component contains one of those would not be detected even though it carries a genuine UUID under a documented root. Confirmed live: `/var/folders/xy=z/<uuid>` is not reported. Kept at Minor rather than Major because no `mktemp`-style generator in this repository's own reach produces such characters (this repository's own suites build their work roots from `${TMPDIR:-/tmp}`/`mktemp`, which never emit `=` or whitespace), and the negative-control population measured in this task's own `## Blast radius` (thirteen `/private/tmp/` citations across five files) carries none of these characters either — so the gap is real but currently has no live carrier and a low-probability future one. Worth a documented limitation or a small widening in a follow-up, not a merge blocker on its own.

#### Adversarial findings

Both items above were surfaced by a dedicated adversarial pass (`codex exec` asked to "play devil's advocate" against `bin/check-pii-shapes.sh`'s two new regexes, the header's self-reference exposure, and the two redacted files) — the round-1 primary pass independently found the first half of the Major (the 3-character floor); the adversarial pass found the leading-character restriction and the temp-session prefix-class gap. The adversarial pass also checked `KNOWN_SHAPE_PATHS` and the redacted files for any widened exemption route or residual PII-shaped byte and found neither disturbed.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `RE_HOME_ENCODED` 3-char-minimum floor (primary) merged with leading-letter-only restriction (adversarial) | primary: major (P2) — adversarial: major | major | Same underlying regex defect (the name-continuation class), reproduced live twice from two independent angles; merged into one finding rather than double-counted. No severity change. |
| 2 | `RE_TEMP_SESSION_ROOT` prefix-class excludes `=`/whitespace | adversarial: major | minor | Downgraded on my own judgment: reproduced live and real, but no live carrier exists in this repository's own reach (no generator here emits `=`/whitespace in a temp-root prefix) and the measured negative-control population carries none either — the practical-exposure gap between this item and item 1 (which maps directly onto a real macOS naming convention, `_www`) is wide enough to warrant different severities even though both were tagged `major` by the adversarial pass. |

#### Recommendation

- REQUEST_CHANGES → set `T-1101` back to `READY_FOR_ENG` with the findings above linked.

#### Fast-follow disposition

- The Minor (`RE_TEMP_SESSION_ROOT` prefix-class gap) is not something I am deferring past this round — since the Major above already returns this task to rework, folding a one-line regex widening into the same round costs nothing extra and avoids a second review cycle. Intended disposition if the engineer judges it out of this round's scope instead: won't-fix, on the same low-live-carrier ground the ledger states, re-opened if a real occurrence is ever measured in this tree.
- no other fast-follow deferrals

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1101
- Codex model: GPT-5 (config alias `gpt-5.6-luna`)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 8 files, +193/-25 lines (`0b7c818..a6fef3d`, this round's rework: the round-1 fix commit `420f571` plus the board's own rework-note commit `a6fef3d`)

Round 2 verifies round 1's rework closes the round-1 findings, probes the repaired regexes' own new boundaries, and checks for fix-induced regressions.

**Round-1 Major (`RE_HOME_ENCODED` coverage gap) — CONFIRMED CLOSED.** Independently re-reproduced all four round-1-named spellings myself in a fresh scratch fixture (`bash bin/check-pii-shapes.sh --base <scratch-base>`): `-Users-<ab>-`, `-Users-<a-b>-`, `-Users-<1234>-`, `-Users-<_www>-` all now fire correctly (they did not before). Tried five further spellings of my own choosing beyond the four: a dotted multi-part name (`-Users-<x.y.z>-`, fires — correct), a double-internal-hyphen name (`-Users-<a--b>-`, fires — correct), a name closing on a mismatched separator type (`-Users-<a>_`, fires — correct, and confirmed this mismatched-open/close-separator tolerance pre-dates this round's rework, present in the original round-0 regex too, so it is not a regression this rework introduced), an unclosed single-dot fragment (`-Users-.` with no closing separator, correctly stays clean), and a single-letter name between underscores (`_Users_<a>_`, fires — correct). The regression-lock fixture's own claim is verified structurally, not just by the one shipped case: a bare **hyphen** separator run (`-Users---`) correctly stays clean, because the anchor design requires a non-separator character at both the start and end of the captured name and hyphen is excluded from that anchor alphabet — reproduced live myself, matching the engineer's own report.

**Round-1 Minor (`RE_TEMP_SESSION_ROOT` prefix class) — CONFIRMED CLOSED per its own fix-or-disclose split.** The `=` widening fires correctly (`/var/folders/xy=z/<uuid>`, and a double-`=` variant of my own, `/var/folders/xy==zz/<uuid>`, both fire). The whitespace gap is disclosed as a declared limitation in both `docs/pii-controls.md` and `docs/pii-controls.ja.md`, re-confirmed present and matching T-111's own canonical-line style (AC11's own byte-lock re-run clean). This was an acceptable disposition per my own round-1 ledger, which explicitly named "won't-fix, disclosed" as an acceptable outcome for this specific gap.

#### Blockers

None.

#### Major

None.

#### Minor / nits

- `bin/check-pii-shapes.sh` — `RE_HOME_ENCODED`'s repaired anchor design (`[A-Za-z0-9_.]([A-Za-z0-9_.-]*[A-Za-z0-9_.])?`) is asymmetric between the two separator characters, and the header/provenance's own claim overstates what it actually guarantees. `_` is a member of both the separator alphabet `[-_]` and the name-anchor alphabet `[A-Za-z0-9_.]`, so any single literal `_` character positioned as "the name" between two valid separators is a false-positive match — reproduced live in three independent shapes: `_Users_<_>_`, `_home_<_>_` (underscore-only separator run — the direct underscore-flavored counterpart of the shipped `-Users---` regression-lock fixture, which only tests the hyphen flavor), and `-Users-<_>-` (a lone underscore as the captured name, between *hyphen* separators — showing the gap is about a bare `_` acting as "the name," not only about underscore-flavored separator runs). Hyphen does not have this problem (correctly excluded from the anchor alphabet at both ends), which is exactly what the shipped `-Users---` fixture proves — but the header comment's and provenance's own claim ("a pure separator run structurally cannot satisfy the mandatory leading anchor") is stated as an unconditional invariant and is therefore inaccurate for the underscore case; this is adopter-facing text (`--help` derives from the header). A primary `codex exec review` pass independently found the same defect (`_Users_<_>_`/`_home_<_>_` false-positive) without being told about it, confirming this cross-provider before I extended it with the third shape. Kept at Minor rather than Major on the same DP-10 asymmetry ground the round-1 ledger already applied to the temp-session gap: this is a false positive (the safe-failure direction), not a false negative, and it requires an essentially unreachable input (a bare separator-and-underscore run with no real name, which does not occur in ordinary prose, kebab-case, or snake_case identifiers). Suggested resolution, same fix-or-disclose pattern already used this round for the temp-session gap: either split the pattern per separator flavor so each separator excludes its own character from its own anchor positions (mirroring how hyphen is already excluded), or narrow the header/provenance claim to its actual (hyphen-only) scope and add a symmetric regression-lock fixture and disclosed-limitation bullet for the underscore case — whichever the engineer judges proportionate.

#### Adversarial findings

A primary `codex exec review --base 0b7c818` pass (unguided, reading the rework diff directly) independently surfaced the underscore-separator-run false positive above without being told about it — the same defect I found via my own live probing, confirming it cross-provider. A follow-up guided adversarial pass (asking Codex to probe further boundaries, the placeholder exclusion, and the two previously-redacted files for a new residual trip) hung on stdin after 1000+ seconds with no output and was abandoned rather than retried indefinitely; the primary pass's independent corroboration of the one finding this round, plus my own direct reproduction across three separate fixture shapes, is sufficient without it. Manually verified (not by Codex, by me): the placeholder exclusion (`<name>`, `<worktree>`, `<session-uuid>` forms) still holds cleanly against the repaired regex; `bash bin/check-pii-shapes.sh --all` stays clean, meaning neither previously-redacted file (`.shell-team/reviews/T-1101.md`, `.shell-team/provenance/T-1101.md`) carries a new residual trip after this round's rework.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `RE_HOME_ENCODED` underscore-as-lone-name false positive | primary: major (P2) — my own manual extension: reproduced in 3 shapes, no separate adversarial-pass tag (that pass hung and was abandoned) | minor | Same DP-10 asymmetry reasoning already applied to the round-1 temp-session finding: this is a false positive (safe-failure direction) against an essentially unreachable synthetic input, not a false negative against a real carrier. Rounding the Codex-native `major` tag down on that documented ground, consistent with round-1's own precedent for the identical reasoning pattern. |

#### Pre-commitment check

The factual-trigger ("two consecutive review rounds landing an independently-new Blocker or Major on the same component") does not fire: round 1 landed a Major on `RE_HOME_ENCODED`; round 2's only new finding on that same component is classified Minor, on the DP-10 asymmetry ground stated in the ledger above, not to avoid the trigger — the classification is argued on its own terms (false positive vs. round 1's false negative) before this section is reached. Recorded explicitly per this task's own pre-commitment discipline, which asks for the check to be auditable rather than silently skipped.

#### Recommendation

- REQUEST_CHANGES → set `T-1101` back to `READY_FOR_ENG` with the Minor finding linked. Not a Major-level block; proportionate to the finding's own severity per the ledger above.

#### Fast-follow disposition

- The Minor above is not something I am deferring past this round on my own initiative — reporting it now, in the same round it was found, costs nothing extra and keeps the header/provenance claim from shipping inaccurate longer than necessary. Intended disposition if the engineer judges it out of this round's scope instead: won't-fix (disclose the narrower, accurate claim rather than chase the regex split), on the same low-live-carrier ground as round 1's analogous item, re-opened if a real occurrence is ever measured in this tree.
- no other fast-follow deferrals

### Codex Review verdict: APPROVE
- Task: T-1101
- Codex model: GPT-5 (config alias `gpt-5.6-luna`)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 5 files, +128/-20 lines (`6a67fb7..8ff9c3d`, round-2's rework plus the board's own rework-note commit)

Round 3 verifies round 2's rework closes the round-2 Minor, probes the repaired regex's own new boundaries (the ones round 2's own message asked me to judge explicitly), confirms the reworded header claim against the shipped regex rather than against intent, and checks for regressions.

**Round-2 Minor (`RE_HOME_ENCODED` bare-`_`-as-name false positive) — CONFIRMED CLOSED, structurally.** Independently re-reproduced all three round-2 shapes myself in a fresh scratch fixture: `_Users_<_>_`, `_home_<_>_`, `-Users-<_>-` all now stay clean (they fired before). The new class (`[A-Za-z0-9_.-]*[A-Za-z0-9.][A-Za-z0-9_.-]*`) closes this the way its own name suggests — by requiring a character from an alphabet with zero overlap with either separator character — rather than by patching the two flavours one at a time, and I confirmed this structurally rather than by trusting the claim: a captured span built entirely from `-`/`_` characters, in any mix or order, contains no member of `[A-Za-z0-9.]` and therefore cannot satisfy the mandatory middle requirement, full stop.

**Boundary variants requested by this round's own message, judged one by one:**
- **`_www` (underscore-led, not underscore-only) still fires** — reproduced live, `-Users-<_www>-` fires correctly. The leading `_` is absorbed by the permissive prefix class; `www` supplies the mandatory alphanumeric-or-dot character. This is the exact distinction the fix's own design makes, and the fixture suite now locks it with a standalone positive control alongside the three new negative locks.
- **A single dot as the entire name (`-Users-<.>-`, `_Users_<.>_`) — fires.** Judged **acceptable**, not a defect. A bare `.` satisfies the mandatory character by design (dot is explicitly in the round-1-established alphabet, needed for real dotfile-style names like a home directory's `.bashrc`), and the residual risk is a false positive (DP-10's safe direction) against a string that requires a literal dot immediately adjacent to a separator immediately adjacent to "Users"/"home" with no space — not meaningfully more reachable than the already-accepted 2-character-name case from round 1 (`-Users-<ab>-`), which nobody has treated as a defect. I looked for a more realistic vector before accepting this (ellipsis-adjacent prose, versioned identifiers) and could not construct one that doesn't already require the same contrived adjacency.
- **`_._` as name (`_Users_<.>_`) — fires, same reasoning as above, judged acceptable for the same reason:** the mandatory character is satisfied by the dot; the surrounding underscores are separators, correctly outside the captured "name" in the structural sense the class enforces (whether they are semantically "part of a real name" is not something a regex can judge, and the false-positive cost is the accepted one).
- **`_-_` mixed runs (`-Users-<_-_>-` style, i.e. no alphanumeric-or-dot character anywhere) — stays clean.** Reproduced live with a run of exactly this shape (`-Users-_-_-`): no finding. This is the mixed-separator generalization of both round-1's and round-2's regression locks, and it holds for the same structural reason — zero characters in the captured span belong to `[A-Za-z0-9.]`.
- **A name that mixes a dot with separator characters (`-Users-<_.__>-`) — fires**, consistent with the above: one dot present anywhere in the span is sufficient regardless of how many separator characters surround it, which is the class's own stated design, not a new discovery.

**Header claim, reworded — CONFIRMED ACCURATE against the shipped regex, not against intent.** Read the actual `RE_HOME_ENCODED` assignment line in `bin/check-pii-shapes.sh` (not the header prose, not the provenance paraphrase) and independently ran the four shapes the reworded sentence now names (`-Users---`, `_Users___`, `_home___`, `-Users-<_>-`) against it directly — all four stay clean, matching the claim exactly, no broader and no narrower. The provenance file's round-1 decision entry keeps its original (now-superseded) reasoning text intact with an inline correction appended, rather than being silently rewritten — read both the original and the correction, and the record is honest about what was believed at rework 1 versus what is true now.

**No regressions.** `bash tests/check-pii-shapes/run.sh` → 93/93 PASS, 0 FAIL (matches engineer's report). `bash bin/check-pii-shapes.sh --all` → clean. `bash bin/check-pii-shapes.sh --base develop` → clean. `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1101-pii-path-shapes.md` → 18/18 PASS (independently re-run, not accepted from the report). `shellcheck bin/check-pii-shapes.sh` → clean. `bash bin/check-intent.sh` → `aligned: T-1101 v2 (0b080cc23d8710e041f29c68cf5082c9fb40868e)`, unchanged — this rework touched no byte inside the frozen intent block. `bash bin/check-handoff.sh` and `bash bin/check-board-headings.sh --base develop` both exit 0. Re-verified the nine family spellings (round-0's four, round-1's four, `_www` standalone) all still fire and both prior regression locks (round-1's hyphen-only, round-2's three underscore/cross-flavour) all still stay clean, via the suite re-run rather than by re-deriving each by hand a second time — the suite's own fixture set is now the record of this, and I already independently reproduced the specific shapes that matter (the four boundary variants above, plus the three round-2 closures) directly against the live checker myself. Checked `docs/pii-controls.md`/`.ja.md` for an analogous overbroad claim this round should have reworded too — none exists (grepped for "separator run"/"anchor", zero hits outside the one unrelated GitHub-noreply anchoring mention), matching the engineer's own report that no docs-pair edit was owed this round.

#### Blockers

None.

#### Major

None.

#### Minor / nits

None.

#### Adversarial findings

A primary `codex exec review --base 6a67fb7` pass (unguided, reading the round-2-to-round-3 diff directly) found no actionable defects, consistent with my own independent probing. I did not run a separate guided adversarial pass this round — round 2's own guided pass hung on stdin and was abandoned, and this round's own targeted boundary probes (five variants beyond the three closure shapes, all requested explicitly by the round's own message) already constitute a focused adversarial pass against the specific new construction, performed directly by me rather than relayed through a second Codex invocation.

#### Synthesis audit ledger

no severity changes

#### Recommendation

- APPROVE → set `T-1101` to `READY_FOR_MERGE`.

#### Fast-follow disposition

- no fast-follow deferrals
