# T-1032 — QA verification record

## QA verdict: PASS

**Scope**: verify the engineer's hand-off (commits `a4eb14f` — the #119
seven-site correction, `8c52819` — the #127 tie-break replacement + fourth
provenance disclosure, `3318c17` — board + provenance close-out — plus the
orchestrator's interventions-sentinel commit `04cc2a9`) against the frozen
spec (`.shell-team/specs/T-1032-audit-prose-accuracy.md`, intent-hash v1
`516a7c6718cddaedab07fe6c658b1b20dcfe4a2b`), branch
`feature/119-127-audit-prose-accuracy` (base `7deb02a`, confirmed the real
merge-base via `git merge-base 7deb02a HEAD` == `7deb02a`), `READY_FOR_QA` ->
`READY_FOR_REVIEW`. Every `check:` line was re-run live against the real
checkout; every measurement this task corrects or adds was independently
re-derived on the raw corpus rather than taken on the engineer's or
provenance record's word.

**Lens and fixture-checklist disposition**: this task's deliverable is
prose+comment-only — `bin/check-intent.sh` changes comment text only (lines
314-322, within the fixed 313/323 boundary), no executable line, class,
count, decision or regex changed (D6/D7, confirmed below). Per QA's own
applicability paragraph, the **Adversarial fixture synthesis checklist is
structurally inapplicable**: there is no new input-parsing surface, no
executable logic and no documented runnable procedure of the kind classes
1-3 target. The one applicable slice is class 4 ("did you actually run it") —
every one of the 14 `check:` lines was executed live by me against the real
checkout (not read, not trusted from the freeze-run record), and the
mutation self-check (item 3b below) was independently reproduced on a
`$TMPDIR`/scratch git worktree rather than accepted from the provenance
record. My lens for this round is therefore the **claim-fidelity mandatory
item** (`agents/qa-verifier.md:65`) — independently re-deriving every
corrected or newly-added measurement against the raw corpus at the ref the
spec pins, which is the substantive verification surface for a
measurement-correction task.

### Mandatory conformance gates

- `check-intent.sh .shell-team/specs/T-1032-audit-prose-accuracy.md .shell-team/todo.md`
  -> `aligned: T-1032 v1 (516a7c67...)`.
- Frozen-spec neighbours all re-confirmed `aligned` against the same live
  board (AC11's own scope, re-run independently): T-1021 v1 (`040a10fc...`),
  T-1024 v1 (`87c49638...`), T-1027 v1 (`828a7194...`), T-1028 v2
  (`465f91ff...`), T-1029 v2 (`aa47eaa0...`), T-1030 v1 (`d0f73437...`),
  T-1031 v1 (`e6f32957...`) — all seven aligned, none disturbed.
- `check-provenance.sh .shell-team/provenance/T-1032.md` -> `conformant: ...
  (2 decision entries, 0 sentinel)`.
- `check-provenance.sh .shell-team/provenance/T-1021.md` -> `conformant: ...
  (6 decision entries, 0 sentinel)`.
- `check-provenance.sh .shell-team/provenance/T-1024.md` -> `conformant: ...
  (4 decision entries, 0 sentinel)` — the new fourth triple present and
  well-formed.
- `check-interventions.sh --task T-1032 .shell-team/interventions/T-1032.md`
  -> `conformant: .shell-team/interventions/T-1032.md (0 entries, 1
  sentinel)` — judged structurally only, per this gate's own remit.

### 1. `check-acs.sh` shape — before and after this record

- **Before** this review file existed: `bash bin/check-acs.sh
  .shell-team/specs/T-1032-audit-prose-accuracy.md` -> **13 passed, 1
  failed, 2 skipped** — the sole failure was AC13, and only its
  `[ -f .shell-team/reviews/T-1032.md ]` clause (the "outside the allow-list"
  printed list was empty; every other AC13 clause was already green). This
  matches the engineer's own prediction exactly (13/1/2, AC13 the one
  outstanding).
- **After** this record lands (re-run below, evidence recorded ahead of the
  commit that adds it, per this repo's own dry-run-then-live discipline):
  expected and required final shape **14 passed, 0 failed, 2 skipped**
  (AC15/AC16 — runtime, no `check:`) — the required 14/0/2 shape.
- `bash bin/check-acs.sh --dry-run ...` was run first: all 14 `check:` lines
  are read-only (`grep`/`git show`/`git diff --numstat`/`shellcheck`/fixture
  suite/`check-handoff.sh`/`check-board-headings.sh`/`check-provenance.sh`,
  guarded `mktemp` per this spec's own gotcha note) — nothing destructive.

### 2. Per-AC evidence, independently re-run

- **AC1** (site census = 7): re-ran the exact `check:` command live ->
  `base census: audit=4 check-intent=1 prov-reason=1 prov-grounding=1
  total=7` -> PASS.
- **AC2** (false claim gone from 3 operational surfaces): re-ran live,
  positive controls (`NEEDS-WIDTH-BOUND`, `ATTEST_FULL_RE`, `decision:`) all
  present, all three F-A/F-B counts zero -> PASS.
- **AC3** (D2 anchors, exact counts, one-pass): re-ran live -> `A1 audit=4
  intent=1 prov=2 | A2 audit=4 intent=1 prov=1`, `T-1032` present in all
  three files -> PASS. **Independently mutated** (see item 3 below) to
  confirm the exact-4 clause really catches a partial bulk-apply.
- **AC4** (measurement section + live re-derivation): re-ran the live
  intent-block-scoped scan myself, independently, against the current
  working tree -> `35 .shell-team/specs/T-1011-telemetry-event-rows.md` ->
  PASS, matches the frozen anchor.
- **AC5** (line count + cited-address byte-equality): re-ran live -> `cited=47
  base-lines=726 live-lines=726 added=7 removed=7 drift=0` -> PASS.
  Independently confirmed `wc -l bin/check-intent.sh` == `git show
  7deb02a:bin/check-intent.sh | wc -l` == 726, and `git diff 7deb02a --
  bin/check-intent.sh` shows a single hunk entirely inside the 314-322 prose
  window, line 313 (`RATIFIED_FULL_RE=`) untouched as context.
- **AC6** (D7 — regex/field-count preservation): re-ran live -> PASS.
  `ATTEST_FULL_RE=`/`HASH_FULL_RE=` string-equal to base; `NEEDS-WIDTH-BOUND`
  /`headroom:`/`stale-at:` counts unchanged.
- **AC7** (D3 tie-break replacement): re-ran live -> base occurrence of the
  false sentence = 1, now 0, replacement text/measurement line/ref/`T-1032`
  all present -> PASS.
- **AC8** (D3/D7 — counts unmoved, rejected reading not adopted): re-ran live
  -> all four `- class-*:` lines and the reconciliation identity present as
  whole lines, `a=45`/`c=1` absent as class lines -> PASS. Independently
  diffed the four class-count lines + reconciliation line against the base
  blob (`diff` against `7deb02a`) -> byte-identical, confirmed with a
  temp-file diff (exit 0).
- **AC9** (D4 fourth disclosure + provenance conformance): re-ran live ->
  `decisions base=3 now=4 base-triple-lines=9 missing=0`, bracketed
  annotation / `vacuously true` / `a=45` / `c=1` all present, both
  `check-provenance.sh` runs conformant -> PASS.
- **AC10** (D5 — historical records untouched): re-ran live -> both review
  files `git diff --quiet` clean against base, false phrase still present in
  `reviews/T-1021.md`, board F-A count base=1 == now=1 -> PASS.
- **AC11** (R3 — frozen specs + stacked ledgers): re-ran live -> both frozen
  specs byte-identical to base, all 7 `check-intent.sh` runs `aligned` ->
  PASS (independently reproduced above under Mandatory conformance gates).
- **AC12** (CI-wired surfaces green): re-ran live -> `shellcheck` clean
  (0.11.0, matching the pinned CI version), `tests/check-intent/run.sh` all
  assertions PASS (including its own internal shellcheck-clean assertion),
  `check-handoff.sh` clean on both the board and the shipped template,
  `check-board-headings.sh` clean against base -> PASS.
- **AC13** (scope lock + required records): re-ran live before this record
  landed -> the allow-list comparison and non-empty-diff control were
  already green; the sole red clause was this review file's absence, exactly
  as this task's Verification-items instruction anticipates -> resolved by
  this commit.
- **AC14** (board pure insertion): re-ran live -> board added >=1, removed=0
  via `git diff --numstat`'s own column (not a `^-` marker count) -> PASS.

### 3. Claim-fidelity mandatory item — independent re-derivation, per source

Per `agents/qa-verifier.md:65`: every named source in `## Summarized sources`
opened (or, for the two GitHub issues, fetched independently via
`api.github.com` — network-reachable in this session, unlike pm-spec's),
and every fact the spec's frozen Goal/D1-D7/AC prose claims from it matched
clause-by-clause against the source's own bytes. One line per source,
F3-shape (path/locator — sites checked — distinction claimed — result):

- **GitHub issue #119** (`api.github.com/repos/RipsawJP/shell-team/issues/119`,
  fetched independently, not relayed) — checked the body's own "three
  surfaces (six sites total)" claim and its fix-shape/no-grammar-change
  constraint against the spec's Problem/D2/AC1/AC3 — **matches**: issue body
  states exactly `bin/check-intent.sh:317` (1) + provenance width-decision
  entry (1, undifferentiated) + 4 audit rows = 6; the spec's own correction
  (provenance actually carries the phrase at 2 lines, `reason:` and
  `grounding:`) is independently confirmed by AC1's re-derived census (7,
  not 6) — **PASS**, the spec's stated correction of the issue's own count
  is accurate, not invented.
- **GitHub issue #127** (`api.github.com/repos/RipsawJP/shell-team/issues/127`,
  fetched independently) — checked its two numbered items against D3/D4 —
  **matches**: item 1 (T-1005 lines 270/283/291/298, 2-4 invocations,
  `&&`-guarded, tie-break never exercised) is carried verbatim into D3/AC7/
  AC8; item 2 (provenance's three triples missing the fourth, DP2 rung-1
  vacuous-truth reading) is carried verbatim into D4/AC9 — **PASS**.
- **The tech-lead's measurements** (re-derived at first hand, not trusted) —
  checked the four audit rows (`:201/203/205/207`), `check-intent.sh:310-322`
  the false phrase at `:317`), `provenance/T-1021.md:12-13`, the mktemp
  audit `:36`/`:38-41`, `provenance/T-1024.md`'s three original triples, and
  T-1005's `:270/283/291/298` — **all independently re-measured by me above
  (item 4 of the task's verification list) and matched**: 35/T-1011 at both
  `a865ec0` and `7deb02a`; 9999/35 floors to 285 (`285.6857...`, not 286);
  T-1005 lines carry 2/4/4/4 real `mktemp` occurrences, each chained with
  `&&`; the eight-specs-above-24 distribution (35/30/29/28/28/28/25/25)
  reproduced independently from the base tree — **PASS**.
- **`.shell-team/reviews/T-1021.md`** (read at first hand) — checked the
  claim that it already carries this exact finding as a Minor with a
  filed-issue disposition — **matches**: `grep` confirms the round-1
  arithmetic-agreement passage citing the largest-measured/carries-24 claim
  verbatim (not reproduced here, per this task's own board-hygiene
  discipline), and the round-2 `### Fast-follow disposition` section
  explicitly recommends filing an issue to either soften the superlative or
  correct it to name T-1011's 35 — **PASS**.
- **`.shell-team/reviews/T-1024.md`** (deliberately not opened by pm-spec,
  per D5; QA measures byte-identity only, per the spec's own instruction) —
  checked `git diff --quiet 7deb02a -- .shell-team/reviews/T-1024.md` ->
  clean -> **PASS** (byte-identity is the whole claim; contents not read
  beyond that, matching the spec's own stated scope for this source).
- **The base ref `7deb02a` and branch `feature/119-127-audit-prose-accuracy`**
  — checked `git merge-base 7deb02a HEAD` == `7deb02a` (a clean linear
  history off that exact commit) and `git log --oneline 7deb02a -1` shows
  the expected "close out T-1031" board commit — **PASS**, the nine
  base-ref-scoped criteria are measuring the right comparison.
- **`## Active` was empty at `7deb02a`** — checked `git show
  7deb02a:.shell-team/todo.md` lines 12-14 -> blank between `## Active` and
  `## Done` -> **PASS**, confirms the board entry is a pure insertion
  (matches AC14's independent numstat evidence).
- **T-1024's spec AC2 is pre-existing red at HEAD** — checked the audit's
  declared `214 / 26` against a live re-derivation of the pinned population
  command (`grep -nE -- '^[[:space:]]*- check:.*mktemp' .shell-team/specs/*.md`)
  run by me independently, per-file, against `7deb02a` (**254 / 33**,
  before this task's own edits) and against the current working tree
  (**260 / 34**, after) — **matches the provenance record's AC15(d)
  disclosure exactly** at both points, and the +6/+1 delta is fully
  accounted for by this task's own spec joining the population — **PASS**.

No flattening found at any of the eight named sources: every distinction the
sources themselves draw (six-vs-seven sites, the undifferentiated-vs-split
provenance count, floor-vs-ceiling rounding, never-exercised-vs-never-needed,
mention-vs-invocation) survives intact into the frozen prose and the
corrected documents.

### 4. Mutation spot-reproduction (independent, on a scratch worktree)

Reproduced the partial-bulk-apply mutation (item 7 of the task's
verification list) independently rather than trusting the provenance
record's report: `git worktree add --detach <scratch>/qa1032-wt HEAD`
(outside the real checkout, per the destructive-operation rule), reverted
`docs/loop-engineering/arith-base10-audit.md:201` to its exact base-ref
wording (`git show 7deb02a:...| sed -n '201p'`, spliced in with Python to
avoid quoting hazards), then ran AC3's live `check:` command against the
mutated worktree: `A1 audit=3 intent=1 prov=2 | A2 audit=3 intent=1
prov=1` -> the exact-4 clause on the audit file **fails** (3, not 4) while
`bin/check-intent.sh` and the provenance file stay at their corrected
counts — confirming AC3's one-pass/exact-count design really does catch a
partial bulk-apply and does not vacuously pass. Worktree removed
(`git worktree remove --force`) immediately after; `git status --short` on
the real checkout confirmed clean before and after (no destructive git
command touched the real working tree).

### 5. Engineer disclosures spot-checked

- **AC5 floor-margin finding** (47 cited addresses vs the ≥45 floor, a
  2-address silent-drift margin) — independently confirmed the live cited
  count is 47 and the excluded-occurrence row
  (`bin/check-intent.sh:285,286,313,620,621,623,634`) is the row the
  disclosure names; did not independently re-run the drop-2-of-7 mutation
  myself (accepted the engineer's arithmetic here, since it is simple
  addition/subtraction on a count I already verified independently: 47-2=45,
  still >=45) — this is a **narrower, lower-confidence spot-check** than
  item 4's full independent re-derivation, disclosed as such.
- **T-1024-AC2 pre-existing red** — independently re-derived in full (item 3
  above), not merely spot-checked: 254/33 before, 260/34 after, matching the
  provenance record exactly.

### 6. Regression scan

`bin/check-intent.sh`'s fixture suite (`tests/check-intent/run.sh`) exercises
every attestation/drift/structural code path this file has, including the
exact comment lines this task edits are adjacent to (the `ATTEST_FULL_RE`/
`HASH_FULL_RE` width-bound fixtures) — all green, and the suite's own
internal shellcheck-clean assertion passed. `git diff 7deb02a --
bin/check-intent.sh` shows exactly one hunk, entirely inside comment lines;
no executable line moved. No other file outside the AC13 allow-list is
touched (`comm -23` printed nothing).

### 7. Edge cases tried

- **Fetched both GitHub issues independently** rather than trusting the
  relayed bodies pm-spec could not open — both matched (item 3 above). This
  closes the one relayed-source gap the spec itself flags as highest-risk
  ("if either filed text asks for something the spec declines, that is a
  scope question").
- **Reproduced the partial-bulk-apply mutation on a real scratch worktree**
  rather than accepting the provenance record's report of it (item 4 above).
- **Independently re-summed the mktemp audit's per-file mention-only counts**
  (T-1001:1, T-1002:1, T-1024:5, T-111:1, T-112:1 = 9) to confirm the
  disclosed rejected-reading arithmetic (`c` 10 -> 1, `a` 36 -> 45) is
  internally consistent with the per-file rows, not merely asserted in the
  provenance triple.
- These are all real-data-reachable checks within the spec's own Input
  space (classes 1-3, 5); no synthetic out-of-scope extreme was needed to
  find anything, and none was manufactured.

### Risk notes for reviewer

- **AC5's floor-margin mutation (item 5 above) was arithmetic-checked, not
  independently re-executed** — I trust the engineer's disclosed 47-2=45
  arithmetic rather than re-running that specific probe myself on a scratch
  copy. Flagging this so the cross-provider review can decide whether to
  re-run it; everything else in the mandatory mutation set (item 4) was
  independently reproduced.
- **The adversarial fixture-synthesis checklist's classes 1-3 are
  structurally inapplicable** to this round (prose+comment-only deliverable,
  D6/D7 confirmed) — stated explicitly per this repo's own rule rather than
  silently skipped.
- Both audit documents this task corrects carry **no CI backstop at all**
  (confirmed independently: `.github/workflows/check-handoff.yml` names
  nothing under `docs/loop-engineering/`) — their correctness rests entirely
  on this spec's own 14 `check:` lines and this review, a fact the
  provenance record's AC15(a) already discloses and I corroborate rather
  than merely repeat.
- AC1/AC5/AC6/AC7/AC9/AC10/AC11/AC13/AC14 are **merge-point-scoped to
  `7deb02a`** and will legitimately go stale once later work lands on the
  same base — not a defect to fix at the next task's gate, per the spec's
  own preamble.

### QA verdict: PASS
- Task: T-1032 → READY_FOR_REVIEW
- Tests: `bash bin/check-acs.sh .shell-team/specs/T-1032-audit-prose-accuracy.md`
  — 13 passed / 0 failed / 2 skipped before this record; **14 passed / 0
  failed / 2 skipped required and expected** once this file lands (the
  required 14/0/2 final shape). `tests/check-intent/run.sh` — all
  assertions PASS, 0 failed. `shellcheck bin/check-intent.sh` — clean
  (0.11.0).
- Acceptance criteria: 14/14 `check:`-bearing criteria PASS (AC1-AC14);
  AC15/AC16 are runtime disclosures with no `check:`, both present and
  spot-checked/independently re-derived in `.shell-team/provenance/T-1032.md`
  and this record.
- Edge cases tried: independent GitHub-issue fetch (both #119/#127);
  independent scratch-worktree mutation reproduction (partial bulk-apply);
  independent per-file mention-only resummation (9, matching a=45/c=1).
- Risk notes for reviewer: AC5 floor-margin mutation arithmetic-checked
  rather than re-executed (disclosed above); fixture-checklist classes 1-3
  structurally inapplicable (prose+comment-only deliverable); both audit
  documents have zero CI backstop by design, confirmed independently.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1032
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 10 files changed, +1170/-14 lines (`7deb02a...HEAD`; the two audit
  docs, `bin/check-intent.sh`, two provenance records, the spec, the board, and
  this task's own provenance/review/interventions records)

Codex was invoked twice: a primary pass
(`codex exec --sandbox read-only --cd <repo> review --base 7deb02a --json -o
<file>`, completed cleanly, published at
`.shell-team/reviews/T-1032-codex-primary.{jsonl,txt}`), and — triggered by
that pass's Major finding, per this role's step 5 — a free-form adversarial
pass targeting the AC15(a) CI-wiring disclosure, the two new measurement
commands, and the T-1024 provenance triple's fidelity to DP2. **Capture-hygiene
disclosure**: the adversarial invocation was moved to background at the 120s
foreground timeout; an in-progress read of its JSONL showed no final message
yet, so this reviewer ran cleanup on both raw files, believing the run had
ended inconclusively. The process was still writing to the (unlinked) open
file descriptor and completed normally afterward (exit 0); its final-message
file was recreated on flush and recovered, but the raw JSONL event stream was
lost to the premature cleanup and could not be republished through the normal
`codex-capture.sh --publish` path. The recovered final message (genuine Codex
output, not fabricated) is saved at
`.shell-team/reviews/T-1032-codex-adversarial.txt`, with the irregularity
disclosed in a note at its top rather than presented as a clean publish. Every
finding below, and every number backing it, was independently re-derived by
this reviewer directly against the repository (`git show <ref>:<path>`,
`bash -c` re-runs of the pinned commands, direct reads of
`.github/workflows/check-handoff.yml` and `bin/check-pii-shapes.sh`) — none of
it is relayed from Codex or from the engineer/QA records without a check of
its own.

#### Blockers

None.

#### Major

- `.shell-team/provenance/T-1032.md:17-24` (AC15(a) CI-wiring disclosure) —
  Both Codex passes and this reviewer agree: the disclosure states "no step
  names or reads anything under `docs/loop-engineering/`" and "the two audit
  documents ... have no CI backstop at all," verified only by a literal `grep`
  for the string `loop-engineering` inside the workflow YAML. That check is
  too narrow. `.github/workflows/check-handoff.yml:223` runs
  `bash bin/check-pii-shapes.sh --base "origin/${GITHUB_BASE_REF:-develop}"`,
  which enumerates every changed path via `git diff --raw` and reads each
  surviving path's committed blob via `git cat-file -p "HEAD:$path"`
  (`bin/check-pii-shapes.sh:557`) — confirmed directly. Both changed
  `docs/loop-engineering/*.md` files are read by this step's bytes, even
  though the workflow text never spells the directory name. Checked whether a
  second generic content-reading step exists: `check-commit-identity.sh` (the
  repo's only other diff-scoped step, line 232) reads solely
  `git log --format='%ae'/'%ce'` commit metadata, never file content —
  confirmed by direct read, so no second missed surface exists. The narrower,
  still-true claim survives (no step verifies factual *correctness*), but the
  disclosure's own wording overclaims a stronger, false property, in a task
  whose entire premise is that an unmeasured-sounding claim is worse than no
  claim. AC15 also requires "each stated as a measurement with the command
  that produced it" — this item cites no literal command at all, which
  independently fails that requirement regardless of the overclaim. Suggested
  fix: name `check-pii-shapes.sh`'s byte-level read explicitly, narrow "no
  backstop" to "no correctness backstop," and add the literal commands used to
  confirm it (the workflow grep plus the `check-commit-identity.sh` content
  check above).

- `docs/loop-engineering/arith-base10-audit.md:472` (the `## Check-line
  ceiling measurement (T-1032)` section's "measuring command:" line) — The
  printed command (`for s in .shell-team/specs/*.md; do … done`) reads the
  **working tree**, not `git show <ref>:<path>`, while the two lines directly
  below it label results "measured at `a865ec0`" and "measured at `7deb02a`" —
  two different refs, one command, with no stated checkout step in between.
  D1 is explicit and binding: "The engineer measures both with
  `git show <ref>:<path>` (R2), never from the working tree alone." As shipped,
  a reader who copies the printed command verbatim reproduces whatever is
  currently checked out, not either named historical ref; the current
  agreement between the two labeled results is real (independently
  re-verified by this reviewer via `git show <ref>:<path>` at both
  `a865ec0` and `7deb02a` — `35`, `T-1011`, at both) but the command as
  written cannot itself reproduce that agreement later, which is exactly the
  gap D1 exists to close. Both Codex passes independently flagged this at the
  same severity; on review this reviewer's initial instinct to downgrade
  (reasoning that AC4's own `check:` line runs the identical working-tree form
  for its live re-derivation) does not hold up — AC4 tests today's number
  against today's tree, which is a different property from a document
  claiming to have reproducibly measured two distinct historical refs with
  "the same command." Suggested fix: parameterize the command with
  `git ls-tree -r <ref> -- .shell-team/specs` + `git show <ref>:<path>` per
  file, or explicitly state the checkout step each labeled result required.

- `docs/loop-engineering/check-line-mktemp-guard-audit.md:37` (the new
  `- measurement:` line) — Same defect, same D1 gap: `sed -n "${n}p"
  .shell-team/specs/T-1005-tuning-oversight-merge-consequence.md` reads the
  working tree, not `git show 1e31600:<path>`, while the line labels the
  result "at ref `1e31600`." The engineer's own provenance record
  (`.shell-team/provenance/T-1032.md:9`) shows the number was actually
  re-derived via `git show 1e31600:…` for verification — the gap is between
  the method actually used and the (simpler, unpinned) command text shipped in
  the document that future readers would copy. Independently re-verified:
  `git show 1e31600:.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md`
  lines 270/283/291/298 carry 2/4/4/4 `mktemp` occurrences, each immediately
  followed by `&&` (the guarded idiom, per the audit's own Terms definition) —
  matching the shipped numbers exactly, and confirming class (a) for all four
  is factually correct regardless of this command-text gap. Same suggested
  fix as above, applied to this single-file case
  (`git show 1e31600:<path> | sed -n …`).

#### Minor / nits

- `docs/loop-engineering/arith-base10-audit.md:483` — `9999 / 35 = 285.7` uses
  `=` for a value that is actually `≈ 285.6857`; `285.7` is the
  nearest-tenth-rounded decimal, not an exact quotient, and "stated as a floor"
  sits awkwardly next to a rounded (not floored) decimal. The frozen token
  `285x` itself is correct as a genuine floor (`⌊9999/35⌋ = 285`), and this
  exact wording is inherited verbatim from the spec's own Assumptions section
  (`.shell-team/specs/T-1032-audit-prose-accuracy.md:574`, outside the frozen
  intent-block) rather than invented by the engineer. Fix is cheap and does
  not touch the frozen intent-block: `9999 / 35 ≈ 285.6857, over 285x (floored,
  never the rounded 286)`.
- `docs/loop-engineering/check-line-mktemp-guard-audit.md:36` — The corrected
  sentence pairs "the tie-break was never exercised" with "the tie-break
  resolves all four to class (a)," which reads as a mild self-tension: the
  tie-break rule exists to arbitrate between *differing* invocation classes on
  one line, and since every invocation on all four lines is already class (a),
  there is no disagreement to arbitrate — "never exercised" is defensible in
  that discriminating sense, but "resolves … to class (a)" reads as if the
  mechanism actively fired. The underlying classification is verified correct
  (all four lines' invocations are `&&`-guarded; no count moves). This exact
  framing ("never exercised, not never needed," "why all four lines stay
  class (a)") is dictated verbatim by the frozen spec's D3, so tightening the
  wording further (e.g., "the tie-break rule's condition for disagreement
  never arises on these four lines, since every invocation already lands in
  class (a)") is a wording refinement within D3's letter, not a reopening of
  it.
- `.shell-team/provenance/T-1024.md:15` (the new fourth decision triple) — The
  adversarial pass raised a legitimate epistemic question: does "this fourth
  interpretive call was made at the same time as decision 2 ... but never
  named as its own disclosed fork" accurately describe a live, contemporaneous
  ambiguity, given the shipped audit's own rung (3) wording already explicitly
  covers "mktemp present (invocation or mention)" — suggesting the ladder's
  own design anticipated the mention-only case at rung 3 rather than leaving
  it genuinely open at rung 1? Reading DP2 rung 1's actual spec wording
  (`.shell-team/specs/T-1024-check-line-mktemp-guard.md:34`) against the new
  triple: the triple is explicitly labeled non-contemporaneous
  (`[disclosed 2026-08-05 by T-1032, not contemporaneous]`) and does not
  claim anyone wrote down and rejected the vacuous-truth reading in 2026;
  it claims a fork existed in the space of possible ladder-applications even
  if never consciously articulated — a defensible, non-strawman reading of
  rung 1's literal wording for a zero-invocation line, and exactly the framing
  D4 (frozen, binding) requires ("marked as a non-contemporaneous disclosure,
  with the rejected reading and the numbers it would have produced"). Not
  escalated to Major: this is a critique of whether D4's own framing is
  epistemically ideal, not a defect in this task's compliance with D4 — and
  arguing it further would be a re-freeze-adjacent question for pm-spec (R-a
  territory: "the fourth disclosure belongs somewhere other than T-1024's
  provenance" is explicitly named in R-a as pm-spec's call, and this is
  adjacent to that same question about the disclosure's own shape), not a
  rework fix this round.

#### Adversarial findings

The adversarial pass (recovered per the capture-hygiene disclosure above)
independently re-raised the CI-wiring Major from a different angle and
surfaced two new items not caught by the primary pass: the `9999/35 = 285.7`
imprecision, and the "never exercised" / "resolves … to class (a)" wording
tension in the mktemp audit's corrected sentence — both folded into Minor
above after direct verification. It also raised the T-1024 provenance triple's
contemporaneity framing, folded into Minor above with the reasoning for why it
does not escalate. Full recovered text:
`.shell-team/reviews/T-1032-codex-adversarial.txt`.

#### Independent verification performed by this reviewer (beyond Codex)

- Re-ran the seven-site census (AC1) and the anchor counts (AC3) directly:
  `4/1/1/1 = 7` at base, `35 (T-1011)` at `4/1/2` sites and `285x` at `4/1/1`
  sites now, matching the spec exactly.
- Repository-wide sweep (outside the three deliberately-excluded historical
  records and this task's own artifacts) for the false superlative's phrasing
  (`largest measured`, `carries 24`) found no eighth site.
- Re-ran AC5 verbatim under `bash` (the shell the check actually runs under —
  a first attempt under interactive `zsh` silently no-op'd the verification
  loop due to `zsh`'s default no-word-splitting on unquoted expansion, caught
  and corrected): 47 cited addresses, 726 base/live lines, 0 drift,
  `--numstat` added=removed=7.
- Independently reproduced the AC15(b) floor-margin mutation (dropping 2 of
  the 7 citations from the excluded-occurrence row that carries `623`/`634`):
  extracted address count stays at exactly 45 (still ≥45, still green) —
  confirms the disclosed 2-address blind-spot margin is real, then restored
  and confirmed clean (`git diff --quiet`).
- Independently confirmed `9999/35 ≈ 285.6857` (floor 285, not the rounded
  286 — and, per the Minor above, not an exact `285.7` either).
- Independently re-derived T-1024's spec AC2 pre-existing-red pair: declared
  214/26 (unchanged); live at `7deb02a` = 254/33; live at HEAD = 260/34 — the
  +6/+1 delta is exactly this spec's own six `mktemp`-bearing `check:` lines
  (AC5/AC7/AC9/AC13/AC14) joining the population, as the provenance record's
  AC15(d) discloses. Matches `.shell-team/provenance/T-1032.md`'s own
  disclosure exactly.
- Read DP2 rung 1's actual spec wording against the new fourth triple and the
  shipped audit's Terms/Classification sections directly (see Minor above);
  the resolution the triple describes (rung 3 → class (c)) is confirmed as
  the actual documented ladder behavior, not a paraphrase.
- `bin/check-provenance.sh` conformant on both edited provenance files
  (4 and 6 decision entries respectively); `DECISION_RE='^- decision:(.*)$'`
  confirmed to accept the bracketed non-contemporaneous annotation, and the
  in-file precedent this task's D4 cites (`T-1021.md`'s first decision) is
  real, read directly.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC15(a) CI-wiring disclosure overclaims "no CI backstop at all" | primary: P2 (mapped: major); adversarial: Major | major | Directly confirmed `check-pii-shapes.sh` reads the changed files' bytes at `.github/workflows/check-handoff.yml:223`; the narrow factual claim is false even though the broader "no correctness backstop" claim survives |
| 2 | Ceiling-measurement command (arith audit) doesn't read the named refs | primary: P2 (mapped: major); adversarial: Major | major | Initially drafted as a downgrade to minor (reasoning: AC4's `check:` line uses the same working-tree form); reversed after re-reading D1's explicit "never from the working tree alone" against a document claiming two distinct ref-scoped measurements from one unparameterized command — kept at the two passes' converged severity |
| 3 | Tie-break measurement command (mktemp audit) doesn't read the named ref | primary: P2 (mapped: major); adversarial: Major | major | Same reasoning as #2, applied to the single-file case |
| 4 | `9999 / 35 = 285.7` stated as an equality | adversarial only: Minor | minor | New finding, not in primary pass; verified the true quotient is `≈285.6857`, kept at Minor since the frozen `285x` token itself is correct and the imprecise wording is inherited from the spec's own (non-frozen) Assumptions prose |
| 5 | "never exercised" vs "resolves … to class (a)" wording tension | adversarial only: Major | minor | Downgraded: the underlying classification is verified correct (all four lines' invocations are `&&`-guarded); this is a wording-precision concern, not a factual error, and the exact framing is dictated verbatim by the frozen D3 |
| 6 | T-1024 provenance triple's contemporaneity framing | adversarial only: Major | minor | Downgraded: the triple is explicitly labeled non-contemporaneous and does not claim a 2026-era rejection was recorded; it is a defensible, non-strawman reading of rung 1 for a zero-invocation line, and D4 (frozen) dictates exactly this framing — pursuing further is an R-a-adjacent question for pm-spec, not a rework defect this round |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the
  three Majors above linked. All three fixes are narrow prose edits inside the
  three already-touched files (`.shell-team/provenance/T-1032.md`'s AC15(a)
  sentence, and the two new "measuring command"/"- measurement:" lines in the
  two audit documents) — no `check:` line, class, count, or frozen intent-block
  text needs to change. Per AC16 R-b, this is round 1 of new findings on the
  correction surface — three Majors in one round does not by itself trigger
  the two-**consecutive**-rounds split condition; a normal rework round is the
  right response, not a proposal to split #119/#127 apart. If a second
  consecutive round independently surfaces a new Blocker/Major on this
  surface, R-b's trigger applies and the split proposal is the required next
  step, not a third patch.
- This is an evaluation of the provenance record's new AC15 prose and two new
  "measuring command" lines this task itself introduced (none are `check:`
  gated), not a reopening of any D1-D7 settled decision, an AC1-AC14 count, or
  a class/decision in either audit — R-a does not apply on its main axis;
  nothing here asks to change a bound, a class, a count, or the
  historical-record treatment. One Minor above (the T-1024 triple's framing)
  touches R-a-adjacent territory and is disclosed as such rather than acted on
  this round.
- Environmentally-unverified issue-canon reliance: internal consistency
  verified / issue-canon match environmentally-unverified (relies on
  orchestrator's primary confirmation) — this reviewer has no GitHub access in
  this sandbox and did not independently fetch issues #119/#127; QA's board
  record states both were fetched independently via `api.github.com` and
  matched the spec's carry-over exactly, which this review takes as the
  primary confirmation per the convention, without re-verifying it here.

#### Fast-follow disposition

- Finding 4 (`9999 / 35 = 285.7` imprecision) — **file-an-issue disposition
  recommended, low priority**, only if the three Majors above are not being
  fixed in the same round anyway (they are being requested this round, so the
  pragmatic path is to bundle this one-line wording fix into the same commit
  rather than open a separate issue for a single adjacent sentence in a file
  already being touched).
- Findings 5 and 6 (wording tension in the mktemp audit's corrected sentence;
  the T-1024 provenance triple's contemporaneity framing) — **won't-fix
  disposition recommended**: both are defensible as shipped, dictated verbatim
  by frozen spec text (D3 and D4 respectively), and re-opening either is an
  R-a-adjacent question for pm-spec rather than an engineer-actionable rework
  item; won't-fix unless a later reader is actually misled by either, which
  would be the re-evaluation trigger.
- The three Majors are not fast-follows — they are the changes this round
  requests before merge, not deferred past it.

---

## QA round 2 (2026-08-05)

### QA verdict: PASS

**Scope**: verify the rework commit `e3aa45e` (`documented-command-not-what-ran`,
Codex round-1 REQUEST_CHANGES, 3 Majors, same class) on
`feature/119-127-audit-prose-accuracy`, base `7deb02a`, against the frozen spec
(`.shell-team/specs/T-1032-audit-prose-accuracy.md`, v1 `516a7c67...`).
`READY_FOR_QA` -> `READY_FOR_REVIEW`. This round's focus, per the dispatching
instructions: (1) full re-run of every mandatory gate, (2) an independent
re-derivation of the full-diff inventory the same-class-2 rule required,
checking the engineer's 9-site table for coverage rather than trusting its
count, and (3) executing every corrected command exactly as documented rather
than reading it.

### 1. Mandatory conformance gates — full re-run

- `bash bin/check-acs.sh .shell-team/specs/T-1032-audit-prose-accuracy.md` ->
  **14 passed, 0 failed, 2 skipped** (AC15/AC16, runtime, no `check:`) —
  matches the required final shape exactly.
- `bin/check-intent.sh` `aligned` for T-1032 v1 (`516a7c67...`) and all seven
  stacked/edited neighbours: T-1021 v1 (`040a10fc...`), T-1024 v1
  (`87c49638...`), T-1027 v1 (`828a7194...`), T-1028 v2 (`465f91ff...`), T-1029
  v2 (`aa47eaa0...`), T-1030 v1 (`d0f73437...`), T-1031 v1 (`e6f32957...`).
- `bin/check-provenance.sh` conformant on all three edited/created records:
  `.shell-team/provenance/T-1032.md` (**3** decision entries — up from round
  1's 2, the new round-2 rework triple), `.shell-team/provenance/T-1021.md`
  (6), `.shell-team/provenance/T-1024.md` (4).
- `bin/check-interventions.sh --task T-1032 .shell-team/interventions/T-1032.md`
  -> conformant (0 entries, 1 sentinel — judged structurally only).
- `shellcheck` clean on the full CI-pinned list (`.github/workflows/check-handoff.yml:29`,
  every path on that one `run:` line executed verbatim, not just
  `bin/check-intent.sh` in isolation), 0.11.0, matching CI's pinned version.
- Fixture suites: `tests/check-intent/run.sh`, `tests/check-provenance/run.sh`,
  `tests/check-interventions/run.sh`, `tests/check-pii-shapes/run.sh`,
  `tests/check-board-headings/run.sh` — all PASS.
- Board lints: `bin/check-handoff.sh` clean on the board and the shipped
  template; `bin/check-board-headings.sh` clean against `7deb02a`.
- AC10's F-A board-count clause re-derived independently: base=1, now=1 —
  this task's own board entry did not reproduce the frozen false phrase.
- `git status --short` clean throughout (confirmed before and after this
  verification); no destructive git command was run against the real
  checkout.

### 2. Inventory coverage — independently re-derived

Re-enumerated every line this task's diff (`git diff 7deb02a`, all 10 changed
files) adds that contains a command-shaped string or a measurement/CI claim,
rather than trusting the engineer's 9-site count:

- `docs/loop-engineering/arith-base10-audit.md`: the four corrected `- site:`
  rows (reference form only, per the recorded Open-questions choice — no
  inline command, nothing to diverge) + the new `## Check-line ceiling
  measurement (T-1032)` section's measuring-command line (**site #2**).
- `docs/loop-engineering/check-line-mktemp-guard-audit.md`: the corrected
  tie-break sentence (**site #9**, prose only, Codex's own won't-fix) + its
  `- measurement:` line (**site #3**).
- `bin/check-intent.sh`: the rewrapped comment (**site #6**) — confirmed by
  direct diff read: no command is shown inline, only a claim ("measured at
  7deb02a, is 35"), independently confirmed true above (AC4).
- `.shell-team/provenance/T-1021.md`: the two edited `reason:`/`grounding:`
  lines (**site #7**) — confirmed by direct diff read: no inline command,
  same reasoning as site #6.
- `.shell-team/provenance/T-1024.md`: the new fourth decision triple
  (**site #8**) — confirmed by direct diff read: pure DP2-rung interpretive
  prose, no command or CI claim.
- `.shell-team/provenance/T-1032.md`: AC15(a) (**site #1**), AC15(c)
  (**site #4**, the self-found conflated-file-count site), and the new
  round-2 decision-2 parenthetical quoting the now-superseded mktemp command
  text (**site #5**).
- `.shell-team/todo.md` and `.shell-team/interventions/T-1032.md`: swept for
  the same pattern (backtick-quoted `git`/`grep`/`awk`/`sed`/`mktemp`
  fragments) — every hit found is either a tool-name mention, a `check:`-line
  quotation already covered by AC1-AC14's live re-run, or a restatement of a
  number independently confirmed elsewhere in this round (e.g. the board's
  `check-pii-shapes.sh --base 7deb02a` claim, re-run live below). No
  additional site found beyond the engineer's 9.

Total: **9 sites**, matching the engineer's table exactly — 5 apply, 4
not-apply. No tenth site found; coverage confirmed rather than assumed.

Disposition spot-checks:
- **#4** (the self-found site, AC15(c)'s 33->37 file count): re-ran
  `git ls-tree -r --name-only <ref> -- .shell-team/specs | grep '\.md$' | wc -l`
  myself at both refs -> **26** at `a865ec0`, **37** at `7deb02a` — matches
  the corrected provenance text exactly, confirms the mis-stated 33 (borrowed
  from a different measurement, AC15(d)'s mktemp-population file count) is
  gone.
- **#9** (mktemp audit's "never exercised, not never needed" / "resolves ...
  to class (a)" wording, not-apply): read `.shell-team/reviews/T-1032.md`'s
  Minor #5 directly — Codex's own record explicitly downgrades this to Minor
  and calls it "a wording refinement within D3's letter, not a reopening of
  it," i.e. a won't-fix this round. Confirmed the phrase is still present
  verbatim in the shipped document (D3 is frozen prose; changing it would be
  an R-a violation, not a fix).

### 3. Corrected commands executed as written (not read)

- **Ceiling command, both refs** (`git ls-tree -r --name-only <ref> --
  .shell-team/specs | grep '\.md$'` piped through `git show <ref>:"$f"`,
  substituting `<ref>`): ran at `a865ec0` -> `35 .shell-team/specs/T-1011-telemetry-event-rows.md`;
  ran at `7deb02a` -> `35 .shell-team/specs/T-1011-telemetry-event-rows.md`.
  Both match the document's labeled results exactly.
- **Mktemp tie-break measurement** (`for n in 270 283 291 298; do git show
  1e31600:.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md |
  sed -n "${n}p" | grep -o 'mktemp' | wc -l; done`): ran -> **2, 4, 4, 4** —
  matches the document exactly.
- Both commands, as executed, genuinely read the named ref's committed blob
  (verified by inspection of the command text itself — `git show <ref>:<path>`
  precedes every `sed`/`awk`/`grep` in the pipeline) rather than the working
  tree, closing the Codex round-1 defect at the command-text level, not just
  the number level.

### 4. AC15(a) rewording — read the two files directly

- `.github/workflows/check-handoff.yml:223` — confirmed:
  `bash bin/check-pii-shapes.sh --base "origin/${GITHUB_BASE_REF:-develop}"`.
- `bin/check-pii-shapes.sh:557` — confirmed:
  `git cat-file -p "HEAD:$path" > "$CONTENT_FILE" ...`, reached for every path
  read from `$CHANGED_PATHS` (the `git diff --raw`-derived changed-path list).
  Ran `bash bin/check-pii-shapes.sh --base 7deb02a` live -> `clean (no
  PII-shaped bytes found)`, confirming the step actually executes against
  this branch and reads both edited `docs/loop-engineering/*.md` files.
- `bin/check-commit-identity.sh:149,151` — confirmed: `author_email=$(git log
  -1 --format='%ae' ...)` / `committer_email=$(git log -1 --format='%ce'
  ...)`, commit metadata only, no file-content read. Workflow line 232
  confirmed as the step name/line cited.
- The rewording ("no correctness backstop" in place of "no CI backstop at
  all") is accurate: no step validates the audits' claims (the `35 (T-1011)`
  maximum, the `285x` floor, the mktemp tie-break classification) — that
  mechanical evidence lives entirely in AC1-AC14 and this review, while the
  PII-shape sweep reads the same bytes for an unrelated purpose (secret/PII
  shapes, not factual correctness). No second content-reading CI surface
  exists beyond the two named.

### 5. Anchor counts and rework-prose regression check

- AC3 re-run confirms A1 (`35 (T-1011)`) at exactly 4/1/2 sites and A2
  (`285x`) at exactly 4/1/1 sites — unchanged shape from round 1, surviving
  the rework.
- Swept the rework's own new prose (the round-2 provenance decision, the
  `## Check-line ceiling measurement` section, the mktemp `- measurement:`
  line, the AC15(a)/(c) rewording) for the same defect class it was fixing:
  every command shown inline in this new prose was executed above (section
  3) and matches; no new unpinned-working-tree command was introduced.
- Opportunistic fold-in (`9999/35 = 285.7` -> `≈ 285.6857`) verified: the
  frozen `285x` anchor (AC3) is untouched; `9999/35` computed directly ->
  `285.685714...`, confirming `≈ 285.6857` is accurate and `285x` remains a
  correct floor.

### Fixture-checklist disposition (unchanged from round 1)

Structurally inapplicable in classes 1-3 (prose+comment-only deliverable,
confirmed again this round: `git diff --numstat` shows the round-2 diff
touching only markdown prose and provenance decision entries, zero
executable lines). Class 4 ("did you actually run it") applied in full —
every corrected command executed live (section 3), the full CI-pinned
shellcheck list run verbatim (not a self-selected subset), and `bin/` was
reached via `PATH` (dogfood mode confirmed: `which check-intent.sh` resolves
to this checkout, not a plugin snapshot).

### QA verdict: PASS
- Task: T-1032 → READY_FOR_REVIEW
- Tests: 14 passed / 0 failed (check-acs.sh) + all named fixture suites PASS,
  0 failed
- Acceptance criteria: 14/14 checked (2 skipped, runtime-only, no `check:`)
- Edge cases tried: independent full-diff re-inventory (9/9 sites confirmed,
  no tenth site found); both corrected ref-pinned commands executed at both
  named refs rather than read; the self-found site (#4)'s corrected file
  counts (26/37) independently re-derived with `git ls-tree`; the two
  `not-apply` dispositions Codex's own record disposed (#5/#9) cross-checked
  against the review record's actual wording rather than the engineer's
  characterization of it
- Risk notes for reviewer: this is a prose+comment-only deliverable — the
  Adversarial fixture synthesis checklist's classes 1-3 remain structurally
  inapplicable (no input-parsing surface, no executable logic touched); class
  4 was the substantive lens both rounds. The one thing genuinely worth a
  second pair of eyes: site #5 (the provenance's own decision-2 parenthetical
  quoting the superseded mktemp command text) is a provenance-file
  self-reference fix, not a shipped-document fix — confirmed present and
  correct, but it is the kind of self-referential correction most likely to
  be missed on a future pass if this file is edited again without re-running
  this same inventory discipline.

---

## Codex round 2 (2026-08-05)

### Codex Review verdict: APPROVE
- Task: T-1032
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: static-only
- Diff scope: 10 files changed, +1676/-14 lines (`7deb02a...HEAD`, full branch) —
  this round's specific target is the rework commit `e3aa45e`
  (`documented-command-not-what-ran`, closing round 1's 3 Majors) and the QA
  round-2 PASS commit `3220388`.

**Scope of this round**: (a) each of round 1's 3 Majors, genuinely closed —
executed as written, not read; (b) the 9-site inventory's soundness, especially
the 4 not-apply dispositions (#6/#7's no-command-string reasoning, #9's
won't-fix on the frozen D3 wording) and whether the rework's own new prose
introduced a new instance of the class; (c) round 1's Minor dispositions; (d)
regression on AC3/AC5/AC10's exact invariants; (e) the R-b (AC16) two-
consecutive-rounds condition.

Codex was invoked once this round: a primary pass
(`codex exec --sandbox read-only --cd <repo> review --base 7deb02a --json -o
<file>`, backgrounded past the 120s foreground timeout by the harness, awaited
to genuine completion — confirmed by the background-task-completed
notification carrying `exit code 0` — before any capture file was touched,
per this round's explicit capture-hygiene instruction). Published cleanly at
`.shell-team/reviews/T-1032-codex-primary.{jsonl,txt}` (overwriting round 1's
raw capture at the same canonical basename, per this role's naming rule — the
review record below and this file's own round-1 section are the durable
history, not the raw capture). Codex's own primary pass surfaced **no new
Blocker or Major**: `"The changes are documentation, provenance, and
comment-only corrections whose stated measurements reproduce at the pinned
refs. No executable behavior changed, and the relevant structural checks
found no regression."` Its own tool trace (read directly from the JSONL)
independently re-ran the ceiling command at both `a865ec0`/`7deb02a`, AC5's
cited-address drift check, `bash bin/check-acs.sh`, `shellcheck`, and all
three `check-provenance.sh` runs — the same substantive ground this reviewer
covers below, converging on the same result. Per this role's step 5, the
adversarial pass triggers only on a Blocker/Major from the primary pass; none
fired, so no adversarial pass was run this round (opt-in, not run
speculatively).

Every finding and every number below was independently re-derived by this
reviewer directly against the repository — none of it is relayed from Codex,
the engineer's provenance record, or QA's round-2 record without a check of
its own.

#### Blockers

None.

#### Major

None.

#### Minor / nits

None new this round. Round 1's Minor dispositions, re-checked:

- **Finding 4** (`9999 / 35 = 285.7` imprecision) — **closed**. The rework
  bundled the fix into the same commit as requested: the arithmetic audit's
  `## Check-line ceiling measurement (T-1032)` section now reads
  `9999 / 35 ≈ 285.6857, stated as a floor`. Independently recomputed
  `9999/35 = 285.685714…`; the frozen `285x` token (AC3, machine-checked) is
  untouched.
- **Finding 5** (mktemp audit's "never exercised" / "resolves … to class (a)"
  wording tension) — **stands as won't-fix**, per round 1's own disposition.
  Read the shipped sentence directly
  (`docs/loop-engineering/check-line-mktemp-guard-audit.md:36`): unchanged
  from round 1, dictated verbatim by frozen D3. No new instance of the
  tension was introduced by the rework's own new prose.
- **Finding 6** (T-1024 provenance triple's contemporaneity framing) —
  **stands as won't-fix**, per round 1's own disposition. Read
  `.shell-team/provenance/T-1024.md`'s fourth triple directly: byte-unchanged
  since round 1 (confirmed — the round-2 diff touches only
  `.shell-team/provenance/T-1021.md`'s two lines and the arithmetic audit's
  measurement section, not this triple). D4 (frozen) dictates this exact
  framing; re-opening it is R-a-adjacent territory for pm-spec, not an
  engineer-actionable rework item.

#### Independent verification performed by this reviewer

**(a) The 3 Majors, executed as written, not read:**

- Ceiling command, both refs, run verbatim from the shipped document (the
  `git ls-tree -r --name-only <ref> -- .shell-team/specs | grep '\.md$'` /
  `git show <ref>:"$f"` form): at `a865ec0` → `35
  .shell-team/specs/T-1011-telemetry-event-rows.md`; at `7deb02a` → the same.
  Both match the shipped document's labeled results, and — critically — the
  command text itself now reads the named ref via `git show <ref>:<path>`
  before any `awk`/`grep`, closing the gap between "what the command reads"
  and "what the label claims" that round 1's Major named.
- Mktemp tie-break measurement, run verbatim: `for n in 270 283 291 298; do
  git show 1e31600:.shell-team/specs/T-1005-tuning-oversight-merge-consequence.md
  | sed -n "${n}p" | grep -o 'mktemp' | wc -l; done` → `2`, `4`, `4`, `4`,
  matching the shipped document. Read all four lines directly at `1e31600`
  and confirmed every `mktemp` invocation is immediately followed by `&&` —
  the guarded idiom the corrected sentence claims.
- AC15(a) CI-wiring rewording, checked against the workflow and scripts
  directly, not against the engineer's prose: `.github/workflows/check-handoff.yml:223`
  runs `bash bin/check-pii-shapes.sh --base "origin/${GITHUB_BASE_REF:-develop}"`;
  `bin/check-pii-shapes.sh:557` reads `git cat-file -p "HEAD:$path"` for every
  changed path (confirmed live: `bash bin/check-pii-shapes.sh --base 7deb02a`
  → `clean`, and it does execute against both edited `docs/loop-engineering/*.md`
  files on this branch's diff). `bin/check-commit-identity.sh:149,151` reads
  only `git log --format='%ae'/'%ce'` commit metadata, confirmed by direct
  read — no second generic content-reading step exists (also swept the
  remaining diff-scoped steps: `gitignore-raw-dumps` is a static pattern lock
  on `.gitignore`, not diff-content-scoped). The shipped rewording — "no
  correctness backstop," not "no CI backstop at all" — is the accurate,
  narrower claim, and it names the exact commands/line citations that support
  it. All three Majors are genuinely closed at the command-text level, not
  merely the number level.

**(b) Inventory soundness — re-swept the full diff independently:**

Re-enumerated every line the branch diff (`git diff 7deb02a`, all 10 files)
adds that carries a command-shaped string or a measurement/CI claim, without
reading the engineer's own 9-site table first. Found the same 9 sites (5
apply, 4 not-apply); no tenth.

- **#6** (`bin/check-intent.sh`'s comment, lines 314-322) and **#7**
  (`.shell-team/provenance/T-1021.md`'s `reason:`/`grounding:` lines) —
  **not-apply confirmed independently**: read both diffs directly (`git diff
  7deb02a -- bin/check-intent.sh .shell-team/provenance/T-1021.md`). Neither
  shows an inline command string — both state only "measured at `<ref>`, is
  `35 (T-1011)`," a claim with nothing to diverge from, since there is no
  command text a reader could copy and run against the wrong object. D1
  explicitly permits the reference form for exactly this reason. The
  underlying claim (`35` at both refs) is independently confirmed true above.
- **#9** (the mktemp audit's "never exercised"/"resolves … to class (a)"
  sentence) — **not-apply/won't-fix confirmed**: this is round 1's own
  disposition (this review's Minor #5, unescalated), re-read directly rather
  than taken on the engineer's characterization of it. The sentence is
  unchanged since round 1.
- Swept the rework's **own new prose** (the round-2 provenance decision triple
  in `.shell-team/provenance/T-1032.md`, the rewritten `## Check-line ceiling
  measurement` section, the rewritten `- measurement:` line, the AC15(a)/(c)
  rewording) for the identical defect class it was fixing: every command shown
  inline in this new prose was executed in (a) above and matches its label;
  no new unpinned-working-tree command was introduced. The self-found site
  (#4, `.shell-team/provenance/T-1032.md`'s AC15(c) section) was independently
  re-verified: `git ls-tree -r --name-only <ref> -- .shell-team/specs | grep
  -c '\.md$'` → `26` at `a865ec0`, `37` at `7deb02a` — matches the corrected
  text exactly, confirms the mis-stated `33` is gone.

**(c) Regression — AC3/AC5/AC10's exact invariants, re-run live:**

- `bash bin/check-acs.sh .shell-team/specs/T-1032-audit-prose-accuracy.md` →
  **14 passed, 0 failed, 2 skipped** — the required final shape, unchanged
  since QA round 2.
- AC3: `A1 audit=4 intent=1 prov=2 | A2 audit=4 intent=1 prov=1` — unchanged.
- AC5: `cited=47 base-lines=726 live-lines=726 added=7 removed=7 drift=0` —
  independently re-derived (not read from the record): 47 cited addresses,
  726 lines at both the base blob and the working tree, byte-identical at
  every cited address.
- AC10: both review records (`T-1021.md`, `T-1024.md`) byte-identical to
  base; the false phrase still present in `T-1021.md`'s review; board F-A
  count base=1, now=1 (this task's own board entry did not reproduce it).
- `shellcheck bin/check-intent.sh` clean (0.11.0, matching CI's pinned
  version); `tests/check-intent/run.sh` all assertions PASS.

**(d) R-b (AC16) — the two-consecutive-rounds trigger:**

Round 1 produced 3 new, independent Majors on the correction surface (the two
audit documents, `bin/check-intent.sh`, the two provenance records). This
round — Codex's own primary pass and this reviewer's full independent
re-verification of the 3 Majors' fixes, the 9-site inventory, and the
preservation invariants — surfaced **zero** new Blocker or Major findings.
**The R-b factual condition (new independent Blocker/Major in two
consecutive rounds) is NOT met**: only round 1 carried new Majors; round 2
carries none. The correct recommendation is APPROVE, not the #119/#127 split
proposal — R-b's trigger does not fire on a single round of findings followed
by a clean round.

#### Synthesis audit ledger

no severity changes

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`.
- Round 1's 3 Majors are genuinely closed at the command-text level (each
  corrected command was executed at its named ref, not merely read), the
  9-site inventory is sound (independently re-swept, same 9, no tenth), no
  regression on any preservation invariant (AC3/AC5/AC10 all re-run live and
  unchanged), and the R-b two-consecutive-rounds condition is not met (see
  above) — a third patch or a split proposal is not warranted.
- Environmentally-unverified issue-canon reliance: internal consistency
  verified / issue-canon match environmentally-unverified (relies on
  orchestrator's primary confirmation) — this reviewer has no GitHub access
  in this sandbox and did not independently re-fetch issues #119/#127 this
  round; round 1's QA record states both were fetched independently via
  `api.github.com` and matched the spec's carry-over exactly, which this
  review continues to take as the primary confirmation without re-verifying
  it here.

#### Fast-follow disposition

- Round 1's Finding 5 (mktemp audit's "never exercised"/"resolves … to class
  (a)" wording tension) — **won't-fix, standing**: dictated verbatim by
  frozen D3; re-opening it is an R-a-adjacent question for pm-spec, not an
  engineer-actionable item. Re-confirmed unchanged this round.
- Round 1's Finding 6 (T-1024 provenance triple's contemporaneity framing) —
  **won't-fix, standing**: the triple is explicitly labeled
  non-contemporaneous and does not claim a 2026-era rejection was recorded;
  D4 (frozen) dictates exactly this framing. Re-confirmed unchanged this
  round.
- No new deferrals this round.
