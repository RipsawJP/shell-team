# T-1017 QA verification record

- Verdict: PASS
- Task: T-1017 — Close-out refuses a task whose interventions record is
  missing or non-conformant
- Board: T-1017 → READY_FOR_REVIEW (this record's first commit satisfies
  AC18's first missing component)
- Commits reviewed: `0bcceda`, `b316e9f`, `131118e` on
  `feature/90-close-out-interventions-gate`, base `e18e50d` (T-1016
  close-out commit, on top of `develop` @ `e63b286`; local `develop` is now
  `cf3602a`, which includes it). Board at `READY_FOR_QA`.
- Lens declaration: **executable-artifact / verification-mechanism** work —
  a third fail-closed gate added to `bin/close-out.sh`, new parsing of a
  sibling script's stderr contract (D3). The adversarial fixture synthesis
  checklist applies in full; AC5 already encodes most of the class-1
  boundary probes as machine `check:` lines.

## Mandatory conformance gates

- `bin/check-intent.sh .shell-team/specs/T-1017-close-out-interventions-gate.md
  .shell-team/todo.md` → `aligned: T-1017 v1
  (77816c0c2937a6a263546d50acd57843212e583a) matches ...` (exit 0).
- `bin/check-provenance.sh .shell-team/provenance/T-1017.md` →
  `conformant (3 decision entries, 0 sentinel)` (exit 0). Read all three
  decisions against the diff: (1) record-readability screened before the
  checker-existence test — matches D3 row (i)'s "screened ... before the
  checker runs" and D6's separate checker-existence test, and matches
  `bin/close-out.sh`'s actual order (`[ ! -f "$RECORD" ]` block precedes the
  `CHECKER=` existence test); (2) checker stderr captured into a bash
  variable rather than a second temp file — matches the Notes-for-engineer
  gotcha about `trap ... EXIT` not being additive, and matches the
  `CK_ERR="$(... 2>&1 >/dev/null)"` line sitting ahead of the pre-existing
  `trap` unmodified; (3) classification matched via bash `case`/glob rather
  than piping into `grep -q` — matches the repo's own recorded
  `pipefail`/`grep -q`/SIGPIPE lesson, and matches the `case "$CK_ERR" in
  *'check-interventions: structural:'*) ...` block under `set -euo
  pipefail`. All three groundings check out against the cited files.
- `bin/check-interventions.sh --task T-1017 -- .shell-team/interventions/T-1017.md`
  → `conformant: ... (0 entries, 1 sentinel)` (exit 0).

## Spec's own gate (`bin/check-acs.sh`)

- `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh --dry-run <spec>` → 0
  unrecognized, all 19 AC labels parsed, no `check:` line begins/ends with a
  backtick.
- `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh <spec>` (before writing this
  record) → **17 passed, 1 failed (AC18), 1 skipped (AC19)** — matches the
  expected shape exactly. Manually decomposed AC18's five components: only
  `test -r .shell-team/reviews/T-1017.md` failed (provenance, interventions,
  `check-handoff`, `check-board-headings --base develop` and
  `check-pii-shapes --base develop` all independently passed already).
- Re-run after this record's first commit → full AC18 passes; **18 passed,
  0 failed, 1 skipped**.

## The four owning suites + shellcheck + PII

All green, run directly (not only through `check-acs.sh`):

- `tests/close-out/run.sh` — all assertions passed, including all 16 of
  AC12's frozen `interventions-*` assertion ids in the suite's own `PASS:`
  output.
- `tests/errexit-safe/run.sh` — all assertions passed, including the
  completeness self-audit and the mutation self-check confirming the
  re-grounded `close-out.sh:382` pin is caught by content, not merely by
  line number.
- `tests/check-interventions/run.sh` — all assertions passed (unchanged
  suite, confirming AC14's byte-identity claim is consistent with reality).
- `tests/check-handoff/run.sh` — all assertions passed (unrelated to this
  task's diff; confirms no regression at the board-lint layer this gate's
  fixtures exercise).
- `shellcheck 0.11.0` (version confirmed locally) on `bin/close-out.sh`,
  `tests/close-out/run.sh`, `tests/errexit-safe/run.sh` — clean, exit 0.
- `bin/check-pii-shapes.sh --base develop` → clean, exit 0.

## D3 boundary table — re-measured independently, outside the checkout

All four assigned rows re-verified in a fresh scratch root under `$TMPDIR`
(not the AC's own fixture text, built by hand from the spec's table), each
with `cmp` proving the board stayed byte-identical:

- **Row (i), missing record**: exit `1`, board byte-identical, full remedy
  block printed (path, cwd, all three record lines).
- **Row (iii), structural (wrong-task-id record)**: exit `1`, the checker's
  `check-interventions: structural: ...` line printed, board byte-identical.
- **Row (iv), usage-classified stub**: a scratch `bin/` copy with
  `check-interventions.sh` replaced by a `usage`-emitting exit-2 stub →
  close-out exit `2`, board byte-identical.
- **Row (vi), fail-closed floor, both sub-shapes**: (a) an unclassified
  exit-2 stub → close-out exit `2`; (b) a stub emitting a *recognized*
  `schema` token but exiting `7` (an unrecognized status) → close-out
  exit `2` — confirms the mapping is keyed on status first, token only
  consulted within status 2, exactly as D3 states. Board byte-identical in
  both.
- Positive control: the same scratch `bin/` copy with a stub exiting `0` →
  close-out exit `0`, entry moved to Done — confirms the scratch copy is
  otherwise functional and the four refusals above are not an artifact of a
  broken harness.

## The remedy actually remedies

Took a fresh missing-record refusal (task `T-902`, no record at all),
followed **only** what the printed stderr said — created
`.shell-team/interventions/T-902.md` at the exact printed path relative to
the printed working directory, with the exact three printed lines and
nothing else — then re-ran close-out with no other changes: **exit `0`,
entry moved to Done.** This is the positive control that D7's one-step
remedy claim is literally true, not merely plausible.

## Dogfood on the real repo

- `bash bin/close-out.sh --help` → both frozen phrases present verbatim,
  `--note` line present (previously cut off by the `2,40p` window), no
  `set -euo pipefail` or other script code in the output.
- Four suites (above) all green on the real checkout.
- `shellcheck 0.11.0` clean on all touched files.
- `check-pii-shapes.sh --base develop` clean.

## Verification-depth disclosure — engineer's mutation self-checks

Independently reproduced 2 of the 9 Notes-for-engineer mutations, each on a
fresh scratch copy of `bin/` (not the working tree — real `bin/close-out.sh`
copied out, mutated, tested, and left untouched in the real checkout):

- **"Delete the gate entirely."** Removed lines 195–298 of
  `bin/close-out.sh` (the entire T-1017 block) from a scratch copy. Result:
  an AC2-shaped fixture (no record at all) now closes out with **exit `0`**
  and the board **is rewritten** (task moved to Done) — confirms this
  fixture class goes red without the gate. An AC1-shaped fixture (conformant
  record present) stays **exit `0`**, task moved to Done — confirms AC1
  stays green, i.e. it is a preservation lock and not a proxy for the gate,
  matching the engineer's claim exactly.
- **"Change row (iii)'s mapping to pass the checker's `2` straight
  through."** Changed the structural branch's `exit 1` (line 276) to
  `exit 2` in a scratch copy. Result: an AC4-shaped fixture (record copied
  from another task, wrong marker id) now exits **`2`** instead of the
  required `1` — confirms this case goes red. An AC5-arm-A-shaped fixture
  (usage-classified stub, unrelated to row iii) stays **exit `2`** —
  confirms the two classification halves are independently measured,
  matching the engineer's claim exactly.

The remaining 7 mutations (row (vi) treated as a pass, checker-stderr
ordering reversed, `.shell-team` hardcoded fallback, env-var read only as a
fallback, gate hoisted above T-068's disposition gate, `-e` instead of
`-f`/`-r`, remedy printed on exit-2 paths) were **not** independently
re-run — they remain the producer's own claim, disclosed as such.

## Boundary probes beyond the AC fixtures (4, all as expected)

1. **Record path is a directory named `T-901.md`** (not merely covered by
   an AC fixture): screened as row (i) before the checker ever runs — exit
   `1`, reason A, board byte-identical.
2. **CRLF-terminated record through the real checker**: accepted, exit `0`,
   entry moved to Done — the checker's own CR-stripping is inherited, the
   gate adds no normalization of its own, matching the Input space's
   explicit CRLF class.
3. **`$TEAM_INTERVENTIONS_DIR` as a relative path** (the AC fixtures only
   use absolute override paths): resolved relative to the cwd the gate runs
   under — exit `0`, entry moved to Done. Matches the Input space's
   "absolute or relative" override class.
4. **`chmod 000` unreadable record file** (a real dangling-permission
   accident, not a directory): exit `1`, reason A, board byte-identical —
   confirms the `-r` screen (not merely `-f`) is load-bearing on a genuinely
   unreadable regular file, distinct from the directory-shaped probe above.

All four are within the spec's declared "Reachable input classes" (a
non-regular-file record path, CRLF records, both layouts plus an explicit
override "absolute or relative", an unreadable file from "a partially
restored checkout or a scaffolding accident") — no `out-of-input-space`
exemption needs to be invoked for any finding, since there were no adverse
findings.

## Environment / working-tree discipline

- `git status --short` on the real checkout showed nothing beyond this
  record's own commit and the subsequent board-flag edit throughout — every
  fixture, mutation, and boundary probe ran against scratch copies under
  `$TMPDIR` (plain directory copies of `bin/`, `mktemp`-free explicit
  scratch roots) or read-only against the real tree. No `git
  checkout`/`restore`/`reset`/`clean`/`stash` was run against the real
  working tree.
- `shellcheck --version` confirmed `0.11.0`, matching the CI-pinned version,
  before trusting the shellcheck-clean checks above.

## Not independently re-run (disclosed, not faked)

- The full ~56-step CI-wired suite+dogfood list from
  `.github/workflows/check-handoff.yml` was **not** re-run in full by QA —
  only the four owning suites plus shellcheck plus the PII checker, per this
  task's own scope. The engineer's hand-off claims the full list ran clean;
  this is the producer's own claim, not independently re-verified end to
  end. AC19 is `SKIP` by design (runtime property, no `check:` line) and the
  spec's own Assumptions section already flags this as a standing
  limitation of local verification, deferring to the real CI run as primary
  evidence.
- 7 of the 9 Notes-for-engineer mutations (see disclosure above).

## Risk notes for reviewer

- AC18/AC19's completion depends on the full CI-wired list actually
  running green in CI (the engineer's claim, not independently re-run here)
  — worth confirming against the actual CI check run before merge, per this
  repo's own "treat CI as primary evidence" convention.
- The classification logic (D3) is the one genuinely new part of this task
  (everything else is pattern-following); it is measured directly at the
  boundary by AC5's stubs and re-measured independently above at 4 of its 7
  rows — worth a reviewer's own glance at the `case`/glob block in
  `bin/close-out.sh` given it is new parsing of another script's stderr
  contract.
- No adverse findings; no `out-of-input-space` exemption was needed.
