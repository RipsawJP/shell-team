# T-1091 Codex review

### Codex Review verdict: REQUEST_CHANGES (round 1)
- Task: T-1091
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — the event stream prints no model identifier, so this is the primary-value method)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 18 files, +796/-29 lines (`git diff --stat 1fca08c313b634d03c4e88e1bf06b6ca4d7a19c6...HEAD`)

**Scope.** Per the routing map's own step 4 and the spec's own `## Verdict scope`, this task carries **full adversarial depth** (`user-visible: yes`; the dev-scaffold review carve-out explicitly does not apply). Judged against the five named in-scope-for-Major items: (i) the `specify` axis's closed-set enforcement in `bin/close-out.sh` and its fixtures; (ii) the conformance-formatter boundary in `agents/pm-spec.md`; (iii) the operator-authored branch's preservation of the freeze/attestation separation; (iv) norm-text cardinality and cross-surface symmetry; (v) the blast-radius inventory's method and completeness. Explicitly out of scope and not re-litigated: whether the axis belongs in `phase-multiplexing.md` (settled, **AC11**-locked); whether `tech-lead`'s Routing Map template gains a third line (Q1, disclosed residual); whether this task's own board entry should carry a `specify` record; the depth axis (#274); issues #332/#333/#334.

**Method.** One foreground primary pass (`codex exec … review --base 1fca08c…`, real-file read, raw capture published) plus one adversarial pass triggered by round-1 Majors, targeting the operator-authored branch's mechanics specifically (`skills/run/SKILL.md`, `agents/tech-lead.md`, `agents/pm-spec.md`, `bin/close-out.sh`, the new note). Raw captures: `.shell-team/reviews/T-1091-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1091-codex-adversarial.{txt,jsonl}`.

**Independent verification — every finding below was checked against the real repository, not accepted on Codex's word.** All 17 `- check:` lines re-run live (`CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1091-operator-authored-entry.md` → `17 passed, 0 failed, 1 skipped, 0 unrecognized`), confirming AC1/AC2 both hold post-refreeze. Both `## Assumptions` count-premise figures independently re-derived from their stated commands (`t1091-relay-count` = 6, `rounds-to-approve-series` = 213 — both match the note exactly). The `## Blast radius` derivation block reproduced byte-identically from its own `- reproduce:` command. T-1019 AC10's predicted-red confirmed live (`git diff --name-only develop -- bin tests/rollup-runs tests/cluster-failures` names `bin/close-out.sh`). The class-M re-freeze (v1→v2, `bf35085`) read directly: exactly one `- check:` line differs (AC2's extraction `sed`, gaining a `.*` before the priced-prefix alternation), the prose is untouched, and the corrected check now passes against the shipped note — the mechanics classification holds.

Two genuine gaps found in the shipped diff itself, both independently reproduced:

1. **`skills/run/SKILL.md:44`** — the operator-authored branch's own landing instruction reads "land the supplied spec at the path `bash bin/team-paths.sh --get specs` resolves." This is a hardcoded relative path. Step 0 of this same file (line 18) already establishes the shipped convention: `team-paths.sh` is called bare ("on PATH when the plugin is loaded; else `bin/team-paths.sh`"). A normal adopter who installed the plugin via the marketplace has no `bin/` directory in their own repository — `bash bin/team-paths.sh` fails (no such file) before the spec can be landed. This is not hypothetical: the engineer's own AC18(b) execution-context-matrix hand-off (board line 324) claims to have tested "the one shipped command this task's own text tells a reader to run," but Cell 2 (adopter-shaped repo, plugin loaded) actually ran `PATH=<repo>/bin:$PATH team-paths.sh --get specs` — the bare form, not the literal `bash bin/team-paths.sh --get specs` the file ships. The verification silently substituted the corrected command for the broken one, so the defect passed undetected.
2. **`agents/pm-spec.md`'s conformance-read has zero mechanical enforcement.** The Goal's own "Why the conformance read is mandatory" section states it is "mandatory and non-waivable" because, in operator-authored mode, author and attester are otherwise the same party. But neither `bin/close-out.sh` nor `bin/check-intent.sh` (nor any other script under `bin/`) references `entry-mode` or `flagged-gap` at all (`grep -rln 'entry-mode\|flagged-gap' bin/` → no hits). No board field is required to record that `pm-spec`'s conformance pass ran, and no checker verifies one exists. A coordinating session that authors a spec, runs the freeze sweep itself, and simply never invokes `pm-spec`'s conformance-formatter pass is caught by nothing mechanical — exactly the self-attestation risk this axis was created to counter.

#### Blockers
None.

#### Major
- `skills/run/SKILL.md:44` — the operator-authored branch's spec-landing instruction (`bash bin/team-paths.sh --get specs`) is a hardcoded relative path that fails for a normal marketplace-plugin adopter (no local `bin/`), contradicting this same file's own Step 0 convention (bare `team-paths.sh`, PATH-resolved when the plugin is loaded). Confirmed the engineer's own AC18(b) execution-context-matrix test (board line 324, Cell 2) verified a *different* (bare-name) command than the one actually shipped, so the defect was not caught. — Suggested fix: change the instruction to the bare `team-paths.sh --get specs` (matching Step 0's own established form), or explicitly restate the "on PATH when the plugin is loaded; else `bin/team-paths.sh`" fallback inline.
- `agents/pm-spec.md`'s "mandatory and non-waivable" conformance-read duty (operator-authored mode) has no mechanical backstop anywhere in `bin/`. Neither the `- entry-mode:` nor `- flagged-gap:` hand-off fields are required on the board by any checker, and nothing verifies `pm-spec`'s conformance pass actually ran before the freeze proceeds. The stated safety rationale for the whole mode (avoiding pure self-attestation) currently rests entirely on the orchestrator's own good-faith compliance with prose, with no artifact trail a later reader (or a checker) could use to confirm it happened. — Suggested fix: either require `- entry-mode:` on the board entry and have `bin/close-out.sh` (or a small new check) verify its presence whenever `- dispatch: specify — operator-authored` is recorded, or explicitly disclose this as an unenforced duty with the same "no checker ships for it yet, tracked as issue #NNN" pattern this repo already uses for the Adopter-facing-documentation gate (T-1061) and the Stacked-branch base-ref-discriminator gate (T-1081) — the current text makes a stronger claim ("mandatory and non-waivable") than either of those two precedents without matching their disclosed-limitation honesty.

#### Minor / nits
- `agents/tech-lead.md`'s Routing Map fenced template (lines 69-71, "Return a markdown block in this exact shape") still shows only two `- dispatch:` lines (`implement`, `verify`); the mirrored norm text below it ("every axis any such note defines is recorded") asks `tech-lead` to print a third `specify` line that appears nowhere in its own literal example. This is explicitly settled as Q1 in the spec's own `## Open questions` ("no third templated line… the norm text is what carries the duty… If the cross-provider review judges that insufficient, the fix is a one-line template addition in a fast-follow") and is out of scope for this verdict per the routing map's own instructions — noted here because both the primary and adversarial Codex passes independently raised it, but not scored as a blocking finding.
- `tests/close-out/run.sh:1211`'s positive `specify` fixture (`OKS`, reused by T-988/990/993/994) cites `cost-input: spec-authorship-judgment-density`, an id that does not exist anywhere in `docs/loop-engineering/spec-authorship-entry.md` (whose real priced ids are `pm-authored-default`, `t1091-relay-count`, `rounds-to-approve-series`). This passes only because `bin/close-out.sh`'s ground-prefix check (line 298) never validates that the cited id resolves to an actual priced line, for *any* of the three axes — the identical, pre-existing behavior the `implement`/`verify` axes already have. Not a gate weakness introduced by this task, but the fixture is misleading as a "conformant" worked example. — Suggested fix: swap the fixture's id for one of the note's three real ids.
- `docs/loop-engineering/spec-authorship-entry.md`'s **AC2** prose says "the ground field's **leading** priced-line id is extracted," but the post-refreeze (v2) check line's extraction regex (`.*(saving|recommendation|break-even|cost-input): …`) matches the priced prefix *anywhere* in the ground field (in fact the last occurrence, since `.*` backtracks greedily), not strictly the first. Harmless for the note's current content (each rule line carries exactly one such prefix), but the prose and the check no longer describe the same thing precisely — worth tightening the wording to "occurring in" rather than "leading" the next time this line is touched.

#### Rejected / out-of-scope (pre-existing corpus conventions, not introduced by T-1091)
- **Base-ref-discriminator's two-arm form (spec Goal, line 46) omits an explicit `refs/remotes/<remote>/…` arm** before falling back to `develop`. Verified this is byte-for-byte the same pattern already shipped and previously reviewed in `T-1089-harness-recut.md:36` and `T-1090-sign-safety-close.md:36` — not a regression this task introduces, and the Goal's own "Residual cases disclosed rather than engineered around" paragraph already names the fetch-depth-gap class this leaves open.
- **AC14/AC15/AC17's hardcoded `.shell-team/provenance/T-1091.md` / `.shell-team/interventions/T-1091.md` / `.shell-team/specs/T-1091-operator-authored-entry.md` paths**, rather than resolving them via `team-paths.sh --get provenance|interventions|specs` (both keys exist and resolve correctly — confirmed live). Verified this is the same convention T-1089/T-1090's own acceptance criteria already use for their own record files; AC15 already resolves the *board* path dynamically (`bash bin/team-paths.sh --get todo`) precisely where cross-layout variance matters within this checkout. Not a T-1091-introduced defect.
- **`bin/close-out.sh`'s dispatch gate stays validate-if-present and never checks ground-id semantic authenticity for any axis.** Both are explicit, frozen Non-goals ("No requirement that a `specify` record be present… Completeness is norm text") and match the `implement`/`verify` axes' identical, already-shipped behavior since T-1084. Not a weakening this task introduces.
- **`DISPATCH_AXIS_TABLE` is a hand-maintained string rather than generated from the priced notes.** This is the table's designed extension point per `bin/close-out.sh`'s own comment ("the ONE place a later axis is added"), unchanged in kind since T-1084; T-1091 uses it exactly as intended.

#### Adversarial findings
Full record: `.shell-team/reviews/T-1091-codex-adversarial.{txt,jsonl}`. Asked to break the operator-authored branch specifically (Routing Map template realism, the spec-landing path, the author/attester separation, and the dispatch gate's cross-axis integrity): Codex independently corroborated both Majors above (normalized "blocker"/"major" on its own priority vocabulary) and one of the two informational items (validate-if-present dispatch omission — judged here as an explicit, frozen Non-goal rather than a new gap). It additionally reiterated the Routing Map template concern (folded into Minor above) and the fixture ground-id concern (folded into Minor above), and it flagged the DISPATCH_AXIS_TABLE's hand-maintenance as a minor synchronization risk (folded into Rejected above, matching pre-existing design).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `agents/tech-lead.md` Routing Map template lacks a `specify` line | primary: Codex-native `[P1]`→blocker (map); adversarial: `major` | minor | explicitly settled in the spec's own Q1 as a disclosed residual, and out of scope per the routing map's own "do not re-litigate placement" instruction; the spec itself pre-states the remedy is a fast-follow if review disagrees |
| 2 | `skills/run/SKILL.md:44` hardcoded `bash bin/team-paths.sh --get specs` | primary: `[P1]`→blocker (map); adversarial: `major` | major | downgraded from blocker: a natural-language instruction to an LLM orchestrator, not a shipped executable — the underlying dispatch gate and freeze mechanics are unaffected — but confirmed real (AC18(b)'s own hand-off tested a different, corrected command in the exact cell meant to catch this), so kept at major and must be fixed before merge |
| 3 | `pm-spec` conformance-read has no mechanical enforcement (self-attestation loophole) | adversarial only: `blocker` | major | downgraded from blocker: consistent with, not categorically worse than, this repo's existing accepted trust model for other freeze-time prose duties (T-1061, T-1081 precedent — both also unenforced by any checker); still a real gap between the "mandatory and non-waivable" language and the shipped (zero) mechanical backstop, so kept at major rather than accepted as settled |
| 4 | base-ref-discriminator's missing `refs/remotes/` arm | primary: `[P2]`→major (map) | rejected (informational) | verified byte-for-byte identical to the already-shipped, previously-reviewed pattern in T-1089/T-1090; not introduced or worsened by T-1091 |
| 5 | AC14/AC15/AC17's hardcoded provenance/interventions/specs paths | primary: `[P2]`→major (map) | rejected (informational) | verified as the same pre-existing convention T-1089/T-1090's own acceptance criteria use for self-referential record paths; AC15 already resolves the board dynamically where it matters |
| 6 | fictional priced-id (`spec-authorship-judgment-density`) in the "conformant" specify fixture | primary: `[P2]`→major (map); adversarial: `major` | minor | real fixture-accuracy nit, but does not weaken the actual gate, which never validates ground-id existence for any of the three axes (pre-existing, cross-axis behavior); worth a quick fixture-text fix, not merge-blocking |
| 7 | `bin/close-out.sh`'s validate-if-present dispatch gate lets `specify` be omitted entirely | adversarial: `major` | rejected | explicit, frozen Non-goal ("No requirement that a `specify` record be present"), matching the identical, already-shipped `implement`/`verify` axis behavior since T-1084 |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md`'s T-1091 entry to `READY_FOR_ENG`, linking the two Majors above (`skills/run/SKILL.md:44`'s landing-path instruction, and `agents/pm-spec.md`'s unenforced conformance-read duty). Both are ordinary rework — neither touches a frozen byte, a locked cardinality, or the never-dropped pre-commitment components (the operator-authored entry-mode branch and `agents/pm-spec.md`'s conformance-formatter boundary themselves are sound in design; the gap is in how the design's own stated guarantees are — or are not — mechanically backed).

#### Fast-follow disposition
- `agents/tech-lead.md`'s Routing Map template omitting a `specify` line (Minor #1 above) — **file-an-issue** (rationale: the spec's own Q1 already names this as the correct disposition — "a one-line template addition in a fast-follow" — if cross-provider review judged the disclosed residual insufficient, which this round does).
- `tests/close-out/run.sh:1211`'s fictional priced-id in the positive `specify` fixture (Minor above) — **won't-fix as a fast-follow; recommended as a same-round typo fix instead** (trivial one-line swap to a real id; low enough cost that deferring it past this rework round is not warranted, but it does not block merge on its own if the engineer chooses to fold it into the same rework pass as the two Majors).
- `docs/loop-engineering/spec-authorship-entry.md`'s AC2 prose/"leading" vs. check-line "anywhere" wording drift (Minor above) — **won't-fix** (harmless for the note's current content; a wording tightening for the next time this line is legitimately touched, not worth a dedicated issue).

## Round 2

### Codex Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1091
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — the event stream prints no model identifier, so this is the primary-value method; unchanged from round 1)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: rework delta 3 files, +16/-2 lines (`git diff --stat ecd50fb...1b04ea9` — `.shell-team/todo.md`, `agents/pm-spec.md`, `skills/run/SKILL.md`); whole-task cumulative diff unchanged in file count from round 1 (18 files touched since the branch point, +812/-31 lines net)

**Scope.** Per the coordinator's round-2 brief: the closure of round 1's two Majors, at full adversarial depth, plus anything new the rework itself introduced. Round 1's downgraded/rejected items (base-ref-discriminator's missing remote arm, hardcoded provenance/interventions/specs paths, the Routing Map template's 2-line shape, the fictional fixture id, `close-out.sh`'s validate-if-present dispatch gate) are not re-opened — none of them was touched by this rework's diff, confirmed by `git diff ecd50fb...1b04ea9 --stat` naming only the three files above.

**Method.** One foreground primary pass (`codex exec … review --base ecd50fb`, scoped to the actual rework delta — an earlier attempt at `--base 1b04ea9` produced an empty diff since HEAD *is* `1b04ea9`, caught and corrected before publishing) plus one adversarial pass targeting the new gate's own ordering guarantees and re-sweeping for any other newly-introduced `bash bin/` hardcoding. Raw captures republished under the same canonical basenames as round 1 (`T-1091-codex-primary.{txt,jsonl}`, `T-1091-codex-adversarial.{txt,jsonl}`), per this role's own never-vary-per-round rule — round 1's raws are superseded, not kept as a separate `-r1`/`-r2` pair (a precedent I found in `T-1089.md`'s own review record cites issue #335 for that different convention, but my own agent instructions are explicit and unambiguous on this point, so I followed them rather than that precedent).

**Independent verification, both closures checked against the live repository before reading Codex's output.**

- **Major 1 closure (path spelling) — CONFIRMED CLOSED.** `skills/run/SKILL.md`'s operator-authored branch now reads "land the supplied spec at the path `team-paths.sh --get specs` resolves (on PATH when the plugin is loaded; else `bin/team-paths.sh --get specs` — …)", matching Step 0's own established convention verbatim in shape. Re-ran the class sweep myself, independent of the rework commit's own count: `git diff --name-only 1fca08c…HEAD -- README.md README.ja.md docs/adopting.md docs/adopting.ja.md skills/run/SKILL.md agents/pm-spec.md agents/tech-lead.md docs/workflow.md docs/workflow.ja.md docs/loop-engineering/spec-authorship-entry.md | while read -r f; do grep -noE 'bash bin/[a-zA-Z-]+\.sh' "$f"; done` → 5 hits: `README.md`/`README.ja.md` (`gen-loop-replay.sh`), `docs/adopting.md`/`.ja.md` (`check-binding.sh`), `skills/run/SKILL.md` (`check-acs.sh`, the pre-existing fan-out illustration) — **zero remaining hardcoded `bash bin/team-paths.sh`**, matching the fix. Confirmed all 5 as line-content-identical sets between the branch-point blob and HEAD (`git show <base>:<file> | grep -F 'bash bin/' | sort` vs. the same on the working tree — `diff` empty for all five files), so none is touched by this task's own added text — QA's "two vs. three pre-existing sites" nit (the rework commit message undercounts by one, omitting the `README`/`.ja` `gen-loop-replay.sh` occurrence) is confirmed accurate and correctly scored non-blocking. Independently re-ran the three execution-context-matrix cells against the exact current shipped bytes (Cell 1 fallback form in this checkout → `.shell-team/specs`; Cell 2 bare form via `PATH=<repo>/bin:$PATH` in a scratch default-layout dir → `.shell-team/specs`; Cell 3 same bare form in a scratch legacy-layout dir → `docs/specs`) — all three exit 0 and match the board's rewritten AC18(b) paragraph.
- **Major 2 closure (disclosed-limitation shape) — SHAPE ACCEPTED, BUT A NEW GAP FOUND (see Major below).** The new **Conformance-read confirmation gate (T-1091, operator-authored mode only)** at `skills/run/SKILL.md:51` sits beside the Adopter-facing-documentation gate (T-1061) and the Stacked-branch base-ref-discriminator gate (T-1081), in the identical fail-closed "a read you perform, not an exit code you consult: no checker ships for it yet" language, correctly scoped ("skip this gate entirely for `pm-authored` mode and for every task with no `specify` record at all… ONLY at a task's first freeze"), and correctly requires an outstanding `- flagged-gap:` to refuse the freeze. `agents/pm-spec.md`'s matching honesty paragraph (line 120) states the same thing in the same words, citing the same T-1061/#250 precedent. This is the exact remedy shape I recommended in round 1, and it is the shape this repository's corpus already accepts for two prior gates of the identical kind — the disclosure pattern itself is sound. `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`; `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1091-operator-authored-entry.md` → `17 passed, 0 failed, 1 skipped, 0 unrecognized` (unaffected — neither edit sits inside the frozen intent block); `bash bin/check-intent.sh` → `aligned: T-1091 v2 (91c0c413…)`, no drift. But reading the new gate's own triggering condition against the rest of this file's dispatch-record mechanics (see Major below) surfaces a genuine ordering gap that can make the gate never fire for the exact input class it exists to protect — so the *closure* is not yet complete even though the *shape* is right.

Both Codex passes (primary and adversarial) independently surfaced the same ordering gap below without being told about it in advance; I verified it myself by reading every "Specify-to-Implement seam" reference in the file (four occurrences: lines 42, 44, 209, 217) and confirming none of them states whether dispatch-record transcription happens before or after the Frozen-intent confirmation sub-step (which is where the new gate lives) during a task's first freeze.

#### Blockers
None.

#### Major
- `skills/run/SKILL.md:44-51, 209, 217` — **the new Conformance-read confirmation gate's own triggering condition can silently evaluate false for a genuine operator-authored task, defeating it entirely.** The gate fires "when — and only when — this task's board entry carries `- dispatch: specify — operator-authored`" and sits inside step 2's Frozen-intent confirmation sub-step, which runs "right after Specify, before Implement." But every mention of *when* the orchestrator transcribes a dispatch record onto the board (lines 42, 44, 209, 217) anchors it to "the Specify-to-Implement seam… before `engineer` is invoked in step 4" — a window that is never disambiguated against the freeze sweep's own position inside that same window. Nothing in this file states that dispatch transcription (in particular, the `specify` record) must happen *before* the Frozen-intent confirmation sub-step runs. A plausible, textually-compliant sequence — freeze first, transcribe all three axis records afterward, right before invoking `engineer` — leaves the board carrying no `- dispatch: specify —` record at the moment the new gate's condition is checked. Per the gate's own stated scope ("skip this gate entirely… for every task with no `specify` record at all"), this reads as a `pm-authored`-shaped absence and the gate silently skips, allowing a genuinely operator-authored task's freeze to proceed without ever confirming `pm-spec`'s conformance-read happened — precisely the self-attestation path round 1's Major 2 was raised to close. Independently corroborated by both Codex passes without prompting; I confirmed the absence of any disambiguating text myself by reading all four "Specify-to-Implement seam" occurrences in the file. This is a new finding, not a re-opening of round 1's Major 2 — round 1 found the duty unenforced by any checker; this finds that even the newly-added *prose* gate can be structurally bypassed by ordering, independent of whether a checker ever ships. — Suggested fix: state explicitly, at the dispatch-record mechanics section (or inline in the new gate's own text), that the orchestrator transcribes any decided `specify` record onto the board immediately after `pm-spec` creates the entry in step 2 and *before* the Frozen-intent confirmation sub-step runs — not deferred to the end of the Specify-to-Implement seam — or, more robustly, key the gate directly off `tech-lead`'s Plan-time routing decision (already known before Specify even starts) rather than off a board record whose transcription timing is otherwise unconstrained.

#### Minor / nits
- `skills/run/SKILL.md:51` — the new gate's own condition text shows `- dispatch: specify — operator-authored — <ground>`, a 3-field shape that omits the modality field (`<unconditional|conditional>`) the shipped grammar requires on every other dispatch-record reference in this same file (`- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>`, spelled out fully at lines 210-213). Unlikely to cause a careful LLM orchestrator to misrecognize a real 4-field record today, since the surrounding prose reads for meaning rather than for an exact string match, but this is the one place in the file that departs from the otherwise meticulous byte-level grammar consistency this document maintains everywhere else — and the disclosed follow-up issue explicitly plans a *mechanical* version of this gate, whose implementer could plausibly copy this 3-field shape verbatim into a regex that then fails to match every real record. Adversarial pass flagged this independently as `major`; downgraded here to minor because the gate is currently read-only prose consumed by an intelligent agent, not a shipped script — but it should be corrected to the full 4-field shape (or explicitly noted as eliding the modality field) before, or together with, the follow-up issue's mechanical gate is built.

#### Rejected (correctly self-scored by the adversarial pass, not elevated)
- **The board's own round-3 QA/rework hand-off prose quoting historical `bash bin/…` commands verbatim** (e.g. the superseded AC18(b) paragraph's own quote of the pre-fix command, the class-sweep's quoted `grep -oE` patterns). The adversarial pass itself correctly scored this a `nit` and named the reason: these are audit-trail transcripts of what was actually run or found, not adopter-facing instructions — no adopter reads `.shell-team/todo.md`'s hand-off prose as a command to execute. Confirmed by my own reading: every such occurrence sits inside a quoted, past-tense evidentiary sentence, not an imperative.

#### Adversarial findings
Full record: `.shell-team/reviews/T-1091-codex-adversarial.{txt,jsonl}`. Asked to (1) find any ordering guarantee for dispatch transcription vs. the new gate, (2) find any other newly-introduced self-defeating ordering/scoping gap in the rework diff, and (3) re-confirm the path-spelling fix's completeness: Codex independently named the same ordering gap as (1) above without being told about it (major), separately flagged the 3-field vs. 4-field grammar mismatch in the new gate's own condition text (major — downgraded to minor in synthesis, see ledger), found no other self-defeating gap in the rework diff, and confirmed the path-spelling fix itself is complete and correct (no remaining unguarded `bash bin/` hardcoding in this task's own added text — matching my own independent class-sweep re-derivation above).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Dispatch-transcription-vs-freeze-sweep ordering ambiguity can silently skip the new Conformance-read confirmation gate | primary: Codex-native `[P1]`→blocker (map); adversarial: `major` | major | kept below the mapped blocker: a natural-language sequencing ambiguity in orchestrator instructions, not a break in any shipped executable or mechanically-checked artifact (all 17 `- check:` lines and `check-prompt-sync` stay green); still a real, confirmed structural gap in the exact remedy round 1 asked for, so kept at major rather than accepted |
| 2 | New gate's condition text shows a 3-field dispatch record, omitting the modality field the shipped grammar requires everywhere else | adversarial: `major` | minor | downgraded: the gate is prose read by an intelligent orchestrator (not a machine regex) and is very unlikely to cause a real record to go unrecognized today; kept as a documentation-precision item because a future *mechanical* version of this gate (the disclosed follow-up issue) could copy this incomplete shape verbatim |
| 3 | Board hand-off prose quoting historical `bash bin/…` commands as evidence | adversarial: `nit` | rejected (no change) | adversarial pass's own scoring is correct as-is: these are past-tense audit transcripts, not adopter-facing instructions; not elevated |
| 4 | "Two" vs. measured "three" pre-existing `bash bin/` sites in the rework commit message | (QA's own disclosed nit, re-verified by this role, not raised independently by Codex this round) | minor (unchanged from QA's own scoring) | independently re-confirmed via my own class-sweep grep (5 hits total, matching 3 distinct pre-existing sites plus the 2 README/.ja duplicates of one site); a commit-message undercount, not a functional gap |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md`'s T-1091 entry to `READY_FOR_ENG`, linking the one Major above (the dispatch-transcription-ordering gap defeating the new Conformance-read confirmation gate). This is ordinary rework — it touches only prose sequencing in `skills/run/SKILL.md`, not a frozen byte, a locked cardinality, or either never-dropped pre-commitment component (the operator-authored entry-mode branch and `agents/pm-spec.md`'s conformance-formatter boundary remain sound in design; round 1's Major 1 is fully closed, and round 1's Major 2 is closed in shape but needs this one further sequencing fix to actually hold in every execution order the surrounding text permits).

#### Fast-follow disposition
- The 3-field vs. 4-field grammar mismatch in the new gate's own condition text (Minor #2 above) — **won't-fix as a same-round requirement, recommended as a fold-in with the Major's fix instead** (both sites are within a few lines of each other in the same bullet; fixing them together in the same rework pass is cheaper than a separate deferred issue, but this Minor alone would not block merge if the engineer chose to defer it).
- No new fast-follow deferrals beyond the one carried forward unchanged from round 1 (the Routing Map template's missing `specify` line, already dispositioned as file-an-issue in round 1's own section) and the fictional fixture id (already dispositioned as a recommended same-round typo fix, not touched by this rework and not re-litigated here since it was not in round 2's scope).

## Round 3

### Codex Review verdict: REQUEST_CHANGES (round 3)
- Task: T-1091
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — the event stream prints no model identifier, so this is the primary-value method; unchanged from rounds 1-2)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: redesign delta 5 files, +25/-4 lines (`git diff --stat 02c24e5...b869fe0` — `.shell-team/interventions/T-1091.md`, `.shell-team/specs/T-1091-operator-authored-entry.md`, `.shell-team/todo.md`, `agents/pm-spec.md`, `skills/run/SKILL.md`)

**Scope.** Per the coordinator's round-3 brief: whether the redesign genuinely closes round 2's Major (the transcription-ordering defeat), whether round 2's Minor (the 3-field record shape) is fixed, a sweep of the redesign delta for anything newly broken, and confirmation that nothing frozen or previously-accepted was contradicted. The residual self-attestation limitation (a coordinating session could forge the hand-off outright) stays out of scope per the coordinator's explicit instruction, unless the redesign made it worse — judged below to be a *different*, narrower gap the redesign newly introduces, not a restatement of that residual, so it is reported rather than waved through.

**Governance trail, read first-hand before any technical judgment.** The pre-commitment fired correctly after round 2: `467222e` records the stop (enforcement-gap ×2 against the same component — the mandatory conformance read had no mechanical backstop, then its replacement gate's own trigger was order-dependent), `4b008d1` records the operator's GO on rework-digest option (a) — a bounded route-back to `pm-spec` to redesign the gate's condition source — in both the board and `.shell-team/interventions/T-1091.md` (a genuine `human-stop` entry, not a sentinel). This is the correct escalation path per this repository's own pre-commitment discipline; nothing here was self-ratified by the loop.

**Independent verification, before reading Codex's output.**

- **Round-2 Major closure — the ordering class is genuinely eliminated, confirmed by direct reading.** The gate's condition source moved from the board's `- dispatch: specify —` record to `pm-spec`'s own `- entry-mode:` hand-off field. I read all four "Specify-to-Implement seam" references in `skills/run/SKILL.md` again (lines 42, 44 [now rewritten], 209, 217) plus the new gate text itself (line 51) and confirmed: step 2's own main instruction is "invoke `pm-spec` to produce / update the spec. **Wait for `READY_FOR_ARCH`**," and only *after* that does any sub-bullet (including the Frozen-intent confirmation containing this gate) run. `pm-spec`'s hand-off — the new condition source — is therefore synchronously available the moment step 2's wait completes, by construction of the phase order, with no separate transcription step whose timing is left open (unlike the board record, whose write point was never fixed against the freeze). There is no textually-compliant sequence in which the gate evaluates before the hand-off exists. This matches QA round 4's own adversarial-reading conclusion (board line 54) and Codex's own adversarial pass this round, which stated plainly: "The ordering concern itself is closed by the explicit before-sweep requirement and fail-closed missing-field rule."
- **Round-2 Minor closure — CONFIRMED.** The only dispatch-record shape quoted anywhere in the redesigned bullet is `` `- dispatch: specify — operator-authored — conditional — <ground>` `` (all four fields), quoted mid-line per this file's own idiom. Fixed.
- **Nothing frozen or shipped/mechanically-checked moved.** `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1091-operator-authored-entry.md` → `17 passed, 0 failed, 1 skipped, 0 unrecognized` (AC8 and AC9 individually re-run and confirmed `PASS` — independently, not trusted from the board's own claim, since the coordinator's brief specifically invited doubt here). `bash bin/check-prompt-sync.sh` → `all registered prompt blocks in sync`. `bash bin/check-intent.sh` → `aligned: T-1091 v2 (91c0c413…)`, unchanged — confirming this round's edits sit outside the frozen intent block. `bin/close-out.sh` and `tests/close-out/run.sh` are untouched by this round's diff (confirmed via the file list above), so round 1's Major 1 and its fixture-population findings are unaffected.
- **My own round-2 suggestion vs. the path actually taken.** Round 2's record offered two remedies: fix the board's transcription-ordering, *or* "key the gate directly off `tech-lead`'s Plan-time routing decision… rather than off a board record." The redesign took a third path — keying off `pm-spec`'s own self-reported hand-off field — which does eliminate the ordering class, but (see Major below) does so without cross-referencing the Plan-time decision at all, which is exactly the property my second suggested alternative would have preserved for free. This is not a contradiction of anything I accepted; it is a different design point on the same problem, with its own new trade-off.

Both Codex passes this round independently found the same new gap below without being prompted with it in advance (I asked only general "attack the new text" questions in the primary pass; the adversarial pass named it more precisely after I supplied the specific mismatch scenario to probe).

#### Blockers
None (see the "Major, severity note" below for why the finding is scored Major rather than the mapped-blocker priority Codex's own vocabulary suggests).

#### Major
- `skills/run/SKILL.md:51` — **the redesigned gate trusts `pm-spec`'s self-reported `- entry-mode:` value with no cross-check against `tech-lead`'s Plan-time `specify`-axis routing decision, so a mismatch between the two silently defeats the gate in either direction.** If Plan decided `operator-authored` but the returned hand-off states `- entry-mode: pm-authored` — plausible after a misclassified formatter invocation, a stale or retried orchestration turn returning an old hand-off, or an ordinary sequencing mistake by the orchestrator itself — the gate's own text branches on "the value you read," finds `pm-authored`, and states plainly "this gate does not apply." A genuinely operator-authored task then reaches the intent-hash record with the mandatory conformance read never confirmed, which is the exact consequence class rounds 1 and 2 both found. The reverse mismatch (Plan says `pm-authored`, the hand-off falsely says `operator-authored`) is also live: the gate would enter the `operator-authored` branch and check for flagged gaps on a spec `pm-spec` never actually ran in conformance-formatter mode, which either passes vacuously (no flagged-gap line to answer) or spuriously blocks legitimate `pm-authored` work. `agents/pm-spec.md:120`'s new paragraph requires *emission* of the field in both modes but never requires it to *agree* with the Plan-time decision. — **Severity note**: both Codex passes normalize this to blocker/`[P1]` on their own vocabulary; I keep it at Major, consistent with rounds 1-2's own treatment of prose-only gate-logic gaps in this same document — it does not break any shipped executable or mechanically-checked artifact (all 17 `- check:` lines, `check-prompt-sync`, and `check-intent` stay green), and unlike round 2's finding (which fired under the *default*, unremarked reading of an ambiguous but ordinary sequence), this one requires an actual mismatch or error condition to manifest rather than being reached by following the text as written in good faith. — Suggested fix: require the hand-off's `- entry-mode:` value to equal the Plan-time `specify` decision `tech-lead` recorded (or, more simply, adopt my round-2 alternative suggestion directly — key the gate off `tech-lead`'s Plan-time decision itself, with `pm-spec`'s hand-off used only to confirm the selected mode actually ran) — either closes the cross-check gap without reopening the ordering class this redesign correctly solved.

**Operational pattern, flagged for the orchestrator's own judgment, not decided here.** This is the **third consecutive round** in which an independent Major lands against the same never-dropped pre-commitment component (the operator-authored entry-mode branch's conformance-read enforcement): round 1 found no mechanical backstop at all; round 2 found the repair's own trigger was order-dependent; this round finds the *next* repair's trigger has no cross-check against the decision it is supposed to reflect. All three are genuinely different bugs, not the same defeat recurring unfixed — each prior finding is independently confirmed closed above — but the *pattern* (each fix for this one component's enforcement gate opens a new, narrower gap in the same place) is exactly the shape the pre-commitment's own trigger describes ("their defeat across two consecutive rounds is evidence about the design premise rather than about implementation craft"). Whether this third Major on the identical component should re-fire that trigger, and whether the answer is a fourth bounded patch, one of the pre-commitment's two droppable items, or something else, is the orchestrator's call to make — I state the pattern and the technical finding; I do not judge the recurrence-count or execute any disposition myself.

#### Minor / nits
None new this round.

#### Adversarial findings
Full record: `.shell-team/reviews/T-1091-codex-adversarial.{txt,jsonl}`. Asked directly whether the redesign eliminates the ordering class or merely relocates it, whether either direction of a Plan-vs-hand-off mismatch is checked, and whether any other new self-defeating gap exists: Codex confirmed the ordering class is genuinely closed ("closed by the explicit before-sweep requirement and fail-closed missing-field rule"), independently found and stated both mismatch directions (matching the primary pass's finding and extending it with the reverse case), and found no other new gap in the redesign delta.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Gate trusts `pm-spec`'s self-reported `- entry-mode:` with no cross-check against `tech-lead`'s Plan-time decision (both mismatch directions) | primary: Codex-native `[P1]`→blocker (map); adversarial: `blocker` | major | kept below the mapped blocker for the same reason rounds 1-2's prose-only gate-logic gaps were: no shipped executable or mechanically-checked artifact is broken (17/17 ACs, `check-prompt-sync`, `check-intent` all green); unlike round 2's finding, this one needs an actual mismatch/error to manifest rather than firing under the ordinary, good-faith reading of the text |
| 2 | Round-2 ordering gap | (closure verified, not re-scored) | closed | independently confirmed via direct reading of all four seam references plus live re-run of AC8/AC9; matches QA round 4's own adversarial-reading conclusion and Codex's own adversarial-pass concurrence this round |
| 3 | Round-2 Minor (3-field record shape) | (closure verified, not re-scored) | closed | confirmed: the only quoted shape now carries all four fields |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md`'s T-1091 entry to `READY_FOR_ENG` (or wherever the orchestrator's own pre-commitment judgment routes it — see the Operational pattern note above), linking the one Major (the Plan-vs-hand-off cross-check gap). This does not touch a frozen byte, a locked cardinality, or a shipped/mechanically-checked artifact; it is a further sequencing/cross-validation fix in the same `skills/run/SKILL.md` bullet already touched this round, and my suggested fix (require agreement between the hand-off and the Plan-time decision, or key the gate off the Plan-time decision directly) is a small, bounded edit rather than a further redesign.

#### Fast-follow disposition
- No new fast-follow deferrals this round — the one Major found is recommended for the next rework round rather than deferred past merge, given this component's own review history.
- Carried forward unchanged from round 1 (not re-litigated, untouched by this round's diff): the Routing Map template's missing `specify` line (file-an-issue) and the fictional priced-id in the positive `specify` fixture (recommended same-round typo fix, still not required to block merge).
