### Codex Review verdict: REQUEST_CHANGES
- Task: T-1083
- Codex model: gpt-5-codex (via codex-cli)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 10 files, +861/-2 lines（base `01ef1108836839120217accfd8a56be4ed2ae008` = predecessor `feature/1082-telemetry-discriminator` の tip、`git diff --stat "$B"...HEAD`）

#### 検証範囲について

spec 自身の `## Verdict scope` を踏襲した。**shipped-code depth（adversarial）**: `templates/prompt-blocks/fanout-orchestration.md`・`skills/run/SKILL.md` ミラー・両 README クローズ。**internal analysis-artifact depth（not adversarial）**: `docs/loop-engineering/agent-launch-fanout.md` とプローブ記録 — 数値と結論の方向性（overlap window が記録されたタイムスタンプから追従するか、verdict が licence condition から追従するか）を検証対象とし、"structure しか見ていない" という指摘はこの pricing の正しい記述であって欠陥ではない、という spec 自身の宣言どおりに扱った。ただし「note 内の主張がその note 自身の記録済み証跡から本当に導けるか」は明示的に in-scope（spec 自身が AC11/AC14 でそれを安く検証できるようにしている）であり、今回の Blocker 2 件はいずれもこのカテゴリに該当する。

#### MANDATORY ITEM — class-M mechanics re-freeze (v1→v2) の検証結果

独立に検証し、記録どおりであることを確認した（orchestrator/engineer/QA の attestation を鵜呑みにせず、自分で再実行）。
- `bin/check-intent.sh --print-hash` を probe-evidence commit `e86f312` 上の spec blob と v1 の pre-refreeze commit `1326156` 上の spec blob の両方に対して自分で実行 → 両方とも `02b43df902d1a0f06c7a2bb353098c0ddceaa7dc`（記録どおりの old-hash、かつ「凍結後に protocol 領域が動いていない」ことの独立確認）。HEAD 自身の `check-intent.sh` → `fb11f4d6dd9f7ce5c6be28be12fe379d451033ed`（記録どおりの new-hash / intent-hash (v2)）。
- 独立に `bin/check-refreeze-class.sh --old-hash 02b43df9... --new-hash fb11f4d6... -- <v1-blob> <v2-blob>` を実行 → `mechanics ... differing=1`（board の記録・QA の独立再実行と一致）。
- intent block 全体（`<!-- BEGIN/END intent-block: T-1083 -->`）を v1/v2 で自分で `diff` → 差分は AC16 の `- check:` 行 1 行のみ。差分の中身も自分で確認: 旧 `test "$(grep -c '^- verdict: ' "$N")" = "1"` → 新 `test "$(awk '/^## Verdict and licence conditions$/{f=1;next} f&&/^## /{exit} f' "$N" | grep -c '^- verdict: ' || true)" = "1"`。probe-protocol 領域（`<!-- BEGIN/END probe-protocol: T-1083 -->`）はこの diff の対象外（AC8 の byte-lock 対象と一致し、動いていない）。
- **意味の保存性**: AC16 の散文が要求する closure は「五値の closed-vocabulary から exactly one」＋「not-met が 1 つでもあれば verdict は undetermined/launch-refused に限定される」という coupling。旧 check の第二アサーションは「ファイル全体で `- verdict: ` から始まる行が 1 本だけ」というバイト単位の代理指標で、この closure を意図したものだったが、`bin/aggregate-verdicts.sh` の実出力（part file・`fanout-verdict` block）が同じ接頭辞の行を必然的に 4 本追加で持ち込むため、AC13/AC14（byte-locked evidence の逐語引用要求）と構造的に両立不能だった。新 check は同じ代理指標を note 自身の `## Verdict and licence conditions` 節に scope しており、その節には実際に verdict 行が 1 本しかないため（自分で note を読んで確認済み）、closure の意味（「権威ある verdict の隣に第二の verdict 主張が紛れ込めない」）は保たれている。この class-M repair は妥当と判断する。

#### Blockers

- `docs/loop-engineering/agent-launch-fanout.md`「### Arm: launch-refused」（quoted error message、及びこの block を参照する `## Agent-type boundary` line 241）— この byte-locked（AC7）な raw evidence 節が、operator 個人 / employer 固有の Claude Code ツール一覧を逐語で公開 repo に commit している。引用されている `Available agents:` リストには本 repo の `shell-team:*` 標準ロールではない operator 個人環境のカスタムツール名（一部は employer 固有）が 13 件含まれていた。［orchestrator による redaction (2026-08-19): この記述自身が同じ実名リストを転記していたため、finding の class 記述だけを残して実名を除去した — 本 finding 自身が指摘する hygiene 規則の適用。round 2 briefing で開示済み。］`CLAUDE.md` の Hygiene 節「Do not add private context: ... session transcripts, or audit output carrying real values」に直接抵触する。primary pass（P2）・adversarial pass（両者とも blocker 相当）の双方が独立に同じ懸念に到達している。**Remediation**: AC7 が byte-lock しているため、`- check:` 行だけの class-M 修正では直せない。frozen protocol の invalid-evidence rule（re-probe は最大 1 回まで許可）の対象として、`launch-refused` arm を re-probe し、harness のエラーメッセージを「拒否が起きたこと・exit class・`shell-team:qa-verifier` が利用可能ロール一覧に含まれていたこと」だけを残して再captureする（Available agents の全リストは quote しない）。他の 3 arm（never-dropped の `plugin-role-n2`/`aggregation-live`/`instance-death`）は re-probe 不要 — この finding は `launch-refused` arm 単体の quote 方法の問題であり、pre-commitment の never-dropped clause には触れない。
- `docs/loop-engineering/agent-launch-fanout.md`「## Agent-type boundary」line 241（"recorded verbatim from the emitted span rows and the launch record quoted in `## Probe evidence` above, for every instance in every arm"）— この task の中心的な実証主張（T-1073 の宣言済み `unobserved` plugin-role boundary を実際に越えたこと）が、byte-locked な raw evidence 自身では裏付けられていない。自分で `## Probe evidence` を精査した: (1) quoted span rows（line 60-61）は `"span":"qa-verifier"` フィールドしか持たず、`subagent_type` フィールドは存在しない — `span` は orchestrator 自身が telemetry に付与するラベルであり、harness が実際に解決した agent type の証跡ではない。(2) launch record 自身の grammar（`launch-record 1` 〜 `launch-record-end`）にも agent-type フィールドは無い。(3) 唯一 harness 側から実際に emit された、subagent_type 文字列に紐づく「偽造不能」な証跡は `launch-refused` arm の verbatim エラーメッセージだが、これは意図的に間違った role 名を使った**否定側**のケースであり、`plugin-role-n2`/`plugin-role-n4` という**肯定側**（実際に "越えた" と主張している側）には対応する証跡が一切引用されていない。Adversarial pass も独立に同じ結論（blocker）に到達し、加えて line 241 の "for every instance in every arm" という書きぶりが `launch-refused` arm（instance が一つも生成されなかった arm）にまで及ぶのは文字どおりには成立し得ない、という指摘も追加している — これも妥当。**Remediation**: (a) harness が実際に解決した agent type を検証可能な形で emit するなら、それを raw evidence に追加して re-probe する、または (b) それが不可能なら、note 自身の epistemic framing を訂正する — `subagent_type` の値は「orchestrator 自身が渡した引数の自己申告」であり、肯定側の arm については harness 発の独立確認が無いことを明記した上で、`plugin-role-subagent-type` licence condition が本当に `met` と言えるか（`undetermined` 寄りに倒すべきでないか）を pm-spec 側で再検討する。いずれの経路でも、この task の一番重い主張の信頼性に関わるため merge 前に解消が必要と判断する。

#### Major

- `templates/prompt-blocks/fanout-orchestration.md`（新設ブレット、`skills/run/SKILL.md` ミラーも同様）— launch-record の grammar 自体に agent-type を記録するフィールドが無い。adversarial pass の指摘（このレビュー独自の追加発見）: 現状のフィールド（population/requested-n/achieved-n/cap-ground/assign/liveness/epoch）は unit 割り当てと attribution を証明できるが、「実際に launch されたのが本物の plugin role であって general-purpose ではないこと」を launch record 自身は一切表現できない。上記 Blocker 2 と根は同じだが、こちらは shipped text（adversarial-depth）自体の設計ギャップであり、この round の merge を止める理由にはしない — 現行の no-new-checker 設計（population/assign/liveness/cap-ground の閉集合による 2 チェッカーへの引数一致）はこのギャップと無関係に機能しているため。Fast-follow として `- agent-type:` 相当の（audit-only の）フィールドを grammar に追加することを推奨する。

#### Minor / nits

- `docs/loop-engineering/agent-launch-fanout.md` の `clock-source-monotonic` licence condition — primary pass は P1（blocker 相当）としたが、格下げした（ledger 参照）。
- `templates/prompt-blocks/fanout-orchestration.md`（no-new-checker の文言）— `- population:`/`- assign:` は shipped checker の**引数**と一致させることで gate されると主張しているが、実際には launch record ファイル自身をどちらのチェッカーも開かない（値が一致しているかは orchestrator の自己規律に依存する）。checker の挙動は正しいままなので機能的な欠陥ではないが、"already gated" という言い切りはやや過大。Fast-follow: 文言を「orchestrator が両者を一致させて書く限り」という前提を明示する方向に調整。
- `templates/prompt-blocks/fanout-orchestration.md` の `- cap-ground:` 閉じた二値語彙（`TEAM_FANOUT_MAX=`/`measured-cores=`）— T-1074 の degree rule が持つ最終フォールバック（`measured-cores` も取れない場合の `2`）に対応する語彙が無い。`getconf` が使えない環境は極めて稀であり、実際のプローブでも発生していないため軽微。Fast-follow: 3 つ目の `cap-ground: fallback=<value>` 相当のトークンを追加。
- launch record の `achieved-n`/`launched-epoch`/`completed-epoch` の書き込みタイミング — grammar は「epoch 2 フィールドを除き launch 前に書く」としつつ、`achieved-n < requested-n`（一部起動失敗）や launch 自体が 0 件で完了する場合の epoch フィールドの扱いを明記していない。QA 自身が round 1 の hand-off で同種の finding（`achieved-n` timing ambiguity）を disclose 済みで fast-follow 判定しており、同意する。Codex primary pass も同じ穴を（epoch フィールドの必須性まで含めて）指摘しており、同一クラスとして 1 件に統合。
- `docs/loop-engineering/agent-launch-fanout.md` line 22-28 — liveness コマンドを "verbatim" と称しつつ、home-directory パスだけ `<operator-home>` にプレースホルダ置換している。AC19（`check-pii-shapes.sh`）で home path 非流出は既に独立確認済みであり機能上の欠陥ではないが、"verbatim" の語をこの hygiene 例外込みで定義し直すと親切。
- `docs/loop-engineering/agent-launch-fanout.md` line 231 の implication 文（"T-1084 may dispatch plugin-role instances concurrently"）— 実測されたのは `shell-team:qa-verifier` 1 role のみだが、この一文だけ読むと一般化して読める。ただし同じ note の `## Agent-type boundary` と `## Limits` は既にこの限定を明示しており、実害は小さい。
- `.shell-team/test-recipe.md` の T-1083 追記 — 87-spec の blast-radius sweep を `xargs -P` で行う際、共有の `.shell-team/runs` telemetry corpus を ref 間で分離していない。spec 自身の `## Blast radius` は今回この artifact を透明に disclose し、影響を受けた 6 criteria を個別に sequential 再検証して確認済みなので今回の報告値は信頼できるが、将来の同種 sweep のための手順改善として fast-follow に値する。

#### Nit

- `.shell-team/specs/T-1083-agent-launch-fanout.md` line 60 の「digit-only timestamp」規則 — Codex primary pass はこれを、無関係な既存の telemetry JSONL の `"ts":"2026-08-18T14:16:14Z"` フィールド（`bin/log-run.sh` 由来の既存スキーマ）にまで適用して矛盾を指摘したが、文脈上この規則は "Clock source" 段落が定義する **probe 自身が読んだ clock の epoch 値**についてのものであり、逐語引用された第三者スキーマのフィールドを指すものではない。過度に文字どおりの読み方だと判断し、won't-fix とする。

#### Adversarial findings

`docs/loop-engineering/agent-launch-fanout.md` 全体と `templates/prompt-blocks/fanout-orchestration.md` の launch-record ブレットに対し、上記 2 つの Blocker 候補を名指しして devil's-advocate pass を実行（raw capture: `T-1083-codex-adversarial.{txt,jsonl}`）。両方を独立に blocker として確認し、加えて (a) line 241 の "for every instance in every arm" が `launch-refused` arm（instance 未生成）にまで及ぶ点の文字どおりの不整合、(b) shipped grammar 自体に agent-type フィールドが無いという設計ギャップ（上記 Major）、を新規に発見した。一方で、hygiene 指摘の対象範囲を probe evidence 節のほぼ全域（生タイムスタンプ・token 数・provider/adapter メタデータ・core 数を含む）にまで広げていたが、これは reject した（ledger 参照）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `clock-source-monotonic` licence condition が `date +%s%N`（wall-clock、非 monotonic）を根拠にしている | primary: P1（blocker 相当） | minor | T-1073 から逐語借用した既存語彙であり本 task の新規発明ではない。note は internal analysis-artifact depth（not adversarial）の pricing 対象。単一 30 分セッションでの NTP step リスクは極めて小さく、margin が 14.5 倍あるため verdict 自体は揺るがない。 |
| 2 | probe evidence 節の hygiene 指摘対象範囲（adversarial pass は生タイムスタンプ・token 数・provider/adapter メタデータ・core 数までを違反とした） | adversarial: blocker（範囲を "Available agents" list 以外にも拡大） | 一部reject（"Available agents" list のみ blocker として維持） | machine-local な生の時刻・所要時間・token 数の逐語引用は、この note クラス（T-1069/T-1073 等、既に merge 済みの precedent）が明示的に要求する設計そのものであり（spec 自身の Non-goals: "Timings, core counts and thresholds are machine-local and are labelled as such"）、PII でも employer 識別情報でもない。個人 / employer 固有のツール名を含む "Available agents" list のみが CLAUDE.md Hygiene 節の実質的な違反であり、Blocker として維持する。 |
| 3 | shipped text の no-new-checker 主張（"already gated" の言い切り） | primary: P2（major 相当） | minor | チェッカー自体は正しく動作しており、機能上の欠陥ではない。本 repo 全体の self-attestation 前提（freeze-attestation・provenance 等も同様に自己申告）と整合する範囲の文書精度の問題。 |
| 4 | `cap-ground` の閉じた二値語彙に degree rule の最終フォールバック（`2`）に対応する形が無い | primary: P2（major 相当） | minor | 発生条件（`getconf` 不可）が極めて稀で、実プローブでも発生していない edge case。 |
| 5 | `achieved-n`/epoch フィールドの部分起動・起動失敗時の書き込みタイミング未定義 | primary: P2（major 相当）、QA 自身も同種を disclose 済み（fast-follow 判定） | minor | QA の既存判定に同意。どのチェッカーも `achieved-n` の値を実際の起動結果と突き合わせないため、不正確な値が既存の gate を壊すことはない。 |
| 6 | liveness コマンドの "verbatim" 表記と home-path プレースホルダ置換の共存 | primary: P2（major 相当） | minor | AC19（`check-pii-shapes.sh`）で home path 非流出は独立検証済み。文書精度の問題にとどまる。 |
| 7 | implication 文（line 231）の一般化しすぎた書きぶり | primary: P2（major 相当） | minor | 同じ note の `## Agent-type boundary`/`## Limits` に既に明示的な限定が記載されており、実害は小さい。 |
| 8 | blast-radius sweep の `xargs -P` 実行が共有 telemetry corpus を ref 間で分離していない | primary: P2（major 相当） | minor | spec 自身が今回の影響（6 criteria）を透明に disclose し、個別に sequential 再検証済みで今回の報告値は信頼できる。将来の手順改善として妥当だが今回の merge を止める理由にはならない。 |
| 9 | digit-only timestamp 規則と telemetry JSONL の ISO `ts` フィールドとの不整合 | primary: P2（major 相当） | nit | 規則の文脈上のスコープ（probe 自身の epoch 値）を無関係な既存スキーマにまで拡大解釈した、過度に文字どおりの読み方。 |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` を READY_FOR_ENG に戻す。Blocker 2 件（hygiene の re-probe、plugin-role 実証主張の補強 or 訂正）を解消した上で再レビューする。class-M re-freeze（v1→v2）自体は独立検証済みで問題なし — この re-freeze を理由に revert する必要はない。

#### Fast-follow disposition
- Finding 3（no-new-checker の "already gated" 言い切りの過大さ） — file-an-issue（`templates/prompt-blocks/fanout-orchestration.md`/`skills/run/SKILL.md` の文言調整。launch record の値と実際の checker 呼び出し引数の一致は orchestrator の自己規律に依存する旨を明示）。
- Finding 4（`cap-ground` フォールバック語彙の欠落） — file-an-issue（`fallback=<value>` 相当のトークン追加）。
- Finding 5（`achieved-n`/epoch の部分起動時セマンティクス未定義） — file-an-issue（QA の既存 finding と統合。1 issue で両方をカバー）。
- Finding 6（liveness コマンドの "verbatim" 表記の hygiene 例外の未文書化） — won't-fix（AC19 で機能的に既に安全。追加文書の価値は低い）。
- Finding 7（implication 文の一般化しすぎた書きぶり） — won't-fix（同じ note 内に既に限定が明記されており冗長）。
- Finding 8（blast-radius sweep の telemetry 分離） — file-an-issue（`.shell-team/test-recipe.md` の手順改善。将来の大規模 full-population sweep で同種の measurement-environment artifact が再発するのを防ぐ）。
- Finding 9（digit-only timestamp 規則の文言スコープ） — won't-fix（実害なし、過度に文字どおりの読み方と判断）。
- Major の launch-record agent-type フィールド欠落 — file-an-issue（T-1084 着手前に検討: audit-only の `- agent-type:` フィールドを shipped grammar に追加する設計。今回の merge は止めない）。

---

### Codex Review verdict (round 2): REQUEST_CHANGES
- Task: T-1083
- Codex model: gpt-5-codex (via codex-cli)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 4 files, +248/-8 lines（round 1 レビュー時点の `e68470b` から今回の `3f14acf` までの delta。`git diff --stat e68470b..HEAD`。whole-branch context の base は round 1 と同じ `01ef1108836839120217accfd8a56be4ed2ae008` のまま）

#### 検証範囲について（round 2、delta round）

spec 自身の `## Verdict scope` を round 1 と同じ適用で踏襲した。shipped-code depth 対象（`templates/prompt-blocks/fanout-orchestration.md`・`skills/run/SKILL.md` ミラー・両 README）はこの delta で無変更（`git diff e68470b..HEAD -- <それらのパス>` が空であることを自分で確認済み）なので再レビューはせず「無変更であることの確認」に留めた。internal analysis-artifact depth 対象（`docs/loop-engineering/agent-launch-fanout.md`）は round 1 の 2 Blocker が実際に閉じられたか、および round 2〜4 で行われた訂正チェーンが数値・結論を壊していないかを中心に検証した。

#### Round 1 Blocker closure — 検証結果

**Blocker 1（hygiene・operator 環境の `Available agents:` 全列挙の逐語公開）— class として closed と判断。**
- 自分で `git grep -n 'dotfiles-sync\|skill-inventory\|sonnet-engineer\|design-system-apply\|codex-rescue\|claude-code-guide\|artifact-builder\|deck-builder\|pdf-builder\|statusline-setup' -- .` を実行 → 0 hit（board の記録どおり）。実名の再転記は tracked ファイルのどこにも存在しない。
- pm-spec が load-bearing と裁定した 2 事実（nine の `shell-team:*` role 全部が出現・`shell-team:qa-verifier` が出現）は redaction 後も明記されたまま残っている（`docs/loop-engineering/agent-launch-fanout.md:134`）。
- `git ls-files -- 'agents/*.md' | wc -l` → `9` を自分で再測定し、round 2/3 QA が「ten」→「nine」に訂正した数値と一致することを確認した。

**Blocker 2（`plugin-role-subagent-type` の証跡主張）— class として closed と判断。**
- `## Verdict and licence conditions` の `- licence-condition: plugin-role-subagent-type` 行（line 225）と `## Agent-type boundary` の `- agent-type: plugin-role-n2` 行（line 247）を自分で通読した。span 行・launch record の `- assign:` id を type evidence として引用する記述は消え、「orchestrator 自身が渡した宣言ラベル」という epistemic footing に一貫して置き換わっている。mandatory counterfactual（"had `general-purpose` been declared instead ... nothing recorded would contradict it"）は line 247 に逐語で、line 225 に paraphrase で存在することを確認した。
- pm-spec 裁定 point 6 が求めた精度（「証跡が一切存在しない」と書かない）も守られている：line 136 の epistemic-status paragraph は "The harness exposes no committable artifact of the resolved type **for a successful launch**" と scope を明示しており、`launch-refused` arm 由来の証跡（同じ paragraph が引用）の存在自体は否定していない。
- span 行の実体（line 60-61）を自分で確認：`"span":"qa-verifier"`・`"model":"sonnet"` のみで `subagent_type` フィールドは存在しない。launch record の `- assign:` 行（line 26-27）も instance-id と spec path のみで agent-type フィールドは無い。line 247 の「span 行の `"span"` field と launch record の `- assign:` id は... deliberately not cited here as type evidence」という記述は実際のデータ構造と整合する。

ここまでは round 1 の 2 Blocker について、狭い意味での closure 確認としては十分である。しかし round 2 の独立レビュー（Codex primary pass、プロンプトは `codex exec review`）と自分自身の adversarial pass で、**Blocker 2 と全く同じ欠陥クラスが、Blocker 2 の修正が一度も触れていない第 3 の箇所（line 16）に生き残っている**ことを新たに発見した。下記 Blockers 参照。

#### レビュー観点 2（QA round 4 の disclosed risk note）— 独立判断

line 148 の "Applies to every instance in **the four arms that created instances**" と line 225/247 の "for every instance in the arms that created **one** (`plugin-role-n2`, `plugin-role-n4`)" の scope の非対称性について、QA round 4 の判断（矛盾ではない — 225/247 は overlap を測定する 2 arm に狭く限定した licence-condition/agent-type 主張であり、148/260 は probe 全体の population についてのより広い主張、という整理）に自分で line 260（"every probe instance this round was `subagent_type=shell-team:qa-verifier`, `model=sonnet`"）を突き合わせて確認し、**同意する**。line 148 の "four arms" は line 260 の population 全体主張と一致しており、225/247 の narrower scoping は別の目的（boundary-crossing の licence condition）のための意図的な絞り込みであって、隠れた unscoped-universal-claim ではない。

#### レビュー観点 3（訂正チェーンが数値・結論を stale にしていないか）— 独立判断

主要な算出値を、note が引用する生タイムスタンプから自分で再計算し、round 4 の inventory と一致することを確認した：
- intersection = 1787062495411558000 − 1787062355026208000 = 140385350000 ns ≈ 140.385 s（note の記述と一致）
- qa-1/qa-2 launch latency = 2694072000 ns / 9650685000 ns ≈ 2.694 s / 9.651 s（一致）
- batch = 1787062574009530000 − 1787062345375523000 = 228634007000 ns ≈ 228.6 s（一致）
- n4 window = 1787063278285424000 − 1787063177712739000 = 100572685000 ns ≈ 100.573 s（一致）

`concurrent-overlapping` verdict とその licence-condition table は、amended evidence から今も導ける。`plugin-role-subagent-type` の "met" は pm-spec 裁定どおり「self-attestation の trust boundary 上で met」という限定付きの意味で成立しており、これは本 repo の freeze-attestation/provenance 全般が既に立っている境界と同じである、という round 2 の防御は妥当と判断する。

#### Blockers

- **`docs/loop-engineering/agent-launch-fanout.md:16`（byte-locked AC7 内）— Blocker 2 と同一クラスの provenance overclaim が、Blocker 2 の修正が触れなかった第 3 のフィールド（`model`）に残存している。** 該当文: "Two instances of the plugin role `shell-team:qa-verifier` (an `agents/qa-verifier.md`-derived agent; **resolved model recorded verbatim from the emitted span rows below: `sonnet`**) were launched..." — "resolved" という語は、`model` の値が harness によって解決された（独立に確認された）ものだという印象を与えるが、同じファイルの line 136（"The `subagent_type=shell-team:qa-verifier` and `model=sonnet` values recorded for the positive arms are the orchestrator's own launch parameters, recorded verbatim as **declared labels**"）と line 148（"the span rows' `"span"`/`"model"` fields are the orchestrator's own telemetry labels and are **not harness evidence** of the resolved type"）は、`model` も `subagent_type` と全く同じ self-declared/not-harness-verified footing にあると明言している。加えて、この値を書き込むスクリプト自身（`bin/log-run.sh:62`、コメント "`model` stays **unvalidated and opaque, deliberately**"）が、`model` は検証されない値だと明記しており、line 16 の "resolved" は本 repo の shipped コード自身が否定する主張になっている。Codex primary pass・adversarial pass の双方が独立にこの箇所を blocker 級として検出した（adversarial pass: "paired with 'recorded verbatim from the emitted span rows,' it asserts unsupported provenance and contradicts the file's own Blocker-2 remediation"）。この箇所は round 1〜4 の "same-class" grep 掃引（"verbatim|resolved|every instance in every arm"）の対象範囲に文字どおり入っていたはずだが（"resolved" も "verbatim" もこの一文に含まれる）、round 4 の "full-evidence claim inventory" を含め、どの回のスイープでも捕捉されなかった。**Remediation**: 過去 3 回（"ten"→"nine"、Blocker-2 の 2 行書き換え、line 148 の第 3 サイト修正）と同じ disposition — byte-locked evidence 内への disclosed, annotated correction。新しい re-probe は不要（この一文の epistemic framing を「宣言された launch parameter」に揃えるだけで足りる）ので、frozen protocol の re-probe 予算は消費しない。verdict・licence condition のどの値も変わらないため、merge を止める理由はこの一点のみで足りる範囲だが、本 task がこれまで一貫してこのクラスの再発を REWORK 扱いしてきたこと（QA round 2・round 3 がいずれも同クラスの単一サイト発見だけで FAIL を出している）との整合を取るため、Blocker として維持する。

#### Major

（今回の delta で major 級として維持するものはなし。round 1 の Major 1 件——launch-record grammar に agent-type フィールドが無い——は shipped text 側が無変更のため再評価せず、fast-follow disposition を維持する。）

#### Minor / nits

- `docs/loop-engineering/agent-launch-fanout.md:247`（`## Agent-type boundary`）— "the one role among this repository's nine `agents/*.md` definitions that can run `bash bin/check-acs.sh`" という一意性主張は事実として誤り。`agents/*.md` の `tools:` 行を自分で確認したところ、`Bash` を持つ role は `qa-verifier` だけでなく `engineer`・`drift-evaluator`・`codex-reviewer`・`triage-orchestrator`・`scrum-master` の計 6 role ある。qa-verifier が選ばれた実際の理由（role contract が acceptance-criteria 検証を担うことなど）は正当だが、「唯一実行できる role」という書きぶりは支持されない。verdict やライセンス条件には影響しない周辺的な正当化文なので blocking はしないが、上記 Blocker の修正と同じ箇所（同じ行の後半）にあるため、同じ編集パスで直すことを推奨する。
- `docs/loop-engineering/agent-launch-fanout.md:5` — "Every filesystem path below is `$TMPDIR`-relative or a `<placeholder>`; no home-directory absolute path appears anywhere in this evidence." という header の主張は、home-directory path が無いという核心（PII/hygiene 上重要な部分）は今も真だが、"`$TMPDIR`-relative" という表現は line 22・85-86・105 に verbatim 引用されている実際の絶対パス（`/tmp/claude-502/t1083-probe-venue/...`）と字面上は整合しない（`$TMPDIR` という変数表記ではなく展開済みの絶対パスが quote されている）。verbatim quoting という制約上、これは避けがたい ("verbatim" と "`$TMPDIR`-relative" は、raw file 自体が展開済み絶対パスを含む限り両立しない)。hygiene 上の実害はなく（`check-pii-shapes` 済み、home path 不在は独立確認済み）、文言精度の話に留まる。
- `docs/loop-engineering/agent-launch-fanout.md:140`（cleanliness read の timing）— post-arm cleanliness read（timestamp `1787063497808615000`）は、Blocker 1 remediation の re-probe（timestamp `1787076324656839000`、約 3.5 時間後）より前に行われている。re-probe 自身の記録（"no instance was created, no transcript file appeared" — オリジナルと re-probe の両方で同一）は、この arm が構造的に「同期的拒否＝ファイルシステムへの書き込みが一切発生しない」事象であることを示しており、実害リスクは adversarial pass 自身も "low practical impact" と評価している。ただし「every arm had finished」という書きぶりが文字どおりには re-probe を含んでいない、という指摘自体は正しい。fast-follow として、re-probe 後に同じ `git status --short`/`git ls-files --others` を再実行して 1 行追記するか、この arm の zero-footprint 性質そのものを明示的に cleanliness の根拠として書き添えるのが望ましい。
- `docs/loop-engineering/agent-launch-fanout.md:5` vs `:124-136` — header は "Everything in this section is a raw observation" と述べるが、line 124-136 には後から追加された amendment/correction blockquote（epistemic-status paragraph を含む、明確に分析的な文章）が同じ byte-locked セクション内に同居している。これは本 task がこれまで 3 回とも採用してきた disposition（訂正は byte-locked evidence 内に `>` の blockquote として disclosed に置く）の必然的な帰結であり、blockquote という視覚的な区別もあるため実害は小さい。Codex adversarial pass は major 級としたが、既に確立された、self-evident な設計判断であるため nit に格下げする。
- `docs/loop-engineering/agent-launch-fanout.md:148` — 見出し的な語 "resolved agent identity" と、本文が明言する "declared labels" という結論が同じ行の中で語感として食い違う（本文は正確なので実害なし）。cosmetic。
- 既存（round 1 から継続、shipped text 側は無変更につき今回は再評価せず据え置き）: no-checker "already gated" の言い切り／`cap-ground` フォールバック語彙欠落／`achieved-n` 部分起動時タイミング未定義／launch record に agent-type フィールドが無い／liveness コマンドの "verbatim" 表記と home-path プレースホルダ置換の共存／implication 文の一般化しすぎた書きぶり／blast-radius sweep の telemetry 分離／digit-only timestamp 規則の scope。

#### Adversarial findings

`docs/loop-engineering/agent-launch-fanout.md` の 4 箇所（line 16 の "resolved model"、line 247 の一意性主張、line 5 の path 表現、line 140 の cleanliness timing）を名指しして devil's-advocate pass を実行（raw capture: `T-1083-codex-adversarial.{txt,jsonl}`）。line 16 を独立に blocker 級と判定し、`bin/log-run.sh:58-62` の "model stays unvalidated and opaque, deliberately" という一次ソースを自分で引用して根拠を補強した。line 247 の一意性主張については `agents/*.md` の `tools:` 行を実際に列挙し、6 role が `Bash` を持つことを示して false と確認した。line 5 の path 表現については「hygiene 上の実害はないが字面上の矛盾」という評価で一致。line 140 については "does not prove zero filesystem footprint... low practical impact" という評価で、私の判断（re-probe の構造的性質により実害は小さい）と一致した。加えて、line 5 の "raw observation" 表現と line 124-136 の amendment 文の同居を major 級として新規指摘したが、これは既存の disposition パターンの必然的帰結と判断し nit に格下げした（ledger 参照）。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | line 16 "resolved model ... verbatim from emitted span rows" | primary: P2（major 相当）、adversarial: blocker | blocker | `bin/log-run.sh:62` が `model` を "unvalidated and opaque, deliberately" と明記しており、line 136/148 の declared-label framing と直接矛盾する。Blocker 2 と同一クラスの provenance overclaim が別フィールドに残存しているため、本 task がこれまでこのクラスに適用してきた扱い（同クラス単発発見でも REWORK）と整合させ、blocker として維持する。 |
| 2 | line 247 "the one role ... that can run bash bin/check-acs.sh" の一意性主張 | primary: P2（major 相当）、adversarial: minor | minor | `agents/*.md` の `tools:` 行を実測すると `Bash` を持つ role は 6 つあり主張は事実として誤りだが、verdict・licence condition のどれにも影響しない周辺的な正当化文。 |
| 3 | line 5 "every path is `$TMPDIR`-relative" vs line 22/85-86/105 の verbatim 絶対パス | primary: P2（major 相当）、adversarial: minor（"literal contradiction, but not a home-path hygiene breach"） | minor | home path 不在（実質的な hygiene 要件）は今も真で `check-pii-shapes` 済み。"verbatim" 引用という制約上、展開済み絶対パスの出現は不可避であり、文言精度の問題に留まる。 |
| 4 | line 140 の cleanliness read が re-probe（3.5 時間後）より前に行われている | primary: P2（major 相当）、adversarial: minor（"low practical impact"） | minor | `launch-refused` arm は同期的拒否で instance も transcript も生成しない、という構造的性質が original・re-probe 双方で同一に観測されており、実質的なリスクは小さい。 |
| 5 | line 5 "raw observation" と line 124-136 の amendment/epistemic-status paragraph の同居 | adversarial: major（新規） | nit | 本 task がこれまで 3 回採用してきた「訂正は byte-locked evidence 内に blockquote で disclosed に置く」disposition の必然的帰結であり、blockquote による視覚的区別もあるため実害は小さい。 |
| 6 | `date +%s%N` を "monotonic" と称する（round 1 finding 1 の再検出） | primary: 変更なし（round 1 と同じ P1 相当） | minor（round 1 と同じ） | round 1 のledgerと同じ理由（T-1073 からの借用語彙、margin 14.5倍）で変更なし。今回新規の判断材料は無い。 |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` を READY_FOR_ENG に戻す。今回の Blocker（`docs/loop-engineering/agent-launch-fanout.md:16` の provenance overclaim、1 文の disclosed correction で足りる、re-probe 不要）を解消した上で再レビューする。round 1 の 2 Blocker は class として closed と判断しており、蒸し返す必要はない。class-M re-freeze（v1→v2）についても round 1 で独立検証済みで今回変更なし。

#### Fast-follow disposition
- line 247 の一意性主張（"the one role ... that can run bash bin/check-acs.sh"）— file-an-issue（今回の Blocker 修正と同じ編集パスで直すことを推奨するが、独立にも issue 化しておく：`agents/*.md` の `Bash` 保有 role は 6 つあるため、選定理由を role contract ベースの表現に書き換える）。
- line 5 の "`$TMPDIR`-relative" 表現と verbatim 絶対パスの共存 — file-an-issue（header の文言を「`$TMPDIR` 配下であり、verbatim 引用箇所は展開済み絶対パスを含む」に調整）。
- line 140 の cleanliness read と re-probe のタイミング — file-an-issue（re-probe 後に同じ cleanliness コマンドを再実行して 1 行追記するか、zero-footprint の構造的根拠を明示する）。
- line 5 "raw observation" と amendment blockquote の同居 — won't-fix（確立済みの disclosed-correction disposition の必然的帰結、blockquote による視覚的区別で十分）。
- line 148 の見出し "resolved agent identity" と本文 "declared labels" の語感の不一致 — won't-fix（本文が正確であり実害なし）。
- round 1 から継続する fast-follow（no-checker "already gated" の言い切り／`cap-ground` フォールバック語彙／`achieved-n` タイミング未定義／launch-record agent-type フィールド欠落／liveness "verbatim" 表記／implication 文の一般化／blast-radius telemetry 分離／digit-only timestamp scope）— disposition 変更なし、round 1 のとおり。

---

### Codex Review verdict (round 3): APPROVE
- Task: T-1083
- Codex model: gpt-5-codex (via codex-cli)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 2 files, +45/-2 lines（round 2 レビュー時点の `c404ee7` から今回の `1b36705` までの delta。`git diff --stat c404ee7..HEAD`。whole-branch context の base は round 1/2 と同じ `01ef1108836839120217accfd8a56be4ed2ae008` のまま）

#### 検証範囲について（round 3、最終ラウンド delta）

自分の round 2 の `REQUEST_CHANGES` 判定（`.shell-team/reviews/T-1083.md` 内、round 2 verdict block）を先に読み直した。今回の delta は `git diff c404ee7..HEAD` で確認したとおり 2 ファイルのみ: (1) `docs/loop-engineering/agent-launch-fanout.md` の 1 文（line 16、round 2 Blocker の修正そのもの）、(2) `.shell-team/todo.md` への board 追記 2 節（round-5 hand-off・QA round 5）と probe-evidence-sha の更新。shipped-code depth 対象（`templates/prompt-blocks/fanout-orchestration.md`・`skills/run/SKILL.md` ミラー・両 README）はこの delta で無変更（round 1 以来ずっと無変更）なので再レビューはせず「無変更であることの確認」に留めた。検証対象は task 指示どおり (a) round 2 Blocker の class 閉塞確認、(b) line 16 の内部整合性、(c) 他が動いていないことの確認、(d) fast-follow disposition の再宣言、の 4 点。

#### 1. round 2 Blocker のクラス閉塞 — 独立検証

- 自分で `grep -n 'verbatim from the emitted span rows' docs/loop-engineering/agent-launch-fanout.md` を実行 → line 16・148 の 2 hit のみ。両方を通読し、いずれも `*(Corrected 2026-08-19, ...)*` という訂正注釈の内部で「削除された旧表現」として引用されているだけで、生きた主張としては存在しないことを確認した（QA round 5 の記録と一致）。
- 自分の phrasing で独自に broader sweep（`grep -niE 'resolved|span row|harness evidence|harness-observed|harness-derived|proves|independently confirm'`）を実行し、全 hit（line 16, 18, 57, 134, 136, 148, 164, 177, 210, 225, 228, 247）を通読した。`subagent_type`/`model` に関する生きた主張はすべて「orchestrator 自身の宣言ラベルであり harness 発の独立確認ではない」という一貫した epistemic footing に立っており、span rows／launch record を type evidence の**出典**として引用している箇所はゼロだった。新規の該当サイトは見つからなかった。
- round 2 の Blocker 自体（line 16 が Blocker 2 と同一クラスの overclaim を第 4 のサイトで残していた件）は、今回のこの delta が唯一の修正対象であり、正しく修正されている。

#### 2. line 16 の内部整合性 — 独立検証

- line 16 の生きた文（"model `sonnet` — the orchestrator's own launch parameter, a declared label like the type itself"）を、line 136（epistemic-status paragraph）・line 148（AC22 items h/i の記録）と突き合わせて読んだ。3 箇所とも「`model`/`subagent_type` は orchestrator 自身が渡した宣言値であり、span rows はその宣言を裏書きする harness 発の独立証跡ではない」という同一の立場で一貫している。
- `bin/log-run.sh` のヘッダを自分で読んだ（`sed -n '58,62p' bin/log-run.sh`）: line 62 = "`model` stays unvalidated and opaque, deliberately" — 訂正注釈が引用する内容と一致し、事実として正確。

#### 3. 他が動いていないことの確認

- AC7 byte-lock: `## Probe evidence (raw, orchestrator-produced)` セクションを `e5aaa2d` と `HEAD` の両方から自分で切り出し `diff` → 完全に無差異（149 行・両側一致）。QA round 5 の claim と一致。
- AC8 byte-lock 対象（spec 側の `probe-protocol` region）: `git diff c404ee7..HEAD -- .shell-team/specs/T-1083-agent-launch-fanout.md` → 空。無変更を確認。
- shipped surfaces（`templates/prompt-blocks/fanout-orchestration.md`・`skills/run/SKILL.md`・両 README）: `git diff --stat c404ee7..HEAD` の対象ファイルリストに一切含まれず、round 1 状態のまま。
- intent alignment: board 上の `intent-hash (v2)` と QA round 5 が独立実行した `check-intent.sh` の結果 (`aligned: T-1083 v2 (fb11f4d6...)`) が一致していることを確認した。v2 は round 1 で既に独立検証済みの mechanics re-freeze であり、今回の delta はこれに触れていない。

#### Blockers

（なし）— round 2 の Blocker はクラスとして閉じたことを独立に確認した。

#### Major

（なし）

#### Minor / nits

- **`.shell-team/todo.md:173`（今回の round-5 hand-off で新規追加）— operator 承認フレーズ「最終1ラウンド許可」の逐語引用が、`CLAUDE.md` Hygiene 節（"Do not add private context: ... session transcripts, or audit output carrying real values"）と字面上ぶつかる。** primary pass（`codex exec review`）は P2（major 相当）としてこれを検出した。Devil's-advocate pass を追加実行（raw capture: `T-1083-codex-adversarial.{txt,jsonl}`）した結果、以下の理由で minor に格下げした（ledger 参照）: 同一ファイル内に事実上同一のパターン（line 216「1ラウンド延長で」、round 4 で追加）が既に存在し、この同じ質問を自分自身の round 1・round 2 のレビュー範囲（round 2 の diff scope `e68470b..HEAD` は round 4 の追記を包含していた）で一度も指摘しなかった precedent がある。adversarial pass 自身も、この引用に PII・雇用主識別情報・秘密情報が一切含まれないことを独立に確認した。一貫性を優先し、今回の最終ラウンドで急に blocker/major に格上げするのは筋が悪いと判断し、fast-follow としてまとめて扱う。

#### Adversarial findings

`.shell-team/todo.md` の line 173（今回の新規追加）と line 216（round 4 由来の precedent）を名指しして devil's-advocate pass を実行した。結論: 「line 173 は line 216 と実質同一の governance-approval marker であり、precedent が 2 回のレビューラウンドを無指摘で通過している以上、line 173 だけを merge-blocking として扱うのは一貫性を欠く。両サイト（および他に残る同種箇所）をまとめた単一の fast-follow クリーンアップが適切」と判定し、severity は `minor`、`is_new_violation_distinct_from_precedent: false`、`contains_pii_or_secrets: false` という構造化結果を返した。私の判断と一致する。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `.shell-team/todo.md:173` の operator 承認フレーズ「最終1ラウンド許可」の逐語引用が CLAUDE.md Hygiene 節の "session transcripts" 禁止と字面上ぶつかる | primary: P2（major 相当）／adversarial: minor（"a single fast-follow cleanup covering both sites... not a merge blocker"） | minor | 同一ファイル内に事実上同一のパターン（line 216「1ラウンド延長で」、round 4 で追加）が既に存在し、この同じレビュアー自身の round 1・round 2 のレビュー範囲で一度も指摘されなかった precedent がある。adversarial pass も PII・秘密情報の非含有を独立確認。一貫性のため単一の fast-follow に統合する。 |

#### Recommendation
- APPROVE → `.shell-team/todo.md` を READY_FOR_MERGE に設定

#### Fast-follow disposition

**round 1 から継続（disposition 変更なし）:**
- no-new-checker の "already gated" という言い切りの過大さ（`templates/prompt-blocks/fanout-orchestration.md`／`skills/run/SKILL.md`）— file-an-issue。
- `cap-ground` の閉じた二値語彙に degree rule の最終フォールバック（`2`）に対応する形が無い — file-an-issue。
- `achieved-n`/epoch フィールドの部分起動・起動失敗時の書き込みタイミング未定義 — file-an-issue（QA の既存 finding と統合）。
- liveness コマンドの "verbatim" 表記と home-path プレースホルダ置換の共存 — won't-fix（AC19 で機能的に安全）。
- implication 文（line 231 系）の一般化しすぎた書きぶり — won't-fix（同じ note 内に既に限定が明記）。
- blast-radius sweep の `xargs -P` 実行が共有 telemetry corpus を ref 間で分離していない — file-an-issue（`.shell-team/test-recipe.md` の手順改善）。
- digit-only timestamp 規則と telemetry JSONL の ISO `ts` フィールドとの不整合 — won't-fix（過度に文字どおりの読み方、実害なし）。
- launch-record grammar に agent-type フィールドが無い（shipped text の設計ギャップ） — file-an-issue（T-1084 着手前に検討）。

**round 2 で追加（disposition 変更なし）:**
- line 247 の一意性主張（"the one role ... that can run bash bin/check-acs.sh"、`Bash` 保有 role は実際には 6 つ） — file-an-issue。
- line 5 の "`$TMPDIR`-relative" 表現と verbatim 絶対パスの共存 — file-an-issue。
- line 140 の cleanliness read と re-probe（3.5 時間後）のタイミング — file-an-issue。
- line 5 "raw observation" と amendment blockquote の同居 — won't-fix（確立済みの disclosed-correction disposition の必然的帰結）。
- line 148 の見出し "resolved agent identity" と本文 "declared labels" の語感の不一致 — won't-fix（本文が正確、実害なし）。

**round 3 で新規追加:**
- `.shell-team/todo.md` に散在する、operator 承認フレーズの日本語逐語引用（line 173「最終1ラウンド許可」・line 216「1ラウンド延長で」、および同種の他サイトがあれば含む） — file-an-issue（governance-approval マーカーは英語のパラフレーズ、または「operator granted an extra round (date)」のような定型英語表現に置き換え、逐語の日本語発話引用を tracked board から除去する。CLAUDE.md Hygiene 節の "session transcripts" 禁止との字面上の抵触を一括解消する狙い。PII・秘密情報は含まれていないため merge は止めないが、公開 repo の hygiene 規律としては直しておくべき）。
