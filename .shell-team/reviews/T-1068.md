### Codex Review verdict: REQUEST_CHANGES
- Task: T-1068
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 5 files changed, +609/-0 lines (`29c35d9...HEAD`; base = `git merge-base feature/1067-context-lifecycle HEAD` = `29c35d97ed1c9468cc82c38598f75f4be1d842ab`)

前提として、`bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` = 10/10 PASS、`bash bin/check-intent.sh .shell-team/specs/T-1068-agent-concurrency.md .shell-team/todo.md` = aligned (`a2547b53...`) を自分で再実行して確認した。凍結項目に問題はない。以下の findings はすべて「Verdict scope」でいう **computed values の正しさ** と **conclusions' direction** に関するもので、connective prose には触れていない。

#### Blockers

- `docs/loop-engineering/agent-concurrency.md:55` — `validate-parallel-review` の `reconcile-cost` サブバレットは「QA が実際に失敗する割合は、この note には測定するための recorded field がなく、見積もらない (`a rate this note does not have a recorded field to measure and does not estimate`)」と書いているが、これは事実に反する。`bin/log-run.sh` の telemetry 契約は各 span 行に `--verdict`（PASS/FAIL/APPROVE/REQUEST_CHANGES、null 許容）と `--iteration`/`--attempt` を記録しており、実データ (`.shell-team/runs/shell-team.jsonl`) にも実際に埋まっている。この note が引用している P11-telemetry の 11 タスクに対して自分で再集計したところ、`qa-verifier` の FAIL 行は T-1058=1件、T-1061=1件、T-1065=4件の計 6 件（11 タスク中 3 タスク、27.3% に少なくとも1回の FAIL）存在した（`grep '"span":"qa-verifier"' .shell-team/runs/shell-team.jsonl | grep -c '"verdict":"FAIL"'` を各タスクの `run_id` で絞って再現、確認済み）。しかも issue #252 の実本文（`gh`/TLS 経路は sandbox で失敗したが `curl https://api.github.com/repos/RipsawJP/shell-team/issues/252` で直接取得・照合済み）は、まさにこの trade-off を名指しで要求している："Model the trade: a review round spent on code QA then fails is wasted; for tasks whose expected review outcome is a 1-round APPROVE the expected saving is large." — つまり「測定できないので見積もらない」という記述自体が誤りであるだけでなく、issue の受入れ条件が明示的に要求した計算をスキップしている。修正すれば headline の 33.4% という `saving` は过大評価であり、下方修正が必要（Adversarial パスの感度計算では 22.9%〜28.6% 相当まで下がる — 下記参照）。この `- saving:` 行自体が computed value であり、`## Recommendation and follow-ups` の "a real modeled saving of 33.4%" という conclusion's direction にも直結するため、DP2 の「reconcile-at-join cost that must be subtracted」という自己規定にも反する。

- `docs/loop-engineering/agent-concurrency.md:48,125` — `validate-parallel-review` は "S" コストで「two readers, no writer / structurally safest」と結論しているが、現行の agent contract 下ではこの通りに同時実行できない。`agents/codex-reviewer.md` 自身の Preconditions は「Task is at READY_FOR_REVIEW in tasks/todo.md」であり、QA が PASS して board flag を進めるまでレビューは開始できない。かつ `agents/codex-reviewer.md` の Step 2 は QA の hand-off を読むことを要求しており、QA が完了する前には読めるものが存在しない。さらに `agents/qa-verifier.md:66` は QA が board への append を書くことを明記し、`codex-reviewer.md` はレビュー record を書く——つまり実際には reader が2つでなく、writer も2つ存在する（disjoint target なので衝突はしないが、"no writer" という言い回し自体が矛盾）。真に並行実行するには、少なくとも (a) `codex-reviewer` の `READY_FOR_REVIEW` precondition の変更、(b) QA hand-off が未完成でも開始できる入力契約、(c) `skills/run/SKILL.md` の phase 順序と join 処理の変更、(d) QA FAIL 時にレビュー結果を破棄する記録規則、が必要であり、これらはいずれも `agents/` または `skills/` の変更＝本タスクの Non-goals（"No mechanism of any kind ships... nothing under ... agents/ or skills/ changes"）が禁じているものそのものである。つまり "S" という cost sizing はこの前提変更のコストを含んでおらず、`## Recommendation and follow-ups` が挙げる 2 つの precondition（telemetry lock、Agent-tool 同時実行の実証）だけでは足りない。

- `docs/loop-engineering/agent-concurrency.md:107-112` — Pilot probe は `- probe: validate-review-concurrent-readers — ran — ` と報告しているが、凍結された DP1（`.shell-team/specs/T-1068-agent-concurrency.md` の Settled decisions、intent block 内）は「Every background agent the probe launches runs with a watchdog started at launch, with no exceptions. A probe that cannot satisfy all of this is `declined`」と例外なく規定している。実際の probe コマンド（line 111: `... & PID1=$!; ... & PID2=$!; wait ...`）に watchdog は一切起動されておらず、note 自身も substrate インベントリで `watchdog — absent`（line 99）と自ら測定している。DP1 の要求を満たせない以上、正しい記述は `declined`（または少なくとも DP1 違反を hazard として明示開示した上での "ran"）であるはずで、現状の無条件の "ran" ラベルは凍結された安全要件の充足を偽って主張している。

#### Major

- `docs/loop-engineering/agent-concurrency.md:92` — `full-population-diff-cpu` を無条件に `hard`（redesignable ではなく）と分類しているが、根拠は「並行実行しても total CPU-seconds は変わらず、単一マシンでは cache/scheduler contention で wall-clock が悪化しうる」という一般論のみで、実際のコア数・並行実行時のコア飽和度は測定していない。マルチコアマシンで単一スレッド中心の2プロセスが別コアで動けば、他の候補（`validate-parallel-review` 含む）と全く同じ理屈で wall-clock は短縮しうる。この note の他のすべての候補が拠って立つ「並行実行で wall-clock が縮む」という前提と、この一箇所だけ矛盾する形で `hard` と断定しており、`- command:` による測定もない。`redesignable`（容量依存）と分類するか、コア飽和度を測ってから `hard` と結論すべき。

- `docs/loop-engineering/agent-concurrency.md:40` — telemetry 再導出テーブルの T-1058 の `codex-reviewer=2165347` は、実際には `.shell-team/runs/shell-team.jsonl` 上の2つの別々の `codex-reviewer` span 行（`seq=12` verdict=APPROVE, duration_ms=1079568 と `seq=100` verdict=REQUEST_CHANGES, duration_ms=1085779、両方とも `iteration=1, attempt=1`）の単純合算である（自分で該当行を確認済み）。しかし正典のレビュー記録 `.shell-team/reviews/T-1058.md` は "## Round 1" の APPROVE 一件のみを記録しており、2ラウンド目の記録が無い。つまりこの task の `codex-reviewer` 合計時間には、正典記録に対応しない telemetry 上の重複（もしくは異常な再実行）が混入しており、これが `avg_per_round_ms=1540756`（line 46、reconcile-cost の proxy として使われる数値）を実際より高く歪めている可能性がある。note が継承した de-dup key（`run_id`,`seq`,`span`, last-encountered wins）はこの種の「同一 iteration/attempt に対する複数の異なる seq」を検出できない——集計前にこの異常を disclose するか、除外すべきだった。

#### Minor / nits

- `docs/loop-engineering/agent-concurrency.md:107` — pilot probe の hazard(2) は AC1/AC8/AC9/AC10 の失敗原因を「detached scratch clone (rather than a branch-tracking one)」に帰属させているが、実際に line 108 のコマンド (`git clone --quiet --branch feature/1067-context-lifecycle --single-branch ...`) を自分で再実行して確認したところ、生成される clone は detached HEAD ではなく `feature/1067-context-lifecycle` を追跡するローカルブランチをチェックアウトした状態だった（`git rev-parse --abbrev-ref HEAD` → ブランチ名そのもの、detached ではない）。観測された4件の AC 失敗自体は実在するが、原因説明として書かれている「detached だから」は誤り。真因は別（別ブランチ由来の ref 解決差異など）にある可能性が高く、この一文は修正が必要。
- `docs/loop-engineering/agent-concurrency.md:125` — Recommendation の "the cheapest and structurally safest candidate (two readers, no writer, disjoint write targets, ...)" は、DP1 が probe 専用に定義した "two readers, no writer" という語句を実際の候補（qa-verifier と codex-reviewer という2つの writer が disjoint な対象に書く）に転用しており、"no writer" と "disjoint write targets" が同じ節内で矛盾している。`## Overlap candidates` 本文（line 48）の記述の方が正確（"write to disjoint targets ... so the two roles never write the same file"）なので、Recommendation側の言い回しを本文に揃えるべき。
- `docs/loop-engineering/agent-concurrency.md:117` — invariant-lock `fail-closed-checkers` は「qa-verifier and codex-reviewer would each still run the same existing fail-closed checks (`check-acs.sh`, `check-pii-shapes.sh`, and the rest)」と述べるが、`agents/codex-reviewer.md` は既定で static-only であり qa-verifier のようにテストスイートは実行せず、`check-intent.sh` の実行も codex-reviewer の責務外と明記されている（同ファイル内の記述）。両ロールが「同じチェック」を走らせるという記述は不正確——実際には検証の深さが異なる。

#### Adversarial findings

Round-1 の blocker/major を受けて、上記3項目を的に「何を壊せるか」を Codex に再度当てた（`.shell-team/reviews/T-1068-codex-adversarial.{txt,jsonl}`）。3項目とも独立に "BREAKS" と再確認され、以下の追加の定量データが得られた（自分で再現・検算済み）:
- P11 全体で `qa-verifier` FAIL は 43 ラウンド中 6 ラウンド＝14.0%、タスク単位では 11 中 3（27.3%）。この失敗ラウンドの平均 codex-reviewer debit（note 自身の `avg_per_round_ms=1540756`）を単純に6回分差し引くと `(29,497,963 − 6×1,540,756) / 88,399,628 ≈ 22.9%`、タスク単位の総計ではなく「実際に PASS→Review が対応したラウンドだけ」の `min` を再集計すると `25,260,594 / 88,399,628 ≈ 28.6%` となり、いずれも headline の 33.4% を下回る。
- `agents/codex-reviewer.md`/`agents/qa-verifier.md`/`skills/run/SKILL.md` の該当行を突き合わせ、"two readers, no writer" 前提が現行契約と矛盾すること、および同時開始には最低5種の contract 変更が要ることを具体的に列挙した。
- Pilot probe の watchdog 欠如について、「shell job が agent でないなら DP1 の agent probe を実施していない／agent の代理とみなすなら watchdog なしで DP1 違反」という二択のいずれでも "ran" は成立しないと結論。
- 追加で見つけた T-1058 telemetry 二重計上、"same fail-closed checks" の不整合、detached-clone の誤帰属は、いずれも自分で一次資料と照合し直して真であることを確認した（上記 Major/Minor に反映済み）。

#### Synthesis audit ledger

no severity changes — Codex 側のネイティブ severity（P1×3, P2×1）は正規化表の通り blocker×3 / major×1 として採用し、格上げ・格下げ・却下は行っていない。追加で見つけた3件（T-1058二重計上・detached-clone誤帰属・fail-closed-checks不整合）は Codex/Adversarial のどちらの回でも severity ラベル無しで提示された新規findingであり、「変更」や「却下」には当たらない（それぞれ自分の判断で major/minor として初回付番した）。Input space の「Out-of-scope synthetic extremes」に照らしても、今回のfindingsはすべて note 自身が reachable と宣言した input class（telemetry の実データ、既存 agent 定義の文面、実行可能な git コマンドの挙動）に基づくものであり、`out-of-input-space` 格下げの対象になるものは無い。

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1068 エントリを `READY_FOR_ENG` に戻す。Blockers 3件（33.4%の再計算/reconcile-cost debitの明示、validate-parallel-reviewの"S"サイジングと安全性記述の是正、pilot probeのDP1準拠——`declined`への変更またはwatchdog付き再実行）と Major 2件（`full-population-diff-cpu`分類の再検討、T-1058 telemetry重複の除外/開示）は、いずれも Verdict scope が定める computed value / conclusion's direction の欠陥であり、次ラウンドでの解消が必須。Minor/nits 3件は connective prose 相当で任意（下記 Fast-follow disposition）。Pre-commitment（同spec同節）の「2回連続でprobeに対する新規独立のBlocker/Majorが着地したらprobeをdeclinedに落とす」自己規律はまだ1round目のため発火しないが、今回のBlocker#3はまさにその対象クラスに当たることを engineer 側に申し送る。

#### Fast-follow disposition

- `docs/loop-engineering/agent-concurrency.md:107`（detached-clone誤帰属の文言修正） — won't-fix 相当で次ラウンドの本修正（Blocker#3のprobe再検討）に自然に巻き込まれる見込みだが、単独では merge を止めない connective-prose 級の誤り。engineer が同じ節を触るなら合わせて直すことを推奨するが、直さなくても再ブロックはしない。
- `docs/loop-engineering/agent-concurrency.md:125`（"two readers, no writer, disjoint write targets" の言い回し矛盾） — 同上、won't-fix 相当（本文lines 48の正確な記述と重複しており実害は小さい）。
- `docs/loop-engineering/agent-concurrency.md:117`（fail-closed-checkers invariant-lockの精度） — file-an-issue 相当の価値はあるが、本タスクのno-mechanism scopeの範囲内で文言修正のみで足りるため、次ラウンドで直せるなら直す方が安く、issueを立てるほどの規模ではないと判断（won't-fix、次ラウンドでの併修正を推奨するに留める）。

### Codex Review verdict (round 2): REQUEST_CHANGES
- Task: T-1068
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 3 files changed, +66/-23 lines (`git diff d78ddd9..83add0c`)

Round-1の3 Blockers+2 Majorsについて、まず解消状況を自分で独立に再検証した（詳細は本節末尾の Additional notes 参照）。結論として、round-1 Blocker 1（33.4%過大評価）と Major 2（T-1058二重計上）は完全に、正確に、独立再現可能な形で解消されている。しかし round-1 Blocker 3（pilot probeのDP1準拠）の「修正」が実質的に機能していないことを新たに発見した——これは round-1 と同じ「probeの安全性」クラスに対する**2連続の独立Blocker**であり、frozen spec の Pre-commitment（`.shell-team/specs/T-1068-agent-concurrency.md:257-259`）を発火させる。

#### Blockers

- `docs/loop-engineering/agent-concurrency.md:128` — **Pre-commitmentの factual trigger が発火**。round 2 で追加された watchdog（`while kill -0 "$PID1" ...; sleep 0.2; if heartbeats>=150 then log STALL; exit 1`）は、実際にはDP1が要求する「stallを検知して対処する」機構になっていない。自分でスクリプトの制御フローを読んだ結果を確認する: (1) watchdog は `PID1` を一切 `kill` しない、(2) 親シェルは watchdog の判定を待たずに無条件で `wait "$PID1"` を先に実行する、(3) したがって `PID1` が本当にstallした場合、親も `wait "$PID1"` で永久に停止し、watchdogの`STALL`ログにも`wait "$WD1"`以降の分岐にも到達しない——つまりこのwatchdogは「PID1が正常終了した場合にのみ`exited-normally`と書ける傍観ログ」に過ぎず、DP1が要求する「検知して対処する」安全機構としては機能しない。round-1 のBlocker 3（「watchdogが一つも無い」）と round-2 のこの発見（「追加されたwatchdogが構造的に無力」）は、どちらも spec の Pre-commitment がいう"the same class of ... findings ... against the live pilot probe — its ... safety"に該当する独立した2件であり、2連続ラウンドの factual trigger（`.shell-team/specs/T-1068-agent-concurrency.md:257`）を満たす。spec はこの場合の disposition を既に決定済み（同`:259`）: probeを`declined`に落とし、`measured`ラベルの saving があれば `modeled`/`undetermined` へ格下げ（現状 `validate-parallel-review` の saving はまだ `modeled` なので該当なし）、probeが観測するはずだったhazardを`unobserved`と明記し、probeの要件を`- follow-up:`行として残す——これは3回目のwatchdog修正を試みるのではなく、**すでに決定済みの declined への切り替えを適用するだけ**でよい。

#### Major

- `docs/loop-engineering/agent-concurrency.md:47,52-55` — `validate-parallel-review`のreconcile-cost debit（7,853,671.67ms、24.47%への調整根拠）は、QA FAIL 1回ごとに「その task の平均per-round codex-reviewer時間の全額」を控除しているが、真に並行実行される設計の下では、reviewerはQAと同時に走っているため、QAがFAILした時点でreviewerの時間の**全額**が追加のwall-clock遅延になるわけではない——追加されるのは高々 `max(0, reviewer時間 − 当該FAILラウンドのQA時間)` である。自分で各FAILラウンドの実測durationを取得し（T-1058のFAIL round=855438ms、T-1061のFAIL round=1606704ms、T-1065の4件=691335/261676/489083/729609ms）、この式で再計算すると debit は約3,387,586msにとどまり（現状より約446万ms小さい）、同じ分子構造を保った近似では saving は **約29.6%**、round単位でPASS→reviewをペアリングし直す、より厳密な再構成では**約25.05%**となる——いずれも現状の24.47%より高く、方向としては「過小評価」側に振れていた（33.4%からの下方修正自体は正しかったが、下げすぎ）。また同じ箇所の「reconcile-cost: **measured**, not declined」という形容も不正確——実測なのはFAIL件数と既存の実durationであり、投機的に走ったreviewerの時間や実際のjoin遅延そのものはこの pilot が観測したものではなく、モデル化された反実仮想値である。数値の方向を反転させるものではないため Blocker ではなく Major とするが、`- saving:`行の"computed value"としての正確性に関わるため次ラウンドで是正が必要。
- `docs/loop-engineering/agent-concurrency.md:113` — substrateインベントリの`background-agent-launch — provided`は、根拠として述べているのがBashの`run_in_background`パラメータ（=シェルジョブのバックグラウンド実行）のみであり、note自身が別の場所（`## Recommendation and follow-ups`、`## Limits`）で「本物のAgent-tool同時実行が可能かはこのpilotでは検証できておらず、経験的確認が必要」と明記している内容と、同じnote内で直接矛盾している。「Bashジョブの並行実行」と「Agent-tool（実際のサブエージェント呼び出し）の並行実行」は別の能力であり、後者について"provided"と書くのは尚早——`background-agent-launch`の評価根拠をBashレベルの能力に限定して明記するか、Agent-tool水準の行を`undetermined`として別立てすべき。

#### Minor / nits

- `.shell-team/interventions/T-1068.md:8` — 「41 of 47 suite scripts under tests/ generating per-run scratch roots via mktemp -d」という記録を自分で再検証したところ、コメントを除外して`mktemp -d`を実際に呼ぶスクリプトは**39件**（`mktemp`全般では40件、コメントを除外しない素朴な`grep -l mktemp`では41件——`tests/check-contract/run.sh`のコメント言及を誤って含む）であり、記録中の「41」は素朴な数字、「mktemp -d」という限定は実際には39件の方を指すべきで、両者が混在している。ただしこのファイルは engineer が編集しない orchestrator-owned の記録（本タスクの provenance / engineer hand-off にも明記済み）であり、`docs/loop-engineering/agent-concurrency.md`自身の該当測定（AC4の`- command:`、40件・comment-excluded、`mktemp -d`限定ではない）はこの誤りを継承していない——deliverable自体のcomputed valueは正しい。

#### Adversarial findings

上記2件（watchdogの無力化、debitの粒度）を的にした敵対的読みは両方とも独立に"BREAKS"と再確認した。Watchdogについては制御フローの読みが自分の再現と完全に一致（`wait`が watchdog の判定を待たない、PID1が真にstallすれば親ごと永久停止）。Debitについては2種類の代替計算を提示: (a) 現状と同じ「task合計min」の分子構造を保ったまま debit だけ `max(0, ...)` に修正 → **約29.58%**、(b) PASS→reviewをround単位でペアリングし直す、より厳密な再構成 → **約25.05%**——いずれも24.47%より高いことで一致し、「現状の24.47%が正しい」ことにはならないが「過大評価ではなくむしろ過小評価だった」という方向性で自分の再計算（29.6%台）と一致した。Pre-commitmentの発火についても、round 1の指摘（"watchdogが皆無"）とround 2の本レビューの指摘（"追加されたwatchdogが構造的に無力"）を同一クラス・独立2件と評価し、`.shell-team/specs/T-1068-agent-concurrency.md:257`（factual trigger）と`:259`（disposition）を正確に引用して発火を確認した。追加で、watchdogのポーリング末尾（最後のsleep後に生存確認せず`STALL`と判定しうる境界条件）と、`kill -0`が進捗ではなく生存のみを見ている点、実際のロール（数分〜数十分）に対して30秒閾値は短すぎるため実運用に転用すると誤検知が常態化する、という指摘も追加で得た——いずれも同じBlockerの内訳として扱う。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | reconcile-cost debitの粒度（validate-parallel-reviewの24.47%） | Codex primary [P1]／adversarialも独立にBREAKS再現、+5.11pt/+0.58ptの2通りの代替値を提示 | major | 数値は動く（24.47%→25.05〜29.6%）が、いずれの方向も現状より**saving が高くなる**側であり、staged-adoptionという recommendation の方向自体は揺るがない。DP2は具体的なsub-formulaを規定しておらず、noteは自らの方法論（task単位平均debit）を根拠付きで開示している。以上より blocker既定（P1→blocker）から major へ格下げした。 |

上記以外（watchdogのBlocker、background-agent-launchのMajor、interventionsのMinor）はCodexのネイティブseverity（P1/P2/P3）どおり blocker/major/minorとして採用しており、格上げ・格下げ・却下はない。Input spaceの「Out-of-scope synthetic extremes」に照らしても、今回のfindingsはすべてnote自身が到達可能と宣言したinput class（pilot probeの実行、telemetryの実データ、既存agentファイルの文面）に基づくものであり、`out-of-input-space`格下げの対象になるものはない。

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md`のT-1068エントリを`READY_FOR_ENG`に戻す。**最優先**: Blocker（pilot probeのDP1違反）は、frozen spec の Pre-commitment が既に決定済みの disposition（`- probe: validate-review-concurrent-readers — declined — ...`への切り替え、hazardの`unobserved`明記、`- follow-up:`行への要件転記）を適用するだけでよく、3回目のwatchdog実装を試みる必要はない——むしろ試みるべきではない（Pre-commitmentの趣旨は「これ以上patchしない」)。次点でMajor 2件（reconcile-cost debitの`max(0,...)`粒度への是正または方法論の明記強化、`background-agent-launch`のBash限定への言い回し修正）を解消すること。Minor（interventionsファイルの39/40/41混在）はorchestrator側での修正が妥当。

#### Fast-follow disposition

- `.shell-team/interventions/T-1068.md:8`（41→39/40の数値混在） — file-an-issue相当ではなく、次にこのファイルに触れる orchestrator 側の作業（もしあれば）で直せば足りる軽微な記録上の不整合。engineerのreworkを待たせるべきではない。won't-fix（このタスクの中では対応不要、次にこの記録を触る機会があれば併修正を推奨）。

#### Additional notes（round 2）

- **round-1 findingsの解消状況、独立再検証済み**: Blocker 1（33.4%過大評価）——自分で `.shell-team/runs/shell-team.jsonl` から `qa-verifier` FAIL率を再集計し `qa_rounds=3,2,2,9,4,4,2,1,8,5,3` / `qa_fail=0,0,1,0,1,0,0,0,4,0,0` を得て note の「43ラウンド中6 FAIL（14.0%）、11タスク中3タスク（27.3%）」と完全一致を確認。T-1058修正後のペア `(1358190, 1079568)` を用いてnoteと同じ`awk`集計を自分で再実行し `sum_qa=34142822 sum_rev=53171027 sum_serial=87313849 sum_parallel_max=58094508 saving_min=29219341 saving_pct=33.46` と `avg_per_round_ms=1442049`、debit `7853671.67`、調整後saving `21365669.33 / 24.47%` を1桁まで完全一致で再現（本ラウンドのMajor findingは、この計算自体の再現性ではなく、採用された debit の**モデルの粒度**についてのもの）。Blocker 2（S→Mサイジング）——`agents/codex-reviewer.md`の`## Preconditions`（`READY_FOR_REVIEW`必須）と`skills/run/SKILL.md`のstep5/6の直列順序を自分で該当行を開いて確認、noteの引用と一致。Blocker 3（watchdog）——上記の通り「一見満たしているが実質満たしていない」ことを新たに発見（round-1のBlockerが完全解消されたわけではなく、別形で継続している）。Major 1（CPU分類）——自分で同じ2並列`check-acs.sh`スイープを実測（serial 6475731000ns/parallel 3230804000ns、約50.1%削減）し、noteの約50.3%・QAの約50.2%と整合、`redesignable`への再分類を支持することを確認。Major 2（T-1058二重計上）——`seq=12`（APPROVE, 1079568ms）と`seq=100`（REQUEST_CHANGES, 1085779ms）の2行を自分で再読し、`.shell-team/reviews/T-1058.md`が`### Codex Review verdict`見出しを1件のみ持つことを確認、修正後の`1079568`が全下流集計に正しく伝播していることも上記の完全一致で確認済み。Minor 2件（detached-clone誤帰属、fail-closed-checks不整合）——自分で同じscratch cloneを再作成し`git rev-parse --abbrev-ref HEAD`が`feature/1067-context-lifecycle`（detachedでない）を返すこと、`feature/1066-effort-time-telemetry`が`rc=1`で解決しないことを確認、noteの訂正済み説明と一致。
- **watchdog-wrapped probeの数値そのものは環境ノイズで再現度が低い**: note/QAは`parallel_ns≈1895029000`(≈1.895s)を報告しているが、自分の再実行では`parallel_ns≈3361405000〜3364545000`(≈3.36s、2回連続で再現、serial baselineは同じ環境で`≈1.804〜1.812s`と安定)と、noteより大幅に遅い値を得た。これはこのラウンドの検証セッション内で直前に重い`check-acs.sh`並列実測を複数回行った後の sandbox 環境依存のノイズと考えられ（`nice(5) failed`警告は自分の実行でも同様に発生）、rc1/rc2・watchdogのheartbeatログ（exited-normally、stallなし）という**質的な結論は完全に一致**しているため、この数値の乖離自体は computed value の誤りとしては扱わない——ただし上記Blocker（watchdogの構造的無力化）と合わせて読むと、この pilot の wall-clock magnitude 自体がそもそも「modeled」の根拠にすぎず「measured」ではないという note 自身の位置付けを補強するものであり、Blocker/Majorの評価には影響しない。
- **issue-canonのreliance**: round 1で`curl https://api.github.com/repos/RipsawJP/shell-team/issues/252`により直接取得・照合済み（`gh`/TLS経路は本ラウンドでも同じ理由で失敗する見込みだが、round 1の直接取得結果は変わらず有効）。本ラウンドで new に issue本文へ依拠した判断は追加していないため、再取得は行っていない。
- **frozen items再検証**: `bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` = 10/10 PASS、`bash bin/check-intent.sh .shell-team/specs/T-1068-agent-concurrency.md .shell-team/todo.md` = aligned（`a2547b53...`、hash不変）をこのラウンドでも自分で再実行して確認した。

### Codex Review verdict (round 3): REQUEST_CHANGES
- Task: T-1068
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 3 files changed, +41/-17 lines (`git diff de915f9..8796133`)

Round-2の1 Blocker+2 Majors+1 Minorの解消状況を独立に再検証した（詳細はAdditional notes参照）。結論として、Blocker（frozen Pre-commitmentの発火・実行）はspecの文言どおり正確に執行されており、これ以上の欠陥はない。Major 2件も正しく解消済み。今回新たに2件のMajor（既にnote自身が開示済みの方法論的選択の射程・S sizingの前提の妥当性）と3件のMinorを発見した——いずれもBlockerには至らない。**今回の2件のMajorはどちらもfrozen spec の Pre-commitment（`## Pilot probe`専用のfactual trigger）の対象外**（probeの安全性ではなく、feasibility matrixの計算方法論・候補の前提記述に関するものであるため）であり、loop capのSTOPエスカレーション条件（"a further gating Blocker"）には該当しないと判断する。

#### Blockers

（なし）

#### Major

- `docs/loop-engineering/agent-concurrency.md:44,65-66` — 29.6%（task-aggregateの`min(ΣQA, ΣReview)`にFAILラウンドごとの`max(0,...)`debitを適用）は、複数ラウンド・reworkを挟むタスクでは、あるcycleのQA時間が別cycleのreview時間と相殺されうるため、依然として過大評価になりうる——候補自身が主張する重なりは「同一frozen commitを両者が読む」という**同一cycle内**の重なりであるのに対し、task合計の`min`はcycleをまたいだ組み合わせを許してしまう。ただしこれはBlockerではない: (1) 29.6%は明確に`modeled`と表示されている、(2) note自身が`## Limits`（line 155）でround-level再構成の約25.05%を並記し、「task-aggregate対round-level」を「方法論上の選択」と明記している、(3) DP2はtotal・min cap・reconcile debitを要求するが、cycle単位の対応付けまでは規定していない——「誤った未開示値で擁護可能な読みがない」というBlocker要件は満たさない。29.6%を唯一の"reduction"として推薦文（line 142）に再掲するのではなく、25.05%（同一cycle推定）〜29.6%（task-aggregate上限）の幅として明示すればMajorは解消する。
- `docs/loop-engineering/agent-concurrency.md:60,70,72` — `implement-tail-parallel-qa`候補の安全前提「only the engineer's own post-commit hand-off narrative (prose, not code) is still being written」は、現行契約と突き合わせると疑わしい。`agents/engineer.md`のstep4「テストスイートをローカルで実行」はstep5「`READY_FOR_QA`に設定」より前に完了しており、`skills/run/SKILL.md`のT-073ガードもQA起動前にworking tree全体がcleanであることを要求する——つまり候補が除去対象とする「the engineer's own tail verification (its last self-check pass before hand-off)」は、実際にはREADY_FOR_QA・commit時点で**既に完了している**。post-commitで残るのはprose記述のみであり、これは候補自身の`removes-phase`記述（「tail verificationとQAの再導出の間の待ち」）が指す実体のある重なりではない。`S`サイジングと「telemetry field待ちのsecond-stage candidate」という位置付けは、この前提の再検討（実際に何を重ねるのかの再定義と、それに伴う契約変更コストの再計上）を経るまで維持できない。ただしこの候補のsavingは既に`undetermined`であり、primary recommendationは`validate-parallel-review`のまま変わらないため、Blockerではない。

#### Minor / nits

- `docs/loop-engineering/agent-concurrency.md:55` — 「every one of the four figures produced across two rounds (24.47%, 25.05%, 29.58%, 29.6%) sits above the 22.9%/28.6% range」という記述を算術的に検算したところ誤り——24.47%と25.05%はいずれも22.9%〜28.6%の範囲**内**にあり、「above」ではない。数値そのものではなく、それらの相対位置を要約する一文の誤りであり、結論の方向やcomputed valueには影響しない。
- `docs/loop-engineering/agent-concurrency.md:67` — `- saving: implement-tail-parallel-qa`行の文末が「...none finer-grained than `engineer` exists to isolate a tail-verification sub-phase from」で目的語なく途切れている（編集時の貼り付け漏れと見られる）。
- `docs/loop-engineering/agent-concurrency.md:152` — `## Limits`冒頭の一文が「the reconcile-at-join proxy under `validate-parallel-review`」（実際にはline 48の`avg_per_round_ms`、P11-telemetry全11タスクの平均、machine-local telemetry由来）と、T-1060固有のn=1系列（board由来）を同じ括弧内に並べた上で、両方を「a single-task n=1 observation... primary record is the board」とまとめている——前者（reconcile-at-join proxy）はn=11・telemetry由来であり、この要約は不正確。実際のFAIL debit自体はline 153で正しく「3 of 11 tasks」と記述されており、この一文だけがLimitsの要約として不正確。computed valueやconclusionの方向には影響しない。

#### Adversarial findings

上記2件のMajorを的にした敵対的読みは両方とも独立に"HOLDS UP — major"（Blockerではない）と評価した。Debit粒度については自分の分析と完全に一致する理由（task-aggregateがcycleをまたぐ組み合わせを許すこと、DP2がcycle単位の対応付けを規定しないこと、noteが両方の数値を開示していること）を提示。implement-tail候補については`agents/engineer.md`（step4がREADY_FOR_QA設定=step5より前にテスト実行を要求）と`skills/run/SKILL.md`のT-073ガードを引用して同じ結論に達し、さらに「本当にheavyweight verification tailを重ねるなら`S`でなく最低`M`、post-commit proseだけなら実質的savingの前提がない」という二択の指摘を追加で提示した——自分の評価をより明確化するもので、severityは変えない。追加で見つけた3件のMinorのうち2件（22.9%/28.6%レンジの誤要約、L67の文末切れ）は自分でも該当箇所を読んで独立に確認・一致。3件目（L152のreconcile-at-join proxyのn=1誤帰属）は自分で`grep -n "reconcile-at-join proxy"`により該当2箇所（line 48=n=11、line 152=誤ってn=1と一括り）を照合し、Codexの指摘が正しいことを確認した。

#### Synthesis audit ledger

no severity changes — Codexのネイティブseverity（primary [P2]×2→major、adversarial확인はseverityラベルなしで"major"と明言）はそのまま採用し、格上げ・格下げはない。Input spaceの「Out-of-scope synthetic extremes」に照らしても、今回のfindingsはすべてnote自身の到達可能なinput class（telemetryの実データ、既存agentファイルの文面、note自身のprose）に基づくものであり、`out-of-input-space`格下げの対象になるものはない。

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md`のT-1068エントリを`READY_FOR_ENG`に戻す。**Blockerは0件**——frozen Pre-commitmentの執行は正確であり、これ以上のprobe再設計は不要（specの`- follow-up:`行に要件が正しく転記済み）。次ラウンドで解消すべきは2件のMajor（29.6%を単一の"reduction"ではなく25.05%〜29.6%の幅として推薦文に明示するか、cycle単位の再集計に切り替える；`implement-tail-parallel-qa`の安全前提を現行契約（T-073・engineer step4/5順序）に照らして再定義するか、`S`サイジングと"second-stage"位置付けを見直す）と3件のMinor（22.9-28.6%レンジの誤要約、L67の文末切れ、L152のreconcile-at-join proxyのn=1誤帰属）。3件のMinorは同じ差分内で安価に直せる。

#### Fast-follow disposition

- no fast-follow deferrals（今回のMinor 3件は次ラウンドの本修正——Major 2件が同じセクションに手を入れる——で一緒に直すのが安価であり、merge後への先送りは提案しない）

#### Additional notes（round 3）

- **round-2 Blocker/Majors/Minorの解消状況、独立再検証済み**: Blocker（inert watchdog→Pre-commitment発火）——`grep -c '^- probe: .* — declined — '`=1、`grep -cE '^- saving: .* — measured — '`=0（自分で再実行）を確認し、spec本文（`:257`のfactual trigger、`:259`のdisposition）と note の`- probe:`行・`- follow-up:`行を突き合わせて、両ラウンドの findings が verbatim で reason に記録され、hazard が `unobserved` と明記され、probe requirements（"watchdog signals the parent, parent selects on both"を含む4項目）が`- follow-up:`行に転記されていることを確認——**disposition は spec の frozen 文言どおり一字一句執行されている**。Major 1（debit粒度）——各FAILラウンドの実測`duration_ms`（T-1058=855438、T-1061=1606704、T-1065=691335/261676/489083/729609）と各taskの`avg_review_per_round`を自分で再取得し、`max(0,...)`を自分で再計算して`TOTAL debit=3387586.00`、調整後saving`29.58%`を完全一致で再現した（round2の自分自身の計算とも一致）。Major 2（substrate分割）——`background-agent-launch`が"Bash-level backgroundingのみ"に限定され、新規`agent-tool-concurrent-launch — absent`行がDP3の閉じた語彙（`provided`/`absent`のみ）に沿って追加されていることを確認、`bash bin/check-acs.sh`のAC4（6行以上、4つの named harness capability、grammar一致）が今も10/10でPASSすることを確認。Minor（interventions mktemp件数）——`.shell-team/interventions/T-1068.md`は未編集のまま（orchestrator-owned、自分のwon't-fix disposition通り）であることを確認、note自身の該当測定（40件、comment-excluded、mktemp -d限定ではない）が正しいままであることも確認。
- **frozen items再検証**: `bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` = 10/10 PASS、`bash bin/check-intent.sh .shell-team/specs/T-1068-agent-concurrency.md .shell-team/todo.md` = aligned（`a2547b53...`、hash不変）を自分で再実行して確認した。
- **QA round 4の"stale parenthetical"指摘の解消**: QA round 4が`## Limits`の"now watchdog-wrapped per DP1"という古い言い回しを非gatingのconnective-prose findingとして記録し、`8796133`で即座に修正されたことを確認した——修正後の文言（"now `declined` per the frozen Pre-commitment's own executed disposition"）を自分で読み、probeの現状と整合していることを確認した。
- **issue-canonのreliance**: round1で直接取得・照合済みの内容から変化なし。本ラウンドで new に issue本文へ依拠した判断は追加していない。

### Codex Review verdict (round 4): REQUEST_CHANGES
- Task: T-1068
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 3 files changed, +45/-14 lines (`git diff 8ec1151..3137a3c`)

Round-3の2 Majors+3 Minorsの解消状況を独立に再検証した——37ペアの再集計（自分で全11タスク分のQA-PASS行とreview行をtimestampでソートしてペアリング）から`pairs=37 sum_min=25260594 sum_qa_pass=29508977 sum_rev=53171027`、adjusted `21873008.0 / 25.05%`を完全一致で再現し、1:1対応の positive control（各task の PASS-QA件数=canonical rounds-to-approve）も自分で確認した。implement-tail-parallel-qaの`S`→`M`再サイジング、22.9-28.6%レンジの算術修正、L67の文末補完、L152のn=1/n=11分離もすべて確認した——**これらはすべて正しく解消されている**。

一方で、今回のCodexラウンドは、この収束ラウンドで初めて**新規のBlocker**を発見した——4ラウンドを通じて一度も指摘されていなかった、flagship候補`validate-parallel-review`自身の安全前提そのものに対する欠陥である。この見解は自分でも一次資料（`agents/qa-verifier.md`・`agents/codex-reviewer.md`）を読み、敵対的読みでも独立に確認したため、「expected final」という前提にかかわらず正直に報告する。

#### Blockers

- `docs/loop-engineering/agent-concurrency.md:65,70` — `validate-parallel-review`候補の安全前提は「the engineer's implementation is already committed with no uncommitted diff in the whole working tree」「both...read the same frozen, already-committed implementation」に立脚しているが、これは`agents/qa-verifier.md`自身のRulesと矛盾する。T-073の"whole working tree clean"確認はValidate開始**前**に一度だけ実行される（`skills/run/SKILL.md`のT-073ガード記述）。しかしqa-verifier自身のRulesは「You do not write production code. Test-only edits (adding a missing test case) are OK; flag them in the verdict.」（`agents/qa-verifier.md:57`）と、Validate**実行中**にworking treeへテストファイルを追加することを明示的に許可している——これはT-073の一度きりのチェックの**後**に起こり得る。一方、codex-reviewer自身の読み取り経路は「reads the working tree directly」（`agents/codex-reviewer.md:31`）であり、SHA固定のsnapshotやworktree isolationの指定はない（`:40`のコマンド自体にも無い）。したがって、qa-verifierがValidate中にテストファイルを追加する（規則上許可された）タイミングとcodex-reviewerが並行してworking treeを読むタイミングが重なれば、codex-reviewerはengineerのcommit時点・追加後・編集途中のいずれかの混在状態を観測しうる——候補自身の「両者が同じfrozen commitを読む」という中核的安全主張が、この経路については成立しない。なお`agents/qa-verifier.md:66`（T-073節、"keep the real repo working tree clean throughout the verification, apart from your own hand-off append to the board"）は`:57`のtest-edit許可と正面から矛盾しており、優先順位・scratch限定・監視のいずれも定義されていないため、「QAは実際には編集しないはず」を安全性の根拠として採用できない。この面はnoteが列挙した5つの契約サーフェス（READY_FOR_REVIEW precondition、hand-off読み込み、phase順序、QA-FAIL discardルール、loop-guardのiteration契約）のいずれにも内包されない——QA-PASS時にも起こりうる「共有working treeのsnapshot/write-set契約」という**第6の、未価格の面**であり、`## Shared-state contention inventory`の`shared-git-index`行（`:96`）も「fixed commitへの concurrent readers」を前提にしてこの経路を見落としている。この安全前提の欠陥を price（第6のprecondition追加、または候補設計自体の見直し）せずに"disjoint writers"・"structurally safest"として`staged-adoption`を推薦する結論（`:148`）は現状成立しない。

#### Major

（なし）

#### Minor / nits

- `.shell-team/specs/T-1068-agent-concurrency.md:25` — `## Summarized sources`の当該行（frozen intent block外、pre-freezeのpm-spec自身の一次サンプリング記述）が2点不正確: (1) `tests/retro-inventory`という名前のディレクトリ／スクリプト群は存在しない（実在するのは`tests/retro-inputs`——自分で`ls tests/ | grep -i retro`で確認）、(2) 「41...via mktemp -d」という記述は、comment-excludedで数え直すと`mktemp`全般が40件、`mktemp -d`限定では39件であり、41は素朴な（コメントを除外しない）`grep -l mktemp`の数——デリバラブルnote自身の該当測定（AC4のcommand、40件・comment-excluded・`-d`限定ではない）はこの誤りを継承しておらず正しいままである。この行はfrozen intent blockの外（`<!-- BEGIN intent-block -->`より前）にあり、修正してもclass-B/M re-freezeは不要——過去にも同じ非frozen領域の"nine-section→ten-section"修正が本タスク自身の commit `8c1158e`で行われている前例と同種の、低コストな記録修正。

#### Adversarial findings

flagship候補の安全前提に対する上記Blockerを的にした敵対的読みは、独立に"BREAKS — blocker"と確認した上で、自分の指摘よりもさらに一段深い論点を追加した: QA-FAILの場合だけでなく**QA-PASSの場合**にも同じ問題が生じる——qa-verifierが未commitのテスト追加を含んだ状態で観測した結果をもってPASSしうるため、codex-reviewerはmerge対象のcommitには存在しないテストをcoverage/correctnessの証拠として読める可能性があり、これは既存の「QA-FAIL時だけreviewを破棄する」モデル（reconcile-cost、`:72`）では一切捕捉されない、という指摘である。また、修正の具体案として (a) QAのworking-tree test-edit権限を廃止しverdictへの記録のみにする、(b) 両ロールを捕捉済みSHAの別々のdetached worktree/cloneで動かす、(c) join時に`reviewed-sha == qa-tested-sha`を検証しQAがtest patchを作った場合は両方を再実行する、の3案を提示した。さらに、この安全ホールが価格付けされていない以上、25.05%というsaving headlineも「安全な上限」とは言えない、という指摘も追加した——これは既存の debit（QA-FAILのみを対象とする）が対象としていない、QA-PASS-with-edit時の再実行コストを指しており、正しい指摘である。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `.shell-team/specs/T-1068-agent-concurrency.md:25`の`tests/retro-inventory`／mktemp件数の不正確 | Codex primary [P2] | minor | デリバラブルnote自身の同等の測定はすでに正しく（40 comment-excluded, `-d`限定でない）、この行はspec自身のfrozen intent block**外**にあるpre-freezeのpm-spec一次サンプリング記述であり、デリバラブルのcomputed valueには影響しない。P2の既定（major）から、record-accuracyのみに閉じた低stakesの訂正として格下げした。 |

上記以外（flagship候補の安全前提のBlocker）はCodexのネイティブseverity（primary [P1]→blocker、adversarialも独立にblockerと明言）どおり採用しており、格上げ・格下げはない。Input spaceの「Out-of-scope synthetic extremes」に照らしても、この finding は note 自身が reachable と宣言した input class（`agents/qa-verifier.md`・`agents/codex-reviewer.md`の実際の文面、これらが規定する実行時の書込み・読み取り経路）に基づくものであり、`out-of-input-space`格下げの対象ではない。

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md`のT-1068エントリを`READY_FOR_ENG`に戻す。**最優先**: `validate-parallel-review`候補の安全前提に第6のcontract surface（QAのtest-only edit権限とcodex-reviewerの working-tree直接読み取りの競合）を追加し、少なくとも一つの緩和策（QAのtest-edit権限の停止、SHA固定のworktree/clone分離、join時のSHA一致検証のいずれか）を候補のsizing・safety precondition・recommendationの3箇所に反映すること。この見直しにより`M`サイジングや25.05%というsaving headlineの位置付け（安全な上限かどうか）も再検討が必要になりうる。次点でMinor（spec自身の`tests/retro-inventory`／mktemp件数の記録修正）を解消すること——これは低コストで、frozen intent block外の修正なのでre-freeze手続きは不要。

#### Fast-follow disposition

唯一のMinorである`.shell-team/specs/T-1068-agent-concurrency.md:25`は、次ラウンドで安全前提の見直しに手を入れるついでに直すのが安価であり、merge後への先送りは提案しない——今回のBlockerを解消する同じラウンド内で修正されることを想定している。

no fast-follow deferrals

#### Additional notes（round 4）

- **round-3 Major/Minorの解消状況、独立再検証済み**: Major 1（debit粒度）——自分で全11タスク分のPASS-QA行とreview行をtimestampで抽出・ソートし、Nth-to-Nthでペアリング（T-1058の異常な2件目のreview行`seq=100`は除外、`seq=12`のみ使用）した結果、`pairs=37 sum_min=25260594 sum_qa_pass=29508977 sum_rev=53171027`——完全一致。1:1対応のpositive control（各taskのPASS-QA件数=canonical rounds-to-approve）も自分で再現し、全11タスクで一致することを確認。adjusted saving `(25260594-3387586.00)/87313849*100=25.05`も完全一致で再現した。Major 2（implement-tail候補の前提）——`agents/engineer.md`のstep4（テスト実行）がstep5（`READY_FOR_QA`設定）より前に完了することを確認し、候補の`M`への再サイジングと二重理由化された`undetermined`のsaving記述を確認した。Minor 3件——22.9-28.6%レンジの算術関係（24.47/25.05が範囲内、29.58が範囲外）を自分で計算し直して確認、L67の文末補完（"...from the rest of that span's own total"）を確認、L152のn=1（T-1060固有）とn=11（reconcile-at-join proxy）の分離を確認——いずれも正しく解消されている。
- **文書全体の一貫性を通読で確認**: `## Terms`から`## Limits`まで全文を通読し、matrix・isolation・serialization・substrate・pilot probe・invariants・recommendation・limitsが一つの整合した物語（headline 25.05%・upper bound 29.6%・implement-tailはM sizing・probeはdeclined・substrate分割）を語っていることを確認した——今回の新規Blocker（QA test-edit競合）以外に矛盾や欠落は見つからなかった。
- **frozen items再検証**: `bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` = 10/10 PASS、`bash bin/check-intent.sh .shell-team/specs/T-1068-agent-concurrency.md .shell-team/todo.md` = aligned（`a2547b53...`、hash不変）を自分で再実行して確認した。今回発見したBlockerはfrozen intent block内の記述（DP1のprobe専用factual trigger）を対象としないため、Pre-commitmentの発火条件には該当しない——これは feasibility matrix 自身の新規欠陥であり、"Never-dropped"節が言う「2連続ラウンドでmatrixが defeated」の条件にも該当しない（今回が初出のため）。
- **issue-canonのreliance**: round1で直接取得・照合済みの内容から変化なし。本ラウンドで new に issue本文へ依拠した判断は追加していない。

### Codex Review verdict (round 5): APPROVE
- Task: T-1068
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 4 files changed, +20/-4 lines (`git diff 132a971..1ef9af5`)

本ラウンドの verdict target は coordinator の briefing が明示する通り「incorporation の honesty」（round-4 のBlockerが正しく組み込まれたか）であり、「pricing completeness」（investigation自体の再価格付け）ではない——operatorが明示的にoption B（minimal close）を裁定し、この先の findings は「no further rework rounds」で notes/follow-ups として記録される（board `c9f0bd2`のstop condition）。以下、まず verdict target の4項目を検証し、次にその範囲外で見つかった追加findingsを——独立性を保ったまま——正直に報告する。

#### Verdict-target verification（本ラウンドの検証対象、4項目すべて確認済み）

1. **2つのblocker文が引用元と一致するか** — `agents/qa-verifier.md:57`（"Test-only edits (adding a missing test case) are OK; flag them in the verdict."）と`agents/codex-reviewer.md:31`（"reads the working tree directly"を含む長い一文——`cut`で切れて見えたが`grep -o`で該当行内に実在することを確認）を自分で直接開いて確認した。note側の引用（`docs/loop-engineering/agent-concurrency.md:65`の候補行）はこの2行を正確に言い換えており、誇張も矮小化もない。
2. **25.05%が6つの契約サーフェスすべてに条件付けされているか** — `:69`のsaving行を確認: "kept as-is this round, now conditional on all six named contract surfaces above (the original five plus the round-4 sixth), since the sixth is a named Blocker on the candidate's own safety premise, not yet priced into this figure." — 数値自体は変更されておらず、「6面すべてに条件付き・第6面は未価格」という限定が明示されている。誠実な記述である。
3. **RecommendationのStage 0が発見を正直に反映しているか** — `:148`を確認: "Stage 0 is the six-surface `agents`/`skills` contract redesign itself (five surfaces already enumerated above ... plus the sixth, round-4 Blocker named on the candidate itself ...), and nothing in `validate-parallel-review` runs before Stage 0 lands." — issue #252の"blocker-named acceptance"形式（"a reasoned recommendation... with the specific blocker named"）に沿った、"not before the contracts change"という順序制約を明確に表現しており、第6面もStage 0に正しく含まれている。
4. **mutable-region typo修正** — `.shell-team/specs/T-1068-agent-concurrency.md:25`を自分で確認: `tests/retro-inventory`（存在しない）→`tests/retro-inputs`（`ls tests/ | grep -i retro`で自分も`retro-inputs`のみ存在することを確認済み）、mktemp件数は「41」（naive）→「39」（comment-excluded, `mktemp -d`限定、自分の`find tests -type f -name "*.sh" -print0 | xargs...`再実行でも39と一致）に修正されている。`bash bin/check-intent.sh`をこのラウンドでも自分で再実行し、hash不変（`a2547b53...`、frozen intent block外の修正であることを確認）。

**frozen items再検証**: `bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` = 10/10 PASS、`bash bin/check-intent.sh` = aligned（hash不変）を自分で再実行して確認した。

4項目すべてに誠実性の欠陥は見つからなかった——**APPROVE**。

#### Blockers（本ラウンドのverdict targetの範囲内）

（なし）

#### Major（本ラウンドのverdict targetの範囲内）

（なし）

#### Minor / nits（本ラウンドのverdict targetの範囲内）

（なし）

#### この範囲外で見つかった追加findings（verdict targetの範囲外・本ラウンドのAPPROVEを左右しないが、独立性に基づき正直に報告する）

operatorのruling（「findings after this pass finalize the task as-is」）と coordinator の明示的な範囲限定（pricing completenessは対象外）に従い、以下はAPPROVE判定をgateしないものとして扱うが、technical merits単体で見た場合の重大度を偽らずに記録する。

- **[技術的にはBlocker級] `docs/loop-engineering/agent-concurrency.md:65,69,148`** — 既に組み込まれた第6サーフェス（QAのtest-edit vs reviewerの working-tree読み取り）は一方向のみを塞いでいる。反対方向として、`agents/codex-reviewer.md:158`は本ロール自身が「coordinating sessionのprompt指示があれば自分のreview recordをstage・commitできる」ことを明記しており、これは実際のgit commit（HEADを進める操作）である。`validate-parallel-review`が実現しqa-verifierとcodex-reviewerが真に並行実行される世界では、reviewerが先にcommitを完了すればHEADが進み、その間qa-verifier自身が実行中のHEAD相対コマンド（`check-acs.sh`の個々の`- check:`コマンド、`git merge-base`/`git rev-parse HEAD`ベースの判定を含む）が、commit前後で異なるHEADを解決しうる。これは候補の安全性・saving計算全体が依拠する「同じfrozen commitを両者が読む」という前提を、round-4で見つかった経路とは**別方向**から壊す。自分で`agents/codex-reviewer.md:158`の該当記述（stage/commit可能という記述）を確認し、敵対的読みも独立に"HOLDS UP — blocker"と判定し、「第7のcontract surface」として(a) reviewerのcommitをjoinまで延期/禁止、(b) QAの全HEAD相対検証をValidate開始時の固定SHAへpin、(c) commitを含むHEAD変更全体をserialize、のいずれかが必要だと具体的に提示した——単にreviewerの読み取りだけをSHAへpinする既存の第6サーフェスの修正では、この「QA側のmoving HEAD」問題は解消しない、という指摘も含めて妥当と判断する。
- **[技術的にはMajor級] `docs/loop-engineering/agent-concurrency.md:123`** — substrateインベントリの`watchdog — absent`は、根拠が「`watchdog`という単語のgrepが0件」のみであり、実際には`bin/check-liveness.sh`が`RUNNING`/`WAITING`/`STALLED`を分類する実在のfail-closed liveness classifier（`--stall-after`閾値付き）として存在し、`bin/check-acs.sh`も`timeout`コマンドが使える環境では各`- check:`コマンドを既定120秒でラップしている——自分で両ファイルを開いて確認した。したがって「no stall-detection or timeout mechanism is part of this shipped loop」という包括的主張は事実に反する。ただし敵対的読みが正しく指摘する通り、これらは「個々のagent呼び出しを監視し、stallした子をkill/notify/reapする」機構ではない（`check-liveness.sh`はloop全体の永続stateを外部timerから見る一回限りの分類器）——正確には「per-agent launch watchdog — absent」に絞った上で、"loop-level stall classifier — provided"・"AC-command timeout — conditionally provided"を別途併記すべきである。substrateインベントリという計算値の一部としては誤りだが、「並行agent用watchdogは未提供」という最終結論そのものは（適切に絞れば）覆らない。

#### Adversarial findings

上記2件を的にした敵対的読みは、いずれも独立に確認した上で、moving-HEAD raceについては自分の想定より一段深刻な評価（blocker級）を下し、具体的な3つの是正案（reviewer commitの延期/禁止、QA側のSHA pin、両者のserialize）を提示した。watchdog substrateについても、`check-liveness.sh`の`--stall-after`既定値・exit code分類・`check-acs.sh`のtimeoutの適用範囲（QA内の個別commandのみ）まで具体的に検証し、「per-agent launch watchdog — absent」という狭めた結論なら成立する、という妥当な着地点を示した。

#### Synthesis audit ledger

no severity changes — 上記2件について、Codexのネイティブ判定（primary: [P1]→blocker相当・[P2]→major、adversarial: 独立にblocker/majorと明言）をそのまま採用し、格上げ・格下げ・却下は行っていない。本ラウンドの verdict 見出しの判定結果自体は、これら2件のtechnical severityを反映したものではなく、coordinator/operatorが明示的に narrow した「incorporation honesty」という別の検証軸に対する判定である——severityの格下げではなく、verdictが答える問いそのものが異なる、という点を明確にしておく。Input spaceの「Out-of-scope synthetic extremes」に照らしても、この2件はnote自身の到達可能なinput class（`agents/codex-reviewer.md`・`agents/qa-verifier.md`・`bin/check-liveness.sh`・`bin/check-acs.sh`の実際の文面）に基づくものであり、`out-of-input-space`格下げの対象ではない。

#### Recommendation

- APPROVE → `.shell-team/todo.md`のT-1068エントリを`READY_FOR_MERGE`に設定する。本ラウンドのverdict targetである「round-4 Blockerの組み込みの誠実性」は4項目とも確認済みであり、operatorのruling（option B minimal close、stop condition）に従い、上記の追加findingsはこのタスク自体の追加reworkをgateしない。

#### Fast-follow disposition

全タスクを通じて蓄積された、merge後に持ち越すnotesとそのdispositionを以下にまとめる。

- `.shell-team/interventions/T-1068.md:8`（round-2 Minor: "41 of 47 ... via mktemp -d"という記述が、comment-excludedの`mktemp -d`限定では39件、any-mktempでは40件と食い違う） — won't-fix（このタスクの範囲では対応不要）。理由: このファイルはorchestrator-owned・engineer/reviewerの編集範囲外であり、デリバラブルnote自身の該当測定（40件、comment-excluded、`-d`限定でない）はこの誤りを継承しておらず正しいまま。次にこの記録に触れる機会があれば併修正を推奨するに留める。
- `docs/loop-engineering/agent-concurrency.md:65,69,148`（round-5で新規発見: reviewer自身のcommit能力によるmoving-HEAD race、"第7のcontract surface"） — file-an-issue相当。理由: 既に組み込まれた第6サーフェスの修正だけでは解消しない、独立した方向の安全性欠陥であり、`validate-parallel-review`が将来Stage 0を経て実装段階に進む際には必ず一緒に手当てする必要がある。investigation自体の再価格付けはoperatorのrulingで本ラウンドの対象外だが、この欠陥の存在そのものを追跡しないまま将来の実装に進むリスクの方が大きいと判断する。
- `docs/loop-engineering/agent-concurrency.md:123`（round-5で新規発見: `watchdog — absent`が`bin/check-liveness.sh`・`bin/check-acs.sh`のtimeout機構を見落としている） — file-an-issue相当。理由: substrateインベントリという計算値の一部に事実誤認があり、「per-agent launch watchdog — absent」に絞れば最終結論は覆らないものの、正確な記述（loop-level stall classifierやAC-command timeoutの存在）を将来の読者・実装者に伝えるための訂正価値がある。severityはMajor相当だが、最終結論を覆さないため緊急度は低い。
