### Codex Review verdict: REQUEST_CHANGES
- Task: T-1068
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 5 files changed, +609/-0 lines (`29c35d9...HEAD`; base = `git merge-base feature/1067-context-lifecycle HEAD` = `29c35d97ed1c9468cc82c38598f75f4be1d842ab`)

前提として、`bash bin/check-acs.sh .shell-team/specs/T-1068-agent-concurrency.md` = 10/10 PASS、`bash bin/check-intent.sh .shell-team/specs/T-1068-agent-concurrency.md .shell-team/todo.md` = aligned (`a2547b53...`) を自分で再実行して確認した。凍結項目に問題はない。以下の findings はすべて「Verdict scope」でいう **computed values の正しさ** と **conclusions' direction** に関するもので、connective prose には触れていない。

#### Blockers

- `docs/loop-engineering/agent-concurrency.md:55` — `validate-parallel-review` の `reconcile-cost` サブバレットは「QA が実際に失敗する割合は、この note には測定するための recorded field がなく、見積もらない (`a rate this note does not have a recorded field to measure and does not estimate`)」と書いているが、これは事実に反する。`bin/log-run.sh` の telemetry 契約は各 span 行に `--verdict`（PASS/FAIL/APPROVE/REQUEST_CHANGES、null 許容）と `--iteration`/`--attempt` を記録しており、実データ (`.shell-team/runs/shell-team.jsonl`) にも実際に埋まっている。この note が引用している P11-telemetry の 11 タスクに対して自分で再集計したところ、`qa-verifier` の FAIL 行は T-1058=1件、T-1061=1件、T-1065=4件の計 6 件（11 タスク中 3 タスク、27.3% に少なくとも1回の FAIL）存在した（`grep '"span":"qa-verifier"' .shell-team/runs/shell-team.jsonl | grep -c '"verdict":"FAIL"'` を各タスクの `run_id` で絞って再現、確認済み）。しかも issue #252 の実本文（`gh`/TLS 経路は sandbox で失敗したが `curl https://api.github.com/repos/RipsawJP/shell-team/issues/252` で直接取得・照合済み）は、まさにこの trade-off を名指しで要求している："Model the trade: a review round spent on code QA then fails is wasted; for tasks whose expected review outcome is a 1-round APPROVE the expected saving is large." — つまり「測定できないので見積もらない」という記述自体が誤りであるだけでなく、issue の受入れ条件が明示的に要求した計算をスキップしている。修正すれば headline の 33.4% という `saving` は过大評価であり、下方修正が必要（Adversarial パスの感度計算では 22.9%〜28.6% 相当まで下がる — 下記参照）。この `- saving:` 行自体が computed value であり、`## Recommendation and follow-ups` の "a real modeled saving of 33.4%" という conclusion's direction にも直結するため、DP2 の「reconcile-at-join cost that must be subtracted」という自己規定にも反する。

- `docs/loop-engineering/agent-concurrency.md:48,125` — `validate-parallel-review` は "S" コストで「two readers, no writer / structurally safest」と結論しているが、現行の agent contract 下ではこの通りに同時実行できない。`agents/codex-reviewer.md` 自身の Preconditions は「Task is at READY_FOR_REVIEW in tasks/todo.md」であり、QA が PASS して board flag を進めるまでレビューは開始できない。かつ `agents/codex-reviewer.md` の Step 2 は QA の hand-off を読むことを要求しており、QA が完了する前には読めるものが存在しない。さらに `agents/qa-verifier.md:66` は QA が board への append を書くことを明記し、`codex-reviewer.md` はレビュー record を書く——つまり実際には reader が2つでなく、writer も2つ存在する（disjoint target なので衝突はしないが、"no writer" という言い回し自体が矛盾）。真に並行実行するには、少なくとも (a) `codex-reviewer` の `READY_FOR_REVIEW` precondition の変更、(b) QA hand-off が未完成でも開始できる入力契約、(c) `skills/run/SKILL.md` の phase 順序と join 処理の変更、(d) QA FAIL 時にレビュー結果を破棄する記録規則、が必要であり、これらはいずれも `agents/` または `skills/` の変更＝本タスクの Non-goals（"No mechanism of any kind ships... nothing under ... agents/ or skills/ changes"）が禁じているものそのものである。つまり "S" という cost sizing はこの前提変更のコストを含んでおらず、`## Recommendation and follow-ups` が挙げる 2 つの precondition（telemetry lock、Agent-tool 同時実行の実証）だけでは足りない。

- `docs/loop-engineering/agent-concurrency.md:107-112` — Pilot probe は `- probe: validate-review-concurrent-readers — ran — ` と報告しているが、凍結された DP1（`.shell-team/specs/T-1068-agent-concurrency.md` の Settled decisions、intent block 内）は「Every background agent the probe launches runs with a watchdog started at launch, with no exceptions. A probe that cannot satisfy all of this is `declined`」と例外なく規定している。実際の probe コマンド（line 111: `... & PID1=$!; ... & PID2=$!; wait ...`）に watchdog は一切起動されておらず、note 自身も substrate インベントリで `watchdog — absent`（line 99）と自ら測定している。DP1 の要求を満たせない以上、正しい記述は `declined`（または少なくとも DP1 違反を hazard として明示開示した上での "ran"）であるはずで、現状の無条件の "ran" ラベルは凍結された安全要件の充足を偽って主張している。

#### Major

- `docs/loop-engineering/agent-concurrency.md:92` — `full-population-diff-cpu` を無条件に `hard`（redesignable ではなく）と分類しているが、根拠は「並行実行しても total CPU-seconds は変わらず、単一マシンでは cache/scheduler contention で wall-clock が悪化しうる」という一般論のみで、実際のコア数・並行実行時のコア飽和度は測定していない。マルチコアマシンで単一スレッド中心の2プロセスが別コアで動けば、他の候補（`validate-parallel-review` 含む）と全く同じ理屈で wall-clock は短縮しうる。この note の他のすべての候補が拠って立つ「並行実行で wall-clock が縮む」という前提と、この一箇所だけ矛盾する形で `hard` と断定しており、`- command:` による測定もない。`redesignable`（容量依存）と分類するか、コア飽和度を測ってから `hard` と結論すべき。

- `docs/loop-engineering/agent-concurrency.md:40` — telemetry 再導出テーブルの T-1058 の `codex-reviewer=2165347` は、実際には `.shell-team/runs/shell-team.jsonl` 上の2つの別々の `codex-reviewer` span 行（`seq=12` verdict=APPROVE, duration_ms=1079568 と `seq=100` verdict=REQUEST_CHANGES, duration_ms=1085779、両方とも `iteration=1, attempt=1`）の単純合算である（自分で該当行を確認済み）。しかし正典のレビュー記録 `.shell-team/reviews/T-1058.md` は "## Round 1" の APPROVE 一件のみを記録しており、2ラウンド目の記録が無い。つまりこの task の `codex-reviewer` 合計時間には、正典記録に対応しない telemetry 上の重複（もしくは異常な再実行）が混入しており、これが `avg_per_round_ms=1540756`（line 46、reconcile-cost の proxy として使われる数値）を実際より高く歪めている可能性がある。note が継承した de-dup key（`run_id`,`seq`,`span`, last-encountered wins）はこの種の「同一 iteration/attempt に対する複数の異なる seq」を検出できない——集計前にこの異常を disclose するか、除外すべきだった。

#### Minor / nits

- `docs/loop-engineering/agent-concurrency.md:107` — pilot probe の hazard(2) は AC1/AC8/AC9/AC10 の失敗原因を「detached scratch clone (rather than a branch-tracking one)」に帰属させているが、実際に line 108 のコマンド (`git clone --quiet --branch feature/1067-context-lifecycle --single-branch ...`) を自分で再実行して確認したところ、生成される clone は detached HEAD ではなく `feature/1067-context-lifecycle` を追跡するローカルブランチをチェックアウトした状態だった（`git rev-parse --abbrev-ref HEAD` → ブランチ名そのもの、detached ではない）。観測された4件の AC 失敗自体は実在するが、原因説明として書かれている「detached だから」は誤り。真因は別（別ブランチ由来の ref 解決差異など）にある可能性が高く、この一文は修正が必要。
- `docs/loop-engineering/agent-concurrency.md:125` — Recommendation の "the cheapest and structurally safest candidate (two readers, no writer, disjoint write targets, ...)" は、DP1 が probe 専用に定義した "two readers, no writer" という語句を実際の候補（qa-verifier と codex-reviewer という2つの writer が disjoint な対象に書く）に転用しており、"no writer" と "disjoint write targets" が同じ節内で矛盾している。`## Overlap candidates` 本文（line 48）の記述の方が正確（"write to disjoint targets ... so the two roles never write the same file"）なので、Recommendation側の言い回しを本文に揃えるべき。
- `docs/loop-engineering/agent-concurrency.md:117` — invariant-lock `fail-closed-checkers` は「qa-verifier and codex-reviewer would each still run the same existing fail-closed checks (`check-acs.sh`, `check-pii-shapes.sh`, and the rest)」と述べるが、`agents/codex-reviewer.md` は既定で static-only であり qa-verifier のようにテストスイートは実行せず、`check-intent.sh` の実行も codex-reviewer の責務外と明記されている（同ファイル内の記述）。両ロールが「同じチェック」を走らせるという記述は不正確——実際には検証の深さが異なる。

#### Adversarial findings

Round-1 の blocker/major を受けて、上記3項目を的に「何を壊せるか」を Codex に再度当てた（`.shell-team/reviews/T-1068-codex-adversarial.{txt,jsonl}`）。3項目とも独立に "BREAKS" と再確認され、以下の追加の定量データが得られた（自分で再現・検算済み）:
- P11 全体で `qa-verifier` FAIL は 43 ラウンド中 6 ラウンド＝14.0%、タスク単位では 11 中 3（27.3%）。この失敗ラウンドの平均 codex-reviewer debit（note 自身の `avg_per_round_ms=1540756`）を単純に6回分差し引くと `(29,497,963 − 6×1,540,756) / 88,399,628 ≈ 22.9%`、タスク単位の総計ではなく「実際に PASS→Review が対応したラウンドだけ」の `min` を再集計すると `25,260,594 / 88,399,628 ≈ 28.6%` となり、いずれも headline の 33.4% を下回る。
- `agents/codex-reviewer.md`/`agents/qa-verifier.md`/`skills/run/SKILL.md` の該当行を突き合わせ、"two readers, no writer" 前提が現行契約と矛盾すること、および同時開始には最低5種の contract 変更が要ることを具体的に列挙した。
- Pilot probe の watchdog 欠如について、「shell job が agent でないなら DP1 の agent probe を実施していない／agent の代理とみなすなら watchdog なしで DP1 違反」という二択のいずれでも "ran" は成立しないと結論。
- 追加で見つけた T-1058 telemetry 二重計上、"same fail-closed checks" の不整合、detached-clone の誤帰属は、いずれも自分で一次資料と照合し直して真であることを確認した（上記 Major/Minor に反映済み）。

#### Synthesis audit ledger

no severity changes — Codex 側のネイティブ severity（P1×3, P2×1）は正規化表の通り blocker×3 / major×1 として採用し、格上げ・格下げ・却下は行っていない。追加で見つけた3件（T-1058二重計上・detached-clone誤帰属・fail-closed-checks不整合）は Codex/Adversarial のどちらの回でも severity ラベル無しで提示された新規findingであり、「変更」や「却下」には当たらない（それぞれ自分の判断で major/minor として初回付番した）。Input space の「Out-of-scope synthetic extremes」に照らしても、今回のfindingsはすべて note 自身が reachable と宣言した input class（telemetry の実データ、既存 agent 定義の文面、実行可能な git コマンドの挙動）に基づくものであり、`out-of-input-space` 格下げの対象になるものは無い。

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1068 エントリを `READY_FOR_ENG` に戻す。Blockers 3件（33.4%の再計算/reconcile-cost debitの明示、validate-parallel-reviewの"S"サイジングと安全性記述の是正、pilot probeのDP1準拠——`declined`への変更またはwatchdog付き再実行）と Major 2件（`full-population-diff-cpu`分類の再検討、T-1058 telemetry重複の除外/開示）は、いずれも Verdict scope が定める computed value / conclusion's direction の欠陥であり、次ラウンドでの解消が必須。Minor/nits 3件は connective prose 相当で任意（下記 Fast-follow disposition）。Pre-commitment（同spec同節）の「2回連続でprobeに対する新規独立のBlocker/Majorが着地したらprobeをdeclinedに落とす」自己規律はまだ1round目のため発火しないが、今回のBlocker#3はまさにその対象クラスに当たることを engineer 側に申し送る。

#### Fast-follow disposition

- `docs/loop-engineering/agent-concurrency.md:107`（detached-clone誤帰属の文言修正） — won't-fix 相当で次ラウンドの本修正（Blocker#3のprobe再検討）に自然に巻き込まれる見込みだが、単独では merge を止めない connective-prose 級の誤り。engineer が同じ節を触るなら合わせて直すことを推奨するが、直さなくても再ブロックはしない。
- `docs/loop-engineering/agent-concurrency.md:125`（"two readers, no writer, disjoint write targets" の言い回し矛盾） — 同上、won't-fix 相当（本文lines 48の正確な記述と重複しており実害は小さい）。
- `docs/loop-engineering/agent-concurrency.md:117`（fail-closed-checkers invariant-lockの精度） — file-an-issue 相当の価値はあるが、本タスクのno-mechanism scopeの範囲内で文言修正のみで足りるため、次ラウンドで直せるなら直す方が安く、issueを立てるほどの規模ではないと判断（won't-fix、次ラウンドでの併修正を推奨するに留める）。
