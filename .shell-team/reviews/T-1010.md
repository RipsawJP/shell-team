# T-1010 — QA verification

**Verdict: PASS** → board to `READY_FOR_REVIEW`

## Mechanical layer

- `bash bin/check-intent.sh .shell-team/specs/T-1010-operator-language-boundary.md .shell-team/todo.md`: `aligned: T-1010 v1 (bd5b414a1270cee56d94acf6d1bb13b6a8e71616)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1010.md`: `conformant: .shell-team/provenance/T-1010.md (4 decision entries, 0 sentinel)`.
- `bash bin/check-acs.sh --dry-run .shell-team/specs/T-1010-operator-language-boundary.md`: 0 unrecognized, all 28 `check:` lines + AC29 (SKIP, runtime) recognized.
- `bash bin/check-acs.sh .shell-team/specs/T-1010-operator-language-boundary.md` (live): **27 PASS, 1 FAIL (AC28), 1 SKIP (AC29, by design)**. AC28's failure was solely `test -r .shell-team/reviews/T-1010.md` — this repository's own precedent (T-1007/T-1008/T-1009, and provenance decision 4 here) is that the QA verification commit creates that file, never the preceding engineer commit. Re-ran after adding this file: **AC28 PASS**, all 28/28 mechanical ACs green.
- **Full CI-ordered local run**, every `run:` step in `.github/workflows/check-handoff.yml` in the workflow's file order (54 executable steps; the one non-executable step, "Install shellcheck", was verified separately by matching the local `shellcheck --version` — `0.11.0` — against the workflow's pinned `SHELLCHECK_VERSION`): **0 failures across all 54 steps**, including the pinned shellcheck argument list (which now carries `tests/machine-tokens/run.sh`), every fixture suite, both new "Dogfood check-retro" steps (repo retros + template), the new "Run machine-tokens fixture suite" step, and both PR-diff steps (`check-pii-shapes.sh --base origin/develop`, `check-commit-identity.sh --base origin/develop` — `origin/develop` fetched and confirmed at `4022158`, matching local `develop`).
- Hygiene: `check-handoff.sh` on the resolved board and `check-board-headings.sh --base develop` both exit 0 (part of AC28, re-confirmed after adding this review file).
- `git status --short` was clean throughout verification (confirmed before and after all fixture work — every adversarial/mutation fixture below was built under `$TMPDIR`, never inside this working tree), apart from this review file and the board append made at hand-off.

## D3 — pure-insertion migration, independently measured

- `git diff --numstat 4022158 -- .shell-team/retros/2026-07-28.md` → `5\t0\t...` — 5 insertions (the five marker lines), **0 deletions**, confirming pure insertion.
- `bash bin/check-retro.sh .shell-team/retros/2026-07-28.md` → exit 0.
- CJK byte sweep (`LC_ALL=C tr -cd '\343-\357' | wc -c`): `bin/check-retro.sh` = 0, `docs/templates/retro-template.md` = 0, migrated retro = 5664 (prose genuinely untouched, still Japanese).

## Vacuous-PASS guards, independently spot-checked

- `tests/check-retro/fixtures/pass-operator-language.md`: 100 CJK bytes present (genuinely Japanese headings) and `check-retro.sh` passes it — the "surface freedom" claim (AC6) is not vacuous.
- `tests/check-retro/fixtures/pass-canonical.md`: 0 CJK bytes (genuinely English/ASCII) and passes — AC5 is not vacuous either.
- AC22's mutation self-check, one of the three groups independently reproduced from a fresh scratch copy under `$TMPDIR` (never `git checkout`): mutated `bin/check-handoff.sh`'s `READY_FOR_MERGE` → `MERGE_JUNBI_KANRYO`, confirmed the mutation landed (`grep` for the old token failed, the new token was present), then ran `tests/machine-tokens/run.sh --root <scratch>` → exit 1 with `missing token: READY_FOR_MERGE` — the mutation is caught, not vacuously passed.

## New mechanism (rule-4 boundary extension, provenance decision 1) — exercised directly

- Built a minimal fixture: a complete, valid `## Retro inputs` ledger (all nine ids) followed **immediately** by `<!-- retro-section: keep -->` with no intervening `## ` heading or blank line (the exact shape D3's migration recipe produces — marker inserted directly before the existing section heading, nothing else). `check-retro.sh` → exit 0, no "unrecognised line inside ## Retro inputs" false positive on the marker line itself. Confirms the fix described in provenance decision 1 works as documented, exercised directly rather than only via the suites.

## Adversarial fixtures synthesized beyond the spec's own fixture set

- **Marker with trailing whitespace** (`<!-- retro-section: try -->   `, reachable input — a markdown editor or trailing-whitespace-preserving save could produce this): confirmed the mutation landed, then `check-retro.sh` → exit 0. Matches the AC preamble's frozen tolerance ("after stripping a trailing CR and trailing whitespace").
- **Marker with trailing prose on the same line** (`<!-- retro-section: try --> (extra trailing prose)`): confirmed exit 1, reason = `missing section marker <!-- retro-section: try -->` — correctly fails closed and names the id per the AC preamble's "no other text on the line" clause and "every marker-class violation's reason line contains the literal `retro-section: <id>`" clause.
- **Empty file / file with no `## ` heading at all**: both exit 1 (structural violation via rule 1's H1 check), not exit 2 — correctly distinguished from **unreadable/non-existent path** (exit 2) and **dangling symlink** (exit 2). A **live symlink** to a valid fixture resolves and exits 0. All four match the AC preamble's `0 clean, 1 violation(s), 2 usage/unreadable` contract.
- **Invocation-form coverage**: `bin/check-retro.sh` and `tests/machine-tokens/run.sh` both carry the exec bit; ran each via all three forms — `bash <script>`, `./<script>` direct, and bare name on `PATH` (`PATH="$(pwd)/bin:$PATH" check-retro.sh ...`) — all three produce the same exit code.
- **Marker quoted inside a fenced code block** (documentation-style: a `keep` marker + `## Keep (example)` pair placed inside a ``` fence in the Notes section, after all five real sections): triggers `duplicated section marker <!-- retro-section: keep -->` (exit 1) on an otherwise fully valid retro. This is a real gap in fence-awareness (the marker regex is a flat per-line match with no fence-state tracking), but it is **explicitly named and declined** in the spec's own Non-goals / out-of-scope list: "Nested or overlapping section regions (a marker inside another section's fenced block claiming to open a section). One flat pass over `## ` boundaries is the model; a markdown parser is not being written here." Recorded here as `out-of-input-space` per the QA input-space exemption rule, not a FAIL driver — the shipped `docs/templates/retro-template.md` itself avoids the landmine by documenting the marker syntax only inside a blockquote (`> `-prefixed), never inside a fence.

## Risk notes for reviewer

- `out-of-input-space`: the fenced-code-block marker-quoting gap above. Real but explicitly declined by the spec's Non-goals; not actioned.
- `environmentally-unverified`: none — every applicable adversarial class (payload shape, filesystem/symlink state, invocation form) was exercised; the resource/budget and time/liveness sub-axes are structurally inapplicable to this artifact (a pure read-only bash/awk script with a fixed 5-id enum and no subprocess/network/timeout surface).
- No production code was written or edited during this verification; every fixture was built under `$TMPDIR`, and the repository working tree stayed clean throughout apart from this file and the board append.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1010
- Codex model: gpt-5-codex
- Verification mode: static-only
- Diff scope: 38 files, +1200/-232 lines (`4022158..09723cc`)

#### Blockers
- None.

#### Major
- `bin/check-retro.sh:315` — Rule 4's ("## Retro inputs" ledger) region-closing branch, `if (in_s && (line ~ /^## / || line ~ /^<!-- retro-section: /)) { in_s = 0 }`, closes the ledger region on **any** line that merely starts with the literal `<!-- retro-section: ` prefix — not only on one of the five well-formed, canonical-id markers that the marker-detection pass (lines 145–170) actually validates (`^<!-- retro-section: [A-Za-z0-9_-]+ -->$` with `id in rank`). A malformed or unknown-id marker-like line (e.g. a typo `<!-- retro-section: typo -->`) placed anywhere inside the ledger silently closes the region **before** it is ever checked against the "any other unrecognised non-blank line inside the section... is a violation" rule the script's own docstring (lines 51–55) and rule-4 header comment (lines 254–283, "a CLOSED enum, validated fail-closed") both promise. This contradicts the repo's own "checkers must fail closed" contract (`CLAUDE.md` §Working rules) for a case squarely inside the spec's declared reachable input space (Input space: "a marker with no heading after it," "markers present but out of canonical order," "the space-less `<!--retro-section:keep-->` spelling a human types from memory" — a typo'd id is the same class of human-authored near-miss, not a synthetic extreme).
  - **Reproduced independently** (not just Codex's report): took `tests/check-retro/fixtures/pass-canonical.md`, inserted `<!-- retro-section: typo -->` as the line immediately after the ledger's last entry (`- input: interventions …`), and ran `bash bin/check-retro.sh` against the result → **exit 0**, no violation reported for the inserted line, even though it is exactly the shape rule 4's docstring says must be flagged.
  - **Suggested fix** (also independently proposed by the adversarial pass): thread `RETRO_SECTION_IDS` into rule 4's awk invocation and only close the region on a line that (a) matches the full canonical marker form used by rule 2/3, trailing-whitespace-trimmed, and (b) parses to an id that is actually in the five-id enum. Any other `<!-- retro-section: ...` -shaped line should fall through to the existing `if (in_s) { ... }` unrecognised-line check rather than silently closing the region. Add a regression fixture placing an unknown-id/malformed marker line inside a complete ledger, asserting exit 1 with the `unrecognised line inside ## Retro inputs` reason.
  - This is confirmed to be a genuine gap in the existing suite, not a suite regression: `tests/check-retro/run.sh` passes in full on this branch (verified: `bash tests/check-retro/run.sh` → "All check-retro assertions passed"), because no existing fixture places a marker-shaped line inside the `## Retro inputs` region — the adversarial pass confirmed `tests/check-retro/run.sh:131`'s near-miss-marker cases exercise ordinary sections only, never this boundary.

#### Minor / nits
- None beyond the Major above.

#### Adversarial findings
- Re-ran Codex read-only against `bin/check-retro.sh`'s rule-4 boundary specifically, asking it to break it further (malformed/unknown markers combined with duplicate/out-of-order markers, the `lessons` label rule, CRLF, blockquote/indent edge cases). Result: **no additional independent defect** — every other combination tried (duplicate canonical marker inside the ledger, out-of-order canonical marker, indented/blockquoted/space-less near-miss markers, a ledger-shaped line after a false closure, the `lessons` marker/label walk) stayed correctly fail-closed. The unknown-marker-inside-ledger case and its CRLF variant are the same root cause as the Major finding above, not separate findings. The adversarial pass independently proposed the same fix shape (restrict the closing condition to the validated canonical-id enum) before I disclosed my own reproduction, which is a second, independent line of evidence for the same conclusion.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes |

Codex's native label was P2 on the sole finding; normalized to `major` per the standing map (P2 → major) and left unchanged after the adversarial pass corroborated it without surfacing anything that would move it up or down. No finding was rejected.

#### Other review-focus areas checked (no findings)
- **(a) Structure/language separation** — genuine, not a re-attachment to a different single language. Read `bin/check-retro.sh`'s marker-detection pass directly: it matches only the language-neutral `<!-- retro-section: <id> -->` form and never inspects the adjacent H2's text. Confirmed both directions myself by re-running `tests/check-retro/fixtures/pass-canonical.md` (ASCII/English headings) and `pass-operator-language.md` (Japanese headings) through `bin/check-retro.sh` — both pass; the shipped template/agent-prompt surface is English by design (D1/D2), which is a separate, correct choice from what the checker enforces.
- **(b) Fail-closed-ness** — the Major finding above is the one place this breaks down; everything else checked (unreadable-file exit 2, missing/duplicated/out-of-order canonical markers, marker-not-followed-by-heading, CRLF handling on the *documented* rules) stayed fail-closed on inspection and re-run.
- **(c) Adopter-environment-breaking literals** — checked every newly-authored line in `CLAUDE.md` / `docs/workflow.md` / `docs/workflow.ja.md` / `agents/scrum-master.md` / `docs/templates/retro-template.md` for `bin/`- or `tasks/`-prefixed literals. The only new `bin/`-prefixed reference (`bin/team-paths.sh --get lessons` in `CLAUDE.md` §Language) is a resolver invocation, not a hardcoded layout-specific path — this is exactly the fix the pre-QA rework (`2da1a1e`) made after the orchestrator flagged the T-1009-class defect on the first draft, and it is correct here. Remaining `tasks/reviews/<task-id>.md` / `tasks/lessons.md` literals in `agents/scrum-master.md` are confirmed pre-existing (present in the same count, 12 occurrences, at base `4022158`) — reflowed by translation of surrounding prose, not newly authored — consistent with the rework commit's own stated audit.
- **(d) Machine-token list machine-asserted** — `tests/machine-tokens/run.sh` exists, fails closed on an unreadable consumer file (`die` → exit 2), and asserts every enumerated token as a literal substring against the actual consuming file rather than trusting prose. Re-ran it directly (`bash tests/machine-tokens/run.sh` → all tokens present, exit 0).
- **(e) `docs/workflow.md`/`docs/workflow.ja.md`/`CLAUDE.md` consistency with D2** — re-ran AC24/AC25/AC26's own check lines directly (not just trusting the QA record): all three pass. The zero-config claim in both language versions of `docs/workflow.md` is retained verbatim alongside the new per-cycle-artifacts paragraph, and `CLAUDE.md` §Language names the third file class and the promotion boundary as AC24 requires.
- **issue-canon reliance**: `tests/machine-tokens/run.sh`'s comment and this task's AC21 both describe the asserted token list as "the full list from issue #20." I have no `gh`/API access in this sandbox and cannot independently read issue #20's body to confirm the enumerated groups (status flags, verdict labels, board-line format, intent-block tokens, prompt-block markers, lesson labels, retro contract tokens) are complete against the issue's own canonical list. **internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation)** — the list is internally consistent with the spec text and provenance I was given, and the suite genuinely asserts what it claims to assert; whether it is the *complete* list issue #20 named is not something I could check myself.

#### Recommendation
- REQUEST_CHANGES → set `.shell-team/todo.md` back to `READY_FOR_ENG` with the Major finding above linked. Fix scope is narrow and contained to `bin/check-retro.sh`'s rule-4 awk block plus a regression fixture in `tests/check-retro/fixtures/` — no spec re-freeze expected (the AC preamble and rule-4 docstring already state the exact behavior the fix restores; this is a bug against the existing intent-block, not a new decision).

#### Fast-follow disposition
- no fast-follow deferrals

---

# T-1010 — QA delta verification (rework round 2)

**Verdict: PASS** → board to `READY_FOR_REVIEW`

Scope: Codex round-1 `REQUEST_CHANGES` (one Major, rule 4's ledger region-close matched any `<!-- retro-section: `-prefixed line instead of only a validated canonical marker) → engineer fix `e577991` → this is a delta re-verification, not a full re-run of every class already covered in the round-1 record above.

## 1 — reproduced the original Major against the pre-fix checker, then confirmed the fix

- Extracted the pre-fix checker via `git show 2da1a1e:bin/check-retro.sh` to a scratch file (confirmed it carries no `valid_section_id`, i.e. genuinely pre-fix).
- Built a complete, valid 9-id `## Retro inputs` ledger with `<!-- retro-section: typo -->` (an unknown id) inserted immediately after the ledger's last entry, followed by a plain prose line, then the five real section markers/headings.
- **Pre-fix checker → exit 0** (the bug reproduces: the unknown-id marker silently closes the region, the trailing prose line is never examined).
- **Same input through the fixed checker (current tree) → exit 1**, reporting both `unrecognised line inside ## Retro inputs: <!-- retro-section: typo -->` and the follow-on prose line — the fix catches exactly what previously escaped.

## 2 — adversarial probes of the new close condition beyond the engineer's own three cases

- **Canonical marker with trailing whitespace inside the ledger** (`<!-- retro-section: keep -->   `, immediately after a complete ledger): must still close the region per the frozen trailing-whitespace tolerance. Confirmed the mutation landed, then `check-retro.sh` → **exit 0** (correctly still closes; the tolerance survives the narrowed condition).
- **Legitimate normal case (baseline regression)**: `tests/check-retro/fixtures/pass-canonical.md` → **exit 0** (unaffected).
- **Space-less near-miss id inside the ledger** (`<!--retro-section:keep-->`, immediately after a complete ledger, followed later by the real canonical `keep` marker): does **not** close the region (not the full canonical form) → correctly reported as `unrecognised line inside ## Retro inputs`, exit 1 — same root cause as the engineer's own near-miss case, independently reproduced.
- **My own added shape — CRLF canonical marker legitimately closing the ledger** (`<!-- retro-section: keep -->\r\n` immediately after a complete ledger): confirmed the CR byte landed, then `check-retro.sh` → **exit 0** (the CRLF tolerance also survives the narrowed close condition — this is the "closes correctly" half of the CRLF case; the engineer's own added case covers the "still caught when malformed" half).

## 3 — suites and mechanical checks re-run

- `bash tests/check-retro/run.sh` → all assertions pass, including the three new "Codex-major regression" cases (unknown id, near-miss spelling, CRLF variant) and `fail-inputs-unknown-marker.md`.
- `bash tests/retro-inputs/run.sh` → exit 0.
- `bash tests/check-prompt-sync/run.sh` → exit 0.
- `bash tests/errexit-safe/run.sh` → exit 0.
- `bash bin/check-acs.sh .shell-team/specs/T-1010-operator-language-boundary.md` (live) → **28 passed, 0 failed, 1 skipped** (AC29 by design).
- `bash bin/check-intent.sh .shell-team/specs/T-1010-operator-language-boundary.md .shell-team/todo.md` → `aligned: T-1010 v1 (bd5b414a1270cee56d94acf6d1bb13b6a8e71616)` — no re-freeze, as expected (the fix lives inside the frozen contract's own stated rule-4 behavior).
- `shellcheck bin/check-retro.sh tests/check-retro/run.sh` → clean, exit 0.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1010.md` → conformant, **5** decision entries (up from 4 — decision 5 added), 0 sentinel.
- Spot-checked 2 additional suites closest to the blast radius: `bash tests/retro-inputs/invariants.sh` (bounded invariant lock over `retro-inputs.sh`'s output shape, which feeds the ledger `check-retro.sh` validates) → exit 0; `bash bin/check-retro.sh .shell-team/retros/*.md docs/templates/retro-template.md` (real shipped data through the changed rule-4 path) → exit 0 both. The engineer's own hand-off reports the full 55-step CI-ordered run green; not independently re-run in full for this delta pass, on the reasoning that the change is scoped to one awk block already exercised directly above and by all four required suites plus these two spot-checks.

## Risk notes for reviewer

- Carries forward unchanged from the round-1 record above: the fenced-code-block marker-quoting gap (`out-of-input-space`, explicitly declined by the spec's Non-goals) and the `environmentally-unverified` disclosure (none applicable).
- `git status --short` was clean throughout this delta pass (all fixtures built under `$TMPDIR`) apart from this append and the board update.

---

### Codex Review verdict: APPROVE
- Task: T-1010
- Codex model: gpt-5-codex
- Verification mode: static-only
- Diff scope: 6 files, +203/-3 lines (`09723cc..7d68486`, round-2 delta only)

#### Blockers
- None.

#### Major
- None. The round-1 Major (`bin/check-retro.sh:315`) is fixed — see below.

#### Minor / nits
- `.shell-team/todo.md`'s T-1010 entry has its "rework round 2" and "qa-verifier round-2 delta PASS" sub-bullets inserted **before** the pre-existing `qa-verifier (2026-07-31): **PASS**` sub-bullet rather than appended after it (confirmed by reading `git diff 09723cc..7d68486 -- .shell-team/todo.md` directly: both are pure `+` insertions immediately after the "engineer" bullet; the pre-existing PASS bullet is untouched but now reads last). Read top to bottom, the narrative is engineer → rework round 2 → round-2 delta QA PASS → original round-1 QA PASS, which is temporally backwards: the last bullet (the position a reader would naturally treat as "the latest event") reports the stale 4-decision, pre-fix state even though `check-provenance.sh` now reports 5. Downgraded from the Codex primary pass's `major` (P2) to `minor` after a devil's-advocate re-check — see ledger below for the reasoning; not a blocker to merge.

#### Adversarial findings
- The primary Codex pass raised the board-ordering issue above at `major` (P2). A second, adversarial-framed Codex pass — instructed to try to overturn its own finding — read `bin/check-handoff.sh`, `bin/check-board-headings.sh`, `bin/check-acs.sh`, `bin/check-intent.sh`, `bin/check-provenance.sh`, `CONTRIBUTING.md`, and `CLAUDE.md` directly and reported: (1) no mechanical check in this repo inspects or enforces sub-bullet ordering within a board entry (`check-handoff.sh` explicitly skips indented sub-bullets; `check-board-headings.sh` only diffs heading ids against the base ref; the others operate on separate files/schemas entirely); (2) neither `CONTRIBUTING.md` §"The board line format" nor `CLAUDE.md` documents an append-only or strictly-chronological sub-bullet convention — the documented contract is "notes go in indented sub-bullets," not an ordering rule; (3) the problem is confined to `.shell-team/todo.md` — `.shell-team/provenance/T-1010.md`'s 5th decision is correctly appended last, and `.shell-team/reviews/T-1010.md` is fully chronological (round-1 QA → my round-1 Codex verdict → round-2 delta QA); (4) the top-line status flag itself (`READY_FOR_REVIEW`) stays unambiguous throughout. I agree with this re-assessment: the actual audit-of-record for this task's decisions and verdicts (`provenance.md`, `reviews.md`) is intact and correctly ordered, so this is a readability wrinkle in the board's own narrative prose, not a corruption of the mechanism this repository actually relies on for its audit trail. Independently re-read the diff myself before accepting the downgrade (see the Minor entry above) rather than taking either Codex pass's word for the "inserted before, not after" claim.
- Re-ran the round-1 code-level adversarial framing (rule-4 boundary, malformed/unknown markers, duplicate/out-of-order interactions, CRLF) was not repeated in full this round — the fix is unchanged in shape from what round 1's adversarial pass already exercised, and the delta's own new mutation cases (unknown id, near-miss spelling, CRLF variant) cover exactly that ground; re-running an unrelated adversarial sweep on unchanged code would not have surfaced anything new.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `.shell-team/todo.md` T-1010 entry: round-2 sub-bullets inserted before the round-1 QA PASS bullet, breaking append order | primary: major (P2) / adversarial: nit | minor | Adversarial pass found no mechanical check or documented convention (`CONTRIBUTING.md`, `CLAUDE.md`) enforces board sub-bullet chronology, and the actual audit-of-record files (`provenance.md`, `reviews.md`) are correctly ordered — so impact is confined to board-narrative readability, not to the mechanism this repo relies on for its audit trail. I split the difference between the adversarial pass's `nit` and the primary pass's `major`: the reader-facing confusion (the last bullet reporting a stale decision count) is real enough, and this repo's own narrative conventions elsewhere (e.g. T-1009's entry) are consistently chronological, that I keep it above a pure style nit.

#### Round-2-specific checks (the coordinator's (a)/(b)/(c))
- **(a) Narrowed close condition matches what round 1 converged on, no new gap**: read `bin/check-retro.sh` directly (lines 285–332). The fix threads `RETRO_SECTION_IDS` into rule 4's awk via a new `-v section_ids=` parameter and requires **both** the exact canonical marker form (`^<!-- retro-section: [A-Za-z0-9_-]+ -->$`, trailing-whitespace-trimmed — byte-identical regex and trim to rule 2/3's own, lines 157–158 vs 325–326) **and** a validated id before closing the ledger region — exactly the fix shape both round-1 Codex passes (primary and adversarial) independently proposed, and exactly what I proposed after my own independent reproduction. One observation, not a defect: the regex/trim pair is textually duplicated across the two separate `awk` invocations (each `for FILE` iteration launches rule 2/3 and rule 4 as two independent `awk` processes, so there is no straightforward way to share a literal pattern between them without a wrapper); both draw their id **list** from the same single-source bash variable `$RETRO_SECTION_IDS`, so the two paths cannot drift on *which ids are valid*, but a future hand-edit to the marker *form* itself (spacing, delimiter) in only one of the two awk blocks would not be caught by inspection alone — it would, however, be caught immediately by the existing mutation-style suite discipline this repo already uses throughout `tests/check-retro/run.sh` (any such drift breaks a fixture on the next run). Not flagged as a fast-follow: this is exactly the kind of thing the suite's own mutation self-check is designed to catch, and no code duplication rule in this repo's contract is violated.
- **(b) No round-1 concern re-opens**: re-ran my own exact round-1 reproduction (a mutated `pass-canonical.md` with `<!-- retro-section: typo -->` inserted after the ledger) against the current tree → now correctly `exit 1` with `unrecognised line inside ## Retro inputs: <!-- retro-section: typo -->`. Re-checked focus areas (a)–(e) from the round-1 review (structure/language separation, adopter-breaking literals, machine-token assertion, workflow-doc consistency) against the round-2 delta's changed files (`bin/check-retro.sh`, the new fixture, the new test cases, the provenance append, the board append) — none of those areas touch new territory this round, and all their round-1 supporting `check:` lines were re-run live (see below) and stay green.
- **(c) Fixture/test wiring is real, not vacuous**: ran `bash tests/check-retro/run.sh` directly — the three new "Codex-major regression" cases (unknown id, near-miss space-less spelling, CRLF variant) and the new `assert_violations "fail-inputs-unknown-marker -> 1 (Codex round 1 Major regression)"` line all executed and reported `PASS` in the suite's own output, not merely present as unreachable/unrun code. Independently confirmed the underlying mutations actually land before assertion (`grep -q` guards precede every assertion in the new cases, matching this repo's existing idiom). Also re-ran, live: `bash tests/machine-tokens/run.sh` (all tokens present), `shellcheck bin/check-retro.sh tests/check-retro/run.sh tests/retro-inputs/run.sh tests/machine-tokens/run.sh` (clean), `bash bin/check-intent.sh` (aligned v1, no re-freeze), `bash bin/check-provenance.sh` (conformant, 5 decisions), and `bash bin/check-acs.sh` live (28 passed, 0 failed, 1 skipped by design) against the round-2 tree myself, independent of the QA record's own numbers.

#### Recommendation
- APPROVE → `.shell-team/todo.md` next moves to `READY_FOR_MERGE` (the orchestrator's action, not mine).

#### Fast-follow disposition
- `.shell-team/todo.md` T-1010 entry's non-chronological sub-bullet order (Minor, above): **won't-fix as a tracked issue** — this is wording confined to one task's own board entry, not a systemic gap in shipped `bin/`/`agents/`/`templates/` surface, so a GitHub issue would track a one-off artifact rather than a reusable defect. Recommend the orchestrator simply reorder the two misplaced sub-bullets (move "rework round 2" and "qa-verifier round-2 delta PASS" to after the pre-existing "qa-verifier PASS" bullet, byte-content unchanged) as part of the close-out board edit it already makes when this task merges — no separate rework round needed.
