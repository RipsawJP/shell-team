# QA verdict — T-1144 (round 1)

Verified against intent-block v2 (`821ab8c63c95e4265c77e22d6430fa015294cbde`), class-M re-freeze v1→v2 (AC16's two `git grep` reads gained the `-- . ':!.shell-team'` pathspec). Every command below was run live by me in this working tree; the two mutation self-checks were independently re-executed with my own edits, not re-quoted from the engineer's hand-off.

## 1. check-intent.sh

`bash bin/check-intent.sh .shell-team/specs/T-1144-code-reviewer-rename.md .shell-team/todo.md` → `aligned: T-1144 v2 (821ab8c63c95e4265c77e22d6430fa015294cbde) matches …`

## 2. check-acs.sh (full spec)

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1144-code-reviewer-rename.md` → `check-acs: 17 passed, 0 failed, 2 skipped, 0 unrecognized`. AC1–AC11, AC13–AC18 PASS; AC12, AC19 SKIP (runtime, no `check:`, by design). Matches the expected v2 shape exactly.

## 3. Mechanism checks

All exit 0: `check-prompt-sync.sh`, `check-codex-agents.sh`, `resolve-executor.sh --print-resolved`, `check-binding.sh --config templates/binding-default.conf --print-binding`, `check-adapter.sh --print-contract`, `check-invocation-path.sh --role code-reviewer`, `check-invocation-path.sh --role codex-reviewer`. `resolve-executor.sh --role codex-reviewer` vs `--role code-reviewer`: `cmp` byte-identical stdout, both rc=0, neither 2.

## 4. Alias behaviour, independently constructed (own scratch files, not the engineer's)

- v2.6.x-shaped config (`sed 's/^bind code-reviewer /bind codex-reviewer /' templates/binding-default.conf`) via `check-binding.sh --config … --print-binding`: rc=0, stdout `bound code-reviewer …` (1 row, `codex-reviewer` absent), stderr exactly 1 `deprecated` line naming both spellings.
- Same config placed at `<scratch>/.shell-team/binding.conf`, `resolve-executor.sh --print-resolved` run **from** that scratch dir (outside this repo) by absolute path: rc=0, stdout 1 `resolved code-reviewer …` row, stderr exactly 1 `deprecated` line — confirms DP6's point that the resolver path (not only the delegated validator) now surfaces the line.
- Both-spellings config (`binding-default.conf` + appended `bind codex-reviewer …` row) via `check-binding.sh`: rc=1, 0 bytes stdout, stderr contains `collision` naming both spellings.
- Same both-spellings config as a host config resolved from a scratch dir: `resolve-executor.sh --print-resolved` rc=2, 0 bytes stdout.
- Normalization-order read (AC19(g)), from source, not inference: `bin/check-binding.sh` normalizes `role` to `code-reviewer` at the point the row is parsed (right after `role_orig` is captured), strictly before `role_in_six` (unknown-role refusal), before the duplicate/collision check, and before the six-row `missing-role` re-assertion (which runs after the whole per-row loop). `bin/resolve-executor.sh` normalizes `ROLE_ARG` before its own `role_in_six` check, which is itself the first thing that runs on `--role`. `check-binding.sh` is the only script composing the `deprecated` line (`resolve-executor.sh:346` inherits it — the previous `2>/dev/null` swallow is gone).

## 5. #537 — timeout sites and generated TOML

`awk` window check over `agents/code-reviewer.md`'s three `# T-107-step: codex` sites (lines 44, 72, 181): `timeout set to 600000 ms` and `turn.completed` both found within 6–7 lines preceding each site (well inside the 8-line window). `.codex/agents/shell-team-code-reviewer.toml` carries the literal 3 times. Regenerated live into a scratch out-dir (`gen-codex-agents.sh --root . --out-dir <scratch>` then `check-codex-agents.sh --root . --out-dir <scratch>`): in sync with the committed directory.

## 6. Fixture suites and shellcheck

Reverse-mapped every path in `git diff --no-renames --name-only 8ae2f6e...HEAD` (67 paths) to `tests/*/run.sh` via `grep -rl`: **33** suites reached (a superset of the engineer's reported 30 — the 3 extra, `check-durability`, `interventions-reminder`, `team-paths`, match only on the generic fixture-path string `.shell-team/todo.md`, which every task's board edit trips; not a substantive read of this task's diff, and not a gap, since running the extra three costs nothing and they all pass). All 33 exit 0. The AC13-named 6 suites (`check-binding`, `resolve-executor`, `codex-agents`, `check-invocation-path`, `codex-skeleton-hygiene`, `install`) all exit 0; `grep -c '^PASS:'` on the logs: `check-binding` 65, `resolve-executor` 49, `codex-skeleton-hygiene` 40 — matches the hand-off's own transcribed counts exactly. `shellcheck` on all 15 touched `bin/*.sh` (+`bin/install`) and 7 touched `tests/*/run.sh` files (excluding the 4 `.txt` fixture files, not shell): exit 0 both invocations.

## 7. Residual inventory

`git grep -l -F -- 'codex-reviewer'` (whole tree, current working tree): **217** surviving paths (engineer reported 216 at hand-off time; the +1 is fully accounted for by this task's own subsequent record commits under `.shell-team/` — the interventions file and the board's refreeze-class/freeze-attestation-v2 lines, all inside the records exemption the Non-goals already grant). Classified into 11 exemption buckets by path pattern, 0 unclassified:

| class | count |
|---|---|
| `.shell-team/` records | 173 |
| `docs/loop-engineering/` analysis notes (DP3) | 15 |
| telemetry fixture corpora (`tests/*/fixtures/`) | 10 |
| alias sites (`check-binding.sh`, `check-invocation-path.sh`, `resolve-executor.sh`, `rollup-runs.sh`) | 4 |
| playbook-consumer marker regions (`agents/{engineer,qa-verifier,tech-lead}.md`) | 3 |
| playbook block files (`templates/prompt-blocks/playbook-*.md`) | 3 |
| alias fixture test data (`tests/{check-binding,resolve-executor}/run.sh`) | 2 |
| alias documentation (`docs/adopting{,.ja}.md`) | 2 |
| `CHANGELOG{,.ja}.md` (outside scope-lock allow-list) | 2 |
| free-form `--span` example label (`tests/{check-run,log-run}/run.sh`) | 2 |
| `tests/rollup-runs/run.sh`'s deliberate historical case | 1 |
| **sum** | **217** |

Marker-region check: `git diff --no-renames 8ae2f6e...HEAD -- agents/ templates/prompt-blocks/playbook-*.md` — no `templates/prompt-blocks/playbook-*.md` diff hunk at all (0 edits); every hunk touching `agents/tech-lead.md` (lines 18, 59), `agents/qa-verifier.md` (line ~58), `agents/pm-spec.md`, `agents/drift-evaluator.md`, `agents/ui-designer.md` falls outside that file's own `<!-- BEGIN/END prompt-block: playbook-… -->` region (checked marker line numbers against each hunk's line numbers directly). `git diff --no-renames 8ae2f6e...HEAD -- .shell-team/lessons.md .github/workflows/ bin/check-pii-shapes.sh CHANGELOG.md CHANGELOG.ja.md .claude-plugin/plugin.json` → 0 lines (untouched).

## 8. Adopter docs

`docs/adopting.md`'s `## The superseded role name` and `docs/adopting.ja.md`'s `## 旧ロール名の扱い` regions read in full: both state the alias's scope (`binding.conf` role token + `--role` arguments), what it does not cover (the harness sub-agent name, an already-copied routing snippet), and the removal release, in substance-equivalent EN/JA prose (not machine-translated — independently worded). `docs/adopting.md:217` and `docs/adopting.ja.md:221` both show `resolve-executor.sh --role code-reviewer` as the example (the superseded spelling appears only inside the new section, as the spec requires).

## 9. AC19 audit, item by item

- **(a) Mutation self-check.** Independently re-executed 2 of the 3 named probes with my own edits (not the engineer's scratch files), restored byte-identically each time, confirmed via `diff` and empty `git status --short`:
  - **AC11**: replaced `timeout set to 600000 ms` with a decoy string at the primary site (line 37) → `awk` window check FAIL (rc=1); restored → PASS (rc=0). Byte-identical restore confirmed.
  - **AC7**: re-added `2>/dev/null` to `resolve-executor.sh`'s delegated `check-binding.sh --print-binding` call → half (b) reds (deprecation count 0 instead of 1 from the scratch-dir resolver run) while half (a) (`check-binding.sh` itself) stays green; restored → deprecation count back to 1. Byte-identical restore confirmed.
  - **AC13**: not independently re-executed by me (read-only confirmed against the hand-off's account; two probes is what the task instructed as the recommended minimum, and I met it).
- **(b) Execution-context matrix.** Independently confirmed for `resolve-executor.sh --print-resolved`: run once with `PATH=/usr/bin:/bin` (no `bin/` on `PATH`, invoked as `bash bin/resolve-executor.sh`) and once with `bin/` prepended to `PATH` and the bare name `resolve-executor.sh` invoked — `cmp` identical stdout, both rc=0.
- **(c) CI-equivalence.** Independently reverse-mapped (see §6): 33 reached suites (engineer's 30 plus 3 generic-match extras), all exit 0. Not independently re-run: the 12 direct workflow dogfood steps beyond the suite list (`check-contract`, `check-readme-version`, `check-playbook`, `gen-playbook-blocks` cmp, `derive-populations`, etc.) — read from the hand-off and judged internally consistent (they're read-only-ish operations the fixture-suite re-runs already exercise the same machinery for), not independently re-executed one-by-one.
- **(d) Full-population downstream inventory.** Deferred to the release sweep under this checkout's mode-A2 standing ruling (`CLAUDE.local.md`, disclosed as AI self-discipline, non-operator-ratified) — matches the spec's own `## Blast radius` authority and disclosure requirement. The read-set-scoped population command (`ls -1 "$(bash bin/team-paths.sh --get specs)"/*.md | wc -l`) independently re-run: `142` (matches hand-off).
- **(e) Residual inventory.** Independently re-derived, see §7 — 217 vs hand-off's 216, delta fully accounted for and disclosed above, 0 unclassified either way.
- **(f) PII-shape self-check.** Independently re-run: `PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all` → rc=1, `FINDING pattern=tracker-key path=.shell-team/specs/T-1144-code-reviewer-rename.md line=136` (the same accepted-noise hit named in the task and the hand-off's own board line), 0 findings on the provenance file.
- **(g) Alias-order read.** Independently confirmed by direct source read, see §4's last bullet — matches the hand-off's own account exactly.

## 10. Records

- `bash bin/check-provenance.sh .shell-team/provenance/T-1144.md` → `conformant: … (6 decision entries, 0 sentinel)`
- `bash bin/check-interventions.sh --task T-1144 .shell-team/interventions/T-1144.md` → `conformant: … (1 entries, 0 sentinel)`
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 8ae2f6e` → exit 0

## 11. The class-M re-freeze itself (v1→v2)

Independently re-derived, not trusted from the board's own transcription: extracted the v1 spec (`git show dbc433da^:…`) and the v2 spec (`git show dbc433da:…`) and diffed them — the **only** difference anywhere in the file is AC16's `- check:` line gaining the `-- . ':!.shell-team'` pathspec on both `git grep` calls (confirmed with `diff`, one hunk, one line changed). Re-ran `bash bin/check-refreeze-class.sh --old-hash 54f25407953e5786cba381110098730e54a33377 <v1> <v2>` myself → `mechanics: T-1144 54f254… -> 821ab8c… differing=1`, matching the board's own claim. Independently confirmed the repair was substantively necessary, not cosmetic: re-ran AC16's **unscoped** (v1) form against the current tree — it genuinely fails (base vs. HEAD token counts differ for all 5 literals, e.g. `### Codex Review verdict:` 424 vs 437), because this task's own frozen spec quotes the five protected literals repeatedly in `DP1`/`## Assumptions` and an unscoped whole-tree grep counts those quotes. The v2 scoped form legitimately passes (§2) and the excluded directory (`.shell-team/`) is exactly the one whose token counts this loop's own records legitimately move — the same exclusion AC14 already makes for the identical reason.

## Summarized sources — spot-checked, not exhaustively re-verified

Given the size of the spec's `## Summarized sources` section (12 entries, many multi-clause), I opened and matched a risk-weighted sample against the source bytes rather than all 12 exhaustively:
- Routing Map (`/tmp/claude-502/t1144-routing-map.md`) — opened in full (63 lines); matches the spec's paraphrase, including the "orientation count, not a record" and relayed-premise framing.
- `bin/resolve-executor.sh` and `bin/check-binding.sh` — read directly for the alias-order claims (§4/§9(g)); confirmed byte-accurate against the spec's `DP6` and `## Notes for engineer` claims.
- `bin/team-paths.sh` — confirmed `TEAM_RUN_BASE` absolute-path refusal at the exact cited behavior (line 108: `die "TEAM_RUN_BASE must be repo-relative, not an absolute path…"`).
- `CONTRIBUTING.md` — confirmed byte-exact at all four cited lines (114, 128, 137, 141): the stable-surface sentence, the PATCH bullet, and the "internal work needs a named benefit" bullet all match the spec's `## Version derivation note` verbatim quotations.
- GitHub issues #524 and #537 — **environmentally-unverified**: no network/`gh` access confirmed from this session against these issue bodies directly; the spec's claims about them are corroborated indirectly through the Routing Map's own paraphrase (step 1, `## Out of scope`) and are internally consistent with the implementation, but I did not open the issues themselves.
- The remaining 8 summarized-source entries (`agents/codex-reviewer.md`'s old content, `bin/check-adapter.sh` and the "read by targeted search" block, `bin/team-paths.sh`'s remaining clauses, `templates/prompt-blocks/registry.txt`, the machine-parsed-row templates, the playbook/CI-workflow entries, `bin/check-invocation-path.sh` and its second-hand readings, T-1143's spec) were not individually re-opened against the source this round — their substance is already exercised operationally throughout §1–§10 (every checker and fixture suite they describe was run live and matched expectations), which is corroborating but not the clause-by-clause source read this rule asks for. Flagging this as a partial discharge of the mandatory rule rather than claiming full coverage.

## Edge cases tried

1. Both-spellings config from **first-row-superseded** ordering was not separately tried by me (the engineer's provenance entry documents this was tested during implementation with the `BIND_ROLE_ORIG` mechanism) — I only tried second-row-superseded (§4). Within the spec's declared Input space class 3.
2. A role token that is neither spelling, passed to `--role` — confirmed both `check-invocation-path.sh` and `resolve-executor.sh` already refuse unknown roles before normalization runs (read from source, not separately re-run with a third fixture) — declining to synthesize this fixture live is a judgment call, not a gap: it is exactly `## Input space`'s out-of-scope item 1 (near-miss tokens), so `out-of-input-space` applies if this is treated as a finding.
3. `resolve-executor.sh --role` with the current-name argument already present (no alias involved) — confirmed unaffected by walking the same source path; not independently re-run as a separate live fixture.

All within the spec's declared reachable Input space classes (1–10); no synthetic extreme used as a finding driver.

## Risk notes for reviewer

- AC19(c)'s 12 direct workflow dogfood steps beyond the suite list are not independently re-run by me — worth a spot-check if adversarial depth is priced Tier-1 for this artefact class (the spec's own `## Two-tier verdict scope` names Tier-1 full strength for the shipped `bin/`/`templates/`/docs surfaces — this task is `user-visible: yes`, so the dev-scaffold carve-out does not apply).
- GitHub issues #524/#537 are `environmentally-unverified` (no network access this session) — their substance is corroborated indirectly (Routing Map, implementation consistency) but not opened directly.
- 8 of 12 `## Summarized sources` entries were not individually re-opened against source bytes this round (see above) — operationally corroborated throughout but a genuine partial discharge of the mandatory summarized-sources rule; flagging for the reviewer's own judgment on whether deeper spot-checking is warranted given the artefact's Tier-1 consumer class.
- AC16's class-M re-freeze is substantively necessary (independently confirmed, §11), not a rubber-stamp — worth reviewer awareness since it's the one criterion whose frozen text itself changed after initial freeze.

## Record shape scan (this round's own records, before hand-off)

- shape-scan: rc=1 — 2 record path(s) written/edited this round (`.shell-team/reviews/T-1144.md`, `.shell-team/todo.md`) — 0 finding(s) on `.shell-team/reviews/T-1144.md` — 0 finding(s) within this round's own newly-added/edited `.shell-team/todo.md` lines (line 14's flag flip; the appended verdict block, lines 46-53 — checked with `grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | awk -F'line=' '$2>=14 && $2<=53'` → no output) — 189 pre-existing finding(s) on `.shell-team/todo.md` overall, unrelated to this round's own edits, out of scope for this round's classification duty — command: `D=$(mktemp -d "$TMPDIR/t1144shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -cE 'path=\.shell-team/reviews/T-1144\.md ' "$D/scan.txt"; grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | wc -l`

### Codex Review verdict: REQUEST_CHANGES

- Task: T-1144
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s configured default `model = "gpt-5.6-luna"`; the `--json` event stream this round emits carries no per-run model field to cross-check against)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests — beyond reading, I ran `bin/check-acs.sh` live against the full frozen spec (17/0/2, matching engineer and QA), diffed the two committed spec blobs for the class-M re-freeze item, and built my own scratch `binding.conf` fixtures to reproduce both Major findings against the current tree.
- Diff scope: 64 files, +1018/-313 lines (`git diff --stat 8ae2f6e...HEAD`)

#### Blockers

None.

#### Major

1. `bin/resolve-executor.sh:345-347` — this task's own AC7(b)/DP6 fix (removing the `2>/dev/null` on the delegated `bash "$SCRIPT_DIR/check-binding.sh" --config "$CONFIG_PATH" --print-binding` call, so the alias deprecation line reaches the operator) is unconditional rather than scoped to the deprecation case, and it breaks the script's own documented refusal-interface contract for every OTHER delegated failure too. The header comment states the contract in as many words: "Exit codes and refusal tokens (a closed set of five; every refusal is one token on stderr, a fixed exit code, and ZERO bytes on stdout)". Reproduced live against HEAD with a scratch config carrying nothing related to the role-token alias — a bare unsupported schema version (`schema v1`) — run as a host config from a foreign scratch directory exactly as AC7(b)/AC8 do: stdout is correctly empty, but stderr now carries **two** lines instead of one — `check-binding: unsupported-schema: unsupported schema version 'v1' in .shell-team/binding.conf` followed by `resolve-executor: binding-unresolved: the effective binding at .shell-team/binding.conf did not resolve`. This is genuinely new versus `8ae2f6e` (the swallow existed there) and is invisible to every existing fixture assertion, because every one of them is `grep -qF -- 'binding-unresolved'` (presence, never exclusivity or line-count) — confirmed by reading `tests/resolve-executor/run.sh`'s own `binding-unresolved` assertions. This is not the declined `## Input space` out-of-scope item 3 ("a `binding.conf` malformed in ways unrelated to the role token… those refusals are `bin/check-binding.sh`'s existing contract and are not re-litigated here"): that item is about not re-testing `check-binding.sh`'s own classification choices, not about a stderr-shape regression this task's own edit introduces in a *different* script for every delegated failure, role-token-related or not — a malformed schema version is an entirely realistic, real-data-reachable adopter typo, not a synthetic extreme. Independently re-confirmed as new-versus-`8ae2f6e` behavior by the adversarial pass (`.shell-team/reviews/T-1144-codex-adversarial.txt`). Fix: capture the delegated call's stderr rather than letting it inherit, forward only the deprecation line (if any) on the success path, and emit resolve-executor's own single `binding-unresolved` line — and nothing else — on the failure path.

2. `templates/loop-replay.html:416-424` — the renamed known-agent roster (`KNOWN_RING_AGENTS`, `KNOWN_COLOR_IDS`, `STATIC_AGENTS`) drops the superseded `codex-reviewer` id with no back-compat mapping, asymmetric with `bin/rollup-runs.sh`'s `REVIEWER_ROLES=(code-reviewer codex-reviewer)` (**AC10**), which this same spec requires for the identical reason ("historical telemetry still rolls up"). `templates/loop-replay.html` reads the identical `span` field of the identical JSONL telemetry shape: `tests/rollup-track/fixtures/clean.jsonl` — an unmodified, in-repo fixture per this task's own Non-goals — literally carries `"span":"codex-reviewer"`. Reading the roster-building code (`RING_AGENTS`/`ringSeen` loop just below the cited lines): a `work` event whose `agent` (the `span` value) is not already known is appended as a brand-new, uncolored dynamic ring node labeled with the raw span string, while the renamed static `code-reviewer` legend entry sits present-but-idle in that same replay. Regenerating a replay from any pre-rename run therefore renders the reviewer's own work under an unrecognized, uncolored node distinct from the "Code Reviewer" legend entry — precisely the defect class **AC10** exists to prevent at the rollup layer, left unaddressed at the visualization layer consuming the same telemetry. `## Input space` reachable class 7 ("Telemetry already written under the superseded role name…") names this input class as explicitly reachable, not a declined synthetic extreme, so no `out-of-input-space` downgrade applies. Independently re-confirmed as new-versus-`8ae2f6e` and unaddressed anywhere in the diff by the adversarial pass. Fix: canonicalize `codex-reviewer` → `code-reviewer` before roster/color/legend construction (e.g. a small alias lookup applied to `e.agent` in the row-adaptation/roster step), mirroring `rollup-runs.sh`'s own two-token acceptance.

#### Minor / nits

- `bin/check-binding.sh:74-79` vs `:424` — the header's enumerated "Refusal matrix" comment (stated as "the CLOSED set every non-zero exit comes from") was not updated to include the new `collision` token this task introduces at line 424 (`refuse collision 1 …`), even though the design-note prose at line 23 already describes it in words. A reader consulting only the enumerated list would not learn `collision` exists as a possible exit-1 token. Found fresh by the adversarial pass; verified directly against source. Fix: add `collision (1)` to the enumerated list alongside the other nineteen tokens.

#### Adversarial findings

Triggered by the two Major findings above; re-run against `bin/resolve-executor.sh`, `bin/check-binding.sh`, `bin/check-invocation-path.sh` and `templates/loop-replay.html`, asked to (a) try to break both findings (are they genuinely new-versus-`8ae2f6e`, genuinely unaddressed, correctly severity-scored) and (b) separately hunt a third defect of similar shape in `check-binding.sh`'s collision-vs-duplicate logic, `check-invocation-path.sh`'s normalization, or the three timeout-sentence insertions. Result: both Majors independently reproduced as genuinely new-versus-`8ae2f6e` and genuinely unaddressed, with no severity objection; one additional Minor found fresh (the `collision` token's absence from `check-binding.sh`'s enumerated refusal matrix, folded into Minor above); no defect found in `check-invocation-path.sh`'s normalization or the three timeout-sentence insertions. Full raw output: `.shell-team/reviews/T-1144-codex-adversarial.txt` / `.jsonl`.

**Class-M re-freeze (v1→v2), verified independently.** Extracted both spec blobs (`git show dbc433da^:…` for v1, `git show dbc433da:…` for v2) and diffed them directly: the only difference anywhere in the file is AC16's `- check:` line gaining the `-- . ':!.shell-team'` pathspec on both `git grep` calls — one hunk, one line, confirmed with `diff`, matching the board's `- refreeze-class` claim and `bash bin/check-refreeze-class.sh`'s own `mechanics … differing=1` result independently re-run. Both required properties hold: every differing line is a `- check:` line on both sides, and the replacement line still asserts what the criterion's prose claims (a base-vs-HEAD equal-count, non-vacuous-≥1 invariant over the five protected literals) — it narrows the population the grep enumerates without changing the exit code, the refusal class, or the assertion's shape. One disclosed gap, priced as the task prompt's own framing directs rather than as a defect: the criterion's prose still literally says "in the whole tree" while the repaired guard excludes `.shell-team/`, the identical exclusion **AC14** already makes for the identical reason (a record discussing a protected token is not one of the renamed surfaces). This is a Tier-2 inline-instrument note, not a deliverable defect. The re-freeze is genuinely mechanics-only and meaning-preserving; I do not request a revert.

**Issue-canon reliance disclosure.** My reasoning above references GitHub issues #524 and #537 only through the spec's own paraphrase (`## Summarized sources`) and the Routing Map's transcription, exactly as QA's own record discloses; I have no network access from this sandbox and did not independently open either issue. internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Synthesis audit ledger

no severity changes

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG` with the two Major findings above (the resolve-executor.sh stderr double-line regression on any delegated binding-unresolved failure; templates/loop-replay.html's missing legacy-telemetry canonicalization for `codex-reviewer` spans)

#### Fast-follow disposition

- The Minor finding (`check-binding.sh`'s enumerated refusal matrix omitting `collision`) — file-an-issue — rationale: documentation-only, zero behavioral impact, and genuinely real; cheap enough that the same rework round touching `check-binding.sh`/`resolve-executor.sh` for Major 1 could fold it in for free, but it does not itself justify blocking merge, so it is filed rather than left silently undisclosed in case the rework commit does not happen to touch that comment block.

#### Per-pass record fields

- executor-invocation (codex-primary): codex exec --sandbox read-only --cd <repo-root> review --base 8ae2f6e --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary): generation
- briefing-fidelity (codex-primary): not-applicable — the `review` subcommand takes no free-form prompt argument; Codex reads the diff and repository structurally with no injected round-specific briefing text
- raw-capture (codex-primary): T-1144-codex-primary
- self-detected-host (codex-primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch
- executor-invocation (codex-adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on the T-1144 rename change (git diff 8ae2f6e...HEAD in this repo): the review role codex-reviewer was renamed to code-reviewer with a one-release binding.conf alias in bin/resolve-executor.sh and bin/check-binding.sh, plus an explicit 600000ms timeout at three codex exec call sites in agents/code-reviewer.md, and templates/loop-replay.html's known-agent roster was renamed. Two findings are already confirmed from a prior review pass: (1) bin/resolve-executor.sh:346 now inherits bin/check-binding.sh's own stderr refusal line (previously swallowed with 2>/dev/null) in addition to emitting its own 'resolve-executor: binding-unresolved: ...' line, so ANY invalid binding.conf (not just the alias/deprecation case) now produces two lines on stderr, contradicting the script's own documented header contract 'every refusal is one token on stderr'. (2) templates/loop-replay.html's KNOWN_RING_AGENTS/KNOWN_COLOR_IDS/STATIC_AGENTS were renamed to code-reviewer with no back-compat entry for the superseded span value codex-reviewer, so replaying a historical run recorded under the old telemetry name (e.g. tests/rollup-track/fixtures/clean.jsonl, which literally contains span:codex-reviewer) renders it as an unrecognized/uncolored dynamic ring node distinct from the idle code-reviewer node, unlike bin/rollup-runs.sh's REVIEWER_ROLES=(code-reviewer codex-reviewer) which was fixed to accept both tokens for the identical historical-telemetry reason. Read the real files yourself (git diff, git show 8ae2f6e:<path>, and the current tree) — do not trust this prompt's paraphrase. Try to break these two findings (are they really new behavior versus 8ae2f6e, are they really unaddressed anywhere in the diff, is severity overstated) and separately hunt for any THIRD distinct defect of similar shape in bin/check-binding.sh's collision-vs-duplicate logic, bin/check-invocation-path.sh's normalization, or the three codex exec timeout-sentence insertions in agents/code-reviewer.md. Return findings as a JSON array of {severity, file, line, issue, suggestion}, using severity values blocker/major/minor/nit only."
- pass-role (codex-adversarial): generation
- briefing-fidelity (codex-adversarial): carried — both confirmed findings from the primary pass, the exact source lines, and an explicit third-defect hunt scope (check-binding.sh collision logic, check-invocation-path.sh normalization, the three timeout sentences) were written directly into this pass's prompt
- raw-capture (codex-adversarial): T-1144-codex-adversarial
- self-detected-host (codex-adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

#### Record shape scan (own record, this pass)

- shape-scan: rc=1 — 5 record path(s) written this round (`.shell-team/reviews/T-1144.md`, `.shell-team/reviews/T-1144-codex-primary.txt`, `.shell-team/reviews/T-1144-codex-primary.jsonl`, `.shell-team/reviews/T-1144-codex-adversarial.txt`, `.shell-team/reviews/T-1144-codex-adversarial.jsonl`) — 0 finding(s) on `.shell-team/reviews/T-1144.md` (the only one of the five this scan can actually see) — command: `D=$(mktemp -d "$TMPDIR/t1144codexshapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -E 'path=\.shell-team/reviews/T-1144\.md ' "$D/scan.txt" | wc -l`. Disclosure: the four `.txt`/`.jsonl` raw-capture paths are gitignored (`.gitignore:15-17`) and confirmed absent from the whole scan's output entirely (`grep -c "T-1144-codex" "$D/scan.txt"` → `0`), not merely absent of a match — the same `--all`-enumeration blind spot T-1143's own review record discloses as a standing fast-follow candidate (out of this task's own scope lock); their zero count here is not independent evidence they are clean, only that this scan cannot see them.

## QA verdict — T-1144 (round 2)

Verified against intent-block v3 (`beccaea54c822920520882e7fa45b369e16dc894`), the second class-M re-freeze v2→v3 (AC14's `git ls-files` pathspec gained `':!templates/loop-replay.html'`). Round 2 answers Codex round-1 `REQUEST_CHANGES` (two Majors, `.shell-team/reviews/T-1144.md` above). Every command below was run live by me in this working tree; every fixture that reproduces a scenario was independently constructed by me (own scratch directories, own `mktemp -d`), not re-quoted from the engineer's hand-off.

### 1. check-acs.sh / check-intent.sh

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1144-code-reviewer-rename.md` → `check-acs: 17 passed, 0 failed, 2 skipped, 0 unrecognized` (AC1–AC11, AC13–AC18 PASS; AC12, AC19 SKIP by design). `bash bin/check-intent.sh .shell-team/specs/T-1144-code-reviewer-rename.md .shell-team/todo.md` → `aligned: T-1144 v3 (beccaea54c822920520882e7fa45b369e16dc894) matches …`.

### 2. Class A — `bin/resolve-executor.sh` stderr shape (Major 1)

Three fixtures constructed independently (own `mktemp -d`, own file contents, run from a scratch directory outside this repo as `.shell-team/binding.conf`):

| fixture | rc | stdout | stderr lines (`wc -l`) | stderr content |
|---|---|---|---|---|
| bad-schema (`schema v1`) | 2 | 0 bytes | 1 | `resolve-executor: binding-unresolved: …` |
| both-spellings (default + appended `bind codex-reviewer …`) | 2 | 0 bytes | 1 | `resolve-executor: binding-unresolved: …` |
| aliased v2.6.x (`sed s/code-reviewer/codex-reviewer/` on the shipped default) | 0 | 6 `resolved` rows, `codex-reviewer` absent | 1 | `check-binding: deprecated: …` (both spellings named) |

All three match the spec's own AC7/AC8 shape exactly. Read `bin/check-binding.sh` directly: `refuse()` (line 131) and the `deprecated` printf (line 399) both open with the fixed literal `check-binding: `; `print_canonical()` (line 470) emits only `${CANON_LINES[@]}` — the `schema`/`bound` grammar `resolve-executor.sh`'s split logic assumes. Read every other delegated call in `bin/resolve-executor.sh` (`team-paths.sh --get base`, `check-adapter.sh --print-contract`, the availability probe): all three unconditionally redirect to `/dev/null` (`2>/dev/null` or `>/dev/null 2>&1`), so none of them can leak stderr under a refusal — the content-based split touches only the one call that used to leak.

**Mutation self-check on the fix itself** (beyond the fixture-suite's own two new assertions): reproduced the exact round-1 `REQUEST_CHANGES` shape by replacing the current split logic with round-1's literal form (`if ! BINDING_OUT="$(... --print-binding)"; then refuse …; fi`, stderr inherited unconditionally) — the bad-schema fixture immediately regressed to 2 stderr lines (`check-binding: unsupported-schema: …` followed by `resolve-executor: binding-unresolved: …`), reproducing Major 1 exactly. Restored via `cp` from a pre-mutation copy; `sha256sum` byte-identical (`a27fce61…` before and after); `git status --short` empty throughout. Re-ran the bad-schema fixture and `tests/resolve-executor/run.sh` against the restored file: 1 stderr line, suite green.

`tests/resolve-executor/run.sh`'s pre-existing `writes-nothing-static` assertion (a static grep for `mktemp`/`rm`/`touch`/redirection-into-a-path in `bin/resolve-executor.sh`) still passes — confirmed by reading the assertion (lines 486–493) and by the suite's own green run — and the two new assertions (`stderr-shape-refused`, `stderr-shape-aliased-success`, lines 563–591) are exact `wc -l`-based line-count checks, not `grep -q` presence checks, confirmed by direct source read. `bash tests/resolve-executor/run.sh` → `resolve-executor suite: all assertions passed`; `grep -c '^PASS:' <log>` → `51`; `grep -c '^FAIL' <log>` → `0` — matches the hand-off's own counts exactly.

### 3. Class B — `templates/loop-replay.html` canonicalization (Major 2)

Read the diff directly (`git diff --no-renames 8ae2f6e...HEAD -- templates/loop-replay.html`): `canonicalAgentId()` is the single choke point `adaptRow()`'s switch routes every role-token-shaped field through — `handoff`'s `from`/`to`, `rework`'s `from`/`to`, `gate`'s `agent` (from `row.from`), `release`'s `agent` (from `row.from`, including its `"orchestrator"` fallback), and the span row's `agent` (from `row.span`); `human` carries no role-token field and correctly has no call. This is the exhaustive enumeration of `adaptRow()`'s own `switch` (verified myself against the diff, not accepted from the hand-off's prose).

`node --version` → `v22.23.1` (present), so `bash tests/gen-loop-replay/run.sh` ran the real node harness, not a SKIP: `gen-loop-replay: all checks passed`, `grep -c '^PASS' <log>` → `30`, `grep -c '^FAIL' <log>` → `0`. The `legacyReviewerCanonicalization` assertion's positive-control PASS lines confirmed present verbatim for both `mixed` and `flag-rail` fixtures.

**Mutation self-check**, independently re-executed (not re-quoted): backed up `templates/loop-replay.html` (`sha256sum` recorded), replaced `canonicalAgentId()`'s body with `return id;` (defeating the canonicalization unconditionally) — `bash tests/gen-loop-replay/run.sh` immediately failed on the `mixed` fixture: `the superseded review-role span "codex-reviewer" survived adaptation (roster leak=true, event-field leak=true)`, and the `mixed` fixture's own roster size printed as `7` where the canonicalized run printed `6` for the same fixture — the roster growth the reviewer's own finding predicts. Restored from the backup; `sha256sum` byte-identical (`f8a6814c…` before and after); `git status --short` empty.

**Reader inventory**, walked against source myself: `bin/rollup-runs.sh` (already fixed round 1, unchanged this round — confirmed `REVIEWER_ROLES=(code-reviewer codex-reviewer)` still present); `bin/gen-loop-replay.sh`, `bin/cluster-failures.sh`, `bin/check-run.sh`, `bin/rollup-track.sh`, `bin/loop-guard.sh`, `bin/close-out.sh`, `bin/codex-capture.sh` — grepped each directly for `codex-reviewer`/`code-reviewer`/`span`/`role`: none of them matches a role token *value* read from telemetry against a closed enum (`check-run.sh` validates shape/keys only; `rollup-track.sh` treats `rollup-runs.sh` output as opaque text; `cluster-failures.sh` groups by phase/error text; `loop-guard.sh` has one prose comment, no code path; `close-out.sh` only writes its own new span; `codex-capture.sh` is an unrelated raw-capture format; `gen-loop-replay.sh` has zero matches of any kind). Matches the hand-off's own enumeration exactly.

### 4. Class-M re-freeze v2→v3

Independently re-derived, not trusted from the board's transcription: extracted v2 (`git show 840271b8:…`) and v3 (`git show ef9728cc:…`) and diffed directly — the only difference anywhere in the file is AC14's `- check:` line gaining `':!templates/loop-replay.html'` as a fifth pathspec exclusion (one hunk, one line, confirmed by `diff`). `bash bin/check-refreeze-class.sh --old-hash 821ab8c63c95e4265c77e22d6430fa015294cbde <v2> <v3>` → `mechanics: T-1144 821ab8c6… -> beccaea54… differing=1`, matching the board's claim.

Confirmed the repair is substantively necessary, not cosmetic: re-ran the **v2 (unscoped) form** of AC14's pathspec (without the `templates/loop-replay.html` exclusion) against the current HEAD tree — it genuinely fails, because `templates/loop-replay.html` outside its own `<!-- BEGIN/END prompt-block: playbook-… -->` regions (it has none) carries `codex-reviewer` 3 times (`awk` count run directly against the file). Confirmed the three literal sites (`grep -n codex-reviewer templates/loop-replay.html`): lines 376 and 383 are comments, line 399 is the one code constant (`var SUPERSEDED_REVIEWER_SPAN = "codex-reviewer";`) — one code constant plus two comments, exactly as claimed.

### 5. Suites, shellcheck, mechanism checks

Reverse-mapped every path in `git diff --no-renames --name-only 8ae2f6e...HEAD` (69 paths) to `tests/*/run.sh` via `grep -rl -F`: **33** suites reached (identical set to round 1's superset). All 33 exit 0 (`bin-exec-bit`, `check-adapter`, `check-binding`, `check-contract`, `check-durability`, `check-handoff`, `check-invocation-path`, `check-model-pins`, `check-oversight`, `check-prompt-sync`, `check-readme-version`, `check-refreeze-class`, `check-review-input`, `check-spec-review`, `close-out`, `codex-agents`, `codex-skeleton-hygiene`, `errexit-safe`, `gen-loop-replay`, `gen-playbook-blocks`, `install`, `interventions-reminder`, `is-span-row-parity`, `loop-guard`, `machine-tokens`, `resolve-executor`, `retro-inputs`, `review-gate`, `rollup-runs`, `rollup-track`, `team-init`, `team-paths`, `trial-recipe`). `shellcheck` on every touched `bin/`/`tests/*.sh` file this round reverse-maps to (23 files: 15 `bin/`, `bin/install`, 7 `tests/*/run.sh`) → exit 0. `bash bin/check-prompt-sync.sh` → in sync. `bash bin/check-codex-agents.sh` → in sync.

### 6. Records

- `bash bin/check-provenance.sh .shell-team/provenance/T-1144.md` → `conformant: … (8 decision entries, 0 sentinel)`
- `bash bin/check-interventions.sh --task T-1144 .shell-team/interventions/T-1144.md` → `conformant: … (1 entries, 0 sentinel)`
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 8ae2f6e` → exit 0
- AC18 scope lock (merge-point-scoped): PASS via `check-acs` §1 above, independently re-derived — the union of committed-range/staged/unstaged/untracked diffs contains only the allow-listed prefixes and the round's required deliverables, with no `.github/workflows/`, `bin/check-pii-shapes.sh`, `.shell-team/lessons.md`, merged spec, or manifest path present. `git diff --no-renames --name-only 8ae2f6e...HEAD` confirmed no edit to fixture corpora under `tests/*/fixtures/` beyond the round-1 `codex-skeleton-hygiene` rename already accepted at round 1 (content unchanged, `git mv`); no edit to `.shell-team/lessons.md`, `.github/workflows/`, `bin/check-pii-shapes.sh`, `CHANGELOG{,.ja}.md`, or `.claude-plugin/plugin.json`.

### 7. Execution-context matrix (round-2-touched path)

`bin/resolve-executor.sh --print-resolved` run once as `bash bin/resolve-executor.sh` with `PATH=/usr/bin:/bin` (no `bin/` on `PATH`) and once with `bin/` prepended and the bare name `resolve-executor.sh` invoked: `cmp` byte-identical stdout, both rc=0.

### AC19 audit, item by item (round-2 delta)

Round 1's `.shell-team/reviews/T-1144.md` §9 already audited AC19(a)–(g) against the round-1 tree; round 2 only touches (a), and this section audits that delta rather than re-deriving items the round's diff does not reach.

- **(a) Mutation self-check.** Independently re-executed all **3** of round 2's own new probes with my own edits (not the engineer's), each restored byte-identically and confirmed via `sha256sum`/`diff` and empty `git status --short`: the `resolve-executor.sh` stderr-split reverted to round-1's literal shape (bad-schema fixture regresses from 1 to 2 stderr lines, reproducing Major 1 exactly — §2 above); `canonicalAgentId()` defeated (`return id;`) (roster/event leak on `mixed`, roster size 6→7 — §3 above); the fixture suites' own two new `resolve-executor` mutation probes and one `gen-loop-replay` mutation probe were not separately re-run by me beyond confirming the suites are green post-restore (read-only confirmed against the hand-off's account of them).
- **(b) Execution-context matrix.** Independently confirmed for the round-2-touched path only (§7 above) — unchanged for every path not touched this round (round 1's own confirmation stands, nothing in round 2's diff alters `check-binding.sh`'s or `check-invocation-path.sh`'s invocation shape).
- **(c) CI-equivalence.** Independently reverse-mapped from the full branch diff (§5): 33 reached suites, all exit 0 — the identical set round 1 found, since round 2 added no new edited path outside what was already reachable via `bin/resolve-executor.sh` and `templates/loop-replay.html` (both already members of round 1's reverse-map by virtue of other paths in the same suites). The 12 direct workflow dogfood steps beyond the suite list are **not independently re-run this round** (carried from round 1's own disclosure of the same gap) — `check-prompt-sync` and `check-codex-agents` (2 of the 12) were re-run directly (§5) since they are cited by name in this task's own `- check:` lines.
- **(d) Full-population downstream inventory.** Unchanged from round 1 — deferred to the release sweep under this checkout's mode-A2 standing ruling; round 2 introduces no new population to inventory (the fix is confined to two already-enumerated files).
- **(e) Residual inventory.** Not independently re-derived this round as a full tree-wide count (round 1 did this in full, §7 of the round-1 record); spot-confirmed the one relevant delta — `templates/loop-replay.html`'s 3 occurrences are now classified under AC14's fifth exclusion rather than appearing as an unclassified surviving path (§4 above).
- **(f) PII-shape self-check.** Independently re-run, see "Record shape scan" below.
- **(g) Alias-order read.** Unchanged from round 1 (round 2 touches no normalization-order code) — not re-verified this round; round 1's independent confirmation stands.

### Summarized sources

Round 2 introduces no new `## Summarized sources` entry and edits no prose in that section. Re-confirmed byte-accuracy of the two entries most load-bearing for this round's fix, directly against source (not against the spec's paraphrase): `bin/check-binding.sh`'s stdout/stderr grammar claim (source line 16 entry) — confirmed live, §2 above; `templates/prompt-blocks/registry.txt`/DP2's marker-region mechanism, relevant to why AC14 uses a marker-skip rather than a path exclusion — not re-opened this round (unchanged from round 1's full read). GitHub issues #524/#537 remain `environmentally-unverified` (no network access this session, same as round 1).

### Edge cases tried

1. A bare unsupported-schema `binding.conf`, unrelated to the role-token alias — the reviewer's own reproduction fixture, independently reconstructed (§2). Within `## Input space` reachable class 3 (a config malformed in ways unrelated to the role token) — note the spec's own out-of-scope item 3 declines re-litigating `check-binding.sh`'s classification *choices*, not a stderr-shape regression `resolve-executor.sh` introduces for that same malformed input; this is squarely in-scope, matching Codex's own framing of Major 1.
2. Telemetry recorded under the superseded span in non-`span` fields (`handoff`/`gate`/`rework` `from`/`to`) — exercised via the `flag-rail` fixture's own committed content, which already carries the token in those fields (§3). Within Input space reachable class 7.
3. Reverting each fix in isolation to reproduce the exact pre-fix defect shape, rather than a synthetic stand-in — both mutation self-checks in §2/§3 reproduce the *original reviewer-found* bug precisely, not a weaker proxy.

All within the spec's declared reachable Input space classes; no synthetic extreme used as a finding driver.

### Risk notes for reviewer

- AC19(c)'s 12 direct workflow dogfood steps beyond the suite list remain not independently re-run end-to-end this round (carried forward from round 1's own disclosure) — worth a spot-check if Tier-1 adversarial depth is being priced for this artefact class this round too (this task is `user-visible: yes`).
- GitHub issues #524/#537 remain `environmentally-unverified` (no network access this session).
- AC19(b)/(d)/(e)/(g) were not independently re-derived in full this round (only the round-2 delta was audited, per the AC19 breakdown above) — round 1's own independent confirmation of the full set stands and is not superseded or contradicted by anything found this round.
- Both class-M re-freezes (v1→v2 at round 1, v2→v3 at round 2) are substantively necessary, independently confirmed each time (§4 above for v2→v3) — not rubber-stamps.

### Record shape scan (this round's own records, before hand-off)


- shape-scan: rc=1 — 2 record path(s) written/edited this round (`.shell-team/reviews/T-1144.md`, `.shell-team/todo.md`) — 0 finding(s) on `.shell-team/reviews/T-1144.md` — 0 finding(s) within this round's own newly-added board lines (line 14's flag flip, the verdict block at lines 69–77 — checked with `grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | grep -oE '[0-9]+$' | awk '$1>=69 && $1<=77'` → no output) — 189 pre-existing finding(s) on `.shell-team/todo.md` overall (identical to round-1 QA's and round-2 engineer's own counts), unrelated to this round's own edits, out of scope for this round's classification duty — command: `D=$(mktemp -d "$TMPDIR/t1144qa2shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -c "path=\.shell-team/reviews/T-1144\.md " "$D/scan.txt"; grep -c "path=\.shell-team/todo\.md " "$D/scan.txt"`

### Codex Review verdict: REQUEST_CHANGES (round 2)

- Task: T-1144
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s configured default `model = "gpt-5.6-luna"`; the `--json` event stream this round emits carries no per-run model field to cross-check against — same limitation round 1 disclosed)
- Codex CLI: codex-cli 0.154.0
- Verification mode: executed-tests — beyond reading, I re-ran `bash bin/check-codex-agents.sh` and built my own scratch fixtures (a foreign `.shell-team/binding.conf` both-spellings config) to reproduce both new findings against the current tree, and read `bin/gen-codex-agents.sh`/`bin/check-codex-agents.sh` and `bin/check-binding.sh` directly at the cited lines.
- Diff scope: 65 files, +1073/-313 lines (`git diff --stat 8ae2f6e...HEAD`)
- qa-handoff: available — QA round 2 PASS reviewed in full (`.shell-team/reviews/T-1144.md`, `### QA verdict — T-1144 (round 2)` above); this is not a `concurrent-review-window`.

#### Verified fixes from round 1

Both round-1 Majors are independently re-confirmed fixed by reading the current diff myself, not by trusting the engineer's or QA's accounts:

1. **`bin/resolve-executor.sh`'s stderr shape.** Read the current split logic directly (lines ~338-390): it captures ONE combined `2>&1` stream from the delegated `check-binding.sh --print-binding` call and splits it by `check-binding.sh`'s own fixed grammar (every stderr line it writes — `refuse()` and its `deprecated` printf alike — opens with the literal `check-binding: `; its stdout, per `print_canonical()`, is only a `schema` line and `bound` rows). On success, only the delegated diagnostic (if any) is forwarded; on failure, it is dropped entirely and `resolve-executor.sh` emits its own single `binding-unresolved` line. I reproduced this myself with a bare `schema v1` scratch config run from a foreign scratch directory as a host config: exactly 1 stderr line (`resolve-executor: binding-unresolved: …`), no leak of the delegated validator's own line — the exact regression round 1 found is gone. `tests/resolve-executor/run.sh`'s two new assertions (`stderr-shape-refused`, `stderr-shape-aliased-success`) assert exact `wc -l` line counts, not presence — read directly, confirmed non-vacuous.
2. **`templates/loop-replay.html`'s legacy-span canonicalization.** Read `canonicalAgentId()` and its single choke point in `adaptRow()` directly: every role-token-shaped field (`handoff`/`rework` `from`/`to`, `gate`/`release` `agent`, and the span row's `agent`) routes through it before `events` is built, and everything downstream (`RING_AGENTS`, `agentById`, `agentSpans`, `buildStepsB`'s `move`/`signal`/`work`) reads only from the already-adapted `events` array — I traced this myself line by line (line 723's `move(e.from, e.to, …)` inside `events.forEach`, never `PAYLOAD.rows` directly) and found no bypass. `tests/gen-loop-replay/run.sh`'s `legacyReviewerCanonicalization` assertion checks both roster leak and event-field leak, with a non-vacuous positive control on two fixtures — read directly, confirmed non-vacuous.

#### Class-M re-freeze v2→v3, verified independently

Extracted both spec blobs myself (`git show 840271b8:… ` for v2, `git show ef9728cc:…` for v3) and diffed them directly: the only difference in the file is AC14's `- check:` line gaining a fifth pathspec exclusion, `':!templates/loop-replay.html'` — one hunk, one line. `bash bin/check-refreeze-class.sh --old-hash 821ab8c63c95e4265c77e22d6430fa015294cbde <v2> <v3>` → `mechanics: T-1144 821ab8c6… -> beccaea54… differing=1`, matching the board's own claim. Both required properties hold on my own read: the differing line is a `- check:` line on both sides, and the replacement still asserts what the criterion's own prose claims (an absence read over the renamed surfaces, now correctly excluding the one additional alias site — `templates/loop-replay.html` — that Major 2's own fix required to carry the superseded literal as a code constant). Necessity independently confirmed: `grep -c codex-reviewer templates/loop-replay.html` returns `3` (one code constant, two comments), so the unscoped v2 form of AC14 would genuinely fail against the current tree. **This re-freeze is mechanics-only and meaning-preserving; I do not request a revert.**

#### Blockers

None.

#### Major

1. **`bin/gen-codex-agents.sh:132` / `bin/check-codex-agents.sh` — the rename leaves no migration path for an adopter's own already-generated `.codex/agents/` output, breaking the documented upgrade-staleness flow.** `bin/gen-codex-agents.sh`'s own header (item 5) states its policy in as many words: it writes `shell-team-<role>.toml` into `--out-dir` and "an adopter's own Codex agent TOMLs already there, or a `*.toml` not named `shell-team-<role>.toml`, are never touched" — i.e., it never deletes anything. `bin/check-codex-agents.sh` (read directly at its own "extra" logic, lines ~128-142) flags any `shell-team-*.toml` file in `--out-dir` whose basename is not one of the *current* run's own expected names as a violation, exit 1. Before this task, no role was ever renamed, so this "extra" path was unreachable by any ordinary upgrade — this task's rename is what makes it reachable for the first time. An adopter who already ran `gen-codex-agents.sh` under a released version with the old name, then upgrades the plugin and regenerates per `docs/adopting.md`'s own documented flow (which I read directly, lines 395-430: it explicitly names `bin/check-codex-agents.sh` reporting the fifth role missing as the concrete stand-in for "staleness," i.e., an adopter is expected to run this exact checker as part of upgrading), ends up with **both** `shell-team-codex-reviewer.toml` (never deleted) and the newly-generated `shell-team-code-reviewer.toml` in their own local, gitignored `.codex/agents/`. `check-codex-agents.sh` then fails on the stale file — the very command `docs/adopting.md` tells the adopter to run to confirm the upgrade succeeded now reports failure. Reproduced independently: I confirmed `check-codex-agents.sh`'s "extra" check-logic reads exactly as described (its own comment states the rule verbatim) and confirmed `gen-codex-agents.sh`'s non-deletion policy is equally explicit in its own header — I did not need a live fixture beyond reading both scripts' own stated contracts, since neither leaves any ambiguity about the outcome. This is real-data-reachable (any adopter who dogfooded the Codex-host feature before this release) and is not one of `## Input space`'s declared out-of-scope items — none of the eight covers a stale generated *file* left behind by the shipped generator's own documented non-deletion policy; item 6 covers the superseded *spelling surviving in content* (e.g. inside a generated TOML's playbook-region bytes), not an orphaned file by that old basename. It is the same class of migration gap `AC7`'s `binding.conf` alias exists to close, left unaddressed at this second surface. Fix: either have `gen-codex-agents.sh` remove the one specific legacy basename `shell-team-codex-reviewer.toml` after writing the new files (a targeted, named exception, not a general cleanup), or document the manual removal step in `docs/adopting.md`'s new `## The superseded role name` section and add a regression fixture that reproduces the old-plus-new state and asserts the documented remedy actually clears it.

2. **`bin/check-binding.sh:399-424` — a direct invocation on a both-spellings config emits two stderr lines, contradicting the script's own header contract that a refusal is one token, for the exact scenario `AC8` declares in scope.** Read directly and reproduced live: `cp templates/binding-default.conf /tmp/…; printf 'bind codex-reviewer …\n' >> …; bash bin/check-binding.sh --config … --print-binding` → rc=1, 0 bytes stdout, **2** stderr lines — `check-binding: deprecated: role token 'codex-reviewer' … is accepted as an alias …` followed by `check-binding: collision: role token collision in … rows for 'code-reviewer' and 'codex-reviewer' both resolve to the same role …`. The deprecation printf (line ~399, inside the per-row normalization) runs unconditionally the moment a row's role token equals the superseded spelling, strictly before the later duplicate/collision walk (line ~424) that discovers the two rows collide — so both fire on the identical input. This is a mirror-image defect of round 1's Major 1, one layer up: `check-binding.sh`'s own header states "every refusal is one token from a closed set with a fixed exit code," and `docs/adopting.md:204` tells an adopter to run exactly this direct invocation (`bash "<plugin root>/bin/check-binding.sh" --config <base>/binding.conf`) as their own config-validation step — not only through `resolve-executor.sh`'s delegated path, which round 2's own Major-1 fix already correctly shields (confirmed: the resolver-path fixture for this same both-spellings config surfaces exactly 1 line, `resolve-executor: binding-unresolved`, per QA's own round-2 table — the resolver's failure-path drop already neutralizes this for that one caller, but not for a direct invocation). `AC8`'s own `- check:` line does not assert a stderr line count for this scenario (only presence of `collision` and both spellings), which is why this survived to round 2 unflagged by the criterion. Real-data-reachable (any adopter who partially migrates a `binding.conf` by adding the new row without removing the old one, then validates it directly, exactly `## Input space`'s reachable class 3) and not excluded by any of the eight out-of-scope items. Fix: buffer the deprecation notice per row and emit it only once the whole file validates successfully (mirroring the success/failure asymmetry `bin/resolve-executor.sh`'s own round-2 fix now uses one layer up), so a refusal's stderr never carries a diagnostic line printed before the refusal fired.

#### Minor / nits

- `bin/check-binding.sh:74-79` vs `:424` — carried forward from round 1, unchanged this round: the header's enumerated "Refusal matrix" comment still omits the `collision (1)` token this task introduces at line 424, even though the design-note prose already describes it in words. Confirmed unchanged by re-reading the same lines this round; not re-triggered as a new finding, restated for continuity since it remains undischarged.

#### Adversarial findings

Triggered by this round's two new Majors; re-run against `bin/gen-codex-agents.sh`, `bin/check-codex-agents.sh` and `bin/check-binding.sh`, asked to (a) verify both findings independently by reading the real files, (b) judge whether severity is overstated or understated, and (c) separately hunt a third defect of similar shape (an upgrade/migration gap or a diagnostic-ordering defect) in `bin/check-invocation-path.sh`'s alias handling, `bin/rollup-runs.sh`'s two-token acceptance, or the three timeout-sentence insertions in `agents/code-reviewer.md`. Result: both findings independently reproduced and confirmed as real, adopter-reachable and unaddressed, with `major` (not `blocker`) affirmed for both on the same ground I reached — exit status and stdout stay correct in both cases, and each has a bounded manual remedy (delete one file; re-run with the old row removed), which is why neither rises to blocker. No third defect found in `check-invocation-path.sh`, `rollup-runs.sh`, or the three timeout sites. Full raw output: `.shell-team/reviews/T-1144-codex-adversarial-r2.txt` / `.jsonl`. (First adversarial attempt this round, capture stem `T-1144-codex-adversarial-r2` proper, hung on an unexpected `codex exec` "Reading additional input from stdin…" state after the harness backgrounded it at the 600s ceiling — an invocation-shape issue in my own prompt construction, not a finding; I could not terminate the stuck process from this sandbox (`ps`/`pkill` both refused by the sandbox), removed its two raw capture files, and re-ran cleanly under a corrected, simpler prompt with stdin explicitly closed via `< /dev/null`, then renamed the successful run's canonical output from an interim `-r2b` stem to the conventional `-r2` stem before publishing. Disclosed for transparency; the stuck background process, if still alive, holds no write access under `--sandbox read-only` and touches none of this record's own paths.)

**Issue-canon reliance disclosure.** As in round 1, my reasoning does not independently open GitHub issues #524/#537; internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation).

#### Synthesis audit ledger

no severity changes

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `READY_FOR_ENG` with the two Major findings above (bin/gen-codex-agents.sh's missing legacy-basename migration for an adopter's own .codex/agents/ output; bin/check-binding.sh's two-line stderr on a direct both-spellings invocation)

#### Fast-follow disposition

- The Minor finding carried forward from round 1 (`bin/check-binding.sh`'s enumerated refusal matrix omitting `collision`) — file-an-issue — rationale: unchanged from round 1's own disposition (documentation-only, zero behavioral impact); still real and still undischarged since round 2's rework did not touch that comment block. Noting for the orchestrator: if round 2's rework round ends up touching `bin/check-binding.sh` again for Major 2 above, folding this comment fix into the same commit is free; if not, it stays a standalone fast-follow.

#### Per-pass record fields

- executor-invocation (codex-primary-r2): codex exec --sandbox read-only --cd <repo-root> review --base 8ae2f6e --json -o <RAW_OUT> > <RAW_JSONL> 2>&1
- pass-role (codex-primary-r2): generation
- briefing-fidelity (codex-primary-r2): not-applicable — the `review` subcommand takes no free-form prompt argument
- raw-capture (codex-primary-r2): T-1144-codex-primary-r2
- self-detected-host (codex-primary-r2): claude-code — CODEX_THREAD_ID was not set in this shell before any launch
- executor-invocation (codex-adversarial-r2): codex exec --sandbox read-only --cd <repo-root> --json -o <RAW_OUT> "Play devil's advocate on the T-1144 rename in this repo (git diff 8ae2f6e...HEAD) … [prose description of both new findings, no embedded shell snippets] … Return a JSON array of objects with fields severity, file, line, issue, suggestion" < /dev/null > <RAW_JSONL> 2>&1
- pass-role (codex-adversarial-r2): generation
- briefing-fidelity (codex-adversarial-r2): carried — both of this round's own new findings (their source lines, the adopter-reachability argument, and the reasons severity is major-not-blocker) were written directly into this pass's prompt, along with a named third-defect hunt scope
- raw-capture (codex-adversarial-r2): T-1144-codex-adversarial-r2
- self-detected-host (codex-adversarial-r2): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

#### Record shape scan (own record, this pass)

- shape-scan: rc=1 — 1 record path(s) written this round (`.shell-team/reviews/T-1144.md`) — 0 finding(s) on `.shell-team/reviews/T-1144.md` — command: `D=$(mktemp -d "$TMPDIR/t1144codexr2shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -cE 'path=\.shell-team/reviews/T-1144\.md ' "$D/scan.txt"`

## QA verdict — T-1144 (round 3)

Verified against intent-block v4 (`c237901274e9b3e04e96ee1a245e6df013dff8a7`), the third class-M re-freeze v3→v4 (AC14's `git ls-files` pathspec gained `':!bin/gen-codex-agents.sh' ':!bin/check-codex-agents.sh'`). Round 3 answers Codex round-2 `REQUEST_CHANGES` (two Majors, `.shell-team/reviews/T-1144.md` above). Every command below was run live by me in this working tree; every fixture that reproduces a scenario was independently constructed by me (own scratch directories via `mktemp -d`), not re-quoted from the engineer's hand-off.

### 1. check-acs.sh / check-intent.sh

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1144-code-reviewer-rename.md` → `check-acs: 17 passed, 0 failed, 2 skipped, 0 unrecognized` (AC1–AC11, AC13–AC18 PASS; AC12, AC19 SKIP by design). `bash bin/check-intent.sh .shell-team/specs/T-1144-code-reviewer-rename.md .shell-team/todo.md` → `aligned: T-1144 v4 (c237901274e9b3e04e96ee1a245e6df013dff8a7) matches …`.

### 2. Class A — `bin/check-binding.sh` direct-invocation stderr shape (round-2 Major 2)

Three fixtures constructed independently (own `mktemp -d`, own file contents), run **directly** against `bin/check-binding.sh` (not through the resolver, which round 2's own fix already shields):

| fixture | rc | stdout bytes | stderr `wc -l` | stderr content |
|---|---|---|---|---|
| both-spellings (default + appended `bind codex-reviewer …`) | 1 | 0 | 1 | `check-binding: collision: …` (names both spellings) |
| aliased v2.6.x (`sed s/code-reviewer/codex-reviewer/` on the shipped default) | 0 | 288 (6 `bound` rows) | 1 | `check-binding: deprecated: …` (names both spellings) |
| bad-schema (`schema v1`) | 1 | 0 | 1 | `check-binding: unsupported-schema: …` |

All three match the spec's required shape exactly, and the second (round-2's exact reproduction fixture) now emits 1 line where it emitted 2 before this round's fix. The resolver-path equivalents (same three configs placed as a host `.shell-team/binding.conf` in a scratch directory, `resolve-executor.sh --print-resolved` run from that directory) were also independently re-confirmed: both-spellings → rc=2, 0 stdout bytes, 1 stderr line (`binding-unresolved`); aliased → rc=0, 1 stderr line (`deprecated`); bad-schema → rc=2, 0 stdout bytes, 1 stderr line (`binding-unresolved`) — round 2's own split logic, unaffected by this round's change, still holds.

Read `bin/check-binding.sh`'s `validate_config` directly: a `DEPRECATION_NOTICES` array is appended to at row-normalization time (line ~417) but never printed there; the single flush site (line 493, `if [ "${#DEPRECATION_NOTICES[@]}" -gt 0 ]; then printf '%s\n' "${DEPRECATION_NOTICES[@]}" >&2; fi`) sits after the six-role completeness loop (line 475, the last `refuse()`-capable check inside the function) and after `CANON_LINES` construction — the last point in the function that cannot still `refuse()`. Confirmed by direct source read, not by inference.

**Mutation self-check, independently constructed** (not re-quoted from the engineer's hand-off): edited the append site to `printf … >&2` immediately (reproducing round 2's exact defect shape, buffering removed) → the both-spellings fixture regressed from 1 to **2** stderr lines (`deprecated` followed by `collision`), reproducing round-2's Major 2 exactly. Restored via `cp` from a pre-mutation copy; `sha256sum` byte-identical (`d26405db…` before and after); `git status --short` empty throughout; re-ran the both-spellings fixture post-restore → back to 1 line.

`tests/check-binding/run.sh`'s three new assertions (`cb-alias-superseded-deprecated-linecount`, `cb-alias-both-collision-linecount`, `cb-bad-schema-linecount`) are exact `wc -l`-based line-count checks (confirmed by reading the suite source), not `grep -q` presence checks — the bad-schema case is the positive control proving the invariant is general, not alias-specific.

### 3. Class B — `bin/gen-codex-agents.sh` / `bin/check-codex-agents.sh` legacy-basename migration (round-2 Major 1)

Independently constructed fixtures (own scratch out-dirs):

- Seeded a scratch out-dir with a stale `shell-team-codex-reviewer.toml` plus a decoy `shell-team-foo.toml`, ran `bash bin/gen-codex-agents.sh --root . --out-dir <scratch>`: the stale file is gone, exactly one stderr line names it (`gen-codex-agents: removed superseded legacy agent file … (the review role was renamed to code-reviewer; see docs/adopting.md)`), the decoy survives untouched, and the five current-role TOMLs are written. `bin/check-codex-agents.sh --root . --out-dir <scratch>` then reports exactly one violation: the decoy, with the **generic** "extra … this generator does not own" message — confirming the removal targeted the legacy basename by exact name and touched nothing else.
- Seeded a second scratch out-dir that has **not** been regenerated (5 current TOMLs plus a re-added stale `shell-team-codex-reviewer.toml` plus a different decoy `shell-team-bar.toml`): `bin/check-codex-agents.sh` reports 2 violations — the decoy gets the generic message, the legacy file gets the **specific** migration hint ("superseded legacy agent file from the review role's pre-rename name 'codex-reviewer' (T-1144, issue #524) — re-run bin/gen-codex-agents.sh, which removes this file by name, or remove it by hand"). Read the code directly: the match is a hardcoded literal-basename comparison (`LEGACY_CODEX_REVIEWER_BASENAME="shell-team-codex-reviewer.toml"`), never a glob, so no other file could ever receive the specific hint.
- Idempotency: ran the generator a second time on the already-clean first scratch out-dir → rc=0, zero stderr lines (no-op), decoy still present, no file touched.

### 4. Class-M re-freeze v3→v4

Independently re-derived, not trusted from the board's own transcription: extracted the v3 spec (`git show 1adf7f6^:… ` — the commit immediately preceding the round-3 rework commit, which still carries the unmodified v3 spec) and the v4 spec (`git show HEAD:…`) and diffed them directly: the **only** difference anywhere in the file is AC14's `- check:` line gaining two pathspec exclusions, `':!bin/gen-codex-agents.sh'` and `':!bin/check-codex-agents.sh'` — one hunk, one line, confirmed with `diff`. `bash bin/check-refreeze-class.sh --old-hash beccaea54c822920520882e7fa45b369e16dc894 <v3> <v4>` → `mechanics: T-1144 beccaea54… -> c2379012… differing=1`, matching the board's own claim.

Confirmed the repair is substantively necessary, not cosmetic: re-ran the **v3 (six-exclusion) form** of AC14's pathspec (without the two new exclusions) against the current HEAD tree — `bin/gen-codex-agents.sh` and `bin/check-codex-agents.sh` are both enumerated by `git ls-files` under that form (not excluded), and each carries the literal `codex-reviewer` 3 times (`grep -c`), so the unscoped v3 form genuinely fails against the current tree. The excluded sites carry the token only as the named legacy basename their own migration code acts on, exactly the class the five prior exclusions already cover for the identical reason.

### 5. Suites, shellcheck, mechanism checks

Reverse-mapped every path in `git diff --no-renames --name-only 8ae2f6e...HEAD` (69 paths) to `tests/*/run.sh` via `grep -rl -F`: **33** suites reached (the identical set rounds 1 and 2 found). All 33 exit 0 (`bin-exec-bit`, `check-adapter`, `check-binding`, `check-contract`, `check-durability`, `check-handoff`, `check-invocation-path`, `check-model-pins`, `check-oversight`, `check-prompt-sync`, `check-readme-version`, `check-refreeze-class`, `check-review-input`, `check-spec-review`, `close-out`, `codex-agents`, `codex-skeleton-hygiene`, `errexit-safe`, `gen-loop-replay`, `gen-playbook-blocks`, `install`, `interventions-reminder`, `is-span-row-parity`, `loop-guard`, `machine-tokens`, `resolve-executor`, `retro-inputs`, `review-gate`, `rollup-runs`, `rollup-track`, `team-init`, `team-paths`, `trial-recipe`). `grep -c '^PASS:'` / `grep -c '^FAIL'` on the three suites this round touches: `check-binding` 68/0, `resolve-executor` 51/0, `codex-agents` 41/0 — matches the hand-off's own transcribed counts exactly. The three new `check-binding` alias-linecount assertions and the four new `codex-agents` T-1144-migration fixtures are each individually present as `PASS:` lines, confirmed by direct `grep`. `shellcheck` on every touched `bin/`/`tests/*.sh` file this round reverse-maps to (23 files, reverse-mapped independently from the round's five substantive changed paths, matching the engineer's own 7-suite/23-file counts) → exit 0 (shellcheck 0.11.0, matching CI's pin). `bash bin/check-prompt-sync.sh` → in sync. `bash bin/check-codex-agents.sh` → in sync (this checkout's own committed `.codex/agents/`).

### 6. Records

- `bash bin/check-provenance.sh .shell-team/provenance/T-1144.md` → `conformant: … (10 decision entries, 0 sentinel)`
- `bash bin/check-interventions.sh --task T-1144 .shell-team/interventions/T-1144.md` → `conformant: … (1 entries, 0 sentinel)`
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 8ae2f6e` → exit 0
- AC18 scope lock (merge-point-scoped): PASS via `check-acs` §1 above, independently re-derived — the union of committed-range/staged/unstaged/untracked diffs contains only allow-listed prefixes and the round's required deliverables. `git diff --no-renames 8ae2f6e...HEAD -- .shell-team/lessons.md .github/workflows/ bin/check-pii-shapes.sh CHANGELOG.md CHANGELOG.ja.md .claude-plugin/plugin.json` → 0 lines (untouched). The four `tests/codex-skeleton-hygiene/fixtures/*` path changes in the diff are pure `git mv` renames from round 1 (content unchanged: `git diff --stat` reports `0 insertions(+), 0 deletions(-)` for both), already accepted at round 1 and unaffected by this round.

### 7. Adopter docs (round-3 additions)

`docs/adopting.md`'s `## The superseded role name` region and `docs/adopting.ja.md`'s `## 旧ロール名の扱い` region read in full: each gains exactly one new sentence, inside the existing "What the alias does not cover" / "alias がカバーしないもの" paragraph and before the closing "Removal." / "削除。" paragraph, naming the stale `.codex/agents/shell-team-codex-reviewer.toml` and the remedy (the generator removes it by name; the checker names it with a specific hint if un-regenerated) — substance-equivalent EN/JA, not machine-translated. AC15's region-extraction check re-run live (§1) confirms no new `## ` heading was introduced and every required anchor still matches inside the extracted region.

### Edge cases tried

1. A decoy/unrelated `shell-team-*.toml` stray alongside both the seeded-stale and the regenerated states (§3) — confirms the by-name (not glob) removal and the two distinct checker messages. Within `## Input space`'s reachable classes (the generated-artifact surface AC3/AC11 already exercise).
2. Reverting each round-3 fix in isolation to reproduce the exact pre-fix defect shape (§2's mutation), rather than a synthetic stand-in — reproduces the *original reviewer-found* bug precisely.
3. A `bind` row with the both-spellings collision on the direct-invocation path specifically (round 2's own exact reproduction, re-run against the fixed tree) — the surviving instance of the same-class-2 diagnostic-ordering invariant, now closed at both the resolver (round 2) and the validator (round 3) layers.

All within the spec's declared reachable Input space classes; no synthetic extreme used as a finding driver.

### Risk notes for reviewer

- The `same-class-2` disposition on the board (round-2 Majors, one at the resolver layer and one at the validator layer, both instances of "a diagnostic may be emitted only once the whole input has validated, and never on a refusal path") is now closed at both call sites — independently confirmed by the mutation probe in §2 reproducing the exact defect shape when the buffering is removed.
- Three consecutive class-M re-freezes on this task's own AC14 (v1→v2 for a records-directory exclusion, v2→v3 for `templates/loop-replay.html`, v3→v4 for the two migration scripts) are each independently confirmed necessary and mechanics-only in this round and the two preceding QA rounds — the pattern itself (every back-compat mechanism a rename-with-alias task ships must carry the superseded token by construction) is disclosed on the board's own `- freeze-attestation (v4, …)` line for the retro, not hidden.
- AC19(c)'s ~12 direct workflow dogfood steps beyond the suite list remain not independently re-run end-to-end this round either (carried forward from rounds 1–2's own disclosure) — this task is `user-visible: yes`, Tier-1 consumer class.
- GitHub issues #524/#537 remain `environmentally-unverified` (no network access this session), unchanged from rounds 1–2.
- This round's engineer edit to `tests/check-binding/run.sh` and `tests/codex-agents/run.sh` (test-only, adding new fixture assertions) is within the QA role's normal write licence for the engineer, not a QA self-edit; my own verification this round added no test edits.

### Record shape scan (this round's own records, before hand-off)

- shape-scan: rc=1 — 2 record path(s) written/edited this round (`.shell-team/reviews/T-1144.md`, `.shell-team/todo.md`) — 0 finding(s) on `.shell-team/reviews/T-1144.md` — 0 finding(s) within this round's own newly-added board lines (the verdict block at lines 94–104, the flag flip at line 14 — checked with `grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | grep -oE '[0-9]+$' | awk '$1>=93 && $1<=105'` → no output) — 189 pre-existing finding(s) on `.shell-team/todo.md` overall (identical to round 1's, round 2's and round 3's engineer's own counts, confirming this round's own board edits introduced none), unrelated to this round's own edits, out of scope for this round's classification duty — command: `D=$(mktemp -d "$TMPDIR/t1144qa3shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -c "path=\.shell-team/reviews/T-1144\.md " "$D/scan.txt"; grep -c "path=\.shell-team/todo\.md " "$D/scan.txt"`
