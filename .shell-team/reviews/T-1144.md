# QA verdict — T-1144 (round 1)

Verified against intent-block v2 (`821ab8c63c95e4265c77e22d6430fa015294cbde`), class-M re-freeze v1→v2 (AC16's two `git grep` reads gained the `-- . ':!.shell-team'` pathspec). Every command below was run live by me in this working tree; the two mutation self-checks were independently re-executed with my own edits, not re-quoted from the engineer's hand-off.

## 1. check-intent.sh

`bash bin/check-intent.sh .shell-team/specs/T-1144-code-reviewer-rename.md .shell-team/todo.md` → `aligned: T-1144 v2 (821ab8c63c95e4265c77e22d6430fa015294cbde) matches …`

## 2. check-acs.sh (full spec)

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1144-code-reviewer-rename.md` → `check-acs: 17 passed, 0 failed, 2 skipped, 0 unrecognized`. AC1–AC11, AC13–AC18 PASS; AC12, AC19 SKIP (runtime, no `check:`, by design). Matches the expected v2 shape exactly.

## 3. Mechanism checks

All exit 0: `check-prompt-sync.sh`, `check-codex-agents.sh`, `resolve-executor.sh --print-resolved`, `check-binding.sh --config templates/binding-default.conf --print-binding`, `check-adapter.sh --print-contract`, `check-invocation-path.sh --role code-reviewer`, `check-invocation-path.sh --role codex-reviewer`. `resolve-executor.sh --role codex-reviewer` vs `--role code-reviewer`: `cmp` byte-identical stdout, both rc=0, neither 2.

## 4. Alias behaviour, independently constructed (own scratch files, not the engineer's)

- v2.6.x-shaped config (`sed 's/^bind code-reviewer /bind codex-reviewer /' templates/binding-default.conf`) via `check-binding.sh --config … --print-binding`: rc=0, stdout `bound code-reviewer …` (1 row, `codex-reviewer` absent), stderr exactly 1 `deprecated` line naming both spellings.
- Same config placed at `<scratch>/.shell-team/binding.conf`, `resolve-executor.sh --print-resolved` run **from** that scratch dir (outside this repo) by absolute path: rc=0, stdout 1 `resolved code-reviewer …` row, stderr exactly 1 `deprecated` line — confirms DP6's point that the resolver path (not only the delegated validator) now surfaces the line.
- Both-spellings config (`binding-default.conf` + appended `bind codex-reviewer …` row) via `check-binding.sh`: rc=1, 0 bytes stdout, stderr contains `collision` naming both spellings.
- Same both-spellings config as a host config resolved from a scratch dir: `resolve-executor.sh --print-resolved` rc=2, 0 bytes stdout.
- Normalization-order read (AC19(g)), from source, not inference: `bin/check-binding.sh` normalizes `role` to `code-reviewer` at the point the row is parsed (right after `role_orig` is captured), strictly before `role_in_six` (unknown-role refusal), before the duplicate/collision check, and before the six-row `missing-role` re-assertion (which runs after the whole per-row loop). `bin/resolve-executor.sh` normalizes `ROLE_ARG` before its own `role_in_six` check, which is itself the first thing that runs on `--role`. `check-binding.sh` is the only script composing the `deprecated` line (`resolve-executor.sh:346` inherits it — the previous `2>/dev/null` swallow is gone).

## 5. #537 — timeout sites and generated TOML

`awk` window check over `agents/code-reviewer.md`'s three `# T-107-step: codex` sites (lines 44, 72, 181): `timeout set to 600000 ms` and `turn.completed` both found within 6–7 lines preceding each site (well inside the 8-line window). `.codex/agents/shell-team-code-reviewer.toml` carries the literal 3 times. Regenerated live into a scratch out-dir (`gen-codex-agents.sh --root . --out-dir <scratch>` then `check-codex-agents.sh --root . --out-dir <scratch>`): in sync with the committed directory.

## 6. Fixture suites and shellcheck

Reverse-mapped every path in `git diff --no-renames --name-only 8ae2f6e...HEAD` (67 paths) to `tests/*/run.sh` via `grep -rl`: **33** suites reached (a superset of the engineer's reported 30 — the 3 extra, `check-durability`, `interventions-reminder`, `team-paths`, match only on the generic fixture-path string `.shell-team/todo.md`, which every task's board edit trips; not a substantive read of this task's diff, and not a gap, since running the extra three costs nothing and they all pass). All 33 exit 0. The AC13-named 6 suites (`check-binding`, `resolve-executor`, `codex-agents`, `check-invocation-path`, `codex-skeleton-hygiene`, `install`) all exit 0; `grep -c '^PASS:'` on the logs: `check-binding` 65, `resolve-executor` 49, `codex-skeleton-hygiene` 40 — matches the hand-off's own transcribed counts exactly. `shellcheck` on all 15 touched `bin/*.sh` (+`bin/install`) and 7 touched `tests/*/run.sh` files (excluding the 4 `.txt` fixture files, not shell): exit 0 both invocations.

## 7. Residual inventory

`git grep -l -F -- 'codex-reviewer'` (whole tree, current working tree): **217** surviving paths (engineer reported 216 at hand-off time; the +1 is fully accounted for by this task's own subsequent record commits under `.shell-team/` — the interventions file and the board's refreeze-class/freeze-attestation-v2 lines, all inside the records exemption the Non-goals already grant). Classified into 11 exemption buckets by path pattern, 0 unclassified:

| class | count |
|---|---|
| `.shell-team/` records | 173 |
| `docs/loop-engineering/` analysis notes (DP3) | 15 |
| telemetry fixture corpora (`tests/*/fixtures/`) | 10 |
| alias sites (`check-binding.sh`, `check-invocation-path.sh`, `resolve-executor.sh`, `rollup-runs.sh`) | 4 |
| playbook-consumer marker regions (`agents/{engineer,qa-verifier,tech-lead}.md`) | 3 |
| playbook block files (`templates/prompt-blocks/playbook-*.md`) | 3 |
| alias fixture test data (`tests/{check-binding,resolve-executor}/run.sh`) | 2 |
| alias documentation (`docs/adopting{,.ja}.md`) | 2 |
| `CHANGELOG{,.ja}.md` (outside scope-lock allow-list) | 2 |
| free-form `--span` example label (`tests/{check-run,log-run}/run.sh`) | 2 |
| `tests/rollup-runs/run.sh`'s deliberate historical case | 1 |
| **sum** | **217** |

Marker-region check: `git diff --no-renames 8ae2f6e...HEAD -- agents/ templates/prompt-blocks/playbook-*.md` — no `templates/prompt-blocks/playbook-*.md` diff hunk at all (0 edits); every hunk touching `agents/tech-lead.md` (lines 18, 59), `agents/qa-verifier.md` (line ~58), `agents/pm-spec.md`, `agents/drift-evaluator.md`, `agents/ui-designer.md` falls outside that file's own `<!-- BEGIN/END prompt-block: playbook-… -->` region (checked marker line numbers against each hunk's line numbers directly). `git diff --no-renames 8ae2f6e...HEAD -- .shell-team/lessons.md .github/workflows/ bin/check-pii-shapes.sh CHANGELOG.md CHANGELOG.ja.md .claude-plugin/plugin.json` → 0 lines (untouched).

## 8. Adopter docs

`docs/adopting.md`'s `## The superseded role name` and `docs/adopting.ja.md`'s `## 旧ロール名の扱い` regions read in full: both state the alias's scope (`binding.conf` role token + `--role` arguments), what it does not cover (the harness sub-agent name, an already-copied routing snippet), and the removal release, in substance-equivalent EN/JA prose (not machine-translated — independently worded). `docs/adopting.md:217` and `docs/adopting.ja.md:221` both show `resolve-executor.sh --role code-reviewer` as the example (the superseded spelling appears only inside the new section, as the spec requires).

## 9. AC19 audit, item by item

- **(a) Mutation self-check.** Independently re-executed 2 of the 3 named probes with my own edits (not the engineer's scratch files), restored byte-identically each time, confirmed via `diff` and empty `git status --short`:
  - **AC11**: replaced `timeout set to 600000 ms` with a decoy string at the primary site (line 37) → `awk` window check FAIL (rc=1); restored → PASS (rc=0). Byte-identical restore confirmed.
  - **AC7**: re-added `2>/dev/null` to `resolve-executor.sh`'s delegated `check-binding.sh --print-binding` call → half (b) reds (deprecation count 0 instead of 1 from the scratch-dir resolver run) while half (a) (`check-binding.sh` itself) stays green; restored → deprecation count back to 1. Byte-identical restore confirmed.
  - **AC13**: not independently re-executed by me (read-only confirmed against the hand-off's account; two probes is what the task instructed as the recommended minimum, and I met it).
- **(b) Execution-context matrix.** Independently confirmed for `resolve-executor.sh --print-resolved`: run once with `PATH=/usr/bin:/bin` (no `bin/` on `PATH`, invoked as `bash bin/resolve-executor.sh`) and once with `bin/` prepended to `PATH` and the bare name `resolve-executor.sh` invoked — `cmp` identical stdout, both rc=0.
- **(c) CI-equivalence.** Independently reverse-mapped (see §6): 33 reached suites (engineer's 30 plus 3 generic-match extras), all exit 0. Not independently re-run: the 12 direct workflow dogfood steps beyond the suite list (`check-contract`, `check-readme-version`, `check-playbook`, `gen-playbook-blocks` cmp, `derive-populations`, etc.) — read from the hand-off and judged internally consistent (they're read-only-ish operations the fixture-suite re-runs already exercise the same machinery for), not independently re-executed one-by-one.
- **(d) Full-population downstream inventory.** Deferred to the release sweep under this checkout's mode-A2 standing ruling (`CLAUDE.local.md`, disclosed as AI self-discipline, non-operator-ratified) — matches the spec's own `## Blast radius` authority and disclosure requirement. The read-set-scoped population command (`ls -1 "$(bash bin/team-paths.sh --get specs)"/*.md | wc -l`) independently re-run: `142` (matches hand-off).
- **(e) Residual inventory.** Independently re-derived, see §7 — 217 vs hand-off's 216, delta fully accounted for and disclosed above, 0 unclassified either way.
- **(f) PII-shape self-check.** Independently re-run: `PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all` → rc=1, `FINDING pattern=tracker-key path=.shell-team/specs/T-1144-code-reviewer-rename.md line=136` (the same accepted-noise hit named in the task and the hand-off's own board line), 0 findings on the provenance file.
- **(g) Alias-order read.** Independently confirmed by direct source read, see §4's last bullet — matches the hand-off's own account exactly.

## 10. Records

- `bash bin/check-provenance.sh .shell-team/provenance/T-1144.md` → `conformant: … (6 decision entries, 0 sentinel)`
- `bash bin/check-interventions.sh --task T-1144 .shell-team/interventions/T-1144.md` → `conformant: … (1 entries, 0 sentinel)`
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 8ae2f6e` → exit 0

## 11. The class-M re-freeze itself (v1→v2)

Independently re-derived, not trusted from the board's own transcription: extracted the v1 spec (`git show dbc433da^:…`) and the v2 spec (`git show dbc433da:…`) and diffed them — the **only** difference anywhere in the file is AC16's `- check:` line gaining the `-- . ':!.shell-team'` pathspec on both `git grep` calls (confirmed with `diff`, one hunk, one line changed). Re-ran `bash bin/check-refreeze-class.sh --old-hash 54f25407953e5786cba381110098730e54a33377 <v1> <v2>` myself → `mechanics: T-1144 54f254… -> 821ab8c… differing=1`, matching the board's own claim. Independently confirmed the repair was substantively necessary, not cosmetic: re-ran AC16's **unscoped** (v1) form against the current tree — it genuinely fails (base vs. HEAD token counts differ for all 5 literals, e.g. `### Codex Review verdict:` 424 vs 437), because this task's own frozen spec quotes the five protected literals repeatedly in `DP1`/`## Assumptions` and an unscoped whole-tree grep counts those quotes. The v2 scoped form legitimately passes (§2) and the excluded directory (`.shell-team/`) is exactly the one whose token counts this loop's own records legitimately move — the same exclusion AC14 already makes for the identical reason.

## Summarized sources — spot-checked, not exhaustively re-verified

Given the size of the spec's `## Summarized sources` section (12 entries, many multi-clause), I opened and matched a risk-weighted sample against the source bytes rather than all 12 exhaustively:
- Routing Map (`/tmp/claude-502/t1144-routing-map.md`) — opened in full (63 lines); matches the spec's paraphrase, including the "orientation count, not a record" and relayed-premise framing.
- `bin/resolve-executor.sh` and `bin/check-binding.sh` — read directly for the alias-order claims (§4/§9(g)); confirmed byte-accurate against the spec's `DP6` and `## Notes for engineer` claims.
- `bin/team-paths.sh` — confirmed `TEAM_RUN_BASE` absolute-path refusal at the exact cited behavior (line 108: `die "TEAM_RUN_BASE must be repo-relative, not an absolute path…"`).
- `CONTRIBUTING.md` — confirmed byte-exact at all four cited lines (114, 128, 137, 141): the stable-surface sentence, the PATCH bullet, and the "internal work needs a named benefit" bullet all match the spec's `## Version derivation note` verbatim quotations.
- GitHub issues #524 and #537 — **environmentally-unverified**: no network/`gh` access confirmed from this session against these issue bodies directly; the spec's claims about them are corroborated indirectly through the Routing Map's own paraphrase (step 1, `## Out of scope`) and are internally consistent with the implementation, but I did not open the issues themselves.
- The remaining 8 summarized-source entries (`agents/codex-reviewer.md`'s old content, `bin/check-adapter.sh` and the "read by targeted search" block, `bin/team-paths.sh`'s remaining clauses, `templates/prompt-blocks/registry.txt`, the machine-parsed-row templates, the playbook/CI-workflow entries, `bin/check-invocation-path.sh` and its second-hand readings, T-1143's spec) were not individually re-opened against the source this round — their substance is already exercised operationally throughout §1–§10 (every checker and fixture suite they describe was run live and matched expectations), which is corroborating but not the clause-by-clause source read this rule asks for. Flagging this as a partial discharge of the mandatory rule rather than claiming full coverage.

## Edge cases tried

1. Both-spellings config from **first-row-superseded** ordering was not separately tried by me (the engineer's provenance entry documents this was tested during implementation with the `BIND_ROLE_ORIG` mechanism) — I only tried second-row-superseded (§4). Within the spec's declared Input space class 3.
2. A role token that is neither spelling, passed to `--role` — confirmed both `check-invocation-path.sh` and `resolve-executor.sh` already refuse unknown roles before normalization runs (read from source, not separately re-run with a third fixture) — declining to synthesize this fixture live is a judgment call, not a gap: it is exactly `## Input space`'s out-of-scope item 1 (near-miss tokens), so `out-of-input-space` applies if this is treated as a finding.
3. `resolve-executor.sh --role` with the current-name argument already present (no alias involved) — confirmed unaffected by walking the same source path; not independently re-run as a separate live fixture.

All within the spec's declared reachable Input space classes (1–10); no synthetic extreme used as a finding driver.

## Risk notes for reviewer

- AC19(c)'s 12 direct workflow dogfood steps beyond the suite list are not independently re-run by me — worth a spot-check if adversarial depth is priced Tier-1 for this artefact class (the spec's own `## Two-tier verdict scope` names Tier-1 full strength for the shipped `bin/`/`templates/`/docs surfaces — this task is `user-visible: yes`, so the dev-scaffold carve-out does not apply).
- GitHub issues #524/#537 are `environmentally-unverified` (no network access this session) — their substance is corroborated indirectly (Routing Map, implementation consistency) but not opened directly.
- 8 of 12 `## Summarized sources` entries were not individually re-opened against source bytes this round (see above) — operationally corroborated throughout but a genuine partial discharge of the mandatory summarized-sources rule; flagging for the reviewer's own judgment on whether deeper spot-checking is warranted given the artefact's Tier-1 consumer class.
- AC16's class-M re-freeze is substantively necessary (independently confirmed, §11), not a rubber-stamp — worth reviewer awareness since it's the one criterion whose frozen text itself changed after initial freeze.

## Record shape scan (this round's own records, before hand-off)

- shape-scan: rc=1 — 2 record path(s) written/edited this round (`.shell-team/reviews/T-1144.md`, `.shell-team/todo.md`) — 0 finding(s) on `.shell-team/reviews/T-1144.md` — 0 finding(s) within this round's own newly-added/edited `.shell-team/todo.md` lines (line 14's flag flip; the appended verdict block, lines 46-53 — checked with `grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | awk -F'line=' '$2>=14 && $2<=53'` → no output) — 189 pre-existing finding(s) on `.shell-team/todo.md` overall, unrelated to this round's own edits, out of scope for this round's classification duty — command: `D=$(mktemp -d "$TMPDIR/t1144shapescan.XXXXXX"); PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all > "$D/scan.txt" 2>&1; grep -cE 'path=\.shell-team/reviews/T-1144\.md ' "$D/scan.txt"; grep -oE 'path=\.shell-team/todo\.md line=[0-9]+' "$D/scan.txt" | wc -l`
