# QA Review — T-1140

## Verification method

Live execution and mutation throughout — no finding below is read-only. Ran the
full fixture suite, all three checker invocation modes, `shellcheck` under the
CI's own verbatim argument list, `check-acs` against the frozen spec, and then
independently reproduced the engineer's key claims from scratch: built ad hoc
scratch git repos and mutated throwaway copies of `bin/check-pii-shapes.sh` to
confirm each new rule is individually load-bearing, confirmed both new ids'
naming/wiring facets, confirmed the opt-in changes no other id's verdict,
confirmed the family-coverage claims (hyphen/digit hosts, multiple left
boundaries; short/long tracker-key namespaces and digit runs), confirmed the
negative-control preconditions reach the outline half and are excluded by the
named narrowing, re-ran both `bin/derive-populations.sh` blocks byte-for-byte
against the embedded output, and reproduced the execution-context matrix in a
scratch adopter-shaped repo with the script on `PATH` under a bare name.

### `check-intent`
```
$ bash bin/check-intent.sh .shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md .shell-team/todo.md
check-intent: aligned: T-1140 v2 (a5979dabe2b97d26146e7b95fe39fd56794e0642) matches .shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md
```
aligned — v2's frozen intent block is untouched since the class-M re-freeze that
repaired AC11's own check line (confirmed via `git show 52c81b1` — the only
substantive change is the residual-filter exemption widening from
`\.local\.` to `\.local[.A-Za-z0-9-]`).

### Fixture suite
```
$ bash tests/check-pii-shapes/run.sh
```
exit 0. `grep -c '^PASS:' <log>` = 114, `grep -c '^FAIL:' <log>` = 0.

### The three corpus-scan modes
```
$ bash bin/check-pii-shapes.sh --all        → exit 0, "clean (no PII-shaped bytes found)" (2 binary skips, PNGs, unrelated)
$ bash bin/check-pii-shapes.sh --base develop → exit 0, clean
$ bash bin/check-pii-shapes.sh --base "$(git merge-base develop HEAD)" → exit 0, clean
```

### shellcheck, CI's own verbatim argument list
```
$ awk '/run: shellcheck/{print; exit}' .github/workflows/check-handoff.yml | sed 's/^ *run: //' > cmd.txt
$ bash -c "$(cat cmd.txt)"
```
exit 0 (shellcheck 0.11.0, matches the pinned CI version).

### check-acs against the frozen v2 spec
```
$ CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh --root "$PWD" .shell-team/specs/T-1140-tracker-key-and-host-local-shapes.md
```
`check-acs: 14 passed, 2 failed, 1 skipped, 0 unrecognized` — AC1–AC14 PASS,
AC15/AC16 FAIL (both only for the absence of this very review file, which did
not exist yet at read time), AC17 SKIP (runtime, no `check:`, audited by hand
below). Matches the board's own v2 freeze-attestation re-run exactly.

### Independent mutation verification (beyond the suite's own embedded mutation locks)
Built scratch git repos and mutated throwaway copies of the real checker:
- `RE_HOST_LOCAL_OUTLINE` neutralised in a throwaway copy → the `host-local`
  positive fixture (`<host>.local`) goes unreported (exit 0, was exit 1).
- `RE_TRACKER_KEY_NAMESPACE` neutralised (tracker-key enabled) → the
  `tracker-key` positive fixture (`ABCDE-1234`) goes unreported.
- **Naming facet**: on `/Users/<name>/<host>.local/config lives here`, the real
  checker reports both `home-path` and `host-local` on the same line;
  neutralising `RE_HOST_LOCAL_OUTLINE` leaves `home-path` alone standing —
  confirms the reported id corresponds to logic actually exercised, not a
  side effect of a shared match.
- **Wiring facet**: neutralising the composed rule itself (`RE_HOST_LOCAL`,
  not just its outline half) also silences the finding — confirms the
  narrowing half sits on the reported id's own execution path.
- **Opt-in isolation**: on `/Users/<name>/proj and see AB-1234 too`, the
  default run reports only `home-path`; enabling `PII_CHECK_TRACKER_KEY=1`
  adds `tracker-key` and leaves the `home-path` verdict on that same content
  byte-for-byte unchanged.
- **Negative controls, precondition proven**: `<host>.local.json` matches
  `RE_HOST_LOCAL_OUTLINE` (reaches the outline) but not the composed
  `RE_HOST_LOCAL` (excluded by the named class) — read directly out of the
  checker's own source, same idiom the suite's precondition helpers use.
  Live-run: stays clean by the real checker too. `X-1234` (single-char
  namespace) and `AB1-1234` (digit-bearing namespace) both stay clean with
  the opt-in enabled.
- **Family coverage**: a digit-bearing host token and a hyphenated host token (both `<host>.local`-shaped), and
  the same token after a line start / mid-sentence / `$ ` prompt separator
  all fire `host-local`. `AB-1` (short namespace, short digit run) and
  `ABCDEFGHIJ-123456789` (long namespace, long digit run) both fire
  `tracker-key` with the opt-in on.
- **Collision/boundary probes** (beyond the spec's own enumerated fixtures):
  `localization` (no dot before "local") never reaches the outline;
  `window.localStorage`/`localStorage.getItem` and `mail.localhost` reach the
  outline but are excluded by the composed rule's own right-side boundary
  (followed by a letter, not end-of-line/non-alnum-non-dot) — matches the
  exact fix the v2 re-freeze made to AC11's own filter. `AC-1 and DP-2` with
  the opt-in enabled correctly fires `tracker-key` on both — this is the
  declared, accepted-noise behaviour DP-1/AC10 document, not a defect.

### `bin/derive-populations.sh` reproductions
Re-ran both `- reproduce:` lines verbatim; both embedded blocks in the spec
are byte-identical to what the command re-emits at HEAD (the
`T-1140-host-local-lookalikes` block matches exactly; the
`T-1140-tracker-key-lookalikes` block's four grounding buckets — `all` 84,
`letters-only` 48, `dp-label` 37, `technical` 8 — are unchanged; the `t-id`
bucket alone drifted 354→355, expected, since this task's own diff adds one
new `T-1140` reference to the tree — none of DP-1's grounding numbers moved).

### Two authoring-site record repairs
```
$ git diff --no-renames --word-diff=porcelain "$B"..HEAD -- .shell-team/interventions/T-1109.md .shell-team/reviews/T-1110.md
```
Exactly one token changed in each file (the base name lacking its extension, `<name>.local:`, gained one: `<name>.local.md:`),
nothing else — confirmed by full-diff read and word-diff both.

### No literal shape byte left standing
```
$ git grep -nE -- '[A-Za-z0-9-]\.local' -- 'bin' 'tests' 'docs' 'agents' 'skills' 'templates' '.shell-team' '*.md' | grep -vE '\.local[.A-Za-z0-9-]'
```
0 residual lines at HEAD (was 4 at the branch point — 1 disclosed and closed
by this task's two authoring-site repairs, 3 pre-existing and unrelated,
re-confirmed present-but-unrelated at the branch point below and now
correctly excluded by v2's own repaired filter rather than by touching those
three unrelated files, consistent with Non-goals).
```
$ B=$(git merge-base develop HEAD); git grep -nE -e '[A-Za-z0-9-]\.local' "$B" -- 'bin' 'tests' 'docs' 'agents' 'skills' 'templates' '.shell-team' '*.md' | grep -vE '\.local\.'
```
4 lines at the branch point, matching the spec's own measurement exactly:
`.shell-team/interventions/T-1109.md:25` (this task's repair),
`templates/loop-replay.html:542`/`:1669` and
`tests/check-pii-shapes/run.sh:581` (pre-existing, unrelated, left alone
per Non-goals).
```
$ find tests/check-pii-shapes -type f | wc -l
```
1 — no new file under that directory.

### Provenance / interventions / handoff / durability
```
$ bash bin/check-provenance.sh .shell-team/provenance/T-1140.md
check-provenance: conformant: .shell-team/provenance/T-1140.md (5 decision entries, 0 sentinel)
$ bash bin/check-interventions.sh --task T-1140 .shell-team/interventions/T-1140.md
check-interventions: conformant: .shell-team/interventions/T-1140.md (1 entries, 0 sentinel)
$ bash bin/check-handoff.sh .shell-team/todo.md
(exit 0)
$ bash bin/check-durability.sh --phase implement --task T-1140 --ref HEAD
check-durability: durable: 4 record(s) for phase implement, task T-1140, observed in HEAD
$ B=$(git merge-base develop HEAD); bash bin/check-board-headings.sh .shell-team/todo.md --base "$B"
(exit 0)
```

### Summarized sources — checked against the source, not the spec's paraphrase
- `bin/check-pii-shapes.sh` header rule-decomposition sentence at the branch
  point: `git show $B:bin/check-pii-shapes.sh` shows the literal "There are
  eleven independently load-bearing rules: seven patterns, plus four
  exclusions" — matches the spec's `## Summarized sources` claim exactly; at
  HEAD that literal is gone (`grep -qF` absent) and replaced by
  derived-count prose, matching AC8(iii).
- `docs/pii-controls.md`/`.ja.md` named-entity lines at the branch point:
  `git show $B:docs/pii-controls.md` lines 83–84 and `$B:docs/pii-controls.ja.md`
  line 77 are byte-identical to the spec's quoted whole-line-locked bullets —
  confirmed present, unedited, at HEAD too (AC10).
- `.github/workflows/check-handoff.yml`'s shellcheck argument list at HEAD
  names both `bin/check-pii-shapes.sh` and `tests/check-pii-shapes/run.sh`
  (confirmed by direct read of the extracted line); the file is byte-identical
  to its base-ref blob (AC12, independently `cmp`'d); no `--all` occurrence
  anywhere in the workflow.
- `bin/check-pii-shapes.sh`'s header id list at HEAD:
  `grep -oE '^#   [a-z][a-z-]+ '` yields exactly the 9 shipped+new ids
  (`email-nonnoreply home-encoded home-path home-path-win host-local
  private-key temp-session token tracker-key`) — matches AC8(ii)'s id-set
  equality claim.

### AC17, audited item by item (no `check:` line — read the hand-off against
each item's own enumeration, not accepted as a blanket claim)

- **(a) Mutation self-check** — hand-off claims every `- check:` line's guard
  broken/restored, plus naming/wiring for both new controls. **Confirmed**:
  independently reproduced the same naming/wiring result for both
  `host-local` and `tracker-key` from scratch (above), matching the hand-off's
  own description exactly.
- **(b) Execution-context matrix** — hand-off claims re-run from the checkout
  root without the plugin on `PATH`, and again in an adopter-shaped repo with
  `bin/` reached by bare name. **Confirmed**: built a scratch repo with the
  script copied under `.bin/` on `PATH`, invoked `check-pii-shapes.sh --all`,
  `--help`, and `PII_CHECK_TRACKER_KEY=1 check-pii-shapes.sh --all` by bare
  name — reproduced shipped-default-silent/opt-in-fires/`--help`-documents-
  both-ids behaviour identically.
- **(c) CI-equivalence** — hand-off claims a reverse-mapping of every edited
  path to the workflow steps that read it, with every inapplicable step
  named individually. **Confirmed** by direct read of
  `.github/workflows/check-handoff.yml`: `check-readme-version`'s dogfood
  step names only `README.md README.ja.md` (not `docs/*.md`); no step
  references `test-recipe.md` at all (`grep -n test-recipe` empty); the
  `check-review-input` step runs only its own synthetic fixture suite, never
  a dogfood sweep over real `reviews/*.md`; `check-interventions`'s dogfood
  step loops `.shell-team/interventions/*.md` — all four claims verified
  accurate against the workflow file itself, not against the hand-off's
  description of it.
- **(d) Downstream impact** — hand-off states the full-population two-arm
  sweep is this cycle's single release-time measurement, not a per-task duty,
  consistent with the spec's own `## Verification duty pricing` (`parent
  verify axis: serial`, no per-task fan-out). This task's own disclosed
  addition (`## Notes from engineer`'s AC11 residual-filter finding) is
  present and, per the class-M re-freeze, already closed at the check-line
  level rather than left open. **Confirmed** as consistent with the spec;
  the sweep itself is out of this task's own duty and is not this QA pass's
  job to run.
- **(e) shellcheck over the workflow's verbatim argument list** — present in
  substance in the hand-off (not tagged "(e)" explicitly, but the exact
  command and result are there). **Confirmed** independently — see above,
  exit 0.
- **(f) Plural-corpus read** — hand-off claims re-reading new documentation
  and header prose against the actual measured corpus for a smuggled
  singular determiner; none found. **Spot-confirmed**: the tracker-key
  accepted-noise bullet uses "standards identifiers" (plural); the
  host-local config-file bullet's singular determiner ("a configuration file
  name...") is the same generic-class phrasing the shipped `home-path`
  bullet already uses for its own class, not a smuggled cardinality-one
  claim about the corpus. No counter-example found on inspection.
- **(g) PII-shape self-check before first commit** — hand-off claims two
  pre-commit runs, the first of which caught the engineer's own draft prose
  transcribing a real host-local shape (since rewritten). **Confirmed the
  outcome**: `bin/check-pii-shapes.sh --all` on the committed tree is clean
  (above); cannot re-verify the pre-commit run itself happened as a
  historical event (procedural, not re-executable after the fact), but the
  substantive artifact it exists to protect — no leak in the committed
  tree — is independently confirmed.

## Edge cases beyond the spec's own enumerated fixtures

All four tried were inside the spec's declared `## Input space` reachable
classes (host tokens with hyphen/digit variants at multiple left boundaries;
`AC-`/`DP-` label collisions with the tracker-key outline under the opt-in;
`localStorage`/`localhost` continuation collisions) — none triggered the
`out-of-input-space` exemption; all confirmed correct behaviour, none is a
finding.

## Risk notes for reviewer

- The engineer hand-off's "Notes for QA" line still says "AC11 is expected to
  FAIL and this is disclosed" — that text predates the v1→v2 class-M
  re-freeze (which repaired AC11's own check-line filter) and is now stale;
  AC11 in fact PASSes at HEAD, confirmed independently above and by the
  board's own v2 freeze-attestation. Not a defect — just don't be misled by
  that one stale sentence when reading the hand-off.
- AC17(e)'s content is present in the hand-off but not explicitly labelled
  "(e)" the way (a)–(d)/(f)/(g) are; substance confirmed independently, so
  this is cosmetic only.
- The droppable-1st pre-commitment component (`tracker-key` in full) was not
  invoked — no defeat occurred and both never-dropped components ((i)
  host-local mechanical detection + its negative control, (ii) the
  0/1/2 exit-code contract, (iii) no byte of the new host-name shape in the
  tree) are all intact, independently confirmed above.

### QA verdict: PASS
- Task: T-1140 → READY_FOR_REVIEW
- Tests: check-pii-shapes fixture suite 114 passed, 0 failed
  (`grep -c '^PASS:' <log>` = 114, `grep -c '^FAIL:' <log>` = 0)
- Acceptance criteria: 14/14 scriptable ACs checked off
  (`bash bin/check-acs.sh` → AC1–AC14 PASS; AC15/AC16 required this file to
  exist, re-run below; AC17 audited by hand item-by-item above, all 7 items
  (a)-(g) confirmed)
- Verification ceiling: unit-and-static — this verdict verifies every
  criterion at or below that level; none above this ceiling
- Edge cases tried: hyphen/digit host tokens at three left boundaries;
  short/long tracker-key namespace and digit-run extremes;
  `AC-`/`DP-` label collisions with the tracker-key outline (opt-in);
  `localStorage`/`localhost` continuation collisions with the host-local
  outline
- Risk notes for reviewer: hand-off's "Notes for QA" AC11-expected-FAIL
  sentence is stale post-v2-refreeze (AC11 now PASSes, confirmed
  independently); AC17(e) present in substance but not explicitly labelled;
  no out-of-input-space or environmentally-unverified findings this round

## Codex Review — T-1140

### Verification method

Two Codex passes, both live: (1) the primary `codex exec … review --base develop --json`
subcommand pass, reading the working tree directly; (2) an adversarial
devil's-advocate pass on the two new regex definitions, triggered because the
primary pass surfaced findings at Major/Blocker strength (per this role's
step-5 trigger). Every finding either pass raised, plus a further finding
I made independently while verifying the mandatory class-M item, was
re-confirmed live against the real shipped `bin/check-pii-shapes.sh`
(concrete `grep -E`/scratch-git-repo reproductions below) rather than
accepted on either pass's say-so; one shared claim from both passes was
checked against the suite's own pre-existing convention and rejected as a
misreading (see the synthesis ledger).

Diff scope: 12 files, +1528/-6 lines (`git diff --stat $B...HEAD`, `$B` =
`git merge-base develop HEAD` = `6603cbd`).

### Mandatory class-M item (AC11 residual-filter re-freeze v1→v2)

Confirmed mechanics-only: `git show 52c81b1` touches only
`.shell-team/interventions/T-1140.md`, the one `- check:` line under AC11 in
the spec, and the board's re-freeze bookkeeping — no Goal/Non-goals/
criterion-prose/Input-space line moved, matching `refreeze-class: mechanics`.

The delta widens AC11's residual-candidate exemption from `\.local\.`
(dot-led continuation only) to `\.local[.A-Za-z0-9-]` (dot, letter, digit,
**or hyphen**). The intervention's own stated cause (`.shell-team/todo.md`
`intent-ratified (v1→v2)` line, and the engineer's `## Notes from engineer`)
is that the *old* filter didn't exempt letter-led continuations
(`window.localStorage`, `mail.localhost`) — it says nothing about needing to
exempt a **hyphen**-led continuation. I built the check the class-M item asks
for — comparing the repaired filter against the real `RE_HOST_LOCAL`'s own
boundary — and it is **not** meaning-preserving:

```
$ printf 'see host.local-foo here\n' > f.txt
$ grep -E -- '[A-Za-z0-9-]\.local' f.txt | grep -vE '\.local[.A-Za-z0-9-]'
(no output — v2's AC11 filter now treats this as a non-issue)
$ grep -E -- '[A-Za-z0-9][A-Za-z0-9-]*\.local($|[^A-Za-z0-9.])' f.txt
see host.local-foo here      # MATCHES — the real shipped RE_HOST_LOCAL fires on this
```

So the just-repaired AC11 audit line and the shipped `RE_HOST_LOCAL` it exists
to independently confirm now **disagree** on the identical hyphen-continuation
shape: production fires, the audit's own residual-candidate filter treats it
as exempt. Whichever side is "correct" (see the Major below — I think the
production side is closer to the design's own model, since the same
character class already treats hyphen as a legitimate name character on the
*left* of `.local`), the audit-line half of AC11 no longer proves what it
claims to prove for this shape, though `AC6`'s independent `--all` run
remains a real backstop today (no such token currently exists in the tree, so
`--all` stays clean and the substantive property AC11 exists to protect is
not currently defeated in practice). Scored Tier 2 (spec-inline check line) —
a real instrument defect, not adversarial guard-mutation completeness — so
**Major**, not Blocker, and not gating on its own.

### Other verdict-scope items, confirmed clean

- **Fail-closed / exit-code contract**: `--help` exits 0 and documents both
  `--all` and `--base`; an unknown flag and `--all --base` combined both
  exit 2; every non-comment `>&2` write carries `|| true` — unchanged by
  this diff, re-confirmed live.
- **Self-reference**: `bash bin/check-pii-shapes.sh --all` on this tree at
  HEAD exits 0 (clean) in the shipped default mode — no shipped file,
  including the checker itself, the two doc mirrors, or the spec, becomes
  its own finding. (With `PII_CHECK_TRACKER_KEY=1` the checker's own
  design-point label vocabulary — `DP-1`, `AC-1`, etc. — does fire
  copiously; this is DP-1's own declared, accepted-noise reason the rule
  ships off by default, not a defect.)
- **Doc/`ja` semantic equivalence**: read `docs/pii-controls.md` and
  `docs/pii-controls.ja.md`'s diffs cell by cell — the two new
  shape-catalogue bullets, the opt-in usage example, and the three new
  "What this gate does not cover" bullets each carry the same content in
  both languages; no asymmetry found.
- **Named-entity claim, reconciled truthfully**: the new bullet in both
  documents ("no pattern naming a specific customer, host or project ships
  in this repository") is true of the two shipped patterns — both match a
  generic outline, neither hard-codes a name — and the two original
  whole-line-locked named-entity bullets survive byte-for-byte in both
  languages.
- **Opt-in bidirectional load-bearing, no coupling**: confirmed by direct
  code reading (see the Adversarial findings section below) that
  `host-local`'s call is unconditional and `tracker-key`'s is the only call
  gated by `PII_CHECK_TRACKER_KEY`, so the opt-in cannot leak into or
  couple with any other pattern id.

### Findings

#### Blocker

- **`bin/check-pii-shapes.sh` (`RE_TRACKER_KEY`, the `report_pattern_lines
  tracker-key` call site) — missing left-word-boundary defeats the declared,
  load-bearing digit-bearing-namespace exclusion (DP-2, AC5, Out-of-scope
  item 3).** `RE_TRACKER_KEY="${RE_TRACKER_KEY_NAMESPACE}-[0-9]+"` carries no
  left anchor at all (unlike every other composed rule in this file, e.g.
  `RE_HOME_PATH_BOUNDARY='(^|[^A-Za-z0-9.-])'`). Confirmed live against the
  real, unmodified shipped checker:
  ```
  $ git init -q repo && cd repo && git config user.email a@b.c && git config user.name x
  $ printf 'note: A1BC-2 is just a line-range style reference, not a tracker key\n' > f.md
  $ git add f.md && git commit -q -m init
  $ PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all
  FINDING pattern=tracker-key path=f.md line=1
  ```
  `A1BC-2` has a digit-bearing namespace (`A1BC`) — Out-of-scope item 3 and
  DP-2's table both declare this class must stay silent by construction
  ("36 measured line-range and label references... excluded by the outline
  itself"), and it does not: `grep -oE '[A-Z]{2,10}-[0-9]+'` on `A1BC-2`
  returns the substring `BC-2`, because nothing requires the match to start
  at the beginning of the alnum run. The population analysis in `## Blast
  radius` that grounds this whole design decision (DP-1, DP-2) was itself
  measured with `bin/derive-populations.sh`'s **word-boundary-anchored**
  PCRE patterns (`-P '\b[A-Z]{2,10}-[0-9]+\b'`), but the shipped detection
  regex carries no equivalent boundary — so the approved 48/84/36-token
  population counts do not describe what the shipped rule will actually match
  in the field. I did not find any of the 36 currently-tracked digit-bearing
  tokens that happen to trigger this specific bypass today (all end directly
  in a digit before the hyphen, e.g. `AC4-13`, `L194-199`) — this is
  incidental to the current corpus's exact spelling, not a property of the
  regex, and the very next digit-bearing token an adopter's tree acquires
  could trip it. Given tracker-key is the pre-commitment's own
  `droppable-1st` component, this is exactly the class of defeat that
  disposition anticipates — but it is round 1, so the two-consecutive-round
  factual-trigger has not fired; I'm reporting it as a Blocker on its own
  substance rather than invoking that disposition myself.
  - **Suggested fix**: add an explicit left-boundary requirement to
    `RE_TRACKER_KEY` (or to `RE_TRACKER_KEY_NAMESPACE`), mirroring
    `RE_HOME_PATH_BOUNDARY`'s `(^|[^A-Za-z0-9.-])` convention, e.g.
    `RE_TRACKER_KEY="(^|[^A-Za-z0-9])${RE_TRACKER_KEY_NAMESPACE}-[0-9]+"` (and
    account for the boundary consuming a character in
    `report_pattern_lines`'s line-number-only reporting, which does not
    depend on match offset, so this is safe to add without touching the
    reporting path).

#### Major

- **`bin/check-pii-shapes.sh` (`RE_TRACKER_KEY`) — missing right-word-boundary
  after the digit run produces false positives outside the declared shape.**
  `AB-123abc` reports `AB-123` even though the full token continues past the
  digit run — not a genuine tracker key by the shape AC1 declares ("an
  upper-case letters-only namespace..., a hyphen, and a digit run"), and not
  covered by the declared accepted-noise class either (that class is
  specifically "standards identifiers of the same outline", not arbitrary
  compound tokens that merely contain a tracker-key-shaped prefix). Same root
  cause as the Blocker above (no boundary anchoring at all on this rule,
  neither side) but a distinct manifestation not covered by either named
  exclusion. Confirmed via direct reproduction:
  ```
  $ printf 'note AB-123abc trailing\n' | grep -oE -- '[A-Z]{2,10}-[0-9]+'
  AB-123
  ```
  The trailing `abc` is simply left unconsumed by the pattern rather than
  disqualifying the match, so the substring `AB-123` is still reported. The
  same missing-anchor root cause also lets an over-length (>10-letter)
  namespace still fire via a truncated substring match — `ABCDEFGHIJK-1`
  (11-letter namespace) reports as `BCDEFGHIJK-1` rather than being excluded
  for exceeding the declared 2-10 character range; AC14's own family test
  only exercises the long end at exactly 10 letters, never 11+, so the suite
  does not catch this.
  - **Suggested fix**: same left-boundary fix as the Blocker, plus a
    right-boundary requirement after the digit run (end-of-line or a
    non-alnum character), matching the shipped `RE_HOME_PATH`-style
    two-sided boundary convention.

- **`bin/check-pii-shapes.sh` (`RE_HOST_LOCAL`) — the right-side boundary
  treats a hyphen as a valid terminator, so `host.local-suffix` fires as a
  `host-local` finding, and this is now inconsistent with the just-repaired
  AC11 audit line (see the class-M item above), which treats the identical
  shape as exempt.** Confirmed live: `grep -oE --
  '[A-Za-z0-9][A-Za-z0-9-]*\.local($|[^A-Za-z0-9.])' <<< 'build.local-host
  end'` → matches `build.local-`. Neither DP-2's table nor the Non-goals
  section takes a position on a hyphen-continued token one way or the other,
  so this is an unresolved design gap rather than a clear regression, but the
  two artifacts (the shipped rule and its own spec-level audit of itself)
  now disagree on the same case, which should not stand unresolved.
  - **Suggested fix**: decide explicitly whether a hyphen-continuation is a
    genuine host-name mention (fire) or a compound-token lookalike (exempt,
    like the dot/letter/digit continuations already are), then make
    `RE_HOST_LOCAL`'s right-side class and AC11's audit-line filter agree.

#### Minor / nits

- **`bin/check-pii-shapes.sh` (`RE_HOST_LOCAL_OUTLINE`/`RE_TRACKER_KEY_NAMESPACE`)
  — neither new rule has a left-word-boundary, so a match can start
  mid-token** (`foo_bar.local` reports on the substring `bar.local`, ignoring
  the `foo_` prefix — confirmed: `grep -oE --
  '[A-Za-z0-9][A-Za-z0-9-]*\.local($|[^A-Za-z0-9.])' <<< 'foo_bar.local end'`
  → `bar.local `). This does not defeat host-local's own named exclusion
  (the config-file class is excluded on the *right* side, unaffected), so it
  stays Minor for host-local — the line still correctly gets flagged, just
  with a narrower reported substring than the whole token. (Its tracker-key
  counterpart is folded into the Blocker/Major above, since there it does
  defeat a named exclusion.)
- **`bin/check-pii-shapes.sh` (`RE_HOST_LOCAL`) — case-sensitive match misses
  an upper/mixed-case `.LOCAL`/`.Local` suffix** (`report_pattern_lines` uses
  plain `grep -nE`, no `-i`; confirmed `HOST.LOCAL` produces no match).
  Real-world mDNS/.local suffixes are conventionally emitted lowercase by the
  OS/network stack (unlike the host-name stem, which is often mixed case),
  and every other rule in this checker is similarly case-sensitive by design
  (matching realistic spelling rather than every DNS-legal casing), so I
  judge this lower-priority than Codex's own "high"/"P2" framing — a real
  but low-reachability gap, not inconsistent with the file's prevailing
  convention. Fast-follow candidate.
- **`tests/check-pii-shapes/run.sh` (AC7 opt-in-isolation assertion, around
  line 1023) — only checks that `home-path`'s finding count is unchanged by
  the opt-in, on a fixture that carries no `host-local` content.** A
  `PII_CHECK_TRACKER_KEY` scope bug that happened to affect `host-local`
  specifically would not be caught by this assertion. Test-completeness gap,
  not a detection defect. Fast-follow candidate.

#### Adversarial findings

Both Codex passes agreed on: the tracker-key left-boundary bypass (headline
Blocker above); the host-local hyphen-continuation ambiguity (Major above);
host-local case-sensitivity (Minor above, severity downgraded on my own
review); and the tracker-key overlong-namespace truncation and
missing-right-boundary symptoms (folded into the Major above). The
adversarial pass additionally raised the AC7 opt-in-isolation test-coverage
gap (Minor above) and — like the primary pass — flagged the `assert_meta_fails`
call sites as "vacuous" (rejected, see ledger below).

I additionally verified, by reading `scan_content_file()` directly rather
than relying on either pass's say-so, that the opt-in mechanism cannot leak
into or couple with any other pattern id: `host-local`'s
`report_pattern_lines` call is unconditional, and `tracker-key`'s is the only
call gated by `PII_CHECK_TRACKER_KEY`; enabling the opt-in adds exactly one
additional call and touches no other rule's code path.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `RE_TRACKER_KEY` unanchored substring defeats digit-bearing-namespace exclusion | primary: Major (P2) — adversarial: confirmed, medium | Blocker | live-reproduced against the real shipped checker; defeats a declared, load-bearing exclusion (DP-2/AC5), and the population analysis that grounds the whole tracker-key design was measured with `\b`-anchored patterns the shipped regex does not carry |
| 2 | `RE_TRACKER_KEY` missing right-boundary after digit run | adversarial: medium | Major | live-reproduced (`AB-123abc` → `AB-123`); same root cause as #1, distinct manifestation not covered by the declared accepted-noise class |
| 3 | `RE_TRACKER_KEY` overlong namespace still fires (truncated) | adversarial: medium | folded into #2 (Major) | same root cause (no left boundary); live-reproduced (`ABCDEFGHIJK-1` → `BCDEFGHIJK-1`) |
| 4 | `RE_HOST_LOCAL` right-side boundary admits hyphen continuation | primary: P2 — adversarial: medium | Major | live-reproduced; now provably inconsistent with the class-M-repaired AC11 audit line for the identical shape (my own manual finding, tied to the mandatory class-M item) |
| 5 | AC11 residual-filter (class-M v2) over-exempts hyphen continuations relative to the real `RE_HOST_LOCAL` | (my own manual finding, not raised by either Codex pass) | Major, Tier 2 | mandatory class-M verification item; real instrument defect (Tier 2 carve-out keeps this in scope), backstopped in practice by AC6 today |
| 6 | `RE_HOST_LOCAL_OUTLINE` missing left-word-boundary (embedded-substring reporting) | adversarial: medium | Minor | does not defeat host-local's own named (right-side) exclusion; reported substring is narrower than the full token but the line is still correctly flagged |
| 7 | `RE_HOST_LOCAL` case-sensitive, misses `.LOCAL`/`.Local` | primary: P2 — adversarial: high | Minor (downgraded from Major/high) | real but low-reachability (mDNS `.local` suffix conventionally lowercase); consistent with every other rule in this file being case-sensitive-by-design rather than case-exhaustive |
| 8 | AC7 opt-in-isolation test only exercises `home-path`, not `host-local` | adversarial: low | Minor | test-coverage gap, not a detection defect; confirmed by code reading that the opt-in cannot structurally couple to other ids regardless |
| 9 | `assert_meta_fails` call sites for `host-local`/`tracker-key` pass a near-miss-negative fixture rather than a neutralised-checker + positive-fixture pairing, "vacuous" | primary: P2 — adversarial: low | **rejected** | read `assert_meta_fails()`'s own definition and comment block (`tests/check-pii-shapes/run.sh:1340-1364`): this is the pre-existing, correct, established convention — every one of the seven shipped patterns' call sites (`home-path`, `home-path-win`, `email-nonnoreply`, `private-key`, `token`, `home-encoded`, `temp-session`) uses the identical `_NEG_REPO`/`_NEG_BASE` pairing, unrelated to and complementary with the separate mutation-lock duty (`assert_neutralised_pattern_unreported`), which *does* use a neutralised-checker + positive-fixture pairing. Both Codex passes conflated the two duties; the new patterns correctly participate in both. |

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1140
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 12 files, +1528/-6 lines

#### Blockers
- `bin/check-pii-shapes.sh` `RE_TRACKER_KEY` has no left-word-boundary and
  defeats the declared digit-bearing-namespace exclusion (DP-2/AC5) —
  live-reproduced, `A1BC-2` reports as `tracker-key`. See Findings above for
  full detail and suggested fix.

#### Major
- `RE_TRACKER_KEY` missing right-boundary after the digit run and missing
  left-boundary on overlong namespaces (folded, same root cause as the
  Blocker) — false positives outside the declared shape.
- `RE_HOST_LOCAL`'s right-side boundary admits a hyphen continuation
  (`host.local-suffix` fires), now provably inconsistent with the
  class-M-repaired AC11 audit line for the identical shape.
- The class-M re-freeze's repaired AC11 filter (`\.local[.A-Za-z0-9-]`) is
  not meaning-preserving against the real `RE_HOST_LOCAL` boundary for the
  hyphen-continuation case (Tier 2, backstopped today by AC6).

#### Minor / nits
- `RE_HOST_LOCAL_OUTLINE`/tracker-key's namespace lack a left-word-boundary
  in general (embedded-substring reporting on host-local; folded into the
  Major above for tracker-key since there it defeats a named exclusion).
- `RE_HOST_LOCAL` is case-sensitive, missing `.LOCAL`/`.Local`.
- AC7's opt-in-isolation test only exercises `home-path`, not `host-local`.

#### Adversarial findings
See `### Findings` → `#### Adversarial findings` above.

#### Synthesis audit ledger
See the ledger above.

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings (the
  Blocker and the two Majors on `bin/check-pii-shapes.sh` itself).

#### Fast-follow disposition
- Host-local case-sensitivity gap (`.LOCAL`/`.Local` not matched) —
  won't-fix-for-this-round / file-an-issue: low reachability, consistent with
  the file's prevailing case-sensitive-by-design convention; worth a small
  follow-up rather than blocking this merge once the Blocker/Major items
  above are fixed.
- AC7 opt-in-isolation test coverage gap (only checks `home-path`) —
  file-an-issue: cheap to broaden once the Blocker/Major regex fixes land
  (the fixture set will need to be touched anyway).
- Embedded-substring reporting on `host-local` (missing left-word-boundary,
  narrower-than-whole-token reported substring) — won't-fix: does not defeat
  the named exclusion and the line is still correctly flagged; low value
  relative to the anchoring fixes already required above.

- executor-invocation (codex-primary): codex exec --sandbox read-only --cd <repo-root> review --base develop --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1140-codex-primary.out.5CXtTG
- pass-role (codex-primary): generation
- briefing-fidelity (codex-primary): not-applicable — the `review` subcommand is a fixed built-in review flow with no prompt-injection channel; this round's specific briefing (verdict scope, class-M mandatory item) was not and could not be carried into its context, though its own default scope (correctness against the spec, edge cases, design smells) covers overlapping ground.
- raw-capture (codex-primary): T-1140-codex-primary
- self-detected-host (codex-primary): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass

- executor-invocation (codex-adversarial): codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1140-codex-adversarial.out.xjzVAw "Review the diff between develop and HEAD in this repo (T-1140: two new PII-shape pattern ids, host-local and tracker-key, in bin/check-pii-shapes.sh). Read the files yourself with git diff / nl -ba — do not expect any diff in this prompt. Play devil's advocate specifically on bin/check-pii-shapes.sh's two new regex definitions and scan_content_file()'s two new report_pattern_lines call sites, given a prior pass's finding that RE_TRACKER_KEY's missing left-word-boundary defeats the digit-bearing-namespace exclusion; look for other anchoring bypasses, opt-in leakage into other pattern ids, and whether the new mutation-lock/meta-assertion tests actually exercise their labels' claims. Return findings as a JSON array of {severity, file, line, issue, suggestion}."
- pass-role (codex-adversarial): generation
- briefing-fidelity (codex-adversarial): carried — the round's specific mandate (the primary pass's own finding, plus the class-M item's anchoring/bypass focus and the verdict scope's opt-in-isolation and test-vacuity checks) was written directly into this pass's prompt.
- raw-capture (codex-adversarial): T-1140-codex-adversarial
- self-detected-host (codex-adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before launching the pass
