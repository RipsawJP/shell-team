### Codex Review verdict: REQUEST_CHANGES
- Task: T-1046
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: static-only
- Diff scope: `git diff --stat d825293...HEAD` — 10 files, +898/-12 lines. Single freeze v1 (`af871096439f0011979b9bb5e72f44cc6534ef40`), no re-freeze; independently confirmed via `bash bin/check-intent.sh` (`aligned: T-1046 v1`, matching hash exactly).

Two `codex exec` passes were run against the real working tree (never pasted): a primary `review --base d825293 --json` pass, and — triggered by that pass's Majors, per this role's step-5 rule — an adversarial pass explicitly scoped to the acquisition layer, since this task's own pre-commitment gives that layer a one-round trigger. Every finding below was independently reproduced live in a scratch fixture before being kept (four separate repros, listed inline).

## Mandatory items, checked directly
- **(a) Every channel read is on the spec's admissible list; no human-readable diagnostic is read to assert anything.** Confirmed by direct grep against `bin/team-init.sh` (not just trusting AC4's own run): `2>&1`, `not a git repository`, `literalPathspecs`, `literal-pathspecs`, `:(literal)`, `GIT_LITERAL_PATHSPECS=1`, `--stdin`, `--no-index`, `ls-files`, `--porcelain` all occur **zero** times; `check-ignore`, `rev-parse`, `LC_ALL=C`, `env -u GIT_DIR` all occur at least once. Only two channels are read, matching the spec's table exactly.
- **(b) Faithful transcription of DT0-DT8, every row reachable or justified.** Read `report_ignored_base_verdict()` line by line against the frozen table: `DT0` (`command -v git` guard) → `DT4` (`rev-parse` nonzero → silence) all match the table's control flow exactly, including the two documented-unreachable rows (`DT3`, `DT8`) being explicit arms rather than a default fall-through. `bash bin/check-acs.sh`'s `AC19` (nine-row/nine-id correspondence) and `AC4` (static admissibility) both re-run live: PASS. The `DT0`/`DT3`/`DT8` disclosure comment in `tests/team-init/run.sh` read directly — sound, matches QA's characterization.
- **(c) Eleven fixtures exist; at least two mutation self-checks reproduce independently.** All eleven `LX01`-`LX11` confirmed present and passing (`bash tests/team-init/run.sh`, live). Two mutation self-checks reproduced independently by this reviewer, not read off the provenance record:
  - **`LX01`** (query normalization): reverted the `check-ignore` pathspec from `"./${TEAM_RUN_BASE%/}/"` to a bare `"$TEAM_RUN_BASE"` in a scratch copy — `TEAM_RUN_BASE='a*'` beside a tracked glob-colliding `abc/file.txt` produced **zero** `WARN:` lines against the mutated copy (red), and **eight** (the full `V1` body) against the real, unmutated file for the identical fixture (green).
  - **`LX09`** (rev-parse `true`/`false` contract): swapped the `"true"`/`"false"` string literals in `DT1`'s and `DT2`'s conditions in a scratch copy — a normal, non-bare ignored work tree produced **zero** `WARN:` lines against the mutated copy (red, because `DT1` no longer matched the real "true" answer), and **eight** against the real file for the identical fixture (green).
- **(d) `bin/team-paths.sh` byte-identity to base.** `git diff --quiet d825293 HEAD -- bin/team-paths.sh` reports identical — confirmed directly, not inferred from the diff-stat's silence.
- **(e) The advisory-not-fail-closed argument is re-made in this spec, not inherited.** D2 read in full: it re-derives the ruling from `docs/adopting.md`'s "ignoring the base dir is the adopter's call" language and states the applicable fail-closed form is `DT7`/`DT8` (undeterminable, never silently "not ignored") — a real, spec-local argument rather than a citation of T-1042's D2. No finding.
- **(f) The allow-list names only paths that can exist.** AC13's own body states this explicitly and its case-pattern lists no `-codex-adversarial.md`-shaped path — confirmed by reading AC13 directly. Closed by construction, as claimed.
- **(g) Board diff structural purity vs `d825293`.** `git diff d825293...HEAD -- .shell-team/todo.md` is a pure addition of one new `## Active` entry; the T-1042 Done entry is untouched context. No finding.

## Findings

### 1. `V1`'s committability claim is false for a previously force-added, already-tracked file
- **File**: `bin/team-init.sh` (the `emit_ignored_base_v1` body, transcribing the spec's frozen `V1`) / `.shell-team/specs/T-1046-ignored-base-verdict.md` D4(ii)
- **Acquisition layer?** No — this is a message-wording defect. The channel read (`check-ignore` exit 0) and its classification (`DT5` → IGNORED) are both correct; the frozen *text* asserted for that classification overclaims.
- D4(ii) argues `V1` "stays true even when the base dir has been force-added," reasoning that the message only reports "what git answered." Reading `V1`'s actual body shows this is only true of its first line (`git reports the resolved base dir as ignored: <base>`); the next four lines assert a consequence — "everything the loop writes under it ... **cannot be committed** ... will report success **having committed nothing**." Live repro: `git init` a fixture, ignore `.shell-team/`, force-add `.shell-team/todo.md`, modify it, then `git add` it with **no** `-f` — the add succeeds (git prints the "ignored" advisory but the file **is** staged, confirmed via `git status --short` showing a staged `M`), and `git commit` succeeds, landing both the original and the modified content in `HEAD`. So for that specific already-tracked file, the loop's commit step does **not** "report success having committed nothing" — it genuinely commits it. `V1`'s blanket "everything ... cannot be committed" is false in this reachable state.
- **Severity: Major.** Real, live-verified, and the same *class* of defect as T-1042's M4 (a frozen message asserting something false in a specific reachable state) — but narrower in practical blast radius, since it requires an adopter to have deliberately force-added a file under an otherwise-ignored directory, which is not the documented remedy (`docs/adopting.md` documents a repo-level `!<base>/` re-include, not per-file force-adds).
- **Suggested fix**: either narrow the wording to "new records the loop writes there" rather than "everything the loop writes," or drop the committability prediction from `V1` entirely and keep only the first line's factual claim ("git reports the resolved base dir as ignored") — which is what D4(ii) says the reword already did, but the shipped text does not.

### 2. `docs/adopting.md` (and its `.ja` mirror) describes `V2` firing for a state that actually routes to silence
- **File**: `docs/adopting.md:64-66` (and `docs/adopting.ja.md`'s mirror paragraph)
- **Acquisition layer?** No — a documentation-accuracy defect. The classification itself (`DT4`: nonzero `rev-parse` → SILENCE) is correct and matches the frozen table exactly; the *documentation describing* that classification is wrong.
- The document states: "If git itself can't give a documented ignored/not-ignored answer (a corrupt or unreachable repository, for instance), `team-init` says so rather than guessing." A corrupt-or-unreachable repository is exactly the state a nonzero `rev-parse --is-inside-work-tree` exit reports — and per `DT4`, that state routes to **SILENCE**, not to `V2`. Live repro: a stubbed `git` on `PATH` that fails `rev-parse` with a fatal, non-"not a git repository" message (simulating a corrupt/unreachable repository) — `bash bin/team-init.sh` against that stub produces **zero** `WARN:` lines. The document promises a notice for exactly the scenario the implementation deliberately silences.
- **Severity: Major.** A user-facing document making a false claim about the tool's own behaviour is a real defect (an adopter reading this text and trusting a notice that will not fire), though — like finding 1 — it does not affect the channel-reading mechanism itself.
- **Suggested fix**: reword the sentence to name the actual `V2`-triggering condition (`check-ignore` itself failing with an unexpected exit status while inside a genuine work tree — a narrower, rarer state than "corrupt or unreachable repository"), or state plainly that a corrupt/unreachable repository is silent rather than reported, matching `DT4`.

### 3. The frozen twelve-variable neutralization list omits `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_<n>`/`GIT_CONFIG_VALUE_<n>`, letting the ambient environment inject an arbitrary `core.excludesFile` and flip the verdict
- **File**: `bin/team-init.sh`'s `git_probe()` / `.shell-team/specs/T-1046-ignored-base-verdict.md`'s "Neutralized environment" invocation constraint (D8)
- **Acquisition layer? YES.** This is exactly the invocation-constraints half of the acquisition layer the spec's own pre-commitment gives a one-round trigger.
- `git-config(1)`'s ENVIRONMENT section documents `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_<n>` and `GIT_CONFIG_VALUE_<n>` as a way to inject arbitrary configuration pairs — including `core.excludesFile` — "for cases where you want to spawn multiple git commands with a common configuration but cannot depend on a configuration file"; these override values in configuration files. None of the three is in `git_probe()`'s `env -u` list (nor in the spec's own frozen twelve-variable list). Live repro, with the repo's own `core.excludesFile` config **correctly persisted to `/dev/null`** and a clean, non-ignored fixture confirmed silent as a baseline: exporting `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.excludesFile GIT_CONFIG_VALUE_0=<a hostile excludes file naming the base dir>` into `bin/team-init.sh`'s environment — with **zero** change to the repo's own config, `.gitignore`, or any file on disk — turns the same, otherwise-silent fixture into a full `V1` firing (8 `WARN:` lines). The ambient environment alone flips the verdict, through a channel the spec's own admissibility framing never considered: not a diagnostic string, not a merged stream, not an unnormalized pathspec — a config-injection mechanism sitting one level below the channel the emitter reads.
- The adversarial pass separately named `GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, `GIT_CEILING_DIRECTORIES`, `GIT_DISCOVERY_ACROSS_FILESYSTEM`, `GIT_CONFIG_NOSYSTEM` and `GIT_FLUSH` as further gaps in the same list; these were not independently re-verified by this reviewer (time-bounded), but the one verified instance is sufficient to establish the class.
- **Severity: Major**, and — because it sits on the acquisition layer — **this fires the spec's own one-round same-class-2 pre-commitment on this, the first cross-provider round.**
- **Not grounded in the spec's own Input Space out-of-scope list**: the Input Space's "ambient environment" bullet enumerates exactly the twelve frozen variables plus locale — it does not affirmatively declare `GIT_CONFIG_*`/`GIT_COMMON_DIR`/etc. out of scope, it simply omits them from the enumeration. Per this role's own severity-grounding rule, a downgrade requires a *cited* out-of-scope line; there is none to cite here, so the finding keeps its Major severity rather than being downgraded for falling outside an incomplete list.

## Same-class-2 pre-commitment — ACQUISITION-LAYER FACTUAL TRIGGER FIRED, round 1
Per the spec's `## Same-class-2 pre-commitment`: "a **single** cross-provider round producing a new Blocker or Major against the verdict-acquisition layer fires this contract." Finding 3 is exactly such a finding, on the very first cross-provider round this task has had. The three dispositions are presented here with their content, per the contract's own text, rather than as a request for an incremental patch:

1. **(a) Ship only `DT5`'s narrow verdict, delete the classification layer.** This would **not** close finding 3 on its own: `check-ignore`'s own read of `core.excludesFile` is exactly what the `GIT_CONFIG_KEY_<n>` injection manipulates, independent of whether `rev-parse`/`DT1`-`DT4`/`V2` exist at all. Keeping `DT5` alone still needs the neutralization list widened.
2. **(b) Answer #167 with documentation, ship no probe at all.** This is the only disposition that closes finding 3 completely, because it removes the attack surface (there is no probe left to manipulate). Cost, as the spec's own text already states: silence for an adopter who does not read the document.
3. **(c) Move the check into an opt-in, fail-closed `bin/check-ignored-base.sh`.** Does not close finding 3 by itself — the checker would read the identical two channels and inherit the identical gap — but a *checker*, unlike an advisory, is the natural place to make "cannot trust the ambient environment enough to answer" a first-class exit-2 (undeterminable) outcome by design, which is arguably the more defensible long-term shape for this specific vulnerability class (an environment-injection attack against git's own configuration is a different threat model than a merged stream or an unnormalized pathspec, and a checker's fail-closed contract handles "I don't trust my inputs" more naturally than an advisory's silence-or-warn contract does).

None of the three, taken as originally worded, closes finding 3 without an additional change (widening the neutralization list at minimum, in dispositions (a) and (c)). This reviewer's read: whichever disposition is chosen, the neutralization list itself needs to grow to at least cover `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_<n>`/`GIT_CONFIG_VALUE_<n>` (and the adversarial pass's other named candidates, independently verified before being added) — but the choice among (a)/(b)/(c) is pm-spec's/the coordinating session's to make, not this role's.

## Blockers
None.

## Major
- Finding 1 (V1's force-added-tracked-file overclaim) — not acquisition layer, house two-round default.
- Finding 2 (docs/adopting.md's DT4-vs-V2 mismatch) — not acquisition layer, house two-round default.
- Finding 3 (GIT_CONFIG_* environment-injection gap) — **acquisition layer, one-round threshold fired.**

## Minor / nits
None beyond the mandatory-items section's clean results.

## Adversarial findings
A `codex exec` pass framed as "what else could defeat the acquisition layer specifically" on `bin/team-init.sh`'s `git_probe()`/`report_ignored_base_verdict()` returned one finding (tagged "Major" natively, `acquisition_layer: true`) — the `GIT_CONFIG_*` gap above, independently reproduced live by this reviewer before being kept. It also named several further candidate gaps (`GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, `GIT_CEILING_DIRECTORIES`, `GIT_DISCOVERY_ACROSS_FILESYSTEM`, `GIT_CONFIG_NOSYSTEM`, `GIT_FLUSH`) that this reviewer did not independently verify and is therefore not keeping as separate findings — they are named here as candidates for whoever closes finding 3, not as accepted findings in their own right. Full raw output: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

## Synthesis audit ledger
no severity changes

## Recommendation
- REQUEST_CHANGES → leave `.shell-team/todo.md`'s T-1046 entry at `READY_FOR_REVIEW`; route to `READY_FOR_ENG` with findings 1 and 2 (ordinary rework, no re-freeze needed — both are implementation/documentation text changes, not frozen-table or frozen-message-body changes... **except** finding 1's fix, if it changes `V1`'s frozen wording, would need a class-B re-freeze, exactly as T-1042's M4 did) and finding 3's fired acquisition-layer pre-commitment presented above for a disposition decision before any patch round begins.

## Fast-follow disposition
- The adversarial pass's unverified candidate gaps (`GIT_COMMON_DIR`, `GIT_OBJECT_DIRECTORY`, `GIT_CEILING_DIRECTORIES`, `GIT_DISCOVERY_ACROSS_FILESYSTEM`, `GIT_CONFIG_NOSYSTEM`, `GIT_FLUSH`) — file-an-issue: each needs its own live verification before being trusted as a real gap (this reviewer verified only `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_<n>`/`GIT_CONFIG_VALUE_<n>`), but they are cheap to check and belong in the same follow-up as finding 3's fix rather than being lost.

---

### Codex Review verdict: APPROVE — round 2
- Task: T-1046
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: `git diff --stat d825293...HEAD` — 11 files, +1115/-12 lines. HEAD `e5e4416`, base `d825293` (unchanged from round 1). Re-freeze v1→v2 (`cf7e8b2c9dbafe523eaa779079f505b8a089b5c7`), class-b per `bin/check-refreeze-class.sh`'s own classification rule — independently confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` (`aligned: T-1046 v2`, hash exact match).

**Context this round is scored against**: round 1 (`.shell-team/reviews/T-1046.md`, above) returned three Majors, one of them (finding 3) landing on the verdict-acquisition layer whose one-round budget v1 had frozen — firing the spec's own same-class-2 pre-commitment on the first round it ever had. The coordinating session ruled disposition (a) — ship only `DT5`'s narrow verdict, delete the classification layer, `V2` and the `rev-parse` channel — paired with replacing the `env -u` blacklist by an `env -i` allow-list (`NEUTSET`: `PATH`, `HOME`, `TMPDIR`, `LC_ALL=C`). This re-spec's own text records the acquisition layer's budget as **zero rounds remaining**: a new Blocker or Major against it here does not get another patch round — the coordinating session executes the documentation-only fallback directly (`bin/team-init.sh` reverts to its base blob). This round's job is therefore to find that finding if it exists, not to soften it if it does.

One `codex exec` pass was run against the real working tree (`review --base d825293 --json`, primary path). It genuinely read the diff, the spec, `bin/team-init.sh`'s history across `84154ed`/`ed2ae0c`/`c305856`/`e5e4416`, and the frozen `NEUTSET`/`V1`/`DOC1`/`DOC1JA` blocks — but its attempt to build real `git init`-ed fixtures and run `tests/team-init/run.sh` failed with `Operation not permitted` (its own sandbox denies `mkdir` under both the repo tree and its `$TMPDIR`), so it returned a static-only verdict: "No actionable correctness issues were identified... the behavioral suite could not run because the read-only sandbox blocked fixture creation." Raw capture: `.shell-team/reviews/T-1046-codex-primary.{txt,jsonl}`. Because that pass raised no new Blocker or Major, the parallel adversarial pass's mechanical trigger (step 5) did not fire this round, and none was launched.

Given the acquisition layer's zero-round budget, I did not stop at that static, sandbox-degraded result. I independently executed the full behavioral surface myself, directly against the real working tree, under a genuinely writable sandbox (`$TMPDIR`-backed and scratchpad-backed fixtures) — the same discipline this role's instructions require ("run your passes against the real working tree... reproduce every finding live before keeping it"), substituting for codex's blocked fixture pass rather than accepting its degraded static read as the whole story:

## Mechanical gates re-run live, independently
- `bash tests/team-init/run.sh` — **all assertions pass**, including all thirteen T-1046 labels (`case: DT5 M1` … `case: D9 — a legitimate global core.excludesFile reached through HOME is still honoured`).
- `bash tests/errexit-safe/run.sh` — pass, including the `team-init-log-warn-guard` and `team-init-log-err-guard` protected-content locks.
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v2 (cf7e8b2c9dbafe523eaa779079f505b8a089b5c7)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — `conformant (19 decision entries, 0 sentinel)`.
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — `conformant (1 entries, 0 sentinel)`.
- `shellcheck bin/team-init.sh tests/team-init/run.sh tests/errexit-safe/run.sh` at the pinned `0.11.0` — clean.
- `bash bin/check-pii-shapes.sh --base d825293` — clean.
- `bash bin/check-handoff.sh .shell-team/todo.md` — clean.
- `git diff --name-only d825293` — exactly the 11 paths AC13 names (`bin/team-init.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `docs/adopting.md`, `docs/adopting.ja.md`, `.shell-team/todo.md`, this spec, provenance, interventions, reviews, `test-recipe.md`); `bin/team-paths.sh` and every other `bin/` script absent, confirming D6's one-emitter ruling mechanically.
- Direct grep on `bin/team-init.sh`: `rev-parse`, `is-inside-work-tree`, `env -u`, the literal `GIT_`, `2>&1`, `ls-files`, `--stdin`, `--no-index`, every explicit pathspec-magic form all occur **zero** times (the one apparent hit, `# shellcheck disable=SC2016 ... backticks ... literal text`, is a false match on the substring "literal" and names none of the retired mechanisms); `env -i` occurs exactly once; `check-ignore`, `LC_ALL=C` each occur at least once.

## Live behavioral reproduction of round 1's three findings, against the shipped v2
1. **Finding 1 (V1's force-added-tracked-file overclaim) — confirmed closed.** Built a fixture, force-added `.shell-team/todo.md` under an ignored base, modified it, then ran a plain `git add` (no `-f`): the file staged (`M` in `git status --short`) and committed successfully — landing both the force-add and the modification in `HEAD`. `V1`'s shipped text now reads "A file already tracked under it is unaffected," which is exactly what happened. The sentence a reviewer would have had to falsify last round is genuinely gone from the message, not narrowed around.
2. **Finding 2 (docs/adopting.md's DT4-vs-V2 mismatch) — confirmed closed.** Reproduced every silent state `DOC1`/`DOC1JA` now name — a genuine bare repository (`git init --bare`), a directory with no repository anywhere in its ancestry, a repository with a corrupt `.git` gitfile (`fatal: invalid gitfile format`, rc=128), and a repository whose `.git/HEAD` is missing outright (also rc=128, "not a git repository") — all four produced **zero** `WARN:` lines against the shipped `team-init.sh`, matching the frozen paragraph's own promise ("bare repository, a directory with no repository at all, and a repository git cannot read... stays silent rather than guessing") byte-for-byte on both `docs/adopting.md` and `docs/adopting.ja.md` (independently re-verified as a contiguous-line match against the frozen `DOC1`/`DOC1JA` blocks, not merely eyeballed).
3. **Finding 3 (the acquisition-layer `GIT_CONFIG_*` injection) — confirmed closed, and I could not defeat the allow-list by any other means.** Reproduced round 1's exact repro (`GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.excludesFile GIT_CONFIG_VALUE_0=<hostile file>`) and the coordinating session's `GIT_CONFIG_PARAMETERS` variant: both flip a bare `git check-ignore` in this runtime (positive control, rc 1→0) but produce **zero** anchor-line occurrences against `team-init.sh` under the identical injection. Independently re-derived QA's `XDG_CONFIG_HOME` finding with my own fixture (isolated `HOME`, hostile `$XDG_CONFIG_HOME/git/config`, no repo-local `core.excludesFile` pin so the global value is actually live) — flips bare git, closed by `team-init.sh`. Independently reproduced row E (`HOME` load-bearing): a legitimate global `core.excludesFile` reached through a scratch `HOME` fires `V1`; the identical fixture under an empty scratch `HOME` stays silent — both directions.

## Additional adversarial hunting against the acquisition layer (my own, beyond round 1's and QA's coverage)
- **`env -i` semantics, checked directly rather than assumed**: `env -i PATH=... FOO=bar /usr/bin/env` on this platform shows exactly the two named variables reach the child process — no ambient variable survives implicitly, and no bash-exported-function leak (`BASH_FUNC_*`) is possible since `env -i` clears the entire environment before applying the explicit list. The `git_probe()` one-liner's quoting (`env -i PATH="$PATH" HOME="${HOME:-}" TMPDIR="${TMPDIR:-/tmp}" LC_ALL=C git -C "$TARGET" "$@"`) is unremarkable: every assignment is quoted, `"$@"` is preserved correctly, and the existing suite's own "target path with a space is handled" case exercises the quoting under `$TARGET`.
- **The default (unconfigured) global excludes path, `$HOME/.config/git/ignore`** (git's XDG-default location when neither `core.excludesFile` nor `$XDG_CONFIG_HOME` is set): confirmed this is also read through `$HOME` alone. This is not a new gap — it is the same class row E already measures (a legitimate ignore source reached only through the deliberately-forwarded `HOME`), just via git's default path instead of an explicit `core.excludesFile` setting. Recorded here as corroboration, not as a finding.
- **Second-divergence hunt, beyond QA's corrupted-`.git/index` fixture**: tried a `.git/HEAD`-missing repository (rc=128, both `rev-parse` and `check-ignore` fail identically — no divergence from v1), a subdirectory of a larger work tree several levels deep (fires `V1` correctly), and a linked-worktree-shaped `.git` gitfile redirection (behaves identically to a normal work tree, since `check-ignore` resolves the same associated repository `rev-parse` would have). Found no state, reachable under v1's own table, where the deleted `rev-parse` gate would have produced a *different, better* answer than the single-channel `check-ignore` read produces now, beyond the one exception the spec already discloses and prices (a genuine work tree in which `check-ignore` itself fails).
- Considered and rejected as in-scope findings, each grounded in the spec's own text: a hostile `PATH` or `HOME` "controlling the verdict" — the Input space's Out-of-scope list names this directly ("A hostile `PATH` that resolves `git` to a substituted binary, or a hostile `HOME` whose `.gitconfig` deliberately misreports... forwarded by design"); `CDPATH` affecting the top-level `TARGET="$(cd "$TARGET" && pwd)"` resolution — that line is unchanged since `d825293` (confirmed via the diff), so it is not part of this task's diff and not part of "the channel, its invocation constraints, its classification" the acquisition layer names; an ambient `safe.directory` injection being dropped — explicitly named as an accepted consequence in the neutralization rule's point (ii).

## Findings
None. No Blocker, Major, Minor or nit against any surface this task touches.

## Blockers
None.

## Major
None. The acquisition layer's zero-round budget is not spent this round — it was never charged, because no new Blocker or Major was found against it.

## Minor / nits
None.

## Adversarial findings
No parallel Codex adversarial pass was launched this round (its mechanical trigger — a new Blocker/Major from the primary pass — did not fire). In its place, the live reproduction and hunting recorded above under "Additional adversarial hunting" was performed directly by this role against the real working tree, aimed specifically at the two angles the acquisition layer's zero-round budget makes highest-stakes: defeating the allow-list itself, and finding a second behavioral divergence beyond the one the spec discloses. Neither turned up a new gap.

## Synthesis audit ledger
no severity changes

## Environmentally-unverified issue-canon reliance
GitHub issue #167's body was not independently read by this role (no network/`gh` access in this sandbox, same limitation pm-spec and qa-verifier both disclose on this task): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- APPROVE → set `.shell-team/todo.md`'s T-1046 entry to `READY_FOR_MERGE`.


## Fast-follow disposition
no fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES — round 2 addendum (supersedes the "APPROVE — round 2" verdict above)
- Task: T-1046
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: unchanged from the round-2 header above — same HEAD (`e5e4416`), same base (`d825293`).

**Why this addendum exists.** The coordinator reviewed the round-2 record above and identified a structural gap I had disclosed but not closed: round 1's decisive finding (finding 3, the acquisition-layer trigger) came from Codex's *adversarial* pass, not its primary pass. In round 2, the primary pass was sandbox-degraded to a static, fixture-free read that returned nothing, so the adversarial pass's mechanical trigger (a Blocker/Major from the primary) never fired — and I substituted my own live behavioral testing for the missing cross-provider signal. That substitution was disclosed honestly, but it is not the same thing as the signal itself: a Claude-family reviewer re-verifying a Claude-family implementation is exactly the blind-spot-sharing risk this repository's cross-provider rule exists to prevent. The coordinator directed me to run the Codex adversarial pass explicitly this round, scoped to the verdict-acquisition layer, reasoning statically (no fixture required) about (1) what reaches `check-ignore` without passing through the process environment at all, and (2) whether `TMPDIR`'s unargued forwarding is actually inert. **Stated plainly, per the coordinator's request: the cross-provider adversarial signal WAS obtained this round, and it found something new.**

## What was run
One `codex exec` free-form pass (not the `review` subcommand — a directive prompt, per the fallback/adversarial path), scoped exactly as the coordinator specified: read `bin/team-init.sh`'s `git_probe()`/`report_ignored_base_verdict()`, the frozen `NEUTSET`/decision-table/neutralization-rule text, and reason from `git(1)`/`git-config(1)`/`gitignore(5)`/`git-check-ignore(1)` documentation about (1) non-environment channels and (2) `TMPDIR`'s forwarding, explicitly declining to re-litigate `PATH`/`HOME` (already priced into the spec's Input space). Raw capture: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

## Codex's findings (native labels, before normalization)
Codex returned a JSON array of three items, tagged with its own severity vocabulary — normalized per this role's fixed map (P1 → blocker, P3 → minor):

1. **[Codex: P1]** `bin/team-init.sh:254` — an ambient, exported Bash function named `env` intercepts `git_probe()`'s bare `env -i ...` call, because Bash's command lookup checks shell functions *before* `$PATH`, independent of what `$PATH` contains. An attacker (or an accidental wrapper/CI harness) needs only to `export -f env` before `team-init.sh` runs; the neutralization line never executes the real `env` binary at all.
2. **[Codex: P1]** `bin/team-init.sh:254` — `env -i` clears the process *environment* but has no effect on Git's file-based system-scope configuration (`$(prefix)/etc/gitconfig`, read unconditionally unless `GIT_CONFIG_NOSYSTEM` is set — which `env -i`'s clean slate does *not* set, since it isn't in `NEUTSET`). A `core.excludesFile` entry in that file flips `check-ignore`'s verdict independent of every allow-listed name.
3. **[Codex: P3]** `bin/team-init.sh:254` — `TMPDIR`'s forwarding: no documented route through `TMPDIR` to any git config/ignore mechanism was found; Codex could not prove it is verdict-changing, but also could not prove it inert against a future git version, since git gives no such guarantee.

## Independent live reproduction (this role, against the real working tree, before keeping either finding)

**Finding A (`env`-function shadowing) — reproduced, confirmed exploitable.** Built a fixture whose repo-local `.gitignore` holds only `build/` (i.e., genuinely *not* ignoring the base dir, `core.excludesFile` pinned to `/dev/null`) and confirmed the baseline is silent. Then wrote a scratch script that `export -f`'s a replacement `env` (one that simply drops the `-i`/assignment arguments and execs its target command with the **ambient, unsanitized environment**), exports a hostile `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.excludesFile GIT_CONFIG_VALUE_0=<hostile-file>` alongside it, and runs `bash bin/team-init.sh <fixture>` from inside that shell. Result: **`V1` fires in full** (the "resolved base dir as ignored" anchor line appears) against a fixture that is, by its own repo configuration, genuinely not ignored — the allow-list is not merely bypassed for one variable, it never executes at all, because the sanitization step itself was intercepted before `git` was ever reached. This requires nothing beyond the ability to set an environment variable ahead of `team-init.sh`'s invocation — the *identical* threat-model tier already established as in-scope by round 1's finding 3 (which also flipped the verdict via an ambient environment variable). The same root cause (Bash resolves shell functions before `$PATH` for every bare command word) would identically let an exported function named `git` bypass the mechanism even more directly, without touching `env` at all — not a separate finding, the same defect.

**Finding B (system-scope config) — reproduced, confirmed real, but at a different privilege tier.** Confirmed this machine has a live, currently-read system-scope config (`git config --system --list --show-origin` → `/opt/homebrew/etc/gitconfig`, owned and writable by the invoking user, not root) and that `env -i PATH=... git config --system --list` **still reads it** — `env -i` clears environment variables, not git's compiled-in system-config file path. Separately (without touching the real system file — that would mutate live host state outside this task's scope) proved via `GIT_CONFIG_SYSTEM=<scratch-file>` that a `core.excludesFile` entry at system scope is honored by `check-ignore` identically to repo-local or global scope (`rc=0`, ignored). Combining the two: anyone with write access to the *actual* system-scope config path — which, on a typical Homebrew-managed macOS dev machine (the exact platform this task was measured on), is the invoking user's own account, not an elevated one — can flip the verdict with zero dependency on any of the four allow-listed names. Unlike Finding A, this requires filesystem write access to a specific path rather than pure environment-variable control, and the spec's Input space already treats one class of "the operator's own configuration" (a legitimate global `core.excludesFile` reached through `HOME`) as an accepted, intentionally-honored source rather than an attack surface — but it names that reasoning only for `HOME`-reached global scope, never for system scope, and the Input space's four-source ignore-rule enumeration ("the repo's `.gitignore`, `.git/info/exclude`, a `core.excludesFile` (repo-local, or global reached through `HOME`...), and a re-include negation") omits system scope entirely. No fixture, AC or lock (unlike `HOME`'s row E / AC21) exercises this scope at all.

**Finding C (`TMPDIR`) — no route found, and I found none either.** No git mechanism consults `TMPDIR` for configuration or ignore-rule resolution to my knowledge, and I could not construct one. Treated as answered, not as an actionable finding.

## Severity determination (this role's synthesis, not a bare copy of Codex's tags)
- **Finding A → Blocker.** This is a live-verified, unconditional, complete defeat of the acquisition layer's central invariant (the Goal's own claim: "no ambient variable, named or unnamed, reaches git"), reachable at the *identical* privilege tier already established as in-scope (ambient environment-variable control — the same tier round 1's finding 3 used). It has no dependency on git's version, git's config system, or any `GIT_*` naming at all; it defeats the sanitization step itself, before git is ever invoked. I found no line in the spec's Input space or Non-goals that prices this in or excuses it, so per this role's own downgrade rule (a downgrade requires a cited out-of-scope line) it keeps the severity the evidence supports. This is, in my independent judgment, the most severe finding this task or its predecessor has drawn across every round — hence the first use of "Blocker" in this task's review history, not a label chosen to match Codex's native "P1" tag by default.
- **Finding B → Major.** Real, live-verified, and squarely on the acquisition layer (it bears on "the channel['s]" classification — which sources feed `check-ignore`'s exit status), but distinguishable from Finding A by requiring a materially different, filesystem-write privilege rather than pure environment-variable control, and partially adjacent to (though not identical to, and not explicitly extended by the spec's own text to) the already-accepted "operator's own configuration" reasoning used for `HOME`. Kept at Major rather than downgraded further because the spec's Input-space enumeration of ignore-rule sources does not name system scope at all, so there is no citable out-of-scope line to justify a deeper downgrade.
- **Finding C → no finding.** Recorded as answered, not scored.

## Synthesis audit ledger
| # | finding | primary / adversarial judgment | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding B (system-scope `core.excludesFile` bypass) | Codex-native "P1" (maps to blocker per the fixed normalization table) | Major | Exploiting it requires filesystem write access to the system-scope git config path — a materially stronger, narrower privilege than the pure environment-variable control that governs Finding A and round 1's finding 3 — and the spec's Input space never extends its "operator's own configuration" reasoning (used for `HOME`) to system scope one way or the other, so it is kept at Major rather than downgraded past it for lack of a citable out-of-scope line. |

Finding A's normalized value (blocker) and this role's final value (blocker) are identical, so it draws no ledger row. Finding C was not scored, so it draws no ledger row either.

## Blockers
- **Finding A** — `bin/team-init.sh:254`, `git_probe()`. An exported Bash function named `env` (or, identically, `git`) intercepts the sanitizing call before `$PATH` is ever consulted, so an ambient environment can defeat the entire `env -i` allow-list and flip the verdict via any variable it likes (demonstrated live with `GIT_CONFIG_COUNT`/`KEY_0`/`VALUE_0`, but the mechanism is generic — it does not depend on that specific injection). **Acquisition layer: YES — this fires the spec's own zero-round same-class-2 pre-commitment.** Suggested direction (not a decision this role makes): resolve the command through `command -p env -i ...` (or an absolute path) rather than a bare word, and the same for the `git` invocation inside it, so shell-function lookup cannot intercept either.

## Major
- **Finding B** — `bin/team-init.sh:254`. System-scope git configuration (`$(prefix)/etc/gitconfig`, read unconditionally by every `check-ignore` call this mechanism makes, `env -i` notwithstanding) can carry a `core.excludesFile` that flips the verdict independent of the four allow-listed names; the Input space's ignore-rule-source enumeration never names this scope. **Acquisition layer: YES.**

## Minor / nits
None new this addendum.

## Adversarial findings
This entire addendum *is* the requested adversarial pass's result — see "What was run" and "Codex's findings" above. Both kept findings were independently reproduced live by this role before being kept, per this role's own discipline; nothing here is a bare pass-through of Codex's raw output.

## Same-class-2 pre-commitment — fired again, automatic fallback, no menu
The spec's own text is explicit that the acquisition layer's budget is zero and that a further Blocker/Major does not reopen a choice: *"No further patch round is taken on this mechanism and no menu is presented a third time. The coordinating session executes the documentation-only fallback directly... `bin/team-init.sh` reverts to its base blob; `docs/adopting.md` and `docs/adopting.ja.md` keep a paragraph telling the adopter to run `git check-ignore -v -- ...` themselves..."* Unlike round 1 (which presented dispositions (a)/(b)/(c) for a decision), this round's contract is not a decision point — the specific fallback action is already fully specified in the spec's own `## Same-class-2 pre-commitment` section, and its execution is the coordinating session's to carry out, not this role's to choose among options for. I am reporting Finding A and Finding B at the severity the evidence supports, exactly as directed, and taking no position on softening that consequence.

## Recommendation
- **REQUEST_CHANGES** (this addendum supersedes the "APPROVE — round 2" verdict recorded earlier in this same round-2 section — do not act on that earlier APPROVE). Per the fired, zero-budget same-class-2 contract, the expected path is not a further patch round on this mechanism but the automatic documentation-only fallback the spec's own text already names.

## Fast-follow disposition
no fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES — round 3
- Task: T-1046
- Codex model: gpt-5.6-sol (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: `git diff --stat d825293...HEAD` — 7 files, +671/-0 lines. HEAD `d44323f`, base `d825293` (unchanged). Spec re-frozen v3→v4 (`cbe2003d9f4eb9a7423d6396cc1b2e22ddf92679`), **class-b**, a pre-review self-repair of `DOC2`/`DOC2JA`'s negated-pattern wording made *before* this round rather than in response to it — independently confirmed via `bash bin/check-intent.sh`: `aligned: T-1046 v4`.

**Context this round is scored against.** The verdict-acquisition mechanism is gone: `bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh` and `.shell-team/test-recipe.md` are all confirmed byte-identical to `d825293` (re-verified by this role with my own `cmp`, not trusted from the diff-stat). The acquisition layer's own zero-round contract fired at round 2 and is **closed, not carried** — a finding about an environment-injection channel, a config scope or shell-function shadowing has no target in this diff, and I raise none. What remains is `DOC2`/`DOC2JA` and the command they print, governed by a **separate** same-class-2 contract: house two-round default, **one round already consumed** (round 1's finding 2, against the predecessor `DOC1`/`DOC1JA`), **one round remaining**. This is the first cross-provider round to evaluate `DOC2`/`DOC2JA` (the v3/v4 text) on their own merits.

## What was run
Two `codex exec` passes against the real working tree, both foreground, neither pasted a diff or excerpt into the prompt:
1. **Primary** — `codex exec --sandbox read-only --cd <repo> review --base d825293 --json -o <file>`. It read the spec, the board, `docs/adopting.md`/`.ja.md`, and ran real `git check-ignore` probes of its own inside this checkout (not a scratch fixture) before returning two findings.
2. **Adversarial** — triggered by the primary pass's Major/`[P1]` finding, per step 5: a free-form `codex exec` pass framed as devil's-advocate against `DOC2`/`DOC2JA`/the printed command specifically, explicitly told to build real scratch fixtures and test empirically rather than reason from memory of gitignore syntax. It did — and it found the same defect the primary pass reasoned to, this time with a live positive control.

Every finding kept below was **independently reproduced by this role** in my own `$TMPDIR`-backed `git init` fixtures before being kept, per this role's discipline — including finding what the primary pass got right in principle but wrong in its specific evidence (below).

## Mechanical gates re-run live, independently
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1046-ignored-base-verdict.md` — **12/12 live PASS**, re-run fresh by this role, not trusted from a prior round's narration.
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v4 (cbe2003d9f4eb9a7423d6396cc1b2e22ddf92679)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — conformant (6 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — conformant (1 entry, 0 sentinel).
- `git diff --name-only d825293...HEAD` — exactly the seven-path allow-list (`docs/adopting.md`, `docs/adopting.ja.md`, `.shell-team/todo.md`, the spec, provenance, interventions, this review file); no `bin/`, test, workflow or template path.
- Byte-identity of all five files the fallback names, `cmp`-verified by this role directly against `git show d825293:<path>`, not inferred from the diff-stat's silence: `bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md` — all **IDENTICAL**.
- Zero-footprint sweep: `grep -rl -- 'check-ignore' bin .github` — no hits; `bin/check-ignored-base.sh` absent as both a regular path and a dangling symlink.

## Live reproduction: are `DOC2`/`DOC2JA` true?
Built my own `$TMPDIR`-backed `git init` fixtures for every configuration named in this round's brief, beyond what AC5 itself builds:
- Repo-level `.gitignore` rule ignoring the whole base dir → matched, printed correctly (`rc=0`).
- `.git/info/exclude` rule → matched, printed correctly (`rc=0`).
- Global `core.excludesFile` rule → matched, printed correctly (`rc=0`).
- Global rule **plus** a repo-level `!<base>/` re-include winning → **nothing printed, `rc=1`** — confirms the v4 repair (Assumption 4) is genuinely true, not merely asserted.
- Base dir absent on disk, matched only by a `.gitignore` line → still detected (`rc=0`), on-disk existence is irrelevant to the match.
- No repository anywhere in the ancestry → `fatal: not a git repository...`, `rc=128`, nothing on stdout.
- A genuine bare repository → `fatal: this operation must be run in a work tree`, `rc=128`, nothing on stdout.
- An unreadable `.git` (`chmod 000`) → also `fatal:`, `rc=128` — covers "git cannot read it" literally, not just by extrapolation.
- The substitution-free form (cell B) against the `team-paths.sh`-substituted form (cell A), same fixture → **byte-identical stdout**, confirming D12's equivalence claim.

Every one of the above matches `DOC2`/`DOC2JA`'s claims exactly. **One class was not on this list, and it breaks the document's central promise.**

## Findings

### 1. `DOC2`/`DOC2JA`'s "prints nothing → base dir not ignored, nothing to do" claim is false for an ordinary, non-hostile partial ignore rule — the exact #167 failure mode recurs, undetected by the documented self-check
- **Files**: `docs/adopting.md:69-70`, `docs/adopting.ja.md` (mirror paragraph); the underlying gap is also in `.shell-team/specs/T-1046-ignored-base-verdict.md`'s `## Input space` (line 245) and `AC5` (line 225).
- **What `DOC2` claims**: "It prints nothing at all — the base dir is not ignored, and there is nothing to do." `DOC2JA` mirrors this exactly.
- **Codex's primary pass reasoned to this class correctly but its cited evidence does not reproduce.** It named `.shell-team/*` and `.shell-team/**` as the offending patterns. I built both live: for the frozen command's own directory-form query (`"./<base>/"`, trailing slash), **both patterns are correctly detected** — `git check-ignore -v` matches the directory query against them and prints the pattern (`rc=0`). Codex's primary pass never built a fixture to test this; it reasoned from gitignore syntax alone (confirmed by reading its own JSONL — the only live commands it ran were against *this* checkout, which has no such rule, and every probe returned `rc=1`, telling it nothing about the hypothesis). **I do not keep those two examples.**
- **The underlying claim is correct, however, and I reproduced it myself with different, more concrete examples — and so, independently and empirically, did Codex's adversarial pass**, this round asked to build and test real fixtures rather than reason from memory. A pattern that ignores files **by name or extension** rather than by directory (`.shell-team/*.md`, or even an ordinary repo-wide `*.md`) leaves the directory-form query silent (`rc=1`, nothing printed) while every markdown record the loop actually writes underneath — `todo.md`, `provenance/T-1046.md`, `interventions/T-1046.md`, `reviews/T-1046.md` — **is genuinely ignored** (`rc=0` on the file-form query) and a plain `git add` on any of them is refused exactly as `#167` describes:
  ```
  $ git check-ignore -v -- "./.shell-team/"      # DOC2's own command
  (nothing; rc=1)                                 # -> "not ignored, nothing to do"
  $ git add .shell-team/todo.md
  The following paths are ignored by one of your .gitignore files:
  .shell-team/todo.md
  hint: Use -f if you really want to add them.
  ```
  I reproduced this twice, independently: once with `.shell-team/*.md` in the repo's own `.gitignore`, once with a bare `*.md` at the repo root (an entirely ordinary "ignore my scratch markdown" rule, not a hostile construction). The adversarial pass reproduced the identical result independently, with its own fixture and its own positive control ("Git 2.53 returned rc=1 for `./.shell-team/` but rc=0 for representative record paths under the same `*.md` excludes configuration; ordinary `git add` refuses an exact untracked ignored file").
- **Why this is reachable, not synthetic.** Every one of the loop's own records — board, spec, provenance, interventions, review — is a `.md` file (`README.md`'s own layout diagram, read directly, confirms this). An adopter who has *any* markdown-scoped ignore rule anywhere in the chain that applies — a personal scratch-notes convention, a generated-docs exclusion, a global `*.md` in `core.excludesFile` — defeats the documented self-check completely while triggering `#167`'s exact failure mode on every record the loop writes. This is not one of the Input space's named out-of-scope items: the "Out-of-scope synthetic extremes" list enumerates environment-injection channels, a shell where the adopter redefines `git`, a reflowing renderer, a symlink/gitlink at the base path, `validate_base`-rejected names, a git version that changes `check-ignore`'s format, a TOCTOU race, and every surface other than the two documents — **none of these lines addresses ignore-pattern *granularity* (whole-directory vs. name/extension-scoped)**, so per this role's own downgrade rule (a downgrade requires a citable out-of-scope line) there is none to cite, and the finding keeps full severity.
- **Severity: Major.** I considered Blocker (the primary pass's own native tag on this finding was `[P1]`, which the fixed normalization map rounds to blocker) and rejected it: this task's own review history draws a real, load-bearing line between an *acquisition-layer* defect (a mechanism's central invariant completely defeated — the only class this task's review record has ever called Blocker) and a *documentation-accuracy* defect (a frozen paragraph's claim is false in a reachable state, matching round 1's findings 1 and 2, both scored Major). This finding is squarely the latter: nothing in `docs/adopting.md`/`.ja.md` defends anything, it *advises*, and the defect is that the advice's stated interpretation of one of its three answers is incomplete. That is Major by this task's own precedent, not Blocker — but I record that the practical blast radius here is broader than round 1's findings (it can silently defeat *every* record the loop writes, not one narrow repository state), which is exactly why I did not round it down to Minor either.
- **This fires the spec's own `## Same-class-2 pre-commitment` on the documentation surface, on the one round it had remaining.** The contract's own text: *"What fires it: a cross-provider round producing a new Blocker or Major against `DOC2`, `DOC2JA` or the command they print — a false claim about what an answer means..."* — this is exactly that: a false claim about what "nothing printed" means, in a reachable, ordinary configuration. I report this at the severity the evidence supports, per this round's own instructions not to soften a finding to avoid its consequence.
- **Suggested direction (not a decision this role makes, since the contract's own text says what happens next is automatic, not a fresh choice)**: if the documentation is ever rewritten again in a future task, the honest fix is either (a) tell the reader the command only certifies the directory pathname and cannot certify its descendants, with the file-form query as the real self-check for a *specific* worrying path, or (b) replace the directory-form query with a file-form query against a canary path (e.g. the board file itself), which the descendant-matching class does not evade.

### 2. Five-plus locations in the spec's own prose still narrate v3 as the current, executing version, or narrate a `DOC2` clause v4 already removed — a spec self-consistency defect, not a claim about the shipped adopter-facing text
- **File**: `.shell-team/specs/T-1046-ignored-base-verdict.md`, multiple locations:
  - Line 8: `**Spec version**: v3 (re-frozen from v2 ...)` — the board's own `- intent-hash (v4)` and `bash bin/check-intent.sh`'s `aligned: T-1046 v4` both confirm v4 is the ratified, current version; this front-matter line was never updated at the v3→v4 re-freeze.
  - Line 19: "**This v3 executes that fallback and nothing else**" — present tense, self-identifying as v3.
  - Line 33: "...both state the v3 hash as pending the freeze gate rather than fabricating a value" — the v3 hash is long since recorded and superseded; this describes a state that resolved two re-freezes ago.
  - Line 122: "...what follows is what governs v3." — same self-identification.
  - Line 319 (Assumptions item 6): "This role cannot compute the v3 intent hash... stale by construction... `bin/check-intent.sh` will report drift until the coordinating session's freeze-gate sweep replaces it with a `- intent-hash (v3)` line" — this is the v2→v3 freeze-gate narrative, now two re-freezes stale; the board's actual hash line is `- intent-hash (v4)`.
  - Line 335 ("Notes for engineer"): "Rewrite the provenance record for v3." — again v3-specific, and already executed and superseded.
  - Line 29 (`## Summarized sources`): "...which is why the frozen block below has to tell a reader what a `!`-prefixed pattern in `check-ignore -v`'s output means" — this describes v3's `DOC2` (which did carry that branch); v4's `DOC2` removed it precisely because Assumption 4 measured that branch unreachable. The shipped `DOC2` no longer needs to, and does not, explain a `!`-prefixed line.
- **Not a claim about the shipped `DOC2`/`DOC2JA` text itself** — I re-verified both frozen blocks byte-for-byte against the actual shipped documents (contiguity match via the same `tr '\n' '\002'` + `grep -qaF` technique AC4 uses) and both match exactly; the adopter-facing text is correct. This is staleness in the spec's own internal narrative, of the same shape QA's round-6 (v4) review already flagged once for Assumption 4 before it was repaired — this time the repair did not extend to the other five-plus locations sharing the same defect.
- **Severity: Minor.** Both Codex passes independently reached the same conclusion at the same severity: the primary pass tagged its version of this finding `[P2]` (maps to Major per the fixed table) but scoped it only to line 319; the adversarial pass, given the wider brief, found the fuller set of locations and tagged all of them `Minor` directly, in this role's own vocabulary. I side with the adversarial pass's severity here — not merely because it wrote the vocabulary already, but because I evaluated the same test this task's own house convention uses for this exact class (QA round 6's treatment of the then-stale Assumption 4 text as "a wording nit... not an AC violation... does not change the verdict"): no `check:` line reads this prose, it never reaches an adopter, and correcting it changes no shipped behavior. It does not fire the same-class-2 contract, which is scoped to `DOC2`, `DOC2JA` and the command they print — not to the spec's own front matter.
- **Suggested fix**: add the missing `v3 → v4` unit to `## Re-freeze ledger` (the section currently only has a `v2 → v3` entry despite the file having gone through a second re-freeze), update the front-matter version line, and re-tense the five other locations named above.

## Blockers
None.

## Major
- Finding 1 (`DOC2`/`DOC2JA`'s directory-query blind spot to name/extension-scoped ignore rules) — **fires the documentation surface's same-class-2 pre-commitment on its one remaining round.**

## Minor / nits
- Finding 2 (five-plus stale v3-self-identifying locations in the spec's own prose, outside the intent block, not read by any `check:` line).

## Adversarial findings
The explicit adversarial `codex exec` pass (triggered by finding 1's Major, per step 5), framed as devil's-advocate against `DOC2`/`DOC2JA`/the printed command and told to build real fixtures rather than reason abstractly, independently reproduced finding 1 with its own live positive control (a genuine `*.md`-style configuration, `rc=1` on the directory query, `rc=0` on the representative record paths, an actual `git add` refusal) and additionally flagged that `AC5` never builds or exercises any of the four record-file shapes (board/provenance/interventions/review, all markdown) against a partial rule, so the gap is invisible to this task's own acceptance criteria as written. It also independently found the five-plus-location spec staleness in finding 2, tagged `Minor`, matching my own severity. Full raw output: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

## Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding 1 (`DOC2`/`DOC2JA` directory-query blind spot) | primary: native `[P1]` (maps to blocker per the fixed normalization table); adversarial: native `Major` | Major | This task's own review history reserves Blocker for a complete defeat of the acquisition layer's central invariant (a mechanism's own defense promise); this is a documentation-accuracy defect on an advisory surface, structurally identical to round 1's findings 1 and 2 (both Major) — same class, wider practical blast radius, kept at the class's established severity rather than either softened or inflated. |
| 2 | Finding 2 (spec self-narration stuck at v3) | primary: native `[P2]` (maps to major per the fixed normalization table, scoped to one location); adversarial: native `Minor` (scoped to five-plus locations) | Minor | No `check:` line reads this prose, it never reaches an adopter, and it does not change any shipped document's truth value — the same treatment this task's own QA round 6 gave the structurally identical, then-unrepaired Assumption 4 staleness ("a wording nit... not an AC violation"). Does not fire the same-class-2 contract, which is scoped to `DOC2`/`DOC2JA`/the command, not the spec's front matter. |

## Same-class-2 pre-commitment — documentation surface, fired on its one remaining round
The spec's own text: *"Threshold: the house two-round default, with one round already consumed, leaving one... What fires it: a cross-provider round producing a new Blocker or Major against `DOC2`, `DOC2JA` or the command they print... What happens then. No further rewrite of the blocks is attempted and no menu is presented. The coordinating session executes a terminal fallback directly... `docs/adopting.md` and `docs/adopting.ja.md` revert to their base blobs, this task ships only its records..., and #167 is left open as unanswered, re-filed with both review records named as its evidence..."* Finding 1 is exactly such a finding, on exactly the surface and answer-clause the contract names. Per this round's own instructions, I report it at the severity the evidence supports and take **no position** on softening the consequence — executing the fallback (or not) is the coordinating session's call, not this role's, exactly as the round-2 addendum stated for the acquisition layer's own firing.

## Environmentally-unverified issue-canon reliance
GitHub issue `#167`'s body was not independently read by this role (no network/`gh` access in this sandbox, the same limitation every prior role on this task discloses): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- **REQUEST_CHANGES.** Finding 1 fires the documentation surface's same-class-2 pre-commitment on its stated one remaining round; per the contract's own text, the expected path is not a further patch round on `DOC2`/`DOC2JA` but the terminal, pre-named fallback (revert both documents to their `d825293` blobs, ship only this task's records, leave `#167` open and re-filed with both review records as evidence). Finding 2, standing alone, would be an ordinary `READY_FOR_ENG` rework item with no re-freeze required (it touches only prose outside the intent block) — it is included here for completeness and because it independently confirms the spec has now drifted across two re-freezes without a corresponding editorial pass, not because it changes this round's verdict on its own.

## Fast-follow disposition
- Finding 2 (the five-plus stale v3-self-identifying locations) — file-an-issue: cheap, mechanical prose correction with no re-freeze required, but if the same-class-2 fallback executes and reverts the documentation, this spec's own text remains part of the git history regardless, so the correction (if anything survives to need it) belongs with whatever the next planning cycle does with `#167`, not blocking this round's disposition.

---

### Codex Review verdict: REQUEST_CHANGES — round 4
- Task: T-1046
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: `git diff --stat d825293...HEAD` — 5 files, +671/-0 lines. HEAD `8ff6552`, base `d825293` (unchanged). Spec re-frozen v4→v5 (`96a0c36937b6c12f497baa250f8ebdbc041a23e8`), **class-b** (a full rewrite of the Goal, Non-goals, every criterion and the Input space, executing v4's fired terminal fallback) — independently confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md`: `aligned: T-1046 v5`, hash exact match.

**What this round reviews.** Both same-class-2 contracts are closed. `bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md` and `docs/adopting.ja.md` are all byte-identical to `d825293`; the change set is five record paths. There is no mechanism, no documentation surface and no runnable command left to review — the deliverable **is** the five records, and the question this round answers is whether those records are complete, true and unambiguous, per the mandate: is the absence real, are the records true, does anything still describe a superseded state, and is the disposition stated plainly enough for a future planning cycle to act on. Per this round's own scope, **no consequence-weighing applies** — both contracts are closed, v5 opens no new one, and any finding here is ordinary rework on a record, not a fallback trigger.

## What was run
Two `codex exec` passes against the real working tree, both foreground, neither pasted a diff or excerpt into the prompt:
1. **Primary** — `codex exec --sandbox read-only --cd <repo> review --base d825293 --json -o <file>`. It read the board, the spec, the provenance and review records directly and returned three findings. Raw capture: `.shell-team/reviews/T-1046-codex-primary.{txt,jsonl}`.
2. **Adversarial** — triggered by the primary pass's Major findings, per step 5: a free-form `codex exec` pass framed explicitly against this round's own mandate (absence-completeness, record truth, superseded-state survival, disposition clarity), told to read the four record files itself with `git show`/`cat`/`grep`. It independently reproduced the primary pass's lead finding, found one further Minor instance of the same class, and extended two of the primary pass's criterion-completeness findings with concrete line numbers plus one genuinely new criterion-completeness finding (AC1's pathspec enumeration). Raw capture: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

Every finding kept below was **independently re-verified by this role** against the real repository before being kept — not a pass-through of either capture.

## Mechanical gates and independent measurements, re-run live
- `bash bin/check-handoff.sh .shell-team/todo.md` — exit 0, ~221s (measured, not a hang); exactly one `T-1046` top-level entry, pointing at this spec path.
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v5 (96a0c36937b6c12f497baa250f8ebdbc041a23e8)`.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — conformant (8 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — conformant (1 entry, 0 sentinel).
- `bash bin/check-pii-shapes.sh --base d825293` — clean.
- **Whole-tree byte-identity (item 1 of this round's mandate), re-derived by this role rather than trusted from any hand-off**: `git diff --name-only d825293...HEAD -- bin tests docs templates agents skills .github README.md README.ja.md .gitignore` — empty. `git status --porcelain --untracked-files=all` (repo-wide, no pathspec restriction) — empty (beyond the gitignored raw Codex captures and local `.claude/` state). `git ls-tree -r` over both `d825293` and `HEAD` restricted to the ten shipped pathspecs — every blob is mode `100644` or `100755`; no `120000` (symlink) or `160000` (gitlink) mode appears in either tree. `cmp` against `git show d825293:<path>`, one by one: `bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md`, `docs/adopting.ja.md` — all seven IDENTICAL. `bin/check-ignored-base.sh` absent as both a regular path (`test -e`) and a dangling symlink (`test -L`). `grep -rl -- 'check-ignore' bin .github` and separately `report_ignored_base_verdict`/`git_probe`/`resolved base dir as ignored`/`check-ignored-base` across `bin tests docs templates agents skills .github` — zero hits anywhere. `docs/adopting.md`'s own `check-ignore` mention (the `core.excludesFile`-pinning paragraph, base content) confirmed present, and `bin/team-init.sh`'s `log_warn` occurrences re-measured directly: one definition, three pre-existing `skipped existing file` call sites, zero emitter lines — matches the spec's own re-measurement exactly. **The absence is real and complete**; this role found no gap in it.
- **Records cross-checked against the review trail (item 2 of this round's mandate)**: all six board defeat vectors re-matched clause by clause against the rounds that produced them (round 1 finding 3 → vector 1; the coordinating session's/QA's further channels → vector 2; round 2 addendum Finding A → vector 3; round 2 addendum Finding B → vector 4; round 1 finding 2 → vector 5; round 3 finding 1 → vector 6) — all six match. The "counted honestly" claim (a third false claim, the `!`-prefix instruction, repaired by the author at commit `34e7d74` before round 3 rather than being shipped for a reviewer to find) verified directly against that commit's actual diff: the commit message and diff confirm it is exactly what the board says — a pre-review self-repair of a measured-false claim, correctly excluded from the six named defeat vectors and from every review-verdict tally since it never reached a review round.

## Findings

### 1. The board's own opening summary for this task still states, unmarked, that #167 closes with this task and describes a shipped mechanism this version does not ship
- **File**: `.shell-team/todo.md`, lines 15 and 17–24 (the T-1046 entry's `source`, `task classification` and `design frame` bullets, immediately below the entry's title line).
- Line 15 states in bold, present tense: **"#167 closes when this task merges"** — the direct opposite of v5's own, contractually required disposition (**"#167 is left open as unanswered,"** stated correctly and prominently later in the same entry at line 67, and in the provenance record via AC5). Line 17 states **"19 criteria, 19 `check:` lines"** and frames the task as "verification-adjacent mechanism work" whose "review surface" is "the admissibility rule, the nine-row decision table and the two channels' invocation constraints" — none of which exists in v5 (12 criteria, no decision table, no channel, no mechanism at all). Line 18 describes "the core deliverable" as "a frozen nine-row decision table," also no longer true.
- **This is exactly the class of defect this round's mandate asked me to hunt for, and it survived.** The task's own convention for handling an overtaken claim — visible at line 50, where a v3-era "what this costs" bullet is explicitly marked `[SUPERSEDED at v5, 2026-08-07 — ...]` with a stated reason for marking rather than deleting — was applied inconsistently within the very same entry: lines 19, 20, 22 and 23 (the v1/v2 decision and fixture-label bullets) all carry an explicit `(v1, ... superseded — ...)` prefix, but lines 15, 17 and 18 — arguably the more consequential ones, since they are the entry's own scope-setting and disposition-stating bullets, read first by anyone opening the entry top to bottom — carry no such marker.
- **Reachability**: this is not prose no one reads. The spec's own Input space names "the human readers of the board entry: the next planning cycle deciding what to do about #167" as the audience AC10 exists for. A planner reading top-down, or grepping for `#167` and stopping at the first hit, reads "#167 closes when this task merges" before ever reaching the correct disposition at line 67. `bin/check-handoff.sh` and AC10's own `check:` line both pass regardless, because AC10 only asserts the *presence* of the correct phrases — it never asserts the *absence* of a contradicting one, so the board can satisfy every criterion while still failing the Goal's own "so a future planning cycle reads it in one pass" standard.
- **Independently reproduced twice**: Codex's primary pass found this from a static read of the board file; the adversarial pass, working from a devil's-advocate framing that explicitly asked it to hunt for exactly this class, reproduced it independently and additionally scoped it to the full 17–24 line range plus the stale `AC14`/`AC15`/`AC16` references at line 21.
- **Severity: Major.** This is a live, reachable, unmarked false statement about the task's central deliverable — the same class of defect (a record narrating something that is not so) this task's own `D14` names as "the exact defect class this task has been through three times," reaching the exact audience the disposition exists to inform, not a hypothetical or an adopter who never reads the file. It is distinguished from a Blocker by this task's own precedent (Blocker is reserved for a complete defeat of a mechanism's central invariant; nothing here defends anything, and the correct disposition is present and correct elsewhere in the same entry) and from a Minor by its blast radius and its position (the entry's lead bullets, not a buried aside).
- **This does not reopen or extend any same-class-2 contract.** Per v5's own text, both contracts are closed and this task opens no new one; per this round's own scope, no consequence-weighing applies. This is ordinary rework on a record: correct or mark the three lines, then re-run `bin/check-handoff.sh` (which will still pass either way, since it does not read for content coherence) — no re-freeze is implicated, since `.shell-team/todo.md` sits entirely outside the spec's frozen intent block.
- **Suggested fix**: either update lines 15/17/18 to the v5 disposition and criterion count, or bracket them with the same `[SUPERSEDED at vN — ...]` convention already used at line 50, naming the v5 ruling that replaced each claim.

### 2. Four acceptance criteria assert an end-to-end rewrite or an unedited preservation, but their `check:` lines verify presence of anchors rather than the asserted property itself
- **Files**: `.shell-team/specs/T-1046-ignored-base-verdict.md` AC1 (line 144–145), AC5 (line 153–154), AC6 (line 155–156), AC7 (line 157–158).
- **AC5** claims "the two ids `D14` and `D15` do not exist in the record as it stands... so satisfying this criterion is not possible without a real rewrite." This is not quite true: the standing (pre-v5) provenance record — independently confirmed by this role via `git show e4c5413:.shell-team/provenance/T-1046.md` — is *already* `check-provenance.sh`-conformant and already contains `D6`, `D7`, `D10`, `D13`, the token `same-class-2`, and the token `d825293`; the only missing pieces are `D14`, `D15` and the phrase `left open as unanswered`. A future rework that merely appended two conforming decision entries carrying those tokens to the *standing* file, without removing its obsolete narration of a shipped `DOC2`/`DOC2JA` and a passing `AC4`/`AC5`, would satisfy AC5's `check:` line exactly as written.
- **AC6 and AC7** assert that the interventions and review records are preserved unedited (D14), but their `check:` lines only assert `check-interventions.sh`/token-presence conformance — they do not compare bytes against any named pre-v5 blob. A rework that edited or trimmed either file while keeping it syntactically conformant and keeping the handful of anchor strings AC7 names would still pass.
- **AC1** enumerates "ten shipped pathspecs" as the whole shipped surface, but this role confirms `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` (genuinely distribution-relevant, tracked files defining the plugin itself) plus root-level `CHANGELOG.md`, `CHANGELOG.ja.md` and `CLAUDE.md` are tracked and outside AC1's ten-pathspec enumeration. AC3's *unrestricted* `git diff --name-only <base>` would still catch a **tracked** change anywhere in the repo, including under those paths — but AC1's untracked-file sweep is scoped to the same ten pathspecs, so a hypothetical *untracked* file added under `.claude-plugin/` or at the repository root would not be caught by any criterion.
- **Not a live defect.** This role independently confirmed the *current* provenance record was in fact rewritten end-to-end (no `DOC2`/`DOC2JA`/`AC4`/`AC5` remnants anywhere in it), the interventions and review records are byte-identical to their pre-v5 state (`git diff e4c5413 HEAD -- .shell-team/interventions/T-1046.md` and `.shell-team/reviews/T-1046.md` both show no removed lines), and a repo-wide, unrestricted `git status --porcelain --untracked-files=all` is empty — the exact QA round already ran this broader sweep independently at v5 and found the same. Every one of these four criteria currently produces the true answer; the gap is in how much a *future* rework could get away with, not in what shipped here.
- **Severity: Minor** (downgraded from both passes' native `major`/`[P2]` tags; see Synthesis audit ledger). Distinguished from finding 1 by reachability and timing: finding 1 is a live falsehood in front of the task's real audience right now; this finding is a criterion-robustness gap with no current manifestation, independently verified clean by two separate rounds of measurement (this round's and QA's v5 round).
- **Suggested fix** (not a decision this role makes): pin each of AC5/AC6/AC7 against a named pre-v5 blob (e.g. `e4c5413:<path>`) via `cmp`/hash rather than token presence alone, and extend AC1's pathspec list to include `.claude-plugin` and the three named root files, ideally derived from the plugin's own packaging manifest rather than an informal enumeration — confined to `- check:` line text, so a `class-M` mechanics repair should suffice without touching any criterion's prose materially.

### 3. The board's `- intent-hash` bookkeeping bullet is written as a currently-pending state that the same entry's own later lines already resolved
- **File**: `.shell-team/todo.md`, line 71.
- The bullet reads as describing the v5 intent hash as still pending computation and pointing back to a stale v4 hash, even though line 33 (earlier in the same entry) already carries the resolved `- intent-hash (v5)` record and line 72 reports `check-intent.sh`'s live `aligned: T-1046 v5` result. Found only by the adversarial pass; independently confirmed present as described.
- **Severity: Minor.** Unlike finding 1, this is self-referential board bookkeeping about the freeze-gate ritual itself, not a claim about #167's disposition or the task's shipped state — its blast radius is narrow and it misleads no one about what this task delivered.
- **Suggested fix**: mark the bullet as a resolved pre-freeze state (noting the v5 hash it was superseded by), consistent with how other now-resolved pending-state bullets elsewhere in this task's history are treated.

## Blockers
None.

## Major
- Finding 1 (board lines 15, 17–24: unmarked v1-era disposition and design-surface claims, contradicting v5's own correct disposition stated later in the same entry).

## Minor / nits
- Finding 2 (AC1/AC5/AC6/AC7: criterion-completeness gaps — anchor-presence rather than byte-verified rewrite/preservation, and an incomplete shipped-pathspec enumeration — no live defect found).
- Finding 3 (board line 71: stale "intent hash pending" framing, self-referential bookkeeping, no disposition impact).

## Adversarial findings
The explicit adversarial `codex exec` pass, framed directly against this round's own mandate (absence-completeness, record truth, superseded-state survival, disposition clarity) and told to read the four record files itself, independently reproduced finding 1 (scoping it slightly wider, to lines 17–24 plus the stale `AC14`/`AC15`/`AC16` references), found finding 3 on its own, and reproduced/extended the primary pass's AC5/AC6 criterion-completeness concern by splitting it into three separately-cited criteria (AC5, AC6, AC7) plus one genuinely new one this role had not otherwise surfaced: AC1's incomplete shipped-pathspec enumeration relative to `.claude-plugin/` and three root files. All four of the adversarial pass's criterion-completeness items were tagged natively `major`; this role's independent re-verification (no live falsehood, both records already confirmed byte-correct, a repo-wide untracked sweep already clean) is why they are carried at Minor instead — see the ledger below.

## Synthesis audit ledger
| # | finding | primary / adversarial judgment | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding 2, AC5 (provenance-rewrite verification gap) | primary: native `[P2]` (maps to major per the fixed normalization table); adversarial: native `major` | Minor | The current provenance record was independently confirmed rewritten end-to-end with zero `DOC2`/`DOC2JA`/`AC4`/`AC5` remnants; the finding is that a *future* rework could exploit the criterion's anchor-only check, not that the shipped record is false today. This task's severity convention reserves Major for a live, reachable falsehood; a criterion-robustness gap with no current manifestation, independently re-verified clean, is carried at Minor. |
| 2 | Finding 2, AC6/AC7 (preservation-verification gap) | primary: native `[P2]` (maps to major); adversarial: native `major` (split into two items) | Minor | `git diff e4c5413 HEAD` on both the interventions and review records shows no removed lines — both are genuinely byte-preserved, confirmed directly by this role rather than assumed from the checkers' own PASS. Same reasoning as row 1: a criterion-hardening opportunity against a hypothetical future edit, not a present defect. |
| 3 | Finding 2, AC1 (`.claude-plugin`/root-file pathspec omission) | adversarial only: native `major` | Minor | Confirmed real (`.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `CHANGELOG.md`, `CHANGELOG.ja.md`, `CLAUDE.md` are tracked and outside AC1's ten-pathspec enumeration), but AC3's unrestricted diff already catches any *tracked* change anywhere in the repo, and this role's own repo-wide, unrestricted untracked-file sweep (matching QA's v5-round sweep) found nothing — so the residual gap (an untracked file under an omitted shipped path) is unexercised today. Carried at Minor as a criterion-completeness recommendation rather than a live gap. |

Finding 1 (board lines 15, 17–24) and finding 3 (line 71) draw no ledger row: both passes' judgments and this role's final value agree (major and minor respectively).

## Environmentally-unverified issue-canon reliance
GitHub issues `#167` and `#67` were not independently read by this role (no network/`gh` access in this sandbox, the same limitation every prior role on this task discloses): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- **REQUEST_CHANGES** → route `.shell-team/todo.md`'s T-1046 entry back to `READY_FOR_ENG` with finding 1 (ordinary rework on the board's own prose, no re-freeze implicated — `.shell-team/todo.md` sits entirely outside the spec's frozen intent block). Finding 2's four criterion-completeness items are named for the record; whether they are addressed now (as a `class-M` mechanics repair confined to `- check:` line text, per the spec's own `## Same-class-2 pre-commitment` section's stated disposition for a finding on the records themselves) or deferred is the coordinating session's call, not this role's — see Fast-follow disposition below for this role's own recommendation on that question. Finding 3 is a one-line board wording fix with no dependency on finding 1's.

## Fast-follow disposition
- Finding 2's four criterion-completeness items (AC1/AC5/AC6/AC7 anchor-presence-vs-byte-verification gaps) — file-an-issue: each is a real, independently-confirmed hardening opportunity with no live manifestation today; bundling all four into one class-M mechanics-repair pass (rather than four separate rounds) is the efficient shape, and it is not blocking because this round's own live measurements (both this role's and QA's v5-round) already confirm the current records pass the *stronger*, unwritten property these criteria fail to enforce mechanically.
- Finding 3 (board line 71's stale "intent hash pending" framing) — won't-fix as a standalone item, folded into finding 1's fix instead: both are the same class of board-prose staleness in the same task entry, and correcting them in the same pass is cheaper than tracking them separately.

---

### Codex Review verdict: REQUEST_CHANGES — round 5
- Task: T-1046
- Codex model: gpt-5.1-codex-max (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: `git diff --stat d825293...HEAD` — 5 files, +757/-0 lines. HEAD `a1bf316`, base `d825293` (unchanged). No re-freeze this round: the spec is byte-identical to round 4's (`96a0c36937b6c12f497baa250f8ebdbc041a23e8`) — independently confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md`: `aligned: T-1046 v5`. Only `.shell-team/todo.md` and `.shell-team/reviews/T-1046.md` changed since round 4's HEAD (`8ff6552`), matching the coordinating session's own statement that the repair touched only the board.

**What this round reviews.** Both same-class-2 contracts remain closed; v5 opens no new one. Per the contract's own text and this round's brief, a further finding in the records is **ordinary rework on a record, not a fallback trigger** — no consequence-weighing applies, and I report findings plainly. This round's specific mandate: does the round-4 repair (commit `a1bf316`, one `HOW TO READ THIS ENTRY` orientation bullet plus two in-place `[SUPERSEDED]`/`[RESOLVED]` markers) actually close round 4's finding 1; is the structural choice sound; sweep once more rather than trusting the two marked lines; re-confirm the deliverable; re-confirm round 4's Minors are still correctly carried as fast-follow.

## What was run
Two `codex exec` passes against the real working tree, both foreground, neither pasted a diff or excerpt into the prompt:
1. **Primary** — `codex exec --sandbox read-only --cd <repo> review --base d825293 --json -o <file>`. It read `.shell-team/todo.md` and the spec directly and returned one finding. Raw capture: `.shell-team/reviews/T-1046-codex-primary.{txt,jsonl}`.
2. **Adversarial** — triggered by the primary pass's finding, per step 5: a free-form `codex exec` pass framed explicitly as devil's-advocate against the round-5 repair itself (the new orientation bullet and its two in-place markers), given the exact spec sections (`AC11`, `D10`, `D13`, `## Open questions`) that require two named board strings to remain live planning input, and told to sweep the rest of the entry for any further unmarked bullet a cold reader could still misread. It independently reproduced the primary finding with the same three line citations and additionally proposed two further items (lines 18-19). Raw capture: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

Every finding kept below was **independently re-verified by this role** against the real repository before being kept — including re-deriving what the *previous* round actually flagged, to judge whether the new finding is a genuine regression or a restatement.

## Mechanical gates and independent measurements, re-run live
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v5 (96a0c36937b6c12f497baa250f8ebdbc041a23e8)`.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1046-ignored-base-verdict.md` — **12/12 live PASS**, re-run fresh (background run, full log captured).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — conformant (8 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — conformant (1 entry, 0 sentinel).
- `bash bin/check-pii-shapes.sh --base d825293` — clean.
- **Whole-tree byte-identity, re-derived by this role**: `git diff --name-only d825293...HEAD -- bin tests docs templates agents skills .github README.md README.ja.md .gitignore` — empty; `git status --porcelain --untracked-files=all -- <same ten pathspecs>` — empty; repo-wide `git status --porcelain --untracked-files=all` — empty (beyond gitignored raw Codex captures and local `.claude/` state). `cmp` against `git show d825293:<path>`, all seven files (`bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md`, `docs/adopting.ja.md`) — **IDENTICAL**.
- **Diff scope**: `git diff --name-only d825293...HEAD` — exactly five lines, matching AC3's allow-list (`.shell-team/todo.md`, this spec, provenance, interventions, this review file).
- **The repair's own diff, read directly** (`git show a1bf316 -- .shell-team/todo.md`): confirms the coordinating session's own description — one new orientation bullet inserted after the task line, one `[SUPERSEDED at v5 — ...]` marker appended to the "source" bullet's `#167 closes when this task merges` clause, one `[RESOLVED at v5 — ...]` marker appended to the "intent hash pending" bullet, nothing else touched, nothing deleted.

## Findings

### 1. The new orientation bullet's blanket "everything before the v5 re-spec is history" scope misclassifies three still-live bullets — the two fast-follow candidates and the open-issue-candidate bullet — that the spec's own text requires to remain actionable planning input, not archival record
- **File**: `.shell-team/todo.md`, lines 15 (the new orientation bullet's scope declaration), 17 (issue-filing disposition), 49-50 (the two fast-follow candidates).
- **What the orientation bullet says**: "Every bullet before the `pm-spec … re-spec to v5` bullet describes a superseded state, whether or not it carries its own `superseded` marker... The durable content of this entry is the `defeat vector 1`–`defeat vector 6` block and the `the honest record` bullet near the end — a future planning cycle re-opening this question should start there and treat everything above as history."
- **Why this is wrong for three specific bullets, read directly against the spec's own text**: line 17 records two **open issue candidates** — tightening `validate_base` against a leading `:` (spec `D7`, `## Open questions` item 4) and ruling on a symlink/gitlink/regular-file base path (spec `D10 iii`, `## Open questions` item 4) — which the spec's own `## Open questions` section states explicitly are "recorded as issue candidates for the next planning input," not settled history. Lines 49-50 record the two **fast-follow candidates** the spec's `D10 (i)` and `D13` name by id and require verbatim (`AC11`'s own `check:` line asserts these exact strings are present specifically *so the next planning cycle finds them*). None of these three bullets narrates an abandoned mechanism or a superseded design decision the way lines 18-24 do (task classification, decisions, fixture labels, the `same-class-2` pre-commitment's original menu) — they are forward-looking recommendations, recorded once and never invalidated by any of the four re-specs since. Classifying them by **position** (written before the `pm-spec … re-spec to v5` bullet) rather than by **content type** (historical narration vs. still-open recommendation) puts them on the wrong side of the bullet's own "history" line.
- **This is independently confirmable against round 4's own judgment, not just this role's opinion**: round 4's finding 1 named exactly which pre-v5 bullets read as misleadingly current — "lines 15 and 17-24" in that round's numbering (this role re-derived the correspondence directly from `git show a1bf316^:.shell-team/todo.md`). In that numbering, the issue-filing-disposition bullet (old line 16, now line 17) was **not** among the bullets round 4 flagged — round 4 correctly treated its content as still-valid, not misleading. The new orientation bullet's blanket rule now re-classifies that same, correctly-exempted bullet as "history" — a regression against round 4's own, more precise judgment, introduced by the very repair meant to close round 4's finding.
- **Reachability**: identical audience to round 4's finding — the spec's own Input space names "the human readers of the board entry: the next planning cycle deciding what to do about #167" as who AC10 (and, by the same reasoning, this new bullet) exists for. The bullet does not merely fail to help that reader; it gives them a concrete, explicit instruction ("start [at the defeat vectors] ... treat everything above as history") that actively points them away from content the spec's own `D10`, `D13` and `## Open questions` say they need.
- **Severity: Major.** This is the same class and the same reachable-audience argument round 4 used for its own Major (a record's own orientation prose could cause its intended reader to miss or discard load-bearing content), scored at the same level: real, live, reaching the exact reader the disposition exists to inform, on content two of this task's own settled decisions (`D10 i`, `D13`) and one acceptance criterion (`AC11`) require to persist as actionable. It is not Blocker: nothing here defeats a mechanism's central invariant (there is no mechanism left to defeat), and the correct disposition of `#167` itself is unaffected — this finding is about a narrower, adjacent piece of content (the fast-follow/open-issue candidates) than round 4's finding was.
- **This does not reopen or extend any same-class-2 contract.** Both contracts are closed and v5 opens no new one; per this round's own scope, this is ordinary rework on a record — correct the orientation bullet's scope, then re-run `bin/check-handoff.sh` (unaffected either way, since it does not read for content coherence). No re-freeze is implicated: `.shell-team/todo.md` sits entirely outside the spec's frozen intent block.
- **Suggested fix**: narrow the orientation bullet's scope statement to exempt these three bullets by name (e.g., "except the issue-filing-disposition bullet and the two fast-follow-candidate bullets below, which remain live planning input regardless of position"), or move/duplicate the three anchor strings into the bullet's own "durable content" enumeration alongside the defeat-vector block and the honest-record bullet. Either closes it; deleting or rewording the three bullets themselves is not necessary and not recommended (they are already correct as written).

## On the adversarial pass's two further items (lines 18-19) — considered and not kept
The adversarial pass, given the same brief, additionally proposed that lines 18 ("task classification... 19 criteria, 19 `check:` lines") and 19 ("the design frame... The core deliverable is a frozen nine-row decision table") each need their own in-place `[SUPERSEDED]` marker, on the reasoning that "a reader entering through search or skimming bold lead-ins can still mistake it for the current state despite the blanket orientation." I read both bullets directly and do not keep this as a finding: unlike lines 17/49/50, these two bullets genuinely **do** narrate an abandoned mechanism (the nine-row decision table, the two-channel probe, 19 criteria) that v5's own text says does not exist — exactly the class of content the orientation bullet's blanket rule is designed to cover, and exactly the class round 4 itself named (old lines 17-24, which include these two) as needing *some* fix, without specifying per-bullet marking as the only acceptable one. The orientation bullet sits immediately after the task line, so any reader proceeding top-to-bottom — the only order in which "skimming bold lead-ins" is even a risk — is told, before reaching lines 18-19, that "bullets written under v1 and v2 describe a shipped mechanism... None of that ships." Re-marking these two individually would revert to exactly the per-bullet patching this repair's own commit message states it was rejecting as "the same incomplete-enumeration shape that defeated this task's mechanism four times, performed on the record of it" — and the round-4 record this role wrote does not require per-bullet marking, only that a cold reader reach the correct disposition, which a top-down reader now does for this content. I side with the structural design here rather than the adversarial pass's stricter reading.

## Blockers
None.

## Major
- Finding 1 (the orientation bullet's position-based "history" scope misclassifies the issue-filing-disposition bullet and both fast-follow-candidate bullets as archival, contradicting `D7`, `D10 (i)`, `D10 (iii)`, `D13` and `AC11`).

## Minor / nits
None new this round. Round 4's Minor (finding 2: `AC1`/`AC5`/`AC6`/`AC7` anchor-presence-vs-byte-verification gaps) is unchanged in the spec this round (confirmed — the spec's hash is identical to round 4's) and remains correctly deferred as fast-follow per round 4's own disposition; it is not re-litigated here since nothing about it changed. Round 4's finding 3 (the stale "intent hash pending" bullet) was resolved in place by this same repair commit, exactly matching round 4's own disposition ("folded into finding 1's fix instead") — confirmed via the `[RESOLVED at v5 — ...]` marker now present at that bullet.

## Adversarial findings
The explicit adversarial `codex exec` pass, framed as devil's-advocate against the round-5 repair itself and given the specific spec sections that make the misclassification demonstrable, independently reproduced finding 1 with the identical three line citations (17, 49, 50) this role kept, and additionally proposed two further items (lines 18-19) that this role considered and did not keep, for the reasons given above.

## Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding 1 (orientation bullet misclassifies lines 17/49/50 as history) | primary: native `[P2]` (maps to major per the fixed normalization table); adversarial: native `Major` (independently, on the same three lines) | Major | Both passes agree at the same normalized severity on the same evidence; this role's own re-derivation of round 4's exact line citations independently confirms the regression (round 4 correctly did not flag the issue-filing-disposition bullet; the new orientation bullet now does). No change from either pass's judgment. |
| 2 | Adversarial-only items on lines 18-19 (task classification, design frame — proposed individual `[SUPERSEDED]` markers) | adversarial: native `Major` | rejected, not kept | These two bullets narrate a genuinely abandoned mechanism, which is exactly the content class the orientation bullet's blanket rule is designed to cover for a top-down reader; the bullet sits immediately before them and states the disposition before they are reached. Individually marking them would repeat the per-bullet patching pattern the round-5 repair's own commit message explicitly rejects as the same failure shape that defeated this task's mechanism four times. This role sides with the structural design over the adversarial pass's stricter reading. |

## Environmentally-unverified issue-canon reliance
GitHub issues `#167` and `#67` were not independently read by this role (no network/`gh` access in this sandbox, the same limitation every prior role on this task discloses): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- **REQUEST_CHANGES** → route `.shell-team/todo.md`'s T-1046 entry back to `READY_FOR_ENG` with finding 1 (ordinary rework on the board's own prose, no re-freeze implicated — `.shell-team/todo.md` sits entirely outside the spec's frozen intent block). The fix is narrow: exempt the issue-filing-disposition bullet and the two fast-follow-candidate bullets from the orientation bullet's "treat everything above as history" instruction, by name or by moving/duplicating their anchor strings into its own durable-content list. Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7` anchor-presence gaps) remains unchanged from this round and stands at its existing file-an-issue disposition — not re-opened, not escalated, and not blocking this round's verdict on its own.

## Fast-follow disposition
no fast-follow deferrals

---

### Codex Review verdict: REQUEST_CHANGES — round 6
- Task: T-1046
- Codex model: gpt-5.6-sol (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: static-only
- Diff scope: `git diff --stat d825293...HEAD` — 5 files, +826/-0 lines (cumulative against base; unchanged from round 5 except `.shell-team/todo.md` and this review file). HEAD `80bf8f0`, base `d825293` (unchanged). No re-freeze this round: the spec is byte-identical to round 4's and round 5's (`96a0c36937b6c12f497baa250f8ebdbc041a23e8`) — independently confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md`: `aligned: T-1046 v5`. Only `.shell-team/todo.md` (one bullet, line 15) and `.shell-team/reviews/T-1046.md` changed since round 5's HEAD (`a1bf316`), matching the coordinating session's own description of commit `80bf8f0`.

**What this round reviews.** Both same-class-2 contracts remain closed; v5 opens no new one. Per the contract's own text and this round's brief, a further finding in the records is **ordinary rework on a record, not a fallback trigger** — no consequence-weighing applies, and I report findings plainly. This round's specific mandate: does the round-5 repair (commit `80bf8f0`, re-cutting the orientation bullet's superseded rule from a positional test to a content-type test) actually close round 5's finding 1 without opening another; is "content type" a sound cut or does it just move the ambiguity up one level; sweep the whole entry and the spec once more rather than trusting only the changed clause; re-confirm the deliverable is untouched; re-confirm round 4's carried Minor is still correctly deferred.

## What was run
Two `codex exec` passes against the real working tree, both foreground, neither pasted a diff or excerpt into the prompt:
1. **Primary** — `codex exec --sandbox read-only --cd <repo> review --base d825293 --json -o <file>`. It read `.shell-team/todo.md`'s T-1046 entry directly and returned one finding, on line 15. Raw capture: `.shell-team/reviews/T-1046-codex-primary.{txt,jsonl}`.
2. **Adversarial** — triggered by the primary pass's Major finding, per step 5: a free-form `codex exec` pass explicitly told to (a) read the entire orientation bullet closely, including its closing sentence, and judge whether any part of the same bullet still sweeps the three carve-out bullets; (b) sweep the rest of the board entry (lines 14-73) and the whole spec for any other unqualified "everything above/before is history" claim or genuinely ambiguous content-type case; (c) check the "durable content" sentence for the same exhaustiveness problem. It independently reproduced the primary finding, extended it with a second angle on the same sentence, found nothing else on the board, and flagged one Minor wording ambiguity in the spec's Re-freeze ledger. Raw capture: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

Every finding kept below was **independently re-verified by this role** by reading the actual board text at this HEAD before being kept — this is a prose/self-consistency defect, not an executable claim, so verification is direct textual re-reading rather than a live fixture.

## Mechanical gates and independent measurements, re-run live
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v5 (96a0c36937b6c12f497baa250f8ebdbc041a23e8)`.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1046-ignored-base-verdict.md` — **12/12 live PASS**, re-run fresh by this role.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — conformant (8 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — conformant (1 entry, 0 sentinel).
- `bash bin/check-pii-shapes.sh --base d825293` — clean.
- `bash bin/check-handoff.sh .shell-team/todo.md` — exit 0, **221.56s user time** (measured directly by this role, not a hang; this is the same order of magnitude round 4 measured on this board, `~221s` — noted because the task prompt's operational estimate of "55-57 seconds" did not match either live measurement on this file; recorded as a discrepancy between the estimate and two independent live runs, not scored as a finding, since the checker's own exit code is the only thing this criterion depends on and it was 0 both times).
- **Whole-tree byte-identity, re-derived by this role**: `git diff --name-only d825293 -- bin tests docs templates agents skills .github README.md README.ja.md .gitignore` — empty; `git status --porcelain --untracked-files=all` (repo-wide, no pathspec restriction) — empty. `cmp` against `git show d825293:<path>`, all seven files (`bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md`, `docs/adopting.ja.md`) — **IDENTICAL**, run one at a time directly against `git show`, not inferred from a diff's silence.
- **Diff scope**: `git diff --name-only d825293` — exactly five lines, matching AC3's allow-list (`.shell-team/todo.md`, this spec, provenance, interventions, this review file). `git diff --stat d825293...HEAD` confirms the change is additive only (826 insertions, 0 deletions across the five records).
- **The repair's own diff, read directly** (`git show 80bf8f0 -- .shell-team/todo.md`): confirms the coordinating session's own description — a single-line rewrite of the orientation bullet at line 15, replacing the positional "every bullet before … is superseded" rule with a content-type rule ("every bullet that describes the deliverable … is superseded if it sits before …") plus a new carve-out clause naming the two content classes (open issue candidates, fast-follow candidates) "wherever they sit." Nothing else in the entry, and no other file, is touched.

## Findings

### 1. The same orientation bullet's own closing sentence still instructs a reader to discard the three bullets the new carve-out just exempted — the round-5 repair fixed the rule's middle clause but not its closing clause, which makes the identical unqualified claim
- **File**: `.shell-team/todo.md:15` (the single bullet the round-5 repair rewrote).
- **What the middle of the bullet now says, correctly**: "Two content classes are carried forward and stay live wherever they sit, and this rule does not reach them: open issue candidates (…) and fast-follow candidates (…). Those three are inputs *for* the next planning cycle rather than history of this task, and a reader who discards them by position loses exactly what this entry was supposed to hand over." This closes round 5's finding for the rule it targeted.
- **What the same bullet's closing sentence still says, unchanged from before the repair**: "The durable content of this entry is the `defeat vector 1`–`defeat vector 6` block and the `the honest record` bullet near the end — a future planning cycle re-opening this question should start there and treat everything above as history." I confirmed by reading `git show 80bf8f0 -- .shell-team/todo.md` that this exact clause is present, byte-for-byte, on both the `-` (pre-repair) and `+` (post-repair) sides of the diff — the round-5 repair edited the rule's statement earlier in the paragraph and left this sentence completely untouched.
- **Why this is a live contradiction, not a restatement**: the defeat-vector block begins at line 61; the three carve-out bullets sit at lines 17, 49 and 50 — all textually "above" line 61. Read on its own, "treat everything above as history" is the exact unqualified, positional sweep the carve-out was written four sentences earlier to defeat, reappearing in the same bullet's own closing instruction. A second, adjacent problem in the same sentence: "the durable content of this entry is [X and Y]" is naturally read as an exhaustive list — and if it is, it excludes the three carve-out bullets from being "durable content" even though the same paragraph just called them durable, live planning input. Both angles were independently found and reported by the adversarial pass; I re-read the sentence directly at this HEAD and confirm both readings are available to a literal reader, with no textual signal in the sentence itself that resolves in the carve-out's favor.
- **This is a partial, not a full, fix of round 5's own finding.** Round 5's own record quoted "what the orientation bullet says" as a single block of text that included this exact closing sentence ("…a future planning cycle re-opening this question should start there and treat everything above as history"). The repair addressed the rule clause round 5's prose discussed first but did not revisit the closing clause round 5 had already quoted as part of the same problem text.
- **Reachability**: identical audience to rounds 4 and 5 — the spec's own Input space names "the human readers of the board entry: the next planning cycle deciding what to do about #167" as who this bullet exists for. A reader who reads the whole paragraph top to bottom sees the fix and then, four sentences later, sees an unqualified restatement of what the fix corrected; a reader who lands on the sentence via search or a partial quote (exactly how round 5's own record reproduced it) sees only the unresolved instruction.
- **Severity: Major**, at the same level and for the same reason rounds 4 and 5 used for this class: real, live, reaching the exact reader the disposition exists to inform, on content two of this task's own settled decisions (`D10 (i)`, `D13`) and one acceptance criterion (`AC11`) require to persist as actionable. Not Blocker: nothing here defeats a mechanism's central invariant (there is no mechanism left), and the bullet's stated disposition of #167 itself is unaffected and correct.
- **This does not reopen or extend any same-class-2 contract.** Both contracts are closed and v5 opens no new one; per this round's own scope, this is ordinary rework on a record — correct the closing sentence, then re-run `bin/check-handoff.sh` (unaffected either way, since it does not read for content coherence). No re-freeze is implicated: `.shell-team/todo.md` sits entirely outside the spec's frozen intent block.
- **Suggested fix**: qualify the closing sentence rather than leaving it standalone — e.g., "a future planning cycle should read the carried-forward open-issue and fast-follow candidates above (wherever they sit) alongside the defeat-vector block and the honest-record bullet, and may treat every other, deliverable-describing bullet above the v5 re-spec as history." Either fold the three exempt bullets into the "durable content" enumeration explicitly, or state plainly that "everything above" in this closing sentence means "every deliverable-describing bullet above," matching the rule it is meant to summarize rather than restating the position-only version of it.

## Findings, kept at a lower severity

### 2. The spec's own Re-freeze ledger describes the five records' content as "the whole deliverable" in a way that, read narrowly, omits the carried-forward candidates
- **File**: `.shell-team/specs/T-1046-ignored-base-verdict.md:50` (Re-freeze ledger, row 1, "new[i]" cell for the Goal sentence).
- The cell reads: "no file the plugin ships changes at all; the change set is five records, and what they carry — two defeated designs, six named defeat vectors, an unanswered issue — is the whole deliverable." Read as an exhaustive enumeration, this omits the two open issue candidates and the two fast-follow candidates, even though `D7`, `D10`, `D13` and `AC11` all treat them as part of what the records must carry forward.
- **Not a live defect and not a reader-facing discard instruction.** This is a ledger cell explaining what changed in the Goal sentence at the v4→v5 re-freeze, addressed to a reviewer or future spec-writer reading the re-freeze accounting, not to the "next planning cycle" the board's orientation bullet targets. Nothing in this cell instructs a reader to discard anything; the spec's own preservation passages (`D7`/`D10` at spec lines 122-123, `D13`/`D14` at 124-125, `AC11` at 165, the correspondence table at 208, the full future-input list in `## Open questions`) are unaffected by this cell's wording and remain the operative text.
- **Severity: Minor.** Distinguished from finding 1 by audience and effect: this is a wording imprecision in an accounting table addressed to a narrower, more careful reader, with no discard instruction attached and no contradiction with any other passage — the spec's own preservation sections stand on their own regardless of this cell's phrasing.
- **Suggested fix**: broaden the cell's parenthetical to note that the records' durable content also includes the carried-forward open-issue and fast-follow candidates, or leave it as an intentionally narrative (not enumerative) description — either resolves the ambiguity Codex raised.

## Blockers
None.

## Major
- Finding 1 (`.shell-team/todo.md:15`'s own closing sentence — "treat everything above as history" and "the durable content of this entry is [X and Y]" — both restate, unqualified, the exact sweep the same bullet's own carve-out was written to defeat).

## Minor / nits
- Finding 2 (spec Re-freeze ledger row 1, `.shell-team/specs/T-1046-ignored-base-verdict.md:50` — "is the whole deliverable" reads as an exhaustive enumeration that omits the carried-forward candidates; no live discard instruction, no contradiction with the spec's own preservation passages).
- Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7` anchor-presence-vs-byte-verification gaps) is **unchanged and re-confirmed correctly carried** this round: the spec's hash is identical to round 4's and round 5's (`96a0c36…`), so nothing about these four criteria's text changed; it remains at its existing file-an-issue disposition from round 4, not re-litigated here.

## Adversarial findings
The explicit adversarial `codex exec` pass, told to read the entire orientation bullet closely (including its closing sentence), sweep the rest of the board entry and the whole spec for any other instance of the same class, and check the "durable content" sentence separately, independently reproduced finding 1 from two angles in the same sentence (the "treat everything above as history" clause and the "durable content is [X and Y]" clause read as exhaustive), reported nothing else on the board (explicitly confirming lines 17 and 49-50 "themselves clearly describe live future inputs" and finding no other unqualified position-based claim in lines 16-73), and additionally flagged finding 2 (the spec's Re-freeze ledger wording) as a Minor, standalone item — which I evaluated on its own merits above and kept at the same severity for a narrower reason than the pass's own framing (the pass called it "ambiguous about whether carried-forward content belongs to the durable records"; I score it Minor because it is a ledger accounting cell with no discard instruction and no effect on the spec's own preservation passages, not because the ambiguity itself is trivial).

## Synthesis audit ledger
| # | finding | primary / adversarial judgment | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding 1 (closing sentence "treat everything above as history" / "durable content is [X and Y]") | primary: native `[P2]` (maps to major per the fixed normalization table, scoped to the closing-sentence clause only); adversarial: native `major` on both clauses independently | Major | Both passes agree at the same normalized severity on the same sentence; this role's own re-reading of the byte-identical pre/post-repair text in `git show 80bf8f0` independently confirms the clause was left untouched by the round-5 repair. No change from either pass's judgment; scored at the same level rounds 4 and 5 used for this class (real, live, reaching the disposition's intended reader), not Blocker (no mechanism-invariant defeat) and not Minor (this is the same reachable-audience defect class as round 5's Major, only partially closed, not a cosmetic aside). |
| 2 | Finding 2 (spec Re-freeze ledger row 1 wording) | adversarial: native `minor` | Minor | No change — adversarial-only finding, kept at its native severity for the reasons stated in the finding text above (accounting-table wording, no discard instruction, no contradiction with the spec's own preservation passages). |

Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7`) draws no ledger row this round: neither pass raised it, this role re-confirmed it is unchanged from round 4's own record, and it is carried forward at its existing severity and disposition without re-scoring.

## Environmentally-unverified issue-canon reliance
GitHub issues `#167` and `#67` were not independently read by this role (no network/`gh` access in this sandbox, the same limitation every prior role on this task discloses): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- **REQUEST_CHANGES** → route `.shell-team/todo.md`'s T-1046 entry back to `READY_FOR_ENG` with finding 1 (ordinary rework on the board's own prose, no re-freeze implicated — `.shell-team/todo.md` sits entirely outside the spec's frozen intent block). The fix is narrow and confined to the one bullet's closing sentence: qualify "treat everything above as history" and the "durable content is [X and Y]" enumeration so neither restates, unqualified, the sweep the same bullet's own carve-out already defeats. Finding 2 (the spec's Re-freeze ledger wording) is named for the record and does not block this round's verdict on its own — see Fast-follow disposition below. Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7` anchor-presence gaps) remains unchanged, not re-opened, not escalated, and not blocking this round's verdict.

## Fast-follow disposition
- Finding 2 (spec Re-freeze ledger row 1's "is the whole deliverable" wording) — won't-fix as a standalone item: it is a narrative accounting-table cell addressed to a re-freeze reviewer, not a reader-facing discard instruction, it does not contradict any of the spec's own preservation passages (`D7`/`D10`, `D13`/`D14`, `AC11`, the correspondence table, `## Open questions`), and per this task's own Non-goals ("No third rewrite of the adoption documentation… a third round of wording fixes… is the runaway it exists to stop" — the same restraint this task states for its shipped documentation applies with equal force to further polish on its own now-frozen spec prose outside the intent block). If finding 1's board fix is revisited anyway, folding this wording tweak into the same pass is cheaper than tracking it separately, but it is not worth its own round.
- Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7` anchor-presence-vs-byte-verification gaps) — unchanged from round 4's own file-an-issue disposition; not re-stated as a new deferral here, carried forward as-is.

---

### Codex Review verdict: REQUEST_CHANGES — round 7
- Task: T-1046
- Codex model: gpt-5.6-sol (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: static-only
- Diff scope: `git diff --stat d825293...HEAD` — 5 files, +907/-0 lines (cumulative against base). HEAD `cbf7223`, base `d825293`. No re-freeze this round: the spec is byte-identical to rounds 4-6's (`96a0c36937b6c12f497baa250f8ebdbc041a23e8`) — independently confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md`: `aligned: T-1046 v5`. Only `.shell-team/todo.md` (one bullet, line 15 — a single-line replace) and this review file changed since round 6's HEAD (`80bf8f0`): `git diff --stat 80bf8f0..cbf7223` shows exactly those two files, matching the coordinating session's own description of commit `cbf7223`.

**What this round reviews.** Both same-class-2 contracts remain closed; v5 opens no new one. Per the contract's own text and this round's brief, a further finding in the records is **ordinary rework on a record, not a fallback trigger** — no consequence-weighing applies, findings are reported plainly, and this round does not manufacture a finding to extend the sequence nor downgrade a real one to end it. This round's specific mandate: is the orientation bullet now internally consistent read end to end (the defect class rounds 4, 5 and 6 each found in it); does any other part of the entry or the spec still contradict it; re-confirm the deliverable is untouched; re-confirm the two carried Minors still read right.

## What was run
Two `codex exec` passes against the real working tree, both foreground, neither pasted a diff or excerpt into the prompt:
1. **Primary** — `codex exec --sandbox read-only --cd <repo> review --base d825293 --json -o <file>`. It read the diff and `.shell-team/todo.md` directly and returned two findings, both on `.shell-team/todo.md`. Raw capture: `.shell-team/reviews/T-1046-codex-primary.{txt,jsonl}`.
2. **Adversarial** — triggered by the primary pass's two Major findings, per step 5: a free-form `codex exec` pass told explicitly to (a) read the whole orientation bullet fresh and judge whether a *third* self-contradiction of the round-4/5/6 class survives; (b) check the T-1046 entry, the adjacent T-1042 Done entry in the same file, and the spec's frozen intent block for anything contradicting the bullet's stated disposition of #167 or the sprint goal item; (c) not take the bullet's own claims at face value — verify by reading the actual referenced lines. It reported the position/content-type carve-out **is now internally self-consistent** (closing round 4/5/6's specific defect class), found no third self-contradiction of that class, and independently reproduced both of the primary pass's findings at native `Major`, citing the identical lines (15, and 78-79) and the identical grounding text (spec lines 115, 125-126, 163; the "honest record" bullet at line 68). Raw capture: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`.

Every finding kept below was **independently re-verified by this role** by reading the actual board and spec text at this HEAD before being kept — this is a prose/self-consistency and cross-record-accuracy defect class, not an executable claim, so verification is direct textual re-reading (`sed`/`grep -n` against real line numbers) rather than a live fixture.

## Mechanical gates and independent measurements, re-run live
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v5 (96a0c36937b6c12f497baa250f8ebdbc041a23e8)`.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1046-ignored-base-verdict.md` — **12/12 live PASS**, re-run fresh by this role (full per-criterion output captured).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — conformant (8 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — conformant (1 entry, 0 sentinel).
- `bash bin/check-pii-shapes.sh --base d825293` — clean.
- `bash bin/check-handoff.sh .shell-team/todo.md` — run live three times by this role (twice directly, once as part of AC9's own `check:` line): **exit 0 all three times**, one directly timed with `/usr/bin/time -p`: **224.15s real / 223.43s user / 0.39s sys.** The task prompt's operational estimate of "55-57 seconds" does not match this live measurement, nor round 6's own directly-measured `~221.56s` on this same 1.29 MB board — the two independent measurements (round 6's and this round's) agree with each other and disagree with the estimate by roughly 4x, not scored as a finding since the checker's own exit code (0, all three runs) is what every AC depends on, and is recorded here so a future round does not budget a shorter timeout against this operation.
- **Whole-tree byte-identity, re-derived by this role**: `git diff --name-only d825293 -- bin tests docs templates agents skills .github README.md README.ja.md .gitignore` — empty; `git status --porcelain --untracked-files=all` (repo-wide, no pathspec restriction) — empty beyond the gitignored raw Codex captures. `cmp` against `git show d825293:<path>`, all seven files (`bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md`, `docs/adopting.ja.md`) — **IDENTICAL**.
- **Diff scope**: `git diff --name-only d825293...HEAD` — exactly five lines, matching AC3's allow-list.
- **The repair's own diff, read directly** (`git show cbf7223 -- .shell-team/todo.md`): confirms the coordinating session's own description — a single-line rewrite of the orientation bullet at line 15, closing sentence rewritten to name four durable things instead of two and to state explicitly (in-line) that the two carried-forward classes sit above the defeat-vector block, plus a small fix to "Those three" (no stated antecedent count) becoming "Those two classes span three bullets in total." Nothing else in the entry, and no other file, is touched.
- **Independent count check on the "three bullets" claim**: located the three bullets by content (`issue-filing disposition:` at line 17; the two `fast-follow candidate:` bullets at lines 49-50) and confirmed all three sit above the `defeat vector 1` bullet (line 61) and the `the honest record` bullet (line 68) — the closing sentence's own claim ("these sit *above* the defeat-vector block") is literally true, not merely plausible.
- **Independent grep for the defect class rounds 4-6 found**: `grep -n "everything above\|everything before\|treat everything"` over the whole board file returns exactly two hits — line 15 (this bullet, now containing the phrase only inside an explicitly negated counter-example: "any rule phrased as ... would discard them, which is why this sentence does not say that") and line 961 (an unrelated T-1011 entry's byte-identity note, "everything above the heading line is byte-identical to base"). No third live occurrence of the swept-position defect survives.

## Findings

### 1. The orientation bullet's own "current and only correct disposition" summary names only `.shell-team/reviews/T-1046.md` as #167's re-filing evidence, omitting `.shell-team/reviews/T-1042.md`, which every other passage that states this disposition (the entry's own "the honest record" bullet, and the spec's Goal, D15 and AC10) requires
- **File**: `.shell-team/todo.md:15`.
- **What the bullet says, verbatim**: "The current and only correct disposition, stated here so no reader has to reconstruct it from the chronology: ... **#167 is left open as unanswered**, re-filed with `.shell-team/reviews/T-1046.md` as its evidence; ..." — one file named.
- **What every other passage carrying the identical disposition says**: line 68 ("the honest record, in the words the contract used"), the bullet AC10's own `check:` line locks onto with a chained triple-grep, reads "re-filed with both review records as evidence — `.shell-team/reviews/T-1042.md` and `.shell-team/reviews/T-1046.md`." The spec's Goal (line 115), D15 (line 126) and the `## Summarized sources` section all likewise say "both review records." Confirmed by direct read at each cited location, not by trusting the bullet's or the spec's own cross-reference.
- **Why this is a real gap and not a restatement**: this bullet's explicit stated purpose is to be sufficient on its own — "so no reader has to reconstruct it from the chronology" — for exactly the audience (the next planning cycle deciding what to do about #167) the spec's Input space names as who AC10, and by the same reasoning this bullet, exist for. A reader who trusts that stated sufficiency and stops at line 15 receives an incomplete evidence citation for the re-filing: `.shell-team/reviews/T-1042.md` carries independent, load-bearing content this task's own `## Summarized sources` names explicitly (the acquisition layer's Blocker finding, the four mechanism defeat vectors) that is not duplicated anywhere in `.shell-team/reviews/T-1046.md`.
- **Reachability**: identical audience to every prior round's Major finding on this bullet — the spec's own Input space names "the human readers of the board entry: the next planning cycle deciding what to do about #167." This is the exact bullet engineered three rounds running to be that reader's single point of truth; its own completeness claim is what makes the omission reach that reader rather than being absorbed by context elsewhere.
- **Severity: Major.** Both the primary and the adversarial `codex exec` passes independently scored this native `Major`; this role's own re-derivation (direct read of lines 68, 115, 125-126, 163) confirms the citation set really is inconsistent between this bullet and every other passage stating the same fact. Not Blocker: nothing here defeats a mechanism's central invariant (none exists), and the *substantive* disposition (open, unanswered, NOT MET) is stated correctly here — only the evidence-file list is incomplete.
- **This does not reopen or extend any same-class-2 contract.** Both contracts are closed and v5 opens no new one; this is ordinary rework on a record — correct the one clause, then re-run `bin/check-handoff.sh`.
- **Suggested fix**: replace the clause with the same wording line 68 already uses — "re-filed with both review records as evidence — `.shell-team/reviews/T-1042.md` and `.shell-team/reviews/T-1046.md`."

### 2. The T-1042 Done entry, in the same board file, twice asserts as settled fact that "#167 closes when T-1046 merges" — a claim T-1046's own record, at every version since v3, states is false
- **File**: `.shell-team/todo.md:78` (the T-1042 Done entry's `closed:` line, which contains the claim twice: once inside the `Half A descoped...` parenthetical, and once as the standalone clause "#167 closes when T-1046 merges").
- **What T-1042's own record says, unqualified, with no hedge or superseded marker**: "closed: 2026-08-07, PR #184 → develop ... **closes neither #167 nor #67** (Half A descoped to successor task T-1046 by the fired same-class-2 disposition (i); **#167 closes when T-1046 merges**; #67 stays open for its remaining surfaces) ..." Confirmed by direct read at line 78, not inferred.
- **Why this is a live, symmetric contradiction rather than a stale historical note that resolves itself**: T-1046's own entry (line 68, "the honest record") and its frozen spec (Goal, D15, Non-goals: "No claim that #167 is answered") state, as the terminal, current outcome, that **#167 is left open as unanswered** and the sprint goal item is **NOT MET** — the direct opposite of "#167 closes when T-1046 merges." This is not merely inconsistent phrasing; it is two board entries in the same file asserting mutually exclusive facts about the same, currently-open GitHub issue, with T-1042's claim carrying no qualifier that would let a reader recognize it as superseded. The exact analogous problem was already found and fixed once in this file: T-1046's own "source" bullet (line 16) originally made the identical claim ("#167 closes when this task merges") and now carries an explicit `[SUPERSEDED at v5 — ...]` marker correcting it. No equivalent marker was ever added to T-1042's entry, which makes the identical claim and was never revisited once T-1046's actual outcome became known.
- **Reachability**: the next planning cycle — the exact reader this task's `## Same-class-2 pre-commitment` and `D15` name — deciding "what, if anything, to do about" #167 is at real risk of consulting T-1042's Done entry (the chronologically earlier, already-closed record narrating "Half A descoped to successor task T-1046") and taking its unqualified "#167 closes when T-1046 merges" at face value, concluding no further action is needed once this PR merges — the opposite of the correct, deliberately-recorded outcome.
- **Severity: Major**, scored at the same level this task's own precedent uses for a reader-facing false claim about a live disposition (round 1's findings 1-2, on the shipped documentation's false claims): real, live, reaching the exact reader the disposition exists to inform, on the single most load-bearing fact this task's records carry (is #167 answered or not). Not Blocker: nothing here defeats a mechanism's central invariant, and it does not touch the frozen intent block of either task's spec — it is board prose, correctable in place.
- **This does not reopen or extend any same-class-2 contract**, and it does not require touching any file outside the already-permitted `.shell-team/todo.md` — T-1042's Done entry lives in the same board file AC3 already allows this task to modify. Per the contract's own text, this is ordinary rework on a record: correct the claim and re-run the gate. No re-freeze is implicated on either task's spec.
- **Suggested fix**: mark both occurrences superseded with the same pattern the source bullet at line 16 already uses, e.g. append "**[SUPERSEDED — #167 does NOT close when T-1046 merges; T-1046 shipped no answer and left #167 open, unanswered; see `.shell-team/todo.md`'s T-1046 entry and `.shell-team/reviews/T-1042.md`/`T-1046.md`.]**" to the `closed:` line.

## Blockers
None.

## Major
- Finding 1 (`.shell-team/todo.md:15`'s "current and only correct disposition" names only `.shell-team/reviews/T-1046.md`, omitting `.shell-team/reviews/T-1042.md` that every other passage stating the same disposition requires).
- Finding 2 (`.shell-team/todo.md:78`, the T-1042 Done entry's unqualified, twice-stated "#167 closes when T-1046 merges," contradicting T-1046's own recorded terminal disposition that #167 is left open, unanswered).

## Minor / nits
- Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7` anchor-presence-vs-byte-verification gaps) — **re-confirmed unchanged this round**: the spec's hash is identical to rounds 4-6's (`96a0c36…`), so nothing about these four criteria's text changed; it remains at its existing file-an-issue disposition, not re-litigated here.
- Round 6's finding 2 (spec Re-freeze ledger row 1, `.shell-team/specs/T-1046-ignored-base-verdict.md:50` — "is the whole deliverable" reads as an exhaustive enumeration that omits the carried-forward candidates) — **re-confirmed unchanged this round**: the spec file is untouched since round 6 (byte-identical, same intent hash); it remains at round 6's own won't-fix disposition, not re-litigated here.

## Findings not kept
Neither `codex exec` pass reported a third self-contradiction of the round-4/5/6 defect class (position-based sweep vs. content-type carve-out) within the orientation bullet itself. This role's own independent re-derivation (the "three bullets" count, the "above the defeat-vector block" claim, and the whole-file grep for the swept-position phrase) agrees: that specific, three-round-recurring defect class is closed by commit `cbf7223`. No finding is manufactured to extend the round count, and none of round 4/5/6's original findings are re-opened.

## Adversarial findings
The explicit adversarial `codex exec` pass, told to read the whole orientation bullet fresh, judge whether a third self-contradiction of the round-4/5/6 class survives, and separately sweep the T-1046 entry, the adjacent T-1042 entry and the spec's frozen intent block for anything contradicting the bullet's stated disposition, reported the position/content-type carve-out **is now internally self-consistent** (explicitly answering this round's mandate item 1), found no third instance of the swept-position defect, and independently reproduced both of the primary pass's findings at native `Major`, citing the identical lines and the identical grounding passages (spec lines 115, 125-126, 163; board line 68) this role kept.

## Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding 1 (line 15's disposition summary names only `T-1046.md`, omitting `T-1042.md`) | primary: native `[P2]` (maps to major per the fixed normalization table); adversarial: native `Major` independently, citing the identical line and the identical spec/board grounding | Major | Both passes agree at the same normalized severity on the same evidence; this role's own direct read of lines 68, 115, 125-126 and 163 confirms the citation set really is inconsistent between this bullet and every other passage stating the identical disposition. Scored at the level this task's own precedent uses for a reachable, disposition-relevant summary gap (not Blocker: no mechanism-invariant defeat; not Minor: the omission sits in the one passage engineered to be the reader's single point of truth, unlike round 6's spec-ledger-cell finding of the same shape, which was addressed to a narrower re-freeze-reviewer audience with the full information available elsewhere in the same document). No change from either pass's judgment. |
| 2 | Finding 2 (T-1042 Done entry's unqualified "#167 closes when T-1046 merges") | primary: native `[P2]` (maps to major); adversarial: native `Major` independently, citing the identical lines (78-79) and the identical contradiction with board line 68 and the frozen intent | Major | Both passes agree at the same normalized severity on the same evidence; this role's own direct read of line 78 confirms the claim is unqualified and carries no superseded marker, unlike the symmetric claim in T-1046's own source bullet (line 16), which already does. Scored at the level this task's own precedent uses for a reader-facing false disposition claim (round 1, findings 1-2): real, live, reaching the exact reader (the next planning cycle) the disposition exists to inform. No change from either pass's judgment. |

## Environmentally-unverified issue-canon reliance
GitHub issues `#167` and `#67` were not independently read by this role (no network/`gh` access in this sandbox, the same limitation every prior role on this task discloses): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- **REQUEST_CHANGES** → route `.shell-team/todo.md`'s T-1046 entry back to `READY_FOR_ENG` with findings 1 and 2 (ordinary rework on the board's own prose, no re-freeze implicated on either task's spec — `.shell-team/todo.md` sits entirely outside T-1046's frozen intent block, and neither fix touches T-1042's frozen intent block either, since T-1042 is already merged and closed). Both fixes are narrow: finding 1 is a one-clause wording match to line 68's own phrasing; finding 2 is a superseded-marker append to T-1042's `closed:` line, following the exact pattern already used at line 16 for the symmetric claim. Round 4's and round 6's carried Minors remain unchanged, not re-opened, not escalated, and not blocking this round's verdict on their own.

## Fast-follow disposition
no fast-follow deferrals

---

### Codex Review verdict: APPROVE — round 8
- Task: T-1046
- Codex model: gpt-5.6-sol (via `codex exec`, `codex-cli 0.145.0`)
- Verification mode: executed-tests
- Diff scope: `git diff --stat d825293...HEAD` — 5 files, +992/-2 lines (cumulative against base). HEAD `a0c2d1e`, base `d825293`. No re-freeze this round: the spec is byte-identical to rounds 4-7's (`96a0c36937b6c12f497baa250f8ebdbc041a23e8`) — independently confirmed via `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md`: `aligned: T-1046 v5`. Only `.shell-team/todo.md` (two edits: the disposition clause at line 15, both occurrences on T-1042's Done `closed:`/`source:` lines) and this review file changed since round 7's HEAD (`cbf7223`): `git diff --stat cbf7223..a0c2d1e` shows exactly those two files, matching the coordinating session's own description of commit `a0c2d1e`.

**What this round reviews.** Both same-class-2 contracts remain closed; v5 opens no new one. Per the contract's own text, a further finding in the records is **ordinary rework on a record, not a fallback trigger** — no consequence-weighing applies. This round's specific mandate: are round 7's two findings (line 15's single-file evidence citation, T-1042's twice-unmarked "closes when T-1046 merges" claim) actually closed by commit `a0c2d1e`; does the board-wide sweep the author performed unprompted (beyond T-1046's own entry boundary — exactly what round 7's finding 2 exposed) hold up; is the spec now consistent with the board on the evidence question across all passages that state it; is the deliverable still untouched; do the two carried Minors (round 4's AC anchor-presence gaps, round 6's spec ledger wording) still read right.

## What was run
Two `codex exec` passes against the real working tree, both foreground, neither pasted a diff or excerpt into the prompt:
1. **Primary** — `codex exec --sandbox read-only --cd <repo> review --base d825293 --json -o <file>`. It read the spec and the board directly, ran its own `bash bin/check-acs.sh`-adjacent reasoning over `bin/check-acs.sh`'s timeout logic, and its own `git show`/`grep` reads of the historical `git_probe()` code, returning two findings, both tagged natively `[P2]` (maps to Major per the fixed normalization table). Raw capture: `.shell-team/reviews/T-1046-codex-primary.{txt,jsonl}`.
2. **Adversarial** — triggered by the primary pass's two P2/Major-normalized findings, per step 5: a free-form `codex exec` pass explicitly told to build real reproductions rather than reason abstractly (whether GNU `timeout` exists on this machine and what `bin/check-acs.sh`'s exact default-timeout logic is; whether an exported `git`-only shell function actually intercepts `env -i ... git -C ...`), and to state for each finding whether fixing it would change any reader's actual conclusion about #167's disposition or about this deliverable's approval. It independently reproduced both findings as factually correct, and independently scored both **Minor**, stating neither changes any reader's conclusion. Raw capture: `.shell-team/reviews/T-1046-codex-adversarial.{txt,jsonl}`. (A first attempt at this pass hung on an unexpected `Reading additional input from stdin...` state, apparently caused by this role's own prompt-quoting on the retry attempt rather than a sandbox defect; it was abandoned, its two raw files removed, and the pass re-run cleanly with simplified quoting — recorded here rather than silently discarded.)

Every finding kept below was **independently re-verified by this role** against the real repository before being kept, including building my own scratch reproduction for finding B (below) rather than trusting either pass's account.

## Mechanical gates and independent measurements, re-run live
- `bash bin/check-intent.sh .shell-team/specs/T-1046-ignored-base-verdict.md .shell-team/todo.md` — `aligned: T-1046 v5 (96a0c36937b6c12f497baa250f8ebdbc041a23e8)`.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1046-ignored-base-verdict.md` — **12/12 live PASS**, re-run fresh by this role, full per-criterion output captured.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1046.md` — conformant (8 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1046 .shell-team/interventions/T-1046.md` — conformant (1 entry, 0 sentinel).
- `bash bin/check-pii-shapes.sh --base d825293` — clean.
- `bash bin/check-handoff.sh .shell-team/todo.md` — timed directly with `/usr/bin/time`: **223.39s user / 0.39s sys / 3:43.82 total**, exit 0. Matches the coordinating session's own re-measurement (226s at 1,336,811 bytes) and rounds 6/7's independent measurements (~221-224s) to within normal variance — the same order of magnitude, not a new number. This machine has no GNU `timeout` on `PATH` (`command -v timeout` → rc=1), so `bin/check-acs.sh`'s per-criterion cap is never actually enforced here, which is why every live `check-acs.sh` run in this task's history (including mine, above) has produced a clean result under `CHECK_ACS_TIMEOUT=300` regardless of whether the override is actually load-bearing on this specific host.
- **Whole-tree byte-identity, re-derived by this role**: `git diff --name-only d825293` restricted to the ten shipped pathspecs — empty; `git status --porcelain --untracked-files=all` (repo-wide, no pathspec restriction) — empty. `cmp` against `git show d825293:<path>`, all seven files (`bin/team-init.sh`, `bin/team-paths.sh`, `tests/team-init/run.sh`, `tests/errexit-safe/run.sh`, `.shell-team/test-recipe.md`, `docs/adopting.md`, `docs/adopting.ja.md`) — **IDENTICAL**, one at a time.
- **Diff scope**: `git diff --name-only d825293` — exactly five lines, matching AC3's allow-list.
- **The repair's own diff, read directly** (`git show a0c2d1e -- .shell-team/todo.md`): confirms the coordinating session's own description exactly — line 15's disposition clause now reads "re-filed with **both** review records as its evidence — `.shell-team/reviews/T-1042.md` and `.shell-team/reviews/T-1046.md`, because the two designs it defeated were specified across both tasks and either record alone understates why the question is hard"; T-1042's Done entry gains two `[SUPERSEDED 2026-08-07 — ...]` markers, one on the `closed:` line's "#167 closes when T-1046 merges" clause and one on the `source:` line's identical clause. Nothing else in the entry, and no other file, is touched.

## Round 7's findings, re-verified closed
1. **Finding 1 (line 15 names only `T-1046.md` as #167's re-filing evidence) — confirmed closed.** Line 15 now names both `.shell-team/reviews/T-1042.md` and `.shell-team/reviews/T-1046.md`, matching line 68's own phrasing exactly.
2. **Finding 2 (T-1042's Done entry twice asserts "#167 closes when T-1046 merges," unmarked) — confirmed closed.** Both occurrences (the `closed:` line's parenthetical and the `source:` line's standalone clause) now carry `[SUPERSEDED 2026-08-07 — ...]` markers, each correctly stating the reverse fact (T-1046 shipped no change; #167 stays open) and pointing at T-1046's own `the honest record` bullet.

## The board-wide sweep, independently re-derived (item 2 of this round's mandate)
The author's commit message states the sweep found `as answered` (inside the v1 pre-commitment bullet, already carrying its own FIRED/rewritten marker) and three `mechanised` occurrences (all in unrelated tasks), and states no further unmarked instance survives. I re-derived this myself rather than trusting the narration:
- `grep -n "closes when" .shell-team/todo.md` — exactly two hits, both already fixed: line 16 (T-1046's own source bullet, `[SUPERSEDED at v5 — ...]`, unchanged since round 4) and line 78 (T-1042's `closed:` line, newly marked this commit). No third occurrence anywhere in the file.
- `grep -n "as answered" .shell-team/todo.md` — exactly one hit, line 23, inside the v1 same-class-2 pre-commitment bullet, which opens with its own `(v1, FIRED on cross-provider round 1 and rewritten in v2 — ...)` prefix scoping the whole bullet as superseded; the phrase itself is disposition (b)'s described menu option, not an assertion of current fact. Correctly left alone.
- `grep -no "mechanised" .shell-team/todo.md` — four hits at lines 51, 73, 175, 362. Line 51 is T-1046's own v3-era "what this costs" bullet, which already carries its own `[SUPERSEDED at v5, 2026-08-07 — ...]` marker later in the same bullet. Line 73 is inside the round-5 (v5) QA hand-off's own prose, explicitly narrating that the earlier claim "is self-scoped in its own text... and is superseded... by the `the honest record` bullet" — analysis, not an assertion needing its own marker. Lines 175 and 362 sit inside the unrelated `T-1039` and `T-1033` entries respectively (confirmed against each entry's own task-line boundary), describing lesson-candidate mechanization with no connection to #167. No unmarked instance of the swept-position defect class survives.
- Broader sweep: `grep -n "#167" .shell-team/todo.md` — every occurrence read directly; none asserts a disposition inconsistent with "left open, unanswered" without a superseded marker.

## Spec-vs-board consistency on the evidence question (item 3 of this round's mandate)
Round 7's finding 1 was a mismatch between one passage (board line 15, pre-fix) and four others. Re-checked all five directly at this HEAD:
- Board line 68 ("the honest record"): "re-filed with both review records as evidence — `.shell-team/reviews/T-1042.md` and `.shell-team/reviews/T-1046.md`".
- Board line 15 (this round's fix): "re-filed with **both** review records as its evidence — `.shell-team/reviews/T-1042.md` and `.shell-team/reviews/T-1046.md`, because...".
- Spec Goal (line 115): "re-filed with both review records as its evidence".
- Spec D15 (line 126): "re-filed with both review records named as evidence".
- Spec `## Summarized sources` (line 31): "re-filed with both review records named as its evidence".
All five now agree on the substantive fact (**both** records, both named explicitly); the wording variance across them (`as evidence` / `as its evidence` / `named as evidence`) is stylistic, not a disposition mismatch, and AC10's own exact-string check (`re-filed with both review records as evidence`) still matches only line 68 — unchanged and unaffected by this round's edits, still PASS.

## Findings

### 1. The spec's own text claims the default `CHECK_ACS_TIMEOUT` suffices, which is false given AC9's own measured runtime — but the claim has never actually misled an execution of this task
- **File**: `.shell-team/specs/T-1046-ignored-base-verdict.md:142` (Acceptance criteria preamble, inside the frozen intent block).
- **What it claims**: "No criterion builds a git fixture or runs a git probe — this task ships no command — so the default `CHECK_ACS_TIMEOUT` is expected to suffice." AC9's own `check:` line invokes `bash bin/check-handoff.sh "$B"`, which rounds 4, 6, 7 and this round all independently measured at ~221-226 seconds on this board. `bin/check-acs.sh` (read directly, lines 226-238) defaults each criterion's cap to 120 seconds when GNU `timeout` is on `PATH`. On a Linux host with coreutils' `timeout` present and no `CHECK_ACS_TIMEOUT` override, AC9 would be killed mid-run and report a spurious FAIL.
- **Independently confirmed by both passes and by this role**: this machine has no `timeout` binary (`command -v timeout` → rc=1), so the cap is never enforced here, and every recorded `check-acs.sh` invocation in this task's review history (including mine, above) explicitly used `CHECK_ACS_TIMEOUT=300` rather than the bare default — the practice already in place sidesteps the false claim in every execution this task has actually had.
- **Severity: Minor**, downgraded from both passes' native `[P2]`/normalized-major tag (ledger below). This is a criterion-robustness/documentation-accuracy gap with no live manifestation on the environment this task has run in, in the same family as round 4's finding 2 (anchor-presence-vs-byte-verification gaps, also downgraded to Minor for the identical reason: real, but not yet biting anyone). It does not change the disposition of #167 or the approval conclusion this round is asked to reach.
- **Suggested fix** (not a decision this role makes): reword the sentence to name the actual reason a longer window may be needed (`AC9`'s own `check-handoff.sh` invocation, not a git-fixture/probe concern), or state the `CHECK_ACS_TIMEOUT=300` override explicitly as the expected invocation. Confined to intent-block prose, not a `- check:` line, so a fix here is class-B, not class-M.

### 2. Board defeat vector 3 overstates the historical shell-function-shadowing bypass: an exported `git` function alone did not bypass the retired mechanism, only an exported `env` function did
- **File**: `.shell-team/todo.md` (defeat vector 3 bullet, `- **defeat vector 3 (mechanism): shell-function shadowing of the sanitizer itself.**`).
- **What it claims**: "An exported bash function named `env` — or, identically, `git` — intercepts the sanitizing call before `PATH` is ever consulted... " The historical `git_probe()` (`git show e5e4416:bin/team-init.sh:254`) was the single line `env -i PATH="$PATH" HOME="${HOME:-}" TMPDIR="${TMPDIR:-/tmp}" LC_ALL=C git -C "$TARGET" "$@"`.
- **Independently reproduced by this role**, before either Codex pass's account was trusted: built a scratch fixture exporting a bash function named `git` (not `env`) and ran the identical command shape against a real repository. Result: the real `git` binary answered (`true`/`rev-parse --is-inside-work-tree` succeeded normally), not the shadowed function — because `env` (the external, compiled binary, unshadowed in this case) execs `git` via its own `execvp`/`PATH` resolution, which never consults the calling bash shell's function table at all. Only shadowing `env` itself (which the review record's own round-2-addendum reproduction did, and kept as the task's sole Blocker) intercepts the call; shadowing `git` alone, without also shadowing `env`, does not. The adversarial pass independently reproduced the identical result with its own fixture.
- **This is a real overstatement, carried onto the board from the review record's own round-2-addendum reasoning** ("The same root cause... would identically let an exported function named `git` bypass the mechanism even more directly, without touching `env` at all — not a separate finding, the same defect") — a claim this reviewer's own predecessor made without building the narrower fixture that would have shown it false for this specific invocation shape.
- **Severity: Minor**, downgraded from both passes' native tag (ledger below). The overstatement does not understate the real, already-Blocker-scored vulnerability (exported `env`); it does not change what a future mechanism attempt would need to defend against (avoiding bare, shadow-able command words for both `env` and `git` is still good practice regardless), and it does not affect #167's disposition or this round's approval question. It is the kind of narrow technical footnote inaccuracy this task's own precedent (round 4's finding 2) treats as Minor rather than Major, distinct from the disposition-level falsehoods rounds 1, 3-7 scored Major (this one does not reach the audience deciding #167's fate with a wrong answer about #167 itself — only with a narrower-than-advertised technical detail about a mechanism that no longer exists to defend).
- **Suggested fix** (not a decision this role makes): narrow the clause to "An exported bash function named `env` intercepts the sanitizing call..." and drop the "or, identically, `git`" addition, or add a footnote that the `git`-alone case requires `env` itself to also be shadowed (or the code to invoke `git` as a bare word without an `env -i` wrapper) to be exploitable.

## Blockers
None.

## Major
None.

## Minor / nits
- Finding 1 (spec `## Acceptance criteria` preamble line 142's `CHECK_ACS_TIMEOUT` sufficiency claim — false given AC9's own measured runtime, no live manifestation on this task's actual execution environment).
- Finding 2 (board defeat vector 3's "or, identically, `git`" clause — factually false for the actual `env -i ... git -C ...` invocation shape, independently reproduced; does not understate the real vulnerability).
- Round 4's finding 2 (`AC1`/`AC5`/`AC6`/`AC7` anchor-presence-vs-byte-verification gaps) — **re-confirmed unchanged this round**: spec hash identical to rounds 4-7's; existing file-an-issue disposition carried, not re-litigated.
- Round 6's finding 2 (spec Re-freeze ledger row 1 wording) — **re-confirmed unchanged this round**: spec file untouched since round 6; existing won't-fix disposition carried, not re-litigated.

## Adversarial findings
The explicit adversarial `codex exec` pass, told to build real reproductions (checking for GNU `timeout` on this machine and reading `bin/check-acs.sh`'s exact timeout logic; exporting a `git`-only shell function and running the exact historical invocation shape) rather than reasoning abstractly, independently confirmed both of the primary pass's findings as factually correct, and independently scored both **Minor** on this task's four-level scale, stating for each that fixing it "would not change #167's open/unanswered disposition or the approval conclusion." Its own reproduction of finding B printed the real `git`'s version string, not a shadowed-function marker, matching this role's own independent fixture exactly.

## Synthesis audit ledger
| # | finding | primary / adversarial judgment | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Finding 1 (spec's `CHECK_ACS_TIMEOUT` sufficiency claim) | primary: native `[P2]` (maps to major per the fixed normalization table); adversarial: native `minor`, explicitly stating no reader-conclusion change | Minor | Confirmed factually true (AC9's ~221-226s measured runtime exceeds the 120s default cap when GNU `timeout` is present) but with no live manifestation on the environment this task has actually run in (no `timeout` binary here) and no reader-conclusion change, since every real invocation in this task's history already overrides to 300s. Matches round 4's own precedent for downgrading a real-but-non-manifesting criterion/documentation-accuracy gap to Minor. |
| 2 | Finding 2 (board defeat vector 3's "or, identically, `git`" clause) | primary: native `[P2]` (maps to major); adversarial: native `minor`, explicitly stating no reader-conclusion change | Minor | Confirmed factually false by this role's own independent fixture (exported `git`-only function did not intercept the historical `env -i ... git -C ...` call) and by the adversarial pass's separate reproduction. Downgraded from the normalized major because it overstates rather than understates the real (already-Blocker-scored) vulnerability, does not change what a future mechanism attempt would need to defend against, and does not reach the disposition question rounds 1 and 3-7's Major findings all turned on. |

## Environmentally-unverified issue-canon reliance
GitHub issues `#167` and `#67` were not independently read by this role (no network/`gh` access in this sandbox, the same limitation every prior role on this task discloses): internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation). This does not change the verdict — it is a transparency record, not a severity judgment.

## Recommendation
- **APPROVE** → set `.shell-team/todo.md`'s T-1046 entry to `READY_FOR_MERGE`. Both round-7 findings are confirmed closed against the real diff, not just the commit message; the board-wide sweep the author performed unprompted is independently re-derived and holds; the spec and board now agree on the evidence question everywhere it is stated; the deliverable remains byte-identical to `d825293` across all ten shipped pathspecs, `cmp`-verified on all seven surviving files, with the diff closed to exactly the five record paths; every checker this task depends on (`check-intent`, `check-provenance`, `check-interventions`, `check-pii-shapes`, `check-acs` at 12/12, `check-handoff`) passes live. This round's own two findings are real but narrow — confirmed by an adversarial pass built on live reproduction rather than reasoning from memory, and independently downgraded to Minor by both that pass and this role's own synthesis, because neither changes any reader's conclusion about #167's disposition (left open, unanswered) or about whether this five-record, no-code deliverable should be approved. Per this round's own calibration, a defect that changes no reader's conclusion is a nit deferred as fast-follow, not a trigger for a ninth round.

## Fast-follow disposition
- Finding 1 (spec's `CHECK_ACS_TIMEOUT` sufficiency claim, `.shell-team/specs/T-1046-ignored-base-verdict.md:142`) — file-an-issue: a real, independently-confirmed documentation-accuracy gap inside a frozen intent block (any fix is class-B), worth a small, batched correction whenever this spec's intent block is next touched for another reason, rather than its own re-freeze; not blocking because every actual execution of this task's `check-acs.sh` already overrides the timeout and has never been bitten by the default.
- Finding 2 (board defeat vector 3's "or, identically, `git`" clause) — file-an-issue: a real, independently-confirmed technical overstatement in a durable defeat-vector record `AC10` requires verbatim, worth a small board-prose correction bundled with any other future editorial pass on this entry; not blocking because it overstates rather than understates the real vulnerability and does not change the mechanism's correctly-retired status or #167's disposition.
