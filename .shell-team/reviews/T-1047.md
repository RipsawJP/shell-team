### Codex Review verdict: APPROVE
- Task: T-1047
- Codex model: gpt-5.6-sol (config default in `~/.codex/config.toml`; not printed by the JSONL event stream itself)
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 15 files, +525/-0 lines (`99d4038...HEAD`)

#### Blockers
- None.

#### Major
- None that block merge. See Adversarial findings below for two Major-severity observations that are dispositioned as fast-follow rather than blockers, with reasoning.

#### Minor / nits
- `.shell-team/todo.md` (T-1047's own board entry, "board hygiene" bullet) says `## Active` "was measured empty at this HEAD (`.shell-team/todo.md:12–14`)" — but at the HEAD where that sentence is read, `## Active` is not empty (T-1047's own entry occupies it). The measurement was actually taken against the *base* ref `99d4038`, not "this HEAD." This is the same citation-imprecision class the entry's own AC17(c) note already discloses for two other citations, just a fourth, previously-undisclosed instance of it.
- A recursive layer on the already-disclosed citation imprecision: the rework hand-off's own correction says T-1046's board entry "sits at line 144"; QA's own correction of that says it "begins at line 149"; independently re-measured here, T-1046's actual heading is at line 150, and line 144 belongs to a different task's entry (T-1043, the `agents/pm-spec.md` check-conventions task) entirely, not merely "the wrong line number for the right entry." Three successive self-corrections inside this task's own append-only record, each correcting the previous one and each still slightly wrong — which is a live demonstration of the exact defect class E4 (this task's own promoted lesson) names, occurring inside the very record that promotes it.

#### Adversarial findings
Ran a second `codex exec` pass specifically to stress-test the primary pass's sole finding and to hunt for anything a less skeptical read would miss. Four results, none blocking:

1. **Primary pass's only finding, rejected.** The primary review pass returned one finding (Codex-native `P1`): that leaving three relayed assumptions in the board's AC17(d) report as "relayed, restated as relayed" (the six candidates' adjudicated dispositions; candidate 6's #188/T-1049 destination; candidate 1's disposition) rather than independently re-measured is a violation requiring rework. The adversarial pass, after reading the spec's `M1` frozen block and `## Assumptions` section plus a cross-check against other already-merged board entries using the identical pattern, judged this **not a finding**: `M1` explicitly defines "record as relayed, name the side holding primary confirmation" as the compliant behaviour for a premise unreachable from any repo-local role (a past planning conversation, an unreachable GitHub issue), and this is the same established convention already used identically in T-1043's and other tasks' "environmentally-unverified" disclosures. My own reading agrees: the source is genuinely unreachable from a sandboxed, non-networked role, the disclosure explicitly names the confirming side, and the task's own frozen `R1` forbids re-litigating this exact settled design. Rejected as a defect; the underlying design is working as intended.
2. **P2/major — AC17(b)'s own frozen prediction text is imprecise for probe 1.** AC17(b) predicts probe 1 (a one-word reword inside E1's `Rule`) makes AC10 red; the actual, reproduced result is AC10 stays green, because E1 is wholly new relative to the base ref and a same-line reword of brand-new content produces no deletion for AC10's deletion-column check to catch. **Already disclosed and judged non-blocking**, independently, by both the engineer's rework hand-off and QA's PASS verdict (both explicitly ran the probe, found the same discrepancy, and recorded it as "a real, load-bearing blind spot in AC10... not the defect this probe was meant to catch, and it does not affect AC17(b)'s own pass/fail"). Consistent with this repo's established handling of comparable disclosed criterion blind spots (AC7's own acknowledged blind spot; T-1043's AC6 order-blindness, waived on the record). No new action from me beyond noting the disposition is sound.
3. **P2/major — `E3`'s frozen `How to apply` field says "the verifying role reads for the scoping clause," but `E3`'s `Applies-to` excludes `qa-verifier`.** On inspection this is not an operational gap: only the `Rule` field is ever spliced into a generated playbook block (confirmed in the spec's own Summarized-sources distinctions and in `bin/gen-playbook-blocks.sh`'s behaviour) — `How to apply` never ships to any role's prompt, so `qa-verifier` receiving no delivery of this text is not a missing obligation, it is the corpus's normal mechanism. DP2 also gives an explicit, individually reasoned justification for excluding `qa-verifier` from E3's `Applies-to` ("a criterion written per the rule already tells QA what to check, so adding the role to the block adds a line without adding an action"), which this task's own AC14 instantiates for `M3`. The phrase "the verifying role" in `E3`'s prose is generic (whichever role verifies a given task, via that task's own criterion), not a claim that `qa-verifier`'s prompt carries the sentence. Any wording change to `E3`'s frozen text is out of scope for this task regardless (DP1: every corpus text is frozen byte-for-byte; Non-goals forbids rewording any existing entry). Won't-fix.
4. **Major — `AC12`/`AC13`/`AC14` check *presence within a region*, not the *exact adjacent/last-item position* their own prose (`DP5`/`DP6`/`DP7`) and the Body-to-AC table claim to enforce.** Independently re-verified by hand: `AC12` would still pass if `M1` sat as the *first* bullet of the self-check list instead of the *last* (it only asserts `M1` occurs once somewhere between the two headings); `AC13` would still pass if `M2A`/`M2B` sat anywhere inside their step's region rather than immediately after each step's `Same-class-2` sub-bullet; `AC14` would still pass if another bullet were appended to `## Rules` after `M3` (it only asserts `M3` occurs once from the `## Rules` heading to end-of-file, not that it is the final line). The *committed* placements are all correct — confirmed directly for all three by reading the files — so this is not a defect in what shipped, but a genuine, previously-undisclosed gap in how tightly the frozen criteria protect that placement against a *future* hand-edit that moves the line within the same region. This is exactly the class of verification-mechanism blind spot the corpus's own `E1`/`E2` entries (which this very task promotes) care about, and the task's own classification explicitly asks for higher-standard review of these three criteria. Any fix requires strengthening a `check:` line inside the frozen intent block, which is itself a re-freeze — not an engineer-side rework this task's own contract permits. Consistent with this repo's established handling of comparable disclosed-but-not-currently-manifesting blind spots (AC7's blind spot, AC10's blind spot above, T-1043's AC6 precedent — all disclosed, none blocking), I judge this non-blocking for *this* merge, and disposition it as a fast-follow for pm-spec to tighten in a future re-freeze.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Relayed assumptions in AC17(d) left as "relayed, restated as relayed" | primary: blocker (Codex-native `P1`) | rejected — not a finding | `M1`'s own frozen contract defines relay-and-disclose as the compliant path for a source unreachable from any repo-local role; adversarial pass and my own read of `M1`/`## Assumptions` and cross-task precedent agree; `R1` forbids relitigating this settled design |
| 2 | AC17(b) predicts AC10-red for probe 1; actual result is AC10-green | adversarial: major | minor, already disclosed / non-blocking | Independently found and judged non-blocking by both the engineer's rework hand-off and QA's PASS verdict before this round; matches this repo's established handling of disclosed criterion blind spots (AC7, T-1043's AC6) |
| 3 | E3's `How to apply` names "the verifying role" while `Applies-to` excludes qa-verifier | adversarial: major | minor — won't-fix | Only `Rule` ships to any role's prompt (`How to apply` never does); DP2 already gives an individually reasoned justification for the exclusion; any wording change is out of scope (frozen text, byte-for-byte) |
| 4 | AC12/AC13/AC14 check region-presence, not exact last/adjacent position, despite DP5/DP6/DP7's stronger prose claim | adversarial: major | major, non-blocking fast-follow | Committed placements are independently confirmed correct; the gap only weakens *future*-edit detection, not this diff's correctness; fixing it requires a re-freeze of the frozen intent block, which is pm-spec's job, not an in-task rework; matches established precedent for disclosed-but-inert criterion gaps in this repo (AC7, AC10, T-1043 AC6) |
| 5 | Board hygiene bullet says `## Active` "was measured empty at this HEAD" when the measurement was against the base ref | own finding | minor, fast-follow | Same citation-imprecision class as the already-disclosed AC17(c) notes; confined to mutable board prose, not a frozen block |
| 6 | The rework's and QA's own "citation-precision" corrections about where T-1046's board entry sits are themselves both wrong (144, then 149; actually 150, and 144 belongs to a different task entirely) | own finding | minor, fast-follow | A third layer of the same self-referential citation-imprecision class inside T-1047's own append-only record — ironic given this task promotes exactly the discipline this violates, but confined to meta-citation of line numbers, not to the substance of any Why-clause, which independent re-reads (engineer, QA, this review) all confirm accurate |

None of the six items changes AC1–AC16's live-green status (re-run and confirmed: `bash bin/check-acs.sh .shell-team/specs/T-1047-promote-retro-2026-08-08.md` → 16 passed, 0 failed, 1 skipped), none touches a frozen block's actual bytes, and none reflects a defect in the four corpus entries, the four regenerated blocks, or the four hand-written prompt-surface texts as shipped.

#### Own verification performed this round
- `bash bin/check-acs.sh .shell-team/specs/T-1047-promote-retro-2026-08-08.md` → 16 passed, 0 failed, 1 skipped (AC17), 0 unrecognized.
- `bin/check-intent.sh`, `bin/check-interventions.sh --task T-1047`, `bin/check-provenance.sh`, `bin/check-handoff.sh .shell-team/todo.md` all re-run directly and green.
- Read `agents/codex-reviewer.md`, `agents/pm-spec.md`, `skills/run/SKILL.md`, `agents/tech-lead.md`, `agents/engineer.md`, `agents/qa-verifier.md` and the four generated `templates/prompt-blocks/playbook-*.md` blocks directly to confirm mode-scope symmetry (M2A/step 5, M2B/step 6; M3's `above` is correct given `## Finding-evaluation mode (review-response)` at line 140 precedes `## Rules` at line 155), the generated-vs-hand-written boundary (no hand edit inside any marker region; `agents/tech-lead.md` touched only by its two entitled generated `Rule` lines), and corpus routing (E1→pm-spec/tech-lead, E2→engineer/qa-verifier/tech-lead, E3→engineer/pm-spec, E4→engineer/pm-spec/qa-verifier, all matching DP2 exactly in every diff).
- Read `.shell-team/retros/2026-08-08.md`'s six candidate lines directly and confirmed E1–E4 map to candidates 2–5 respectively, and that candidates 1 and 6 are genuinely absent from the corpus (`AC6` independently re-run, confirmed exactly four new headings matching E1–E4's keys).

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- AC12/AC13/AC14 check region-presence rather than exact last/adjacent position (finding 4 above) — **file-an-issue**: a real, previously-undisclosed verification-mechanism gap in three frozen criteria; the fix is a class-B re-freeze (tighten the `check:` lines to assert last-line/adjacent-line position, e.g. via a last-non-blank-line comparison), which belongs to pm-spec on a future task, not to this one's rework.
- Board hygiene bullet's "at this HEAD" imprecision (finding 5 above) — **file-an-issue**, batched together with the next item: a small, one-pass editorial correction to T-1047's own board entry's citations, consistent with the corpus's own `2026-08-06` "batched editorial pass" precedent for exactly this defect class.
- The recursive T-1046-line-number citation chain (144 → 149 → actually 150; finding 6 above) — **file-an-issue**, same batched pass as the previous item.
- AC17(b)'s AC10-blind-spot prediction text (finding 2) — **won't-fix for now**: already disclosed and judged non-blocking twice (engineer, QA); a standalone re-freeze for this one clause is lower value than folding it into the same future AC12–14/citation batch above, so I am not filing a separate issue for it — note it there instead when that issue is filed.
- E3's `How to apply` wording tension with its `Applies-to` (finding 3) — **won't-fix**: resolved by DP2's own reasoning already in the frozen spec; not worth a future re-freeze on its own.
