### Codex Review verdict: APPROVE
- Task: T-1088
- Codex model: gpt-5-codex (codex-cli)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 5 files, +735/-0 lines (base `69ee536054a2864b3e95449fd2a3524282a9b505` = `git merge-base feature/1087-judge-briefing HEAD`)

#### Confirmation pass (re-derivation from the evidence region + phase-multiplexing.md/tier2-subject-harness.md committed blobs)
Codex independently re-derived, via live `python3`/`git show` commands it ran itself (not pasted): `discard-term = 338206`, `judge-pass-cost = 153356`, `sum = 491562`, `measured-threshold = 0.3408774598…`, `modelled threshold (worked substitution) = 0.7390810688…` (matching both the note and my own independent shell re-derivation below byte-for-byte), and `rounds-avoided = (3−3)/2 = 0`. It recounted `r_winner = 3` and `r_loser = 3` directly from the note's own `- gate-round:` lines (three codex entries each, ending `APPROVE`), confirmed the loser's gates run strictly after the judge pass in both the prose ordering and the telemetry sequence (judge at seq. 8, all gate entries at seq. 9–20), and confirmed no overclaim beyond one firing's license (explicitly checked the "no expected rate" / "no judge-accuracy figure" / "`undetermined` unchanged" sentences and found them all present and correctly scoped). It flagged the gate-instrument-variance limit as "honestly and unusually strongly disclosed." One wording nuance it raised: the Supersession paragraph's "declares... performs none of it" phrasing could, read in isolation, be misread as superseding the general `undetermined` parameter rather than only the documentation-gap claim — surrounding text (and the note's own second Supersession paragraph, which explicitly says the `not-yet` recommendation is *not* superseded) resolves the ambiguity, so this is a nit, not a real overclaim.

I independently re-ran the same arithmetic myself, from the evidence region's raw bytes and `git show 4fff6f78…:docs/loop-engineering/phase-multiplexing.md`, before reading Codex's numbers, and got identical results: `0.34087745978118633` and `0.7390810688673338`. I also independently `cmp`'d the working-tree evidence region against `git show <evidence-sha>:docs/loop-engineering/tier2-firing.md` (74 lines, byte-identical) and independently confirmed the branch-point ⊑ evidence ⊑ HEAD ancestry ordering AC1 requires.

#### Unguided pass (`codex exec ... review --base 69ee536…`, no pre-named focus)
Codex's own `review` subcommand read the diff and repository state itself and returned six findings (three `[P1]`, three `[P2]`). Adjudicated below — two `[P1]`s are rejected as misreadings, one `[P1]` is downgraded, and the three `[P2]`s are real but Minor. None survive as Blocker/Major.

#### Blockers
- none

#### Major
- none

#### Minor / nits
- `.shell-team/provenance/T-1088.md:11-12` — decision-record 3's own `reason`/`grounding` text states "the workflow carries 77 named steps" and cites `grep -n 'name:'` as the counting command. Independently re-run: `grep -c '^      - name: '  .github/workflows/check-handoff.yml` → **76** (the correct, anchored count — matching the board hand-off's own tally `2+1+4+51+13+5=76` and the note's own CI-equivalence report exactly). `grep -n 'name:'` (unanchored) over-counts by 2 (it additionally matches the workflow's own top-level `name: check-handoff` and the job's `name: check-handoff lint` lines). This is a real, confirmed internal inconsistency inside a governance record — the *substantive* classification and tally used everywhere it is load-bearing (the board hand-off, the note) is the correct 76, so no CI-equivalence conclusion is actually wrong, but the provenance record's own stated grounding is factually incorrect and should read 76.
- `docs/loop-engineering/tier2-firing.md:82-83` vs. AC7 item (b)'s literal text ("the liveness-check command for **every** instance, verbatim, with the launch moment it started at") — the evidence region carries exactly two `- liveness:` lines (candidate-01, candidate-02) and none for the judge instance, the six gate instances (three QA/codex-pair rounds × two candidates), or the four rework instances. Confirmed by direct grep against the evidence region — no other `- liveness:` line exists. This is a genuine completeness gap against AC7(b)'s own stated granularity, not one of `## Verdict scope`'s seven in-scope Major categories (it doesn't touch comparator soundness, direction, overclaim, hand-derivation, a frozen-boundary crossing, `bin/`, or a predecessor regression) — every one of the unrecorded instances is independently known to have completed via its own telemetry `duration_ms` row, so no instance in fact ran unbounded; the gap is in the audit trail's completeness, not in the arm's actual safety.
- `docs/loop-engineering/tier2-firing.md:142` — the evidence region's single aggregate `- cleanliness:` line ("real checkout... both read clean after venue construction, after candidate completion, after the judge pass and after the gate rounds") satisfies AC7(c)'s "reported with the stage it followed" clause (it does name all four stages), but never discloses the teardown disposition of the scratch clones/venues (`$TMPDIR/t1088arm/clone-{01,02}`, `venue-{01,02}`) as AC7(c) and the frozen protocol's own "a scratch clone whose removal is declined is disclosed as retained rather than forced" sentence require, and does not show a `bash bin/check-pii-shapes.sh` invocation run specifically against the evidence region before its own first commit (the PII sweep that *is* recorded — QA's `check-pii-shapes.sh --base develop → clean` — is a diff-scoped sweep run later, not the per-record sweep AC7(c) itself describes). Real gap in the self-run report's completeness; does not touch the measurement's substantive correctness.
- `.shell-team/specs/T-1088-tier2-firing.md:248` — the Blast radius derivation's `ALL=` command carries nine `-e <path>` flags (the contract source, harness note, briefing, five subject-01 files, and the note's own path) but none of the four predecessor spec paths themselves (`.shell-team/specs/T-1086-subject-harness.md` etc.) as independent search targets — a spec that referenced one of those four *spec paths* as a bare string without independently mentioning any of the nine already-searched paths would be invisible to this derivation. Checked empirically: `git grep -F -l -e "T-1086-subject-harness.md" -e "T-1087-judge-briefing.md" -e "T-1089-harness-recut.md" -e "T-1090-sign-safety-close.md" -- .shell-team/specs/` returns only the four specs themselves plus T-1088's own — the hypothetical gap is real in principle but empirically empty today, and is already substantially mitigated by AC7 item (d)'s own explicit backstop ("T-1090's, T-1089's, T-1087's, T-1086's and T-1079's full criterion sets are run at both refs regardless of what the literal-byte derivation returns"). Worth hardening the derivation's own `-e` list in a future task that reuses this pattern.

#### Adversarial findings
- Not separately triggered as a third pass: no Blocker/Major survived adjudication from either the confirmation or unguided pass, so the opt-in adversarial pass condition (step 5) was not met. The confirmation pass's own targeted stress-test of the comparator's isolation ordering (loser strictly after the judge, verified against both the prose and the telemetry sequence) and of the overclaim boundary (explicitly hunting for a sentence exceeding one firing's license and finding none) substantively covers the two highest-priority in-scope Major categories from `## Verdict scope` without requiring a further round.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | judge-input contract (`candidate-diff`/`frozen-criteria`/`check-results`) unsatisfiable from only the two venue paths | unguided: P1 (blocker per normalization) | rejected | the judge is launched as `subagent_type=general-purpose` with the loop's default tool access (no `agents/judge.md` restricts it), the extracted briefing region itself instructs "read the inputs below for each candidate" and the `score-domain: ac-pass-completeness` line instructs reading `acceptance.sh`'s own printed output per candidate, and each venue is deliberately `git init`'d over the four regenerated files specifically so `candidate-diff` is self-computable by whoever holds the venue path — the finding assumes a passive, text-fed judge that contradicts the protocol's own stated design rationale for the venue's `git init` step |
| 2 | per-candidate verdict line required (one `- judge-pick:` line per candidate) | unguided: P1 (blocker per normalization) | rejected | contradicts AC2's own explicit, mechanically-verified requirement of **exactly one** `- judge-pick:` line at column 0 in the whole note (re-run live: `check-acs.sh` reports AC2 PASS); the briefing's `per-criterion-score` output field already carries both candidates' comparative values inline within that one line (`test-depth=partial(c02)>absent(c01)`), so no candidate's information is actually omitted |
| 3 | missing liveness/timeout records for non-candidate instances (judge, gates, rework) | unguided: P1 (blocker per normalization) | minor | real completeness gap against AC7(b)'s literal text, confirmed by direct grep (only 2 of ~13 instances have a `- liveness:` line) — but not one of `## Verdict scope`'s seven in-scope Major categories, and every instance's own telemetry `duration_ms` independently shows it completed, so the gap is in audit-trail completeness rather than in the arm's actual bounded-execution safety |
| 4 | aggregate cleanliness line omits scratch-clone teardown disposition and a per-record PII sweep | unguided: P2 (major per normalization) | minor | real gap against AC7(c)'s stated granularity; does not touch comparator soundness, direction, overclaim, hand-derivation, a frozen-boundary crossing, `bin/`, or a predecessor regression, and the substantive safety property (real checkout stayed clean) is independently evidenced at all four stages |
| 5 | Blast-radius `-e` list omits the four predecessor spec paths as search targets | unguided: P2 (major per normalization) | minor | real in-principle indirection gap, confirmed empirically empty (no additional spec surfaces via those four extra paths) and already substantially mitigated by AC7 item (d)'s explicit "run these five regardless" backstop |
| 6 | provenance.md states "77 named steps" against the correct 76 | unguided: P2 (major per normalization) | minor | confirmed factually wrong via independent `grep -c`; the correct figure (76) is the one actually used in every load-bearing place (board hand-off tally, note's CI-equivalence report), so no CI-equivalence conclusion is affected — this is a governance-record accuracy defect, not a substantive finding |
| 7 | Supersession paragraph's "declares... performs none of it" phrasing, read in isolation, could be misread as superseding the general `undetermined` parameter | confirmation: nit | nit | surrounding text (including the note's own second Supersession paragraph, which explicitly states the `not-yet` recommendation is not superseded) resolves the ambiguity for a reader of the whole section |

#### Recommendation
- APPROVE → set tasks/todo.md to READY_FOR_MERGE

#### Fast-follow disposition
- provenance.md's "77 named steps" grounding error (ledger #6) — file-an-issue: a one-line factual correction to a durable governance record, cheap to track, does not touch any check line or gate.
- missing liveness/timeout records for non-candidate instances (ledger #3) — file-an-issue: a process-completeness gap worth closing before the *next* Tier-2 firing (the note's own Follow-ups section anticipates a second firing), since AC7(b)'s own text already commits to this granularity.
- aggregate cleanliness line's missing teardown/PII-sweep detail (ledger #4) — won't-fix: the underlying safety property is independently evidenced at all four stages; itemizing scratch-clone teardown is a nice-to-have for a future firing's evidence template, not a defect in this one's own validity.
- Blast-radius `-e` list narrowness (ledger #5) — file-an-issue: small, concrete hardening (add the four predecessor spec paths as additional `-e` flags) for any future task reusing this derivation pattern; confirmed empirically non-materializing here.
- Supersession paragraph wording nuance (ledger #7) — won't-fix: resolved by the surrounding text; not misleading to a reader of the whole section.

#### Pre-commitment status
The never-dropped firing chain is fully present and independently confirmed: two real concurrent candidate instances (telemetry seq. 6–7), the oracle run per candidate (`acceptance: 11 passed, 0 failed` both, exit 0), the judge pass through the extracted briefing region (seq. 8, all three conduct checks PASS), the picked candidate through both real gates to `APPROVE` (candidate-02, 3 rounds), and the byte-locked evidence commit (independently `cmp`'d byte-identical, ancestry independently verified). No independent Blocker or Major landed on any component of the firing chain in this round (all six raw unguided findings adjudicated to rejected or Minor), so nothing invokes the pre-commitment's stop-and-route-to-#307 branch, and neither droppable component (the loser's gate pass; the cap `K`) was needed.
