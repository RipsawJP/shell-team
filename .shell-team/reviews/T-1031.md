# T-1031 — QA verification record

## QA verdict: PASS

**Scope**: verify the engineer's hand-off (commit `91b97a8` — the flag-grammar
fix, D1-D8 — plus the orchestrator's interventions-sentinel commit `b30ad68`)
against the frozen spec
(`.shell-team/specs/T-1031-check-handoff-flag-anchor.md`, intent-hash v1
`e6f3295d77698bf4ceafec97da28a4001a42a681`), branch
`feature/122-check-handoff-flag-anchor` (base `1686495`), `READY_FOR_QA` ->
`READY_FOR_REVIEW`. Every `check:` line was re-run live against the real
checkout; the mutation self-check, the R2 before/after flip, and every
`## Summarized sources` restatement were independently re-derived rather than
taken from the provenance file's report.

**Lens**: this IS an executable-artifact task — `bin/check-handoff.sh`'s
grammar changed — so the adversarial fixture-synthesis checklist applies in
full, not just the acceptance-criteria walkthrough.

### Mandatory conformance gates

- `check-intent.sh .shell-team/specs/T-1031-check-handoff-flag-anchor.md .shell-team/todo.md`
  -> `aligned: T-1031 v1 (e6f3295d...)`.
- Stacked neighbours re-confirmed aligned against the same edited board:
  T-1027 v1 (`828a7194...`), T-1028 v2 (`465f91ff...`), T-1029 v2
  (`aa47eaa0...`), T-1030 v1 (`d0f73437...`) — all four unchanged.
- `check-provenance.sh .shell-team/provenance/T-1031.md` -> `conformant: ...
  (2 decision entries, 0 sentinel)`.
- `check-interventions.sh --task T-1031 .shell-team/interventions/T-1031.md`
  -> `conformant: ... (0 entries, 1 sentinel)` — judged structurally only,
  per this repo's rule (not whether "no interventions" was the correct class
  to claim — that belongs to the retro).

### `check-acs.sh` shape (item 1) — before and after this record

- **Before** this review file existed: `CHECK_ACS_TIMEOUT=900 bash
  bin/check-acs.sh .shell-team/specs/T-1031-check-handoff-flag-anchor.md` ->
  **15 passed, 1 failed, 2 skipped** — the single failure was AC15, and only
  its `[ -f .shell-team/reviews/T-1031.md ]` clause; the printed "outside the
  allow-list" list was empty and every other AC15 clause (deletions column
  0, provenance file present, scope-lock file set) was green. Matches the
  engineer's own prediction in the hand-off exactly.
- **After** writing this record (re-run below, same invocation): **16
  passed, 0 failed, 2 skipped** — the required final **16/0/2** shape.

```
AC1: PASS  AC2: PASS  AC3: PASS  AC4: PASS  AC5: PASS  AC6: PASS  AC7: PASS
AC8: PASS  AC9: PASS  AC10: PASS  AC11: PASS  AC12: PASS  AC13: PASS
AC14: PASS  AC15: PASS  AC16: PASS  AC17: SKIP (runtime)  AC18: SKIP (runtime)
check-acs: 16 passed, 0 failed, 2 skipped, 0 unrecognized
```

### Full suites (item 3)

- `tests/check-handoff/run.sh` -> **OK**, all cases pass, including both new
  T-1031 lines (`decoy-real-flag-invalid` false-PASS-closed,
  `decoy-real-flag-valid` false-FAIL-closed) — both new fixtures are
  genuinely exercised by the suite, not only by AC1/AC2 (confirmed
  `grep -qF` for both basenames in `run.sh`, per AC14).
- `tests/close-out/run.sh` -> **all assertions passed**, including
  `closeout-lineshape-differential corpus=29 refused=13 accepted=12
  notlocated=4 mismatches=0` — the exact required post-flip shape, and the
  exact one-flip delta from the engineer's disclosed base measurement
  (`corpus=29 refused=14 accepted=11 notlocated=4 mismatches=0` at
  `1686495`). Wall-clock **14.2s** on this host — consistent with the
  engineer's disclosed 14.7s; the `CHECK_ACS_TIMEOUT=900` elevation is not
  actually needed on this host but was applied anyway per the spec's
  instruction.
- `tests/errexit-safe/run.sh` -> **all assertions passed**, including the
  re-derived ledger (`check-handoff.sh:77:` count = 0, `:27:` count = 1, and
  the live `emit()` line matched exactly once) and the completeness
  self-audit.
- `tests/machine-tokens/run.sh` -> **all tokens present in their consuming
  files**.
- `check-prompt-sync.sh` -> **all registered prompt blocks in sync**.
- `shellcheck bin/check-handoff.sh bin/close-out.sh` -> clean, exit 0.

### Adversarial probing beyond the fixtures (item 4)

All probes run against the live, edited `bin/check-handoff.sh` on scratch
files under `$TMPDIR` (never the tracked checkout):

1. **Two decoy separators in one title** (`... — \`DECOY_ONE\` — spec:
   d1.md second decoy — \`DECOY_TWO\` — spec: d2.md real title — \`
   READY_FOR_QA\` — spec: real.md`) -> exit **0**. The last slot on the line
   is judged and it holds a valid flag — matches the Input-space rule
   ("the resolution rule... is total over any count").
2. **Decoy holds a VALID flag, real (last) slot holds a DIFFERENT valid
   flag** -> exit **0**. Both slots happen to be valid so this probe alone
   cannot distinguish which slot governs (both readings pass); it does
   confirm no crash/mis-parse on a title carrying a second well-formed
   backticked flag-shaped token.
3. **CRLF decoy line**, decoy holds a valid flag, real (last) slot holds an
   invalid one -> exit **1**, `unknown status flag 'T1031_CRLF_BAD'` at the
   correct line — the real (last) slot governs even under CRLF line
   endings, not the CRLF-adjacent decoy.
4. **Decoy directly in a synthesized single-entry board** (`## Active` +
   blank line + one candidate line, no `## Done` — `bin/close-out.sh`'s own
   caller shape, Input-space class 7), decoy valid / real invalid -> exit
   **1**, correct flag reported at the correct line. The fix holds in the
   exact shape close-out synthesizes, not only in a full board file.
5. **Capture group would match empty** (`` — `` — spec: x.md ``, i.e. a
   backtick pair with nothing inside) -> exit **1**, classified `format
   mismatch` (not a crash, not misclassified as an unknown flag) — `[^`]+`
   requires one-or-more non-backtick characters, so an empty pair correctly
   fails `LINE_RE` entirely and falls into the format-mismatch path, per
   D7's frozen contract.

All five probes resolve exactly per the one-grammar, rightmost-slot
contract; none exposed a divergence.

### Engineer disclosures re-derived (item 5)

**(a) AC2 blind-spot disclosure.** Independently confirmed: AC2's own
non-vacuity guard is `grep -qF -- 'T1031_DECOY_BAD'
tests/check-handoff/fixtures/decoy-real-flag-valid.md`, which is a
plain-substring search over the whole file, not a check that the token still
sits inside the decoy-separator shape. A fixture where the token has been
moved into a harmless continuation sub-bullet (out of the title's `—
\`<token>\` — spec:` shape entirely) still passes this guard while the
checker's own verdict (0 violations) would be correct for the mutated
fixture. This is a real, narrowly-scoped gap in the **test guard**, not in
`bin/check-handoff.sh` under test — the checker's own behavior on that
mutated input is correct. **Assessment: does not need a fast-follow as a
blocking item.** AC2's `check:` line is inside the frozen intent block
(widening it is a spec change, out of this task's scope per the Non-goals'
"widening or narrowing what the checker accepts" principle extended to test
guards), and the disclosed failure mode requires an adversarial rewrite of
the fixture file itself — not a board input a real board author or
`close-out.sh` caller would ever produce. Worth a low-priority issue for a
future guard tightening (e.g. anchoring the grep to the title line), but not
a rework driver here.

**(b) D2 reason-letter judgment.** Read `tests/errexit-safe/run.sh`'s
comment block in full. The only place the `(a)`-`(e)` taxonomy is defined is
the pre-publication T-096 spec, which per this repo's `CLAUDE.md` does not
exist in this repository (T-001–T-111 range); `(b)`, `(c)`, `(e)` are never
otherwise used anywhere in the live corpus, so their exact original
definitions cannot be independently re-derived here — the same limitation
pm-spec's own spec disclosed and explicitly delegated to the engineer's
judgment ("this judgement needs the file in front of it and both outcomes
satisfy AC6 and AC7"). What IS independently checkable: the engineer's
narrowed rationale is internally consistent (the two `(a)` lines are inside
`bin/check-handoff.sh`'s own D7-frozen, byte-locked observable contract,
which this task restates rather than hardens) and it is mechanically
harmless either way — `tests/errexit-safe/run.sh` passes (AC6, AC7) under
the recorded reasoning. Accepted as a reasoned, disclosed judgment call per
the spec's own Open-questions delegation, not re-litigated.

**(c) `BASH_REMATCH[1]: unbound variable` mutation.** Read the live control
flow directly (`bin/check-handoff.sh:165-174`): `flag="${BASH_REMATCH[1]}"`
sits only in the `else` branch of the same `if [[ ! "$content" =~ $LINE_RE
]]` conditional whose `if`-branch does `emit ...; continue`. The `else` is
reached only when `$LINE_RE` just matched on that exact statement, which is
precisely what populates `BASH_REMATCH[1]` — so no code path in the shipped
file can reach that `else` with an empty match array. **Confirmed: the live
code cannot reach the unbound-variable state**; that crash is reachable only
under the reverted mutation (capture group removed), exactly as disclosed.

### Claim-fidelity check (item 6)

Every `## Summarized sources` restatement checked against the source's own
bytes:

- `bin/check-handoff.sh:68` (`LINE_RE`) and `:73` (`FLAG_RE`) at base
  `1686495` — **match** the spec's quoted regex text verbatim.
- `bin/check-handoff.sh:160-170` at base — **match**: the `if [[ "$content"
  =~ $FLAG_RE ]]` block genuinely has no `else`, confirming the fail-open
  claim.
- `bin/close-out.sh:316-318` at base — **match**: the comment does say "same
  disambiguation as check-handoff.sh's FLAG_RE", confirmed false as claimed
  (`close-out.sh:319` resolves rightmost via a greedy `(.+)` prefix;
  `FLAG_RE` was unanchored/leftmost).
- `tests/errexit-safe/run.sh:297-298` and `:304-305` at base — **match**:
  the T-110-era "ONLY (a) ... single inviolable, byte-unchanged file (DP-1)"
  prose and the `:77:` ledger pin are exactly as quoted.
- `tests/close-out/run.sh:967` — **match**: the exact `T-901 decoy
  separator` line quoted by AC12 is present verbatim.
- `tests/close-out/run.sh:1054-1058` — **match**: the `refused>=6` /
  `accepted>=8` / `notlocated>=3` / `corpus>=24` / `mismatches=0` floors are
  exactly as cited.
- `tests/machine-tokens/run.sh:48-68` — **match**: the seven flag-token loop
  and the `board-line-format.md` verbatim-line loop are exactly as
  described.
- `docs/interventions-reminder-hook.sample.sh:60` — **match**: `IN_FLIGHT_RE`
  is boolean-alternation-only (no capture group), unanchored at the end
  (no `$`) — confirming both the "no capture" and "deliberately
  end-unanchored" claims.
- `tests/check-handoff/run.sh:4` — **match**: "M1: unanchored flag
  extraction" is the file's own header text verbatim.

No restatement site diverges from its source. Independently re-derived the
R2 flip itself (not just trusted the provenance table): extracted the base
`bin/check-handoff.sh` via `git show 1686495:...`, ran it against the same
`T-901 decoy separator` line synthesized as a single-entry board — **exit 1,
`unknown status flag 'x'`** (the decoy title token, confirming the
pre-fix leftmost-resolution bug) — then ran the current, edited checker
against the identical input — **exit 0** (the real, last slot's
`READY_FOR_REVIEW` governs). This is the false-PASS/false-FAIL asymmetry the
Problem statement describes, closed exactly as the Goal specifies.

### Regressions and adjacent-code checks

- `bin/close-out.sh`'s rewrite conditional at `:319` is byte-identical to
  base (AC8) — only the comment above it changed.
- All nine pre-existing `tests/check-handoff/fixtures/*.md` are
  byte-identical to base (AC3) — the M1 regression guard (`valid.md`'s
  backticked `API`/`URL` title tokens) still lints clean (AC5).
- `docs/interventions-reminder-hook.sample.sh` is byte-identical to base and
  still carries `IN_FLIGHT_RE` (AC9) — the D5 not-applicable declaration is
  honoured, not silently dropped.
- `bin/check-board-headings.sh .shell-team/todo.md --base 1686495` finds no
  heading deletion/replacement.
- `git status --short` clean throughout this verification session, apart
  from this record's own append.

### Edge cases tried

- Beyond the five adversarial probes above: an empty-capture-group input
  (class 5) exercises a boundary the spec's Input space does not name
  explicitly but that `[^`]+` (one-or-more) structurally defends against —
  confirmed it resolves to `format mismatch`, the correct classification,
  not a crash or an `unknown status flag` misreport.
- All probes stayed inside the spec's declared reachable Input-space classes
  (1-9); none relied on an out-of-scope synthetic extreme (escalating decoy
  counts, adversarial width, non-UTF-8 bytes, etc.), so the
  `out-of-input-space` exemption was not needed for any finding here — there
  were no adverse findings to exempt.

### Risk notes for reviewer

- Item 5(a)'s AC2 guard blind spot is a genuine, disclosed limitation of the
  **test fixture's own non-vacuity guard** (not of the checker under test).
  I assessed it as non-blocking for this task and a candidate for a
  low-priority fast-follow issue (tighten the grep to the title's
  separator shape) — worth the reviewer's independent read since it is the
  one item in this hand-off that is a judgment call rather than a
  mechanical pass.
- Item 5(b)'s D2 reason-letter judgment rests on a taxonomy `(a)`-`(e)`
  whose authoritative definition lives in a pre-publication spec this
  repository does not carry (per `CLAUDE.md`'s T-001–T-111 note) — this is
  a structural limit on how far either QA or the engineer can independently
  verify the "correct" letter; both mechanically pass (AC6, AC7) under the
  recorded reasoning, which is what the spec's own Open-questions section
  asked for.
- `bin/close-out.sh:316-318`'s comment correction was deliberately kept at
  the same 3-line length to avoid disturbing the unrelated `close-out.sh:444`
  ledger pin — confirmed this constraint is real (the engineer's provenance
  record documents hitting the break with an 8-line first draft); I did not
  re-attempt the longer draft myself since the failure mode is already
  disclosed and grounded in a specific, reproducible ledger break.

Task: T-1031 → READY_FOR_REVIEW

---

## Codex round-1 (cross-provider review)

### Codex Review verdict: APPROVE
- Task: T-1031
- Codex model: gpt-5.6-sol (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 11 files, +1121/-24 lines (`git diff --stat 1686495...HEAD`); core mechanism edit is `bin/check-handoff.sh` (+18/-14) and `bin/close-out.sh` (comment-only, +2/-2)

Codex (`codex exec --sandbox read-only review --base 1686495 --json`) read the
real working tree directly — no diff or source was pasted into the prompt.
It ran its own commands inside the sandbox: `git diff --stat`/`git show
1686495:...` to diff the grammar against base, read the full spec (D1, AC1-AC18,
Input space), and synthesized two decoy-line probes plus a `[[ =~ ]]` micro-test
of the anchored capture-group behavior directly in the sandbox shell, confirming
rightmost-slot resolution empirically rather than by inspection alone. Final
message: "The change consistently derives the status flag from the same
anchored line-grammar match, correctly resolving decoy separators without
altering the established checker contract. The accompanying fixtures and
ledger updates align with the implementation, and no actionable regressions
were found." No Blocker/Major/Minor findings were raised in Codex's own pass.

Because Codex's pass surfaced nothing at Blocker/Major, the adversarial
"devil's advocate" pass (step 5) was not triggered — this is opt-in on a
Blocker/Major finding only, per the operating contract, and none occurred.

Given the "highest standard" instruction for verification-mechanism work on
the canonical checker, and that this is the **second** fix to the same
extraction (AC18/R4 pre-commitment), this round did not stop at reading
Codex's summary — every claim below was independently re-executed against the
live checkout (not merely re-read from the provenance/QA records) before
reaching a verdict:

- **(a) One-grammar claim, formulaically.** Directly probed `bin/check-handoff.sh`
  on this host (`GNU bash 3.2.57(1)-release`, BSD regex) against the four named
  boundary shapes:
  - Empty capture (`` — `` — spec: x.md``) → `format mismatch` (exit 1) — `[^`]+`
    requires one-or-more chars, so the empty pair correctly fails `LINE_RE`
    entirely rather than being misclassified as an unknown flag.
  - A literal backslash immediately before a backtick inside the flag slot
    (`` `BAD\`ESC` ``) → `format mismatch`. This is not a real input-space
    member — `ALLOWED_FLAGS` is a fixed uppercase enum with no backtick or
    backslash, so no producer can emit this shape — but the behavior is
    fail-closed regardless (it cannot slip past `flag_allowed()` misclassified
    as a valid or "unknown status flag" case).
  - Greedy-title interaction with **two** decoys plus a real slot: both
    "last slot valid" (exit 0) and "last slot invalid" (`unknown status flag`,
    correct flag string reported, exit 1) resolved to the rightmost slot in
    both directions — matching the Input-space claim that resolution is
    "total over any count", not just one decoy.
  - CRLF: confirmed `content="${content%$'\r'}"` (line 134) strips the
    trailing CR **before** `LINE_RE` is tested, so a CRLF-terminated decoy
    line resolves identically to its LF counterpart in both directions
    (exit 0 / exit 1 with correct flag), independently reproducing what the
    engineer's R3/AC1/AC2/AC12 disclosure claims.
  - No input was found where the shape verdict (`LINE_RE` match/no-match) and
    the flag-vocabulary verdict (`BASH_REMATCH[1]`) derive from different
    substrings — they are now structurally the same match, which is what D1
    set out to guarantee.

- **(b) Fail-closed posture.** Read the live control flow at
  `bin/check-handoff.sh:167-176` directly: the `else` branch that binds
  `flag="${BASH_REMATCH[1]}"` is reachable **only** through the same `if [[ !
  "$content" =~ $LINE_RE ]]` conditional whose non-match branch already
  `emit`s and `continue`s — there is no third path. Every shape-valid line
  that reaches past that conditional falls straight into the unconditional
  `if ! flag_allowed "$flag"` check with no branch in between. `BASH_REMATCH`
  cannot be stale from an earlier match: the only reference to it in the
  whole file (confirmed via `grep -c 'BASH_REMATCH\[1\]'` = 1, matching AC4)
  is bound in the same statement-adjacent `else` of the conditional whose
  `if` just performed the match, with no intervening command. No fail-open
  path exists.

- **(c) Contract preservation.** Byte-compared (not merely grep-matched) the
  base and HEAD `emit()` printf format line — identical
  (`printf '%s:%s: %s: %s\n' "$FILE" "$1" "$2" "$3" >&2`). Confirmed the three
  classification strings (`format mismatch`, `unknown status flag '...'`,
  the frozen stranded-continuation reason) and the three exit paths (0/1/2)
  are unchanged in the live file. Confirmed `bin/close-out.sh:319`'s rewrite
  conditional is byte-identical to its base blob via direct diff of the
  extracted line (only the comment above it changed, per D3).

- **(d) Ledger/prose reconciliation (D2).** Confirmed by direct grep that
  `tests/errexit-safe/run.sh`'s `NOT_APPLY` ledger now pins
  `check-handoff.sh:79:` (zero occurrences of the stale `:77:`), and that
  line 79 of the live `bin/check-handoff.sh` is in fact the `emit()` printf —
  the re-derivation is correct, not merely re-typed. The corrected scope
  prose no longer claims "check-handoff.sh is the ONLY (a) — the single
  inviolable, byte-unchanged file (DP-1)" and does cite T-1031, replacing it
  with a narrower, still-true "these two lines sit inside the frozen
  observable contract" framing. Ran `bash tests/errexit-safe/run.sh` live —
  all assertions pass, including the completeness self-audit.

- **(e) QA's two carried risk notes.** Concur with QA's severity call on
  both:
  - AC2's non-vacuity guard is a plain substring grep, not shape-anchored —
    confirmed by reading the guard clause directly. This is a real, narrow
    gap in the **test fixture's own guard**, not in the checker under test,
    and is not reachable by any real board author or `close-out.sh` caller
    (it requires deliberately relocating the decoy token out of its
    separator shape inside the fixture file itself). Severity: **minor**,
    appropriate as a non-blocking fast-follow candidate rather than a rework
    driver.
  - D2's reason-letter judgment (keeping `(a)` with narrowed rationale) is
    internally consistent and mechanically harmless (AC6/AC7 both green,
    independently re-run). The authoritative `(a)`-`(e)` taxonomy predates
    this repository's public history (T-001–T-111 range) and cannot be
    independently re-derived further — this is a structural limit on
    verification, not a defect, and the spec's own Open Questions section
    explicitly delegates this exact choice to the engineer. No finding.

- **(f) Close-out alignment.** Independently re-ran `bash
  tests/close-out/run.sh` (with `CHECK_ACS_TIMEOUT=900` per the spec's
  instruction, though the run completed in well under 120s on this host) and
  reproduced `closeout-lineshape-differential corpus=29 refused=13
  accepted=12 notlocated=4 mismatches=0` — the exact post-flip shape
  disclosed by both the engineer and QA, confirming the one-line
  refused→accepted flip and zero mismatches across the full corpus. The
  close-out comment now correctly states both regexes resolve rightmost
  (verified against the live text at `bin/close-out.sh:316-318`).

**Full independent re-run of `check-acs.sh`** (not merely trusting the
provenance/QA transcripts): `CHECK_ACS_TIMEOUT=900 bin/check-acs.sh
.shell-team/specs/T-1031-check-handoff-flag-anchor.md` →
**16 passed, 0 failed, 2 skipped** (AC17/AC18 are runtime-disclosure items
with no `check:`), matching the QA record exactly. Also independently
re-ran `tests/check-handoff/run.sh` (OK, both new decoy fixtures exercised),
`tests/errexit-safe/run.sh` (all pass), `tests/machine-tokens/run.sh` (all
tokens present), and `shellcheck bin/check-handoff.sh bin/close-out.sh`
(clean, exit 0).

**Issue-canon disclosure.** This role has no network/`gh` access, the same
constraint pm-spec disclosed. The task classification ("issue #122 already
exists and is acted on here in full; no separate issue was filed, and none
should be") and the provenance record's claim that the issue's own Fix-shape
section already suggested D1's exact shape (BASH_REMATCH of the full-line
match) could not be independently confirmed against the issue's actual body
from this sandbox: **internal consistency verified / issue-canon match
environmentally-unverified (relies on orchestrator's primary confirmation)**.
Nothing in the diff or spec is inconsistent with the relayed issue summary,
but the issue's own bytes were not read by this role.

**AC18/R4 pre-commitment status.** This round produced **zero** new,
independent Blocker or Major findings against the check-handoff subsystem,
and the prior round (QA) also produced none (QA verdict PASS). The
two-consecutive-rounds trigger does not fire; no grammar-rebuild/split
proposal is warranted from this round.

#### Blockers

None.

#### Major

None.

#### Minor / nits

- `tests/check-handoff/fixtures/decoy-real-flag-valid.md`'s own non-vacuity
  guard (`AC2`'s `check:` — a plain `grep -qF` for the decoy token anywhere in
  the file) does not confirm the token still sits inside the decoy-separator
  shape; a fixture where the token is relocated to an unrelated continuation
  line would still satisfy the guard while no longer testing the intended
  class. Not reachable by any real board input; a test-guard hardening
  candidate, not a rework driver. (Carried forward from QA's own disclosure,
  independently confirmed here.)

#### Adversarial findings

Not run as a separate pass — no Blocker/Major surfaced in the primary pass to
trigger it (see the operating-contract condition in step 5). Codex's own
primary-pass sandbox commands already included two direct empirical decoy
probes (a two-decoy title and a rightmost-slot capture-group micro-test) as
part of its single foreground review, and this round added independent
boundary probes (empty capture, CRLF, escaped-backtick, multi-decoy both
directions) executed directly against the live checker outside the Codex
sandbox — all resolved per the one-grammar, rightmost-slot contract with no
divergence found.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | no severity changes | — | — | Codex's primary pass and this round's independent re-execution agree: zero findings at any severity beyond the one minor item QA had already carried forward, whose severity is unchanged from QA's own assessment. |

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition

- AC2's non-vacuity guard is substring-only, not shape-anchored (see Minor
  above) — intended disposition: **file-an-issue**, low priority. Rationale:
  it is a real, disclosed gap in a test fixture's own guard rather than in
  the checker under test, is not reachable by any real board input, and
  tightening it (e.g. anchoring the grep to the title's separator shape) is
  a small, well-scoped fix that does not warrant blocking this merge — but
  is worth tracking so it does not silently recur if this fixture is ever
  copied as a template for a future decoy-class test.
- No other minor/nit is being deferred beyond the one above.

Raw Codex capture: `.shell-team/reviews/T-1031-codex-primary.txt` (final
message), `.shell-team/reviews/T-1031-codex-primary.jsonl` (full JSONL event
stream, including the sandbox commands Codex ran against the real files).
