# T-1092 Codex review

### Codex Review verdict: REQUEST_CHANGES (round 1)
- Task: T-1092
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — the event stream prints no model identifier, so this is the primary-value method, same convention as T-1091's record)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 20 files, +1063/-52 lines (`git diff --stat b49d29e3f7504651f6f8cf1dd24b0e02185ef22e...HEAD`)

**Scope.** Per the routing map's step 4 and the spec's own `## Verdict scope`, this task carries **full adversarial depth** (`user-visible: yes`; the dev-scaffold review carve-out explicitly does not apply). Judged against the six named in-scope-for-Major items: (i) the `spec-review` axis enforcement and the close-out teeth gate; (ii) the Specify-seam gate's condition-source design in `skills/run/SKILL.md`, attacked specifically against order-dependence and uncross-checked self-report; (iii) the spec-review mode's boundary in `agents/codex-reviewer.md`; (iv) hygiene-suite coherence; (v) norm-text cardinality/cross-surface symmetry; (vi) the blast-radius inventory's method and completeness. Out of scope and not re-litigated: axis placement outside `phase-multiplexing.md`; the loop-guard non-mirroring; the omitted human-approval step (operator-ratified, #343); no `spec-review` record on this task's own entry; #341/#245/#274/#333/#334; measured-saving claims (none made).

**Method.** One foreground primary pass (`codex exec … review --base b49d29e…`, real-file read, raw capture published) plus one adversarial pass, triggered by the primary pass's own Blockers/Majors, targeting `skills/run/SKILL.md`, `agents/codex-reviewer.md`, `agents/pm-spec.md`, `bin/close-out.sh` and the new note, specifically attacking order-dependence and uncross-checked self-report per the launch mandate. Raw captures: `.shell-team/reviews/T-1092-codex-primary.{txt,jsonl}`, `.shell-team/reviews/T-1092-codex-adversarial.{txt,jsonl}`.

**Independent verification — every finding below was checked against the real repository, not accepted on Codex's word.** All 20 `- check:` lines re-run live (`CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1092-specify-seam-review.md` → `20 passed, 0 failed, 1 skipped, 0 unrecognized`), confirming the engineer's and QA's own counts. `bash tests/close-out/run.sh` → exit 0, 103 PASS / 0 FAIL / 12 `PASS: T-1092` / 7 `PASS: T-1091` / 18 `PASS: T-1084`, matching QA's hand-off exactly. `bash tests/codex-skeleton-hygiene/run.sh` → exit 0, 40 PASS / 0 FAIL. `shellcheck --version` → `0.11.0`; `shellcheck bin/close-out.sh tests/close-out/run.sh tests/codex-skeleton-hygiene/run.sh` → exit 0 on all three. Gate checkers independently re-run, all exit 0: `check-handoff.sh`, `check-board-headings.sh --base b49d29e`, `check-pii-shapes.sh --base b49d29e`, `check-prompt-sync.sh`, `check-provenance.sh` (conformant, 5 decision entries, 0 sentinel), `check-interventions.sh` (conformant, 1 entry, 0 sentinel), `check-intent.sh` (`aligned: T-1092 v1 (48e84c9f…)`). Two of the spec's own `## Blast radius` predicted flips independently re-run against the real repository at HEAD: `check-acs.sh` on `T-1091-operator-authored-entry.md` → `12 passed, 5 failed` with the exact predicted new-fail set (AC9, AC12, AC15, AC16, AC17); `T-1084-dispatch-routing-record.md` → `12 passed, 4 failed` with the exact predicted pre-existing set (AC5, AC14, AC15, AC16); `T-1036-wording-batch-141-143-144.md` → AC2/AC6/AC10 all FAIL as predicted. The `- cost-input: t1092-domain-premise-count` priced line's own `- command:` reproduced live (`git show c23743d:… | grep -c '^- \*\*relayed'` → 8, matching the note). The board's dogfooded record itself was read line-by-line as corroborating evidence for the Blocker below (see there).

One genuine, load-bearing gap found in the shipped diff, plus three further real gaps, all independently reproduced or directly read against the live repository:

1. **`agents/codex-reviewer.md`'s spec-review-mode "Route-back" paragraph resolves the answering author from a condition source this task's own surrounding text already knows is unreliable at this exact seam, and never cross-checks it.** The paragraph reads: "route the findings back to the spec's own author, resolved through the `specify` decision **recorded on the task's board entry**." But `skills/run/SKILL.md:44` — three sentences inside the very same file this task edits — states the opposite lesson for the identical seam: "nothing here depends on the transcription's timing relative to the freeze sweep, and the confirmation gate below correspondingly keys off `pm-spec`'s own `- entry-mode:` hand-off field **rather than off the board record**," precisely because the board's `- dispatch:` sub-bullets are transcribed "at the Specify-to-Implement seam … and before `engineer` is invoked in step 4" — a window with no fixed relationship to the freeze sweep the new spec-review gate is embedded in. Direct empirical confirmation: every board entry with dispatch records sampled in this checkout (T-1092's own entry, T-1089, T-1087, T-1091, and others) transcribes its `- dispatch:` sub-bullets **strictly after** its `- intent-hash (vN)` line — never before. The Specify-seam spec-review gate fires only **before** the intent-hash is appended (both this task's own Goal and `skills/run/SKILL.md`'s placement require this). So at the one point this codex-reviewer mode ever runs, the board's `- dispatch: specify —` sub-bullet its own Route-back paragraph names as the resolution source is, by this repository's own demonstrated and universal sequence, guaranteed not to exist yet. Separately, even where a stale or mistyped `specify` sub-bullet from a *previous* task's entry template happened to be present, the paragraph never cross-checks it against `pm-spec`'s hand-off or the Routing Map's printed decision the way the Specify-seam gate itself is required to (`agents/pm-spec.md`'s own Route-back cross-check discipline: "in either direction... a mismatch... refuses"), so a wrong board value would silently misroute a `REQUEST_CHANGES` to the wrong party rather than refuse. Both the primary pass (Codex-native P1, "Resolve spec-review route-backs from the Plan decision") and the adversarial pass (`blocker` at `skills/run/SKILL.md:42`, plus a distinct `major` at `agents/codex-reviewer.md:196` for the missing cross-check) found this independently.
2. **`agents/codex-reviewer.md`'s top-level `## Preconditions` section is not updated for the new mode's own entry point.** It still enumerates exactly two entry conditions — `Task is at READY_FOR_REVIEW` and the T-1080 `concurrent-review-window` exception (`READY_FOR_QA`) — closing with "absent that declaration, the `READY_FOR_REVIEW` gate above is the whole of your entry condition." The new `## Spec-review mode` section, far below, documents a third, genuinely board-state-gated entry point (`READY_FOR_ARCH`) that this canonical Preconditions enumeration never mentions. T-1080 itself established the precedent for exactly this class of change — adding a sibling Preconditions bullet for a conditional additional entry point — and this task does not follow it. Both Codex passes flagged this independently (primary: P1; adversarial: `major`).
3. **`bin/close-out.sh:337`'s teeth-gate verdict match is not anchored.** `grep -qE '^### Codex Spec-Review verdict: APPROVE'` has no end-of-line or word-boundary anchor, so any heading with `APPROVE` as a literal prefix satisfies this fail-closed refusal gate — `### Codex Spec-Review verdict: APPROVE_WITH_CAVEATS`, `### Codex Spec-Review verdict: APPROVED`, or even an unfilled copy of the mode's own output template, `### Codex Spec-Review verdict: APPROVE | REQUEST_CHANGES` (the adversarial pass's own example). Separately, the scan is not scoped to the record's `## Spec review` section and does not require the matched heading to be that section's most recent round — an earlier `APPROVE` surviving alongside a later `REQUEST_CHANGES` (e.g. after a re-invoked spec-review round) still satisfies the gate. Confirmed by direct code read; reproduced independently by the primary pass (P2) and the adversarial pass (two separate `major` findings on the same line).
4. **The Route-back / record-shape prose doesn't say the spec-review mode may be the first writer of the task's review record.** Since this mode always runs before the default review pass (it fires at the Specify seam, well before `READY_FOR_REVIEW`), it is frequently the very first codex-reviewer activity for a task, yet its own text says the section is "appended to the task's existing review record," which reads as presupposing the file already exists. The board-and-write-boundary Rule's write grant plausibly covers create-on-first-write, and no live firing has occurred to test this (this task ships no arm, per its own Non-goals), but the wording should say so explicitly given the close-out teeth's correctness depends on the file existing.

#### Blockers
- `agents/codex-reviewer.md`'s spec-review-mode Route-back paragraph (`:196`) resolves the answering author from the board's `- dispatch: specify —` sub-bullet, a condition source `skills/run/SKILL.md:44` — inside the very same diff — already explicitly disclaims as unreliable at this seam for the identical timing reason, and which this repository's own board (every sampled entry, including this task's own) demonstrably transcribes only *after* the intent-hash the spec-review gate runs *before*. — Suggested fix: resolve route-back the same way the Specify-seam gate itself resolves `spec-review`'s own election — from `pm-spec`'s hand-off (`- entry-mode:` / the `specify` field) cross-checked against the Routing Map's printed decision — never from the board, and fail closed / escalate to the human on an unreadable or mismatched value exactly as the gate's own condition-source design already does for the axis election itself.

#### Major
- `agents/codex-reviewer.md`'s `## Preconditions` section (`:16-21`) is not extended for the new mode's `READY_FOR_ARCH` entry point, unlike the precedent T-1080's own conditional-entry bullet set for this exact class of addition. — Suggested fix: add a fourth Preconditions bullet mirroring the T-1080 bullet's shape, naming the spec-review mode's own conditional entry and pointing at its section.
- `bin/close-out.sh:337`'s verdict-match regex is unanchored, accepting any heading with `APPROVE` as a literal prefix (including the mode's own unfilled output template text). — Suggested fix: anchor to `^### Codex Spec-Review verdict: APPROVE[[:space:]]*$` (or equivalent) and add a near-miss fixture (`APPROVE_WITH_CAVEATS`, the literal template text) to **AC6**'s refusal cases.
- Same line: the scan is not scoped to the `## Spec review` section and does not require the matched heading to be that section's own latest round, so an earlier `APPROVE` can survive alongside a later `REQUEST_CHANGES` in the same file. — Suggested fix: extract the last `## Spec review` block (mirroring `agents/codex-reviewer.md`'s own multi-round record precedent, e.g. `.shell-team/reviews/T-1091.md`'s `## Round N` shape) and match only within it.

#### Minor / nits
- `agents/tech-lead.md`'s frozen Routing Map template (`### Dispatch`) still shows only `implement`/`verify` example rows; the mirrored norm text's own "every axis any such note defines is recorded" rule asks for a fourth line the literal example never shows. This is explicitly disclosed and frozen as this task's own Non-goal, carried over verbatim from T-1091's own Q1 residual — the identical class T-1091's own cross-provider review (round 1) already scored Minor rather than Major on identical reasoning ("explicitly settled... out of scope... the norm text is what carries the duty"). Noted here for the same reason it was noted there: both Codex passes raised it (primary P2, adversarial `major`), but it is not scored as blocking given the precedent and the disclosed-residual honesty.
- The spec-review mode's record-shape prose does not explicitly state create-if-absent behavior for `T-XXX.md` when this mode is the first review activity for a task (see gap 4 above). Low real risk (the write-boundary Rule's grant plausibly covers it, and no arm has fired to test it), but worth a one-line clarification alongside the Blocker fix.

#### Rejected / out-of-scope (pre-existing corpus conventions, or explicitly disclosed and frozen residuals, not new gaps introduced by T-1092)
- **`bin/close-out.sh`'s teeth gate is validate-if-present and never re-verifies the true Plan-time election against the board record at close-out time** (a post-freeze de-election, or an election omitted from the board entirely, is invisible to it). Raised by the adversarial pass (`major`, `bin/close-out.sh:328`). Verified this is explicitly disclosed as open (not claimed closed) in the frozen Goal's own condition-source design — residual **(ii)** ("the teeth verify only that an *elected* review reached APPROVE, never that the gate ran") and residual **(iv)** ("a post-freeze de-election is not `drift-detected` and is visible only as a Routing-Map-versus-board divergence") — and matches the identical, already-shipped validate-if-present behavior the `implement`/`verify`/`specify` axes have had since T-1084/T-1091. T-1091's own cross-provider review (round 1) rejected the identical-class finding on the identical grounds ("explicit, frozen Non-goal... matching the identical, already-shipped `implement`/`verify` axis behavior"); the same treatment applies here.

#### Adversarial findings
Full record: `.shell-team/reviews/T-1092-codex-adversarial.{txt,jsonl}`. Asked to attack order-dependence and uncross-checked self-report specifically (the two classes T-1091 lost rounds to), the close-out teeth's verdict match, and the Preconditions gap: Codex independently corroborated the Blocker above from two angles (`blocker` on the order-dependence half at `skills/run/SKILL.md:42`, a separate `major` on the missing-cross-check half at `agents/codex-reviewer.md:196`), corroborated both close-out regex Majors (as two separate findings on the same line), corroborated the Preconditions Major, and additionally raised the validate-if-present teeth-completeness point folded into Rejected above (matching T-1091's own precedent) and the Routing-Map-template point folded into Minor above (matching T-1091's own Q1 precedent).

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Route-back reads the board's `specify` sub-bullet, which the same file's own T-1091 paragraph already knows is order-dependent at this seam | primary: Codex-native P1 (map); adversarial: `blocker` (order-dependence half) + `major` (missing-cross-check half) | blocker | confirmed by direct repository inspection — every sampled board entry transcribes `- dispatch:` strictly after `- intent-hash`, while the spec-review gate fires strictly before it, so the named condition source is provably absent at the only point this mode runs; the two Codex-reported angles (absence, and no cross-check even when present) are the same root defect and share the same fix |
| 2 | `## Preconditions` not extended for the new `READY_FOR_ARCH` entry point | primary: Codex-native P1 (map); adversarial: `major` | major | real inconsistency against the T-1080 precedent for this exact class of addition; downgraded from the P1/blocker-adjacent primary framing because the actual invocation path is always explicitly named by `skills/run/SKILL.md`'s own gate text ("invoke `codex-reviewer`'s spec-review mode"), so the risk is a documentation/consistency gap rather than a proven misrouting |
| 3 | teeth-gate verdict regex unanchored (accepts a prefix-matching near-miss verdict value) | primary: Codex-native P2 (map); adversarial: `major` | major | confirmed by direct code read; a genuine refusal-completeness gap in a fail-closed merge/close-out backstop, the exact axis the Verdict scope calls out, with a one-line fix |
| 4 | teeth-gate scan not section-scoped / not latest-round-only | adversarial only: `major` | major | confirmed by direct code read (the `grep` has no section boundary and no round-recency logic); a distinct property from #3 sharing the same line and the same fix direction |
| 5 | record-shape prose doesn't state create-if-absent for the first-ever review record write | primary: P1 (map, "Initialize a review record") | minor | downgraded from primary's P1/blocker framing: the write-boundary Rule's existing grant plausibly already covers file creation, and no arm has fired to test this in practice (this task ships no live arm, per its own Non-goals); a wording tightening, not a proven functional break |
| 6 | `agents/tech-lead.md`'s frozen Routing Map template omits a fourth example row | primary: Codex-native P2 (map); adversarial: `major` | minor | identical class to T-1091's own Q1 residual, which that task's own cross-provider review (round 1) already scored Minor on identical reasoning (disclosed, frozen Non-goal; norm text carries the duty); not re-litigated as a new gap |
| 7 | teeth gate never re-verifies the true election against the board at close-out (validate-if-present, no drift detection on a post-freeze de-election) | adversarial only: `major` | rejected | explicitly disclosed as open (not claimed closed) by the frozen Goal's own residuals (ii) and (iv); matches the identical, already-shipped validate-if-present behavior of the `implement`/`verify`/`specify` axes since T-1084/T-1091; T-1091's own review rejected the identical-class finding on identical grounds |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md`'s T-1092 entry to `READY_FOR_ENG`, linking the Blocker (route-back's order-dependent, uncross-checked board read) and the three Majors above (Preconditions gap, and the two close-out verdict-match gaps). None of these touch a frozen byte, a locked cardinality, or the never-dropped pre-commitment components (the spec-review mode's mandate and record shape, and the Specify-seam gate's placement and its own condition-source design, are sound — the gap is specifically in the Route-back paragraph's *own*, separately-authored condition source, which does not follow the design the surrounding gate itself already established). Per this task's pre-commitment: this is the first round against the never-dropped components, so no droppable-component disposition is triggered yet.

#### Fast-follow disposition
- The Route-back create-if-absent wording gap (Minor #5 above) — **won't-fix as a fast-follow; fold into the same rework pass as the Blocker** (the Route-back paragraph is being edited regardless to fix the Blocker, so a one-line create-if-absent clarification costs nothing extra in the same edit).
- `agents/tech-lead.md`'s Routing Map template omitting a fourth example row (Minor #6 above) — **won't-fix** (identical, already-adjudicated class to T-1091's own Q1 residual; that task's own disposition — file an issue if a future review judges the disclosed residual insufficient — already covers this token, and this round does not judge it insufficient).

---

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1092
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — the event stream prints no model identifier)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 5 files, +109/-3 lines (`git diff --stat b981355bbbcaa252b3d1320d1e4b4dfb8190c616...42e0fed`) — the rework round against round 1's Blocker and three Majors only.

#### Round-2 asks — verdicts
**(i) Is the round-1 Blocker closed?** The *original* defect (route-back reading the board's `- dispatch: specify —` sub-bullet, provably absent at the seam) is closed — verified by direct diff read and by an independent repository-wide sweep (`git diff b49d29e...HEAD -- <every touched doc/skill/agent file> | grep '^+' | grep -iE "recorded on the task.s board entry|on the board entry"` → 0 hits), matching the engineer's own claim exactly. But the *replacement* condition-source design has its own gap (see Major finding 1 below) — the class is closed, the component is not yet clean.
**(ii) Are the three round-1 Majors closed?** The Preconditions gap (Major 1) is cleanly closed — confirmed by direct read, no residual ambiguity. The two close-out teeth fixes (Majors 2+3) are **not** cleanly closed: both are independently, empirically confirmed correct against every fixture that exists, but the round's own new code introduces two further defects the fixtures do not exercise (see Blockers below) — one of which (the section-boundary mismatch) can silently **defeat the very property Major 3 exists to guarantee**, through an input class no fixture happens to construct.
**(iii) Anything new the rework introduced, beyond the four findings' own fix code?** Yes — both Blockers below are new code this round added, not present in round 1's diff.

**Method.** One foreground pass (`codex exec` with a free-form prompt targeting `git diff b981355...42e0fed` plus the surrounding files, since this round reviews a scoped fix rather than the whole task's diff — real-file read, raw capture published). Raw capture: `.shell-team/reviews/T-1092-codex-round2.{txt,jsonl}`.

**Independent verification — every finding below was checked against the real repository, not accepted on Codex's word.** All 20 `- check:` lines re-run live (`20 passed, 0 failed, 1 skipped`); `check-intent.sh` confirms the frozen spec is byte-unchanged (`aligned: T-1092 v1 (48e84c9f…)` — the same hash as round 1, proving no frozen byte moved this round). `bash tests/close-out/run.sh` → exit 0, 107 PASS / 0 FAIL / 16 `PASS: T-1092` (12 round-1 + 4 new `T-99506`–`T-99509`), matching the engineer's and QA's own counts exactly. `bash tests/codex-skeleton-hygiene/run.sh` → exit 0, 40/0, unaffected as expected. `shellcheck` 0.11.0 clean on all three edited scripts. All gate checkers re-run, all exit 0. **Mutation-probed the two claimed teeth fixes myself, independently of the engineer's own worktree probes**: an anchor-only reversion (drop the round-2 anchor, keep the round-2 scoping) flips exactly `T-99506` red, restored green; a scoping-only reversion (drop the round-2 scoping, keep the round-2 anchor) flips exactly `T-99508` red, restored green — confirming both requirements are independently load-bearing, matching the engineer's own claimed mutation-probe results. **Then went past the shipped fixtures to construct inputs neither the fixtures nor the mutation probes exercise**, against the real, unmutated, currently-shipped `bin/close-out.sh`:
- A tab-after-colon verdict heading (`### Codex Spec-Review verdict:\tAPPROVE`) → correctly refused (fail-closed direction; not a vulnerability, confirms the anchor is exact rather than loose).
- **A review record with a well-formed `## Spec review` / APPROVE round followed by a second `## Spec review ` heading (one trailing space) / REQUEST_CHANGES round → close-out incorrectly PASSED (`rc=0`, task moved to `## Done`).** Reproduced directly against the live script in a scratch dir with all four env redirections; this is the exact stale-APPROVE-survives class Major 3 exists to close, reachable through a heading `grep -n '^## Spec review$'` (exact match) fails to recognize as a section boundary while `awk`'s own boundary check `/^## /` (loose prefix match) *does* treat it as one — so `grep` anchors the scan to the *first* section while `awk` correctly stops before the second section's content, and the newer REQUEST_CHANGES round is never read at all. See Blocker 1 below.
- **A `## Spec review` / APPROVE section padded with roughly 200KB of filler text before and after the verdict line → close-out incorrectly REFUSED a genuinely valid, exactly-matching APPROVE** (confirmed the file itself contains the matching line via a direct `grep` against it, `rc=0`; the same content run through the shipped `printf '%s\n' "$LATEST_SECTION" | grep -qE ...` pipeline reports no match under `set -o pipefail`). Root cause: `grep -q` exits immediately on its first match without draining the rest of its input; the upstream `printf`, still writing the remainder of a payload larger than the pipe buffer, receives `SIGPIPE` (exit 141); under `pipefail` the pipeline's reported status becomes non-zero despite `grep` itself having matched. Confirmed the fix: replacing the pipe with a here-string (`grep -qE '...' <<< "$LATEST_SECTION"`) reports the match correctly against the identical oversized payload. See Blocker 2 below.

Two further real, more moderate gaps, independently corroborated by this round's Codex pass:

3. **The rewritten Route-back paragraph's condition-source design is directionally correct but under-specified relative to the precedent it names.** It says "resolved the same way `skills/run/SKILL.md`'s Specify-seam spec-review gate resolves this axis's own election," but unlike that gate's own full text (and unlike the Conformance-read confirmation gate's own full text), the paragraph never itself states what happens when `pm-spec`'s hand-off is *missing, duplicated, or out-of-vocabulary* (as opposed to merely *mismatched* against the Routing Map) — the cross-referenced gates spell out "fails closed... never assumed" for exactly this case, but the Route-back paragraph's own text does not repeat it, only the mismatch-refusal half. Separately, its parenthetical claim that "the Conformance-read confirmation gate and the Specify-seam spec-review gate both key off" `pm-spec`'s `- entry-mode:` field is factually imprecise: the Specify-seam spec-review gate keys off a *different* hand-off field, `- spec-review:` (confirmed by direct read of `skills/run/SKILL.md`'s own gate text) — the two gates share the *design pattern* (a pre-intent-hash hand-off field, cross-checked against the printed Routing Map decision), not the *same field*. Neither defect changes what the paragraph actually tells `codex-reviewer` to *do* (it still names the correct field, `- entry-mode:`, for resolving the author), so this is a specification-completeness and accuracy gap rather than a proven routing failure — downgraded from Codex-native `blocker` accordingly.
4. **The new Preconditions bullet's "the two preconditions above" is an imprecise count.** Four bullets precede it (Codex CLI installed; `/codex:setup` run; `READY_FOR_REVIEW`; the T-1080 concurrent-review-window exception), not two. The likely intended reading — "the two literal precondition bullets, as distinct from the tooling-setup bullet and the other conditional-entry bullet" — is defensible but not stated, unlike the T-1080 bullet's own more precise "the precondition immediately above" (singular, unambiguous). Low real risk given the whole file is read as one prompt and the surrounding text is otherwise unambiguous, but worth a precise count or an explicit list.

#### Blockers
- **`bin/close-out.sh`'s teeth-gate section-boundary logic uses two different heading-match strictnesses that disagree on malformed input, letting a stale `APPROVE` survive alongside a genuinely newer `REQUEST_CHANGES` round — reintroducing the exact defect Major 3 (round 1) was fixed to close.** `LAST_SECTION_LINE` is found by an *exact* match (`grep -n '^## Spec review$'`), but the *end* of the extracted section is found by a *loose* match (`awk`'s `/^## /`, matching any line opening with those three characters regardless of what follows). A `## Spec review` heading carrying any trailing content the exact grep doesn't recognize (a trailing space is the reproduced case; the same class covers a trailing tab, double space, or any other deviation) is invisible to `grep`'s heading search but *is* recognized by `awk`'s boundary check — so the scan anchors to an *earlier* `## Spec review` heading and stops at the malformed *later* one, never reading anything after it. Empirically confirmed: a two-round record (round 1 exact `## Spec review` / `APPROVE`, round 2 `## Spec review ` with one trailing space / `REQUEST_CHANGES`) passes close-out silently (`rc=0`) against the live, unmutated script. — Suggested fix: make the heading search and the boundary check use the *same* match strictness in a single pass (e.g. one `awk` script that finds every line matching `/^## /`, records which are exactly `## Spec review`, and extracts from the *last* exact match to the *next* `/^## /` line of *any* kind found after it) rather than splitting the two checks across `grep` and `awk` with different anchors; add a fixture reproducing the trailing-space (or other malformed) later heading.
- **The same gate's verdict match is racy under `set -o pipefail` and can falsely refuse a genuinely valid, exactly-matching `APPROVE` when the extracted section is large.** `printf '%s\n' "$LATEST_SECTION" | grep -qE '...'` — `grep -q` exits as soon as it finds its first match, without necessarily consuming the rest of its input; if `LATEST_SECTION` is large enough that `printf` is still writing when `grep` closes the read end, `printf` receives `SIGPIPE` (exit 141), and under `pipefail` the *pipeline's* reported exit status becomes non-zero even though `grep` itself matched successfully — causing `close-out` to refuse a task whose review record genuinely, exactly carries the required `APPROVE` heading. Empirically confirmed with a ~200KB padded section (well under any plausible pipe-buffer-avoiding threshold for a real, detailed cross-provider review write-up); the identical content matches correctly via a direct `grep` against the file, and via a here-string (`grep -qE '...' <<< "$LATEST_SECTION"`) in place of the pipe. Not exercised by any shipped fixture, all of which use small payloads. — Suggested fix: replace the `printf | grep -q` pipe with a here-string (`grep -qE '...' <<< "$LATEST_SECTION"`) or perform the match inside the same `awk` pass that already does the extraction, eliminating the inter-process pipe (and its SIGPIPE exposure) entirely; add a fixture with a deliberately oversized `## Spec review` section to make this class of regression visible.

#### Major
- The Route-back paragraph's condition-source design is directionally correct (reads the right field, cross-checks against the right decision) but does not itself restate the fail-closed-on-absence/duplication/out-of-vocabulary behavior the gates it cross-references both state explicitly, and its own justification for why `codex-reviewer` should trust `- entry-mode:` inaccurately claims the Specify-seam spec-review gate keys off that same field (it keys off `- spec-review:`). — Suggested fix: state the absence/duplication/out-of-vocabulary-refuses-and-escalates behavior directly in this paragraph rather than only by cross-reference, and correct the parenthetical to describe the *shared design pattern* rather than a *shared field*.

#### Minor / nits
- The new Preconditions bullet's "the two preconditions above" undercounts the four preceding bullets; tighten to a precise count or an explicit list, matching the unambiguous singular form the T-1080 bullet it mirrors already uses ("the precondition immediately above").

#### Rejected / out-of-scope (this round)
- Codex's own concern that "no precedence rule handles a launch carrying both spec-review and review-response inputs simultaneously" — rejected as an unreachable synthetic extreme: the two modes are activated through entirely disjoint invocation surfaces (the `/review-response` skill's explicit findings-plus-diff input vs. the run skill's own Specify-seam gate invocation), and no shipped caller in this repository could construct a launch carrying both sets of inputs by ordinary operation.
- Codex's own concern that the Preconditions bullet should itself re-validate `pm-spec`'s `- spec-review: cross-provider` hand-off value — rejected: that validation is already the Specify-seam gate's own job, performed by the orchestrator *before* `codex-reviewer` is ever invoked in this mode (the same division of labor the default mode's own `READY_FOR_REVIEW` precondition already relies on, trusting the caller rather than re-verifying `qa-verifier`'s own internal work).
- Codex's own concern that `grep`'s `|| true` "turns I/O errors into the same permissive fallback" — rejected: the grep in question runs only after `[ -f "$REVIEW_RECORD" ] && [ -r "$REVIEW_RECORD" ]` has already confirmed the file is readable, so an I/O-error-on-read scenario is not reachable at that point.

#### Synthesis audit ledger

| # | finding | primary (this round) | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | section-boundary strictness mismatch (`grep` exact vs. `awk` loose) lets a malformed later heading hide a genuinely newer verdict | Codex-native `major` (map) | blocker | independently and empirically reproduced against the live, unmutated script (`rc=0` on a record whose latest round actually rejects); this is not a synthetic extreme — a stray trailing space on a heading an LLM-authored round emits is ordinary, plausible imprecision, and it defeats the exact property Major 3 (round 1) was fixed to guarantee |
| 2 | `printf \| grep -q` pipeline is racy under `pipefail`, can false-refuse a large, valid verdict match | Codex-native `major` (map) | blocker | independently and empirically reproduced (a ~200KB payload flips a genuine match to a refusal); upgraded from Codex's own `major` framing because it is a confirmed, reproducible correctness bug in shipped code with a one-line, well-understood fix, not a theoretical concern |
| 3 | Route-back paragraph under-specifies absence/duplication handling and misdescribes which field the Specify-seam gate keys off | Codex-native `blocker` (map) | major | downgraded: the underlying mechanism is sound (correct field, correct cross-check target) and the missing behavior is strongly implied by its own "resolved the same way..." cross-reference to gates that do state it explicitly; a completeness/accuracy gap, not a proven routing failure like round 1's Blocker |
| 4 | "the two preconditions above" undercounts the four actually-preceding bullets | Codex-native `major` (map) | minor | real but low-risk wording imprecision; the whole file is read as one prompt and the surrounding text is otherwise unambiguous |
| 5 | no precedence rule for a launch carrying both spec-review and review-response inputs | Codex-native `major` (map, folded into finding on line 22) | rejected | unreachable synthetic extreme — the two modes have entirely disjoint invocation surfaces, and no shipped caller could construct a launch mixing both |
| 6 | Preconditions bullet doesn't itself re-validate `pm-spec`'s `- spec-review:` hand-off value | Codex-native `major` (map, folded into finding on line 22) | rejected | that validation belongs to the Specify-seam gate (performed by the orchestrator before invocation), the same division of labor the default mode's own precondition already relies on |
| 7 | `grep`'s `\|\| true` treated as masking I/O errors into the permissive fallback | Codex-native `major` (map, folded into finding on close-out.sh:357) | rejected | unreachable given the preceding `[ -f ] && [ -r ]` guard already confirms readability at that point |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md`'s T-1092 entry to `READY_FOR_ENG`, linking the two Blockers (the section-boundary strictness mismatch, and the `pipefail`/SIGPIPE false-refusal) and the one Major (Route-back specification completeness/accuracy) above. The round-1 Blocker's own class (board-as-condition-source) is confirmed closed and is not to be re-litigated; these are new defects in this round's own replacement code, not a reopening of round 1. Per this task's pre-commitment: the never-dropped components (the spec-review mode's mandate and record shape, the Specify-seam gate's placement and condition-source design) remain untouched by this round's findings — the two Blockers are implementation bugs in the teeth gate's *new* matching logic, not a defeat of the design itself, so no droppable-component disposition is triggered.

#### Fast-follow disposition
- The "two preconditions above" imprecise count (Minor above) — **won't-fix as a fast-follow; fold into the same pass that fixes the two Blockers** (that pass is already editing this file's immediate surroundings, so a precise count or explicit list costs nothing extra in the same edit).

---

## Round 3

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1092
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — the event stream prints no model identifier)
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 8 files, +206/-173 lines (`git diff --stat 5b12dd7bbbcaa252b3d1320d1e4b4dfb8190c616...94cca36`) — the rework trail from round 2's verdict through six further QA-found guard-anchor-weakness defects, five operator rulings, a class-B re-freeze (v1→v2), and the final teeth-gate removal (operator option B, issue #344).

#### Round-3 mandate — verdicts
**(1) Governance legitimacy.** Confirmed mechanically and by direct read, not accepted on the board's own narrative: `bin/check-refreeze-class.sh --old-hash 48e84c9f… <v1 copy> <v2 spec>` independently classifies the delta **`class-b`** (a differing line at intent-block index 1 is not a `- check:` line on both sides) — the re-freeze genuinely touches frozen prose, matching the board's own claim rather than being a lighter class-M edit dressed up as B. The board carries exactly **five** `- operator ruling` bullets (2026-08-23, then "second"/"third"/"fourth"/"fifth") plus one `- intent-ratified (2026-08-23): v1→v2` bullet naming the fifth ruling as its ratification. `.shell-team/interventions/T-1092.md` carries exactly **six** entries (`bash bin/check-interventions.sh` → "6 entries, 0 sentinel"), one per escalation, each with a human ruling recorded. The pre-commitment paragraph itself — the record of what fired and licensed the Droppable-1st — is **byte-identical** between v1 and v2 (`awk`-extracted and `cmp`'d directly: identical), so the ratified outcome is not being retroactively justified by editing the very rule that authorized it. All confirmed independently. No finding.
**(2) Removal completeness.** `bin/close-out.sh`: the entire teeth block (176 lines: the `SPEC_REVIEW_ELECTED` capture, the whole `if [ "${SPEC_REVIEW_ELECTED:-}" = "cross-provider" ]` gate, its `awk`-based single-normalization extraction, `LAST_SECTION_LINE`/`LATEST_SECTION`/`normv`/`any_heading`/`AWK_RC`, the sentinel, and the here-string verdict match) is removed cleanly; `grep` for every one of these identifiers plus `TEAM_REVIEWS_DIR` and `Codex Spec-Review verdict` against the file returns zero for all. `tests/close-out/run.sh`: the `spec_review_case` helper and all 18 teeth fixtures across the whole saga (`T-99501`–`T-99509` from rounds 1–2, `T-99517`–`T-99523` from the guard-anchor-weakness rework rounds 3–6) are gone; the 7 grammar fixtures (`T-99510`–`T-99516`) remain and pass (`bash tests/close-out/run.sh` → 98 PASS / 0 FAIL / 7 `PASS: T-1092`, matching AC8's own floor exactly). **But removal was not fully complete** — see Minor findings below, both independently found by the primary pass and cross-provider corroborated.
**(3) Closure of round-2 findings.** Both cleanly closed. The Preconditions bullet now reads "Every precondition above stands unchanged," resolving the miscount. The Route-back paragraph now explicitly states the field-name distinction ("the *correct* field for identifying who authored the spec... the Specify-seam spec-review gate resolves a *different* election... off its own `- spec-review:` hand-off field, not this one") and explicitly enumerates the fail-closed-on-absence/duplication behavior ("an unreadable `- entry-mode:` value — missing, duplicated, or outside the closed pair — fails closed and refuses the route-back... never assumed to be `pm-authored` by default"), closing both halves of the round-2 Major precisely as the review's own Suggested fixes asked.
**(4) Honesty of shipped surfaces.** The v2 spec's Goal, condition-source paragraph, residual (ii), the new Non-goal, and `## Verdict scope` all state plainly that no mechanical backstop ships and that issue #344 owns it. `skills/run/SKILL.md`'s residual (ii) is reworded to match. `agents/codex-reviewer.md`'s mode section is unaffected (it never described the close-out backstop — that lived only in `bin/close-out.sh` and the spec). The three adopter docs (English and Japanese, none touched this round) were independently read and never claimed a close-out enforcement mechanism in the first place — their scope was always the Specify-seam freeze-time gate, which is real and unaffected — so their silence is accurate, not stale; this matches QA's own round-7 Item 5 finding exactly. **But this mandate item's own "nowhere imply a mechanical close-out backstop exists" test fails at the one place with the most reach: this task's own title** — see the Major finding below.
**(5) Round-2 rejected items and scope boundary.** All three round-2 rejected items (cross-mode input conflict, Preconditions re-validating the hand-off, `|| true` masking I/O errors) are unaffected by this round's diff and are not re-litigated. `## Verdict scope`'s own text explicitly forecloses re-opening whether a close-out backstop should ship at all ("re-opening it here is exactly what the carve-out exists to prevent") while explicitly keeping "the *completeness of its removal*" in scope, naming "a surviving remnant, a dead helper, a residual `$TEAM_REVIEWS_DIR` reference" verbatim as the class to check — precisely the class the two Minor findings below fall into.

**Method.** One foreground pass (`codex exec` with a free-form prompt targeting `git diff 5b12dd7...94cca36`, the current HEAD state of the touched files, and the three untouched adopter docs, since this round reviews a scoped removal plus a re-freeze rather than a fresh feature diff). Raw capture: `.shell-team/reviews/T-1092-codex-round3.{txt,jsonl}`.

**Independent verification — every finding below was checked against the real repository, not accepted on Codex's word.** All 20 `- check:` lines re-run live (`20 passed, 0 failed, 1 skipped`), including `AC5`/`AC6` specifically (both flip PASS on the removal, as their own bodies require). `check-intent.sh` → `aligned: T-1092 v2 (1c55474d…)`, matching the ratified hash. `bash tests/close-out/run.sh` → 98/0/7 as stated above. `bash tests/codex-skeleton-hygiene/run.sh` → 40 PASS / 0 FAIL, unaffected. `shellcheck` 0.11.0 clean on all three edited scripts. All gate checkers re-run, all exit 0 (`check-handoff`, `check-board-headings --base b49d29e`, `check-pii-shapes --base b49d29e`, `check-prompt-sync`, `check-provenance` — 12 decision entries, 0 sentinel, `check-interventions` — 6 entries, 0 sentinel). Both Minor findings below were located by direct `grep`/read against the live files, not accepted on the primary pass's own line numbers.

Three genuine gaps, one of them load-bearing for this round's own mandate item 4:

1. **This task's own title, in both places it appears, still claims the removed mechanism ships.** `.shell-team/specs/T-1092-specify-seam-review.md:1` (the document's H1) and `.shell-team/todo.md:14` (the board's Active entry title, verbatim-copied from the spec's H1 at authoring) both read "...elected through a fourth dispatch axis and **backstopped by a fail-closed close-out gate**." This is now false at HEAD: the fail-closed close-out gate was the mechanism carved out to issue #344; nothing backstops the election at close-out any more. Confirmed by direct read of both files; neither line was touched anywhere in the round-3 diff (`git diff 5b12dd7...94cca36` names neither the spec's line 1 nor the board's line 14). Both lines sit **outside** the frozen intent block (the spec's `<!-- BEGIN intent-block: T-1092 -->` marker opens well after the title), so correcting either requires no re-freeze ceremony — an ordinary prose edit closes this. This is exactly the shape mandate item 4 asks after ("must nowhere imply a mechanical close-out backstop exists"), found in the single most visible and most-read location in the entire task record: its own name.
2. **`tests/close-out/run.sh`'s `dispatch_case` helper (`:1117`–`:1128`) still creates a scratch reviews directory and exports `TEAM_REVIEWS_DIR`.** The comment at `:1117` reads "T-1092: every call site is redirected to a scratch reviews directory too... so no fixture here can ever read or write this repository's own `.shell-team/reviews/`" — a purpose that existed only to protect against the now-removed teeth gate accidentally reading the real reviews directory during a T-1091/T-1084 fixture run. `bin/close-out.sh` no longer reads `TEAM_REVIEWS_DIR` anywhere (confirmed: zero occurrences), so the `mkdir -p "$root/.shell-team/reviews"` and the env-var export are both dead plumbing, and the comment's own stated rationale is now inaccurate. Purely a hygiene gap: every one of `dispatch_case`'s 23 call sites still passes with this plumbing present, since it is inert.
3. **The spec's own `## Summarized sources` entry for `bin/team-paths.sh` (line 34) still asserts the withdrawn design's own rationale as current fact.** It reads "`TEAM_REVIEWS_DIR` is a name this script **exports** in `--export` mode, not an override it reads for `--get reviews`, **which is why the reviews-dir override must live in `bin/close-out.sh`** exactly as the interventions one does" — stating a design conclusion (that an override belongs in `bin/close-out.sh`) that the carve-out withdrew. This is the same class of staleness the v2 re-freeze's own consequential-passage sweep correctly fixed one bullet earlier in the same section (the `bin/close-out.sh` entry, confirmed updated) but missed for this sibling `bin/team-paths.sh` entry describing the identical withdrawn override. Since `## Summarized sources` sits outside the frozen intent block too, this is also a plain prose fix, not a re-freeze.

#### Blockers
None.

#### Major
- `.shell-team/specs/T-1092-specify-seam-review.md:1` and `.shell-team/todo.md:14` — both instances of this task's own title still claim "backstopped by a fail-closed close-out gate," which is false at HEAD after the carve-out. — Suggested fix: drop the backstop clause from both titles (e.g. "...elected through a fourth dispatch axis" ending there), or replace it with an accurate one ("...with the mechanical enforcement tracked separately as issue #344"). Neither edit touches the frozen intent block.

#### Minor / nits
- `tests/close-out/run.sh:1117`–`:1128` — `dispatch_case`'s scratch-reviews-directory `mkdir` and `TEAM_REVIEWS_DIR` export are dead plumbing from the removed teeth gate; the comment's stated rationale no longer applies. — Suggested fix: remove the `mkdir -p "$root/.shell-team/reviews"` line, its comment, and the `TEAM_REVIEWS_DIR=...` clause from the `bash "$CLOSEOUT"` invocation.
- `.shell-team/specs/T-1092-specify-seam-review.md:34` — the `bin/team-paths.sh` summarized-source bullet still states the withdrawn "the reviews-dir override must live in `bin/close-out.sh`" design conclusion as current fact. — Suggested fix: reword to state this as the withdrawn design's own historical rationale (mirroring how the sibling `bin/close-out.sh` bullet two lines above it was already reworded in this same re-freeze), or delete the clause.

#### Rejected / out-of-scope (this round)
- None raised this round that needed rejecting — the primary pass's three findings (title staleness, the `dispatch_case` remnant, the `bin/team-paths.sh` bullet staleness) are all accepted as genuine per direct verification above.

#### Synthesis audit ledger

| # | finding | primary (this round) | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | task title (spec H1 + board title) still claims a close-out backstop ships | Codex-native `Major` (map, `.shell-team/specs/…:1`, `.shell-team/todo.md:14`) | major | confirmed by direct read of both files, byte-for-byte; neither line was touched in this round's diff; the single most visible location in the whole task record for exactly the honesty property mandate item 4 asks after |
| 2 | `dispatch_case`'s `TEAM_REVIEWS_DIR`/scratch-reviews-dir plumbing is dead, teeth-only remnant | Codex-native `Minor` (map, `tests/close-out/run.sh:1117`) | minor | confirmed by direct read and by grepping `bin/close-out.sh` for `TEAM_REVIEWS_DIR` (zero); purely inert — no fixture's PASS/FAIL depends on this plumbing; the spec's own `## Verdict scope` names this exact token class as Major-*eligible*, but eligibility for evaluation is not the same as automatic severity, and zero functional or security consequence keeps this at minor, consistent with this repo's own precedent for fixture/comment-accuracy nits with no behavioral effect (e.g. T-1091's own review, Minor: fictional priced-id in a fixture) |
| 3 | `## Summarized sources`' `bin/team-paths.sh` bullet still states the withdrawn override-placement rationale as current | Codex-native `Minor` (map, `.shell-team/specs/…:34`) | minor | confirmed by direct read; the sibling `bin/close-out.sh` bullet two lines above was already correctly reworded in this same re-freeze, so this is an incomplete-sweep gap of the same class rather than an oversight the re-freeze never considered; purely a background/context passage, not read by an adopter and not gating any check line |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md`'s T-1092 entry to `READY_FOR_ENG`, linking the Major (both title instances) and the two Minors above. All three are plain prose edits outside the frozen intent block — none requires a re-freeze, a new operator ruling, or touches any `- check:` line, and none re-opens the carved-out design question (per this task's own `## Verdict scope`, which stays correctly out of scope here and is not re-litigated). The governance trail, the removal's functional completeness (AC5/AC6, the grammar gate, the two closed round-2 findings), and the honesty of every other shipped surface are all confirmed sound.

#### Fast-follow disposition
no fast-follow deferrals
