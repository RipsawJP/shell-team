# T-1089 QA review

<!-- QA verification record, round 1 -->

## Verdict: PASS → READY_FOR_REVIEW

The operator-approved re-cut of T-1086 closes the codex round-2 pre-commitment firing by narrowing the oracle's claim rather than patching a third instance. All three closed classes (basis conflation, zero-padded arithmetic, rep-spelling normalization) independently re-verified with fresh fixtures and, where possible, variants of my own beyond what the engineer's own mutation self-check covered. T-1086's frozen bytes and byte-locked evidence confirmed untouched from both sides.

## 1. Full sweep and gate checkers

- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1089-harness-recut.md 2>&1 | tail -3` → `check-acs: 12 passed, 0 failed, 1 skipped, 0 unrecognized` (AC13 SKIP by design) — exactly the conformant profile.
- `check-intent` → `aligned: T-1089 v1 (e1df61d316736a00e5898bc3f4e2449003971ec3)`.
- `check-handoff` → exit 0.
- `check-pii-shapes --base $(git merge-base feature/1086-subject-harness HEAD)` → `clean`.
- `check-provenance` → `conformant (6 decision entries, 0 sentinel)`.
- `check-interventions --task T-1089` → `conformant (0 entries, 1 sentinel)`.

## 2. T-1086's profile at HEAD

`CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1086-subject-harness.md 2>&1 | tail -3` → `17 passed, 3 failed, 1 skipped`, `FAILED: AC18 AC19 AC20` — exactly the expected profile. Individually decomposed: **AC12** PASS (the `- baseline-basis: machine-time — ` prefix survived T-1089's correction), **AC17** PASS (the byte-locked evidence section untouched) — both confirmed green as required. AC18 is the pre-existing ratification red (unaffected by this task); AC19/AC20 are the disclosed merge-point-scoped classes this task's own new spec file and board entry move (predicted in the spec's own Blast radius, exactly as it fired).

## 3. Claim re-scope, read adversarially as text — judged honest, not an excuse

Read all three boundary sites directly:
- `acceptance.sh`'s header (lines 24-32): "This is an enumerated-case instrument... It is NOT a decision procedure for conformance to interface.md's frozen contract — a candidate can pass every case here and still violate that contract on an input no case constructs." Stated plainly, in the file a reader opens first, before any case runs.
- `README.md`'s "What the oracle claims, and what it does not" paragraph: "Passing every case is not proof that every byte of interface.md's frozen contract is satisfied on every input" — same honesty, aimed at the subject's own implementer.
- The note's `- oracle-claim:`/three `- claim-limit:` lines: each limit names concrete, real inputs it declines to cover (`+5`, 64-bit integers, CRLF, duplicate headers, quoted CSV, Unicode digits for `contract-conformance-unbounded`; all four measured zero-padded arithmetic behaviors for its own class; the 7-vs-11 case-set drift, generalized, for the third) rather than a vague disclaimer.

This reads as a genuine boundary, not a defect wearing a boundary's clothes: it doesn't claim more than the instrument does, it doesn't retroactively excuse either of the two closed defect classes (both are still closed as classes, not merely re-labelled as "out of scope"), and it is stated in all three places a reader could reasonably stop.

**Zero-padded arithmetic literals, independently re-measured live on this host** (not accepted from the hand-off): `bash -c 'echo $((010))'` → `8`, rc=0; `bash -c 'echo $((08))'` → stderr `value too great for base`, rc=1; `bash -c 'echo $((-010))'` → `-8`, rc=0; `bash -c 'echo $((00))'` → `0`, rc=0. All four match the note's `- claim-limit: zero-padded-arithmetic-class` line exactly.

## 4. Class closures, exercised empirically (own fixtures, scratch worktree)

**AC5 (rep-normalization).** On a scratch `git worktree add --detach` at HEAD: mutated rep 2's evidence line to `rep=01` (the exact round-2 defeat shape, decimal-equal to rep 1) → `AC5: FAIL` — correctly caught. Then, beyond the engineer's own mutation set, mutated it a second way to `rep=001` (a differently-padded spelling the engineer's own probe didn't literally test) → `AC5: FAIL` again — confirms the `10#`-before-dedup ordering closes the whole class, not the one spelling. Restored, re-confirmed 12/0/1.

**AC4 (case set unchanged), own independent re-derivation.** `sed -nE 's/^[[:space:]]*(pass|fail) "([^"]+)".*$/\2/p' acceptance.sh | sort -u`, run against both the branch-point blob and HEAD directly (not through `check-acs.sh`) → both produce the same 11 labels, `diff` empty.

## 5. Signature sweep — recorded signatures plus own variants, no surviving hit

Re-ran the engineer's own recorded signatures (`coincide`, `with seven cases`, `same reasoning`, `single serial instance`) and confirmed their disposition: `coincide`/`same reasoning` survive only inside the `- supersedes: t1086-basis-coincidence` line's own description of what T-1086's frozen text says (a legitimate quote of the superseded claim, not a live restatement); `with seven cases` is fully absent; `single serial instance` is fully absent. Then ran **my own additional variants** the hand-off did not check (`same basis`, `same figure`, `same quantity`, `identical basis`, `equivalent to`, `equal to`, `is the same as`, `no different`, `same value`) — zero hits, and a targeted co-occurrence search of `wall-clock` and `machine-time` in the same line found none outside the corrected `- baseline-basis:` line itself and the already-correct `## Commensurability` section. No surviving basis-conflation sibling found.

## 6. Evidence byte-lock, from both sides plus a direct third check

- T-1089's own **AC6** and T-1086's own **AC17**: both PASS, confirmed in the full sweeps above.
- Own direct decomposition: `git show 5860078a…:docs/loop-engineering/tier2-subject-harness.md`'s extracted `## Baseline evidence` section vs. the working tree's own extracted section — both 12 non-blank lines, `cmp` exit 0, byte-identical.
- Ancestry, both directions independently confirmed: `git merge-base --is-ancestor 5860078a… ea393b4` (evidence ⊑ branch point) exits 0; `git merge-base --is-ancestor ea393b4 HEAD` (branch point ⊑ HEAD) exits 0 — the reversed ordering AC6 asserts, confirmed correct.

## 7. Fan-out audit

- Aggregation summary re-confirmed: `units: 93 — verdicts: 1527 — sentinels: 3`.
- T-1001: 12 fails in the new fan-out, matching the established clone-venue baseline exactly (the round-1 contention artifact vanished).
- T-1002: 5 fails (`AC15/23/26/27/28`), matching the established clone-venue baseline exactly.
- T-1086: 6 fails in the clone (`AC2/8/17/18/19/20`) — one more than the prior T-1086 fan-out's 5, and the delta is exactly `AC18`, now genuinely red at HEAD (confirmed in §2 above) rather than a clone-venue artifact; the other 5 remain the same disclosed base-ref clone artifacts already characterized.
- T-1089's own 8 clone fails (`AC2/3/4/6/7/10/11/12`) are all base-ref-reading criteria that cannot resolve `feature/1086-subject-harness`/`develop` inside a bare clone — consistent with the same disclosed venue-artifact class, and the real checkout's own 12/0/1 sweep (§1) settles what actually holds.
- `T-1020-supersede-adjudication.md`'s sentinel independently re-confirmed: `grep -c '^\- \[ \] \*\*AC' .shell-team/specs/T-1020-supersede-adjudication.md` → `0`; `bash bin/check-acs.sh` on it directly → `no acceptance criteria... found` — a correct sentinel, not a fan-out defect.

## Risk notes for reviewer

1. Per the spec's own `## Verdict scope`, my PASS on the claim-re-scope's own honesty judgment (§3) is a necessary but not sufficient read — whether the boundary is honest or an excuse is exactly the kind of judgment the cross-provider review's adversarial depth is priced for on this task.
2. T-1086's AC18/AC19/AC20 remain the designed, disclosed reds; none is a regression to chase.
3. This task's pre-commitment recurrence branch (spec Goal, final paragraph) governs any future round that lands an independent Blocker/Major on the re-scoped claim itself — flagged for the reviewer's own awareness, not because anything fired this round.
4. The mid-task harness-crash disclosure (engineer hand-off) was not independently re-verifiable after the fact (the affected fan-out logs no longer exist to inspect), but its stated recovery discipline (re-confirming the full 12/12 sweep against the pristine worktree rather than trusting pre-crash notes) is consistent with the clean final state I independently reproduced.

### Codex Review verdict: REQUEST_CHANGES (round 1)
- Task: T-1089
- Codex model: gpt-5.6-luna（`~/.codex/config.toml` の `model` 設定値。イベントストリームにモデル識別子が出力されないため、この一次値取得方法を採用）
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 8 files, +582/-8 lines (`git diff --stat ea393b4831d0296c50b12018117a77defb70f145...HEAD`)

**スコープの前提。** タスク説明の「Verdict scope」節に従い、凍結 v1 intent（`e1df61d3…`）・spec 自身のランタイム criteria（T-1089: 12/0/1、T-1086: 17/3/1 with AC18 ratified-red・AC19/AC20 disclosed merge-point classes — いずれも設計どおりの red であり defect ではない）・今ラウンド新規発見の finding に限定して判定した。11 ケースが「正しい 11 個か」「example-based oracle が最良の mechanism か」「T-1086 を patch すべきだったか」の 3 点は明示的にスコープ外（spec 自身の `## Verdict scope` が凍結）として扱った。

**独立再導出（自分の lens）。**

(i) **ゼロパディング算術 4 挙動の再測定**: `bash -c 'echo $((010))'` → `8`（rc=0）、`bash -c 'echo $((08))'` → `value too great for base`（rc=1）、`bash -c 'echo $((-010))'` → `-8`（rc=0）、`bash -c 'echo $((00))'` → `0`（rc=0）。note の `- claim-limit: zero-padded-arithmetic-class` 行の記載と完全一致。

(ii) **AC5 の normalize-before-dedup パイプラインを自分でトレース**: check 本体を抽出し、scratch cwd tree（note の相対パスを持つディレクトリ、check 内の `N=` ハードコードに対応）で 4 パターンをテスト — ①無改変（PASS 確認）、②`rep=01`/`rep=02` の両方をゼロパディングしつつ数値的には依然として distinct（`1`/`2`）にした fixture（**PASS を確認 — false positive を起こさないことの確認**）、③arm-id を `alien-arm-id` に書き換え（**FAIL を確認**）、④`oracle-exit=0` のまま `passed/total` を不整合にする書き換え（**FAIL を確認**）。round 2 の defeat 形状（`rep=01`/`rep=1`）と QA 自身の追加 variant（`rep=001`）は engineer/QA が既に検証済みなので重複実行しなかった。正規化はクラス全体を閉じており、かつ正当なゼロパディングを誤検出しないことを独立に確認した。

(iii) **signature sweep の not-hit 側を自分で再検証**: `phase-multiplexing.md` と `.shell-team/specs/T-1086-subject-harness.md` を branch point の blob と `cmp` し、両方とも byte-identical であることを確認。さらに QA の 9 variant（`same basis`/`same figure` 等）とは異なる自分自身の 12 variant（`are the same`/`match exactly`/`are identical`/`one and the same`/`no distinction`/`amounts to the same`/`same underlying`/`collapse into`/`reduce to the same`/`interchangeable`/`swap freely`/`either figure works`）で note 全体を sweep — 全て 0 件。加えて `verifies conformance`/`checks conformance`/`ensures the candidate`/`fully verifies`/`guarantees`/`proves conformance`/`confirms conformance`/`validates the contract`/`satisfies the contract`/`meets the contract` の 10 variant で note・README・acceptance.sh の 3 箇所すべてを sweep — 全て 0 件。basis conflation の姉妹インスタンス、および oracle-claim の残存 overclaim はいずれも見つからなかった。

(iv) **AC6 の reversed ancestry を自分で再導出**: `git merge-base --is-ancestor 5860078a… ea393b4` exit 0（evidence ⊑ branch-point）、`git merge-base --is-ancestor ea393b4 HEAD` exit 0（branch-point ⊑ HEAD）、evidence commit の blob から抽出した `## Baseline evidence` section と working tree のそれを `cmp` — byte-identical。QA の記載と完全一致。

(v) **T-1086 の HEAD プロファイルを自分で再現**: `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1086-subject-harness.md` → `17 passed, 3 failed, 1 skipped`、`FAILED: AC18 AC19 AC20` — 期待どおり一致（AC12・AC17 は green のまま）。

**Codex 起動記録。** `T-1086` の反省を踏まえ、今ラウンドから canonical basename に `-r1` サフィックスを付けて実行した（`T-1089-codex-primary-r1.{txt,jsonl}`、`T-1089-codex-adversarial-r1.{txt,jsonl}` — issue #335 対応）。primary pass（`codex exec ... review --base ea393b4…`）と、Major 検出後の adversarial pass（devil's-advocate プロンプトで AC4/AC9/claim-honesty の 3 点を検証）を実施。

#### Blockers

なし。今ラウンドの finding はいずれも spec 自身が明記する 4 つの never-dropped pre-commitment 対象（① 3 箇所での re-scoped claim、② basis signature sweep の 2 dispositions、③ 数値正規化された rep reconciliation、④ T-1086 の frozen bytes）のいずれにも該当しない（下記「Pre-commitment 発火判定」参照）。

#### Major

- `.shell-team/specs/T-1089-harness-recut.md:85`（**AC4** の `- check:` 行）— **ケース内容の変更をラベル保持のまま検出できない。** AC4 は `pass`/`fail` に渡されるラベル文字列の集合のみを branch-point と HEAD で比較しており、各ケースの fixture 内容（入力 CSV の値）や期待される stdout/stderr の中身は一切比較しない。自分で scratch worktree を作り、case9（`case9-negative-integers-truncate-toward-zero`）の入力値を `-3`/`-4` から `-30`/`-40` に書き換え、ラベル文字列自体は変更しないパッチを適用して AC4 の check を実行した結果、**rc=0（PASS）** のままだった — ラベルを保ったままケースの実体を差し替えても検出されない。spec 自身の言葉を借りれば「a renamed case is how a substituted case would look」と明記されているにもかかわらず、「same label, different content」という等価に危険な差し替え経路は未検証のまま残っている。この criterion は「11 ケース凍結」という再カット全体の中心規律を mechanical にするためのものであり、その検証機構自体にこの穴があることは看過できない。Codex の primary pass（`[P2]`）・adversarial pass（`P1`）双方が独立に検出し、自分の mutation test で再現した。
  - 提案: ラベル集合だけでなく、各 `pass "<label>"`/`fail "<label>"` 呼び出しを含むケースブロック全体（入力データ・期待 stdout/stderr の構築部分）を branch-point と byte-identical に比較するか、`acceptance.sh` の該当範囲（ケース定義部分）自体を byte-lock する。
- `.shell-team/specs/T-1089-harness-recut.md:101`（**AC9** の `- check:` 行）— **supersedes 行の「どこにあるかを示す」要件が非空文字列チェックでしか担保されていない。** `t1086-basis-coincidence` 行には `declared, never performed`/`b782ea39` の literal 要件があるが、もう一方の `t1086-oracle-claim-implicit` 行には非空（`.+$`）以外の要件がなく、テキストを `- supersedes: t1086-oracle-claim-implicit — see above.` のような空虚な内容に書き換えても AC9 は通過する（自分で scratch tree を用意し実際に確認、rc=0）。spec 本文は「each naming where the superseded claim lives so a reader can go read it」と明記しており、check がその要件を実際には強制していない。現在コミットされているテキスト自体は十分な内容（T-1086 のどのファイル・どの段落かを具体的に説明）を持っているため、今回の finding は将来ラウンドでの劣化を防げないという check の堅牢性の欠如であり、現在の記述内容そのものの defect ではない。Codex の primary/adversarial 両方が独立に `[P2]` として検出し、自分の mutation test で再現した。
  - 提案: `t1086-oracle-claim-implicit` 行にも、ファイルパスまたは section 名を示す literal（例: `.shell-team/specs/T-1086-subject-harness.md` または `tier2-subject-harness.md` の具体的な行/section 参照）の存在を要求する。

#### Minor / nits

- `docs/loop-engineering/subjects/subject-01/README.md`（"What the oracle claims" 段落）— README のこの段落は `docs/loop-engineering/tier2-subject-harness.md` の `- oracle-claim:`/`- claim-limit:` 行を「see」するよう指示しているが、`regenerate.sh` が実際に候補者の venue にコピーする 4 ファイル（README.md, interface.md, manifest.txt, acceptance.sh）に note は含まれない。したがって、venue のみを受け取る実際の候補者インスタンスはこの参照先を読むことができない。ただし README 自身の本文（「Passing every case is not proof that every byte of interface.md's frozen contract is satisfied on every input」）は自己完結しており、AC1 の本来の目的（「a reader who opens only the script, or only the brief, meets the boundary without having to find the note」）は満たされている — 欠けているのは 3 つの named limit の詳細な参照先のみ。Codex の primary pass（`[P3]`）と一致。fast-follow disposition: **won't-fix**（実害は限定的 — venue の読者はコアの境界宣言自体は読めており、詳細な note への到達不能性は編集上の改善点に留まる）。
- **ゼロパディング算術クラスの claim-limit がオラクル未検証であることを自ら開示している点** — adversarial pass に明示的に問うた（「defect を relabel した limit ではないか」）。結論: **HOLDS（正当な limit であり、defect の言い換えではない）**。理由: AC4 が「新規 fixture 追加禁止」を機械的に強制している以上、11 ケースへの新規 fixture 追加は設計上不可能であり、README の新規 `- subject-ac: 8` という contract obligation を追加した上で、oracle 自体はそれを検証しない旨を明示的に disclose するのが、この再カットの設計そのもの（route A: re-scope the claim であって re-widen the fixture set ではない）と完全に整合する。この開示は「instrument が enforce しないこと」を隠さず名指ししており、将来の judge（T-1087/T-1088）が `frozen-criteria` を直接読む際に見落とす余地を作らない。finding としては計上しない。

#### Adversarial findings

devil's-advocate プロンプトで AC4/AC9 の check body と claim-limit の honesty を Codex 自身に再検証させた（詳細生データ: `.shell-team/reviews/T-1089-codex-adversarial-r1.{txt,jsonl}`）。

1. 「AC4 の check はラベル保持のまま fixture 内容/期待出力の改変を検出できない」→ **breaks**（`case2` の `10,100`→`10,999` 等、`sum=60`→`sum=999` 等の具体例で確認）。
2. 「AC9 の check は supersedes 行が実際に displaced claim の所在を示すことを強制する」→ **breaks**（`see above` の vacuous text で通過）。
3. 「zero-padded-arithmetic-class limit の『oracle 自体は enforce しない』という開示は正当な limit であり、直すべき defect を relabel したものではない」→ **holds**（正当、11/11 のスコアを subject-ac 8 の充足とみなさない限り）。

#### Pre-commitment 発火判定（今ラウンドの必須確認事項）

T-1089 自身の frozen pre-commitment（`## Goal` 末尾）が定める 4 つの never-dropped コンポーネントは「① 3 箇所での re-scoped claim」「② basis signature sweep の 2 dispositions」「③ 数値正規化された rep reconciliation」「④ T-1086 の frozen bytes」である。今ラウンドの 2 件の Major（AC4・AC9）は、いずれもこの 4 コンポーネントのどれにも該当しない — AC4 は「11 ケース凍結」という T-1089 自身が**新規に導入した**別の discipline（pre-commitment の never-dropped リストには明記されていない）を守る check であり、AC9 は supersession の traceability という、これも pre-commitment のリストに無い別の discipline である。①〜④ 自体（re-scoped claim の文言、signature sweep、rep reconciliation、T-1086 の frozen bytes）は上記の独立再導出ですべて健全であることを確認した。したがって、**今ラウンドの finding は T-1089 自身の pre-commitment の recurrence 条項を発火させない** — カウントはオーケストレーターの責務だが、コンポーネント帰属をここに明記する。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC4: ラベル保持のままケース内容が変更可能 | primary: Codex-native `[P2]`→major(map)；adversarial: `P1`→機械的マッピングでは blocker | major | 現在コミットされている 11 ケースの内容自体は branch point から一切変更されていないことを自分で確認済み（claim は現時点で真）；穴は将来ラウンドに対する check の堅牢性であり、現行成果物の虚偽ではない — T-1086 round 1 の oracle 網羅性 finding と同種の降格理由 |
| 2 | AC9: supersedes 行の非空チェックのみで所在明示を強制しない | primary: `[P2]`→major；adversarial: `P2`→major | major | 変更なし。現行テキストは十分具体的だが、check 自体が spec 本文の要件（「naming where」）を強制していないことを自分の mutation test で確認 |
| 3 | README の note 参照が venue から到達不能 | primary: `[P3]`→minor | minor | 変更なし。AC1 の中核目的（venue-only reader が境界を理解できる）は README 本文単体で既に満たされているため実害は限定的 |
| 4 | zero-padded-arithmetic-class limit の自己開示 | adversarial: 明示的に "holds"（info） | 該当なし（finding として計上せず） | 設計意図（route A: re-scope, not re-widen）と完全に整合する正当な開示であり、defect の relabel ではない |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1089 を `READY_FOR_ENG` に戻し、Major 2 件（AC4・AC9 の check-line 堅牢性）を紐付ける。いずれも通常のワーク（ordinary rework round）で解決可能であり、pre-commitment の recurrence 条項は発火していない — 上記「Pre-commitment 発火判定」参照。

#### Fast-follow disposition

- README の note 参照が venue から到達不能（dangling cross-reference） — **won't-fix**（AC1 の中核目的は README 本文単体で満たされており、実害は限定的な編集上の改善点に留まる）。

### Codex Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1089
- Codex model: gpt-5.6-luna（`~/.codex/config.toml` の `model` 設定値。イベントストリームにモデル識別子が出力されないため、この一次値取得方法を採用 — round 1 と同一値、config.toml 未変更を確認済み）
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: branch全体 8 files, +657/-8 lines（`git diff --stat ea393b4831d0296c50b12018117a77defb70f145...HEAD`）；今ラウンドの rework delta は 2 files, +9/-5 lines（`git diff --stat 72e2d3a..HEAD` — `.shell-team/specs/T-1089-harness-recut.md` の AC1/AC3/AC4/AC9 check 行と `.shell-team/todo.md` の re-freeze 記録のみ）

**スコープ。** round 1 の 2 Major（AC4: label-set-only 比較、AC9: non-empty-only テスト）に対する engineer のリワーク（commit `ecbfc7a`、route (a): pm-spec の全数 inventory による AC1/AC3/AC4/AC9 の 4 行修正）を検証する round。手順は (1) unguided pass（round 1 の finding を一切教えずに `codex exec review --base` でブランチ diff をそのまま読ませる）→ (2) guided confirmation pass（round 1 の 2 Major と修復内容を明示し、「具体的な defeat する mutation を構築できるか」を問う）→ (3) Major 検出を受けた adversarial pass（AC4/AC9/AC12 の 3 サイトに対する devil's-advocate プロンプトで最小修正案と新たな defeat 可能性、および 4 サイト目の存在可能性を問う）の 3 段。生データ: `.shell-team/reviews/T-1089-codex-primary-r2.{txt,jsonl}`（unguided）、`.shell-team/reviews/T-1089-codex-confirmation-r2.{txt,jsonl}`（guided confirmation — 命名規約上の canonical basename ではないが `-r2` サフィックス規約に従う追加 capture）、`.shell-team/reviews/T-1089-codex-adversarial-r2.{txt,jsonl}`（adversarial）。

**独立再導出（自分の lens、Codex の主張はすべて自分でも mutation を作って再現済み）。**

Codex に頼らず、まず自分で全 12 個の `- check:` 行をスクリプト化して現行ツリーに対して実行 — **12/12 rc=0**（`bin/check-acs.sh` の結果と一致、独自の抽出スクリプトによる再現）。その上で `git worktree add --detach` によるスクラッチ木を都度作成・削除し、以下の具体的な mutation を自分の手で適用して check の rc を確認した。

1. **AC4（`- check: ` 行、spec:85）— label 保持のまま byte-lock 範囲外にケースを追加できる。** 修復後の check は「`# --- case` マーカーから `printf 'acceptance:'` 行まで」を byte-lock しているが、この範囲の**外側**（最初のマーカーより前、またはサマリー行より後）に既存ラベルを再利用した `pass`/`fail` 呼び出しを追加しても、ラベル集合数は 11 のまま変わらず、byte-lock 領域も変更されないため検出できない。自分で 2 通り確認: ①`# --- case1` マーカー直前に `if false; then pass "case1-missing-file"; fi` を挿入 → **rc=0**（検出されず）。②`printf 'acceptance:'` 行の直後に `if false; then pass "case2-valid-numeric-columns"; fi` を追加 → **rc=0**（検出されず）。Codex の unguided pass（`[P2]`、prepend 型）と confirmation pass（append 型）が独立にそれぞれ異なる mutation 形状で検出し、自分の再現で両方とも確認できた。
2. **AC9（`- check: ` 行、spec:101）— `t1086-oracle-claim-implicit` 側の supersedes 行には「所在明示」を強制する具体性が依然として無い。** 修復後の check は `t1086-oracle-claim-implicit` の抽出テキストに対し、literal `T-1086` の存在と `(##[[:space:]]+[A-Za-z]|-[[:space:]][a-z][a-z0-9-]+:)` という汎用パターン（見出しまたは `- key:` 形の何か）のいずれかを要求するのみで、実在するファイル・section を具体的に指すことまでは強制しない。自分で `- supersedes: t1086-oracle-claim-implicit — T-1086 — - placeholder: see above.` に書き換えて確認 → **rc=0**（通過）。Codex confirmation pass の変種 `T-1086 - bogus: nothing real here.` でも同様に確認 → **rc=0**。両方とも自分で再現。
3. **AC12（`- check: ` 行、spec:110）— NEW（今ラウンド新規発見、round 1 の inventory は捕捉していない）。derivation ラベルの重複を検出できない。** 現行の抽出ロジックは `awk` で該当ラベルの**最初の出現**のみを block として抽出し、`cmp` の比較対象もそれ一つだけ。同一ラベル `t1089-spec-population` の `<!-- BEGIN derivation --> ... <!-- END derivation -->` ブロックをもう一つ、同じ `- reproduce:` コマンドとともに複製し、複製側の `- set: base` 行の数値だけを `999` に改竄したものを追加して確認 → **rc=0**（改竄された複製ブロックは一度も検証されずに通過）。Codex の unguided pass が独立に発見し（`gpt-5.6-luna` 自身の指摘）、自分の scratch worktree で再現した。
4. **AC6（`- check: ` 行、spec:91）— NEW（今ラウンド新規発見、round 1 の inventory も pm-spec の全数 inventory も捕捉していない）。working tree 側の見出し重複を検出できない。** check は branch-point 側の blob に対してのみ `## Baseline evidence (raw, orchestrator-produced)` 見出しの出現回数が厳密に1であることを assert しているが、working tree（`$N`）側には同じ assert が無く、抽出も `awk` の最初の一致で止まる。working tree の note の末尾に同じ見出しをもう一つ追加し、明らかに改竄された内容（`TAMPERED DUPLICATE SECTION`）を書いても → **rc=0**（通過）。Codex の adversarial pass が「3 サイトは網羅的か」という問いへの回答として独立に発見し、自分で再現した。

**AC1・AC3 の姉妹修復は健全（round 1 の two Majors への repair 検証と別に、自分でも再確認）。** AC1: `enumerated` の検索が id prefix を sed で剥がした後のテキストに対して行われており、id 部分文字列への vacuous match が閉じられていることを、テキストから `enumerated` を除いた mutation で確認（→ rc=1、正しく red）。`acceptance.sh` の header 抽出が `set -uo pipefail` 行で確実に止まることも実ファイルの行番号で確認済み。AC3: `- claim-limit: zero-padded-arithmetic-class` 行が厳密に1本であることを assert してから4リテラルを要求するようになっており、同一 id の行を2本に分割して4リテラルを分散させる mutation で確認（→ rc=1、正しく red）。Codex confirmation pass も両方を独立に "sound" と判定。

**Pre-commitment 発火判定。** T-1089 自身の frozen pre-commitment が定める4つの never-dropped コンポーネント（① 3箇所での re-scoped claim、② basis signature sweep の2 dispositions、③ 数値正規化された rep reconciliation、④ T-1086 の frozen bytes）を、今ラウンドの4件の Major はいずれも対象としていない。AC4（ケース集合の byte-lock）、AC9（supersession の traceability）、AC12（population figure の再現可能性）、AC6（inherited evidence の re-anchoring）は、いずれも T-1089 自身が**新規に導入した**メカニカルガードであり、pre-commitment の never-dropped リストには載っていない。①〜④ 自体の内容は round 1 と同様に独立再導出でき、いずれも健全であることを確認した（round 1 の記録を再実行し矛盾なし、今ラウンドは差分がないため重複実行しない）。したがって、**今ラウンドの finding は T-1089 自身の pre-commitment の recurrence 条項を発火させない**。カウントと今後の運用判断はオーケストレーターの責務だが、コンポーネント帰属をここに明記する。

**運用上の懸念（判定に影響しないが明示する）。** 今回の4件は round 1 の2件（AC4・AC9）と**同一クラス**（guard-weaker-than-prose）の再発であり、しかも repair 後もなお同じ2サイトが**別の mutation 形状**で defeat 可能なまま残っている。加えて、route (a) で実施された pm-spec の「全12行に対する全数 inventory」は AC12・AC6 という**同型の欠陥**（最初の一致のみを抽出し、対象側の重複を検出しない）を見落としている。これは round 1 findingsの individual literal/vacuous-text という表層形状でinventoryを行ったためと推測され、「最初の出現のみを抽出/比較する」という、より一般的な部分クラスの探索が行われなかった可能性を示唆する。記憶ノート `feedback_switch-strategy-when-the-class-is-an-omission.md` の言う「欠落クラスは掃いても閉じない」パターンに一致する所見として記録する。

#### Blockers

なし。

#### Major

- `.shell-team/specs/T-1089-harness-recut.md:85`（**AC4**）— byte-lock 範囲（`# --- case` マーカー〜`printf 'acceptance:'`）の**外側**に既存ラベルを再利用した `pass`/`fail` 呼び出しを追加しても検出できない（prepend 型・append 型の両方で自分と Codex 双方が再現、rc=0）。
  - 提案: ケース定義部分全体（ファイル先頭のヘルパー定義を除く実行可能部分、または `acceptance.sh` 全体）を byte-lock するか、全 `pass`/`fail` 呼び出しの**出現回数**（重複除去前）を branch-point と比較する。
- `.shell-team/specs/T-1089-harness-recut.md:101`（**AC9**）— `t1086-oracle-claim-implicit` 行の「所在明示」要件が汎用的な正規表現でしか強制されておらず、実在しないファイル/section 名でも通過する（自分と Codex 双方が再現、rc=0）。
  - 提案: `t1086-basis-coincidence` 行と同様に、`t1086-oracle-claim-implicit` 行にも実在するファイルパスの literal（例: `docs/loop-engineering/tier2-subject-harness.md` の具体的な行/section 参照、または `- oracle-claim:` literal そのもの）の存在を要求する。
- `.shell-team/specs/T-1089-harness-recut.md:110`（**AC12**、NEW） — 同一ラベルの derivation ブロックを複製し、複製側の内容だけ改竄しても検出できない。抽出ロジックが常に最初の一致のみを使うため（自分で再現、rc=0）。
  - 提案: ループの先頭で各ファイルの derivation ラベルが重複していないこと（`sort -u` 後の件数が BEGIN 件数と一致すること）を assert する。
- `.shell-team/specs/T-1089-harness-recut.md:91`（**AC6**、NEW） — working tree 側で `## Baseline evidence (raw, orchestrator-produced)` 見出しが重複しても検出できない。branch-point 側のみ「出現回数=1」を assert しており、working tree 側には同等の assert が無いため（自分で再現、rc=0）。
  - 提案: working tree側（`$N`）にも同じ「出現回数=1」の assert を追加する。

#### Minor / nits

- round 1 で記録済みの README note 参照の到達不能性（`docs/loop-engineering/subjects/subject-01/README.md`）は今ラウンドで変化なし。round 1 の fast-follow disposition（won't-fix）を維持する。

#### Adversarial findings

`.shell-team/reviews/T-1089-codex-adversarial-r2.txt` に生データ。AC4/AC9/AC12 それぞれについて「最小修正で閉じられるか」「自分の提案した修正を自分で defeat できるか」を問うたところ、Codex は3件とも**新たな defeat 経路がある**と回答した（AC4: `eval 'pass "..."'` のような間接呼び出しは正規表現で捕まらない、AC9: 正しいファイルパスと `check-results` literal を両方含みながら間違った section を指すテキストは依然通過しうる、AC12: `grep -B 2` によるコマンド対応付けが空行以外のデコイ行を許容する）。加えて、3サイトが網羅的かを問うたところ、独立に **AC6** を第4のサイトとして指摘した（working tree側の見出し一意性が assert されていない、historical blob側のみ assert されている非対称性）。これは本 review 記録が自分で再現した4件目の finding と一致する。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC4: byte-lock 範囲外への重複ラベル挿入（prepend/append 両形状） | unguided: `[P2]`→major；confirmation: 明示的 major；adversarial: major、かつ「間接呼び出しでの再defeat」を追加指摘 | major | 現行コミット済み11ケースの内容自体は branch point から一切変更されていないことを自分で確認済み（claim は現時点で真）；穴は将来ラウンドに対する check の堅牢性の欠如であり、round 1 の AC4 finding と同一クラスの再発 |
| 2 | AC9: `t1086-oracle-claim-implicit` 行の所在明示が汎用パターンのみで強制される | unguided: `[P2]`→major；confirmation: 明示的 major；adversarial: major、かつ「正しいパスを含みつつ誤った section を指す」再defeatを追加指摘 | major | 現行テキスト自体は十分具体的（round1 と同じ降格理由は適用しない — repair後もテキスト内容自体は正しいが check の強制力が不十分という round1と同型の欠陥） |
| 3 | AC12: 同一ラベルの derivation ブロック重複が検出不能 | unguided: 独立発見、major；adversarial: 「ラベル一意性の事前assertで閉じられるが `grep -B 2` のデコイ耐性は別課題」と指摘 | major | 新規発見（round 1 のinventory・repairの対象外）。同型クラス（最初の一致のみ抽出）の第3のサイト |
| 4 | AC6: working tree 側の見出し重複が検出不能（branch-point側のみ一意性assert） | confirmation: 未問；adversarial: 独立発見、major | major | 新規発見。同型クラスの第4のサイト。round1のpm-spec全数inventoryも捕捉していなかった |

#### Recommendation

- REQUEST_CHANGES → `.shell-team/todo.md` の T-1089 を `READY_FOR_ENG` に戻し、Major 4 件（AC4・AC9・AC12・AC6 の check-line 堅牢性、いずれも「最初の一致/範囲外の重複を検出できない」という共通の部分クラス）を紐付ける。4件とも T-1089 自身の never-dropped pre-commitment コンポーネントには該当せず、recurrence 条項は発火していない — 上記「Pre-commitment 発火判定」参照。ただし round 1 と同一クラスの再発であり、かつ全数 inventory（route (a)）が見落とした新規サイトが2件出た点は、次のリワークの検索方針（「vacuous text/match」という表層形状ではなく「最初の一致のみを抽出/比較し、対象側の重複・範囲外を検出しない」という一般クラスでの再スキャン）に反映されるべきという所見をオーケストレーターへ申し送る。

#### Fast-follow disposition

- round 1 から継続の README note 参照到達不能性 — **won't-fix**（round 1 と同一理由: AC1 の中核目的は README 本文単体で満たされており実害は限定的）。
