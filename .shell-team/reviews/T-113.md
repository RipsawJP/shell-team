# T-113 QA verification record

- Verdict: PASS
- Commit reviewed: 9751516 (comment-only additions to two `bin/` scripts, two
  bullets in `agents/scrum-master.md`, two Japanese blockquote lines in
  `docs/templates/retro-template.md`, provenance file, board flag)
- Board: T-113 → READY_FOR_REVIEW
- Lens declaration: this task is **prose-only** — no new executable artifact,
  no new runnable procedure, no schema/behavior change (AC3 locks this
  mechanically and is independently re-verified below). The adversarial
  fixture synthesis checklist's "executable artifact" trigger is touched only
  at the file level (`bin/check-playbook.sh`, `bin/playbook-promote.sh`), not
  at the content level — every added/removed line in both is a comment, so
  there is no new runtime input surface for this task to synthesize fixtures
  against. The pre-existing fixture suites for both scripts (which already
  cover the classes 1-4 checklist) were re-run unchanged and stayed green,
  confirming no input-surface regression, not exercising a "new" surface that
  doesn't exist.

## Structural gates

- `bin/check-acs.sh .shell-team/specs/T-113-lessons-deidentification.md` →
  12 passed, 0 failed, 0 unrecognized (exit 0). Full per-AC output reviewed by
  hand; all 12 genuinely exercised (not vacuous — several ACs assert on the
  live `git diff develop` state, not just static file content).
- `bin/check-intent.sh .shell-team/specs/T-113-lessons-deidentification.md
  .shell-team/todo.md` → `aligned: T-113 v2 (c33b85a...)` (exit 0).
- `bin/check-provenance.sh .shell-team/provenance/T-113.md` →
  `conformant (2 decision entries, 0 sentinel)` (exit 0).
  Read both recorded decisions against the actual diff:
  - **DP-1 (no schema change)**: grounded in `bin/check-playbook.sh`'s existing
    `Source` field description text (`"<non-empty free text ... or \"n/a\">"`)
    and AC3's comment-only lock. Verified: the field description block is
    byte-unchanged except for the one inserted comment line in each script
    (see diff below) — the grounding citation is accurate, not padding.
  - **DP-2 (rule not machine-validated)**: grounded in the "NOT a semantic or
    security review" / "judges STRUCTURE ONLY" disclaimers already present in
    `bin/check-playbook.sh`'s and `bin/check-provenance.sh`'s headers
    respectively. Confirmed both strings exist verbatim in those files. Both
    decisions are genuinely non-trivial (a real "why not enforce this
    mechanically" judgment call, not a restated fact) and neither pads the
    record.

## AC3's comment-only lock, independently re-derived

Full diff of both touched `bin/` scripts vs `develop`:

```
diff --git a/bin/check-playbook.sh b/bin/check-playbook.sh
@@ -12,6 +12,7 @@
 #   - **Source**:       <non-empty free text — task/issue/PR ref, external
 #                        citation, or "n/a">
+#   A lesson records the pattern and the reason it recurs, never the identifying details of the incident. Source points at an artifact in this repository, or is n/a.
 #   - **Rule**:         <existing field>

diff --git a/bin/playbook-promote.sh b/bin/playbook-promote.sh
@@ -73,6 +73,7 @@
 #   --source        task/issue/PR reference, external citation, or n/a
+#   A lesson records the pattern and the reason it recurs, never the identifying details of the incident. Source points at an artifact in this repository, or is n/a.
 #   --rule          the takeaway, in one sentence (this is what eventually
```

Exactly one `+` line per file, both comment lines, zero deletions. Confirmed
by grepping the full diff for `^[+-]` and manually classifying every hit —
both are `#`-prefixed. No hidden non-comment change.

- `bash tests/check-playbook/run.sh` → all assertions passed (exit 0, 60+
  fixtures including T-047/T-108 regression locks).
- `bash tests/playbook-promote/run.sh` → all assertions passed (exit 0).

Behavior unchanged: both suites are the pre-existing fixture suites, run
verbatim, with no modification.

## Canonical-line placement, independently re-derived

- `grep -qxF` (byte-exact, full-line) for all three English canonical-line
  sites (`bin/check-playbook.sh`, `bin/playbook-promote.sh`,
  `agents/scrum-master.md`) — all three MATCH.
- AC2's uniqueness claim: `grep -rlF 'A lesson records the pattern and the
  reason it recurs' bin agents docs` → exactly the same 3 files, no fourth
  copy anywhere under `bin/`, `agents/`, or `docs/`.

## The rule text follows the rule

- `bash bin/check-pii-shapes.sh --base develop` → `clean (no PII-shaped bytes
  found)` (exit 0).
- Read every added line by hand (both `bin/` comment lines, both
  `agents/scrum-master.md` bullets, both `docs/templates/retro-template.md`
  blockquote lines): all are generic rule prose describing the discipline
  itself — no name, path, incident detail, or company/user identifier in any
  of them.

## Regression

All green, run directly (not taking the engineer's word for it):

- `bash bin/check-prompt-sync.sh` → all registered prompt blocks in sync.
- `bash tests/check-retro/run.sh` → all assertions passed.
- `bash tests/gen-playbook-blocks/run.sh` → all assertions passed.
- `bash tests/check-pii-shapes/run.sh` → all assertions passed (AC12).
- `bash tests/check-commit-identity/run.sh` → all assertions passed (AC12).
- `bash tests/gitignore-raw-dumps/run.sh` → all assertions passed (AC12).
- `bash bin/check-pii-shapes.sh --base develop` → clean (AC10).
- `bash bin/check-commit-identity.sh --base develop` → clean (AC10).
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0 (board lint).
- `bash bin/check-board-headings.sh .shell-team/todo.md` → exit 0.
- `shellcheck` on the exact CI argument list (from
  `.github/workflows/check-handoff.yml`, local shellcheck 0.11.0, matching
  the CI-pinned version) → exit 0, no findings.
- Commit identity on 9751516: `RipsawJP <15995527+RipsawJP@users.noreply.github.com>`
  for both author and committer — conformant shape.
- `git status --short` clean before and after this verification (aside from
  this review file and the board flag flip); no stray edits to `bin/`,
  `tests/`, `docs/`, or `agents/` were introduced during verification.

## What could NOT be verified by execution (prose-only lens, disclosed per rule)

- **Whether the rule actually changes future lesson-writing behaviour.** This
  is an authoring-time discipline aimed at a human/agent author; nothing in
  this repository's tooling can mechanically prove a future author reads and
  follows it. That is out of QA's execution lens — it belongs to the
  Codex/static review lens (does the wording read as intended, is it
  discoverable at the point of authoring) and, ultimately, to operational
  observation over time (does a future lesson entry actually omit
  identifying detail). Not faked here.
- **AC9's "agent stays discoverable at drafting time" placement judgment**
  (are the two bullets in the section an author actually reads) is a
  structural check (`## Rules` section, no frontmatter touch) — confirmed
  mechanically — but whether placement is *effective* in practice is the same
  unverifiable-by-execution class as above.

## Edge cases tried

- Confirmed no fourth undocumented copy of the canonical sentence exists
  anywhere under `bin/`, `agents/`, `docs/` (AC2, re-derived independently of
  the spec's own check).
- Confirmed the two `bin/` script diffs contain literally nothing but the one
  comment line each — read every `+`/`-` line by hand rather than trusting
  the count-based AC3 check alone.
- Confirmed `agents/scrum-master.md` has no `.ja.md` counterpart and its
  frontmatter (`name`/`description`/`tools`/`model`) is untouched (AC9),
  consistent with this repository's English-only agent-file policy.

No out-of-input-space or environmentally-unverified findings to flag — this
task has no runtime input surface changed by its diff, so the "Input space"
rule's exemption and the adversarial-fixture checklist's skip/environmental
tracking do not have any applicable finding to attach to (spec's own Input
space section is unchanged from — and validated against — the unchanged
`bin/check-playbook.sh`).

## Risk notes for reviewer

- The task's empirical surface is genuinely thin by design (prose/comment-only,
  per its own Non-goals). Everything mechanically checkable is green; the only
  open question is a human-judgment one (does the wording land with a future
  author), which is explicitly out of scope for this task per DP-2 and is the
  right thing for the Codex/static reviewer to weigh in on, not something QA
  can close by running a command.
- T-111/T-112 are both already `READY_FOR_MERGE`; the only cross-checks
  performed here against them are the ones AC10/AC12 themselves demand
  (self-application of both checkers, and re-running both suites +
  `gitignore-raw-dumps`) — no broader re-review of T-111/T-112 was performed,
  consistent with the task scope given to this verification.

---

### Codex Review verdict: REQUEST_CHANGES
- Task: T-113
- Codex model: gpt-5-codex（codex-cli 0.144.6, `codex exec --sandbox read-only`）
- Verification mode: static-only
- Diff scope: commit `9751516` — 6 files, +21/-1（`.shell-team/provenance/T-113.md`, `.shell-team/todo.md`, `agents/scrum-master.md`, `bin/check-playbook.sh`, `bin/playbook-promote.sh`, `docs/templates/retro-template.md`）。`bin/check-playbook.sh` / `bin/playbook-promote.sh` は各 +1 行（コメント行のみ）で自分で `git diff develop` を取って再確認済み（QA の再導出と一致）。

このレビューは T-113 のみを対象とする。T-111 / T-112 は既に `READY_FOR_MERGE` 済みで対象外。

- internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation): 本レビューの背景（GitHub issue #6, Layer 1 item 3）は spec の Problem 節の paraphrase を根拠にした。sandbox 内から `gh issue view 6 --repo RipsawJP/shell-team` を実際に試みたが `tls: failed to verify certificate` でネットワーク到達不能だったため、issue #6 の実文と spec の paraphrase が一致しているかどうかは確認できていない。spec 本文・intent-block・4 surface の実装内容が内部的に整合しているかは直接ファイルを読んで検証済みだが、issue 自体の正典との突合はできていない — 存在しないのではなく到達できないだけであり、orchestrator 側の一次確認に委ねる。

#### Blockers
- なし。

#### Major
- **`bin/playbook-promote.sh` — canonical surface #2（CLI の usage block）が実際には `--help` に一切表示されない。** `--help|-h)` の実装は `sed -n '2,45p' "$script_path"`（162-163 行目）で、ファイルの 2〜45 行目しか出力しない。今回追加された canonical rule（76 行目）はもちろん、`# Usage:` ブロック自体（63 行目開始、`--source` の説明は 75 行目）が丸ごとこの範囲の外にある。つまり、このスクリプトを実際に `--source ... --help` で使う人間は、canonical rule はおろか `--source` の説明さえ一度も目にしない。この欠陥自体は T-113 が触れる前から存在する（今回の 1 行差分が `sed` の範囲を変えたわけではない）が、spec の「canonical-surface inventory」が `bin/playbook-promote.sh` の usage block を「the CLI that authors an entry — the format definition's operative pair」と明記して canonical surface に指定し、AC1 がそこへの配置を要求している以上、**この配置が実際には到達不能である事実は T-113 自身の目的（「lesson を書く人に規律を伝える」）を裏切る**。
  - 独立検証: `grep -n "sed -n" bin/playbook-promote.sh` → `sed -n '2,45p' "$script_path"`（163 行目）。`grep -n "^# Usage:\|^#   --source"` → `# Usage:` は 63 行目、`--source` は 75 行目、canonical rule は 76 行目 — いずれも 45 行目より後。
  - 是正は AC3 の comment-only ロックを破らずに可能: canonical rule の行を、既存の 2〜45 行目の範囲内（例えばファイル冒頭の説明コメント近辺）にも重ねて置く、または `--help` の表示範囲を広げる（後者は sed の引数変更＝コメント行以外の変更になるため AC3 と抵触する可能性がある。前者＝2 行目の重複コメント追加なら AC1（`grep -qxF` は存在確認のみ）・AC2（ファイル単位のカウントで file count は変わらない）・AC3（追加行はコメント行）のいずれも満たしたまま解決できる）。engineer 側で対応可能な、この round 内で閉じられる指摘。
  - Codex adversarial pass（devil's-advocate）でも `holds` / P2（=Major）で維持。ただし adversarial pass が根拠として挙げた「同一リポジトリの T-101 で同種の sed 範囲欠陥を修正した先例がある」という主張は、**このリポジトリに T-101 という task/spec/ファイルが一切存在しないことを `grep -rn "T-101"` と `find . -iname "*T-101*"` で確認し、裏付けが取れなかった**（存在しない先例を Codex が生成した可能性が高い）。この点は指摘の severity 判断には採用せず、削除している（下記 synthesis audit ledger 参照）。Finding の本体（sed 範囲と行番号の不一致）自体は私自身が直接ファイルを読んで独立に確認した事実であり、この誤った先例主張とは独立に成立する。

#### Minor / nits
- **canonical sentence の「Source points at an artifact in this repository, or is n/a.」が、直前・不変の field description（`bin/check-playbook.sh` 13 行目 / `bin/playbook-promote.sh` 75 行目の「task/issue/PR ref, external citation, or n/a」）と同一コメントブロック内で二択 vs 三択の枠組みの緊張を生んでいる。** spec の Input space class 3 は「external citation は引き続きスキーマに受理され、識別情報を含む場合に限ってルールが抑制する」と明記するが、canonical sentence 自体（4 surface すべてに逐語コピーされる文）はこの nuance を持たず、単純な二択に読める。`agents/scrum-master.md` と `docs/templates/retro-template.md` にはこの三択の既存文脈が一切隣接していないため、その 2 面だけを読む著者は external citation が是か非か判断できない。
  - severity 判断: 第 1 ラウンドでは Major（Codex 判定 P1）としたが、adversarial pass で `partially holds` / P3 に是正された。理由: (a) Input space が「T-NNN のような task id も in-repo artifact の例」として挙げており、「artifact in this repository」は追跡ファイルに限らず GitHub issue/PR 参照も指すと合理的に読める余地がある（GitHub issue #42 のような同一リポジトリ内参照が禁止される、という当初の読みは過大だった）。(b) 残る external citation 周りの曖昧さも、スキーマ・shape checker・実行動作を一切破らず、実データの漏洩を新たに生むものでもなく、DP-2 が明記する「このルールは意図的に機械検証しない・human-approval と PR review が実質的な防御」という設計とも整合する。結果として severity は Minor に是正。
  - 是正は容易（例: 「... or an external citation that itself carries no identifying detail, or is n/a.」のような一文追加）だが、AC1/AC6 が canonical line を逐語ロックしているため、変更には spec 側の intent-block 改版（v2→v3）が必要になる。今回の merge をブロックするほどではないため fast-follow とする。
- **「identifying details of the incident」という語のみでは、雇用主名・顧客名・チケット/JIRA ID・hostname・IP・username・ローカル絶対パスのすべてを明確に含意するとは言い切れない。** 日本語版「インシデント固有の識別情報」も同じ幅を引き継ぐ。ただし、このリポジトリの他のチェッカー（T-111 の shape checker、DP-2 の「semantic judgment は行わない」という設計哲学）と同様、このルールは authoring-time discipline であって網羅列挙のチェックリストではない。列挙を追加すると「列挙されていないものは対象外」という誤読を新たに招くリスクがあり、現状の一般的な表現の方が this task の設計思想（機械強制しない・良識に訴える）に整合する。severity は Minor 止まりとし、blocking しない。
- **`docs/templates/retro-template.md` の「Lesson 候補」ブロックの実際のブレット形式（`[common]` / `[target-specific]` + 自由記述 1〜3 行）には Source サブフィールドが存在しない。** 新しく追加された匿名化ガイダンス（80-81 行目）が唐突に「Source は...」と述べるため、retro を書く人間が「今この候補ブレットに Source を書くべきか」「後で `tasks/lessons.md` に昇格する時の話か」を判断しづらい。実際には後者（`bin/playbook-promote.sh` での昇格時点の規律）のはずだが、そのことがテキスト上明示されていない。severity は Minor。

#### Adversarial findings
- Finding「Source の二択/三択の緊張」は adversarial pass で severity を Major → Minor に是正（上記参照）。
- Finding「`bin/playbook-promote.sh` の `--help` 到達不能」は adversarial pass でも Major（P2）で維持。ただし挙げられた「T-101 の修正先例」という根拠は独立検証の結果、リポジトリ内に存在せず不採用（下記 ledger 参照）。
- 追加で破壊的な組み合わせ（PII shape checker を実際に壊す、実データが漏洩する等）は adversarial pass でも発見されず、Blocker 相当への格上げ要素はなし。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Source の二択（canonical sentence）vs 三択（既存 field description）の枠組み矛盾 | primary: Major（Codex P1）／adversarial: partially holds, P3 | minor | Input space が task id を in-repo artifact の例として挙げており「artifact in this repository」は issue/PR 参照も含み得る読みが成立し、当初の「issue 参照まで排除される」という読みは過大だった。残る external citation の曖昧さも schema・実行動作・shape checker を破らず、DP-2 の「意図的に機械検証しない」設計と整合するため、Major から Minor へ是正。 |
| 2 | `bin/playbook-promote.sh` の adversarial pass が挙げた「T-101 での同種修正の先例」という根拠 | adversarial: この根拠を提示して Major 維持を補強 | 不採用（finding 自体の severity には影響せず） | `grep -rn "T-101"` および `find . -iname "*T-101*"` でリポジトリ内に該当タスク/ファイルが一切存在しないことを確認。Codex が存在しない先例を生成した可能性が高く、根拠として採用しない。ただし finding 本体（`--help` の `sed -n '2,45p'` と canonical rule の行番号 76 の不一致）は私自身が直接ファイルを読んで独立に確認済みの事実であり、この不採用は finding 本体の severity（下記参照）を変えない。 |
| 3 | `bin/playbook-promote.sh` の `--help` 到達不能（canonical surface #2 が実質無効） | primary: Major（Codex P1）／adversarial: holds, P2 | major（据え置き） | 私自身の独立検証（`sed -n` の実装と行番号の直接確認）で primary/adversarial 双方の判定を支持。pre-existing な欠陥だが、AC3 の comment-only ロックを破らずに是正可能（2〜45 行目の範囲内に canonical rule を重ねて置く等）なので、engineer 側で本 round 内に対応可能と判断し、対応必須の指摘として扱う。 |

#### Recommendation
- REQUEST_CHANGES → `.shell-team/todo.md` の T-113 を `READY_FOR_ENG` に戻す。対応すべきは Major 1 件（`bin/playbook-promote.sh` の `--help` 到達範囲に canonical rule が入っていない）。comment-only の追加で解決可能（AC1/AC2/AC3 いずれも維持したまま是正できる）ので、rework の規模は小さい想定。

#### Fast-follow disposition
- **Source の二択/三択の枠組み矛盾（Minor、external citation の扱いが canonical sentence だけでは不明瞭）** — file-an-issue（理由: 是正は容易だが AC1/AC6 が canonical line を逐語ロックしているため、spec の intent-block 改版（v2→v3）を伴う。今回の merge をブロックするほどの実害はないが、独立した改善として追跡する価値がある）。
- **「identifying details of the incident」の網羅性が語のみでは自明でない（Minor）** — won't-fix（理由: 列挙を追加すると「列挙されていないものは対象外」という誤読を新たに招くリスクがあり、DP-2 が明記する「意図的に機械検証しない・良識に訴える authoring-time discipline」という設計思想には現状の一般的な表現の方が整合する）。
- **`docs/templates/retro-template.md` の Lesson 候補ブロックに Source サブフィールドが実在しないまま Source ガイダンスが挿入されている（Minor）** — file-an-issue（理由: 「`tasks/lessons.md` への昇格時点の規律である」という一言を添えるだけで解消でき、実害は小さいが著者の誤解を防げる）。

---

## QA round 2 (rework re-verification, scoped to `git diff 4722e05..ad573d2`)

- Verdict: PASS
- Scope: this round re-verifies only the round-2 rework delta (commit
  `ad573d2` on top of `4722e05`) against the Codex round-1 Major finding —
  `bin/playbook-promote.sh`'s canonical rule (surface #2) was placed at line
  76-77, entirely outside the `--help` display range (`sed -n '2,45p'`,
  line 163), so a CLI user reading `--help` never saw it. T-111/T-112 out of
  scope, per instruction; not re-touched.

### Item 1 — the acceptance, empirically

- `bash bin/playbook-promote.sh --help` **does now print the canonical rule
  sentence** — confirmed by running the actual command, not by reading the
  diff. Ran it a second time piped through `grep -c` to confirm it appears
  **exactly once** (no duplicate-display confusion): the new copy sits at
  line 4 (inside the `2,45p` window); the pre-existing copy at line 77
  (inside the `--source` usage-block entry, at line 76-of-old/+1) sits
  **outside** the window and is correctly not echoed by `--help` — this is
  expected and matches the provenance decision's own reasoning, not a
  regression.
- Confirmed the pre-existing copy near `--source` is still present, byte-for
  -byte, at its original relative position (still immediately preceding the
  `--rule` line in the usage block) — nothing was moved or deleted, only a
  second copy was added.
- AC1's byte-exact matches (`grep -qxF`, full physical line, for all three
  English canonical-line surfaces — `bin/check-playbook.sh`,
  `bin/playbook-promote.sh`, `agents/scrum-master.md`) — all three still
  MATCH, run directly, not inferred from AC-runner output alone.

### Item 2 — AC3 comment-only lock, re-derived on the FULL branch diff

Re-ran the exact AC3 check command against `develop` (not just the round-2
delta):

```
test "$(git diff develop -- bin/check-playbook.sh bin/playbook-promote.sh | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vcE '^[+-][[:space:]]*#')" -eq 0
```

→ true (no non-comment `+`/`-` line in either script across the whole
branch). Both `+`-comment presence assertions also true. This subsumes both
round 1's addition (line 77) and round 2's addition (line 4) — confirmed
they are both `#`-prefixed and the full diff contains zero non-comment
churn.

Cross-checked independently against `tests/errexit-safe/run.sh`'s part
(iii) completeness self-audit, per the file:line-pin lesson in the
playbook: that suite's content-aware re-derivation of every errexit-unsafe
candidate site in `bin/` (exact file:line:CONTENT, not file:line alone)
still passes — `check-playbook-usage`/`check-playbook-unreadable` and
`playbook-promote-die` behavioral rows all still fire correctly, and the
suite's own mutation self-check (proving the content-aware match would
catch a rewritten NOT_APPLY line) still passes. The 1-line insertion in
each script shifted line numbers below the insertion point, but the
registry pins by content, not line number alone, so nothing silently
drifted out of coverage. `bash tests/errexit-safe/run.sh` → all assertions
passed (exit 0).

### Item 3 — check-acs / check-intent / check-provenance

- `bash bin/check-acs.sh .shell-team/specs/T-113-lessons-deidentification.md`
  → **12 passed, 0 failed, 0 skipped, 0 unrecognized** (exit 0). Full
  per-AC output read; AC1-AC12 all genuinely exercised (AC3 re-runs both
  fixture suites live, not just a diff-shape grep).
- `bash bin/check-intent.sh .shell-team/specs/T-113-lessons-deidentification.md
  .shell-team/todo.md` → `aligned: T-113 v2 (c33b85a...)` (exit 0) — intent
  version unchanged from round 1, consistent with this round being an
  engineering fix to a Codex finding, not a respec.
- `bash bin/check-provenance.sh .shell-team/provenance/T-113.md` →
  `conformant (3 decision entries, 0 sentinel)` (exit 0).
  Judged the new 3rd decision (structural conformance already confirmed by
  the checker; semantic judgment is QA's own):
  - **Decision**: duplicate the canonical line into the `--help`-displayed
    range rather than move the `sed` range or drop the `--source`-adjacent
    copy.
  - **Non-trivial**: it weighs three real constraints against each other —
    moving the `sed -n '2,45p'` line itself would touch a non-comment line
    and break AC3's lock; removing the `--source`-adjacent copy would
    contradict the spec's own "Notes for engineer" instruction (verified:
    `.shell-team/specs/T-113-lessons-deidentification.md:268-272` literally
    says to place the line "inside the `--source` entry of
    `bin/playbook-promote.sh`'s usage block"); duplicating satisfies AC1
    (existence), AC2 (file-count, not occurrence-count — confirmed a
    same-file duplicate cannot move the AC2 count), and AC3 (comment-only)
    simultaneously. This is a genuine "why not the alternatives" call, not
    a restated fact.
  - **Grounding verified against the actual file**: `sed -n '2,45p'
    "$script_path"` — confirmed present at `bin/playbook-promote.sh:164`
    (unchanged, not touched by this round). The cited round-1 Major finding
    text is present verbatim in this same review file's round-1 section.
    Both citations are accurate, not padding.

### Item 4 — AC2 file-count uniqueness, re-derived independently

```
grep -rlF 'A lesson records the pattern and the reason it recurs' bin agents docs | wc -l
```
→ **3** (`bin/check-playbook.sh`, `bin/playbook-promote.sh`,
`agents/scrum-master.md`). Confirmed the second copy inside
`bin/playbook-promote.sh` (two occurrences in one file, via `grep -n`) does
**not** inflate the file-count to 4 — `-l` lists files, not occurrences, so
a same-file duplicate is invisible to this metric by construction, matching
the provenance decision's own claim. Independently ran `grep -c` on
`bin/playbook-promote.sh` alone to confirm 2 occurrences within that one
file, consistent with the intended round-2 duplication.

### Item 5 — regression

All run directly, not taken on the engineer's word:

- `bash tests/playbook-promote/run.sh` → all 24 assertions passed (exit 0).
- `bash tests/check-playbook/run.sh` → all 56 assertions passed (exit 0).
- `bash bin/check-pii-shapes.sh --base develop` → clean (exit 0).
- `bash bin/check-commit-identity.sh --base develop` → clean (exit 0).
- shellcheck on the exact CI argument list (from
  `.github/workflows/check-handoff.yml`, local 0.11.0 matching the CI pin)
  → exit 0, no findings (both touched scripts included in the list).
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0 (board lint).
- `bash bin/check-board-headings.sh .shell-team/todo.md` → exit 0.
- `git status --short` clean before this verification, and remained clean
  throughout (checked again immediately before appending this section) —
  no stray edits introduced to `bin/`, `tests/`, `docs/`, or `agents/`
  during verification. No destructive git operation was run at any point.

### Adversarial fixture synthesis checklist — applicability

This task's deliverable is comment-only prose inside two executable
scripts (no new runtime input surface — same lens declaration as round 1)
**plus, this round specifically, a documented runnable procedure**:
`bin/playbook-promote.sh --help`. Per checklist class 4 ("test-harness
short-circuit"), that documented command was run exactly as written
(bare invocation, not just read from the diff) — this is the one new
empirical obligation round 2 introduces over round 1, and it is the
obligation this round's finding was about in the first place. No new
adversarial payload-shape fixtures apply: the change is still two comment
lines with no new argument, no new field, no new schema branch.

### Risk notes for reviewer

- The fix is a duplication, not a structural fix to the pre-existing
  `sed -n '2,45p'` truncation bug in `--help` (which also cuts off the
  `--source` field description itself, not just the canonical rule — this
  pre-existing defect is explicitly out of this round's scope per the
  coordinator's instruction and per the engineer's hand-off, and remains
  unaddressed). A future task widening the `--help` range would need to
  re-check for now-duplicate display of the canonical line.
- The three round-1 Minor findings (two-vs-three-way `Source` framing
  tension, "identifying details" not enumerating a checklist, and the
  retro-template's Lesson-候補 block having no literal `Source` sub-field)
  were explicitly deferred as fast-follow and are untouched this round —
  consistent with the coordinator's scoping instruction. Not re-litigated
  here.
- No out-of-input-space or environmentally-unverified findings this round;
  no runtime input surface was introduced by the delta.

### QA verdict: PASS
- Task: T-113 → READY_FOR_REVIEW
- Tests: playbook-promote 24/24 passed, check-playbook 56/56 passed,
  errexit-safe all assertions passed, 0 failed
- Acceptance criteria: 12/12 checked off (check-acs), check-intent aligned
  (v2), check-provenance conformant (3/3 decisions, all grounding verified)
- Edge cases tried: `--help` occurrence-count (exactly 1, no duplicate
  display); AC2 file-count non-inflation from a same-file duplicate;
  errexit-safe file:line-pin registry survival across the line-number shift
- Risk notes for reviewer: pre-existing `sed -n '2,45p'` truncation of
  `--help` (also cuts off `--source`'s own description) remains unfixed by
  design (out of this round's scope); 3 round-1 Minor findings remain
  deferred as fast-follow, untouched this round
