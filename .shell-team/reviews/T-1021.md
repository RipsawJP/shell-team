# T-1021 QA verification record

- Verdict: PASS
- Commit reviewed: `7cd77ae` (fixes + audit document + provenance/interventions), on top of `7a927b7` (spec + board entry, intent frozen v1). Base `develop` @ `a865ec0` (confirmed `origin/develop` == `a865ec0`).
- Board: T-1021 `READY_FOR_QA` → `READY_FOR_REVIEW`
- Frozen intent: v1 `040a10fc9ae2cca351561baa033758410eceb4d3`, zero re-freezes. `bash bin/check-intent.sh .shell-team/specs/T-1021-arith-base10-audit.md .shell-team/todo.md` → `aligned: T-1021 v1 (040a10fc9ae2cca351561baa033758410eceb4d3) matches ...` — run directly, not taken on the engineer's word.

## Mandatory structural gates

- `bash bin/check-intent.sh .shell-team/specs/T-1021-arith-base10-audit.md .shell-team/todo.md` → aligned (exit 0).
- `bash bin/check-provenance.sh .shell-team/provenance/T-1021.md` → conformant (4 decision entries, 0 sentinel). Read all four decision triples; each names two structurally valid designs and grounds the choice in a specific spec clause (D4's declination clause, the Notes-for-engineer's normalize-once model, D2's ladder). Structurally sound.
- `bash bin/check-interventions.sh --task T-1021 .shell-team/interventions/T-1021.md` → conformant (0 entries, 1 sentinel — "no interventions occurred").

## check-acs.sh

`bash bin/check-acs.sh --dry-run .shell-team/specs/T-1021-arith-base10-audit.md` reviewed first (read-only, 24 `check:` lines, 0 unrecognized).

Live run *before* this file existed: **23 passed, 1 failed (AC22), 0 skipped** — AC22 failed solely on `test -r .shell-team/reviews/T-1021.md` (this file did not exist yet), exactly matching the engineer's own predicted post-implementation shape.

**Final re-run after writing this file and the board sub-bullet below:**

```
$ CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1021-arith-base10-audit.md
...
check-acs: 24 passed, 0 failed, 0 skipped, 0 unrecognized (.shell-team/specs/T-1021-arith-base10-audit.md)
```

24/24 — AC1 through AC24 all PASS.

## 1. The completeness accounting — independently re-derived, not spot-checked

Re-ran all eight of the document's pinned search commands myself against the
current tree (not transcribed from the document):

| context | my live recount | document's declared raw |
|---|---|---|
| `$(( ))` | 140 | 140 |
| `(( ))` | 12 | 12 |
| `for (( ))` | 0 | 0 |
| `let` | 11 | 11 |
| array subscript | 162 | 162 |
| `${v:off:len}` | 2 | 2 |
| `[[ -eq ]]` | 65 | 65 |
| `printf '%d'` | 17 | 17 |

All eight match exactly. Live raw total = 140+12+0+11+162+2+65+17 = **409**.

**Population**: `find bin -maxdepth 1 -type f | wc -l` → **35**, matching the
document's 35 `- file:` lines exactly (byte-for-byte set comparison, same as
AC1's own check). Per-file `sites:` values sum to **273**, matching the total
count of `- site:` lines (**273**) exactly.

**Reconciliation identity, re-derived per context** (not just each
reconcile line's own arithmetic, which is all AC4 checks): for every one of
the 8 contexts I independently counted `- site:` rows carrying that exact
context label and confirmed the count equals the reconcile line's declared
`inventoried sites` value:

```
$(( ))            -> 136 site-rows  (reconcile declares 136)
(( ))             -> 8   site-rows  (reconcile declares 8)
for (( ))         -> 0   site-rows  (reconcile declares 0)
let               -> 0   site-rows  (reconcile declares 0)
array subscript   -> 52  site-rows  (reconcile declares 52)
${v:off:len}      -> 0   site-rows  (reconcile declares 0)
[[ -eq ]]         -> 65  site-rows  (reconcile declares 65)
printf '%d'       -> 12  site-rows  (reconcile declares 12)
```

All 8 match. I additionally attributed every one of the document's
`- excluded:` lines to a context by its stated reason (wholly-literal-operand
→ its context; comment-only and awk-internal → the context whose raw-hit
pattern the quoted/embedded text would actually match) and summed per
context: `$((` context excluded=4, `((` context excluded=4, `let`
excluded=11, array-subscript excluded=110, `${v:off:len}` excluded=2,
`[[ -eq ]]` excluded=0, `printf '%d'` excluded=5, `for ((` excluded=0 — **every
one matches its reconcile line's declared `excluded occurrences` field, and
the raw/inventoried/excluded totals close with zero overlap and zero gap
against my own independent counts**: 409 raw = 273 inventoried + 136
excluded, at both the whole-document level and, more strongly, at every one
of the 8 per-context rows individually.

**One discrepancy found, in board prose only, not in the audit document or
any `check:`-verified content**: the board's engineer hand-off text states
"405 raw occurrences reconciled to 273 inventoried sites ... + 136
excluded" — but 273+136=409, not 405, and my independent recount (409)
matches the audit document's own per-context ledger arithmetic exactly. No
`check:` line asserts a raw total anywhere, and the audit document's own
Summary section never states a raw total either (only 273 inventoried / 136
excluded), so this is a transcription slip in the board's summary prose, not
a defect in the graded deliverable. Flagged for the record; not a FAIL
driver.

## 2. AC4's disclosed structural blind spot — compensating control confirmed

The engineer's hand-off discloses that AC4's `check:` reads `raw` /
`inventoried sites` / `excluded occurrences` only from each `- reconcile:`
line's own text — never from a live `grep -c` of the `- site:` lines for
that context — so deleting one `- site:` row does not, on its own, turn AC4
red. This is correct and is a real, disclosed gap in the frozen AC's own
reach (the intent is frozen; this is not something this round can widen).

The compensating control is exactly the cross-check performed in section 1
above: I independently recomputed, per context, the live count of `- site:`
rows carrying that context label and confirmed it equals the reconcile
line's declared `inventoried sites` value, **row-for-row, for all 8
contexts, with no mismatch**. The live recount matches the ledger
row-for-row. This closes the gap AC4 cannot reach, for this verification
pass.

## 3. Producer-run mutation self-checks — independently reproduced (4 of 9)

Per this repo's partial-verification disclosure practice: reproduced **4 of
the engineer's 9** mutation self-checks, on a full scratch copy under
`$TMPDIR` outside the checkout (`cp -R bin`, `cp -R tests/{goal-state,
loop-guard,rollup-runs}`), each red → restore → green, independently
verified (not taken on the engineer's word):

1. **Revert `10#` on `it` only in `bin/goal-state.sh`'s `bump`** (leaving
   `start`'s `10#` intact): `bump` on `iteration=010` returned `9` (exit 0,
   no stderr) instead of `11` — the historical bug, reproduced exactly.
   `tests/goal-state/run.sh` went red on `T-1021-goal-state-bump` specifically.
   Restored: suite green again (14 PASS, 0 FAIL).
2. **Revert `10#` on `MAX_ITER` in `bin/loop-guard.sh`**: `max_iterations:
   08` / `--iteration 999` yielded `CONTINUE` at exit 0 with the raw bash
   `value too great for base` text on stderr — the measured fail-open
   (runaway guard silently disabled), reproduced exactly. `max_iterations:
   010` / `--iteration 9` also went red (shrunk cap to octal-8, wrongly
   `STOP:max_iterations_reached`) — both fixtures caught it, not just one.
   `tests/loop-guard/run.sh` went red (`T-1021-loop-guard-contract`).
   Restored: suite green again (30 PASS, 0 FAIL).
3. **Replace `EPOCH_RE`'s `{1,11}` with `{1,9}`** (this round's required
   mutation): both the 10-digit (`start_epoch=1767225600`) and 11-digit
   (`start_epoch=10000000000`) epoch probes went red — `{1,9}` rejects a
   10-digit value outright (`corrupt state (start_epoch is not an integer:
   1767225600)`), not merely the 11-digit one. This independently confirms
   the engineer's own measurement in item 4(b) below: the spec's
   Notes-for-engineer bullet predicted "the 10-digit probe stays green"; the
   live measurement (mine, independently, and the engineer's) says
   otherwise — both go red. Restored: both probes resolve to `10` again.
4. **Change the `bin/goal-state.sh` `bump` lock fixture from `010` to `07`**,
   *while the `it` `10#` bug from mutation 1 is still reverted*: the
   `07`-based lock case itself still **passed** (`8` either base, proves
   nothing) even with the bug present — confirming D7's reasoning for
   forbidding `07` as a fixture. The suite as a whole still went red, but
   only because of the separate `08`-fixture lock case, not because of `07`.
   Restored both files: suite green again (14 PASS, 0 FAIL).

Not independently reproduced (5 of 9): the two class-field mutations (AC5's
sum arm / `grep -c -v` arm), the two AC6 line-number mutations, the markdown-
table rewrite, the `lock:`-field-to-uncovered-suite mutation, and the
`source` line / file-deletion mutations against AC20. These are all
straightforward `check:`-line mechanics against the audit document's own
text (grep/sed arithmetic already exercised structurally via AC4/AC5/AC6/
AC13/AC20's live PASS above) rather than behavioural code paths, and are
lower-risk to leave unreproduced than the three code-behaviour mutations and
the fixture-vacuity mutation chosen above.

## 4. The two disclosed judgment calls — audited

**(a) `bin/check-intent.sh` left untouched (NO-BOUND).** Confirmed the audit
document carries this as a reasoned `- overflow:` row, not an omission:

```
- overflow: bin/check-intent.sh:539 — decision: NO-BOUND — reason: hash_version is a spec-ratification version counter read from the board, already treated as fully-trusted operator-authored content by this repository's security model; realistic values are 1-2 digits, and this task's scope-lock keeps bin/check-intent.sh untouched unless the width decision required a fix here (it does not — Notes for engineer)
- overflow: bin/check-intent.sh:616 — decision: NO-BOUND — reason: attest_version/ran/total/p/f are freeze-attestation counts read from the board, same board-trust reasoning as hash_version; the P+F==ran and ran==total cross-checks these values feed are internal consistency checks, not a security boundary against adversarial digit crafting
```

I agree this is a scope decision, not an omission: `bin/check-intent.sh`'s
own trust boundary for board/spec content is a pre-existing, repo-wide
assumption (this repo's `CLAUDE.md` states the same trust boundary for
config files an operator authors), the scope-lock's own allow-list framed
this file as "Permitted... only if NEEDS-WIDTH-BOUND" (explicitly
anticipating it might not apply), and D4 explicitly licenses a stated
decline where "an overflow cannot change any comparison's answer" — these
values feed internal consistency checks (`P+F==ran`), not a security gate.
`git diff --name-only a865ec0` confirms `bin/check-intent.sh` is untouched.

**(b) The `EPOCH_RE {1,11}→{1,9}` mutation prediction.** The spec's
Notes-for-engineer predicted the 10-digit probe would stay green under this
mutation; the engineer measured both probes go red and called it a prose
slip. Confirmed by my own independent measurement in section 3, mutation 3
above: both the 10-digit and 11-digit probes go red under `{1,9}}`, for the
straightforward reason that `{1,9}` cannot match a 10-digit string at all —
this is exactly what AC10's own `check:` text (not the Notes prose) already
implies a naive `{1,9}` bound would do to *both* probes. This contradicts
nothing in the frozen region: the Notes-for-engineer section sits outside
the intent block (`<!-- BEGIN/END intent-block: T-1021 -->` brackets only
Goal/Non-goals/AC/Input space), and the spec's own Assumptions section
explicitly licenses exactly this kind of prose correction.

## 5. Behavioural probes beyond the AC fixtures (boundary probing discipline)

Three boundary shapes not literally covered by the AC12/AC7/AC9 fixtures,
each on a scratch copy of the real state file / contract / JSONL row:

- **`iteration=00` in a `bin/goal-state.sh` state file**: `bump` → `1` at
  exit 0 (0 decimal + 1 = 1; not silently re-based, not refused — a defined,
  correct outcome).
- **`max_wallclock_min: 010` in a `bin/loop-guard.sh` contract, with
  `--elapsed-min 9` then `--elapsed-min 10`**: `010` correctly resolves to
  decimal 10 (not octal-invalid 8): `--elapsed-min 9` → `CONTINUE` (exit 0),
  `--elapsed-min 10` → `STOP:budget_exhausted` (exit 3) — the cap was not
  silently shrunk to 8.
- **A JSONL row with `"duration_ms":010`** fed to `bin/rollup-runs.sh`:
  output shows `duration: 10ms` (exit 0, no stderr) — decimal, not the
  octal-derived 8.

All three land on a defined, correct base-10 outcome; none silently
re-based, none leaked raw bash arithmetic text on stderr.

## 6. Full CI-wired suite + dogfood list

Ran every `shellcheck` invocation, every fixture suite, and every dogfood
step from `.github/workflows/check-handoff.yml`, in the exact order the
workflow lists them, from a fresh shell (split across three batches due to
total runtime, matching this repo's test-recipe note about long-running
suites — not a partial run):

- Batch 1 (shellcheck through `tests/check-prompt-sync`): all PASS.
- Batch 2 (dogfood check-prompt-sync through `tests/codex-skeleton-hygiene`,
  including the `gen-playbook-blocks` scratch-regeneration dogfood step):
  all PASS.
- Batch 3 (`tests/check-pii-shapes`, `check-pii-shapes --base origin/develop`,
  `tests/check-commit-identity`, `tests/gitignore-raw-dumps`,
  `check-commit-identity --base origin/develop`): all PASS.

`shellcheck --version` confirmed 0.11.0 (the pinned version) before trusting
the shellcheck-clean claim. `origin/develop` confirmed == `a865ec0`.

**Result: 0 failures across all ~56 steps.** This independently confirms
the engineer's own "0 failures" claim rather than accepting it as
self-reported.

## Diff scope

`git diff --name-only a865ec0` →
`.shell-team/interventions/T-1021.md`, `.shell-team/provenance/T-1021.md`,
`.shell-team/specs/T-1021-arith-base10-audit.md`, `.shell-team/todo.md`,
`bin/goal-state.sh`, `bin/loop-guard.sh`, `bin/rollup-runs.sh`,
`docs/loop-engineering/arith-base10-audit.md`, `tests/goal-state/run.sh`,
`tests/loop-guard/run.sh`, `tests/rollup-runs/run.sh` — every path inside the
allow-list; `bin/check-intent.sh`, `agents/**`, `skills/**`, `.github/**`,
`templates/**`, both READMEs, both CHANGELOGs and `.shell-team/lessons.md`
show no diff. Read the actual diffs in the three fixed `bin/` scripts
directly (not just AC15's shellcheck-clean signal): each fix normalizes
once, immediately after the existing width-bound regex, on a dedicated
reassignment line, matching the `bin/check-intent.sh:614-623` model the
spec's Notes-for-engineer names — no unrelated refactors, no scope creep.

## Adversarial fixture synthesis checklist

This task's deliverable includes three modified executable `bin/*.sh`
scripts with a real runtime input surface, so the checklist applies.

- **Class 1 (beyond happy-path)**: payload-shape sub-axis covered by AC7–
  AC12's own fixtures (empty, `08`, `010`, 20-digit overflow) plus my own
  section-5 probes (`00`, `010` on a second quantity, JSONL `010`).
  Filesystem/environment-state and resource/budget-competition sub-axes are
  structurally not reachable by this fix's own input surface (a leading-zero
  digit string, not a path or a budget count). Time/liveness (a hanging
  command) is structurally not applicable — none of the three fixes touch a
  subprocess-timeout code path; recorded as structurally-not-applicable
  rather than skipped silently.
- **Class 2 (regex/charset anchoring)**: exercised directly — `ITER_RE`/
  `EPOCH_RE`/loop-guard's `{1,9}` bounds against `00`, `08`, `010`, empty,
  and 20-/22-digit overflow strings, each asserting the specific outcome
  (correct decimal value vs. the script's own namespaced refusal), not just
  "some rejection happened."
- **Class 3 (Markdown/CommonMark)**: not applicable — the audit document is
  ledger lines read by `grep`/`sed`, not a fenced-block/heading parser this
  task creates or modifies.
- **Class 4 (test-harness short-circuit)**: satisfied — ran the full
  CI-wired list myself, from a fresh shell, exactly as documented (bare
  `bash tests/<suite>/run.sh` invocations, real dogfood commands against the
  real repo state), not a stubbed subset.

## Edge cases tried

- `iteration=00`, `max_wallclock_min: 010` with both a passing and a
  failing `--elapsed-min`, and JSONL `duration_ms:010` (section 5).
- The `07`-fixture vacuity mutation (section 3, item 4) — a real-data-
  reachable class-9 boundary shape per the spec's own Input-space table, not
  a synthetic extreme.
- Confirmed `git status --short` shows nothing beyond my own review-file
  addition throughout verification — no residue from the scratch-copy
  mutation runs (all writes were under `$TMPDIR`, outside the checkout).

## Risk notes for reviewer

- The board hand-off's "405 raw occurrences" figure (section 1) is an
  arithmetic slip in board prose only (273+136=409, matching both my
  independent recount and the audit document's own per-context ledger) —
  not a defect in any graded artifact. Worth a one-line correction on a
  future board touch, not a blocker.
- AC4's structural blind spot (a deleted `- site:` row does not by itself
  turn AC4 red) is a disclosed, frozen-AC limitation, not a live gap in
  this pass — the compensating control (independent per-context row count
  vs. each reconcile line) is in section 2 above and closed with zero
  mismatch.
- 5 of the engineer's 9 mutation self-checks were not independently
  reproduced (see section 3) — all five are straightforward ledger-text
  `check:`-line mechanics already exercised structurally via the live
  AC4/AC5/AC6/AC13/AC20 PASSes above, judged lower-risk to leave
  unreproduced than the four chosen (three behavioural code-path mutations
  plus the fixture-vacuity check).

## Codex Reviewer round 1 (2026-08-03)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1021
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 12 files, +1305/-8 lines (`git diff --stat a865ec0...HEAD`)

Two synchronous foreground `codex exec --sandbox read-only --cd .` passes, both
reading the real files directly (never a pasted diff or excerpt): primary
via `review --base a865ec0 --json`, adversarial via a free-form prompt
targeting the primary pass's sole Major/Blocker-mapped finding (the trigger
condition for the opt-in adversarial pass). Both captured through
`codex-capture.sh --alloc`/`--publish` to the canonical
`.shell-team/reviews/T-1021-codex-primary.{txt,jsonl}` and
`.shell-team/reviews/T-1021-codex-adversarial.{txt,jsonl}` (gitignored raw
captures; both publishes exit 0).

Independently re-verified, myself, executing real commands against this
checkout (not just reading the audit document's prose):

- All eight of the audit document's pinned search commands, live: `140/12/0/
  11/162/2/65/17` — matches the document's `- searched:` lines exactly and
  matches QA's own independent recount. Population `find bin -maxdepth 1
  -type f | wc -l` → 35, matches all 35 `- file:` lines byte-for-byte.
  Per-context `- site:` row counts (`8/65/136/52/12` across the five
  contexts that have any) match every `- reconcile:` line's `inventoried
  sites` field exactly, and the four class totals (234/29/10/0) sum to 273,
  matching the per-file `sites:` sum.
- All 8 owning/parallel-surface suites green (`goal-state`, `loop-guard`,
  `rollup-runs`, `rollup-track`, `cluster-failures`, `is-span-row-parity`,
  `check-intent`, `errexit-safe`); `tests/errexit-safe/run.sh` byte-identical
  to `a865ec0`. shellcheck 0.11.0 clean on the six touched files. Full
  `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1021-arith-base10-audit.md`
  → 24/24 PASS, reproducing QA's result independently.
- Read the actual diffs in `bin/goal-state.sh`, `bin/loop-guard.sh`,
  `bin/rollup-runs.sh` line-by-line against the audit document's `- site:`
  rows for those three files: every `10#` normalization lands exactly where
  the document's `lock:`/`n10:` fields say it does, D5's non-empty-implies-
  digit reasoning holds for `rollup-runs.sh`'s guard, and D6's fail-closed
  invariant holds under direct probing (huge 20-digit `--iteration`/
  `iteration=`/empty operands all refuse with the script's own namespaced
  message, never bash's raw `value too great for base`/`syntax error` text).
- Board structural diff: clean insertion under `## Active`, zero heading
  lines touched (`git diff ... | grep -E '^[+-]#'` — empty).

### Blockers

None.

### Major

- **`docs/loop-engineering/arith-base10-audit.md:191,197,199,201,203,205,207,209,`**
  **`210,443,444` (the `bin/check-intent.sh` freeze-attestation/hash-version**
  **counters)** — Codex's primary pass flagged this as P1 (`hash_version`,
  `av`/`ar`/`at`/`ap`/`af`, parsed by `HASH_FULL_RE`/`ATTEST_FULL_RE`, both
  unbounded `[0-9]+` grammars) and I independently reproduced the mechanism:
  `bash -c 'echo $((10#18446744073709551626))'` → `10`, a silent 64-bit
  wraparound, not an arithmetic error. This is a real violation of D2's own
  ladder step 3 ("does its grammar admit a digit string wide enough to cross
  bash's signed 64-bit range and change a comparison's answer? Yes ⇒
  `NEEDS-WIDTH-BOUND`") — the audit document classifies these sites
  `NORMALIZED`/`width: none` instead. The provenance record's grounding for
  the decline (`.shell-team/provenance/T-1021.md` decision 1 — board/spec
  content is "already treated as fully-trusted operator-authored content by
  this repository's security model") is a materiality reading of D4's
  declination clause; the adversarial pass (reasoning independently, having
  read `bin/check-intent.sh:380-470,605-650`, the regexes, and the
  provenance record) confirms the mechanism is real, confirms no other
  consumer reads the raw digit text (only `check-intent.sh`'s own exit
  classification and human PR review touch these fields), and — this is
  the load-bearing conclusion — finds the overflow **does not** hand a
  malicious board author a materially stronger bypass than writing small,
  plausible, dishonest numbers directly (the fields are 100% author-
  controlled free text either way), but **does** violate the checker's own
  stated fail-closed contract for arithmetically-inconsistent records, is
  cheap to close (`{1,9}` on the four count captures plus `hash_version`,
  matching `bin/loop-guard.sh`'s own convention, with no capture-index
  shift), and is squarely inside the frozen spec's own anticipated scope —
  the Notes for engineer's scope-lock explicitly permits `bin/check-intent.sh`
  and `tests/check-intent/run.sh` "only if the inventory's width decision
  lands on `NEEDS-WIDTH-BOUND` there." Given the ladder's literal reading
  disagrees with the decline and the fix is cheap and in-scope-if-triggered,
  this is a required correction, not a live critical exploit — hence Major,
  not Blocker. Two acceptable remediations: (a) bound `HASH_FULL_RE`'s
  version capture and `ATTEST_FULL_RE`'s four count captures to `{1,9}}`
  (`[1-9][0-9]{0,8}` for `hash_version`/`av`, which already forbid a leading
  zero), reclassify the affected `- site:`/`- overflow:` rows to
  `NEEDS-WIDTH-BOUND`/`BOUND` with headroom + stale-at, and add one
  regression case to `tests/check-intent/run.sh` proving an overflow record
  is refused rather than silently re-based; or (b) keep the decline, but
  replace the provenance's trust-boundary grounding (which slightly
  overstates what `CLAUDE.md` actually says — see Minor below) with an
  argument that engages D2's literal ladder text directly, since "no
  materially new bypass" is a defensible position but is not the argument
  currently on record.

### Minor / nits

- **`.shell-team/provenance/T-1021.md`, decision 1** — the adversarial pass
  flags that the grounding sentence slightly overstates its citation:
  `CLAUDE.md` declares itself (`CLAUDE.md`) a trusted instruction channel;
  it does not make the identical trust claim about board/spec *content*.
  `bin/check-intent.sh`'s own comment block frames board attestations as
  executor claims whose residual protection is committed-artifact history
  and PR review, not blanket trust. Doesn't change the outcome either way
  (see Major above), but the citation should be tightened if decision 1 is
  kept.
- **`docs/loop-engineering/arith-base10-audit.md:462` (Summary section)** —
  found independently (not from Codex): `bin/goal-state.sh (6 sites, 2
  width-bound)` is wrong on both numbers. `grep -c` against the document's
  own `- site:` rows for `bin/goal-state.sh` shows 4 `NEEDS-10#` rows, all
  4 `width: bound` (none `width: none`) — the correct text is "4 sites, 4
  width-bound." The paragraph's own arithmetic is internally inconsistent
  with itself: 6+4+2=12 contradicts the same paragraph's stated total of
  **10** `NEEDS-10#` sites, while the correct 4+4+2=10 matches it. Prose-
  only — the Summary section is not machine-parsed by any `check:` line
  (D9's ledger-line grammar covers `- site:`/`- reconcile:`/etc., not this
  bullet), so no AC catches it, but it is a factual error in the audit
  document's own accounting of itself and should be corrected.
- **Board hand-off prose, "405 raw occurrences"** — already disclosed and
  correctly diagnosed by QA (`.shell-team/reviews/T-1021.md`, QA section
  §1): the correct figure is 409 (273+136), confirmed again independently
  here via the same live per-context recount. Board prose only, not a
  graded artifact; agree with QA's classification as a transcription slip,
  not a finding against the code.

### Adversarial findings

Targeted re-run against the sole Major above (`bin/check-intent.sh`'s
attestation/hash-version counters), reading the real files itself
(`bin/check-intent.sh:380-470,605-650`, the two regexes,
`.shell-team/provenance/T-1021.md`, the audit document): confirmed the
overflow mechanism is real and current text is inconsistent with D2's
literal ladder; confirmed no downstream machine consumer other than
`check-intent.sh`'s own classification reads the raw digit text (one
exception checked and found non-issue: oversized `av` is rejected by the
existing `av`-in-range check before it can wrap into an "aligned" result);
concluded the human-review trust boundary is not materially weakened (an
attacker with full control of the line gains nothing an honest-looking
small lie didn't already give them) but the fix is cheap, matches an
existing repo convention, and is squarely inside the frozen spec's own
"permitted... only if" scope-lock — net verdict "width bound: yes, cheap
and warranted" even while agreeing "no materially new bypass: correct."
This is the reasoning the Major finding above is built on; nothing in the
adversarial pass surfaced a second independent issue.

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `bin/check-intent.sh`'s freeze-attestation/hash-version counters classified `NORMALIZED`/`width: none` despite an unbounded `[0-9]+` grammar that can overflow 64-bit and change the `P+F==ran`/`ran==total` comparisons' raw answer | primary: P1 (→ blocker per normalization map); adversarial: major | major | Independent reproduction (`$((10#18446744073709551626))` → `10`) and the adversarial pass together confirm the overflow is real and disagrees with D2's literal ladder text (a required correction), but also confirm it grants no materially new bypass of the human-review trust boundary beyond directly writing dishonest small numbers — the affected file is outside this task's required Goal-scope (three named fail-opens), the fix is cheap and squarely anticipated by the spec's own conditional scope-lock, so the finding is downgraded from the raw P1/blocker mapping to major rather than treated as a live critical exploit. |

### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1021 entry back to
  `READY_FOR_ENG` with this section linked. The one required change is
  bounded and cheap: resolve the Major above, either by bounding
  `HASH_FULL_RE`/`ATTEST_FULL_RE`'s numeric captures and reclassifying the
  affected `- site:`/`- overflow:` rows, or by replacing decision 1's
  grounding with an argument that engages D2's ladder text directly rather
  than the `CLAUDE.md` citation flagged above. The two Minors (Summary
  section arithmetic, provenance citation precision) are not merge-blocking
  on their own; fix opportunistically in the same round since the file is
  already open, or file as fast-follows per the disposition below.

### Fast-follow disposition

- `docs/loop-engineering/arith-base10-audit.md:462`'s Summary-section
  arithmetic slip (`bin/goal-state.sh (6 sites, 2 width-bound)` should read
  "4 sites, 4 width-bound") — **file-an-issue** if not fixed in this rework
  round: it is a factual error in the audit document's own self-description
  with no `check:` line guarding it, so a future reader has no mechanical
  signal that it is wrong.
- Board hand-off's "405 raw occurrences" transcription slip — **won't-fix**
  as a separate action: QA already disclosed this in the same round this
  board entry was written, board prose is not amended retroactively per
  this repo's own board-format norms, and the corpus-facing correct figure
  (409) is recorded in both the audit document's own ledger arithmetic and
  this section.
- `.shell-team/provenance/T-1021.md` decision 1's `CLAUDE.md` citation
  precision (Minor, above) — **won't-fix** as a standalone action: it rides
  on whichever remediation path is chosen for the Major finding above (if
  decision 1 is superseded by an actual width bound, the citation question
  is moot; if decision 1 is kept, tightening the citation is part of that
  same edit, not a separate deferral).

---

## QA round 2 (2026-08-03) — re-verification of rework round 1 against Codex round-1 findings

- Verdict: PASS
- Delta reviewed: `dd9b183` (Codex round 1 REQUEST_CHANGES) → `4962c4d` (rework round 1) → `a7c39a2` (interventions human-stop record, content-neutral). Board: T-1021 `READY_FOR_QA` → `READY_FOR_REVIEW`.
- `git diff --stat dd9b183..a7c39a2`: 6 files, +241/-48 — `.shell-team/interventions/T-1021.md`, `.shell-team/provenance/T-1021.md`, `.shell-team/todo.md`, `bin/check-intent.sh`, `docs/loop-engineering/arith-base10-audit.md`, `tests/check-intent/run.sh`. Nothing outside this list touched.

### 1. The Major is closed — reproduced independently, not taken from the hand-off

Read the actual `bin/check-intent.sh` diff directly: `HASH_FULL_RE`'s version capture is now `([0-9]{1,4})` (was `([0-9]+)`), and `ATTEST_FULL_RE`'s version + four count captures are now `([1-9][0-9]{0,3})` / `([0-9]{1,4})` ×4 (was `([1-9][0-9]*)` / `([0-9]+)` ×4).

**Reproduced the reviewer's original wrap probe against the fixed checker myself**, on a scratch fixture built by hand (not the suite's own harness) outside the checkout:

```
# huge grammar-conformant intent-hash version
$ bash bin/check-intent.sh spec.md board.md   # board carries "- intent-hash (v99999999999999999999): <40-hex>"
exit=2
stderr: check-intent: structural: expected exactly one well-formed intent-hash
record for T-900 in board.md (found valid=0 malformed=1; expected
'- intent-hash (vN): <40-hex>')

# huge grammar-conformant freeze-attestation version
$ bash bin/check-intent.sh spec.md board2.md  # board carries a "freeze-attestation (v99999999999999999999, ...)" line
exit=2
stderr: check-intent: attestation: 1 malformed freeze-attestation record(s)
for T-900 in board2.md (expected '- freeze-attestation (vN, YYYY-MM-DD):
lines=<ran>/<total> sweep=mutual-satisfiability verdict=<P>P/<F>F
owner=<value>')
```

Both refuse structurally/as attestation, with the checker's own message, no raw bash arithmetic text. **Confirmed the fail-open reproduces on the pre-fix grammar**: on a scratch copy with `HASH_FULL_RE`/`ATTEST_FULL_RE` reverted to unbounded, the identical hash-version probe now returns exit 1 with `check-intent: drift-detected: version-chain count mismatch for T-900: board declares v99999999999999999999 but board.md carries 0 intent-ratified record(s) (expected 7766279631452241918)` — the classic 64-bit wrap value, matching the Codex Major's own repro (`$((10#18446744073709551626))` → `10`) in kind. Restored: exit 2/structural again.

**The 7 reclassified `- site:` rows and corrected `- overflow:` rows agree with the shipped grammar** — read each row against the actual code:

| site | line | code | row |
|---|---|---|---|
| `hash_version` | `:564` | `declared_n=$((10#$hash_version))` | `NEEDS-WIDTH-BOUND`, lock `T-1021-check-intent-hash-version-overflow` |
| `av` | `:641` | `av=$((10#${attest_version[$idx]}))` | `NEEDS-WIDTH-BOUND`, lock `T-1021-check-intent-attest-version-overflow` |
| `ar` | `:645` | `ar=$((10#${attest_ran[$idx]}))` | `NEEDS-WIDTH-BOUND`, lock `T-1021-check-intent-attest-count-ran-overflow` |
| `at` | `:646` | `at=$((10#${attest_total[$idx]}))` | `NEEDS-WIDTH-BOUND`, lock `-total-overflow` |
| `ap` | `:647` | `ap=$((10#${attest_p[$idx]}))` | `NEEDS-WIDTH-BOUND`, lock `-p-overflow` |
| `af` | `:648` | `af=$((10#${attest_f[$idx]}))` | `NEEDS-WIDTH-BOUND`, lock `-f-overflow` |
| `hash_version` (2nd use) | `:678` | `version_int=$((10#$hash_version))` | `NEEDS-WIDTH-BOUND`, lock (same hash-version test) |

All 7 lines read exactly as the audit document describes, all non-blank, all resolve at HEAD (`sed -n '<line>p'` on each confirmed non-empty; `bash bin/check-acs.sh` AC6 also PASSes live). The two downstream derivations `bin/check-intent.sh:654`'s `$((ap + af))` and `:682`'s `$((version_int - 1))` correctly stay `NORMALIZED`/`width: none` — each consumes only already-`{1,4}`-bounded operands.

**Independently re-derived the reconciliation arithmetic for the `$(( ))` context** (the only context this round's diff touches): raw recount `awk '...' bin/*` → **143** (was 140, +3 — matches the document's own claimed 3 new comment-only mentions from the rework's own added comment blocks); `- site:` rows carrying `$(( ))` → **136** (unchanged — reclassification, not addition/removal); excluded → **7** (was 4, +3, all three new ones are comment-only, matching the document's updated `- excluded:` line for `bin/check-intent.sh`). `143 = 136 + 7`. All other 7 contexts' raw/inventoried/excluded numbers are byte-identical to round 1 (confirmed unchanged by re-running all 8 pinned commands, see section 5 below).

**Classification totals re-derived**: `NOT-REACHABLE` 234 (unchanged), `NORMALIZED` 22 (was 29, −7), `NEEDS-10#` 10 (unchanged), `NEEDS-WIDTH-BOUND` 7 (was 0, +7). 234+22+10+7 = 273 (total site-row count unchanged — a pure reclassification, confirmed against the Summary section's own restated numbers, which match my independent count exactly).

### 2. The width choice — `{1,4}`, not a copy-pasted `{1,9}` — grounded and judged sound

Read provenance decisions 2 and 3 (the two new triples this round adds) and the two corrected `- overflow:` rows in the audit document. The stated reasoning: `hash_version`/`av` count spec re-freeze/attestation events (this repository's own history stays in the low single digits even for its most-reworked specs), and `ar`/`at`/`ap`/`af` count `- check:` lines inside one spec's intent block. Independently confirmed the cited ceiling: `grep -c '^\s*- check:' .shell-team/specs/T-1021-arith-base10-audit.md` → **24**, matching the provenance's own "this task's own spec, the largest measured, carries 24" claim exactly (not taken on its word). `9999 / 24 ≈ 416×` headroom, and `9999` versus a low-single-digit real ceiling is on the order of `1000×` — both comfortably inside the provenance's stated "100–1000×" range. The provenance explicitly names and rejects the copy-paste alternative (`{1,9}`, `bin/loop-guard.sh`'s convention) as the option the coordinator's own rework instruction ruled out ("pick from the quantity's real ceiling, don't copy-paste"). I agree with the reasoning: `{1,4}` is materially tighter than `{1,9}` while still carrying multiple orders of magnitude of headroom over every measured real value, and the stale-at clause (a spec's intent block reaching 10,000 acceptance criteria, or a single spec being re-frozen 10,000 times) is the same order-of-magnitude framing D4 already establishes for `bin/goal-state.sh`'s bounds in round 1.

### 3. The two Minors — closed

- **Provenance decision 1 reworded/superseded, history retained.** Read the diff directly: the original decision's text is now prefixed `[superseded this round, retained as the historical record]`, its `reason:` field points forward to the corrected decision, and its `grounding:` field now cites the actual Codex Minor finding that flagged the overstated `CLAUDE.md` citation — rather than being deleted or silently rewritten. `bash bin/check-provenance.sh .shell-team/provenance/T-1021.md` → conformant (6 decision entries, 0 sentinel) confirms the structural shape survives the edit.
- **Audit-document Summary arithmetic now reconciles.** The Summary's `bin/goal-state.sh` line now reads "**4** sites, **4** width-bound" (was "6 sites, 2 width-bound", which the Codex Minor caught as internally contradictory against the paragraph's own stated total of 10). Independently counted: `grep '^- site: ' docs/loop-engineering/arith-base10-audit.md | grep -c 'bin/goal-state.sh'` → **4**, and all 4 carry `NEEDS-10#`/width-bound (confirmed by reading the 4 rows: `:88` ×2, `:106` ×2 — matching the `elapsed-min`/`bump` fix sites from round 1). 4+4+2=10, consistent with the paragraph's stated total. Fixed.
- **Board prose "405"→"409" landed.** Read the engineer hand-off sub-bullet directly: it now states "409 raw occurrences (corrected here from an earlier "405" transcription slip in this same sub-bullet — QA's note below already flagged it; 273+136=409)" — the correction is explicit and attributed, not silently rewritten. Confirmed this reads correctly against my own round-1 finding.

### 4. The engineer's two self-found test-gap fixes — both independently reproduced

Read `tests/check-intent/run.sh`'s new comment block (lines above the 8 new cases) describing both self-found gaps, then reproduced each on a fresh scratch copy under `$TMPDIR` (mutating `ATTEST_FULL_RE` to leave only the `ran` capture unbounded, siblings left at `{1,4}`):

- **Gap 1 (masking): the combined "all-4-huge" test cannot prove every capture is independently bounded.** With only `ran`'s quantifier reverted, `T-1021-check-intent-attest-counts-overflow` (which oversizes all four fields at once) still **PASSED** — because the other three fields, still bounded, fail to match the grammar regardless of `ran`, so the whole line still fails to match for a reason unrelated to `ran`'s own bound. Independently reproduces the engineer's own described gap exactly.
- **Gap 2 (message specificity): a weaker `grep -q 'attestation:'` assertion would pass for the wrong reason.** Under the same mutation, the per-field `T-1021-check-intent-attest-count-ran-overflow` case correctly went **red**, and the actual failure message was `check-intent: attestation: freeze-attestation v1 ... has lines=7766279631452241919/1 (expected ran == total)` — the *downstream* `ran == total` cross-check catching the silently-`10#`-wrapped value, not the grammar-side `malformed freeze-attestation record` refusal. Confirmed directly: `echo "$msg" | grep -q 'attestation:'` → true (a weak check would have passed/masked this); `echo "$msg" | grep -qF 'malformed freeze-attestation record'` → false (the strengthened, specific-message assertion the engineer actually shipped correctly fails here, which is why the suite goes red for the right reason). Restored the mutation: suite green again (50 PASS, 0 FAIL) both times.

**Lock-case CI membership**: `tests/check-intent/run.sh` is already wired into `.github/workflows/check-handoff.yml` (line 189, unconditionally) and was confirmed green in the full CI-wired run below (section 5), so every one of the new cases inside it runs in CI automatically — no new workflow step was needed. `bash bin/check-acs.sh`'s AC13 (lock-field-names-a-CI-wired-suite) also PASSes live against the 7 `NEEDS-WIDTH-BOUND` rows' `lock:` fields.

**Discrepancy found, count only (not a functional gap)**: the coordinator's message and the engineer's own hand-off both say "9 new lock cases." I independently counted the actual diff twice (`grep -c '^pass "T-1021-'` against `tests/check-intent/run.sh`, and a distinct-id `grep -oE` sweep of the whole diff): there are exactly **8** — `hash-version-overflow`, `attest-version-overflow`, `attest-counts-overflow` (combined), `attest-count-{ran,total,p,f}-overflow` (4), `attest-counts-bounded-pass`. All 8 exist, are correctly wired, and are individually verified above (or via the full suite run in section 5); this is a miscount in the hand-off narrative that the coordinator's message inherited, not a missing test.

### 5. Standard re-verification

- `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1021-arith-base10-audit.md` → **24/24 PASS, 0 FAIL, 0 SKIP**.
- `bash bin/check-intent.sh .shell-team/specs/T-1021-arith-base10-audit.md .shell-team/todo.md` → `aligned: T-1021 v1 (040a10fc9ae2cca351561baa033758410eceb4d3) matches ...` — **hash unchanged from round 1**, confirming the rework never touched the frozen intent block.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1021.md` → conformant (6 decisions, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1021 .shell-team/interventions/T-1021.md` → conformant (1 entry, 0 sentinel).
- **Independently re-ran all 8 pinned search commands against the current tree** (not transcribed): 143/12/0/11/162/2/65/17 — all match the document's declared raw counts exactly. Per-file `sites:` sum (273) and population (35 files) both still match byte-for-byte. Per-context `- site:` row counts match every `- reconcile:` line's declared `inventoried sites` for all 8 contexts, with no mismatch.
- **Full 56-step CI-wired list** from `.github/workflows/check-handoff.yml`, in workflow order, from a fresh shell, run once (split across three batches for total runtime): **0 failures**, including `tests/check-intent` (all 8 new lock cases + all pre-existing cases) and `tests/errexit-safe`.
- `tests/errexit-safe/run.sh`: `git diff --quiet a865ec0 -- tests/errexit-safe/run.sh` → byte-identical; `bash tests/errexit-safe/run.sh` → green. `bin/check-intent.sh` is not in the `NOT_APPLY` registry's pinned file set (confirmed by reading the registry directly), so editing it, per D11, moves nothing that suite tracks.
- `shellcheck` (0.11.0) on the diff-derived file list (`git diff --name-only a865ec0 -- bin tests`, 8 files including `bin/check-intent.sh`/`tests/check-intent/run.sh`) → clean.
- `git diff --name-only a865ec0` → 14 files, all inside the allow-list (`bin/check-intent.sh` and `tests/check-intent/run.sh` are the "permitted... only if NEEDS-WIDTH-BOUND" files the scope-lock anticipated, now legitimately triggered); `bash bin/check-pii-shapes.sh --base a865ec0` → clean.

### Edge cases tried (round 2)

- Both the hash-version and freeze-attestation-version overflow shapes, reproduced by hand (not via the suite's own harness) against both the fixed and a deliberately-reverted checker — red → restore → green each time.
- The per-field-vs-combined masking mutation and the message-specificity mutation, both independently reproduced with the exact same fixture shape the engineer describes finding.
- Cross-checked the provenance's cited real-ceiling number (24 `check:` lines) against this spec's own file directly, rather than trusting the provenance's restated figure.

### Risk notes for reviewer

- The "9 vs 8" lock-case count discrepancy (section 4) is a hand-off/coordinator-message miscount, not a missing or broken test — all 8 real cases are present, correct, and confirmed running in CI. Worth a one-line correction on a future board touch.
- The round-1 QA record's own two disclosed items (the 405→409 board-prose slip, AC4's structural blind spot) both remain correctly resolved/documented through this round — re-confirmed rather than assumed stale.
- No new adversarial-fixture-synthesis-checklist gap surfaced this round: the rework's own diff is exactly the same class of executable-artifact change as round 1 (a regex width bound), and the class-1/class-2 boundary probing this round performed (the wrap probe, the per-field mutation, the message-specificity mutation) is the direct continuation of round 1's practice against the newly-touched file.

---

## Codex Reviewer round 2 (2026-08-03)

### Codex Review verdict: APPROVE
- Task: T-1021
- Codex model: gpt-5-codex (codex-cli 0.145.0)
- Verification mode: executed-tests
- Diff scope: 7 files, +338/-48 lines (`git diff --stat dd9b183...b4e197d`) — the
  rework commit `4962c4d`, the content-neutral interventions commit `a7c39a2`,
  and QA round 2's own record `b4e197d`

One synchronous foreground `codex exec --sandbox read-only --cd . review
--base dd9b183 --json` pass, reading the real files itself (it ran its own
`git diff --unified=80 dd9b183 -- bin/check-intent.sh tests/check-intent/
run.sh .shell-team/todo.md docs/loop-engineering/arith-base10-audit.md`,
read `bin/check-intent.sh`'s full context, and syntax/shellcheck-verified
both touched files itself). Final message: "The patch bounds every affected
numeric capture before arithmetic evaluation, updates the audit
consistently, and adds targeted regression coverage for each field. No
blocking regressions or newly introduced functional bugs were identified."
No Blocker/Major surfaced by the primary pass, so the adversarial pass's
sole trigger did not fire this round — not run, per the opt-in design
(round 1's carryover adversarial capture is left untouched at its round-1
content; nothing this round supersedes it). Captured via
`codex-capture.sh --alloc`/`--publish` to the canonical
`.shell-team/reviews/T-1021-codex-primary.{txt,jsonl}` (publish exit 0) —
these are overwritten in place with this round's content, per this role's
"never vary the filename per round" rule; the round-1 capture is not
separately retained (the round-1 verdict text in this same file is the
durable record of round 1's findings).

### Round-1 findings — re-verified independently, not taken on the hand-off's word

1. **Major (D2-ladder contradiction on `bin/check-intent.sh`'s counters) —**
   **CLOSED.** Read the actual diff: `HASH_FULL_RE`'s version capture is now
   `([0-9]{1,4})` (was `([0-9]+)`); `ATTEST_FULL_RE`'s version and four count
   captures are now `([1-9][0-9]{0,3})` / `([0-9]{1,4})` ×4 (was
   `([1-9][0-9]*)` / `([0-9]+)` ×4). Built my own scratch spec/board fixture
   pair (not the suite's harness) and reproduced the exact historical wrap
   value end-to-end: a freeze-attestation `verdict=18446744073709551626P/0F`
   with `lines=10/10` now refuses — `check-intent: attestation: 1 malformed
   freeze-attestation record(s)...` — exit 2, no raw bash arithmetic text.
   All 7 reclassified `- site:` rows (`hash_version` ×2 at `:564`/`:678`,
   `av`/`ar`/`at`/`ap`/`af` at `:641`/`:645`-`:648`) read exactly against the
   live code at those line numbers; the 2 downstream derivations that stay
   `NORMALIZED` (`:654`'s `$((ap + af))`, `:682`'s `$((version_int - 1))`)
   correctly consume only already-bounded operands. Independently re-ran all
   8 pinned search commands (143/12/0/11/162/2/65/17 — all match); the
   reconciliation identity for the one context this diff touches
   (`$(( ))`: 143 raw = 136 sites + 7 excluded) holds, and the four class
   totals (234/22/10/7) sum to 273.
   **Boundary probes I added beyond QA's own fixtures**: a hash version at
   exactly the bound's own edge (`v9999`, 4 digits) passes the grammar
   cleanly and proceeds to the (unrelated) downstream version-chain check —
   never wrongly refused as malformed; a 5-digit version (`v10000`) at every
   individually-tested field (hash version, attest version, `ran` count) is
   refused as malformed at the grammar every time; `v0` and a blank version
   both refuse via the pre-existing, unrelated downstream checks
   (`version_int -ge 1`, the 40-hex/version grammar), unaffected by this
   round's change. No new fail-open shape found at any of these edges.
   **The engineer's own self-found test-gap (message-specificity) —
   independently reproduced by mutation**: reverted only the `ran` capture's
   `{1,4}` back to unbounded on a scratch copy. The combined
   "all-four-huge" lock case still passed (masked, as the hand-off
   describes). The per-field `ran`-overflow case correctly went red, but for
   the reason the hand-off names: the failure text was `check-intent:
   attestation: freeze-attestation v1 ... has lines=7766279631452241919/1
   (expected ran == total)` — the wrapped value, caught by the *downstream*
   `ran == total` cross-check, not by `attest_bad_count`'s grammar-side
   `malformed freeze-attestation record` message. A weaker `grep -q
   'attestation:'` assertion would have passed here for the wrong reason;
   the shipped, message-specific assertion correctly does not. Restored:
   green again.
2. **Minor 1 (`CLAUDE.md` trust-boundary over-read) — CLOSED.** Read
   `.shell-team/provenance/T-1021.md` directly: decision 1 is marked
   `[superseded this round, retained as the historical record]`, its
   `grounding:` field now cites the actual round-1 Minor that flagged the
   overstated citation, and a new decision (2) documents the reversal.
   History retained, not deleted or silently rewritten.
3. **Minor 2 (Summary-section arithmetic) — CLOSED.** The audit document's
   Summary now reads `bin/goal-state.sh (4 sites, 4 width-bound)`.
   Independently recounted: `grep -c` against the document's own `- site:`
   rows for `bin/goal-state.sh` → 4, all 4 carrying `NEEDS-10#`/a width
   bound. 4+4+2=10, consistent with the same paragraph's stated total.

### Blockers

None.

### Major

None.

### Minor / nits

- **`bin/check-intent.sh:317`, `.shell-team/provenance/T-1021.md` (decision**
  **2/3), `docs/loop-engineering/arith-base10-audit.md` (4 `- site:` rows**
  **at `:645`/`:646`/`:647`/`:648`) — found independently, not from Codex's**
  **primary pass.** The `{1,4}` width choice's grounding text states "this
  task's own spec, the largest measured, carries 24" `- check:` lines. This
  superlative is factually wrong: `T-1021`'s own spec does carry 24, but it
  is not the largest in this repository — `grep -c '^\s*- check:'` across
  every file in `.shell-team/specs/*.md` shows `T-1011-telemetry-event-
  rows.md` at 35, plus seven other specs above 24 (`T-1001` 30, `T-111` 29,
  `T-1012`/`T-1010`/`T-1002` 28, `T-112`/`T-1008` 25). This does **not**
  threaten the width bound's soundness — `9999` headroom over the true
  measured max of 35 is still ≈286×, comfortably inside the same "100–1000×"
  order of magnitude the provenance itself claims as its justification, so
  the bound stays correct and safe regardless of which number is cited as
  "largest." It is the same class of finding as round 1's two Minors (a
  factual-accuracy slip in the audit's own self-description, not a
  functional or security gap) — worth a one-line correction (either drop
  "the largest measured" or correct it to name `T-1011`'s 35) the next time
  any of these three files is opened.

### Synthesis audit ledger

no severity changes

### Recommendation

- APPROVE → set `.shell-team/todo.md`'s T-1021 entry to `READY_FOR_MERGE`.

### Fast-follow disposition

- The "largest measured" citation inaccuracy above (Minor) — **file-an-**
  **issue**: it repeats identically across three files (a `bin/`-shipped
  code comment, the provenance record, and four rows of the audit document),
  so a future reader of any one of them could reasonably trust the
  superlative claim when reasoning about whether `{1,4}` stays safe as new
  specs are written — even though the actual number (`9999`) remains safe
  regardless of which spec is truly largest, the false "largest measured"
  claim is exactly the kind of unverified assertion this task's own
  discipline (never state a number without measuring it) exists to prevent,
  so it is worth a tracked correction rather than a silent carry-forward.
- No other deferrals; round 1's three prior findings are fully closed (see
  above), not fast-follows.
