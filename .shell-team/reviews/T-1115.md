# T-1115 — review record

## Delivered-change review

### Codex Review verdict: APPROVE
- Task: T-1115
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 5 files, +475/-0 lines (`git diff 6b60585...2004da3 --stat`); the reviewed deliverable is the one-line addition to `CONTRIBUTING.md` (`git diff 6b60585...2004da3 -- CONTRIBUTING.md` → `+1/-0`), the rest of the diff is this task's own records (`.shell-team/todo.md`, the spec, provenance, interventions).
- Review depth: full (per the launch briefing) — `user-visible: yes` shipped adopter-facing governance prose read by two derivation sites; the gitignored 2026-08-22 dev-scaffold carve-out does not apply.

**Verdict scope, per the launch briefing.** Two questions gate this verdict: (1) does the added sentence encode the operator ruling in issue #412 and no more — remove the PATCH performance-work reading for a default/claim inversion, leave the PATCH bullet's separate latent-behind-an-opt-in item untouched, and let the two existing tests govern jointly rather than granting MINOR outright; (2) can the sentence be read to license MINOR for Input-space class 2 (a documented-claim falsification whose default path is unchanged) — a genuine finding — versus a synthetic reworded restatement with no reachable instance (out of scope). Process-bookkeeping / records-hygiene findings are reported as notes, never as verdict-gating.

#### Independent primary reading (before consulting Codex's output)

Read first-hand: the full spec (`.shell-team/specs/T-1115-version-tier-tiebreaker.md`, all 387 lines — Problem, Summarized sources, the frozen intent block including Goal/Non-goals/AC1–AC14/Input space, Body-to-AC correspondence, Decision points DP1–DP4, Assumptions, Blast radius); the current `CONTRIBUTING.md:120–133` section directly (`sed -n '115,135p'`); the board's T-1115 entry in full including the version-derivation, freeze-attestation, blast-radius-sweep and both engineer/QA hand-off blocks; issue #412's relayed body (the verbatim copy at the path the launch briefing names).

**Citation-fidelity spot checks, verified against primary bytes:**
- The added line is byte-for-byte identical to the spec's `## Notes for engineer` recommended wording — confirmed by direct comparison, matching QA's independent finding.
- `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1115-version-tier-tiebreaker.md` (re-run independently, myself) → `13 passed, 0 failed, 1 skipped, 0 unrecognized` — matches both the engineer's and QA's reported result exactly.
- `.shell-team/specs/T-1020-lessons-supersede-sweep.md` AC14's own `- check:` line (`:110`) opened directly: a merge-point-scoped lock `git diff --name-only develop -- bin .github tests skills docs CONTRIBUTING.md README.md ...` — confirms the Blast-radius section's claim that this criterion's own text names `CONTRIBUTING.md` and is expected to redden once this diff lands on `develop`, and confirms T-1020's own spec (`:42`) frames a scope lock as intentionally ephemeral rather than perpetually binding. "Disclosed, not repaired" is the correct disposition: repairing another Done task's frozen criterion would be a class-B re-freeze of someone else's intent, which this task's `## Non-goals` correctly declines.
- `.shell-team/specs/T-1075-fanout-adoption-versioning.md` AC8's own check line (`:71`) opened directly: nine named bold anchors exactly once each plus exactly 3 tier bullets — confirms the added line (`**A default or claim inversion is not performance work.**`) duplicates none of the nine and matches no `^- \*\*(MAJOR|MINOR|PATCH)\.\*\*` pattern.
- Issue #412's relayed body: the operator ruling's own wording is "A change that inverts a shipped default or falsifies a documented behavioral claim takes precedence over the performance-work clause and derives MINOR... Wording is not prescribed; the freeze settles it." `environmentally-unverified: GitHub issue #412` for me too (no network read path in this sandbox for this session) — the spec's A-1 freeze-run measurement (2026-09-02, GitHub API) is the primary confirmation on record, and the relayed copy's three verbatim qualifiers ("not prescribed", "(2026-09-02)", "explicitly *withdrawn*") match the scratchpad text I was given.

**Independent judgment on the two gating questions, performed before consulting Codex's output:**

*(1) Faithful encoding, no more.* The shipped sentence: does **not** take the change's tier from the performance-work item (removes it from that grouping, matching the ruling's disjunctive trigger — "inverts a shipped default **or** falsifies a documented behavioral claim"); explicitly leaves "the PATCH bullet's latent-behind-an-opt-in item... as it stands" (matches DP1(a)'s narrow scope, and AC3 mechanically forbids `outranks the PATCH bullet` / `does not apply` / bare `regardless` on the line); and states "the two tests above govern it jointly" rather than granting MINOR unconditionally — this is DP2's deliberate departure from a literal outright-grant reading of the ruling's own sentence, and DP2's own ground (the ruling's mechanism is the *property* argument — shared "adopter's observed contract does not change" — which removes the change from the PATCH grouping rather than placing it directly in MINOR) is sound: an unconditional MINOR grant would let a change earn MINOR by a route no other change in this document uses (`CONTRIBUTING.md`'s own words, "passing only one of the two is not sufficient" — a document-wide invariant), so the joint-gate reading is the interpretation that keeps the file internally consistent rather than one that keeps it more literal to a single sentence in the ruling. This is the correct trade to make on a "wording is not prescribed" ruling.

*(2) The exploit test for Input-space class 2.* The sentence's MINOR-granting clause is narrowly worded: "an inversion of the **default shipped path** meets both and derives MINOR" — this names only the default-path-inversion subtype, not the claim-falsification subtype generally. A class-2 change (documented-claim falsification, default path unchanged) fails the default-reachability test on its own terms regardless of this new sentence, so applying "the two tests above govern it jointly" to it yields PATCH, matching the spec's own expected outcome for Input-space class 2 and the Non-goals' explicit statement ("a documented-claim falsification whose default path is unchanged still derives PATCH under this rule"). I found no clause in the shipped wording that a careful reader could use to license MINOR for class 2 without misreading "the two tests above govern it jointly" as inapplicable to that subtype — and Input-space class 4 explicitly excludes an adversarial reworded restatement with no reachable instance behind it. No finding here.

No other over-reach: the line names neither MAJOR nor an amendment to either test bullet (AC3/AC4 mechanically lock both), and `## Non-goals` correctly disclaims any MAJOR-tier interaction.

#### Codex's independent findings (raw output in `T-1115-codex-primary.txt`)

Primary pass (`codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>"`, free-form prompt scoped to the two gating questions above, read from stdin/argv — see `executor-invocation` below) returned an **empty findings array** with the closing remark: "Class 2 is constrained by 'the two tests above govern it jointly'; MINOR is expressly tied to 'an inversion of the default shipped path meets both and derives MINOR.'" — independently reaching the same conclusion on question (2) that my own reading reached, via the same textual anchor.

**Hygiene note, not a finding on the diff.** Mid-run, Codex's own tool trace (visible in the JSONL) opened a **pre-existing, gitignored, uncommitted** file at `.shell-team/reviews/T-1115-codex-primary.txt` that predates this round and whose content (references to a "flips:" record, base ref `4020028`, a legacy `tasks/`+`docs/specs/` layout, `.shell-team/AGENTS.md`) does not correspond to anything in this repository's actual T-1115 spec, board entry, or history — a stale artifact from an unrelated prior session, left on disk under a canonical-looking name. Codex's tool call quoted that stale content mid-transcript but did **not** carry it into its final verdict message (the final message is scoped correctly to the real diff and the real spec). This raw-capture stem is inside this role's own write boundary, so publishing this round's real capture (`codex-capture.sh --publish --stem T-1115-codex-primary ...`) atomically overwrote the stale file with this round's genuine output — resolved as part of this round's own housekeeping, not deferred.

No blocker or major surfaced by either reading, so the opt-in adversarial pass (triggered only by a blocker/major) was not run.

#### Blast radius disposition — judged, not merely reported

The launch briefing asks whether "disclosed, not repaired" is the right disposition for the one flip the freeze-and-QA-confirmed sweep found (`T-1020` AC14, reddened). Independently re-derived above: the criterion is a `develop`-scoped byte-identity lock that necessarily reddens once *any* later task touches `CONTRIBUTING.md` on top of `develop` — its own spec frames this as the intended lifetime of a scope lock, not a defect, and this task's own AC12 carries an identical self-disclosure about itself ("expected to go stale after merge... a scope lock's intended lifetime rather than a defect to chase"). Repairing T-1020's frozen criterion here would itself be an unauthorized class-B re-freeze of a Done task's intent, which `## Non-goals` correctly declines. Agree: disclosed, not repaired, is the correct disposition.

#### Executor-invocation record

- executor-invocation (s1): codex exec --sandbox read-only --cd <repo-root> --json -o "<RAW_OUT>" "Review the T-1115 deliverable in this repository: the diff between base ref 6b60585 and HEAD (2004da3), scoped to CONTRIBUTING.md's '## What a version number encodes' section only... [full prompt scoped to the two gating questions in this record's Verdict scope section, plus a MAJOR-interaction/test-amendment over-reach check] Return findings as a JSON array of {severity, file, line, issue, suggestion}..." > "<RAW_JSONL>" 2>&1
- pass-role (s1): generation
- briefing-fidelity (s1): carried — the launch briefing's verdict-scope questions (1) and (2), the exact operator-ruling wording, and the spec's DP1/DP2 framing were reproduced directly in the prompt argv passed to `codex exec`; the calibration note (process-bookkeeping findings never gate the verdict) was applied by me in synthesis rather than passed into the Codex prompt, since it governs how *I* weigh findings, not what Codex is asked to look for.
- raw-capture (s1): T-1115-codex-primary

#### Synthesis audit ledger

no severity changes

#### Recommendation
- APPROVE → set `.shell-team/todo.md` T-1115 to READY_FOR_MERGE

#### Fast-follow disposition
- no fast-follow deferrals
