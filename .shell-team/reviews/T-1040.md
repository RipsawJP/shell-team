<!-- BEGIN review: T-1040 -->

# QA verification — T-1040

**Verdict: PASS** — READY_FOR_QA → READY_FOR_REVIEW

## check-acs.sh

`CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh .shell-team/specs/T-1040-frozen-repair-batch.md`
→ **19 passed, 0 failed, 1 skipped (AC20, runtime)** — measured live myself (dry-run first: 0 unrecognized, every check read-only apart from its own `mktemp -d` scratch), matching the engineer's reported shape exactly.

## Frozen-intent alignment and class

`bin/check-intent.sh` run independently against the resolved board for T-1040 itself and for all ten re-frozen targets — **11/11 aligned**, versions exactly vK+1 as declared: T-1017 v2, T-1018 v2, T-1024 v3, T-1027 v5, T-1029 v6, T-1030 v3, T-1033 v2, T-1035 v2, T-1036 v6, T-1037 v3, T-1040 v1 (`09c3efff620f7ede4abbe2412ef77ddc22c041bb`). No other spec on the board is currently open (`.shell-team/todo.md`'s `## Active` carries only T-1040 plus the template placeholder line), so this is the whole open-spec population.

`bin/check-refreeze-class.sh -- <base@6fc3910> <current>` run independently for all ten targets: **exit exactly 1 (class-b), ten times**, matching DP5's claim exactly (no 0, no 2).

## The three `caused-here` blast-matrix reds — adjudicated

- **T-1024 AC8 → (a), acceptable.** T-1024's own spec self-declares AC8 merge-point-scoped: its sibling AC7 states "every criterion it reports as FAIL must be one of AC7, AC8 or AC10 — the three that resolve the superseded ref `1e31600` and that T-1024's own text declares merge-point-scoped," independently corroborated by T-1035's DP-G ("T-1024's own merge-point-scoped criteria are disclosed as stale...") and Non-goals ("No re-derivation... of T-1024's merge-point-scoped criteria (its AC7, AC8, AC10)"). The red is caused by this pass's own legitimate ten-line board deletion, exactly the class of diff AC8's own declaration anticipates.
- **T-1035 AC5 → (a), acceptable.** T-1035's own AC5 text carries the literal sentence "**Merge-point-scoped and expected to go stale after merge** — once later work lands on `03e6250`, a diff against it no longer describes this task." The red is caused by this pass's own further edit to T-1024's DP1 (the `X24a`–`c` repair, `.shell-team/specs/T-1040`'s AC2), which changes the delta against that same older ref — exactly the kind of legitimate additional diff the declaration anticipates and the spec's own Notes-for-engineer item (ii) names as expected.
- **T-1037 AC3 → (b), a genuine but disclosed, non-blocking defect.** Reproduced live: `AC3: FAIL (exit 1)` on `.shell-team/specs/T-1037-checker-retro-precision.md` at HEAD (was PASS at base `6fc3910`, confirmed). Root cause, independently confirmed: `agents/scrum-master.md`'s line 78 now carries `X1` (spliced verbatim per AC4), which contains the literal substring `` `git log --merges --first-parent` ``; T-1037's own AC3 forbidden-token sweep (frozen at that spec's own v1→v2) requires zero occurrences of `git log` in both `S1`/`S2` and their live shipped lines. AC3's own text carries no ref-diff framing and is **not** declared merge-point-scoped anywhere — it is a permanent content lock, not a claim about "what changed since a named commit" — so this does not qualify for the (a) bucket under the task's own rule. This is a previously-green, already-merged sibling criterion broken by this pass's shipped bytes. **Does not block this PASS**: none of T-1040's own AC1–AC19 assert T-1037's own AC-suite outcome (AC18's triple `check-acs.sh` recursion is explicitly scoped to T-1029/T-1030/T-1036 only, per DP11); the repair route is a pm-spec re-freeze of either `X1`'s wording (T-1040's own frozen DP13) or T-1037's own AC3 sweep (T-1037's frozen intent block) — out of engineer's and QA's authority per R1/R3; and it is fully, accurately and independently disclosed in three places I cross-checked against my own reproduction (spec's own `## Notes from engineer`, `.shell-team/provenance/T-1040.md` decision 4, and the board's engineer hand-off sub-bullet), all matching my measurement exactly. Routed to the cross-provider review's independent veto over this one re-freeze (AC20(f)), and flagged here for the next pm-spec touch of either spec.

## AC20(b)/(c) spot-checks — scratch `git worktree` only, real checkout confirmed clean before/during/after (`git status --short` empty; `git worktree remove` after)

Component-form-only sites (both of the two named): **T-1030 AC6** and **T-1036 AC9**.

- **Decoy, T-1030 AC6** (component-level, since its own "base" read is `git show 53955c5:agents/codex-reviewer.md`, a fixed historical blob — the spec's own AC20(b) wording, "the base blob's **stand-in**," anticipates exactly this; mutating real git history is forbidden by R4/the destructive-op rule). Built a stand-in file with a duplicated anchor line and ran both locators' own logic directly: repaired form (`c=$(awk ...); test "$c"="1"`) → **rc=1, RED** (catches the duplicate); superseded form (`grep -n | head -1`) → **rc=0, GREEN** (silently accepts the first match). Matches DP4's decoy contract exactly.
- **Decoy, T-1036 AC9** (full end-to-end, live scratch board): appended a second, non-adjacent `- [x] **T-1029** ...` top-level header to the scratch board (leaving T-1029's own real sub-bullet region undisturbed, so extraction of it would otherwise succeed). Repaired form (`cwork=$(awk '{c++}...')`; `test "$cwork"="1"`) → **RED** (whole-file header count = 2). Superseded form (direct region-extraction awk, no count check) → region for T-1029 still extracts non-empty and correct (extraction already stops at T-1030's header, before reaching the appended duplicate) → **stays GREEN**, silently missing the decoy elsewhere in the file. (An earlier attempt with an *adjacent* duplicate confounded both forms via the shared extraction state machine's own pre-existing empty-region positive control; the non-adjacent placement isolates the intended signal cleanly and is the fixture that actually demonstrates the fix.)
- **Revert-detection, T-1030 AC6**: replaced T-1030 spec's AC6 check-line text in the scratch worktree with its verbatim base-`6fc3910` (superseded) form → T-1040's own **AC8 flips to FAIL (exit 1)**; restored → **AC8 back to PASS (exit 0)**. Works exactly as designed.
- **Revert-detection, T-1036 AC9 → a genuine, newly-discovered, non-blocking finding.** Replaced T-1036 spec's AC9 check-line text with its verbatim base-`6fc3910` (superseded) form → T-1040's own **AC9 stays PASS (exit 0)** — it does **not** catch the revert. Root cause, independently confirmed: T-1040's own AC9 check verifies T-1036's three #157 repairs (AC1, AC8, AC9) with three different rigours — AC1 and AC8 each pair a specific positive anchor token (`A36a`/`A36b`, both long, specific, frozen-block-extracted strings) with a specific negative/absent-form anchor (`A36d`/`A36c`); AC9's own sub-check is a *bare, unanchored* substring test, `grep -qF -- '= "1"' "$T/AC9"`, with no corresponding negative anchor. T-1036's superseded (pre-T-1040) AC9 check line already incidentally contains the literal substring `= "1"` from unrelated, pre-existing boilerplate (the `R29`/`R30` frozen-block extraction-count assertions, `test "$(grep -c . "$T/$n" || true)" = "1"`), so the bare substring test cannot distinguish "the specific per-task-entry uniqueness fix landed" from "some unrelated assertion happens to contain the same three characters" — exactly the grep/regex-vocabulary-collision class the playbook's lessons warn about, and exactly the defect class (`DP4`/`DP12`) this whole batch exists to eliminate elsewhere. This is distinct from, and finer-grained than, the engineer's own disclosed AC9 component probe (board sub-bullet, item (b)): the engineer's probe and my decoy test above both confirm **T-1036's own real repair** is sound; my revert-detection additionally shows that **T-1040's own AC9 check**, specifically, cannot detect a targeted revert of *only* the AC9 sub-repair (as opposed to the engineer's own revert-detection sweep, item (c), which reverted each *target file in full* and so also exercised AC1's/AC8's still-intact specific anchors alongside AC9's weak one, masking this narrower gap). **Judged non-blocking**: it does not affect current correctness (AC9 correctly reports PASS on the real, correctly-repaired state, independently confirmed via the decoy test above); the actual T-1036 deliverable is sound; the check line lives inside T-1040's own frozen intent block, so the fix is a pm-spec re-freeze of T-1040 itself, not an engineer edit; and it is a narrow, single-sub-component gap in an otherwise well-anchored eight-site batch. Flagged here as a Risk Note and a candidate for a light follow-up re-freeze (tighten the anchor to a literal specific to the fix, e.g. the `{c++} END{print c+0}` / `10#$cwork` fragment, mirroring `A36a`–`d`'s shape).

## AC20(d) blast-matrix spot-check — 3 members re-measured at HEAD, real checkout

`bash bin/check-acs.sh` re-run directly on three population members: `T-1025-assert-parity-dead-comparison.md` (AC1–5 PASS, AC6/AC7 FAIL, AC8 SKIP — matches the declared row exactly, `unchanged`), `T-113-lessons-deidentification.md` (AC3/AC6 FAIL, all else PASS including AC9 — matches the declared **at-head** row exactly, `pre-existing`), `T-1004-optin-hook-sample.md` (AC1–16 PASS, AC17/AC18 FAIL — matches the declared row exactly, `unchanged`). All three match the spec's own coverage table byte-for-byte.

## Full CI-wired suite list — `.github/workflows/check-handoff.yml`, every `run:` step, in workflow order

`shellcheck --version` confirmed `0.11.0` (pinned) before running the full multi-file argument list → clean. All ~50 fixture suites and dogfood steps run individually, in workflow order, PR-diff steps against `origin/develop` (fetched and resolved locally): **all green, 0 failures.** The four suites sharing fixed scratch paths (`check-playbook`, `gen-playbook-blocks`, `playbook-promote`, `retro-inputs`) run strictly sequentially, never concurrently. Both directory/glob-enumerating dogfood steps run rather than reasoned about: `check-interventions` over every committed `.shell-team/interventions/*.md` (found ≥ 1, all conformant) and the `gen-playbook-blocks` scratch-regeneration (zero diff on all 8 registry files).

## Explicit item-6 checks

`bash bin/check-handoff.sh "$(bin/team-paths.sh --get todo)"` → exit 0. `bash bin/check-board-headings.sh <board> --base 6fc3910` → exit 0. `bash bin/check-provenance.sh .shell-team/provenance/T-1040.md` → conformant, 5 decision entries. `bash bin/check-interventions.sh --task T-1040 -- .shell-team/interventions/T-1040.md` → conformant, 1 entry. `bash bin/check-pii-shapes.sh --base 6fc3910` → clean.

## Known non-findings (frozen) — confirmed consistent

T-1035 AC4's pre-existing red carries its `defer-key: t1035-ac4-corpus-lock` row (confirmed via AC16's own live PASS). T-1036 AC9/AC13 merge-point reds are present in the blast matrix and in AC18's permitted-FAIL set (confirmed). The 40-line block-size warning is unaffected (`gen-playbook-blocks` dogfood green, zero diff). Merge-point criteria are explicitly declared stale-after-merge in AC14/AC15/AC19's own text.

## `## Summarized sources` — verified against primary bytes, per source

Every frozen-region restatement site plus the declaration line itself, checked against the source's own bytes (never against the spec's paraphrase):

- **GitHub issues #151, #155, #157, #159** — `environmentally-unverified`: this session has no working `gh`/GitHub-API path (`gh auth status` shows an invalid keyring token; `gh issue view` fails TLS verification in this sandbox; no GitHub MCP tool is available in this session's toolset). Declared relay-not-opened by the spec itself; consistent with the engineer's own independent restatement of the same status.
- `.shell-team/reviews/T-1033.md`, `## 7. Judgment on the AC15(c)…` — opened. Confirmed: N2/N3 correct as named, N1's real source is `.shell-team/reviews/T-1028.md` (not `T-1032.md`), and "T-1032.md contains no tmpfile/EXIT-trap/subshell incident at all" (line 103) — matches the spec's "structurally impossible... rather than merely imprecise" claim exactly. **Match.**
- `.shell-team/retros/2026-08-05.md`, `## Lesson candidates` — opened. `Source:` citations for candidates 2/3/5 (`.shell-team/reviews/T-1028.md`（Major 1）, `.shell-team/reviews/T-1032.md`（Major 2/3）, `.shell-team/interventions/T-1028.md`（entry 3）) confirmed byte-exact at lines 63/64/66. **Match.**
- `.shell-team/reviews/T-1028.md`, `#### (a) Round-1 findings — closure verification` / `**Major 1 (` — opened. Line 566/570: the TMP_FILES-registered-inside-a-command-substitution-subshell description confirmed exact. **Match.**
- `.shell-team/reviews/T-1035.md`, `## AC8 blind-spot judgment` — opened. Confirmed exactly: the referents are T-1035's own AC7/AC8 (not T-1024's), and AC8 measured green because its gained-row set is a `comm -13` difference, not board-deletion arithmetic. **Match.**
- `.shell-team/reviews/T-1037.md`, round-2 synthesis row 1 — opened. Line 282: "Minor... fails conservatively — it can only push a matchable sub-bullet into the 'unverified, disclose in `## Notes`' path, never fabricate a match" — confirmed byte-exact. **Match.**
- `.shell-team/reviews/T-1038.md`, lines 40 and 77 — opened at those exact line numbers. Both confirm "genuinely pre-existing at `2dc423b`... T-1017's changed-file set is identical at base and at HEAD (eleven files...)" exactly as claimed. **Match.**
- `.shell-team/reviews/T-1036.md` — explicitly not opened by pm-spec (AC20(e) delegates to the engineer round). I opened it independently myself: confirmed the full eight-site enumeration (T-1029 AC2/AC3/AC4/AC10, T-1030 AC6, T-1036 AC1/AC8/AC9, lines 738 and 998–1002) and the single remedy shape ("fold into the same class-phrased issue... since all eight share the identical root cause") match T-1040's DP4/site-ledger exactly. **Match** (and independently discharges AC20(e)'s substance).
- `bin/check-intent.sh` — opened. `ATTEST_FULL_RE` (line 367) and the exact carve-out conditional `[ "$hash_valid_count" -eq 1 ] && [ "$attest_shaped_count" -eq 0 ]` (line 620) confirmed byte-exact; the pace rule and version-N-only cross-check independently confirmed operationally via my own eleven live `check-intent.sh` runs above. **Match.**
- `bin/check-refreeze-class.sh` — exercised live across ten pairs; every FAIL message quotes the exact three-part rule (line-count equality, differing-index existence, both-sides `- check:` shape) the spec describes. **Match.**
- `CONTRIBUTING.md`, `## Re-freezing a frozen intent block` — opened. The four closed tokens `broken-as-command`, `vacuous`, `contradictory`, `review-rejected` confirmed byte-exact (line 96). **Match.**
- `bin/retro-inputs.sh` — opened. `git log --merges --first-parent` and `promote_empty cycle-window` both present (also independently confirmed operationally via T-1040's own live AC5 PASS). **Match.**
- `agents/pm-spec.md`, `## Spec completion self-check` — opened. Fourth bullet head `**Diff-scope-closure staleness norm (forward-only, #240 item 2)**` confirmed byte-exact at line 77, closing `**` after the parenthetical as claimed. **Match.**
- `.shell-team/todo.md`'s `## Done` entries for the ten targets — independently re-derived via `check-intent.sh`'s own reporting (see Frozen-intent alignment above): every version is exactly vK+1, and T-1017 uniquely carries the v1 backfill attestation (confirmed via AC13's own live PASS). **Match.**
- The operator's own `CLAUDE.local.md` — cross-checked against this session's own loaded copy of that file: "凍結（初回・mechanics再凍結とも）にホストGOを取らない…新設はホストの明示裁定のみ" matches DP5's claimed ground ("orchestrator attestation," "no new human gate is invented") exactly. **Match.**

No flattened distinction found in any source this session could open. Four sources (the GitHub issues) remain `environmentally-unverified` for the reason stated above; that status is disclosed rather than silently omitted and does not by itself drive a FAIL.

## Edge cases / adversarial fixture synthesis

- **Class 1 (beyond happy-path)**: decoy duplicates (adjacent and non-adjacent board headers; stand-in duplicate anchor lines) and full-text reverts against 2 of the 8 #157-repaired anchors, on a disposable scratch worktree.
- **Class 2 (regex/character-set anchoring)** — the one that surfaced the AC9 finding above: deliberately probed the `= "1"` substring for a vocabulary collision against T-1036's own pre-existing, unrelated boilerplate.
- **Class 3 (Markdown/CommonMark)**: not the primary surface for this task's own deliverable (frozen-block splicing over already-well-formed markdown); `check-intent.sh`/`check-refreeze-class.sh` were nonetheless exercised live over eleven real, full-sized spec files as part of the above, which exercises realistic fence/marker shapes.
- **Class 4 (test-harness short-circuit)**: every CI `run:` step executed exactly as written, in order, via real invocation (not stubbed); `CHECK_ACS_TIMEOUT=900 bash bin/check-acs.sh …` run for real, not read.

## Records / hygiene

`.shell-team/provenance/T-1040.md` — conformant, 5 decision entries, all independently corroborated by direct reproduction above (frozen-splice-programmatically, the class-M→class-b prose-sentence fix, the post-all-ten-updates attestation-timing rationale, the X1/AC3 collision disclosure, the AC15/DP8 boundary reasoning). `.shell-team/interventions/T-1040.md` — conformant, 1 entry (assumption-contradicted, the relay-trim re-check), independently spot-checked against the cited trimmed parentheticals — accurate, immaterial as claimed.

## Risk notes for reviewer

1. **T-1037's AC3 is a genuine, disclosed regression** (see adjudication above) — not this task's own scope, but worth a deliberate look at the cross-provider review's independent veto over the T-1037 re-freeze specifically (AC20(f)); the recommended follow-up is a pm-spec re-freeze rewording `X1`'s parenthetical or narrowing AC3's forbidden-token sweep.
2. **T-1040's own AC9 has a narrow, non-blocking self-verification gap** (see AC20(b)/(c) spot-check above) — a bare-substring sub-check for T-1036's AC9 repair, unlike its sibling anchors. Does not affect current correctness; recommended as a light follow-up re-freeze target.
3. Both risk notes point at frozen bytes; per R1, the repair route for either is a pm-spec re-freeze, not an engineer edit — this PASS does not ask the engineer to touch either.

<!-- END review: T-1040 -->

<!-- BEGIN review: T-1040-codex -->

### Codex Review verdict: APPROVE
- Task: T-1040
- Codex model: gpt-5.6-sol (config default, `model_reasoning_effort = "high"`; no `-m` override passed)
- Verification mode: executed-tests — independently re-ran `bin/check-intent.sh` (11/11 aligned) and `bin/check-refreeze-class.sh` (10/10 exit-1 class-b) live against the resolved board and all ten targets; reproduced the T-1037 AC3 regression live (`check-acs.sh` on `T-1037-checker-retro-precision.md`: AC3 FAIL at HEAD, confirmed PASS at base `6fc3910`); reproduced T-1040's own AC9 self-verification gap by diffing the `= "1"` occurrence count in T-1036's AC9 check line at base (2) vs head (4) and confirming the bare substring test cannot distinguish them; reproduced the new T-1017 AC15 / T-1040 AC11 anchor-vacuity gap by mutating a scratch copy of `bin/close-out.sh` (removed the executable `check-interventions.sh` invocation, left the line-31 comment) and confirming the check still reports `rc=0`; verified all `X`/`O`/`A`/`S`/`Q`/`B17` frozen-block splices byte-exact against their three target sites via `grep -Ff`; verified the board's exactly-ten-deletions claim via `git diff -U0`; verified T-1017's `B17` v1 backfill against the base board's own pre-freeze live-run sub-bullet (2026-08-02, 18/18, 3P/15F, coordinating session) — all on a scratch `git worktree`, real checkout confirmed clean before/during/after.
- Diff scope: 16 files, +768/-46 (`git diff --stat 6fc3910...1b6fb58`)

#### The ten mandatory disclosure items — independently verified, each with its own disposition

Each item below was checked against the board's actual diff (not the spec's prose) and against `bin/check-intent.sh` / `bin/check-refreeze-class.sh` run live by me. All ten: version transition exact, `check-refreeze-class.sh` exit exactly `1` (class-b), GO grounds present and consistent (sprint v1.7.0 planning GO 2026-08-06 + the issue's own prescribed route + the operator's standing-authority ruling), no independent objection from me. A `REQUEST_CHANGES` against one would not implicate the others; none is requested.

1. **T-1017 v1→v2** — confirmed `check-intent.sh: aligned v2 (c78abb5…)`. The **v1 `B17` backfill** is a genuine transcription, not a fabrication: the board at base `6fc3910` already carried the exact pre-freeze live-run sub-bullet ("2026-08-02, all 18 `check:` lines at base `e18e50d`… 15 FAIL, 3 PASS… owner: coordinating session"), and `B17`'s frozen text (`lines=18/18 sweep=mutual-satisfiability verdict=3P/15F owner=coordinating session`) is that record's numbers repackaged into the grammar, byte-checked. **No objection.**
2. **T-1018 v1→v2** — confirmed `aligned v2 (f81e873…)`, `check-refreeze-class` exit 1. AC18's replacement (two permanent invariants + two green suites) verified live: `tests/errexit-safe/run.sh` and `tests/codex-skeleton-hygiene/run.sh` both exit 0; the `NOT_APPLY` pin registry carries zero `check-intent.sh:`-anchored lines. **No objection.**
3. **T-1024 v2→v3** — confirmed `aligned v3 (b752af5…)`. DP1's three replacement lines (`X24a`–`c`) verified present exactly once in the spec, superseded `O24a`–`c` verified absent, and the ref-pinned `git show` command (`P` block) verified to survive as exactly one copy in `T-1035`'s spec rather than being duplicated into DP1. **No objection.**
4. **T-1027 v4→v5** — confirmed `aligned v5 (73854a3…)`. `F3`/`F4` blocks carry `X1`/`X2` byte-exact (see item 1 of the AC3 judgment below for the one caveat, which is T-1037's, not T-1027's own). **No objection.**
5. **T-1029 v5→v6** — confirmed `aligned v6 (743a5f2…)`. Four repaired anchors (AC2/AC3/AC4/AC10) verified against their frozen `A29a`–`d` tokens. One new, previously-undisclosed finding surfaced while probing AC2's own decoy resistance — see Major finding 3 below; it does not implicate the ten-item disclosure itself (the repaired anchor's stated fix — closing the label's boundary — is genuinely present and correct; the gap is a narrower, orthogonal ordering-uniqueness property DP4's summary sentence implies but this one check does not fully deliver). **No objection to the re-freeze; a fast-follow is recommended.**
6. **T-1030 v2→v3** — confirmed `aligned v3 (051d700…)`. AC6's anchored count-of-one locator verified via the engineer's own component-level decoy (reproduced its logic directly against a stand-in file, matching the disclosed shape exactly). **No objection.**
7. **T-1033 v1→v2** — confirmed `aligned v2 (90c6933…)`. AC15(c)'s three-record replacement (`X151`) verified against the three cited primary records (`T-1028.md` review + interventions, `T-1032.md`), all readable and carrying the cited anchors. **No objection.**
8. **T-1035 v1→v2** — confirmed `aligned v2 (20b8597…)`. AC14(b)'s corrected prediction (`X155`) verified consistent with the `comm -13` mechanism it now names. **No objection.**
9. **T-1036 v5→v6** — confirmed `aligned v6 (fb71144…)`. Three repaired anchors (AC1/AC8/AC9) verified against `A36a`–`d`; the T-1036-side repair is sound (confirmed via the engineer's disclosed decoy and my own read of the live check line). The self-verification weakness is on T-1040's own AC9, not on this re-freeze — see Minor finding below. **No objection to the re-freeze.**
10. **T-1037 v2→v3** — confirmed `aligned v3 (7be8c3c…)`. This is the load-bearing item; full judgment immediately below. **No objection — APPROVE with a disclosed, non-blocking Major finding and a fast-follow.**

#### Judgment on item 1 — the T-1037 AC3 collision (the single most load-bearing item)

Reproduced independently, not accepted on the spec's or QA's word: `bin/check-acs.sh` against `.shell-team/specs/T-1037-checker-retro-precision.md` reports **AC3: FAIL** at HEAD and **PASS** at base `6fc3910`. Root cause confirmed by direct read: `X1` (spliced into `agents/scrum-master.md:78`, `T-1027`'s `F3` block, and `T-1037`'s own `S1` block) contains the literal substring `` `git log --merges --first-parent` ``, and `T-1037`'s AC3 forbidden-token sweep — which this same re-freeze edits only the *positive* pin literals (`X3a`/`X3b`) of, leaving the *negative* list (`git log`, `git rev-list`, `spot-check`, …) untouched — requires zero occurrences of `git log` in both shipped lines. Codex's own primary pass (via `codex exec review`, reading the files directly) independently found the identical root cause and location without being told about it.

**Disposition: APPROVE, non-blocking, Major, fast-follow — not REQUEST_CHANGES against this re-freeze.** Grounds:
- The break is *self-inflicted but low-risk*: `git log --merges --first-parent` in `X1`'s parenthetical is purely descriptive documentation of what `retro-inputs.sh` does internally; nothing in the shipped agent instructions tells `scrum-master` to run `git log` itself. AC3's forbidden-token sweep is a blunt instrument (any literal occurrence of the string, regardless of context) catching a documentation mention, not an actual optionality/fetch-drift regression in the described behavior — the property AC3 exists to protect (`agents/scrum-master.md` never reads as a suggestion, never fetches) is intact.
- It is disclosed **three times independently and accurately** (spec's `## Notes from engineer`, `.shell-team/provenance/T-1040.md` decision 4, board hand-off), and QA's own reproduction matches mine exactly, including the routing judgment.
- None of T-1040's own AC1–AC19 assert T-1037's AC-suite outcome (confirmed: AC18's triple `check-acs.sh` recursion is scoped to `T-1029`/`T-1030`/`T-1036` only, per DP11) — so this is a disclosed collateral effect on a sibling spec's already-merged criterion, not a defect in what T-1040 itself claims to deliver.
- The repair route is correctly identified as out of engineer/QA authority (R1/R3: frozen text, a further pm-spec re-freeze) — reverting the whole group-1 re-freeze to avoid this would sacrifice the genuine, correctly-targeted #159 fix (the over-broad "never appears in a sub-bullet" claim) to avoid a narrow, already-disclosed, low-risk checker false positive on a documentation string. That is not a good trade.
- Recorded honestly as a Major (not waved through as trivial): shipping v3 of a spec with one of its own already-merged, permanent-content-lock criteria newly red is a real quality defect in this pass's own execution, and the fix (reword `X1`'s parenthetical, or narrow AC3's sweep to exclude the one literal) is cheap and well-scoped for the next touch.

#### Blockers
- none

#### Major
1. **T-1037 AC3 collision** (`.shell-team/specs/T-1037-checker-retro-precision.md`, `agents/scrum-master.md:78`) — see judgment above. Disclosed, non-blocking, fast-follow.
2. **T-1040's own AC9 self-verification gap** (`.shell-team/specs/T-1040-frozen-repair-batch.md`, the AC9 check line verifying `T-1036`'s AC9 repair) — already disclosed by QA and independently reproduced by me: `grep -qF -- '= "1"'` is a bare, unanchored substring test; T-1036's AC9 check line contains that substring from unrelated pre-existing boilerplate (the `R29`/`R30` extraction-count assertions) both at base (2 occurrences) and at head (4 occurrences), so a targeted revert of only T-1036's AC9 repair leaves T-1040's own AC9 at PASS. Does not affect current correctness (the real T-1036 repair is independently confirmed sound via decoy testing); a narrow, single-sub-component self-check gap in an otherwise well-anchored batch. **Downgraded to Minor in the final synthesis below** — see ledger.
3. **T-1017 AC15 / T-1040 AC11 — new, previously undisclosed anchor-vacuity gap** (`.shell-team/specs/T-1017-close-out-interventions-gate.md` AC15, `.shell-team/specs/T-1040-frozen-repair-batch.md` AC11) — found by the adversarial pass, independently confirmed by direct mutation. AC15's replacement (DP8's final-state repair for the base-red history claim) asserts the interventions gate is invoked from exactly one `bin/` file via `grep -qF -- 'check-interventions.sh --task' bin/close-out.sh`. This is satisfied by the **comment** at `bin/close-out.sh:31` ("...verified with the sibling check-interventions.sh --task T-NNN...") — the real, executable invocation at line 262 uses the indirected form `bash "$CHECKER" --task "$TASK"` and never spells out the literal filename. I mutated a scratch copy of `bin/close-out.sh`, replacing the real invocation (line 262) with a no-op while leaving the comment untouched: the check still reports `rc=0`. T-1040's own AC11 (which verifies this exact criterion) re-runs the identical vacuous test and inherits the same gap. This does not currently produce a wrong verdict (the real invocation is present today), but it means the "permanent invariant" DP8 designed this criterion to durably guard — the interventions gate cannot be silently deleted — is not actually enforced against a comment-preserving deletion. This is exactly the anchor-vacuity class this whole batch exists to eliminate elsewhere (the eight #157 sites), newly introduced in one of the two base-red repairs, which fell outside the #157 remedy's own explicit scope. Non-blocking for this merge (current state correctly green; frozen text, so the fix route is a further pm-spec re-freeze, not an engineer edit), but a real gap. Recommend folding into the same issue candidate `## Non-goals` already names for "a checker that detects an under-anchored `check:` line."

#### Minor / nits
- **T-1029 AC2 ordering-anchor lacks a uniqueness guard before positional extraction** (`.shell-team/specs/T-1029-claim-fidelity-qa-step.md` AC2) — the repaired anchor (closing `**`/parenthetical on the fourth bullet head) is genuinely correct and closes the specific collision it targets. But the four bullet-head position lookups it depends on (including the repaired one) use `awk 'index($0,p)==1{print NR; exit}'` — first-match, no occurrence-count guard. A duplicated bullet head (one copy above `F2`, the real one displaced below it) would report the first (decoy) occurrence's line number and pass, missing a genuine ordering violation. Verified real via direct construction reasoning against the live check line (not executed against the working tree). Practical risk is low: `agents/pm-spec.md`'s `## Spec completion self-check` section is a static, rarely-edited, code-reviewed prose section, not a structurally-recurring shape like the board's per-task entries (which is exactly why the analogous `T-1036` AC9 gap was judged worth a decoy test and this one is lower-priority). Fast-follow.
- **`agents/scrum-master.md:78,110` (`T-1027`'s `F3`/`F4`, `T-1037`'s `S1`/`S2`) — a task ID read in a merge subject is now trusted as matchable identity** without corroboration against the cycle's own merge-SHA material. This is the first time the "matchable if read" design (the #159 fix itself) reaches a live, shipped agent file rather than only spec prose. Low practical impact: `scrum-master`'s output is human-reviewed retro prose, not an automated/production action, and the realistic trigger (a foreign merge subject coincidentally or deliberately containing a `T-\d+`-shaped string) requires an already-code-reviewed merge to carry it. Not a security/trust-boundary break in the sense Codex's adversarial pass first framed it — DP3's "never fabricates a match" claim is about the agent's own behavior (it doesn't invent matches), which holds; the residual risk is about the reliability of free-text as corroborating evidence, which is a precision question, not a safety one. Fast-follow (document the residual risk, or require SHA-level corroboration in a future re-freeze).
- **AC15 (`T-1017`)/AC18 (`T-1018`)'s workflow-file-count check uses `ls -1 .github/workflows`**, which omits dotfiles and counts subdirectories as entries. A hidden second workflow file would be invisible to the count (false PASS on a duplicate); a subdirectory would inflate the count (false FAIL). Low practical likelihood (GitHub Actions workflow files are conventionally non-hidden, directly under `.github/workflows/`), but a real completeness gap in a "permanent invariant" criterion. Fast-follow.

#### Adversarial findings
Triggered per role default (two Major findings survived the primary pass — the T-1037 AC3 collision and T-1040's own AC9 gap — both already known/disclosed before this round). Ran `codex exec` with a devil's-advocate framing against the diff, explicitly told about both known items and asked to find anything worse. Codex surfaced two *new* findings (the T-1017 AC15/AC11 anchor-vacuity gap, and the T-1029 AC2 ordering-anchor gap) plus one restatement (the scrum-master.md task-ID-trust concern, which the adversarial pass framed as a security/trust-boundary issue at Major — re-judged Minor in synthesis, see ledger) and one Minor (`ls -1` dotfile/directory gap). Both new findings were independently reproduced by me before acceptance (direct mutation for the AC15/AC11 gap; direct read of the check-line logic for the AC2 gap) — neither was accepted on Codex's word alone.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | T-1040's own AC9 self-verification gap for T-1036's AC9 repair (Codex primary: `[P2]`, `.shell-team/specs/T-1040-frozen-repair-batch.md:343`) | primary: major (P2) | minor | Independently reproduced (the `= "1"` substring exists in T-1036's AC9 check line at both base and head from unrelated boilerplate). Matches QA's own already-disclosed, low-weight framing exactly ("narrow, single-sub-component gap," "does not affect current correctness," the real T-1036 repair independently confirmed sound). Self-verification-only; irrelevant once this one-time batch task closes. |
| 2 | scrum-master.md/T-1027/T-1037 task-ID matchable-if-read trust concern (adversarial: major, framed as a trust-boundary issue) | adversarial: major | minor | Real but low-impact: the property this framing worries about (a foreign merge subject spoofing a task ID) affects human-reviewed retro-documentation accuracy only, not a security/production boundary or an automated action. DP3's own "never fabricates a match" claim is about agent behavior and holds; the residual concern is about input reliability, a precision question rather than a safety one. |
| 3 | T-1029 AC2 ordering-anchor lacks a uniqueness guard (adversarial: major) | adversarial: major | minor | Real gap, confirmed by reading the live check line, but low practical exploitability: the target file (`agents/pm-spec.md`'s self-check section) is static and code-reviewed, unlike the board's structurally-recurring per-task entries that made the analogous T-1036 AC9 gap a realistic, worth-decoy-testing scenario. |

#### Recommendation
- APPROVE → set `.shell-team/todo.md` to `READY_FOR_MERGE`

#### Fast-follow disposition
- T-1037 AC3 forbidden-token collision (Major finding 1) — **file-an-issue**: a pm-spec re-freeze rewording `X1`'s parenthetical to avoid the literal `git log`, or narrowing AC3's forbidden-token sweep to exclude that one documentation mention. Already flagged by both the engineer's provenance and QA's own risk notes as the recommended next touch; this review confirms the same disposition independently.
- T-1040's own AC9 self-verification gap (finding, downgraded to Minor) — **file-an-issue**: tighten the anchor to a fragment specific to the fix (e.g. the `{c++} END{print c+0}` / `10#$cwork` fragment), mirroring `A36a`–`d`'s shape, per QA's own recommendation.
- T-1017 AC15 / T-1040 AC11 comment-vs-executable-call vacuity gap (Major finding 3) — **file-an-issue**: bind the assertion to the executable call form (`bash "$CHECKER" --task`), excluding comment lines, and add a mutation self-check that deletes the real invocation while preserving the comment. Recommend folding into the `## Non-goals`-recorded issue candidate for an anchor-quality checker, since this is a concrete instance of exactly that class.
- T-1029 AC2 ordering-anchor uniqueness gap (Minor, downgraded from adversarial major) — **file-an-issue**: add a count-of-one guard before the four bullet-head positional lookups, same class as the fold-in above.
- scrum-master.md task-ID matchable-if-read residual trust concern (Minor, downgraded from adversarial major) — **file-an-issue**: consider requiring SHA-level corroboration for a subject-carried task ID, or explicitly document the accepted residual risk at the next natural re-freeze of this text.
- AC15/AC18 `ls -1` dotfile/directory-counting gap (Minor) — **file-an-issue**: same fold-in as above — replace with a hidden-file-aware, regular-file-only enumeration.

<!-- END review: T-1040-codex -->

> Post-close-out editorial note (2026-08-06, orchestrator): two occurrences of a fully-hyphenated four-word compound (task + ID + matchable + if-read) above were re-spaced to the form "task-ID matchable-if-read". The original compound embedded a substring beginning `sk-` followed by 19 token-charset characters, which matches check-pii-shapes RE_TOKEN (an unboundaried `sk-` prefix) and turned CI step 57 red on PR #177 — a measured false positive, filed as a checker-boundary issue. No verdict, finding, or disposition text was changed beyond the two hyphens.
