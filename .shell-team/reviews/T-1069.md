### Codex Review verdict (round 1): APPROVE
- Task: T-1069
- Codex model: gpt-5.6-sol
- Codex CLI: codex-cli 0.145.0
- Verification mode: static-only
- Diff scope: 5 files changed, +667/-0 lines (`336c7835af04382ccb920a332ecd4dc2b2f0ddca...HEAD`; base = `git merge-base feature/1068-agent-concurrency HEAD` = `336c7835af04382ccb920a332ecd4dc2b2f0ddca`)

前提として、本タスクの `### Verdict scope (operator-ratified)` は (1) computed values の正しさ（各figureが出典を明示し、併記されたcommandを再実行して再現すること）と (2) conclusions' direction（tier disposition・dividing-line・measured-label・aggregation-correctness verdict・per-tier recommendationの各方向性が正しいこと）のみをverdict-gatingとし、connective prose（totality wordingの範囲の広さ・labelの不一致・narrative asideの陳腐化・不正確な要約）はNoteとして記録するのみで変更要求を単独では正当化しない、と明記している（`.shell-team/interventions/T-1066.md` の `human-correction` rulingを継承）。Timing figuresはmachine-local one-shot観測であり再実行での再現を要求しないという本レビュー依頼自体の方針にも従った。`## Input space`のOut-of-scope synthetic extremes（高N sweep要求・synthetic benchmark・inferential statistics・Tier-2/3実験・Agent-tool concurrency probe・watchdog再設計・real-repo live trial）に該当するfindingは無かった。

自分で以下を独立に再現・確認した:
- headline 43.84%: `python3 -c "print((297164683500-166883349000)/297164683500*100)"` → `43.841459545444266`（43.85%ではなく43.84%が正しい。QA round 1のFAILとround 1 reworkの修正は正確で、round 2 QAの再確認とも一致）。
- knee drop percentages: n1→n2 ≈33.7412%、n2→n4 ≈13.0810%、n4→n8 ≈2.1965%、n8→n9 ≈0.2981% — noteの「≈33.7%」「≈13.1%」「≈2.2%」「≈0.3%」と一致。
- outlier share: `142965525000/297164683500*100` ≈48.1099% — noteの「≈48.1%」と一致。
- row totals: 76+50+8+1=135 — noteの全アーム共通の内訳と一致。
- sibling note `docs/loop-engineering/agent-concurrency.md` の `- invariant-lock:` は自分の`grep -c`で正確に6件、"seventh"という語は一度も出現せず、自身のrecommendationを "the six-surface `agents`/`skills` contract redesign" と呼んでいる — T-1069のnoteが主張する「sibling noteはsixのみ、seventhはissue #274のみ」というattributionは正確。
- `design-note-T-1012.md`に対して実際に`bash bin/check-acs.sh`を実行 → `exit=2`、`AC<n>:`行ゼロ — noteが主張するsentinel rowの根拠（exit=2, no verdict lines）は実機で確認済み。
- `CHECK_ACS_TIMEOUT=180 bash bin/check-acs.sh .shell-team/specs/T-1069-phase-multiplexing.md` → 10/10 PASS、`bash bin/check-intent.sh` → aligned（`bdba9bb0...`、hash不変）、`bash bin/check-provenance.sh` → conformant（6 decisions, 0 sentinel）、`bash bin/check-interventions.sh --task T-1069` → conformant（0 entries, 1 sentinel）、`bash bin/check-pii-shapes.sh --base 336c7835...` → clean。
- issue #253・#274の本文を`curl https://api.github.com/repos/RipsawJP/shell-team/issues/{253,274}`で直接取得し照合した（`api.github.com`はこのsandboxのネットワーク許可リストに含まれる）。#253本文は3 tier・4 cost axes・「measured pilot of tier 1」「T-1062の133 check-runs, ~45+分」「per-instance discriminator」を含む invariants paragraph まで、specの`## Summarized sources`の要約と完全に一致。#274本文item 7も「`agents/codex-reviewer.md`may commit its own review record when instructed... the note's sixth surface covers only the QA→reviewer direction; this is the reviewer→QA direction, unpriced」まで一言一句一致し、seventh contract surfaceの帰属先としてissue #274のみを挙げるnoteの記述は正確。issue-canonとの一致は環境的に未確認ではなく直接確認済み。

#### Blockers
（なし）

#### Major
（なし — 下記Synthesis audit ledgerの2件はCodexの一次判定ではMajor相当だったが、本ロールの独立検証とadversarial passを経てnon-gatingと判断しMinorへ格下げした）

#### Minor / nits
- `docs/loop-engineering/phase-multiplexing.md:111` — aggregation-correctness（9件の`- aggregation-check: ... — identical — ...`行）は、noteが quoteする`- command:`行が`<merged-arm-rep>.tsv`のようなテンプレート形式（実ファイルパスではない）であるため、記載されたコマンドをそのまま再実行して独立にreproduceすることはできない。ただし: (a) 本レビューはstatic-only（pilotを再実行する手段を持たない）であり、(b) pilotのvenueはDP-C/Non-goalsにより意図的にthrowaway（`$TMPDIR`内、実リポジトリに書き戻さない）に設計されており、(c) `## Input space`のReachable input classesに「Machine-local input that is absent or unreadable ... Criteria read the note's record of the pilot, never the pilot's own environment.」とあり、one-shot pilot artifactへのliteral reproduction要求はこのタスクの設計意図に反する。Timing figuresと同じ「machine-local one-shot observation」のクラスとして扱い、独立に確認できる範囲（135=76+50+8+1の内訳算術、design-note-T-1012.mdのsentinel化根拠の実機再現、5アーム×2rep−1baseline=9のaggregation-check対応数）はすべて整合していたため、Majorではなく Minor とし、変更要求の根拠にはしない。
  - Suggested fix: 将来のpilotではsorted出力のチェックサム（例: `sha256sum`）をnote内に記録するか、`## Limits`に「中間成果物はthrowaway cloneの破棄とともに失われ、再現には同一refでの再実行が必要」と明記すると、この種の検証可能性の懸念に先回りできる。

- `docs/loop-engineering/phase-multiplexing.md:151-156` — Tier-3のbreak-even model（`## Tier-3 feasibility and break-even model (paper only)`）は、4つの`- break-even:`行（`engineer-phase-duration`, `partition-overhead`, `reconcile-cost`, `rework-amplification-input`）を宣言しているが、唯一の`- break-even-condition:`行の不等式（N engineers' own combined implement-phase wall clock + reconcile-cost < single-engineer serial wall clock）には`partition-overhead`と`rework-amplification-input`の項が現れない。にもかかわらず同じ文が「この条件は今日数値評価できない、なぜなら`partition-overhead`が`undetermined`だから」と述べており、不等式に現れない変数を評価不能の理由として名指ししている点は内部的に不整合。自分の独立読解、およびadversarial pass（Codexに「これはchecklist的な前提列挙にすぎず不等式の項である必要はない、というcharitable readingは成立するか」を明示的に問うた）の両方が独立に**"HOLDS UP"**（内部不整合は実在する）と判定した。ただし: (1) この不整合はTier-3のrecommendation（`not-yet`、未構築のreconcile機構を理由とする）の方向を変えない。(2) AC6はbreak-even-conditionの内容を構造（非空・grammar一致）でのみ検査しており、パラメータの網羅性までは要求していない。adversarial passも「computed valueもconclusion's direction（Tier-3の`not-yet`）も動かないため、このfinding単独では変更要求をgateすべきでない」と明言した。したがって、モデルの厳密性としては直すべきreal defectだが、Major/Blockerとしてgatingするのではなく、修正推奨のMinorとして記録する。
  - Suggested fix: break-even-conditionの不等式に`partition-overhead`と`rework-amplification-input`を明示的な項として組み込む（例: `partition-overhead + max(worker durations) + reconcile-cost + incremental-rework-cost < serial engineer duration`）か、あるいは「4つの`- break-even:`行はこの条件のevaluationに必要な前提のチェックリストであり、不等式の直接の項ではない」という位置づけを本文で明示する。

- `.shell-team/specs/T-1069-phase-multiplexing.md` AC2（spec quality、現行deliverable自体は正しいが将来のdriftに脆弱）— `- tier:`行のgrammar正規表現 `^- tier: [a-z0-9-]+ — (mechanical|design-judgment) — (S|M|L) — .` はtier idを汎用パターン`[a-z0-9-]+`でマッチしており、AC3/AC5/AC7が採用する「closed alternationで名前を列挙する」方式になっていない。仮に4件目の`- tier: tier4-foo — mechanical — S — ...`と対応する`- saving: tier4-foo — ...`が追加されても、3つの named tierのexactly-1チェックと集合の等価性チェックは通ってしまい、DP-B/Goalが謳う「3 tierにclosed」はchecker レベルでは強制されていない。現行noteには実際に3 tierしか無いことを自分で確認済みであり、現行deliverableの正しさには影響しない。
  - Suggested fix: AC3のように`(tier1-verification-fanout|tier2-parallel-implementations-judge|tier3-work-splitting)`のclosed alternationをgrammar側にも使う。ただしこのspecは既にfrozenであり、修正にはclass-B/M re-freezeの手続きが必要になる点に注意。

- `.shell-team/specs/T-1069-phase-multiplexing.md` AC5 — `- aggregation-check:`の総数チェックは`test "$GT" -ge 1`のみで、実際のpilot-arm数（sweepの非baseline arm/rep数）と突き合わせていない。理論上、1行のfabricatedな`- aggregation-check: x — identical — ...`だけでもAC5は通る。現行noteは実際に9件の正しい`- aggregation-check:`行を持ち、5アーム×2rep−1baseline=9と正確に対応していることを自分で確認済み。
  - Suggested fix: `- pilot-arm:`の総数から期待される非baseline (n,rep)の集合を導出し、`- aggregation-check:`の集合と比較する。

- `.shell-team/specs/T-1069-phase-multiplexing.md` AC6 — `- break-even-condition:`は非空文字列であることしか要求しておらず、上記のTier-3 findingのような内部不整合を検出できない。
  - Suggested fix: 「breaks even if」を含む、または宣言済みの`- break-even:`パラメータIDのうち一定数が本文に出現する、といった軽い構造要件を追加するか、この種の完全性判断は明示的にreading gate（Codex review）の管轄とBody-to-AC correspondence tableに記載する。

#### Connective-prose notes（non-gating、`### Verdict scope`により変更要求の根拠にはならない）
- `docs/loop-engineering/phase-multiplexing.md:79`付近 — pilotのpopulationを一貫して「spec population」/「9 specs」と呼んでいるが、実際には8個のspecと1個のdesign note（`design-note-T-1012.md`）の混合である。note自身がこの事実を明記し、sentinel rowの根拠としても正しく扱っているため、legitimacyへの影響はない。
- `docs/loop-engineering/phase-multiplexing.md:90`付近 — 「selected once, before any arm ran」という記述は、放棄されたstride-5選択が実際にはserial runを開始していた事実（`## Limits`で開示済み）と厳密には整合しない。ただしDP-Cの「verdictを見てからの再選択の禁止」には抵触せず、放棄はwall-clock budgetの判断であったことがLimits節で明記されている。

#### Adversarial findings
Major相当だった2件のうち、Tier-3 break-even modelの内部不整合についてCodexにdevil's advocate（「これはchecklist的な前提列挙にすぎず不等式の項である必要はない、というcharitable readingは成立するか」）を当てた。結果は独立に**HOLDS UP**（内部不整合は実在する — DP-Eの"honest symbolic form"の趣旨には反するが、AC6は構造のみを検査するためAC6違反ではない）、かつ**「このfinding単独では変更要求をgateすべきではない」**（computed valueもconclusion's directionのTier-3 `not-yet`も変えないため）という一致した結論を得た。aggregation reproducibilityについては、adversarial passではなく自分自身の独立検証（135行内訳の算術、design-note-T-1012.mdの実機再現、pilot-arm×aggregation-checkの対応数）でnon-gatingと判断した。

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | aggregation-correctnessの9件の`identical`検証が、note自身のquoted command（テンプレート形式、実ファイルパスではない）からはliteralに再現できない（`phase-multiplexing.md:111`） | Codex primary: Major（verdict-gating: computed value / aggregation conclusion） | minor | pilotのvenueはDP-C/Non-goalsにより意図的にthrowaway（`$TMPDIR`内、実リポジトリに書き戻さない）であり、`## Input space`の「Criteria read the note's record of the pilot, never the pilot's own environment」の原則が、one-shot pilot artifactへのliteral reproduction要求は妥当でないことを裏付ける。timing figuresと同じ「machine-local one-shot observation」のクラスとして扱い、内部整合性（135=76+50+8+1、design-note-T-1012.mdの実機再現、pilot-arm×aggregation-checkの対応数）を自分で独立確認した上でMinorへ格下げした。 |
| 2 | Tier-3 break-even-conditionの不等式が`partition-overhead`と`rework-amplification-input`を含まないにもかかわらず、`partition-overhead`のundeterminedを評価不能の理由として名指ししている内部不整合（`phase-multiplexing.md:149-156`） | Codex primary: Major（verdict-gating: conclusion direction）／adversarial: 独立に"HOLDS UP"（内部不整合は実在）、かつ「このfinding単独では変更要求をgateすべきでない」と明言 | minor | adversarial passの結論どおり、この不整合はTier-3の`not-yet`というconclusion's directionを変えず、AC6は構造のみを検査するためcomputed valueの再現性にも該当しない。`### Verdict scope`の2バケット（computed value / conclusion's direction）のいずれにも直接は該当しないという判断で、real defectとして記録しつつMinorへ格下げした。 |

上記以外のfindings（AC2/AC5/AC6のspec-quality gap 3件、connective-prose notes 2件）はCodexのnativeな判定のままMinor/Noteとして採用しており、格上げ・格下げはない。Input spaceの「Out-of-scope synthetic extremes」に照らしても、今回のfindingsはすべてnote自身がreachableと宣言したinput class（note本文の実際のprose、frozen specの実際のcheck行、既存の`bin/check-acs.sh`の挙動、sibling noteの実際のprose）に基づくものであり、`out-of-input-space`格下げの対象になるものは無い。

#### Recommendation
- APPROVE → `.shell-team/todo.md`のT-1069エントリを`READY_FOR_MERGE`に設定する。Blocker/Majorは無く、Minor 5件・Note 2件はいずれも`### Verdict scope`の定めるcomputed value / conclusion's directionのいずれにも該当しないか、変更後もdirectionが動かないことを独立に確認済み。

#### Fast-follow disposition
- `docs/loop-engineering/phase-multiplexing.md:149-156`（Tier-3 break-even-conditionの内部不整合） — file-an-issue相当。理由: モデルの厳密性を将来直す価値はあるが（`- verification-class: no-mechanism`のため軽微な文言修正で足りるはず）、computed valueもconclusion's directionも動かさないためmergeを止める重大度ではなく、今回のroundで解消を必須にはしない。
- `.shell-team/specs/T-1069-phase-multiplexing.md` AC2/AC5/AC6のspec-quality gap 3件 — won't-fix相当。理由: このspecは既にfrozenであり、修正にはclass-B/M re-freeze手続きが必要になる。現行deliverable自体は正しい（3 tierのみ、9件の正しいaggregation-check、無矛盾な構造）ため緊急性は低い。将来同種のspecを書くpm-spec側の教訓としての記録価値はあるが、本タスクのre-freezeを要求するほどではない。
- `docs/loop-engineering/phase-multiplexing.md:79`（population labeling: "spec"にdesign noteが1件混在） — won't-fix。理由: note自身が既に開示済みであり、legitimacyに実害はない。
- `docs/loop-engineering/phase-multiplexing.md:90`（"selected once, before any arm ran"の精度） — won't-fix。理由: Limits節で放棄されたstride-5選択の経緯が既に開示されており、DP-Cの核心的な禁止事項（verdictを見てからの再選択）には抵触しない。
