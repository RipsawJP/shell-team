# T-1077 QA verification record

## Round 1

### QA verdict: PASS

- Task: T-1077 → READY_FOR_REVIEW
- Spec: `.shell-team/specs/T-1077-worktree-reconcile.md`, frozen intent v1 `574f48d9393d7377a59487b44f07640dadc5d103`
- Engineer commits verified: `582c1a4`, `b8cb535`, `525a003` (branch point `1f166329a200f33e916ea89e857a9b1a887648a1`)

#### Mandatory gates

- `bash bin/check-intent.sh .shell-team/specs/T-1077-worktree-reconcile.md .shell-team/todo.md` → `aligned: T-1077 v1 (574f48d9393d7377a59487b44f07640dadc5d103) matches ...` — re-run myself, hash unchanged. Command: `bash bin/check-intent.sh .shell-team/specs/T-1077-worktree-reconcile.md .shell-team/todo.md`.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md` → **14 passed, 0 failed, 0 skipped, 0 unrecognized** — re-run myself (own command, exact: `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md 2>&1 | tail -5`), matches the engineer's own claim.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1077.md` → conformant (7 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1077 -- .shell-team/interventions/T-1077.md` → conformant (2 entries, 0 sentinel).
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 1f166329a200f33e916ea89e857a9b1a887648a1` → exit 0.
- `bash bin/check-pii-shapes.sh --base 1f166329a200f33e916ea89e857a9b1a887648a1` → exit 0.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.

#### Test suite

- `bash tests/land-worktree/run.sh` (own timed run, `date +%s` bracket): elapsed **16s**, `grep -c '^PASS:' <log>` = **34**, `grep -c '^FAIL:' <log>` = **0**. Matches the recorded suite composition (workers=38, mutation-cases=11, refusal-classes=19 in the suite's own `landing-parameters` line) and the negative control's own `detected` verdict (re-observed directly in my own run's output, not taken from the hand-off).
- AC10's six regression suites, each run individually: `tests/check-prompt-sync/run.sh` rc=0, `tests/machine-tokens/run.sh` rc=0, `tests/errexit-safe/run.sh` rc=0, `tests/check-handoff/run.sh` rc=0, `tests/log-run/run.sh` rc=0 (37 PASS / 0 FAIL, `grep -c '^FAIL:' <log>` = 0), `tests/aggregate-verdicts/run.sh` rc=0. `bash bin/check-prompt-sync.sh` rc=0.
- `shellcheck bin/land-worktree.sh tests/land-worktree/run.sh` → clean, with `shellcheck --version` confirmed **0.11.0**, matching `.github/workflows/check-handoff.yml`'s pinned `SHELLCHECK_VERSION: "0.11.0"` (`grep -n SHELLCHECK_VERSION .github/workflows/check-handoff.yml`).
- `bash tests/bin-exec-bit/run.sh` → rc=0.
- CI wiring confirmed directly: `.github/workflows/check-handoff.yml` names `bin/land-worktree.sh` and `tests/land-worktree/run.sh` in the single shellcheck argument list (line 29) and carries `run: bash tests/land-worktree/run.sh` as its own step (lines 279-280).

#### Acceptance criteria — all 14 checked off

AC1-AC14 all confirmed via the live `check-acs.sh` run above (14/14). Beyond the mechanical check, I independently exercised the mechanism with my own fixtures (not the suite's), in throwaway `git init` scratch repos under `$TMPDIR` (never touching `bin/`/`tests/` in the real checkout):

- **Clean 3-worker disjoint landing** (AC2's `clean-landing-*` family): built three independent workers (f1.txt/f2.sh/f3link, including a symlink and an executable-bit change) from a shared base with my own plumbing-built commits, landed all three in sequence. Confirmed the composed tree equals the union of base + all three workers' claims (`git ls-tree onto`), confirmed each landed commit carries exactly two parents in coordinator-tip-first/worker-tip-second order (`git show -s --format='%P'` at each step), confirmed all three worker commit SHAs remain reachable and unrewritten.
- **Overlap, all three D7 relations, independently constructed**:
  - Exact: two workers both touching `shared.txt` → `overlap: ... collides (exact) ...`, exit 1, empty stdout.
  - Directory-prefix, both directions: an early worker landing `x` as a file then a late worker adding `x/y` → refused (`directory-prefix`); and the reverse (early lands `a/b`, late claims plain `a`) → refused (`directory-prefix`) too.
  - ASCII case-fold: `README.txt` vs `readme.txt` → refused (`ASCII-case-fold`).
  - Rename claiming both paths: after correcting my own first fixture-construction mistake (base needs to actually carry the renamed-from path — I first built a base lacking `orig.txt`, which produced a false pass; redone with `orig.txt` genuinely present at base), a worker deleting `orig.txt` and adding `newname.txt` correctly collides on `orig.txt` against an early worker that touched it — confirmed via `git diff --name-status` on the late worker (shows both `D orig.txt` and `A newname.txt`) before confirming the refusal.
- **Adversarial paths**: a path with an embedded space lands cleanly with the record's path line C-escaped (`- path: has\ space.txt`); a path with an embedded newline lands cleanly (record still one line per path, confirmed by the suite's own `landing-record-path-escaping` token and my own run showing the escaped line count).
- **A vocabulary-colliding filename** (a worker path literally named `overlap`, not a synthetic extreme but a plausible ordinary filename): lands cleanly the first time and is correctly refused as `overlap` (citing the real collision reason, not confused by the name) the second time a colliding worker touches the same path.
- **Diverged worker** (a worker whose history shares no commit with the declared base at all — an orphan tree via `commit-tree` with no parent, simulating a mid-flight rebase/reset): refused `diverged`, exit 1, empty stdout.
- **empty-landing**: worker tip == base → refused `empty-landing`, exit 1, empty stdout.
- **ref-moved**: raced the coordinator's own read-of-tip against an out-of-band `update-ref` using the tool's own disclosed `TEAM_LAND_WORKTREE_TESTHOOK_DIR` rendezvous seam (the only practical way to hit this sub-second window deterministically; a wall-clock race is not reliable enough to trust as my own independent evidence, so I used the same disclosed mechanism the suite uses) → refused `ref-moved`, exit 1, empty stdout, and the ref ended up exactly at the externally-written value (no clobber).
- **--onto checked out**: tested against a genuinely separate second worktree (not the suite's own "sole worktree" shape) → refused `structural`, exit 2, empty stdout.
- **--worker-tree not a worktree of this repository / structural preconditions**: unresolvable `--worker` ref → `structural`, exit 2; no arguments → `usage`, exit 2, exactly one `land-worktree: usage: ` stderr line, empty stdout; unknown flag (`--bogus-flag`) → `usage`, exit 2; missing value for `--base` → `usage`, exit 2.
- **Lock timeout / never-steal, mutation-proved**: with a foreign (pre-existing, not-mine) lock directory at the exact computed path, the real tool refuses within the bounded wait (`TEAM_LAND_LOCK_TIMEOUT=2` → refused at ~2s, exit 3, class `lock`, lock left untouched). Mutation-proved this is load-bearing: a scratch copy under `$TMPDIR` with `mkdir "$LOCK_DIR"` changed to `mkdir -p "$LOCK_DIR"` **steals** the foreign lock and lands (rc=0) where the real, unmutated tool refuses (rc=3) — confirms the plain-`mkdir`-never-`mkdir -p` guard is genuinely load-bearing, not vacuous.
- **Disjointness check, mutation-proved independently of the suite's own AC3 negative control**: a scratch copy with the single `die 1 overlap` call neutered wrongly lands a pair of workers that collide exactly on `shared.txt` (rc=0 both times) where the real tool refuses the second — confirms the check is load-bearing. (The suite's own AC3 negative control, re-run live by me, independently reports the same `detected` verdict.)
- **`TEAM_LAND_LOCK_TIMEOUT` invalid-value handling**: `-5`, `abc`, `0` each print `land-worktree: ignoring invalid TEAM_LAND_LOCK_TIMEOUT=<v>, using default 10` and fall back correctly. **Finding, not a FAIL**: an *absent/unset* value (the ordinary default path every non-overriding invocation takes) does **not** print this warning — `${TEAM_LAND_LOCK_TIMEOUT:-10}`'s own bash semantics substitute the default before the warn-eligible case statement ever sees an empty string, so the "absent" branch in that case statement is effectively dead code. D3's frozen prose reads "an absent, non-integer, zero or negative value falls back to the default and warns on stderr naming the variable," which taken literally also expects a warning on "absent." No `- check:` line in this spec asserts this specific claim (AC2's `lock-timeout-refusal` case only exercises a *valid* override, `TEAM_LAND_LOCK_TIMEOUT=1`, against a pre-existing lock — it never exercises an invalid value at all), so this is a prose/behavior mismatch on an uncovered claim, not a criterion violation. The shipped behavior (silent default on the common, non-overriding path) is also the objectively better UX choice — printing a warning on every ordinary invocation that never touches this variable would be noise. I record this for the reviewer/editorial pass rather than kicking back on it.
- **macOS `/tmp` vs `/private/tmp` symlink fix (provenance decision #4), reproduced**: confirmed `git rev-parse --git-common-dir`, invoked with cwd reached through a symlinked path (I built my own symlink under scratch to reproduce this without writing to the real `/tmp`, since sandbox policy restricts direct `/tmp` writes to the session scratch area), returns a **relative** path (`.git`) — `cd .git && pwd` (logical) resolves through the *same* symlinked ancestor, while `cd .git && pwd -P` (physical) resolves to the canonical form a linked worktree's own `git worktree add`-recorded common-dir already uses. Reproduced the fix directly: invoked the coordinator with its own cwd reached via the symlink and `--worker-tree` given as the worktree's canonical physical path — lands cleanly (rc=0), confirming the shipped `pwd -P` unification is what makes this succeed (the bug this decision fixes would otherwise false-refuse this ordinary case with `--worker-tree is not a worktree of this repository`).
- **`TEAM_LAND_WORKTREE_TESTHOOK_DIR`, judged per this task's own explicit request**: confirmed genuinely inert across dozens of ordinary (unset) invocations throughout my probing — no observable difference in behavior, timing or output. When set to a valid, existing directory, it only delays the point right before the compare-and-swap (bounded internally to ~30s, then proceeds regardless) — it sits **after** every content/structural/diverged/overlap check has already resolved, so it structurally cannot bypass any refusal, and it runs no caller-supplied command (only a file-existence poll and an empty marker write), so it does not create a new execution surface within the Non-goals' meaning. **Finding, minor, not a FAIL under this task's own stated bar**: pointing it at a *nonexistent* directory (a plausible misconfiguration, e.g. a stale leftover value from an earlier test session) crashes the script via an uncaught `errexit` failure on the marker-file write — the process exits 1 with a raw bash line-number message (`bin/land-worktree.sh: line 562: .../ready: No such file or directory`), not the closed `land-worktree: <class>: <detail>` vocabulary D6 promises. This does not authorize a landing (the coordinator ref is never advanced — the crash happens before the compare-and-swap) and does not bypass any refusal (every substantive check already passed), so per this task's own explicit bar ("if it can alter verdicts or execute anything, that is a FAIL finding; if genuinely inert synchronization-only, report your evidence") this does not cross into FAIL territory — the variable "has no shipped caller, no acceptance criterion and no normal operation ever sets it" per its own header comment, so this is disclosed for the record rather than kicked back.

#### Blast radius — spot-checked

Independently re-ran 3 of the 30 disclosed-unswept specs at both refs (base: the pre-existing `git worktree add --detach` scratch tree at `1f166329a200f33e916ea89e857a9b1a887648a1` under this session's scratchpad, reused rather than recreated; head: this checkout) — own commands, each run individually:
- `T-1074-fanout-orchestration.md`: base **17 passed, 2 failed** (`AC14 AC18`), head **17 passed, 2 failed** (`AC14 AC18`) — identical set, no new flip.
- `T-1052-records-editorial.md`: base **9 passed, 6 failed, 1 skipped** (`AC9 AC10 AC11 AC12 AC14 AC15`), head identical — no new flip.
- `T-1071-record-set-derivation.md`: base **12 passed, 2 failed, 1 skipped** (`AC8 AC13`), head identical — no new flip.

All three match the spec's own disclosed "reasoned expectation, not a measurement." The remaining 27/30 were **not** independently re-verified by me either — this task's own disclosed limit stands; I am not extending the sweep further given the time budget and per-spec suite costs several of these specs carry (some invoke whole nested fixture suites inside their own `- check:` lines).

- `T-1001-retro-input-acquisition.md` **AC29** transient-flip claim: re-ran the whole spec at HEAD in isolation (no concurrent sweep tooling running) — `CHECK_ACS_TIMEOUT=120 bash bin/check-acs.sh .shell-team/specs/T-1001-retro-input-acquisition.md` → 18 passed, 12 failed (`AC2 AC3 AC6 AC10 AC13 AC14 AC16 AC18 AC21 AC23 AC25 AC26`) — **AC29 is absent from the FAILED set**, i.e. it passes in isolation, confirming the engineer's own re-measurement (the "moved" reading was their sweep tooling transiently mutating the shared working tree it was reading, not a genuine regression).

#### Regressions in adjacent code paths

- No existing `bin/` file changed except the new `bin/land-worktree.sh` — confirmed via AC12's own byte-identity check (all 14 named preservation paths matched their branch-point blobs) plus my own spot-checks above.
- `skills/run/SKILL.md`'s T-055 rewrite preserves the two unrelated instructions in the same bullet (non-worktree-by-default, isolation controlled by the Agent invocation parameter) — confirmed by reading the current text directly.
- `bash bin/check-prompt-sync.sh` passes, confirming the new `reconcile-step` marker region does not collide with any registered `contain` block's span.

#### Edge cases tried beyond the spec's own enumeration

- A vocabulary-colliding filename (`overlap`) — see above; ordinary, real-data-reachable, not a synthetic extreme.
- Bare-name `PATH` invocation (`land-worktree.sh --help`, with `bin/` on `PATH`) and direct `./land-worktree.sh --help` execution (exec bit) — both work, confirming the launch-shape lesson is respected.
- `TEAM_LAND_LOCK_TIMEOUT` unset entirely (not merely empty) — silent default, no warning, as expected for the ordinary path.
- Two directions of the directory-prefix overlap relation (not just the one the suite names) — both refuse correctly.

#### Not independently verified (environmental)

- `environmentally-unverified: class 1 / host-portability` — "hosts without `flock`, and hosts whose `sleep` rejects a fractional argument" (Input space class 11) was not independently re-measured by me; I ran everything on the same macOS/bash-3.2 host the engineer used. The Assumptions section's own freeze-time measurement (`flock` absent, fractional `sleep` works) is the only measurement on record for this host, and no other host is available in this session.
- The full 30-spec blast-radius sweep tail (27 of 30 unswept specs) — not independently completed by me either; same disclosed limit as the hand-off, not extended.

#### Risk notes for reviewer

- Two disclosed-but-narrow findings above (`TEAM_LAND_LOCK_TIMEOUT`'s "absent" case not warning contrary to D3's literal prose; `TEAM_LAND_WORKTREE_TESTHOOK_DIR` crashing uncleanly on a nonexistent directory) — neither is AC-covered, neither alters a verdict or bypasses a refusal, and both are candidates for a batched editorial/robustness pass rather than blocking rework.
- `out-of-input-space`: I did not attempt to reproduce the Input space's declared out-of-scope synthetic extremes (non-ASCII case-folding collisions, cross-host/NFS concurrency, adversarial lock tampering, corrupt-repo/old-git, crash consistency, submodules/LFS) — these are explicitly declined by the spec and none of my exploratory edge cases relied on them.
- Working tree hygiene: `git status --short` clean throughout and at the end; `git worktree list` shows only the main checkout plus three pre-existing entries from prior sessions (`t1077-base-worktree`, `t1061-blast-worktree`, `t1070-base`) that I neither created nor removed (none are mine — all my own fixture repos were separate `git init` scratch trees under this session's scratchpad, never `git worktree add` against the real checkout).

### QA verdict: PASS

- Task: T-1077 → READY_FOR_REVIEW
- Tests: 34 passed, 0 failed (`tests/land-worktree/run.sh`, own command: `grep -c '^PASS:' <log>` = 34, `grep -c '^FAIL:' <log>` = 0); AC10's six regression suites all exit 0; `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md 2>&1 | tail -1` → 14 passed, 0 failed, 0 skipped, 0 unrecognized
- Acceptance criteria: 14/14 checked off (own re-run of `bin/check-acs.sh`, matching the engineer's claim)
- Edge cases tried: see above (vocabulary-collision filename, bare-name/`./` launch shapes, both directions of directory-prefix overlap, unset `TEAM_LAND_LOCK_TIMEOUT`, mutation-proving disjointness and lock never-steal, macOS symlink reproduction, testhook inertness/misuse probing)
- Risk notes for reviewer: see "Risk notes for reviewer" above — two narrow, uncovered, non-blocking findings disclosed for an editorial pass; blast-radius sweep spot-checked (3/30) rather than completed (same disclosed limit as the hand-off); host-portability input class not independently re-measured (environmentally-unverified)

## Round 1 (codex)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1077
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: 12 files, +1872/-2 lines (`1f166329a200f33e916ea89e857a9b1a887648a1`..`c3d8016`)

#### Blockers

- `bin/land-worktree.sh:519-538` — The composition loop resolves each worker-claimed path with `git ls-tree -z "$W" -- ":(literal)$p"` and then treats whatever it gets back as a blob/symlink cache entry (`git update-index --add --cacheinfo <mode> <sha> <path>`) without checking the entry's *type*. Input space item 5 ("A directory replaced by a file, or a file replaced by a directory") is declared a **reachable** input class this mechanism must handle correctly (not an out-of-scope synthetic extreme), and D5 promises the composed tree equals the coordinator tip's tree with exactly the worker's claimed paths substituted. I independently reproduced both directions in scratch git repos (not merely relaying Codex's report):
  - **file→directory** (base has file `x`; worker replaces it with directory `x/y`): `ls-tree` returns `040000 tree <sha>  x` for the claimed path `x`; the code blindly stages mode `040000` via `--cacheinfo` as if it were a blob. This **lands successfully (exit 0)** but the resulting tree is corrupted — `git ls-tree -r onto` shows both the correct `x/y` entry *and* a bogus extra entry literally named `xy` (no slash) at the same blob SHA, alongside a `warning: index entry is a directory, but not sparse` on stderr. This is **silent data corruption behind a green exit code**, not a refusal — it directly contradicts `skills/run/SKILL.md`'s own frozen `- invariant-lock: fail-closed-checkers — implemented — ... refuses ... rather than ever landing a partial, colliding or diverged result` line (SKILL.md:186), which this input class falsifies.
  - **directory→file** (the reverse): the composition step fails with `git update-index`'s own `'x' appears as both a file and as a directory` / `cannot add to the index` error, surfacing as `land-worktree: structural: failed to stage a changed path into the scratch index: x` (exit 2). At least fail-closed here, but misclassified — this is a content/composition defect (D6's exit-1 territory), not an invocation/usage problem (D6's exit-2 territory), and it wrongly consumes the `structural` class D6 reserves for input that "is not there or not usable."
  - AC2's `overlap-dir-file-replacement` token does **not** cover either direction: reading `tests/land-worktree/run.sh:372` and its `run_overlap_case` helper (lines 335-362) confirms that fixture only exercises **cross-worker overlap detection** (an early worker lands `x` cleanly; a late worker's own diff touches `x/y`, refused as `overlap` via the directory-prefix relation *before* composition is ever reached) — never a single worker's own directory↔file replacement composing with nothing else already landed, which is the path this bug lives in. Repro:
    ```
    # file -> directory (base: x is a file; worker: x becomes a directory containing x/y)
    git init -q repo && cd repo && git config user.email a@b.c && git config user.name a
    printf 'file content\n' > x && git add x && git commit -q -m base
    BASE=$(git rev-parse HEAD); git branch onto "$BASE"
    git rm -q x; mkdir x; printf 'nested\n' > x/y; git add x/y; git commit -q -m worker
    WORKER=$(git rev-parse HEAD)
    bash bin/land-worktree.sh --base "$BASE" --onto onto --worker "$WORKER"   # exit 0, but `git ls-tree -r onto` shows a corrupt extra "xy" entry
    ```
  - Suggested fix: type-check the `ls-tree` entry before staging (reject/handle `tree` entries explicitly — e.g. recursively expand a directory-becomes-file or file-becomes-directory replacement by first force-removing every existing path under the old prefix, or refuse the shape explicitly with a classified `overlap`/new class rather than silently mis-staging it), and add a same-worker, no-cross-worker-collision fixture for both replacement directions to `tests/land-worktree/run.sh`.

#### Major

- `bin/land-worktree.sh:254-266` vs `370-397` — TOCTOU: the `--onto` checked-out-in-a-worktree check (D5's "`--onto` checked out in any worktree of the repository is refused") runs entirely **before** the lock is acquired (the lock loop starts at line 370, and the coordinator tip `T` is read fresh only at line 397, inside the critical section). If this invocation must wait behind another coordinator holding the lock (up to `TEAM_LAND_LOCK_TIMEOUT`, default 10s), a worktree can check out `--onto` during that exact wait window, and the check is never repeated once the lock is held. The subsequent `update-ref` compare-and-swap then advances a now-checked-out branch, leaving that worktree's own index and working tree silently disagreeing with it — precisely the hazard D5 names and Non-goals declares out of bounds ("no modification of any worktree ... other than the coordinator's own `--onto` ref"). Confirmed by both Codex passes independently (primary P1, adversarial "major", with a concrete repro: hold the lock, start the coordinator, `git worktree add` + checkout `--onto` during the wait, release the lock). Fix: repeat the `git worktree list --porcelain` check inside the critical section, immediately before reading `T`.

#### Minor / nits

- `bin/land-worktree.sh:301-307` vs `370-397` — Same TOCTOU class, narrower blast radius: the optional `--worker-tree` cleanliness check also runs pre-lock. A worker tree that is clean at check time but becomes dirty (or gains an untracked-non-ignored file) during the lock wait is landed anyway, with `- worker-tree: <path>` recorded rather than `not-checked` — silently contradicting D4's dirty-worktree refusal for that landing. Confined to the worker's own disclosure accuracy (no other worktree or path is touched), which is why I rate this below the `--onto` race above rather than at the same level.
- `bin/land-worktree.sh:422,432` — `WFOLD`/`LFOLD` are derived via `printf '%s' "$p" | tr 'A-Z' 'a-z'` inside a `$(...)` command substitution, which strips trailing newline bytes per bash's own substitution semantics. A claimed path legitimately ending in a newline (Input space item 2 — reachable, not synthetic) is folded to a shortened string; a distinct already-landed path whose fold coincidentally equals that shortened form is then **falsely reported as an ASCII-case-fold overlap**, refusing a genuinely disjoint landing. Fails closed/safe (a false refusal, not a false acceptance) but is a genuine bug on a declared-reachable input class; `overlap-path-with-newline` (`tests/land-worktree/run.sh`) only exercises the exact-match path (both workers touch the identical newline-carrying path), not this fold-truncation case. Fix: preserve a trailing sentinel through the substitution (append then strip a non-newline marker byte) rather than relying on `$(...)`'s raw output.
- `bin/land-worktree.sh:251` vs `360` — `WORKDIR` (the per-invocation scratch dir under `$TMPDIR`) is created at line 251, but its `EXIT`-trap cleanup is not installed until line 360. Every pre-lock refusal (`--onto` checked out, worker-tree dirty, diverged, empty-landing, and any earlier `structural`) exits without ever removing it, leaking one `land-worktree.XXXXXX` directory per refused invocation. Low-impact (small scratch files, host-local) but a real, unbounded-over-time leak; move the trap installation to immediately after the `mktemp -d` succeeds.
- `bin/land-worktree.sh:321-333` vs D3's frozen intent-block prose — I read D3's own frozen literal directly (not a paraphrase): "an absent, non-integer, zero or negative value falls back to the default and warns on stderr naming the variable." The shipped behavior does **not** warn when `TEAM_LAND_LOCK_TIMEOUT` is simply unset (the ordinary, non-overriding path every default invocation takes) — bash's own `${TEAM_LAND_LOCK_TIMEOUT:-10}` substitutes the default before the `case` statement's "invalid" arm ever observes an empty string, so the "absent" branch of that case is dead code. This is a genuine contradiction of a **frozen literal**, not merely its paraphrase, though QA's own assessment that the shipped behavior (silent default on the common path) is arguably the better UX stands — no ordinary invocation is harmed, and no `- check:` line asserts this specific claim. Concur with QA: non-blocking, but a fast-follow should reconcile code and frozen prose (either emit the warning on "absent" too, or amend D3's prose to say "explicitly-invalid" rather than "absent").
- `TEAM_LAND_WORKTREE_TESTHOOK_DIR` crashing uncleanly (raw bash line-number error, not a D6-classified exit) when pointed at a nonexistent directory — concur with QA's own disposition in full: inert on every ordinary (unset) invocation, gated behind an env var with no shipped caller and no acceptance criterion, runs no caller-supplied command, and the crash occurs strictly before the compare-and-swap, so it authorizes nothing and bypasses no refusal. Does not, on its own, violate the Non-goals' "No execution surface in the coordinator" clause. Non-blocking; worth hardening in a fast-follow (guard the directory's existence before writing the marker, or fail with a classified refusal instead of an uncaught bash error).

#### Adversarial findings

- Codex's own adversarial pass (framed as "what could break this?" against the composition loop, the two pre-lock checks, and the fold derivation) independently reproduced the same four issues as the primary pass's own read: the untyped `ls-tree`-entry composition bug (called "major" there — my own live reproduction of the file→directory direction upgrades this to Blocker, see above, since that direction silently corrupts the tree under exit 0 rather than merely failing, which the adversarial pass's text described only as "the landing fails instead of composing the tree" — that description holds for the directory→file direction but not its mirror), the `--onto`-checked-out TOCTOU race (major), the worker-tree-dirty TOCTOU race (minor), and the WFOLD/LFOLD trailing-newline fold bug (minor). No new class of failure surfaced beyond what the primary pass and my own reproduction already found — the adversarial framing corroborated rather than expanded the finding set.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | untyped `ls-tree` entry in composition (directory↔file replacement) | primary: P1; adversarial: major (described as "landing fails" in both directions) | blocker | Live reproduction (mine, not relayed) shows the file→directory direction actually **lands with exit 0 and a corrupted tree** (a bogus extra `xy` entry), not a refusal — silent data corruption on a declared-reachable input class outranks a classified refusal, and it falsifies SKILL.md's own frozen `fail-closed-checkers — implemented` line. |
| 2 | `--onto` checked-out TOCTOU (pre-lock) | primary: P1; adversarial: major | major | Confirmed by static reading (lock loop at 370, `T` read at 397, no re-check) — kept at major, not escalated to blocker, because it requires genuine lock contention (another coordinator actually holding the lock) as a precondition, unlike finding 1 which fires on an ordinary, uncontended single-worker landing. |
| 3 | worker-tree dirty TOCTOU (pre-lock) | primary: P2; adversarial: minor | minor | Adopted adversarial's rating over primary's P2: blast radius is confined to the worker's own disclosure accuracy (no other worktree, index or branch is touched), unlike finding 2. |
| 4 | WFOLD/LFOLD trailing-newline truncation | primary: P2; adversarial: minor | minor | No change — narrow (requires a coincidental truncated-fold collision), fails closed (over-refusal, not under-refusal). |
| 5 | WORKDIR EXIT-trap installed late | primary: P2; adversarial: minor | minor | No change — real but low-impact resource leak. |
| 6 | `TEAM_LAND_LOCK_TIMEOUT` absent case not warning | QA: disclosed, "uncovered claim," did not resolve literal-vs-paraphrase | minor (unchanged from QA's implicit non-blocking read) | I read D3's frozen literal directly and confirm it is a **literal** contradiction, not a paraphrase mismatch — this changes the *characterization* QA left open, not the severity; still non-blocking per QA's own (concurred) UX reasoning. |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` back to READY_FOR_ENG with the Blocker (composition of a reachable directory↔file replacement input, both directions) and the Major (`--onto` checked-out TOCTOU race) as the findings to resolve; the Minor items below are candidates for the same rework round but do not block it on their own.

#### Fast-follow disposition

- WFOLD/LFOLD trailing-newline fold-truncation false-positive-overlap bug — file-an-issue: narrow, fails safe, not required to block this round's merge if the engineer chooses to defer it past the Blocker/Major fix, but real and worth tracking independently.
- WORKDIR EXIT-trap-installed-late scratch-dir leak on pre-lock refusals — file-an-issue: low-impact resource leak, straightforward one-line fix (move trap installation earlier), reasonable to bundle into the same rework commit as the Blocker/Major fixes but not required to gate on its own.
- `TEAM_LAND_LOCK_TIMEOUT` absent-value not warning, contradicting D3's frozen literal — file-an-issue: needs either a code fix (warn on absent too) or a spec correction (amend D3's prose to "explicitly-invalid" rather than "absent") — a reconciliation between frozen prose and shipped behavior, not something this review can resolve by picking one side.
- `TEAM_LAND_WORKTREE_TESTHOOK_DIR` uncaught-crash-on-nonexistent-directory — file-an-issue: low priority (test-only seam, no shipped caller, already fails before any authorization), concur with QA it is not FAIL-worthy; worth a small hardening pass (classified failure instead of a raw bash error) whenever the Blocker/Major rework touches this file anyway.

## Round 2 (QA — rework verification)

### Scope note

Round-scoped per this round's own instruction: does not re-litigate round-1-verified properties (Summarized sources cross-check, Blast-radius spot-check, host-portability environmental note, macOS symlink fix, lock never-steal/disjointness mutation-proofs, launch-shape probes) — the spec's frozen intent block, `## Summarized sources` and `## Blast radius` are all untouched this round (`check-intent` re-confirms the v1 hash unchanged; the rework's changed-file set, read directly, is exactly `bin/land-worktree.sh`, `tests/land-worktree/run.sh`, `.shell-team/provenance/T-1077.md`). Verifies the Finding 1 (Blocker) and Finding 2 (Major + same-class Minor) rework, plus regression across everything the round-1 gates covered.

### Mandatory gates (re-run)

- `bash bin/check-intent.sh .shell-team/specs/T-1077-worktree-reconcile.md .shell-team/todo.md` → `aligned: T-1077 v1 (574f48d9393d7377a59487b44f07640dadc5d103) matches ...` — re-run myself, hash unchanged, frozen block untouched this round.
- `git diff --name-only f075cee~1 f075cee` (own command) → exactly `.shell-team/provenance/T-1077.md`, `bin/land-worktree.sh`, `tests/land-worktree/run.sh` — matches the hand-off's declared changed-file set, no stray edit.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1077.md` → conformant (11 decision entries, 0 sentinel — matches the hand-off's "4 new this round" claim: 7 round-1 + 4 round-2 = 11).
- `bash bin/check-interventions.sh --task T-1077 -- .shell-team/interventions/T-1077.md` → conformant (2 entries, 0 sentinel) — re-run myself.
- `bash bin/check-board-headings.sh .shell-team/todo.md --base 1f166329a200f33e916ea89e857a9b1a887648a1` → exit 0.
- `bash bin/check-pii-shapes.sh --base 1f166329a200f33e916ea89e857a9b1a887648a1` → exit 0.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.

### 1. The Blocker's class closure — my own fixtures, throwaway `$TMPDIR` git repos

Built and ran the following in independent scratch repos (never touching the real checkout), reading the composed tree with `git ls-tree -r onto` after each landing:

- **file→directory** (`x` blob → `x/y` tree): `rc=0`, empty stderr, `git ls-tree -r onto` shows exactly `x/y` (100644 blob) — no stray `x` blob, no phantom `xy` entry, no index warning. Matches the engineer's `composition-file-to-directory` fixture and closes the exact corruption Codex reproduced.
- **directory→file** (`x/y` tree → `x` blob): `rc=0`, empty stderr, `git ls-tree -r onto` shows exactly one entry `x` (100644 blob), `x/y` gone — no longer misclassified `structural` as in round 1.
- **blob→symlink** and **symlink→blob** (both directions): each `rc=0`, `git ls-tree onto -- l` shows the correct target mode (120000 / 100644) and correct sha, content never dereferenced.
- **mode flip 644→755** and **755→644** (both directions): each `rc=0`, `git ls-tree onto -- s.sh` shows the correct mode with the same content sha.
- **gitlink refused, new-side** (absent at base, worker introduces a fabricated 160000 gitlink entry): `rc=2`, empty stdout (`wc -c` = 0), exactly one `land-worktree: structural: path sub touches a gitlink ...` stderr line, `onto` unchanged (landing never happened).
- **gitlink refused, existing-side** (base already carries a gitlink at the touched path, worker converts it to a regular file): `rc=2`, empty stdout, same one classified stderr line, `onto` unchanged.
- **Regression probe on a pre-existing cell** (blob→absent, i.e. an ordinary deletion): `rc=0`, `a.txt` remains, `b.txt` is gone — no regression from the two-pass rewrite.
- **Beyond the engineer's own matrix — a mixed, multi-transition landing** (single worker commit: `a` file→directory, `b` directory→file, **and** an ordinary modification to `c.txt`, all three claimed paths in one landing): `rc=0`, empty stderr, `git ls-tree -r onto` shows exactly `a/nested.txt`, `b` (blob), `c.txt` (modified content) — confirms the two-pass ordering is genuinely order-independent across the **whole** claim set, not merely correct when each transition direction is landed in isolation (the shape the matrix's own per-cell fixtures test one at a time).

Audited the engineer's transition matrix cell by cell against these results: every cell I could independently reproduce (absent↔blob, blob↔symlink, mode flip, file↔directory both ways, gitlink both ways) matches the matrix's claimed handling and observed result exactly. No discrepancy found.

### 2. The TOCTOU fix

Read the critical section directly (`bin/land-worktree.sh:427-440`): `check_onto_not_checked_out` and `check_worker_tree_clean` are called once pre-lock (306, 337) and once more immediately after `T` is read inside the critical section (439-440), before claim-set derivation and composition. Both call sites share one function definition each.

**Independently reproduced both races empirically** (not merely read the code), with my own fixtures structurally distinct from the suite's own (manual `mkdir` lock hold, background coordinator launch, timed interleave, manual `rmdir` release — not the suite's own helper functions):
- **`--onto` checked out during the wait**: held the lock manually, launched the coordinator in the background (`TEAM_LAND_LOCK_TIMEOUT=8`) against a clean `onto` (so the pre-lock check passes), waited 1.5s, then `git worktree add`'d `onto` in a second worktree while the coordinator was still blocked on the lock, waited 0.5s more, then released the lock → coordinator exits **2**, `structural`, empty stdout, correctly refusing on the post-lock recheck rather than landing against a now-checked-out branch.
- **worker-tree dirty during the wait**: same shape, dirtying the worker's worktree (an `>>` append to a tracked file) during the wait instead → coordinator exits **1**, `diverged`, empty stdout.

Both reproductions confirm the post-lock recheck is genuinely load-bearing, not merely present in the diff.

**19-site inventory audit** — spot-checked more than the requested 3 not-apply justifications directly against the current code (not against the hand-off's prose):
- **(3)/(4) `--base`/`--worker` resolve to `$B`/`$W`**: read once at lines 215/217 via `git rev-parse --verify --quiet ...^{commit}`, stored in plain (non-function-local) variables never re-assigned afterward — genuinely captured as immutable SHAs at first resolution. Matches "not-apply."
- **(5) `--onto` pre-lock existence check vs. `$T`'s in-critical-section read**: line 224 (pre-lock) and line 427 (inside the critical section) use the byte-identical expression `"${ONTO_REF}^{commit}"` — confirmed "already effectively re-verified," no code change needed, correctly characterized.
- **(9) `--worker-tree` identity/existence**: lines 289-304, pre-lock only, genuinely not re-verified post-lock. Confirmed the inventory's own fallback reasoning holds: if the worktree directory were removed entirely during the wait, `check_worker_tree_clean`'s `git -C "$WORKER_TREE" status ...` (line 318) would itself fail (non-zero exit, since the directory-turned-non-repo) and `die 2 structural` — so (10)'s bulk-applied recheck does genuinely catch that case too, as claimed.
- **(11) ancestry** (`git merge-base --is-ancestor "$B" "$W"`, line 326): both operands are the same captured-immutable `$B`/`$W` from (3)/(4) — confirmed "not-apply" holds by construction, not by assertion.
- **(13) empty-landing** (`git diff --quiet "$B" "$W"`, line 339): same immutable-SHA reasoning, pre-lock — confirmed.
- **(16) claim-set derivation** (lines 448-453): both `git diff` calls read `$B`/`$W` (immutable) and `$T` (read fresh at line 427, inside the critical section, itself inside the lock acquired at line 411-421) — confirmed genuinely inside the critical section, correctly characterized as "already correctly ordered."

No dishonest or mischaracterized entry found among the sites I checked. The inventory's own claims match the code exactly at every site I audited.

### 3. Regression

- `bash tests/land-worktree/run.sh` (own timed run): elapsed **27s**, `grep -c '^PASS:' <log>` = **44**, `grep -c '^FAIL:' <log>` = **0**. All 34 round-1 tokens plus all 10 new tokens (8 composition + 2 TOCTOU) present exactly once each (own command: `grep '^PASS: T-1077 ' <log> | sed -E 's/^PASS: T-1077 //' | sort` — 44 lines, nothing dropped from round 1).
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md` → **14 passed, 0 failed, 0 skipped, 0 unrecognized** — re-run myself.
- `shellcheck bin/land-worktree.sh tests/land-worktree/run.sh` → clean, `shellcheck --version` = **0.11.0** (matches CI's pin).
- AC10's six suites, each run individually: `check-prompt-sync` rc=0, `machine-tokens` rc=0, `errexit-safe` rc=0, `check-handoff` rc=0, `log-run` rc=0 (37 PASS / 0 FAIL, `grep -c '^FAIL' <log>` = 0), `aggregate-verdicts` rc=0. `bash bin/check-prompt-sync.sh` rc=0.
- `bash tests/bin-exec-bit/run.sh` → rc=0.
- `bash bin/check-pii-shapes.sh --base 1f166329a200f33e916ea89e857a9b1a887648a1` → clean.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- **The four untouched Minors are genuinely untouched**: `git diff f075cee~1 f075cee -- bin/land-worktree.sh` (already read in full above) touches only the composition loop and the two TOCTOU functions/call-sites; reading the current file directly confirms the WFOLD/LFOLD `tr 'A-Z' 'a-z'` derivation (lines 464/474), the `WORKDIR` mktemp (251) vs. its EXIT-trap installation (390, still late), the `TEAM_LAND_LOCK_TIMEOUT` absent-case silent fallback (350-363), and the `TEAM_LAND_WORKTREE_TESTHOOK_DIR` block (683-698, unchanged) all still carry the exact same code as round 1 — none of the four disclosed-untouched Minors were silently repaired or regressed.

### 4. Hygiene

- `git status --short` → empty (before my board/review-file edits).
- `git worktree list` → the main checkout plus three pre-existing entries from prior sessions (`t1077-base-worktree`, `t1061-blast-worktree`, `t1070-base`) — none created or removed by me, none mine (all my own fixture repos this round were separate `git init` scratch trees under `$TMPDIR`, never `git worktree add` against the real checkout — except the two throwaway worktrees I created and removed inside my own scratch fixture repos for the TOCTOU race reproductions, which live entirely under `$TMPDIR/t1077-qa2/*` and never touch the real repository).
- `git status --porcelain -- bin/ tests/` → empty, confirmed after the suite run (AC3's own clause).

### Edge cases tried beyond the spec's own enumeration and beyond the engineer's matrix

- A **mixed, multi-transition landing** (file→dir, dir→file and an ordinary modification, all three claimed paths in one worker commit) — see §1 above; not in the engineer's per-cell matrix, real-data-reachable (an ordinary worker touching several paths of different transition shapes in one commit is plausible), confirms order-independence across the whole claim set rather than per-transition correctness only.
- Both TOCTOU races reproduced with my own timing harness, structurally distinct from the suite's own fixture helpers (manual lock hold/release, not the suite's `lockdir_of`/background-subshell helper functions) — an independent confirmation rather than a re-run of the same code path.
- Gitlink refusal on **both** the new-side-introduces-a-gitlink and existing-side-already-has-a-gitlink shapes, each independently fabricated via plumbing (`update-index --add --cacheinfo 160000,...`) rather than relying on the suite's own fixture construction.

### Not independently verified (environmental)

- No new environmental gaps this round. The round-1 host-portability note (`flock` absence, fractional `sleep`) is unchanged and not re-litigated per this round's scope.

### Risk notes for reviewer

- The four disclosed, non-blocking Minors from round 1 (WFOLD/LFOLD trailing-newline fold-truncation, WORKDIR EXIT-trap-installed-late leak, `TEAM_LAND_LOCK_TIMEOUT` absent-not-warning vs. D3's frozen literal, `TEAM_LAND_WORKTREE_TESTHOOK_DIR` uncaught crash) remain genuinely untouched and unresolved this round, per the engineer's own disclosed disposition and the reviewer's own fast-follow ledger — confirmed by direct code inspection, not merely by the hand-off's claim.
- `out-of-input-space`: none of my own new exploratory fixtures this round relied on an out-of-scope synthetic extreme; the gitlink fixtures exercise the Input space's own explicitly-declared-out-of-scope-for-composition (but in-scope-for-refusal) submodule boundary, which the spec's D5/composition text and the engineer's own commit message both address directly (refuse, don't compose) — not a synthetic extreme I invented.
- Working-tree hygiene confirmed clean throughout and at the end (see §4); the pre-existing worktree entries are unrelated prior-session artifacts, unchanged by this round.

### QA verdict: PASS

- Task: T-1077 → READY_FOR_REVIEW
- Tests: 44 passed, 0 failed (`tests/land-worktree/run.sh`, own command: `grep -c '^PASS:' <log>` = 44, `grep -c '^FAIL:' <log>` = 0, including all 8 new composition fixtures and both new TOCTOU fixtures); AC10's six regression suites all exit 0; `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md` → 14 passed, 0 failed, 0 skipped, 0 unrecognized
- Acceptance criteria: 14/14 checked off (own re-run of `bin/check-acs.sh`)
- Edge cases tried: mixed multi-transition landing (file↔dir plus ordinary change in one commit), both TOCTOU races reproduced with an independently-built timing harness, gitlink refusal both directions via independently-fabricated plumbing, a pre-existing composition cell (blob→absent) re-probed for regression
- Risk notes for reviewer: the four disclosed round-1 Minors remain genuinely untouched (confirmed by direct inspection this round, not merely accepted from the hand-off); no new environmental or out-of-input-space gaps this round; hygiene clean throughout

## Round 2 (codex)

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1077
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: round-1 HEAD (`77ff36f`) → current HEAD (`be30ac8`), 5 files, +559/-48 lines (`bin/land-worktree.sh`, `tests/land-worktree/run.sh`, `.shell-team/provenance/T-1077.md`, `.shell-team/reviews/T-1077.md`, `.shell-team/todo.md`)

### Scope note

Round-scoped per this round's instructions: audits whether the rework closes round 1's Blocker (entry-type-unaware composition) and Major (`--onto` checked-out TOCTOU, bulk-applied with the worker-tree-dirty Minor) as classes, checks for regression, and re-verifies the frozen mandatory items. `check-intent.sh` re-run myself: `aligned: T-1077 v1 (574f48d9393d7377a59487b44f07640dadc5d103) matches ...` — unchanged.

#### Blockers

None. The round-1 Blocker (entry-type-unaware composition — `git ls-tree` entries staged via `--cacheinfo` without checking type, corrupting file→directory and misclassifying directory→file) is closed as a class. Read `bin/land-worktree.sh:599-676` directly: the rework is a genuinely entry-type-aware two-pass procedure (pass 1 force-removes every claimed path that is absent or resolves to a TREE at the worker tip; pass 2 adds every claimed path that resolves to a blob/symlink via its own `ls-tree` mode+sha, never re-reading blob content), applied to the WHOLE claim set before either pass touches anything — order-independent by construction, not merely for the two reported repro shapes. I independently traced both directions symbolically against the code (not merely re-reading the hand-off's prose): file→directory now correctly force-removes the stale leaf entry (`WKIND=tree`) before adding the new child leaf, closing the exact "xy" phantom-entry corruption; directory→file now correctly force-removes the old child leaf (`WKIND=absent` once it no longer resolves at the worker tip) before adding the new leaf as a blob, rather than misclassifying the shape as `structural`. Cross-checked this against `tests/land-worktree/run.sh`'s new `composition-file-to-directory` (lines 425-452) and `composition-directory-to-file` (454-474) fixtures: both assert the property their names claim, not merely the new entry's presence — `composition-file-to-directory` explicitly asserts the ABSENCE of both a stray `x` blob entry and the phantom `xy` entry Codex's round-1 reproduction found (line 445, `[ -z "$(tree_line ... xy)" ]`), plus an exact entry-count check (`ls-tree -r onto | grep -c .` = 1). Also confirmed the mode-flip, blob↔symlink, and gitlink (both directions, `structural`) cells against the code directly — all match the Input space's declared-reachable classes and the Non-goals' "submodules not modelled" boundary; the gitlink classification as `structural` is a defensible, honest reading of D6's own "not there or not usable" text (an out-of-scope entry KIND, not a misclassified in-scope shape — the opposite of round 1's directory↔file misclassification, which wrongly refused an ordinary, in-scope input under the same label).

#### Major

- `tests/land-worktree/run.sh:597-608, 643-658` — The two new TOCTOU regression fixtures (`toctou-onto-checked-out-during-wait`, `toctou-worker-tree-dirty-during-wait`) mutate shared state (checking out `onto` in a second worktree / dirtying the worker's worktree) after a fixed `5 × sleep 0.1` (0.5s) wait inside a `while kill -0 "$BG_PID"` liveness loop, rather than a genuine readiness barrier proving the coordinator has finished its pre-lock checks and is specifically blocked in the lock's `mkdir` retry loop. `kill -0` only proves the background process is alive, not which phase of its own execution it has reached. If pre-lock checks (worktree enumeration, worker-tree status, ancestry, diff) take longer than 0.5s on a slow or loaded runner, the mutation can land BEFORE the pre-lock check ever runs — in which case the PRE-LOCK check itself (not the new post-lock recheck this fixture exists to verify) catches the mutated state and produces the identical observable exit code and class, so the fixture reports PASS without ever having exercised the intended post-lock code path. This is exactly the defect class this repository's own `.shell-team/test-recipe.md` (T-1076 round 3, "signal-race-release-side", rated Blocker there) already established as unacceptable for a race fixture — "the fixture's own kill was scheduling-dependent" — and for which this exact codebase already ships a working, precedented alternative (the marker-file rendezvous `TEAM_LAND_WORKTREE_TESTHOOK_DIR` seam used by the `ref-moved` fixture, `bin/land-worktree.sh:682-703`), which the new TOCTOU fixtures do not reuse or replicate. Practically low-probability in this suite's own tiny scratch repos (pre-lock checks are single-digit-millisecond git operations there), but the gap is real, cheap to close with an idiom this repo already has, and is precisely the property this round's own audit charter asks about ("do the new fixtures assert the properties their names claim, not just presence of the new one"). Independently confirmed both empirically-unverified-by-me claims by direct code reading (not merely accepting the hand-off's prose): the post-lock recheck itself (`check_onto_not_checked_out` / `check_worker_tree_clean`, shared function definitions, called once pre-lock and once more inside the critical section at `bin/land-worktree.sh:439-440`, immediately after `$T` is read and before composition) is genuinely present and correctly wired regardless of this fixture question — this finding is about the regression PROOF's reliability under load, not about the shipped mechanism's correctness, which the code itself demonstrates independent of the fixture. Suggested fix: add a test-only marker (mirroring the existing `TEAM_LAND_WORKTREE_TESTHOOK_DIR` idiom, or a new one scoped to "entered the lock retry loop") that the coordinator writes once it has completed its pre-lock checks and made its first failed `mkdir` attempt; have both fixtures wait for that marker (bounded) before mutating, rather than a fixed sleep count.

#### Minor / nits

The four round-1 Minors declared as fast-follow deferrals remain genuinely untouched this round (confirmed by direct reading, not merely accepting the hand-off's disclosure): `bin/land-worktree.sh`'s `WFOLD`/`LFOLD` trailing-newline fold-truncation (lines ~464/474, `tr 'A-Z' 'a-z'` inside `$(...)`), the `WORKDIR` mktemp (251) vs. its EXIT-trap installation (390, still late — every pre-lock refusal still leaks the scratch dir), the `TEAM_LAND_LOCK_TIMEOUT` absent-case silent fallback vs. D3's frozen "absent... warns" prose (350-363), and the `TEAM_LAND_WORKTREE_TESTHOOK_DIR` uncaught-crash-on-nonexistent-directory (683-703, unchanged). No new minor findings this round beyond the Major above.

#### Adversarial findings

Re-ran `codex exec` against `bin/land-worktree.sh`'s TOCTOU functions/call-sites and `tests/land-worktree/run.sh`'s two new TOCTOU fixtures, framed as "try to break this finding — is it actually exploitable, is there a compensating factor, and independently confirm/refute the post-lock recheck's wiring and the composition transition matrix." Result: corroborated the Major finding above without expanding it ("the timing concern is real, though unlikely on ordinary tiny-repo CI runs... no existing seam neutralizes this; the test hook at bin/land-worktree.sh:695 is later and unrelated"), and independently confirmed both of my own direct-reading conclusions: the post-lock recheck is genuinely present and correctly wired ("both shared checks are called pre-lock and again immediately after the fresh coordinator-tip read inside the critical section, before claim derivation and composition"), and the composition rework has no mishandled valid transition cell ("removals cover absent/tree states, additions preserve blob/symlink mode and SHA, two-pass ordering handles file-directory replacements, and commit-typed gitlinks are refused from either side before mutation"). No new class of failure surfaced.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes — primary (P2) and adversarial (P2) agreed on the sole finding this round, and I kept it at the normalized major level; nothing was rejected |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` back to READY_FOR_ENG with the Major finding (TOCTOU regression-fixture readiness barrier) as the item to resolve. The round-1 Blocker and Major are both confirmed closed as classes and do not need to be revisited; this is a narrow, single-file test-hygiene fix (add a marker-based readiness barrier to the two new TOCTOU fixtures, reusing the existing `TEAM_LAND_WORKTREE_TESTHOOK_DIR`-style idiom) rather than a design-premise concern, and should not need more than a small, targeted rework round.

#### Fast-follow disposition

- WFOLD/LFOLD trailing-newline fold-truncation false-positive-overlap bug — file-an-issue: unchanged from round 1's disposition; narrow, fails safe, real and worth tracking independently of this round's fix.
- WORKDIR EXIT-trap-installed-late scratch-dir leak on pre-lock refusals — file-an-issue: unchanged from round 1's disposition; low-impact resource leak, straightforward one-line fix, reasonable to bundle whenever this file is next touched.
- `TEAM_LAND_LOCK_TIMEOUT` absent-value not warning, contradicting D3's frozen literal — file-an-issue: unchanged from round 1's disposition; needs either a code fix or a spec correction, a reconciliation this review cannot resolve by picking one side.
- `TEAM_LAND_WORKTREE_TESTHOOK_DIR` uncaught-crash-on-nonexistent-directory — file-an-issue: unchanged from round 1's disposition; low priority, test-only seam, worth a small hardening pass whenever this file is next touched for another reason.

## Round 3 (QA — rework verification, readiness-barrier fix)

### Scope note

Round-scoped per this round's own instruction: verifies only the round-2 rework's delta (the marker-based readiness barrier replacing the `5×sleep 0.1` fixed wait in the two TOCTOU fixtures) plus regression. Does not re-litigate the composition Blocker or the TOCTOU Major's underlying mechanism (both already verified twice, round 1 and round 2). `bin/land-worktree.sh` is confirmed byte-identical since before round-3's own starting point: `git diff e130127..HEAD -- bin/` is empty. `check-intent` re-run myself: `aligned: T-1077 v1 (574f48d9393d7377a59487b44f07640dadc5d103) matches ...` — unchanged.

### 1. Does the marker genuinely certify "pre-lock checks already passed"?

Read `make_fake_git` and `wait_for_marker` (`tests/land-worktree/run.sh:190-255`) directly: the wrapper resolves the real `git` binary *before* `PATH` is overridden (so it can never call itself), and for exactly the two invocation shapes the coordinator's pre-lock checks make (`git worktree list --porcelain`, `git -C <path> status --porcelain --untracked-files=normal`), it invokes the real `git` call **first** and writes the marker file **only after that call returns**. `wait_for_marker` is a bounded (10s) poll on the marker's existence — the `sleep 0.1` inside it paces the poll only, never the synchronization itself. Cross-checked against `bin/land-worktree.sh`: `check_onto_not_checked_out` (271-278) and `check_worker_tree_clean` (315-323) are each defined once and called twice — once pre-lock (306, 337) and once more inside the critical section (439-440), immediately after `$T` is re-read and strictly before the lock-acquire loop can even be attempted by the pre-lock call (the coordinator's execution is single-threaded and sequential, so the first marker write can only be the pre-lock call — the post-lock call cannot yet have started while the test still holds the lock externally). This confirms the marker is a genuine synchronization point, not a guessed duration.

### 2. `bin/land-worktree.sh` byte-identical

`git diff e130127..HEAD -- bin/` → empty (own command). Confirms the round-3 rework touched test files only, as its own scope guard required.

### 3. Independent reproduction of the vacuity, my own method (not the engineer's)

Built my own scratch harness under `$TMPDIR` (distinct from the suite's `make_fake_git`/`wait_for_marker` — a separate PATH-shadowing wrapper with an artificial delay injected **before** the real `git` call, plus a liveness probe via `kill -0` on the coordinator's PID taken *before* releasing the lock, rather than the engineer's own described method), run against the real, unmodified `bin/land-worktree.sh`:

- **`toctou-onto-checked-out-during-wait`'s mechanism** — simulated a slow pre-lock check (2s injected delay before the real `git worktree list --porcelain` call) and compared the OLD fixed-wait shape (mutate after a fixed `5×0.1s`) against the NEW marker-gated shape:
  - OLD shape: mutated at t≈0.56s (fixed wait), and the coordinator process had **already exited by the time I probed liveness, well before I released the externally-held lock** (`EXITED_BEFORE_LOCK_RELEASE=yes`) — proving the mutation landed before the delayed pre-lock `git worktree list` call ever executed, so the **pre-lock check itself** caught it, never touching the post-lock recheck. This reproduces the exact vacuity Codex's round-2 finding described.
  - NEW shape (`wait_for_marker` against the same delayed wrapper): the wait genuinely blocked ~2.4s for the marker, mutated only then, and the coordinator was **still alive, blocked on the lock**, when I probed liveness before releasing it (`EXITED_BEFORE_LOCK_RELEASE=no`) — it only exited immediately after I released the lock, proving the **post-lock recheck** is what caught it this time.
- **`toctou-worker-tree-dirty-during-wait`'s mechanism** — repeated the identical experiment against `check_worker_tree_clean`'s own call (`git status --porcelain --untracked-files=normal`, delayed the same way): OLD shape → `EXITED_BEFORE_LOCK_RELEASE=yes` (vacuous, pre-lock catch); NEW shape → `EXITED_BEFORE_LOCK_RELEASE=no` (genuine, post-lock catch, exit 1 `diverged`).

Both fixtures independently confirmed, by a method structurally distinct from the engineer's own mutation self-check (provenance decision, round 3, sleep-based synchronization entry) — same conclusion reached independently rather than merely re-reading the hand-off's own report of it.

### 4. Sleep-site inventory audit (5 sites, 2 apply / 3 not-apply)

`grep -n 'sleep' tests/land-worktree/run.sh bin/land-worktree.sh` (own command) → confirms exactly the claimed shape: line 252 (`wait_for_marker`'s poll, now gating the two rewritten TOCTOU fixtures — **2 APPLY** sites, the two call sites at 677/724), and three pre-existing polling loops at lines 896, 911, 970 — **3 NOT-APPLY** sites. Read each directly: 896/970 poll `while [ ! -e "$HOOK/ready" ]` against `bin/land-worktree.sh`'s own shipped `TEAM_LAND_WORKTREE_TESTHOOK_DIR` marker seam (line 682-703, itself unchanged, byte-identical to round 2); 911 polls `while kill -0 "$PID"` — the kernel's own process-death signal, not a guessed duration. Both are real conditions, correctly left untouched — this is not the same defect class as the two TOCTOU fixtures' fixed iteration count, which was itself the (vacuous) synchronization primitive. No mischaracterization found; the "5 sites, 2/3" count matches the code exactly.

### 5. Regression

- `bash tests/land-worktree/run.sh` run **twice** (own commands, elapsed 24.07s and 24.10s): both runs **44 PASS, 0 FAIL** (`grep -c '^PASS:' <log>` = 44 both times, `grep -c '^FAIL:' <log>` = 0 both times) — no flake observed.
- `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md` → **14 passed, 0 failed, 0 skipped, 0 unrecognized** (re-run myself; all 14 ACs carry `- check:` sub-bullets, so the runtime-AC-without-check: audit rule does not apply this round).
- `bash bin/check-intent.sh .shell-team/specs/T-1077-worktree-reconcile.md .shell-team/todo.md` → aligned, v1 hash unchanged.
- `shellcheck bin/land-worktree.sh tests/land-worktree/run.sh` (0.11.0, matching CI's pin) → clean.
- `bash bin/check-handoff.sh .shell-team/todo.md` → exit 0.
- `bash bin/check-provenance.sh .shell-team/provenance/T-1077.md` → conformant (13 decision entries, 0 sentinel).
- `bash bin/check-interventions.sh --task T-1077 -- .shell-team/interventions/T-1077.md` → conformant (2 entries, 0 sentinel).
- `git status --short` → empty (before my own board/review edits).
- `git status --porcelain -- bin/ tests/` → empty, confirmed after both suite runs.

### Summarized sources

Not re-litigated this round: the spec's `## Summarized sources` section is inside the frozen intent block (unchanged this round, confirmed by the unchanged intent hash above) and was fully cross-checked against its named sources in round 1's own review record — nothing in this round's delta (test-fixture synchronization only) touches or restates any summarized-source fact.

### Edge cases tried beyond the engineer's own self-check

- Both TOCTOU races reproduced with an independently-built delay-injection harness distinct from the suite's own `make_fake_git`, and with an explicit pre-release liveness probe (`kill -0` before `rmdir`) as the discriminator between "pre-lock caught it" and "post-lock caught it" — a stronger, more direct falsification test than re-reading the hand-off's own described self-check.
- Confirmed the second fixture's readiness barrier independently as well (not merely by code-reading analogy to the first) — the identical OLD-vacuous/NEW-genuine split reproduced for `check_worker_tree_clean`'s own marker.

### Not independently verified (environmental)

None this round — no new environmental gaps; the round-1 host-portability note (`flock` absence, fractional `sleep`) is unchanged and not re-litigated per this round's scope.

### Risk notes for reviewer

- My own reproduction harness lives entirely under `$TMPDIR` (`.../scratchpad/qa3`), never touching the real checkout or its git-common-dir; confirmed by `git status --short` staying empty throughout.
- The four disclosed, non-blocking Minors from round 1 remain genuinely untouched this round (this round's own diff is test-file-only, confirmed above) — not re-inspected line-by-line this round since nothing could have touched them, per the round-1/round-2 records' own direct confirmation.
- `out-of-input-space`: none of my own new exploratory work this round relied on an out-of-scope synthetic extreme — the artificial delay I injected simulates ordinary host-load variance (a slow `git` call on a loaded runner), which is exactly the real-data-reachable condition the readiness-barrier finding itself was about, not a synthetic extreme.

### QA verdict: PASS

- Task: T-1077 → READY_FOR_REVIEW
- Tests: 44 passed, 0 failed, run twice (`tests/land-worktree/run.sh`, own commands: `grep -c '^PASS:' <log>` = 44 both runs, `grep -c '^FAIL:' <log>` = 0 both runs); `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1077-worktree-reconcile.md` → 14 passed, 0 failed, 0 skipped, 0 unrecognized
- Acceptance criteria: 14/14 checked off (own re-run of `bin/check-acs.sh`)
- Edge cases tried: independent delay-injection reproduction of both TOCTOU fixtures' readiness barrier (own harness, own liveness-probe discriminator, structurally distinct from the engineer's own self-check), confirming the OLD fixed-wait shape is genuinely vacuous (pre-lock catch) and the NEW marker-gated shape is genuinely load-bearing (post-lock catch) for both fixtures
- Risk notes for reviewer: sleep-site inventory (5 sites, 2 apply / 3 not-apply) confirmed by direct code reading, matches the hand-off exactly; `bin/land-worktree.sh` confirmed byte-identical across this round (`git diff e130127..HEAD -- bin/` empty); no new environmental or out-of-input-space gaps this round; hygiene clean throughout (own reproduction confined to `$TMPDIR`)

## Round 3 (codex)

### Codex Review verdict: APPROVE
- Task: T-1077
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.147.0
- Verification mode: static-only
- Diff scope: `e130127` → HEAD (`240df63`), 4 files, +192/-36 lines (`tests/land-worktree/run.sh`, `.shell-team/provenance/T-1077.md`, `.shell-team/todo.md`, `.shell-team/reviews/T-1077.md`)

### スコープ注記

本ラウンドの指示どおり、round-2 Major（2 つの TOCTOU フィクスチャの固定 `5×sleep 0.1` 待機＝readiness barrier 不在によるロード下 vacuous 化）のクラス閉鎖のみを監査対象とし、round-1/round-2 で既に二重検証済みの合成（composition）Blocker とロック機構そのものの正しさは再監査しない。`check-intent.sh` 自分で再実行: `aligned: T-1077 v1 (574f48d9393d7377a59487b44f07640dadc5d103) matches ...` — 不変。`git diff e130127..HEAD -- bin/` → 空（自分のコマンド）。`bin/land-worktree.sh` は本ラウンド完全にバイト同一。差分は `tests/land-worktree/run.sh` の 2 ハンク（`make_fake_git`/`wait_for_marker` ヘルパーの追加＝1 ハンク、2 つの TOCTOU フィクスチャ本体の書き換え＝残りのハンク）のみで、それ以外のアサーションは一切触れられていない（`git diff b84bada~1 b84bada -- tests/land-worktree/run.sh` の `@@` ヘッダを自分で確認）。

### Blockers

なし。

### Major

なし。round-2 の唯一の Major はクラスとして閉じている。

- **監査点1（マーカーの書き込み位置は本当に「pre-lock チェックが既に走った」ことを証明するか）**: `make_fake_git`/`wait_for_marker`（`tests/land-worktree/run.sh:190-255`）を直接読んだ。ラッパーは `git worktree list --porcelain`（`check_onto_not_checked_out` の呼び出し文字列と厳密一致）または `git -C <path> status --porcelain --untracked-files=normal`（`check_worker_tree_clean` の呼び出しを部分文字列一致で拾う）の**実 git 呼び出しが返った後にのみ**マーカーを書く。`bin/land-worktree.sh` を直接読み、両関数がそれぞれ pre-lock（306/337）と post-lock（439-440、`$T` 再読み込み直後）の 2 箇所でのみ呼ばれ、同一の呼び出し文字列が両呼び出し間で不変であることを確認した（`grep -n` で該当文字列がソース中に 1 回ずつしか出現しないことも確認 — 関数定義は 1 回、呼び出しは 2 回）。誤発火（post-lock 側の呼び出しがマーカーを書いてしまい、`wait_for_marker` がそれを「pre-lock 完了」と誤認する）が原理的に起きないことを、コーディネータがシングルスレッド・逐次実行であり、かつフィクスチャがロックを外部から `mkdir` で保持し続けている限り post-lock 側の再チェックには物理的に到達できない、という制御構造から確認した——マーカーの**最初の出現**は必ず pre-lock 呼び出し由来になる。QA round 3 の独立再現（`kill -0` によるロック解放**前**の生存確認で post-lock 側にまだ到達していないことを実測）とも整合する。misfire は起きない。
- **監査点2（`wait_for_marker` は fail-closed か）**: タイムアウト時（10 秒境界）は `fail "$desc: marker ... never appeared ..."` を呼び `fails` カウンタをインクリメントする。ただしこの戻り値はどのフィクスチャの呼び出し側でもチェックされておらず（`wait_for_marker ...` の後に無条件で mutate/`rmdir`/`wait`/`assert_*`/`pass` が続く）、ローカルなスクリプト実行は中断されない。これは一見「proceed してしまう」ように見えるが、`run.sh` 末尾の集計（`if [ "$fails" -eq 0 ]; then exit 0; else exit 1; fi`）により、後続の `assert_empty_stdout_refusal` が偶然パスしても `fails` は既に非ゼロなのでスイート全体としては確実に FAIL する——これはこのファイル全体で一貫した既存パターン（`assert_*` 系は内部で `fail()` を呼ぶが呼び出し元は無条件に `pass "T-1077 ..."` を続ける）であり、本ラウンドの改修が新規に導入した緩みではない。スイート判定レベルでは fail-closed。
- **監査点3（3 つの not-apply sleep サイトは本当にペースドポールか）**: `grep -n sleep tests/land-worktree/run.sh bin/land-worktree.sh` で 5 サイトに一致することを自分で確認（252 = `wait_for_marker` 自身、677/724 = その 2 つの APPLY 呼び出し箇所、896/911/970 = NOT-APPLY）。896 行目と 970 行目は `TEAM_LAND_WORKTREE_TESTHOOK_DIR/ready` ファイルの存在（`lock-released-on-signal`/`ref-moved` フィクスチャ、`bin/land-worktree.sh` 自身が出荷する既存の testhook seam）を、911 行目は `kill -0 "$PID"`（カーネル自身のプロセス死活シグナル）をポールしており、いずれも本ラウンドの diff の外（既存コード）にあり実条件に対するペースドポールとして正しい。件数・内訳とも記録の主張と一致。
- **監査点4（他 42 アサーションへの劣化）**: diff は上記 2 ハンクに限定されており（確認済み）、自分で 2 回スイートを実行し双方とも **44 PASS / 0 FAIL**（1 回目 24.40s、2 回目は QA の 2 回の実測 24.07s/24.10s と同水準）を確認。劣化なし。

自分自身の独立再現（QA のものとは別のアプローチ）として、`make_fake_git`/`wait_for_marker` を静的に読み、かつコーディネータの呼び出し構造（`check_onto_not_checked_out`/`check_worker_tree_clean` の定義 1 回・呼び出し 2 回、呼び出し文字列の pre/post 不変性）をソースコードから直接検証した。時間注入（delay injection）による動的再現は QA round 3 が既に独立した手法で行っており（`EXITED_BEFORE_LOCK_RELEASE` の yes/no 判別）、静的読解の結論と完全に整合するため、同じ動的実験を重ねて反復する必要はないと判断した。

### Minor / nits

round-1 で開示され round-2 で未着手のまま据え置かれた 4 件の Minor は、本ラウンドの diff が test file only（`bin/land-worktree.sh` バイト同一）である以上、構造的に本ラウンドでは触れられていない（コード上の該当行も未変更のまま）。据え置き内容は round-1/round-2 の記録と同一で変更なし。

### Adversarial findings

本ラウンドは新規の敵対的パスを走らせていない：round-2 review の Major は round-2 review 自身の敵対的パスで既に一度コーロボレートされており（round-2 record 参照）、本ラウンドの争点は「その Major がクラスとして閉じたか」という限定された検証項目であって、新しい設計上の攻撃面を探すものではない。静的読解と QA round 3 の独立した動的再現（別手法）が同じ結論に収束しており、追加の敵対的ラウンドを起動する規約上のトリガ（新規 Blocker/Major の発生）は本ラウンドに存在しない。

### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| — | — | — | — | no severity changes — round-2 の唯一の Major は本ラウンドの改修でクラスとして閉じたと判定し、新規の finding も reject した finding もない |

### Recommendation

- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE

### Fast-follow disposition

- WFOLD/LFOLD の末尾改行 fold-truncation による偽陽性 overlap バグ — file-an-issue: round-1/round-2 から変更なし。狭いケース・fail-safe（過剰拒否であり過小拒否ではない）だが実在し独立追跡の価値がある。
- `WORKDIR` の `mktemp` と `EXIT` トラップ設置の遅延によるスクラッチディレクトリ・リーク（pre-lock 拒否時） — file-an-issue: round-1/round-2 から変更なし。低インパクトな資源リーク、1 行修正で足りる程度なので次にこのファイルを触る機会にまとめてよい。
- `TEAM_LAND_LOCK_TIMEOUT` の absent ケースが D3 の凍結された文言（"absent... warns"）と矛盾して警告を出さない件 — file-an-issue: round-1/round-2 から変更なし。コード修正（absent でも警告する）か spec 修正（D3 の文言を "explicitly-invalid" に変更する）のどちらかで解消すべき、本レビューが一方に決め切れる性質のものではない。
- `TEAM_LAND_WORKTREE_TESTHOOK_DIR` に存在しないディレクトリを指定した際の未捕捉クラッシュ — file-an-issue: round-1/round-2 から変更なし。優先度は低い（テスト専用シーム、出荷済み呼び出し元なし、認可以前にクラッシュする）が、次にこのファイルを触る際に分類済み失敗にハードニングする価値はある。
