# Codex cross-provider review — T-1152

## Round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1152
- Codex model: gpt-6-luna
- Codex CLI: codex-cli 0.156.1
- Verification mode: static-only plus one live dogfood run of the shipped shape (this round's own primary pass)
- Diff scope: 14 files, +277/-62 lines (`30d71fcb...c0839063`, reviewed at `f3b79206` with the QA and provenance commits on top)

#### Blockers

None.

#### Major

- `bin/codex-capture.sh:375` (the `msg_null` clause of the review-did-not-run gate) — class `gate-refuses-the-shipped-primary-shape`. **A completed `codex exec … review --base <base>` run writes a rollout whose `task_complete` line carries `"last_agent_message":null` on success** — the review text travels in `ExitedReviewMode` / `AgentMessage` items and in the `-o` file — so the gate's null-final-message condition refuses every primary review by construction. Measured live this round on the shipped block itself: 8 `CommandExecution` items, all `exit_code` 0, `task_complete` present, no `error` key, `-o` written (754 bytes), and `--publish --thread-id` exited 3 with `review did not run (thread=… commands_executed=8 commands_succeeded=8 failure_markers=8 task_complete=present error=absent)`, removing both raws. The premise came from the free-form probes, whose `task_complete` does carry the message; the spec's success fixtures never include the `review`-subcommand shape (Input space item 5 names the subcommand, AC6 has no fixture for it). Confirmed by the adversarial pass.
  - Suggested fix: drop the `last_agent_message` clause (the `-o` non-empty check `--publish` already performs is the "a final message exists" evidence) and report the field informationally; add a review-subcommand-shaped success fixture to AC6 — a class-B amendment of AC5/AC6 (orchestrator ruling R3'').
- `bin/codex-capture.sh:357-366` (the awk gate's field detection) — class `substring-match-for-structural-field`. The gate recognizes `"type":"CommandExecution"`, `"exit_code":0`, `"task_complete"`, `"last_agent_message":null` and `"error":` as substrings of the serialized event line, so the same bytes inside a string value — a `CommandExecution`'s `aggregated_output`, or the final message's text — are read as top-level fields, in both directions (a failed command whose output quotes `"exit_code":0` counts as succeeded; a final message quoting `"error": {` reads as an error). This repository's own diffs and review messages contain exactly those tokens. Raised by the primary pass (P2), confirmed by the adversarial pass with the escaping analysis: escaping the quotes inside JSON strings does not stop these regexes from matching them.
  - Suggested fix: a small awk lexer that splits each line into JSON string literals (`"(\\.|[^"\\])*"`) and the bytes between them, tracks object/array nesting, and reads `payload.type`, `payload.item.type`, `payload.item.exit_code` and the `task_complete` fields as key/value pairs at their structural positions; fail closed on a key or value carrying a `\u` escape; the last `task_complete` event wins (orchestrator ruling R3'').
- `bin/codex-capture.sh:331` (the exactly-one-match search) — class `search-not-fail-closed`. `find` does not descend a symlinked `sessions` root, so a relocated session store reports a real rollout as missing; `|| true` discards traversal errors, so one visible match beside an unreadable subtree is accepted as exactly one; matches are counted newline-delimited. Raised by the adversarial pass (P2, normalized major under the map). Not reproduced live this round; accepted on the code read (the three mechanisms are visible at the cited lines).
  - Suggested fix: resolve the root once with `cd -P`, run `find … -print0` and check its status, count NUL-delimited, and reject symlink raws with `! -L` (orchestrator ruling R14).

#### Minor / nits

- `bin/codex-capture.sh:310` — an empty symlink supplied directly as `--publish-jsonl` passes the `-f`/`-s` precondition and the later `cp`/`mv` publish the symlink itself. Outside the shipped alloc flow (Input space item 7 excludes raw replacement); folded into R14's `! -L` check rather than priced on its own.
- The adversarial pass confirmed, and this round re-read, that the four agent `codex` blocks and the recipe line carry no second shell part, and that exit-3 cleanup removes both raws (required by AC6/AC7) — no finding.

#### Adversarial findings

Framed as devil's advocate against the new `--publish --thread-id` import and gate, briefed with the two Majors already on the table (the live primary refusal with its exact stderr line and rollout shape; the P2 substring finding), the spec's Input space, and three question groups (reachability and minimal pure-bash fix for each Major, including whether an awk tokenizer suffices and what still defeats it; further defeats of the id check, empty-raw precondition, exactly-one search and EXIT trap; any remaining second shell part on a codex line). Returned four findings: Finding 1 CONFIRMED and reachable (P2), Finding 2 CONFIRMED and reachable (P2), the search-not-fail-closed defeat (P2), and the symlink-raw precondition (P3). It also states that a trailing newline in the thread id is rejected by the anchored regex and that the agent blocks and recipe line remain single commands.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | null `last_agent_message` on a completed `review` run refuses the primary pass | not raised by primary (it was the refusal itself); adversarial P2 | major | measured live on the shipped shape this round; blocks the loop's own primary review path on every run |
| 2 | substring matching of structural fields | primary P2; adversarial P2 (confirmed) | major | map P2 → major; reachable from this repository's own diffs and messages |
| 3 | exactly-one search not fail-closed (symlinked root, `\|\| true`, newline counting) | adversarial P2 | major | map P2 → major; the repository's checker rule is fail-closed, and a relocated session store is an ordinary adopter configuration |
| 4 | symlink raw passes the precondition | adversarial P3 | minor | outside the shipped alloc flow (Input space item 7); one-line hardening folded into finding 3's fix |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to `REWORK` with the three Majors above linked. Findings 1 and 2 change what AC5/AC6 assert and finding 3 extends AC7, so the fix is a class-B re-freeze (v2) of those criteria before the engineer's round 2 — the orchestrator's rulings R3'' and R14 name the exact deltas.

#### Fast-follow disposition

- no fast-follow deferrals — the three Majors block merge; nothing is deferred past them.

## Per-pass provenance

### Round 1

Round 1's primary pass ran the `codex exec … review --base 30d71fcb --json -o` subcommand in the shipped single-command shape (no redirection, no `< /dev/null`), from this sandbox-enabled Claude Code 2.1.281 session with `"codex *"` in `sandbox.excludedCommands`; it ran outside the sandbox (Codex executed 8 commands, all exit 0). Its `-o` capture was written but **not published**: the shipped `--publish --thread-id` refused it with exit 3 (Major 1) and, as AC6 requires, removed both raws. The final review message was recovered from the `--json` event stream the Bash tool returned (the `agent_message` item), and the full stream is preserved in the coordinating session's scratch directory, outside this repository.

- pass-role (primary): generation
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base 30d71fcb --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1152-codex-primary.out.X6vl4i`
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt; the role file's primary skeleton was run exactly as shipped by this task.
- raw-capture (primary): T-1152-codex-primary
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Round 1's adversarial pass was triggered by the primary refusal and the primary's P2 finding, framed as devil's advocate against the gate and import code with both Majors carried verbatim (the refusal's stderr line and rollout shape; the substring mechanism), per step 5's instruction to target those specific files. It ran in the same shipped shape and its `--publish --thread-id` succeeded: `rollout thread=… commands_executed=19 commands_succeeded=19 failure_markers=16` (the marker count is the spec's own vocabulary appearing in the files Codex read, informational by design).

- pass-role (adversarial): generation
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/.codex-capture.T-1152-codex-adversarial.out.TGLzqO "Play devil's advocate against bin/codex-capture.sh's new --publish --thread-id rollout import and its review-did-not-run gate (task T-1152, spec .shell-team/specs/T-1152-codex-sandbox-hotfix.md, diff base 30d71fcb). Read the files yourself with git diff 30d71fcb...HEAD -- bin/codex-capture.sh and nl -ba bin/codex-capture.sh, plus the spec's Goal, Non-goals, Input space and AC4-AC8 — do not expect any diff or excerpt in this prompt. Two Major findings are already on the table; confirm or refute each by tracing the code, then hunt for independent defeats. [Finding 1 and Finding 2 as stated in the Major section above; three question groups: reachability and minimal pure-bash/awk fix per finding, further defeats of id check / empty-raw precondition / exactly-one search / EXIT trap, any remaining second shell part on a codex line.] Return findings as a JSON array of {severity, file, line, issue, suggestion}, using P1/P2/P3 severities, and state explicitly for findings 1 and 2 whether you confirm them."`
- briefing-fidelity (adversarial): carried — both Majors' exact mechanisms, the live stderr line, the rollout shape and the spec sections were relayed into the prompt; the bracketed summary above stands for text that repeats the Major section verbatim.
- raw-capture (adversarial): T-1152-codex-adversarial
- self-detected-host (adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Raw captures: `T-1152-codex-adversarial.{txt,jsonl}` in this directory (the `.jsonl` is the imported rollout, 238 lines of `event_msg`/`response_item` records, not a `--json` stream). `T-1152-codex-primary.{txt,jsonl}` do not exist: refused at publish (Major 1).

## Independent verification performed this round (beyond the two Codex passes)

- Own read of `git diff 30d71fcb...c0839063` in full, `bin/codex-capture.sh`'s new `--thread-id` path (`:300`–`:400`), the four agent blocks and the recipe line as shipped, the spec's AC4–AC8 and `## Review depth`, the engineer hand-off and the QA verdict on the board, the provenance file (8 entries).
- Own live dogfood of the shipped shape, twice: (a) before QA, with a reviews dir outside the repository — a free-form prompt run imported and published (`commands_executed=2 commands_succeeded=2`), the earlier sandboxed failure's thread id refused with exit 3, the `--ephemeral` probe's thread id refused as `matches=0` (recorded in `.shell-team/provenance/T-1152.md`); (b) this round's primary pass on the real reviews dir — refused as Major 1 describes.
- Own read of the primary pass's rollout (47 lines): exactly one `task_complete` line, `last_agent_message` null, no `error` key, 8 `CommandExecution` items all `"exit_code":0`, final items `ExitedReviewMode` → `AgentMessage` → `task_complete`.
- `codex --version` (own run): `codex-cli 0.156.1`; model from the rollout's `session_meta`: `gpt-6-luna`.
- shape-scan: rc=1 — 1 record path written this round (`.shell-team/reviews/T-1152.md`) — findings on it are the project's own `T-NNN` tracker-key vocabulary only (accepted noise class) — command: `PII_CHECK_TRACKER_KEY=1 bash bin/check-pii-shapes.sh --all`.

## Notes on this round's Majors

Major 1 was found by running the shipped path on the task's own branch, not by reading — the same dogfood dependency the spec's `## Notes for engineer` names for AC19. Its cause is a premise error in the orchestrator's ruling (R3), not the engineer's craft: the spec froze the condition. The repair therefore goes through a class-B re-freeze of AC5/AC6 (and AC7 for Major 3) before round 2, under this checkout's standing freeze rule, and is recorded in `.shell-team/interventions/T-1152.md`.
