### Codex Review verdict: APPROVE

## Diff review

- Task: T-1120
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 8 files, +569/-9 lines (`4f96989...HEAD`; `git diff --stat`)

- raw-capture: `T-1120-codex-primary.jsonl` / `T-1120-codex-primary.txt`
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> --ignore-user-config --ignore-rules --ephemeral --json -o "<RAW_OUT>" "$(cat <briefing-file>)" < /dev/null > "<RAW_JSONL>" 2>&1` — briefing content: the two consumer classes this diff touches and their depth pricing (shipped `bin/` gate text at full strength; adopter norm text for claim truth against the shipped tree; the spec's own inline `- check:` lines at dev-scaffold depth per the 2026-08-22 operator ruling); the specific questions to answer for each site (pipefail/BSD-GNU-grep correctness and teaching completeness for `bin/check-count-claims.sh:298`; truth-against-shipped-tree for the two README bullets and the `phase-multiplexing.md` dispatch-rule/follow-up lines; the T-1112 AC3 single-line-extraction preservation constraint); pointers to the records to read rather than re-derive (spec, interventions, provenance, board entry, QA block); a request to write the review to `.shell-team/reviews/T-1120.md` in this repo's grammar with verdict `APPROVE`/`REQUEST_CHANGES`.
- pass-role (primary): generation
- briefing-fidelity (primary): carried — the prompt reached the executor and was read as the sole instruction-bearing input; its own JSONL trace shows six completed `command_execution` items covering every named surface: `git diff --stat`/`--name-status` and a wide-context unified diff of all eight changed files plus `T-1119.md`'s review grammar; `git log`/`git merge-base` plus a second unified diff at the exact touched lines; a targeted spec/board read (`## Acceptance criteria`, `QA verification` block); a live re-derivation pass (`nl -ba` on both the checker and its fixture suite, `T-1113`/`T-1112` spec excerpts, `skills/run/SKILL.md:268-288`, the CI workflow's `check-fanout-instances` wiring, `ls -l` on both fan-out files, and the `#!/usr/bin/env bash`/`set -`/`pipefail` idiom sweep); a live execution pass (`bash tests/check-count-claims/run.sh`, `shellcheck bin/check-count-claims.sh`, guarded/unguarded pipeline probes under `set -e -o pipefail` for BSD grep and, where installed, GNU `ggrep`, backtick-span extraction on the live message line, the T-1112 AC3 discharged-line count, and a stale-phrase sweep) — the first attempt at this block hit a quoting defect of its own construction (several sub-statements produced no visible output) and was re-run cleanly in the next command with corrected quoting, which is what this record's findings are drawn from; and a final confirmation pass (`git diff --check`, per-file `--numstat`, a byte-for-byte `shasum` comparison of the T-1112 AC3 extraction at base vs. HEAD, a grep-based presence/absence check of all four required message literals and both retired unguarded examples, and a Plan-before-Validate token-order check on both READMEs). This covers every region the briefing named. One self-attempted file write (`.shell-team/reviews/T-1120.md`) was rejected by the read-only sandbox mid-run and self-reported by the model in its final message — expected behavior under this role's read-only invocation contract; this reviewer (not Codex) is the one that writes the record.
- raw-capture (primary): T-1120-codex-primary

### Invocation hygiene observations (non-licensing)

- **Non-repo read under `--ignore-user-config --ignore-rules`**: the pass's first `command_execution` item read `<home>/.agents/skills/model-moderator/SKILL.md` and `<home>/.agents/skills/hard-task-protocol/SKILL.md` — files outside this repository, under the invoking operator's own home directory — despite both ignore flags being present. This is the same class this repository's own reviews already record for prior tasks (see `.shell-team/reviews/T-1119.md`'s `specreview-r1`/`specreview-r2`/primary hygiene notes): the CLI's built-in skill-discovery path is not scoped by `--ignore-user-config`/`--ignore-rules`, which govern provider configuration and hooks rather than a separately-discovered skill directory. Codex's own agent_message trace shows it explicitly named this read as informing its own internal `model-moderator`/`hard-task-protocol` self-classification ("これは返却物の正否を裁くレビュー判断なので KEEP とした... `hard-task-protocol` に従い...") rather than influencing any judgment about this diff's own content; every finding below is independently traced against files this repository ships. Recorded here as required, not as a licensing concern.
- **`item.type=error` plugin/hook items**: none. The stream carries one `thread.started`, one `turn.started`, several `command_execution` and `todo_list` item pairs, six `agent_message` items and one `turn.completed`; one embedded `ERROR codex_core::tools::router` line appears mid-stream as raw (non-JSON) text, not a structured `error`-typed item — it is the sandbox's own denial of Codex's self-initiated attempt to write `.shell-team/reviews/T-1120.md` (`patch rejected: writing is blocked by read-only sandbox`), expected under this pass's read-only invocation and not a plugin/hook fault.
- **First request's `usage.input_tokens`**: one `turn.completed` event, `input_tokens=792271, cached_input_tokens=702848, cache_write_input_tokens=0, output_tokens=5672, reasoning_output_tokens=1089` for the whole turn (six completed tool calls). Not compared against any baseline — none was requested in this round's briefing.
- **Sandbox-imposed verification ceiling, disclosed by the model itself**: `tests/check-count-claims/run.sh`'s first live invocation stalled at its own initial `mktemp` call under the read-only sandbox (no writable path exists for the suite's scratch root), so the reviewer could not itself re-run the fixture suite end to end; it relied on the board's own QA record (PASS, 14/14 assertions, independently re-run there) for that specific claim rather than re-deriving it, and disclosed this explicitly rather than silently asserting the suite passed. `shellcheck bin/check-count-claims.sh` (which needs no writable scratch) did complete, exit 0.
- No stall, hang, mutating flag, or licensing-relevant injection beyond the skill-file read above.

### Independent primary reading (before consulting Codex's output)

Read the diff directly (`git log --oneline 4f96989..HEAD`, `git diff --stat`/`--numstat`, per-file diffs of `bin/check-count-claims.sh`, `README.md`, `README.ja.md`, `docs/loop-engineering/phase-multiplexing.md`) and the spec's own AC1/AC2/AC5/AC6 check bodies to know what each site is required to demonstrate. Independently re-ran the mechanical checks Codex's own trace also ran, from this session, before opening Codex's raw output:

- `bin/check-count-claims.sh:298`'s new message quotes exactly one pipeline naming both `grep` and `wc -l` in one backtick span — `(grep zzz file || true) | wc -l` — and it is guarded; the two retired unguarded examples (`grep zzz file | wc -l`, `grep -c zzz file | wc -l`) are gone from the rendered message (confirmed with `grep -oF` against the live line: zero hits for both). The `set -o pipefail`-under-`bash -c` context (line `:288`–`:293`) means an unguarded `grep | wc -l` on a genuine zero match still exits non-zero — reproduced live (`bash -c 'set -o pipefail; grep zzz /dev/null | wc -l'` → rc 1) — so the surviving guarded recipe is the only one the message now teaches, closing the exact defect issue #409 reports. `(grep zzz file || true) | wc -l` itself reproduced live under `set -e -o pipefail` with BSD `grep`, exiting 0 and printing `0`; GNU `grep`'s exit-1-on-no-match behavior is identical by POSIX contract, so the recipe is portable across both.
- `grep -c zzz /dev/null | wc -l` reproduces the exact double-count trap the message's prose names: `grep -c` prints the text `0` and exits 1, and piping that text through `wc -l` counts one line — live output confirms `1`, not `0`, matching the message's own claim.
- `tests/check-count-claims/run.sh:140`'s `nonzero-exit-remedy` case and `:228`'s echo-before-run-ordering case still key off the bare `| wc -l` substring, which the new message still carries; T-1113 AC6/AC9's own frozen check lines read the same substring and are unaffected by the reordering inside the message.
- `README.md:174` / `README.ja.md:174`: each is a one-line, in-place rewrite (`--numstat` 1/1 confirmed), Plan appears before Validate in both, and the JA mirror's machine tokens (`Plan`/`Specify`/`Validate`/`verify`) are verbatim English (confirmed by literal count against the live line: 1/1/1/2). The amended claim — that using fan-out for other verification work is a Plan-time `verify`-axis dispatch decision `tech-lead` elects and the orchestrator transcribes at the Specify-to-Implement seam, read at Validate — matches `skills/run/SKILL.md:269`'s own words ("Each axis is decided at Plan by `tech-lead`... the orchestrator transcribes each decision onto the task's own board entry... at the Specify-to-Implement seam") essentially verbatim; this is a repair of a genuinely stale claim (the branch-point text said the choice was still made "inside the Validate phase," which T-1084's dispatch-record mechanism superseded), not an overshoot into a new, unsupported claim.
- `docs/loop-engineering/phase-multiplexing.md`: `bin/check-fanout-instances.sh` exists and is executable; `.github/workflows/check-handoff.yml:346`–`:347` wires its fixture suite (`Run check-fanout-instances fixture suite` → `bash tests/check-fanout-instances/run.sh`), confirming the "shipped as `bin/check-fanout-instances.sh`, with its fixture suite CI-wired" claim on both the recommendation line and the discharged follow-up line. The stale "two unbuilt preconditions" / "preconditions exist" phrasing this task exists to remove occurs zero times in the file at HEAD (confirmed by direct grep). The T-1112 AC3-relevant `^- follow-up: DISCHARGED` extraction stays exactly one line and is byte-identical (SHA-256) between the base blob and HEAD — the rewritten discharged follow-up line replaced the design/`no-mechanism` line in place rather than adding a second `DISCHARGED` line, so no new hazard is introduced on that lock.
- `git diff --check 4f96989...HEAD` reports no whitespace errors; total line counts and the touched files' shape match the spec's DP-D line-count-preserving constraint (no line added or removed in any of the three prose files).

No overclaim or false claim found on either consumer class. Both Codex's own independent reading (per its agent_message trace, reaching APPROVE before I compared notes) and this reading converge on the same conclusion.

#### Blockers

None.

#### Major

None.

#### Minor / nits

None.

#### Adversarial findings

Not triggered — no blocker/major finding exists to warrant the opt-in adversarial pass (step 5's sole trigger condition).

#### Synthesis audit ledger

no severity changes

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE.

#### Fast-follow disposition

no fast-follow deferrals
