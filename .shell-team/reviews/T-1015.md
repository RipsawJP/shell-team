# QA verification — T-1015

Branch `feature/29-31-cutting-a-release`, HEAD `b4b86ef` (base `develop` @
`c9d88ae` + board repair `6750dcf`). Spec: `.shell-team/specs/T-1015-cutting-a-release.md`
(intent v1, aligned, 13 ACs — 11 mechanical, AC12/AC13 SKIP by design). This
task also carries a human-ratified amendment of `T-1000`'s frozen intent
(v6→v7, AC1/AC3/AC17). Working tree kept clean throughout (`git status --short`
shows nothing beyond this review file and the board edit); all mutation
testing ran on scratch copies under `$TMPDIR`, never on the real checkout.
This file replaces the engineer's disclosed hand-off placeholder (evaluated
in §5) with the real verification record. No real filesystem path or
environment-specific tooling claim is quoted anywhere below.

## 1. Mechanical acceptance criteria (T-1015)

```
$ bash bin/check-acs.sh --dry-run .shell-team/specs/T-1015-cutting-a-release.md
13 recognized, 0 unrecognized

$ bash bin/check-acs.sh .shell-team/specs/T-1015-cutting-a-release.md
check-acs: 11 passed, 0 failed, 2 skipped, 0 unrecognized (AC12/AC13 SKIP)
```
All 11 mechanical criteria pass already (the placeholder reviews file
satisfied AC9's `test -r` before this file replaced it; re-run after
replacement below confirms it still passes).

## 2. Both intent ledgers

```
$ bash bin/check-intent.sh .shell-team/specs/T-1015-cutting-a-release.md .shell-team/todo.md
check-intent: aligned: T-1015 v1 (2cb76bc36168d77a49fa1f79fc0de6b12ccbd4cb) matches ... -> rc=0

$ bash bin/check-intent.sh .shell-team/specs/T-1000-operating-conventions.md .shell-team/todo.md
check-intent: aligned: T-1000 v7 (983c62417b10325bcddb25c5868e95a645fc6bff) matches ... -> rc=0
```
Both ledgers aligned. `T-1000`'s is the one nothing else in the workflow
sweeps (no CI step runs `check-intent.sh` against a merged task's spec) —
confirmed directly rather than trusted to the hand-off's own report.

## 3. T-1000's amended criteria, executed directly

Ran `T-1000`'s own `check:` lines for AC1, AC3 and AC17 (already embedded in
the full run below, isolated here for the explicit ask):

```
AC1:  PASS (exit 0)  — five headings present, the one surviving absence assertion holds
AC3:  PASS (exit 0)  — the reworded board-hygiene pair, in execution order; both superseded openings absent
AC17: PASS (exit 0)  — CLAUDE.md's pointer names four destinations, each a real CONTRIBUTING.md heading
```

Ran the **full** `bin/check-acs.sh` over `T-1000`'s 24 criteria directly
(not only through T-1015's own nested AC6):

```
$ bash bin/check-acs.sh .shell-team/specs/T-1000-operating-conventions.md
20 PASS: AC1,AC3,AC4,AC5,AC9,AC10,AC11,AC12,AC13,AC15,AC16,AC17,AC18,AC19,AC20,AC21,AC24,AC25,AC26,AC27
4 FAIL:  AC2, AC14, AC22, AC23
```
24 total, matching AC6's pinned count. Spot-verified each of the four FAILs
against its declared reason in the spec's D2/disposition table, to confirm
none is new breakage:

- **AC2** (merge-point-scoped): its own positive control anchors a 4-line
  paragraph on `develop`; re-extracted that anchor directly —
  `git show develop:CONTRIBUTING.md` now yields **1 line** at that anchor
  (AC27's own rewrite collapsed it), so the `-eq 4` assertion fires exactly
  as the disposition table predicts.
- **AC14**: greps the board for `T-1000` in `- [ ]` Active form. Confirmed
  directly: `T-1000`'s board entry is `- [x] **T-1000** ...` under `## Done`
  today (closed out already), so the Active-form grep correctly finds
  nothing.
- **AC22** (merge-point-scoped): requires every file in `git diff --name-only
  develop` to be on `T-1000`'s own five-path allow-list. Confirmed directly:
  the current diff against `develop` includes `T-1015`'s own spec,
  provenance, reviews and interventions files — none on `T-1000`'s
  allow-list — so AC22 correctly reports the extra-path failure it is
  designed to report once a later task shares the same diff.
- **AC23** (stale, not fixed here): greps for two strings a later task
  (issue #30) removed. Confirmed directly: `feat/T-XXX-<slug>` is absent
  from `docs/workflow.md` and `Lint tasks/todo.md` is absent from
  `agents/pm-spec.md`; `docs/` shows zero diff against `develop` on this
  branch, confirming this task did not touch either file and did not cause
  the staleness.

All four are confirmed stale for their declared reasons, not new breakage.

## 4. T-1000 board-entry structural diff (mechanical, not just `check-board-headings.sh`)

`check-board-headings.sh` only diffs the *set* of `T-NNN` heading ids — it
cannot see a mutation inside a surviving entry's body. Extracted `T-1000`'s
full Done-entry text (from its heading line to the next top-level `- [`
line) at `6750dcf` and at HEAD, and diffed them directly:

```
$ diff <extracted-6750dcf> <extracted-HEAD>
14c14,15
<   - intent-hash (v6): 713e14f69c74474484d853fe2beb9ce3b10bf163
---
>   - intent-ratified (2026-08-02): v6→v7 — human GO in session ...
>   - intent-hash (v7): 983c62417b10325bcddb25c5868e95a645fc6bff
```
That is the **entire** diff — one hunk, nothing else in the 34/35-line
entry changed. Counted both sub-bullet classes with an anchored regex on
each side:

```
intent-hash    (anchored `^[[:space:]]*- intent-hash \(`):     1 before, 1 after   (replaced, never duplicated)
intent-ratified (anchored `^[[:space:]]*- intent-ratified \(`): 5 before, 6 after  (exactly one new record appended)
```
Confirms exactly the claimed shape: one hash bullet, six ratification
records, and no other byte inside the entry touched.

## 5. The engineer's disclosed deviation — the placeholder reviews file

Evaluated the reading against the spec's actual text, not against the
general precedent alone. `T-1013`'s precedent (reviews file left absent,
an expected disclosed FAIL for QA to resolve) is the *default* this
repository has followed across `T-1012`/`T-1013`/`T-1014`. `T-1015`'s own
Notes for engineer explicitly overrides that default in so many words:
*"The three record files under `.shell-team/` are **required by AC9** even
though the scope lock only permits them — write all three, including the
interventions record as a zero-entry sentinel if nothing interrupted the
task. This task dogfoods the line it adds."* No prior spec's Notes carried
this sentence; `T-1015`'s does, and "all three" unambiguously includes the
reviews file (only two files would be "two," not "all three"). Given this
task's whole deliverable is the convention that a task's records —
interventions included — are complete before its board entry moves, an
engineer leaving the reviews file absent at its own hand-off would be the
one instance where the new convention visibly failed to hold of the very
task introducing it. The placeholder's own text discloses plainly that it
is a stand-in, not a completed pass, so a reader cannot mistake it for a
real QA record. **Reading accepted as correct and well-grounded, not a
deviation** — this is a deliberate, textually-supported departure from
the `T-1013` precedent, not an engineer overreach.

This file replaces that placeholder in full, as instructed.

## 6. Empirical content check — the omission lens

Read the shipped `## Cutting a release` section (10 canonical bullets) and
checked every claim on it that is checkable against this tree today.

- **The version lives only in `.claude-plugin/plugin.json`**: confirmed —
  `plugin.json` carries `"version": "1.1.0"`; `.claude-plugin/marketplace.json`
  carries no `version` field anywhere in the file (`grep -i version` finds
  nothing).
- **`bash bin/check-readme-version.sh README.md README.ja.md`**: exit 0.
- **CHANGELOG style claims match the files' own opening declarations**:
  `CHANGELOG.md`'s own first paragraph states newest-first ordering and
  says the file was "scrubbed of internal task/issue references"; the
  Japanese file's opening paragraph makes the identical claim in Japanese.
  Both match the section's "newest entry first ... with no internal task
  or issue references, which is the style those files declare for
  themselves" exactly.
- **`git log <previous-tag>..develop --oneline` runs**: confirmed with
  `v1.1.0` as `<previous-tag>` — both `v1.0.0` and `v1.1.0` are real, local
  tags (`git tag -l`), and the command against `v1.1.0..develop` returns
  the real commit range, exit 0.
- **The close-out command form matches `bin/close-out.sh`'s real usage
  line**: the script's own usage comment is
  `close-out.sh --task T-NNN [--issue N] [--pr N] [--date YYYY-MM-DD] [--note TEXT]`;
  `T-1000` AC3's pinned bullet (unaffected, untouched by this task) uses
  the same three flag names (`--task`/`--issue`/`--pr`) in the same order.
  The placeholder digit count (`T-NNNN` in the doc vs. `T-NNN` in the
  script comment) is cosmetic — the script's actual validation regex is
  `^T-[0-9]+$`, unconstrained on digit count — not a functional mismatch.
- **The `release/vX.Y.Z` head-branch prescription is consistent with the
  board-hygiene bullets' branching rules**: `T-1000` AC3's branching bullet
  states branch names take the form `<type>/<slug>` with an explicitly
  **open** type set (`docs`, `chore`, `feature` observed so far, "the set
  is open"); `release/vX.Y.Z` fits that shape (`type` = `release`, `slug`
  = `vX.Y.Z`) without contradicting the open-set framing. Consistent.
- **The `develop`→`main`/tag/publish steps are execution-time-only**: read
  each for a checkable-and-false claim. None of the three canonical
  bullets asserts a fact this sandbox can falsify today — they are
  written as instructions ("promote," "tag," "publish"), not as
  present-tense claims about the current tree, matching Non-goals'
  "states what has to be true and what must be verified," not what has
  already happened. What only the v1.2.0 execution can validate: whether
  the promotion pull request is actually opened from a `release/vX.Y.Z`
  branch, whether `develop` survives the merge, whether the tag is
  correctly annotated and pushed, and whether the release notes are
  drafted, approved and published as described. None of that is
  checkable from this sandbox and none of it is asserted as already true.

**One genuine finding, from re-measuring the spec's own grounding rather
than the shipped prose.** R9's evidence row (the spec's internal
claim-to-evidence table, not the CONTRIBUTING.md bullet itself) reads:
*"the single pre-existing tag `v1.0.0` is annotated, so the annotated form
is this project's practice and not an invention."* Re-measured directly:
`v1.0.0` is genuinely an annotated tag object (`git cat-file -t v1.0.0` →
`tag`, carrying a tagger/date/message). But **`v1.1.0` — the release this
whole section is written from — is a lightweight tag**, not annotated
(`git cat-file -t v1.1.0` → `commit` directly; `git rev-parse v1.1.0` and
`git rev-parse v1.1.0^{}` are identical, which only happens with no tag
object wrapping the ref). R9's framing ("the single pre-existing tag") was
accurate when `T-1000` DP-7 first measured it (before `v1.1.0` existed);
by the time this spec was drafted (after `v1.1.0` had already been cut,
per the Problem section), a second tag existed and it broke the described
"practice." **This does not make the shipped, adopter-facing
`CONTRIBUTING.md` bullet false** — it prescribes using an annotated tag
going forward, as an instruction, and never asserts to the reader that
this has consistently been the practice — so no criterion is violated and
no reader is misled. But it is exactly the omission-class finding this
task names as its own top risk: the spec's internal grounding (feeding
AC13's attestation) is measurably stale on this one row, and the
inconsistency between the two real releases is worth a human's attention,
if only to decide whether v1.1.0's tag should be corrected or the
practice framing should be softened to "prescribed going forward" rather
than "this project's practice." **Not FAIL-driving** (AC13 is my own
judgment call and the criterion it feeds asks about the prose that ships,
which does not overclaim) — recorded here for the reviewer.

## 7. Mutation self-checks — independently reproduced

Both on scratch copies under `$TMPDIR` (never `git checkout`; real
checkout confirmed clean via `git status --short` before and after).

**Mutation — AC7's second-hash-bullet (structural).** Appended a second,
well-formed `- intent-hash (v8): 000...0` sub-bullet directly under the
real `- intent-hash (v7): ...` line in a scratch copy of the board.
```
$ bash bin/check-intent.sh .shell-team/specs/T-1000-operating-conventions.md <mutated board>
check-intent: structural: expected exactly one well-formed intent-hash
record for T-1000 ... (found valid=2 malformed=0) -> rc=2
```
Confirms the checker's own exit is the **structural 2**, not a drift 1, as
AC7 requires. Re-ran AC7's own criterion logic against the same mutated
board: the anchored hash-count assertion (`grep -cE '^[[:space:]]*-
intent-hash \('`) returns 2, failing the `= 1` check — AC7 goes red on
exactly the clause it names.

**Mutation — an AC3 canonical-byte alteration.** In a scratch copy of
`CONTRIBUTING.md`, inserted one extra space inside the tag-bullet's own
sentence (a single-character-class change, not a word change). Ran AC3's
own `check:` logic against the mutated file: the `grep -qxF` exact-match
on that specific canonical line failed while the other two bullets still
matched — AC3 goes red on precisely the mutated line, confirmed via the
isolated per-bullet grep output. Restored the file byte-for-byte
afterward and confirmed with `cmp` against the real, unmutated
`CONTRIBUTING.md`: identical. The real checkout was never touched by
either mutation.

## 8. Full CI-ordered local run + seam gates

Ran every step in `.github/workflows/check-handoff.yml`'s own order,
serially. All 55 steps passed (0 failures). `shellcheck --version` on this
host = 0.11.0, matching the workflow's pin; `develop`/`origin/develop`
identical locally, so `--base develop` runs are equivalent to CI's
`--base origin/${GITHUB_BASE_REF:-develop}`.

```
$ bash bin/check-pii-shapes.sh --base develop
check-pii-shapes: clean (no PII-shaped bytes found)   (rc=0)

$ bash bin/check-provenance.sh .shell-team/provenance/T-1015.md
check-provenance: conformant: .shell-team/provenance/T-1015.md (2 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1015 .shell-team/interventions/T-1015.md
check-interventions: conformant: .shell-team/interventions/T-1015.md (0 entries, 1 sentinel) -> rc=0
```

Read both provenance decisions in full. **Decision 1** (the unpinned
intro sentence's wording against AC11's vocabulary lock): confirmed the
shipped intro line ("The steps of a release, written from the release
this repository has actually run.") contains none of `host`/`terminal`/
`laptop` (case-insensitive) nor the standalone word `gh`, matching the
decision's own reasoning and AC11's own live PASS. **Decision 2** (the
placeholder-reviews-file deviation): evaluated independently in §5 above,
before reading this decision's text in detail, and reached the same
conclusion the decision states — accepted, not a deviation.

## Verdict

All mechanical criteria pass (11/11 on `T-1015`, both `check-intent.sh`
ledgers aligned, 20/24 must-pass on `T-1000` with the 4 stale-by-design
failures each confirmed for their declared reason). The `T-1000` board
entry's structural diff is exactly the claimed shape — one hash bullet
replaced, one ratification record appended, nothing else touched. The
engineer's placeholder-reviews-file reading is accepted as correct and
replaced with this record. Every checkable claim in the new section holds
against the real tree today; the `develop`→`main`/tag/publish steps make
no checkable-and-false claim and are correctly left for the v1.2.0
execution to validate. One real, non-FAIL-driving finding is recorded: the
spec's own R9 grounding ("the annotated form is this project's practice")
is measurably stale now that `v1.1.0`'s own tag is lightweight, though the
shipped instruction to the reader is unaffected and no criterion is
violated. Both independently-reproduced mutations behave exactly as
claimed. Full CI green; seam gates conformant.

