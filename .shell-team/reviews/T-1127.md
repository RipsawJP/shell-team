### Codex Review verdict: REQUEST_CHANGES
- Task: T-1127
- Codex model: gpt-5-codex
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 6 files, +146/-2 lines (base `8cfe1ea`, stacked behind PR #446)

#### Blockers
- none

#### Major
- `.shell-team/specs/T-1127-history-status-stale-version.md:65` — AC5's four-way scope-lock pipeline (`git diff`/`git diff --cached`/`git diff`/`git ls-files` piped into `sort | tr`) has no `pipefail` and no per-command status check, so a failed inner git invocation does not propagate — the outer `test "$got" = '...'` can still evaluate against a partial `$got` and spuriously pass or fail without surfacing the failure. Adversarial pass reproduced a reachable fail-open path under abnormal Git configuration/index state (e.g. a malformed `core.excludesFile`): the exact AC5 command and `bin/check-acs.sh` both still reported PASS while Git itself emitted fatal diagnostics on stderr that the checker discards on exit 0. This is a concrete instance of this repo's own stated rule ("Checkers must fail closed" — `CLAUDE.md`) being violated in a check line this task's own frozen spec adds. Suggested fix: `set -o pipefail` inside the check's subshell, or capture and test each git invocation's own exit status before concatenating.
- `.shell-team/interventions/T-1127.md:4` — carries the zero-entry sentinel `no interventions occurred`, but the board (`todo.md:15`, commit `e9fc79e`) shows the freeze attestation was corrected before engineering started: an initial claim that AC5 was "green at base" was replaced with "red at base by design" after live measurement. `skills/run/SKILL.md:28` defines `assumption-contradicted` as exactly this — "a measurement contradicts an assumption you have stated" — self-checked at every phase transition, with no carve-out for a correction made before hand-off. The interventions file as committed misstates the audit trail. Suggested fix: replace the sentinel with a dated `assumption-contradicted` entry describing the AC5 base-side correction.

#### Minor / nits
- `.shell-team/todo.md:32` — the QA line's phrasing `AC1 exit 1 (clean absence, correct per contract) → PASS` mixes two different exit-code axes in one clause (the inner `git grep` return code, which is 1 for a clean absence, vs. the outer check's own exit code, which `bin/check-acs.sh` independently reports as `AC1: PASS (exit 0)`), while the adjacent `AC2 exit 0 → PASS` etc. use "exit N" to mean the outer check's own code. Live re-run confirms both readings are individually correct, and the line's own parenthetical + "→ PASS" already disambiguate it, so this is not inaccurate evidence — just an internally inconsistent shorthand that a fast skim could misread as a self-contradiction. Suggested fix: rephrase to `AC1: inner grep rc=1 (clean absence) → outer check exit 0 → PASS` or similar, so "exit" is used consistently across the list.

#### Adversarial findings
- Re-tested both Major findings above with a devil's-advocate pass on the same two files. Finding 1 (AC5 pipefail) survived: reproduced a reachable fail-open path via abnormal Git config/index state, not merely shell theory — verdict left at major. Finding 2 (missing intervention entry) also survived as a real, structurally valid violation of the `assumption-contradicted` class per `skills/run/SKILL.md:28`, but the adversarial pass argued for a downgrade to minor on grounds of non-blocking impact (correction was immediately visible in `todo.md` itself, occurred pre-hand-off, and changed no gate result) — see ledger row 2 for why this review keeps it at major regardless.
- No adversarial finding surfaced against the delivered doc sentences themselves (`docs/history.md` / `docs/history.ja.md`): `.claude-plugin/plugin.json:4` was independently confirmed to carry `"version": "2.5.1"`, so the replacement anchor is a real, currently-true pointer, not a claim that will silently go stale the way the sentence it replaces did.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | AC5 scope-lock pipeline lacks pipefail / per-command status checks | primary: major / adversarial: major (reproduced reachable fail-open path) | major | Both passes agree; matches this repo's own stated fail-closed rule and the failure path was demonstrated, not merely theoretical. |
| 2 | `interventions/T-1127.md` sentinel omits the freeze-attestation `assumption-contradicted` event | primary: major / adversarial: minor (non-blocking — visible elsewhere, no gate impact) | major | Kept at major over the adversarial pass's downgrade: `skills/run/SKILL.md:28`'s self-check obligation is unconditional at every phase transition, with no exemption for "no downstream impact" — the record is inaccurate regardless of whether anything downstream depended on it, and the fix (one dated entry) is trivial. |
| 3 | QA board line's `AC1 exit 1 ... → PASS` phrasing | primary: major (P2) | minor | Downgraded on this reviewer's own re-run of `bin/check-acs.sh`: both readings (inner grep rc=1, outer check exit 0) are independently correct and the line's own parenthetical already disambiguates them — not inaccurate evidence, just an internally inconsistent shorthand across an otherwise-uniform list. |

#### Recommendation
- REQUEST_CHANGES → set back to READY_FOR_ENG with linked findings (both Majors above; the AC5 fix requires a spec correction + re-freeze of that one check line, the interventions fix is a single dated entry + commit)

#### Fast-follow disposition
- Minor 1 (QA board line phrasing) — won't-fix for this round: the line is not factually wrong once the parenthetical is read, and the rest of this task's records are otherwise accurate; not worth blocking on a wording nit in a line QA already committed. Fine to leave as-is or clean up opportunistically in a later pass.
