# T-1095 — Codex Reviewer records

## Spec review

### Codex Spec-Review verdict: REQUEST_CHANGES
- Task: T-1095
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — this run's own JSONL prints no genuine model-identifier field; the free-form `codex exec` prompt path was used, not `codex exec review`)
- Mode: Spec-review (specify seam, T-1092), round 1, first invocation for this task
- Consumer class: shipped, adopter-executed surfaces (`skills/run/SKILL.md`, `skills/goal/SKILL.md`, `templates/prompt-blocks/`, `bin/rework-digest.sh`, `docs/workflow*`) — the spec declares an adopter surface under **AC12**, so the 2026-08-22 dev-scaffold carve-out does not apply. Full adversarial depth earned per the spec's own `## Verdict scope`.
- Input reviewed: `.shell-team/specs/T-1095-means-ends-reflection.md` (frozen intent block + declaration region, read in full including `## Input space`, `## Parallel-surface symmetry`, `## Singular-determiner sweep`, `## Assumptions`, `## Blast radius`, `## Verdict scope`), cross-read against the Routing Map (`t1095-routing-map.md`), issue #346's relayed body, `bin/rework-digest.sh` (full file), `tests/rework-digest/run.sh` (full file), `templates/prompt-blocks/registry.txt`, `skills/run/SKILL.md` (steps 5/6, lines 74–101, confirmed against the spec's own line citations), `skills/goal/SKILL.md` (Decide list, lines 229–245), and `.shell-team/todo.md`'s T-1093 loop-escape record (lines 2301–2311) plus commit `a0c3c38`. No `--base`, no branch diff — this pass runs before any implementation exists.
- Verification mode: static-only.
- issue-canon note: internal consistency verified / issue-canon match environmentally-unverified (relies on orchestrator's primary confirmation) — the spec's own `## Assumptions` records the coordinating session fetched issue #346 live via the GitHub API at freeze-prep time (verbatim, no edits since fetch); this role holds no GitHub-reading tool and defers to that confirmation.

#### Domain-premise findings (Codex primary pass, normalized and synthesized against my own reading)

1. **Major** (verdict-scope item 1 — escalation-boundary premise, vacuous-truth branch). The Goal's execution predicate — "(1) every item of the never-dropped set green, (2) every finding in the firing class against auxiliary components, (3) a pre-priced disposition existing for each of those components" — has no stated behavior for a firing with **zero classified findings** (e.g. `STOP:budget_exhausted` or `STOP:guard_error` reached before any rework round was ever classified). Conditions (2) and (3) are vacuously true over an empty set, yet nothing in the frozen text, `## Input space`'s reachable-class list, or the canonical-block fragments required by **AC2** states that an empty finding set fails closed to escalation rather than to an undefined "earliest of them." This is exactly the class of gap the repo's own checker discipline ("a check that cannot evaluate its input reports an error; it never silently passes") is written to close, and the norm text is a duty rather than a checker for precisely this reason — which makes the omission more consequential, not less. In practice the risk is bounded (there is no disposition text to name without a matching finding, so a diligent orchestrator reading the prose would not manufacture one), but the frozen text does not say so, and this is the highest-stakes surface in the operating contract to leave to inference.
   - Route-back ask: add one sentence to the canonical block (and therefore to **AC2**'s required-fragment list) stating that an empty or unclassified finding set at any firing point fails closed to escalation, never to execution.

2. **Minor** (verdict-scope item 1 — escalation-boundary premise, disposition side-effects). A reversible, auxiliary-component drop can still have side effects beyond the finding itself — this task's own named Droppable-1st disposition is "goes to a fresh issue carrying both rounds' findings as its requirement list," which creates new tracked work. Downgraded: filing an issue without prior approval is already-normalized, within-authority practice in this repository's own operating rules (CLAUDE.local.md's propose-and-proceed norm), and the norm's three exceptions already stop genuinely destructive/irreversible actions; a future task's pre-commitment naming a disposition with broader side effects is that task's own authoring responsibility, not a defect in this general mechanism.

3. **Major** (verdict-scope item 2 — n=1 generalization, rule shape). The frozen text states n=1 honestly (**AC11**'s `n=1` anchor, the `## Verdict scope`'s own disclosure), but the rule's *shape* ships as an unconditional, permanent standing authority from a single observed firing — no canary count, no staged trust period, and no re-evaluation trigger tied to firing volume, unlike several of this spec's own other norms (e.g. `## Assumptions`' relayed-premise rows, which are explicitly re-measured; **AC13**'s own declared staleness trigger). The downside is partially self-bounding — the three exceptions still gate every firing regardless of count, and every execution is recorded on the board for retro visibility — but that bound is not stated as the justification; the text simply asserts n=1 and proceeds. This is squarely what verdict-scope item 2 asks to be judged honestly.
   - Route-back ask: either add a stated re-evaluation trigger (e.g., "after N executions, or at the next retro touching this pattern, re-read whether the observed firings still match T-1093's shape"), or add one sentence naming the self-bounding argument above explicitly so a reader does not have to reconstruct it.

4. **Minor** (verdict-scope item 3 — exception-set completeness, malformed/ambiguous pre-commitments). Codex raised ambiguous, contradictory or stale pre-commitments as an unhandled firing state. Downgraded: the spec's own `## Input space` Out-of-scope list already names this exact scenario and gives a reason ("that is a defect in the spec that wrote it, caught by the freeze sweep for that task, and this norm does not attempt to arbitrate it at firing time"). The reasoning is defensible — a frozen pre-commitment cannot become ambiguous through later drift without a class-B/class-M re-freeze, which has its own gate — so this is a disclosed residual, not a hidden one.

5. **Minor** (verdict-scope item 3 — the four answers' grounding). Codex questioned whether the four reflection questions are answerable from a single readable artifact, noting the digest itself carries no component-to-disposition mapping or current never-dropped-set status. Downgraded: the mapping lives in the task's own frozen pre-commitment text (a git-tracked artifact) and the never-dropped-set status is read from the task's own current QA/review record — both genuinely readable at firing time, and T-1093's own worked example (cited by **AC11**) answered exactly this way by citing spec-review/QA history directly. The mechanism requires synthesis across more than one artifact rather than one packaged lookup, which is a real but non-fatal design characteristic.

6. **Nit** (verdict-scope item 1 — question 4 is not a gating conjunct). The disposition executes when conditions (1)–(3) hold; the fourth reflection question (aggregate cost of rounds so far vs. the pre-priced exit) is asked but never gates execution. This is intentional under the spec's own stated philosophy ("At the trigger the drop is the DEFAULT, executed on the orchestrator's own authority, not an option presented for negotiation") — the decision was already made at freeze time, so re-litigating it on cost at firing time would contradict the norm's own point. Worth one clarifying clause in the canonical block that question 4 is recorded for audit, not gating, so a future reader does not mistake the omission for an oversight.

7. **Minor** (verdict-scope item 4 — `--reflection` enum, non-mutually-exclusive escalation reasons). The five enum values do not define precedence when more than one escalation condition holds simultaneously (e.g. a firing that is both `escalated-never-dropped` and `escalated-primary-not-green`). This only affects which single audit-trail value gets recorded, not whether escalation correctly occurs — every one of the failure branches still routes to escalation — so the safety property holds even though the audit label is under-specified.

8. **Minor** (verdict-scope item 4 — `--reflection` test coverage). **AC9**'s fixture only exercises `--reflection` together with `--trigger same-class-2`; no case in **AC9** or `tests/rework-digest/run.sh` exercises the field together with `--stop-reason` (the STOP-mode digest), even though the Goal text says the digest "optionally gains" the field for the reflection generally. This is a real coverage gap, scoped down by the field's own status as the pre-commitment's Droppable-1st component.

9. **Minor** (verdict-scope item 4 — AC10's mutation resistance). **AC10** greps stderr for the literal phrase `golden output diff` rather than independently re-verifying the underlying `cmp`-based comparison `tests/rework-digest/run.sh:39–54` performs; a future edit that kept the phrase but weakened the comparison would not be caught by **AC10** alone. This is subsumed by the `mechanism` verification class's own standing mutation-probe obligation (already declared in the spec's `- verification-class:` line) rather than being a defect specific to this criterion.

10. **Major** (verdict-scope item 5 — parallel-surface semantic equivalence). **AC4**/**AC5**/**AC6** bound their search to a region (step 5, step 6, the Decide list) and require the canonical fragments to appear as fixed-string substrings within it, plus co-located pre-existing anchors. That still does not exclude a negated or hedged rewrite from passing: a bullet reading "the disposition does **not** execute on the loop's own authority without first confirming with the operator" contains the literal substring `executes on the loop's own authority` and would satisfy **AC4** while inverting the norm's meaning. The region-scoping and required anchors make an accidental collision unlikely, but they do not make a deliberate or careless paraphrase-with-negation detectable — precisely the "textual presence vs. semantic equivalence" gap verdict-scope item 5 names as the thing to check, and this is a concrete, constructible counterexample rather than a hypothetical one.
    - Route-back ask: either note explicitly (Notes for engineer) that a mutation probe negating each required fragment is owed under the `mechanism` verification class already declared, or tighten one or two of the highest-leverage fragments (e.g. "executes on the loop's own authority") to include enough trailing context that a negation could not share the same substring.

11. **Major** (verdict-scope item 6 — structural negatives, dispatch-axis loophole). **AC7**'s diff-wide zero is scoped to lines matching `- dispatch-axis:` / `- dispatch-rule:` literally. It does not check for a **new** axis introduced through the pre-existing `- dispatch:` board-record key (the format `- dispatch: <axis-name> — …` this task's own board entry already carries four of) — nothing in **AC7**, **AC13**, or any other criterion forbids a fifth `- dispatch: <novel-name> — …` sub-bullet being added to T-1095's own board entry, since `.shell-team/todo.md` is already in **AC13**'s allow-list for ordinary board edits. The practical risk is bounded (this diff is authored under the same task's own engineer instructions, which do not call for a fifth line), but the check as written would not catch it if it happened.
    - Route-back ask: extend **AC7** (or add a clause to **AC13**) to assert that any `- dispatch:` sub-bullet added under T-1095's own board entry uses only the four already-elected axis names (`implement`, `verify`, `specify`, `spec-review`).

12. **Minor** (Non-goals vs. AC7 correspondence — lessons-corpus citation). The Non-goals text claims "no shipped line of this task's norm text cites a lessons entry as its authority (**AC7**)," but **AC7** only asserts `.shell-team/lessons.md` and the four playbook files are byte-identical to their base blobs — it does not check whether the **new** canonical block or consumer-region text cites `.shell-team/lessons.md` by reference. A citation (not a functional dependency) could be added without tripping any criterion. Low practical risk given the explicit Non-goals instruction not to do this, but the textual claim and the cited criterion do not actually match.

13. **Rejected — already disclosed, no additional severity** (verdict-scope's own exclusion list). Codex flagged that **AC8** does not mechanically rule out a hypothetical new human gate reusing an existing `templates/liveness-reasons.txt` token. The spec's own **AC8** body states this residual almost verbatim ("a hypothetical new gate reusing an existing reason token such as `gate-escalation` would move neither file... the sufficient half is the shipped text's own assertion... plus a reading the cross-provider review performs"), and `## Verdict scope` explicitly names "the necessary-but-not-sufficient reach of AC8" as out of the verdict's scope, stating a finding there is "a note or a fast-follow, not a verdict-gating Major." Per the spec's own governing rule, this is recorded as a fast-follow, not scored.

**Totals**: 4 Major (1, 3, 10, 11), 7 Minor (2, 4, 5, 7, 8, 9, 12), 1 Nit (6), 1 rejected/fast-follow (13) — 13 Codex-raised items, all accounted for.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | vacuous-truth branch (zero-finding firing) undefined | Codex: P1 (blocker per normalization map) | major | genuine gap in the highest-stakes area, but practically self-bounding (no disposition text to name without a finding to match) — not a fundamental break |
| 2 | opening a fresh issue is a disposition side-effect beyond the finding itself | Codex: P1 (blocker per normalization map) | minor | this task's own actual Droppable-1st disposition (file a fresh issue) is already-normalized within-authority practice in this repo's own operating rules; a future task's broader side-effect risk is that task's own authoring responsibility |
| 3 | n=1 rule shape has no canary/re-evaluation trigger | Codex: P1 (blocker per normalization map) | major | real design-hardening ask; downside already partially bounded by the untouched exception set and board-visible logging, so major rather than blocker |
| 4 | ambiguous/contradictory pre-commitment unhandled at firing time | Codex: P1 (blocker per normalization map) | minor | spec's own Input space already discloses this exact scenario with a defensible reason (freeze-time defect, not a firing-time one) |
| 5 | four answers not grounded in one packaged artifact | Codex: P1 (blocker per normalization map) | minor | answers are grounded across multiple genuinely-readable artifacts (pre-commitment text + QA/review history + digest); T-1093's own worked example already answered this way |
| 6 | cost question (Q4) is not a gating conjunct | Codex: P2 (major per normalization map) | nit | intentional under the spec's own "drop is DEFAULT, not negotiable" philosophy; worth a one-line clarification only |
| 7 | enum values not mutually exclusive | Codex: P2 (major per normalization map) | minor | affects only which audit label is recorded, never whether escalation correctly occurs |
| 8 | --reflection untested in --stop-reason mode | Codex: P2 (major per normalization map) | minor | real coverage gap, scoped down because the field is the pre-commitment's own named Droppable-1st component |
| 9 | AC10 mutation resistance (stderr-phrase only) | Codex: P2 (major per normalization map) | minor | subsumed by the mechanism-class's own standing mutation-probe obligation, already declared |
| 10 | AC4/AC5/AC6 substring match does not exclude negation/paraphrase | Codex: P1 (blocker per normalization map) | major | concrete, constructible counterexample (a negated sentence containing the same fragment) against the exact question verdict-scope item 5 asks; fixable pre-freeze at low cost |
| 11 | AC7 does not cover a 5th axis introduced via the `- dispatch:` board key | Codex: P1 (blocker per normalization map) | major | concrete, demonstrable gap: a `- dispatch: <novel-name> — …` sub-bullet on T-1095's own board entry would not be caught by AC7 or AC13 as written |
| 12 | Non-goals' lessons-citation claim not actually verified by AC7 | Codex: P2 (major per normalization map) | minor | real textual/AC mismatch, low practical risk given explicit Non-goals instruction |
| 13 | AC8 necessary-but-not-sufficient reach | Codex: P1 (blocker per normalization map) | rejected (fast-follow) | spec's own Verdict scope names this exact residual as explicitly out of the verdict's scope; disclosed almost verbatim in AC8's own body |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec**. Resolution per the spec-review-mode Route-back paragraph: `pm-spec`'s hand-off for this task reports `- entry-mode: pm-authored`, cross-checked against the Routing Map's own printed `specify — pm-authored` decision (`t1095-routing-map.md`, `### Dispatch`) — the two sources agree, so the route-back target is `pm-spec`.
- Ask: close findings 1, 3, 10 and 11 (all Major) before the freeze proceeds:
  1. State that an empty/unclassified finding set at any firing point fails closed to escalation.
  2. Add a stated re-evaluation trigger for the n=1-grounded standing rule, or state the self-bounding argument explicitly.
  3. Note the mutation-probe obligation against negated/paraphrased rewrites of the canonical fragments (or tighten the fragments themselves), so **AC4**/**AC5**/**AC6** cannot be satisfied by an inverted-meaning rewrite.
  4. Extend **AC7** (or **AC13**) to forbid a fifth `- dispatch:` sub-bullet with a novel axis name on this task's own board entry.
- This APPROVE/REQUEST_CHANGES gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- Finding 13 (AC8 necessary-but-not-sufficient reach): won't-fix for this task — explicitly named as out of this verdict's scope by the spec's own `## Verdict scope` and already disclosed in **AC8**'s own body.
- Findings 2, 4, 5, 7, 9, 12 (Minor): won't-fix as blocking items for this freeze — each is either already-normalized practice (2), an already-disclosed residual (4), grounded via a multi-artifact synthesis already demonstrated by T-1093's worked example (5), a recording-only ambiguity that never weakens the escalation safety property (7), subsumed by the mechanism class's own standing mutation-probe obligation (9), or a low-practical-risk textual/AC mismatch given the explicit Non-goals instruction (12) — none block the freeze on their own; pm-spec may fold any of them into the same revision that addresses the four Majors.
- Finding 6 (Nit, Q4 not gating): won't-fix — intentional by design; a one-line clarification is welcome but not required.
- Finding 8 (Minor, --reflection untested in --stop-reason mode): recommend as a fast-follow to file-an-issue at implementation time if not folded into the same revision, since it is a real but scoped-down gap in the pre-commitment's own named Droppable-1st component.

Raw Codex output: `.shell-team/reviews/T-1095-codex-specreview.txt` (final message), `.shell-team/reviews/T-1095-codex-specreview.jsonl` (full event stream).

---

### Codex Spec-Review verdict: REQUEST_CHANGES (round 2)
- Task: T-1095
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — this run's own JSONL prints no genuine model-identifier field; the free-form `codex exec` prompt path was used, not `codex exec review`)
- Mode: Spec-review (specify seam, T-1092), round 2, revised spec in response to round 1's REQUEST_CHANGES
- Consumer class: unchanged from round 1 — full adversarial depth (adopter surface declared under **AC12**).
- Input reviewed: the revised `.shell-team/specs/T-1095-means-ends-reflection.md` (306 lines, re-read in full: `## Goal`'s frozen intent block including the four new/expanded paragraphs — "The empty-finding-set branch", "Question (4) is recorded, not gating", "What bounds a standing rule grounded in one live firing" — `## Input space`'s three new reachable classes, **AC2**/**AC4**/**AC5**/**AC6**/**AC7**/**AC11**'s revised check bodies read verbatim, `## Verdict scope`'s new "Accepted residuals from spec-review round 1" subsection, and `## Notes for engineer`'s new mutation-probe and exact-string guidance), cross-read against `skills/run/SKILL.md` and `skills/goal/SKILL.md`'s current text (to confirm the three forbidden negation strings are genuinely absent today) and the board's existing `- dispatch:` sub-bullet transcription format (`.shell-team/todo.md`, several recent entries, to confirm AC7's new regex matches real transcription).
- Verification mode: static-only.
- Scope for this round, as set by the coordinating session: verify each of the four round-1 Majors' fixes actually closes the finding (not just mentions it); check the fixes for newly introduced defects; confirm the deferred Minors' reasons are honest. Out-of-scope set unchanged from round 1.

#### Verification of the four round-1 Majors

1. **Major 1 (vacuous-truth branch for empty/unclassified finding sets) — CLOSED.** The Goal now states explicitly: "escalation to the operator stays mandatory when... the finding set is empty or unclassified — an empty or unclassified finding set escalates and never executes a disposition, because conditions (2) and (3) are vacuously true over an empty set and a predicate that cannot evaluate its input must report that rather than pass." This sentence is a required fragment in **AC2**, **AC4**, **AC5** and **AC6**, and `## Input space` gained matching reachable-class bullets for the empty case, the not-yet-classified case, and the multiple-conditions-at-once case. Codex raised a residual concern about a **mixed** set (some findings classified against an auxiliary component, one left unclassified) not being unambiguously "empty or unclassified" under a literal reading. On inspection this is not a fresh vacuous-truth hole: condition (2), "every finding in the firing class against auxiliary components," is a universal claim that an unclassified member cannot satisfy by construction, so a mixed set already fails condition (2) under ordinary quantifier logic and forces escalation without needing the new exception sentence to cover it. Downgraded to a wording-clarity Minor (below), not a continuing Major.

2. **Major 2 (n=1 generalization has no bounding shape) — PARTIALLY CLOSED, one real off-by-one remains.** The new paragraph ("What bounds a standing rule grounded in one live firing") adds a stated re-evaluation trigger: "while fewer than **three** live firings are recorded, this norm's standing is re-read at each retro that follows a cycle in which it fired... a re-read finding a firing whose shape departs from T-1093's returns the norm to planning." Read literally and traced through the sequence: after firing #1 (count=1, "fewer than three" holds) the next retro re-reads; after firing #2 (count=2, still "fewer than three") the next retro re-reads; but after firing #3 itself (count=3, no longer "fewer than three"), the retro that follows **that** firing is exactly the one the condition excludes — the rule's own stated rationale for the threshold ("second establishes, third confirms") requires firing #3 to be checked for shape-consistency, yet the literal wording of "while fewer than three are recorded" means the check that would confirm or refute firing #3 never fires. This is a genuine boundary defect in the frozen text, not a hypothetical: the norm as written graduates to permanent, unreviewed status exactly at the firing whose review was supposed to be the confirming one.
   - Route-back ask: change the condition to cover the retro following the third firing too — e.g. "at each retro that follows a cycle in which it fired, while at most three live firings are recorded" (or, more directly, "including the retro following the third firing").

3. **Major 3 (AC4/AC5/AC6 substring matching didn't exclude a negated/inverted rewrite) — NOT CLOSED. A concrete, mechanically-verified bypass remains.** The fix joined the disposition fragment into one longer required substring and added three forbidden negation strings as required-zero-counts. I constructed the following sentence and ran it directly against **AC2**'s own check logic (fragment presence + the five zero-count assertions) using the exact `grep -qF` / `grep -cF` invocations the criterion specifies: *"The rejected interpretation is that the disposition executes on the loop's own authority and is recorded on the board with the four answers as its ground; execution instead requires operator authorization."* This sentence (a) contains the full joined required fragment verbatim as a substring, and (b) contains none of the three forbidden strings (`not execute on the loop's own authority`, `never executes on the loop's own authority`, `without first confirming with the operator`). Run mechanically: `rc=0` — **AC2 (and by the same logic AC4/AC5/AC6, which reuse the identical fragment and zero-count set) would pass while the sentence asserts the opposite of the norm.** This is not a contrived formatting trick; it is fluent, natural prose a careful writer might produce while explaining a design's rejected alternative. The mutation-probe obligation the spec now names (`## Notes for engineer`'s new bullet) tells the engineer to test *deletion* and *simple negation* of the load-bearing verb, but does not anticipate a "quoted as the rejected reading" construction, so the probe as specified would not have caught this either.
   - Route-back ask: the fixed-string approach cannot close this class of counterexample by adding more forbidden phrases (the space of hedges and reframings is open-ended). Either (a) require the joined fragment to appear as a **freestanding sentence** — bounded by sentence-start/sentence-end punctuation in the extracted region, so it cannot sit inside a larger quoting/rejecting clause — or (b) accept this as a disclosed residual of `contain`-mode's fixed-string design (same shape as the already-disclosed `registry.txt:25–29` and `contain`-mode limits) and rely on the cross-provider review's own judgment at the delivered-change round to catch a materialized instance, stating that reliance explicitly rather than implying **AC2**/**AC4**/**AC5**/**AC6** mechanically guarantee semantic correctness.

4. **Major 4 (AC7's dispatch-axis check didn't cover a 5th axis via the `- dispatch:` board key) — SUBSTANTIALLY CLOSED, one narrow residual.** The new clause (`nd` = count of added `- dispatch: ` lines, `na` = count of those matching the four elected axis names, asserted equal, with a positive control that `nd >= 1`) correctly catches a canonical fifth-axis line and correctly catches a different em-dash character (both verified by construction against the check's own regex). I confirmed by direct mechanical test that a doubled-space variant (`-  dispatch: fifth-axis — …`, two spaces after the hyphen) is invisible to **both** `nd` and `na` — neither count sees it, so the equality holds and **AC7** passes despite the rogue line's presence. This is a real gap in the regex's whitespace tolerance, but the practical risk is low: every existing board transcription I checked (a dozen-plus recent `- dispatch:` entries) uses exactly one space after the hyphen, so this requires a deliberately malformed line that would look visibly broken to anyone scanning the diff. Downgraded to Minor (below), not a continuing Major.

#### Additional finding (newly raised this round, not a round-1 carryover)

5. **Minor** (verdict-scope item 6 adjacent — authorship-accountability at the Review firing point). **AC5**'s required-fragment list (step 6, Review) does not include the `AI self-discipline` anchor that **AC4** (step 5, Validate) requires. This mirrors the already-disclosed, pre-existing step-6 authorship-declaration-bullet asymmetry (`## Non-goals`: "No retro-fixing of the step-6 authorship-declaration asymmetry"), which round 1 correctly left alone as out of this task's stated scope. What is newly true because of *this* task, though, is that step 6 is now also a surface where the disposition rule can **autonomously execute** — previously that surface only composed escalation messages to a human, where the authorship-declaration duty's absence was lower-stakes. The frozen Goal's own claim ("every report that names an executed disposition satisfies the existing authorship-declaration duty") is therefore not mechanically testable at the Review firing point the way it is at Validate and Decide. Low risk (the four reflection answers are still required and recorded regardless), but a cheap, mechanical fix is available.
   - Suggested fix: add `AI self-discipline` to **AC5**'s required-fragment list, mirroring **AC4** and **AC6** (both of which already require it).

#### Sanity checks performed independently

- **Negation-string absence, confirmed live**: `grep -cF` for all three forbidden strings (`not execute on the loop's own authority`, `never executes on the loop's own authority`, `without first confirming with the operator`) against the current `skills/run/SKILL.md` and `skills/goal/SKILL.md` returns 0 for all six file×string pairs — the zero-count assertions in **AC2**/**AC4**/**AC5**/**AC6** measure this task's own future text, not pre-existing prose, as claimed.
- **AC7's `- dispatch:` regex against real transcription**: checked against a dozen-plus recent board entries' actual `- dispatch: <axis> — <value> — …` lines; the pattern `^\+[[:space:]]*- dispatch: (implement|verify|specify|spec-review) — ` matches every one of them, so the fix is not trippable by legitimate transcription (confirmed the concern named in this round's own scope item (c)).
- **Accepted residuals from spec-review round 1** (the six-item subsection under `## Verdict scope`): re-read against my own round-1 reasoning. All six restate reasoning I gave or accept in round 1 without material change; none hides a re-opened Major. The two Codex flagged as still-concerning (the ambiguous/contradictory pre-commitment, and **AC8**'s necessary-but-not-sufficient reach) are re-litigations of round-1 findings 4 and 13 respectively — both already carried a defensible, disclosed reason in round 1 and nothing in the revision changed the facts underlying that reasoning; **AC8**'s residual is additionally named explicitly out of this verdict's scope by the spec's own `## Verdict scope` text, which this review does not re-litigate absent new grounds.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | Major 1 fix — mixed (partially-classified) finding set ambiguity | Codex: P1 (blocker per normalization map) | minor | condition (2)'s ordinary universal-quantifier logic already forces escalation for a mixed set; not a fresh vacuous-truth hole, a wording-clarity ask |
| 2 | Major 2 fix — re-evaluation trigger has an off-by-one excluding firing #3's own confirming retro | Codex: P1 (blocker per normalization map) | major | mechanically traced through the sequence; the rule's own stated rationale (confirm at the third firing) is defeated by its own literal threshold wording |
| 3 | Major 3 fix — "rejected interpretation" sentence bypasses AC2/AC4/AC5/AC6 while inverting meaning | Codex: P1 (blocker per normalization map) | major | mechanically verified (rc=0) against the actual check logic; natural, plausible prose, not a contrived edge case; the mutation-probe note does not anticipate this shape |
| 4 | Major 4 fix — double-space-after-hyphen variant of `- dispatch:` invisible to AC7's nd/na counts | Codex: P1 (blocker per normalization map) | minor | mechanically confirmed, but requires a deliberately malformed line no real transcription produces; substantially closes the realistic risk the round-1 finding raised |
| 5 | AC5 omits the `AI self-discipline` anchor that AC4/AC6 require (step-6 authorship accountability at an autonomous-execution surface) | Codex: P1 (blocker per normalization map), newly raised | minor | mirrors an already-disclosed pre-existing asymmetry; low risk since the four reflection answers are still required regardless; cheap mechanical fix available |
| 6 | ambiguous/contradictory pre-commitment still undefined at firing time (accepted-residuals re-litigation) | Codex: P1 (blocker per normalization map) | minor, unchanged from round 1 | no new grounds beyond round 1's already-accepted, disclosed reasoning |
| 7 | AC8 necessary-but-not-sufficient reach (accepted-residuals re-litigation) | Codex: P1 (blocker per normalization map) | rejected (fast-follow), unchanged from round 1 | explicitly named out of this verdict's scope by the spec's own `## Verdict scope`; not re-litigated absent new grounds |

#### Recommendation
- REQUEST_CHANGES → route back to **pm-spec** (same route-back resolution as round 1: `- entry-mode: pm-authored`, agreeing with the Routing Map's printed `specify — pm-authored` decision).
- Ask: close findings 2 and 3 (both Major) before the freeze proceeds:
  1. Fix the re-evaluation trigger's boundary condition so the retro following the third firing is itself covered (e.g. "while at most three live firings are recorded," or an explicit inclusion of the third firing's own retro).
  2. Address the fixed-string matching's open-ended vulnerability to a hedged/rejected-reading construction — either bound the required fragment to a freestanding sentence in the extracted region, or explicitly disclose this as a residual of `contain`-mode's own design limit (matching how **AC8**'s and `registry.txt:25–29`'s residuals are already disclosed) rather than implying the criteria mechanically guarantee semantic correctness.
- This is round 2's REQUEST_CHANGES; findings 1 and 4 from round 1's Majors are treated as closed (downgraded to Minor residuals, disclosed above) and do not need further rounds unless a future round finds otherwise.
- This APPROVE/REQUEST_CHANGES gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either.

#### Fast-follow disposition
- Finding 1 (Minor, mixed-set wording clarity): recommend folding into the same revision that addresses findings 2 and 3, since it is a one-clause addition to `## Input space` and the canonical block; won't-fix as a separate blocking item if not folded in.
- Finding 4 (Minor, whitespace-variant AC7 loophole): recommend tightening `- dispatch:` to `-[[:space:]]+dispatch:` in the same revision; won't-fix as blocking on its own given the low practical risk.
- Finding 5 (Minor, AC5 missing `AI self-discipline` anchor): recommend adding it to AC5 in the same revision that addresses the two Majors; cheap and mechanical.
- Findings 6 and 7 (Minor / rejected, accepted-residuals re-litigation): won't-fix — unchanged from round 1's already-accepted reasoning; no new grounds surfaced this round.

Raw Codex output: `.shell-team/reviews/T-1095-codex-specreview-r2.txt` (final message), `.shell-team/reviews/T-1095-codex-specreview-r2.jsonl` (full event stream).

---

### Codex Spec-Review verdict: APPROVE (round 3)
- Task: T-1095
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting — this run's own JSONL prints no genuine model-identifier field; the free-form `codex exec` prompt path was used, not `codex exec review`)
- Mode: Spec-review (specify seam, T-1092), round 3, disposition-verification only per the coordinating session's narrowed scope — verify the three round-2 findings' dispositions; do not re-open settled ground.
- Consumer class: unchanged — full adversarial depth (adopter surface declared under **AC12**).
- Input reviewed: the revised `.shell-team/specs/T-1095-means-ends-reflection.md` (323 lines): the Goal's "What bounds a standing rule grounded in one live firing" paragraph, **AC11**'s anchor list, **AC2**/**AC4**/**AC5**/**AC6**'s revised bodies (the new necessary/sufficient split paragraph in **AC2**, the matching sentences added to **AC4**/**AC5**/**AC6**), `## Input space`'s new "mixed firing" bullet, **AC7**'s revised check body and check line, `## Verdict scope`'s new "Mandatory item for the delivered-change review" paragraph and its "Accepted residuals from spec-review round 2" subsection, and `## Notes for engineer`'s corresponding updates.
- Verification mode: static-only, with three items independently confirmed by direct mechanical test (not merely read) — see below.
- Scope for this round, as set by the coordinating session: verify only the dispositions of round 2's three findings; do not re-enumerate new findings across the spec; do not demand a fourth forbidden-substring patch for finding 2 (explicitly ruled out this round); if the disclosed-limit design for finding 2 is itself unsound, say so as a design objection rather than a patch request. Out-of-scope set otherwise unchanged.

#### Verification of the three round-2 findings' dispositions

1. **Off-by-one in the re-evaluation trigger — CLOSED, verified by sequence trace.** The bound changed from "while fewer than three live firings are recorded" to "**while at most three live firings are recorded** — the retro following the third firing itself included, deliberately, because that retro is the confirming read the threshold's own rationale exists to obtain." Traced through the sequence: after firing #1 (count=1, ≤3, included), firing #2 (count=2, ≤3, included), firing #3 (count=3, ≤3, **now included** — this is exactly the retro round 2 found excluded), firing #4 (count=4, >3, correctly excluded, since the rule's own standing is settled by then). **AC11**'s anchor list was updated to match verbatim (`while at most three live firings are recorded`), and `## Notes for engineer` explicitly flags the distinction ("**at most**, not 'fewer than'"). No remaining boundary bug. Codex's independent read agrees, tracing the identical arithmetic.

2. **Framing-prose inversion — CLOSED AS A DESIGN, not as a patch, and the design is sound.** The disposition does not add a fourth forbidden string; it relabels **AC2**/**AC4**/**AC5**/**AC6** as establishing only the **necessary** condition (presence detection: the norm's sentences are present, three named inversions are absent), names the round-2 counterexample verbatim inside **AC2**'s own body, and assigns the **sufficient** condition — a "semantic-assent read" of whether each region asserts the norm in its own voice rather than quoting, hedging, attributing or rejecting it — as a **mandatory, verdict-gating** item to the delivered-change cross-provider review, a real, existing gate that already governs this task's completion (not a new gate, and not this spec-review round). This is the identical necessary/sufficient split **AC8** already uses elsewhere in this same spec for a different residual (already accepted, without objection, in round 1), so the pattern is internally consistent rather than a special exception invented for this one finding. I judge this **sound** rather than a cop-out: fixed-string / forbidden-substring matching cannot decide an open-ended natural-language property (whether a sentence asserts or merely quotes-and-rejects a claim) — no finite list of forbidden phrases closes a space that includes rejected readings, hypotheticals, attributed opinions and conditionals, and this repository has already measured the identical guard-weaker-than-prose class as non-convergent across six adversarial rounds elsewhere (T-1092). Routing the residual risk to a real, mandatory, verdict-gating downstream review — with the exact counterexample text handed to that future reviewer as a named worked example — is a concrete, actionable resolution, not a deferral into silence. I considered whether a cheaper structural fix existed (e.g., requiring the fragment to be a freestanding, unembedded sentence in the extracted region) and judge it would only narrow the counterexample space, not close it (a rejection clause can itself be phrased as two freestanding sentences), so it would not have been a materially better use of a fourth round. Codex's independent read reaches the same conclusion for the same underlying reason (substring matching cannot establish semantic assent; closing it mechanically would require a new grammar or semantic validator, which this task's own Non-goals correctly declines to build). No remaining concern from either read.

3. **AC5 anchor / mixed-set clarity / AC7 regex — CLOSED, all three verified.**
   - (a) **AC5** now requires the `AI self-discipline` fragment, with a stated rationale distinguishing this task's own new autonomous-execution surface at step 6 from the older, still-disclosed, still-unfixed step-5/step-6 authorship-declaration-bullet asymmetry (`## Non-goals` is unchanged on that point, correctly). Confirmed by direct read of **AC5**'s body and check line.
   - (b) `## Input space` gained an explicit "mixed firing" bullet stating that condition (2)'s universal quantifier already forces escalation for a set with any unclassified member, so no new required fragment was needed — this matches the resolution I proposed in round 2 without fragment bloat. Confirmed by direct read.
   - (c) **AC7**'s regex changed on **both** the total-count (`nd`) and the axis-matching sub-count (`na`) from a literal single space to `-[[:space:]]+dispatch: `, so a doubled-space rogue line is now counted by the total but not by the axis-restricted subset, breaking the equality rather than passing vacuously. I mechanically re-ran the round-2 bypass construction (`+-  dispatch: fifth-axis — …`, a doubled space after the hyphen) against the new pattern: `nd=5, na=4` when the rogue line is present (equality breaks, check correctly fails), and `nd=4, na=4` for the four legitimate single-space lines alone (equality holds, check correctly passes) — confirming both that the bypass is closed and that no regression was introduced against real transcription. This directly answers the coordinating session's own verification instruction (whether both regex copies were changed, avoiding an asymmetric-tightening mistake): both were changed identically, and the two mechanical runs above rule out a vacuous pass. Codex's independent read reaches the same conclusion by inspection.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | re-evaluation trigger off-by-one | round 2: major | closed | sequence-traced by hand and confirmed by Codex's independent trace; the retro after firing #3 is now included, firing #4 onward correctly excluded |
| 2 | framing-prose inversion (AC2/AC4/AC5/AC6 bypass) | round 2: major | closed (as a disclosed-limit design, not a mechanical guarantee) | the necessary/sufficient split mirrors AC8's already-accepted precedent; the sufficient half is routed to a real, mandatory, verdict-gating existing gate rather than deferred to silence; both my own reasoning and Codex's independent judgment find no better mechanical or structural alternative was available |
| 3a | AC5 missing AI self-discipline anchor | round 2: minor | closed | fragment added with a stated, correct rationale distinguishing it from the still-disclosed older asymmetry |
| 3b | mixed finding-set ambiguity | round 2: minor | closed | Input space bullet added; relies correctly on condition (2)'s existing universal-quantifier logic, no fragment bloat |
| 3c | AC7 doubled-space bypass | round 2: minor | closed | both regex copies tightened identically; verified mechanically (bypass line now breaks the equality; legitimate transcription still passes) |

No severity changes beyond what round 2 already priced (all three round-2 findings priced Major/Minor as shown above are now closed); nothing rejected this round; no new findings raised, per this round's own narrowed scope.

#### Recommendation
- APPROVE. All three round-2 findings verified closed — findings 1 and 3(a–c) by direct mechanical/textual confirmation, finding 2 by a sound design assignment (not a mechanical guarantee, disclosed as such) to a real, existing, mandatory verdict-gating downstream review. No further spec-review round is needed on the grounds raised in rounds 1–2.
- This APPROVE gates the freeze only. It is never one of the two both-gates-green signals (`qa-verifier`'s PASS and the default-mode delivered-change review's own APPROVE) and does not substitute for either. In particular, the semantic-assent read `## Verdict scope` now assigns to the delivered-change review is a **live obligation carried forward**, not discharged by this APPROVE — the coordinating session's briefing to `codex-reviewer` at the delivered-change round should confirm that role reads `## Verdict scope`'s "Mandatory item for the delivered-change review" paragraph and performs that read.

#### Fast-follow disposition
- No fast-follow deferrals raised this round — this round's scope was disposition-verification only, and all three dispositions closed without residual concern from either this review or Codex's independent read.

Raw Codex output: `.shell-team/reviews/T-1095-codex-specreview-r3.txt` (final message), `.shell-team/reviews/T-1095-codex-specreview-r3.jsonl` (full event stream).

---

## Delivered-change review

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1095
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope: 15 files, +894/-1 lines (`git diff --stat 7d22a3f...HEAD`)

#### Three-region semantic-assent judgment (mandatory item, ## Verdict scope)
- `skills/run/SKILL.md` step 5 (Validate), the T-1095 bullet at `:81`: **asserts the norm in its own voice** — a direct, unhedged, declarative statement of the four questions, the execution branch and the four-case exception set. No quoting, no attribution, no rejected-reading framing, no conditional hedge. PASS.
- `skills/run/SKILL.md` step 6 (Review), the T-1095 bullet at `:100`: **asserts the norm in its own voice**, same wording as step 5 plus the added `AI self-discipline` authorship-class sentence. No inversion, no hedge. PASS.
- `skills/goal/SKILL.md` step 4 (Decide), the T-1095 bullet at `:242`: **asserts the norm in its own voice**, byte-parallel to the run-skill copies. No inversion, no hedge. PASS.
- All three regions pass the semantic-assent read. The verdict-gating defect found this round (Blocker 1 below) is a **different** failure mode than the one this read screens for: each region correctly asserts the norm — the problem is that adjacent, pre-existing, unedited bullets in the same numbered steps assert a conflicting unconditional instruction that the new bullet never gates or supersedes.

#### Blockers
- `skills/run/SKILL.md:81` (Validate), `skills/run/SKILL.md:100` (Review), `skills/goal/SKILL.md:242` (Decide) — **the disposition rule's claimed authority to skip escalation is not wired into the pre-existing, unedited sibling bullets that fire at the identical two firing points (same-class-2, STOP), so the shipped mechanism cannot reliably do what the frozen Goal says it does.** The new bullet states escalation is mandatory "in exactly four cases" (implying: not otherwise), yet: `skills/run/SKILL.md:77` unconditionally says a STOP means "**stop retrying** and escalate to the user"; `:87`/`:97` (Same-class-2 rule) unconditionally says to "switch the instruction you give engineer" at same-class-2; `:89`/`:99` (Early escalation branch, T-100) unconditionally says to "surface the recommended-action block to the human" at same-class-2; `skills/goal/SKILL.md:235-236` unconditionally says STOP means "stop and report the reason verbatim to the user"; `:241` carries the same unconditional T-100 surface-to-human instruction. None of these six pre-existing bullets was edited by this task, and the new bullet's own text nowhere states that it overrides, gates, or is gated by them. This affects 2 of the task's 3 named firing points (same-class-2 and STOP; the third, the spec pre-commitment trigger, has no competing unconditional bullet and is unaffected). Root-cause note: `AC4`'s own body states "the norm is added beside them rather than in place of any of them" — that sentence is about fragment-text survival for the checker's own presence assertion, not a behavioral claim, but it correctly predicts the delivered shape: the new bullet was added as an extra paragraph without rewiring the bullets it needed to condition. This is squarely inside this round's remaining scope ("the disposition rule's soundness against the four-case exception set as SHIPPED in the canonical block and both skills") and inside `## Verdict scope`'s own first sentence ("does executing a pre-priced drop without escalating preserve the three standing human gates in every branch the frozen text enumerates?") — it is not a defect the presence-detection criteria (AC2/AC4/AC5/AC6) or the semantic-assent read were built to catch, since both operate on the new bullet's own text in isolation, never on its interaction with untouched siblings. Confirmed independently by direct read of both files (line numbers above) and by an adversarial Codex pass targeted at exactly this question, which reached the same conclusion at all three regions, citing the same six sibling bullets.
  - Suggested fix: add an explicit precedence clause to the new bullet (or to each of the six sibling bullets) stating, in each of the three regions, that when the disposition executes per the reflection's own conditions, the sibling bullet's engineer-instruction-switch / human-surfacing / escalate-to-user action for that same finding class is not required — or, conversely, state explicitly that the reflection's execution branch never suppresses those actions (i.e., the disposition always executes alongside a report to engineer/human, never instead of it) if that was actually the intended design. Either resolution is a same-file, non-frozen-spec edit (both SKILL.md files are outside the frozen `<!-- BEGIN/END intent-block: T-1095 -->` region) and does not require a re-freeze.

#### Major
(none beyond the Blocker above)

#### Minor / nits
- `docs/workflow.md:65-79` / `docs/workflow.ja.md` (the new Hand-off-contract paragraph) — the adopter-facing gloss states "when a pre-priced disposition already exists for the auxiliary component every finding targets, that disposition executes" as though that one condition were sufficient, omitting the other two execution conditions (every never-dropped item green; the disposition itself non-destructive/reversible) and the plural, ordered multi-component case the skills and `## Input space` both support. Nothing stated is literally false, and the full predicate is one link away in `docs/loop-engineering/means-ends-reflection.md` (linked from the same paragraph), so this is a completeness gap in the adopter summary rather than a misleading claim. Given the frozen spec's own emphasis that this is "the single most adopter-relevant fact in this task," worth tightening, but it does not on its own invalidate the shipped deliverable.

#### Adversarial findings
- Targeted adversarial Codex pass on `skills/run/SKILL.md` and `skills/goal/SKILL.md`, framed as "does the new bullet's claimed authority to skip escalation actually gate the pre-existing unconditional instructions in the same steps?" Independently reached the same conclusion at all three regions (`skills/run/SKILL.md:81`, `:100`, `skills/goal/SKILL.md:242`): "real defect... No text explicitly supersedes or gates line 77" (STOP), and equivalent findings for the same-class-2 sibling bullets at `:87`/`:89`, `:97`/`:99`, and `:241`. No additional defect surfaced beyond the Blocker above.

#### Synthesis audit ledger
| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | disposition rule's execution branch conflicts with unedited sibling bullets (STOP escalate-to-user, same-class-2 engineer-switch, same-class-2 surface-to-human) at all three regions | primary: P1 (blocker per map); adversarial: "high" (major per map, but describing the identical substantive defect as the primary P1 tag) | blocker | conservative resolution of the primary/adversarial tag mismatch (take the higher of the two per the role's own tie-break rule), and independently confirmed by my own line-by-line read: the defect reaches 2 of 3 firing points and undermines the deliverable's central claim (the loop may decline to escalate), which is squarely verdict-gating rather than a bookkeeping note |
| 2 | docs/workflow.md/.ja paragraph states the disposition-executes condition incompletely (omits two of three conditions and the plural case) | primary: P2 (major per map) | minor | nothing stated is false, only incomplete; the full predicate is one link away in the linked worked-example note; downgraded because it does not itself misdirect an adopter into an unsafe action or invalidate the shipped mechanism |

#### Recommendation
- REQUEST_CHANGES → route back to `engineer`. The fix (an explicit precedence/gating clause connecting the T-1095 bullet to the six named sibling bullets, in both skill files) is a same-file edit outside the frozen `<!-- BEGIN/END intent-block: T-1095 -->` region and does not require a re-freeze or a return to `pm-spec`.
- Independently confirmed this round, unchanged from the engineer/QA hand-offs: `bash bin/check-prompt-sync.sh` (green), `CHECK_ACS_TIMEOUT=600 bash bin/check-acs.sh .shell-team/specs/T-1095-means-ends-reflection.md` → 15/15 passed, `bash tests/rework-digest/run.sh` (green, golden diff purely additive, 7 insertions/0 deletions), all nine structural-negative files byte-identical to `7d22a3f`, `AC7`'s `- dispatch:` count equality (4/4) holds on this board entry. None of these are in question; the Blocker above is a design-integration gap the mechanical criteria were never built to catch, exactly as `## Verdict scope` and `AC2`'s own disclosure anticipate for this review round.

#### Fast-follow disposition
- Minor finding 2 (docs/workflow.md/.ja predicate incompleteness): recommend folding into the same rework round that closes the Blocker, since the fix sits in the immediate neighborhood of the files already being touched; if not folded in, file-an-issue as a low-urgency adopter-documentation-accuracy gap.

Raw Codex output: `.shell-team/reviews/T-1095-codex-primary.txt` / `.jsonl` (primary `codex exec review --base 7d22a3f`), `.shell-team/reviews/T-1095-codex-adversarial.txt` / `.jsonl` (targeted adversarial pass on the escalation-conflict question).

---

## Delivered-change review — round 2

### Codex Review verdict: APPROVE
- Task: T-1095
- Codex model: gpt-5.6-luna (`~/.codex/config.toml`'s `model` setting)
- Codex CLI: codex-cli 0.149.1
- Verification mode: static-only
- Diff scope, this round's rework delta (`e1cd051..74a7b8d`): 6 files, +88/-23 lines (`git diff --stat`) — the rework commit `d66860a` touches `skills/run/SKILL.md`, `skills/goal/SKILL.md`, `docs/workflow.md`, `docs/workflow.ja.md`; the remaining two files (`.shell-team/todo.md`, `.shell-team/reviews/T-1095.md`) are board/record bookkeeping for the intervening rounds.
- Mode: round 2, disposition-verification only per the coordinating session's narrowed scope (round-1 findings' dispositions plus no-new-defect; settled ground from round 1 not re-litigated).

#### Disposition judgments (one line each, per the coordinating session's four verification items)

1. **Blocker closed.** Read all seven edited bullets in their post-fix form (`skills/run/SKILL.md:77,87,89,97,99`; `skills/goal/SKILL.md`'s STOP bullet and its Early-escalation/T-100 bullet). Each now carries a coherent skip-if-executed clause ("unless/skip ... when the means-ends reflection's disposition (T-1095) already executed for this firing ... since the executed disposition and its board record are themselves the loop's response") rather than an unconditional escalate-always instruction. No remaining contradiction at either firing point (STOP, same-class-2). `skills/run/SKILL.md:84`'s loop-guard-not-callable fallback was correctly left unedited: that branch fires only when `loop-guard.sh` itself cannot run, so no `STOP:<reason>` token is ever produced there, and T-1095's third firing point is specifically "a loop-guard `STOP:<reason>`" — the fallback sits outside that firing point by construction, not a missed eighth site. Independently confirmed by direct read (all seven sites plus `:84`) and by a targeted Codex pass, which reached the same conclusion on all four points and explicitly agreed with the `:84` reasoning.
2. **No new defect.** The three canonical reflection bullets (`skills/run/SKILL.md:81`, `:100`, `skills/goal/SKILL.md`'s own Decide-list T-1095 bullet) are byte-unchanged by this round's diff (confirmed: `git diff e1cd051..74a7b8d -- skills/run/SKILL.md skills/goal/SKILL.md | grep -E '^[+-]'` touches no line of those three bullets) and still assert the norm in their own voice — semantic-assent verdict from round 1 stands unchanged. The three forbidden negation strings (`not execute on the loop's own authority`, `never executes on the loop's own authority`, `without first confirming with the operator`) remain at zero occurrences in both files. `AC2`/`AC4`/`AC5`/`AC6`'s required anchors (`rework-digest.sh`, `--trigger same-class-2`, `AI self-discipline`, `reconsider-the-design-premise`) all survive. `check-acs.sh` re-run: 15/15 green; `check-prompt-sync.sh` green.
3. **Minor NOT fully closed — replaced by a different, narrower inaccuracy, newly introduced by this round's own fix.** `docs/workflow.md`'s rewritten condition (3) reads "a pre-priced disposition exists for **the earliest of** them," but the canonical rule requires a pre-priced disposition for **each** of the targeted components before **any** executes (`templates/prompt-blocks/means-ends-reflection.md`: "a pre-priced disposition exists for each of those components, the disposition for the earliest such component ... executes"). The docs conflate the existence condition (must hold for every targeted component) with the execution choice (only the earliest one's disposition actually runs). `docs/workflow.ja.md` carries the identical inaccuracy in Japanese. Risk direction is conservative, not dangerous: the shipped mechanism (unchanged skill-file text, confirmed above) correctly requires all-priced before any executes, so an adopter relying on the docs would expect the loop to self-execute in a scenario (only the earliest component priced) where the real system still escalates — the docs overstate how readily execution happens, never understate the safety net. Not verdict-gating on its own (see Minor below); flagged because item 3 of this round's own checklist asked for confirmation and a clean pass would not have been honest. Independently confirmed by direct comparison against the canonical block's text and by a targeted Codex pass, which reached the same conclusion in both languages.
4. **Out-of-scope set stayed out.** `git diff --stat e1cd051..74a7b8d` touches exactly 6 files: the four rework files plus `.shell-team/todo.md` and `.shell-team/reviews/T-1095.md` (board/record bookkeeping for the intervening QA/review rounds) — all within `AC13`'s allow-list. No canonical-block change (confirmed: `templates/prompt-blocks/means-ends-reflection.md` untouched), no re-freeze (intent-hash unchanged: `aligned: T-1095 v1 (34304e12…)`), nothing from the round-1 accepted-out-of-scope set (intervention-rate effect, step-6 authorship asymmetry, AC8's disclosed reach, contain-mode's file-level limit, registry.txt's unregistered-consumer blind spot) was re-opened.

#### Remaining Blocker/Major
- None.

#### Minor / nits
- `docs/workflow.md`/`.ja` condition (3) states "a pre-priced disposition exists for the earliest of them" where the canonical rule requires "for each of those components" before the earliest one executes — a real, freshly-introduced wording inaccuracy in a conservative (over-cautious, not under-cautious) direction. See disposition judgment 3 above for the full reasoning.

#### Synthesis audit ledger
No severity changes beyond what round 1 already priced (the round-1 Blocker is closed as verified above); nothing rejected this round. One new finding this round (docs condition-3 wording), scored Minor on first pass — not a change from a prior judgment, since round 1 never scored this specific sentence.

#### Recommendation
- APPROVE. The round-1 Blocker is genuinely closed — the disposition rule's execution branch is now coherently wired into all seven previously-unconditional sibling bullets at both affected firing points, with no remaining contradiction, and the `:84` fallback's exclusion is sound. No new defect was introduced in the reflection norm's own three canonical bullets (byte-unchanged, still semantically assertive) or in the mechanical criteria (15/15 `check-acs`, `check-prompt-sync` green, anchors intact). The one new finding (docs condition-3 wording) is Minor, doc-only, and conservative in risk direction — it does not invalidate the deliverable and is not verdict-gating.
- This is the second and (pending human GO) final delivered-change review round for this task. `qa-verifier`'s round-3 PASS plus this round's APPROVE together satisfy the `both-gates-green` invariant.

#### Fast-follow disposition
- Docs condition-3 wording (Minor, `docs/workflow.md`/`.ja`): recommend file-an-issue — cheap, isolated, one-sentence fix in each language ("a pre-priced disposition exists for each of them" in place of "for the earliest of them"), but not urgent enough to justify a third rework round given the conservative risk direction and the fact that this task's own sprint is otherwise ready to close.

Raw Codex output: `.shell-team/reviews/T-1095-codex-primary-r2.txt` / `.jsonl` (round-2 verification pass against `e1cd051..74a7b8d`).
