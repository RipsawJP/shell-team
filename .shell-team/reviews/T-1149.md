# Codex cross-provider review — T-1149

## Round 1

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1149
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 8 files, +525/-10 lines (`a28b01e9...705df192`)

#### Blockers

None.

#### Major

- `docs/distribution.md:6` / `docs/distribution.ja.md:6` — The lead paragraph states "Installing the plugin is a one-time step per machine, and it makes the team's sub-agents and `bin/` helpers reachable from either host" with no role-count qualifier anywhere in the same file. `bin/gen-codex-agents.sh:144`'s default `ROLES=` list generates exactly 5 of the repository's 9 `agents/*.md` role files (`tech-lead`, `pm-spec`, `engineer`, `qa-verifier`, `code-reviewer`) as Codex-dispatchable custom agents; `ui-designer`, `scrum-master`, `drift-evaluator` and `triage-orchestrator` are not generated and are not `spawn_agent`-reachable on a Codex CLI host by default. The spec's own `## Parity-claim-word inventory` states the rule this sentence violates verbatim: an identity/either-host word "is not admissible for the role roster as a whole (`ui-designer` and `scrum-master` are not generated)" — and neither `docs/distribution.md` nor `docs/distribution.ja.md` names or bounds the role count anywhere; only `docs/workflow.md`'s separate `## Two hosts, one loop` section (a different file) does. This is precisely Input-space class 2 ("An adopter reading either distribution file on a Codex CLI host, deciding how to install — the class the current text serves worst, and the reason this task exists") reading the one file this task exists to fix and getting the exact overclaim the spec forbids. Both the primary and the adversarial pass independently flagged this (each `[P2]`), and the adversarial pass, briefed to check whether the sentence is actually bounded elsewhere in the same file, confirmed it is not and confirmed this is the same defect class the spec's own inventory names inadmissible. QA's own round-3 hand-off read "sub-agents" as already scoped to the generated subset and passed it as a disclosed borderline for this review's independent judgment; a strict reading — the one both Codex passes and the spec's own admissibility rule converge on — finds the unqualified sentence is a genuine role-roster parity overclaim, not merely a defensible generous reading.
  - Suggested fix: name the five default-generated roles (or point to `## Two hosts, one loop` for the list) directly in `docs/distribution.md:6` / `docs/distribution.ja.md:6`'s lead paragraph, or narrow "the team's sub-agents" to a phrase that does not claim the full nine-role roster (e.g. "the sub-agents `bin/gen-codex-agents.sh` generates"). This is a prose-only fix inside the region `## Non-goals` leaves mutable; it touches no AC-pinned literal.

#### Minor / nits

- `docs/distribution.md:38`-`53` / `docs/distribution.ja.md`'s parallel block — After the new Codex install block ends at the generator command, the very next section, `## Adopt in a target repo` (unedited by this diff, and its subject matter is this task's own declared Non-goal 9), still shows only the Claude-Code-only slash command `/shell-team:team-init`, with no `bin/team-init.sh` form shown anywhere in `docs/distribution.md`. `docs/adopting.md`'s own "Using shell-team from Codex CLI" runbook (`:363`-`:686`) never runs or mentions `team-init` either; the bash form (`bin/team-init.sh`) is documented only under a separate, unrelated heading, `## Trying the team on one ticket` — itself host-neutral and not linked from the Codex install path. Both passes independently confirmed (primary and adversarial, both `[P1]`, mapped to blocker by the raw severity-normalization table) that a Codex CLI adopter who follows only `docs/distribution.md` has no shown path to scaffold the repo's operating files, and both were briefed to check whether this predates the diff. It does: `git show a28b01e9:docs/distribution.md` carries the identical, unqualified `/shell-team:team-init` line, before this task existed and before this file said anything about Codex at all — this is a pre-existing gap in a section this task's own frozen Non-goal explicitly declines to touch ("Neither is host-specific in the sense this task addresses, and neither is required to change"), and the runbook itself, which this task is not permitted to edit, does not close it either. Downgraded from the raw `[P1]`/blocker mapping to Minor because the affected text is unedited by any round of this task, the spec's own Non-goal names it by section and gives a reason, and the practical effect is an incremental improvement over base (before this task a Codex adopter had no install path in this file at all) rather than a regression this task introduces — the same disposition shape `.shell-team/reviews/T-1148.md`'s round-2 Minor 2 established for a pre-existing, untouched, explicitly-scoped-out tension made more visible without being created by that task's own diff.
- `docs/distribution.md:56` / `docs/distribution.ja.md:56` — The unedited `## Adopt in a target repo` sentence "The framework itself (agents/skills/scripts/templates) stays in the plugin — update once, every repo benefits" sits in new tension with the `## Update` section's own Codex-host regen-and-check requirement, added a few lines above by this diff, which states the generator and checker must be re-run per adopted repository. Surfaced only by the adversarial pass, independent of the primary pass and independent of the finding above. The sentence remains literally true of what it actually names — the framework prose (`agents/*.md`, `skills/*`, `bin/*`) genuinely does update once and benefit every repo — and is not a claim about the derived, per-repository `.codex/agents/*.toml` artifacts the new Update section is about; the tension is a stretch reading a careful comparison across two sections produces, not a false statement in either section read alone. Not escalated to Major: the sentence is pre-existing, unedited by any round of this task, and `## Adopt in a target repo` is the same explicitly Non-goal'd section the finding above concerns — the same disposition class.

#### Adversarial findings

Framed as devil's-advocate on the primary pass's role-roster candidate, and asked to check whether the sentence is bounded elsewhere in the same file, whether the team-init gap predates the diff or is newly created/newly exposed by it, and to hunt for any other host-parity overclaim or printed-procedure omission the primary pass missed, especially rereading `## Update` and `## Two hosts, one loop` for an inadmissible claim word. Confirmed the role-roster candidate without qualification — the sentence is not bounded anywhere else in `docs/distribution.md` or `docs/distribution.ja.md`, and the finding is exactly the defect class the spec's own `## Parity-claim-word inventory` names inadmissible. Confirmed the team-init gap independently, and confirmed via `git show a28b01e9:docs/distribution.md` that it is pre-existing rather than newly created, while noting it is newly *exposed* as a fresh adopter's dead end now that a working Codex install path precedes it. Surfaced one new finding beyond either primary candidate: the "update once, every repo benefits" sentence (`docs/distribution.md:56` / `.ja.md:56`) in tension with the new Update section's per-repository regen requirement. Re-read `## Update` and `## Two hosts, one loop` for any other inadmissible claim word and found none — both sections' identity/either-host and per-machine words are each scoped to what the spec's inventory admits (the upgrade command and the plugin install itself for "per machine"; the phase flow, status flags, both gates and the five generated roles for "identical"/"same").

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | "sub-agents ... reachable from either host" claims parity for the full 9-role roster; only 5 are Codex-generated | both passes: `[P2]`/major | major | matches the spec's own `## Parity-claim-word inventory` inadmissibility rule verbatim ("not admissible for the role roster as a whole"); neither distribution file bounds the claim anywhere; this is Input-space class 2, the class this task exists to serve, reading the exact file the overclaim sits in |
| 2 | `## Adopt in a target repo` shows only the Claude-Code-only `/shell-team:team-init` form, no Codex path to scaffold operating files | both passes: `[P1]`/blocker | minor | downgraded from the raw blocker mapping: the section is unedited by any round of this task, is this task's own declared Non-goal 9 by name and by reason, is confirmed pre-existing at `a28b01e9` (before this file said anything about Codex at all), and this task is a net improvement over base rather than a regression it introduces |
| 3 | "update once, every repo benefits" (`## Adopt in a target repo`, unedited) vs. the new Update section's per-repository regen requirement | adversarial only: `[P2]`/major | minor | same disposition class as finding 2 — pre-existing, unedited, explicitly Non-goal'd section; the sentence remains literally true of the framework prose it actually names and is not a claim about the derived per-repository artifacts the new Update section covers |

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to READY_FOR_ENG with the Major above linked (`docs/distribution.md:6` / `docs/distribution.ja.md:6` — the lead paragraph's "sub-agents ... reachable from either host" claim overstates the role roster Codex generates by default).

#### Fast-follow disposition

- Finding 2 (`## Adopt in a target repo` shows no Codex-reachable team-init path, `docs/distribution.md:38`-`53` / `.ja.md`'s parallel block) — Intent: file-an-issue. The gap is real and pre-existing, but fixing it means editing a section this task's own frozen Non-goal 9 explicitly excludes by name; a follow-up task can decide whether to add `bash "<plugin root>/bin/team-init.sh" .` beside the slash-command form, or to cross-link `## Trying the team on one ticket`'s existing bash form.
- Finding 3 ("update once, every repo benefits" vs. per-repository regen, `docs/distribution.md:56` / `.ja.md:56`) — Intent: file-an-issue, folded into the same follow-up as finding 2 (same section, same underlying Non-goal boundary, same root cause — `## Adopt in a target repo` was never made host-aware because this task's own Non-goals excluded it).
- No other fast-follow deferrals.

## Per-pass provenance

### Round 1

Round 1's primary pass ran the `codex exec … review --base a28b01e9 --json` subcommand — a base-diff review with no free-form prompt, as prescribed by the default fresh-review path. The base ref is `a28b01e9` rather than `develop`, per this task's own frozen `base-ref-discriminator` (this branch is stacked on the open, unmerged T-1148 predecessor pull request).

- pass-role (primary): generation
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base a28b01e9 --json -o <repo-root>/.shell-team/reviews/T-1149-codex-primary.txt > <repo-root>/.shell-team/reviews/T-1149-codex-primary.jsonl 2>&1`
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; ran exactly the role file's primary-pass skeleton with no deviation.
- raw-capture (primary): T-1149-codex-primary
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Round 1's adversarial pass was triggered by the primary pass's role-roster Major finding, per the role file's step-5 mandate; it was framed as a devil's-advocate pass naming both the primary's Major candidate and its own disclosed team-init observation, their concrete grounding, and the reading/scope questions at issue, and asked to hunt for any other host-parity overclaim or printed-procedure omission the primary pass missed.

- pass-role (adversarial): generation
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/T-1149-codex-adversarial.txt "Play devil's advocate on the diff between a28b01e9 and HEAD in the repository at <repo-root>. Read the files yourself with git diff a28b01e9...HEAD / nl -ba -- do not expect any diff pasted here. [...briefing carrying both candidate findings, their grounding, and reading/scope questions (a), (b), (c), verbatim per the role file's step-5 instruction to target 'those specific files'...] Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <repo-root>/.shell-team/reviews/T-1149-codex-adversarial.jsonl 2>&1`
- briefing-fidelity (adversarial): carried — the role-roster candidate, the team-init observation, their concrete grounding (`bin/gen-codex-agents.sh:144`'s role list, the runbook's silence on `team-init`, the base-blob comparison question), and the reading/scope questions at issue were all relayed verbatim into the prompt, per step 5's own instruction to target "those specific files."
- raw-capture (adversarial): T-1149-codex-adversarial
- self-detected-host (adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Raw captures: `T-1149-codex-primary.{txt,jsonl}`, `T-1149-codex-adversarial.{txt,jsonl}` in this directory.

## Independent verification performed this round (beyond the two Codex passes)

- `bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md 2>&1 | tail -3` (own run) → `check-acs: 15 passed, 0 failed, 1 skipped, 0 unrecognized` — matches the engineer's and QA's own reported runs.
- `bash bin/check-intent.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md .shell-team/todo.md` (own run) → `aligned: T-1149 v1 (e7d6a3721ca18b2be816d6d8edf8286dc53e477a)`.
- Own base-relative `^## ` heading count derivation (not read back from the spec's own tables): `docs/distribution.md` 8→8, `docs/distribution.ja.md` 7→7 (distribution pair unchanged, matching **AC12**'s required relationship); `docs/workflow.md` 7→8, `docs/workflow.ja.md` 7→8 (workflow pair gains exactly one heading each, matching **AC12**).
- Own read of `bin/gen-codex-agents.sh:144` — confirms the default `ROLES="tech-lead pm-spec engineer qa-verifier code-reviewer"`, five roles, and confirms `agents/*.md` holds nine role files total (`ui-designer`, `scrum-master`, `drift-evaluator`, `triage-orchestrator` not among the generated five) — the grounding for the Major finding above.
- Own read of `.claude-plugin/marketplace.json` — confirms the first `"name"` field is `ripsawjp`, the value **AC4** derives at check time for the `codex plugin marketplace upgrade` command, and confirms `docs/distribution.md:113` / `docs/distribution.ja.md:83` both print `codex plugin marketplace upgrade ripsawjp` with that exact derived value.
- Own read of `docs/adopting.md:363`-`465` (runbook steps 1-3) — confirms every printed Codex command in `docs/distribution.md`/`.ja.md` (`codex plugin marketplace add RipsawJP/shell-team`, `codex plugin add shell-team@ripsawjp`, `bash "<plugin root>/bin/gen-codex-agents.sh" --out-dir .codex/agents`, the `installed, enabled` STATUS reading, the `VERSION`-column comparison, the conditional re-install) occurs verbatim there, and confirms the own-root qualifier ("from that repository's own root" / "リポジトリそのものの root から") matches the runbook's own language.
- Own read of `docs/workflow.md`'s `## Two hosts, one loop` region and its Japanese counterpart — confirms both name `ui-designer` and `scrum-master` as not generated, confirms the shipped-default-binding hedge on the review-provider claim ("under the shipped default binding... a host may rebind... the loop no longer guarantees this"), and confirms the section distinguishes itself from `## Codex CLI quick-reference` without editing it — the region this round's read confirms is byte-identical to base per `check-acs.sh`'s own **AC13** pass.
- EN/JA semantic read (not a byte-level lock, per the spec's own Non-goal on word-for-word translation) — confirmed the two languages carry the same facts, the same hedges (the shipped-default-binding caveat, the per-repository qualifiers) and the same role-count disclosure in `## Two hosts, one loop`, with no fact present in one language and absent from the other.
- No claim, in either file, about what a Codex CLI host loads as instruction — confirmed by the same `for tok in ...` grep pattern `.shell-team/reviews/T-1148.md`'s own round-5 record used, re-run against all four of this task's files: all eight forbidden tokens report 0 occurrences (**AC11**, never-dropped).
- Nothing this review could not judge from the static, read-only sandbox this round: **AC16** (a real Codex CLI host accepting the printed commands) stays above the spec's own declared ceiling, and this review does not attempt to substitute a claim for that measurement — the spec's own `## Non-goals` and `above-ceiling` marker already name the operator's next Codex-host adopter run as its owner.

## Round 2

### Codex Review verdict: REQUEST_CHANGES
- Task: T-1149
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 9 files, +646/-12 lines (`a28b01e9...5b2247f9`)

#### Blockers

None.

#### Major

- `docs/distribution.md:46` / `docs/distribution.ja.md:46` — class `host-scope-omission`. Immediately after this task's own newly-added Codex CLI install block (`## Install`, `:14`-`:44`), the very next paragraph — pre-existing at `a28b01e9:29`, unedited by any round of this task — states "The plugin's agents resolve as `/shell-team:<agent>`, skills as `/shell-team:<skill>` (e.g. `/shell-team:run`)…" with no host qualifier. Those are the Claude Code slash-command forms; the Codex CLI path this task itself documents two paragraphs above dispatches generated `shell-team-<role>` custom agents via Codex's own `spawn_agent` tool instead (`docs/adopting.md:559`-`:566`). A Codex CLI reader following this page sequentially — Input-space class 2, "the class the current text serves worst, and the reason this task exists" — reaches this sentence right after the Codex commands and has no signal that it switches back to describing the other host; they can copy `/shell-team:run` and have it fail. Both the primary and the adversarial pass independently flagged this (`[P2]`/Major on both), and the adversarial pass, briefed to check whether the sentence predates the diff and whether any Non-goal covers it, confirmed both: the sentence is pre-existing (verified first-hand against `a28b01e9:29`, byte-identical) but sits inside `## Install`, a section this task's own **D-2** names as one of the four sections gaining Codex counterparts — not `## Adopt in a target repo` or `## Host-only scheduling`, the two sections Non-goal 9 actually excludes, and not one of the five regions **AC13** protects as byte-identical. No Non-goal, and no criterion, covers this sentence; the gap that closed the identical vocabulary in the lead paragraph (round 4's role-roster qualifier) was never propagated to this second occurrence of the same ambiguity, one section down, that this task's own edit newly placed it beside. Per the frozen `## Review depth`, this doc is priced at full strength as `user-visible: yes` shipped adopter documentation, and a finding of this shape — an install-path sentence a Codex adopter reaches that omits which host it describes, in the section this task is actively rewriting for both hosts — is priced at the same severity the spec's own pricing list assigns a parity claim exceeding what ships per host, which is Major.
  - Suggested fix: qualify the sentence as the Claude Code form (e.g. prefix it "On Claude Code, the plugin's agents resolve as…") and state separately, in the same paragraph or immediately after, that Codex CLI dispatches the generated `shell-team-<role>` custom agents via `spawn_agent` instead — the same shape round 4 already used to fix the lead paragraph's identical ambiguity. This is a prose-only fix inside `## Install`, a region **AC13** does not protect and **AC3** does not byte-lock beyond the strings it names.

#### Minor / nits

None new this round. Round 1's Minor 1 (`## Adopt in a target repo` team-init path) and Minor 2 (`## Update` cross-reference for "update once, every repo benefits") are both closed — see closure detail below.

#### Adversarial findings

Framed as devil's-advocate on the primary pass's `docs/distribution.md:46` / `.ja.md:46` candidate, briefed to confirm whether the sentence predates the diff, whether it is newly exposed rather than newly created, whether any Non-goal covers `## Install`, and to independently re-run the `## Parity-claim-word inventory` and the per-machine/per-repository qualifier check over every new Codex sentence across all four files, hunting for anything the primary pass missed. Confirmed the candidate at both severity and both language files without qualification, confirmed via `git show a28b01e9:docs/distribution.md` that the sentence is pre-existing rather than newly written, and confirmed that the Non-goal excluding `## Adopt in a target repo` and `## Host-only scheduling` does not reach `## Install`. Found no other unqualified single-host claim, no host-parity overclaim, and no new Codex sentence missing its per-machine/per-repository or own-root qualifier anywhere in the four files — every occurrence of the inventory's claim words (`same`, `identical`, `either`, `both`, `per machine`, `every repo` and their Japanese counterparts) it re-checked against the shipped text stayed inside the two rules the spec's own inventory states.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | `docs/distribution.md:46` / `.ja.md:46` — the pre-existing "plugin's agents resolve as `/shell-team:<agent>`…" sentence sits unqualified immediately after this task's own new Codex install block | both passes: `[P2]`/major | major | genuine host-conflation risk in `## Install`, a section this task actively edits and neither Non-goal 9 nor **AC13** protects; matches Input-space class 2, the class this task exists to serve; `## Review depth` prices this doc at full strength |

No other severity changes or rejections this round.

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md` to READY_FOR_ENG with the Major above linked (`docs/distribution.md:46` / `docs/distribution.ja.md:46` — the pre-existing plugin-agents-resolve-as sentence, now sitting beside new Codex content, needs the same Claude-Code-only qualifier round 4 already gave the lead paragraph's identical ambiguity).

#### Fast-follow disposition

- no fast-follow deferrals

### Closure of round 1's three findings (re-verified this round)

- **Major `host-parity-overclaim`** (`docs/distribution.md:6` / `.ja.md:6`, full nine-role roster claim) — **closed**. The lead paragraph now names the five roles `bin/gen-codex-agents.sh:144` generates by default and states the roles it does not generate stay Claude Code roles; "sub-agents" was dropped from the either-host claim, leaving only `bin/` helpers claimed reachable from either host. Re-read first-hand; matches the engineer's round 4 hand-off and QA round 4's independent confirmation.
- **Minor 1** (`## Adopt in a target repo` — no Codex-reachable `team-init` path) — **closed**. `bash "<plugin root>/bin/team-init.sh" .` now appears in its own fenced block in both `docs/distribution.md:56`-`:59` and `docs/distribution.ja.md:56`-`:59`, byte-identical to `README.md:79` / `README.ja.md:79` (re-diffed this round: no difference), and traces to `docs/adopting.md:774`'s bare-form source for the underlying command.
- **Minor 2** ("update once, every repo benefits" vs. the new Update section's per-repository regen requirement) — **closed**. Both distribution files now state the framework updates once per machine on plugin install, and that a Codex CLI host's generated custom agents in each adopted repository still need their own regeneration and drift check after that, pointing at `## Update` / `## アップデート` by heading name.

## Per-pass provenance (round 2)

### Round 2

Round 2's primary pass ran the `codex exec … review --base a28b01e9 --json` subcommand, per the default fresh-review path — the same base ref as round 1, per this task's own frozen `base-ref-discriminator` (unchanged: the predecessor branch `feature/564-readme-two-hosts` still resolves in this checkout).

- pass-role (primary): generation
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base a28b01e9 --json -o <repo-root>/.shell-team/reviews/T-1149-codex-primary-r2.txt > <repo-root>/.shell-team/reviews/T-1149-codex-primary-r2.jsonl 2>&1`
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; ran exactly the role file's primary-pass skeleton with no deviation.
- raw-capture (primary): T-1149-codex-primary-r2
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Round 2's adversarial pass was triggered by the primary pass's Major finding, per the role file's step-5 mandate; it was framed as a devil's-advocate pass naming the primary's candidate, its concrete grounding (the Non-goal boundary, the base-blob comparison), and the reading/scope questions at issue, and asked to independently re-run the Parity-claim-word inventory and the per-machine/per-repository qualifier check over the whole shipped text.

- pass-role (adversarial): generation
- executor-invocation (adversarial): `codex exec --sandbox read-only --cd <repo-root> --json -o <repo-root>/.shell-team/reviews/T-1149-codex-adversarial-r2.txt "Play devil's advocate on the diff between a28b01e9 and HEAD in the repository at <repo-root>. Read the files yourself with git diff a28b01e9...HEAD -- docs/distribution.md docs/distribution.ja.md docs/workflow.md docs/workflow.ja.md, and read the final text of all four files with nl -ba -- do not expect any diff pasted here. [...briefing carrying the primary's candidate, its grounding, and the reading/scope questions at issue, per step 5's own instruction to target those specific files...] Then hunt independently for any other unqualified single-host claim, sentence not derived from a runbook line, or host-parity overclaim across all four files. Return findings as a JSON array of {severity, file, line, issue, suggestion}." > <repo-root>/.shell-team/reviews/T-1149-codex-adversarial-r2.jsonl 2>&1`
- briefing-fidelity (adversarial): carried — the candidate, its grounding (the Non-goal boundary, the base-blob comparison question, the Parity-claim-word inventory), and the reading/scope questions at issue were all relayed verbatim into the prompt, per step 5's own instruction to target "those specific files."
- raw-capture (adversarial): T-1149-codex-adversarial-r2
- self-detected-host (adversarial): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

Raw captures: `T-1149-codex-primary-r2.{txt,jsonl}`, `T-1149-codex-adversarial-r2.{txt,jsonl}` in this directory.

## Independent verification performed round 2 (beyond the two Codex passes)

- `bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md 2>&1 | tail -3` (own run) → `check-acs: 15 passed, 0 failed, 1 skipped, 0 unrecognized` — matches the engineer's and QA's own round 4 reports.
- `bash bin/check-intent.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md .shell-team/todo.md` (own run) → `aligned: T-1149 v1 (e7d6a3721ca18b2be816d6d8edf8286dc53e477a)`.
- Own base-relative `^## ` heading count re-derivation (not read back from the spec's own tables, resolved through `git merge-base feature/564-readme-two-hosts HEAD` = `a28b01e9`): `docs/distribution.md` 8→8, `docs/distribution.ja.md` 7→7 (distribution pair unchanged, matching **AC12**); `docs/workflow.md` 7→8, `docs/workflow.ja.md` 7→8 (workflow pair gains exactly one heading each, matching **AC12**).
- Own byte-comparison of all five **AC13**-protected regions against their base blobs at the same resolved branch point (`## Sandbox-enabled permission settings` and `## Develop / dogfood this repo` in `docs/distribution.md`; `## このリポジトリを開発 / dogfood する` in `docs/distribution.ja.md`; `## Codex CLI quick-reference` in `docs/workflow.md`; `## Codex CLI クイックリファレンス` in `docs/workflow.ja.md`) — all five `cmp` clean, independently re-run this round rather than trusted from `check-acs.sh`'s own pass.
- Own confirmation that `docs/distribution.md:46` / `.ja.md:46`'s flagged sentence is byte-identical to `git show a28b01e9:docs/distribution.md` (lines 25-29 of the base blob) — pre-existing, not introduced by any round of this task, and that neither Non-goal 9 (`## Adopt in a target repo`, `## Host-only scheduling`) nor **AC13**'s protected-region list names `## Install` or this sentence.
- Own confirmation that the byte-identical `bash "<plugin root>/bin/team-init.sh" .` line appears identically at `docs/distribution.md:59`, `docs/distribution.ja.md:59`, `README.md:79` and `README.ja.md:79` (all four re-read this round), and that `docs/adopting.md:774` carries the bare `team-init.sh .` form the shipped line's underlying command derives from.
- `bash bin/check-pii-shapes.sh --base a28b01e9` (own run) → `check-pii-shapes: clean (no PII-shaped bytes found)`.
- Nothing this review could not judge from the static, read-only sandbox this round: **AC16** (a real Codex CLI host accepting the printed commands) stays above the spec's own declared ceiling, unchanged from round 1 — the operator's next Codex-host adopter run remains its named owner.

## Round 3

### Codex Review verdict: APPROVE
- Task: T-1149
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.154.0
- Verification mode: static-only
- Diff scope: 9 files, +754/-14 lines (`a28b01e9...c271b334`)

#### Blockers

None.

#### Major

None. Round 2's sole Major (class `host-scope-omission`, `docs/distribution.md:46` / `.ja.md:46`) is closed — see closure detail below.

#### Minor / nits

None.

#### Adversarial findings

Not triggered this round. The primary pass (`codex exec … review --base a28b01e9 --json`) returned zero findings of any severity, and per the role file's step-5 mandate the parallel adversarial pass is triggered solely by a primary blocker/major — none was raised, so no adversarial pass was launched. Its place is taken by this round's own independent sweep (below), run directly by this reviewing session rather than by a second Codex invocation, since the coordinating session's own systematic-sweep mandate for this round is a reviewing-session responsibility, not a Codex-pass one.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|

no severity changes

#### Recommendation

- APPROVE → set `.shell-team/todo.md` to READY_FOR_MERGE.

#### Fast-follow disposition

- no fast-follow deferrals

### Closure of round 2's Major (re-verified this round)

- **Major `host-scope-omission`** (`docs/distribution.md:46` / `.ja.md:46`, the pre-existing `/shell-team:<agent>`/`/shell-team:<skill>` sentence sitting unqualified beside the new Codex install block) — **closed, no under-claim**. `git diff 5b2247f9..HEAD -- docs` (own run) shows the sentence rewritten to "On **Claude Code**, the plugin's agents resolve as `/shell-team:<agent>` and skills as `/shell-team:<skill>` … On **Codex CLI**, a role's generated custom agent is dispatched with Codex's own `spawn_agent` tool instead — see [Using shell-team from Codex CLI](adopting.md#using-shell-team-from-codex-cli). On either host, `bin/` scripts are invoked as …" — the same three-way shape (Claude-Code-only clause, Codex-CLI-only clause with a runbook pointer, host-neutral clause) round 4 already used to close the lead paragraph's identical ambiguity. The Japanese file carries the same three clauses in natural Japanese (`**Claude Code** では、… **Codex CLI** では、… どちらの host でも、…`). The `spawn_agent` claim traces to `docs/adopting.md:559`–`:566` (own read, confirmed this round). No under-claim: the sentence still names both the slash-command surface and the `bin/` invocation form it replaces, and `check-acs.sh` (own run) still passes AC3 (the own-root qualifier, the three literal Codex commands) unaffected by this prose-only region.

### Independent host-behaviour sentence sweep performed this round (beyond the two Codex passes)

Own first-hand read, at the spec's `## Review depth` pricing, of the lead paragraph, `## Install`, `## Adopt in a target repo`, `## Update`, `## Version line`, the air-gapped section (both languages) and `## Two hosts, one loop` / `## 2 つのホスト、1 つのループ` (both languages), against every class named in the briefing (`host-parity-overclaim`, `printed-procedure-omits-runbook-check`, `per-machine-vs-per-repo-scope-unstated`, missing own-root qualifier, `unqualified-invariant-overclaim`, `unmeasured-manifest-parity-claim`, `host-scope-omission`, an unmeasured Codex-host-loading claim, or a Codex-path sentence not traceable to `docs/adopting.md:363`–`:686` / `:774`).

- `docs/distribution.md:6` / `.ja.md:6` (lead paragraph) — "`bin/` helpers reachable from either host," the five named roles, "the roles it does not generate stay Claude Code roles," the skill-surface sentence, the per-adopted-repository / own-root qualifier on the generated custom agents — all closed since round 4, unchanged this round, no new class found.
- `docs/distribution.md:12` / `.ja.md:12` (`## Install` opening) — "Installing the plugin is a one-time step per machine on whichever host you use" — host-neutral, correctly scoped to the install step alone (not the generator), no finding.
- `docs/distribution.md:44` / `.ja.md:44` — the setup-remainder pointer at the runbook, host-neutral, traceable, no finding.
- `docs/distribution.md:46` / `.ja.md:46` — round 2's Major, now closed (detail above).
- `docs/distribution.md:56`–`:62` / `.ja.md:56`–`:62` (`## Adopt in a target repo`) — the Codex `team-init.sh` fenced block, the "updates once per machine … the generated custom agents in each adopted repository still need their own regeneration" sentence — both closed since round 4, no new class found this round; this section's own remaining subject matter (Non-goal 9) is out of this task's scope, per the briefing's own disclosure, and not treated as a finding.
- `docs/distribution.md:106`–`:129` / `.ja.md:76`–`:99` (`## Update`) — the pre-split "Bump `version` … then on each machine:" intro, both host-scoped code blocks, and the "Omitting `version` makes the plugin track the latest commit SHA instead of pinned releases" sentence sitting between the Claude Code block and the "**Codex CLI:**" header. Read closely against the `host-scope-omission` class round 2 found one section up: this sentence differs from round 2's Major in a load-bearing way — it sits directly under an explicit "**Claude Code:**" sub-header with a parallel "**Codex CLI:**" sub-header immediately below it, giving it the visual scoping round 2's candidate sentence lacked (that one sat in the section's *shared* prose, outside either host's sub-block). It is also the same sentence QA's own round 2 FAIL priced as `unmeasured-manifest-parity-claim` when a prior engineer round tried to state it applies identically to both hosts — that attempt was reverted in round 3, restoring this exact base-identical wording and position. Leaving it unqualified and positioned under the Claude Code sub-header is therefore the deliberate, already-adjudicated disposition (avoid an unmeasured cross-host manifest claim), not a fresh instance of the omission class. No finding.
- `docs/distribution.md:131`–`:139` / `.ja.md:101`–`:109` (`## Version line`) — round 5's own self-detected fix (the `On **Claude Code**, …` / `**Claude Code** では、…` prefix), verified present and symmetric with the pre-existing `On a **Codex CLI** host, …` / `**Codex CLI** host では、…` sentence beside it; the closing "always gets the latest release" is a recency claim about a single host's own resolution, not a cross-host identity word, and stays inside the now-qualified Claude Code clause. No finding.
- `docs/distribution.md:154`–`:158` / `.ja.md:124`–`:128` (air-gapped section) — "A checkout of this repository also works as `<plugin root>` on either host — Claude Code or Codex CLI — needing no install or upgrade at all," traced to `docs/adopting.md:423` / `:440`–`:442`, unedited since round 1, no finding.
- `docs/workflow.md:21`–`:29` / `.ja.md:21`–`:29` (`## Two hosts, one loop` / `## 2 つのホスト、1 つのループ`) — confirmed byte-identical to base by `cmp` (own run, part of the AC13 re-derivation above — this section is not one of the five AC13-protected regions but is unedited by any round since round 1 regardless); the identity claim stays scoped to the phase flow, status flags and both gates; the dispatch-mechanism sentence names both mechanisms and the five generated roles; the review-pass sentence carries the "under the shipped default binding … not an unconditional invariant … a host may rebind" hedge; no unqualified `always`/`常に` present. No finding.

Disclosed out of scope per the briefing, not treated as findings: the bare `/shell-team:team-init` line in `## Adopt in a target repo` (the Codex form is printed directly after it); `## Host-only scheduling` (`:143`/`:147`, including "manual is always the fallback" at `:150`, a recency/fallback claim about the scheduling layer itself, not a cross-host identity claim, and pre-existing/unedited regardless); `docs/workflow.md:116`/`:148`'s language-mirroring section.

**Comparison with the two inventories on the board.** The engineer's round-5 hand-off inventory (`.shell-team/todo.md`, round-5 engineer block) and QA round 5's independent seven-point re-read (runbook-entailment table, parity-claim-word inventory, per-machine/own-root qualifier check, AC11 ninth-phrasing scan, EN/JA semantic equivalence, non-duplication with `## Codex CLI quick-reference`, traceability) both concluded no new gating class beyond round 2's Major, which round 5 closed. This sweep agrees with both: no discrepancy found. The one candidate this sweep examined that neither inventory names explicitly — the `## Update` section's "Omitting `version`" sentence — was traced to its own prior adjudication (the reverted `unmeasured-manifest-parity-claim` round 2/3 episode) rather than left unexamined, and is not a gating finding for the reason given above.

**Disposition on the coordinating session's stop rule.** No gating finding this round. The stop rule's trigger (any gating finding at cross-provider review round 3) is not met; nothing to present to the operator as a continue/replan choice.

## Per-pass provenance (round 3)

### Round 3

Round 3's primary pass ran the `codex exec … review --base a28b01e9 --json` subcommand, per the default fresh-review path — the same base ref as rounds 1 and 2, per this task's own frozen `base-ref-discriminator`.

- pass-role (primary): generation
- executor-invocation (primary): `codex exec --sandbox read-only --cd <repo-root> review --base a28b01e9 --json -o <repo-root>/.shell-team/reviews/T-1149-codex-primary-r3.txt > <repo-root>/.shell-team/reviews/T-1149-codex-primary-r3.jsonl 2>&1`
- briefing-fidelity (primary): not-applicable — the `review` subcommand takes no free-form prompt argument; ran exactly the role file's primary-pass skeleton with no deviation.
- raw-capture (primary): T-1149-codex-primary-r3
- self-detected-host (primary): claude-code — CODEX_THREAD_ID was not set in this shell before any launch.

No adversarial pass this round: the primary pass returned zero findings of any severity, and per the role file's step-5 mandate the parallel adversarial pass is triggered solely by a primary blocker/major finding.

Raw captures: `T-1149-codex-primary-r3.{txt,jsonl}` in this directory.

## Independent verification performed round 3 (beyond the Codex pass and the sentence sweep above)

- `bash bin/check-acs.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md 2>&1 | tail -3` (own run) → `check-acs: 15 passed, 0 failed, 1 skipped, 0 unrecognized` — matches the engineer's and QA's round 5 reports.
- `bash bin/check-intent.sh .shell-team/specs/T-1149-distribution-workflow-two-hosts.md .shell-team/todo.md` (own run) → `aligned: T-1149 v1 (e7d6a3721ca18b2be816d6d8edf8286dc53e477a)`.
- Own base-relative `^## ` heading count re-derivation, resolved through `git merge-base feature/564-readme-two-hosts HEAD` = `a28b01e9`: `docs/distribution.md` 8→8, `docs/distribution.ja.md` 7→7 (distribution pair unchanged, matching **AC12**); `docs/workflow.md` 7→8, `docs/workflow.ja.md` 7→8 (workflow pair each +1, matching **AC12**).
- Own byte-comparison of all five **AC13**-protected regions against their base blobs at the same resolved branch point (`## Sandbox-enabled permission settings` and `## Develop / dogfood this repo` in `docs/distribution.md`; `## このリポジトリを開発 / dogfood する` in `docs/distribution.ja.md`; `## Codex CLI quick-reference` in `docs/workflow.md`; `## Codex CLI クイックリファレンス` in `docs/workflow.ja.md`) — all five `cmp` clean, independently re-run this round.
- Own confirmation that `git diff a28b01e9..HEAD -- docs/workflow.md docs/workflow.ja.md` shows only the single `## Two hosts, one loop` / `## 2 つのホスト、1 つのループ` section, unchanged since round 1, and that `git diff 5b2247f9..HEAD -- docs` shows exactly the two round-5 fixes (four hunks, two per language file) the engineer's hand-off claims — no undisclosed edit found.
- `bash bin/check-pii-shapes.sh --base a28b01e9` (own run) → `check-pii-shapes: clean (no PII-shaped bytes found)`.
- Nothing this review could not judge from the static, read-only sandbox this round: **AC16** (a real Codex CLI host accepting the printed commands) stays above the spec's own declared ceiling, unchanged from rounds 1–2 — the operator's next Codex-host adopter run remains its named owner.
