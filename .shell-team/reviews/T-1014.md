# QA verification — T-1014

Branch `feature/83-flag-rail-data-path`, HEAD `4748e73` (base `develop` @
`76e64a4`). Spec: `.shell-team/specs/T-1014-flag-rail-data-path.md` (intent
v1, aligned, 21 ACs — 19 mechanical, AC20/AC21 SKIP by design). Working tree
kept clean throughout (`git status --short` shows nothing beyond this review
file and the board edit); all mutation testing below ran on file copies
under `$TMPDIR`, never on the real checkout. No real filesystem path is
quoted anywhere in this file — only shapes, per the T-1012 H8/Blocker#1
lesson.

## 1. Mechanical acceptance criteria

```
$ bash bin/check-acs.sh --dry-run .shell-team/specs/T-1014-flag-rail-data-path.md
21 recognized, 0 unrecognized

$ bash bin/check-acs.sh .shell-team/specs/T-1014-flag-rail-data-path.md   (BEFORE creating this file)
check-acs: 18 passed, 1 failed, 2 skipped, 0 unrecognized
check-acs: FAILED: AC18
```
Confirmed the pre-state matched the expected shape exactly (same as
T-1012's AC27 precedent): AC1–AC17, AC19 all PASS; AC18 FAIL solely because
`.shell-team/reviews/T-1014.md` did not exist yet; AC20/AC21 SKIP by design.

AC18's own sub-components verified individually before creating this file:
```
$ bash bin/check-handoff.sh "$(bash bin/team-paths.sh --get todo)"        -> rc=0
$ bash bin/check-board-headings.sh "$(bash bin/team-paths.sh --get todo)" --base develop -> rc=0
$ bash bin/check-pii-shapes.sh --base develop                             -> "clean (no PII-shaped bytes found)", rc=0
```

After creating this file:
```
$ bash bin/check-acs.sh .shell-team/specs/T-1014-flag-rail-data-path.md   (AFTER)
check-acs: 19 passed, 0 failed, 2 skipped, 0 unrecognized (AC20/AC21 SKIP)
```

## 2. Full CI-ordered local run (`.github/workflows/check-handoff.yml`)

Ran every step in the workflow's own order, serially, each as its own
command with rc captured. All 55 steps passed (0 failures). `shellcheck
--version` on this host = 0.11.0, matching the workflow's pin; `develop` and
`origin/develop` are identical locally (confirmed by `git rev-parse` on
both), so the `--base develop` runs above are equivalent to the workflow's
`--base origin/${GITHUB_BASE_REF:-develop}`.

Confirmed node actually ran the harness — no `SKIP:` line anywhere in
`tests/gen-loop-replay/run.sh`'s output. The real, unmutated run shows all
eight frozen rail assertion ids executing and passing, for both fixtures
that exercise them:
```
PASS [flag-rail]: computeSceneB() executed cleanly across 201 timeline samples (0..TOTAL_MS=505000), 23 events, roster size 6
PASS [flag-rail]: rail-precondition: 7 flag events, 5 rail stops, as expected
PASS [flag-rail]: rail-determinism: the rail at simMs=270000 is identical whether reached forward-only or by rewinding first
PASS [flag-rail]: rail-forward-reached: all 5 dots reached, exactly one current after seeking to TOTAL_MS
PASS [flag-rail]: rail-step-back: the first four stops stay reached (including the later READY_FOR_REVIEW), current moves back to READY_FOR_QA
PASS [flag-rail]: rail-rewind: seeking back to 0 after reaching the end leaves zero dots reached and zero current
PASS [flag-rail]: rail-near-miss: none of the three near-miss labels (prose-embedded, lowercase, trailing-space) derive a flag, directly or via the adapted event set
PASS [flag-rail]: rail-uniform-kind: the human-kind row's bare token (READY_FOR_MERGE) derives a flag and is the last flagEvents entry
PASS [mixed]: rail-empty-state: the mixed fixture carries 13 events and zero flag events
gen-loop-replay: all checks passed   (rc=0)
```
Note `rail-determinism` runs immediately after `rail-precondition`, before
`rail-forward-reached`/`rail-step-back`/`rail-rewind` — the actual execution
order matches the reordering the provenance file describes (see §7).

## 3. Mutation self-checks — independently reproduced

Both on file copies under `$TMPDIR` (never `git checkout`; real checkout
confirmed clean throughout). The harness's own `label === 'flag-rail'`
dispatch is fixture-label-sensitive — I generated pages via the real,
unmutated `bin/gen-loop-replay.sh --template <mutated-copy>` and ran the
harness (extracted verbatim from `tests/gen-loop-replay/run.sh`'s heredoc)
against the output, passing the fixture label `flag-rail` exactly (a
misspelled label silently skips every rail-* assertion — confirmed this by
first passing a different label by mistake and observing the rail block
vanish from the output entirely, which is itself the exact detector
blind-spot the spec's own mutation-table warns about).

**Mutation #2 — make the match rule trim the label before comparing.**
Changed `flagFromLabel(label)` to `label.trim()` before the `FLAG_STOPS`
membership check. Regenerated the `flag-rail` fixture's page from the
mutated template (real generator, real fixture) and ran the harness:
```
FAIL [flag-rail]: rail-precondition: expected 7 flag events and 5 rail stops on the flag-rail fixture, got 8 flag event(s) and 5 rail stop(s)
...
FAIL [flag-rail]: rail-near-miss: flagFromLabel() derived a flag for the near-miss label "READY_FOR_QA "
FAIL [flag-rail]: rail-near-miss: an adapted event carrying the near-miss label "READY_FOR_QA " derived a flag
```
Confirms Mutation #2's own spec claim exactly ("`rail-near-miss` must go red
on the trailing-space row specifically") — and also independently confirms
provenance decision #3's reasoning (no early return after
`rail-precondition`): both the generic precondition-count message AND the
specific, more-informative `rail-near-miss` message on the exact offending
row ("READY_FOR_QA ") appear together, rather than the specific one being
swallowed.

**Mutation #5 — restore the module-level accumulator.** Replaced
`railReachedAt(simMs)` with a module-level `flagStopsReached` object that
`updateRailB` writes into and never clears (the pre-T-1014 shape). Ran the
real (unmodified order) harness against the mutated page:
```
PASS [flag-rail]: rail-precondition: 7 flag events, 5 rail stops, as expected
FAIL [flag-rail]: rail-determinism: forward snapshot (...) != rewind-first snapshot (...) at simMs=270000
PASS [flag-rail]: rail-forward-reached: all 5 dots reached, exactly one current after seeking to TOTAL_MS
FAIL [flag-rail]: rail-step-back: expected reached=[...REVIEW] current=[READY_FOR_QA], got reached=[...REVIEW,READY_FOR_MERGE] current=[READY_FOR_QA]
FAIL [flag-rail]: rail-rewind: expected zero reached and zero current after rewinding to 0, got reached=[...all five...] current=[]
```
Confirms the exact predicted asymmetry: `rail-rewind`/`rail-determinism`
red, `rail-forward-reached` still green (`rail-step-back` also goes red
here as a further, consistent consequence — it runs after
`rail-forward-reached` has already saturated the broken accumulator, which
is the same mechanism, not a contradiction of the provenance note).

**Confirmed the reordering fix is itself load-bearing** (this was an
explicit ask): built a second copy of the harness with `rail-determinism`'s
block moved back to its original table-listed position (after
`rail-rewind`, before `rail-near-miss`) and re-ran it against the *same*
Mutation #5 artifact (accumulator still broken):
```
PASS [flag-rail]: rail-forward-reached: all 5 dots reached, exactly one current after seeking to TOTAL_MS
FAIL [flag-rail]: rail-step-back: ...
FAIL [flag-rail]: rail-rewind: ...
PASS [flag-rail]: rail-determinism: the rail at simMs=270000 is identical whether reached forward-only or by rewinding first   <- FALSE PASS, reproduced
```
This reproduces the exact false-pass the provenance file describes:
`rail-determinism` reports green even though the underlying accumulator bug
is still present, purely because `rail-forward-reached`/`rail-step-back`/
`rail-rewind` already ran first and saturated the broken accumulator to
"everything reached" before `rail-determinism`'s own forward/rewind
comparison ran. This directly validates that the assertion-reorder fix
(provenance decision #4) is real and necessary, not cosmetic.

Mutations #1, #3, #4, #6, #7 and the blind-spot attack were not
independently re-run this round (2 of 7 required by the task; time was
spent instead on confirming the reordering fix specifically, per the
coordinator's explicit ask, and on the AC29-successor item below).

## 4. The empty-state direction (`mixed` fixture)

```
$ git diff --name-only 76e64a4 -- tests/gen-loop-replay/fixtures/mixed   -> empty (byte-identical)
$ grep -qF -- 'READY_FOR' tests/gen-loop-replay/fixtures/mixed/shell-team.jsonl  -> no match (AC14 PASS)
```
`tests/gen-loop-replay/run.sh`'s own real (unmutated) run shows
`rail-empty-state` PASS on `mixed` (§2 above): 13 events, zero flag events —
confirming the emptiness is a property of the labels (no `READY_FOR_*`
string anywhere in that fixture), not of an absent event set, and that the
page still renders the ratified empty-state caption path.

## 5. Human-judgment item — emission wording vs. `flagFromLabel` side by side

Read `skills/run/SKILL.md`'s Telemetry section (the `handoff`/`rework`/
`gate`/`human`/`release` bullets) against `templates/loop-replay.html`'s
`flagFromLabel`/`adaptRow`.

**Found one real, subtle asymmetry, matching exactly the class D2's
symmetry table warns is invisible to grep.** D2's table row "uniform across
event kinds (D1)" requires: *emission wording* — "the rule is stated for
the label, not per event id"; *match rule* — "the derivation sits in the
`kind === 'event'` branch, not in a per-id case." The match rule side is
correct and independently confirmed (`adaptRow` derives `flag` in every one
of the five `case` branches, uniformly — and the `rail-uniform-kind`
assertion above proves a bare token on a `human` row really does derive a
flag). The **emission wording side does not match this requirement as
written**: the "put the token here" instruction is placed exclusively
inside the `handoff` bullet ("put it on this same `handoff` event's
`--label`..."), not stated generally for "the label" independent of which
event kind carries it. The separate `human` bullet gives its own,
different recommended label vocabulary (the interventions-class slugs, a
bare `GO`, a `STOP:<reason>`) and never mentions that a bare `READY_FOR_*`
token written there is *also* a legitimate, recognised way to record the
flag — even though D1's own rejected-alternative reasoning is built
explicitly around exactly that scenario ("a human settling the board flag
directly... is exactly the kind of row an operator writes"), and the
fixture's own uniformity-probe row (seq 22) is a `human` event carrying a
bare token for precisely this reason.

Practical consequence: an orchestrator reading `SKILL.md` literally would
learn only one place to put a flag token (`handoff`) and would not learn
that the implementation's deliberate uniformity (D1) also makes a `human`
row's bare token work — the intended breadth of D1's own design goes
under-communicated at the one place a reader would look. This does **not**
break any AC: AC5/AC15 only assert that the two frozen phrases and the five
tokens are present verbatim somewhere in the file, which they are, and the
code's actual behaviour is correct and uniform regardless of what the prose
emphasises. **Not FAIL-driving** — recording it here as the residual
judgment call the spec assigns to QA, for the reviewer/human to decide
whether it merits a documentation fast-follow (a one-clause addition to the
`human` bullet, or a general note above the per-event-id list, would close
it without touching AC5/AC6's additive-only constraint materially).

## 6. AC21 — the rendered page

**No GUI/headless browser was reachable in this sandbox** (same
environmental wall recorded at T-1012 rounds 1 and 2 — a sandboxed
filesystem write restriction outside this session's control, not a
workaround declined; not re-attempted this round since the constraint is
unchanged and already exhaustively probed twice before). **I am not
claiming AC21 verified by observation.**

Generated the `flag-rail` fixture's page anyway with the real, unmutated
generator (non-empty file, `id="loop-replay-data"` present, `READY_FOR_ARCH`
appearing twice as expected — once in the payload, once in `FLAG_STOPS`).
The deepest available substitute is the harness evidence from §2/§3 above:
the rail's dot state (`reached`/`current`/`flash` classes on all five
`railStopsB` entries) is proven, by real execution against the real
generated page, to (a) light for a run carrying flag tokens, (b) advance
`current` forward through playback, (c) preserve a later-reached stop
across a rework step-back, (d) un-reach every stop on a rewind to 0, (e)
render identically for a given `simMs` regardless of how playback got
there, and (f) stay dark/empty for the `mixed` fixture. This is strong
behavioural evidence for exactly the properties AC21 names, but it is
execution-level evidence over class-state, not a pixel observation — I did
not see the dashed-vs-solid dot rendering, the `bNowFlag` chip's visible
text, the `flash` animation's actual appearance, or the transport UI
actually being dragged backward by a human hand. **The maintainer's visual
check at merge time is the correct disposition for those, exactly as this
task's own Problem section names as the gate that caught the original
defect** — I am stating this plainly rather than reporting the visual check
as performed.

## 7. Frozen intent + seam gates

```
$ bash bin/check-intent.sh .shell-team/specs/T-1014-flag-rail-data-path.md .shell-team/todo.md
check-intent: aligned: T-1014 v1 (5066dfa676252cd0c7dbe4d7bdcc0d348243a70c) matches ... -> rc=0

$ bash bin/check-provenance.sh .shell-team/provenance/T-1014.md
check-provenance: conformant: .shell-team/provenance/T-1014.md (4 decision entries, 0 sentinel) -> rc=0

$ bash bin/check-interventions.sh --task T-1014 .shell-team/interventions/T-1014.md
check-interventions: conformant: .shell-team/interventions/T-1014.md (0 entries, 1 sentinel) -> rc=0
```

Spot-checked the assertion-reorder decision (provenance's 4th entry) in
particular, since it is this task's most subtle claim: independently
reproduced its exact failure mode in §3 above (the false-pass on
`rail-determinism` when reordered back to table position, against the same
broken-accumulator artifact) — the decision's account matches what I
observed byte-for-byte, including the specific green/red pattern named.
Also spot-checked decision #1 (the `FLAG_STOPS`/`flagFromLabel` hoist
position) directly against the template: confirmed both are declared
immediately above `adaptRow`'s definition, and `adaptRow`'s five `case`
branches all read `FLAG_STOPS`/`flagFromLabel` after that point in
execution order, so the hoisting hazard the spec's Assumptions flagged is
resolved as described.

## 8. Regressions / adjacent code paths

AC16's byte-for-byte diff-against-base check covers `bin/`, the workflow
directory, both READMEs, both CHANGELOGs, `docs/`, `agents/`, the
prompt-block directory/registry, the board template, the shipped gitignore,
the machine-token and ignore-lock suites, every skill other than `run`, and
T-1011's/T-1012's frozen specs — all unchanged, confirmed by the live
`check:` run, with the positive control (the three named `bin/` paths
actually exist) also passing. No other file in the repository greps for
`flagStopsReached`/`flagFromLabel`/`railReachedAt`, so no adjacent consumer
exists to regress.

## 9. Edge cases tried beyond the spec's own fixtures

- Passing the harness a fixture-label mismatch (a misnamed label instead of
  the exact string `flag-rail`) to confirm the dispatch's own blind spot:
  confirmed the rail-* block silently produces no output at all for a
  mismatched label — exactly the risk the spec's Notes for engineer names.
  This stays inside the harness's own documented dispatch mechanism, not an
  out-of-input-space synthetic extreme.
- The reordering-fix double-check in §3 (undoing the reorder while keeping
  the accumulator mutation) is itself an edge case beyond the spec's literal
  mutation-table wording (which names the accumulator restoration only) —
  grounded directly in the coordinator's explicit ask and in the
  provenance file's own claim, not a synthetic extreme.

## Verdict

All items pass. No regression found in the touched surface; both
independently-reproduced mutations (and the reordering double-check) behave
exactly as the engineer's provenance entries claim; one real but
non-FAIL-driving documentation asymmetry disclosed for the reviewer/human
(§5); AC21 stays honestly unverified on pixels rather than faked.

