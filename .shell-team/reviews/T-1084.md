### Codex Review verdict: REQUEST_CHANGES
- Task: T-1084
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: executed-tests
- Diff scope: 13 files, +725/-3 lines (against branch point `012bd9c6b081bd12e864a3ffecfb514add3a2f21`)

Scope and calibration as stated in the launch brief: deliverable correctness against
the frozen intent (v1, hash `1cd29f980384d18dc2565b9557025d5290c21094`) and the
spec's runtime criteria, shipped-code adversarial depth on the gate. Two Codex
passes ran: a primary `codex exec review --base <branch point>` pass reading the
real diff, and — triggered by that pass's own Major finding, per this role's
step-5 opt-in rule — one adversarial `codex exec` pass targeting
`bin/close-out.sh`'s new gate specifically. Every finding below was independently
reproduced by me, live, against the real `bin/close-out.sh` on scratch boards
under `$TMPDIR` (`TEAM_TODO`/`TEAM_INTERVENTIONS_DIR`/`TEAM_RUNS_DIR` redirected,
the real board never touched) — not accepted from Codex's text unverified.

#### Blockers
None.

#### Major

- `bin/close-out.sh` (T-1084 gate, ground-field check, ~line 273) — **an empty
  priced-line id passes the ground-shape check.** The check is
  `grep -qE -- '^(saving|recommendation|break-even|cost-input): '` — it verifies
  the prefix and the following space only, never that a non-empty `<id>` token
  follows. A record such as
  `- dispatch: implement — serial — unconditional — recommendation: ` (prefix,
  space, nothing) satisfies this regex and is accepted. Reproduced live: a board
  entry carrying exactly that line closes out with exit 0 and is moved to
  `## Done` unchanged. This directly contradicts the shipped grammar's own text
  (`templates/prompt-blocks/dispatch-record.md`: "`<ground>` opens with a priced
  line id the note already records ... `recommendation: <id>` ... so a routing
  choice cites a recorded price and never an invented one") and this
  repository's own fail-closed discipline (`CLAUDE.md` Working rules: "A check
  that cannot evaluate its input reports an error; it never silently passes").
  Not covered by the spec's `## Input space` "Out-of-scope synthetic extremes"
  list (that list is about megabyte-long/NUL/control-character ground fields
  and unicode-lookalike separators, not an empty id) — this is a realistic
  authoring slip (dropping the id while leaving the label), not a synthetic
  extreme, so no `out-of-input-space` downgrade applies.
  Suggested fix: require a non-empty `[a-z0-9-]+`-shaped (or otherwise
  non-whitespace) token immediately after the matched prefix before accepting
  the ground field, and add an id-less-ground fixture to `tests/close-out/run.sh`.

- `bin/close-out.sh` (T-1084 gate, candidate-line scan, ~line 230) — **the
  initial candidate-detection anchor requires a literal ASCII space
  immediately after `dispatch:`, so a line opening `- dispatch:` with anything
  else there (a tab is the reproduced case) is invisible to the whole gate —
  not merely refused on a looser check, but never even inspected.** The scan is
  `grep -E -- '^[[:space:]]*- dispatch: '`, which is a literal-string match
  requiring the exact byte `0x20` right after the colon. Reproduced live: a
  board entry carrying
  `  - dispatch:<TAB>verify — serial — maybe — because I said so` (a line that
  is simultaneously malformed on *three* independent axes — a modality outside
  `unconditional|conditional`, and a ground opening with none of the four
  priced-line prefixes) closes out with **exit 0, no stderr output at all**, and
  is copied verbatim into `## Done`. This is a full bypass of the gate's central
  purpose (fail-closed validation of every `- dispatch:` sub-bullet present),
  triggered by a single-character typo that is entirely plausible in hand-edited
  or copy-pasted board content, and it is not caught by any other safety net —
  unlike the Unicode-indentation finding below, the line stays correctly nested
  inside the entry's continuation extent (`bin/check-handoff.sh`'s canon and
  `bin/close-out.sh`'s own `A_START..A_END` awk-based extent computation both
  still see it as part of the entry), so there is no downstream re-validation
  failure to catch it. I confirmed by contrast that a *recognized* candidate
  with a non-em-dash separator (e.g. `- dispatch: verify - serial - maybe - ...`
  with hyphen-minus) **is** correctly refused, because it reaches the
  field-extraction `sed` passes and those fail closed when a field doesn't
  extract — the defect is specifically that the wrong internal separator after
  `dispatch:` prevents the line from ever reaching that extraction path at all.
  Suggested fix (matches the adversarial pass's own suggestion, confirmed by my
  own reproduction): loosen the candidate-scan anchor to
  `^[[:space:]]*- dispatch:` (no trailing-space requirement) and let the
  existing field-extraction `sed` passes do the fail-closed rejection of
  anything that doesn't decompose into the four ` — `-separated fields — the
  same pattern the gate already uses correctly for a wrong separator. Add a
  wrong-whitespace-after-colon fixture to `tests/close-out/run.sh`.

  **Root-cause observation, common to both Major findings above:** the
  engineer's own 18-mutation self-check and QA's own 8 additional empirical
  probes are unusually thorough (per the board's `### Local test result` and
  `### QA round 1` sub-bullets), yet neither tried "prefix present, id empty"
  nor "correct-shaped opener, wrong internal separator" — both are typo classes
  a real board author would plausibly produce, and both are input-space gaps
  in the fixture matrix rather than gaps the spec's own Input space section
  discusses. I recommend the fixed matrix explicitly add these two shapes.

#### Minor / nits

- `bin/close-out.sh` (shared, pre-existing `A_START..A_END` entry-extent scan,
  `pass 1`, ~line 172) vs. `bin/check-handoff.sh`'s `CONTINUATION_RE`
  (~line 131) — **the two share the identical pattern text
  `^[[:space:]]+[^[:space:]]` but apply it through two different regex
  engines (awk vs. bash `[[ =~ ]]`), which classify at least one real
  character differently on this host.** Reproduced directly on this checkout
  (macOS, bash 3.2.57, "one true awk" 20200816, `en_US.UTF-8`): a line
  indented with U+3000 (IDEOGRAPHIC SPACE, a full-width space real in
  Japanese-authored text, which this very repository's board and
  `CLAUDE.local.md` already carry elsewhere) matches bash's `[[:space:]]`
  class (`bash: MATCH`) but **not** awk's (`awk: NO MATCH`). Consequence: a
  malformed `- dispatch:` sub-bullet indented that way is excluded from
  `close-out.sh`'s own `A_START..A_END` scan window (so the T-1084 gate never
  sees it), while `check-handoff.sh` still considers it part of the entry.
  I reproduced the full end-to-end consequence: close-out.sh's own final
  safety step — re-validating the rewritten temp board against
  `check-handoff.sh` before it may replace the real board (this script's own
  stated design, header comment lines 51–53) — catches the resulting
  "stranded continuation line" and **refuses the whole close-out (exit 1, real
  board byte-untouched)**. So the fail-closed guarantee holds in practice: the
  malformed record never reaches `## Done`. The residual defect is a
  **misleading refusal message** (a strand-violation message from
  `check-handoff.sh`, not this gate's own `malformed dispatch record` message
  naming the actual defect) that could misdirect an operator's diagnosis.
  This is **inherited, not new**: the `A_START..A_END` computation predates
  T-1084 (T-1084 only adds a scan *inside* that pre-existing window) and the
  same divergence risk already applies identically to the T-068 `pending:`
  gate this task is explicitly required to mirror — so it is a
  shared-infrastructure property, not a defect this task's own diff
  introduces. Not gating this task's merge; recommended as a fast-follow (see
  below).

#### Adversarial findings

Two independent Codex adversarial passes were run (the first hung on a
quoting/stdin issue and was abandoned after producing no usable output; the
second completed and is the one reported here — both attempts and their raw
captures are preserved verbatim in `.shell-team/reviews/T-1084-codex-adversarial.{jsonl,txt}`,
the completed run's own output). Devil's-advocate framing against
`bin/close-out.sh`'s new gate surfaced, verbatim severities as Codex assigned
them (before my own re-classification above): the id-less-ground gap (major,
matches the primary pass's independent finding of the same defect), the
tab-after-`dispatch:` candidate-detection bypass (major), and the awk-vs-bash
Unicode-whitespace divergence on `A_START..A_END` (major, which I
re-classified to minor after confirming the downstream `check-handoff.sh`
safety net actually catches it — see the ledger below). The primary pass
(`codex exec review --base <branch point>`, real-file read, no pasted diff)
independently surfaced only the id-less-ground finding as its single reported
comment, after an extensive 26-command-execution investigation that also
covered the three disclosed permanent new reds (T-1048 AC12, T-1083 AC2/AC4)
without objecting to their disclosure.

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | awk-vs-bash Unicode-whitespace divergence excludes a malformed `- dispatch:` line from the gate's scan window (U+3000 indentation) | adversarial: major | minor | reproduced end-to-end on this host: `close-out.sh`'s own pre-existing downstream `check-handoff.sh` re-validation of the rewritten temp board catches the resulting stranded-continuation-line and refuses (exit 1, board byte-untouched) before any write — the malformed record never reaches `## Done`, so the fail-closed guarantee holds; the residual defect is a misleading refusal message, not a bypass, and the underlying `A_START..A_END` scan predates this task. |

No other finding's severity was changed and none was rejected — both Major
findings above matched between the primary and/or adversarial passes and my
own independent live reproduction, and I found no additional gate holes beyond
the three Codex surfaced.

#### Independent verification performed beyond the two Codex passes

- **Fail-closed completeness (dimension 1):** confirmed the value-set check
  (`grep -qE -- "^(${d_valueset})\$"`), the modality check, and the axis-key
  lookup are all correctly anchored both directions (a value from the wrong
  axis, an unknown axis, and a duplicated axis are all refused; live-tested
  during reproduction of the wrong-separator case above, which correctly
  refuses). Confirmed the axis→value-set table
  (`DISPATCH_AXIS_TABLE`) is a plain data table with no axis-count assumption
  anywhere in the loop logic, matching the extensibility claim (adding
  issue #274's depth axis is a data edit).
- **Grammar closure (dimension 2):** confirmed both closed value sets and the
  four-prefix ground vocabulary are closed in both directions (except the
  id-emptiness gap above); confirmed the shipped canonical block's own
  "Validation" bullet does not overclaim ground-id-existence checking (that
  half is correctly left to **AC9**, this repository's own concern, not an
  adopter-facing claim) — the portability-boundary text is honest.
- **Mirror/symmetry (dimension 3):** re-derived `contain`-mode containment
  independently of `bin/check-prompt-sync.sh` (all 10 non-empty lines of
  `templates/prompt-blocks/dispatch-record.md` found verbatim in both
  `agents/tech-lead.md` and `skills/run/SKILL.md`); `check-prompt-sync.sh`
  itself reports in sync; `registry.txt` gained exactly the one expected
  `contain dispatch-record.md ...` line; `agents/tech-lead.md`'s `tools:` line
  is untouched in the diff; the T-1075 SKILL.md bullet is a genuine demotion
  (mechanics text retained, decision-ownership language moved to the
  `dispatch: verify` record) rather than a silent duplication.
- **The three disclosed permanent new reds (dimension 4):** spot-re-ran
  `bin/check-acs.sh` on `T-1083-agent-launch-fanout.md` myself at HEAD —
  confirmed **AC2** and **AC4** both FAIL exactly as disclosed, for exactly the
  reasons stated (T-1084's own **AC6** rewrites the pre-existing T-1075 bullet
  in place; T-1084's own diff touches `bin/`/`tests/`). My own judgment: these
  are genuine, structural, correctly-disclosed conflicts between this task's
  own frozen mandate and two earlier tasks' undisclaimed byte-identity/
  line-survival locks — not avoidable without this task violating its own
  frozen AC1/AC6/AC7, and not this task's Non-goals-permitted to repair. Agree
  with engineer's and QA's independent judgment.
  **Issue-canon note:** this judgment, and **AC2**'s literal precedence
  sentence, both rest in part on issue #308 item 2's actual wording. I have no
  GitHub access in this sandbox: internal consistency verified / issue-canon
  match environmentally-unverified (relies on the coordinating session's own
  primary confirmation, already recorded in this spec's `## Assumptions`
  "Relayed premises" section as measured live against the GitHub API at
  freeze time).
- **Live records (dimension 5):** cross-checked both of this task's own board
  sub-bullets against `docs/loop-engineering/phase-multiplexing.md`'s actual
  `- dispatch-rule:`/`- recommendation:` lines. Both resolve correctly; the
  `verify` axis record's own stated reasoning (population clause satisfied,
  second conjunct — default-path N-agent launching — not, so the precedence
  rider correctly falls back to the `serial` default with its ground) matches
  the note's own `- dispatch-rule: verify — tier1-fanout — conditional —
  trigger: ...` line precisely.
- **Untouched surfaces (dimension 6):** confirmed
  `docs/loop-engineering/phase-multiplexing.md`,
  `templates/prompt-blocks/fanout-orchestration.md`,
  `bin/aggregate-verdicts.sh`, `bin/check-fanout-instances.sh` and
  `docs/loop-engineering/agent-launch-fanout.md` are absent from the diff
  entirely (not merely byte-identical after an edit); confirmed
  `- agent-type:` occurs zero times in `fanout-orchestration.md` (issue #319
  not smuggled in).

#### Recommendation

- REQUEST_CHANGES → set `.shell-team/todo.md`'s T-1084 entry back to
  `READY_FOR_ENG` with the two Major findings above linked (the id-less-ground
  gap and the tab-after-`dispatch:` candidate-detection bypass, both in
  `bin/close-out.sh`'s new gate). Both are realistic, reproduced, silent
  fail-closed violations of this task's own central deliverable and should be
  fixed and re-fixtured before merge. The Minor finding (awk-vs-bash Unicode
  divergence) does not block merge — see the fast-follow disposition below.

#### Fast-follow disposition

- The awk-vs-bash `[[:space:]]` classification divergence on
  `A_START..A_END` (U+3000 reproduced) — **file-an-issue**. Rationale: it is
  real, reproducible on at least one supported host, and produces a
  misleading refusal message, but it does not defeat the fail-closed
  guarantee (the downstream `check-handoff.sh` re-validation catches it) and
  the defective computation is pre-existing shared infrastructure
  (`A_START..A_END`, reused by the T-068 `pending:` gate too) rather than
  code this task's own diff introduces — fixing it well means auditing every
  gate that scans within that window, which is a larger, cross-cutting task
  than a rework round on T-1084 alone.

---

### Codex Review verdict (round 2 — delta over round 1): APPROVE
- Task: T-1084
- Codex model: gpt-5.6-luna
- Codex CLI: codex-cli 0.147.0
- Verification mode: executed-tests
- Diff scope (round-1 review commit `d69cb16` → round-2 HEAD `dd64db4`): 4 files, +150/-4 lines — `bin/close-out.sh` (32行、2ハンクのみ)、`tests/close-out/run.sh`（+47 行の回帰フィクスチャ追加）、`.shell-team/provenance/T-1084.md`（決定エントリ 1 件追加）、`.shell-team/todo.md`（rework hand-off + QA round 2 の 2 セクション追加）。

このラウンドは round 1 の 2 件の Major をエンジニアが「1 クラス」として修正したか、QA がそれを再検証したかのデルタレビュー。round 1 の全文（Blockers/Major/Minor/Adversarial/Ledger/Independent verification）は上のブロックを参照 — ここでは再論しない。

Codex 呼び出しは 1 回のみ（primary、`codex exec review --base d69cb16 --json -o ... `、real diff を自分で読む方式）。primary が何も新規欠陥を報告しなかったため（下記引用）、round 1 のステップ 5 の opt-in トリガ（新規 Blocker/Major）は不成立 — adversarial パスは今回起動しない。primary の生出力（`.shell-team/reviews/T-1084-codex-primary.{txt,jsonl}` — round 1 の同名ファイルを round 2 の内容で正しく上書き、`-r2` という誤った別名で一時的に作成してしまった副産物は同一ラウンド内で削除済み）:

> "The changes correctly close the two previously identified validation bypasses and add regression coverage for them. No additional defects were found in the diff."

#### Blockers
None.

#### Major
None.

**1. round 1 の 2 件の Major がクラスとして閉じているか — 自分自身のスクラッチボードで両方を再現。** `$TMPDIR` 配下のスクラッチボード（実ボード非接触、`TEAM_TODO`/`TEAM_INTERVENTIONS_DIR` をリダイレクト）で、エンジニアのハンドオフの記述通りの正確な再現を自分の手で実行した:
- Major 1（空の ground id）: `- dispatch: verify — serial — unconditional — recommendation: ` → **rc=1**, `does not open with a non-empty id after saving:/recommendation:/break-even:/cost-input:` — 拒否、board 未変更。
- Major 2（`- dispatch:` 直後の TAB）: `  - dispatch:<TAB>verify — serial — unconditional — recommendation: tier1-verification-fanout` → **rc=1**, `does not match the grammar '- dispatch: <axis> — <value> — <unconditional|conditional> — <ground>'` — 見えて（SEEN）拒否されている。silent bypass ではない。

両方ともエンジニア/QA のハンドオフの記述と完全一致。加えて `tests/close-out/run.sh` を自分で実行（`bash tests/close-out/run.sh`）: **rc=0 / PASS 84 / PASS:T-1084 18 / FAIL 0** — ハンドオフの数値と完全一致。

**2. 自分自身の追加ハントで境界を越えた形状を探索**（round 1 で自分が見つけた 2 件の Major の同じクラス＝「アンカー直後の空白揺れ」を、アンカーの前後・別バイトで再確認）:
- 垂直タブ (0x0B) を `- dispatch:` 直後に置いた行 → `does not match the grammar` で拒否（rc=1）。widened anchor が「見える化」した上で strict extraction が拒否する設計どおり。
- ground id が単一ハイフン `-` のみ（`[a-z0-9-]+` の最小一致）→ dispatch gate 自体は通過する。これは新規の穴ではなく、**spec 自身が明示的に宣言した portability boundary**（"The shipped gate validates the ground field's **shape**... never whether the named id exists" — spec 400 行目）と一致する。既存の存在確認は **AC9**（この repo 自身の live instance チェック）の役割であり、shipped gate の責務外と明記されている。honest な境界。
- `recommendation:-x`（`:` の直後に空白なし、id 直結）→ ground 正規表現は `: ` (colon+space) を要求するため一致せず、`does not open with a non-empty id` で正しく拒否。

**3. `-- dispatch:`（二重ハイフン）／コロン欠落／コロン直前スペース — レビュー focus item 1 の「honest か dodge か」判定。** ハンドオフの enumeration table row 12 はこの 3 形状を「同一の anchor-literal boundary、pending: gate と同じ、scope 外」として一括りにしている。自分のスクラッチボードで実測（正しい hand-off grammar のボード雛形を用意した上で）:
  - 二重ハイフン `-- dispatch: ...` → **rc=0, silent, Done へ移動**（gate は一切発火しない）。
  - コロン欠落 `- dispatch verify — ...` → 同じく **rc=0, silent**。
  - コロン直前スペース `- dispatch : verify — ...` → 同じく **rc=0, silent**。

  **判定: 二重ハイフンとコロン欠落は honest。** これらは「`- dispatch:` という文字列そのものにすら一致しない」＝markdown 上も別物のトークンで、pending: gate の `- fast-follow disposition (` という anchor が全く同じ設計判断（literal 文字列一致のみ、内側のフィールド検証は sed 抽出に委ねる）を今回のタスク以前から持っている。round 1 で閉じた 2 件の Major は「anchor に一致した行が、その先の中身検証をすり抜けていた」という別のバグクラスであり、anchor 自体が一致しない入力を「no record present」として扱うのは AC7/Input space row 1 の既存の設計（record 不在は adopter 互換のため無言で通す）と整合する。

  **コロン直前スペースだけは判断が割れる、が最終的に non-blocking Minor と判定。** 見た目上は正当な record とほぼ区別がつかない一文字の打鍵ミスで、視覚的に怪しく見えない点は round 1 で閉じた TAB-after-colon の Major 2 と同種のリスク特性を持つ。一方で、この打鍵ミスは「anchor 文字列そのもの」への揺れであり、「anchor 一致後のフィールド内空白」への揺れだった Major 2 とは技術的に異なるレイヤーにある。pending: gate も同じ「anchor 文字列そのものへの空白揺れ」に対して同じ脆弱性を昔から持っており、今回のタスクが発明した dodge ではなく、この repo が長年採用してきた「anchor は literal 固定文字列、内部のみ fail-closed」という設計方針の一貫した適用と判断する。frozen `## Input space` にもこの形状は reachable class として名指しされていない。**ブロッキングではないが、fast-follow として明示的にディスポジションする**（下記参照）。

**4. tightened ground regex と shipped norm text / AC9 抽出の整合性、tab-indent positive control。** `CHECK_ACS_TIMEOUT=300 bash bin/check-acs.sh .shell-team/specs/T-1084-dispatch-routing-record.md` → **`16 passed, 0 failed, 1 skipped`**（round 1・hand-off と一致、AC9 含む）。自分のスクラッチボードで tab インデントされた**適合レコード**（この spec 自身の reachable Input space クラス）を直接再実行 → **rc=0, silent, Done へ移動** — widened anchor が実在レコードを誤って拒否していないことを独立に確認（`tests/close-out/run.sh` の `T-987` と同じ結論を自分のコマンドで再現）。

**5. round 1 の Minor（awk/bash U+3000 divergence）が未変更であることの確認。** `git diff d69cb16..HEAD -- bin/close-out.sh` は 2 ハンク（216行目、288行目周辺）のみで、いずれも A_START..A_END の awk 計算（163〜180行目）から独立している。`git diff d69cb16..HEAD -- bin/close-out.sh | grep -c "A_START..A_END\|awk -v task"` の 1 件のヒットはコンテキスト行（変更なし）のみで、実際の awk ブロックへの diff は **ゼロ行**。Minor の disposition（`file-an-issue`）に対応するコードパスは物理的に触れられていない。

**6. それ以外は何も動いていないことの確認。** `git diff d69cb16..HEAD --name-only` は上記 4 ファイルのみ。`git diff d69cb16..HEAD -- templates/ agents/ skills/ docs/ README.md README.ja.md` は **0 行**（凍結 intent block・canonical block・mirrors・両 README はバイト同一）。`bash bin/check-intent.sh` → `aligned: T-1084 v1 (1cd29f980384d18dc2565b9557025d5290c21094)` — hash 変化なし。`shellcheck bin/close-out.sh tests/close-out/run.sh` → rc=0。`bash bin/check-provenance.sh .shell-team/provenance/T-1084.md` → `conformant (4 decision entries, 0 sentinel)`。

**7. 3 件の disclosed permanent red の現状確認。** `T-1048` **AC12** は round 1 と同一理由で FAIL のまま（`bin/close-out.sh`/`tests/close-out/run.sh` が base から変化し続けているため — 恒久的な性質、正しく disclosed）。`T-1083` **AC2**/**AC4** も round 1 と同一理由で FAIL のまま（T-1084 自身の frozen AC1/AC6/AC7 が要求する編集が T-1083 の byte-identical/line-survival ロックと構造的に衝突）。加えて、この 2 つの spec でそれぞれ **AC16**（T-1048、`- close-out.sh`/`tests/close-out/run.sh` は allow-list 内だが T-1084 の provenance/spec/review/interventions ファイルは allow-list 外）と **AC20**（T-1083、同様の理由）も FAIL していることを確認した。**これは新規の懸念ではない**: 両方とも spec 本文が「**merge-point-scoped and expected to go stale after merge**...not a defect to chase」と明示的に自己宣言している criteria で、T-1048 の AC16 は実測で T-1050〜T-1084 の全タスクの成果物（spec/provenance/review/interventions が数十件）が既に allow-list 外に積み上がっており、はるか以前から stale。T-1084 のこのラウンドの diff が原因で新たに壊れたものではなく、review round 1 が確認した「3件の disclosed permanent red」とは別カテゴリ（merge-point-scoped の自己宣言済み staleness）に属する。round 1 のレビュー範囲・disclosure の正確性を損なうものではない。

#### Minor / nits

- `bin/close-out.sh`（dispatch gate、candidate-scan anchor）— **コロン直前の空白揺れ**（`- dispatch : ...`、二重ハイフンやコロン欠落とは異なり、正当な record と視覚的にほぼ区別がつかない一文字の打鍵ミス）は round 2 のクラス修正の対象外のまま、silent bypass（rc=0, Done へ移動、実測確認済み）。round 1 で閉じた 2 件の Major と同種のリスク特性を持つが、pending: gate も同一の anchor-literal 設計を長年採用しており、このタスクが発明した dodge ではないと判断し non-blocking。fast-follow として明示的にディスポジションする（下記）。

#### Adversarial findings

今回は primary パスが新規欠陥を報告しなかったため（opt-in トリガ不成立）、adversarial パスは起動していない。上記の Minor（コロン直前の空白揺れ）は Codex ではなく自分自身の追加ハントで見つけたもの。

#### Synthesis audit ledger

| # | finding | primary / adversarial | final | reason |
|---|---------|-----------------------|-------|--------|
| 1 | colon 直前の空白揺れ（`- dispatch : ...`）による silent bypass | 自分の追加ハント（Codex 未報告） | minor | round 1 の Major 2 と同種のリスク特性を持つが、anchor 文字列そのものへの揺れであり、既存の pending: gate も同一設計の脆弱性を持つ長年の一貫した境界。frozen Input space にも reachable class として名指しされていない。ブロッキングではないが記録し fast-follow へ回す。 |

上記 1 件以外、severity の変更・却下はなし。round 1 の 2 件の Major はいずれも「クラスとして閉じた」と判定し、新規の Blocker/Major は今回のラウンドで発見していない。

#### Recommendation

- **APPROVE** → `.shell-team/todo.md` の T-1084 エントリを `READY_FOR_MERGE` にする。round 1 の 2 件の Major は自分自身の live reproduction で class-closed と確認済み、既存の 84/18/0 スイート結果・check-acs 16/16・check-intent hash 一致・shellcheck clean を自分のコマンドで再実行し全て一致。frozen intent・canonical block・mirrors・disclosed permanent red は全てバイト同一/理由同一で不変。新規の Blocker/Major なし。

#### Fast-follow disposition

- round 1 由来: awk-vs-bash `[[:space:]]` classification divergence（U+3000、`A_START..A_END`）— **file-an-issue**（round 1 の理由のまま、今回のラウンドで一切触れられていないことを確認済み）。
- round 2 で新規発見: `bin/close-out.sh` dispatch gate の candidate-scan anchor が、コロン**直前**の空白揺れ（例: `- dispatch : ...`）に対しては round 1 で閉じたコロン**直後**の空白揺れと同じ silent-bypass 特性を残している — **file-an-issue**。理由: 正当な record と視覚的にほぼ区別がつかない打鍵ミスであり、コロン直後の空白揺れ（round 1 の Major 2）と対称的な入力形状であるため、修正するなら「anchor を colon の前後両方向で widen し、strict extraction に fail-closed を委ねる」という round 1 と同じパターンの適用で済むはず。ただし frozen Input space に reachable class として名指しされておらず、pending: gate も同一の設計上の境界を長年抱えたまま一度も指摘されていないため、このラウンドのリワークとして押し込むより、A_START..A_END の Minor と合わせて「close-out.sh の anchor-literal 境界の横断監査」としてまとめて次の fast-follow で扱うのが妥当。
