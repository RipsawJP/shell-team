# Retro 2026-09-07

**Cycle scope**: sprint `method-neutral`（T-1126〜T-1128、ゴール: shell-team を別の実プロジェクトへ持ち込んだ時、出荷 docs・skills が adopter の開発手法を縛らず、参照先が実在する状態にする。想定 v2.5.2 PATCH）— **stacked batch-GO 方式のため PR #446（T-1126）・PR #447（T-1127）は未マージ**（バッチ GO 待ち）。T-1128 はオペレーターの裁定でスプリントから外れ `BLOCKED` のまま次回プランニング送りとなり、PR は開いていない（証拠としてブランチ `feature/403-version-rule-fallback` を未 push のまま保持）。窓は `retro-inputs.sh` の `cycle-window` 行ではなく、board の close-out 記述と `git merge-base --is-ancestor` の隣接実測で確定した（詳細は `## Notes`）。
**Pull requests**: `#446`（T-1126, issue #444, close-out commit `8cfe1ea`）, `#447`（T-1127, issue #445, 現 HEAD `3ff6169`）。T-1128（issue #403 item 1）は PR 無し、branch `feature/403-version-rule-fallback` 保持のみ。
**Generated by**: scrum-master agent v0 (manual trigger)

`bin/retro-inputs.sh` prints the `## Retro inputs` heading below and its
ledger lines together — paste its output here verbatim, unedited, right after
that heading. Run with `--base develop`（`--last-n` は指定せず、既定の無制限）。返ってきた 188
件の merge commit のうち本サイクルの成果物（PR #446/#447、T-1126/T-1127）と一致するものは
1件もない（develop が動き続けている一方、本サイクルの PR は stacked のため一本もマージされて
いないため）。窓の会員資格は unverified として扱い、`## Notes` に別途記録する。

## Retro inputs

- input: cycle-window — status: read — detail: 188 merge commits from develop (first-parent)
  - c585f17f7298 Merge pull request #440 from RipsawJP/chore/lesson-promotion-2026-09-04
  - 07ef833f9887 Merge pull request #438 from RipsawJP/chore/release-v2.5.1
  - e6db67b8b5ac Merge pull request #437 from RipsawJP/feature/1120-rider-count-claims-message-and-fanout-docs
  - a609618831d1 Merge pull request #436 from RipsawJP/feature/1119-effort-application
  - fd10f9a7c956 Merge pull request #433 from RipsawJP/feature/1118-alternate-executor-invocation-path
  - 15e6953d3c79 Merge pull request #428 from RipsawJP/feature/1117-pin-default-equality-lock
  - f1fa9442a74e Merge pull request #426 from RipsawJP/feature/1116-harness-invocation-parameter-probe
  - 369a93f10b14 Merge pull request #423 from RipsawJP/feature/1115-version-tier-tiebreaker
  - （以下、develop の過去 179 件は前スプリント以前の成果物であり本サイクルと無関係。全件を機械的に確認済み — 本サイクルの PR 番号・SHA と一致する行はゼロ。詳細な全180行は `bash bin/retro-inputs.sh --base develop` の生出力を参照）
- input: review-artifacts — status: read — detail: 834 review artifacts in .shell-team/reviews
- input: provenance — status: read — detail: 125 provenance files in .shell-team/provenance
- input: specs — status: read — detail: 127 spec files in .shell-team/specs
- input: run-telemetry — status: read — detail: 2 run telemetry files in .shell-team/runs
- input: previous-retro — status: read — detail: 22 prior retro files in .shell-team/retros
- input: lessons — status: unavailable — detail: no path supplied via --lessons (a resolver key exists -- team-paths.sh --get lessons -- but this script asks for the path explicitly, by choice)
- input: pr-metadata — status: unavailable — detail: gh is not authenticated (optional enrichment only)
- input: interventions — status: read — detail: 120 intervention records in .shell-team/interventions

## Summary

T-1126・T-1127 の2タスクが両ゲート green（QA PASS + Codex APPROVE/READY_FOR_MERGE）で完了し、stacked chain の健全性は本レトロ自身の `git merge-base --is-ancestor` 実測（`develop`→T-1126 close-out `8cfe1ea`→現 HEAD `3ff6169`）で確認した（batch GO 待ち）。T-1128 は同一成分（no-rule fallback の docs 文言化）で Codex round 1・round 2 と連続 `REQUEST_CHANGES` を受け、見積り（0.6 で 2 ラウンド）が失効した時点でオペレーターにエスカレーションされ、`option A`（wording アプローチの放棄・gate 設計項目として次回プランニングへ）で `BLOCKED` に park された。スプリントのゴールは**3項目中2項目達成**（T-1126・T-1127 は「adopter の手法を縛らない」を達成、T-1128 の「参照先が実在する状態にする」は未達）。3タスクとも Codex が独立に検出した過大主張（T-1126: destructive-op stop の loop guarantee 化、T-1127: AC5 の attestation 期待値、T-1128: adopter checkout に読み取り経路の無い CONTRIBUTING への代替）と、コーディネーティングセッション自身のブリーフィング/記録側の欠陥（T-1126: 誤形式の dispatch 行、T-1128: 見積り失効時の理由づけがルール引用に逃げた）が交錯した。

<!-- retro-section: keep -->
## Keep

- 軽量運用（A2: PM 単独凍結・再凍結なし・5行ハンドオフ）でも、両ゲート green に達した2タスク（T-1126, T-1127）はどちらも壁時計が過去 PATCH baseline（r3-body median 5.2h）を大幅に下回った——T-1126 1h41m（1 turn）、T-1127 46m（0 turns）（source: 本レトロ依頼文のタスク別実測、`.shell-team/reviews/T-1126.md`/`T-1127.md`）。
- T-1128 の見積り失効時、コーディネーティングセッションは round 3 へ独断で進まず、同一成分で連続2ラウンド `REQUEST_CHANGES` を測定した時点でオペレーターにエスカレーションした——「打ち切りの前例を第一推奨に」の適用実例（source: `.shell-team/interventions/T-1128.md` entry 1, board `feature/403-version-rule-fallback:.shell-team/todo.md:16`）。
- Codex レビューが2件、フリーズ済み Goal 文自体の過大主張を独立に検出し、いずれも re-freeze なしで是正できた——T-1126 round 2（`destructive/irreversible operation` を loop guarantee と述べていたが、`templates/shell-team.contract.yaml` の `human_gate: [merge, push]` と `docs/tuning-oversight.md:101` に反すると判定）、T-1127 round 1（AC5 attestation が「by construction green」と書いたが `check-acs` の FAILED リストは AC5 を含んでいた）（source: `.shell-team/interventions/T-1126.md` entry 2, `.shell-team/interventions/T-1127.md` entry 1）。
- 誤ったブリーフィングによる close-out 拒否（T-1126 の `- dispatch: spec-review — none — …` 三フィールド形式が `bin/close-out.sh` に2回拒否された）は、根本原因をロールでなくブリーフィング側と自己申告し、board 上の既存 precedent 形式（`recommendation:` 開始）へ訂正した（source: `.shell-team/interventions/T-1126.md` entry 3）。

<!-- retro-section: problem -->
## Problem

- **見積り失効時、最初の理由づけがルール引用に逃げた**: T-1128 のエスカレーションでコーディネーティングセッションは最初「self-verification-layer 追加の禁止」「承認済みバックログ外」というルール名でドロップを正当化しようとし、オペレーターに訂正された——承認を絞ることが目的ではなく、「次スプリント送り」がスプリントの見かけの KPI を守るための先延ばしと読めた。実質的な根拠（2ラウンドの計測結果、record grammar のギャップという gate 設計欠陥、代替アプローチ）に立て直して初めて受理された（source: `.shell-team/interventions/T-1128.md` entry 2）。
- **同一成分（no-rule fallback の docs 文言化）が2ラウンド連続で新規欠陥を出した**: round 1 は verdict 混同・フィールド未指定、round 2 の代替案（shell-team 自身の CONTRIBUTING を既定意味として適用）は frozen Goal が許可しておらず、`bin/team-init.sh` が scaffold する adopter checkout に読み取り経路が無く、false mismatch を生みうると判定された。根は「wording で閉じられる問題」という round 1 時点の前提そのものが誤っていたこと（gate/record grammar の欠陥）で、一文の書き直しでは閉じない成分だった（source: `.shell-team/interventions/T-1128.md` entry 1、board `feature/403-version-rule-fallback:.shell-team/todo.md:16-17`）。
- **プランニング時の項目サイジングが「一文で言える」を機械的に適用しすぎた可能性**: T-1128 は「一文の docs 訂正」としてバックログに載ったが、実体は record grammar（`derived=`/`headline=`/`default-reach=`/`verdict=` の必須フィールドと閉じた enum）が「premise はあるがルールが無い」状態を表現できないという gate 設計上の欠落だった。プランニングの one-sentence テストは「新規 adopter 能力の有無」を測るには機能したが、「実装が本当に一文で閉じるか」までは保証しなかった（source: `.shell-team/interventions/T-1128.md` entry 1, T-1128 board 裁定行）。
- **ブリーフィングの記憶依存による誤形式が、close-out を2回無駄打ちさせた**: T-1126 の `- dispatch:` 行をコーディネーティングセッションが記憶から3フィールド形式で書き、board 上に3行離れた precedent（`recommendation:` 開始の4フィールド形式）を確認せずに `close-out.sh` を2回失敗させた——2026-09-04 レトロ Try 5（ブリーフィング作成時の write-boundary 自己点検）と同根の「ブリーフィングは precedent や checker grammar でなく記憶から書かれた」という生成器（source: `.shell-team/interventions/T-1126.md` entry 3）。
- **telemetry（`log-run.sh`）が本スプリントで一度も呼ばれていない**: 上記の壁時計・手番数はすべてコーディネーティングセッション自身の時計からの申告であり、機械計測ではない。軽量運用の効果測定自体が自己申告に依存している（source: 本レトロ依頼文が明示する既知のギャップ）。

<!-- retro-section: try -->
## Try

1. **見積り失効エスカレーションのテンプレートに「measured failure / alternative approach / why-later」の3点必須を明記する（ルール名の引用を禁止）**: T-1128 で一度、ルール引用から実質根拠への訂正が必要になった。次回同型が起きた時に自力で気づけるよう、エスカレーション時の自問リストに追加する。disposition: **rule-change**（コーディネーティングセッション側の手順プロンプト）。source: `.shell-team/interventions/T-1128.md` entry 2。
2. **プランニングの one-sentence テストに「record grammar が既に表現できる状態か」の確認を追加する**: T-1128 は新規 adopter 能力ゼロ（headline not-met）で PATCH と判定されたが、premise-present-but-rule-absent という状態自体が既存の `verdict=` enum に無く、これは実装規模でなく gate 設計の欠落だった。次回プランニングで「一文の docs/skill 訂正」に見える項目のうち、既存の record/check 文法に新しい状態を書き込む必要があるものは、別建ての gate-design 項目として切り出す。disposition: **next-sprint-candidate**。source: `.shell-team/interventions/T-1128.md` entry 1, T-1128 board 裁定行。
3. **ブリーフィング作成時、機械検査済みレコード行（`- dispatch:` 等）は必ず board 上の直近 precedent を1件確認してから書く**: 2026-09-04 レトロ Try 5（write-boundary 自己点検）と同じ生成器が別の症状（記録の「形」の記憶依存）で再発した。disposition: **rule-change**（コーディネーティングセッション手順、Try 5 の姉妹ルール）。source: `.shell-team/interventions/T-1126.md` entry 3。
4. **`log-run.sh` を軽量運用（A2）のタスクにも実際に呼び出す**: 壁時計・手番数の申告が自己申告のままでは、次スプリントの見積り較正（`~/.claude/knowledge/estimate-calibration.md`）が一次資料を持てない。disposition: **next-sprint-candidate**。source: 本レトロ依頼文が明示するギャップ。
5. **T-1128 の gate-design 項目（premise-present-but-rule-absent の record grammar 拡張）を issue #403 のコメントから次回プランニングの入力として実際に拾う**: 起票済みで着手していない状態を放置しない（2026-08-30 レトロ Try 3 と同型の再発を避ける）。disposition: **next-sprint-candidate**。source: `.shell-team/interventions/T-1128.md` entry 2、issue #403 item 1（コメント済み）。

<!-- retro-section: traps -->
## Loop-trap check

> AI-attest（現行運用）: 以下は scrum-master 自身が本サイクルの一次資料（board `.shell-team/todo.md` の T-1126/T-1127 Done entry、branch `feature/403-version-rule-fallback` 上の T-1128 board entry、`.shell-team/reviews/T-1126.md`/`T-1127.md`、`.shell-team/interventions/T-1126.md`/`T-1127.md`、branch 上の `T-1128.md` interventions、git のコミット・ブランチ履歴）を実際に読んだ上での判断であり、検証できなかった項目だけを「unverified — human judgment needed」として明示する。S4 drift report（`.shell-team/reviews/<task-id>-drift.md`）は本サイクルのどのタスクにも存在しない（`ls .shell-team/reviews/T-1126-drift.md .shell-team/reviews/T-1127-drift.md .shell-team/reviews/T-1128-drift.md` 全件なし、`find .shell-team -iname "*drift*"` も無出力で確認済み）ので、引用すべき対象はない。

- **Comprehension debt**: 本サイクルの実装差分そのもの（`docs/workflow.md`/`.ja.md`、`docs/adopting.md`/`.ja.md`、`skills/run/SKILL.md`、`skills/goal/SKILL.md`、`templates/prompt-blocks/means-ends-reflection.md`、`templates/prompt-blocks/fanout-orchestration.md` の T-1126 の実差分、`docs/history.md`/`.ja.md` の T-1127 の実差分）は読んでいない——読んだのは board のハンドオフ記述、`.shell-team/reviews/*.md` のレビュー記録、`.shell-team/interventions/*.md` の介入記録であり、生の diff そのものではない。**唯一、`- (none)` を書ける対象がない**: `## Keep`/`## Problem` の各引用はこれら一次記録に基づくものであって、私自身の実装コード読解に基づくものではない。特に T-1128 は3ラウンド分のレビューテキスト（`bf9357a`/`dcd81ee`）自体も読んでおらず、コーディネーティングセッションの relay を経由した記述に依拠している——**unverified — human judgment needed**（T-1128 review 本文の直接一次確認）。
- **Review criteria re-stated**: 本レトロが「マージ可能（両ゲート green）」と判定した根拠は2点のみ——(1) T-1126/T-1127 の各 board エントリが最終ラウンドで QA `PASS`・Codex `APPROVE`/`READY_FOR_MERGE` を記録していること（"エージェントがそう言ったから"ではなく `- [x]` 状態と `READY_FOR_MERGE` ラベルを直接読んだ）、(2) `develop`(`c585f17`) → T-1126 close-out(`8cfe1ea`) → 現 HEAD/T-1127 close-out(`3ff6169`) の隣接リンクを `git merge-base --is-ancestor` で自分で実行し、申告どおり連続していることを実測したこと。T-1128 は両ゲートに到達しておらず（`REWORK`→`BLOCKED`）、そもそも「マージ可能」の判定対象ではない——これは達成できなかった1項目として `## Summary` に明記した。各タスクの実装コード自体のレビューはしていない。
- **Unverified self-report**: T-1126 のスプリント壁時計（1h41m、1 turn）・T-1127（46m、0 turns）・T-1128（≈1h21m まで、1 turn）は、いずれも `log-run.sh` telemetry ではなくコーディネーティングセッション自身の申告時刻に基づく——機械計測での裏取りはできていない。**unverified — human judgment needed**（`.shell-team/runs/` の telemetry ファイル2件は本サイクルの3タスクいずれとも紐づく記録が見当たらなかった）。T-1128 の「見積り 0.6 で2ラウンド」という値自体の出所（プランニング時のどの記録に立てられた数字か）も、本レトロの一次資料（interventions・board）には転記された結果しか無く、プランニング原記録は確認していない——**unverified — human judgment needed**。issue #403/#444/#445 の現在の open/closed 状態、PR #446/#447 の live な mergeable 状態は `gh` 未認証のため本レトロの一次資料からは確認できていない。

<!-- retro-section: lessons -->
## Lesson candidates

- `[common]` 見積りが失効した際の停止・繰り延べ提案は、測定された失敗（同一成分での連続 REQUEST_CHANGES 等の具体的事実）・代替アプローチ・「今でなく後にすべき理由」の3点を根拠として述べ、ルール名やポリシー名の引用でドロップを正当化してはならない——ルール引用は「承認を絞ることが目的化していないか」「見かけの KPI を守るための先延ばしになっていないか」という自己点検を経ずに使われがちで、実質根拠への訂正を後から要することになる。発火条件: 同一成分で連続して REQUEST_CHANGES/FAIL が出て見積りが失効し、その項目を今サイクルから外す提案をする時。firing agent: 見積り失効を判断してエスカレーションを起草する任意のロール(coordinating session / tech-lead)。supersede-vs-new: 「打ち切りの前例を第一推奨に」（`~/.claude/knowledge`）・「義務の発明と安さの捏造」に隣接するが軸が異なる新規——あちらは打ち切り判断そのものの正しさを扱い、本候補は打ち切り**提案の書き方**（正当化の根拠選び）を扱う。mechanization-vs-prose: prose（エスカレーション文のチェックリスト: 測定された失敗／代替アプローチ／why-later の3点、ルール名は理由に使わない）。who-is-hurt: オペレーターが実質根拠を確認するために毎回訂正の1往復を要する。public-fitness: 一般化可能（見積りが失効するあらゆる spec 駆動ループ）。source: `.shell-team/interventions/T-1128.md` entry 2。
- `[target-specific]` プランニングの one-sentence テスト（「新規 adopter 能力の有無」で headline/default-reach を判定する）は項目の**バージョンティア**を正しく導出できても、その項目が**実装として本当に一文で閉じるか**（既存の record grammar・check 文法が対象の状態を既に表現できるか）までは保証しない——T-1128 は「adopter 向け一文の docs 訂正」としてバックログに載ったが、実体は record grammar（`derived=`/`headline=`/`default-reach=`/`verdict=` の必須フィールドと閉じた enum）が「premise はあるがルールが無い」状態を表現できないという gate 設計上の欠落だった。発火条件: プランニングでバックログ項目が「一文の adopter-doc/skill 訂正」に見え、かつその項目が本スプリントの機構（record grammar・check 文法・board フォーマット）が定義する状態のいずれかに新しい値を書き込む必要がある時。firing agent: tech-lead / プランニングを承認するオペレーター。supersede-vs-new: 2026-09-04 レトロの「blast-radius 候補集合が実効的な prune になっていない」候補（`.shell-team/lessons.md:1121` 隣接）とは軸が異なる新規——あちらは検証コストの母集団規模を扱い、本候補は項目サイジングの妥当性検査軸(record grammar 適合性)を扱う。mechanization-vs-prose: まず prose（プランニング時のチェックリスト項目: 対象項目が書き込む先の record grammar/check 文法が既にその状態を表現できるか確認する)。who-is-hurt: 見積り(0.6 で2ラウンド)がバックログ確定後すぐに失効し、スプリント目標未達(3項目中2項目)の直接原因になった。public-fitness: このリポジトリの T-1110 record grammar・`verdict=` enum 機構固有(一般化するなら「wording タスクのサイジングは対象の記録文法の表現力を確認してから行う」という広い原則になりうるが、ここでは target-specific として提案する)。source: `.shell-team/interventions/T-1128.md` entry 1, board `feature/403-version-rule-fallback:.shell-team/todo.md:14-17`。
- `[target-specific]` ブリーフィングが機械検査済みのレコード行（`- dispatch:` 等）の形式を記憶から書くと、board 上に直近の precedent があっても見ずに誤形式を出し、`close-out.sh` の無駄打ちを招く——2026-09-04 レトロ Try 5（ブリーフィングは対象ロールの write-boundary と突き合わせる）と同じ「ブリーフィングは precedent や checker grammar でなく記憶から書かれた」生成器が、書き込み境界でなく記録の形式という別の症状で再発した。発火条件: コーディネーティングセッションが機械検査対象のレコード行(`- dispatch:`・`- version-derivation:`等)を含むブリーフィングや board 編集を、直近の precedent 行を確認せず記憶で書く時。firing agent: ブリーフィング/board 編集を書くコーディネーティングセッション。supersede-vs-new: 2026-09-04 レトロ lesson candidate「オーケストレーターが書くブリーフィングが write-boundary を破らせる」の姉妹候補(同じ「ブリーフィングは記憶から書かれる」根に立つが、対象が write-boundary でなく record grammar である点で軸が異なる新規)。mechanization-vs-prose: prose(機械検査済みレコード行を書く前に board 上の直近 precedent を1件確認する規律)——今回が2件目の類似再発であり機構化を検討する閾値に近い。who-is-hurt: close-out が無駄に2回失敗し、コーディネーティングセッションと engineer/qa の往復コストが増える。public-fitness: このリポジトリの close-out.sh grammar 固有。source: `.shell-team/interventions/T-1126.md` entry 3。

## Notes

- **サイクル窓の宣言について（stacked batch-GO 前提の適用、前回・前々回レトロと同型）**: `retro-inputs.sh --base develop` の `cycle-window` 行（uncapped、188件）が返した merge commit は、いずれも本サイクルの成果物（PR #446/T-1126、PR #447/T-1127）と一致しない——develop は動き続けている一方、本サイクルの2 PR は stacked batch-GO 待ちのため一本もマージされていない。T-1128 はそもそも PR が開かれていない(BLOCKED で park)。代わりに board の close-out 記述を出発点に `git merge-base --is-ancestor` を実行して確認した: `develop`(`c585f17`) is ancestor of T-1126 close-out commit(`8cfe1ea`) is ancestor of 現 HEAD/T-1127 close-out commit(`3ff6169`)——両リンクとも `OK`。このリポジトリは PR #446 専用の別ブランチを別途持たず、T-1126 と T-1127 は同一ローカルブランチ `feature/445-history-status` 上で連続してコミットされている(履歴上に別ブランチへの分岐点は見当たらない)ため、2026-09-04 レトロで見つかった「中間に独立の board entry を持たないブランチ」型の懸念はここでは生じない。ウィンドウの会員資格はこの2リンクの実測をもって確定とし、`retro-inputs.sh` の返す188件を本サイクルのものとして提示しない。
- T-1128 のレビュー本文(`.shell-team/reviews/T-1128-codex-primary.txt` 等)、board エントリ(`feature/403-version-rule-fallback:.shell-team/todo.md`)は git show で読んだが、このブランチ自体は未 push(operator ruling により証拠保持のみ)であり、コーディネーティングセッションの relay と一致するかの独立検証は git 上の記録同士の突き合わせに留まる。
- `pr-metadata` は `gh` 未認証のため unavailable。PR タイトル・ブランチ名からの機械的な task-id 抽出は行っていない——board のタスクエントリ自身が issue 番号・PR 番号を明示していたため、review artifact(`.shell-team/reviews/T-1126.md`/`T-1127.md`/`T-1128-codex-*.txt`)・interventions artifact(`.shell-team/interventions/T-1126.md`/`T-1127.md`、branch 上の `T-1128.md`)の存在を直接確認した(欠落なし)。
- `lessons` は本レトロでは unavailable(`--lessons` 未指定のため)。近傍隣接の確認は行えなかった——本サイクルの lesson candidate 4件は、2026-09-04 レトロ本文・過去レトロの Try 項目との隣接のみを根拠に記述した(`.shell-team/lessons.md` 本体はスキミングしていない)。
- モデル変更サイクル判定: channel (a)(本依頼文中の明示的なモデル変更宣言)は無し。channel (b)(`agents/*.md` の `model:` frontmatter 行の追加/削除/変更)を `git log --oneline -1 -- agents/` で確認したが、直近の agents/ 変更はレッスン昇格コミット(`7cb81b5`)のみで model 行の変更ではない。両チャネルとも変更を示さないため、本レトロでは scaffolding audit(棚卸し)を実行していない。
- release-sweep(2腕、本スプリントのバッチ GO 前に実行予定)の結果は依頼文の時点で pending であり、本レトロの一次資料には含まれていない——次回の判断材料として、その結果は別途 board か次回レトロで確認されたい。
- issue #403/#444/#445 の現在の open/closed 状態、PR #446/#447 の live な mergeable 状態は `gh` 未認証のため本レトロの一次資料からは確認できていない。
