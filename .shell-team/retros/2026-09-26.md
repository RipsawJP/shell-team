# Retro 2026-09-26

**Cycle scope**: スプリント `codex-host-adopter-run`（開始 2026-09-25 22:27 JST。ゴール:「Codex CLI host 上の実行が、plugin 側の欠陥によって停止したりレコードを失ったりしない」）。4 タスク・両ゲート green・PR は stacked のまま develop へ未マージ（スプリント一括 GO 待ち）: develop → T-1154(`#609`) → T-1155(`#613`) → T-1156(`#617`) → T-1157(`#618`)。
**Pull requests**: `#609`(T-1154, issue #594)、`#613`(T-1155, issue #606, operator-inserted stop-the-bleed)、`#617`(T-1156, issue #593)、`#618`(T-1157, issue #592)。いずれも board 上 `READY_FOR_MERGE`。

`bin/retro-inputs.sh --lessons .shell-team/lessons.md` の `## Retro inputs` をそのまま貼る。**この cycle-window 行はこのサイクルの窓ではない**: このサイクルは stacked batch-GO 運用中で、4 PR とも develop へ未マージのため、develop 上の merge-commit 履歴を歩く既定の cycle-window は必然的にこのサイクルの 0 コミットを返し、代わりに直前までの 253 件の無関係な過去マージを返す（`.shell-team/lessons.md:1095`-`1103` が既に文書化した形そのもの）。真の窓は board 自身の merge-chain 記述から declare し、`git merge-base --is-ancestor` で own-verify した: T-1154 の board entry (`.shell-team/todo.md:6971`) は「issue #594」を根拠に置き、T-1156 の entry (`:6925`) が「Stacked on T-1155 (PR #613) ... itself stacked on T-1154 (PR #609)」、T-1157 の entry (`:6892`) が「Stacked on T-1156 (PR #617) ... itself stacked on T-1155 (PR #613) and T-1154 (PR #609)」と明示する。隣接リンク 4 本全てを `git merge-base --is-ancestor` で own-verify（`develop`→`feature/594-...`：YES、`feature/594-...`→`feature/606-...`：YES、`feature/606-...`→`feature/593-...`：YES、`feature/593-...`→`feature/592-...`：YES）、かつ `feature/592-bin-help-flag` の tip が現在の `HEAD`(`ba0b7f6e`) と一致し、`develop` へはまだ到達していない（`git merge-base --is-ancestor feature/592-bin-help-flag develop` → NO）ことも確認した。窓は 4 タスク・4 PR・0 マージコミット（未マージ）で、以下の `cycle-window` 行はこの数字ではなく developロ自身の直近履歴を示しているに過ぎない。

## Retro inputs

- input: cycle-window — status: read — detail: 253 merge commits from develop (first-parent) — **この数字は上記の理由によりこのサイクルの窓ではない。真の窓は 4 タスク・4 PR・stacked chain（本文参照）**
- input: review-artifacts — status: read — detail: 1091 review artifacts in .shell-team/reviews
- input: provenance — status: read — detail: 154 provenance files in .shell-team/provenance
- input: specs — status: read — detail: 156 spec files in .shell-team/specs
- input: run-telemetry — status: read — detail: 2 run telemetry files in .shell-team/runs
- input: previous-retro — status: read — detail: 37 prior retro files in .shell-team/retros
- input: lessons — status: read — detail: 1484 non-blank lines in .shell-team/lessons.md
- input: pr-metadata — status: unavailable — detail: gh is not authenticated (optional enrichment only)
- input: interventions — status: read — detail: 149 intervention records in .shell-team/interventions

## Summary

4 タスクとも QA `PASS` と cross-provider `APPROVE` に到達したが、経路は大きく分かれた。T-1154・T-1155・T-1157 は単一 review round・単一 QA round・1〜1.75 時間で closed。T-1156 だけは 5 review round（round1 は evidence pass が Codex CLI 側の `workspace routing discovery failed` で 4 回失敗し `BLOCKED — review not established` のまま primary pass の 1 finding だけで REQUEST_CHANGES 相当に扱われた）・5 QA round・13h39m（うちオペレーター待ち約 9h13m、2 回の「計画に戻る」ゲート）を要し、根本原因は「issue #593 の本文ではなく一行要約から briefing された」ことだった。T-1155 は issue #606 内の 2 項目のうち stop-the-bleed 側だけをスコープに入れた operator-inserted タスクとして走った。review record 自身の必須フィールド欠落・sandbox 固有の絶対パス混入という記録衛生上の欠陥が 2 件（T-1155, T-1156/T-1157）見つかり、いずれも別コミットで修正された。release sweep はまだ走っておらず、このサイクルの逃し件数は pending。

<!-- retro-section: keep -->
## Keep

- **前回レトロの Try が今回のタスク dispatch 宣言に反映されている**: (a) release sweep の既定「並列 4・per-spec 出力を腕の外に」が 4 タスク全ての `dispatch: verify-mechanism` 行に明記されている — source: `.shell-team/todo.md:6901`(T-1157),`:6933`(T-1156),`:6960`(T-1155),`:6978`近傍(T-1154) の `dispatch: verify-mechanism` 行；前回レトロ `.shell-team/retros/2026-09-25-hotfix-v2.7.4.md` の Try。(b) board の operator-gate 記録が「escalated: <ts> — answered: <ts>」の 2 時刻形式で書かれている（2026-09-19 レトロ裁定どおり）— source: `.shell-team/todo.md:6938`, `:6942`。
- **spec の pre-commitment（never-dropped コンポーネント＋2 連続 defeat で計画へ戻る条項）が設計どおり 2 回連続で発火した**: T-1156 round2・round3 は同じ never-dropped paragraph に対しそれぞれ独立した実欠陥を見つけ、spec 自身の条項が 3 回目の patch round を許さずタスクを計画へ差し戻した — source: `.shell-team/reviews/T-1156.md` round2/round3 の「REQUEST_CHANGES — but not to READY_FOR_ENG」段落; `.shell-team/interventions/T-1156.md` entries 2, 4, 5。
- **cross-provider review が凍結 spec 自身に埋め込まれたスコープドリフトを、マージ前に 2 回独立して検出した**: round2 の Major（「決定は既に下された」という言い回しの過大主張）と round3 の Major（同一段落の別の欠陥）が、最終的に v3 の「issue 本文からの再計画」に直結した — source: `.shell-team/reviews/T-1156.md` rounds 2-3; `.shell-team/interventions/T-1156.md` entry 5。
- **単純なタスクは単一 round で closed**: T-1154(1h09m)・T-1155(1h21m)・T-1157(1h43m) はいずれも review 1 round APPROVE・QA 1 round PASS — source: `.shell-team/reviews/T-1154.md`,`T-1155.md`,`T-1157.md`; telemetry run_id `20260925T144146Z-t1154`/`20260925T160533Z-t1155`/`20260926T070924Z-t1157`（own-verify: 各 run の最初のイベント ts → `release` イベント ts の差分を算出、以下の計測表参照）。
- **T-1156 round1 で 4 回連続失敗した Codex CLI の `workspace routing discovery failed` は、同セッション内の他の review round（round2〜5、および T-1154・T-1155 の round1）では 1 度も再発しなかった** — source: `.shell-team/interventions/T-1156.md` entry 1。

<!-- retro-section: problem -->
## Problem

- **P1・T-1156 が issue #593 本文ではなく一行要約から briefing され、issue が求めていない機構（ネットワーク拒否 vs 認証失敗の診断ロジック）を凍結し続けた**: round1〜round3 の 3 round・約 13h39m（うちオペレーター待ち約 9h13m、2 回の「計画に戻る」ゲート）を要した末、round3 後にオペレーターが「means と ends が入れ替わっていないか、スコープを外すのは完了を強制する手段になっていないか」と問い直したことで、オーケストレータが初めて issue #593 を読み、frozen spec が issue の Expected（`claude -p` の dispatch がサンドボックス外で走ること・`Not logged in` をサンドボックス実行の signature と名指すこと・`docs/distribution.md` へのルール追記）とは別物の診断機構を作っていたと判明した — source: `.shell-team/todo.md:6942`-`:6943`（operator gate + v2→v3 の intent-ratified）; `.shell-team/interventions/T-1156.md` entries 4-5; telemetry `20260925T172915Z-t1156`（最初のイベント 17:29:40Z → release 07:09:23Z = 13h39m; own-verify 済み）。
- **P2・review record 自身の記録衛生が 2 件、書いた直後ではなく事後の別コミットで修正された**: T-1155 の record は `pass-role`/`briefing-fidelity` フィールド欠落と pass id の不整合（`evidence` vs `evidence-r1`）を `check-review-input.sh` に指摘され、別コミット `376af0b3` で修正; T-1156 round2 と T-1157 round1 の adversarial セクションには、このセッション自身の sandbox scratch root（`/tmp/claude-502`、default uid のセッション固有パス）が絶対パスのまま記録され、それぞれ round3 コミット `6b2a8e2a`（本題の review と同時）と別コミット `417931f4`（"records maintenance" 単独）で `$TMPDIR` に置換された — source: commits `376af0b3`, `6b2a8e2a`, `417931f4`; `.shell-team/reviews/T-1155.md`,`T-1156.md`,`T-1157.md`。
- **P3・CI 待ちでターンを終えて約 8 分アイドルした**: T-1155 着手前、前タスクの CI 完了を待つターンをオーケストレータが終了し、オペレーターに「進めているか」と問われて初めて再開した（CI success 15:57:24Z → T-1155 branch 16:05:33Z）— source: `.shell-team/interventions/T-1155.md` entry 1。
- **P4・T-1154 の初回試行がハーネスの auto-mode classifier に拒否されて丸ごと失われた**: run `20260925T132803Z-t1154` は Routing Map の後で停止し、board entry・spec・commit を一切残さなかった（同セッション内でその夜 3 回目の同種拒否）。復旧に 2 回の追加オペレーター介入（モデル依存の切り分け測定、通常権限モードでのスクリーンショット確認）を要し、最終的にオペレーターが「このチェックアウトのセッションは既定で `--plugin-dir ./` なしで起動する」と裁定した — source: `.shell-team/interventions/T-1154.md` entries 1-3。
- **P5・release sweep の逃し件数はまだ計測できない**: このサイクル向けの `release-sweep (2026-09-2…` 相当の board 行はまだ 1 件も存在しない（own-verify: `grep -n "release-sweep (2026-09-2[56]" .shell-team/todo.md` → 0 件）。sweep は現在進行中で、実数は release PR 側に出る。

<!-- retro-section: try -->
## Try

- **一行要約（sprint memory・triage note 等）から起票された pm-authored spec は、freeze attestation に「凍結した Goal/AC が、issue 自身の Expected/本文と（要約とではなく）突き合わせ済みである」旨を 1 行明記する** — トリガ: `source:` 行が issue を名指すが、routing 入力自体には issue 本文全文が渡っていない pm-authored spec 全て。根拠: P1。
- **`check-review-input.sh`（またはそれに相当する形式チェック）を review record 執筆の直後、その round が完了扱いになる前に走らせる** — トリガ: mode A2 下の Codex cross-provider review 全 round。根拠: P2。T-1153 で凍結された review-evidence-contract がこのゲートを書く時点で既に強制しているか、それとも事後監査止まりかは要再確認（`.shell-team/specs/T-1153-review-evidence-contract.md`）。
- **CI 待ちを「ターンを終える理由」ではなく「同じターン内でポーリングする背景条件」として扱う** — トリガ: stacked PR の train で 2 タスク間の CI 待ち全て。根拠: P3（既に interventions に是正記録あり — 新規則ではなく、次回計画で「チェック済み」項目として運ぶ）。
- **CLAUDE.local.md の「セッション起動規約」節（`--plugin-dir ./` での起動を既定とする現行文言）を、オペレーターが既に裁定した新しい既定（plugin-dir なし起動）に置き換える** — トリガ: このリポジトリ自身のセッション起動慣行（project memory に置き換え待ちとして既に記録済み）。根拠: P4（この項目自体は private memory 側の運用課題であり、shipped コーパスへの提案ではない — 下記 Lesson candidates の該当項目参照）。
- **sweep 完了後、`release-sweep (2026-09-26...)` 形式の board 行を必ず 1 行残す**（直前サイクルの retro Problem P4 で指摘された「記録の慣行が途切れた」の再発防止）— トリガ: このサイクルの release。根拠: P5 + `.shell-team/retros/2026-09-25-hotfix-v2.7.4.md` Problem P4 / Try。

<!-- retro-section: traps -->
## Loop-trap check

- **Comprehension debt**: T-1154 の resolver 実装（`bin/codex-capture.sh` のシンボリックリンク解決ブロック）、T-1155 の `.gitignore` テンプレート差分、T-1156 v3 の `host-dispatch.md` 最終段落、T-1157 の 14 スクリプトへの `--help` 挿入は、自分では diff を一行ずつ読んでいない — review record・QA hand-off・provenance の記述と、board の該当箇所を own-read して個別の主張（所要時間、review round 数、intervention 内容、stacked chain、2 件の temp-path 修正）を repo に対して直接検証した。直接全文を own-read したのは `.shell-team/reviews/T-1154.md` 全文、`.shell-team/interventions/T-115{4,5,6,7}.md` 全文、`.shell-team/todo.md` の該当 board セクション、および field 補完・temp-path 修正コミット（`376af0b3`, `6b2a8e2a`, `417931f4`）の `git show` 出力。
- **Review criteria re-stated**: このレトロでの「検証済み」は (a) タスクの briefing が主張する事実を、この repo に対して独立に再現できたこと（`git merge-base --is-ancestor` 4 本、telemetry の ts 差分計算、該当コミット・行の `grep`/`git log` 検索）、または (b) repo が既に持つ記録（review verdict・intervention entry・board dispatch 行）を直接引用したこと、のいずれかであり、briefing の記述をそのまま鵜呑みにした箇所はない。
- **Unverified self-report**: T-1154 の「auto-mode classifier の拒否が `--plugin-dir ./` セッションに相関する」という判断は `.shell-team/interventions/T-1154.md` entry 2 に「the only discriminating factor found」として記録されているが、その測定自体（どのモデル・サイズ・セッションで試したか）はこの repo の記録には存在せず、オペレーターとの会話・private memory にのみ残る — **unverified — human judgment needed**。このサイクルの 4 タスクいずれにも S4 drift report（`*-drift.md`）は存在しない（own-verify: `ls .shell-team/reviews | grep -i drift` → 0 件）ので、その半分の引用対象はない。

<!-- retro-section: lessons -->
## Lesson candidates

- `[common]` harness の auto-mode classifier が pm-spec の spec Write を判定なしで繰り返し拒否する挙動が、特定の起動形態（`--plugin-dir ./` でこの repo 内へ書き込むセッション）に相関して観測された。plugin をチェックアウトから dogfood する運用は shell-team に限らず一般的なので、この相関自体は shipped コーパスに載せる価値がある候補だが、測定条件（どのモデル・セッション形態を比較したか）がこの repo の記録に残っていない（operator の会話のみ）。次に同じ相関が記録つきで再現した時点で昇格を検討する — source: `.shell-team/interventions/T-1154.md` entry 2。
- `[common]` 一行要約からタスクを briefing すると、issue 本文が実際に要求する範囲（Expected セクション）と、要約から起こされた spec の範囲がズレたまま凍結・複数レビューラウンドを通過しうる。隣接する既存エントリあり: `.shell-team/lessons.md:1423`-`1424`（"verbatim carriage of a source" ルール、コスト修飾語の relay 違反を扱う）。今回のズレはコスト修飾語ではなく Expected セクション全体の取り違えで直接の重複ではないが、「要約は relay 違反になり得る」という同じ角度の隣接候補 — source: `.shell-team/todo.md:6943`; `.shell-team/interventions/T-1156.md` entries 4-5。
- `[target-specific]` review record の必須フィールド（`pass-role`/`briefing-fidelity`/pass id の一貫性）欠落と、セッション固有の絶対パス（sandbox scratch root 等）の混入は、`check-review-input.sh` が事後には検出できるが、書いた直後にその場で実行されない限り複数ラウンドをまたいで見逃され続ける。今回 2 件（T-1155、T-1156/T-1157）が完成後の別コミットで修正された — source: commits `376af0b3`, `6b2a8e2a`, `417931f4`。
- `[target-specific]` CI 待ちを理由にターンを終えて次タスクへの着手を止める挙動は、CLAUDE.local.md の既存の「サブエージェント監視」規律（stall はハーネス通知で検知・完了通知は裏取り）とは別の、独立した「CI 待ちそのものを理由にターンを終える」パターンとして今回 intervention に記録された。是正自体は既に `.shell-team/interventions/T-1155.md` entry 1 の effect 節に place 済みのため、再発時は記録漏れの有無だけを確認すればよい — source: `.shell-team/interventions/T-1155.md` entry 1。

## Notes

- `pr-metadata` は `gh` 未認証のため `unavailable`（想定内）。PR は全て未マージのため、review-artifact との対応は board の `source:`/`closed:` 行から直接取り、`gh` には依存していない。
- `cycle-window` 行（253 merge commits）はこのサイクルの窓ではない（本文冒頭で reconcile 済み）。真の窓（4 タスク・4 PR・stacked chain、`git merge-base --is-ancestor` 4 本 own-verify）は Summary 手前の段落と計測表を参照。
- モデル変更検知（step 8, scaffolding audit）: channel (a)（明示的な model 変更宣言）はこの retro の依頼に含まれていない。channel (b) — `git log --oneline c730dc4a..HEAD -- agents/` は 0 件（own-verify）、`agents/*.md` の `model:` frontmatter の追加・削除・変更なし。コミットの co-author 行が異なるセッション tier（"Claude Sonnet 5" と "Claude Opus 5.5 (1M context)"）を名乗っている事実は、main-session runtime tier の変化を示唆しうるが、この agent は自分から main-session tier を観測できず、それを channel (b) の根拠にしてはならない規律に従い、シグナルとして採用しなかった。したがってこのサイクルは model-change サイクルに該当せず、scaffolding audit は実施していない（実施しないサイクルと挙動的に同一）。
- interventions: T-1154 に 3 件、T-1155 に 2 件、T-1156 に 5 件、T-1157 に 1 件、計 11 件。全件このレトロで直接読んだ。
- release sweep の逃し件数: pending, see release PR（このサイクル向けの board `release-sweep (...)` 行はまだ 0 件、own-verify）。

## 計測数値表（telemetry 実測・own-verify）

| 項目 | T-1154 | T-1155 | T-1156 | T-1157 |
|---|---|---|---|---|
| run_id | `20260925T144146Z-t1154` | `20260925T160533Z-t1155` | `20260925T172915Z-t1156` | `20260926T070924Z-t1157` |
| 最初のイベント ts | 2026-09-25T14:43:33Z | 2026-09-25T16:08:23Z | 2026-09-25T17:29:40Z | 2026-09-26T07:28:31Z |
| release イベント ts | 2026-09-25T15:52:25Z | 2026-09-25T17:29:15Z | 2026-09-26T07:09:23Z | 2026-09-26T09:11:26Z |
| wall clock | 1h09m | 1h21m | 13h39m | 1h43m |
| うちオペレーター待ち（telemetry `human` イベント間隔） | 0（telemetry 上 `human` イベントなし） | 0（telemetry 上 `human` イベントなし。ただしタスク開始前後に intervention 記録あり — 下記参照） | 約9h13m（2 回:5h55m + 3h18m） | 0 |
| engineer round 数 | 1 | 1 | 5（初回＋rework×3＋v3 再着手×1） | 1 |
| QA round 数 | 1 PASS | 1 PASS | 5 PASS | 1 PASS |
| review round 数 | 1（APPROVE） | 1（APPROVE） | 5（round1 BLOCKED相当→REQUEST_CHANGES／round2 REQUEST_CHANGES／round3 REQUEST_CHANGES／round4 REQUEST_CHANGES／round5 APPROVE） | 1（APPROVE） |
| intervention 記録数 | 3 | 2 | 5 | 1 |

T-1154・T-1155 は telemetry の `human` イベントとしては 0 件だが、両タスクとも interventions ファイルに人間トリガーのエントリが記録されている（T-1154: run 開始前の 3 件、いずれもこの run_id の外側＝前回中断した run の復旧局面；T-1155: run 開始直前の CI 待ち是正 2 件）。telemetry の `human` イベントは board の「operator gate（escalated/answered）」形式の正式なゲート往復だけを捉え、それ以外の口頭での是正は intervention ファイルにしか残らない — 前回レトロの「recall は無料・salience は無料でない」系の観察と同型の記録ギャップ。
