# Retro 2026-07-31

**対象スプリント**: v1.1.0（2026-07-28 → 2026-07-31、本日リリース）
**対象タスク**: T-1005 / T-1006 / T-1007 / T-1008 / T-1009 / T-1010（6 ループ完走）＋リリース本体
**対象 PR**: #55, #56（T-1005）／ #59, #60（T-1006）／ #62, #63（T-1007）／ #64, #65（T-1008）／ #66, #69（T-1009）／ #71, #72（T-1010）／ #73, #74（リリース、`main` へ）
**生成元**: scrum-master agent（manual trigger）— このレトロが T-1010 のマーカー契約（`<!-- retro-section: … -->`）下で書かれる最初のレトロ

`bin/retro-inputs.sh --base develop --last-n 13 --lessons .shell-team/lessons.md` の出力を検証・貼付。cycle-window はこのスプリントの 13 merge commits（PR #55〜#73、develop の first-parent）に一致することを確認済み。リリース最終 PR #74（`develop`→`main`）は develop の history には現れない（`main` 側の merge のため）——別途 `git log main` / `git show b5df2a7` で直接確認した。

## Retro inputs

- input: cycle-window — status: read — detail: 13 merge commits from develop (first-parent, PR #55–#73); capped at --last-n 13, matching this sprint's declared task range T-1005–T-1010 exactly (PR #51/T-1004 close-out correctly excluded)
- input: review-artifacts — status: read — detail: 6 review artifacts read in full for this sprint (`.shell-team/reviews/T-1005.md` 410行, `T-1006.md` 217行, `T-1007.md` 219行, `T-1008.md` 83行, `T-1009.md` 158行, `T-1010.md` 163行); raw `-codex-*.jsonl/.txt` captures not opened (curated .md only)
- input: provenance — status: read — detail: 6 provenance files read in full (`.shell-team/provenance/T-1005.md`〜`T-1010.md`, 15–23行each)
- input: specs — status: read — detail: 6 spec files exist for this sprint's tasks (`.shell-team/specs/T-1005-*.md`〜`T-1010-*.md`); consulted via board excerpts and review citations, not read end-to-end line-by-line
- input: run-telemetry — status: read — detail: `.shell-team/runs/shell-team.jsonl`（118行）と `close-out.jsonl`（12行）を確認。T-1010 の run_id `20260731T115849-t1010` のスパン列（qa-verifier PASS→codex-reviewer REQUEST_CHANGES→engineer→qa-verifier PASS→codex-reviewer APPROVE）を直接引用
- input: previous-retro — status: read — detail: 1 prior retro file (`.shell-team/retros/2026-07-28.md`, T-111/T-112/T-113 PII controls cycle — pre-marker, migrated by T-1010's pure-insertion recipe, referenced here for continuity but out of this sprint's scope)
- input: lessons — status: read — detail: `.shell-team/lessons.md` 700 non-blank lines read/grepped; two directly-adjacent entries found and cited below (2026-07-25 stale file:line:content pin; 2026-07-27/pm-spec check: pre-verification lineage)
- input: pr-metadata — status: unavailable — detail: `gh` not authenticated in this sandbox (per `bin/retro-inputs.sh`'s own report); PR numbers/branches/merge commits below come from `git log --merges` directly, not from `gh pr list`
- input: interventions — status: read — detail: 8 intervention records in `.shell-team/interventions/`; T-1002–T-1009 all zero-entry sentinels ("no interventions occurred"); **T-1010 has no intervention file at all** (confirmed via `find` — zero hits), a genuine gap, not a sentinel — recorded in `## Notes`

## サマリ

T-1005 から T-1010 まで 6 タスクが完走し、全て両ゲート（QA `READY_FOR_REVIEW` と cross-provider Codex `READY_FOR_MERGE`）green でマージされ、本日 v1.1.0 としてタグ付け・GitHub Release 公開済み（PR #73/#74、タグ `v1.1.0` 確認済み）。人間の介入は 8 タスク中 8 つが zero-entry sentinel（design された gate のみ、想定外の割り込みなし）。re-freeze（intent-hash v1→v2）は T-1005 の 1 回のみ、T-1006 の QA FAIL は同一サイクル内で収束、T-1007〜T-1010 は zero re-freeze。Codex は 5/6 タスクで少なくとも 1 回 Major を出し round 2 で APPROVE、T-1008 のみ 1 ラウンドで両ゲート green。T-1006 の QA FAIL は 2026-07-25 付で既に corpus に書かれていた「`file:line:content` pin の再走査」ルールと同型の再発。

<!-- retro-section: keep -->
## Keep（続けたい良い動き）

- **`check:` 行の凍結前ライブ実行が、通常の同一クラス-2 走査を上回るスケールで機能した**: T-1010 は pm-spec がシェルを持たないため、オーケストレータが凍結前に spec の 28 本の `check:` 行を全実行し、3 件の欠陥（ホストの grep が ugrep 7.5.0 で CJK バイトクラス検出が不発火／AC19 の末尾 `;` が前段の失敗を握りつぶす／AC22 が suite 不在（exit 127）を mutation 捕捉と誤カウント）を凍結前に修正した（`.shell-team/todo.md` T-1010 の "intent-freeze" 記述）。これは `.shell-team/lessons.md:760-761`（2026-07-27 付、「pm-spec has no shell … whichever side has execution capability must run every check: line live … before finalizing the intent hash」）の**既存ルールを、通常より大きいスケール（28 AC 全数）で適用**した実例であり、同種の欠陥が凍結後に発見されていたら re-freeze の churn になっていたはずのものを未然に防いだ（Source: `.shell-team/todo.md:27` T-1010 intent-freeze エントリ; `.shell-team/lessons.md:760-761`）。
- **スコープロックの allow-list に機構的結合ファイルを最初から含める、という前サイクルの教訓が T-1007 で再現され、re-freeze ゼロで着地した**: T-1007 の AC16 allow-list はテストの fixture companion（`tests/gen-playbook-blocks/fixtures/root/tasks/lessons.md` 等）まで最初から含んでおり、Codex round 1 の Major（`playbook-promote.sh` が空の `--bound-in` を静かに落とす）の修正が allow-list 内で完結し re-freeze 不要だった（`.shell-team/reviews/T-1007.md:46,151`）。同じ形の教訓は T-1005・T-1006 では機能せず（後述 Problem）、T-1007 では効いた、という対照が同一スプリント内に残っている。
- **QA が FAIL を出すことを恐れず、正しく `READY_FOR_QA` に留めた**: T-1006 の QA は `tests/errexit-safe/run.sh` の stale pin を検出し、「orchestrator instruction (`if FAIL: do not flip`)」に従って board flag を上げずに FAIL 判定を記録した（`.shell-team/reviews/T-1006.md:4,104-106`）。同サイクル内で rework→re-verify→PASS まで到達し、無理に green を偽装しなかった。
- **Codex の adversarial パスが実際に severity を動かし続けている**: T-1005 round 1 で primary の "board close-out needs no stop" 指摘（primary は major 相当）を adversarial パスも major と判定しつつ、synthesis audit ledger で spec の Input space の宣言（境界事例のこれ以上の深掘りを declined）を根拠に minor へ収める、という二段階の判断プロセスが記録されている（`.shell-team/reviews/T-1005.md` "Synthesis audit ledger"）。T-1010 でも Codex 自身が round 1 の Major を round 2 で再実行・再現してから APPROVE している（`.shell-team/reviews/T-1010.md`、round 2 delta の記述）。
- **QA が pre-fix/post-fix の両側から Codex Major を再現した**: T-1010 の QA round 2 delta は、修正前のチェッカー（`git show 2da1a1e:bin/check-retro.sh`）で実際に bug が再現すること、修正後の同一入力で正しく exit 1 になることの両方を確認しており、「テストが通った」だけでなく「以前は通り抜けていたものを今は捕まえる」ことを実証している（`.shell-team/todo.md:32` qa-verifier round-2 delta エントリ）。
- **人間の介入は全て設計された gate だった**: 8 タスクの intervention record は全て zero-entry sentinel（`.shell-team/interventions/T-1002.md`〜`T-1009.md` 全て "no interventions occurred"）。想定外の割り込みはこのスプリントで一度も発生しておらず、人間の関与は merge GO とレシオの ratify（T-1005 の v1→v2 re-freeze 承認）に限られていた。

<!-- retro-section: problem -->
## Problem（直面した課題 / 痛み）

- **`file:line:content` pin の stale 化ルールが 2026-07-25 に corpus 化されていたにも関わらず、5〜6 日後の T-1006 で同型の欠陥が再発した**: `.shell-team/lessons.md:723-732`（"Editing a file-line-pinned `bin/` script needs an explicit check that cross-suite registries still match"）は、まさに `tests/errexit-safe/run.sh` の `NOT_APPLY` レジストリが `bin/` 編集後の行番号ズレで stale になるクラスを名指ししていたが、T-1006 のエンジニアは「Five required suites」（`tests/team-paths` 等）だけを実行し、CI 必須リストに含まれる `tests/errexit-safe/run.sh` を一度も走らせなかった（`.shell-team/reviews/T-1006.md:41,65`）。QA round 1 が FAIL として検出し、同サイクル内で収束したが、書かれたルールが実行の手前で参照されなかった点は前回レトロ（2026-07-28）の「dry-run 止まりの検算」と同型の"lesson が書かれても実行時に参照されない"クラスである。
- **同じ「機構的結合companionをallow-listに最初から含める」教訓が、T-1005/T-1006ではre-freezeを防がず、T-1007では防いだ**: T-1005 は `.shell-team/loops/shell-team.contract.yaml` を instantiation list から漏らして Codex Major→v1→v2 re-freeze が必要になった（`.shell-team/reviews/T-1005.md` "Major" 節）。T-1006 は `bin/gen-playbook-blocks.sh` 編集後の line-number pin ずれ（上記）で QA FAIL に至った。T-1007 では同種の懸念（allow-list の網羅性）が最初から設計に織り込まれ、Codex Major が出ても re-freeze なしで収束した。3 タスク中 1 タスクだけが対策を最初から適用できた、という非対称性がある。
- **T-1010 の intervention record が存在しない**: `.shell-team/interventions/` を直接確認したところ T-1005〜T-1009 は全てファイルが存在し（zero-entry sentinel）、T-1010 だけファイル自体が無い（`find .shell-team/interventions -iname "*T-1010*"` はゼロヒット）。T-1010 の閉じ処理（close-out, run_id `co-2026-07-31-T-1010`）は実行されているが、他タスクと同じ sentinel 生成手順がこのタスクでは踏まれなかった可能性がある——原因はこの retro からは確認できず、未検証のまま残す。
- **T-1010 の Codex round 1 Major は「fail-closed ゲート自身の fail-closed 性の穴」というメタなクラスだった**: `bin/check-retro.sh` の rule 4（`## Retro inputs` region-close 判定）が、非正規の marker 風の行（未知 id や近似スペル）でも region を閉じてしまい、その後の "unrecognised line" チェックに到達しない、という fail-closed チェッカー自身の fail-open な穴だった(`.shell-team/todo.md:31` rework round 2 エントリ)。fail-closed を謳うチェッカーを書くタスク自身が fail-open な一箇所を生んでいた、という自己言及的な finding。
- **リリース手順が無い状態でリリースが実行され、CI の一過性失敗が発生した**: issue #29 により「リリースは手順書からではなく実行から書く」方針が意図的に採られている一方、board-only diff（`c530745` "ci: retrigger" コミット）で `tests/retro-inputs/invariants.sh` が CI 上でのみ失敗しローカルでは再現しなかった。オーケストレータ供給情報によれば根本原因は issue #70（大きいログ fixture の auto-gc レース）だが、この主張自体は `gh` 未認証のためこの retro からは直接検証できておらず、オーケストレータ供給の未検証情報として扱う。

<!-- retro-section: try -->
## Try（次サイクルで試すこと）

- `tests/errexit-safe/run.sh` のような「CI必須だが各タスクの『必要 suite』リストには載らない」suite を、`bin/` を触るタスクの完了チェックリストに機械的に強制する仕組みを検討する——`.shell-team/lessons.md:723-732` のルールは書かれているが、T-1006 で実行時に参照されなかった。プロンプト規律だけに頼らず、「`bin/` 差分がある場合は CI ワークフローに列挙された全 suite を実行したか」を `check-acs.sh` 相当の機構でチェックできないか検討する価値がある（前回レトロの「dry-run止まりの検算」と同根の再発防止パターン）。
- 「機構的結合 companion ファイルを scope-lock allow-list に最初から含める」ことを、T-1007 の一事例に留めず、pm-spec 向けの spec-drafting チェックリスト項目として明文化することを検討する（`.shell-team/lessons.md:643-651` の既存教訓「A scope-lock allow-list should include a task's required deliverables from the start」に隣接するが、そちらは「成果物」のみを対象にしており、「編集対象 bin/ スクリプトに機構的に結合された companion」までは明示的にカバーしていない——スコープを広げる形の拡張候補）。
- close-out 手順が `.shell-team/interventions/<task-id>.md` の sentinel 生成を毎回確実に行うかどうかを、次サイクルで T-1010 のケースを起点に確認する（オーケストレータへの確認事項として）。原因が close-out スクリプト側か手順の踏み忘れかは、この retro からは切り分けられない。
- リリース手順まわりの一過性 CI 失敗（board-only diff での `tests/retro-inputs/invariants.sh` 失敗）の再発有無を、次回リリース時に注視する。issue #70 の内容自体をこの retro は検証できていないため、次にこの suite が board-only diff で落ちた際に issue #70 の診断が実際に再現するかを確認する。

<!-- retro-section: traps -->
## 罠の点検（Comprehension Debt / Cognitive Surrender）

- **理解の負債**: このスプリントで実際に通読したのは `.shell-team/reviews/T-1005.md`〜`T-1010.md`（curated markdown、計 1250 行）、`.shell-team/provenance/T-1005.md`〜`T-1010.md`（全 6 ファイル）、`.shell-team/todo.md` の該当 6 エントリ全文（board 上の engineer/qa-verifier/codex-reviewer の記録すべて）、`.shell-team/lessons.md` 全 700 行（grep + 該当節の直接読解）、`.shell-team/runs/shell-team.jsonl` と `close-out.jsonl`、CHANGELOG.md の v1.1.0 節、`bin/check-retro.sh` 全文（このレトロ自身が満たすべき contract）。**読んでいないもの**: 6 タスクの生 Codex キャプチャ（`-codex-{primary,adversarial}.jsonl/.txt`、計 20 ファイル超）は開いていない——curated な `.md` の記述を読んだ結果であり、Codex の生出力を自分で再現検証したわけではない。6 本の spec ファイル（`.shell-team/specs/T-1005〜1010-*.md`）も全文通読はしておらず、board / review からの引用に依拠している。実装コード自体の diff（`bin/check-retro.sh` を除く）も直接は読んでいない。これは他者の記録を読んだ結果の理解であり、明示しておく。
- **レビュー基準の再言語化**: このスプリントで「マージしてよい」と判定された基準は一貫して (a) spec の全 `check:` 行が live 実行で green（`check-acs.sh`）、(b) `check-intent.sh` が board の記録済み intent-hash と一致（drift なし。T-1005 のみ v1→v2 の人間承認済み re-freeze を経て一致）、(c) QA の PASS が mutation-証明・独立再現に裏打ちされている（例: T-1010 の pre-fix/post-fix 両側再現）、(d) cross-provider の Codex reviewer が Blocker/Major ゼロで APPROVE に到達（Minor は fast-follow 起票または waived）——の 4 条件がすべて揃うことであり、単に「AI がそう言ったから」ではない。この記録自体が繰り返しそれを示している（T-1006 の QA が自身の判断で board flag を上げずに FAIL を記録した事実、T-1010 の QA が old/new 両チェッカーを実際に走らせて差を確認した事実）。
- **未検証の自己申告**:
  - **検証跡なし面（機械的に導出）**: このスプリントの 6 タスクすべてに `.shell-team/reviews/<task-id>.md` と `.shell-team/provenance/<task-id>.md` が存在し、レビュー成果物の欠落は無かった。唯一の欠落は上記の **T-1010 の intervention record**（`.shell-team/interventions/T-1010.md` が存在しない）であり、機械的に確認済み（`find` ゼロヒット）。原因（close-out 手順の不備か、単発の踏み忘れか）はこの retro からは判断できず、未検証・要人間判断として残す。
  - **issue #70 の内容**: オーケストレータ供給情報が挙げる「リリース時の一過性 CI 失敗は issue #70（大きいログ fixture の auto-gc レース）で root-cause 済み」という主張は、`gh` がこのサンドボックスから未認証のため、issue #70 の実在・内容を私自身は確認できていない。`c530745` のコミットメッセージ（"ci: retrigger — retro-inputs invariants step failed on a board-only diff, not reproducible locally"）はリポジトリ内で直接確認できたが、その根本原因が issue #70 の内容と一致するかは未検証・要人間判断として残す。
  - **S4 drift report**: このスプリントの 6 タスクいずれにも `.shell-team/reviews/<task-id>-drift.md` は存在しない（`find .shell-team -iname "*drift*"` で確認済み——唯一のヒットは spec ファイル名 `T-1009-doc-drift-and-false-ci-claim.md` で、drift report ではない）。引用できる cross-provider drift verdict はこのスプリントには無い。
  - **モデル変更の scaffolding audit（Step 7）は本サイクルでは発火していない**: channel (a)（明示的なモデル変更の宣言）はこのタスクの入力に含まれていない。channel (b)（`agents/*.md` の `model:` frontmatter 変更の git 観測）も、スプリント開始点（T-1004 close-out commit `1602889`）から現在までの `git diff` で確認したが、`model:` 行の追加・削除・変更はゼロ件だった。したがって未決定（undeterminable）ではなく明確に「変更なし」——監査自体は発火条件を満たさずスキップした（`no scaffolding relaxation candidates` という文言は監査が実際に走って0件だった場合専用のため、ここでは使っていない）。

<!-- retro-section: lessons -->
## Lesson 候補（ユーザー判断で `.shell-team/lessons.md` にマージ）

- `[common]` `file:line:content` でピン留めされたクロスサイト・レジストリ（例: errexit-safe の `NOT_APPLY` 表）を持つ `bin/` スクリプトを編集するタスクでは、「その suite 自身の green」だけでなく「CI 必須リストに載っている全 suite を実際に一度は実行したか」を実装完了チェックリストの明示項目にする——このスプリントの T-1006 は、既に 2026-07-25 付で corpus 化されていた同型ルール（`.shell-team/lessons.md:723-732`）にも関わらず、「Five required suites」という狭い自己申告リストで完了と判断し、CI必須の `tests/errexit-safe/run.sh` を一度も実行せず QA round 1 FAIL を招いた。書かれたルールが実行前のチェックリスト生成の場面で参照されなかった、という再発パターン自体を次サイクルで機構化候補にする価値がある。(Source: `.shell-team/lessons.md:723-732`「2026-07-25 — Editing a file-line-pinned bin/ script…」に隣接; `.shell-team/reviews/T-1006.md:41,65,104-106`)
- `[target-specific]` scope-lock allow-list に「編集対象の `bin/` スクリプトへ機構的に結合された companion ファイル（テスト fixture、CI ワークフローの手動インスタンス化リスト等）」を spec-drafting の時点で網羅的に含める、という手順を pm-spec 向けに明文化する候補。既存の `.shell-team/lessons.md:643-651`（"A scope-lock allow-list should include a task's required deliverables from the start"）は成果物（provenance/review record）をカバーするが、機構的結合ファイルの網羅性までは明示していない。このスプリントでは T-1007 が allow-list にテスト fixture companion を最初から含めて re-freeze ゼロで着地した一方、T-1005 は instantiation list から `.shell-team/loops/` を漏らして re-freeze が必要になった——同一スプリント内で対照が出ている。(Source: `.shell-team/reviews/T-1007.md:46,151`; `.shell-team/reviews/T-1005.md` "Major" 節; `.shell-team/lessons.md:643-651`)
- `[target-specific]` close-out 手順（`.shell-team/interventions/<task-id>.md` の zero-entry sentinel 生成）が全タスクで一貫して踏まれるよう、close-out 完了時のチェックリストに「intervention record が存在するか」の確認を追加することを検討する——このスプリントの T-1005〜T-1009 は全て sentinel が存在する一方、T-1010 だけファイル自体が存在しない。原因は未検証（close-out 側の欠陥か単発の踏み忘れかは、このレトロからは切り分けられない）。(Source: `.shell-team/interventions/` ディレクトリの直接確認 — `find` で T-1010 のみゼロヒット; 他 5 タスクは全て "no interventions occurred" の sentinel を確認済み)
- `[common]` fail-closed を謳う検証ゲート自身の region-close / 境界判定ロジックに、非正規の入力形状（未知 id・近似スペルの marker 風の行等）を投げるテストケースを、そのゲートを作るタスク自身の acceptance criteria に含める——T-1010 の Codex round 1 Major は、`check-retro.sh` の rule 4 が「正規の marker 判定」と「region を閉じる判定」で異なる正規表現を使っていたために生じた不整合であり、fail-closed を謳うチェッカー自身が持っていた fail-open な一箇所だった。修正は「両方の判定が同じ正規化された id セットを共有する」形に揃えることで解消した。(Source: `.shell-team/todo.md:31,33` T-1010 rework round 2 / codex-reviewer round 2 エントリ)
- `[common]` pm-spec がシェルを持たないタスクで、凍結前に「全 `check:` 行をライブ実行して壊れている行・vacuous に pass する行を修正してから hash を記録する」という既存ルール（`.shell-team/lessons.md:760-761`）を、AC 数が多い spec（このスプリントでは 28 件）でも省略せずに適用し続けることが、re-freeze を未然に防ぐ効果を持つと再確認された——既存ルールの「効いた」実例として、レトロの通常の"新規発見"ではなく"適用実績の記録"として corpus 側の `How to apply` に一言（適用規模のスケール上限を設けない旨)を足す価値があるかもしれない。(Source: `.shell-team/todo.md:27` T-1010 intent-freeze エントリ; `.shell-team/lessons.md:760-761`)

## Notes

- **`gh` はこのサンドボックスから未認証**（`bin/retro-inputs.sh` 自身の出力どおり）。PR番号・マージコミット・ブランチ名は `git log --merges` から直接導出し、PR 本文は一切読んでいない（読まない設計どおり）。
- **T-1010 の intervention record が存在しない**ことを機械的に確認したが（`find .shell-team/interventions -iname "*T-1010*"` ゼロヒット）、原因は未検証のまま Try / Lesson 候補に反映した。
- **issue #70（リリース時の一過性 CI 失敗の root cause）はオーケストレータ供給情報であり、この retro は `gh` 到達不能のため独立に検証できていない**。コミット `c530745`「ci: retrigger」自体はリポジトリ内で直接確認済み。
- Step 7（モデル変更時の scaffolding audit）はこのサイクルでは発火していない: channel (a)（明示的宣言）はこのタスクの入力に無く、channel (b)（`agents/*.md` の `model:` frontmatter の git diff、スプリント開始点 `1602889` から現在まで）もゼロ件で「変更なし」と判定できた（undeterminable ではなく明確な no-change）。したがって「監査が走って0件だった」という `no scaffolding relaxation candidates` の文言はここでは使っていない——監査自体をスキップした。
- 前回レトロ（`.shell-team/retros/2026-07-28.md`）は T-111/T-112/T-113（issue #6 PII controls）を対象としており、本スプリント（T-1005〜T-1010, v1.1.0）とは完全に別サイクル。継続性の参照としてのみ引用した。
- 6 タスクすべてに `.shell-team/reviews/<task-id>.md` と `.shell-team/provenance/<task-id>.md` が揃っており、レビュー成果物・provenance の欠落は無かった。spec ファイルも全 6 タスク分存在する。
