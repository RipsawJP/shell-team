# Retro 2026-09-17

**サイクル範囲**: スプリント `codex-upgrade-b-run6` の2タスク — **T-1146**（issue #546＋#545、PR #556、branch `feature/546-codex-agents-migration-followups`、tip `cf135df3`）→ **T-1147**（issue #515、PR #559、branch `feature/515-class-m-grant-codex-host`、tip `31bcc4ae`、現在の HEAD）。develop へのマージはこの cycle の task から **0 件**——2 PR とも stacked・open・base は develop・スプリント一括 GO でのバッチマージ待ち。この cycle は scrum-master.md の「stacked batch-GO cycle」に該当するため、窓は `retro-inputs.sh` の merge-commit walk からではなく **board の `stacked-on` 行＋`git merge-base --is-ancestor` の own-run** から宣言する。
**Pull requests**: `#556`（T-1146）、`#559`（T-1147）。
**Generated by**: scrum-master agent v0 (manual trigger)

**サイクル窓の宣言方法（stacked batch-GO・own-run で確認済み）**: T-1147 の board エントリの `stacked-on` 記述どおり、T-1147 の branch（`feature/515-class-m-grant-codex-host`）は T-1146 の branch tip（`cf135df3`、PR #556）から分岐している。own-run で確認した内容: (1) `git rev-parse feature/546-codex-agents-migration-followups` と `git rev-parse origin/feature/546-codex-agents-migration-followups` はいずれも `cf135df37fc2d294e06bbb69addc3006c0a6509c` に一致（local/remote 同一）。(2) `git merge-base --is-ancestor cf135df3 31bcc4ae` は exit 0 ——predecessor（T-1146）の tip が successor（T-1147）の tip の祖先であることを確認（この cycle はタスク2件のみで隣接リンクは1本、中間コミット・中間 PR は無い——T-1146 の close-out コミット自体が T-1147 の分岐点であり、board の `stacked-on` 記述と一致）。(3) `git merge-base --is-ancestor cf135df3 origin/develop` および `git merge-base --is-ancestor 31bcc4ae origin/develop` はいずれも exit 1 ——両 PR とも develop に未到達であることも確認済み（バッチ GO 待ちという前提と整合）。

`retro-inputs.sh --last-n 2` が返す2件は own-run で全て foreign と確認済み——件名の PR 番号（#553, #550）はいずれもこのサイクルの2 PR（#556/#559）と一致せず、前サイクル `loop-signals-honest` の lesson-promotion マージ（#553）と v2.7.0 リリース（#550）に属する。foreign な merge を「このサイクルの窓」として扱わないことをここで明示する——正典窓は上記の board `stacked-on` 記述＋own-run の `git merge-base --is-ancestor` のみである。

## Retro inputs

- input: cycle-window — status: read — detail: 2 merge commits from develop (first-parent); capped at --last-n 2 (a declared cap, not a truncation)
  - e66b21bf7d63 Merge pull request #553 from RipsawJP/chore/lessons-2026-09-16
  - 88b4b0476ca4 Merge pull request #550 from RipsawJP/chore/release-v2.7.0
- input: review-artifacts — status: read — detail: 1001 review artifacts in .shell-team/reviews
- input: provenance — status: read — detail: 144 provenance files in .shell-team/provenance
- input: specs — status: read — detail: 146 spec files in .shell-team/specs
- input: run-telemetry — status: read — detail: 2 run telemetry files in .shell-team/runs
- input: previous-retro — status: read — detail: 33 prior retro files in .shell-team/retros
- input: lessons — status: read — detail: 1438 non-blank lines in .shell-team/lessons.md
- input: pr-metadata — status: unavailable — detail: gh is not authenticated (optional enrichment only)
- input: interventions — status: read — detail: 139 intervention records in .shell-team/interventions

## Summary

スプリント `codex-upgrade-b-run6` のゴール（issue #546＋#545 と #515、v2.7.1/PATCH）は、Codex ホスト adopter が v2.7.x への移行フォローアップを取り込み、class-M 標準グラント（standing grant）をホスト中立な場所に記録できるようにすること。T-1146（レガシー basename マイグレーションの非通常ファイル対応・チェッカーの残留ヒントのロールスコープ整合・`check-binding.sh` の refusal matrix コメント完備）は QA 3 round・Codex レビュー 2 round（REQUEST_CHANGES→APPROVE）で完結し、same-class-2（`remedy-claim-vs-generator-gate`）が review round 1 の Major と QA round 2 の FAIL の2件で発火、never-dropped コンポーネント自体への着弾（means-ends reflection の case 2）としてオペレーターへ一度エスカレーションされ、ホスト入力1回（`round 3 GO`）で round 3 が2×2の全4セルを閉じて完結した。T-1147（class-M グラントの記録をホスト中立な base dir 配下に置き、両オーバーサイトガイドがホストごとの記録場所を明示する）は QA 3 round・Codex レビュー 2 round（REQUEST_CHANGES→APPROVE）で完結し、同じく same-class-2（`unmeasured-host-claim`）が QA round 1 の FAIL と review round 1 の Blocker の2件で発火、round 2 の class-closure ブリーフィングが `docs/` とスキル行に inventory の範囲を絞ったため、reader スクリプト自身のヘッダーコメント（`--help` で逐語露出）が見落とされた——エスカレーション後、ホストは同じ trigger を T-1146 の既答と誤読して2回訂正を入れ（合計ホスト入力3回）、round 3 でヘッダー書き換え＋inventory 範囲をタスク diff 全体と全 `--help` ストリームへ拡張して完結した。両タスクとも両ゲート green・stacked PR の base は develop（前サイクルの target-specific lesson を継続適用）・v2.7.1 リリース sweep（2,278 AC pair、41 flip、うち predicted 2・unexplained 0・見落とし 0）を1回実施済み。fast-follow は #554・#555（T-1146 由来）、#557・#558（T-1147 由来）として起票済み。

<!-- retro-section: keep -->
## Keep

- same-class-2 の mandatory-escalation 機構が両タスクで正しく発火し、ループ自身の権限では処分を実行せず、means-ends reflection の4問（never-dropped set／対象コンポーネント／drop order／累積コスト）を経てオペレーターへ委譲された——T-1146 は case 2（never-dropped set 着弾）、T-1147 は case 1（対象コンポーネントに事前処分なし）と、異なる根拠を own-read で区別して記録している。source: board T-1146/T-1147 の `same-class-2` 行、`.shell-team/reviews/T-1146.md`（round2 Major 2 の再発）、`.shell-team/reviews/T-1147.md`（round1 Blocker）。
- v2.7.1 の release sweep は two-arm full-population `check-acs` diff（144 spec、2,278 AC pair）を4-wide で実施し、41 flip 全件を merge-point-scoped（39件）または predicted（2件、いずれも merged spec 自身の構造ロックが今回の正当な編集を検知しただけと own-read で確認）に説明しきり、unexplained 0・sweep miss 0 を記録した。source: board T-1147 の `release-sweep (v2.7.1, 2026-09-17)` 行。
- T-1146 の凍結時、pm-spec の own-run が AC14 の固定文字列がハードラップをまたいで絶対にマッチしない不備を凍結前に発見し、ホワイトスペース平坦化＋2つのオンライン制御に再設計してから凍結した——post-implementation 状態を読み直すという凍結手順が実際に機能した実例。source: board T-1146 の `freeze-attestation (v1)` 行。
- reviewer（code-reviewer）が T-1146 round 2 で自分自身の round-1 review record が `check-review-input.sh` の閉じた語彙（`pass-role`/`briefing-fidelity`/`raw-capture`）に違反していることを own-read で発見し、判定・所見を一切変えずにその場で修正し、`bin/close-out.sh` がマージ時に refuse していたはずの記録衛生上の欠陥を未然に防いだ。source: `.shell-team/reviews/T-1146.md` round 2 の Minor/nits 節＋Per-pass provenance の correction note。
- T-1147 の review round 2 は、round 1 の Blocker（reader のヘッダー自身の host-loading 主張）を own-run（`--help` の repo-wide sweep、byte-scope diff、fixture vacuity 再現）で独立に閉鎖確認し、かつ round 1 の2件の Major（空文字 `--base`/`--config` のフォールスルー、root 実行時のカバレッジ欠落を隠す PASS）を「この round の diff が触れていない」ことまで own-run で確認した上で継続 fast-follow として維持した——再発防止の確認が hand-off 任せでなく own-run 主体だった。source: `.shell-team/reviews/T-1147.md` round 2「Verification performed this round」節。
- 2 PR とも base は develop に固定されている（前サイクルの target-specific lesson「stacked PR の base は develop に向ける」が継続適用された）——CI トリガの空白期間を今回も生まなかった。source: board T-1146/T-1147 の `closed:` 行。

<!-- retro-section: problem -->
## Problem

- **両タスクとも、round 2 の class-closure リワーク・ブリーフィングの inventory 範囲が、コーディネーティングセッション自身によって過小に指定されていた**——T-1146 は round 2 のブリーフィングが「報告された2バリアント」の閉鎖だけを求め、`--roles` が `code-reviewer` を除外するケースの全2×2セルを求めなかった（review round 1 Major が指摘した1セルだけを塞ぎ、QA round 2 が別セルで FAIL）。T-1147 は round 2 のブリーフィングが inventory 範囲を `docs/` とスキル行に限定し（engineer の round-2 hand-off の grep 範囲、QA round 2 の diff-scope 行 `git diff cf135df3...HEAD -- docs/ skills/run/SKILL.md` がいずれもこの範囲）、`bin/check-refreeze-grant.sh` 自身のヘッダーコメント（`--help` で逐語露出）を2ラウンド連続で見落とした。両者とも根はエンジニアや QA の実行でなく、コーディネーティングセッションが書いたブリーフィングの scope そのものにある——review 記録自身がそう明記している。source: board T-1146 の `same-class-2` 行、`.shell-team/reviews/T-1147.md` round 1 Blocker（"the round-2 inventory and QA's re-audit were scoped to `docs/` and `skills/run/SKILL.md` by the coordinating session's briefing"）。
- **T-1147 の same-class-2 エスカレーションメッセージが、直前に答えたばかりの T-1146 のものと同じ形をそのまま再利用した結果、オペレーターがこれを「再度の同じ質問」と誤読し、2回の訂正入力を要した**——「私の指示は round 3 GO だったはず、なぜまた round 3 なのか」という訂正と、「ゲートのキーワードを打たせること自体がコストだ、以後は選択式にする」という訂正の2件。エスカレーションが「前回入力からの差分」を明示していなかったことが直接の原因で、T-1147 のループ内ホスト入力回数は（GO 1回では済まず）3回に膨らんだ。source: `.shell-team/interventions/T-1147.md` の `human-correction` 2件。
- **board の記述が2件の訂正入力を「同じやり取りの中で（in the same exchange）」と要約したが、この run 自身の telemetry はその2件を約2時間35分離して記録している**——own-run で `.shell-team/runs/shell-team.jsonl` の `run_id":"20260916T193102Z-t1147"` を実測: `human-correction` イベントが `2026-09-17T00:31:12Z` と `2026-09-17T03:06:47Z` の2回、`GO` イベントが `03:10:18Z`（2件目の3分31秒後）。前者と後者の間には2時間35分の間隔があり、「同じやり取り」という board の要約表現（closed 行、operator answer 行に相当する箇所）はこの間隔を圧縮している——記録された事実（recall）と、記録が伝える重要度・時間感覚（salience）が一致しない一例。この差は本レトロの Loop-trap check「Unverified self-report」節で扱う。source: `.shell-team/runs/shell-team.jsonl`（own-run による実測、上記2タイムスタンプ）。
- **A2 の loop time 目標（≤3.1h）を両タスクとも外した**——own-run で telemetry のタイムスタンプから再計算: T-1146 は総経過 12:37:39→19:30:07（6h52m28s）から同タスクのホスト待ち（`same-class-2` 15:55:33Z → `GO` 18:21:20Z、2h25m47s）を除いて約4h27m、T-1147 は総経過 19:31:39→翌03:50:34（8h18m55s）からホスト待ち（`same-class-2` 22:18:51Z → 最終 `GO` 03:10:18Z、4h51m27s）を除いて約3h27m——いずれも目標の3.1hを超過し、超過分は上記の同一クラス再発による round 3 の追加コストにほぼ対応する。source: own-run 実測（`.shell-team/runs/shell-team.jsonl` の `human` イベントと最初/最後のイベントのタイムスタンプ差分）、CLAUDE.local.md「軽い運用 A2」節の目標値。
- **T-1147 round 1 の engineer は「完了」を hand-back した後もバックグラウンドの spec 実行が終わるのを待って board の hand-off 行に1件の追記コミットを行い、その時点で round 2 の engineer は既に別ファイルを編集中だった**——ファイル競合は起きなかったが、「完了通知が来た＝そのサブエージェントは木への書き込みを終えた」という前提が、バックグラウンドの子プロセスが残っている場合には成立しないことを示す実例。source: `.shell-team/interventions/T-1147.md` の `assumption-contradicted` エントリ。

<!-- retro-section: try -->
## Try

- class-closure のリワーク・ブリーフィングは、コーディネーティングセッションが「報告済みの箇所」や「特定ディレクトリ／特定ファイル」に inventory 範囲を絞らず、**タスクの diff 全体＋全てのアダプター可視ストリーム（出荷ドキュメント、スキル行、変更したスクリプト自身のヘッダー／`--help` テキストを含む）**を対象にする一文を毎回明示する。owning: コーディネーティングセッションのリワーク・ブリーフィング作成手順（mode A2 のような pm-spec 非経由のリワークで特に重要）。source: `.shell-team/reviews/T-1147.md` round 1 Blocker、board T-1146/T-1147 の `same-class-2` 行。
- 発見された欠陥の構造が小さな組み合わせ条件表（例: ロールスコープ×occupant種別の2×2）である場合、リワーク指示・AC は「報告されたセル＋隣接1セル」ではなく**表の全セルを名指しで列挙**する。T-1146 の`remedy-claim-vs-generator-gate` は review round 1 の Major が1セルを塞いだ後、QA round 2 が別セルで再発した。owning: pm-spec / コーディネーティングセッションの rework 指示作成、AC 設計時のチェックリスト。source: board T-1146 の `same-class-2` 行、`.shell-team/reviews/T-1146.md` round 2 の4セル再導出。
- 同一トリガークラスが別タスクで再度発火した際のエスカレーションメッセージは、**前回のホスト入力からの差分（何が完了し何が新規か）を冒頭で明示し、要求する選択肢にタスク ID を名指しする**——T-1147 のエスカレーションが T-1146 と同じ文面を使い回したことが誤読の直接原因だった。この規律はすでに `.shell-team/interventions/T-1147.md` にオペレーター裁定として記録されており、今後の全ゲート提示（型は選択式）にも適用する。owning: コーディネーティングセッションのエスカレーションメッセージ作成手順。source: `.shell-team/interventions/T-1147.md` の `human-correction` 2件。
- 記録（board のクローズドライン等）が、interventions ログの複数エントリの時間関係を「同じやり取り」のように圧縮して要約する場合、書き込み前にその run の telemetry（`.shell-team/runs/shell-team.jsonl` の該当 `human` イベントのタイムスタンプ）と突き合わせる一手順を、レトロ以外の場（board 記述時点）にも設ける。owning: コーディネーティングセッションの board 記述手順。source: 上記 Problem 節の telemetry 実測。

<!-- retro-section: traps -->
## Loop-trap check

> AI-attest（own-read による自己申告）。人間へ丸投げしない。読めなかったものだけを unverified として明示する。S4 drift report は T-1146/T-1147 のいずれにも存在しない（`ls .shell-team/reviews/T-1146-drift.md .shell-team/reviews/T-1147-drift.md` を own-run、いずれも該当ファイル無し、exit 1）ため、S4 引用は本レトロに含まれていない。

- **Comprehension debt**: own read したもの——`.shell-team/todo.md` の T-1146/T-1147 全ラウンドの hand-off・QA verdict・close-out エントリ（`closed`、`codex-reviewer round 1/2`、`same-class-2`、`operator answer`、`qa-verifier round 1`（T-1147 のみ明示的な board bullet あり）、`fast-follow disposition`、`entry-mode`/`spec-review`/`source`/`base`/`stacked-on`、`release-tier premise`、`blast-radius-deferral`、`dispatch-reflection`/`dispatch` 各5行、`version-derivation`、`freeze-attestation`、`intent-hash`、T-1146 の `note`、T-1147 の `release-sweep` 行）を `sed -n`/Read で通読、`.shell-team/reviews/T-1146.md`（149行、全2ラウンド全文）、`.shell-team/reviews/T-1147.md`（162行、全2ラウンド全文）、`.shell-team/interventions/T-1146.md`（`no interventions occurred` の3行）・`T-1147.md`（14行、全3エントリ）を全文読了。`.shell-team/runs/shell-team.jsonl` から両タスクの `run_id` を特定し、`event`種別の集計、`human` イベントの全タイムスタンプ、`span`/`phase`/`iteration` の集計を own-run で実測した。git 上で `cf135df3`（T-1146 tip）と `31bcc4ae`（T-1147 tip、HEAD）の祖先関係、develop への未到達を own-run で確認した。lessons.md を `same-class`/`class-closure`/`inventory`/`escalation message`/`choice list` 等のキーワードで検索し、既存の隣接エントリ（2026-07-21・2026-08-02・2026-09-16 の各エントリ）を own-read した。**未読・未検証のまま**: (1) `.shell-team/specs/T-1146-codex-agents-migration-followups.md` と `T-1147-class-m-grant-host-neutral-record.md` の全文（見出し構造・凍結行番号は board の freeze-attestation 引用経由でのみ把握し、AC の生の文言を自分で通読していない）。(2) 両タスクの実際の `git diff`（`bin/check-codex-agents.sh`／`bin/gen-codex-agents.sh`／`bin/check-refreeze-grant.sh`／`bin/check-binding.sh`／`docs/adopting.md`＋`.ja.md`／`docs/tuning-oversight.md`＋`.ja.md`／`skills/run/SKILL.md` の実コード変更そのもの）——board と review record の引用経由でのみ把握し、diff 自体を own-run で確認していない。(3) `.shell-team/provenance/T-1146.md`（114行）・`T-1147.md`（31行）の本文（`wc -l` で行数は確認したが内容は未読）。(4) `tests/codex-agents/run.sh`・`tests/check-refreeze-grant/run.sh`・`tests/check-binding/run.sh` の fixture コード自体。
- **Review criteria re-stated**: このサイクルで実際に「マージ可能」判定に使われた基準は、own read で以下の通り確認できる——(1) QA と code-reviewer は毎ラウンド own-run で凍結済み `- check:` 行を再実行し、Codex は自分自身の scratch out-dir（QA や engineer のものを流用しない）で4セル／2セルの組み合わせを再導出してから閉鎖を認めた——T-1146 round 2 の「own scratch out-dirs, not the QA's, not the engineer's — independently seeded」、T-1147 round 2 の repo-wide sweep と fixture vacuity 再現がその代表例。(2) code-reviewer は毎ラウンド、まず primary pass（`codex exec review --base`）を own-run し、Blocker/Major が出た回だけ adversarial pass（devil's-advocate）を追加実行して第三の欠陥を能動的に探した——T-1146 round 1 とT-1147 round 1 はいずれも adversarial pass を実行、両者の round 2 は「primary が新規 Blocker/Major を出さなかった」ため adversarial をスキップし、その理由を synthesis audit ledger に own-record している。(3) same-class-2 は「Codex がそう言ったから」ではなく、`rework-digest.sh --trigger same-class-2` という機械判定＋事前凍結された means-ends reflection の4問という手続きに基づく——T-1146 は「never-dropped set 着弾」（case 2）、T-1147 は「対象コンポーネントに事前処分なし」（case 1）という異なる根拠を、board の `same-class-2` 行自身の記述から own-read で区別した。(4) T-1147 review round 2 は、review 自身の frozen spec の `:40`/`:67` の record-prose 文言（同じクラスの主張を凍結宣言領域内に持つ）を「shipped surface に対する gate ではない」という基準で note に留め、class-B re-freeze を要しないと own-judge した——この判定基準（shipped/adopter-visible surface のみが gate 対象、spec 自身の宣言プロースは対象外）は review record 自身が明示的に述べている。
- **Unverified self-report**: (1) 本レトロの依頼元（コーディネーティングセッション）のサリエンス情報「2つの operator corrections が同じやり取りの中で（in the same exchange）」着地したという記述は own-run で裏取りできなかった——`.shell-team/runs/shell-team.jsonl` の `run_id":"20260916T193102Z-t1147"` を own-run で読んだところ、2件の `human-correction` イベントは `2026-09-17T00:31:12Z` と `03:06:47Z` に記録されており、その間隔は約2時間35分ある。「同じやり取り」という表現は少なくとも telemetry のイベント記録上は裏取りできず、この2件の間にホストが一度離れて戻ってきた可能性を排除できない——**unverified — human judgment needed**（この2時間35分の間にホスト側で何が起きていたか、あるいは単に board 記述側の圧縮表現なのかは、このレトロの入力からは判定できない）。この主張はレトロ本文（Problem 節）では「telemetry 上は2時間35分離れている」という own-run の実測値のみを事実として採用し、「同じやり取り」という要約は board 側の記述として引用に留めた。(2) T-1146 の board `operator answer` 行が記す時刻「2026-09-17 00:5x JST」は telemetry の `same-class-2` イベント（`2026-09-16T15:55:33Z` = `2026-09-17T00:55:33` JST）と一致するが、実際の `GO` イベントは telemetry 上 `2026-09-16T18:21:20Z`（`2026-09-17T03:21:20` JST）であり、board 行の時刻ラベルがどちらの時点を指しているのか（エスカレーション発生時刻か、それとも承認そのものの時刻か）は本レトロの入力からは一意に確定できない——両者とも「00:5x」という丸め表記であるため実質的な矛盾とまでは断定できず、Problem 節では「ホスト待ち時間」の実測値（`same-class-2`→`GO` の差分）のみを事実として用い、board 行の時刻ラベルの解釈自体は unverified のまま扱った。(3) T-1147 の engineer が round 1 の hand-back 後にバックグラウンドの spec 実行完了を待って board に追記コミットしたという `.shell-team/interventions/T-1147.md` の `assumption-contradicted` エントリは own-read で確認できたが、そのコミット自体（`fe1b877c`）の diff 内容は own-run で確認していない——コミットハッシュと時刻はエントリの記述をそのまま採用し、diff の中身自体は未検証。

<!-- retro-section: lessons -->
## Lesson candidates

- `[common]` class-closure のリワーク・ブリーフィングを書く役割（pm-spec、あるいは mode A2 のようにコーディネーティングセッション自身がリワーク指示を書く場合はそのセッション自身）が、inventory の対象範囲を「報告された箇所」や特定ディレクトリに絞ってしまうと、同一クラスの欠陥が範囲外の箇所（本サイクルでは reader スクリプト自身のヘッダーコメント、あるいは組み合わせ条件表の別セル）で再発する。既存の 2026-07-21 lesson「複数の canonical file に同じ class の norm が現れたら、直す前に全箇所を inventory する」を extend する候補——今回の新しい点は、inventory 範囲を誤って狭めた主体が実装者（engineer/QA）ではなく**リワーク指示そのものの著者**だったこと、および対象が「canonical file の集合」だけでなく「アダプター可視のストリーム全体（出荷ドキュメント・スキル行・変更対象スクリプト自身の `--help`/ヘッダーテキスト）」に及ぶこと。発火条件: 1つの類似クラスの是正が複数のファイル・複数の出力面（コード・ドキュメント・CLIヘルプ文言等）にまたがって存在し、リワーク指示がそのうちの一部だけを名指しするとき。誰が助かるか: pm-spec を持つ、あるいは mode A2 のような軽量運用でコーディネーティングセッション自身がリワーク指示を書く、任意の spec 駆動開発ループ。source: `.shell-team/reviews/T-1147.md` round 1 Blocker（"the round-2 inventory ... were scoped to `docs/` and `skills/run/SKILL.md` by the coordinating session's briefing"）、board T-1146 の `same-class-2` 行、`.shell-team/lessons.md:611-612`（既存エントリ、拡張候補）。
- `[common]` レビュー／QA の欠陥の根本原因が、小さな組み合わせ条件表（例: 「ロールがリクエストされているか」×「occupant の種別」の2×2）の1セルである場合、そのセルだけを塞ぐ修正は表の別セルで同一クラスを再発させる——本サイクルの T-1146 では review round 1 の Major がセル3を、QA round 2 が別のセル（`--roles` 除外×非削除可能 occupant）を独立に発火させた。拡張候補: 診断行の複数ディスパッチ地点に関する既存の2026-09-16 lesson（`.shell-team/lessons.md:1581`）や、canonical file 横断 inventory の既存 lesson（同 `:611`）と隣接するが、対象が「ファイルの集合」「ディスパッチ地点の集合」ではなく「条件の直積で定義されるセルの集合」である点で異なる。発火条件: あるレビュー finding の根本原因が、2つ以上の独立した二値条件の組み合わせのうち1つの組（セル）に起因すると特定できるとき。誰が助かるか: 状態やフラグの組み合わせに応じて分岐する挙動を持つスクリプト・関数を修正する任意のプロジェクトの spec 著者・レビュアー。source: board T-1146 の `same-class-2` 行、`.shell-team/reviews/T-1146.md` round 2 の4セル・6プローブ再導出。
- `[common]` 同一トリガークラスのエスカレーションが別タスクで再度発火した際、直前のタスクで既に答えたメッセージと同じ文面をそのまま再利用すると、オペレーターがそれを「既に答えた質問の再送」と誤読しうる。拡張候補: 全てのホスト入力要求メッセージ（エスカレーション・リワーク承認依頼・ゲート提示）は、冒頭で「前回のホスト入力から何が完了し、何が新規か」を明示し、対象タスク ID を選択肢の文言自体に含める。既存の2026-08-02 lesson「承認ゲートは各選択肢の内容を必ず示す」と隣接するが、あちらは選択肢の内容の完全性を扱い、こちらは「前回とのメッセージ形状の類似による誤認」という別の軸を扱う。発火条件: 同じトリガークラス（same-class-2 等）のエスカレーションが、直前の別タスクへの応答の直後に再度発生するとき。誰が助かるか: 同種のゲートが複数タスクにまたがって繰り返し発火する、任意のオペレーター向けエスカレーション設計。source: `.shell-team/interventions/T-1147.md` の `human-correction` 2件、`.shell-team/lessons.md:829-836`（既存エントリ、隣接）。
- `[common]` 凍結済み spec 自身の宣言領域プロース（Non-goal／Goal／Assumptions 等）が、出荷面（コード・ドキュメント・CLI）からは完全に除去されたはずの同じクラスの主張を、record 上の言い回しとして残し続けることがある——T-1147 round 2 のレビューは、spec の `:40`/`:67` の文言を「shipped surface に対する gate ではない、record prose のnote」と own-judge し、そのレビュー自身が「将来の spec 著作でこの曖昧さパターンを避けたいなら、これは lessons-corpus 候補であってこのタスクが負う fix ではない」と明記した。拡張候補: spec 著作時に、Non-goal/Goal のような宣言領域プロースが将来「出荷面のクラス閉鎖の是非」を判定する対象になり得る文言（今回のような、対象ホストの挙動に関する否定的な断定文）を含む場合、それが「出荷面のクラス閉鎖の対象である」か「record 自身の記述であり対象外」かを spec 自身に明示する一文を freeze 時に追加する。発火条件: 凍結済み spec の宣言領域が、shipped/adopter-visible surface からは既に除去された主張と同じクラスの文言を、issue の relayed premise の言い換えとして保持しているとき。誰が助かるか: 出荷面のクラス閉鎖を審査する cross-provider レビューを持つ、任意の spec 駆動開発ループ。source: `.shell-team/reviews/T-1147.md` round 2 の Minor/nits・Synthesis audit ledger #4・Fast-follow disposition（"that is a lessons-corpus candidate, not a fix owed by this task"）。

## Notes

- **サイクル窓**: `retro-inputs.sh` の merge-commit walk が返す2件（#553, #550）は own-run で全て foreign と確認済み（PR番号がこのサイクルの2タスクのいずれとも一致しない）。正典窓は本文冒頭に記した board の `stacked-on` 記述＋`git merge-base --is-ancestor` の own-run 確認であり、この ledger の `cycle-window` 行はこのサイクルの窓としては扱っていない。
- **手順8（scaffolding audit・モデル変更サイクル限定）はスキップした**: 依頼文（本レトロのタスクプロンプト）に実行モデル変更の明示宣言（channel a）は無い。channel b の best-effort own-run（`git diff e66b21bf..HEAD -- 'agents/*.md' | grep -n '^[+-]model:'`）も実施したが、`model:` frontmatter の追加・削除は0件だった——ただしこれは「変更なし」を証明するものではなく、silence は inconclusive である。channel a が無い以上、判定不能として意図的にスキップした——`no scaffolding relaxation candidates` という「実施してゼロ件」の裁定ではなく、発火条件自体が成立しなかったスキップである。
- `pr-metadata` は `gh` 未認証で unavailable——PR番号（#556/#559）と issue 番号（#546+#545/#515、fast-follow の #554・#555・#557・#558）は board の close-out 行・fast-follow disposition 行の own-read で直接確認しており、この欠落によるレトロ本体への影響は無い。
- **本レトロが lessons.md corpus 昇格候補に含めなかった、オペレーター運用固有の裁定**——(1)「ゲートは全てキーワードを打たせず選択式で提示する」という T-1147 の operator ruling は、この checkout のホスト個人の対話様式に関する裁定であり、他の adopter のループ運用が一般に採用すべき技術的規律とは性質が異なる。(2) 上記の Try 節「エスカレーションメッセージは前回からの差分を冒頭に書く」規律も、既に `.shell-team/interventions/T-1147.md` にオペレーター裁定として記録済みであり、この checkout の運用規約（CLAUDE.local.md 相当）に反映するのが妥当で、lessons.md corpus には昇格候補として含めていない。次スプリントのプランニングで、この2点を CLAUDE.local.md 側の「承認ゲートの範囲」節へ反映するかどうかを検討する候補として残す。
- **次スプリントのプランニングで確認すべきこと**: このサイクルで起票された fast-follow issue（T-1146 由来: #554・#555／T-1147 由来: #557・#558、いずれも本文中に own-read で確認済み）が、次スプリントの候補ゴールのいずれかに合致するかを、起票理由の原文（board の `fast-follow disposition` 行）に照らして個別に確認する。特に #557（`--base ""`/`--config ""` の空文字フォールスルーが `bin/check-refreeze-grant.sh` と `bin/check-oversight.sh` の両方に同一パターンで存在）と #555（T-1146 由来、`--root /` の正規化差異）は同じ根（2つの兄弟スクリプト間の未統一な引数処理）を持つ可能性があり、まとめて着手できるか検討する価値がある。
- 本レトロの執筆時、`templates/prompt-blocks/record-portability.md` と本サイクル自身の CLAUDE.local.md「公開面の記述規律」節の双方に従い、外部プロジェクト・トラッカー・人・端末の実名は引用していない（この cycle は該当する外部 run を持たない——b run #6 はこのサイクルの後、リリース後に実施される）。検出した finding はいずれも construction（パターン名・パス・行番号）でのみ言及し、`check-pii-shapes.sh` が検出する類の実バイト自体は引用していない。
