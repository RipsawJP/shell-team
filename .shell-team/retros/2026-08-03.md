# Retro 2026-08-03

**Cycle scope**: sprint v1.3.0（2026-08-02〜2026-08-03、ゴール: "turn loop invariants that were only written down into fail-closed mechanisms and release v1.3.0" — ACHIEVED、v1.3.0 は develop 上に公開済み）。`bin/retro-inputs.sh --last-n 11` の cycle-window はこのスプリントの11件のマージに正確に一致する: PR `#99`/`#100`（T-1016）、`#102`/`#103`（T-1017）、`#106`/`#107`（T-1018）、`#110`/`#111`（T-1019）、`#113`/`#114`（T-1020）、`#115`（v1.3.0 リリース実行、task ID なし）。直前の `#94`（retro）・`#95`（lessons promotion）は前スプリント v1.2.0 に属するためスコープ外。
**Pull requests**: `#99`, `#100`, `#102`, `#103`, `#106`, `#107`, `#110`, `#111`, `#113`, `#114`, `#115`
**Generated by**: scrum-master agent v0（人間トリガー、スプリントレトロ）

`bash bin/retro-inputs.sh --base develop --last-n 11 --lessons "$(bash bin/team-paths.sh --get lessons)"` の出力を以下に逐語的に貼り付ける。

## Retro inputs

- input: cycle-window — status: read — detail: 11 merge commits from develop (first-parent); capped at --last-n 11 (a declared cap, not a truncation)
  - ac3c3b0870b4 Merge pull request #115 from RipsawJP/chore/release-v1.3.0
  - 3294b0d92c34 Merge pull request #114 from RipsawJP/chore/close-out-t1020
  - 5ef9a1ce3c8c Merge pull request #113 from RipsawJP/feature/97-lessons-supersede-sweep
  - 392b934cdd88 Merge pull request #111 from RipsawJP/chore/close-out-t1019
  - ce16574cfddb Merge pull request #110 from RipsawJP/feature/80-is-span-row-shared-fixtures
  - 3778f6209f89 Merge pull request #107 from RipsawJP/chore/close-out-t1018
  - 39b60f6775e6 Merge pull request #106 from RipsawJP/feature/96-freeze-attestation-gate
  - 87daf7430fd2 Merge pull request #103 from RipsawJP/chore/close-out-t1017
  - 48a63198f67b Merge pull request #102 from RipsawJP/feature/90-close-out-interventions-gate
  - cf3602a6e9c3 Merge pull request #100 from RipsawJP/chore/close-out-t1016
  - e63b2867b352 Merge pull request #99 from RipsawJP/fix/89-close-out-stranding
- input: review-artifacts — status: read — detail: 24 review artifacts in .shell-team/reviews
- input: provenance — status: read — detail: 24 provenance files in .shell-team/provenance
- input: specs — status: read — detail: 26 spec files in .shell-team/specs
- input: run-telemetry — status: read — detail: 2 run telemetry files in .shell-team/runs
- input: previous-retro — status: read — detail: 3 prior retro files in .shell-team/retros
- input: lessons — status: read — detail: 785 non-blank lines in .shell-team/lessons.md
- input: pr-metadata — status: unavailable — detail: gh is not authenticated (optional enrichment only)
- input: interventions — status: read — detail: 18 intervention records in .shell-team/interventions

## Summary

スプリント v1.3.0 は「口頭・文書だけで存在していたループ不変条件を fail-closed な機構へ変換し、v1.3.0 をリリースする」というゴールを、5タスク（T-1016〜T-1020）全件出荷＋リリース公開で達成した。5タスク中3タスク（T-1016, T-1018, T-1020）が Codex round-1 で `REQUEST_CHANGES` を受け、うち2件（T-1018 の fail-closed ゲート自体の fail-open、T-1020 の網羅性主張の未検証）は QA が PASS 済みの後に cross-provider レビューだけが独立に発見した。人間の介入は2タスクで3件記録され（T-1016: `assumption-contradicted` ×2、T-1020: `human-interrupt` ×1）、T-1020 の介入は CDT 世代の未移植記録（issue #112）を掘り起こした。

<!-- retro-section: keep -->
## 続けること（Keep）

- **凍結済み `check:` 行が構造的に矛盾していた時、エンジニアは黙って書き換えず `BLOCKED` として報告し、人間承認の再凍結（v1→v2）で解決する、という規律が今スプリントも機能した。** T-1016 の AC10（インライン spec fixture に Task ID 行が無く、check-intent が blank-line ロジックに到達する前に構造的に落ちる）と AC16（D5/D6 の編集が `tests/errexit-safe/run.sh` の行番号ピンをずらし、スコープロックと矛盾）——source: `.shell-team/interventions/T-1016.md`（両エントリ）, PR #99。
- **cross-provider（Codex）レビューが、QA の実行ベース検証を通過した後でも fail-closed ゲート自身の欠陥を独立に発見した。** T-1018: `bin/check-intent.sh` の attestation クロスチェックが `08`/`09` のような先頭ゼロ付き2桁カウントで bash の八進数解釈によりクラッシュし、classified でないエラーのまま `exit=0`/`aligned` を返す fail-open（Blocker）——この検証機構自体が「checkers must fail closed」という repo の契約を守る目的のタスクで発生した。T-1020: 24件の supersede 候補が候補空間全体を尽くしているという「exhaustive」主張に、選定手法・会計の記述が無いまま出荷されかけた（Major）。source: `.shell-team/reviews/T-1018.md`（Blocker, `bin/check-intent.sh:606`）, `.shell-team/reviews/T-1020.md`（Major, `.shell-team/specs/T-1020-supersede-adjudication.md` 356行目付近）, PR #106/#107, PR #113/#114。
- **T-1018 が出荷した freeze-attestation ゲートが、同一スプリント内で即座に2回（T-1019, T-1020）自己適用され、実データで運用された。** board 上に `freeze-attestation (v1, ...)` レコードが T-1019・T-1020 双方の entry に記録されている——機構が出荷された直後に空文ではなく実運用される、という短いフィードバックループが成立した。source: `.shell-team/todo.md`（T-1019 entry, T-1020 entry の freeze-attestation 行）, PR #106/#107, PR #110/#111, PR #113/#114。
- **same-class-2（同一クラス2ラウンド連続）の pre-commitment 発火を、QA 自身の裁量的な降格判断が正しく回避した。** T-1018 round 2 で Codex の adversarial パスが新規 Blocker/Major を主張したが、QA は spec の Input space の "out-of-scope synthetic extremes" 宣言と安全な失敗モード（corrupting write ではなく safe stall）の区別を根拠に minor へ降格し、「Codex の生ラベルをそのまま受け入れていたら pre-commitment の redesign-or-split 提示が必要だった」という判断過程自体を記録として残した。source: `.shell-team/reviews/T-1018.md`（round 2, L643-660, "Transparency note for the coordinator"）。
- **T-1020 round 2 の QA が、round 1 の Major を pm-spec の算術を再チェックする形ではなく、生コーパスから独立に会計を再構築する形で閉じた。** 80件全件を自分で再走査し、36/13/22/9=80・重複ゼロ・欠落ゼロを確認してから resolved と判定している。source: `.shell-team/todo.md`（T-1020 QA round 2 entry）。

<!-- retro-section: problem -->
## 課題（Problem）

- **fail-closed ゲートを新設するタスク自身（T-1018）が、round 1 で fail-open のバグを出荷しかけた。** `bin/check-intent.sh` の attestation クロスチェックが `$(( ))` の先頭ゼロを八進数として解釈しクラッシュ、classified でない stderr を残したまま `exit=0`/`aligned` を返す——同じファイル内で `hash_version` には既に `10#` prefix の慣行があったにもかかわらず、新設コードはそれを踏襲しなかった。QA の round-1 実行ベース検証はこれを見逃し、Codex の独立発見でのみ捕捉された。source: `.shell-team/reviews/T-1018.md`（Blocker, `bin/check-intent.sh:606`）。
- **T-1020 が「24件で候補空間を尽くした」という exhaustive 主張を、選定手法・会計の記述なしに出荷しようとした。** issue #97 自体が体系的な棚卸しを要求しているにもかかわらず、companion 文書のどこにも80件のうちなぜこの24件が全体を尽くすかの説明が無く、QA も round 1 ではこれを見逃した（Codex の primary/adversarial 両パスが独立に発見）。round 2 で pm-spec が §3 に選定手法と全80件の会計を追加して解消。source: `.shell-team/reviews/T-1020.md`（Major, L208）。
- **T-1020 の人間承認ゲートで、issue #97 が要求する棚卸しの一部が、この repo に移植されていない CDT 世代の記録（2026-07-26 の74件 pairwise domination sweep、ゼロ dominations）としてすでに一度行われていた可能性が、機械的な発見ではなく operator の記憶による割り込みでのみ表面化した。** その場で issue #112 が起票されたが、恒久的な機構は無い——同種の「先行世代の判断がこの repo に移植されていない」ギャップは次回も人間の記憶頼みで発見される。source: `.shell-team/interventions/T-1020.md`, `.shell-team/specs/T-1020-supersede-adjudication.md`（L8, L108）。
- **T-1016 round 1 で、新設のストランド検知（entry-opener 用の正規表現）がフォーマット検証ゲートより厳格になり、D4「一つの欠陥に一つのメッセージ」という凍結済み Settled Decision に違反するカスケード誤検知を起こした。** チェック済み（`- [x]`）行の場合は根本原因の行に一切メッセージが出ないまま、正当な子行だけがストランド扱いされる——2つの matcher が同じ判定を異なる厳しさで行う、という境界述語ミスマッチのクラス。source: `.shell-team/reviews/T-1016.md`（Blocker, L226-240; Major, L242-244）。
- **T-1016 は単一タスク内で2件の凍結前提の崩れ（`assumption-contradicted`）を記録し、その解決に human-ratified 再凍結（v1→v2）を要した。** 一つは凍結検証時の compound-check 分解漏れ（expected-FAIL の check: 行が「なぜ FAIL するか」まで検証されていなかった）、もう一つはスコープロックの許可リストが機構的に連動する companion suite（`tests/errexit-safe/run.sh`）を最初から含んでいなかったこと。source: `.shell-team/interventions/T-1016.md`（両エントリ）。

<!-- retro-section: try -->
## 次に試すこと（Try）

- **fail-closed ゲートの実装レビューに、算術式コンテキスト（`$(( ))`）で使われる正規表現キャプチャ済み数値文字列が、ファイル内の既存の `10#` prefix 慣行と一貫しているかを確認する項目を明示的に加える。** T-1018 の欠陥は「新設コードが同じファイル内の既存の安全な書き方を踏襲しなかった」という、単発の入力形状バグではなく横展開可能な確認観点——2026-08-01 の既存 lesson（fail-closed ゲートの境界形状 AC）に隣接するが、あちらは region-closing の境界形状を扱うもので、数値リテラル解釈という別の角度を明示していない。担当: qa-verifier, pm-spec。
- **spec / companion 文書が「網羅的」「exhaustive」「尽くした」と主張する箇所には、対象空間の総数・選定手法・除外理由の3点を明示することを標準的な検証観点にする。** T-1020 で Codex primary/adversarial 両パスが独立にこの欠落を発見し、round 2 で pm-spec が実際に埋めて解消した——既存の 2026-07-19 lesson（"same-class bulk-fix inventory claim needs the actual grep command and hit count attached"）に隣接するテーマだが、あちらは同一クラスの一括修正の申告を対象にしており、本件は候補集合の完全性そのものの主張を対象にする点で異なる。担当: pm-spec, qa-verifier。
- **issue #112（CDT 世代の74件 pairwise domination sweep の移植）を次スプリントのプランニング入力として明記する。** T-1020 が実例を示した通り、この移植ギャップは人間の記憶に依存した偶発的発見にしか頼れていない——本レトロはこれを issue 化するのではなく、既に存在する issue #112 を次プランニングの候補として再掲するに留める（スプリント運用ルールにより issue 新設はレトロの役割ではない）。
- **エンジニア/QA の hand-off が開示する「N件のミューテーション自己検証のうち M件のみ独立再現した」という部分検証の開示を、単発の透明性メモとしてではなく、未検証残数を継続的に追跡する対象として次スプリントの入力にする。** T-1016（2/8）、T-1017（2/9）と、開示自体は一貫して行われているが、残りが後続で拾われる保証は無い。担当: qa-verifier。

<!-- retro-section: traps -->
## ループトラップ自己点検（Loop-trap check）

このスプリントに S4 ドリフトレポート（`.shell-team/reviews/<task-id>-drift.md`）は存在しない（`.shell-team/reviews/` を `find -name '*drift*'` で確認、該当ファイルなし）ので、二層 attest の引用は行わない——以下はすべて本レトロを書いた自分自身の一次読解に基づく attest である。

- **Comprehension debt**: `.shell-team/reviews/T-1016.md`〜`T-1020.md`（全ラウンド分、Codex verdict 節を含む）、対応する `.shell-team/interventions/T-1016.md`〜`T-1020.md` 全件、`.shell-team/specs/T-1020-supersede-adjudication.md`（issue #112 関連箇所）、`.shell-team/specs/T-1020-lessons-supersede-sweep.md`（issue #112 関連箇所）、および board（`.shell-team/todo.md`）の T-1016〜T-1020・freeze-attestation 記録行は自分で直接 Read/grep した。**一方、`bin/check-intent.sh`・`bin/check-handoff.sh`・`bin/close-out.sh` の実装コード本体（今スプリントの変更箇所そのもの）は自分では一切読んでいない**——T-1018 の八進数 fail-open や T-1016 の境界述語ミスマッチについての理解は、QA/Codex の記録が引用する行番号・再現コマンド・出力に依拠しており、diff を自分の目で確認したわけではない。正直な comprehension debt として明示する（source: 上記の3ファイル自体を本セッションで Read していないこと）。
- **Review criteria re-stated**: 本レトロでは「あるタスクが完了した」の判定基準として、(1) QA が `READY_FOR_REVIEW`（またはその先の `READY_FOR_MERGE`）に到達し、かつ (2) cross-provider の Codex レビューが `READY_FOR_MERGE` に到達し、かつ (3) `.shell-team/{provenance,reviews,interventions}/T-10XX.md` の3種の記録ファイルが実在し内容が該当タスクと整合すること、の3点を採用した——「エージェントがそう言ったから」ではなく、5タスク全件についてこの3点を自分で `Read`/`grep` して確認した上でこの基準を適用している。
- **Unverified self-report**: 本セッション中、自分は一つの `check:` 行も `check-acs.sh`/`check-intent.sh`/`check-handoff.sh` も実行していない——上記の全ての「PASS」「conformant」「exit 0」という記述は QA/Codex の記録自身の自己申告であり、私（本レトロ執筆エージェント）による第三者的な再実行ではない。QA と cross-provider レビューという二重の検証チェーン自体は5タスクを通じて一貫しており矛盾は見つからなかったが、**この二重チェーンをさらに独立した三者目として再検証したわけではない点は「unverified — human judgment needed」として明示的にエスカレートする**。特に T-1016 の「2 of 8」・T-1017 の「2 of 9」というミューテーション自己検証の部分再現（QA 自身が開示している未検証範囲）は、そのまま「unverified — human judgment needed」として扱う。

<!-- retro-section: lessons -->
## Lesson candidates

- `[target-specific]` fail-closed ゲートの新設コードが、算術式コンテキスト（`$(( ))`）で正規表現キャプチャ済みの数値文字列を扱う際、同一ファイル内に既存の `10#` prefix 慣行（先頭ゼロを八進数と誤解釈させない書き方）が既にあるにもかかわらずそれを踏襲せず、bash の八進数解釈による classified でないクラッシュ経由の fail-open を出荷しかけた（T-1018, `bin/check-intent.sh:606`）。新設コードのレビューに「同一ファイル内の既存の数値パース慣行と一貫しているか」を明示的な確認項目として加えることを提案する。（既存の2026-08-01「A task that builds a fail-closed gate writes boundary-shape acceptance criteria against the gate itself」に隣接するが、あちらは region-closing の境界形状（未知の識別子・近似スペル）を対象にしたもので、算術コンテキストでの数値リテラル解釈という別の入力次元は対象にしていない、という欠落を指摘するもの。）
- `[common]` spec やその companion 文書が「網羅的（exhaustive）」「尽くした」と主張する時、対象空間の総数・実際に用いた選定手法・除外理由の3点セットが揃っていないと、その主張自体が未検証のまま出荷されうる（T-1020, 24/80件の supersede 候補選定）。cross-provider レビューの primary/adversarial 両パスが独立にこの欠落を発見して初めて是正された。（既存の2026-07-19「A same-class bulk-fix inventory claim needs the actual grep command and hit count attached」に隣接するが、あちらは同一クラスの一括修正の申告を対象にしており、本件は候補集合そのものの完全性の主張を対象にする点で異なる。）
- `[target-specific]` 過去世代（本 repo 移植前の CDT 期）で既に行われた判断・測定の記録がこの repo に移植されていない場合、同種の要求（issue #97 の「棚卸し」）が再度発生した時にその重複・先例が機械的には見つからず、人間の記憶による偶発的な割り込みでしか発見されない（T-1020, issue #112）。CDT からの移植ギャップは今回に限らず repo の中で繰り返し観測されているクラスであり、移植候補の棚卸し自体を次プランニングの定期的な検討項目にする価値がある。
- `[common]` エンジニア/QA が「N件のミューテーション自己検証のうちM件のみ独立に再現した」という部分検証を開示する慣行は、透明性としては機能しているが、開示された未検証残数がその後どこにも追跡されない（T-1016: 2/8, T-1017: 2/9）。開示自体を維持しつつ、未検証範囲を次ラウンド・次タスクの入力として拾う仕組みがあれば、単なる免責の記録から実際のカバレッジ改善に繋がる。

## Notes

- **モデル変更棚卸し監査（役割ステップ8）はスキップした。** channel (a)（本レトロ起動指示内の明示的なモデル階層変更宣言）は無く、channel (b)（`agents/*.md` の `model:` frontmatter の追加・削除・変更を `git diff` で確認）もオーケストレーターにより事前に確認済みでゼロ件と報告されている。役割の規律により、channel (a) 不在かつ channel (b) が手がかり無しの場合は "undeterminable" として発火させない——本レトロにはこの監査結果を含めていない（監査自体を実行していないため、「no scaffolding relaxation candidates」という zero-candidate 表記もここでは用いていない。それは監査が実際に走って何も見つからなかった場合の表記であり、今回は監査そのものを skip したケースにあたる）。
- **PR とタスク ID の対応は `gh` 経由の title/headRefName から機械抽出したものではない。** `pr-metadata` はこのサンドボックスで `gh` が未認証のため `unavailable` であり、`#99`→T-1016 のような対応関係は、本レトロを起動したオーケストレーター（人間）から直接提示された指示に基づく——PR の自由記述本文（body）から抽出したものでもない。これは本役割の「title/headRefName のみを機械的に信頼する」制約と矛盾しない代替経路として扱う（前回のレトロ 2026-08-02 でも同じ扱いを記録済み）。
- **レビュー記録の欠落はこのスプリントの5タスクには無かった。** T-1016〜T-1020 いずれも対応する `.shell-team/reviews/T-10XX.md` が存在し、内容も本レトロの執筆過程で確認済み。PR `#115`（v1.3.0 リリース実行そのもの）はタスク ID を持たないリリース作業であり、この役割の定義上レビュー記録の対象ではない——欠落として扱っていない。
- **本レトロで新規の issue 起票は行っていない。** issue #112（CDT の74件ドミネーションスイープの移植）は既に起票済みであり、Try / lesson candidates の中で次プランニングの入力として再掲したに留まる。本 repo のスプリント運用ルール上、レトロは issue を増やす場ではなく次スプリント候補を出す場である。
- `tasks/lessons.md`（`.shell-team/lessons.md`）は本レトロで一切編集していない。上記の lesson candidates はすべて本ファイル内のみに存在し、人間の判断による promotion 待ちである。
