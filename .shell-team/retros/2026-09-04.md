# Retro 2026-09-04

**Cycle scope**: sprint `r3-body`（T-1115〜T-1120、ゴール: `binding.conf` がロールの「どう呼ぶか」（executor invocation path + effort application）を統べるようにする、想定 v2.5.1 PATCH）— **stacked batch-GO 方式のため 7 本の PR は全て未マージ**（バッチ GO 待ち）。窓は `retro-inputs.sh` の `cycle-window` 行（develop 上の過去 merge commit のみを返し、本スプリントの成果物を1件も含まない）ではなく、board の merge-chain を `git merge-base --is-ancestor` で隣接リンクごとに全数実測して確定した（詳細は `## Notes`）。
**Pull requests**: `#417`（rider, lesson 昇格 chore・T-1115 の枝分かれ点より後に伸びた train head の追加コミットを含む・詳細は `## Notes`）, `#423`（T-1115, #412）, `#426`（T-1116, #418）, `#428`（T-1117, #235）, `#433`（T-1118, #419）, `#436`（T-1119, #420）, `#437`（T-1120, #409+#407, 現 HEAD）
**Generated by**: scrum-master agent v0 (manual trigger)

`bin/retro-inputs.sh` prints the `## Retro inputs` heading below and its
ledger lines together — paste its output here verbatim, unedited, right after
that heading. `cycle-window` was run with `--last-n 8` for readability; the
capped count changes nothing about the identity problem this cycle has (see
`## Notes`) — no `--last-n` value returns this cycle's own merges, because
none of T-1115–T-1120 (nor PR #417) has merged to `develop` yet.

## Retro inputs

- input: cycle-window — status: read — detail: 8 merge commits from develop (first-parent); capped at --last-n 8 (a declared cap, not a truncation)
  - d4e1f45e1291 Merge pull request #413 from RipsawJP/chore/release-v2.5.0
  - a453e67df67b Merge pull request #411 from RipsawJP/feature/1114-scope-lock-worktree-deltas
  - 55d73b42985c Merge pull request #410 from RipsawJP/feature/1113-quantity-relay-checker
  - e93e3bd71423 Merge pull request #408 from RipsawJP/feature/1112-parallel-default-and-executor-hosting
  - d717ee4d06df Merge pull request #406 from RipsawJP/feature/1111-agent-concurrency-verification
  - 40466baf3e05 Merge pull request #404 from RipsawJP/feature/1110-freeze-version-derivation
  - 80e86fac7641 Merge pull request #401 from RipsawJP/chore/lesson-promotion-2026-08-31
  - 310085a1f045 Merge pull request #394 from RipsawJP/chore/release-v2.4.0
- input: review-artifacts — status: read — detail: 784 review artifacts in .shell-team/reviews
- input: provenance — status: read — detail: 123 provenance files in .shell-team/provenance
- input: specs — status: read — detail: 125 spec files in .shell-team/specs
- input: run-telemetry — status: read — detail: 2 run telemetry files in .shell-team/runs
- input: previous-retro — status: read — detail: 21 prior retro files in .shell-team/retros
- input: lessons — status: read — detail: 1313 non-blank lines in .shell-team/lessons.md
- input: pr-metadata — status: unavailable — detail: gh is not authenticated (optional enrichment only)
- input: interventions — status: read — detail: 118 intervention records in .shell-team/interventions

## Summary

T-1115〜T-1120 の6タスクが両ゲート green（QA PASS + codex APPROVE/READY_FOR_MERGE）で完了し、stacked chain は本レトロ自身の `git merge-base --is-ancestor` 実測（`## Notes`）で健全性を確認した（batch GO 待ち）。スプリントの目的（`binding.conf` が呼び出し経路と effort 適用を統べる）は達成された一方、freeze-sweep のミスが6タスク中5タスクで独立に再発し（原因クラスは毎回違うが、根は「凍結前に測定すべき前提を prose から想起して済ませた」という同一の生成器）、うち3件（T-1118/T-1119/T-1120）は post-implementation の BLOCKED→v1→v2 再凍結を要した。ホストからは PATCH タスクの工程規模そのものへの疑問（「PATCH の割に工程が大きい」）が上がり、コーディネーティングセッション自身の分析で per-task 固定コスト（凍結・blast-radius 二腕・QA・レビュー・close-out）が支配的で、blast-radius の候補集合の絞り込みが実質機能していない（125件中120件選択）ことが判明した。harness 側の中断（背景スライス kill 4件・API 529 2件・ネットワーク起因 stall 1件）は全て回収済みで成果物の損失はない。

<!-- retro-section: keep -->
## Keep

- freeze-sweep のミスは全件、実装後に engineer が `STATUS: BLOCKED` で停止し凍結済みテキストを自分で編集することなく報告した——T-1118（3件同時発見: `admits-authority`/`codex-reviewer` の相互充足矛盾、BSD grep 255文字上限、AC17 の unanchored count）、T-1119（AC4 の base-side positive control が偽）、T-1120（AC5 の "still present" リテラルが偽）のいずれも v1→v2 の class-M/B 再凍結で復旧し、フリーズ済み intent の書き換えは一度も engineer 自身の手では起きていない（source: `.shell-team/interventions/T-1118.md` entry 4、`T-1119.md` entry 3、`T-1120.md` entry 2、board の各 `intent-ratified` 行）。
- cross-provider review が独自に到達した Major が同一ラウンド内で closed した実例2件: T-1119 の diff review round 1 Major（`codex-reviewer`/`wrapper-hosted` の effort 適用主張が dispatch shape 未区別で偽）は primary/adversarial 両パスが独立に指摘し round 2 で完全に closed（source: `.shell-team/reviews/T-1119.md` Major finding + synthesis ledger）。T-1116 の spec-review は3ラウンドを要したが `frozen-rules-contradict`/`verdict-value-unreachable` 等6件の Major がいずれも round 3 の APPROVE までに解消された（source: coordinator ledger `t1116-rework-ledger.md`、`.shell-team/reviews/T-1116.md`）。
- スプリント内で発案された機構化候補（`bash -x` で全 `- check:` 行をトレースし、`.base` capture を読む `grep|test|cmp` の直後に `rc=1` が来るものを機械列挙する手法）が、その場しのぎでなく T-1119/T-1120 の v2 再凍結で実際に使われ、AC4/AC5 の欠陥をそれぞれ的中させた（source: coordinator ledger "T-1119 (2026-09-04)" 節、board T-1119/T-1120 の `freeze-attestation (v2)` 行の `bash -x` トレース記述）。
- harness 起因の中断（背景スライス kill 4件: T-1118 ×2・T-1120 ×2、API 529 2件: T-1118 engineer・T-1119 pm-spec、ネットワーク起因 stall 1件: T-1118 qa-verifier）は、resumable runner・verify-first resume により成果物の損失ゼロで全件回収された（source: `.shell-team/interventions/T-1118.md` entries 5,6,8,9、`T-1119.md` entry 2、`T-1120.md` entries 6,7）。
- ロールの write-boundary 逸脱が2件とも自己開示・機械検出で捕捉された: qa-verifier は自分自身の live tree への `git checkout --` 誤操作を自己申告し `cmp` で byte-identical 復元を検証済み、codex-reviewer の board 書き込みは `check-count-claims.sh` が `[x]` への変更を拒否したことで顕在化した（source: `.shell-team/interventions/T-1120.md` entries 4, 5）。

<!-- retro-section: problem -->
## Problem

- **freeze-sweep のミスが6タスク中5タスクで独立に再発した**（T-1117 のみミスなし）: T-1115（branch point の想定違い、ブロッキングではないが assumption-contradicted）、T-1116（`check-pii-shapes` が AC15 の原因として列挙されなかった）、T-1118（相互充足矛盾・BSD grep 上限・unanchored count の3件同時）、T-1119（AC4 の base-side positive control が prose からの一般化で偽と判明）、T-1120（AC5 の "still present" リテラルが T-1112 で既に削除済みの文言を主張——5件目の再発、`.shell-team/interventions/T-1120.md` entry 2 が自称する「fifth consecutive」）。共通の欠陥クラス: base 側の前提を実際の blob に対して literal で実行する代わりに prose/記憶から分類していた（source: `.shell-team/interventions/T-1115.md`、`T-1116.md` entry 2、`T-1118.md` entry 4、`T-1119.md` entry 3、`T-1120.md` entry 2）。
- **PATCH タスクの工程規模がタスクの見た目の大きさに比例していない**: ホストが「PATCH の割に工程が大きい」と直接質問し（2026-09-03）、コーディネーティングセッション自身のコスト分析で、per-task 固定コスト（凍結・blast-radius 二腕〈各60分程度〉・QA・レビュー・close-out）が支配的であり、blast-radius の候補集合の絞り込みが実質機能していない（125件中120件が選択される——board/spec 読み取りがほぼ普遍的なため prune が空振りしている）ことが判明した。小さい PATCH に分割する提案は、この固定コストが下がらない限りコスト中立〜悪化と評価された（source: coordinator ledger `t1116-rework-ledger.md` "T-1118 retro inputs" 節）。
- **harness 起因の中断件数**: 背景スライス kill は4件（T-1118 ×2、T-1120 ×2）で確認できたが、依頼文が挙げた「API 529 terminations (3)」は一次資料では2件（T-1118 engineer, T-1119 pm-spec——T-1119 自身の記録が「today の2件目」と明記）しか裏付けられなかった。原因は未検証: T-1118 の1回目の背景スライス kill と、ホストが別プロジェクトのセッションから `/effort` を書き換えた時刻の一致が「もっともらしいが未検証」の因果として記録されているのみ（source: `.shell-team/interventions/T-1118.md` entry 5、`T-1119.md` entry 2）。
- **オーケストレーター自身のブリーフィングが、ロールの write-boundary が禁じる書き込みを誘発した**: T-1120 の codex-reviewer briefing が「board 上に verdict を記録し…APPROVE の時だけ flip」と明示的に書き込みを依頼し、`agents/codex-reviewer.md` 自身の board-and-write-boundary ルールに反する結果を招いた。根本原因は record 自身が「ロールでなくブリーフィングにある」と明記している（source: `.shell-team/interventions/T-1120.md` entry 5）。
- **Codex CLI の隔離フラグ（`--ignore-user-config`/`--ignore-rules`/`--sandbox read-only`）が `~/.agents/skills` の discovery を閉じない**ことが本スプリントだけで独立に少なくとも4回観測された（T-1116 spec-review r2a の stall、T-1118、T-1119 の primary/adversarial 両ラウンド、T-1120 primary）——いずれも issue **#430** に comment されたのみで、このスプリント内での修正は行われなかった（source: `.shell-team/reviews/T-1119.md`/`T-1120.md` の Invocation hygiene observations、board T-1118/T-1119 の `fast-follow disposition` 行 "Commented: #430"）。
- **T-1119 の effort-application 主張が dispatch shape 未区別のまま出荷されかけた**: 4つの adopter doc（README/README.ja/docs/adopting/docs/adopting.ja）の「`codex-cli` の行なら effort が適用される」という文が、shipped default 自身の `codex-reviewer`/`codex-cli` 行（`wrapper-hosted` で dispatch され effort フラグを一切受け取らない）に対して偽だった。primary・adversarial 両 Codex パスが独立に検出し round 2 で closed（source: `.shell-team/reviews/T-1119.md` Major finding, synthesis audit ledger）。

<!-- retro-section: try -->
## Try

1. **base-side positive control の literal 実行を check-acs.sh に機構化する**: `- check:` 行を `bash -x` でトレースし、`.base` capture に対する `grep|test|cmp` の直後に `rc=1` が来るものを機械列挙する（`--base-controls` モード相当）。すでに T-1119/T-1120 の v2 再凍結でアドホックに使われ、狙い通り欠陥を的中させた。disposition: **mechanize-as-issue**。source: coordinator ledger "T-1119 (2026-09-04)" 節、board T-1119/T-1120 `freeze-attestation (v2)` 行。
2. **「still present / remains / unchanged」リテラルの sweep 手順をフリーズ手順に明記する**: このタスクが書かない文言について HEAD で「まだ存在する」と主張する check 行は、部分文字列でなく完全なリテラルで `git show <base>:<path>` に対して実行する——本スプリントだけで5件目の再発。disposition: **mechanize-as-issue**（Keep 3 の機構が対応候補）。source: `.shell-team/interventions/T-1120.md` entry 2。
3. **issue #430（Codex 隔離フラグが `~/.agents/skills` discovery を閉じない）を次スプリントで実際にスケジュールする**: 5回 comment されただけで未着手。disposition: **next-sprint-candidate**。source: `.shell-team/reviews/T-1119.md`/`T-1120.md`、board T-1118/T-1119 の fast-follow disposition 行。
4. **blast-radius の候補集合導出を、増分方式（変更パス起点）か board/spec-population-only read の除外により実効的な prune にする**: 現状は125件中120件選択で実質 prune していない。ホストの「PATCH の割に工程が大きい」という疑問への直接の答えはこの一点にある——固定コストが下がらない限り小さい PATCH への分割はコスト中立〜悪化。disposition: **next-sprint-candidate**。source: coordinator ledger "T-1118 retro inputs" 節。
5. **ブリーフィング作成時に「このロールの agents/<role>.md の write-boundary と矛盾しないか」を自己点検するチェックリスト項目を追加する**: T-1120 の breach はロールでなくブリーフィングの作成側に根がある。disposition: **rule-change**（prose、オーケストレーター側の手順）。source: `.shell-team/interventions/T-1120.md` entry 5。
6. **横断的なインシデント件数（harness kill・API エラー等）は、レトロや報告に転記する前に一次資料（interventions ファイル）へ再照合する**: 本レトロの依頼文自身が「API 529 terminations (3)」と述べたが、一次資料は2件しか裏付けなかった（本レトロ自身が今回それを検出し訂正した）。disposition: **next-sprint-candidate**（capture_t1065 の生成器診断と同根)。source: `.shell-team/interventions/T-1118.md`/`T-1119.md`、本レトロ自身の再集計。

<!-- retro-section: traps -->
## Loop-trap check

> AI-attest（現行運用）: 以下は scrum-master 自身が本スプリントの一次資料（board T-1115〜T-1120 の Done entry、`.shell-team/reviews/T-111{5,6,7,8,9}.md`・`T-1120.md`、`.shell-team/interventions/T-111{5,6,7,8,9}.md`・`T-1120.md`、`.shell-team/provenance/T-111{5,6,7,8,9}.md`・`T-1120.md`、コーディネーティングセッション自身の `t1116-rework-ledger.md`、`capture_t1065-record-accuracy-analysis.md`、git のブランチ・コミット履歴）を実際に読んだ上での判断であり、検証できなかった項目だけを「unverified — human judgment needed」として明示する。S4 drift report（`.shell-team/reviews/<task-id>-drift.md`）は本スプリントのどのタスクにも存在しない（`ls .shell-team/reviews/T-111{5,6,7,8,9}-drift.md .shell-team/reviews/T-1120-drift.md` 全件なし・全件確認済み）ので、引用すべき対象はない。

- **Comprehension debt**: 本スプリントの実装コード自体（`bin/check-invocation-path.sh`、`bin/check-model-pins.sh`、`bin/check-count-claims.sh:298` の実新メッセージ、`docs/loop-engineering/harness-invocation-parameters.md`、4本の adopter doc の実際の差分）は読んでいない——読んだのは board のハンドオフ記述、`.shell-team/reviews/*.md` のレビュー記録、`.shell-team/provenance/*.md` の意思決定記録（`T-1118.md` の93行に及ぶ decision 群を含む）であり、生の diff そのものではない。`## Keep`/`## Problem` の各引用はこれら一次記録に基づくものであって、私自身の実装コード読解に基づくものではない。
- **Review criteria re-stated**: 本レトロが「マージ可能」と判定した根拠は2点のみ——(1) T-1115〜T-1120 の各 board エントリが最終ラウンドで QA `PASS`・codex `READY_FOR_MERGE`/`APPROVE` を記録していること（"エージェントがそう言ったから" ではなく `- [x]` 状態と `READY_FOR_MERGE` ラベルを直接読んだ）、(2) `develop` → `6b60585`（`chore/lesson-promotion-2026-09-02` 上の中間コミット、独立の board entry なし）→ `feature/1115`(`d056359`) → `feature/1116`(`6004ef2`) → `feature/1117`(`dfac5cd`) → `feature/1118`(`61a99f9`) → `feature/1119`(`4f96989`) → `feature/1120`(`e7df319`, 現HEAD) の全隣接リンクを `git merge-base --is-ancestor` で自分で実行し、申告どおり連続していることを実測したこと。各タスクの実装コード自体のレビューはしていない。
- **Unverified self-report**: 本レトロの依頼文が挙げた「API 529 terminations (3)」は、一次資料（`.shell-team/interventions/T-1118.md`・`T-1119.md`）を再照合した結果2件しか確認できなかった（T-1118 engineer, T-1119 pm-spec——後者自身が「today の2件目」と明記）——**unverified — human judgment needed**（3件目が本レトロの入力に含まれない箇所で発生したか、依頼文側の集計が1件多いかのいずれか。`## Try` 6 に記録）。背景スライス kill の件数「4」は一次資料と正確に一致した（T-1118 ×2、T-1120 ×2）。「freeze-sweep misses on five consecutive tasks」も、T-1117 単体には freeze-sweep ミスの記録がない（`no interventions occurred`）ものの、`.shell-team/interventions/T-1120.md` entry 2 自身が「Fifth consecutive task with a freeze-sweep miss」と自称しており、この自称の数え方（T-1117 を跳ばして数える）自体は一次資料からは裏取りできなかった——**unverified — human judgment needed**（実質的な事象自体〈T-1115/T-1116/T-1118/T-1119/T-1120 の5件〉は一次資料で確認できたので `## Problem`/`## Try` に反映したが、「consecutive」という語の妥当性は保留する）。issue #430/#429 等の現在の open/closed 状態は `gh` 未認証のため本レトロからは確認できていない——**unverified — human judgment needed**。

<!-- retro-section: lessons -->
## Lesson candidates

- `[common]` フリーズ済み check 行が「(このタスクが書かない文言が) still present / remains / unchanged」と HEAD 上の状態を主張する場合、それは spelling が違うだけの base-side positive control であり、完全なリテラルで `git show <base>:<path>` に対して literal 実行して確認しなければならない——部分文字列一致や prose からの分類は不十分（すでに red になっている criterion は意図した原因がその他すべての原因を覆い隠す）。発火条件: acceptance criterion の check 行または prose が HEAD 上のテキストの不変・残存を主張する時。firing agent: 凍結時の pm-spec / フリーズ sweep を実行するコーディネーティングセッション。supersede-vs-new: `.shell-team/lessons.md:1063`（base blob discriminator の姉妹criterion間一貫性）・`:1383`（フリーズ sweep のクロスジョインは live artifact を含む）に隣接するが軸が異なる新規（HEAD側の"still present"主張がbase側controlになるという発見自体が新しい）。mechanization-vs-prose: 機構化——`check-acs.sh` に `bash -x` トレースで `.base` capture 直後の `rc=1` を機械列挙するモードを追加する（T-1119/T-1120 の v2 再凍結でアドホックに実証済み）。who-is-hurt: engineer が実装後に不満足な criterion で BLOCKED を報告し、再凍結の1ラウンド分のコストが毎回発生する(本スプリントで3回)。public-fitness: 一般化可能（凍結済み受け入れ基準を持つ spec 駆動ループ全般）。source: `.shell-team/interventions/T-1120.md` entry 2、`T-1119.md` entry 3、`T-1118.md` entry 4。
- `[common]` Codex CLI の `--ignore-user-config`/`--ignore-rules`/`--sandbox read-only` フラグは provider 設定・sandbox 書き込みのみを制御し、`~/.agents/skills/*.md` の discovery 経路は閉じない——「隔離された」レビュー呼び出しでも operator ローカルな非リポジトリファイルを読みうる。発火条件: レビュー・調査目的で `codex exec` を隔離フラグ付きで呼び出す時。firing agent: codex-reviewer / `codex exec` を呼ぶ任意のロール。supersede-vs-new: 新規（lessons.md にこの CLI 固有の隔離ギャップに相当する既存エントリは見当たらなかった）。mechanization-vs-prose: 修正が用意でき次第機構化（issue #430 に5回 comment されたが未着手）。who-is-hurt: レビュー判断が意図しない operator ローカルな skill prose に影響されうる（本スプリントでは実害の証拠はないが、読み込み自体が露出）。public-fitness: 一般化可能（Codex CLI の隔離フラグを cross-provider review に使う任意のチーム）。source: `.shell-team/reviews/T-1119.md`（round 1/2 hygiene notes）、`T-1120.md`（hygiene notes）、board T-1118/T-1119 の `fast-follow disposition` 行。
- `[target-specific]` オーケストレーターが書くブリーフィングが、対象ロール自身の `agents/<role>.md` write-boundary ルールが禁じる書き込みを名指しで依頼すると、そのロールが素直に従うことで breach が発生する——breach の根はロールの実行でなくブリーフィングの著作にある。発火条件: コーディネーティングセッションが特定の書き込み対象（ファイル・board flag）を名指しするブリーフィング/起動指示を作成する時。firing agent: ブリーフィングを作成するコーディネーティングセッション/オーケストレーター。supersede-vs-new: `.shell-team/lessons.md:1296` 付近の既存エントリ（レビュアー自身の迷子書き込みからの回復）とは軸が異なる新規（あちらはロール側の逸脱、本候補はブリーフィング側が逸脱を誘発するケース）。mechanization-vs-prose: まず prose（ブリーフィング作成前のチェックリスト項目: 名指しした書き込みを対象ロールの write-boundary ルールと突き合わせる）——1回の再発では機構化の投資に見合わない。who-is-hurt: 権限のない書き手による board 変異は共有状態を静かに破損しうる（今回は `check-count-claims.sh` が `[x]` への変更をたまたま拒否したことで顕在化しただけ）。public-fitness: このリポジトリの board-and-write-boundary 機構固有（一般化するなら「委譲層は委譲先の制約を自分の指示より優先する」という広い原則になりうるが、ここでは target-specific として提案する）。source: `.shell-team/interventions/T-1120.md` entry 5。
- `[target-specific]` PATCH タスクの工程規模がタスクの見た目の大きさに比例しないのは、blast-radius の候補集合導出が実質的な prune になっていないため（本スプリントで125件中120件選択）——per-task 固定コスト（凍結・blast-radius 二腕・QA・レビュー・close-out）が支配的な限り、小さい PATCH への分割はコスト中立〜悪化。発火条件: ホストまたはプランニングが PATCH タスクの工程規模を疑問視する時、または複数の小さい PATCH リリースへの分割を検討する時。firing agent: tech-lead（プランニング時の見積り）/ コーディネーティングセッション（工程コストの説明責任を負う時）。supersede-vs-new: `.shell-team/lessons.md:1121`（mechanism-vs-wording-only の検証形の価格付け）に隣接するが軸が異なる新規——あちらは検証の「形」を価格付けし、本候補は blast-radius の「母集団規模」を価格付けする。mechanization-vs-prose: 機構化として起票（変更パス起点の増分 sweep、または board/spec-population-only read の候補集合からの除外）。who-is-hurt: ホストの時間と壁時計コストが、成果物の実サイズに連動しない apparatus に費やされ続け、リリース頻度化の提案自体が機能しなくなる。public-fitness: 一般化可能(候補集合導出がコーパス成長と共に狭まらない機械的検証パイプライン全般)。source: coordinator ledger `t1116-rework-ledger.md` "T-1118 retro inputs" 節（2026-09-03、ホスト質問「PATCH の割に工程が大きい」）。
- `[common]` 横断的なインシデント件数（harness の背景プロセス kill・API エラー等)の集計は、レトロや報告に転記する前に一次資料（interventions ファイル等)へ command で再照合してから書く——記憶や前段サマリーからの転記は同じ生成器（LLM の文章生成は出力瞬間には想起であって測定でない）の再発である。本レトロの依頼文自身が「API 529 terminations (3)」と述べたが、一次資料は2件のみ裏付けた。発火条件: 任意のレトロ・報告が複数タスク/複数記録にまたがるインシデントや量の tally を再掲する時。firing agent: tally を書く役割（ここでは scrum-master 自身への briefing を書いたコーディネーティングセッション）。supersede-vs-new: `.shell-team/lessons.md:1357`-`:1358`（量的主張は command-derived が望ましく、記憶での転記が defect そのもの）の直接の隣接データ点——単一spec内の量から、複数タスクにまたがる横断集計への coverage 拡張。mechanization-vs-prose: prose（転記前に再 grep する規律）——この頻度では専用チェッカーへの投資は見合わない。who-is-hurt: 誤った tally が次スプリントの優先順位付けを誤導する。public-fitness: 一般化可能（横断集計を行う任意のレトロ/報告ロール）。source: 本レトロ自身の再集計（`.shell-team/interventions/T-1118.md`・`T-1119.md`）、`.shell-team/lessons.md:1357-1358`。

## Notes

- **サイクル窓の宣言について（stacked batch-GO 前提の適用、前々回・前回レトロと同型、かつ中間コミットの発見あり）**: `retro-inputs.sh` の `cycle-window` 行（`--last-n 8` で capped）が返した8件の merge commit（`#413`〜`#394`）は、いずれも本スプリント（T-1115〜T-1120、PR `#417`/`#423`/`#426`/`#428`/`#433`/`#436`/`#437`）と一致しない——全て前スプリント `recurrence-mechanization` の成果物であり、stacked batch-GO 方式のため本スプリントの成果物は develop に一本もマージされていない（uncapped で実行した場合は develop の全179 merge commit が返り、本スプリントの成果物を1件も含まないことを別途確認済み）。代わりに board の各タスク Done エントリが記す `- branch:`/`- stacked-on:` の宣言を出発点に、`git merge-base --is-ancestor` を隣接ペアごとに実行して連鎖を確認した: `develop`(`d4e1f45`) is ancestor of `6b60585`（`chore/lesson-promotion-2026-09-02` 上の中間コミット、`.shell-team/interventions/no-task.md` への r3-body プランニング前提の記録——**独立の board entry を持たない中間コミット**）is ancestor of `feature/1115-version-tier-tiebreaker`(`d056359`, PR #423, T-1115) is ancestor of `feature/1116-harness-invocation-parameter-probe`(`6004ef2`, PR #426, T-1116) is ancestor of `feature/1117-pin-default-equality-lock`(`dfac5cd`, PR #428, T-1117) is ancestor of `feature/1118-alternate-executor-invocation-path`(`61a99f9`, PR #433, T-1118) is ancestor of `feature/1119-effort-application`(`4f96989`, PR #436, T-1119) is ancestor of `feature/1120-rider-count-claims-message-and-fanout-docs`(`e7df319`, PR #437, T-1120, 現HEAD)——全7リンク `OK`（`git rev-list --count` の合計も150コミットで一致）。**重要な発見**: `chore/lesson-promotion-2026-09-02` ブランチ自身の最終コミット（`8be7350`、PR #417 の HEAD になる想定）は、T-1115 以降のどの stacked tip の祖先でもない（`git merge-base --is-ancestor 8be7350 e7df319` → FAIL）——`.shell-team/interventions/T-1116.md` entry 1 が自ら記録するとおり、T-1115 の枝分かれ点はその途中のコミット（`6b60585`）であり、chore ブランチ自身はその後もう1コミット（discard record、`8be7350`）を積んで独立に develop へマージされる。これはまさに本チェッカー/このレトロの運用ルールが警告する「中間に独立の board entry を持たないブランチ／PR」の実例であり、`#417` を単純に「T-1115 の直前に挟まる1本のブランチ」とみなすのは不正確——PR #417 は「sprint train には含まれるが T-1115〜T-1120 の祖先には含まれない、並行する枝」である。
- `pr-metadata` は `gh` 未認証のため unavailable。PR タイトル・ブランチ名からの機械的な task-id 抽出は行わなかったが、board のタスクエントリ自身が issue 番号・PR 番号を明示していたため、6タスク全ての review artifact（`.shell-team/reviews/T-1115.md`〜`T-1120.md`）・interventions artifact（`.shell-team/interventions/T-1115.md`〜`T-1120.md`）・provenance artifact の存在を直接確認できた（欠落なし。T-1117 のみ interventions の実質記録が「no interventions occurred」）。
- モデル変更サイクル判定: channel (a)（本依頼文中の明示的なモデル変更宣言）は無し。channel (b)（`git diff d056359..e7df319 -- 'agents/*.md'` での `model:` frontmatter 行の追加/削除/変更）を確認したが該当差分ゼロ。両チャネルとも変更を示さないため、本レトロでは scaffolding audit（棚卸し）を実行していない。
- 「API 529 terminations (3)」という依頼文中の観測は、`.shell-team/interventions/T-1118.md`・`T-1119.md`（本スプリントで読んだ全 interventions ファイル）のいずれからも2件しか裏付けられなかった——`## Loop-trap check` および `## Try` 6 参照。issue #430/#429/#434/#435 の現在の open/closed 状態、#417/#423/#426/#428/#433/#436/#437 各 PR の live な mergeable 状態は `gh` 未認証のため本レトロの一次資料からは確認できていない——board の各タスク entry が freeze-attestation 内で `- environmentally-unverified:` として同じ制約を記録している箇所と符合する。
- lessons.md（1313行）を今回スキミングした範囲では、`[common]` base-side positive control 候補は `:1063`/`:1383` に隣接、`[target-specific]` 書き込み境界候補は `:1296` 付近と軸違いで隣接、`[target-specific]` PATCH 工程規模候補は `:1121` に隣接、`[common]` 横断集計の再照合候補は `:1357`-`:1358`（quantity-claim exposure、capture_t1065-record-accuracy-analysis と同根）に直接隣接、と本文中に注記済み。`[common]` Codex skills-discovery 候補には近傍の既存エントリは見当たらなかった。
