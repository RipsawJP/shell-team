<!-- BEGIN interventions: T-1062 -->
- intervention: human-correction
  date: 2026-08-13
  summary: During the Validate phase the operator challenged the orchestrator's "lightweight" framing of T-1062, pointing at the engineer turn's multi-hour wall clock.
  effect: Framing corrected in-conversation — "lightweight" described the diff (one bullet, ~4 lines), not the process cost, which is dominated by the frozen AC7 verification (8 mutation probes, 71-step CI-equivalent run, full-population blast-radius diff over 66+67 specs) and is roughly fixed per task regardless of edit size. No change to the running task; the observation feeds issues #248 (workload-vs-latency study, operator-directed) and #249 as next-planning input, and the retro's mandatory planning-degeneration scope.
- intervention: assumption-contradicted
  date: 2026-08-13
  summary: The v2 freeze sweep attested that the repaired AC1/AC3 check lines "cannot disagree about where the bullet ends" and the v1→v2 evidence recorded the class closed; Codex round 2 measured two further exploitable shapes in the same boundary logic (a whitespace-only line not treated as blank, and f re-ignition by a later marker line carrying the same literal), both byte-for-byte reproduced and pandoc-confirmed.
  effect: The v1→v2 re-freeze stands (the reviewer's mandatory item validated it — the round-1 shapes really are closed); the contradicted part is the closure claim for the whole class. Consequence drawn at round 2's rework: the repair mechanism itself changes (incremental state-machine patching is abandoned for a first-match-latched, line-number two-pass extraction) rather than patching the same awk a third time — the same-class-2 digest's reconsider-the-design-premise choice applied at the mechanism level, within class-M bounds. Cumulative class count check-line-under-assertion x4 (r1 x2, r2 x2).
- intervention: human-correction
  date: 2026-08-13
  summary: At the STOP escalation the operator rejected the whole trajectory, not a branch of it — this sprint exists to fix a documentation gap ("a major update shipped with its core feature undocumented"), yet a one-bullet docs fix has consumed more effort than a feature task, and the operator named the failure directly - the loop lost sight of the purpose, specialized in the means, and kept polishing something worthless.
  effect: Read as the design-premise ruling the two same-class-2 digests had already recommended and the orchestrator twice under-applied (round 2 answered it at the mechanism level, swapping awk strategies, instead of at the premise level - whether a frozen grep should assert markdown rendering semantics at all). Disposition - execute the class-B rescope (option A of the STOP package): strip AC1/AC3's placement/confinement mechanics back to the corpus's honest instruction-presence bound (T-1059 AC5 precedent), keep the never-broken region byte-identity, and let placement stay verified by the already-shipped runtime reads (AC7(g) render check, QA rule-4) that confirmed the deliverable three times. Orchestrator accountability, not tooling: the over-reach was freezable at v1 (the sweep read the check lines for mechanics, never for proportionality), round 1's ledger already cited the presence-only precedent without the orchestrator applying it to AC1/AC3, and the reviewer's per-round class-M prescriptions were followed where the goal said stop. Primary retro exemplar for the mandatory planning-degeneration scope; measured cost feeds #248.
<!-- END interventions: T-1062 -->
