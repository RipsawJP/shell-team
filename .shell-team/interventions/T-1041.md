<!-- BEGIN interventions: T-1041 -->
- intervention: assumption-contradicted
  date: 2026-08-06
  summary: In the freeze-gate feedback that routed the mktemp repair to pm-spec, the coordinating session asserted "your own AC preamble (line 76) promises 'a mktemp -d scratch directory created with the guarded idiom'". Measured wrong by pm-spec against the file: T-1041's spec has no AC preamble and line 76 is blank — the quoted sentence lives in T-1040's spec (line 130), and the orchestrator transferred it across specs from memory. The only in-file text making the guarded-idiom claim is the Notes-for-engineer prior-art bullet, which the repair made true.
  effect: The repair itself was unaffected (the mktemp breakage was measured, not inferred from the phantom anchor), so nothing was rolled back; pm-spec's hand-off records the correction and this entry preserves it as the task's own N1-class instance — a premise written into a downstream prompt from memory instead of from the artifact, caught by the receiving role re-reading the source. Same class as T-1040's trimmed-relay entry and the T-1040 freeze-gate's under-anchored version grep: the third orchestrator-side instance this sprint, which is retro input (same-class counting), not merely a curiosity.
<!-- END interventions: T-1041 -->
