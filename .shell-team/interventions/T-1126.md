# Interventions — T-1126

<!-- BEGIN interventions: T-1126 -->
- intervention: work-deferred
  date: 2026-09-06
  summary: Codex review round 1 returned BLOCKED before any read — `codex exec … review --base develop` (codex-cli 0.149.1, Homebrew) failed with a server-side 400 `The gpt-6-astra model requires a newer version of Codex`; the operator-side CLI config pins `model = "gpt-6-astra"`. Not a sandbox or auth failure; a CLI/model version mismatch outside this repository.
  effect: The cross-provider review is deferred until the operator upgrades the Codex CLI or repoints its configured model; the board stays at `READY_FOR_REVIEW`, no executor substitution, review record `.shell-team/reviews/T-1126.md` (`8840df0`) carries the exact error. Sprint clock keeps running (lightweight-mode measurement counts this wait as operator turn 1 for T-1126).
- intervention: assumption-contradicted
  date: 2026-09-06
  summary: The frozen spec's Goal and the round-1 implementation stated, at all 8 in-scope sites, that the loop "stops before a destructive or irreversible operation" as part of what it guarantees. Codex review round 2 (primary + adversarial) measured the shipped generic contract `templates/shell-team.contract.yaml` (`human_gate: [merge, push]` only) and `docs/tuning-oversight.md:101` ("a discipline the operator imposes on themselves, not a rule the loop enforces"): the destructive-operation stop is operator configuration, not a loop guarantee. The task that existed to remove one over-claim about gates had written another in new words; pm-spec, engineer and qa-verifier all relayed it from the issue's deliverable sentence without checking the contract.
  effect: Rework routed to engineer (`REWORK`): the fixed layer is stated as the completion gate plus merge authority; the destructive/irreversible stop moves into "whatever the operator's own oversight configuration adds". The frozen Goal prose is left as written and this entry records the correction (lightweight mode: one freeze, wording mutable; no check line's subject moved). Lesson instance: a claim about what the loop guarantees is verified against the shipped contract file, not against the issue text or a sibling doc's prose.
- intervention: assumption-contradicted
  date: 2026-09-06
  summary: The coordinating session's briefing to pm-spec dictated the dispatch record line `- dispatch: spec-review — none — operator-approved lightweight mode (…)`, a three-field form; `bin/close-out.sh` refused the close-out twice (four fields required, then the ground must open with `recommendation:`/`saving:`/`break-even:`/`cost-input:` plus an id). The briefing was authored from memory of the record's shape instead of from the board's existing `spec-review — none` precedents three entries away.
  effect: The line was rewritten at close-out to the `recommendation: spec-review-none-default (…)` form the precedents use, and the correction is stated on the line itself. No agent was at fault; the defect is the briefing side. Lesson instance: a briefing that dictates a machine-checked record line copies the shape from a precedent on the board (or from the checker's grammar), never from memory.
<!-- END interventions: T-1126 -->
