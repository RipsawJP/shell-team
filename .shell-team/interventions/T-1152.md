<!-- BEGIN interventions: T-1152 -->
- intervention: unclassified
  date: 2026-09-24
  summary: the harness's auto-mode classifier refused pm-spec's Write of the spec file and Edit of the board (no verdict returned, hard fail); the orchestrator escalated, the operator chose "T-1152 配置 GO", and the orchestrator placed the reviewed spec and board entry itself
  effect: Specify completed one seam later than the role hand-off; the same escalation applies to any later sub-agent repo write refused on the same ground
- intervention: human-correction
  date: 2026-09-24
  summary: operator (during the freeze sweep, before engineer): "ハーネスのバージョンアップで、これまで動いていた処理が動かなくなっている可能性がある。これはこれで調査が必要。無理やり進めるのでなく、きちんと知見を集めてこれまで通り快適に開発が進められるようプラグインをアップデートする必要がある"
  effect: the hotfix loop is paused at the freeze sweep (spec and board entry placed, no intent hash recorded, engineer not invoked); a harness-compatibility investigation (Claude Code 2.1.275→2.1.281) is planned first and its findings decide what the plugin update contains
- intervention: assumption-contradicted
  date: 2026-09-24
  summary: the orchestrator's design ruling assumed the Codex event stream cannot be obtained without a shell redirection on the codex exec line; measurement shows Codex persists every thread's full event record itself under ~/.codex/sessions/YYYY/MM/DD/rollout-<ts>-<thread_id>.jsonl (probe A's thread id resolved to a 22-line rollout carrying its command executions and token usage)
  effect: rulings 1-3 (drop the JSONL raw, make --publish-jsonl optional, move all failure detection to agent prose) are withdrawn before the freeze; the spec is revised so codex-capture.sh --publish takes the thread id and imports the rollout as the .jsonl raw, keeping the T-097/T-107 two-raw hygiene contract and enabling a mechanical commands-executed / failure-marker check
- intervention: human-interrupt
  date: 2026-09-24
  summary: operator, before the revised-spec request to pm-spec was sent: "サブエージェントに依頼する前に一時停止して", then "区切れる？" — the session is closed at this point
  effect: the pm-spec revision (rollout-import design, orchestrator rulings v2) is not yet requested; the task stays at READY_FOR_ARCH with the first-version spec committed and no intent hash recorded; the next session starts by writing rulings v2 and requesting the spec revision
- intervention: human-interrupt
  date: 2026-09-24
  summary: operator resumed the paused session with 「続き」 and, offered the choice list, selected "T-1152 spec 改訂 GO" (11:18 UTC) with no redirection; the recorded next step (orchestrator rulings v2, then the pm-spec revision request) is what runs
  effect: the pause recorded in the previous entry ends; the human event is logged in telemetry as `GO` (seq 8, its ts is the logging time, about fifteen minutes after the answer); before the revision request the rollout was re-read and one v2 premise fell — a failed run's rollout also carries `task_complete` (with `last_agent_message` null and an `error` object), so the publish gate must read those two fields, not the event's presence
- intervention: assumption-contradicted
  date: 2026-09-24
  summary: two premises of the orchestrator's rulings v2 fell to measurement before the freeze — (a) a successful run's rollout `task_complete` line carries no `error` key at all (v2 §1 wrote `"error":null`; only the failed run's line carries an error object), so the publish gate reads "key absent or null" as no error; (b) `codex exec --ephemeral` writes no rollout ("Run without persisting session files to disk"; a bare recipe-shaped probe completed with exit 0, `-o` written, one command executed, and zero session files match its thread id), so the alternate-executor recipe's `--publish --thread-id` could never find its source
  effect: pm-spec's first revision already specified (a) correctly and flagged (b) as blocking; the orchestrator ruled R12 (the recipe line drops `--ephemeral`, keeps `--ignore-user-config --ignore-rules`) and R3' (the gate fires on zero succeeded commands — `CommandExecution` with `exit_code` 0 — instead of zero executed commands, so a sandboxed 2.1.278 run whose tool calls all fail is refused by the script, not only by prose); the spec is amended before the freeze, no re-freeze
- intervention: assumption-contradicted
  date: 2026-09-24
  summary: the orchestrator's dogfood of the shipped primary review shape on this task's own branch contradicted ruling R3's premise — a completed `codex exec … review --base` run writes a rollout whose `task_complete` carries `"last_agent_message":null` on success (the review text travels in `ExitedReviewMode` / `AgentMessage` items and `-o`), so the frozen gate's null-message clause refused the completed review (exit 3, `commands_executed=8 commands_succeeded=8 task_complete=present error=absent`); the premise had been measured only on free-form runs, whose `task_complete` does carry the message
  effect: round 1 of the cross-provider review is REQUEST_CHANGES with three Majors (the null-message clause; substring matching of structural fields, raised by the primary pass; the exactly-one search not fail-closed, raised by the adversarial pass); AC5/AC6/AC7 are amended by a class-B re-freeze (v2, orchestrator rulings R3'' and R14) before the engineer's round 2, under this checkout's standing freeze rule; the primary pass's raw capture was not published (the refusal removed it) and its final message is preserved outside the repository
- intervention: unclassified
  date: 2026-09-24
  summary: engineer round 2 returned BLOCKED instead of READY_FOR_QA — AC6's `\u`-escaped-key fixture in the v2 spec was byte-identical to the plain error fixture (the `\u0065` escape was never written), so one check line demanded two classifications of the same bytes; the engineer reproduced it live in an isolated store and declined to special-case the fixture bytes in the shipped script
  effect: the orchestrator repaired the one fixture under the class-M standing grant (re-freeze v2→v3, `differing=1`, `check-refreeze-class` exit 0), re-ran all 21 check lines against the engineer's unchanged commit (21 PASS), and asked the engineer to re-verify and set READY_FOR_QA; the operator's requested stop (before `/reload-plugins` and the real code-reviewer round) follows that flip
- intervention: human-correction
  date: 2026-09-24
  summary: operator, asked why the orchestrator was running the cross-provider review itself (「なんでレビューを代行？」) and told that this session's loaded `agents/code-reviewer.md` predates the fix so the role would run the old redirected block and go BLOCKED, ruled: 「A-1のタイミングで一度止めて、合図して」 — stop once the round-2 engineer commit is verified, signal, and let the operator run `/reload-plugins` so round 2's review is done by the real code-reviewer role instead of the orchestrator's R6 stand-in
  effect: the loop pauses at READY_FOR_QA after engineer round 2 (`c38987ba`, 21/21, intent v3 aligned); QA round 2 and the code-reviewer round 2 start only after the operator's reload signal; R6 (orchestrator-run review) is demoted to the fallback for a session that cannot reload — escalated: 2026-09-24T14:48:24Z; answered: 2026-09-24T14:50:16Z (operator ran `/reload-plugins` — 23 agents reloaded — and signalled 「リロードした」)
<!-- END interventions: T-1152 -->
