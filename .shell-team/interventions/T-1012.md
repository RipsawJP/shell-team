<!-- BEGIN interventions: T-1012 -->
- intervention: assumption-contradicted
  date: 2026-08-01
  summary: Engineer measured that frozen AC17's check line is mechanically unsatisfiable for any non-empty seq list (command substitution strips the final newline from `got` while `sort` re-adds one to `want`, so the tr-joined strings differ by a trailing space forever); orchestrator reproduced it minimally. The freeze-time disclosure had recorded "No check: line needed correction" — the defect was masked pre-implementation because AC17 already failed for the artifact-missing reason. Second occurrence of this class (first: T-1011 AC10), so the response is an inventory of all check lines for shell-mechanics artifacts, not a spot fix.
  effect: Orchestrator fixes the out-of-block H8 PII-shape self-trigger directly, inventories the spec's check lines for the same mechanics class, and escalates AC17's meaning-preserving correction to the human for a ratified re-freeze (v1->v2). QA does not start until the re-freeze lands.
- intervention: human-correction
  date: 2026-08-01
  summary: At the merge gate, performing the AC29 visual check the sandbox could not, the human observed the board-flag rail shows its empty-state caption even on the full-fidelity mixed fixture. Orchestrator traced it end-to-end - adaptRow never derives a flag property, and the T-1011 emission instructions put phases/verdicts (never READY_FOR_* tokens) into event fields - so flagEvents is empty for every run and the rail can never light, though issue #77's ratified design says the current flag lights and a rework steps back. Static AC13 anchors passed; both QA rounds were environmentally-unverified on pixels; the human eye caught the class reserved for it.
  effect: Merge of PR #82 paused; orchestrator presents dispositions (rework round 2 with a v3 re-freeze vs merge as-is and fix the rail data path in a follow-up task inside the sprint before the v1.2.0 release) and waits for the human's call.
<!-- END interventions: T-1012 -->
