<!-- BEGIN interventions: T-1011 -->
- intervention: assumption-contradicted
  date: 2026-08-01
  summary: Engineer measured that frozen AC10 (live glob over tests/check-run/fixtures/*.jsonl, base-vs-current exit-code equality) and AC11 (this task adds valid-events.jsonl to that directory) are mutually unsatisfiable; orchestrator reproduced it (base checker exit 1, new checker exit 0). The pre-freeze validation had passed AC10 vacuously because the fixture did not exist yet, contradicting the recorded assumption that no check: line needed correction.
  effect: Task set to BLOCKED by the engineer; orchestrator escalates to the human for a ratified re-freeze (v1->v2) scoping AC10 to the seven base-ref fixture filenames instead of a live glob. Implementation work is complete and committed; QA is not started until the re-freeze lands.
<!-- END interventions: T-1011 -->
