<!-- BEGIN interventions: T-1038 -->
- intervention: assumption-contradicted
  date: 2026-08-06
  summary: AC15(b)'s producer-run mutation self-check states that swapping the counting step for a deduplicating set difference (and, separately, dropping the line field before the raw `sort -u`) "confirm AC4 goes red while AC2, AC5, AC7 and the suite all stay green." Reproducing both mutations on a scratch `git worktree` measured the opposite pairing for AC4 specifically: AC4 stayed PASS in both cases (its own `- check:` line re-derives independently from `bin/` and the registry text via a self-contained, always-correct counting `d()`, and never executes `tests/errexit-safe/run.sh`'s own functions, so a mutation confined to that file's internal implementation cannot move it), while the suite itself went RED at its own internal duplicate-site self-check (the exact site the mutation targets), and AC2/AC5/AC7 stayed green as stated.
  effect: Reported the measured pairing (AC4 green, suite red at the duplicate-site self-check) rather than the spec's literally-stated one, in the engineer hand-off and this record, per AC15(c)/(e)'s own disclosure requirement. No spec text was touched — this is a description of AC4's actual (stronger than the AC15(b) prose implies) independence from `run.sh`'s runtime, not a defect in the implementation delivered here; the reviewer keeps the disposition of whether the AC15(b) wording itself should be corrected at a future re-freeze.
<!-- END interventions: T-1038 -->
