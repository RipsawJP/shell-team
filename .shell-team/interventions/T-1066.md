<!-- BEGIN interventions: T-1066 -->
- intervention: assumption-contradicted
  date: 2026-08-14
  summary: The Routing Map's missing-from-telemetry task list included t1032 - pm-spec's first-hand read measured this false; run `20260804T154536Z-t1032` exists with 27 rows in `.shell-team/runs/shell-team.jsonl`. The t1046/t1053/t1054/t1055 gaps held as measured.
  effect: The spec's AC4 derives the missing set mechanically at check time instead of inheriting any list - the read-before-relay duty held at the pm-spec seam, which is where it is designed to hold.
- intervention: assumption-contradicted
  date: 2026-08-14
  summary: The Routing Map stated `tests/rollup-track/run.sh` asserts the TRACKING of `.shell-team/rollups/` - pm-spec's first-hand read measured this overstated; the suite asserts ignorability via `git check-ignore` (that ignore rules do not sweep the path), not tracking or existence.
  effect: The spec's rejection of `.shell-team/rollups/` as the landing surface rests on the narrower measured ground (the directory is a never-overwrite generated-artifact domain owned by `bin/rollup-track.sh`), not on the overstated relay - the landing choice (docs/loop-engineering/) and its verification-class judgment stand on measured facts.
- intervention: assumption-contradicted
  date: 2026-08-14
  summary: The Routing Map counted the reviews corpus at 67 files - pm-spec's first-hand count measured 68 (`T-111`-`T-113` plus `T-1000`-`T-1065`, only `T-1049` missing).
  effect: Population counts are not baked into any criterion as literals; the affected criteria re-derive the count live, so the discrepancy costs nothing downstream and is recorded here rather than silently normalized.
- intervention: assumption-contradicted
  date: 2026-08-14
  summary: The freeze sweep measured that AC4's live-equality clause (declared total-rows/event-rows equal a re-derivation at check time) is structurally unsatisfiable at QA time - the loop ITSELF appends telemetry rows for this very task (4 rows for run_id 20260814T032316Z-t1066 already present at sweep time; more land at every phase transition), so the live count at any later check necessarily exceeds whatever the note declared at authoring.
  effect: AC4 routed back to pm-spec pre-hash for a meaning-preserving correction (filter this task's own run rows out of both sides of the equality, or an equivalent self-measurement exclusion, so the criterion measures the analyzed corpus rather than the loop's own observation overhead). Found at the designed point (verify-then-correct-then-attest); no frozen bytes recorded yet.
- intervention: human-correction
  date: 2026-08-14
  summary: The operator rejected the orchestrator's halt/rework framing for this task - a quantitative survey (work volume x wall-clock trend, a baseline for future executor changes) does not need 100 percent prose precision to serve its purpose, and blocks/reworks over connective-prose findings while every number had been verified correct since round 1 is means-obsession (the same class the operator named at T-1062, now committed at the orchestration layer).
  effect: The A/B/C framing is withdrawn. The orchestrator's task-scoped halt condition and totality-class ratchet are retired for this task class (internal analysis notes) as disproportionate self-imposed discipline, superseded by the operator's proportionality ruling. Exit path - one minimal fix commit (the factually false subset sentence plus the four one-line minors), then QA and Codex rounds briefed with the correctly priced verdict scope: for a quantitative baseline note, deliverable correctness means the computed values and the conclusions' direction; prose findings are notes. The root lesson (review depth is priced by the deliverable's consumer and stakes, not uniformly by class ledgers) goes to the retro.
<!-- END interventions: T-1066 -->
