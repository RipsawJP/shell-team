<!-- BEGIN interventions: T-1110 -->
- intervention: human-interrupt
  date: 2026-08-31
  summary: Mid-QA, the host interrupted with 「qaのエージェントが6並列ではなく、一つのqaエージェントが6shellを並列稼働　以前と方法が変わってますが、この運用変更の理由は明確な検証によるものですか？」— questioning whether the coordinating session's choice to have a single qa-verifier sub-agent run the 6 parallel sweep slices internally (instead of the previous arrangement) was backed by explicit verification.
  effect: Answered honestly no, after re-measuring the precedent at the primary source: T-1109's completed 113/113 two-arm fan-out was the coordinating session's OWN harness-tracked background shell slices over isolated staged worktrees (board, T-1109 QA verdict round 2: "the coordinating session's own completed fan-out"), never a sub-agent-internal fan-out. The delegation into qa-verifier was a division-of-labor choice, not a verified configuration, and carries three unverified risks the briefing had papered over: (1) a sub-agent's shell tool caps a single call at 10 minutes, so the briefed "collect your slices synchronously" is impossible for a ~90-minute sweep and forces detached polling; (2) background processes detached inside a sub-agent are untracked by the harness (standing lesson); (3) the interaction with the harness's 600s no-progress stall detection over a long quiet sweep is unmeasured. Corrected mid-round rather than defended: duty 2 (completing the blast-radius sweep to the full 113-member population) was descoped from qa-verifier by a verify-first message; the coordinating session runs that sweep itself as harness-tracked background slices per T-1109's measured procedure (isolated detached worktrees per slice, corpus staged per the T-1108 recipe entry, serial within a slice so the bin/log-run.sh directory lock never sees two contenders in one tree). qa-verifier keeps its judgment duties (full check-acs run, quantitative-claim re-derivation, freeze-record verification, board/record gates, verdict), with the sweep disclosed in its verdict as coordinating-session-owned. Classified: same class as T-1109's own "why a single agent for this volume" interruption — an execution-hosting election made without citing its measured precedent; the corrective rule is that the fan-out's host session is part of the recorded procedure, not a free variable.
- intervention: human-correction
  date: 2026-08-31
  summary: Immediately after the first interrupt, the host escalated with three messages — 「このスプリントのきっかけになった時と同じことが今起きている」「今のやり方を変えろと言っているんじゃない　なぜこのやり方をしているのか　やり方が変わっても成果は変わらないのか　成果が変わるのであればそれはどのように検証したものなのか（いつどのタスクで）」「全て、データに基づいたベターな方法であるという裏付けの上で規則を作ってほしい　現状ハーネスとして弱すぎる」— naming the coordinating session's behavior as a live re-enactment of the recurrence class this sprint exists to stop: an execution-hosting election made without measured grounds, then reactively flipped when questioned, with neither state data-backed.
  effect: Accounting owned by the coordinating session, against primary sources. (1) The original election (qa-verifier hosts the 6-slice fan-out) had NO measured ground — it was division-of-labor inertia, and its briefing cited ".shell-team/test-recipe.md ~:2273" as "the T-1109 parallel-slice entry" WITHOUT having read it; that entry is actually the lock-contention quirk (which recommends a serial same-tree driver), so the citation was an unverified recall — the same class as T-1109's ":375 discharged" overclaim. (2) The reactive correction (pull the fan-out into the coordinating session) rests on one measured completion (T-1109, 2026-08-30: 113 members x 2 arms, 6 slices over isolated staged worktrees, ~92 min, hosted by the coordinating session as tracked background shells — board T-1109 QA verdict round 2) plus two documented harness facts (a sub-agent shell call caps at 10 minutes; sub-agent-detached background processes are harness-untracked), but NO A/B comparison exists: a qa-hosted fan-out has never been attempted, so "the outcome does not change" is analysis (deterministic check-acs runs in pinned worktrees yield identical verdict bytes) and not measurement (completion reliability and observability are where the configurations differ, and only one configuration has ever been measured to complete). (3) Root cause the harness half: the execution HOST of a fan-out is a free variable — the dispatch record grammar prices serial-vs-fanout but never who hosts the slices — and T-1109's completed 6-slice procedure was never appended to the test recipe (the standing append duty existed and was not applied), so today's setup is a reconstruction from a memory summary rather than a recipe replay. Dispositions, each priced by existing data rather than invented: (a) apply the standing test-recipe append duty when this sweep completes — procedure plus fresh measurements, from the session that ran it; (b) file a mechanization issue making the execution host a recorded, priced dimension of the fan-out election (T-1109's completion is the priced line; a sub-agent-hosted fan-out is unverified-until-piloted); (c) carry the whole incident to the retro as a lesson candidate in the sprint's own recurrence-class frame. The in-flight sweep continues under the one measured configuration, per the host's own words that the method is not being ordered changed.
- intervention: human-correction
  date: 2026-08-31
  summary: Third message in the same thread — 「6エージェントよりも1エージェント6シェルの方が、クオリティが高い（ミスが少ない）とか、コストがわかるとか、ただ材料によっては有意の順位が変わるのか　裏付けなくやっているのであれば、常に統一すべきだし　統一しないのであれば分岐のルールが必要だし」— the host ruled that executor configuration must either be unified on one form or governed by an explicit branching rule, and that any ranking claim (quality, cost, material-dependent flip) must be evidence-backed.
  effect: Answered from measured data only. Quality: for mechanical units (a deterministic script run per member) the unit's output is byte-identical under any executor by construction, and the five measured quantity-relay defects of this sprint cycle all occurred at agent-boundary prose transcription — so inserting agents into a mechanical path adds relay legs, the one measured defect site, while adding no per-unit quality. Cost: a background shell slice costs ~0 marginal tokens; each sub-agent invocation in this task measured 150k-310k tokens (harness usage stats per notification) — a 6-agent mechanical fan-out would spend on the order of 1M tokens for work shells do for ~0. Material-dependent flip: yes, and the flip condition is the work-type axis the loop already dispatches on (mechanism-class = no LLM judgment inside the unit vs judgment work) — shipped by T-1100 as the verify-axis split. What was NOT covered by any rule: (i) who hosts the shell slices (only measured configuration = coordinating session, harness-tracked, T-1109 completion + T-1110 in flight; sub-agent-hosted = zero runs), and (ii) N-agent parallelism for judgment work (undischarged phase-multiplexing.md:375 — exactly this sprint's T-1111/#398 pilot). Disposition per the host's ruling: unify within the mechanical branch (coordinating-session-hosted tracked background slices, the sole measured form; sub-agent-hosted shell fan-out prohibited until piloted), keep the branch condition itself on the existing work-type dispatch axis, and route the rule's shipping home to issue #399 (this sprint's fan-out default rule-change task) via an issue comment carrying this incident's data, so T-1112 prices it rather than a rule being invented mid-task.
- intervention: human-correction
  date: 2026-08-31
  summary: Fourth message in the same thread — 「簡単に思いつく危険性としては、1エージェント6プロセスの場合は、サブプロセスエージェントのプロセスが障害点になりうる」— the host named the structural risk directly: in the one-agent-six-processes form, the hosting sub-agent's own process is a single point of failure for the whole fan-out.
  effect: Confirmed, and the failure rate of that single point is measured, not hypothetical: the 2026-08-17..08-31 transcript audit recorded six real sub-agent stalls in two weeks (T-1079, T-1083, T-1098, T-1099, T-1100 pm-spec, T-1102 qa-4 — the watchdog-retirement ruling's own data). A sub-agent-hosted fan-out couples that measured stall mode to all six slices at once, and because slices detached inside a sub-agent are harness-untracked, the orphaned work is invisible until someone re-derives it. Under coordinating-session hosting, each slice is an independent harness-tracked process with its own completion notification, so one slice's death loses one slice, not six. This SPOF argument travels with the incident data into the #399 comment and the retro lesson candidate.
- intervention: human-correction
  date: 2026-08-31
  summary: Fifth message in the same thread — 「逆に 6エージェント各1プロセスの場合、オーケストレーター管理が増えるため、コストが増える可能性」— the host named the symmetric cost of the many-agents form: per-agent orchestrator management overhead.
  effect: Confirmed, and the components are each measurable in this session's own records: (1) per-agent token cost 150k-310k (harness usage stats, this task); (2) orchestrator-side cost per agent — briefing authoring, result verification against primary sources, and hand-off reconciliation — scales linearly with agent count, and each additional hand-off is one more relay leg of the measured quantity-defect class (5 defects this cycle, all at agent boundaries); (3) stall-recovery expectation — 6 measured sub-agent stalls in two weeks means an N-agent fan-out prices in verify-first resumes, each a manual orchestrator intervention; (4) orchestrator context accumulation from N result payloads (the safeguard-fallback incident of 2026-08-07 is the measured extreme of orchestrator context tax). Net: the many-agents form pays coordination cost on BOTH sides (agent tokens and orchestrator management) while the mechanical work class gains nothing from agency — reinforcing the unification: shells for mechanical units under coordinating-session hosting; agents only where judgment is required, and then as few as the work demands. Carried to #399 as a pricing addendum.
<!-- END interventions: T-1110 -->
