# Interventions — T-1135

<!-- BEGIN interventions: T-1135 -->
- intervention: assumption-contradicted
  date: 2026-09-12
  summary: the orchestrator's pre-Validate probe of the shipped `claude -p` recipe (Claude Code 2.1.268, one host) contradicted two premises the implementation rested on — `--tools` does not accept permission-rule syntax (`Bash(git diff:*)` there left the pass with no Bash tool at all, so `git diff` could not run), and `--tools` does not exclude MCP servers (every claude.ai connector tool, including write-capable GitHub/Gmail/Slack/Jira tools, stayed exposed); a further probe showed `--allowedTools` alone does not deny non-listed Bash commands under the adopter's own permission settings
  effect: routed back to engineer before qa-verifier was invoked (board `READY_FOR_QA` → `READY_FOR_ENG`, loop-guard iteration unchanged at 0); the corrected shape measured to hold — `--permission-mode dontAsk --setting-sources "" --strict-mcp-config --tools Read Grep Glob Bash --allowedTools "Bash(git diff:*)" "Bash(git log:*)" "Bash(git rev-parse:*)" --disallowedTools Write,Edit,NotebookEdit < /dev/null` (git allowed, `touch` denied by the harness, no MCP tools, cost $0.21 vs $4.29) — is handed to the engineer as measured fact; the class is "a CLI flag's semantics inferred from its help text rather than measured", and the rework requires the shipped line to be run once as shipped
- intervention: assumption-contradicted
  date: 2026-09-12
  summary: QA round 1 recorded `--sandbox workspace-write` and `--sandbox read-only` as "wholly non-functional on this host" (`sandbox-exec: sandbox_apply: Operation not permitted`, reproduced 4x) and walked the live run under `danger-full-access`, leaving runbook state-space row 7 `environmentally-unverified` and row 8 substituted; the orchestrator's own bare-first-token `codex exec --sandbox workspace-write` against the same fixture, minutes later, ran `echo` and `git status --short` normally (0 `sandbox_apply` events) — the same mode had also passed four P0 probes hours earlier
  effect: the environmental claim is attributed to the invocation shape (a `codex` call whose first token is not `codex` runs inside the calling agent's own sandbox, where Codex's seatbelt cannot nest — the documented pitfall), not to the host CLI; qa-verifier is sent back to re-walk rows 7 and 8 with bare invocations under the runbook's literal `workspace-write` configuration and to correct the board record; the deliverable is not implicated and the flag is left at `READY_FOR_REVIEW` pending that addendum
- intervention: human-correction
  date: 2026-09-12
  summary: the operator asked what the orchestrator had been waiting on for three hours with no operator input pending; the orchestrator had armed a background `curl` poll against api.github.com to wait for PR #495's CI conclusion, the sandbox returned an empty body with no error, the `until` loop spun silently and no wake-up fired — CI had in fact passed at 06:36 JST (check run completed, conclusion success), 2.5 hours before the question
  effect: the merge-GO presentation was delayed by the orchestrator's own broken wait, not by the loop; CI state is read through the GitHub MCP tool from here on and a wait with no visible failure branch is not armed again; the GO presentation is issued now
<!-- END interventions: T-1135 -->
