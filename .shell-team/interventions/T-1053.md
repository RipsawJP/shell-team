<!-- BEGIN interventions: T-1053 -->
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: AC10(a)'s frozen mutation-list predicted that duplicating the `BLAST` line a second time inside `agents/pm-spec.md` would redden both AC3 and AC5 ("one mutation, two observers"). Measured on a disposable worktree outside the checkout: AC3 went red as predicted, but AC5 stayed green. `git grep -lF` (AC5's check) lists matching *files*, once per file, regardless of how many times the pattern occurs inside that file — a same-file duplicate is structurally invisible to a file-count check. AC3 independently catches the mutation because it separately asserts the pattern's occurrence *count* (`grep -cxFf`) is exactly 1.
  effect: the underlying safety property (no second copy of `BLAST` ships anywhere) is still caught by AC3 alone, so nothing is broken in practice; the frozen AC10(a) text is not edited by this role. Disclosed in `## Notes for engineer` and here. Retro input: a same-file duplicate is a distinct mutation shape from a cross-file duplicate for any `git grep -l`-based single-copy check, and a mutation-list prediction naming "two observers" for a same-file case should be verified against `-l`'s file-vs-occurrence semantics before being frozen.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: the spec's own pre-measurement note in `## Blast radius` predicted that `T-1052-records-editorial.md`'s AC9 and `T-1051-inspection-ux-polish.md`'s AC12 were "most likely to differ" between the base ref and HEAD, since both pin `agents/pm-spec.md` byte-identical to their own historical base refs and this task edits that file. The full-population diff measured both **already FAIL at this branch's own base ref** (298129c…), for a cause that predates and is unrelated to this task: commit `23908e1` (PR #215, "docs: promote 2026-08-09 retro lesson candidates into the lessons corpus") had already regenerated `agents/pm-spec.md`'s generated prompt-block region before this branch was cut. Neither criterion's verdict actually moves (FAIL at base, FAIL at head). Separately, three criteria the spec's own prediction did **not** name (`T-1020-lessons-supersede-sweep.md` AC14, `T-1041-freeze-ux.md` AC15, `T-1050-check-layer-fast-follow.md` AC5) genuinely flipped PASS→FAIL as a direct, real consequence of this task's edits, and two more (`T-1003-retro-reads-interventions.md` AC19, `T-1004-optin-hook-sample.md` AC2) flipped FAIL→PASS for an unrelated, self-labelled "any diff against develop exists" staleness trigger that this branch trips merely by having commits ahead of `develop`.
  effect: the two predicted rows are recorded in `## Blast radius` as "predicted, not moved" with the actual root cause named and independently verified (`git diff --stat` and `git log` against each spec's own historical base ref); the five rows the diff actually found are recorded as their own rows, three real and two staleness-trigger artifacts, each labelled with its mechanism. No merged criterion is repaired, re-based, widened, or annotated with a `- stale-at:` sub-bullet (DP8). Retro input: a spec author's own by-reading prediction of which merged criteria a change will redden is a useful first pass but is neither necessary nor sufficient — it can name the right file for the wrong reason (both predicted rows here) and miss real, mechanically-different consequences entirely (all three of the newly-found real rows); the full-population diff this task ships is what actually closed the gap, exactly as its own `## Blast radius` prose argues a `grep` inventory could not have.
<!-- END interventions: T-1053 -->
