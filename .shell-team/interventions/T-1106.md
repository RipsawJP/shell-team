# Interventions — T-1106

<!-- BEGIN interventions: T-1106 -->
- intervention: assumption-contradicted
  date: 2026-08-30
  summary: The orchestrator's relay of QA's sweep disposition into pm-spec's table-correction briefing carried two transcription errors from a compressed interim summary instead of the committed addendum - the not-caused tally (relayed 6 = 1+5; the addendum records 7 = 4 runs-corpus + 2 board-checkbox-drift + 1 flaky) and the divergence direction (the six methodology artifacts are base:FAIL to head:PASS, favorable; only the flaky one head-failing). QA's recount caught the tally; pm-spec caught the direction by reading the addendum first-hand rather than transcribing the relay.
  effect: Corrected at spec commit 60e29f6 with the numbers attributed to the addendum's own lines. Retro input - the quantity-claim relay class reproduced at the orchestration layer twice in one task, one turn after the corpus lesson about it was applied downstream: a relay that restates counted values is itself a literal-writing exposure site, and the fix that held was the receiving role reading the primary artifact instead of the relay.
<!-- END interventions: T-1106 -->
