<!-- BEGIN interventions: T-1048 -->
- intervention: assumption-contradicted
  date: 2026-08-08
  summary: AC8's non-vacuity control assumed `git hash-object <file>` hashes raw working-tree bytes; measured on this host (git 2.53.0), a path-form invocation applies the `text`-gitattribute filter by default, so the control's `raw != blob` premise is false and the check line is measured-contradictory with its own prose ("the board's raw hash-object differs from its committed blob"). Verified independently by the orchestrator in a scratch fixture (plain == blob == --path; only --no-filters differs).
  effect: class-M mechanics re-freeze v1→v2 replacing the control's invocation with `git hash-object --no-filters` inside AC8's check line only; the criterion's meaning (DP4's --path choice is observable, with a loud red if the environment does not normalize) is unchanged. Recorded on the board per the standing grant in the operator's CLAUDE.local.md.
- intervention: work-deferred
  date: 2026-08-08
  summary: the engineer drafted a `.shell-team/test-recipe.md` append (documenting the templates/ copy requirement for any scratch bin/ copy and the close-out suite's runtime increase) and reverted it because the file is outside AC16's frozen allow-list.
  effect: deferred to a fast-follow issue to be filed at close-out rather than widening this task's scope mid-flight.
- intervention: work-deferred
  date: 2026-08-08
  summary: codex round 2 found the round-1 Blocker-2 repair itself defective (close-out's `_abspath()` canonicalizes logically while the kernel resolves physically; a mundane symlink+`..` override defeats the divergence check) — the path-identity-divergence class recurred across two consecutive review rounds, firing the spec's frozen DP10(ii) pre-commitment. rework-digest judgment: same-class-repetition; recommended-action leads with reconsider-design-premise.
  effect: DP10(ii) executes without reopening the design — the close-out durability wiring is DROPPED from this task (the recurring class's host surface); the `working-tree-only` opt-out is retained because round 2 independently confirmed its repair sound, so dropping it would shed a healthy surface without closing the recurring class; the core predicate stays at the Implement→Validate and pre-merge seams. Both rounds' close-out findings (override divergence; logical-vs-physical canonicalization) travel to a successor issue as its requirement list, filed at close-out. Requires a class-B re-freeze v2→v3 (AC12, DP11 table, Goal wiring sentence), ratification source: DP10(ii) itself, frozen at v1 and host-GO'd with the sprint plan.
- intervention: human-stop
  date: 2026-08-08
  summary: the operator sent "stop here" while the orchestrator was executing the post-APPROVE recording step (fast-follow dispositions and the READY_FOR_MERGE flip were already recorded and committed; close-out, branch push and PR creation had not yet run).
  effect: loop progression halted immediately after committing the in-flight board record. Both gates are green (QA PASS round 3, codex APPROVE round 3); remaining steps at resume — pre-merge durability check, interventions durability backstop, close-out, push, stacked PR creation (Closes #167) — are recorded in the session hand-off. No further phase was started after the stop.
<!-- END interventions: T-1048 -->
