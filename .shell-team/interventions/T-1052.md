<!-- BEGIN interventions: T-1052 -->
- intervention: assumption-contradicted
  date: 2026-08-09
  summary: the spec's predicted pre-implementation `check-acs.sh` shape listed AC4 as FAIL (classed with the deliverable-absent reds); the freeze-gate live run measured AC4 PASS — pre-implementation T-1034's spec is byte-identical to its base blob, so AC4's invariant comparison (check-line list, criterion count, stale-at count) is trivially green, with its positive controls (non-empty lists, at least one criterion bullet) measured as having fired. Same benign class as T-1050's AC12 prediction mismatch: an invariant-comparison criterion is green before the work starts, not red.
  effect: the measured shape (4 PASS / 11 FAIL / 1 SKIP) is recorded in the freeze attestation with the explanation; the prediction bullet in the spec's Notes for engineer is left as written per the mismatch-is-a-finding rule. No spec byte affected.
- intervention: assumption-contradicted
  date: 2026-08-09
  summary: the v1 freeze-attestation declared nothing structurally unsatisfiable, but AC14's frozen check line is self-contradictory post-commit - its for-loop requires `.shell-team/specs/T-1052-records-editorial.md` in the changed-and-added set while its `sp=` sub-clause forbids any spec-directory diff beyond the two targets, never excluding this task's own spec; the clause was green at freeze only because the spec was still untracked (the tracked-range diff was empty), so the unsatisfiability hid inside a predicted deliverable-absent red. The engineer measured it, disclosed it as a non-blocking FAIL with the reasoning in provenance, and correctly did not escalate (no STOP trigger matched).
  effect: routed to pm-spec for a mechanical v1→v2 re-freeze (add the one missing exclusion; the criterion's meaning is unchanged - the safety property is already proven by the extra= sub-clause, measured 0). Retro input, second occurrence of the class "frozen check line unsatisfiable by construction, missed by the freeze attestation" this sprint (T-1051 AC1's fixture arithmetic was the first): the attestation's satisfiability sweep must evaluate each clause against the POST-implementation tree state (including this task's own records once committed), not only against the pre-implementation tree where deliverable-absent reds mask structural ones.
<!-- END interventions: T-1052 -->
