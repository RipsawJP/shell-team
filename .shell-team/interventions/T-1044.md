<!-- BEGIN interventions: T-1044 -->
- intervention: assumption-contradicted
  date: 2026-08-08
  summary: DP6's frozen prose — "Every conversion uses the in-repo idiom at tests/check-refreeze-class/run.sh:82-87 verbatim in shape — both arms mktemp -d, the TMPDIR/$HERE fallback preserved" — did not survive implementation for three of the twenty files; tests/check-board-headings/run.sh and tests/codex-skeleton-hygiene/run.sh keep a single ${TMPDIR:-/tmp} arm (their scratch roots host git-init repos, which the $HERE arm cannot in this sandbox), and tests/rework-digest/run.sh keeps a single $HERE arm (its fixtures rely on a fixed relative-symlink hop count).
  effect: The engineer shipped the three as single-arm mktemp conversions and grounded each departure in .shell-team/provenance/T-1044.md rather than silently conforming or silently deviating; AC8/AC9/AC11 measure the property the conversion exists for (no fixed scratch roots, mktemp-created) and all pass; QA adjudicates whether the frozen-prose deviation needs a re-freeze or records as a disclosed known deviation.
- intervention: work-deferred
  date: 2026-08-08
  summary: DP11's P1 factual trigger fired on the scratch-lock subsystem — Codex round 1 raised two new independent Majors against tests/scratch-root-hygiene/run.sh (silent-continue on an unreadable tracked file; over-broad line-substring mktemp exemption) and Codex round 2, after the rework, raised new independent Majors against the same subsystem (the exemption fix closed only the literal reproducer, with three executed bypass variants; one of the four frozen non-vacuity controls never matches the FSR signatures) — two consecutive rounds, same subsystem; rework-digest --trigger same-class-2 judged same-class-repetition with reconsider-design-premise as first choice.
  effect: Per the frozen P2 split order (droppable first = the lock suite and its CI wiring), tests/scratch-root-hygiene/run.sh and its workflow step are deferred out of this task into their own follow-up task, carrying both review rounds as design input; the scratch conversion, the retirement claim, the exec-bit rule, the normalizations and the SIGW note stay; pm-spec executes the pre-committed descope as a class-B re-freeze (v1→v2).
<!-- END interventions: T-1044 -->
