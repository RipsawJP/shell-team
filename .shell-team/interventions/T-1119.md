# Interventions — T-1119

<!-- BEGIN interventions: T-1119 -->
- intervention: assumption-contradicted
  date: 2026-09-03
  summary: The coordinating session's probe summary handed to tech-lead (`t1119-inputs/effort-probe.md`) described the provider's rejection of an out-of-set `model_reasoning_effort` value as "one `item.type=error` item" in the `--json` stream. tech-lead re-read the raw stream (`effort-probe-events.jsonl`): the rejection is carried by **top-level** events `{"type":"error",…}` and `{"type":"turn.failed",…}`; no `item.type` key exists on those lines. A criterion frozen against the summary's spelling would never have matched.
  effect: The spec asserts the rejection by the `[ReasoningEffortParam]` / `invalid_enum_value` substring plus the process exit status (and, where a shape is named, the top-level `type` values), never by `item.type`. Lesson instance (same class as T-1118's file-delivery premise): a summary of a machine stream is a claim about bytes — cite the raw line or hand the raw file, not the paraphrase.
- intervention: unclassified
  date: 2026-09-03
  summary: The `pm-spec` authoring run was terminated by the harness at ~14:49Z with `API Error: 529 Overloaded` (server-side; second 529 termination today after the T-1118 engineer's). State measured on the notification: working tree clean, no spec file and no board entry written — the run left no artifact.
  effect: The same agent is resumed verify-first once the API recovers (its transcript is intact); any partial file it wrote is left in place for it to re-read rather than deleted. Recorded per the 2026-08-31 ruling on harness-collected interruptions.
- intervention: assumption-contradicted
  date: 2026-09-04
  summary: The coordinating session's round-1 rework message to pm-spec proposed setting the scratch tree's base directory with `TEAM_RUN_BASE=<scratch>/.shell-team`. pm-spec measured `bin/team-paths.sh:105`–`:133`: an absolute `TEAM_RUN_BASE` is refused (`must be repo-relative, not an absolute path`), so that spelling fails closed instead of redirecting the base; the resolver reads `$BASE_DIR/binding.conf` relative to the process cwd (`:77` `ROOT="."`, `bin/resolve-executor.sh:319`), so the protocol's step 0 is a plain `cd` into the scratch tree with no environment variable.
  effect: The frozen protocol carries the measured form and cites both lines; no criterion was built on the wrong spelling. Lesson instance (third of the class this sprint): an environment-variable contract is a claim about a specific script's parser — read the parser before proposing the variable.
<!-- END interventions: T-1119 -->
