# Interventions — T-1131

<!-- BEGIN interventions: T-1131 -->
- intervention: unclassified
  date: 2026-09-08
  summary: The codex-reviewer sub-agent for review round 2 launched its Codex primary pass as a background job and returned its turn to the coordinating session with the pass still running (the `.codex-capture.*-r2.jsonl` temp under the reviews dir kept growing after the hand-back); a background job inside a sub-agent is not harness-tracked, so nothing would have resumed the role when the pass finished.
  effect: The coordinating session measured the capture still growing, then resumed the role by message with an instruction to wait synchronously (foreground loop until the `-o` file was non-empty) and finish the record in one turn; the round completed ~40 minutes later with APPROVE. No task content changed. Lesson instance already in the corpus/operating notes: Codex review runs synchronously by default; a sub-agent yield on a background job is recovered by resume, not by waiting for a notification that will not come.
<!-- END interventions: T-1131 -->
