<!-- BEGIN interventions: T-1073 -->
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: Relayed premise partially false: the tech-lead's routing map states 8 `agents/*.md` `tools:` lines; the measured count is 9 (`agents/*.md` enumerated at authoring time). The load-bearing half — that none of them carries an `Agent` or `Task` token, so only the orchestrator session can launch a sub-agent — holds across all nine. No criterion changed.
  effect: The spec's Summarized sources and Assumptions carry the corrected count of nine with the full enumeration; the task's structural shape (the probe is an orchestrator step) is unchanged and strengthened.
- intervention: unclassified
  date: 2026-08-15
  summary: The invalid-evidence rule's single permitted re-probe was invoked after engineer round 1 disclosed three structurally unsatisfiable criteria, all three orchestrator-side misses — (1) AC3's positive-control constant (10) contradicted the frozen protocol region's own line count (7), a freeze-sweep post-implementation-satisfiability miss, repaired as a class-M mechanics re-freeze v1→v2 (check line only; the one-word prose correction "ten"→"seven" is a class-B candidate surfaced to the operator, not performed); (2) the orchestrator-authored evidence section did not follow AC4's `- command:` line grammar; (3) the probe ran two watchdog thresholds where the frozen protocol's single-threshold ordering (control-sleep > stall-threshold > unit-duration) admits one.
  effect: Re-probe 1 of max 1, recorded here per the frozen rule: all five arms re-run under a single 300000 ms threshold (control sleep raised to 360000 ms so the ordering 360000 > 300000 > 147968 holds), evidence re-authored with `- command:` lines; a second evidence invalidation stops the task and returns it to planning. Responsibility: orchestration-layer execution and freeze-sweep misses — not the engineer's, whose round-1 disclosure was correct on all three counts and is what surfaced them.
- intervention: unclassified
  date: 2026-08-15
  summary: Engineer round 2 surfaced a home-directory-shaped literal path inside the orchestrator-authored re-probe evidence (the clone command's source argument) — the third orchestrator authoring miss this task, caught by AC13/check-pii-shapes. The engineer correctly escalated whether repairing it counts as a second evidence invalidation under the frozen invalid-evidence rule (which stops the task).
  effect: Orchestrator ruling (AI self-discipline, grounds recorded) — the frozen rule bounds re-PROBES (re-executions of the arms); a redaction-only amendment that replaces one literal path with a placeholder, changes no measurement byte, re-runs nothing, and is recorded with both commits in history is not a re-probe, and the PII-scrub discipline makes the redaction mandatory for a public repository. Applied as a surgical one-string edit to the evidence commit lineage with the board's probe-evidence-sha updated; if QA or the cross-provider review judges this reading wrong, that finding gates and the task stops per the rule.
<!-- END interventions: T-1073 -->
