<!-- BEGIN interventions: T-1112 -->
- intervention: human-correction
  date: 2026-09-01
  summary: The freeze-time version-derivation gate (shipped by T-1110) fired its first live deviation notice: pm-spec's authoring-time derivation and the coordinating session's independent derivation both reached MINOR for T-1112 (#399's default flip falsifies the shipped README opt-in sentence and changes what an adopter feels on the default path), breaking the approved sprint premise (v2.4.1 PATCH, every item internal-mechanism). The notice was issued in the ruled form with a recommendation. The operator ruled — verbatim 「1　ただし、この内容は開始前から変わっていないはずで、今はじめてわかったのはおかしい　レトロで改善してください」— approving continuation with the expected release revised to v2.5.0 (MINOR), and naming the detection timing itself as the defect: #399's content is unchanged since planning, so the MINOR-ness was derivable at planning time, and discovering it only at the third task's spec round is the failure to improve at the retro.
  effect: (1) Premise amended per the ruling: expected release v2.4.1 -> v2.5.0 (MINOR), earned by T-1112's default flip; the remaining backlog items (T-1113 quantity-relay checker, T-1114 scope-lock gap) stay internal-mechanism and are expected to derive PATCH-compatible (their freezes compare against the amended premise). Amendment recorded in the backstage decision file and the sprint memory; every later freeze's version-derivation row anchors premise= to the amended value. (2) Root cause, owned by the coordinating session: the planning-time derivation applied CONTRIBUTING's two tests to the BACKLOG AS A BUCKET ("every item stays internal-mechanism") instead of per item against each issue's own text — #399 part 2's own words ("make parallel fan-out the default") plus the shipped README sentence ("Opt-in only — no phase is rewired to fan out by default") jointly entail the falsification, with no new information needed. The freeze-time gate is a backstop that worked as designed; the primary derivation belongs at planning. (3) Retro lesson candidate, indexed at occurrence: planning-time version derivation runs the joint headline/default-reachability tests PER BACKLOG ITEM against that item's own canon, and any item whose diff surface includes a shipped default, a shipped doc claim, or a README-stated behavior is presumed adopter-visible until derived otherwise; the planning-approval presentation's version-rationale point enumerates the per-item derivations rather than one bucket sentence. Candidate mechanization: fold the per-item derivation table into the planning presentation requirements (operator-owned rule file) — to be proposed at the retro, not adopted unilaterally.
- intervention: assumption-contradicted
  date: 2026-09-01
  summary: Freeze-executor self-inflicted near-miss, twice, self-caught both times: while embedding the sweep result into the spec's Blast radius, the coordinating session's insertion anchored on `s.index()` of the derivation END marker — which matched the marker's MID-LINE quotation inside AC11's own frozen criterion prose first, landing the text inside the intent block; check-intent reported drift immediately on both attempts, the edit was reverted from the committed blob each time, and the third attempt anchored on the marker as a whole line with an asserted-unique match, landing correctly (aligned, 13/13 green).
  effect: Third measured instance of the non-unique-anchor class at the coordinating session's own keyboard (T-1111's intent-hash rindex mis-append; now twice on a quoted marker) — and the second demonstration that the write-then-verify coupling (edit, then immediately run the checker that consumes the region) converts the class from latent corruption into a sixty-second repair; AC11's own prose had even named the exact hazard ("an unanchored search would match the criterion that asks the question instead of the record that answers it"). Rule re-adopted with teeth: every scripted insertion into a board or spec asserts its anchor matches exactly once as a whole line before writing (assert count==1 on a whole-line comparison), never a substring index. Carried to the retro on the same ledger as the prior two instances.
<!-- END interventions: T-1112 -->
