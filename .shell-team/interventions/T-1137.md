# Interventions — T-1137

<!-- BEGIN interventions: T-1137 -->
- intervention: human-interrupt
  date: 2026-09-12
  summary: during T-1137 Specify (pm-spec running), the operator asked why a Codex reviewer agent was left sitting in the session; measured: no `codex` or `claude -p` process alive, all T-1136 capture pairs published, but the completed round-1 `codex-reviewer` subagent entry (T-1136) was still listed by the harness two hours after its completion notification, because the orchestrator consumed its result and never stopped the finished agent
  effect: the finished subagent was stopped after re-verifying its result was committed (review record in `74a45aa`, captures published); no task state changed; the omission is the orchestrator's — stop a reviewer/QA subagent once its completion notification has been verified against its artifacts, rather than leaving the completed entry to linger
- intervention: assumption-contradicted
  date: 2026-09-12
  summary: the sprint plan (approved 2026-09-12 14:29 JST) priced issue #475 as one PATCH task delivering all three of its gaps; pm-spec measured at the primary sources that gap 2 (`team-init` runs `git check-ignore` at init and warns) is forbidden by merged frozen intent — T-1046's Non-goal (spec `:131`) and live-tree AC4 (zero files under `bin/` and `.github/` carrying the token `check-ignore`, re-run by the orchestrator: still zero), and T-1129's Non-goal (spec `:76`, closing the different-subcommand workaround) whose AC3 copies T-1046's line — and that T-1129 was re-scoped away from this exact proposal (issue #452) under the operator's ruling of 2026-09-07; the orchestrator filed #475 on 2026-09-10 without that prior-art read
  effect: option (A) taken on the orchestrator's authority as the disposition consistent with the operator's standing ruling and the PATCH premise — T-1137 ships gaps 1 and 3 (README consequence sentence in both languages; `not-durable` message naming the misconfiguration and the re-include remedy) and refuses gap 2 as a Non-goal locked by two criteria (five files byte-identical to base; T-1046 AC4 re-run verbatim); option (B), a class-B re-freeze of two other tasks' merged intent, is not self-authorized and is surfaced to the operator in the progress report as the only route to gap 2; no new issue is filed for gap 2 — the refusal and its re-evaluation trigger are recorded on #475 at close-out instead of a third issue re-proposing a design ruled out twice
- intervention: work-abandoned
  date: 2026-09-12
  summary: issue #475 gap 2 (init-time `git check-ignore` detection in `team-init`) is dropped from T-1137's scope
  effect: #475 closes partially at T-1137's close-out with a comment naming T-1046 `:131`/AC4 and T-1129 `:76`/AC3 as the ground and "the operator re-ratifies T-1046's Non-goal and AC4 and T-1129's Non-goal and AC3" as the only re-evaluation trigger; the sprint's item 1 (Codex-host adopter run) proceeds with gaps 1 and 3 only, so the operator's own machine still needs the documented `!.shell-team/` re-include before that run
- intervention: work-abandoned
  date: 2026-09-12
  summary: T-1137 AC2 (issue #475 gap 3, the `not-durable` message clause) dropped by the spec's own pre-commitment after its component drew an independent new defect in each of two consecutive rounds (QA r1: cause overclaim for four of five tokens; review r1: T-1048 tail-contract breach, unresolvable-`--ref` case, deleted-after-commit case)
  effect: `bin/check-durability.sh` reverts to its base blob; issue #502 carries AC2's text, the findings and the measured constraints as its requirement list; T-1137 ships #475 gap 1 only; recorded on the board as an executed means-ends disposition (AI self-discipline under the operator's 2026-08-23 loop-escape instruction)
<!-- END interventions: T-1137 -->
