<!-- BEGIN interventions: T-1028 -->
- intervention: assumption-contradicted
  date: 2026-08-04
  summary: The orchestrator relayed the tech-lead's "check-intent.sh has four consumers (run skill, goal skill, qa-verifier, close-out)" into the pm-spec prompt as fact; the pre-freeze sweep's measurement (grep -n 'check-intent' bin/close-out.sh — 0 hits) contradicted the fourth member, and the claim had already propagated into three sites of the spec's frozen region.
  effect: pm-spec corrected all three frozen-region sites meaning-preservingly before attestation (bulk fidelity, zero check: lines moved); the confirmed consumer set (three) is recorded on the board's freeze-run record. Standing correction — the E3 lesson this sprint promoted (re-measure relayed claims against the primary artifact before transcription) applies to the orchestrator's own prompts, not only to retro inputs.
- intervention: human-interrupt
  date: 2026-08-04
  summary: The operator interrupted the qa-verifier launch tool call at the Implement-to-Validate seam, then sent a plain "続けて" (continue) with no redirection.
  effect: The QA launch was re-issued unchanged after recording this entry; no scope, spec or routing change resulted.
- intervention: human-correction
  date: 2026-08-04
  summary: The operator corrected the orchestrator's monitoring posture — agent-watchdog.sh was built precisely because this project's agents (pm-spec among them) stall mid-run, sometimes resuming after greatly exceeding their natural completion time, and a harness completion notification cannot detect that state; the orchestrator had classified the running background pm-spec resume as "watchdog 対象外" and was waiting on the notification alone.
  effect: A watchdog was attached immediately to the in-flight pm-spec v2 re-freeze; the requirement (every background-running agent in this checkout gets agent-watchdog.sh alongside it at launch, notification-waiting is not a substitute) was written into CLAUDE.local.md as a standing local rule; project memory updated to the same effect.
- intervention: human-stop
  date: 2026-08-04
  summary: The operator directed a pause at the next checkpoint — once the Codex round-2 review returned — citing a possible network outage; the review returned APPROVE and the loop stopped there as directed.
  effect: Post-review steps (fast-follow issue filing, READY_FOR_MERGE flag advance, push, PR creation, close-out, next-task start) are deferred until the operator resumes; only local recording (this entry, telemetry, watchdog teardown) was performed at the stop point.
<!-- END interventions: T-1028 -->
