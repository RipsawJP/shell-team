<!-- BEGIN interventions: T-1092 -->
- intervention: human-correction
  date: 2026-08-22
  summary: While pm-spec was authoring this spec, the orchestrator disclosed to the operator that the Routing Map classifies issue #332's own sketch element (the quoted phrase reading that a human approves before implementation enters the loop) as omitted under the standing three-gate ruling. The operator ratified the omission for full-loop autonomous operation AND generalized the requirement - some operating environments impose IT-governance segregation of duties (the human approver must be a different person from the developer), and the loop should let an operator select between an autonomous profile and a governance-controlled profile in a future sprint.
  effect: The selectable oversight-profile capability was filed as issue #343 (not this sprint, not absorbed here); pm-spec was instructed mid-authoring to freeze the omission as a closed, operator-ratified decision citing #343 rather than as a pending disclosure, and the frozen Goal's decision paragraph and Non-goals carry exactly that. The freeze run confirmed #343 exists and is open.
- intervention: human-stop
  date: 2026-08-23
  summary: The spec's pre-commitment fired after review round 2 - rounds 1 and 2 each landed an independent Blocker/Major against the same two components (the close-out teeth gate and the mode's route-back), with the digest reporting same-class-repetition (condition-source-order-dependence x2, guard-anchor-weakness x2). The orchestrator stopped rather than take a third patch round on its own authority and escalated with the digest verbatim, a Japanese reading, and three options; when asked, it also reported that T-1092 itself had zero design re-dos so far and disclosed the cross-task pattern (the condition-source class spans six review rounds across T-1091 and T-1092).
  effect: The operator ruled option (A) - one bounded final fix round covering exactly the four reviewer-verified items (heading-boundary match consistency, here-string in place of the racy pipe, route-back fail-closed restatement plus the field-name correction, and the Preconditions count wording), with review round 3 keeping full adversarial depth (no re-pricing - the teeth are executable code, a different class from T-1091's prose gate). The pre-commitment's prescribed teeth carve-out (option B) was declined as costlier (it would itself require a class-B re-freeze). The cross-task condition-source pattern is recorded as retro input.
- intervention: human-correction
  date: 2026-08-23
  summary: QA round 3's own creative probe (explicitly invited by the orchestrator's brief) found a new variant of the guard-anchor-weakness class outside the operator's bounded four-item scope - a heading with INTERNAL double whitespace as the true-latest spec-review round silently passes because the trim normalizes trailing whitespace only. The orchestrator asked the operator whether to extend the bounded scope by this one item before review round 3, predicting that an undisposed same-class finding would likely trigger a third consecutive stop.
  effect: The operator approved the one-item scope extension (2026-08-23). The fix (whitespace normalization widened to internal runs in the heading comparison, plus a regression fixture) proceeds before cross-provider review round 3; everything else stays within the option-(A) bounds.
- intervention: human-stop
  date: 2026-08-23
  summary: Second stop on this task - QA round 4's own probe found a third distinct guard-anchor-weakness defect in the teeth's section extraction, this time in the dangerous direction (a later unrelated heading with leading whitespace defeats the boundary check, leaking a stray APPROVE into the extracted latest round and producing a false PASS on a genuinely REQUEST_CHANGES record; reproduced). The digest reported guard-anchor-weakness x4 across rounds 1-4; QA assessed the two-discipline hand-rolled scan as possibly at its structural ceiling. The orchestrator stopped and escalated with design-level options.
  effect: The operator ruled option (A) - a design fix, not a patch - rewrite the extraction as a single pass with ONE normalization applied to every line, where "is a heading at all" and "is THE Spec review heading" are both judged from the same normalized view, structurally removing the divergence class (one discipline cannot disagree with itself). The false-PASS reproduction becomes a regression fixture; QA re-probes and cross-provider review round 3 follow. Options (B) teeth carve-out (class-B re-freeze) and (C) continued per-byte patching were declined.
<!-- END interventions: T-1092 -->
