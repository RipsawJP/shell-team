# Interventions — T-1120

<!-- BEGIN interventions: T-1120 -->
- intervention: assumption-contradicted
  date: 2026-09-04
  summary: The coordinating session's Plan briefing to tech-lead stated that a grep for #407's quoted phrase "chosen inside the Validate phase" found no occurrence on the train head, and asked whether item 2 had already been repaired. Measured minutes later against the file's own words: `README.md:174` reads "a per-task choice made inside the Validate phase" and `README.ja.md:174` reads "Validate フェーズの中でタスクごとに明示的に選ぶ" — the issue paraphrased the sentence and the grep used the paraphrase.
  effect: The briefing statement was withdrawn to tech-lead before the Routing Map was returned; #407 item 2 stays in scope. Lesson instance: an issue's quoted phrase is a paraphrase until matched against the file — grep the file's own words (or a stable fragment) before asserting absence.
- intervention: assumption-contradicted
  date: 2026-09-04
  summary: The frozen v1 spec asserted, in AC5's prose, its check line and premise A-9, that the T-1074 sentence `Opt-in only — no phase is rewired to fan out by default` was still present in `README.md` (A-9 placed it on line 180). The engineer measured before hand-off that the full literal occurs zero times at both `HEAD` and the branch-point blob `4f96989`: T-1112 AC8 removed it at `6a625f3`, and the only `Opt-in only` on line 180 is the unrelated concurrent-worktree bullet's own sentence — A-9's measurement matched the substring, not the literal. The freeze sweep (coordinating session's live run plus the Sonnet static read) did not catch it: AC5 was already red for its intended causes, which masked the extra cause, and the sweep's base-side control extraction classified only `.base`-capture reads as controls, while a `HEAD`-side "still present" assertion over untouched text is a base-side control in everything but spelling. Fifth consecutive task with a freeze-sweep miss.
  effect: Engineer stopped `STATUS: BLOCKED` at `4762b0a` without editing frozen text. Class-M re-freeze v2: the false presence clause is removed from AC5 (prose and check line) and A-9 is corrected to record the removal; T-1075 AC5's real constraint — the four forbidden phrases absent — stays asserted. Lesson instance: every `still present` / `remains` / `unchanged` literal over text this task does not write is a base-side positive control and must be run against `git show <base>:<path>` with the full literal, not a substring; a criterion that is already red for its intended cause needs its clauses enumerated one by one, because the intended red masks every other cause.
- intervention: assumption-contradicted
  date: 2026-09-04
  summary: The v2 freeze-attestation named two causes for AC15's red — the v2 intent-hash not yet on the board, and `check-review-input.sh --task T-1120` having no review record to read. The engineer's re-run at `142817f` measured AC15 green before any review record existed: the review-input gate passes without one, so the second cause was asserted, not measured.
  effect: The attestation row was corrected in place (dated parenthetical) rather than rewritten, so the original claim stays legible. Lesson instance: an enumerated red cause is a measurement — run the sub-gate alone before naming it as a cause.
<!-- END interventions: T-1120 -->
