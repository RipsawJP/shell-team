<!-- BEGIN interventions: T-1072 -->
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: The sprint plan and the orchestrator's briefing relayed issue #273's gap 1 as "orchestration/gate spans carry no cost figures — add duration_ms to span rows"; the tech-lead's measurement shows bin/log-run.sh:313 already emits duration_ms on span rows (nullable, documented at L49-51) — the real gap is that EVENT rows (the shape gate transitions are recorded as) carry no cost fields, and span-only keys are doubly forbidden on event rows by log-run.sh's SPAN_ONLY_FLAGS loop and check-run.sh's D2 shape-mixing rule.
  effect: The design question changes from "add a field to span rows" to "emit gate/orchestration work as span rows (recommended) vs relax the event-row shape in two mechanisms (costly)"; the spec carries this as its central D3 decision instead of the issue's original framing.
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: The sprint plan carried "log-run flock" (issue #274's wording) as the fix for the measured duplicate-seq pairs; the tech-lead's measurement shows --seq is a caller-supplied required argument (log-run.sh:185, integer validation only), so the duplicates are a counter-assignment defect that append locking cannot fix — repairing it needs a numbering contract change (a --seq auto shape) that is its own design question.
  effect: T-1072 is split — this car ships the span-schema settlement plus the per-instance discriminator only, and append locking plus seq assignment becomes a separate car in the same sprint (train grows from 6 to 7 cars), sequenced after this one because the contention fixture needs the discriminator vocabulary this car establishes; the new car's issue records the "locking does not fix duplicate-seq" finding so the successor does not re-inherit the false premise.
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: The orchestrator's pm-spec briefing carried two more premises that measurement refuted at spec time — (1) the D6-third backward-compat condition "a span row written without the new flag is byte-identical to a pre-task row" is structurally impossible, because bin/log-run.sh:300-321 always emits every nullable key (a post-T-1058 row carries "provider":null,"effort":null,"adapter":null), and log-run.sh:66-69's own prose claiming that byte-identity is a pre-existing documentation defect; (2) "log-run.sh --help / header docs as the adopter surface" — no --help exists (unknown args exit 2 at log-run.sh:177), so the adopter surface is the README pair.
  effect: AC2 measures frozen-prefix preservation (HEAD row == branch-point writer's row + trailing ,"instance":null after ts normalization) instead of the impossible byte-identity; the user-visible declaration's adopter surface is the README Telemetry bullet pair with an EN/JA mirror invariant (AC13); the stale log-run.sh:66-69 prose is repaired in-task as part of the D2 definition-point sweep.
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: The relayed premise ".shell-team/runs/shell-team.jsonl and close-out.jsonl are git-tracked exceptions to the runs/ gitignore rule" was measured false by the freeze run — `git ls-files .shell-team/runs/` returns nothing; both corpus files are untracked and machine-local, and the corpus is growing live under this session's own telemetry (1,490 relayed → 1,492 rows measured at the freeze probe).
  effect: AC6's check is unaffected (it reads the working tree by design and uses shape-based positive controls that survive growth), but the frozen prose word "committed" in AC6's title and the corresponding Assumptions bullet were corrected pre-freeze to "local" via a meaning-preserving pm-spec edit; a fresh clone will not carry this corpus, so the backward-compat oracle is explicitly this operating machine's, stated as such.
<!-- END interventions: T-1072 -->
