<!-- BEGIN interventions: T-1042 -->
- intervention: assumption-contradicted
  date: 2026-08-06
  summary: The relayed adopter premise (issue #67 comment) that `bin/retro-inputs.sh` resolves `specs` "against the default location rather than through `team-paths.sh`" did not survive tech-lead's re-measurement at `6439eb6` — line 454 reads `TEAM_SPECS_DIR`, set by the resolver invocation at line 372, so the path does go through the resolver.
  effect: The spec corrects the premise instead of absorbing it (D1 discriminates three candidate mechanisms and the relayed symptom pair rules out both code-level candidates); what ships is the measured `.md`-only suffix defect plus the doc-drift repair, while the cwd-relative resolver convention is preserved and locked by AC4 — the relay-re-measurement discipline firing inside a task, recorded for same-class retro counting.
- intervention: assumption-contradicted
  date: 2026-08-06
  summary: At the freeze gate, AC11's authored assumption that a whole-file `--flag` extraction expresses "the script's flag surface" was contradicted by measurement — the extraction would harvest the `git rev-parse --is-inside-work-tree` / `--git-dir` literal that distinguishing D6's N2 outcome class requires, making the criterion mutually unsatisfiable with any conforming Half A implementation (the cross-join sweep class T-1041 mechanised, first live detection).
  effect: Three check lines were repaired meaning-preservingly by pm-spec before attestation (AC11 line-anchored to the parser/help flag surface, AC6 zero-headroom header window replaced with a comment-block walk, AC17 dead `--task` invocation shape collapsed); the freeze recorded v1 only after the repaired lines re-ran 8P/12F with every red failing for the not-yet-implemented reason.
<!-- END interventions: T-1042 -->
