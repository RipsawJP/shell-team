# Interventions — T-1016

<!-- BEGIN interventions: T-1016 -->
- intervention: assumption-contradicted
  date: 2026-08-02
  summary: The pre-freeze live run was attested as having caught every mechanically broken check: line, but the engineer proved AC10's frozen check: line structurally unsatisfiable (its inline spec fixture lacks a Task ID line, so check-intent fails structurally before the blank-line logic is ever reached — the single-hash control can never return 1).
  effect: Engineer reported BLOCKED without touching the frozen region; a human-ratified re-freeze (v1 to v2) with a meaning-preserving AC10 fixture fix is required before QA. The compound-check decomposition gap in the freeze verification (an expected-FAIL line was not decomposed to confirm each control's mechanics) is retro input.
- intervention: assumption-contradicted
  date: 2026-08-02
  summary: The frozen scope-lock allow-list (AC16) was assumed complete, but the D5/D6 edits shift line numbers pinned by tests/errexit-safe/run.sh — a mechanically-coupled companion the corpus lesson on scope-lock allow-lists says must be included from the start — so the full-CI-list obligation and the scope lock contradict each other.
  effect: Engineer kept the scope lock (did not touch the pin registry), left the errexit-safe suite red locally, and recorded the conflict; the v2 re-freeze must add tests/errexit-safe/run.sh to AC16's required list so the pins can be updated and the full CI list can go green.
<!-- END interventions: T-1016 -->
