<!-- BEGIN interventions: T-1114 -->
- intervention: assumption-contradicted
  date: 2026-09-01
  summary: Relayed premise measured false at its primary source (spec `## Assumptions` row A-4): the tech-lead Routing Map, relayed verbatim by the coordinating session into the pm-spec briefing, states that `agents/pm-spec.md` "carries six registered prompt-block marker regions (`templates/prompt-blocks/registry.txt:31,35,37,45,46,51`)". pm-spec re-measured the registry first-hand: those six lines are six registered RULES, of which exactly two are `marker`-mode regions (`:31` language.md, `:51` playbook-pm-spec.md — regions `agents/pm-spec.md:143`–`:230` and `:232`–`:236`) and four are `contain`-mode rules creating no delimited region at all. The briefing's conclusion (both edit sites outside every marker region) is confirmed, but its framing hid the constraint that actually binds: `contain derived-populations.md` is mirrored into line 72, immediately adjacent to the `## Rules` edit site, so a reflow there breaks a `contain` rule while every marker region stays byte-identical.
  effect: AC7 asserts both halves (marker regions byte-identical AND `bin/check-prompt-sync.sh` exit 0) rather than the marker half alone; `## Notes for engineer` Trap 1 names the adjacency. Ledger class note, owned by the coordinating session: this is the NON-NUMERIC half of the quantity/cardinality relay family T-1113 just mechanized for numbers — a cardinality-and-kind claim ("six marker regions") that crossed two relay legs (tech-lead print → coordinating-session briefing) without re-measurement, caught by the receiving role's re-measure-what-you-rely-on discipline. Sixth-plus instance of the relay family this sprint; carried to the retro as evidence that the `- count:` grammar's coverage boundary (numbers only) leaves the kind/cardinality-in-prose half open.
- intervention: assumption-contradicted
  date: 2026-09-01
  summary: Relayed premise measured false at its primary source (spec `## Assumptions` row A-5): the Routing Map directs "For the `- user-visible:` call, read `.shell-team/specs/T-1043-pm-spec-check-conventions.md` — the direct precedent (same file, same kind of norm) — and follow it, stating the reasoning rather than inventing a disposition." pm-spec read T-1043 first-hand: it carries NO `- user-visible:` declaration of any kind — nor any of the other three declaration keys — because it predates the T-1061 gate that introduced them. There is no disposition in it to follow, and reading its silence as an implicit `no` would be the invention the instruction warns against.
  effect: pm-spec honoured the instruction's intent by following the nearest precedents that actually carry the key and are the same file and same norm class (T-1081:40, T-1093:35, both `yes` on the adopter-loaded-prompt ground), declared `- user-visible: yes` discharged by AC8's `- adopter-surface:` pair, and kept T-1043 cited for what it genuinely is (prior art for editing this file's hand-written `## Rules`; evidence the class touches no `bin/`/`tests/`/`.github/`/`skills/`). Ledger class: a precedent relayed by DESCRIPTION rather than by reading, where the described field does not exist in the cited artifact — the citation named a real file but attributed to it a property of the citing party's expectation. Neither this nor A-4 is an issue-intent contradiction or scope change, so no STOP fired; both recorded here per the board entry's owed-record row.
<!-- END interventions: T-1114 -->
