<!-- BEGIN interventions: T-1074 -->
- intervention: human-correction
  date: 2026-08-16
  summary: While this task's engineer was implementing, the operator challenged the sprint's premise chain and a transcript search confirmed the correction. The operator's original request (2026-08-13, verbatim in the session transcript) named "engineer x3 to halve implementation time" as its headline example — Tier-3 work splitting — and the originating complaint (2026-08-12) was the engineer phase's own 3-4 hour turns. T-1069's tier triage classified both of the operator's actual asks (Tier-3, and the depth-axis pipeline) as not-yet and recommended only Tier-1 (verification fan-out) — and that gap was never surfaced back to the operator with their own words, through the v2.1.0 planning approval and into this task, roughly 90 hours of unre-anchored drift. Measured this sprint: the engineer phase is 43.5% of loop wall-clock, the largest single share — the operator's instinct was correct.
  effect: The sprint completes per the operator's instruction, and this task's mechanism ships as specified, but the release execution (tag/publish of v2.1.0) is split out of T-1075 into a separate operator ruling at the batch GO — the operator does not currently see minor-release value in a train whose headline capability does not touch the phase they originally asked about. A re-anchor rule is proposed for the retro: when an investigation's triage classifies the client's own headline example as not-yet/out-of-scope, that classification is presented back at the next human gate with the client's verbatim words; planning approvals gain a fourth required item (origin-request trace). Recorded as the sprint retro's top input; the operator-side memory carries the standing rule.
- intervention: human-correction
  date: 2026-08-16
  summary: Following the re-anchor correction, the operator ruled on versioning itself: work volume and how invasive/destructive the changes were carry no meaning for the client or the user receiving the artifact; the version number must encode how much more useful the deliverable became and how much better it meets needs. The operator directed that a version-numbering rule be established, and added the sharper principle in the same thread: a large invasive change that produces no change for the user is nothing but pure wasted cost — internal work is justifiable only as a traced precondition to a user-felt benefit, named at planning time.
  effect: A value-anchored versioning rule is drafted for CONTRIBUTING.md's release section (MINOR requires an adopter-perceivable benefit reachable in the default shipped experience or closing a documented adopter need; fixes, performance, internal mechanisms and latent opt-in capabilities are PATCH regardless of diff size; releases decouple from merges — trains may merge unversioned and the version is assigned when a value-bearing release ships). Under this rule the current train is PATCH-grade at best until the fan-out capability is reachable and felt. The rule lands through the loop (T-1075's re-scoped car), not as an out-of-scope edit to this task's diff.
<!-- END interventions: T-1074 -->
