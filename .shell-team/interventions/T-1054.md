<!-- BEGIN interventions: T-1054 -->
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: the fixture suite's first draft proved `--verify`'s comment-only-edit tolerance and its `binding-changed` refusal by writing the mutated config to a NEW, differently-named file (`base-commented.conf`, `base-changed.conf`) and pointing `--config` at it, on the assumption that only the CONTENT difference would matter to the assertion under test. Both cases failed with `path-mismatch` instead of the intended `verified`/`binding-changed`: `--verify` checks the `--config` value against the lock's recorded `config-path` before it ever validates that file's content (a deliberate ordering — see this task's own provenance entry on `path-mismatch` ordering), so a differently-named file trips that check first regardless of what its content proves.
  effect: both cases were rewritten to edit `$base` IN PLACE at its original path (comment-tolerance: append a comment, verify, restore; binding-changed: flip one field, verify, restore byte-for-byte before the next case runs), which is the only way to isolate the content-tolerance property from the path-identity property. Retro input: a fixture proving a content-tolerance or content-sensitivity property against a checker that ALSO checks path/name identity must reuse the exact path the reference artifact (a lock, a manifest) recorded, never a copy at a new name — the two properties are independent and a differently-named copy tests the wrong one first.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: the AC7-style CANARY behavioral proof for a backtick-bearing field value (`` `touch CANARY` ``, a beyond-the-criteria coverage case — this repository's `- check:` lines carry no backticks by convention, so the spec itself cannot test this shape) initially assumed any payload string containing shell metacharacters would be refused `bad-token` the same way the spec's own `$(...)`/`;...;` payloads are. Measured: `` `touch CANARY` `` contains an embedded space, so this format's whitespace-delimited grammar splits it into two fields, and the mutated `bind` row now has 7 fields instead of 6 — refused `unparseable-line` (a field-count defect), not `bad-token` (a single malformed token), even though no evaluation occurred either way.
  effect: the fixture was corrected to a single, space-free backtick token (`` `id` ``), which reaches the intended `bad-token` path (a malformed single field) and is now recorded, with the general shape of the trap, in `.shell-team/test-recipe.md`'s T-1054 entry so a later payload-shaped fixture in this suite does not rediscover it. No change to `bin/check-binding.sh` itself: both classifications are members of the closed refusal-token set and neither represents evaluation of the payload, so the underlying safety property (AC7: no CANARY file created either way) held throughout — only the fixture's own token assertion was wrong.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: AC16(a)'s producer-run mutation self-check for "read the adapter registry from the working directory instead of the script's own directory" (a scratch copy outside the checkout, mutating `TEMPLATES_ROOT` to `$(pwd)`) assumed the existing single-row decoy fixture (`tests/check-binding/run.sh`'s `cb-decoy-registry-ignored`, which changes only `pm-spec`'s adapter to the decoy-only token) would go red under this mutation. Measured: it did not. With the mutation active, `tech-lead`'s row (still using the real `claude-cli` adapter, untouched by the mutation's config edit) is validated FIRST against the now-wrongly-resolved decoy registry, which does not recognize `claude-cli` either — so the run still refuses `unknown-adapter`, just for a different row and a different reason, and the case's substring assertion (`grep -qF -- 'unknown-adapter'`) cannot tell the two apart.
  effect: `tests/check-binding/run.sh` gained a second, discriminating case (`cb-decoy-registry-ignored-allrows`): every one of the six rows is bound to the decoy's only pair (`my-cli`/`claude`), so decoy-consulted and decoy-ignored now produce different EXIT CODES (0 vs 1) rather than the same token. Re-run under the same mutation: the new case correctly goes red (exit 0, wrongly accepted). The frozen spec's own AC6 decoy sub-assertion carries the identical single-row construction and the identical blind spot — disclosed here rather than edited, since this role may not alter frozen `- check:` text; the real, shipped checker was never affected (`adapter_known` is still checked in file order exactly as designed), only the ORIGINAL test construction's power to detect a specific class of regression. Retro input: a decoy/negative-control fixture proving "an external source has zero effect" must make every row in the probe consistent with what the external source WOULD legitimize if consulted — a single mismatched row elsewhere in the same config can produce the same refusal token whether the external source was consulted or not, silently vacuating the assertion.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: this spec's own `## Blast radius` prediction section (authored by pm-spec before implementation) grouped four criteria — `T-1051` AC12, `T-1052` AC9, `T-1041` AC15, `T-1050` AC5 — as "already at their T-1053 verdict at this base and expected not to move." The measured full-population diff (AC16(c)) found this correct for the first two (both pin `agents/pm-spec.md`, independently confirmed already broken at this task's own base ref by an unrelated, pre-existing commit `23908e1`, per T-1053's own provenance) but wrong for the other two: both pin `skills/run/SKILL.md`, which — unlike `agents/pm-spec.md` — was untouched at this task's base ref (`develop`'s tip, before T-1053 merges) and only differs once T-1053's own commit `fa7fbd3` (present on this stacked branch, not on `develop`) is included at HEAD. Both flipped PASS(base)→FAIL(head), contradicting the prediction.
  effect: the `## Blast radius` table (a mutable section outside the frozen intent block) records both rows with the precise root-cause commit named, exactly as T-1053's own provenance modeled for its two predicted-but-unmoved rows. No merged criterion is repaired, re-based, widened, or annotated with a `- stale-at:` sub-bullet, per this spec's own Non-goals. Retro input: a spec-authoring-time prediction that groups several criteria under one shared reason should distinguish, file by file, whether that reason (an unrelated prior commit vs. a same-branch stacked commit) actually applies to each file individually — two criteria pinning two different files can share a superficial shape ("byte-identical to a historical ref, expected stable") while resting on very different, individually-checkable premises.
- intervention: human-correction
  date: 2026-08-10
  summary: Codex review round 1 returned REQUEST_CHANGES with one reproduced Blocker — `bin/check-binding.sh`'s `SCRIPT_DIR`/`TEMPLATES_ROOT` bootstrap resolved symlinks on the FINAL path component only (`BASH_SOURCE[0]` itself, via the ported symlink-chasing loop) and used logical `cd && pwd` rather than physical `pwd -P`, so an ANCESTOR directory symlink (an adopter's `bin/` symlinked into the plugin's real `bin/` — ordinary vendoring, no hostile action) was never followed. The reviewer's PoC built exactly that topology and got a six-role, decoy-adapter-only config to validate `exit 0` through the symlinked path — directly contradicting DP6's "never dropped" registry-identity guarantee (`## Refusal matrix`/pre-commitment section) and the identical claim in `templates/binding-adapters.txt`'s own header and on the board. AC6's existing decoy fixtures (`cb-decoy-registry-ignored`, `cb-decoy-registry-ignored-allrows`) only varied invocation cwd against the real, un-symlinked script path — they never exercised a symlinked ancestor directory, so they could not have caught this class.
  effect: `SCRIPT_DIR`, the symlink-loop's `link_dir`, and `TEMPLATES_ROOT` now derive via `cd DIR && pwd -P` (physical) instead of plain `pwd` — the identical fix shape `bin/check-board-headings.sh` and `bin/codex-capture.sh` already use for the same reason, no `cd -P` needed since `pwd -P` alone reports the OS-canonical path regardless of how `cd` arrived there (confirmed: `cd link/sub && pwd -P` and `cd -P link/sub && pwd -P` return the identical physical path). Reproduced the reviewer's exact PoC topology independently (`adopter/bin -> plugin/bin` symlink, decoy registry, all-six-roles config) before and after the fix: pre-fix reproduces `exit 0` (wrongly valid) exactly as reported; post-fix refuses `unknown-adapter`, `exit 1`. A new fixture case (`cb-ancestor-symlink-registry-ignored`, `tests/check-binding/run.sh`) locks this topology against the REAL shipped script (symlinked `bin/` -> `$REPO_ROOT/bin`, not a copy); reverting the three `pwd -P` sites back to plain `pwd` was independently confirmed to redden this exact new case (`expected exit 1 ... got 0`), then restored. Class-swept every other path-resolution site in the script for this same trust boundary: `TEAM_PATHS`/`SELF`/`REGISTRY` are pure string concatenations of the now-physical `SCRIPT_DIR`/`TEMPLATES_ROOT`, not independently vulnerable; `--config`/`--adapters`/`--lock` values are used exactly as the caller gave them and opened via direct kernel path resolution (`[ -f ]`, `<` redirection), which is not the "this script's own location" class at all; the lock's recorded `config-path` is compared as a pure string against the caller's `--config` value and, per DP9, is deliberately never re-resolved as a filesystem path — the opposite of the vulnerable pattern, not an instance of it. `bin/check-durability.sh` shares the identical pre-existing bootstrap shape (confirmed by the reviewer via direct code comparison) but is out of this task's own scope-lock (`AC13`/Non-goals forbid editing any other `bin/` file) and was left untouched; its fix is a fast-follow the coordinating session files as a new issue. Retro input: a symlink-chasing bootstrap ported between scripts should be re-verified against BOTH shapes — final-component symlink (the loop already handles) and ancestor-directory symlink (a plain `cd && pwd` does not) — every time it's reused, not assumed safe because the ported original passed review once.
<!-- END interventions: T-1054 -->
