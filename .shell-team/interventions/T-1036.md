<!-- BEGIN interventions: T-1036 -->
- intervention: assumption-contradicted
  date: 2026-08-05
  summary: The spec's prediction (and the freeze gate's own expectation) was that AC9 flips green once the ledger lines are written; the post-ledger measurement kept AC9 red and the clause-level decomposition found its check line asserting "exactly one intent-hash line matching (v2) board-wide" — contradictory with the board state this train's own approved re-freezes lawfully created (T-1027 v2 via T-1033 and T-1024 v2 via T-1035 already carry (v2) hash records, so the count is 3, permanently).
  effect: The clause is repaired as a class-M mechanics re-freeze (v1→v2) under the standing grant — the fix rewrites only that clause to a base-relative delta (the (v2) count grows by exactly one against the base blob), meaning-preserving with the sibling attestation-count clauses in the same line. The AC9 prose's "exactly one matching (v2)" phrasing is ambiguous under the same reading and is left un-edited to keep the delta check-line-confined; the ambiguity is disclosed to the cross-provider review as part of the mandatory class-M item. Detected because the freeze gate measures both sides of a predicted flip instead of assuming it.
- intervention: assumption-contradicted
  date: 2026-08-05
  summary: The class-M repair route recorded in the previous entry was itself built on a measured-false assumption — the repair clause assumed the base board carried 2 (v2)-shaped hash records (this train's T-1027/T-1024) when the full board history carries 15, the (v3) count was already 1 at base (an older task), and the line's total-count clause greps an unanchored substring that counts prose mentions (68→70) rather than records (anchored shape: 39→40, the correct +1). Clause decomposition found AC9 broken in three clauses with the same three claims frozen in its prose — a clause-only class-M fix would leave frozen prose asserting counts the board contradicts.
  effect: The intermediate clause edit was discarded (spec restored byte-exact to v1, re-verified aligned at 86df188f); the repair is re-routed to pm-spec as a class-B v1→v2 re-freeze proposal fixing AC9's prose and check line together, with the measured board-population facts supplied as inputs. Same lesson as this sprint's T-1024 case at one remove: ledger-accounting clauses over a growing population need base-relative or value-anchored forms, never global "exactly one" literals — and the repair route must be chosen after decomposing the whole criterion, not after the first broken clause found.
<!-- END interventions: T-1036 -->
