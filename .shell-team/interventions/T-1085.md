<!-- BEGIN interventions: T-1085 -->
- intervention: human-stop
  date: 2026-08-19
  summary: The operator instructed an immediate pause of the loop's progression during Implement round 1, minutes after the engineer frame-round agent was launched (freeze v1 and the dispatch-record transcription were already committed at 30460b0 and a60fdad).
  effect: Orchestration halted at that point — no firing, no further phase advances. The already-launched engineer round-1 agent was left to finish its frame round; it then died on an API session limit before writing anything, so the pause landed on a clean tree with zero loss.
- intervention: human-stop
  date: 2026-08-19
  summary: Seconds after the orchestrator resumed the engineer agent post session-limit reset, the operator interrupted the resume and instructed a stop — wrap up the session at that boundary.
  effect: A stand-down message was sent to the just-resumed engineer agent; orchestration halted and session state was consolidated to memory. The agent had by then written a partial note frame and later died on an auth error without committing it (see the work-deferred entry below). Resume point: engineer round 1 continuing from the preserved frame.
- intervention: work-deferred
  date: 2026-08-20
  summary: Engineer round 1 had written a partial note frame (docs/loop-engineering/default-path-firing.md, 103 lines) before the stand-down reached it; the agent then died on an auth error without committing. The orchestrator preserved the untracked file verbatim as WIP commit 7b3826c.
  effect: Round 1 was deferred, to resume from the preserved frame; nothing was lost and nothing was rewritten. The continuation pass later verified the preserved frame faithful and completed the round at 70cd472.
- intervention: human-stop
  date: 2026-08-20
  summary: During the firing's fanned arm (8 qa-verifier instances running), the operator pre-instructed a pause at the boundary before the serial baseline arm starts (F4 in the orchestrator's progress numbering).
  effect: The fanned arm runs to completion and its own bookkeeping is finished (completed-epoch and terminator on the launch record, orchestrator-emitted telemetry span rows for qa-1..qa-8); the serial baseline arm, the aggregation/parity comparison and everything after them are not started. Resume point: the serial arm launch, from the same pinned commit and the already-prepared serial clone.
- intervention: assumption-contradicted
  date: 2026-08-20
  summary: The orchestrator's re-run instruction to instance qa-3 stated as fact that its first k=7 attempt (unit T-1050) "wrote nothing to your part file"; measurement contradicted this — the first attempt had been moved by the harness to a tracked background continuation at the 600 s foreground boundary, completed at launch+1322 s, and appended its own unit/verdict pair, so the ordered re-run produced a duplicate `- unit:` entry for T-1050 in part-qa-3.
  effect: Resolved by deduplication with full disclosure: the pristine pre-correction part file is preserved alongside; the first attempt's pair (whose verdict payload was additionally malformed — grep classified the unit's log as binary and emitted "Binary file … matches" instead of the check-acs summary) was removed, and the re-run's well-formed pair (same rc=1, correct summary, 447.2 s uncontended) was kept. Both attempts' timestamps stay recorded in ts-qa-3.txt. The extraction defect is a class with exactly one carrier (measured across all 8 part files); the serial arm's runner will use text-mode extraction (grep -a) and the note will disclose both the race and the correction.
- intervention: assumption-contradicted
  date: 2026-08-20
  summary: The freeze-attestation (v1) asserted every clause of every check line satisfiable against the post-implementation tree; measurement after the evidence commit contradicted this for AC4 — its positive control requires the extracted firing-protocol region to hold at least 20 non-blank lines, while the frozen region itself holds exactly 16 (14 single-line paragraphs plus 2 markers), making the clause structurally unsatisfiable as frozen. The freeze sweep read AC4's red as pure absence (no probe-evidence-sha yet) and missed the threshold-versus-actual-region-length comparison, which was measurable at freeze time.
  effect: Disposition per the loop's class-M mechanics path (the T-1083 v1→v2 precedent): a meaning-preserving threshold correction to AC4's check line via pm-spec, validated with check-refreeze-class.sh, recorded as a refreeze-class record with the v1 hash retained, attested and re-frozen as v2 under the standing grant. A mechanical threshold sweep over all 18 check lines runs first so the same class cannot survive elsewhere.
<!-- END interventions: T-1085 -->
