# Interventions — T-1105

<!-- BEGIN interventions: T-1105 -->
- intervention: assumption-contradicted
  date: 2026-08-29
  summary: The freeze run executed T-1060's own AC1-AC4+AC7 check lines at this task's branch point and AC4 measured FAIL. Attribution measured, not assumed - the stacked predecessor docs/ja-brushup (PR #384, a standalone docs brush-up run outside the loop) had re-wrapped docs/adopting.ja.md so the frozen anchor 呼び出し経路 spanned a line break, zeroing its contiguous in-section occurrence. Separately, A-5 (T-1020 AC14 green at base) measured false for the same structural reason: the predecessor is a docs branch, so that one-argument develop-relative scope lock is already red at this branch's base. A-6's derivation command was also caught pre-freeze: unscoped git ls-files counted .gitkeep, putting the declared population one above AC18's live *.md count.
  effect: Three repairs, each at its own layer. (1) The anchor defect was fixed where it was introduced - commit c7b9053 on docs/ja-brushup - and this task's branch was re-cut from that tip; T-1060 AC1-AC4+AC7 re-measured all green at the new base. (2) pm-spec corrected A-5 and the blast-radius row for T-1020 AC14 to the measurement (pre-existing red at base, not newly reddened), and the stale ref-value snapshots (A-2, board branch bullet). (3) pm-spec corrected A-6's two --set commands to *.md scope before any population value was written. Retro input: a standalone docs task that skips the loop also skips the full-population sweep that would have caught (1) at its own merge point - the freeze sweep of the next stacked task caught it instead, which is the mechanism working but one task late.
<!-- END interventions: T-1105 -->
