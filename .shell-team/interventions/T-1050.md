<!-- BEGIN interventions: T-1050 -->
- intervention: assumption-contradicted
  date: 2026-08-08
  summary: the T-1050 board entry's predicted pre-implementation `check-acs.sh` shape listed AC12 as FAIL (classed with the deliverable-absent reds); the freeze-gate live run measured AC12 PASS — pre-implementation every target spec is byte-identical to its base blob, so AC12's count-equality comparison is trivially green, with its `nb >= 7` positive control measured as having fired (not a vacuous pass).
  effect: the measured shape (3 PASS / 13 FAIL / 1 SKIP) is recorded in the freeze attestation and its measurement sub-bullet; the prediction bullet is left as written, since the entry's own rule is that a mismatch is a finding to record rather than a record to repair. No spec byte is affected — the prediction sits on the board, outside the intent block.
<!-- END interventions: T-1050 -->
