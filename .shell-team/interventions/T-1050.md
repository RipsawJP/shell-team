<!-- BEGIN interventions: T-1050 -->
- intervention: assumption-contradicted
  date: 2026-08-08
  summary: the T-1050 board entry's predicted pre-implementation `check-acs.sh` shape listed AC12 as FAIL (classed with the deliverable-absent reds); the freeze-gate live run measured AC12 PASS — pre-implementation every target spec is byte-identical to its base blob, so AC12's count-equality comparison is trivially green, with its `nb >= 7` positive control measured as having fired (not a vacuous pass).
  effect: the measured shape (3 PASS / 13 FAIL / 1 SKIP) is recorded in the freeze attestation and its measurement sub-bullet; the prediction bullet is left as written, since the entry's own rule is that a mismatch is a finding to record rather than a record to repair. No spec byte is affected — the prediction sits on the board, outside the intent block.
- intervention: assumption-contradicted
  date: 2026-08-09
  summary: DP8 named T-1040 AC13 and AC14 as the two merged criteria this pass perturbs by construction; the engineer's blast-radius differencing measured AC13 red exactly as predicted, AC14 NOT red (prediction contradicted in the safe direction), and T-1040 AC17 red UNPREDICTED — caused by T-1050's own new spec file matching AC17's population-scan pattern, found only because the methodology differences base against HEAD instead of asserting the predicted set.
  effect: all three deltas are disclosed in the spec's `## Blast radius` table with reasons and none is repaired (R3 forbids chasing a merge-point-scoped red); recorded here because a measurement contradicting a stated prediction is this file's trigger, same class as the freeze-gate AC12 entry above.
- intervention: work-deferred
  date: 2026-08-09
  summary: codex round 2 lifted the round-1 veto but found one new Major — T-1050's own Blast radius omits the T-1047 AC11/AC15 flips (team-paths.sh indirection blind spot, second cumulative occurrence of the blast-radius-disclosure-gap class after QA round 1's T-1043 AC12). loop-guard at iteration 3 printed STOP:max_iterations_reached, so the round-3 rework is not routed.
  effect: task held at REWORK; rework-digest (trigger same-class-2, judgment same-class-repetition) escalated to the operator verbatim for the extend / premise-change / drop decision. The candidate premise change on the table is replacing the heuristic grep-literal inventory with full-suite differencing across all merged specs at base and HEAD, which is complete by construction.
<!-- END interventions: T-1050 -->
