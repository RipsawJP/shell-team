# T-1087 intervention capture

<!-- BEGIN interventions: T-1087 -->
- intervention: assumption-contradicted
  date: 2026-08-22
  summary: the tech-lead routing map's `## Predicted inherited-criterion profile` names only T-1090, T-1089 and T-1086 as the downstream inventory to run; this role's own authoring-time read of `.shell-team/specs/T-1079-tier2-judge.md`'s AC1–AC4 found that spec's criteria scope their own reads to `docs/loop-engineering/phase-multiplexing.md` by name — the very file this task's region instantiates — so the routing map's list undershot the real read set by one spec. The spec's own `## Assumptions` section already records this as "a finding about the hand-off, not a silent correction" at authoring time.
  effect: T-1079 was added to `## Predicted inherited-criterion profile` and to the Blast radius indirection-discharge list at spec-authoring time, before implementation began, so the engineer's own AC9 item (b) sweep ran T-1079's full criterion set at both refs from the start rather than discovering the gap during implementation.
- intervention: assumption-contradicted
  date: 2026-08-22
  summary: the spec's own reasoned Blast radius discharge list names four specs (T-1090, T-1089, T-1086, T-1079) as the read set to run in full; running the spec's own prescribed mechanical derivation (`bin/derive-populations.sh` over one `git grep -l` per path this task edits or names, against `.shell-team/specs/`) at hand-off surfaced twelve additional merged specs referencing `docs/loop-engineering/phase-multiplexing.md` by path (T-1069, T-1072, T-1073, T-1074, T-1075, T-1077, T-1078, T-1080, T-1082, T-1083, T-1084, T-1085) that the reasoned list did not name — a real read set of sixteen specs, not four.
  effect: per the spec's own instruction ("the reasoned four-spec discharge list above is reconciled against it, with any spec the derivation adds run in full too"), all sixteen merged specs' full criterion sets were run at both the branch point and HEAD for AC9 item (b), not only the four the routing map and the spec's own reasoning named; the engineer hand-off reports every one of the sixteen individually rather than trusting the narrower reasoned list.
- intervention: human-interrupt
  date: 2026-08-22
  summary: While the engineer was implementing on this branch, the T-1090 codex-reviewer agent - resumed by stale background-task notifications after its verdict had already been committed and processed - wrote a review addendum commit into the shared checkout without noticing the checkout had since switched to feature/1087-judge-briefing, landing the stray commit 6f541c1 directly on top of this task's frozen-spec commit dbb0b8c. The reviewer then held its own write boundary: it stopped, refused to checkout/cherry-pick/reset on its own authority, and reported to the coordinating session.
  effect: The coordinating session recovered inside the clean-tree window before any engineer commit landed - cherry-picked the addendum onto its correct branch in a separate worktree (feature/1090-sign-safety-close, commit de79795, one board-line conflict hand-resolved, pushed, PR #337 updated), verified the preservation twice over (cherry-picked copy plus reflog), then reset this branch to dbb0b8c with the engineer's untracked deliverable untouched. Zero work lost; the merge-base every base-reading criterion resolves stayed 65b1b8d throughout. Retro input: a stale-notification resume revives an agent's write access after its mandate completed, and a shared checkout makes the current branch a race - the known writer-reader-one-worktree class in a new firing shape.
<!-- END interventions: T-1087 -->
