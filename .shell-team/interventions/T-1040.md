<!-- BEGIN interventions: T-1040 -->
- intervention: assumption-contradicted
  date: 2026-08-06
  summary: The coordinating session labelled the four issue bodies relayed into pm-spec's task prompt as "verbatim", and the spec's Assumption 4 carries that label. Re-reading the sent prompt against the API-fetched bodies at the freeze gate measured the label wrong in the strict sense — several context parentheticals were trimmed in the relay (from #155, "(`sites:`/file-count identities)" and "(classification class-b, GO grounds, shipped shape)"; from #157, "filed as the class per the same-class discipline", the sentence recording that T-1036's v5 re-freezes eliminated the reachable instances "hence Minor, deferred", and "none blocks the current train (round-2 P2 judgment: threshold not fired)"; from #159, "(the same scheduling shape #135 itself used)" and the operator-behaviour detail inside the conservative-failure-direction parenthetical). Checked fragment by fragment: every trimmed piece is rationale or context; no repair ask, site, route or scope element was dropped, and pm-spec independently rebuilt the substance from primary records (its Assumptions 6-8 and DP2 corrections all came from the records, not the relay).
  effect: Recorded rather than silently absorbed, because this task's own N1 rule is that an unlabelled-or-mislabelled relay is the defect class. Assumption 4's dependent clause ("if a filed body asks for something this spec declines, raise it before the freeze") was re-evaluated against the true verbatim bodies: nothing the bodies ask for is declined beyond what the spec already declares (T-1035 AC8 weighing, per-line-uniqueness deferral), so the freeze proceeds. The reviewer receives this record as part of the AC20 (e) per-source report; the tech-lead's copy of the relay was checked and is verbatim.
<!-- END interventions: T-1040 -->
