<!-- BEGIN interventions: T-1109 -->
- intervention: assumption-contradicted
  date: 2026-08-30
  summary: assumed the `xargs -P`-style fan-out sweep pattern `.shell-team/test-recipe.md`'s T-1084/T-1083 entries document for a full-population Blast-radius sweep would transfer safely to this task's own AC14 sweep. Measured directly: an 8-way parallel batch of `bin/check-acs.sh` runs across distinct specs stalled for several minutes on a batch containing `T-1058-telemetry-binding.md`, whose AC1 invokes `bin/log-run.sh` multiple times; a foreground re-run of that same spec alone, started while the parallel batch was still contending for the same run, took roughly 8 minutes to return a result it produces in seconds when nothing else is running concurrently against the shared directory lock `bin/log-run.sh` (and other telemetry-touching specs' checks) take. This machine also has no `timeout`/`gtimeout` binary on `PATH`, so `bin/check-acs.sh`'s own `CHECK_ACS_TIMEOUT` wrapping is inert here (its documented fallback: "back to a plain run") and cannot bound a genuinely stuck check.
  effect: abandoned the 8-way parallel driver for this task's AC14 sweep (left running in the background rather than force-killed, since no process-management tool was available to this role) and replaced it with a strictly serial driver — one spec's full `check-acs.sh` run at a time, with a hand-rolled wall-clock timeout (`sleep N && kill`, since the `timeout` binary is unavailable) bounding each individual spec run instead of relying on the script's own inert internal timeout. This is a work-abandoned/methodology-correction event about the SWEEP TECHNIQUE only; it does not affect the correctness of any acceptance criterion.
<!-- END interventions: T-1109 -->
