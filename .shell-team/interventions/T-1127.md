# Interventions — T-1127

<!-- BEGIN interventions: T-1127 -->
- intervention: assumption-contradicted
  date: 2026-09-06
  summary: The coordinating session's freeze-attestation line (`736fcba`) stated that **AC5** (scope lock) was green at the pre-implementation tree "by construction"; the live `check-acs` run recorded in the same line said `2 passed, 3 failed` with AC5 among the reds — at base the changed set is empty, not the two history files the criterion asserts. The attestation prose was written from an expectation of the criterion's shape, not from the run's own FAILED list one line above it.
  effect: Corrected in `e9fc79e` before the engineer started (AC5 recorded as red at base by design). The event was not ledgered here at the time; Codex review round 1 (`cef2e0b`) measured the gap and this entry records it. Lesson instance: an attestation's per-criterion green/red list is copied from the checker's own FAILED line, never inferred from what the criterion "should" do.
<!-- END interventions: T-1127 -->
