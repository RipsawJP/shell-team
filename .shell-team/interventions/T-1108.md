<!-- BEGIN interventions: T-1108 -->
- intervention: assumption-contradicted
  date: 2026-08-30
  summary: Acceptance criterion AC4's frozen check line assumes the checkbox-line marker `**ACn**` begins at column 6 (`index($0,p)==6`) when extracting a referenced criterion's check body from a population spec; measured directly against the universal `- [ ] **ACn** …` format (e.g. `.shell-team/specs/T-1066-effort-time-telemetry.md:91`, and this spec's own `AC1` line), the marker actually begins at column 7 in every instance, with no exception found.
  effect: AC4's extraction step finds zero check lines for all five population members regardless of whether the staging step exists, so the criterion is unconditionally unsatisfiable as frozen — independent of the correctness of this task's implementation. Per the intent-block freeze boundary, the engineer cannot correct the frozen check line; the finding is escalated in the hand-off as a stop-and-report item rather than worked around, alongside an out-of-band confirmation (in a disposable scratch copy with the constant corrected to 7) that the staging design and this task's prose changes make the paired control pass cleanly once the constant is fixed.
<!-- END interventions: T-1108 -->
