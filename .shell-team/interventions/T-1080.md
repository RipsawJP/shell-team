<!-- BEGIN interventions: T-1080 -->
- intervention: assumption-contradicted
  date: 2026-08-17
  summary: The frozen AC5 check line's premise "the literal `both-gates-green` occurs zero times in `skills/run/SKILL.md` at the branch point" was contradicted by measurement — the engineer measured `git show cf643a9d…:skills/run/SKILL.md | grep -c both-gates-green` → 2 (pre-existing occurrences from already-merged T-1074/T-1077 fan-out work, unrelated to this task), and the orchestrator reproduced the same count independently before acting. The criterion is unsatisfiable by any implementation choice; the engineer escalated as BLOCKED rather than working around a frozen line.
  effect: A v1→v2 re-freeze repairing AC5 — the absolute zero-count-at-base premise replaced by a base-relative delta (HEAD count = base count + 1, base read from the blob) plus a step-6-region clause, and the criterion's prose corrected to match its repaired check. Classified **class-B, not class-M**, by `bin/check-refreeze-class.sh` (run live by the orchestrator: exit 1, first differing non-check line at intent-block index 80), because the delta touches criterion prose alongside the check line; the prose repair was taken deliberately rather than leaving frozen prose asserting a premise its own check no longer makes. The task's intent is unchanged and no other criterion moves.
- intervention: work-deferred
  date: 2026-08-17
  summary: The spec's frozen pre-commitment's factual trigger fired — two consecutive Codex review rounds landed an independent Major against the same component, surface S7 (round 1: the deferral covered the reviewer's record-commit exception but not its untracked review-record writes; round 2: the round-1 fix's `$TMPDIR` buffering is unreconciled with `bin/codex-capture.sh`'s fail-closed allocation/publish paths, verified in the script's code by the reviewer). The orchestrator attested the trigger against both review-round records before executing, and did not weigh the cheapness of the remaining fix (`--reviews-dir` is one flag away) as grounds to continue — the disposition was decided at freeze time precisely to remove that in-the-moment judgment.
  effect: Drop ① executed as frozen — S7's contract text removed from `agents/codex-reviewer.md`, `agents/qa-verifier.md` and `skills/run/SKILL.md`; both rounds' findings travel to follow-up issue #309 as its requirement list; the spec's mutable `## Pre-commitment disposition record` gains the conformant `- dropped-surface: s7 — ` line, which waives AC9's positive obligations without a re-freeze (the waiver wiring was probed both ways by QA round 1's AC19(a)). Surfaces S1–S6 and the three errata stand.
<!-- END interventions: T-1080 -->
