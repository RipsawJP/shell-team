<!-- BEGIN interventions: T-1058 -->
- intervention: assumption-contradicted
  date: 2026-08-11
  summary: the v1 freeze attested the Non-goals claim "no binding value reaches the git-tracked roll-up artifact" (and DP7's fixed-field assertion); QA round-1 reproduced the contradiction live — rollup-track.sh's verbatim-capture wrapper design carries the new providers:/review: lines into the tracked artifact with the script itself byte-untouched, exactly the future item the spec's own Notes mis-assumed had not happened yet
  effect: class-B re-freeze v1->v2 routed to pm-spec to decide between correcting the frozen prose (accepting the capture, with the closed-vocabulary content argument recorded and pinned) or excluding the two lines at the track layer; the sweep's own gap (attesting a Non-goal without decomposing it against the wrapper design its own Summarized sources described) recorded here
<!-- END interventions: T-1058 -->
