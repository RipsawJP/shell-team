<!-- BEGIN interventions: T-1055 -->
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: the two shipped adapter definitions were first authored with visually-aligned `carries <field> <channel>` rows (extra internal whitespace between the field and the channel, matching this repo's common column-alignment convention), on the assumption that the checker's `read -r -a f` field splitting would make alignment cosmetic. AC4's own `- check:` line asserts field coverage with `grep -cE "^carries $fl [a-z][a-z0-9-]*$"` — a single literal space on each side, not a whitespace class — so the aligned rows measured zero matches and AC4 failed live.
  effect: reformatted both `templates/adapters/claude-cli.txt` and `templates/adapters/codex-cli.txt` to single-space-delimited `carries` rows (no column alignment); re-ran `bash bin/check-acs.sh` and confirmed AC4 (and every other criterion touching these files) green. `bin/check-adapter.sh`'s own parser is unaffected either way (it reads fields generically via `read -r -a f`, tolerant of any amount of whitespace) — only the frozen `- check:` line's exact-single-space grep is what makes source-file alignment a real, load-bearing constraint on the two shipped definitions. Retro input: when a frozen acceptance criterion's own `check:` line greps a shipped data file with a fixed single-space pattern, author that file's rows single-space-delimited from the start rather than assuming a tolerant parser downstream makes cosmetic alignment safe — the criterion's own regex, not the checker's parser, is what a source file must satisfy.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: producing this task's `## Blast radius` full-population diff (AC16(d)) required running `bash bin/check-acs.sh` against all 60 live specs and all 57 base-ref specs (a disposable `git worktree add --detach`), each run taking tens of minutes end to end. The first attempt to background the live-side scan used a manual `... &` shell backgrounding form (which itself printed a sandbox warning, `nice(5) failed: operation not permitted`, but appeared to have started); a second attempt then invoked the identical script again through the harness's own auto-backgrounding after the first call's tool-level timeout elapsed, on the assumption the first attempt had not actually started a competing process. Measured: the resulting `head.tsv` carried every AC-verdict line for several early specs (e.g. `T-1004-optin-hook-sample.md`) duplicated exactly twice, with no duplication for later specs — the signature of two concurrent instances of the same script racing to truncate-then-append the same output file, not a bug in the extraction logic itself.
  effect: discarded the corrupted `head.tsv`, re-ran the live-side scan exactly once (via the harness's `run_in_background` parameter, confirmed as the sole invocation, actively polled to completion rather than waited on a completion notification) into a fresh file, and confirmed zero duplicate lines and the expected 60-spec/1044-line total before using it for the diff. The base-side scan (`base.tsv`) was independently checked for the same defect and found clean (a first attempt was genuinely killed by the tool's 2-minute timeout at 83 lines, with no evidence of a second concurrent instance, before a clean single re-run produced the full 1001-line result). Retro input: a shell-level background attempt (`&`) that itself surfaces a sandbox permission error is not necessarily evidence of a failed launch — verify a single instance is running (or, since process inspection is itself sandboxed here, verify the OUTPUT file for row-count doubling) before layering a second launch of the identical long-running script on top.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: (round 2 / v2 rework) the canon region's marker placement was first authored with the two marker HTML comments OUTSIDE the fenced code block (```` ``` ````) — `<!-- BEGIN contract-canon -->` then the fence then the content then the fence then `<!-- END contract-canon -->` — on the assumption this satisfied the spec's Notes-for-engineer instruction to "put both markers inside the fenced block." Measured: `bin/check-adapter.sh --doc` immediately refused `doc-drift` against the freshly-authored, unmutated document, because the `awk` extraction between the two marker LINES captured the fence lines (```` ``` ````) themselves as part of the "canon region," which are absent from `--print-contract`'s own output.
  effect: swapped the order to fence-then-BEGIN-marker ... content ... END-marker-then-fence, so both markers sit strictly between the two fence lines (i.e. inside the rendered code block) and the `awk` extraction — which runs between the marker lines, not the fence lines — captures exactly the canonical content with no fence lines mixed in. Re-ran `--doc`: passes clean. Retro input: "put marker X inside delimiter Y" is ambiguous between "X sits inside the region Y visually renders" and "X sits inside the byte range the automated EXTRACTION of X's own pair reads" — when authoring a marker-pair-plus-surrounding-delimiter shape a checker will programmatically extract between the markers (not the delimiter), verify the extraction's own byte range experimentally on the first draft rather than reasoning about the two nested boundaries in the abstract.
- intervention: assumption-contradicted
  date: 2026-08-10
  summary: (round 2 / v2 rework) the `--binding` mode's re-asserted canonical-output shape check (R6) was first written on the assumption that `bin/check-binding.sh --print-binding`'s output is exactly six `bound <role> ...` rows and nothing else, so the parsing loop refused any non-`bound` first token immediately as `binding-invalid`. Measured: running it against the REAL sibling script and a genuinely valid config produced `binding-invalid: unexpected canonical binding row shape: schema 1` — the canonical output's own DP7-documented shape (in `.shell-team/specs/T-1054-binding-config.md`, already summarized in this spec's own `## Summarized sources`) is a leading `schema <version>` line FOLLOWED BY six `bound` rows, which this task's own AC7 stub fixture (`printf "schema 1" "bound ..." "bound ..."`) already modeled correctly — the bug was in the PRODUCTION code, not the fixture.
  effect: the parsing loop now recognizes exactly one leading `schema` line (refusing `binding-invalid` if it is not first, or if a second one appears) before the six `bound` rows, matching `bin/check-binding.sh`'s own documented canonical form exactly. Re-verified against the real sibling (both the shipped specimen and a hand-built six-role config) and against the AC7/suite stub fixtures (which already carried the `schema` line and therefore continued to pass unmodified). Retro input: when re-implementing a shape check against another component's OWN documented output format, re-read that format's full grammar (here, `schema` line + N data rows, not just "N data rows") before writing the parser, rather than inferring the shape from the one row type the new check cares about.
- intervention: work-abandoned
  date: 2026-08-10
  summary: (round 3 / v3 rework) round 2's Codex cross-provider review returned REQUEST_CHANGES a second consecutive time against `bin/check-adapter.sh` — 1 new Blocker + 6 Major (`.shell-team/reviews/T-1055.md`, `## Round 2`) — which is exactly this spec's own pre-commitment's factual trigger ("two consecutive rework rounds of new independent Blocker/Major findings against one component"). The recorded disposition (v2's `## Notes for engineer`) was executed rather than reopened: droppable item (1), the `--doc [PATH]` mode, was carved out of this branch entirely to successor issue #224 (named on the board's v2→v3 intent-ratification line and in the v3 spec's `## Notes for engineer`), rather than patched a third time.
  effect: deleted from this branch (v3, this round): `bin/check-adapter.sh`'s `--doc` flag and its argument parsing, the `doc` mode's dispatch arm, the `doc-drift`/`doc-unreadable` refusal tokens (including their rows in the script's own header refusal-matrix comment), every `tests/check-adapter/run.sh` fixture case exercising the mode (`ca-doc-valid`, `ca-doc-unreadable`, `ca-doc-drift-direction`, `ca-doc-drift-requiredness`, `ca-doc-drift-required-when`, `ca-doc-drift-canon-line` — 6 cases; suite total measured 88 -> 82 after removal, 91 after this round's 9 new V1-V4 cases), and the `.github/workflows/check-handoff.yml` `--doc` dogfood step. What travelled to #224: round 2's two `--doc`-specific findings (a zero-match `grep` under `set -euo pipefail` aborting with a bare unclassified exit 1; a `head -n1` per-field comparison never reading a second contradicting bullet) plus DP16 v2's original mode design, as the successor's requirement list. What was NOT dropped, and is retained in this branch instead: two-form agreement between `docs/loop-engineering/task-envelope.md` and `templates/task-envelope.txt` is still required and still checked, by the spec's own AC8 performing the comparison directly at spec-check time (canon-region byte equality, per-field direction/requiredness tuples, exactly-one-bullet-per-field) rather than by a standing CI-gated flag of this script — the stated, in-advance-named loss is that this comparison is checked when the spec's criteria are checked, not on every CI run. Verified: `git grep -l -e '--doc' -- 'bin/*.sh' 'skills/*/SKILL.md' 'agents/*.md' 'tests/*/run.sh'` returns zero files (AC12's widened absence clause); `bash bin/check-acs.sh .shell-team/specs/T-1055-adapter-envelope.md` shows 17 PASS / 0 FAIL / 1 SKIP (AC17) post-removal.
<!-- END interventions: T-1055 -->
