# Interventions — T-1129

<!-- BEGIN interventions: T-1129 -->
- intervention: assumption-contradicted
  date: 2026-09-07
  summary: The sprint planning row for this item ("an earlier warning for an already-documented trap; PATCH; ~3 h") and issue #452 were written without the prior art: #167 (closed 2026-08-09, completed) and the merged frozen spec T-1046, whose Non-goals forbid "any mechanism for asking git this question inside the plugin", whose AC4 keeps `check-ignore` out of `bin/` and `.github/`, and whose defeat vector 6 shows a directory-form ignore query is blind to `.shell-team/*.md` and repo-wide `*.md` rules. pm-spec measured this first-hand and refused to freeze (flag `BLOCKED`, 0 ACs). The coordinating session filed #452 after a topic search for a different subject only and never searched for the ignored-base history.
  effect: Freeze refused; task returned to the operator as a premise break (the approved estimate no longer holds). Two relayed premises corrected on the board by pm-spec: the no-git-claim site count is 6 not 5 (`docs/adopting.ja.md:415`), and the routing map's "tracked base dir ⇒ no warning" decision is unverified. pm-spec's "#167 still open" was also wrong: #167 is closed as completed. Lesson instance: an issue filed by the coordinating session is searched for prior art on its own subject before it becomes a planning row.
- intervention: work-abandoned
  date: 2026-09-07
  summary: The git-query design of issue #452 (ask git whether the base dir is ignored, warn if so) is abandoned by operator ruling "C" (2026-09-07), chosen over "A" (withdraw, ship nothing) and "B'" (re-scope the query onto the record files and amend T-1046 by class-B ratification). C replaces the mechanism with one static line in `team-init`'s closing summary pointing at the documented remedy, so no git call is added, T-1097's git-free invariant and T-1046's AC4 stay green, and the ordering problem (docs read after the symptom) is addressed without a detector.
  effect: T-1129 is re-scoped in place — pm-spec re-authors the spec for the static line; issue #452 is annotated with the prior art and the re-scope. The sprint row for this item is re-derived under the same tier (PATCH; no new adopter capability) with the estimate revised to about 1 h at 0.8.
<!-- END interventions: T-1129 -->
