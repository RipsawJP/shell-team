<!-- BEGIN interventions: T-1111 -->
- intervention: assumption-contradicted
  date: 2026-08-31
  summary: Relayed premise measured false: the Plan-time premise table nominates `harness-agent-concurrency.md:374`'s unobserved plugin-role boundary as T-1111's subject; that boundary was crossed by T-1083 (`plugin-role-n2`, `plugin-role-n4`) and replicated by T-1085 (N=8), and `:374` is itself a stale surface T-1083's own follow-up asked a later maintainer round to repair. The genuinely open residual is the declared-label-versus-resolved-type gap both notes disclose with an explicit counterfactual. The spec is re-scoped accordingly and held at DRAFT/`BLOCKED` pending the disposition in `## Open questions`.
  effect: pm-spec stopped rather than proceeding on its own authority (issue-intent contradiction + scope change = standing stop-and-raise classes) and issued the ruled deviation-notice form with three contentful options and a recommendation. Layered-relay accounting, owned by the coordinating session: the falsification chain now has three strata — (1) r3-start's whole ":375 open" narrative (retro Try 2, issues #398/#399, sprint planning, and two operator interventions) ran while `harness-agent-concurrency.md` (2026-08-15) already recorded the general-purpose verdict; (2) the coordinating session's premise correction found stratum 1 but stopped there, nominating the plugin-role boundary as open; (3) tech-lead verified stratum 2's citations at their named lines but did not find `agent-launch-fanout.md`'s superseding arms, because the stale surface (`:374`) is exactly what all three readers anchored on. Three successive readers stopped at three different depths of the same supersession-declared-not-performed chain — the strongest data yet that declared-but-unperformed supersession is a defect class, not a convention; carried to the retro and into T-1111's repair scope whichever disposition the operator selects.
- intervention: work-deferred
  date: 2026-08-31
  summary: Second occurrence of the same coordinating-session relay omission: the Routing Map's printed `spec-review` decision was again not carried into the pm-spec briefing (first occurrence: T-1110, where the omission forced a tech-lead re-print at the freeze gate). pm-spec flagged the gap and emitted `cross-provider` from the axis rule's own trigger rather than assuming a value.
  effect: No divergence this time — tech-lead's Plan-time print for T-1111 is `spec-review — cross-provider — conditional — cost-input: t1092-domain-premise-count`, and pm-spec's independently derived value agrees, so the T-1092 two-source cross-check will pass at the freeze without a re-print. The omission class itself is now same-class-2 at the orchestration layer: the corrective rule adopted forward is that every pm-spec briefing carries the Routing Map's printed dispatch decisions for all four axes verbatim, alongside the issue canon. Recorded for the retro as an orchestrator-layer lesson candidate rather than repaired silently.
- intervention: human-correction
  date: 2026-08-31
  summary: The operator ruled on the deviation notice's three dispositions with a single-word selection — 「a」— after requesting and receiving a plain-language restatement of the finding and the options.
  effect: Disposition (a) adopted: the reconciliation note, the stale-surface repair, and the narrowed live probe closing the declared-label-versus-resolved-type residual (two three-instance qa-verifier arms, tool-set discriminator). The spec proceeds from DRAFT to its normal flow; the board entry unblocks. The probe's added value as ruled: it is the first verification that a plugin role's shipped tool restriction is actually enforced on concurrently launched instances. Sprint version premise (v2.4.1 PATCH) unaffected.
- intervention: assumption-contradicted
  date: 2026-09-01
  summary: Freeze-run self-inflicted slip, self-caught: the intent-hash append anchored on the sentence "Frozen on the coordinating session's own authority..." via a last-occurrence search, but that sentence appears in six-plus Done entries' attestation records, so the hash line landed inside T-1105's Done entry instead of T-1111's Active entry. check-intent caught it immediately (found valid=0 for T-1111), the stray line was removed with an exact-line assertion and re-inserted at the verified correct position, and both T-1111 (5c9f7800...) and T-1110 (0de3a297...) re-verified aligned afterward.
  effect: Same defect class as the T-1107 marker corruption (anchoring an edit on a non-unique text fragment): the standing full-line-anchor rule was under-applied — the anchor WAS a full line but not a UNIQUE one, and last-occurrence resolution silently selected the wrong entry. Corrective rule adopted forward: board-entry edits anchor on text asserted unique within the file (or on a measured line number with an exact-content assertion before writing), and every hash/attestation append is followed immediately by the checker read that consumes it, which is what converted this from a latent corruption into a 60-second repair. Recorded for the retro as a data point that the write-then-verify coupling (append, then immediately run check-intent) is the mechanism that caught it.
- intervention: human-correction
  date: 2026-09-01
  summary: The coordinating session proposed a class-B re-freeze of AC4 (pointing its command-lines clause at the protocol restatement) to resolve the one red criterion left by its own evidence-drafting omission. The operator challenged it directly — 「実験をやり直さないためにチェックの見る場所を一箇所ずらすというのは、それこそ後から結果に都合よく直している行為そのものですが、それは問題ないんですか？」— naming the proposal as an instance of the very fit-the-check-to-the-result class the freeze-then-probe design exists to forbid.
  effect: Proposal withdrawn, conceded on the merits: when work fails a frozen gate, the default repair direction is work-toward-gate, never gate-toward-work; a gate edit is for a gate that is WRONG, and AC4 was not wrong — its requirement was satisfiable and the evidence author (the coordinating session) simply failed to follow it. The distinction the session had relied on (record-format clause vs verdict-licensing machinery) is real but subordinate: admitting "it is only formatting" once creates the erosion precedent. Disposition taken instead: the frozen protocol's own invalid-evidence rule — the first probe's evidence is declared invalid for the recorded reason "evidence section drafted without AC4's required `- command:` line grammar (the freeze executor's own conformance omission)", and the one permitted re-probe runs under the identical frozen protocol with the evidence rewritten to the frozen grammar; no frozen byte (AC4, protocol region, byte-lock rules) is touched. Rule adopted forward for the freeze executor: before committing any orchestrator-produced evidence, re-read every AC whose subject is that evidence and check the drafted bytes against each required grammar (the same write-then-verify coupling that caught the intent-hash mis-append). Carried to the retro alongside the T-1108 AC14(g) placeholder precedent — the second measured instance of the freeze-executor-omission class caught downstream.
<!-- END interventions: T-1111 -->
