<!-- BEGIN interventions: T-1081 -->
- intervention: assumption-contradicted
  date: 2026-08-17
  summary: Three relayed premises in the tech-lead Routing Map were contradicted by pm-spec's first-hand measurements — the prompt-block topology of agents/pm-spec.md is two marker regions plus four contain-mode blocks, not "five contain-mode blocks including language" (templates/prompt-blocks/registry.txt line 31 registers language.md in marker mode for this file); the 2026-08-17 retro's claim that the 2026-08-11 Try item "is not yet executed" is false (agents/pm-spec.md line 88 is that item, shipped); and only two board sections carry an explicit base-ref discriminator declaration line (T-1079, T-1080), not four.
  effect: AC11 was written against the measured topology instead of the relayed one; the spec's deliverable 1 was positioned as complementary to the existing line-88 cross-check with no reliance on the retro's account; and the two-of-four measurement became the concrete evidence for D2's choice of a declaration line over a prose convention. All three corrections are recorded in the spec's Assumptions and on the board entry.
- intervention: assumption-contradicted
  date: 2026-08-17
  summary: The freeze run's live sweep contradicted the implicit authoring assumption that every check line runs in the operating environment — measured, diff cannot read /dev/fd process substitutions under the session sandbox (AC11), bare /tmp writes are denied (AC15), and the run-time reverse-map over the whole commit range derives ten suites against three declared because .shell-team/todo.md and .shell-team/test-recipe.md appear as fixture literals in seven suites that never read this task's prose (AC17, structurally unsatisfiable as first written).
  effect: One pre-freeze route-back to pm-spec; all three lines repaired meaning-preservingly (TMPDIR extraction files for AC11, mktemp -d under TMPDIR for AC15, population scoped to the deliverable prose paths agents/ skills/ docs/ for AC17) and re-swept before the v1 freeze; the environment facts were recorded in the spec's Notes for engineer and later in .shell-team/test-recipe.md by the engineer.
- intervention: unclassified
  date: 2026-08-18
  summary: The orchestrator's round-1 rework brief compressed Codex review round 1's Major 1 while relaying it — the review's own text named "or is simply not fetched" and (in the adversarial pass) "rebased, force-pushed, or squash-merged", but the brief relayed only the remote-tracking-only and rebased/force-pushed cases, so two named cases never entered any rework round and resurfaced as review round 2 Majors 2 and 3; discovered when Codex round 2 quoted its own round-1 wording back.
  effect: The single-namespace-existence-test class reached three occurrences and loop-guard stopped at max_iterations_reached partly on relay-compressed reworks; escalated to the operator with the STOP digest, and the next rework brief (if extended) points the engineer at the review file itself instead of an orchestrator paraphrase.
- intervention: human-stop
  date: 2026-08-18
  summary: The operator instructed the loop to stop at the task boundary ("タスクの切れ目で止めて") immediately after T-1081's close-out — PR #315 open, both gates green, CI in progress at the moment of the stop.
  effect: T-1082 (the sprint's next backlog item) was not started; the sprint pauses at the T-1081/T-1082 boundary with state fixed to memory, resumable at the next session.
<!-- END interventions: T-1081 -->
