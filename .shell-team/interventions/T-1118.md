# Interventions — T-1118

<!-- BEGIN interventions: T-1118 -->
- intervention: assumption-contradicted
  date: 2026-09-03
  summary: The coordinating session's re-Plan briefing to tech-lead stated that `agents/codex-reviewer.md` is "the only shipped `codex exec` wrapper shape". tech-lead re-measured the primary source and found two: `agents/drift-evaluator.md` runs the same bare-first-token `codex exec` skeleton through the same `bin/codex-capture.sh` helper, whose header (`:3`–`:4`) names both callers. Re-run by the coordinating session: `grep -lF 'codex exec' agents/*.md | wc -l` → 2.
  effect: No deliverable changes shape; DP-a's fallback arm (a thin Claude wrapper sub-agent) now has two measured precedents instead of one, and the spec's `## Assumptions` must cite the count with its regenerating command rather than the briefing's wording. Lesson instance: a briefing premise about "the only X in the repo" is a count, and a count travels with its command (T-1113 quantity relay) — the coordinating session stated it from memory.
- intervention: assumption-contradicted
  date: 2026-09-03
  summary: The Routing Map the coordinating session handed to pm-spec as the plan to formalize (step 3, adversarial fixture synthesis) stated two expectations: an unknown adapter token "must refuse `no-recipe`", and `codex-reviewer` bound to `claude-cli` "must still refuse rather than invent a path". pm-spec measured both against `bin/resolve-executor.sh` at authoring time: an unknown adapter token is refused upstream as `binding-unresolved` at exit 2 and never reaches the recipe lookup (so `no-recipe` is reachable only for a registered adapter with no shipped recipe), and the `codex-reviewer` → `claude-cli` rebind is a documented shipped capability that the gate admits at exit 0 (`README.md:292`, `CLAUDE.md:42`).
  effect: Both corrections are encoded in the spec (DP-i; AC12, AC13(iii), AC14; AC28(g) carries them into QA's briefing) instead of shipping fixtures built against the map's wording. No deliverable changes shape. Lesson instance: a fixture expectation written before the checker exists is a prediction about a sibling script's behaviour — it is measured against that script before it becomes a criterion, and the map's author held no shell to measure it.
- intervention: assumption-contradicted
  date: 2026-09-03
  summary: The coordinating session's route-back message to pm-spec stated that passing the prompt as `"$(cat <file>)"` "is the shipped convention in `agents/codex-reviewer.md`'s own invocations". pm-spec measured the three shipped skeletons (`agents/codex-reviewer.md:43`, `:67`–`:72`, `:176`–`:180`): all pass their prompts as inline double-quoted argv tokens with backslash continuations; none uses a file or a command substitution. The file form is the T-1116 review record's own practice (`.shell-team/reviews/T-1116.md:134`–`:142`), not the agent file's.
  effect: The correction itself stands (the briefing is delivered as a file because its frozen text embeds a double-quoted sentence, which the shipped inline form cannot carry); only its stated ground moved, and the spec now records the measured fact instead of the non-existent precedent. Lesson instance: "the shipped convention" is a claim about a specific file's bytes — name the file and the line before citing it, or cite the record where the practice actually lives.
- intervention: assumption-contradicted
  date: 2026-09-03
  summary: The freeze attestation (board, v1, `28784c3`) stated "Mutual satisfiability: no cross-criterion conflict" after a live run of all 27 check lines and a clause-by-clause static read. The engineer's implementation measured three contradictions the sweep missed: (1) AC2 pins the `codex-cli` recipe to exactly one `wires-role codex-cli tech-lead` row and one `admits-authority codex-cli none` row while AC12 requires all six roles admitted under the shipped default, whose `templates/binding-default.conf:43` already binds `codex-reviewer` (`proposes`) to `codex-cli` — no gate can satisfy both, and the Non-goals/Input-space class 3 sentence that `codex-reviewer` "must be refused" contradicts the shipped default path; (2) AC6's `grep -oE '.{0,400}…'` exceeds BSD grep's repetition bound of 255, so the line cannot run under the `bash`-resolved `/usr/bin/grep` (the coordinating session's own shell resolves `grep` to ugrep, which accepts it — the sweep ran the line under the wrong binary); (3) AC17's unanchored `grep -c 'codex exec '` counts prose mentions (8/4 at both refs) against the asserted bare-invocation counts 3/1.
  effect: The engineer stopped with `BLOCKED` (`d5d3054`) instead of fixing frozen lines unilaterally (correct). Disposition: class-B ratified re-freeze (v2) for (1) with the coordinating session's decision — admission is derived, not hardcoded: a role whose own shipped `agents/<role>.md` hosts a bare `codex exec` invocation is a Claude-hosted wrapper whose write authority the wrapper carries, so the read-only recipe never receives its write and the gate admits it with a distinct reason token; the Non-goals/Input-space sentence is corrected, and `authority-incompatible` keeps firing for `writes`/`proposes` roles with no such wrapper. Class-M mechanics repairs for (2) and (3). Retro input: third consecutive task whose freeze sweep missed a cause-class (T-1115 cause enumeration, T-1116 pii gate, T-1118 satisfiability against the shipped default and the host's grep binary) — the sweep must run each check line under `bash` with the CI-equivalent PATH and must evaluate every criterion against the shipped default configuration, not only against the tree.
- intervention: unclassified
  date: 2026-09-03
  summary: All eight harness-tracked background Bash slices of the HEAD-arm re-run (blast-radius sweep at `65813b4`, launched ~07:50Z) were reported by the harness as `killed` at ~08:20Z, mid-way (5–9 of 15–16 units per slice). The coordinating session did not stop them and no host message accompanied the event; whether the stop was the operator's action or a harness/session interruption was not determinable from the session at the time; the operator confirmed on 2026-09-03 that they took no action, so the stop is attributed to the harness/session side. The operator also reported changing the effort level from a different project's session at that moment (`/effort` writes the shared `settings.json`); a settings-file reload killing another session's background shells is a plausible, **unverified** cause — the in-session `/effort high` at ~07:05Z while the first HEAD arm's slices were running did not kill them, and no debug log was available to confirm (the debug symlink is dangling). Progress is preserved: the slice runner is resumable (completed units carry a `check-acs:` end marker and are skipped on re-launch), the eight detached worktrees are intact at `65813b4`, and the working tree is clean.
  effect: The re-run is paused, not lost; the coordinating session asked the operator before re-launching (an unexplained stop is treated as a possible stop signal); with the operator's answer the eight slices were re-launched with the resumable runner. The first HEAD arm (`1223f48`, complete, 123 units) is retained as `fanout-t1118-head-r1` for the record. Recorded because a harness-collected interruption that leaves no ledger line cannot be classified at the retro.
- intervention: unclassified
  date: 2026-09-03
  summary: The `qa-verifier` run launched at ~09:30Z was reported by the harness as `Agent stalled: no progress for 600s (stream watchdog did not recover)` at ~11:07Z. The operator had announced a network outage ("ネット切れます") minutes after the launch, so the stall is attributed to the outage. Measured on recovery: working tree clean at `296737b`, board flag still `READY_FOR_QA`, no `### QA verification — T-1118` section written — the run left no artifact.
  effect: The QA run is resumed verify-first through the same agent (its transcript is intact) rather than relaunched blind; nothing on the branch changed. Recorded because a harness-collected interruption without a ledger line cannot be classified at the retro (CLAUDE.local.md, 2026-08-31 ruling).
- intervention: human-stop
  date: 2026-09-03
  summary: The operator wrote 「一旦停止」 at ~11:33Z while the resumed `qa-verifier` run was executing (11 minutes in, no board artifact yet). The coordinating session stopped the running QA agent and paused the task at branch HEAD `e02a081` (flag `READY_FOR_QA`, tree clean).
  effect: Nothing on the branch is lost; the QA verification restarts from its briefing (`t1118-inputs/qa-briefing.md`) when the operator resumes. Recorded per T-1002 (a human stop is an intervention).
- intervention: unclassified
  date: 2026-09-03
  summary: The `engineer` rework run (Codex round-1 Blockers) was terminated by the harness at ~12:38Z with `API Error: 529 Overloaded` (server-side, request id recorded in the harness notification) while it was about to verify its edits. Measured: one uncommitted modification in the working tree (`bin/check-invocation-path.sh`, +18 lines — the in-progress `admits-authority` duplicate-key detection), no rework commit yet, board flag `REWORK`.
  effect: The uncommitted edit is left in place and the same agent is resumed verify-first (its transcript is intact), instructed to re-read the tree state before continuing rather than assume its earlier steps landed. Recorded per the 2026-08-31 ruling that harness-collected interruptions get a ledger line.
<!-- END interventions: T-1118 -->
