<!-- BEGIN interventions: T-1100 -->
- intervention: assumption-contradicted
  date: 2026-08-26
  summary: AC5 (byte-identical two-arm back-compat verdict across every distinct `- dispatch:` line on the resolved board) and AC12 (this task's own board entry must carry `verify-fixture`/`verify-mechanism` rows by the time it closes out) are structurally incompatible once implemented, because the board's DP-5 substitution landed at commit 82deff3 before the engineer phase began: AC5's population is read from the CURRENT board at check time, so it necessarily includes the two new rows AC12 requires, and those rows can never agree between the base-ref close-out.sh (which correctly refuses an axis it does not define) and HEAD's (which correctly accepts it) — the entire reason the task exists.
  effect: implemented every other criterion to green and did not edit the frozen intent block or revert the committed board substitution to force AC5 green. AC5 is reported as a measured, reproducible FAIL in the hand-off with full root-cause and reproduction, routed back to pm-spec for a class-B correction (most likely: AC5's population should exclude this task's own board entry, or exclude rows whose axis key this same task introduces) rather than silently patched. Full detail recorded in `.shell-team/provenance/T-1100.md`'s fourth entry.
<!-- END interventions: T-1100 -->
