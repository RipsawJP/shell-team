<!-- BEGIN interventions: T-1004 -->
- intervention: assumption-contradicted
  date: 2026-07-30
  summary: The frozen criteria set was assumed internally consistent — the orchestrator's pre-freeze run could not see otherwise, because AC2 passes on a pre-implementation tree — but the engineer measured a contradiction that no implementation can resolve: AC2's check line forbids any diff path under agents/ while AC16 mandates the issue-44 clause in agents/scrum-master.md and AC18's allow-list permits it.
  effect: The engineer stopped at BLOCKED with 17 of 18 green rather than bending either criterion, and the resolution routes through a human-ratified v1-to-v2 re-freeze narrowing AC2's check to added files only (--diff-filter=A), which is what its own prose ("the diff adds nothing") said all along.
- intervention: human-correction
  date: 2026-07-30
  summary: The orchestrator escalated the frozen-set contradiction for a per-instance human GO as the ratification design requires, and the maintainer answered that this class of stop is one they do not want — asking for an explicit decomposition of what in the ratification genuinely requires a human, so that whatever does not can be delegated into the loop design.
  effect: The ratification authority is being decomposed into a mechanics-repair class (candidate for standing delegation under fail-closed machine-checkable boundaries with cross-provider verification) and an intent-change class that stays human because the human is the source of truth for their own intent; the loop-design change is negotiated instead of the stop being re-run.
<!-- END interventions: T-1004 -->
