<!-- BEGIN interventions: T-1070 -->
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: Planning stated issue #256's per-line blank-line normalization quadratic as the identified root-cause candidate; the tech-lead's live measurement (the board's `## Active` section is 8 lines, so that loop runs 8 iterations) contradicted its plausibility as the 122s driver, and pm-spec's second measurement (the T-1068 board record of ≈12.3s for a single 5008-byte Active line, UTF-8 locale) then showed the hypothesis is not refuted either — there are two growth axes (done axis = awk full-file scan; active axis = line-length-dependent bash loop).
  effect: The spec freezes root cause as unconfirmed (AC1 requires profiling to re-derive the dominant term arithmetically before implementation) and the scaling criterion was extended from a single board-size series to a two-axis 1x/2x/4x before/after matrix (12 points); sprint plan unchanged — only #256's eventual disposition (close vs correction comment) depends on the profile outcome.
- intervention: assumption-contradicted
  date: 2026-08-15
  summary: Sprint planning and the task brief stated the checker "sits at the edge of the harness's 120s timeout" as a standing fact (relayed from issue #269's ~122s reading); the freeze sweep's own live run measured the identical command against the live board at 1.1s, exit 0 (2.8MB-class board whose `## Active` holds only the T-1070 entry) — the ~122s reading was an artifact of the board state at its measurement time (a long task entry resident in `## Active`), not a standing property of the board today.
  effect: No scope or plan change — the spec had already framed the cost as two growth axes with event-pinned measurements before this datum arrived; the 1.1s figure is handed to the engineer as an AC14(f) relayed-premise re-measurement input, and the operating framing is corrected from "currently at the timeout edge" to "the edge recurs whenever a long entry sits in Active, and the done axis grows monotonically regardless".
<!-- END interventions: T-1070 -->
