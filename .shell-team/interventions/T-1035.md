<!-- BEGIN interventions: T-1035 -->
- intervention: assumption-contradicted
  date: 2026-08-05
  summary: The sprint planning premise (carried from the 2026-08-05 retro's Problem 3 wording and relayed into the tech-lead prompt) stated that T-1024's spec AC2 froze the population values 214/26 and that a spec-side re-freeze would update them; the tech-lead's read of the primary files measured this false — the 214/26 literals live in the audit document (docs/loop-engineering/check-line-mktemp-guard-audit.md:26), while the frozen AC2 holds only the identity checks (document declared values == live re-derivation) and anti-vacuity floors, with no population literal in the spec at all.
  effect: The coordinating session corrected the route on its own authority with the goal unchanged (planning approved "resolve AC2's drift by updating the declared values, with a stale-at trigger"; only the assumed mechanism was wrong): option D adopted — the audit document's inventory is re-derived at the branch base ref via committed-blob reads (git show), the declared values updated there, and AC2 re-frozen class-B to read the same ref, which also severs the self-reference coupling (this task's own spec would otherwise move the population it counts). The added inventory work for the ~10 spec files newer than the audit is the discovered cost of the approved outcome, bounded by the P1 pre-commitment (two consecutive Blocker/Major rounds on the T-1024 repair surface carve out the template half and revert T-1024 to v1). Fourth relayed-claim contradiction this sprint — this one originated in the retro's own summary sentence, showing the class also reaches ratified planning inputs.
<!-- END interventions: T-1035 -->
