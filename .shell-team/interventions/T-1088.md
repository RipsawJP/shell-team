# T-1088 intervention capture

<!-- BEGIN interventions: T-1088 -->
- intervention: assumption-contradicted
  date: 2026-08-22
  summary: the launching prompt instructed a "ten-file exclusion byte-lock" and then enumerated twelve files in the same sentence (the contract source, the harness note, the briefing, five subject-01 files, and four predecessor specs T-1086/T-1087/T-1089/T-1090). The tech-lead routing map's own "Out of scope" section independently enumerates the same twelve. Counted first-hand by pm-spec at authoring: 1 + 1 + 1 + 5 + 4 = 12, not 10. T-1087's own AC6 locked exactly ten files (contract source, harness note, five subject files, three predecessor specs), because at T-1087's own authoring time the briefing and T-1087's own spec did not yet exist as things to lock; the "ten" figure was carried forward into this task's launching prompt without adding the two files this task must now additionally lock (the briefing itself, and T-1087's own now-merged spec).
  effect: the spec's own AC4 locks twelve files, not ten, and asserts the sweep loop visited exactly twelve paths (`test "$((10#$n))" = "12"`) rather than trusting the "ten" figure silently. The engineer independently re-verified all twelve byte-locks live this round (AC4: PASS) before assembling the note, so the corrected count was carried through implementation rather than only at spec-authoring time.
- intervention: assumption-contradicted
  date: 2026-08-22
  summary: the spec's own reasoned Blast radius discharge list names five specs (T-1090, T-1089, T-1087, T-1086, T-1079) as the read set AC7 item (d) requires running in full regardless of what the mechanical derivation returns. Running the spec's own prescribed mechanical derivation (one `git grep -F -l` call carrying one `-e <path>` per path this task edits or names, over `.shell-team/specs/`, per `bin/derive-populations.sh`) at hand-off surfaced eighteen items total — seventeen merged specs plus this task's own spec (self-reference) — twelve more than the five the reasoned list named: T-1069, T-1072, T-1073, T-1074, T-1075, T-1077, T-1078, T-1080, T-1082, T-1083, T-1084, T-1085 (all twelve reference `docs/loop-engineering/phase-multiplexing.md`'s path by text), in addition to T-1079, T-1086, T-1087, T-1089, T-1090.
  effect: per the spec's own instruction ("any further spec the derivation surfaces is run in full too"), all seventeen merged specs' full criterion sets were run at both the branch point (`69ee536054a2864b3e95449fd2a3524282a9b505`) and HEAD for AC7 item (d), not only the five the spec's own reasoning named. The engineer hand-off reports every one of the seventeen individually against `## Predicted inherited-criterion profile`, rather than trusting the narrower reasoned list. This is the same class of gap T-1087's own interventions record (`.shell-team/interventions/T-1087.md`) disclosed for its own downstream inventory (four named, sixteen surfaced) — recurring here at a different scale (five named, seventeen surfaced) because the read-set path list itself grew between the two tasks (T-1088 additionally names the judge briefing and all five subject-01 files, which T-1087's own read set did not name as edited paths).
<!-- END interventions: T-1088 -->
