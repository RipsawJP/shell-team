<!-- BEGIN interventions: T-1046 -->
- intervention: assumption-contradicted
  date: 2026-08-07
  summary: The spec's `## Notes for engineer` frames the per-label mutation self-check as breaking "the specific element the fixture guards" (singular) for each of the thirteen v2 labels. Running the self-check for real showed that assumption does not hold for `LX03` specifically: it is defended by TWO independent mechanisms (the `check-ignore` query's `./`-prefixed, trailing-slash normalization AND `report_ignored_base_verdict`'s after-scaffolding call ordering) — breaking only the query normalization left the fixture green, and only breaking both together (query AND ordering) reproduced the historical M3 hazard (red). This matches the same finding already recorded against v1's LX03 in the prior round, so the redundancy survived the v2 rewrite intact rather than being newly introduced.
  effect: Disclosed rather than silently narrowed to "single-element" framing for this label: the provenance record's `LX03` entry names both defended elements explicitly, records the single-mutation-stays-green result before the double-mutation-goes-red result, and treats the redundancy as an accurate finding about the implementation (belt-and-suspenders: the after-scaffolding ordering closes the pre-existence gap independently of the query fix) rather than a gap in the fixture. No spec or code change followed from this. Separately, `LX07` — doubly protected in v1 (stream separation AND the `env -u` chain) — is now singly protected in v2: with the blacklist replaced by the `env -i` allow-list (D8), reverting `git_probe()`'s wrapper to a bare `git` call alone reproduces the ambient-`GIT_*` hazard under the identical fixture, with no second element needed. This is the expected consequence of D8's strategy change (the allow-list collapses what were two defensive layers in v1 into one in v2) and is recorded here as confirmation that the re-freeze's intended simplification actually took effect, not as a gap.
<!-- END interventions: T-1046 -->
