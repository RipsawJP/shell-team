<!-- BEGIN interventions: T-1046 -->
- intervention: assumption-contradicted
  date: 2026-08-07
  summary: The spec's `## Notes for engineer` frames the per-label mutation self-check as breaking "the specific element the fixture guards" (singular) for each of `LX01`-`LX11`; running the self-check for real on `LX03` and `LX07` showed that assumption does not hold for those two labels specifically — each is defended by TWO independent mechanisms (for `LX03`: the query's trailing-slash normalization AND the after-scaffolding call ordering; for `LX07`: stream separation AND the `env -u GIT_TRACE` neutralization), so breaking only one of the two left the fixture green, and only breaking both together reproduced the historical hazard (red).
  effect: Disclosed rather than silently narrowed to "single-element" framing for those two labels: the provenance record's `LX03`/`LX07` entries name both defended elements explicitly and record the combined-break result, and note the redundancy as an accurate finding about the implementation (belt-and-suspenders per D8) rather than a gap in the fixture. No spec or code change followed from this — the double protection is a property of the design already frozen (D8's "each is the direct remedy for one of the seven historical faces" plus the `env -u` hygiene layer being explicitly complementary to stream separation, per the measurement record P6/P7), not a defect.
<!-- END interventions: T-1046 -->
