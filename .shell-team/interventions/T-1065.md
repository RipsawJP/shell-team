<!-- BEGIN interventions: T-1065 -->
- intervention: assumption-contradicted
  date: 2026-08-13
  summary: The freeze sweep's live probe measured that this host's BSD awk (version 20200816, en_US.UTF-8) treats distinct Japanese heading strings as equal under `$0==h` (strcoll collation) - `## 実行方法` matched four different headings (lines 12/86/244/287 of docs/adopting.ja.md) - contradicting the spec's implicit assumption (Input space: headings mixing ASCII and Japanese are handled) that AC6's awk heading-equality idiom compares bytes. AC6's Japanese adjacency clauses would fail post-implementation and its `## 運用ルール` byte-identity clause would vacuously compare the wrong section on both sides.
  effect: AC6 routed back to pm-spec for a meaning-preserving correction (pin the collation, e.g. LC_ALL=C, so heading equality is byte equality) before the freeze; no intent change - the criterion's meaning (byte-exact headings, pinned adjacency) is what the correction restores on this host. Found at the designed point (verify-then-correct-then-attest, pre-hash), so no frozen bytes moved.
<!-- END interventions: T-1065 -->
