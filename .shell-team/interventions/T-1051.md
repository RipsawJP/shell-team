<!-- BEGIN interventions: T-1051 -->
- intervention: work-deferred
  date: 2026-08-09
  summary: issue #180 (class-M ledger grammar widening) was split out of T-1051 at spec time — pm-spec measured that the widening's canonical target, the operator-grant sample fenced block in docs/tuning-oversight.md, is pinned byte-for-byte by T-1028's frozen AC12 to that spec's F5 region inside T-1028's own intent block (with T-1005's line-270 check additionally pinning the en/ja blocks to each other), so a faithful widening requires a class-B re-freeze of a merged frozen spec; the routing map's own split trigger (more than two cells of genuinely new wording across the six surfaces) fired independently on cell count.
  effect: T-1051 ships #178+#179 only; AC12 locks all six #180 surfaces byte-identical to base so the split is observable; the split-off work's requirement list (the six surfaces, the resolved row-13 generator question, the frozen byte-lock measurement) is recorded in the spec's scope decision for the next planning; issue #180 stays open.
- intervention: assumption-contradicted
  date: 2026-08-09
  summary: the v1 freeze-attestation declared "nothing mechanically broken, vacuously passing or structurally unsatisfiable", but the engineer measured AC1's gh-form positive-control fragment (18 combined characters) as arithmetically unable to satisfy the unmodified `gh[oprs]_` alternative's 20-character minimum — a structurally unsatisfiable clause inside a frozen check line, present at freeze and confirmed three independent ways. The freeze sweep decomposed the failing negative clause (probed red-for-the-right-reason) but did not probe each positive-control clause's satisfiability, which is exactly where this hid.
  effect: task at BLOCKED per the engineer's escalation; routed to pm-spec for a mechanical v1→v2 re-freeze (lengthen the fragment, no design change), with the orchestrator re-attesting v2 clause-by-clause including positive-control satisfiability. Sweep-method lesson indexed for the retro: an attestation's satisfiability sweep must exercise every synthetic positive control against the live pattern, not only the clauses that are red at freeze.
- intervention: assumption-contradicted
  date: 2026-08-09
  summary: the orchestrator resumed pm-spec to apply the v1→v2 AC1 repair onto the live working tree on the assumption the engineer had finished (grounds - a BLOCKED hand-off commit existed and the watchdog had exited at its lifetime); the assumption was false — the engineer's completion notification had not arrived, and the still-running engineer found the uncommitted intent-block mutation it did not author, correctly snapshotted it to scratchpad and reverted it via git checkout, mid-way through the orchestrator's v2 freeze-gate check-acs run.
  effect: the first v2 freeze-gate aggregate (16 PASS) is discarded as tainted (early criteria measured the v2 spec, later ones the reverted v1); the v2 board ledger lines were never committed (the pre-commit check-intent caught the drift and aborted the chain, fail-closed as designed). Recovery - pm-spec re-applies its recorded delta on the now-quiescent tree, and the gate re-runs single-writer. Lesson for the retro, same class as the standing no-concurrent-writer-and-reader rule: an agent is finished when its completion notification arrives — a hand-off commit or a watchdog lifetime exit is not the completion signal, and no second writer touches the tree before it.
<!-- END interventions: T-1051 -->
