# Interventions — T-1107

<!-- BEGIN interventions: T-1107 -->
- intervention: work-deferred
  date: 2026-08-30
  summary: The freeze run's derivation-embed step corrupted AC14's own check line - the spec legitimately quotes the derivation BEGIN/END marker literals inside that check line, and the orchestrator's string-index replacement matched the FIRST occurrence (inside the check line) instead of the placeholder region, splicing the 119-line tool block into the middle of a quoted for-list and leaving an unbalanced quote (the sweep surfaced it as AC14 exit 2, the broken-as-command class). The spec was uncommitted and unfrozen, so no frozen byte was involved.
  effect: Mechanically restored - the check line reassembled from its surviving prefix and suffix around the injected span, the block re-embedded at the exact-match placeholder lines, AC14 re-run exit 0, dry-run 0 unrecognized, quote count even. Rule applied going forward in this session - marker-region edits match whole lines exactly (the placeholder region's markers stand alone on their lines; a check-line's quoted marker literal never does), the same unique-anchor discipline the corpus already records for Edit insertions. Retro input: a freeze-run tooling step that writes into a spec must anchor on whole-line matches, because specs quote their own scaffolding literals inside check lines by design.
- intervention: human-correction
  date: 2026-08-30
  summary: At the class-B ratification gate for AC10, the host first asked what options existed besides GO; the orchestrator presented four alternatives as a neutral menu, and the host corrected the framing - an option that does not advance the goal is not a valid option, and the gate should have been presented as validity-tested choices, not labels. The host then approved the re-freeze and directed a retro discussion (verbatim, operator language): "有効な選択肢がない場合その選択は一意なのでホスト確認ではなく進めるというパターンをレトロで議論してください。おそらく「有効であるかどうか」をどう判定するのかがポイントになると思います。その際に、選択肢の有効性について先にCodexでも敵対評価するというのもアリかもしれません".
  effect: This ratification proceeded on the host GO as usual. Retro input, not yet a standing rule - candidate pattern: when a gate decision's alternatives all fail a goal-advancement validity test, the choice is unique and may proceed on the loop's own authority instead of a host confirmation; the open design question is the validity-judgment procedure itself, and one candidate mechanism is an adversarial cross-provider (Codex) evaluation of option validity BEFORE the gate is raised. Interacts with the standing three-human-gates rule (class-B ratification is currently unconditional) - any narrowing is the host's promotion decision at the retro, never this task's.
<!-- END interventions: T-1107 -->
