<!-- BEGIN interventions: T-1157 -->
- intervention: assumption-contradicted
  date: 2026-09-26
  summary: issue #592 states "the usage text already exists as a header comment in each of the 14"; at the branch point 12 of the 14 headers carry a Usage line and `check-contract.sh` and `check-handoff.sh` do not (pm-spec read, re-derived at the freeze)
  effect: the spec makes the Usage clause conditional and the two print their existing header as-is, keeping the issue's "surfacing, not writing" requirement; no header text is authored in this task
- intervention: unclassified
  date: 2026-09-26
  summary: review round 2 (the release-sweep rework) completed its primary pass with no finding on the delta, but the evidence pass failed with the Codex CLI's own `workspace routing discovery failed` followed by `turn.failed` — the second occurrence this sprint (the first was T-1156 round 1); the reviewer did not vary flags or widen the allowlist and stopped with BLOCKED — review not established
  effect: treated as a REQUEST_CHANGES round; the review is re-invoked as round 3 with no change to the delivered files
<!-- END interventions: T-1157 -->
