# Harness per-invocation parameter surface for a plugin-role sub-agent launch (T-1116)

## Probe evidence

Filled by the coordinating session on 2026-09-02 after the intent-hash v1 freeze (`afa6204ebc2a82f32ef88aa1d040b9ba3572e04c`, spec at commit `97d9b8b`) and before the engineer was invoked. Every line below follows the grammar frozen in the spec's `probe-protocol` region; the engineer alters not one byte of this section. Absolute paths are written as `<repo-root>`.

- launch: cal-haiku-rep1 — subagent_type=general-purpose — model=haiku — isolation=omitted — status=accepted — tool_uses=0 — duration_ms=22731 — Item B briefing (bytes between the `briefing-b` markers of the spec at the frozen hash); Agent tool call by the coordinating session, `model` parameter passed as `haiku`; usage figures transcribed from the result's `<usage>` block.
- launch: cal-haiku-rep2 — subagent_type=general-purpose — model=haiku — isolation=omitted — status=accepted — tool_uses=0 — duration_ms=13648 — Item B briefing (bytes between the `briefing-b` markers of the spec at the frozen hash); Agent tool call by the coordinating session, `model` parameter passed as `haiku`; usage figures transcribed from the result's `<usage>` block.
- launch: cal-haiku-rep3 — subagent_type=general-purpose — model=haiku — isolation=omitted — status=accepted — tool_uses=0 — duration_ms=18676 — Item B briefing (bytes between the `briefing-b` markers of the spec at the frozen hash); Agent tool call by the coordinating session, `model` parameter passed as `haiku`; usage figures transcribed from the result's `<usage>` block.
- launch: cal-opus-rep1 — subagent_type=general-purpose — model=opus — isolation=omitted — status=accepted — tool_uses=0 — duration_ms=6841 — Item B briefing (bytes between the `briefing-b` markers of the spec at the frozen hash); Agent tool call by the coordinating session, `model` parameter passed as `opus`; usage figures transcribed from the result's `<usage>` block.
- launch: cal-opus-rep2 — subagent_type=general-purpose — model=opus — isolation=omitted — status=accepted — tool_uses=0 — duration_ms=4402 — Item B briefing (bytes between the `briefing-b` markers of the spec at the frozen hash); Agent tool call by the coordinating session, `model` parameter passed as `opus`; usage figures transcribed from the result's `<usage>` block.
- launch: cal-opus-rep3 — subagent_type=general-purpose — model=opus — isolation=omitted — status=accepted — tool_uses=0 — duration_ms=11557 — Item B briefing (bytes between the `briefing-b` markers of the spec at the frozen hash); Agent tool call by the coordinating session, `model` parameter passed as `opus`; usage figures transcribed from the result's `<usage>` block.
- launch: plug-base-rep1 — subagent_type=shell-team:tech-lead — model=omitted — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-base-rep2 — subagent_type=shell-team:tech-lead — model=omitted — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-base-rep3 — subagent_type=shell-team:tech-lead — model=omitted — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-haiku-rep1 — subagent_type=shell-team:tech-lead — model=haiku — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-haiku-rep2 — subagent_type=shell-team:tech-lead — model=haiku — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-haiku-rep3 — subagent_type=shell-team:tech-lead — model=haiku — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-opus-rep1 — subagent_type=shell-team:tech-lead — model=opus — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-opus-rep2 — subagent_type=shell-team:tech-lead — model=opus — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- launch: plug-opus-rep3 — subagent_type=shell-team:tech-lead — model=opus — isolation=omitted — status=not-run — tool_uses=na — duration_ms=na — not launched: the calibration arm reached its second non-admission (Item A then Item B), so the frozen protocol's value is `instrument-inadmissible` by clause 1 and the plugin arm had no admitted instrument to run; running it would have produced evidence no clause of the frozen total function reads.
- capability-outcome: cal-haiku-rep1 — pass — final-shape=conforming — integers=1 — value=46879980 — graded against Item B's declared answer 46879980; the reply's final non-empty line transcribed verbatim as the terminal field — final-line=answer=46879980
- capability-outcome: cal-haiku-rep2 — pass — final-shape=conforming — integers=1 — value=46879980 — graded against Item B's declared answer 46879980; the reply's final non-empty line transcribed verbatim as the terminal field — final-line=answer=46879980
- capability-outcome: cal-haiku-rep3 — pass — final-shape=conforming — integers=1 — value=46879980 — graded against Item B's declared answer 46879980; the reply's final non-empty line transcribed verbatim as the terminal field — final-line=answer=46879980
- capability-outcome: cal-opus-rep1 — pass — final-shape=conforming — integers=1 — value=46879980 — graded against Item B's declared answer 46879980; the reply's final non-empty line transcribed verbatim as the terminal field — final-line=answer=46879980
- capability-outcome: cal-opus-rep2 — pass — final-shape=conforming — integers=1 — value=46879980 — graded against Item B's declared answer 46879980; the reply's final non-empty line transcribed verbatim as the terminal field — final-line=answer=46879980
- capability-outcome: cal-opus-rep3 — pass — final-shape=conforming — integers=1 — value=46879980 — graded against Item B's declared answer 46879980; the reply's final non-empty line transcribed verbatim as the terminal field — final-line=answer=46879980
- model-self-report: cal-haiku-rep1 — reported=claude-haiku-4-5-20251001 — the reply's first line, `self_model=` value verbatim; non-licensing by the pinned channel role.
- model-self-report: cal-haiku-rep2 — reported=claude-haiku-4-5-20251001 — the reply's first line, `self_model=` value verbatim; non-licensing by the pinned channel role.
- model-self-report: cal-haiku-rep3 — reported=claude-haiku-4-5-20251001 — the reply's first line, `self_model=` value verbatim; non-licensing by the pinned channel role.
- model-self-report: cal-opus-rep1 — reported=Claude Opus 5 (1M context), per this session's stated model id claude-opus-5[1m] — an unverifiable belief — the reply's first line, `self_model=` value verbatim; non-licensing by the pinned channel role.
- model-self-report: cal-opus-rep2 — reported=Claude Opus 5 (1M context) — my honest belief, unverifiable — the reply's first line, `self_model=` value verbatim; non-licensing by the pinned channel role.
- model-self-report: cal-opus-rep3 — reported=Claude Opus 5 (1M) per my system prompt, though a commit trailer in my instructions says "Claude Fable 5.1" — unverifiable either way — the reply's first line, `self_model=` value verbatim; non-licensing by the pinned channel role.
- item-in-force: B — answer=46879980 — expression `9431 * 7283 - 6127 * 3559`; recompute command `echo $((9431 * 7283 - 6127 * 3559))`; in force because a substitution line exists below.
- void-reps: 9 — the nine plugin-arm launches recorded `status=not-run` are void by the status clause; all six accepted calibration reps are valid (`tool_uses=0`, conforming final line, one integer run).
- briefing-sha256: eb86dd50626fca7cae9993f4a8a653ee39ffb636670009e23d1afe6b2d6faceb — SHA-256 over the lines strictly between `<!-- BEGIN briefing-b: T-1116 -->` and `<!-- END briefing-b: T-1116 -->` of the spec at the frozen hash, computed with `shasum -a 256`; one value for every launch that ran (the six Item B calibration reps); the plugin arm did not run.
- calibration-verdict: not-admitted — Item B: `cal-opus` 3/3 `pass`, `cal-haiku` 3/3 `pass`, zero void reps; admission requires `cal-haiku` unanimous `fail`, so the item does not discriminate the two tiers under the calibration role. This is the second non-admission (Item A, superseded below, was not admitted on the same shape), so the model verdict is `instrument-inadmissible` by clause 1.
- model-schema-read: enum=sonnet,opus,haiku,fable — precedence=declared — read first-hand from the Agent tool's JSON schema as loaded in the coordinating session: `"enum": ["sonnet", "opus", "haiku", "fable"]`; the parameter description declares the override "takes precedence over the agent definition's model frontmatter" and is "ignored for subagent_type: fork". Non-licensing for the model question; both tiers this instrument passes are in the enum.
- effort-schema-read: absent — the Agent tool schema's properties are exactly `description`, `prompt`, `subagent_type`, `model`, `isolation`; no effort or reasoning-effort key exists (harness-surface reading, Claude Code 2.1.258).
- effort-attempt: not-attempted — effect=na — the schema read is `absent`, so there is no key to attempt; fixed recorded value under that premise.
- pre-commitment-executed: drop-1 — the two non-launching falsification calls left the protocol at spec time (spec-review rounds 1 and 2 each landed a Major on that component; means-ends reflection recorded on the board entry); no `- falsification:` line exists here by construction.
- probe-writes: none — after every launch the coordinating session read the real checkout with `git status --short` and `git ls-files --others --exclude-standard`; both printed nothing after each of the twelve launches that ran (six superseded Item A reps, six Item B reps). No clone was cut and no worktree was added for the probe.
- harness-version: Claude Code 2.1.258 (`claude --version`), macOS host, coordinating session model Fable 5.1; same machine and same session for every launch.
- substitution: from=A — to=B — superseded-briefing-sha256=5f0764ca093fca1c998c465cedefbca2a18744a81f5464f21ea1e503c0d21b98 — decided and recorded before any plugin-arm launch; trigger satisfied on the frozen predicate: superseded `cal-opus` 3/3 `pass` with zero void reps and superseded `cal-haiku` with three non-void `pass` reps (under-discrimination, not a strong-tier miss).
- superseded-calibration: cal-haiku-rep1 — model=haiku — status=accepted — tool_uses=0 — pass — final-shape=conforming — integers=1 — value=53466367 — Item A round, graded against 53466367; duration_ms=8094; self_model line: claude-haiku-4-5-20251001 — final-line=answer=53466367
- superseded-calibration: cal-haiku-rep2 — model=haiku — status=accepted — tool_uses=0 — pass — final-shape=conforming — integers=1 — value=53466367 — Item A round, graded against 53466367; duration_ms=21155; self_model line: claude-haiku-4-5-20251001 — final-line=answer=53466367
- superseded-calibration: cal-haiku-rep3 — model=haiku — status=accepted — tool_uses=0 — pass — final-shape=conforming — integers=1 — value=53466367 — Item A round, graded against 53466367; duration_ms=23282; self_model line: claude-haiku-4-5-20251001 — final-line=answer=53466367
- superseded-calibration: cal-opus-rep1 — model=opus — status=accepted — tool_uses=0 — pass — final-shape=conforming — integers=1 — value=53466367 — Item A round, graded against 53466367; duration_ms=6097; self_model line: Claude Opus 5 (claude-opus-5[1m], per this session's stated model ID; unverifiable from inside) — final-line=answer=53466367
- superseded-calibration: cal-opus-rep2 — model=opus — status=accepted — tool_uses=0 — pass — final-shape=conforming — integers=1 — value=53466367 — Item A round, graded against 53466367; duration_ms=10481; self_model line: Claude Opus 5 (as my context reports; I have no way to introspect or verify this) — final-line=answer=53466367
- superseded-calibration: cal-opus-rep3 — model=opus — status=accepted — tool_uses=0 — pass — final-shape=conforming — integers=1 — value=53466367 — Item A round, graded against 53466367; duration_ms=8450; self_model line: Claude Fable 5.1 (though this harness's system context declares the model as Opus 5 / claude-opus-5[1m]; I cannot verify either) — final-line=answer=53466367
- command: git status --short; git ls-files --others --exclude-standard — the cleanliness read, run in `<repo-root>` after every launch.
- command: awk '/^<!-- BEGIN briefing-b: T-1116 -->$/{f=1;next} /^<!-- END briefing-b: T-1116 -->$/{f=0} f' .shell-team/specs/T-1116-harness-invocation-parameter-probe.md | shasum -a 256 — the briefing-hash derivation (the same command with `briefing-a` yields the superseded value).
- command: echo $((9431 * 7283 - 6127 * 3559)) — the answer recompute for the item in force (`echo $((8237 * 6491))` for the superseded item).
- reproduce: cd <repo-root> && awk '/^<!-- BEGIN briefing-b: T-1116 -->$/{f=1;next} /^<!-- END briefing-b: T-1116 -->$/{f=0} f' .shell-team/specs/T-1116-harness-invocation-parameter-probe.md | shasum -a 256 | cut -d' ' -f1 — must print the `- briefing-sha256:` value above when run against the spec at the frozen intent hash.
