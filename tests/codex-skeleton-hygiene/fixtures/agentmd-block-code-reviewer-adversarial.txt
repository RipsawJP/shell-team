# T-107-step: codex
codex exec --sandbox read-only --cd <repo> --json -o "<RAW_OUT>" \
"Open the file at the absolute path <PERSPECTIVES_FILE> without changing a character of the path, \
and follow the instructions it contains."
