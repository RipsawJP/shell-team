# Tasks

## Active

- [ ] **T-601** alpha task in flight — `READY_FOR_ARCH` — spec: docs/specs/t-601.md

## Done

- [x] **T-072** done alpha — `READY_FOR_MERGE` — spec: docs/specs/t-072.md

## Format

Example showing nested code fences (the outer fence uses 4 backticks so a 3-backtick example can safely appear inside it; a 3-backtick line inside must NOT close the 4-backtick outer fence per CommonMark — a closer must be at least as long as its opener):

````markdown
Nested code sample below:
```
## Active

- [ ] **T-602** phantom example — still meant to be OUTER fence CONTENT; the 3-backtick line above is shorter than the 4-backtick opener and must NOT close it — spec: docs/specs/example.md
````
