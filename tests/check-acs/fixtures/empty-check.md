# Fixture spec — T-050 (#132): an empty check: value

## Acceptance criteria

- [ ] **AC1** a check: line present but with nothing at all after the colon *(scriptable)*
  - check:
- [ ] **AC2** a genuinely-omitted check: (no such sub-bullet at all) stays SKIP *(runtime — user dogfood)*
