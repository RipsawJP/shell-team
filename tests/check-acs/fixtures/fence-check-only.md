# Fixture — a criterion whose ONLY check: line is fenced is SKIP, not executed (T-1102 AC3a)

## Acceptance criteria

- [ ] **AC1** criterion whose only check: line is fenced *(scriptable)*

```
  - check: touch "$T1102_FENCE_CHECK_SENTINEL"
```
