# Consumer that carries the core with a legitimate role-specific tail

## Core

- The canonical core sentence that every consumer must carry verbatim. For this role specifically, an extra tail clause is allowed because contain-mode only requires the core.
