- **Convergence verdict in the STOP escalation (T-1145, #491)**: when you run the rework-history digest for a `STOP:` escalation, supply the convergence inputs — `--rounds-total <the total number of rounds run>`, one `--never-dropped <name>=<green|hit-this-round|hit-earlier>` per never-dropped component, and one `--instance <class>=<same|distinct>` for every class slug appearing twice or more in the records — then open the escalation message with the digest's own `convergence:` value written in the operator's language, followed by its three grounds (`trend:`, `never-dropped:`, `instances:`) in that same language, and then the digest's raw stdout pasted verbatim below them. The verdict states, it never decides: it gates nothing, suppresses no escalation, shortens none and creates no gate. Where `convergence: converging` stands beside `judgment: same-class-repetition`, say so in the same message — a repeated class label whose instances are distinct while the trend falls is exactly the case a label count alone reads wrong — and let the digest's `convergence-action:` line name which action leads. The script's stdout stays the single source of truth for the choice text; never hand-copy or re-describe it. Supplying none of the three inputs leaves the digest exactly as it was before this field existed; supplying some but not all of them is refused with exit 2 and nothing on stdout, never a partial verdict.
