# 更新履歴（Changelog）

`shell-team` プラグインのリリース履歴 — リリースごとに 1 バレット・新しい順。リリース履歴はまず `README.ja.md` の「バージョニング」節からこのファイルへ移設され、その後公開準備の一環で全面リライトされた（新しい順への並べ替え・サブ箇条への分割・内部タスク/issue 参照のスクラブ）。以降の新しいリリースエントリは本プロジェクトのリリース手順の一部としてこのファイルに追記する。

English version: [CHANGELOG.md](CHANGELOG.md)

- **v2.5.0**
  - **mechanism-class の検証 sweep が既定で並列 fan-out するようになりました。** 検証対象が機械的に列挙可能なフェーズは、既定の出荷パスで N インスタンスに分割されます — 従来の「Opt-in only（既定ではどのフェーズも fan-out しない）」という記載は事実でなくなったため両 README から削除しました。あわせて、fan-out の起動記録は実行ホストを必ず明記します: コーディネーティングセッションが分離 staged worktree 上に harness-tracked な background shell slice を起動する形が唯一の準拠形で、detached 子プロセスを抱えるサブエージェント hosting は専用パイロットで実測されるまで禁止です。本リポジトリ自身の対象群では、並列形は operator が最も長く待つフェーズで実測約 44% の壁時計時間短縮でした。
  - **freeze がリリースティア自体を導出し、作業が承認済みプランを超えた時点で停止するようになりました。** 初回・再 freeze を問わず毎回、spec 自身の宣言から headline テストと default-reachability テストを結合適用してティアを再導出し、board エントリに記録して承認済みプランニング前提と照合します。不一致なら停止し、見積もり超過の明示・continue-or-stop の問い・根拠つき推奨の 3 要素を備えた逸脱通知を出します — 裸の「続行しますか？」は出しません。このゲートは初の実発火で、まさに本リリースの名前を変えた前提崩れを検出しました。
  - **プラグイン役割のツール制限が、並列起動下でも実際に強制されることが検証されました。** 制限つき役割のインスタンス群を無制限のコントロールと並べて同時起動する差分プローブが、宣言されたツールセットが並列実行中のインスタンスから実際に取り上げられていることの初の物理的証拠を産出しました。プロトコルと証拠をバイト単位で凍結した loop-engineering ノートとして記録し、測定に先行していた stale な文書面はこの測定を引くよう修復済みです。
  - **board エントリ上の数量主張が、自身の導出コマンドを携行し、fail-closed チェッカーで検証・再実行されるようになりました。** opt-in の `- count:` 記録文法が、relay された数値をそれを導出するコマンドに束縛します。live 検証は各主張のコマンドを再実行して不一致を拒否し、文法の近似ミスは黙ってスキップせず収集して拒否、close-out は不正な主張があれば board 書き込み前に拒否します。稼働初日にこのチェッカーは自らの作者の陳腐化したカウントを 2 回捕まえ、続くタスクでは文法がまだ対象にしない手転記の集計を 1 件捕まえました — 何を閉じ何を閉じないかの実測境界です。
  - **scope-lock の記述規約が、base から worktree までの全チェーンを rename-safe に測定するようになりました。** spec の scope lock は 4 つの read — committed range・staged delta・unstaged delta・untracked strays — の union として教えられ、3 つの diff read すべてで rename 検出を無効化します。git の既定設定では rename の移動元パスが全 read から同時に不可視になり、管理対象パスからの staged rename が「scope はクリーン」と報告される裏で無許可の削除が隠れられるためです。規約の正典は 1 ファイルに置かれ（2 つ目のコピーは削除）、両言語の adopter ガイドがそれを指します。適用は forward only: マージ済み spec の凍結 lock は修復しません。
- **v2.4.0**
  - **stacked train のタスクは、直前タスクが各 dispatch 軸で何を選んだかの記録（dispatch-reflection）を board エントリに持てるようになり、凍結前に fail-closed で検証されます。** コーディネーティングセッションが、エントリ自身が記録する dispatch 軸ごとに 1 行 — 直前タスク名・判定（`repeat`/`differs`/`no-predecessor-row`）・根拠 — を dispatch 転記と同じ窓のその前に書くため、繰り返された誤選定が operator の質問を待たずループ内で表面化します。entry-mode チェッカーは記録がある場合にのみ全面検証します: 文法と軸カバレッジの双方向一致、直前エントリ自身の記録値との判定照合（直前エントリは Done セクションにあっても解決）、単一 predecessor 不変条件と自己参照ガード、どちら側の重複 dispatch 行も拒否、no-predecessor 形式の区別。記録を持たないエントリは従来どおり通過するため、既存の board や未採用リポジトリには影響しません。
  - **review / review-response スキルが executor を同梱リゾルバとホストの `binding.conf` 経由で解決するようになり**、設定に記録した rebind がレビュー経路で実際に効くようになりました。同梱の既定バインディングは不変です。
  - **binding 整合チェッカーが宣言本文の突合・空値の拒否・自身の母集団ガードを行うようになり**、形だけの宣言が技術的な抜け道で通過しなくなりました。
  - **close-out スクリプトは、エントリ自身のフラグがマージ可でない board エントリの移動を拒否し、issue クローズの帰結を常に明示します** — issue 指定がない場合も、統合ブランチへのマージでは issue が自動クローズされない旨を明示的に出力します。
  - **正規の two-arm 検証スイープが、未追跡テレメトリコーパスの凍結スナップショットを base arm に staging するようになりました。** レイアウトリゾルバ経由で解決し実ディレクトリとしてメンバー単位でコピーするため、コーパスを読む基準が base arm で偽の失敗を出し、スイープのたびに手で再分類する必要がなくなりました。
  - **日本語ドキュメント一式に読みやすさの改稿を実施し**、機械翻訳調の文を自然な文章に書き直しました（機械検査対象のトークンは不変）。
- **v2.3.0**
  - **リポジトリが oversight プロファイルを宣言し、宣言したループ継ぎ目で記録済みの人間承認を必須にできるようになった。** ホストは `oversight.conf` で `autonomous`（出荷既定 — どこも変えない）か `governance-controlled` を選ぶ。後者は宣言した各継ぎ目で、成果物の producer と別人の approver による board 記録済み承認（承認対象を今も指す anchor つき）を要求し、評価できないものは fail-closed で拒否する。加入済みリポジトリは宣言ファイルが消えただけでは離脱にならない: board に承認記録が 1 つでもあれば、宣言消失は silent 復帰でなく拒否になる。出荷ドキュメントは機構を誠実に命名し（認証つき職務分掌統制ではなく、content anchor つきの自己申告型 conflict check）、閉じていない残余を列挙する。
  - **レビュー記録の全 executor pass が検証可能な input-fidelity ブロックを持つようになった。** レビュアーは executor 起動コマンドの verbatim 記録（ホーム配下の絶対パスはプレースホルダに正規化）、finding 生成と confirmation を区別する閉集合の pass-role ラベル、その pass に届いた briefing-fidelity 値、ラウンド間で上書きされない per-round raw-capture stem を記録する。新しい fail-closed checker が presence と文法を検証し（プロンプトの内容は決して判定しない）、close-out ゲートは評価できない記録を拒否する。
  - **バッククォートのフェンス内コードブロックが受け入れ条件パーサに対して不活性になった。** フェンス内に引用された AC 形の行が実行されたり phantom criterion として報告されたりしなくなった。
  - **verify 軸の dispatch 選択が作業種別で分割され**、fixture スイート半分と mechanism クラスの全数 diff 半分が独立の根拠つき記録を持つようになった。
  - **マシンローカルなセッション・scratchpad パス形状が、既存の PII 形状と並んでコミット記録から遮蔽されるようになった。**
- **v2.2.1**
  - **記録された entry mode が 2 つの記録源で食い違うタスクを、凍結時にループが拒否するようになりました。** 新しい fail-closed checker が、独立に書かれた 2 つのコミット済み記録 — planning 役の board サブ箇条と orchestrator の dispatch 転記 — を読み、どちらかが欠けているか食い違う場合は、読者がたまたま目にした側の記述を信用する代わりに進行を拒否します。初のライブ発火で、実際の二者間転記衝突を凍結前に捕捉しました。
  - **選出された spec review が承認 verdict に到達していないタスクの close-out を拒否するようになりました。** 新しい backstop checker が、review 記録の最終アンカー行を閉じた 4 形式文法で特定し、承認と読めない限り close-out をブロックします — 従来方式への敵対的レビューが列挙した散文パース系の敗北クラスに構造的に免疫です。
  - **どの記録完全性義務にテスト済み checker を与えるかが、個別判断でなく成文規則で決まるようになりました。** 改竄耐性の記録に criticality 規則が加わります: verdict がループ遷移をゲートし、かつ機械実行可能な義務は、fixture suite と CI 配線つきのテスト済みスクリプトを得る。それ以外は文書化された instrument を得て、再分類経路も記録されます。本リリースだけで 3 つの義務がこの規則の下で昇格し、3 つ目の着地で規則自身の再評価トリガが発火・記録されました。
  - **trial 導入レシピが、文書化されるだけでなく実行されるようになりました。** CI 配線された fixture suite が、出荷レシピを adopting ガイド自身のバイト列から抽出し（手打ちの複製ではないため、ガイド編集がテスト対象と黙って乖離できません）、使い捨てリポジトリで端から端まで実行します — 両方のディレクトリレイアウト × 両方の文書化されたセットアップ経路で。これにより、以前は先送りされていた実行時の発見が恒久的な regression lock になりました。
  - **専用ブランチでの trial 開始がフラグ 1 つになりました。** `team-init --trial-branch <name>` は scaffold のバイトを 1 つも書く前に trial ブランチを作成して切り替え、3 つの名指しされた危険状態を exit 2 と個別の対処法つきで拒否し、フラグなし経路は従来どおり完全に git-free のままです。この修正で偽になった 2 つの記述は両言語の adopting ガイドから削除されています。フラグは opt-in です。
  - **タスクボードが、静かな構造破損クラスから守られるようになりました。** board-heading checker が、従来は base revision との比較でしか見ていなかった構造見出しの存在と一意性を、base に依存せず無条件に検証します。これが閉じる見出し重複破損は本プロジェクト自身のライブボードで発見・修理されたもので、この checker は本リポジトリでは push ごとにボードを lint します。
  - **エスカレーション地点で、人間を巻き込む前に成文の手段目的セルフチェックが走るようになりました。** 3 つのエスカレーション発火点で、4 つの正典質問（主価値はまだ green か・これは補助成分か・事前価格済みの処分はあるか・この経路の累積コストは）に答え、事前価格済みの drop は新しい人間ゲートを発明する代わりに自分の権限で執行します — 常設の人間ゲート 3 つは不変で、回答は rework digest に載せられます。
- **v2.2.0**
  - **完成した spec をチームに渡しても、全機構がそのまま走るようになりました。** spec を誰が書くかはタスクごとに記録されるルーティング決定になります: 出荷時の既定では従来どおり PM 役が起草し、既に spec を書き上げた operator はそれを持ち込むだけで、PM 役は conformance formatter に切り替わります — 文法と構造は整形し、実質的な不足は著者へ差し戻し、意図は決して書き換えません。下流の全ゲート（凍結 sweep・QA・cross-provider review）は両モードで無変更に走り、記録されたモードと hand-off が食い違う場合は fail-closed の凍結時 read が進行を拒否します。
  - **spec 文書自体が、凍結前に cross-provider review を受けられるようになりました。** spec の正しさがこの repository 自身には測定できない前提に依存する場合、プランニング役は Specify seam — intent hash が記録される前 — で spec 文書を読む追加レビューパスを選出します。欠陥のある前提は、実装後に批准級の修復として表面化する代わりに、再凍結コストゼロの通常の著者編集として修正されます。初の実運用サイクルでは、従来なら凍結後に落ちていた 11 件のレビュー指摘をこのパスが凍結前に消化しました。
  - **green フラグが「実際に何を検証したか」を語るようになりました。** すべての spec は検証到達水準 — ループ自身のゲートが到達できるレベル（`unit-and-static` または `real-environment`）— を宣言します。人間の担当に残る作業には criterion 単位の例外を付け、宣言が何も証明しない状態を許さない非空規則と、宣言レベルで何も検証できなかった唯一のケースのための固定開示トークンを備えます。QA は宣言値を verdict と、読者が実際に見る board 行へ逐語転記するので、`READY_FOR_REVIEW` は裸の green ではなく「このレベル*まで* green、これらの項目はその上に残る」と読めます。
  - **1 チケットだけチームを試すために、導入を確定させる必要はなくなりました。** 導入ガイドにサポート付きの trial ルートが加わります: scaffold と稼働ファイルは組織が決してマージしない専用ブランチに置かれ、全機構はそのまま動き、後始末はブランチ削除 1 回です — 伝播に関する注意・グローバル gitignore への対処・両方のディレクトリレイアウトを明記済み。ガイドには untracked local-only モードの棄却決定も記録され、そのモードが黙って無効化してしまう fail-closed ゲートが 1 つずつ名指しされています。
- **v2.1.0**
  - **チームがタスクごとに各フェーズの実行方式を決定し、その決定を記録するようになりました。** プランニング役が 2 つの直交軸（実装フェーズの走らせ方・検証フェーズの走らせ方）に書かれた優先順位規則を適用し、決定はタスク自身のボードエントリに機械検証される `dispatch` レコード（軸・選ばれた機構・条件付きトリガか無条件既定か・根拠となる priced line）として記録されます。close-out ゲートは不正なレコードを拒否するため、ルーティング決定が非構造の散文として紛れ込むことはもうありません。
  - **検証フェーズのファンアウトが既定経路で到達可能になりました — フラグも opt-in スイッチも不要です。** 記録された verify 軸の決定が Tier-1 ファンアウトを指すとき、run スキルの Validate ステップは、そのフェーズの機械的に列挙可能な検証作業を出荷済みファンアウトステップへ書かれたとおりに委ねます: 固定 population ファイル・出荷済み cap 規則による並列度・インスタンスごとの liveness・1 インスタンス 1 part ファイル・単一の権威 verdict への還元・帰属不能な集約を拒否する attribution チェック。実 90 unit の検証母集団と実 8 インスタンスで end-to-end に実測: ファンアウト腕は同一母集団の単一インスタンス基準より wall-clock で 46.89% 短く完了し、両腕の verdict region は byte 一致でした。
  - **すべてのファンアウト起動が永続的な launch record を残すようになりました。** ファンアウトごとに 1 ファイルが population・要求/達成並列度・cap の根拠・全 unit-インスタンス割当・全 liveness watcher の verbatim コマンド・起動/完了 epoch を記録します — 「走った」ファンアウトと「走ったと主張するだけ」のものが事後に区別できます。
  - **spec の凍結が誤りにくくなりました。** 凍結ゲートは stacked ブランチ上で実体化された two-arm base-ref discriminator を要求し（silent fallback 経路は拒否）、借用語彙の count premise を branch point のコミット済み blob に対して attest 前に実測し、兄弟タスクが stack に持ち込んだトークンを値の継承でなく再実測します。
  - **この changelog 自体が、ここに書かれた機構によって検証されました**: 本リリース自身の検証 sweep が上記の実測発火です。
- **v2.0.3**
  - **board の hand-off lint が大規模 board で最大 17 倍高速化。** push のたびに走る checker の内側のホットスキャンを再実装し、このリポジトリ自身の board で 1 回あたり約 141ms → 8ms に短縮（判定はコーパス全体で不変）。
  - **telemetry の並行書き込みでログが壊れなくなった。** telemetry writer は追記のたびに never-stealing なディレクトリロックを取り、同じロックの下で次のシーケンス番号を導出できるようになった（`--seq auto`）。実ログに重複 `(run_id, seq)` を生んでいた競合はこれで閉じる。ロックを取れない writer は専用の exit code で拒否し、行を破損させる代わりに何も書かない。加えて、どのロールのどのインスタンスが書いた行かを記録できる（`--instance`）ため、並列ワーカーの記録も帰属を保つ。
  - **実験的機能（既定では未使用）: フェーズ内の検証ファンアウト機構。** 新しい `aggregate-verdicts.sh` は、インスタンスごとの verdict ファイル群を 1 つの正式な block に集約する — 同じ固定作業集合のどんな分割でも byte 単位で同一 — か、部分結果を昇格させる代わりに分類つきエラーで拒否する。run skill の Validate フェーズに、オーケストレータがこれを使ってよい opt-in の判断点が明記された。意図的に選ばない限り、どのフェーズもファンアウトしない。この系統の作業は「実装フェーズの並列化」という依頼に遡るが、本リリースはまだそれを満たしていない: 実測済みの土台が出荷され、依頼された能力そのものは未達のまま残る。
  - **バージョン番号が明文化された価値基準に従うようになった。** `CONTRIBUTING.md` に `## What a version number encodes` が加わった: 番号が符号化するのは「受け取る側にとってどれだけ有用になったか」だけであり、作業量や diff の大きさではない — 本リリースが変更の幅に反して patch である理由そのものである。

- **v2.0.2**
  - **検証の労力が、一律でなく「そのタスクが何を出荷するか」で価格付けされるようになった。** すべての spec は凍結 intent block 内で deliverable class — `mechanism` か `no-mechanism` — を宣言し、実行面を変えない文言・docs・分析タスクはより狭い検証で足りる: read-set スコープの影響分析（間接参照の逃げ道は実行または開示）・diff が入力に届く step だけの CI 等価確認・タスク自身が追加した check 行だけの mutation probe。スクリプト・テスト・テンプレート・CI・checker の意味論に触れるものは従来どおりの満額のまま。
  - **レビュアーへの briefing が、verdict の範囲と深刻度の較正を冒頭で明示するようになった。** orchestrator のレビュー briefing は、そのタスクの deliverable class で何が verdict を左右するか・手続き上の記録類の所見をどう重み付けするかをラウンド開始前に述べる — レビュアーが何を発見するかの独立性には明示的に手を触れない。
  - **loop-engineering の調査 4 本が着地し、ループは初めての実測コスト地図を得た。** タスクごとの労力と壁時計が実際にどこへ行くか（rework ラウンドはトークン単調増加だが壁時計は単調でない）・スプリントを通じてコンテキストがどう蓄積し、なぜ持ち越しがリセットに勝つか・フェーズは重ねられるか（契約再設計を前提とする段階的採用・共有状態の競合インベントリを実測）・N エージェントは 1 フェーズを分担できるか — 検証ファンアウトの実測パイロット込み: 掃引した並列度で壁時計 43.84% 短縮・集約された判定は直列実行と同一であることを証明・そして「突出した 1 ユニットがどの並列度でも回収できる上限を規定する」という発見。
  - **運用レッスン 4 件をコーパスへ昇格。** 凍結時の検証コストは deliverable class で価格付けする・レビュアーの前提レベルの勧告は言い換えでも抽象度を保つ・operator 向け報告は引用された既決 disposition の所有者を宣言する・stacked マージチェーンのレトロ窓は board エントリを持たない中間ノードを許容する。

- **v2.0.1**
  - **binding config のドキュメントが、読者が探す場所に着地した。** 両 README に executor アサイン機能の存在と正典ドキュメントの所在を示すポインタ節が追加され、adopting ガイド（英日）が単一の正典 how-to になった — 設定文法の実例、誤設定が到達しうる全拒否、有効な effort 値、rebind が変えるもの・変えないものの 2 軸境界まで。
  - **ユーザー可視の変更は、凍結時点で adopter 向けドキュメントを宣言するようになった。** 新しい spec は理由つきの `user-visible` 宣言を必ず持ち、`yes` はドキュメントの着地面の名指し（または明示的な waiver）を追加で要求し、満たさない凍結は拒否される — 出荷される機能がドキュメントを黙ってスコープアウトしたままリリースに到達することはもうできない。
  - **リリースノート template の compare link が正しい範囲を指すようになった。** `Full changelog` の compare URL は前リリースのタグと新リリースのタグを取る — 起草者が気づく必要のあったプレースホルダのペアではなく。
  - **出荷済みの文章が、それが記述する機構と一致するようになった。** 出荷済み prompt・skill・docs・記録の正確性一括修正: durability 確認は board タスク不在時に（再パラメータ化でなく）スキップされることを明記し、テストレシピは durability checker の scratch コピーが必要とする 2 つの sibling を記録し、oversight ページは instantiation list の境界を決定し（列挙パスは機構的に強制される面であり、助言的な指示ソースも基準の届く範囲として名指しされる）、grep-scope 規約は `git grep` の実際の symlink 挙動（tracked symlink エントリを丸ごとスキップする）を述べる — 誤った機構主張ではなく。

- **v2.0.0**
  - **executor のアサインがホスト設定可能になった: binding config・adapter contract・fail-closed な resolver が「どの役割を誰が実行するか」をホスト所有のファイルに接続する。** 行指向の `binding.conf`（閉じた拒否語彙と固定 exit code を持つ新 checker が検証）が各 inner-loop 役割を `{provider, model, effort, adapter}` に束縛し、task-envelope 契約文書と出荷済み adapter 定義 2 点（`claude-cli`・`codex-cli`）が executor の受け取る・返す・対応するものを宣言し、役割単位の resolver が各起動の直前に「この役割はどの executor が実行し、起動してよいか」に答える — ホストの config が最優先・無ければプラグイン出荷の v1 相当既定 — config の無いリポジトリはこれまでと完全に同じ挙動を保つ。失敗は常に代替でなく拒否: adapter が宣言しない effort 値・board 権限とチャネルの矛盾・executor 不在・使用不能な config。
  - **クロスプロバイダレビューは「無条件の不変条件」から「理由つきの出荷時既定」になった。** レビュアーは出荷既定 binding で Codex CLI に束縛される — 同一ファミリーの出力をレビューするモデルは盲点を共有するため。ホストは rebind できるが、それで変わるのは resolution の報告値と telemetry の記録値であり、ドキュメントはちょうどその範囲だけを主張する。レビュアー executor の不在・未認証は今もループを止める — 静かな代替は決して起きない。
  - **「止まったループ」と「人間を待つループ」が同じ沈黙でなくなった。** 両ループスキルは人間に制御を渡す時に gate 宣言 artifact を書き、回答に基づいて動く時に消す。外部タイマーが起動する新しい out-of-band 分類器 — 永続状態を read-only で読み、どこにも書かない — が `RUNNING` / `WAITING` / `STALLED` を stdout と exit code の両方で答え、読めない入力は閉じたトークン集合のエラーであって健全判定には決してならない。
  - **telemetry span が解決済み binding を記録し、集計がプロバイダ関係を保存せず導出する。** span は解決済みの provider・effort・adapter を運べる。per-run の roll-up はプロバイダの内訳と、レビューラウンドごとに 1 つの判定（cross-provider / same-provider / undetermined）を印字する。本リリース以前の行は常に `undetermined` と読まれ、捏造された答えにはならない。
  - **凍結ゲートが spec を「その後の暮らし方」で読むようになった。** run スキルの凍結前 sweep は、全 criterion を凍結時点の tree だけでなく実装後の tree 状態に対しても評価し、下流影響の開示に全母集団差分を要求し、複数面へ複製される norm 文を複製前に単数限定詞について読む。
  - **リリースページは以後 2 つの固定規則に従う。** タイトルはタグ名そのまま。本文は出荷 template から起こす — 本文内バージョン見出しなし・Highlights は changelog から逐語転記・Migration は行動が必要な時のみ。forward-only: 過去のリリースは編集しない。
  - **出荷スクリプトの自己位置解決を強化。** `bin/` から出荷 `templates/` を跨いで参照する 5 スクリプトが自分のディレクトリを物理解決するようになり、symlink された祖先ディレクトリ（普通の vendoring）が別ツリーのデータを読ませることはできなくなった。影響最大の 2 件は、修正前コードで実際に落ちることを観測した fixture でロック済み。
  - **`team-init` は binding の見本を不活性で設置する。** 導入リポジトリが受け取るのは `binding.conf.example` — ドキュメントであって executor アサインではない。有効化のリネームは意図的な行為。
- **v1.8.0**
  - **永続性ゲートが、ループ自身の記録が実際に永続化されたかを「予測」ではなく「事後観測」するようになりました。** 新しいチェッカーが、ハンドオフが永続と主張する各記録を固定コミット内の blob に解決し、ワーキングファイルと突き合わせます。run スキルの 2 つの永続性シームに既存のステータスガードと並べて組み込まれ、記録集合は `git status` ではなく同梱レジストリから得るため、迷子の ignore ルールで不可視になった記録 — 予測ベースの過去 2 設計が捕捉できなかったまさにその失敗 — が黙って通過せず捕捉されます。記録の除外は、コミット済み内容がワーキングコピーと一致する追跡モードファイル経由でのみ可能です。
  - **資格情報シェイプの走査が、このプロジェクト自身のハイフン区切りラベルで誤発火しなくなりました。** OpenAI 形式のトークンパターンに左境界ガードが加わり、ハイフンの前の 2 文字で終わる通常語＋長い識別子が鍵として報告されなくなりました。ガードが同時に抑止する唯一の本物の鍵シェイプ — 区切りなしで直前の英字・数字に密着した鍵 — は、チェッカー自身の散文と PII 統制文書に「意図的で境界の限定された妥協」として開示され、スイートで双方向にピン留めされます（黙った穴として残しません）。
  - **intent チェッカーの help とハッシュ処理がクリーンに失敗するようになりました。** `--help` はフラグ順に関わらず優先され、計算ハッシュはどの消費者が読む前でも 40 桁小文字 hex でなければ拒否され、印字経路は broken pipe で死ぬ代わりに分類済み exit に到達します。
  - **どのコマンドでも検証できない種類の実行時受け入れ基準が、項目ごとに報告・監査されるようになりました。** 実装側と検証側の両ロールが、spec 内のそのような基準を「1 つずつ・完全に」処理する規則を持つようになり、集合全体を主張する各クレームはそれが要約する列挙を伴います。圧縮された「全部完了」の要約は合格ではなく所見です。
  - **spec 作成ロールとレビューロールが、実運用から得た機構化契約を得ました。** 検証機構の事前確約は最初のレビューラウンド前に drop 順を名指しし、修正は報告された事例ではなく欠陥のクラスを閉じることを要求され、共有プロンプト領域に加える義務はそのファイル自身のスコープ慣用句を再利用し、追記専用記録への追記は記録全体の再読の後に行われます。あわせて、grep スコープ規約は再帰検索より追跡ファイル列挙を優先し、弱すぎる部分文字列で修理を守っていたアンカーが締められ、テストスイートが git 出力をクォート安全に読みます。
  - **黙って陳腐化しつつあった基準にラベルが付き、文書の数え違いが一括修正されました。** 出荷済み spec 2 件が、マージ後に red になることが想定される基準に merge-point-scoped ラベルを得て、1 つずれた台帳カウントが修正され、いくつかの陳腐化した引用が 1 回の editorial パスで訂正されました — 挙動は不変で、記録を正直にしただけです。
- **v1.7.0**
  - **凍結はこれから記録するハッシュを自分で印字でき、拒否は実測した数を名指しするようになりました。** intent チェッカーに `--print-hash` モードが追加されました — 凍結ブロックの検証に使うのと同じ正規化＋ハッシュのパイプラインを公開し、記録側が正規化を手で再実装せずに書き込む値を計算できます。attestation の拒否は実際にカウントした行数を、そのまま使える修正案として印字し、freeze ゲートの sweep には明示的なクロス結合ステップが加わりました: 既に red と判明した基準を、他の全基準が要求する充足条件の集合と突き合わせます（基準を個別に読むだけでは不十分）。
  - **spec の check 行に「ツリーをどう観測するか」の明文規約が入りました。** diff 系の基準はワーキングツリーではなくコミット範囲を名指しし（無関係な未コミット編集で偽 FAIL しない）、grep 系の不在基準は読むファイルを名指しするか再帰検索をスコープし（gitignore されたビルド成果物が基準を満たすことも破ることもできない）、集計の規律は生成済み playbook が既に持つものを重複させず同じ場所から索引します。ワーキングツリー形式を指示していた唯一のセルフチェック項目はその場で修正され、陳腐化に関する主張は維持されています。
  - **実行ビットのロックがテストスイートを双方向にカバーするようになりました。** テストツリー配下の全追跡ファイルについて、内容が shebang で始まるなら実行可能モードを持ち、実行可能なら shebang で始まる — index から判定し、git 出力が空またはエラーなら fail-closed、両方向と空 population に合成コントロール付き。生まれてから一度も実行可能になったことのなかった 11 のスイートファイルが正規化されました。
  - **テストスイートが推測可能なスクラッチパスを共有しなくなりました。** 20 スイートにまたがる 30 の固定スクラッチルート代入が明示テンプレート付きの `mktemp` に移行し、共有親ディレクトリへの cleanup glob（並行実行中の別ランのファイルを消し得た）は自分のランのディレクトリ内に閉じ込められました。同一スイートの 2 つの起動が衝突せずに同時実行できます。
  - **クロスプロバイダの全 verdict が、それを出した判定者を記録するようになりました。** レビュアーの verdict ブロックに必須の `Codex CLI` 行が加わり、そのラウンド自身の `codex --version` 実行の出力を逐語で運びます — 記録であって固定ではありません: 最低バージョンも、比較も、値に対するゲートもなし — advisory の drift evaluator にもバイト同一でミラーされ、verdict ブロックを書かない finding 評価モードには履行不能な義務が及ばないようスコープされています。ワークフローのクイックリファレンスはバージョン確認を「暗黙」と呼ぶのをやめました。
  - **閉じられないシグナル配送の窓が、オペレーターの目に入る場所に文書化されました。** 一時ファイルの作成から cleanup 登録までの間 HUP/INT/TERM を短時間無視する 2 つのチェッカーが、その残余を説明するバイト同一のヘッダ段落を持ちます — 失敗ではなくハングする `mktemp` はこの窓を割り込み不能にすることを含めて — 段落は既存の動的ヘッダ導出を通じて `--help` に到達します。挙動の変更はありません。
  - **レトロの入力収集が「実際にそこにあるもの」を読むようになりました。** spec リゾルバは慣習的な拡張子付きファイルだけでなく任意の通常ファイルを受け付け、リゾルバが使っていないレガシーマーカーを説明していた 4 つの文書サイトが修正されました。
  - **lessons コーパスが実運用から成長しました。** 3 エントリが追加 — 中継された前提は下流プロンプトへ書く前に一次資料で再測定する（プランニングロールの手順に対応する手書きルール付き）、集計基準は base 相対のデルタか値非依存の不変条件で書き絶対リテラルにしない、陳腐化した凍結プローズは都度の再凍結ではなく一括エディトリアルパスに蓄積する — ロール別プロンプトブロックはコーパスから再生成されています。
- **v1.6.0**
  - **出荷スクリプトの実行ビット欠落を、レビュアーではなく機械が捕まえるようになりました。** 新しいリポジトリ健全性スイートが、`bin/` 配下の全追跡ファイルが実行可能モードを持つことを git index から判定し（`core.fileMode=false` の checkout に騙されない）、git 自体の失敗や空出力では fail-closed になります — 部分的な一覧の後に非ゼロ終了が来た場合は失敗であり、決して clean pass ではありません — そして CI ステップとして走ります。このロックが封じる欠陥クラスは、初出荷時にクロスプロバイダレビューだけが発見したものです。
  - **再凍結分類器と intent チェッカーが、自身の失敗経路を生き延びるようになりました。** 一時ファイルの追跡はコマンド置換内の追記に依存しなくなり（レジストリは呼び出し側シェルに置かれ、EXIT トラップから実際に見えます）、比較ツールの I/O エラーは正当な「不一致」として読まれる代わりに usage 失敗として分類され、両スクリプトは HUP/INT/TERM を終了コード保存かつ一時ファイル残留ゼロで処理し（ファイル作成からクリーンアップ登録までの窓も閉鎖）、`--help` は動的導出になったためヘッダ編集で切れなくなりました。凍結ブロックへのインデント変更と末尾追加を分類器がどう判定するかを、新しい 5 本の fixture が固定します。
  - **spec 作成ロールに 2 つの著述規律が組み込まれました。** 「measured at `<ref>`」ラベルの隣にコマンドを印字する成果物には、そのコマンドの読み取り先（名指しされた ref のコミット済みバイト列であって working tree ではない）を狙う受入条件が必須になり、凍結された母集団カウント型の基準は、固定 ref を読むか明示的な `stale-at:` 再測定トリガを持つかのどちらかが必須になりました — 宣言値と実測値が静かに乖離できません。両クラスを露呈させた出荷済み監査文書は、最初の実適用として固定 ref で再導出されています。
  - **会計系の基準は出荷後の最終状態を記述し、そのアンカーは対象の境界で閉じるようになりました。** 実機再現された exploit の連続が、凍結基準の実務に 2 つの構造規則をもたらしました: 基準は「現在のバイト列が何回の批准済み修理を経たか」を数えてはならない（台帳の履歴は intent チェッカーの領分）、そして会計のマッチは部分文字列・散文中の言及・他タスクのエントリ配下の同一リテラルで充足されてはならない。あわせて: レビュアーのボード境界がオーケストレーター向けスキルでも規則として記述され、書き込み面の列挙はツール内部の状態ディレクトリを（意図的書き込みを許可せずに）除外し、summarized-sources 宣言行を誤分類していた凍結文言が訂正されました。
  - **バッチマージのレトロは、件数だけでなくコミットの同一性を照合しなければなりません。** レトロエージェントのサイクル窓規範は、返ってきたマージコミットをタスクの文脈が既に持つ同一性材料と照合することを要求します — マージ SHA は常に、PR 番号はマージ subject が運ぶ場合に、何も fetch せずに — そして返ってきた各エントリは照合されるか明示的に異物とフラグされるかのいずれかで、検証不能なメンバーシップはサイクルのものとして提示せず記録します。hand-off チェッカーのスイートの囮 fixture ガードは、スロット位置を含む行全体の形にアンカーされ、形を失った fixture は静かに何も検査しなくなる代わりに大声で失敗します。
  - **errexit 監査の除外レジストリが、固定行より上のあらゆる編集に課税しなくなりました。** レコードは `file:line:content` の位置固定（7 つのタスクがそれぞれ手動再測定を払い、1 つの固定が 6 箇所のスイートサイトを結合していました）から、位置独立・多重度明示の内容キーへ移行しました: レコードごとに宣言された出現数を実測と比較し、「未登録の書き込みが現れた」と「登録済みの書き込みが消えた・増えた」の両方向を 1 つの機構から得ます。除外行と byte 同一の複製は無審査で吸収される代わりに人間審査へエスカレートされ、設計上意図的に捨てたものは出荷 spec に文書化されています。
- **v1.5.0**
  - **機械的に壊れた凍結済み `check:` 行を、オペレーターが権限を付与した場合に限り、都度の人間 GO なしでループが修理できるようになりました。** 新しい分類器 `bin/check-refreeze-class.sh` が再凍結の差分を判定し（`- check:` 行に限定される差分＝`mechanics`／intent の散文に触れる差分＝`class-b`）、ボード記録は置き換えられたハッシュと差し替え行の全ペアを保持し、クロスプロバイダレビューには必須検証項目が載ります — その却下は再凍結をバイト単位で復元し、権限を停止します。**出荷時の既定は不変**: 付与がなければ、すべての再凍結は今までどおり人間のところで止まります。ノブ・記録形式・強制力の正直な限界・サンプル権限ブロックは `docs/tuning-oversight.md`（および日本語版）と `CONTRIBUTING.md` に文書化されています。
  - **spec は自分が要約する文書を宣言し、検証はその原本を開くようになりました。** spec テンプレートに必須の「Summarized sources」セクション（該当なしの場合の明示 sentinel 付き）が加わり、QA ロールには必須項目が加わります: 宣言された各 source について、凍結領域内の全再記述箇所を source 自身のバイト列と条項単位で照合する — spec 自身の言い換えの読み返しは検証になりません。lessons コーパスにも、区別の保存・byte-identity の一括理由づけ・継承/伝聞された主張の再測定に関する対応エントリが入ります。
  - **クロスプロバイダレビュアーの書き込み境界が「能力の不在」ではなく「従う規則」として記述されました。** 従来のボード編集禁止は「書き込みアクセスがない」という誤った前提に依拠していました。現在は 1 本の正典規則として、実際の能力面・書き込み可能なパスの閉じた列挙・1 つの限定的指示例外を持つ git 変更禁止・動機となったインシデント・再評価トリガを備えています。
  - **`bin/check-handoff.sh` のステータスフラグ検証が単一文法になりました。** 行形状の判定とフラグ語彙の判定が別々の正規表現を使っており、タイトルに囮の `` — `flag` — spec: `` 連鎖を含む行で異なるセパレータを解決して偽 PASS / 偽 FAIL を生み得ました。フラグは形状を判定したのと同じマッチから抽出されるようになり、囮の両方向が fixture として固定され、終了コードとメッセージはバイト単位で不変です。
  - **出荷済みの監査文書が、主張どおりに測定するようになりました。** 算術監査の桁数上限の根拠文は、誤った最上級ではなく実測された最大値を ref 固定の測定コマンド付きで名指し、headroom をそれに対して接地し直しました。mktemp ガード監査の tie-break の主張は訂正され（発火しなかったのであって、不要だったのではない）、測定がインラインで記載されます。未開示だった分類判断 1 件が対応する provenance 記録に開示されました。
  - **レトロエージェントは、バッチマージ方式のスプリントで明示的なサイクル窓の指定を必須とするようになりました。** PR を open のまま積んでまとめてマージする方式では、サイクルの既知のマージ数から `--last-n` を導出して明示的に渡します — リポジトリ全履歴を遡る無制限の既定を継承しません。

- **v1.4.0**
  - **キャプチャした数字列が到達しうる出荷スクリプト内の全算術サイトを棚卸しし、分類し、10 進正規化した。** 実測済みの fail-open 挙動 3 件を閉鎖: goal ループ state ファイルの先頭ゼロ値が bash の 8 進規則で黙って再解釈されることはなくなり、ゼロ埋めされたループ契約の上限値が暴走ガードを黙って無効化することもなくなり、先頭ゼロのテレメトリ値が誤集計や生のインタープリタエラーで中断することもなくなった — 拒否はすべて文法側で fail-closed に行われ、各スクリプト自身の名前付き診断を伴う。全会計 — 全ファイル・8 つの算術文脈・サイト別 4 値分類とサイト別の幅判断 — は監査記録としてリポジトリに同梱され、幅上限は量ごとに導出される（epoch は 11 桁の余裕を保つ）— 一律コピペではない。
  - **close-out は不正な board 行を done セクションへ持ち込むことを拒否し、その判定を hand-off lint 自身に委ねる。** close-out スクリプトは対象タスクの board 行を単一エントリの合成 board として兄弟の hand-off checker に渡し、その判定をそのまま採用する — 行文法の第二の複製は存在せず drift しようがない — ため、board lint が拒否するはずの行が全ゲートを素通りして `## Done` に乗ることはなくなり、不正な status flag は黙って書き換えられる代わりに拒否される。close-out の隣に hand-off checker が欠落・読取不能な場合は、board 内容の問題とは区別された install エラーとして報告される。あらゆる拒否で board ファイルはバイト不変。
  - **ブロックサイズ警告は、2 回の独立測定で不成立と判明した掃討を指示するのをやめた。** 生成器の閾値超過警告は、リポジトリに同梱された決定記録 — 移植された公開前繰延記録で、両測定・閾値に技術的根拠が無いという状態・再検討を開く 4 つのシグナルを運ぶ — を指すようになり、「stale なエントリの供養を検討せよ」という、既に丸ごと 1 回の裁定を空振りで再生成させた remedy を将来のメンテナに繰り返させない。
  - **パリティスイートの到達不能な第 3 比較を削除し、コメントは 3 回検査の主張をやめた。** fail-fast 下では、両レポータが同じ凍結期待値に一致すれば相互パリティは既に含意されるため、死んだ行は証明を現地に記録した上で削除された — 先頭への並べ替えなら「どちらのレポータが逸れたか」を名指しする能力を消していた理由も含めて — そしてコメントは 2 回検査の構造と「再追加するな」の恒久ポインタを明記する。
  - **run スキルは、オペレーターが到達しうるのに文書に無かった 2 つの結果を記載した。** テレメトリ節は、human イベント行の label に載せた素の rail トークンが hand-off 行と同様に replay の rail 停留を導出することを明記し — その行の label 自体は必須であることも明示した上で — 凍結 intent 分岐は追記後の第 4 の結果を名指しする: checker が次に欠けているバージョンの attestation レコードを改めて要求すること、そのバージョンで実測した値を埋めること、印字された remedy スケルトンの逐語コピーは決して通らないこと。
  - **lessons コーパスは completeness-accounting ルールを獲得し、その狭い前身を退役させた。** 完全性の主張は会計 — 母集団総数・文字どおりの選定手法・除外ごとの理由 — を伴って運ばれ、検証役は生の母集団から独立に再導出する; これが一般化する旧 bulk-fix-inventory ルールはこの新ルールを後継として退役し、生成される役割別プロンプトブロックはサイズ不変のまま 1 ルールを広いものに置き換えた。
- **v1.3.0**
  - **spec の凍結は、`check:` 行を live 実行したと board が attest するまで拒否される。** タスクの intent hash が最初に記録されるその瞬間、intent checker は board エントリ上の準拠した freeze-attestation レコードを要求するようになった — しかもレコードの算術（実行行数 vs spec 自身の `check:` 行の実カウント、PASS + FAIL vs 実行行数）を信用せずに検算し、不備は専用のエラークラスで fail-closed に拒否して、実カウント数を埋め込んだ即応の remedy スケルトンを印字する。本リリース以前に凍結された全レコードは carve-out で判定不変 — バックフィル不要・遡及要求なし・履歴走査なし。批准済み再凍結には運用開始後のペースルールが効き、run / goal スキルは attested freeze 後の三分岐を含む新しい分岐を文書化した。
  - **close-out は、interventions レコードが欠落・非準拠のタスクを拒否する。** board への書き込みの前に、close-out スクリプトがレコードを interventions checker で検証する — 無条件に、フラグも環境変数の逃げ道も warn-only モードも無しで。拒否は board をバイト不変のまま残し、checker 自身の分類に続けて、レコードのパスと準拠 zero-entry レコードの正確なバイト列を印字する 1 ステップの remedy を出す。interventions レコードを書いたことのない利用者は新たに拒否されるが、remedy は印字される 3 行そのもの。
  - **board エントリの終端に唯一の答えができ、迷子の継続行は fail-closed になった。** エントリの範囲を決めるすべての箇所にただ 1 つの継続行述語が適用され、close-out はエントリを丸ごと動かし — 内部の空行・インデントされた表の行・タブインデント・CRLF・セクション末尾やファイル末尾のエントリ — どのエントリにも属さない継続行を抱えた board の書き込みを拒否する。intent checker は空行をスコープ終端として扱うのをやめ、intent 台帳と board の移動処理が「何がエントリに属するか」でついに一致した。出荷 board テンプレートは checker が強制する文法を明記するようになった。
  - **2 つの telemetry レポータは「span 行とは何か」について同じ答えを持つことを強制される。** roll-up と failure-cluster のレポータは意図的に同じ行分類器の独立コピーを保持している。新しい parity スイートが両者を単一のコミット済み fixture 集合 — 6 つの discriminator クラス — で駆動し、レポータごとの凍結された期待値とレポータ間の一致を両方アサートする。片方のコピーだけに変更が入るとスイートが red になり、乖離したスクリプトと fixture を名指しする。重複自体は設計どおり残る。
  - **lessons corpus の退役在庫を棚卸しし、「無い」ことを測定した。** 人間批准つきの supersede パスが全 active エントリを三条件述語で裁定: 退役はちょうど 1 件（maintainer スコープ・自分を名指しする新しい出荷ルールへ委譲）で、生成される役割別 prompt block はバイト不変。サイズ警告閾値を超える 2 ブロックは理由を記録した上で受容された — 独立した全数走査の 2 回目が「書き込み時点の重複排除により退役対象は残らない」ことを確認した形 — 裁定の全文とエントリ全件の disposition 会計はレコードとしてリポジトリに同梱される。
- **v1.2.0**
  - **run をアニメーションとして再生できるようになった。** run telemetry に第二の行シェイプ — hand-off・rework・ゲート判定・人間の介入・リリースを記録する event 行 — が既存のエージェント別 span と並んで加わり、新しいゼロ依存のジェネレータが任意の run の行を自己完結の replay ページ（ネットワークアクセス不要・ローカルファイルから開ける）として描画する: グラウンドリング上のエージェント、board ステータスを運ぶ単一のボール、rework で赤くなるボール、カメラと再生コントロール、JA/EN 表示切替、ズームで現れるエージェント別詳細カード。本リリース以前に書かれた telemetry はどこでも有効なまま — lint も roll-up も不変で、欠けているものを自ら明示する正直な劣化ページとして描画される。
  - **replay の board ステータスレールが実データで点灯する。** label に board フラグトークンを単体で載せた event がレールを点灯させ、現在位置を進め、rework で赤くフラッシュし、再生位置を戻せば消灯する。フラグを載せない run はラベル付きの空状態表示を保つ。オーケストレータの telemetry 指示に対応する label 規約が加わった。
  - **リリース手順が、実行されたリリースの観測から文書化された。** コントリビュータガイドに Cutting-a-release 節が加わった — 統合ブランチを直接昇格させるとマージ時に自動削除される罠や、どの check にも捕まらない stale クローンの罠を含む、前リリースの観測手順そのもの。使い捨てリリースブランチ・分岐前 fetch・各 check が実際に見える範囲で分けた 2 段のローカル検証を処方する。隣接する 2 修正が同乗: board 衛生手順が実際の実行順で読めるようになり、close-out がタスクの記録の完備を確認してから board エントリを動かすようになった。
  - **出荷スクリプト 3 本の実行ビットが復元された**（replay ジェネレータと checker 2 本）。ドキュメントが記述する裸の PATH 呼び出しが再び動く。
  - **出荷 lessons corpus が実運用から成長した** — 新規 3 エントリ（スクリプト編集時の CI カバレッジ・close-out での記録検証・fail-closed ゲートへの境界受け入れ条件）と既存 2 エントリの拡張。役割別 prompt block はそこから再生成済み。
- **v1.1.0**
  - **出荷される prompt コンテンツが英語になり、注入 prompt が約 30% 小さくなった。** 生成される役割別 prompt block の背後にある構造化 lessons corpus を英語で再著述して移植し（74 エントリ・各エントリを記録済みの 4 分岐ルーティングルールで triage・公開 disposition ledger 付き）、4 つの役割別 block と consumer エージェントをすべて再生成した。adopter のエージェント prompt にメンテナの作業言語が注入されることはもう無い。CI が corpus の provenance を証明する: corpus は fail-closed でスキーマ検証され、出荷 block は pull request ごとに scratch root で再生成されコミット済みファイルと diff 比較される。
  - **retro 検証が言語中立になった。** retro checker は特定言語の装飾見出しの全行一致をやめ、5 つのセクションを言語中立マーカー（`<!-- retro-section: keep|problem|try|traps|lessons -->`）でアンカーする——見出しテキストはどの言語でも自由で、どの言語かを宣言するものは何も無い: 操作者の言語は設定されるのではなく、単に制約されなくなった。retro テンプレートは英語で出荷され、マーカー契約より前に書かれた retro の移行手順を文書化している（5 本のマーカー行の挿入のみ・既存の散文は無変更）——マーカー未挿入の retro は移行まで fail-closed で弾かれる。列挙された machine-token リスト（status flag・verdict ラベル・マーカー語彙・その他 grep される契約文字列）は、散文への信頼ではなく専用テストスイートで assert されるようになった。
  - **lessons ledger が Scope 型付きになり、出荷境界が機械強制になった。** すべての corpus エントリは fail-closed な `Scope`（`loop` / `maintainer`）を持ち、`maintainer` エントリはルールの拘束先の明示（形状検証付き）を義務付けられ、supersession は方向付き（loop エントリが maintainer ルールを黙って上書きすることはできない）で、生成される prompt block が `loop` エントリのみから作られることが両方向で証明される。
  - **lessons ファイルはハードコードではなく resolver 経由になった。** パス resolver に `lessons` キーが追加され、すべての consumer は legacy レイアウトを仮定する代わりに resolver に問い合わせる。
  - **残存 merge gate は「merge」という語ではなく帰結で発火する。** 人間の pledge のために停止すべき場面を教える例が、base ブランチでもキーワード一致でもなく「その merge が実行されるものを変えるか」で gate するようになり、それを不可逆操作に対する別個の停止と分離した。
  - **エージェントと contributor が誘導される 2 つの文書がツリーと矛盾しなくなった。** workflow 文書の規約節を adopter に真である形で再接地し（ブランチ命名形式の主張を排し・操作パスをレイアウトリテラルではなく resolver 形式で記述）、出荷 corpus エントリの CI に関する誤った現在形の主張をその場で修正した。
- **v1.0.0**
  - **`shell-team` へのリブランドと、最初の安定・公開準備リリース。** プラグイン・namespace・フラッグシップコマンドを改名——`claude-dev-team` → `shell-team`／`team-run` → `run`（`/shell-team:run` で起動）——し、リポジトリごとの footprint ディレクトリを `.team-run/` → `.shell-team/` へ移動。他のコマンド名（`team-init`・`goal`・`review`・`loop-triage`・`review-response`）は不変。
  - **単一の配布線。** v0.2 / v0.3 の並行配布を廃止: `shell-team` を `main` 上の単一リリース線として配布し、pin・切り替え・backport の対象となる別系統はない。
  - **1.0 は API 安定をコミット。** namespace とコマンド面が公開形に確定したため、本リリースでそれらを semantic versioning 下の安定した公開面として採用する（以後の破壊的変更は major を上げる）。
  - **挙動変更なし。** 純粋な識別子／ブランドの改名＋配布の単純化であり、チームループ・エージェント・チェック・contract は v0.3.0 と機能的に同一。
- **v0.3.0**
  - **Oversight モデル進化が GA に到達。** 完了ゲートの中核契約を「特定人間が生成コードを読み続ける」から「機械強制された接地ナレッジ＋独立 AI evaluator」へ移設し、人間は out-of-distribution（OOD）例外の回路遮断器として残す。
  - **全 5 段階、すべてこのラインで出荷**: この転換が守るべき新しい失敗モード（チームが機械速度で自分自身に同意してしまう）を命名する oversight doctrine。タスクの Goal・受入条件・入力空間・非ゴールを hash-pin し後の drift を fail-closed で検知可能にする凍結 intent 機構。非自明な実装判断に対する（決定・理由・接地引用）三つ組の決定 provenance 記録。何かが接地できないときだけ人間へエスカレーションする構造化 verdict を返す独立した drift/alignment evaluator（cross-provider・playbook 注入除外）。そして「人間よ答えよ」から retro を書くエージェントが直接エビデンスを読み自ら attest する形へ移した retro プロセス。
  - **3 つの譲れない土台を通して維持**: evaluator 独立性（別モデルプロバイダ・メインループの外）、capture の機械強制（凍結 intent と provenance は checker で必須化・願望ではない）、人間の例外パス（消さない）。
  - **autonomy は本リリースでも控え目に留める**: すべてのマージは今も人間の「go」を必要とする——evaluator はその判断を後押しするが、cross-provider の QA＋レビュー二重ゲートを置き換えるものではない。さらなる検証段階と低リスク auto-merge 段階は本リリースでは意図的に非スコープ。
  - **GA で配布が反転**: main ブランチは今や v0.3 系を追従する。旧 v0.2 系に留まりたい adopter は最終 tag か専用の maintenance ブランチを pin し、fix の方向は「v0.3 先行・v0.2 は maintenance-only」へ反転する。
- **v0.2.15**
  - **engineer→QA 継ぎ目の実装保全。** engineer は READY_FOR_QA 宣言前に実装を commit する義務を負い、QA は実 working tree への破壊的 git 操作（checkout／restore／hard reset／clean／stash、および実装ファイルを変更・削除・退避しうる操作全般）を禁止される——probe は捨て可能なコピーに限定。フルチームループと自己検証ランタイムループの両方が、この継ぎ目で未コミット差分を確認する二次ガードを得た（多層防御。実際のクロスリポインシデントで未コミット作業が失われた事案がきっかけ）。
  - **oversight doctrine 改訂。** 内部の「罠」文書を「人間の規律でしか防げない」から grounded AI evaluator モデルへ改訂し、新しい失敗モードを命名し、改訂済み design record を hash-pin して後の drift を検知可能にした。
  - **並行配布の設計スパイクの未決事項が決着し記録された** — drift evaluator がモデルプロバイダを共用しつつ呼び出しを分離し provider 中立な verdict 契約を保つ方法、凍結 intent のハッシュ化方法、決定 provenance の構造。
  - **changelog 分離**: リリース履歴を両 README から本ファイルと日本語版に移設（byte 照合済み）、README は方針要約とリンクのみを残す。
  - **lesson 数件が structured playbook へ昇格**、両 README に概念図が追加された。
  - **v0.2 系の並行メンテ枠が凍結**された（v0.3 開発が進む間）: 共有の統合ブランチに入るのは safety-class fix（データ損失／セキュリティ）のみ。
- **v0.2.14**
  - **fast-follow 記録の terminal disposition。** 前回レビューの follow-up 項目を決着（正味の fix 1 件＋理由付き won't-fix 3 件、いずれも該当 spec の disposition ledger に記録）し、reviewer の fast-follow 意図が issue へ転記されるか明示的に waive されるかを保証する両側の記録規律を追加。
  - **scaffolding 監査を retro 点検観点として導入。** モデル更新のあったサイクル限定で、旧モデルの弱さを補うために入れた制約が現行モデルの実力を隠していないかを retro が点検——propose-only、候補ゼロも正当な結果として扱う。
  - **living docs 陳腐化 sweep** がドキュメント一式の drift した参照を一括修正し、lesson 数件が structured playbook へさらに昇格。
  - **リリースチェックリストが機械チェック証跡を必須化**（各リリースの board エントリで散文の主張だけでなく）。
- **v0.2.13**
  - **自己検証ランタイムループのタスク適性自己申告。** README とループ自身の skill が「向く/向かないタスク」を明記——第一の分岐は「タスクの最終検証面がループ内で閉じるか、それとも最終的に人間の目が要るか（視覚/レイアウト出力等）」。視覚出力タスクには 1 回のフルループでなく短い手動サイクルの暫定運用。
  - **STOP 時の 3 択再ルーティング**: ランタイムループの guard が run を停止するとき、単純な延長/停止の 2 択でなく「仕様差し戻し／revert／延長」のいずれかを推奨するようになった。
  - **入力空間定義を spec に必須化**: すべての spec は守るべき入力空間を宣言し、QA / レビューの severity 判定はその宣言に接地する。
  - **2 系統の並行配布モデル**により、安定ラインと次期 pre-release ラインを単一プラグイン名の下で並行配布しつつ、インストールは相互排他のまま——adopter はどの marketplace ref で add したかによって、どちらか一方の系統だけを得た。
  - **QA の敵対的 fixture 合成チェックリスト**を導入——破損/敵対入力・語彙衝突とデリミタ・fence 境界・起動形状網羅を対象とし、engineer にも対になる先回りチェックリストを追加。
  - **lesson 数件が structured playbook へ昇格。**
- **v0.2.12**
  - **全ての非保護サイトで dangling-symlink write-through を遮断。** リポ scaffolding とロールアップスクリプト群の占有/衝突判定を統一し、dangling symlink を占有扱いにし、強制上書きは常に書き込み前に symlink を除去して内容が正しい場所に着地するようにした。
  - **lesson 数件が structured playbook へ昇格**、既定 non-worktree engineer が feature ブランチへ直接コミットする挙動を実行時証跡で確認し、以前のタスクの未決着 AC を close した。
- **v0.2.11**
  - **engineer の isolation 既定を反転。** 既定は non-worktree（編集が現 feature ブランチに直接着地）となり、worktree 隔離は並列実装限定の明示 opt-in になった。
  - **spec 完成セルフチェック。** spec 書き手は本文の規範的記述をすべて受入条件へ 1:1 昇格させる義務を負い、対応表と理由付き除外を伴う。
  - **レビュー verdict 契約の強化**: severity レベルを正規化し、severity の変更/却下はすべて監査可能な ledger に記録され、verdict は静的判定か実行テストに基づくかを明示するようになった。
  - **STOP 時の rework 履歴ダイジェスト**: run が停止したとき、ラウンド別の失敗履歴を整形するヘルパーが、延長/停止の判断を勘でなく根拠に基づかせる。
  - **same-class inventory の精緻化**: 同一根本原因が繰り返し見つかったとき、修正インベントリは 1 ファイル限定でなく repo 全体・cross-file でなければならない。
  - **per-repo テストレシピ**: リポ scaffolding にローカルテスト実行手順を記す append-only ファイルが加わり、engineer と QA が毎回それを再導出しなくて済むようになった。
- **v0.2.10**
  - **install 経路と CI の堅牢化。** vendoring フォールバックスクリプトを現行のソースレイアウトに追従させ、そのテストスイートを CI に配線——これが実際に、テストハーネス自体の BSD↔GNU 非可搬性バグを即座に捕捉した。
  - **モデル tier スキームの明文化。** 判断層のエージェントを同一モデル tier に揃え、プロジェクトの 3-tier モデル割当スキーム（計画層・実行層・cross-provider evaluator）と明示的な再評価トリガを文書化した。
  - **受入条件 dry-run を fail-closed 化**し、不正な形式の受入条件チェックが黙って skip されず捕捉されるようにした。
  - **v0.3.0 oversight モデルの design note が着地**し、次期メジャーラインの「人間 comprehension ベースの安全 → 接地済み独立 AI evaluator」への転換を根拠づけた。
- **v0.2.9**
  - **跨-run failure clustering。** ヘルパーが多数のテレメトリ run にわたる失敗パターンをランク付き正規化シグネチャへ集約——系統的な問題が 1 run ずつしか見えなかった既知のギャップを解消。
  - **structured かつ機械検証済みの lessons playbook。** lessons ファイルが機械可読スキーマを獲得し、generator が役割別のプロンプトダイジェストをそこから導出し、validator が fail-closed でスキーマを検証し、更新は人間ゲート付きの promotion スクリプト経由でのみ行われる——直編集は決してしない。
  - **実運用からのリワーク規律の逆流**: レビューが同一根本原因クラスを 2 件見つけたとき、リワーク指示は個別修正からクラス全数インベントリ掃討へ切り替わり、QA / レビューの起動指示は使っている検出面（実測実行 vs 静的/境界検算）を明示する。
- **v0.2.8**
  - **Codex レビューのファイル読取経路を正典化。** cross-provider reviewer は撤回された inline 貼付ワークアラウンド（前提が陳腐化と実証された）ではなく、文書化されたサンドボックスコマンドでブランチ diff を直接読むようになった。
  - **outer-loop の発火点を配線。** テレメトリログはその linter へ自動連鎖し、retro ステップはリリースチェックリストの明示項目になり、非実行のスケジューリング例が追加され、ヘルパーが各 run のロールアップを git 追跡 artifact として、書き込み時の個人情報混入ガード付きで捕捉するようになった。
  - **ロールアップ guard を強化**し、Windows 形式のホームディレクトリパスと一般的な secret 形状トークンも拒否するようにした（各々最小長で anchor し、無関係な短い文字列を誤検知しない）。
- **v0.2.7**
  - **board/issue クローズアウト自動化。** スクリプトが board 更新と対応する issue クローズ手順を出力し、project-status スナップショットのマーカ限定領域のみを再生成する。
  - **エージェントプロンプトの正典化と drift チェック。** 共有プロンプトブロックを 1 箇所に集約し、checker が各エージェント定義のバイト単位同期を検証する。
  - **always-on の慎重実行規律**を主要エージェント定義に焼き込んだ: 検証可能な継ぎ目での分解・観測エビデンスを伴う完了主張・前進/停滞/後退の 3 分類と 2 ストライク再計画・不確実性の明示。
- **v0.2.6**
  - **PR に返ってきたレビュー指摘に対応する review-response ワークフロー。** 変更要求相当のコメントを検出し、cross-provider reviewer に各指摘を評価させ、ハイブリッドゲートでリスクゲートする——決定論フロアが最もリスクの高い指摘を人間確認へ直送し、グレーゾーンはモデルが判断する——したうえで採用分をフルチームループへ単一 spec として渡す。
  - **各ゲート判定が記録される**ため retro プロセスが学習できる。
- **v0.2.5**
  - **モデル tier 再編。** 呼び出し量の最も多いエージェント役割（engineer・spec 書き手・UI designer）が重い判断層モデルから軽い実行層モデルへ移行し、大幅なコスト削減とほぼ同等の品質を両立。計画/ルーティング役割は重い tier に留まった。
  - **レビュー・triage・retro・QA の役割はすでに軽い tier だった**ため、本リリースでチーム全体の意図したコスト/品質分離が完成した。
- **v0.2.4**
  - **outer-loop 成熟の完遂。** triage 統合エージェントとスクリプトが discovery スキャンとテレメトリ escalation を単一の重複排除済み・budget 上限付き・propose-only な triage ファイルへ統合——ボード自体は決して編集しない。
  - **host-only スケジューリングアダプタ。** 時間駆動のトリガ種別を、非有効な例示ラッパーと移植性の注意書きとともに文書化。手動トリガは常に使える fallback のまま。
- **v0.2.3**
  - **チーム出力がユーザーの会話言語にミラー**するようになった（ゼロ config）。機械解析トークンは英語 verbatim を維持し、ハンドオフパイプラインが引き続き正しくパースできる。
  - **cross-repo な受入条件バグを修正**: `check:` コマンドが呼び出し元の working directory（または明示的なターゲットディレクトリ）で実行されるようになり、常にプラグイン自身のリポジトリで実行される挙動をやめた——自己検証ランタイムループを別リポで dogfood して初めて表面化したバグ。
- **v0.2.2**
  - **design-note ゲートバリデータ**が、UI デザインのハンドオフに対する存在チェックのプレースホルダを、実質的な構造検証＋陳腐化検知に置き換えた。
  - **自己検証ランタイムループ本体が出荷。** 以前の実現可能性スパイクの wiring を実装した新スキル: フルチームループと同じ暴走ガードで bound された self-paced ケイデンス・階層化された完了ゲート（決定的チェック → QA → cross-provider レビュー）・cross-tick state（経過時間・イテレーション数・正規化された失敗シグネチャ）を追うヘルパー。end-to-end のループ実行は CI でなく dogfood で検証。
- **v0.2.1**
  - **v0.2.0 基線上の最初の機能リリース。** Loop Engineering 深掘り: Loop 契約のメタデータフィールド・3 つの運用上の「罠」を明示的な規律へ転換・cross-tool state ファイル・自己検証ランタイムループの実現可能性スパイク。
  - **条件付き UI Designer エージェント**が、利用可能なときは任意のデザイン指針スキルを使い、UI 作業時のみチームに参加する。
  - **リリース前のドキュメント整合性スイープ**がこのサイクルを締めた。
- **v0.2.0**
  - **痕跡の集約（安定基線）。** per-repo の運用ファイルすべてを単一の設定可能な base directory へ集約し、リポ scaffolding が host リポ自身の `CLAUDE.md` やルート `.gitignore` を改変しなくなった。使い捨てリポでの dogfood で検証済。
  - **これが v0.2.x ラインの基線となり**、より広い Loop Engineering の概念に基づくプリミティブ（Loop 契約メタデータ・3 つの罠の運用規律化・cross-tool state ファイル・自己検証ランタイムループ）で深掘りを続けた。
  - **このバージョンで新しい public repo へ移行する従来計画は中止された** — repo は private のまま維持し、このサイクルの労力は Loop Engineering の深化に充てられた。
- **v0.1.0 以降**
  - **プラグイン + Loop Engineering ラインが始まる。** プロジェクトはファイルコピー配布から、一度インストールすれば全リポで使える正式な Claude Code プラグインへ移行した。
  - **この境界では破壊的変更が許容される** — プラグイン化前の v0.0.x ベースラインと v0.1.x プラグインラインは互換を意図していない。
- **v0.0.1**
  - **プラグイン化前のベースライン。** スナップショットコピーの install スクリプトで配布していた 5 エージェントの単発パスパイプライン。
  - **プラグインもマーケットプレイスも運用ループも無し** — あるのはエージェント間のハンドオフ契約だけで、それ以降すべてがその上に積み上げられた出発点。
